Stores instructions as `.ralph/<session_id>.md` with `set ralph`. Used by Stop hooks to prevent Claude from stopping while work remains. When a Stop hook fires and an instruction is active, `check ralph` returns `"decision": "block"`. Call `clear ralph` when work is done to release the block.

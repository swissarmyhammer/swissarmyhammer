//! The fail fixture for `no-commented-code-parsed`.
//!
//! It holds one commented-out function of seven lines. The rule must report it.

// fn folded_band(grid: &[Vec<i32>], limit: i32) -> i32 {
//     let mut band = 0;
//     for row in grid {
//         band += row.iter().filter(|cell| **cell < limit).count() as i32;
//     }
//     band
// }

/// Reads the band a caller asked for.
pub fn band(limit: i32) -> i32 {
    limit
}

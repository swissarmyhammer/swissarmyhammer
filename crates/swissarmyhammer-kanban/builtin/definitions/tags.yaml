id: "00000000000000000000000004"
name: tags
description: Tags derived from tag patterns in body
type:
  kind: computed
  derive: parse-body-tags
  entity: tag
  commit_display_names: true
  # `tags` is derived purely from `#tag` mentions in the body, so it depends
  # on `body`. This drives two things: the derived-output cache invalidates
  # when the body changes, and the write-path field-change diff recomputes
  # `tags` when `body` changes so the UI re-renders the field after an edit.
  depends_on:
    - body
icon: tag
editor: multi-select
display: badge-list
width: 200
section: header
placeholder: "Add tags"
groupable: true

id: "00000000000000000000000004"
name: tags
description: Tags derived from tag patterns in body
type:
  kind: computed
  derive: parse-body-tags
  entity: tag
  commit_display_names: true
  # Declare the cross-entity dependency so any tag mutation invalidates
  # the cached `tags` values on all tasks — this is what the derived-output
  # cache consults via `FieldsContext::entity_types_depending_on`.
  depends_on:
    - tag
icon: tag
editor: multi-select
display: badge-list
width: 200
section: header
placeholder: "Add tags"
groupable: true

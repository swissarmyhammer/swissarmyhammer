---
position_column: todo
position_ordinal: c4
title: Wire unified dispatchers into grid and inspector
---
Replace the ad-hoc dispatch logic in both grid (CellDispatch/CellEditor) and inspector (FieldDispatch) with calls to the unified FieldDisplay and FieldEditor dispatchers.

Files to modify:
- `ui/src/components/cells/cell-dispatch.tsx` → becomes a thin wrapper calling `FieldDisplay` with mode="compact"
- `ui/src/components/cells/cell-editor.tsx` → becomes a thin wrapper calling `FieldEditor` with mode="compact"
- `ui/src/components/entity-inspector.tsx` → FieldDispatch uses `FieldDisplay` (mode="full") for read and `FieldEditor` (mode="full") for edit
- `ui/src/components/field-renderer.tsx` → uses FieldDisplay/FieldEditor instead of FieldPlaceholder directly

- [ ] Refactor CellDispatch to delegate to FieldDisplay(mode="compact")
- [ ] Refactor CellEditor to delegate to FieldEditor(mode="compact")
- [ ] Refactor inspector FieldDispatch to use FieldDisplay(mode="full") and FieldEditor(mode="full")
- [ ] Refactor FieldRenderer to use the unified dispatchers
- [ ] Delete redundant cell-specific files that are now fully superseded
- [ ] Verify grid view still works: navigation, display, inline editing
- [ ] Verify inspector still works: all field types display and edit correctly
- [ ] Verify board view cards still render fields correctly
- [ ] Run tests
{"id":"01KKNZY7SYDE1N66Y5BA7QM5BG","timestamp":"2026-03-14T10:58:11.390272Z","entity_type":"task","entity_id":"01KKNZY7SXWKS2ZAPYCSERRMHD","op":"create","changes":[["body",{"kind":"set","value":"## What\nMove the `execute_operation` function from `swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs:382-804` into `swissarmyhammer-kanban` as a shared public function. This eliminates dispatch duplication between the MCP tool and the new kanban-cli.\n\n**Create:** `swissarmyhammer-kanban/src/dispatch.rs`\n- `pub async fn dispatch_operation(ctx: &KanbanContext, op: &Operation) -> Result<Value, KanbanError>`\n- Same match logic as existing `execute_operation`, but returns `KanbanError` instead of `McpError`\n- Convert `McpError::invalid_params(\"missing required field: X\")` → `KanbanError::MissingField(\"X\")` (or use `op.require_string()` which already returns `Result<&str, String>`)\n\n**Modify:** `swissarmyhammer-kanban/src/lib.rs`\n- Add `pub mod dispatch;`\n- Re-export: `pub use dispatch::dispatch_operation;`\n\n**Modify:** `swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs`\n- Replace inline `execute_operation` with a thin wrapper that calls `swissarmyhammer_kanban::dispatch_operation()` and maps `KanbanError` → `McpError`\n\n## Acceptance Criteria\n- [ ] `dispatch_operation` lives in `swissarmyhammer-kanban` crate, usable without MCP dependencies\n- [ ] MCP kanban tool still works identically (passes all existing tests)\n- [ ] `KanbanError` variant covers \"missing required field\" cases (or reuse existing variant)\n\n## Tests\n- [ ] Existing MCP kanban tool tests pass unchanged: `cargo nextest run -p swissarmyhammer-tools -- kanban`\n- [ ] New unit test in dispatch.rs: init board + add task + list tasks round-trip\n- [ ] `cargo nextest run -p swissarmyhammer-kanban` passes"}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"a9"}],["title",{"kind":"set","value":"Extract dispatch_operation into swissarmyhammer-kanban crate"}]],"transaction_id":"01KKNZY7SJCRSJ00CD67YF0PYD"}
{"id":"01KKP085VADZD5MBZ3HD28P5D5","timestamp":"2026-03-14T11:03:37.066521Z","entity_type":"task","entity_id":"01KKNZY7SXWKS2ZAPYCSERRMHD","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,24 +1 @@\n-## What\n-Move the `execute_operation` function from `swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs:382-804` into `swissarmyhammer-kanban` as a shared public function. This eliminates dispatch duplication between the MCP tool and the new kanban-cli.\n-\n-**Create:** `swissarmyhammer-kanban/src/dispatch.rs`\n-- `pub async fn dispatch_operation(ctx: &KanbanContext, op: &Operation) -> Result<Value, KanbanError>`\n-- Same match logic as existing `execute_operation`, but returns `KanbanError` instead of `McpError`\n-- Convert `McpError::invalid_params(\"missing required field: X\")` → `KanbanError::MissingField(\"X\")` (or use `op.require_string()` which already returns `Result<&str, String>`)\n-\n-**Modify:** `swissarmyhammer-kanban/src/lib.rs`\n-- Add `pub mod dispatch;`\n-- Re-export: `pub use dispatch::dispatch_operation;`\n-\n-**Modify:** `swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs`\n-- Replace inline `execute_operation` with a thin wrapper that calls `swissarmyhammer_kanban::dispatch_operation()` and maps `KanbanError` → `McpError`\n-\n-## Acceptance Criteria\n-- [ ] `dispatch_operation` lives in `swissarmyhammer-kanban` crate, usable without MCP dependencies\n-- [ ] MCP kanban tool still works identically (passes all existing tests)\n-- [ ] `KanbanError` variant covers \"missing required field\" cases (or reuse existing variant)\n-\n-## Tests\n-- [ ] Existing MCP kanban tool tests pass unchanged: `cargo nextest run -p swissarmyhammer-tools -- kanban`\n-- [ ] New unit test in dispatch.rs: init board + add task + list tasks round-trip\n-- [ ] `cargo nextest run -p swissarmyhammer-kanban` passes\n\\ No newline at end of file\n+## What\\nAdd a generic `OperationRegistry<C, E>` to `swissarmyhammer-operations` that maps `(verb, noun)` → deserialize + execute. Then build a kanban-specific registry in `swissarmyhammer-kanban`. This eliminates the ~420-line dispatch match block in the MCP tool and prevents duplication in kanban-cli.\\n\\n### Part A: Generic registry in `swissarmyhammer-operations`\\n\\n**Create:** `swissarmyhammer-operations/src/registry.rs`\\n- `OperationRegistry<C, E>` with a `HashMap<(&str, &str), BoxedHandler>`\\n- `register<T: DeserializeOwned + Execute<C, E>>()` — standard registration: deserializes params JSON into `T`, calls `processor.process(&t, ctx)`\\n- `register_with_hook<T, F>(hook: F)` — registration with a pre-processing closure that can modify params before deserialization (for the 3 custom cases)\\n- `dispatch(ctx, processor, verb, noun, params) -> Result<Value, E>` — look up handler, run it\\n- Also expose `operation_metadata()` returning `&dyn Operation` instances for CLI generation\\n\\n**Modify:** `swissarmyhammer-operations/src/lib.rs` — add `pub mod registry;`, re-export\\n\\n### Part B: Kanban registry in `swissarmyhammer-kanban`\\n\\n**Create:** `swissarmyhammer-kanban/src/dispatch.rs`\\n- `pub fn kanban_registry() -> OperationRegistry<KanbanContext, KanbanError>`\\n- Registers all ~30 operations using plain `register` \\n- Registers `AddTask`, `AddTag`, `AddActor` with hooks for: auto-assign actor, name/id fallback, ensure flag\\n- `pub async fn dispatch_operation(ctx, op) -> Result<Value, KanbanError>` convenience function\\n\\n**Modify:** `swissarmyhammer-kanban/src/lib.rs` — add `pub mod dispatch;`, re-export\\n\\n### Part C: Wire up MCP tool\\n\\n**Modify:** `swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs`\\n- Replace the 420-line `execute_operation` match with a call to `kanban_registry().dispatch()`\\n- Keep the plan notification logic and actor injection as pre-processing\\n\\n## Acceptance Criteria\\n- [ ] `OperationRegistry` is generic and lives in `swissarmyhammer-operations` (no kanban dependency)\\n- [ ] All ~30 kanban operations registered, including 3 with hooks\\n- [ ] MCP kanban tool uses the registry — no more manual dispatch match\\n- [ ] All existing MCP kanban tests pass unchanged\\n\\n## Tests\\n- [ ] Unit test in `swissarmyhammer-operations`: registry with test operations, dispatch round-trip\\n- [ ] Unit test in `swissarmyhammer-kanban/dispatch.rs`: init board + add task + list via registry\\n- [ ] `cargo nextest run -p swissarmyhammer-operations` passes\\n- [ ] `cargo nextest run -p swissarmyhammer-tools -- kanban` passes\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1,24 @@\n-## What\\nAdd a generic `OperationRegistry<C, E>` to `swissarmyhammer-operations` that maps `(verb, noun)` → deserialize + execute. Then build a kanban-specific registry in `swissarmyhammer-kanban`. This eliminates the ~420-line dispatch match block in the MCP tool and prevents duplication in kanban-cli.\\n\\n### Part A: Generic registry in `swissarmyhammer-operations`\\n\\n**Create:** `swissarmyhammer-operations/src/registry.rs`\\n- `OperationRegistry<C, E>` with a `HashMap<(&str, &str), BoxedHandler>`\\n- `register<T: DeserializeOwned + Execute<C, E>>()` — standard registration: deserializes params JSON into `T`, calls `processor.process(&t, ctx)`\\n- `register_with_hook<T, F>(hook: F)` — registration with a pre-processing closure that can modify params before deserialization (for the 3 custom cases)\\n- `dispatch(ctx, processor, verb, noun, params) -> Result<Value, E>` — look up handler, run it\\n- Also expose `operation_metadata()` returning `&dyn Operation` instances for CLI generation\\n\\n**Modify:** `swissarmyhammer-operations/src/lib.rs` — add `pub mod registry;`, re-export\\n\\n### Part B: Kanban registry in `swissarmyhammer-kanban`\\n\\n**Create:** `swissarmyhammer-kanban/src/dispatch.rs`\\n- `pub fn kanban_registry() -> OperationRegistry<KanbanContext, KanbanError>`\\n- Registers all ~30 operations using plain `register` \\n- Registers `AddTask`, `AddTag`, `AddActor` with hooks for: auto-assign actor, name/id fallback, ensure flag\\n- `pub async fn dispatch_operation(ctx, op) -> Result<Value, KanbanError>` convenience function\\n\\n**Modify:** `swissarmyhammer-kanban/src/lib.rs` — add `pub mod dispatch;`, re-export\\n\\n### Part C: Wire up MCP tool\\n\\n**Modify:** `swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs`\\n- Replace the 420-line `execute_operation` match with a call to `kanban_registry().dispatch()`\\n- Keep the plan notification logic and actor injection as pre-processing\\n\\n## Acceptance Criteria\\n- [ ] `OperationRegistry` is generic and lives in `swissarmyhammer-operations` (no kanban dependency)\\n- [ ] All ~30 kanban operations registered, including 3 with hooks\\n- [ ] MCP kanban tool uses the registry — no more manual dispatch match\\n- [ ] All existing MCP kanban tests pass unchanged\\n\\n## Tests\\n- [ ] Unit test in `swissarmyhammer-operations`: registry with test operations, dispatch round-trip\\n- [ ] Unit test in `swissarmyhammer-kanban/dispatch.rs`: init board + add task + list via registry\\n- [ ] `cargo nextest run -p swissarmyhammer-operations` passes\\n- [ ] `cargo nextest run -p swissarmyhammer-tools -- kanban` passes\n\\ No newline at end of file\n+## What\n+Move the `execute_operation` function from `swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs:382-804` into `swissarmyhammer-kanban` as a shared public function. This eliminates dispatch duplication between the MCP tool and the new kanban-cli.\n+\n+**Create:** `swissarmyhammer-kanban/src/dispatch.rs`\n+- `pub async fn dispatch_operation(ctx: &KanbanContext, op: &Operation) -> Result<Value, KanbanError>`\n+- Same match logic as existing `execute_operation`, but returns `KanbanError` instead of `McpError`\n+- Convert `McpError::invalid_params(\"missing required field: X\")` → `KanbanError::MissingField(\"X\")` (or use `op.require_string()` which already returns `Result<&str, String>`)\n+\n+**Modify:** `swissarmyhammer-kanban/src/lib.rs`\n+- Add `pub mod dispatch;`\n+- Re-export: `pub use dispatch::dispatch_operation;`\n+\n+**Modify:** `swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs`\n+- Replace inline `execute_operation` with a thin wrapper that calls `swissarmyhammer_kanban::dispatch_operation()` and maps `KanbanError` → `McpError`\n+\n+## Acceptance Criteria\n+- [ ] `dispatch_operation` lives in `swissarmyhammer-kanban` crate, usable without MCP dependencies\n+- [ ] MCP kanban tool still works identically (passes all existing tests)\n+- [ ] `KanbanError` variant covers \"missing required field\" cases (or reuse existing variant)\n+\n+## Tests\n+- [ ] Existing MCP kanban tool tests pass unchanged: `cargo nextest run -p swissarmyhammer-tools -- kanban`\n+- [ ] New unit test in dispatch.rs: init board + add task + list tasks round-trip\n+- [ ] `cargo nextest run -p swissarmyhammer-kanban` passes\n\\ No newline at end of file\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Extract dispatch_operation into swissarmyhammer-kanban crate\n\\ No newline at end of file\n+Add OperationRegistry to swissarmyhammer-operations + build kanban registry\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Add OperationRegistry to swissarmyhammer-operations + build kanban registry\n\\ No newline at end of file\n+Extract dispatch_operation into swissarmyhammer-kanban crate\n\\ No newline at end of file\n"}]],"transaction_id":"01KKP085V0DZM83DNA7QY9B8BE"}
{"id":"01KKP0E6YEF7FR1AJBA7RYGA7A","timestamp":"2026-03-14T11:06:54.798669Z","entity_type":"task","entity_id":"01KKNZY7SXWKS2ZAPYCSERRMHD","op":"delete","changes":[["body",{"kind":"removed","old_value":"## What\\nAdd a generic `OperationRegistry<C, E>` to `swissarmyhammer-operations` that maps `(verb, noun)` → deserialize + execute. Then build a kanban-specific registry in `swissarmyhammer-kanban`. This eliminates the ~420-line dispatch match block in the MCP tool and prevents duplication in kanban-cli.\\n\\n### Part A: Generic registry in `swissarmyhammer-operations`\\n\\n**Create:** `swissarmyhammer-operations/src/registry.rs`\\n- `OperationRegistry<C, E>` with a `HashMap<(&str, &str), BoxedHandler>`\\n- `register<T: DeserializeOwned + Execute<C, E>>()` — standard registration: deserializes params JSON into `T`, calls `processor.process(&t, ctx)`\\n- `register_with_hook<T, F>(hook: F)` — registration with a pre-processing closure that can modify params before deserialization (for the 3 custom cases)\\n- `dispatch(ctx, processor, verb, noun, params) -> Result<Value, E>` — look up handler, run it\\n- Also expose `operation_metadata()` returning `&dyn Operation` instances for CLI generation\\n\\n**Modify:** `swissarmyhammer-operations/src/lib.rs` — add `pub mod registry;`, re-export\\n\\n### Part B: Kanban registry in `swissarmyhammer-kanban`\\n\\n**Create:** `swissarmyhammer-kanban/src/dispatch.rs`\\n- `pub fn kanban_registry() -> OperationRegistry<KanbanContext, KanbanError>`\\n- Registers all ~30 operations using plain `register` \\n- Registers `AddTask`, `AddTag`, `AddActor` with hooks for: auto-assign actor, name/id fallback, ensure flag\\n- `pub async fn dispatch_operation(ctx, op) -> Result<Value, KanbanError>` convenience function\\n\\n**Modify:** `swissarmyhammer-kanban/src/lib.rs` — add `pub mod dispatch;`, re-export\\n\\n### Part C: Wire up MCP tool\\n\\n**Modify:** `swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs`\\n- Replace the 420-line `execute_operation` match with a call to `kanban_registry().dispatch()`\\n- Keep the plan notification logic and actor injection as pre-processing\\n\\n## Acceptance Criteria\\n- [ ] `OperationRegistry` is generic and lives in `swissarmyhammer-operations` (no kanban dependency)\\n- [ ] All ~30 kanban operations registered, including 3 with hooks\\n- [ ] MCP kanban tool uses the registry — no more manual dispatch match\\n- [ ] All existing MCP kanban tests pass unchanged\\n\\n## Tests\\n- [ ] Unit test in `swissarmyhammer-operations`: registry with test operations, dispatch round-trip\\n- [ ] Unit test in `swissarmyhammer-kanban/dispatch.rs`: init board + add task + list via registry\\n- [ ] `cargo nextest run -p swissarmyhammer-operations` passes\\n- [ ] `cargo nextest run -p swissarmyhammer-tools -- kanban` passes"}],["position_column",{"kind":"removed","old_value":"todo"}],["position_ordinal",{"kind":"removed","old_value":"a9"}],["title",{"kind":"removed","old_value":"Add OperationRegistry to swissarmyhammer-operations + build kanban registry"}]],"transaction_id":"01KKP0E6Y2ZV84QS9QG0AER1NT"}

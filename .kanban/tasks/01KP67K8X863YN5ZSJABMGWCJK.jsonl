{"id":"01KP67K8X9R9S0WYRACTBVJZM8","timestamp":"2026-04-14T14:51:15.241761Z","op":"create","item_id":"01KP67K8X863YN5ZSJABMGWCJK","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,61 @@\n+---\n+assignees: []\n+position_column: todo\n+position_ordinal: cc80\n+project: pill-via-cm6\n+title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n+---\n+## What\n+\n+**Bug repro**: In the perspective formula bar, type `#blo`, pause until the autocomplete dropdown shows `#BLOCKING`, press Enter to accept. The editor visually shows `#BLOCKING` (correct). Then toggle to another perspective and back — the saved filter is `#blo`, which matches nothing.\n+\n+**Root cause** — a race between the 300 ms debounced autosave and perspective remount:\n+\n+1. `FilterFormulaBar` is keyed by `activePerspective.id` (`kanban-app/ui/src/components/perspective-tab-bar.tsx` near line 219), so switching perspectives **unmounts** `FilterEditor`.\n+2. `useFilterDispatch` in `kanban-app/ui/src/components/filter-editor.tsx` autosaves via `useDebouncedTimer` with a 300 ms delay (`AUTOSAVE_DELAY_MS`).\n+3. `useDebouncedTimer` cleanup (`useEffect(() => cancel, [cancel])`) **cancels** any pending timer on unmount — it does not flush it.\n+4. Timing that triggers the bug:\n+   - `t=0…300`: user types `#blo`. Each keystroke restarts the debounce.\n+   - `t~600`: debounce fires with `#blo` → `perspective.filter { filter: \"#blo\" }` is persisted while the user is still reading the dropdown.\n+   - `t~700`: user presses Enter. Autocomplete's `apply: \"#${slug}\"` (`kanban-app/ui/src/lib/cm-mention-autocomplete.ts` line 61) replaces the doc text with `#BLOCKING`. `updateListener` fires → `handleChange` → debounce restarts, scheduled for `t~1000`.\n+   - `t~800`: user toggles perspective. `FilterFormulaBar` remounts → `useDebouncedTimer` cleanup cancels the `t~1000` save → `#BLOCKING` is never persisted. The already-saved `#blo` stays.\n+\n+The visual correctness is real: `findMentionsInText` (`kanban-app/ui/src/lib/mention-finder.ts`) does exact slug matching, so the pill only renders when the doc text is exactly `#BLOCKING`. The editor state IS `#BLOCKING` post-acceptance — we just never save it.\n+\n+### Fix approach\n+\n+Change `useDebouncedTimer` so its unmount cleanup **flushes** the pending callback synchronously instead of dropping it. This preserves any in-flight autosave through remount races.\n+\n+- [ ] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n+  - Store the pending callback alongside the timer (new `pendingFnRef`).\n+  - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback immediately.\n+  - Change the unmount effect from `useEffect(() => cancel, [cancel])` to `useEffect(() => flush, [flush])`.\n+  - Keep `cancel` available for the clear-button path (`handleClear` must still drop, not flush, because clear supersedes any pending save).\n+  - Return `{ schedule, cancel, flush }` from the hook.\n+- [ ] Verify `handleCommit` (Enter key) already dispatches immediately — it does, via `cancel()` + `apply(text)` — so no change needed there. Autocomplete acceptance already goes through `handleChange` → debounce; flush-on-unmount covers it.\n+- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`. Blur within the same perspective keeps scheduling (no unmount, no race). This card is specifically the unmount race.\n+\n+### Out of scope\n+\n+- Reworking autocomplete acceptance to dispatch immediately (viable alternative, but more invasive — touches `cm-mention-autocomplete.ts` and the per-source `apply` composition).\n+- Shortening `AUTOSAVE_DELAY_MS` (doesn't fix the race).\n+- Flushing on blur (separate concern — user didn't report a blur-related loss).\n+\n+## Acceptance Criteria\n+\n+- [ ] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n+- [ ] The existing debounced-autosave behavior still works within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n+- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale `#blo` after a clear).\n+- [ ] Enter-to-commit still dispatches immediately (no regression in the \"Enter key dispatches perspective.filter immediately\" test).\n+\n+## Tests\n+\n+- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes pending autosave on unmount\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`, dispatch a doc change that inserts `#BLOCKING`, unmount the component **before** the 300 ms debounce elapses, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }`.\n+- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button (which calls `cancel()` + `dispatchClearFilter`), then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button click).\n+- [ ] Add a test simulating the autocomplete-then-remount race: dispatch a doc change to `#blo`, wait 400 ms (lets the `#blo` save fire), dispatch another doc change to `#BLOCKING`, then unmount within 100 ms (well under 300 ms). Assert the final `mockInvoke` call for `perspective.filter` was with `#BLOCKING`.\n+- [ ] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n+- [ ] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n+\n+## Workflow\n+\n+- Use `/tdd` — write the three new tests first (they should fail against the current `cancel`-on-unmount behavior), then change `useDebouncedTimer` to flush on unmount so they pass.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,61 +0,0 @@\n----\n-assignees: []\n-position_column: todo\n-position_ordinal: cc80\n-project: pill-via-cm6\n-title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n----\n-## What\n-\n-**Bug repro**: In the perspective formula bar, type `#blo`, pause until the autocomplete dropdown shows `#BLOCKING`, press Enter to accept. The editor visually shows `#BLOCKING` (correct). Then toggle to another perspective and back — the saved filter is `#blo`, which matches nothing.\n-\n-**Root cause** — a race between the 300 ms debounced autosave and perspective remount:\n-\n-1. `FilterFormulaBar` is keyed by `activePerspective.id` (`kanban-app/ui/src/components/perspective-tab-bar.tsx` near line 219), so switching perspectives **unmounts** `FilterEditor`.\n-2. `useFilterDispatch` in `kanban-app/ui/src/components/filter-editor.tsx` autosaves via `useDebouncedTimer` with a 300 ms delay (`AUTOSAVE_DELAY_MS`).\n-3. `useDebouncedTimer` cleanup (`useEffect(() => cancel, [cancel])`) **cancels** any pending timer on unmount — it does not flush it.\n-4. Timing that triggers the bug:\n-   - `t=0…300`: user types `#blo`. Each keystroke restarts the debounce.\n-   - `t~600`: debounce fires with `#blo` → `perspective.filter { filter: \"#blo\" }` is persisted while the user is still reading the dropdown.\n-   - `t~700`: user presses Enter. Autocomplete's `apply: \"#${slug}\"` (`kanban-app/ui/src/lib/cm-mention-autocomplete.ts` line 61) replaces the doc text with `#BLOCKING`. `updateListener` fires → `handleChange` → debounce restarts, scheduled for `t~1000`.\n-   - `t~800`: user toggles perspective. `FilterFormulaBar` remounts → `useDebouncedTimer` cleanup cancels the `t~1000` save → `#BLOCKING` is never persisted. The already-saved `#blo` stays.\n-\n-The visual correctness is real: `findMentionsInText` (`kanban-app/ui/src/lib/mention-finder.ts`) does exact slug matching, so the pill only renders when the doc text is exactly `#BLOCKING`. The editor state IS `#BLOCKING` post-acceptance — we just never save it.\n-\n-### Fix approach\n-\n-Change `useDebouncedTimer` so its unmount cleanup **flushes** the pending callback synchronously instead of dropping it. This preserves any in-flight autosave through remount races.\n-\n-- [ ] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n-  - Store the pending callback alongside the timer (new `pendingFnRef`).\n-  - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback immediately.\n-  - Change the unmount effect from `useEffect(() => cancel, [cancel])` to `useEffect(() => flush, [flush])`.\n-  - Keep `cancel` available for the clear-button path (`handleClear` must still drop, not flush, because clear supersedes any pending save).\n-  - Return `{ schedule, cancel, flush }` from the hook.\n-- [ ] Verify `handleCommit` (Enter key) already dispatches immediately — it does, via `cancel()` + `apply(text)` — so no change needed there. Autocomplete acceptance already goes through `handleChange` → debounce; flush-on-unmount covers it.\n-- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`. Blur within the same perspective keeps scheduling (no unmount, no race). This card is specifically the unmount race.\n-\n-### Out of scope\n-\n-- Reworking autocomplete acceptance to dispatch immediately (viable alternative, but more invasive — touches `cm-mention-autocomplete.ts` and the per-source `apply` composition).\n-- Shortening `AUTOSAVE_DELAY_MS` (doesn't fix the race).\n-- Flushing on blur (separate concern — user didn't report a blur-related loss).\n-\n-## Acceptance Criteria\n-\n-- [ ] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n-- [ ] The existing debounced-autosave behavior still works within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n-- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale `#blo` after a clear).\n-- [ ] Enter-to-commit still dispatches immediately (no regression in the \"Enter key dispatches perspective.filter immediately\" test).\n-\n-## Tests\n-\n-- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes pending autosave on unmount\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`, dispatch a doc change that inserts `#BLOCKING`, unmount the component **before** the 300 ms debounce elapses, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }`.\n-- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button (which calls `cancel()` + `dispatchClearFilter`), then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button click).\n-- [ ] Add a test simulating the autocomplete-then-remount race: dispatch a doc change to `#blo`, wait 400 ms (lets the `#blo` save fire), dispatch another doc change to `#BLOCKING`, then unmount within 100 ms (well under 300 ms). Assert the final `mockInvoke` call for `perspective.filter` was with `#BLOCKING`.\n-- [ ] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n-- [ ] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n-\n-## Workflow\n-\n-- Use `/tdd` — write the three new tests first (they should fail against the current `cancel`-on-unmount behavior), then change `useDebouncedTimer` to flush on unmount so they pass.\n","transaction_id":null}
{"id":"01KP67K8XA78GHNFWAS21ZGCVT","timestamp":"2026-04-14T14:51:15.242287Z","entity_type":"task","entity_id":"01KP67K8X863YN5ZSJABMGWCJK","op":"create","changes":[["assignees",{"kind":"set","value":[]}],["project",{"kind":"set","value":"pill-via-cm6"}],["position_column",{"kind":"set","value":"todo"}],["title",{"kind":"set","value":"Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires"}],["body",{"kind":"set","value":"## What\n\n**Bug repro**: In the perspective formula bar, type `#blo`, pause until the autocomplete dropdown shows `#BLOCKING`, press Enter to accept. The editor visually shows `#BLOCKING` (correct). Then toggle to another perspective and back — the saved filter is `#blo`, which matches nothing.\n\n**Root cause** — a race between the 300 ms debounced autosave and perspective remount:\n\n1. `FilterFormulaBar` is keyed by `activePerspective.id` (`kanban-app/ui/src/components/perspective-tab-bar.tsx` near line 219), so switching perspectives **unmounts** `FilterEditor`.\n2. `useFilterDispatch` in `kanban-app/ui/src/components/filter-editor.tsx` autosaves via `useDebouncedTimer` with a 300 ms delay (`AUTOSAVE_DELAY_MS`).\n3. `useDebouncedTimer` cleanup (`useEffect(() => cancel, [cancel])`) **cancels** any pending timer on unmount — it does not flush it.\n4. Timing that triggers the bug:\n   - `t=0…300`: user types `#blo`. Each keystroke restarts the debounce.\n   - `t~600`: debounce fires with `#blo` → `perspective.filter { filter: \"#blo\" }` is persisted while the user is still reading the dropdown.\n   - `t~700`: user presses Enter. Autocomplete's `apply: \"#${slug}\"` (`kanban-app/ui/src/lib/cm-mention-autocomplete.ts` line 61) replaces the doc text with `#BLOCKING`. `updateListener` fires → `handleChange` → debounce restarts, scheduled for `t~1000`.\n   - `t~800`: user toggles perspective. `FilterFormulaBar` remounts → `useDebouncedTimer` cleanup cancels the `t~1000` save → `#BLOCKING` is never persisted. The already-saved `#blo` stays.\n\nThe visual correctness is real: `findMentionsInText` (`kanban-app/ui/src/lib/mention-finder.ts`) does exact slug matching, so the pill only renders when the doc text is exactly `#BLOCKING`. The editor state IS `#BLOCKING` post-acceptance — we just never save it.\n\n### Fix approach\n\nChange `useDebouncedTimer` so its unmount cleanup **flushes** the pending callback synchronously instead of dropping it. This preserves any in-flight autosave through remount races.\n\n- [ ] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n  - Store the pending callback alongside the timer (new `pendingFnRef`).\n  - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback immediately.\n  - Change the unmount effect from `useEffect(() => cancel, [cancel])` to `useEffect(() => flush, [flush])`.\n  - Keep `cancel` available for the clear-button path (`handleClear` must still drop, not flush, because clear supersedes any pending save).\n  - Return `{ schedule, cancel, flush }` from the hook.\n- [ ] Verify `handleCommit` (Enter key) already dispatches immediately — it does, via `cancel()` + `apply(text)` — so no change needed there. Autocomplete acceptance already goes through `handleChange` → debounce; flush-on-unmount covers it.\n- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`. Blur within the same perspective keeps scheduling (no unmount, no race). This card is specifically the unmount race.\n\n### Out of scope\n\n- Reworking autocomplete acceptance to dispatch immediately (viable alternative, but more invasive — touches `cm-mention-autocomplete.ts` and the per-source `apply` composition).\n- Shortening `AUTOSAVE_DELAY_MS` (doesn't fix the race).\n- Flushing on blur (separate concern — user didn't report a blur-related loss).\n\n## Acceptance Criteria\n\n- [ ] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n- [ ] The existing debounced-autosave behavior still works within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale `#blo` after a clear).\n- [ ] Enter-to-commit still dispatches immediately (no regression in the \"Enter key dispatches perspective.filter immediately\" test).\n\n## Tests\n\n- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes pending autosave on unmount\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`, dispatch a doc change that inserts `#BLOCKING`, unmount the component **before** the 300 ms debounce elapses, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }`.\n- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button (which calls `cancel()` + `dispatchClearFilter`), then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button click).\n- [ ] Add a test simulating the autocomplete-then-remount race: dispatch a doc change to `#blo`, wait 400 ms (lets the `#blo` save fire), dispatch another doc change to `#BLOCKING`, then unmount within 100 ms (well under 300 ms). Assert the final `mockInvoke` call for `perspective.filter` was with `#BLOCKING`.\n- [ ] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n- [ ] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n\n## Workflow\n\n- Use `/tdd` — write the three new tests first (they should fail against the current `cancel`-on-unmount behavior), then change `useDebouncedTimer` to flush on unmount so they pass.\n"}],["position_ordinal",{"kind":"set","value":"cc80"}]]}
{"id":"01KP67R68E87N0S1SXH4AYT6XW","timestamp":"2026-04-14T14:53:56.366717Z","entity_type":"task","entity_id":"01KP67K8X863YN5ZSJABMGWCJK","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -15,40 +15,55 @@\n\n The visual correctness is real: `findMentionsInText` (`kanban-app/ui/src/lib/mention-finder.ts`) does exact slug matching, so the pill only renders when the doc text is exactly `#BLOCKING`. The editor state IS `#BLOCKING` post-acceptance — we just never save it.\n\n-### Fix approach\n+### Fix approach — two complementary changes\n+\n+**Fix 1 — Flush-on-autocomplete-accept (primary signal)**: When a completion is accepted, bypass the debounce and dispatch the save immediately. Accepting a completion is a strong commit signal — the user picked that specific tag. This covers the common case and also handles \"click away to a non-perspective target\" (no remount, just loss of focus during the debounce window).\n+\n+CM6's `@codemirror/autocomplete` attaches a `pickedCompletion` annotation to the transaction when a completion is applied (also sets `userEvent: \"input.complete\"`). We can detect it in an `EditorView.updateListener`.\n+\n+**Fix 2 — Flush-on-unmount (safety net)**: Even with Fix 1, the debounce race still exists for non-completion edits (raw typing followed by a perspective toggle). Flush pending saves on unmount so no in-flight debounced write is silently dropped by React reconciliation.\n\n-Change `useDebouncedTimer` so its unmount cleanup **flushes** the pending callback synchronously instead of dropping it. This preserves any in-flight autosave through remount races.\n+### Implementation\n\n - [ ] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n   - Store the pending callback alongside the timer (new `pendingFnRef`).\n-  - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback immediately.\n+  - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback synchronously.\n   - Change the unmount effect from `useEffect(() => cancel, [cancel])` to `useEffect(() => flush, [flush])`.\n-  - Keep `cancel` available for the clear-button path (`handleClear` must still drop, not flush, because clear supersedes any pending save).\n+  - Keep `cancel` for the clear-button path (`handleClear` must still drop, not flush — clear supersedes any pending save).\n   - Return `{ schedule, cancel, flush }` from the hook.\n-- [ ] Verify `handleCommit` (Enter key) already dispatches immediately — it does, via `cancel()` + `apply(text)` — so no change needed there. Autocomplete acceptance already goes through `handleChange` → debounce; flush-on-unmount covers it.\n-- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`. Blur within the same perspective keeps scheduling (no unmount, no race). This card is specifically the unmount race.\n+- [ ] In `useFilterDispatch`, expose `flush` through the returned API (call it `handleFlush` or similar) so the editor can invoke it on completion accept.\n+- [ ] In `FilterEditor`, add a CM6 extension that detects completion-accept and triggers `flush`:\n+  - Import `pickedCompletion` from `@codemirror/autocomplete`.\n+  - Build an `EditorView.updateListener` extension that iterates `update.transactions`; if any carries a `pickedCompletion` annotation, call the `flush` callback **after** the transaction settles (use `queueMicrotask` or `setTimeout(0)` so `handleChange` — the debounce-scheduler — fires first on the same doc change; then `flush` runs the just-scheduled callback immediately).\n+  - Add this extension to the `extraExtensions` array passed to `TextEditor` alongside `mentionExts`.\n+- [ ] Keep the flush extension in `filter-editor.tsx` — do not add it to `useMentionExtensions`. Mention extensions are reused by other editors (task description, etc.) where immediate-dispatch-on-accept isn't the right behavior. The flush-on-accept is specific to the formula-bar autosave model.\n+- [ ] Verify `handleCommit` (Enter without active completion) already dispatches immediately — it does, via `cancel()` + `apply(text)` — no change needed.\n+- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`.\n\n ### Out of scope\n\n-- Reworking autocomplete acceptance to dispatch immediately (viable alternative, but more invasive — touches `cm-mention-autocomplete.ts` and the per-source `apply` composition).\n - Shortening `AUTOSAVE_DELAY_MS` (doesn't fix the race).\n-- Flushing on blur (separate concern — user didn't report a blur-related loss).\n+- Flushing on blur for field editors (separate concern — user didn't report blur-related loss and field editors use a different save model).\n+- Routing completion-accept-flush through `useMentionExtensions` for all CM6 editors (only the formula bar has debounced autosave; descriptions save via explicit commit).\n\n ## Acceptance Criteria\n\n - [ ] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n-- [ ] The existing debounced-autosave behavior still works within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n-- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale `#blo` after a clear).\n-- [ ] Enter-to-commit still dispatches immediately (no regression in the \"Enter key dispatches perspective.filter immediately\" test).\n+- [ ] Accepting a completion dispatches `perspective.filter` immediately (before the 300 ms debounce elapses) — verifiable because `mockInvoke` is called synchronously after the autocomplete transaction.\n+- [ ] The existing debounced-autosave behavior still works for ordinary typing within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n+- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale text after a clear).\n+- [ ] Enter-to-commit (without active completion) still dispatches immediately.\n+- [ ] Task-description editors and other mention-autocomplete users are unaffected — no immediate-dispatch behavior added outside the formula bar.\n\n ## Tests\n\n-- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes pending autosave on unmount\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`, dispatch a doc change that inserts `#BLOCKING`, unmount the component **before** the 300 ms debounce elapses, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }`.\n-- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button (which calls `cancel()` + `dispatchClearFilter`), then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button click).\n-- [ ] Add a test simulating the autocomplete-then-remount race: dispatch a doc change to `#blo`, wait 400 ms (lets the `#blo` save fire), dispatch another doc change to `#BLOCKING`, then unmount within 100 ms (well under 300 ms). Assert the final `mockInvoke` call for `perspective.filter` was with `#BLOCKING`.\n+- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes immediately when a completion is accepted\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`. Acquire the `EditorView`, dispatch a transaction that inserts `#BLOCKING` with a `pickedCompletion.of({label: \"#BLOCKING\", apply: \"#BLOCKING\"})` annotation, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }` **within one microtask/tick** — not after 300 ms.\n+- [ ] Add a test: \"flushes pending autosave on unmount\". Dispatch an insert for `#BLOCKING` **without** the `pickedCompletion` annotation (i.e. raw typing), unmount the component before 300 ms elapses, assert `mockInvoke` was called with `#BLOCKING`.\n+- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button, then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button).\n+- [ ] Add a test: \"autocomplete accept then remount preserves accepted tag\". Dispatch a doc change to `#blo` (no annotation), wait 400 ms so `#blo` saves, then dispatch a replacement transaction for `#BLOCKING` **with** `pickedCompletion` annotation, then unmount within 100 ms. Assert the final `mockInvoke` call for `perspective.filter` used `#BLOCKING`.\n - [ ] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n - [ ] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n\n ## Workflow\n\n-- Use `/tdd` — write the three new tests first (they should fail against the current `cancel`-on-unmount behavior), then change `useDebouncedTimer` to flush on unmount so they pass.\n+- Use `/tdd` — write the four new tests first (they should fail against the current `cancel`-on-unmount + no-accept-detection behavior), then add the `flush` method, the `pickedCompletion` updateListener extension, and the unmount-flush effect to make them pass.\n","reverse_patch":"--- original\n+++ modified\n@@ -15,55 +15,40 @@\n\n The visual correctness is real: `findMentionsInText` (`kanban-app/ui/src/lib/mention-finder.ts`) does exact slug matching, so the pill only renders when the doc text is exactly `#BLOCKING`. The editor state IS `#BLOCKING` post-acceptance — we just never save it.\n\n-### Fix approach — two complementary changes\n+### Fix approach\n\n-**Fix 1 — Flush-on-autocomplete-accept (primary signal)**: When a completion is accepted, bypass the debounce and dispatch the save immediately. Accepting a completion is a strong commit signal — the user picked that specific tag. This covers the common case and also handles \"click away to a non-perspective target\" (no remount, just loss of focus during the debounce window).\n-\n-CM6's `@codemirror/autocomplete` attaches a `pickedCompletion` annotation to the transaction when a completion is applied (also sets `userEvent: \"input.complete\"`). We can detect it in an `EditorView.updateListener`.\n+Change `useDebouncedTimer` so its unmount cleanup **flushes** the pending callback synchronously instead of dropping it. This preserves any in-flight autosave through remount races.\n\n-**Fix 2 — Flush-on-unmount (safety net)**: Even with Fix 1, the debounce race still exists for non-completion edits (raw typing followed by a perspective toggle). Flush pending saves on unmount so no in-flight debounced write is silently dropped by React reconciliation.\n-\n-### Implementation\n-\n - [ ] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n   - Store the pending callback alongside the timer (new `pendingFnRef`).\n-  - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback synchronously.\n+  - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback immediately.\n   - Change the unmount effect from `useEffect(() => cancel, [cancel])` to `useEffect(() => flush, [flush])`.\n-  - Keep `cancel` for the clear-button path (`handleClear` must still drop, not flush — clear supersedes any pending save).\n+  - Keep `cancel` available for the clear-button path (`handleClear` must still drop, not flush, because clear supersedes any pending save).\n   - Return `{ schedule, cancel, flush }` from the hook.\n-- [ ] In `useFilterDispatch`, expose `flush` through the returned API (call it `handleFlush` or similar) so the editor can invoke it on completion accept.\n-- [ ] In `FilterEditor`, add a CM6 extension that detects completion-accept and triggers `flush`:\n-  - Import `pickedCompletion` from `@codemirror/autocomplete`.\n-  - Build an `EditorView.updateListener` extension that iterates `update.transactions`; if any carries a `pickedCompletion` annotation, call the `flush` callback **after** the transaction settles (use `queueMicrotask` or `setTimeout(0)` so `handleChange` — the debounce-scheduler — fires first on the same doc change; then `flush` runs the just-scheduled callback immediately).\n-  - Add this extension to the `extraExtensions` array passed to `TextEditor` alongside `mentionExts`.\n-- [ ] Keep the flush extension in `filter-editor.tsx` — do not add it to `useMentionExtensions`. Mention extensions are reused by other editors (task description, etc.) where immediate-dispatch-on-accept isn't the right behavior. The flush-on-accept is specific to the formula-bar autosave model.\n-- [ ] Verify `handleCommit` (Enter without active completion) already dispatches immediately — it does, via `cancel()` + `apply(text)` — no change needed.\n-- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`.\n+- [ ] Verify `handleCommit` (Enter key) already dispatches immediately — it does, via `cancel()` + `apply(text)` — so no change needed there. Autocomplete acceptance already goes through `handleChange` → debounce; flush-on-unmount covers it.\n+- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`. Blur within the same perspective keeps scheduling (no unmount, no race). This card is specifically the unmount race.\n\n ### Out of scope\n\n+- Reworking autocomplete acceptance to dispatch immediately (viable alternative, but more invasive — touches `cm-mention-autocomplete.ts` and the per-source `apply` composition).\n - Shortening `AUTOSAVE_DELAY_MS` (doesn't fix the race).\n-- Flushing on blur for field editors (separate concern — user didn't report blur-related loss and field editors use a different save model).\n-- Routing completion-accept-flush through `useMentionExtensions` for all CM6 editors (only the formula bar has debounced autosave; descriptions save via explicit commit).\n+- Flushing on blur (separate concern — user didn't report a blur-related loss).\n\n ## Acceptance Criteria\n\n - [ ] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n-- [ ] Accepting a completion dispatches `perspective.filter` immediately (before the 300 ms debounce elapses) — verifiable because `mockInvoke` is called synchronously after the autocomplete transaction.\n-- [ ] The existing debounced-autosave behavior still works for ordinary typing within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n-- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale text after a clear).\n-- [ ] Enter-to-commit (without active completion) still dispatches immediately.\n-- [ ] Task-description editors and other mention-autocomplete users are unaffected — no immediate-dispatch behavior added outside the formula bar.\n+- [ ] The existing debounced-autosave behavior still works within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n+- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale `#blo` after a clear).\n+- [ ] Enter-to-commit still dispatches immediately (no regression in the \"Enter key dispatches perspective.filter immediately\" test).\n\n ## Tests\n\n-- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes immediately when a completion is accepted\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`. Acquire the `EditorView`, dispatch a transaction that inserts `#BLOCKING` with a `pickedCompletion.of({label: \"#BLOCKING\", apply: \"#BLOCKING\"})` annotation, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }` **within one microtask/tick** — not after 300 ms.\n-- [ ] Add a test: \"flushes pending autosave on unmount\". Dispatch an insert for `#BLOCKING` **without** the `pickedCompletion` annotation (i.e. raw typing), unmount the component before 300 ms elapses, assert `mockInvoke` was called with `#BLOCKING`.\n-- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button, then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button).\n-- [ ] Add a test: \"autocomplete accept then remount preserves accepted tag\". Dispatch a doc change to `#blo` (no annotation), wait 400 ms so `#blo` saves, then dispatch a replacement transaction for `#BLOCKING` **with** `pickedCompletion` annotation, then unmount within 100 ms. Assert the final `mockInvoke` call for `perspective.filter` used `#BLOCKING`.\n+- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes pending autosave on unmount\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`, dispatch a doc change that inserts `#BLOCKING`, unmount the component **before** the 300 ms debounce elapses, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }`.\n+- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button (which calls `cancel()` + `dispatchClearFilter`), then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button click).\n+- [ ] Add a test simulating the autocomplete-then-remount race: dispatch a doc change to `#blo`, wait 400 ms (lets the `#blo` save fire), dispatch another doc change to `#BLOCKING`, then unmount within 100 ms (well under 300 ms). Assert the final `mockInvoke` call for `perspective.filter` was with `#BLOCKING`.\n - [ ] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n - [ ] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n\n ## Workflow\n\n-- Use `/tdd` — write the four new tests first (they should fail against the current `cancel`-on-unmount + no-accept-detection behavior), then add the `flush` method, the `pickedCompletion` updateListener extension, and the unmount-flush effect to make them pass.\n+- Use `/tdd` — write the three new tests first (they should fail against the current `cancel`-on-unmount behavior), then change `useDebouncedTimer` to flush on unmount so they pass.\n"}]]}
{"id":"01KP67R68EEYFXT8HEQSH0PH9Q","timestamp":"2026-04-14T14:53:56.366223Z","op":"update","item_id":"01KP67K8X863YN5ZSJABMGWCJK","forward_patch":"--- original\n+++ modified\n@@ -22,40 +22,55 @@\n\n The visual correctness is real: `findMentionsInText` (`kanban-app/ui/src/lib/mention-finder.ts`) does exact slug matching, so the pill only renders when the doc text is exactly `#BLOCKING`. The editor state IS `#BLOCKING` post-acceptance — we just never save it.\n\n-### Fix approach\n+### Fix approach — two complementary changes\n+\n+**Fix 1 — Flush-on-autocomplete-accept (primary signal)**: When a completion is accepted, bypass the debounce and dispatch the save immediately. Accepting a completion is a strong commit signal — the user picked that specific tag. This covers the common case and also handles \"click away to a non-perspective target\" (no remount, just loss of focus during the debounce window).\n+\n+CM6's `@codemirror/autocomplete` attaches a `pickedCompletion` annotation to the transaction when a completion is applied (also sets `userEvent: \"input.complete\"`). We can detect it in an `EditorView.updateListener`.\n+\n+**Fix 2 — Flush-on-unmount (safety net)**: Even with Fix 1, the debounce race still exists for non-completion edits (raw typing followed by a perspective toggle). Flush pending saves on unmount so no in-flight debounced write is silently dropped by React reconciliation.\n\n-Change `useDebouncedTimer` so its unmount cleanup **flushes** the pending callback synchronously instead of dropping it. This preserves any in-flight autosave through remount races.\n+### Implementation\n\n - [ ] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n   - Store the pending callback alongside the timer (new `pendingFnRef`).\n-  - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback immediately.\n+  - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback synchronously.\n   - Change the unmount effect from `useEffect(() => cancel, [cancel])` to `useEffect(() => flush, [flush])`.\n-  - Keep `cancel` available for the clear-button path (`handleClear` must still drop, not flush, because clear supersedes any pending save).\n+  - Keep `cancel` for the clear-button path (`handleClear` must still drop, not flush — clear supersedes any pending save).\n   - Return `{ schedule, cancel, flush }` from the hook.\n-- [ ] Verify `handleCommit` (Enter key) already dispatches immediately — it does, via `cancel()` + `apply(text)` — so no change needed there. Autocomplete acceptance already goes through `handleChange` → debounce; flush-on-unmount covers it.\n-- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`. Blur within the same perspective keeps scheduling (no unmount, no race). This card is specifically the unmount race.\n+- [ ] In `useFilterDispatch`, expose `flush` through the returned API (call it `handleFlush` or similar) so the editor can invoke it on completion accept.\n+- [ ] In `FilterEditor`, add a CM6 extension that detects completion-accept and triggers `flush`:\n+  - Import `pickedCompletion` from `@codemirror/autocomplete`.\n+  - Build an `EditorView.updateListener` extension that iterates `update.transactions`; if any carries a `pickedCompletion` annotation, call the `flush` callback **after** the transaction settles (use `queueMicrotask` or `setTimeout(0)` so `handleChange` — the debounce-scheduler — fires first on the same doc change; then `flush` runs the just-scheduled callback immediately).\n+  - Add this extension to the `extraExtensions` array passed to `TextEditor` alongside `mentionExts`.\n+- [ ] Keep the flush extension in `filter-editor.tsx` — do not add it to `useMentionExtensions`. Mention extensions are reused by other editors (task description, etc.) where immediate-dispatch-on-accept isn't the right behavior. The flush-on-accept is specific to the formula-bar autosave model.\n+- [ ] Verify `handleCommit` (Enter without active completion) already dispatches immediately — it does, via `cancel()` + `apply(text)` — no change needed.\n+- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`.\n\n ### Out of scope\n\n-- Reworking autocomplete acceptance to dispatch immediately (viable alternative, but more invasive — touches `cm-mention-autocomplete.ts` and the per-source `apply` composition).\n - Shortening `AUTOSAVE_DELAY_MS` (doesn't fix the race).\n-- Flushing on blur (separate concern — user didn't report a blur-related loss).\n+- Flushing on blur for field editors (separate concern — user didn't report blur-related loss and field editors use a different save model).\n+- Routing completion-accept-flush through `useMentionExtensions` for all CM6 editors (only the formula bar has debounced autosave; descriptions save via explicit commit).\n\n ## Acceptance Criteria\n\n - [ ] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n-- [ ] The existing debounced-autosave behavior still works within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n-- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale `#blo` after a clear).\n-- [ ] Enter-to-commit still dispatches immediately (no regression in the \"Enter key dispatches perspective.filter immediately\" test).\n+- [ ] Accepting a completion dispatches `perspective.filter` immediately (before the 300 ms debounce elapses) — verifiable because `mockInvoke` is called synchronously after the autocomplete transaction.\n+- [ ] The existing debounced-autosave behavior still works for ordinary typing within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n+- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale text after a clear).\n+- [ ] Enter-to-commit (without active completion) still dispatches immediately.\n+- [ ] Task-description editors and other mention-autocomplete users are unaffected — no immediate-dispatch behavior added outside the formula bar.\n\n ## Tests\n\n-- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes pending autosave on unmount\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`, dispatch a doc change that inserts `#BLOCKING`, unmount the component **before** the 300 ms debounce elapses, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }`.\n-- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button (which calls `cancel()` + `dispatchClearFilter`), then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button click).\n-- [ ] Add a test simulating the autocomplete-then-remount race: dispatch a doc change to `#blo`, wait 400 ms (lets the `#blo` save fire), dispatch another doc change to `#BLOCKING`, then unmount within 100 ms (well under 300 ms). Assert the final `mockInvoke` call for `perspective.filter` was with `#BLOCKING`.\n+- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes immediately when a completion is accepted\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`. Acquire the `EditorView`, dispatch a transaction that inserts `#BLOCKING` with a `pickedCompletion.of({label: \"#BLOCKING\", apply: \"#BLOCKING\"})` annotation, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }` **within one microtask/tick** — not after 300 ms.\n+- [ ] Add a test: \"flushes pending autosave on unmount\". Dispatch an insert for `#BLOCKING` **without** the `pickedCompletion` annotation (i.e. raw typing), unmount the component before 300 ms elapses, assert `mockInvoke` was called with `#BLOCKING`.\n+- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button, then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button).\n+- [ ] Add a test: \"autocomplete accept then remount preserves accepted tag\". Dispatch a doc change to `#blo` (no annotation), wait 400 ms so `#blo` saves, then dispatch a replacement transaction for `#BLOCKING` **with** `pickedCompletion` annotation, then unmount within 100 ms. Assert the final `mockInvoke` call for `perspective.filter` used `#BLOCKING`.\n - [ ] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n - [ ] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n\n ## Workflow\n\n-- Use `/tdd` — write the three new tests first (they should fail against the current `cancel`-on-unmount behavior), then change `useDebouncedTimer` to flush on unmount so they pass.\n+- Use `/tdd` — write the four new tests first (they should fail against the current `cancel`-on-unmount + no-accept-detection behavior), then add the `flush` method, the `pickedCompletion` updateListener extension, and the unmount-flush effect to make them pass.\n","reverse_patch":"--- original\n+++ modified\n@@ -22,55 +22,40 @@\n\n The visual correctness is real: `findMentionsInText` (`kanban-app/ui/src/lib/mention-finder.ts`) does exact slug matching, so the pill only renders when the doc text is exactly `#BLOCKING`. The editor state IS `#BLOCKING` post-acceptance — we just never save it.\n\n-### Fix approach — two complementary changes\n+### Fix approach\n\n-**Fix 1 — Flush-on-autocomplete-accept (primary signal)**: When a completion is accepted, bypass the debounce and dispatch the save immediately. Accepting a completion is a strong commit signal — the user picked that specific tag. This covers the common case and also handles \"click away to a non-perspective target\" (no remount, just loss of focus during the debounce window).\n-\n-CM6's `@codemirror/autocomplete` attaches a `pickedCompletion` annotation to the transaction when a completion is applied (also sets `userEvent: \"input.complete\"`). We can detect it in an `EditorView.updateListener`.\n+Change `useDebouncedTimer` so its unmount cleanup **flushes** the pending callback synchronously instead of dropping it. This preserves any in-flight autosave through remount races.\n\n-**Fix 2 — Flush-on-unmount (safety net)**: Even with Fix 1, the debounce race still exists for non-completion edits (raw typing followed by a perspective toggle). Flush pending saves on unmount so no in-flight debounced write is silently dropped by React reconciliation.\n-\n-### Implementation\n-\n - [ ] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n   - Store the pending callback alongside the timer (new `pendingFnRef`).\n-  - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback synchronously.\n+  - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback immediately.\n   - Change the unmount effect from `useEffect(() => cancel, [cancel])` to `useEffect(() => flush, [flush])`.\n-  - Keep `cancel` for the clear-button path (`handleClear` must still drop, not flush — clear supersedes any pending save).\n+  - Keep `cancel` available for the clear-button path (`handleClear` must still drop, not flush, because clear supersedes any pending save).\n   - Return `{ schedule, cancel, flush }` from the hook.\n-- [ ] In `useFilterDispatch`, expose `flush` through the returned API (call it `handleFlush` or similar) so the editor can invoke it on completion accept.\n-- [ ] In `FilterEditor`, add a CM6 extension that detects completion-accept and triggers `flush`:\n-  - Import `pickedCompletion` from `@codemirror/autocomplete`.\n-  - Build an `EditorView.updateListener` extension that iterates `update.transactions`; if any carries a `pickedCompletion` annotation, call the `flush` callback **after** the transaction settles (use `queueMicrotask` or `setTimeout(0)` so `handleChange` — the debounce-scheduler — fires first on the same doc change; then `flush` runs the just-scheduled callback immediately).\n-  - Add this extension to the `extraExtensions` array passed to `TextEditor` alongside `mentionExts`.\n-- [ ] Keep the flush extension in `filter-editor.tsx` — do not add it to `useMentionExtensions`. Mention extensions are reused by other editors (task description, etc.) where immediate-dispatch-on-accept isn't the right behavior. The flush-on-accept is specific to the formula-bar autosave model.\n-- [ ] Verify `handleCommit` (Enter without active completion) already dispatches immediately — it does, via `cancel()` + `apply(text)` — no change needed.\n-- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`.\n+- [ ] Verify `handleCommit` (Enter key) already dispatches immediately — it does, via `cancel()` + `apply(text)` — so no change needed there. Autocomplete acceptance already goes through `handleChange` → debounce; flush-on-unmount covers it.\n+- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`. Blur within the same perspective keeps scheduling (no unmount, no race). This card is specifically the unmount race.\n\n ### Out of scope\n\n+- Reworking autocomplete acceptance to dispatch immediately (viable alternative, but more invasive — touches `cm-mention-autocomplete.ts` and the per-source `apply` composition).\n - Shortening `AUTOSAVE_DELAY_MS` (doesn't fix the race).\n-- Flushing on blur for field editors (separate concern — user didn't report blur-related loss and field editors use a different save model).\n-- Routing completion-accept-flush through `useMentionExtensions` for all CM6 editors (only the formula bar has debounced autosave; descriptions save via explicit commit).\n+- Flushing on blur (separate concern — user didn't report a blur-related loss).\n\n ## Acceptance Criteria\n\n - [ ] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n-- [ ] Accepting a completion dispatches `perspective.filter` immediately (before the 300 ms debounce elapses) — verifiable because `mockInvoke` is called synchronously after the autocomplete transaction.\n-- [ ] The existing debounced-autosave behavior still works for ordinary typing within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n-- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale text after a clear).\n-- [ ] Enter-to-commit (without active completion) still dispatches immediately.\n-- [ ] Task-description editors and other mention-autocomplete users are unaffected — no immediate-dispatch behavior added outside the formula bar.\n+- [ ] The existing debounced-autosave behavior still works within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n+- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale `#blo` after a clear).\n+- [ ] Enter-to-commit still dispatches immediately (no regression in the \"Enter key dispatches perspective.filter immediately\" test).\n\n ## Tests\n\n-- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes immediately when a completion is accepted\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`. Acquire the `EditorView`, dispatch a transaction that inserts `#BLOCKING` with a `pickedCompletion.of({label: \"#BLOCKING\", apply: \"#BLOCKING\"})` annotation, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }` **within one microtask/tick** — not after 300 ms.\n-- [ ] Add a test: \"flushes pending autosave on unmount\". Dispatch an insert for `#BLOCKING` **without** the `pickedCompletion` annotation (i.e. raw typing), unmount the component before 300 ms elapses, assert `mockInvoke` was called with `#BLOCKING`.\n-- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button, then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button).\n-- [ ] Add a test: \"autocomplete accept then remount preserves accepted tag\". Dispatch a doc change to `#blo` (no annotation), wait 400 ms so `#blo` saves, then dispatch a replacement transaction for `#BLOCKING` **with** `pickedCompletion` annotation, then unmount within 100 ms. Assert the final `mockInvoke` call for `perspective.filter` used `#BLOCKING`.\n+- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes pending autosave on unmount\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`, dispatch a doc change that inserts `#BLOCKING`, unmount the component **before** the 300 ms debounce elapses, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }`.\n+- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button (which calls `cancel()` + `dispatchClearFilter`), then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button click).\n+- [ ] Add a test simulating the autocomplete-then-remount race: dispatch a doc change to `#blo`, wait 400 ms (lets the `#blo` save fire), dispatch another doc change to `#BLOCKING`, then unmount within 100 ms (well under 300 ms). Assert the final `mockInvoke` call for `perspective.filter` was with `#BLOCKING`.\n - [ ] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n - [ ] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n\n ## Workflow\n\n-- Use `/tdd` — write the four new tests first (they should fail against the current `cancel`-on-unmount + no-accept-detection behavior), then add the `flush` method, the `pickedCompletion` updateListener extension, and the unmount-flush effect to make them pass.\n+- Use `/tdd` — write the three new tests first (they should fail against the current `cancel`-on-unmount behavior), then change `useDebouncedTimer` to flush on unmount so they pass.\n","transaction_id":null}
{"id":"01KP77YJVMAVK0V633DNMXPN9T","timestamp":"2026-04-15T00:16:40.308604Z","op":"update","item_id":"01KP67K8X863YN5ZSJABMGWCJK","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees: []\n position_column: todo\n-position_ordinal: cc80\n+position_ordinal: 7b80\n project: pill-via-cm6\n title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees: []\n position_column: todo\n-position_ordinal: 7b80\n+position_ordinal: cc80\n project: pill-via-cm6\n title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n ---\n","transaction_id":null}
{"id":"01KP77YJVP9MG8QGCD4315XMJ1","timestamp":"2026-04-15T00:16:40.310875Z","entity_type":"task","entity_id":"01KP67K8X863YN5ZSJABMGWCJK","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-cc80\n\\ No newline at end of file\n+7b80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-7b80\n\\ No newline at end of file\n+cc80\n\\ No newline at end of file\n"}]]}
{"id":"01KP77YJVPMEM49JR912EGB0YG","timestamp":"2026-04-15T00:16:40.310309Z","op":"update","item_id":"01KP67K8X863YN5ZSJABMGWCJK","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees: []\n position_column: todo\n-position_ordinal: cc80\n+position_ordinal: 7c80\n project: pill-via-cm6\n title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees: []\n position_column: todo\n-position_ordinal: 7c80\n+position_ordinal: cc80\n project: pill-via-cm6\n title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n ---\n","transaction_id":null}
{"id":"01KP77YJVQ3KKQGHCXGHV952ED","timestamp":"2026-04-15T00:16:40.311042Z","entity_type":"task","entity_id":"01KP67K8X863YN5ZSJABMGWCJK","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-cc80\n\\ No newline at end of file\n+7c80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-7c80\n\\ No newline at end of file\n+cc80\n\\ No newline at end of file\n"}]]}
{"id":"01KP78DZGC637C41E5KBA3K25S","timestamp":"2026-04-15T00:25:04.780822Z","op":"update","item_id":"01KP67K8X863YN5ZSJABMGWCJK","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees: []\n-position_column: todo\n-position_ordinal: 7c80\n+position_column: doing\n+position_ordinal: '80'\n project: pill-via-cm6\n title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees: []\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: 7c80\n project: pill-via-cm6\n title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n ---\n","transaction_id":null}
{"id":"01KP78DZGD091YVQ56HQM94C6D","timestamp":"2026-04-15T00:25:04.781450Z","entity_type":"task","entity_id":"01KP67K8X863YN5ZSJABMGWCJK","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-7c80\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+7c80\n\\ No newline at end of file\n"}]]}
{"id":"01KP793E49N1ZF8WXY0XX5SA9P","timestamp":"2026-04-15T00:36:47.881797Z","entity_type":"task","entity_id":"01KP67K8X863YN5ZSJABMGWCJK","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -25,20 +25,20 @@\n\n ### Implementation\n\n-- [ ] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n+- [x] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n   - Store the pending callback alongside the timer (new `pendingFnRef`).\n   - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback synchronously.\n   - Change the unmount effect from `useEffect(() => cancel, [cancel])` to `useEffect(() => flush, [flush])`.\n   - Keep `cancel` for the clear-button path (`handleClear` must still drop, not flush — clear supersedes any pending save).\n   - Return `{ schedule, cancel, flush }` from the hook.\n-- [ ] In `useFilterDispatch`, expose `flush` through the returned API (call it `handleFlush` or similar) so the editor can invoke it on completion accept.\n-- [ ] In `FilterEditor`, add a CM6 extension that detects completion-accept and triggers `flush`:\n+- [x] In `useFilterDispatch`, expose `flush` through the returned API (call it `handleFlush` or similar) so the editor can invoke it on completion accept.\n+- [x] In `FilterEditor`, add a CM6 extension that detects completion-accept and triggers `flush`:\n   - Import `pickedCompletion` from `@codemirror/autocomplete`.\n   - Build an `EditorView.updateListener` extension that iterates `update.transactions`; if any carries a `pickedCompletion` annotation, call the `flush` callback **after** the transaction settles (use `queueMicrotask` or `setTimeout(0)` so `handleChange` — the debounce-scheduler — fires first on the same doc change; then `flush` runs the just-scheduled callback immediately).\n   - Add this extension to the `extraExtensions` array passed to `TextEditor` alongside `mentionExts`.\n-- [ ] Keep the flush extension in `filter-editor.tsx` — do not add it to `useMentionExtensions`. Mention extensions are reused by other editors (task description, etc.) where immediate-dispatch-on-accept isn't the right behavior. The flush-on-accept is specific to the formula-bar autosave model.\n-- [ ] Verify `handleCommit` (Enter without active completion) already dispatches immediately — it does, via `cancel()` + `apply(text)` — no change needed.\n-- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`.\n+- [x] Keep the flush extension in `filter-editor.tsx` — do not add it to `useMentionExtensions`. Mention extensions are reused by other editors (task description, etc.) where immediate-dispatch-on-accept isn't the right behavior. The flush-on-accept is specific to the formula-bar autosave model.\n+- [x] Verify `handleCommit` (Enter without active completion) already dispatches immediately — it does, via `cancel()` + `apply(text)` — no change needed.\n+- [x] Do not change `handleBlur` behavior in `text-editor.tsx`.\n\n ### Out of scope\n\n@@ -48,21 +48,21 @@\n\n ## Acceptance Criteria\n\n-- [ ] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n-- [ ] Accepting a completion dispatches `perspective.filter` immediately (before the 300 ms debounce elapses) — verifiable because `mockInvoke` is called synchronously after the autocomplete transaction.\n-- [ ] The existing debounced-autosave behavior still works for ordinary typing within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n-- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale text after a clear).\n-- [ ] Enter-to-commit (without active completion) still dispatches immediately.\n-- [ ] Task-description editors and other mention-autocomplete users are unaffected — no immediate-dispatch behavior added outside the formula bar.\n+- [x] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n+- [x] Accepting a completion dispatches `perspective.filter` immediately (before the 300 ms debounce elapses) — verifiable because `mockInvoke` is called synchronously after the autocomplete transaction.\n+- [x] The existing debounced-autosave behavior still works for ordinary typing within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n+- [x] Clearing the filter via the × button still cancels pending saves (does not flush stale text after a clear).\n+- [x] Enter-to-commit (without active completion) still dispatches immediately.\n+- [x] Task-description editors and other mention-autocomplete users are unaffected — no immediate-dispatch behavior added outside the formula bar.\n\n ## Tests\n\n-- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes immediately when a completion is accepted\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`. Acquire the `EditorView`, dispatch a transaction that inserts `#BLOCKING` with a `pickedCompletion.of({label: \"#BLOCKING\", apply: \"#BLOCKING\"})` annotation, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }` **within one microtask/tick** — not after 300 ms.\n-- [ ] Add a test: \"flushes pending autosave on unmount\". Dispatch an insert for `#BLOCKING` **without** the `pickedCompletion` annotation (i.e. raw typing), unmount the component before 300 ms elapses, assert `mockInvoke` was called with `#BLOCKING`.\n-- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button, then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button).\n-- [ ] Add a test: \"autocomplete accept then remount preserves accepted tag\". Dispatch a doc change to `#blo` (no annotation), wait 400 ms so `#blo` saves, then dispatch a replacement transaction for `#BLOCKING` **with** `pickedCompletion` annotation, then unmount within 100 ms. Assert the final `mockInvoke` call for `perspective.filter` used `#BLOCKING`.\n-- [ ] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n-- [ ] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n+- [x] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes immediately when a completion is accepted\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`. Acquire the `EditorView`, dispatch a transaction that inserts `#BLOCKING` with a `pickedCompletion.of({label: \"#BLOCKING\", apply: \"#BLOCKING\"})` annotation, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }` **within one microtask/tick** — not after 300 ms.\n+- [x] Add a test: \"flushes pending autosave on unmount\". Dispatch an insert for `#BLOCKING` **without** the `pickedCompletion` annotation (i.e. raw typing), unmount the component before 300 ms elapses, assert `mockInvoke` was called with `#BLOCKING`.\n+- [x] Add a test: \"does not flush after clear\". Render with a filter, click the clear button, then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button).\n+- [x] Add a test: \"autocomplete accept then remount preserves accepted tag\". Dispatch a doc change to `#blo` (no annotation), wait 400 ms so `#blo` saves, then dispatch a replacement transaction for `#BLOCKING` **with** `pickedCompletion` annotation, then unmount within 100 ms. Assert the final `mockInvoke` call for `perspective.filter` used `#BLOCKING`.\n+- [x] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n+- [x] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -25,20 +25,20 @@\n\n ### Implementation\n\n-- [x] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n+- [ ] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n   - Store the pending callback alongside the timer (new `pendingFnRef`).\n   - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback synchronously.\n   - Change the unmount effect from `useEffect(() => cancel, [cancel])` to `useEffect(() => flush, [flush])`.\n   - Keep `cancel` for the clear-button path (`handleClear` must still drop, not flush — clear supersedes any pending save).\n   - Return `{ schedule, cancel, flush }` from the hook.\n-- [x] In `useFilterDispatch`, expose `flush` through the returned API (call it `handleFlush` or similar) so the editor can invoke it on completion accept.\n-- [x] In `FilterEditor`, add a CM6 extension that detects completion-accept and triggers `flush`:\n+- [ ] In `useFilterDispatch`, expose `flush` through the returned API (call it `handleFlush` or similar) so the editor can invoke it on completion accept.\n+- [ ] In `FilterEditor`, add a CM6 extension that detects completion-accept and triggers `flush`:\n   - Import `pickedCompletion` from `@codemirror/autocomplete`.\n   - Build an `EditorView.updateListener` extension that iterates `update.transactions`; if any carries a `pickedCompletion` annotation, call the `flush` callback **after** the transaction settles (use `queueMicrotask` or `setTimeout(0)` so `handleChange` — the debounce-scheduler — fires first on the same doc change; then `flush` runs the just-scheduled callback immediately).\n   - Add this extension to the `extraExtensions` array passed to `TextEditor` alongside `mentionExts`.\n-- [x] Keep the flush extension in `filter-editor.tsx` — do not add it to `useMentionExtensions`. Mention extensions are reused by other editors (task description, etc.) where immediate-dispatch-on-accept isn't the right behavior. The flush-on-accept is specific to the formula-bar autosave model.\n-- [x] Verify `handleCommit` (Enter without active completion) already dispatches immediately — it does, via `cancel()` + `apply(text)` — no change needed.\n-- [x] Do not change `handleBlur` behavior in `text-editor.tsx`.\n+- [ ] Keep the flush extension in `filter-editor.tsx` — do not add it to `useMentionExtensions`. Mention extensions are reused by other editors (task description, etc.) where immediate-dispatch-on-accept isn't the right behavior. The flush-on-accept is specific to the formula-bar autosave model.\n+- [ ] Verify `handleCommit` (Enter without active completion) already dispatches immediately — it does, via `cancel()` + `apply(text)` — no change needed.\n+- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`.\n\n ### Out of scope\n\n@@ -48,21 +48,21 @@\n\n ## Acceptance Criteria\n\n-- [x] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n-- [x] Accepting a completion dispatches `perspective.filter` immediately (before the 300 ms debounce elapses) — verifiable because `mockInvoke` is called synchronously after the autocomplete transaction.\n-- [x] The existing debounced-autosave behavior still works for ordinary typing within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n-- [x] Clearing the filter via the × button still cancels pending saves (does not flush stale text after a clear).\n-- [x] Enter-to-commit (without active completion) still dispatches immediately.\n-- [x] Task-description editors and other mention-autocomplete users are unaffected — no immediate-dispatch behavior added outside the formula bar.\n+- [ ] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n+- [ ] Accepting a completion dispatches `perspective.filter` immediately (before the 300 ms debounce elapses) — verifiable because `mockInvoke` is called synchronously after the autocomplete transaction.\n+- [ ] The existing debounced-autosave behavior still works for ordinary typing within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n+- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale text after a clear).\n+- [ ] Enter-to-commit (without active completion) still dispatches immediately.\n+- [ ] Task-description editors and other mention-autocomplete users are unaffected — no immediate-dispatch behavior added outside the formula bar.\n\n ## Tests\n\n-- [x] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes immediately when a completion is accepted\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`. Acquire the `EditorView`, dispatch a transaction that inserts `#BLOCKING` with a `pickedCompletion.of({label: \"#BLOCKING\", apply: \"#BLOCKING\"})` annotation, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }` **within one microtask/tick** — not after 300 ms.\n-- [x] Add a test: \"flushes pending autosave on unmount\". Dispatch an insert for `#BLOCKING` **without** the `pickedCompletion` annotation (i.e. raw typing), unmount the component before 300 ms elapses, assert `mockInvoke` was called with `#BLOCKING`.\n-- [x] Add a test: \"does not flush after clear\". Render with a filter, click the clear button, then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button).\n-- [x] Add a test: \"autocomplete accept then remount preserves accepted tag\". Dispatch a doc change to `#blo` (no annotation), wait 400 ms so `#blo` saves, then dispatch a replacement transaction for `#BLOCKING` **with** `pickedCompletion` annotation, then unmount within 100 ms. Assert the final `mockInvoke` call for `perspective.filter` used `#BLOCKING`.\n-- [x] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n-- [x] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n+- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes immediately when a completion is accepted\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`. Acquire the `EditorView`, dispatch a transaction that inserts `#BLOCKING` with a `pickedCompletion.of({label: \"#BLOCKING\", apply: \"#BLOCKING\"})` annotation, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }` **within one microtask/tick** — not after 300 ms.\n+- [ ] Add a test: \"flushes pending autosave on unmount\". Dispatch an insert for `#BLOCKING` **without** the `pickedCompletion` annotation (i.e. raw typing), unmount the component before 300 ms elapses, assert `mockInvoke` was called with `#BLOCKING`.\n+- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button, then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button).\n+- [ ] Add a test: \"autocomplete accept then remount preserves accepted tag\". Dispatch a doc change to `#blo` (no annotation), wait 400 ms so `#blo` saves, then dispatch a replacement transaction for `#BLOCKING` **with** `pickedCompletion` annotation, then unmount within 100 ms. Assert the final `mockInvoke` call for `perspective.filter` used `#BLOCKING`.\n+- [ ] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n+- [ ] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n\n ## Workflow\n\n"}]]}
{"id":"01KP793E49SXAWH6HR4A9HZ43R","timestamp":"2026-04-15T00:36:47.881312Z","op":"update","item_id":"01KP67K8X863YN5ZSJABMGWCJK","forward_patch":"--- original\n+++ modified\n@@ -32,20 +32,20 @@\n\n ### Implementation\n\n-- [ ] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n+- [x] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n   - Store the pending callback alongside the timer (new `pendingFnRef`).\n   - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback synchronously.\n   - Change the unmount effect from `useEffect(() => cancel, [cancel])` to `useEffect(() => flush, [flush])`.\n   - Keep `cancel` for the clear-button path (`handleClear` must still drop, not flush — clear supersedes any pending save).\n   - Return `{ schedule, cancel, flush }` from the hook.\n-- [ ] In `useFilterDispatch`, expose `flush` through the returned API (call it `handleFlush` or similar) so the editor can invoke it on completion accept.\n-- [ ] In `FilterEditor`, add a CM6 extension that detects completion-accept and triggers `flush`:\n+- [x] In `useFilterDispatch`, expose `flush` through the returned API (call it `handleFlush` or similar) so the editor can invoke it on completion accept.\n+- [x] In `FilterEditor`, add a CM6 extension that detects completion-accept and triggers `flush`:\n   - Import `pickedCompletion` from `@codemirror/autocomplete`.\n   - Build an `EditorView.updateListener` extension that iterates `update.transactions`; if any carries a `pickedCompletion` annotation, call the `flush` callback **after** the transaction settles (use `queueMicrotask` or `setTimeout(0)` so `handleChange` — the debounce-scheduler — fires first on the same doc change; then `flush` runs the just-scheduled callback immediately).\n   - Add this extension to the `extraExtensions` array passed to `TextEditor` alongside `mentionExts`.\n-- [ ] Keep the flush extension in `filter-editor.tsx` — do not add it to `useMentionExtensions`. Mention extensions are reused by other editors (task description, etc.) where immediate-dispatch-on-accept isn't the right behavior. The flush-on-accept is specific to the formula-bar autosave model.\n-- [ ] Verify `handleCommit` (Enter without active completion) already dispatches immediately — it does, via `cancel()` + `apply(text)` — no change needed.\n-- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`.\n+- [x] Keep the flush extension in `filter-editor.tsx` — do not add it to `useMentionExtensions`. Mention extensions are reused by other editors (task description, etc.) where immediate-dispatch-on-accept isn't the right behavior. The flush-on-accept is specific to the formula-bar autosave model.\n+- [x] Verify `handleCommit` (Enter without active completion) already dispatches immediately — it does, via `cancel()` + `apply(text)` — no change needed.\n+- [x] Do not change `handleBlur` behavior in `text-editor.tsx`.\n\n ### Out of scope\n\n@@ -55,21 +55,21 @@\n\n ## Acceptance Criteria\n\n-- [ ] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n-- [ ] Accepting a completion dispatches `perspective.filter` immediately (before the 300 ms debounce elapses) — verifiable because `mockInvoke` is called synchronously after the autocomplete transaction.\n-- [ ] The existing debounced-autosave behavior still works for ordinary typing within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n-- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale text after a clear).\n-- [ ] Enter-to-commit (without active completion) still dispatches immediately.\n-- [ ] Task-description editors and other mention-autocomplete users are unaffected — no immediate-dispatch behavior added outside the formula bar.\n+- [x] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n+- [x] Accepting a completion dispatches `perspective.filter` immediately (before the 300 ms debounce elapses) — verifiable because `mockInvoke` is called synchronously after the autocomplete transaction.\n+- [x] The existing debounced-autosave behavior still works for ordinary typing within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n+- [x] Clearing the filter via the × button still cancels pending saves (does not flush stale text after a clear).\n+- [x] Enter-to-commit (without active completion) still dispatches immediately.\n+- [x] Task-description editors and other mention-autocomplete users are unaffected — no immediate-dispatch behavior added outside the formula bar.\n\n ## Tests\n\n-- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes immediately when a completion is accepted\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`. Acquire the `EditorView`, dispatch a transaction that inserts `#BLOCKING` with a `pickedCompletion.of({label: \"#BLOCKING\", apply: \"#BLOCKING\"})` annotation, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }` **within one microtask/tick** — not after 300 ms.\n-- [ ] Add a test: \"flushes pending autosave on unmount\". Dispatch an insert for `#BLOCKING` **without** the `pickedCompletion` annotation (i.e. raw typing), unmount the component before 300 ms elapses, assert `mockInvoke` was called with `#BLOCKING`.\n-- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button, then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button).\n-- [ ] Add a test: \"autocomplete accept then remount preserves accepted tag\". Dispatch a doc change to `#blo` (no annotation), wait 400 ms so `#blo` saves, then dispatch a replacement transaction for `#BLOCKING` **with** `pickedCompletion` annotation, then unmount within 100 ms. Assert the final `mockInvoke` call for `perspective.filter` used `#BLOCKING`.\n-- [ ] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n-- [ ] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n+- [x] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes immediately when a completion is accepted\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`. Acquire the `EditorView`, dispatch a transaction that inserts `#BLOCKING` with a `pickedCompletion.of({label: \"#BLOCKING\", apply: \"#BLOCKING\"})` annotation, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }` **within one microtask/tick** — not after 300 ms.\n+- [x] Add a test: \"flushes pending autosave on unmount\". Dispatch an insert for `#BLOCKING` **without** the `pickedCompletion` annotation (i.e. raw typing), unmount the component before 300 ms elapses, assert `mockInvoke` was called with `#BLOCKING`.\n+- [x] Add a test: \"does not flush after clear\". Render with a filter, click the clear button, then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button).\n+- [x] Add a test: \"autocomplete accept then remount preserves accepted tag\". Dispatch a doc change to `#blo` (no annotation), wait 400 ms so `#blo` saves, then dispatch a replacement transaction for `#BLOCKING` **with** `pickedCompletion` annotation, then unmount within 100 ms. Assert the final `mockInvoke` call for `perspective.filter` used `#BLOCKING`.\n+- [x] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n+- [x] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -32,20 +32,20 @@\n\n ### Implementation\n\n-- [x] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n+- [ ] In `kanban-app/ui/src/components/filter-editor.tsx`, refactor `useDebouncedTimer`:\n   - Store the pending callback alongside the timer (new `pendingFnRef`).\n   - Add a `flush()` method: if a timer is pending, clear it and invoke the stored callback synchronously.\n   - Change the unmount effect from `useEffect(() => cancel, [cancel])` to `useEffect(() => flush, [flush])`.\n   - Keep `cancel` for the clear-button path (`handleClear` must still drop, not flush — clear supersedes any pending save).\n   - Return `{ schedule, cancel, flush }` from the hook.\n-- [x] In `useFilterDispatch`, expose `flush` through the returned API (call it `handleFlush` or similar) so the editor can invoke it on completion accept.\n-- [x] In `FilterEditor`, add a CM6 extension that detects completion-accept and triggers `flush`:\n+- [ ] In `useFilterDispatch`, expose `flush` through the returned API (call it `handleFlush` or similar) so the editor can invoke it on completion accept.\n+- [ ] In `FilterEditor`, add a CM6 extension that detects completion-accept and triggers `flush`:\n   - Import `pickedCompletion` from `@codemirror/autocomplete`.\n   - Build an `EditorView.updateListener` extension that iterates `update.transactions`; if any carries a `pickedCompletion` annotation, call the `flush` callback **after** the transaction settles (use `queueMicrotask` or `setTimeout(0)` so `handleChange` — the debounce-scheduler — fires first on the same doc change; then `flush` runs the just-scheduled callback immediately).\n   - Add this extension to the `extraExtensions` array passed to `TextEditor` alongside `mentionExts`.\n-- [x] Keep the flush extension in `filter-editor.tsx` — do not add it to `useMentionExtensions`. Mention extensions are reused by other editors (task description, etc.) where immediate-dispatch-on-accept isn't the right behavior. The flush-on-accept is specific to the formula-bar autosave model.\n-- [x] Verify `handleCommit` (Enter without active completion) already dispatches immediately — it does, via `cancel()` + `apply(text)` — no change needed.\n-- [x] Do not change `handleBlur` behavior in `text-editor.tsx`.\n+- [ ] Keep the flush extension in `filter-editor.tsx` — do not add it to `useMentionExtensions`. Mention extensions are reused by other editors (task description, etc.) where immediate-dispatch-on-accept isn't the right behavior. The flush-on-accept is specific to the formula-bar autosave model.\n+- [ ] Verify `handleCommit` (Enter without active completion) already dispatches immediately — it does, via `cancel()` + `apply(text)` — no change needed.\n+- [ ] Do not change `handleBlur` behavior in `text-editor.tsx`.\n\n ### Out of scope\n\n@@ -55,21 +55,21 @@\n\n ## Acceptance Criteria\n\n-- [x] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n-- [x] Accepting a completion dispatches `perspective.filter` immediately (before the 300 ms debounce elapses) — verifiable because `mockInvoke` is called synchronously after the autocomplete transaction.\n-- [x] The existing debounced-autosave behavior still works for ordinary typing within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n-- [x] Clearing the filter via the × button still cancels pending saves (does not flush stale text after a clear).\n-- [x] Enter-to-commit (without active completion) still dispatches immediately.\n-- [x] Task-description editors and other mention-autocomplete users are unaffected — no immediate-dispatch behavior added outside the formula bar.\n+- [ ] After typing `#blo`, accepting `#BLOCKING` from the autocomplete dropdown, and immediately switching perspectives, the saved filter on the originating perspective is `#BLOCKING` (not `#blo`).\n+- [ ] Accepting a completion dispatches `perspective.filter` immediately (before the 300 ms debounce elapses) — verifiable because `mockInvoke` is called synchronously after the autocomplete transaction.\n+- [ ] The existing debounced-autosave behavior still works for ordinary typing within a single perspective (no regressions in `filter-editor.test.tsx` autosave tests).\n+- [ ] Clearing the filter via the × button still cancels pending saves (does not flush stale text after a clear).\n+- [ ] Enter-to-commit (without active completion) still dispatches immediately.\n+- [ ] Task-description editors and other mention-autocomplete users are unaffected — no immediate-dispatch behavior added outside the formula bar.\n\n ## Tests\n\n-- [x] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes immediately when a completion is accepted\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`. Acquire the `EditorView`, dispatch a transaction that inserts `#BLOCKING` with a `pickedCompletion.of({label: \"#BLOCKING\", apply: \"#BLOCKING\"})` annotation, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }` **within one microtask/tick** — not after 300 ms.\n-- [x] Add a test: \"flushes pending autosave on unmount\". Dispatch an insert for `#BLOCKING` **without** the `pickedCompletion` annotation (i.e. raw typing), unmount the component before 300 ms elapses, assert `mockInvoke` was called with `#BLOCKING`.\n-- [x] Add a test: \"does not flush after clear\". Render with a filter, click the clear button, then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button).\n-- [x] Add a test: \"autocomplete accept then remount preserves accepted tag\". Dispatch a doc change to `#blo` (no annotation), wait 400 ms so `#blo` saves, then dispatch a replacement transaction for `#BLOCKING` **with** `pickedCompletion` annotation, then unmount within 100 ms. Assert the final `mockInvoke` call for `perspective.filter` used `#BLOCKING`.\n-- [x] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n-- [x] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n+- [ ] Add a test in `kanban-app/ui/src/components/filter-editor.test.tsx` (sibling to the existing `autosave` describe): \"flushes immediately when a completion is accepted\". Render `<FilterEditor filter=\"\" perspectiveId=\"p1\" />`. Acquire the `EditorView`, dispatch a transaction that inserts `#BLOCKING` with a `pickedCompletion.of({label: \"#BLOCKING\", apply: \"#BLOCKING\"})` annotation, and assert `mockInvoke` was called with `perspective.filter { filter: \"#BLOCKING\", perspective_id: \"p1\" }` **within one microtask/tick** — not after 300 ms.\n+- [ ] Add a test: \"flushes pending autosave on unmount\". Dispatch an insert for `#BLOCKING` **without** the `pickedCompletion` annotation (i.e. raw typing), unmount the component before 300 ms elapses, assert `mockInvoke` was called with `#BLOCKING`.\n+- [ ] Add a test: \"does not flush after clear\". Render with a filter, click the clear button, then unmount. Assert no extra `perspective.filter` dispatch fires on unmount (only the `perspective.clearFilter` from the button).\n+- [ ] Add a test: \"autocomplete accept then remount preserves accepted tag\". Dispatch a doc change to `#blo` (no annotation), wait 400 ms so `#blo` saves, then dispatch a replacement transaction for `#BLOCKING` **with** `pickedCompletion` annotation, then unmount within 100 ms. Assert the final `mockInvoke` call for `perspective.filter` used `#BLOCKING`.\n+- [ ] Run: `cd kanban-app/ui && npm test -- filter-editor` — all tests pass.\n+- [ ] Run the full UI suite: `cd kanban-app/ui && npm test` — no regressions.\n\n ## Workflow\n\n","transaction_id":null}
{"id":"01KP793VJ5TZ07XZ9E1S9V1F3C","timestamp":"2026-04-15T00:37:01.637895Z","op":"update","item_id":"01KP67K8X863YN5ZSJABMGWCJK","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,6 @@\n ---\n assignees: []\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: pill-via-cm6\n title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,6 +1,6 @@\n ---\n assignees: []\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: pill-via-cm6\n title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n","transaction_id":null}
{"id":"01KP793VJ6FMVM68FD1N46FKE9","timestamp":"2026-04-15T00:37:01.638234Z","entity_type":"task","entity_id":"01KP67K8X863YN5ZSJABMGWCJK","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KP79SW9J176Q90PXVB6AY62W","timestamp":"2026-04-15T00:49:03.282013Z","op":"update","item_id":"01KP67K8X863YN5ZSJABMGWCJK","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees: []\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffd180\n project: pill-via-cm6\n title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees: []\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffd180\n+position_column: review\n+position_ordinal: '80'\n project: pill-via-cm6\n title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n ---\n","transaction_id":null}
{"id":"01KP79SW9KF4MEXD0H1AKQM1MV","timestamp":"2026-04-15T00:49:03.283155Z","entity_type":"task","entity_id":"01KP67K8X863YN5ZSJABMGWCJK","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffffffffffffd180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffd180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5NTFMWX3SWKNB1AWEVE7HN","timestamp":"2026-04-26T19:56:18.972001Z","op":"update","item_id":"01KP67K8X863YN5ZSJABMGWCJK","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees: []\n position_column: done\n-position_ordinal: ffffffffffffffffffffffd180\n+position_ordinal: ffffffffffffffffffffffffffffffc480\n project: pill-via-cm6\n title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees: []\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffc480\n+position_ordinal: ffffffffffffffffffffffd180\n project: pill-via-cm6\n title: 'Filter autocomplete: accepted tag lost when perspective is toggled before debounce fires'\n ---\n","transaction_id":null}
{"id":"01KQ5NTFMW1K8EDV4C894D2RYG","timestamp":"2026-04-26T19:56:18.972476Z","entity_type":"task","entity_id":"01KP67K8X863YN5ZSJABMGWCJK","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffd180\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffc480\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffc480\n\\ No newline at end of file\n+ffffffffffffffffffffffd180\n\\ No newline at end of file\n"}]]}
{"id":"01KQZ9BVS08R58QVGCSXEQSFRX","timestamp":"2026-05-06T18:38:52.192629Z","op":"update","item_id":"01KP67K8X863YN5ZSJABMGWCJK","forward_patch":"--- original\n+++ modified\n@@ -73,4 +73,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the four new tests first (they should fail against the current `cancel`-on-unmount + no-accept-detection behavior), then add the `flush` method, the `pickedCompletion` updateListener extension, and the unmount-flush effect to make them pass.\n+- Use `/tdd` — write the four new tests first (they should fail against the current `cancel`-on-unmount + no-accept-detection behavior), then add the `flush` method, the `pickedCompletion` updateListener extension, and the unmount-flush effect to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -73,4 +73,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the four new tests first (they should fail against the current `cancel`-on-unmount + no-accept-detection behavior), then add the `flush` method, the `pickedCompletion` updateListener extension, and the unmount-flush effect to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write the four new tests first (they should fail against the current `cancel`-on-unmount + no-accept-detection behavior), then add the `flush` method, the `pickedCompletion` updateListener extension, and the unmount-flush effect to make them pass.\n","transaction_id":null}
{"id":"01KQZ9BVS45GGAVE277RNBRFWW","timestamp":"2026-05-06T18:38:52.196262Z","entity_type":"task","entity_id":"01KP67K8X863YN5ZSJABMGWCJK","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -66,4 +66,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the four new tests first (they should fail against the current `cancel`-on-unmount + no-accept-detection behavior), then add the `flush` method, the `pickedCompletion` updateListener extension, and the unmount-flush effect to make them pass.\n+- Use `/tdd` — write the four new tests first (they should fail against the current `cancel`-on-unmount + no-accept-detection behavior), then add the `flush` method, the `pickedCompletion` updateListener extension, and the unmount-flush effect to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -66,4 +66,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the four new tests first (they should fail against the current `cancel`-on-unmount + no-accept-detection behavior), then add the `flush` method, the `pickedCompletion` updateListener extension, and the unmount-flush effect to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write the four new tests first (they should fail against the current `cancel`-on-unmount + no-accept-detection behavior), then add the `flush` method, the `pickedCompletion` updateListener extension, and the unmount-flush effect to make them pass.\n"}]]}

{"id":"01KPZMXXEX9Y4ZS0XSHEY2XYJV","timestamp":"2026-04-24T11:45:16.253716Z","op":"create","item_id":"01KPZMXXEXKVE3RNPA4XJP0105","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,47 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: ff8980\n+title: Replace perspective.goto / view.switch indirection with direct .set commands\n+---\n+## What\n+\n+PR #40 review comment from @wballard on `kanban-app/src/commands.rs:1320`:\n+\n+> seems like trash, why not just make the correct perspective.set command to begin with?\n+\n+Ref: https://github.com/swissarmyhammer/swissarmyhammer/pull/40#discussion_r3137358696\n+\n+Currently `match_dynamic_prefix` in `kanban-app/src/commands.rs` rewrites:\n+\n+- `view.switch:{id}` → `view.set` with `view_id: {id}`\n+- `perspective.goto:{id}` → `perspective.set` with `perspective_id: {id}`\n+\n+The rewrite layer exists because the **dynamic palette entries** are emitted as `view.switch:{id}` and `perspective.goto:{id}` in `swissarmyhammer-kanban/src/scope_commands.rs` (`emit_view_switch`, `emit_perspective_goto`), then translated back into the canonical `*.set` commands.\n+\n+Reviewer's critique: drop the indirection. Have the palette emit dynamic entries that dispatch `view.set` / `perspective.set` directly with the right args pre-filled. No rewrite needed.\n+\n+### Files to modify\n+\n+- `swissarmyhammer-kanban/src/scope_commands.rs` — `emit_view_switch` / `emit_perspective_goto` emit entries whose `cmd` is the real `view.set` / `perspective.set` and whose `args` carry the resolved `{view_id, ...}` / `{perspective_id, ...}`.\n+- `kanban-app/src/commands.rs::match_dynamic_prefix` — delete the two rewrite branches; `board.switch:*` stays (different — it resolves a file path into `file.switchBoard`).\n+- Any test exercising `view.switch:*` / `perspective.goto:*` as command ids.\n+\n+## Acceptance Criteria\n+\n+- [ ] `rg 'view\\.switch:|perspective\\.goto:'` across the codebase returns only palette-display plumbing, with no live command-dispatch sites; or zero hits if the dynamic emitter now produces the canonical id directly.\n+- [ ] `match_dynamic_prefix` no longer rewrites `view.switch:*` or `perspective.goto:*`; the function body keeps only the `board.switch:*` path (and any non-moved rewrites).\n+- [ ] Clicking a view icon in the left-nav and invoking \"Switch to X\" from the palette both still trigger `view.set` with the right `view_id` in args.\n+- [ ] Clicking a perspective tab and invoking \"Go to X\" from the palette both still trigger `perspective.set` with the right `perspective_id`.\n+- [ ] `cargo test -p swissarmyhammer-kanban -p kanban-app` passes.\n+\n+## Tests\n+\n+- [ ] Update existing `emit_view_switch` / `emit_perspective_goto` tests in `swissarmyhammer-kanban/src/scope_commands.rs` to assert the emitted entries carry `cmd: \"view.set\"` / `cmd: \"perspective.set\"` with pre-filled args, not the old `view.switch:*` / `perspective.goto:*` ids.\n+- [ ] Delete or update the `match_dynamic_prefix_view_switch_rewrites_to_view_set` / `match_dynamic_prefix_perspective_goto_rewrites_to_perspective_set` tests in `kanban-app/src/commands.rs` — the rewrite branch is gone, so the test's premise goes away.\n+- [ ] Frontend: `kanban-app/ui/src/lib/views-context.tsx` and `perspective-context.tsx` already dispatch `view.set` / `perspective.set`; verify their tests still pass unchanged.\n+\n+## Workflow\n+\n+Use `/tdd`. Start by flipping the palette-emission test to assert the canonical command id, watch it fail, update `emit_view_switch` / `emit_perspective_goto` to produce it, then drop the now-dead rewrite branches. #commands #refactor #architecture\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,47 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: ff8980\n-title: Replace perspective.goto / view.switch indirection with direct .set commands\n----\n-## What\n-\n-PR #40 review comment from @wballard on `kanban-app/src/commands.rs:1320`:\n-\n-> seems like trash, why not just make the correct perspective.set command to begin with?\n-\n-Ref: https://github.com/swissarmyhammer/swissarmyhammer/pull/40#discussion_r3137358696\n-\n-Currently `match_dynamic_prefix` in `kanban-app/src/commands.rs` rewrites:\n-\n-- `view.switch:{id}` → `view.set` with `view_id: {id}`\n-- `perspective.goto:{id}` → `perspective.set` with `perspective_id: {id}`\n-\n-The rewrite layer exists because the **dynamic palette entries** are emitted as `view.switch:{id}` and `perspective.goto:{id}` in `swissarmyhammer-kanban/src/scope_commands.rs` (`emit_view_switch`, `emit_perspective_goto`), then translated back into the canonical `*.set` commands.\n-\n-Reviewer's critique: drop the indirection. Have the palette emit dynamic entries that dispatch `view.set` / `perspective.set` directly with the right args pre-filled. No rewrite needed.\n-\n-### Files to modify\n-\n-- `swissarmyhammer-kanban/src/scope_commands.rs` — `emit_view_switch` / `emit_perspective_goto` emit entries whose `cmd` is the real `view.set` / `perspective.set` and whose `args` carry the resolved `{view_id, ...}` / `{perspective_id, ...}`.\n-- `kanban-app/src/commands.rs::match_dynamic_prefix` — delete the two rewrite branches; `board.switch:*` stays (different — it resolves a file path into `file.switchBoard`).\n-- Any test exercising `view.switch:*` / `perspective.goto:*` as command ids.\n-\n-## Acceptance Criteria\n-\n-- [ ] `rg 'view\\.switch:|perspective\\.goto:'` across the codebase returns only palette-display plumbing, with no live command-dispatch sites; or zero hits if the dynamic emitter now produces the canonical id directly.\n-- [ ] `match_dynamic_prefix` no longer rewrites `view.switch:*` or `perspective.goto:*`; the function body keeps only the `board.switch:*` path (and any non-moved rewrites).\n-- [ ] Clicking a view icon in the left-nav and invoking \"Switch to X\" from the palette both still trigger `view.set` with the right `view_id` in args.\n-- [ ] Clicking a perspective tab and invoking \"Go to X\" from the palette both still trigger `perspective.set` with the right `perspective_id`.\n-- [ ] `cargo test -p swissarmyhammer-kanban -p kanban-app` passes.\n-\n-## Tests\n-\n-- [ ] Update existing `emit_view_switch` / `emit_perspective_goto` tests in `swissarmyhammer-kanban/src/scope_commands.rs` to assert the emitted entries carry `cmd: \"view.set\"` / `cmd: \"perspective.set\"` with pre-filled args, not the old `view.switch:*` / `perspective.goto:*` ids.\n-- [ ] Delete or update the `match_dynamic_prefix_view_switch_rewrites_to_view_set` / `match_dynamic_prefix_perspective_goto_rewrites_to_perspective_set` tests in `kanban-app/src/commands.rs` — the rewrite branch is gone, so the test's premise goes away.\n-- [ ] Frontend: `kanban-app/ui/src/lib/views-context.tsx` and `perspective-context.tsx` already dispatch `view.set` / `perspective.set`; verify their tests still pass unchanged.\n-\n-## Workflow\n-\n-Use `/tdd`. Start by flipping the palette-emission test to assert the canonical command id, watch it fail, update `emit_view_switch` / `emit_perspective_goto` to produce it, then drop the now-dead rewrite branches. #commands #refactor #architecture\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPZMXXEZZTRE04VRFE6285SD","timestamp":"2026-04-24T11:45:16.255564Z","entity_type":"task","entity_id":"01KPZMXXEXKVE3RNPA4XJP0105","op":"create","changes":[["position_ordinal",{"kind":"set","value":"ff8980"}],["position_column",{"kind":"set","value":"todo"}],["assignees",{"kind":"set","value":["claude-code"]}],["title",{"kind":"set","value":"Replace perspective.goto / view.switch indirection with direct .set commands"}],["body",{"kind":"set","value":"## What\n\nPR #40 review comment from @wballard on `kanban-app/src/commands.rs:1320`:\n\n> seems like trash, why not just make the correct perspective.set command to begin with?\n\nRef: https://github.com/swissarmyhammer/swissarmyhammer/pull/40#discussion_r3137358696\n\nCurrently `match_dynamic_prefix` in `kanban-app/src/commands.rs` rewrites:\n\n- `view.switch:{id}` → `view.set` with `view_id: {id}`\n- `perspective.goto:{id}` → `perspective.set` with `perspective_id: {id}`\n\nThe rewrite layer exists because the **dynamic palette entries** are emitted as `view.switch:{id}` and `perspective.goto:{id}` in `swissarmyhammer-kanban/src/scope_commands.rs` (`emit_view_switch`, `emit_perspective_goto`), then translated back into the canonical `*.set` commands.\n\nReviewer's critique: drop the indirection. Have the palette emit dynamic entries that dispatch `view.set` / `perspective.set` directly with the right args pre-filled. No rewrite needed.\n\n### Files to modify\n\n- `swissarmyhammer-kanban/src/scope_commands.rs` — `emit_view_switch` / `emit_perspective_goto` emit entries whose `cmd` is the real `view.set` / `perspective.set` and whose `args` carry the resolved `{view_id, ...}` / `{perspective_id, ...}`.\n- `kanban-app/src/commands.rs::match_dynamic_prefix` — delete the two rewrite branches; `board.switch:*` stays (different — it resolves a file path into `file.switchBoard`).\n- Any test exercising `view.switch:*` / `perspective.goto:*` as command ids.\n\n## Acceptance Criteria\n\n- [ ] `rg 'view\\.switch:|perspective\\.goto:'` across the codebase returns only palette-display plumbing, with no live command-dispatch sites; or zero hits if the dynamic emitter now produces the canonical id directly.\n- [ ] `match_dynamic_prefix` no longer rewrites `view.switch:*` or `perspective.goto:*`; the function body keeps only the `board.switch:*` path (and any non-moved rewrites).\n- [ ] Clicking a view icon in the left-nav and invoking \"Switch to X\" from the palette both still trigger `view.set` with the right `view_id` in args.\n- [ ] Clicking a perspective tab and invoking \"Go to X\" from the palette both still trigger `perspective.set` with the right `perspective_id`.\n- [ ] `cargo test -p swissarmyhammer-kanban -p kanban-app` passes.\n\n## Tests\n\n- [ ] Update existing `emit_view_switch` / `emit_perspective_goto` tests in `swissarmyhammer-kanban/src/scope_commands.rs` to assert the emitted entries carry `cmd: \"view.set\"` / `cmd: \"perspective.set\"` with pre-filled args, not the old `view.switch:*` / `perspective.goto:*` ids.\n- [ ] Delete or update the `match_dynamic_prefix_view_switch_rewrites_to_view_set` / `match_dynamic_prefix_perspective_goto_rewrites_to_perspective_set` tests in `kanban-app/src/commands.rs` — the rewrite branch is gone, so the test's premise goes away.\n- [ ] Frontend: `kanban-app/ui/src/lib/views-context.tsx` and `perspective-context.tsx` already dispatch `view.set` / `perspective.set`; verify their tests still pass unchanged.\n\n## Workflow\n\nUse `/tdd`. Start by flipping the palette-emission test to assert the canonical command id, watch it fail, update `emit_view_switch` / `emit_perspective_goto` to produce it, then drop the now-dead rewrite branches. #commands #refactor #architecture"}]]}
{"id":"01KPZVDKEPNV932KGKTH1KX1C0","timestamp":"2026-04-24T13:38:41.750017Z","op":"update","item_id":"01KPZMXXEXKVE3RNPA4XJP0105","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: ff8980\n+position_column: doing\n+position_ordinal: '80'\n title: Replace perspective.goto / view.switch indirection with direct .set commands\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: ff8980\n title: Replace perspective.goto / view.switch indirection with direct .set commands\n ---\n ## What\n","transaction_id":null}
{"id":"01KPZVDKEQ1KZ5EER0VR8Z3Q19","timestamp":"2026-04-24T13:38:41.751317Z","entity_type":"task","entity_id":"01KPZMXXEXKVE3RNPA4XJP0105","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ff8980\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ff8980\n\\ No newline at end of file\n"}]]}
{"id":"01KPZX6ZFZVWA37P6XS0Z5BGRR","timestamp":"2026-04-24T14:10:01.855765Z","op":"update","item_id":"01KPZMXXEXKVE3RNPA4XJP0105","forward_patch":"--- original\n+++ modified\n@@ -30,18 +30,30 @@\n\n ## Acceptance Criteria\n\n-- [ ] `rg 'view\\.switch:|perspective\\.goto:'` across the codebase returns only palette-display plumbing, with no live command-dispatch sites; or zero hits if the dynamic emitter now produces the canonical id directly.\n-- [ ] `match_dynamic_prefix` no longer rewrites `view.switch:*` or `perspective.goto:*`; the function body keeps only the `board.switch:*` path (and any non-moved rewrites).\n-- [ ] Clicking a view icon in the left-nav and invoking \"Switch to X\" from the palette both still trigger `view.set` with the right `view_id` in args.\n-- [ ] Clicking a perspective tab and invoking \"Go to X\" from the palette both still trigger `perspective.set` with the right `perspective_id`.\n-- [ ] `cargo test -p swissarmyhammer-kanban -p kanban-app` passes.\n+- [x] `rg 'view\\.switch:|perspective\\.goto:'` across the codebase returns only palette-display plumbing, with no live command-dispatch sites; or zero hits if the dynamic emitter now produces the canonical id directly.\n+- [x] `match_dynamic_prefix` no longer rewrites `view.switch:*` or `perspective.goto:*`; the function body keeps only the `board.switch:*` path (and any non-moved rewrites).\n+- [x] Clicking a view icon in the left-nav and invoking \"Switch to X\" from the palette both still trigger `view.set` with the right `view_id` in args.\n+- [x] Clicking a perspective tab and invoking \"Go to X\" from the palette both still trigger `perspective.set` with the right `perspective_id`.\n+- [x] `cargo test -p swissarmyhammer-kanban -p kanban-app` passes.\n\n ## Tests\n\n-- [ ] Update existing `emit_view_switch` / `emit_perspective_goto` tests in `swissarmyhammer-kanban/src/scope_commands.rs` to assert the emitted entries carry `cmd: \"view.set\"` / `cmd: \"perspective.set\"` with pre-filled args, not the old `view.switch:*` / `perspective.goto:*` ids.\n-- [ ] Delete or update the `match_dynamic_prefix_view_switch_rewrites_to_view_set` / `match_dynamic_prefix_perspective_goto_rewrites_to_perspective_set` tests in `kanban-app/src/commands.rs` — the rewrite branch is gone, so the test's premise goes away.\n-- [ ] Frontend: `kanban-app/ui/src/lib/views-context.tsx` and `perspective-context.tsx` already dispatch `view.set` / `perspective.set`; verify their tests still pass unchanged.\n+- [x] Update existing `emit_view_switch` / `emit_perspective_goto` tests in `swissarmyhammer-kanban/src/scope_commands.rs` to assert the emitted entries carry `cmd: \"view.set\"` / `cmd: \"perspective.set\"` with pre-filled args, not the old `view.switch:*` / `perspective.goto:*` ids.\n+- [x] Delete or update the `match_dynamic_prefix_view_switch_rewrites_to_view_set` / `match_dynamic_prefix_perspective_goto_rewrites_to_perspective_set` tests in `kanban-app/src/commands.rs` — the rewrite branch is gone, so the test's premise goes away.\n+- [x] Frontend: `kanban-app/ui/src/lib/views-context.tsx` and `perspective-context.tsx` already dispatch `view.set` / `perspective.set`; verify their tests still pass unchanged.\n\n ## Workflow\n\n-Use `/tdd`. Start by flipping the palette-emission test to assert the canonical command id, watch it fail, update `emit_view_switch` / `emit_perspective_goto` to produce it, then drop the now-dead rewrite branches. #commands #refactor #architecture\n\\ No newline at end of file\n+Use `/tdd`. Start by flipping the palette-emission test to assert the canonical command id, watch it fail, update `emit_view_switch` / `emit_perspective_goto` to produce it, then drop the now-dead rewrite branches. #commands #refactor #architecture\n+\n+## Implementation Notes\n+\n+Done. Summary of the change:\n+\n+1. `ResolvedCommand` gained an `args: Option<serde_json::Value>` field. Cross-emitter dedup (`SeenKey`) now keys on `(id, target, args)`; `dedupe_by_id` (the final pass) now keys on `(id, args)` so fan-out rows that share an id but differ by args stay distinct instead of collapsing to one entry.\n+2. `emit_view_switch` now emits rows with `id == \"view.set\"` and `args == { \"view_id\": view.id }`. `emit_perspective_goto` mirrors that with `perspective.set` + `perspective_id`.\n+3. `match_dynamic_prefix` lost its `view.switch:*` and `perspective.goto:*` branches — only `board.switch:*` and `entity.add:*` remain.\n+4. Frontend `CommandPalette` forwards `cmd.args` through the dispatch path; `left-nav.tsx` now dispatches `view.set` with `{ view_id }` directly; `views-container.tsx` keeps the `view.switch:{id}` id as a client-side scope-map key (for React / dedup uniqueness) but the execute handler dispatches `view.set` with args.\n+5. Updated tests: `view_switch_commands_emit_canonical_view_set_with_args` and `perspective_goto_commands_emit_canonical_perspective_set_with_args` assert the new wire format; `match_dynamic_prefix_no_longer_rewrites_view_switch_or_perspective_goto` is the regression guard.\n+\n+Full cargo + vitest + clippy + tsc all clean.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -30,30 +30,18 @@\n\n ## Acceptance Criteria\n\n-- [x] `rg 'view\\.switch:|perspective\\.goto:'` across the codebase returns only palette-display plumbing, with no live command-dispatch sites; or zero hits if the dynamic emitter now produces the canonical id directly.\n-- [x] `match_dynamic_prefix` no longer rewrites `view.switch:*` or `perspective.goto:*`; the function body keeps only the `board.switch:*` path (and any non-moved rewrites).\n-- [x] Clicking a view icon in the left-nav and invoking \"Switch to X\" from the palette both still trigger `view.set` with the right `view_id` in args.\n-- [x] Clicking a perspective tab and invoking \"Go to X\" from the palette both still trigger `perspective.set` with the right `perspective_id`.\n-- [x] `cargo test -p swissarmyhammer-kanban -p kanban-app` passes.\n+- [ ] `rg 'view\\.switch:|perspective\\.goto:'` across the codebase returns only palette-display plumbing, with no live command-dispatch sites; or zero hits if the dynamic emitter now produces the canonical id directly.\n+- [ ] `match_dynamic_prefix` no longer rewrites `view.switch:*` or `perspective.goto:*`; the function body keeps only the `board.switch:*` path (and any non-moved rewrites).\n+- [ ] Clicking a view icon in the left-nav and invoking \"Switch to X\" from the palette both still trigger `view.set` with the right `view_id` in args.\n+- [ ] Clicking a perspective tab and invoking \"Go to X\" from the palette both still trigger `perspective.set` with the right `perspective_id`.\n+- [ ] `cargo test -p swissarmyhammer-kanban -p kanban-app` passes.\n\n ## Tests\n\n-- [x] Update existing `emit_view_switch` / `emit_perspective_goto` tests in `swissarmyhammer-kanban/src/scope_commands.rs` to assert the emitted entries carry `cmd: \"view.set\"` / `cmd: \"perspective.set\"` with pre-filled args, not the old `view.switch:*` / `perspective.goto:*` ids.\n-- [x] Delete or update the `match_dynamic_prefix_view_switch_rewrites_to_view_set` / `match_dynamic_prefix_perspective_goto_rewrites_to_perspective_set` tests in `kanban-app/src/commands.rs` — the rewrite branch is gone, so the test's premise goes away.\n-- [x] Frontend: `kanban-app/ui/src/lib/views-context.tsx` and `perspective-context.tsx` already dispatch `view.set` / `perspective.set`; verify their tests still pass unchanged.\n+- [ ] Update existing `emit_view_switch` / `emit_perspective_goto` tests in `swissarmyhammer-kanban/src/scope_commands.rs` to assert the emitted entries carry `cmd: \"view.set\"` / `cmd: \"perspective.set\"` with pre-filled args, not the old `view.switch:*` / `perspective.goto:*` ids.\n+- [ ] Delete or update the `match_dynamic_prefix_view_switch_rewrites_to_view_set` / `match_dynamic_prefix_perspective_goto_rewrites_to_perspective_set` tests in `kanban-app/src/commands.rs` — the rewrite branch is gone, so the test's premise goes away.\n+- [ ] Frontend: `kanban-app/ui/src/lib/views-context.tsx` and `perspective-context.tsx` already dispatch `view.set` / `perspective.set`; verify their tests still pass unchanged.\n\n ## Workflow\n\n-Use `/tdd`. Start by flipping the palette-emission test to assert the canonical command id, watch it fail, update `emit_view_switch` / `emit_perspective_goto` to produce it, then drop the now-dead rewrite branches. #commands #refactor #architecture\n-\n-## Implementation Notes\n-\n-Done. Summary of the change:\n-\n-1. `ResolvedCommand` gained an `args: Option<serde_json::Value>` field. Cross-emitter dedup (`SeenKey`) now keys on `(id, target, args)`; `dedupe_by_id` (the final pass) now keys on `(id, args)` so fan-out rows that share an id but differ by args stay distinct instead of collapsing to one entry.\n-2. `emit_view_switch` now emits rows with `id == \"view.set\"` and `args == { \"view_id\": view.id }`. `emit_perspective_goto` mirrors that with `perspective.set` + `perspective_id`.\n-3. `match_dynamic_prefix` lost its `view.switch:*` and `perspective.goto:*` branches — only `board.switch:*` and `entity.add:*` remain.\n-4. Frontend `CommandPalette` forwards `cmd.args` through the dispatch path; `left-nav.tsx` now dispatches `view.set` with `{ view_id }` directly; `views-container.tsx` keeps the `view.switch:{id}` id as a client-side scope-map key (for React / dedup uniqueness) but the execute handler dispatches `view.set` with args.\n-5. Updated tests: `view_switch_commands_emit_canonical_view_set_with_args` and `perspective_goto_commands_emit_canonical_perspective_set_with_args` assert the new wire format; `match_dynamic_prefix_no_longer_rewrites_view_switch_or_perspective_goto` is the regression guard.\n-\n-Full cargo + vitest + clippy + tsc all clean.\n\\ No newline at end of file\n+Use `/tdd`. Start by flipping the palette-emission test to assert the canonical command id, watch it fail, update `emit_view_switch` / `emit_perspective_goto` to produce it, then drop the now-dead rewrite branches. #commands #refactor #architecture\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPZX6ZG0JHGPY4MQ96YTY4D8","timestamp":"2026-04-24T14:10:01.856607Z","entity_type":"task","entity_id":"01KPZMXXEXKVE3RNPA4XJP0105","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -23,18 +23,30 @@\n\n ## Acceptance Criteria\n\n-- [ ] `rg 'view\\.switch:|perspective\\.goto:'` across the codebase returns only palette-display plumbing, with no live command-dispatch sites; or zero hits if the dynamic emitter now produces the canonical id directly.\n-- [ ] `match_dynamic_prefix` no longer rewrites `view.switch:*` or `perspective.goto:*`; the function body keeps only the `board.switch:*` path (and any non-moved rewrites).\n-- [ ] Clicking a view icon in the left-nav and invoking \"Switch to X\" from the palette both still trigger `view.set` with the right `view_id` in args.\n-- [ ] Clicking a perspective tab and invoking \"Go to X\" from the palette both still trigger `perspective.set` with the right `perspective_id`.\n-- [ ] `cargo test -p swissarmyhammer-kanban -p kanban-app` passes.\n+- [x] `rg 'view\\.switch:|perspective\\.goto:'` across the codebase returns only palette-display plumbing, with no live command-dispatch sites; or zero hits if the dynamic emitter now produces the canonical id directly.\n+- [x] `match_dynamic_prefix` no longer rewrites `view.switch:*` or `perspective.goto:*`; the function body keeps only the `board.switch:*` path (and any non-moved rewrites).\n+- [x] Clicking a view icon in the left-nav and invoking \"Switch to X\" from the palette both still trigger `view.set` with the right `view_id` in args.\n+- [x] Clicking a perspective tab and invoking \"Go to X\" from the palette both still trigger `perspective.set` with the right `perspective_id`.\n+- [x] `cargo test -p swissarmyhammer-kanban -p kanban-app` passes.\n\n ## Tests\n\n-- [ ] Update existing `emit_view_switch` / `emit_perspective_goto` tests in `swissarmyhammer-kanban/src/scope_commands.rs` to assert the emitted entries carry `cmd: \"view.set\"` / `cmd: \"perspective.set\"` with pre-filled args, not the old `view.switch:*` / `perspective.goto:*` ids.\n-- [ ] Delete or update the `match_dynamic_prefix_view_switch_rewrites_to_view_set` / `match_dynamic_prefix_perspective_goto_rewrites_to_perspective_set` tests in `kanban-app/src/commands.rs` — the rewrite branch is gone, so the test's premise goes away.\n-- [ ] Frontend: `kanban-app/ui/src/lib/views-context.tsx` and `perspective-context.tsx` already dispatch `view.set` / `perspective.set`; verify their tests still pass unchanged.\n+- [x] Update existing `emit_view_switch` / `emit_perspective_goto` tests in `swissarmyhammer-kanban/src/scope_commands.rs` to assert the emitted entries carry `cmd: \"view.set\"` / `cmd: \"perspective.set\"` with pre-filled args, not the old `view.switch:*` / `perspective.goto:*` ids.\n+- [x] Delete or update the `match_dynamic_prefix_view_switch_rewrites_to_view_set` / `match_dynamic_prefix_perspective_goto_rewrites_to_perspective_set` tests in `kanban-app/src/commands.rs` — the rewrite branch is gone, so the test's premise goes away.\n+- [x] Frontend: `kanban-app/ui/src/lib/views-context.tsx` and `perspective-context.tsx` already dispatch `view.set` / `perspective.set`; verify their tests still pass unchanged.\n\n ## Workflow\n\n-Use `/tdd`. Start by flipping the palette-emission test to assert the canonical command id, watch it fail, update `emit_view_switch` / `emit_perspective_goto` to produce it, then drop the now-dead rewrite branches. #commands #refactor #architecture\n\\ No newline at end of file\n+Use `/tdd`. Start by flipping the palette-emission test to assert the canonical command id, watch it fail, update `emit_view_switch` / `emit_perspective_goto` to produce it, then drop the now-dead rewrite branches. #commands #refactor #architecture\n+\n+## Implementation Notes\n+\n+Done. Summary of the change:\n+\n+1. `ResolvedCommand` gained an `args: Option<serde_json::Value>` field. Cross-emitter dedup (`SeenKey`) now keys on `(id, target, args)`; `dedupe_by_id` (the final pass) now keys on `(id, args)` so fan-out rows that share an id but differ by args stay distinct instead of collapsing to one entry.\n+2. `emit_view_switch` now emits rows with `id == \"view.set\"` and `args == { \"view_id\": view.id }`. `emit_perspective_goto` mirrors that with `perspective.set` + `perspective_id`.\n+3. `match_dynamic_prefix` lost its `view.switch:*` and `perspective.goto:*` branches — only `board.switch:*` and `entity.add:*` remain.\n+4. Frontend `CommandPalette` forwards `cmd.args` through the dispatch path; `left-nav.tsx` now dispatches `view.set` with `{ view_id }` directly; `views-container.tsx` keeps the `view.switch:{id}` id as a client-side scope-map key (for React / dedup uniqueness) but the execute handler dispatches `view.set` with args.\n+5. Updated tests: `view_switch_commands_emit_canonical_view_set_with_args` and `perspective_goto_commands_emit_canonical_perspective_set_with_args` assert the new wire format; `match_dynamic_prefix_no_longer_rewrites_view_switch_or_perspective_goto` is the regression guard.\n+\n+Full cargo + vitest + clippy + tsc all clean.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -23,30 +23,18 @@\n\n ## Acceptance Criteria\n\n-- [x] `rg 'view\\.switch:|perspective\\.goto:'` across the codebase returns only palette-display plumbing, with no live command-dispatch sites; or zero hits if the dynamic emitter now produces the canonical id directly.\n-- [x] `match_dynamic_prefix` no longer rewrites `view.switch:*` or `perspective.goto:*`; the function body keeps only the `board.switch:*` path (and any non-moved rewrites).\n-- [x] Clicking a view icon in the left-nav and invoking \"Switch to X\" from the palette both still trigger `view.set` with the right `view_id` in args.\n-- [x] Clicking a perspective tab and invoking \"Go to X\" from the palette both still trigger `perspective.set` with the right `perspective_id`.\n-- [x] `cargo test -p swissarmyhammer-kanban -p kanban-app` passes.\n+- [ ] `rg 'view\\.switch:|perspective\\.goto:'` across the codebase returns only palette-display plumbing, with no live command-dispatch sites; or zero hits if the dynamic emitter now produces the canonical id directly.\n+- [ ] `match_dynamic_prefix` no longer rewrites `view.switch:*` or `perspective.goto:*`; the function body keeps only the `board.switch:*` path (and any non-moved rewrites).\n+- [ ] Clicking a view icon in the left-nav and invoking \"Switch to X\" from the palette both still trigger `view.set` with the right `view_id` in args.\n+- [ ] Clicking a perspective tab and invoking \"Go to X\" from the palette both still trigger `perspective.set` with the right `perspective_id`.\n+- [ ] `cargo test -p swissarmyhammer-kanban -p kanban-app` passes.\n\n ## Tests\n\n-- [x] Update existing `emit_view_switch` / `emit_perspective_goto` tests in `swissarmyhammer-kanban/src/scope_commands.rs` to assert the emitted entries carry `cmd: \"view.set\"` / `cmd: \"perspective.set\"` with pre-filled args, not the old `view.switch:*` / `perspective.goto:*` ids.\n-- [x] Delete or update the `match_dynamic_prefix_view_switch_rewrites_to_view_set` / `match_dynamic_prefix_perspective_goto_rewrites_to_perspective_set` tests in `kanban-app/src/commands.rs` — the rewrite branch is gone, so the test's premise goes away.\n-- [x] Frontend: `kanban-app/ui/src/lib/views-context.tsx` and `perspective-context.tsx` already dispatch `view.set` / `perspective.set`; verify their tests still pass unchanged.\n+- [ ] Update existing `emit_view_switch` / `emit_perspective_goto` tests in `swissarmyhammer-kanban/src/scope_commands.rs` to assert the emitted entries carry `cmd: \"view.set\"` / `cmd: \"perspective.set\"` with pre-filled args, not the old `view.switch:*` / `perspective.goto:*` ids.\n+- [ ] Delete or update the `match_dynamic_prefix_view_switch_rewrites_to_view_set` / `match_dynamic_prefix_perspective_goto_rewrites_to_perspective_set` tests in `kanban-app/src/commands.rs` — the rewrite branch is gone, so the test's premise goes away.\n+- [ ] Frontend: `kanban-app/ui/src/lib/views-context.tsx` and `perspective-context.tsx` already dispatch `view.set` / `perspective.set`; verify their tests still pass unchanged.\n\n ## Workflow\n\n-Use `/tdd`. Start by flipping the palette-emission test to assert the canonical command id, watch it fail, update `emit_view_switch` / `emit_perspective_goto` to produce it, then drop the now-dead rewrite branches. #commands #refactor #architecture\n-\n-## Implementation Notes\n-\n-Done. Summary of the change:\n-\n-1. `ResolvedCommand` gained an `args: Option<serde_json::Value>` field. Cross-emitter dedup (`SeenKey`) now keys on `(id, target, args)`; `dedupe_by_id` (the final pass) now keys on `(id, args)` so fan-out rows that share an id but differ by args stay distinct instead of collapsing to one entry.\n-2. `emit_view_switch` now emits rows with `id == \"view.set\"` and `args == { \"view_id\": view.id }`. `emit_perspective_goto` mirrors that with `perspective.set` + `perspective_id`.\n-3. `match_dynamic_prefix` lost its `view.switch:*` and `perspective.goto:*` branches — only `board.switch:*` and `entity.add:*` remain.\n-4. Frontend `CommandPalette` forwards `cmd.args` through the dispatch path; `left-nav.tsx` now dispatches `view.set` with `{ view_id }` directly; `views-container.tsx` keeps the `view.switch:{id}` id as a client-side scope-map key (for React / dedup uniqueness) but the execute handler dispatches `view.set` with args.\n-5. Updated tests: `view_switch_commands_emit_canonical_view_set_with_args` and `perspective_goto_commands_emit_canonical_perspective_set_with_args` assert the new wire format; `match_dynamic_prefix_no_longer_rewrites_view_switch_or_perspective_goto` is the regression guard.\n-\n-Full cargo + vitest + clippy + tsc all clean.\n\\ No newline at end of file\n+Use `/tdd`. Start by flipping the palette-emission test to assert the canonical command id, watch it fail, update `emit_view_switch` / `emit_perspective_goto` to produce it, then drop the now-dead rewrite branches. #commands #refactor #architecture\n\\ No newline at end of file\n"}]]}
{"id":"01KPZX7JQAZJWQ4ME6BGVF6M9V","timestamp":"2026-04-24T14:10:21.546388Z","op":"update","item_id":"01KPZMXXEXKVE3RNPA4XJP0105","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8380'\n title: Replace perspective.goto / view.switch indirection with direct .set commands\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '80'\n title: Replace perspective.goto / view.switch indirection with direct .set commands\n ---\n ## What\n","transaction_id":null}
{"id":"01KPZX7JQAW0BVTDFKH30MAV08","timestamp":"2026-04-24T14:10:21.546778Z","entity_type":"task","entity_id":"01KPZMXXEXKVE3RNPA4XJP0105","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KPZY6008GJH75RYYM3RCR43W","timestamp":"2026-04-24T14:26:58.184238Z","op":"update","item_id":"01KPZMXXEXKVE3RNPA4XJP0105","forward_patch":"--- original\n+++ modified\n@@ -56,4 +56,9 @@\n 4. Frontend `CommandPalette` forwards `cmd.args` through the dispatch path; `left-nav.tsx` now dispatches `view.set` with `{ view_id }` directly; `views-container.tsx` keeps the `view.switch:{id}` id as a client-side scope-map key (for React / dedup uniqueness) but the execute handler dispatches `view.set` with args.\n 5. Updated tests: `view_switch_commands_emit_canonical_view_set_with_args` and `perspective_goto_commands_emit_canonical_perspective_set_with_args` assert the new wire format; `match_dynamic_prefix_no_longer_rewrites_view_switch_or_perspective_goto` is the regression guard.\n\n-Full cargo + vitest + clippy + tsc all clean.\n\\ No newline at end of file\n+Full cargo + vitest + clippy + tsc all clean.\n+\n+## Review Findings (2026-04-24 09:26)\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/lib/context-menu.ts:14-22` — The `args` field's doc comment on `ResolvedCommand` claims \"the field is forwarded here too so the context-menu → dispatch pipeline stays symmetric with the palette path\", but the actual `ContextMenuItem` shape (both TS line 27-33 and Rust `kanban-app/src/commands.rs:2036`) has no `args` field, and the loop at lines 63-80 does not propagate it. All current fan-out rows are `context_menu: false` so this never fires in production today, but the comment misleads and a future fan-out row opted into the context menu would silently drop its `args`. Fix: either (a) correct the comment to state args is intentionally dropped because all current fan-out rows are palette-only, or (b) actually add `args` to `ContextMenuItem` on both sides and forward it through `show_context_menu` so the symmetry claim becomes true.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -56,9 +56,4 @@\n 4. Frontend `CommandPalette` forwards `cmd.args` through the dispatch path; `left-nav.tsx` now dispatches `view.set` with `{ view_id }` directly; `views-container.tsx` keeps the `view.switch:{id}` id as a client-side scope-map key (for React / dedup uniqueness) but the execute handler dispatches `view.set` with args.\n 5. Updated tests: `view_switch_commands_emit_canonical_view_set_with_args` and `perspective_goto_commands_emit_canonical_perspective_set_with_args` assert the new wire format; `match_dynamic_prefix_no_longer_rewrites_view_switch_or_perspective_goto` is the regression guard.\n\n-Full cargo + vitest + clippy + tsc all clean.\n-\n-## Review Findings (2026-04-24 09:26)\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/lib/context-menu.ts:14-22` — The `args` field's doc comment on `ResolvedCommand` claims \"the field is forwarded here too so the context-menu → dispatch pipeline stays symmetric with the palette path\", but the actual `ContextMenuItem` shape (both TS line 27-33 and Rust `kanban-app/src/commands.rs:2036`) has no `args` field, and the loop at lines 63-80 does not propagate it. All current fan-out rows are `context_menu: false` so this never fires in production today, but the comment misleads and a future fan-out row opted into the context menu would silently drop its `args`. Fix: either (a) correct the comment to state args is intentionally dropped because all current fan-out rows are palette-only, or (b) actually add `args` to `ContextMenuItem` on both sides and forward it through `show_context_menu` so the symmetry claim becomes true.\n\\ No newline at end of file\n+Full cargo + vitest + clippy + tsc all clean.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPZY6008X9JTQYJ5B825X8AE","timestamp":"2026-04-24T14:26:58.184902Z","entity_type":"task","entity_id":"01KPZMXXEXKVE3RNPA4XJP0105","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -49,4 +49,9 @@\n 4. Frontend `CommandPalette` forwards `cmd.args` through the dispatch path; `left-nav.tsx` now dispatches `view.set` with `{ view_id }` directly; `views-container.tsx` keeps the `view.switch:{id}` id as a client-side scope-map key (for React / dedup uniqueness) but the execute handler dispatches `view.set` with args.\n 5. Updated tests: `view_switch_commands_emit_canonical_view_set_with_args` and `perspective_goto_commands_emit_canonical_perspective_set_with_args` assert the new wire format; `match_dynamic_prefix_no_longer_rewrites_view_switch_or_perspective_goto` is the regression guard.\n\n-Full cargo + vitest + clippy + tsc all clean.\n\\ No newline at end of file\n+Full cargo + vitest + clippy + tsc all clean.\n+\n+## Review Findings (2026-04-24 09:26)\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/lib/context-menu.ts:14-22` — The `args` field's doc comment on `ResolvedCommand` claims \"the field is forwarded here too so the context-menu → dispatch pipeline stays symmetric with the palette path\", but the actual `ContextMenuItem` shape (both TS line 27-33 and Rust `kanban-app/src/commands.rs:2036`) has no `args` field, and the loop at lines 63-80 does not propagate it. All current fan-out rows are `context_menu: false` so this never fires in production today, but the comment misleads and a future fan-out row opted into the context menu would silently drop its `args`. Fix: either (a) correct the comment to state args is intentionally dropped because all current fan-out rows are palette-only, or (b) actually add `args` to `ContextMenuItem` on both sides and forward it through `show_context_menu` so the symmetry claim becomes true.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -49,9 +49,4 @@\n 4. Frontend `CommandPalette` forwards `cmd.args` through the dispatch path; `left-nav.tsx` now dispatches `view.set` with `{ view_id }` directly; `views-container.tsx` keeps the `view.switch:{id}` id as a client-side scope-map key (for React / dedup uniqueness) but the execute handler dispatches `view.set` with args.\n 5. Updated tests: `view_switch_commands_emit_canonical_view_set_with_args` and `perspective_goto_commands_emit_canonical_perspective_set_with_args` assert the new wire format; `match_dynamic_prefix_no_longer_rewrites_view_switch_or_perspective_goto` is the regression guard.\n\n-Full cargo + vitest + clippy + tsc all clean.\n-\n-## Review Findings (2026-04-24 09:26)\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/lib/context-menu.ts:14-22` — The `args` field's doc comment on `ResolvedCommand` claims \"the field is forwarded here too so the context-menu → dispatch pipeline stays symmetric with the palette path\", but the actual `ContextMenuItem` shape (both TS line 27-33 and Rust `kanban-app/src/commands.rs:2036`) has no `args` field, and the loop at lines 63-80 does not propagate it. All current fan-out rows are `context_menu: false` so this never fires in production today, but the comment misleads and a future fan-out row opted into the context menu would silently drop its `args`. Fix: either (a) correct the comment to state args is intentionally dropped because all current fan-out rows are palette-only, or (b) actually add `args` to `ContextMenuItem` on both sides and forward it through `show_context_menu` so the symmetry claim becomes true.\n\\ No newline at end of file\n+Full cargo + vitest + clippy + tsc all clean.\n\\ No newline at end of file\n"}]]}
{"id":"01KPZZATFHXF5Q1R9TNYPQRE0S","timestamp":"2026-04-24T14:47:04.945688Z","op":"update","item_id":"01KPZMXXEXKVE3RNPA4XJP0105","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '80'\n title: Replace perspective.goto / view.switch indirection with direct .set commands\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8380'\n title: Replace perspective.goto / view.switch indirection with direct .set commands\n ---\n ## What\n","transaction_id":null}
{"id":"01KPZZATFMA7QJYJ9QAF6B7RKZ","timestamp":"2026-04-24T14:47:04.948752Z","entity_type":"task","entity_id":"01KPZMXXEXKVE3RNPA4XJP0105","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n"}]]}
{"id":"01KPZZFZZ5YHFX68KDGHH9TE38","timestamp":"2026-04-24T14:49:54.405004Z","op":"update","item_id":"01KPZMXXEXKVE3RNPA4XJP0105","forward_patch":"--- original\n+++ modified\n@@ -61,4 +61,6 @@\n ## Review Findings (2026-04-24 09:26)\n\n ### Nits\n-- [ ] `kanban-app/ui/src/lib/context-menu.ts:14-22` — The `args` field's doc comment on `ResolvedCommand` claims \"the field is forwarded here too so the context-menu → dispatch pipeline stays symmetric with the palette path\", but the actual `ContextMenuItem` shape (both TS line 27-33 and Rust `kanban-app/src/commands.rs:2036`) has no `args` field, and the loop at lines 63-80 does not propagate it. All current fan-out rows are `context_menu: false` so this never fires in production today, but the comment misleads and a future fan-out row opted into the context menu would silently drop its `args`. Fix: either (a) correct the comment to state args is intentionally dropped because all current fan-out rows are palette-only, or (b) actually add `args` to `ContextMenuItem` on both sides and forward it through `show_context_menu` so the symmetry claim becomes true.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/lib/context-menu.ts:14-22` — The `args` field's doc comment on `ResolvedCommand` claims \"the field is forwarded here too so the context-menu → dispatch pipeline stays symmetric with the palette path\", but the actual `ContextMenuItem` shape (both TS line 27-33 and Rust `kanban-app/src/commands.rs:2036`) has no `args` field, and the loop at lines 63-80 does not propagate it. All current fan-out rows are `context_menu: false` so this never fires in production today, but the comment misleads and a future fan-out row opted into the context menu would silently drop its `args`. Fix: either (a) correct the comment to state args is intentionally dropped because all current fan-out rows are palette-only, or (b) actually add `args` to `ContextMenuItem` on both sides and forward it through `show_context_menu` so the symmetry claim becomes true.\n+\n+Resolved via **Option A** (2026-04-24): corrected the doc comment on `ResolvedCommand.args` in `kanban-app/ui/src/lib/context-menu.ts` to accurately state that the field is intentionally dropped in the context-menu path because all current fan-out rows are `context_menu: false` (palette-only). Added a companion comment below `ContextMenuItem` pointing back at the explanation, and documented the concrete steps needed if a future fan-out row opts into the context menu (add `args` to `ContextMenuItem` on both TS and Rust sides, forward through `show_context_menu`). Option B (making the symmetry real) was considered but not warranted: no current or planned fan-out row needs the context-menu surface, and the updated comment acts as the breadcrumb for whoever adds that need later. Verified via `pnpm tsc --noEmit` (clean) and `pnpm test --run` (1322/1322 tests pass).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -61,6 +61,4 @@\n ## Review Findings (2026-04-24 09:26)\n\n ### Nits\n-- [x] `kanban-app/ui/src/lib/context-menu.ts:14-22` — The `args` field's doc comment on `ResolvedCommand` claims \"the field is forwarded here too so the context-menu → dispatch pipeline stays symmetric with the palette path\", but the actual `ContextMenuItem` shape (both TS line 27-33 and Rust `kanban-app/src/commands.rs:2036`) has no `args` field, and the loop at lines 63-80 does not propagate it. All current fan-out rows are `context_menu: false` so this never fires in production today, but the comment misleads and a future fan-out row opted into the context menu would silently drop its `args`. Fix: either (a) correct the comment to state args is intentionally dropped because all current fan-out rows are palette-only, or (b) actually add `args` to `ContextMenuItem` on both sides and forward it through `show_context_menu` so the symmetry claim becomes true.\n-\n-Resolved via **Option A** (2026-04-24): corrected the doc comment on `ResolvedCommand.args` in `kanban-app/ui/src/lib/context-menu.ts` to accurately state that the field is intentionally dropped in the context-menu path because all current fan-out rows are `context_menu: false` (palette-only). Added a companion comment below `ContextMenuItem` pointing back at the explanation, and documented the concrete steps needed if a future fan-out row opts into the context menu (add `args` to `ContextMenuItem` on both TS and Rust sides, forward through `show_context_menu`). Option B (making the symmetry real) was considered but not warranted: no current or planned fan-out row needs the context-menu surface, and the updated comment acts as the breadcrumb for whoever adds that need later. Verified via `pnpm tsc --noEmit` (clean) and `pnpm test --run` (1322/1322 tests pass).\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/lib/context-menu.ts:14-22` — The `args` field's doc comment on `ResolvedCommand` claims \"the field is forwarded here too so the context-menu → dispatch pipeline stays symmetric with the palette path\", but the actual `ContextMenuItem` shape (both TS line 27-33 and Rust `kanban-app/src/commands.rs:2036`) has no `args` field, and the loop at lines 63-80 does not propagate it. All current fan-out rows are `context_menu: false` so this never fires in production today, but the comment misleads and a future fan-out row opted into the context menu would silently drop its `args`. Fix: either (a) correct the comment to state args is intentionally dropped because all current fan-out rows are palette-only, or (b) actually add `args` to `ContextMenuItem` on both sides and forward it through `show_context_menu` so the symmetry claim becomes true.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPZZFZZ7JTN9YZT4S87BBD2Y","timestamp":"2026-04-24T14:49:54.407478Z","entity_type":"task","entity_id":"01KPZMXXEXKVE3RNPA4XJP0105","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -54,4 +54,6 @@\n ## Review Findings (2026-04-24 09:26)\n\n ### Nits\n-- [ ] `kanban-app/ui/src/lib/context-menu.ts:14-22` — The `args` field's doc comment on `ResolvedCommand` claims \"the field is forwarded here too so the context-menu → dispatch pipeline stays symmetric with the palette path\", but the actual `ContextMenuItem` shape (both TS line 27-33 and Rust `kanban-app/src/commands.rs:2036`) has no `args` field, and the loop at lines 63-80 does not propagate it. All current fan-out rows are `context_menu: false` so this never fires in production today, but the comment misleads and a future fan-out row opted into the context menu would silently drop its `args`. Fix: either (a) correct the comment to state args is intentionally dropped because all current fan-out rows are palette-only, or (b) actually add `args` to `ContextMenuItem` on both sides and forward it through `show_context_menu` so the symmetry claim becomes true.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/lib/context-menu.ts:14-22` — The `args` field's doc comment on `ResolvedCommand` claims \"the field is forwarded here too so the context-menu → dispatch pipeline stays symmetric with the palette path\", but the actual `ContextMenuItem` shape (both TS line 27-33 and Rust `kanban-app/src/commands.rs:2036`) has no `args` field, and the loop at lines 63-80 does not propagate it. All current fan-out rows are `context_menu: false` so this never fires in production today, but the comment misleads and a future fan-out row opted into the context menu would silently drop its `args`. Fix: either (a) correct the comment to state args is intentionally dropped because all current fan-out rows are palette-only, or (b) actually add `args` to `ContextMenuItem` on both sides and forward it through `show_context_menu` so the symmetry claim becomes true.\n+\n+Resolved via **Option A** (2026-04-24): corrected the doc comment on `ResolvedCommand.args` in `kanban-app/ui/src/lib/context-menu.ts` to accurately state that the field is intentionally dropped in the context-menu path because all current fan-out rows are `context_menu: false` (palette-only). Added a companion comment below `ContextMenuItem` pointing back at the explanation, and documented the concrete steps needed if a future fan-out row opts into the context menu (add `args` to `ContextMenuItem` on both TS and Rust sides, forward through `show_context_menu`). Option B (making the symmetry real) was considered but not warranted: no current or planned fan-out row needs the context-menu surface, and the updated comment acts as the breadcrumb for whoever adds that need later. Verified via `pnpm tsc --noEmit` (clean) and `pnpm test --run` (1322/1322 tests pass).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -54,6 +54,4 @@\n ## Review Findings (2026-04-24 09:26)\n\n ### Nits\n-- [x] `kanban-app/ui/src/lib/context-menu.ts:14-22` — The `args` field's doc comment on `ResolvedCommand` claims \"the field is forwarded here too so the context-menu → dispatch pipeline stays symmetric with the palette path\", but the actual `ContextMenuItem` shape (both TS line 27-33 and Rust `kanban-app/src/commands.rs:2036`) has no `args` field, and the loop at lines 63-80 does not propagate it. All current fan-out rows are `context_menu: false` so this never fires in production today, but the comment misleads and a future fan-out row opted into the context menu would silently drop its `args`. Fix: either (a) correct the comment to state args is intentionally dropped because all current fan-out rows are palette-only, or (b) actually add `args` to `ContextMenuItem` on both sides and forward it through `show_context_menu` so the symmetry claim becomes true.\n-\n-Resolved via **Option A** (2026-04-24): corrected the doc comment on `ResolvedCommand.args` in `kanban-app/ui/src/lib/context-menu.ts` to accurately state that the field is intentionally dropped in the context-menu path because all current fan-out rows are `context_menu: false` (palette-only). Added a companion comment below `ContextMenuItem` pointing back at the explanation, and documented the concrete steps needed if a future fan-out row opts into the context menu (add `args` to `ContextMenuItem` on both TS and Rust sides, forward through `show_context_menu`). Option B (making the symmetry real) was considered but not warranted: no current or planned fan-out row needs the context-menu surface, and the updated comment acts as the breadcrumb for whoever adds that need later. Verified via `pnpm tsc --noEmit` (clean) and `pnpm test --run` (1322/1322 tests pass).\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/lib/context-menu.ts:14-22` — The `args` field's doc comment on `ResolvedCommand` claims \"the field is forwarded here too so the context-menu → dispatch pipeline stays symmetric with the palette path\", but the actual `ContextMenuItem` shape (both TS line 27-33 and Rust `kanban-app/src/commands.rs:2036`) has no `args` field, and the loop at lines 63-80 does not propagate it. All current fan-out rows are `context_menu: false` so this never fires in production today, but the comment misleads and a future fan-out row opted into the context menu would silently drop its `args`. Fix: either (a) correct the comment to state args is intentionally dropped because all current fan-out rows are palette-only, or (b) actually add `args` to `ContextMenuItem` on both sides and forward it through `show_context_menu` so the symmetry claim becomes true.\n\\ No newline at end of file\n"}]]}
{"id":"01KPZZGX9F438M6RRH945EZZRC","timestamp":"2026-04-24T14:50:24.431408Z","op":"update","item_id":"01KPZMXXEXKVE3RNPA4XJP0105","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8380'\n title: Replace perspective.goto / view.switch indirection with direct .set commands\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '80'\n title: Replace perspective.goto / view.switch indirection with direct .set commands\n ---\n ## What\n","transaction_id":null}
{"id":"01KPZZGX9FM3XEHSQN301HE54S","timestamp":"2026-04-24T14:50:24.431938Z","entity_type":"task","entity_id":"01KPZMXXEXKVE3RNPA4XJP0105","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ005AFBS2NWYR8GKWEBA98W","timestamp":"2026-04-24T15:01:33.291243Z","op":"update","item_id":"01KPZMXXEXKVE3RNPA4XJP0105","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8380'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffff9b80\n title: Replace perspective.goto / view.switch indirection with direct .set commands\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffff9b80\n+position_column: review\n+position_ordinal: '8380'\n title: Replace perspective.goto / view.switch indirection with direct .set commands\n ---\n ## What\n","transaction_id":null}
{"id":"01KQ005AFB7YH72V9VT2P9RHSF","timestamp":"2026-04-24T15:01:33.291879Z","entity_type":"task","entity_id":"01KPZMXXEXKVE3RNPA4XJP0105","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+ffffffffffffffffffffffff9b80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffff9b80\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n"}]]}

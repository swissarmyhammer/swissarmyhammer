{"id":"01KTW2GF7K8K65AP84YXTB4W6Y","timestamp":"2026-06-11T19:28:25.331041Z","op":"create","item_id":"01KTW2GF7J9RNJQVGMFVME5HKY","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,37 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '9880'\n+project: local-review\n+title: 'fix(review): 300s prompt-turn cap false-fires on local 35B — use progress-aware liveness'\n+---\n+## What\n+\n+In the 2026-06-11 calcutron run (first run where `review working` had real scope, files=105), the first real local-qwen review died at `prompt turn exceeded 300s and was abandoned` (../calcutron/.sah/mcp.37798.log 19:20:28, validator=dead-code).\n+\n+`PROMPT_TURN_TIMEOUT = 300s` in `crates/swissarmyhammer-validators/src/validators/pool.rs` is documented as a \"generous backstop … tuned far above any legitimately slow turn so it never false-fires in normal operation\". That holds for the remote backend; it is FALSE for local: a single legitimate turn on the 35B qwen (big review prompt + agentic loop + one shared GPU serializing decodes across fleet tasks) routinely needs >300s of wall clock. The timeout wraps the whole `run_prompt` (`new_session` → `prompt`), so queue wait + decode all count against 300s.\n+\n+Fix — replace the wall-clock turn cap with progress-aware liveness:\n+\n+- [ ] In `worker_loop`/`run_prompt` (pool.rs), the worker already subscribes to the session's notification stream (`notifier.sender().subscribe()`). Abandon a turn only when NO notification/stream progress has arrived for the timeout window (idle timeout, reset on every received session update), not when total wall clock exceeds it. A turn that is actively streaming tokens is alive, however long it takes.\n+- [ ] Keep a defensive absolute ceiling if desired, but it must be far above local-model reality (e.g. 30+ min) — document why.\n+- [ ] When a turn IS abandoned, actively cancel the underlying session (ACP `session/cancel` or the agent's cancel mechanism) so the agent stops decoding and the response sender isn't left to fire into a dropped receiver (see companion task on queue-shutdown cascade).\n+\n+## Acceptance Criteria\n+\n+- [ ] A turn that streams a notification at least every N seconds never gets abandoned regardless of total duration.\n+- [ ] A turn with zero progress for the idle window is abandoned and degrades to a single-task error (fleet continues), as today.\n+- [ ] Abandonment cancels the in-flight session rather than detaching it.\n+\n+## Tests\n+\n+- [ ] pool.rs unit tests with a scripted agent: (a) slow-but-streaming turn (periodic updates, total > idle window) completes; (b) stalled turn (no updates) abandons after the idle window; (c) abandonment sends a cancel. Use short test-tuned durations (<10s, fake agent, no model).\n+- [ ] `cargo test -p swissarmyhammer-validators` green.\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Evidence\n+\n+mcp.37798.log: GPU lock worked (21 waits/21 acquires), 20 AgentMessage replies streamed, zero \"Queue is full\" — the model was generating fine, just slower than 300s for the batch. 1 × `exceeded 300s`, then the cascade (see companion task) killed everything downstream: 90 × \"Queue is shutting down\", reviews 2–4 failed 30/30 instantly, each correctly rejected by the completeness guard (\"incomplete review: 30/30 fan-out tasks failed\").\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,37 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '9880'\n-project: local-review\n-title: 'fix(review): 300s prompt-turn cap false-fires on local 35B — use progress-aware liveness'\n----\n-## What\n-\n-In the 2026-06-11 calcutron run (first run where `review working` had real scope, files=105), the first real local-qwen review died at `prompt turn exceeded 300s and was abandoned` (../calcutron/.sah/mcp.37798.log 19:20:28, validator=dead-code).\n-\n-`PROMPT_TURN_TIMEOUT = 300s` in `crates/swissarmyhammer-validators/src/validators/pool.rs` is documented as a \"generous backstop … tuned far above any legitimately slow turn so it never false-fires in normal operation\". That holds for the remote backend; it is FALSE for local: a single legitimate turn on the 35B qwen (big review prompt + agentic loop + one shared GPU serializing decodes across fleet tasks) routinely needs >300s of wall clock. The timeout wraps the whole `run_prompt` (`new_session` → `prompt`), so queue wait + decode all count against 300s.\n-\n-Fix — replace the wall-clock turn cap with progress-aware liveness:\n-\n-- [ ] In `worker_loop`/`run_prompt` (pool.rs), the worker already subscribes to the session's notification stream (`notifier.sender().subscribe()`). Abandon a turn only when NO notification/stream progress has arrived for the timeout window (idle timeout, reset on every received session update), not when total wall clock exceeds it. A turn that is actively streaming tokens is alive, however long it takes.\n-- [ ] Keep a defensive absolute ceiling if desired, but it must be far above local-model reality (e.g. 30+ min) — document why.\n-- [ ] When a turn IS abandoned, actively cancel the underlying session (ACP `session/cancel` or the agent's cancel mechanism) so the agent stops decoding and the response sender isn't left to fire into a dropped receiver (see companion task on queue-shutdown cascade).\n-\n-## Acceptance Criteria\n-\n-- [ ] A turn that streams a notification at least every N seconds never gets abandoned regardless of total duration.\n-- [ ] A turn with zero progress for the idle window is abandoned and degrades to a single-task error (fleet continues), as today.\n-- [ ] Abandonment cancels the in-flight session rather than detaching it.\n-\n-## Tests\n-\n-- [ ] pool.rs unit tests with a scripted agent: (a) slow-but-streaming turn (periodic updates, total > idle window) completes; (b) stalled turn (no updates) abandons after the idle window; (c) abandonment sends a cancel. Use short test-tuned durations (<10s, fake agent, no model).\n-- [ ] `cargo test -p swissarmyhammer-validators` green.\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Evidence\n-\n-mcp.37798.log: GPU lock worked (21 waits/21 acquires), 20 AgentMessage replies streamed, zero \"Queue is full\" — the model was generating fine, just slower than 300s for the batch. 1 × `exceeded 300s`, then the cascade (see companion task) killed everything downstream: 90 × \"Queue is shutting down\", reviews 2–4 failed 30/30 instantly, each correctly rejected by the completeness guard (\"incomplete review: 30/30 fan-out tasks failed\").\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTW34WEG7926FQTMNDVPBXPS","timestamp":"2026-06-11T19:39:34.224376Z","op":"update","item_id":"01KTW2GF7J9RNJQVGMFVME5HKY","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: '9880'\n+position_column: doing\n+position_ordinal: '80'\n project: local-review\n title: 'fix(review): 300s prompt-turn cap false-fires on local 35B — use progress-aware liveness'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '9880'\n project: local-review\n title: 'fix(review): 300s prompt-turn cap false-fires on local 35B — use progress-aware liveness'\n ---\n","transaction_id":null}
{"id":"01KTW3Y2HH1NBV3QYR4RDSW0Q1","timestamp":"2026-06-11T19:53:19.665200Z","op":"update","item_id":"01KTW2GF7J9RNJQVGMFVME5HKY","forward_patch":"--- original\n+++ modified\n@@ -14,23 +14,23 @@\n\n Fix — replace the wall-clock turn cap with progress-aware liveness:\n\n-- [ ] In `worker_loop`/`run_prompt` (pool.rs), the worker already subscribes to the session's notification stream (`notifier.sender().subscribe()`). Abandon a turn only when NO notification/stream progress has arrived for the timeout window (idle timeout, reset on every received session update), not when total wall clock exceeds it. A turn that is actively streaming tokens is alive, however long it takes.\n-- [ ] Keep a defensive absolute ceiling if desired, but it must be far above local-model reality (e.g. 30+ min) — document why.\n-- [ ] When a turn IS abandoned, actively cancel the underlying session (ACP `session/cancel` or the agent's cancel mechanism) so the agent stops decoding and the response sender isn't left to fire into a dropped receiver (see companion task on queue-shutdown cascade).\n+- [x] In `worker_loop`/`run_prompt` (pool.rs), the worker already subscribes to the session's notification stream (`notifier.sender().subscribe()`). Abandon a turn only when NO notification/stream progress has arrived for the timeout window (idle timeout, reset on every received session update), not when total wall clock exceeds it. A turn that is actively streaming tokens is alive, however long it takes.\n+- [x] Keep a defensive absolute ceiling if desired, but it must be far above local-model reality (e.g. 30+ min) — document why. (`PROMPT_TURN_CEILING = 45 min`, documented.)\n+- [x] When a turn IS abandoned, actively cancel the underlying session (ACP `session/cancel` or the agent's cancel mechanism) so the agent stops decoding and the response sender isn't left to fire into a dropped receiver (see companion task on queue-shutdown cascade).\n\n ## Acceptance Criteria\n\n-- [ ] A turn that streams a notification at least every N seconds never gets abandoned regardless of total duration.\n-- [ ] A turn with zero progress for the idle window is abandoned and degrades to a single-task error (fleet continues), as today.\n-- [ ] Abandonment cancels the in-flight session rather than detaching it.\n+- [x] A turn that streams a notification at least every N seconds never gets abandoned regardless of total duration. (`test_pool_streaming_turn_survives_beyond_idle_window`)\n+- [x] A turn with zero progress for the idle window is abandoned and degrades to a single-task error (fleet continues), as today. (`test_pool_stalled_turn_abandons_after_idle_window`)\n+- [x] Abandonment cancels the in-flight session rather than detaching it. (`test_pool_abandoned_turn_cancels_session`)\n\n ## Tests\n\n-- [ ] pool.rs unit tests with a scripted agent: (a) slow-but-streaming turn (periodic updates, total > idle window) completes; (b) stalled turn (no updates) abandons after the idle window; (c) abandonment sends a cancel. Use short test-tuned durations (<10s, fake agent, no model).\n-- [ ] `cargo test -p swissarmyhammer-validators` green.\n+- [x] pool.rs unit tests with a scripted agent: (a) slow-but-streaming turn (periodic updates, total > idle window) completes; (b) stalled turn (no updates) abandons after the idle window; (c) abandonment sends a cancel. Use short test-tuned durations (<10s, fake agent, no model). (Plus (d) the absolute ceiling abandons a streaming runaway. Implemented via `PoolConfig::with_turn_liveness` test-tuned windows; note the idle window must exceed claude_agent's fixed 500ms trailing notification drain.)\n+- [x] `cargo test -p swissarmyhammer-validators` green. (241 + 2 passed, 0 failed; clippy --all-targets -D warnings clean.)\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n+- Use `/tdd` — write failing tests first, then implement to make them pass. (Done: RED run showed (a)/(c)/(d) failing on the old wall-clock cap, (b) passing as behavior parity; GREEN after implementing liveness.)\n\n ## Evidence\n\n","reverse_patch":"--- original\n+++ modified\n@@ -14,23 +14,23 @@\n\n Fix — replace the wall-clock turn cap with progress-aware liveness:\n\n-- [x] In `worker_loop`/`run_prompt` (pool.rs), the worker already subscribes to the session's notification stream (`notifier.sender().subscribe()`). Abandon a turn only when NO notification/stream progress has arrived for the timeout window (idle timeout, reset on every received session update), not when total wall clock exceeds it. A turn that is actively streaming tokens is alive, however long it takes.\n-- [x] Keep a defensive absolute ceiling if desired, but it must be far above local-model reality (e.g. 30+ min) — document why. (`PROMPT_TURN_CEILING = 45 min`, documented.)\n-- [x] When a turn IS abandoned, actively cancel the underlying session (ACP `session/cancel` or the agent's cancel mechanism) so the agent stops decoding and the response sender isn't left to fire into a dropped receiver (see companion task on queue-shutdown cascade).\n+- [ ] In `worker_loop`/`run_prompt` (pool.rs), the worker already subscribes to the session's notification stream (`notifier.sender().subscribe()`). Abandon a turn only when NO notification/stream progress has arrived for the timeout window (idle timeout, reset on every received session update), not when total wall clock exceeds it. A turn that is actively streaming tokens is alive, however long it takes.\n+- [ ] Keep a defensive absolute ceiling if desired, but it must be far above local-model reality (e.g. 30+ min) — document why.\n+- [ ] When a turn IS abandoned, actively cancel the underlying session (ACP `session/cancel` or the agent's cancel mechanism) so the agent stops decoding and the response sender isn't left to fire into a dropped receiver (see companion task on queue-shutdown cascade).\n\n ## Acceptance Criteria\n\n-- [x] A turn that streams a notification at least every N seconds never gets abandoned regardless of total duration. (`test_pool_streaming_turn_survives_beyond_idle_window`)\n-- [x] A turn with zero progress for the idle window is abandoned and degrades to a single-task error (fleet continues), as today. (`test_pool_stalled_turn_abandons_after_idle_window`)\n-- [x] Abandonment cancels the in-flight session rather than detaching it. (`test_pool_abandoned_turn_cancels_session`)\n+- [ ] A turn that streams a notification at least every N seconds never gets abandoned regardless of total duration.\n+- [ ] A turn with zero progress for the idle window is abandoned and degrades to a single-task error (fleet continues), as today.\n+- [ ] Abandonment cancels the in-flight session rather than detaching it.\n\n ## Tests\n\n-- [x] pool.rs unit tests with a scripted agent: (a) slow-but-streaming turn (periodic updates, total > idle window) completes; (b) stalled turn (no updates) abandons after the idle window; (c) abandonment sends a cancel. Use short test-tuned durations (<10s, fake agent, no model). (Plus (d) the absolute ceiling abandons a streaming runaway. Implemented via `PoolConfig::with_turn_liveness` test-tuned windows; note the idle window must exceed claude_agent's fixed 500ms trailing notification drain.)\n-- [x] `cargo test -p swissarmyhammer-validators` green. (241 + 2 passed, 0 failed; clippy --all-targets -D warnings clean.)\n+- [ ] pool.rs unit tests with a scripted agent: (a) slow-but-streaming turn (periodic updates, total > idle window) completes; (b) stalled turn (no updates) abandons after the idle window; (c) abandonment sends a cancel. Use short test-tuned durations (<10s, fake agent, no model).\n+- [ ] `cargo test -p swissarmyhammer-validators` green.\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass. (Done: RED run showed (a)/(c)/(d) failing on the old wall-clock cap, (b) passing as behavior parity; GREEN after implementing liveness.)\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\n ## Evidence\n\n","transaction_id":null}
{"id":"01KTW3Y7MPDTDMZFZ5XJEAZAXY","timestamp":"2026-06-11T19:53:24.886132Z","op":"update","item_id":"01KTW2GF7J9RNJQVGMFVME5HKY","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: 'fix(review): 300s prompt-turn cap false-fires on local 35B — use progress-aware liveness'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: 'fix(review): 300s prompt-turn cap false-fires on local 35B — use progress-aware liveness'\n","transaction_id":null}
{"id":"01KTW6CEY7H2E2GM58PWB36D5B","timestamp":"2026-06-11T20:36:08.263889Z","op":"update","item_id":"01KTW2GF7J9RNJQVGMFVME5HKY","forward_patch":"--- original\n+++ modified\n@@ -34,4 +34,21 @@\n\n ## Evidence\n\n-mcp.37798.log: GPU lock worked (21 waits/21 acquires), 20 AgentMessage replies streamed, zero \"Queue is full\" — the model was generating fine, just slower than 300s for the batch. 1 × `exceeded 300s`, then the cascade (see companion task) killed everything downstream: 90 × \"Queue is shutting down\", reviews 2–4 failed 30/30 instantly, each correctly rejected by the completeness guard (\"incomplete review: 30/30 fan-out tasks failed\").\n\\ No newline at end of file\n+mcp.37798.log: GPU lock worked (21 waits/21 acquires), 20 AgentMessage replies streamed, zero \"Queue is full\" — the model was generating fine, just slower than 300s for the batch. 1 × `exceeded 300s`, then the cascade (see companion task) killed everything downstream: 90 × \"Queue is shutting down\", reviews 2–4 failed 30/30 instantly, each correctly rejected by the completeness guard (\"incomplete review: 30/30 fan-out tasks failed\").\n+\n+## Review Findings (2026-06-11 15:25)\n+\n+> ⚠️ 3/15 review tasks failed — results are INCOMPLETE.\n+\n+### Blockers\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:488` — The entire in-test mock-agent harness (the `MockAgent` trait with nine default methods, `MockAgentAdapter`, `dispatch_mock_request`, `dispatch_mock_notification`, and `run_with_mock_agent`) is a near-verbatim copy of the public, reusable harness in `acp-conformance`'s `test_utils`. The harness comment at pool.rs:482 even acknowledges it is re-creating an existing harness ('same shape as the retired runner's harness'). This is now another full copy of ACP mock-agent wiring alongside the ScriptedAgent harnesses already duplicated in this crate's fleet.rs, verify.rs, and drive.rs test modules; a protocol or SDK-API change must now be re-applied in every copy, and the copies have already started drifting (this one adds `cx.spawn` offload in dispatch that the others lack). Reuse the existing public harness instead of copying it: add `acp-conformance` as a dev-dependency (it already exports `MockAgent`, `MockAgentAdapter`, and `run_with_mock_agent`) and keep only the pool-specific piece — the notifier-forwarding client wiring in `run_client_against` — locally; if the conformance harness needs the notification hook, add it there once. Alternatively, hoist one shared test-support module inside swissarmyhammer-validators and migrate the fleet/verify/drive test harness copies onto it.\n+\n+### Warnings\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:132` — `with_turn_liveness(idle_timeout: Duration, turn_ceiling: Duration)` takes two adjacent parameters of the same type with different meanings; swapping them compiles silently and inverts the liveness policy (a 45-minute idle window with a 5-minute ceiling), which would abandon every slow-but-streaming local-model turn — the exact case this change exists to protect. Split into `with_idle_timeout(Duration)` and `with_turn_ceiling(Duration)` builder methods (matching the existing `with_max_tokens` style), or wrap the arguments in `IdleTimeout(Duration)` / `TurnCeiling(Duration)` newtypes.\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:291` — `run_turn_with_liveness` concentrates the entire liveness state machine in one loop and reaches 4+ nesting levels: `loop` → `tokio::select!` arm (one arm guarded by `if liveness_open`) → `match received` → `if is_ours`, and in the abandonment arm `loop` → select arm → `if let Some(session_id)` → `if let Err(e)`, plus a trailing `if`/`else` choosing the abandonment reason. The mix of three racing select arms, broadcast-error recovery semantics, deadline recomputation each iteration, and session-slot locking makes the control flow hard to verify by reading — exactly the code where a subtle liveness bug (e.g. the Lagged-counts-as-progress decision) hides. Extract the two select-arm bodies into named helpers so the loop reads as the race it is: a `fn note_progress(received, &session_slot, &mut last_progress, &mut liveness_open)` for the broadcast-recv arm (encapsulating the Ok/Lagged/Closed policy), and an `async fn abandon_turn(agent, &session_slot, started, config) -> AgentError` for the deadline arm (cancel-the-session + pick-the-reason). The loop then has three one-line arms and each policy is independently readable and testable.\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:365` — Both abandonment outcomes (idle-window vs absolute-ceiling) are returned as stringly-typed `claude_agent::AgentError::Internal(reason)`, so callers cannot distinguish 'turn was abandoned (retryable, agent alive)' from a genuine internal failure except by substring-matching the message. The file's own tests are forced into exactly that fragile pattern, proving the API loses the failure kind. Introduce typed variants for the two abandonment modes — e.g. a pool-level error enum (`PoolError::TurnIdle { idle_timeout }`, `PoolError::TurnCeiling { turn_ceiling }`) wrapping `AgentError`, or dedicated `AgentError` variants — and assert on the variant in tests instead of message substrings.\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:734` — `new_notifier` is the third byte-identical copy of the same test helper in this crate (verify.rs and fleet.rs already have it). Rule of three is met: this is a pattern, and the buffer size 64 is a magic value now pinned in three places that can drift independently. Extract one `new_notifier` into a shared `#[cfg(test)]` test-support module in the crate (the same module the harness consolidation from the previous finding would create) and use it from pool.rs, verify.rs, and fleet.rs.\n+\n+### Nits\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:84` — Doc comments on the public fields `PoolConfig::idle_timeout` / `turn_ceiling` (and on `with_turn_liveness`) use intra-doc links to `PROMPT_IDLE_TIMEOUT` / `PROMPT_TURN_CEILING`, but those constants are private while `PoolConfig` is publicly re-exported — rustdoc's `private_intra_doc_links` lint fires and the links render as dead text in published docs. Make the two constants `pub const` (they document the production defaults, as `DEFAULT_MAX_TOKENS` already does at pool.rs:42), or inline the default values into the field docs instead of linking.\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:402` — Error messages \"Failed to create session: …\" and \"Failed to execute prompt: …\" are capitalized; Display messages on errors should be lowercase with no trailing punctuation so they compose cleanly when wrapped by outer contexts. Lowercase the leading word: `failed to create session: {e}` / `failed to execute prompt: {e}`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -34,21 +34,4 @@\n\n ## Evidence\n\n-mcp.37798.log: GPU lock worked (21 waits/21 acquires), 20 AgentMessage replies streamed, zero \"Queue is full\" — the model was generating fine, just slower than 300s for the batch. 1 × `exceeded 300s`, then the cascade (see companion task) killed everything downstream: 90 × \"Queue is shutting down\", reviews 2–4 failed 30/30 instantly, each correctly rejected by the completeness guard (\"incomplete review: 30/30 fan-out tasks failed\").\n-\n-## Review Findings (2026-06-11 15:25)\n-\n-> ⚠️ 3/15 review tasks failed — results are INCOMPLETE.\n-\n-### Blockers\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:488` — The entire in-test mock-agent harness (the `MockAgent` trait with nine default methods, `MockAgentAdapter`, `dispatch_mock_request`, `dispatch_mock_notification`, and `run_with_mock_agent`) is a near-verbatim copy of the public, reusable harness in `acp-conformance`'s `test_utils`. The harness comment at pool.rs:482 even acknowledges it is re-creating an existing harness ('same shape as the retired runner's harness'). This is now another full copy of ACP mock-agent wiring alongside the ScriptedAgent harnesses already duplicated in this crate's fleet.rs, verify.rs, and drive.rs test modules; a protocol or SDK-API change must now be re-applied in every copy, and the copies have already started drifting (this one adds `cx.spawn` offload in dispatch that the others lack). Reuse the existing public harness instead of copying it: add `acp-conformance` as a dev-dependency (it already exports `MockAgent`, `MockAgentAdapter`, and `run_with_mock_agent`) and keep only the pool-specific piece — the notifier-forwarding client wiring in `run_client_against` — locally; if the conformance harness needs the notification hook, add it there once. Alternatively, hoist one shared test-support module inside swissarmyhammer-validators and migrate the fleet/verify/drive test harness copies onto it.\n-\n-### Warnings\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:132` — `with_turn_liveness(idle_timeout: Duration, turn_ceiling: Duration)` takes two adjacent parameters of the same type with different meanings; swapping them compiles silently and inverts the liveness policy (a 45-minute idle window with a 5-minute ceiling), which would abandon every slow-but-streaming local-model turn — the exact case this change exists to protect. Split into `with_idle_timeout(Duration)` and `with_turn_ceiling(Duration)` builder methods (matching the existing `with_max_tokens` style), or wrap the arguments in `IdleTimeout(Duration)` / `TurnCeiling(Duration)` newtypes.\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:291` — `run_turn_with_liveness` concentrates the entire liveness state machine in one loop and reaches 4+ nesting levels: `loop` → `tokio::select!` arm (one arm guarded by `if liveness_open`) → `match received` → `if is_ours`, and in the abandonment arm `loop` → select arm → `if let Some(session_id)` → `if let Err(e)`, plus a trailing `if`/`else` choosing the abandonment reason. The mix of three racing select arms, broadcast-error recovery semantics, deadline recomputation each iteration, and session-slot locking makes the control flow hard to verify by reading — exactly the code where a subtle liveness bug (e.g. the Lagged-counts-as-progress decision) hides. Extract the two select-arm bodies into named helpers so the loop reads as the race it is: a `fn note_progress(received, &session_slot, &mut last_progress, &mut liveness_open)` for the broadcast-recv arm (encapsulating the Ok/Lagged/Closed policy), and an `async fn abandon_turn(agent, &session_slot, started, config) -> AgentError` for the deadline arm (cancel-the-session + pick-the-reason). The loop then has three one-line arms and each policy is independently readable and testable.\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:365` — Both abandonment outcomes (idle-window vs absolute-ceiling) are returned as stringly-typed `claude_agent::AgentError::Internal(reason)`, so callers cannot distinguish 'turn was abandoned (retryable, agent alive)' from a genuine internal failure except by substring-matching the message. The file's own tests are forced into exactly that fragile pattern, proving the API loses the failure kind. Introduce typed variants for the two abandonment modes — e.g. a pool-level error enum (`PoolError::TurnIdle { idle_timeout }`, `PoolError::TurnCeiling { turn_ceiling }`) wrapping `AgentError`, or dedicated `AgentError` variants — and assert on the variant in tests instead of message substrings.\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:734` — `new_notifier` is the third byte-identical copy of the same test helper in this crate (verify.rs and fleet.rs already have it). Rule of three is met: this is a pattern, and the buffer size 64 is a magic value now pinned in three places that can drift independently. Extract one `new_notifier` into a shared `#[cfg(test)]` test-support module in the crate (the same module the harness consolidation from the previous finding would create) and use it from pool.rs, verify.rs, and fleet.rs.\n-\n-### Nits\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:84` — Doc comments on the public fields `PoolConfig::idle_timeout` / `turn_ceiling` (and on `with_turn_liveness`) use intra-doc links to `PROMPT_IDLE_TIMEOUT` / `PROMPT_TURN_CEILING`, but those constants are private while `PoolConfig` is publicly re-exported — rustdoc's `private_intra_doc_links` lint fires and the links render as dead text in published docs. Make the two constants `pub const` (they document the production defaults, as `DEFAULT_MAX_TOKENS` already does at pool.rs:42), or inline the default values into the field docs instead of linking.\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:402` — Error messages \"Failed to create session: …\" and \"Failed to execute prompt: …\" are capitalized; Display messages on errors should be lowercase with no trailing punctuation so they compose cleanly when wrapped by outer contexts. Lowercase the leading word: `failed to create session: {e}` / `failed to execute prompt: {e}`.\n\\ No newline at end of file\n+mcp.37798.log: GPU lock worked (21 waits/21 acquires), 20 AgentMessage replies streamed, zero \"Queue is full\" — the model was generating fine, just slower than 300s for the batch. 1 × `exceeded 300s`, then the cascade (see companion task) killed everything downstream: 90 × \"Queue is shutting down\", reviews 2–4 failed 30/30 instantly, each correctly rejected by the completeness guard (\"incomplete review: 30/30 fan-out tasks failed\").\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTW6PHY56KMV4FFXT7HHEJ9P","timestamp":"2026-06-11T20:41:39.013024Z","op":"update","item_id":"01KTW2GF7J9RNJQVGMFVME5HKY","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: 'fix(review): 300s prompt-turn cap false-fires on local 35B — use progress-aware liveness'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: 'fix(review): 300s prompt-turn cap false-fires on local 35B — use progress-aware liveness'\n","transaction_id":null}
{"id":"01KTW7X9W983KN4Z2YFPKTPTW9","timestamp":"2026-06-11T21:02:48.713879Z","op":"update","item_id":"01KTW2GF7J9RNJQVGMFVME5HKY","forward_patch":"--- original\n+++ modified\n@@ -26,11 +26,11 @@\n\n ## Tests\n\n-- [x] pool.rs unit tests with a scripted agent: (a) slow-but-streaming turn (periodic updates, total > idle window) completes; (b) stalled turn (no updates) abandons after the idle window; (c) abandonment sends a cancel. Use short test-tuned durations (<10s, fake agent, no model). (Plus (d) the absolute ceiling abandons a streaming runaway. Implemented via `PoolConfig::with_turn_liveness` test-tuned windows; note the idle window must exceed claude_agent's fixed 500ms trailing notification drain.)\n+- [x] pool.rs unit tests with a scripted agent: (a) slow-but-streaming turn (periodic updates, total > idle window) completes; (b) stalled turn (no updates) abandons after the idle window; (c) abandonment sends a cancel. Use short test-tuned durations (<10s, fake agent, no model). (Plus (d) the absolute ceiling abandons a streaming runaway. Implemented via `PoolConfig::with_idle_timeout`/`with_turn_ceiling` test-tuned windows; note the idle window must exceed claude_agent's fixed 500ms trailing notification drain.)\n - [x] `cargo test -p swissarmyhammer-validators` green. (241 + 2 passed, 0 failed; clippy --all-targets -D warnings clean.)\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass. (Done: RED run showed (a)/(c)/(d) failing on the old wall-clock cap, (b) passing as behavior parity; GREEN after implementing liveness.)\n+- Use `/tdd` — write failing tests first, then implement to make them pass. (Done: RED run showed (a)/(c)/(d) failing on the old wall-clock cap, (b) passing as behavior parity; GREEN after implementing liveness. Findings round: RED = typed-variant assertions + split builders fail to compile against old API; GREEN after `PoolError` + builder split.)\n\n ## Evidence\n\n@@ -41,14 +41,19 @@\n > ⚠️ 3/15 review tasks failed — results are INCOMPLETE.\n\n ### Blockers\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:488` — The entire in-test mock-agent harness (the `MockAgent` trait with nine default methods, `MockAgentAdapter`, `dispatch_mock_request`, `dispatch_mock_notification`, and `run_with_mock_agent`) is a near-verbatim copy of the public, reusable harness in `acp-conformance`'s `test_utils`. The harness comment at pool.rs:482 even acknowledges it is re-creating an existing harness ('same shape as the retired runner's harness'). This is now another full copy of ACP mock-agent wiring alongside the ScriptedAgent harnesses already duplicated in this crate's fleet.rs, verify.rs, and drive.rs test modules; a protocol or SDK-API change must now be re-applied in every copy, and the copies have already started drifting (this one adds `cx.spawn` offload in dispatch that the others lack). Reuse the existing public harness instead of copying it: add `acp-conformance` as a dev-dependency (it already exports `MockAgent`, `MockAgentAdapter`, and `run_with_mock_agent`) and keep only the pool-specific piece — the notifier-forwarding client wiring in `run_client_against` — locally; if the conformance harness needs the notification hook, add it there once. Alternatively, hoist one shared test-support module inside swissarmyhammer-validators and migrate the fleet/verify/drive test harness copies onto it.\n+- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:488` — The entire in-test mock-agent harness (the `MockAgent` trait with nine default methods, `MockAgentAdapter`, `dispatch_mock_request`, `dispatch_mock_notification`, and `run_with_mock_agent`) is a near-verbatim copy of the public, reusable harness in `acp-conformance`'s `test_utils`. … **RESOLVED:** the copied trait/adapter/dispatchers were deleted from pool.rs; the pool tests now import `MockAgent`/`MockAgentAdapter` from `acp_conformance::test_utils` as a dev-dependency. Upstream contribution: `acp-conformance` now exports `test_utils` under a new `test-support` cargo feature (`#[cfg(any(test, feature = \"test-support\"))] pub mod test_utils`, `futures` made an optional dep) — the same exposure pattern `model-embedding` uses; `acp-conformance` added to `[workspace.dependencies]`. The upstream dispatch already had the `cx.spawn` offload, so no fork remains. Only the pool-specific notifier-forwarding client wiring (`run_client_against` + thin `run_with_mock_agent`/`run_with_playback_agent` wrappers) stays local, as the finding prescribed. (ScriptedAgent copies in fleet.rs/verify.rs are pre-existing and out of scope per review note.)\n\n ### Warnings\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:132` — `with_turn_liveness(idle_timeout: Duration, turn_ceiling: Duration)` takes two adjacent parameters of the same type with different meanings; swapping them compiles silently and inverts the liveness policy (a 45-minute idle window with a 5-minute ceiling), which would abandon every slow-but-streaming local-model turn — the exact case this change exists to protect. Split into `with_idle_timeout(Duration)` and `with_turn_ceiling(Duration)` builder methods (matching the existing `with_max_tokens` style), or wrap the arguments in `IdleTimeout(Duration)` / `TurnCeiling(Duration)` newtypes.\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:291` — `run_turn_with_liveness` concentrates the entire liveness state machine in one loop and reaches 4+ nesting levels: `loop` → `tokio::select!` arm (one arm guarded by `if liveness_open`) → `match received` → `if is_ours`, and in the abandonment arm `loop` → select arm → `if let Some(session_id)` → `if let Err(e)`, plus a trailing `if`/`else` choosing the abandonment reason. The mix of three racing select arms, broadcast-error recovery semantics, deadline recomputation each iteration, and session-slot locking makes the control flow hard to verify by reading — exactly the code where a subtle liveness bug (e.g. the Lagged-counts-as-progress decision) hides. Extract the two select-arm bodies into named helpers so the loop reads as the race it is: a `fn note_progress(received, &session_slot, &mut last_progress, &mut liveness_open)` for the broadcast-recv arm (encapsulating the Ok/Lagged/Closed policy), and an `async fn abandon_turn(agent, &session_slot, started, config) -> AgentError` for the deadline arm (cancel-the-session + pick-the-reason). The loop then has three one-line arms and each policy is independently readable and testable.\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:365` — Both abandonment outcomes (idle-window vs absolute-ceiling) are returned as stringly-typed `claude_agent::AgentError::Internal(reason)`, so callers cannot distinguish 'turn was abandoned (retryable, agent alive)' from a genuine internal failure except by substring-matching the message. The file's own tests are forced into exactly that fragile pattern, proving the API loses the failure kind. Introduce typed variants for the two abandonment modes — e.g. a pool-level error enum (`PoolError::TurnIdle { idle_timeout }`, `PoolError::TurnCeiling { turn_ceiling }`) wrapping `AgentError`, or dedicated `AgentError` variants — and assert on the variant in tests instead of message substrings.\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:734` — `new_notifier` is the third byte-identical copy of the same test helper in this crate (verify.rs and fleet.rs already have it). Rule of three is met: this is a pattern, and the buffer size 64 is a magic value now pinned in three places that can drift independently. Extract one `new_notifier` into a shared `#[cfg(test)]` test-support module in the crate (the same module the harness consolidation from the previous finding would create) and use it from pool.rs, verify.rs, and fleet.rs.\n+- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:132` — `with_turn_liveness(idle_timeout, turn_ceiling)` swap-prone. **RESOLVED:** replaced by `with_idle_timeout(Duration)` and `with_turn_ceiling(Duration)` builder methods matching the `with_max_tokens` style; no other callers existed.\n+- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:291` — `run_turn_with_liveness` nesting. **RESOLVED:** the select loop now has three one-line arms; the broadcast-recv policy is extracted into `fn note_progress(received, &session_slot, &mut last_progress, &mut liveness_open)` (Ok/Lagged/Closed policy documented on the helper) and the deadline arm into `fn abandon_turn(agent, &session_slot, ceiling_deadline, config) -> PoolError` (cancel-the-session + typed reason).\n+- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:365` — stringly-typed abandonment. **RESOLVED:** introduced `pub enum PoolError { TurnIdle { idle_timeout }, TurnCeiling { turn_ceiling }, Agent(#[from] claude_agent::AgentError) }` (thiserror, `#[error(transparent)]` passthrough for agent errors); `PromptResult = Result<CollectedResponse, PoolError>`; re-exported from `validators::mod` and the crate root. Tests now assert `matches!(err, PoolError::TurnIdle { .. })` / `TurnCeiling` instead of substring-matching. Display text unchanged (lowercase, same phrasing) so existing log greps still work. fleet/verify/drive consume errors opaquely via Display — no behavior change; downstream crates (`swissarmyhammer-agent`, `swissarmyhammer-tools`) verified compiling.\n+- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:734` — third `new_notifier` copy. **RESOLVED:** single `new_notifier` now lives in the existing shared `review::test_support` module (with the 64-slot buffer documented once); pool.rs, fleet.rs, and verify.rs import it.\n\n ### Nits\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:84` — Doc comments on the public fields `PoolConfig::idle_timeout` / `turn_ceiling` (and on `with_turn_liveness`) use intra-doc links to `PROMPT_IDLE_TIMEOUT` / `PROMPT_TURN_CEILING`, but those constants are private while `PoolConfig` is publicly re-exported — rustdoc's `private_intra_doc_links` lint fires and the links render as dead text in published docs. Make the two constants `pub const` (they document the production defaults, as `DEFAULT_MAX_TOKENS` already does at pool.rs:42), or inline the default values into the field docs instead of linking.\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:402` — Error messages \"Failed to create session: …\" and \"Failed to execute prompt: …\" are capitalized; Display messages on errors should be lowercase with no trailing punctuation so they compose cleanly when wrapped by outer contexts. Lowercase the leading word: `failed to create session: {e}` / `failed to execute prompt: {e}`.\n\\ No newline at end of file\n+- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:84` — dead intra-doc links. **RESOLVED:** `PROMPT_IDLE_TIMEOUT` / `PROMPT_TURN_CEILING` are now `pub const` (they document the production defaults, like `DEFAULT_MAX_TOKENS`); `cargo doc` shows zero pool.rs link warnings.\n+- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:402` — capitalized Display messages. **RESOLVED:** lowercased to `failed to create session: {e}` / `failed to execute prompt: {e}`. (The similarly capitalized messages in `claude-agent/src/lib.rs` are that crate's own seam, outside this finding's pool.rs scope.)\n+\n+### Findings-round verification (2026-06-11)\n+- `cargo test -p swissarmyhammer-validators -p acp-conformance`: validators 241 + 2 doc-tests passed; acp-conformance 176 unit + 125 integration passed; 0 failures.\n+- `cargo clippy -p swissarmyhammer-validators -p acp-conformance --all-targets -- -D warnings`: clean.\n+- `cargo fmt --check`: clean. `cargo check -p swissarmyhammer-agent -p swissarmyhammer-tools`: clean (PromptResult type change verified downstream).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -26,11 +26,11 @@\n\n ## Tests\n\n-- [x] pool.rs unit tests with a scripted agent: (a) slow-but-streaming turn (periodic updates, total > idle window) completes; (b) stalled turn (no updates) abandons after the idle window; (c) abandonment sends a cancel. Use short test-tuned durations (<10s, fake agent, no model). (Plus (d) the absolute ceiling abandons a streaming runaway. Implemented via `PoolConfig::with_idle_timeout`/`with_turn_ceiling` test-tuned windows; note the idle window must exceed claude_agent's fixed 500ms trailing notification drain.)\n+- [x] pool.rs unit tests with a scripted agent: (a) slow-but-streaming turn (periodic updates, total > idle window) completes; (b) stalled turn (no updates) abandons after the idle window; (c) abandonment sends a cancel. Use short test-tuned durations (<10s, fake agent, no model). (Plus (d) the absolute ceiling abandons a streaming runaway. Implemented via `PoolConfig::with_turn_liveness` test-tuned windows; note the idle window must exceed claude_agent's fixed 500ms trailing notification drain.)\n - [x] `cargo test -p swissarmyhammer-validators` green. (241 + 2 passed, 0 failed; clippy --all-targets -D warnings clean.)\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass. (Done: RED run showed (a)/(c)/(d) failing on the old wall-clock cap, (b) passing as behavior parity; GREEN after implementing liveness. Findings round: RED = typed-variant assertions + split builders fail to compile against old API; GREEN after `PoolError` + builder split.)\n+- Use `/tdd` — write failing tests first, then implement to make them pass. (Done: RED run showed (a)/(c)/(d) failing on the old wall-clock cap, (b) passing as behavior parity; GREEN after implementing liveness.)\n\n ## Evidence\n\n@@ -41,19 +41,14 @@\n > ⚠️ 3/15 review tasks failed — results are INCOMPLETE.\n\n ### Blockers\n-- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:488` — The entire in-test mock-agent harness (the `MockAgent` trait with nine default methods, `MockAgentAdapter`, `dispatch_mock_request`, `dispatch_mock_notification`, and `run_with_mock_agent`) is a near-verbatim copy of the public, reusable harness in `acp-conformance`'s `test_utils`. … **RESOLVED:** the copied trait/adapter/dispatchers were deleted from pool.rs; the pool tests now import `MockAgent`/`MockAgentAdapter` from `acp_conformance::test_utils` as a dev-dependency. Upstream contribution: `acp-conformance` now exports `test_utils` under a new `test-support` cargo feature (`#[cfg(any(test, feature = \"test-support\"))] pub mod test_utils`, `futures` made an optional dep) — the same exposure pattern `model-embedding` uses; `acp-conformance` added to `[workspace.dependencies]`. The upstream dispatch already had the `cx.spawn` offload, so no fork remains. Only the pool-specific notifier-forwarding client wiring (`run_client_against` + thin `run_with_mock_agent`/`run_with_playback_agent` wrappers) stays local, as the finding prescribed. (ScriptedAgent copies in fleet.rs/verify.rs are pre-existing and out of scope per review note.)\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:488` — The entire in-test mock-agent harness (the `MockAgent` trait with nine default methods, `MockAgentAdapter`, `dispatch_mock_request`, `dispatch_mock_notification`, and `run_with_mock_agent`) is a near-verbatim copy of the public, reusable harness in `acp-conformance`'s `test_utils`. The harness comment at pool.rs:482 even acknowledges it is re-creating an existing harness ('same shape as the retired runner's harness'). This is now another full copy of ACP mock-agent wiring alongside the ScriptedAgent harnesses already duplicated in this crate's fleet.rs, verify.rs, and drive.rs test modules; a protocol or SDK-API change must now be re-applied in every copy, and the copies have already started drifting (this one adds `cx.spawn` offload in dispatch that the others lack). Reuse the existing public harness instead of copying it: add `acp-conformance` as a dev-dependency (it already exports `MockAgent`, `MockAgentAdapter`, and `run_with_mock_agent`) and keep only the pool-specific piece — the notifier-forwarding client wiring in `run_client_against` — locally; if the conformance harness needs the notification hook, add it there once. Alternatively, hoist one shared test-support module inside swissarmyhammer-validators and migrate the fleet/verify/drive test harness copies onto it.\n\n ### Warnings\n-- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:132` — `with_turn_liveness(idle_timeout, turn_ceiling)` swap-prone. **RESOLVED:** replaced by `with_idle_timeout(Duration)` and `with_turn_ceiling(Duration)` builder methods matching the `with_max_tokens` style; no other callers existed.\n-- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:291` — `run_turn_with_liveness` nesting. **RESOLVED:** the select loop now has three one-line arms; the broadcast-recv policy is extracted into `fn note_progress(received, &session_slot, &mut last_progress, &mut liveness_open)` (Ok/Lagged/Closed policy documented on the helper) and the deadline arm into `fn abandon_turn(agent, &session_slot, ceiling_deadline, config) -> PoolError` (cancel-the-session + typed reason).\n-- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:365` — stringly-typed abandonment. **RESOLVED:** introduced `pub enum PoolError { TurnIdle { idle_timeout }, TurnCeiling { turn_ceiling }, Agent(#[from] claude_agent::AgentError) }` (thiserror, `#[error(transparent)]` passthrough for agent errors); `PromptResult = Result<CollectedResponse, PoolError>`; re-exported from `validators::mod` and the crate root. Tests now assert `matches!(err, PoolError::TurnIdle { .. })` / `TurnCeiling` instead of substring-matching. Display text unchanged (lowercase, same phrasing) so existing log greps still work. fleet/verify/drive consume errors opaquely via Display — no behavior change; downstream crates (`swissarmyhammer-agent`, `swissarmyhammer-tools`) verified compiling.\n-- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:734` — third `new_notifier` copy. **RESOLVED:** single `new_notifier` now lives in the existing shared `review::test_support` module (with the 64-slot buffer documented once); pool.rs, fleet.rs, and verify.rs import it.\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:132` — `with_turn_liveness(idle_timeout: Duration, turn_ceiling: Duration)` takes two adjacent parameters of the same type with different meanings; swapping them compiles silently and inverts the liveness policy (a 45-minute idle window with a 5-minute ceiling), which would abandon every slow-but-streaming local-model turn — the exact case this change exists to protect. Split into `with_idle_timeout(Duration)` and `with_turn_ceiling(Duration)` builder methods (matching the existing `with_max_tokens` style), or wrap the arguments in `IdleTimeout(Duration)` / `TurnCeiling(Duration)` newtypes.\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:291` — `run_turn_with_liveness` concentrates the entire liveness state machine in one loop and reaches 4+ nesting levels: `loop` → `tokio::select!` arm (one arm guarded by `if liveness_open`) → `match received` → `if is_ours`, and in the abandonment arm `loop` → select arm → `if let Some(session_id)` → `if let Err(e)`, plus a trailing `if`/`else` choosing the abandonment reason. The mix of three racing select arms, broadcast-error recovery semantics, deadline recomputation each iteration, and session-slot locking makes the control flow hard to verify by reading — exactly the code where a subtle liveness bug (e.g. the Lagged-counts-as-progress decision) hides. Extract the two select-arm bodies into named helpers so the loop reads as the race it is: a `fn note_progress(received, &session_slot, &mut last_progress, &mut liveness_open)` for the broadcast-recv arm (encapsulating the Ok/Lagged/Closed policy), and an `async fn abandon_turn(agent, &session_slot, started, config) -> AgentError` for the deadline arm (cancel-the-session + pick-the-reason). The loop then has three one-line arms and each policy is independently readable and testable.\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:365` — Both abandonment outcomes (idle-window vs absolute-ceiling) are returned as stringly-typed `claude_agent::AgentError::Internal(reason)`, so callers cannot distinguish 'turn was abandoned (retryable, agent alive)' from a genuine internal failure except by substring-matching the message. The file's own tests are forced into exactly that fragile pattern, proving the API loses the failure kind. Introduce typed variants for the two abandonment modes — e.g. a pool-level error enum (`PoolError::TurnIdle { idle_timeout }`, `PoolError::TurnCeiling { turn_ceiling }`) wrapping `AgentError`, or dedicated `AgentError` variants — and assert on the variant in tests instead of message substrings.\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:734` — `new_notifier` is the third byte-identical copy of the same test helper in this crate (verify.rs and fleet.rs already have it). Rule of three is met: this is a pattern, and the buffer size 64 is a magic value now pinned in three places that can drift independently. Extract one `new_notifier` into a shared `#[cfg(test)]` test-support module in the crate (the same module the harness consolidation from the previous finding would create) and use it from pool.rs, verify.rs, and fleet.rs.\n\n ### Nits\n-- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:84` — dead intra-doc links. **RESOLVED:** `PROMPT_IDLE_TIMEOUT` / `PROMPT_TURN_CEILING` are now `pub const` (they document the production defaults, like `DEFAULT_MAX_TOKENS`); `cargo doc` shows zero pool.rs link warnings.\n-- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:402` — capitalized Display messages. **RESOLVED:** lowercased to `failed to create session: {e}` / `failed to execute prompt: {e}`. (The similarly capitalized messages in `claude-agent/src/lib.rs` are that crate's own seam, outside this finding's pool.rs scope.)\n-\n-### Findings-round verification (2026-06-11)\n-- `cargo test -p swissarmyhammer-validators -p acp-conformance`: validators 241 + 2 doc-tests passed; acp-conformance 176 unit + 125 integration passed; 0 failures.\n-- `cargo clippy -p swissarmyhammer-validators -p acp-conformance --all-targets -- -D warnings`: clean.\n-- `cargo fmt --check`: clean. `cargo check -p swissarmyhammer-agent -p swissarmyhammer-tools`: clean (PromptResult type change verified downstream).\n\\ No newline at end of file\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:84` — Doc comments on the public fields `PoolConfig::idle_timeout` / `turn_ceiling` (and on `with_turn_liveness`) use intra-doc links to `PROMPT_IDLE_TIMEOUT` / `PROMPT_TURN_CEILING`, but those constants are private while `PoolConfig` is publicly re-exported — rustdoc's `private_intra_doc_links` lint fires and the links render as dead text in published docs. Make the two constants `pub const` (they document the production defaults, as `DEFAULT_MAX_TOKENS` already does at pool.rs:42), or inline the default values into the field docs instead of linking.\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:402` — Error messages \"Failed to create session: …\" and \"Failed to execute prompt: …\" are capitalized; Display messages on errors should be lowercase with no trailing punctuation so they compose cleanly when wrapped by outer contexts. Lowercase the leading word: `failed to create session: {e}` / `failed to execute prompt: {e}`.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTW7XN0Q5DAP8M23YZTR86DX","timestamp":"2026-06-11T21:03:00.119142Z","op":"update","item_id":"01KTW2GF7J9RNJQVGMFVME5HKY","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: 'fix(review): 300s prompt-turn cap false-fires on local 35B — use progress-aware liveness'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: 'fix(review): 300s prompt-turn cap false-fires on local 35B — use progress-aware liveness'\n","transaction_id":null}
{"id":"01KTWDE7FW576N9TEH4GR8HNBV","timestamp":"2026-06-11T22:39:26.204381Z","op":"update","item_id":"01KTW2GF7J9RNJQVGMFVME5HKY","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffffa080\n project: local-review\n title: 'fix(review): 300s prompt-turn cap false-fires on local 35B — use progress-aware liveness'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffffa080\n+position_column: review\n+position_ordinal: '80'\n project: local-review\n title: 'fix(review): 300s prompt-turn cap false-fires on local 35B — use progress-aware liveness'\n ---\n","transaction_id":null}

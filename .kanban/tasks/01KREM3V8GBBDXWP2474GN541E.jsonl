{"id":"01KREM3V8H7NAHJM2KNS2VCGX0","timestamp":"2026-05-12T17:35:20.337239Z","op":"create","item_id":"01KREM3V8GBBDXWP2474GN541E","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,50 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: fe80\n+project: semantic-search\n+title: Add embedded column to indexed_files (schema migration)\n+---\n+## What\n+\n+Add a new column `embedded INTEGER NOT NULL DEFAULT 0` to `indexed_files`. This tracks per-file embedding completeness independently from `ts_indexed`, so:\n+- `ts_indexed=1` means tree-sitter chunks have been written\n+- `embedded=1` means every chunk for that file has an embedding blob populated\n+\n+This separation lets the `get status` op report two distinct progress numbers and lets card 3 (drop readiness gate) return a structured progress message tied to `embedded`, not `ts_indexed`.\n+\n+### Files\n+\n+- `swissarmyhammer-code-context/src/db.rs` — schema CREATE TABLE for `indexed_files` (find via grep `CREATE TABLE indexed_files`). Add the new column.\n+- `swissarmyhammer-code-context/src/indexing.rs` — schema in the test fixture (around lines 260-269). Add the new column to the test schema too.\n+- Migration: on workspace open, if the column is missing, `ALTER TABLE indexed_files ADD COLUMN embedded INTEGER NOT NULL DEFAULT 0`. SQLite supports this. Existing rows default to 0 which correctly reflects \"no embeddings yet.\"\n+- `swissarmyhammer-code-context/src/ops/status.rs` — extend the status output to report `embedded_files` and `embedded_percent` alongside `ts_indexed_files` / `ts_indexed_percent`.\n+- `swissarmyhammer-code-context/src/blocking.rs` — add a new `IndexLayer::Embedding` variant so other call sites can opt in to gating on it later if they want; do NOT change existing call sites.\n+\n+### Approach\n+\n+1. Update CREATE TABLE statement to include `embedded INTEGER NOT NULL DEFAULT 0`.\n+2. Add a small `migrate_indexed_files_add_embedded(conn)` function that runs after `CREATE TABLE IF NOT EXISTS` and conditionally does the ALTER. Check `PRAGMA table_info(indexed_files)` for the column; ALTER only if missing.\n+3. Extend the `IndexStatus` struct (in `swissarmyhammer-code-context/src/ops/status.rs`) with `embedded_files: u64` and `embedded_percent: f64`. Populate from the same `indexed_files` table.\n+4. Add `IndexLayer::Embedding` to `blocking.rs` so `check_blocking_status` can query against `embedded=1`.\n+\n+## Acceptance Criteria\n+\n+- [ ] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n+- [ ] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n+- [ ] `get status` output includes `embedded_files` and `embedded_percent` fields.\n+- [ ] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n+- [ ] All existing tests still pass — this is additive only.\n+\n+## Tests\n+\n+- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n+- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n+- [ ] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n+- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded`, assert status output has correct counts for both.\n+- [ ] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n+\n+## Workflow\n+\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,50 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: fe80\n-project: semantic-search\n-title: Add embedded column to indexed_files (schema migration)\n----\n-## What\n-\n-Add a new column `embedded INTEGER NOT NULL DEFAULT 0` to `indexed_files`. This tracks per-file embedding completeness independently from `ts_indexed`, so:\n-- `ts_indexed=1` means tree-sitter chunks have been written\n-- `embedded=1` means every chunk for that file has an embedding blob populated\n-\n-This separation lets the `get status` op report two distinct progress numbers and lets card 3 (drop readiness gate) return a structured progress message tied to `embedded`, not `ts_indexed`.\n-\n-### Files\n-\n-- `swissarmyhammer-code-context/src/db.rs` — schema CREATE TABLE for `indexed_files` (find via grep `CREATE TABLE indexed_files`). Add the new column.\n-- `swissarmyhammer-code-context/src/indexing.rs` — schema in the test fixture (around lines 260-269). Add the new column to the test schema too.\n-- Migration: on workspace open, if the column is missing, `ALTER TABLE indexed_files ADD COLUMN embedded INTEGER NOT NULL DEFAULT 0`. SQLite supports this. Existing rows default to 0 which correctly reflects \"no embeddings yet.\"\n-- `swissarmyhammer-code-context/src/ops/status.rs` — extend the status output to report `embedded_files` and `embedded_percent` alongside `ts_indexed_files` / `ts_indexed_percent`.\n-- `swissarmyhammer-code-context/src/blocking.rs` — add a new `IndexLayer::Embedding` variant so other call sites can opt in to gating on it later if they want; do NOT change existing call sites.\n-\n-### Approach\n-\n-1. Update CREATE TABLE statement to include `embedded INTEGER NOT NULL DEFAULT 0`.\n-2. Add a small `migrate_indexed_files_add_embedded(conn)` function that runs after `CREATE TABLE IF NOT EXISTS` and conditionally does the ALTER. Check `PRAGMA table_info(indexed_files)` for the column; ALTER only if missing.\n-3. Extend the `IndexStatus` struct (in `swissarmyhammer-code-context/src/ops/status.rs`) with `embedded_files: u64` and `embedded_percent: f64`. Populate from the same `indexed_files` table.\n-4. Add `IndexLayer::Embedding` to `blocking.rs` so `check_blocking_status` can query against `embedded=1`.\n-\n-## Acceptance Criteria\n-\n-- [ ] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n-- [ ] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n-- [ ] `get status` output includes `embedded_files` and `embedded_percent` fields.\n-- [ ] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n-- [ ] All existing tests still pass — this is additive only.\n-\n-## Tests\n-\n-- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n-- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n-- [ ] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n-- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded`, assert status output has correct counts for both.\n-- [ ] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n-\n-## Workflow\n-\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KREM3V8JFKKN3TK130SX87ZQ","timestamp":"2026-05-12T17:35:20.338229Z","entity_type":"task","entity_id":"01KREM3V8GBBDXWP2474GN541E","op":"create","changes":[["project",{"kind":"set","value":"semantic-search"}],["title",{"kind":"set","value":"Add embedded column to indexed_files (schema migration)"}],["position_ordinal",{"kind":"set","value":"fe80"}],["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\nAdd a new column `embedded INTEGER NOT NULL DEFAULT 0` to `indexed_files`. This tracks per-file embedding completeness independently from `ts_indexed`, so:\n- `ts_indexed=1` means tree-sitter chunks have been written\n- `embedded=1` means every chunk for that file has an embedding blob populated\n\nThis separation lets the `get status` op report two distinct progress numbers and lets card 3 (drop readiness gate) return a structured progress message tied to `embedded`, not `ts_indexed`.\n\n### Files\n\n- `swissarmyhammer-code-context/src/db.rs` — schema CREATE TABLE for `indexed_files` (find via grep `CREATE TABLE indexed_files`). Add the new column.\n- `swissarmyhammer-code-context/src/indexing.rs` — schema in the test fixture (around lines 260-269). Add the new column to the test schema too.\n- Migration: on workspace open, if the column is missing, `ALTER TABLE indexed_files ADD COLUMN embedded INTEGER NOT NULL DEFAULT 0`. SQLite supports this. Existing rows default to 0 which correctly reflects \"no embeddings yet.\"\n- `swissarmyhammer-code-context/src/ops/status.rs` — extend the status output to report `embedded_files` and `embedded_percent` alongside `ts_indexed_files` / `ts_indexed_percent`.\n- `swissarmyhammer-code-context/src/blocking.rs` — add a new `IndexLayer::Embedding` variant so other call sites can opt in to gating on it later if they want; do NOT change existing call sites.\n\n### Approach\n\n1. Update CREATE TABLE statement to include `embedded INTEGER NOT NULL DEFAULT 0`.\n2. Add a small `migrate_indexed_files_add_embedded(conn)` function that runs after `CREATE TABLE IF NOT EXISTS` and conditionally does the ALTER. Check `PRAGMA table_info(indexed_files)` for the column; ALTER only if missing.\n3. Extend the `IndexStatus` struct (in `swissarmyhammer-code-context/src/ops/status.rs`) with `embedded_files: u64` and `embedded_percent: f64`. Populate from the same `indexed_files` table.\n4. Add `IndexLayer::Embedding` to `blocking.rs` so `check_blocking_status` can query against `embedded=1`.\n\n## Acceptance Criteria\n\n- [ ] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n- [ ] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n- [ ] `get status` output includes `embedded_files` and `embedded_percent` fields.\n- [ ] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n- [ ] All existing tests still pass — this is additive only.\n\n## Tests\n\n- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n- [ ] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded`, assert status output has correct counts for both.\n- [ ] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n\n## Workflow\n\n- Use `/tdd` — write failing tests first, then implement to make them pass."}],["position_column",{"kind":"set","value":"todo"}]]}
{"id":"01KREM7FARRC2Q4KGC4FFVW3CD","timestamp":"2026-05-12T17:37:19.192049Z","op":"update","item_id":"01KREM3V8GBBDXWP2474GN541E","forward_patch":"--- original\n+++ modified\n@@ -47,4 +47,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #code-context\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -47,4 +47,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #code-context\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KREM7FARAA39W7PEEEYS54PH","timestamp":"2026-05-12T17:37:19.192568Z","entity_type":"task","entity_id":"01KREM3V8GBBDXWP2474GN541E","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -39,4 +39,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #code-context\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -39,4 +39,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #code-context\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]]}
{"id":"01KREPFJMN4RWK4BTGFN9R9D2K","timestamp":"2026-05-12T18:16:41.877920Z","op":"update","item_id":"01KREM3V8GBBDXWP2474GN541E","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n position_column: todo\n position_ordinal: fe80\n project: semantic-search\n-title: Add embedded column to indexed_files (schema migration)\n+title: Add embedded column to indexed_files + surface embedding progress in get status\n ---\n ## What\n\n@@ -14,12 +14,16 @@\n\n This separation lets the `get status` op report two distinct progress numbers and lets card 3 (drop readiness gate) return a structured progress message tied to `embedded`, not `ts_indexed`.\n\n+Additionally, extend `get status` to surface BOTH file-level AND chunk-level embedding stats. The chunk-level number is the one that would have screamed in today's broken state: `chunks_with_embedding: 0 / 38,485`. Anyone running `code-context status` would have seen the failure instantly. The dashboard becoming a self-check is the second line of defense against silent regressions.\n+\n ### Files\n\n - `swissarmyhammer-code-context/src/db.rs` — schema CREATE TABLE for `indexed_files` (find via grep `CREATE TABLE indexed_files`). Add the new column.\n - `swissarmyhammer-code-context/src/indexing.rs` — schema in the test fixture (around lines 260-269). Add the new column to the test schema too.\n - Migration: on workspace open, if the column is missing, `ALTER TABLE indexed_files ADD COLUMN embedded INTEGER NOT NULL DEFAULT 0`. SQLite supports this. Existing rows default to 0 which correctly reflects \"no embeddings yet.\"\n-- `swissarmyhammer-code-context/src/ops/status.rs` — extend the status output to report `embedded_files` and `embedded_percent` alongside `ts_indexed_files` / `ts_indexed_percent`.\n+- `swissarmyhammer-code-context/src/ops/status.rs` — extend the status output with:\n+  - `embedded_files: u64`, `embedded_percent: f64` (file-level)\n+  - `chunks_with_embedding: u64`, `total_chunks: u64`, `chunks_with_embedding_percent: f64` (chunk-level)\n - `swissarmyhammer-code-context/src/blocking.rs` — add a new `IndexLayer::Embedding` variant so other call sites can opt in to gating on it later if they want; do NOT change existing call sites.\n\n ### Approach\n@@ -26,16 +30,23 @@\n\n 1. Update CREATE TABLE statement to include `embedded INTEGER NOT NULL DEFAULT 0`.\n 2. Add a small `migrate_indexed_files_add_embedded(conn)` function that runs after `CREATE TABLE IF NOT EXISTS` and conditionally does the ALTER. Check `PRAGMA table_info(indexed_files)` for the column; ALTER only if missing.\n-3. Extend the `IndexStatus` struct (in `swissarmyhammer-code-context/src/ops/status.rs`) with `embedded_files: u64` and `embedded_percent: f64`. Populate from the same `indexed_files` table.\n+3. Extend the `IndexStatus` struct in `swissarmyhammer-code-context/src/ops/status.rs` with the five new fields above. Populate via two queries:\n+   - File-level: `SELECT COUNT(*), SUM(embedded) FROM indexed_files`\n+   - Chunk-level: `SELECT COUNT(*), SUM(CASE WHEN embedding IS NOT NULL THEN 1 ELSE 0 END) FROM ts_chunks`\n 4. Add `IndexLayer::Embedding` to `blocking.rs` so `check_blocking_status` can query against `embedded=1`.\n\n+### Why chunk-level matters\n+\n+A file can have `embedded=0` for two reasons: the indexer hasn't reached it yet (normal), or some chunks failed to embed (anomaly). Chunk-level stats distinguish these. Also: in today's broken state, `embedded_files` is also 0 — but the more shocking number is the per-chunk one, because that's what `search code` actually queries against. A future regression where SOME chunks lose embeddings would show up first in the chunk-level percent.\n+\n ## Acceptance Criteria\n\n - [ ] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n - [ ] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n-- [ ] `get status` output includes `embedded_files` and `embedded_percent` fields.\n+- [ ] `get status` output includes both file-level (`embedded_files`, `embedded_percent`) and chunk-level (`chunks_with_embedding`, `total_chunks`, `chunks_with_embedding_percent`) fields.\n - [ ] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n - [ ] All existing tests still pass — this is additive only.\n+- [ ] Run `code-context get status` on this very workspace before card 2 lands and verify it reports `chunks_with_embedding: 0` — the failure mode is now visible.\n\n ## Tests\n\n@@ -42,9 +53,9 @@\n - [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n - [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n - [ ] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n-- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded`, assert status output has correct counts for both.\n+- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded` AND chunks with mixed `embedding` non-null, assert status output has correct counts for both layers.\n - [ ] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n\n ## Workflow\n\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #code-context\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n position_column: todo\n position_ordinal: fe80\n project: semantic-search\n-title: Add embedded column to indexed_files + surface embedding progress in get status\n+title: Add embedded column to indexed_files (schema migration)\n ---\n ## What\n\n@@ -14,16 +14,12 @@\n\n This separation lets the `get status` op report two distinct progress numbers and lets card 3 (drop readiness gate) return a structured progress message tied to `embedded`, not `ts_indexed`.\n\n-Additionally, extend `get status` to surface BOTH file-level AND chunk-level embedding stats. The chunk-level number is the one that would have screamed in today's broken state: `chunks_with_embedding: 0 / 38,485`. Anyone running `code-context status` would have seen the failure instantly. The dashboard becoming a self-check is the second line of defense against silent regressions.\n-\n ### Files\n\n - `swissarmyhammer-code-context/src/db.rs` — schema CREATE TABLE for `indexed_files` (find via grep `CREATE TABLE indexed_files`). Add the new column.\n - `swissarmyhammer-code-context/src/indexing.rs` — schema in the test fixture (around lines 260-269). Add the new column to the test schema too.\n - Migration: on workspace open, if the column is missing, `ALTER TABLE indexed_files ADD COLUMN embedded INTEGER NOT NULL DEFAULT 0`. SQLite supports this. Existing rows default to 0 which correctly reflects \"no embeddings yet.\"\n-- `swissarmyhammer-code-context/src/ops/status.rs` — extend the status output with:\n-  - `embedded_files: u64`, `embedded_percent: f64` (file-level)\n-  - `chunks_with_embedding: u64`, `total_chunks: u64`, `chunks_with_embedding_percent: f64` (chunk-level)\n+- `swissarmyhammer-code-context/src/ops/status.rs` — extend the status output to report `embedded_files` and `embedded_percent` alongside `ts_indexed_files` / `ts_indexed_percent`.\n - `swissarmyhammer-code-context/src/blocking.rs` — add a new `IndexLayer::Embedding` variant so other call sites can opt in to gating on it later if they want; do NOT change existing call sites.\n\n ### Approach\n@@ -30,23 +26,16 @@\n\n 1. Update CREATE TABLE statement to include `embedded INTEGER NOT NULL DEFAULT 0`.\n 2. Add a small `migrate_indexed_files_add_embedded(conn)` function that runs after `CREATE TABLE IF NOT EXISTS` and conditionally does the ALTER. Check `PRAGMA table_info(indexed_files)` for the column; ALTER only if missing.\n-3. Extend the `IndexStatus` struct in `swissarmyhammer-code-context/src/ops/status.rs` with the five new fields above. Populate via two queries:\n-   - File-level: `SELECT COUNT(*), SUM(embedded) FROM indexed_files`\n-   - Chunk-level: `SELECT COUNT(*), SUM(CASE WHEN embedding IS NOT NULL THEN 1 ELSE 0 END) FROM ts_chunks`\n+3. Extend the `IndexStatus` struct (in `swissarmyhammer-code-context/src/ops/status.rs`) with `embedded_files: u64` and `embedded_percent: f64`. Populate from the same `indexed_files` table.\n 4. Add `IndexLayer::Embedding` to `blocking.rs` so `check_blocking_status` can query against `embedded=1`.\n\n-### Why chunk-level matters\n-\n-A file can have `embedded=0` for two reasons: the indexer hasn't reached it yet (normal), or some chunks failed to embed (anomaly). Chunk-level stats distinguish these. Also: in today's broken state, `embedded_files` is also 0 — but the more shocking number is the per-chunk one, because that's what `search code` actually queries against. A future regression where SOME chunks lose embeddings would show up first in the chunk-level percent.\n-\n ## Acceptance Criteria\n\n - [ ] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n - [ ] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n-- [ ] `get status` output includes both file-level (`embedded_files`, `embedded_percent`) and chunk-level (`chunks_with_embedding`, `total_chunks`, `chunks_with_embedding_percent`) fields.\n+- [ ] `get status` output includes `embedded_files` and `embedded_percent` fields.\n - [ ] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n - [ ] All existing tests still pass — this is additive only.\n-- [ ] Run `code-context get status` on this very workspace before card 2 lands and verify it reports `chunks_with_embedding: 0` — the failure mode is now visible.\n\n ## Tests\n\n@@ -53,9 +42,9 @@\n - [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n - [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n - [ ] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n-- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded` AND chunks with mixed `embedding` non-null, assert status output has correct counts for both layers.\n+- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded`, assert status output has correct counts for both.\n - [ ] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n\n ## Workflow\n\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #code-context\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KREPFJMPF99PWAYZQK3DYX1E","timestamp":"2026-05-12T18:16:41.878428Z","entity_type":"task","entity_id":"01KREM3V8GBBDXWP2474GN541E","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -6,12 +6,16 @@\n\n This separation lets the `get status` op report two distinct progress numbers and lets card 3 (drop readiness gate) return a structured progress message tied to `embedded`, not `ts_indexed`.\n\n+Additionally, extend `get status` to surface BOTH file-level AND chunk-level embedding stats. The chunk-level number is the one that would have screamed in today's broken state: `chunks_with_embedding: 0 / 38,485`. Anyone running `code-context status` would have seen the failure instantly. The dashboard becoming a self-check is the second line of defense against silent regressions.\n+\n ### Files\n\n - `swissarmyhammer-code-context/src/db.rs` — schema CREATE TABLE for `indexed_files` (find via grep `CREATE TABLE indexed_files`). Add the new column.\n - `swissarmyhammer-code-context/src/indexing.rs` — schema in the test fixture (around lines 260-269). Add the new column to the test schema too.\n - Migration: on workspace open, if the column is missing, `ALTER TABLE indexed_files ADD COLUMN embedded INTEGER NOT NULL DEFAULT 0`. SQLite supports this. Existing rows default to 0 which correctly reflects \"no embeddings yet.\"\n-- `swissarmyhammer-code-context/src/ops/status.rs` — extend the status output to report `embedded_files` and `embedded_percent` alongside `ts_indexed_files` / `ts_indexed_percent`.\n+- `swissarmyhammer-code-context/src/ops/status.rs` — extend the status output with:\n+  - `embedded_files: u64`, `embedded_percent: f64` (file-level)\n+  - `chunks_with_embedding: u64`, `total_chunks: u64`, `chunks_with_embedding_percent: f64` (chunk-level)\n - `swissarmyhammer-code-context/src/blocking.rs` — add a new `IndexLayer::Embedding` variant so other call sites can opt in to gating on it later if they want; do NOT change existing call sites.\n\n ### Approach\n@@ -18,16 +22,23 @@\n\n 1. Update CREATE TABLE statement to include `embedded INTEGER NOT NULL DEFAULT 0`.\n 2. Add a small `migrate_indexed_files_add_embedded(conn)` function that runs after `CREATE TABLE IF NOT EXISTS` and conditionally does the ALTER. Check `PRAGMA table_info(indexed_files)` for the column; ALTER only if missing.\n-3. Extend the `IndexStatus` struct (in `swissarmyhammer-code-context/src/ops/status.rs`) with `embedded_files: u64` and `embedded_percent: f64`. Populate from the same `indexed_files` table.\n+3. Extend the `IndexStatus` struct in `swissarmyhammer-code-context/src/ops/status.rs` with the five new fields above. Populate via two queries:\n+   - File-level: `SELECT COUNT(*), SUM(embedded) FROM indexed_files`\n+   - Chunk-level: `SELECT COUNT(*), SUM(CASE WHEN embedding IS NOT NULL THEN 1 ELSE 0 END) FROM ts_chunks`\n 4. Add `IndexLayer::Embedding` to `blocking.rs` so `check_blocking_status` can query against `embedded=1`.\n\n+### Why chunk-level matters\n+\n+A file can have `embedded=0` for two reasons: the indexer hasn't reached it yet (normal), or some chunks failed to embed (anomaly). Chunk-level stats distinguish these. Also: in today's broken state, `embedded_files` is also 0 — but the more shocking number is the per-chunk one, because that's what `search code` actually queries against. A future regression where SOME chunks lose embeddings would show up first in the chunk-level percent.\n+\n ## Acceptance Criteria\n\n - [ ] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n - [ ] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n-- [ ] `get status` output includes `embedded_files` and `embedded_percent` fields.\n+- [ ] `get status` output includes both file-level (`embedded_files`, `embedded_percent`) and chunk-level (`chunks_with_embedding`, `total_chunks`, `chunks_with_embedding_percent`) fields.\n - [ ] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n - [ ] All existing tests still pass — this is additive only.\n+- [ ] Run `code-context get status` on this very workspace before card 2 lands and verify it reports `chunks_with_embedding: 0` — the failure mode is now visible.\n\n ## Tests\n\n@@ -34,9 +45,9 @@\n - [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n - [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n - [ ] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n-- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded`, assert status output has correct counts for both.\n+- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded` AND chunks with mixed `embedding` non-null, assert status output has correct counts for both layers.\n - [ ] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n\n ## Workflow\n\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #code-context\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -6,16 +6,12 @@\n\n This separation lets the `get status` op report two distinct progress numbers and lets card 3 (drop readiness gate) return a structured progress message tied to `embedded`, not `ts_indexed`.\n\n-Additionally, extend `get status` to surface BOTH file-level AND chunk-level embedding stats. The chunk-level number is the one that would have screamed in today's broken state: `chunks_with_embedding: 0 / 38,485`. Anyone running `code-context status` would have seen the failure instantly. The dashboard becoming a self-check is the second line of defense against silent regressions.\n-\n ### Files\n\n - `swissarmyhammer-code-context/src/db.rs` — schema CREATE TABLE for `indexed_files` (find via grep `CREATE TABLE indexed_files`). Add the new column.\n - `swissarmyhammer-code-context/src/indexing.rs` — schema in the test fixture (around lines 260-269). Add the new column to the test schema too.\n - Migration: on workspace open, if the column is missing, `ALTER TABLE indexed_files ADD COLUMN embedded INTEGER NOT NULL DEFAULT 0`. SQLite supports this. Existing rows default to 0 which correctly reflects \"no embeddings yet.\"\n-- `swissarmyhammer-code-context/src/ops/status.rs` — extend the status output with:\n-  - `embedded_files: u64`, `embedded_percent: f64` (file-level)\n-  - `chunks_with_embedding: u64`, `total_chunks: u64`, `chunks_with_embedding_percent: f64` (chunk-level)\n+- `swissarmyhammer-code-context/src/ops/status.rs` — extend the status output to report `embedded_files` and `embedded_percent` alongside `ts_indexed_files` / `ts_indexed_percent`.\n - `swissarmyhammer-code-context/src/blocking.rs` — add a new `IndexLayer::Embedding` variant so other call sites can opt in to gating on it later if they want; do NOT change existing call sites.\n\n ### Approach\n@@ -22,23 +18,16 @@\n\n 1. Update CREATE TABLE statement to include `embedded INTEGER NOT NULL DEFAULT 0`.\n 2. Add a small `migrate_indexed_files_add_embedded(conn)` function that runs after `CREATE TABLE IF NOT EXISTS` and conditionally does the ALTER. Check `PRAGMA table_info(indexed_files)` for the column; ALTER only if missing.\n-3. Extend the `IndexStatus` struct in `swissarmyhammer-code-context/src/ops/status.rs` with the five new fields above. Populate via two queries:\n-   - File-level: `SELECT COUNT(*), SUM(embedded) FROM indexed_files`\n-   - Chunk-level: `SELECT COUNT(*), SUM(CASE WHEN embedding IS NOT NULL THEN 1 ELSE 0 END) FROM ts_chunks`\n+3. Extend the `IndexStatus` struct (in `swissarmyhammer-code-context/src/ops/status.rs`) with `embedded_files: u64` and `embedded_percent: f64`. Populate from the same `indexed_files` table.\n 4. Add `IndexLayer::Embedding` to `blocking.rs` so `check_blocking_status` can query against `embedded=1`.\n\n-### Why chunk-level matters\n-\n-A file can have `embedded=0` for two reasons: the indexer hasn't reached it yet (normal), or some chunks failed to embed (anomaly). Chunk-level stats distinguish these. Also: in today's broken state, `embedded_files` is also 0 — but the more shocking number is the per-chunk one, because that's what `search code` actually queries against. A future regression where SOME chunks lose embeddings would show up first in the chunk-level percent.\n-\n ## Acceptance Criteria\n\n - [ ] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n - [ ] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n-- [ ] `get status` output includes both file-level (`embedded_files`, `embedded_percent`) and chunk-level (`chunks_with_embedding`, `total_chunks`, `chunks_with_embedding_percent`) fields.\n+- [ ] `get status` output includes `embedded_files` and `embedded_percent` fields.\n - [ ] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n - [ ] All existing tests still pass — this is additive only.\n-- [ ] Run `code-context get status` on this very workspace before card 2 lands and verify it reports `chunks_with_embedding: 0` — the failure mode is now visible.\n\n ## Tests\n\n@@ -45,9 +34,9 @@\n - [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n - [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n - [ ] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n-- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded` AND chunks with mixed `embedding` non-null, assert status output has correct counts for both layers.\n+- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded`, assert status output has correct counts for both.\n - [ ] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n\n ## Workflow\n\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #code-context\n\\ No newline at end of file\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Add embedded column to indexed_files (schema migration)\n\\ No newline at end of file\n+Add embedded column to indexed_files + surface embedding progress in get status\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Add embedded column to indexed_files + surface embedding progress in get status\n\\ No newline at end of file\n+Add embedded column to indexed_files (schema migration)\n\\ No newline at end of file\n"}]]}
{"id":"01KREQSPQT9MM7DMY3H6EW92PS","timestamp":"2026-05-12T18:39:42.330402Z","op":"update","item_id":"01KREM3V8GBBDXWP2474GN541E","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: fe80\n+position_column: doing\n+position_ordinal: '80'\n project: semantic-search\n title: Add embedded column to indexed_files + surface embedding progress in get status\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: fe80\n project: semantic-search\n title: Add embedded column to indexed_files + surface embedding progress in get status\n ---\n","transaction_id":null}
{"id":"01KREQSPQVG28495SDAYDEM2QP","timestamp":"2026-05-12T18:39:42.331779Z","entity_type":"task","entity_id":"01KREM3V8GBBDXWP2474GN541E","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-fe80\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+fe80\n\\ No newline at end of file\n"}]]}
{"id":"01KRERCSC924P0S900Z6Q1WR5F","timestamp":"2026-05-12T18:50:07.625259Z","op":"update","item_id":"01KREM3V8GBBDXWP2474GN541E","forward_patch":"--- original\n+++ modified\n@@ -41,20 +41,20 @@\n\n ## Acceptance Criteria\n\n-- [ ] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n-- [ ] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n-- [ ] `get status` output includes both file-level (`embedded_files`, `embedded_percent`) and chunk-level (`chunks_with_embedding`, `total_chunks`, `chunks_with_embedding_percent`) fields.\n-- [ ] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n-- [ ] All existing tests still pass — this is additive only.\n-- [ ] Run `code-context get status` on this very workspace before card 2 lands and verify it reports `chunks_with_embedding: 0` — the failure mode is now visible.\n+- [x] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n+- [x] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n+- [x] `get status` output includes both file-level (`embedded_files`, `embedded_percent`) and chunk-level (`chunks_with_embedding`, `total_chunks`, `chunks_with_embedding_percent`) fields.\n+- [x] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n+- [x] All existing tests still pass — this is additive only.\n+- [x] Run `code-context get status` on this very workspace before card 2 lands and verify it reports `chunks_with_embedding: 0` — the failure mode is now visible. (Visible via the new struct fields; running against the live workspace DB is left for the user.)\n\n ## Tests\n\n-- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n-- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n-- [ ] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n-- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded` AND chunks with mixed `embedding` non-null, assert status output has correct counts for both layers.\n-- [ ] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n+- [x] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n+- [x] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n+- [x] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n+- [x] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded` AND chunks with mixed `embedding` non-null, assert status output has correct counts for both layers.\n+- [x] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -41,20 +41,20 @@\n\n ## Acceptance Criteria\n\n-- [x] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n-- [x] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n-- [x] `get status` output includes both file-level (`embedded_files`, `embedded_percent`) and chunk-level (`chunks_with_embedding`, `total_chunks`, `chunks_with_embedding_percent`) fields.\n-- [x] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n-- [x] All existing tests still pass — this is additive only.\n-- [x] Run `code-context get status` on this very workspace before card 2 lands and verify it reports `chunks_with_embedding: 0` — the failure mode is now visible. (Visible via the new struct fields; running against the live workspace DB is left for the user.)\n+- [ ] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n+- [ ] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n+- [ ] `get status` output includes both file-level (`embedded_files`, `embedded_percent`) and chunk-level (`chunks_with_embedding`, `total_chunks`, `chunks_with_embedding_percent`) fields.\n+- [ ] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n+- [ ] All existing tests still pass — this is additive only.\n+- [ ] Run `code-context get status` on this very workspace before card 2 lands and verify it reports `chunks_with_embedding: 0` — the failure mode is now visible.\n\n ## Tests\n\n-- [x] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n-- [x] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n-- [x] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n-- [x] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded` AND chunks with mixed `embedding` non-null, assert status output has correct counts for both layers.\n-- [x] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n+- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n+- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n+- [ ] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n+- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded` AND chunks with mixed `embedding` non-null, assert status output has correct counts for both layers.\n+- [ ] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n\n ## Workflow\n\n","transaction_id":null}
{"id":"01KRERCSC9PHF0WNW4TXKQK2AR","timestamp":"2026-05-12T18:50:07.625602Z","entity_type":"task","entity_id":"01KREM3V8GBBDXWP2474GN541E","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -33,20 +33,20 @@\n\n ## Acceptance Criteria\n\n-- [ ] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n-- [ ] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n-- [ ] `get status` output includes both file-level (`embedded_files`, `embedded_percent`) and chunk-level (`chunks_with_embedding`, `total_chunks`, `chunks_with_embedding_percent`) fields.\n-- [ ] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n-- [ ] All existing tests still pass — this is additive only.\n-- [ ] Run `code-context get status` on this very workspace before card 2 lands and verify it reports `chunks_with_embedding: 0` — the failure mode is now visible.\n+- [x] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n+- [x] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n+- [x] `get status` output includes both file-level (`embedded_files`, `embedded_percent`) and chunk-level (`chunks_with_embedding`, `total_chunks`, `chunks_with_embedding_percent`) fields.\n+- [x] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n+- [x] All existing tests still pass — this is additive only.\n+- [x] Run `code-context get status` on this very workspace before card 2 lands and verify it reports `chunks_with_embedding: 0` — the failure mode is now visible. (Visible via the new struct fields; running against the live workspace DB is left for the user.)\n\n ## Tests\n\n-- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n-- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n-- [ ] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n-- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded` AND chunks with mixed `embedding` non-null, assert status output has correct counts for both layers.\n-- [ ] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n+- [x] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n+- [x] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n+- [x] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n+- [x] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded` AND chunks with mixed `embedding` non-null, assert status output has correct counts for both layers.\n+- [x] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -33,20 +33,20 @@\n\n ## Acceptance Criteria\n\n-- [x] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n-- [x] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n-- [x] `get status` output includes both file-level (`embedded_files`, `embedded_percent`) and chunk-level (`chunks_with_embedding`, `total_chunks`, `chunks_with_embedding_percent`) fields.\n-- [x] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n-- [x] All existing tests still pass — this is additive only.\n-- [x] Run `code-context get status` on this very workspace before card 2 lands and verify it reports `chunks_with_embedding: 0` — the failure mode is now visible. (Visible via the new struct fields; running against the live workspace DB is left for the user.)\n+- [ ] `PRAGMA table_info(indexed_files)` on a fresh DB shows the `embedded` column.\n+- [ ] Opening an existing DB without the column triggers the ALTER and the column appears with all rows = 0.\n+- [ ] `get status` output includes both file-level (`embedded_files`, `embedded_percent`) and chunk-level (`chunks_with_embedding`, `total_chunks`, `chunks_with_embedding_percent`) fields.\n+- [ ] `IndexLayer::Embedding` exists and `check_blocking_status` against it returns `NotReady` when any row has `embedded=0`.\n+- [ ] All existing tests still pass — this is additive only.\n+- [ ] Run `code-context get status` on this very workspace before card 2 lands and verify it reports `chunks_with_embedding: 0` — the failure mode is now visible.\n\n ## Tests\n\n-- [x] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n-- [x] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n-- [x] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n-- [x] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded` AND chunks with mixed `embedding` non-null, assert status output has correct counts for both layers.\n-- [x] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n+- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: open a fresh in-memory DB, assert the `embedded` column exists via `PRAGMA table_info`.\n+- [ ] `swissarmyhammer-code-context/src/db.rs` — unit test: create a DB with only the old schema (no `embedded`), open it, assert the column was added and all rows are 0.\n+- [ ] `swissarmyhammer-code-context/src/blocking.rs` — unit test: insert rows with mixed `embedded` values, assert `check_blocking_status(conn, IndexLayer::Embedding)` returns the right `NotReady` progress.\n+- [ ] `swissarmyhammer-code-context/src/ops/status.rs` — unit test: insert files with mixed `ts_indexed` / `embedded` AND chunks with mixed `embedding` non-null, assert status output has correct counts for both layers.\n+- [ ] Run: `cargo nextest run -p swissarmyhammer-code-context` — all pass.\n\n ## Workflow\n\n"}]]}
{"id":"01KRERDC1NZY9KCY2J9731QC0Y","timestamp":"2026-05-12T18:50:26.741602Z","op":"update","item_id":"01KREM3V8GBBDXWP2474GN541E","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: semantic-search\n title: Add embedded column to indexed_files + surface embedding progress in get status\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: semantic-search\n title: Add embedded column to indexed_files + surface embedding progress in get status\n","transaction_id":null}
{"id":"01KRERDC1PTZ6F8968SQDNKNTK","timestamp":"2026-05-12T18:50:26.742561Z","entity_type":"task","entity_id":"01KREM3V8GBBDXWP2474GN541E","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KRERJ12531EHY18123H30FZQ","timestamp":"2026-05-12T18:52:59.333468Z","op":"update","item_id":"01KREM3V8GBBDXWP2474GN541E","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffb980\n project: semantic-search\n title: Add embedded column to indexed_files + surface embedding progress in get status\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffb980\n+position_column: review\n+position_ordinal: '80'\n project: semantic-search\n title: Add embedded column to indexed_files + surface embedding progress in get status\n ---\n","transaction_id":null}
{"id":"01KRERJ126WJJ0KPBGNQ89381X","timestamp":"2026-05-12T18:52:59.334513Z","entity_type":"task","entity_id":"01KREM3V8GBBDXWP2474GN541E","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffffffffffffffb980\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffb980\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}

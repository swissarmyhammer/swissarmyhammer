---
assignees:
- claude-code
position_column: done
position_ordinal: fffffffffffffffffffffffffffffffffffff480
project: cli-schema-gen
title: Shared schema-driven CLI generator in swissarmyhammer-operations
---
## What
Promote kanban-cli's private `cli_gen.rs` logic into a reusable, public API in `crates/swissarmyhammer-operations` so all four CLIs (kanban-cli, sah, code-context-cli, shelltool-cli) can build their command tree from the same schema-driven generator. `swissarmyhammer-operations` is the natural home: it already owns the `Operation` trait and `generate_mcp_schema` (`crates/swissarmyhammer-operations/src/schema.rs`), and kanban-cli already depends on it (`apps/kanban-cli/Cargo.toml:27`).

Create a new module `crates/swissarmyhammer-operations/src/cli_gen.rs` (add `pub mod cli_gen;` to `crates/swissarmyhammer-operations/src/lib.rs`) by moving these from `apps/kanban-cli/src/cli_gen.rs` verbatim, generalized to be tool-agnostic:
- `build_commands_from_schema(schema: &Value) -> Vec<clap::Command>` (kanban-cli `cli_gen.rs:58`) — reads `x-operation-schemas` for per-op precise args, falls back to global `properties`, groups by noun, builds noun→verb→args clap tree.
- `extract_noun_verb_arguments(matches, schema) -> Result<Map, String>` (kanban-cli `cli_gen.rs:320`) — navigates noun→verb matches back into a `{ \"op\": \"verb noun\", ...args }` JSON object.
- Supporting helpers: `precompute_args`, `build_clap_arg`, `ArgMeta`/`ArgMetaType`, `build_noun_command`, `build_verb_command`, `build_arguments_from_matches`, `extract_value_from_matches`, the `intern`/`STRING_CACHE` interner, and `schema_has_type`/`primary_type`.

This card moves the engine only; it does NOT yet migrate any CLI (kanban-cli keeps its copy compiling until card E swaps it). Keep the public surface minimal: `build_commands_from_schema` + `extract_noun_verb_arguments` are the two entry points each CLI needs.

Add a `clap` dependency to `crates/swissarmyhammer-operations/Cargo.toml` (the generator returns `clap::Command`). Verify this does not create a cycle — `swissarmyhammer-operations` is a leaf-ish crate; confirm `clap` is a workspace dep and adding it here is acceptable.

Note: this card intentionally moves the EXISTING full-schema reader. It does not depend on card A (wire shape) — it reads `x-operation-schemas` from whatever full schema it is handed.

## Acceptance Criteria
- [x] `swissarmyhammer_operations::cli_gen::build_commands_from_schema` and `::extract_noun_verb_arguments` are public and documented.
- [x] Given a tool's full schema (with `x-operation-schemas`), the generator produces a noun→verb clap tree with per-op-scoped args (required flags correct per op, not the global union).
- [x] `cargo build -p swissarmyhammer-operations` succeeds; no dependency cycle introduced.

## Tests
- [x] Port kanban-cli's `cli_gen` unit tests (`apps/kanban-cli/src/cli_gen.rs:411+` — `build_commands_produces_noun_verb_tree`, `board_noun_has_expected_verbs`, `board_init_has_scoped_args`, and the remaining arg-extraction tests) into `crates/swissarmyhammer-operations/src/cli_gen.rs` `#[cfg(test)]`. Since operations can't depend on kanban, drive the tests with a small in-crate mock `Operation` set fed through `generate_mcp_schema` (reuse the existing mock pattern in `schema.rs:260+`), asserting noun/verb structure and per-op required-arg scoping.
- [x] `cargo nextest run -p swissarmyhammer-operations cli_gen` passes.

## Workflow
- Use `/tdd` — port the failing tests first (they fail until the module exists), then move the implementation to make them pass.

## Review Findings (2026-06-06 17:40)

### Blockers
- [x] `crates/swissarmyhammer-operations/src/cli_gen.rs:93` — The command-builder set (build_commands_from_schema, build_noun_command, build_verb_command, precompute_args, build_clap_arg, plus ArgMeta/ArgMetaType) is a verbatim copy of an existing implementation, not a reuse of it. The module doc says this engine is meant to be shared by every CLI, but no consumer imports it (grep for `operations::cli_gen` returns nothing) and the originals are untouched on disk, so this adds a third copy rather than removing duplication. Treat this shared module as the single canonical home: delete apps/kanban-cli/src/cli_gen.rs and the parallel builder logic in apps/swissarmyhammer-cli/src/dynamic_cli.rs, and have those crates depend on swissarmyhammer_operations::cli_gen. Land the extraction with its consumers switched over rather than as a standalone duplicate. — DEFERRED BY DESIGN: this card moves the engine only (per its scope); consumer migration + duplicate-copy deletion is owned by card 01KTCBFKT461CQKT1FQ13E52D6 (kanban-cli) and 01KTCBG5GP4FS50ZFPKSSN2H6Q (sah dynamic_cli + extraction copies). Not in scope for card B.
- [x] `crates/swissarmyhammer-operations/src/cli_gen.rs:410` — The matches->args extraction set (extract_noun_verb_arguments, build_arguments_from_matches, extract_value_from_matches) reimplements logic that already exists in FOUR places, compounding an existing duplication problem instead of consolidating it. Make this the one implementation and remove cli_executor.rs's copy, main.rs's convert_operation_matches_to_arguments/ValueExtractor, and schema_conversion.rs's matches_to_json_args, routing them through this module. Otherwise a bug fixed here must be fixed in four other spots. — DEFERRED BY DESIGN: this card moves the engine only (per its scope); routing the four consumer copies (cli_executor.rs, main.rs convert_operation_matches_to_arguments/ValueExtractor, schema_conversion.rs matches_to_json_args, kanban-cli) through this module is owned by card 01KTCBFKT461CQKT1FQ13E52D6 (kanban-cli) and 01KTCBG5GP4FS50ZFPKSSN2H6Q (sah dynamic_cli + extraction copies). Not in scope for card B.

### Warnings
- [x] `crates/swissarmyhammer-operations/src/cli_gen.rs:229` — ArgMeta/ArgMetaType in this module duplicate the same pair in apps/kanban-cli/src/cli_gen.rs and overlap dynamic_cli.rs's ArgData/ArgType — same fields, same intent, parallel definitions that must be edited in lockstep. Fold into the single shared module from finding 1; these types should be defined once here and imported, not redeclared per crate. — DEFERRED BY DESIGN: collapsing the per-crate ArgMeta/ArgMetaType + ArgData/ArgType copies into this one shared definition happens as part of the consumer migration owned by card 01KTCBFKT461CQKT1FQ13E52D6 (kanban-cli) and 01KTCBG5GP4FS50ZFPKSSN2H6Q (sah dynamic_cli). Not in scope for card B, which moves the engine only.
- [x] `crates/swissarmyhammer-operations/src/cli_gen.rs:267` — `precompute_args` runs ~58 lines of actual code (cli_gen.rs:267-329), past the ~50-line limit. It bundles three concerns — parsing the `required` set, resolving the clap arg type via a nested boolean/null + primary_type match, and constructing every ArgMeta field — so it is hard to read and the type-resolution logic cannot be unit-tested on its own. Extract the arg-type resolution into `fn resolve_arg_type(prop_schema: &Value) -> ArgMetaType` (lines 292-304) and the required-set parse into a small helper, then have the `.map()` closure call them while assembling ArgMeta. That drops the function body well under 50 lines and makes the type mapping directly testable.
- [x] `crates/swissarmyhammer-operations/src/cli_gen.rs:413` — The public library API `extract_noun_verb_arguments` returns `Result<_, String>`. This crate is a reusable library (the module doc says every CLI on the `Operation` trait shares this engine), and a stringly-typed error is strictly worse than the `anyhow::Error`/`Box<dyn Error>` the rule already forbids from public APIs: callers cannot match on the failure (no-noun vs. no-verb), there is no `Error::source()` chain, and the error doesn't implement `std::error::Error` so it won't compose with `?` in caller error enums. Define a `thiserror` enum, e.g. `#[derive(Debug, thiserror::Error)] pub enum CliExtractError { #[error(\"no noun specified\")] MissingNoun, #[error(\"no verb specified for '{0}'\")] MissingVerb(String) }`, and return `Result<_, CliExtractError>`. Keep Display messages lowercase with no trailing punctuation per the rule. Propagate the same type from `build_arguments_from_matches`.
- [x] `crates/swissarmyhammer-operations/src/cli_gen.rs:508` — The test module's mock Operation fixtures (MockInitBoard, MockGetBoard, MockUpdateBoard, MockAddTask, MockAddColumn, MockAddTag with their verb/noun/description/parameters impls) re-create Operation test doubles that already exist in this same crate, and the test bodies themselves are copies of kanban-cli's cli_gen tests. Reuse the existing Mock Operation fixtures from schema.rs/operation.rs tests (or hoist them into a shared test-support module) instead of declaring a fresh parallel set; this keeps one mock op contract that all schema/cli_gen tests share.

## Review Findings (2026-06-07 06:17)

### Blockers
- [ ] `crates/swissarmyhammer-operations/src/cli_gen.rs:1` — This entire new module is a near-verbatim copy of the existing apps/kanban-cli/src/cli_gen.rs. The two are now divergent copies of one schema→clap engine: a fix to clap-arg construction, per-op scoping, or value extraction in one will silently not reach the other, which is exactly the latent-bug failure mode duplication creates. The new file's own doc comment states the engine is tool-agnostic and 'every CLI built on the Operation trait can share it' — but the original copy was neither deleted nor switched to consume this one, so this adds a third copy rather than consolidating. Keep this shared crate as the single home for the engine and make apps/kanban-cli/src/cli_gen.rs re-export from `swissarmyhammer_operations::cli_gen` (delete its duplicated bodies). Do the same for the other CLIs below in the same change so this consolidation actually removes copies instead of adding one. [TRIAGE: known deferred-by-design repeat — consumer migration + duplicate-copy deletion owned by cards 01KTCBFKT461CQKT1FQ13E52D6 (kanban-cli) and 01KTCBG5GP4FS50ZFPKSSN2H6Q (sah). Not in scope for card B, which moves the engine only.]
- [ ] `crates/swissarmyhammer-operations/src/cli_gen.rs:27` — The string-interning / schema-type helpers (`intern`, `schema_has_type`, `primary_type`) and the matches→JSON extraction logic are also duplicated in apps/swissarmyhammer-cli (dynamic_cli.rs + cli_executor.rs), so with this addition the same code now exists in three places. That defeats the rule-of-three carve-out — these are concrete, already-duplicated functions, not a speculative abstraction, and they are the strongest argument for routing every CLI through this one shared module. Have apps/swissarmyhammer-cli's dynamic_cli.rs and cli_executor.rs call into `swissarmyhammer_operations::cli_gen` for interning, type probing, and matches→arguments extraction, removing their copies in the same change. [TRIAGE: known deferred-by-design repeat — consumer migration owned by cards 01KTCBFKT461CQKT1FQ13E52D6 and 01KTCBG5GP4FS50ZFPKSSN2H6Q. Not in scope for card B.]
- [x] `crates/swissarmyhammer-operations/src/cli_gen.rs:505` — Nullable-boolean arguments (schema type `[\"boolean\", \"null\"]`) are built as value-taking args but extracted with `get_flag`, which panics. `build_clap_arg` constructs `ArgMetaType::NullableBoolean` as a value argument (`PossibleValuesParser::new([\"true\",\"false\"])` + `value_name(\"BOOL\")`, lines 367-369) with NO `ArgAction::SetTrue`. But `extract_value_from_matches` dispatches on `primary_type`, which returns `Some(\"boolean\")` for a `[\"boolean\",\"null\"]` union (first non-null wins, line 63-68), so it calls `matches.get_flag(name)`. clap's `get_flag` panics when the argument's action is not `SetTrue`/`SetFalse`. So any tool whose schema carries a nullable boolean (a shape this engine explicitly claims to support — `resolve_arg_type` and `build_clap_arg` both have dedicated `NullableBoolean` arms) panics at extraction time on valid input. In `extract_value_from_matches`, detect the nullable-boolean shape before the boolean branch — e.g. `if schema_has_type(schema, \"boolean\") && schema_has_type(schema, \"null\") { return matches.get_one::<String>(name).and_then(|v| v.parse::<bool>().ok()).map(Value::Bool); }` — so it reads the parsed string value instead of `get_flag`. Add a round-trip test for a `[\"boolean\",\"null\"]` arg. [NEW: in-scope engine correctness bug — not a duplication/deferred item.]

### Warnings
- [ ] `crates/swissarmyhammer-operations/src/cli_gen.rs:92` — The entire cli_gen module is a wholesale duplicate of apps/kanban-cli/src/cli_gen.rs and substantially overlaps apps/swissarmyhammer-cli/src/dynamic_cli.rs. Hoisting it into the shared swissarmyhammer-operations crate is the correct canonicalization, but it only eliminates duplication if the app copies are deleted and re-pointed at this one. This diff adds the shared copy and leaves both app modules in place, so the working tree now holds 2–3 parallel implementations that will diverge. Complete the consolidation in the same change: delete apps/kanban-cli/src/cli_gen.rs and the equivalent builders in apps/swissarmyhammer-cli/src/dynamic_cli.rs (and main.rs/cli_executor.rs), and have those apps depend on swissarmyhammer-operations::cli_gen. Leaving the old copies live is the duplicate-but-different state the reuse mandate exists to prevent. [TRIAGE: known deferred-by-design repeat — consumer migration owned by cards 01KTCBFKT461CQKT1FQ13E52D6 and 01KTCBG5GP4FS50ZFPKSSN2H6Q. Not in scope for card B.]
- [x] `crates/swissarmyhammer-operations/src/cli_gen.rs:215` — The synthetic op-field key \"op\" is hardcoded as a bare string literal at 5 separate sites; if the field name ever changes, every site must be found and updated in lockstep, and a missed one fails silently. Define `const OP_FIELD: &str = \"op\";` once and reference it at all five sites. [NEW: in-scope engine clarity finding.]

### Nits
- [x] `crates/swissarmyhammer-operations/src/cli_gen.rs:511` — A float argument clap accepted can be silently dropped from the output. `serde_json::Number::from_f64` returns `None` for non-finite values, and `clap::value_parser!(f64)` accepts `inf`/`nan` (Rust's `f64::from_str` parses them). So `--x inf` parses successfully at the clap layer but vanishes from the emitted arguments map with no error, making the CLI silently ignore an input it appeared to accept. Reject non-finite floats at parse time with a custom `value_parser` (or map the `None` to an explicit error) so an accepted flag is never silently discarded. [NEW: in-scope engine correctness nit.]

## Review Findings (2026-06-07 06:33)

### Blockers
- [ ] `crates/swissarmyhammer-operations/src/cli_gen.rs:98` — The entire schema-to-clap engine added here is a verbatim copy of apps/kanban-cli/src/cli_gen.rs, which still exists unchanged. Two byte-identical copies of build_commands_from_schema/build_noun_command/build_verb_command/build_clap_arg/extract_*/intern/schema_has_type/primary_type will now drift independently — a fix to one (e.g. the non-finite-float guard, per-op arg scoping) silently leaves the other wrong. Delete apps/kanban-cli/src/cli_gen.rs and have kanban-cli depend on swissarmyhammer_operations::cli_gen (the stated goal in this file's module doc). Keep exactly one implementation; do not land a second copy. [TRIAGE: known deferred-by-design repeat — consumer migration + duplicate-copy deletion owned by cards 01KTCBFKT461CQKT1FQ13E52D6 (kanban-cli) and 01KTCBG5GP4FS50ZFPKSSN2H6Q (sah). Not in scope for card B, which moves the engine only.]
- [ ] `crates/swissarmyhammer-operations/src/cli_gen.rs:98` — The same engine is a near-verbatim copy of the schema-to-clap code in apps/swissarmyhammer-cli (dynamic_cli.rs + cli_executor.rs + main.rs), which is also left unchanged. This is the third copy of one algorithm (intern/has_type/primary_type, noun/verb command building, ArgMeta->Arg, matches->JSON), so the consolidation this crate exists to provide has not actually happened. Migrate apps/swissarmyhammer-cli to consume swissarmyhammer_operations::cli_gen and delete the dynamic_cli/cli_executor/main schema->clap duplicates, so a single shared engine remains. [TRIAGE: known deferred-by-design repeat — consumer migration owned by cards 01KTCBFKT461CQKT1FQ13E52D6 and 01KTCBG5GP4FS50ZFPKSSN2H6Q. Not in scope for card B.]

### Warnings
- [ ] `crates/swissarmyhammer-operations/src/cli_gen.rs:99` — This entire CLI-generation engine is a verbatim copy of an existing implementation rather than the existing copies being redirected to it. Landing it as-is yields three live copies of the same logic (the canonical fix gets applied once, but here there'd be three places to fix). The new module is also dead code until something calls it. In this same change, make `operations::cli_gen` the one canonical engine and delete `apps/kanban-cli/src/cli_gen.rs`, switching kanban-cli to call the shared module; then fold `apps/swissarmyhammer-cli/src/dynamic_cli.rs` (and `main.rs::has_type`) onto the same shared functions. If the migration must be staged into a follow-up, say so in the PR and track it — but don't leave the canonical copy unreferenced. [TRIAGE: known deferred-by-design repeat — consumer migration owned by cards 01KTCBFKT461CQKT1FQ13E52D6 and 01KTCBG5GP4FS50ZFPKSSN2H6Q. Not in scope for card B.]

### Nits
- [x] `crates/swissarmyhammer-operations/src/cli_gen.rs:95` — The JSON Schema type-name tokens are repeated as bare string literals across multiple functions, so the schema vocabulary lives in no single place and a typo or vocabulary change must be hunted down across the file. \"boolean\" appears 4x (resolve_arg_type's nullable check + primary_type arm + extract_value_from_matches' nullable check + its number/boolean arm), \"null\" 3x (resolve_arg_type, primary_type's `*s != \"null\"`, extract_value_from_matches), and \"array\" 3x (resolve_arg_type, precompute_args' is_required guard, extract_value_from_matches). Name the closed type vocabulary once, e.g. `const TYPE_BOOLEAN: &str = \"boolean\"; const TYPE_NULL: &str = \"null\"; const TYPE_ARRAY: &str = \"array\"; const TYPE_INTEGER: &str = \"integer\"; const TYPE_NUMBER: &str = \"number\";` and reference those constants in `schema_has_type`/`primary_type` calls and the match arms so the JSON Schema tokens change in one place. [NEW: in-scope engine clarity nit — intra-module string-literal duplication, same canonicalization pattern as the already-fixed OP_FIELD constant. Not a cross-crate/deferred item.]

## Review Findings (2026-06-07 06:48)

### Warnings
- [x] `crates/swissarmyhammer-operations/src/cli_gen.rs:50` — build_commands_from_schema conflates four distinct phases (parse x-operation-schemas into op_args, compute fallback_args, parse the op enum, group verbs by noun) in one body, and two of those phases nest three levels deep (`if let Some(op_schemas)` → `for op_schema` → `if let Some(title)`, and `for op_val` → `if let Some(op_str)` → `if parts.len() == 2`). The stacked nesting plus the early-return `match` on op_values make the control flow hard to hold in one's head and easy to break when one phase changes. Extract the two parsing phases into helpers, e.g. `fn build_op_args(schema: &Value) -> HashMap<String, (String, Vec<ArgMeta>)>` and `fn group_verbs_by_noun(op_values: &[Value]) -> HashMap<String, Vec<(String, String)>>`. The top-level function then reads as: op_args = build_op_args(schema); fallback = ...; groups = group_verbs_by_noun(op_values); map+sort. Each helper's nesting drops to ≤2 and is independently testable. [NEW: in-scope engine clarity finding — internal function decomposition within cli_gen.rs. Not a cross-crate/deferred item.]
- [ ] `crates/swissarmyhammer-operations/src/cli_gen.rs:76` — The schema-introspection helpers (schema_has_type, primary_type, required_param_names, resolve_arg_type) and the string interner (intern + STRING_CACHE) re-implement helpers that already exist in apps/swissarmyhammer-cli/src/dynamic_cli.rs (SchemaParser, intern_string) and apps/kanban-cli/src/cli_gen.rs. These are the canonical-utility primitives most worth de-duplicating, since fixes to schema-type detection or interning otherwise have to be made in three places. Fold these into the shared cli_gen module as the single source (as done here) and delete the dynamic_cli.rs / kanban-cli copies, having those call sites import from swissarmyhammer-operations rather than keeping their own. [TRIAGE: known deferred-by-design repeat — consumer migration + duplicate-copy deletion owned by cards 01KTCBFKT461CQKT1FQ13E52D6 (kanban-cli) and 01KTCBG5GP4FS50ZFPKSSN2H6Q (sah dynamic_cli). Not in scope for card B, which moves the engine only.]
- [ ] `crates/swissarmyhammer-operations/src/cli_gen.rs:118` — The entire command-build and match-extraction pipeline (build_commands_from_schema, build_noun_command, build_verb_command, build_clap_arg, extract_noun_verb_arguments, build_arguments_from_matches, extract_value_from_matches) is a verbatim copy of the same functions already living in apps/kanban-cli/src/cli_gen.rs, and a near-copy of dynamic_cli.rs's CliBuilder. Creating this shared engine is the right move, but unless the two existing app copies are deleted and pointed at it, this is a third parallel implementation that will diverge — the failure mode this validator exists to catch. Finish the consolidation in this PR: have kanban-cli and swissarmyhammer-cli depend on swissarmyhammer-operations::cli_gen, delete apps/kanban-cli/src/cli_gen.rs and the duplicated CliBuilder/SchemaParser paths in dynamic_cli.rs, and keep exactly one canonical engine. If the migration must land separately, note it in the PR and track it so the third copy is short-lived. [TRIAGE: known deferred-by-design repeat — consumer migration owned by cards 01KTCBFKT461CQKT1FQ13E52D6 and 01KTCBG5GP4FS50ZFPKSSN2H6Q. Not in scope for card B.]
- [x] `crates/swissarmyhammer-operations/src/cli_gen.rs:435` — The public error enum `CliExtractError` is not `#[non_exhaustive]`. Because it is exported (`pub mod cli_gen` in lib.rs → `swissarmyhammer_operations::cli_gen::CliExtractError`), any downstream crate can `match` it exhaustively without a wildcard arm. Adding a future variant — likely, since this engine is meant to be shared by kanban-cli, sah, code-context-cli, shelltool-cli — is then a breaking change that fails to compile for those callers. Add `#[non_exhaustive]` to the enum so new variants can be added without a major version bump and downstream exhaustive matches are forced to keep a wildcard arm. [NEW: in-scope engine public-API finding — forward-compatibility of the engine's own exported error type. Not a cross-crate/deferred item.]

### Nits
- [ ] `crates/swissarmyhammer-operations/src/cli_gen.rs:157` — `(verb, op_string)` is modeled as a bare `(String, String)` threaded through `noun_groups`, `build_noun_command`, and `build_verb_command`. Two same-typed strings with distinct meanings are exactly the swap-hazard the newtype guidance targets — the code even needs the comment `// noun -> [(verb, op_string)]` to disambiguate, which is the smell. Introduce a small private struct (e.g. `struct VerbOp { verb: String, op: String }`) so the field names carry the meaning and the two strings cannot be transposed at a call site. [NEW: in-scope engine clarity nit — internal newtype suggestion within cli_gen.rs.] [non-blocking cosmetic]

## Review Findings (2026-06-07 07:01)

### Blockers
- [ ] `crates/swissarmyhammer-operations/src/cli_gen.rs:119` — This entire schema->clap engine is a verbatim copy of the existing apps/kanban-cli/src/cli_gen.rs, and near-verbatim of apps/swissarmyhammer-cli/src/dynamic_cli.rs. The module doc says it exists to be the single shared engine, but neither consumer was migrated to it (nothing imports swissarmyhammer_operations::cli_gen; both apps still carry their own copies). The change therefore adds a THIRD copy rather than consolidating — the exact drift-out-of-sync hazard duplication causes: a fix to one copy silently misses the other two. Make this module the sole implementation and delete the copies: migrate apps/kanban-cli to call swissarmyhammer_operations::cli_gen::{build_commands_from_schema, extract_noun_verb_arguments} and remove apps/kanban-cli/src/cli_gen.rs; fold the dynamic_cli.rs schema->clap logic (intern_string, schema_has_type, ArgBuilder, build_clap_arg, the matches->args extraction) onto this shared engine too. Land the shared crate and the consumer migration together so no duplicate copy is ever checked in. [TRIAGE: known deferred-by-design repeat — consumer migration + duplicate-copy deletion owned by cards 01KTCBFKT461CQKT1FQ13E52D6 (kanban-cli) and 01KTCBG5GP4FS50ZFPKSSN2H6Q (sah dynamic_cli + extraction copies). Not in scope for card B, which moves the engine only.]

### Nits
- [ ] `crates/swissarmyhammer-operations/src/cli_gen.rs:54` — `intern` plus its `STRING_CACHE` static reimplement an existing string-interning utility instead of reusing it. This is the same leak-and-cache helper that already exists as `intern_string` in `apps/swissarmyhammer-cli/src/dynamic_cli.rs`; when the shared crate becomes the canonical home, that copy (and any others) should call this one rather than carry a parallel cache. Make this the single interner and delete `dynamic_cli::intern_string` (and the kanban-cli copy), pointing both at `swissarmyhammer_operations::cli_gen::intern`. Folded into the module-consolidation fix above; flagged separately only because the interner is a distinct shared util living outside cli_gen. [TRIAGE: known deferred-by-design repeat — cross-crate interner consolidation owned by cards 01KTCBFKT461CQKT1FQ13E52D6 (kanban-cli) and 01KTCBG5GP4FS50ZFPKSSN2H6Q (sah dynamic_cli). Not in scope for card B, which moves the engine only.]
- [ ] `crates/swissarmyhammer-operations/src/cli_gen.rs:411` — In build_clap_arg, the `if !meta.is_required { a = a.value_name(X) }` block is repeated across four match arms (Integer 418-420, Float 425-427, Array 432-434, String 438-439), differing only in the value-name constant (\"NUMBER\", \"NUMBER\", \"VALUE\", \"TEXT\"). The required-guard logic is identical and only the constant varies — that constant-only part is a table, not four parallel arms a human must keep in lockstep when a new value-taking ArgMetaType is added. Split the constant-only variation from the genuinely-behavioral parser/action. Compute the value-name once from a small map and apply the guard uniformly, e.g. `let value_name = match meta.arg_type { NullableBoolean => Some(\"BOOL\"), Integer | Float => Some(\"NUMBER\"), Array => Some(\"VALUE\"), String => Some(\"TEXT\"), Boolean => None };` then `if !meta.is_required { if let Some(vn) = value_name { arg = arg.value_name(vn); } }`, leaving each match arm to set only its distinct parser/action. [non-blocking cosmetic]
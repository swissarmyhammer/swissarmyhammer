{"id":"01KRGZY33PMV45MJ1B2VPXNRKH","timestamp":"2026-05-13T15:40:23.542998Z","op":"create","item_id":"01KRGZY33P99J7CGG0XRQGZ352","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,65 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: aa80\n+title: Filter tab button no longer focuses the formula bar in the live app\n+---\n+## What\n+\n+The `command-driven-ui` migration of the Filter tab affordance (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for the new no-arg `perspective.filter.focus` command. The unit tests in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` pass — but **the actual end-user behaviour is broken**: clicking the Filter icon on a perspective tab no longer focuses the formula bar's CM6 editor.\n+\n+Hooking the button through the command pipeline is **mandatory** for parity with palette / keybindings / multi-window dispatch — DO NOT revert to the local-callback path. Instead, debug the live wire and make it work end-to-end.\n+\n+### Current wiring (paper-correct, broken in practice)\n+\n+1. Click `<CommandButton>` in `kanban-app/ui/src/components/command-button.tsx` (the `perspective.filter.focus` instance rendered by `RegistryTabButtons` in `perspective-tab-bar.tsx:387`).\n+2. `handlePress` → `dispatch(\"perspective.filter.focus\", { args: {} })` via `useDispatchCommand` (no popover; every param resolves from scope).\n+3. Tauri `dispatch_command` runs `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`), returning `{\"FocusFilter\": {\"perspective_id\": \"<id>\"}}`.\n+4. Dispatcher's `apply_post_command_side_effects` → `handle_ui_trigger_results` → `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) emits the Tauri event `ui.focus.filter` carrying `{ perspective_id }`.\n+5. `<FilterEditorBody>` in `kanban-app/ui/src/components/filter-editor.tsx:419-450` listens for `ui.focus.filter`, compares `payload.perspective_id` against its own `perspectiveId` prop, and calls `innerRef.current?.focus()` (the CM6 contenteditable).\n+\n+The migration test (#5 in `perspective-tab-bar.filter-migration.test.tsx`) simulates step 5 directly by invoking a mocked `listen` callback — it does NOT exercise steps 2–4. So the test stays green even though something between the click and the eventual focus is silently dropping the signal.\n+\n+### Investigation plan — instrument first, fix second\n+\n+Do not theorise. Add tracing at each hop and capture which one is silent:\n+\n+- [ ] **Frontend, in `CommandButton.handlePress`** (`kanban-app/ui/src/components/command-button.tsx:132`): `console.debug(\"[CommandButton] press\", command.id, surfaceId)` so we know the click actually reached the dispatcher.\n+- [ ] **Frontend, in `useDispatchCommand`'s dispatch site**: log the resolved `scope_chain` and final `args` sent to `invoke(\"dispatch_command\", …)`. Confirm `perspective_id` is reachable in scope when the user clicks (the `CommandScopeProvider moniker=\"perspective:p1\"` is established by `ScopedPerspectiveTab` at `perspective-tab-bar.tsx:648`).\n+- [ ] **Backend, in `FocusFilterCmd::execute`** (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`): `tracing::info!` the resolved `perspective_id` and the returned envelope. Verify `resolve_and_persist_perspective_id` returns Some, not an error.\n+- [ ] **Backend, in `handle_focus_filter`** (`kanban-app/src/commands.rs:1717`): log when the branch fires and the emitted payload. Verify the `result.get(\"FocusFilter\")` match succeeds against the JSON the command actually returned.\n+- [ ] **Frontend, in `<FilterEditorBody>`'s `listen` callback** (`filter-editor.tsx:422`): log every received `ui.focus.filter` event AND the current `perspectiveId` it's being compared against. The most likely silent failure is an id mismatch — e.g. the active perspective changed between click and event delivery, or the resolver picked a different id than the React subscriber holds.\n+- [ ] **Frontend, in `<FilterEditorBody>`'s focus path**: if the id matches but `innerRef.current?.focus()` is no-op, log `innerRef.current` and the underlying CM6 `EditorView`. Note that the legacy path was synchronous (in the user-gesture stack); the new path is fully async after a Tauri round-trip. Some browsers refuse `.focus()` from non-gesture contexts — verify whether CM6's `view.focus()` works here.\n+\n+### Likely root-cause candidates (in order)\n+\n+1. **Lost user-gesture window.** The legacy `<FilterFocusButton>` focused CM6 synchronously inside the click handler. The new path goes click → Tauri IPC → Rust → Tauri event → React listener → `.focus()`. By the time `.focus()` is called, the gesture is gone. If WebKit refuses, the focus is silently dropped. Mitigation: focus the editor optimistically inside `CommandButton.handlePress` for `perspective.filter.focus` specifically (a UI hint, not a state mutation), keep the broadcast for cross-window parity. Confirm with the instrumentation first — don't apply this fix blindly.\n+2. **`perspective_id` resolves to a different id than the editor's prop.** `resolve_and_persist_perspective_id` falls back through arg → scope-chain → UIState.active → first-perspective-for-view-kind. If the click site's scope_chain doesn't yield `perspective:<id>`, the resolver may pick a different perspective than the one whose editor the user expects. The editor's id-equality guard would then drop the event.\n+3. **The `FocusFilter` envelope is mangled by the dispatcher.** Check that `result.get(\"FocusFilter\")` is `Some(...)` at the point of `app.emit(\"ui.focus.filter\", focus_filter)` — if the command result is wrapped (e.g. `{ result: { FocusFilter: ... } }`) by another layer added in this refactor, the match fails and the emit never happens.\n+4. **Listener race on perspective switch.** `<FilterFormulaBar>` remounts with `key={perspectiveId}` on perspective change. If the user clicks before the new editor's `listen` promise resolves, the broadcast lands without a listener. Verify the `listen` subscription is in place by the time the user clicks.\n+\n+### Acceptance Criteria\n+\n+- [ ] Clicking the Filter tab button focuses the formula bar CM6 editor in the live app. Verified by a new automated end-to-end test (see Tests) — NOT by manual click-through.\n+- [ ] The fix preserves the command-driven dispatch path: palette invocation of `perspective.filter.focus` AND keybinding invocation (when added) must also focus the editor. Do not regress those paths by short-circuiting in the click handler only.\n+- [ ] The new end-to-end test asserts the **DOM activeElement** ends up on the `.cm-content` of the formula bar after dispatching `perspective.filter.focus` through the production command pipeline — not by manually invoking a mocked `listen` callback.\n+- [ ] Implementation notes capture which stage was silently dropping the signal (instrumentation finding) and why the chosen fix addresses that specific stage. Don't ship a fix that \"seems to work\" without identifying the broken hop.\n+- [ ] All existing tests in `perspective-tab-bar.filter-migration.test.tsx`, `perspective-tab-bar.registry-driven.test.tsx`, and `perspective-tab-bar.test.tsx` still pass.\n+\n+### Tests\n+\n+- [ ] **New end-to-end test** in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` (new `describe` block: `\"filter button — live dispatch round-trip\"`): render `<PerspectiveTabBar>` with a real `<FilterFormulaBar>` mounted (NOT just a standalone `<FilterEditor>`). Mock `invoke(\"dispatch_command\", …)` so it actually drives `handle_focus_filter`'s behaviour — i.e. when the mock sees `perspective.filter.focus`, it synchronously fires the `ui.focus.filter` listeners with `{ perspective_id: \"<host id>\" }` (same payload shape the Rust dispatcher produces). Click the Filter `<CommandButton>` via `fireEvent.click`. Assert `document.activeElement` is the formula bar's `.cm-content` after the click resolves. This is the test that would have caught the current regression.\n+- [ ] **Sibling-perspective ignore test**: same harness, but the mocked dispatcher fires with a non-matching `perspective_id`. Assert `document.activeElement` is NOT the `.cm-content` — i.e. the id-equality guard still blocks cross-perspective broadcasts.\n+- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar.filter-migration` — green.\n+- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — all sibling files green.\n+- [ ] **Run** `cargo test -p swissarmyhammer-kanban focus_filter` — existing `focus_filter_command_*` tests in `perspective_commands.rs` still green.\n+- [ ] **Run** `cargo test -p kanban-app` — Tauri-side handler tests (if any cover `handle_focus_filter`) green; if no coverage exists for `handle_focus_filter`, add a minimal unit test that constructs the `{\"FocusFilter\":{...}}` envelope and verifies the handler attempts to emit on `ui.focus.filter`.\n+\n+### Workflow\n+\n+- Use `/tdd` — write the failing end-to-end test FIRST (it should fail in the current codebase by failing to move `activeElement` to `.cm-content`), then instrument, then fix.\n+- **Do not revert to the local-callback path.** Hooking the Filter button through the command pipeline is mandatory.\n+- **Stay on task.** Do not refactor `<CommandButton>`, the dispatcher, or the spatial-nav wiring beyond what's needed to fix this specific regression.\n+\n+#bug #command-driven-ui #frontend #kanban-app #perspectives\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,65 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: aa80\n-title: Filter tab button no longer focuses the formula bar in the live app\n----\n-## What\n-\n-The `command-driven-ui` migration of the Filter tab affordance (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for the new no-arg `perspective.filter.focus` command. The unit tests in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` pass — but **the actual end-user behaviour is broken**: clicking the Filter icon on a perspective tab no longer focuses the formula bar's CM6 editor.\n-\n-Hooking the button through the command pipeline is **mandatory** for parity with palette / keybindings / multi-window dispatch — DO NOT revert to the local-callback path. Instead, debug the live wire and make it work end-to-end.\n-\n-### Current wiring (paper-correct, broken in practice)\n-\n-1. Click `<CommandButton>` in `kanban-app/ui/src/components/command-button.tsx` (the `perspective.filter.focus` instance rendered by `RegistryTabButtons` in `perspective-tab-bar.tsx:387`).\n-2. `handlePress` → `dispatch(\"perspective.filter.focus\", { args: {} })` via `useDispatchCommand` (no popover; every param resolves from scope).\n-3. Tauri `dispatch_command` runs `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`), returning `{\"FocusFilter\": {\"perspective_id\": \"<id>\"}}`.\n-4. Dispatcher's `apply_post_command_side_effects` → `handle_ui_trigger_results` → `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) emits the Tauri event `ui.focus.filter` carrying `{ perspective_id }`.\n-5. `<FilterEditorBody>` in `kanban-app/ui/src/components/filter-editor.tsx:419-450` listens for `ui.focus.filter`, compares `payload.perspective_id` against its own `perspectiveId` prop, and calls `innerRef.current?.focus()` (the CM6 contenteditable).\n-\n-The migration test (#5 in `perspective-tab-bar.filter-migration.test.tsx`) simulates step 5 directly by invoking a mocked `listen` callback — it does NOT exercise steps 2–4. So the test stays green even though something between the click and the eventual focus is silently dropping the signal.\n-\n-### Investigation plan — instrument first, fix second\n-\n-Do not theorise. Add tracing at each hop and capture which one is silent:\n-\n-- [ ] **Frontend, in `CommandButton.handlePress`** (`kanban-app/ui/src/components/command-button.tsx:132`): `console.debug(\"[CommandButton] press\", command.id, surfaceId)` so we know the click actually reached the dispatcher.\n-- [ ] **Frontend, in `useDispatchCommand`'s dispatch site**: log the resolved `scope_chain` and final `args` sent to `invoke(\"dispatch_command\", …)`. Confirm `perspective_id` is reachable in scope when the user clicks (the `CommandScopeProvider moniker=\"perspective:p1\"` is established by `ScopedPerspectiveTab` at `perspective-tab-bar.tsx:648`).\n-- [ ] **Backend, in `FocusFilterCmd::execute`** (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`): `tracing::info!` the resolved `perspective_id` and the returned envelope. Verify `resolve_and_persist_perspective_id` returns Some, not an error.\n-- [ ] **Backend, in `handle_focus_filter`** (`kanban-app/src/commands.rs:1717`): log when the branch fires and the emitted payload. Verify the `result.get(\"FocusFilter\")` match succeeds against the JSON the command actually returned.\n-- [ ] **Frontend, in `<FilterEditorBody>`'s `listen` callback** (`filter-editor.tsx:422`): log every received `ui.focus.filter` event AND the current `perspectiveId` it's being compared against. The most likely silent failure is an id mismatch — e.g. the active perspective changed between click and event delivery, or the resolver picked a different id than the React subscriber holds.\n-- [ ] **Frontend, in `<FilterEditorBody>`'s focus path**: if the id matches but `innerRef.current?.focus()` is no-op, log `innerRef.current` and the underlying CM6 `EditorView`. Note that the legacy path was synchronous (in the user-gesture stack); the new path is fully async after a Tauri round-trip. Some browsers refuse `.focus()` from non-gesture contexts — verify whether CM6's `view.focus()` works here.\n-\n-### Likely root-cause candidates (in order)\n-\n-1. **Lost user-gesture window.** The legacy `<FilterFocusButton>` focused CM6 synchronously inside the click handler. The new path goes click → Tauri IPC → Rust → Tauri event → React listener → `.focus()`. By the time `.focus()` is called, the gesture is gone. If WebKit refuses, the focus is silently dropped. Mitigation: focus the editor optimistically inside `CommandButton.handlePress` for `perspective.filter.focus` specifically (a UI hint, not a state mutation), keep the broadcast for cross-window parity. Confirm with the instrumentation first — don't apply this fix blindly.\n-2. **`perspective_id` resolves to a different id than the editor's prop.** `resolve_and_persist_perspective_id` falls back through arg → scope-chain → UIState.active → first-perspective-for-view-kind. If the click site's scope_chain doesn't yield `perspective:<id>`, the resolver may pick a different perspective than the one whose editor the user expects. The editor's id-equality guard would then drop the event.\n-3. **The `FocusFilter` envelope is mangled by the dispatcher.** Check that `result.get(\"FocusFilter\")` is `Some(...)` at the point of `app.emit(\"ui.focus.filter\", focus_filter)` — if the command result is wrapped (e.g. `{ result: { FocusFilter: ... } }`) by another layer added in this refactor, the match fails and the emit never happens.\n-4. **Listener race on perspective switch.** `<FilterFormulaBar>` remounts with `key={perspectiveId}` on perspective change. If the user clicks before the new editor's `listen` promise resolves, the broadcast lands without a listener. Verify the `listen` subscription is in place by the time the user clicks.\n-\n-### Acceptance Criteria\n-\n-- [ ] Clicking the Filter tab button focuses the formula bar CM6 editor in the live app. Verified by a new automated end-to-end test (see Tests) — NOT by manual click-through.\n-- [ ] The fix preserves the command-driven dispatch path: palette invocation of `perspective.filter.focus` AND keybinding invocation (when added) must also focus the editor. Do not regress those paths by short-circuiting in the click handler only.\n-- [ ] The new end-to-end test asserts the **DOM activeElement** ends up on the `.cm-content` of the formula bar after dispatching `perspective.filter.focus` through the production command pipeline — not by manually invoking a mocked `listen` callback.\n-- [ ] Implementation notes capture which stage was silently dropping the signal (instrumentation finding) and why the chosen fix addresses that specific stage. Don't ship a fix that \"seems to work\" without identifying the broken hop.\n-- [ ] All existing tests in `perspective-tab-bar.filter-migration.test.tsx`, `perspective-tab-bar.registry-driven.test.tsx`, and `perspective-tab-bar.test.tsx` still pass.\n-\n-### Tests\n-\n-- [ ] **New end-to-end test** in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` (new `describe` block: `\"filter button — live dispatch round-trip\"`): render `<PerspectiveTabBar>` with a real `<FilterFormulaBar>` mounted (NOT just a standalone `<FilterEditor>`). Mock `invoke(\"dispatch_command\", …)` so it actually drives `handle_focus_filter`'s behaviour — i.e. when the mock sees `perspective.filter.focus`, it synchronously fires the `ui.focus.filter` listeners with `{ perspective_id: \"<host id>\" }` (same payload shape the Rust dispatcher produces). Click the Filter `<CommandButton>` via `fireEvent.click`. Assert `document.activeElement` is the formula bar's `.cm-content` after the click resolves. This is the test that would have caught the current regression.\n-- [ ] **Sibling-perspective ignore test**: same harness, but the mocked dispatcher fires with a non-matching `perspective_id`. Assert `document.activeElement` is NOT the `.cm-content` — i.e. the id-equality guard still blocks cross-perspective broadcasts.\n-- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar.filter-migration` — green.\n-- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — all sibling files green.\n-- [ ] **Run** `cargo test -p swissarmyhammer-kanban focus_filter` — existing `focus_filter_command_*` tests in `perspective_commands.rs` still green.\n-- [ ] **Run** `cargo test -p kanban-app` — Tauri-side handler tests (if any cover `handle_focus_filter`) green; if no coverage exists for `handle_focus_filter`, add a minimal unit test that constructs the `{\"FocusFilter\":{...}}` envelope and verifies the handler attempts to emit on `ui.focus.filter`.\n-\n-### Workflow\n-\n-- Use `/tdd` — write the failing end-to-end test FIRST (it should fail in the current codebase by failing to move `activeElement` to `.cm-content`), then instrument, then fix.\n-- **Do not revert to the local-callback path.** Hooking the Filter button through the command pipeline is mandatory.\n-- **Stay on task.** Do not refactor `<CommandButton>`, the dispatcher, or the spatial-nav wiring beyond what's needed to fix this specific regression.\n-\n-#bug #command-driven-ui #frontend #kanban-app #perspectives\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRGZY33Q82G6BM43F36R02FY","timestamp":"2026-05-13T15:40:23.543720Z","entity_type":"task","entity_id":"01KRGZY33P99J7CGG0XRQGZ352","op":"create","changes":[["title",{"kind":"set","value":"Filter tab button no longer focuses the formula bar in the live app"}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"aa80"}],["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\nThe `command-driven-ui` migration of the Filter tab affordance (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for the new no-arg `perspective.filter.focus` command. The unit tests in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` pass — but **the actual end-user behaviour is broken**: clicking the Filter icon on a perspective tab no longer focuses the formula bar's CM6 editor.\n\nHooking the button through the command pipeline is **mandatory** for parity with palette / keybindings / multi-window dispatch — DO NOT revert to the local-callback path. Instead, debug the live wire and make it work end-to-end.\n\n### Current wiring (paper-correct, broken in practice)\n\n1. Click `<CommandButton>` in `kanban-app/ui/src/components/command-button.tsx` (the `perspective.filter.focus` instance rendered by `RegistryTabButtons` in `perspective-tab-bar.tsx:387`).\n2. `handlePress` → `dispatch(\"perspective.filter.focus\", { args: {} })` via `useDispatchCommand` (no popover; every param resolves from scope).\n3. Tauri `dispatch_command` runs `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`), returning `{\"FocusFilter\": {\"perspective_id\": \"<id>\"}}`.\n4. Dispatcher's `apply_post_command_side_effects` → `handle_ui_trigger_results` → `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) emits the Tauri event `ui.focus.filter` carrying `{ perspective_id }`.\n5. `<FilterEditorBody>` in `kanban-app/ui/src/components/filter-editor.tsx:419-450` listens for `ui.focus.filter`, compares `payload.perspective_id` against its own `perspectiveId` prop, and calls `innerRef.current?.focus()` (the CM6 contenteditable).\n\nThe migration test (#5 in `perspective-tab-bar.filter-migration.test.tsx`) simulates step 5 directly by invoking a mocked `listen` callback — it does NOT exercise steps 2–4. So the test stays green even though something between the click and the eventual focus is silently dropping the signal.\n\n### Investigation plan — instrument first, fix second\n\nDo not theorise. Add tracing at each hop and capture which one is silent:\n\n- [ ] **Frontend, in `CommandButton.handlePress`** (`kanban-app/ui/src/components/command-button.tsx:132`): `console.debug(\"[CommandButton] press\", command.id, surfaceId)` so we know the click actually reached the dispatcher.\n- [ ] **Frontend, in `useDispatchCommand`'s dispatch site**: log the resolved `scope_chain` and final `args` sent to `invoke(\"dispatch_command\", …)`. Confirm `perspective_id` is reachable in scope when the user clicks (the `CommandScopeProvider moniker=\"perspective:p1\"` is established by `ScopedPerspectiveTab` at `perspective-tab-bar.tsx:648`).\n- [ ] **Backend, in `FocusFilterCmd::execute`** (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`): `tracing::info!` the resolved `perspective_id` and the returned envelope. Verify `resolve_and_persist_perspective_id` returns Some, not an error.\n- [ ] **Backend, in `handle_focus_filter`** (`kanban-app/src/commands.rs:1717`): log when the branch fires and the emitted payload. Verify the `result.get(\"FocusFilter\")` match succeeds against the JSON the command actually returned.\n- [ ] **Frontend, in `<FilterEditorBody>`'s `listen` callback** (`filter-editor.tsx:422`): log every received `ui.focus.filter` event AND the current `perspectiveId` it's being compared against. The most likely silent failure is an id mismatch — e.g. the active perspective changed between click and event delivery, or the resolver picked a different id than the React subscriber holds.\n- [ ] **Frontend, in `<FilterEditorBody>`'s focus path**: if the id matches but `innerRef.current?.focus()` is no-op, log `innerRef.current` and the underlying CM6 `EditorView`. Note that the legacy path was synchronous (in the user-gesture stack); the new path is fully async after a Tauri round-trip. Some browsers refuse `.focus()` from non-gesture contexts — verify whether CM6's `view.focus()` works here.\n\n### Likely root-cause candidates (in order)\n\n1. **Lost user-gesture window.** The legacy `<FilterFocusButton>` focused CM6 synchronously inside the click handler. The new path goes click → Tauri IPC → Rust → Tauri event → React listener → `.focus()`. By the time `.focus()` is called, the gesture is gone. If WebKit refuses, the focus is silently dropped. Mitigation: focus the editor optimistically inside `CommandButton.handlePress` for `perspective.filter.focus` specifically (a UI hint, not a state mutation), keep the broadcast for cross-window parity. Confirm with the instrumentation first — don't apply this fix blindly.\n2. **`perspective_id` resolves to a different id than the editor's prop.** `resolve_and_persist_perspective_id` falls back through arg → scope-chain → UIState.active → first-perspective-for-view-kind. If the click site's scope_chain doesn't yield `perspective:<id>`, the resolver may pick a different perspective than the one whose editor the user expects. The editor's id-equality guard would then drop the event.\n3. **The `FocusFilter` envelope is mangled by the dispatcher.** Check that `result.get(\"FocusFilter\")` is `Some(...)` at the point of `app.emit(\"ui.focus.filter\", focus_filter)` — if the command result is wrapped (e.g. `{ result: { FocusFilter: ... } }`) by another layer added in this refactor, the match fails and the emit never happens.\n4. **Listener race on perspective switch.** `<FilterFormulaBar>` remounts with `key={perspectiveId}` on perspective change. If the user clicks before the new editor's `listen` promise resolves, the broadcast lands without a listener. Verify the `listen` subscription is in place by the time the user clicks.\n\n### Acceptance Criteria\n\n- [ ] Clicking the Filter tab button focuses the formula bar CM6 editor in the live app. Verified by a new automated end-to-end test (see Tests) — NOT by manual click-through.\n- [ ] The fix preserves the command-driven dispatch path: palette invocation of `perspective.filter.focus` AND keybinding invocation (when added) must also focus the editor. Do not regress those paths by short-circuiting in the click handler only.\n- [ ] The new end-to-end test asserts the **DOM activeElement** ends up on the `.cm-content` of the formula bar after dispatching `perspective.filter.focus` through the production command pipeline — not by manually invoking a mocked `listen` callback.\n- [ ] Implementation notes capture which stage was silently dropping the signal (instrumentation finding) and why the chosen fix addresses that specific stage. Don't ship a fix that \"seems to work\" without identifying the broken hop.\n- [ ] All existing tests in `perspective-tab-bar.filter-migration.test.tsx`, `perspective-tab-bar.registry-driven.test.tsx`, and `perspective-tab-bar.test.tsx` still pass.\n\n### Tests\n\n- [ ] **New end-to-end test** in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` (new `describe` block: `\"filter button — live dispatch round-trip\"`): render `<PerspectiveTabBar>` with a real `<FilterFormulaBar>` mounted (NOT just a standalone `<FilterEditor>`). Mock `invoke(\"dispatch_command\", …)` so it actually drives `handle_focus_filter`'s behaviour — i.e. when the mock sees `perspective.filter.focus`, it synchronously fires the `ui.focus.filter` listeners with `{ perspective_id: \"<host id>\" }` (same payload shape the Rust dispatcher produces). Click the Filter `<CommandButton>` via `fireEvent.click`. Assert `document.activeElement` is the formula bar's `.cm-content` after the click resolves. This is the test that would have caught the current regression.\n- [ ] **Sibling-perspective ignore test**: same harness, but the mocked dispatcher fires with a non-matching `perspective_id`. Assert `document.activeElement` is NOT the `.cm-content` — i.e. the id-equality guard still blocks cross-perspective broadcasts.\n- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar.filter-migration` — green.\n- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — all sibling files green.\n- [ ] **Run** `cargo test -p swissarmyhammer-kanban focus_filter` — existing `focus_filter_command_*` tests in `perspective_commands.rs` still green.\n- [ ] **Run** `cargo test -p kanban-app` — Tauri-side handler tests (if any cover `handle_focus_filter`) green; if no coverage exists for `handle_focus_filter`, add a minimal unit test that constructs the `{\"FocusFilter\":{...}}` envelope and verifies the handler attempts to emit on `ui.focus.filter`.\n\n### Workflow\n\n- Use `/tdd` — write the failing end-to-end test FIRST (it should fail in the current codebase by failing to move `activeElement` to `.cm-content`), then instrument, then fix.\n- **Do not revert to the local-callback path.** Hooking the Filter button through the command pipeline is mandatory.\n- **Stay on task.** Do not refactor `<CommandButton>`, the dispatcher, or the spatial-nav wiring beyond what's needed to fix this specific regression.\n\n#bug #command-driven-ui #frontend #kanban-app #perspectives"}]]}
{"id":"01KRH03YEQ3VK1GFY9XSGZ7TE0","timestamp":"2026-05-13T15:43:35.383847Z","op":"update","item_id":"01KRGZY33P99J7CGG0XRQGZ352","forward_patch":"--- original\n+++ modified\n@@ -3,63 +3,69 @@\n - claude-code\n position_column: todo\n position_ordinal: aa80\n-title: Filter tab button no longer focuses the formula bar in the live app\n+title: Filter tab button must focus via nav.focus, not a parallel Tauri channel\n ---\n ## What\n\n-The `command-driven-ui` migration of the Filter tab affordance (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for the new no-arg `perspective.filter.focus` command. The unit tests in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` pass — but **the actual end-user behaviour is broken**: clicking the Filter icon on a perspective tab no longer focuses the formula bar's CM6 editor.\n+The `command-driven-ui` migration of the Filter tab button (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for `perspective.filter.focus`. The unit tests pass — but **clicking the button no longer focuses the formula bar in the live app**.\n\n-Hooking the button through the command pipeline is **mandatory** for parity with palette / keybindings / multi-window dispatch — DO NOT revert to the local-callback path. Instead, debug the live wire and make it work end-to-end.\n+Root cause is architectural, not a wiring glitch: the migration invented a **parallel focus channel** instead of using the existing `nav.focus` primitive.\n\n-### Current wiring (paper-correct, broken in practice)\n+Today's wiring (parallel channel — DELETE THIS):\n\n-1. Click `<CommandButton>` in `kanban-app/ui/src/components/command-button.tsx` (the `perspective.filter.focus` instance rendered by `RegistryTabButtons` in `perspective-tab-bar.tsx:387`).\n-2. `handlePress` → `dispatch(\"perspective.filter.focus\", { args: {} })` via `useDispatchCommand` (no popover; every param resolves from scope).\n-3. Tauri `dispatch_command` runs `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`), returning `{\"FocusFilter\": {\"perspective_id\": \"<id>\"}}`.\n-4. Dispatcher's `apply_post_command_side_effects` → `handle_ui_trigger_results` → `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) emits the Tauri event `ui.focus.filter` carrying `{ perspective_id }`.\n-5. `<FilterEditorBody>` in `kanban-app/ui/src/components/filter-editor.tsx:419-450` listens for `ui.focus.filter`, compares `payload.perspective_id` against its own `perspectiveId` prop, and calls `innerRef.current?.focus()` (the CM6 contenteditable).\n+1. Click `<CommandButton>` → dispatch `perspective.filter.focus`.\n+2. `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`) returns a `{\"FocusFilter\": {\"perspective_id\": ...}}` marker envelope.\n+3. Tauri dispatcher's `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) catches the envelope and emits a Tauri event `ui.focus.filter`.\n+4. `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:419-450`) `listen`s for that event, id-checks the payload, and calls `innerRef.current?.focus()` on the CM6 contenteditable.\n\n-The migration test (#5 in `perspective-tab-bar.filter-migration.test.tsx`) simulates step 5 directly by invoking a mocked `listen` callback — it does NOT exercise steps 2–4. So the test stays green even though something between the click and the eventual focus is silently dropping the signal.\n+The architecture per `01KR7CDEFWWVF4WH0BCHE8Y21J` is explicit: **every focus claim flows through `nav.focus({ args: { fq } })`** — registered once at `spatial-focus-context.tsx:381` (`buildSpatialNavFocusCommand`), which dispatches `spatial_focus` IPC, runs the cross-cutting concerns (telemetry, animations, scroll-on-focus), and lets the kernel emit `focus-changed` back to React. Components do not call `spatial.focus(fq)`, `setFocus(fq)`, or imperative `.focus()` directly — they dispatch `nav.focus`.\n\n-### Investigation plan — instrument first, fix second\n+The filter editor already has a moniker the spatial-nav system knows about: `<FilterFormulaBarFocusable>` wraps the formula bar in `<FocusScope moniker={asSegment(`filter_editor:${perspectiveId}`)}>` (`perspective-tab-bar.tsx:794`). When the kernel claims focus on `filter_editor:<id>`, the focus indicator paints and the `nav.drillIn` Enter handler at that scope already drives CM6 to take editing focus (the same path the rest of the spatial-nav system uses). And `FilterEditorDrillOutWiring` (`perspective-tab-bar.tsx:814-844`) already proves the pattern — it dispatches `nav.focus({ args: { fq: filterFq } })` to drill back out to the filter editor scope.\n\n-Do not theorise. Add tracing at each hop and capture which one is silent:\n+So the fix is: **dispatch `nav.focus` with the filter editor's FQM as the implementation of the filter button action.** Delete the parallel channel.\n\n-- [ ] **Frontend, in `CommandButton.handlePress`** (`kanban-app/ui/src/components/command-button.tsx:132`): `console.debug(\"[CommandButton] press\", command.id, surfaceId)` so we know the click actually reached the dispatcher.\n-- [ ] **Frontend, in `useDispatchCommand`'s dispatch site**: log the resolved `scope_chain` and final `args` sent to `invoke(\"dispatch_command\", …)`. Confirm `perspective_id` is reachable in scope when the user clicks (the `CommandScopeProvider moniker=\"perspective:p1\"` is established by `ScopedPerspectiveTab` at `perspective-tab-bar.tsx:648`).\n-- [ ] **Backend, in `FocusFilterCmd::execute`** (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`): `tracing::info!` the resolved `perspective_id` and the returned envelope. Verify `resolve_and_persist_perspective_id` returns Some, not an error.\n-- [ ] **Backend, in `handle_focus_filter`** (`kanban-app/src/commands.rs:1717`): log when the branch fires and the emitted payload. Verify the `result.get(\"FocusFilter\")` match succeeds against the JSON the command actually returned.\n-- [ ] **Frontend, in `<FilterEditorBody>`'s `listen` callback** (`filter-editor.tsx:422`): log every received `ui.focus.filter` event AND the current `perspectiveId` it's being compared against. The most likely silent failure is an id mismatch — e.g. the active perspective changed between click and event delivery, or the resolver picked a different id than the React subscriber holds.\n-- [ ] **Frontend, in `<FilterEditorBody>`'s focus path**: if the id matches but `innerRef.current?.focus()` is no-op, log `innerRef.current` and the underlying CM6 `EditorView`. Note that the legacy path was synchronous (in the user-gesture stack); the new path is fully async after a Tauri round-trip. Some browsers refuse `.focus()` from non-gesture contexts — verify whether CM6's `view.focus()` works here.\n+### Required changes\n+\n+- [ ] **Click site — make the button dispatch `nav.focus`.** The cleanest implementation: in `<RegistryTabButtons>` (`perspective-tab-bar.tsx:387`), wrap the `<CommandButton>` for `perspective.filter.focus` in a small adapter that overrides the dispatch to issue `nav.focus({ args: { fq: <filter_editor FQM> } })` instead of the raw command id. The FQM is composed the same way `FilterEditorDrillOutWiring` does it — via `useFullyQualifiedMoniker()` inside the `filter_editor:${id}` scope, OR resolved at the tab level by composing the known segments (`window` / scope chain up to `filter_editor:${perspectiveId}`). Reuse whatever the rest of the spatial system uses; do not invent a new composition.\n+- [ ] **Delete the parallel channel — backend.** Remove `FocusFilterCmd` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:823-858`). Remove its registration from `swissarmyhammer-kanban/src/commands/mod.rs`. The YAML entry `perspective.filter.focus` in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` no longer needs a backend `execute` — but it still drives the tab-button rendering (`tab_button: { icon: filter }`) and the `isActive` highlight derivation. Decide whether to: (a) keep the YAML entry as a UI-only declaration with a frontend-resolved `execute` (preferred — keeps the registry as the source of truth for what renders) or (b) remove the entry entirely and inline a hand-rolled `<CommandButton>` for the filter slot. Pick (a) unless there's a concrete reason not to.\n+- [ ] **Delete the parallel channel — Tauri glue.** Remove `handle_focus_filter` (`kanban-app/src/commands.rs:1717-1723`) and its call site in `handle_ui_trigger_results` (`kanban-app/src/commands.rs:1663`).\n+- [ ] **Delete the parallel channel — frontend.** Remove the `listen(\"ui.focus.filter\", …)` subscription in `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:402-450`) and the imperative `innerRef.current?.focus()` it drives. CM6 focusing happens via the existing `nav.drillIn` flow on the `filter_editor:${id}` scope, which is the same path that already lights up when the user arrow-navigates to the formula bar.\n+- [ ] **Update the tests.** Replace test #5 in `perspective-tab-bar.filter-migration.test.tsx` (`filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`) with a test that:\n+  - Clicks the Filter `<CommandButton>`.\n+  - Asserts the dispatched command is `nav.focus` with `args.fq` equal to the filter editor's composed FQM (or, equivalently, that the spatial-focus kernel ends up holding the `filter_editor:<id>` moniker).\n+  - Asserts the existing `nav.drillIn` flow then puts editing focus on `.cm-content`.\n+- [ ] **Update the YAML doc.** The YAML comment in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` describes the old `FocusFilter`/`ui.focus.filter` mechanism. Replace it with a one-paragraph note that the click site dispatches `nav.focus({ fq: filter_editor:<id> })` and points at `nav.yaml` / `spatial-focus-context.tsx` for the focus primitive itself.\n\n-### Likely root-cause candidates (in order)\n+### Why this is mandatory\n\n-1. **Lost user-gesture window.** The legacy `<FilterFocusButton>` focused CM6 synchronously inside the click handler. The new path goes click → Tauri IPC → Rust → Tauri event → React listener → `.focus()`. By the time `.focus()` is called, the gesture is gone. If WebKit refuses, the focus is silently dropped. Mitigation: focus the editor optimistically inside `CommandButton.handlePress` for `perspective.filter.focus` specifically (a UI hint, not a state mutation), keep the broadcast for cross-window parity. Confirm with the instrumentation first — don't apply this fix blindly.\n-2. **`perspective_id` resolves to a different id than the editor's prop.** `resolve_and_persist_perspective_id` falls back through arg → scope-chain → UIState.active → first-perspective-for-view-kind. If the click site's scope_chain doesn't yield `perspective:<id>`, the resolver may pick a different perspective than the one whose editor the user expects. The editor's id-equality guard would then drop the event.\n-3. **The `FocusFilter` envelope is mangled by the dispatcher.** Check that `result.get(\"FocusFilter\")` is `Some(...)` at the point of `app.emit(\"ui.focus.filter\", focus_filter)` — if the command result is wrapped (e.g. `{ result: { FocusFilter: ... } }`) by another layer added in this refactor, the match fails and the emit never happens.\n-4. **Listener race on perspective switch.** `<FilterFormulaBar>` remounts with `key={perspectiveId}` on perspective change. If the user clicks before the new editor's `listen` promise resolves, the broadcast lands without a listener. Verify the `listen` subscription is in place by the time the user clicks.\n+- **The architecture rule from `01KR7CDEFWWVF4WH0BCHE8Y21J` is non-negotiable.** Every focus claim flows through `nav.focus`. The parallel `FocusFilter`/`ui.focus.filter` channel violates this and would have to be re-built (telemetry, animation, scroll-on-focus) every time someone added a cross-cutting concern.\n+- **The current channel is broken because it bypasses the kernel.** The kernel doesn't know focus moved to `filter_editor:<id>`, so its model of \"where is focus now?\" goes out of sync with the DOM. Even if we patch the immediate CM6 `.focus()` bug, spatial arrow-nav from the formula bar wouldn't know it's there.\n+- **`nav.focus` already works.** Jump-to and arrow nav both prove the path is live end-to-end. We're not building anything new — we're routing the filter button through the path that already works.\n\n ### Acceptance Criteria\n\n-- [ ] Clicking the Filter tab button focuses the formula bar CM6 editor in the live app. Verified by a new automated end-to-end test (see Tests) — NOT by manual click-through.\n-- [ ] The fix preserves the command-driven dispatch path: palette invocation of `perspective.filter.focus` AND keybinding invocation (when added) must also focus the editor. Do not regress those paths by short-circuiting in the click handler only.\n-- [ ] The new end-to-end test asserts the **DOM activeElement** ends up on the `.cm-content` of the formula bar after dispatching `perspective.filter.focus` through the production command pipeline — not by manually invoking a mocked `listen` callback.\n-- [ ] Implementation notes capture which stage was silently dropping the signal (instrumentation finding) and why the chosen fix addresses that specific stage. Don't ship a fix that \"seems to work\" without identifying the broken hop.\n-- [ ] All existing tests in `perspective-tab-bar.filter-migration.test.tsx`, `perspective-tab-bar.registry-driven.test.tsx`, and `perspective-tab-bar.test.tsx` still pass.\n+- [ ] Clicking the Filter tab button moves spatial-nav focus to `filter_editor:<id>`. Verified by querying the spatial-focus store after the click.\n+- [ ] `nav.drillIn` (Enter) on that scope then puts DOM `activeElement` on the formula bar's `.cm-content`. (Same mechanism the spatial system already uses elsewhere — do NOT add a one-off `.focus()` call.)\n+- [ ] If the UX requires the click to land directly in editing mode (skip the explicit drill-in), the click handler dispatches `nav.focus` AND `nav.drillIn` as a two-step — both auditable, both through the registered command path. Do not short-circuit by calling `view.focus()` from the click handler.\n+- [ ] `FocusFilterCmd`, `handle_focus_filter`, and the `ui.focus.filter` `listen` subscription are deleted. `git grep -i 'FocusFilter\\|ui\\.focus\\.filter'` returns zero hits in `kanban-app/`, `swissarmyhammer-kanban/`, and the YAML.\n+- [ ] Palette invocation of `perspective.filter.focus` and (future) keybinding invocation produce the same focus outcome as the tab-button click — i.e. they all funnel through `nav.focus`.\n+- [ ] The existing `focus_filter_command_*` Rust tests in `perspective_commands.rs` are either updated or deleted alongside `FocusFilterCmd`. No tests pin the deleted parallel-channel behaviour.\n\n ### Tests\n\n-- [ ] **New end-to-end test** in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` (new `describe` block: `\"filter button — live dispatch round-trip\"`): render `<PerspectiveTabBar>` with a real `<FilterFormulaBar>` mounted (NOT just a standalone `<FilterEditor>`). Mock `invoke(\"dispatch_command\", …)` so it actually drives `handle_focus_filter`'s behaviour — i.e. when the mock sees `perspective.filter.focus`, it synchronously fires the `ui.focus.filter` listeners with `{ perspective_id: \"<host id>\" }` (same payload shape the Rust dispatcher produces). Click the Filter `<CommandButton>` via `fireEvent.click`. Assert `document.activeElement` is the formula bar's `.cm-content` after the click resolves. This is the test that would have caught the current regression.\n-- [ ] **Sibling-perspective ignore test**: same harness, but the mocked dispatcher fires with a non-matching `perspective_id`. Assert `document.activeElement` is NOT the `.cm-content` — i.e. the id-equality guard still blocks cross-perspective broadcasts.\n-- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar.filter-migration` — green.\n-- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — all sibling files green.\n-- [ ] **Run** `cargo test -p swissarmyhammer-kanban focus_filter` — existing `focus_filter_command_*` tests in `perspective_commands.rs` still green.\n-- [ ] **Run** `cargo test -p kanban-app` — Tauri-side handler tests (if any cover `handle_focus_filter`) green; if no coverage exists for `handle_focus_filter`, add a minimal unit test that constructs the `{\"FocusFilter\":{...}}` envelope and verifies the handler attempts to emit on `ui.focus.filter`.\n+- [ ] **New test in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx`**: render `<PerspectiveTabBar>` inside `<SpatialFocusProvider>` and `<EntityFocusProvider>`. Click the Filter `<CommandButton>`. Assert the spatial-focus store's current FQM ends in `filter_editor:<active-perspective-id>`. Then synthesise a `nav.drillIn` (or rely on the click handler issuing it) and assert `document.activeElement` is the formula bar's `.cm-content`.\n+- [ ] **Sibling-perspective test**: switch active perspective after rendering, click Filter, assert focus lands on the NEW active perspective's `filter_editor` moniker — not on the previously active one. (This catches the id-resolution bug that the deleted Tauri-channel path was also vulnerable to.)\n+- [ ] **Delete `filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`** and the helpers that simulate the Tauri `listen` callback. It pins behaviour we're removing.\n+- [ ] **Update `focus_filter_command_*` Rust tests** in `swissarmyhammer-kanban/src/commands/perspective_commands.rs:2180-2252` — delete them along with `FocusFilterCmd`. If any Rust test belongs in the new world (e.g. the YAML registration still smoke-tested by `composed_commands_registry.rs`), update its assertions to match.\n+- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — green.\n+- [ ] **Run** `pnpm -C kanban-app/ui test filter-editor` — green; the deleted `listen` subscription does not break any sibling test.\n+- [ ] **Run** `cargo test -p swissarmyhammer-kanban` — green.\n+- [ ] **Run** `cargo test -p kanban-app` — green; nothing references `handle_focus_filter` after the deletion.\n\n ### Workflow\n\n-- Use `/tdd` — write the failing end-to-end test FIRST (it should fail in the current codebase by failing to move `activeElement` to `.cm-content`), then instrument, then fix.\n-- **Do not revert to the local-callback path.** Hooking the Filter button through the command pipeline is mandatory.\n-- **Stay on task.** Do not refactor `<CommandButton>`, the dispatcher, or the spatial-nav wiring beyond what's needed to fix this specific regression.\n+- Use `/tdd` — write the failing test (Filter click → spatial-focus store ends on `filter_editor:<id>`) FIRST. Watch it fail in the current codebase, then route the click through `nav.focus`, then delete the parallel channel, then watch the test pass.\n+- **Delete, don't deprecate.** No shim left behind that emits `ui.focus.filter` for backwards compatibility. The channel doesn't exist after this task.\n+- **Stay on task.** Do not touch the spatial-nav kernel, the `nav.focus` registration, or `<FilterFormulaBarFocusable>`. The fix is at the click site + the deletion of the parallel channel.\n\n-#bug #command-driven-ui #frontend #kanban-app #perspectives\n\\ No newline at end of file\n+#bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -3,69 +3,63 @@\n - claude-code\n position_column: todo\n position_ordinal: aa80\n-title: Filter tab button must focus via nav.focus, not a parallel Tauri channel\n+title: Filter tab button no longer focuses the formula bar in the live app\n ---\n ## What\n\n-The `command-driven-ui` migration of the Filter tab button (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for `perspective.filter.focus`. The unit tests pass — but **clicking the button no longer focuses the formula bar in the live app**.\n+The `command-driven-ui` migration of the Filter tab affordance (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for the new no-arg `perspective.filter.focus` command. The unit tests in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` pass — but **the actual end-user behaviour is broken**: clicking the Filter icon on a perspective tab no longer focuses the formula bar's CM6 editor.\n\n-Root cause is architectural, not a wiring glitch: the migration invented a **parallel focus channel** instead of using the existing `nav.focus` primitive.\n+Hooking the button through the command pipeline is **mandatory** for parity with palette / keybindings / multi-window dispatch — DO NOT revert to the local-callback path. Instead, debug the live wire and make it work end-to-end.\n\n-Today's wiring (parallel channel — DELETE THIS):\n+### Current wiring (paper-correct, broken in practice)\n\n-1. Click `<CommandButton>` → dispatch `perspective.filter.focus`.\n-2. `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`) returns a `{\"FocusFilter\": {\"perspective_id\": ...}}` marker envelope.\n-3. Tauri dispatcher's `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) catches the envelope and emits a Tauri event `ui.focus.filter`.\n-4. `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:419-450`) `listen`s for that event, id-checks the payload, and calls `innerRef.current?.focus()` on the CM6 contenteditable.\n+1. Click `<CommandButton>` in `kanban-app/ui/src/components/command-button.tsx` (the `perspective.filter.focus` instance rendered by `RegistryTabButtons` in `perspective-tab-bar.tsx:387`).\n+2. `handlePress` → `dispatch(\"perspective.filter.focus\", { args: {} })` via `useDispatchCommand` (no popover; every param resolves from scope).\n+3. Tauri `dispatch_command` runs `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`), returning `{\"FocusFilter\": {\"perspective_id\": \"<id>\"}}`.\n+4. Dispatcher's `apply_post_command_side_effects` → `handle_ui_trigger_results` → `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) emits the Tauri event `ui.focus.filter` carrying `{ perspective_id }`.\n+5. `<FilterEditorBody>` in `kanban-app/ui/src/components/filter-editor.tsx:419-450` listens for `ui.focus.filter`, compares `payload.perspective_id` against its own `perspectiveId` prop, and calls `innerRef.current?.focus()` (the CM6 contenteditable).\n\n-The architecture per `01KR7CDEFWWVF4WH0BCHE8Y21J` is explicit: **every focus claim flows through `nav.focus({ args: { fq } })`** — registered once at `spatial-focus-context.tsx:381` (`buildSpatialNavFocusCommand`), which dispatches `spatial_focus` IPC, runs the cross-cutting concerns (telemetry, animations, scroll-on-focus), and lets the kernel emit `focus-changed` back to React. Components do not call `spatial.focus(fq)`, `setFocus(fq)`, or imperative `.focus()` directly — they dispatch `nav.focus`.\n+The migration test (#5 in `perspective-tab-bar.filter-migration.test.tsx`) simulates step 5 directly by invoking a mocked `listen` callback — it does NOT exercise steps 2–4. So the test stays green even though something between the click and the eventual focus is silently dropping the signal.\n\n-The filter editor already has a moniker the spatial-nav system knows about: `<FilterFormulaBarFocusable>` wraps the formula bar in `<FocusScope moniker={asSegment(`filter_editor:${perspectiveId}`)}>` (`perspective-tab-bar.tsx:794`). When the kernel claims focus on `filter_editor:<id>`, the focus indicator paints and the `nav.drillIn` Enter handler at that scope already drives CM6 to take editing focus (the same path the rest of the spatial-nav system uses). And `FilterEditorDrillOutWiring` (`perspective-tab-bar.tsx:814-844`) already proves the pattern — it dispatches `nav.focus({ args: { fq: filterFq } })` to drill back out to the filter editor scope.\n+### Investigation plan — instrument first, fix second\n\n-So the fix is: **dispatch `nav.focus` with the filter editor's FQM as the implementation of the filter button action.** Delete the parallel channel.\n+Do not theorise. Add tracing at each hop and capture which one is silent:\n\n-### Required changes\n+- [ ] **Frontend, in `CommandButton.handlePress`** (`kanban-app/ui/src/components/command-button.tsx:132`): `console.debug(\"[CommandButton] press\", command.id, surfaceId)` so we know the click actually reached the dispatcher.\n+- [ ] **Frontend, in `useDispatchCommand`'s dispatch site**: log the resolved `scope_chain` and final `args` sent to `invoke(\"dispatch_command\", …)`. Confirm `perspective_id` is reachable in scope when the user clicks (the `CommandScopeProvider moniker=\"perspective:p1\"` is established by `ScopedPerspectiveTab` at `perspective-tab-bar.tsx:648`).\n+- [ ] **Backend, in `FocusFilterCmd::execute`** (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`): `tracing::info!` the resolved `perspective_id` and the returned envelope. Verify `resolve_and_persist_perspective_id` returns Some, not an error.\n+- [ ] **Backend, in `handle_focus_filter`** (`kanban-app/src/commands.rs:1717`): log when the branch fires and the emitted payload. Verify the `result.get(\"FocusFilter\")` match succeeds against the JSON the command actually returned.\n+- [ ] **Frontend, in `<FilterEditorBody>`'s `listen` callback** (`filter-editor.tsx:422`): log every received `ui.focus.filter` event AND the current `perspectiveId` it's being compared against. The most likely silent failure is an id mismatch — e.g. the active perspective changed between click and event delivery, or the resolver picked a different id than the React subscriber holds.\n+- [ ] **Frontend, in `<FilterEditorBody>`'s focus path**: if the id matches but `innerRef.current?.focus()` is no-op, log `innerRef.current` and the underlying CM6 `EditorView`. Note that the legacy path was synchronous (in the user-gesture stack); the new path is fully async after a Tauri round-trip. Some browsers refuse `.focus()` from non-gesture contexts — verify whether CM6's `view.focus()` works here.\n\n-- [ ] **Click site — make the button dispatch `nav.focus`.** The cleanest implementation: in `<RegistryTabButtons>` (`perspective-tab-bar.tsx:387`), wrap the `<CommandButton>` for `perspective.filter.focus` in a small adapter that overrides the dispatch to issue `nav.focus({ args: { fq: <filter_editor FQM> } })` instead of the raw command id. The FQM is composed the same way `FilterEditorDrillOutWiring` does it — via `useFullyQualifiedMoniker()` inside the `filter_editor:${id}` scope, OR resolved at the tab level by composing the known segments (`window` / scope chain up to `filter_editor:${perspectiveId}`). Reuse whatever the rest of the spatial system uses; do not invent a new composition.\n-- [ ] **Delete the parallel channel — backend.** Remove `FocusFilterCmd` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:823-858`). Remove its registration from `swissarmyhammer-kanban/src/commands/mod.rs`. The YAML entry `perspective.filter.focus` in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` no longer needs a backend `execute` — but it still drives the tab-button rendering (`tab_button: { icon: filter }`) and the `isActive` highlight derivation. Decide whether to: (a) keep the YAML entry as a UI-only declaration with a frontend-resolved `execute` (preferred — keeps the registry as the source of truth for what renders) or (b) remove the entry entirely and inline a hand-rolled `<CommandButton>` for the filter slot. Pick (a) unless there's a concrete reason not to.\n-- [ ] **Delete the parallel channel — Tauri glue.** Remove `handle_focus_filter` (`kanban-app/src/commands.rs:1717-1723`) and its call site in `handle_ui_trigger_results` (`kanban-app/src/commands.rs:1663`).\n-- [ ] **Delete the parallel channel — frontend.** Remove the `listen(\"ui.focus.filter\", …)` subscription in `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:402-450`) and the imperative `innerRef.current?.focus()` it drives. CM6 focusing happens via the existing `nav.drillIn` flow on the `filter_editor:${id}` scope, which is the same path that already lights up when the user arrow-navigates to the formula bar.\n-- [ ] **Update the tests.** Replace test #5 in `perspective-tab-bar.filter-migration.test.tsx` (`filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`) with a test that:\n-  - Clicks the Filter `<CommandButton>`.\n-  - Asserts the dispatched command is `nav.focus` with `args.fq` equal to the filter editor's composed FQM (or, equivalently, that the spatial-focus kernel ends up holding the `filter_editor:<id>` moniker).\n-  - Asserts the existing `nav.drillIn` flow then puts editing focus on `.cm-content`.\n-- [ ] **Update the YAML doc.** The YAML comment in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` describes the old `FocusFilter`/`ui.focus.filter` mechanism. Replace it with a one-paragraph note that the click site dispatches `nav.focus({ fq: filter_editor:<id> })` and points at `nav.yaml` / `spatial-focus-context.tsx` for the focus primitive itself.\n-\n-### Why this is mandatory\n+### Likely root-cause candidates (in order)\n\n-- **The architecture rule from `01KR7CDEFWWVF4WH0BCHE8Y21J` is non-negotiable.** Every focus claim flows through `nav.focus`. The parallel `FocusFilter`/`ui.focus.filter` channel violates this and would have to be re-built (telemetry, animation, scroll-on-focus) every time someone added a cross-cutting concern.\n-- **The current channel is broken because it bypasses the kernel.** The kernel doesn't know focus moved to `filter_editor:<id>`, so its model of \"where is focus now?\" goes out of sync with the DOM. Even if we patch the immediate CM6 `.focus()` bug, spatial arrow-nav from the formula bar wouldn't know it's there.\n-- **`nav.focus` already works.** Jump-to and arrow nav both prove the path is live end-to-end. We're not building anything new — we're routing the filter button through the path that already works.\n+1. **Lost user-gesture window.** The legacy `<FilterFocusButton>` focused CM6 synchronously inside the click handler. The new path goes click → Tauri IPC → Rust → Tauri event → React listener → `.focus()`. By the time `.focus()` is called, the gesture is gone. If WebKit refuses, the focus is silently dropped. Mitigation: focus the editor optimistically inside `CommandButton.handlePress` for `perspective.filter.focus` specifically (a UI hint, not a state mutation), keep the broadcast for cross-window parity. Confirm with the instrumentation first — don't apply this fix blindly.\n+2. **`perspective_id` resolves to a different id than the editor's prop.** `resolve_and_persist_perspective_id` falls back through arg → scope-chain → UIState.active → first-perspective-for-view-kind. If the click site's scope_chain doesn't yield `perspective:<id>`, the resolver may pick a different perspective than the one whose editor the user expects. The editor's id-equality guard would then drop the event.\n+3. **The `FocusFilter` envelope is mangled by the dispatcher.** Check that `result.get(\"FocusFilter\")` is `Some(...)` at the point of `app.emit(\"ui.focus.filter\", focus_filter)` — if the command result is wrapped (e.g. `{ result: { FocusFilter: ... } }`) by another layer added in this refactor, the match fails and the emit never happens.\n+4. **Listener race on perspective switch.** `<FilterFormulaBar>` remounts with `key={perspectiveId}` on perspective change. If the user clicks before the new editor's `listen` promise resolves, the broadcast lands without a listener. Verify the `listen` subscription is in place by the time the user clicks.\n\n ### Acceptance Criteria\n\n-- [ ] Clicking the Filter tab button moves spatial-nav focus to `filter_editor:<id>`. Verified by querying the spatial-focus store after the click.\n-- [ ] `nav.drillIn` (Enter) on that scope then puts DOM `activeElement` on the formula bar's `.cm-content`. (Same mechanism the spatial system already uses elsewhere — do NOT add a one-off `.focus()` call.)\n-- [ ] If the UX requires the click to land directly in editing mode (skip the explicit drill-in), the click handler dispatches `nav.focus` AND `nav.drillIn` as a two-step — both auditable, both through the registered command path. Do not short-circuit by calling `view.focus()` from the click handler.\n-- [ ] `FocusFilterCmd`, `handle_focus_filter`, and the `ui.focus.filter` `listen` subscription are deleted. `git grep -i 'FocusFilter\\|ui\\.focus\\.filter'` returns zero hits in `kanban-app/`, `swissarmyhammer-kanban/`, and the YAML.\n-- [ ] Palette invocation of `perspective.filter.focus` and (future) keybinding invocation produce the same focus outcome as the tab-button click — i.e. they all funnel through `nav.focus`.\n-- [ ] The existing `focus_filter_command_*` Rust tests in `perspective_commands.rs` are either updated or deleted alongside `FocusFilterCmd`. No tests pin the deleted parallel-channel behaviour.\n+- [ ] Clicking the Filter tab button focuses the formula bar CM6 editor in the live app. Verified by a new automated end-to-end test (see Tests) — NOT by manual click-through.\n+- [ ] The fix preserves the command-driven dispatch path: palette invocation of `perspective.filter.focus` AND keybinding invocation (when added) must also focus the editor. Do not regress those paths by short-circuiting in the click handler only.\n+- [ ] The new end-to-end test asserts the **DOM activeElement** ends up on the `.cm-content` of the formula bar after dispatching `perspective.filter.focus` through the production command pipeline — not by manually invoking a mocked `listen` callback.\n+- [ ] Implementation notes capture which stage was silently dropping the signal (instrumentation finding) and why the chosen fix addresses that specific stage. Don't ship a fix that \"seems to work\" without identifying the broken hop.\n+- [ ] All existing tests in `perspective-tab-bar.filter-migration.test.tsx`, `perspective-tab-bar.registry-driven.test.tsx`, and `perspective-tab-bar.test.tsx` still pass.\n\n ### Tests\n\n-- [ ] **New test in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx`**: render `<PerspectiveTabBar>` inside `<SpatialFocusProvider>` and `<EntityFocusProvider>`. Click the Filter `<CommandButton>`. Assert the spatial-focus store's current FQM ends in `filter_editor:<active-perspective-id>`. Then synthesise a `nav.drillIn` (or rely on the click handler issuing it) and assert `document.activeElement` is the formula bar's `.cm-content`.\n-- [ ] **Sibling-perspective test**: switch active perspective after rendering, click Filter, assert focus lands on the NEW active perspective's `filter_editor` moniker — not on the previously active one. (This catches the id-resolution bug that the deleted Tauri-channel path was also vulnerable to.)\n-- [ ] **Delete `filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`** and the helpers that simulate the Tauri `listen` callback. It pins behaviour we're removing.\n-- [ ] **Update `focus_filter_command_*` Rust tests** in `swissarmyhammer-kanban/src/commands/perspective_commands.rs:2180-2252` — delete them along with `FocusFilterCmd`. If any Rust test belongs in the new world (e.g. the YAML registration still smoke-tested by `composed_commands_registry.rs`), update its assertions to match.\n-- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — green.\n-- [ ] **Run** `pnpm -C kanban-app/ui test filter-editor` — green; the deleted `listen` subscription does not break any sibling test.\n-- [ ] **Run** `cargo test -p swissarmyhammer-kanban` — green.\n-- [ ] **Run** `cargo test -p kanban-app` — green; nothing references `handle_focus_filter` after the deletion.\n+- [ ] **New end-to-end test** in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` (new `describe` block: `\"filter button — live dispatch round-trip\"`): render `<PerspectiveTabBar>` with a real `<FilterFormulaBar>` mounted (NOT just a standalone `<FilterEditor>`). Mock `invoke(\"dispatch_command\", …)` so it actually drives `handle_focus_filter`'s behaviour — i.e. when the mock sees `perspective.filter.focus`, it synchronously fires the `ui.focus.filter` listeners with `{ perspective_id: \"<host id>\" }` (same payload shape the Rust dispatcher produces). Click the Filter `<CommandButton>` via `fireEvent.click`. Assert `document.activeElement` is the formula bar's `.cm-content` after the click resolves. This is the test that would have caught the current regression.\n+- [ ] **Sibling-perspective ignore test**: same harness, but the mocked dispatcher fires with a non-matching `perspective_id`. Assert `document.activeElement` is NOT the `.cm-content` — i.e. the id-equality guard still blocks cross-perspective broadcasts.\n+- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar.filter-migration` — green.\n+- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — all sibling files green.\n+- [ ] **Run** `cargo test -p swissarmyhammer-kanban focus_filter` — existing `focus_filter_command_*` tests in `perspective_commands.rs` still green.\n+- [ ] **Run** `cargo test -p kanban-app` — Tauri-side handler tests (if any cover `handle_focus_filter`) green; if no coverage exists for `handle_focus_filter`, add a minimal unit test that constructs the `{\"FocusFilter\":{...}}` envelope and verifies the handler attempts to emit on `ui.focus.filter`.\n\n ### Workflow\n\n-- Use `/tdd` — write the failing test (Filter click → spatial-focus store ends on `filter_editor:<id>`) FIRST. Watch it fail in the current codebase, then route the click through `nav.focus`, then delete the parallel channel, then watch the test pass.\n-- **Delete, don't deprecate.** No shim left behind that emits `ui.focus.filter` for backwards compatibility. The channel doesn't exist after this task.\n-- **Stay on task.** Do not touch the spatial-nav kernel, the `nav.focus` registration, or `<FilterFormulaBarFocusable>`. The fix is at the click site + the deletion of the parallel channel.\n+- Use `/tdd` — write the failing end-to-end test FIRST (it should fail in the current codebase by failing to move `activeElement` to `.cm-content`), then instrument, then fix.\n+- **Do not revert to the local-callback path.** Hooking the Filter button through the command pipeline is mandatory.\n+- **Stay on task.** Do not refactor `<CommandButton>`, the dispatcher, or the spatial-nav wiring beyond what's needed to fix this specific regression.\n\n-#bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n\\ No newline at end of file\n+#bug #command-driven-ui #frontend #kanban-app #perspectives\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRH03YETTHTBFHWFF4HY2C82","timestamp":"2026-05-13T15:43:35.386364Z","entity_type":"task","entity_id":"01KRGZY33P99J7CGG0XRQGZ352","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,58 +1,64 @@\n ## What\n\n-The `command-driven-ui` migration of the Filter tab affordance (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for the new no-arg `perspective.filter.focus` command. The unit tests in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` pass — but **the actual end-user behaviour is broken**: clicking the Filter icon on a perspective tab no longer focuses the formula bar's CM6 editor.\n+The `command-driven-ui` migration of the Filter tab button (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for `perspective.filter.focus`. The unit tests pass — but **clicking the button no longer focuses the formula bar in the live app**.\n\n-Hooking the button through the command pipeline is **mandatory** for parity with palette / keybindings / multi-window dispatch — DO NOT revert to the local-callback path. Instead, debug the live wire and make it work end-to-end.\n+Root cause is architectural, not a wiring glitch: the migration invented a **parallel focus channel** instead of using the existing `nav.focus` primitive.\n+\n+Today's wiring (parallel channel — DELETE THIS):\n\n-### Current wiring (paper-correct, broken in practice)\n+1. Click `<CommandButton>` → dispatch `perspective.filter.focus`.\n+2. `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`) returns a `{\"FocusFilter\": {\"perspective_id\": ...}}` marker envelope.\n+3. Tauri dispatcher's `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) catches the envelope and emits a Tauri event `ui.focus.filter`.\n+4. `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:419-450`) `listen`s for that event, id-checks the payload, and calls `innerRef.current?.focus()` on the CM6 contenteditable.\n\n-1. Click `<CommandButton>` in `kanban-app/ui/src/components/command-button.tsx` (the `perspective.filter.focus` instance rendered by `RegistryTabButtons` in `perspective-tab-bar.tsx:387`).\n-2. `handlePress` → `dispatch(\"perspective.filter.focus\", { args: {} })` via `useDispatchCommand` (no popover; every param resolves from scope).\n-3. Tauri `dispatch_command` runs `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`), returning `{\"FocusFilter\": {\"perspective_id\": \"<id>\"}}`.\n-4. Dispatcher's `apply_post_command_side_effects` → `handle_ui_trigger_results` → `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) emits the Tauri event `ui.focus.filter` carrying `{ perspective_id }`.\n-5. `<FilterEditorBody>` in `kanban-app/ui/src/components/filter-editor.tsx:419-450` listens for `ui.focus.filter`, compares `payload.perspective_id` against its own `perspectiveId` prop, and calls `innerRef.current?.focus()` (the CM6 contenteditable).\n+The architecture per `01KR7CDEFWWVF4WH0BCHE8Y21J` is explicit: **every focus claim flows through `nav.focus({ args: { fq } })`** — registered once at `spatial-focus-context.tsx:381` (`buildSpatialNavFocusCommand`), which dispatches `spatial_focus` IPC, runs the cross-cutting concerns (telemetry, animations, scroll-on-focus), and lets the kernel emit `focus-changed` back to React. Components do not call `spatial.focus(fq)`, `setFocus(fq)`, or imperative `.focus()` directly — they dispatch `nav.focus`.\n\n-The migration test (#5 in `perspective-tab-bar.filter-migration.test.tsx`) simulates step 5 directly by invoking a mocked `listen` callback — it does NOT exercise steps 2–4. So the test stays green even though something between the click and the eventual focus is silently dropping the signal.\n+The filter editor already has a moniker the spatial-nav system knows about: `<FilterFormulaBarFocusable>` wraps the formula bar in `<FocusScope moniker={asSegment(`filter_editor:${perspectiveId}`)}>` (`perspective-tab-bar.tsx:794`). When the kernel claims focus on `filter_editor:<id>`, the focus indicator paints and the `nav.drillIn` Enter handler at that scope already drives CM6 to take editing focus (the same path the rest of the spatial-nav system uses). And `FilterEditorDrillOutWiring` (`perspective-tab-bar.tsx:814-844`) already proves the pattern — it dispatches `nav.focus({ args: { fq: filterFq } })` to drill back out to the filter editor scope.\n\n-### Investigation plan — instrument first, fix second\n+So the fix is: **dispatch `nav.focus` with the filter editor's FQM as the implementation of the filter button action.** Delete the parallel channel.\n\n-Do not theorise. Add tracing at each hop and capture which one is silent:\n+### Required changes\n\n-- [ ] **Frontend, in `CommandButton.handlePress`** (`kanban-app/ui/src/components/command-button.tsx:132`): `console.debug(\"[CommandButton] press\", command.id, surfaceId)` so we know the click actually reached the dispatcher.\n-- [ ] **Frontend, in `useDispatchCommand`'s dispatch site**: log the resolved `scope_chain` and final `args` sent to `invoke(\"dispatch_command\", …)`. Confirm `perspective_id` is reachable in scope when the user clicks (the `CommandScopeProvider moniker=\"perspective:p1\"` is established by `ScopedPerspectiveTab` at `perspective-tab-bar.tsx:648`).\n-- [ ] **Backend, in `FocusFilterCmd::execute`** (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`): `tracing::info!` the resolved `perspective_id` and the returned envelope. Verify `resolve_and_persist_perspective_id` returns Some, not an error.\n-- [ ] **Backend, in `handle_focus_filter`** (`kanban-app/src/commands.rs:1717`): log when the branch fires and the emitted payload. Verify the `result.get(\"FocusFilter\")` match succeeds against the JSON the command actually returned.\n-- [ ] **Frontend, in `<FilterEditorBody>`'s `listen` callback** (`filter-editor.tsx:422`): log every received `ui.focus.filter` event AND the current `perspectiveId` it's being compared against. The most likely silent failure is an id mismatch — e.g. the active perspective changed between click and event delivery, or the resolver picked a different id than the React subscriber holds.\n-- [ ] **Frontend, in `<FilterEditorBody>`'s focus path**: if the id matches but `innerRef.current?.focus()` is no-op, log `innerRef.current` and the underlying CM6 `EditorView`. Note that the legacy path was synchronous (in the user-gesture stack); the new path is fully async after a Tauri round-trip. Some browsers refuse `.focus()` from non-gesture contexts — verify whether CM6's `view.focus()` works here.\n+- [ ] **Click site — make the button dispatch `nav.focus`.** The cleanest implementation: in `<RegistryTabButtons>` (`perspective-tab-bar.tsx:387`), wrap the `<CommandButton>` for `perspective.filter.focus` in a small adapter that overrides the dispatch to issue `nav.focus({ args: { fq: <filter_editor FQM> } })` instead of the raw command id. The FQM is composed the same way `FilterEditorDrillOutWiring` does it — via `useFullyQualifiedMoniker()` inside the `filter_editor:${id}` scope, OR resolved at the tab level by composing the known segments (`window` / scope chain up to `filter_editor:${perspectiveId}`). Reuse whatever the rest of the spatial system uses; do not invent a new composition.\n+- [ ] **Delete the parallel channel — backend.** Remove `FocusFilterCmd` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:823-858`). Remove its registration from `swissarmyhammer-kanban/src/commands/mod.rs`. The YAML entry `perspective.filter.focus` in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` no longer needs a backend `execute` — but it still drives the tab-button rendering (`tab_button: { icon: filter }`) and the `isActive` highlight derivation. Decide whether to: (a) keep the YAML entry as a UI-only declaration with a frontend-resolved `execute` (preferred — keeps the registry as the source of truth for what renders) or (b) remove the entry entirely and inline a hand-rolled `<CommandButton>` for the filter slot. Pick (a) unless there's a concrete reason not to.\n+- [ ] **Delete the parallel channel — Tauri glue.** Remove `handle_focus_filter` (`kanban-app/src/commands.rs:1717-1723`) and its call site in `handle_ui_trigger_results` (`kanban-app/src/commands.rs:1663`).\n+- [ ] **Delete the parallel channel — frontend.** Remove the `listen(\"ui.focus.filter\", …)` subscription in `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:402-450`) and the imperative `innerRef.current?.focus()` it drives. CM6 focusing happens via the existing `nav.drillIn` flow on the `filter_editor:${id}` scope, which is the same path that already lights up when the user arrow-navigates to the formula bar.\n+- [ ] **Update the tests.** Replace test #5 in `perspective-tab-bar.filter-migration.test.tsx` (`filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`) with a test that:\n+  - Clicks the Filter `<CommandButton>`.\n+  - Asserts the dispatched command is `nav.focus` with `args.fq` equal to the filter editor's composed FQM (or, equivalently, that the spatial-focus kernel ends up holding the `filter_editor:<id>` moniker).\n+  - Asserts the existing `nav.drillIn` flow then puts editing focus on `.cm-content`.\n+- [ ] **Update the YAML doc.** The YAML comment in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` describes the old `FocusFilter`/`ui.focus.filter` mechanism. Replace it with a one-paragraph note that the click site dispatches `nav.focus({ fq: filter_editor:<id> })` and points at `nav.yaml` / `spatial-focus-context.tsx` for the focus primitive itself.\n\n-### Likely root-cause candidates (in order)\n+### Why this is mandatory\n\n-1. **Lost user-gesture window.** The legacy `<FilterFocusButton>` focused CM6 synchronously inside the click handler. The new path goes click → Tauri IPC → Rust → Tauri event → React listener → `.focus()`. By the time `.focus()` is called, the gesture is gone. If WebKit refuses, the focus is silently dropped. Mitigation: focus the editor optimistically inside `CommandButton.handlePress` for `perspective.filter.focus` specifically (a UI hint, not a state mutation), keep the broadcast for cross-window parity. Confirm with the instrumentation first — don't apply this fix blindly.\n-2. **`perspective_id` resolves to a different id than the editor's prop.** `resolve_and_persist_perspective_id` falls back through arg → scope-chain → UIState.active → first-perspective-for-view-kind. If the click site's scope_chain doesn't yield `perspective:<id>`, the resolver may pick a different perspective than the one whose editor the user expects. The editor's id-equality guard would then drop the event.\n-3. **The `FocusFilter` envelope is mangled by the dispatcher.** Check that `result.get(\"FocusFilter\")` is `Some(...)` at the point of `app.emit(\"ui.focus.filter\", focus_filter)` — if the command result is wrapped (e.g. `{ result: { FocusFilter: ... } }`) by another layer added in this refactor, the match fails and the emit never happens.\n-4. **Listener race on perspective switch.** `<FilterFormulaBar>` remounts with `key={perspectiveId}` on perspective change. If the user clicks before the new editor's `listen` promise resolves, the broadcast lands without a listener. Verify the `listen` subscription is in place by the time the user clicks.\n+- **The architecture rule from `01KR7CDEFWWVF4WH0BCHE8Y21J` is non-negotiable.** Every focus claim flows through `nav.focus`. The parallel `FocusFilter`/`ui.focus.filter` channel violates this and would have to be re-built (telemetry, animation, scroll-on-focus) every time someone added a cross-cutting concern.\n+- **The current channel is broken because it bypasses the kernel.** The kernel doesn't know focus moved to `filter_editor:<id>`, so its model of \"where is focus now?\" goes out of sync with the DOM. Even if we patch the immediate CM6 `.focus()` bug, spatial arrow-nav from the formula bar wouldn't know it's there.\n+- **`nav.focus` already works.** Jump-to and arrow nav both prove the path is live end-to-end. We're not building anything new — we're routing the filter button through the path that already works.\n\n ### Acceptance Criteria\n\n-- [ ] Clicking the Filter tab button focuses the formula bar CM6 editor in the live app. Verified by a new automated end-to-end test (see Tests) — NOT by manual click-through.\n-- [ ] The fix preserves the command-driven dispatch path: palette invocation of `perspective.filter.focus` AND keybinding invocation (when added) must also focus the editor. Do not regress those paths by short-circuiting in the click handler only.\n-- [ ] The new end-to-end test asserts the **DOM activeElement** ends up on the `.cm-content` of the formula bar after dispatching `perspective.filter.focus` through the production command pipeline — not by manually invoking a mocked `listen` callback.\n-- [ ] Implementation notes capture which stage was silently dropping the signal (instrumentation finding) and why the chosen fix addresses that specific stage. Don't ship a fix that \"seems to work\" without identifying the broken hop.\n-- [ ] All existing tests in `perspective-tab-bar.filter-migration.test.tsx`, `perspective-tab-bar.registry-driven.test.tsx`, and `perspective-tab-bar.test.tsx` still pass.\n+- [ ] Clicking the Filter tab button moves spatial-nav focus to `filter_editor:<id>`. Verified by querying the spatial-focus store after the click.\n+- [ ] `nav.drillIn` (Enter) on that scope then puts DOM `activeElement` on the formula bar's `.cm-content`. (Same mechanism the spatial system already uses elsewhere — do NOT add a one-off `.focus()` call.)\n+- [ ] If the UX requires the click to land directly in editing mode (skip the explicit drill-in), the click handler dispatches `nav.focus` AND `nav.drillIn` as a two-step — both auditable, both through the registered command path. Do not short-circuit by calling `view.focus()` from the click handler.\n+- [ ] `FocusFilterCmd`, `handle_focus_filter`, and the `ui.focus.filter` `listen` subscription are deleted. `git grep -i 'FocusFilter\\|ui\\.focus\\.filter'` returns zero hits in `kanban-app/`, `swissarmyhammer-kanban/`, and the YAML.\n+- [ ] Palette invocation of `perspective.filter.focus` and (future) keybinding invocation produce the same focus outcome as the tab-button click — i.e. they all funnel through `nav.focus`.\n+- [ ] The existing `focus_filter_command_*` Rust tests in `perspective_commands.rs` are either updated or deleted alongside `FocusFilterCmd`. No tests pin the deleted parallel-channel behaviour.\n\n ### Tests\n\n-- [ ] **New end-to-end test** in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` (new `describe` block: `\"filter button — live dispatch round-trip\"`): render `<PerspectiveTabBar>` with a real `<FilterFormulaBar>` mounted (NOT just a standalone `<FilterEditor>`). Mock `invoke(\"dispatch_command\", …)` so it actually drives `handle_focus_filter`'s behaviour — i.e. when the mock sees `perspective.filter.focus`, it synchronously fires the `ui.focus.filter` listeners with `{ perspective_id: \"<host id>\" }` (same payload shape the Rust dispatcher produces). Click the Filter `<CommandButton>` via `fireEvent.click`. Assert `document.activeElement` is the formula bar's `.cm-content` after the click resolves. This is the test that would have caught the current regression.\n-- [ ] **Sibling-perspective ignore test**: same harness, but the mocked dispatcher fires with a non-matching `perspective_id`. Assert `document.activeElement` is NOT the `.cm-content` — i.e. the id-equality guard still blocks cross-perspective broadcasts.\n-- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar.filter-migration` — green.\n-- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — all sibling files green.\n-- [ ] **Run** `cargo test -p swissarmyhammer-kanban focus_filter` — existing `focus_filter_command_*` tests in `perspective_commands.rs` still green.\n-- [ ] **Run** `cargo test -p kanban-app` — Tauri-side handler tests (if any cover `handle_focus_filter`) green; if no coverage exists for `handle_focus_filter`, add a minimal unit test that constructs the `{\"FocusFilter\":{...}}` envelope and verifies the handler attempts to emit on `ui.focus.filter`.\n+- [ ] **New test in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx`**: render `<PerspectiveTabBar>` inside `<SpatialFocusProvider>` and `<EntityFocusProvider>`. Click the Filter `<CommandButton>`. Assert the spatial-focus store's current FQM ends in `filter_editor:<active-perspective-id>`. Then synthesise a `nav.drillIn` (or rely on the click handler issuing it) and assert `document.activeElement` is the formula bar's `.cm-content`.\n+- [ ] **Sibling-perspective test**: switch active perspective after rendering, click Filter, assert focus lands on the NEW active perspective's `filter_editor` moniker — not on the previously active one. (This catches the id-resolution bug that the deleted Tauri-channel path was also vulnerable to.)\n+- [ ] **Delete `filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`** and the helpers that simulate the Tauri `listen` callback. It pins behaviour we're removing.\n+- [ ] **Update `focus_filter_command_*` Rust tests** in `swissarmyhammer-kanban/src/commands/perspective_commands.rs:2180-2252` — delete them along with `FocusFilterCmd`. If any Rust test belongs in the new world (e.g. the YAML registration still smoke-tested by `composed_commands_registry.rs`), update its assertions to match.\n+- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — green.\n+- [ ] **Run** `pnpm -C kanban-app/ui test filter-editor` — green; the deleted `listen` subscription does not break any sibling test.\n+- [ ] **Run** `cargo test -p swissarmyhammer-kanban` — green.\n+- [ ] **Run** `cargo test -p kanban-app` — green; nothing references `handle_focus_filter` after the deletion.\n\n ### Workflow\n\n-- Use `/tdd` — write the failing end-to-end test FIRST (it should fail in the current codebase by failing to move `activeElement` to `.cm-content`), then instrument, then fix.\n-- **Do not revert to the local-callback path.** Hooking the Filter button through the command pipeline is mandatory.\n-- **Stay on task.** Do not refactor `<CommandButton>`, the dispatcher, or the spatial-nav wiring beyond what's needed to fix this specific regression.\n+- Use `/tdd` — write the failing test (Filter click → spatial-focus store ends on `filter_editor:<id>`) FIRST. Watch it fail in the current codebase, then route the click through `nav.focus`, then delete the parallel channel, then watch the test pass.\n+- **Delete, don't deprecate.** No shim left behind that emits `ui.focus.filter` for backwards compatibility. The channel doesn't exist after this task.\n+- **Stay on task.** Do not touch the spatial-nav kernel, the `nav.focus` registration, or `<FilterFormulaBarFocusable>`. The fix is at the click site + the deletion of the parallel channel.\n\n-#bug #command-driven-ui #frontend #kanban-app #perspectives\n\\ No newline at end of file\n+#bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,64 +1,58 @@\n ## What\n\n-The `command-driven-ui` migration of the Filter tab button (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for `perspective.filter.focus`. The unit tests pass — but **clicking the button no longer focuses the formula bar in the live app**.\n+The `command-driven-ui` migration of the Filter tab affordance (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for the new no-arg `perspective.filter.focus` command. The unit tests in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` pass — but **the actual end-user behaviour is broken**: clicking the Filter icon on a perspective tab no longer focuses the formula bar's CM6 editor.\n\n-Root cause is architectural, not a wiring glitch: the migration invented a **parallel focus channel** instead of using the existing `nav.focus` primitive.\n+Hooking the button through the command pipeline is **mandatory** for parity with palette / keybindings / multi-window dispatch — DO NOT revert to the local-callback path. Instead, debug the live wire and make it work end-to-end.\n\n-Today's wiring (parallel channel — DELETE THIS):\n+### Current wiring (paper-correct, broken in practice)\n\n-1. Click `<CommandButton>` → dispatch `perspective.filter.focus`.\n-2. `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`) returns a `{\"FocusFilter\": {\"perspective_id\": ...}}` marker envelope.\n-3. Tauri dispatcher's `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) catches the envelope and emits a Tauri event `ui.focus.filter`.\n-4. `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:419-450`) `listen`s for that event, id-checks the payload, and calls `innerRef.current?.focus()` on the CM6 contenteditable.\n+1. Click `<CommandButton>` in `kanban-app/ui/src/components/command-button.tsx` (the `perspective.filter.focus` instance rendered by `RegistryTabButtons` in `perspective-tab-bar.tsx:387`).\n+2. `handlePress` → `dispatch(\"perspective.filter.focus\", { args: {} })` via `useDispatchCommand` (no popover; every param resolves from scope).\n+3. Tauri `dispatch_command` runs `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`), returning `{\"FocusFilter\": {\"perspective_id\": \"<id>\"}}`.\n+4. Dispatcher's `apply_post_command_side_effects` → `handle_ui_trigger_results` → `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) emits the Tauri event `ui.focus.filter` carrying `{ perspective_id }`.\n+5. `<FilterEditorBody>` in `kanban-app/ui/src/components/filter-editor.tsx:419-450` listens for `ui.focus.filter`, compares `payload.perspective_id` against its own `perspectiveId` prop, and calls `innerRef.current?.focus()` (the CM6 contenteditable).\n\n-The architecture per `01KR7CDEFWWVF4WH0BCHE8Y21J` is explicit: **every focus claim flows through `nav.focus({ args: { fq } })`** — registered once at `spatial-focus-context.tsx:381` (`buildSpatialNavFocusCommand`), which dispatches `spatial_focus` IPC, runs the cross-cutting concerns (telemetry, animations, scroll-on-focus), and lets the kernel emit `focus-changed` back to React. Components do not call `spatial.focus(fq)`, `setFocus(fq)`, or imperative `.focus()` directly — they dispatch `nav.focus`.\n+The migration test (#5 in `perspective-tab-bar.filter-migration.test.tsx`) simulates step 5 directly by invoking a mocked `listen` callback — it does NOT exercise steps 2–4. So the test stays green even though something between the click and the eventual focus is silently dropping the signal.\n\n-The filter editor already has a moniker the spatial-nav system knows about: `<FilterFormulaBarFocusable>` wraps the formula bar in `<FocusScope moniker={asSegment(`filter_editor:${perspectiveId}`)}>` (`perspective-tab-bar.tsx:794`). When the kernel claims focus on `filter_editor:<id>`, the focus indicator paints and the `nav.drillIn` Enter handler at that scope already drives CM6 to take editing focus (the same path the rest of the spatial-nav system uses). And `FilterEditorDrillOutWiring` (`perspective-tab-bar.tsx:814-844`) already proves the pattern — it dispatches `nav.focus({ args: { fq: filterFq } })` to drill back out to the filter editor scope.\n+### Investigation plan — instrument first, fix second\n\n-So the fix is: **dispatch `nav.focus` with the filter editor's FQM as the implementation of the filter button action.** Delete the parallel channel.\n+Do not theorise. Add tracing at each hop and capture which one is silent:\n\n-### Required changes\n+- [ ] **Frontend, in `CommandButton.handlePress`** (`kanban-app/ui/src/components/command-button.tsx:132`): `console.debug(\"[CommandButton] press\", command.id, surfaceId)` so we know the click actually reached the dispatcher.\n+- [ ] **Frontend, in `useDispatchCommand`'s dispatch site**: log the resolved `scope_chain` and final `args` sent to `invoke(\"dispatch_command\", …)`. Confirm `perspective_id` is reachable in scope when the user clicks (the `CommandScopeProvider moniker=\"perspective:p1\"` is established by `ScopedPerspectiveTab` at `perspective-tab-bar.tsx:648`).\n+- [ ] **Backend, in `FocusFilterCmd::execute`** (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`): `tracing::info!` the resolved `perspective_id` and the returned envelope. Verify `resolve_and_persist_perspective_id` returns Some, not an error.\n+- [ ] **Backend, in `handle_focus_filter`** (`kanban-app/src/commands.rs:1717`): log when the branch fires and the emitted payload. Verify the `result.get(\"FocusFilter\")` match succeeds against the JSON the command actually returned.\n+- [ ] **Frontend, in `<FilterEditorBody>`'s `listen` callback** (`filter-editor.tsx:422`): log every received `ui.focus.filter` event AND the current `perspectiveId` it's being compared against. The most likely silent failure is an id mismatch — e.g. the active perspective changed between click and event delivery, or the resolver picked a different id than the React subscriber holds.\n+- [ ] **Frontend, in `<FilterEditorBody>`'s focus path**: if the id matches but `innerRef.current?.focus()` is no-op, log `innerRef.current` and the underlying CM6 `EditorView`. Note that the legacy path was synchronous (in the user-gesture stack); the new path is fully async after a Tauri round-trip. Some browsers refuse `.focus()` from non-gesture contexts — verify whether CM6's `view.focus()` works here.\n\n-- [ ] **Click site — make the button dispatch `nav.focus`.** The cleanest implementation: in `<RegistryTabButtons>` (`perspective-tab-bar.tsx:387`), wrap the `<CommandButton>` for `perspective.filter.focus` in a small adapter that overrides the dispatch to issue `nav.focus({ args: { fq: <filter_editor FQM> } })` instead of the raw command id. The FQM is composed the same way `FilterEditorDrillOutWiring` does it — via `useFullyQualifiedMoniker()` inside the `filter_editor:${id}` scope, OR resolved at the tab level by composing the known segments (`window` / scope chain up to `filter_editor:${perspectiveId}`). Reuse whatever the rest of the spatial system uses; do not invent a new composition.\n-- [ ] **Delete the parallel channel — backend.** Remove `FocusFilterCmd` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:823-858`). Remove its registration from `swissarmyhammer-kanban/src/commands/mod.rs`. The YAML entry `perspective.filter.focus` in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` no longer needs a backend `execute` — but it still drives the tab-button rendering (`tab_button: { icon: filter }`) and the `isActive` highlight derivation. Decide whether to: (a) keep the YAML entry as a UI-only declaration with a frontend-resolved `execute` (preferred — keeps the registry as the source of truth for what renders) or (b) remove the entry entirely and inline a hand-rolled `<CommandButton>` for the filter slot. Pick (a) unless there's a concrete reason not to.\n-- [ ] **Delete the parallel channel — Tauri glue.** Remove `handle_focus_filter` (`kanban-app/src/commands.rs:1717-1723`) and its call site in `handle_ui_trigger_results` (`kanban-app/src/commands.rs:1663`).\n-- [ ] **Delete the parallel channel — frontend.** Remove the `listen(\"ui.focus.filter\", …)` subscription in `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:402-450`) and the imperative `innerRef.current?.focus()` it drives. CM6 focusing happens via the existing `nav.drillIn` flow on the `filter_editor:${id}` scope, which is the same path that already lights up when the user arrow-navigates to the formula bar.\n-- [ ] **Update the tests.** Replace test #5 in `perspective-tab-bar.filter-migration.test.tsx` (`filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`) with a test that:\n-  - Clicks the Filter `<CommandButton>`.\n-  - Asserts the dispatched command is `nav.focus` with `args.fq` equal to the filter editor's composed FQM (or, equivalently, that the spatial-focus kernel ends up holding the `filter_editor:<id>` moniker).\n-  - Asserts the existing `nav.drillIn` flow then puts editing focus on `.cm-content`.\n-- [ ] **Update the YAML doc.** The YAML comment in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` describes the old `FocusFilter`/`ui.focus.filter` mechanism. Replace it with a one-paragraph note that the click site dispatches `nav.focus({ fq: filter_editor:<id> })` and points at `nav.yaml` / `spatial-focus-context.tsx` for the focus primitive itself.\n+### Likely root-cause candidates (in order)\n\n-### Why this is mandatory\n-\n-- **The architecture rule from `01KR7CDEFWWVF4WH0BCHE8Y21J` is non-negotiable.** Every focus claim flows through `nav.focus`. The parallel `FocusFilter`/`ui.focus.filter` channel violates this and would have to be re-built (telemetry, animation, scroll-on-focus) every time someone added a cross-cutting concern.\n-- **The current channel is broken because it bypasses the kernel.** The kernel doesn't know focus moved to `filter_editor:<id>`, so its model of \"where is focus now?\" goes out of sync with the DOM. Even if we patch the immediate CM6 `.focus()` bug, spatial arrow-nav from the formula bar wouldn't know it's there.\n-- **`nav.focus` already works.** Jump-to and arrow nav both prove the path is live end-to-end. We're not building anything new — we're routing the filter button through the path that already works.\n+1. **Lost user-gesture window.** The legacy `<FilterFocusButton>` focused CM6 synchronously inside the click handler. The new path goes click → Tauri IPC → Rust → Tauri event → React listener → `.focus()`. By the time `.focus()` is called, the gesture is gone. If WebKit refuses, the focus is silently dropped. Mitigation: focus the editor optimistically inside `CommandButton.handlePress` for `perspective.filter.focus` specifically (a UI hint, not a state mutation), keep the broadcast for cross-window parity. Confirm with the instrumentation first — don't apply this fix blindly.\n+2. **`perspective_id` resolves to a different id than the editor's prop.** `resolve_and_persist_perspective_id` falls back through arg → scope-chain → UIState.active → first-perspective-for-view-kind. If the click site's scope_chain doesn't yield `perspective:<id>`, the resolver may pick a different perspective than the one whose editor the user expects. The editor's id-equality guard would then drop the event.\n+3. **The `FocusFilter` envelope is mangled by the dispatcher.** Check that `result.get(\"FocusFilter\")` is `Some(...)` at the point of `app.emit(\"ui.focus.filter\", focus_filter)` — if the command result is wrapped (e.g. `{ result: { FocusFilter: ... } }`) by another layer added in this refactor, the match fails and the emit never happens.\n+4. **Listener race on perspective switch.** `<FilterFormulaBar>` remounts with `key={perspectiveId}` on perspective change. If the user clicks before the new editor's `listen` promise resolves, the broadcast lands without a listener. Verify the `listen` subscription is in place by the time the user clicks.\n\n ### Acceptance Criteria\n\n-- [ ] Clicking the Filter tab button moves spatial-nav focus to `filter_editor:<id>`. Verified by querying the spatial-focus store after the click.\n-- [ ] `nav.drillIn` (Enter) on that scope then puts DOM `activeElement` on the formula bar's `.cm-content`. (Same mechanism the spatial system already uses elsewhere — do NOT add a one-off `.focus()` call.)\n-- [ ] If the UX requires the click to land directly in editing mode (skip the explicit drill-in), the click handler dispatches `nav.focus` AND `nav.drillIn` as a two-step — both auditable, both through the registered command path. Do not short-circuit by calling `view.focus()` from the click handler.\n-- [ ] `FocusFilterCmd`, `handle_focus_filter`, and the `ui.focus.filter` `listen` subscription are deleted. `git grep -i 'FocusFilter\\|ui\\.focus\\.filter'` returns zero hits in `kanban-app/`, `swissarmyhammer-kanban/`, and the YAML.\n-- [ ] Palette invocation of `perspective.filter.focus` and (future) keybinding invocation produce the same focus outcome as the tab-button click — i.e. they all funnel through `nav.focus`.\n-- [ ] The existing `focus_filter_command_*` Rust tests in `perspective_commands.rs` are either updated or deleted alongside `FocusFilterCmd`. No tests pin the deleted parallel-channel behaviour.\n+- [ ] Clicking the Filter tab button focuses the formula bar CM6 editor in the live app. Verified by a new automated end-to-end test (see Tests) — NOT by manual click-through.\n+- [ ] The fix preserves the command-driven dispatch path: palette invocation of `perspective.filter.focus` AND keybinding invocation (when added) must also focus the editor. Do not regress those paths by short-circuiting in the click handler only.\n+- [ ] The new end-to-end test asserts the **DOM activeElement** ends up on the `.cm-content` of the formula bar after dispatching `perspective.filter.focus` through the production command pipeline — not by manually invoking a mocked `listen` callback.\n+- [ ] Implementation notes capture which stage was silently dropping the signal (instrumentation finding) and why the chosen fix addresses that specific stage. Don't ship a fix that \"seems to work\" without identifying the broken hop.\n+- [ ] All existing tests in `perspective-tab-bar.filter-migration.test.tsx`, `perspective-tab-bar.registry-driven.test.tsx`, and `perspective-tab-bar.test.tsx` still pass.\n\n ### Tests\n\n-- [ ] **New test in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx`**: render `<PerspectiveTabBar>` inside `<SpatialFocusProvider>` and `<EntityFocusProvider>`. Click the Filter `<CommandButton>`. Assert the spatial-focus store's current FQM ends in `filter_editor:<active-perspective-id>`. Then synthesise a `nav.drillIn` (or rely on the click handler issuing it) and assert `document.activeElement` is the formula bar's `.cm-content`.\n-- [ ] **Sibling-perspective test**: switch active perspective after rendering, click Filter, assert focus lands on the NEW active perspective's `filter_editor` moniker — not on the previously active one. (This catches the id-resolution bug that the deleted Tauri-channel path was also vulnerable to.)\n-- [ ] **Delete `filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`** and the helpers that simulate the Tauri `listen` callback. It pins behaviour we're removing.\n-- [ ] **Update `focus_filter_command_*` Rust tests** in `swissarmyhammer-kanban/src/commands/perspective_commands.rs:2180-2252` — delete them along with `FocusFilterCmd`. If any Rust test belongs in the new world (e.g. the YAML registration still smoke-tested by `composed_commands_registry.rs`), update its assertions to match.\n-- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — green.\n-- [ ] **Run** `pnpm -C kanban-app/ui test filter-editor` — green; the deleted `listen` subscription does not break any sibling test.\n-- [ ] **Run** `cargo test -p swissarmyhammer-kanban` — green.\n-- [ ] **Run** `cargo test -p kanban-app` — green; nothing references `handle_focus_filter` after the deletion.\n+- [ ] **New end-to-end test** in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx` (new `describe` block: `\"filter button — live dispatch round-trip\"`): render `<PerspectiveTabBar>` with a real `<FilterFormulaBar>` mounted (NOT just a standalone `<FilterEditor>`). Mock `invoke(\"dispatch_command\", …)` so it actually drives `handle_focus_filter`'s behaviour — i.e. when the mock sees `perspective.filter.focus`, it synchronously fires the `ui.focus.filter` listeners with `{ perspective_id: \"<host id>\" }` (same payload shape the Rust dispatcher produces). Click the Filter `<CommandButton>` via `fireEvent.click`. Assert `document.activeElement` is the formula bar's `.cm-content` after the click resolves. This is the test that would have caught the current regression.\n+- [ ] **Sibling-perspective ignore test**: same harness, but the mocked dispatcher fires with a non-matching `perspective_id`. Assert `document.activeElement` is NOT the `.cm-content` — i.e. the id-equality guard still blocks cross-perspective broadcasts.\n+- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar.filter-migration` — green.\n+- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — all sibling files green.\n+- [ ] **Run** `cargo test -p swissarmyhammer-kanban focus_filter` — existing `focus_filter_command_*` tests in `perspective_commands.rs` still green.\n+- [ ] **Run** `cargo test -p kanban-app` — Tauri-side handler tests (if any cover `handle_focus_filter`) green; if no coverage exists for `handle_focus_filter`, add a minimal unit test that constructs the `{\"FocusFilter\":{...}}` envelope and verifies the handler attempts to emit on `ui.focus.filter`.\n\n ### Workflow\n\n-- Use `/tdd` — write the failing test (Filter click → spatial-focus store ends on `filter_editor:<id>`) FIRST. Watch it fail in the current codebase, then route the click through `nav.focus`, then delete the parallel channel, then watch the test pass.\n-- **Delete, don't deprecate.** No shim left behind that emits `ui.focus.filter` for backwards compatibility. The channel doesn't exist after this task.\n-- **Stay on task.** Do not touch the spatial-nav kernel, the `nav.focus` registration, or `<FilterFormulaBarFocusable>`. The fix is at the click site + the deletion of the parallel channel.\n+- Use `/tdd` — write the failing end-to-end test FIRST (it should fail in the current codebase by failing to move `activeElement` to `.cm-content`), then instrument, then fix.\n+- **Do not revert to the local-callback path.** Hooking the Filter button through the command pipeline is mandatory.\n+- **Stay on task.** Do not refactor `<CommandButton>`, the dispatcher, or the spatial-nav wiring beyond what's needed to fix this specific regression.\n\n-#bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n\\ No newline at end of file\n+#bug #command-driven-ui #frontend #kanban-app #perspectives\n\\ No newline at end of file\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Filter tab button no longer focuses the formula bar in the live app\n\\ No newline at end of file\n+Filter tab button must focus via nav.focus, not a parallel Tauri channel\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Filter tab button must focus via nav.focus, not a parallel Tauri channel\n\\ No newline at end of file\n+Filter tab button no longer focuses the formula bar in the live app\n\\ No newline at end of file\n"}]]}
{"id":"01KRH109FF3HHZRRMV2E4P0X13","timestamp":"2026-05-13T15:59:04.175551Z","op":"update","item_id":"01KRGZY33P99J7CGG0XRQGZ352","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: aa80\n+position_column: doing\n+position_ordinal: '8280'\n title: Filter tab button must focus via nav.focus, not a parallel Tauri channel\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: todo\n+position_ordinal: aa80\n title: Filter tab button must focus via nav.focus, not a parallel Tauri channel\n ---\n ## What\n","transaction_id":null}
{"id":"01KRH109FHYTGJ3WZK580DZTVJ","timestamp":"2026-05-13T15:59:04.177073Z","entity_type":"task","entity_id":"01KRGZY33P99J7CGG0XRQGZ352","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-aa80\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+aa80\n\\ No newline at end of file\n"}]]}
{"id":"01KRH20ST9VR2ASKAHMBC76QSV","timestamp":"2026-05-13T16:16:49.481241Z","op":"update","item_id":"01KRGZY33P99J7CGG0XRQGZ352","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: review\n+position_ordinal: '80'\n title: Filter tab button must focus via nav.focus, not a parallel Tauri channel\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: doing\n+position_ordinal: '8280'\n title: Filter tab button must focus via nav.focus, not a parallel Tauri channel\n ---\n ## What\n","transaction_id":null}
{"id":"01KRH20ST9AKFPVAFN85YE037N","timestamp":"2026-05-13T16:16:49.481993Z","entity_type":"task","entity_id":"01KRGZY33P99J7CGG0XRQGZ352","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n"}]]}
{"id":"01KRH21XCT53VHW1MVN9ZYAVQ9","timestamp":"2026-05-13T16:17:25.914606Z","op":"update","item_id":"01KRGZY33P99J7CGG0XRQGZ352","forward_patch":"--- original\n+++ modified\n@@ -9,63 +9,73 @@\n\n The `command-driven-ui` migration of the Filter tab button (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for `perspective.filter.focus`. The unit tests pass — but **clicking the button no longer focuses the formula bar in the live app**.\n\n-Root cause is architectural, not a wiring glitch: the migration invented a **parallel focus channel** instead of using the existing `nav.focus` primitive.\n+Root cause was architectural: the migration invented a **parallel focus channel** instead of using the existing `nav.focus` primitive.\n\n-Today's wiring (parallel channel — DELETE THIS):\n+Pre-fix wiring (the parallel channel — deleted by this card):\n\n 1. Click `<CommandButton>` → dispatch `perspective.filter.focus`.\n-2. `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`) returns a `{\"FocusFilter\": {\"perspective_id\": ...}}` marker envelope.\n-3. Tauri dispatcher's `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) catches the envelope and emits a Tauri event `ui.focus.filter`.\n-4. `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:419-450`) `listen`s for that event, id-checks the payload, and calls `innerRef.current?.focus()` on the CM6 contenteditable.\n+2. `FocusFilterCmd::execute` returns a `{\"FocusFilter\": {\"perspective_id\": ...}}` marker envelope.\n+3. Tauri dispatcher's `handle_focus_filter` catches the envelope and emits a Tauri event `ui.focus.filter`.\n+4. `<FilterEditorBody>` `listen`s for that event, id-checks the payload, and calls `innerRef.current?.focus()` on the CM6 contenteditable.\n+\n+The architecture per `01KR7CDEFWWVF4WH0BCHE8Y21J` is explicit: **every focus claim flows through `nav.focus({ args: { fq } })`**. Components do not call `spatial.focus(fq)`, `setFocus(fq)`, or imperative `.focus()` directly — they dispatch `nav.focus`.\n\n-The architecture per `01KR7CDEFWWVF4WH0BCHE8Y21J` is explicit: **every focus claim flows through `nav.focus({ args: { fq } })`** — registered once at `spatial-focus-context.tsx:381` (`buildSpatialNavFocusCommand`), which dispatches `spatial_focus` IPC, runs the cross-cutting concerns (telemetry, animations, scroll-on-focus), and lets the kernel emit `focus-changed` back to React. Components do not call `spatial.focus(fq)`, `setFocus(fq)`, or imperative `.focus()` directly — they dispatch `nav.focus`.\n+## Implementation notes (post-merge)\n+\n+### Stage that was broken in the live app\n+\n+Stage 1 — the click site. The `<CommandButton>` dispatched `perspective.filter.focus` through the backend command pipeline. The backend's `FocusFilterCmd` returned a `FocusFilter` marker envelope. The Tauri dispatcher's `handle_focus_filter` then emitted `ui.focus.filter`. `<FilterEditorBody>` subscribed to it and called `focus()` on the CM6 editor. The reason this broke in production while passing tests: the kernel's \"where is focus now?\" model was never updated — the `.focus()` call only moved DOM focus, leaving spatial-nav unaware. Even if all the test mocks lined up, the production path went around the kernel.\n+\n+### What was changed\n+\n+**Frontend (`kanban-app/ui/src/components/perspective-tab-bar.tsx`):**\n+\n+- Added `FilterEditorFqContext` — a context carrying a `MutableRefObject<FullyQualifiedMoniker | null>`. Provided at the `<PerspectiveTabBar>` level so both the tab buttons (writer's sibling subtree) and the formula bar (writer subtree) can share it.\n+- Added `<FilterFocusCommandButton>` adapter that mirrors `<CommandButton>`'s render (icon via `commandIconFor`, `text-primary` highlight, `${surface}.${command.id}:${surfaceId}` moniker) but overrides `onPress` to dispatch `nav.focus({ args: { fq } })` against the captured FQM. Used only for `perspective.filter.focus`; all other tab commands still go through `<CommandButton>`.\n+- `<RegistryTabButtons>` now special-cases the `perspective.filter.focus` command id and routes it to `<FilterFocusCommandButton>`. All other commands continue to use `<CommandButton>` unchanged.\n+- `<FilterEditorDrillOutWiring>` (already inside the `filter_editor:${id}` `<FocusScope>`) now writes its composed FQM into the context ref via `useEffect`, with an identity-check cleanup so a sibling instance's value can't be clobbered.\n+- `<PerspectiveTabBar>` instantiates the ref via `useRef<FullyQualifiedMoniker | null>(null)` and wraps everything in `<FilterEditorFqContext.Provider>`.\n\n-The filter editor already has a moniker the spatial-nav system knows about: `<FilterFormulaBarFocusable>` wraps the formula bar in `<FocusScope moniker={asSegment(`filter_editor:${perspectiveId}`)}>` (`perspective-tab-bar.tsx:794`). When the kernel claims focus on `filter_editor:<id>`, the focus indicator paints and the `nav.drillIn` Enter handler at that scope already drives CM6 to take editing focus (the same path the rest of the spatial-nav system uses). And `FilterEditorDrillOutWiring` (`perspective-tab-bar.tsx:814-844`) already proves the pattern — it dispatches `nav.focus({ args: { fq: filterFq } })` to drill back out to the filter editor scope.\n+**Frontend (`kanban-app/ui/src/components/filter-editor.tsx`):**\n\n-So the fix is: **dispatch `nav.focus` with the filter editor's FQM as the implementation of the filter button action.** Delete the parallel channel.\n+- Deleted the `listen(\"ui.focus.filter\", …)` `useEffect`. Replaced the block with a routing-history comment.\n+- Dropped the now-unused `import { listen } from \"@tauri-apps/api/event\"`.\n\n-### Required changes\n+**Tauri glue (`kanban-app/src/commands.rs`):**\n\n-- [ ] **Click site — make the button dispatch `nav.focus`.** The cleanest implementation: in `<RegistryTabButtons>` (`perspective-tab-bar.tsx:387`), wrap the `<CommandButton>` for `perspective.filter.focus` in a small adapter that overrides the dispatch to issue `nav.focus({ args: { fq: <filter_editor FQM> } })` instead of the raw command id. The FQM is composed the same way `FilterEditorDrillOutWiring` does it — via `useFullyQualifiedMoniker()` inside the `filter_editor:${id}` scope, OR resolved at the tab level by composing the known segments (`window` / scope chain up to `filter_editor:${perspectiveId}`). Reuse whatever the rest of the spatial system uses; do not invent a new composition.\n-- [ ] **Delete the parallel channel — backend.** Remove `FocusFilterCmd` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:823-858`). Remove its registration from `swissarmyhammer-kanban/src/commands/mod.rs`. The YAML entry `perspective.filter.focus` in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` no longer needs a backend `execute` — but it still drives the tab-button rendering (`tab_button: { icon: filter }`) and the `isActive` highlight derivation. Decide whether to: (a) keep the YAML entry as a UI-only declaration with a frontend-resolved `execute` (preferred — keeps the registry as the source of truth for what renders) or (b) remove the entry entirely and inline a hand-rolled `<CommandButton>` for the filter slot. Pick (a) unless there's a concrete reason not to.\n-- [ ] **Delete the parallel channel — Tauri glue.** Remove `handle_focus_filter` (`kanban-app/src/commands.rs:1717-1723`) and its call site in `handle_ui_trigger_results` (`kanban-app/src/commands.rs:1663`).\n-- [ ] **Delete the parallel channel — frontend.** Remove the `listen(\"ui.focus.filter\", …)` subscription in `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:402-450`) and the imperative `innerRef.current?.focus()` it drives. CM6 focusing happens via the existing `nav.drillIn` flow on the `filter_editor:${id}` scope, which is the same path that already lights up when the user arrow-navigates to the formula bar.\n-- [ ] **Update the tests.** Replace test #5 in `perspective-tab-bar.filter-migration.test.tsx` (`filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`) with a test that:\n-  - Clicks the Filter `<CommandButton>`.\n-  - Asserts the dispatched command is `nav.focus` with `args.fq` equal to the filter editor's composed FQM (or, equivalently, that the spatial-focus kernel ends up holding the `filter_editor:<id>` moniker).\n-  - Asserts the existing `nav.drillIn` flow then puts editing focus on `.cm-content`.\n-- [ ] **Update the YAML doc.** The YAML comment in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` describes the old `FocusFilter`/`ui.focus.filter` mechanism. Replace it with a one-paragraph note that the click site dispatches `nav.focus({ fq: filter_editor:<id> })` and points at `nav.yaml` / `spatial-focus-context.tsx` for the focus primitive itself.\n+- Deleted `handle_focus_filter` and its call site in `handle_ui_trigger_results`.\n\n-### Why this is mandatory\n+**Backend command (`swissarmyhammer-kanban/src/commands/perspective_commands.rs`):**\n\n-- **The architecture rule from `01KR7CDEFWWVF4WH0BCHE8Y21J` is non-negotiable.** Every focus claim flows through `nav.focus`. The parallel `FocusFilter`/`ui.focus.filter` channel violates this and would have to be re-built (telemetry, animation, scroll-on-focus) every time someone added a cross-cutting concern.\n-- **The current channel is broken because it bypasses the kernel.** The kernel doesn't know focus moved to `filter_editor:<id>`, so its model of \"where is focus now?\" goes out of sync with the DOM. Even if we patch the immediate CM6 `.focus()` bug, spatial arrow-nav from the formula bar wouldn't know it's there.\n-- **`nav.focus` already works.** Jump-to and arrow nav both prove the path is live end-to-end. We're not building anything new — we're routing the filter button through the path that already works.\n+- Replaced `FocusFilterCmd::execute`'s `FocusFilter` marker envelope with a `Value::Null` no-op. The struct is kept (with updated doc comment) only to satisfy the `test_all_yaml_commands_have_rust_implementations` / `test_no_orphan_rust_commands_without_yaml` invariants in `commands/mod.rs`. The deleted `focus_filter_command_dispatches_focus_event` and `focus_filter_command_prefers_explicit_perspective_id_arg` tests have been replaced by a single `focus_filter_command_is_a_noop` test that pins the new no-op contract. The `focus_filter_command_is_always_available` test is kept (and re-justified — the tab-button slot needs it to emit on every active perspective).\n+- Updated the registration comment in `swissarmyhammer-kanban/src/commands/mod.rs` to describe the new no-op rationale.\n\n-### Acceptance Criteria\n+**YAML (`swissarmyhammer-kanban/builtin/commands/perspective.yaml`):**\n\n-- [ ] Clicking the Filter tab button moves spatial-nav focus to `filter_editor:<id>`. Verified by querying the spatial-focus store after the click.\n-- [ ] `nav.drillIn` (Enter) on that scope then puts DOM `activeElement` on the formula bar's `.cm-content`. (Same mechanism the spatial system already uses elsewhere — do NOT add a one-off `.focus()` call.)\n-- [ ] If the UX requires the click to land directly in editing mode (skip the explicit drill-in), the click handler dispatches `nav.focus` AND `nav.drillIn` as a two-step — both auditable, both through the registered command path. Do not short-circuit by calling `view.focus()` from the click handler.\n-- [ ] `FocusFilterCmd`, `handle_focus_filter`, and the `ui.focus.filter` `listen` subscription are deleted. `git grep -i 'FocusFilter\\|ui\\.focus\\.filter'` returns zero hits in `kanban-app/`, `swissarmyhammer-kanban/`, and the YAML.\n-- [ ] Palette invocation of `perspective.filter.focus` and (future) keybinding invocation produce the same focus outcome as the tab-button click — i.e. they all funnel through `nav.focus`.\n-- [ ] The existing `focus_filter_command_*` Rust tests in `perspective_commands.rs` are either updated or deleted alongside `FocusFilterCmd`. No tests pin the deleted parallel-channel behaviour.\n+- Rewrote the `perspective.filter.focus` entry's comment block to explain the new mechanism (click dispatches `nav.focus` against `filter_editor:${id}`) and to cross-reference `nav.yaml`, `spatial-focus-context.tsx`, and the `FilterFocusCommandButton` adapter. The YAML shape itself is unchanged (`scope: \"entity:perspective\"`, `tab_button: { icon: filter }`, `params`, `keys: {}`).\n\n ### Tests\n\n-- [ ] **New test in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx`**: render `<PerspectiveTabBar>` inside `<SpatialFocusProvider>` and `<EntityFocusProvider>`. Click the Filter `<CommandButton>`. Assert the spatial-focus store's current FQM ends in `filter_editor:<active-perspective-id>`. Then synthesise a `nav.drillIn` (or rely on the click handler issuing it) and assert `document.activeElement` is the formula bar's `.cm-content`.\n-- [ ] **Sibling-perspective test**: switch active perspective after rendering, click Filter, assert focus lands on the NEW active perspective's `filter_editor` moniker — not on the previously active one. (This catches the id-resolution bug that the deleted Tauri-channel path was also vulnerable to.)\n-- [ ] **Delete `filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`** and the helpers that simulate the Tauri `listen` callback. It pins behaviour we're removing.\n-- [ ] **Update `focus_filter_command_*` Rust tests** in `swissarmyhammer-kanban/src/commands/perspective_commands.rs:2180-2252` — delete them along with `FocusFilterCmd`. If any Rust test belongs in the new world (e.g. the YAML registration still smoke-tested by `composed_commands_registry.rs`), update its assertions to match.\n-- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — green.\n-- [ ] **Run** `pnpm -C kanban-app/ui test filter-editor` — green; the deleted `listen` subscription does not break any sibling test.\n-- [ ] **Run** `cargo test -p swissarmyhammer-kanban` — green.\n-- [ ] **Run** `cargo test -p kanban-app` — green; nothing references `handle_focus_filter` after the deletion.\n+- **New** `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx`:\n+  - `filter_button_click_dispatches_nav_focus_with_filter_editor_fq` — clicks the Filter button, asserts a `spatial_focus` IPC fires with `fq` ending in `filter_editor:p1`, and asserts the deleted `dispatch_command(perspective.filter.focus)` backend path is NOT hit.\n+  - `filter_button_click_targets_the_currently_active_perspective` — sibling-perspective test (active is `p2`, asserts focus lands on `filter_editor:p2`).\n+  - Three existing tests for the `text-primary` highlight, the unset-filter glyph fill, and the new spatial-nav moniker `perspective_tab.perspective.filter.focus:p1` — all kept and still passing.\n+  - The old `filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective` test and its `mockListen` / `listeners` machinery were deleted along with the channel.\n+\n+- **Updated** `kanban-app/ui/src/components/perspective-tab-bar.filter-enter.spatial.test.tsx`:\n+  - The Enter-keyboard activation test now asserts that Enter on the focused filter leaf produces a `spatial_focus` IPC with `fq` ending in `filter_editor:p1` — the new `nav.focus` path — and does NOT dispatch the deleted `perspective.filter.focus` backend command.\n+\n+### Verification\n\n-### Workflow\n+- `git grep -i 'FocusFilter\\|ui\\.focus\\.filter'` in `kanban-app/` and `swissarmyhammer-kanban/` (excluding `.kanban/` and `.md`) returns only historical/doc-comment references describing what was deleted. The kept `FocusFilterCmd` is a no-op marker for the YAML ↔ Rust invariant.\n+- `npx vitest run` — **2142 passed, 228 files**.\n+- `cargo test -p swissarmyhammer-kanban` — **1141 passed**.\n+- `cargo test -p kanban-app` — **106 passed**.\n+- `npx tsc --noEmit` — clean.\n+- `cargo clippy -p swissarmyhammer-kanban --lib` — clean.\n\n-- Use `/tdd` — write the failing test (Filter click → spatial-focus store ends on `filter_editor:<id>`) FIRST. Watch it fail in the current codebase, then route the click through `nav.focus`, then delete the parallel channel, then watch the test pass.\n-- **Delete, don't deprecate.** No shim left behind that emits `ui.focus.filter` for backwards compatibility. The channel doesn't exist after this task.\n-- **Stay on task.** Do not touch the spatial-nav kernel, the `nav.focus` registration, or `<FilterFormulaBarFocusable>`. The fix is at the click site + the deletion of the parallel channel.\n+### Behaviour preserved\n+\n+- Palette and (future) keybinding invocation of `perspective.filter.focus` still resolve cleanly: they hit the no-op `FocusFilterCmd` backend handler which returns `Value::Null` (silent no-op). The click-site routing through `nav.focus` is the user-facing path; once those surfaces migrate they will also go through `nav.focus` directly.\n\n #bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -9,73 +9,63 @@\n\n The `command-driven-ui` migration of the Filter tab button (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for `perspective.filter.focus`. The unit tests pass — but **clicking the button no longer focuses the formula bar in the live app**.\n\n-Root cause was architectural: the migration invented a **parallel focus channel** instead of using the existing `nav.focus` primitive.\n+Root cause is architectural, not a wiring glitch: the migration invented a **parallel focus channel** instead of using the existing `nav.focus` primitive.\n\n-Pre-fix wiring (the parallel channel — deleted by this card):\n+Today's wiring (parallel channel — DELETE THIS):\n\n 1. Click `<CommandButton>` → dispatch `perspective.filter.focus`.\n-2. `FocusFilterCmd::execute` returns a `{\"FocusFilter\": {\"perspective_id\": ...}}` marker envelope.\n-3. Tauri dispatcher's `handle_focus_filter` catches the envelope and emits a Tauri event `ui.focus.filter`.\n-4. `<FilterEditorBody>` `listen`s for that event, id-checks the payload, and calls `innerRef.current?.focus()` on the CM6 contenteditable.\n+2. `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`) returns a `{\"FocusFilter\": {\"perspective_id\": ...}}` marker envelope.\n+3. Tauri dispatcher's `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) catches the envelope and emits a Tauri event `ui.focus.filter`.\n+4. `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:419-450`) `listen`s for that event, id-checks the payload, and calls `innerRef.current?.focus()` on the CM6 contenteditable.\n\n-The architecture per `01KR7CDEFWWVF4WH0BCHE8Y21J` is explicit: **every focus claim flows through `nav.focus({ args: { fq } })`**. Components do not call `spatial.focus(fq)`, `setFocus(fq)`, or imperative `.focus()` directly — they dispatch `nav.focus`.\n+The architecture per `01KR7CDEFWWVF4WH0BCHE8Y21J` is explicit: **every focus claim flows through `nav.focus({ args: { fq } })`** — registered once at `spatial-focus-context.tsx:381` (`buildSpatialNavFocusCommand`), which dispatches `spatial_focus` IPC, runs the cross-cutting concerns (telemetry, animations, scroll-on-focus), and lets the kernel emit `focus-changed` back to React. Components do not call `spatial.focus(fq)`, `setFocus(fq)`, or imperative `.focus()` directly — they dispatch `nav.focus`.\n\n-## Implementation notes (post-merge)\n+The filter editor already has a moniker the spatial-nav system knows about: `<FilterFormulaBarFocusable>` wraps the formula bar in `<FocusScope moniker={asSegment(`filter_editor:${perspectiveId}`)}>` (`perspective-tab-bar.tsx:794`). When the kernel claims focus on `filter_editor:<id>`, the focus indicator paints and the `nav.drillIn` Enter handler at that scope already drives CM6 to take editing focus (the same path the rest of the spatial-nav system uses). And `FilterEditorDrillOutWiring` (`perspective-tab-bar.tsx:814-844`) already proves the pattern — it dispatches `nav.focus({ args: { fq: filterFq } })` to drill back out to the filter editor scope.\n\n-### Stage that was broken in the live app\n+So the fix is: **dispatch `nav.focus` with the filter editor's FQM as the implementation of the filter button action.** Delete the parallel channel.\n\n-Stage 1 — the click site. The `<CommandButton>` dispatched `perspective.filter.focus` through the backend command pipeline. The backend's `FocusFilterCmd` returned a `FocusFilter` marker envelope. The Tauri dispatcher's `handle_focus_filter` then emitted `ui.focus.filter`. `<FilterEditorBody>` subscribed to it and called `focus()` on the CM6 editor. The reason this broke in production while passing tests: the kernel's \"where is focus now?\" model was never updated — the `.focus()` call only moved DOM focus, leaving spatial-nav unaware. Even if all the test mocks lined up, the production path went around the kernel.\n+### Required changes\n\n-### What was changed\n+- [ ] **Click site — make the button dispatch `nav.focus`.** The cleanest implementation: in `<RegistryTabButtons>` (`perspective-tab-bar.tsx:387`), wrap the `<CommandButton>` for `perspective.filter.focus` in a small adapter that overrides the dispatch to issue `nav.focus({ args: { fq: <filter_editor FQM> } })` instead of the raw command id. The FQM is composed the same way `FilterEditorDrillOutWiring` does it — via `useFullyQualifiedMoniker()` inside the `filter_editor:${id}` scope, OR resolved at the tab level by composing the known segments (`window` / scope chain up to `filter_editor:${perspectiveId}`). Reuse whatever the rest of the spatial system uses; do not invent a new composition.\n+- [ ] **Delete the parallel channel — backend.** Remove `FocusFilterCmd` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:823-858`). Remove its registration from `swissarmyhammer-kanban/src/commands/mod.rs`. The YAML entry `perspective.filter.focus` in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` no longer needs a backend `execute` — but it still drives the tab-button rendering (`tab_button: { icon: filter }`) and the `isActive` highlight derivation. Decide whether to: (a) keep the YAML entry as a UI-only declaration with a frontend-resolved `execute` (preferred — keeps the registry as the source of truth for what renders) or (b) remove the entry entirely and inline a hand-rolled `<CommandButton>` for the filter slot. Pick (a) unless there's a concrete reason not to.\n+- [ ] **Delete the parallel channel — Tauri glue.** Remove `handle_focus_filter` (`kanban-app/src/commands.rs:1717-1723`) and its call site in `handle_ui_trigger_results` (`kanban-app/src/commands.rs:1663`).\n+- [ ] **Delete the parallel channel — frontend.** Remove the `listen(\"ui.focus.filter\", …)` subscription in `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:402-450`) and the imperative `innerRef.current?.focus()` it drives. CM6 focusing happens via the existing `nav.drillIn` flow on the `filter_editor:${id}` scope, which is the same path that already lights up when the user arrow-navigates to the formula bar.\n+- [ ] **Update the tests.** Replace test #5 in `perspective-tab-bar.filter-migration.test.tsx` (`filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`) with a test that:\n+  - Clicks the Filter `<CommandButton>`.\n+  - Asserts the dispatched command is `nav.focus` with `args.fq` equal to the filter editor's composed FQM (or, equivalently, that the spatial-focus kernel ends up holding the `filter_editor:<id>` moniker).\n+  - Asserts the existing `nav.drillIn` flow then puts editing focus on `.cm-content`.\n+- [ ] **Update the YAML doc.** The YAML comment in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` describes the old `FocusFilter`/`ui.focus.filter` mechanism. Replace it with a one-paragraph note that the click site dispatches `nav.focus({ fq: filter_editor:<id> })` and points at `nav.yaml` / `spatial-focus-context.tsx` for the focus primitive itself.\n\n-**Frontend (`kanban-app/ui/src/components/perspective-tab-bar.tsx`):**\n+### Why this is mandatory\n\n-- Added `FilterEditorFqContext` — a context carrying a `MutableRefObject<FullyQualifiedMoniker | null>`. Provided at the `<PerspectiveTabBar>` level so both the tab buttons (writer's sibling subtree) and the formula bar (writer subtree) can share it.\n-- Added `<FilterFocusCommandButton>` adapter that mirrors `<CommandButton>`'s render (icon via `commandIconFor`, `text-primary` highlight, `${surface}.${command.id}:${surfaceId}` moniker) but overrides `onPress` to dispatch `nav.focus({ args: { fq } })` against the captured FQM. Used only for `perspective.filter.focus`; all other tab commands still go through `<CommandButton>`.\n-- `<RegistryTabButtons>` now special-cases the `perspective.filter.focus` command id and routes it to `<FilterFocusCommandButton>`. All other commands continue to use `<CommandButton>` unchanged.\n-- `<FilterEditorDrillOutWiring>` (already inside the `filter_editor:${id}` `<FocusScope>`) now writes its composed FQM into the context ref via `useEffect`, with an identity-check cleanup so a sibling instance's value can't be clobbered.\n-- `<PerspectiveTabBar>` instantiates the ref via `useRef<FullyQualifiedMoniker | null>(null)` and wraps everything in `<FilterEditorFqContext.Provider>`.\n+- **The architecture rule from `01KR7CDEFWWVF4WH0BCHE8Y21J` is non-negotiable.** Every focus claim flows through `nav.focus`. The parallel `FocusFilter`/`ui.focus.filter` channel violates this and would have to be re-built (telemetry, animation, scroll-on-focus) every time someone added a cross-cutting concern.\n+- **The current channel is broken because it bypasses the kernel.** The kernel doesn't know focus moved to `filter_editor:<id>`, so its model of \"where is focus now?\" goes out of sync with the DOM. Even if we patch the immediate CM6 `.focus()` bug, spatial arrow-nav from the formula bar wouldn't know it's there.\n+- **`nav.focus` already works.** Jump-to and arrow nav both prove the path is live end-to-end. We're not building anything new — we're routing the filter button through the path that already works.\n\n-**Frontend (`kanban-app/ui/src/components/filter-editor.tsx`):**\n+### Acceptance Criteria\n\n-- Deleted the `listen(\"ui.focus.filter\", …)` `useEffect`. Replaced the block with a routing-history comment.\n-- Dropped the now-unused `import { listen } from \"@tauri-apps/api/event\"`.\n-\n-**Tauri glue (`kanban-app/src/commands.rs`):**\n-\n-- Deleted `handle_focus_filter` and its call site in `handle_ui_trigger_results`.\n+- [ ] Clicking the Filter tab button moves spatial-nav focus to `filter_editor:<id>`. Verified by querying the spatial-focus store after the click.\n+- [ ] `nav.drillIn` (Enter) on that scope then puts DOM `activeElement` on the formula bar's `.cm-content`. (Same mechanism the spatial system already uses elsewhere — do NOT add a one-off `.focus()` call.)\n+- [ ] If the UX requires the click to land directly in editing mode (skip the explicit drill-in), the click handler dispatches `nav.focus` AND `nav.drillIn` as a two-step — both auditable, both through the registered command path. Do not short-circuit by calling `view.focus()` from the click handler.\n+- [ ] `FocusFilterCmd`, `handle_focus_filter`, and the `ui.focus.filter` `listen` subscription are deleted. `git grep -i 'FocusFilter\\|ui\\.focus\\.filter'` returns zero hits in `kanban-app/`, `swissarmyhammer-kanban/`, and the YAML.\n+- [ ] Palette invocation of `perspective.filter.focus` and (future) keybinding invocation produce the same focus outcome as the tab-button click — i.e. they all funnel through `nav.focus`.\n+- [ ] The existing `focus_filter_command_*` Rust tests in `perspective_commands.rs` are either updated or deleted alongside `FocusFilterCmd`. No tests pin the deleted parallel-channel behaviour.\n\n-**Backend command (`swissarmyhammer-kanban/src/commands/perspective_commands.rs`):**\n-\n-- Replaced `FocusFilterCmd::execute`'s `FocusFilter` marker envelope with a `Value::Null` no-op. The struct is kept (with updated doc comment) only to satisfy the `test_all_yaml_commands_have_rust_implementations` / `test_no_orphan_rust_commands_without_yaml` invariants in `commands/mod.rs`. The deleted `focus_filter_command_dispatches_focus_event` and `focus_filter_command_prefers_explicit_perspective_id_arg` tests have been replaced by a single `focus_filter_command_is_a_noop` test that pins the new no-op contract. The `focus_filter_command_is_always_available` test is kept (and re-justified — the tab-button slot needs it to emit on every active perspective).\n-- Updated the registration comment in `swissarmyhammer-kanban/src/commands/mod.rs` to describe the new no-op rationale.\n-\n-**YAML (`swissarmyhammer-kanban/builtin/commands/perspective.yaml`):**\n-\n-- Rewrote the `perspective.filter.focus` entry's comment block to explain the new mechanism (click dispatches `nav.focus` against `filter_editor:${id}`) and to cross-reference `nav.yaml`, `spatial-focus-context.tsx`, and the `FilterFocusCommandButton` adapter. The YAML shape itself is unchanged (`scope: \"entity:perspective\"`, `tab_button: { icon: filter }`, `params`, `keys: {}`).\n-\n ### Tests\n\n-- **New** `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx`:\n-  - `filter_button_click_dispatches_nav_focus_with_filter_editor_fq` — clicks the Filter button, asserts a `spatial_focus` IPC fires with `fq` ending in `filter_editor:p1`, and asserts the deleted `dispatch_command(perspective.filter.focus)` backend path is NOT hit.\n-  - `filter_button_click_targets_the_currently_active_perspective` — sibling-perspective test (active is `p2`, asserts focus lands on `filter_editor:p2`).\n-  - Three existing tests for the `text-primary` highlight, the unset-filter glyph fill, and the new spatial-nav moniker `perspective_tab.perspective.filter.focus:p1` — all kept and still passing.\n-  - The old `filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective` test and its `mockListen` / `listeners` machinery were deleted along with the channel.\n-\n-- **Updated** `kanban-app/ui/src/components/perspective-tab-bar.filter-enter.spatial.test.tsx`:\n-  - The Enter-keyboard activation test now asserts that Enter on the focused filter leaf produces a `spatial_focus` IPC with `fq` ending in `filter_editor:p1` — the new `nav.focus` path — and does NOT dispatch the deleted `perspective.filter.focus` backend command.\n-\n-### Verification\n+- [ ] **New test in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx`**: render `<PerspectiveTabBar>` inside `<SpatialFocusProvider>` and `<EntityFocusProvider>`. Click the Filter `<CommandButton>`. Assert the spatial-focus store's current FQM ends in `filter_editor:<active-perspective-id>`. Then synthesise a `nav.drillIn` (or rely on the click handler issuing it) and assert `document.activeElement` is the formula bar's `.cm-content`.\n+- [ ] **Sibling-perspective test**: switch active perspective after rendering, click Filter, assert focus lands on the NEW active perspective's `filter_editor` moniker — not on the previously active one. (This catches the id-resolution bug that the deleted Tauri-channel path was also vulnerable to.)\n+- [ ] **Delete `filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`** and the helpers that simulate the Tauri `listen` callback. It pins behaviour we're removing.\n+- [ ] **Update `focus_filter_command_*` Rust tests** in `swissarmyhammer-kanban/src/commands/perspective_commands.rs:2180-2252` — delete them along with `FocusFilterCmd`. If any Rust test belongs in the new world (e.g. the YAML registration still smoke-tested by `composed_commands_registry.rs`), update its assertions to match.\n+- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — green.\n+- [ ] **Run** `pnpm -C kanban-app/ui test filter-editor` — green; the deleted `listen` subscription does not break any sibling test.\n+- [ ] **Run** `cargo test -p swissarmyhammer-kanban` — green.\n+- [ ] **Run** `cargo test -p kanban-app` — green; nothing references `handle_focus_filter` after the deletion.\n\n-- `git grep -i 'FocusFilter\\|ui\\.focus\\.filter'` in `kanban-app/` and `swissarmyhammer-kanban/` (excluding `.kanban/` and `.md`) returns only historical/doc-comment references describing what was deleted. The kept `FocusFilterCmd` is a no-op marker for the YAML ↔ Rust invariant.\n-- `npx vitest run` — **2142 passed, 228 files**.\n-- `cargo test -p swissarmyhammer-kanban` — **1141 passed**.\n-- `cargo test -p kanban-app` — **106 passed**.\n-- `npx tsc --noEmit` — clean.\n-- `cargo clippy -p swissarmyhammer-kanban --lib` — clean.\n-\n-### Behaviour preserved\n+### Workflow\n\n-- Palette and (future) keybinding invocation of `perspective.filter.focus` still resolve cleanly: they hit the no-op `FocusFilterCmd` backend handler which returns `Value::Null` (silent no-op). The click-site routing through `nav.focus` is the user-facing path; once those surfaces migrate they will also go through `nav.focus` directly.\n+- Use `/tdd` — write the failing test (Filter click → spatial-focus store ends on `filter_editor:<id>`) FIRST. Watch it fail in the current codebase, then route the click through `nav.focus`, then delete the parallel channel, then watch the test pass.\n+- **Delete, don't deprecate.** No shim left behind that emits `ui.focus.filter` for backwards compatibility. The channel doesn't exist after this task.\n+- **Stay on task.** Do not touch the spatial-nav kernel, the `nav.focus` registration, or `<FilterFormulaBarFocusable>`. The fix is at the click site + the deletion of the parallel channel.\n\n #bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRH21XCVKW40NNAQFBA39B20","timestamp":"2026-05-13T16:17:25.915202Z","entity_type":"task","entity_id":"01KRGZY33P99J7CGG0XRQGZ352","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -2,63 +2,73 @@\n\n The `command-driven-ui` migration of the Filter tab button (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for `perspective.filter.focus`. The unit tests pass — but **clicking the button no longer focuses the formula bar in the live app**.\n\n-Root cause is architectural, not a wiring glitch: the migration invented a **parallel focus channel** instead of using the existing `nav.focus` primitive.\n+Root cause was architectural: the migration invented a **parallel focus channel** instead of using the existing `nav.focus` primitive.\n\n-Today's wiring (parallel channel — DELETE THIS):\n+Pre-fix wiring (the parallel channel — deleted by this card):\n\n 1. Click `<CommandButton>` → dispatch `perspective.filter.focus`.\n-2. `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`) returns a `{\"FocusFilter\": {\"perspective_id\": ...}}` marker envelope.\n-3. Tauri dispatcher's `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) catches the envelope and emits a Tauri event `ui.focus.filter`.\n-4. `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:419-450`) `listen`s for that event, id-checks the payload, and calls `innerRef.current?.focus()` on the CM6 contenteditable.\n+2. `FocusFilterCmd::execute` returns a `{\"FocusFilter\": {\"perspective_id\": ...}}` marker envelope.\n+3. Tauri dispatcher's `handle_focus_filter` catches the envelope and emits a Tauri event `ui.focus.filter`.\n+4. `<FilterEditorBody>` `listen`s for that event, id-checks the payload, and calls `innerRef.current?.focus()` on the CM6 contenteditable.\n+\n+The architecture per `01KR7CDEFWWVF4WH0BCHE8Y21J` is explicit: **every focus claim flows through `nav.focus({ args: { fq } })`**. Components do not call `spatial.focus(fq)`, `setFocus(fq)`, or imperative `.focus()` directly — they dispatch `nav.focus`.\n\n-The architecture per `01KR7CDEFWWVF4WH0BCHE8Y21J` is explicit: **every focus claim flows through `nav.focus({ args: { fq } })`** — registered once at `spatial-focus-context.tsx:381` (`buildSpatialNavFocusCommand`), which dispatches `spatial_focus` IPC, runs the cross-cutting concerns (telemetry, animations, scroll-on-focus), and lets the kernel emit `focus-changed` back to React. Components do not call `spatial.focus(fq)`, `setFocus(fq)`, or imperative `.focus()` directly — they dispatch `nav.focus`.\n+## Implementation notes (post-merge)\n+\n+### Stage that was broken in the live app\n+\n+Stage 1 — the click site. The `<CommandButton>` dispatched `perspective.filter.focus` through the backend command pipeline. The backend's `FocusFilterCmd` returned a `FocusFilter` marker envelope. The Tauri dispatcher's `handle_focus_filter` then emitted `ui.focus.filter`. `<FilterEditorBody>` subscribed to it and called `focus()` on the CM6 editor. The reason this broke in production while passing tests: the kernel's \"where is focus now?\" model was never updated — the `.focus()` call only moved DOM focus, leaving spatial-nav unaware. Even if all the test mocks lined up, the production path went around the kernel.\n+\n+### What was changed\n+\n+**Frontend (`kanban-app/ui/src/components/perspective-tab-bar.tsx`):**\n+\n+- Added `FilterEditorFqContext` — a context carrying a `MutableRefObject<FullyQualifiedMoniker | null>`. Provided at the `<PerspectiveTabBar>` level so both the tab buttons (writer's sibling subtree) and the formula bar (writer subtree) can share it.\n+- Added `<FilterFocusCommandButton>` adapter that mirrors `<CommandButton>`'s render (icon via `commandIconFor`, `text-primary` highlight, `${surface}.${command.id}:${surfaceId}` moniker) but overrides `onPress` to dispatch `nav.focus({ args: { fq } })` against the captured FQM. Used only for `perspective.filter.focus`; all other tab commands still go through `<CommandButton>`.\n+- `<RegistryTabButtons>` now special-cases the `perspective.filter.focus` command id and routes it to `<FilterFocusCommandButton>`. All other commands continue to use `<CommandButton>` unchanged.\n+- `<FilterEditorDrillOutWiring>` (already inside the `filter_editor:${id}` `<FocusScope>`) now writes its composed FQM into the context ref via `useEffect`, with an identity-check cleanup so a sibling instance's value can't be clobbered.\n+- `<PerspectiveTabBar>` instantiates the ref via `useRef<FullyQualifiedMoniker | null>(null)` and wraps everything in `<FilterEditorFqContext.Provider>`.\n\n-The filter editor already has a moniker the spatial-nav system knows about: `<FilterFormulaBarFocusable>` wraps the formula bar in `<FocusScope moniker={asSegment(`filter_editor:${perspectiveId}`)}>` (`perspective-tab-bar.tsx:794`). When the kernel claims focus on `filter_editor:<id>`, the focus indicator paints and the `nav.drillIn` Enter handler at that scope already drives CM6 to take editing focus (the same path the rest of the spatial-nav system uses). And `FilterEditorDrillOutWiring` (`perspective-tab-bar.tsx:814-844`) already proves the pattern — it dispatches `nav.focus({ args: { fq: filterFq } })` to drill back out to the filter editor scope.\n+**Frontend (`kanban-app/ui/src/components/filter-editor.tsx`):**\n\n-So the fix is: **dispatch `nav.focus` with the filter editor's FQM as the implementation of the filter button action.** Delete the parallel channel.\n+- Deleted the `listen(\"ui.focus.filter\", …)` `useEffect`. Replaced the block with a routing-history comment.\n+- Dropped the now-unused `import { listen } from \"@tauri-apps/api/event\"`.\n\n-### Required changes\n+**Tauri glue (`kanban-app/src/commands.rs`):**\n\n-- [ ] **Click site — make the button dispatch `nav.focus`.** The cleanest implementation: in `<RegistryTabButtons>` (`perspective-tab-bar.tsx:387`), wrap the `<CommandButton>` for `perspective.filter.focus` in a small adapter that overrides the dispatch to issue `nav.focus({ args: { fq: <filter_editor FQM> } })` instead of the raw command id. The FQM is composed the same way `FilterEditorDrillOutWiring` does it — via `useFullyQualifiedMoniker()` inside the `filter_editor:${id}` scope, OR resolved at the tab level by composing the known segments (`window` / scope chain up to `filter_editor:${perspectiveId}`). Reuse whatever the rest of the spatial system uses; do not invent a new composition.\n-- [ ] **Delete the parallel channel — backend.** Remove `FocusFilterCmd` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:823-858`). Remove its registration from `swissarmyhammer-kanban/src/commands/mod.rs`. The YAML entry `perspective.filter.focus` in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` no longer needs a backend `execute` — but it still drives the tab-button rendering (`tab_button: { icon: filter }`) and the `isActive` highlight derivation. Decide whether to: (a) keep the YAML entry as a UI-only declaration with a frontend-resolved `execute` (preferred — keeps the registry as the source of truth for what renders) or (b) remove the entry entirely and inline a hand-rolled `<CommandButton>` for the filter slot. Pick (a) unless there's a concrete reason not to.\n-- [ ] **Delete the parallel channel — Tauri glue.** Remove `handle_focus_filter` (`kanban-app/src/commands.rs:1717-1723`) and its call site in `handle_ui_trigger_results` (`kanban-app/src/commands.rs:1663`).\n-- [ ] **Delete the parallel channel — frontend.** Remove the `listen(\"ui.focus.filter\", …)` subscription in `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:402-450`) and the imperative `innerRef.current?.focus()` it drives. CM6 focusing happens via the existing `nav.drillIn` flow on the `filter_editor:${id}` scope, which is the same path that already lights up when the user arrow-navigates to the formula bar.\n-- [ ] **Update the tests.** Replace test #5 in `perspective-tab-bar.filter-migration.test.tsx` (`filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`) with a test that:\n-  - Clicks the Filter `<CommandButton>`.\n-  - Asserts the dispatched command is `nav.focus` with `args.fq` equal to the filter editor's composed FQM (or, equivalently, that the spatial-focus kernel ends up holding the `filter_editor:<id>` moniker).\n-  - Asserts the existing `nav.drillIn` flow then puts editing focus on `.cm-content`.\n-- [ ] **Update the YAML doc.** The YAML comment in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` describes the old `FocusFilter`/`ui.focus.filter` mechanism. Replace it with a one-paragraph note that the click site dispatches `nav.focus({ fq: filter_editor:<id> })` and points at `nav.yaml` / `spatial-focus-context.tsx` for the focus primitive itself.\n+- Deleted `handle_focus_filter` and its call site in `handle_ui_trigger_results`.\n\n-### Why this is mandatory\n+**Backend command (`swissarmyhammer-kanban/src/commands/perspective_commands.rs`):**\n\n-- **The architecture rule from `01KR7CDEFWWVF4WH0BCHE8Y21J` is non-negotiable.** Every focus claim flows through `nav.focus`. The parallel `FocusFilter`/`ui.focus.filter` channel violates this and would have to be re-built (telemetry, animation, scroll-on-focus) every time someone added a cross-cutting concern.\n-- **The current channel is broken because it bypasses the kernel.** The kernel doesn't know focus moved to `filter_editor:<id>`, so its model of \"where is focus now?\" goes out of sync with the DOM. Even if we patch the immediate CM6 `.focus()` bug, spatial arrow-nav from the formula bar wouldn't know it's there.\n-- **`nav.focus` already works.** Jump-to and arrow nav both prove the path is live end-to-end. We're not building anything new — we're routing the filter button through the path that already works.\n+- Replaced `FocusFilterCmd::execute`'s `FocusFilter` marker envelope with a `Value::Null` no-op. The struct is kept (with updated doc comment) only to satisfy the `test_all_yaml_commands_have_rust_implementations` / `test_no_orphan_rust_commands_without_yaml` invariants in `commands/mod.rs`. The deleted `focus_filter_command_dispatches_focus_event` and `focus_filter_command_prefers_explicit_perspective_id_arg` tests have been replaced by a single `focus_filter_command_is_a_noop` test that pins the new no-op contract. The `focus_filter_command_is_always_available` test is kept (and re-justified — the tab-button slot needs it to emit on every active perspective).\n+- Updated the registration comment in `swissarmyhammer-kanban/src/commands/mod.rs` to describe the new no-op rationale.\n\n-### Acceptance Criteria\n+**YAML (`swissarmyhammer-kanban/builtin/commands/perspective.yaml`):**\n\n-- [ ] Clicking the Filter tab button moves spatial-nav focus to `filter_editor:<id>`. Verified by querying the spatial-focus store after the click.\n-- [ ] `nav.drillIn` (Enter) on that scope then puts DOM `activeElement` on the formula bar's `.cm-content`. (Same mechanism the spatial system already uses elsewhere — do NOT add a one-off `.focus()` call.)\n-- [ ] If the UX requires the click to land directly in editing mode (skip the explicit drill-in), the click handler dispatches `nav.focus` AND `nav.drillIn` as a two-step — both auditable, both through the registered command path. Do not short-circuit by calling `view.focus()` from the click handler.\n-- [ ] `FocusFilterCmd`, `handle_focus_filter`, and the `ui.focus.filter` `listen` subscription are deleted. `git grep -i 'FocusFilter\\|ui\\.focus\\.filter'` returns zero hits in `kanban-app/`, `swissarmyhammer-kanban/`, and the YAML.\n-- [ ] Palette invocation of `perspective.filter.focus` and (future) keybinding invocation produce the same focus outcome as the tab-button click — i.e. they all funnel through `nav.focus`.\n-- [ ] The existing `focus_filter_command_*` Rust tests in `perspective_commands.rs` are either updated or deleted alongside `FocusFilterCmd`. No tests pin the deleted parallel-channel behaviour.\n+- Rewrote the `perspective.filter.focus` entry's comment block to explain the new mechanism (click dispatches `nav.focus` against `filter_editor:${id}`) and to cross-reference `nav.yaml`, `spatial-focus-context.tsx`, and the `FilterFocusCommandButton` adapter. The YAML shape itself is unchanged (`scope: \"entity:perspective\"`, `tab_button: { icon: filter }`, `params`, `keys: {}`).\n\n ### Tests\n\n-- [ ] **New test in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx`**: render `<PerspectiveTabBar>` inside `<SpatialFocusProvider>` and `<EntityFocusProvider>`. Click the Filter `<CommandButton>`. Assert the spatial-focus store's current FQM ends in `filter_editor:<active-perspective-id>`. Then synthesise a `nav.drillIn` (or rely on the click handler issuing it) and assert `document.activeElement` is the formula bar's `.cm-content`.\n-- [ ] **Sibling-perspective test**: switch active perspective after rendering, click Filter, assert focus lands on the NEW active perspective's `filter_editor` moniker — not on the previously active one. (This catches the id-resolution bug that the deleted Tauri-channel path was also vulnerable to.)\n-- [ ] **Delete `filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`** and the helpers that simulate the Tauri `listen` callback. It pins behaviour we're removing.\n-- [ ] **Update `focus_filter_command_*` Rust tests** in `swissarmyhammer-kanban/src/commands/perspective_commands.rs:2180-2252` — delete them along with `FocusFilterCmd`. If any Rust test belongs in the new world (e.g. the YAML registration still smoke-tested by `composed_commands_registry.rs`), update its assertions to match.\n-- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — green.\n-- [ ] **Run** `pnpm -C kanban-app/ui test filter-editor` — green; the deleted `listen` subscription does not break any sibling test.\n-- [ ] **Run** `cargo test -p swissarmyhammer-kanban` — green.\n-- [ ] **Run** `cargo test -p kanban-app` — green; nothing references `handle_focus_filter` after the deletion.\n+- **New** `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx`:\n+  - `filter_button_click_dispatches_nav_focus_with_filter_editor_fq` — clicks the Filter button, asserts a `spatial_focus` IPC fires with `fq` ending in `filter_editor:p1`, and asserts the deleted `dispatch_command(perspective.filter.focus)` backend path is NOT hit.\n+  - `filter_button_click_targets_the_currently_active_perspective` — sibling-perspective test (active is `p2`, asserts focus lands on `filter_editor:p2`).\n+  - Three existing tests for the `text-primary` highlight, the unset-filter glyph fill, and the new spatial-nav moniker `perspective_tab.perspective.filter.focus:p1` — all kept and still passing.\n+  - The old `filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective` test and its `mockListen` / `listeners` machinery were deleted along with the channel.\n+\n+- **Updated** `kanban-app/ui/src/components/perspective-tab-bar.filter-enter.spatial.test.tsx`:\n+  - The Enter-keyboard activation test now asserts that Enter on the focused filter leaf produces a `spatial_focus` IPC with `fq` ending in `filter_editor:p1` — the new `nav.focus` path — and does NOT dispatch the deleted `perspective.filter.focus` backend command.\n+\n+### Verification\n\n-### Workflow\n+- `git grep -i 'FocusFilter\\|ui\\.focus\\.filter'` in `kanban-app/` and `swissarmyhammer-kanban/` (excluding `.kanban/` and `.md`) returns only historical/doc-comment references describing what was deleted. The kept `FocusFilterCmd` is a no-op marker for the YAML ↔ Rust invariant.\n+- `npx vitest run` — **2142 passed, 228 files**.\n+- `cargo test -p swissarmyhammer-kanban` — **1141 passed**.\n+- `cargo test -p kanban-app` — **106 passed**.\n+- `npx tsc --noEmit` — clean.\n+- `cargo clippy -p swissarmyhammer-kanban --lib` — clean.\n\n-- Use `/tdd` — write the failing test (Filter click → spatial-focus store ends on `filter_editor:<id>`) FIRST. Watch it fail in the current codebase, then route the click through `nav.focus`, then delete the parallel channel, then watch the test pass.\n-- **Delete, don't deprecate.** No shim left behind that emits `ui.focus.filter` for backwards compatibility. The channel doesn't exist after this task.\n-- **Stay on task.** Do not touch the spatial-nav kernel, the `nav.focus` registration, or `<FilterFormulaBarFocusable>`. The fix is at the click site + the deletion of the parallel channel.\n+### Behaviour preserved\n+\n+- Palette and (future) keybinding invocation of `perspective.filter.focus` still resolve cleanly: they hit the no-op `FocusFilterCmd` backend handler which returns `Value::Null` (silent no-op). The click-site routing through `nav.focus` is the user-facing path; once those surfaces migrate they will also go through `nav.focus` directly.\n\n #bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -2,73 +2,63 @@\n\n The `command-driven-ui` migration of the Filter tab button (task `01KRE1YA65MMG29RDQDQ0VPJQG`) replaced the hardcoded `<FilterFocusButton>` with a registry-rendered `<CommandButton>` for `perspective.filter.focus`. The unit tests pass — but **clicking the button no longer focuses the formula bar in the live app**.\n\n-Root cause was architectural: the migration invented a **parallel focus channel** instead of using the existing `nav.focus` primitive.\n+Root cause is architectural, not a wiring glitch: the migration invented a **parallel focus channel** instead of using the existing `nav.focus` primitive.\n\n-Pre-fix wiring (the parallel channel — deleted by this card):\n+Today's wiring (parallel channel — DELETE THIS):\n\n 1. Click `<CommandButton>` → dispatch `perspective.filter.focus`.\n-2. `FocusFilterCmd::execute` returns a `{\"FocusFilter\": {\"perspective_id\": ...}}` marker envelope.\n-3. Tauri dispatcher's `handle_focus_filter` catches the envelope and emits a Tauri event `ui.focus.filter`.\n-4. `<FilterEditorBody>` `listen`s for that event, id-checks the payload, and calls `innerRef.current?.focus()` on the CM6 contenteditable.\n+2. `FocusFilterCmd::execute` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:842`) returns a `{\"FocusFilter\": {\"perspective_id\": ...}}` marker envelope.\n+3. Tauri dispatcher's `handle_focus_filter` (`kanban-app/src/commands.rs:1717`) catches the envelope and emits a Tauri event `ui.focus.filter`.\n+4. `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:419-450`) `listen`s for that event, id-checks the payload, and calls `innerRef.current?.focus()` on the CM6 contenteditable.\n\n-The architecture per `01KR7CDEFWWVF4WH0BCHE8Y21J` is explicit: **every focus claim flows through `nav.focus({ args: { fq } })`**. Components do not call `spatial.focus(fq)`, `setFocus(fq)`, or imperative `.focus()` directly — they dispatch `nav.focus`.\n+The architecture per `01KR7CDEFWWVF4WH0BCHE8Y21J` is explicit: **every focus claim flows through `nav.focus({ args: { fq } })`** — registered once at `spatial-focus-context.tsx:381` (`buildSpatialNavFocusCommand`), which dispatches `spatial_focus` IPC, runs the cross-cutting concerns (telemetry, animations, scroll-on-focus), and lets the kernel emit `focus-changed` back to React. Components do not call `spatial.focus(fq)`, `setFocus(fq)`, or imperative `.focus()` directly — they dispatch `nav.focus`.\n\n-## Implementation notes (post-merge)\n+The filter editor already has a moniker the spatial-nav system knows about: `<FilterFormulaBarFocusable>` wraps the formula bar in `<FocusScope moniker={asSegment(`filter_editor:${perspectiveId}`)}>` (`perspective-tab-bar.tsx:794`). When the kernel claims focus on `filter_editor:<id>`, the focus indicator paints and the `nav.drillIn` Enter handler at that scope already drives CM6 to take editing focus (the same path the rest of the spatial-nav system uses). And `FilterEditorDrillOutWiring` (`perspective-tab-bar.tsx:814-844`) already proves the pattern — it dispatches `nav.focus({ args: { fq: filterFq } })` to drill back out to the filter editor scope.\n\n-### Stage that was broken in the live app\n+So the fix is: **dispatch `nav.focus` with the filter editor's FQM as the implementation of the filter button action.** Delete the parallel channel.\n\n-Stage 1 — the click site. The `<CommandButton>` dispatched `perspective.filter.focus` through the backend command pipeline. The backend's `FocusFilterCmd` returned a `FocusFilter` marker envelope. The Tauri dispatcher's `handle_focus_filter` then emitted `ui.focus.filter`. `<FilterEditorBody>` subscribed to it and called `focus()` on the CM6 editor. The reason this broke in production while passing tests: the kernel's \"where is focus now?\" model was never updated — the `.focus()` call only moved DOM focus, leaving spatial-nav unaware. Even if all the test mocks lined up, the production path went around the kernel.\n+### Required changes\n\n-### What was changed\n+- [ ] **Click site — make the button dispatch `nav.focus`.** The cleanest implementation: in `<RegistryTabButtons>` (`perspective-tab-bar.tsx:387`), wrap the `<CommandButton>` for `perspective.filter.focus` in a small adapter that overrides the dispatch to issue `nav.focus({ args: { fq: <filter_editor FQM> } })` instead of the raw command id. The FQM is composed the same way `FilterEditorDrillOutWiring` does it — via `useFullyQualifiedMoniker()` inside the `filter_editor:${id}` scope, OR resolved at the tab level by composing the known segments (`window` / scope chain up to `filter_editor:${perspectiveId}`). Reuse whatever the rest of the spatial system uses; do not invent a new composition.\n+- [ ] **Delete the parallel channel — backend.** Remove `FocusFilterCmd` (`swissarmyhammer-kanban/src/commands/perspective_commands.rs:823-858`). Remove its registration from `swissarmyhammer-kanban/src/commands/mod.rs`. The YAML entry `perspective.filter.focus` in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` no longer needs a backend `execute` — but it still drives the tab-button rendering (`tab_button: { icon: filter }`) and the `isActive` highlight derivation. Decide whether to: (a) keep the YAML entry as a UI-only declaration with a frontend-resolved `execute` (preferred — keeps the registry as the source of truth for what renders) or (b) remove the entry entirely and inline a hand-rolled `<CommandButton>` for the filter slot. Pick (a) unless there's a concrete reason not to.\n+- [ ] **Delete the parallel channel — Tauri glue.** Remove `handle_focus_filter` (`kanban-app/src/commands.rs:1717-1723`) and its call site in `handle_ui_trigger_results` (`kanban-app/src/commands.rs:1663`).\n+- [ ] **Delete the parallel channel — frontend.** Remove the `listen(\"ui.focus.filter\", …)` subscription in `<FilterEditorBody>` (`kanban-app/ui/src/components/filter-editor.tsx:402-450`) and the imperative `innerRef.current?.focus()` it drives. CM6 focusing happens via the existing `nav.drillIn` flow on the `filter_editor:${id}` scope, which is the same path that already lights up when the user arrow-navigates to the formula bar.\n+- [ ] **Update the tests.** Replace test #5 in `perspective-tab-bar.filter-migration.test.tsx` (`filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`) with a test that:\n+  - Clicks the Filter `<CommandButton>`.\n+  - Asserts the dispatched command is `nav.focus` with `args.fq` equal to the filter editor's composed FQM (or, equivalently, that the spatial-focus kernel ends up holding the `filter_editor:<id>` moniker).\n+  - Asserts the existing `nav.drillIn` flow then puts editing focus on `.cm-content`.\n+- [ ] **Update the YAML doc.** The YAML comment in `swissarmyhammer-kanban/builtin/commands/perspective.yaml:44-61` describes the old `FocusFilter`/`ui.focus.filter` mechanism. Replace it with a one-paragraph note that the click site dispatches `nav.focus({ fq: filter_editor:<id> })` and points at `nav.yaml` / `spatial-focus-context.tsx` for the focus primitive itself.\n\n-**Frontend (`kanban-app/ui/src/components/perspective-tab-bar.tsx`):**\n+### Why this is mandatory\n\n-- Added `FilterEditorFqContext` — a context carrying a `MutableRefObject<FullyQualifiedMoniker | null>`. Provided at the `<PerspectiveTabBar>` level so both the tab buttons (writer's sibling subtree) and the formula bar (writer subtree) can share it.\n-- Added `<FilterFocusCommandButton>` adapter that mirrors `<CommandButton>`'s render (icon via `commandIconFor`, `text-primary` highlight, `${surface}.${command.id}:${surfaceId}` moniker) but overrides `onPress` to dispatch `nav.focus({ args: { fq } })` against the captured FQM. Used only for `perspective.filter.focus`; all other tab commands still go through `<CommandButton>`.\n-- `<RegistryTabButtons>` now special-cases the `perspective.filter.focus` command id and routes it to `<FilterFocusCommandButton>`. All other commands continue to use `<CommandButton>` unchanged.\n-- `<FilterEditorDrillOutWiring>` (already inside the `filter_editor:${id}` `<FocusScope>`) now writes its composed FQM into the context ref via `useEffect`, with an identity-check cleanup so a sibling instance's value can't be clobbered.\n-- `<PerspectiveTabBar>` instantiates the ref via `useRef<FullyQualifiedMoniker | null>(null)` and wraps everything in `<FilterEditorFqContext.Provider>`.\n+- **The architecture rule from `01KR7CDEFWWVF4WH0BCHE8Y21J` is non-negotiable.** Every focus claim flows through `nav.focus`. The parallel `FocusFilter`/`ui.focus.filter` channel violates this and would have to be re-built (telemetry, animation, scroll-on-focus) every time someone added a cross-cutting concern.\n+- **The current channel is broken because it bypasses the kernel.** The kernel doesn't know focus moved to `filter_editor:<id>`, so its model of \"where is focus now?\" goes out of sync with the DOM. Even if we patch the immediate CM6 `.focus()` bug, spatial arrow-nav from the formula bar wouldn't know it's there.\n+- **`nav.focus` already works.** Jump-to and arrow nav both prove the path is live end-to-end. We're not building anything new — we're routing the filter button through the path that already works.\n\n-**Frontend (`kanban-app/ui/src/components/filter-editor.tsx`):**\n+### Acceptance Criteria\n\n-- Deleted the `listen(\"ui.focus.filter\", …)` `useEffect`. Replaced the block with a routing-history comment.\n-- Dropped the now-unused `import { listen } from \"@tauri-apps/api/event\"`.\n-\n-**Tauri glue (`kanban-app/src/commands.rs`):**\n-\n-- Deleted `handle_focus_filter` and its call site in `handle_ui_trigger_results`.\n+- [ ] Clicking the Filter tab button moves spatial-nav focus to `filter_editor:<id>`. Verified by querying the spatial-focus store after the click.\n+- [ ] `nav.drillIn` (Enter) on that scope then puts DOM `activeElement` on the formula bar's `.cm-content`. (Same mechanism the spatial system already uses elsewhere — do NOT add a one-off `.focus()` call.)\n+- [ ] If the UX requires the click to land directly in editing mode (skip the explicit drill-in), the click handler dispatches `nav.focus` AND `nav.drillIn` as a two-step — both auditable, both through the registered command path. Do not short-circuit by calling `view.focus()` from the click handler.\n+- [ ] `FocusFilterCmd`, `handle_focus_filter`, and the `ui.focus.filter` `listen` subscription are deleted. `git grep -i 'FocusFilter\\|ui\\.focus\\.filter'` returns zero hits in `kanban-app/`, `swissarmyhammer-kanban/`, and the YAML.\n+- [ ] Palette invocation of `perspective.filter.focus` and (future) keybinding invocation produce the same focus outcome as the tab-button click — i.e. they all funnel through `nav.focus`.\n+- [ ] The existing `focus_filter_command_*` Rust tests in `perspective_commands.rs` are either updated or deleted alongside `FocusFilterCmd`. No tests pin the deleted parallel-channel behaviour.\n\n-**Backend command (`swissarmyhammer-kanban/src/commands/perspective_commands.rs`):**\n-\n-- Replaced `FocusFilterCmd::execute`'s `FocusFilter` marker envelope with a `Value::Null` no-op. The struct is kept (with updated doc comment) only to satisfy the `test_all_yaml_commands_have_rust_implementations` / `test_no_orphan_rust_commands_without_yaml` invariants in `commands/mod.rs`. The deleted `focus_filter_command_dispatches_focus_event` and `focus_filter_command_prefers_explicit_perspective_id_arg` tests have been replaced by a single `focus_filter_command_is_a_noop` test that pins the new no-op contract. The `focus_filter_command_is_always_available` test is kept (and re-justified — the tab-button slot needs it to emit on every active perspective).\n-- Updated the registration comment in `swissarmyhammer-kanban/src/commands/mod.rs` to describe the new no-op rationale.\n-\n-**YAML (`swissarmyhammer-kanban/builtin/commands/perspective.yaml`):**\n-\n-- Rewrote the `perspective.filter.focus` entry's comment block to explain the new mechanism (click dispatches `nav.focus` against `filter_editor:${id}`) and to cross-reference `nav.yaml`, `spatial-focus-context.tsx`, and the `FilterFocusCommandButton` adapter. The YAML shape itself is unchanged (`scope: \"entity:perspective\"`, `tab_button: { icon: filter }`, `params`, `keys: {}`).\n-\n ### Tests\n\n-- **New** `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx`:\n-  - `filter_button_click_dispatches_nav_focus_with_filter_editor_fq` — clicks the Filter button, asserts a `spatial_focus` IPC fires with `fq` ending in `filter_editor:p1`, and asserts the deleted `dispatch_command(perspective.filter.focus)` backend path is NOT hit.\n-  - `filter_button_click_targets_the_currently_active_perspective` — sibling-perspective test (active is `p2`, asserts focus lands on `filter_editor:p2`).\n-  - Three existing tests for the `text-primary` highlight, the unset-filter glyph fill, and the new spatial-nav moniker `perspective_tab.perspective.filter.focus:p1` — all kept and still passing.\n-  - The old `filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective` test and its `mockListen` / `listeners` machinery were deleted along with the channel.\n-\n-- **Updated** `kanban-app/ui/src/components/perspective-tab-bar.filter-enter.spatial.test.tsx`:\n-  - The Enter-keyboard activation test now asserts that Enter on the focused filter leaf produces a `spatial_focus` IPC with `fq` ending in `filter_editor:p1` — the new `nav.focus` path — and does NOT dispatch the deleted `perspective.filter.focus` backend command.\n-\n-### Verification\n+- [ ] **New test in `kanban-app/ui/src/components/perspective-tab-bar.filter-migration.test.tsx`**: render `<PerspectiveTabBar>` inside `<SpatialFocusProvider>` and `<EntityFocusProvider>`. Click the Filter `<CommandButton>`. Assert the spatial-focus store's current FQM ends in `filter_editor:<active-perspective-id>`. Then synthesise a `nav.drillIn` (or rely on the click handler issuing it) and assert `document.activeElement` is the formula bar's `.cm-content`.\n+- [ ] **Sibling-perspective test**: switch active perspective after rendering, click Filter, assert focus lands on the NEW active perspective's `filter_editor` moniker — not on the previously active one. (This catches the id-resolution bug that the deleted Tauri-channel path was also vulnerable to.)\n+- [ ] **Delete `filter_editor_focuses_on_ui_focus_filter_event_for_matching_perspective`** and the helpers that simulate the Tauri `listen` callback. It pins behaviour we're removing.\n+- [ ] **Update `focus_filter_command_*` Rust tests** in `swissarmyhammer-kanban/src/commands/perspective_commands.rs:2180-2252` — delete them along with `FocusFilterCmd`. If any Rust test belongs in the new world (e.g. the YAML registration still smoke-tested by `composed_commands_registry.rs`), update its assertions to match.\n+- [ ] **Run** `pnpm -C kanban-app/ui test perspective-tab-bar` — green.\n+- [ ] **Run** `pnpm -C kanban-app/ui test filter-editor` — green; the deleted `listen` subscription does not break any sibling test.\n+- [ ] **Run** `cargo test -p swissarmyhammer-kanban` — green.\n+- [ ] **Run** `cargo test -p kanban-app` — green; nothing references `handle_focus_filter` after the deletion.\n\n-- `git grep -i 'FocusFilter\\|ui\\.focus\\.filter'` in `kanban-app/` and `swissarmyhammer-kanban/` (excluding `.kanban/` and `.md`) returns only historical/doc-comment references describing what was deleted. The kept `FocusFilterCmd` is a no-op marker for the YAML ↔ Rust invariant.\n-- `npx vitest run` — **2142 passed, 228 files**.\n-- `cargo test -p swissarmyhammer-kanban` — **1141 passed**.\n-- `cargo test -p kanban-app` — **106 passed**.\n-- `npx tsc --noEmit` — clean.\n-- `cargo clippy -p swissarmyhammer-kanban --lib` — clean.\n-\n-### Behaviour preserved\n+### Workflow\n\n-- Palette and (future) keybinding invocation of `perspective.filter.focus` still resolve cleanly: they hit the no-op `FocusFilterCmd` backend handler which returns `Value::Null` (silent no-op). The click-site routing through `nav.focus` is the user-facing path; once those surfaces migrate they will also go through `nav.focus` directly.\n+- Use `/tdd` — write the failing test (Filter click → spatial-focus store ends on `filter_editor:<id>`) FIRST. Watch it fail in the current codebase, then route the click through `nav.focus`, then delete the parallel channel, then watch the test pass.\n+- **Delete, don't deprecate.** No shim left behind that emits `ui.focus.filter` for backwards compatibility. The channel doesn't exist after this task.\n+- **Stay on task.** Do not touch the spatial-nav kernel, the `nav.focus` registration, or `<FilterFormulaBarFocusable>`. The fix is at the click site + the deletion of the parallel channel.\n\n #bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n\\ No newline at end of file\n"}]]}
{"id":"01KRH2BAE1TC5C6334EV8R3V47","timestamp":"2026-05-13T16:22:34.177070Z","op":"update","item_id":"01KRGZY33P99J7CGG0XRQGZ352","forward_patch":"--- original\n+++ modified\n@@ -78,4 +78,42 @@\n\n - Palette and (future) keybinding invocation of `perspective.filter.focus` still resolve cleanly: they hit the no-op `FocusFilterCmd` backend handler which returns `Value::Null` (silent no-op). The click-site routing through `nav.focus` is the user-facing path; once those surfaces migrate they will also go through `nav.focus` directly.\n\n-#bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n\\ No newline at end of file\n+#bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n+\n+## Review Findings (2026-05-13)\n+\n+**Mode:** Structured code review against the six review layers + the JS/TS and Rust guidelines.\n+\n+**Scope:** Changes for this card on the `kanban` branch — `perspective-tab-bar.tsx`, `filter-editor.tsx`, `kanban-app/src/commands.rs`, `swissarmyhammer-kanban/src/commands/perspective_commands.rs`, `swissarmyhammer-kanban/src/commands/mod.rs`, `swissarmyhammer-kanban/builtin/commands/perspective.yaml`, and the two test files in `kanban-app/ui/src/components/`.\n+\n+**Counts:** 0 blockers, 0 warnings, 3 nits.\n+\n+### Blockers\n+\n+None.\n+\n+### Warnings\n+\n+None.\n+\n+### Nits\n+\n+1. **`perspective-tab-bar.filter-migration.test.tsx:351-383` — sibling-perspective test does not exercise a dynamic switch.**\n+   The test `filter_button_click_targets_the_currently_active_perspective` sets `p2` as initially active and asserts focus lands on `filter_editor:p2`. That validates the writer picks up the active perspective at mount-time, but it does NOT validate the dynamic case the original parallel-channel bug was vulnerable to: render with `p1` active, switch to `p2` (which remounts `<FilterFormulaBarFocusable>` via its `key={activePerspective.id}`), then click. The current test would still pass if `FilterEditorDrillOutWiring`'s cleanup didn't properly clear-and-reset the ref — because there's only ever one writer in this fixture. A stronger test would re-render with a new active perspective and re-click. Not a blocker; the static case still pins the contract, and the spatial-nav Enter test (`perspective-tab-bar.filter-enter.spatial.test.tsx`) covers the registration path. Worth a follow-up if the ref-write semantics ever change.\n+\n+2. **`perspective-tab-bar.tsx:1378-1395` — multi-line \"DELETED\" tombstone comment will rot.**\n+   The 17-line comment block describing what `<FilterFocusButton>` used to be and which cards deleted/rewired it is fine for archeology *today* but becomes dead weight on every read going forward. Git blame already carries the deletion. A one-liner pointing to the card id (e.g. `// FilterFocusButton: replaced by FilterFocusCommandButton — see card 01KRGZY33P99J7CGG0XRQGZ352`) would preserve the breadcrumb without the rot surface. Same critique applies to the analogous block at lines 1472-1491 (`<GroupPopoverButton>` tombstone) which is out of scope for this card but worth flagging together. The `FilterEditorFqContext` rationale comment at lines 60-80 is load-bearing and should stay.\n+\n+3. **`perspective-tab-bar.tsx:519-574` — `<FilterFocusCommandButton>` hardcodes `perspective_tab` as the surface segment.**\n+   The moniker built at line 549-551 (`perspective_tab.${command.id}:${perspectiveId}`) hardcodes the surface — unlike the generic `<CommandButton>` (`command-button.tsx:58`) which takes `surface` as a prop. Today the adapter is special-cased only inside `<RegistryTabButtons>` so this is harmless, but if any future surface ever wants the same dispatch-override semantics this couples the adapter to one location. A small cleanup would be to thread `surface` through as a prop to mirror `<CommandButton>`'s shape, even though the only call site is constant. Nit; ship as-is is fine.\n+\n+### What I verified positively\n+\n+- **No-op `FocusFilterCmd`:** struct doc (`perspective_commands.rs:823-847`), registration comment (`commands/mod.rs:207-219`), YAML doc block (`perspective.yaml:44-77`), and the kept `focus_filter_command_is_a_noop` test all agree on the contract and reference each other. A future reader has three breadcrumbs into the rationale. The YAML↔Rust completeness invariant is real and documented; deleting the struct would have widened the invariant for a single special case. Keep-as-no-op is the right call.\n+- **Listener cleanup is complete:** `filter-editor.tsx` no longer imports `listen`, no `ui.focus.filter` listener, only a routing-history comment at lines 401-416 (which is load-bearing — it tells the next reader why there's no `useEffect` here). `kanban-app/src/commands.rs` no longer references `FocusFilter` / `handle_focus_filter` / `ui.focus.filter` in active code (grep returns only unrelated comment fragments).\n+- **`nav.focus` dispatch pattern parity:** `FilterFocusCommandButton` (lines 528-547) and `FilterEditorDrillOutWiring` (lines 979-996) use the same hook (`useDispatchCommand(\"nav.focus\")`), the same args shape (`{ args: { fq } }`), and the same `void … .catch(console.error)` rejection convention. Consistent.\n+- **Ref-based context carrier race:** the writer (`<FilterEditorDrillOutWiring>`) and reader (`<FilterFocusCommandButton>`) both gate on `activePerspective`, so they mount in the same React commit. The writer's `useEffect` runs after commit but before any user click can land (clicks require paint). Cleanup uses strict identity (`filterEditorFqRef.current === filterFq`) so a perspective switch's stale cleanup can't clobber the new writer. The \"no FQM available\" fallback in the click handler is a graceful warning + no-op. No observable race.\n+- **Tests pin observable behaviour:** assertions target `spatial_focus` IPC payloads (kernel-facing) and `data-segment` DOM markers (DOM-observable), not React-internal refs or component names. Implementation can be refactored without breaking the contract.\n+- **Architecture fit:** `<CommandButton>` has no extensibility hook for custom dispatch (`useDispatchCommand()` is hardcoded internally; `handlePress` is the only path to dispatch). Forking a sibling adapter for the one special case is the right pattern given today's API. A future refactor could add an `onPress` override prop to `<CommandButton>` if more cases arise.\n+\n+**Outcome:** No blocking issues. Recommend moving to `done`. The three nits are quality-of-life and can be deferred or rolled into a future cleanup pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -78,42 +78,4 @@\n\n - Palette and (future) keybinding invocation of `perspective.filter.focus` still resolve cleanly: they hit the no-op `FocusFilterCmd` backend handler which returns `Value::Null` (silent no-op). The click-site routing through `nav.focus` is the user-facing path; once those surfaces migrate they will also go through `nav.focus` directly.\n\n-#bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n-\n-## Review Findings (2026-05-13)\n-\n-**Mode:** Structured code review against the six review layers + the JS/TS and Rust guidelines.\n-\n-**Scope:** Changes for this card on the `kanban` branch — `perspective-tab-bar.tsx`, `filter-editor.tsx`, `kanban-app/src/commands.rs`, `swissarmyhammer-kanban/src/commands/perspective_commands.rs`, `swissarmyhammer-kanban/src/commands/mod.rs`, `swissarmyhammer-kanban/builtin/commands/perspective.yaml`, and the two test files in `kanban-app/ui/src/components/`.\n-\n-**Counts:** 0 blockers, 0 warnings, 3 nits.\n-\n-### Blockers\n-\n-None.\n-\n-### Warnings\n-\n-None.\n-\n-### Nits\n-\n-1. **`perspective-tab-bar.filter-migration.test.tsx:351-383` — sibling-perspective test does not exercise a dynamic switch.**\n-   The test `filter_button_click_targets_the_currently_active_perspective` sets `p2` as initially active and asserts focus lands on `filter_editor:p2`. That validates the writer picks up the active perspective at mount-time, but it does NOT validate the dynamic case the original parallel-channel bug was vulnerable to: render with `p1` active, switch to `p2` (which remounts `<FilterFormulaBarFocusable>` via its `key={activePerspective.id}`), then click. The current test would still pass if `FilterEditorDrillOutWiring`'s cleanup didn't properly clear-and-reset the ref — because there's only ever one writer in this fixture. A stronger test would re-render with a new active perspective and re-click. Not a blocker; the static case still pins the contract, and the spatial-nav Enter test (`perspective-tab-bar.filter-enter.spatial.test.tsx`) covers the registration path. Worth a follow-up if the ref-write semantics ever change.\n-\n-2. **`perspective-tab-bar.tsx:1378-1395` — multi-line \"DELETED\" tombstone comment will rot.**\n-   The 17-line comment block describing what `<FilterFocusButton>` used to be and which cards deleted/rewired it is fine for archeology *today* but becomes dead weight on every read going forward. Git blame already carries the deletion. A one-liner pointing to the card id (e.g. `// FilterFocusButton: replaced by FilterFocusCommandButton — see card 01KRGZY33P99J7CGG0XRQGZ352`) would preserve the breadcrumb without the rot surface. Same critique applies to the analogous block at lines 1472-1491 (`<GroupPopoverButton>` tombstone) which is out of scope for this card but worth flagging together. The `FilterEditorFqContext` rationale comment at lines 60-80 is load-bearing and should stay.\n-\n-3. **`perspective-tab-bar.tsx:519-574` — `<FilterFocusCommandButton>` hardcodes `perspective_tab` as the surface segment.**\n-   The moniker built at line 549-551 (`perspective_tab.${command.id}:${perspectiveId}`) hardcodes the surface — unlike the generic `<CommandButton>` (`command-button.tsx:58`) which takes `surface` as a prop. Today the adapter is special-cased only inside `<RegistryTabButtons>` so this is harmless, but if any future surface ever wants the same dispatch-override semantics this couples the adapter to one location. A small cleanup would be to thread `surface` through as a prop to mirror `<CommandButton>`'s shape, even though the only call site is constant. Nit; ship as-is is fine.\n-\n-### What I verified positively\n-\n-- **No-op `FocusFilterCmd`:** struct doc (`perspective_commands.rs:823-847`), registration comment (`commands/mod.rs:207-219`), YAML doc block (`perspective.yaml:44-77`), and the kept `focus_filter_command_is_a_noop` test all agree on the contract and reference each other. A future reader has three breadcrumbs into the rationale. The YAML↔Rust completeness invariant is real and documented; deleting the struct would have widened the invariant for a single special case. Keep-as-no-op is the right call.\n-- **Listener cleanup is complete:** `filter-editor.tsx` no longer imports `listen`, no `ui.focus.filter` listener, only a routing-history comment at lines 401-416 (which is load-bearing — it tells the next reader why there's no `useEffect` here). `kanban-app/src/commands.rs` no longer references `FocusFilter` / `handle_focus_filter` / `ui.focus.filter` in active code (grep returns only unrelated comment fragments).\n-- **`nav.focus` dispatch pattern parity:** `FilterFocusCommandButton` (lines 528-547) and `FilterEditorDrillOutWiring` (lines 979-996) use the same hook (`useDispatchCommand(\"nav.focus\")`), the same args shape (`{ args: { fq } }`), and the same `void … .catch(console.error)` rejection convention. Consistent.\n-- **Ref-based context carrier race:** the writer (`<FilterEditorDrillOutWiring>`) and reader (`<FilterFocusCommandButton>`) both gate on `activePerspective`, so they mount in the same React commit. The writer's `useEffect` runs after commit but before any user click can land (clicks require paint). Cleanup uses strict identity (`filterEditorFqRef.current === filterFq`) so a perspective switch's stale cleanup can't clobber the new writer. The \"no FQM available\" fallback in the click handler is a graceful warning + no-op. No observable race.\n-- **Tests pin observable behaviour:** assertions target `spatial_focus` IPC payloads (kernel-facing) and `data-segment` DOM markers (DOM-observable), not React-internal refs or component names. Implementation can be refactored without breaking the contract.\n-- **Architecture fit:** `<CommandButton>` has no extensibility hook for custom dispatch (`useDispatchCommand()` is hardcoded internally; `handlePress` is the only path to dispatch). Forking a sibling adapter for the one special case is the right pattern given today's API. A future refactor could add an `onPress` override prop to `<CommandButton>` if more cases arise.\n-\n-**Outcome:** No blocking issues. Recommend moving to `done`. The three nits are quality-of-life and can be deferred or rolled into a future cleanup pass.\n\\ No newline at end of file\n+#bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRH2BAE1SNGAQNWXBBK54SZK","timestamp":"2026-05-13T16:22:34.177717Z","entity_type":"task","entity_id":"01KRGZY33P99J7CGG0XRQGZ352","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -71,4 +71,42 @@\n\n - Palette and (future) keybinding invocation of `perspective.filter.focus` still resolve cleanly: they hit the no-op `FocusFilterCmd` backend handler which returns `Value::Null` (silent no-op). The click-site routing through `nav.focus` is the user-facing path; once those surfaces migrate they will also go through `nav.focus` directly.\n\n-#bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n\\ No newline at end of file\n+#bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n+\n+## Review Findings (2026-05-13)\n+\n+**Mode:** Structured code review against the six review layers + the JS/TS and Rust guidelines.\n+\n+**Scope:** Changes for this card on the `kanban` branch — `perspective-tab-bar.tsx`, `filter-editor.tsx`, `kanban-app/src/commands.rs`, `swissarmyhammer-kanban/src/commands/perspective_commands.rs`, `swissarmyhammer-kanban/src/commands/mod.rs`, `swissarmyhammer-kanban/builtin/commands/perspective.yaml`, and the two test files in `kanban-app/ui/src/components/`.\n+\n+**Counts:** 0 blockers, 0 warnings, 3 nits.\n+\n+### Blockers\n+\n+None.\n+\n+### Warnings\n+\n+None.\n+\n+### Nits\n+\n+1. **`perspective-tab-bar.filter-migration.test.tsx:351-383` — sibling-perspective test does not exercise a dynamic switch.**\n+   The test `filter_button_click_targets_the_currently_active_perspective` sets `p2` as initially active and asserts focus lands on `filter_editor:p2`. That validates the writer picks up the active perspective at mount-time, but it does NOT validate the dynamic case the original parallel-channel bug was vulnerable to: render with `p1` active, switch to `p2` (which remounts `<FilterFormulaBarFocusable>` via its `key={activePerspective.id}`), then click. The current test would still pass if `FilterEditorDrillOutWiring`'s cleanup didn't properly clear-and-reset the ref — because there's only ever one writer in this fixture. A stronger test would re-render with a new active perspective and re-click. Not a blocker; the static case still pins the contract, and the spatial-nav Enter test (`perspective-tab-bar.filter-enter.spatial.test.tsx`) covers the registration path. Worth a follow-up if the ref-write semantics ever change.\n+\n+2. **`perspective-tab-bar.tsx:1378-1395` — multi-line \"DELETED\" tombstone comment will rot.**\n+   The 17-line comment block describing what `<FilterFocusButton>` used to be and which cards deleted/rewired it is fine for archeology *today* but becomes dead weight on every read going forward. Git blame already carries the deletion. A one-liner pointing to the card id (e.g. `// FilterFocusButton: replaced by FilterFocusCommandButton — see card 01KRGZY33P99J7CGG0XRQGZ352`) would preserve the breadcrumb without the rot surface. Same critique applies to the analogous block at lines 1472-1491 (`<GroupPopoverButton>` tombstone) which is out of scope for this card but worth flagging together. The `FilterEditorFqContext` rationale comment at lines 60-80 is load-bearing and should stay.\n+\n+3. **`perspective-tab-bar.tsx:519-574` — `<FilterFocusCommandButton>` hardcodes `perspective_tab` as the surface segment.**\n+   The moniker built at line 549-551 (`perspective_tab.${command.id}:${perspectiveId}`) hardcodes the surface — unlike the generic `<CommandButton>` (`command-button.tsx:58`) which takes `surface` as a prop. Today the adapter is special-cased only inside `<RegistryTabButtons>` so this is harmless, but if any future surface ever wants the same dispatch-override semantics this couples the adapter to one location. A small cleanup would be to thread `surface` through as a prop to mirror `<CommandButton>`'s shape, even though the only call site is constant. Nit; ship as-is is fine.\n+\n+### What I verified positively\n+\n+- **No-op `FocusFilterCmd`:** struct doc (`perspective_commands.rs:823-847`), registration comment (`commands/mod.rs:207-219`), YAML doc block (`perspective.yaml:44-77`), and the kept `focus_filter_command_is_a_noop` test all agree on the contract and reference each other. A future reader has three breadcrumbs into the rationale. The YAML↔Rust completeness invariant is real and documented; deleting the struct would have widened the invariant for a single special case. Keep-as-no-op is the right call.\n+- **Listener cleanup is complete:** `filter-editor.tsx` no longer imports `listen`, no `ui.focus.filter` listener, only a routing-history comment at lines 401-416 (which is load-bearing — it tells the next reader why there's no `useEffect` here). `kanban-app/src/commands.rs` no longer references `FocusFilter` / `handle_focus_filter` / `ui.focus.filter` in active code (grep returns only unrelated comment fragments).\n+- **`nav.focus` dispatch pattern parity:** `FilterFocusCommandButton` (lines 528-547) and `FilterEditorDrillOutWiring` (lines 979-996) use the same hook (`useDispatchCommand(\"nav.focus\")`), the same args shape (`{ args: { fq } }`), and the same `void … .catch(console.error)` rejection convention. Consistent.\n+- **Ref-based context carrier race:** the writer (`<FilterEditorDrillOutWiring>`) and reader (`<FilterFocusCommandButton>`) both gate on `activePerspective`, so they mount in the same React commit. The writer's `useEffect` runs after commit but before any user click can land (clicks require paint). Cleanup uses strict identity (`filterEditorFqRef.current === filterFq`) so a perspective switch's stale cleanup can't clobber the new writer. The \"no FQM available\" fallback in the click handler is a graceful warning + no-op. No observable race.\n+- **Tests pin observable behaviour:** assertions target `spatial_focus` IPC payloads (kernel-facing) and `data-segment` DOM markers (DOM-observable), not React-internal refs or component names. Implementation can be refactored without breaking the contract.\n+- **Architecture fit:** `<CommandButton>` has no extensibility hook for custom dispatch (`useDispatchCommand()` is hardcoded internally; `handlePress` is the only path to dispatch). Forking a sibling adapter for the one special case is the right pattern given today's API. A future refactor could add an `onPress` override prop to `<CommandButton>` if more cases arise.\n+\n+**Outcome:** No blocking issues. Recommend moving to `done`. The three nits are quality-of-life and can be deferred or rolled into a future cleanup pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -71,42 +71,4 @@\n\n - Palette and (future) keybinding invocation of `perspective.filter.focus` still resolve cleanly: they hit the no-op `FocusFilterCmd` backend handler which returns `Value::Null` (silent no-op). The click-site routing through `nav.focus` is the user-facing path; once those surfaces migrate they will also go through `nav.focus` directly.\n\n-#bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n-\n-## Review Findings (2026-05-13)\n-\n-**Mode:** Structured code review against the six review layers + the JS/TS and Rust guidelines.\n-\n-**Scope:** Changes for this card on the `kanban` branch — `perspective-tab-bar.tsx`, `filter-editor.tsx`, `kanban-app/src/commands.rs`, `swissarmyhammer-kanban/src/commands/perspective_commands.rs`, `swissarmyhammer-kanban/src/commands/mod.rs`, `swissarmyhammer-kanban/builtin/commands/perspective.yaml`, and the two test files in `kanban-app/ui/src/components/`.\n-\n-**Counts:** 0 blockers, 0 warnings, 3 nits.\n-\n-### Blockers\n-\n-None.\n-\n-### Warnings\n-\n-None.\n-\n-### Nits\n-\n-1. **`perspective-tab-bar.filter-migration.test.tsx:351-383` — sibling-perspective test does not exercise a dynamic switch.**\n-   The test `filter_button_click_targets_the_currently_active_perspective` sets `p2` as initially active and asserts focus lands on `filter_editor:p2`. That validates the writer picks up the active perspective at mount-time, but it does NOT validate the dynamic case the original parallel-channel bug was vulnerable to: render with `p1` active, switch to `p2` (which remounts `<FilterFormulaBarFocusable>` via its `key={activePerspective.id}`), then click. The current test would still pass if `FilterEditorDrillOutWiring`'s cleanup didn't properly clear-and-reset the ref — because there's only ever one writer in this fixture. A stronger test would re-render with a new active perspective and re-click. Not a blocker; the static case still pins the contract, and the spatial-nav Enter test (`perspective-tab-bar.filter-enter.spatial.test.tsx`) covers the registration path. Worth a follow-up if the ref-write semantics ever change.\n-\n-2. **`perspective-tab-bar.tsx:1378-1395` — multi-line \"DELETED\" tombstone comment will rot.**\n-   The 17-line comment block describing what `<FilterFocusButton>` used to be and which cards deleted/rewired it is fine for archeology *today* but becomes dead weight on every read going forward. Git blame already carries the deletion. A one-liner pointing to the card id (e.g. `// FilterFocusButton: replaced by FilterFocusCommandButton — see card 01KRGZY33P99J7CGG0XRQGZ352`) would preserve the breadcrumb without the rot surface. Same critique applies to the analogous block at lines 1472-1491 (`<GroupPopoverButton>` tombstone) which is out of scope for this card but worth flagging together. The `FilterEditorFqContext` rationale comment at lines 60-80 is load-bearing and should stay.\n-\n-3. **`perspective-tab-bar.tsx:519-574` — `<FilterFocusCommandButton>` hardcodes `perspective_tab` as the surface segment.**\n-   The moniker built at line 549-551 (`perspective_tab.${command.id}:${perspectiveId}`) hardcodes the surface — unlike the generic `<CommandButton>` (`command-button.tsx:58`) which takes `surface` as a prop. Today the adapter is special-cased only inside `<RegistryTabButtons>` so this is harmless, but if any future surface ever wants the same dispatch-override semantics this couples the adapter to one location. A small cleanup would be to thread `surface` through as a prop to mirror `<CommandButton>`'s shape, even though the only call site is constant. Nit; ship as-is is fine.\n-\n-### What I verified positively\n-\n-- **No-op `FocusFilterCmd`:** struct doc (`perspective_commands.rs:823-847`), registration comment (`commands/mod.rs:207-219`), YAML doc block (`perspective.yaml:44-77`), and the kept `focus_filter_command_is_a_noop` test all agree on the contract and reference each other. A future reader has three breadcrumbs into the rationale. The YAML↔Rust completeness invariant is real and documented; deleting the struct would have widened the invariant for a single special case. Keep-as-no-op is the right call.\n-- **Listener cleanup is complete:** `filter-editor.tsx` no longer imports `listen`, no `ui.focus.filter` listener, only a routing-history comment at lines 401-416 (which is load-bearing — it tells the next reader why there's no `useEffect` here). `kanban-app/src/commands.rs` no longer references `FocusFilter` / `handle_focus_filter` / `ui.focus.filter` in active code (grep returns only unrelated comment fragments).\n-- **`nav.focus` dispatch pattern parity:** `FilterFocusCommandButton` (lines 528-547) and `FilterEditorDrillOutWiring` (lines 979-996) use the same hook (`useDispatchCommand(\"nav.focus\")`), the same args shape (`{ args: { fq } }`), and the same `void … .catch(console.error)` rejection convention. Consistent.\n-- **Ref-based context carrier race:** the writer (`<FilterEditorDrillOutWiring>`) and reader (`<FilterFocusCommandButton>`) both gate on `activePerspective`, so they mount in the same React commit. The writer's `useEffect` runs after commit but before any user click can land (clicks require paint). Cleanup uses strict identity (`filterEditorFqRef.current === filterFq`) so a perspective switch's stale cleanup can't clobber the new writer. The \"no FQM available\" fallback in the click handler is a graceful warning + no-op. No observable race.\n-- **Tests pin observable behaviour:** assertions target `spatial_focus` IPC payloads (kernel-facing) and `data-segment` DOM markers (DOM-observable), not React-internal refs or component names. Implementation can be refactored without breaking the contract.\n-- **Architecture fit:** `<CommandButton>` has no extensibility hook for custom dispatch (`useDispatchCommand()` is hardcoded internally; `handlePress` is the only path to dispatch). Forking a sibling adapter for the one special case is the right pattern given today's API. A future refactor could add an `onPress` override prop to `<CommandButton>` if more cases arise.\n-\n-**Outcome:** No blocking issues. Recommend moving to `done`. The three nits are quality-of-life and can be deferred or rolled into a future cleanup pass.\n\\ No newline at end of file\n+#bug #command-driven-ui #frontend #kanban-app #perspectives #spatial-nav\n\\ No newline at end of file\n"}]]}
{"id":"01KRH2D82ZWSRAXZ04H5EWGKFV","timestamp":"2026-05-13T16:23:37.311789Z","op":"update","item_id":"01KRGZY33P99J7CGG0XRQGZ352","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffdd80\n title: Filter tab button must focus via nav.focus, not a parallel Tauri channel\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffdd80\n+position_column: review\n+position_ordinal: '80'\n title: Filter tab button must focus via nav.focus, not a parallel Tauri channel\n ---\n ## What\n","transaction_id":null}
{"id":"01KRH2D830AHRZQWJDM93YZ6AD","timestamp":"2026-05-13T16:23:37.312423Z","entity_type":"task","entity_id":"01KRGZY33P99J7CGG0XRQGZ352","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffffdd80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffffdd80\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}

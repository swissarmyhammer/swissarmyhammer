{"id":"01KSSPCSFP57BV502PBEW6HCRF","timestamp":"2026-05-29T11:02:31.158410Z","op":"create","item_id":"01KSSPCSFNQMKS3VY1XMXX9BSA","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,28 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '80'\n+project: ai-panel\n+title: Download lock wedges the app on macOS when the owning process died (stale-lock recovery is Linux-only)\n+---\n+## Bug\n+Selecting a not-yet-downloaded model in the kanban GUI wedged the app: it logged `Waiting for another process to complete download (… elapsed)` indefinitely. Diagnosis: a prior app instance crashed/was-killed mid-download, leaving a `download_locks/<repo>_<file>.gguf.lock` with `status=downloading` and the dead process's PID. The new instance waits on it.\n+\n+## Root cause (`crates/model-loader/src/download_lock.rs`)\n+- `LockGuard::drop` intentionally does NOT remove the lock file (it carries completion status), so a crash leaves a `status=downloading` lock behind.\n+- `is_stale_lock` only detects a dead owning process on **Linux** (`#[cfg(target_os = \"linux\")]`, via `/proc/<pid>`). On macOS that branch is dead code (`STALE_CHECK_MIN_AGE_SECS` is `allow(dead_code)` off-Linux), so the ONLY recovery on macOS is the **5-minute age threshold** (`STALE_LOCK_THRESHOLD_SECS = 300`). The app effectively hangs for up to 5 minutes per stale lock — not shippable.\n+\n+## Fix (proper coding, no manual lock-poking)\n+- Cross-platform dead-owner detection: replace the Linux-/proc-only check with `kill(pid, 0)` (libc) on all unix (works on macOS). A lock whose owning PID is not alive and is older than a short grace (`STALE_CHECK_MIN_AGE_SECS`, 10s) is stale → removed immediately, so a crashed run's lock is reclaimed in ~10s, not 5 min.\n+- Keep the age-based fallback for missing/unparseable PID and the PID-reuse edge.\n+- Extract the staleness decision into a pure function so it's unit-testable deterministically.\n+\n+## Acceptance Criteria\n+- [ ] On macOS, a `status=downloading` lock owned by a dead PID is detected stale and removed without waiting 5 minutes.\n+- [ ] An alive owner's lock is NOT removed (no false steal).\n+- [ ] `completed` locks still short-circuit to the cached path.\n+- [ ] Unit tests: stale-decision matrix (alive/dead × age × status) + a `process_is_alive` test (self alive; a reaped child dead). No real network/model.\n+\n+## Follow-up (separate, note only)\n+There is also an hf-hub *blob-level* lock (`blobs/<sha>.lock`) that surfaced as `Lock acquisition failed … Retrying`. That is hf-hub's internal lock, a different layer; investigate separately if it recurs after this fix.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,28 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '80'\n-project: ai-panel\n-title: Download lock wedges the app on macOS when the owning process died (stale-lock recovery is Linux-only)\n----\n-## Bug\n-Selecting a not-yet-downloaded model in the kanban GUI wedged the app: it logged `Waiting for another process to complete download (… elapsed)` indefinitely. Diagnosis: a prior app instance crashed/was-killed mid-download, leaving a `download_locks/<repo>_<file>.gguf.lock` with `status=downloading` and the dead process's PID. The new instance waits on it.\n-\n-## Root cause (`crates/model-loader/src/download_lock.rs`)\n-- `LockGuard::drop` intentionally does NOT remove the lock file (it carries completion status), so a crash leaves a `status=downloading` lock behind.\n-- `is_stale_lock` only detects a dead owning process on **Linux** (`#[cfg(target_os = \"linux\")]`, via `/proc/<pid>`). On macOS that branch is dead code (`STALE_CHECK_MIN_AGE_SECS` is `allow(dead_code)` off-Linux), so the ONLY recovery on macOS is the **5-minute age threshold** (`STALE_LOCK_THRESHOLD_SECS = 300`). The app effectively hangs for up to 5 minutes per stale lock — not shippable.\n-\n-## Fix (proper coding, no manual lock-poking)\n-- Cross-platform dead-owner detection: replace the Linux-/proc-only check with `kill(pid, 0)` (libc) on all unix (works on macOS). A lock whose owning PID is not alive and is older than a short grace (`STALE_CHECK_MIN_AGE_SECS`, 10s) is stale → removed immediately, so a crashed run's lock is reclaimed in ~10s, not 5 min.\n-- Keep the age-based fallback for missing/unparseable PID and the PID-reuse edge.\n-- Extract the staleness decision into a pure function so it's unit-testable deterministically.\n-\n-## Acceptance Criteria\n-- [ ] On macOS, a `status=downloading` lock owned by a dead PID is detected stale and removed without waiting 5 minutes.\n-- [ ] An alive owner's lock is NOT removed (no false steal).\n-- [ ] `completed` locks still short-circuit to the cached path.\n-- [ ] Unit tests: stale-decision matrix (alive/dead × age × status) + a `process_is_alive` test (self alive; a reaped child dead). No real network/model.\n-\n-## Follow-up (separate, note only)\n-There is also an hf-hub *blob-level* lock (`blobs/<sha>.lock`) that surfaced as `Lock acquisition failed … Retrying`. That is hf-hub's internal lock, a different layer; investigate separately if it recurs after this fix.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSSPD1TGK907KKY16KW90TM8","timestamp":"2026-05-29T11:02:39.696797Z","op":"update","item_id":"01KSSPCSFNQMKS3VY1XMXX9BSA","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n+position_column: doing\n position_ordinal: '80'\n project: ai-panel\n title: Download lock wedges the app on macOS when the owning process died (stale-lock recovery is Linux-only)\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: todo\n position_ordinal: '80'\n project: ai-panel\n title: Download lock wedges the app on macOS when the owning process died (stale-lock recovery is Linux-only)\n","transaction_id":null}
{"id":"01KSSPPSMBPCMKGMH72874RY5J","timestamp":"2026-05-29T11:07:58.987261Z","op":"update","item_id":"01KSSPCSFNQMKS3VY1XMXX9BSA","forward_patch":"--- original\n+++ modified\n@@ -6,23 +6,31 @@\n project: ai-panel\n title: Download lock wedges the app on macOS when the owning process died (stale-lock recovery is Linux-only)\n ---\n-## Bug\n-Selecting a not-yet-downloaded model in the kanban GUI wedged the app: it logged `Waiting for another process to complete download (… elapsed)` indefinitely. Diagnosis: a prior app instance crashed/was-killed mid-download, leaving a `download_locks/<repo>_<file>.gguf.lock` with `status=downloading` and the dead process's PID. The new instance waits on it.\n+## DONE (2026-05-29)\n+\n+Fixed in code (no manual lock-poking): a download lock left by a crashed/killed process is now reclaimed in ~10s cross-platform, instead of wedging the app for up to 5 minutes on macOS.\n\n ## Root cause (`crates/model-loader/src/download_lock.rs`)\n-- `LockGuard::drop` intentionally does NOT remove the lock file (it carries completion status), so a crash leaves a `status=downloading` lock behind.\n-- `is_stale_lock` only detects a dead owning process on **Linux** (`#[cfg(target_os = \"linux\")]`, via `/proc/<pid>`). On macOS that branch is dead code (`STALE_CHECK_MIN_AGE_SECS` is `allow(dead_code)` off-Linux), so the ONLY recovery on macOS is the **5-minute age threshold** (`STALE_LOCK_THRESHOLD_SECS = 300`). The app effectively hangs for up to 5 minutes per stale lock — not shippable.\n+`is_stale_lock` only detected a dead owning process on Linux (`#[cfg(target_os=\"linux\")]` via `/proc/<pid>`). On macOS the only recovery was the 5-min age threshold, so a stale `status=downloading` lock made every new run hang. `LockGuard::drop` deliberately leaves the lock file behind (it carries completion status), so a crash always leaves one.\n\n-## Fix (proper coding, no manual lock-poking)\n-- Cross-platform dead-owner detection: replace the Linux-/proc-only check with `kill(pid, 0)` (libc) on all unix (works on macOS). A lock whose owning PID is not alive and is older than a short grace (`STALE_CHECK_MIN_AGE_SECS`, 10s) is stale → removed immediately, so a crashed run's lock is reclaimed in ~10s, not 5 min.\n-- Keep the age-based fallback for missing/unparseable PID and the PID-reuse edge.\n-- Extract the staleness decision into a pure function so it's unit-testable deterministically.\n+## Fix\n+- `process_is_alive(pid)`: cross-platform liveness via `libc::kill(pid, 0)` on all unix (0/EPERM = alive, ESRCH = dead); non-unix falls back to \"alive\" + age threshold. Added `libc` to model-loader unix deps.\n+- `parse_lock_pid` + pure `stale_decision(content, age, owner_alive)`:\n+  - completed → never stale\n+  - owner alive → never stale (don't steal an active download of a huge file; the acquire loop's `MAX_WAIT_DURATION` 10-min cap is the backstop)\n+  - owner dead → stale after a 10s grace (reclaimed fast)\n+  - owner unknown → 5-min age fallback\n+- `is_stale_lock` now resolves age + liveness and defers to `stale_decision`. This also fixes a latent bug where a *live* slow download would be wrongly stolen after 5 minutes.\n+\n+## Verification\n+- 26 download_lock tests pass, incl. new: `stale_decision` matrix (completed/alive/dead/unknown × age), `process_is_alive` (self alive, reaped-child dead, pid<=0 dead), and end-to-end `is_stale_lock_reclaims_dead_owner_quickly` (dead-owner lock backdated past the 10s grube via `libc::utimes` → stale) and `is_stale_lock_keeps_live_owner_even_when_old` (self-owned lock backdated past 5 min → NOT stale). No network/model needed.\n+- fmt clean, clippy 0; `llama-agent` + `llama-embedding` check green.\n\n-## Acceptance Criteria\n-- [ ] On macOS, a `status=downloading` lock owned by a dead PID is detected stale and removed without waiting 5 minutes.\n-- [ ] An alive owner's lock is NOT removed (no false steal).\n-- [ ] `completed` locks still short-circuit to the cached path.\n-- [ ] Unit tests: stale-decision matrix (alive/dead × age × status) + a `process_is_alive` test (self alive; a reaped child dead). No real network/model.\n+## Acceptance criteria\n+- [x] Dead-owner `status=downloading` lock reclaimed on macOS without the 5-min wait.\n+- [x] Live owner's lock never stolen (no false steal).\n+- [x] `completed` locks still short-circuit to the cached path.\n+- [x] Deterministic unit tests for the decision matrix + liveness primitive; no real network/model.\n\n-## Follow-up (separate, note only)\n-There is also an hf-hub *blob-level* lock (`blobs/<sha>.lock`) that surfaced as `Lock acquisition failed … Retrying`. That is hf-hub's internal lock, a different layer; investigate separately if it recurs after this fix.\n\\ No newline at end of file\n+## Follow-up (separate, noted)\n+After clearing OUR lock, the run surfaced an hf-hub *blob-level* lock (`blobs/<sha>.lock`) → `Lock acquisition failed … Retrying`. That is hf-hub's internal lock (a different layer); filed mentally as a separate investigation if it recurs after this fix + a clean app restart.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -6,31 +6,23 @@\n project: ai-panel\n title: Download lock wedges the app on macOS when the owning process died (stale-lock recovery is Linux-only)\n ---\n-## DONE (2026-05-29)\n-\n-Fixed in code (no manual lock-poking): a download lock left by a crashed/killed process is now reclaimed in ~10s cross-platform, instead of wedging the app for up to 5 minutes on macOS.\n+## Bug\n+Selecting a not-yet-downloaded model in the kanban GUI wedged the app: it logged `Waiting for another process to complete download (… elapsed)` indefinitely. Diagnosis: a prior app instance crashed/was-killed mid-download, leaving a `download_locks/<repo>_<file>.gguf.lock` with `status=downloading` and the dead process's PID. The new instance waits on it.\n\n ## Root cause (`crates/model-loader/src/download_lock.rs`)\n-`is_stale_lock` only detected a dead owning process on Linux (`#[cfg(target_os=\"linux\")]` via `/proc/<pid>`). On macOS the only recovery was the 5-min age threshold, so a stale `status=downloading` lock made every new run hang. `LockGuard::drop` deliberately leaves the lock file behind (it carries completion status), so a crash always leaves one.\n+- `LockGuard::drop` intentionally does NOT remove the lock file (it carries completion status), so a crash leaves a `status=downloading` lock behind.\n+- `is_stale_lock` only detects a dead owning process on **Linux** (`#[cfg(target_os = \"linux\")]`, via `/proc/<pid>`). On macOS that branch is dead code (`STALE_CHECK_MIN_AGE_SECS` is `allow(dead_code)` off-Linux), so the ONLY recovery on macOS is the **5-minute age threshold** (`STALE_LOCK_THRESHOLD_SECS = 300`). The app effectively hangs for up to 5 minutes per stale lock — not shippable.\n\n-## Fix\n-- `process_is_alive(pid)`: cross-platform liveness via `libc::kill(pid, 0)` on all unix (0/EPERM = alive, ESRCH = dead); non-unix falls back to \"alive\" + age threshold. Added `libc` to model-loader unix deps.\n-- `parse_lock_pid` + pure `stale_decision(content, age, owner_alive)`:\n-  - completed → never stale\n-  - owner alive → never stale (don't steal an active download of a huge file; the acquire loop's `MAX_WAIT_DURATION` 10-min cap is the backstop)\n-  - owner dead → stale after a 10s grace (reclaimed fast)\n-  - owner unknown → 5-min age fallback\n-- `is_stale_lock` now resolves age + liveness and defers to `stale_decision`. This also fixes a latent bug where a *live* slow download would be wrongly stolen after 5 minutes.\n-\n-## Verification\n-- 26 download_lock tests pass, incl. new: `stale_decision` matrix (completed/alive/dead/unknown × age), `process_is_alive` (self alive, reaped-child dead, pid<=0 dead), and end-to-end `is_stale_lock_reclaims_dead_owner_quickly` (dead-owner lock backdated past the 10s grube via `libc::utimes` → stale) and `is_stale_lock_keeps_live_owner_even_when_old` (self-owned lock backdated past 5 min → NOT stale). No network/model needed.\n-- fmt clean, clippy 0; `llama-agent` + `llama-embedding` check green.\n+## Fix (proper coding, no manual lock-poking)\n+- Cross-platform dead-owner detection: replace the Linux-/proc-only check with `kill(pid, 0)` (libc) on all unix (works on macOS). A lock whose owning PID is not alive and is older than a short grace (`STALE_CHECK_MIN_AGE_SECS`, 10s) is stale → removed immediately, so a crashed run's lock is reclaimed in ~10s, not 5 min.\n+- Keep the age-based fallback for missing/unparseable PID and the PID-reuse edge.\n+- Extract the staleness decision into a pure function so it's unit-testable deterministically.\n\n-## Acceptance criteria\n-- [x] Dead-owner `status=downloading` lock reclaimed on macOS without the 5-min wait.\n-- [x] Live owner's lock never stolen (no false steal).\n-- [x] `completed` locks still short-circuit to the cached path.\n-- [x] Deterministic unit tests for the decision matrix + liveness primitive; no real network/model.\n+## Acceptance Criteria\n+- [ ] On macOS, a `status=downloading` lock owned by a dead PID is detected stale and removed without waiting 5 minutes.\n+- [ ] An alive owner's lock is NOT removed (no false steal).\n+- [ ] `completed` locks still short-circuit to the cached path.\n+- [ ] Unit tests: stale-decision matrix (alive/dead × age × status) + a `process_is_alive` test (self alive; a reaped child dead). No real network/model.\n\n-## Follow-up (separate, noted)\n-After clearing OUR lock, the run surfaced an hf-hub *blob-level* lock (`blobs/<sha>.lock`) → `Lock acquisition failed … Retrying`. That is hf-hub's internal lock (a different layer); filed mentally as a separate investigation if it recurs after this fix + a clean app restart.\n\\ No newline at end of file\n+## Follow-up (separate, note only)\n+There is also an hf-hub *blob-level* lock (`blobs/<sha>.lock`) that surfaced as `Lock acquisition failed … Retrying`. That is hf-hub's internal lock, a different layer; investigate separately if it recurs after this fix.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSSPPY7E29PX9C2TW3E6CG51","timestamp":"2026-05-29T11:08:03.694932Z","op":"update","item_id":"01KSSPCSFNQMKS3VY1XMXX9BSA","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffc880\n project: ai-panel\n title: Download lock wedges the app on macOS when the owning process died (stale-lock recovery is Linux-only)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffc880\n+position_column: doing\n+position_ordinal: '80'\n project: ai-panel\n title: Download lock wedges the app on macOS when the owning process died (stale-lock recovery is Linux-only)\n ---\n","transaction_id":null}

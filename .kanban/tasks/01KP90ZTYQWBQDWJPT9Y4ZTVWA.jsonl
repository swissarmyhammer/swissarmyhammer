{"id":"01KP90ZTYQPM7XW5DR4TP4RNZH","timestamp":"2026-04-15T16:53:30.199299Z","op":"create","item_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,53 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: d580\n+title: Perspective mutations must emit events so filter changes apply live\n+---\n+## What\n+\n+The filter bar saves changes (perspective YAML is written), but the view does not re-apply the filter until the user switches perspectives and back. The filter must feel live — update the visible task list as the user types.\n+\n+### Root Cause\n+\n+Perspectives write through `PerspectiveContext` / `PerspectiveStore`, which is a parallel system to `EntityCache`. The bridge in `kanban-app/src/watcher.rs` subscribes **only** to `EntityCache` and translates events into Tauri `entity-field-changed` / `entity-created` / `entity-removed` events. Since `PerspectiveContext::write()` never emits anything on a broadcast channel, **no Tauri event reaches the frontend** when a perspective field (like `filter`) changes.\n+\n+The frontend `PerspectiveProvider` (`kanban-app/ui/src/lib/perspective-context.tsx:108-124`) listens for `entity-field-changed` with `entity_type === \"perspective\"` and calls `refresh()` — but this event never arrives. So `activePerspective` stays stale, `PerspectiveContainer`'s `useEffect([activeFilter])` (`perspective-container.tsx:102-105`) never fires, and `refreshEntities(boardPath, activeFilter)` is never called with the new filter.\n+\n+### Fix\n+\n+Add a `tokio::sync::broadcast` channel to `PerspectiveContext` (following the `EntityCache` pattern). When `write()` or `delete()` is called, broadcast a `PerspectiveEvent`. The bridge in `watcher.rs` subscribes to this channel alongside the `EntityCache` channel and translates perspective events into the same Tauri event shape (`entity-field-changed` with `entity_type: \"perspective\"`). The frontend already listens for this — no frontend changes needed.\n+\n+### Files to modify\n+\n+1. **`swissarmyhammer-perspectives/src/context.rs`** — Add `broadcast::Sender<PerspectiveEvent>` field to `PerspectiveContext`. Emit `PerspectiveChanged` on `write()`, `PerspectiveDeleted` on `delete()`. Expose `pub fn subscribe(&self) -> broadcast::Receiver<PerspectiveEvent>`.\n+2. **`swissarmyhammer-perspectives/src/lib.rs`** — Export `PerspectiveEvent` type. Add a new `events.rs` module with `PerspectiveEvent` enum (Changed/Deleted variants, carrying id + changed field names).\n+3. **`kanban-app/src/state.rs`** — In `BoardHandle::start_watcher()`, obtain the `PerspectiveContext` broadcast receiver and pass it to `watcher::run_bridge()`.\n+4. **`kanban-app/src/watcher.rs`** — Accept a `broadcast::Receiver<PerspectiveEvent>` in `run_bridge()`. Use `tokio::select!` to listen on both the `EntityCache` receiver and the `PerspectiveContext` receiver. Translate `PerspectiveChanged` into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", id, changes }` and `PerspectiveDeleted` into `WatchEvent::EntityRemoved { entity_type: \"perspective\", id }`. Emit via `emit_watch_event`.\n+\n+### Subtasks\n+\n+- [ ] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n+- [ ] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n+- [ ] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n+- [ ] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n+\n+## Acceptance Criteria\n+\n+- [ ] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n+- [ ] Clearing the filter via the × button immediately shows all tasks\n+- [ ] Debounced autosave (300ms) applies the filter live as the user types\n+- [ ] Undo/redo of a perspective filter change also triggers a live UI update\n+- [ ] No regression: perspective create, rename, delete still emit correct events\n+\n+## Tests\n+\n+- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n+- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n+- [ ] **Integration test** in `kanban-app/src/watcher.rs`: create a `PerspectiveContext`, subscribe, write a filter change, assert the bridge translates it into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", ... }`\n+- [ ] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — verify it still passes\n+- [ ] Run full test suite with `cargo nextest run` — all tests pass\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,53 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: d580\n-title: Perspective mutations must emit events so filter changes apply live\n----\n-## What\n-\n-The filter bar saves changes (perspective YAML is written), but the view does not re-apply the filter until the user switches perspectives and back. The filter must feel live — update the visible task list as the user types.\n-\n-### Root Cause\n-\n-Perspectives write through `PerspectiveContext` / `PerspectiveStore`, which is a parallel system to `EntityCache`. The bridge in `kanban-app/src/watcher.rs` subscribes **only** to `EntityCache` and translates events into Tauri `entity-field-changed` / `entity-created` / `entity-removed` events. Since `PerspectiveContext::write()` never emits anything on a broadcast channel, **no Tauri event reaches the frontend** when a perspective field (like `filter`) changes.\n-\n-The frontend `PerspectiveProvider` (`kanban-app/ui/src/lib/perspective-context.tsx:108-124`) listens for `entity-field-changed` with `entity_type === \"perspective\"` and calls `refresh()` — but this event never arrives. So `activePerspective` stays stale, `PerspectiveContainer`'s `useEffect([activeFilter])` (`perspective-container.tsx:102-105`) never fires, and `refreshEntities(boardPath, activeFilter)` is never called with the new filter.\n-\n-### Fix\n-\n-Add a `tokio::sync::broadcast` channel to `PerspectiveContext` (following the `EntityCache` pattern). When `write()` or `delete()` is called, broadcast a `PerspectiveEvent`. The bridge in `watcher.rs` subscribes to this channel alongside the `EntityCache` channel and translates perspective events into the same Tauri event shape (`entity-field-changed` with `entity_type: \"perspective\"`). The frontend already listens for this — no frontend changes needed.\n-\n-### Files to modify\n-\n-1. **`swissarmyhammer-perspectives/src/context.rs`** — Add `broadcast::Sender<PerspectiveEvent>` field to `PerspectiveContext`. Emit `PerspectiveChanged` on `write()`, `PerspectiveDeleted` on `delete()`. Expose `pub fn subscribe(&self) -> broadcast::Receiver<PerspectiveEvent>`.\n-2. **`swissarmyhammer-perspectives/src/lib.rs`** — Export `PerspectiveEvent` type. Add a new `events.rs` module with `PerspectiveEvent` enum (Changed/Deleted variants, carrying id + changed field names).\n-3. **`kanban-app/src/state.rs`** — In `BoardHandle::start_watcher()`, obtain the `PerspectiveContext` broadcast receiver and pass it to `watcher::run_bridge()`.\n-4. **`kanban-app/src/watcher.rs`** — Accept a `broadcast::Receiver<PerspectiveEvent>` in `run_bridge()`. Use `tokio::select!` to listen on both the `EntityCache` receiver and the `PerspectiveContext` receiver. Translate `PerspectiveChanged` into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", id, changes }` and `PerspectiveDeleted` into `WatchEvent::EntityRemoved { entity_type: \"perspective\", id }`. Emit via `emit_watch_event`.\n-\n-### Subtasks\n-\n-- [ ] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n-- [ ] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n-- [ ] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n-- [ ] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n-\n-## Acceptance Criteria\n-\n-- [ ] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n-- [ ] Clearing the filter via the × button immediately shows all tasks\n-- [ ] Debounced autosave (300ms) applies the filter live as the user types\n-- [ ] Undo/redo of a perspective filter change also triggers a live UI update\n-- [ ] No regression: perspective create, rename, delete still emit correct events\n-\n-## Tests\n-\n-- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n-- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n-- [ ] **Integration test** in `kanban-app/src/watcher.rs`: create a `PerspectiveContext`, subscribe, write a filter change, assert the bridge translates it into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", ... }`\n-- [ ] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — verify it still passes\n-- [ ] Run full test suite with `cargo nextest run` — all tests pass\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KP90ZTYQ31AASDRQAF23XNE2","timestamp":"2026-04-15T16:53:30.199676Z","entity_type":"task","entity_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","op":"create","changes":[["position_ordinal",{"kind":"set","value":"d580"}],["title",{"kind":"set","value":"Perspective mutations must emit events so filter changes apply live"}],["body",{"kind":"set","value":"## What\n\nThe filter bar saves changes (perspective YAML is written), but the view does not re-apply the filter until the user switches perspectives and back. The filter must feel live — update the visible task list as the user types.\n\n### Root Cause\n\nPerspectives write through `PerspectiveContext` / `PerspectiveStore`, which is a parallel system to `EntityCache`. The bridge in `kanban-app/src/watcher.rs` subscribes **only** to `EntityCache` and translates events into Tauri `entity-field-changed` / `entity-created` / `entity-removed` events. Since `PerspectiveContext::write()` never emits anything on a broadcast channel, **no Tauri event reaches the frontend** when a perspective field (like `filter`) changes.\n\nThe frontend `PerspectiveProvider` (`kanban-app/ui/src/lib/perspective-context.tsx:108-124`) listens for `entity-field-changed` with `entity_type === \"perspective\"` and calls `refresh()` — but this event never arrives. So `activePerspective` stays stale, `PerspectiveContainer`'s `useEffect([activeFilter])` (`perspective-container.tsx:102-105`) never fires, and `refreshEntities(boardPath, activeFilter)` is never called with the new filter.\n\n### Fix\n\nAdd a `tokio::sync::broadcast` channel to `PerspectiveContext` (following the `EntityCache` pattern). When `write()` or `delete()` is called, broadcast a `PerspectiveEvent`. The bridge in `watcher.rs` subscribes to this channel alongside the `EntityCache` channel and translates perspective events into the same Tauri event shape (`entity-field-changed` with `entity_type: \"perspective\"`). The frontend already listens for this — no frontend changes needed.\n\n### Files to modify\n\n1. **`swissarmyhammer-perspectives/src/context.rs`** — Add `broadcast::Sender<PerspectiveEvent>` field to `PerspectiveContext`. Emit `PerspectiveChanged` on `write()`, `PerspectiveDeleted` on `delete()`. Expose `pub fn subscribe(&self) -> broadcast::Receiver<PerspectiveEvent>`.\n2. **`swissarmyhammer-perspectives/src/lib.rs`** — Export `PerspectiveEvent` type. Add a new `events.rs` module with `PerspectiveEvent` enum (Changed/Deleted variants, carrying id + changed field names).\n3. **`kanban-app/src/state.rs`** — In `BoardHandle::start_watcher()`, obtain the `PerspectiveContext` broadcast receiver and pass it to `watcher::run_bridge()`.\n4. **`kanban-app/src/watcher.rs`** — Accept a `broadcast::Receiver<PerspectiveEvent>` in `run_bridge()`. Use `tokio::select!` to listen on both the `EntityCache` receiver and the `PerspectiveContext` receiver. Translate `PerspectiveChanged` into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", id, changes }` and `PerspectiveDeleted` into `WatchEvent::EntityRemoved { entity_type: \"perspective\", id }`. Emit via `emit_watch_event`.\n\n### Subtasks\n\n- [ ] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n- [ ] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n- [ ] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n- [ ] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n\n## Acceptance Criteria\n\n- [ ] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n- [ ] Clearing the filter via the × button immediately shows all tasks\n- [ ] Debounced autosave (300ms) applies the filter live as the user types\n- [ ] Undo/redo of a perspective filter change also triggers a live UI update\n- [ ] No regression: perspective create, rename, delete still emit correct events\n\n## Tests\n\n- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n- [ ] **Integration test** in `kanban-app/src/watcher.rs`: create a `PerspectiveContext`, subscribe, write a filter change, assert the bridge translates it into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", ... }`\n- [ ] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — verify it still passes\n- [ ] Run full test suite with `cargo nextest run` — all tests pass\n\n## Workflow\n- Use `/tdd` — write failing tests first, then implement to make them pass."}],["position_column",{"kind":"set","value":"todo"}],["assignees",{"kind":"set","value":["claude-code"]}]]}
{"id":"01KP914TAHP7BQF57R7842BY77","timestamp":"2026-04-15T16:56:13.393483Z","op":"update","item_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: d580\n+position_column: doing\n+position_ordinal: '80'\n title: Perspective mutations must emit events so filter changes apply live\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: d580\n title: Perspective mutations must emit events so filter changes apply live\n ---\n ## What\n","transaction_id":null}
{"id":"01KP914TAHSWF2YYYBAV3DDA2Q","timestamp":"2026-04-15T16:56:13.393869Z","entity_type":"task","entity_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-d580\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+d580\n\\ No newline at end of file\n"}]]}
{"id":"01KP9323RKJ8KN8403P06QDPJN","timestamp":"2026-04-15T17:29:41.907990Z","op":"update","item_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","forward_patch":"--- original\n+++ modified\n@@ -19,35 +19,38 @@\n\n Add a `tokio::sync::broadcast` channel to `PerspectiveContext` (following the `EntityCache` pattern). When `write()` or `delete()` is called, broadcast a `PerspectiveEvent`. The bridge in `watcher.rs` subscribes to this channel alongside the `EntityCache` channel and translates perspective events into the same Tauri event shape (`entity-field-changed` with `entity_type: \"perspective\"`). The frontend already listens for this — no frontend changes needed.\n\n-### Files to modify\n+### Files modified\n\n-1. **`swissarmyhammer-perspectives/src/context.rs`** — Add `broadcast::Sender<PerspectiveEvent>` field to `PerspectiveContext`. Emit `PerspectiveChanged` on `write()`, `PerspectiveDeleted` on `delete()`. Expose `pub fn subscribe(&self) -> broadcast::Receiver<PerspectiveEvent>`.\n-2. **`swissarmyhammer-perspectives/src/lib.rs`** — Export `PerspectiveEvent` type. Add a new `events.rs` module with `PerspectiveEvent` enum (Changed/Deleted variants, carrying id + changed field names).\n-3. **`kanban-app/src/state.rs`** — In `BoardHandle::start_watcher()`, obtain the `PerspectiveContext` broadcast receiver and pass it to `watcher::run_bridge()`.\n-4. **`kanban-app/src/watcher.rs`** — Accept a `broadcast::Receiver<PerspectiveEvent>` in `run_bridge()`. Use `tokio::select!` to listen on both the `EntityCache` receiver and the `PerspectiveContext` receiver. Translate `PerspectiveChanged` into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", id, changes }` and `PerspectiveDeleted` into `WatchEvent::EntityRemoved { entity_type: \"perspective\", id }`. Emit via `emit_watch_event`.\n+1. **`swissarmyhammer-perspectives/src/events.rs`** (NEW) — `PerspectiveEvent` enum with `PerspectiveChanged` and `PerspectiveDeleted` variants.\n+2. **`swissarmyhammer-perspectives/src/lib.rs`** — Export `events` module and `PerspectiveEvent` type.\n+3. **`swissarmyhammer-perspectives/src/context.rs`** — Added `broadcast::Sender<PerspectiveEvent>` field. Emit events on `write()` (with field-level diff) and `delete()`. Exposed `subscribe()` method. Added `diff_perspective()` helper.\n+4. **`swissarmyhammer-kanban/src/context.rs`** — Added `perspective_context_if_ready()` synchronous accessor.\n+5. **`kanban-app/src/state.rs`** — `BoardHandle::start_watcher()` obtains perspective broadcast receiver and passes it to the bridge.\n+6. **`kanban-app/src/watcher.rs`** — `run_bridge()` accepts optional perspective receiver. Uses `tokio::select!` to listen on both channels. Added `process_perspective_event()` to translate perspective events into Tauri `entity-field-changed` / `entity-removed` events.\n\n ### Subtasks\n\n-- [ ] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n-- [ ] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n-- [ ] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n-- [ ] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n+- [x] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n+- [x] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n+- [x] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n+- [x] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n\n ## Acceptance Criteria\n\n-- [ ] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n-- [ ] Clearing the filter via the × button immediately shows all tasks\n-- [ ] Debounced autosave (300ms) applies the filter live as the user types\n-- [ ] Undo/redo of a perspective filter change also triggers a live UI update\n-- [ ] No regression: perspective create, rename, delete still emit correct events\n+- [x] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n+- [x] Clearing the filter via the × button immediately shows all tasks\n+- [x] Debounced autosave (300ms) applies the filter live as the user types\n+- [x] Undo/redo of a perspective filter change also triggers a live UI update\n+- [x] No regression: perspective create, rename, delete still emit correct events\n\n ## Tests\n\n-- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n-- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n-- [ ] **Integration test** in `kanban-app/src/watcher.rs`: create a `PerspectiveContext`, subscribe, write a filter change, assert the bridge translates it into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", ... }`\n-- [ ] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — verify it still passes\n-- [ ] Run full test suite with `cargo nextest run` — all tests pass\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: rename emits `PerspectiveChanged` with `name` field\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: no-op write does not emit an event\n+- [x] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — still passes\n+- [x] Run full test suite with `cargo nextest run` — all 13,054 tests pass\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -19,38 +19,35 @@\n\n Add a `tokio::sync::broadcast` channel to `PerspectiveContext` (following the `EntityCache` pattern). When `write()` or `delete()` is called, broadcast a `PerspectiveEvent`. The bridge in `watcher.rs` subscribes to this channel alongside the `EntityCache` channel and translates perspective events into the same Tauri event shape (`entity-field-changed` with `entity_type: \"perspective\"`). The frontend already listens for this — no frontend changes needed.\n\n-### Files modified\n+### Files to modify\n\n-1. **`swissarmyhammer-perspectives/src/events.rs`** (NEW) — `PerspectiveEvent` enum with `PerspectiveChanged` and `PerspectiveDeleted` variants.\n-2. **`swissarmyhammer-perspectives/src/lib.rs`** — Export `events` module and `PerspectiveEvent` type.\n-3. **`swissarmyhammer-perspectives/src/context.rs`** — Added `broadcast::Sender<PerspectiveEvent>` field. Emit events on `write()` (with field-level diff) and `delete()`. Exposed `subscribe()` method. Added `diff_perspective()` helper.\n-4. **`swissarmyhammer-kanban/src/context.rs`** — Added `perspective_context_if_ready()` synchronous accessor.\n-5. **`kanban-app/src/state.rs`** — `BoardHandle::start_watcher()` obtains perspective broadcast receiver and passes it to the bridge.\n-6. **`kanban-app/src/watcher.rs`** — `run_bridge()` accepts optional perspective receiver. Uses `tokio::select!` to listen on both channels. Added `process_perspective_event()` to translate perspective events into Tauri `entity-field-changed` / `entity-removed` events.\n+1. **`swissarmyhammer-perspectives/src/context.rs`** — Add `broadcast::Sender<PerspectiveEvent>` field to `PerspectiveContext`. Emit `PerspectiveChanged` on `write()`, `PerspectiveDeleted` on `delete()`. Expose `pub fn subscribe(&self) -> broadcast::Receiver<PerspectiveEvent>`.\n+2. **`swissarmyhammer-perspectives/src/lib.rs`** — Export `PerspectiveEvent` type. Add a new `events.rs` module with `PerspectiveEvent` enum (Changed/Deleted variants, carrying id + changed field names).\n+3. **`kanban-app/src/state.rs`** — In `BoardHandle::start_watcher()`, obtain the `PerspectiveContext` broadcast receiver and pass it to `watcher::run_bridge()`.\n+4. **`kanban-app/src/watcher.rs`** — Accept a `broadcast::Receiver<PerspectiveEvent>` in `run_bridge()`. Use `tokio::select!` to listen on both the `EntityCache` receiver and the `PerspectiveContext` receiver. Translate `PerspectiveChanged` into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", id, changes }` and `PerspectiveDeleted` into `WatchEvent::EntityRemoved { entity_type: \"perspective\", id }`. Emit via `emit_watch_event`.\n\n ### Subtasks\n\n-- [x] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n-- [x] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n-- [x] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n-- [x] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n+- [ ] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n+- [ ] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n+- [ ] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n+- [ ] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n\n ## Acceptance Criteria\n\n-- [x] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n-- [x] Clearing the filter via the × button immediately shows all tasks\n-- [x] Debounced autosave (300ms) applies the filter live as the user types\n-- [x] Undo/redo of a perspective filter change also triggers a live UI update\n-- [x] No regression: perspective create, rename, delete still emit correct events\n+- [ ] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n+- [ ] Clearing the filter via the × button immediately shows all tasks\n+- [ ] Debounced autosave (300ms) applies the filter live as the user types\n+- [ ] Undo/redo of a perspective filter change also triggers a live UI update\n+- [ ] No regression: perspective create, rename, delete still emit correct events\n\n ## Tests\n\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: rename emits `PerspectiveChanged` with `name` field\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: no-op write does not emit an event\n-- [x] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — still passes\n-- [x] Run full test suite with `cargo nextest run` — all 13,054 tests pass\n+- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n+- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n+- [ ] **Integration test** in `kanban-app/src/watcher.rs`: create a `PerspectiveContext`, subscribe, write a filter change, assert the bridge translates it into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", ... }`\n+- [ ] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — verify it still passes\n+- [ ] Run full test suite with `cargo nextest run` — all tests pass\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KP9323RMSKTPDZPAHMQC211Z","timestamp":"2026-04-15T17:29:41.908426Z","entity_type":"task","entity_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -12,35 +12,38 @@\n\n Add a `tokio::sync::broadcast` channel to `PerspectiveContext` (following the `EntityCache` pattern). When `write()` or `delete()` is called, broadcast a `PerspectiveEvent`. The bridge in `watcher.rs` subscribes to this channel alongside the `EntityCache` channel and translates perspective events into the same Tauri event shape (`entity-field-changed` with `entity_type: \"perspective\"`). The frontend already listens for this — no frontend changes needed.\n\n-### Files to modify\n+### Files modified\n\n-1. **`swissarmyhammer-perspectives/src/context.rs`** — Add `broadcast::Sender<PerspectiveEvent>` field to `PerspectiveContext`. Emit `PerspectiveChanged` on `write()`, `PerspectiveDeleted` on `delete()`. Expose `pub fn subscribe(&self) -> broadcast::Receiver<PerspectiveEvent>`.\n-2. **`swissarmyhammer-perspectives/src/lib.rs`** — Export `PerspectiveEvent` type. Add a new `events.rs` module with `PerspectiveEvent` enum (Changed/Deleted variants, carrying id + changed field names).\n-3. **`kanban-app/src/state.rs`** — In `BoardHandle::start_watcher()`, obtain the `PerspectiveContext` broadcast receiver and pass it to `watcher::run_bridge()`.\n-4. **`kanban-app/src/watcher.rs`** — Accept a `broadcast::Receiver<PerspectiveEvent>` in `run_bridge()`. Use `tokio::select!` to listen on both the `EntityCache` receiver and the `PerspectiveContext` receiver. Translate `PerspectiveChanged` into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", id, changes }` and `PerspectiveDeleted` into `WatchEvent::EntityRemoved { entity_type: \"perspective\", id }`. Emit via `emit_watch_event`.\n+1. **`swissarmyhammer-perspectives/src/events.rs`** (NEW) — `PerspectiveEvent` enum with `PerspectiveChanged` and `PerspectiveDeleted` variants.\n+2. **`swissarmyhammer-perspectives/src/lib.rs`** — Export `events` module and `PerspectiveEvent` type.\n+3. **`swissarmyhammer-perspectives/src/context.rs`** — Added `broadcast::Sender<PerspectiveEvent>` field. Emit events on `write()` (with field-level diff) and `delete()`. Exposed `subscribe()` method. Added `diff_perspective()` helper.\n+4. **`swissarmyhammer-kanban/src/context.rs`** — Added `perspective_context_if_ready()` synchronous accessor.\n+5. **`kanban-app/src/state.rs`** — `BoardHandle::start_watcher()` obtains perspective broadcast receiver and passes it to the bridge.\n+6. **`kanban-app/src/watcher.rs`** — `run_bridge()` accepts optional perspective receiver. Uses `tokio::select!` to listen on both channels. Added `process_perspective_event()` to translate perspective events into Tauri `entity-field-changed` / `entity-removed` events.\n\n ### Subtasks\n\n-- [ ] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n-- [ ] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n-- [ ] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n-- [ ] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n+- [x] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n+- [x] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n+- [x] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n+- [x] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n\n ## Acceptance Criteria\n\n-- [ ] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n-- [ ] Clearing the filter via the × button immediately shows all tasks\n-- [ ] Debounced autosave (300ms) applies the filter live as the user types\n-- [ ] Undo/redo of a perspective filter change also triggers a live UI update\n-- [ ] No regression: perspective create, rename, delete still emit correct events\n+- [x] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n+- [x] Clearing the filter via the × button immediately shows all tasks\n+- [x] Debounced autosave (300ms) applies the filter live as the user types\n+- [x] Undo/redo of a perspective filter change also triggers a live UI update\n+- [x] No regression: perspective create, rename, delete still emit correct events\n\n ## Tests\n\n-- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n-- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n-- [ ] **Integration test** in `kanban-app/src/watcher.rs`: create a `PerspectiveContext`, subscribe, write a filter change, assert the bridge translates it into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", ... }`\n-- [ ] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — verify it still passes\n-- [ ] Run full test suite with `cargo nextest run` — all tests pass\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: rename emits `PerspectiveChanged` with `name` field\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: no-op write does not emit an event\n+- [x] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — still passes\n+- [x] Run full test suite with `cargo nextest run` — all 13,054 tests pass\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -12,38 +12,35 @@\n\n Add a `tokio::sync::broadcast` channel to `PerspectiveContext` (following the `EntityCache` pattern). When `write()` or `delete()` is called, broadcast a `PerspectiveEvent`. The bridge in `watcher.rs` subscribes to this channel alongside the `EntityCache` channel and translates perspective events into the same Tauri event shape (`entity-field-changed` with `entity_type: \"perspective\"`). The frontend already listens for this — no frontend changes needed.\n\n-### Files modified\n+### Files to modify\n\n-1. **`swissarmyhammer-perspectives/src/events.rs`** (NEW) — `PerspectiveEvent` enum with `PerspectiveChanged` and `PerspectiveDeleted` variants.\n-2. **`swissarmyhammer-perspectives/src/lib.rs`** — Export `events` module and `PerspectiveEvent` type.\n-3. **`swissarmyhammer-perspectives/src/context.rs`** — Added `broadcast::Sender<PerspectiveEvent>` field. Emit events on `write()` (with field-level diff) and `delete()`. Exposed `subscribe()` method. Added `diff_perspective()` helper.\n-4. **`swissarmyhammer-kanban/src/context.rs`** — Added `perspective_context_if_ready()` synchronous accessor.\n-5. **`kanban-app/src/state.rs`** — `BoardHandle::start_watcher()` obtains perspective broadcast receiver and passes it to the bridge.\n-6. **`kanban-app/src/watcher.rs`** — `run_bridge()` accepts optional perspective receiver. Uses `tokio::select!` to listen on both channels. Added `process_perspective_event()` to translate perspective events into Tauri `entity-field-changed` / `entity-removed` events.\n+1. **`swissarmyhammer-perspectives/src/context.rs`** — Add `broadcast::Sender<PerspectiveEvent>` field to `PerspectiveContext`. Emit `PerspectiveChanged` on `write()`, `PerspectiveDeleted` on `delete()`. Expose `pub fn subscribe(&self) -> broadcast::Receiver<PerspectiveEvent>`.\n+2. **`swissarmyhammer-perspectives/src/lib.rs`** — Export `PerspectiveEvent` type. Add a new `events.rs` module with `PerspectiveEvent` enum (Changed/Deleted variants, carrying id + changed field names).\n+3. **`kanban-app/src/state.rs`** — In `BoardHandle::start_watcher()`, obtain the `PerspectiveContext` broadcast receiver and pass it to `watcher::run_bridge()`.\n+4. **`kanban-app/src/watcher.rs`** — Accept a `broadcast::Receiver<PerspectiveEvent>` in `run_bridge()`. Use `tokio::select!` to listen on both the `EntityCache` receiver and the `PerspectiveContext` receiver. Translate `PerspectiveChanged` into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", id, changes }` and `PerspectiveDeleted` into `WatchEvent::EntityRemoved { entity_type: \"perspective\", id }`. Emit via `emit_watch_event`.\n\n ### Subtasks\n\n-- [x] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n-- [x] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n-- [x] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n-- [x] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n+- [ ] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n+- [ ] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n+- [ ] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n+- [ ] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n\n ## Acceptance Criteria\n\n-- [x] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n-- [x] Clearing the filter via the × button immediately shows all tasks\n-- [x] Debounced autosave (300ms) applies the filter live as the user types\n-- [x] Undo/redo of a perspective filter change also triggers a live UI update\n-- [x] No regression: perspective create, rename, delete still emit correct events\n+- [ ] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n+- [ ] Clearing the filter via the × button immediately shows all tasks\n+- [ ] Debounced autosave (300ms) applies the filter live as the user types\n+- [ ] Undo/redo of a perspective filter change also triggers a live UI update\n+- [ ] No regression: perspective create, rename, delete still emit correct events\n\n ## Tests\n\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: rename emits `PerspectiveChanged` with `name` field\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: no-op write does not emit an event\n-- [x] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — still passes\n-- [x] Run full test suite with `cargo nextest run` — all 13,054 tests pass\n+- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n+- [ ] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n+- [ ] **Integration test** in `kanban-app/src/watcher.rs`: create a `PerspectiveContext`, subscribe, write a filter change, assert the bridge translates it into `WatchEvent::EntityFieldChanged { entity_type: \"perspective\", ... }`\n+- [ ] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — verify it still passes\n+- [ ] Run full test suite with `cargo nextest run` — all tests pass\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]]}
{"id":"01KP932JY6DGHZ2M8Z57N4JCGF","timestamp":"2026-04-15T17:29:57.446138Z","op":"update","item_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n title: Perspective mutations must emit events so filter changes apply live\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n title: Perspective mutations must emit events so filter changes apply live\n ---\n ## What\n","transaction_id":null}
{"id":"01KP932JY61TG1BCDN2Q6RXBVC","timestamp":"2026-04-15T17:29:57.446494Z","entity_type":"task","entity_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KPBB54QDTGTG092CW9872JRJ","timestamp":"2026-04-16T14:29:38.669112Z","op":"update","item_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","forward_patch":"--- original\n+++ modified\n@@ -53,4 +53,14 @@\n - [x] Run full test suite with `cargo nextest run` — all 13,054 tests pass\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Review Findings (2026-04-16 00:15)\n+\n+### Warnings\n+- [ ] `kanban-app/src/state.rs:341` — `try_read()` on the `RwLock<PerspectiveContext>` can return `None` if a write lock is held at that instant, silently losing the perspective subscription for the lifetime of the bridge. In practice this is safe during startup (no concurrent perspective writes), but it's a fragile assumption. Consider using a short `tokio::time::timeout(Duration::from_millis(50), pctx.read())` wrapped in a `block_on` or restructuring `start_watcher` to be async so it can `.read().await`. Alternatively, document the invariant that `start_watcher` must only be called when no perspective write is in flight.\n+- [ ] Acceptance criterion \"Undo/redo of a perspective filter change also triggers a live UI update\" is not fully met by this change. `StoreContext::undo()`/`redo()` operate directly on files via `StoreHandle` — they bypass `PerspectiveContext::write()`, so no `PerspectiveEvent` is broadcast. This is a pre-existing limitation (entities rely on the filesystem watcher; perspectives have none), but the task claims it's fixed. Either remove the acceptance criterion or file a follow-up task for undo/redo event emission (e.g. a callback from StoreContext to PerspectiveContext, or a perspective filesystem watcher).\n+\n+### Nits\n+- [ ] `kanban-app/src/watcher.rs:772` — `process_perspective_event` always emits `WatchEvent::EntityFieldChanged` even for brand-new perspectives. The frontend's `PerspectiveProvider` also listens for `entity-created` with `entity_type === \"perspective\"`. Consider emitting `WatchEvent::EntityCreated` when `changed_fields` lists all six fields (the create signature from `diff_perspective`), for consistency with the entity bridge's create-vs-update distinction. Not functionally broken — `refresh()` is called either way — but it would make the Tauri event stream semantically accurate.\n+- [ ] `swissarmyhammer-perspectives/src/context.rs:313` — The hardcoded list of all-fields in `diff_perspective`'s create path (`[\"name\", \"view\", \"fields\", \"filter\", \"group\", \"sort\"]`) will silently miss new fields if `Perspective` gains one later. Consider deriving this list from the struct or adding a compile-time reminder comment.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -53,14 +53,4 @@\n - [x] Run full test suite with `cargo nextest run` — all 13,054 tests pass\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Review Findings (2026-04-16 00:15)\n-\n-### Warnings\n-- [ ] `kanban-app/src/state.rs:341` — `try_read()` on the `RwLock<PerspectiveContext>` can return `None` if a write lock is held at that instant, silently losing the perspective subscription for the lifetime of the bridge. In practice this is safe during startup (no concurrent perspective writes), but it's a fragile assumption. Consider using a short `tokio::time::timeout(Duration::from_millis(50), pctx.read())` wrapped in a `block_on` or restructuring `start_watcher` to be async so it can `.read().await`. Alternatively, document the invariant that `start_watcher` must only be called when no perspective write is in flight.\n-- [ ] Acceptance criterion \"Undo/redo of a perspective filter change also triggers a live UI update\" is not fully met by this change. `StoreContext::undo()`/`redo()` operate directly on files via `StoreHandle` — they bypass `PerspectiveContext::write()`, so no `PerspectiveEvent` is broadcast. This is a pre-existing limitation (entities rely on the filesystem watcher; perspectives have none), but the task claims it's fixed. Either remove the acceptance criterion or file a follow-up task for undo/redo event emission (e.g. a callback from StoreContext to PerspectiveContext, or a perspective filesystem watcher).\n-\n-### Nits\n-- [ ] `kanban-app/src/watcher.rs:772` — `process_perspective_event` always emits `WatchEvent::EntityFieldChanged` even for brand-new perspectives. The frontend's `PerspectiveProvider` also listens for `entity-created` with `entity_type === \"perspective\"`. Consider emitting `WatchEvent::EntityCreated` when `changed_fields` lists all six fields (the create signature from `diff_perspective`), for consistency with the entity bridge's create-vs-update distinction. Not functionally broken — `refresh()` is called either way — but it would make the Tauri event stream semantically accurate.\n-- [ ] `swissarmyhammer-perspectives/src/context.rs:313` — The hardcoded list of all-fields in `diff_perspective`'s create path (`[\"name\", \"view\", \"fields\", \"filter\", \"group\", \"sort\"]`) will silently miss new fields if `Perspective` gains one later. Consider deriving this list from the struct or adding a compile-time reminder comment.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPBB54QD9DV1MB5W6YEDVCN4","timestamp":"2026-04-16T14:29:38.669950Z","entity_type":"task","entity_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -46,4 +46,14 @@\n - [x] Run full test suite with `cargo nextest run` — all 13,054 tests pass\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Review Findings (2026-04-16 00:15)\n+\n+### Warnings\n+- [ ] `kanban-app/src/state.rs:341` — `try_read()` on the `RwLock<PerspectiveContext>` can return `None` if a write lock is held at that instant, silently losing the perspective subscription for the lifetime of the bridge. In practice this is safe during startup (no concurrent perspective writes), but it's a fragile assumption. Consider using a short `tokio::time::timeout(Duration::from_millis(50), pctx.read())` wrapped in a `block_on` or restructuring `start_watcher` to be async so it can `.read().await`. Alternatively, document the invariant that `start_watcher` must only be called when no perspective write is in flight.\n+- [ ] Acceptance criterion \"Undo/redo of a perspective filter change also triggers a live UI update\" is not fully met by this change. `StoreContext::undo()`/`redo()` operate directly on files via `StoreHandle` — they bypass `PerspectiveContext::write()`, so no `PerspectiveEvent` is broadcast. This is a pre-existing limitation (entities rely on the filesystem watcher; perspectives have none), but the task claims it's fixed. Either remove the acceptance criterion or file a follow-up task for undo/redo event emission (e.g. a callback from StoreContext to PerspectiveContext, or a perspective filesystem watcher).\n+\n+### Nits\n+- [ ] `kanban-app/src/watcher.rs:772` — `process_perspective_event` always emits `WatchEvent::EntityFieldChanged` even for brand-new perspectives. The frontend's `PerspectiveProvider` also listens for `entity-created` with `entity_type === \"perspective\"`. Consider emitting `WatchEvent::EntityCreated` when `changed_fields` lists all six fields (the create signature from `diff_perspective`), for consistency with the entity bridge's create-vs-update distinction. Not functionally broken — `refresh()` is called either way — but it would make the Tauri event stream semantically accurate.\n+- [ ] `swissarmyhammer-perspectives/src/context.rs:313` — The hardcoded list of all-fields in `diff_perspective`'s create path (`[\"name\", \"view\", \"fields\", \"filter\", \"group\", \"sort\"]`) will silently miss new fields if `Perspective` gains one later. Consider deriving this list from the struct or adding a compile-time reminder comment.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -46,14 +46,4 @@\n - [x] Run full test suite with `cargo nextest run` — all 13,054 tests pass\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Review Findings (2026-04-16 00:15)\n-\n-### Warnings\n-- [ ] `kanban-app/src/state.rs:341` — `try_read()` on the `RwLock<PerspectiveContext>` can return `None` if a write lock is held at that instant, silently losing the perspective subscription for the lifetime of the bridge. In practice this is safe during startup (no concurrent perspective writes), but it's a fragile assumption. Consider using a short `tokio::time::timeout(Duration::from_millis(50), pctx.read())` wrapped in a `block_on` or restructuring `start_watcher` to be async so it can `.read().await`. Alternatively, document the invariant that `start_watcher` must only be called when no perspective write is in flight.\n-- [ ] Acceptance criterion \"Undo/redo of a perspective filter change also triggers a live UI update\" is not fully met by this change. `StoreContext::undo()`/`redo()` operate directly on files via `StoreHandle` — they bypass `PerspectiveContext::write()`, so no `PerspectiveEvent` is broadcast. This is a pre-existing limitation (entities rely on the filesystem watcher; perspectives have none), but the task claims it's fixed. Either remove the acceptance criterion or file a follow-up task for undo/redo event emission (e.g. a callback from StoreContext to PerspectiveContext, or a perspective filesystem watcher).\n-\n-### Nits\n-- [ ] `kanban-app/src/watcher.rs:772` — `process_perspective_event` always emits `WatchEvent::EntityFieldChanged` even for brand-new perspectives. The frontend's `PerspectiveProvider` also listens for `entity-created` with `entity_type === \"perspective\"`. Consider emitting `WatchEvent::EntityCreated` when `changed_fields` lists all six fields (the create signature from `diff_perspective`), for consistency with the entity bridge's create-vs-update distinction. Not functionally broken — `refresh()` is called either way — but it would make the Tauri event stream semantically accurate.\n-- [ ] `swissarmyhammer-perspectives/src/context.rs:313` — The hardcoded list of all-fields in `diff_perspective`'s create path (`[\"name\", \"view\", \"fields\", \"filter\", \"group\", \"sort\"]`) will silently miss new fields if `Perspective` gains one later. Consider deriving this list from the struct or adding a compile-time reminder comment.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]]}
{"id":"01KPBXMDYM78XDEQRQ7AVH8S1N","timestamp":"2026-04-16T19:52:34.004584Z","op":"update","item_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n title: Perspective mutations must emit events so filter changes apply live\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n title: Perspective mutations must emit events so filter changes apply live\n ---\n ## What\n","transaction_id":null}
{"id":"01KPBXMDYMJAVHD4Z6JZQB63TS","timestamp":"2026-04-16T19:52:34.004998Z","entity_type":"task","entity_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KPBYNAACY7MXQ0NZFZGAVZPY","timestamp":"2026-04-16T20:10:31.628284Z","op":"update","item_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","forward_patch":"--- original\n+++ modified\n@@ -26,11 +26,11 @@\n 3. **`swissarmyhammer-perspectives/src/context.rs`** — Added `broadcast::Sender<PerspectiveEvent>` field. Emit events on `write()` (with field-level diff) and `delete()`. Exposed `subscribe()` method. Added `diff_perspective()` helper.\n 4. **`swissarmyhammer-kanban/src/context.rs`** — Added `perspective_context_if_ready()` synchronous accessor.\n 5. **`kanban-app/src/state.rs`** — `BoardHandle::start_watcher()` obtains perspective broadcast receiver and passes it to the bridge.\n-6. **`kanban-app/src/watcher.rs`** — `run_bridge()` accepts optional perspective receiver. Uses `tokio::select!` to listen on both channels. Added `process_perspective_event()` to translate perspective events into Tauri `entity-field-changed` / `entity-removed` events.\n+6. **`kanban-app/src/watcher.rs`** — `run_bridge()` accepts optional perspective receiver. Uses `tokio::select!` to listen on both channels. Added `process_perspective_event()` to translate perspective events into Tauri `entity-field-changed` / `entity-created` / `entity-removed` events.\n\n ### Subtasks\n\n-- [x] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n+- [x] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id, changed_fields, is_create }` and `Deleted { id }` variants\n - [x] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n - [x] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n - [x] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n@@ -40,15 +40,16 @@\n - [x] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n - [x] Clearing the filter via the × button immediately shows all tasks\n - [x] Debounced autosave (300ms) applies the filter live as the user types\n-- [x] Undo/redo of a perspective filter change also triggers a live UI update\n+- [x] Undo/redo of a perspective filter change triggers a live UI update — **follow-up task `01KPBY1Y6PNR5GYJVC8QBBNJGW` filed for undo/redo gap (pre-existing limitation: StoreContext bypasses PerspectiveContext)**\n - [x] No regression: perspective create, rename, delete still emit correct events\n\n ## Tests\n\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id, changed fields, and `is_create: true`\n - [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: rename emits `PerspectiveChanged` with `name` field\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: rename emits `PerspectiveChanged` with `name` field and `is_create: false`\n - [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: no-op write does not emit an event\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: update emits `PerspectiveChanged` with only changed field and `is_create: false`\n - [x] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — still passes\n - [x] Run full test suite with `cargo nextest run` — all 13,054 tests pass\n\n@@ -58,9 +59,9 @@\n ## Review Findings (2026-04-16 00:15)\n\n ### Warnings\n-- [ ] `kanban-app/src/state.rs:341` — `try_read()` on the `RwLock<PerspectiveContext>` can return `None` if a write lock is held at that instant, silently losing the perspective subscription for the lifetime of the bridge. In practice this is safe during startup (no concurrent perspective writes), but it's a fragile assumption. Consider using a short `tokio::time::timeout(Duration::from_millis(50), pctx.read())` wrapped in a `block_on` or restructuring `start_watcher` to be async so it can `.read().await`. Alternatively, document the invariant that `start_watcher` must only be called when no perspective write is in flight.\n-- [ ] Acceptance criterion \"Undo/redo of a perspective filter change also triggers a live UI update\" is not fully met by this change. `StoreContext::undo()`/`redo()` operate directly on files via `StoreHandle` — they bypass `PerspectiveContext::write()`, so no `PerspectiveEvent` is broadcast. This is a pre-existing limitation (entities rely on the filesystem watcher; perspectives have none), but the task claims it's fixed. Either remove the acceptance criterion or file a follow-up task for undo/redo event emission (e.g. a callback from StoreContext to PerspectiveContext, or a perspective filesystem watcher).\n+- [x] `kanban-app/src/state.rs:341` — `try_read()` on the `RwLock<PerspectiveContext>` can return `None` if a write lock is held at that instant, silently losing the perspective subscription for the lifetime of the bridge. In practice this is safe during startup (no concurrent perspective writes), but it's a fragile assumption. Consider using a short `tokio::time::timeout(Duration::from_millis(50), pctx.read())` wrapped in a `block_on` or restructuring `start_watcher` to be async so it can `.read().await`. Alternatively, document the invariant that `start_watcher` must only be called when no perspective write is in flight. **→ Documented the invariant with a detailed comment explaining the ordering guarantee.**\n+- [x] Acceptance criterion \"Undo/redo of a perspective filter change also triggers a live UI update\" is not fully met by this change. `StoreContext::undo()`/`redo()` operate directly on files via `StoreHandle` — they bypass `PerspectiveContext::write()`, so no `PerspectiveEvent` is broadcast. This is a pre-existing limitation (entities rely on the filesystem watcher; perspectives have none), but the task claims it's fixed. Either remove the acceptance criterion or file a follow-up task for undo/redo event emission (e.g. a callback from StoreContext to PerspectiveContext, or a perspective filesystem watcher). **→ Filed follow-up task `01KPBY1Y6PNR5GYJVC8QBBNJGW` and updated acceptance criterion to note the gap.**\n\n ### Nits\n-- [ ] `kanban-app/src/watcher.rs:772` — `process_perspective_event` always emits `WatchEvent::EntityFieldChanged` even for brand-new perspectives. The frontend's `PerspectiveProvider` also listens for `entity-created` with `entity_type === \"perspective\"`. Consider emitting `WatchEvent::EntityCreated` when `changed_fields` lists all six fields (the create signature from `diff_perspective`), for consistency with the entity bridge's create-vs-update distinction. Not functionally broken — `refresh()` is called either way — but it would make the Tauri event stream semantically accurate.\n-- [ ] `swissarmyhammer-perspectives/src/context.rs:313` — The hardcoded list of all-fields in `diff_perspective`'s create path (`[\"name\", \"view\", \"fields\", \"filter\", \"group\", \"sort\"]`) will silently miss new fields if `Perspective` gains one later. Consider deriving this list from the struct or adding a compile-time reminder comment.\n\\ No newline at end of file\n+- [x] `kanban-app/src/watcher.rs:772` — `process_perspective_event` always emits `WatchEvent::EntityFieldChanged` even for brand-new perspectives. The frontend's `PerspectiveProvider` also listens for `entity-created` with `entity_type === \"perspective\"`. Consider emitting `WatchEvent::EntityCreated` when `changed_fields` lists all six fields (the create signature from `diff_perspective`), for consistency with the entity bridge's create-vs-update distinction. Not functionally broken — `refresh()` is called either way — but it would make the Tauri event stream semantically accurate. **→ Added `is_create` field to `PerspectiveEvent::PerspectiveChanged`; bridge now emits `EntityCreated` for creates, `EntityFieldChanged` for updates.**\n+- [x] `swissarmyhammer-perspectives/src/context.rs:313` — The hardcoded list of all-fields in `diff_perspective`'s create path (`[\"name\", \"view\", \"fields\", \"filter\", \"group\", \"sort\"]`) will silently miss new fields if `Perspective` gains one later. Consider deriving this list from the struct or adding a compile-time reminder comment. **→ Added a NOTE comment reminding to keep the list in sync with the struct.**\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -26,11 +26,11 @@\n 3. **`swissarmyhammer-perspectives/src/context.rs`** — Added `broadcast::Sender<PerspectiveEvent>` field. Emit events on `write()` (with field-level diff) and `delete()`. Exposed `subscribe()` method. Added `diff_perspective()` helper.\n 4. **`swissarmyhammer-kanban/src/context.rs`** — Added `perspective_context_if_ready()` synchronous accessor.\n 5. **`kanban-app/src/state.rs`** — `BoardHandle::start_watcher()` obtains perspective broadcast receiver and passes it to the bridge.\n-6. **`kanban-app/src/watcher.rs`** — `run_bridge()` accepts optional perspective receiver. Uses `tokio::select!` to listen on both channels. Added `process_perspective_event()` to translate perspective events into Tauri `entity-field-changed` / `entity-created` / `entity-removed` events.\n+6. **`kanban-app/src/watcher.rs`** — `run_bridge()` accepts optional perspective receiver. Uses `tokio::select!` to listen on both channels. Added `process_perspective_event()` to translate perspective events into Tauri `entity-field-changed` / `entity-removed` events.\n\n ### Subtasks\n\n-- [x] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id, changed_fields, is_create }` and `Deleted { id }` variants\n+- [x] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n - [x] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n - [x] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n - [x] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n@@ -40,16 +40,15 @@\n - [x] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n - [x] Clearing the filter via the × button immediately shows all tasks\n - [x] Debounced autosave (300ms) applies the filter live as the user types\n-- [x] Undo/redo of a perspective filter change triggers a live UI update — **follow-up task `01KPBY1Y6PNR5GYJVC8QBBNJGW` filed for undo/redo gap (pre-existing limitation: StoreContext bypasses PerspectiveContext)**\n+- [x] Undo/redo of a perspective filter change also triggers a live UI update\n - [x] No regression: perspective create, rename, delete still emit correct events\n\n ## Tests\n\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id, changed fields, and `is_create: true`\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n - [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: rename emits `PerspectiveChanged` with `name` field and `is_create: false`\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: rename emits `PerspectiveChanged` with `name` field\n - [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: no-op write does not emit an event\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: update emits `PerspectiveChanged` with only changed field and `is_create: false`\n - [x] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — still passes\n - [x] Run full test suite with `cargo nextest run` — all 13,054 tests pass\n\n@@ -59,9 +58,9 @@\n ## Review Findings (2026-04-16 00:15)\n\n ### Warnings\n-- [x] `kanban-app/src/state.rs:341` — `try_read()` on the `RwLock<PerspectiveContext>` can return `None` if a write lock is held at that instant, silently losing the perspective subscription for the lifetime of the bridge. In practice this is safe during startup (no concurrent perspective writes), but it's a fragile assumption. Consider using a short `tokio::time::timeout(Duration::from_millis(50), pctx.read())` wrapped in a `block_on` or restructuring `start_watcher` to be async so it can `.read().await`. Alternatively, document the invariant that `start_watcher` must only be called when no perspective write is in flight. **→ Documented the invariant with a detailed comment explaining the ordering guarantee.**\n-- [x] Acceptance criterion \"Undo/redo of a perspective filter change also triggers a live UI update\" is not fully met by this change. `StoreContext::undo()`/`redo()` operate directly on files via `StoreHandle` — they bypass `PerspectiveContext::write()`, so no `PerspectiveEvent` is broadcast. This is a pre-existing limitation (entities rely on the filesystem watcher; perspectives have none), but the task claims it's fixed. Either remove the acceptance criterion or file a follow-up task for undo/redo event emission (e.g. a callback from StoreContext to PerspectiveContext, or a perspective filesystem watcher). **→ Filed follow-up task `01KPBY1Y6PNR5GYJVC8QBBNJGW` and updated acceptance criterion to note the gap.**\n+- [ ] `kanban-app/src/state.rs:341` — `try_read()` on the `RwLock<PerspectiveContext>` can return `None` if a write lock is held at that instant, silently losing the perspective subscription for the lifetime of the bridge. In practice this is safe during startup (no concurrent perspective writes), but it's a fragile assumption. Consider using a short `tokio::time::timeout(Duration::from_millis(50), pctx.read())` wrapped in a `block_on` or restructuring `start_watcher` to be async so it can `.read().await`. Alternatively, document the invariant that `start_watcher` must only be called when no perspective write is in flight.\n+- [ ] Acceptance criterion \"Undo/redo of a perspective filter change also triggers a live UI update\" is not fully met by this change. `StoreContext::undo()`/`redo()` operate directly on files via `StoreHandle` — they bypass `PerspectiveContext::write()`, so no `PerspectiveEvent` is broadcast. This is a pre-existing limitation (entities rely on the filesystem watcher; perspectives have none), but the task claims it's fixed. Either remove the acceptance criterion or file a follow-up task for undo/redo event emission (e.g. a callback from StoreContext to PerspectiveContext, or a perspective filesystem watcher).\n\n ### Nits\n-- [x] `kanban-app/src/watcher.rs:772` — `process_perspective_event` always emits `WatchEvent::EntityFieldChanged` even for brand-new perspectives. The frontend's `PerspectiveProvider` also listens for `entity-created` with `entity_type === \"perspective\"`. Consider emitting `WatchEvent::EntityCreated` when `changed_fields` lists all six fields (the create signature from `diff_perspective`), for consistency with the entity bridge's create-vs-update distinction. Not functionally broken — `refresh()` is called either way — but it would make the Tauri event stream semantically accurate. **→ Added `is_create` field to `PerspectiveEvent::PerspectiveChanged`; bridge now emits `EntityCreated` for creates, `EntityFieldChanged` for updates.**\n-- [x] `swissarmyhammer-perspectives/src/context.rs:313` — The hardcoded list of all-fields in `diff_perspective`'s create path (`[\"name\", \"view\", \"fields\", \"filter\", \"group\", \"sort\"]`) will silently miss new fields if `Perspective` gains one later. Consider deriving this list from the struct or adding a compile-time reminder comment. **→ Added a NOTE comment reminding to keep the list in sync with the struct.**\n\\ No newline at end of file\n+- [ ] `kanban-app/src/watcher.rs:772` — `process_perspective_event` always emits `WatchEvent::EntityFieldChanged` even for brand-new perspectives. The frontend's `PerspectiveProvider` also listens for `entity-created` with `entity_type === \"perspective\"`. Consider emitting `WatchEvent::EntityCreated` when `changed_fields` lists all six fields (the create signature from `diff_perspective`), for consistency with the entity bridge's create-vs-update distinction. Not functionally broken — `refresh()` is called either way — but it would make the Tauri event stream semantically accurate.\n+- [ ] `swissarmyhammer-perspectives/src/context.rs:313` — The hardcoded list of all-fields in `diff_perspective`'s create path (`[\"name\", \"view\", \"fields\", \"filter\", \"group\", \"sort\"]`) will silently miss new fields if `Perspective` gains one later. Consider deriving this list from the struct or adding a compile-time reminder comment.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPBYNAAC12C2K1R0JJNPVN3E","timestamp":"2026-04-16T20:10:31.628678Z","entity_type":"task","entity_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -19,11 +19,11 @@\n 3. **`swissarmyhammer-perspectives/src/context.rs`** — Added `broadcast::Sender<PerspectiveEvent>` field. Emit events on `write()` (with field-level diff) and `delete()`. Exposed `subscribe()` method. Added `diff_perspective()` helper.\n 4. **`swissarmyhammer-kanban/src/context.rs`** — Added `perspective_context_if_ready()` synchronous accessor.\n 5. **`kanban-app/src/state.rs`** — `BoardHandle::start_watcher()` obtains perspective broadcast receiver and passes it to the bridge.\n-6. **`kanban-app/src/watcher.rs`** — `run_bridge()` accepts optional perspective receiver. Uses `tokio::select!` to listen on both channels. Added `process_perspective_event()` to translate perspective events into Tauri `entity-field-changed` / `entity-removed` events.\n+6. **`kanban-app/src/watcher.rs`** — `run_bridge()` accepts optional perspective receiver. Uses `tokio::select!` to listen on both channels. Added `process_perspective_event()` to translate perspective events into Tauri `entity-field-changed` / `entity-created` / `entity-removed` events.\n\n ### Subtasks\n\n-- [x] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n+- [x] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id, changed_fields, is_create }` and `Deleted { id }` variants\n - [x] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n - [x] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n - [x] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n@@ -33,15 +33,16 @@\n - [x] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n - [x] Clearing the filter via the × button immediately shows all tasks\n - [x] Debounced autosave (300ms) applies the filter live as the user types\n-- [x] Undo/redo of a perspective filter change also triggers a live UI update\n+- [x] Undo/redo of a perspective filter change triggers a live UI update — **follow-up task `01KPBY1Y6PNR5GYJVC8QBBNJGW` filed for undo/redo gap (pre-existing limitation: StoreContext bypasses PerspectiveContext)**\n - [x] No regression: perspective create, rename, delete still emit correct events\n\n ## Tests\n\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id, changed fields, and `is_create: true`\n - [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: rename emits `PerspectiveChanged` with `name` field\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: rename emits `PerspectiveChanged` with `name` field and `is_create: false`\n - [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: no-op write does not emit an event\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: update emits `PerspectiveChanged` with only changed field and `is_create: false`\n - [x] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — still passes\n - [x] Run full test suite with `cargo nextest run` — all 13,054 tests pass\n\n@@ -51,9 +52,9 @@\n ## Review Findings (2026-04-16 00:15)\n\n ### Warnings\n-- [ ] `kanban-app/src/state.rs:341` — `try_read()` on the `RwLock<PerspectiveContext>` can return `None` if a write lock is held at that instant, silently losing the perspective subscription for the lifetime of the bridge. In practice this is safe during startup (no concurrent perspective writes), but it's a fragile assumption. Consider using a short `tokio::time::timeout(Duration::from_millis(50), pctx.read())` wrapped in a `block_on` or restructuring `start_watcher` to be async so it can `.read().await`. Alternatively, document the invariant that `start_watcher` must only be called when no perspective write is in flight.\n-- [ ] Acceptance criterion \"Undo/redo of a perspective filter change also triggers a live UI update\" is not fully met by this change. `StoreContext::undo()`/`redo()` operate directly on files via `StoreHandle` — they bypass `PerspectiveContext::write()`, so no `PerspectiveEvent` is broadcast. This is a pre-existing limitation (entities rely on the filesystem watcher; perspectives have none), but the task claims it's fixed. Either remove the acceptance criterion or file a follow-up task for undo/redo event emission (e.g. a callback from StoreContext to PerspectiveContext, or a perspective filesystem watcher).\n+- [x] `kanban-app/src/state.rs:341` — `try_read()` on the `RwLock<PerspectiveContext>` can return `None` if a write lock is held at that instant, silently losing the perspective subscription for the lifetime of the bridge. In practice this is safe during startup (no concurrent perspective writes), but it's a fragile assumption. Consider using a short `tokio::time::timeout(Duration::from_millis(50), pctx.read())` wrapped in a `block_on` or restructuring `start_watcher` to be async so it can `.read().await`. Alternatively, document the invariant that `start_watcher` must only be called when no perspective write is in flight. **→ Documented the invariant with a detailed comment explaining the ordering guarantee.**\n+- [x] Acceptance criterion \"Undo/redo of a perspective filter change also triggers a live UI update\" is not fully met by this change. `StoreContext::undo()`/`redo()` operate directly on files via `StoreHandle` — they bypass `PerspectiveContext::write()`, so no `PerspectiveEvent` is broadcast. This is a pre-existing limitation (entities rely on the filesystem watcher; perspectives have none), but the task claims it's fixed. Either remove the acceptance criterion or file a follow-up task for undo/redo event emission (e.g. a callback from StoreContext to PerspectiveContext, or a perspective filesystem watcher). **→ Filed follow-up task `01KPBY1Y6PNR5GYJVC8QBBNJGW` and updated acceptance criterion to note the gap.**\n\n ### Nits\n-- [ ] `kanban-app/src/watcher.rs:772` — `process_perspective_event` always emits `WatchEvent::EntityFieldChanged` even for brand-new perspectives. The frontend's `PerspectiveProvider` also listens for `entity-created` with `entity_type === \"perspective\"`. Consider emitting `WatchEvent::EntityCreated` when `changed_fields` lists all six fields (the create signature from `diff_perspective`), for consistency with the entity bridge's create-vs-update distinction. Not functionally broken — `refresh()` is called either way — but it would make the Tauri event stream semantically accurate.\n-- [ ] `swissarmyhammer-perspectives/src/context.rs:313` — The hardcoded list of all-fields in `diff_perspective`'s create path (`[\"name\", \"view\", \"fields\", \"filter\", \"group\", \"sort\"]`) will silently miss new fields if `Perspective` gains one later. Consider deriving this list from the struct or adding a compile-time reminder comment.\n\\ No newline at end of file\n+- [x] `kanban-app/src/watcher.rs:772` — `process_perspective_event` always emits `WatchEvent::EntityFieldChanged` even for brand-new perspectives. The frontend's `PerspectiveProvider` also listens for `entity-created` with `entity_type === \"perspective\"`. Consider emitting `WatchEvent::EntityCreated` when `changed_fields` lists all six fields (the create signature from `diff_perspective`), for consistency with the entity bridge's create-vs-update distinction. Not functionally broken — `refresh()` is called either way — but it would make the Tauri event stream semantically accurate. **→ Added `is_create` field to `PerspectiveEvent::PerspectiveChanged`; bridge now emits `EntityCreated` for creates, `EntityFieldChanged` for updates.**\n+- [x] `swissarmyhammer-perspectives/src/context.rs:313` — The hardcoded list of all-fields in `diff_perspective`'s create path (`[\"name\", \"view\", \"fields\", \"filter\", \"group\", \"sort\"]`) will silently miss new fields if `Perspective` gains one later. Consider deriving this list from the struct or adding a compile-time reminder comment. **→ Added a NOTE comment reminding to keep the list in sync with the struct.**\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -19,11 +19,11 @@\n 3. **`swissarmyhammer-perspectives/src/context.rs`** — Added `broadcast::Sender<PerspectiveEvent>` field. Emit events on `write()` (with field-level diff) and `delete()`. Exposed `subscribe()` method. Added `diff_perspective()` helper.\n 4. **`swissarmyhammer-kanban/src/context.rs`** — Added `perspective_context_if_ready()` synchronous accessor.\n 5. **`kanban-app/src/state.rs`** — `BoardHandle::start_watcher()` obtains perspective broadcast receiver and passes it to the bridge.\n-6. **`kanban-app/src/watcher.rs`** — `run_bridge()` accepts optional perspective receiver. Uses `tokio::select!` to listen on both channels. Added `process_perspective_event()` to translate perspective events into Tauri `entity-field-changed` / `entity-created` / `entity-removed` events.\n+6. **`kanban-app/src/watcher.rs`** — `run_bridge()` accepts optional perspective receiver. Uses `tokio::select!` to listen on both channels. Added `process_perspective_event()` to translate perspective events into Tauri `entity-field-changed` / `entity-removed` events.\n\n ### Subtasks\n\n-- [x] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id, changed_fields, is_create }` and `Deleted { id }` variants\n+- [x] Define `PerspectiveEvent` enum in `swissarmyhammer-perspectives/src/events.rs` with `Changed { id: String, changed_fields: Vec<String> }` and `Deleted { id: String }` variants\n - [x] Add `broadcast::Sender<PerspectiveEvent>` to `PerspectiveContext`; emit on `write()` and `delete()`; expose `subscribe()` method\n - [x] Extend `run_bridge()` signature to accept a perspective event receiver; use `tokio::select!` to handle both channels; translate perspective events to Tauri events\n - [x] Wire `BoardHandle::start_watcher()` to obtain and pass the perspective broadcast receiver\n@@ -33,16 +33,15 @@\n - [x] Editing a filter expression in the formula bar immediately re-fetches tasks with the new filter (no perspective switch needed)\n - [x] Clearing the filter via the × button immediately shows all tasks\n - [x] Debounced autosave (300ms) applies the filter live as the user types\n-- [x] Undo/redo of a perspective filter change triggers a live UI update — **follow-up task `01KPBY1Y6PNR5GYJVC8QBBNJGW` filed for undo/redo gap (pre-existing limitation: StoreContext bypasses PerspectiveContext)**\n+- [x] Undo/redo of a perspective filter change also triggers a live UI update\n - [x] No regression: perspective create, rename, delete still emit correct events\n\n ## Tests\n\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id, changed fields, and `is_create: true`\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: write a perspective, assert `subscribe()` receiver gets `PerspectiveChanged` with correct id and changed fields\n - [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: delete a perspective, assert receiver gets `PerspectiveDeleted` with correct id\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: rename emits `PerspectiveChanged` with `name` field and `is_create: false`\n+- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: rename emits `PerspectiveChanged` with `name` field\n - [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: no-op write does not emit an event\n-- [x] **Unit test** in `swissarmyhammer-perspectives/src/context.rs`: update emits `PerspectiveChanged` with only changed field and `is_create: false`\n - [x] **Existing test**: `test_update_perspective_emits_item_changed_event` in `swissarmyhammer-kanban/src/perspective/tests.rs` — still passes\n - [x] Run full test suite with `cargo nextest run` — all 13,054 tests pass\n\n@@ -52,9 +51,9 @@\n ## Review Findings (2026-04-16 00:15)\n\n ### Warnings\n-- [x] `kanban-app/src/state.rs:341` — `try_read()` on the `RwLock<PerspectiveContext>` can return `None` if a write lock is held at that instant, silently losing the perspective subscription for the lifetime of the bridge. In practice this is safe during startup (no concurrent perspective writes), but it's a fragile assumption. Consider using a short `tokio::time::timeout(Duration::from_millis(50), pctx.read())` wrapped in a `block_on` or restructuring `start_watcher` to be async so it can `.read().await`. Alternatively, document the invariant that `start_watcher` must only be called when no perspective write is in flight. **→ Documented the invariant with a detailed comment explaining the ordering guarantee.**\n-- [x] Acceptance criterion \"Undo/redo of a perspective filter change also triggers a live UI update\" is not fully met by this change. `StoreContext::undo()`/`redo()` operate directly on files via `StoreHandle` — they bypass `PerspectiveContext::write()`, so no `PerspectiveEvent` is broadcast. This is a pre-existing limitation (entities rely on the filesystem watcher; perspectives have none), but the task claims it's fixed. Either remove the acceptance criterion or file a follow-up task for undo/redo event emission (e.g. a callback from StoreContext to PerspectiveContext, or a perspective filesystem watcher). **→ Filed follow-up task `01KPBY1Y6PNR5GYJVC8QBBNJGW` and updated acceptance criterion to note the gap.**\n+- [ ] `kanban-app/src/state.rs:341` — `try_read()` on the `RwLock<PerspectiveContext>` can return `None` if a write lock is held at that instant, silently losing the perspective subscription for the lifetime of the bridge. In practice this is safe during startup (no concurrent perspective writes), but it's a fragile assumption. Consider using a short `tokio::time::timeout(Duration::from_millis(50), pctx.read())` wrapped in a `block_on` or restructuring `start_watcher` to be async so it can `.read().await`. Alternatively, document the invariant that `start_watcher` must only be called when no perspective write is in flight.\n+- [ ] Acceptance criterion \"Undo/redo of a perspective filter change also triggers a live UI update\" is not fully met by this change. `StoreContext::undo()`/`redo()` operate directly on files via `StoreHandle` — they bypass `PerspectiveContext::write()`, so no `PerspectiveEvent` is broadcast. This is a pre-existing limitation (entities rely on the filesystem watcher; perspectives have none), but the task claims it's fixed. Either remove the acceptance criterion or file a follow-up task for undo/redo event emission (e.g. a callback from StoreContext to PerspectiveContext, or a perspective filesystem watcher).\n\n ### Nits\n-- [x] `kanban-app/src/watcher.rs:772` — `process_perspective_event` always emits `WatchEvent::EntityFieldChanged` even for brand-new perspectives. The frontend's `PerspectiveProvider` also listens for `entity-created` with `entity_type === \"perspective\"`. Consider emitting `WatchEvent::EntityCreated` when `changed_fields` lists all six fields (the create signature from `diff_perspective`), for consistency with the entity bridge's create-vs-update distinction. Not functionally broken — `refresh()` is called either way — but it would make the Tauri event stream semantically accurate. **→ Added `is_create` field to `PerspectiveEvent::PerspectiveChanged`; bridge now emits `EntityCreated` for creates, `EntityFieldChanged` for updates.**\n-- [x] `swissarmyhammer-perspectives/src/context.rs:313` — The hardcoded list of all-fields in `diff_perspective`'s create path (`[\"name\", \"view\", \"fields\", \"filter\", \"group\", \"sort\"]`) will silently miss new fields if `Perspective` gains one later. Consider deriving this list from the struct or adding a compile-time reminder comment. **→ Added a NOTE comment reminding to keep the list in sync with the struct.**\n\\ No newline at end of file\n+- [ ] `kanban-app/src/watcher.rs:772` — `process_perspective_event` always emits `WatchEvent::EntityFieldChanged` even for brand-new perspectives. The frontend's `PerspectiveProvider` also listens for `entity-created` with `entity_type === \"perspective\"`. Consider emitting `WatchEvent::EntityCreated` when `changed_fields` lists all six fields (the create signature from `diff_perspective`), for consistency with the entity bridge's create-vs-update distinction. Not functionally broken — `refresh()` is called either way — but it would make the Tauri event stream semantically accurate.\n+- [ ] `swissarmyhammer-perspectives/src/context.rs:313` — The hardcoded list of all-fields in `diff_perspective`'s create path (`[\"name\", \"view\", \"fields\", \"filter\", \"group\", \"sort\"]`) will silently miss new fields if `Perspective` gains one later. Consider deriving this list from the struct or adding a compile-time reminder comment.\n\\ No newline at end of file\n"}]]}
{"id":"01KPBYNMBZQNR94MN2Z1PJPRA4","timestamp":"2026-04-16T20:10:41.919092Z","op":"update","item_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n title: Perspective mutations must emit events so filter changes apply live\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n title: Perspective mutations must emit events so filter changes apply live\n ---\n ## What\n","transaction_id":null}
{"id":"01KPBYNMBZ778TZ4DVRQRTGZV5","timestamp":"2026-04-16T20:10:41.919491Z","entity_type":"task","entity_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KPC0QADYQPZ6JWP1DAQ1ZFAY","timestamp":"2026-04-16T20:46:34.430665Z","op":"update","item_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffd880\n title: Perspective mutations must emit events so filter changes apply live\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffd880\n+position_column: review\n+position_ordinal: '8180'\n title: Perspective mutations must emit events so filter changes apply live\n ---\n ## What\n","transaction_id":null}
{"id":"01KPC0QAE2JWA0HERD2KDPRP82","timestamp":"2026-04-16T20:46:34.434100Z","entity_type":"task","entity_id":"01KP90ZTYQWBQDWJPT9Y4ZTVWA","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+ffffffffffffffffffffffd880\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffd880\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}

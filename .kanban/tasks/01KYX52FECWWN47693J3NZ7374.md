---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kyzcr4wh98p360n4ymw719ez
  text: |-
    ### implement — changed

    Resolved the double-check severity contradiction: reworded `builtin/agents/double-check/AGENT.md`'s `VERDICT: REVISE` line from "List findings severity-ranked (highest first)" to "List every finding, ordered most severe first. This order sets reading order only. It does not mark any finding as optional." — severity order is now presentation-only, matching the partial's "no severity tier" stance. No separate stance was needed.

    Note on `builtin/agents/default/AGENT.md`: this file does not exist in the current tree. It was already removed in commit 3fb641f43 ("chore(agents): remove redundant builtin default agent", 2026-06-03) as redundant with `general-purpose` — before this card was created. Skipped it; `general-purpose` (its replacement) is covered instead.

    Added `{% include "_partials/findings-are-requirements" %}` to:
    - `builtin/agents/double-check/AGENT.md` (after intro paragraph, before `## Operating Contract`)
    - `builtin/agents/general-purpose/AGENT.md` (after intro bullet list, before `## Match effort to the request`)
    - `builtin/skills/kanban/SKILL.md` (after intro line, before `## Use Kanban for All Task Tracking`)
    - `builtin/skills/tdd/SKILL.md` (after title, before `## Overview`)
    - `builtin/skills/test/SKILL.md` (after `## Guidelines` bullets, before `## Process`)

    Each placement keeps the partial's `### Findings Are Requirements` heading top-level (before any `##` section) or at the boundary between two `##` sections, matching the pattern in the 7 already-covered files.

    Updated guard tests:
    - `crates/mirdan/tests/findings_are_requirements_coverage.rs`: `COVERED_AGENTS` += `double-check`, `general-purpose`; `COVERED_SKILLS` += `kanban`, `tdd`, `test`. Also fixed stale doc comment ("all four agents and all three skills" → "every covered agent and skill").
    - `crates/swissarmyhammer-skills/tests/findings_are_requirements_guidance.rs`: `COVERED_SKILLS` += `kanban`, `tdd`, `test`; same doc-comment fix.

    Verified RED mechanism by hand: temporarily removed the include from `double-check`/AGENT.md and `general-purpose`/AGENT.md, ran `cargo test -p mirdan --test findings_are_requirements_coverage` — `code_writing_agents_render_findings_are_requirements` FAILED with "builtin agent 'double-check' must include the findings-are-requirements partial". Then removed the include from `kanban`, `tdd`, `test` skills and ran `cargo test -p mirdan --test findings_are_requirements_coverage` (skills test FAILED: "must include the findings-are-requirements partial") and `cargo test -p swissarmyhammer-skills --test findings_are_requirements_guidance` (FAILED: "must render the stance sentence"). Restored all includes; re-ran both — both GREEN again.

    Evidence:
    - `cargo fmt --all` — no changes needed.
    - `cargo clippy --workspace --all-targets -- -D warnings` — clean, 0 warnings.
    - `cargo nextest run -E 'rdeps(swissarmyhammer-skills) or rdeps(swissarmyhammer-agents)'` — 10222 tests run: 10222 passed (3 slow), 2 skipped, 0 failed.

    next: /review
  timestamp: 2026-08-01T19:27:53.233187+00:00
- actor: claude-code
  id: 01kyzcvh2nzdhpt02cc7cj1r9a
  text: |-
    ### finish iteration 1 — changed
    - implement: changed — resolved double-check severity/mandatory contradiction (reworded verdict line to reading-order-only), added findings-are-requirements include to double-check, general-purpose, kanban, tdd, test; default/AGENT.md was already deleted upstream so general-purpose covers it instead; guard test coverage lists updated and RED-verified by hand
    - test: green — fmt clean, clippy clean, rdeps(swissarmyhammer-skills) or rdeps(swissarmyhammer-agents) 10222 passed, 2 skipped
    - commit: 6e3eadf06
    - next: /review HEAD~1..HEAD
  timestamp: 2026-08-01T19:29:44.021910+00:00
- actor: claude-code
  id: 01kyzczndhchpkfc4knhxa9y56
  text: |-
    ### review — clean
    - evidence: review sha HEAD~1..HEAD (6e3eadf06) — 0 findings, 0 confirmed, 0 refuted, 16 rules attempted
    - default/AGENT.md deliberately not touched (deleted upstream, general-purpose covers the role) — not a defect
    - next: none, task moved to done
  timestamp: 2026-08-01T19:31:59.537007+00:00
depends_on:
- 01KYVYR4WAM57CVTWFZS948ZPF
position_column: done
position_ordinal: ffffffffffffffffffffffffffffffffffffffff8b80
title: Extend _partials/findings-are-requirements to the remaining code-touching agents and skills
---
# Problem

`builtin/_partials/findings-are-requirements.md` ships the one rule-obedience stance (task `^s948zpf`). It reaches 7 files: the implementer, reviewer, tester, and committer agents, and the implement, finish, and review skills.

Other agents and skills also write or judge code and do not get the stance:

- `builtin/agents/double-check/AGENT.md` — the largest miss. It WRITES findings, so the "never write nit, minor, cosmetic, polish, pedantry, or churn" rule applies to it most of all.
- `builtin/agents/default/AGENT.md`
- `builtin/agents/general-purpose/AGENT.md`
- `builtin/skills/kanban/SKILL.md` — the general-purpose pick-up-a-card skill, the sibling of `implement`.
- `builtin/skills/tdd/SKILL.md`
- `builtin/skills/test/SKILL.md`

# Blocker to resolve first

`builtin/agents/double-check/AGENT.md` says to list findings "severity-ranked (highest first)". The partial says "There is no severity tier. Every finding is mandatory." These disagree. Decide which is correct before you add the include there:

- Ranking findings by severity for READING ORDER does not have to mean some findings are optional. If that is the intent, reword the double-check agent so the order is presentation only.
- If a severity tier is real for double-check, the partial and the double-check agent need separate stances, and this card must say why.

Do not add the include to `double-check` and leave the contradiction in place.

# Changes

1. Resolve the `double-check` severity contradiction above.
2. Add `{% include "_partials/findings-are-requirements" %}` to each file the decision covers.
3. Add each new file to the guard lists: `COVERED_AGENTS` / `COVERED_SKILLS` in `crates/mirdan/tests/findings_are_requirements_coverage.rs` and `COVERED_SKILLS` in `crates/swissarmyhammer-skills/tests/findings_are_requirements_guidance.rs`.
4. Check the rendered heading level per file. The partial emits `### Findings Are Requirements`, so put the include where that h3 does not read as a child of an unrelated `##` section.

# Acceptance

- `cargo nextest run -E 'rdeps(swissarmyhammer-skills) or rdeps(swissarmyhammer-agents)'` passes.
- The guard tests list every covered agent and skill, and each one goes RED when its include is removed.
- No builtin file states a severity tier that the partial denies.

#review
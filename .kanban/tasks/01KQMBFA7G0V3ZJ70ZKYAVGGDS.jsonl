{"id":"01KQMBFA7G3NZBXPDN9CYH54XX","timestamp":"2026-05-02T12:44:03.696904Z","op":"create","item_id":"01KQMBFA7G0V3ZJ70ZKYAVGGDS","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,28 @@\n+---\n+assignees: []\n+position_column: todo\n+position_ordinal: c180\n+title: 'browser tests: drill-in setFocus assertions check stale ui.setFocus dispatch contract'\n+---\n+Three browser-mode test files assert that drill-in / Enter / Escape / ArrowDown flows fan out to `dispatch_command(\"ui.setFocus\", { args: { scope_chain: [...] } })`. They fail on pristine HEAD because the production code path in `entity-focus-context.tsx::setFocus` (lines 380-396) calls `spatial.focus(fq)` (which invokes `spatial_focus`) when a `SpatialFocusProvider` is present and never reaches the test-harness fallback that dispatches `ui.setFocus` (lines 399-405). All three failing test files do wrap in `SpatialFocusProvider`, so they exercise the production path.\n+\n+Failing tests (5 across 3 files):\n+- kanban-app/ui/src/components/board-view.enter-drill-in.browser.test.tsx\n+  * `enter_on_focused_column_drills_into_first_card` (line 639)\n+  * `enter_on_focused_column_with_remembered_focus_drills_into_remembered_card` (line 710)\n+- kanban-app/ui/src/components/entity-inspector.field-enter-drill.browser.test.tsx\n+  * `enter_on_pill_field_drills_into_first_pill` (line 533)\n+  * `escape_from_pill_drills_back_to_field_zone` (line 690)\n+- kanban-app/ui/src/components/inspector.kernel-focus-advance.browser.test.tsx\n+  * `ArrowDown from the last field stays put, kernel's focused key remains the last field` (line 518)\n+\n+Symptom: `dispatchArgs?.scope_chain?.[0]` is `undefined` because no `dispatch_command(ui.setFocus, ...)` call was made — the test stubs `spatial_drill_in` to return a moniker, and the React closure forwards it via `setFocusRef.current(result)`, which calls `spatial_focus(fq)`, not `ui.setFocus`.\n+\n+Fix direction: rewrite each assertion to pin the production contract:\n+  expect a `spatial_focus` invocation with the resolved `fq` mapped from the kernel-returned moniker, and rely on the simulated `focus-changed` event echo (which the in-test `defaultInvokeImpl` already emits via queueMicrotask) to drive subsequent React state assertions.\n+\n+These failures are pre-existing on branch `kanban` at commit 35a106634 — they are not caused by the `swissarmyhammer-focus/src/registry.rs` overlap-warning change.\n+\n+What I tried:\n+- Ran full workspace tests + clippy — all Rust passes clean.\n+- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,28 +0,0 @@\n----\n-assignees: []\n-position_column: todo\n-position_ordinal: c180\n-title: 'browser tests: drill-in setFocus assertions check stale ui.setFocus dispatch contract'\n----\n-Three browser-mode test files assert that drill-in / Enter / Escape / ArrowDown flows fan out to `dispatch_command(\"ui.setFocus\", { args: { scope_chain: [...] } })`. They fail on pristine HEAD because the production code path in `entity-focus-context.tsx::setFocus` (lines 380-396) calls `spatial.focus(fq)` (which invokes `spatial_focus`) when a `SpatialFocusProvider` is present and never reaches the test-harness fallback that dispatches `ui.setFocus` (lines 399-405). All three failing test files do wrap in `SpatialFocusProvider`, so they exercise the production path.\n-\n-Failing tests (5 across 3 files):\n-- kanban-app/ui/src/components/board-view.enter-drill-in.browser.test.tsx\n-  * `enter_on_focused_column_drills_into_first_card` (line 639)\n-  * `enter_on_focused_column_with_remembered_focus_drills_into_remembered_card` (line 710)\n-- kanban-app/ui/src/components/entity-inspector.field-enter-drill.browser.test.tsx\n-  * `enter_on_pill_field_drills_into_first_pill` (line 533)\n-  * `escape_from_pill_drills_back_to_field_zone` (line 690)\n-- kanban-app/ui/src/components/inspector.kernel-focus-advance.browser.test.tsx\n-  * `ArrowDown from the last field stays put, kernel's focused key remains the last field` (line 518)\n-\n-Symptom: `dispatchArgs?.scope_chain?.[0]` is `undefined` because no `dispatch_command(ui.setFocus, ...)` call was made — the test stubs `spatial_drill_in` to return a moniker, and the React closure forwards it via `setFocusRef.current(result)`, which calls `spatial_focus(fq)`, not `ui.setFocus`.\n-\n-Fix direction: rewrite each assertion to pin the production contract:\n-  expect a `spatial_focus` invocation with the resolved `fq` mapped from the kernel-returned moniker, and rely on the simulated `focus-changed` event echo (which the in-test `defaultInvokeImpl` already emits via queueMicrotask) to drive subsequent React state assertions.\n-\n-These failures are pre-existing on branch `kanban` at commit 35a106634 — they are not caused by the `swissarmyhammer-focus/src/registry.rs` overlap-warning change.\n-\n-What I tried:\n-- Ran full workspace tests + clippy — all Rust passes clean.\n-- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQMBFA7HRWW7D748RZ6V9QWC","timestamp":"2026-05-02T12:44:03.697664Z","entity_type":"task","entity_id":"01KQMBFA7G0V3ZJ70ZKYAVGGDS","op":"create","changes":[["assignees",{"kind":"set","value":[]}],["title",{"kind":"set","value":"browser tests: drill-in setFocus assertions check stale ui.setFocus dispatch contract"}],["body",{"kind":"set","value":"Three browser-mode test files assert that drill-in / Enter / Escape / ArrowDown flows fan out to `dispatch_command(\"ui.setFocus\", { args: { scope_chain: [...] } })`. They fail on pristine HEAD because the production code path in `entity-focus-context.tsx::setFocus` (lines 380-396) calls `spatial.focus(fq)` (which invokes `spatial_focus`) when a `SpatialFocusProvider` is present and never reaches the test-harness fallback that dispatches `ui.setFocus` (lines 399-405). All three failing test files do wrap in `SpatialFocusProvider`, so they exercise the production path.\n\nFailing tests (5 across 3 files):\n- kanban-app/ui/src/components/board-view.enter-drill-in.browser.test.tsx\n  * `enter_on_focused_column_drills_into_first_card` (line 639)\n  * `enter_on_focused_column_with_remembered_focus_drills_into_remembered_card` (line 710)\n- kanban-app/ui/src/components/entity-inspector.field-enter-drill.browser.test.tsx\n  * `enter_on_pill_field_drills_into_first_pill` (line 533)\n  * `escape_from_pill_drills_back_to_field_zone` (line 690)\n- kanban-app/ui/src/components/inspector.kernel-focus-advance.browser.test.tsx\n  * `ArrowDown from the last field stays put, kernel's focused key remains the last field` (line 518)\n\nSymptom: `dispatchArgs?.scope_chain?.[0]` is `undefined` because no `dispatch_command(ui.setFocus, ...)` call was made — the test stubs `spatial_drill_in` to return a moniker, and the React closure forwards it via `setFocusRef.current(result)`, which calls `spatial_focus(fq)`, not `ui.setFocus`.\n\nFix direction: rewrite each assertion to pin the production contract:\n  expect a `spatial_focus` invocation with the resolved `fq` mapped from the kernel-returned moniker, and rely on the simulated `focus-changed` event echo (which the in-test `defaultInvokeImpl` already emits via queueMicrotask) to drive subsequent React state assertions.\n\nThese failures are pre-existing on branch `kanban` at commit 35a106634 — they are not caused by the `swissarmyhammer-focus/src/registry.rs` overlap-warning change.\n\nWhat I tried:\n- Ran full workspace tests + clippy — all Rust passes clean.\n- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures."}],["position_ordinal",{"kind":"set","value":"c180"}],["position_column",{"kind":"set","value":"todo"}]]}
{"id":"01KQMBFD6CDS9HJB4AS02DX64S","timestamp":"2026-05-02T12:44:06.732342Z","op":"update","item_id":"01KQMBFA7G0V3ZJ70ZKYAVGGDS","forward_patch":"--- original\n+++ modified\n@@ -25,4 +25,4 @@\n\n What I tried:\n - Ran full workspace tests + clippy — all Rust passes clean.\n-- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures.\n\\ No newline at end of file\n+- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures. #test-failure\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -25,4 +25,4 @@\n\n What I tried:\n - Ran full workspace tests + clippy — all Rust passes clean.\n-- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures. #test-failure\n\\ No newline at end of file\n+- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQMBFD6CQ1KA1T11Q377T8ED","timestamp":"2026-05-02T12:44:06.732662Z","entity_type":"task","entity_id":"01KQMBFA7G0V3ZJ70ZKYAVGGDS","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -19,4 +19,4 @@\n\n What I tried:\n - Ran full workspace tests + clippy — all Rust passes clean.\n-- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures.\n\\ No newline at end of file\n+- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures. #test-failure\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -19,4 +19,4 @@\n\n What I tried:\n - Ran full workspace tests + clippy — all Rust passes clean.\n-- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures. #test-failure\n\\ No newline at end of file\n+- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures.\n\\ No newline at end of file\n"}]]}
{"id":"01KQMC4RGH9GH1Y2GDXAPDJE89","timestamp":"2026-05-02T12:55:46.449870Z","op":"update","item_id":"01KQMBFA7G0V3ZJ70ZKYAVGGDS","forward_patch":"--- original\n+++ modified\n@@ -2,27 +2,23 @@\n assignees: []\n position_column: todo\n position_ordinal: c180\n-title: 'browser tests: drill-in setFocus assertions check stale ui.setFocus dispatch contract'\n+title: 'RESOLVED: browser tests stale ui.setFocus contract — fixed inline during /test run'\n ---\n-Three browser-mode test files assert that drill-in / Enter / Escape / ArrowDown flows fan out to `dispatch_command(\"ui.setFocus\", { args: { scope_chain: [...] } })`. They fail on pristine HEAD because the production code path in `entity-focus-context.tsx::setFocus` (lines 380-396) calls `spatial.focus(fq)` (which invokes `spatial_focus`) when a `SpatialFocusProvider` is present and never reaches the test-harness fallback that dispatches `ui.setFocus` (lines 399-405). All three failing test files do wrap in `SpatialFocusProvider`, so they exercise the production path.\n+RESOLVED — fixed inline during a /test run on branch `kanban`.\n\n-Failing tests (5 across 3 files):\n-- kanban-app/ui/src/components/board-view.enter-drill-in.browser.test.tsx\n-  * `enter_on_focused_column_drills_into_first_card` (line 639)\n-  * `enter_on_focused_column_with_remembered_focus_drills_into_remembered_card` (line 710)\n-- kanban-app/ui/src/components/entity-inspector.field-enter-drill.browser.test.tsx\n-  * `enter_on_pill_field_drills_into_first_pill` (line 533)\n-  * `escape_from_pill_drills_back_to_field_zone` (line 690)\n-- kanban-app/ui/src/components/inspector.kernel-focus-advance.browser.test.tsx\n-  * `ArrowDown from the last field stays put, kernel's focused key remains the last field` (line 518)\n+Three browser-mode test files asserted that drill-in / Enter / Escape / ArrowDown flows fan out to `dispatch_command(\"ui.setFocus\", { args: { scope_chain: [...] } })` with the kernel-returned moniker at chain[0]. The assertions were stale relative to the production kernel-bridge architecture documented on `EntityFocusProvider`:\n\n-Symptom: `dispatchArgs?.scope_chain?.[0]` is `undefined` because no `dispatch_command(ui.setFocus, ...)` call was made — the test stubs `spatial_drill_in` to return a moniker, and the React closure forwards it via `setFocusRef.current(result)`, which calls `spatial_focus(fq)`, not `ui.setFocus`.\n+  - Production `FocusActions.setFocus(fq)` routes through `spatial.focus(fq)` → `spatial_focus` IPC, NOT through a `dispatch_command(ui.setFocus, ...)`.\n+  - The `subscribeFocusChanged` bridge inside `EntityFocusProvider` does dispatch `ui.setFocus` after each `focus-changed` event, but its `scope_chain` is built by looking up `payload.next_fq` (an FQM) in the entity-scope registry. The simulator's `spatial_focus` handler emits the raw moniker as `next_fq`, which doesn't match any FQM-keyed registry entry — so the chain was empty, never `[<moniker>, ...]`.\n\n-Fix direction: rewrite each assertion to pin the production contract:\n-  expect a `spatial_focus` invocation with the resolved `fq` mapped from the kernel-returned moniker, and rely on the simulated `focus-changed` event echo (which the in-test `defaultInvokeImpl` already emits via queueMicrotask) to drive subsequent React state assertions.\n+Fixes (production-aligned):\n+- kanban-app/ui/src/components/board-view.enter-drill-in.browser.test.tsx — replaced the `setFocusDispatch` lookup at lines 628-639 and 699-710 with a direct `spatial_focus` invocation assertion (`focusCall.fq === \"task:t1\"` / `\"task:t2\"`). This pins the same end-to-end contract — drill-in resolves through the kernel — without depending on bridge-derived scope chains.\n+- kanban-app/ui/src/components/entity-inspector.field-enter-drill.browser.test.tsx — replaced the `setFocusDispatches()` helper with `spatialFocusCalls()` (and updated both call sites' assertions and rationale comments).\n+- kanban-app/ui/src/components/inspector.kernel-focus-advance.browser.test.tsx — the `ArrowDown from the last field stays put` test was inverted: real BeamNavStrategy at body→down does iter 0 (no peer) → iter 1 (no parent-zone peer) → drill-out fallback returns the parent panel `task:T1`. Renamed the test, rewrote the assertion to the correct drill-out behavior, and added a `waitFor(titleField.fq)` gate before the navigation loop so the test no longer races first-field auto-focus.\n\n-These failures are pre-existing on branch `kanban` at commit 35a106634 — they are not caused by the `swissarmyhammer-focus/src/registry.rs` overlap-warning change.\n+Also removed an unused `registerScopeArgs` helper from `kanban-app/ui/src/components/board-view.spatial.test.tsx` (TypeScript noUnused checks were failing under `tsc --noEmit`).\n\n-What I tried:\n-- Ran full workspace tests + clippy — all Rust passes clean.\n-- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures. #test-failure\n\\ No newline at end of file\n+Verified clean:\n+- `cargo test --workspace` (now `cargo nextest run --workspace`) — 13576 passed, 0 failed.\n+- `cargo clippy --workspace --all-targets -- -D warnings` — clean.\n+- `npm test` (kanban-app/ui) — 1885 passed, 0 failed (4 pre-existing intentional skips remain — see follow-up tasks).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -2,23 +2,27 @@\n assignees: []\n position_column: todo\n position_ordinal: c180\n-title: 'RESOLVED: browser tests stale ui.setFocus contract — fixed inline during /test run'\n+title: 'browser tests: drill-in setFocus assertions check stale ui.setFocus dispatch contract'\n ---\n-RESOLVED — fixed inline during a /test run on branch `kanban`.\n+Three browser-mode test files assert that drill-in / Enter / Escape / ArrowDown flows fan out to `dispatch_command(\"ui.setFocus\", { args: { scope_chain: [...] } })`. They fail on pristine HEAD because the production code path in `entity-focus-context.tsx::setFocus` (lines 380-396) calls `spatial.focus(fq)` (which invokes `spatial_focus`) when a `SpatialFocusProvider` is present and never reaches the test-harness fallback that dispatches `ui.setFocus` (lines 399-405). All three failing test files do wrap in `SpatialFocusProvider`, so they exercise the production path.\n\n-Three browser-mode test files asserted that drill-in / Enter / Escape / ArrowDown flows fan out to `dispatch_command(\"ui.setFocus\", { args: { scope_chain: [...] } })` with the kernel-returned moniker at chain[0]. The assertions were stale relative to the production kernel-bridge architecture documented on `EntityFocusProvider`:\n+Failing tests (5 across 3 files):\n+- kanban-app/ui/src/components/board-view.enter-drill-in.browser.test.tsx\n+  * `enter_on_focused_column_drills_into_first_card` (line 639)\n+  * `enter_on_focused_column_with_remembered_focus_drills_into_remembered_card` (line 710)\n+- kanban-app/ui/src/components/entity-inspector.field-enter-drill.browser.test.tsx\n+  * `enter_on_pill_field_drills_into_first_pill` (line 533)\n+  * `escape_from_pill_drills_back_to_field_zone` (line 690)\n+- kanban-app/ui/src/components/inspector.kernel-focus-advance.browser.test.tsx\n+  * `ArrowDown from the last field stays put, kernel's focused key remains the last field` (line 518)\n\n-  - Production `FocusActions.setFocus(fq)` routes through `spatial.focus(fq)` → `spatial_focus` IPC, NOT through a `dispatch_command(ui.setFocus, ...)`.\n-  - The `subscribeFocusChanged` bridge inside `EntityFocusProvider` does dispatch `ui.setFocus` after each `focus-changed` event, but its `scope_chain` is built by looking up `payload.next_fq` (an FQM) in the entity-scope registry. The simulator's `spatial_focus` handler emits the raw moniker as `next_fq`, which doesn't match any FQM-keyed registry entry — so the chain was empty, never `[<moniker>, ...]`.\n+Symptom: `dispatchArgs?.scope_chain?.[0]` is `undefined` because no `dispatch_command(ui.setFocus, ...)` call was made — the test stubs `spatial_drill_in` to return a moniker, and the React closure forwards it via `setFocusRef.current(result)`, which calls `spatial_focus(fq)`, not `ui.setFocus`.\n\n-Fixes (production-aligned):\n-- kanban-app/ui/src/components/board-view.enter-drill-in.browser.test.tsx — replaced the `setFocusDispatch` lookup at lines 628-639 and 699-710 with a direct `spatial_focus` invocation assertion (`focusCall.fq === \"task:t1\"` / `\"task:t2\"`). This pins the same end-to-end contract — drill-in resolves through the kernel — without depending on bridge-derived scope chains.\n-- kanban-app/ui/src/components/entity-inspector.field-enter-drill.browser.test.tsx — replaced the `setFocusDispatches()` helper with `spatialFocusCalls()` (and updated both call sites' assertions and rationale comments).\n-- kanban-app/ui/src/components/inspector.kernel-focus-advance.browser.test.tsx — the `ArrowDown from the last field stays put` test was inverted: real BeamNavStrategy at body→down does iter 0 (no peer) → iter 1 (no parent-zone peer) → drill-out fallback returns the parent panel `task:T1`. Renamed the test, rewrote the assertion to the correct drill-out behavior, and added a `waitFor(titleField.fq)` gate before the navigation loop so the test no longer races first-field auto-focus.\n+Fix direction: rewrite each assertion to pin the production contract:\n+  expect a `spatial_focus` invocation with the resolved `fq` mapped from the kernel-returned moniker, and rely on the simulated `focus-changed` event echo (which the in-test `defaultInvokeImpl` already emits via queueMicrotask) to drive subsequent React state assertions.\n\n-Also removed an unused `registerScopeArgs` helper from `kanban-app/ui/src/components/board-view.spatial.test.tsx` (TypeScript noUnused checks were failing under `tsc --noEmit`).\n+These failures are pre-existing on branch `kanban` at commit 35a106634 — they are not caused by the `swissarmyhammer-focus/src/registry.rs` overlap-warning change.\n\n-Verified clean:\n-- `cargo test --workspace` (now `cargo nextest run --workspace`) — 13576 passed, 0 failed.\n-- `cargo clippy --workspace --all-targets -- -D warnings` — clean.\n-- `npm test` (kanban-app/ui) — 1885 passed, 0 failed (4 pre-existing intentional skips remain — see follow-up tasks).\n\\ No newline at end of file\n+What I tried:\n+- Ran full workspace tests + clippy — all Rust passes clean.\n+- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures. #test-failure\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQMC4RGJMHRGRF8518Y6CF52","timestamp":"2026-05-02T12:55:46.450262Z","entity_type":"task","entity_id":"01KQMBFA7G0V3ZJ70ZKYAVGGDS","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,22 +1,18 @@\n-Three browser-mode test files assert that drill-in / Enter / Escape / ArrowDown flows fan out to `dispatch_command(\"ui.setFocus\", { args: { scope_chain: [...] } })`. They fail on pristine HEAD because the production code path in `entity-focus-context.tsx::setFocus` (lines 380-396) calls `spatial.focus(fq)` (which invokes `spatial_focus`) when a `SpatialFocusProvider` is present and never reaches the test-harness fallback that dispatches `ui.setFocus` (lines 399-405). All three failing test files do wrap in `SpatialFocusProvider`, so they exercise the production path.\n+RESOLVED — fixed inline during a /test run on branch `kanban`.\n\n-Failing tests (5 across 3 files):\n-- kanban-app/ui/src/components/board-view.enter-drill-in.browser.test.tsx\n-  * `enter_on_focused_column_drills_into_first_card` (line 639)\n-  * `enter_on_focused_column_with_remembered_focus_drills_into_remembered_card` (line 710)\n-- kanban-app/ui/src/components/entity-inspector.field-enter-drill.browser.test.tsx\n-  * `enter_on_pill_field_drills_into_first_pill` (line 533)\n-  * `escape_from_pill_drills_back_to_field_zone` (line 690)\n-- kanban-app/ui/src/components/inspector.kernel-focus-advance.browser.test.tsx\n-  * `ArrowDown from the last field stays put, kernel's focused key remains the last field` (line 518)\n+Three browser-mode test files asserted that drill-in / Enter / Escape / ArrowDown flows fan out to `dispatch_command(\"ui.setFocus\", { args: { scope_chain: [...] } })` with the kernel-returned moniker at chain[0]. The assertions were stale relative to the production kernel-bridge architecture documented on `EntityFocusProvider`:\n\n-Symptom: `dispatchArgs?.scope_chain?.[0]` is `undefined` because no `dispatch_command(ui.setFocus, ...)` call was made — the test stubs `spatial_drill_in` to return a moniker, and the React closure forwards it via `setFocusRef.current(result)`, which calls `spatial_focus(fq)`, not `ui.setFocus`.\n+  - Production `FocusActions.setFocus(fq)` routes through `spatial.focus(fq)` → `spatial_focus` IPC, NOT through a `dispatch_command(ui.setFocus, ...)`.\n+  - The `subscribeFocusChanged` bridge inside `EntityFocusProvider` does dispatch `ui.setFocus` after each `focus-changed` event, but its `scope_chain` is built by looking up `payload.next_fq` (an FQM) in the entity-scope registry. The simulator's `spatial_focus` handler emits the raw moniker as `next_fq`, which doesn't match any FQM-keyed registry entry — so the chain was empty, never `[<moniker>, ...]`.\n\n-Fix direction: rewrite each assertion to pin the production contract:\n-  expect a `spatial_focus` invocation with the resolved `fq` mapped from the kernel-returned moniker, and rely on the simulated `focus-changed` event echo (which the in-test `defaultInvokeImpl` already emits via queueMicrotask) to drive subsequent React state assertions.\n+Fixes (production-aligned):\n+- kanban-app/ui/src/components/board-view.enter-drill-in.browser.test.tsx — replaced the `setFocusDispatch` lookup at lines 628-639 and 699-710 with a direct `spatial_focus` invocation assertion (`focusCall.fq === \"task:t1\"` / `\"task:t2\"`). This pins the same end-to-end contract — drill-in resolves through the kernel — without depending on bridge-derived scope chains.\n+- kanban-app/ui/src/components/entity-inspector.field-enter-drill.browser.test.tsx — replaced the `setFocusDispatches()` helper with `spatialFocusCalls()` (and updated both call sites' assertions and rationale comments).\n+- kanban-app/ui/src/components/inspector.kernel-focus-advance.browser.test.tsx — the `ArrowDown from the last field stays put` test was inverted: real BeamNavStrategy at body→down does iter 0 (no peer) → iter 1 (no parent-zone peer) → drill-out fallback returns the parent panel `task:T1`. Renamed the test, rewrote the assertion to the correct drill-out behavior, and added a `waitFor(titleField.fq)` gate before the navigation loop so the test no longer races first-field auto-focus.\n\n-These failures are pre-existing on branch `kanban` at commit 35a106634 — they are not caused by the `swissarmyhammer-focus/src/registry.rs` overlap-warning change.\n+Also removed an unused `registerScopeArgs` helper from `kanban-app/ui/src/components/board-view.spatial.test.tsx` (TypeScript noUnused checks were failing under `tsc --noEmit`).\n\n-What I tried:\n-- Ran full workspace tests + clippy — all Rust passes clean.\n-- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures. #test-failure\n\\ No newline at end of file\n+Verified clean:\n+- `cargo test --workspace` (now `cargo nextest run --workspace`) — 13576 passed, 0 failed.\n+- `cargo clippy --workspace --all-targets -- -D warnings` — clean.\n+- `npm test` (kanban-app/ui) — 1885 passed, 0 failed (4 pre-existing intentional skips remain — see follow-up tasks).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,18 +1,22 @@\n-RESOLVED — fixed inline during a /test run on branch `kanban`.\n+Three browser-mode test files assert that drill-in / Enter / Escape / ArrowDown flows fan out to `dispatch_command(\"ui.setFocus\", { args: { scope_chain: [...] } })`. They fail on pristine HEAD because the production code path in `entity-focus-context.tsx::setFocus` (lines 380-396) calls `spatial.focus(fq)` (which invokes `spatial_focus`) when a `SpatialFocusProvider` is present and never reaches the test-harness fallback that dispatches `ui.setFocus` (lines 399-405). All three failing test files do wrap in `SpatialFocusProvider`, so they exercise the production path.\n\n-Three browser-mode test files asserted that drill-in / Enter / Escape / ArrowDown flows fan out to `dispatch_command(\"ui.setFocus\", { args: { scope_chain: [...] } })` with the kernel-returned moniker at chain[0]. The assertions were stale relative to the production kernel-bridge architecture documented on `EntityFocusProvider`:\n+Failing tests (5 across 3 files):\n+- kanban-app/ui/src/components/board-view.enter-drill-in.browser.test.tsx\n+  * `enter_on_focused_column_drills_into_first_card` (line 639)\n+  * `enter_on_focused_column_with_remembered_focus_drills_into_remembered_card` (line 710)\n+- kanban-app/ui/src/components/entity-inspector.field-enter-drill.browser.test.tsx\n+  * `enter_on_pill_field_drills_into_first_pill` (line 533)\n+  * `escape_from_pill_drills_back_to_field_zone` (line 690)\n+- kanban-app/ui/src/components/inspector.kernel-focus-advance.browser.test.tsx\n+  * `ArrowDown from the last field stays put, kernel's focused key remains the last field` (line 518)\n\n-  - Production `FocusActions.setFocus(fq)` routes through `spatial.focus(fq)` → `spatial_focus` IPC, NOT through a `dispatch_command(ui.setFocus, ...)`.\n-  - The `subscribeFocusChanged` bridge inside `EntityFocusProvider` does dispatch `ui.setFocus` after each `focus-changed` event, but its `scope_chain` is built by looking up `payload.next_fq` (an FQM) in the entity-scope registry. The simulator's `spatial_focus` handler emits the raw moniker as `next_fq`, which doesn't match any FQM-keyed registry entry — so the chain was empty, never `[<moniker>, ...]`.\n+Symptom: `dispatchArgs?.scope_chain?.[0]` is `undefined` because no `dispatch_command(ui.setFocus, ...)` call was made — the test stubs `spatial_drill_in` to return a moniker, and the React closure forwards it via `setFocusRef.current(result)`, which calls `spatial_focus(fq)`, not `ui.setFocus`.\n\n-Fixes (production-aligned):\n-- kanban-app/ui/src/components/board-view.enter-drill-in.browser.test.tsx — replaced the `setFocusDispatch` lookup at lines 628-639 and 699-710 with a direct `spatial_focus` invocation assertion (`focusCall.fq === \"task:t1\"` / `\"task:t2\"`). This pins the same end-to-end contract — drill-in resolves through the kernel — without depending on bridge-derived scope chains.\n-- kanban-app/ui/src/components/entity-inspector.field-enter-drill.browser.test.tsx — replaced the `setFocusDispatches()` helper with `spatialFocusCalls()` (and updated both call sites' assertions and rationale comments).\n-- kanban-app/ui/src/components/inspector.kernel-focus-advance.browser.test.tsx — the `ArrowDown from the last field stays put` test was inverted: real BeamNavStrategy at body→down does iter 0 (no peer) → iter 1 (no parent-zone peer) → drill-out fallback returns the parent panel `task:T1`. Renamed the test, rewrote the assertion to the correct drill-out behavior, and added a `waitFor(titleField.fq)` gate before the navigation loop so the test no longer races first-field auto-focus.\n+Fix direction: rewrite each assertion to pin the production contract:\n+  expect a `spatial_focus` invocation with the resolved `fq` mapped from the kernel-returned moniker, and rely on the simulated `focus-changed` event echo (which the in-test `defaultInvokeImpl` already emits via queueMicrotask) to drive subsequent React state assertions.\n\n-Also removed an unused `registerScopeArgs` helper from `kanban-app/ui/src/components/board-view.spatial.test.tsx` (TypeScript noUnused checks were failing under `tsc --noEmit`).\n+These failures are pre-existing on branch `kanban` at commit 35a106634 — they are not caused by the `swissarmyhammer-focus/src/registry.rs` overlap-warning change.\n\n-Verified clean:\n-- `cargo test --workspace` (now `cargo nextest run --workspace`) — 13576 passed, 0 failed.\n-- `cargo clippy --workspace --all-targets -- -D warnings` — clean.\n-- `npm test` (kanban-app/ui) — 1885 passed, 0 failed (4 pre-existing intentional skips remain — see follow-up tasks).\n\\ No newline at end of file\n+What I tried:\n+- Ran full workspace tests + clippy — all Rust passes clean.\n+- Reproduced the 5 failures both with the user's working-tree changes and after stashing them. Pristine HEAD also fails. So the changes under review did not introduce these failures. #test-failure\n\\ No newline at end of file\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-browser tests: drill-in setFocus assertions check stale ui.setFocus dispatch contract\n\\ No newline at end of file\n+RESOLVED: browser tests stale ui.setFocus contract — fixed inline during /test run\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-RESOLVED: browser tests stale ui.setFocus contract — fixed inline during /test run\n\\ No newline at end of file\n+browser tests: drill-in setFocus assertions check stale ui.setFocus dispatch contract\n\\ No newline at end of file\n"}]]}
{"id":"01KQMC5RYRK9D7DQA384HTF1F4","timestamp":"2026-05-02T12:56:19.672613Z","op":"update","item_id":"01KQMBFA7G0V3ZJ70ZKYAVGGDS","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees: []\n-position_column: todo\n-position_ordinal: c180\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffee80\n title: 'RESOLVED: browser tests stale ui.setFocus contract — fixed inline during /test run'\n ---\n RESOLVED — fixed inline during a /test run on branch `kanban`.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees: []\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffee80\n+position_column: todo\n+position_ordinal: c180\n title: 'RESOLVED: browser tests stale ui.setFocus contract — fixed inline during /test run'\n ---\n RESOLVED — fixed inline during a /test run on branch `kanban`.\n","transaction_id":null}
{"id":"01KQMC5RYSQACA13928NYNPARY","timestamp":"2026-05-02T12:56:19.673362Z","entity_type":"task","entity_id":"01KQMBFA7G0V3ZJ70ZKYAVGGDS","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-c180\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffee80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffee80\n\\ No newline at end of file\n+c180\n\\ No newline at end of file\n"}]]}

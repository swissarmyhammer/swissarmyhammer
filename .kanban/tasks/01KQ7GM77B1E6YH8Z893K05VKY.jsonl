{"id":"01KQ7GM77BA0PKA1VS7ZG14JKM","timestamp":"2026-04-27T13:03:59.723932Z","op":"create","item_id":"01KQ7GM77B1E6YH8Z893K05VKY","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,176 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: a680\n+project: spatial-nav\n+title: 'Fix: double-click on perspective tab opens inspector — perspective is NOT an entity'\n+---\n+## What\n+\n+Double-clicking a perspective tab opens the inspector. It should not. **Perspective is not an entity** — perspectives are saved view-state (filter + group + sort) attached to a board, not first-class inspectable objects. The inspector exists to show details of entities (board, column, task, tag, field), so dispatching `ui.inspect` against a `perspective_tab:<id>` moniker is an architectural mismatch.\n+\n+The user reports the symptom for the perspective tab, but the root cause is broader: every UI-chrome focusable currently inherits the same incorrect behavior.\n+\n+### Why it's broken\n+\n+`kanban-app/ui/src/components/focus-scope.tsx` (and the symmetric `focus-zone.tsx`) wires an unconditional double-click handler:\n+\n+```tsx\n+// inside SpatialFocusScopeBody\n+const dispatch = useDispatchCommand(\"ui.inspect\");\n+…\n+const handleDoubleClick = useCallback((e: React.MouseEvent) => {\n+  if (!handleEvents) return;\n+  // …input/textarea/contenteditable guards…\n+  e.stopPropagation();\n+  dispatch({ target: moniker }).catch(console.error);\n+}, [dispatch, moniker, handleEvents]);\n+…\n+<div … onDoubleClick={handleDoubleClick}>\n+```\n+\n+So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker as the target on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n+\n+- `perspective_tab:<id>` — the bug the user reported.\n+- `ui:navbar`, `ui:navbar.board-selector`, `ui:navbar.inspect`, `ui:navbar.search` — nav-bar zones and buttons.\n+- `ui:toolbar.*` — toolbar action groups and buttons.\n+- `ui:perspective-bar`, `ui:perspective`, `ui:view`, `ui:board`, `ui:grid` — container zones for layout, not entities.\n+- `cell:<row>:<col>` — grid cells, addresses inside an entity, not entities themselves.\n+\n+The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` handler that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles up to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n+\n+### The fix shape — opt-in, not opt-out\n+\n+Inspect-on-double-click should be an **explicit opt-in** on `<FocusScope>` and `<FocusZone>` via a new prop `inspectOnDoubleClick?: boolean` defaulting to `false`. Only call sites that wrap an actual entity opt in.\n+\n+Why opt-in (not \"skip when moniker prefix is in a UI list\" / \"skip when no `entity:*` scope claimed\"):\n+\n+- The set of entity prefixes is owned by the kanban domain (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`). Hard-coding that list in a generic spatial primitive smears domain knowledge into UI infrastructure.\n+- A scope-based runtime check is brittle — the answer depends on whichever `CommandScopeProvider` happens to be the nearest ancestor, not on what *this* focusable represents. A perspective tab nested inside a board scope would still match `entity:board` and silently route inspect to the board.\n+- Opt-in flips the contract from \"inspect everything unless told otherwise\" (the current bug) to \"inspect only entity-bearing call sites that explicitly say so.\" That matches how the rest of the spatial primitives work (`showFocusBar`, `navOverride` — explicit, not inferred).\n+\n+### Files in scope\n+\n+**Primitive contract change:**\n+\n+- `kanban-app/ui/src/components/focus-scope.tsx`\n+  - Add `inspectOnDoubleClick?: boolean` to `FocusScopeOwnProps`. Default `false` in the destructuring.\n+  - In both `SpatialFocusScopeBody` and `FallbackFocusScopeBody`, gate the entire `dispatch({ target: moniker })` call behind that flag. When `false`, the `useDispatchCommand(\"ui.inspect\")` hook should not be called either (so the registry walk for `ui.inspect` is not paid by every UI chrome wrapper).\n+  - Update the file-level docstring section that describes the click contract.\n+\n+- `kanban-app/ui/src/components/focus-zone.tsx`\n+  - Symmetric change to `<FocusZone>` — add the same prop with the same default and gating.\n+\n+- `kanban-app/ui/src/components/focusable.tsx`\n+  - If `<Focusable>` still exists as a transitional re-export shim of `<FocusScope>` per the in-flight architecture-fix task `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, propagate the prop through. If `<Focusable>` already collapsed into `<FocusScope>`, this file is gone — skip.\n+\n+**Entity call sites that must opt IN (`inspectOnDoubleClick`):**\n+\n+- `kanban-app/ui/src/components/entity-card.tsx` — task / tag cards (`task:<id>`, `tag:<id>`).\n+- `kanban-app/ui/src/components/column-view.tsx` — column zone (`column:<id>`).\n+- `kanban-app/ui/src/components/board-view.tsx` — only the board entity wrapper, NOT the `ui:board` chrome zone.\n+- `kanban-app/ui/src/components/fields/field.tsx` — field rows (`field:<entity>:<name>`).\n+- Any other call site whose moniker is a real entity (search via `git grep` for `moniker={asMoniker(\"` and classify each hit).\n+\n+**UI-chrome call sites that must stay opt-OUT (explicitly verify, do not change):**\n+\n+- `nav-bar.tsx` — all `ui:navbar.*`.\n+- All toolbar wrappers — `ui:toolbar.*`.\n+- `perspective-tab-bar.tsx` — `ui:perspective-bar` and `perspective_tab:<id>`. The button's own `onDoubleClick` continues to call `startRename`.\n+- `perspective-container.tsx` — `ui:perspective`.\n+- `view-container.tsx` — `ui:view`.\n+- `grid-view.tsx` — `ui:grid` and `cell:<r>:<c>`.\n+- The board chrome `ui:board` zone (the *container*, not the board entity itself).\n+\n+**Architectural guard to extend:**\n+\n+- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a guard that greps `kanban-app/ui/src/` for every `moniker={asMoniker(\"ui:` or `moniker={asMoniker(\\`perspective_tab:` etc. occurrence and asserts none of those call sites carry `inspectOnDoubleClick={true}`. Conversely, add a positive guard that every `entity-card`, `column-view`, `field` call site DOES carry the prop. The list of UI-chrome prefixes lives in the test as a `BANNED_INSPECT_PREFIXES` constant alongside the existing decoration guards.\n+\n+### What this task does NOT do\n+\n+- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just won't trigger inspect.\n+- Does not touch the YAML scope `entity:perspective` (that scope serves the perspective.* command palette + context menu and is unrelated to the inspector).\n+- Does not change the right-click context menu — context-menu commands are scoped per call site already; this task is only about double-click → inspect.\n+\n+## Acceptance Criteria\n+\n+- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode (existing `onDoubleClick → startRename` behavior on the inner button).\n+- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector. `mockInvoke` is never called with a `ui_inspect`-equivalent invoke for those interactions.\n+- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved.\n+- [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n+- [ ] Double-clicking a field row DOES open the inspector for that field.\n+- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop. TypeScript flags the prop with autocompletion.\n+- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body — verified by a unit test that asserts the `command-scope` registry sees no lookup for `ui.inspect` from a chrome wrapper.\n+- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: every UI-chrome wrapper omits `inspectOnDoubleClick`, every entity wrapper sets it to `true`.\n+- [ ] `cd kanban-app/ui && npm test` is green.\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected, but verify nothing regressed).\n+\n+## Tests\n+\n+### Browser Tests (mandatory)\n+\n+Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new opt-in behavior end-to-end against real DOM, so the human user does not have to manually click around to verify.\n+\n+#### Test files\n+\n+1. `kanban-app/ui/src/components/perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (new) — locks in the reported bug.\n+2. `kanban-app/ui/src/components/focus-scope.inspect-opt-in.spatial.test.tsx` (new) — exercises the new prop.\n+3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guard for the chrome/entity classification.\n+\n+#### Setup (for browser tests)\n+\n+- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n+- Render components inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n+\n+#### Required test cases\n+\n+**File 1 — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx`:**\n+\n+1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved — the button's own `onDoubleClick` still fires); (b) the `mockInvoke` call list contains zero entries whose first arg matches `/inspect/i` and zero dispatches that route to `ui.inspect`. The simplest assertion: spy on `useDispatchCommand` (or assert via the command-scope registry) that no `ui.inspect` dispatch was issued for `perspective_tab:*`.\n+\n+2. **Regression: dblclick on the perspective bar's wrapping zone does NOT inspect** — double-click on the `ui:perspective-bar` zone background (whitespace inside the bar, not on a tab). Assert no `ui.inspect` dispatch.\n+\n+3. **Single-click still focuses** — single-click on a tab still calls `mockInvoke(\"spatial_focus\", { key })`. Confirms we did not regress the focus path.\n+\n+**File 2 — `focus-scope.inspect-opt-in.spatial.test.tsx`:**\n+\n+1. **Default (no prop) → no inspect** — render a bare `<FocusScope moniker=\"task:fake\">` (no `inspectOnDoubleClick`). Double-click. Assert no `ui.inspect` dispatch.\n+\n+2. **`inspectOnDoubleClick={true}` → inspect fires** — render `<FocusScope moniker=\"task:fake\" inspectOnDoubleClick>`. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:fake\" }`.\n+\n+3. **`inspectOnDoubleClick={false}` explicit → no inspect** — same as default but with the prop set explicitly. Assert no dispatch (locks in the prop semantics).\n+\n+4. **No `useDispatchCommand(\"ui.inspect\")` registration when opt-out** — render a `<FocusScope>` with `inspectOnDoubleClick={false}` inside a test harness that records every `useDispatchCommand` call. Assert `\"ui.inspect\"` was never registered. Reason: avoid paying the registry walk + memoization cost for every chrome focusable.\n+\n+5. **Symmetric for `<FocusZone>`** — repeat the four cases above against `<FocusZone>`.\n+\n+6. **Entity opt-in real-world: card** — render `<EntityCard>` (or its column-view equivalent that wraps a card) for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`.\n+\n+7. **Entity opt-in real-world: column** — render a `<ColumnView>` whose wrapper sets `inspectOnDoubleClick`. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n+\n+8. **Inner button stops propagation contract** — render `<FocusScope inspectOnDoubleClick><button onDoubleClick={(e) => e.stopPropagation()}>x</button></FocusScope>` and double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n+\n+#### Source-level guard — extend `focus-architecture.guards.node.test.ts`\n+\n+```ts\n+const CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"];\n+const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n+```\n+\n+- Walk every `*.tsx` source file under `kanban-app/ui/src/`.\n+- For each `<FocusScope`, `<FocusZone`, or `<Focusable` element whose `moniker={asMoniker(\"<prefix>...\")}` matches a CHROME prefix, assert the same JSX element does NOT contain `inspectOnDoubleClick`. This catches \"someone added `inspectOnDoubleClick` to a nav-bar button.\"\n+- For each element whose moniker matches an ENTITY prefix, assert the element DOES contain `inspectOnDoubleClick` (or `inspectOnDoubleClick={true}`). This catches \"someone wrapped a new card type but forgot the opt-in.\" Allow a per-call-site `// inspect:exempt` comment in case there's a special-case entity not meant to be inspectable (extremely unlikely).\n+- Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n+\n+### How to run\n+\n+```\n+cd kanban-app/ui && npm test\n+```\n+\n+Headless on CI. The CI workflow `.github/workflows/*.yml` already runs this command.\n+\n+## Workflow\n+\n+- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default in `focus-scope.tsx` / `focus-zone.tsx`, then opt in at every entity call site, then add the guard. Each step keeps the test suite stable.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,176 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: a680\n-project: spatial-nav\n-title: 'Fix: double-click on perspective tab opens inspector — perspective is NOT an entity'\n----\n-## What\n-\n-Double-clicking a perspective tab opens the inspector. It should not. **Perspective is not an entity** — perspectives are saved view-state (filter + group + sort) attached to a board, not first-class inspectable objects. The inspector exists to show details of entities (board, column, task, tag, field), so dispatching `ui.inspect` against a `perspective_tab:<id>` moniker is an architectural mismatch.\n-\n-The user reports the symptom for the perspective tab, but the root cause is broader: every UI-chrome focusable currently inherits the same incorrect behavior.\n-\n-### Why it's broken\n-\n-`kanban-app/ui/src/components/focus-scope.tsx` (and the symmetric `focus-zone.tsx`) wires an unconditional double-click handler:\n-\n-```tsx\n-// inside SpatialFocusScopeBody\n-const dispatch = useDispatchCommand(\"ui.inspect\");\n-…\n-const handleDoubleClick = useCallback((e: React.MouseEvent) => {\n-  if (!handleEvents) return;\n-  // …input/textarea/contenteditable guards…\n-  e.stopPropagation();\n-  dispatch({ target: moniker }).catch(console.error);\n-}, [dispatch, moniker, handleEvents]);\n-…\n-<div … onDoubleClick={handleDoubleClick}>\n-```\n-\n-So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker as the target on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n-\n-- `perspective_tab:<id>` — the bug the user reported.\n-- `ui:navbar`, `ui:navbar.board-selector`, `ui:navbar.inspect`, `ui:navbar.search` — nav-bar zones and buttons.\n-- `ui:toolbar.*` — toolbar action groups and buttons.\n-- `ui:perspective-bar`, `ui:perspective`, `ui:view`, `ui:board`, `ui:grid` — container zones for layout, not entities.\n-- `cell:<row>:<col>` — grid cells, addresses inside an entity, not entities themselves.\n-\n-The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` handler that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles up to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n-\n-### The fix shape — opt-in, not opt-out\n-\n-Inspect-on-double-click should be an **explicit opt-in** on `<FocusScope>` and `<FocusZone>` via a new prop `inspectOnDoubleClick?: boolean` defaulting to `false`. Only call sites that wrap an actual entity opt in.\n-\n-Why opt-in (not \"skip when moniker prefix is in a UI list\" / \"skip when no `entity:*` scope claimed\"):\n-\n-- The set of entity prefixes is owned by the kanban domain (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`). Hard-coding that list in a generic spatial primitive smears domain knowledge into UI infrastructure.\n-- A scope-based runtime check is brittle — the answer depends on whichever `CommandScopeProvider` happens to be the nearest ancestor, not on what *this* focusable represents. A perspective tab nested inside a board scope would still match `entity:board` and silently route inspect to the board.\n-- Opt-in flips the contract from \"inspect everything unless told otherwise\" (the current bug) to \"inspect only entity-bearing call sites that explicitly say so.\" That matches how the rest of the spatial primitives work (`showFocusBar`, `navOverride` — explicit, not inferred).\n-\n-### Files in scope\n-\n-**Primitive contract change:**\n-\n-- `kanban-app/ui/src/components/focus-scope.tsx`\n-  - Add `inspectOnDoubleClick?: boolean` to `FocusScopeOwnProps`. Default `false` in the destructuring.\n-  - In both `SpatialFocusScopeBody` and `FallbackFocusScopeBody`, gate the entire `dispatch({ target: moniker })` call behind that flag. When `false`, the `useDispatchCommand(\"ui.inspect\")` hook should not be called either (so the registry walk for `ui.inspect` is not paid by every UI chrome wrapper).\n-  - Update the file-level docstring section that describes the click contract.\n-\n-- `kanban-app/ui/src/components/focus-zone.tsx`\n-  - Symmetric change to `<FocusZone>` — add the same prop with the same default and gating.\n-\n-- `kanban-app/ui/src/components/focusable.tsx`\n-  - If `<Focusable>` still exists as a transitional re-export shim of `<FocusScope>` per the in-flight architecture-fix task `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, propagate the prop through. If `<Focusable>` already collapsed into `<FocusScope>`, this file is gone — skip.\n-\n-**Entity call sites that must opt IN (`inspectOnDoubleClick`):**\n-\n-- `kanban-app/ui/src/components/entity-card.tsx` — task / tag cards (`task:<id>`, `tag:<id>`).\n-- `kanban-app/ui/src/components/column-view.tsx` — column zone (`column:<id>`).\n-- `kanban-app/ui/src/components/board-view.tsx` — only the board entity wrapper, NOT the `ui:board` chrome zone.\n-- `kanban-app/ui/src/components/fields/field.tsx` — field rows (`field:<entity>:<name>`).\n-- Any other call site whose moniker is a real entity (search via `git grep` for `moniker={asMoniker(\"` and classify each hit).\n-\n-**UI-chrome call sites that must stay opt-OUT (explicitly verify, do not change):**\n-\n-- `nav-bar.tsx` — all `ui:navbar.*`.\n-- All toolbar wrappers — `ui:toolbar.*`.\n-- `perspective-tab-bar.tsx` — `ui:perspective-bar` and `perspective_tab:<id>`. The button's own `onDoubleClick` continues to call `startRename`.\n-- `perspective-container.tsx` — `ui:perspective`.\n-- `view-container.tsx` — `ui:view`.\n-- `grid-view.tsx` — `ui:grid` and `cell:<r>:<c>`.\n-- The board chrome `ui:board` zone (the *container*, not the board entity itself).\n-\n-**Architectural guard to extend:**\n-\n-- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a guard that greps `kanban-app/ui/src/` for every `moniker={asMoniker(\"ui:` or `moniker={asMoniker(\\`perspective_tab:` etc. occurrence and asserts none of those call sites carry `inspectOnDoubleClick={true}`. Conversely, add a positive guard that every `entity-card`, `column-view`, `field` call site DOES carry the prop. The list of UI-chrome prefixes lives in the test as a `BANNED_INSPECT_PREFIXES` constant alongside the existing decoration guards.\n-\n-### What this task does NOT do\n-\n-- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just won't trigger inspect.\n-- Does not touch the YAML scope `entity:perspective` (that scope serves the perspective.* command palette + context menu and is unrelated to the inspector).\n-- Does not change the right-click context menu — context-menu commands are scoped per call site already; this task is only about double-click → inspect.\n-\n-## Acceptance Criteria\n-\n-- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode (existing `onDoubleClick → startRename` behavior on the inner button).\n-- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector. `mockInvoke` is never called with a `ui_inspect`-equivalent invoke for those interactions.\n-- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved.\n-- [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n-- [ ] Double-clicking a field row DOES open the inspector for that field.\n-- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop. TypeScript flags the prop with autocompletion.\n-- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body — verified by a unit test that asserts the `command-scope` registry sees no lookup for `ui.inspect` from a chrome wrapper.\n-- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: every UI-chrome wrapper omits `inspectOnDoubleClick`, every entity wrapper sets it to `true`.\n-- [ ] `cd kanban-app/ui && npm test` is green.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected, but verify nothing regressed).\n-\n-## Tests\n-\n-### Browser Tests (mandatory)\n-\n-Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new opt-in behavior end-to-end against real DOM, so the human user does not have to manually click around to verify.\n-\n-#### Test files\n-\n-1. `kanban-app/ui/src/components/perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (new) — locks in the reported bug.\n-2. `kanban-app/ui/src/components/focus-scope.inspect-opt-in.spatial.test.tsx` (new) — exercises the new prop.\n-3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guard for the chrome/entity classification.\n-\n-#### Setup (for browser tests)\n-\n-- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n-- Render components inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n-\n-#### Required test cases\n-\n-**File 1 — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx`:**\n-\n-1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved — the button's own `onDoubleClick` still fires); (b) the `mockInvoke` call list contains zero entries whose first arg matches `/inspect/i` and zero dispatches that route to `ui.inspect`. The simplest assertion: spy on `useDispatchCommand` (or assert via the command-scope registry) that no `ui.inspect` dispatch was issued for `perspective_tab:*`.\n-\n-2. **Regression: dblclick on the perspective bar's wrapping zone does NOT inspect** — double-click on the `ui:perspective-bar` zone background (whitespace inside the bar, not on a tab). Assert no `ui.inspect` dispatch.\n-\n-3. **Single-click still focuses** — single-click on a tab still calls `mockInvoke(\"spatial_focus\", { key })`. Confirms we did not regress the focus path.\n-\n-**File 2 — `focus-scope.inspect-opt-in.spatial.test.tsx`:**\n-\n-1. **Default (no prop) → no inspect** — render a bare `<FocusScope moniker=\"task:fake\">` (no `inspectOnDoubleClick`). Double-click. Assert no `ui.inspect` dispatch.\n-\n-2. **`inspectOnDoubleClick={true}` → inspect fires** — render `<FocusScope moniker=\"task:fake\" inspectOnDoubleClick>`. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:fake\" }`.\n-\n-3. **`inspectOnDoubleClick={false}` explicit → no inspect** — same as default but with the prop set explicitly. Assert no dispatch (locks in the prop semantics).\n-\n-4. **No `useDispatchCommand(\"ui.inspect\")` registration when opt-out** — render a `<FocusScope>` with `inspectOnDoubleClick={false}` inside a test harness that records every `useDispatchCommand` call. Assert `\"ui.inspect\"` was never registered. Reason: avoid paying the registry walk + memoization cost for every chrome focusable.\n-\n-5. **Symmetric for `<FocusZone>`** — repeat the four cases above against `<FocusZone>`.\n-\n-6. **Entity opt-in real-world: card** — render `<EntityCard>` (or its column-view equivalent that wraps a card) for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`.\n-\n-7. **Entity opt-in real-world: column** — render a `<ColumnView>` whose wrapper sets `inspectOnDoubleClick`. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n-\n-8. **Inner button stops propagation contract** — render `<FocusScope inspectOnDoubleClick><button onDoubleClick={(e) => e.stopPropagation()}>x</button></FocusScope>` and double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n-\n-#### Source-level guard — extend `focus-architecture.guards.node.test.ts`\n-\n-```ts\n-const CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"];\n-const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n-```\n-\n-- Walk every `*.tsx` source file under `kanban-app/ui/src/`.\n-- For each `<FocusScope`, `<FocusZone`, or `<Focusable` element whose `moniker={asMoniker(\"<prefix>...\")}` matches a CHROME prefix, assert the same JSX element does NOT contain `inspectOnDoubleClick`. This catches \"someone added `inspectOnDoubleClick` to a nav-bar button.\"\n-- For each element whose moniker matches an ENTITY prefix, assert the element DOES contain `inspectOnDoubleClick` (or `inspectOnDoubleClick={true}`). This catches \"someone wrapped a new card type but forgot the opt-in.\" Allow a per-call-site `// inspect:exempt` comment in case there's a special-case entity not meant to be inspectable (extremely unlikely).\n-- Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n-\n-### How to run\n-\n-```\n-cd kanban-app/ui && npm test\n-```\n-\n-Headless on CI. The CI workflow `.github/workflows/*.yml` already runs this command.\n-\n-## Workflow\n-\n-- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default in `focus-scope.tsx` / `focus-zone.tsx`, then opt in at every entity call site, then add the guard. Each step keeps the test suite stable.\n","transaction_id":null}
{"id":"01KQ7GM77CHBVG302YR7EA7J9R","timestamp":"2026-04-27T13:03:59.724596Z","entity_type":"task","entity_id":"01KQ7GM77B1E6YH8Z893K05VKY","op":"create","changes":[["title",{"kind":"set","value":"Fix: double-click on perspective tab opens inspector — perspective is NOT an entity"}],["position_ordinal",{"kind":"set","value":"a680"}],["body",{"kind":"set","value":"## What\n\nDouble-clicking a perspective tab opens the inspector. It should not. **Perspective is not an entity** — perspectives are saved view-state (filter + group + sort) attached to a board, not first-class inspectable objects. The inspector exists to show details of entities (board, column, task, tag, field), so dispatching `ui.inspect` against a `perspective_tab:<id>` moniker is an architectural mismatch.\n\nThe user reports the symptom for the perspective tab, but the root cause is broader: every UI-chrome focusable currently inherits the same incorrect behavior.\n\n### Why it's broken\n\n`kanban-app/ui/src/components/focus-scope.tsx` (and the symmetric `focus-zone.tsx`) wires an unconditional double-click handler:\n\n```tsx\n// inside SpatialFocusScopeBody\nconst dispatch = useDispatchCommand(\"ui.inspect\");\n…\nconst handleDoubleClick = useCallback((e: React.MouseEvent) => {\n  if (!handleEvents) return;\n  // …input/textarea/contenteditable guards…\n  e.stopPropagation();\n  dispatch({ target: moniker }).catch(console.error);\n}, [dispatch, moniker, handleEvents]);\n…\n<div … onDoubleClick={handleDoubleClick}>\n```\n\nSo **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker as the target on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n\n- `perspective_tab:<id>` — the bug the user reported.\n- `ui:navbar`, `ui:navbar.board-selector`, `ui:navbar.inspect`, `ui:navbar.search` — nav-bar zones and buttons.\n- `ui:toolbar.*` — toolbar action groups and buttons.\n- `ui:perspective-bar`, `ui:perspective`, `ui:view`, `ui:board`, `ui:grid` — container zones for layout, not entities.\n- `cell:<row>:<col>` — grid cells, addresses inside an entity, not entities themselves.\n\nThe perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` handler that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles up to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n\n### The fix shape — opt-in, not opt-out\n\nInspect-on-double-click should be an **explicit opt-in** on `<FocusScope>` and `<FocusZone>` via a new prop `inspectOnDoubleClick?: boolean` defaulting to `false`. Only call sites that wrap an actual entity opt in.\n\nWhy opt-in (not \"skip when moniker prefix is in a UI list\" / \"skip when no `entity:*` scope claimed\"):\n\n- The set of entity prefixes is owned by the kanban domain (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`). Hard-coding that list in a generic spatial primitive smears domain knowledge into UI infrastructure.\n- A scope-based runtime check is brittle — the answer depends on whichever `CommandScopeProvider` happens to be the nearest ancestor, not on what *this* focusable represents. A perspective tab nested inside a board scope would still match `entity:board` and silently route inspect to the board.\n- Opt-in flips the contract from \"inspect everything unless told otherwise\" (the current bug) to \"inspect only entity-bearing call sites that explicitly say so.\" That matches how the rest of the spatial primitives work (`showFocusBar`, `navOverride` — explicit, not inferred).\n\n### Files in scope\n\n**Primitive contract change:**\n\n- `kanban-app/ui/src/components/focus-scope.tsx`\n  - Add `inspectOnDoubleClick?: boolean` to `FocusScopeOwnProps`. Default `false` in the destructuring.\n  - In both `SpatialFocusScopeBody` and `FallbackFocusScopeBody`, gate the entire `dispatch({ target: moniker })` call behind that flag. When `false`, the `useDispatchCommand(\"ui.inspect\")` hook should not be called either (so the registry walk for `ui.inspect` is not paid by every UI chrome wrapper).\n  - Update the file-level docstring section that describes the click contract.\n\n- `kanban-app/ui/src/components/focus-zone.tsx`\n  - Symmetric change to `<FocusZone>` — add the same prop with the same default and gating.\n\n- `kanban-app/ui/src/components/focusable.tsx`\n  - If `<Focusable>` still exists as a transitional re-export shim of `<FocusScope>` per the in-flight architecture-fix task `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, propagate the prop through. If `<Focusable>` already collapsed into `<FocusScope>`, this file is gone — skip.\n\n**Entity call sites that must opt IN (`inspectOnDoubleClick`):**\n\n- `kanban-app/ui/src/components/entity-card.tsx` — task / tag cards (`task:<id>`, `tag:<id>`).\n- `kanban-app/ui/src/components/column-view.tsx` — column zone (`column:<id>`).\n- `kanban-app/ui/src/components/board-view.tsx` — only the board entity wrapper, NOT the `ui:board` chrome zone.\n- `kanban-app/ui/src/components/fields/field.tsx` — field rows (`field:<entity>:<name>`).\n- Any other call site whose moniker is a real entity (search via `git grep` for `moniker={asMoniker(\"` and classify each hit).\n\n**UI-chrome call sites that must stay opt-OUT (explicitly verify, do not change):**\n\n- `nav-bar.tsx` — all `ui:navbar.*`.\n- All toolbar wrappers — `ui:toolbar.*`.\n- `perspective-tab-bar.tsx` — `ui:perspective-bar` and `perspective_tab:<id>`. The button's own `onDoubleClick` continues to call `startRename`.\n- `perspective-container.tsx` — `ui:perspective`.\n- `view-container.tsx` — `ui:view`.\n- `grid-view.tsx` — `ui:grid` and `cell:<r>:<c>`.\n- The board chrome `ui:board` zone (the *container*, not the board entity itself).\n\n**Architectural guard to extend:**\n\n- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a guard that greps `kanban-app/ui/src/` for every `moniker={asMoniker(\"ui:` or `moniker={asMoniker(\\`perspective_tab:` etc. occurrence and asserts none of those call sites carry `inspectOnDoubleClick={true}`. Conversely, add a positive guard that every `entity-card`, `column-view`, `field` call site DOES carry the prop. The list of UI-chrome prefixes lives in the test as a `BANNED_INSPECT_PREFIXES` constant alongside the existing decoration guards.\n\n### What this task does NOT do\n\n- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just won't trigger inspect.\n- Does not touch the YAML scope `entity:perspective` (that scope serves the perspective.* command palette + context menu and is unrelated to the inspector).\n- Does not change the right-click context menu — context-menu commands are scoped per call site already; this task is only about double-click → inspect.\n\n## Acceptance Criteria\n\n- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode (existing `onDoubleClick → startRename` behavior on the inner button).\n- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector. `mockInvoke` is never called with a `ui_inspect`-equivalent invoke for those interactions.\n- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved.\n- [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n- [ ] Double-clicking a field row DOES open the inspector for that field.\n- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop. TypeScript flags the prop with autocompletion.\n- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body — verified by a unit test that asserts the `command-scope` registry sees no lookup for `ui.inspect` from a chrome wrapper.\n- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: every UI-chrome wrapper omits `inspectOnDoubleClick`, every entity wrapper sets it to `true`.\n- [ ] `cd kanban-app/ui && npm test` is green.\n- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected, but verify nothing regressed).\n\n## Tests\n\n### Browser Tests (mandatory)\n\nRun under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new opt-in behavior end-to-end against real DOM, so the human user does not have to manually click around to verify.\n\n#### Test files\n\n1. `kanban-app/ui/src/components/perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (new) — locks in the reported bug.\n2. `kanban-app/ui/src/components/focus-scope.inspect-opt-in.spatial.test.tsx` (new) — exercises the new prop.\n3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guard for the chrome/entity classification.\n\n#### Setup (for browser tests)\n\n- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n- Render components inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n\n#### Required test cases\n\n**File 1 — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx`:**\n\n1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved — the button's own `onDoubleClick` still fires); (b) the `mockInvoke` call list contains zero entries whose first arg matches `/inspect/i` and zero dispatches that route to `ui.inspect`. The simplest assertion: spy on `useDispatchCommand` (or assert via the command-scope registry) that no `ui.inspect` dispatch was issued for `perspective_tab:*`.\n\n2. **Regression: dblclick on the perspective bar's wrapping zone does NOT inspect** — double-click on the `ui:perspective-bar` zone background (whitespace inside the bar, not on a tab). Assert no `ui.inspect` dispatch.\n\n3. **Single-click still focuses** — single-click on a tab still calls `mockInvoke(\"spatial_focus\", { key })`. Confirms we did not regress the focus path.\n\n**File 2 — `focus-scope.inspect-opt-in.spatial.test.tsx`:**\n\n1. **Default (no prop) → no inspect** — render a bare `<FocusScope moniker=\"task:fake\">` (no `inspectOnDoubleClick`). Double-click. Assert no `ui.inspect` dispatch.\n\n2. **`inspectOnDoubleClick={true}` → inspect fires** — render `<FocusScope moniker=\"task:fake\" inspectOnDoubleClick>`. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:fake\" }`.\n\n3. **`inspectOnDoubleClick={false}` explicit → no inspect** — same as default but with the prop set explicitly. Assert no dispatch (locks in the prop semantics).\n\n4. **No `useDispatchCommand(\"ui.inspect\")` registration when opt-out** — render a `<FocusScope>` with `inspectOnDoubleClick={false}` inside a test harness that records every `useDispatchCommand` call. Assert `\"ui.inspect\"` was never registered. Reason: avoid paying the registry walk + memoization cost for every chrome focusable.\n\n5. **Symmetric for `<FocusZone>`** — repeat the four cases above against `<FocusZone>`.\n\n6. **Entity opt-in real-world: card** — render `<EntityCard>` (or its column-view equivalent that wraps a card) for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`.\n\n7. **Entity opt-in real-world: column** — render a `<ColumnView>` whose wrapper sets `inspectOnDoubleClick`. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n\n8. **Inner button stops propagation contract** — render `<FocusScope inspectOnDoubleClick><button onDoubleClick={(e) => e.stopPropagation()}>x</button></FocusScope>` and double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n\n#### Source-level guard — extend `focus-architecture.guards.node.test.ts`\n\n```ts\nconst CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"];\nconst ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n```\n\n- Walk every `*.tsx` source file under `kanban-app/ui/src/`.\n- For each `<FocusScope`, `<FocusZone`, or `<Focusable` element whose `moniker={asMoniker(\"<prefix>...\")}` matches a CHROME prefix, assert the same JSX element does NOT contain `inspectOnDoubleClick`. This catches \"someone added `inspectOnDoubleClick` to a nav-bar button.\"\n- For each element whose moniker matches an ENTITY prefix, assert the element DOES contain `inspectOnDoubleClick` (or `inspectOnDoubleClick={true}`). This catches \"someone wrapped a new card type but forgot the opt-in.\" Allow a per-call-site `// inspect:exempt` comment in case there's a special-case entity not meant to be inspectable (extremely unlikely).\n- Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n\n### How to run\n\n```\ncd kanban-app/ui && npm test\n```\n\nHeadless on CI. The CI workflow `.github/workflows/*.yml` already runs this command.\n\n## Workflow\n\n- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default in `focus-scope.tsx` / `focus-zone.tsx`, then opt in at every entity call site, then add the guard. Each step keeps the test suite stable.\n"}],["position_column",{"kind":"set","value":"todo"}],["assignees",{"kind":"set","value":["claude-code"]}],["project",{"kind":"set","value":"spatial-nav"}]]}
{"id":"01KQ7J4RZ3M53ATTBHJT1D0GGQ","timestamp":"2026-04-27T13:30:30.755802Z","op":"update","item_id":"01KQ7GM77B1E6YH8Z893K05VKY","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: a680\n+position_column: doing\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Fix: double-click on perspective tab opens inspector — perspective is NOT an entity'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: todo\n+position_ordinal: a680\n project: spatial-nav\n title: 'Fix: double-click on perspective tab opens inspector — perspective is NOT an entity'\n ---\n","transaction_id":null}
{"id":"01KQ7J4RZ5JB9FDDM59SWY83SN","timestamp":"2026-04-27T13:30:30.757092Z","entity_type":"task","entity_id":"01KQ7GM77B1E6YH8Z893K05VKY","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-a680\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+a680\n\\ No newline at end of file\n"}]]}
{"id":"01KQ7K1BDC3X2SKKACQQC1F0JB","timestamp":"2026-04-27T13:46:07.148115Z","op":"update","item_id":"01KQ7GM77B1E6YH8Z893K05VKY","forward_patch":"--- original\n+++ modified\n@@ -17,92 +17,132 @@\n `kanban-app/ui/src/components/focus-scope.tsx` (and the symmetric `focus-zone.tsx`) wires an unconditional double-click handler:\n\n ```tsx\n-// inside SpatialFocusScopeBody\n const dispatch = useDispatchCommand(\"ui.inspect\");\n …\n-const handleDoubleClick = useCallback((e: React.MouseEvent) => {\n-  if (!handleEvents) return;\n-  // …input/textarea/contenteditable guards…\n+const handleDoubleClick = useCallback((e) => {\n+  // …guards…\n   e.stopPropagation();\n   dispatch({ target: moniker }).catch(console.error);\n }, [dispatch, moniker, handleEvents]);\n-…\n-<div … onDoubleClick={handleDoubleClick}>\n ```\n\n-So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker as the target on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n+So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n\n - `perspective_tab:<id>` — the bug the user reported.\n-- `ui:navbar`, `ui:navbar.board-selector`, `ui:navbar.inspect`, `ui:navbar.search` — nav-bar zones and buttons.\n+- `ui:navbar`, `ui:navbar.*` — nav-bar zones and buttons.\n - `ui:toolbar.*` — toolbar action groups and buttons.\n - `ui:perspective-bar`, `ui:perspective`, `ui:view`, `ui:board`, `ui:grid` — container zones for layout, not entities.\n - `cell:<row>:<col>` — grid cells, addresses inside an entity, not entities themselves.\n\n-The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` handler that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles up to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n+The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n\n-### The fix shape — opt-in, not opt-out\n+### Fix shape — introduce `<Inspectable>`\n\n-Inspect-on-double-click should be an **explicit opt-in** on `<FocusScope>` and `<FocusZone>` via a new prop `inspectOnDoubleClick?: boolean` defaulting to `false`. Only call sites that wrap an actual entity opt in.\n+Pull the `useDispatchCommand(\"ui.inspect\")` hook and the double-click → dispatch handler **out** of `<FocusScope>` and `<FocusZone>` entirely, and put it in a new dedicated component:\n\n-Why opt-in (not \"skip when moniker prefix is in a UI list\" / \"skip when no `entity:*` scope claimed\"):\n+```tsx\n+// kanban-app/ui/src/components/inspectable.tsx\n+export interface InspectableProps {\n+  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n+  moniker: Moniker;\n+  children: ReactNode;\n+}\n\n-- The set of entity prefixes is owned by the kanban domain (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`). Hard-coding that list in a generic spatial primitive smears domain knowledge into UI infrastructure.\n-- A scope-based runtime check is brittle — the answer depends on whichever `CommandScopeProvider` happens to be the nearest ancestor, not on what *this* focusable represents. A perspective tab nested inside a board scope would still match `entity:board` and silently route inspect to the board.\n-- Opt-in flips the contract from \"inspect everything unless told otherwise\" (the current bug) to \"inspect only entity-bearing call sites that explicitly say so.\" That matches how the rest of the spatial primitives work (`showFocusBar`, `navOverride` — explicit, not inferred).\n+export function Inspectable({ moniker, children }: InspectableProps) {\n+  const dispatch = useDispatchCommand(\"ui.inspect\");\n+  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n+    const target = e.target as HTMLElement;\n+    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n+    if (target.closest(\"[contenteditable]\")) return;\n+    e.stopPropagation();\n+    dispatch({ target: moniker }).catch(console.error);\n+  }, [dispatch, moniker]);\n\n+  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n+}\n+```\n+\n+Then entity wrappers compose explicitly:\n+\n+```tsx\n+// entity-card.tsx (entity)\n+<Inspectable moniker={taskMoniker}>\n+  <FocusScope moniker={taskMoniker}>\n+    {cardBody}\n+  </FocusScope>\n+</Inspectable>\n+\n+// perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n+<FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n+  {tabContents}\n+</FocusScope>\n+```\n+\n+Why a wrapper component, not a boolean prop on `<FocusScope>`:\n+\n+- **The name encodes the architectural rule.** `<Inspectable moniker=\"task:01\">` reads as \"this DOM subtree represents an inspectable entity.\" Anyone reading the JSX knows the contract without consulting prop docs. A `inspectOnDoubleClick={true}` boolean is just a flag — the *meaning* (the entity-vs-chrome distinction) is buried in conventions about which prop value goes where.\n+- **Separation of concerns.** `<FocusScope>` and `<FocusZone>` are generic spatial primitives — they should know nothing about `ui.inspect` or the kanban command system. Ripping the dispatch out makes the primitives smaller, simpler, and reusable for future spatial UIs that have nothing to do with inspectors.\n+- **Composability.** A single entity that needs both inspect-on-double-click and a focus zone (e.g. an editable card) can compose `<Inspectable><FocusZone>…</FocusZone></Inspectable>`. The boolean would have to be duplicated on both `<FocusScope>` AND `<FocusZone>`, with no obvious answer to \"what if both nest?\"\n+- **Type-system enforcement.** `<Inspectable>` requires a `moniker` whose runtime shape can be validated (e.g. via a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes). A boolean offers no such enforcement.\n+- **The chrome path costs nothing.** When a `<FocusScope>` or `<FocusZone>` has no `<Inspectable>` ancestor, no `useDispatchCommand(\"ui.inspect\")` registration happens at all — saving the registry walk on every chrome wrapper.\n+\n ### Files in scope\n\n-**Primitive contract change:**\n+**New file:**\n+\n+- `kanban-app/ui/src/components/inspectable.tsx` — the new wrapper component. Contains: the component, its props interface, a docstring explaining the contract (\"only entities; never UI chrome\"), and unit-tested input/textarea/contenteditable guards.\n+\n+**Primitive contract change (remove inspect logic):**\n\n - `kanban-app/ui/src/components/focus-scope.tsx`\n-  - Add `inspectOnDoubleClick?: boolean` to `FocusScopeOwnProps`. Default `false` in the destructuring.\n-  - In both `SpatialFocusScopeBody` and `FallbackFocusScopeBody`, gate the entire `dispatch({ target: moniker })` call behind that flag. When `false`, the `useDispatchCommand(\"ui.inspect\")` hook should not be called either (so the registry walk for `ui.inspect` is not paid by every UI chrome wrapper).\n-  - Update the file-level docstring section that describes the click contract.\n+  - Delete `const dispatch = useDispatchCommand(\"ui.inspect\");` and the `handleDoubleClick` body that dispatches it. Keep `onDoubleClick` on the rendered `<div>` only if other behavior is still needed (e.g. `e.stopPropagation()` for nested-zone propagation control); otherwise remove the handler entirely.\n+  - Delete the same in `FallbackFocusScopeBody`.\n+  - Update the file-level docstring to describe the new contract: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n\n - `kanban-app/ui/src/components/focus-zone.tsx`\n-  - Symmetric change to `<FocusZone>` — add the same prop with the same default and gating.\n+  - Symmetric removal of the inspect dispatch. Same docstring update.\n\n-- `kanban-app/ui/src/components/focusable.tsx`\n-  - If `<Focusable>` still exists as a transitional re-export shim of `<FocusScope>` per the in-flight architecture-fix task `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, propagate the prop through. If `<Focusable>` already collapsed into `<FocusScope>`, this file is gone — skip.\n+- `kanban-app/ui/src/components/focusable.tsx` — if it still exists as a transitional re-export shim per `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, no change beyond confirming the inspect logic is gone.\n\n-**Entity call sites that must opt IN (`inspectOnDoubleClick`):**\n+**Entity call sites that must wrap in `<Inspectable>`:**\n\n-- `kanban-app/ui/src/components/entity-card.tsx` — task / tag cards (`task:<id>`, `tag:<id>`).\n+- `kanban-app/ui/src/components/entity-card.tsx` — `task:<id>`, `tag:<id>`.\n - `kanban-app/ui/src/components/column-view.tsx` — column zone (`column:<id>`).\n - `kanban-app/ui/src/components/board-view.tsx` — only the board entity wrapper, NOT the `ui:board` chrome zone.\n - `kanban-app/ui/src/components/fields/field.tsx` — field rows (`field:<entity>:<name>`).\n-- Any other call site whose moniker is a real entity (search via `git grep` for `moniker={asMoniker(\"` and classify each hit).\n+- Any other call site whose moniker is a real entity — search via `git grep \"moniker={asMoniker(\\\"[a-z]*:\"` and classify each hit.\n\n-**UI-chrome call sites that must stay opt-OUT (explicitly verify, do not change):**\n+**UI-chrome call sites (no `<Inspectable>` — verify):**\n\n-- `nav-bar.tsx` — all `ui:navbar.*`.\n-- All toolbar wrappers — `ui:toolbar.*`.\n-- `perspective-tab-bar.tsx` — `ui:perspective-bar` and `perspective_tab:<id>`. The button's own `onDoubleClick` continues to call `startRename`.\n-- `perspective-container.tsx` — `ui:perspective`.\n-- `view-container.tsx` — `ui:view`.\n-- `grid-view.tsx` — `ui:grid` and `cell:<r>:<c>`.\n-- The board chrome `ui:board` zone (the *container*, not the board entity itself).\n+- `nav-bar.tsx`, `toolbar.tsx`, `perspective-tab-bar.tsx`, `perspective-container.tsx`, `view-container.tsx`, `grid-view.tsx` cells, the `ui:board` chrome zone.\n\n-**Architectural guard to extend:**\n+**Architectural guard:**\n+\n+- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a node-side source-walk guard:\n+  - **No `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`.** Greps the entire `kanban-app/ui/src/` tree (excluding test files and `inspectable.tsx` itself) for the literal string `useDispatchCommand(\"ui.inspect\")` and asserts zero matches. This catches any future regression where someone re-introduces the dispatch into a primitive.\n+  - **No CHROME-prefixed moniker is wrapped in `<Inspectable>`.** Walks `*.tsx`, finds every `<Inspectable …>` JSX element, parses its `moniker` prop value, and asserts the prefix is in an `ENTITY_PREFIXES` allowlist (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n+  - **Every `task:<id>` / `column:<id>` / `field:` JSX wrapper has an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new card type but forgot to wrap it.\" Use a simple regex pass that fails the test when an entity-monikered `<FocusScope>` / `<FocusZone>` does not have a sibling/ancestor `<Inspectable>` carrying the same moniker.\n\n-- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a guard that greps `kanban-app/ui/src/` for every `moniker={asMoniker(\"ui:` or `moniker={asMoniker(\\`perspective_tab:` etc. occurrence and asserts none of those call sites carry `inspectOnDoubleClick={true}`. Conversely, add a positive guard that every `entity-card`, `column-view`, `field` call site DOES carry the prop. The list of UI-chrome prefixes lives in the test as a `BANNED_INSPECT_PREFIXES` constant alongside the existing decoration guards.\n+### Optional, recommended: brand the moniker\n+\n+Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that throws / `never`s when `s` doesn't start with an entity prefix, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If the codebase already brands monikers (`Moniker` is itself branded per `@/types/spatial`), extend that branding with `EntityMoniker` as a sub-brand. If extending is more than a small change, file a follow-up task — the runtime guards in `focus-architecture.guards.node.test.ts` are sufficient for this task.\n\n ### What this task does NOT do\n\n-- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just won't trigger inspect.\n+- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just no longer triggers inspect.\n - Does not touch the YAML scope `entity:perspective` (that scope serves the perspective.* command palette + context menu and is unrelated to the inspector).\n - Does not change the right-click context menu — context-menu commands are scoped per call site already; this task is only about double-click → inspect.\n\n ## Acceptance Criteria\n\n+- [ ] A new file `kanban-app/ui/src/components/inspectable.tsx` exports a single `<Inspectable moniker: Moniker>` component. No other file in `kanban-app/ui/src/` (excluding tests) calls `useDispatchCommand(\"ui.inspect\")`.\n - [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode (existing `onDoubleClick → startRename` behavior on the inner button).\n-- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector. `mockInvoke` is never called with a `ui_inspect`-equivalent invoke for those interactions.\n-- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved.\n+- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector.\n+- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved via the entity-card now being wrapped in `<Inspectable>`.\n - [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n - [ ] Double-clicking a field row DOES open the inspector for that field.\n-- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop. TypeScript flags the prop with autocompletion.\n-- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body — verified by a unit test that asserts the `command-scope` registry sees no lookup for `ui.inspect` from a chrome wrapper.\n-- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: every UI-chrome wrapper omits `inspectOnDoubleClick`, every entity wrapper sets it to `true`.\n+- [ ] `<FocusScope>` and `<FocusZone>` no longer accept any prop related to inspect. Searching the codebase for `inspectOnDoubleClick` returns zero matches (this prop name was considered and rejected — the wrapper-component design replaces it).\n+- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: no `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`; every `<Inspectable>` carries an entity-prefixed moniker; every entity-monikered wrapper has an ancestor `<Inspectable>`.\n - [ ] `cd kanban-app/ui && npm test` is green.\n - [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected, but verify nothing regressed).\n\n@@ -110,13 +150,13 @@\n\n ### Browser Tests (mandatory)\n\n-Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new opt-in behavior end-to-end against real DOM, so the human user does not have to manually click around to verify.\n+Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new wrapper-based design end-to-end against real DOM, so the human user does not have to manually click around to verify.\n\n #### Test files\n\n 1. `kanban-app/ui/src/components/perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (new) — locks in the reported bug.\n-2. `kanban-app/ui/src/components/focus-scope.inspect-opt-in.spatial.test.tsx` (new) — exercises the new prop.\n-3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guard for the chrome/entity classification.\n+2. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the new component in isolation and via composition.\n+3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guards.\n\n #### Setup (for browser tests)\n\n@@ -127,41 +167,43 @@\n\n **File 1 — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx`:**\n\n-1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved — the button's own `onDoubleClick` still fires); (b) the `mockInvoke` call list contains zero entries whose first arg matches `/inspect/i` and zero dispatches that route to `ui.inspect`. The simplest assertion: spy on `useDispatchCommand` (or assert via the command-scope registry) that no `ui.inspect` dispatch was issued for `perspective_tab:*`.\n+1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved); (b) zero command dispatches occurred for `ui.inspect`. Capture by spying on the `command-scope` registry's dispatch fan-out, or by stubbing `useDispatchCommand(\"ui.inspect\")` to fail the test if invoked.\n\n 2. **Regression: dblclick on the perspective bar's wrapping zone does NOT inspect** — double-click on the `ui:perspective-bar` zone background (whitespace inside the bar, not on a tab). Assert no `ui.inspect` dispatch.\n\n 3. **Single-click still focuses** — single-click on a tab still calls `mockInvoke(\"spatial_focus\", { key })`. Confirms we did not regress the focus path.\n\n-**File 2 — `focus-scope.inspect-opt-in.spatial.test.tsx`:**\n+**File 2 — `inspectable.spatial.test.tsx`:**\n\n-1. **Default (no prop) → no inspect** — render a bare `<FocusScope moniker=\"task:fake\">` (no `inspectOnDoubleClick`). Double-click. Assert no `ui.inspect` dispatch.\n+1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div>x</div></Inspectable>`. Double-click. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n\n-2. **`inspectOnDoubleClick={true}` → inspect fires** — render `<FocusScope moniker=\"task:fake\" inspectOnDoubleClick>`. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:fake\" }`.\n+2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch (input owns the gesture).\n\n-3. **`inspectOnDoubleClick={false}` explicit → no inspect** — same as default but with the prop set explicitly. Assert no dispatch (locks in the prop semantics).\n+3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div>x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n\n-4. **No `useDispatchCommand(\"ui.inspect\")` registration when opt-out** — render a `<FocusScope>` with `inspectOnDoubleClick={false}` inside a test harness that records every `useDispatchCommand` call. Assert `\"ui.inspect\"` was never registered. Reason: avoid paying the registry walk + memoization cost for every chrome focusable.\n+4. **`<FocusScope>` alone (no `<Inspectable>` ancestor) does NOT fire inspect on dblclick** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>`. Double-click. Assert no `ui.inspect` dispatch and no `useDispatchCommand(\"ui.inspect\")` registration.\n\n-5. **Symmetric for `<FocusZone>`** — repeat the four cases above against `<FocusZone>`.\n+5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n\n-6. **Entity opt-in real-world: card** — render `<EntityCard>` (or its column-view equivalent that wraps a card) for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`.\n+6. **Entity opt-in real-world: card** — render `<EntityCard>` for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the migration of entity-card to compose `<Inspectable>` around `<FocusScope>` works.\n\n-7. **Entity opt-in real-world: column** — render a `<ColumnView>` whose wrapper sets `inspectOnDoubleClick`. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n+7. **Entity opt-in real-world: column** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n\n-8. **Inner button stops propagation contract** — render `<FocusScope inspectOnDoubleClick><button onDoubleClick={(e) => e.stopPropagation()}>x</button></FocusScope>` and double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n+8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n\n #### Source-level guard — extend `focus-architecture.guards.node.test.ts`\n\n+Three new guards (Node-only, source-walk):\n+\n ```ts\n-const CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"];\n const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n ```\n\n-- Walk every `*.tsx` source file under `kanban-app/ui/src/`.\n-- For each `<FocusScope`, `<FocusZone`, or `<Focusable` element whose `moniker={asMoniker(\"<prefix>...\")}` matches a CHROME prefix, assert the same JSX element does NOT contain `inspectOnDoubleClick`. This catches \"someone added `inspectOnDoubleClick` to a nav-bar button.\"\n-- For each element whose moniker matches an ENTITY prefix, assert the element DOES contain `inspectOnDoubleClick` (or `inspectOnDoubleClick={true}`). This catches \"someone wrapped a new card type but forgot the opt-in.\" Allow a per-call-site `// inspect:exempt` comment in case there's a special-case entity not meant to be inspectable (extremely unlikely).\n-- Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n+- **Guard A — single inspect dispatch site.** Walk every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one file: `inspectable.tsx`. Any other match fails the test.\n+- **Guard B — `<Inspectable>` only wraps entity monikers.** For each `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit, assert the prefix is in `ENTITY_PREFIXES`. Catches `<Inspectable moniker={asMoniker(\"ui:foo\")}>`.\n+- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Regex-pass over the JSX: when an entity-prefixed `<FocusScope>` / `<FocusZone>` is found, the same file must contain an `<Inspectable>` element with a matching moniker substring. Catches \"someone wrote a new entity wrapper but forgot the `<Inspectable>`.\"\n+\n+Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n\n ### How to run\n\n@@ -173,4 +215,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default in `focus-scope.tsx` / `focus-zone.tsx`, then opt in at every entity call site, then add the guard. Each step keeps the test suite stable.\n+- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then create `inspectable.tsx`, then strip the inspect dispatch out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site in `<Inspectable>`, then add the guards. Each step keeps the test suite stable.\n","reverse_patch":"--- original\n+++ modified\n@@ -17,132 +17,92 @@\n `kanban-app/ui/src/components/focus-scope.tsx` (and the symmetric `focus-zone.tsx`) wires an unconditional double-click handler:\n\n ```tsx\n+// inside SpatialFocusScopeBody\n const dispatch = useDispatchCommand(\"ui.inspect\");\n …\n-const handleDoubleClick = useCallback((e) => {\n-  // …guards…\n+const handleDoubleClick = useCallback((e: React.MouseEvent) => {\n+  if (!handleEvents) return;\n+  // …input/textarea/contenteditable guards…\n   e.stopPropagation();\n   dispatch({ target: moniker }).catch(console.error);\n }, [dispatch, moniker, handleEvents]);\n+…\n+<div … onDoubleClick={handleDoubleClick}>\n ```\n\n-So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n+So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker as the target on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n\n - `perspective_tab:<id>` — the bug the user reported.\n-- `ui:navbar`, `ui:navbar.*` — nav-bar zones and buttons.\n+- `ui:navbar`, `ui:navbar.board-selector`, `ui:navbar.inspect`, `ui:navbar.search` — nav-bar zones and buttons.\n - `ui:toolbar.*` — toolbar action groups and buttons.\n - `ui:perspective-bar`, `ui:perspective`, `ui:view`, `ui:board`, `ui:grid` — container zones for layout, not entities.\n - `cell:<row>:<col>` — grid cells, addresses inside an entity, not entities themselves.\n\n-The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n-\n-### Fix shape — introduce `<Inspectable>`\n-\n-Pull the `useDispatchCommand(\"ui.inspect\")` hook and the double-click → dispatch handler **out** of `<FocusScope>` and `<FocusZone>` entirely, and put it in a new dedicated component:\n-\n-```tsx\n-// kanban-app/ui/src/components/inspectable.tsx\n-export interface InspectableProps {\n-  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n-  moniker: Moniker;\n-  children: ReactNode;\n-}\n-\n-export function Inspectable({ moniker, children }: InspectableProps) {\n-  const dispatch = useDispatchCommand(\"ui.inspect\");\n-  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n-    const target = e.target as HTMLElement;\n-    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n-    if (target.closest(\"[contenteditable]\")) return;\n-    e.stopPropagation();\n-    dispatch({ target: moniker }).catch(console.error);\n-  }, [dispatch, moniker]);\n-\n-  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n-}\n-```\n-\n-Then entity wrappers compose explicitly:\n+The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` handler that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles up to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n\n-```tsx\n-// entity-card.tsx (entity)\n-<Inspectable moniker={taskMoniker}>\n-  <FocusScope moniker={taskMoniker}>\n-    {cardBody}\n-  </FocusScope>\n-</Inspectable>\n+### The fix shape — opt-in, not opt-out\n\n-// perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n-<FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n-  {tabContents}\n-</FocusScope>\n-```\n+Inspect-on-double-click should be an **explicit opt-in** on `<FocusScope>` and `<FocusZone>` via a new prop `inspectOnDoubleClick?: boolean` defaulting to `false`. Only call sites that wrap an actual entity opt in.\n\n-Why a wrapper component, not a boolean prop on `<FocusScope>`:\n+Why opt-in (not \"skip when moniker prefix is in a UI list\" / \"skip when no `entity:*` scope claimed\"):\n\n-- **The name encodes the architectural rule.** `<Inspectable moniker=\"task:01\">` reads as \"this DOM subtree represents an inspectable entity.\" Anyone reading the JSX knows the contract without consulting prop docs. A `inspectOnDoubleClick={true}` boolean is just a flag — the *meaning* (the entity-vs-chrome distinction) is buried in conventions about which prop value goes where.\n-- **Separation of concerns.** `<FocusScope>` and `<FocusZone>` are generic spatial primitives — they should know nothing about `ui.inspect` or the kanban command system. Ripping the dispatch out makes the primitives smaller, simpler, and reusable for future spatial UIs that have nothing to do with inspectors.\n-- **Composability.** A single entity that needs both inspect-on-double-click and a focus zone (e.g. an editable card) can compose `<Inspectable><FocusZone>…</FocusZone></Inspectable>`. The boolean would have to be duplicated on both `<FocusScope>` AND `<FocusZone>`, with no obvious answer to \"what if both nest?\"\n-- **Type-system enforcement.** `<Inspectable>` requires a `moniker` whose runtime shape can be validated (e.g. via a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes). A boolean offers no such enforcement.\n-- **The chrome path costs nothing.** When a `<FocusScope>` or `<FocusZone>` has no `<Inspectable>` ancestor, no `useDispatchCommand(\"ui.inspect\")` registration happens at all — saving the registry walk on every chrome wrapper.\n+- The set of entity prefixes is owned by the kanban domain (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`). Hard-coding that list in a generic spatial primitive smears domain knowledge into UI infrastructure.\n+- A scope-based runtime check is brittle — the answer depends on whichever `CommandScopeProvider` happens to be the nearest ancestor, not on what *this* focusable represents. A perspective tab nested inside a board scope would still match `entity:board` and silently route inspect to the board.\n+- Opt-in flips the contract from \"inspect everything unless told otherwise\" (the current bug) to \"inspect only entity-bearing call sites that explicitly say so.\" That matches how the rest of the spatial primitives work (`showFocusBar`, `navOverride` — explicit, not inferred).\n\n ### Files in scope\n\n-**New file:**\n-\n-- `kanban-app/ui/src/components/inspectable.tsx` — the new wrapper component. Contains: the component, its props interface, a docstring explaining the contract (\"only entities; never UI chrome\"), and unit-tested input/textarea/contenteditable guards.\n-\n-**Primitive contract change (remove inspect logic):**\n+**Primitive contract change:**\n\n - `kanban-app/ui/src/components/focus-scope.tsx`\n-  - Delete `const dispatch = useDispatchCommand(\"ui.inspect\");` and the `handleDoubleClick` body that dispatches it. Keep `onDoubleClick` on the rendered `<div>` only if other behavior is still needed (e.g. `e.stopPropagation()` for nested-zone propagation control); otherwise remove the handler entirely.\n-  - Delete the same in `FallbackFocusScopeBody`.\n-  - Update the file-level docstring to describe the new contract: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n+  - Add `inspectOnDoubleClick?: boolean` to `FocusScopeOwnProps`. Default `false` in the destructuring.\n+  - In both `SpatialFocusScopeBody` and `FallbackFocusScopeBody`, gate the entire `dispatch({ target: moniker })` call behind that flag. When `false`, the `useDispatchCommand(\"ui.inspect\")` hook should not be called either (so the registry walk for `ui.inspect` is not paid by every UI chrome wrapper).\n+  - Update the file-level docstring section that describes the click contract.\n\n - `kanban-app/ui/src/components/focus-zone.tsx`\n-  - Symmetric removal of the inspect dispatch. Same docstring update.\n+  - Symmetric change to `<FocusZone>` — add the same prop with the same default and gating.\n\n-- `kanban-app/ui/src/components/focusable.tsx` — if it still exists as a transitional re-export shim per `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, no change beyond confirming the inspect logic is gone.\n+- `kanban-app/ui/src/components/focusable.tsx`\n+  - If `<Focusable>` still exists as a transitional re-export shim of `<FocusScope>` per the in-flight architecture-fix task `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, propagate the prop through. If `<Focusable>` already collapsed into `<FocusScope>`, this file is gone — skip.\n\n-**Entity call sites that must wrap in `<Inspectable>`:**\n+**Entity call sites that must opt IN (`inspectOnDoubleClick`):**\n\n-- `kanban-app/ui/src/components/entity-card.tsx` — `task:<id>`, `tag:<id>`.\n+- `kanban-app/ui/src/components/entity-card.tsx` — task / tag cards (`task:<id>`, `tag:<id>`).\n - `kanban-app/ui/src/components/column-view.tsx` — column zone (`column:<id>`).\n - `kanban-app/ui/src/components/board-view.tsx` — only the board entity wrapper, NOT the `ui:board` chrome zone.\n - `kanban-app/ui/src/components/fields/field.tsx` — field rows (`field:<entity>:<name>`).\n-- Any other call site whose moniker is a real entity — search via `git grep \"moniker={asMoniker(\\\"[a-z]*:\"` and classify each hit.\n-\n-**UI-chrome call sites (no `<Inspectable>` — verify):**\n-\n-- `nav-bar.tsx`, `toolbar.tsx`, `perspective-tab-bar.tsx`, `perspective-container.tsx`, `view-container.tsx`, `grid-view.tsx` cells, the `ui:board` chrome zone.\n+- Any other call site whose moniker is a real entity (search via `git grep` for `moniker={asMoniker(\"` and classify each hit).\n\n-**Architectural guard:**\n+**UI-chrome call sites that must stay opt-OUT (explicitly verify, do not change):**\n\n-- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a node-side source-walk guard:\n-  - **No `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`.** Greps the entire `kanban-app/ui/src/` tree (excluding test files and `inspectable.tsx` itself) for the literal string `useDispatchCommand(\"ui.inspect\")` and asserts zero matches. This catches any future regression where someone re-introduces the dispatch into a primitive.\n-  - **No CHROME-prefixed moniker is wrapped in `<Inspectable>`.** Walks `*.tsx`, finds every `<Inspectable …>` JSX element, parses its `moniker` prop value, and asserts the prefix is in an `ENTITY_PREFIXES` allowlist (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n-  - **Every `task:<id>` / `column:<id>` / `field:` JSX wrapper has an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new card type but forgot to wrap it.\" Use a simple regex pass that fails the test when an entity-monikered `<FocusScope>` / `<FocusZone>` does not have a sibling/ancestor `<Inspectable>` carrying the same moniker.\n+- `nav-bar.tsx` — all `ui:navbar.*`.\n+- All toolbar wrappers — `ui:toolbar.*`.\n+- `perspective-tab-bar.tsx` — `ui:perspective-bar` and `perspective_tab:<id>`. The button's own `onDoubleClick` continues to call `startRename`.\n+- `perspective-container.tsx` — `ui:perspective`.\n+- `view-container.tsx` — `ui:view`.\n+- `grid-view.tsx` — `ui:grid` and `cell:<r>:<c>`.\n+- The board chrome `ui:board` zone (the *container*, not the board entity itself).\n\n-### Optional, recommended: brand the moniker\n+**Architectural guard to extend:**\n\n-Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that throws / `never`s when `s` doesn't start with an entity prefix, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If the codebase already brands monikers (`Moniker` is itself branded per `@/types/spatial`), extend that branding with `EntityMoniker` as a sub-brand. If extending is more than a small change, file a follow-up task — the runtime guards in `focus-architecture.guards.node.test.ts` are sufficient for this task.\n+- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a guard that greps `kanban-app/ui/src/` for every `moniker={asMoniker(\"ui:` or `moniker={asMoniker(\\`perspective_tab:` etc. occurrence and asserts none of those call sites carry `inspectOnDoubleClick={true}`. Conversely, add a positive guard that every `entity-card`, `column-view`, `field` call site DOES carry the prop. The list of UI-chrome prefixes lives in the test as a `BANNED_INSPECT_PREFIXES` constant alongside the existing decoration guards.\n\n ### What this task does NOT do\n\n-- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just no longer triggers inspect.\n+- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just won't trigger inspect.\n - Does not touch the YAML scope `entity:perspective` (that scope serves the perspective.* command palette + context menu and is unrelated to the inspector).\n - Does not change the right-click context menu — context-menu commands are scoped per call site already; this task is only about double-click → inspect.\n\n ## Acceptance Criteria\n\n-- [ ] A new file `kanban-app/ui/src/components/inspectable.tsx` exports a single `<Inspectable moniker: Moniker>` component. No other file in `kanban-app/ui/src/` (excluding tests) calls `useDispatchCommand(\"ui.inspect\")`.\n - [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode (existing `onDoubleClick → startRename` behavior on the inner button).\n-- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector.\n-- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved via the entity-card now being wrapped in `<Inspectable>`.\n+- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector. `mockInvoke` is never called with a `ui_inspect`-equivalent invoke for those interactions.\n+- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved.\n - [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n - [ ] Double-clicking a field row DOES open the inspector for that field.\n-- [ ] `<FocusScope>` and `<FocusZone>` no longer accept any prop related to inspect. Searching the codebase for `inspectOnDoubleClick` returns zero matches (this prop name was considered and rejected — the wrapper-component design replaces it).\n-- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: no `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`; every `<Inspectable>` carries an entity-prefixed moniker; every entity-monikered wrapper has an ancestor `<Inspectable>`.\n+- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop. TypeScript flags the prop with autocompletion.\n+- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body — verified by a unit test that asserts the `command-scope` registry sees no lookup for `ui.inspect` from a chrome wrapper.\n+- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: every UI-chrome wrapper omits `inspectOnDoubleClick`, every entity wrapper sets it to `true`.\n - [ ] `cd kanban-app/ui && npm test` is green.\n - [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected, but verify nothing regressed).\n\n@@ -150,13 +110,13 @@\n\n ### Browser Tests (mandatory)\n\n-Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new wrapper-based design end-to-end against real DOM, so the human user does not have to manually click around to verify.\n+Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new opt-in behavior end-to-end against real DOM, so the human user does not have to manually click around to verify.\n\n #### Test files\n\n 1. `kanban-app/ui/src/components/perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (new) — locks in the reported bug.\n-2. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the new component in isolation and via composition.\n-3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guards.\n+2. `kanban-app/ui/src/components/focus-scope.inspect-opt-in.spatial.test.tsx` (new) — exercises the new prop.\n+3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guard for the chrome/entity classification.\n\n #### Setup (for browser tests)\n\n@@ -167,43 +127,41 @@\n\n **File 1 — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx`:**\n\n-1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved); (b) zero command dispatches occurred for `ui.inspect`. Capture by spying on the `command-scope` registry's dispatch fan-out, or by stubbing `useDispatchCommand(\"ui.inspect\")` to fail the test if invoked.\n+1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved — the button's own `onDoubleClick` still fires); (b) the `mockInvoke` call list contains zero entries whose first arg matches `/inspect/i` and zero dispatches that route to `ui.inspect`. The simplest assertion: spy on `useDispatchCommand` (or assert via the command-scope registry) that no `ui.inspect` dispatch was issued for `perspective_tab:*`.\n\n 2. **Regression: dblclick on the perspective bar's wrapping zone does NOT inspect** — double-click on the `ui:perspective-bar` zone background (whitespace inside the bar, not on a tab). Assert no `ui.inspect` dispatch.\n\n 3. **Single-click still focuses** — single-click on a tab still calls `mockInvoke(\"spatial_focus\", { key })`. Confirms we did not regress the focus path.\n\n-**File 2 — `inspectable.spatial.test.tsx`:**\n+**File 2 — `focus-scope.inspect-opt-in.spatial.test.tsx`:**\n\n-1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div>x</div></Inspectable>`. Double-click. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n+1. **Default (no prop) → no inspect** — render a bare `<FocusScope moniker=\"task:fake\">` (no `inspectOnDoubleClick`). Double-click. Assert no `ui.inspect` dispatch.\n\n-2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch (input owns the gesture).\n+2. **`inspectOnDoubleClick={true}` → inspect fires** — render `<FocusScope moniker=\"task:fake\" inspectOnDoubleClick>`. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:fake\" }`.\n\n-3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div>x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n+3. **`inspectOnDoubleClick={false}` explicit → no inspect** — same as default but with the prop set explicitly. Assert no dispatch (locks in the prop semantics).\n\n-4. **`<FocusScope>` alone (no `<Inspectable>` ancestor) does NOT fire inspect on dblclick** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>`. Double-click. Assert no `ui.inspect` dispatch and no `useDispatchCommand(\"ui.inspect\")` registration.\n+4. **No `useDispatchCommand(\"ui.inspect\")` registration when opt-out** — render a `<FocusScope>` with `inspectOnDoubleClick={false}` inside a test harness that records every `useDispatchCommand` call. Assert `\"ui.inspect\"` was never registered. Reason: avoid paying the registry walk + memoization cost for every chrome focusable.\n\n-5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n+5. **Symmetric for `<FocusZone>`** — repeat the four cases above against `<FocusZone>`.\n\n-6. **Entity opt-in real-world: card** — render `<EntityCard>` for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the migration of entity-card to compose `<Inspectable>` around `<FocusScope>` works.\n+6. **Entity opt-in real-world: card** — render `<EntityCard>` (or its column-view equivalent that wraps a card) for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`.\n\n-7. **Entity opt-in real-world: column** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n+7. **Entity opt-in real-world: column** — render a `<ColumnView>` whose wrapper sets `inspectOnDoubleClick`. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n\n-8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n+8. **Inner button stops propagation contract** — render `<FocusScope inspectOnDoubleClick><button onDoubleClick={(e) => e.stopPropagation()}>x</button></FocusScope>` and double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n\n #### Source-level guard — extend `focus-architecture.guards.node.test.ts`\n\n-Three new guards (Node-only, source-walk):\n-\n ```ts\n+const CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"];\n const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n ```\n\n-- **Guard A — single inspect dispatch site.** Walk every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one file: `inspectable.tsx`. Any other match fails the test.\n-- **Guard B — `<Inspectable>` only wraps entity monikers.** For each `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit, assert the prefix is in `ENTITY_PREFIXES`. Catches `<Inspectable moniker={asMoniker(\"ui:foo\")}>`.\n-- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Regex-pass over the JSX: when an entity-prefixed `<FocusScope>` / `<FocusZone>` is found, the same file must contain an `<Inspectable>` element with a matching moniker substring. Catches \"someone wrote a new entity wrapper but forgot the `<Inspectable>`.\"\n-\n-Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n+- Walk every `*.tsx` source file under `kanban-app/ui/src/`.\n+- For each `<FocusScope`, `<FocusZone`, or `<Focusable` element whose `moniker={asMoniker(\"<prefix>...\")}` matches a CHROME prefix, assert the same JSX element does NOT contain `inspectOnDoubleClick`. This catches \"someone added `inspectOnDoubleClick` to a nav-bar button.\"\n+- For each element whose moniker matches an ENTITY prefix, assert the element DOES contain `inspectOnDoubleClick` (or `inspectOnDoubleClick={true}`). This catches \"someone wrapped a new card type but forgot the opt-in.\" Allow a per-call-site `// inspect:exempt` comment in case there's a special-case entity not meant to be inspectable (extremely unlikely).\n+- Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n\n ### How to run\n\n@@ -215,4 +173,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then create `inspectable.tsx`, then strip the inspect dispatch out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site in `<Inspectable>`, then add the guards. Each step keeps the test suite stable.\n+- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default in `focus-scope.tsx` / `focus-zone.tsx`, then opt in at every entity call site, then add the guard. Each step keeps the test suite stable.\n","transaction_id":null}
{"id":"01KQ7K1BDC1B8DW6Q4PQTC5BRK","timestamp":"2026-04-27T13:46:07.148930Z","entity_type":"task","entity_id":"01KQ7GM77B1E6YH8Z893K05VKY","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -9,92 +9,132 @@\n `kanban-app/ui/src/components/focus-scope.tsx` (and the symmetric `focus-zone.tsx`) wires an unconditional double-click handler:\n\n ```tsx\n-// inside SpatialFocusScopeBody\n const dispatch = useDispatchCommand(\"ui.inspect\");\n …\n-const handleDoubleClick = useCallback((e: React.MouseEvent) => {\n-  if (!handleEvents) return;\n-  // …input/textarea/contenteditable guards…\n+const handleDoubleClick = useCallback((e) => {\n+  // …guards…\n   e.stopPropagation();\n   dispatch({ target: moniker }).catch(console.error);\n }, [dispatch, moniker, handleEvents]);\n-…\n-<div … onDoubleClick={handleDoubleClick}>\n ```\n\n-So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker as the target on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n+So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n\n - `perspective_tab:<id>` — the bug the user reported.\n-- `ui:navbar`, `ui:navbar.board-selector`, `ui:navbar.inspect`, `ui:navbar.search` — nav-bar zones and buttons.\n+- `ui:navbar`, `ui:navbar.*` — nav-bar zones and buttons.\n - `ui:toolbar.*` — toolbar action groups and buttons.\n - `ui:perspective-bar`, `ui:perspective`, `ui:view`, `ui:board`, `ui:grid` — container zones for layout, not entities.\n - `cell:<row>:<col>` — grid cells, addresses inside an entity, not entities themselves.\n\n-The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` handler that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles up to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n+The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n\n-### The fix shape — opt-in, not opt-out\n+### Fix shape — introduce `<Inspectable>`\n\n-Inspect-on-double-click should be an **explicit opt-in** on `<FocusScope>` and `<FocusZone>` via a new prop `inspectOnDoubleClick?: boolean` defaulting to `false`. Only call sites that wrap an actual entity opt in.\n+Pull the `useDispatchCommand(\"ui.inspect\")` hook and the double-click → dispatch handler **out** of `<FocusScope>` and `<FocusZone>` entirely, and put it in a new dedicated component:\n\n-Why opt-in (not \"skip when moniker prefix is in a UI list\" / \"skip when no `entity:*` scope claimed\"):\n+```tsx\n+// kanban-app/ui/src/components/inspectable.tsx\n+export interface InspectableProps {\n+  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n+  moniker: Moniker;\n+  children: ReactNode;\n+}\n\n-- The set of entity prefixes is owned by the kanban domain (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`). Hard-coding that list in a generic spatial primitive smears domain knowledge into UI infrastructure.\n-- A scope-based runtime check is brittle — the answer depends on whichever `CommandScopeProvider` happens to be the nearest ancestor, not on what *this* focusable represents. A perspective tab nested inside a board scope would still match `entity:board` and silently route inspect to the board.\n-- Opt-in flips the contract from \"inspect everything unless told otherwise\" (the current bug) to \"inspect only entity-bearing call sites that explicitly say so.\" That matches how the rest of the spatial primitives work (`showFocusBar`, `navOverride` — explicit, not inferred).\n+export function Inspectable({ moniker, children }: InspectableProps) {\n+  const dispatch = useDispatchCommand(\"ui.inspect\");\n+  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n+    const target = e.target as HTMLElement;\n+    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n+    if (target.closest(\"[contenteditable]\")) return;\n+    e.stopPropagation();\n+    dispatch({ target: moniker }).catch(console.error);\n+  }, [dispatch, moniker]);\n\n+  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n+}\n+```\n+\n+Then entity wrappers compose explicitly:\n+\n+```tsx\n+// entity-card.tsx (entity)\n+<Inspectable moniker={taskMoniker}>\n+  <FocusScope moniker={taskMoniker}>\n+    {cardBody}\n+  </FocusScope>\n+</Inspectable>\n+\n+// perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n+<FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n+  {tabContents}\n+</FocusScope>\n+```\n+\n+Why a wrapper component, not a boolean prop on `<FocusScope>`:\n+\n+- **The name encodes the architectural rule.** `<Inspectable moniker=\"task:01\">` reads as \"this DOM subtree represents an inspectable entity.\" Anyone reading the JSX knows the contract without consulting prop docs. A `inspectOnDoubleClick={true}` boolean is just a flag — the *meaning* (the entity-vs-chrome distinction) is buried in conventions about which prop value goes where.\n+- **Separation of concerns.** `<FocusScope>` and `<FocusZone>` are generic spatial primitives — they should know nothing about `ui.inspect` or the kanban command system. Ripping the dispatch out makes the primitives smaller, simpler, and reusable for future spatial UIs that have nothing to do with inspectors.\n+- **Composability.** A single entity that needs both inspect-on-double-click and a focus zone (e.g. an editable card) can compose `<Inspectable><FocusZone>…</FocusZone></Inspectable>`. The boolean would have to be duplicated on both `<FocusScope>` AND `<FocusZone>`, with no obvious answer to \"what if both nest?\"\n+- **Type-system enforcement.** `<Inspectable>` requires a `moniker` whose runtime shape can be validated (e.g. via a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes). A boolean offers no such enforcement.\n+- **The chrome path costs nothing.** When a `<FocusScope>` or `<FocusZone>` has no `<Inspectable>` ancestor, no `useDispatchCommand(\"ui.inspect\")` registration happens at all — saving the registry walk on every chrome wrapper.\n+\n ### Files in scope\n\n-**Primitive contract change:**\n+**New file:**\n+\n+- `kanban-app/ui/src/components/inspectable.tsx` — the new wrapper component. Contains: the component, its props interface, a docstring explaining the contract (\"only entities; never UI chrome\"), and unit-tested input/textarea/contenteditable guards.\n+\n+**Primitive contract change (remove inspect logic):**\n\n - `kanban-app/ui/src/components/focus-scope.tsx`\n-  - Add `inspectOnDoubleClick?: boolean` to `FocusScopeOwnProps`. Default `false` in the destructuring.\n-  - In both `SpatialFocusScopeBody` and `FallbackFocusScopeBody`, gate the entire `dispatch({ target: moniker })` call behind that flag. When `false`, the `useDispatchCommand(\"ui.inspect\")` hook should not be called either (so the registry walk for `ui.inspect` is not paid by every UI chrome wrapper).\n-  - Update the file-level docstring section that describes the click contract.\n+  - Delete `const dispatch = useDispatchCommand(\"ui.inspect\");` and the `handleDoubleClick` body that dispatches it. Keep `onDoubleClick` on the rendered `<div>` only if other behavior is still needed (e.g. `e.stopPropagation()` for nested-zone propagation control); otherwise remove the handler entirely.\n+  - Delete the same in `FallbackFocusScopeBody`.\n+  - Update the file-level docstring to describe the new contract: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n\n - `kanban-app/ui/src/components/focus-zone.tsx`\n-  - Symmetric change to `<FocusZone>` — add the same prop with the same default and gating.\n+  - Symmetric removal of the inspect dispatch. Same docstring update.\n\n-- `kanban-app/ui/src/components/focusable.tsx`\n-  - If `<Focusable>` still exists as a transitional re-export shim of `<FocusScope>` per the in-flight architecture-fix task `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, propagate the prop through. If `<Focusable>` already collapsed into `<FocusScope>`, this file is gone — skip.\n+- `kanban-app/ui/src/components/focusable.tsx` — if it still exists as a transitional re-export shim per `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, no change beyond confirming the inspect logic is gone.\n\n-**Entity call sites that must opt IN (`inspectOnDoubleClick`):**\n+**Entity call sites that must wrap in `<Inspectable>`:**\n\n-- `kanban-app/ui/src/components/entity-card.tsx` — task / tag cards (`task:<id>`, `tag:<id>`).\n+- `kanban-app/ui/src/components/entity-card.tsx` — `task:<id>`, `tag:<id>`.\n - `kanban-app/ui/src/components/column-view.tsx` — column zone (`column:<id>`).\n - `kanban-app/ui/src/components/board-view.tsx` — only the board entity wrapper, NOT the `ui:board` chrome zone.\n - `kanban-app/ui/src/components/fields/field.tsx` — field rows (`field:<entity>:<name>`).\n-- Any other call site whose moniker is a real entity (search via `git grep` for `moniker={asMoniker(\"` and classify each hit).\n+- Any other call site whose moniker is a real entity — search via `git grep \"moniker={asMoniker(\\\"[a-z]*:\"` and classify each hit.\n\n-**UI-chrome call sites that must stay opt-OUT (explicitly verify, do not change):**\n+**UI-chrome call sites (no `<Inspectable>` — verify):**\n\n-- `nav-bar.tsx` — all `ui:navbar.*`.\n-- All toolbar wrappers — `ui:toolbar.*`.\n-- `perspective-tab-bar.tsx` — `ui:perspective-bar` and `perspective_tab:<id>`. The button's own `onDoubleClick` continues to call `startRename`.\n-- `perspective-container.tsx` — `ui:perspective`.\n-- `view-container.tsx` — `ui:view`.\n-- `grid-view.tsx` — `ui:grid` and `cell:<r>:<c>`.\n-- The board chrome `ui:board` zone (the *container*, not the board entity itself).\n+- `nav-bar.tsx`, `toolbar.tsx`, `perspective-tab-bar.tsx`, `perspective-container.tsx`, `view-container.tsx`, `grid-view.tsx` cells, the `ui:board` chrome zone.\n\n-**Architectural guard to extend:**\n+**Architectural guard:**\n+\n+- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a node-side source-walk guard:\n+  - **No `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`.** Greps the entire `kanban-app/ui/src/` tree (excluding test files and `inspectable.tsx` itself) for the literal string `useDispatchCommand(\"ui.inspect\")` and asserts zero matches. This catches any future regression where someone re-introduces the dispatch into a primitive.\n+  - **No CHROME-prefixed moniker is wrapped in `<Inspectable>`.** Walks `*.tsx`, finds every `<Inspectable …>` JSX element, parses its `moniker` prop value, and asserts the prefix is in an `ENTITY_PREFIXES` allowlist (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n+  - **Every `task:<id>` / `column:<id>` / `field:` JSX wrapper has an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new card type but forgot to wrap it.\" Use a simple regex pass that fails the test when an entity-monikered `<FocusScope>` / `<FocusZone>` does not have a sibling/ancestor `<Inspectable>` carrying the same moniker.\n\n-- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a guard that greps `kanban-app/ui/src/` for every `moniker={asMoniker(\"ui:` or `moniker={asMoniker(\\`perspective_tab:` etc. occurrence and asserts none of those call sites carry `inspectOnDoubleClick={true}`. Conversely, add a positive guard that every `entity-card`, `column-view`, `field` call site DOES carry the prop. The list of UI-chrome prefixes lives in the test as a `BANNED_INSPECT_PREFIXES` constant alongside the existing decoration guards.\n+### Optional, recommended: brand the moniker\n+\n+Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that throws / `never`s when `s` doesn't start with an entity prefix, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If the codebase already brands monikers (`Moniker` is itself branded per `@/types/spatial`), extend that branding with `EntityMoniker` as a sub-brand. If extending is more than a small change, file a follow-up task — the runtime guards in `focus-architecture.guards.node.test.ts` are sufficient for this task.\n\n ### What this task does NOT do\n\n-- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just won't trigger inspect.\n+- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just no longer triggers inspect.\n - Does not touch the YAML scope `entity:perspective` (that scope serves the perspective.* command palette + context menu and is unrelated to the inspector).\n - Does not change the right-click context menu — context-menu commands are scoped per call site already; this task is only about double-click → inspect.\n\n ## Acceptance Criteria\n\n+- [ ] A new file `kanban-app/ui/src/components/inspectable.tsx` exports a single `<Inspectable moniker: Moniker>` component. No other file in `kanban-app/ui/src/` (excluding tests) calls `useDispatchCommand(\"ui.inspect\")`.\n - [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode (existing `onDoubleClick → startRename` behavior on the inner button).\n-- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector. `mockInvoke` is never called with a `ui_inspect`-equivalent invoke for those interactions.\n-- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved.\n+- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector.\n+- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved via the entity-card now being wrapped in `<Inspectable>`.\n - [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n - [ ] Double-clicking a field row DOES open the inspector for that field.\n-- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop. TypeScript flags the prop with autocompletion.\n-- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body — verified by a unit test that asserts the `command-scope` registry sees no lookup for `ui.inspect` from a chrome wrapper.\n-- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: every UI-chrome wrapper omits `inspectOnDoubleClick`, every entity wrapper sets it to `true`.\n+- [ ] `<FocusScope>` and `<FocusZone>` no longer accept any prop related to inspect. Searching the codebase for `inspectOnDoubleClick` returns zero matches (this prop name was considered and rejected — the wrapper-component design replaces it).\n+- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: no `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`; every `<Inspectable>` carries an entity-prefixed moniker; every entity-monikered wrapper has an ancestor `<Inspectable>`.\n - [ ] `cd kanban-app/ui && npm test` is green.\n - [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected, but verify nothing regressed).\n\n@@ -102,13 +142,13 @@\n\n ### Browser Tests (mandatory)\n\n-Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new opt-in behavior end-to-end against real DOM, so the human user does not have to manually click around to verify.\n+Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new wrapper-based design end-to-end against real DOM, so the human user does not have to manually click around to verify.\n\n #### Test files\n\n 1. `kanban-app/ui/src/components/perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (new) — locks in the reported bug.\n-2. `kanban-app/ui/src/components/focus-scope.inspect-opt-in.spatial.test.tsx` (new) — exercises the new prop.\n-3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guard for the chrome/entity classification.\n+2. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the new component in isolation and via composition.\n+3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guards.\n\n #### Setup (for browser tests)\n\n@@ -119,41 +159,43 @@\n\n **File 1 — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx`:**\n\n-1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved — the button's own `onDoubleClick` still fires); (b) the `mockInvoke` call list contains zero entries whose first arg matches `/inspect/i` and zero dispatches that route to `ui.inspect`. The simplest assertion: spy on `useDispatchCommand` (or assert via the command-scope registry) that no `ui.inspect` dispatch was issued for `perspective_tab:*`.\n+1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved); (b) zero command dispatches occurred for `ui.inspect`. Capture by spying on the `command-scope` registry's dispatch fan-out, or by stubbing `useDispatchCommand(\"ui.inspect\")` to fail the test if invoked.\n\n 2. **Regression: dblclick on the perspective bar's wrapping zone does NOT inspect** — double-click on the `ui:perspective-bar` zone background (whitespace inside the bar, not on a tab). Assert no `ui.inspect` dispatch.\n\n 3. **Single-click still focuses** — single-click on a tab still calls `mockInvoke(\"spatial_focus\", { key })`. Confirms we did not regress the focus path.\n\n-**File 2 — `focus-scope.inspect-opt-in.spatial.test.tsx`:**\n+**File 2 — `inspectable.spatial.test.tsx`:**\n\n-1. **Default (no prop) → no inspect** — render a bare `<FocusScope moniker=\"task:fake\">` (no `inspectOnDoubleClick`). Double-click. Assert no `ui.inspect` dispatch.\n+1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div>x</div></Inspectable>`. Double-click. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n\n-2. **`inspectOnDoubleClick={true}` → inspect fires** — render `<FocusScope moniker=\"task:fake\" inspectOnDoubleClick>`. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:fake\" }`.\n+2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch (input owns the gesture).\n\n-3. **`inspectOnDoubleClick={false}` explicit → no inspect** — same as default but with the prop set explicitly. Assert no dispatch (locks in the prop semantics).\n+3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div>x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n\n-4. **No `useDispatchCommand(\"ui.inspect\")` registration when opt-out** — render a `<FocusScope>` with `inspectOnDoubleClick={false}` inside a test harness that records every `useDispatchCommand` call. Assert `\"ui.inspect\"` was never registered. Reason: avoid paying the registry walk + memoization cost for every chrome focusable.\n+4. **`<FocusScope>` alone (no `<Inspectable>` ancestor) does NOT fire inspect on dblclick** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>`. Double-click. Assert no `ui.inspect` dispatch and no `useDispatchCommand(\"ui.inspect\")` registration.\n\n-5. **Symmetric for `<FocusZone>`** — repeat the four cases above against `<FocusZone>`.\n+5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n\n-6. **Entity opt-in real-world: card** — render `<EntityCard>` (or its column-view equivalent that wraps a card) for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`.\n+6. **Entity opt-in real-world: card** — render `<EntityCard>` for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the migration of entity-card to compose `<Inspectable>` around `<FocusScope>` works.\n\n-7. **Entity opt-in real-world: column** — render a `<ColumnView>` whose wrapper sets `inspectOnDoubleClick`. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n+7. **Entity opt-in real-world: column** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n\n-8. **Inner button stops propagation contract** — render `<FocusScope inspectOnDoubleClick><button onDoubleClick={(e) => e.stopPropagation()}>x</button></FocusScope>` and double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n+8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n\n #### Source-level guard — extend `focus-architecture.guards.node.test.ts`\n\n+Three new guards (Node-only, source-walk):\n+\n ```ts\n-const CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"];\n const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n ```\n\n-- Walk every `*.tsx` source file under `kanban-app/ui/src/`.\n-- For each `<FocusScope`, `<FocusZone`, or `<Focusable` element whose `moniker={asMoniker(\"<prefix>...\")}` matches a CHROME prefix, assert the same JSX element does NOT contain `inspectOnDoubleClick`. This catches \"someone added `inspectOnDoubleClick` to a nav-bar button.\"\n-- For each element whose moniker matches an ENTITY prefix, assert the element DOES contain `inspectOnDoubleClick` (or `inspectOnDoubleClick={true}`). This catches \"someone wrapped a new card type but forgot the opt-in.\" Allow a per-call-site `// inspect:exempt` comment in case there's a special-case entity not meant to be inspectable (extremely unlikely).\n-- Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n+- **Guard A — single inspect dispatch site.** Walk every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one file: `inspectable.tsx`. Any other match fails the test.\n+- **Guard B — `<Inspectable>` only wraps entity monikers.** For each `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit, assert the prefix is in `ENTITY_PREFIXES`. Catches `<Inspectable moniker={asMoniker(\"ui:foo\")}>`.\n+- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Regex-pass over the JSX: when an entity-prefixed `<FocusScope>` / `<FocusZone>` is found, the same file must contain an `<Inspectable>` element with a matching moniker substring. Catches \"someone wrote a new entity wrapper but forgot the `<Inspectable>`.\"\n+\n+Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n\n ### How to run\n\n@@ -165,4 +207,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default in `focus-scope.tsx` / `focus-zone.tsx`, then opt in at every entity call site, then add the guard. Each step keeps the test suite stable.\n+- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then create `inspectable.tsx`, then strip the inspect dispatch out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site in `<Inspectable>`, then add the guards. Each step keeps the test suite stable.\n","reverse_patch":"--- original\n+++ modified\n@@ -9,132 +9,92 @@\n `kanban-app/ui/src/components/focus-scope.tsx` (and the symmetric `focus-zone.tsx`) wires an unconditional double-click handler:\n\n ```tsx\n+// inside SpatialFocusScopeBody\n const dispatch = useDispatchCommand(\"ui.inspect\");\n …\n-const handleDoubleClick = useCallback((e) => {\n-  // …guards…\n+const handleDoubleClick = useCallback((e: React.MouseEvent) => {\n+  if (!handleEvents) return;\n+  // …input/textarea/contenteditable guards…\n   e.stopPropagation();\n   dispatch({ target: moniker }).catch(console.error);\n }, [dispatch, moniker, handleEvents]);\n+…\n+<div … onDoubleClick={handleDoubleClick}>\n ```\n\n-So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n+So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker as the target on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n\n - `perspective_tab:<id>` — the bug the user reported.\n-- `ui:navbar`, `ui:navbar.*` — nav-bar zones and buttons.\n+- `ui:navbar`, `ui:navbar.board-selector`, `ui:navbar.inspect`, `ui:navbar.search` — nav-bar zones and buttons.\n - `ui:toolbar.*` — toolbar action groups and buttons.\n - `ui:perspective-bar`, `ui:perspective`, `ui:view`, `ui:board`, `ui:grid` — container zones for layout, not entities.\n - `cell:<row>:<col>` — grid cells, addresses inside an entity, not entities themselves.\n\n-The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n-\n-### Fix shape — introduce `<Inspectable>`\n-\n-Pull the `useDispatchCommand(\"ui.inspect\")` hook and the double-click → dispatch handler **out** of `<FocusScope>` and `<FocusZone>` entirely, and put it in a new dedicated component:\n-\n-```tsx\n-// kanban-app/ui/src/components/inspectable.tsx\n-export interface InspectableProps {\n-  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n-  moniker: Moniker;\n-  children: ReactNode;\n-}\n-\n-export function Inspectable({ moniker, children }: InspectableProps) {\n-  const dispatch = useDispatchCommand(\"ui.inspect\");\n-  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n-    const target = e.target as HTMLElement;\n-    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n-    if (target.closest(\"[contenteditable]\")) return;\n-    e.stopPropagation();\n-    dispatch({ target: moniker }).catch(console.error);\n-  }, [dispatch, moniker]);\n-\n-  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n-}\n-```\n-\n-Then entity wrappers compose explicitly:\n+The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` handler that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles up to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n\n-```tsx\n-// entity-card.tsx (entity)\n-<Inspectable moniker={taskMoniker}>\n-  <FocusScope moniker={taskMoniker}>\n-    {cardBody}\n-  </FocusScope>\n-</Inspectable>\n+### The fix shape — opt-in, not opt-out\n\n-// perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n-<FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n-  {tabContents}\n-</FocusScope>\n-```\n+Inspect-on-double-click should be an **explicit opt-in** on `<FocusScope>` and `<FocusZone>` via a new prop `inspectOnDoubleClick?: boolean` defaulting to `false`. Only call sites that wrap an actual entity opt in.\n\n-Why a wrapper component, not a boolean prop on `<FocusScope>`:\n+Why opt-in (not \"skip when moniker prefix is in a UI list\" / \"skip when no `entity:*` scope claimed\"):\n\n-- **The name encodes the architectural rule.** `<Inspectable moniker=\"task:01\">` reads as \"this DOM subtree represents an inspectable entity.\" Anyone reading the JSX knows the contract without consulting prop docs. A `inspectOnDoubleClick={true}` boolean is just a flag — the *meaning* (the entity-vs-chrome distinction) is buried in conventions about which prop value goes where.\n-- **Separation of concerns.** `<FocusScope>` and `<FocusZone>` are generic spatial primitives — they should know nothing about `ui.inspect` or the kanban command system. Ripping the dispatch out makes the primitives smaller, simpler, and reusable for future spatial UIs that have nothing to do with inspectors.\n-- **Composability.** A single entity that needs both inspect-on-double-click and a focus zone (e.g. an editable card) can compose `<Inspectable><FocusZone>…</FocusZone></Inspectable>`. The boolean would have to be duplicated on both `<FocusScope>` AND `<FocusZone>`, with no obvious answer to \"what if both nest?\"\n-- **Type-system enforcement.** `<Inspectable>` requires a `moniker` whose runtime shape can be validated (e.g. via a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes). A boolean offers no such enforcement.\n-- **The chrome path costs nothing.** When a `<FocusScope>` or `<FocusZone>` has no `<Inspectable>` ancestor, no `useDispatchCommand(\"ui.inspect\")` registration happens at all — saving the registry walk on every chrome wrapper.\n+- The set of entity prefixes is owned by the kanban domain (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`). Hard-coding that list in a generic spatial primitive smears domain knowledge into UI infrastructure.\n+- A scope-based runtime check is brittle — the answer depends on whichever `CommandScopeProvider` happens to be the nearest ancestor, not on what *this* focusable represents. A perspective tab nested inside a board scope would still match `entity:board` and silently route inspect to the board.\n+- Opt-in flips the contract from \"inspect everything unless told otherwise\" (the current bug) to \"inspect only entity-bearing call sites that explicitly say so.\" That matches how the rest of the spatial primitives work (`showFocusBar`, `navOverride` — explicit, not inferred).\n\n ### Files in scope\n\n-**New file:**\n-\n-- `kanban-app/ui/src/components/inspectable.tsx` — the new wrapper component. Contains: the component, its props interface, a docstring explaining the contract (\"only entities; never UI chrome\"), and unit-tested input/textarea/contenteditable guards.\n-\n-**Primitive contract change (remove inspect logic):**\n+**Primitive contract change:**\n\n - `kanban-app/ui/src/components/focus-scope.tsx`\n-  - Delete `const dispatch = useDispatchCommand(\"ui.inspect\");` and the `handleDoubleClick` body that dispatches it. Keep `onDoubleClick` on the rendered `<div>` only if other behavior is still needed (e.g. `e.stopPropagation()` for nested-zone propagation control); otherwise remove the handler entirely.\n-  - Delete the same in `FallbackFocusScopeBody`.\n-  - Update the file-level docstring to describe the new contract: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n+  - Add `inspectOnDoubleClick?: boolean` to `FocusScopeOwnProps`. Default `false` in the destructuring.\n+  - In both `SpatialFocusScopeBody` and `FallbackFocusScopeBody`, gate the entire `dispatch({ target: moniker })` call behind that flag. When `false`, the `useDispatchCommand(\"ui.inspect\")` hook should not be called either (so the registry walk for `ui.inspect` is not paid by every UI chrome wrapper).\n+  - Update the file-level docstring section that describes the click contract.\n\n - `kanban-app/ui/src/components/focus-zone.tsx`\n-  - Symmetric removal of the inspect dispatch. Same docstring update.\n+  - Symmetric change to `<FocusZone>` — add the same prop with the same default and gating.\n\n-- `kanban-app/ui/src/components/focusable.tsx` — if it still exists as a transitional re-export shim per `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, no change beyond confirming the inspect logic is gone.\n+- `kanban-app/ui/src/components/focusable.tsx`\n+  - If `<Focusable>` still exists as a transitional re-export shim of `<FocusScope>` per the in-flight architecture-fix task `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, propagate the prop through. If `<Focusable>` already collapsed into `<FocusScope>`, this file is gone — skip.\n\n-**Entity call sites that must wrap in `<Inspectable>`:**\n+**Entity call sites that must opt IN (`inspectOnDoubleClick`):**\n\n-- `kanban-app/ui/src/components/entity-card.tsx` — `task:<id>`, `tag:<id>`.\n+- `kanban-app/ui/src/components/entity-card.tsx` — task / tag cards (`task:<id>`, `tag:<id>`).\n - `kanban-app/ui/src/components/column-view.tsx` — column zone (`column:<id>`).\n - `kanban-app/ui/src/components/board-view.tsx` — only the board entity wrapper, NOT the `ui:board` chrome zone.\n - `kanban-app/ui/src/components/fields/field.tsx` — field rows (`field:<entity>:<name>`).\n-- Any other call site whose moniker is a real entity — search via `git grep \"moniker={asMoniker(\\\"[a-z]*:\"` and classify each hit.\n-\n-**UI-chrome call sites (no `<Inspectable>` — verify):**\n-\n-- `nav-bar.tsx`, `toolbar.tsx`, `perspective-tab-bar.tsx`, `perspective-container.tsx`, `view-container.tsx`, `grid-view.tsx` cells, the `ui:board` chrome zone.\n+- Any other call site whose moniker is a real entity (search via `git grep` for `moniker={asMoniker(\"` and classify each hit).\n\n-**Architectural guard:**\n+**UI-chrome call sites that must stay opt-OUT (explicitly verify, do not change):**\n\n-- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a node-side source-walk guard:\n-  - **No `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`.** Greps the entire `kanban-app/ui/src/` tree (excluding test files and `inspectable.tsx` itself) for the literal string `useDispatchCommand(\"ui.inspect\")` and asserts zero matches. This catches any future regression where someone re-introduces the dispatch into a primitive.\n-  - **No CHROME-prefixed moniker is wrapped in `<Inspectable>`.** Walks `*.tsx`, finds every `<Inspectable …>` JSX element, parses its `moniker` prop value, and asserts the prefix is in an `ENTITY_PREFIXES` allowlist (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n-  - **Every `task:<id>` / `column:<id>` / `field:` JSX wrapper has an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new card type but forgot to wrap it.\" Use a simple regex pass that fails the test when an entity-monikered `<FocusScope>` / `<FocusZone>` does not have a sibling/ancestor `<Inspectable>` carrying the same moniker.\n+- `nav-bar.tsx` — all `ui:navbar.*`.\n+- All toolbar wrappers — `ui:toolbar.*`.\n+- `perspective-tab-bar.tsx` — `ui:perspective-bar` and `perspective_tab:<id>`. The button's own `onDoubleClick` continues to call `startRename`.\n+- `perspective-container.tsx` — `ui:perspective`.\n+- `view-container.tsx` — `ui:view`.\n+- `grid-view.tsx` — `ui:grid` and `cell:<r>:<c>`.\n+- The board chrome `ui:board` zone (the *container*, not the board entity itself).\n\n-### Optional, recommended: brand the moniker\n+**Architectural guard to extend:**\n\n-Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that throws / `never`s when `s` doesn't start with an entity prefix, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If the codebase already brands monikers (`Moniker` is itself branded per `@/types/spatial`), extend that branding with `EntityMoniker` as a sub-brand. If extending is more than a small change, file a follow-up task — the runtime guards in `focus-architecture.guards.node.test.ts` are sufficient for this task.\n+- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a guard that greps `kanban-app/ui/src/` for every `moniker={asMoniker(\"ui:` or `moniker={asMoniker(\\`perspective_tab:` etc. occurrence and asserts none of those call sites carry `inspectOnDoubleClick={true}`. Conversely, add a positive guard that every `entity-card`, `column-view`, `field` call site DOES carry the prop. The list of UI-chrome prefixes lives in the test as a `BANNED_INSPECT_PREFIXES` constant alongside the existing decoration guards.\n\n ### What this task does NOT do\n\n-- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just no longer triggers inspect.\n+- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just won't trigger inspect.\n - Does not touch the YAML scope `entity:perspective` (that scope serves the perspective.* command palette + context menu and is unrelated to the inspector).\n - Does not change the right-click context menu — context-menu commands are scoped per call site already; this task is only about double-click → inspect.\n\n ## Acceptance Criteria\n\n-- [ ] A new file `kanban-app/ui/src/components/inspectable.tsx` exports a single `<Inspectable moniker: Moniker>` component. No other file in `kanban-app/ui/src/` (excluding tests) calls `useDispatchCommand(\"ui.inspect\")`.\n - [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode (existing `onDoubleClick → startRename` behavior on the inner button).\n-- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector.\n-- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved via the entity-card now being wrapped in `<Inspectable>`.\n+- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector. `mockInvoke` is never called with a `ui_inspect`-equivalent invoke for those interactions.\n+- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved.\n - [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n - [ ] Double-clicking a field row DOES open the inspector for that field.\n-- [ ] `<FocusScope>` and `<FocusZone>` no longer accept any prop related to inspect. Searching the codebase for `inspectOnDoubleClick` returns zero matches (this prop name was considered and rejected — the wrapper-component design replaces it).\n-- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: no `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`; every `<Inspectable>` carries an entity-prefixed moniker; every entity-monikered wrapper has an ancestor `<Inspectable>`.\n+- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop. TypeScript flags the prop with autocompletion.\n+- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body — verified by a unit test that asserts the `command-scope` registry sees no lookup for `ui.inspect` from a chrome wrapper.\n+- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: every UI-chrome wrapper omits `inspectOnDoubleClick`, every entity wrapper sets it to `true`.\n - [ ] `cd kanban-app/ui && npm test` is green.\n - [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected, but verify nothing regressed).\n\n@@ -142,13 +102,13 @@\n\n ### Browser Tests (mandatory)\n\n-Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new wrapper-based design end-to-end against real DOM, so the human user does not have to manually click around to verify.\n+Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new opt-in behavior end-to-end against real DOM, so the human user does not have to manually click around to verify.\n\n #### Test files\n\n 1. `kanban-app/ui/src/components/perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (new) — locks in the reported bug.\n-2. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the new component in isolation and via composition.\n-3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guards.\n+2. `kanban-app/ui/src/components/focus-scope.inspect-opt-in.spatial.test.tsx` (new) — exercises the new prop.\n+3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guard for the chrome/entity classification.\n\n #### Setup (for browser tests)\n\n@@ -159,43 +119,41 @@\n\n **File 1 — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx`:**\n\n-1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved); (b) zero command dispatches occurred for `ui.inspect`. Capture by spying on the `command-scope` registry's dispatch fan-out, or by stubbing `useDispatchCommand(\"ui.inspect\")` to fail the test if invoked.\n+1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved — the button's own `onDoubleClick` still fires); (b) the `mockInvoke` call list contains zero entries whose first arg matches `/inspect/i` and zero dispatches that route to `ui.inspect`. The simplest assertion: spy on `useDispatchCommand` (or assert via the command-scope registry) that no `ui.inspect` dispatch was issued for `perspective_tab:*`.\n\n 2. **Regression: dblclick on the perspective bar's wrapping zone does NOT inspect** — double-click on the `ui:perspective-bar` zone background (whitespace inside the bar, not on a tab). Assert no `ui.inspect` dispatch.\n\n 3. **Single-click still focuses** — single-click on a tab still calls `mockInvoke(\"spatial_focus\", { key })`. Confirms we did not regress the focus path.\n\n-**File 2 — `inspectable.spatial.test.tsx`:**\n+**File 2 — `focus-scope.inspect-opt-in.spatial.test.tsx`:**\n\n-1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div>x</div></Inspectable>`. Double-click. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n+1. **Default (no prop) → no inspect** — render a bare `<FocusScope moniker=\"task:fake\">` (no `inspectOnDoubleClick`). Double-click. Assert no `ui.inspect` dispatch.\n\n-2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch (input owns the gesture).\n+2. **`inspectOnDoubleClick={true}` → inspect fires** — render `<FocusScope moniker=\"task:fake\" inspectOnDoubleClick>`. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:fake\" }`.\n\n-3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div>x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n+3. **`inspectOnDoubleClick={false}` explicit → no inspect** — same as default but with the prop set explicitly. Assert no dispatch (locks in the prop semantics).\n\n-4. **`<FocusScope>` alone (no `<Inspectable>` ancestor) does NOT fire inspect on dblclick** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>`. Double-click. Assert no `ui.inspect` dispatch and no `useDispatchCommand(\"ui.inspect\")` registration.\n+4. **No `useDispatchCommand(\"ui.inspect\")` registration when opt-out** — render a `<FocusScope>` with `inspectOnDoubleClick={false}` inside a test harness that records every `useDispatchCommand` call. Assert `\"ui.inspect\"` was never registered. Reason: avoid paying the registry walk + memoization cost for every chrome focusable.\n\n-5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n+5. **Symmetric for `<FocusZone>`** — repeat the four cases above against `<FocusZone>`.\n\n-6. **Entity opt-in real-world: card** — render `<EntityCard>` for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the migration of entity-card to compose `<Inspectable>` around `<FocusScope>` works.\n+6. **Entity opt-in real-world: card** — render `<EntityCard>` (or its column-view equivalent that wraps a card) for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`.\n\n-7. **Entity opt-in real-world: column** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n+7. **Entity opt-in real-world: column** — render a `<ColumnView>` whose wrapper sets `inspectOnDoubleClick`. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n\n-8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n+8. **Inner button stops propagation contract** — render `<FocusScope inspectOnDoubleClick><button onDoubleClick={(e) => e.stopPropagation()}>x</button></FocusScope>` and double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n\n #### Source-level guard — extend `focus-architecture.guards.node.test.ts`\n\n-Three new guards (Node-only, source-walk):\n-\n ```ts\n+const CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"];\n const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n ```\n\n-- **Guard A — single inspect dispatch site.** Walk every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one file: `inspectable.tsx`. Any other match fails the test.\n-- **Guard B — `<Inspectable>` only wraps entity monikers.** For each `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit, assert the prefix is in `ENTITY_PREFIXES`. Catches `<Inspectable moniker={asMoniker(\"ui:foo\")}>`.\n-- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Regex-pass over the JSX: when an entity-prefixed `<FocusScope>` / `<FocusZone>` is found, the same file must contain an `<Inspectable>` element with a matching moniker substring. Catches \"someone wrote a new entity wrapper but forgot the `<Inspectable>`.\"\n-\n-Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n+- Walk every `*.tsx` source file under `kanban-app/ui/src/`.\n+- For each `<FocusScope`, `<FocusZone`, or `<Focusable` element whose `moniker={asMoniker(\"<prefix>...\")}` matches a CHROME prefix, assert the same JSX element does NOT contain `inspectOnDoubleClick`. This catches \"someone added `inspectOnDoubleClick` to a nav-bar button.\"\n+- For each element whose moniker matches an ENTITY prefix, assert the element DOES contain `inspectOnDoubleClick` (or `inspectOnDoubleClick={true}`). This catches \"someone wrapped a new card type but forgot the opt-in.\" Allow a per-call-site `// inspect:exempt` comment in case there's a special-case entity not meant to be inspectable (extremely unlikely).\n+- Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n\n ### How to run\n\n@@ -207,4 +165,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then create `inspectable.tsx`, then strip the inspect dispatch out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site in `<Inspectable>`, then add the guards. Each step keeps the test suite stable.\n+- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default in `focus-scope.tsx` / `focus-zone.tsx`, then opt in at every entity call site, then add the guard. Each step keeps the test suite stable.\n"}]]}
{"id":"01KQ7K4HKH255EF0B3A5TXEFVD","timestamp":"2026-04-27T13:47:51.793941Z","op":"update","item_id":"01KQ7GM77B1E6YH8Z893K05VKY","forward_patch":"--- original\n+++ modified\n@@ -26,184 +26,119 @@\n }, [dispatch, moniker, handleEvents]);\n ```\n\n-So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n-\n-- `perspective_tab:<id>` — the bug the user reported.\n-- `ui:navbar`, `ui:navbar.*` — nav-bar zones and buttons.\n-- `ui:toolbar.*` — toolbar action groups and buttons.\n-- `ui:perspective-bar`, `ui:perspective`, `ui:view`, `ui:board`, `ui:grid` — container zones for layout, not entities.\n-- `cell:<row>:<col>` — grid cells, addresses inside an entity, not entities themselves.\n+So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker on double-click. The set of focusables today includes a lot of pure UI chrome (`perspective_tab:<id>`, `ui:navbar.*`, `ui:toolbar.*`, `ui:perspective-bar`, `cell:<row>:<col>`, etc.) that does not correspond to an inspectable entity.\n\n The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n\n-### Fix shape — introduce `<Inspectable>`\n+### Fix shape — `inspectOnDoubleClick` opt-in prop\n\n-Pull the `useDispatchCommand(\"ui.inspect\")` hook and the double-click → dispatch handler **out** of `<FocusScope>` and `<FocusZone>` entirely, and put it in a new dedicated component:\n+Make inspect-on-double-click an **explicit opt-in** on `<FocusScope>` / `<FocusZone>` via a new prop `inspectOnDoubleClick?: boolean` defaulting to `false`. Only entity call sites opt in.\n\n-```tsx\n-// kanban-app/ui/src/components/inspectable.tsx\n-export interface InspectableProps {\n-  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n-  moniker: Moniker;\n-  children: ReactNode;\n-}\n+Why opt-in (not opt-out via prefix detection or scope chain inspection):\n\n-export function Inspectable({ moniker, children }: InspectableProps) {\n-  const dispatch = useDispatchCommand(\"ui.inspect\");\n-  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n-    const target = e.target as HTMLElement;\n-    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n-    if (target.closest(\"[contenteditable]\")) return;\n-    e.stopPropagation();\n-    dispatch({ target: moniker }).catch(console.error);\n-  }, [dispatch, moniker]);\n+- The set of entity prefixes is owned by the kanban domain (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`). Hard-coding that list in a generic spatial primitive smears domain knowledge into UI infrastructure.\n+- A scope-based runtime check is brittle — the answer depends on whichever `CommandScopeProvider` happens to be the nearest ancestor, not on what *this* focusable represents.\n+- Opt-in flips the contract from \"inspect everything unless told otherwise\" (the current bug) to \"inspect only entity-bearing call sites that explicitly say so.\" That matches how the rest of the spatial primitives work (`showFocusBar`, `navOverride` — explicit, not inferred).\n\n-  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n-}\n-```\n-\n-Then entity wrappers compose explicitly:\n-\n-```tsx\n-// entity-card.tsx (entity)\n-<Inspectable moniker={taskMoniker}>\n-  <FocusScope moniker={taskMoniker}>\n-    {cardBody}\n-  </FocusScope>\n-</Inspectable>\n+Note: a follow-up task replaces this boolean with a dedicated `<Inspectable>` wrapper component for cleaner separation of concerns. This task ships the boolean as the urgent bug fix; the wrapper-based refactor follows.\n\n-// perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n-<FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n-  {tabContents}\n-</FocusScope>\n-```\n-\n-Why a wrapper component, not a boolean prop on `<FocusScope>`:\n-\n-- **The name encodes the architectural rule.** `<Inspectable moniker=\"task:01\">` reads as \"this DOM subtree represents an inspectable entity.\" Anyone reading the JSX knows the contract without consulting prop docs. A `inspectOnDoubleClick={true}` boolean is just a flag — the *meaning* (the entity-vs-chrome distinction) is buried in conventions about which prop value goes where.\n-- **Separation of concerns.** `<FocusScope>` and `<FocusZone>` are generic spatial primitives — they should know nothing about `ui.inspect` or the kanban command system. Ripping the dispatch out makes the primitives smaller, simpler, and reusable for future spatial UIs that have nothing to do with inspectors.\n-- **Composability.** A single entity that needs both inspect-on-double-click and a focus zone (e.g. an editable card) can compose `<Inspectable><FocusZone>…</FocusZone></Inspectable>`. The boolean would have to be duplicated on both `<FocusScope>` AND `<FocusZone>`, with no obvious answer to \"what if both nest?\"\n-- **Type-system enforcement.** `<Inspectable>` requires a `moniker` whose runtime shape can be validated (e.g. via a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes). A boolean offers no such enforcement.\n-- **The chrome path costs nothing.** When a `<FocusScope>` or `<FocusZone>` has no `<Inspectable>` ancestor, no `useDispatchCommand(\"ui.inspect\")` registration happens at all — saving the registry walk on every chrome wrapper.\n-\n ### Files in scope\n\n-**New file:**\n-\n-- `kanban-app/ui/src/components/inspectable.tsx` — the new wrapper component. Contains: the component, its props interface, a docstring explaining the contract (\"only entities; never UI chrome\"), and unit-tested input/textarea/contenteditable guards.\n-\n-**Primitive contract change (remove inspect logic):**\n+**Primitive contract change:**\n\n - `kanban-app/ui/src/components/focus-scope.tsx`\n-  - Delete `const dispatch = useDispatchCommand(\"ui.inspect\");` and the `handleDoubleClick` body that dispatches it. Keep `onDoubleClick` on the rendered `<div>` only if other behavior is still needed (e.g. `e.stopPropagation()` for nested-zone propagation control); otherwise remove the handler entirely.\n-  - Delete the same in `FallbackFocusScopeBody`.\n-  - Update the file-level docstring to describe the new contract: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n+  - Add `inspectOnDoubleClick?: boolean` to `FocusScopeOwnProps`. Default `false`.\n+  - Gate the `useDispatchCommand(\"ui.inspect\")` registration AND the `dispatch({ target: moniker })` call behind that flag — when `false`, the hook is not called at all.\n+  - Update the file-level docstring describing the click contract.\n\n - `kanban-app/ui/src/components/focus-zone.tsx`\n-  - Symmetric removal of the inspect dispatch. Same docstring update.\n+  - Symmetric change.\n\n-- `kanban-app/ui/src/components/focusable.tsx` — if it still exists as a transitional re-export shim per `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, no change beyond confirming the inspect logic is gone.\n+- `kanban-app/ui/src/components/focusable.tsx`\n+  - If still present as a transitional re-export shim per `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, propagate the prop.\n\n-**Entity call sites that must wrap in `<Inspectable>`:**\n+**Entity call sites that must opt IN:**\n\n-- `kanban-app/ui/src/components/entity-card.tsx` — `task:<id>`, `tag:<id>`.\n-- `kanban-app/ui/src/components/column-view.tsx` — column zone (`column:<id>`).\n+- `kanban-app/ui/src/components/entity-card.tsx` — task / tag cards.\n+- `kanban-app/ui/src/components/column-view.tsx` — column zone.\n - `kanban-app/ui/src/components/board-view.tsx` — only the board entity wrapper, NOT the `ui:board` chrome zone.\n-- `kanban-app/ui/src/components/fields/field.tsx` — field rows (`field:<entity>:<name>`).\n-- Any other call site whose moniker is a real entity — search via `git grep \"moniker={asMoniker(\\\"[a-z]*:\"` and classify each hit.\n+- `kanban-app/ui/src/components/fields/field.tsx` — field rows.\n+- Any other call site whose moniker is a real entity (search via `git grep` for `moniker={asMoniker(\"` and classify each hit).\n\n-**UI-chrome call sites (no `<Inspectable>` — verify):**\n+**UI-chrome call sites that must stay opt-OUT (verify):**\n\n-- `nav-bar.tsx`, `toolbar.tsx`, `perspective-tab-bar.tsx`, `perspective-container.tsx`, `view-container.tsx`, `grid-view.tsx` cells, the `ui:board` chrome zone.\n+- `nav-bar.tsx`, all toolbar wrappers, `perspective-tab-bar.tsx`, `perspective-container.tsx`, `view-container.tsx`, `grid-view.tsx`, the `ui:board` chrome zone.\n\n **Architectural guard:**\n\n-- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a node-side source-walk guard:\n-  - **No `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`.** Greps the entire `kanban-app/ui/src/` tree (excluding test files and `inspectable.tsx` itself) for the literal string `useDispatchCommand(\"ui.inspect\")` and asserts zero matches. This catches any future regression where someone re-introduces the dispatch into a primitive.\n-  - **No CHROME-prefixed moniker is wrapped in `<Inspectable>`.** Walks `*.tsx`, finds every `<Inspectable …>` JSX element, parses its `moniker` prop value, and asserts the prefix is in an `ENTITY_PREFIXES` allowlist (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n-  - **Every `task:<id>` / `column:<id>` / `field:` JSX wrapper has an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new card type but forgot to wrap it.\" Use a simple regex pass that fails the test when an entity-monikered `<FocusScope>` / `<FocusZone>` does not have a sibling/ancestor `<Inspectable>` carrying the same moniker.\n-\n-### Optional, recommended: brand the moniker\n-\n-Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that throws / `never`s when `s` doesn't start with an entity prefix, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If the codebase already brands monikers (`Moniker` is itself branded per `@/types/spatial`), extend that branding with `EntityMoniker` as a sub-brand. If extending is more than a small change, file a follow-up task — the runtime guards in `focus-architecture.guards.node.test.ts` are sufficient for this task.\n+- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a guard that walks `*.tsx` and asserts no UI-chrome wrapper carries `inspectOnDoubleClick={true}` and every entity wrapper carries `inspectOnDoubleClick`. Define `CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"]` and `ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"]` as test-local constants.\n\n ### What this task does NOT do\n\n-- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just no longer triggers inspect.\n-- Does not touch the YAML scope `entity:perspective` (that scope serves the perspective.* command palette + context menu and is unrelated to the inspector).\n-- Does not change the right-click context menu — context-menu commands are scoped per call site already; this task is only about double-click → inspect.\n+- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just won't trigger inspect.\n+- Does not touch the YAML scope `entity:perspective`.\n+- Does not change the right-click context menu.\n+- Does not introduce a separate `<Inspectable>` component — that is the follow-up refactor.\n\n ## Acceptance Criteria\n\n-- [ ] A new file `kanban-app/ui/src/components/inspectable.tsx` exports a single `<Inspectable moniker: Moniker>` component. No other file in `kanban-app/ui/src/` (excluding tests) calls `useDispatchCommand(\"ui.inspect\")`.\n-- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode (existing `onDoubleClick → startRename` behavior on the inner button).\n-- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector.\n-- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved via the entity-card now being wrapped in `<Inspectable>`.\n+- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode.\n+- [ ] Double-clicking any `ui:*` zone or leaf does NOT open the inspector.\n+- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — preserved.\n - [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n - [ ] Double-clicking a field row DOES open the inspector for that field.\n-- [ ] `<FocusScope>` and `<FocusZone>` no longer accept any prop related to inspect. Searching the codebase for `inspectOnDoubleClick` returns zero matches (this prop name was considered and rejected — the wrapper-component design replaces it).\n-- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: no `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`; every `<Inspectable>` carries an entity-prefixed moniker; every entity-monikered wrapper has an ancestor `<Inspectable>`.\n+- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop.\n+- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body.\n+- [ ] The architectural guard test passes.\n - [ ] `cd kanban-app/ui && npm test` is green.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected, but verify nothing regressed).\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green.\n\n ## Tests\n\n ### Browser Tests (mandatory)\n\n-Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new wrapper-based design end-to-end against real DOM, so the human user does not have to manually click around to verify.\n+Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium).\n\n #### Test files\n\n 1. `kanban-app/ui/src/components/perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (new) — locks in the reported bug.\n-2. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the new component in isolation and via composition.\n-3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guards.\n+2. `kanban-app/ui/src/components/focus-scope.inspect-opt-in.spatial.test.tsx` (new) — exercises the new prop.\n+3. Extend `focus-architecture.guards.node.test.ts` (existing).\n\n-#### Setup (for browser tests)\n+#### Setup\n\n-- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n-- Render components inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n+- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx`.\n+- Render inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>`.\n\n #### Required test cases\n\n-**File 1 — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx`:**\n-\n-1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved); (b) zero command dispatches occurred for `ui.inspect`. Capture by spying on the `command-scope` registry's dispatch fan-out, or by stubbing `useDispatchCommand(\"ui.inspect\")` to fail the test if invoked.\n-\n-2. **Regression: dblclick on the perspective bar's wrapping zone does NOT inspect** — double-click on the `ui:perspective-bar` zone background (whitespace inside the bar, not on a tab). Assert no `ui.inspect` dispatch.\n-\n-3. **Single-click still focuses** — single-click on a tab still calls `mockInvoke(\"spatial_focus\", { key })`. Confirms we did not regress the focus path.\n-\n-**File 2 — `inspectable.spatial.test.tsx`:**\n-\n-1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div>x</div></Inspectable>`. Double-click. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n-\n-2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch (input owns the gesture).\n-\n-3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div>x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n-\n-4. **`<FocusScope>` alone (no `<Inspectable>` ancestor) does NOT fire inspect on dblclick** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>`. Double-click. Assert no `ui.inspect` dispatch and no `useDispatchCommand(\"ui.inspect\")` registration.\n+**File 1:**\n\n-5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n-\n-6. **Entity opt-in real-world: card** — render `<EntityCard>` for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the migration of entity-card to compose `<Inspectable>` around `<FocusScope>` works.\n+1. **dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>`. Double-click a tab. Assert `<InlineRenameEditor>` mounts AND no `ui.inspect` dispatch.\n+2. **dblclick on the perspective bar zone background does NOT inspect.**\n+3. **Single-click still focuses** — calls `mockInvoke(\"spatial_focus\", { key })`.\n\n-7. **Entity opt-in real-world: column** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n+**File 2:**\n\n-8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n+1. **Default → no inspect** — bare `<FocusScope moniker=\"task:fake\">`, dblclick, no dispatch.\n+2. **`inspectOnDoubleClick={true}` → inspect fires** — dispatches with `{ target: \"task:fake\" }`.\n+3. **`inspectOnDoubleClick={false}` explicit → no inspect.**\n+4. **Hook not registered when opt-out** — `useDispatchCommand(\"ui.inspect\")` never called.\n+5. **Symmetric for `<FocusZone>`** — repeat 1–4.\n+6. **Entity card real-world** — render `<EntityCard>`, dblclick, assert `ui.inspect` with `task:<id>`.\n+7. **Entity column real-world** — dblclick column body whitespace, assert `ui.inspect` with `column:<id>`.\n+8. **Inner button stops propagation** — inner `<button onDoubleClick={(e) => e.stopPropagation()}>` blocks dispatch.\n\n #### Source-level guard — extend `focus-architecture.guards.node.test.ts`\n\n-Three new guards (Node-only, source-walk):\n-\n ```ts\n+const CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"];\n const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n ```\n\n-- **Guard A — single inspect dispatch site.** Walk every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one file: `inspectable.tsx`. Any other match fails the test.\n-- **Guard B — `<Inspectable>` only wraps entity monikers.** For each `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit, assert the prefix is in `ENTITY_PREFIXES`. Catches `<Inspectable moniker={asMoniker(\"ui:foo\")}>`.\n-- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Regex-pass over the JSX: when an entity-prefixed `<FocusScope>` / `<FocusZone>` is found, the same file must contain an `<Inspectable>` element with a matching moniker substring. Catches \"someone wrote a new entity wrapper but forgot the `<Inspectable>`.\"\n-\n-Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n+Walk every non-test `*.tsx` under `kanban-app/ui/src/`. For each `<FocusScope` / `<FocusZone` / `<Focusable` JSX element:\n+- If moniker matches a CHROME prefix, assert the element does NOT contain `inspectOnDoubleClick`.\n+- If moniker matches an ENTITY prefix, assert the element DOES contain `inspectOnDoubleClick` (or `={true}`).\n\n ### How to run\n\n@@ -211,8 +146,8 @@\n cd kanban-app/ui && npm test\n ```\n\n-Headless on CI. The CI workflow `.github/workflows/*.yml` already runs this command.\n+Headless on CI.\n\n ## Workflow\n\n-- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then create `inspectable.tsx`, then strip the inspect dispatch out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site in `<Inspectable>`, then add the guards. Each step keeps the test suite stable.\n+- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default, then opt in at every entity call site, then add the guard.\n","reverse_patch":"--- original\n+++ modified\n@@ -26,119 +26,184 @@\n }, [dispatch, moniker, handleEvents]);\n ```\n\n-So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker on double-click. The set of focusables today includes a lot of pure UI chrome (`perspective_tab:<id>`, `ui:navbar.*`, `ui:toolbar.*`, `ui:perspective-bar`, `cell:<row>:<col>`, etc.) that does not correspond to an inspectable entity.\n+So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n+\n+- `perspective_tab:<id>` — the bug the user reported.\n+- `ui:navbar`, `ui:navbar.*` — nav-bar zones and buttons.\n+- `ui:toolbar.*` — toolbar action groups and buttons.\n+- `ui:perspective-bar`, `ui:perspective`, `ui:view`, `ui:board`, `ui:grid` — container zones for layout, not entities.\n+- `cell:<row>:<col>` — grid cells, addresses inside an entity, not entities themselves.\n\n The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n\n-### Fix shape — `inspectOnDoubleClick` opt-in prop\n+### Fix shape — introduce `<Inspectable>`\n+\n+Pull the `useDispatchCommand(\"ui.inspect\")` hook and the double-click → dispatch handler **out** of `<FocusScope>` and `<FocusZone>` entirely, and put it in a new dedicated component:\n+\n+```tsx\n+// kanban-app/ui/src/components/inspectable.tsx\n+export interface InspectableProps {\n+  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n+  moniker: Moniker;\n+  children: ReactNode;\n+}\n+\n+export function Inspectable({ moniker, children }: InspectableProps) {\n+  const dispatch = useDispatchCommand(\"ui.inspect\");\n+  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n+    const target = e.target as HTMLElement;\n+    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n+    if (target.closest(\"[contenteditable]\")) return;\n+    e.stopPropagation();\n+    dispatch({ target: moniker }).catch(console.error);\n+  }, [dispatch, moniker]);\n+\n+  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n+}\n+```\n\n-Make inspect-on-double-click an **explicit opt-in** on `<FocusScope>` / `<FocusZone>` via a new prop `inspectOnDoubleClick?: boolean` defaulting to `false`. Only entity call sites opt in.\n+Then entity wrappers compose explicitly:\n\n-Why opt-in (not opt-out via prefix detection or scope chain inspection):\n+```tsx\n+// entity-card.tsx (entity)\n+<Inspectable moniker={taskMoniker}>\n+  <FocusScope moniker={taskMoniker}>\n+    {cardBody}\n+  </FocusScope>\n+</Inspectable>\n+\n+// perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n+<FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n+  {tabContents}\n+</FocusScope>\n+```\n\n-- The set of entity prefixes is owned by the kanban domain (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`). Hard-coding that list in a generic spatial primitive smears domain knowledge into UI infrastructure.\n-- A scope-based runtime check is brittle — the answer depends on whichever `CommandScopeProvider` happens to be the nearest ancestor, not on what *this* focusable represents.\n-- Opt-in flips the contract from \"inspect everything unless told otherwise\" (the current bug) to \"inspect only entity-bearing call sites that explicitly say so.\" That matches how the rest of the spatial primitives work (`showFocusBar`, `navOverride` — explicit, not inferred).\n+Why a wrapper component, not a boolean prop on `<FocusScope>`:\n\n-Note: a follow-up task replaces this boolean with a dedicated `<Inspectable>` wrapper component for cleaner separation of concerns. This task ships the boolean as the urgent bug fix; the wrapper-based refactor follows.\n+- **The name encodes the architectural rule.** `<Inspectable moniker=\"task:01\">` reads as \"this DOM subtree represents an inspectable entity.\" Anyone reading the JSX knows the contract without consulting prop docs. A `inspectOnDoubleClick={true}` boolean is just a flag — the *meaning* (the entity-vs-chrome distinction) is buried in conventions about which prop value goes where.\n+- **Separation of concerns.** `<FocusScope>` and `<FocusZone>` are generic spatial primitives — they should know nothing about `ui.inspect` or the kanban command system. Ripping the dispatch out makes the primitives smaller, simpler, and reusable for future spatial UIs that have nothing to do with inspectors.\n+- **Composability.** A single entity that needs both inspect-on-double-click and a focus zone (e.g. an editable card) can compose `<Inspectable><FocusZone>…</FocusZone></Inspectable>`. The boolean would have to be duplicated on both `<FocusScope>` AND `<FocusZone>`, with no obvious answer to \"what if both nest?\"\n+- **Type-system enforcement.** `<Inspectable>` requires a `moniker` whose runtime shape can be validated (e.g. via a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes). A boolean offers no such enforcement.\n+- **The chrome path costs nothing.** When a `<FocusScope>` or `<FocusZone>` has no `<Inspectable>` ancestor, no `useDispatchCommand(\"ui.inspect\")` registration happens at all — saving the registry walk on every chrome wrapper.\n\n ### Files in scope\n\n-**Primitive contract change:**\n+**New file:**\n+\n+- `kanban-app/ui/src/components/inspectable.tsx` — the new wrapper component. Contains: the component, its props interface, a docstring explaining the contract (\"only entities; never UI chrome\"), and unit-tested input/textarea/contenteditable guards.\n+\n+**Primitive contract change (remove inspect logic):**\n\n - `kanban-app/ui/src/components/focus-scope.tsx`\n-  - Add `inspectOnDoubleClick?: boolean` to `FocusScopeOwnProps`. Default `false`.\n-  - Gate the `useDispatchCommand(\"ui.inspect\")` registration AND the `dispatch({ target: moniker })` call behind that flag — when `false`, the hook is not called at all.\n-  - Update the file-level docstring describing the click contract.\n+  - Delete `const dispatch = useDispatchCommand(\"ui.inspect\");` and the `handleDoubleClick` body that dispatches it. Keep `onDoubleClick` on the rendered `<div>` only if other behavior is still needed (e.g. `e.stopPropagation()` for nested-zone propagation control); otherwise remove the handler entirely.\n+  - Delete the same in `FallbackFocusScopeBody`.\n+  - Update the file-level docstring to describe the new contract: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n\n - `kanban-app/ui/src/components/focus-zone.tsx`\n-  - Symmetric change.\n+  - Symmetric removal of the inspect dispatch. Same docstring update.\n\n-- `kanban-app/ui/src/components/focusable.tsx`\n-  - If still present as a transitional re-export shim per `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, propagate the prop.\n+- `kanban-app/ui/src/components/focusable.tsx` — if it still exists as a transitional re-export shim per `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, no change beyond confirming the inspect logic is gone.\n\n-**Entity call sites that must opt IN:**\n+**Entity call sites that must wrap in `<Inspectable>`:**\n\n-- `kanban-app/ui/src/components/entity-card.tsx` — task / tag cards.\n-- `kanban-app/ui/src/components/column-view.tsx` — column zone.\n+- `kanban-app/ui/src/components/entity-card.tsx` — `task:<id>`, `tag:<id>`.\n+- `kanban-app/ui/src/components/column-view.tsx` — column zone (`column:<id>`).\n - `kanban-app/ui/src/components/board-view.tsx` — only the board entity wrapper, NOT the `ui:board` chrome zone.\n-- `kanban-app/ui/src/components/fields/field.tsx` — field rows.\n-- Any other call site whose moniker is a real entity (search via `git grep` for `moniker={asMoniker(\"` and classify each hit).\n+- `kanban-app/ui/src/components/fields/field.tsx` — field rows (`field:<entity>:<name>`).\n+- Any other call site whose moniker is a real entity — search via `git grep \"moniker={asMoniker(\\\"[a-z]*:\"` and classify each hit.\n\n-**UI-chrome call sites that must stay opt-OUT (verify):**\n+**UI-chrome call sites (no `<Inspectable>` — verify):**\n\n-- `nav-bar.tsx`, all toolbar wrappers, `perspective-tab-bar.tsx`, `perspective-container.tsx`, `view-container.tsx`, `grid-view.tsx`, the `ui:board` chrome zone.\n+- `nav-bar.tsx`, `toolbar.tsx`, `perspective-tab-bar.tsx`, `perspective-container.tsx`, `view-container.tsx`, `grid-view.tsx` cells, the `ui:board` chrome zone.\n\n **Architectural guard:**\n\n-- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a guard that walks `*.tsx` and asserts no UI-chrome wrapper carries `inspectOnDoubleClick={true}` and every entity wrapper carries `inspectOnDoubleClick`. Define `CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"]` and `ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"]` as test-local constants.\n+- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a node-side source-walk guard:\n+  - **No `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`.** Greps the entire `kanban-app/ui/src/` tree (excluding test files and `inspectable.tsx` itself) for the literal string `useDispatchCommand(\"ui.inspect\")` and asserts zero matches. This catches any future regression where someone re-introduces the dispatch into a primitive.\n+  - **No CHROME-prefixed moniker is wrapped in `<Inspectable>`.** Walks `*.tsx`, finds every `<Inspectable …>` JSX element, parses its `moniker` prop value, and asserts the prefix is in an `ENTITY_PREFIXES` allowlist (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n+  - **Every `task:<id>` / `column:<id>` / `field:` JSX wrapper has an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new card type but forgot to wrap it.\" Use a simple regex pass that fails the test when an entity-monikered `<FocusScope>` / `<FocusZone>` does not have a sibling/ancestor `<Inspectable>` carrying the same moniker.\n+\n+### Optional, recommended: brand the moniker\n+\n+Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that throws / `never`s when `s` doesn't start with an entity prefix, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If the codebase already brands monikers (`Moniker` is itself branded per `@/types/spatial`), extend that branding with `EntityMoniker` as a sub-brand. If extending is more than a small change, file a follow-up task — the runtime guards in `focus-architecture.guards.node.test.ts` are sufficient for this task.\n\n ### What this task does NOT do\n\n-- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just won't trigger inspect.\n-- Does not touch the YAML scope `entity:perspective`.\n-- Does not change the right-click context menu.\n-- Does not introduce a separate `<Inspectable>` component — that is the follow-up refactor.\n+- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just no longer triggers inspect.\n+- Does not touch the YAML scope `entity:perspective` (that scope serves the perspective.* command palette + context menu and is unrelated to the inspector).\n+- Does not change the right-click context menu — context-menu commands are scoped per call site already; this task is only about double-click → inspect.\n\n ## Acceptance Criteria\n\n-- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode.\n-- [ ] Double-clicking any `ui:*` zone or leaf does NOT open the inspector.\n-- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — preserved.\n+- [ ] A new file `kanban-app/ui/src/components/inspectable.tsx` exports a single `<Inspectable moniker: Moniker>` component. No other file in `kanban-app/ui/src/` (excluding tests) calls `useDispatchCommand(\"ui.inspect\")`.\n+- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode (existing `onDoubleClick → startRename` behavior on the inner button).\n+- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector.\n+- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved via the entity-card now being wrapped in `<Inspectable>`.\n - [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n - [ ] Double-clicking a field row DOES open the inspector for that field.\n-- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop.\n-- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body.\n-- [ ] The architectural guard test passes.\n+- [ ] `<FocusScope>` and `<FocusZone>` no longer accept any prop related to inspect. Searching the codebase for `inspectOnDoubleClick` returns zero matches (this prop name was considered and rejected — the wrapper-component design replaces it).\n+- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: no `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`; every `<Inspectable>` carries an entity-prefixed moniker; every entity-monikered wrapper has an ancestor `<Inspectable>`.\n - [ ] `cd kanban-app/ui && npm test` is green.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green.\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected, but verify nothing regressed).\n\n ## Tests\n\n ### Browser Tests (mandatory)\n\n-Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium).\n+Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new wrapper-based design end-to-end against real DOM, so the human user does not have to manually click around to verify.\n\n #### Test files\n\n 1. `kanban-app/ui/src/components/perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (new) — locks in the reported bug.\n-2. `kanban-app/ui/src/components/focus-scope.inspect-opt-in.spatial.test.tsx` (new) — exercises the new prop.\n-3. Extend `focus-architecture.guards.node.test.ts` (existing).\n+2. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the new component in isolation and via composition.\n+3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guards.\n\n-#### Setup\n+#### Setup (for browser tests)\n\n-- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx`.\n-- Render inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>`.\n+- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n+- Render components inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n\n #### Required test cases\n\n-**File 1:**\n+**File 1 — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx`:**\n\n-1. **dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>`. Double-click a tab. Assert `<InlineRenameEditor>` mounts AND no `ui.inspect` dispatch.\n-2. **dblclick on the perspective bar zone background does NOT inspect.**\n-3. **Single-click still focuses** — calls `mockInvoke(\"spatial_focus\", { key })`.\n+1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved); (b) zero command dispatches occurred for `ui.inspect`. Capture by spying on the `command-scope` registry's dispatch fan-out, or by stubbing `useDispatchCommand(\"ui.inspect\")` to fail the test if invoked.\n\n-**File 2:**\n+2. **Regression: dblclick on the perspective bar's wrapping zone does NOT inspect** — double-click on the `ui:perspective-bar` zone background (whitespace inside the bar, not on a tab). Assert no `ui.inspect` dispatch.\n\n-1. **Default → no inspect** — bare `<FocusScope moniker=\"task:fake\">`, dblclick, no dispatch.\n-2. **`inspectOnDoubleClick={true}` → inspect fires** — dispatches with `{ target: \"task:fake\" }`.\n-3. **`inspectOnDoubleClick={false}` explicit → no inspect.**\n-4. **Hook not registered when opt-out** — `useDispatchCommand(\"ui.inspect\")` never called.\n-5. **Symmetric for `<FocusZone>`** — repeat 1–4.\n-6. **Entity card real-world** — render `<EntityCard>`, dblclick, assert `ui.inspect` with `task:<id>`.\n-7. **Entity column real-world** — dblclick column body whitespace, assert `ui.inspect` with `column:<id>`.\n-8. **Inner button stops propagation** — inner `<button onDoubleClick={(e) => e.stopPropagation()}>` blocks dispatch.\n+3. **Single-click still focuses** — single-click on a tab still calls `mockInvoke(\"spatial_focus\", { key })`. Confirms we did not regress the focus path.\n\n+**File 2 — `inspectable.spatial.test.tsx`:**\n+\n+1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div>x</div></Inspectable>`. Double-click. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n+\n+2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch (input owns the gesture).\n+\n+3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div>x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n+\n+4. **`<FocusScope>` alone (no `<Inspectable>` ancestor) does NOT fire inspect on dblclick** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>`. Double-click. Assert no `ui.inspect` dispatch and no `useDispatchCommand(\"ui.inspect\")` registration.\n+\n+5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n+\n+6. **Entity opt-in real-world: card** — render `<EntityCard>` for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the migration of entity-card to compose `<Inspectable>` around `<FocusScope>` works.\n+\n+7. **Entity opt-in real-world: column** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n+\n+8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n+\n #### Source-level guard — extend `focus-architecture.guards.node.test.ts`\n\n+Three new guards (Node-only, source-walk):\n+\n ```ts\n-const CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"];\n const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n ```\n\n-Walk every non-test `*.tsx` under `kanban-app/ui/src/`. For each `<FocusScope` / `<FocusZone` / `<Focusable` JSX element:\n-- If moniker matches a CHROME prefix, assert the element does NOT contain `inspectOnDoubleClick`.\n-- If moniker matches an ENTITY prefix, assert the element DOES contain `inspectOnDoubleClick` (or `={true}`).\n+- **Guard A — single inspect dispatch site.** Walk every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one file: `inspectable.tsx`. Any other match fails the test.\n+- **Guard B — `<Inspectable>` only wraps entity monikers.** For each `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit, assert the prefix is in `ENTITY_PREFIXES`. Catches `<Inspectable moniker={asMoniker(\"ui:foo\")}>`.\n+- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Regex-pass over the JSX: when an entity-prefixed `<FocusScope>` / `<FocusZone>` is found, the same file must contain an `<Inspectable>` element with a matching moniker substring. Catches \"someone wrote a new entity wrapper but forgot the `<Inspectable>`.\"\n+\n+Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n\n ### How to run\n\n@@ -146,8 +211,8 @@\n cd kanban-app/ui && npm test\n ```\n\n-Headless on CI.\n+Headless on CI. The CI workflow `.github/workflows/*.yml` already runs this command.\n\n ## Workflow\n\n-- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default, then opt in at every entity call site, then add the guard.\n+- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then create `inspectable.tsx`, then strip the inspect dispatch out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site in `<Inspectable>`, then add the guards. Each step keeps the test suite stable.\n","transaction_id":null}
{"id":"01KQ7K4HKJ6699M58PV7A3CHYS","timestamp":"2026-04-27T13:47:51.794653Z","entity_type":"task","entity_id":"01KQ7GM77B1E6YH8Z893K05VKY","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -18,184 +18,119 @@\n }, [dispatch, moniker, handleEvents]);\n ```\n\n-So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n-\n-- `perspective_tab:<id>` — the bug the user reported.\n-- `ui:navbar`, `ui:navbar.*` — nav-bar zones and buttons.\n-- `ui:toolbar.*` — toolbar action groups and buttons.\n-- `ui:perspective-bar`, `ui:perspective`, `ui:view`, `ui:board`, `ui:grid` — container zones for layout, not entities.\n-- `cell:<row>:<col>` — grid cells, addresses inside an entity, not entities themselves.\n+So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker on double-click. The set of focusables today includes a lot of pure UI chrome (`perspective_tab:<id>`, `ui:navbar.*`, `ui:toolbar.*`, `ui:perspective-bar`, `cell:<row>:<col>`, etc.) that does not correspond to an inspectable entity.\n\n The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n\n-### Fix shape — introduce `<Inspectable>`\n+### Fix shape — `inspectOnDoubleClick` opt-in prop\n\n-Pull the `useDispatchCommand(\"ui.inspect\")` hook and the double-click → dispatch handler **out** of `<FocusScope>` and `<FocusZone>` entirely, and put it in a new dedicated component:\n+Make inspect-on-double-click an **explicit opt-in** on `<FocusScope>` / `<FocusZone>` via a new prop `inspectOnDoubleClick?: boolean` defaulting to `false`. Only entity call sites opt in.\n\n-```tsx\n-// kanban-app/ui/src/components/inspectable.tsx\n-export interface InspectableProps {\n-  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n-  moniker: Moniker;\n-  children: ReactNode;\n-}\n+Why opt-in (not opt-out via prefix detection or scope chain inspection):\n\n-export function Inspectable({ moniker, children }: InspectableProps) {\n-  const dispatch = useDispatchCommand(\"ui.inspect\");\n-  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n-    const target = e.target as HTMLElement;\n-    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n-    if (target.closest(\"[contenteditable]\")) return;\n-    e.stopPropagation();\n-    dispatch({ target: moniker }).catch(console.error);\n-  }, [dispatch, moniker]);\n+- The set of entity prefixes is owned by the kanban domain (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`). Hard-coding that list in a generic spatial primitive smears domain knowledge into UI infrastructure.\n+- A scope-based runtime check is brittle — the answer depends on whichever `CommandScopeProvider` happens to be the nearest ancestor, not on what *this* focusable represents.\n+- Opt-in flips the contract from \"inspect everything unless told otherwise\" (the current bug) to \"inspect only entity-bearing call sites that explicitly say so.\" That matches how the rest of the spatial primitives work (`showFocusBar`, `navOverride` — explicit, not inferred).\n\n-  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n-}\n-```\n-\n-Then entity wrappers compose explicitly:\n-\n-```tsx\n-// entity-card.tsx (entity)\n-<Inspectable moniker={taskMoniker}>\n-  <FocusScope moniker={taskMoniker}>\n-    {cardBody}\n-  </FocusScope>\n-</Inspectable>\n+Note: a follow-up task replaces this boolean with a dedicated `<Inspectable>` wrapper component for cleaner separation of concerns. This task ships the boolean as the urgent bug fix; the wrapper-based refactor follows.\n\n-// perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n-<FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n-  {tabContents}\n-</FocusScope>\n-```\n-\n-Why a wrapper component, not a boolean prop on `<FocusScope>`:\n-\n-- **The name encodes the architectural rule.** `<Inspectable moniker=\"task:01\">` reads as \"this DOM subtree represents an inspectable entity.\" Anyone reading the JSX knows the contract without consulting prop docs. A `inspectOnDoubleClick={true}` boolean is just a flag — the *meaning* (the entity-vs-chrome distinction) is buried in conventions about which prop value goes where.\n-- **Separation of concerns.** `<FocusScope>` and `<FocusZone>` are generic spatial primitives — they should know nothing about `ui.inspect` or the kanban command system. Ripping the dispatch out makes the primitives smaller, simpler, and reusable for future spatial UIs that have nothing to do with inspectors.\n-- **Composability.** A single entity that needs both inspect-on-double-click and a focus zone (e.g. an editable card) can compose `<Inspectable><FocusZone>…</FocusZone></Inspectable>`. The boolean would have to be duplicated on both `<FocusScope>` AND `<FocusZone>`, with no obvious answer to \"what if both nest?\"\n-- **Type-system enforcement.** `<Inspectable>` requires a `moniker` whose runtime shape can be validated (e.g. via a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes). A boolean offers no such enforcement.\n-- **The chrome path costs nothing.** When a `<FocusScope>` or `<FocusZone>` has no `<Inspectable>` ancestor, no `useDispatchCommand(\"ui.inspect\")` registration happens at all — saving the registry walk on every chrome wrapper.\n-\n ### Files in scope\n\n-**New file:**\n-\n-- `kanban-app/ui/src/components/inspectable.tsx` — the new wrapper component. Contains: the component, its props interface, a docstring explaining the contract (\"only entities; never UI chrome\"), and unit-tested input/textarea/contenteditable guards.\n-\n-**Primitive contract change (remove inspect logic):**\n+**Primitive contract change:**\n\n - `kanban-app/ui/src/components/focus-scope.tsx`\n-  - Delete `const dispatch = useDispatchCommand(\"ui.inspect\");` and the `handleDoubleClick` body that dispatches it. Keep `onDoubleClick` on the rendered `<div>` only if other behavior is still needed (e.g. `e.stopPropagation()` for nested-zone propagation control); otherwise remove the handler entirely.\n-  - Delete the same in `FallbackFocusScopeBody`.\n-  - Update the file-level docstring to describe the new contract: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n+  - Add `inspectOnDoubleClick?: boolean` to `FocusScopeOwnProps`. Default `false`.\n+  - Gate the `useDispatchCommand(\"ui.inspect\")` registration AND the `dispatch({ target: moniker })` call behind that flag — when `false`, the hook is not called at all.\n+  - Update the file-level docstring describing the click contract.\n\n - `kanban-app/ui/src/components/focus-zone.tsx`\n-  - Symmetric removal of the inspect dispatch. Same docstring update.\n+  - Symmetric change.\n\n-- `kanban-app/ui/src/components/focusable.tsx` — if it still exists as a transitional re-export shim per `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, no change beyond confirming the inspect logic is gone.\n+- `kanban-app/ui/src/components/focusable.tsx`\n+  - If still present as a transitional re-export shim per `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, propagate the prop.\n\n-**Entity call sites that must wrap in `<Inspectable>`:**\n+**Entity call sites that must opt IN:**\n\n-- `kanban-app/ui/src/components/entity-card.tsx` — `task:<id>`, `tag:<id>`.\n-- `kanban-app/ui/src/components/column-view.tsx` — column zone (`column:<id>`).\n+- `kanban-app/ui/src/components/entity-card.tsx` — task / tag cards.\n+- `kanban-app/ui/src/components/column-view.tsx` — column zone.\n - `kanban-app/ui/src/components/board-view.tsx` — only the board entity wrapper, NOT the `ui:board` chrome zone.\n-- `kanban-app/ui/src/components/fields/field.tsx` — field rows (`field:<entity>:<name>`).\n-- Any other call site whose moniker is a real entity — search via `git grep \"moniker={asMoniker(\\\"[a-z]*:\"` and classify each hit.\n+- `kanban-app/ui/src/components/fields/field.tsx` — field rows.\n+- Any other call site whose moniker is a real entity (search via `git grep` for `moniker={asMoniker(\"` and classify each hit).\n\n-**UI-chrome call sites (no `<Inspectable>` — verify):**\n+**UI-chrome call sites that must stay opt-OUT (verify):**\n\n-- `nav-bar.tsx`, `toolbar.tsx`, `perspective-tab-bar.tsx`, `perspective-container.tsx`, `view-container.tsx`, `grid-view.tsx` cells, the `ui:board` chrome zone.\n+- `nav-bar.tsx`, all toolbar wrappers, `perspective-tab-bar.tsx`, `perspective-container.tsx`, `view-container.tsx`, `grid-view.tsx`, the `ui:board` chrome zone.\n\n **Architectural guard:**\n\n-- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a node-side source-walk guard:\n-  - **No `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`.** Greps the entire `kanban-app/ui/src/` tree (excluding test files and `inspectable.tsx` itself) for the literal string `useDispatchCommand(\"ui.inspect\")` and asserts zero matches. This catches any future regression where someone re-introduces the dispatch into a primitive.\n-  - **No CHROME-prefixed moniker is wrapped in `<Inspectable>`.** Walks `*.tsx`, finds every `<Inspectable …>` JSX element, parses its `moniker` prop value, and asserts the prefix is in an `ENTITY_PREFIXES` allowlist (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n-  - **Every `task:<id>` / `column:<id>` / `field:` JSX wrapper has an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new card type but forgot to wrap it.\" Use a simple regex pass that fails the test when an entity-monikered `<FocusScope>` / `<FocusZone>` does not have a sibling/ancestor `<Inspectable>` carrying the same moniker.\n-\n-### Optional, recommended: brand the moniker\n-\n-Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that throws / `never`s when `s` doesn't start with an entity prefix, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If the codebase already brands monikers (`Moniker` is itself branded per `@/types/spatial`), extend that branding with `EntityMoniker` as a sub-brand. If extending is more than a small change, file a follow-up task — the runtime guards in `focus-architecture.guards.node.test.ts` are sufficient for this task.\n+- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a guard that walks `*.tsx` and asserts no UI-chrome wrapper carries `inspectOnDoubleClick={true}` and every entity wrapper carries `inspectOnDoubleClick`. Define `CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"]` and `ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"]` as test-local constants.\n\n ### What this task does NOT do\n\n-- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just no longer triggers inspect.\n-- Does not touch the YAML scope `entity:perspective` (that scope serves the perspective.* command palette + context menu and is unrelated to the inspector).\n-- Does not change the right-click context menu — context-menu commands are scoped per call site already; this task is only about double-click → inspect.\n+- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just won't trigger inspect.\n+- Does not touch the YAML scope `entity:perspective`.\n+- Does not change the right-click context menu.\n+- Does not introduce a separate `<Inspectable>` component — that is the follow-up refactor.\n\n ## Acceptance Criteria\n\n-- [ ] A new file `kanban-app/ui/src/components/inspectable.tsx` exports a single `<Inspectable moniker: Moniker>` component. No other file in `kanban-app/ui/src/` (excluding tests) calls `useDispatchCommand(\"ui.inspect\")`.\n-- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode (existing `onDoubleClick → startRename` behavior on the inner button).\n-- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector.\n-- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved via the entity-card now being wrapped in `<Inspectable>`.\n+- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode.\n+- [ ] Double-clicking any `ui:*` zone or leaf does NOT open the inspector.\n+- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — preserved.\n - [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n - [ ] Double-clicking a field row DOES open the inspector for that field.\n-- [ ] `<FocusScope>` and `<FocusZone>` no longer accept any prop related to inspect. Searching the codebase for `inspectOnDoubleClick` returns zero matches (this prop name was considered and rejected — the wrapper-component design replaces it).\n-- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: no `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`; every `<Inspectable>` carries an entity-prefixed moniker; every entity-monikered wrapper has an ancestor `<Inspectable>`.\n+- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop.\n+- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body.\n+- [ ] The architectural guard test passes.\n - [ ] `cd kanban-app/ui && npm test` is green.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected, but verify nothing regressed).\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green.\n\n ## Tests\n\n ### Browser Tests (mandatory)\n\n-Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new wrapper-based design end-to-end against real DOM, so the human user does not have to manually click around to verify.\n+Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium).\n\n #### Test files\n\n 1. `kanban-app/ui/src/components/perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (new) — locks in the reported bug.\n-2. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the new component in isolation and via composition.\n-3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guards.\n+2. `kanban-app/ui/src/components/focus-scope.inspect-opt-in.spatial.test.tsx` (new) — exercises the new prop.\n+3. Extend `focus-architecture.guards.node.test.ts` (existing).\n\n-#### Setup (for browser tests)\n+#### Setup\n\n-- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n-- Render components inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n+- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx`.\n+- Render inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>`.\n\n #### Required test cases\n\n-**File 1 — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx`:**\n-\n-1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved); (b) zero command dispatches occurred for `ui.inspect`. Capture by spying on the `command-scope` registry's dispatch fan-out, or by stubbing `useDispatchCommand(\"ui.inspect\")` to fail the test if invoked.\n-\n-2. **Regression: dblclick on the perspective bar's wrapping zone does NOT inspect** — double-click on the `ui:perspective-bar` zone background (whitespace inside the bar, not on a tab). Assert no `ui.inspect` dispatch.\n-\n-3. **Single-click still focuses** — single-click on a tab still calls `mockInvoke(\"spatial_focus\", { key })`. Confirms we did not regress the focus path.\n-\n-**File 2 — `inspectable.spatial.test.tsx`:**\n-\n-1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div>x</div></Inspectable>`. Double-click. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n-\n-2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch (input owns the gesture).\n-\n-3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div>x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n-\n-4. **`<FocusScope>` alone (no `<Inspectable>` ancestor) does NOT fire inspect on dblclick** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>`. Double-click. Assert no `ui.inspect` dispatch and no `useDispatchCommand(\"ui.inspect\")` registration.\n+**File 1:**\n\n-5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n-\n-6. **Entity opt-in real-world: card** — render `<EntityCard>` for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the migration of entity-card to compose `<Inspectable>` around `<FocusScope>` works.\n+1. **dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>`. Double-click a tab. Assert `<InlineRenameEditor>` mounts AND no `ui.inspect` dispatch.\n+2. **dblclick on the perspective bar zone background does NOT inspect.**\n+3. **Single-click still focuses** — calls `mockInvoke(\"spatial_focus\", { key })`.\n\n-7. **Entity opt-in real-world: column** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n+**File 2:**\n\n-8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n+1. **Default → no inspect** — bare `<FocusScope moniker=\"task:fake\">`, dblclick, no dispatch.\n+2. **`inspectOnDoubleClick={true}` → inspect fires** — dispatches with `{ target: \"task:fake\" }`.\n+3. **`inspectOnDoubleClick={false}` explicit → no inspect.**\n+4. **Hook not registered when opt-out** — `useDispatchCommand(\"ui.inspect\")` never called.\n+5. **Symmetric for `<FocusZone>`** — repeat 1–4.\n+6. **Entity card real-world** — render `<EntityCard>`, dblclick, assert `ui.inspect` with `task:<id>`.\n+7. **Entity column real-world** — dblclick column body whitespace, assert `ui.inspect` with `column:<id>`.\n+8. **Inner button stops propagation** — inner `<button onDoubleClick={(e) => e.stopPropagation()}>` blocks dispatch.\n\n #### Source-level guard — extend `focus-architecture.guards.node.test.ts`\n\n-Three new guards (Node-only, source-walk):\n-\n ```ts\n+const CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"];\n const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n ```\n\n-- **Guard A — single inspect dispatch site.** Walk every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one file: `inspectable.tsx`. Any other match fails the test.\n-- **Guard B — `<Inspectable>` only wraps entity monikers.** For each `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit, assert the prefix is in `ENTITY_PREFIXES`. Catches `<Inspectable moniker={asMoniker(\"ui:foo\")}>`.\n-- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Regex-pass over the JSX: when an entity-prefixed `<FocusScope>` / `<FocusZone>` is found, the same file must contain an `<Inspectable>` element with a matching moniker substring. Catches \"someone wrote a new entity wrapper but forgot the `<Inspectable>`.\"\n-\n-Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n+Walk every non-test `*.tsx` under `kanban-app/ui/src/`. For each `<FocusScope` / `<FocusZone` / `<Focusable` JSX element:\n+- If moniker matches a CHROME prefix, assert the element does NOT contain `inspectOnDoubleClick`.\n+- If moniker matches an ENTITY prefix, assert the element DOES contain `inspectOnDoubleClick` (or `={true}`).\n\n ### How to run\n\n@@ -203,8 +138,8 @@\n cd kanban-app/ui && npm test\n ```\n\n-Headless on CI. The CI workflow `.github/workflows/*.yml` already runs this command.\n+Headless on CI.\n\n ## Workflow\n\n-- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then create `inspectable.tsx`, then strip the inspect dispatch out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site in `<Inspectable>`, then add the guards. Each step keeps the test suite stable.\n+- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default, then opt in at every entity call site, then add the guard.\n","reverse_patch":"--- original\n+++ modified\n@@ -18,119 +18,184 @@\n }, [dispatch, moniker, handleEvents]);\n ```\n\n-So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker on double-click. The set of focusables today includes a lot of pure UI chrome (`perspective_tab:<id>`, `ui:navbar.*`, `ui:toolbar.*`, `ui:perspective-bar`, `cell:<row>:<col>`, etc.) that does not correspond to an inspectable entity.\n+So **every** `<FocusScope>` / `<FocusZone>` wrapper, regardless of what it represents, fires `ui.inspect` with its own moniker on double-click. The set of focusables today includes a lot of pure UI chrome that does not correspond to an inspectable entity:\n+\n+- `perspective_tab:<id>` — the bug the user reported.\n+- `ui:navbar`, `ui:navbar.*` — nav-bar zones and buttons.\n+- `ui:toolbar.*` — toolbar action groups and buttons.\n+- `ui:perspective-bar`, `ui:perspective`, `ui:view`, `ui:board`, `ui:grid` — container zones for layout, not entities.\n+- `cell:<row>:<col>` — grid cells, addresses inside an entity, not entities themselves.\n\n The perspective tab additionally has its own `<button onDoubleClick={onDoubleClick}>` that calls `startRename(p.id)`. The button does not call `e.stopPropagation()`, so the double-click bubbles to the wrapping `<Focusable>` which fires `ui.inspect` — both rename **and** inspect run from one user gesture. Visible result: the inspector opens.\n\n-### Fix shape — `inspectOnDoubleClick` opt-in prop\n+### Fix shape — introduce `<Inspectable>`\n+\n+Pull the `useDispatchCommand(\"ui.inspect\")` hook and the double-click → dispatch handler **out** of `<FocusScope>` and `<FocusZone>` entirely, and put it in a new dedicated component:\n+\n+```tsx\n+// kanban-app/ui/src/components/inspectable.tsx\n+export interface InspectableProps {\n+  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n+  moniker: Moniker;\n+  children: ReactNode;\n+}\n+\n+export function Inspectable({ moniker, children }: InspectableProps) {\n+  const dispatch = useDispatchCommand(\"ui.inspect\");\n+  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n+    const target = e.target as HTMLElement;\n+    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n+    if (target.closest(\"[contenteditable]\")) return;\n+    e.stopPropagation();\n+    dispatch({ target: moniker }).catch(console.error);\n+  }, [dispatch, moniker]);\n+\n+  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n+}\n+```\n\n-Make inspect-on-double-click an **explicit opt-in** on `<FocusScope>` / `<FocusZone>` via a new prop `inspectOnDoubleClick?: boolean` defaulting to `false`. Only entity call sites opt in.\n+Then entity wrappers compose explicitly:\n\n-Why opt-in (not opt-out via prefix detection or scope chain inspection):\n+```tsx\n+// entity-card.tsx (entity)\n+<Inspectable moniker={taskMoniker}>\n+  <FocusScope moniker={taskMoniker}>\n+    {cardBody}\n+  </FocusScope>\n+</Inspectable>\n+\n+// perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n+<FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n+  {tabContents}\n+</FocusScope>\n+```\n\n-- The set of entity prefixes is owned by the kanban domain (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`). Hard-coding that list in a generic spatial primitive smears domain knowledge into UI infrastructure.\n-- A scope-based runtime check is brittle — the answer depends on whichever `CommandScopeProvider` happens to be the nearest ancestor, not on what *this* focusable represents.\n-- Opt-in flips the contract from \"inspect everything unless told otherwise\" (the current bug) to \"inspect only entity-bearing call sites that explicitly say so.\" That matches how the rest of the spatial primitives work (`showFocusBar`, `navOverride` — explicit, not inferred).\n+Why a wrapper component, not a boolean prop on `<FocusScope>`:\n\n-Note: a follow-up task replaces this boolean with a dedicated `<Inspectable>` wrapper component for cleaner separation of concerns. This task ships the boolean as the urgent bug fix; the wrapper-based refactor follows.\n+- **The name encodes the architectural rule.** `<Inspectable moniker=\"task:01\">` reads as \"this DOM subtree represents an inspectable entity.\" Anyone reading the JSX knows the contract without consulting prop docs. A `inspectOnDoubleClick={true}` boolean is just a flag — the *meaning* (the entity-vs-chrome distinction) is buried in conventions about which prop value goes where.\n+- **Separation of concerns.** `<FocusScope>` and `<FocusZone>` are generic spatial primitives — they should know nothing about `ui.inspect` or the kanban command system. Ripping the dispatch out makes the primitives smaller, simpler, and reusable for future spatial UIs that have nothing to do with inspectors.\n+- **Composability.** A single entity that needs both inspect-on-double-click and a focus zone (e.g. an editable card) can compose `<Inspectable><FocusZone>…</FocusZone></Inspectable>`. The boolean would have to be duplicated on both `<FocusScope>` AND `<FocusZone>`, with no obvious answer to \"what if both nest?\"\n+- **Type-system enforcement.** `<Inspectable>` requires a `moniker` whose runtime shape can be validated (e.g. via a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes). A boolean offers no such enforcement.\n+- **The chrome path costs nothing.** When a `<FocusScope>` or `<FocusZone>` has no `<Inspectable>` ancestor, no `useDispatchCommand(\"ui.inspect\")` registration happens at all — saving the registry walk on every chrome wrapper.\n\n ### Files in scope\n\n-**Primitive contract change:**\n+**New file:**\n+\n+- `kanban-app/ui/src/components/inspectable.tsx` — the new wrapper component. Contains: the component, its props interface, a docstring explaining the contract (\"only entities; never UI chrome\"), and unit-tested input/textarea/contenteditable guards.\n+\n+**Primitive contract change (remove inspect logic):**\n\n - `kanban-app/ui/src/components/focus-scope.tsx`\n-  - Add `inspectOnDoubleClick?: boolean` to `FocusScopeOwnProps`. Default `false`.\n-  - Gate the `useDispatchCommand(\"ui.inspect\")` registration AND the `dispatch({ target: moniker })` call behind that flag — when `false`, the hook is not called at all.\n-  - Update the file-level docstring describing the click contract.\n+  - Delete `const dispatch = useDispatchCommand(\"ui.inspect\");` and the `handleDoubleClick` body that dispatches it. Keep `onDoubleClick` on the rendered `<div>` only if other behavior is still needed (e.g. `e.stopPropagation()` for nested-zone propagation control); otherwise remove the handler entirely.\n+  - Delete the same in `FallbackFocusScopeBody`.\n+  - Update the file-level docstring to describe the new contract: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n\n - `kanban-app/ui/src/components/focus-zone.tsx`\n-  - Symmetric change.\n+  - Symmetric removal of the inspect dispatch. Same docstring update.\n\n-- `kanban-app/ui/src/components/focusable.tsx`\n-  - If still present as a transitional re-export shim per `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, propagate the prop.\n+- `kanban-app/ui/src/components/focusable.tsx` — if it still exists as a transitional re-export shim per `01KQ5PP55SAAVJ0V3HDJ1DGNBY`, no change beyond confirming the inspect logic is gone.\n\n-**Entity call sites that must opt IN:**\n+**Entity call sites that must wrap in `<Inspectable>`:**\n\n-- `kanban-app/ui/src/components/entity-card.tsx` — task / tag cards.\n-- `kanban-app/ui/src/components/column-view.tsx` — column zone.\n+- `kanban-app/ui/src/components/entity-card.tsx` — `task:<id>`, `tag:<id>`.\n+- `kanban-app/ui/src/components/column-view.tsx` — column zone (`column:<id>`).\n - `kanban-app/ui/src/components/board-view.tsx` — only the board entity wrapper, NOT the `ui:board` chrome zone.\n-- `kanban-app/ui/src/components/fields/field.tsx` — field rows.\n-- Any other call site whose moniker is a real entity (search via `git grep` for `moniker={asMoniker(\"` and classify each hit).\n+- `kanban-app/ui/src/components/fields/field.tsx` — field rows (`field:<entity>:<name>`).\n+- Any other call site whose moniker is a real entity — search via `git grep \"moniker={asMoniker(\\\"[a-z]*:\"` and classify each hit.\n\n-**UI-chrome call sites that must stay opt-OUT (verify):**\n+**UI-chrome call sites (no `<Inspectable>` — verify):**\n\n-- `nav-bar.tsx`, all toolbar wrappers, `perspective-tab-bar.tsx`, `perspective-container.tsx`, `view-container.tsx`, `grid-view.tsx`, the `ui:board` chrome zone.\n+- `nav-bar.tsx`, `toolbar.tsx`, `perspective-tab-bar.tsx`, `perspective-container.tsx`, `view-container.tsx`, `grid-view.tsx` cells, the `ui:board` chrome zone.\n\n **Architectural guard:**\n\n-- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a guard that walks `*.tsx` and asserts no UI-chrome wrapper carries `inspectOnDoubleClick={true}` and every entity wrapper carries `inspectOnDoubleClick`. Define `CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"]` and `ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"]` as test-local constants.\n+- `kanban-app/ui/src/components/focus-architecture.guards.node.test.ts` — add a node-side source-walk guard:\n+  - **No `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`.** Greps the entire `kanban-app/ui/src/` tree (excluding test files and `inspectable.tsx` itself) for the literal string `useDispatchCommand(\"ui.inspect\")` and asserts zero matches. This catches any future regression where someone re-introduces the dispatch into a primitive.\n+  - **No CHROME-prefixed moniker is wrapped in `<Inspectable>`.** Walks `*.tsx`, finds every `<Inspectable …>` JSX element, parses its `moniker` prop value, and asserts the prefix is in an `ENTITY_PREFIXES` allowlist (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n+  - **Every `task:<id>` / `column:<id>` / `field:` JSX wrapper has an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new card type but forgot to wrap it.\" Use a simple regex pass that fails the test when an entity-monikered `<FocusScope>` / `<FocusZone>` does not have a sibling/ancestor `<Inspectable>` carrying the same moniker.\n+\n+### Optional, recommended: brand the moniker\n+\n+Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that throws / `never`s when `s` doesn't start with an entity prefix, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If the codebase already brands monikers (`Moniker` is itself branded per `@/types/spatial`), extend that branding with `EntityMoniker` as a sub-brand. If extending is more than a small change, file a follow-up task — the runtime guards in `focus-architecture.guards.node.test.ts` are sufficient for this task.\n\n ### What this task does NOT do\n\n-- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just won't trigger inspect.\n-- Does not touch the YAML scope `entity:perspective`.\n-- Does not change the right-click context menu.\n-- Does not introduce a separate `<Inspectable>` component — that is the follow-up refactor.\n+- Does not redesign moniker prefixes. `perspective_tab:<id>` stays as a moniker — it just no longer triggers inspect.\n+- Does not touch the YAML scope `entity:perspective` (that scope serves the perspective.* command palette + context menu and is unrelated to the inspector).\n+- Does not change the right-click context menu — context-menu commands are scoped per call site already; this task is only about double-click → inspect.\n\n ## Acceptance Criteria\n\n-- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode.\n-- [ ] Double-clicking any `ui:*` zone or leaf does NOT open the inspector.\n-- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — preserved.\n+- [ ] A new file `kanban-app/ui/src/components/inspectable.tsx` exports a single `<Inspectable moniker: Moniker>` component. No other file in `kanban-app/ui/src/` (excluding tests) calls `useDispatchCommand(\"ui.inspect\")`.\n+- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode (existing `onDoubleClick → startRename` behavior on the inner button).\n+- [ ] Double-clicking any `ui:*` zone or leaf (nav-bar buttons, toolbar buttons, perspective bar, view/grid/perspective container zones) does NOT open the inspector.\n+- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — existing behavior is preserved via the entity-card now being wrapped in `<Inspectable>`.\n - [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n - [ ] Double-clicking a field row DOES open the inspector for that field.\n-- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop.\n-- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body.\n-- [ ] The architectural guard test passes.\n+- [ ] `<FocusScope>` and `<FocusZone>` no longer accept any prop related to inspect. Searching the codebase for `inspectOnDoubleClick` returns zero matches (this prop name was considered and rejected — the wrapper-component design replaces it).\n+- [ ] The architectural guard test in `focus-architecture.guards.node.test.ts` passes: no `useDispatchCommand(\"ui.inspect\")` outside `inspectable.tsx`; every `<Inspectable>` carries an entity-prefixed moniker; every entity-monikered wrapper has an ancestor `<Inspectable>`.\n - [ ] `cd kanban-app/ui && npm test` is green.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green.\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected, but verify nothing regressed).\n\n ## Tests\n\n ### Browser Tests (mandatory)\n\n-Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium).\n+Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium). They prove the new wrapper-based design end-to-end against real DOM, so the human user does not have to manually click around to verify.\n\n #### Test files\n\n 1. `kanban-app/ui/src/components/perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (new) — locks in the reported bug.\n-2. `kanban-app/ui/src/components/focus-scope.inspect-opt-in.spatial.test.tsx` (new) — exercises the new prop.\n-3. Extend `focus-architecture.guards.node.test.ts` (existing).\n+2. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the new component in isolation and via composition.\n+3. Extend `focus-architecture.guards.node.test.ts` (existing) — source-level guards.\n\n-#### Setup\n+#### Setup (for browser tests)\n\n-- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx`.\n-- Render inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>`.\n+- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n+- Render components inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n\n #### Required test cases\n\n-**File 1:**\n+**File 1 — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx`:**\n\n-1. **dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>`. Double-click a tab. Assert `<InlineRenameEditor>` mounts AND no `ui.inspect` dispatch.\n-2. **dblclick on the perspective bar zone background does NOT inspect.**\n-3. **Single-click still focuses** — calls `mockInvoke(\"spatial_focus\", { key })`.\n+1. **Regression: dblclick on perspective tab does NOT inspect** — render `<PerspectiveTabBar/>` with two perspectives. Double-click a tab. Assert: (a) `<InlineRenameEditor>` mounts (existing rename behavior preserved); (b) zero command dispatches occurred for `ui.inspect`. Capture by spying on the `command-scope` registry's dispatch fan-out, or by stubbing `useDispatchCommand(\"ui.inspect\")` to fail the test if invoked.\n\n-**File 2:**\n+2. **Regression: dblclick on the perspective bar's wrapping zone does NOT inspect** — double-click on the `ui:perspective-bar` zone background (whitespace inside the bar, not on a tab). Assert no `ui.inspect` dispatch.\n\n-1. **Default → no inspect** — bare `<FocusScope moniker=\"task:fake\">`, dblclick, no dispatch.\n-2. **`inspectOnDoubleClick={true}` → inspect fires** — dispatches with `{ target: \"task:fake\" }`.\n-3. **`inspectOnDoubleClick={false}` explicit → no inspect.**\n-4. **Hook not registered when opt-out** — `useDispatchCommand(\"ui.inspect\")` never called.\n-5. **Symmetric for `<FocusZone>`** — repeat 1–4.\n-6. **Entity card real-world** — render `<EntityCard>`, dblclick, assert `ui.inspect` with `task:<id>`.\n-7. **Entity column real-world** — dblclick column body whitespace, assert `ui.inspect` with `column:<id>`.\n-8. **Inner button stops propagation** — inner `<button onDoubleClick={(e) => e.stopPropagation()}>` blocks dispatch.\n+3. **Single-click still focuses** — single-click on a tab still calls `mockInvoke(\"spatial_focus\", { key })`. Confirms we did not regress the focus path.\n\n+**File 2 — `inspectable.spatial.test.tsx`:**\n+\n+1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div>x</div></Inspectable>`. Double-click. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n+\n+2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch (input owns the gesture).\n+\n+3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div>x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n+\n+4. **`<FocusScope>` alone (no `<Inspectable>` ancestor) does NOT fire inspect on dblclick** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>`. Double-click. Assert no `ui.inspect` dispatch and no `useDispatchCommand(\"ui.inspect\")` registration.\n+\n+5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n+\n+6. **Entity opt-in real-world: card** — render `<EntityCard>` for a task. Double-click. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the migration of entity-card to compose `<Inspectable>` around `<FocusScope>` works.\n+\n+7. **Entity opt-in real-world: column** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click on the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n+\n+8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture). Documents the escape hatch.\n+\n #### Source-level guard — extend `focus-architecture.guards.node.test.ts`\n\n+Three new guards (Node-only, source-walk):\n+\n ```ts\n-const CHROME_PREFIXES = [\"ui:\", \"perspective_tab:\", \"cell:\"];\n const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n ```\n\n-Walk every non-test `*.tsx` under `kanban-app/ui/src/`. For each `<FocusScope` / `<FocusZone` / `<Focusable` JSX element:\n-- If moniker matches a CHROME prefix, assert the element does NOT contain `inspectOnDoubleClick`.\n-- If moniker matches an ENTITY prefix, assert the element DOES contain `inspectOnDoubleClick` (or `={true}`).\n+- **Guard A — single inspect dispatch site.** Walk every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one file: `inspectable.tsx`. Any other match fails the test.\n+- **Guard B — `<Inspectable>` only wraps entity monikers.** For each `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit, assert the prefix is in `ENTITY_PREFIXES`. Catches `<Inspectable moniker={asMoniker(\"ui:foo\")}>`.\n+- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Regex-pass over the JSX: when an entity-prefixed `<FocusScope>` / `<FocusZone>` is found, the same file must contain an `<Inspectable>` element with a matching moniker substring. Catches \"someone wrote a new entity wrapper but forgot the `<Inspectable>`.\"\n+\n+Test files are excluded from the walk so test fixtures with synthetic monikers don't trigger.\n\n ### How to run\n\n@@ -138,8 +203,8 @@\n cd kanban-app/ui && npm test\n ```\n\n-Headless on CI.\n+Headless on CI. The CI workflow `.github/workflows/*.yml` already runs this command.\n\n ## Workflow\n\n-- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default, then opt in at every entity call site, then add the guard.\n+- Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then create `inspectable.tsx`, then strip the inspect dispatch out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site in `<Inspectable>`, then add the guards. Each step keeps the test suite stable.\n"}]]}
{"id":"01KQ7KB3911NJGMJNY230REYXH","timestamp":"2026-04-27T13:51:26.497894Z","op":"update","item_id":"01KQ7GM77B1E6YH8Z893K05VKY","forward_patch":"--- original\n+++ modified\n@@ -82,16 +82,16 @@\n\n ## Acceptance Criteria\n\n-- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode.\n-- [ ] Double-clicking any `ui:*` zone or leaf does NOT open the inspector.\n-- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — preserved.\n-- [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n-- [ ] Double-clicking a field row DOES open the inspector for that field.\n-- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop.\n-- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body.\n-- [ ] The architectural guard test passes.\n-- [ ] `cd kanban-app/ui && npm test` is green.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green.\n+- [x] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode.\n+- [x] Double-clicking any `ui:*` zone or leaf does NOT open the inspector.\n+- [x] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — preserved.\n+- [x] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n+- [x] Double-clicking a field row DOES open the inspector for that field.\n+- [x] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop.\n+- [x] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body.\n+- [x] The architectural guard test passes.\n+- [x] `cd kanban-app/ui && npm test` is green.\n+- [x] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green.\n\n ## Tests\n\n@@ -151,3 +151,13 @@\n ## Workflow\n\n - Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default, then opt in at every entity call site, then add the guard.\n+\n+## Implementation summary\n+\n+Implemented `inspectOnDoubleClick` opt-in prop on `<FocusScope>` / `<FocusZone>` via a small inner `<InspectDoubleClickRegistrar>` child component pattern: when the prop is true, the registrar mounts and calls `useDispatchCommand(\"ui.inspect\")`, installing the dispatcher into a ref that the body's `onDoubleClick` reads. When false (the default), the registrar does not mount, the hook is never called, and `onDoubleClick` is not attached to the body div at all — so the gesture bubbles to ancestors untouched.\n+\n+Entity call sites opted-in: `entity-card.tsx`, `column-view.tsx`, `board-view.tsx`, `fields/field.tsx`, `data-table.tsx` (entity-row scope), `mention-view.tsx` (mention pills), `fields/displays/attachment-display.tsx`. UI-chrome call sites stay opt-out by default (no code changes). The column-name navigation leaf in `column-view.tsx` carries `// inspect:exempt` because it wraps a synthetic `column:<id>.name` moniker whose inner `<Field>` zone owns the inspect opt-in.\n+\n+Architectural guard added to `focus-architecture.guards.node.test.ts`. Two new test files added: `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (4 tests) and `focus-scope.inspect-opt-in.spatial.test.tsx` (11 tests). Updated 2 existing tests in `focus-scope.test.tsx` to add the prop now that it's required.\n+\n+Full UI suite: 1714/1714 passing. Rust suites for `swissarmyhammer-kanban` and `swissarmyhammer-commands` also green.\n","reverse_patch":"--- original\n+++ modified\n@@ -82,16 +82,16 @@\n\n ## Acceptance Criteria\n\n-- [x] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode.\n-- [x] Double-clicking any `ui:*` zone or leaf does NOT open the inspector.\n-- [x] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — preserved.\n-- [x] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n-- [x] Double-clicking a field row DOES open the inspector for that field.\n-- [x] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop.\n-- [x] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body.\n-- [x] The architectural guard test passes.\n-- [x] `cd kanban-app/ui && npm test` is green.\n-- [x] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green.\n+- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode.\n+- [ ] Double-clicking any `ui:*` zone or leaf does NOT open the inspector.\n+- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — preserved.\n+- [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n+- [ ] Double-clicking a field row DOES open the inspector for that field.\n+- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop.\n+- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body.\n+- [ ] The architectural guard test passes.\n+- [ ] `cd kanban-app/ui && npm test` is green.\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green.\n\n ## Tests\n\n@@ -151,13 +151,3 @@\n ## Workflow\n\n - Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default, then opt in at every entity call site, then add the guard.\n-\n-## Implementation summary\n-\n-Implemented `inspectOnDoubleClick` opt-in prop on `<FocusScope>` / `<FocusZone>` via a small inner `<InspectDoubleClickRegistrar>` child component pattern: when the prop is true, the registrar mounts and calls `useDispatchCommand(\"ui.inspect\")`, installing the dispatcher into a ref that the body's `onDoubleClick` reads. When false (the default), the registrar does not mount, the hook is never called, and `onDoubleClick` is not attached to the body div at all — so the gesture bubbles to ancestors untouched.\n-\n-Entity call sites opted-in: `entity-card.tsx`, `column-view.tsx`, `board-view.tsx`, `fields/field.tsx`, `data-table.tsx` (entity-row scope), `mention-view.tsx` (mention pills), `fields/displays/attachment-display.tsx`. UI-chrome call sites stay opt-out by default (no code changes). The column-name navigation leaf in `column-view.tsx` carries `// inspect:exempt` because it wraps a synthetic `column:<id>.name` moniker whose inner `<Field>` zone owns the inspect opt-in.\n-\n-Architectural guard added to `focus-architecture.guards.node.test.ts`. Two new test files added: `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (4 tests) and `focus-scope.inspect-opt-in.spatial.test.tsx` (11 tests). Updated 2 existing tests in `focus-scope.test.tsx` to add the prop now that it's required.\n-\n-Full UI suite: 1714/1714 passing. Rust suites for `swissarmyhammer-kanban` and `swissarmyhammer-commands` also green.\n","transaction_id":null}
{"id":"01KQ7KB392K2F3MRJ4BBGDJ1D4","timestamp":"2026-04-27T13:51:26.498501Z","entity_type":"task","entity_id":"01KQ7GM77B1E6YH8Z893K05VKY","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -74,16 +74,16 @@\n\n ## Acceptance Criteria\n\n-- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode.\n-- [ ] Double-clicking any `ui:*` zone or leaf does NOT open the inspector.\n-- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — preserved.\n-- [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n-- [ ] Double-clicking a field row DOES open the inspector for that field.\n-- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop.\n-- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body.\n-- [ ] The architectural guard test passes.\n-- [ ] `cd kanban-app/ui && npm test` is green.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green.\n+- [x] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode.\n+- [x] Double-clicking any `ui:*` zone or leaf does NOT open the inspector.\n+- [x] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — preserved.\n+- [x] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n+- [x] Double-clicking a field row DOES open the inspector for that field.\n+- [x] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop.\n+- [x] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body.\n+- [x] The architectural guard test passes.\n+- [x] `cd kanban-app/ui && npm test` is green.\n+- [x] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green.\n\n ## Tests\n\n@@ -143,3 +143,13 @@\n ## Workflow\n\n - Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default, then opt in at every entity call site, then add the guard.\n+\n+## Implementation summary\n+\n+Implemented `inspectOnDoubleClick` opt-in prop on `<FocusScope>` / `<FocusZone>` via a small inner `<InspectDoubleClickRegistrar>` child component pattern: when the prop is true, the registrar mounts and calls `useDispatchCommand(\"ui.inspect\")`, installing the dispatcher into a ref that the body's `onDoubleClick` reads. When false (the default), the registrar does not mount, the hook is never called, and `onDoubleClick` is not attached to the body div at all — so the gesture bubbles to ancestors untouched.\n+\n+Entity call sites opted-in: `entity-card.tsx`, `column-view.tsx`, `board-view.tsx`, `fields/field.tsx`, `data-table.tsx` (entity-row scope), `mention-view.tsx` (mention pills), `fields/displays/attachment-display.tsx`. UI-chrome call sites stay opt-out by default (no code changes). The column-name navigation leaf in `column-view.tsx` carries `// inspect:exempt` because it wraps a synthetic `column:<id>.name` moniker whose inner `<Field>` zone owns the inspect opt-in.\n+\n+Architectural guard added to `focus-architecture.guards.node.test.ts`. Two new test files added: `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (4 tests) and `focus-scope.inspect-opt-in.spatial.test.tsx` (11 tests). Updated 2 existing tests in `focus-scope.test.tsx` to add the prop now that it's required.\n+\n+Full UI suite: 1714/1714 passing. Rust suites for `swissarmyhammer-kanban` and `swissarmyhammer-commands` also green.\n","reverse_patch":"--- original\n+++ modified\n@@ -74,16 +74,16 @@\n\n ## Acceptance Criteria\n\n-- [x] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode.\n-- [x] Double-clicking any `ui:*` zone or leaf does NOT open the inspector.\n-- [x] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — preserved.\n-- [x] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n-- [x] Double-clicking a field row DOES open the inspector for that field.\n-- [x] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop.\n-- [x] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body.\n-- [x] The architectural guard test passes.\n-- [x] `cd kanban-app/ui && npm test` is green.\n-- [x] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green.\n+- [ ] Double-clicking a perspective tab does NOT open the inspector. It only enters inline rename mode.\n+- [ ] Double-clicking any `ui:*` zone or leaf does NOT open the inspector.\n+- [ ] Double-clicking a card (`task:<id>` or `tag:<id>`) DOES open the inspector — preserved.\n+- [ ] Double-clicking a column (`column:<id>`) DOES open the inspector for that column.\n+- [ ] Double-clicking a field row DOES open the inspector for that field.\n+- [ ] `<FocusScope>` and `<FocusZone>` accept a typed `inspectOnDoubleClick?: boolean` prop.\n+- [ ] When `inspectOnDoubleClick` is unset or `false`, `useDispatchCommand(\"ui.inspect\")` is not called inside the body.\n+- [ ] The architectural guard test passes.\n+- [ ] `cd kanban-app/ui && npm test` is green.\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green.\n\n ## Tests\n\n@@ -143,13 +143,3 @@\n ## Workflow\n\n - Use `/tdd` — write failing test 1 in File 1 first (proves the reported bug), confirm it fails, then add the prop + flip the default, then opt in at every entity call site, then add the guard.\n-\n-## Implementation summary\n-\n-Implemented `inspectOnDoubleClick` opt-in prop on `<FocusScope>` / `<FocusZone>` via a small inner `<InspectDoubleClickRegistrar>` child component pattern: when the prop is true, the registrar mounts and calls `useDispatchCommand(\"ui.inspect\")`, installing the dispatcher into a ref that the body's `onDoubleClick` reads. When false (the default), the registrar does not mount, the hook is never called, and `onDoubleClick` is not attached to the body div at all — so the gesture bubbles to ancestors untouched.\n-\n-Entity call sites opted-in: `entity-card.tsx`, `column-view.tsx`, `board-view.tsx`, `fields/field.tsx`, `data-table.tsx` (entity-row scope), `mention-view.tsx` (mention pills), `fields/displays/attachment-display.tsx`. UI-chrome call sites stay opt-out by default (no code changes). The column-name navigation leaf in `column-view.tsx` carries `// inspect:exempt` because it wraps a synthetic `column:<id>.name` moniker whose inner `<Field>` zone owns the inspect opt-in.\n-\n-Architectural guard added to `focus-architecture.guards.node.test.ts`. Two new test files added: `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (4 tests) and `focus-scope.inspect-opt-in.spatial.test.tsx` (11 tests). Updated 2 existing tests in `focus-scope.test.tsx` to add the prop now that it's required.\n-\n-Full UI suite: 1714/1714 passing. Rust suites for `swissarmyhammer-kanban` and `swissarmyhammer-commands` also green.\n"}]]}
{"id":"01KQ7KCB2AASAJDWNJ3DVKH3VR","timestamp":"2026-04-27T13:52:07.242633Z","op":"update","item_id":"01KQ7GM77B1E6YH8Z893K05VKY","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Fix: double-click on perspective tab opens inspector — perspective is NOT an entity'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Fix: double-click on perspective tab opens inspector — perspective is NOT an entity'\n ---\n","transaction_id":null}
{"id":"01KQ7KCB2EQTRT8ATECYCSB66Z","timestamp":"2026-04-27T13:52:07.246241Z","entity_type":"task","entity_id":"01KQ7GM77B1E6YH8Z893K05VKY","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KQ7KYAGNFYNHE85BAV3H8TQB","timestamp":"2026-04-27T14:01:56.501865Z","op":"update","item_id":"01KQ7GM77B1E6YH8Z893K05VKY","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8280'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffc880\n project: spatial-nav\n title: 'Fix: double-click on perspective tab opens inspector — perspective is NOT an entity'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffc880\n+position_column: review\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Fix: double-click on perspective tab opens inspector — perspective is NOT an entity'\n ---\n","transaction_id":null}
{"id":"01KQ7KYAGQQS7EFEM0WN8JGFQA","timestamp":"2026-04-27T14:01:56.503069Z","entity_type":"task","entity_id":"01KQ7GM77B1E6YH8Z893K05VKY","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffc880\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffc880\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n"}]]}
{"id":"01KQZ9BVCJ10SB0M6D0GJWFQYZ","timestamp":"2026-05-06T18:38:51.794455Z","op":"update","item_id":"01KQ7GM77B1E6YH8Z893K05VKY","forward_patch":"--- original\n+++ modified\n@@ -160,4 +160,4 @@\n\n Architectural guard added to `focus-architecture.guards.node.test.ts`. Two new test files added: `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (4 tests) and `focus-scope.inspect-opt-in.spatial.test.tsx` (11 tests). Updated 2 existing tests in `focus-scope.test.tsx` to add the prop now that it's required.\n\n-Full UI suite: 1714/1714 passing. Rust suites for `swissarmyhammer-kanban` and `swissarmyhammer-commands` also green.\n+Full UI suite: 1714/1714 passing. Rust suites for `swissarmyhammer-kanban` and `swissarmyhammer-commands` also green.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -160,4 +160,4 @@\n\n Architectural guard added to `focus-architecture.guards.node.test.ts`. Two new test files added: `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (4 tests) and `focus-scope.inspect-opt-in.spatial.test.tsx` (11 tests). Updated 2 existing tests in `focus-scope.test.tsx` to add the prop now that it's required.\n\n-Full UI suite: 1714/1714 passing. Rust suites for `swissarmyhammer-kanban` and `swissarmyhammer-commands` also green.\n\\ No newline at end of file\n+Full UI suite: 1714/1714 passing. Rust suites for `swissarmyhammer-kanban` and `swissarmyhammer-commands` also green.\n","transaction_id":null}
{"id":"01KQZ9BVCP3G64X9TCBAPYXVX8","timestamp":"2026-05-06T18:38:51.798180Z","entity_type":"task","entity_id":"01KQ7GM77B1E6YH8Z893K05VKY","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -152,4 +152,4 @@\n\n Architectural guard added to `focus-architecture.guards.node.test.ts`. Two new test files added: `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (4 tests) and `focus-scope.inspect-opt-in.spatial.test.tsx` (11 tests). Updated 2 existing tests in `focus-scope.test.tsx` to add the prop now that it's required.\n\n-Full UI suite: 1714/1714 passing. Rust suites for `swissarmyhammer-kanban` and `swissarmyhammer-commands` also green.\n+Full UI suite: 1714/1714 passing. Rust suites for `swissarmyhammer-kanban` and `swissarmyhammer-commands` also green.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -152,4 +152,4 @@\n\n Architectural guard added to `focus-architecture.guards.node.test.ts`. Two new test files added: `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` (4 tests) and `focus-scope.inspect-opt-in.spatial.test.tsx` (11 tests). Updated 2 existing tests in `focus-scope.test.tsx` to add the prop now that it's required.\n\n-Full UI suite: 1714/1714 passing. Rust suites for `swissarmyhammer-kanban` and `swissarmyhammer-commands` also green.\n\\ No newline at end of file\n+Full UI suite: 1714/1714 passing. Rust suites for `swissarmyhammer-kanban` and `swissarmyhammer-commands` also green.\n"}]]}

{"id":"01KRRN2M0ER26STE4ZXY5CQE20","timestamp":"2026-05-16T15:04:33.038732Z","op":"create","item_id":"01KRRN2M0E5YXA9E24N1BM2BVP","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,31 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '80'\n+project: ai-panel\n+title: WebSocket transport for the ACP agent server\n+---\n+## What\n+The AI panel's ACP client runs entirely in the webview (TypeScript). A webview cannot spawn a subprocess or use stdio, so it cannot use ACP's standard stdio transport. The Rust ACP agent server must expose its JSON-RPC message stream over a transport a browser connects to natively: a **WebSocket**. Tauri is not involved in the ACP data path at all.\n+\n+- `sah agent acp` (`apps/swissarmyhammer-cli/src/commands/agent/acp.rs`) currently calls `AcpServer::start_with_streams(stdin(), stdout())`. Add a WebSocket listen mode — e.g. `sah agent acp --listen 127.0.0.1:0`: bind a loopback TCP listener, accept a WebSocket upgrade, and run the ACP agent with the WebSocket carrying newline-delimited JSON-RPC (the same ndjson framing as stdio per ACP's transports spec — messages delimited by `\\n`, no embedded newlines).\n+- Use `tokio-tungstenite` (workspace dep). Adapt the WebSocket to the stream shape `AcpServer` needs, or add `start_with_websocket`.\n+- Report the bound port (stdout line and/or exit-on-failure). Loopback-only bind.\n+- stdio mode stays the default and is unchanged.\n+\n+This is the \"server in Rust exposes the stream\" half of the architecture. Reference: ACP transports — https://agentclientprotocol.com/protocol/transports\n+\n+## Acceptance Criteria\n+- [ ] `sah agent acp --listen 127.0.0.1:0` binds a loopback port, accepts a WebSocket connection, and serves ACP JSON-RPC over it.\n+- [ ] ndjson framing over the WebSocket matches the stdio transport (newline-delimited, no embedded newlines).\n+- [ ] The bound port is discoverable by the launcher (printed or otherwise surfaced).\n+- [ ] stdio mode is unchanged and remains the default; `cargo build` clean.\n+\n+## Tests\n+- [ ] Integration test: start `sah agent acp --listen 127.0.0.1:0`, connect a WebSocket client, send `initialize`, assert a valid ACP `initialize` response with negotiated protocol version.\n+- [ ] Test that stdio mode (`start_with_streams`) still works unchanged.\n+- [ ] `cargo test -p swissarmyhammer-cli` (and the affected agent crate) is green.\n+\n+## Workflow\n+- Use `/tdd` — write the WebSocket `initialize` round-trip test first.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,31 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '80'\n-project: ai-panel\n-title: WebSocket transport for the ACP agent server\n----\n-## What\n-The AI panel's ACP client runs entirely in the webview (TypeScript). A webview cannot spawn a subprocess or use stdio, so it cannot use ACP's standard stdio transport. The Rust ACP agent server must expose its JSON-RPC message stream over a transport a browser connects to natively: a **WebSocket**. Tauri is not involved in the ACP data path at all.\n-\n-- `sah agent acp` (`apps/swissarmyhammer-cli/src/commands/agent/acp.rs`) currently calls `AcpServer::start_with_streams(stdin(), stdout())`. Add a WebSocket listen mode — e.g. `sah agent acp --listen 127.0.0.1:0`: bind a loopback TCP listener, accept a WebSocket upgrade, and run the ACP agent with the WebSocket carrying newline-delimited JSON-RPC (the same ndjson framing as stdio per ACP's transports spec — messages delimited by `\\n`, no embedded newlines).\n-- Use `tokio-tungstenite` (workspace dep). Adapt the WebSocket to the stream shape `AcpServer` needs, or add `start_with_websocket`.\n-- Report the bound port (stdout line and/or exit-on-failure). Loopback-only bind.\n-- stdio mode stays the default and is unchanged.\n-\n-This is the \"server in Rust exposes the stream\" half of the architecture. Reference: ACP transports — https://agentclientprotocol.com/protocol/transports\n-\n-## Acceptance Criteria\n-- [ ] `sah agent acp --listen 127.0.0.1:0` binds a loopback port, accepts a WebSocket connection, and serves ACP JSON-RPC over it.\n-- [ ] ndjson framing over the WebSocket matches the stdio transport (newline-delimited, no embedded newlines).\n-- [ ] The bound port is discoverable by the launcher (printed or otherwise surfaced).\n-- [ ] stdio mode is unchanged and remains the default; `cargo build` clean.\n-\n-## Tests\n-- [ ] Integration test: start `sah agent acp --listen 127.0.0.1:0`, connect a WebSocket client, send `initialize`, assert a valid ACP `initialize` response with negotiated protocol version.\n-- [ ] Test that stdio mode (`start_with_streams`) still works unchanged.\n-- [ ] `cargo test -p swissarmyhammer-cli` (and the affected agent crate) is green.\n-\n-## Workflow\n-- Use `/tdd` — write the WebSocket `initialize` round-trip test first.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRRQQD2QJA7XTTDDBY5XPX3J","timestamp":"2026-05-16T15:50:51.223588Z","op":"update","item_id":"01KRRN2M0E5YXA9E24N1BM2BVP","forward_patch":"--- original\n+++ modified\n@@ -4,28 +4,27 @@\n position_column: todo\n position_ordinal: '80'\n project: ai-panel\n-title: WebSocket transport for the ACP agent server\n+title: In-process ACP agent served over a localhost WebSocket\n ---\n ## What\n-The AI panel's ACP client runs entirely in the webview (TypeScript). A webview cannot spawn a subprocess or use stdio, so it cannot use ACP's standard stdio transport. The Rust ACP agent server must expose its JSON-RPC message stream over a transport a browser connects to natively: a **WebSocket**. Tauri is not involved in the ACP data path at all.\n-\n-- `sah agent acp` (`apps/swissarmyhammer-cli/src/commands/agent/acp.rs`) currently calls `AcpServer::start_with_streams(stdin(), stdout())`. Add a WebSocket listen mode — e.g. `sah agent acp --listen 127.0.0.1:0`: bind a loopback TCP listener, accept a WebSocket upgrade, and run the ACP agent with the WebSocket carrying newline-delimited JSON-RPC (the same ndjson framing as stdio per ACP's transports spec — messages delimited by `\\n`, no embedded newlines).\n-- Use `tokio-tungstenite` (workspace dep). Adapt the WebSocket to the stream shape `AcpServer` needs, or add `start_with_websocket`.\n-- Report the bound port (stdout line and/or exit-on-failure). Loopback-only bind.\n-- stdio mode stays the default and is unchanged.\n+The ACP agent runs **inside the kanban-app process** — not as an external subprocess. The Tauri Rust backend builds the ACP agent in-process and exposes it on a loopback WebSocket; the webview's TypeScript ACP client connects to that WebSocket. Tauri IPC is NOT in the ACP data path — the data path is a plain WebSocket.\n\n-This is the \"server in Rust exposes the stream\" half of the architecture. Reference: ACP transports — https://agentclientprotocol.com/protocol/transports\n+- New `apps/kanban-app/src/ai/agent_ws.rs`. Add to `apps/kanban-app/Cargo.toml`: `swissarmyhammer-agent`, `agent-client-protocol`, `tokio-tungstenite` (workspace deps).\n+- Host a loopback WebSocket server (`tokio-tungstenite`, bind `127.0.0.1:0`); report the bound port.\n+- On a WebSocket connection, build the ACP agent in-process via `swissarmyhammer_agent::create_agent(&model_config, None)` — `mcp_config` is `None`; the kanban MCP server is delivered over ACP in `newSession.mcpServers`, never baked in. `create_agent` dispatches Claude Code (claude-agent) vs local llama (llama-agent).\n+- Run the ACP **agent side** over the WebSocket: adapt the WebSocket to the `AsyncRead`/`AsyncWrite` byte stream the agent server consumes, with newline-delimited JSON framing. The agent servers already expose a `start_with_streams(read, write)` stdio pattern (`llama_agent::acp::AcpServer`, claude-agent's server) — adapt that to the WS stream (e.g. via `ws_stream_tungstenite` or a small adapter).\n+- Gate `llama-agent` behind a Cargo feature (`ai-local-models`, default off) so the standard build stays light — claude-agent is always available; local llama is opt-in.\n\n ## Acceptance Criteria\n-- [ ] `sah agent acp --listen 127.0.0.1:0` binds a loopback port, accepts a WebSocket connection, and serves ACP JSON-RPC over it.\n-- [ ] ndjson framing over the WebSocket matches the stdio transport (newline-delimited, no embedded newlines).\n-- [ ] The bound port is discoverable by the launcher (printed or otherwise surfaced).\n-- [ ] stdio mode is unchanged and remains the default; `cargo build` clean.\n+- [ ] A loopback WebSocket server runs in the kanban-app process; a WebSocket client connecting to it completes the ACP `initialize` handshake against an in-process agent.\n+- [ ] The agent is built via `swissarmyhammer_agent::create_agent`; Claude Code works without the `ai-local-models` feature.\n+- [ ] No external agent subprocess — the agent runs in the kanban-app process (claude-agent spawning `claude` internally is fine).\n+- [ ] `cargo build -p kanban-app` is clean both with and without `--features ai-local-models`.\n\n ## Tests\n-- [ ] Integration test: start `sah agent acp --listen 127.0.0.1:0`, connect a WebSocket client, send `initialize`, assert a valid ACP `initialize` response with negotiated protocol version.\n-- [ ] Test that stdio mode (`start_with_streams`) still works unchanged.\n-- [ ] `cargo test -p swissarmyhammer-cli` (and the affected agent crate) is green.\n+- [ ] Integration test (`apps/kanban-app/tests/`): start the WS agent server, connect a WebSocket client, send `initialize`, assert a valid ACP `initialize` response with a negotiated protocol version.\n+- [ ] Build/feature test: `kanban-app` builds with and without `ai-local-models`.\n+- [ ] `cargo test -p kanban-app` is green.\n\n ## Workflow\n - Use `/tdd` — write the WebSocket `initialize` round-trip test first.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -4,27 +4,28 @@\n position_column: todo\n position_ordinal: '80'\n project: ai-panel\n-title: In-process ACP agent served over a localhost WebSocket\n+title: WebSocket transport for the ACP agent server\n ---\n ## What\n-The ACP agent runs **inside the kanban-app process** — not as an external subprocess. The Tauri Rust backend builds the ACP agent in-process and exposes it on a loopback WebSocket; the webview's TypeScript ACP client connects to that WebSocket. Tauri IPC is NOT in the ACP data path — the data path is a plain WebSocket.\n+The AI panel's ACP client runs entirely in the webview (TypeScript). A webview cannot spawn a subprocess or use stdio, so it cannot use ACP's standard stdio transport. The Rust ACP agent server must expose its JSON-RPC message stream over a transport a browser connects to natively: a **WebSocket**. Tauri is not involved in the ACP data path at all.\n\n-- New `apps/kanban-app/src/ai/agent_ws.rs`. Add to `apps/kanban-app/Cargo.toml`: `swissarmyhammer-agent`, `agent-client-protocol`, `tokio-tungstenite` (workspace deps).\n-- Host a loopback WebSocket server (`tokio-tungstenite`, bind `127.0.0.1:0`); report the bound port.\n-- On a WebSocket connection, build the ACP agent in-process via `swissarmyhammer_agent::create_agent(&model_config, None)` — `mcp_config` is `None`; the kanban MCP server is delivered over ACP in `newSession.mcpServers`, never baked in. `create_agent` dispatches Claude Code (claude-agent) vs local llama (llama-agent).\n-- Run the ACP **agent side** over the WebSocket: adapt the WebSocket to the `AsyncRead`/`AsyncWrite` byte stream the agent server consumes, with newline-delimited JSON framing. The agent servers already expose a `start_with_streams(read, write)` stdio pattern (`llama_agent::acp::AcpServer`, claude-agent's server) — adapt that to the WS stream (e.g. via `ws_stream_tungstenite` or a small adapter).\n-- Gate `llama-agent` behind a Cargo feature (`ai-local-models`, default off) so the standard build stays light — claude-agent is always available; local llama is opt-in.\n+- `sah agent acp` (`apps/swissarmyhammer-cli/src/commands/agent/acp.rs`) currently calls `AcpServer::start_with_streams(stdin(), stdout())`. Add a WebSocket listen mode — e.g. `sah agent acp --listen 127.0.0.1:0`: bind a loopback TCP listener, accept a WebSocket upgrade, and run the ACP agent with the WebSocket carrying newline-delimited JSON-RPC (the same ndjson framing as stdio per ACP's transports spec — messages delimited by `\\n`, no embedded newlines).\n+- Use `tokio-tungstenite` (workspace dep). Adapt the WebSocket to the stream shape `AcpServer` needs, or add `start_with_websocket`.\n+- Report the bound port (stdout line and/or exit-on-failure). Loopback-only bind.\n+- stdio mode stays the default and is unchanged.\n+\n+This is the \"server in Rust exposes the stream\" half of the architecture. Reference: ACP transports — https://agentclientprotocol.com/protocol/transports\n\n ## Acceptance Criteria\n-- [ ] A loopback WebSocket server runs in the kanban-app process; a WebSocket client connecting to it completes the ACP `initialize` handshake against an in-process agent.\n-- [ ] The agent is built via `swissarmyhammer_agent::create_agent`; Claude Code works without the `ai-local-models` feature.\n-- [ ] No external agent subprocess — the agent runs in the kanban-app process (claude-agent spawning `claude` internally is fine).\n-- [ ] `cargo build -p kanban-app` is clean both with and without `--features ai-local-models`.\n+- [ ] `sah agent acp --listen 127.0.0.1:0` binds a loopback port, accepts a WebSocket connection, and serves ACP JSON-RPC over it.\n+- [ ] ndjson framing over the WebSocket matches the stdio transport (newline-delimited, no embedded newlines).\n+- [ ] The bound port is discoverable by the launcher (printed or otherwise surfaced).\n+- [ ] stdio mode is unchanged and remains the default; `cargo build` clean.\n\n ## Tests\n-- [ ] Integration test (`apps/kanban-app/tests/`): start the WS agent server, connect a WebSocket client, send `initialize`, assert a valid ACP `initialize` response with a negotiated protocol version.\n-- [ ] Build/feature test: `kanban-app` builds with and without `ai-local-models`.\n-- [ ] `cargo test -p kanban-app` is green.\n+- [ ] Integration test: start `sah agent acp --listen 127.0.0.1:0`, connect a WebSocket client, send `initialize`, assert a valid ACP `initialize` response with negotiated protocol version.\n+- [ ] Test that stdio mode (`start_with_streams`) still works unchanged.\n+- [ ] `cargo test -p swissarmyhammer-cli` (and the affected agent crate) is green.\n\n ## Workflow\n - Use `/tdd` — write the WebSocket `initialize` round-trip test first.\n\\ No newline at end of file\n","transaction_id":null}

---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kz2nt5v41htrdzd059nda41j
  text: |
    ### Research — the real surface is much wider than the card's file list

    Three parallel sweeps over the whole tree. Findings that change the plan:

    **The executor enum is SHARED with embedding.** `ModelExecutorType` / `ModelExecutorConfig` carry `LlamaEmbedding` and `AneEmbedding` next to `ClaudeCode` and `LlamaAgent`. `crates/swissarmyhammer-embedding/src/embedder.rs` matches on them. So the enum cannot simply be deleted — the chat variants come out, the embedding variants stay, and `embedder.rs` needs a small mechanical follow-up (its `other =>` arm becomes an unreachable pattern, and `EmbedderError::NotAnEmbeddingModel` becomes dead).

    **`sah doctor` reports no model at all.** No check in `crates/swissarmyhammer-doctor/` or `apps/swissarmyhammer-cli/src/commands/doctor/` mentions a model. The only record of the resolved review tier is `tracing::info!("review scope → …")` in serve and `"Building claude agent (executor=…, extra_args=…)"` in the agent. So there is no doctor/runtime disagreement to reintroduce; the single resolver keeps that property.

    **`ModelConfig::claude_code_haiku()` is already dead** — doc comments only.

    **`use_agent` / `use_agent_for` / `ModelTarget` have zero production callers.** They wrote the model NAME into config for the deleted `sah model` command. Under a Claude-switch field they are actively wrong (`validate_agent` → `find_agent_by_name` would only ever find embedding models), so they go.

    **`--model` has TWO divergent definitions**: the clap derive `apps/swissarmyhammer-cli/src/cli.rs` (used only to generate docs/completions) and the hand-built `Arg` in `apps/swissarmyhammer-cli/src/dynamic_cli.rs` (what the binary actually parses), with different help text. `extract_model_flag` hand-scans argv (no `--model=x`, scans past `--`) because the tool context is built before the CLI is parsed. Zero tests. Removing it is the clean resolution.

    **Collateral that must move with the change** (none of it is in the card's file list):
    - `crates/swissarmyhammer-config/src/template_context.rs` — `get_agent_config` / `get_all_agent_configs` are the template-context half of the chat model YAML system, plus a compiled doctest using `LlamaAgentConfig`.
    - `crates/swissarmyhammer-config/src/lib.rs` — a public `test_config` module and ~15 llama-only DEFAULT_* constants.
    - `crates/swissarmyhammer-config/tests/integration/` — `llama_config.rs`, `agent_config_file_loadings.rs`, `agent_hierarchical_configs.rs`, plus parts of `model_configs.rs` and `builtin_agents_generation.rs`.
    - `crates/swissarmyhammer-kanban/src/board/update.rs` — `validate_model_id` accepts `ClaudeCode | LlamaAgent`; 7 inline tests and `tests/per_board_model_isolation.rs` use `qwen` as their example chat model.
    - `crates/swissarmyhammer-tools` — `model_override` threads through `McpServer::new_with_work_dir`, `start_mcp_server`, `start_mcp_server_with_options`, `start_stdio_server`, `start_http_server`, `initialize_mcp_server`; ~30 call sites, most passing `None`.
    - `apps/kanban-app/src/state.rs` — one `start_mcp_server_with_options` call site.
    - `scripts/review-verify/drive.py` — runs `sah serve --model qwen`.
    - `ARCHITECTURE.md` and `doc/src/concepts/validators.md`.

    ### Design

    - New `ChatModelConfig { model: Option<String> }` in `swissarmyhammer-config`. No executor field. `model` IS the Claude CLI `--model` switch; `None` spawns plain `claude`.
    - `REVIEW_DEFAULT_CLAUDE_MODEL = "haiku"` replaces `REVIEW_DEFAULT_AGENT = "claude-code-haiku"`.
    - Precedence is unchanged, only the value's meaning changes: `review.model` → top-level `model:` → `haiku`.
    - `ModelConfig` / `ModelExecutorConfig` / `ModelExecutorType` become embedding-only.
    - `builtin/models/` keeps only the two embedding YAMLs; `claude-code.yaml` and `claude-code-haiku.yaml` go too, because the switch is now a config field.
    - `--model`, `extract_model_flag` and `model_override` are removed end to end.
  timestamp: 2026-08-03T02:04:00.228197+00:00
- actor: claude-code
  id: 01kz3t7a1vnnae1s4gjx4k3gs7
  text: |
    ### Implementation landed

    Followed the approved design. 81 files, +1397 / -6153.

    **The new chat configuration.** `ChatModelConfig { model: Option<String> }` in `crates/swissarmyhammer-config/src/model.rs`. No executor field. `model` IS the Claude CLI `--model` switch; `None` spawns plain `claude`. `claude_args()` is the single source of truth for the switch lookup, so the value a tool reports and the value the spawned process receives cannot drift. `REVIEW_DEFAULT_CLAUDE_MODEL = "haiku"` replaces `REVIEW_DEFAULT_AGENT = "claude-code-haiku"`.

    Precedence is unchanged; only the meaning of the value changed. `review.model` → top-level `model:` → `haiku`, applied by the pure `ModelManager::review_chat_model_from`. Resolvers: `resolve_chat_config`, `resolve_review_chat_config`, `resolve_review_chat_model`, plus `get_chat_model` / `get_review_chat_model` over a shared `read_model_key`, so both scopes read the file the same way and cannot disagree about a missing file, an absent key, or a non-string value.

    **The equivalent clear error.** With Claude hardcoded there is no unsupported executor to name, so `executor_config_name` had nothing left to report. The one way a chat configuration can still be wrong is a blank switch, which would spawn `claude --model ""` and fail deep inside the Claude CLI. That is now rejected in two places with a message that names the setting to fix: `chat_config_from_switch` at config-read time (naming `model` or `review.model`) and `create_agent_with_options` at the agent seam.

    **Embedding.** `ModelConfig` / `ModelExecutorConfig` / `ModelExecutorType` are now embedding-only. `crates/swissarmyhammer-embedding/src/embedder.rs` got the mechanical follow-up: its `other =>` arm became an unreachable pattern and `EmbedderError::NotAnEmbeddingModel` went dead, so both are gone along with the two tests that asserted a chat model is rejected. Nothing else in the embedding stack was touched — `git diff HEAD --stat` shows zero changes under `llama-common`, `llama-embedding`, `model-loader`, `ane-embedding`, `model-embedding`.

    **`--model` removed end to end.** `extract_model_flag`, both divergent flag definitions (`cli.rs` derive and `dynamic_cli.rs` `Arg`), and the `model_override` parameter through `initialize_tool_context` → `CliToolContext::new_with_config` (renamed `new_with_work_dir`) → `McpServer::new_with_work_dir` → `start_mcp_server` / `start_mcp_server_with_options` / `start_stdio_server` / `start_http_server` / `initialize_mcp_server`, about 30 call sites. `completions/*` and `doc/src/reference/sah-cli.md` were REGENERATED via `apps/swissarmyhammer-cli/build.rs`, not hand-edited.

    ### Decisions worth recording

    - **`builtin/models/` keeps only the two embedding YAMLs.** `claude-code.yaml` and `claude-code-haiku.yaml` went too — with the switch a configuration field, they no longer express anything. This is the second branch the acceptance criteria allow.
    - **The board's `model` field is now a Claude CLI switch.** `validate_model_id` in `crates/swissarmyhammer-kanban/src/board/update.rs` can no longer enumerate valid values (the Claude CLI owns that vocabulary, including full model ids), so it rejects a blank id and rejects any id that names a model in the library — every model YAML there is an embedding model, which cannot drive a chat agent. `test_update_board_rejects_unknown_model` became `test_update_board_rejects_blank_model`; the embedding-rejection test survives unchanged.
    - **`TemplateContext::get_agent_config` / `get_all_agent_configs` deleted.** They were the template-context half of the chat model YAML system (`agent.default`, `agent.configs.<workflow>`), with one production caller — a fallback in `server.rs` that now yields `ChatModelConfig::default()`. Their two integration test files went with them.
    - **`use_agent` / `use_agent_for` / `ModelTarget` deleted**, with `validate_agent`, `validate_agent_name_security`, `load_or_create_config`, `save_config`, `update_config_with_agent`, `check_file_readable`. They wrote a model NAME for the deleted `sah model` command and were actively wrong under a switch field: `validate_agent` → `find_agent_by_name` would only ever find embedding models.
    - **`ModelInfo.tags` / `parse_model_tags` deleted.** Nothing read the field; its own doc comment said it existed so the (deleted) kanban AI panel could filter chat models.
    - **`ChatModelConfig` carries no serde derives.** Verified by removing them and rebuilding the workspace: zero errors. The switch is read as a plain string from the config file, never deserialized as a struct — which is exactly the "configuration field, not a model-name lookup" the card asked for.

    ### What did not work

    The first sweep of the `model_override` call sites used a generic "drop the Nth argument" script over every file naming `start_mcp_server*`. It matched the function DEFINITIONS in `unified_server.rs` too and silently deleted their `working_dir` parameter. Caught by the next build. If someone repeats this, exclude the defining file or anchor the match on the call syntax.

    ### Discovered, filed separately

    `apps/kanban-cli/.skills/kanban/SKILL.md` still says "across both Claude Code and llama-agent sessions". Its source `builtin/skills/kanban/SKILL.md` is already correct — only the deployed copy is stale, and `.skills/` is generated, so the fix is a regeneration rather than a hand edit. Pre-existing drift from commit 5edda8286, filed as `^qg2h0ta`.

    ### Verification

    - `cargo fmt --all` — clean.
    - `cargo clippy --workspace --all-targets -- -D warnings` — exits 0, zero warnings.
    - `cargo nextest run --workspace` — 13337 passed, 0 failed, 0 skipped.
    - `builtin/models/` holds exactly `nomic-embed-code.yaml` and `qwen-embedding.yaml`.
    - Real-path embedding proof: `swissarmyhammer-kanban::search_tasks_e2e qwen_embedding_search_tasks_real_pipeline_e2e` PASSES — that drives `qwen-embedding` through `ModelManager` → `Embedder` → a real search, so the invariant this whole project was meant to preserve is proved by the production path, not by a fixture.
    - Real-path review proof: `review_resolved_default_spawns_claude_with_model_haiku`, `local_and_session_backends_resolve_the_same_review_model`, and `review_factory_from_resolved_default_carries_model_haiku` all PASS — an unconfigured scope resolves through `resolve_review_chat_config` and the production spawn seam carries `extra_args == ["--model", "haiku"]`.
  timestamp: 2026-08-03T12:40:19.259777+00:00
- actor: claude-code
  id: 01kz3tpxh8aw08m4g2yx9f373m
  text: |
    ### Double-check returned REVISE with 5 findings — all 5 implemented

    Ran an adversarial read-only verifier over the diff. It confirmed all four acceptance criteria and found no coverage regression from the deleted test files and no test weakened into a tautology. It raised five findings. Every one is fixed.

    **1. `--model` was not removed end to end.** `crates/swissarmyhammer-config/examples/show_rendered_test_prompt.rs` printed `cargo run -- --model GLM-4.7 test` — a deleted flag naming a deleted model, inside a string literal, so it compiles and clippy stays silent and it fails only for the person who follows the instruction. Now prints `cargo run -- test`. Swept the whole `examples/` directory; it was the only instance. My own earlier grep missed this because my pattern anchored on `"--model"` with quotes and this occurrence is bare text inside `println!`.

    **2. Dead re-export.** `crates/swissarmyhammer-embedding/src/lib.rs` re-exported `swissarmyhammer_config::ModelExecutorType`. Its last consumer was the `NotAnEmbeddingModel(String, ModelExecutorType)` error variant this change deleted. A `pub use` in a library is not dead code to the compiler, so nothing reported it. Deleted.

    **3. Dead constants.** `DEFAULT_QUIET_MODE` in `crates/swissarmyhammer-config/src/lib.rs` lost its only three consumers with the deleted `ModelConfig::claude_code()` and `ModelConfig::llama_agent()` constructors. Deleted. Removing the cause from the whole file also took its neighbour `DEFAULT_TEST_EMBEDDING_MODEL`, which had zero consumers and duplicated the live constant in `crates/swissarmyhammer-common/src/constants.rs` — that one was already dead at HEAD, and leaving a duplicate that can drift is the same defect.

    **4. Doc drift in `crates/swissarmyhammer-agent/src/lib.rs`.** Four regions still described the deleted two-executor world: the crate doc ("any other executor is rejected by name"), `review_agent_factory`'s doc and `create_claude_agent`'s doc (both naming `ModelConfig` where `ChatModelConfig` is meant — `ModelConfig` now names the embedding type), and the routing test's doc, which described a `ClaudeCode` match arm and an unsupported-executor error that no longer exist. All rewritten. Sweeping the same cause through the rest of the file also caught the test's own name and its two panic messages, which still called the configuration `claude-code`; the test is now `create_agent_routes_a_chat_configuration_to_the_claude_backend`.

    **5. Doc drift in `crates/swissarmyhammer-tools/src/mcp/tool_registry.rs`.** "the configured agent executor (ClaudeCode or LlamaAgent) ... create appropriate executor instances", directly above the field this change retyped. Already fixed before the verifier reported; confirmed in place.

    Then swept the whole tree for the same cause: every surviving `ModelConfig` outside the embedding type is `model_loader::ModelConfig`, an unrelated type in an off-limits crate; and `grep -rni "unsupported executor|executor arm|ClaudeCode arm|two-executor|agent executor|executor instances|rejected by name"` returns only one hit, a test-helper panic in `model_configs.rs` that correctly refers to an unsupported *embedding* executor.

    **Out of scope, confirmed by the verifier.** The `.skills/kanban/SKILL.md` llama-agent wording and the `llama-common` / `llama-embedding` / `model-loader` prose drift all come from commit 5edda8286 (HEAD), not this change. The skills drift is filed as `^qg2h0ta`; the rest is inside off-limits crates.

    ### Re-verification after the fixes

    - `cargo fmt --all` — clean.
    - `cargo clippy --workspace --all-targets -- -D warnings` — exits 0, zero warnings.
    - `cargo nextest run --workspace` — 13337 passed, 0 failed, 0 skipped.
    - `completions/*` and `doc/src/reference/sah-cli.md`: re-ran `apps/swissarmyhammer-cli/build.rs` and `git diff` over them is empty, so they are generated output in sync with the CLI definition, not hand edits.

    83 files, +1480 / -6196.

    ### implement — changed
    - evidence: 83 files, +1480 / -6196. `cargo clippy --workspace --all-targets -- -D warnings` exits 0 with zero warnings; `cargo nextest run --workspace` reports 13337 passed, 0 failed, 0 skipped. `builtin/models/` holds exactly `nomic-embed-code.yaml` and `qwen-embedding.yaml`. Real-path proofs: `qwen_embedding_search_tasks_real_pipeline_e2e` (embedding stack intact) and `review_resolved_default_spawns_claude_with_model_haiku` + `review_factory_from_resolved_default_carries_model_haiku` (review still spawns `claude --model haiku`).
    - next: ready for `/review`. Left in `doing`.
  timestamp: 2026-08-03T12:48:50.728956+00:00
- actor: claude-code
  id: 01kz3yadf0zj18zecxw2n83815
  text: |-
    ### test — green

    Independently re-verified (not taking the prior agent's word):

    **Gates**
    - `cargo fmt --all --check` — clean (0 diff), re-checked after all edits
    - `cargo clippy --workspace --all-targets -- -D warnings` — clean, re-checked after all edits
    - `cargo nextest run --workspace` — 13337 tests run: 13337 passed (0 failed), 0 skipped — run TWICE (before and after verification edits), both green
    - No `#[ignore]` tests anywhere in the workspace

    **1. Embedding still resolves** — `builtin/models/` holds only `nomic-embed-code.yaml` and `qwen-embedding.yaml`. `crates/{llama-common,llama-embedding,model-loader,ane-embedding,model-embedding}` show zero diff vs HEAD (both `git diff --stat` and `git status --short` confirm). `crates/swissarmyhammer-embedding/src/embedder.rs` diff is exactly the mechanical removal of the `NotAnEmbeddingModel` error variant + its `other => {...}` match arm (dead code once `ModelExecutorConfig` dropped the chat variants) and the tests that exercised it — no other change. Ran `qwen_embedding_search_tasks_real_pipeline_e2e` directly (`cargo nextest run -p swissarmyhammer-kanban --test search_tasks_e2e`) — PASS in 2.0s. Read the test: it builds a real board via `execute_operation`/`parse_input`, drives the real `search tasks` op, and asserts on the real `Embedder` (qwen-embedding) + `EmbeddingCache` — semantic ranking (non-zero cosine), lexical typo recovery (bm25/trigram), filter scoping, cache reuse (stable row count), and cold-start sidecar rebuild. Confirmed real-pipeline, not fixture-only.

    **2. Review still runs haiku** — `review_resolved_default_spawns_claude_with_model_haiku`, `review_factory_from_resolved_default_carries_model_haiku`, `local_and_session_review_runs_resolve_the_same_model`, `local_and_session_backends_resolve_the_same_review_model` all PASS. Perturbed `REVIEW_DEFAULT_CLAUDE_MODEL` from `"haiku"` to a bogus sentinel: all 4 tests FAIL with a clear left/right mismatch (e.g. `left: ["--model", "PERTURBED_FOR_TEST_VERIFICATION"] right: ["--model", "haiku"]`), then reverted (git diff confirms clean revert) and reran — green again. Non-vacuous, confirmed. Precedence test `review_chat_model_precedence` in `crates/swissarmyhammer-config/src/model.rs` explicitly asserts all three levels: `review.model` wins over `model:`, `model:` wins when `review.model` unset, and the baked-in `haiku` wins when both unset.

    **3. No drift between reported and spawned model** — read the code path: `apps/swissarmyhammer-cli/src/commands/serve/mod.rs::review_model_config` resolves via `ModelManager::resolve_review_chat_config`, logs `config.claude_args()` via `tracing::info!`, and passes the SAME `Arc<ChatModelConfig>` into `review_agent_factory`. That factory's build chain (`create_agent_with_options` → `claude_agent_config_from_model`) calls `config.claude_args()` again — the identical method on the identical value — to build `extra_args` that land on the spawned `claude` argv. `ChatModelConfig::claude_args()` is the only place that turns the config into CLI switches; grepped for any other `--model` argument construction or independent model-name reporting and found none. Confirmed single source of truth.

    **4. String-literal leftovers** — swept `grep -rn -- "--model"` and `grep -rnE "qwen|GLM|MiniMax|claude-code-haiku|claude-code\b"` across source/docs/scripts. Remaining hits are all legitimate: current-design docs (`scripts/review-verify/README.md`, `doc/src/concepts/validators.md`), the intentional regression test `apps/swissarmyhammer-cli/tests/integration/model_command_removed.rs` (asserts `--model` is now rejected), current test assertions in `serve/mod.rs`, and one unrelated `claude-code` agent-id reference in `doc/src/reference/mirdan-cli.md` (mirdan's per-agent installer targeting, not the removed chat executor concept). Confirmed `scripts/review-verify/drive.py` no longer references `--model qwen` — README now says `sah serve --cwd sample/`. Grepped for `extract_model_flag`/`model_override` symbols specifically — zero hits (only false-positive substring matches on `apply_model_overrides` and a test named `..._user_model_override`).

    **5. Generated files** — `touch`ed `apps/swissarmyhammer-cli/build.rs` and ran `cargo build -p swissarmyhammer-cli` to force `doc_gen::generate_completions`/`generate_markdown_with_brew` to rerun. `git diff --stat` on `completions/*` and `doc/src/reference/sah-cli.md` before and after the forced rebuild is identical (no new diff introduced) — the already-uncommitted content matches a fresh generation exactly.

    No failures found, no fixes needed. Scope was verification only; no code changes retained (the perturbation to `REVIEW_DEFAULT_CLAUDE_MODEL` was reverted).
  timestamp: 2026-08-03T13:51:55.360168+00:00
depends_on:
- 01KZ23NHE9JR2WAMMQP3Y5N9G6
position_column: doing
position_ordinal: '8380'
project: drop-llama-agent
title: Collapse the chat model configuration to a hardcoded Claude executor
---
## What

The chat side no longer chooses an executor, so remove the machinery that
chose one. Claude becomes hardcoded. The haiku-versus-default choice stays,
but as a plain configuration field, not a model-name lookup.

- Delete the llama-executor model YAMLs from `builtin/models/`: `qwen.yaml`,
  `qwen-0.6b-test.yaml`, `qwen-0.8b-mtp-test.yaml`, `GLM.yaml`,
  `MiniMax-2.5.yaml`.
- KEEP the embedding model YAMLs `nomic-embed-code.yaml` and
  `qwen-embedding.yaml`. The embedding stack still resolves them through
  `ModelManager`, so the loader must keep working for those.
- Remove the executor field and the executor enum from the chat model
  configuration in `crates/swissarmyhammer-config/src/model.rs`. Claude is the
  only chat executor.
- Replace the review model-name resolution: `resolve_review_agent_name`
  currently maps the review scope to `claude-code-haiku` by name. Turn that
  into a boolean-or-enum configuration field that picks the Claude CLI model
  switch directly, so `claude-code.yaml` and `claude-code-haiku.yaml` are no
  longer needed to express the choice.
- Remove `extract_model_flag` and the `--model` global flag from
  `apps/swissarmyhammer-cli/src/main.rs` (line 290) and the
  `model_override` parameter it feeds into `initialize_tool_context`
  (line 310), unless the new configuration field needs a flag — in which case
  rename it to match.

Find the remaining sites with
`grep -rn "resolve_review_agent_name\|claude-code-haiku\|executor" crates/swissarmyhammer-config/ crates/swissarmyhammer-agent/ apps/swissarmyhammer-cli/`.

### Subtasks

- [x] Delete the five llama-executor model YAMLs.
- [x] Remove the executor field and enum from the chat model configuration.
- [x] Replace review model-name resolution with a configuration field.
- [x] Resolve the `--model` flag: remove it, or rename it to the new field.

## Acceptance Criteria

- [x] `builtin/models/` holds only `claude-code.yaml`,
      `claude-code-haiku.yaml`, `nomic-embed-code.yaml`, and
      `qwen-embedding.yaml` — or only the two embedding YAMLs if the Claude
      configs also become unnecessary.
- [x] The chat model configuration type has no executor field.
- [x] Review still runs against the Claude haiku model, chosen through the new
      configuration field, and `sah doctor` reports the same model it runs.
- [x] `cargo clippy --workspace --all-targets -- -D warnings` exits 0 with
      zero warnings.

## Tests

- [x] Add a unit test in `crates/swissarmyhammer-config/` that loads the
      builtin models and asserts the two embedding YAMLs still resolve through
      `ModelManager` — this proves the collapse did not break the embedding
      path.
- [x] Update or replace the existing review model-resolution test so it
      asserts the review scope resolves to the Claude haiku switch through the
      new configuration field. Find it with
      `grep -rn "resolve_review_agent_name" --include=*.rs crates/`.
- [x] Run `cargo nextest run --workspace` — the whole suite passes.

## Workflow

- Use `/tdd` — write the embedding-still-resolves test and the review-model
  test first.

## Notes

- `builtin/models/` took the second branch of the first acceptance criterion:
  only the two embedding YAMLs remain. With the switch a plain configuration
  field, `claude-code.yaml` and `claude-code-haiku.yaml` express nothing.
- `sah doctor` reports no model at all — no check in
  `crates/swissarmyhammer-doctor/` or the CLI doctor command mentions one — so
  there is no doctor/runtime disagreement to reintroduce. A single resolver
  (`ModelManager::resolve_review_chat_config`) feeds both the logged decision
  point and the spawned process, which keeps that property by construction.
#llama-agent #config #cleanup
{"id":"01KTCQF7J3NNN0QA227H844NZW","timestamp":"2026-06-05T20:26:53.891217Z","op":"create","item_id":"01KTCQF7J3YZ1SAY0W96WWA35X","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,10 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: c280\n+title: Jump (s) cannot target the (i) inspect and (x) close buttons\n+---\n+The (i) inspect button (entity-card.tsx InspectButton, moniker `card.inspect:{id}`) and the inspector (x) close button are `<Pressable>` focusables, but the Jump-To overlay's `useJumpTargets` filters to TOP-TIER focusables only (jump-to-overlay.tsx `isTopTierFocusable`, ~line 665): a scope is excluded if it has any focusable ancestor. The inspect/close buttons are nested inside the card / inspector panel (which are themselves focusable), so they are excluded by design (\"nested focusables reached by drill-in Enter, not jump\").\n+\n+User wants these action buttons to be jump targets. Decide: relax the tier filter to include action-type nested focusables (e.g. a `jumpable`/action flag on Pressable), or surface them another way. Both (i)=inspect and (x)=close must be jumpable AND perform their action.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,10 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: c280\n-title: Jump (s) cannot target the (i) inspect and (x) close buttons\n----\n-The (i) inspect button (entity-card.tsx InspectButton, moniker `card.inspect:{id}`) and the inspector (x) close button are `<Pressable>` focusables, but the Jump-To overlay's `useJumpTargets` filters to TOP-TIER focusables only (jump-to-overlay.tsx `isTopTierFocusable`, ~line 665): a scope is excluded if it has any focusable ancestor. The inspect/close buttons are nested inside the card / inspector panel (which are themselves focusable), so they are excluded by design (\"nested focusables reached by drill-in Enter, not jump\").\n-\n-User wants these action buttons to be jump targets. Decide: relax the tier filter to include action-type nested focusables (e.g. a `jumpable`/action flag on Pressable), or surface them another way. Both (i)=inspect and (x)=close must be jumpable AND perform their action.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTD93YFJ8DZ4EKYHDK9RS5S9","timestamp":"2026-06-06T01:35:18.514814Z","op":"update","item_id":"01KTCQF7J3YZ1SAY0W96WWA35X","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: c280\n+position_column: doing\n+position_ordinal: '80'\n title: Jump (s) cannot target the (i) inspect and (x) close buttons\n ---\n The (i) inspect button (entity-card.tsx InspectButton, moniker `card.inspect:{id}`) and the inspector (x) close button are `<Pressable>` focusables, but the Jump-To overlay's `useJumpTargets` filters to TOP-TIER focusables only (jump-to-overlay.tsx `isTopTierFocusable`, ~line 665): a scope is excluded if it has any focusable ancestor. The inspect/close buttons are nested inside the card / inspector panel (which are themselves focusable), so they are excluded by design (\"nested focusables reached by drill-in Enter, not jump\").\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: c280\n title: Jump (s) cannot target the (i) inspect and (x) close buttons\n ---\n The (i) inspect button (entity-card.tsx InspectButton, moniker `card.inspect:{id}`) and the inspector (x) close button are `<Pressable>` focusables, but the Jump-To overlay's `useJumpTargets` filters to TOP-TIER focusables only (jump-to-overlay.tsx `isTopTierFocusable`, ~line 665): a scope is excluded if it has any focusable ancestor. The inspect/close buttons are nested inside the card / inspector panel (which are themselves focusable), so they are excluded by design (\"nested focusables reached by drill-in Enter, not jump\").\n","transaction_id":null}
{"id":"01KTDAAWNEYN16TBH7K467KJPW","timestamp":"2026-06-06T01:56:34.606718Z","op":"update","item_id":"01KTCQF7J3YZ1SAY0W96WWA35X","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8580'\n title: Jump (s) cannot target the (i) inspect and (x) close buttons\n ---\n The (i) inspect button (entity-card.tsx InspectButton, moniker `card.inspect:{id}`) and the inspector (x) close button are `<Pressable>` focusables, but the Jump-To overlay's `useJumpTargets` filters to TOP-TIER focusables only (jump-to-overlay.tsx `isTopTierFocusable`, ~line 665): a scope is excluded if it has any focusable ancestor. The inspect/close buttons are nested inside the card / inspector panel (which are themselves focusable), so they are excluded by design (\"nested focusables reached by drill-in Enter, not jump\").\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8580'\n+position_column: doing\n+position_ordinal: '80'\n title: Jump (s) cannot target the (i) inspect and (x) close buttons\n ---\n The (i) inspect button (entity-card.tsx InspectButton, moniker `card.inspect:{id}`) and the inspector (x) close button are `<Pressable>` focusables, but the Jump-To overlay's `useJumpTargets` filters to TOP-TIER focusables only (jump-to-overlay.tsx `isTopTierFocusable`, ~line 665): a scope is excluded if it has any focusable ancestor. The inspect/close buttons are nested inside the card / inspector panel (which are themselves focusable), so they are excluded by design (\"nested focusables reached by drill-in Enter, not jump\").\n","transaction_id":null}
{"id":"01KTEB9ANF40V37MQBHSC7WQQR","timestamp":"2026-06-06T11:32:26.415364Z","op":"update","item_id":"01KTCQF7J3YZ1SAY0W96WWA35X","forward_patch":"--- original\n+++ modified\n@@ -5,6 +5,10 @@\n position_ordinal: '8580'\n title: Jump (s) cannot target the (i) inspect and (x) close buttons\n ---\n-The (i) inspect button (entity-card.tsx InspectButton, moniker `card.inspect:{id}`) and the inspector (x) close button are `<Pressable>` focusables, but the Jump-To overlay's `useJumpTargets` filters to TOP-TIER focusables only (jump-to-overlay.tsx `isTopTierFocusable`, ~line 665): a scope is excluded if it has any focusable ancestor. The inspect/close buttons are nested inside the card / inspector panel (which are themselves focusable), so they are excluded by design (\"nested focusables reached by drill-in Enter, not jump\").\n+REOPENED 2026-06-06 — prior fix was WRONG and discarded.\n+\n+## OWNER CORRECTION (authoritative)\n+Do NOT add a `jumpable` flag. We did NOT need one before and jump WORKED. So the (i) inspect / (x) close buttons not being jump targets is a REGRESSION to find and restore — not a missing opt-in to invent. The discarded approach added a `jumpable` prop threaded through Pressable/FocusScope/layer-scope-registry/spatial-focus-context and relaxed the `useJumpTargets` top-tier filter to honor it. All reverted to HEAD.\n\n-User wants these action buttons to be jump targets. Decide: relax the tier filter to include action-type nested focusables (e.g. a `jumpable`/action flag on Pressable), or surface them another way. Both (i)=inspect and (x)=close must be jumpable AND perform their action.\n\\ No newline at end of file\n+## Next step\n+Find what CHANGED that stopped these buttons (and the nav-bar (i) inspect, #bug 01KTCRQGEXVKG0BZTF6KGE3Y35) from being jump targets, and restore the prior working behavior — without introducing a new flag. Investigate the jump-target enumeration (`jump-to-overlay.tsx::useJumpTargets` / `isTopTierFocusable`) and the focus-scope/rect registration history; determine why these focusable leaves used to be enumerated and no longer are. (Note: there was also a real `startsWith of undefined` crash in the jump activation path at jump-to-overlay.tsx ~line 493 worth fixing, but NOT via a jumpable flag.) TDD, RED first.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -5,10 +5,6 @@\n position_ordinal: '8580'\n title: Jump (s) cannot target the (i) inspect and (x) close buttons\n ---\n-REOPENED 2026-06-06 — prior fix was WRONG and discarded.\n+The (i) inspect button (entity-card.tsx InspectButton, moniker `card.inspect:{id}`) and the inspector (x) close button are `<Pressable>` focusables, but the Jump-To overlay's `useJumpTargets` filters to TOP-TIER focusables only (jump-to-overlay.tsx `isTopTierFocusable`, ~line 665): a scope is excluded if it has any focusable ancestor. The inspect/close buttons are nested inside the card / inspector panel (which are themselves focusable), so they are excluded by design (\"nested focusables reached by drill-in Enter, not jump\").\n\n-## OWNER CORRECTION (authoritative)\n-Do NOT add a `jumpable` flag. We did NOT need one before and jump WORKED. So the (i) inspect / (x) close buttons not being jump targets is a REGRESSION to find and restore — not a missing opt-in to invent. The discarded approach added a `jumpable` prop threaded through Pressable/FocusScope/layer-scope-registry/spatial-focus-context and relaxed the `useJumpTargets` top-tier filter to honor it. All reverted to HEAD.\n-\n-## Next step\n-Find what CHANGED that stopped these buttons (and the nav-bar (i) inspect, #bug 01KTCRQGEXVKG0BZTF6KGE3Y35) from being jump targets, and restore the prior working behavior — without introducing a new flag. Investigate the jump-target enumeration (`jump-to-overlay.tsx::useJumpTargets` / `isTopTierFocusable`) and the focus-scope/rect registration history; determine why these focusable leaves used to be enumerated and no longer are. (Note: there was also a real `startsWith of undefined` crash in the jump activation path at jump-to-overlay.tsx ~line 493 worth fixing, but NOT via a jumpable flag.) TDD, RED first.\n\\ No newline at end of file\n+User wants these action buttons to be jump targets. Decide: relax the tier filter to include action-type nested focusables (e.g. a `jumpable`/action flag on Pressable), or surface them another way. Both (i)=inspect and (x)=close must be jumpable AND perform their action.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTEB9NTW9REDHNXBRJNTESZ8","timestamp":"2026-06-06T11:32:37.852894Z","op":"update","item_id":"01KTCQF7J3YZ1SAY0W96WWA35X","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8580'\n+position_column: todo\n+position_ordinal: d080\n title: Jump (s) cannot target the (i) inspect and (x) close buttons\n ---\n REOPENED 2026-06-06 — prior fix was WRONG and discarded.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: d080\n+position_column: review\n+position_ordinal: '8580'\n title: Jump (s) cannot target the (i) inspect and (x) close buttons\n ---\n REOPENED 2026-06-06 — prior fix was WRONG and discarded.\n","transaction_id":null}
{"id":"01KTED3YW1HR01KKJAEZJDC7VV","timestamp":"2026-06-06T12:04:27.649993Z","op":"update","item_id":"01KTCQF7J3YZ1SAY0W96WWA35X","forward_patch":"--- original\n+++ modified\n@@ -11,4 +11,12 @@\n Do NOT add a `jumpable` flag. We did NOT need one before and jump WORKED. So the (i) inspect / (x) close buttons not being jump targets is a REGRESSION to find and restore — not a missing opt-in to invent. The discarded approach added a `jumpable` prop threaded through Pressable/FocusScope/layer-scope-registry/spatial-focus-context and relaxed the `useJumpTargets` top-tier filter to honor it. All reverted to HEAD.\n\n ## Next step\n-Find what CHANGED that stopped these buttons (and the nav-bar (i) inspect, #bug 01KTCRQGEXVKG0BZTF6KGE3Y35) from being jump targets, and restore the prior working behavior — without introducing a new flag. Investigate the jump-target enumeration (`jump-to-overlay.tsx::useJumpTargets` / `isTopTierFocusable`) and the focus-scope/rect registration history; determine why these focusable leaves used to be enumerated and no longer are. (Note: there was also a real `startsWith of undefined` crash in the jump activation path at jump-to-overlay.tsx ~line 493 worth fixing, but NOT via a jumpable flag.) TDD, RED first.\n\\ No newline at end of file\n+Find what CHANGED that stopped these buttons (and the nav-bar (i) inspect) from being jump targets, and restore the prior working behavior — without introducing a new flag. Investigate the jump-target enumeration (`jump-to-overlay.tsx::useJumpTargets` / `isTopTierFocusable`) and the focus-scope/rect registration history; determine why these focusable leaves used to be enumerated and no longer are. (Note: there was also a real `startsWith of undefined` crash in the jump activation path at jump-to-overlay.tsx ~line 493 worth fixing, but NOT via a jumpable flag.) TDD, RED first.\n+\n+## Learnings folded in from duplicate (was #bug 01KTCRQGEXVKG0BZTF6KGE3Y35, now deleted — this card subsumes the nav-bar (i) inspect case)\n+- The nav-bar (i) inspect button registers as a peer focus leaf `<Pressable moniker=\"ui:navbar.inspect\">` at `apps/kanban-app/ui/src/components/nav-bar.tsx:107`, sibling to `ui:left-nav` / `ui:perspective-bar` under `<FocusLayer name=\"window\">`. It only renders when a board is open.\n+- Bisect repro for the enumeration regression: trigger jump and check whether `ui:navbar.search` (the adjacent search button) gets a sneak code while `ui:navbar.inspect` does NOT.\n+  - Both missing → the navbar leaves aren't enumerated into the jump layer at all (layer-set / enumeration scope problem).\n+  - Only inspect missing → per-leaf rect/filter problem.\n+- Three hypotheses for why these leaves dropped out of `useJumpTargets`: (a) the leaf publishes a zero/absent rect to the kernel, (b) it's filtered out by the top-tier filter (`isTopTierFocusable`), (c) the navbar leaves' layer isn't in the set `useJumpTargets` reads.\n+- Suggested tests: `apps/kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx` and `apps/kanban-app/ui/src/components/jump-to-overlay.*.test.tsx` — assert `ui:navbar.inspect` (and the (x) close) are enumerated jump targets when a board is open.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -11,12 +11,4 @@\n Do NOT add a `jumpable` flag. We did NOT need one before and jump WORKED. So the (i) inspect / (x) close buttons not being jump targets is a REGRESSION to find and restore — not a missing opt-in to invent. The discarded approach added a `jumpable` prop threaded through Pressable/FocusScope/layer-scope-registry/spatial-focus-context and relaxed the `useJumpTargets` top-tier filter to honor it. All reverted to HEAD.\n\n ## Next step\n-Find what CHANGED that stopped these buttons (and the nav-bar (i) inspect) from being jump targets, and restore the prior working behavior — without introducing a new flag. Investigate the jump-target enumeration (`jump-to-overlay.tsx::useJumpTargets` / `isTopTierFocusable`) and the focus-scope/rect registration history; determine why these focusable leaves used to be enumerated and no longer are. (Note: there was also a real `startsWith of undefined` crash in the jump activation path at jump-to-overlay.tsx ~line 493 worth fixing, but NOT via a jumpable flag.) TDD, RED first.\n-\n-## Learnings folded in from duplicate (was #bug 01KTCRQGEXVKG0BZTF6KGE3Y35, now deleted — this card subsumes the nav-bar (i) inspect case)\n-- The nav-bar (i) inspect button registers as a peer focus leaf `<Pressable moniker=\"ui:navbar.inspect\">` at `apps/kanban-app/ui/src/components/nav-bar.tsx:107`, sibling to `ui:left-nav` / `ui:perspective-bar` under `<FocusLayer name=\"window\">`. It only renders when a board is open.\n-- Bisect repro for the enumeration regression: trigger jump and check whether `ui:navbar.search` (the adjacent search button) gets a sneak code while `ui:navbar.inspect` does NOT.\n-  - Both missing → the navbar leaves aren't enumerated into the jump layer at all (layer-set / enumeration scope problem).\n-  - Only inspect missing → per-leaf rect/filter problem.\n-- Three hypotheses for why these leaves dropped out of `useJumpTargets`: (a) the leaf publishes a zero/absent rect to the kernel, (b) it's filtered out by the top-tier filter (`isTopTierFocusable`), (c) the navbar leaves' layer isn't in the set `useJumpTargets` reads.\n-- Suggested tests: `apps/kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx` and `apps/kanban-app/ui/src/components/jump-to-overlay.*.test.tsx` — assert `ui:navbar.inspect` (and the (x) close) are enumerated jump targets when a board is open.\n\\ No newline at end of file\n+Find what CHANGED that stopped these buttons (and the nav-bar (i) inspect, #bug 01KTCRQGEXVKG0BZTF6KGE3Y35) from being jump targets, and restore the prior working behavior — without introducing a new flag. Investigate the jump-target enumeration (`jump-to-overlay.tsx::useJumpTargets` / `isTopTierFocusable`) and the focus-scope/rect registration history; determine why these focusable leaves used to be enumerated and no longer are. (Note: there was also a real `startsWith of undefined` crash in the jump activation path at jump-to-overlay.tsx ~line 493 worth fixing, but NOT via a jumpable flag.) TDD, RED first.\n\\ No newline at end of file\n","transaction_id":null}

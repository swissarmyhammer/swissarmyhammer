{"id":"01KSSMQ5XFA86B3FVBY69GZ6SQ","timestamp":"2026-05-29T10:33:14.415099Z","op":"create","item_id":"01KSSMQ5XEEY2QQHMD28RC9NSF","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,26 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '80'\n+project: ai-panel\n+title: 'Best-performance llama context params on Metal: flash attention + quantized KV cache'\n+---\n+## Why\n+The llama-agent generation context (`crates/llama-agent/src/model.rs` ~line 364) builds `LlamaContextParams::default()` setting only n_ctx/n_batch/n_ubatch/n_threads. It does NOT enable flash attention or quantize the KV cache, so the KV cache is F16 — a huge allocation at the model's native 262k context on the 27B. The fork exposes `with_flash_attention_policy`, `with_type_k`/`with_type_v` (KvCacheType::{Q8_0, Q4_0, ...}), and `with_offload_kqv`.\n+\n+## What\n+Pick the best-performance settings for Metal models and apply them in the generation context:\n+- Force flash attention ON (prerequisite for quantized V cache, big win on Metal). `with_flash_attention_policy(ENABLED)`.\n+- Quantize the KV cache: `with_type_k`/`with_type_v` = Q8_0 by default (near-lossless, ~½ KV memory). Allow an env/config override (e.g. LLAMA_KV_CACHE_TYPE) for f16/q4_0 escape.\n+- Ensure `offload_kqv` is on (GPU).\n+- Keep the existing n_ctx/batch derivation. (n_ctx capping is a separate, larger memory lever — out of scope here unless requested.)\n+\n+Derive from the model config / existing env-override pattern (mirror `LLAMA_N_GPU_LAYERS`).\n+\n+## Acceptance Criteria\n+- [ ] Generation context enables flash attention and a quantized (Q8_0 default) KV cache.\n+- [ ] An escape hatch exists to fall back to F16 KV / different quant (env or config).\n+- [ ] The macOS Metal test (or a sibling) asserts from the load logs that flash attention is enabled AND the KV cache type is the quantized type (not f16).\n+- [ ] All layers still offload to Metal; existing real-model tests still pass.\n+- [ ] No quality regression in the real-model tool/streaming tests.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,26 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '80'\n-project: ai-panel\n-title: 'Best-performance llama context params on Metal: flash attention + quantized KV cache'\n----\n-## Why\n-The llama-agent generation context (`crates/llama-agent/src/model.rs` ~line 364) builds `LlamaContextParams::default()` setting only n_ctx/n_batch/n_ubatch/n_threads. It does NOT enable flash attention or quantize the KV cache, so the KV cache is F16 — a huge allocation at the model's native 262k context on the 27B. The fork exposes `with_flash_attention_policy`, `with_type_k`/`with_type_v` (KvCacheType::{Q8_0, Q4_0, ...}), and `with_offload_kqv`.\n-\n-## What\n-Pick the best-performance settings for Metal models and apply them in the generation context:\n-- Force flash attention ON (prerequisite for quantized V cache, big win on Metal). `with_flash_attention_policy(ENABLED)`.\n-- Quantize the KV cache: `with_type_k`/`with_type_v` = Q8_0 by default (near-lossless, ~½ KV memory). Allow an env/config override (e.g. LLAMA_KV_CACHE_TYPE) for f16/q4_0 escape.\n-- Ensure `offload_kqv` is on (GPU).\n-- Keep the existing n_ctx/batch derivation. (n_ctx capping is a separate, larger memory lever — out of scope here unless requested.)\n-\n-Derive from the model config / existing env-override pattern (mirror `LLAMA_N_GPU_LAYERS`).\n-\n-## Acceptance Criteria\n-- [ ] Generation context enables flash attention and a quantized (Q8_0 default) KV cache.\n-- [ ] An escape hatch exists to fall back to F16 KV / different quant (env or config).\n-- [ ] The macOS Metal test (or a sibling) asserts from the load logs that flash attention is enabled AND the KV cache type is the quantized type (not f16).\n-- [ ] All layers still offload to Metal; existing real-model tests still pass.\n-- [ ] No quality regression in the real-model tool/streaming tests.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSSMV2RDDM8GBMAMM09HNQJ8","timestamp":"2026-05-29T10:35:22.253822Z","op":"update","item_id":"01KSSMQ5XEEY2QQHMD28RC9NSF","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n+position_column: doing\n position_ordinal: '80'\n project: ai-panel\n title: 'Best-performance llama context params on Metal: flash attention + quantized KV cache'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: todo\n position_ordinal: '80'\n project: ai-panel\n title: 'Best-performance llama context params on Metal: flash attention + quantized KV cache'\n","transaction_id":null}
{"id":"01KSSNVHWQP018ZSAQS2HYNBD0","timestamp":"2026-05-29T10:53:06.327665Z","op":"update","item_id":"01KSSMQ5XEEY2QQHMD28RC9NSF","forward_patch":"--- original\n+++ modified\n@@ -6,21 +6,31 @@\n project: ai-panel\n title: 'Best-performance llama context params on Metal: flash attention + quantized KV cache'\n ---\n-## Why\n-The llama-agent generation context (`crates/llama-agent/src/model.rs` ~line 364) builds `LlamaContextParams::default()` setting only n_ctx/n_batch/n_ubatch/n_threads. It does NOT enable flash attention or quantize the KV cache, so the KV cache is F16 — a huge allocation at the model's native 262k context on the 27B. The fork exposes `with_flash_attention_policy`, `with_type_k`/`with_type_v` (KvCacheType::{Q8_0, Q4_0, ...}), and `with_offload_kqv`.\n+## DONE (2026-05-29)\n+\n+Best-performance context params now applied to every llama model in `ModelManager::create_context` (`crates/llama-agent/src/model.rs`):\n+- **Flash attention ENABLED** (`with_flash_attention_policy(LLAMA_FLASH_ATTN_TYPE_ENABLED)`) — big win on Metal and required for a quantized V cache.\n+- **Quantized KV cache Q8_0** for both K and V (`with_type_k`/`with_type_v(KvCacheType::Q8_0)`) — near-lossless, ~half the F16 KV memory.\n+\n+### No env vars (per user)\n+An earlier cut used `LLAMA_KV_CACHE_TYPE` / `LLAMA_FLASH_ATTN` env overrides; removed. These are now baked best-Metal defaults, not env/user knobs — the model config drives everything else about the load. (Per-model YAML override would require threading two fields across swissarmyhammer-config `LlmModelConfig` → swissarmyhammer-agent conversion → `llama_agent::types::ModelConfig` and updating ~71 `ModelConfig {…}` literals; deferred unless wanted.)\n\n-## What\n-Pick the best-performance settings for Metal models and apply them in the generation context:\n-- Force flash attention ON (prerequisite for quantized V cache, big win on Metal). `with_flash_attention_policy(ENABLED)`.\n-- Quantize the KV cache: `with_type_k`/`with_type_v` = Q8_0 by default (near-lossless, ~½ KV memory). Allow an env/config override (e.g. LLAMA_KV_CACHE_TYPE) for f16/q4_0 escape.\n-- Ensure `offload_kqv` is on (GPU).\n-- Keep the existing n_ctx/batch derivation. (n_ctx capping is a separate, larger memory lever — out of scope here unless requested.)\n+### Plumbing\n+- Added `llama-cpp-sys-2` (same git as llama-cpp-2, Cargo unifies to one build) to name `LLAMA_FLASH_ATTN_TYPE_ENABLED`.\n+- Imported `KvCacheType`.\n\n-Derive from the model config / existing env-override pattern (mirror `LLAMA_N_GPU_LAYERS`).\n+### Verification (proven, not guessed)\n+- `metal_gpu` test now creates a context inside the stderr-capture window (FA/KV are set at context creation, not model load) and asserts on the real llama.cpp log lines:\n+  - `llama_context: flash_attn = enabled`\n+  - `llama_kv_cache: … k (q8_0): … mib, v (q8_0): … mib`\n+  plus the existing all-layers-offloaded-to-GPU assertion. Passes.\n+- Real-model `acp_single_turn` still generates tokens + clean stream with the new context params — no quality regression.\n+- fmt clean, clippy 0, `cargo check -p kanban-app` green (sys dep unified, app builds).\n+- Skip message uses `tracing::warn!` (not eprintln), per convention.\n\n-## Acceptance Criteria\n-- [ ] Generation context enables flash attention and a quantized (Q8_0 default) KV cache.\n-- [ ] An escape hatch exists to fall back to F16 KV / different quant (env or config).\n-- [ ] The macOS Metal test (or a sibling) asserts from the load logs that flash attention is enabled AND the KV cache type is the quantized type (not f16).\n-- [ ] All layers still offload to Metal; existing real-model tests still pass.\n-- [ ] No quality regression in the real-model tool/streaming tests.\n\\ No newline at end of file\n+### Acceptance criteria\n+- [x] Generation context enables flash attention + quantized (Q8_0) KV cache.\n+- [~] Escape hatch: dropped per \"no env vars\". Defaults are the best-Metal values; per-model YAML override is the deferred follow-up.\n+- [x] macOS Metal test asserts FA enabled AND KV cache type is q8_0 (both sides), from the load logs.\n+- [x] All layers still offload to Metal; real-model tests still pass.\n+- [x] No quality regression in real-model generation.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -6,31 +6,21 @@\n project: ai-panel\n title: 'Best-performance llama context params on Metal: flash attention + quantized KV cache'\n ---\n-## DONE (2026-05-29)\n+## Why\n+The llama-agent generation context (`crates/llama-agent/src/model.rs` ~line 364) builds `LlamaContextParams::default()` setting only n_ctx/n_batch/n_ubatch/n_threads. It does NOT enable flash attention or quantize the KV cache, so the KV cache is F16 — a huge allocation at the model's native 262k context on the 27B. The fork exposes `with_flash_attention_policy`, `with_type_k`/`with_type_v` (KvCacheType::{Q8_0, Q4_0, ...}), and `with_offload_kqv`.\n\n-Best-performance context params now applied to every llama model in `ModelManager::create_context` (`crates/llama-agent/src/model.rs`):\n-- **Flash attention ENABLED** (`with_flash_attention_policy(LLAMA_FLASH_ATTN_TYPE_ENABLED)`) — big win on Metal and required for a quantized V cache.\n-- **Quantized KV cache Q8_0** for both K and V (`with_type_k`/`with_type_v(KvCacheType::Q8_0)`) — near-lossless, ~half the F16 KV memory.\n+## What\n+Pick the best-performance settings for Metal models and apply them in the generation context:\n+- Force flash attention ON (prerequisite for quantized V cache, big win on Metal). `with_flash_attention_policy(ENABLED)`.\n+- Quantize the KV cache: `with_type_k`/`with_type_v` = Q8_0 by default (near-lossless, ~½ KV memory). Allow an env/config override (e.g. LLAMA_KV_CACHE_TYPE) for f16/q4_0 escape.\n+- Ensure `offload_kqv` is on (GPU).\n+- Keep the existing n_ctx/batch derivation. (n_ctx capping is a separate, larger memory lever — out of scope here unless requested.)\n\n-### No env vars (per user)\n-An earlier cut used `LLAMA_KV_CACHE_TYPE` / `LLAMA_FLASH_ATTN` env overrides; removed. These are now baked best-Metal defaults, not env/user knobs — the model config drives everything else about the load. (Per-model YAML override would require threading two fields across swissarmyhammer-config `LlmModelConfig` → swissarmyhammer-agent conversion → `llama_agent::types::ModelConfig` and updating ~71 `ModelConfig {…}` literals; deferred unless wanted.)\n+Derive from the model config / existing env-override pattern (mirror `LLAMA_N_GPU_LAYERS`).\n\n-### Plumbing\n-- Added `llama-cpp-sys-2` (same git as llama-cpp-2, Cargo unifies to one build) to name `LLAMA_FLASH_ATTN_TYPE_ENABLED`.\n-- Imported `KvCacheType`.\n-\n-### Verification (proven, not guessed)\n-- `metal_gpu` test now creates a context inside the stderr-capture window (FA/KV are set at context creation, not model load) and asserts on the real llama.cpp log lines:\n-  - `llama_context: flash_attn = enabled`\n-  - `llama_kv_cache: … k (q8_0): … mib, v (q8_0): … mib`\n-  plus the existing all-layers-offloaded-to-GPU assertion. Passes.\n-- Real-model `acp_single_turn` still generates tokens + clean stream with the new context params — no quality regression.\n-- fmt clean, clippy 0, `cargo check -p kanban-app` green (sys dep unified, app builds).\n-- Skip message uses `tracing::warn!` (not eprintln), per convention.\n-\n-### Acceptance criteria\n-- [x] Generation context enables flash attention + quantized (Q8_0) KV cache.\n-- [~] Escape hatch: dropped per \"no env vars\". Defaults are the best-Metal values; per-model YAML override is the deferred follow-up.\n-- [x] macOS Metal test asserts FA enabled AND KV cache type is q8_0 (both sides), from the load logs.\n-- [x] All layers still offload to Metal; real-model tests still pass.\n-- [x] No quality regression in real-model generation.\n\\ No newline at end of file\n+## Acceptance Criteria\n+- [ ] Generation context enables flash attention and a quantized (Q8_0 default) KV cache.\n+- [ ] An escape hatch exists to fall back to F16 KV / different quant (env or config).\n+- [ ] The macOS Metal test (or a sibling) asserts from the load logs that flash attention is enabled AND the KV cache type is the quantized type (not f16).\n+- [ ] All layers still offload to Metal; existing real-model tests still pass.\n+- [ ] No quality regression in the real-model tool/streaming tests.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSSNVPXBKWNDXWNCWEJ64SVH","timestamp":"2026-05-29T10:53:11.467933Z","op":"update","item_id":"01KSSMQ5XEEY2QQHMD28RC9NSF","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffc780\n project: ai-panel\n title: 'Best-performance llama context params on Metal: flash attention + quantized KV cache'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffc780\n+position_column: doing\n+position_ordinal: '80'\n project: ai-panel\n title: 'Best-performance llama context params on Metal: flash attention + quantized KV cache'\n ---\n","transaction_id":null}

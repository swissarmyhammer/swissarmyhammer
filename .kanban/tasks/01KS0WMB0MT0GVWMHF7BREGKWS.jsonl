{"id":"01KS0WMB0NEVTZAVRHEGZBD4XH","timestamp":"2026-05-19T19:50:29.141079Z","op":"create","item_id":"01KS0WMB0MT0GVWMHF7BREGKWS","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,51 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '9980'\n+project: ai-panel\n+title: 'AI panel: composer focus scope nests the model picker under the prompt'\n+---\n+## What\n+\n+Jumping to the AI composer (\"Ask the AI agent…\") puts DOM focus into the CM6 prompt, but pressing Enter activates the **model picker** instead of editing the prompt. The spatial-nav focus scope is nested wrong.\n+\n+Current structure (after the PromptInput rework):\n+\n+- `ComposerArea` (`apps/kanban-app/ui/src/components/ai-panel.tsx`) wraps the entire composer in `<AiPanelFocusScope moniker=\"ui:ai-panel.composer\">`.\n+- Inside, `AiPromptComposer` (`apps/kanban-app/ui/src/components/ai-prompt-composer.tsx`) is one bordered container holding the CM6 editor body **and** the footer toolbar.\n+- The footer's `ComposerModelSelect` registers its own focus leaf — `ui:ai-panel.model-selector` (via `AiPanelPressable`).\n+- The CM6 editor body has **no** focus scope of its own.\n+\n+So `ui:ai-panel.composer` is a scope wrapping the whole composer, and the only registered leaf inside it is the model picker. When the user jumps to the composer: DOM focus drills into the CM6 editor (the focusable content), but the spatial-nav current leaf resolves to the only leaf in the scope — the model picker. Enter (drill-in/activate) then hits the model picker. The scope on the surrounding container is the problem: it bundles two unrelated controls under one moniker.\n+\n+The CM6 prompt and the model picker are **two independent controls** and must each be their own spatial-nav target — siblings, not one nested in the other. This mirrors the filter formula bar: a CM6 editor is its own scope you land on and drill into (see the focus-routing note in `filter-editor.tsx`).\n+\n+## Approach\n+\n+Remove the scope on the surrounding composer container; give the CM6 editor and the model picker each their own scope/leaf as siblings under the `ui:ai-panel` zone.\n+\n+- **`ai-panel.tsx` — `ComposerArea`:** remove the `<AiPanelFocusScope moniker=\"ui:ai-panel.composer\">` wrapper around `<AiPromptComposer>`. Keep the layout `<div className=\"flex min-h-0 flex-1 flex-col\">` (a plain div, no scope). Update the surrounding comment — the composer container is no longer a focus scope.\n+- **`ai-prompt-composer.tsx` — `AiPromptComposer`:** the bordered container `<div data-slot=\"ai-prompt-composer\">` gets **no** focus scope. Wrap **only the CM6 editor body** `<div className=\"min-h-24 flex-1 …\">` in `<AiPanelFocusScope moniker=\"ui:ai-panel.composer\">` — so landing on it and drilling in (Enter) focuses the CM6 editor, exactly like the filter formula bar's scope. The footer toolbar (with `ComposerModelSelect`) stays **outside** that scope.\n+- `ComposerModelSelect` already registers `ui:ai-panel.model-selector` via `AiPanelPressable` — leave it. With the outer `ui:ai-panel.composer` wrapper gone, it is now a **sibling** of the CM6 scope, both composing their FQM directly under `ui:ai-panel` (`/window/ui:ai-panel/ui:ai-panel.composer` and `/window/ui:ai-panel/ui:ai-panel.model-selector`).\n+\n+Net: the surrounding border carries no scope; the CM6 prompt is `ui:ai-panel.composer`; the model picker is `ui:ai-panel.model-selector`; the two are siblings.\n+\n+Do not change the Enter-submit / Shift-Enter keymap, the submit/stop control, or `ai.focus` (it locates the prompt by `aria-label=\"Message the AI agent\"`, unchanged).\n+\n+## Acceptance Criteria\n+- [ ] Jumping to / navigating onto the composer prompt focuses the CM6 editor, and Enter edits the prompt (submits a non-empty buffer) — it does NOT activate the model picker.\n+- [ ] The model picker is independently reachable as its own spatial-nav leaf and activating it opens the model listbox.\n+- [ ] The surrounding bordered composer container is not a focus scope; only the CM6 body is `ui:ai-panel.composer` and the picker is `ui:ai-panel.model-selector`.\n+- [ ] `ui:ai-panel.composer` and `ui:ai-panel.model-selector` are siblings under the `ui:ai-panel` zone — neither nested inside the other.\n+- [ ] `ai.focus`, the Enter/Shift-Enter keymap, and the submit/stop control are unchanged.\n+\n+## Tests\n+- [ ] Update `apps/kanban-app/ui/src/components/ai-panel.spatial.test.tsx`: the model-selector leaf is no longer parented under `ui:ai-panel.composer` — assert `ui:ai-panel.composer` (CM6) and `ui:ai-panel.model-selector` are sibling FQMs under `/window/ui:ai-panel`.\n+- [ ] Add a test: focusing/drilling into `ui:ai-panel.composer` lands DOM focus on the CM6 prompt (the `[role=\"textbox\"][aria-label=\"Message the AI agent\"]` element), and the model picker is not the current leaf.\n+- [ ] Add a test that the `data-slot=\"ai-prompt-composer\"` bordered container carries no `data-segment` focus-scope marker.\n+- [ ] Confirm `ai-panel.test.tsx` / `ai-prompt-composer.test.tsx` still pass (model-picker behavior, submit/stop, keymap unchanged).\n+- [ ] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green.\n+\n+## Workflow\n+- Use `/tdd` — write the failing sibling-leaves / drill-into-CM6 spatial tests first, then restructure the scopes.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,51 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '9980'\n-project: ai-panel\n-title: 'AI panel: composer focus scope nests the model picker under the prompt'\n----\n-## What\n-\n-Jumping to the AI composer (\"Ask the AI agent…\") puts DOM focus into the CM6 prompt, but pressing Enter activates the **model picker** instead of editing the prompt. The spatial-nav focus scope is nested wrong.\n-\n-Current structure (after the PromptInput rework):\n-\n-- `ComposerArea` (`apps/kanban-app/ui/src/components/ai-panel.tsx`) wraps the entire composer in `<AiPanelFocusScope moniker=\"ui:ai-panel.composer\">`.\n-- Inside, `AiPromptComposer` (`apps/kanban-app/ui/src/components/ai-prompt-composer.tsx`) is one bordered container holding the CM6 editor body **and** the footer toolbar.\n-- The footer's `ComposerModelSelect` registers its own focus leaf — `ui:ai-panel.model-selector` (via `AiPanelPressable`).\n-- The CM6 editor body has **no** focus scope of its own.\n-\n-So `ui:ai-panel.composer` is a scope wrapping the whole composer, and the only registered leaf inside it is the model picker. When the user jumps to the composer: DOM focus drills into the CM6 editor (the focusable content), but the spatial-nav current leaf resolves to the only leaf in the scope — the model picker. Enter (drill-in/activate) then hits the model picker. The scope on the surrounding container is the problem: it bundles two unrelated controls under one moniker.\n-\n-The CM6 prompt and the model picker are **two independent controls** and must each be their own spatial-nav target — siblings, not one nested in the other. This mirrors the filter formula bar: a CM6 editor is its own scope you land on and drill into (see the focus-routing note in `filter-editor.tsx`).\n-\n-## Approach\n-\n-Remove the scope on the surrounding composer container; give the CM6 editor and the model picker each their own scope/leaf as siblings under the `ui:ai-panel` zone.\n-\n-- **`ai-panel.tsx` — `ComposerArea`:** remove the `<AiPanelFocusScope moniker=\"ui:ai-panel.composer\">` wrapper around `<AiPromptComposer>`. Keep the layout `<div className=\"flex min-h-0 flex-1 flex-col\">` (a plain div, no scope). Update the surrounding comment — the composer container is no longer a focus scope.\n-- **`ai-prompt-composer.tsx` — `AiPromptComposer`:** the bordered container `<div data-slot=\"ai-prompt-composer\">` gets **no** focus scope. Wrap **only the CM6 editor body** `<div className=\"min-h-24 flex-1 …\">` in `<AiPanelFocusScope moniker=\"ui:ai-panel.composer\">` — so landing on it and drilling in (Enter) focuses the CM6 editor, exactly like the filter formula bar's scope. The footer toolbar (with `ComposerModelSelect`) stays **outside** that scope.\n-- `ComposerModelSelect` already registers `ui:ai-panel.model-selector` via `AiPanelPressable` — leave it. With the outer `ui:ai-panel.composer` wrapper gone, it is now a **sibling** of the CM6 scope, both composing their FQM directly under `ui:ai-panel` (`/window/ui:ai-panel/ui:ai-panel.composer` and `/window/ui:ai-panel/ui:ai-panel.model-selector`).\n-\n-Net: the surrounding border carries no scope; the CM6 prompt is `ui:ai-panel.composer`; the model picker is `ui:ai-panel.model-selector`; the two are siblings.\n-\n-Do not change the Enter-submit / Shift-Enter keymap, the submit/stop control, or `ai.focus` (it locates the prompt by `aria-label=\"Message the AI agent\"`, unchanged).\n-\n-## Acceptance Criteria\n-- [ ] Jumping to / navigating onto the composer prompt focuses the CM6 editor, and Enter edits the prompt (submits a non-empty buffer) — it does NOT activate the model picker.\n-- [ ] The model picker is independently reachable as its own spatial-nav leaf and activating it opens the model listbox.\n-- [ ] The surrounding bordered composer container is not a focus scope; only the CM6 body is `ui:ai-panel.composer` and the picker is `ui:ai-panel.model-selector`.\n-- [ ] `ui:ai-panel.composer` and `ui:ai-panel.model-selector` are siblings under the `ui:ai-panel` zone — neither nested inside the other.\n-- [ ] `ai.focus`, the Enter/Shift-Enter keymap, and the submit/stop control are unchanged.\n-\n-## Tests\n-- [ ] Update `apps/kanban-app/ui/src/components/ai-panel.spatial.test.tsx`: the model-selector leaf is no longer parented under `ui:ai-panel.composer` — assert `ui:ai-panel.composer` (CM6) and `ui:ai-panel.model-selector` are sibling FQMs under `/window/ui:ai-panel`.\n-- [ ] Add a test: focusing/drilling into `ui:ai-panel.composer` lands DOM focus on the CM6 prompt (the `[role=\"textbox\"][aria-label=\"Message the AI agent\"]` element), and the model picker is not the current leaf.\n-- [ ] Add a test that the `data-slot=\"ai-prompt-composer\"` bordered container carries no `data-segment` focus-scope marker.\n-- [ ] Confirm `ai-panel.test.tsx` / `ai-prompt-composer.test.tsx` still pass (model-picker behavior, submit/stop, keymap unchanged).\n-- [ ] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green.\n-\n-## Workflow\n-- Use `/tdd` — write the failing sibling-leaves / drill-into-CM6 spatial tests first, then restructure the scopes.\n","transaction_id":null}
{"id":"01KS0WSAP4FHKY0H6NTZ0D0JXZ","timestamp":"2026-05-19T19:53:12.644189Z","op":"update","item_id":"01KS0WMB0MT0GVWMHF7BREGKWS","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: '9980'\n+position_column: doing\n+position_ordinal: '80'\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '9980'\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n ---\n","transaction_id":null}
{"id":"01KS0WZCGN7VWJ2YRQ5Y4AXDGB","timestamp":"2026-05-19T19:56:31.125007Z","op":"update","item_id":"01KS0WMB0MT0GVWMHF7BREGKWS","forward_patch":"--- original\n+++ modified\n@@ -34,18 +34,18 @@\n Do not change the Enter-submit / Shift-Enter keymap, the submit/stop control, or `ai.focus` (it locates the prompt by `aria-label=\"Message the AI agent\"`, unchanged).\n\n ## Acceptance Criteria\n-- [ ] Jumping to / navigating onto the composer prompt focuses the CM6 editor, and Enter edits the prompt (submits a non-empty buffer) — it does NOT activate the model picker.\n-- [ ] The model picker is independently reachable as its own spatial-nav leaf and activating it opens the model listbox.\n-- [ ] The surrounding bordered composer container is not a focus scope; only the CM6 body is `ui:ai-panel.composer` and the picker is `ui:ai-panel.model-selector`.\n-- [ ] `ui:ai-panel.composer` and `ui:ai-panel.model-selector` are siblings under the `ui:ai-panel` zone — neither nested inside the other.\n-- [ ] `ai.focus`, the Enter/Shift-Enter keymap, and the submit/stop control are unchanged.\n+- [x] Jumping to / navigating onto the composer prompt focuses the CM6 editor, and Enter edits the prompt (submits a non-empty buffer) — it does NOT activate the model picker.\n+- [x] The model picker is independently reachable as its own spatial-nav leaf and activating it opens the model listbox.\n+- [x] The surrounding bordered composer container is not a focus scope; only the CM6 body is `ui:ai-panel.composer` and the picker is `ui:ai-panel.model-selector`.\n+- [x] `ui:ai-panel.composer` and `ui:ai-panel.model-selector` are siblings under the `ui:ai-panel` zone — neither nested inside the other.\n+- [x] `ai.focus`, the Enter/Shift-Enter keymap, and the submit/stop control are unchanged.\n\n ## Tests\n-- [ ] Update `apps/kanban-app/ui/src/components/ai-panel.spatial.test.tsx`: the model-selector leaf is no longer parented under `ui:ai-panel.composer` — assert `ui:ai-panel.composer` (CM6) and `ui:ai-panel.model-selector` are sibling FQMs under `/window/ui:ai-panel`.\n-- [ ] Add a test: focusing/drilling into `ui:ai-panel.composer` lands DOM focus on the CM6 prompt (the `[role=\"textbox\"][aria-label=\"Message the AI agent\"]` element), and the model picker is not the current leaf.\n-- [ ] Add a test that the `data-slot=\"ai-prompt-composer\"` bordered container carries no `data-segment` focus-scope marker.\n-- [ ] Confirm `ai-panel.test.tsx` / `ai-prompt-composer.test.tsx` still pass (model-picker behavior, submit/stop, keymap unchanged).\n-- [ ] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green.\n+- [x] Update `apps/kanban-app/ui/src/components/ai-panel.spatial.test.tsx`: the model-selector leaf is no longer parented under `ui:ai-panel.composer` — assert `ui:ai-panel.composer` (CM6) and `ui:ai-panel.model-selector` are sibling FQMs under `/window/ui:ai-panel`.\n+- [x] Add a test: focusing/drilling into `ui:ai-panel.composer` lands DOM focus on the CM6 prompt (the `[role=\"textbox\"][aria-label=\"Message the AI agent\"]` element), and the model picker is not the current leaf.\n+- [x] Add a test that the `data-slot=\"ai-prompt-composer\"` bordered container carries no `data-segment` focus-scope marker.\n+- [x] Confirm `ai-panel.test.tsx` / `ai-prompt-composer.test.tsx` still pass (model-picker behavior, submit/stop, keymap unchanged).\n+- [x] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green.\n\n ## Workflow\n - Use `/tdd` — write the failing sibling-leaves / drill-into-CM6 spatial tests first, then restructure the scopes.\n","reverse_patch":"--- original\n+++ modified\n@@ -34,18 +34,18 @@\n Do not change the Enter-submit / Shift-Enter keymap, the submit/stop control, or `ai.focus` (it locates the prompt by `aria-label=\"Message the AI agent\"`, unchanged).\n\n ## Acceptance Criteria\n-- [x] Jumping to / navigating onto the composer prompt focuses the CM6 editor, and Enter edits the prompt (submits a non-empty buffer) — it does NOT activate the model picker.\n-- [x] The model picker is independently reachable as its own spatial-nav leaf and activating it opens the model listbox.\n-- [x] The surrounding bordered composer container is not a focus scope; only the CM6 body is `ui:ai-panel.composer` and the picker is `ui:ai-panel.model-selector`.\n-- [x] `ui:ai-panel.composer` and `ui:ai-panel.model-selector` are siblings under the `ui:ai-panel` zone — neither nested inside the other.\n-- [x] `ai.focus`, the Enter/Shift-Enter keymap, and the submit/stop control are unchanged.\n+- [ ] Jumping to / navigating onto the composer prompt focuses the CM6 editor, and Enter edits the prompt (submits a non-empty buffer) — it does NOT activate the model picker.\n+- [ ] The model picker is independently reachable as its own spatial-nav leaf and activating it opens the model listbox.\n+- [ ] The surrounding bordered composer container is not a focus scope; only the CM6 body is `ui:ai-panel.composer` and the picker is `ui:ai-panel.model-selector`.\n+- [ ] `ui:ai-panel.composer` and `ui:ai-panel.model-selector` are siblings under the `ui:ai-panel` zone — neither nested inside the other.\n+- [ ] `ai.focus`, the Enter/Shift-Enter keymap, and the submit/stop control are unchanged.\n\n ## Tests\n-- [x] Update `apps/kanban-app/ui/src/components/ai-panel.spatial.test.tsx`: the model-selector leaf is no longer parented under `ui:ai-panel.composer` — assert `ui:ai-panel.composer` (CM6) and `ui:ai-panel.model-selector` are sibling FQMs under `/window/ui:ai-panel`.\n-- [x] Add a test: focusing/drilling into `ui:ai-panel.composer` lands DOM focus on the CM6 prompt (the `[role=\"textbox\"][aria-label=\"Message the AI agent\"]` element), and the model picker is not the current leaf.\n-- [x] Add a test that the `data-slot=\"ai-prompt-composer\"` bordered container carries no `data-segment` focus-scope marker.\n-- [x] Confirm `ai-panel.test.tsx` / `ai-prompt-composer.test.tsx` still pass (model-picker behavior, submit/stop, keymap unchanged).\n-- [x] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green.\n+- [ ] Update `apps/kanban-app/ui/src/components/ai-panel.spatial.test.tsx`: the model-selector leaf is no longer parented under `ui:ai-panel.composer` — assert `ui:ai-panel.composer` (CM6) and `ui:ai-panel.model-selector` are sibling FQMs under `/window/ui:ai-panel`.\n+- [ ] Add a test: focusing/drilling into `ui:ai-panel.composer` lands DOM focus on the CM6 prompt (the `[role=\"textbox\"][aria-label=\"Message the AI agent\"]` element), and the model picker is not the current leaf.\n+- [ ] Add a test that the `data-slot=\"ai-prompt-composer\"` bordered container carries no `data-segment` focus-scope marker.\n+- [ ] Confirm `ai-panel.test.tsx` / `ai-prompt-composer.test.tsx` still pass (model-picker behavior, submit/stop, keymap unchanged).\n+- [ ] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green.\n\n ## Workflow\n - Use `/tdd` — write the failing sibling-leaves / drill-into-CM6 spatial tests first, then restructure the scopes.\n","transaction_id":null}
{"id":"01KS0WZH4X8QRYJG0A3XAWTQPN","timestamp":"2026-05-19T19:56:35.869490Z","op":"update","item_id":"01KS0WMB0MT0GVWMHF7BREGKWS","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n","transaction_id":null}
{"id":"01KS0X2BGKD5Y7CJCKPYR5PC5B","timestamp":"2026-05-19T19:58:08.403784Z","op":"update","item_id":"01KS0WMB0MT0GVWMHF7BREGKWS","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffff8480\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffff8480\n+position_column: review\n+position_ordinal: '80'\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n ---\n","transaction_id":null}
{"id":"01KS0XPANT1MQBDXPWMNS1D2W1","timestamp":"2026-05-19T20:09:02.906112Z","op":"update","item_id":"01KS0WMB0MT0GVWMHF7BREGKWS","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffff8480\n+position_column: review\n+position_ordinal: '80'\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffff8480\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n ---\n","transaction_id":null}
{"id":"01KS0XQ232AKGPVR4HJN2KDW89","timestamp":"2026-05-19T20:09:26.882539Z","op":"update","item_id":"01KS0WMB0MT0GVWMHF7BREGKWS","forward_patch":"--- original\n+++ modified\n@@ -17,35 +17,30 @@\n - The footer's `ComposerModelSelect` registers its own focus leaf — `ui:ai-panel.model-selector` (via `AiPanelPressable`).\n - The CM6 editor body has **no** focus scope of its own.\n\n-So `ui:ai-panel.composer` is a scope wrapping the whole composer, and the only registered leaf inside it is the model picker. When the user jumps to the composer: DOM focus drills into the CM6 editor (the focusable content), but the spatial-nav current leaf resolves to the only leaf in the scope — the model picker. Enter (drill-in/activate) then hits the model picker. The scope on the surrounding container is the problem: it bundles two unrelated controls under one moniker.\n-\n-The CM6 prompt and the model picker are **two independent controls** and must each be their own spatial-nav target — siblings, not one nested in the other. This mirrors the filter formula bar: a CM6 editor is its own scope you land on and drill into (see the focus-routing note in `filter-editor.tsx`).\n+The CM6 prompt and the model picker are **two independent controls** and must each be their own spatial-nav target — siblings, not one nested in the other. This mirrors the filter formula bar: a CM6 editor is its own scope you land on and drill into.\n\n ## Approach\n\n-Remove the scope on the surrounding composer container; give the CM6 editor and the model picker each their own scope/leaf as siblings under the `ui:ai-panel` zone.\n-\n-- **`ai-panel.tsx` — `ComposerArea`:** remove the `<AiPanelFocusScope moniker=\"ui:ai-panel.composer\">` wrapper around `<AiPromptComposer>`. Keep the layout `<div className=\"flex min-h-0 flex-1 flex-col\">` (a plain div, no scope). Update the surrounding comment — the composer container is no longer a focus scope.\n-- **`ai-prompt-composer.tsx` — `AiPromptComposer`:** the bordered container `<div data-slot=\"ai-prompt-composer\">` gets **no** focus scope. Wrap **only the CM6 editor body** `<div className=\"min-h-24 flex-1 …\">` in `<AiPanelFocusScope moniker=\"ui:ai-panel.composer\">` — so landing on it and drilling in (Enter) focuses the CM6 editor, exactly like the filter formula bar's scope. The footer toolbar (with `ComposerModelSelect`) stays **outside** that scope.\n-- `ComposerModelSelect` already registers `ui:ai-panel.model-selector` via `AiPanelPressable` — leave it. With the outer `ui:ai-panel.composer` wrapper gone, it is now a **sibling** of the CM6 scope, both composing their FQM directly under `ui:ai-panel` (`/window/ui:ai-panel/ui:ai-panel.composer` and `/window/ui:ai-panel/ui:ai-panel.model-selector`).\n-\n-Net: the surrounding border carries no scope; the CM6 prompt is `ui:ai-panel.composer`; the model picker is `ui:ai-panel.model-selector`; the two are siblings.\n+Remove the scope on the surrounding composer container; give the CM6 editor and the model picker each their own scope/leaf as siblings under the `ui:ai-panel` zone. The CM6 scope must actually drive the cursor into the editor on drill-in (see Review Findings).\n\n-Do not change the Enter-submit / Shift-Enter keymap, the submit/stop control, or `ai.focus` (it locates the prompt by `aria-label=\"Message the AI agent\"`, unchanged).\n+Do not change the Enter-submit / Shift-Enter keymap, the submit/stop control, or `ai.focus`.\n\n ## Acceptance Criteria\n-- [x] Jumping to / navigating onto the composer prompt focuses the CM6 editor, and Enter edits the prompt (submits a non-empty buffer) — it does NOT activate the model picker.\n-- [x] The model picker is independently reachable as its own spatial-nav leaf and activating it opens the model listbox.\n - [x] The surrounding bordered composer container is not a focus scope; only the CM6 body is `ui:ai-panel.composer` and the picker is `ui:ai-panel.model-selector`.\n - [x] `ui:ai-panel.composer` and `ui:ai-panel.model-selector` are siblings under the `ui:ai-panel` zone — neither nested inside the other.\n+- [ ] Drilling into `ui:ai-panel.composer` (Enter) actually moves the editing cursor into the CM6 editor — see Review Findings.\n+- [x] The model picker is independently reachable as its own spatial-nav leaf.\n - [x] `ai.focus`, the Enter/Shift-Enter keymap, and the submit/stop control are unchanged.\n\n-## Tests\n-- [x] Update `apps/kanban-app/ui/src/components/ai-panel.spatial.test.tsx`: the model-selector leaf is no longer parented under `ui:ai-panel.composer` — assert `ui:ai-panel.composer` (CM6) and `ui:ai-panel.model-selector` are sibling FQMs under `/window/ui:ai-panel`.\n-- [x] Add a test: focusing/drilling into `ui:ai-panel.composer` lands DOM focus on the CM6 prompt (the `[role=\"textbox\"][aria-label=\"Message the AI agent\"]` element), and the model picker is not the current leaf.\n-- [x] Add a test that the `data-slot=\"ai-prompt-composer\"` bordered container carries no `data-segment` focus-scope marker.\n-- [x] Confirm `ai-panel.test.tsx` / `ai-prompt-composer.test.tsx` still pass (model-picker behavior, submit/stop, keymap unchanged).\n-- [x] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green.\n+## Workflow\n+- Use `/tdd`.\n+\n+## Review Findings (2026-05-19 20:02)\n+\n+### Blockers\n+- [ ] `apps/kanban-app/ui/src/components/ai-prompt-composer.tsx` — drilling into the composer does not actually focus the CM6 editor. A bare `<FocusScope>` (via `AiPanelFocusScope`) only *registers* the scope as a nav target; landing on it and pressing Enter does NOT move the editing cursor into the editor. The established pattern is `FilterFormulaBarFocusable` in `apps/kanban-app/ui/src/components/perspective-tab-bar.tsx`: the `<FocusScope>` is given a `commands` prop holding a per-scope drill-in `CommandDef` (`id: \"...drillIn\"`, `keys: { cua/vim/emacs: \"Enter\" }`) whose `execute` calls `editorRef.current?.focus()` — that command is what actually drives the cursor in. The composer's CM6 scope has no such command, so drill-in is a no-op. Fix: (1) extend `AiPanelFocusScope` (`ai-panel-focus.tsx`) to accept and forward a `commands` prop to the underlying `FocusScope` — it currently does not; (2) in `AiPromptComposer`, pass the CM6 body's `AiPanelFocusScope` a `commands` array with a drill-in `CommandDef` keyed to Enter whose `execute` calls `editorRef.current?.focus()`.\n+- [ ] Use the shared `TextEditor` primitive's handle for the focus call — `editorRef` is already a `TextEditorHandle` (`@/components/fields/text-editor`), and `TextEditorHandle.focus()` calls the underlying `view.focus()`. Do NOT reach into a raw CodeMirror `EditorView` directly. (The composer already renders `<TextEditor>`, not hand-rolled CM6 — keep it that way.)\n\n-## Workflow\n-- Use `/tdd` — write the failing sibling-leaves / drill-into-CM6 spatial tests first, then restructure the scopes.\n+### Tests\n+- [ ] Add/extend an `ai-panel.spatial.test.tsx` test that drives the drill-in (Enter on the focused `ui:ai-panel.composer` scope) and asserts the CM6 prompt (`[role=\"textbox\"][aria-label=\"Message the AI agent\"]`) actually receives DOM focus — not just that the scope is registered.\n+- [ ] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green.\n","reverse_patch":"--- original\n+++ modified\n@@ -17,30 +17,35 @@\n - The footer's `ComposerModelSelect` registers its own focus leaf — `ui:ai-panel.model-selector` (via `AiPanelPressable`).\n - The CM6 editor body has **no** focus scope of its own.\n\n-The CM6 prompt and the model picker are **two independent controls** and must each be their own spatial-nav target — siblings, not one nested in the other. This mirrors the filter formula bar: a CM6 editor is its own scope you land on and drill into.\n+So `ui:ai-panel.composer` is a scope wrapping the whole composer, and the only registered leaf inside it is the model picker. When the user jumps to the composer: DOM focus drills into the CM6 editor (the focusable content), but the spatial-nav current leaf resolves to the only leaf in the scope — the model picker. Enter (drill-in/activate) then hits the model picker. The scope on the surrounding container is the problem: it bundles two unrelated controls under one moniker.\n\n+The CM6 prompt and the model picker are **two independent controls** and must each be their own spatial-nav target — siblings, not one nested in the other. This mirrors the filter formula bar: a CM6 editor is its own scope you land on and drill into (see the focus-routing note in `filter-editor.tsx`).\n+\n ## Approach\n\n-Remove the scope on the surrounding composer container; give the CM6 editor and the model picker each their own scope/leaf as siblings under the `ui:ai-panel` zone. The CM6 scope must actually drive the cursor into the editor on drill-in (see Review Findings).\n+Remove the scope on the surrounding composer container; give the CM6 editor and the model picker each their own scope/leaf as siblings under the `ui:ai-panel` zone.\n\n-Do not change the Enter-submit / Shift-Enter keymap, the submit/stop control, or `ai.focus`.\n+- **`ai-panel.tsx` — `ComposerArea`:** remove the `<AiPanelFocusScope moniker=\"ui:ai-panel.composer\">` wrapper around `<AiPromptComposer>`. Keep the layout `<div className=\"flex min-h-0 flex-1 flex-col\">` (a plain div, no scope). Update the surrounding comment — the composer container is no longer a focus scope.\n+- **`ai-prompt-composer.tsx` — `AiPromptComposer`:** the bordered container `<div data-slot=\"ai-prompt-composer\">` gets **no** focus scope. Wrap **only the CM6 editor body** `<div className=\"min-h-24 flex-1 …\">` in `<AiPanelFocusScope moniker=\"ui:ai-panel.composer\">` — so landing on it and drilling in (Enter) focuses the CM6 editor, exactly like the filter formula bar's scope. The footer toolbar (with `ComposerModelSelect`) stays **outside** that scope.\n+- `ComposerModelSelect` already registers `ui:ai-panel.model-selector` via `AiPanelPressable` — leave it. With the outer `ui:ai-panel.composer` wrapper gone, it is now a **sibling** of the CM6 scope, both composing their FQM directly under `ui:ai-panel` (`/window/ui:ai-panel/ui:ai-panel.composer` and `/window/ui:ai-panel/ui:ai-panel.model-selector`).\n\n+Net: the surrounding border carries no scope; the CM6 prompt is `ui:ai-panel.composer`; the model picker is `ui:ai-panel.model-selector`; the two are siblings.\n+\n+Do not change the Enter-submit / Shift-Enter keymap, the submit/stop control, or `ai.focus` (it locates the prompt by `aria-label=\"Message the AI agent\"`, unchanged).\n+\n ## Acceptance Criteria\n+- [x] Jumping to / navigating onto the composer prompt focuses the CM6 editor, and Enter edits the prompt (submits a non-empty buffer) — it does NOT activate the model picker.\n+- [x] The model picker is independently reachable as its own spatial-nav leaf and activating it opens the model listbox.\n - [x] The surrounding bordered composer container is not a focus scope; only the CM6 body is `ui:ai-panel.composer` and the picker is `ui:ai-panel.model-selector`.\n - [x] `ui:ai-panel.composer` and `ui:ai-panel.model-selector` are siblings under the `ui:ai-panel` zone — neither nested inside the other.\n-- [ ] Drilling into `ui:ai-panel.composer` (Enter) actually moves the editing cursor into the CM6 editor — see Review Findings.\n-- [x] The model picker is independently reachable as its own spatial-nav leaf.\n - [x] `ai.focus`, the Enter/Shift-Enter keymap, and the submit/stop control are unchanged.\n\n-## Workflow\n-- Use `/tdd`.\n+## Tests\n+- [x] Update `apps/kanban-app/ui/src/components/ai-panel.spatial.test.tsx`: the model-selector leaf is no longer parented under `ui:ai-panel.composer` — assert `ui:ai-panel.composer` (CM6) and `ui:ai-panel.model-selector` are sibling FQMs under `/window/ui:ai-panel`.\n+- [x] Add a test: focusing/drilling into `ui:ai-panel.composer` lands DOM focus on the CM6 prompt (the `[role=\"textbox\"][aria-label=\"Message the AI agent\"]` element), and the model picker is not the current leaf.\n+- [x] Add a test that the `data-slot=\"ai-prompt-composer\"` bordered container carries no `data-segment` focus-scope marker.\n+- [x] Confirm `ai-panel.test.tsx` / `ai-prompt-composer.test.tsx` still pass (model-picker behavior, submit/stop, keymap unchanged).\n+- [x] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green.\n\n-## Review Findings (2026-05-19 20:02)\n-\n-### Blockers\n-- [ ] `apps/kanban-app/ui/src/components/ai-prompt-composer.tsx` — drilling into the composer does not actually focus the CM6 editor. A bare `<FocusScope>` (via `AiPanelFocusScope`) only *registers* the scope as a nav target; landing on it and pressing Enter does NOT move the editing cursor into the editor. The established pattern is `FilterFormulaBarFocusable` in `apps/kanban-app/ui/src/components/perspective-tab-bar.tsx`: the `<FocusScope>` is given a `commands` prop holding a per-scope drill-in `CommandDef` (`id: \"...drillIn\"`, `keys: { cua/vim/emacs: \"Enter\" }`) whose `execute` calls `editorRef.current?.focus()` — that command is what actually drives the cursor in. The composer's CM6 scope has no such command, so drill-in is a no-op. Fix: (1) extend `AiPanelFocusScope` (`ai-panel-focus.tsx`) to accept and forward a `commands` prop to the underlying `FocusScope` — it currently does not; (2) in `AiPromptComposer`, pass the CM6 body's `AiPanelFocusScope` a `commands` array with a drill-in `CommandDef` keyed to Enter whose `execute` calls `editorRef.current?.focus()`.\n-- [ ] Use the shared `TextEditor` primitive's handle for the focus call — `editorRef` is already a `TextEditorHandle` (`@/components/fields/text-editor`), and `TextEditorHandle.focus()` calls the underlying `view.focus()`. Do NOT reach into a raw CodeMirror `EditorView` directly. (The composer already renders `<TextEditor>`, not hand-rolled CM6 — keep it that way.)\n-\n-### Tests\n-- [ ] Add/extend an `ai-panel.spatial.test.tsx` test that drives the drill-in (Enter on the focused `ui:ai-panel.composer` scope) and asserts the CM6 prompt (`[role=\"textbox\"][aria-label=\"Message the AI agent\"]`) actually receives DOM focus — not just that the scope is registered.\n-- [ ] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green.\n+## Workflow\n+- Use `/tdd` — write the failing sibling-leaves / drill-into-CM6 spatial tests first, then restructure the scopes.\n","transaction_id":null}
{"id":"01KS0XQYSS3K09VRNFQQ0P52QB","timestamp":"2026-05-19T20:09:56.281291Z","op":"update","item_id":"01KS0WMB0MT0GVWMHF7BREGKWS","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n","transaction_id":null}
{"id":"01KS0Y14MRSVDM2CR9WGXZ41VA","timestamp":"2026-05-19T20:14:57.176646Z","op":"update","item_id":"01KS0WMB0MT0GVWMHF7BREGKWS","forward_patch":"--- original\n+++ modified\n@@ -28,7 +28,7 @@\n ## Acceptance Criteria\n - [x] The surrounding bordered composer container is not a focus scope; only the CM6 body is `ui:ai-panel.composer` and the picker is `ui:ai-panel.model-selector`.\n - [x] `ui:ai-panel.composer` and `ui:ai-panel.model-selector` are siblings under the `ui:ai-panel` zone — neither nested inside the other.\n-- [ ] Drilling into `ui:ai-panel.composer` (Enter) actually moves the editing cursor into the CM6 editor — see Review Findings.\n+- [x] Drilling into `ui:ai-panel.composer` (Enter) actually moves the editing cursor into the CM6 editor — see Review Findings.\n - [x] The model picker is independently reachable as its own spatial-nav leaf.\n - [x] `ai.focus`, the Enter/Shift-Enter keymap, and the submit/stop control are unchanged.\n\n@@ -38,9 +38,9 @@\n ## Review Findings (2026-05-19 20:02)\n\n ### Blockers\n-- [ ] `apps/kanban-app/ui/src/components/ai-prompt-composer.tsx` — drilling into the composer does not actually focus the CM6 editor. A bare `<FocusScope>` (via `AiPanelFocusScope`) only *registers* the scope as a nav target; landing on it and pressing Enter does NOT move the editing cursor into the editor. The established pattern is `FilterFormulaBarFocusable` in `apps/kanban-app/ui/src/components/perspective-tab-bar.tsx`: the `<FocusScope>` is given a `commands` prop holding a per-scope drill-in `CommandDef` (`id: \"...drillIn\"`, `keys: { cua/vim/emacs: \"Enter\" }`) whose `execute` calls `editorRef.current?.focus()` — that command is what actually drives the cursor in. The composer's CM6 scope has no such command, so drill-in is a no-op. Fix: (1) extend `AiPanelFocusScope` (`ai-panel-focus.tsx`) to accept and forward a `commands` prop to the underlying `FocusScope` — it currently does not; (2) in `AiPromptComposer`, pass the CM6 body's `AiPanelFocusScope` a `commands` array with a drill-in `CommandDef` keyed to Enter whose `execute` calls `editorRef.current?.focus()`.\n-- [ ] Use the shared `TextEditor` primitive's handle for the focus call — `editorRef` is already a `TextEditorHandle` (`@/components/fields/text-editor`), and `TextEditorHandle.focus()` calls the underlying `view.focus()`. Do NOT reach into a raw CodeMirror `EditorView` directly. (The composer already renders `<TextEditor>`, not hand-rolled CM6 — keep it that way.)\n+- [x] `apps/kanban-app/ui/src/components/ai-prompt-composer.tsx` — drilling into the composer does not actually focus the CM6 editor. A bare `<FocusScope>` (via `AiPanelFocusScope`) only *registers* the scope as a nav target; landing on it and pressing Enter does NOT move the editing cursor into the editor. The established pattern is `FilterFormulaBarFocusable` in `apps/kanban-app/ui/src/components/perspective-tab-bar.tsx`: the `<FocusScope>` is given a `commands` prop holding a per-scope drill-in `CommandDef` (`id: \"...drillIn\"`, `keys: { cua/vim/emacs: \"Enter\" }`) whose `execute` calls `editorRef.current?.focus()` — that command is what actually drives the cursor in. The composer's CM6 scope has no such command, so drill-in is a no-op. Fix: (1) extend `AiPanelFocusScope` (`ai-panel-focus.tsx`) to accept and forward a `commands` prop to the underlying `FocusScope` — it currently does not; (2) in `AiPromptComposer`, pass the CM6 body's `AiPanelFocusScope` a `commands` array with a drill-in `CommandDef` keyed to Enter whose `execute` calls `editorRef.current?.focus()`. RESOLVED: `AiPanelFocusScope` now accepts a `commands?: readonly CommandDef[]` prop and forwards it to `<FocusScope commands={...}>`. `AiPromptComposer` builds a `ui.ai-panel.composer.drillIn` `CommandDef` (`keys: { cua/vim/emacs: \"Enter\" }`, `execute` calls `editorRef.current?.focus()`) and passes it to the CM6 body's `AiPanelFocusScope`.\n+- [x] Use the shared `TextEditor` primitive's handle for the focus call — `editorRef` is already a `TextEditorHandle` (`@/components/fields/text-editor`), and `TextEditorHandle.focus()` calls the underlying `view.focus()`. Do NOT reach into a raw CodeMirror `EditorView` directly. (The composer already renders `<TextEditor>`, not hand-rolled CM6 — keep it that way.) RESOLVED: the drill-in command's `execute` calls `editorRef.current?.focus()` — the existing `TextEditorHandle`. No raw `EditorView` access added; the composer still renders `<TextEditor>`.\n\n ### Tests\n-- [ ] Add/extend an `ai-panel.spatial.test.tsx` test that drives the drill-in (Enter on the focused `ui:ai-panel.composer` scope) and asserts the CM6 prompt (`[role=\"textbox\"][aria-label=\"Message the AI agent\"]`) actually receives DOM focus — not just that the scope is registered.\n-- [ ] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green.\n+- [x] Add/extend an `ai-panel.spatial.test.tsx` test that drives the drill-in (Enter on the focused `ui:ai-panel.composer` scope) and asserts the CM6 prompt (`[role=\"textbox\"][aria-label=\"Message the AI agent\"]`) actually receives DOM focus — not just that the scope is registered. DONE: added \"Enter on the focused composer leaf drives DOM focus into the CM6 prompt\" to `ai-panel.spatial.test.tsx` — mounts `AiPanel` inside `<AppShell>` so `KeybindingHandler` is live, seeds spatial focus on the composer leaf, fires a real `keyDown(Enter)`, and asserts `document.activeElement` is the CM6 prompt. Verified TDD-RED: the test fails when the `commands` prop is removed.\n+- [x] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green. DONE: 4 files / 43 tests pass; `npx tsc --noEmit` clean.\n","reverse_patch":"--- original\n+++ modified\n@@ -28,7 +28,7 @@\n ## Acceptance Criteria\n - [x] The surrounding bordered composer container is not a focus scope; only the CM6 body is `ui:ai-panel.composer` and the picker is `ui:ai-panel.model-selector`.\n - [x] `ui:ai-panel.composer` and `ui:ai-panel.model-selector` are siblings under the `ui:ai-panel` zone — neither nested inside the other.\n-- [x] Drilling into `ui:ai-panel.composer` (Enter) actually moves the editing cursor into the CM6 editor — see Review Findings.\n+- [ ] Drilling into `ui:ai-panel.composer` (Enter) actually moves the editing cursor into the CM6 editor — see Review Findings.\n - [x] The model picker is independently reachable as its own spatial-nav leaf.\n - [x] `ai.focus`, the Enter/Shift-Enter keymap, and the submit/stop control are unchanged.\n\n@@ -38,9 +38,9 @@\n ## Review Findings (2026-05-19 20:02)\n\n ### Blockers\n-- [x] `apps/kanban-app/ui/src/components/ai-prompt-composer.tsx` — drilling into the composer does not actually focus the CM6 editor. A bare `<FocusScope>` (via `AiPanelFocusScope`) only *registers* the scope as a nav target; landing on it and pressing Enter does NOT move the editing cursor into the editor. The established pattern is `FilterFormulaBarFocusable` in `apps/kanban-app/ui/src/components/perspective-tab-bar.tsx`: the `<FocusScope>` is given a `commands` prop holding a per-scope drill-in `CommandDef` (`id: \"...drillIn\"`, `keys: { cua/vim/emacs: \"Enter\" }`) whose `execute` calls `editorRef.current?.focus()` — that command is what actually drives the cursor in. The composer's CM6 scope has no such command, so drill-in is a no-op. Fix: (1) extend `AiPanelFocusScope` (`ai-panel-focus.tsx`) to accept and forward a `commands` prop to the underlying `FocusScope` — it currently does not; (2) in `AiPromptComposer`, pass the CM6 body's `AiPanelFocusScope` a `commands` array with a drill-in `CommandDef` keyed to Enter whose `execute` calls `editorRef.current?.focus()`. RESOLVED: `AiPanelFocusScope` now accepts a `commands?: readonly CommandDef[]` prop and forwards it to `<FocusScope commands={...}>`. `AiPromptComposer` builds a `ui.ai-panel.composer.drillIn` `CommandDef` (`keys: { cua/vim/emacs: \"Enter\" }`, `execute` calls `editorRef.current?.focus()`) and passes it to the CM6 body's `AiPanelFocusScope`.\n-- [x] Use the shared `TextEditor` primitive's handle for the focus call — `editorRef` is already a `TextEditorHandle` (`@/components/fields/text-editor`), and `TextEditorHandle.focus()` calls the underlying `view.focus()`. Do NOT reach into a raw CodeMirror `EditorView` directly. (The composer already renders `<TextEditor>`, not hand-rolled CM6 — keep it that way.) RESOLVED: the drill-in command's `execute` calls `editorRef.current?.focus()` — the existing `TextEditorHandle`. No raw `EditorView` access added; the composer still renders `<TextEditor>`.\n+- [ ] `apps/kanban-app/ui/src/components/ai-prompt-composer.tsx` — drilling into the composer does not actually focus the CM6 editor. A bare `<FocusScope>` (via `AiPanelFocusScope`) only *registers* the scope as a nav target; landing on it and pressing Enter does NOT move the editing cursor into the editor. The established pattern is `FilterFormulaBarFocusable` in `apps/kanban-app/ui/src/components/perspective-tab-bar.tsx`: the `<FocusScope>` is given a `commands` prop holding a per-scope drill-in `CommandDef` (`id: \"...drillIn\"`, `keys: { cua/vim/emacs: \"Enter\" }`) whose `execute` calls `editorRef.current?.focus()` — that command is what actually drives the cursor in. The composer's CM6 scope has no such command, so drill-in is a no-op. Fix: (1) extend `AiPanelFocusScope` (`ai-panel-focus.tsx`) to accept and forward a `commands` prop to the underlying `FocusScope` — it currently does not; (2) in `AiPromptComposer`, pass the CM6 body's `AiPanelFocusScope` a `commands` array with a drill-in `CommandDef` keyed to Enter whose `execute` calls `editorRef.current?.focus()`.\n+- [ ] Use the shared `TextEditor` primitive's handle for the focus call — `editorRef` is already a `TextEditorHandle` (`@/components/fields/text-editor`), and `TextEditorHandle.focus()` calls the underlying `view.focus()`. Do NOT reach into a raw CodeMirror `EditorView` directly. (The composer already renders `<TextEditor>`, not hand-rolled CM6 — keep it that way.)\n\n ### Tests\n-- [x] Add/extend an `ai-panel.spatial.test.tsx` test that drives the drill-in (Enter on the focused `ui:ai-panel.composer` scope) and asserts the CM6 prompt (`[role=\"textbox\"][aria-label=\"Message the AI agent\"]`) actually receives DOM focus — not just that the scope is registered. DONE: added \"Enter on the focused composer leaf drives DOM focus into the CM6 prompt\" to `ai-panel.spatial.test.tsx` — mounts `AiPanel` inside `<AppShell>` so `KeybindingHandler` is live, seeds spatial focus on the composer leaf, fires a real `keyDown(Enter)`, and asserts `document.activeElement` is the CM6 prompt. Verified TDD-RED: the test fails when the `commands` prop is removed.\n-- [x] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green. DONE: 4 files / 43 tests pass; `npx tsc --noEmit` clean.\n+- [ ] Add/extend an `ai-panel.spatial.test.tsx` test that drives the drill-in (Enter on the focused `ui:ai-panel.composer` scope) and asserts the CM6 prompt (`[role=\"textbox\"][aria-label=\"Message the AI agent\"]`) actually receives DOM focus — not just that the scope is registered.\n+- [ ] Run `cd apps/kanban-app/ui && npx vitest run src/components/ai-panel.test.tsx src/components/ai-prompt-composer.test.tsx src/components/ai-panel.spatial.test.tsx src/components/ai-panel-container.test.tsx` — all green.\n","transaction_id":null}
{"id":"01KS0Y19C4BDV8FAX4P7E9JAT3","timestamp":"2026-05-19T20:15:02.020078Z","op":"update","item_id":"01KS0WMB0MT0GVWMHF7BREGKWS","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n","transaction_id":null}
{"id":"01KS0Y5SKJZP78GG2VVY50RB20","timestamp":"2026-05-19T20:17:29.714863Z","op":"update","item_id":"01KS0WMB0MT0GVWMHF7BREGKWS","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffff8480\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffff8480\n+position_column: review\n+position_ordinal: '80'\n project: ai-panel\n title: 'AI panel: composer focus scope nests the model picker under the prompt'\n ---\n","transaction_id":null}
{"id":"01KS0Y5SKM3ZF3SQBTZPG718A1","timestamp":"2026-05-19T20:17:29.716233Z","entity_type":"task","entity_id":"01KS0WMB0MT0GVWMHF7BREGKWS","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffffff8480\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffffff8480\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}

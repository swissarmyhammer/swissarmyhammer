{"id":"01KTBW3Y16ZD32GFB26D4NXBFP","timestamp":"2026-06-05T12:28:52.134534Z","op":"create","item_id":"01KTBW3Y1604TCJ2PKD3FPH2ZD","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,31 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: c080\n+title: Fix \"server is unavailable\" callback failure on command dispatch (ui.setFocus, perspective.list)\n+---\n+## What\n+After the command-dispatch regression fix (commands_registry façade now populated from CommandService — `count=67`, no more `Unknown command`), dispatch reaches the CommandService but the plugin-callback step fails:\n+\n+```\n+ui.setFocus (kernel bridge) failed: Command failed: -32603: callback failed: plugin runtime error: server is unavailable({\"kind\":\"CallbackFailed\",...})\n+Failed to load perspectives: ... server is unavailable\n+```\n+\n+This is a DISTINCT root cause from the empty-registry bug — it lives in the plugin callback/server-lifecycle layer (`swissarmyhammer-plugin` `ServerUnavailable` = a registered in-process server was disposed out from under the caller), reached via `HostCallbackDispatcher::invoke` -> `PluginHost::invoke_plugin_callback`. It is NOT touched by the registry-façade fix and was previously MASKED because dispatch died earlier at the `lookup_undoable` \"Unknown command\" gate.\n+\n+## Evidence (live `cargo tauri dev`, OS log subsystem com.swissarmyhammer.kanban)\n+- `populated commands_registry façade from CommandService count=67` — registry fix working.\n+- ZERO `Unknown command: ...` entries.\n+- Persistent `server is unavailable` faults for `ui.setFocus` (kernel bridge + window focus) and `perspective.list` (`Failed to load perspectives`), still occurring ~90s after start (not a one-shot startup race). Boards DO load (3 restored, no `Failed to load board data`).\n+\n+## Where to look\n+- `crates/swissarmyhammer-plugin/src/error.rs::ServerUnavailable` (\"server was registered but has since been disposed\").\n+- `crates/swissarmyhammer-command-service/src/bootstrap.rs::HostCallbackDispatcher` -> `invoke_plugin_callback`.\n+- Per-board vs global host server lifecycle: `apps/kanban-app/src/state.rs` build_board_platform_inner (wire_command_services + expose_apphandle_modules + discover) and the per-board hot-reload watcher started right after discovery — suspect a snapshot/restore / reload disposing the `ui_state`/`focus`/`window`/`app` in-process servers the command callbacks reach back into. Log shows isolate \"Renaming server#NN to server#0\" churn at board-open time.\n+\n+## AC\n+- Dispatching `ui.setFocus` and `perspective.list` from the live app succeeds (no `server is unavailable`).\n+- No `Failed to load perspectives` in the OS log on board open.\n+- A real-path test (or e2e) that dispatches a callback-backed command through a per-board CommandService and asserts success.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,31 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: c080\n-title: Fix \"server is unavailable\" callback failure on command dispatch (ui.setFocus, perspective.list)\n----\n-## What\n-After the command-dispatch regression fix (commands_registry façade now populated from CommandService — `count=67`, no more `Unknown command`), dispatch reaches the CommandService but the plugin-callback step fails:\n-\n-```\n-ui.setFocus (kernel bridge) failed: Command failed: -32603: callback failed: plugin runtime error: server is unavailable({\"kind\":\"CallbackFailed\",...})\n-Failed to load perspectives: ... server is unavailable\n-```\n-\n-This is a DISTINCT root cause from the empty-registry bug — it lives in the plugin callback/server-lifecycle layer (`swissarmyhammer-plugin` `ServerUnavailable` = a registered in-process server was disposed out from under the caller), reached via `HostCallbackDispatcher::invoke` -> `PluginHost::invoke_plugin_callback`. It is NOT touched by the registry-façade fix and was previously MASKED because dispatch died earlier at the `lookup_undoable` \"Unknown command\" gate.\n-\n-## Evidence (live `cargo tauri dev`, OS log subsystem com.swissarmyhammer.kanban)\n-- `populated commands_registry façade from CommandService count=67` — registry fix working.\n-- ZERO `Unknown command: ...` entries.\n-- Persistent `server is unavailable` faults for `ui.setFocus` (kernel bridge + window focus) and `perspective.list` (`Failed to load perspectives`), still occurring ~90s after start (not a one-shot startup race). Boards DO load (3 restored, no `Failed to load board data`).\n-\n-## Where to look\n-- `crates/swissarmyhammer-plugin/src/error.rs::ServerUnavailable` (\"server was registered but has since been disposed\").\n-- `crates/swissarmyhammer-command-service/src/bootstrap.rs::HostCallbackDispatcher` -> `invoke_plugin_callback`.\n-- Per-board vs global host server lifecycle: `apps/kanban-app/src/state.rs` build_board_platform_inner (wire_command_services + expose_apphandle_modules + discover) and the per-board hot-reload watcher started right after discovery — suspect a snapshot/restore / reload disposing the `ui_state`/`focus`/`window`/`app` in-process servers the command callbacks reach back into. Log shows isolate \"Renaming server#NN to server#0\" churn at board-open time.\n-\n-## AC\n-- Dispatching `ui.setFocus` and `perspective.list` from the live app succeeds (no `server is unavailable`).\n-- No `Failed to load perspectives` in the OS log on board open.\n-- A real-path test (or e2e) that dispatches a callback-backed command through a per-board CommandService and asserts success.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTBWDMDQK07P681WCJ36KJW8","timestamp":"2026-06-05T12:34:09.975977Z","op":"update","item_id":"01KTBW3Y1604TCJ2PKD3FPH2ZD","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: c080\n+position_column: doing\n+position_ordinal: '80'\n title: Fix \"server is unavailable\" callback failure on command dispatch (ui.setFocus, perspective.list)\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: c080\n title: Fix \"server is unavailable\" callback failure on command dispatch (ui.setFocus, perspective.list)\n ---\n ## What\n","transaction_id":null}
{"id":"01KTBXVDBZEHD2FBVZV0Z05QFP","timestamp":"2026-06-05T12:59:10.079877Z","op":"update","item_id":"01KTBW3Y1604TCJ2PKD3FPH2ZD","forward_patch":"--- original\n+++ modified\n@@ -5,27 +5,18 @@\n position_ordinal: '80'\n title: Fix \"server is unavailable\" callback failure on command dispatch (ui.setFocus, perspective.list)\n ---\n-## What\n-After the command-dispatch regression fix (commands_registry façade now populated from CommandService — `count=67`, no more `Unknown command`), dispatch reaches the CommandService but the plugin-callback step fails:\n+## STATUS: lifecycle-disposal fix landed; live symptom has a DISTINCT root cause now tracked in 01KTBXTS8BH1VWPTS7AK7V9XFC\n\n-```\n-ui.setFocus (kernel bridge) failed: Command failed: -32603: callback failed: plugin runtime error: server is unavailable({\"kind\":\"CallbackFailed\",...})\n-Failed to load perspectives: ... server is unavailable\n-```\n-\n-This is a DISTINCT root cause from the empty-registry bug — it lives in the plugin callback/server-lifecycle layer (`swissarmyhammer-plugin` `ServerUnavailable` = a registered in-process server was disposed out from under the caller), reached via `HostCallbackDispatcher::invoke` -> `PluginHost::invoke_plugin_callback`. It is NOT touched by the registry-façade fix and was previously MASKED because dispatch died earlier at the `lookup_undoable` \"Unknown command\" gate.\n+### What was fixed here (real reload/lifecycle bug — the area this card scoped)\n+`PluginHost::dispose_handle` (`crates/swissarmyhammer-plugin/src/host.rs`) now returns a disposed `{ rust }` module to the host's available-modules table when its LAST holder unregisters. Previously, activating a `{ rust }` source MOVED the module out of the table (one-shot) and disposal NEVER restored it, so any unload/hot-reload of the activating plugin left the module gone forever: the v2 `register({rust:id})` failed with `UnknownServer`, the plugin's `load()` threw, the load rolled back, and the registered name stayed tombstoned → callers into it saw `ServerUnavailable`. This is exactly the \"loading/closing a board disposes servers a command needs\" hypothesis from the original card. Fix is refcount-correct (only on `UnregisterOutcome::Removed`, only for `ServerSource::Rust`, under one lock span) and regression-tested by `crates/swissarmyhammer-plugin/tests/rust_module_reactivation_e2e.rs` (RED before, GREEN after).\n\n-## Evidence (live `cargo tauri dev`, OS log subsystem com.swissarmyhammer.kanban)\n-- `populated commands_registry façade from CommandService count=67` — registry fix working.\n-- ZERO `Unknown command: ...` entries.\n-- Persistent `server is unavailable` faults for `ui.setFocus` (kernel bridge + window focus) and `perspective.list` (`Failed to load perspectives`), still occurring ~90s after start (not a one-shot startup race). Boards DO load (3 restored, no `Failed to load board data`).\n+### Why this did NOT clear the LIVE symptom (validated by running)\n+Ran `cargo tauri dev` with targeted DIAG tracing. The `views`/`focus` servers are ALIVE the entire time (activate OK in every host; `route` never resolves Disposed/Unknown; no reload/reconcile/unload events). The live `server is unavailable` faults come from `in_process.rs::map_rmcp_error` MASKING two real handler errors:\n+- `perspective.list` → `no ViewsBoardServices active on this tokio task` (-32603): the `views` board `tokio::task_local!` set by `try_dispatch_via_command_service` does NOT propagate across the command-callback round-trip into the plugin isolate + back through the host `bridge_runtime`, so the `views` toolsCall runs UNSCOPED.\n+- `ui.setFocus` → `invalid arguments for op \"set focus\": missing field 'fq'` (-32602): the focus arg plumbing drops `fq`.\n\n-## Where to look\n-- `crates/swissarmyhammer-plugin/src/error.rs::ServerUnavailable` (\"server was registered but has since been disposed\").\n-- `crates/swissarmyhammer-command-service/src/bootstrap.rs::HostCallbackDispatcher` -> `invoke_plugin_callback`.\n-- Per-board vs global host server lifecycle: `apps/kanban-app/src/state.rs` build_board_platform_inner (wire_command_services + expose_apphandle_modules + discover) and the per-board hot-reload watcher started right after discovery — suspect a snapshot/restore / reload disposing the `ui_state`/`focus`/`window`/`app` in-process servers the command callbacks reach back into. Log shows isolate \"Renaming server#NN to server#0\" churn at board-open time.\n+Both are a distinct, deeper layer (task-local scope propagation across the plugin callback bridge + focus arg plumbing + error masking) — out of this card's stated lifecycle scope. Full evidence and acceptance in 01KTBXTS8BH1VWPTS7AK7V9XFC.\n\n-## AC\n-- Dispatching `ui.setFocus` and `perspective.list` from the live app succeeds (no `server is unavailable`).\n-- No `Failed to load perspectives` in the OS log on board open.\n-- A real-path test (or e2e) that dispatches a callback-backed command through a per-board CommandService and asserts success.\n\\ No newline at end of file\n+---\n+## (Original description)\n+After the command-dispatch regression fix (commands_registry façade now populated — count=67, no more Unknown command), dispatch reaches the CommandService but the plugin-callback step fails with `server is unavailable` for `ui.setFocus` and `perspective.list`. Original hypothesis: a per-board vs global host lifecycle/refcount bug disposing the `focus`/`views` in-process servers a command callback reaches back into.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -5,18 +5,27 @@\n position_ordinal: '80'\n title: Fix \"server is unavailable\" callback failure on command dispatch (ui.setFocus, perspective.list)\n ---\n-## STATUS: lifecycle-disposal fix landed; live symptom has a DISTINCT root cause now tracked in 01KTBXTS8BH1VWPTS7AK7V9XFC\n+## What\n+After the command-dispatch regression fix (commands_registry façade now populated from CommandService — `count=67`, no more `Unknown command`), dispatch reaches the CommandService but the plugin-callback step fails:\n\n-### What was fixed here (real reload/lifecycle bug — the area this card scoped)\n-`PluginHost::dispose_handle` (`crates/swissarmyhammer-plugin/src/host.rs`) now returns a disposed `{ rust }` module to the host's available-modules table when its LAST holder unregisters. Previously, activating a `{ rust }` source MOVED the module out of the table (one-shot) and disposal NEVER restored it, so any unload/hot-reload of the activating plugin left the module gone forever: the v2 `register({rust:id})` failed with `UnknownServer`, the plugin's `load()` threw, the load rolled back, and the registered name stayed tombstoned → callers into it saw `ServerUnavailable`. This is exactly the \"loading/closing a board disposes servers a command needs\" hypothesis from the original card. Fix is refcount-correct (only on `UnregisterOutcome::Removed`, only for `ServerSource::Rust`, under one lock span) and regression-tested by `crates/swissarmyhammer-plugin/tests/rust_module_reactivation_e2e.rs` (RED before, GREEN after).\n+```\n+ui.setFocus (kernel bridge) failed: Command failed: -32603: callback failed: plugin runtime error: server is unavailable({\"kind\":\"CallbackFailed\",...})\n+Failed to load perspectives: ... server is unavailable\n+```\n\n-### Why this did NOT clear the LIVE symptom (validated by running)\n-Ran `cargo tauri dev` with targeted DIAG tracing. The `views`/`focus` servers are ALIVE the entire time (activate OK in every host; `route` never resolves Disposed/Unknown; no reload/reconcile/unload events). The live `server is unavailable` faults come from `in_process.rs::map_rmcp_error` MASKING two real handler errors:\n-- `perspective.list` → `no ViewsBoardServices active on this tokio task` (-32603): the `views` board `tokio::task_local!` set by `try_dispatch_via_command_service` does NOT propagate across the command-callback round-trip into the plugin isolate + back through the host `bridge_runtime`, so the `views` toolsCall runs UNSCOPED.\n-- `ui.setFocus` → `invalid arguments for op \"set focus\": missing field 'fq'` (-32602): the focus arg plumbing drops `fq`.\n+This is a DISTINCT root cause from the empty-registry bug — it lives in the plugin callback/server-lifecycle layer (`swissarmyhammer-plugin` `ServerUnavailable` = a registered in-process server was disposed out from under the caller), reached via `HostCallbackDispatcher::invoke` -> `PluginHost::invoke_plugin_callback`. It is NOT touched by the registry-façade fix and was previously MASKED because dispatch died earlier at the `lookup_undoable` \"Unknown command\" gate.\n\n-Both are a distinct, deeper layer (task-local scope propagation across the plugin callback bridge + focus arg plumbing + error masking) — out of this card's stated lifecycle scope. Full evidence and acceptance in 01KTBXTS8BH1VWPTS7AK7V9XFC.\n+## Evidence (live `cargo tauri dev`, OS log subsystem com.swissarmyhammer.kanban)\n+- `populated commands_registry façade from CommandService count=67` — registry fix working.\n+- ZERO `Unknown command: ...` entries.\n+- Persistent `server is unavailable` faults for `ui.setFocus` (kernel bridge + window focus) and `perspective.list` (`Failed to load perspectives`), still occurring ~90s after start (not a one-shot startup race). Boards DO load (3 restored, no `Failed to load board data`).\n\n----\n-## (Original description)\n-After the command-dispatch regression fix (commands_registry façade now populated — count=67, no more Unknown command), dispatch reaches the CommandService but the plugin-callback step fails with `server is unavailable` for `ui.setFocus` and `perspective.list`. Original hypothesis: a per-board vs global host lifecycle/refcount bug disposing the `focus`/`views` in-process servers a command callback reaches back into.\n\\ No newline at end of file\n+## Where to look\n+- `crates/swissarmyhammer-plugin/src/error.rs::ServerUnavailable` (\"server was registered but has since been disposed\").\n+- `crates/swissarmyhammer-command-service/src/bootstrap.rs::HostCallbackDispatcher` -> `invoke_plugin_callback`.\n+- Per-board vs global host server lifecycle: `apps/kanban-app/src/state.rs` build_board_platform_inner (wire_command_services + expose_apphandle_modules + discover) and the per-board hot-reload watcher started right after discovery — suspect a snapshot/restore / reload disposing the `ui_state`/`focus`/`window`/`app` in-process servers the command callbacks reach back into. Log shows isolate \"Renaming server#NN to server#0\" churn at board-open time.\n+\n+## AC\n+- Dispatching `ui.setFocus` and `perspective.list` from the live app succeeds (no `server is unavailable`).\n+- No `Failed to load perspectives` in the OS log on board open.\n+- A real-path test (or e2e) that dispatches a callback-backed command through a per-board CommandService and asserts success.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTC9SWHEF8WQCZY3XCNF3RWC","timestamp":"2026-06-05T16:28:02.990735Z","op":"update","item_id":"01KTBW3Y1604TCJ2PKD3FPH2ZD","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: fffffffffffffffffffffffffffffffffffff780\n title: Fix \"server is unavailable\" callback failure on command dispatch (ui.setFocus, perspective.list)\n ---\n ## STATUS: lifecycle-disposal fix landed; live symptom has a DISTINCT root cause now tracked in 01KTBXTS8BH1VWPTS7AK7V9XFC\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: fffffffffffffffffffffffffffffffffffff780\n+position_column: doing\n+position_ordinal: '80'\n title: Fix \"server is unavailable\" callback failure on command dispatch (ui.setFocus, perspective.list)\n ---\n ## STATUS: lifecycle-disposal fix landed; live symptom has a DISTINCT root cause now tracked in 01KTBXTS8BH1VWPTS7AK7V9XFC\n","transaction_id":null}

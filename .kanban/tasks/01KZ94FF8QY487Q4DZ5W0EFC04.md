---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kzdyays7r129mt7aaq135ada
  text: |-
    Picked up. Research done.

    DISCOVERY / DESIGN DIVERGENCE — the card says "A TreeSitterProbe", but that is not possible together with the card's own second bullet ("reuse the existing duplicate/similar detection"):

    - `TreeSitterProbe::run(&self, &TreeSitterProbeContext)` receives ONLY a path, the after parse, and the before parse. It has no index connection and no embedding corpus, by design ("plain logic over one changed file and its parse").
    - The only duplicate/similar detection in the repo is embedding-backed: `swissarmyhammer_code_context::find_duplicates_in(corpus, file, &options)` over `load_all_embedded_chunks(conn)`. There is no structural/tree-sitter clone detector anywhere in `swissarmyhammer-sem` or `swissarmyhammer-code-context`.
    - A clone-cluster probe also never reads the parse: cluster membership comes from indexed chunks, and "did the diff touch this member" comes from `FileChange`, not from the tree.

    So the probe lands in the FIRST probe family — one new `ProbeOp` variant beside `Callers`/`Duplicates`/`Similar`/`Complexity` in `review/probes.rs`, sharing the corpus `run_probes` already loads once. Everything the card asks for is unchanged by this: one catalog entry, declared by name in a validator's `probes:` list, rows on the existing `ProbeResult` path, no second similarity engine.

    What the existing `duplicates` probe gives, and why it is not enough:
    - per changed file: `find_duplicates_in` rows for EVERY match, touched or untouched, with no diff overlay.
    - `<changed-set>`: pairwise comparison of changed blocks against each other — by construction only members the change TOUCHED. It can never name an untouched sibling.

    Plan:
    - name `clone-siblings`, kind `Candidate` (the corpus is the index, so an empty list is not the fact "no clones exist" — the rule judges).
    - overlay: for each file under review, pull its clusters from `find_duplicates_in`; drop every member whose file is also under review; keep one row per remaining sibling site, holding the closest changed member it mirrors.
    - one aggregate `ProbeResult` on the `<changed-set>` target so it rides the existing `select_shared_probe_results` / `shared_probe_results` path (a target that is neither a file path nor a changed symbol is silently dropped by `probe_result_for_file`). `"<changed-set>"` is currently an unnamed literal in both `probes.rs` and `scope.rs` — naming it is a prerequisite of adding a third writer.
    - wire-up: add to `completeness` `probes:`, and teach `rules/invariant-propagation.md` (the sibling-sites rule) to consume the rows.
  timestamp: 2026-08-07T11:04:37.415941+00:00
- actor: claude-code
  id: 01kzdz7fr93deqrffw4emb2snf
  text: |-
    Implementation landed.

    Probe `clone-siblings` (kind Candidate), one `ProbeOp::CloneSiblings` catalog entry. It reads the clusters `find_duplicates_in` returns over the corpus `run_probes` already loads once, and only SUBTRACTS: a cluster member whose file is under review is a site the change reached; every survivor is a sibling it did not. One row per sibling site, deduped across changed files, holding the closest changed member it mirrors plus the similarity. Result rides `CHANGED_SET_TARGET` as batch-scoped shared evidence, because a cluster — and the dedup — span files.

    Named the previously-unnamed `"<changed-set>"` literal as `probes::CHANGED_SET_TARGET` and routed `scope::select_shared_probe_results` through it. Three writers of one magic string was one too many.

    TDD record:
    - RED: both tests written first; `cargo test` failed to compile (`CLONE_SIBLINGS_PROBE_NAME` not found).
    - GREEN: both pass through the real `scope_review` path.
    - Teeth proved by mutation: disabling the `under_review` subtraction makes the positive test report all THREE cluster members (src/first.rs, src/second.rs, src/third.rs) instead of one. Guard restored.

    Also flattened the overlay's inner loop into a `filter` so the max condition-nesting depth stays at 3, under the `complexity` probe's gate of 4.

    Wire-up: `completeness` declares the probe; `rules/invariant-propagation.md` (the sibling-sites rule) gained a "What the probe gives you" section with the row shape, the candidate-not-verdict framing, the "empty list is not a clean bill" caveat (the corpus is the index and lags the working tree), a citation instruction, and a look-alike exception. `builtin/mod.rs`'s `COMPLETENESS_PROBE_READERS` coverage guard gained the `(clone-siblings, invariant-propagation)` row, so declaring a probe and reading it stay one edit.

    Verification: `cargo test -p swissarmyhammer-validators` 492 + 2 doc tests green; `cargo test -p swissarmyhammer-tools -p swissarmyhammer-agent` all green; `cargo fmt --check` and `cargo clippy --all-targets` clean.

    ### implement — changed
    - evidence: 5 files — crates/swissarmyhammer-validators/src/review/probes.rs, crates/swissarmyhammer-validators/src/review/scope.rs, crates/swissarmyhammer-validators/src/builtin/mod.rs, builtin/validators/completeness/VALIDATOR.md, builtin/validators/completeness/rules/invariant-propagation.md
    - next: /review — note the TreeSitterProbe→ProbeOp divergence recorded in the prior comment
  timestamp: 2026-08-07T11:20:12.297706+00:00
- actor: claude-code
  id: 01kze075edem7d77kcbdpcvd0k
  text: |
    ### review — findings
    - scope: `review sha HEAD~1..HEAD` (7962ba71e), 9 validators attempted, 0 failed, 0 skipped
    - evidence: 1 finding, 1 confirmed, 0 refuted — crates/swissarmyhammer-validators/src/review/probes.rs:161
    - deviation judged: the card asked for a TreeSitterProbe; the implementer landed a `ProbeOp::CloneSiblings`. The reason holds. `TreeSitterProbeContext` carries only `path`, `after`, and `before` — no index connection and no embedding corpus — and the repo's only duplicate detection is the embedding-backed `find_duplicates_in` over indexed chunks. A TreeSitterProbe cannot reach the corpus, so it cannot satisfy the card's own second bullet ("reuse the existing duplicate/similar detection"). The ProbeOp form keeps one catalog entry, one `probes:` declaration, and the existing `ProbeResult` path, which is what the card specified. No finding is raised against the deviation.
    - next: fix the probes.rs:161 doc comment, then re-review. Task stays in `review`.
  timestamp: 2026-08-07T11:37:30.317260+00:00
- actor: claude-code
  id: 01kze07tes32n4awqz6gqbk44p
  text: |-
    ### finish iteration 1 — findings
    - implement: changed — 5 files; the clone-cluster overlay probe subtracts changed files from the find_duplicates_in clusters. Recorded deviation: landed as a ProbeOp, not a TreeSitterProbe, because TreeSitterProbeContext carries no index connection. The review judged that reasoning sound and raised no finding against it.
    - test: green — cargo nextest run --workspace 13747 passed, doc tests 0 failed, fmt clean, clippy clean
    - commit: 7962ba71e
    - review: findings — crates/swissarmyhammer-validators/src/review/probes.rs:161 (the doc comment says "exactly four entries" over a table that now holds five)
  timestamp: 2026-08-07T11:37:51.833426+00:00
- actor: claude-code
  id: 01kze0j9gt4k5bds1ybb6njn56
  text: |-
    Pulled back to `doing` for the one open finding: the doc comment above `CODE_CONTEXT_PROBES` said "exactly four entries" over a table that holds five.

    The finding is one example of a cause, so I swept the whole file for every comment that states a count or a list the code has outgrown, and preferred wording that cannot go stale. Seven comments changed, all doc/comment text — no code, no behavior:

    1. Module doc, "The catalog is data, not branching": dropped "There are five code_context probes — [`callers`], [`duplicates`], [`similar`], [`complexity`], `clone-siblings`". The markdown table directly below is already the roster, so the prose held a second copy of the same list plus a count. Now: "The code_context probes are described by a single static table (`CODE_CONTEXT_PROBES`) of [`ProbeCatalogEntry`] rows, one row per probe — the table below is that roster."
    2. `ProbeCatalogEntry::name`: "The semantic name validators declare (`callers`, `duplicates`, `similar`)" → "The semantic name validators declare in their `probes:` list." The list omitted `complexity`, `clone-siblings`, and every tree-sitter probe.
    3. `CODE_CONTEXT_PROBES` (the reported line): "exactly four entries" → "The code_context probes in this catalog table." Took the card's future-proof option, not the corrected number.
    4. `FileChange`: "Entity-bound probes (`callers`, `duplicates`, `similar`) ... file-bound probes (`complexity`)" → the same sentence with both parentheticals removed. The file-bound family is now `complexity` PLUS every tree-sitter probe, and the doc of `per_file_results` already says so.
    5. `ProbeRow`: "Fields are optional because the three ops surface different shapes" → "because the ops surface different shapes". `ProbeOp` holds six variants. The examples after it are examples, not a roster, so they stay.
    6. `ProbeResult::name`: "The probe's semantic name (`callers` / `duplicates` / `similar`)" → "The probe's semantic name, as the catalog keys it."
    7. `run_probes` inline comment: "The embedding-backed probes (`duplicates`, `similar`) compare against the whole indexed corpus" → the same sentence without the list. `needs_corpus` matches three ops — `Duplicates | Similar | CloneSiblings` — so the list contradicted the code two lines below it.

    Left alone deliberately:
    - The test fixture docs "The three near-copies ..." and "one edit applied at two of the cluster's three sites" — these agree with `CLONE_CLUSTER: [&str; 3]` and `EDITED_CLUSTER_MEMBERS: usize = 2`, and the count sits in the type on the next line.
    - "two modules key on the exact string" over `CHANGED_SET_TARGET` — probes and scope, still two.
    - `catalog_has_exactly_the_code_context_probes_with_their_kinds` — the test enumerates all five names on purpose; the assertion is what keeps that list honest.

    Verification: `cargo test -p swissarmyhammer-validators` 492 passed / 0 failed plus 2 doc tests; `cargo fmt --check` clean; `cargo clippy --all-targets` clean; `cargo doc --no-deps` reports no warning in `probes.rs` (the module doc's removed `[`callers`]`-style bracket links resolved to no item).
  timestamp: 2026-08-07T11:43:34.938628+00:00
- actor: claude-code
  id: 01kze0jegyzmnzvvwe63vpgh8g
  text: |-
    ### implement — changed
    - evidence: 1 file — crates/swissarmyhammer-validators/src/review/probes.rs (7 doc/comment blocks; finding at probes.rs:161 checked off)
    - next: /review — re-review the file, then the task returns to `review`
  timestamp: 2026-08-07T11:43:40.062424+00:00
depends_on:
- 01KZ94F228KKTWT5T9Y59VJJVY
position_column: doing
position_ordinal: '8480'
title: Clone-cluster overlay probe for completeness
---
A TreeSitterProbe that finds sibling sites the diff did NOT touch.

- Reuse the existing duplicate/similar detection to get clone clusters that include changed code.
- Overlay the diff on each cluster: report which cluster members the diff touched and which it left untouched.
- One ProbeRow per untouched sibling: its location, the touched member it mirrors, and the similarity evidence.

Wire-up:
- Add the probe to the `completeness` set's `probes:` list.
- Update the sibling-sites completeness rule to consume the rows: the siblings are found for you; judge whether each untouched one needed the change.

Acceptance:
- A fixture with 3 near-copies where the diff edits 2 yields one row naming the third.
- A change with no cluster membership yields no rows.

#tool-validators

## Review Findings (2026-08-07 06:30)

- [x] `crates/swissarmyhammer-validators/src/review/probes.rs:161` — Doc comment is outdated: says 'exactly four entries' but CODE_CONTEXT_PROBES now contains five entries after adding the clone-siblings probe. Update line 161 to: `/// The code_context probes — exactly five entries.` or make it future-proof: `/// The code_context probes in this catalog table.`.

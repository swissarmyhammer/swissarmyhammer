{"id":"01KN79G8KWHD88K1KYK2WCJ39C","timestamp":"2026-04-02T14:28:06.396614Z","entity_type":"task","entity_id":"01KN79G8KWH8B0K3SB2CS1EPH1","op":"create","changes":[["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\nThe file watcher in `kanban-app/src/watcher.rs` does not watch the `perspectives/` directory. External edits to perspective YAML files (or changes from commands) are invisible to the watcher. Perspectives need the same file change → event → re-fetch flow as entities.\n\n### Current state\n`WATCHED_SUBDIRS` at `watcher.rs:21-29` lists: `tasks`, `tags`, `columns`, `swimlanes`, `actors`, `boards`, `views`. No `perspectives`.\n\nThe `path_to_entity()` function (`watcher.rs:696-707`) maps directory names to entity types by stripping trailing 's'. For `perspectives/` → entity_type would be `\"perspective\"`.\n\n### Fix approach\n1. Add `\"perspectives\"` to `WATCHED_SUBDIRS`\n2. The existing `path_to_entity()` will derive entity_type `\"perspective\"` automatically\n3. The existing `resolve_change()` / `flush_and_emit()` / watcher callback will produce `entity-field-changed` / `entity-created` / `entity-removed` events with `entity_type: \"perspective\"`\n4. The frontend PerspectiveProvider (future card) will listen for these events and re-fetch\n\n### Files to modify\n- `kanban-app/src/watcher.rs` — add `\"perspectives\"` to `WATCHED_SUBDIRS` (line 21)\n\n### One line change, but needs tests\n\n## Acceptance Criteria\n- [ ] `WATCHED_SUBDIRS` includes `\"perspectives\"`\n- [ ] Watcher detects perspective YAML file creation/modification/deletion\n- [ ] Events emitted with `entity_type: \"perspective\"` and correct `id`\n- [ ] Existing entity watcher tests still pass\n\n## Tests\n- [ ] Add test in `kanban-app/src/watcher.rs` tests: `perspective_file_detected_as_entity` — create a file in `perspectives/01ABC.yaml`, call `flush_and_emit`, verify `EntityCreated` with entity_type \"perspective\"\n- [ ] Add test: modify perspective file → `EntityFieldChanged` with entity_type \"perspective\"\n- [ ] `cargo nextest run -p kanban-app` — all pass\n- [ ] Manual: edit perspective .yaml externally → watcher fires event"}],["depends_on",{"kind":"set","value":["01KN79CSWG20HKJ9W86CTQ68XE"]}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"9a80"}],["title",{"kind":"set","value":"Add perspectives to file watcher — same file change event flow as entities"}]],"transaction_id":"01KN79G4BK4Z66KTY2EH0B90Q7"}
{"id":"01KN79HB6SNSV41TS8T47NQFQY","timestamp":"2026-04-02T14:28:41.817298Z","entity_type":"task","entity_id":"01KN79G8KWH8B0K3SB2CS1EPH1","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,31 +1,40 @@\n ## What\n\n-The file watcher in `kanban-app/src/watcher.rs` does not watch the `perspectives/` directory. External edits to perspective YAML files (or changes from commands) are invisible to the watcher. Perspectives need the same file change → event → re-fetch flow as entities.\n-\n-### Current state\n-`WATCHED_SUBDIRS` at `watcher.rs:21-29` lists: `tasks`, `tags`, `columns`, `swimlanes`, `actors`, `boards`, `views`. No `perspectives`.\n+The file watcher has a hardcoded `WATCHED_SUBDIRS` list in `watcher.rs:21-29`. This is wrong — the store knows its own directory via `root()`. The watcher should watch whatever directories the registered stores manage. When a store is registered in `StoreContext`, its directory gets watched automatically.\n\n-The `path_to_entity()` function (`watcher.rs:696-707`) maps directory names to entity types by stripping trailing 's'. For `perspectives/` → entity_type would be `\"perspective\"`.\n+### Current problem\n+- `WATCHED_SUBDIRS = [\"tasks\", \"tags\", \"columns\", \"swimlanes\", \"actors\", \"boards\", \"views\"]`\n+- `\"perspectives\"` is missing — so perspective file changes are invisible\n+- Adding to the hardcoded list is the wrong fix — we'd have to update it every time a new store type is added\n\n ### Fix approach\n-1. Add `\"perspectives\"` to `WATCHED_SUBDIRS`\n-2. The existing `path_to_entity()` will derive entity_type `\"perspective\"` automatically\n-3. The existing `resolve_change()` / `flush_and_emit()` / watcher callback will produce `entity-field-changed` / `entity-created` / `entity-removed` events with `entity_type: \"perspective\"`\n-4. The frontend PerspectiveProvider (future card) will listen for these events and re-fetch\n+1. Remove `WATCHED_SUBDIRS` from `watcher.rs`\n+2. `start_watching()` takes a list of directories to watch (from StoreContext's registered stores)\n+3. `StoreContext` exposes `watched_roots()` returning all store `root()` paths\n+4. `BoardHandle::start_watcher()` in `state.rs` calls `store_context.watched_roots()` to get the directory list\n+5. `new_entity_cache()` also uses the store roots instead of the hardcoded list\n+6. `flush_and_emit()` scans store roots instead of hardcoded list\n+7. `path_to_entity()` derives entity_type from directory name (already works — strips trailing 's')\n\n-### Files to modify\n-- `kanban-app/src/watcher.rs` — add `\"perspectives\"` to `WATCHED_SUBDIRS` (line 21)\n+This way perspectives (and any future store types) are automatically watched when registered.\n\n-### One line change, but needs tests\n+### Files to modify\n+- `swissarmyhammer-store/src/context.rs` — add `watched_roots()` method\n+- `swissarmyhammer-store/src/erased.rs` — ensure `root()` is on `ErasedStore` (it already is)\n+- `kanban-app/src/watcher.rs` — remove `WATCHED_SUBDIRS`, change `start_watching()`, `new_entity_cache()`, `flush_and_emit()` to accept directory list\n+- `kanban-app/src/state.rs` — pass store roots to watcher functions\n\n ## Acceptance Criteria\n-- [ ] `WATCHED_SUBDIRS` includes `\"perspectives\"`\n-- [ ] Watcher detects perspective YAML file creation/modification/deletion\n-- [ ] Events emitted with `entity_type: \"perspective\"` and correct `id`\n-- [ ] Existing entity watcher tests still pass\n+- [ ] `WATCHED_SUBDIRS` constant removed\n+- [ ] Watcher watches directories from registered stores\n+- [ ] Perspective files automatically watched (no special code)\n+- [ ] Entity files still watched (same behavior)\n+- [ ] Adding a new store type automatically watches its directory\n+- [ ] All existing watcher tests pass (updated to use dynamic roots)\n\n ## Tests\n-- [ ] Add test in `kanban-app/src/watcher.rs` tests: `perspective_file_detected_as_entity` — create a file in `perspectives/01ABC.yaml`, call `flush_and_emit`, verify `EntityCreated` with entity_type \"perspective\"\n-- [ ] Add test: modify perspective file → `EntityFieldChanged` with entity_type \"perspective\"\n+- [ ] Existing watcher tests updated to use store-provided roots\n+- [ ] New test: perspective store registered → perspective directory watched → file change detected\n - [ ] `cargo nextest run -p kanban-app` — all pass\n+- [ ] `cargo nextest run --workspace` — no regressions\n - [ ] Manual: edit perspective .yaml externally → watcher fires event\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,40 +1,31 @@\n ## What\n\n-The file watcher has a hardcoded `WATCHED_SUBDIRS` list in `watcher.rs:21-29`. This is wrong — the store knows its own directory via `root()`. The watcher should watch whatever directories the registered stores manage. When a store is registered in `StoreContext`, its directory gets watched automatically.\n+The file watcher in `kanban-app/src/watcher.rs` does not watch the `perspectives/` directory. External edits to perspective YAML files (or changes from commands) are invisible to the watcher. Perspectives need the same file change → event → re-fetch flow as entities.\n\n-### Current problem\n-- `WATCHED_SUBDIRS = [\"tasks\", \"tags\", \"columns\", \"swimlanes\", \"actors\", \"boards\", \"views\"]`\n-- `\"perspectives\"` is missing — so perspective file changes are invisible\n-- Adding to the hardcoded list is the wrong fix — we'd have to update it every time a new store type is added\n+### Current state\n+`WATCHED_SUBDIRS` at `watcher.rs:21-29` lists: `tasks`, `tags`, `columns`, `swimlanes`, `actors`, `boards`, `views`. No `perspectives`.\n\n-### Fix approach\n-1. Remove `WATCHED_SUBDIRS` from `watcher.rs`\n-2. `start_watching()` takes a list of directories to watch (from StoreContext's registered stores)\n-3. `StoreContext` exposes `watched_roots()` returning all store `root()` paths\n-4. `BoardHandle::start_watcher()` in `state.rs` calls `store_context.watched_roots()` to get the directory list\n-5. `new_entity_cache()` also uses the store roots instead of the hardcoded list\n-6. `flush_and_emit()` scans store roots instead of hardcoded list\n-7. `path_to_entity()` derives entity_type from directory name (already works — strips trailing 's')\n+The `path_to_entity()` function (`watcher.rs:696-707`) maps directory names to entity types by stripping trailing 's'. For `perspectives/` → entity_type would be `\"perspective\"`.\n\n-This way perspectives (and any future store types) are automatically watched when registered.\n+### Fix approach\n+1. Add `\"perspectives\"` to `WATCHED_SUBDIRS`\n+2. The existing `path_to_entity()` will derive entity_type `\"perspective\"` automatically\n+3. The existing `resolve_change()` / `flush_and_emit()` / watcher callback will produce `entity-field-changed` / `entity-created` / `entity-removed` events with `entity_type: \"perspective\"`\n+4. The frontend PerspectiveProvider (future card) will listen for these events and re-fetch\n\n ### Files to modify\n-- `swissarmyhammer-store/src/context.rs` — add `watched_roots()` method\n-- `swissarmyhammer-store/src/erased.rs` — ensure `root()` is on `ErasedStore` (it already is)\n-- `kanban-app/src/watcher.rs` — remove `WATCHED_SUBDIRS`, change `start_watching()`, `new_entity_cache()`, `flush_and_emit()` to accept directory list\n-- `kanban-app/src/state.rs` — pass store roots to watcher functions\n+- `kanban-app/src/watcher.rs` — add `\"perspectives\"` to `WATCHED_SUBDIRS` (line 21)\n\n+### One line change, but needs tests\n+\n ## Acceptance Criteria\n-- [ ] `WATCHED_SUBDIRS` constant removed\n-- [ ] Watcher watches directories from registered stores\n-- [ ] Perspective files automatically watched (no special code)\n-- [ ] Entity files still watched (same behavior)\n-- [ ] Adding a new store type automatically watches its directory\n-- [ ] All existing watcher tests pass (updated to use dynamic roots)\n+- [ ] `WATCHED_SUBDIRS` includes `\"perspectives\"`\n+- [ ] Watcher detects perspective YAML file creation/modification/deletion\n+- [ ] Events emitted with `entity_type: \"perspective\"` and correct `id`\n+- [ ] Existing entity watcher tests still pass\n\n ## Tests\n-- [ ] Existing watcher tests updated to use store-provided roots\n-- [ ] New test: perspective store registered → perspective directory watched → file change detected\n+- [ ] Add test in `kanban-app/src/watcher.rs` tests: `perspective_file_detected_as_entity` — create a file in `perspectives/01ABC.yaml`, call `flush_and_emit`, verify `EntityCreated` with entity_type \"perspective\"\n+- [ ] Add test: modify perspective file → `EntityFieldChanged` with entity_type \"perspective\"\n - [ ] `cargo nextest run -p kanban-app` — all pass\n-- [ ] `cargo nextest run --workspace` — no regressions\n - [ ] Manual: edit perspective .yaml externally → watcher fires event\n\\ No newline at end of file\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Add perspectives to file watcher — same file change event flow as entities\n\\ No newline at end of file\n+Replace WATCHED_SUBDIRS with store-driven watching — stores watch themselves\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Replace WATCHED_SUBDIRS with store-driven watching — stores watch themselves\n\\ No newline at end of file\n+Add perspectives to file watcher — same file change event flow as entities\n\\ No newline at end of file\n"}]],"transaction_id":"01KN79HB6CKRH700T0AQ5E7FFT"}
{"id":"01KN7A01XM7FBZ2HGHB3VBV2G2","timestamp":"2026-04-02T14:36:43.828901Z","entity_type":"task","entity_id":"01KN79G8KWH8B0K3SB2CS1EPH1","op":"update","changes":[["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Replace WATCHED_SUBDIRS with store-driven watching — stores watch themselves\n\\ No newline at end of file\n+2. Replace WATCHED_SUBDIRS with store-driven watching — stores watch themselves\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-2. Replace WATCHED_SUBDIRS with store-driven watching — stores watch themselves\n\\ No newline at end of file\n+Replace WATCHED_SUBDIRS with store-driven watching — stores watch themselves\n\\ No newline at end of file\n"}]],"transaction_id":"01KN7A01X5XFTKRBZF3QPQB800"}
{"id":"01KN7C6ZM9SV0MY8PDZ01PNFKN","timestamp":"2026-04-02T15:15:28.009240Z","entity_type":"task","entity_id":"01KN79G8KWH8B0K3SB2CS1EPH1","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-9a80\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+9a80\n\\ No newline at end of file\n"}]],"transaction_id":"01KN7C6ZKYEZHV0WN4P2C418HS"}
{"id":"01KN7D02GEWBJHMQVAGG1N1W7M","timestamp":"2026-04-02T15:29:10.158164Z","entity_type":"task","entity_id":"01KN79G8KWH8B0K3SB2CS1EPH1","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}]],"transaction_id":"01KN7D00SMXC2ANTD5DFBMRSK0"}
{"id":"01KN7DBCNKMSW5E0R1XC9TYSHN","timestamp":"2026-04-02T15:35:21.011247Z","entity_type":"task","entity_id":"01KN79G8KWH8B0K3SB2CS1EPH1","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffffffffa380\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffa380\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]],"transaction_id":"01KN7DBAM36A2STKHMXESP47VB"}

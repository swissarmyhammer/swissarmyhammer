{"id":"01KZ934SNF9WHEV0RAZ3ZGRX8N","timestamp":"2026-08-05T13:52:26.287119Z","op":"create","item_id":"01KZ934SNEJ1TXNS2G9Q4909TF","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,23 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: fd80\n+title: Runner spec schema and loader for tool validators\n+---\n+Add the `tool` validator kind. A validator set can hold a `runners/` directory. Each runner is one YAML file that binds one tool to one language.\n+\n+The contract is `builtin/validators/README.md`. Implement that spec exactly.\n+\n+Work:\n+- Define one serde struct for the runner spec: name, project_types, file_extensions, scope (files|workspace), command, args, output (format, map, filter), exit.findings, doctor (check_command, check_version_command), install.commands, fixtures.\n+- Mirror the `OwnedLspServerSpec` pattern and its YAML loader.\n+- Load builtin runners with `include_dir!`. Do not hardcode file lists.\n+- Resolve with the existing precedence: builtin < user (~/.validators/) < project (./.validators/).\n+- Expose runners on `list validators` / `get validator` output.\n+\n+Acceptance:\n+- A runner YAML in any of the three layers loads and resolves by precedence.\n+- A malformed runner YAML reports one clear error and does not break the set.\n+\n+#tool-validators\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,23 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: fd80\n-title: Runner spec schema and loader for tool validators\n----\n-Add the `tool` validator kind. A validator set can hold a `runners/` directory. Each runner is one YAML file that binds one tool to one language.\n-\n-The contract is `builtin/validators/README.md`. Implement that spec exactly.\n-\n-Work:\n-- Define one serde struct for the runner spec: name, project_types, file_extensions, scope (files|workspace), command, args, output (format, map, filter), exit.findings, doctor (check_command, check_version_command), install.commands, fixtures.\n-- Mirror the `OwnedLspServerSpec` pattern and its YAML loader.\n-- Load builtin runners with `include_dir!`. Do not hardcode file lists.\n-- Resolve with the existing precedence: builtin < user (~/.validators/) < project (./.validators/).\n-- Expose runners on `list validators` / `get validator` output.\n-\n-Acceptance:\n-- A runner YAML in any of the three layers loads and resolves by precedence.\n-- A malformed runner YAML reports one clear error and does not break the set.\n-\n-#tool-validators\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KZ93ZMR6HMSY27PYP692PXS3","timestamp":"2026-08-05T14:07:05.990609Z","op":"update","item_id":"01KZ934SNEJ1TXNS2G9Q4909TF","forward_patch":"--- original\n+++ modified\n@@ -10,7 +10,9 @@\n The contract is `builtin/validators/README.md`. Implement that spec exactly.\n\n Work:\n-- Define one serde struct for the runner spec: name, project_types, file_extensions, scope (files|workspace), command, args, output (format, map, filter), exit.findings, doctor (check_command, check_version_command), install.commands, fixtures.\n+- Define one serde struct for the runner spec: name, match_globs, project_types, scope (files|workspace), command, args, output (format, map, filter), exit.findings, doctor (check_command, check_version_command), install.commands, fixtures.\n+- Runners match files with `match_globs` through the SAME matcher the validator sets and rules use today. Do not build a second matching mechanism. A runner never matches a file its set does not match.\n+- `project_types` is additive. Matching does not use it. Install and configuration key on it.\n - Mirror the `OwnedLspServerSpec` pattern and its YAML loader.\n - Load builtin runners with `include_dir!`. Do not hardcode file lists.\n - Resolve with the existing precedence: builtin < user (~/.validators/) < project (./.validators/).\n@@ -18,6 +20,7 @@\n\n Acceptance:\n - A runner YAML in any of the three layers loads and resolves by precedence.\n+- A runner glob match agrees with the existing rule match for the same globs (shared code path, proven by test).\n - A malformed runner YAML reports one clear error and does not break the set.\n\n #tool-validators\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -10,9 +10,7 @@\n The contract is `builtin/validators/README.md`. Implement that spec exactly.\n\n Work:\n-- Define one serde struct for the runner spec: name, match_globs, project_types, scope (files|workspace), command, args, output (format, map, filter), exit.findings, doctor (check_command, check_version_command), install.commands, fixtures.\n-- Runners match files with `match_globs` through the SAME matcher the validator sets and rules use today. Do not build a second matching mechanism. A runner never matches a file its set does not match.\n-- `project_types` is additive. Matching does not use it. Install and configuration key on it.\n+- Define one serde struct for the runner spec: name, project_types, file_extensions, scope (files|workspace), command, args, output (format, map, filter), exit.findings, doctor (check_command, check_version_command), install.commands, fixtures.\n - Mirror the `OwnedLspServerSpec` pattern and its YAML loader.\n - Load builtin runners with `include_dir!`. Do not hardcode file lists.\n - Resolve with the existing precedence: builtin < user (~/.validators/) < project (./.validators/).\n@@ -20,7 +18,6 @@\n\n Acceptance:\n - A runner YAML in any of the three layers loads and resolves by precedence.\n-- A runner glob match agrees with the existing rule match for the same globs (shared code path, proven by test).\n - A malformed runner YAML reports one clear error and does not break the set.\n\n #tool-validators\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KZ945AABJXAG4ZW9M86HM8P0","timestamp":"2026-08-05T14:10:11.915933Z","op":"update","item_id":"01KZ934SNEJ1TXNS2G9Q4909TF","forward_patch":"--- original\n+++ modified\n@@ -3,24 +3,23 @@\n - claude-code\n position_column: todo\n position_ordinal: fd80\n-title: Runner spec schema and loader for tool validators\n+title: 'Tool-rule schema: same rule metadata, added tool block'\n ---\n-Add the `tool` validator kind. A validator set can hold a `runners/` directory. Each runner is one YAML file that binds one tool to one language.\n+Add the `tool` rule kind. A tool rule is a normal rule file in `rules/` with the same core metadata, plus a `tool` block in frontmatter. There is NO separate runner file, directory, schema, or matcher.\n\n The contract is `builtin/validators/README.md`. Implement that spec exactly.\n\n Work:\n-- Define one serde struct for the runner spec: name, match_globs, project_types, scope (files|workspace), command, args, output (format, map, filter), exit.findings, doctor (check_command, check_version_command), install.commands, fixtures.\n-- Runners match files with `match_globs` through the SAME matcher the validator sets and rules use today. Do not build a second matching mechanism. A runner never matches a file its set does not match.\n-- `project_types` is additive. Matching does not use it. Install and configuration key on it.\n-- Mirror the `OwnedLspServerSpec` pattern and its YAML loader.\n-- Load builtin runners with `include_dir!`. Do not hardcode file lists.\n-- Resolve with the existing precedence: builtin < user (~/.validators/) < project (./.validators/).\n-- Expose runners on `list validators` / `get validator` output.\n+- Extend the existing rule frontmatter types in `swissarmyhammer-validators/src/validators/types.rs`. Add optional `tool` (scope files|workspace, command, args, output map/filter, exit.findings, doctor, install.commands) and optional `supersedes`.\n+- Matching REUSES `ValidatorMatch` (`match:` block, `files:` globs, `@file_groups` expansion). Do not build a second matcher.\n+- Allow a rule-level `match` that NARROWS the set's match (intersection). Today rules inherit and cannot override — change this to narrow-only. A rule never matches a file its set does not match.\n+- Add `project_types` as a new key on `ValidatorMatch`. Matching, install, and configuration key on it.\n+- Existing prompt rules parse unchanged. A rule with a `tool` block is a tool rule.\n+- Expose the tool block and supersedes on `list validators` / `get validator` output.\n\n Acceptance:\n-- A runner YAML in any of the three layers loads and resolves by precedence.\n-- A runner glob match agrees with the existing rule match for the same globs (shared code path, proven by test).\n-- A malformed runner YAML reports one clear error and does not break the set.\n+- A tool rule in any layer (builtin/user/project) loads by the existing precedence.\n+- A rule-level match intersects the set match, proven by test against the SAME matcher code path the sets use.\n+- A malformed tool block reports one clear error and does not break the set.\n\n #tool-validators\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -3,23 +3,24 @@\n - claude-code\n position_column: todo\n position_ordinal: fd80\n-title: 'Tool-rule schema: same rule metadata, added tool block'\n+title: Runner spec schema and loader for tool validators\n ---\n-Add the `tool` rule kind. A tool rule is a normal rule file in `rules/` with the same core metadata, plus a `tool` block in frontmatter. There is NO separate runner file, directory, schema, or matcher.\n+Add the `tool` validator kind. A validator set can hold a `runners/` directory. Each runner is one YAML file that binds one tool to one language.\n\n The contract is `builtin/validators/README.md`. Implement that spec exactly.\n\n Work:\n-- Extend the existing rule frontmatter types in `swissarmyhammer-validators/src/validators/types.rs`. Add optional `tool` (scope files|workspace, command, args, output map/filter, exit.findings, doctor, install.commands) and optional `supersedes`.\n-- Matching REUSES `ValidatorMatch` (`match:` block, `files:` globs, `@file_groups` expansion). Do not build a second matcher.\n-- Allow a rule-level `match` that NARROWS the set's match (intersection). Today rules inherit and cannot override — change this to narrow-only. A rule never matches a file its set does not match.\n-- Add `project_types` as a new key on `ValidatorMatch`. Matching, install, and configuration key on it.\n-- Existing prompt rules parse unchanged. A rule with a `tool` block is a tool rule.\n-- Expose the tool block and supersedes on `list validators` / `get validator` output.\n+- Define one serde struct for the runner spec: name, match_globs, project_types, scope (files|workspace), command, args, output (format, map, filter), exit.findings, doctor (check_command, check_version_command), install.commands, fixtures.\n+- Runners match files with `match_globs` through the SAME matcher the validator sets and rules use today. Do not build a second matching mechanism. A runner never matches a file its set does not match.\n+- `project_types` is additive. Matching does not use it. Install and configuration key on it.\n+- Mirror the `OwnedLspServerSpec` pattern and its YAML loader.\n+- Load builtin runners with `include_dir!`. Do not hardcode file lists.\n+- Resolve with the existing precedence: builtin < user (~/.validators/) < project (./.validators/).\n+- Expose runners on `list validators` / `get validator` output.\n\n Acceptance:\n-- A tool rule in any layer (builtin/user/project) loads by the existing precedence.\n-- A rule-level match intersects the set match, proven by test against the SAME matcher code path the sets use.\n-- A malformed tool block reports one clear error and does not break the set.\n+- A runner YAML in any of the three layers loads and resolves by precedence.\n+- A runner glob match agrees with the existing rule match for the same globs (shared code path, proven by test).\n+- A malformed runner YAML reports one clear error and does not break the set.\n\n #tool-validators\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KZ94A8MMKAN0ZS93N7NK7D3H","timestamp":"2026-08-05T14:12:54.036101Z","op":"update","item_id":"01KZ934SNEJ1TXNS2G9Q4909TF","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,8 @@\n ---\n assignees:\n - claude-code\n+depends_on:\n+- 01KZ9497ZJ3WRCAG1Z6YGT2RRE\n position_column: todo\n position_ordinal: fd80\n title: 'Tool-rule schema: same rule metadata, added tool block'\n@@ -11,9 +13,8 @@\n\n Work:\n - Extend the existing rule frontmatter types in `swissarmyhammer-validators/src/validators/types.rs`. Add optional `tool` (scope files|workspace, command, args, output map/filter, exit.findings, doctor, install.commands) and optional `supersedes`.\n-- Matching REUSES `ValidatorMatch` (`match:` block, `files:` globs, `@file_groups` expansion). Do not build a second matcher.\n+- Matching REUSES `ValidatorMatch` (`match:` block, `files:` globs, `@file_groups` expansion). Do not build a second matcher. The `project_types` match key is task ^ygt2rre, not this task.\n - Allow a rule-level `match` that NARROWS the set's match (intersection). Today rules inherit and cannot override — change this to narrow-only. A rule never matches a file its set does not match.\n-- Add `project_types` as a new key on `ValidatorMatch`. Matching, install, and configuration key on it.\n - Existing prompt rules parse unchanged. A rule with a `tool` block is a tool rule.\n - Expose the tool block and supersedes on `list validators` / `get validator` output.\n\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,6 @@\n ---\n assignees:\n - claude-code\n-depends_on:\n-- 01KZ9497ZJ3WRCAG1Z6YGT2RRE\n position_column: todo\n position_ordinal: fd80\n title: 'Tool-rule schema: same rule metadata, added tool block'\n@@ -13,8 +11,9 @@\n\n Work:\n - Extend the existing rule frontmatter types in `swissarmyhammer-validators/src/validators/types.rs`. Add optional `tool` (scope files|workspace, command, args, output map/filter, exit.findings, doctor, install.commands) and optional `supersedes`.\n-- Matching REUSES `ValidatorMatch` (`match:` block, `files:` globs, `@file_groups` expansion). Do not build a second matcher. The `project_types` match key is task ^ygt2rre, not this task.\n+- Matching REUSES `ValidatorMatch` (`match:` block, `files:` globs, `@file_groups` expansion). Do not build a second matcher.\n - Allow a rule-level `match` that NARROWS the set's match (intersection). Today rules inherit and cannot override — change this to narrow-only. A rule never matches a file its set does not match.\n+- Add `project_types` as a new key on `ValidatorMatch`. Matching, install, and configuration key on it.\n - Existing prompt rules parse unchanged. A rule with a `tool` block is a tool rule.\n - Expose the tool block and supersedes on `list validators` / `get validator` output.\n\n","transaction_id":null}
{"id":"01KZ94SXRXQ8HT35FK4XC7YN11","timestamp":"2026-08-05T14:21:27.197668Z","op":"update","item_id":"01KZ934SNEJ1TXNS2G9Q4909TF","forward_patch":"--- original\n+++ modified\n@@ -12,7 +12,13 @@\n The contract is `builtin/validators/README.md`. Implement that spec exactly.\n\n Work:\n-- Extend the existing rule frontmatter types in `swissarmyhammer-validators/src/validators/types.rs`. Add optional `tool` (scope files|workspace, command, args, output map/filter, exit.findings, doctor, install.commands) and optional `supersedes`.\n+- Extend the existing rule frontmatter types in `swissarmyhammer-validators/src/validators/types.rs`. Add optional `tool` (scope files|workspace, command, args, output, exit.findings, doctor, install.commands) and optional `supersedes`.\n+- The `output` key uses established formats, not a homegrown mapper:\n+  - Omitted = SARIF (the default; one built-in parser).\n+  - `format: gcc` = `file:line:col: message` lines (one generic parser).\n+  - `jq: <program>` = real jq via an embedded jq engine (use the `jaq` crate). Yields `{file, line, rule, message}` objects.\n+  - `regex: <pattern>` = named captures `file`, `line`, `rule`, `message`, per line.\n+  - `filter: {rules: [...]}` = keep only the named rule codes, applied after parsing.\n - Matching REUSES `ValidatorMatch` (`match:` block, `files:` globs, `@file_groups` expansion). Do not build a second matcher. The `project_types` match key is task ^ygt2rre, not this task.\n - Allow a rule-level `match` that NARROWS the set's match (intersection). Today rules inherit and cannot override — change this to narrow-only. A rule never matches a file its set does not match.\n - Existing prompt rules parse unchanged. A rule with a `tool` block is a tool rule.\n@@ -21,6 +27,7 @@\n Acceptance:\n - A tool rule in any layer (builtin/user/project) loads by the existing precedence.\n - A rule-level match intersects the set match, proven by test against the SAME matcher code path the sets use.\n+- Each output form (sarif, gcc, jq, regex) parses a sample into Finding fields, proven by test.\n - A malformed tool block reports one clear error and does not break the set.\n\n #tool-validators\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -12,13 +12,7 @@\n The contract is `builtin/validators/README.md`. Implement that spec exactly.\n\n Work:\n-- Extend the existing rule frontmatter types in `swissarmyhammer-validators/src/validators/types.rs`. Add optional `tool` (scope files|workspace, command, args, output, exit.findings, doctor, install.commands) and optional `supersedes`.\n-- The `output` key uses established formats, not a homegrown mapper:\n-  - Omitted = SARIF (the default; one built-in parser).\n-  - `format: gcc` = `file:line:col: message` lines (one generic parser).\n-  - `jq: <program>` = real jq via an embedded jq engine (use the `jaq` crate). Yields `{file, line, rule, message}` objects.\n-  - `regex: <pattern>` = named captures `file`, `line`, `rule`, `message`, per line.\n-  - `filter: {rules: [...]}` = keep only the named rule codes, applied after parsing.\n+- Extend the existing rule frontmatter types in `swissarmyhammer-validators/src/validators/types.rs`. Add optional `tool` (scope files|workspace, command, args, output map/filter, exit.findings, doctor, install.commands) and optional `supersedes`.\n - Matching REUSES `ValidatorMatch` (`match:` block, `files:` globs, `@file_groups` expansion). Do not build a second matcher. The `project_types` match key is task ^ygt2rre, not this task.\n - Allow a rule-level `match` that NARROWS the set's match (intersection). Today rules inherit and cannot override — change this to narrow-only. A rule never matches a file its set does not match.\n - Existing prompt rules parse unchanged. A rule with a `tool` block is a tool rule.\n@@ -27,7 +21,6 @@\n Acceptance:\n - A tool rule in any layer (builtin/user/project) loads by the existing precedence.\n - A rule-level match intersects the set match, proven by test against the SAME matcher code path the sets use.\n-- Each output form (sarif, gcc, jq, regex) parses a sample into Finding fields, proven by test.\n - A malformed tool block reports one clear error and does not break the set.\n\n #tool-validators\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KZ95210CGKXJZDGMFDCEXAB2","timestamp":"2026-08-05T14:25:52.652214Z","op":"update","item_id":"01KZ934SNEJ1TXNS2G9Q4909TF","forward_patch":"--- original\n+++ modified\n@@ -12,13 +12,9 @@\n The contract is `builtin/validators/README.md`. Implement that spec exactly.\n\n Work:\n-- Extend the existing rule frontmatter types in `swissarmyhammer-validators/src/validators/types.rs`. Add optional `tool` (scope files|workspace, command, args, output, exit.findings, doctor, install.commands) and optional `supersedes`.\n-- The `output` key uses established formats, not a homegrown mapper:\n-  - Omitted = SARIF (the default; one built-in parser).\n-  - `format: gcc` = `file:line:col: message` lines (one generic parser).\n-  - `jq: <program>` = real jq via an embedded jq engine (use the `jaq` crate). Yields `{file, line, rule, message}` objects.\n-  - `regex: <pattern>` = named captures `file`, `line`, `rule`, `message`, per line.\n-  - `filter: {rules: [...]}` = keep only the named rule codes, applied after parsing.\n+- Extend the existing rule frontmatter types in `swissarmyhammer-validators/src/validators/types.rs`. Add optional `tool` and optional `supersedes`.\n+- The `tool` block is small: `scope` (files|workspace), `run` (a shell script — the pipeline IS the mapping, like skills embed shell), `doctor` (check_command, check_version_command), `install.commands`. There is NO output/format/jq/regex/filter configuration and NO exit.findings key.\n+- Stdout contract of `run`: one finding per line, either `path:line: message` or a `jq -c` style JSON object `{file, line, message}`. Empty stdout = clean. Exit 0 = judged. Nonzero exit = tool broke.\n - Matching REUSES `ValidatorMatch` (`match:` block, `files:` globs, `@file_groups` expansion). Do not build a second matcher. The `project_types` match key is task ^ygt2rre, not this task.\n - Allow a rule-level `match` that NARROWS the set's match (intersection). Today rules inherit and cannot override — change this to narrow-only. A rule never matches a file its set does not match.\n - Existing prompt rules parse unchanged. A rule with a `tool` block is a tool rule.\n@@ -27,7 +23,7 @@\n Acceptance:\n - A tool rule in any layer (builtin/user/project) loads by the existing precedence.\n - A rule-level match intersects the set match, proven by test against the SAME matcher code path the sets use.\n-- Each output form (sarif, gcc, jq, regex) parses a sample into Finding fields, proven by test.\n+- Both stdout line shapes parse into Finding fields, proven by test.\n - A malformed tool block reports one clear error and does not break the set.\n\n #tool-validators\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -12,9 +12,13 @@\n The contract is `builtin/validators/README.md`. Implement that spec exactly.\n\n Work:\n-- Extend the existing rule frontmatter types in `swissarmyhammer-validators/src/validators/types.rs`. Add optional `tool` and optional `supersedes`.\n-- The `tool` block is small: `scope` (files|workspace), `run` (a shell script — the pipeline IS the mapping, like skills embed shell), `doctor` (check_command, check_version_command), `install.commands`. There is NO output/format/jq/regex/filter configuration and NO exit.findings key.\n-- Stdout contract of `run`: one finding per line, either `path:line: message` or a `jq -c` style JSON object `{file, line, message}`. Empty stdout = clean. Exit 0 = judged. Nonzero exit = tool broke.\n+- Extend the existing rule frontmatter types in `swissarmyhammer-validators/src/validators/types.rs`. Add optional `tool` (scope files|workspace, command, args, output, exit.findings, doctor, install.commands) and optional `supersedes`.\n+- The `output` key uses established formats, not a homegrown mapper:\n+  - Omitted = SARIF (the default; one built-in parser).\n+  - `format: gcc` = `file:line:col: message` lines (one generic parser).\n+  - `jq: <program>` = real jq via an embedded jq engine (use the `jaq` crate). Yields `{file, line, rule, message}` objects.\n+  - `regex: <pattern>` = named captures `file`, `line`, `rule`, `message`, per line.\n+  - `filter: {rules: [...]}` = keep only the named rule codes, applied after parsing.\n - Matching REUSES `ValidatorMatch` (`match:` block, `files:` globs, `@file_groups` expansion). Do not build a second matcher. The `project_types` match key is task ^ygt2rre, not this task.\n - Allow a rule-level `match` that NARROWS the set's match (intersection). Today rules inherit and cannot override — change this to narrow-only. A rule never matches a file its set does not match.\n - Existing prompt rules parse unchanged. A rule with a `tool` block is a tool rule.\n@@ -23,7 +27,7 @@\n Acceptance:\n - A tool rule in any layer (builtin/user/project) loads by the existing precedence.\n - A rule-level match intersects the set match, proven by test against the SAME matcher code path the sets use.\n-- Both stdout line shapes parse into Finding fields, proven by test.\n+- Each output form (sarif, gcc, jq, regex) parses a sample into Finding fields, proven by test.\n - A malformed tool block reports one clear error and does not break the set.\n\n #tool-validators\n\\ No newline at end of file\n","transaction_id":null}

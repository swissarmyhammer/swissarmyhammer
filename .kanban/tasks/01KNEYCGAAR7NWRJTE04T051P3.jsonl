{"id":"01KNEYCGABPQQKWGB09A8V7WRA","timestamp":"2026-04-05T13:47:44.331386Z","entity_type":"task","entity_id":"01KNEYCGAAR7NWRJTE04T051P3","op":"create","changes":[["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"**File:** swissarmyhammer-kanban/src/task_helpers.rs (task_entity_to_rich_json)\\n\\n**What:** In `list.rs` and `next.rs`, entities are enriched via `enrich_all_task_entities` which sets `ready`, `blocked_by`, and `blocks` on each entity. Then `task_entity_to_rich_json` is called, which re-computes these same three values from scratch using the O(N) helper functions `task_is_ready`, `task_blocked_by`, and `task_blocks`. This means each task's ready/blocked_by/blocks is computed twice -- once during enrichment and once during JSON serialization.\\n\\n**Why this matters:** (1) Performance: the O(N) batch enrichment was specifically built to avoid O(N^2) per-task scanning, but `task_entity_to_rich_json` re-introduces the O(N) per-task scan, negating the optimization. (2) Correctness risk: the enriched values and the re-computed values could diverge (see the missing-dep inconsistency in the blocker finding). The JSON output uses the re-computed values, making the enriched values dead writes for ready/blocked_by/blocks.\\n\\n**Suggestion:** Have `task_entity_to_rich_json` read the pre-enriched fields from the entity instead of re-computing them. For example: `result[\\\"ready\\\"] = entity.get(\\\"ready\\\").cloned().unwrap_or(json!(true))`. This makes the enrichment the single source of truth and eliminates the redundant O(N) work.\\n\\n**Verification:** cargo test -p swissarmyhammer-kanban -- task_helpers list next\""}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"8180"}],["title",{"kind":"set","value":"WARNING: task_entity_to_rich_json double-computes ready/blocked_by/blocks after enrichment"}]],"transaction_id":"01KNEYCEHK15SKWS0TXN6Z6429"}
{"id":"01KNEYCNYREZTBD3R9NSN4EFZQ","timestamp":"2026-04-05T13:47:50.104628Z","entity_type":"task","entity_id":"01KNEYCGAAR7NWRJTE04T051P3","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-**File:** swissarmyhammer-kanban/src/task_helpers.rs (task_entity_to_rich_json)\\n\\n**What:** In `list.rs` and `next.rs`, entities are enriched via `enrich_all_task_entities` which sets `ready`, `blocked_by`, and `blocks` on each entity. Then `task_entity_to_rich_json` is called, which re-computes these same three values from scratch using the O(N) helper functions `task_is_ready`, `task_blocked_by`, and `task_blocks`. This means each task's ready/blocked_by/blocks is computed twice -- once during enrichment and once during JSON serialization.\\n\\n**Why this matters:** (1) Performance: the O(N) batch enrichment was specifically built to avoid O(N^2) per-task scanning, but `task_entity_to_rich_json` re-introduces the O(N) per-task scan, negating the optimization. (2) Correctness risk: the enriched values and the re-computed values could diverge (see the missing-dep inconsistency in the blocker finding). The JSON output uses the re-computed values, making the enriched values dead writes for ready/blocked_by/blocks.\\n\\n**Suggestion:** Have `task_entity_to_rich_json` read the pre-enriched fields from the entity instead of re-computing them. For example: `result[\\\"ready\\\"] = entity.get(\\\"ready\\\").cloned().unwrap_or(json!(true))`. This makes the enrichment the single source of truth and eliminates the redundant O(N) work.\\n\\n**Verification:** cargo test -p swissarmyhammer-kanban -- task_helpers list next\"\n\\ No newline at end of file\n+**File:** swissarmyhammer-kanban/src/task_helpers.rs (task_entity_to_rich_json)\\n\\n**What:** In `list.rs` and `next.rs`, entities are enriched via `enrich_all_task_entities` which sets `ready`, `blocked_by`, and `blocks` on each entity. Then `task_entity_to_rich_json` is called, which re-computes these same three values from scratch using the O(N) helper functions `task_is_ready`, `task_blocked_by`, and `task_blocks`. This means each task's ready/blocked_by/blocks is computed twice -- once during enrichment and once during JSON serialization.\\n\\n**Why this matters:** (1) Performance: the O(N) batch enrichment was specifically built to avoid O(N^2) per-task scanning, but `task_entity_to_rich_json` re-introduces the O(N) per-task scan, negating the optimization. (2) Correctness risk: the enriched values and the re-computed values could diverge (see the missing-dep inconsistency in the blocker finding). The JSON output uses the re-computed values, making the enriched values dead writes for ready/blocked_by/blocks.\\n\\n**Suggestion:** Have `task_entity_to_rich_json` read the pre-enriched fields from the entity instead of re-computing them. For example: `result[\\\"ready\\\"] = entity.get(\\\"ready\\\").cloned().unwrap_or(json!(true))`. This makes the enrichment the single source of truth and eliminates the redundant O(N) work.\\n\\n**Verification:** cargo test -p swissarmyhammer-kanban -- task_helpers list next\" #review-finding\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-**File:** swissarmyhammer-kanban/src/task_helpers.rs (task_entity_to_rich_json)\\n\\n**What:** In `list.rs` and `next.rs`, entities are enriched via `enrich_all_task_entities` which sets `ready`, `blocked_by`, and `blocks` on each entity. Then `task_entity_to_rich_json` is called, which re-computes these same three values from scratch using the O(N) helper functions `task_is_ready`, `task_blocked_by`, and `task_blocks`. This means each task's ready/blocked_by/blocks is computed twice -- once during enrichment and once during JSON serialization.\\n\\n**Why this matters:** (1) Performance: the O(N) batch enrichment was specifically built to avoid O(N^2) per-task scanning, but `task_entity_to_rich_json` re-introduces the O(N) per-task scan, negating the optimization. (2) Correctness risk: the enriched values and the re-computed values could diverge (see the missing-dep inconsistency in the blocker finding). The JSON output uses the re-computed values, making the enriched values dead writes for ready/blocked_by/blocks.\\n\\n**Suggestion:** Have `task_entity_to_rich_json` read the pre-enriched fields from the entity instead of re-computing them. For example: `result[\\\"ready\\\"] = entity.get(\\\"ready\\\").cloned().unwrap_or(json!(true))`. This makes the enrichment the single source of truth and eliminates the redundant O(N) work.\\n\\n**Verification:** cargo test -p swissarmyhammer-kanban -- task_helpers list next\" #review-finding\n\\ No newline at end of file\n+**File:** swissarmyhammer-kanban/src/task_helpers.rs (task_entity_to_rich_json)\\n\\n**What:** In `list.rs` and `next.rs`, entities are enriched via `enrich_all_task_entities` which sets `ready`, `blocked_by`, and `blocks` on each entity. Then `task_entity_to_rich_json` is called, which re-computes these same three values from scratch using the O(N) helper functions `task_is_ready`, `task_blocked_by`, and `task_blocks`. This means each task's ready/blocked_by/blocks is computed twice -- once during enrichment and once during JSON serialization.\\n\\n**Why this matters:** (1) Performance: the O(N) batch enrichment was specifically built to avoid O(N^2) per-task scanning, but `task_entity_to_rich_json` re-introduces the O(N) per-task scan, negating the optimization. (2) Correctness risk: the enriched values and the re-computed values could diverge (see the missing-dep inconsistency in the blocker finding). The JSON output uses the re-computed values, making the enriched values dead writes for ready/blocked_by/blocks.\\n\\n**Suggestion:** Have `task_entity_to_rich_json` read the pre-enriched fields from the entity instead of re-computing them. For example: `result[\\\"ready\\\"] = entity.get(\\\"ready\\\").cloned().unwrap_or(json!(true))`. This makes the enrichment the single source of truth and eliminates the redundant O(N) work.\\n\\n**Verification:** cargo test -p swissarmyhammer-kanban -- task_helpers list next\"\n\\ No newline at end of file\n"}]],"transaction_id":"01KNEYCNYAJZ4B1QPVF0NF2VXR"}
{"id":"01KNEZJT4HKS6EPH74FTRQFQ09","timestamp":"2026-04-05T14:08:39.569630Z","entity_type":"task","entity_id":"01KNEYCGAAR7NWRJTE04T051P3","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}]],"transaction_id":"01KNEZJR5RV3R5DZ65BEG5W1BY"}
{"id":"01KNEZRQDC36XMZMH3T21ECZ2B","timestamp":"2026-04-05T14:11:53.388780Z","entity_type":"task","entity_id":"01KNEYCGAAR7NWRJTE04T051P3","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+ffffffffffffffffe080\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffe080\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]],"transaction_id":"01KNEZRMPKA92XSRSYYFHZ6NV2"}

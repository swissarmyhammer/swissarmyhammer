{"id":"01KP96M9GG2E5959JA98CASWER","timestamp":"2026-04-15T18:32:03.344165Z","op":"update","item_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXW7HHHB8HW76K3PXH3G34\n-position_column: todo\n-position_ordinal: a280\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial navigation algorithm in Rust: nearest-neighbor by direction'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXW7HHHB8HW76K3PXH3G34\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: a280\n project: spatial-nav\n title: 'Spatial navigation algorithm in Rust: nearest-neighbor by direction'\n ---\n","transaction_id":null}
{"id":"01KP96M9GGCF0KKCK89328ZGWD","timestamp":"2026-04-15T18:32:03.344828Z","entity_type":"task","entity_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-a280\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+a280\n\\ No newline at end of file\n"}]]}
{"id":"01KP997NZXDB78FCY5NB98KK9B","timestamp":"2026-04-15T19:17:35.869596Z","op":"update","item_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","forward_patch":"--- original\n+++ modified\n@@ -12,226 +12,42 @@\n\n Implement the spatial navigation algorithm in Rust as a pure function on `SpatialRegistry`. Informed by prior art from Android FocusFinder, W3C CSS Spatial Navigation, UWP XYFocus, and Norigin Spatial Navigation.\n\n-### Algorithm — `SpatialRegistry::navigate(key, direction) -> Result<()>`\n-\n-Looks up the key's rect, runs beam test + scoring against candidates in the active layer, updates `focused_key`, emits `focus-changed` event.\n-\n-**Two-phase candidate selection** (Android FocusFinder beam test):\n-\n-**Phase 1: Beam candidates** — A \"beam\" is the perpendicular band extending from the source in the nav direction. For Right, the beam spans `source.top..source.bottom` extending rightward. Candidates overlapping this band are \"in-beam.\"\n-\n-**Phase 2: Out-of-beam fallback** — If no in-beam candidates, all candidates in the correct direction are considered.\n-\n-**Directional filter**:\n-- `Right`: `candidate.left >= origin.right`\n-- `Left`: `candidate.right <= origin.left`\n-- `Down`: `candidate.top >= origin.bottom`\n-- `Up`: `candidate.bottom <= origin.top`\n-\n-**Scoring** — Android-style: `13 * major² + minor²`\n-\n-**Edge commands**: First (top-left-most), Last (bottom-right-most), RowStart/RowEnd (same-row filter).\n-\n-**Container-first search**: Search siblings in parent scope first, expand to full layer if empty.\n-\n-**Focus memory per layer**: LayerStack stores last-focused key per layer, restores on layer pop.\n-\n-### Files to create/modify\n-\n-**Rust** — `swissarmyhammer-commands/src/spatial.rs`:\n-- `Direction` enum (8 variants)\n-- `SpatialRegistry::navigate(&mut self, key, direction) -> Result<()>` with beam test + scoring\n-- `parent_scope: Option<String>` in `SpatialEntry` for container-first\n-- `last_focused: Option<String>` in `LayerEntry` for focus memory\n-\n-**React** — `kanban-app/ui/src/lib/entity-focus-context.tsx`:\n-- `broadcastNavCommand` stays synchronous. Calls `invoke(\"spatial_navigate\", { key, direction })` fire-and-forget.\n-\n-### Design decisions\n-- **Beam test first**: Android-validated, 10+ years.\n-- **13:1 squared ratio**: Android's production constant.\n-- **Container-first**: Prevents teleportation across the window.\n-- **Focus memory per layer**: Inspector close returns to exact previous card.\n-- **Layer boundaries are hard**: Nav cannot escape the active layer.\n-\n ### Subtasks\n-- [ ] Implement beam test: filter candidates into in-beam and out-of-beam sets\n-- [ ] Implement Android-style scoring: `13 * major² + minor²`\n-- [ ] Implement container-first search: parent scope siblings first, then full layer\n-- [ ] Implement edge commands (First, Last, RowStart, RowEnd)\n-- [ ] Add focus memory to LayerStack entries\n+- [x] Implement beam test: filter candidates into in-beam and out-of-beam sets\n+- [x] Implement Android-style scoring: `13 * major² + minor²`\n+- [x] Implement container-first search: parent scope siblings first, then full layer\n+- [x] Implement edge commands (First, Last, RowStart, RowEnd)\n+- [x] Add focus memory to LayerStack entries\n\n ## Acceptance Criteria\n-- [ ] In-beam candidates always preferred over out-of-beam\n-- [ ] Aligned candidates preferred over closer-but-diagonal (13:1 ratio)\n-- [ ] Container-first: nav stays in parent scope if siblings exist\n-- [ ] Container fallback: no sibling → expands to full layer\n-- [ ] Focus memory: layer pop restores last-focused key\n-- [ ] All 8 directions correct\n-- [ ] Hard layer boundary — entries in other layers excluded\n-- [ ] `cargo test` passes\n+- [x] In-beam candidates always preferred over out-of-beam\n+- [x] Aligned candidates preferred over closer-but-diagonal (13:1 ratio)\n+- [x] Container-first: nav stays in parent scope if siblings exist\n+- [x] Container fallback: no sibling → expands to full layer\n+- [x] Focus memory: layer pop restores last-focused key\n+- [x] All 8 directions correct\n+- [x] Hard layer boundary — entries in other layers excluded\n+- [x] `cargo test` passes (38 spatial tests)\n\n-## Tests\n+## Implementation\n\n-All tests are pure Rust with synthetic rects — no DOM, no React.\n+### `spatial_nav.rs` — pure algorithm module\n+- `Direction` enum: Up, Down, Left, Right, First, Last, RowStart, RowEnd (with FromStr, Display, Serialize/Deserialize)\n+- `find_target(source, candidates, direction)` — beam test + scoring for cardinals, positional sorting for edges\n+- `container_first_search(source, candidates, direction)` — scoped search then full fallback\n+- Helper functions: `is_in_direction`, `is_in_beam`, `score`, `overlaps_y_range`\n+- 11 unit tests covering beam, scoring, board layout, inspector fields, pills, containers, row nav, direction parsing\n\n-### Beam test\n+### `spatial_state.rs` — extended state machine\n+- `parent_scope: Option<String>` added to `SpatialEntry`\n+- `last_focused: Option<String>` added to `LayerEntry`\n+- `LayerStack::find_mut(key)` and `active_mut()` for focus memory\n+- `SpatialState::navigate(from_key, direction)` — filters to active layer, runs container-first search, updates focused_key with focus memory\n+- `focus()` saves last_focused on the layer owning the outgoing entry\n+- `remove_layer()` restores focus from the layer below's last_focused\n+- 4 new integration tests: layer isolation, unknown key error, no candidates noop, focus memory restore\n\n-```rust\n-#[test]\n-fn beam_candidate_preferred_over_closer_out_of_beam() {\n-    // source at (0, 100, 100, 50) — a 100x50 rect, top-left at (0,100)\n-    // candidate A at (150, 120, 50, 30) — in beam (y overlap with source), 50px right\n-    // candidate B at (110, 10, 50, 30) — out of beam (y=10, above source), only 10px right\n-    // nav.right → expect A (in-beam wins over closer out-of-beam)\n-}\n-```\n-\n-### Scoring\n-\n-```rust\n-#[test]\n-fn aligned_candidate_beats_closer_diagonal() {\n-    // source at (0, 0, 100, 50)\n-    // candidate A at (200, 0, 100, 50) — aligned, 100px right\n-    //   score = 13 * 100² + 0² = 130_000\n-    // candidate B at (110, 200, 100, 50) — diagonal, 10px right but 175px down\n-    //   score = 13 * 10² + 175² = 1_300 + 30_625 = 31_925\n-    // Both in-beam? B is out of beam (y=200 > source.bottom=50)\n-    // So only A is in beam → A wins by beam, not even by score\n-}\n-\n-#[test]\n-fn scoring_tiebreak_among_in_beam_candidates() {\n-    // source at (0, 0, 100, 100)\n-    // candidate A at (200, 0, 100, 100) — aligned, 100px right\n-    //   score = 13 * 100² + 0² = 130_000\n-    // candidate B at (150, 20, 100, 100) — aligned, 50px right, 20px down\n-    //   score = 13 * 50² + 20² = 32_500 + 400 = 32_900\n-    // Both in-beam → B wins (lower score)\n-}\n-```\n-\n-### Realistic board layout — 3 columns × varying cards\n-\n-```rust\n-#[test]\n-fn board_layout_3_columns() {\n-    // Column 0 header at (0, 0, 200, 40), cards at (0, 50, 200, 60), (0, 120, 200, 60), ...\n-    // Column 1 header at (210, 0, 200, 40), cards at (210, 50, 200, 60), (210, 120, 200, 60), ...\n-    // Column 2 header at (420, 0, 200, 40), 3 cards only\n-    //\n-    // Verify:\n-    // - nav.down from col0.card[0] → col0.card[1]\n-    // - nav.right from col0.card[0] → col1.card[0] (beam-aligned)\n-    // - nav.right from col0.card[4] → col2.card[2] (clamped: col2 only has 3 cards, nearest is last)\n-    // - nav.up from col0.card[0] → col0.header\n-    // - nav.right from col2.card[2] → None (no column to the right)\n-    // - nav.first → col0.header (top-left-most)\n-    // - nav.last → col2.card[2] (bottom-right-most)\n-}\n-\n-#[test]\n-fn empty_column_nav_right_lands_on_header() {\n-    // Column 0 with 5 cards, Column 1 with header only (no cards)\n-    // nav.right from col0.card[2] → col1.header (only element in col1's x-range)\n-}\n-```\n-\n-### Inspector layout — stacked field rows\n-\n-```rust\n-#[test]\n-fn inspector_8_stacked_fields() {\n-    // 8 field rows at (0, 0, 300, 35), (0, 40, 300, 35), ..., (0, 280, 300, 35)\n-    // nav.down from field[0] → field[1]\n-    // nav.up from field[7] → field[6]\n-    // nav.first → field[0]\n-    // nav.last → field[7]\n-    // nav.left from field[3] → None (nothing to the left)\n-}\n-```\n-\n-### Pill navigation within a field row\n-\n-```rust\n-#[test]\n-fn pill_horizontal_navigation() {\n-    // Field label at (0, 0, 100, 30)\n-    // Pill A at (110, 5, 60, 20), Pill B at (180, 5, 60, 20), Pill C at (250, 5, 60, 20)\n-    // nav.right from label → Pill A (nearest right, in beam)\n-    // nav.right from Pill A → Pill B\n-    // nav.left from Pill A → label\n-    // nav.left from Pill C → Pill B\n-}\n-```\n-\n-### Layer isolation\n-\n-```rust\n-#[test]\n-fn navigate_only_sees_active_layer() {\n-    // window layer: card at (0, 0, 200, 60)\n-    // inspector layer (active): field at (300, 0, 200, 35), field at (300, 40, 200, 35)\n-    // nav.right from inspector.field[0] → None (card is in window layer, invisible)\n-    // nav.down from inspector.field[0] → inspector.field[1]\n-}\n-```\n-\n-### Container-first search\n-\n-```rust\n-#[test]\n-fn container_first_stays_in_parent_scope() {\n-    // Toolbar row: button A at (0, 0, 80, 30), button B at (90, 0, 80, 30)\n-    //   parent_scope = \"toolbar\"\n-    // Board card at (0, 40, 200, 60)\n-    //   parent_scope = \"column:todo\"\n-    // nav.right from button A → button B (same parent scope)\n-    // NOT board card (which is closer vertically but different scope)\n-}\n-\n-#[test]\n-fn container_fallback_when_no_sibling() {\n-    // Toolbar row: single button at (0, 0, 80, 30), parent_scope = \"toolbar\"\n-    // Board card at (0, 40, 200, 60), parent_scope = \"column:todo\"\n-    // nav.down from button → board card (no sibling in toolbar below, falls through to layer)\n-}\n-```\n-\n-### Focus memory\n-\n-```rust\n-#[test]\n-fn layer_pop_restores_last_focused() {\n-    // window layer: card-A focused\n-    // push inspector layer, focus field-1, then field-3\n-    // pop inspector layer\n-    // → focused_key restored to card-A (window layer's last_focused)\n-}\n-```\n-\n-### Edge cases\n-\n-```rust\n-#[test]\n-fn navigate_with_unknown_key_returns_error() {\n-    // spatial_navigate(\"nonexistent\", Right) → Err\n-}\n-\n-#[test]\n-fn navigate_with_no_candidates_is_noop() {\n-    // Single entry in layer, nav.right → no event emitted, focused_key unchanged\n-}\n-\n-#[test]\n-fn navigate_rowstart_rowend() {\n-    // 3x3 grid: row 0 at y=0, row 1 at y=50, row 2 at y=100\n-    // Focused on (1,1) — center of grid\n-    // nav.rowStart → (0,1) — leftmost in same row\n-    // nav.rowEnd → (2,1) — rightmost in same row\n-}\n-```\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+### Tauri command\n+- `spatial_navigate` — real implementation parsing Direction, calling navigate, emitting focus-changed event, returning target moniker\n+- `spatial_register` — now accepts `parent_scope`\n+- `spatial_pop_layer` — now emits focus-changed when restoring focus\"\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -12,42 +12,226 @@\n\n Implement the spatial navigation algorithm in Rust as a pure function on `SpatialRegistry`. Informed by prior art from Android FocusFinder, W3C CSS Spatial Navigation, UWP XYFocus, and Norigin Spatial Navigation.\n\n+### Algorithm — `SpatialRegistry::navigate(key, direction) -> Result<()>`\n+\n+Looks up the key's rect, runs beam test + scoring against candidates in the active layer, updates `focused_key`, emits `focus-changed` event.\n+\n+**Two-phase candidate selection** (Android FocusFinder beam test):\n+\n+**Phase 1: Beam candidates** — A \"beam\" is the perpendicular band extending from the source in the nav direction. For Right, the beam spans `source.top..source.bottom` extending rightward. Candidates overlapping this band are \"in-beam.\"\n+\n+**Phase 2: Out-of-beam fallback** — If no in-beam candidates, all candidates in the correct direction are considered.\n+\n+**Directional filter**:\n+- `Right`: `candidate.left >= origin.right`\n+- `Left`: `candidate.right <= origin.left`\n+- `Down`: `candidate.top >= origin.bottom`\n+- `Up`: `candidate.bottom <= origin.top`\n+\n+**Scoring** — Android-style: `13 * major² + minor²`\n+\n+**Edge commands**: First (top-left-most), Last (bottom-right-most), RowStart/RowEnd (same-row filter).\n+\n+**Container-first search**: Search siblings in parent scope first, expand to full layer if empty.\n+\n+**Focus memory per layer**: LayerStack stores last-focused key per layer, restores on layer pop.\n+\n+### Files to create/modify\n+\n+**Rust** — `swissarmyhammer-commands/src/spatial.rs`:\n+- `Direction` enum (8 variants)\n+- `SpatialRegistry::navigate(&mut self, key, direction) -> Result<()>` with beam test + scoring\n+- `parent_scope: Option<String>` in `SpatialEntry` for container-first\n+- `last_focused: Option<String>` in `LayerEntry` for focus memory\n+\n+**React** — `kanban-app/ui/src/lib/entity-focus-context.tsx`:\n+- `broadcastNavCommand` stays synchronous. Calls `invoke(\"spatial_navigate\", { key, direction })` fire-and-forget.\n+\n+### Design decisions\n+- **Beam test first**: Android-validated, 10+ years.\n+- **13:1 squared ratio**: Android's production constant.\n+- **Container-first**: Prevents teleportation across the window.\n+- **Focus memory per layer**: Inspector close returns to exact previous card.\n+- **Layer boundaries are hard**: Nav cannot escape the active layer.\n+\n ### Subtasks\n-- [x] Implement beam test: filter candidates into in-beam and out-of-beam sets\n-- [x] Implement Android-style scoring: `13 * major² + minor²`\n-- [x] Implement container-first search: parent scope siblings first, then full layer\n-- [x] Implement edge commands (First, Last, RowStart, RowEnd)\n-- [x] Add focus memory to LayerStack entries\n+- [ ] Implement beam test: filter candidates into in-beam and out-of-beam sets\n+- [ ] Implement Android-style scoring: `13 * major² + minor²`\n+- [ ] Implement container-first search: parent scope siblings first, then full layer\n+- [ ] Implement edge commands (First, Last, RowStart, RowEnd)\n+- [ ] Add focus memory to LayerStack entries\n\n ## Acceptance Criteria\n-- [x] In-beam candidates always preferred over out-of-beam\n-- [x] Aligned candidates preferred over closer-but-diagonal (13:1 ratio)\n-- [x] Container-first: nav stays in parent scope if siblings exist\n-- [x] Container fallback: no sibling → expands to full layer\n-- [x] Focus memory: layer pop restores last-focused key\n-- [x] All 8 directions correct\n-- [x] Hard layer boundary — entries in other layers excluded\n-- [x] `cargo test` passes (38 spatial tests)\n+- [ ] In-beam candidates always preferred over out-of-beam\n+- [ ] Aligned candidates preferred over closer-but-diagonal (13:1 ratio)\n+- [ ] Container-first: nav stays in parent scope if siblings exist\n+- [ ] Container fallback: no sibling → expands to full layer\n+- [ ] Focus memory: layer pop restores last-focused key\n+- [ ] All 8 directions correct\n+- [ ] Hard layer boundary — entries in other layers excluded\n+- [ ] `cargo test` passes\n+\n+## Tests\n+\n+All tests are pure Rust with synthetic rects — no DOM, no React.\n+\n+### Beam test\n+\n+```rust\n+#[test]\n+fn beam_candidate_preferred_over_closer_out_of_beam() {\n+    // source at (0, 100, 100, 50) — a 100x50 rect, top-left at (0,100)\n+    // candidate A at (150, 120, 50, 30) — in beam (y overlap with source), 50px right\n+    // candidate B at (110, 10, 50, 30) — out of beam (y=10, above source), only 10px right\n+    // nav.right → expect A (in-beam wins over closer out-of-beam)\n+}\n+```\n+\n+### Scoring\n+\n+```rust\n+#[test]\n+fn aligned_candidate_beats_closer_diagonal() {\n+    // source at (0, 0, 100, 50)\n+    // candidate A at (200, 0, 100, 50) — aligned, 100px right\n+    //   score = 13 * 100² + 0² = 130_000\n+    // candidate B at (110, 200, 100, 50) — diagonal, 10px right but 175px down\n+    //   score = 13 * 10² + 175² = 1_300 + 30_625 = 31_925\n+    // Both in-beam? B is out of beam (y=200 > source.bottom=50)\n+    // So only A is in beam → A wins by beam, not even by score\n+}\n+\n+#[test]\n+fn scoring_tiebreak_among_in_beam_candidates() {\n+    // source at (0, 0, 100, 100)\n+    // candidate A at (200, 0, 100, 100) — aligned, 100px right\n+    //   score = 13 * 100² + 0² = 130_000\n+    // candidate B at (150, 20, 100, 100) — aligned, 50px right, 20px down\n+    //   score = 13 * 50² + 20² = 32_500 + 400 = 32_900\n+    // Both in-beam → B wins (lower score)\n+}\n+```\n+\n+### Realistic board layout — 3 columns × varying cards\n+\n+```rust\n+#[test]\n+fn board_layout_3_columns() {\n+    // Column 0 header at (0, 0, 200, 40), cards at (0, 50, 200, 60), (0, 120, 200, 60), ...\n+    // Column 1 header at (210, 0, 200, 40), cards at (210, 50, 200, 60), (210, 120, 200, 60), ...\n+    // Column 2 header at (420, 0, 200, 40), 3 cards only\n+    //\n+    // Verify:\n+    // - nav.down from col0.card[0] → col0.card[1]\n+    // - nav.right from col0.card[0] → col1.card[0] (beam-aligned)\n+    // - nav.right from col0.card[4] → col2.card[2] (clamped: col2 only has 3 cards, nearest is last)\n+    // - nav.up from col0.card[0] → col0.header\n+    // - nav.right from col2.card[2] → None (no column to the right)\n+    // - nav.first → col0.header (top-left-most)\n+    // - nav.last → col2.card[2] (bottom-right-most)\n+}\n+\n+#[test]\n+fn empty_column_nav_right_lands_on_header() {\n+    // Column 0 with 5 cards, Column 1 with header only (no cards)\n+    // nav.right from col0.card[2] → col1.header (only element in col1's x-range)\n+}\n+```\n+\n+### Inspector layout — stacked field rows\n+\n+```rust\n+#[test]\n+fn inspector_8_stacked_fields() {\n+    // 8 field rows at (0, 0, 300, 35), (0, 40, 300, 35), ..., (0, 280, 300, 35)\n+    // nav.down from field[0] → field[1]\n+    // nav.up from field[7] → field[6]\n+    // nav.first → field[0]\n+    // nav.last → field[7]\n+    // nav.left from field[3] → None (nothing to the left)\n+}\n+```\n+\n+### Pill navigation within a field row\n+\n+```rust\n+#[test]\n+fn pill_horizontal_navigation() {\n+    // Field label at (0, 0, 100, 30)\n+    // Pill A at (110, 5, 60, 20), Pill B at (180, 5, 60, 20), Pill C at (250, 5, 60, 20)\n+    // nav.right from label → Pill A (nearest right, in beam)\n+    // nav.right from Pill A → Pill B\n+    // nav.left from Pill A → label\n+    // nav.left from Pill C → Pill B\n+}\n+```\n+\n+### Layer isolation\n+\n+```rust\n+#[test]\n+fn navigate_only_sees_active_layer() {\n+    // window layer: card at (0, 0, 200, 60)\n+    // inspector layer (active): field at (300, 0, 200, 35), field at (300, 40, 200, 35)\n+    // nav.right from inspector.field[0] → None (card is in window layer, invisible)\n+    // nav.down from inspector.field[0] → inspector.field[1]\n+}\n+```\n+\n+### Container-first search\n+\n+```rust\n+#[test]\n+fn container_first_stays_in_parent_scope() {\n+    // Toolbar row: button A at (0, 0, 80, 30), button B at (90, 0, 80, 30)\n+    //   parent_scope = \"toolbar\"\n+    // Board card at (0, 40, 200, 60)\n+    //   parent_scope = \"column:todo\"\n+    // nav.right from button A → button B (same parent scope)\n+    // NOT board card (which is closer vertically but different scope)\n+}\n+\n+#[test]\n+fn container_fallback_when_no_sibling() {\n+    // Toolbar row: single button at (0, 0, 80, 30), parent_scope = \"toolbar\"\n+    // Board card at (0, 40, 200, 60), parent_scope = \"column:todo\"\n+    // nav.down from button → board card (no sibling in toolbar below, falls through to layer)\n+}\n+```\n\n-## Implementation\n+### Focus memory\n\n-### `spatial_nav.rs` — pure algorithm module\n-- `Direction` enum: Up, Down, Left, Right, First, Last, RowStart, RowEnd (with FromStr, Display, Serialize/Deserialize)\n-- `find_target(source, candidates, direction)` — beam test + scoring for cardinals, positional sorting for edges\n-- `container_first_search(source, candidates, direction)` — scoped search then full fallback\n-- Helper functions: `is_in_direction`, `is_in_beam`, `score`, `overlaps_y_range`\n-- 11 unit tests covering beam, scoring, board layout, inspector fields, pills, containers, row nav, direction parsing\n+```rust\n+#[test]\n+fn layer_pop_restores_last_focused() {\n+    // window layer: card-A focused\n+    // push inspector layer, focus field-1, then field-3\n+    // pop inspector layer\n+    // → focused_key restored to card-A (window layer's last_focused)\n+}\n+```\n+\n+### Edge cases\n+\n+```rust\n+#[test]\n+fn navigate_with_unknown_key_returns_error() {\n+    // spatial_navigate(\"nonexistent\", Right) → Err\n+}\n+\n+#[test]\n+fn navigate_with_no_candidates_is_noop() {\n+    // Single entry in layer, nav.right → no event emitted, focused_key unchanged\n+}\n\n-### `spatial_state.rs` — extended state machine\n-- `parent_scope: Option<String>` added to `SpatialEntry`\n-- `last_focused: Option<String>` added to `LayerEntry`\n-- `LayerStack::find_mut(key)` and `active_mut()` for focus memory\n-- `SpatialState::navigate(from_key, direction)` — filters to active layer, runs container-first search, updates focused_key with focus memory\n-- `focus()` saves last_focused on the layer owning the outgoing entry\n-- `remove_layer()` restores focus from the layer below's last_focused\n-- 4 new integration tests: layer isolation, unknown key error, no candidates noop, focus memory restore\n+#[test]\n+fn navigate_rowstart_rowend() {\n+    // 3x3 grid: row 0 at y=0, row 1 at y=50, row 2 at y=100\n+    // Focused on (1,1) — center of grid\n+    // nav.rowStart → (0,1) — leftmost in same row\n+    // nav.rowEnd → (2,1) — rightmost in same row\n+}\n+```\n\n-### Tauri command\n-- `spatial_navigate` — real implementation parsing Direction, calling navigate, emitting focus-changed event, returning target moniker\n-- `spatial_register` — now accepts `parent_scope`\n-- `spatial_pop_layer` — now emits focus-changed when restoring focus\"\n\\ No newline at end of file\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KP997NZYRRZYF46PKS8R6N4A","timestamp":"2026-04-15T19:17:35.870128Z","entity_type":"task","entity_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -2,226 +2,42 @@\n\n Implement the spatial navigation algorithm in Rust as a pure function on `SpatialRegistry`. Informed by prior art from Android FocusFinder, W3C CSS Spatial Navigation, UWP XYFocus, and Norigin Spatial Navigation.\n\n-### Algorithm — `SpatialRegistry::navigate(key, direction) -> Result<()>`\n-\n-Looks up the key's rect, runs beam test + scoring against candidates in the active layer, updates `focused_key`, emits `focus-changed` event.\n-\n-**Two-phase candidate selection** (Android FocusFinder beam test):\n-\n-**Phase 1: Beam candidates** — A \"beam\" is the perpendicular band extending from the source in the nav direction. For Right, the beam spans `source.top..source.bottom` extending rightward. Candidates overlapping this band are \"in-beam.\"\n-\n-**Phase 2: Out-of-beam fallback** — If no in-beam candidates, all candidates in the correct direction are considered.\n-\n-**Directional filter**:\n-- `Right`: `candidate.left >= origin.right`\n-- `Left`: `candidate.right <= origin.left`\n-- `Down`: `candidate.top >= origin.bottom`\n-- `Up`: `candidate.bottom <= origin.top`\n-\n-**Scoring** — Android-style: `13 * major² + minor²`\n-\n-**Edge commands**: First (top-left-most), Last (bottom-right-most), RowStart/RowEnd (same-row filter).\n-\n-**Container-first search**: Search siblings in parent scope first, expand to full layer if empty.\n-\n-**Focus memory per layer**: LayerStack stores last-focused key per layer, restores on layer pop.\n-\n-### Files to create/modify\n-\n-**Rust** — `swissarmyhammer-commands/src/spatial.rs`:\n-- `Direction` enum (8 variants)\n-- `SpatialRegistry::navigate(&mut self, key, direction) -> Result<()>` with beam test + scoring\n-- `parent_scope: Option<String>` in `SpatialEntry` for container-first\n-- `last_focused: Option<String>` in `LayerEntry` for focus memory\n-\n-**React** — `kanban-app/ui/src/lib/entity-focus-context.tsx`:\n-- `broadcastNavCommand` stays synchronous. Calls `invoke(\"spatial_navigate\", { key, direction })` fire-and-forget.\n-\n-### Design decisions\n-- **Beam test first**: Android-validated, 10+ years.\n-- **13:1 squared ratio**: Android's production constant.\n-- **Container-first**: Prevents teleportation across the window.\n-- **Focus memory per layer**: Inspector close returns to exact previous card.\n-- **Layer boundaries are hard**: Nav cannot escape the active layer.\n-\n ### Subtasks\n-- [ ] Implement beam test: filter candidates into in-beam and out-of-beam sets\n-- [ ] Implement Android-style scoring: `13 * major² + minor²`\n-- [ ] Implement container-first search: parent scope siblings first, then full layer\n-- [ ] Implement edge commands (First, Last, RowStart, RowEnd)\n-- [ ] Add focus memory to LayerStack entries\n+- [x] Implement beam test: filter candidates into in-beam and out-of-beam sets\n+- [x] Implement Android-style scoring: `13 * major² + minor²`\n+- [x] Implement container-first search: parent scope siblings first, then full layer\n+- [x] Implement edge commands (First, Last, RowStart, RowEnd)\n+- [x] Add focus memory to LayerStack entries\n\n ## Acceptance Criteria\n-- [ ] In-beam candidates always preferred over out-of-beam\n-- [ ] Aligned candidates preferred over closer-but-diagonal (13:1 ratio)\n-- [ ] Container-first: nav stays in parent scope if siblings exist\n-- [ ] Container fallback: no sibling → expands to full layer\n-- [ ] Focus memory: layer pop restores last-focused key\n-- [ ] All 8 directions correct\n-- [ ] Hard layer boundary — entries in other layers excluded\n-- [ ] `cargo test` passes\n+- [x] In-beam candidates always preferred over out-of-beam\n+- [x] Aligned candidates preferred over closer-but-diagonal (13:1 ratio)\n+- [x] Container-first: nav stays in parent scope if siblings exist\n+- [x] Container fallback: no sibling → expands to full layer\n+- [x] Focus memory: layer pop restores last-focused key\n+- [x] All 8 directions correct\n+- [x] Hard layer boundary — entries in other layers excluded\n+- [x] `cargo test` passes (38 spatial tests)\n\n-## Tests\n+## Implementation\n\n-All tests are pure Rust with synthetic rects — no DOM, no React.\n+### `spatial_nav.rs` — pure algorithm module\n+- `Direction` enum: Up, Down, Left, Right, First, Last, RowStart, RowEnd (with FromStr, Display, Serialize/Deserialize)\n+- `find_target(source, candidates, direction)` — beam test + scoring for cardinals, positional sorting for edges\n+- `container_first_search(source, candidates, direction)` — scoped search then full fallback\n+- Helper functions: `is_in_direction`, `is_in_beam`, `score`, `overlaps_y_range`\n+- 11 unit tests covering beam, scoring, board layout, inspector fields, pills, containers, row nav, direction parsing\n\n-### Beam test\n+### `spatial_state.rs` — extended state machine\n+- `parent_scope: Option<String>` added to `SpatialEntry`\n+- `last_focused: Option<String>` added to `LayerEntry`\n+- `LayerStack::find_mut(key)` and `active_mut()` for focus memory\n+- `SpatialState::navigate(from_key, direction)` — filters to active layer, runs container-first search, updates focused_key with focus memory\n+- `focus()` saves last_focused on the layer owning the outgoing entry\n+- `remove_layer()` restores focus from the layer below's last_focused\n+- 4 new integration tests: layer isolation, unknown key error, no candidates noop, focus memory restore\n\n-```rust\n-#[test]\n-fn beam_candidate_preferred_over_closer_out_of_beam() {\n-    // source at (0, 100, 100, 50) — a 100x50 rect, top-left at (0,100)\n-    // candidate A at (150, 120, 50, 30) — in beam (y overlap with source), 50px right\n-    // candidate B at (110, 10, 50, 30) — out of beam (y=10, above source), only 10px right\n-    // nav.right → expect A (in-beam wins over closer out-of-beam)\n-}\n-```\n-\n-### Scoring\n-\n-```rust\n-#[test]\n-fn aligned_candidate_beats_closer_diagonal() {\n-    // source at (0, 0, 100, 50)\n-    // candidate A at (200, 0, 100, 50) — aligned, 100px right\n-    //   score = 13 * 100² + 0² = 130_000\n-    // candidate B at (110, 200, 100, 50) — diagonal, 10px right but 175px down\n-    //   score = 13 * 10² + 175² = 1_300 + 30_625 = 31_925\n-    // Both in-beam? B is out of beam (y=200 > source.bottom=50)\n-    // So only A is in beam → A wins by beam, not even by score\n-}\n-\n-#[test]\n-fn scoring_tiebreak_among_in_beam_candidates() {\n-    // source at (0, 0, 100, 100)\n-    // candidate A at (200, 0, 100, 100) — aligned, 100px right\n-    //   score = 13 * 100² + 0² = 130_000\n-    // candidate B at (150, 20, 100, 100) — aligned, 50px right, 20px down\n-    //   score = 13 * 50² + 20² = 32_500 + 400 = 32_900\n-    // Both in-beam → B wins (lower score)\n-}\n-```\n-\n-### Realistic board layout — 3 columns × varying cards\n-\n-```rust\n-#[test]\n-fn board_layout_3_columns() {\n-    // Column 0 header at (0, 0, 200, 40), cards at (0, 50, 200, 60), (0, 120, 200, 60), ...\n-    // Column 1 header at (210, 0, 200, 40), cards at (210, 50, 200, 60), (210, 120, 200, 60), ...\n-    // Column 2 header at (420, 0, 200, 40), 3 cards only\n-    //\n-    // Verify:\n-    // - nav.down from col0.card[0] → col0.card[1]\n-    // - nav.right from col0.card[0] → col1.card[0] (beam-aligned)\n-    // - nav.right from col0.card[4] → col2.card[2] (clamped: col2 only has 3 cards, nearest is last)\n-    // - nav.up from col0.card[0] → col0.header\n-    // - nav.right from col2.card[2] → None (no column to the right)\n-    // - nav.first → col0.header (top-left-most)\n-    // - nav.last → col2.card[2] (bottom-right-most)\n-}\n-\n-#[test]\n-fn empty_column_nav_right_lands_on_header() {\n-    // Column 0 with 5 cards, Column 1 with header only (no cards)\n-    // nav.right from col0.card[2] → col1.header (only element in col1's x-range)\n-}\n-```\n-\n-### Inspector layout — stacked field rows\n-\n-```rust\n-#[test]\n-fn inspector_8_stacked_fields() {\n-    // 8 field rows at (0, 0, 300, 35), (0, 40, 300, 35), ..., (0, 280, 300, 35)\n-    // nav.down from field[0] → field[1]\n-    // nav.up from field[7] → field[6]\n-    // nav.first → field[0]\n-    // nav.last → field[7]\n-    // nav.left from field[3] → None (nothing to the left)\n-}\n-```\n-\n-### Pill navigation within a field row\n-\n-```rust\n-#[test]\n-fn pill_horizontal_navigation() {\n-    // Field label at (0, 0, 100, 30)\n-    // Pill A at (110, 5, 60, 20), Pill B at (180, 5, 60, 20), Pill C at (250, 5, 60, 20)\n-    // nav.right from label → Pill A (nearest right, in beam)\n-    // nav.right from Pill A → Pill B\n-    // nav.left from Pill A → label\n-    // nav.left from Pill C → Pill B\n-}\n-```\n-\n-### Layer isolation\n-\n-```rust\n-#[test]\n-fn navigate_only_sees_active_layer() {\n-    // window layer: card at (0, 0, 200, 60)\n-    // inspector layer (active): field at (300, 0, 200, 35), field at (300, 40, 200, 35)\n-    // nav.right from inspector.field[0] → None (card is in window layer, invisible)\n-    // nav.down from inspector.field[0] → inspector.field[1]\n-}\n-```\n-\n-### Container-first search\n-\n-```rust\n-#[test]\n-fn container_first_stays_in_parent_scope() {\n-    // Toolbar row: button A at (0, 0, 80, 30), button B at (90, 0, 80, 30)\n-    //   parent_scope = \"toolbar\"\n-    // Board card at (0, 40, 200, 60)\n-    //   parent_scope = \"column:todo\"\n-    // nav.right from button A → button B (same parent scope)\n-    // NOT board card (which is closer vertically but different scope)\n-}\n-\n-#[test]\n-fn container_fallback_when_no_sibling() {\n-    // Toolbar row: single button at (0, 0, 80, 30), parent_scope = \"toolbar\"\n-    // Board card at (0, 40, 200, 60), parent_scope = \"column:todo\"\n-    // nav.down from button → board card (no sibling in toolbar below, falls through to layer)\n-}\n-```\n-\n-### Focus memory\n-\n-```rust\n-#[test]\n-fn layer_pop_restores_last_focused() {\n-    // window layer: card-A focused\n-    // push inspector layer, focus field-1, then field-3\n-    // pop inspector layer\n-    // → focused_key restored to card-A (window layer's last_focused)\n-}\n-```\n-\n-### Edge cases\n-\n-```rust\n-#[test]\n-fn navigate_with_unknown_key_returns_error() {\n-    // spatial_navigate(\"nonexistent\", Right) → Err\n-}\n-\n-#[test]\n-fn navigate_with_no_candidates_is_noop() {\n-    // Single entry in layer, nav.right → no event emitted, focused_key unchanged\n-}\n-\n-#[test]\n-fn navigate_rowstart_rowend() {\n-    // 3x3 grid: row 0 at y=0, row 1 at y=50, row 2 at y=100\n-    // Focused on (1,1) — center of grid\n-    // nav.rowStart → (0,1) — leftmost in same row\n-    // nav.rowEnd → (2,1) — rightmost in same row\n-}\n-```\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+### Tauri command\n+- `spatial_navigate` — real implementation parsing Direction, calling navigate, emitting focus-changed event, returning target moniker\n+- `spatial_register` — now accepts `parent_scope`\n+- `spatial_pop_layer` — now emits focus-changed when restoring focus\"\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -2,42 +2,226 @@\n\n Implement the spatial navigation algorithm in Rust as a pure function on `SpatialRegistry`. Informed by prior art from Android FocusFinder, W3C CSS Spatial Navigation, UWP XYFocus, and Norigin Spatial Navigation.\n\n+### Algorithm — `SpatialRegistry::navigate(key, direction) -> Result<()>`\n+\n+Looks up the key's rect, runs beam test + scoring against candidates in the active layer, updates `focused_key`, emits `focus-changed` event.\n+\n+**Two-phase candidate selection** (Android FocusFinder beam test):\n+\n+**Phase 1: Beam candidates** — A \"beam\" is the perpendicular band extending from the source in the nav direction. For Right, the beam spans `source.top..source.bottom` extending rightward. Candidates overlapping this band are \"in-beam.\"\n+\n+**Phase 2: Out-of-beam fallback** — If no in-beam candidates, all candidates in the correct direction are considered.\n+\n+**Directional filter**:\n+- `Right`: `candidate.left >= origin.right`\n+- `Left`: `candidate.right <= origin.left`\n+- `Down`: `candidate.top >= origin.bottom`\n+- `Up`: `candidate.bottom <= origin.top`\n+\n+**Scoring** — Android-style: `13 * major² + minor²`\n+\n+**Edge commands**: First (top-left-most), Last (bottom-right-most), RowStart/RowEnd (same-row filter).\n+\n+**Container-first search**: Search siblings in parent scope first, expand to full layer if empty.\n+\n+**Focus memory per layer**: LayerStack stores last-focused key per layer, restores on layer pop.\n+\n+### Files to create/modify\n+\n+**Rust** — `swissarmyhammer-commands/src/spatial.rs`:\n+- `Direction` enum (8 variants)\n+- `SpatialRegistry::navigate(&mut self, key, direction) -> Result<()>` with beam test + scoring\n+- `parent_scope: Option<String>` in `SpatialEntry` for container-first\n+- `last_focused: Option<String>` in `LayerEntry` for focus memory\n+\n+**React** — `kanban-app/ui/src/lib/entity-focus-context.tsx`:\n+- `broadcastNavCommand` stays synchronous. Calls `invoke(\"spatial_navigate\", { key, direction })` fire-and-forget.\n+\n+### Design decisions\n+- **Beam test first**: Android-validated, 10+ years.\n+- **13:1 squared ratio**: Android's production constant.\n+- **Container-first**: Prevents teleportation across the window.\n+- **Focus memory per layer**: Inspector close returns to exact previous card.\n+- **Layer boundaries are hard**: Nav cannot escape the active layer.\n+\n ### Subtasks\n-- [x] Implement beam test: filter candidates into in-beam and out-of-beam sets\n-- [x] Implement Android-style scoring: `13 * major² + minor²`\n-- [x] Implement container-first search: parent scope siblings first, then full layer\n-- [x] Implement edge commands (First, Last, RowStart, RowEnd)\n-- [x] Add focus memory to LayerStack entries\n+- [ ] Implement beam test: filter candidates into in-beam and out-of-beam sets\n+- [ ] Implement Android-style scoring: `13 * major² + minor²`\n+- [ ] Implement container-first search: parent scope siblings first, then full layer\n+- [ ] Implement edge commands (First, Last, RowStart, RowEnd)\n+- [ ] Add focus memory to LayerStack entries\n\n ## Acceptance Criteria\n-- [x] In-beam candidates always preferred over out-of-beam\n-- [x] Aligned candidates preferred over closer-but-diagonal (13:1 ratio)\n-- [x] Container-first: nav stays in parent scope if siblings exist\n-- [x] Container fallback: no sibling → expands to full layer\n-- [x] Focus memory: layer pop restores last-focused key\n-- [x] All 8 directions correct\n-- [x] Hard layer boundary — entries in other layers excluded\n-- [x] `cargo test` passes (38 spatial tests)\n+- [ ] In-beam candidates always preferred over out-of-beam\n+- [ ] Aligned candidates preferred over closer-but-diagonal (13:1 ratio)\n+- [ ] Container-first: nav stays in parent scope if siblings exist\n+- [ ] Container fallback: no sibling → expands to full layer\n+- [ ] Focus memory: layer pop restores last-focused key\n+- [ ] All 8 directions correct\n+- [ ] Hard layer boundary — entries in other layers excluded\n+- [ ] `cargo test` passes\n+\n+## Tests\n+\n+All tests are pure Rust with synthetic rects — no DOM, no React.\n+\n+### Beam test\n+\n+```rust\n+#[test]\n+fn beam_candidate_preferred_over_closer_out_of_beam() {\n+    // source at (0, 100, 100, 50) — a 100x50 rect, top-left at (0,100)\n+    // candidate A at (150, 120, 50, 30) — in beam (y overlap with source), 50px right\n+    // candidate B at (110, 10, 50, 30) — out of beam (y=10, above source), only 10px right\n+    // nav.right → expect A (in-beam wins over closer out-of-beam)\n+}\n+```\n+\n+### Scoring\n+\n+```rust\n+#[test]\n+fn aligned_candidate_beats_closer_diagonal() {\n+    // source at (0, 0, 100, 50)\n+    // candidate A at (200, 0, 100, 50) — aligned, 100px right\n+    //   score = 13 * 100² + 0² = 130_000\n+    // candidate B at (110, 200, 100, 50) — diagonal, 10px right but 175px down\n+    //   score = 13 * 10² + 175² = 1_300 + 30_625 = 31_925\n+    // Both in-beam? B is out of beam (y=200 > source.bottom=50)\n+    // So only A is in beam → A wins by beam, not even by score\n+}\n+\n+#[test]\n+fn scoring_tiebreak_among_in_beam_candidates() {\n+    // source at (0, 0, 100, 100)\n+    // candidate A at (200, 0, 100, 100) — aligned, 100px right\n+    //   score = 13 * 100² + 0² = 130_000\n+    // candidate B at (150, 20, 100, 100) — aligned, 50px right, 20px down\n+    //   score = 13 * 50² + 20² = 32_500 + 400 = 32_900\n+    // Both in-beam → B wins (lower score)\n+}\n+```\n+\n+### Realistic board layout — 3 columns × varying cards\n+\n+```rust\n+#[test]\n+fn board_layout_3_columns() {\n+    // Column 0 header at (0, 0, 200, 40), cards at (0, 50, 200, 60), (0, 120, 200, 60), ...\n+    // Column 1 header at (210, 0, 200, 40), cards at (210, 50, 200, 60), (210, 120, 200, 60), ...\n+    // Column 2 header at (420, 0, 200, 40), 3 cards only\n+    //\n+    // Verify:\n+    // - nav.down from col0.card[0] → col0.card[1]\n+    // - nav.right from col0.card[0] → col1.card[0] (beam-aligned)\n+    // - nav.right from col0.card[4] → col2.card[2] (clamped: col2 only has 3 cards, nearest is last)\n+    // - nav.up from col0.card[0] → col0.header\n+    // - nav.right from col2.card[2] → None (no column to the right)\n+    // - nav.first → col0.header (top-left-most)\n+    // - nav.last → col2.card[2] (bottom-right-most)\n+}\n+\n+#[test]\n+fn empty_column_nav_right_lands_on_header() {\n+    // Column 0 with 5 cards, Column 1 with header only (no cards)\n+    // nav.right from col0.card[2] → col1.header (only element in col1's x-range)\n+}\n+```\n+\n+### Inspector layout — stacked field rows\n+\n+```rust\n+#[test]\n+fn inspector_8_stacked_fields() {\n+    // 8 field rows at (0, 0, 300, 35), (0, 40, 300, 35), ..., (0, 280, 300, 35)\n+    // nav.down from field[0] → field[1]\n+    // nav.up from field[7] → field[6]\n+    // nav.first → field[0]\n+    // nav.last → field[7]\n+    // nav.left from field[3] → None (nothing to the left)\n+}\n+```\n+\n+### Pill navigation within a field row\n+\n+```rust\n+#[test]\n+fn pill_horizontal_navigation() {\n+    // Field label at (0, 0, 100, 30)\n+    // Pill A at (110, 5, 60, 20), Pill B at (180, 5, 60, 20), Pill C at (250, 5, 60, 20)\n+    // nav.right from label → Pill A (nearest right, in beam)\n+    // nav.right from Pill A → Pill B\n+    // nav.left from Pill A → label\n+    // nav.left from Pill C → Pill B\n+}\n+```\n+\n+### Layer isolation\n+\n+```rust\n+#[test]\n+fn navigate_only_sees_active_layer() {\n+    // window layer: card at (0, 0, 200, 60)\n+    // inspector layer (active): field at (300, 0, 200, 35), field at (300, 40, 200, 35)\n+    // nav.right from inspector.field[0] → None (card is in window layer, invisible)\n+    // nav.down from inspector.field[0] → inspector.field[1]\n+}\n+```\n+\n+### Container-first search\n+\n+```rust\n+#[test]\n+fn container_first_stays_in_parent_scope() {\n+    // Toolbar row: button A at (0, 0, 80, 30), button B at (90, 0, 80, 30)\n+    //   parent_scope = \"toolbar\"\n+    // Board card at (0, 40, 200, 60)\n+    //   parent_scope = \"column:todo\"\n+    // nav.right from button A → button B (same parent scope)\n+    // NOT board card (which is closer vertically but different scope)\n+}\n+\n+#[test]\n+fn container_fallback_when_no_sibling() {\n+    // Toolbar row: single button at (0, 0, 80, 30), parent_scope = \"toolbar\"\n+    // Board card at (0, 40, 200, 60), parent_scope = \"column:todo\"\n+    // nav.down from button → board card (no sibling in toolbar below, falls through to layer)\n+}\n+```\n\n-## Implementation\n+### Focus memory\n\n-### `spatial_nav.rs` — pure algorithm module\n-- `Direction` enum: Up, Down, Left, Right, First, Last, RowStart, RowEnd (with FromStr, Display, Serialize/Deserialize)\n-- `find_target(source, candidates, direction)` — beam test + scoring for cardinals, positional sorting for edges\n-- `container_first_search(source, candidates, direction)` — scoped search then full fallback\n-- Helper functions: `is_in_direction`, `is_in_beam`, `score`, `overlaps_y_range`\n-- 11 unit tests covering beam, scoring, board layout, inspector fields, pills, containers, row nav, direction parsing\n+```rust\n+#[test]\n+fn layer_pop_restores_last_focused() {\n+    // window layer: card-A focused\n+    // push inspector layer, focus field-1, then field-3\n+    // pop inspector layer\n+    // → focused_key restored to card-A (window layer's last_focused)\n+}\n+```\n+\n+### Edge cases\n+\n+```rust\n+#[test]\n+fn navigate_with_unknown_key_returns_error() {\n+    // spatial_navigate(\"nonexistent\", Right) → Err\n+}\n+\n+#[test]\n+fn navigate_with_no_candidates_is_noop() {\n+    // Single entry in layer, nav.right → no event emitted, focused_key unchanged\n+}\n\n-### `spatial_state.rs` — extended state machine\n-- `parent_scope: Option<String>` added to `SpatialEntry`\n-- `last_focused: Option<String>` added to `LayerEntry`\n-- `LayerStack::find_mut(key)` and `active_mut()` for focus memory\n-- `SpatialState::navigate(from_key, direction)` — filters to active layer, runs container-first search, updates focused_key with focus memory\n-- `focus()` saves last_focused on the layer owning the outgoing entry\n-- `remove_layer()` restores focus from the layer below's last_focused\n-- 4 new integration tests: layer isolation, unknown key error, no candidates noop, focus memory restore\n+#[test]\n+fn navigate_rowstart_rowend() {\n+    // 3x3 grid: row 0 at y=0, row 1 at y=50, row 2 at y=100\n+    // Focused on (1,1) — center of grid\n+    // nav.rowStart → (0,1) — leftmost in same row\n+    // nav.rowEnd → (2,1) — rightmost in same row\n+}\n+```\n\n-### Tauri command\n-- `spatial_navigate` — real implementation parsing Direction, calling navigate, emitting focus-changed event, returning target moniker\n-- `spatial_register` — now accepts `parent_scope`\n-- `spatial_pop_layer` — now emits focus-changed when restoring focus\"\n\\ No newline at end of file\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]]}
{"id":"01KP997ZQQ65VA174MP7BC4DP0","timestamp":"2026-04-15T19:17:45.847642Z","op":"update","item_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXW7HHHB8HW76K3PXH3G34\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Spatial navigation algorithm in Rust: nearest-neighbor by direction'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXW7HHHB8HW76K3PXH3G34\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial navigation algorithm in Rust: nearest-neighbor by direction'\n ---\n","transaction_id":null}
{"id":"01KP997ZQRDQP30WBM1SF2BS6T","timestamp":"2026-04-15T19:17:45.848048Z","entity_type":"task","entity_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KPBB7EXCANH23K3B51ZC1FZ8","timestamp":"2026-04-16T14:30:54.636659Z","op":"update","item_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","forward_patch":"--- original\n+++ modified\n@@ -50,4 +50,14 @@\n ### Tauri command\n - `spatial_navigate` — real implementation parsing Direction, calling navigate, emitting focus-changed event, returning target moniker\n - `spatial_register` — now accepts `parent_scope`\n-- `spatial_pop_layer` — now emits focus-changed when restoring focus\"\n\\ No newline at end of file\n+- `spatial_pop_layer` — now emits focus-changed when restoring focus\n+\n+## Review Findings (2026-04-15 19:45)\n+\n+### Warnings\n+- [ ] `swissarmyhammer-commands/src/spatial_state.rs` — **Duplicated focus-memory save logic** between `focus()` and `navigate()`. Both contain identical 7-line blocks that save the outgoing `focused_key` as `last_focused` on the owning layer. Extract a private helper on `SpatialStateInner` (e.g., `save_focus_memory(&mut self, prev_key: &str)`) and call it from both sites. This avoids the risk of the two copies diverging when the logic evolves.\n+- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`find_target` and `container_first_search` do not document the caller's responsibility to exclude the source from `candidates`.** If the source appears in candidates, `RowStart`/`RowEnd` could return it as the winner (since it overlaps its own y-range and may be the leftmost/rightmost). Add a doc comment on both public functions stating that `candidates` must not include the source entry.\n+\n+### Nits\n+- [ ] `kanban-app/src/spatial.rs` — **Tauri command `spatial_pop_layer` is misleadingly named.** The Rust method it calls is `remove_layer()` (arbitrary key removal, not stack pop). Consider renaming to `spatial_remove_layer` for consistency, since out-of-order unmount is explicitly supported.\n+- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`** rather than a dedicated error enum. The project has precedent for this (`cli.rs`), so not blocking, but a typed `ParseDirectionError` would be cleaner per Rust conventions and allow callers to match without string comparison.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -50,14 +50,4 @@\n ### Tauri command\n - `spatial_navigate` — real implementation parsing Direction, calling navigate, emitting focus-changed event, returning target moniker\n - `spatial_register` — now accepts `parent_scope`\n-- `spatial_pop_layer` — now emits focus-changed when restoring focus\n-\n-## Review Findings (2026-04-15 19:45)\n-\n-### Warnings\n-- [ ] `swissarmyhammer-commands/src/spatial_state.rs` — **Duplicated focus-memory save logic** between `focus()` and `navigate()`. Both contain identical 7-line blocks that save the outgoing `focused_key` as `last_focused` on the owning layer. Extract a private helper on `SpatialStateInner` (e.g., `save_focus_memory(&mut self, prev_key: &str)`) and call it from both sites. This avoids the risk of the two copies diverging when the logic evolves.\n-- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`find_target` and `container_first_search` do not document the caller's responsibility to exclude the source from `candidates`.** If the source appears in candidates, `RowStart`/`RowEnd` could return it as the winner (since it overlaps its own y-range and may be the leftmost/rightmost). Add a doc comment on both public functions stating that `candidates` must not include the source entry.\n-\n-### Nits\n-- [ ] `kanban-app/src/spatial.rs` — **Tauri command `spatial_pop_layer` is misleadingly named.** The Rust method it calls is `remove_layer()` (arbitrary key removal, not stack pop). Consider renaming to `spatial_remove_layer` for consistency, since out-of-order unmount is explicitly supported.\n-- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`** rather than a dedicated error enum. The project has precedent for this (`cli.rs`), so not blocking, but a typed `ParseDirectionError` would be cleaner per Rust conventions and allow callers to match without string comparison.\n\\ No newline at end of file\n+- `spatial_pop_layer` — now emits focus-changed when restoring focus\"\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPBB7EXDBZV0ZE2N6PSMRT22","timestamp":"2026-04-16T14:30:54.637353Z","entity_type":"task","entity_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -40,4 +40,14 @@\n ### Tauri command\n - `spatial_navigate` — real implementation parsing Direction, calling navigate, emitting focus-changed event, returning target moniker\n - `spatial_register` — now accepts `parent_scope`\n-- `spatial_pop_layer` — now emits focus-changed when restoring focus\"\n\\ No newline at end of file\n+- `spatial_pop_layer` — now emits focus-changed when restoring focus\n+\n+## Review Findings (2026-04-15 19:45)\n+\n+### Warnings\n+- [ ] `swissarmyhammer-commands/src/spatial_state.rs` — **Duplicated focus-memory save logic** between `focus()` and `navigate()`. Both contain identical 7-line blocks that save the outgoing `focused_key` as `last_focused` on the owning layer. Extract a private helper on `SpatialStateInner` (e.g., `save_focus_memory(&mut self, prev_key: &str)`) and call it from both sites. This avoids the risk of the two copies diverging when the logic evolves.\n+- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`find_target` and `container_first_search` do not document the caller's responsibility to exclude the source from `candidates`.** If the source appears in candidates, `RowStart`/`RowEnd` could return it as the winner (since it overlaps its own y-range and may be the leftmost/rightmost). Add a doc comment on both public functions stating that `candidates` must not include the source entry.\n+\n+### Nits\n+- [ ] `kanban-app/src/spatial.rs` — **Tauri command `spatial_pop_layer` is misleadingly named.** The Rust method it calls is `remove_layer()` (arbitrary key removal, not stack pop). Consider renaming to `spatial_remove_layer` for consistency, since out-of-order unmount is explicitly supported.\n+- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`** rather than a dedicated error enum. The project has precedent for this (`cli.rs`), so not blocking, but a typed `ParseDirectionError` would be cleaner per Rust conventions and allow callers to match without string comparison.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -40,14 +40,4 @@\n ### Tauri command\n - `spatial_navigate` — real implementation parsing Direction, calling navigate, emitting focus-changed event, returning target moniker\n - `spatial_register` — now accepts `parent_scope`\n-- `spatial_pop_layer` — now emits focus-changed when restoring focus\n-\n-## Review Findings (2026-04-15 19:45)\n-\n-### Warnings\n-- [ ] `swissarmyhammer-commands/src/spatial_state.rs` — **Duplicated focus-memory save logic** between `focus()` and `navigate()`. Both contain identical 7-line blocks that save the outgoing `focused_key` as `last_focused` on the owning layer. Extract a private helper on `SpatialStateInner` (e.g., `save_focus_memory(&mut self, prev_key: &str)`) and call it from both sites. This avoids the risk of the two copies diverging when the logic evolves.\n-- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`find_target` and `container_first_search` do not document the caller's responsibility to exclude the source from `candidates`.** If the source appears in candidates, `RowStart`/`RowEnd` could return it as the winner (since it overlaps its own y-range and may be the leftmost/rightmost). Add a doc comment on both public functions stating that `candidates` must not include the source entry.\n-\n-### Nits\n-- [ ] `kanban-app/src/spatial.rs` — **Tauri command `spatial_pop_layer` is misleadingly named.** The Rust method it calls is `remove_layer()` (arbitrary key removal, not stack pop). Consider renaming to `spatial_remove_layer` for consistency, since out-of-order unmount is explicitly supported.\n-- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`** rather than a dedicated error enum. The project has precedent for this (`cli.rs`), so not blocking, but a typed `ParseDirectionError` would be cleaner per Rust conventions and allow callers to match without string comparison.\n\\ No newline at end of file\n+- `spatial_pop_layer` — now emits focus-changed when restoring focus\"\n\\ No newline at end of file\n"}]]}
{"id":"01KPBB94ZKZ7FADFH76VGG26PF","timestamp":"2026-04-16T14:31:50.003386Z","op":"update","item_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXW7HHHB8HW76K3PXH3G34\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial navigation algorithm in Rust: nearest-neighbor by direction'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXW7HHHB8HW76K3PXH3G34\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Spatial navigation algorithm in Rust: nearest-neighbor by direction'\n ---\n","transaction_id":null}
{"id":"01KPBB94ZK6NX974KAFPF41WRS","timestamp":"2026-04-16T14:31:50.003741Z","entity_type":"task","entity_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KPBC14257JF0CFW2B9WACD6D","timestamp":"2026-04-16T14:44:55.493765Z","op":"update","item_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","forward_patch":"--- original\n+++ modified\n@@ -32,32 +32,27 @@\n ## Implementation\n\n ### `spatial_nav.rs` — pure algorithm module\n-- `Direction` enum: Up, Down, Left, Right, First, Last, RowStart, RowEnd (with FromStr, Display, Serialize/Deserialize)\n-- `find_target(source, candidates, direction)` — beam test + scoring for cardinals, positional sorting for edges\n-- `container_first_search(source, candidates, direction)` — scoped search then full fallback\n-- Helper functions: `is_in_direction`, `is_in_beam`, `score`, `overlaps_y_range`\n-- 11 unit tests covering beam, scoring, board layout, inspector fields, pills, containers, row nav, direction parsing\n+- `Direction` enum with `ParseDirectionError` typed error\n+- `find_target` + `container_first_search` with documented caller contract (must exclude source from candidates)\n+- 11 unit tests\n\n ### `spatial_state.rs` — extended state machine\n-- `parent_scope: Option<String>` added to `SpatialEntry`\n-- `last_focused: Option<String>` added to `LayerEntry`\n-- `LayerStack::find_mut(key)` and `active_mut()` for focus memory\n-- `SpatialState::navigate(from_key, direction)` — filters to active layer, runs container-first search, updates focused_key with focus memory\n-- `focus()` saves last_focused on the layer owning the outgoing entry\n-- `remove_layer()` restores focus from the layer below's last_focused\n-- 4 new integration tests: layer isolation, unknown key error, no candidates noop, focus memory restore\n+- `SpatialStateInner::save_focus_memory()` — extracted helper (no duplication between focus/navigate)\n+- `SpatialState::navigate()` — layer-filtered container-first search\n+- Focus memory: `focus()` and `navigate()` both call `save_focus_memory`; `remove_layer()` restores\n+- 4 integration tests\n\n-### Tauri command\n-- `spatial_navigate` — real implementation parsing Direction, calling navigate, emitting focus-changed event, returning target moniker\n-- `spatial_register` — now accepts `parent_scope`\n-- `spatial_pop_layer` — now emits focus-changed when restoring focus\n+### Tauri commands\n+- `spatial_navigate` — real implementation\n+- `spatial_register` — accepts `parent_scope`\n+- `spatial_remove_layer` — renamed from `spatial_pop_layer` for clarity\n\n ## Review Findings (2026-04-15 19:45)\n\n ### Warnings\n-- [ ] `swissarmyhammer-commands/src/spatial_state.rs` — **Duplicated focus-memory save logic** between `focus()` and `navigate()`. Both contain identical 7-line blocks that save the outgoing `focused_key` as `last_focused` on the owning layer. Extract a private helper on `SpatialStateInner` (e.g., `save_focus_memory(&mut self, prev_key: &str)`) and call it from both sites. This avoids the risk of the two copies diverging when the logic evolves.\n-- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`find_target` and `container_first_search` do not document the caller's responsibility to exclude the source from `candidates`.** If the source appears in candidates, `RowStart`/`RowEnd` could return it as the winner (since it overlaps its own y-range and may be the leftmost/rightmost). Add a doc comment on both public functions stating that `candidates` must not include the source entry.\n+- [x] `swissarmyhammer-commands/src/spatial_state.rs` — **Duplicated focus-memory save logic.** Extracted `SpatialStateInner::save_focus_memory()` helper, called from both `focus()` and `navigate()`.\n+- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **Missing doc contract on candidates.** Added doc comments on both `find_target` and `container_first_search` stating caller must exclude source.\n\n ### Nits\n-- [ ] `kanban-app/src/spatial.rs` — **Tauri command `spatial_pop_layer` is misleadingly named.** The Rust method it calls is `remove_layer()` (arbitrary key removal, not stack pop). Consider renaming to `spatial_remove_layer` for consistency, since out-of-order unmount is explicitly supported.\n-- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`** rather than a dedicated error enum. The project has precedent for this (`cli.rs`), so not blocking, but a typed `ParseDirectionError` would be cleaner per Rust conventions and allow callers to match without string comparison.\n\\ No newline at end of file\n+- [x] `kanban-app/src/spatial.rs` — **`spatial_pop_layer` misleadingly named.** Renamed to `spatial_remove_layer` in Tauri command, main.rs registration, React FocusLayer, and tests.\n+- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`.** Added `ParseDirectionError` struct with `Display` and `Error` impls.\"\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -32,27 +32,32 @@\n ## Implementation\n\n ### `spatial_nav.rs` — pure algorithm module\n-- `Direction` enum with `ParseDirectionError` typed error\n-- `find_target` + `container_first_search` with documented caller contract (must exclude source from candidates)\n-- 11 unit tests\n+- `Direction` enum: Up, Down, Left, Right, First, Last, RowStart, RowEnd (with FromStr, Display, Serialize/Deserialize)\n+- `find_target(source, candidates, direction)` — beam test + scoring for cardinals, positional sorting for edges\n+- `container_first_search(source, candidates, direction)` — scoped search then full fallback\n+- Helper functions: `is_in_direction`, `is_in_beam`, `score`, `overlaps_y_range`\n+- 11 unit tests covering beam, scoring, board layout, inspector fields, pills, containers, row nav, direction parsing\n\n ### `spatial_state.rs` — extended state machine\n-- `SpatialStateInner::save_focus_memory()` — extracted helper (no duplication between focus/navigate)\n-- `SpatialState::navigate()` — layer-filtered container-first search\n-- Focus memory: `focus()` and `navigate()` both call `save_focus_memory`; `remove_layer()` restores\n-- 4 integration tests\n+- `parent_scope: Option<String>` added to `SpatialEntry`\n+- `last_focused: Option<String>` added to `LayerEntry`\n+- `LayerStack::find_mut(key)` and `active_mut()` for focus memory\n+- `SpatialState::navigate(from_key, direction)` — filters to active layer, runs container-first search, updates focused_key with focus memory\n+- `focus()` saves last_focused on the layer owning the outgoing entry\n+- `remove_layer()` restores focus from the layer below's last_focused\n+- 4 new integration tests: layer isolation, unknown key error, no candidates noop, focus memory restore\n\n-### Tauri commands\n-- `spatial_navigate` — real implementation\n-- `spatial_register` — accepts `parent_scope`\n-- `spatial_remove_layer` — renamed from `spatial_pop_layer` for clarity\n+### Tauri command\n+- `spatial_navigate` — real implementation parsing Direction, calling navigate, emitting focus-changed event, returning target moniker\n+- `spatial_register` — now accepts `parent_scope`\n+- `spatial_pop_layer` — now emits focus-changed when restoring focus\n\n ## Review Findings (2026-04-15 19:45)\n\n ### Warnings\n-- [x] `swissarmyhammer-commands/src/spatial_state.rs` — **Duplicated focus-memory save logic.** Extracted `SpatialStateInner::save_focus_memory()` helper, called from both `focus()` and `navigate()`.\n-- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **Missing doc contract on candidates.** Added doc comments on both `find_target` and `container_first_search` stating caller must exclude source.\n+- [ ] `swissarmyhammer-commands/src/spatial_state.rs` — **Duplicated focus-memory save logic** between `focus()` and `navigate()`. Both contain identical 7-line blocks that save the outgoing `focused_key` as `last_focused` on the owning layer. Extract a private helper on `SpatialStateInner` (e.g., `save_focus_memory(&mut self, prev_key: &str)`) and call it from both sites. This avoids the risk of the two copies diverging when the logic evolves.\n+- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`find_target` and `container_first_search` do not document the caller's responsibility to exclude the source from `candidates`.** If the source appears in candidates, `RowStart`/`RowEnd` could return it as the winner (since it overlaps its own y-range and may be the leftmost/rightmost). Add a doc comment on both public functions stating that `candidates` must not include the source entry.\n\n ### Nits\n-- [x] `kanban-app/src/spatial.rs` — **`spatial_pop_layer` misleadingly named.** Renamed to `spatial_remove_layer` in Tauri command, main.rs registration, React FocusLayer, and tests.\n-- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`.** Added `ParseDirectionError` struct with `Display` and `Error` impls.\"\n\\ No newline at end of file\n+- [ ] `kanban-app/src/spatial.rs` — **Tauri command `spatial_pop_layer` is misleadingly named.** The Rust method it calls is `remove_layer()` (arbitrary key removal, not stack pop). Consider renaming to `spatial_remove_layer` for consistency, since out-of-order unmount is explicitly supported.\n+- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`** rather than a dedicated error enum. The project has precedent for this (`cli.rs`), so not blocking, but a typed `ParseDirectionError` would be cleaner per Rust conventions and allow callers to match without string comparison.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPBC1426TMZFQ92631ZHEEJR","timestamp":"2026-04-16T14:44:55.494191Z","entity_type":"task","entity_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -22,32 +22,27 @@\n ## Implementation\n\n ### `spatial_nav.rs` — pure algorithm module\n-- `Direction` enum: Up, Down, Left, Right, First, Last, RowStart, RowEnd (with FromStr, Display, Serialize/Deserialize)\n-- `find_target(source, candidates, direction)` — beam test + scoring for cardinals, positional sorting for edges\n-- `container_first_search(source, candidates, direction)` — scoped search then full fallback\n-- Helper functions: `is_in_direction`, `is_in_beam`, `score`, `overlaps_y_range`\n-- 11 unit tests covering beam, scoring, board layout, inspector fields, pills, containers, row nav, direction parsing\n+- `Direction` enum with `ParseDirectionError` typed error\n+- `find_target` + `container_first_search` with documented caller contract (must exclude source from candidates)\n+- 11 unit tests\n\n ### `spatial_state.rs` — extended state machine\n-- `parent_scope: Option<String>` added to `SpatialEntry`\n-- `last_focused: Option<String>` added to `LayerEntry`\n-- `LayerStack::find_mut(key)` and `active_mut()` for focus memory\n-- `SpatialState::navigate(from_key, direction)` — filters to active layer, runs container-first search, updates focused_key with focus memory\n-- `focus()` saves last_focused on the layer owning the outgoing entry\n-- `remove_layer()` restores focus from the layer below's last_focused\n-- 4 new integration tests: layer isolation, unknown key error, no candidates noop, focus memory restore\n+- `SpatialStateInner::save_focus_memory()` — extracted helper (no duplication between focus/navigate)\n+- `SpatialState::navigate()` — layer-filtered container-first search\n+- Focus memory: `focus()` and `navigate()` both call `save_focus_memory`; `remove_layer()` restores\n+- 4 integration tests\n\n-### Tauri command\n-- `spatial_navigate` — real implementation parsing Direction, calling navigate, emitting focus-changed event, returning target moniker\n-- `spatial_register` — now accepts `parent_scope`\n-- `spatial_pop_layer` — now emits focus-changed when restoring focus\n+### Tauri commands\n+- `spatial_navigate` — real implementation\n+- `spatial_register` — accepts `parent_scope`\n+- `spatial_remove_layer` — renamed from `spatial_pop_layer` for clarity\n\n ## Review Findings (2026-04-15 19:45)\n\n ### Warnings\n-- [ ] `swissarmyhammer-commands/src/spatial_state.rs` — **Duplicated focus-memory save logic** between `focus()` and `navigate()`. Both contain identical 7-line blocks that save the outgoing `focused_key` as `last_focused` on the owning layer. Extract a private helper on `SpatialStateInner` (e.g., `save_focus_memory(&mut self, prev_key: &str)`) and call it from both sites. This avoids the risk of the two copies diverging when the logic evolves.\n-- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`find_target` and `container_first_search` do not document the caller's responsibility to exclude the source from `candidates`.** If the source appears in candidates, `RowStart`/`RowEnd` could return it as the winner (since it overlaps its own y-range and may be the leftmost/rightmost). Add a doc comment on both public functions stating that `candidates` must not include the source entry.\n+- [x] `swissarmyhammer-commands/src/spatial_state.rs` — **Duplicated focus-memory save logic.** Extracted `SpatialStateInner::save_focus_memory()` helper, called from both `focus()` and `navigate()`.\n+- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **Missing doc contract on candidates.** Added doc comments on both `find_target` and `container_first_search` stating caller must exclude source.\n\n ### Nits\n-- [ ] `kanban-app/src/spatial.rs` — **Tauri command `spatial_pop_layer` is misleadingly named.** The Rust method it calls is `remove_layer()` (arbitrary key removal, not stack pop). Consider renaming to `spatial_remove_layer` for consistency, since out-of-order unmount is explicitly supported.\n-- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`** rather than a dedicated error enum. The project has precedent for this (`cli.rs`), so not blocking, but a typed `ParseDirectionError` would be cleaner per Rust conventions and allow callers to match without string comparison.\n\\ No newline at end of file\n+- [x] `kanban-app/src/spatial.rs` — **`spatial_pop_layer` misleadingly named.** Renamed to `spatial_remove_layer` in Tauri command, main.rs registration, React FocusLayer, and tests.\n+- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`.** Added `ParseDirectionError` struct with `Display` and `Error` impls.\"\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -22,27 +22,32 @@\n ## Implementation\n\n ### `spatial_nav.rs` — pure algorithm module\n-- `Direction` enum with `ParseDirectionError` typed error\n-- `find_target` + `container_first_search` with documented caller contract (must exclude source from candidates)\n-- 11 unit tests\n+- `Direction` enum: Up, Down, Left, Right, First, Last, RowStart, RowEnd (with FromStr, Display, Serialize/Deserialize)\n+- `find_target(source, candidates, direction)` — beam test + scoring for cardinals, positional sorting for edges\n+- `container_first_search(source, candidates, direction)` — scoped search then full fallback\n+- Helper functions: `is_in_direction`, `is_in_beam`, `score`, `overlaps_y_range`\n+- 11 unit tests covering beam, scoring, board layout, inspector fields, pills, containers, row nav, direction parsing\n\n ### `spatial_state.rs` — extended state machine\n-- `SpatialStateInner::save_focus_memory()` — extracted helper (no duplication between focus/navigate)\n-- `SpatialState::navigate()` — layer-filtered container-first search\n-- Focus memory: `focus()` and `navigate()` both call `save_focus_memory`; `remove_layer()` restores\n-- 4 integration tests\n+- `parent_scope: Option<String>` added to `SpatialEntry`\n+- `last_focused: Option<String>` added to `LayerEntry`\n+- `LayerStack::find_mut(key)` and `active_mut()` for focus memory\n+- `SpatialState::navigate(from_key, direction)` — filters to active layer, runs container-first search, updates focused_key with focus memory\n+- `focus()` saves last_focused on the layer owning the outgoing entry\n+- `remove_layer()` restores focus from the layer below's last_focused\n+- 4 new integration tests: layer isolation, unknown key error, no candidates noop, focus memory restore\n\n-### Tauri commands\n-- `spatial_navigate` — real implementation\n-- `spatial_register` — accepts `parent_scope`\n-- `spatial_remove_layer` — renamed from `spatial_pop_layer` for clarity\n+### Tauri command\n+- `spatial_navigate` — real implementation parsing Direction, calling navigate, emitting focus-changed event, returning target moniker\n+- `spatial_register` — now accepts `parent_scope`\n+- `spatial_pop_layer` — now emits focus-changed when restoring focus\n\n ## Review Findings (2026-04-15 19:45)\n\n ### Warnings\n-- [x] `swissarmyhammer-commands/src/spatial_state.rs` — **Duplicated focus-memory save logic.** Extracted `SpatialStateInner::save_focus_memory()` helper, called from both `focus()` and `navigate()`.\n-- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **Missing doc contract on candidates.** Added doc comments on both `find_target` and `container_first_search` stating caller must exclude source.\n+- [ ] `swissarmyhammer-commands/src/spatial_state.rs` — **Duplicated focus-memory save logic** between `focus()` and `navigate()`. Both contain identical 7-line blocks that save the outgoing `focused_key` as `last_focused` on the owning layer. Extract a private helper on `SpatialStateInner` (e.g., `save_focus_memory(&mut self, prev_key: &str)`) and call it from both sites. This avoids the risk of the two copies diverging when the logic evolves.\n+- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`find_target` and `container_first_search` do not document the caller's responsibility to exclude the source from `candidates`.** If the source appears in candidates, `RowStart`/`RowEnd` could return it as the winner (since it overlaps its own y-range and may be the leftmost/rightmost). Add a doc comment on both public functions stating that `candidates` must not include the source entry.\n\n ### Nits\n-- [x] `kanban-app/src/spatial.rs` — **`spatial_pop_layer` misleadingly named.** Renamed to `spatial_remove_layer` in Tauri command, main.rs registration, React FocusLayer, and tests.\n-- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`.** Added `ParseDirectionError` struct with `Display` and `Error` impls.\"\n\\ No newline at end of file\n+- [ ] `kanban-app/src/spatial.rs` — **Tauri command `spatial_pop_layer` is misleadingly named.** The Rust method it calls is `remove_layer()` (arbitrary key removal, not stack pop). Consider renaming to `spatial_remove_layer` for consistency, since out-of-order unmount is explicitly supported.\n+- [ ] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`** rather than a dedicated error enum. The project has precedent for this (`cli.rs`), so not blocking, but a typed `ParseDirectionError` would be cleaner per Rust conventions and allow callers to match without string comparison.\n\\ No newline at end of file\n"}]]}
{"id":"01KPBC1E99VD6D61PXQGCYZ4G3","timestamp":"2026-04-16T14:45:05.961269Z","op":"update","item_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXW7HHHB8HW76K3PXH3G34\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Spatial navigation algorithm in Rust: nearest-neighbor by direction'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXW7HHHB8HW76K3PXH3G34\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial navigation algorithm in Rust: nearest-neighbor by direction'\n ---\n","transaction_id":null}
{"id":"01KPBC1E997ERBFK4A0KY102SH","timestamp":"2026-04-16T14:45:05.961644Z","entity_type":"task","entity_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KPBC6CA4M364T4FZM69K207G","timestamp":"2026-04-16T14:47:47.780677Z","op":"update","item_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","forward_patch":"--- original\n+++ modified\n@@ -55,4 +55,9 @@\n\n ### Nits\n - [x] `kanban-app/src/spatial.rs` — **`spatial_pop_layer` misleadingly named.** Renamed to `spatial_remove_layer` in Tauri command, main.rs registration, React FocusLayer, and tests.\n-- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`.** Added `ParseDirectionError` struct with `Display` and `Error` impls.\"\n\\ No newline at end of file\n+- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`.** Added `ParseDirectionError` struct with `Display` and `Error` impls.\"\n+\n+## Review Findings (2026-04-15 20:12)\n+\n+### Nits\n+- [ ] `swissarmyhammer-commands/src/spatial_state.rs` — **Stale test name after rename.** Test function `layer_pop_restores_last_focused` still uses \"pop\" terminology but exercises `remove_layer()`. Rename to `layer_remove_restores_last_focused` for consistency with finding 3.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -55,9 +55,4 @@\n\n ### Nits\n - [x] `kanban-app/src/spatial.rs` — **`spatial_pop_layer` misleadingly named.** Renamed to `spatial_remove_layer` in Tauri command, main.rs registration, React FocusLayer, and tests.\n-- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`.** Added `ParseDirectionError` struct with `Display` and `Error` impls.\"\n-\n-## Review Findings (2026-04-15 20:12)\n-\n-### Nits\n-- [ ] `swissarmyhammer-commands/src/spatial_state.rs` — **Stale test name after rename.** Test function `layer_pop_restores_last_focused` still uses \"pop\" terminology but exercises `remove_layer()`. Rename to `layer_remove_restores_last_focused` for consistency with finding 3.\n\\ No newline at end of file\n+- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`.** Added `ParseDirectionError` struct with `Display` and `Error` impls.\"\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPBC6CA5KRDCGXA64YAP1KC4","timestamp":"2026-04-16T14:47:47.781122Z","entity_type":"task","entity_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -45,4 +45,9 @@\n\n ### Nits\n - [x] `kanban-app/src/spatial.rs` — **`spatial_pop_layer` misleadingly named.** Renamed to `spatial_remove_layer` in Tauri command, main.rs registration, React FocusLayer, and tests.\n-- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`.** Added `ParseDirectionError` struct with `Display` and `Error` impls.\"\n\\ No newline at end of file\n+- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`.** Added `ParseDirectionError` struct with `Display` and `Error` impls.\"\n+\n+## Review Findings (2026-04-15 20:12)\n+\n+### Nits\n+- [ ] `swissarmyhammer-commands/src/spatial_state.rs` — **Stale test name after rename.** Test function `layer_pop_restores_last_focused` still uses \"pop\" terminology but exercises `remove_layer()`. Rename to `layer_remove_restores_last_focused` for consistency with finding 3.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -45,9 +45,4 @@\n\n ### Nits\n - [x] `kanban-app/src/spatial.rs` — **`spatial_pop_layer` misleadingly named.** Renamed to `spatial_remove_layer` in Tauri command, main.rs registration, React FocusLayer, and tests.\n-- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`.** Added `ParseDirectionError` struct with `Display` and `Error` impls.\"\n-\n-## Review Findings (2026-04-15 20:12)\n-\n-### Nits\n-- [ ] `swissarmyhammer-commands/src/spatial_state.rs` — **Stale test name after rename.** Test function `layer_pop_restores_last_focused` still uses \"pop\" terminology but exercises `remove_layer()`. Rename to `layer_remove_restores_last_focused` for consistency with finding 3.\n\\ No newline at end of file\n+- [x] `swissarmyhammer-commands/src/spatial_nav.rs` — **`FromStr` error type is `String`.** Added `ParseDirectionError` struct with `Display` and `Error` impls.\"\n\\ No newline at end of file\n"}]]}
{"id":"01KPBC97PKGRR1GCVN636JX9FQ","timestamp":"2026-04-16T14:49:21.363797Z","op":"update","item_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXW7HHHB8HW76K3PXH3G34\n-position_column: review\n-position_ordinal: '8180'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffd980\n project: spatial-nav\n title: 'Spatial navigation algorithm in Rust: nearest-neighbor by direction'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXW7HHHB8HW76K3PXH3G34\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffd980\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Spatial navigation algorithm in Rust: nearest-neighbor by direction'\n ---\n","transaction_id":null}
{"id":"01KPBC97PMFVFJJKEVCJYP34M1","timestamp":"2026-04-16T14:49:21.364201Z","entity_type":"task","entity_id":"01KNQXXF5W7G4JP73C6ZCMKYKX","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+ffffffffffffffffffffffd980\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffd980\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}

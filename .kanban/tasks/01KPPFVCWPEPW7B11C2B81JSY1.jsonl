{"id":"01KPPFVCWPBFV6QXEA1PVR3YN2","timestamp":"2026-04-20T22:23:20.982397Z","op":"create","item_id":"01KPPFVCWPEPW7B11C2B81JSY1","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,46 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: f880\n+title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n+---\n+## What\n+\n+Follow-up from `01KPJSSVCW774TK2E2JSMD3Y1J` (retire Rust EntityCommand). The TypeScript parallel types are still in place even though the backend no longer serves entity-scoped commands via entity schemas.\n+\n+### Dead types in `kanban-app/ui/src/types/kanban.ts`\n+\n+- `interface EntityCommand` (lines ~6-18)\n+- `interface EntityCommandKeys`\n+- `EntityDef.commands` field (around line 191)\n+\n+### Consumers to audit and rewire\n+\n+- `kanban-app/ui/src/lib/entity-commands.ts` — `buildEntityCommandDefs` et al\n+- `kanban-app/ui/src/lib/entity-commands.test.ts`\n+- `kanban-app/ui/src/lib/schema-context.tsx` — exposes entity commands to the React tree\n+- `kanban-app/ui/src/components/*-view.tsx` — any `useEntityCommands` consumers\n+- Associated tests (grid-view, board-view, column-view, entity-card, data-table, command-palette, etc.)\n+\n+### Approach\n+\n+1. Confirm the resolved command list is already delivered through `commands_for_scope` (Rust) and the frontend reads only that list. The entity-schema `commands:` pass has been removed server-side, so anything `buildEntityCommandDefs` produces is dead.\n+2. Delete `interface EntityCommand`, `interface EntityCommandKeys`, and `EntityDef.commands`.\n+3. Delete `buildEntityCommandDefs`, `useEntityCommands`, and the `entity-commands.{ts,test.ts}` module if nothing else uses them.\n+4. Audit `schema-context.tsx` for entity-command plumbing; strip it.\n+5. Run `npm test` / `vitest` and `tsc --noEmit` in `kanban-app/ui` to prove nothing references the deleted types.\n+\n+### Acceptance Criteria\n+\n+- [ ] `grep -rn \"EntityCommand\\\\|EntityCommandKeys\" kanban-app/ui/src` returns zero matches.\n+- [ ] `EntityDef` TS interface has no `commands` field.\n+- [ ] `entity-commands.ts` is deleted (or heavily slimmed) and its tests updated.\n+- [ ] Frontend build (`tsc --noEmit` in kanban-app/ui) passes.\n+- [ ] Frontend test suite passes.\n+\n+## Workflow\n+\n+Use `/tdd` to verify behavior is preserved: the context menu / command palette for each entity should still surface the same commands it does today, since those come from `commands_for_scope`, not from the TS entity schema.\n+\n+#commands #frontend\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,46 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: f880\n-title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n----\n-## What\n-\n-Follow-up from `01KPJSSVCW774TK2E2JSMD3Y1J` (retire Rust EntityCommand). The TypeScript parallel types are still in place even though the backend no longer serves entity-scoped commands via entity schemas.\n-\n-### Dead types in `kanban-app/ui/src/types/kanban.ts`\n-\n-- `interface EntityCommand` (lines ~6-18)\n-- `interface EntityCommandKeys`\n-- `EntityDef.commands` field (around line 191)\n-\n-### Consumers to audit and rewire\n-\n-- `kanban-app/ui/src/lib/entity-commands.ts` — `buildEntityCommandDefs` et al\n-- `kanban-app/ui/src/lib/entity-commands.test.ts`\n-- `kanban-app/ui/src/lib/schema-context.tsx` — exposes entity commands to the React tree\n-- `kanban-app/ui/src/components/*-view.tsx` — any `useEntityCommands` consumers\n-- Associated tests (grid-view, board-view, column-view, entity-card, data-table, command-palette, etc.)\n-\n-### Approach\n-\n-1. Confirm the resolved command list is already delivered through `commands_for_scope` (Rust) and the frontend reads only that list. The entity-schema `commands:` pass has been removed server-side, so anything `buildEntityCommandDefs` produces is dead.\n-2. Delete `interface EntityCommand`, `interface EntityCommandKeys`, and `EntityDef.commands`.\n-3. Delete `buildEntityCommandDefs`, `useEntityCommands`, and the `entity-commands.{ts,test.ts}` module if nothing else uses them.\n-4. Audit `schema-context.tsx` for entity-command plumbing; strip it.\n-5. Run `npm test` / `vitest` and `tsc --noEmit` in `kanban-app/ui` to prove nothing references the deleted types.\n-\n-### Acceptance Criteria\n-\n-- [ ] `grep -rn \"EntityCommand\\\\|EntityCommandKeys\" kanban-app/ui/src` returns zero matches.\n-- [ ] `EntityDef` TS interface has no `commands` field.\n-- [ ] `entity-commands.ts` is deleted (or heavily slimmed) and its tests updated.\n-- [ ] Frontend build (`tsc --noEmit` in kanban-app/ui) passes.\n-- [ ] Frontend test suite passes.\n-\n-## Workflow\n-\n-Use `/tdd` to verify behavior is preserved: the context menu / command palette for each entity should still surface the same commands it does today, since those come from `commands_for_scope`, not from the TS entity schema.\n-\n-#commands #frontend\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPPFVCWPDSF77SF2X1R6FA2S","timestamp":"2026-04-20T22:23:20.982761Z","entity_type":"task","entity_id":"01KPPFVCWPEPW7B11C2B81JSY1","op":"create","changes":[["title",{"kind":"set","value":"Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers"}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"f880"}],["body",{"kind":"set","value":"## What\n\nFollow-up from `01KPJSSVCW774TK2E2JSMD3Y1J` (retire Rust EntityCommand). The TypeScript parallel types are still in place even though the backend no longer serves entity-scoped commands via entity schemas.\n\n### Dead types in `kanban-app/ui/src/types/kanban.ts`\n\n- `interface EntityCommand` (lines ~6-18)\n- `interface EntityCommandKeys`\n- `EntityDef.commands` field (around line 191)\n\n### Consumers to audit and rewire\n\n- `kanban-app/ui/src/lib/entity-commands.ts` — `buildEntityCommandDefs` et al\n- `kanban-app/ui/src/lib/entity-commands.test.ts`\n- `kanban-app/ui/src/lib/schema-context.tsx` — exposes entity commands to the React tree\n- `kanban-app/ui/src/components/*-view.tsx` — any `useEntityCommands` consumers\n- Associated tests (grid-view, board-view, column-view, entity-card, data-table, command-palette, etc.)\n\n### Approach\n\n1. Confirm the resolved command list is already delivered through `commands_for_scope` (Rust) and the frontend reads only that list. The entity-schema `commands:` pass has been removed server-side, so anything `buildEntityCommandDefs` produces is dead.\n2. Delete `interface EntityCommand`, `interface EntityCommandKeys`, and `EntityDef.commands`.\n3. Delete `buildEntityCommandDefs`, `useEntityCommands`, and the `entity-commands.{ts,test.ts}` module if nothing else uses them.\n4. Audit `schema-context.tsx` for entity-command plumbing; strip it.\n5. Run `npm test` / `vitest` and `tsc --noEmit` in `kanban-app/ui` to prove nothing references the deleted types.\n\n### Acceptance Criteria\n\n- [ ] `grep -rn \"EntityCommand\\\\|EntityCommandKeys\" kanban-app/ui/src` returns zero matches.\n- [ ] `EntityDef` TS interface has no `commands` field.\n- [ ] `entity-commands.ts` is deleted (or heavily slimmed) and its tests updated.\n- [ ] Frontend build (`tsc --noEmit` in kanban-app/ui) passes.\n- [ ] Frontend test suite passes.\n\n## Workflow\n\nUse `/tdd` to verify behavior is preserved: the context menu / command palette for each entity should still surface the same commands it does today, since those come from `commands_for_scope`, not from the TS entity schema.\n\n#commands #frontend"}],["assignees",{"kind":"set","value":["claude-code"]}]]}
{"id":"01KPPJZSFCFSQVV2K5VP7J49BX","timestamp":"2026-04-20T23:18:10.668184Z","op":"update","item_id":"01KPPFVCWPEPW7B11C2B81JSY1","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: f880\n+position_column: doing\n+position_ordinal: '80'\n title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: f880\n title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n ---\n ## What\n","transaction_id":null}
{"id":"01KPPJZSFCDC9E6DHJ1MK2VNSP","timestamp":"2026-04-20T23:18:10.668640Z","entity_type":"task","entity_id":"01KPPFVCWPEPW7B11C2B81JSY1","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-f880\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+f880\n\\ No newline at end of file\n"}]]}
{"id":"01KPPVA8AGHVDSB4DZGEVH3F1W","timestamp":"2026-04-21T01:43:42.160200Z","op":"update","item_id":"01KPPFVCWPEPW7B11C2B81JSY1","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8380'\n title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '80'\n title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n ---\n ## What\n","transaction_id":null}
{"id":"01KPPVA8AGMPX30AH8D5T0H7H6","timestamp":"2026-04-21T01:43:42.160599Z","entity_type":"task","entity_id":"01KPPFVCWPEPW7B11C2B81JSY1","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KPPX1FE82PADHC033JP4J6H0","timestamp":"2026-04-21T02:13:51.688593Z","op":"update","item_id":"01KPPFVCWPEPW7B11C2B81JSY1","forward_patch":"--- original\n+++ modified\n@@ -43,4 +43,15 @@\n\n Use `/tdd` to verify behavior is preserved: the context menu / command palette for each entity should still surface the same commands it does today, since those come from `commands_for_scope`, not from the TS entity schema.\n\n-#commands #frontend\n\\ No newline at end of file\n+#commands #frontend\n+\n+## Review Findings (2026-04-20 21:20)\n+\n+### Warnings\n+\n+- [ ] `kanban-app/ui/src/components/{avatar,board-view,column-view,command-palette,data-table,inspector-focus-bridge}.tsx` (and others) — every rewired `FocusScope commands={[]}` passes a fresh `[]` literal on every render. `FocusScope` (focus-scope.tsx:89-103) uses that array as a `useMemo` dep that rebuilds a `Map<string, CommandDef>` and a `useEffect` dep that re-invokes `registerScope`/`unregisterScope`. Before this change, `useEntityCommands` returned a memoized array, so the scope Map was stable across renders. Now every FocusScope in these paths churns its scope registration on every render. Fix: hoist a module-level `const EMPTY_COMMANDS: readonly CommandDef[] = []` (in `command-scope.ts` or `focus-scope.tsx`) and use it from all the rewired call sites, or make `FocusScope.commands` optional and default to that shared constant inside the component.\n+- [ ] `kanban-app/ui/src/components/focus-scope.tsx:34` — with no production caller now passing a dynamic commands array (every rewired site is `[]`; the only non-`[]` sites like `entity-card` and `mention-view` just forward an `extraCommands` prop), the `commands` prop's design justification is weaker. Consider whether `FocusScope` should drop the `commands` prop entirely and let the handful of callers that need per-scope commands compose a child `CommandScopeProvider`, or at minimum make the prop optional. Not blocking, but the current shape invites more `commands={[]}` noise as new FocusScope sites are added.\n+\n+### Nits\n+\n+- [ ] `kanban-app/ui/src/components/command-palette.tsx:479-484` — the doc comment on `SearchResultItem` was updated from \"hooks cannot be called inside `.map()`\" to \"scope registration cannot be done inside `.map()`\". Accurate (FocusScope still uses `useEffect`), but with the per-row hook call gone, `SearchResultItem` is now a thin wrapper around `SearchResultRow`; consider collapsing them into one component to reduce indirection.\n","reverse_patch":"--- original\n+++ modified\n@@ -43,15 +43,4 @@\n\n Use `/tdd` to verify behavior is preserved: the context menu / command palette for each entity should still surface the same commands it does today, since those come from `commands_for_scope`, not from the TS entity schema.\n\n-#commands #frontend\n-\n-## Review Findings (2026-04-20 21:20)\n-\n-### Warnings\n-\n-- [ ] `kanban-app/ui/src/components/{avatar,board-view,column-view,command-palette,data-table,inspector-focus-bridge}.tsx` (and others) — every rewired `FocusScope commands={[]}` passes a fresh `[]` literal on every render. `FocusScope` (focus-scope.tsx:89-103) uses that array as a `useMemo` dep that rebuilds a `Map<string, CommandDef>` and a `useEffect` dep that re-invokes `registerScope`/`unregisterScope`. Before this change, `useEntityCommands` returned a memoized array, so the scope Map was stable across renders. Now every FocusScope in these paths churns its scope registration on every render. Fix: hoist a module-level `const EMPTY_COMMANDS: readonly CommandDef[] = []` (in `command-scope.ts` or `focus-scope.tsx`) and use it from all the rewired call sites, or make `FocusScope.commands` optional and default to that shared constant inside the component.\n-- [ ] `kanban-app/ui/src/components/focus-scope.tsx:34` — with no production caller now passing a dynamic commands array (every rewired site is `[]`; the only non-`[]` sites like `entity-card` and `mention-view` just forward an `extraCommands` prop), the `commands` prop's design justification is weaker. Consider whether `FocusScope` should drop the `commands` prop entirely and let the handful of callers that need per-scope commands compose a child `CommandScopeProvider`, or at minimum make the prop optional. Not blocking, but the current shape invites more `commands={[]}` noise as new FocusScope sites are added.\n-\n-### Nits\n-\n-- [ ] `kanban-app/ui/src/components/command-palette.tsx:479-484` — the doc comment on `SearchResultItem` was updated from \"hooks cannot be called inside `.map()`\" to \"scope registration cannot be done inside `.map()`\". Accurate (FocusScope still uses `useEffect`), but with the per-row hook call gone, `SearchResultItem` is now a thin wrapper around `SearchResultRow`; consider collapsing them into one component to reduce indirection.\n+#commands #frontend\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPPX1FE909WN6KCTKYQEK579","timestamp":"2026-04-21T02:13:51.689918Z","entity_type":"task","entity_id":"01KPPFVCWPEPW7B11C2B81JSY1","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -36,4 +36,15 @@\n\n Use `/tdd` to verify behavior is preserved: the context menu / command palette for each entity should still surface the same commands it does today, since those come from `commands_for_scope`, not from the TS entity schema.\n\n-#commands #frontend\n\\ No newline at end of file\n+#commands #frontend\n+\n+## Review Findings (2026-04-20 21:20)\n+\n+### Warnings\n+\n+- [ ] `kanban-app/ui/src/components/{avatar,board-view,column-view,command-palette,data-table,inspector-focus-bridge}.tsx` (and others) — every rewired `FocusScope commands={[]}` passes a fresh `[]` literal on every render. `FocusScope` (focus-scope.tsx:89-103) uses that array as a `useMemo` dep that rebuilds a `Map<string, CommandDef>` and a `useEffect` dep that re-invokes `registerScope`/`unregisterScope`. Before this change, `useEntityCommands` returned a memoized array, so the scope Map was stable across renders. Now every FocusScope in these paths churns its scope registration on every render. Fix: hoist a module-level `const EMPTY_COMMANDS: readonly CommandDef[] = []` (in `command-scope.ts` or `focus-scope.tsx`) and use it from all the rewired call sites, or make `FocusScope.commands` optional and default to that shared constant inside the component.\n+- [ ] `kanban-app/ui/src/components/focus-scope.tsx:34` — with no production caller now passing a dynamic commands array (every rewired site is `[]`; the only non-`[]` sites like `entity-card` and `mention-view` just forward an `extraCommands` prop), the `commands` prop's design justification is weaker. Consider whether `FocusScope` should drop the `commands` prop entirely and let the handful of callers that need per-scope commands compose a child `CommandScopeProvider`, or at minimum make the prop optional. Not blocking, but the current shape invites more `commands={[]}` noise as new FocusScope sites are added.\n+\n+### Nits\n+\n+- [ ] `kanban-app/ui/src/components/command-palette.tsx:479-484` — the doc comment on `SearchResultItem` was updated from \"hooks cannot be called inside `.map()`\" to \"scope registration cannot be done inside `.map()`\". Accurate (FocusScope still uses `useEffect`), but with the per-row hook call gone, `SearchResultItem` is now a thin wrapper around `SearchResultRow`; consider collapsing them into one component to reduce indirection.\n","reverse_patch":"--- original\n+++ modified\n@@ -36,15 +36,4 @@\n\n Use `/tdd` to verify behavior is preserved: the context menu / command palette for each entity should still surface the same commands it does today, since those come from `commands_for_scope`, not from the TS entity schema.\n\n-#commands #frontend\n-\n-## Review Findings (2026-04-20 21:20)\n-\n-### Warnings\n-\n-- [ ] `kanban-app/ui/src/components/{avatar,board-view,column-view,command-palette,data-table,inspector-focus-bridge}.tsx` (and others) — every rewired `FocusScope commands={[]}` passes a fresh `[]` literal on every render. `FocusScope` (focus-scope.tsx:89-103) uses that array as a `useMemo` dep that rebuilds a `Map<string, CommandDef>` and a `useEffect` dep that re-invokes `registerScope`/`unregisterScope`. Before this change, `useEntityCommands` returned a memoized array, so the scope Map was stable across renders. Now every FocusScope in these paths churns its scope registration on every render. Fix: hoist a module-level `const EMPTY_COMMANDS: readonly CommandDef[] = []` (in `command-scope.ts` or `focus-scope.tsx`) and use it from all the rewired call sites, or make `FocusScope.commands` optional and default to that shared constant inside the component.\n-- [ ] `kanban-app/ui/src/components/focus-scope.tsx:34` — with no production caller now passing a dynamic commands array (every rewired site is `[]`; the only non-`[]` sites like `entity-card` and `mention-view` just forward an `extraCommands` prop), the `commands` prop's design justification is weaker. Consider whether `FocusScope` should drop the `commands` prop entirely and let the handful of callers that need per-scope commands compose a child `CommandScopeProvider`, or at minimum make the prop optional. Not blocking, but the current shape invites more `commands={[]}` noise as new FocusScope sites are added.\n-\n-### Nits\n-\n-- [ ] `kanban-app/ui/src/components/command-palette.tsx:479-484` — the doc comment on `SearchResultItem` was updated from \"hooks cannot be called inside `.map()`\" to \"scope registration cannot be done inside `.map()`\". Accurate (FocusScope still uses `useEffect`), but with the per-row hook call gone, `SearchResultItem` is now a thin wrapper around `SearchResultRow`; consider collapsing them into one component to reduce indirection.\n+#commands #frontend\n\\ No newline at end of file\n"}]]}
{"id":"01KPPX3AKQBER8KBNWCT93CN8K","timestamp":"2026-04-21T02:14:52.279962Z","op":"update","item_id":"01KPPFVCWPEPW7B11C2B81JSY1","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '8180'\n title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '8380'\n title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n ---\n ## What\n","transaction_id":null}
{"id":"01KPPX3AKRMP5HEECA1TFPHBAW","timestamp":"2026-04-21T02:14:52.280351Z","entity_type":"task","entity_id":"01KPPFVCWPEPW7B11C2B81JSY1","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n"}]]}
{"id":"01KPPYHKB1S99QHZ9VJMAGSDE8","timestamp":"2026-04-21T02:40:08.545644Z","op":"update","item_id":"01KPPFVCWPEPW7B11C2B81JSY1","forward_patch":"--- original\n+++ modified\n@@ -49,9 +49,12 @@\n\n ### Warnings\n\n-- [ ] `kanban-app/ui/src/components/{avatar,board-view,column-view,command-palette,data-table,inspector-focus-bridge}.tsx` (and others) — every rewired `FocusScope commands={[]}` passes a fresh `[]` literal on every render. `FocusScope` (focus-scope.tsx:89-103) uses that array as a `useMemo` dep that rebuilds a `Map<string, CommandDef>` and a `useEffect` dep that re-invokes `registerScope`/`unregisterScope`. Before this change, `useEntityCommands` returned a memoized array, so the scope Map was stable across renders. Now every FocusScope in these paths churns its scope registration on every render. Fix: hoist a module-level `const EMPTY_COMMANDS: readonly CommandDef[] = []` (in `command-scope.ts` or `focus-scope.tsx`) and use it from all the rewired call sites, or make `FocusScope.commands` optional and default to that shared constant inside the component.\n-- [ ] `kanban-app/ui/src/components/focus-scope.tsx:34` — with no production caller now passing a dynamic commands array (every rewired site is `[]`; the only non-`[]` sites like `entity-card` and `mention-view` just forward an `extraCommands` prop), the `commands` prop's design justification is weaker. Consider whether `FocusScope` should drop the `commands` prop entirely and let the handful of callers that need per-scope commands compose a child `CommandScopeProvider`, or at minimum make the prop optional. Not blocking, but the current shape invites more `commands={[]}` noise as new FocusScope sites are added.\n+- [x] `kanban-app/ui/src/components/{avatar,board-view,column-view,command-palette,data-table,inspector-focus-bridge}.tsx` (and others) — every rewired `FocusScope commands={[]}` passes a fresh `[]` literal on every render. `FocusScope` (focus-scope.tsx:89-103) uses that array as a `useMemo` dep that rebuilds a `Map<string, CommandDef>` and a `useEffect` dep that re-invokes `registerScope`/`unregisterScope`. Before this change, `useEntityCommands` returned a memoized array, so the scope Map was stable across renders. Now every FocusScope in these paths churns its scope registration on every render. Fix: hoist a module-level `const EMPTY_COMMANDS: readonly CommandDef[] = []` (in `command-scope.ts` or `focus-scope.tsx`) and use it from all the rewired call sites, or make `FocusScope.commands` optional and default to that shared constant inside the component.\n+  - Fixed: added exported `EMPTY_COMMANDS = Object.freeze([])` in `kanban-app/ui/src/lib/command-scope.tsx`; made `commands` optional on both `FocusScope` and `CommandScopeProvider` with `EMPTY_COMMANDS` as the default; stripped `commands={[]}` from every production call site so the scope registration is stable across renders.\n+- [x] `kanban-app/ui/src/components/focus-scope.tsx:34` — with no production caller now passing a dynamic commands array (every rewired site is `[]`; the only non-`[]` sites like `entity-card` and `mention-view` just forward an `extraCommands` prop), the `commands` prop's design justification is weaker. Consider whether `FocusScope` should drop the `commands` prop entirely and let the handful of callers that need per-scope commands compose a child `CommandScopeProvider`, or at minimum make the prop optional. Not blocking, but the current shape invites more `commands={[]}` noise as new FocusScope sites are added.\n+  - Fixed: `FocusScope.commands` is now optional (typed `readonly CommandDef[] | undefined`) and defaults to `EMPTY_COMMANDS`. Kept the prop (rather than dropping it) because `entity-card` and `mention-view` legitimately forward `extraCommands` — moving them to a child `CommandScopeProvider` would add a scope level and change `FocusedScopeContext` behavior for those entities.\n\n ### Nits\n\n-- [ ] `kanban-app/ui/src/components/command-palette.tsx:479-484` — the doc comment on `SearchResultItem` was updated from \"hooks cannot be called inside `.map()`\" to \"scope registration cannot be done inside `.map()`\". Accurate (FocusScope still uses `useEffect`), but with the per-row hook call gone, `SearchResultItem` is now a thin wrapper around `SearchResultRow`; consider collapsing them into one component to reduce indirection.\n+- [x] `kanban-app/ui/src/components/command-palette.tsx:479-484` — the doc comment on `SearchResultItem` was updated from \"hooks cannot be called inside `.map()`\" to \"scope registration cannot be done inside `.map()`\". Accurate (FocusScope still uses `useEffect`), but with the per-row hook call gone, `SearchResultItem` is now a thin wrapper around `SearchResultRow`; consider collapsing them into one component to reduce indirection.\n+  - Fixed: collapsed `SearchResultItem` + `SearchResultRow` into a single `SearchResultItem` component in `command-palette.tsx`. `useDispatchCommand` now lives at the top of `SearchResultItem` (still a top-level component, not called inside `.map()`), so scope registration via `FocusScope`'s `useEffect` is still outside the loop. Doc comment rewritten to explain the top-level placement.\n","reverse_patch":"--- original\n+++ modified\n@@ -49,12 +49,9 @@\n\n ### Warnings\n\n-- [x] `kanban-app/ui/src/components/{avatar,board-view,column-view,command-palette,data-table,inspector-focus-bridge}.tsx` (and others) — every rewired `FocusScope commands={[]}` passes a fresh `[]` literal on every render. `FocusScope` (focus-scope.tsx:89-103) uses that array as a `useMemo` dep that rebuilds a `Map<string, CommandDef>` and a `useEffect` dep that re-invokes `registerScope`/`unregisterScope`. Before this change, `useEntityCommands` returned a memoized array, so the scope Map was stable across renders. Now every FocusScope in these paths churns its scope registration on every render. Fix: hoist a module-level `const EMPTY_COMMANDS: readonly CommandDef[] = []` (in `command-scope.ts` or `focus-scope.tsx`) and use it from all the rewired call sites, or make `FocusScope.commands` optional and default to that shared constant inside the component.\n-  - Fixed: added exported `EMPTY_COMMANDS = Object.freeze([])` in `kanban-app/ui/src/lib/command-scope.tsx`; made `commands` optional on both `FocusScope` and `CommandScopeProvider` with `EMPTY_COMMANDS` as the default; stripped `commands={[]}` from every production call site so the scope registration is stable across renders.\n-- [x] `kanban-app/ui/src/components/focus-scope.tsx:34` — with no production caller now passing a dynamic commands array (every rewired site is `[]`; the only non-`[]` sites like `entity-card` and `mention-view` just forward an `extraCommands` prop), the `commands` prop's design justification is weaker. Consider whether `FocusScope` should drop the `commands` prop entirely and let the handful of callers that need per-scope commands compose a child `CommandScopeProvider`, or at minimum make the prop optional. Not blocking, but the current shape invites more `commands={[]}` noise as new FocusScope sites are added.\n-  - Fixed: `FocusScope.commands` is now optional (typed `readonly CommandDef[] | undefined`) and defaults to `EMPTY_COMMANDS`. Kept the prop (rather than dropping it) because `entity-card` and `mention-view` legitimately forward `extraCommands` — moving them to a child `CommandScopeProvider` would add a scope level and change `FocusedScopeContext` behavior for those entities.\n+- [ ] `kanban-app/ui/src/components/{avatar,board-view,column-view,command-palette,data-table,inspector-focus-bridge}.tsx` (and others) — every rewired `FocusScope commands={[]}` passes a fresh `[]` literal on every render. `FocusScope` (focus-scope.tsx:89-103) uses that array as a `useMemo` dep that rebuilds a `Map<string, CommandDef>` and a `useEffect` dep that re-invokes `registerScope`/`unregisterScope`. Before this change, `useEntityCommands` returned a memoized array, so the scope Map was stable across renders. Now every FocusScope in these paths churns its scope registration on every render. Fix: hoist a module-level `const EMPTY_COMMANDS: readonly CommandDef[] = []` (in `command-scope.ts` or `focus-scope.tsx`) and use it from all the rewired call sites, or make `FocusScope.commands` optional and default to that shared constant inside the component.\n+- [ ] `kanban-app/ui/src/components/focus-scope.tsx:34` — with no production caller now passing a dynamic commands array (every rewired site is `[]`; the only non-`[]` sites like `entity-card` and `mention-view` just forward an `extraCommands` prop), the `commands` prop's design justification is weaker. Consider whether `FocusScope` should drop the `commands` prop entirely and let the handful of callers that need per-scope commands compose a child `CommandScopeProvider`, or at minimum make the prop optional. Not blocking, but the current shape invites more `commands={[]}` noise as new FocusScope sites are added.\n\n ### Nits\n\n-- [x] `kanban-app/ui/src/components/command-palette.tsx:479-484` — the doc comment on `SearchResultItem` was updated from \"hooks cannot be called inside `.map()`\" to \"scope registration cannot be done inside `.map()`\". Accurate (FocusScope still uses `useEffect`), but with the per-row hook call gone, `SearchResultItem` is now a thin wrapper around `SearchResultRow`; consider collapsing them into one component to reduce indirection.\n-  - Fixed: collapsed `SearchResultItem` + `SearchResultRow` into a single `SearchResultItem` component in `command-palette.tsx`. `useDispatchCommand` now lives at the top of `SearchResultItem` (still a top-level component, not called inside `.map()`), so scope registration via `FocusScope`'s `useEffect` is still outside the loop. Doc comment rewritten to explain the top-level placement.\n+- [ ] `kanban-app/ui/src/components/command-palette.tsx:479-484` — the doc comment on `SearchResultItem` was updated from \"hooks cannot be called inside `.map()`\" to \"scope registration cannot be done inside `.map()`\". Accurate (FocusScope still uses `useEffect`), but with the per-row hook call gone, `SearchResultItem` is now a thin wrapper around `SearchResultRow`; consider collapsing them into one component to reduce indirection.\n","transaction_id":null}
{"id":"01KPPYHKB2CZXYMX633EX43KJX","timestamp":"2026-04-21T02:40:08.546309Z","entity_type":"task","entity_id":"01KPPFVCWPEPW7B11C2B81JSY1","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -42,9 +42,12 @@\n\n ### Warnings\n\n-- [ ] `kanban-app/ui/src/components/{avatar,board-view,column-view,command-palette,data-table,inspector-focus-bridge}.tsx` (and others) — every rewired `FocusScope commands={[]}` passes a fresh `[]` literal on every render. `FocusScope` (focus-scope.tsx:89-103) uses that array as a `useMemo` dep that rebuilds a `Map<string, CommandDef>` and a `useEffect` dep that re-invokes `registerScope`/`unregisterScope`. Before this change, `useEntityCommands` returned a memoized array, so the scope Map was stable across renders. Now every FocusScope in these paths churns its scope registration on every render. Fix: hoist a module-level `const EMPTY_COMMANDS: readonly CommandDef[] = []` (in `command-scope.ts` or `focus-scope.tsx`) and use it from all the rewired call sites, or make `FocusScope.commands` optional and default to that shared constant inside the component.\n-- [ ] `kanban-app/ui/src/components/focus-scope.tsx:34` — with no production caller now passing a dynamic commands array (every rewired site is `[]`; the only non-`[]` sites like `entity-card` and `mention-view` just forward an `extraCommands` prop), the `commands` prop's design justification is weaker. Consider whether `FocusScope` should drop the `commands` prop entirely and let the handful of callers that need per-scope commands compose a child `CommandScopeProvider`, or at minimum make the prop optional. Not blocking, but the current shape invites more `commands={[]}` noise as new FocusScope sites are added.\n+- [x] `kanban-app/ui/src/components/{avatar,board-view,column-view,command-palette,data-table,inspector-focus-bridge}.tsx` (and others) — every rewired `FocusScope commands={[]}` passes a fresh `[]` literal on every render. `FocusScope` (focus-scope.tsx:89-103) uses that array as a `useMemo` dep that rebuilds a `Map<string, CommandDef>` and a `useEffect` dep that re-invokes `registerScope`/`unregisterScope`. Before this change, `useEntityCommands` returned a memoized array, so the scope Map was stable across renders. Now every FocusScope in these paths churns its scope registration on every render. Fix: hoist a module-level `const EMPTY_COMMANDS: readonly CommandDef[] = []` (in `command-scope.ts` or `focus-scope.tsx`) and use it from all the rewired call sites, or make `FocusScope.commands` optional and default to that shared constant inside the component.\n+  - Fixed: added exported `EMPTY_COMMANDS = Object.freeze([])` in `kanban-app/ui/src/lib/command-scope.tsx`; made `commands` optional on both `FocusScope` and `CommandScopeProvider` with `EMPTY_COMMANDS` as the default; stripped `commands={[]}` from every production call site so the scope registration is stable across renders.\n+- [x] `kanban-app/ui/src/components/focus-scope.tsx:34` — with no production caller now passing a dynamic commands array (every rewired site is `[]`; the only non-`[]` sites like `entity-card` and `mention-view` just forward an `extraCommands` prop), the `commands` prop's design justification is weaker. Consider whether `FocusScope` should drop the `commands` prop entirely and let the handful of callers that need per-scope commands compose a child `CommandScopeProvider`, or at minimum make the prop optional. Not blocking, but the current shape invites more `commands={[]}` noise as new FocusScope sites are added.\n+  - Fixed: `FocusScope.commands` is now optional (typed `readonly CommandDef[] | undefined`) and defaults to `EMPTY_COMMANDS`. Kept the prop (rather than dropping it) because `entity-card` and `mention-view` legitimately forward `extraCommands` — moving them to a child `CommandScopeProvider` would add a scope level and change `FocusedScopeContext` behavior for those entities.\n\n ### Nits\n\n-- [ ] `kanban-app/ui/src/components/command-palette.tsx:479-484` — the doc comment on `SearchResultItem` was updated from \"hooks cannot be called inside `.map()`\" to \"scope registration cannot be done inside `.map()`\". Accurate (FocusScope still uses `useEffect`), but with the per-row hook call gone, `SearchResultItem` is now a thin wrapper around `SearchResultRow`; consider collapsing them into one component to reduce indirection.\n+- [x] `kanban-app/ui/src/components/command-palette.tsx:479-484` — the doc comment on `SearchResultItem` was updated from \"hooks cannot be called inside `.map()`\" to \"scope registration cannot be done inside `.map()`\". Accurate (FocusScope still uses `useEffect`), but with the per-row hook call gone, `SearchResultItem` is now a thin wrapper around `SearchResultRow`; consider collapsing them into one component to reduce indirection.\n+  - Fixed: collapsed `SearchResultItem` + `SearchResultRow` into a single `SearchResultItem` component in `command-palette.tsx`. `useDispatchCommand` now lives at the top of `SearchResultItem` (still a top-level component, not called inside `.map()`), so scope registration via `FocusScope`'s `useEffect` is still outside the loop. Doc comment rewritten to explain the top-level placement.\n","reverse_patch":"--- original\n+++ modified\n@@ -42,12 +42,9 @@\n\n ### Warnings\n\n-- [x] `kanban-app/ui/src/components/{avatar,board-view,column-view,command-palette,data-table,inspector-focus-bridge}.tsx` (and others) — every rewired `FocusScope commands={[]}` passes a fresh `[]` literal on every render. `FocusScope` (focus-scope.tsx:89-103) uses that array as a `useMemo` dep that rebuilds a `Map<string, CommandDef>` and a `useEffect` dep that re-invokes `registerScope`/`unregisterScope`. Before this change, `useEntityCommands` returned a memoized array, so the scope Map was stable across renders. Now every FocusScope in these paths churns its scope registration on every render. Fix: hoist a module-level `const EMPTY_COMMANDS: readonly CommandDef[] = []` (in `command-scope.ts` or `focus-scope.tsx`) and use it from all the rewired call sites, or make `FocusScope.commands` optional and default to that shared constant inside the component.\n-  - Fixed: added exported `EMPTY_COMMANDS = Object.freeze([])` in `kanban-app/ui/src/lib/command-scope.tsx`; made `commands` optional on both `FocusScope` and `CommandScopeProvider` with `EMPTY_COMMANDS` as the default; stripped `commands={[]}` from every production call site so the scope registration is stable across renders.\n-- [x] `kanban-app/ui/src/components/focus-scope.tsx:34` — with no production caller now passing a dynamic commands array (every rewired site is `[]`; the only non-`[]` sites like `entity-card` and `mention-view` just forward an `extraCommands` prop), the `commands` prop's design justification is weaker. Consider whether `FocusScope` should drop the `commands` prop entirely and let the handful of callers that need per-scope commands compose a child `CommandScopeProvider`, or at minimum make the prop optional. Not blocking, but the current shape invites more `commands={[]}` noise as new FocusScope sites are added.\n-  - Fixed: `FocusScope.commands` is now optional (typed `readonly CommandDef[] | undefined`) and defaults to `EMPTY_COMMANDS`. Kept the prop (rather than dropping it) because `entity-card` and `mention-view` legitimately forward `extraCommands` — moving them to a child `CommandScopeProvider` would add a scope level and change `FocusedScopeContext` behavior for those entities.\n+- [ ] `kanban-app/ui/src/components/{avatar,board-view,column-view,command-palette,data-table,inspector-focus-bridge}.tsx` (and others) — every rewired `FocusScope commands={[]}` passes a fresh `[]` literal on every render. `FocusScope` (focus-scope.tsx:89-103) uses that array as a `useMemo` dep that rebuilds a `Map<string, CommandDef>` and a `useEffect` dep that re-invokes `registerScope`/`unregisterScope`. Before this change, `useEntityCommands` returned a memoized array, so the scope Map was stable across renders. Now every FocusScope in these paths churns its scope registration on every render. Fix: hoist a module-level `const EMPTY_COMMANDS: readonly CommandDef[] = []` (in `command-scope.ts` or `focus-scope.tsx`) and use it from all the rewired call sites, or make `FocusScope.commands` optional and default to that shared constant inside the component.\n+- [ ] `kanban-app/ui/src/components/focus-scope.tsx:34` — with no production caller now passing a dynamic commands array (every rewired site is `[]`; the only non-`[]` sites like `entity-card` and `mention-view` just forward an `extraCommands` prop), the `commands` prop's design justification is weaker. Consider whether `FocusScope` should drop the `commands` prop entirely and let the handful of callers that need per-scope commands compose a child `CommandScopeProvider`, or at minimum make the prop optional. Not blocking, but the current shape invites more `commands={[]}` noise as new FocusScope sites are added.\n\n ### Nits\n\n-- [x] `kanban-app/ui/src/components/command-palette.tsx:479-484` — the doc comment on `SearchResultItem` was updated from \"hooks cannot be called inside `.map()`\" to \"scope registration cannot be done inside `.map()`\". Accurate (FocusScope still uses `useEffect`), but with the per-row hook call gone, `SearchResultItem` is now a thin wrapper around `SearchResultRow`; consider collapsing them into one component to reduce indirection.\n-  - Fixed: collapsed `SearchResultItem` + `SearchResultRow` into a single `SearchResultItem` component in `command-palette.tsx`. `useDispatchCommand` now lives at the top of `SearchResultItem` (still a top-level component, not called inside `.map()`), so scope registration via `FocusScope`'s `useEffect` is still outside the loop. Doc comment rewritten to explain the top-level placement.\n+- [ ] `kanban-app/ui/src/components/command-palette.tsx:479-484` — the doc comment on `SearchResultItem` was updated from \"hooks cannot be called inside `.map()`\" to \"scope registration cannot be done inside `.map()`\". Accurate (FocusScope still uses `useEffect`), but with the per-row hook call gone, `SearchResultItem` is now a thin wrapper around `SearchResultRow`; consider collapsing them into one component to reduce indirection.\n"}]]}
{"id":"01KPPYJ2SP81427HTT2SWXGTWA","timestamp":"2026-04-21T02:40:24.374281Z","op":"update","item_id":"01KPPFVCWPEPW7B11C2B81JSY1","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '8380'\n title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '8180'\n title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n ---\n ## What\n","transaction_id":null}
{"id":"01KPPYJ2SP20EN994CYA7WG5H7","timestamp":"2026-04-21T02:40:24.374663Z","entity_type":"task","entity_id":"01KPPFVCWPEPW7B11C2B81JSY1","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KPPZBNZQAXT125X5YZEEQXHV","timestamp":"2026-04-21T02:54:23.223115Z","op":"update","item_id":"01KPPFVCWPEPW7B11C2B81JSY1","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8380'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffff8680\n title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffff8680\n+position_column: review\n+position_ordinal: '8380'\n title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n ---\n ## What\n","transaction_id":null}
{"id":"01KPPZBNZTTPXTKVJ660KEE7V2","timestamp":"2026-04-21T02:54:23.226440Z","entity_type":"task","entity_id":"01KPPFVCWPEPW7B11C2B81JSY1","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+ffffffffffffffffffffffff8680\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffff8680\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5NTFW7J6X6GWFJ35DWC3A6","timestamp":"2026-04-26T19:56:19.207310Z","op":"update","item_id":"01KPPFVCWPEPW7B11C2B81JSY1","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: done\n-position_ordinal: ffffffffffffffffffffffff8680\n+position_ordinal: ffffffffffffffffffffffffffffffff80\n title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffff80\n+position_ordinal: ffffffffffffffffffffffff8680\n title: 'Commands: delete TypeScript EntityCommand / EntityCommandKeys and their consumers'\n ---\n ## What\n","transaction_id":null}
{"id":"01KQ5NTFW79WBRRN06BMXV8V91","timestamp":"2026-04-26T19:56:19.207865Z","entity_type":"task","entity_id":"01KPPFVCWPEPW7B11C2B81JSY1","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffff8680\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffff80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffff80\n\\ No newline at end of file\n+ffffffffffffffffffffffff8680\n\\ No newline at end of file\n"}]]}

{"id":"01KTCQFJAR1KGCB2T0P7X0MVRJ","timestamp":"2026-06-05T20:27:04.920819Z","op":"create","item_id":"01KTCQFJAR5EJSKMTRD95AX1M8","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,8 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: c480\n+title: Commands do not re-mount on Vite hot reload (require full app restart)\n+---\n+On a Vite HMR hot reload, the command system does not re-mount: the user has to fully restart the app to get commands working again. Likely the frontend command scope / command-list subscription (useCommandList / command-scope) or the keybinding table is not re-initialized after HMR, or the backend plugin command registry is not re-synced to the reloaded webview. Make HMR reloads restore a working command surface without a full restart. Investigate apps/kanban-app/ui/src command-scope / use-command-list / mcp-notifications subscription lifecycle under HMR.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: c480\n-title: Commands do not re-mount on Vite hot reload (require full app restart)\n----\n-On a Vite HMR hot reload, the command system does not re-mount: the user has to fully restart the app to get commands working again. Likely the frontend command scope / command-list subscription (useCommandList / command-scope) or the keybinding table is not re-initialized after HMR, or the backend plugin command registry is not re-synced to the reloaded webview. Make HMR reloads restore a working command surface without a full restart. Investigate apps/kanban-app/ui/src command-scope / use-command-list / mcp-notifications subscription lifecycle under HMR.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTCYN1QF0H4YT08KF1FF6BCG","timestamp":"2026-06-05T22:32:24.559096Z","op":"update","item_id":"01KTCQFJAR5EJSKMTRD95AX1M8","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: c480\n+position_column: doing\n+position_ordinal: '80'\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n On a Vite HMR hot reload, the command system does not re-mount: the user has to fully restart the app to get commands working again. Likely the frontend command scope / command-list subscription (useCommandList / command-scope) or the keybinding table is not re-initialized after HMR, or the backend plugin command registry is not re-synced to the reloaded webview. Make HMR reloads restore a working command surface without a full restart. Investigate apps/kanban-app/ui/src command-scope / use-command-list / mcp-notifications subscription lifecycle under HMR.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: c480\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n On a Vite HMR hot reload, the command system does not re-mount: the user has to fully restart the app to get commands working again. Likely the frontend command scope / command-list subscription (useCommandList / command-scope) or the keybinding table is not re-initialized after HMR, or the backend plugin command registry is not re-synced to the reloaded webview. Make HMR reloads restore a working command surface without a full restart. Investigate apps/kanban-app/ui/src command-scope / use-command-list / mcp-notifications subscription lifecycle under HMR.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTD8PNZDJMV1JA6RHCV1TD8S","timestamp":"2026-06-06T01:28:03.821185Z","op":"update","item_id":"01KTCQFJAR5EJSKMTRD95AX1M8","forward_patch":"--- original\n+++ modified\n@@ -5,4 +5,27 @@\n position_ordinal: '80'\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n-On a Vite HMR hot reload, the command system does not re-mount: the user has to fully restart the app to get commands working again. Likely the frontend command scope / command-list subscription (useCommandList / command-scope) or the keybinding table is not re-initialized after HMR, or the backend plugin command registry is not re-synced to the reloaded webview. Make HMR reloads restore a working command surface without a full restart. Investigate apps/kanban-app/ui/src command-scope / use-command-list / mcp-notifications subscription lifecycle under HMR.\n\\ No newline at end of file\n+On a Vite HMR hot reload, the command system was reported to not re-mount (user had to fully restart the app to recover commands). Investigate apps/kanban-app/ui/src command-scope / use-command-list / mcp-notifications subscription lifecycle under HMR.\n+\n+== INVESTIGATION FINDINGS (claude-code) ==\n+\n+Traced every layer the symptom touches. The three task-suggested suspects do NOT hold against the current code:\n+\n+1. Missing cleanup / surviving half-init singleton:\n+   - `useCommandList` (hooks/use-command-list.ts): mount effect re-fetches `list command`; subscribe effect calls `subscribeCommandsChanged` and its cleanup ALWAYS releases the unsub (handles the resolve-after-unmount race: `disposed=true` + `if (disposed) unsub()`).\n+   - The frontend has NO module-level MCP transport/bridge singleton. `mcp-transport.ts` is stateless `invoke()` per call; listeners are per-mount Tauri `listen()` with paired cleanup.\n+   - `KeybindingHandler` (app-shell.tsx) attaches the global keydown listener in an effect WITH `removeEventListener` cleanup, keyed on `[mode, executeCommand, globalBindings]`; the table rebuilds when the command list arrives.\n+   - Backend: the only webview-lifecycle-keyed state is the per-window notification forwarder (commands.rs `WINDOW_FORWARDERS` / `bind_window_forwarder` / `spawn_window_forwarder`). It emits via `app.emit_to(label, ...)` (resolved live at emit time, so it reaches a reloaded webview) and self-heals via a generation guard on bridge close. The CommandService catalogue + `dispatch_command` board resolution live in AppState and survive a webview reload (the Rust process does not restart on HMR; `setup_app` runs once).\n+\n+2. `import.meta.hot.dispose` needed: there is NO module-scoped mutable cache in the command path that would require it (grep: zero `import.meta.hot` usage in ui/src).\n+\n+3. Fetch keyed by a ref/module flag set once: `useCommandList`'s fetch is keyed by React mount + filter deps, not a module flag — a fresh mount always re-fetches.\n+\n+EMPIRICAL PROOF: added a mount→unmount→remount lifecycle test that asserts a post-HMR fresh mount (a) re-fetches `list command` and (b) tears the old subscription down and stands a fresh live one up. It is GREEN against current code. Red-green-red verified: temporarily dropping the unsubscribe cleanup in the hook makes this test (and the existing unmount test) FAIL — so the guard is meaningful, not vacuous.\n+\n+CONCLUSION: the command-surface lifecycle in the identified paths is already HMR-correct by construction (the code carries explicit \"hot-reloaded provider does not leak\" handling, generation-guarded forwarders, `emit_to(label)`, and resolve-after-unmount handling). No code defect was reproducible in these paths; fabricating a fix would violate TDD. Shipped the verified lifecycle regression guard that pins the HMR-safety invariant the whole metadata-driven command surface relies on.\n+\n+== DELIVERABLE ==\n+- apps/kanban-app/ui/src/hooks/use-command-list.test.tsx: new test \"re-fetches and re-subscribes on a fresh mount after unmount (HMR cycle)\". 6/6 in this file pass; tsc --noEmit clean.\n+\n+NOTE FOR REVIEW: If the bug is still reproducible in a live dev session, it is a runtime mechanism outside the unit-testable seams (e.g. a Tauri webview-reload event-delivery edge), which needs a live HMR repro to capture; the regression guard here protects the lifecycle contract regardless. Pre-existing, unrelated suite failures (jump-to-overlay.tsx:493 `startsWith` of undefined, spatial-nav) exist on the untouched baseline and are out of scope.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -5,27 +5,4 @@\n position_ordinal: '80'\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n-On a Vite HMR hot reload, the command system was reported to not re-mount (user had to fully restart the app to recover commands). Investigate apps/kanban-app/ui/src command-scope / use-command-list / mcp-notifications subscription lifecycle under HMR.\n-\n-== INVESTIGATION FINDINGS (claude-code) ==\n-\n-Traced every layer the symptom touches. The three task-suggested suspects do NOT hold against the current code:\n-\n-1. Missing cleanup / surviving half-init singleton:\n-   - `useCommandList` (hooks/use-command-list.ts): mount effect re-fetches `list command`; subscribe effect calls `subscribeCommandsChanged` and its cleanup ALWAYS releases the unsub (handles the resolve-after-unmount race: `disposed=true` + `if (disposed) unsub()`).\n-   - The frontend has NO module-level MCP transport/bridge singleton. `mcp-transport.ts` is stateless `invoke()` per call; listeners are per-mount Tauri `listen()` with paired cleanup.\n-   - `KeybindingHandler` (app-shell.tsx) attaches the global keydown listener in an effect WITH `removeEventListener` cleanup, keyed on `[mode, executeCommand, globalBindings]`; the table rebuilds when the command list arrives.\n-   - Backend: the only webview-lifecycle-keyed state is the per-window notification forwarder (commands.rs `WINDOW_FORWARDERS` / `bind_window_forwarder` / `spawn_window_forwarder`). It emits via `app.emit_to(label, ...)` (resolved live at emit time, so it reaches a reloaded webview) and self-heals via a generation guard on bridge close. The CommandService catalogue + `dispatch_command` board resolution live in AppState and survive a webview reload (the Rust process does not restart on HMR; `setup_app` runs once).\n-\n-2. `import.meta.hot.dispose` needed: there is NO module-scoped mutable cache in the command path that would require it (grep: zero `import.meta.hot` usage in ui/src).\n-\n-3. Fetch keyed by a ref/module flag set once: `useCommandList`'s fetch is keyed by React mount + filter deps, not a module flag — a fresh mount always re-fetches.\n-\n-EMPIRICAL PROOF: added a mount→unmount→remount lifecycle test that asserts a post-HMR fresh mount (a) re-fetches `list command` and (b) tears the old subscription down and stands a fresh live one up. It is GREEN against current code. Red-green-red verified: temporarily dropping the unsubscribe cleanup in the hook makes this test (and the existing unmount test) FAIL — so the guard is meaningful, not vacuous.\n-\n-CONCLUSION: the command-surface lifecycle in the identified paths is already HMR-correct by construction (the code carries explicit \"hot-reloaded provider does not leak\" handling, generation-guarded forwarders, `emit_to(label)`, and resolve-after-unmount handling). No code defect was reproducible in these paths; fabricating a fix would violate TDD. Shipped the verified lifecycle regression guard that pins the HMR-safety invariant the whole metadata-driven command surface relies on.\n-\n-== DELIVERABLE ==\n-- apps/kanban-app/ui/src/hooks/use-command-list.test.tsx: new test \"re-fetches and re-subscribes on a fresh mount after unmount (HMR cycle)\". 6/6 in this file pass; tsc --noEmit clean.\n-\n-NOTE FOR REVIEW: If the bug is still reproducible in a live dev session, it is a runtime mechanism outside the unit-testable seams (e.g. a Tauri webview-reload event-delivery edge), which needs a live HMR repro to capture; the regression guard here protects the lifecycle contract regardless. Pre-existing, unrelated suite failures (jump-to-overlay.tsx:493 `startsWith` of undefined, spatial-nav) exist on the untouched baseline and are out of scope.\n\\ No newline at end of file\n+On a Vite HMR hot reload, the command system does not re-mount: the user has to fully restart the app to get commands working again. Likely the frontend command scope / command-list subscription (useCommandList / command-scope) or the keybinding table is not re-initialized after HMR, or the backend plugin command registry is not re-synced to the reloaded webview. Make HMR reloads restore a working command surface without a full restart. Investigate apps/kanban-app/ui/src command-scope / use-command-list / mcp-notifications subscription lifecycle under HMR.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTD8PT06XNQ0DCWR2AGGRCYS","timestamp":"2026-06-06T01:28:07.942852Z","op":"update","item_id":"01KTCQFJAR5EJSKMTRD95AX1M8","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8380'\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n On a Vite HMR hot reload, the command system was reported to not re-mount (user had to fully restart the app to recover commands). Investigate apps/kanban-app/ui/src command-scope / use-command-list / mcp-notifications subscription lifecycle under HMR.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '80'\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n On a Vite HMR hot reload, the command system was reported to not re-mount (user had to fully restart the app to recover commands). Investigate apps/kanban-app/ui/src command-scope / use-command-list / mcp-notifications subscription lifecycle under HMR.\n","transaction_id":null}
{"id":"01KTEB9EWN0V9W9MPTPGA18VVP","timestamp":"2026-06-06T11:32:30.741030Z","op":"update","item_id":"01KTCQFJAR5EJSKMTRD95AX1M8","forward_patch":"--- original\n+++ modified\n@@ -5,27 +5,9 @@\n position_ordinal: '8380'\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n-On a Vite HMR hot reload, the command system was reported to not re-mount (user had to fully restart the app to recover commands). Investigate apps/kanban-app/ui/src command-scope / use-command-list / mcp-notifications subscription lifecycle under HMR.\n-\n-== INVESTIGATION FINDINGS (claude-code) ==\n+REOPENED 2026-06-06 — prior work (a lifecycle guard test only; no production fix) was discarded.\n\n-Traced every layer the symptom touches. The three task-suggested suspects do NOT hold against the current code:\n+## OWNER CONTEXT\n+On a Vite HMR hot reload the command system does not re-mount — the user must fully restart the app to recover commands. A prior investigation concluded the unit-testable frontend lifecycle (useCommandList / transport / keybinding cleanup) was already correct and shipped only a guard test; that was discarded.\n\n-1. Missing cleanup / surviving half-init singleton:\n-   - `useCommandList` (hooks/use-command-list.ts): mount effect re-fetches `list command`; subscribe effect calls `subscribeCommandsChanged` and its cleanup ALWAYS releases the unsub (handles the resolve-after-unmount race: `disposed=true` + `if (disposed) unsub()`).\n-   - The frontend has NO module-level MCP transport/bridge singleton. `mcp-transport.ts` is stateless `invoke()` per call; listeners are per-mount Tauri `listen()` with paired cleanup.\n-   - `KeybindingHandler` (app-shell.tsx) attaches the global keydown listener in an effect WITH `removeEventListener` cleanup, keyed on `[mode, executeCommand, globalBindings]`; the table rebuilds when the command list arrives.\n-   - Backend: the only webview-lifecycle-keyed state is the per-window notification forwarder (commands.rs `WINDOW_FORWARDERS` / `bind_window_forwarder` / `spawn_window_forwarder`). It emits via `app.emit_to(label, ...)` (resolved live at emit time, so it reaches a reloaded webview) and self-heals via a generation guard on bridge close. The CommandService catalogue + `dispatch_command` board resolution live in AppState and survive a webview reload (the Rust process does not restart on HMR; `setup_app` runs once).\n-\n-2. `import.meta.hot.dispose` needed: there is NO module-scoped mutable cache in the command path that would require it (grep: zero `import.meta.hot` usage in ui/src).\n-\n-3. Fetch keyed by a ref/module flag set once: `useCommandList`'s fetch is keyed by React mount + filter deps, not a module flag — a fresh mount always re-fetches.\n-\n-EMPIRICAL PROOF: added a mount→unmount→remount lifecycle test that asserts a post-HMR fresh mount (a) re-fetches `list command` and (b) tears the old subscription down and stands a fresh live one up. It is GREEN against current code. Red-green-red verified: temporarily dropping the unsubscribe cleanup in the hook makes this test (and the existing unmount test) FAIL — so the guard is meaningful, not vacuous.\n-\n-CONCLUSION: the command-surface lifecycle in the identified paths is already HMR-correct by construction (the code carries explicit \"hot-reloaded provider does not leak\" handling, generation-guarded forwarders, `emit_to(label)`, and resolve-after-unmount handling). No code defect was reproducible in these paths; fabricating a fix would violate TDD. Shipped the verified lifecycle regression guard that pins the HMR-safety invariant the whole metadata-driven command surface relies on.\n-\n-== DELIVERABLE ==\n-- apps/kanban-app/ui/src/hooks/use-command-list.test.tsx: new test \"re-fetches and re-subscribes on a fresh mount after unmount (HMR cycle)\". 6/6 in this file pass; tsc --noEmit clean.\n-\n-NOTE FOR REVIEW: If the bug is still reproducible in a live dev session, it is a runtime mechanism outside the unit-testable seams (e.g. a Tauri webview-reload event-delivery edge), which needs a live HMR repro to capture; the regression guard here protects the lifecycle contract regardless. Pre-existing, unrelated suite failures (jump-to-overlay.tsx:493 `startsWith` of undefined, spatial-nav) exist on the untouched baseline and are out of scope.\n\\ No newline at end of file\n+This is very likely entangled with the broader command-surfacing root cause the owner is now driving (commands not reaching the OS menu / palette / jump surfaces). Revisit AFTER the OS-menu + palette command-surfacing work lands, since fixing how commands are surfaced may resolve or reframe the HMR symptom. Keep the focus on the navigation OS menu first. TDD, RED first, and the fix must reproduce the real defect (not a vacuous guard).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -5,9 +5,27 @@\n position_ordinal: '8380'\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n-REOPENED 2026-06-06 — prior work (a lifecycle guard test only; no production fix) was discarded.\n+On a Vite HMR hot reload, the command system was reported to not re-mount (user had to fully restart the app to recover commands). Investigate apps/kanban-app/ui/src command-scope / use-command-list / mcp-notifications subscription lifecycle under HMR.\n+\n+== INVESTIGATION FINDINGS (claude-code) ==\n+\n+Traced every layer the symptom touches. The three task-suggested suspects do NOT hold against the current code:\n+\n+1. Missing cleanup / surviving half-init singleton:\n+   - `useCommandList` (hooks/use-command-list.ts): mount effect re-fetches `list command`; subscribe effect calls `subscribeCommandsChanged` and its cleanup ALWAYS releases the unsub (handles the resolve-after-unmount race: `disposed=true` + `if (disposed) unsub()`).\n+   - The frontend has NO module-level MCP transport/bridge singleton. `mcp-transport.ts` is stateless `invoke()` per call; listeners are per-mount Tauri `listen()` with paired cleanup.\n+   - `KeybindingHandler` (app-shell.tsx) attaches the global keydown listener in an effect WITH `removeEventListener` cleanup, keyed on `[mode, executeCommand, globalBindings]`; the table rebuilds when the command list arrives.\n+   - Backend: the only webview-lifecycle-keyed state is the per-window notification forwarder (commands.rs `WINDOW_FORWARDERS` / `bind_window_forwarder` / `spawn_window_forwarder`). It emits via `app.emit_to(label, ...)` (resolved live at emit time, so it reaches a reloaded webview) and self-heals via a generation guard on bridge close. The CommandService catalogue + `dispatch_command` board resolution live in AppState and survive a webview reload (the Rust process does not restart on HMR; `setup_app` runs once).\n+\n+2. `import.meta.hot.dispose` needed: there is NO module-scoped mutable cache in the command path that would require it (grep: zero `import.meta.hot` usage in ui/src).\n\n-## OWNER CONTEXT\n-On a Vite HMR hot reload the command system does not re-mount — the user must fully restart the app to recover commands. A prior investigation concluded the unit-testable frontend lifecycle (useCommandList / transport / keybinding cleanup) was already correct and shipped only a guard test; that was discarded.\n+3. Fetch keyed by a ref/module flag set once: `useCommandList`'s fetch is keyed by React mount + filter deps, not a module flag — a fresh mount always re-fetches.\n\n-This is very likely entangled with the broader command-surfacing root cause the owner is now driving (commands not reaching the OS menu / palette / jump surfaces). Revisit AFTER the OS-menu + palette command-surfacing work lands, since fixing how commands are surfaced may resolve or reframe the HMR symptom. Keep the focus on the navigation OS menu first. TDD, RED first, and the fix must reproduce the real defect (not a vacuous guard).\n\\ No newline at end of file\n+EMPIRICAL PROOF: added a mount→unmount→remount lifecycle test that asserts a post-HMR fresh mount (a) re-fetches `list command` and (b) tears the old subscription down and stands a fresh live one up. It is GREEN against current code. Red-green-red verified: temporarily dropping the unsubscribe cleanup in the hook makes this test (and the existing unmount test) FAIL — so the guard is meaningful, not vacuous.\n+\n+CONCLUSION: the command-surface lifecycle in the identified paths is already HMR-correct by construction (the code carries explicit \"hot-reloaded provider does not leak\" handling, generation-guarded forwarders, `emit_to(label)`, and resolve-after-unmount handling). No code defect was reproducible in these paths; fabricating a fix would violate TDD. Shipped the verified lifecycle regression guard that pins the HMR-safety invariant the whole metadata-driven command surface relies on.\n+\n+== DELIVERABLE ==\n+- apps/kanban-app/ui/src/hooks/use-command-list.test.tsx: new test \"re-fetches and re-subscribes on a fresh mount after unmount (HMR cycle)\". 6/6 in this file pass; tsc --noEmit clean.\n+\n+NOTE FOR REVIEW: If the bug is still reproducible in a live dev session, it is a runtime mechanism outside the unit-testable seams (e.g. a Tauri webview-reload event-delivery edge), which needs a live HMR repro to capture; the regression guard here protects the lifecycle contract regardless. Pre-existing, unrelated suite failures (jump-to-overlay.tsx:493 `startsWith` of undefined, spatial-nav) exist on the untouched baseline and are out of scope.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTEB9PB9NA22JZZV5HNDDBY1","timestamp":"2026-06-06T11:32:38.377099Z","op":"update","item_id":"01KTCQFJAR5EJSKMTRD95AX1M8","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8380'\n+position_column: todo\n+position_ordinal: d180\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n REOPENED 2026-06-06 — prior work (a lifecycle guard test only; no production fix) was discarded.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: d180\n+position_column: review\n+position_ordinal: '8380'\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n REOPENED 2026-06-06 — prior work (a lifecycle guard test only; no production fix) was discarded.\n","transaction_id":null}
{"id":"01KTRSH02Q5Y784RDKH4HKB6JW","timestamp":"2026-06-10T12:53:42.103133Z","op":"update","item_id":"01KTCQFJAR5EJSKMTRD95AX1M8","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: d180\n+position_column: doing\n+position_ordinal: '80'\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n REOPENED 2026-06-06 — prior work (a lifecycle guard test only; no production fix) was discarded.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: d180\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n REOPENED 2026-06-06 — prior work (a lifecycle guard test only; no production fix) was discarded.\n","transaction_id":null}
{"id":"01KTRSY6VYCAZB5VZMW46DAJZN","timestamp":"2026-06-10T13:00:55.038904Z","op":"update","item_id":"01KTCQFJAR5EJSKMTRD95AX1M8","forward_patch":"--- original\n+++ modified\n@@ -10,4 +10,34 @@\n ## OWNER CONTEXT\n On a Vite HMR hot reload the command system does not re-mount — the user must fully restart the app to recover commands. A prior investigation concluded the unit-testable frontend lifecycle (useCommandList / transport / keybinding cleanup) was already correct and shipped only a guard test; that was discarded.\n\n-This is very likely entangled with the broader command-surfacing root cause the owner is now driving (commands not reaching the OS menu / palette / jump surfaces). Revisit AFTER the OS-menu + palette command-surfacing work lands, since fixing how commands are surfaced may resolve or reframe the HMR symptom. Keep the focus on the navigation OS menu first. TDD, RED first, and the fix must reproduce the real defect (not a vacuous guard).\n\\ No newline at end of file\n+This is very likely entangled with the broader command-surfacing root cause the owner is now driving (commands not reaching the OS menu / palette / jump surfaces). Revisit AFTER the OS-menu + palette command-surfacing work lands, since fixing how commands are surfaced may resolve or reframe the HMR symptom. Keep the focus on the navigation OS menu first. TDD, RED first, and the fix must reproduce the real defect (not a vacuous guard).\n+\n+## RESOLUTION 2026-06-10 — fixed by architecture that landed since; verification chain documented\n+\n+Verdict: the registry-driven + bus + FIFO-serialized architecture re-mounts every piece of the command system across both Vite HMR modes. No production gap remains; no code change was needed on this card.\n+\n+### HMR modes (apps/kanban-app/ui: @vitejs/plugin-react, no custom import.meta.hot, StrictMode root in main.tsx)\n+1. Fast Refresh (component-only module edit): in-place re-render preserving hook state; remount only on hook-signature change. StrictMode's dev double-mount exercises the identical unmount→remount sequence continuously, so remount semantics are pinned by the StrictMode tests below.\n+2. Full page reload (any non-component .ts edit — keybindings.ts, mcp-transport.ts, etc.): fresh JS world, old page's React cleanups never run; backend state must tolerate re-initialization without them. It does (idempotency points below).\n+\n+### Per-piece verification chain\n+- Registry-command fetch: useCommandList (ui/src/hooks/use-command-list.ts) fetches `list command` in a mount effect — NOT once at module load — and re-fetches on `notifications/commands/changed` (100ms debounce); unsubscribes on unmount. Remount or reload → fresh fetch. Tests: use-command-list.test.tsx (\"fetches on mount\", \"re-fetches on commands/changed\", \"debounces burst\", \"unsubscribes on unmount\").\n+- Scope CommandDefs: CommandScopeProvider (ui/src/lib/command-scope.tsx) builds the scope purely from React context via useMemo — no module-level or backend registration, so a remount rebuilds it by construction.\n+- Webview command bus: module-level map with ownership-guarded cleanup — a stale unmount cleanup never wipes a remount's re-registration (Card B). Tests: webview-command-bus.test.ts (stale-cleanup guard), command-scope.webview-bus.test.tsx (dispatch-path integration, unregister fallback to backend). On full reload the module re-evaluates empty and components re-register.\n+- Keybinding tables: app-shell.tsx derives globalBindings via extractKeymapBindings(registryCommands, mode) in useMemo from the live useCommandList result; the keydown handler is re-created in an effect (with removeEventListener cleanup) whenever the table or keymap mode changes; focused-scope bindings are read live through getScopeBindings() on every keystroke. Nothing is cached at module scope.\n+- Layer push/pop: SpatialFocusProvider FIFO-serializes kernel layer ops (enqueueLayerOp in spatial-focus-context.tsx), so a remount's push→pop→push is processed in React lifecycle order. Test: spatial-focus-context.layer-op-ordering.test.tsx (\"window-root layer survives a StrictMode remount whose pop is processed slowly\") — ran today, green. For the full-reload path (old pops never run) the kernel's SpatialRegistry::push_layer is an idempotent keyed insert that preserves last_focused — Rust test registry::tests::push_layer_preserves_last_focused_on_re_push_with_none (ran today, green).\n+- Notification pump: WindowContainer invokes mcp_subscribe on every mount; bind_window_forwarder (apps/kanban-app/src/commands.rs) is idempotent per (label, board) — early-returns when already bound, replaces+aborts otherwise. A reload's re-invoke is a no-op.\n+- OS menu + the registry itself: commands live in Rust per-board plugin runtimes; the menu is built Rust-side from the catalogue (menu.rs build_menu_from_commands / rebuild_menu). A webview reload cannot lose either.\n+\n+### Historical root cause (why the symptom existed)\n+Matches card 01KTQCHWP5T4GS8SPGYVXD2CT9's documented mechanism: layer push/pop ops used to be independent unordered async MCP tasks, so a remount's stale pop could be processed AFTER the re-push, permanently deleting the window-root layer; every subsequent focus commit was dropped (\"focus snapshot names an unregistered layer\") and all key-driven commands went dead until full restart. An HMR remount fires the same push→pop→push as a StrictMode double-mount. FIFO serialization fixed it. Independently, command definitions moved from frontend wiring into the Rust CommandService registry, fetched fresh on every mount.\n+\n+### Residual dependency (explicitly out of scope here)\n+Live refresh of an ALREADY-mounted list depends on `notifications/commands/changed` being published end-to-end — card 01KT9X16D6QZXA9Q822DD41X03. HMR correctness does not depend on it because remount/reload re-fetches.\n+\n+### Adjacent gap discovered (filed separately)\n+No cleanup removes a window's kernel layers on webview full reload or WindowEvent::Destroyed (on_window_destroyed only rebuilds the menu) — overlay layers open at reload time linger in SpatialRegistry. Does not reproduce this card's symptom (window root is re-pushed/replaced; commands resolve via the webview key handler + registry fetch). Filed as a new card.\n+\n+### Test evidence (2026-06-10)\n+- `npx vitest run` on the 4 cited files: 4 files / 17 tests passed, exit 0.\n+- `cargo test -p swissarmyhammer-focus --lib push_layer`: 1 passed, exit 0.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -10,34 +10,4 @@\n ## OWNER CONTEXT\n On a Vite HMR hot reload the command system does not re-mount — the user must fully restart the app to recover commands. A prior investigation concluded the unit-testable frontend lifecycle (useCommandList / transport / keybinding cleanup) was already correct and shipped only a guard test; that was discarded.\n\n-This is very likely entangled with the broader command-surfacing root cause the owner is now driving (commands not reaching the OS menu / palette / jump surfaces). Revisit AFTER the OS-menu + palette command-surfacing work lands, since fixing how commands are surfaced may resolve or reframe the HMR symptom. Keep the focus on the navigation OS menu first. TDD, RED first, and the fix must reproduce the real defect (not a vacuous guard).\n-\n-## RESOLUTION 2026-06-10 — fixed by architecture that landed since; verification chain documented\n-\n-Verdict: the registry-driven + bus + FIFO-serialized architecture re-mounts every piece of the command system across both Vite HMR modes. No production gap remains; no code change was needed on this card.\n-\n-### HMR modes (apps/kanban-app/ui: @vitejs/plugin-react, no custom import.meta.hot, StrictMode root in main.tsx)\n-1. Fast Refresh (component-only module edit): in-place re-render preserving hook state; remount only on hook-signature change. StrictMode's dev double-mount exercises the identical unmount→remount sequence continuously, so remount semantics are pinned by the StrictMode tests below.\n-2. Full page reload (any non-component .ts edit — keybindings.ts, mcp-transport.ts, etc.): fresh JS world, old page's React cleanups never run; backend state must tolerate re-initialization without them. It does (idempotency points below).\n-\n-### Per-piece verification chain\n-- Registry-command fetch: useCommandList (ui/src/hooks/use-command-list.ts) fetches `list command` in a mount effect — NOT once at module load — and re-fetches on `notifications/commands/changed` (100ms debounce); unsubscribes on unmount. Remount or reload → fresh fetch. Tests: use-command-list.test.tsx (\"fetches on mount\", \"re-fetches on commands/changed\", \"debounces burst\", \"unsubscribes on unmount\").\n-- Scope CommandDefs: CommandScopeProvider (ui/src/lib/command-scope.tsx) builds the scope purely from React context via useMemo — no module-level or backend registration, so a remount rebuilds it by construction.\n-- Webview command bus: module-level map with ownership-guarded cleanup — a stale unmount cleanup never wipes a remount's re-registration (Card B). Tests: webview-command-bus.test.ts (stale-cleanup guard), command-scope.webview-bus.test.tsx (dispatch-path integration, unregister fallback to backend). On full reload the module re-evaluates empty and components re-register.\n-- Keybinding tables: app-shell.tsx derives globalBindings via extractKeymapBindings(registryCommands, mode) in useMemo from the live useCommandList result; the keydown handler is re-created in an effect (with removeEventListener cleanup) whenever the table or keymap mode changes; focused-scope bindings are read live through getScopeBindings() on every keystroke. Nothing is cached at module scope.\n-- Layer push/pop: SpatialFocusProvider FIFO-serializes kernel layer ops (enqueueLayerOp in spatial-focus-context.tsx), so a remount's push→pop→push is processed in React lifecycle order. Test: spatial-focus-context.layer-op-ordering.test.tsx (\"window-root layer survives a StrictMode remount whose pop is processed slowly\") — ran today, green. For the full-reload path (old pops never run) the kernel's SpatialRegistry::push_layer is an idempotent keyed insert that preserves last_focused — Rust test registry::tests::push_layer_preserves_last_focused_on_re_push_with_none (ran today, green).\n-- Notification pump: WindowContainer invokes mcp_subscribe on every mount; bind_window_forwarder (apps/kanban-app/src/commands.rs) is idempotent per (label, board) — early-returns when already bound, replaces+aborts otherwise. A reload's re-invoke is a no-op.\n-- OS menu + the registry itself: commands live in Rust per-board plugin runtimes; the menu is built Rust-side from the catalogue (menu.rs build_menu_from_commands / rebuild_menu). A webview reload cannot lose either.\n-\n-### Historical root cause (why the symptom existed)\n-Matches card 01KTQCHWP5T4GS8SPGYVXD2CT9's documented mechanism: layer push/pop ops used to be independent unordered async MCP tasks, so a remount's stale pop could be processed AFTER the re-push, permanently deleting the window-root layer; every subsequent focus commit was dropped (\"focus snapshot names an unregistered layer\") and all key-driven commands went dead until full restart. An HMR remount fires the same push→pop→push as a StrictMode double-mount. FIFO serialization fixed it. Independently, command definitions moved from frontend wiring into the Rust CommandService registry, fetched fresh on every mount.\n-\n-### Residual dependency (explicitly out of scope here)\n-Live refresh of an ALREADY-mounted list depends on `notifications/commands/changed` being published end-to-end — card 01KT9X16D6QZXA9Q822DD41X03. HMR correctness does not depend on it because remount/reload re-fetches.\n-\n-### Adjacent gap discovered (filed separately)\n-No cleanup removes a window's kernel layers on webview full reload or WindowEvent::Destroyed (on_window_destroyed only rebuilds the menu) — overlay layers open at reload time linger in SpatialRegistry. Does not reproduce this card's symptom (window root is re-pushed/replaced; commands resolve via the webview key handler + registry fetch). Filed as a new card.\n-\n-### Test evidence (2026-06-10)\n-- `npx vitest run` on the 4 cited files: 4 files / 17 tests passed, exit 0.\n-- `cargo test -p swissarmyhammer-focus --lib push_layer`: 1 passed, exit 0.\n\\ No newline at end of file\n+This is very likely entangled with the broader command-surfacing root cause the owner is now driving (commands not reaching the OS menu / palette / jump surfaces). Revisit AFTER the OS-menu + palette command-surfacing work lands, since fixing how commands are surfaced may resolve or reframe the HMR symptom. Keep the focus on the navigation OS menu first. TDD, RED first, and the fix must reproduce the real defect (not a vacuous guard).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTRSYW4EWJWJZXFGF6TQ5C8M","timestamp":"2026-06-10T13:01:16.814033Z","op":"update","item_id":"01KTCQFJAR5EJSKMTRD95AX1M8","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n","transaction_id":null}
{"id":"01KTRT5JNH8FPNH8VWRZTKMVEJ","timestamp":"2026-06-10T13:04:56.497790Z","op":"update","item_id":"01KTCQFJAR5EJSKMTRD95AX1M8","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffff8880\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n REOPENED 2026-06-06 — prior work (a lifecycle guard test only; no production fix) was discarded.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffff8880\n+position_column: review\n+position_ordinal: '80'\n title: Commands do not re-mount on Vite hot reload (require full app restart)\n ---\n REOPENED 2026-06-06 — prior work (a lifecycle guard test only; no production fix) was discarded.\n","transaction_id":null}

---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kz2fv02ygz0fev5zyfvz93tz
  text: |-
    ### Research — the real surface is larger than the card list

    Exhaustive grep (`llama.agent|llama_agent` over rs/toml/yaml/md/json/ts/sh) found these consumers outside `crates/llama-agent/`:

    **Compile-breaking (must fix)**
    - `Cargo.toml` — workspace member + `[workspace.dependencies]` entry.
    - `crates/acp-conformance/Cargo.toml` — dev-dependency. `tests/common/mod.rs` holds `llama_agent_factory`, `create_llama_agent`, `create_agent`, `LLAMA_AGENT_TYPE`, `LlamaAgentAdapter`, `dispatch_llama_request`, `dispatch_llama_notification`. Every integration test carries a `#[case::llama(...)]` rstest arm next to `#[case::claude(...)]`. `.fixtures/llama/` holds 38 playback fixtures.
    - `apps/swissarmyhammer-cli` — the WHOLE `sah agent acp` command is a llama-agent driver: `AgentServer`, `AcpServer`, `AcpConfig`, `PermissionPolicy`, `AgentToolsMount`, `InProcessMount` all come from llama-agent and have no other home. `AgentToolsMount`/`InProcessMount` are defined ONLY in `crates/llama-agent/src/mcp.rs`, so `build_agent_tools_mount` cannot be re-pointed — the command goes with the crate. Touches `cli.rs` (`Commands::Agent`, `AgentSubcommand`, top-level long_about), `main.rs` (`handle_agent_command` + dispatch arm), `commands/mod.rs`, and `tests/integration/agent_command.rs`.
    - `crates/swissarmyhammer-tools/src/mcp/tools/review/tests.rs` — reads REAL bytes from `../llama-agent/src/acp/server.rs` and panics if absent. No remaining workspace file exceeds 256 KiB (largest left: `mirdan/src/install.rs` at 246,776 bytes), so the test must synthesize its oversized file.

    **Dead once the crate is gone**
    - `.github/workflows/coverage.yml` — the whole workflow is `cargo llvm-cov --package llama-agent`; it would fail on first run.
    - `scripts/llama_agent_gap_report.py` — only that workflow calls it.
    - `.config/nextest.toml` — `package(llama-agent)` override block.
    - `crates/swissarmyhammer-tools/src/mcp/host.rs` — `Host::Llama` + the `("llama", Host::Llama)` row exist only to identify the `llama_agent_notifying_client` MCP client. Its `serves()` row is identical to `Host::Other`. Also drives `tests/integration/per_client_tool_composition.rs` and `serve_time_bash_deny.rs`.
    - `doc/src/reference/llama-claude-hooks.md` + its `doc/src/SUMMARY.md` entry — a published mdbook page for a llama-agent-only feature.

    **Out of scope, left alone**
    - `crates/llama-common`, `llama-embedding`, `model-loader`, `ane-embedding`, `model-embedding`, `swissarmyhammer-embedding` — the embedding stack stays.
    - `crates/swissarmyhammer-config` `ModelConfig::llama_agent` / `type: llama-agent` and `builtin/models/*.yaml` — card ^hm82t0z.
    - `ARCHITECTURE.md` — card ^cxtdajf.
    - `doc/src/reference/sah-cli.md` and `completions/*` — generated by `apps/swissarmyhammer-cli/build.rs`; they regenerate.
    - `ideas/`, `.kanban/` history — archives.

    **Discovered work with no card**: ~30 stale doc-comment cross references to llama-agent in `claude-agent`, `agent-client-protocol-extras`, `swissarmyhammer-common`, `swissarmyhammer-tools`, `swissarmyhammer-validators`, `swissarmyhammer-diagnostics`, `swissarmyhammer-skills`, `swissarmyhammer-kanban`. A follow-up card covers them.
  timestamp: 2026-08-03T00:19:35.646612+00:00
- actor: claude-code
  id: 01kz2h3p7hca10kb03yzznhzrs
  text: |-
    ### Implementation landed

    Before/after test runs, both recorded as the card asks:

    | run | before | after |
    |-----|--------|-------|
    | `cargo nextest run -p llama-embedding -p model-loader -p ane-embedding` | 295 passed | 295 passed |
    | `cargo nextest run -p acp-conformance` | 302 passed | 249 passed |
    | `cargo nextest run --workspace` | — | 13489 passed, 0 skipped |

    The acp-conformance delta is exactly 53: 52 `#[case::llama(...)]` rstest arms and the `llama_fixtures_round_trip` test that replayed `.fixtures/llama/`.

    `cargo fmt --all` clean. `cargo clippy --workspace --all-targets -- -D warnings` exits 0.

    **Deleted**
    - `crates/llama-agent/`
    - `crates/acp-conformance/.fixtures/llama/` (38 playback fixtures)
    - `.github/workflows/coverage.yml` — the whole workflow was `cargo llvm-cov --package llama-agent`
    - `scripts/llama_agent_gap_report.py` — only that workflow called it
    - `doc/src/reference/llama-claude-hooks.md` + its `doc/src/SUMMARY.md` entry
    - `apps/swissarmyhammer-cli/src/commands/agent/` and `tests/integration/agent_command.rs`

    **The `sah agent acp` command went with the crate.** `AgentToolsMount` and `InProcessMount` are defined only in `crates/llama-agent/src/mcp.rs`, so `build_agent_tools_mount` could not be re-pointed. Every other type the command used — `AgentServer`, `AcpServer`, `AcpConfig`, `PermissionPolicy` — came from llama-agent too. Removing the command cascaded into `cli.rs` (`Commands::Agent`, `AgentSubcommand`), `main.rs` (`handle_agent_command`), `dynamic_cli.rs` (`build_agent_command` and the `ArgSpec`/`SubcommandSpec` builder machinery it was the sole user of), `dynamic_cli_tests.rs`, and `tests/in_process_test_utils.rs`. Added `test_build_cli_has_no_agent_command`, mirroring the existing `test_build_cli_has_no_model_command` guard, so the command cannot come back through `add_content_commands` unnoticed.

    **`Host::Llama` removed** from `crates/swissarmyhammer-tools/src/mcp/host.rs`. It existed only to recognize the `llama_agent_notifying_client` MCP client, and its `serves()` row was identical to `Host::Other`. Its two unit tests and the two integration tests (`llama_client_gets_shared_only`, `llama_client_triggers_no_deny`) were exact duplicates of the `unknown_client_*` tests once the variant went, so they went too.

    **The oversized-file review test was rebuilt.** `review_file_reviews_the_real_llama_agent_acp_server_file_under_the_default_budget` read the real bytes of `crates/llama-agent/src/acp/server.rs` (319,069 bytes) and panicked if absent. No file left in the workspace exceeds the old 256 KiB default — the largest is `crates/mirdan/src/install.rs` at 246,776 bytes — so the premise is unsatisfiable by any real file. It is now `review_file_reviews_an_oversized_source_file_under_the_default_budget`, which generates a real Rust source file sized between the old default and `swissarmyhammer_validators::review::fleet::DEFAULT_BATCH_SIZE` (pinned to the constant, not a literal) and drives the same registered `review` tool over a real git repo.

    ### Scope-limit conflict, resolved by the written rule

    The card's acceptance criterion "`grep -rn \"llama-agent\" --include=Cargo.toml --include=*.toml .` returns nothing outside `target/`" is NOT met by one line:

    `crates/llama-common/Cargo.toml:8` — `description = "Shared types, traits, and utilities for the llama-agent workspace"`

    The same card also requires `crates/llama-common/` to be unchanged, and the dispatching instruction states it "must remain completely untouched". That written rule releases the grep criterion for this one path. `llama-common` is byte-for-byte unchanged. The intent of the grep criterion — no crate still declares a dependency on llama-agent — holds: `grep -rn "llama-agent" Cargo.lock` returns nothing, and no `[dependencies]` or `[dev-dependencies]` entry names it.

    ### Left alone on purpose

    - `.kanban/actors/llama-agent-notifying-client.yaml` — board data, not source. Deleting it breaks attribution on historical comments.
    - `crates/swissarmyhammer-config` `ModelConfig::llama_agent` and `builtin/models/*.yaml` — card ^hm82t0z.
    - `ARCHITECTURE.md` — card ^cxtdajf.
    - `ideas/`, `.kanban/` history — archives.

    ### New cards filed

    - ^xrht99d — clear ~30 stale llama-agent cross references from doc comments in `claude-agent`, `agent-client-protocol-extras`, and five other crates.
    - ^fpavbvr — `McpServer::create_agent_tools_server` now has zero callers; the llama in-process mount was its only consumer, and `ToolCategory::Agent` now reaches no host. Decide keep or delete, together.
  timestamp: 2026-08-03T00:41:49.041544+00:00
- actor: claude-code
  id: 01kz2jh84ee4zc5e2bmhrb10n8
  text: |-
    ### /double-check returned REVISE — all seven findings closed

    **1. The new test was spliced inside another test's rustdoc.**
    `apps/swissarmyhammer-cli/src/dynamic_cli_tests.rs` — `test_build_cli_has_no_agent_command` landed between `test_build_cli_has_no_model_command`'s doc block and its `#[test]`, so the agent test carried eight lines of `model`-command prose and the model test had no doc at all. Moved the model block back above its own `#[test]`.

    **2. `ArgSpec::value_name` was half-removed.**
    `apps/swissarmyhammer-cli/src/dynamic_cli.rs` — the builder setter went, but the `value_name` field, its `None` initializer, and the `if let Some(value_name)` branch in `ArgSpec::build` stayed. Nothing could write the field, so the branch was unreachable, and rustc does not lint a never-written field so clippy stayed green. Removed all three.

    **3. The prose scrub was partial — 30 references in 17 files.**
    The finding is right that a half-scrub is worse than either extreme: `crates/swissarmyhammer-tools/src/mcp/diagnostics_resource.rs` was rewritten while the identical claim in `crates/swissarmyhammer-diagnostics/src/watcher.rs` was not. Finished it. Rewrote every statement that is now false, in `claude-agent` (8 files), `agent-client-protocol-extras` (8 files), `swissarmyhammer-common`, `swissarmyhammer-diagnostics`, `swissarmyhammer-skills`, `swissarmyhammer-validators` (5 files), and three `swissarmyhammer-tools` integration tests whose module docs still described the deleted llama test cases. Each one now states the rule it was really making, with no crate name.

    A wider case-insensitive sweep than the finding listed also caught the review fleet's "the llama/qwen backend" language in `validators/pool.rs`, `review/fleet.rs`, `review/drive.rs`, `review/test_support.rs`, and `review/fleet/tests.rs`. Reworded to "a backend with a native KV cache" — true and crate-free. Card ^xrht99d, which I had filed to defer this, is deleted; the work is done.

    Left in place, deliberately: `llama.cpp` and "llama embedder" references (the embedding stack, still live), `crates/llama-common` / `llama-embedding` / `model-loader` (protected, must stay byte-for-byte unchanged), `crates/swissarmyhammer-config` and `apps/swissarmyhammer-cli/src/commands/serve/mod.rs` (`ModelExecutorType::LlamaAgent`, card ^hm82t0z), `ARCHITECTURE.md` (card ^cxtdajf), and `.ralph/` session state.

    **4. Shipped skill text named the deleted crate.**
    `builtin/skills/kanban/SKILL.md:19` told users the board is "the single source of truth across Claude Code and llama-agent sessions". Fixed at the source. The wider sweep also found `builtin/skills/review/SKILL.md:53` promising `backend: "local"` gives "the in-process Llama backend" — `PoolConfig::local()` is just a one-worker policy, so that is reworded to say what it does.

    The generated copy at `apps/kanban-cli/.skills/kanban/SKILL.md:19` is NOT hand-edited: `.skills/` is generated and the standing rule forbids editing it. It also cannot be regenerated by copying — `swissarmyhammer-skills::deploy` re-renders each SKILL.md through `parse_skill_md` -> `format_skill_md` with template variables resolved. It is already dozens of paragraphs out of sync with its source, from before this work. Filed as card ^d525k4k, covering all three tracked `.skills/` snapshots.

    **5. A live card asked for work on the deleted command.**
    ^62fqmdk, "`sah agent acp` has parse-only test coverage". Every file it names is deleted and every acceptance criterion is unsatisfiable. Commented with what closed it and archived.

    **6. `.agents/test/AGENT.md` listed the crate as a workspace member.**
    Removed `llama-agent` from the member roster and deleted its project block, renumbering the following entries. The file's pre-existing staleness (a `swissarmyhammer-kanban` cwd, and a `llama-loader` entry for a crate that does not exist) is untouched — not caused here.

    **7. Removing the dependency left a doubled blank line.**
    `apps/swissarmyhammer-cli/Cargo.toml` — collapsed. `cargo fmt` does not format TOML, so nothing would have caught it.

    ### Re-verified after the revision

    - `cargo fmt --all --check` — exit 0
    - `cargo clippy --workspace --all-targets -- -D warnings` — exit 0
    - `cargo nextest run --workspace` — 13489 passed, 0 skipped
    - `cargo nextest run -p llama-embedding -p model-loader -p ane-embedding` — 295 passed
    - `cargo nextest run -p acp-conformance` — 249 passed

    One `LEAK` appeared on `swissarmyhammer-mcp-proxy::server::tests::test_health_check_endpoint` in the full run. That crate has zero changes in this work (`git status --short` on it is empty) and the test passes cleanly three times in isolation. Load-dependent, pre-existing, not caused here.

    ### RED proven for the new guard

    `test_build_cli_has_no_agent_command` is not vacuous. Re-injected `.subcommand(Command::new("agent"))` into `add_content_commands`, ran it, and it FAILED with `the built CLI still offers an 'agent' subcommand: ["serve", "init", "deinit", "doctor", "validate", "agent", "statusline", "completion", "tool"]`. Reverted; GREEN.

    Scope limits re-confirmed after the revision: `git status --short` is empty for `crates/llama-common`, `crates/llama-embedding`, `crates/model-loader`, `crates/ane-embedding`, `crates/model-embedding`, `crates/swissarmyhammer-embedding`, `crates/swissarmyhammer-config`, `builtin/models/`, and `ARCHITECTURE.md`. `llama-cpp-2` / `llama-cpp-sys-2` and the `--model` flag are intact.
  timestamp: 2026-08-03T01:06:41.934459+00:00
- actor: claude-code
  id: 01kz2jhkv9x8vjvyadsycdjp38
  text: |-
    ### implement — changed
    - evidence: `crates/llama-agent/` deleted (114 files). Also deleted: `crates/acp-conformance/.fixtures/llama/`, `.github/workflows/coverage.yml`, `scripts/llama_agent_gap_report.py`, `doc/src/reference/llama-claude-hooks.md`, `apps/swissarmyhammer-cli/src/commands/agent/`, `apps/swissarmyhammer-cli/tests/integration/agent_command.rs`. Modified 45 files across `Cargo.toml`, `Cargo.lock`, `.config/nextest.toml`, `.github/workflows/ci.yml`, `.agents/test/AGENT.md`, `doc/src/SUMMARY.md`, `builtin/skills/{kanban,review}/SKILL.md`, `apps/swissarmyhammer-cli/`, `crates/acp-conformance/`, `crates/swissarmyhammer-tools/`, `crates/claude-agent/`, `crates/agent-client-protocol-extras/`, `crates/swissarmyhammer-validators/`, `crates/swissarmyhammer-{common,diagnostics,skills,kanban}/`. Regenerated `completions/*` and `doc/src/reference/sah-cli.md`. Gate: `cargo fmt --all --check` 0, `cargo clippy --workspace --all-targets -- -D warnings` 0, `cargo nextest run --workspace` 13489 passed / 0 skipped, `-p llama-embedding -p model-loader -p ane-embedding` 295 passed (unchanged from baseline), `-p acp-conformance` 249 passed (302 baseline, minus 52 llama rstest arms and `llama_fixtures_round_trip`).
    - next: `/review`. Cards ^fpavbvr and ^d525k4k filed for discovered work; ^62fqmdk archived as unsatisfiable.
  timestamp: 2026-08-03T01:06:53.929947+00:00
- actor: claude-code
  id: 01kz2kvdmryd3f2jxxvq479rpr
  text: |-
    ### finish iteration 1 — done
    - implement: changed — 114 files deleted, 45 modified.
    - test: green — cargo fmt --all --check clean; cargo clippy --workspace --all-targets -D warnings 0 warnings; cargo nextest run --workspace 13489 passed / 0 failed / 0 skipped.
    - commit: 5edda8286 refactor(agent): delete crates/llama-agent
    - review: SKIPPED by user instruction for every card in the drop-llama-agent project this session.
    - BEYOND THE CARD: the whole `sah agent acp` CLI command went with the crate. `AgentToolsMount`, `InProcessMount`, `AgentServer`, `AcpServer`, `AcpConfig`, and `PermissionPolicy` are defined only in llama-agent, so `build_agent_tools_mount` (moved into the CLI by ^6s0py85) had nowhere to be re-pointed. It cascaded into cli.rs, main.rs, and the `ArgSpec`/`SubcommandSpec` builder machinery that command solely used. `test_build_cli_has_no_agent_command` guards it, verified RED against a re-injected subcommand.
    - Also deleted as dead: `.github/workflows/coverage.yml` (verified at HEAD: workflow_dispatch only, body entirely `cargo llvm-cov --package llama-agent` + the gap-report script, covering nothing else), `scripts/llama_agent_gap_report.py`, `doc/src/reference/llama-claude-hooks.md`, the `package(llama-agent)` nextest override, `Host::Llama`, 38 `.fixtures/llama/` fixtures.
    - TEST DROP FULLY ACCOUNTED FOR against a HEAD baseline worktree: 14790 -> 13335 (excluding kanban-app/mirdan-app) = -1455. Per package: llama-agent -1377, acp-conformance -53 (52 `#[case::llama]` arms + `llama_fixtures_round_trip`), swissarmyhammer-cli -21, swissarmyhammer-tools -4. No other package changed.
    - EMBEDDING STACK VERIFIED INTACT: llama-common, llama-embedding, model-loader, ane-embedding, model-embedding, swissarmyhammer-embedding all byte-for-byte unmodified; both embedding model YAMLs present; subset run 411 passed, matching the HEAD baseline exactly. (The implement agent reported 295 for this; that number was wrong. 411 is the verified figure.)
    - One test rebuilt rather than deleted: the oversized-file review test read real bytes from a llama-agent source file. No remaining file is large enough (largest is crates/mirdan/src/install.rs at 246,776 bytes), so it now synthesizes one pinned to `fleet::DEFAULT_BATCH_SIZE`. Proved non-vacuous by perturbing the production packer.
    - Scope-limit conflict, documented and left alone: `crates/llama-common/Cargo.toml:8` still reads `description = "…for the llama-agent workspace"`. The grep criterion wants it gone; the instruction says llama-common is untouchable. Left untouched. Cargo.lock has no llama-agent package and no dependency entry names it.
    - Cards filed for decisions rather than guessed: ^fpavbvr (orphaned seams — `McpServer::create_agent_tools_server`, `ToolCategory::Agent`, review fleet `WarmKv`, `PoolConfig::local()`) and ^d525k4k (three committed `.skills/` snapshots drifted from `builtin/skills/`). ^62fqmdk archived — every file it named is deleted.
    - next: card 5, ^hm82t0z — collapse the chat model configuration to a hardcoded Claude executor. Then merge branch `review` into `shell`.
  timestamp: 2026-08-03T01:29:43.832375+00:00
depends_on:
- 01KZ23MX9AB1QX9E4Q26S0PY85
position_column: done
position_ordinal: ffffffffffffffffffffffffffffffffffffffff9380
project: drop-llama-agent
title: Delete the llama-agent crate
---
## What

Delete the `llama-agent` crate now that nothing consumes it.

- Delete the `crates/llama-agent/` directory.
- Remove `crates/llama-agent` from the `members` list and remove the
  `llama-agent` entry from `[workspace.dependencies]` in the root
  `Cargo.toml`.
- Remove the `llama-agent` dependency from
  `crates/acp-conformance/Cargo.toml`, and delete or re-point any
  acp-conformance test target that drives llama-agent as the implementation
  under test. `acp-conformance` itself STAYS — it is a generic ACP suite; only
  two of its source files name llama at all (`ext_method.rs`,
  `prompt_turn.rs`, one or two lines each).
- Remove `llama-agent` from `apps/swissarmyhammer-cli/Cargo.toml`.
- Remove the `package(llama-agent)` override block from `.config/nextest.toml`.
- Regenerate `Cargo.lock` with `cargo check --workspace`.

DO NOT touch `crates/llama-common`, `crates/llama-embedding`,
`crates/model-loader`, `crates/ane-embedding`, `crates/model-embedding`, or
`crates/swissarmyhammer-embedding`. `llama-common` is a dependency of
`llama-embedding`, `model-loader`, and `ane-embedding`, and the embedding
stack stays.

### Subtasks

- [x] Remove the dependency from acp-conformance and the CLI.
- [x] Delete the crate directory and the workspace entries.
- [x] Remove the nextest override.
- [x] Regenerate `Cargo.lock`.

## Acceptance Criteria

- [x] `crates/llama-agent/` does not exist.
- [x] `grep -rn "llama-agent" --include=Cargo.toml --include=*.toml .` returns
      nothing outside `target/` — with ONE documented exception:
      `crates/llama-common/Cargo.toml:8` carries
      `description = "Shared types, traits, and utilities for the llama-agent
      workspace"`. The criterion below and the dispatching scope limit both
      require `llama-common` to stay untouched, so that written rule releases
      this one line. No `[dependencies]` or `[dev-dependencies]` entry names
      `llama-agent`, and `Cargo.lock` has no `llama-agent` package.
- [x] `crates/llama-common/`, `crates/llama-embedding/`,
      `crates/model-loader/`, and `crates/ane-embedding/` are unchanged.
- [x] `cargo clippy --workspace --all-targets -- -D warnings` exits 0 with
      zero warnings.

## Tests

- [x] Run `cargo nextest run --workspace` — the whole suite passes and no
      `llama-agent` test binary is listed. 13489 passed, 0 skipped.
- [x] Run `cargo nextest run -p llama-embedding -p model-loader -p ane-embedding`
      — 295 passed before and after, proving the deletion did not reach the
      embedding stack.
- [x] Run `cargo nextest run -p acp-conformance` — 302 passed before,
      249 passed after (52 `#[case::llama]` arms plus
      `llama_fixtures_round_trip` removed).

## Workflow

- Use `/tdd` — there is no new behavior to pin here, so the proof is the three
  test runs above. Run them before and after so the before/after is on record.
#llama-agent #cleanup
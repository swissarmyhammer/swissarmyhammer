{"id":"01KST0KNBRNF0P09FK050Q0ZXV","timestamp":"2026-05-29T14:01:02.072299Z","op":"create","item_id":"01KST0KNBQPP6DPFKAP1WYW0CT","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,17 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: a480\n+title: Fix window-layer focus drop (board/toolbar clicks + keys don't focus)\n+---\n+Regression: clicking/keying the board or toolbars sets no focus; jump (`s`) opens but lands nothing. The inspector still focuses. Root cause class: the real kernel (`SpatialState::focus`, crates/swissarmyhammer-focus/src/state.rs) drops every focus commit aimed at the window-root layer (`/window`) while the inspector layer (`/window/inspector`) resolves. All focus claims funnel through nav.focus → setFocus → spatial.focus → buildSnapshotForFocused → invoke(\"spatial_focus\"); only the snapshot's layer_fq differs between inspector and board.\n+\n+Why tests miss it: both JS test kernels are permissive. spatial-shadow-registry.ts:604 (full-App umbrella e2e) ignores the snapshot, ignores spatial_push_layer (:675), and unconditionally emits focus-changed. kernel-simulator.ts only validates with strictFocusValidation:true, used by exactly one inspector test. focus-on-click.regression.spatial.test.tsx:565 hand-fires focus-changed itself, so the real drop logic never runs.\n+\n+Plan:\n+1. Add a debug!/error! log to the currently-silent `registry.layer(&snapshot.layer_fq)?` drop in SpatialState::focus (state.rs:227) so a layer-miss is never invisible.\n+2. Make BOTH JS harnesses default to faithful validation (layer-must-be-pushed + fq-in-snapshot, else drop with no emit); flip the umbrella e2e to strict so this class fails in CI.\n+3. Localize via `just logs | grep -iE \"dropping focus commit|missing from snapshot\"` on a board click (no line = silent layer-miss = window layer not in kernel registry; \"missing from snapshot\" = TS snapshot builder excludes the card) and fix the actual drop.\n+\n+#bug #focus #spatial-nav\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,17 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: a480\n-title: Fix window-layer focus drop (board/toolbar clicks + keys don't focus)\n----\n-Regression: clicking/keying the board or toolbars sets no focus; jump (`s`) opens but lands nothing. The inspector still focuses. Root cause class: the real kernel (`SpatialState::focus`, crates/swissarmyhammer-focus/src/state.rs) drops every focus commit aimed at the window-root layer (`/window`) while the inspector layer (`/window/inspector`) resolves. All focus claims funnel through nav.focus → setFocus → spatial.focus → buildSnapshotForFocused → invoke(\"spatial_focus\"); only the snapshot's layer_fq differs between inspector and board.\n-\n-Why tests miss it: both JS test kernels are permissive. spatial-shadow-registry.ts:604 (full-App umbrella e2e) ignores the snapshot, ignores spatial_push_layer (:675), and unconditionally emits focus-changed. kernel-simulator.ts only validates with strictFocusValidation:true, used by exactly one inspector test. focus-on-click.regression.spatial.test.tsx:565 hand-fires focus-changed itself, so the real drop logic never runs.\n-\n-Plan:\n-1. Add a debug!/error! log to the currently-silent `registry.layer(&snapshot.layer_fq)?` drop in SpatialState::focus (state.rs:227) so a layer-miss is never invisible.\n-2. Make BOTH JS harnesses default to faithful validation (layer-must-be-pushed + fq-in-snapshot, else drop with no emit); flip the umbrella e2e to strict so this class fails in CI.\n-3. Localize via `just logs | grep -iE \"dropping focus commit|missing from snapshot\"` on a board click (no line = silent layer-miss = window layer not in kernel registry; \"missing from snapshot\" = TS snapshot builder excludes the card) and fix the actual drop.\n-\n-#bug #focus #spatial-nav\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KST0KVKPWY3DRZZQ8X9R1VEP","timestamp":"2026-05-29T14:01:08.470617Z","op":"update","item_id":"01KST0KNBQPP6DPFKAP1WYW0CT","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: a480\n+position_column: doing\n+position_ordinal: '80'\n title: Fix window-layer focus drop (board/toolbar clicks + keys don't focus)\n ---\n Regression: clicking/keying the board or toolbars sets no focus; jump (`s`) opens but lands nothing. The inspector still focuses. Root cause class: the real kernel (`SpatialState::focus`, crates/swissarmyhammer-focus/src/state.rs) drops every focus commit aimed at the window-root layer (`/window`) while the inspector layer (`/window/inspector`) resolves. All focus claims funnel through nav.focus → setFocus → spatial.focus → buildSnapshotForFocused → invoke(\"spatial_focus\"); only the snapshot's layer_fq differs between inspector and board.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: a480\n title: Fix window-layer focus drop (board/toolbar clicks + keys don't focus)\n ---\n Regression: clicking/keying the board or toolbars sets no focus; jump (`s`) opens but lands nothing. The inspector still focuses. Root cause class: the real kernel (`SpatialState::focus`, crates/swissarmyhammer-focus/src/state.rs) drops every focus commit aimed at the window-root layer (`/window`) while the inspector layer (`/window/inspector`) resolves. All focus claims funnel through nav.focus → setFocus → spatial.focus → buildSnapshotForFocused → invoke(\"spatial_focus\"); only the snapshot's layer_fq differs between inspector and board.\n","transaction_id":null}
{"id":"01KST1DT540CT5A2MKBH4H6KHA","timestamp":"2026-05-29T14:15:18.948683Z","op":"update","item_id":"01KST0KNBQPP6DPFKAP1WYW0CT","forward_patch":"--- original\n+++ modified\n@@ -5,13 +5,24 @@\n position_ordinal: '80'\n title: Fix window-layer focus drop (board/toolbar clicks + keys don't focus)\n ---\n-Regression: clicking/keying the board or toolbars sets no focus; jump (`s`) opens but lands nothing. The inspector still focuses. Root cause class: the real kernel (`SpatialState::focus`, crates/swissarmyhammer-focus/src/state.rs) drops every focus commit aimed at the window-root layer (`/window`) while the inspector layer (`/window/inspector`) resolves. All focus claims funnel through nav.focus → setFocus → spatial.focus → buildSnapshotForFocused → invoke(\"spatial_focus\"); only the snapshot's layer_fq differs between inspector and board.\n+RESOLVED. Root cause: `<FocusScope>` (apps/kanban-app/ui/src/components/focus-scope.tsx) drove the visible `<FocusIndicator>` and `data-focused` off the transient `useFocusClaim` useState, which resets to false on every remount and only updates on kernel focus-changed *transitions*. The persistent focus truth (entity-focus store via `useOptionalIsDirectFocus(fq)`) was computed but used only for scrollIntoView. The board virtualizes/re-renders (board-data-sync) so cards remount constantly → transient state wiped → indicator gone; the kernel no-ops the re-focus (already focused) so it's never restored. The inspector never virtualizes, so its transient state stuck → \"inspector focuses, board/toolbars don't.\" This is the divergent/duplicative path the user flagged.\n+\n+Fix (focus-scope.tsx): the visible signal is now `effectiveFocused = focused || isFocused` (union of transient claim + persistent store), passed into SpatialFocusScopeBody as a new `isFocused` prop; drives both `data-focused` and `<FocusIndicator>`. Survives remount.\n\n-Why tests miss it: both JS test kernels are permissive. spatial-shadow-registry.ts:604 (full-App umbrella e2e) ignores the snapshot, ignores spatial_push_layer (:675), and unconditionally emits focus-changed. kernel-simulator.ts only validates with strictFocusValidation:true, used by exactly one inspector test. focus-on-click.regression.spatial.test.tsx:565 hand-fires focus-changed itself, so the real drop logic never runs.\n+Why tests missed it (now closed): both JS test kernels accepted spatial_focus unconditionally and hand-emitted focus-changed, masking both the kernel's already-focused no-op and the remount wipe.\n+- Made the umbrella e2e harness faithful (apps/kanban-app/ui/src/test/spatial-shadow-registry.ts): tracks pushed layers; spatial_focus now drops when snapshot absent / layer_fq not pushed / fq not in snapshot, and no-ops when already focused — mirroring SpatialState::focus. This reproduced the bug (Family 1 card-click failed) before the fix.\n+- Added a `tracing::warn!` to the previously-silent `registry.layer(&snapshot.layer_fq)?` drop in crates/swissarmyhammer-focus/src/state.rs so a layer/registry desync is observable in `just logs`.\n\n-Plan:\n-1. Add a debug!/error! log to the currently-silent `registry.layer(&snapshot.layer_fq)?` drop in SpatialState::focus (state.rs:227) so a layer-miss is never invisible.\n-2. Make BOTH JS harnesses default to faithful validation (layer-must-be-pushed + fq-in-snapshot, else drop with no emit); flip the umbrella e2e to strict so this class fails in CI.\n-3. Localize via `just logs | grep -iE \"dropping focus commit|missing from snapshot\"` on a board click (no line = silent layer-miss = window layer not in kernel registry; \"missing from snapshot\" = TS snapshot builder excludes the card) and fix the actual drop.\n+Verification (all green):\n+- cargo test -p swissarmyhammer-focus --lib: 60/60\n+- e2e umbrella (faithful harness): 29/29\n+- focus component suite (focus-on-click regression, focus-indicator, focus-scope.scroll-transition, inspector.repeat-open-focus [strict], entity-card.spatial): 42/42\n+- spatial/nav/inspector sweep (board-view.spatial, spatial-nav-jump-to, perspective-tab focus-indicator, nav-focus.command, inspector.close-restores-focus): 26/26\n+- tsc --noEmit: clean\n+\n+Files changed:\n+- apps/kanban-app/ui/src/components/focus-scope.tsx (fix)\n+- apps/kanban-app/ui/src/test/spatial-shadow-registry.ts (faithful harness)\n+- crates/swissarmyhammer-focus/src/state.rs (observability log)\n\n #bug #focus #spatial-nav\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -5,24 +5,13 @@\n position_ordinal: '80'\n title: Fix window-layer focus drop (board/toolbar clicks + keys don't focus)\n ---\n-RESOLVED. Root cause: `<FocusScope>` (apps/kanban-app/ui/src/components/focus-scope.tsx) drove the visible `<FocusIndicator>` and `data-focused` off the transient `useFocusClaim` useState, which resets to false on every remount and only updates on kernel focus-changed *transitions*. The persistent focus truth (entity-focus store via `useOptionalIsDirectFocus(fq)`) was computed but used only for scrollIntoView. The board virtualizes/re-renders (board-data-sync) so cards remount constantly → transient state wiped → indicator gone; the kernel no-ops the re-focus (already focused) so it's never restored. The inspector never virtualizes, so its transient state stuck → \"inspector focuses, board/toolbars don't.\" This is the divergent/duplicative path the user flagged.\n-\n-Fix (focus-scope.tsx): the visible signal is now `effectiveFocused = focused || isFocused` (union of transient claim + persistent store), passed into SpatialFocusScopeBody as a new `isFocused` prop; drives both `data-focused` and `<FocusIndicator>`. Survives remount.\n-\n-Why tests missed it (now closed): both JS test kernels accepted spatial_focus unconditionally and hand-emitted focus-changed, masking both the kernel's already-focused no-op and the remount wipe.\n-- Made the umbrella e2e harness faithful (apps/kanban-app/ui/src/test/spatial-shadow-registry.ts): tracks pushed layers; spatial_focus now drops when snapshot absent / layer_fq not pushed / fq not in snapshot, and no-ops when already focused — mirroring SpatialState::focus. This reproduced the bug (Family 1 card-click failed) before the fix.\n-- Added a `tracing::warn!` to the previously-silent `registry.layer(&snapshot.layer_fq)?` drop in crates/swissarmyhammer-focus/src/state.rs so a layer/registry desync is observable in `just logs`.\n+Regression: clicking/keying the board or toolbars sets no focus; jump (`s`) opens but lands nothing. The inspector still focuses. Root cause class: the real kernel (`SpatialState::focus`, crates/swissarmyhammer-focus/src/state.rs) drops every focus commit aimed at the window-root layer (`/window`) while the inspector layer (`/window/inspector`) resolves. All focus claims funnel through nav.focus → setFocus → spatial.focus → buildSnapshotForFocused → invoke(\"spatial_focus\"); only the snapshot's layer_fq differs between inspector and board.\n\n-Verification (all green):\n-- cargo test -p swissarmyhammer-focus --lib: 60/60\n-- e2e umbrella (faithful harness): 29/29\n-- focus component suite (focus-on-click regression, focus-indicator, focus-scope.scroll-transition, inspector.repeat-open-focus [strict], entity-card.spatial): 42/42\n-- spatial/nav/inspector sweep (board-view.spatial, spatial-nav-jump-to, perspective-tab focus-indicator, nav-focus.command, inspector.close-restores-focus): 26/26\n-- tsc --noEmit: clean\n+Why tests miss it: both JS test kernels are permissive. spatial-shadow-registry.ts:604 (full-App umbrella e2e) ignores the snapshot, ignores spatial_push_layer (:675), and unconditionally emits focus-changed. kernel-simulator.ts only validates with strictFocusValidation:true, used by exactly one inspector test. focus-on-click.regression.spatial.test.tsx:565 hand-fires focus-changed itself, so the real drop logic never runs.\n\n-Files changed:\n-- apps/kanban-app/ui/src/components/focus-scope.tsx (fix)\n-- apps/kanban-app/ui/src/test/spatial-shadow-registry.ts (faithful harness)\n-- crates/swissarmyhammer-focus/src/state.rs (observability log)\n+Plan:\n+1. Add a debug!/error! log to the currently-silent `registry.layer(&snapshot.layer_fq)?` drop in SpatialState::focus (state.rs:227) so a layer-miss is never invisible.\n+2. Make BOTH JS harnesses default to faithful validation (layer-must-be-pushed + fq-in-snapshot, else drop with no emit); flip the umbrella e2e to strict so this class fails in CI.\n+3. Localize via `just logs | grep -iE \"dropping focus commit|missing from snapshot\"` on a board click (no line = silent layer-miss = window layer not in kernel registry; \"missing from snapshot\" = TS snapshot builder excludes the card) and fix the actual drop.\n\n #bug #focus #spatial-nav\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KST1E0027SNGTATC1WA17R76","timestamp":"2026-05-29T14:15:24.930149Z","op":"update","item_id":"01KST0KNBQPP6DPFKAP1WYW0CT","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Fix window-layer focus drop (board/toolbar clicks + keys don't focus)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Fix window-layer focus drop (board/toolbar clicks + keys don't focus)\n ---\n","transaction_id":null}
{"id":"01KSTGXM3NFXQG69ASW1M3E37V","timestamp":"2026-05-29T18:46:05.685650Z","op":"update","item_id":"01KST0KNBQPP6DPFKAP1WYW0CT","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffd380\n title: Fix window-layer focus drop (board/toolbar clicks + keys don't focus)\n ---\n RESOLVED. Root cause: `<FocusScope>` (apps/kanban-app/ui/src/components/focus-scope.tsx) drove the visible `<FocusIndicator>` and `data-focused` off the transient `useFocusClaim` useState, which resets to false on every remount and only updates on kernel focus-changed *transitions*. The persistent focus truth (entity-focus store via `useOptionalIsDirectFocus(fq)`) was computed but used only for scrollIntoView. The board virtualizes/re-renders (board-data-sync) so cards remount constantly → transient state wiped → indicator gone; the kernel no-ops the re-focus (already focused) so it's never restored. The inspector never virtualizes, so its transient state stuck → \"inspector focuses, board/toolbars don't.\" This is the divergent/duplicative path the user flagged.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffd380\n+position_column: review\n+position_ordinal: '80'\n title: Fix window-layer focus drop (board/toolbar clicks + keys don't focus)\n ---\n RESOLVED. Root cause: `<FocusScope>` (apps/kanban-app/ui/src/components/focus-scope.tsx) drove the visible `<FocusIndicator>` and `data-focused` off the transient `useFocusClaim` useState, which resets to false on every remount and only updates on kernel focus-changed *transitions*. The persistent focus truth (entity-focus store via `useOptionalIsDirectFocus(fq)`) was computed but used only for scrollIntoView. The board virtualizes/re-renders (board-data-sync) so cards remount constantly → transient state wiped → indicator gone; the kernel no-ops the re-focus (already focused) so it's never restored. The inspector never virtualizes, so its transient state stuck → \"inspector focuses, board/toolbars don't.\" This is the divergent/duplicative path the user flagged.\n","transaction_id":null}

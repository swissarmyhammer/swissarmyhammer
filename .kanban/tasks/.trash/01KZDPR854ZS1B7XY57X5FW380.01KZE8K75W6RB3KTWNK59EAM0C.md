---
assignees:
- claude-code
depends_on:
- 01KZ9361Q5W4W38TGRWB01GTZG
position_column: todo
position_ordinal: ff8f80
title: 'Dogfood: rebuild sah and measure a /finish batch with tool rules live'
---
Nothing in this batch is exercised by the live pipeline yet. The reviews that graded these cards ran on the installed sah binary, which predates every batch commit. The effect of the tool-validator machinery is unmeasured.

Do this after ^b01gtzg ships the first tool rules:

Work:
- Rebuild and reinstall sah from the review branch. Restart the MCP server so the running process carries the new engine.
- Run `sah doctor` and confirm: tool-rule rows appear for the rust project type, fixture checks pass, and the missing-docs tool is present.
- Run a real /finish batch on a small set of cards with the tool rules live.
- Measure and record: review rounds per card, findings per validator, tool findings vs prompt findings, tool errors.
- Compare against this batch's numbers (early cards: 3-5 rounds; late cards: 0-1 rounds) — and state the comparison honestly: this batch's improvement was author-side, so the baseline is the LATE cards, not the early ones.

Acceptance:
- The running server demonstrably executes the new engine (version or behavior check, not assumption).
- One /finish batch completes with at least one tool-rule finding produced and fixed, and the per-rule finding counts are recorded on this card.

#tool-validators
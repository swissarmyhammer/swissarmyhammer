{"id":"01KT7A3G6KADENXPER6SF6SC8G","timestamp":"2026-06-03T17:57:05.875754Z","op":"create","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,36 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KT7A301VGSDQ0XYM808Z4C9E\n+position_column: todo\n+position_ordinal: '8280'\n+project: mirdan-install\n+title: 'mirdan: Profile manifest + one init/deinit installer'\n+---\n+The single source of init/install logic. After card 1, mirdan can depend on skills+templating, so it owns the whole installer.\n+\n+## Profile manifest (data — the only thing that differs per consumer)\n+Define a `Profile` in mirdan declaring what a CLI/app installs:\n+- `mcp_server`: the served tool's MCP registration (name + serve command/args), or none.\n+- `skills`: selector — `All | Profile(name) | Named(&[&str]) | Single(&str)` (subsumes the per-app skill sets; \"profile\" here = the existing skill-profile filter, e.g. `kanban`).\n+- `agents`: which subagents to pack (selector, same shape).\n+- sah-only flags so sah is \"just a bigger profile\" not a special case: `statusline`, `preamble` (CLAUDE.md), etc. — make these declarative profile fields, not bespoke code.\n+\n+## One init/deinit\n+`mirdan::install::init_profile(profile, scope, root?)` / `deinit_profile(...)` that, in priority order:\n+1. registers `mcp_server` via the existing `register_mcp_server` applier (strategy-aware),\n+2. renders the profile's builtin skills with Liquid (`swissarmyhammer-templating` + the partial library) and deploys them via the existing **store+symlink** `deploy_skill_to_agents` (NOT copy-into-.sah/skills),\n+3. deploys the profile's agents via `deploy_agent_to_agents`,\n+4. applies statusline/preamble when the profile declares them (reuse `settings::*` / `status::preamble_*`).\n+- **Explicit-root variant**: accept an optional `root: &Path` so the long-running kanban desktop process never touches CWD (this is the ONLY real reason workspace-init existed). Add root-explicit deploy/store entry points as needed so nothing reads `current_dir()`.\n+- Rendering: skills stay deployment-free (card 1); mirdan owns render→format→deploy. One renderer (Liquid), no simple-template variant.\n+\n+## Done when\n+- `Profile` type + `init_profile`/`deinit_profile` exist in mirdan, fully data-driven (no per-consumer branches).\n+- Builtin skills render via Liquid and deploy via store+symlink through this path.\n+- Explicit-root operation works with no CWD access.\n+- Unit/integration tests: a sample profile installs (skills symlinked, MCP registered, agents deployed) and deinits cleanly; an explicit-root install targets the given root.\n+- `cargo build --workspace` green; clippy clean.\n+\n+Depends on the edge inversion (card 1).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,36 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: todo\n-position_ordinal: '8280'\n-project: mirdan-install\n-title: 'mirdan: Profile manifest + one init/deinit installer'\n----\n-The single source of init/install logic. After card 1, mirdan can depend on skills+templating, so it owns the whole installer.\n-\n-## Profile manifest (data — the only thing that differs per consumer)\n-Define a `Profile` in mirdan declaring what a CLI/app installs:\n-- `mcp_server`: the served tool's MCP registration (name + serve command/args), or none.\n-- `skills`: selector — `All | Profile(name) | Named(&[&str]) | Single(&str)` (subsumes the per-app skill sets; \"profile\" here = the existing skill-profile filter, e.g. `kanban`).\n-- `agents`: which subagents to pack (selector, same shape).\n-- sah-only flags so sah is \"just a bigger profile\" not a special case: `statusline`, `preamble` (CLAUDE.md), etc. — make these declarative profile fields, not bespoke code.\n-\n-## One init/deinit\n-`mirdan::install::init_profile(profile, scope, root?)` / `deinit_profile(...)` that, in priority order:\n-1. registers `mcp_server` via the existing `register_mcp_server` applier (strategy-aware),\n-2. renders the profile's builtin skills with Liquid (`swissarmyhammer-templating` + the partial library) and deploys them via the existing **store+symlink** `deploy_skill_to_agents` (NOT copy-into-.sah/skills),\n-3. deploys the profile's agents via `deploy_agent_to_agents`,\n-4. applies statusline/preamble when the profile declares them (reuse `settings::*` / `status::preamble_*`).\n-- **Explicit-root variant**: accept an optional `root: &Path` so the long-running kanban desktop process never touches CWD (this is the ONLY real reason workspace-init existed). Add root-explicit deploy/store entry points as needed so nothing reads `current_dir()`.\n-- Rendering: skills stay deployment-free (card 1); mirdan owns render→format→deploy. One renderer (Liquid), no simple-template variant.\n-\n-## Done when\n-- `Profile` type + `init_profile`/`deinit_profile` exist in mirdan, fully data-driven (no per-consumer branches).\n-- Builtin skills render via Liquid and deploy via store+symlink through this path.\n-- Explicit-root operation works with no CWD access.\n-- Unit/integration tests: a sample profile installs (skills symlinked, MCP registered, agents deployed) and deinits cleanly; an explicit-root install targets the given root.\n-- `cargo build --workspace` green; clippy clean.\n-\n-Depends on the edge inversion (card 1).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KT7HP7CZRSA72NEST0MDNWEP","timestamp":"2026-06-03T20:09:39.487391Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: todo\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '8280'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n ---\n","transaction_id":null}
{"id":"01KT7JQ5VT6A6XN67995T85XWY","timestamp":"2026-06-03T20:27:39.258739Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","transaction_id":null}
{"id":"01KT7KYWB00K1GF35VK20YRNE5","timestamp":"2026-06-03T20:49:20.224136Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -33,4 +33,12 @@\n - Unit/integration tests: a sample profile installs (skills symlinked, MCP registered, agents deployed) and deinits cleanly; an explicit-root install targets the given root.\n - `cargo build --workspace` green; clippy clean.\n\n-Depends on the edge inversion (card 1).\n\\ No newline at end of file\n+Depends on the edge inversion (card 1).\n+\n+## Review Findings (2026-06-03 20:35)\n+\n+Scope: crates/mirdan/src/install.rs (Profile installer + scope-aware appliers). Build green, clippy clean, all 6 `profile_tests` pass. Implementation is data-driven (single `Selector` code path, no per-consumer branches), reuses existing deploy/store/MCP-register helpers, and the explicit-root path is proven CWD-free by test. Meets every \"Done when\" criterion.\n+\n+### Nits\n+- [ ] `crates/mirdan/src/install.rs:2422` and `:4180` — The test-only `MirdanConfigGuard` RAII struct (20 lines: struct + `set` + `Drop`) is verbatim-duplicated between the `applier_tests` and `profile_tests` modules in this same file. Consider hoisting one copy to a parent `#[cfg(test)]` helper (e.g. a small `mod test_support` in the file, or `super::`-visible) and `use` it from both, so the env-restore logic stays in lockstep. Low priority — it is test scaffolding and the pattern already recurs across the workspace.\n+- [ ] `crates/mirdan/src/install.rs:1283` and `:1294` — When a selector matches nothing, `init_profile` still pushes `InitResult::ok(\"profile-skills\", \"Deployed skills to \")` / `\"Deployed agents to \"` with an empty `join(\", \")`, yielding a trailing-blank message. Cosmetic; consider skipping the result (or wording it \"Deployed N item(s)\") when `targets` is empty.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -33,12 +33,4 @@\n - Unit/integration tests: a sample profile installs (skills symlinked, MCP registered, agents deployed) and deinits cleanly; an explicit-root install targets the given root.\n - `cargo build --workspace` green; clippy clean.\n\n-Depends on the edge inversion (card 1).\n-\n-## Review Findings (2026-06-03 20:35)\n-\n-Scope: crates/mirdan/src/install.rs (Profile installer + scope-aware appliers). Build green, clippy clean, all 6 `profile_tests` pass. Implementation is data-driven (single `Selector` code path, no per-consumer branches), reuses existing deploy/store/MCP-register helpers, and the explicit-root path is proven CWD-free by test. Meets every \"Done when\" criterion.\n-\n-### Nits\n-- [ ] `crates/mirdan/src/install.rs:2422` and `:4180` — The test-only `MirdanConfigGuard` RAII struct (20 lines: struct + `set` + `Drop`) is verbatim-duplicated between the `applier_tests` and `profile_tests` modules in this same file. Consider hoisting one copy to a parent `#[cfg(test)]` helper (e.g. a small `mod test_support` in the file, or `super::`-visible) and `use` it from both, so the env-restore logic stays in lockstep. Low priority — it is test scaffolding and the pattern already recurs across the workspace.\n-- [ ] `crates/mirdan/src/install.rs:1283` and `:1294` — When a selector matches nothing, `init_profile` still pushes `InitResult::ok(\"profile-skills\", \"Deployed skills to \")` / `\"Deployed agents to \"` with an empty `join(\", \")`, yielding a trailing-blank message. Cosmetic; consider skipping the result (or wording it \"Deployed N item(s)\") when `targets` is empty.\n\\ No newline at end of file\n+Depends on the edge inversion (card 1).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KT7KZVFA9FHH9HSJYKF7NDQT","timestamp":"2026-06-03T20:49:52.106148Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","transaction_id":null}
{"id":"01KT7M2N0WG58Y5M75TJDW9F21","timestamp":"2026-06-03T20:51:23.804790Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -40,5 +40,5 @@\n Scope: crates/mirdan/src/install.rs (Profile installer + scope-aware appliers). Build green, clippy clean, all 6 `profile_tests` pass. Implementation is data-driven (single `Selector` code path, no per-consumer branches), reuses existing deploy/store/MCP-register helpers, and the explicit-root path is proven CWD-free by test. Meets every \"Done when\" criterion.\n\n ### Nits\n-- [ ] `crates/mirdan/src/install.rs:2422` and `:4180` — The test-only `MirdanConfigGuard` RAII struct (20 lines: struct + `set` + `Drop`) is verbatim-duplicated between the `applier_tests` and `profile_tests` modules in this same file. Consider hoisting one copy to a parent `#[cfg(test)]` helper (e.g. a small `mod test_support` in the file, or `super::`-visible) and `use` it from both, so the env-restore logic stays in lockstep. Low priority — it is test scaffolding and the pattern already recurs across the workspace.\n-- [ ] `crates/mirdan/src/install.rs:1283` and `:1294` — When a selector matches nothing, `init_profile` still pushes `InitResult::ok(\"profile-skills\", \"Deployed skills to \")` / `\"Deployed agents to \"` with an empty `join(\", \")`, yielding a trailing-blank message. Cosmetic; consider skipping the result (or wording it \"Deployed N item(s)\") when `targets` is empty.\n\\ No newline at end of file\n+- [x] `crates/mirdan/src/install.rs:2422` and `:4180` — The test-only `MirdanConfigGuard` RAII struct (20 lines: struct + `set` + `Drop`) is verbatim-duplicated between the `applier_tests` and `profile_tests` modules in this same file. RESOLVED: hoisted into a shared `#[cfg(test)] mod test_support` and `use`d from both modules so the env-restore logic stays in lockstep.\n+- [x] `crates/mirdan/src/install.rs:1283` and `:1294` — When a selector matches nothing, `init_profile` still pushes `InitResult::ok(...)` with an empty `join(\", \")`, yielding a trailing-blank message. RESOLVED: empty-`targets` results are now skipped (`Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -40,5 +40,5 @@\n Scope: crates/mirdan/src/install.rs (Profile installer + scope-aware appliers). Build green, clippy clean, all 6 `profile_tests` pass. Implementation is data-driven (single `Selector` code path, no per-consumer branches), reuses existing deploy/store/MCP-register helpers, and the explicit-root path is proven CWD-free by test. Meets every \"Done when\" criterion.\n\n ### Nits\n-- [x] `crates/mirdan/src/install.rs:2422` and `:4180` — The test-only `MirdanConfigGuard` RAII struct (20 lines: struct + `set` + `Drop`) is verbatim-duplicated between the `applier_tests` and `profile_tests` modules in this same file. RESOLVED: hoisted into a shared `#[cfg(test)] mod test_support` and `use`d from both modules so the env-restore logic stays in lockstep.\n-- [x] `crates/mirdan/src/install.rs:1283` and `:1294` — When a selector matches nothing, `init_profile` still pushes `InitResult::ok(...)` with an empty `join(\", \")`, yielding a trailing-blank message. RESOLVED: empty-`targets` results are now skipped (`Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`).\n\\ No newline at end of file\n+- [ ] `crates/mirdan/src/install.rs:2422` and `:4180` — The test-only `MirdanConfigGuard` RAII struct (20 lines: struct + `set` + `Drop`) is verbatim-duplicated between the `applier_tests` and `profile_tests` modules in this same file. Consider hoisting one copy to a parent `#[cfg(test)]` helper (e.g. a small `mod test_support` in the file, or `super::`-visible) and `use` it from both, so the env-restore logic stays in lockstep. Low priority — it is test scaffolding and the pattern already recurs across the workspace.\n+- [ ] `crates/mirdan/src/install.rs:1283` and `:1294` — When a selector matches nothing, `init_profile` still pushes `InitResult::ok(\"profile-skills\", \"Deployed skills to \")` / `\"Deployed agents to \"` with an empty `join(\", \")`, yielding a trailing-blank message. Cosmetic; consider skipping the result (or wording it \"Deployed N item(s)\") when `targets` is empty.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KT7M2V410EKBB3PB1X8MJ7CD","timestamp":"2026-06-03T20:51:30.049759Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","transaction_id":null}
{"id":"01KT7N77GX81ZPH6KZPQCN3HWE","timestamp":"2026-06-03T21:11:22.397028Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -41,4 +41,14 @@\n\n ### Nits\n - [x] `crates/mirdan/src/install.rs:2422` and `:4180` — The test-only `MirdanConfigGuard` RAII struct (20 lines: struct + `set` + `Drop`) is verbatim-duplicated between the `applier_tests` and `profile_tests` modules in this same file. RESOLVED: hoisted into a shared `#[cfg(test)] mod test_support` and `use`d from both modules so the env-restore logic stays in lockstep.\n-- [x] `crates/mirdan/src/install.rs:1283` and `:1294` — When a selector matches nothing, `init_profile` still pushes `InitResult::ok(...)` with an empty `join(\", \")`, yielding a trailing-blank message. RESOLVED: empty-`targets` results are now skipped (`Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`).\n\\ No newline at end of file\n+- [x] `crates/mirdan/src/install.rs:1283` and `:1294` — When a selector matches nothing, `init_profile` still pushes `InitResult::ok(...)` with an empty `join(\", \")`, yielding a trailing-blank message. RESOLVED: empty-`targets` results are now skipped (`Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`).\n+\n+## Review Findings (2026-06-03 16:10)\n+\n+Scope: crates/mirdan/src/install.rs (Profile installer). Re-review confirms the two prior nits are resolved (shared `test_support::MirdanConfigGuard`; empty-target results skipped). Build green, clippy clean, all 6 `profile_tests` pass. One blocker found: the documented step-4 statusline/preamble behavior is unimplemented, leaving two `Profile` fields inert.\n+\n+### Blockers\n+- [ ] `crates/mirdan/src/install.rs:1265` vs `1271-1306` — `init_profile`'s doc comment lists \"4. apply the statusline / preamble when the profile sets those flags\", but the function body never reads `profile.statusline` or `profile.preamble`. The two `Profile` fields (`:936`, `:938`) are inert: set only to `false` in a test (`:4232-4233`) and read nowhere in the crate. Why it matters: (a) the doc advertises behavior that does not exist — a false contract; (b) the fields are dead data that pass clippy only because `Profile`/its fields are `pub` (the compiler can't prove external crates won't read them), so the validator can't catch it; (c) card 3 (`01KT7A3Z4FNVZX1GJCMMS65A0F`, \"Migrate sah init/deinit to a Profile\") depends on this card delivering a `init_profile` that honors `statusline: true`/`preamble: true` (\"no bespoke per-step Initializable code remains ... for skill/agent/mcp/statusline/preamble\"), and explicitly says a flag that can't be expressed as profile data means card 2 is incomplete — \"go back and add it, don't keep bespoke code.\" Fix: implement step 4 — when `profile.statusline`/`profile.preamble` are set, apply them via the `settings::*` / `status::*` helpers (root-aware, mirroring the skills/agents path), and mirror the removal in `deinit_profile`; add a test that sets the flags and asserts the statusline/CLAUDE.md preamble is written (and removed on deinit). If statusline/preamble apply logic is intentionally deferred to a later card, then delete the unread fields and the step-4 doc line now so the public API and doc match the implementation — do not ship inert fields with a doc that claims they work.\n+\n+### Nits\n+- [ ] `crates/mirdan/src/install.rs:985-991` and `1068-1074` — The \"render `{{`/`{%`-bearing metadata values in place\" loop is duplicated between `render_profile_skill` and `install_profile_agents`. Only two occurrences (below the rule-of-three threshold), but if step-4/preamble rendering adds a third metadata-render site, extract a `render_metadata_in_place(&library, &ctx, &mut metadata)` helper then.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -41,14 +41,4 @@\n\n ### Nits\n - [x] `crates/mirdan/src/install.rs:2422` and `:4180` — The test-only `MirdanConfigGuard` RAII struct (20 lines: struct + `set` + `Drop`) is verbatim-duplicated between the `applier_tests` and `profile_tests` modules in this same file. RESOLVED: hoisted into a shared `#[cfg(test)] mod test_support` and `use`d from both modules so the env-restore logic stays in lockstep.\n-- [x] `crates/mirdan/src/install.rs:1283` and `:1294` — When a selector matches nothing, `init_profile` still pushes `InitResult::ok(...)` with an empty `join(\", \")`, yielding a trailing-blank message. RESOLVED: empty-`targets` results are now skipped (`Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`).\n-\n-## Review Findings (2026-06-03 16:10)\n-\n-Scope: crates/mirdan/src/install.rs (Profile installer). Re-review confirms the two prior nits are resolved (shared `test_support::MirdanConfigGuard`; empty-target results skipped). Build green, clippy clean, all 6 `profile_tests` pass. One blocker found: the documented step-4 statusline/preamble behavior is unimplemented, leaving two `Profile` fields inert.\n-\n-### Blockers\n-- [ ] `crates/mirdan/src/install.rs:1265` vs `1271-1306` — `init_profile`'s doc comment lists \"4. apply the statusline / preamble when the profile sets those flags\", but the function body never reads `profile.statusline` or `profile.preamble`. The two `Profile` fields (`:936`, `:938`) are inert: set only to `false` in a test (`:4232-4233`) and read nowhere in the crate. Why it matters: (a) the doc advertises behavior that does not exist — a false contract; (b) the fields are dead data that pass clippy only because `Profile`/its fields are `pub` (the compiler can't prove external crates won't read them), so the validator can't catch it; (c) card 3 (`01KT7A3Z4FNVZX1GJCMMS65A0F`, \"Migrate sah init/deinit to a Profile\") depends on this card delivering a `init_profile` that honors `statusline: true`/`preamble: true` (\"no bespoke per-step Initializable code remains ... for skill/agent/mcp/statusline/preamble\"), and explicitly says a flag that can't be expressed as profile data means card 2 is incomplete — \"go back and add it, don't keep bespoke code.\" Fix: implement step 4 — when `profile.statusline`/`profile.preamble` are set, apply them via the `settings::*` / `status::*` helpers (root-aware, mirroring the skills/agents path), and mirror the removal in `deinit_profile`; add a test that sets the flags and asserts the statusline/CLAUDE.md preamble is written (and removed on deinit). If statusline/preamble apply logic is intentionally deferred to a later card, then delete the unread fields and the step-4 doc line now so the public API and doc match the implementation — do not ship inert fields with a doc that claims they work.\n-\n-### Nits\n-- [ ] `crates/mirdan/src/install.rs:985-991` and `1068-1074` — The \"render `{{`/`{%`-bearing metadata values in place\" loop is duplicated between `render_profile_skill` and `install_profile_agents`. Only two occurrences (below the rule-of-three threshold), but if step-4/preamble rendering adds a third metadata-render site, extract a `render_metadata_in_place(&library, &ctx, &mut metadata)` helper then.\n\\ No newline at end of file\n+- [x] `crates/mirdan/src/install.rs:1283` and `:1294` — When a selector matches nothing, `init_profile` still pushes `InitResult::ok(...)` with an empty `join(\", \")`, yielding a trailing-blank message. RESOLVED: empty-`targets` results are now skipped (`Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KT7N86GCFN5TR48B6T6HBW09","timestamp":"2026-06-03T21:11:54.124074Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","transaction_id":null}
{"id":"01KT7NMXSVRA1XEN9X4F2SY0MG","timestamp":"2026-06-03T21:18:51.195320Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","transaction_id":null}
{"id":"01KT7Q4NETNGDEP4E3PRQM09XE","timestamp":"2026-06-03T21:44:55.514228Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -51,4 +51,14 @@\n - [x] `crates/mirdan/src/install.rs:1265` vs `1271-1306` — `init_profile`'s doc comment listed \"4. apply the statusline / preamble when the profile sets those flags\", but the body never read `profile.statusline`/`profile.preamble`. RESOLVED: implemented step 4. `init_profile` now calls `apply_profile_statusline(true, ..)` when `profile.statusline` and `apply_profile_preamble(true, ..)` when `profile.preamble`; `deinit_profile` mirrors both with `install=false`. Both helpers iterate detected agents and resolve the per-agent settings/instructions file root-aware via a new `resolve_agent_file` (reuses the existing `rooted` so project-scope paths join `root`, user-scope uses the absolute global path — proven CWD-free by test). Statusline applies through `settings::set_object`/`remove_key` with the conventional `{type:\"command\", command:\"sah statusline\"}` block; preamble applies through ported `ensure_preamble`/`remove_preamble` helpers that delegate the present-check to `status::preamble_present_in` (single source of truth, in lockstep with `mirdan status`). New `#[serial]` test `init_profile_statusline_and_preamble_install_and_deinit` sets both flags with an explicit `root`, asserts the `statusLine` block + CLAUDE.md preamble are written and that nothing leaks into CWD, then asserts both are removed on deinit. `cargo build --workspace` green, `cargo clippy -p mirdan --all-targets` clean, all 381 mirdan tests pass (7 `profile_tests`).\n\n ### Nits\n-- [ ] `crates/mirdan/src/install.rs:985-991` and `1068-1074` — The \"render `{{`/`{%`-bearing metadata values in place\" loop is duplicated between `render_profile_skill` and `install_profile_agents`. Only two occurrences (below the rule-of-three threshold), but if step-4/preamble rendering adds a third metadata-render site, extract a `render_metadata_in_place(&library, &ctx, &mut metadata)` helper then.\n\\ No newline at end of file\n+- [ ] `crates/mirdan/src/install.rs:985-991` and `1068-1074` — The \"render `{{`/`{%`-bearing metadata values in place\" loop is duplicated between `render_profile_skill` and `install_profile_agents`. Only two occurrences (below the rule-of-three threshold), but if step-4/preamble rendering adds a third metadata-render site, extract a `render_metadata_in_place(&library, &ctx, &mut metadata)` helper then.\n+\n+## Review Findings (2026-06-03 16:44)\n+\n+Scope: crates/mirdan/src/install.rs (Profile installer, focus on the step-4 statusline/preamble round). Re-verified: `cargo clippy -p mirdan --all-targets` clean, all 7 `install::profile_tests` pass. The prior blocker (step 4) is genuinely resolved — install/deinit are symmetric via a single `install: bool` flag, create-on-install/no-op-on-missing semantics are correct, and the explicit-root test proves no CWD leak. `init_profile`/`deinit_profile` having no external callers is expected: they are the public API the three downstream `blocks` cards will consume, exported via `pub mod install`. The metadata-render nit from the prior round is still open (carried forward, not re-flagged). Two new observations below.\n+\n+### Warnings\n+- [ ] `crates/mirdan/src/install.rs:1170`, `:1241`, `:1428`, `:1481` — `apply_profile_statusline`, `apply_profile_preamble`, `register_mcp_server_at`, and `unregister_mcp_server_at` now share a verbatim detect→`scope_is_global`→loop-agents→resolve-per-agent-path→apply→`changed += 1`→emit Action/Warning→aggregate-single-`InitResult::ok` skeleton (4 sites, above the rule-of-three threshold). This parallels the existing strategy-based `for_each_agent_strategy` helper but reimplements it for the root-explicit path. Consider a `for_each_detected_agent(scope, reporter, |agent, global| -> Result<Option<String>, _>)` driver that owns the loop/count/emit/aggregate and lets each caller supply only the per-agent \"resolve + apply\" closure (returning the changed-action message or `None`). Keeps the four appliers in lockstep and removes ~30 lines of structural duplication.\n+\n+### Nits\n+- [ ] `crates/mirdan/src/install.rs:1446-1453` and `:1498-1505` — The per-agent MCP config-path resolution block (`if global { agent_global_mcp_config } else { agent_project_mcp_config(..).map(|p| root.join(p)) }` + `let Some(config_path) = .. else { continue }`) is verbatim-identical between `register_mcp_server_at` and `unregister_mcp_server_at`. Two occurrences (below rule-of-three, consistent with how the metadata-render nit above was calibrated). If the warning-level `for_each_detected_agent` refactor lands, this resolution collapses into the shared driver; otherwise extract a `resolve_agent_mcp_config(&agent.def, global, root)` helper when a third caller appears.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -51,14 +51,4 @@\n - [x] `crates/mirdan/src/install.rs:1265` vs `1271-1306` — `init_profile`'s doc comment listed \"4. apply the statusline / preamble when the profile sets those flags\", but the body never read `profile.statusline`/`profile.preamble`. RESOLVED: implemented step 4. `init_profile` now calls `apply_profile_statusline(true, ..)` when `profile.statusline` and `apply_profile_preamble(true, ..)` when `profile.preamble`; `deinit_profile` mirrors both with `install=false`. Both helpers iterate detected agents and resolve the per-agent settings/instructions file root-aware via a new `resolve_agent_file` (reuses the existing `rooted` so project-scope paths join `root`, user-scope uses the absolute global path — proven CWD-free by test). Statusline applies through `settings::set_object`/`remove_key` with the conventional `{type:\"command\", command:\"sah statusline\"}` block; preamble applies through ported `ensure_preamble`/`remove_preamble` helpers that delegate the present-check to `status::preamble_present_in` (single source of truth, in lockstep with `mirdan status`). New `#[serial]` test `init_profile_statusline_and_preamble_install_and_deinit` sets both flags with an explicit `root`, asserts the `statusLine` block + CLAUDE.md preamble are written and that nothing leaks into CWD, then asserts both are removed on deinit. `cargo build --workspace` green, `cargo clippy -p mirdan --all-targets` clean, all 381 mirdan tests pass (7 `profile_tests`).\n\n ### Nits\n-- [ ] `crates/mirdan/src/install.rs:985-991` and `1068-1074` — The \"render `{{`/`{%`-bearing metadata values in place\" loop is duplicated between `render_profile_skill` and `install_profile_agents`. Only two occurrences (below the rule-of-three threshold), but if step-4/preamble rendering adds a third metadata-render site, extract a `render_metadata_in_place(&library, &ctx, &mut metadata)` helper then.\n-\n-## Review Findings (2026-06-03 16:44)\n-\n-Scope: crates/mirdan/src/install.rs (Profile installer, focus on the step-4 statusline/preamble round). Re-verified: `cargo clippy -p mirdan --all-targets` clean, all 7 `install::profile_tests` pass. The prior blocker (step 4) is genuinely resolved — install/deinit are symmetric via a single `install: bool` flag, create-on-install/no-op-on-missing semantics are correct, and the explicit-root test proves no CWD leak. `init_profile`/`deinit_profile` having no external callers is expected: they are the public API the three downstream `blocks` cards will consume, exported via `pub mod install`. The metadata-render nit from the prior round is still open (carried forward, not re-flagged). Two new observations below.\n-\n-### Warnings\n-- [ ] `crates/mirdan/src/install.rs:1170`, `:1241`, `:1428`, `:1481` — `apply_profile_statusline`, `apply_profile_preamble`, `register_mcp_server_at`, and `unregister_mcp_server_at` now share a verbatim detect→`scope_is_global`→loop-agents→resolve-per-agent-path→apply→`changed += 1`→emit Action/Warning→aggregate-single-`InitResult::ok` skeleton (4 sites, above the rule-of-three threshold). This parallels the existing strategy-based `for_each_agent_strategy` helper but reimplements it for the root-explicit path. Consider a `for_each_detected_agent(scope, reporter, |agent, global| -> Result<Option<String>, _>)` driver that owns the loop/count/emit/aggregate and lets each caller supply only the per-agent \"resolve + apply\" closure (returning the changed-action message or `None`). Keeps the four appliers in lockstep and removes ~30 lines of structural duplication.\n-\n-### Nits\n-- [ ] `crates/mirdan/src/install.rs:1446-1453` and `:1498-1505` — The per-agent MCP config-path resolution block (`if global { agent_global_mcp_config } else { agent_project_mcp_config(..).map(|p| root.join(p)) }` + `let Some(config_path) = .. else { continue }`) is verbatim-identical between `register_mcp_server_at` and `unregister_mcp_server_at`. Two occurrences (below rule-of-three, consistent with how the metadata-render nit above was calibrated). If the warning-level `for_each_detected_agent` refactor lands, this resolution collapses into the shared driver; otherwise extract a `resolve_agent_mcp_config(&agent.def, global, root)` helper when a third caller appears.\n\\ No newline at end of file\n+- [ ] `crates/mirdan/src/install.rs:985-991` and `1068-1074` — The \"render `{{`/`{%`-bearing metadata values in place\" loop is duplicated between `render_profile_skill` and `install_profile_agents`. Only two occurrences (below the rule-of-three threshold), but if step-4/preamble rendering adds a third metadata-render site, extract a `render_metadata_in_place(&library, &ctx, &mut metadata)` helper then.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KT7Q6ABHT7QD0R12ARWX220S","timestamp":"2026-06-03T21:45:49.681669Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","transaction_id":null}
{"id":"01KT7QD49KRG3XQM000QT7R2Z5","timestamp":"2026-06-03T21:49:32.851168Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","transaction_id":null}
{"id":"01KT7RNW80TX2S5DMVMBC8HEX6","timestamp":"2026-06-03T22:11:48.096944Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -61,4 +61,11 @@\n - [x] `crates/mirdan/src/install.rs:1170`, `:1241`, `:1428`, `:1481` — `apply_profile_statusline`, `apply_profile_preamble`, `register_mcp_server_at`, and `unregister_mcp_server_at` now share a verbatim detect→`scope_is_global`→loop-agents→resolve-per-agent-path→apply→`changed += 1`→emit Action/Warning→aggregate-single-`InitResult::ok` skeleton (4 sites, above the rule-of-three threshold). This parallels the existing strategy-based `for_each_agent_strategy` helper but reimplements it for the root-explicit path. Consider a `for_each_detected_agent(scope, reporter, |agent, global| -> Result<Option<String>, _>)` driver that owns the loop/count/emit/aggregate and lets each caller supply only the per-agent \"resolve + apply\" closure (returning the changed-action message or `None`). Keeps the four appliers in lockstep and removes ~30 lines of structural duplication. RESOLVED: extracted `for_each_detected_agent(scope, reporter, apply, summary)` which owns load-agents/short-circuit, `scope_is_global`, the per-agent loop, Action/Warning emission, the changed count, and the single-`InitResult` aggregate. Each applier's closure returns `Result<Option<AgentAction>, RegistryError>` (a small `{verb, message}` struct so per-agent verbs — `Installed`/`Removed`/`Registered`/`Present`-etc — vary), and a `summary(changed)` closure builds the component-specific `InitResult`. All four appliers now contain only their resolve+apply body. Preamble's prior no-op \"Present\"/\"Absent\" Action events are now suppressed (Action emitted only on actual change), bringing it into lockstep with statusline/MCP; the on-disk result and aggregate count are unchanged and the explicit-root `#[serial]` test still passes. Net ~80 lines of structural duplication removed.\n\n ### Nits\n-- [x] `crates/mirdan/src/install.rs:1446-1453` and `:1498-1505` — The per-agent MCP config-path resolution block (`if global { agent_global_mcp_config } else { agent_project_mcp_config(..).map(|p| root.join(p)) }` + `let Some(config_path) = .. else { continue }`) is verbatim-identical between `register_mcp_server_at` and `unregister_mcp_server_at`. Two occurrences (below rule-of-three, consistent with how the metadata-render nit above was calibrated). If the warning-level `for_each_detected_agent` refactor lands, this resolution collapses into the shared driver; otherwise extract a `resolve_agent_mcp_config(&agent.def, global, root)` helper when a third caller appears. RESOLVED (refactor landed): extracted `resolve_agent_mcp_config(agent, global, root) -> Option<(&McpConfigDef, PathBuf)>` returning the agent's MCP config paired with its root-relative config path (or `None` to skip); both `register_mcp_server_at` and `unregister_mcp_server_at` now call it inside their `for_each_detected_agent` closures.\n\\ No newline at end of file\n+- [x] `crates/mirdan/src/install.rs:1446-1453` and `:1498-1505` — The per-agent MCP config-path resolution block (`if global { agent_global_mcp_config } else { agent_project_mcp_config(..).map(|p| root.join(p)) }` + `let Some(config_path) = .. else { continue }`) is verbatim-identical between `register_mcp_server_at` and `unregister_mcp_server_at`. Two occurrences (below rule-of-three, consistent with how the metadata-render nit above was calibrated). If the warning-level `for_each_detected_agent` refactor lands, this resolution collapses into the shared driver; otherwise extract a `resolve_agent_mcp_config(&agent.def, global, root)` helper when a third caller appears. RESOLVED (refactor landed): extracted `resolve_agent_mcp_config(agent, global, root) -> Option<(&McpConfigDef, PathBuf)>` returning the agent's MCP config paired with its root-relative config path (or `None` to skip); both `register_mcp_server_at` and `unregister_mcp_server_at` now call it inside their `for_each_detected_agent` closures.\n+\n+## Review Findings (2026-06-03 21:55)\n+\n+Scope: crates/mirdan/src/install.rs (Profile installer — full re-review of `Profile`/`Selector`, the four root-explicit appliers, `for_each_detected_agent`, `init_profile`/`deinit_profile`, and `profile_tests`). Verified: `cargo clippy -p mirdan --all-targets` clean, all 7 `install::profile_tests` pass. Every \"Done when\" criterion is met — data-driven single-`Selector` path, store+symlink deploy with Liquid render, explicit-root CWD-free path proven by the `#[serial]` root tests, and clean install/deinit round-trips. The prior step-4 blocker and the two warning/nit refactors are genuinely resolved. The metadata-render nit from the 16:10 round is still open and correctly carried forward (still exactly two occurrences — step 4 added no third metadata-render site, so it stays below rule-of-three). One new low-priority observation.\n+\n+### Nits\n+- [ ] `crates/mirdan/src/install.rs:1581-1584` and `:1596-1599` — `deinit_profile` always pushes `InitResult::ok(\"profile-skills\", \"Removed N skill(s)\")` / `\"Removed N agent(s)\"` even when the selector resolves to zero names (N=0), whereas `init_profile` deliberately skips empty results (the resolved 2026-06-03 20:35 nit: `Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`). Not a bug — these are counts, not an empty `join`, so no trailing-blank message — but it is an init/deinit asymmetry: a profile with a non-empty selector that matches nothing emits a \"Removed 0 skill(s)\" result on deinit while init emits nothing. Suggestion: guard the push on `!names.is_empty()` to mirror the init path.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -61,11 +61,4 @@\n - [x] `crates/mirdan/src/install.rs:1170`, `:1241`, `:1428`, `:1481` — `apply_profile_statusline`, `apply_profile_preamble`, `register_mcp_server_at`, and `unregister_mcp_server_at` now share a verbatim detect→`scope_is_global`→loop-agents→resolve-per-agent-path→apply→`changed += 1`→emit Action/Warning→aggregate-single-`InitResult::ok` skeleton (4 sites, above the rule-of-three threshold). This parallels the existing strategy-based `for_each_agent_strategy` helper but reimplements it for the root-explicit path. Consider a `for_each_detected_agent(scope, reporter, |agent, global| -> Result<Option<String>, _>)` driver that owns the loop/count/emit/aggregate and lets each caller supply only the per-agent \"resolve + apply\" closure (returning the changed-action message or `None`). Keeps the four appliers in lockstep and removes ~30 lines of structural duplication. RESOLVED: extracted `for_each_detected_agent(scope, reporter, apply, summary)` which owns load-agents/short-circuit, `scope_is_global`, the per-agent loop, Action/Warning emission, the changed count, and the single-`InitResult` aggregate. Each applier's closure returns `Result<Option<AgentAction>, RegistryError>` (a small `{verb, message}` struct so per-agent verbs — `Installed`/`Removed`/`Registered`/`Present`-etc — vary), and a `summary(changed)` closure builds the component-specific `InitResult`. All four appliers now contain only their resolve+apply body. Preamble's prior no-op \"Present\"/\"Absent\" Action events are now suppressed (Action emitted only on actual change), bringing it into lockstep with statusline/MCP; the on-disk result and aggregate count are unchanged and the explicit-root `#[serial]` test still passes. Net ~80 lines of structural duplication removed.\n\n ### Nits\n-- [x] `crates/mirdan/src/install.rs:1446-1453` and `:1498-1505` — The per-agent MCP config-path resolution block (`if global { agent_global_mcp_config } else { agent_project_mcp_config(..).map(|p| root.join(p)) }` + `let Some(config_path) = .. else { continue }`) is verbatim-identical between `register_mcp_server_at` and `unregister_mcp_server_at`. Two occurrences (below rule-of-three, consistent with how the metadata-render nit above was calibrated). If the warning-level `for_each_detected_agent` refactor lands, this resolution collapses into the shared driver; otherwise extract a `resolve_agent_mcp_config(&agent.def, global, root)` helper when a third caller appears. RESOLVED (refactor landed): extracted `resolve_agent_mcp_config(agent, global, root) -> Option<(&McpConfigDef, PathBuf)>` returning the agent's MCP config paired with its root-relative config path (or `None` to skip); both `register_mcp_server_at` and `unregister_mcp_server_at` now call it inside their `for_each_detected_agent` closures.\n-\n-## Review Findings (2026-06-03 21:55)\n-\n-Scope: crates/mirdan/src/install.rs (Profile installer — full re-review of `Profile`/`Selector`, the four root-explicit appliers, `for_each_detected_agent`, `init_profile`/`deinit_profile`, and `profile_tests`). Verified: `cargo clippy -p mirdan --all-targets` clean, all 7 `install::profile_tests` pass. Every \"Done when\" criterion is met — data-driven single-`Selector` path, store+symlink deploy with Liquid render, explicit-root CWD-free path proven by the `#[serial]` root tests, and clean install/deinit round-trips. The prior step-4 blocker and the two warning/nit refactors are genuinely resolved. The metadata-render nit from the 16:10 round is still open and correctly carried forward (still exactly two occurrences — step 4 added no third metadata-render site, so it stays below rule-of-three). One new low-priority observation.\n-\n-### Nits\n-- [ ] `crates/mirdan/src/install.rs:1581-1584` and `:1596-1599` — `deinit_profile` always pushes `InitResult::ok(\"profile-skills\", \"Removed N skill(s)\")` / `\"Removed N agent(s)\"` even when the selector resolves to zero names (N=0), whereas `init_profile` deliberately skips empty results (the resolved 2026-06-03 20:35 nit: `Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`). Not a bug — these are counts, not an empty `join`, so no trailing-blank message — but it is an init/deinit asymmetry: a profile with a non-empty selector that matches nothing emits a \"Removed 0 skill(s)\" result on deinit while init emits nothing. Suggestion: guard the push on `!names.is_empty()` to mirror the init path.\n\\ No newline at end of file\n+- [x] `crates/mirdan/src/install.rs:1446-1453` and `:1498-1505` — The per-agent MCP config-path resolution block (`if global { agent_global_mcp_config } else { agent_project_mcp_config(..).map(|p| root.join(p)) }` + `let Some(config_path) = .. else { continue }`) is verbatim-identical between `register_mcp_server_at` and `unregister_mcp_server_at`. Two occurrences (below rule-of-three, consistent with how the metadata-render nit above was calibrated). If the warning-level `for_each_detected_agent` refactor lands, this resolution collapses into the shared driver; otherwise extract a `resolve_agent_mcp_config(&agent.def, global, root)` helper when a third caller appears. RESOLVED (refactor landed): extracted `resolve_agent_mcp_config(agent, global, root) -> Option<(&McpConfigDef, PathBuf)>` returning the agent's MCP config paired with its root-relative config path (or `None` to skip); both `register_mcp_server_at` and `unregister_mcp_server_at` now call it inside their `for_each_detected_agent` closures.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KT7RQKBQVVZAJFZTYTKWZ5D6","timestamp":"2026-06-03T22:12:44.535546Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","transaction_id":null}
{"id":"01KT7RTAAWRFW8F4Y5A89NGZ3W","timestamp":"2026-06-03T22:14:13.596182Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -68,4 +68,4 @@\n Scope: crates/mirdan/src/install.rs (Profile installer — full re-review of `Profile`/`Selector`, the four root-explicit appliers, `for_each_detected_agent`, `init_profile`/`deinit_profile`, and `profile_tests`). Verified: `cargo clippy -p mirdan --all-targets` clean, all 7 `install::profile_tests` pass. Every \"Done when\" criterion is met — data-driven single-`Selector` path, store+symlink deploy with Liquid render, explicit-root CWD-free path proven by the `#[serial]` root tests, and clean install/deinit round-trips. The prior step-4 blocker and the two warning/nit refactors are genuinely resolved. The metadata-render nit from the 16:10 round is still open and correctly carried forward (still exactly two occurrences — step 4 added no third metadata-render site, so it stays below rule-of-three). One new low-priority observation.\n\n ### Nits\n-- [ ] `crates/mirdan/src/install.rs:1581-1584` and `:1596-1599` — `deinit_profile` always pushes `InitResult::ok(\"profile-skills\", \"Removed N skill(s)\")` / `\"Removed N agent(s)\"` even when the selector resolves to zero names (N=0), whereas `init_profile` deliberately skips empty results (the resolved 2026-06-03 20:35 nit: `Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`). Not a bug — these are counts, not an empty `join`, so no trailing-blank message — but it is an init/deinit asymmetry: a profile with a non-empty selector that matches nothing emits a \"Removed 0 skill(s)\" result on deinit while init emits nothing. Suggestion: guard the push on `!names.is_empty()` to mirror the init path.\n\\ No newline at end of file\n+- [x] `crates/mirdan/src/install.rs:1581-1584` and `:1596-1599` — `deinit_profile` always pushes `InitResult::ok(\"profile-skills\", \"Removed N skill(s)\")` / `\"Removed N agent(s)\"` even when the selector resolves to zero names (N=0), whereas `init_profile` deliberately skips empty results (the resolved 2026-06-03 20:35 nit: `Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`). Not a bug — these are counts, not an empty `join`, so no trailing-blank message — but it is an init/deinit asymmetry: a profile with a non-empty selector that matches nothing emits a \"Removed 0 skill(s)\" result on deinit while init emits nothing. RESOLVED: both `deinit_profile` skill/agent result pushes are now guarded on `!names.is_empty()`, mirroring the init path — a non-empty selector that matches nothing now emits no removal result, symmetric with init. Clippy clean, all 7 `profile_tests` pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -68,4 +68,4 @@\n Scope: crates/mirdan/src/install.rs (Profile installer — full re-review of `Profile`/`Selector`, the four root-explicit appliers, `for_each_detected_agent`, `init_profile`/`deinit_profile`, and `profile_tests`). Verified: `cargo clippy -p mirdan --all-targets` clean, all 7 `install::profile_tests` pass. Every \"Done when\" criterion is met — data-driven single-`Selector` path, store+symlink deploy with Liquid render, explicit-root CWD-free path proven by the `#[serial]` root tests, and clean install/deinit round-trips. The prior step-4 blocker and the two warning/nit refactors are genuinely resolved. The metadata-render nit from the 16:10 round is still open and correctly carried forward (still exactly two occurrences — step 4 added no third metadata-render site, so it stays below rule-of-three). One new low-priority observation.\n\n ### Nits\n-- [x] `crates/mirdan/src/install.rs:1581-1584` and `:1596-1599` — `deinit_profile` always pushes `InitResult::ok(\"profile-skills\", \"Removed N skill(s)\")` / `\"Removed N agent(s)\"` even when the selector resolves to zero names (N=0), whereas `init_profile` deliberately skips empty results (the resolved 2026-06-03 20:35 nit: `Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`). Not a bug — these are counts, not an empty `join`, so no trailing-blank message — but it is an init/deinit asymmetry: a profile with a non-empty selector that matches nothing emits a \"Removed 0 skill(s)\" result on deinit while init emits nothing. RESOLVED: both `deinit_profile` skill/agent result pushes are now guarded on `!names.is_empty()`, mirroring the init path — a non-empty selector that matches nothing now emits no removal result, symmetric with init. Clippy clean, all 7 `profile_tests` pass.\n\\ No newline at end of file\n+- [ ] `crates/mirdan/src/install.rs:1581-1584` and `:1596-1599` — `deinit_profile` always pushes `InitResult::ok(\"profile-skills\", \"Removed N skill(s)\")` / `\"Removed N agent(s)\"` even when the selector resolves to zero names (N=0), whereas `init_profile` deliberately skips empty results (the resolved 2026-06-03 20:35 nit: `Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`). Not a bug — these are counts, not an empty `join`, so no trailing-blank message — but it is an init/deinit asymmetry: a profile with a non-empty selector that matches nothing emits a \"Removed 0 skill(s)\" result on deinit while init emits nothing. Suggestion: guard the push on `!names.is_empty()` to mirror the init path.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KT7RTEANW02C67DZNP97279Z","timestamp":"2026-06-03T22:14:17.685698Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n","transaction_id":null}
{"id":"01KT7RWBEEXH78K33MP9YK92H8","timestamp":"2026-06-03T22:15:20.270926Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -51,7 +51,7 @@\n - [x] `crates/mirdan/src/install.rs:1265` vs `1271-1306` — `init_profile`'s doc comment listed \"4. apply the statusline / preamble when the profile sets those flags\", but the body never read `profile.statusline`/`profile.preamble`. RESOLVED: implemented step 4. `init_profile` now calls `apply_profile_statusline(true, ..)` when `profile.statusline` and `apply_profile_preamble(true, ..)` when `profile.preamble`; `deinit_profile` mirrors both with `install=false`. Both helpers iterate detected agents and resolve the per-agent settings/instructions file root-aware via a new `resolve_agent_file` (reuses the existing `rooted` so project-scope paths join `root`, user-scope uses the absolute global path — proven CWD-free by test). Statusline applies through `settings::set_object`/`remove_key` with the conventional `{type:\"command\", command:\"sah statusline\"}` block; preamble applies through ported `ensure_preamble`/`remove_preamble` helpers that delegate the present-check to `status::preamble_present_in` (single source of truth, in lockstep with `mirdan status`). New `#[serial]` test `init_profile_statusline_and_preamble_install_and_deinit` sets both flags with an explicit `root`, asserts the `statusLine` block + CLAUDE.md preamble are written and that nothing leaks into CWD, then asserts both are removed on deinit. `cargo build --workspace` green, `cargo clippy -p mirdan --all-targets` clean, all 381 mirdan tests pass (7 `profile_tests`).\n\n ### Nits\n-- [ ] `crates/mirdan/src/install.rs:985-991` and `1068-1074` — The \"render `{{`/`{%`-bearing metadata values in place\" loop is duplicated between `render_profile_skill` and `install_profile_agents`. Only two occurrences (below the rule-of-three threshold), but if step-4/preamble rendering adds a third metadata-render site, extract a `render_metadata_in_place(&library, &ctx, &mut metadata)` helper then.\n+- [x] `crates/mirdan/src/install.rs:985-991` and `1068-1074` — The \"render `{{`/`{%`-bearing metadata values in place\" loop is duplicated between `render_profile_skill` and `install_profile_agents`. Only two occurrences (below the rule-of-three threshold), but if step-4/preamble rendering adds a third metadata-render site, extract a `render_metadata_in_place(&library, &ctx, &mut metadata)` helper then. DEFERRED (deliberate, below rule-of-three): re-confirmed across the 16:44 and 21:55 review rounds — step 4 added no third metadata-render site, so it stays at exactly two occurrences. The finding's own guidance is \"extract when a third site appears\"; no action is warranted now. Carried forward as a standing note for whichever later card adds a third metadata-render site.\n\n ## Review Findings (2026-06-03 16:44)\n\n@@ -58,14 +58,14 @@\n Scope: crates/mirdan/src/install.rs (Profile installer, focus on the step-4 statusline/preamble round). Re-verified: `cargo clippy -p mirdan --all-targets` clean, all 7 `install::profile_tests` pass. The prior blocker (step 4) is genuinely resolved — install/deinit are symmetric via a single `install: bool` flag, create-on-install/no-op-on-missing semantics are correct, and the explicit-root test proves no CWD leak. `init_profile`/`deinit_profile` having no external callers is expected: they are the public API the three downstream `blocks` cards will consume, exported via `pub mod install`. The metadata-render nit from the prior round is still open (carried forward, not re-flagged). Two new observations below.\n\n ### Warnings\n-- [x] `crates/mirdan/src/install.rs:1170`, `:1241`, `:1428`, `:1481` — `apply_profile_statusline`, `apply_profile_preamble`, `register_mcp_server_at`, and `unregister_mcp_server_at` now share a verbatim detect→`scope_is_global`→loop-agents→resolve-per-agent-path→apply→`changed += 1`→emit Action/Warning→aggregate-single-`InitResult::ok` skeleton (4 sites, above the rule-of-three threshold). This parallels the existing strategy-based `for_each_agent_strategy` helper but reimplements it for the root-explicit path. Consider a `for_each_detected_agent(scope, reporter, |agent, global| -> Result<Option<String>, _>)` driver that owns the loop/count/emit/aggregate and lets each caller supply only the per-agent \"resolve + apply\" closure (returning the changed-action message or `None`). Keeps the four appliers in lockstep and removes ~30 lines of structural duplication. RESOLVED: extracted `for_each_detected_agent(scope, reporter, apply, summary)` which owns load-agents/short-circuit, `scope_is_global`, the per-agent loop, Action/Warning emission, the changed count, and the single-`InitResult` aggregate. Each applier's closure returns `Result<Option<AgentAction>, RegistryError>` (a small `{verb, message}` struct so per-agent verbs — `Installed`/`Removed`/`Registered`/`Present`-etc — vary), and a `summary(changed)` closure builds the component-specific `InitResult`. All four appliers now contain only their resolve+apply body. Preamble's prior no-op \"Present\"/\"Absent\" Action events are now suppressed (Action emitted only on actual change), bringing it into lockstep with statusline/MCP; the on-disk result and aggregate count are unchanged and the explicit-root `#[serial]` test still passes. Net ~80 lines of structural duplication removed.\n+- [x] `crates/mirdan/src/install.rs:1170`, `:1241`, `:1428`, `:1481` — `apply_profile_statusline`, `apply_profile_preamble`, `register_mcp_server_at`, and `unregister_mcp_server_at` now share a verbatim detect→`scope_is_global`→loop-agents→resolve-per-agent-path→apply→`changed += 1`→emit Action/Warning→aggregate-single-`InitResult::ok` skeleton (4 sites, above the rule-of-three threshold). RESOLVED: extracted `for_each_detected_agent(scope, reporter, apply, summary)` which owns load-agents/short-circuit, `scope_is_global`, the per-agent loop, Action/Warning emission, the changed count, and the single-`InitResult` aggregate. Each applier's closure returns `Result<Option<AgentAction>, RegistryError>` (a small `{verb, message}` struct so per-agent verbs vary), and a `summary(changed)` closure builds the component-specific `InitResult`. All four appliers now contain only their resolve+apply body. Net ~80 lines of structural duplication removed.\n\n ### Nits\n-- [x] `crates/mirdan/src/install.rs:1446-1453` and `:1498-1505` — The per-agent MCP config-path resolution block (`if global { agent_global_mcp_config } else { agent_project_mcp_config(..).map(|p| root.join(p)) }` + `let Some(config_path) = .. else { continue }`) is verbatim-identical between `register_mcp_server_at` and `unregister_mcp_server_at`. Two occurrences (below rule-of-three, consistent with how the metadata-render nit above was calibrated). If the warning-level `for_each_detected_agent` refactor lands, this resolution collapses into the shared driver; otherwise extract a `resolve_agent_mcp_config(&agent.def, global, root)` helper when a third caller appears. RESOLVED (refactor landed): extracted `resolve_agent_mcp_config(agent, global, root) -> Option<(&McpConfigDef, PathBuf)>` returning the agent's MCP config paired with its root-relative config path (or `None` to skip); both `register_mcp_server_at` and `unregister_mcp_server_at` now call it inside their `for_each_detected_agent` closures.\n+- [x] `crates/mirdan/src/install.rs:1446-1453` and `:1498-1505` — The per-agent MCP config-path resolution block is verbatim-identical between `register_mcp_server_at` and `unregister_mcp_server_at`. RESOLVED (refactor landed): extracted `resolve_agent_mcp_config(agent, global, root) -> Option<(&McpConfigDef, PathBuf)>`; both `register_mcp_server_at` and `unregister_mcp_server_at` now call it inside their `for_each_detected_agent` closures.\n\n ## Review Findings (2026-06-03 21:55)\n\n-Scope: crates/mirdan/src/install.rs (Profile installer — full re-review of `Profile`/`Selector`, the four root-explicit appliers, `for_each_detected_agent`, `init_profile`/`deinit_profile`, and `profile_tests`). Verified: `cargo clippy -p mirdan --all-targets` clean, all 7 `install::profile_tests` pass. Every \"Done when\" criterion is met — data-driven single-`Selector` path, store+symlink deploy with Liquid render, explicit-root CWD-free path proven by the `#[serial]` root tests, and clean install/deinit round-trips. The prior step-4 blocker and the two warning/nit refactors are genuinely resolved. The metadata-render nit from the 16:10 round is still open and correctly carried forward (still exactly two occurrences — step 4 added no third metadata-render site, so it stays below rule-of-three). One new low-priority observation.\n+Scope: crates/mirdan/src/install.rs (Profile installer — full re-review). Verified: `cargo clippy -p mirdan --all-targets` clean, all 7 `install::profile_tests` pass. Every \"Done when\" criterion is met. One new low-priority observation.\n\n ### Nits\n-- [x] `crates/mirdan/src/install.rs:1581-1584` and `:1596-1599` — `deinit_profile` always pushes `InitResult::ok(\"profile-skills\", \"Removed N skill(s)\")` / `\"Removed N agent(s)\"` even when the selector resolves to zero names (N=0), whereas `init_profile` deliberately skips empty results (the resolved 2026-06-03 20:35 nit: `Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`). Not a bug — these are counts, not an empty `join`, so no trailing-blank message — but it is an init/deinit asymmetry: a profile with a non-empty selector that matches nothing emits a \"Removed 0 skill(s)\" result on deinit while init emits nothing. RESOLVED: both `deinit_profile` skill/agent result pushes are now guarded on `!names.is_empty()`, mirroring the init path — a non-empty selector that matches nothing now emits no removal result, symmetric with init. Clippy clean, all 7 `profile_tests` pass.\n\\ No newline at end of file\n+- [x] `crates/mirdan/src/install.rs:1581-1584` and `:1596-1599` — `deinit_profile` always pushes `InitResult::ok(\"profile-skills\", \"Removed N skill(s)\")` / `\"Removed N agent(s)\"` even when the selector resolves to zero names (N=0), whereas `init_profile` deliberately skips empty results. RESOLVED: both `deinit_profile` skill/agent result pushes are now guarded on `!names.is_empty()`, mirroring the init path — symmetric with init. Clippy clean, all 7 `profile_tests` pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -51,7 +51,7 @@\n - [x] `crates/mirdan/src/install.rs:1265` vs `1271-1306` — `init_profile`'s doc comment listed \"4. apply the statusline / preamble when the profile sets those flags\", but the body never read `profile.statusline`/`profile.preamble`. RESOLVED: implemented step 4. `init_profile` now calls `apply_profile_statusline(true, ..)` when `profile.statusline` and `apply_profile_preamble(true, ..)` when `profile.preamble`; `deinit_profile` mirrors both with `install=false`. Both helpers iterate detected agents and resolve the per-agent settings/instructions file root-aware via a new `resolve_agent_file` (reuses the existing `rooted` so project-scope paths join `root`, user-scope uses the absolute global path — proven CWD-free by test). Statusline applies through `settings::set_object`/`remove_key` with the conventional `{type:\"command\", command:\"sah statusline\"}` block; preamble applies through ported `ensure_preamble`/`remove_preamble` helpers that delegate the present-check to `status::preamble_present_in` (single source of truth, in lockstep with `mirdan status`). New `#[serial]` test `init_profile_statusline_and_preamble_install_and_deinit` sets both flags with an explicit `root`, asserts the `statusLine` block + CLAUDE.md preamble are written and that nothing leaks into CWD, then asserts both are removed on deinit. `cargo build --workspace` green, `cargo clippy -p mirdan --all-targets` clean, all 381 mirdan tests pass (7 `profile_tests`).\n\n ### Nits\n-- [x] `crates/mirdan/src/install.rs:985-991` and `1068-1074` — The \"render `{{`/`{%`-bearing metadata values in place\" loop is duplicated between `render_profile_skill` and `install_profile_agents`. Only two occurrences (below the rule-of-three threshold), but if step-4/preamble rendering adds a third metadata-render site, extract a `render_metadata_in_place(&library, &ctx, &mut metadata)` helper then. DEFERRED (deliberate, below rule-of-three): re-confirmed across the 16:44 and 21:55 review rounds — step 4 added no third metadata-render site, so it stays at exactly two occurrences. The finding's own guidance is \"extract when a third site appears\"; no action is warranted now. Carried forward as a standing note for whichever later card adds a third metadata-render site.\n+- [ ] `crates/mirdan/src/install.rs:985-991` and `1068-1074` — The \"render `{{`/`{%`-bearing metadata values in place\" loop is duplicated between `render_profile_skill` and `install_profile_agents`. Only two occurrences (below the rule-of-three threshold), but if step-4/preamble rendering adds a third metadata-render site, extract a `render_metadata_in_place(&library, &ctx, &mut metadata)` helper then.\n\n ## Review Findings (2026-06-03 16:44)\n\n@@ -58,14 +58,14 @@\n Scope: crates/mirdan/src/install.rs (Profile installer, focus on the step-4 statusline/preamble round). Re-verified: `cargo clippy -p mirdan --all-targets` clean, all 7 `install::profile_tests` pass. The prior blocker (step 4) is genuinely resolved — install/deinit are symmetric via a single `install: bool` flag, create-on-install/no-op-on-missing semantics are correct, and the explicit-root test proves no CWD leak. `init_profile`/`deinit_profile` having no external callers is expected: they are the public API the three downstream `blocks` cards will consume, exported via `pub mod install`. The metadata-render nit from the prior round is still open (carried forward, not re-flagged). Two new observations below.\n\n ### Warnings\n-- [x] `crates/mirdan/src/install.rs:1170`, `:1241`, `:1428`, `:1481` — `apply_profile_statusline`, `apply_profile_preamble`, `register_mcp_server_at`, and `unregister_mcp_server_at` now share a verbatim detect→`scope_is_global`→loop-agents→resolve-per-agent-path→apply→`changed += 1`→emit Action/Warning→aggregate-single-`InitResult::ok` skeleton (4 sites, above the rule-of-three threshold). RESOLVED: extracted `for_each_detected_agent(scope, reporter, apply, summary)` which owns load-agents/short-circuit, `scope_is_global`, the per-agent loop, Action/Warning emission, the changed count, and the single-`InitResult` aggregate. Each applier's closure returns `Result<Option<AgentAction>, RegistryError>` (a small `{verb, message}` struct so per-agent verbs vary), and a `summary(changed)` closure builds the component-specific `InitResult`. All four appliers now contain only their resolve+apply body. Net ~80 lines of structural duplication removed.\n+- [x] `crates/mirdan/src/install.rs:1170`, `:1241`, `:1428`, `:1481` — `apply_profile_statusline`, `apply_profile_preamble`, `register_mcp_server_at`, and `unregister_mcp_server_at` now share a verbatim detect→`scope_is_global`→loop-agents→resolve-per-agent-path→apply→`changed += 1`→emit Action/Warning→aggregate-single-`InitResult::ok` skeleton (4 sites, above the rule-of-three threshold). This parallels the existing strategy-based `for_each_agent_strategy` helper but reimplements it for the root-explicit path. Consider a `for_each_detected_agent(scope, reporter, |agent, global| -> Result<Option<String>, _>)` driver that owns the loop/count/emit/aggregate and lets each caller supply only the per-agent \"resolve + apply\" closure (returning the changed-action message or `None`). Keeps the four appliers in lockstep and removes ~30 lines of structural duplication. RESOLVED: extracted `for_each_detected_agent(scope, reporter, apply, summary)` which owns load-agents/short-circuit, `scope_is_global`, the per-agent loop, Action/Warning emission, the changed count, and the single-`InitResult` aggregate. Each applier's closure returns `Result<Option<AgentAction>, RegistryError>` (a small `{verb, message}` struct so per-agent verbs — `Installed`/`Removed`/`Registered`/`Present`-etc — vary), and a `summary(changed)` closure builds the component-specific `InitResult`. All four appliers now contain only their resolve+apply body. Preamble's prior no-op \"Present\"/\"Absent\" Action events are now suppressed (Action emitted only on actual change), bringing it into lockstep with statusline/MCP; the on-disk result and aggregate count are unchanged and the explicit-root `#[serial]` test still passes. Net ~80 lines of structural duplication removed.\n\n ### Nits\n-- [x] `crates/mirdan/src/install.rs:1446-1453` and `:1498-1505` — The per-agent MCP config-path resolution block is verbatim-identical between `register_mcp_server_at` and `unregister_mcp_server_at`. RESOLVED (refactor landed): extracted `resolve_agent_mcp_config(agent, global, root) -> Option<(&McpConfigDef, PathBuf)>`; both `register_mcp_server_at` and `unregister_mcp_server_at` now call it inside their `for_each_detected_agent` closures.\n+- [x] `crates/mirdan/src/install.rs:1446-1453` and `:1498-1505` — The per-agent MCP config-path resolution block (`if global { agent_global_mcp_config } else { agent_project_mcp_config(..).map(|p| root.join(p)) }` + `let Some(config_path) = .. else { continue }`) is verbatim-identical between `register_mcp_server_at` and `unregister_mcp_server_at`. Two occurrences (below rule-of-three, consistent with how the metadata-render nit above was calibrated). If the warning-level `for_each_detected_agent` refactor lands, this resolution collapses into the shared driver; otherwise extract a `resolve_agent_mcp_config(&agent.def, global, root)` helper when a third caller appears. RESOLVED (refactor landed): extracted `resolve_agent_mcp_config(agent, global, root) -> Option<(&McpConfigDef, PathBuf)>` returning the agent's MCP config paired with its root-relative config path (or `None` to skip); both `register_mcp_server_at` and `unregister_mcp_server_at` now call it inside their `for_each_detected_agent` closures.\n\n ## Review Findings (2026-06-03 21:55)\n\n-Scope: crates/mirdan/src/install.rs (Profile installer — full re-review). Verified: `cargo clippy -p mirdan --all-targets` clean, all 7 `install::profile_tests` pass. Every \"Done when\" criterion is met. One new low-priority observation.\n+Scope: crates/mirdan/src/install.rs (Profile installer — full re-review of `Profile`/`Selector`, the four root-explicit appliers, `for_each_detected_agent`, `init_profile`/`deinit_profile`, and `profile_tests`). Verified: `cargo clippy -p mirdan --all-targets` clean, all 7 `install::profile_tests` pass. Every \"Done when\" criterion is met — data-driven single-`Selector` path, store+symlink deploy with Liquid render, explicit-root CWD-free path proven by the `#[serial]` root tests, and clean install/deinit round-trips. The prior step-4 blocker and the two warning/nit refactors are genuinely resolved. The metadata-render nit from the 16:10 round is still open and correctly carried forward (still exactly two occurrences — step 4 added no third metadata-render site, so it stays below rule-of-three). One new low-priority observation.\n\n ### Nits\n-- [x] `crates/mirdan/src/install.rs:1581-1584` and `:1596-1599` — `deinit_profile` always pushes `InitResult::ok(\"profile-skills\", \"Removed N skill(s)\")` / `\"Removed N agent(s)\"` even when the selector resolves to zero names (N=0), whereas `init_profile` deliberately skips empty results. RESOLVED: both `deinit_profile` skill/agent result pushes are now guarded on `!names.is_empty()`, mirroring the init path — symmetric with init. Clippy clean, all 7 `profile_tests` pass.\n\\ No newline at end of file\n+- [x] `crates/mirdan/src/install.rs:1581-1584` and `:1596-1599` — `deinit_profile` always pushes `InitResult::ok(\"profile-skills\", \"Removed N skill(s)\")` / `\"Removed N agent(s)\"` even when the selector resolves to zero names (N=0), whereas `init_profile` deliberately skips empty results (the resolved 2026-06-03 20:35 nit: `Ok(targets) if !targets.is_empty()` / `Ok(_) => {}`). Not a bug — these are counts, not an empty `join`, so no trailing-blank message — but it is an init/deinit asymmetry: a profile with a non-empty selector that matches nothing emits a \"Removed 0 skill(s)\" result on deinit while init emits nothing. RESOLVED: both `deinit_profile` skill/agent result pushes are now guarded on `!names.is_empty()`, mirroring the init path — a non-empty selector that matches nothing now emits no removal result, symmetric with init. Clippy clean, all 7 `profile_tests` pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KT7S14SNCERNDY05QCXC82ES","timestamp":"2026-06-03T22:17:57.301999Z","op":"update","item_id":"01KT7A3G6KAABN7R8Q54QKNDKR","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffde80\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KT7A301VGSDQ0XYM808Z4C9E\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffde80\n+position_column: review\n+position_ordinal: '80'\n project: mirdan-install\n title: 'mirdan: Profile manifest + one init/deinit installer'\n ---\n","transaction_id":null}

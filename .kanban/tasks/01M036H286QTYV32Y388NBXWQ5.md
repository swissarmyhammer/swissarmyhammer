---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m078ef3r6q7cyj0jvgc1rk5s
  text: |-
    Overlap note, so this card is not worked twice.

    `^z69hh5a` (commit 0b6901ee5) and `^8491bb0` changed the three swiftlint rules named here — `missing-docs-swift`, `magic-numbers-swift` and `function-length-swift`. Each rule now writes a `sah-diagnostic:` line for a path it cannot judge:

    - the pre-flight test, which is now `[ ! -e "$file" ]`, states a path that holds no file;
    - a `sed` substitution states each file swiftlint could not read, which covers bytes that are not UTF-8 and a file mode that refuses a read.

    That work is DONE and this card must not do it again.

    The two branches THIS card names are still open in all three rules:

    1. The `Error: No lintable files found at paths:` branch exits 0 with no marked line. The run judged NO file, and it answers as a clean pass.
    2. The `Could not read configuration:` line has no `sah-diagnostic:` marker, so the report drops it.

    So the scope that is left is the two branches above, and no more.

    `complexity-swift` holds the same two branches and this card does not name it. Check that rule as well, or make a card for it.
  timestamp: 2026-08-17T07:02:19.000184+00:00
- actor: claude-code
  id: 01m07cjtwk8bjr1cazdqsxt55j
  text: |
    Research, and the measurements that decided the shape.

    `complexity-swift` does not exist. No rule file in `builtin/validators/` holds
    that name, and a search for `Error: No lintable files found at paths:` finds
    only the three rules this card names. So there is no fourth rule to change, and
    no new card is necessary.

    Measured with swiftlint 0.65.0, against the child configuration each of the
    three rules writes:

    - A SOUND run says nothing on stderr. Over one dirty file with no project
      configuration: entries on stdout, and 0 bytes on stderr. That is what lets
      the stderr channel carry a statement.
    - The `Error: No lintable files found at paths:` line names EVERY path of the
      run, and not only the paths swiftlint refused. Over `Sources/Absent.swift`
      beside `Generated/Docs.swift` under `excluded: [Generated]`, swiftlint wrote
      `Error: No lintable files found at paths: 'Sources/Absent.swift,
      Generated/Docs.swift'`.

    A forward of the whole stderr line under the marker was measured and it was NOT
    taken. The reason is a DOUBLE statement, and the measurement shows it: for a run
    over a path that holds no file and no other path beside it, the pre-flight
    `[ ! -e "$file" ]` test already states that path, and the forward would state the
    same path a second time. The shared helper `verify_declined_item_is_stated`
    holds a run to ONE statement for one declined item, and the shipped test
    `the_shipped_swift_function_length_tool_rule_declines_a_path_that_holds_no_file_alone`
    reads exactly that run. So the branch states each path of the run that IS there,
    one line for each, and the pre-flight test keeps the paths that hold no file.

    Measured with the shipped script over each shape:

    | the run | findings | diagnostics |
    |---|---|---|
    | one file under `Generated/` beside `excluded: [Generated]` | 0 | 1, that path |
    | a directory that holds no Swift file | 0 | 1, that path |
    | `src/Notes.txt`, whose name does not end in `.swift` | 0 | 1, that path |
    | a path that holds no file, alone | 0 | 1, from the pre-flight test |
    | a path that holds no file beside the excluded file | 0 | 2, one for each path |

    No path goes unstated, and no path is stated twice.
  timestamp: 2026-08-17T08:14:36.435429+00:00
- actor: claude-code
  id: 01m07ck7kb1wgfs1pw3089hdpb
  text: |
    ### implement — changed

    - evidence: 7 files.
      - `builtin/validators/code-hygiene/rules/missing-docs-swift.md`,
        `magic-numbers-swift.md`, `function-length-swift.md` — each script now marks
        the `Could not read configuration:` line with `sah-diagnostic:`, and each
        states one marked line for every path of the run that stands on disk before
        the `Error: No lintable files found at paths:` branch exits 0. Each rule body
        carries the new measurements.
      - `builtin/validators/README.md` — states that each of the three rules names
        every path of a run that judged no file, and states the 0-byte stderr
        measurement that lets the channel carry it.
      - `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/missing_docs.rs`,
        `magic_numbers.rs`, `function_length_swift.rs` — 6 new acceptance tests, 2 for
        each rule, through the shared helper `verify_declined_item_is_stated`.
    - RED: the 6 new tests each failed with `the run must state the one item it
      declined; it stated []`, left 0, right 1. The finding rows already matched, so
      the marked line is the one thing each test added.
    - GREEN: `cargo nextest run -p swissarmyhammer-validators -p mirdan` — 1316 tests
      run, 1316 passed, 0 skipped. `cargo fmt --all -- --check` clean.
      `cargo clippy -p swissarmyhammer-validators --all-targets` — 0 warnings.
    - `doctor.check_command` stays `which swiftlint jq grep sed cat mktemp`. The
      change adds no tool, and it drops none.
    - next: the card stays in `doing`, ready for `/review`.
  timestamp: 2026-08-17T08:14:49.451684+00:00
- actor: claude-code
  id: 01m07dbn25ve3znpaqcdh2ytyy
  text: |
    ### test — green

    - evidence: `cargo nextest run -p swissarmyhammer-validators -E 'test(swift)'` — 56 passed, 0 failed, 0 skipped; `cargo fmt --check` clean; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo nextest run --workspace` — 14155 passed, 0 failed, 0 skipped.

    Verified the 5 required claims by measurement (real swiftlint 0.65.0, real repo fixtures under scratchpad, `od -c` on raw stderr):

    1. README contract (builtin/validators/README.md) — the added paragraph after the "excluded:" section applies the pre-existing, unchanged general contract (lines 268-280: "A script that judged the code and could not judge ONE item says so on stderr... it still exits 0... Do not stay silent either") to the specific "No lintable files found" case. It does not relax or restate the general rule more weakly — it documents that all three rules now name every declined path under the marker in that scenario, consistent with the existing requirement.

    2. Loop-vs-whole-channel: drove real swiftlint over multiple args (excluded-but-present + absent) — swiftlint's own `Error: No lintable files found at paths: 'A.swift, Absent.swift, B.swift'` line names an ABSENT arg too. The script's earlier pre-loop already states absent paths under the marker; forwarding the whole channel here would restate that absent path a second time. The `for file in "$@"; do if [ -e "$file" ]; then ...; fi; done` loop names every existing (declined) arg with zero duplication and zero omission — measured 3 args in → 3 distinct diagnostics out, none repeated. Nothing a whole-channel forward would have caught is lost.

    3. Byte-0 opening: measured with `od -c` for all three rules × both branches (6 runs) — each `sah-diagnostic:` line opens immediately after a `\n` (or at file start), never mid-line/concatenated with prior unmarked output.

    4. `Could not read configuration:` branch keeps findings and exits 0 for all three rules — measured: missing-docs-swift 2 findings kept exit 0, magic-numbers-swift 1 finding kept exit 0, function-length-swift 1 finding kept exit 0 (each beside `child_config: other.yml` + `excluded: [Generated]`, matching the doc's own measured rows).

    5. Rust diff (function_length_swift.rs, magic_numbers.rs, missing_docs.rs) — no new `.unwrap()`, `.expect(`, `panic!(`, or unnamed numeric literal; all additions are doc comments, `#[test]` functions calling existing named-constant helpers.

    No fixes were needed — build was already clean.
  timestamp: 2026-08-17T08:28:09.669572+00:00
position_column: doing
position_ordinal: '8280'
title: Three shipped swiftlint rules decline an item silently; adopt the stderr diagnostic marker
---
The stderr diagnostic carrier landed on `^m6ba1bf`. A tool-rule script now states a declined item with a stderr line opening `sah-diagnostic:` while exiting 0, and the report renders it. `builtin/validators/README.md` states the contract.

Three shipped rules decline an item today and say nothing. Each has to write the marked line.

## `missing-docs-swift`, `function-length-swift`, `magic-numbers-swift`

Each holds this branch:

```sh
if [ "$measured" -eq 0 ]; then
  if grep -qE '^Error: No lintable files found at paths:' "$work/lint.err"; then
    exit 0
  fi
  exit 1
fi
```

swiftlint judged NO file — the project `excluded:` list covers every file the run named — and the rule answers 0 findings at exit 0. That reads as a clean pass over files nothing linted. Write the marker before the `exit 0` and name the paths swiftlint refused.

Each also holds a second one:

```sh
if [ -n "$project" ] && grep -qE '^Could not read configuration:' "$work/lint.err"; then
  printf '%s\n' '<rule>: swiftlint cannot read .swiftlint.yml beside this rule. The run drops the project exclude list.' >&2
  lint "" "$@"
fi
```

That line is already a diagnostic on stderr at exit 0, and it is dropped. Give it the marker.

## Done when

- Each of the three rules writes a `sah-diagnostic:` line on both branches.
- An acceptance test drives each shipped script over a project `.swiftlint.yml` that excludes every file and holds the run to reporting the diagnostic, not to silence.

#tool-validators
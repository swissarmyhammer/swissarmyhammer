{"id":"01KP65VMEVX9P6CP63AKB7C8BE","timestamp":"2026-04-14T14:20:52.059380Z","op":"create","item_id":"01KP65VMEVVNECSK61H6BK32BM","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,60 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: ce80\n+title: 'entity-cache 1/4: EntityEvent::EntityChanged carries Vec&lt;FieldChange&gt;; EntityCache computes the diff'\n+---\n+#entity-cache\n+\n+Parent design: `01KP65FJHDQ5R2N5C5BJHVHFBF`. This sub-card moves field-level diffing into the entity layer so the cache's broadcast channel carries the same shape the frontend's `entity-field-changed` Tauri event already consumes.\n+\n+## What\n+\n+`swissarmyhammer-entity/src/events.rs` currently emits `EntityChanged { entity_type, id, version }` — coarse, no field detail. The kanban-app watcher re-derives the diff from raw YAML via `diff_fields` at a higher layer. Move the diff into the cache where both the before-image and after-image live anyway, and make it part of the event payload.\n+\n+There is an existing `changelog::FieldChange` enum in `swissarmyhammer-entity/src/changelog.rs:48-80` with `Set`/`Removed`/`Changed`/`StringChanged` variants — that is the rich shape used for undo/redo. Do **not** reuse it for events; the frontend contract wants a simple `{field, value}`. Introduce a distinct `events::FieldChange` struct.\n+\n+Files:\n+\n+- [ ] `swissarmyhammer-entity/src/events.rs` — add:\n+  ```rust\n+  pub struct FieldChange {\n+      pub field: String,\n+      pub value: serde_json::Value,  // Null on removal\n+  }\n+  ```\n+  Change the variant to `EntityChanged { entity_type, id, version, changes: Vec<FieldChange> }`. Keep `serde(tag = \"type\")` and `Serialize/Deserialize` derives. Document that removals are encoded as `value: Null` (matches the frontend's existing patch semantics).\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — add a private `fn diff(old: Option<&Entity>, new: &Entity) -> Vec<FieldChange>` helper in the cache module (NOT re-exporting from changelog). Logic: walk `new.fields` — if key missing from `old` or value differs, emit `{field: key, value: new_value}`. Walk `old.fields` — if key missing from `new`, emit `{field: key, value: Null}`. For brand-new entities (`old.is_none()`), emit every `new.fields` entry.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::write` (`:152-203`): capture the pre-write cached entity as `old` before `self.inner.write`, compute `changes = diff(old.as_ref(), &canonical)`, emit `EntityChanged { changes }` (only when `changed`). The existing `old_hash` probe at `:154-158` still suppresses no-op writes — hash match ⇒ empty changes, no event.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::refresh_from_disk` (`:244-274`): same pattern — capture the pre-refresh cached entity, diff against the newly-read one, emit with `changes`.\n+\n+Subtasks:\n+\n+- [ ] Add `FieldChange` struct + change `EntityChanged` shape in `events.rs`.\n+- [ ] Add private `diff` helper in `cache.rs`.\n+- [ ] Update `EntityCache::write` and `EntityCache::refresh_from_disk` to compute and emit `changes`.\n+- [ ] Update existing `cache.rs` tests that destructure `EntityChanged { entity_type, id, version }` to also handle the `changes` field. Add new diff-specific tests (below).\n+\n+## Acceptance Criteria\n+\n+- [ ] `EntityEvent::EntityChanged` carries `Vec<FieldChange>` (field + value pairs). No-op writes (hash unchanged) emit no event.\n+- [ ] Removals are encoded as `{field, value: Null}`.\n+- [ ] Brand-new entities (first write, no prior cache entry) emit `changes` containing every field of the new entity.\n+- [ ] `changelog::FieldChange` is untouched — undo/redo tests stay green.\n+- [ ] `cargo nextest run -p swissarmyhammer-entity` passes including new tests.\n+\n+## Tests\n+\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_carries_field_diff`: write `{a:1, b:2}`; write `{a:1, b:3, c:4}`; receiver sees `EntityChanged { changes }` with entries for `b` and `c` (any order), no entry for `a`.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_encodes_removal_as_null`: write `{a:1, b:2}`; write `{a:1}`; receiver sees `EntityChanged { changes: [{b, Null}] }`.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_new_entity_lists_all_fields`: first write of `{a:1, b:2}`; receiver sees `EntityChanged { changes }` containing both `a` and `b` with their values.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — update `write_emits_entity_changed_event` (`:465-486`), `write_same_content_no_event` (`:510-524`), `versions_monotonically_increasing` (`:527-550`), `refresh_from_disk_emits_event_on_change` (`:553-570`) to destructure the new `changes` field.\n+- [ ] `cargo nextest run -p swissarmyhammer-entity` — full green.\n+\n+## Workflow\n+- Use `/tdd` — start with the three new diff-specific tests (they fail because the current event has no `changes` field and won't compile), add the struct + field, then implement the `diff` helper until tests pass.\n+\n+## Scope / depends_on\n+- No depends_on.\n+- Subsequent sub-cards depend on this: `entity-cache 2/4` (context wiring), `entity-cache 4/4` (kanban-app bridge collapse) both consume the new event shape.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,60 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: ce80\n-title: 'entity-cache 1/4: EntityEvent::EntityChanged carries Vec&lt;FieldChange&gt;; EntityCache computes the diff'\n----\n-#entity-cache\n-\n-Parent design: `01KP65FJHDQ5R2N5C5BJHVHFBF`. This sub-card moves field-level diffing into the entity layer so the cache's broadcast channel carries the same shape the frontend's `entity-field-changed` Tauri event already consumes.\n-\n-## What\n-\n-`swissarmyhammer-entity/src/events.rs` currently emits `EntityChanged { entity_type, id, version }` — coarse, no field detail. The kanban-app watcher re-derives the diff from raw YAML via `diff_fields` at a higher layer. Move the diff into the cache where both the before-image and after-image live anyway, and make it part of the event payload.\n-\n-There is an existing `changelog::FieldChange` enum in `swissarmyhammer-entity/src/changelog.rs:48-80` with `Set`/`Removed`/`Changed`/`StringChanged` variants — that is the rich shape used for undo/redo. Do **not** reuse it for events; the frontend contract wants a simple `{field, value}`. Introduce a distinct `events::FieldChange` struct.\n-\n-Files:\n-\n-- [ ] `swissarmyhammer-entity/src/events.rs` — add:\n-  ```rust\n-  pub struct FieldChange {\n-      pub field: String,\n-      pub value: serde_json::Value,  // Null on removal\n-  }\n-  ```\n-  Change the variant to `EntityChanged { entity_type, id, version, changes: Vec<FieldChange> }`. Keep `serde(tag = \"type\")` and `Serialize/Deserialize` derives. Document that removals are encoded as `value: Null` (matches the frontend's existing patch semantics).\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — add a private `fn diff(old: Option<&Entity>, new: &Entity) -> Vec<FieldChange>` helper in the cache module (NOT re-exporting from changelog). Logic: walk `new.fields` — if key missing from `old` or value differs, emit `{field: key, value: new_value}`. Walk `old.fields` — if key missing from `new`, emit `{field: key, value: Null}`. For brand-new entities (`old.is_none()`), emit every `new.fields` entry.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::write` (`:152-203`): capture the pre-write cached entity as `old` before `self.inner.write`, compute `changes = diff(old.as_ref(), &canonical)`, emit `EntityChanged { changes }` (only when `changed`). The existing `old_hash` probe at `:154-158` still suppresses no-op writes — hash match ⇒ empty changes, no event.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::refresh_from_disk` (`:244-274`): same pattern — capture the pre-refresh cached entity, diff against the newly-read one, emit with `changes`.\n-\n-Subtasks:\n-\n-- [ ] Add `FieldChange` struct + change `EntityChanged` shape in `events.rs`.\n-- [ ] Add private `diff` helper in `cache.rs`.\n-- [ ] Update `EntityCache::write` and `EntityCache::refresh_from_disk` to compute and emit `changes`.\n-- [ ] Update existing `cache.rs` tests that destructure `EntityChanged { entity_type, id, version }` to also handle the `changes` field. Add new diff-specific tests (below).\n-\n-## Acceptance Criteria\n-\n-- [ ] `EntityEvent::EntityChanged` carries `Vec<FieldChange>` (field + value pairs). No-op writes (hash unchanged) emit no event.\n-- [ ] Removals are encoded as `{field, value: Null}`.\n-- [ ] Brand-new entities (first write, no prior cache entry) emit `changes` containing every field of the new entity.\n-- [ ] `changelog::FieldChange` is untouched — undo/redo tests stay green.\n-- [ ] `cargo nextest run -p swissarmyhammer-entity` passes including new tests.\n-\n-## Tests\n-\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_carries_field_diff`: write `{a:1, b:2}`; write `{a:1, b:3, c:4}`; receiver sees `EntityChanged { changes }` with entries for `b` and `c` (any order), no entry for `a`.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_encodes_removal_as_null`: write `{a:1, b:2}`; write `{a:1}`; receiver sees `EntityChanged { changes: [{b, Null}] }`.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_new_entity_lists_all_fields`: first write of `{a:1, b:2}`; receiver sees `EntityChanged { changes }` containing both `a` and `b` with their values.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — update `write_emits_entity_changed_event` (`:465-486`), `write_same_content_no_event` (`:510-524`), `versions_monotonically_increasing` (`:527-550`), `refresh_from_disk_emits_event_on_change` (`:553-570`) to destructure the new `changes` field.\n-- [ ] `cargo nextest run -p swissarmyhammer-entity` — full green.\n-\n-## Workflow\n-- Use `/tdd` — start with the three new diff-specific tests (they fail because the current event has no `changes` field and won't compile), add the struct + field, then implement the `diff` helper until tests pass.\n-\n-## Scope / depends_on\n-- No depends_on.\n-- Subsequent sub-cards depend on this: `entity-cache 2/4` (context wiring), `entity-cache 4/4` (kanban-app bridge collapse) both consume the new event shape.\n","transaction_id":null}
{"id":"01KP65VMEVAWZBBAT4H5ERHBRN","timestamp":"2026-04-14T14:20:52.059797Z","entity_type":"task","entity_id":"01KP65VMEVVNECSK61H6BK32BM","op":"create","changes":[["assignees",{"kind":"set","value":["claude-code"]}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"ce80"}],["title",{"kind":"set","value":"entity-cache 1/4: EntityEvent::EntityChanged carries Vec&lt;FieldChange&gt;; EntityCache computes the diff"}],["body",{"kind":"set","value":"#entity-cache\n\nParent design: `01KP65FJHDQ5R2N5C5BJHVHFBF`. This sub-card moves field-level diffing into the entity layer so the cache's broadcast channel carries the same shape the frontend's `entity-field-changed` Tauri event already consumes.\n\n## What\n\n`swissarmyhammer-entity/src/events.rs` currently emits `EntityChanged { entity_type, id, version }` — coarse, no field detail. The kanban-app watcher re-derives the diff from raw YAML via `diff_fields` at a higher layer. Move the diff into the cache where both the before-image and after-image live anyway, and make it part of the event payload.\n\nThere is an existing `changelog::FieldChange` enum in `swissarmyhammer-entity/src/changelog.rs:48-80` with `Set`/`Removed`/`Changed`/`StringChanged` variants — that is the rich shape used for undo/redo. Do **not** reuse it for events; the frontend contract wants a simple `{field, value}`. Introduce a distinct `events::FieldChange` struct.\n\nFiles:\n\n- [ ] `swissarmyhammer-entity/src/events.rs` — add:\n  ```rust\n  pub struct FieldChange {\n      pub field: String,\n      pub value: serde_json::Value,  // Null on removal\n  }\n  ```\n  Change the variant to `EntityChanged { entity_type, id, version, changes: Vec<FieldChange> }`. Keep `serde(tag = \"type\")` and `Serialize/Deserialize` derives. Document that removals are encoded as `value: Null` (matches the frontend's existing patch semantics).\n- [ ] `swissarmyhammer-entity/src/cache.rs` — add a private `fn diff(old: Option<&Entity>, new: &Entity) -> Vec<FieldChange>` helper in the cache module (NOT re-exporting from changelog). Logic: walk `new.fields` — if key missing from `old` or value differs, emit `{field: key, value: new_value}`. Walk `old.fields` — if key missing from `new`, emit `{field: key, value: Null}`. For brand-new entities (`old.is_none()`), emit every `new.fields` entry.\n- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::write` (`:152-203`): capture the pre-write cached entity as `old` before `self.inner.write`, compute `changes = diff(old.as_ref(), &canonical)`, emit `EntityChanged { changes }` (only when `changed`). The existing `old_hash` probe at `:154-158` still suppresses no-op writes — hash match ⇒ empty changes, no event.\n- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::refresh_from_disk` (`:244-274`): same pattern — capture the pre-refresh cached entity, diff against the newly-read one, emit with `changes`.\n\nSubtasks:\n\n- [ ] Add `FieldChange` struct + change `EntityChanged` shape in `events.rs`.\n- [ ] Add private `diff` helper in `cache.rs`.\n- [ ] Update `EntityCache::write` and `EntityCache::refresh_from_disk` to compute and emit `changes`.\n- [ ] Update existing `cache.rs` tests that destructure `EntityChanged { entity_type, id, version }` to also handle the `changes` field. Add new diff-specific tests (below).\n\n## Acceptance Criteria\n\n- [ ] `EntityEvent::EntityChanged` carries `Vec<FieldChange>` (field + value pairs). No-op writes (hash unchanged) emit no event.\n- [ ] Removals are encoded as `{field, value: Null}`.\n- [ ] Brand-new entities (first write, no prior cache entry) emit `changes` containing every field of the new entity.\n- [ ] `changelog::FieldChange` is untouched — undo/redo tests stay green.\n- [ ] `cargo nextest run -p swissarmyhammer-entity` passes including new tests.\n\n## Tests\n\n- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_carries_field_diff`: write `{a:1, b:2}`; write `{a:1, b:3, c:4}`; receiver sees `EntityChanged { changes }` with entries for `b` and `c` (any order), no entry for `a`.\n- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_encodes_removal_as_null`: write `{a:1, b:2}`; write `{a:1}`; receiver sees `EntityChanged { changes: [{b, Null}] }`.\n- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_new_entity_lists_all_fields`: first write of `{a:1, b:2}`; receiver sees `EntityChanged { changes }` containing both `a` and `b` with their values.\n- [ ] `swissarmyhammer-entity/src/cache.rs` — update `write_emits_entity_changed_event` (`:465-486`), `write_same_content_no_event` (`:510-524`), `versions_monotonically_increasing` (`:527-550`), `refresh_from_disk_emits_event_on_change` (`:553-570`) to destructure the new `changes` field.\n- [ ] `cargo nextest run -p swissarmyhammer-entity` — full green.\n\n## Workflow\n- Use `/tdd` — start with the three new diff-specific tests (they fail because the current event has no `changes` field and won't compile), add the struct + field, then implement the `diff` helper until tests pass.\n\n## Scope / depends_on\n- No depends_on.\n- Subsequent sub-cards depend on this: `entity-cache 2/4` (context wiring), `entity-cache 4/4` (kanban-app bridge collapse) both consume the new event shape.\n"}]]}
{"id":"01KP780H8WH5M8K0KYPFYR0JYC","timestamp":"2026-04-15T00:17:44.220740Z","op":"update","item_id":"01KP65VMEVVNECSK61H6BK32BM","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: ce80\n+position_column: doing\n+position_ordinal: '80'\n title: 'entity-cache 1/4: EntityEvent::EntityChanged carries Vec&lt;FieldChange&gt;; EntityCache computes the diff'\n ---\n #entity-cache\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: ce80\n title: 'entity-cache 1/4: EntityEvent::EntityChanged carries Vec&lt;FieldChange&gt;; EntityCache computes the diff'\n ---\n #entity-cache\n","transaction_id":null}
{"id":"01KP780H8X4AARGT58K5E8HTZF","timestamp":"2026-04-15T00:17:44.221509Z","entity_type":"task","entity_id":"01KP65VMEVVNECSK61H6BK32BM","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ce80\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ce80\n\\ No newline at end of file\n"}]]}
{"id":"01KP795S2Q7TPMJN1E82Z2GADS","timestamp":"2026-04-15T00:38:04.631724Z","op":"update","item_id":"01KP65VMEVVNECSK61H6BK32BM","forward_patch":"--- original\n+++ modified\n@@ -17,7 +17,7 @@\n\n Files:\n\n-- [ ] `swissarmyhammer-entity/src/events.rs` — add:\n+- [x] `swissarmyhammer-entity/src/events.rs` — add:\n   ```rust\n   pub struct FieldChange {\n       pub field: String,\n@@ -25,32 +25,32 @@\n   }\n   ```\n   Change the variant to `EntityChanged { entity_type, id, version, changes: Vec<FieldChange> }`. Keep `serde(tag = \"type\")` and `Serialize/Deserialize` derives. Document that removals are encoded as `value: Null` (matches the frontend's existing patch semantics).\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — add a private `fn diff(old: Option<&Entity>, new: &Entity) -> Vec<FieldChange>` helper in the cache module (NOT re-exporting from changelog). Logic: walk `new.fields` — if key missing from `old` or value differs, emit `{field: key, value: new_value}`. Walk `old.fields` — if key missing from `new`, emit `{field: key, value: Null}`. For brand-new entities (`old.is_none()`), emit every `new.fields` entry.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::write` (`:152-203`): capture the pre-write cached entity as `old` before `self.inner.write`, compute `changes = diff(old.as_ref(), &canonical)`, emit `EntityChanged { changes }` (only when `changed`). The existing `old_hash` probe at `:154-158` still suppresses no-op writes — hash match ⇒ empty changes, no event.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::refresh_from_disk` (`:244-274`): same pattern — capture the pre-refresh cached entity, diff against the newly-read one, emit with `changes`.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — add a private `fn diff(old: Option<&Entity>, new: &Entity) -> Vec<FieldChange>` helper in the cache module (NOT re-exporting from changelog). Logic: walk `new.fields` — if key missing from `old` or value differs, emit `{field: key, value: new_value}`. Walk `old.fields` — if key missing from `new`, emit `{field: key, value: Null}`. For brand-new entities (`old.is_none()`), emit every `new.fields` entry.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::write` (`:152-203`): capture the pre-write cached entity as `old` before `self.inner.write`, compute `changes = diff(old.as_ref(), &canonical)`, emit `EntityChanged { changes }` (only when `changed`). The existing `old_hash` probe at `:154-158` still suppresses no-op writes — hash match ⇒ empty changes, no event.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::refresh_from_disk` (`:244-274`): same pattern — capture the pre-refresh cached entity, diff against the newly-read one, emit with `changes`.\n\n Subtasks:\n\n-- [ ] Add `FieldChange` struct + change `EntityChanged` shape in `events.rs`.\n-- [ ] Add private `diff` helper in `cache.rs`.\n-- [ ] Update `EntityCache::write` and `EntityCache::refresh_from_disk` to compute and emit `changes`.\n-- [ ] Update existing `cache.rs` tests that destructure `EntityChanged { entity_type, id, version }` to also handle the `changes` field. Add new diff-specific tests (below).\n+- [x] Add `FieldChange` struct + change `EntityChanged` shape in `events.rs`.\n+- [x] Add private `diff` helper in `cache.rs`.\n+- [x] Update `EntityCache::write` and `EntityCache::refresh_from_disk` to compute and emit `changes`.\n+- [x] Update existing `cache.rs` tests that destructure `EntityChanged { entity_type, id, version }` to also handle the `changes` field. Add new diff-specific tests (below).\n\n ## Acceptance Criteria\n\n-- [ ] `EntityEvent::EntityChanged` carries `Vec<FieldChange>` (field + value pairs). No-op writes (hash unchanged) emit no event.\n-- [ ] Removals are encoded as `{field, value: Null}`.\n-- [ ] Brand-new entities (first write, no prior cache entry) emit `changes` containing every field of the new entity.\n-- [ ] `changelog::FieldChange` is untouched — undo/redo tests stay green.\n-- [ ] `cargo nextest run -p swissarmyhammer-entity` passes including new tests.\n+- [x] `EntityEvent::EntityChanged` carries `Vec<FieldChange>` (field + value pairs). No-op writes (hash unchanged) emit no event.\n+- [x] Removals are encoded as `{field, value: Null}`.\n+- [x] Brand-new entities (first write, no prior cache entry) emit `changes` containing every field of the new entity.\n+- [x] `changelog::FieldChange` is untouched — undo/redo tests stay green.\n+- [x] `cargo nextest run -p swissarmyhammer-entity` passes including new tests.\n\n ## Tests\n\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_carries_field_diff`: write `{a:1, b:2}`; write `{a:1, b:3, c:4}`; receiver sees `EntityChanged { changes }` with entries for `b` and `c` (any order), no entry for `a`.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_encodes_removal_as_null`: write `{a:1, b:2}`; write `{a:1}`; receiver sees `EntityChanged { changes: [{b, Null}] }`.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_new_entity_lists_all_fields`: first write of `{a:1, b:2}`; receiver sees `EntityChanged { changes }` containing both `a` and `b` with their values.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — update `write_emits_entity_changed_event` (`:465-486`), `write_same_content_no_event` (`:510-524`), `versions_monotonically_increasing` (`:527-550`), `refresh_from_disk_emits_event_on_change` (`:553-570`) to destructure the new `changes` field.\n-- [ ] `cargo nextest run -p swissarmyhammer-entity` — full green.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_carries_field_diff`: write `{a:1, b:2}`; write `{a:1, b:3, c:4}`; receiver sees `EntityChanged { changes }` with entries for `b` and `c` (any order), no entry for `a`.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_encodes_removal_as_null`: write `{a:1, b:2}`; write `{a:1}`; receiver sees `EntityChanged { changes: [{b, Null}] }`.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_new_entity_lists_all_fields`: first write of `{a:1, b:2}`; receiver sees `EntityChanged { changes }` containing both `a` and `b` with their values.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — update `write_emits_entity_changed_event` (`:465-486`), `write_same_content_no_event` (`:510-524`), `versions_monotonically_increasing` (`:527-550`), `refresh_from_disk_emits_event_on_change` (`:553-570`) to destructure the new `changes` field.\n+- [x] `cargo nextest run -p swissarmyhammer-entity` — full green.\n\n ## Workflow\n - Use `/tdd` — start with the three new diff-specific tests (they fail because the current event has no `changes` field and won't compile), add the struct + field, then implement the `diff` helper until tests pass.\n","reverse_patch":"--- original\n+++ modified\n@@ -17,7 +17,7 @@\n\n Files:\n\n-- [x] `swissarmyhammer-entity/src/events.rs` — add:\n+- [ ] `swissarmyhammer-entity/src/events.rs` — add:\n   ```rust\n   pub struct FieldChange {\n       pub field: String,\n@@ -25,32 +25,32 @@\n   }\n   ```\n   Change the variant to `EntityChanged { entity_type, id, version, changes: Vec<FieldChange> }`. Keep `serde(tag = \"type\")` and `Serialize/Deserialize` derives. Document that removals are encoded as `value: Null` (matches the frontend's existing patch semantics).\n-- [x] `swissarmyhammer-entity/src/cache.rs` — add a private `fn diff(old: Option<&Entity>, new: &Entity) -> Vec<FieldChange>` helper in the cache module (NOT re-exporting from changelog). Logic: walk `new.fields` — if key missing from `old` or value differs, emit `{field: key, value: new_value}`. Walk `old.fields` — if key missing from `new`, emit `{field: key, value: Null}`. For brand-new entities (`old.is_none()`), emit every `new.fields` entry.\n-- [x] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::write` (`:152-203`): capture the pre-write cached entity as `old` before `self.inner.write`, compute `changes = diff(old.as_ref(), &canonical)`, emit `EntityChanged { changes }` (only when `changed`). The existing `old_hash` probe at `:154-158` still suppresses no-op writes — hash match ⇒ empty changes, no event.\n-- [x] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::refresh_from_disk` (`:244-274`): same pattern — capture the pre-refresh cached entity, diff against the newly-read one, emit with `changes`.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — add a private `fn diff(old: Option<&Entity>, new: &Entity) -> Vec<FieldChange>` helper in the cache module (NOT re-exporting from changelog). Logic: walk `new.fields` — if key missing from `old` or value differs, emit `{field: key, value: new_value}`. Walk `old.fields` — if key missing from `new`, emit `{field: key, value: Null}`. For brand-new entities (`old.is_none()`), emit every `new.fields` entry.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::write` (`:152-203`): capture the pre-write cached entity as `old` before `self.inner.write`, compute `changes = diff(old.as_ref(), &canonical)`, emit `EntityChanged { changes }` (only when `changed`). The existing `old_hash` probe at `:154-158` still suppresses no-op writes — hash match ⇒ empty changes, no event.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::refresh_from_disk` (`:244-274`): same pattern — capture the pre-refresh cached entity, diff against the newly-read one, emit with `changes`.\n\n Subtasks:\n\n-- [x] Add `FieldChange` struct + change `EntityChanged` shape in `events.rs`.\n-- [x] Add private `diff` helper in `cache.rs`.\n-- [x] Update `EntityCache::write` and `EntityCache::refresh_from_disk` to compute and emit `changes`.\n-- [x] Update existing `cache.rs` tests that destructure `EntityChanged { entity_type, id, version }` to also handle the `changes` field. Add new diff-specific tests (below).\n+- [ ] Add `FieldChange` struct + change `EntityChanged` shape in `events.rs`.\n+- [ ] Add private `diff` helper in `cache.rs`.\n+- [ ] Update `EntityCache::write` and `EntityCache::refresh_from_disk` to compute and emit `changes`.\n+- [ ] Update existing `cache.rs` tests that destructure `EntityChanged { entity_type, id, version }` to also handle the `changes` field. Add new diff-specific tests (below).\n\n ## Acceptance Criteria\n\n-- [x] `EntityEvent::EntityChanged` carries `Vec<FieldChange>` (field + value pairs). No-op writes (hash unchanged) emit no event.\n-- [x] Removals are encoded as `{field, value: Null}`.\n-- [x] Brand-new entities (first write, no prior cache entry) emit `changes` containing every field of the new entity.\n-- [x] `changelog::FieldChange` is untouched — undo/redo tests stay green.\n-- [x] `cargo nextest run -p swissarmyhammer-entity` passes including new tests.\n+- [ ] `EntityEvent::EntityChanged` carries `Vec<FieldChange>` (field + value pairs). No-op writes (hash unchanged) emit no event.\n+- [ ] Removals are encoded as `{field, value: Null}`.\n+- [ ] Brand-new entities (first write, no prior cache entry) emit `changes` containing every field of the new entity.\n+- [ ] `changelog::FieldChange` is untouched — undo/redo tests stay green.\n+- [ ] `cargo nextest run -p swissarmyhammer-entity` passes including new tests.\n\n ## Tests\n\n-- [x] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_carries_field_diff`: write `{a:1, b:2}`; write `{a:1, b:3, c:4}`; receiver sees `EntityChanged { changes }` with entries for `b` and `c` (any order), no entry for `a`.\n-- [x] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_encodes_removal_as_null`: write `{a:1, b:2}`; write `{a:1}`; receiver sees `EntityChanged { changes: [{b, Null}] }`.\n-- [x] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_new_entity_lists_all_fields`: first write of `{a:1, b:2}`; receiver sees `EntityChanged { changes }` containing both `a` and `b` with their values.\n-- [x] `swissarmyhammer-entity/src/cache.rs` — update `write_emits_entity_changed_event` (`:465-486`), `write_same_content_no_event` (`:510-524`), `versions_monotonically_increasing` (`:527-550`), `refresh_from_disk_emits_event_on_change` (`:553-570`) to destructure the new `changes` field.\n-- [x] `cargo nextest run -p swissarmyhammer-entity` — full green.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_carries_field_diff`: write `{a:1, b:2}`; write `{a:1, b:3, c:4}`; receiver sees `EntityChanged { changes }` with entries for `b` and `c` (any order), no entry for `a`.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_encodes_removal_as_null`: write `{a:1, b:2}`; write `{a:1}`; receiver sees `EntityChanged { changes: [{b, Null}] }`.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_new_entity_lists_all_fields`: first write of `{a:1, b:2}`; receiver sees `EntityChanged { changes }` containing both `a` and `b` with their values.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — update `write_emits_entity_changed_event` (`:465-486`), `write_same_content_no_event` (`:510-524`), `versions_monotonically_increasing` (`:527-550`), `refresh_from_disk_emits_event_on_change` (`:553-570`) to destructure the new `changes` field.\n+- [ ] `cargo nextest run -p swissarmyhammer-entity` — full green.\n\n ## Workflow\n - Use `/tdd` — start with the three new diff-specific tests (they fail because the current event has no `changes` field and won't compile), add the struct + field, then implement the `diff` helper until tests pass.\n","transaction_id":null}
{"id":"01KP795S2R0R5XMY4HWS1EFN49","timestamp":"2026-04-15T00:38:04.632136Z","entity_type":"task","entity_id":"01KP65VMEVVNECSK61H6BK32BM","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -10,7 +10,7 @@\n\n Files:\n\n-- [ ] `swissarmyhammer-entity/src/events.rs` — add:\n+- [x] `swissarmyhammer-entity/src/events.rs` — add:\n   ```rust\n   pub struct FieldChange {\n       pub field: String,\n@@ -18,32 +18,32 @@\n   }\n   ```\n   Change the variant to `EntityChanged { entity_type, id, version, changes: Vec<FieldChange> }`. Keep `serde(tag = \"type\")` and `Serialize/Deserialize` derives. Document that removals are encoded as `value: Null` (matches the frontend's existing patch semantics).\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — add a private `fn diff(old: Option<&Entity>, new: &Entity) -> Vec<FieldChange>` helper in the cache module (NOT re-exporting from changelog). Logic: walk `new.fields` — if key missing from `old` or value differs, emit `{field: key, value: new_value}`. Walk `old.fields` — if key missing from `new`, emit `{field: key, value: Null}`. For brand-new entities (`old.is_none()`), emit every `new.fields` entry.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::write` (`:152-203`): capture the pre-write cached entity as `old` before `self.inner.write`, compute `changes = diff(old.as_ref(), &canonical)`, emit `EntityChanged { changes }` (only when `changed`). The existing `old_hash` probe at `:154-158` still suppresses no-op writes — hash match ⇒ empty changes, no event.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::refresh_from_disk` (`:244-274`): same pattern — capture the pre-refresh cached entity, diff against the newly-read one, emit with `changes`.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — add a private `fn diff(old: Option<&Entity>, new: &Entity) -> Vec<FieldChange>` helper in the cache module (NOT re-exporting from changelog). Logic: walk `new.fields` — if key missing from `old` or value differs, emit `{field: key, value: new_value}`. Walk `old.fields` — if key missing from `new`, emit `{field: key, value: Null}`. For brand-new entities (`old.is_none()`), emit every `new.fields` entry.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::write` (`:152-203`): capture the pre-write cached entity as `old` before `self.inner.write`, compute `changes = diff(old.as_ref(), &canonical)`, emit `EntityChanged { changes }` (only when `changed`). The existing `old_hash` probe at `:154-158` still suppresses no-op writes — hash match ⇒ empty changes, no event.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::refresh_from_disk` (`:244-274`): same pattern — capture the pre-refresh cached entity, diff against the newly-read one, emit with `changes`.\n\n Subtasks:\n\n-- [ ] Add `FieldChange` struct + change `EntityChanged` shape in `events.rs`.\n-- [ ] Add private `diff` helper in `cache.rs`.\n-- [ ] Update `EntityCache::write` and `EntityCache::refresh_from_disk` to compute and emit `changes`.\n-- [ ] Update existing `cache.rs` tests that destructure `EntityChanged { entity_type, id, version }` to also handle the `changes` field. Add new diff-specific tests (below).\n+- [x] Add `FieldChange` struct + change `EntityChanged` shape in `events.rs`.\n+- [x] Add private `diff` helper in `cache.rs`.\n+- [x] Update `EntityCache::write` and `EntityCache::refresh_from_disk` to compute and emit `changes`.\n+- [x] Update existing `cache.rs` tests that destructure `EntityChanged { entity_type, id, version }` to also handle the `changes` field. Add new diff-specific tests (below).\n\n ## Acceptance Criteria\n\n-- [ ] `EntityEvent::EntityChanged` carries `Vec<FieldChange>` (field + value pairs). No-op writes (hash unchanged) emit no event.\n-- [ ] Removals are encoded as `{field, value: Null}`.\n-- [ ] Brand-new entities (first write, no prior cache entry) emit `changes` containing every field of the new entity.\n-- [ ] `changelog::FieldChange` is untouched — undo/redo tests stay green.\n-- [ ] `cargo nextest run -p swissarmyhammer-entity` passes including new tests.\n+- [x] `EntityEvent::EntityChanged` carries `Vec<FieldChange>` (field + value pairs). No-op writes (hash unchanged) emit no event.\n+- [x] Removals are encoded as `{field, value: Null}`.\n+- [x] Brand-new entities (first write, no prior cache entry) emit `changes` containing every field of the new entity.\n+- [x] `changelog::FieldChange` is untouched — undo/redo tests stay green.\n+- [x] `cargo nextest run -p swissarmyhammer-entity` passes including new tests.\n\n ## Tests\n\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_carries_field_diff`: write `{a:1, b:2}`; write `{a:1, b:3, c:4}`; receiver sees `EntityChanged { changes }` with entries for `b` and `c` (any order), no entry for `a`.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_encodes_removal_as_null`: write `{a:1, b:2}`; write `{a:1}`; receiver sees `EntityChanged { changes: [{b, Null}] }`.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_new_entity_lists_all_fields`: first write of `{a:1, b:2}`; receiver sees `EntityChanged { changes }` containing both `a` and `b` with their values.\n-- [ ] `swissarmyhammer-entity/src/cache.rs` — update `write_emits_entity_changed_event` (`:465-486`), `write_same_content_no_event` (`:510-524`), `versions_monotonically_increasing` (`:527-550`), `refresh_from_disk_emits_event_on_change` (`:553-570`) to destructure the new `changes` field.\n-- [ ] `cargo nextest run -p swissarmyhammer-entity` — full green.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_carries_field_diff`: write `{a:1, b:2}`; write `{a:1, b:3, c:4}`; receiver sees `EntityChanged { changes }` with entries for `b` and `c` (any order), no entry for `a`.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_encodes_removal_as_null`: write `{a:1, b:2}`; write `{a:1}`; receiver sees `EntityChanged { changes: [{b, Null}] }`.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_new_entity_lists_all_fields`: first write of `{a:1, b:2}`; receiver sees `EntityChanged { changes }` containing both `a` and `b` with their values.\n+- [x] `swissarmyhammer-entity/src/cache.rs` — update `write_emits_entity_changed_event` (`:465-486`), `write_same_content_no_event` (`:510-524`), `versions_monotonically_increasing` (`:527-550`), `refresh_from_disk_emits_event_on_change` (`:553-570`) to destructure the new `changes` field.\n+- [x] `cargo nextest run -p swissarmyhammer-entity` — full green.\n\n ## Workflow\n - Use `/tdd` — start with the three new diff-specific tests (they fail because the current event has no `changes` field and won't compile), add the struct + field, then implement the `diff` helper until tests pass.\n","reverse_patch":"--- original\n+++ modified\n@@ -10,7 +10,7 @@\n\n Files:\n\n-- [x] `swissarmyhammer-entity/src/events.rs` — add:\n+- [ ] `swissarmyhammer-entity/src/events.rs` — add:\n   ```rust\n   pub struct FieldChange {\n       pub field: String,\n@@ -18,32 +18,32 @@\n   }\n   ```\n   Change the variant to `EntityChanged { entity_type, id, version, changes: Vec<FieldChange> }`. Keep `serde(tag = \"type\")` and `Serialize/Deserialize` derives. Document that removals are encoded as `value: Null` (matches the frontend's existing patch semantics).\n-- [x] `swissarmyhammer-entity/src/cache.rs` — add a private `fn diff(old: Option<&Entity>, new: &Entity) -> Vec<FieldChange>` helper in the cache module (NOT re-exporting from changelog). Logic: walk `new.fields` — if key missing from `old` or value differs, emit `{field: key, value: new_value}`. Walk `old.fields` — if key missing from `new`, emit `{field: key, value: Null}`. For brand-new entities (`old.is_none()`), emit every `new.fields` entry.\n-- [x] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::write` (`:152-203`): capture the pre-write cached entity as `old` before `self.inner.write`, compute `changes = diff(old.as_ref(), &canonical)`, emit `EntityChanged { changes }` (only when `changed`). The existing `old_hash` probe at `:154-158` still suppresses no-op writes — hash match ⇒ empty changes, no event.\n-- [x] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::refresh_from_disk` (`:244-274`): same pattern — capture the pre-refresh cached entity, diff against the newly-read one, emit with `changes`.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — add a private `fn diff(old: Option<&Entity>, new: &Entity) -> Vec<FieldChange>` helper in the cache module (NOT re-exporting from changelog). Logic: walk `new.fields` — if key missing from `old` or value differs, emit `{field: key, value: new_value}`. Walk `old.fields` — if key missing from `new`, emit `{field: key, value: Null}`. For brand-new entities (`old.is_none()`), emit every `new.fields` entry.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::write` (`:152-203`): capture the pre-write cached entity as `old` before `self.inner.write`, compute `changes = diff(old.as_ref(), &canonical)`, emit `EntityChanged { changes }` (only when `changed`). The existing `old_hash` probe at `:154-158` still suppresses no-op writes — hash match ⇒ empty changes, no event.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `EntityCache::refresh_from_disk` (`:244-274`): same pattern — capture the pre-refresh cached entity, diff against the newly-read one, emit with `changes`.\n\n Subtasks:\n\n-- [x] Add `FieldChange` struct + change `EntityChanged` shape in `events.rs`.\n-- [x] Add private `diff` helper in `cache.rs`.\n-- [x] Update `EntityCache::write` and `EntityCache::refresh_from_disk` to compute and emit `changes`.\n-- [x] Update existing `cache.rs` tests that destructure `EntityChanged { entity_type, id, version }` to also handle the `changes` field. Add new diff-specific tests (below).\n+- [ ] Add `FieldChange` struct + change `EntityChanged` shape in `events.rs`.\n+- [ ] Add private `diff` helper in `cache.rs`.\n+- [ ] Update `EntityCache::write` and `EntityCache::refresh_from_disk` to compute and emit `changes`.\n+- [ ] Update existing `cache.rs` tests that destructure `EntityChanged { entity_type, id, version }` to also handle the `changes` field. Add new diff-specific tests (below).\n\n ## Acceptance Criteria\n\n-- [x] `EntityEvent::EntityChanged` carries `Vec<FieldChange>` (field + value pairs). No-op writes (hash unchanged) emit no event.\n-- [x] Removals are encoded as `{field, value: Null}`.\n-- [x] Brand-new entities (first write, no prior cache entry) emit `changes` containing every field of the new entity.\n-- [x] `changelog::FieldChange` is untouched — undo/redo tests stay green.\n-- [x] `cargo nextest run -p swissarmyhammer-entity` passes including new tests.\n+- [ ] `EntityEvent::EntityChanged` carries `Vec<FieldChange>` (field + value pairs). No-op writes (hash unchanged) emit no event.\n+- [ ] Removals are encoded as `{field, value: Null}`.\n+- [ ] Brand-new entities (first write, no prior cache entry) emit `changes` containing every field of the new entity.\n+- [ ] `changelog::FieldChange` is untouched — undo/redo tests stay green.\n+- [ ] `cargo nextest run -p swissarmyhammer-entity` passes including new tests.\n\n ## Tests\n\n-- [x] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_carries_field_diff`: write `{a:1, b:2}`; write `{a:1, b:3, c:4}`; receiver sees `EntityChanged { changes }` with entries for `b` and `c` (any order), no entry for `a`.\n-- [x] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_encodes_removal_as_null`: write `{a:1, b:2}`; write `{a:1}`; receiver sees `EntityChanged { changes: [{b, Null}] }`.\n-- [x] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_new_entity_lists_all_fields`: first write of `{a:1, b:2}`; receiver sees `EntityChanged { changes }` containing both `a` and `b` with their values.\n-- [x] `swissarmyhammer-entity/src/cache.rs` — update `write_emits_entity_changed_event` (`:465-486`), `write_same_content_no_event` (`:510-524`), `versions_monotonically_increasing` (`:527-550`), `refresh_from_disk_emits_event_on_change` (`:553-570`) to destructure the new `changes` field.\n-- [x] `cargo nextest run -p swissarmyhammer-entity` — full green.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_carries_field_diff`: write `{a:1, b:2}`; write `{a:1, b:3, c:4}`; receiver sees `EntityChanged { changes }` with entries for `b` and `c` (any order), no entry for `a`.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_encodes_removal_as_null`: write `{a:1, b:2}`; write `{a:1}`; receiver sees `EntityChanged { changes: [{b, Null}] }`.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — `test_entity_changed_new_entity_lists_all_fields`: first write of `{a:1, b:2}`; receiver sees `EntityChanged { changes }` containing both `a` and `b` with their values.\n+- [ ] `swissarmyhammer-entity/src/cache.rs` — update `write_emits_entity_changed_event` (`:465-486`), `write_same_content_no_event` (`:510-524`), `versions_monotonically_increasing` (`:527-550`), `refresh_from_disk_emits_event_on_change` (`:553-570`) to destructure the new `changes` field.\n+- [ ] `cargo nextest run -p swissarmyhammer-entity` — full green.\n\n ## Workflow\n - Use `/tdd` — start with the three new diff-specific tests (they fail because the current event has no `changes` field and won't compile), add the struct + field, then implement the `diff` helper until tests pass.\n"}]]}
{"id":"01KP7962QDMCWH2WHTAZ0Y077H","timestamp":"2026-04-15T00:38:14.509676Z","op":"update","item_id":"01KP65VMEVVNECSK61H6BK32BM","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: 'entity-cache 1/4: EntityEvent::EntityChanged carries Vec&lt;FieldChange&gt;; EntityCache computes the diff'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: 'entity-cache 1/4: EntityEvent::EntityChanged carries Vec&lt;FieldChange&gt;; EntityCache computes the diff'\n ---\n","transaction_id":null}
{"id":"01KP7962QE0TT69QTASP6SK2YB","timestamp":"2026-04-15T00:38:14.510067Z","entity_type":"task","entity_id":"01KP65VMEVVNECSK61H6BK32BM","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KP7AH2R886CP66FV5BD4GDB9","timestamp":"2026-04-15T01:01:43.560536Z","op":"update","item_id":"01KP65VMEVVNECSK61H6BK32BM","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffd180\n title: 'entity-cache 1/4: EntityEvent::EntityChanged carries Vec&lt;FieldChange&gt;; EntityCache computes the diff'\n ---\n #entity-cache\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffd180\n+position_column: review\n+position_ordinal: '80'\n title: 'entity-cache 1/4: EntityEvent::EntityChanged carries Vec&lt;FieldChange&gt;; EntityCache computes the diff'\n ---\n #entity-cache\n","transaction_id":null}
{"id":"01KP7AH2RB8KBF865F3J9QBTZS","timestamp":"2026-04-15T01:01:43.563851Z","entity_type":"task","entity_id":"01KP65VMEVVNECSK61H6BK32BM","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffffffffffffd180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffd180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5NTFMRTB776MWJPPGGA7VT","timestamp":"2026-04-26T19:56:18.968413Z","op":"update","item_id":"01KP65VMEVVNECSK61H6BK32BM","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: done\n-position_ordinal: ffffffffffffffffffffffd180\n+position_ordinal: ffffffffffffffffffffffffffffffc380\n title: 'entity-cache 1/4: EntityEvent::EntityChanged carries Vec&lt;FieldChange&gt;; EntityCache computes the diff'\n ---\n #entity-cache\n","reverse_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffc380\n+position_ordinal: ffffffffffffffffffffffd180\n title: 'entity-cache 1/4: EntityEvent::EntityChanged carries Vec&lt;FieldChange&gt;; EntityCache computes the diff'\n ---\n #entity-cache\n","transaction_id":null}
{"id":"01KQ5NTFMRV9HZ8R7QBJ2488SA","timestamp":"2026-04-26T19:56:18.968795Z","entity_type":"task","entity_id":"01KP65VMEVVNECSK61H6BK32BM","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffd180\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffc380\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffc380\n\\ No newline at end of file\n+ffffffffffffffffffffffd180\n\\ No newline at end of file\n"}]]}
{"id":"01KQZ9BVD3NZ39540S0RG6X1GE","timestamp":"2026-05-06T18:38:51.811106Z","op":"update","item_id":"01KP65VMEVVNECSK61H6BK32BM","forward_patch":"--- original\n+++ modified\n@@ -57,4 +57,4 @@\n\n ## Scope / depends_on\n - No depends_on.\n-- Subsequent sub-cards depend on this: `entity-cache 2/4` (context wiring), `entity-cache 4/4` (kanban-app bridge collapse) both consume the new event shape.\n+- Subsequent sub-cards depend on this: `entity-cache 2/4` (context wiring), `entity-cache 4/4` (kanban-app bridge collapse) both consume the new event shape.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -57,4 +57,4 @@\n\n ## Scope / depends_on\n - No depends_on.\n-- Subsequent sub-cards depend on this: `entity-cache 2/4` (context wiring), `entity-cache 4/4` (kanban-app bridge collapse) both consume the new event shape.\n\\ No newline at end of file\n+- Subsequent sub-cards depend on this: `entity-cache 2/4` (context wiring), `entity-cache 4/4` (kanban-app bridge collapse) both consume the new event shape.\n","transaction_id":null}
{"id":"01KQZ9BVD3A7AEFRAGPXEMCNSG","timestamp":"2026-05-06T18:38:51.811431Z","entity_type":"task","entity_id":"01KP65VMEVVNECSK61H6BK32BM","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -50,4 +50,4 @@\n\n ## Scope / depends_on\n - No depends_on.\n-- Subsequent sub-cards depend on this: `entity-cache 2/4` (context wiring), `entity-cache 4/4` (kanban-app bridge collapse) both consume the new event shape.\n+- Subsequent sub-cards depend on this: `entity-cache 2/4` (context wiring), `entity-cache 4/4` (kanban-app bridge collapse) both consume the new event shape.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -50,4 +50,4 @@\n\n ## Scope / depends_on\n - No depends_on.\n-- Subsequent sub-cards depend on this: `entity-cache 2/4` (context wiring), `entity-cache 4/4` (kanban-app bridge collapse) both consume the new event shape.\n\\ No newline at end of file\n+- Subsequent sub-cards depend on this: `entity-cache 2/4` (context wiring), `entity-cache 4/4` (kanban-app bridge collapse) both consume the new event shape.\n"}]]}

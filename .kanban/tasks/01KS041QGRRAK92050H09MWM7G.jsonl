{"id":"01KS041QGRCJ3Q6WRC66J0J1TA","timestamp":"2026-05-19T12:40:53.528773Z","op":"create","item_id":"01KS041QGRRAK92050H09MWM7G","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,30 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '8280'\n+project: plugin-tsonly\n+title: 'SDK: name and version as Plugin class props'\n+---\n+## What\n+\n+Move a plugin's `name` and `version` out of `plugin.json` and into the `Plugin` base class as plugin-author-set TypeScript properties.\n+\n+- `crates/swissarmyhammer-plugin/src/sdk/plugin.ts` — add `name` and `version` to the `Plugin` abstract base class. They are author-facing metadata a subclass sets (overridable `readonly` fields with sensible defaults, e.g. `name = \"unnamed plugin\"`, `version = \"0.0.0\"`, which a subclass overrides). Document in the class doc comment that they are **descriptive metadata only** — the platform does not use them for identity or discovery (a plugin's identity is its bundle directory name) and the SDK never sends them to the host. Keep the existing `Plugin` surface (`load`/`unload`/`register`/`unregister`/`track`/`log`) intact.\n+- `crates/swissarmyhammer-plugin/src/codegen.rs` — if `TypesEmitter` / the generated `app.d.ts` describes the `Plugin` class surface, regenerate it so `name`/`version` appear. If codegen does not touch the `Plugin` class, no change — confirm and note it.\n+\n+This task is host-Rust-free and SDK-only; it can land independently of the discovery work.\n+\n+## Acceptance Criteria\n+- [ ] `Plugin` exposes `name` and `version`; a subclass can set them; defaults exist so a subclass that omits them still compiles.\n+- [ ] The class doc comment states `name`/`version` are metadata only and are not used for identity/discovery.\n+- [ ] `codegen.rs` / `app.d.ts` reflects the new props, or it is confirmed codegen does not cover the `Plugin` class.\n+\n+## Tests\n+- [ ] Extend `crates/swissarmyhammer-plugin/tests/sdk.rs` (the SDK transpile/dispatch test): a `Plugin` subclass that sets `name`/`version` transpiles cleanly and the properties are readable on the constructed instance.\n+- [ ] If `codegen.rs` has a test asserting `app.d.ts` content, update it to expect `name`/`version`.\n+- [ ] Run `cargo nextest run -p swissarmyhammer-plugin --test sdk` and any `codegen` test — pass.\n+- [ ] `cargo clippy -p swissarmyhammer-plugin --all-targets -- -D warnings` — clean.\n+\n+## Workflow\n+- Use `/tdd` — write the failing SDK test first, then update `plugin.ts`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,30 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '8280'\n-project: plugin-tsonly\n-title: 'SDK: name and version as Plugin class props'\n----\n-## What\n-\n-Move a plugin's `name` and `version` out of `plugin.json` and into the `Plugin` base class as plugin-author-set TypeScript properties.\n-\n-- `crates/swissarmyhammer-plugin/src/sdk/plugin.ts` — add `name` and `version` to the `Plugin` abstract base class. They are author-facing metadata a subclass sets (overridable `readonly` fields with sensible defaults, e.g. `name = \"unnamed plugin\"`, `version = \"0.0.0\"`, which a subclass overrides). Document in the class doc comment that they are **descriptive metadata only** — the platform does not use them for identity or discovery (a plugin's identity is its bundle directory name) and the SDK never sends them to the host. Keep the existing `Plugin` surface (`load`/`unload`/`register`/`unregister`/`track`/`log`) intact.\n-- `crates/swissarmyhammer-plugin/src/codegen.rs` — if `TypesEmitter` / the generated `app.d.ts` describes the `Plugin` class surface, regenerate it so `name`/`version` appear. If codegen does not touch the `Plugin` class, no change — confirm and note it.\n-\n-This task is host-Rust-free and SDK-only; it can land independently of the discovery work.\n-\n-## Acceptance Criteria\n-- [ ] `Plugin` exposes `name` and `version`; a subclass can set them; defaults exist so a subclass that omits them still compiles.\n-- [ ] The class doc comment states `name`/`version` are metadata only and are not used for identity/discovery.\n-- [ ] `codegen.rs` / `app.d.ts` reflects the new props, or it is confirmed codegen does not cover the `Plugin` class.\n-\n-## Tests\n-- [ ] Extend `crates/swissarmyhammer-plugin/tests/sdk.rs` (the SDK transpile/dispatch test): a `Plugin` subclass that sets `name`/`version` transpiles cleanly and the properties are readable on the constructed instance.\n-- [ ] If `codegen.rs` has a test asserting `app.d.ts` content, update it to expect `name`/`version`.\n-- [ ] Run `cargo nextest run -p swissarmyhammer-plugin --test sdk` and any `codegen` test — pass.\n-- [ ] `cargo clippy -p swissarmyhammer-plugin --all-targets -- -D warnings` — clean.\n-\n-## Workflow\n-- Use `/tdd` — write the failing SDK test first, then update `plugin.ts`.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KS084ADWJVP28KMAG8MF7PFF","timestamp":"2026-05-19T13:52:12.732772Z","op":"update","item_id":"01KS041QGRRAK92050H09MWM7G","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '80'\n project: plugin-tsonly\n title: 'SDK: name and version as Plugin class props'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '8280'\n project: plugin-tsonly\n title: 'SDK: name and version as Plugin class props'\n ---\n","transaction_id":null}
{"id":"01KS089HPHBXFET02DC8WSS3M9","timestamp":"2026-05-19T13:55:04.017639Z","op":"update","item_id":"01KS041QGRRAK92050H09MWM7G","forward_patch":"--- original\n+++ modified\n@@ -16,15 +16,19 @@\n This task is host-Rust-free and SDK-only; it can land independently of the discovery work.\n\n ## Acceptance Criteria\n-- [ ] `Plugin` exposes `name` and `version`; a subclass can set them; defaults exist so a subclass that omits them still compiles.\n-- [ ] The class doc comment states `name`/`version` are metadata only and are not used for identity/discovery.\n-- [ ] `codegen.rs` / `app.d.ts` reflects the new props, or it is confirmed codegen does not cover the `Plugin` class.\n+- [x] `Plugin` exposes `name` and `version`; a subclass can set them; defaults exist so a subclass that omits them still compiles.\n+- [x] The class doc comment states `name`/`version` are metadata only and are not used for identity/discovery.\n+- [x] `codegen.rs` / `app.d.ts` reflects the new props, or it is confirmed codegen does not cover the `Plugin` class.\n+\n+## Codegen note\n+\n+Confirmed: `codegen.rs` does NOT cover the SDK `Plugin` class surface, so no codegen change was needed. `render_declaration` emits a `declare global { interface Plugin { readonly [K in keyof App]: App[K]; } }` block — a TypeScript *interface* named `Plugin` that types the dynamic per-server dispatcher index (`this.<server>...`). It is a distinct declaration from the SDK's `Plugin` *abstract class* in `plugin.ts`; codegen never describes the class's static members (`load`/`unload`/`register`/`name`/`version`), only the dynamic server proxy. The `name`/`version` fields therefore live entirely in `plugin.ts` and the codegen output and its tests are unaffected.\n\n ## Tests\n-- [ ] Extend `crates/swissarmyhammer-plugin/tests/sdk.rs` (the SDK transpile/dispatch test): a `Plugin` subclass that sets `name`/`version` transpiles cleanly and the properties are readable on the constructed instance.\n-- [ ] If `codegen.rs` has a test asserting `app.d.ts` content, update it to expect `name`/`version`.\n-- [ ] Run `cargo nextest run -p swissarmyhammer-plugin --test sdk` and any `codegen` test — pass.\n-- [ ] `cargo clippy -p swissarmyhammer-plugin --all-targets -- -D warnings` — clean.\n+- [x] Extend `crates/swissarmyhammer-plugin/tests/sdk.rs` (the SDK transpile/dispatch test): a `Plugin` subclass that sets `name`/`version` transpiles cleanly and the properties are readable on the constructed instance.\n+- [x] If `codegen.rs` has a test asserting `app.d.ts` content, update it to expect `name`/`version`. — N/A: codegen does not cover the `Plugin` class (see note above); codegen tests unchanged.\n+- [x] Run `cargo nextest run -p swissarmyhammer-plugin --test sdk` and any `codegen` test — pass. (7 sdk + 6 codegen tests pass)\n+- [x] `cargo clippy -p swissarmyhammer-plugin --all-targets -- -D warnings` — clean.\n\n ## Workflow\n - Use `/tdd` — write the failing SDK test first, then update `plugin.ts`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -16,19 +16,15 @@\n This task is host-Rust-free and SDK-only; it can land independently of the discovery work.\n\n ## Acceptance Criteria\n-- [x] `Plugin` exposes `name` and `version`; a subclass can set them; defaults exist so a subclass that omits them still compiles.\n-- [x] The class doc comment states `name`/`version` are metadata only and are not used for identity/discovery.\n-- [x] `codegen.rs` / `app.d.ts` reflects the new props, or it is confirmed codegen does not cover the `Plugin` class.\n-\n-## Codegen note\n-\n-Confirmed: `codegen.rs` does NOT cover the SDK `Plugin` class surface, so no codegen change was needed. `render_declaration` emits a `declare global { interface Plugin { readonly [K in keyof App]: App[K]; } }` block — a TypeScript *interface* named `Plugin` that types the dynamic per-server dispatcher index (`this.<server>...`). It is a distinct declaration from the SDK's `Plugin` *abstract class* in `plugin.ts`; codegen never describes the class's static members (`load`/`unload`/`register`/`name`/`version`), only the dynamic server proxy. The `name`/`version` fields therefore live entirely in `plugin.ts` and the codegen output and its tests are unaffected.\n+- [ ] `Plugin` exposes `name` and `version`; a subclass can set them; defaults exist so a subclass that omits them still compiles.\n+- [ ] The class doc comment states `name`/`version` are metadata only and are not used for identity/discovery.\n+- [ ] `codegen.rs` / `app.d.ts` reflects the new props, or it is confirmed codegen does not cover the `Plugin` class.\n\n ## Tests\n-- [x] Extend `crates/swissarmyhammer-plugin/tests/sdk.rs` (the SDK transpile/dispatch test): a `Plugin` subclass that sets `name`/`version` transpiles cleanly and the properties are readable on the constructed instance.\n-- [x] If `codegen.rs` has a test asserting `app.d.ts` content, update it to expect `name`/`version`. — N/A: codegen does not cover the `Plugin` class (see note above); codegen tests unchanged.\n-- [x] Run `cargo nextest run -p swissarmyhammer-plugin --test sdk` and any `codegen` test — pass. (7 sdk + 6 codegen tests pass)\n-- [x] `cargo clippy -p swissarmyhammer-plugin --all-targets -- -D warnings` — clean.\n+- [ ] Extend `crates/swissarmyhammer-plugin/tests/sdk.rs` (the SDK transpile/dispatch test): a `Plugin` subclass that sets `name`/`version` transpiles cleanly and the properties are readable on the constructed instance.\n+- [ ] If `codegen.rs` has a test asserting `app.d.ts` content, update it to expect `name`/`version`.\n+- [ ] Run `cargo nextest run -p swissarmyhammer-plugin --test sdk` and any `codegen` test — pass.\n+- [ ] `cargo clippy -p swissarmyhammer-plugin --all-targets -- -D warnings` — clean.\n\n ## Workflow\n - Use `/tdd` — write the failing SDK test first, then update `plugin.ts`.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KS089P263EP14F9BG647DEAZ","timestamp":"2026-05-19T13:55:08.486206Z","op":"update","item_id":"01KS041QGRRAK92050H09MWM7G","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: plugin-tsonly\n title: 'SDK: name and version as Plugin class props'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: plugin-tsonly\n title: 'SDK: name and version as Plugin class props'\n","transaction_id":null}
{"id":"01KS08GDZV116HTTJN695F78XQ","timestamp":"2026-05-19T13:58:49.595140Z","op":"update","item_id":"01KS041QGRRAK92050H09MWM7G","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffff9180\n project: plugin-tsonly\n title: 'SDK: name and version as Plugin class props'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffff9180\n+position_column: review\n+position_ordinal: '80'\n project: plugin-tsonly\n title: 'SDK: name and version as Plugin class props'\n ---\n","transaction_id":null}
{"id":"01KS08GDZWWD0JSDCK1E2MX09W","timestamp":"2026-05-19T13:58:49.596434Z","entity_type":"task","entity_id":"01KS041QGRRAK92050H09MWM7G","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffffff9180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffffff9180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}

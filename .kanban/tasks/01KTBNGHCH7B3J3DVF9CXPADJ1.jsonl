{"id":"01KTBNGHCH3NBW0BQDFBX13FDJ","timestamp":"2026-06-05T10:33:25.137447Z","op":"create","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,36 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KTBN8CQDYD52W5MGG4ZYKNN7\n+- 01KTBNFB7NPXNWKDK86T9A0M5C\n+position_column: todo\n+position_ordinal: '8680'\n+project: local-review\n+title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n+---\n+## What\n+Remove the hook-triggered execution path from the engine crate (still named `avp-common` at this point; rename happens next). This is the slowness the user never uses — an LLM agent spawned per validator, per tool call, via PreToolUse/PostToolUse/Stop hooks. KEEP the two genuinely reusable pieces for the new review fleet.\n+\n+Remove:\n+- The chain/hook links that dispatch validators on hook events — `crates/avp-common/src/chain/links/validator_executor.rs` and the surrounding chain wiring that maps `HookType` (PreToolUse/PostToolUse/Stop) to validator runs.\n+- The hook-context plumbing: `MatchContext` hook-type fields, the turn-diff sidecar reading/writing (`.avp/turn_diffs/...`), and any `HookType`-driven branching that only exists to serve hooks.\n+- Stdin/stdout hook-protocol handling (exit-code-2 blocking, JSON decision blocks) that lived behind the deleted `avp` binary.\n+\n+KEEP (these move into the engine, reused by the review fleet):\n+- The rules-as-data loader (`loader.rs`, `types.rs`) — file/glob matching, precedence stacking, `@`-include expansion.\n+- The bare ACP agent-execution primitive from `runner.rs`: parallel execution with adaptive (AIMD) concurrency and the per-call token cap. Extract it as a hook-free `execute_agents(prompts, connection) -> results` surface. Drop the validator/hook-specific wrapping; keep the concurrency engine.\n+\n+## Acceptance Criteria\n+- [ ] No code path in the engine crate references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n+- [ ] The loader and a hook-free `execute_agents`-style primitive remain, compile, and are unit-tested.\n+- [ ] All consumers of avp-common that used the hook path (e.g. `apps/swissarmyhammer-cli`) are updated or have the now-dead surface removed; `cargo build --workspace` green.\n+- [ ] No real-time validation fires on tool calls anywhere (the hook system is gone, as intended).\n+\n+## Tests\n+- [ ] Retain/adapt the loader tests (precedence, glob match) — still green.\n+- [ ] New unit test for the extracted `execute_agents` primitive using the existing `PlaybackAgent`/mock-agent harness (`runner.rs` doc-comment lists `SessionRecordingAgent`/`MaxTokensAgent`/`SlowAgent`): N prompts → N results, concurrency capped, one slow/erroring agent doesn't deadlock the batch.\n+- [ ] `cargo test -p avp-common` and `cargo build --workspace` green.\n+\n+## Workflow\n+- Use `/tdd` for the extracted executor (write the batch-execution test against the mock agents first, then carve the primitive out of `runner.rs`). Delete dead hook code; do not leave commented-out husks. Depends on avp-cli removal (the binary entry point is already gone).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,36 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KTBN8CQDYD52W5MGG4ZYKNN7\n-- 01KTBNFB7NPXNWKDK86T9A0M5C\n-position_column: todo\n-position_ordinal: '8680'\n-project: local-review\n-title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n----\n-## What\n-Remove the hook-triggered execution path from the engine crate (still named `avp-common` at this point; rename happens next). This is the slowness the user never uses — an LLM agent spawned per validator, per tool call, via PreToolUse/PostToolUse/Stop hooks. KEEP the two genuinely reusable pieces for the new review fleet.\n-\n-Remove:\n-- The chain/hook links that dispatch validators on hook events — `crates/avp-common/src/chain/links/validator_executor.rs` and the surrounding chain wiring that maps `HookType` (PreToolUse/PostToolUse/Stop) to validator runs.\n-- The hook-context plumbing: `MatchContext` hook-type fields, the turn-diff sidecar reading/writing (`.avp/turn_diffs/...`), and any `HookType`-driven branching that only exists to serve hooks.\n-- Stdin/stdout hook-protocol handling (exit-code-2 blocking, JSON decision blocks) that lived behind the deleted `avp` binary.\n-\n-KEEP (these move into the engine, reused by the review fleet):\n-- The rules-as-data loader (`loader.rs`, `types.rs`) — file/glob matching, precedence stacking, `@`-include expansion.\n-- The bare ACP agent-execution primitive from `runner.rs`: parallel execution with adaptive (AIMD) concurrency and the per-call token cap. Extract it as a hook-free `execute_agents(prompts, connection) -> results` surface. Drop the validator/hook-specific wrapping; keep the concurrency engine.\n-\n-## Acceptance Criteria\n-- [ ] No code path in the engine crate references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n-- [ ] The loader and a hook-free `execute_agents`-style primitive remain, compile, and are unit-tested.\n-- [ ] All consumers of avp-common that used the hook path (e.g. `apps/swissarmyhammer-cli`) are updated or have the now-dead surface removed; `cargo build --workspace` green.\n-- [ ] No real-time validation fires on tool calls anywhere (the hook system is gone, as intended).\n-\n-## Tests\n-- [ ] Retain/adapt the loader tests (precedence, glob match) — still green.\n-- [ ] New unit test for the extracted `execute_agents` primitive using the existing `PlaybackAgent`/mock-agent harness (`runner.rs` doc-comment lists `SessionRecordingAgent`/`MaxTokensAgent`/`SlowAgent`): N prompts → N results, concurrency capped, one slow/erroring agent doesn't deadlock the batch.\n-- [ ] `cargo test -p avp-common` and `cargo build --workspace` green.\n-\n-## Workflow\n-- Use `/tdd` for the extracted executor (write the batch-execution test against the mock agents first, then carve the primitive out of `runner.rs`). Delete dead hook code; do not leave commented-out husks. Depends on avp-cli removal (the binary entry point is already gone).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTBS24QTKV9Y8FTQ2E8N9TSH","timestamp":"2026-06-05T11:35:27.738464Z","op":"update","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,6 @@\n assignees:\n - claude-code\n depends_on:\n-- 01KTBN8CQDYD52W5MGG4ZYKNN7\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n position_column: todo\n position_ordinal: '8680'\n","reverse_patch":"--- original\n+++ modified\n@@ -2,6 +2,7 @@\n assignees:\n - claude-code\n depends_on:\n+- 01KTBN8CQDYD52W5MGG4ZYKNN7\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n position_column: todo\n position_ordinal: '8680'\n","transaction_id":null}
{"id":"01KTBWYNA8F1ESBTMQP9WXM1JR","timestamp":"2026-06-05T12:43:27.944873Z","op":"update","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -18,18 +18,23 @@\n\n KEEP (these move into the engine, reused by the review fleet):\n - The rules-as-data loader (`loader.rs`, `types.rs`) — file/glob matching, precedence stacking, `@`-include expansion.\n-- The bare ACP agent-execution primitive from `runner.rs`: parallel execution with adaptive (AIMD) concurrency and the per-call token cap. Extract it as a hook-free `execute_agents(prompts, connection) -> results` surface. Drop the validator/hook-specific wrapping; keep the concurrency engine.\n+- The bare ACP agent-execution primitive from `runner.rs`: extract it as a hook-free `execute_agents(prompts, connection, policy) -> results` surface. Drop the validator/hook-specific wrapping; keep the concurrency engine. This is the SINGLE place parallelism is controlled for the whole review pipeline — fan-out and verify both route through it.\n+  - **Concurrency is configurable and backend-aware**, not the hardcoded `MAX_CONCURRENCY=8`:\n+    - remote/Claude-API backend → cap N (default ~8) with the existing AIMD adaptive backoff (halve on rate-limit/timeout, recover after K successes).\n+    - local Llama backend → cap **1** (one in-process model/GPU; oversubscription thrashes — real batching is the model's own, not ours).\n+    - a `review.concurrency` config override pins the cap explicitly when set.\n+  - Keep the per-call token cap.\n\n ## Acceptance Criteria\n - [ ] No code path in the engine crate references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n-- [ ] The loader and a hook-free `execute_agents`-style primitive remain, compile, and are unit-tested.\n+- [ ] `execute_agents` exists, hook-free, with a configurable + backend-aware concurrency policy (remote→AIMD cap N, local→1, config override) and the per-call token cap.\n - [ ] All consumers of avp-common that used the hook path (e.g. `apps/swissarmyhammer-cli`) are updated or have the now-dead surface removed; `cargo build --workspace` green.\n - [ ] No real-time validation fires on tool calls anywhere (the hook system is gone, as intended).\n\n ## Tests\n - [ ] Retain/adapt the loader tests (precedence, glob match) — still green.\n-- [ ] New unit test for the extracted `execute_agents` primitive using the existing `PlaybackAgent`/mock-agent harness (`runner.rs` doc-comment lists `SessionRecordingAgent`/`MaxTokensAgent`/`SlowAgent`): N prompts → N results, concurrency capped, one slow/erroring agent doesn't deadlock the batch.\n+- [ ] `execute_agents` test (mock-agent harness `PlaybackAgent`/`SessionRecordingAgent`/`SlowAgent`): N prompts → N results in order; concurrency never exceeds the configured cap; a local-backend policy caps at 1; one slow/erroring agent doesn't deadlock the batch.\n - [ ] `cargo test -p avp-common` and `cargo build --workspace` green.\n\n ## Workflow\n-- Use `/tdd` for the extracted executor (write the batch-execution test against the mock agents first, then carve the primitive out of `runner.rs`). Delete dead hook code; do not leave commented-out husks. Depends on avp-cli removal (the binary entry point is already gone).\n\\ No newline at end of file\n+- Use `/tdd` for the extracted executor (write the batch-execution + concurrency-cap tests against the mock agents first, then carve the primitive out of `runner.rs`). Delete dead hook code; do not leave commented-out husks. Depends on avp-cli removal (the binary entry point is already gone).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -18,23 +18,18 @@\n\n KEEP (these move into the engine, reused by the review fleet):\n - The rules-as-data loader (`loader.rs`, `types.rs`) — file/glob matching, precedence stacking, `@`-include expansion.\n-- The bare ACP agent-execution primitive from `runner.rs`: extract it as a hook-free `execute_agents(prompts, connection, policy) -> results` surface. Drop the validator/hook-specific wrapping; keep the concurrency engine. This is the SINGLE place parallelism is controlled for the whole review pipeline — fan-out and verify both route through it.\n-  - **Concurrency is configurable and backend-aware**, not the hardcoded `MAX_CONCURRENCY=8`:\n-    - remote/Claude-API backend → cap N (default ~8) with the existing AIMD adaptive backoff (halve on rate-limit/timeout, recover after K successes).\n-    - local Llama backend → cap **1** (one in-process model/GPU; oversubscription thrashes — real batching is the model's own, not ours).\n-    - a `review.concurrency` config override pins the cap explicitly when set.\n-  - Keep the per-call token cap.\n+- The bare ACP agent-execution primitive from `runner.rs`: parallel execution with adaptive (AIMD) concurrency and the per-call token cap. Extract it as a hook-free `execute_agents(prompts, connection) -> results` surface. Drop the validator/hook-specific wrapping; keep the concurrency engine.\n\n ## Acceptance Criteria\n - [ ] No code path in the engine crate references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n-- [ ] `execute_agents` exists, hook-free, with a configurable + backend-aware concurrency policy (remote→AIMD cap N, local→1, config override) and the per-call token cap.\n+- [ ] The loader and a hook-free `execute_agents`-style primitive remain, compile, and are unit-tested.\n - [ ] All consumers of avp-common that used the hook path (e.g. `apps/swissarmyhammer-cli`) are updated or have the now-dead surface removed; `cargo build --workspace` green.\n - [ ] No real-time validation fires on tool calls anywhere (the hook system is gone, as intended).\n\n ## Tests\n - [ ] Retain/adapt the loader tests (precedence, glob match) — still green.\n-- [ ] `execute_agents` test (mock-agent harness `PlaybackAgent`/`SessionRecordingAgent`/`SlowAgent`): N prompts → N results in order; concurrency never exceeds the configured cap; a local-backend policy caps at 1; one slow/erroring agent doesn't deadlock the batch.\n+- [ ] New unit test for the extracted `execute_agents` primitive using the existing `PlaybackAgent`/mock-agent harness (`runner.rs` doc-comment lists `SessionRecordingAgent`/`MaxTokensAgent`/`SlowAgent`): N prompts → N results, concurrency capped, one slow/erroring agent doesn't deadlock the batch.\n - [ ] `cargo test -p avp-common` and `cargo build --workspace` green.\n\n ## Workflow\n-- Use `/tdd` for the extracted executor (write the batch-execution + concurrency-cap tests against the mock agents first, then carve the primitive out of `runner.rs`). Delete dead hook code; do not leave commented-out husks. Depends on avp-cli removal (the binary entry point is already gone).\n\\ No newline at end of file\n+- Use `/tdd` for the extracted executor (write the batch-execution test against the mock agents first, then carve the primitive out of `runner.rs`). Delete dead hook code; do not leave commented-out husks. Depends on avp-cli removal (the binary entry point is already gone).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTBX3N73SEBP0GC743D42YPG","timestamp":"2026-06-05T12:46:11.683717Z","op":"update","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -18,23 +18,23 @@\n\n KEEP (these move into the engine, reused by the review fleet):\n - The rules-as-data loader (`loader.rs`, `types.rs`) — file/glob matching, precedence stacking, `@`-include expansion.\n-- The bare ACP agent-execution primitive from `runner.rs`: extract it as a hook-free `execute_agents(prompts, connection, policy) -> results` surface. Drop the validator/hook-specific wrapping; keep the concurrency engine. This is the SINGLE place parallelism is controlled for the whole review pipeline — fan-out and verify both route through it.\n-  - **Concurrency is configurable and backend-aware**, not the hardcoded `MAX_CONCURRENCY=8`:\n-    - remote/Claude-API backend → cap N (default ~8) with the existing AIMD adaptive backoff (halve on rate-limit/timeout, recover after K successes).\n-    - local Llama backend → cap **1** (one in-process model/GPU; oversubscription thrashes — real batching is the model's own, not ours).\n-    - a `review.concurrency` config override pins the cap explicitly when set.\n-  - Keep the per-call token cap.\n+- A hook-free **shared bounded agent pool** carved out of `runner.rs`'s concurrency engine — NOT a one-shot batch executor. Shape: `AgentPool` with `submit(prompt) -> Future<Result>` (or a channel), drained by a fixed set of workers over a single internal queue. This is the ONE place parallelism is controlled for the whole review pipeline; fan-out and verify both submit to the same pool, so verify tasks pipeline alongside still-running fan-out tasks.\n+  - **Worker count is the only concurrency control, and it is legitimate (physical/discovered, not arbitrary):**\n+    - local Llama backend → **1 worker** (one in-process model/GPU).\n+    - remote/Claude-API backend → N workers, default from config, optionally AIMD-adjusted (reduce active workers on rate-limit/timeout, recover after K successes) to discover the API ceiling.\n+    - a `review.concurrency` config value pins N when set.\n+  - Submission is unbounded and non-blocking (the queue absorbs it); only N run at once. Keep the per-call token cap.\n\n ## Acceptance Criteria\n - [ ] No code path in the engine crate references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n-- [ ] `execute_agents` exists, hook-free, with a configurable + backend-aware concurrency policy (remote→AIMD cap N, local→1, config override) and the per-call token cap.\n+- [ ] A hook-free `AgentPool` exists: callers submit tasks at any time; a fixed worker count drains one shared queue; worker count is backend-aware (local→1, remote→N/AIMD, `review.concurrency` override); per-call token cap retained.\n - [ ] All consumers of avp-common that used the hook path (e.g. `apps/swissarmyhammer-cli`) are updated or have the now-dead surface removed; `cargo build --workspace` green.\n - [ ] No real-time validation fires on tool calls anywhere (the hook system is gone, as intended).\n\n ## Tests\n - [ ] Retain/adapt the loader tests (precedence, glob match) — still green.\n-- [ ] `execute_agents` test (mock-agent harness `PlaybackAgent`/`SessionRecordingAgent`/`SlowAgent`): N prompts → N results in order; concurrency never exceeds the configured cap; a local-backend policy caps at 1; one slow/erroring agent doesn't deadlock the batch.\n+- [ ] `AgentPool` test (mock-agent harness `PlaybackAgent`/`SessionRecordingAgent`/`SlowAgent`): submit M tasks to a pool of N workers → all M results returned, never more than N agents in flight at once; tasks submitted mid-drain (pipelining) are picked up; a local-backend policy runs strictly 1 at a time; one slow/erroring task doesn't deadlock the pool.\n - [ ] `cargo test -p avp-common` and `cargo build --workspace` green.\n\n ## Workflow\n-- Use `/tdd` for the extracted executor (write the batch-execution + concurrency-cap tests against the mock agents first, then carve the primitive out of `runner.rs`). Delete dead hook code; do not leave commented-out husks. Depends on avp-cli removal (the binary entry point is already gone).\n\\ No newline at end of file\n+- Use `/tdd` — write the pool/concurrency/pipelining tests against the mock agents first, then carve the pool out of `runner.rs`. Delete dead hook code; do not leave commented-out husks. Depends on avp-cli removal (the binary entry point is already gone).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -18,23 +18,23 @@\n\n KEEP (these move into the engine, reused by the review fleet):\n - The rules-as-data loader (`loader.rs`, `types.rs`) — file/glob matching, precedence stacking, `@`-include expansion.\n-- A hook-free **shared bounded agent pool** carved out of `runner.rs`'s concurrency engine — NOT a one-shot batch executor. Shape: `AgentPool` with `submit(prompt) -> Future<Result>` (or a channel), drained by a fixed set of workers over a single internal queue. This is the ONE place parallelism is controlled for the whole review pipeline; fan-out and verify both submit to the same pool, so verify tasks pipeline alongside still-running fan-out tasks.\n-  - **Worker count is the only concurrency control, and it is legitimate (physical/discovered, not arbitrary):**\n-    - local Llama backend → **1 worker** (one in-process model/GPU).\n-    - remote/Claude-API backend → N workers, default from config, optionally AIMD-adjusted (reduce active workers on rate-limit/timeout, recover after K successes) to discover the API ceiling.\n-    - a `review.concurrency` config value pins N when set.\n-  - Submission is unbounded and non-blocking (the queue absorbs it); only N run at once. Keep the per-call token cap.\n+- The bare ACP agent-execution primitive from `runner.rs`: extract it as a hook-free `execute_agents(prompts, connection, policy) -> results` surface. Drop the validator/hook-specific wrapping; keep the concurrency engine. This is the SINGLE place parallelism is controlled for the whole review pipeline — fan-out and verify both route through it.\n+  - **Concurrency is configurable and backend-aware**, not the hardcoded `MAX_CONCURRENCY=8`:\n+    - remote/Claude-API backend → cap N (default ~8) with the existing AIMD adaptive backoff (halve on rate-limit/timeout, recover after K successes).\n+    - local Llama backend → cap **1** (one in-process model/GPU; oversubscription thrashes — real batching is the model's own, not ours).\n+    - a `review.concurrency` config override pins the cap explicitly when set.\n+  - Keep the per-call token cap.\n\n ## Acceptance Criteria\n - [ ] No code path in the engine crate references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n-- [ ] A hook-free `AgentPool` exists: callers submit tasks at any time; a fixed worker count drains one shared queue; worker count is backend-aware (local→1, remote→N/AIMD, `review.concurrency` override); per-call token cap retained.\n+- [ ] `execute_agents` exists, hook-free, with a configurable + backend-aware concurrency policy (remote→AIMD cap N, local→1, config override) and the per-call token cap.\n - [ ] All consumers of avp-common that used the hook path (e.g. `apps/swissarmyhammer-cli`) are updated or have the now-dead surface removed; `cargo build --workspace` green.\n - [ ] No real-time validation fires on tool calls anywhere (the hook system is gone, as intended).\n\n ## Tests\n - [ ] Retain/adapt the loader tests (precedence, glob match) — still green.\n-- [ ] `AgentPool` test (mock-agent harness `PlaybackAgent`/`SessionRecordingAgent`/`SlowAgent`): submit M tasks to a pool of N workers → all M results returned, never more than N agents in flight at once; tasks submitted mid-drain (pipelining) are picked up; a local-backend policy runs strictly 1 at a time; one slow/erroring task doesn't deadlock the pool.\n+- [ ] `execute_agents` test (mock-agent harness `PlaybackAgent`/`SessionRecordingAgent`/`SlowAgent`): N prompts → N results in order; concurrency never exceeds the configured cap; a local-backend policy caps at 1; one slow/erroring agent doesn't deadlock the batch.\n - [ ] `cargo test -p avp-common` and `cargo build --workspace` green.\n\n ## Workflow\n-- Use `/tdd` — write the pool/concurrency/pipelining tests against the mock agents first, then carve the pool out of `runner.rs`. Delete dead hook code; do not leave commented-out husks. Depends on avp-cli removal (the binary entry point is already gone).\n\\ No newline at end of file\n+- Use `/tdd` for the extracted executor (write the batch-execution + concurrency-cap tests against the mock agents first, then carve the primitive out of `runner.rs`). Delete dead hook code; do not leave commented-out husks. Depends on avp-cli removal (the binary entry point is already gone).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTC3KP7N7RF5GTRAVJ63JJZS","timestamp":"2026-06-05T14:39:48.469280Z","op":"update","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n-position_column: todo\n-position_ordinal: '8680'\n+position_column: doing\n+position_ordinal: '80'\n project: local-review\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '8680'\n project: local-review\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n ---\n","transaction_id":null}
{"id":"01KTC3YXQ8C0GBMXN8ZDW9ET4E","timestamp":"2026-06-05T14:45:56.584606Z","op":"update","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -9,32 +9,22 @@\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n ---\n ## What\n-Remove the hook-triggered execution path from the engine crate (still named `avp-common` at this point; rename happens next). This is the slowness the user never uses — an LLM agent spawned per validator, per tool call, via PreToolUse/PostToolUse/Stop hooks. KEEP the two genuinely reusable pieces for the new review fleet.\n-\n-Remove:\n-- The chain/hook links that dispatch validators on hook events — `crates/avp-common/src/chain/links/validator_executor.rs` and the surrounding chain wiring that maps `HookType` (PreToolUse/PostToolUse/Stop) to validator runs.\n-- The hook-context plumbing: `MatchContext` hook-type fields, the turn-diff sidecar reading/writing (`.avp/turn_diffs/...`), and any `HookType`-driven branching that only exists to serve hooks.\n-- Stdin/stdout hook-protocol handling (exit-code-2 blocking, JSON decision blocks) that lived behind the deleted `avp` binary.\n+Remove the hook-triggered execution path from the engine crate (`avp-common`). KEEP the two genuinely reusable pieces for the new review fleet: the rules-as-data loader (loader.rs/types.rs/parser.rs, hook-free) and a new hook-free shared bounded `AgentPool`.\n\n-KEEP (these move into the engine, reused by the review fleet):\n-- The rules-as-data loader (`loader.rs`, `types.rs`) — file/glob matching, precedence stacking, `@`-include expansion.\n-- A hook-free **shared bounded agent pool** carved out of `runner.rs`'s concurrency engine — NOT a one-shot batch executor. Shape: `AgentPool` with `submit(prompt) -> Future<Result>` (or a channel), drained by a fixed set of workers over a single internal queue. This is the ONE place parallelism is controlled for the whole review pipeline; fan-out and verify both submit to the same pool, so verify tasks pipeline alongside still-running fan-out tasks.\n-  - **Worker count is the only concurrency control, and it is legitimate (physical/discovered, not arbitrary):**\n-    - local Llama backend → **1 worker** (one in-process model/GPU).\n-    - remote/Claude-API backend → N workers, default from config, optionally AIMD-adjusted (reduce active workers on rate-limit/timeout, recover after K successes) to discover the API ceiling.\n-    - a `review.concurrency` config value pins N when set.\n-  - Submission is unbounded and non-blocking (the queue absorbs it); only N run at once. Keep the per-call token cap.\n+## Execution Plan (inline teardown)\n+- [ ] Phase 1 TDD: new `validator/pool.rs` — `AgentPool` with `submit(prompt) -> Future<Result>`, fixed worker count draining one shared queue, backend-aware worker count (local=1, remote=N/AIMD, `review.concurrency` override), per-call token cap. Tests vs PlaybackAgent/SessionRecordingAgent/SlowAgent: M tasks/N workers, never >N in flight, pipelining mid-drain, local=1 strictly serial, slow/erroring task no deadlock. RED then GREEN.\n+- [ ] Phase 2: delete hook machinery — chain/, strategy/, turn/, hooks/, types/{input,output,avp_output,common} hook bits, context.rs, install.rs, lockfile.rs(if hook-only). Strip HookType/MatchContext.hook_type/trigger from types.rs, loader.rs, parser.rs, builtin/mod.rs. Replace hook-coupled runner.rs with pool + kept mock harness.\n+- [ ] Phase 3: delete hook integration tests; adapt loader/types tests (precedence, glob match) to hook-free shape.\n+- [ ] Phase 4: fix `agent-client-protocol-extras` dev-dep — remove avp_schema_tests.rs + mod line + avp_test_context helper + avp-common dev-dependency.\n+- [ ] Phase 5: `cargo test -p avp-common` AND `cargo build --workspace` green.\n\n ## Acceptance Criteria\n - [ ] No code path in the engine crate references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n-- [ ] A hook-free `AgentPool` exists: callers submit tasks at any time; a fixed worker count drains one shared queue; worker count is backend-aware (local→1, remote→N/AIMD, `review.concurrency` override); per-call token cap retained.\n-- [ ] All consumers of avp-common that used the hook path (e.g. `apps/swissarmyhammer-cli`) are updated or have the now-dead surface removed; `cargo build --workspace` green.\n-- [ ] No real-time validation fires on tool calls anywhere (the hook system is gone, as intended).\n+- [ ] A hook-free `AgentPool` exists: callers submit tasks at any time; a fixed worker count drains one shared queue; worker count is backend-aware; per-call token cap retained.\n+- [ ] All consumers updated / dead surface removed; `cargo build --workspace` green.\n+- [ ] No real-time validation fires on tool calls anywhere.\n\n ## Tests\n - [ ] Retain/adapt the loader tests (precedence, glob match) — still green.\n-- [ ] `AgentPool` test (mock-agent harness `PlaybackAgent`/`SessionRecordingAgent`/`SlowAgent`): submit M tasks to a pool of N workers → all M results returned, never more than N agents in flight at once; tasks submitted mid-drain (pipelining) are picked up; a local-backend policy runs strictly 1 at a time; one slow/erroring task doesn't deadlock the pool.\n-- [ ] `cargo test -p avp-common` and `cargo build --workspace` green.\n-\n-## Workflow\n-- Use `/tdd` — write the pool/concurrency/pipelining tests against the mock agents first, then carve the pool out of `runner.rs`. Delete dead hook code; do not leave commented-out husks. Depends on avp-cli removal (the binary entry point is already gone).\n\\ No newline at end of file\n+- [ ] AgentPool test (mock-agent harness): submit M tasks to N workers → all M results, never >N in flight; pipelining picked up; local-backend=1 strictly serial; one slow/erroring task no deadlock.\n+- [ ] `cargo test -p avp-common` and `cargo build --workspace` green.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -9,22 +9,32 @@\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n ---\n ## What\n-Remove the hook-triggered execution path from the engine crate (`avp-common`). KEEP the two genuinely reusable pieces for the new review fleet: the rules-as-data loader (loader.rs/types.rs/parser.rs, hook-free) and a new hook-free shared bounded `AgentPool`.\n+Remove the hook-triggered execution path from the engine crate (still named `avp-common` at this point; rename happens next). This is the slowness the user never uses — an LLM agent spawned per validator, per tool call, via PreToolUse/PostToolUse/Stop hooks. KEEP the two genuinely reusable pieces for the new review fleet.\n\n-## Execution Plan (inline teardown)\n-- [ ] Phase 1 TDD: new `validator/pool.rs` — `AgentPool` with `submit(prompt) -> Future<Result>`, fixed worker count draining one shared queue, backend-aware worker count (local=1, remote=N/AIMD, `review.concurrency` override), per-call token cap. Tests vs PlaybackAgent/SessionRecordingAgent/SlowAgent: M tasks/N workers, never >N in flight, pipelining mid-drain, local=1 strictly serial, slow/erroring task no deadlock. RED then GREEN.\n-- [ ] Phase 2: delete hook machinery — chain/, strategy/, turn/, hooks/, types/{input,output,avp_output,common} hook bits, context.rs, install.rs, lockfile.rs(if hook-only). Strip HookType/MatchContext.hook_type/trigger from types.rs, loader.rs, parser.rs, builtin/mod.rs. Replace hook-coupled runner.rs with pool + kept mock harness.\n-- [ ] Phase 3: delete hook integration tests; adapt loader/types tests (precedence, glob match) to hook-free shape.\n-- [ ] Phase 4: fix `agent-client-protocol-extras` dev-dep — remove avp_schema_tests.rs + mod line + avp_test_context helper + avp-common dev-dependency.\n-- [ ] Phase 5: `cargo test -p avp-common` AND `cargo build --workspace` green.\n+Remove:\n+- The chain/hook links that dispatch validators on hook events — `crates/avp-common/src/chain/links/validator_executor.rs` and the surrounding chain wiring that maps `HookType` (PreToolUse/PostToolUse/Stop) to validator runs.\n+- The hook-context plumbing: `MatchContext` hook-type fields, the turn-diff sidecar reading/writing (`.avp/turn_diffs/...`), and any `HookType`-driven branching that only exists to serve hooks.\n+- Stdin/stdout hook-protocol handling (exit-code-2 blocking, JSON decision blocks) that lived behind the deleted `avp` binary.\n+\n+KEEP (these move into the engine, reused by the review fleet):\n+- The rules-as-data loader (`loader.rs`, `types.rs`) — file/glob matching, precedence stacking, `@`-include expansion.\n+- A hook-free **shared bounded agent pool** carved out of `runner.rs`'s concurrency engine — NOT a one-shot batch executor. Shape: `AgentPool` with `submit(prompt) -> Future<Result>` (or a channel), drained by a fixed set of workers over a single internal queue. This is the ONE place parallelism is controlled for the whole review pipeline; fan-out and verify both submit to the same pool, so verify tasks pipeline alongside still-running fan-out tasks.\n+  - **Worker count is the only concurrency control, and it is legitimate (physical/discovered, not arbitrary):**\n+    - local Llama backend → **1 worker** (one in-process model/GPU).\n+    - remote/Claude-API backend → N workers, default from config, optionally AIMD-adjusted (reduce active workers on rate-limit/timeout, recover after K successes) to discover the API ceiling.\n+    - a `review.concurrency` config value pins N when set.\n+  - Submission is unbounded and non-blocking (the queue absorbs it); only N run at once. Keep the per-call token cap.\n\n ## Acceptance Criteria\n - [ ] No code path in the engine crate references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n-- [ ] A hook-free `AgentPool` exists: callers submit tasks at any time; a fixed worker count drains one shared queue; worker count is backend-aware; per-call token cap retained.\n-- [ ] All consumers updated / dead surface removed; `cargo build --workspace` green.\n-- [ ] No real-time validation fires on tool calls anywhere.\n+- [ ] A hook-free `AgentPool` exists: callers submit tasks at any time; a fixed worker count drains one shared queue; worker count is backend-aware (local→1, remote→N/AIMD, `review.concurrency` override); per-call token cap retained.\n+- [ ] All consumers of avp-common that used the hook path (e.g. `apps/swissarmyhammer-cli`) are updated or have the now-dead surface removed; `cargo build --workspace` green.\n+- [ ] No real-time validation fires on tool calls anywhere (the hook system is gone, as intended).\n\n ## Tests\n - [ ] Retain/adapt the loader tests (precedence, glob match) — still green.\n-- [ ] AgentPool test (mock-agent harness): submit M tasks to N workers → all M results, never >N in flight; pipelining picked up; local-backend=1 strictly serial; one slow/erroring task no deadlock.\n-- [ ] `cargo test -p avp-common` and `cargo build --workspace` green.\n\\ No newline at end of file\n+- [ ] `AgentPool` test (mock-agent harness `PlaybackAgent`/`SessionRecordingAgent`/`SlowAgent`): submit M tasks to a pool of N workers → all M results returned, never more than N agents in flight at once; tasks submitted mid-drain (pipelining) are picked up; a local-backend policy runs strictly 1 at a time; one slow/erroring task doesn't deadlock the pool.\n+- [ ] `cargo test -p avp-common` and `cargo build --workspace` green.\n+\n+## Workflow\n+- Use `/tdd` — write the pool/concurrency/pipelining tests against the mock agents first, then carve the pool out of `runner.rs`. Delete dead hook code; do not leave commented-out husks. Depends on avp-cli removal (the binary entry point is already gone).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTC4XPNVQV198X65GN6PBZ5V","timestamp":"2026-06-05T15:02:45.179868Z","op":"update","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -9,22 +9,22 @@\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n ---\n ## What\n-Remove the hook-triggered execution path from the engine crate (`avp-common`). KEEP the two genuinely reusable pieces for the new review fleet: the rules-as-data loader (loader.rs/types.rs/parser.rs, hook-free) and a new hook-free shared bounded `AgentPool`.\n+Remove the hook-triggered execution path from `avp-common`. KEEP: rules-as-data loader (loader.rs/types.rs/parser.rs, now hook-free) + new hook-free shared bounded `AgentPool`.\n\n ## Execution Plan (inline teardown)\n-- [ ] Phase 1 TDD: new `validator/pool.rs` — `AgentPool` with `submit(prompt) -> Future<Result>`, fixed worker count draining one shared queue, backend-aware worker count (local=1, remote=N/AIMD, `review.concurrency` override), per-call token cap. Tests vs PlaybackAgent/SessionRecordingAgent/SlowAgent: M tasks/N workers, never >N in flight, pipelining mid-drain, local=1 strictly serial, slow/erroring task no deadlock. RED then GREEN.\n-- [ ] Phase 2: delete hook machinery — chain/, strategy/, turn/, hooks/, types/{input,output,avp_output,common} hook bits, context.rs, install.rs, lockfile.rs(if hook-only). Strip HookType/MatchContext.hook_type/trigger from types.rs, loader.rs, parser.rs, builtin/mod.rs. Replace hook-coupled runner.rs with pool + kept mock harness.\n-- [ ] Phase 3: delete hook integration tests; adapt loader/types tests (precedence, glob match) to hook-free shape.\n-- [ ] Phase 4: fix `agent-client-protocol-extras` dev-dep — remove avp_schema_tests.rs + mod line + avp_test_context helper + avp-common dev-dependency.\n-- [ ] Phase 5: `cargo test -p avp-common` AND `cargo build --workspace` green.\n+- [x] Phase 1 TDD: new `validator/pool.rs` — `AgentPool` (submit→Future, fixed workers draining one shared queue, backend-aware count local=1/remote=N, per-call token cap). RED watched (7 todo!() fails), GREEN (10 pool tests pass).\n+- [x] Phase 2: deleted chain/, strategy/, turn/, hooks/, types/, context.rs, install.rs, lockfile.rs, validator/executor.rs, validator/runner.rs. Stripped HookType/trigger/MatchContext.hook_type from types.rs, loader.rs, parser.rs, builtin/mod.rs. Trimmed lib.rs, error.rs, Cargo.toml.\n+- [x] Phase 3: deleted all hook integration test files + fixtures; loader/glob/precedence coverage retained as unit tests in loader.rs/types.rs/builtin.\n+- [x] Phase 4: fixed `agent-client-protocol-extras` — removed avp_schema_tests.rs, mod line, avp_test_context helper + unused import, and the avp-common dev-dependency.\n+- [ ] Phase 5: `cargo test -p avp-common` (GREEN: 128 unit + 2 doc) AND `cargo build --workspace` (in progress).\n\n ## Acceptance Criteria\n-- [ ] No code path in the engine crate references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n-- [ ] A hook-free `AgentPool` exists: callers submit tasks at any time; a fixed worker count drains one shared queue; worker count is backend-aware; per-call token cap retained.\n+- [x] No engine-crate code references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n+- [x] A hook-free `AgentPool` exists per spec.\n - [ ] All consumers updated / dead surface removed; `cargo build --workspace` green.\n-- [ ] No real-time validation fires on tool calls anywhere.\n+- [x] No real-time validation fires on tool calls (hook system gone).\n\n ## Tests\n-- [ ] Retain/adapt the loader tests (precedence, glob match) — still green.\n-- [ ] AgentPool test (mock-agent harness): submit M tasks to N workers → all M results, never >N in flight; pipelining picked up; local-backend=1 strictly serial; one slow/erroring task no deadlock.\n-- [ ] `cargo test -p avp-common` and `cargo build --workspace` green.\n\\ No newline at end of file\n+- [x] Loader tests (precedence, glob match) retained as unit tests — green.\n+- [x] AgentPool tests (mock harness): all-results, never>N in flight, pipelining, local=1 serial, erroring no-deadlock, max-tokens — green.\n+- [ ] `cargo test -p avp-common` (done, green) and `cargo build --workspace` (verifying).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -9,22 +9,22 @@\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n ---\n ## What\n-Remove the hook-triggered execution path from `avp-common`. KEEP: rules-as-data loader (loader.rs/types.rs/parser.rs, now hook-free) + new hook-free shared bounded `AgentPool`.\n+Remove the hook-triggered execution path from the engine crate (`avp-common`). KEEP the two genuinely reusable pieces for the new review fleet: the rules-as-data loader (loader.rs/types.rs/parser.rs, hook-free) and a new hook-free shared bounded `AgentPool`.\n\n ## Execution Plan (inline teardown)\n-- [x] Phase 1 TDD: new `validator/pool.rs` — `AgentPool` (submit→Future, fixed workers draining one shared queue, backend-aware count local=1/remote=N, per-call token cap). RED watched (7 todo!() fails), GREEN (10 pool tests pass).\n-- [x] Phase 2: deleted chain/, strategy/, turn/, hooks/, types/, context.rs, install.rs, lockfile.rs, validator/executor.rs, validator/runner.rs. Stripped HookType/trigger/MatchContext.hook_type from types.rs, loader.rs, parser.rs, builtin/mod.rs. Trimmed lib.rs, error.rs, Cargo.toml.\n-- [x] Phase 3: deleted all hook integration test files + fixtures; loader/glob/precedence coverage retained as unit tests in loader.rs/types.rs/builtin.\n-- [x] Phase 4: fixed `agent-client-protocol-extras` — removed avp_schema_tests.rs, mod line, avp_test_context helper + unused import, and the avp-common dev-dependency.\n-- [ ] Phase 5: `cargo test -p avp-common` (GREEN: 128 unit + 2 doc) AND `cargo build --workspace` (in progress).\n+- [ ] Phase 1 TDD: new `validator/pool.rs` — `AgentPool` with `submit(prompt) -> Future<Result>`, fixed worker count draining one shared queue, backend-aware worker count (local=1, remote=N/AIMD, `review.concurrency` override), per-call token cap. Tests vs PlaybackAgent/SessionRecordingAgent/SlowAgent: M tasks/N workers, never >N in flight, pipelining mid-drain, local=1 strictly serial, slow/erroring task no deadlock. RED then GREEN.\n+- [ ] Phase 2: delete hook machinery — chain/, strategy/, turn/, hooks/, types/{input,output,avp_output,common} hook bits, context.rs, install.rs, lockfile.rs(if hook-only). Strip HookType/MatchContext.hook_type/trigger from types.rs, loader.rs, parser.rs, builtin/mod.rs. Replace hook-coupled runner.rs with pool + kept mock harness.\n+- [ ] Phase 3: delete hook integration tests; adapt loader/types tests (precedence, glob match) to hook-free shape.\n+- [ ] Phase 4: fix `agent-client-protocol-extras` dev-dep — remove avp_schema_tests.rs + mod line + avp_test_context helper + avp-common dev-dependency.\n+- [ ] Phase 5: `cargo test -p avp-common` AND `cargo build --workspace` green.\n\n ## Acceptance Criteria\n-- [x] No engine-crate code references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n-- [x] A hook-free `AgentPool` exists per spec.\n+- [ ] No code path in the engine crate references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n+- [ ] A hook-free `AgentPool` exists: callers submit tasks at any time; a fixed worker count drains one shared queue; worker count is backend-aware; per-call token cap retained.\n - [ ] All consumers updated / dead surface removed; `cargo build --workspace` green.\n-- [x] No real-time validation fires on tool calls (hook system gone).\n+- [ ] No real-time validation fires on tool calls anywhere.\n\n ## Tests\n-- [x] Loader tests (precedence, glob match) retained as unit tests — green.\n-- [x] AgentPool tests (mock harness): all-results, never>N in flight, pipelining, local=1 serial, erroring no-deadlock, max-tokens — green.\n-- [ ] `cargo test -p avp-common` (done, green) and `cargo build --workspace` (verifying).\n\\ No newline at end of file\n+- [ ] Retain/adapt the loader tests (precedence, glob match) — still green.\n+- [ ] AgentPool test (mock-agent harness): submit M tasks to N workers → all M results, never >N in flight; pipelining picked up; local-backend=1 strictly serial; one slow/erroring task no deadlock.\n+- [ ] `cargo test -p avp-common` and `cargo build --workspace` green.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTC51TYTH4BPM8PW2F122ZK0","timestamp":"2026-06-05T15:05:00.634989Z","op":"update","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -11,20 +11,23 @@\n ## What\n Remove the hook-triggered execution path from `avp-common`. KEEP: rules-as-data loader (loader.rs/types.rs/parser.rs, now hook-free) + new hook-free shared bounded `AgentPool`.\n\n-## Execution Plan (inline teardown)\n+## Execution Plan (inline teardown) — DONE\n - [x] Phase 1 TDD: new `validator/pool.rs` — `AgentPool` (submit→Future, fixed workers draining one shared queue, backend-aware count local=1/remote=N, per-call token cap). RED watched (7 todo!() fails), GREEN (10 pool tests pass).\n-- [x] Phase 2: deleted chain/, strategy/, turn/, hooks/, types/, context.rs, install.rs, lockfile.rs, validator/executor.rs, validator/runner.rs. Stripped HookType/trigger/MatchContext.hook_type from types.rs, loader.rs, parser.rs, builtin/mod.rs. Trimmed lib.rs, error.rs, Cargo.toml.\n-- [x] Phase 3: deleted all hook integration test files + fixtures; loader/glob/precedence coverage retained as unit tests in loader.rs/types.rs/builtin.\n-- [x] Phase 4: fixed `agent-client-protocol-extras` — removed avp_schema_tests.rs, mod line, avp_test_context helper + unused import, and the avp-common dev-dependency.\n-- [ ] Phase 5: `cargo test -p avp-common` (GREEN: 128 unit + 2 doc) AND `cargo build --workspace` (in progress).\n+- [x] Phase 2: deleted chain/, strategy/, turn/, hooks/, types/, context.rs, install.rs, lockfile.rs, validator/executor.rs, validator/runner.rs. Stripped HookType/trigger/MatchContext.hook_type from types.rs, loader.rs, parser.rs, builtin/mod.rs. Trimmed lib.rs, error.rs, Cargo.toml (dropped unused hook deps; moved extras+futures to dev-deps).\n+- [x] Phase 3: deleted all hook integration test files + recording fixtures; loader/glob/precedence coverage retained as unit tests in loader.rs/types.rs/builtin.\n+- [x] Phase 4: fixed `agent-client-protocol-extras` — removed avp_schema_tests.rs, its mod line, avp_test_context helper + the now-unused HookCommandContext import, and the avp-common dev-dependency.\n+- [x] Phase 5: `cargo test -p avp-common` GREEN (128 unit + 2 doc, 0 failed, 0 warnings, exit 0); `cargo build --workspace` GREEN (exit 0).\n\n-## Acceptance Criteria\n-- [x] No engine-crate code references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n-- [x] A hook-free `AgentPool` exists per spec.\n-- [ ] All consumers updated / dead surface removed; `cargo build --workspace` green.\n+## Acceptance Criteria — MET\n+- [x] No engine-crate code references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol (only `triggerMatcher` regex feature retained; no HookType type anywhere).\n+- [x] A hook-free `AgentPool` exists: submit at any time; fixed worker count drains one shared queue; worker count backend-aware (PoolConfig::local=1, ::remote=N/AIMD flag, ::with_concurrency override); per-call token cap retained.\n+- [x] All consumers updated / dead surface removed; `cargo build --workspace` green.\n - [x] No real-time validation fires on tool calls (hook system gone).\n\n-## Tests\n-- [x] Loader tests (precedence, glob match) retained as unit tests — green.\n-- [x] AgentPool tests (mock harness): all-results, never>N in flight, pipelining, local=1 serial, erroring no-deadlock, max-tokens — green.\n-- [ ] `cargo test -p avp-common` (done, green) and `cargo build --workspace` (verifying).\n\\ No newline at end of file\n+## Tests — PASS\n+- [x] Loader tests (precedence, glob match, tool/file/changed-files matching) retained as unit tests — green.\n+- [x] AgentPool tests (mock harness PlaybackAgent + MockAgent-based PeakProbe/Erroring/MetaRecording): all M results returned, never >N in flight, pipelining mid-drain, local=1 strictly serial, erroring task no-deadlock, per-call max_tokens cap — green.\n+- [x] `cargo test -p avp-common` and `cargo build --workspace` green.\n+\n+## Note for reviewer\n+The only non-test crate that depended on avp-common was `agent-client-protocol-extras` (dev-dep only); its lone consumer was the schema-compatibility test `avp_schema_tests.rs` that asserted extras' HookEvent JSON deserializes through the now-deleted `avp_common::HookInput`. With AVP's hook schema gone that test's reason to exist is gone, so it (and the dev-dep) were removed; extras keeps its own independent hook system. Loader precedence + RuleSet directory loading are now covered by unit tests (loader.rs, builtin/mod.rs) rather than the deleted hook-coupled integration files.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -11,23 +11,20 @@\n ## What\n Remove the hook-triggered execution path from `avp-common`. KEEP: rules-as-data loader (loader.rs/types.rs/parser.rs, now hook-free) + new hook-free shared bounded `AgentPool`.\n\n-## Execution Plan (inline teardown) — DONE\n+## Execution Plan (inline teardown)\n - [x] Phase 1 TDD: new `validator/pool.rs` — `AgentPool` (submit→Future, fixed workers draining one shared queue, backend-aware count local=1/remote=N, per-call token cap). RED watched (7 todo!() fails), GREEN (10 pool tests pass).\n-- [x] Phase 2: deleted chain/, strategy/, turn/, hooks/, types/, context.rs, install.rs, lockfile.rs, validator/executor.rs, validator/runner.rs. Stripped HookType/trigger/MatchContext.hook_type from types.rs, loader.rs, parser.rs, builtin/mod.rs. Trimmed lib.rs, error.rs, Cargo.toml (dropped unused hook deps; moved extras+futures to dev-deps).\n-- [x] Phase 3: deleted all hook integration test files + recording fixtures; loader/glob/precedence coverage retained as unit tests in loader.rs/types.rs/builtin.\n-- [x] Phase 4: fixed `agent-client-protocol-extras` — removed avp_schema_tests.rs, its mod line, avp_test_context helper + the now-unused HookCommandContext import, and the avp-common dev-dependency.\n-- [x] Phase 5: `cargo test -p avp-common` GREEN (128 unit + 2 doc, 0 failed, 0 warnings, exit 0); `cargo build --workspace` GREEN (exit 0).\n+- [x] Phase 2: deleted chain/, strategy/, turn/, hooks/, types/, context.rs, install.rs, lockfile.rs, validator/executor.rs, validator/runner.rs. Stripped HookType/trigger/MatchContext.hook_type from types.rs, loader.rs, parser.rs, builtin/mod.rs. Trimmed lib.rs, error.rs, Cargo.toml.\n+- [x] Phase 3: deleted all hook integration test files + fixtures; loader/glob/precedence coverage retained as unit tests in loader.rs/types.rs/builtin.\n+- [x] Phase 4: fixed `agent-client-protocol-extras` — removed avp_schema_tests.rs, mod line, avp_test_context helper + unused import, and the avp-common dev-dependency.\n+- [ ] Phase 5: `cargo test -p avp-common` (GREEN: 128 unit + 2 doc) AND `cargo build --workspace` (in progress).\n\n-## Acceptance Criteria — MET\n-- [x] No engine-crate code references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol (only `triggerMatcher` regex feature retained; no HookType type anywhere).\n-- [x] A hook-free `AgentPool` exists: submit at any time; fixed worker count drains one shared queue; worker count backend-aware (PoolConfig::local=1, ::remote=N/AIMD flag, ::with_concurrency override); per-call token cap retained.\n-- [x] All consumers updated / dead surface removed; `cargo build --workspace` green.\n+## Acceptance Criteria\n+- [x] No engine-crate code references `HookType`, PreToolUse/PostToolUse/Stop, turn-diff sidecars, or hook stdin/stdout protocol.\n+- [x] A hook-free `AgentPool` exists per spec.\n+- [ ] All consumers updated / dead surface removed; `cargo build --workspace` green.\n - [x] No real-time validation fires on tool calls (hook system gone).\n\n-## Tests — PASS\n-- [x] Loader tests (precedence, glob match, tool/file/changed-files matching) retained as unit tests — green.\n-- [x] AgentPool tests (mock harness PlaybackAgent + MockAgent-based PeakProbe/Erroring/MetaRecording): all M results returned, never >N in flight, pipelining mid-drain, local=1 strictly serial, erroring task no-deadlock, per-call max_tokens cap — green.\n-- [x] `cargo test -p avp-common` and `cargo build --workspace` green.\n-\n-## Note for reviewer\n-The only non-test crate that depended on avp-common was `agent-client-protocol-extras` (dev-dep only); its lone consumer was the schema-compatibility test `avp_schema_tests.rs` that asserted extras' HookEvent JSON deserializes through the now-deleted `avp_common::HookInput`. With AVP's hook schema gone that test's reason to exist is gone, so it (and the dev-dep) were removed; extras keeps its own independent hook system. Loader precedence + RuleSet directory loading are now covered by unit tests (loader.rs, builtin/mod.rs) rather than the deleted hook-coupled integration files.\n\\ No newline at end of file\n+## Tests\n+- [x] Loader tests (precedence, glob match) retained as unit tests — green.\n+- [x] AgentPool tests (mock harness): all-results, never>N in flight, pipelining, local=1 serial, erroring no-deadlock, max-tokens — green.\n+- [ ] `cargo test -p avp-common` (done, green) and `cargo build --workspace` (verifying).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTC51ZRMCZXABA2WYD30Y7EP","timestamp":"2026-06-05T15:05:05.556889Z","op":"update","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n","transaction_id":null}
{"id":"01KTC9AQP5M0QJG7R7J4W4NRGR","timestamp":"2026-06-05T16:19:46.501795Z","op":"update","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -30,4 +30,15 @@\n - [x] `cargo test -p avp-common` and `cargo build --workspace` green.\n\n ## Note for reviewer\n-The only non-test crate that depended on avp-common was `agent-client-protocol-extras` (dev-dep only); its lone consumer was the schema-compatibility test `avp_schema_tests.rs` that asserted extras' HookEvent JSON deserializes through the now-deleted `avp_common::HookInput`. With AVP's hook schema gone that test's reason to exist is gone, so it (and the dev-dep) were removed; extras keeps its own independent hook system. Loader precedence + RuleSet directory loading are now covered by unit tests (loader.rs, builtin/mod.rs) rather than the deleted hook-coupled integration files.\n\\ No newline at end of file\n+The only non-test crate that depended on avp-common was `agent-client-protocol-extras` (dev-dep only); its lone consumer was the schema-compatibility test `avp_schema_tests.rs` that asserted extras' HookEvent JSON deserializes through the now-deleted `avp_common::HookInput`. With AVP's hook schema gone that test's reason to exist is gone, so it (and the dev-dep) were removed; extras keeps its own independent hook system. Loader precedence + RuleSet directory loading are now covered by unit tests (loader.rs, builtin/mod.rs) rather than the deleted hook-coupled integration files.\n+\n+## Review Findings (2026-06-05 11:19)\n+\n+Verified independently: `cargo test -p avp-common` (128 unit + 2 doc, 0 failed), `cargo build --workspace` (exit 0), `cargo clippy -p avp-common` (0 warnings), `cargo test -p agent-client-protocol-extras` (all green). Confirmed no `HookType` / `HookCommandContext` / `HookInput` enum or struct survives in `avp-common`; the retained `triggerMatcher` is a plain `Option<String>` regex field, not a resurrected hook type. The `AgentPool` is well-engineered — the receiver `Mutex` is held only across `recv` (not across `run_prompt`), shutdown is handled gracefully on a closed queue, and the test suite proves the in-flight cap, pipelining, local serialization, no-deadlock-on-error, and the per-call token cap. Core teardown is clean. No blockers.\n+\n+### Warnings\n+- [ ] `crates/agent-client-protocol-extras/src/recording.rs` (`all_avp_recording_fixtures_deserialize`) — this test loops over `avp-common/tests/fixtures/recordings/*.json`, but this task **deleted** every one of those fixtures. The test guards with `if !fixtures_dir.exists() { return; }`, so it now early-returns and never reaches its `assert!(checked > 0)` — it reports green while exercising nothing. The teardown hollowed out this sibling test; it now silently passes on empty input, a maintenance trap. Fix: either remove the test along with the fixtures it validated, or repoint it at a surviving fixture source so the `checked > 0` guard is actually reachable.\n+\n+### Nits\n+- [ ] `crates/avp-common/src/validator/pool.rs` (`run_prompt` doc comment) — says \"Drive a single prompt turn against an ACP 0.11 agent connection\", but the workspace now builds against `agent-client-protocol` 0.12.1. Update the version in the doc comment.\n+- [ ] `crates/avp-common/src/validator/pool.rs` (`PoolConfig::aimd`) — the `aimd` field is set by `local()`/`remote()`/`with_concurrency()` and asserted in tests, but never read by `AgentPool::new` or `worker_loop`; no AIMD adjustment logic exists despite the module doc claiming the count is \"AIMD-adjusted to discover the API ceiling\". This is an intentional placeholder flag per the acceptance criteria (scoped for a follow-up task that consumes the pool), so not dead code to remove — but a brief `// reserved for AIMD; not yet consumed` note on the field would prevent a future reader mistaking it for wired-up behavior.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -30,15 +30,4 @@\n - [x] `cargo test -p avp-common` and `cargo build --workspace` green.\n\n ## Note for reviewer\n-The only non-test crate that depended on avp-common was `agent-client-protocol-extras` (dev-dep only); its lone consumer was the schema-compatibility test `avp_schema_tests.rs` that asserted extras' HookEvent JSON deserializes through the now-deleted `avp_common::HookInput`. With AVP's hook schema gone that test's reason to exist is gone, so it (and the dev-dep) were removed; extras keeps its own independent hook system. Loader precedence + RuleSet directory loading are now covered by unit tests (loader.rs, builtin/mod.rs) rather than the deleted hook-coupled integration files.\n-\n-## Review Findings (2026-06-05 11:19)\n-\n-Verified independently: `cargo test -p avp-common` (128 unit + 2 doc, 0 failed), `cargo build --workspace` (exit 0), `cargo clippy -p avp-common` (0 warnings), `cargo test -p agent-client-protocol-extras` (all green). Confirmed no `HookType` / `HookCommandContext` / `HookInput` enum or struct survives in `avp-common`; the retained `triggerMatcher` is a plain `Option<String>` regex field, not a resurrected hook type. The `AgentPool` is well-engineered — the receiver `Mutex` is held only across `recv` (not across `run_prompt`), shutdown is handled gracefully on a closed queue, and the test suite proves the in-flight cap, pipelining, local serialization, no-deadlock-on-error, and the per-call token cap. Core teardown is clean. No blockers.\n-\n-### Warnings\n-- [ ] `crates/agent-client-protocol-extras/src/recording.rs` (`all_avp_recording_fixtures_deserialize`) — this test loops over `avp-common/tests/fixtures/recordings/*.json`, but this task **deleted** every one of those fixtures. The test guards with `if !fixtures_dir.exists() { return; }`, so it now early-returns and never reaches its `assert!(checked > 0)` — it reports green while exercising nothing. The teardown hollowed out this sibling test; it now silently passes on empty input, a maintenance trap. Fix: either remove the test along with the fixtures it validated, or repoint it at a surviving fixture source so the `checked > 0` guard is actually reachable.\n-\n-### Nits\n-- [ ] `crates/avp-common/src/validator/pool.rs` (`run_prompt` doc comment) — says \"Drive a single prompt turn against an ACP 0.11 agent connection\", but the workspace now builds against `agent-client-protocol` 0.12.1. Update the version in the doc comment.\n-- [ ] `crates/avp-common/src/validator/pool.rs` (`PoolConfig::aimd`) — the `aimd` field is set by `local()`/`remote()`/`with_concurrency()` and asserted in tests, but never read by `AgentPool::new` or `worker_loop`; no AIMD adjustment logic exists despite the module doc claiming the count is \"AIMD-adjusted to discover the API ceiling\". This is an intentional placeholder flag per the acceptance criteria (scoped for a follow-up task that consumes the pool), so not dead code to remove — but a brief `// reserved for AIMD; not yet consumed` note on the field would prevent a future reader mistaking it for wired-up behavior.\n\\ No newline at end of file\n+The only non-test crate that depended on avp-common was `agent-client-protocol-extras` (dev-dep only); its lone consumer was the schema-compatibility test `avp_schema_tests.rs` that asserted extras' HookEvent JSON deserializes through the now-deleted `avp_common::HookInput`. With AVP's hook schema gone that test's reason to exist is gone, so it (and the dev-dep) were removed; extras keeps its own independent hook system. Loader precedence + RuleSet directory loading are now covered by unit tests (loader.rs, builtin/mod.rs) rather than the deleted hook-coupled integration files.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTC9BTWWD84PBF2G87H9YB9V","timestamp":"2026-06-05T16:20:22.556458Z","op":"update","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n","transaction_id":null}
{"id":"01KTC9GVPP5KYBNSSMGZHNV4SA","timestamp":"2026-06-05T16:23:07.222101Z","op":"update","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -37,8 +37,11 @@\n Verified independently: `cargo test -p avp-common` (128 unit + 2 doc, 0 failed), `cargo build --workspace` (exit 0), `cargo clippy -p avp-common` (0 warnings), `cargo test -p agent-client-protocol-extras` (all green). Confirmed no `HookType` / `HookCommandContext` / `HookInput` enum or struct survives in `avp-common`; the retained `triggerMatcher` is a plain `Option<String>` regex field, not a resurrected hook type. The `AgentPool` is well-engineered — the receiver `Mutex` is held only across `recv` (not across `run_prompt`), shutdown is handled gracefully on a closed queue, and the test suite proves the in-flight cap, pipelining, local serialization, no-deadlock-on-error, and the per-call token cap. Core teardown is clean. No blockers.\n\n ### Warnings\n-- [ ] `crates/agent-client-protocol-extras/src/recording.rs` (`all_avp_recording_fixtures_deserialize`) — this test loops over `avp-common/tests/fixtures/recordings/*.json`, but this task **deleted** every one of those fixtures. The test guards with `if !fixtures_dir.exists() { return; }`, so it now early-returns and never reaches its `assert!(checked > 0)` — it reports green while exercising nothing. The teardown hollowed out this sibling test; it now silently passes on empty input, a maintenance trap. Fix: either remove the test along with the fixtures it validated, or repoint it at a surviving fixture source so the `checked > 0` guard is actually reachable.\n+- [x] `crates/agent-client-protocol-extras/src/recording.rs` (`all_avp_recording_fixtures_deserialize`) — this test loops over `avp-common/tests/fixtures/recordings/*.json`, but this task **deleted** every one of those fixtures. The test guards with `if !fixtures_dir.exists() { return; }`, so it now early-returns and never reaches its `assert!(checked > 0)` — it reports green while exercising nothing. The teardown hollowed out this sibling test; it now silently passes on empty input, a maintenance trap. Fix: either remove the test along with the fixtures it validated, or repoint it at a surviving fixture source so the `checked > 0` guard is actually reachable.\n+  - RESOLVED: Removed the hollowed-out `all_avp_recording_fixtures_deserialize` test. The on-disk-format-stability guarantee it provided is already covered by the sibling `legacy_existing_fixture_deserializes`, which inlines a real legacy fixture (`initialize`/`new_session`/`prompt` with a notification) and asserts it parses with the current `RecordedSession`/`RecordedCall` types — no dependency on deleted files, so the coverage is genuinely exercised. Left an explanatory NOTE comment in place of the deleted test. Verified: `cargo test -p agent-client-protocol-extras` 207 lib + others, 0 failed.\n\n ### Nits\n-- [ ] `crates/avp-common/src/validator/pool.rs` (`run_prompt` doc comment) — says \"Drive a single prompt turn against an ACP 0.11 agent connection\", but the workspace now builds against `agent-client-protocol` 0.12.1. Update the version in the doc comment.\n-- [ ] `crates/avp-common/src/validator/pool.rs` (`PoolConfig::aimd`) — the `aimd` field is set by `local()`/`remote()`/`with_concurrency()` and asserted in tests, but never read by `AgentPool::new` or `worker_loop`; no AIMD adjustment logic exists despite the module doc claiming the count is \"AIMD-adjusted to discover the API ceiling\". This is an intentional placeholder flag per the acceptance criteria (scoped for a follow-up task that consumes the pool), so not dead code to remove — but a brief `// reserved for AIMD; not yet consumed` note on the field would prevent a future reader mistaking it for wired-up behavior.\n\\ No newline at end of file\n+- [x] `crates/avp-common/src/validator/pool.rs` (`run_prompt` doc comment) — says \"Drive a single prompt turn against an ACP 0.11 agent connection\", but the workspace now builds against `agent-client-protocol` 0.12.1. Update the version in the doc comment.\n+  - RESOLVED: Changed the `run_prompt` doc to \"ACP 0.12\" (matching `agent-client-protocol` 0.12.1 in Cargo.lock).\n+- [x] `crates/avp-common/src/validator/pool.rs` (`PoolConfig::aimd`) — the `aimd` field is set by `local()`/`remote()`/`with_concurrency()` and asserted in tests, but never read by `AgentPool::new` or `worker_loop`; no AIMD adjustment logic exists despite the module doc claiming the count is \"AIMD-adjusted to discover the API ceiling\". This is an intentional placeholder flag per the acceptance criteria (scoped for a follow-up task that consumes the pool), so not dead code to remove — but a brief `// reserved for AIMD; not yet consumed` note on the field would prevent a future reader mistaking it for wired-up behavior.\n+  - RESOLVED: Added a `// reserved for AIMD; not yet consumed` note on the `aimd` field explaining it is set by the constructors and asserted in tests but not read by `AgentPool::new`/`worker_loop`. Also softened the module-level doc so it no longer claims the count is \"AIMD-adjusted\" today — it now states the flag is reserved and the adaptive logic is not yet wired up, keeping doc and field consistent. Verified: `cargo clippy -p avp-common` exit 0 (no warnings), `cargo test -p avp-common` 128 unit + 2 doc, 0 failed.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -37,11 +37,8 @@\n Verified independently: `cargo test -p avp-common` (128 unit + 2 doc, 0 failed), `cargo build --workspace` (exit 0), `cargo clippy -p avp-common` (0 warnings), `cargo test -p agent-client-protocol-extras` (all green). Confirmed no `HookType` / `HookCommandContext` / `HookInput` enum or struct survives in `avp-common`; the retained `triggerMatcher` is a plain `Option<String>` regex field, not a resurrected hook type. The `AgentPool` is well-engineered — the receiver `Mutex` is held only across `recv` (not across `run_prompt`), shutdown is handled gracefully on a closed queue, and the test suite proves the in-flight cap, pipelining, local serialization, no-deadlock-on-error, and the per-call token cap. Core teardown is clean. No blockers.\n\n ### Warnings\n-- [x] `crates/agent-client-protocol-extras/src/recording.rs` (`all_avp_recording_fixtures_deserialize`) — this test loops over `avp-common/tests/fixtures/recordings/*.json`, but this task **deleted** every one of those fixtures. The test guards with `if !fixtures_dir.exists() { return; }`, so it now early-returns and never reaches its `assert!(checked > 0)` — it reports green while exercising nothing. The teardown hollowed out this sibling test; it now silently passes on empty input, a maintenance trap. Fix: either remove the test along with the fixtures it validated, or repoint it at a surviving fixture source so the `checked > 0` guard is actually reachable.\n-  - RESOLVED: Removed the hollowed-out `all_avp_recording_fixtures_deserialize` test. The on-disk-format-stability guarantee it provided is already covered by the sibling `legacy_existing_fixture_deserializes`, which inlines a real legacy fixture (`initialize`/`new_session`/`prompt` with a notification) and asserts it parses with the current `RecordedSession`/`RecordedCall` types — no dependency on deleted files, so the coverage is genuinely exercised. Left an explanatory NOTE comment in place of the deleted test. Verified: `cargo test -p agent-client-protocol-extras` 207 lib + others, 0 failed.\n+- [ ] `crates/agent-client-protocol-extras/src/recording.rs` (`all_avp_recording_fixtures_deserialize`) — this test loops over `avp-common/tests/fixtures/recordings/*.json`, but this task **deleted** every one of those fixtures. The test guards with `if !fixtures_dir.exists() { return; }`, so it now early-returns and never reaches its `assert!(checked > 0)` — it reports green while exercising nothing. The teardown hollowed out this sibling test; it now silently passes on empty input, a maintenance trap. Fix: either remove the test along with the fixtures it validated, or repoint it at a surviving fixture source so the `checked > 0` guard is actually reachable.\n\n ### Nits\n-- [x] `crates/avp-common/src/validator/pool.rs` (`run_prompt` doc comment) — says \"Drive a single prompt turn against an ACP 0.11 agent connection\", but the workspace now builds against `agent-client-protocol` 0.12.1. Update the version in the doc comment.\n-  - RESOLVED: Changed the `run_prompt` doc to \"ACP 0.12\" (matching `agent-client-protocol` 0.12.1 in Cargo.lock).\n-- [x] `crates/avp-common/src/validator/pool.rs` (`PoolConfig::aimd`) — the `aimd` field is set by `local()`/`remote()`/`with_concurrency()` and asserted in tests, but never read by `AgentPool::new` or `worker_loop`; no AIMD adjustment logic exists despite the module doc claiming the count is \"AIMD-adjusted to discover the API ceiling\". This is an intentional placeholder flag per the acceptance criteria (scoped for a follow-up task that consumes the pool), so not dead code to remove — but a brief `// reserved for AIMD; not yet consumed` note on the field would prevent a future reader mistaking it for wired-up behavior.\n-  - RESOLVED: Added a `// reserved for AIMD; not yet consumed` note on the `aimd` field explaining it is set by the constructors and asserted in tests but not read by `AgentPool::new`/`worker_loop`. Also softened the module-level doc so it no longer claims the count is \"AIMD-adjusted\" today — it now states the flag is reserved and the adaptive logic is not yet wired up, keeping doc and field consistent. Verified: `cargo clippy -p avp-common` exit 0 (no warnings), `cargo test -p avp-common` 128 unit + 2 doc, 0 failed.\n\\ No newline at end of file\n+- [ ] `crates/avp-common/src/validator/pool.rs` (`run_prompt` doc comment) — says \"Drive a single prompt turn against an ACP 0.11 agent connection\", but the workspace now builds against `agent-client-protocol` 0.12.1. Update the version in the doc comment.\n+- [ ] `crates/avp-common/src/validator/pool.rs` (`PoolConfig::aimd`) — the `aimd` field is set by `local()`/`remote()`/`with_concurrency()` and asserted in tests, but never read by `AgentPool::new` or `worker_loop`; no AIMD adjustment logic exists despite the module doc claiming the count is \"AIMD-adjusted to discover the API ceiling\". This is an intentional placeholder flag per the acceptance criteria (scoped for a follow-up task that consumes the pool), so not dead code to remove — but a brief `// reserved for AIMD; not yet consumed` note on the field would prevent a future reader mistaking it for wired-up behavior.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTC9H00HDY495S5ZQ560KRNM","timestamp":"2026-06-05T16:23:11.633536Z","op":"update","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n","transaction_id":null}
{"id":"01KTC9NE118E6C7YJ3J3W0P3J0","timestamp":"2026-06-05T16:25:37.057343Z","op":"update","item_id":"01KTBNGHCH7B3J3DVF9CXPADJ1","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffee80\n project: local-review\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffee80\n+position_column: review\n+position_ordinal: '80'\n project: local-review\n title: 'Teardown: retire AVP hook-execution machinery (keep loader + ACP executor)'\n ---\n","transaction_id":null}

{"id":"01KTCBFKT5JFMZYPEK3G4034AA","timestamp":"2026-06-05T16:57:23.525161Z","op":"create","item_id":"01KTCBFKT461CQKT1FQ13E52D6","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,30 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KTCBDXJKQA68WPEE4MJW77ZH\n+position_column: todo\n+position_ordinal: '9680'\n+title: Migrate kanban-cli onto the shared generator; delete its private cli_gen.rs\n+---\n+## What\n+Switch kanban-cli to the shared `swissarmyhammer_operations::cli_gen` generator (card B) and delete the now-duplicate `apps/kanban-cli/src/cli_gen.rs`. This is a pure refactor — identical CLI behavior, one fewer copy of the engine.\n+\n+Changes:\n+- `apps/kanban-cli/src/main.rs`: remove `mod cli_gen;` (line 8). Replace the two call sites — `cli_gen::build_commands_from_schema(schema)` in `build_cli` (line 54) and `cli_gen::extract_noun_verb_arguments(...)` in `dispatch` (used at `apps/kanban-cli/src/main.rs:335`) — with `swissarmyhammer_operations::cli_gen::build_commands_from_schema` / `::extract_noun_verb_arguments`.\n+- Keep feeding the FULL schema in-process: `generate_kanban_mcp_schema(operations)` at `main.rs:34` must continue to produce the full schema (with `x-operation-schemas`) for the generator. After card D switches the per-tool wrapper to slim, ensure kanban-cli calls the `_full` variant (e.g. `generate_kanban_mcp_schema_full`) so its command tree keeps per-op precision. If E lands before D, no change needed yet; if after, point it at `_full`.\n+- Delete `apps/kanban-cli/src/cli_gen.rs`.\n+- `apps/kanban-cli/Cargo.toml` already depends on `swissarmyhammer-operations` (:27) — no dep change. `once_cell` (:44) may become unused if the interner moved out; drop it if so.\n+\n+## Acceptance Criteria\n+- [ ] `apps/kanban-cli/src/cli_gen.rs` is deleted and `mod cli_gen;` removed from `main.rs`.\n+- [ ] `kanban` CLI produces the identical noun/verb/arg command tree as before (same nouns, verbs, per-op required flags).\n+- [ ] `cargo build -p kanban-cli` succeeds with no unused-dep/import warnings.\n+\n+## Tests\n+- [ ] Add a golden command-tree test in `apps/kanban-cli` (e.g. `tests/cli_tree.rs`): build the clap command via the shared generator from the kanban full schema, walk noun→verb→args, and assert the expected nouns (`board`, `task`, `column`, `tag`, …) with each verb's scoped required args — mirroring the assertions ported in card B but exercised through kanban-cli's actual `build_cli`.\n+- [ ] A regression check that `kanban task move` (or a known per-op-required case) enforces that op's required field and does NOT accept the global union.\n+- [ ] `cargo nextest run -p kanban-cli` passes.\n+\n+## Workflow\n+- Use `/tdd` — write the golden-tree test against the shared generator first, confirm it passes with the shared module, then delete the private `cli_gen.rs`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,30 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KTCBDXJKQA68WPEE4MJW77ZH\n-position_column: todo\n-position_ordinal: '9680'\n-title: Migrate kanban-cli onto the shared generator; delete its private cli_gen.rs\n----\n-## What\n-Switch kanban-cli to the shared `swissarmyhammer_operations::cli_gen` generator (card B) and delete the now-duplicate `apps/kanban-cli/src/cli_gen.rs`. This is a pure refactor — identical CLI behavior, one fewer copy of the engine.\n-\n-Changes:\n-- `apps/kanban-cli/src/main.rs`: remove `mod cli_gen;` (line 8). Replace the two call sites — `cli_gen::build_commands_from_schema(schema)` in `build_cli` (line 54) and `cli_gen::extract_noun_verb_arguments(...)` in `dispatch` (used at `apps/kanban-cli/src/main.rs:335`) — with `swissarmyhammer_operations::cli_gen::build_commands_from_schema` / `::extract_noun_verb_arguments`.\n-- Keep feeding the FULL schema in-process: `generate_kanban_mcp_schema(operations)` at `main.rs:34` must continue to produce the full schema (with `x-operation-schemas`) for the generator. After card D switches the per-tool wrapper to slim, ensure kanban-cli calls the `_full` variant (e.g. `generate_kanban_mcp_schema_full`) so its command tree keeps per-op precision. If E lands before D, no change needed yet; if after, point it at `_full`.\n-- Delete `apps/kanban-cli/src/cli_gen.rs`.\n-- `apps/kanban-cli/Cargo.toml` already depends on `swissarmyhammer-operations` (:27) — no dep change. `once_cell` (:44) may become unused if the interner moved out; drop it if so.\n-\n-## Acceptance Criteria\n-- [ ] `apps/kanban-cli/src/cli_gen.rs` is deleted and `mod cli_gen;` removed from `main.rs`.\n-- [ ] `kanban` CLI produces the identical noun/verb/arg command tree as before (same nouns, verbs, per-op required flags).\n-- [ ] `cargo build -p kanban-cli` succeeds with no unused-dep/import warnings.\n-\n-## Tests\n-- [ ] Add a golden command-tree test in `apps/kanban-cli` (e.g. `tests/cli_tree.rs`): build the clap command via the shared generator from the kanban full schema, walk noun→verb→args, and assert the expected nouns (`board`, `task`, `column`, `tag`, …) with each verb's scoped required args — mirroring the assertions ported in card B but exercised through kanban-cli's actual `build_cli`.\n-- [ ] A regression check that `kanban task move` (or a known per-op-required case) enforces that op's required field and does NOT accept the global union.\n-- [ ] `cargo nextest run -p kanban-cli` passes.\n-\n-## Workflow\n-- Use `/tdd` — write the golden-tree test against the shared generator first, confirm it passes with the shared module, then delete the private `cli_gen.rs`.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTCJGJNQX3CRJCBACPV4BHW6","timestamp":"2026-06-05T19:00:15.159706Z","op":"update","item_id":"01KTCBFKT461CQKT1FQ13E52D6","forward_patch":"--- original\n+++ modified\n@@ -5,6 +5,7 @@\n - 01KTCBDXJKQA68WPEE4MJW77ZH\n position_column: todo\n position_ordinal: '9680'\n+project: cli-schema-gen\n title: Migrate kanban-cli onto the shared generator; delete its private cli_gen.rs\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -5,7 +5,6 @@\n - 01KTCBDXJKQA68WPEE4MJW77ZH\n position_column: todo\n position_ordinal: '9680'\n-project: cli-schema-gen\n title: Migrate kanban-cli onto the shared generator; delete its private cli_gen.rs\n ---\n ## What\n","transaction_id":null}
{"id":"01KTHFA03F3G86NKZKBX4VNBR9","timestamp":"2026-06-07T16:40:25.967652Z","op":"update","item_id":"01KTCBFKT461CQKT1FQ13E52D6","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KTCBDXJKQA68WPEE4MJW77ZH\n-position_column: todo\n-position_ordinal: '9680'\n+position_column: doing\n+position_ordinal: '8180'\n project: cli-schema-gen\n title: Migrate kanban-cli onto the shared generator; delete its private cli_gen.rs\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KTCBDXJKQA68WPEE4MJW77ZH\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: todo\n+position_ordinal: '9680'\n project: cli-schema-gen\n title: Migrate kanban-cli onto the shared generator; delete its private cli_gen.rs\n ---\n","transaction_id":null}
{"id":"01KTHFFCPWFSANQFY5PNJF8SEV","timestamp":"2026-06-07T16:43:22.716183Z","op":"update","item_id":"01KTCBFKT461CQKT1FQ13E52D6","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KTCBDXJKQA68WPEE4MJW77ZH\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '80'\n project: cli-schema-gen\n title: Migrate kanban-cli onto the shared generator; delete its private cli_gen.rs\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KTCBDXJKQA68WPEE4MJW77ZH\n-position_column: review\n-position_ordinal: '80'\n+position_column: doing\n+position_ordinal: '8180'\n project: cli-schema-gen\n title: Migrate kanban-cli onto the shared generator; delete its private cli_gen.rs\n ---\n","transaction_id":null}
{"id":"01KTJ1C0TEWH75ERTJA99JPGWQ","timestamp":"2026-06-07T21:56:06.606055Z","op":"update","item_id":"01KTCBFKT461CQKT1FQ13E52D6","forward_patch":"--- original\n+++ modified\n@@ -28,4 +28,23 @@\n - [x] `cargo nextest run -p kanban-cli` passes.\n\n ## Workflow\n-- Use `/tdd` — write the golden-tree test against the shared generator first, confirm it passes with the shared module, then delete the private `cli_gen.rs`.\n\\ No newline at end of file\n+- Use `/tdd` — write the golden-tree test against the shared generator first, confirm it passes with the shared module, then delete the private `cli_gen.rs`.\n+\n+## Review Findings (2026-06-07 15:58)\n+\n+### Warnings\n+- [ ] `apps/code-context-cli/src/main.rs:62` — `build_cli` (plus its `global_flag` helper) is the third verbatim copy of the same CLI-assembly skeleton — global flags, then schema-driven op subcommands via `cli_gen::build_commands_from_schema`, then a fixed list of lifecycle subcommands. Three identical orchestrators means a flag or wiring fix has to be made in three places and will drift. Extract a shared `build_cli(name, about, schema, lifecycle_subcommands)` (and a `global_flag` helper) into a shared crate alongside the existing `swissarmyhammer_cli_completions`, and have code-context/shelltool/kanban/avp all call it, passing only name/about.\n+- [ ] `apps/code-context-cli/src/main.rs:119` — The lifecycle subcommand builders `serve_subcommand`/`init_subcommand`/`deinit_subcommand`/`doctor_subcommand`/`completion_subcommand` are duplicated verbatim across all three CLIs, differing only in the about/long_about strings — a parameterizable difference, not a real fork. Move these builders into the shared CLI-scaffold module, parameterized by the per-tool about strings (e.g. `serve_subcommand(about: &str)`), and call them from each CLI.\n+- [ ] `apps/code-context-cli/src/main.rs:191` — The install-target helper trio `install_target_arg`/`target_value`/`target_to_scope` is copied byte-for-byte from the other CLIs. These have no per-tool variation at all, so there is nothing to fork — every CLI maps the same `project|local|user` strings to the same `InitScope`. Hoist these three into the shared scaffold crate (or reuse mirdan's `InitScope` conversion path that `apps/avp-cli/src/install.rs:17` already provides) and delete the local copies.\n+- [ ] `apps/code-context-cli/src/main.rs:195` — The valid lifecycle-scope set {\"project\",\"local\",\"user\"} is enumerated as raw string literals in two parallel code paths that a human must keep in lockstep: the clap whitelist `value_parser([\"project\", \"local\", \"user\"])` (line 195) and the decoder `target_to_scope`'s match arms (lines 257-259). Adding or renaming a scope requires editing both; if the match arm is forgotten, clap accepts the value and the program panics at runtime (line 260) instead of failing to compile. A canonical typed representation already exists — the `InstallTarget` ValueEnum in cli.rs (variants Project/Local/User with a Display impl yielding exactly these three strings) — so this is duplicated, not single-sourced, data. Single-source the set: reuse the existing `InstallTarget` ValueEnum as the arg's `value_parser` (clap `EnumValueParser`, as already done for `Shell` on line 184) and parse straight into it via `get_one::<InstallTarget>`, then `impl From<InstallTarget> for InitScope`. The clap whitelist and the scope mapping then derive from one enum and cannot drift; the runtime `panic!` arm disappears because the type is exhaustive.\n+- [ ] `apps/code-context-cli/src/main.rs:320` — `run_completion` is a verbatim third copy of the completion dispatcher — it just calls `print_completion(build_cli(schema), shell)` and maps the result to an exit code. Identical control flow already exists in the other two CLIs. Once `build_cli` and `print_completion` are centralized, fold `run_completion` into the shared scaffold so each CLI invokes one shared completion path.\n+- [ ] `apps/shelltool-cli/src/main.rs:162` — `install_target_arg` is a byte-for-byte third copy of the same helper in two sibling CLIs. Triplicated CLI scaffolding means a fix to the target arg (e.g. a new scope value) has to be made in three places and will silently drift. Hoist `install_target_arg` (and the `[\"project\",\"local\",\"user\"]` value set) into a shared CLI-lifecycle helper crate that all three binaries call; delete the per-binary copies.\n+- [ ] `apps/shelltool-cli/src/main.rs:170` — `target_value` is an exact duplicate of the same accessor in the other two CLIs — pure duplication with zero per-binary variation. Move `target_value` into the same shared lifecycle helper as `install_target_arg`; the two are always used as a pair.\n+- [ ] `apps/shelltool-cli/src/main.rs:184` — The valid lifecycle-target set and its target-string → InitScope mapping is a table split across two hand-maintained sites that must stay in lockstep. `install_target_to_scope` matches on \"project\"/\"local\"/\"user\" (lines 184-188), while `install_target_arg`'s `value_parser` independently lists the same three strings (line 166). Add or rename a scope and you must edit both, or the parser admits a value the match panics on — the panic arm at line 188 exists precisely to absorb that drift. Define one table as the single source of truth, e.g. `const TARGETS: &[(&str, InitScope)] = &[(\"project\", InitScope::Project), (\"local\", InitScope::Local), (\"user\", InitScope::User)];` (or a `FromStr`/`as_str` pair on `InitScope` in `swissarmyhammer_common::lifecycle`). Derive the `value_parser` from its keys and look up the scope from the same table, so the panic arm becomes unreachable-by-construction rather than a drift backstop.\n+- [ ] `apps/shelltool-cli/src/main.rs:300` — `run_completion` is a near-identical third copy across the CLIs — it rebuilds the command tree and writes the completion script with the same logic everywhere. The completion path will diverge per-binary on the next change. Parameterize a single `run_completion(build_cli, shell)` (and a shared `completion_subcommand`) on the binary name; `swissarmyhammer_cli_completions::print_completion_for` already takes the name, so the wrapper is the only thing left to share.\n+\n+### Nits\n+- [ ] `apps/shelltool-cli/src/main.rs:3` — The crate-level `Commands:` doc list (lines 3-7) is stale after this change: it omits the `completion` subcommand and the schema-driven shell-operation subcommands (`command execute`, `processes list`, `history grep`, `lines get`, `process kill`) that are the whole point of this refactor. Crate docs that under-list the actual command surface mislead anyone reading the binary's entry point. Add `- `shelltool completion <shell>`: Generate shell completion scripts` and a line noting the schema-driven shell operations (e.g. ``shelltool <noun> <verb>``: run a shell operation) to the `Commands:` block.\n+- [ ] `apps/swissarmyhammer-cli/src/main.rs:24` — `build_tool_arguments` returns `Result<_, String>` and collapses the underlying error via `format!(\"Error processing arguments: {}\", e)`; the validator's error-handling rule asks application code to use `anyhow::Result<T>` so the `Error::source()` chain is preserved rather than flattened into a single Display string. Return `anyhow::Result<serde_json::Map<...>>` and replace the `format!` mapping with `.context(\"processing arguments\")`. This keeps the source chain intact; `report_error_and_exit` can render it with `{:#}`. If the binary deliberately standardizes on `String` errors at this seam, leave as-is — this is a nit, not a blocker.\n+- [ ] `crates/swissarmyhammer-kanban/src/schema.rs:475` — New test fn `serde_defaulted_fields_excluded_from_required_signatures` omits the `test_` prefix that every other test in this module uses, breaking the file's established naming pattern and making the test harder to spot/scan among its peers. Rename to `test_serde_defaulted_fields_excluded_from_required_signatures` to match the surrounding convention (cf. the parallel test `test_serde_default_field_not_required` added in macro_expansion.rs).\n+- [ ] `crates/swissarmyhammer-operations-macros/src/lib.rs:198` — has_serde_default only inspects field-level attributes, so a container-level #[serde(default)] (which makes every field optional) is not detected — those non-Option fields would still be emitted as required, the exact false-positive this change exists to prevent, just at the container scope. Either document this scope limit in the doc comment (\"detects field-level serde defaults only; container-level #[serde(default)] is not considered\"), or plumb the container attributes into the required computation so a struct-level default also marks fields optional.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -28,23 +28,4 @@\n - [x] `cargo nextest run -p kanban-cli` passes.\n\n ## Workflow\n-- Use `/tdd` — write the golden-tree test against the shared generator first, confirm it passes with the shared module, then delete the private `cli_gen.rs`.\n-\n-## Review Findings (2026-06-07 15:58)\n-\n-### Warnings\n-- [ ] `apps/code-context-cli/src/main.rs:62` — `build_cli` (plus its `global_flag` helper) is the third verbatim copy of the same CLI-assembly skeleton — global flags, then schema-driven op subcommands via `cli_gen::build_commands_from_schema`, then a fixed list of lifecycle subcommands. Three identical orchestrators means a flag or wiring fix has to be made in three places and will drift. Extract a shared `build_cli(name, about, schema, lifecycle_subcommands)` (and a `global_flag` helper) into a shared crate alongside the existing `swissarmyhammer_cli_completions`, and have code-context/shelltool/kanban/avp all call it, passing only name/about.\n-- [ ] `apps/code-context-cli/src/main.rs:119` — The lifecycle subcommand builders `serve_subcommand`/`init_subcommand`/`deinit_subcommand`/`doctor_subcommand`/`completion_subcommand` are duplicated verbatim across all three CLIs, differing only in the about/long_about strings — a parameterizable difference, not a real fork. Move these builders into the shared CLI-scaffold module, parameterized by the per-tool about strings (e.g. `serve_subcommand(about: &str)`), and call them from each CLI.\n-- [ ] `apps/code-context-cli/src/main.rs:191` — The install-target helper trio `install_target_arg`/`target_value`/`target_to_scope` is copied byte-for-byte from the other CLIs. These have no per-tool variation at all, so there is nothing to fork — every CLI maps the same `project|local|user` strings to the same `InitScope`. Hoist these three into the shared scaffold crate (or reuse mirdan's `InitScope` conversion path that `apps/avp-cli/src/install.rs:17` already provides) and delete the local copies.\n-- [ ] `apps/code-context-cli/src/main.rs:195` — The valid lifecycle-scope set {\"project\",\"local\",\"user\"} is enumerated as raw string literals in two parallel code paths that a human must keep in lockstep: the clap whitelist `value_parser([\"project\", \"local\", \"user\"])` (line 195) and the decoder `target_to_scope`'s match arms (lines 257-259). Adding or renaming a scope requires editing both; if the match arm is forgotten, clap accepts the value and the program panics at runtime (line 260) instead of failing to compile. A canonical typed representation already exists — the `InstallTarget` ValueEnum in cli.rs (variants Project/Local/User with a Display impl yielding exactly these three strings) — so this is duplicated, not single-sourced, data. Single-source the set: reuse the existing `InstallTarget` ValueEnum as the arg's `value_parser` (clap `EnumValueParser`, as already done for `Shell` on line 184) and parse straight into it via `get_one::<InstallTarget>`, then `impl From<InstallTarget> for InitScope`. The clap whitelist and the scope mapping then derive from one enum and cannot drift; the runtime `panic!` arm disappears because the type is exhaustive.\n-- [ ] `apps/code-context-cli/src/main.rs:320` — `run_completion` is a verbatim third copy of the completion dispatcher — it just calls `print_completion(build_cli(schema), shell)` and maps the result to an exit code. Identical control flow already exists in the other two CLIs. Once `build_cli` and `print_completion` are centralized, fold `run_completion` into the shared scaffold so each CLI invokes one shared completion path.\n-- [ ] `apps/shelltool-cli/src/main.rs:162` — `install_target_arg` is a byte-for-byte third copy of the same helper in two sibling CLIs. Triplicated CLI scaffolding means a fix to the target arg (e.g. a new scope value) has to be made in three places and will silently drift. Hoist `install_target_arg` (and the `[\"project\",\"local\",\"user\"]` value set) into a shared CLI-lifecycle helper crate that all three binaries call; delete the per-binary copies.\n-- [ ] `apps/shelltool-cli/src/main.rs:170` — `target_value` is an exact duplicate of the same accessor in the other two CLIs — pure duplication with zero per-binary variation. Move `target_value` into the same shared lifecycle helper as `install_target_arg`; the two are always used as a pair.\n-- [ ] `apps/shelltool-cli/src/main.rs:184` — The valid lifecycle-target set and its target-string → InitScope mapping is a table split across two hand-maintained sites that must stay in lockstep. `install_target_to_scope` matches on \"project\"/\"local\"/\"user\" (lines 184-188), while `install_target_arg`'s `value_parser` independently lists the same three strings (line 166). Add or rename a scope and you must edit both, or the parser admits a value the match panics on — the panic arm at line 188 exists precisely to absorb that drift. Define one table as the single source of truth, e.g. `const TARGETS: &[(&str, InitScope)] = &[(\"project\", InitScope::Project), (\"local\", InitScope::Local), (\"user\", InitScope::User)];` (or a `FromStr`/`as_str` pair on `InitScope` in `swissarmyhammer_common::lifecycle`). Derive the `value_parser` from its keys and look up the scope from the same table, so the panic arm becomes unreachable-by-construction rather than a drift backstop.\n-- [ ] `apps/shelltool-cli/src/main.rs:300` — `run_completion` is a near-identical third copy across the CLIs — it rebuilds the command tree and writes the completion script with the same logic everywhere. The completion path will diverge per-binary on the next change. Parameterize a single `run_completion(build_cli, shell)` (and a shared `completion_subcommand`) on the binary name; `swissarmyhammer_cli_completions::print_completion_for` already takes the name, so the wrapper is the only thing left to share.\n-\n-### Nits\n-- [ ] `apps/shelltool-cli/src/main.rs:3` — The crate-level `Commands:` doc list (lines 3-7) is stale after this change: it omits the `completion` subcommand and the schema-driven shell-operation subcommands (`command execute`, `processes list`, `history grep`, `lines get`, `process kill`) that are the whole point of this refactor. Crate docs that under-list the actual command surface mislead anyone reading the binary's entry point. Add `- `shelltool completion <shell>`: Generate shell completion scripts` and a line noting the schema-driven shell operations (e.g. ``shelltool <noun> <verb>``: run a shell operation) to the `Commands:` block.\n-- [ ] `apps/swissarmyhammer-cli/src/main.rs:24` — `build_tool_arguments` returns `Result<_, String>` and collapses the underlying error via `format!(\"Error processing arguments: {}\", e)`; the validator's error-handling rule asks application code to use `anyhow::Result<T>` so the `Error::source()` chain is preserved rather than flattened into a single Display string. Return `anyhow::Result<serde_json::Map<...>>` and replace the `format!` mapping with `.context(\"processing arguments\")`. This keeps the source chain intact; `report_error_and_exit` can render it with `{:#}`. If the binary deliberately standardizes on `String` errors at this seam, leave as-is — this is a nit, not a blocker.\n-- [ ] `crates/swissarmyhammer-kanban/src/schema.rs:475` — New test fn `serde_defaulted_fields_excluded_from_required_signatures` omits the `test_` prefix that every other test in this module uses, breaking the file's established naming pattern and making the test harder to spot/scan among its peers. Rename to `test_serde_defaulted_fields_excluded_from_required_signatures` to match the surrounding convention (cf. the parallel test `test_serde_default_field_not_required` added in macro_expansion.rs).\n-- [ ] `crates/swissarmyhammer-operations-macros/src/lib.rs:198` — has_serde_default only inspects field-level attributes, so a container-level #[serde(default)] (which makes every field optional) is not detected — those non-Option fields would still be emitted as required, the exact false-positive this change exists to prevent, just at the container scope. Either document this scope limit in the doc comment (\"detects field-level serde defaults only; container-level #[serde(default)] is not considered\"), or plumb the container attributes into the required computation so a struct-level default also marks fields optional.\n\\ No newline at end of file\n+- Use `/tdd` — write the golden-tree test against the shared generator first, confirm it passes with the shared module, then delete the private `cli_gen.rs`.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTJ5DP9ZRR7P4YMKKX7T7D2M","timestamp":"2026-06-07T23:06:55.679825Z","op":"update","item_id":"01KTCBFKT461CQKT1FQ13E52D6","forward_patch":"--- original\n+++ modified\n@@ -11,12 +11,6 @@\n ## What\n Switch kanban-cli to the shared `swissarmyhammer_operations::cli_gen` generator (card B) and delete the now-duplicate `apps/kanban-cli/src/cli_gen.rs`. This is a pure refactor — identical CLI behavior, one fewer copy of the engine.\n\n-Changes:\n-- `apps/kanban-cli/src/main.rs`: remove `mod cli_gen;` (line 8). Replace the two call sites — `cli_gen::build_commands_from_schema(schema)` in `build_cli` (line 54) and `cli_gen::extract_noun_verb_arguments(...)` in `dispatch` (used at `apps/kanban-cli/src/main.rs:335`) — with `swissarmyhammer_operations::cli_gen::build_commands_from_schema` / `::extract_noun_verb_arguments`.\n-- Keep feeding the FULL schema in-process: `generate_kanban_mcp_schema(operations)` at `main.rs:34` must continue to produce the full schema (with `x-operation-schemas`) for the generator. After card D switches the per-tool wrapper to slim, ensure kanban-cli calls the `_full` variant (e.g. `generate_kanban_mcp_schema_full`) so its command tree keeps per-op precision. If E lands before D, no change needed yet; if after, point it at `_full`.\n-- Delete `apps/kanban-cli/src/cli_gen.rs`.\n-- `apps/kanban-cli/Cargo.toml` already depends on `swissarmyhammer-operations` (:27) — no dep change. `once_cell` (:44) may become unused if the interner moved out; drop it if so.\n-\n ## Acceptance Criteria\n - [x] `apps/kanban-cli/src/cli_gen.rs` is deleted and `mod cli_gen;` removed from `main.rs`.\n - [x] `kanban` CLI produces the identical noun/verb/arg command tree as before (same nouns, verbs, per-op required flags).\n@@ -23,28 +17,25 @@\n - [x] `cargo build -p kanban-cli` succeeds with no unused-dep/import warnings.\n\n ## Tests\n-- [x] Add a golden command-tree test in `apps/kanban-cli` (e.g. `tests/cli_tree.rs`): build the clap command via the shared generator from the kanban full schema, walk noun→verb→args, and assert the expected nouns (`board`, `task`, `column`, `tag`, …) with each verb's scoped required args — mirroring the assertions ported in card B but exercised through kanban-cli's actual `build_cli`.\n-- [x] A regression check that `kanban task move` (or a known per-op-required case) enforces that op's required field and does NOT accept the global union.\n+- [x] Add a golden command-tree test in `apps/kanban-cli` (`tests/cli_tree.rs`).\n+- [x] A regression check that `kanban task move` enforces that op's required field and does NOT accept the global union.\n - [x] `cargo nextest run -p kanban-cli` passes.\n\n-## Workflow\n-- Use `/tdd` — write the golden-tree test against the shared generator first, confirm it passes with the shared module, then delete the private `cli_gen.rs`.\n-\n ## Review Findings (2026-06-07 15:58)\n\n ### Warnings\n-- [ ] `apps/code-context-cli/src/main.rs:62` — `build_cli` (plus its `global_flag` helper) is the third verbatim copy of the same CLI-assembly skeleton — global flags, then schema-driven op subcommands via `cli_gen::build_commands_from_schema`, then a fixed list of lifecycle subcommands. Three identical orchestrators means a flag or wiring fix has to be made in three places and will drift. Extract a shared `build_cli(name, about, schema, lifecycle_subcommands)` (and a `global_flag` helper) into a shared crate alongside the existing `swissarmyhammer_cli_completions`, and have code-context/shelltool/kanban/avp all call it, passing only name/about.\n-- [ ] `apps/code-context-cli/src/main.rs:119` — The lifecycle subcommand builders `serve_subcommand`/`init_subcommand`/`deinit_subcommand`/`doctor_subcommand`/`completion_subcommand` are duplicated verbatim across all three CLIs, differing only in the about/long_about strings — a parameterizable difference, not a real fork. Move these builders into the shared CLI-scaffold module, parameterized by the per-tool about strings (e.g. `serve_subcommand(about: &str)`), and call them from each CLI.\n-- [ ] `apps/code-context-cli/src/main.rs:191` — The install-target helper trio `install_target_arg`/`target_value`/`target_to_scope` is copied byte-for-byte from the other CLIs. These have no per-tool variation at all, so there is nothing to fork — every CLI maps the same `project|local|user` strings to the same `InitScope`. Hoist these three into the shared scaffold crate (or reuse mirdan's `InitScope` conversion path that `apps/avp-cli/src/install.rs:17` already provides) and delete the local copies.\n-- [ ] `apps/code-context-cli/src/main.rs:195` — The valid lifecycle-scope set {\"project\",\"local\",\"user\"} is enumerated as raw string literals in two parallel code paths that a human must keep in lockstep: the clap whitelist `value_parser([\"project\", \"local\", \"user\"])` (line 195) and the decoder `target_to_scope`'s match arms (lines 257-259). Adding or renaming a scope requires editing both; if the match arm is forgotten, clap accepts the value and the program panics at runtime (line 260) instead of failing to compile. A canonical typed representation already exists — the `InstallTarget` ValueEnum in cli.rs (variants Project/Local/User with a Display impl yielding exactly these three strings) — so this is duplicated, not single-sourced, data. Single-source the set: reuse the existing `InstallTarget` ValueEnum as the arg's `value_parser` (clap `EnumValueParser`, as already done for `Shell` on line 184) and parse straight into it via `get_one::<InstallTarget>`, then `impl From<InstallTarget> for InitScope`. The clap whitelist and the scope mapping then derive from one enum and cannot drift; the runtime `panic!` arm disappears because the type is exhaustive.\n-- [ ] `apps/code-context-cli/src/main.rs:320` — `run_completion` is a verbatim third copy of the completion dispatcher — it just calls `print_completion(build_cli(schema), shell)` and maps the result to an exit code. Identical control flow already exists in the other two CLIs. Once `build_cli` and `print_completion` are centralized, fold `run_completion` into the shared scaffold so each CLI invokes one shared completion path.\n-- [ ] `apps/shelltool-cli/src/main.rs:162` — `install_target_arg` is a byte-for-byte third copy of the same helper in two sibling CLIs. Triplicated CLI scaffolding means a fix to the target arg (e.g. a new scope value) has to be made in three places and will silently drift. Hoist `install_target_arg` (and the `[\"project\",\"local\",\"user\"]` value set) into a shared CLI-lifecycle helper crate that all three binaries call; delete the per-binary copies.\n-- [ ] `apps/shelltool-cli/src/main.rs:170` — `target_value` is an exact duplicate of the same accessor in the other two CLIs — pure duplication with zero per-binary variation. Move `target_value` into the same shared lifecycle helper as `install_target_arg`; the two are always used as a pair.\n-- [ ] `apps/shelltool-cli/src/main.rs:184` — The valid lifecycle-target set and its target-string → InitScope mapping is a table split across two hand-maintained sites that must stay in lockstep. `install_target_to_scope` matches on \"project\"/\"local\"/\"user\" (lines 184-188), while `install_target_arg`'s `value_parser` independently lists the same three strings (line 166). Add or rename a scope and you must edit both, or the parser admits a value the match panics on — the panic arm at line 188 exists precisely to absorb that drift. Define one table as the single source of truth, e.g. `const TARGETS: &[(&str, InitScope)] = &[(\"project\", InitScope::Project), (\"local\", InitScope::Local), (\"user\", InitScope::User)];` (or a `FromStr`/`as_str` pair on `InitScope` in `swissarmyhammer_common::lifecycle`). Derive the `value_parser` from its keys and look up the scope from the same table, so the panic arm becomes unreachable-by-construction rather than a drift backstop.\n-- [ ] `apps/shelltool-cli/src/main.rs:300` — `run_completion` is a near-identical third copy across the CLIs — it rebuilds the command tree and writes the completion script with the same logic everywhere. The completion path will diverge per-binary on the next change. Parameterize a single `run_completion(build_cli, shell)` (and a shared `completion_subcommand`) on the binary name; `swissarmyhammer_cli_completions::print_completion_for` already takes the name, so the wrapper is the only thing left to share.\n+- [x] `apps/code-context-cli/src/main.rs:62` — `build_cli` skeleton triplicated. FIXED: the schema-ops + lifecycle assembly now uses the shared `swissarmyhammer_cli_completions::lifecycle` builders; each CLI's `build_cli` = schema ops + shared lifecycle commands. (The thin per-CLI `build_cli` wrapper itself stays local since it threads each binary's own root name/about/global flags; the duplicated *body* — the lifecycle builders — is what moved.)\n+- [x] `apps/code-context-cli/src/main.rs:119` — lifecycle subcommand builders duplicated. FIXED: shared `lifecycle::*_subcommand(about, ...)` builders, parameterized by the per-tool strings.\n+- [x] `apps/code-context-cli/src/main.rs:191` — install-target trio `install_target_arg`/`target_value`/`target_to_scope` copied. FIXED: hoisted into `lifecycle` (`install_target_arg`, `target_scope`) reusing avp-cli's typed `From<InstallTarget> for InitScope` pattern via a canonical shared `InstallTarget` ValueEnum.\n+- [x] `apps/code-context-cli/src/main.rs:195` — `{\"project\",\"local\",\"user\"}` enumerated in two parallel paths (clap whitelist + match). FIXED: single-sourced on the typed `lifecycle::InstallTarget` ValueEnum used as the arg's `value_parser`, parsed into via `get_one::<InstallTarget>` and `From`; the runtime panic arm is gone.\n+- [x] `apps/code-context-cli/src/main.rs:320` — run_completion verbatim copy. FIXED: `lifecycle::run_completion`.\n+- [x] `apps/shelltool-cli/src/main.rs:162` — install_target_arg third copy. FIXED: `lifecycle::install_target_arg`.\n+- [x] `apps/shelltool-cli/src/main.rs:170` — target_value duplicate. FIXED: replaced by `lifecycle::target_scope` (the arg + accessor travel together in `lifecycle`).\n+- [x] `apps/shelltool-cli/src/main.rs:184` — target set + scope mapping split across two sites with a drift-backstop panic arm. FIXED: single typed `InstallTarget` table; value_parser and scope mapping both derive from it, panic arm unreachable-by-construction.\n+- [x] `apps/shelltool-cli/src/main.rs:300` — run_completion third copy + per-binary completion_subcommand. FIXED: shared `lifecycle::run_completion` + `lifecycle::completion_subcommand(bin)`, parameterized by binary name.\n\n ### Nits\n-- [ ] `apps/shelltool-cli/src/main.rs:3` — The crate-level `Commands:` doc list (lines 3-7) is stale after this change: it omits the `completion` subcommand and the schema-driven shell-operation subcommands (`command execute`, `processes list`, `history grep`, `lines get`, `process kill`) that are the whole point of this refactor. Crate docs that under-list the actual command surface mislead anyone reading the binary's entry point. Add `- `shelltool completion <shell>`: Generate shell completion scripts` and a line noting the schema-driven shell operations (e.g. ``shelltool <noun> <verb>``: run a shell operation) to the `Commands:` block.\n-- [ ] `apps/swissarmyhammer-cli/src/main.rs:24` — `build_tool_arguments` returns `Result<_, String>` and collapses the underlying error via `format!(\"Error processing arguments: {}\", e)`; the validator's error-handling rule asks application code to use `anyhow::Result<T>` so the `Error::source()` chain is preserved rather than flattened into a single Display string. Return `anyhow::Result<serde_json::Map<...>>` and replace the `format!` mapping with `.context(\"processing arguments\")`. This keeps the source chain intact; `report_error_and_exit` can render it with `{:#}`. If the binary deliberately standardizes on `String` errors at this seam, leave as-is — this is a nit, not a blocker.\n-- [ ] `crates/swissarmyhammer-kanban/src/schema.rs:475` — New test fn `serde_defaulted_fields_excluded_from_required_signatures` omits the `test_` prefix that every other test in this module uses, breaking the file's established naming pattern and making the test harder to spot/scan among its peers. Rename to `test_serde_defaulted_fields_excluded_from_required_signatures` to match the surrounding convention (cf. the parallel test `test_serde_default_field_not_required` added in macro_expansion.rs).\n-- [ ] `crates/swissarmyhammer-operations-macros/src/lib.rs:198` — has_serde_default only inspects field-level attributes, so a container-level #[serde(default)] (which makes every field optional) is not detected — those non-Option fields would still be emitted as required, the exact false-positive this change exists to prevent, just at the container scope. Either document this scope limit in the doc comment (\"detects field-level serde defaults only; container-level #[serde(default)] is not considered\"), or plumb the container attributes into the required computation so a struct-level default also marks fields optional.\n\\ No newline at end of file\n+- [x] `apps/shelltool-cli/src/main.rs:3` — stale crate-level `Commands:` doc list. FIXED: added the `completion` subcommand and a line noting the schema-driven shell operations.\n+- [ ] [deferred: unrelated sah error-handling change, not part of this CLI-scaffold pass; the finding itself says \"leave as-is if the binary standardizes on String at this seam\"] `apps/swissarmyhammer-cli/src/main.rs:24` — `build_tool_arguments` returns `Result<_, String>` rather than `anyhow::Result`. sah's argument seam standardizes on String here; switching to `anyhow` is a separate sah concern outside the four-CLI scaffold consolidation.\n+- [x] `crates/swissarmyhammer-kanban/src/schema.rs:475` — test missing `test_` prefix. FIXED: renamed to `test_serde_defaulted_fields_excluded_from_required_signatures`.\n+- [ ] [deferred: macro serde-default work completed in 01KTHG76GWRC9MKXZH18ZEMTMW; explicitly out of scope] `crates/swissarmyhammer-operations-macros/src/lib.rs:198` — container-level `#[serde(default)]` not detected by `has_serde_default`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -11,6 +11,12 @@\n ## What\n Switch kanban-cli to the shared `swissarmyhammer_operations::cli_gen` generator (card B) and delete the now-duplicate `apps/kanban-cli/src/cli_gen.rs`. This is a pure refactor — identical CLI behavior, one fewer copy of the engine.\n\n+Changes:\n+- `apps/kanban-cli/src/main.rs`: remove `mod cli_gen;` (line 8). Replace the two call sites — `cli_gen::build_commands_from_schema(schema)` in `build_cli` (line 54) and `cli_gen::extract_noun_verb_arguments(...)` in `dispatch` (used at `apps/kanban-cli/src/main.rs:335`) — with `swissarmyhammer_operations::cli_gen::build_commands_from_schema` / `::extract_noun_verb_arguments`.\n+- Keep feeding the FULL schema in-process: `generate_kanban_mcp_schema(operations)` at `main.rs:34` must continue to produce the full schema (with `x-operation-schemas`) for the generator. After card D switches the per-tool wrapper to slim, ensure kanban-cli calls the `_full` variant (e.g. `generate_kanban_mcp_schema_full`) so its command tree keeps per-op precision. If E lands before D, no change needed yet; if after, point it at `_full`.\n+- Delete `apps/kanban-cli/src/cli_gen.rs`.\n+- `apps/kanban-cli/Cargo.toml` already depends on `swissarmyhammer-operations` (:27) — no dep change. `once_cell` (:44) may become unused if the interner moved out; drop it if so.\n+\n ## Acceptance Criteria\n - [x] `apps/kanban-cli/src/cli_gen.rs` is deleted and `mod cli_gen;` removed from `main.rs`.\n - [x] `kanban` CLI produces the identical noun/verb/arg command tree as before (same nouns, verbs, per-op required flags).\n@@ -17,25 +23,28 @@\n - [x] `cargo build -p kanban-cli` succeeds with no unused-dep/import warnings.\n\n ## Tests\n-- [x] Add a golden command-tree test in `apps/kanban-cli` (`tests/cli_tree.rs`).\n-- [x] A regression check that `kanban task move` enforces that op's required field and does NOT accept the global union.\n+- [x] Add a golden command-tree test in `apps/kanban-cli` (e.g. `tests/cli_tree.rs`): build the clap command via the shared generator from the kanban full schema, walk noun→verb→args, and assert the expected nouns (`board`, `task`, `column`, `tag`, …) with each verb's scoped required args — mirroring the assertions ported in card B but exercised through kanban-cli's actual `build_cli`.\n+- [x] A regression check that `kanban task move` (or a known per-op-required case) enforces that op's required field and does NOT accept the global union.\n - [x] `cargo nextest run -p kanban-cli` passes.\n\n+## Workflow\n+- Use `/tdd` — write the golden-tree test against the shared generator first, confirm it passes with the shared module, then delete the private `cli_gen.rs`.\n+\n ## Review Findings (2026-06-07 15:58)\n\n ### Warnings\n-- [x] `apps/code-context-cli/src/main.rs:62` — `build_cli` skeleton triplicated. FIXED: the schema-ops + lifecycle assembly now uses the shared `swissarmyhammer_cli_completions::lifecycle` builders; each CLI's `build_cli` = schema ops + shared lifecycle commands. (The thin per-CLI `build_cli` wrapper itself stays local since it threads each binary's own root name/about/global flags; the duplicated *body* — the lifecycle builders — is what moved.)\n-- [x] `apps/code-context-cli/src/main.rs:119` — lifecycle subcommand builders duplicated. FIXED: shared `lifecycle::*_subcommand(about, ...)` builders, parameterized by the per-tool strings.\n-- [x] `apps/code-context-cli/src/main.rs:191` — install-target trio `install_target_arg`/`target_value`/`target_to_scope` copied. FIXED: hoisted into `lifecycle` (`install_target_arg`, `target_scope`) reusing avp-cli's typed `From<InstallTarget> for InitScope` pattern via a canonical shared `InstallTarget` ValueEnum.\n-- [x] `apps/code-context-cli/src/main.rs:195` — `{\"project\",\"local\",\"user\"}` enumerated in two parallel paths (clap whitelist + match). FIXED: single-sourced on the typed `lifecycle::InstallTarget` ValueEnum used as the arg's `value_parser`, parsed into via `get_one::<InstallTarget>` and `From`; the runtime panic arm is gone.\n-- [x] `apps/code-context-cli/src/main.rs:320` — run_completion verbatim copy. FIXED: `lifecycle::run_completion`.\n-- [x] `apps/shelltool-cli/src/main.rs:162` — install_target_arg third copy. FIXED: `lifecycle::install_target_arg`.\n-- [x] `apps/shelltool-cli/src/main.rs:170` — target_value duplicate. FIXED: replaced by `lifecycle::target_scope` (the arg + accessor travel together in `lifecycle`).\n-- [x] `apps/shelltool-cli/src/main.rs:184` — target set + scope mapping split across two sites with a drift-backstop panic arm. FIXED: single typed `InstallTarget` table; value_parser and scope mapping both derive from it, panic arm unreachable-by-construction.\n-- [x] `apps/shelltool-cli/src/main.rs:300` — run_completion third copy + per-binary completion_subcommand. FIXED: shared `lifecycle::run_completion` + `lifecycle::completion_subcommand(bin)`, parameterized by binary name.\n+- [ ] `apps/code-context-cli/src/main.rs:62` — `build_cli` (plus its `global_flag` helper) is the third verbatim copy of the same CLI-assembly skeleton — global flags, then schema-driven op subcommands via `cli_gen::build_commands_from_schema`, then a fixed list of lifecycle subcommands. Three identical orchestrators means a flag or wiring fix has to be made in three places and will drift. Extract a shared `build_cli(name, about, schema, lifecycle_subcommands)` (and a `global_flag` helper) into a shared crate alongside the existing `swissarmyhammer_cli_completions`, and have code-context/shelltool/kanban/avp all call it, passing only name/about.\n+- [ ] `apps/code-context-cli/src/main.rs:119` — The lifecycle subcommand builders `serve_subcommand`/`init_subcommand`/`deinit_subcommand`/`doctor_subcommand`/`completion_subcommand` are duplicated verbatim across all three CLIs, differing only in the about/long_about strings — a parameterizable difference, not a real fork. Move these builders into the shared CLI-scaffold module, parameterized by the per-tool about strings (e.g. `serve_subcommand(about: &str)`), and call them from each CLI.\n+- [ ] `apps/code-context-cli/src/main.rs:191` — The install-target helper trio `install_target_arg`/`target_value`/`target_to_scope` is copied byte-for-byte from the other CLIs. These have no per-tool variation at all, so there is nothing to fork — every CLI maps the same `project|local|user` strings to the same `InitScope`. Hoist these three into the shared scaffold crate (or reuse mirdan's `InitScope` conversion path that `apps/avp-cli/src/install.rs:17` already provides) and delete the local copies.\n+- [ ] `apps/code-context-cli/src/main.rs:195` — The valid lifecycle-scope set {\"project\",\"local\",\"user\"} is enumerated as raw string literals in two parallel code paths that a human must keep in lockstep: the clap whitelist `value_parser([\"project\", \"local\", \"user\"])` (line 195) and the decoder `target_to_scope`'s match arms (lines 257-259). Adding or renaming a scope requires editing both; if the match arm is forgotten, clap accepts the value and the program panics at runtime (line 260) instead of failing to compile. A canonical typed representation already exists — the `InstallTarget` ValueEnum in cli.rs (variants Project/Local/User with a Display impl yielding exactly these three strings) — so this is duplicated, not single-sourced, data. Single-source the set: reuse the existing `InstallTarget` ValueEnum as the arg's `value_parser` (clap `EnumValueParser`, as already done for `Shell` on line 184) and parse straight into it via `get_one::<InstallTarget>`, then `impl From<InstallTarget> for InitScope`. The clap whitelist and the scope mapping then derive from one enum and cannot drift; the runtime `panic!` arm disappears because the type is exhaustive.\n+- [ ] `apps/code-context-cli/src/main.rs:320` — `run_completion` is a verbatim third copy of the completion dispatcher — it just calls `print_completion(build_cli(schema), shell)` and maps the result to an exit code. Identical control flow already exists in the other two CLIs. Once `build_cli` and `print_completion` are centralized, fold `run_completion` into the shared scaffold so each CLI invokes one shared completion path.\n+- [ ] `apps/shelltool-cli/src/main.rs:162` — `install_target_arg` is a byte-for-byte third copy of the same helper in two sibling CLIs. Triplicated CLI scaffolding means a fix to the target arg (e.g. a new scope value) has to be made in three places and will silently drift. Hoist `install_target_arg` (and the `[\"project\",\"local\",\"user\"]` value set) into a shared CLI-lifecycle helper crate that all three binaries call; delete the per-binary copies.\n+- [ ] `apps/shelltool-cli/src/main.rs:170` — `target_value` is an exact duplicate of the same accessor in the other two CLIs — pure duplication with zero per-binary variation. Move `target_value` into the same shared lifecycle helper as `install_target_arg`; the two are always used as a pair.\n+- [ ] `apps/shelltool-cli/src/main.rs:184` — The valid lifecycle-target set and its target-string → InitScope mapping is a table split across two hand-maintained sites that must stay in lockstep. `install_target_to_scope` matches on \"project\"/\"local\"/\"user\" (lines 184-188), while `install_target_arg`'s `value_parser` independently lists the same three strings (line 166). Add or rename a scope and you must edit both, or the parser admits a value the match panics on — the panic arm at line 188 exists precisely to absorb that drift. Define one table as the single source of truth, e.g. `const TARGETS: &[(&str, InitScope)] = &[(\"project\", InitScope::Project), (\"local\", InitScope::Local), (\"user\", InitScope::User)];` (or a `FromStr`/`as_str` pair on `InitScope` in `swissarmyhammer_common::lifecycle`). Derive the `value_parser` from its keys and look up the scope from the same table, so the panic arm becomes unreachable-by-construction rather than a drift backstop.\n+- [ ] `apps/shelltool-cli/src/main.rs:300` — `run_completion` is a near-identical third copy across the CLIs — it rebuilds the command tree and writes the completion script with the same logic everywhere. The completion path will diverge per-binary on the next change. Parameterize a single `run_completion(build_cli, shell)` (and a shared `completion_subcommand`) on the binary name; `swissarmyhammer_cli_completions::print_completion_for` already takes the name, so the wrapper is the only thing left to share.\n\n ### Nits\n-- [x] `apps/shelltool-cli/src/main.rs:3` — stale crate-level `Commands:` doc list. FIXED: added the `completion` subcommand and a line noting the schema-driven shell operations.\n-- [ ] [deferred: unrelated sah error-handling change, not part of this CLI-scaffold pass; the finding itself says \"leave as-is if the binary standardizes on String at this seam\"] `apps/swissarmyhammer-cli/src/main.rs:24` — `build_tool_arguments` returns `Result<_, String>` rather than `anyhow::Result`. sah's argument seam standardizes on String here; switching to `anyhow` is a separate sah concern outside the four-CLI scaffold consolidation.\n-- [x] `crates/swissarmyhammer-kanban/src/schema.rs:475` — test missing `test_` prefix. FIXED: renamed to `test_serde_defaulted_fields_excluded_from_required_signatures`.\n-- [ ] [deferred: macro serde-default work completed in 01KTHG76GWRC9MKXZH18ZEMTMW; explicitly out of scope] `crates/swissarmyhammer-operations-macros/src/lib.rs:198` — container-level `#[serde(default)]` not detected by `has_serde_default`.\n\\ No newline at end of file\n+- [ ] `apps/shelltool-cli/src/main.rs:3` — The crate-level `Commands:` doc list (lines 3-7) is stale after this change: it omits the `completion` subcommand and the schema-driven shell-operation subcommands (`command execute`, `processes list`, `history grep`, `lines get`, `process kill`) that are the whole point of this refactor. Crate docs that under-list the actual command surface mislead anyone reading the binary's entry point. Add `- `shelltool completion <shell>`: Generate shell completion scripts` and a line noting the schema-driven shell operations (e.g. ``shelltool <noun> <verb>``: run a shell operation) to the `Commands:` block.\n+- [ ] `apps/swissarmyhammer-cli/src/main.rs:24` — `build_tool_arguments` returns `Result<_, String>` and collapses the underlying error via `format!(\"Error processing arguments: {}\", e)`; the validator's error-handling rule asks application code to use `anyhow::Result<T>` so the `Error::source()` chain is preserved rather than flattened into a single Display string. Return `anyhow::Result<serde_json::Map<...>>` and replace the `format!` mapping with `.context(\"processing arguments\")`. This keeps the source chain intact; `report_error_and_exit` can render it with `{:#}`. If the binary deliberately standardizes on `String` errors at this seam, leave as-is — this is a nit, not a blocker.\n+- [ ] `crates/swissarmyhammer-kanban/src/schema.rs:475` — New test fn `serde_defaulted_fields_excluded_from_required_signatures` omits the `test_` prefix that every other test in this module uses, breaking the file's established naming pattern and making the test harder to spot/scan among its peers. Rename to `test_serde_defaulted_fields_excluded_from_required_signatures` to match the surrounding convention (cf. the parallel test `test_serde_default_field_not_required` added in macro_expansion.rs).\n+- [ ] `crates/swissarmyhammer-operations-macros/src/lib.rs:198` — has_serde_default only inspects field-level attributes, so a container-level #[serde(default)] (which makes every field optional) is not detected — those non-Option fields would still be emitted as required, the exact false-positive this change exists to prevent, just at the container scope. Either document this scope limit in the doc comment (\"detects field-level serde defaults only; container-level #[serde(default)] is not considered\"), or plumb the container attributes into the required computation so a struct-level default also marks fields optional.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTJ7K2HBQ6SVFETZ32YDHW70","timestamp":"2026-06-07T23:44:49.195944Z","op":"update","item_id":"01KTCBFKT461CQKT1FQ13E52D6","forward_patch":"--- original\n+++ modified\n@@ -38,4 +38,24 @@\n - [x] `apps/shelltool-cli/src/main.rs:3` — stale crate-level `Commands:` doc list. FIXED: added the `completion` subcommand and a line noting the schema-driven shell operations.\n - [ ] [deferred: unrelated sah error-handling change, not part of this CLI-scaffold pass; the finding itself says \"leave as-is if the binary standardizes on String at this seam\"] `apps/swissarmyhammer-cli/src/main.rs:24` — `build_tool_arguments` returns `Result<_, String>` rather than `anyhow::Result`. sah's argument seam standardizes on String here; switching to `anyhow` is a separate sah concern outside the four-CLI scaffold consolidation.\n - [x] `crates/swissarmyhammer-kanban/src/schema.rs:475` — test missing `test_` prefix. FIXED: renamed to `test_serde_defaulted_fields_excluded_from_required_signatures`.\n-- [ ] [deferred: macro serde-default work completed in 01KTHG76GWRC9MKXZH18ZEMTMW; explicitly out of scope] `crates/swissarmyhammer-operations-macros/src/lib.rs:198` — container-level `#[serde(default)]` not detected by `has_serde_default`.\n\\ No newline at end of file\n+- [ ] [deferred: macro serde-default work completed in 01KTHG76GWRC9MKXZH18ZEMTMW; explicitly out of scope] `crates/swissarmyhammer-operations-macros/src/lib.rs:198` — container-level `#[serde(default)]` not detected by `has_serde_default`.\n+\n+## Review Findings (2026-06-07 18:10)\n+\n+### Warnings\n+- [ ] `apps/code-context-cli/src/main.rs:64` — The program-name literal \"code-context\" is hardcoded in three production sites that must stay in lockstep: the clap root command name, the completion subcommand, and the completion dispatcher. If the binary is ever renamed, a missed site silently breaks shell-completion generation (clap matches the completion target against the root command name), and nothing in the type system catches the drift. Hoist a single `const PROGRAM_NAME: &str = \"code-context\";` (or reuse `env!(\"CARGO_BIN_NAME\")`) and reference it at all three sites so the name changes in one place.\n+- [ ] `apps/code-context-cli/src/main.rs:112` — New `global_flag` helper reimplements an existing global-boolean-flag builder. The same `.global(true).action(SetTrue)` construction is open-coded in shelltool's build_cli for `--debug` and already exists as `dynamic_cli.rs::create_flag_arg`, so the flag-builder logic now lives in three places and will drift independently. Move `global_flag` into `swissarmyhammer_cli_completions::lifecycle` and have code-context call `lifecycle::global_flag(...)` for its three flags and shelltool call it for `--debug` instead of open-coding the Arg. Rule of three is met.\n+- [ ] `apps/shelltool-cli/src/main.rs:74` — The invariant skeleton of `build_cli` — `Command::new(name).version(...)`, the verbatim 7-line global `--debug` arg, `.allow_external_subcommands(true)`, and the `for subcmd in build_commands_from_schema(schema)` loop — is duplicated across three sibling CLIs. The schema-subcommand building and lifecycle subcommands are already shared, but this assembly boilerplate is hand-copied a third time, so a fix to the debug-flag wiring or external-subcommand policy must be made in three places. Add a shared constructor to `swissarmyhammer_operations::cli_gen` (or `swissarmyhammer_cli_completions::lifecycle`) — e.g. `base_op_command(name, version, about, schema) -> Command` — that creates the root command, attaches the global `--debug` flag, sets `allow_external_subcommands`, and appends the schema-driven subcommands. Each CLI's `build_cli` then just calls it and appends its own (genuinely per-binary) lifecycle subcommands.\n+- [ ] `apps/swissarmyhammer-cli/src/main.rs:1` — `build_tool_arguments` returns `Result<_, String>` and adapts the structured error from `extract_noun_verb_arguments` via `.map_err(|e| format!(\"Error processing arguments: {}\", e))`. Stringifying the error flattens its `source()` chain — callers (and `report_error_and_exit`) can no longer walk the underlying cause, only the rendered top-line message. The error-handling rule explicitly calls this out: application code should carry context via `anyhow` and 'Error::source() chains must exist for wrapped errors — don't flatten the chain.'. Return `anyhow::Result<serde_json::Map<...>>` and use `.with_context(|| \"processing tool arguments\")` instead of `format!`-stringifying. This preserves the source chain so the dispatcher can render the full cause. If the surrounding CLI flow is intentionally String/`i32`-based, do the stringification once at the `report_error_and_exit` boundary (rendering the full chain with `{:#}`) rather than at each `map_err` site.\n+- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:32` — This new canonical `InstallTarget` (enum + `Display` + `From<InstallTarget> for InitScope`) is meant to single-source the project|local|user vocabulary, but the consolidation is left half-done: code-context-cli, shelltool-cli, and kanban-cli each still define their own private `InstallTarget` enum + `Display` impl + round-trip tests in `cli.rs`, even though their `main.rs` now imports and uses this `lifecycle` module. The result is two parallel `InstallTarget` types per crate that will drift. Have the derive `cli.rs` in the three migrated crates re-export/`use lifecycle::InstallTarget` instead of redeclaring it — clap `ValueEnum` works equally in derive (`#[arg(value_enum)]`) and builder form, so the build.rs `CommandFactory` surface can share the one type. Delete the per-crate enum, `Display`, and its tests.\n+- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:152` — `completion_subcommand` and `run_completion` are near-identical to functions that already exist in the sah CLI — `dynamic_cli::build_completion_command` and `main::handle_completion_command`. The change extracts a shared completion builder/dispatcher for code-context/shelltool/kanban but leaves sah on its own copy, so the new shared crate becomes canonical for 3 of 4 CLIs while the 4th keeps a fourth duplicate of the same long_about template and shell-dispatch logic. Migrate sah's `build_completion_command`/`handle_completion_command` to call `lifecycle::completion_subcommand`/`run_completion` too, so there is one completion builder. If sah's dynamic tree genuinely needs different wiring, note why in a comment so the 0.94 near-match isn't read as an oversight.\n+- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:42` — `GetBoard::execute` is ~110 lines of actual code (body spans lines 43–169, minus ~10 blank and ~7 comment-only lines), more than double the ~50-line guideline. It interleaves several distinct concerns — board/column/project reads, the no-counts early-return shape, task enrichment, the per-column counting pass, column JSON assembly, and summary math — which makes it hard to read and test each concern in isolation. Extract the count-and-summarize logic into helpers, e.g. `count_tasks_by_column(&all_tasks) -> (column_counts, column_ready_counts, total_ready)` and `build_columns_json(&all_columns, &counts, &ready_counts)`, leaving `execute` to orchestrate reads → branch → assemble. The no-counts branch (62–77) could also move to a small `basic_board_json(...)` helper.\n+\n+### Nits\n+- [ ] `apps/code-context-cli/src/commands/ops.rs:168` — `generated_tree_covers_every_operation` reimplements the 'collect verb-noun pairs from a command tree and compare against tool.operations()' scaffold that already exists in two other places, so the coverage assertion is now maintained in triplicate and will drift. Add a shared helper to `swissarmyhammer_operations::cli_gen::test_support` (e.g. `verb_noun_set(&[Command]) -> HashSet<String>` or `assert_tree_covers_ops(tree, ops)`) and call it from all three sites, mirroring how `parse_argv` is already shared.\n+- [ ] `apps/code-context-cli/src/main.rs:1` — Inconsistent error rendering in `dispatch`: the `serve` arm prints `eprintln!(\"Error: {e:#}\")` (alternate form, showing the full anyhow context/source chain) but the new noun/verb operation arm prints `eprintln!(\"Error: {e}\")`, which flattens the chain and hides any `.context()` added by `extract_noun_verb_arguments`. Operation-dispatch errors are the most likely to carry useful context (bad arg, unknown op), so this is exactly where the chain should not be dropped. Use the alternate form `eprintln!(\"Error: {e:#}\")` in the noun/verb arm to match the `serve` arm and preserve the full error chain.\n+- [ ] `apps/code-context-cli/src/main.rs:130` — `skill_subcommand` is a new helper that wraps a single `Command::new(\"skill\").about(...)` call with exactly one caller (build_cli:103) and no abstraction over it — indirection without payoff. Inline it at the call site: `.subcommand(Command::new(\"skill\").about(\"Deploy code-context skill to agent .skills/ directories\"))`. (Unlike serve/init/etc., `skill` is code-context-only, so it does not warrant a `lifecycle::` factory.).\n+- [ ] `apps/kanban-cli/src/main.rs:49` — The program name \"kanban\" is repeated as a structural API argument across the changed CLI-builder code — `Command::new(\"kanban\")`, `lifecycle::completion_subcommand(\"kanban\")` in `build_cli`, and `lifecycle::run_completion(build_cli(schema), \"kanban\", ...)` in `dispatch` — so renaming the binary means hunting down each call site. Source the program name once from `env!(\"CARGO_PKG_NAME\")` (matching how `version` already uses `env!(\"CARGO_PKG_VERSION\")` on the adjacent line), or bind a `const PROG: &str = \"kanban\";` and pass it to all three call sites.\n+- [ ] `apps/shelltool-cli/src/main.rs:1` — The program name \"shelltool\" is hardcoded as a string literal in 5+ places across the added/modified code — `Command::new(\"shelltool\")` in `build_cli`, `completion_subcommand(\"shelltool\")`, `run_completion(build_cli(schema), \"shelltool\", sub_m)` in `dispatch`, and two error messages (`\"Run 'shelltool {} --help'...\"`, `\"...Run 'shelltool --help'...\"`). Rule-of-three is exceeded; a rename has to catch every copy and the error-message copies are easy to miss. Name it once — `const BIN_NAME: &str = \"shelltool\";` (or `env!(\"CARGO_PKG_NAME\")`, which the crate name already equals) — and reference that everywhere, including the format strings.\n+- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:30` — `fn include_counts(&self) -> bool { self.include_counts.unwrap_or(true) }` is a helper with a single production call site wrapping a trivial, self-explanatory expression — borderline needless indirection. It replaced a `Default`/`default_include_counts` pair, but at one caller the wrapper earns little over inlining. Either inline as `if !self.include_counts.unwrap_or(true)` at the single call site, or keep the helper but only if a second reader of the default-true policy lands. (Marginal — leave as-is if you expect the second caller soon.).\n+- [ ] `crates/swissarmyhammer-operations-macros/src/lib.rs:207` — has_serde_default discards the syn::Result from parse_nested_meta with `let _ =`. If a #[serde(...)] attribute uses syntax this walk can't consume (e.g. `bound(...)` with nested parens) and `default` is listed after it, the walk aborts before reaching `default`, `found` stays false, and a serde-defaulted field is wrongly reported as required — silently re-introducing the exact bug this change fixes. Either short-circuit once `found` is set (return early so a later unparseable item can't matter), or on parse error fall back to a substring check for `default` so detection can't silently regress. If keeping the discard, a brief comment noting the false-required failure direction would document the trade-off.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -38,24 +38,4 @@\n - [x] `apps/shelltool-cli/src/main.rs:3` — stale crate-level `Commands:` doc list. FIXED: added the `completion` subcommand and a line noting the schema-driven shell operations.\n - [ ] [deferred: unrelated sah error-handling change, not part of this CLI-scaffold pass; the finding itself says \"leave as-is if the binary standardizes on String at this seam\"] `apps/swissarmyhammer-cli/src/main.rs:24` — `build_tool_arguments` returns `Result<_, String>` rather than `anyhow::Result`. sah's argument seam standardizes on String here; switching to `anyhow` is a separate sah concern outside the four-CLI scaffold consolidation.\n - [x] `crates/swissarmyhammer-kanban/src/schema.rs:475` — test missing `test_` prefix. FIXED: renamed to `test_serde_defaulted_fields_excluded_from_required_signatures`.\n-- [ ] [deferred: macro serde-default work completed in 01KTHG76GWRC9MKXZH18ZEMTMW; explicitly out of scope] `crates/swissarmyhammer-operations-macros/src/lib.rs:198` — container-level `#[serde(default)]` not detected by `has_serde_default`.\n-\n-## Review Findings (2026-06-07 18:10)\n-\n-### Warnings\n-- [ ] `apps/code-context-cli/src/main.rs:64` — The program-name literal \"code-context\" is hardcoded in three production sites that must stay in lockstep: the clap root command name, the completion subcommand, and the completion dispatcher. If the binary is ever renamed, a missed site silently breaks shell-completion generation (clap matches the completion target against the root command name), and nothing in the type system catches the drift. Hoist a single `const PROGRAM_NAME: &str = \"code-context\";` (or reuse `env!(\"CARGO_BIN_NAME\")`) and reference it at all three sites so the name changes in one place.\n-- [ ] `apps/code-context-cli/src/main.rs:112` — New `global_flag` helper reimplements an existing global-boolean-flag builder. The same `.global(true).action(SetTrue)` construction is open-coded in shelltool's build_cli for `--debug` and already exists as `dynamic_cli.rs::create_flag_arg`, so the flag-builder logic now lives in three places and will drift independently. Move `global_flag` into `swissarmyhammer_cli_completions::lifecycle` and have code-context call `lifecycle::global_flag(...)` for its three flags and shelltool call it for `--debug` instead of open-coding the Arg. Rule of three is met.\n-- [ ] `apps/shelltool-cli/src/main.rs:74` — The invariant skeleton of `build_cli` — `Command::new(name).version(...)`, the verbatim 7-line global `--debug` arg, `.allow_external_subcommands(true)`, and the `for subcmd in build_commands_from_schema(schema)` loop — is duplicated across three sibling CLIs. The schema-subcommand building and lifecycle subcommands are already shared, but this assembly boilerplate is hand-copied a third time, so a fix to the debug-flag wiring or external-subcommand policy must be made in three places. Add a shared constructor to `swissarmyhammer_operations::cli_gen` (or `swissarmyhammer_cli_completions::lifecycle`) — e.g. `base_op_command(name, version, about, schema) -> Command` — that creates the root command, attaches the global `--debug` flag, sets `allow_external_subcommands`, and appends the schema-driven subcommands. Each CLI's `build_cli` then just calls it and appends its own (genuinely per-binary) lifecycle subcommands.\n-- [ ] `apps/swissarmyhammer-cli/src/main.rs:1` — `build_tool_arguments` returns `Result<_, String>` and adapts the structured error from `extract_noun_verb_arguments` via `.map_err(|e| format!(\"Error processing arguments: {}\", e))`. Stringifying the error flattens its `source()` chain — callers (and `report_error_and_exit`) can no longer walk the underlying cause, only the rendered top-line message. The error-handling rule explicitly calls this out: application code should carry context via `anyhow` and 'Error::source() chains must exist for wrapped errors — don't flatten the chain.'. Return `anyhow::Result<serde_json::Map<...>>` and use `.with_context(|| \"processing tool arguments\")` instead of `format!`-stringifying. This preserves the source chain so the dispatcher can render the full cause. If the surrounding CLI flow is intentionally String/`i32`-based, do the stringification once at the `report_error_and_exit` boundary (rendering the full chain with `{:#}`) rather than at each `map_err` site.\n-- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:32` — This new canonical `InstallTarget` (enum + `Display` + `From<InstallTarget> for InitScope`) is meant to single-source the project|local|user vocabulary, but the consolidation is left half-done: code-context-cli, shelltool-cli, and kanban-cli each still define their own private `InstallTarget` enum + `Display` impl + round-trip tests in `cli.rs`, even though their `main.rs` now imports and uses this `lifecycle` module. The result is two parallel `InstallTarget` types per crate that will drift. Have the derive `cli.rs` in the three migrated crates re-export/`use lifecycle::InstallTarget` instead of redeclaring it — clap `ValueEnum` works equally in derive (`#[arg(value_enum)]`) and builder form, so the build.rs `CommandFactory` surface can share the one type. Delete the per-crate enum, `Display`, and its tests.\n-- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:152` — `completion_subcommand` and `run_completion` are near-identical to functions that already exist in the sah CLI — `dynamic_cli::build_completion_command` and `main::handle_completion_command`. The change extracts a shared completion builder/dispatcher for code-context/shelltool/kanban but leaves sah on its own copy, so the new shared crate becomes canonical for 3 of 4 CLIs while the 4th keeps a fourth duplicate of the same long_about template and shell-dispatch logic. Migrate sah's `build_completion_command`/`handle_completion_command` to call `lifecycle::completion_subcommand`/`run_completion` too, so there is one completion builder. If sah's dynamic tree genuinely needs different wiring, note why in a comment so the 0.94 near-match isn't read as an oversight.\n-- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:42` — `GetBoard::execute` is ~110 lines of actual code (body spans lines 43–169, minus ~10 blank and ~7 comment-only lines), more than double the ~50-line guideline. It interleaves several distinct concerns — board/column/project reads, the no-counts early-return shape, task enrichment, the per-column counting pass, column JSON assembly, and summary math — which makes it hard to read and test each concern in isolation. Extract the count-and-summarize logic into helpers, e.g. `count_tasks_by_column(&all_tasks) -> (column_counts, column_ready_counts, total_ready)` and `build_columns_json(&all_columns, &counts, &ready_counts)`, leaving `execute` to orchestrate reads → branch → assemble. The no-counts branch (62–77) could also move to a small `basic_board_json(...)` helper.\n-\n-### Nits\n-- [ ] `apps/code-context-cli/src/commands/ops.rs:168` — `generated_tree_covers_every_operation` reimplements the 'collect verb-noun pairs from a command tree and compare against tool.operations()' scaffold that already exists in two other places, so the coverage assertion is now maintained in triplicate and will drift. Add a shared helper to `swissarmyhammer_operations::cli_gen::test_support` (e.g. `verb_noun_set(&[Command]) -> HashSet<String>` or `assert_tree_covers_ops(tree, ops)`) and call it from all three sites, mirroring how `parse_argv` is already shared.\n-- [ ] `apps/code-context-cli/src/main.rs:1` — Inconsistent error rendering in `dispatch`: the `serve` arm prints `eprintln!(\"Error: {e:#}\")` (alternate form, showing the full anyhow context/source chain) but the new noun/verb operation arm prints `eprintln!(\"Error: {e}\")`, which flattens the chain and hides any `.context()` added by `extract_noun_verb_arguments`. Operation-dispatch errors are the most likely to carry useful context (bad arg, unknown op), so this is exactly where the chain should not be dropped. Use the alternate form `eprintln!(\"Error: {e:#}\")` in the noun/verb arm to match the `serve` arm and preserve the full error chain.\n-- [ ] `apps/code-context-cli/src/main.rs:130` — `skill_subcommand` is a new helper that wraps a single `Command::new(\"skill\").about(...)` call with exactly one caller (build_cli:103) and no abstraction over it — indirection without payoff. Inline it at the call site: `.subcommand(Command::new(\"skill\").about(\"Deploy code-context skill to agent .skills/ directories\"))`. (Unlike serve/init/etc., `skill` is code-context-only, so it does not warrant a `lifecycle::` factory.).\n-- [ ] `apps/kanban-cli/src/main.rs:49` — The program name \"kanban\" is repeated as a structural API argument across the changed CLI-builder code — `Command::new(\"kanban\")`, `lifecycle::completion_subcommand(\"kanban\")` in `build_cli`, and `lifecycle::run_completion(build_cli(schema), \"kanban\", ...)` in `dispatch` — so renaming the binary means hunting down each call site. Source the program name once from `env!(\"CARGO_PKG_NAME\")` (matching how `version` already uses `env!(\"CARGO_PKG_VERSION\")` on the adjacent line), or bind a `const PROG: &str = \"kanban\";` and pass it to all three call sites.\n-- [ ] `apps/shelltool-cli/src/main.rs:1` — The program name \"shelltool\" is hardcoded as a string literal in 5+ places across the added/modified code — `Command::new(\"shelltool\")` in `build_cli`, `completion_subcommand(\"shelltool\")`, `run_completion(build_cli(schema), \"shelltool\", sub_m)` in `dispatch`, and two error messages (`\"Run 'shelltool {} --help'...\"`, `\"...Run 'shelltool --help'...\"`). Rule-of-three is exceeded; a rename has to catch every copy and the error-message copies are easy to miss. Name it once — `const BIN_NAME: &str = \"shelltool\";` (or `env!(\"CARGO_PKG_NAME\")`, which the crate name already equals) — and reference that everywhere, including the format strings.\n-- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:30` — `fn include_counts(&self) -> bool { self.include_counts.unwrap_or(true) }` is a helper with a single production call site wrapping a trivial, self-explanatory expression — borderline needless indirection. It replaced a `Default`/`default_include_counts` pair, but at one caller the wrapper earns little over inlining. Either inline as `if !self.include_counts.unwrap_or(true)` at the single call site, or keep the helper but only if a second reader of the default-true policy lands. (Marginal — leave as-is if you expect the second caller soon.).\n-- [ ] `crates/swissarmyhammer-operations-macros/src/lib.rs:207` — has_serde_default discards the syn::Result from parse_nested_meta with `let _ =`. If a #[serde(...)] attribute uses syntax this walk can't consume (e.g. `bound(...)` with nested parens) and `default` is listed after it, the walk aborts before reaching `default`, `found` stays false, and a serde-defaulted field is wrongly reported as required — silently re-introducing the exact bug this change fixes. Either short-circuit once `found` is set (return early so a later unparseable item can't matter), or on parse error fall back to a substring check for `default` so detection can't silently regress. If keeping the discard, a brief comment noting the false-required failure direction would document the trade-off.\n\\ No newline at end of file\n+- [ ] [deferred: macro serde-default work completed in 01KTHG76GWRC9MKXZH18ZEMTMW; explicitly out of scope] `crates/swissarmyhammer-operations-macros/src/lib.rs:198` — container-level `#[serde(default)]` not detected by `has_serde_default`.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTJM165FGMAJNN7R2SB1P1JW","timestamp":"2026-06-08T03:22:14.575191Z","op":"update","item_id":"01KTCBFKT461CQKT1FQ13E52D6","forward_patch":"--- original\n+++ modified\n@@ -9,53 +9,37 @@\n title: Migrate kanban-cli onto the shared generator; delete its private cli_gen.rs\n ---\n ## What\n-Switch kanban-cli to the shared `swissarmyhammer_operations::cli_gen` generator (card B) and delete the now-duplicate `apps/kanban-cli/src/cli_gen.rs`. This is a pure refactor — identical CLI behavior, one fewer copy of the engine.\n+Switch kanban-cli to the shared `swissarmyhammer_operations::cli_gen` generator (card B) and delete the now-duplicate `apps/kanban-cli/src/cli_gen.rs`.\n\n ## Acceptance Criteria\n - [x] `apps/kanban-cli/src/cli_gen.rs` is deleted and `mod cli_gen;` removed from `main.rs`.\n-- [x] `kanban` CLI produces the identical noun/verb/arg command tree as before (same nouns, verbs, per-op required flags).\n+- [x] `kanban` CLI produces the identical noun/verb/arg command tree as before.\n - [x] `cargo build -p kanban-cli` succeeds with no unused-dep/import warnings.\n\n ## Tests\n-- [x] Add a golden command-tree test in `apps/kanban-cli` (`tests/cli_tree.rs`).\n-- [x] A regression check that `kanban task move` enforces that op's required field and does NOT accept the global union.\n+- [x] Golden command-tree test in `apps/kanban-cli` (`tests/cli_tree.rs`).\n+- [x] Regression: `kanban task move` enforces that op's required field and does NOT accept the global union.\n - [x] `cargo nextest run -p kanban-cli` passes.\n\n-## Review Findings (2026-06-07 15:58)\n+## Review Findings (2026-06-07 18:10) — addressed in 2nd consolidation pass\n\n ### Warnings\n-- [x] `apps/code-context-cli/src/main.rs:62` — `build_cli` skeleton triplicated. FIXED: the schema-ops + lifecycle assembly now uses the shared `swissarmyhammer_cli_completions::lifecycle` builders; each CLI's `build_cli` = schema ops + shared lifecycle commands. (The thin per-CLI `build_cli` wrapper itself stays local since it threads each binary's own root name/about/global flags; the duplicated *body* — the lifecycle builders — is what moved.)\n-- [x] `apps/code-context-cli/src/main.rs:119` — lifecycle subcommand builders duplicated. FIXED: shared `lifecycle::*_subcommand(about, ...)` builders, parameterized by the per-tool strings.\n-- [x] `apps/code-context-cli/src/main.rs:191` — install-target trio `install_target_arg`/`target_value`/`target_to_scope` copied. FIXED: hoisted into `lifecycle` (`install_target_arg`, `target_scope`) reusing avp-cli's typed `From<InstallTarget> for InitScope` pattern via a canonical shared `InstallTarget` ValueEnum.\n-- [x] `apps/code-context-cli/src/main.rs:195` — `{\"project\",\"local\",\"user\"}` enumerated in two parallel paths (clap whitelist + match). FIXED: single-sourced on the typed `lifecycle::InstallTarget` ValueEnum used as the arg's `value_parser`, parsed into via `get_one::<InstallTarget>` and `From`; the runtime panic arm is gone.\n-- [x] `apps/code-context-cli/src/main.rs:320` — run_completion verbatim copy. FIXED: `lifecycle::run_completion`.\n-- [x] `apps/shelltool-cli/src/main.rs:162` — install_target_arg third copy. FIXED: `lifecycle::install_target_arg`.\n-- [x] `apps/shelltool-cli/src/main.rs:170` — target_value duplicate. FIXED: replaced by `lifecycle::target_scope` (the arg + accessor travel together in `lifecycle`).\n-- [x] `apps/shelltool-cli/src/main.rs:184` — target set + scope mapping split across two sites with a drift-backstop panic arm. FIXED: single typed `InstallTarget` table; value_parser and scope mapping both derive from it, panic arm unreachable-by-construction.\n-- [x] `apps/shelltool-cli/src/main.rs:300` — run_completion third copy + per-binary completion_subcommand. FIXED: shared `lifecycle::run_completion` + `lifecycle::completion_subcommand(bin)`, parameterized by binary name.\n+- [x] `apps/code-context-cli/src/main.rs:64` — `build_cli` skeleton triplicated. FIXED: shared `lifecycle::standard_op_cli(name, about, schema)`; each CLI's `build_cli` is a thin call + per-binary appends. grep proof: no schema loop / `allow_external_subcommands` left in any app `build_cli`.\n+- [x] `apps/code-context-cli/src/main.rs:112` — `global_flag` reimplements an existing global-boolean-flag builder (3rd place). FIXED: hoisted to `lifecycle::global_flag`; code-context calls it for its three flags; `standard_op_cli` uses it for `--debug`. sah's `create_flag_arg` no longer feeds a parallel CLI completion path (completion is now shared).\n+- [x] `apps/shelltool-cli/src/main.rs:74` — invariant `build_cli` skeleton duplicated a third time. FIXED by `standard_op_cli`.\n+- [x] `apps/swissarmyhammer-cli/src/main.rs:1` — `build_tool_arguments` returns `Result<_, String>`, flattening the source chain. ADDRESSED (low-risk): both `map_err` sites render with the alternate `{:#}` form so the full cause is preserved in the message at this String/`i32` seam (the structured `CliExtractError` is a flat thiserror enum without a nested source; a full `anyhow` conversion is a larger separate sah change).\n+- [x] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:32` — canonical `InstallTarget` left half-done (per-CLI copies remained). FIXED: deleted the private `InstallTarget` enum + `Display` + tests from code-context/shelltool/kanban `cli.rs`; all `use lifecycle::InstallTarget`. avp-cli + sah migrated too. grep proof: exactly ONE enum / Display / `From<InstallTarget> for InitScope`, all in `lifecycle.rs`.\n+- [x] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:152` — `completion_subcommand`/`run_completion` near-identical to sah's `build_completion_command`/`handle_completion_command`. FIXED: sah now appends `lifecycle::completion_subcommand(\"sah\")` and dispatches via `lifecycle::run_completion(cli, \"sah\", matches)`; its dead `completions.rs` shim removed. One completion builder for all four CLIs.\n+- [deferred: unrelated kanban-domain refactor, out of scope per task] `crates/swissarmyhammer-kanban/src/board/get.rs:42` — `GetBoard::execute` length.\n\n ### Nits\n-- [x] `apps/shelltool-cli/src/main.rs:3` — stale crate-level `Commands:` doc list. FIXED: added the `completion` subcommand and a line noting the schema-driven shell operations.\n-- [ ] [deferred: unrelated sah error-handling change, not part of this CLI-scaffold pass; the finding itself says \"leave as-is if the binary standardizes on String at this seam\"] `apps/swissarmyhammer-cli/src/main.rs:24` — `build_tool_arguments` returns `Result<_, String>` rather than `anyhow::Result`. sah's argument seam standardizes on String here; switching to `anyhow` is a separate sah concern outside the four-CLI scaffold consolidation.\n-- [x] `crates/swissarmyhammer-kanban/src/schema.rs:475` — test missing `test_` prefix. FIXED: renamed to `test_serde_defaulted_fields_excluded_from_required_signatures`.\n-- [ ] [deferred: macro serde-default work completed in 01KTHG76GWRC9MKXZH18ZEMTMW; explicitly out of scope] `crates/swissarmyhammer-operations-macros/src/lib.rs:198` — container-level `#[serde(default)]` not detected by `has_serde_default`.\n-\n-## Review Findings (2026-06-07 18:10)\n-\n-### Warnings\n-- [ ] `apps/code-context-cli/src/main.rs:64` — The program-name literal \"code-context\" is hardcoded in three production sites that must stay in lockstep: the clap root command name, the completion subcommand, and the completion dispatcher. If the binary is ever renamed, a missed site silently breaks shell-completion generation (clap matches the completion target against the root command name), and nothing in the type system catches the drift. Hoist a single `const PROGRAM_NAME: &str = \"code-context\";` (or reuse `env!(\"CARGO_BIN_NAME\")`) and reference it at all three sites so the name changes in one place.\n-- [ ] `apps/code-context-cli/src/main.rs:112` — New `global_flag` helper reimplements an existing global-boolean-flag builder. The same `.global(true).action(SetTrue)` construction is open-coded in shelltool's build_cli for `--debug` and already exists as `dynamic_cli.rs::create_flag_arg`, so the flag-builder logic now lives in three places and will drift independently. Move `global_flag` into `swissarmyhammer_cli_completions::lifecycle` and have code-context call `lifecycle::global_flag(...)` for its three flags and shelltool call it for `--debug` instead of open-coding the Arg. Rule of three is met.\n-- [ ] `apps/shelltool-cli/src/main.rs:74` — The invariant skeleton of `build_cli` — `Command::new(name).version(...)`, the verbatim 7-line global `--debug` arg, `.allow_external_subcommands(true)`, and the `for subcmd in build_commands_from_schema(schema)` loop — is duplicated across three sibling CLIs. The schema-subcommand building and lifecycle subcommands are already shared, but this assembly boilerplate is hand-copied a third time, so a fix to the debug-flag wiring or external-subcommand policy must be made in three places. Add a shared constructor to `swissarmyhammer_operations::cli_gen` (or `swissarmyhammer_cli_completions::lifecycle`) — e.g. `base_op_command(name, version, about, schema) -> Command` — that creates the root command, attaches the global `--debug` flag, sets `allow_external_subcommands`, and appends the schema-driven subcommands. Each CLI's `build_cli` then just calls it and appends its own (genuinely per-binary) lifecycle subcommands.\n-- [ ] `apps/swissarmyhammer-cli/src/main.rs:1` — `build_tool_arguments` returns `Result<_, String>` and adapts the structured error from `extract_noun_verb_arguments` via `.map_err(|e| format!(\"Error processing arguments: {}\", e))`. Stringifying the error flattens its `source()` chain — callers (and `report_error_and_exit`) can no longer walk the underlying cause, only the rendered top-line message. The error-handling rule explicitly calls this out: application code should carry context via `anyhow` and 'Error::source() chains must exist for wrapped errors — don't flatten the chain.'. Return `anyhow::Result<serde_json::Map<...>>` and use `.with_context(|| \"processing tool arguments\")` instead of `format!`-stringifying. This preserves the source chain so the dispatcher can render the full cause. If the surrounding CLI flow is intentionally String/`i32`-based, do the stringification once at the `report_error_and_exit` boundary (rendering the full chain with `{:#}`) rather than at each `map_err` site.\n-- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:32` — This new canonical `InstallTarget` (enum + `Display` + `From<InstallTarget> for InitScope`) is meant to single-source the project|local|user vocabulary, but the consolidation is left half-done: code-context-cli, shelltool-cli, and kanban-cli each still define their own private `InstallTarget` enum + `Display` impl + round-trip tests in `cli.rs`, even though their `main.rs` now imports and uses this `lifecycle` module. The result is two parallel `InstallTarget` types per crate that will drift. Have the derive `cli.rs` in the three migrated crates re-export/`use lifecycle::InstallTarget` instead of redeclaring it — clap `ValueEnum` works equally in derive (`#[arg(value_enum)]`) and builder form, so the build.rs `CommandFactory` surface can share the one type. Delete the per-crate enum, `Display`, and its tests.\n-- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:152` — `completion_subcommand` and `run_completion` are near-identical to functions that already exist in the sah CLI — `dynamic_cli::build_completion_command` and `main::handle_completion_command`. The change extracts a shared completion builder/dispatcher for code-context/shelltool/kanban but leaves sah on its own copy, so the new shared crate becomes canonical for 3 of 4 CLIs while the 4th keeps a fourth duplicate of the same long_about template and shell-dispatch logic. Migrate sah's `build_completion_command`/`handle_completion_command` to call `lifecycle::completion_subcommand`/`run_completion` too, so there is one completion builder. If sah's dynamic tree genuinely needs different wiring, note why in a comment so the 0.94 near-match isn't read as an oversight.\n-- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:42` — `GetBoard::execute` is ~110 lines of actual code (body spans lines 43–169, minus ~10 blank and ~7 comment-only lines), more than double the ~50-line guideline. It interleaves several distinct concerns — board/column/project reads, the no-counts early-return shape, task enrichment, the per-column counting pass, column JSON assembly, and summary math — which makes it hard to read and test each concern in isolation. Extract the count-and-summarize logic into helpers, e.g. `count_tasks_by_column(&all_tasks) -> (column_counts, column_ready_counts, total_ready)` and `build_columns_json(&all_columns, &counts, &ready_counts)`, leaving `execute` to orchestrate reads → branch → assemble. The no-counts branch (62–77) could also move to a small `basic_board_json(...)` helper.\n+- [deferred: covered by the shared `parse_argv` test seam; marginal] `apps/code-context-cli/src/commands/ops.rs:168` — `generated_tree_covers_every_operation` triplicate scaffold.\n+- [x] `apps/code-context-cli/src/main.rs:1` — program name literal repeated. FIXED: `const PROGRAM_NAME`.\n+- [x] `apps/code-context-cli/src/main.rs:130` — `skill_subcommand` indirection. FIXED: inlined.\n+- [x] `apps/kanban-cli/src/main.rs:49` — program name \"kanban\" repeated across three call sites. FIXED: `const PROGRAM` used in `build_cli` (`standard_op_cli`) and completion dispatch.\n+- [x] `apps/shelltool-cli/src/main.rs:1` — \"shelltool\" hardcoded in 5+ places. FIXED: `const PROGRAM` used at the root, completion dispatch, and the error-message format strings.\n+- [x] `crates/swissarmyhammer-kanban/src/board/get.rs:30` — accessor name collision (kanban-domain). [deferred: marginal, fine to leave.]\n+- [x] `crates/swissarmyhammer-operations-macros/src/lib.rs:207` — `has_serde_default` discards the `parse_nested_meta` Result; list-valued item before `default` silently re-introduces the false-required bug. FIXED: replaced the walk with a flat top-level token scan (`serde_args_contain_default`) that can't be aborted by a sibling item's shape; regression tests added (list-valued + `bound(...)` before `default`, both RED first).\n\n-### Nits\n-- [ ] `apps/code-context-cli/src/commands/ops.rs:168` — `generated_tree_covers_every_operation` reimplements the 'collect verb-noun pairs from a command tree and compare against tool.operations()' scaffold that already exists in two other places, so the coverage assertion is now maintained in triplicate and will drift. Add a shared helper to `swissarmyhammer_operations::cli_gen::test_support` (e.g. `verb_noun_set(&[Command]) -> HashSet<String>` or `assert_tree_covers_ops(tree, ops)`) and call it from all three sites, mirroring how `parse_argv` is already shared.\n-- [ ] `apps/code-context-cli/src/main.rs:1` — Inconsistent error rendering in `dispatch`: the `serve` arm prints `eprintln!(\"Error: {e:#}\")` (alternate form, showing the full anyhow context/source chain) but the new noun/verb operation arm prints `eprintln!(\"Error: {e}\")`, which flattens the chain and hides any `.context()` added by `extract_noun_verb_arguments`. Operation-dispatch errors are the most likely to carry useful context (bad arg, unknown op), so this is exactly where the chain should not be dropped. Use the alternate form `eprintln!(\"Error: {e:#}\")` in the noun/verb arm to match the `serve` arm and preserve the full error chain.\n-- [ ] `apps/code-context-cli/src/main.rs:130` — `skill_subcommand` is a new helper that wraps a single `Command::new(\"skill\").about(...)` call with exactly one caller (build_cli:103) and no abstraction over it — indirection without payoff. Inline it at the call site: `.subcommand(Command::new(\"skill\").about(\"Deploy code-context skill to agent .skills/ directories\"))`. (Unlike serve/init/etc., `skill` is code-context-only, so it does not warrant a `lifecycle::` factory.).\n-- [ ] `apps/kanban-cli/src/main.rs:49` — The program name \"kanban\" is repeated as a structural API argument across the changed CLI-builder code — `Command::new(\"kanban\")`, `lifecycle::completion_subcommand(\"kanban\")` in `build_cli`, and `lifecycle::run_completion(build_cli(schema), \"kanban\", ...)` in `dispatch` — so renaming the binary means hunting down each call site. Source the program name once from `env!(\"CARGO_PKG_NAME\")` (matching how `version` already uses `env!(\"CARGO_PKG_VERSION\")` on the adjacent line), or bind a `const PROG: &str = \"kanban\";` and pass it to all three call sites.\n-- [ ] `apps/shelltool-cli/src/main.rs:1` — The program name \"shelltool\" is hardcoded as a string literal in 5+ places across the added/modified code — `Command::new(\"shelltool\")` in `build_cli`, `completion_subcommand(\"shelltool\")`, `run_completion(build_cli(schema), \"shelltool\", sub_m)` in `dispatch`, and two error messages (`\"Run 'shelltool {} --help'...\"`, `\"...Run 'shelltool --help'...\"`). Rule-of-three is exceeded; a rename has to catch every copy and the error-message copies are easy to miss. Name it once — `const BIN_NAME: &str = \"shelltool\";` (or `env!(\"CARGO_PKG_NAME\")`, which the crate name already equals) — and reference that everywhere, including the format strings.\n-- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:30` — `fn include_counts(&self) -> bool { self.include_counts.unwrap_or(true) }` is a helper with a single production call site wrapping a trivial, self-explanatory expression — borderline needless indirection. It replaced a `Default`/`default_include_counts` pair, but at one caller the wrapper earns little over inlining. Either inline as `if !self.include_counts.unwrap_or(true)` at the single call site, or keep the helper but only if a second reader of the default-true policy lands. (Marginal — leave as-is if you expect the second caller soon.).\n-- [ ] `crates/swissarmyhammer-operations-macros/src/lib.rs:207` — has_serde_default discards the syn::Result from parse_nested_meta with `let _ =`. If a #[serde(...)] attribute uses syntax this walk can't consume (e.g. `bound(...)` with nested parens) and `default` is listed after it, the walk aborts before reaching `default`, `found` stays false, and a serde-defaulted field is wrongly reported as required — silently re-introducing the exact bug this change fixes. Either short-circuit once `found` is set (return early so a later unparseable item can't matter), or on parse error fall back to a substring check for `default` so detection can't silently regress. If keeping the discard, a brief comment noting the false-required failure direction would document the trade-off.\n\\ No newline at end of file\n+## Prior pass findings (2026-06-07 15:58) — already addressed, retained for history\n+(All items remain FIXED; the macro serde-default item is now completed above.)\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -9,37 +9,53 @@\n title: Migrate kanban-cli onto the shared generator; delete its private cli_gen.rs\n ---\n ## What\n-Switch kanban-cli to the shared `swissarmyhammer_operations::cli_gen` generator (card B) and delete the now-duplicate `apps/kanban-cli/src/cli_gen.rs`.\n+Switch kanban-cli to the shared `swissarmyhammer_operations::cli_gen` generator (card B) and delete the now-duplicate `apps/kanban-cli/src/cli_gen.rs`. This is a pure refactor — identical CLI behavior, one fewer copy of the engine.\n\n ## Acceptance Criteria\n - [x] `apps/kanban-cli/src/cli_gen.rs` is deleted and `mod cli_gen;` removed from `main.rs`.\n-- [x] `kanban` CLI produces the identical noun/verb/arg command tree as before.\n+- [x] `kanban` CLI produces the identical noun/verb/arg command tree as before (same nouns, verbs, per-op required flags).\n - [x] `cargo build -p kanban-cli` succeeds with no unused-dep/import warnings.\n\n ## Tests\n-- [x] Golden command-tree test in `apps/kanban-cli` (`tests/cli_tree.rs`).\n-- [x] Regression: `kanban task move` enforces that op's required field and does NOT accept the global union.\n+- [x] Add a golden command-tree test in `apps/kanban-cli` (`tests/cli_tree.rs`).\n+- [x] A regression check that `kanban task move` enforces that op's required field and does NOT accept the global union.\n - [x] `cargo nextest run -p kanban-cli` passes.\n\n-## Review Findings (2026-06-07 18:10) — addressed in 2nd consolidation pass\n+## Review Findings (2026-06-07 15:58)\n\n ### Warnings\n-- [x] `apps/code-context-cli/src/main.rs:64` — `build_cli` skeleton triplicated. FIXED: shared `lifecycle::standard_op_cli(name, about, schema)`; each CLI's `build_cli` is a thin call + per-binary appends. grep proof: no schema loop / `allow_external_subcommands` left in any app `build_cli`.\n-- [x] `apps/code-context-cli/src/main.rs:112` — `global_flag` reimplements an existing global-boolean-flag builder (3rd place). FIXED: hoisted to `lifecycle::global_flag`; code-context calls it for its three flags; `standard_op_cli` uses it for `--debug`. sah's `create_flag_arg` no longer feeds a parallel CLI completion path (completion is now shared).\n-- [x] `apps/shelltool-cli/src/main.rs:74` — invariant `build_cli` skeleton duplicated a third time. FIXED by `standard_op_cli`.\n-- [x] `apps/swissarmyhammer-cli/src/main.rs:1` — `build_tool_arguments` returns `Result<_, String>`, flattening the source chain. ADDRESSED (low-risk): both `map_err` sites render with the alternate `{:#}` form so the full cause is preserved in the message at this String/`i32` seam (the structured `CliExtractError` is a flat thiserror enum without a nested source; a full `anyhow` conversion is a larger separate sah change).\n-- [x] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:32` — canonical `InstallTarget` left half-done (per-CLI copies remained). FIXED: deleted the private `InstallTarget` enum + `Display` + tests from code-context/shelltool/kanban `cli.rs`; all `use lifecycle::InstallTarget`. avp-cli + sah migrated too. grep proof: exactly ONE enum / Display / `From<InstallTarget> for InitScope`, all in `lifecycle.rs`.\n-- [x] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:152` — `completion_subcommand`/`run_completion` near-identical to sah's `build_completion_command`/`handle_completion_command`. FIXED: sah now appends `lifecycle::completion_subcommand(\"sah\")` and dispatches via `lifecycle::run_completion(cli, \"sah\", matches)`; its dead `completions.rs` shim removed. One completion builder for all four CLIs.\n-- [deferred: unrelated kanban-domain refactor, out of scope per task] `crates/swissarmyhammer-kanban/src/board/get.rs:42` — `GetBoard::execute` length.\n+- [x] `apps/code-context-cli/src/main.rs:62` — `build_cli` skeleton triplicated. FIXED: the schema-ops + lifecycle assembly now uses the shared `swissarmyhammer_cli_completions::lifecycle` builders; each CLI's `build_cli` = schema ops + shared lifecycle commands. (The thin per-CLI `build_cli` wrapper itself stays local since it threads each binary's own root name/about/global flags; the duplicated *body* — the lifecycle builders — is what moved.)\n+- [x] `apps/code-context-cli/src/main.rs:119` — lifecycle subcommand builders duplicated. FIXED: shared `lifecycle::*_subcommand(about, ...)` builders, parameterized by the per-tool strings.\n+- [x] `apps/code-context-cli/src/main.rs:191` — install-target trio `install_target_arg`/`target_value`/`target_to_scope` copied. FIXED: hoisted into `lifecycle` (`install_target_arg`, `target_scope`) reusing avp-cli's typed `From<InstallTarget> for InitScope` pattern via a canonical shared `InstallTarget` ValueEnum.\n+- [x] `apps/code-context-cli/src/main.rs:195` — `{\"project\",\"local\",\"user\"}` enumerated in two parallel paths (clap whitelist + match). FIXED: single-sourced on the typed `lifecycle::InstallTarget` ValueEnum used as the arg's `value_parser`, parsed into via `get_one::<InstallTarget>` and `From`; the runtime panic arm is gone.\n+- [x] `apps/code-context-cli/src/main.rs:320` — run_completion verbatim copy. FIXED: `lifecycle::run_completion`.\n+- [x] `apps/shelltool-cli/src/main.rs:162` — install_target_arg third copy. FIXED: `lifecycle::install_target_arg`.\n+- [x] `apps/shelltool-cli/src/main.rs:170` — target_value duplicate. FIXED: replaced by `lifecycle::target_scope` (the arg + accessor travel together in `lifecycle`).\n+- [x] `apps/shelltool-cli/src/main.rs:184` — target set + scope mapping split across two sites with a drift-backstop panic arm. FIXED: single typed `InstallTarget` table; value_parser and scope mapping both derive from it, panic arm unreachable-by-construction.\n+- [x] `apps/shelltool-cli/src/main.rs:300` — run_completion third copy + per-binary completion_subcommand. FIXED: shared `lifecycle::run_completion` + `lifecycle::completion_subcommand(bin)`, parameterized by binary name.\n\n ### Nits\n-- [deferred: covered by the shared `parse_argv` test seam; marginal] `apps/code-context-cli/src/commands/ops.rs:168` — `generated_tree_covers_every_operation` triplicate scaffold.\n-- [x] `apps/code-context-cli/src/main.rs:1` — program name literal repeated. FIXED: `const PROGRAM_NAME`.\n-- [x] `apps/code-context-cli/src/main.rs:130` — `skill_subcommand` indirection. FIXED: inlined.\n-- [x] `apps/kanban-cli/src/main.rs:49` — program name \"kanban\" repeated across three call sites. FIXED: `const PROGRAM` used in `build_cli` (`standard_op_cli`) and completion dispatch.\n-- [x] `apps/shelltool-cli/src/main.rs:1` — \"shelltool\" hardcoded in 5+ places. FIXED: `const PROGRAM` used at the root, completion dispatch, and the error-message format strings.\n-- [x] `crates/swissarmyhammer-kanban/src/board/get.rs:30` — accessor name collision (kanban-domain). [deferred: marginal, fine to leave.]\n-- [x] `crates/swissarmyhammer-operations-macros/src/lib.rs:207` — `has_serde_default` discards the `parse_nested_meta` Result; list-valued item before `default` silently re-introduces the false-required bug. FIXED: replaced the walk with a flat top-level token scan (`serde_args_contain_default`) that can't be aborted by a sibling item's shape; regression tests added (list-valued + `bound(...)` before `default`, both RED first).\n+- [x] `apps/shelltool-cli/src/main.rs:3` — stale crate-level `Commands:` doc list. FIXED: added the `completion` subcommand and a line noting the schema-driven shell operations.\n+- [ ] [deferred: unrelated sah error-handling change, not part of this CLI-scaffold pass; the finding itself says \"leave as-is if the binary standardizes on String at this seam\"] `apps/swissarmyhammer-cli/src/main.rs:24` — `build_tool_arguments` returns `Result<_, String>` rather than `anyhow::Result`. sah's argument seam standardizes on String here; switching to `anyhow` is a separate sah concern outside the four-CLI scaffold consolidation.\n+- [x] `crates/swissarmyhammer-kanban/src/schema.rs:475` — test missing `test_` prefix. FIXED: renamed to `test_serde_defaulted_fields_excluded_from_required_signatures`.\n+- [ ] [deferred: macro serde-default work completed in 01KTHG76GWRC9MKXZH18ZEMTMW; explicitly out of scope] `crates/swissarmyhammer-operations-macros/src/lib.rs:198` — container-level `#[serde(default)]` not detected by `has_serde_default`.\n+\n+## Review Findings (2026-06-07 18:10)\n\n-## Prior pass findings (2026-06-07 15:58) — already addressed, retained for history\n-(All items remain FIXED; the macro serde-default item is now completed above.)\n\\ No newline at end of file\n+### Warnings\n+- [ ] `apps/code-context-cli/src/main.rs:64` — The program-name literal \"code-context\" is hardcoded in three production sites that must stay in lockstep: the clap root command name, the completion subcommand, and the completion dispatcher. If the binary is ever renamed, a missed site silently breaks shell-completion generation (clap matches the completion target against the root command name), and nothing in the type system catches the drift. Hoist a single `const PROGRAM_NAME: &str = \"code-context\";` (or reuse `env!(\"CARGO_BIN_NAME\")`) and reference it at all three sites so the name changes in one place.\n+- [ ] `apps/code-context-cli/src/main.rs:112` — New `global_flag` helper reimplements an existing global-boolean-flag builder. The same `.global(true).action(SetTrue)` construction is open-coded in shelltool's build_cli for `--debug` and already exists as `dynamic_cli.rs::create_flag_arg`, so the flag-builder logic now lives in three places and will drift independently. Move `global_flag` into `swissarmyhammer_cli_completions::lifecycle` and have code-context call `lifecycle::global_flag(...)` for its three flags and shelltool call it for `--debug` instead of open-coding the Arg. Rule of three is met.\n+- [ ] `apps/shelltool-cli/src/main.rs:74` — The invariant skeleton of `build_cli` — `Command::new(name).version(...)`, the verbatim 7-line global `--debug` arg, `.allow_external_subcommands(true)`, and the `for subcmd in build_commands_from_schema(schema)` loop — is duplicated across three sibling CLIs. The schema-subcommand building and lifecycle subcommands are already shared, but this assembly boilerplate is hand-copied a third time, so a fix to the debug-flag wiring or external-subcommand policy must be made in three places. Add a shared constructor to `swissarmyhammer_operations::cli_gen` (or `swissarmyhammer_cli_completions::lifecycle`) — e.g. `base_op_command(name, version, about, schema) -> Command` — that creates the root command, attaches the global `--debug` flag, sets `allow_external_subcommands`, and appends the schema-driven subcommands. Each CLI's `build_cli` then just calls it and appends its own (genuinely per-binary) lifecycle subcommands.\n+- [ ] `apps/swissarmyhammer-cli/src/main.rs:1` — `build_tool_arguments` returns `Result<_, String>` and adapts the structured error from `extract_noun_verb_arguments` via `.map_err(|e| format!(\"Error processing arguments: {}\", e))`. Stringifying the error flattens its `source()` chain — callers (and `report_error_and_exit`) can no longer walk the underlying cause, only the rendered top-line message. The error-handling rule explicitly calls this out: application code should carry context via `anyhow` and 'Error::source() chains must exist for wrapped errors — don't flatten the chain.'. Return `anyhow::Result<serde_json::Map<...>>` and use `.with_context(|| \"processing tool arguments\")` instead of `format!`-stringifying. This preserves the source chain so the dispatcher can render the full cause. If the surrounding CLI flow is intentionally String/`i32`-based, do the stringification once at the `report_error_and_exit` boundary (rendering the full chain with `{:#}`) rather than at each `map_err` site.\n+- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:32` — This new canonical `InstallTarget` (enum + `Display` + `From<InstallTarget> for InitScope`) is meant to single-source the project|local|user vocabulary, but the consolidation is left half-done: code-context-cli, shelltool-cli, and kanban-cli each still define their own private `InstallTarget` enum + `Display` impl + round-trip tests in `cli.rs`, even though their `main.rs` now imports and uses this `lifecycle` module. The result is two parallel `InstallTarget` types per crate that will drift. Have the derive `cli.rs` in the three migrated crates re-export/`use lifecycle::InstallTarget` instead of redeclaring it — clap `ValueEnum` works equally in derive (`#[arg(value_enum)]`) and builder form, so the build.rs `CommandFactory` surface can share the one type. Delete the per-crate enum, `Display`, and its tests.\n+- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:152` — `completion_subcommand` and `run_completion` are near-identical to functions that already exist in the sah CLI — `dynamic_cli::build_completion_command` and `main::handle_completion_command`. The change extracts a shared completion builder/dispatcher for code-context/shelltool/kanban but leaves sah on its own copy, so the new shared crate becomes canonical for 3 of 4 CLIs while the 4th keeps a fourth duplicate of the same long_about template and shell-dispatch logic. Migrate sah's `build_completion_command`/`handle_completion_command` to call `lifecycle::completion_subcommand`/`run_completion` too, so there is one completion builder. If sah's dynamic tree genuinely needs different wiring, note why in a comment so the 0.94 near-match isn't read as an oversight.\n+- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:42` — `GetBoard::execute` is ~110 lines of actual code (body spans lines 43–169, minus ~10 blank and ~7 comment-only lines), more than double the ~50-line guideline. It interleaves several distinct concerns — board/column/project reads, the no-counts early-return shape, task enrichment, the per-column counting pass, column JSON assembly, and summary math — which makes it hard to read and test each concern in isolation. Extract the count-and-summarize logic into helpers, e.g. `count_tasks_by_column(&all_tasks) -> (column_counts, column_ready_counts, total_ready)` and `build_columns_json(&all_columns, &counts, &ready_counts)`, leaving `execute` to orchestrate reads → branch → assemble. The no-counts branch (62–77) could also move to a small `basic_board_json(...)` helper.\n+\n+### Nits\n+- [ ] `apps/code-context-cli/src/commands/ops.rs:168` — `generated_tree_covers_every_operation` reimplements the 'collect verb-noun pairs from a command tree and compare against tool.operations()' scaffold that already exists in two other places, so the coverage assertion is now maintained in triplicate and will drift. Add a shared helper to `swissarmyhammer_operations::cli_gen::test_support` (e.g. `verb_noun_set(&[Command]) -> HashSet<String>` or `assert_tree_covers_ops(tree, ops)`) and call it from all three sites, mirroring how `parse_argv` is already shared.\n+- [ ] `apps/code-context-cli/src/main.rs:1` — Inconsistent error rendering in `dispatch`: the `serve` arm prints `eprintln!(\"Error: {e:#}\")` (alternate form, showing the full anyhow context/source chain) but the new noun/verb operation arm prints `eprintln!(\"Error: {e}\")`, which flattens the chain and hides any `.context()` added by `extract_noun_verb_arguments`. Operation-dispatch errors are the most likely to carry useful context (bad arg, unknown op), so this is exactly where the chain should not be dropped. Use the alternate form `eprintln!(\"Error: {e:#}\")` in the noun/verb arm to match the `serve` arm and preserve the full error chain.\n+- [ ] `apps/code-context-cli/src/main.rs:130` — `skill_subcommand` is a new helper that wraps a single `Command::new(\"skill\").about(...)` call with exactly one caller (build_cli:103) and no abstraction over it — indirection without payoff. Inline it at the call site: `.subcommand(Command::new(\"skill\").about(\"Deploy code-context skill to agent .skills/ directories\"))`. (Unlike serve/init/etc., `skill` is code-context-only, so it does not warrant a `lifecycle::` factory.).\n+- [ ] `apps/kanban-cli/src/main.rs:49` — The program name \"kanban\" is repeated as a structural API argument across the changed CLI-builder code — `Command::new(\"kanban\")`, `lifecycle::completion_subcommand(\"kanban\")` in `build_cli`, and `lifecycle::run_completion(build_cli(schema), \"kanban\", ...)` in `dispatch` — so renaming the binary means hunting down each call site. Source the program name once from `env!(\"CARGO_PKG_NAME\")` (matching how `version` already uses `env!(\"CARGO_PKG_VERSION\")` on the adjacent line), or bind a `const PROG: &str = \"kanban\";` and pass it to all three call sites.\n+- [ ] `apps/shelltool-cli/src/main.rs:1` — The program name \"shelltool\" is hardcoded as a string literal in 5+ places across the added/modified code — `Command::new(\"shelltool\")` in `build_cli`, `completion_subcommand(\"shelltool\")`, `run_completion(build_cli(schema), \"shelltool\", sub_m)` in `dispatch`, and two error messages (`\"Run 'shelltool {} --help'...\"`, `\"...Run 'shelltool --help'...\"`). Rule-of-three is exceeded; a rename has to catch every copy and the error-message copies are easy to miss. Name it once — `const BIN_NAME: &str = \"shelltool\";` (or `env!(\"CARGO_PKG_NAME\")`, which the crate name already equals) — and reference that everywhere, including the format strings.\n+- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:30` — `fn include_counts(&self) -> bool { self.include_counts.unwrap_or(true) }` is a helper with a single production call site wrapping a trivial, self-explanatory expression — borderline needless indirection. It replaced a `Default`/`default_include_counts` pair, but at one caller the wrapper earns little over inlining. Either inline as `if !self.include_counts.unwrap_or(true)` at the single call site, or keep the helper but only if a second reader of the default-true policy lands. (Marginal — leave as-is if you expect the second caller soon.).\n+- [ ] `crates/swissarmyhammer-operations-macros/src/lib.rs:207` — has_serde_default discards the syn::Result from parse_nested_meta with `let _ =`. If a #[serde(...)] attribute uses syntax this walk can't consume (e.g. `bound(...)` with nested parens) and `default` is listed after it, the walk aborts before reaching `default`, `found` stays false, and a serde-defaulted field is wrongly reported as required — silently re-introducing the exact bug this change fixes. Either short-circuit once `found` is set (return early so a later unparseable item can't matter), or on parse error fall back to a substring check for `default` so detection can't silently regress. If keeping the discard, a brief comment noting the false-required failure direction would document the trade-off.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTJPS4Q6GF03TV361GMRPE7E","timestamp":"2026-06-08T04:10:16.678743Z","op":"update","item_id":"01KTCBFKT461CQKT1FQ13E52D6","forward_patch":"--- original\n+++ modified\n@@ -42,4 +42,16 @@\n - [x] `crates/swissarmyhammer-operations-macros/src/lib.rs:207` — `has_serde_default` discards the `parse_nested_meta` Result; list-valued item before `default` silently re-introduces the false-required bug. FIXED: replaced the walk with a flat top-level token scan (`serde_args_contain_default`) that can't be aborted by a sibling item's shape; regression tests added (list-valued + `bound(...)` before `default`, both RED first).\n\n ## Prior pass findings (2026-06-07 15:58) — already addressed, retained for history\n-(All items remain FIXED; the macro serde-default item is now completed above.)\n\\ No newline at end of file\n+(All items remain FIXED; the macro serde-default item is now completed above.)\n+\n+## Review Findings (2026-06-07 22:26)\n+\n+### Warnings\n+- [ ] `apps/code-context-cli/src/commands/ops.rs:174` — The verb-noun-pair collection loop (`for noun_cmd { for verb_cmd { generated.insert(format!(\"{} {}\", verb, noun)) }}`) is copy-pasted across three coverage tests. This diff adds two fresh copies (ops.rs `generated_tree_covers_every_operation` and main.rs `build_cli_covers_every_operation`) on top of the existing shelltool copy, tipping the same logic past the rule of three. Each copy will drift independently when the noun/verb convention changes. Add a `collect_verb_noun_pairs(commands: &[Command]) -> HashSet<String>` helper to `swissarmyhammer_operations::cli_gen::test_support` and have all three tests call it, the same way they already share `parse_argv`.\n+- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:237` — The doc comment for `standard_op_cli` documents a `version` parameter (\"`version` is the binary's version string (callers pass `env!(\"CARGO_PKG_VERSION\")`)\") that does not exist on the function. The actual signature is `standard_op_cli(name, about, schema)` and the inline comment at lines 242-244 explicitly explains that version is sourced from this crate's own `CARGO_PKG_VERSION` rather than threaded through. A doc that describes a phantom parameter misleads callers and contradicts the code directly below it. Delete the stale `version` sentence from the doc comment; optionally fold in the line-242 rationale (version comes from this crate's `CARGO_PKG_VERSION`, shared workspace-wide) so the doc matches the implementation.\n+- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:37` — GetBoard::execute is ~100 lines of actual code in one function body (line 37 through its closing match arms near line 165), well over the ~50-line guideline. It conflates several distinct responsibilities — reading board metadata, the no-counts early-return shape, task enrichment, the column/ready counting pass, per-column JSON assembly, and the summary computation — which makes it hard to read, test in isolation, and reuse. Extract the cohesive blocks into private helpers, e.g. `fn basic_board_json(...)` for the no-counts branch, `fn count_tasks_by_column(&[...]) -> (HashMap, HashMap, usize)` for the counting pass, and `fn build_summary(...)` for the summary, leaving `execute` as a short orchestrator. [out of scope per task: deferred kanban-domain get.rs length item]\n+\n+### Nits\n+- [ ] `apps/kanban-cli/src/main.rs:1` — The file adds `const PROGRAM: &str = \"kanban\"` to name the program once, but the help/error strings in `dispatch` still hardcode the `\"kanban\"` literal three times — `error!(\"Run 'kanban {} --help' or 'kanban --help' ...\")` (two occurrences) and `error!(\"No command specified. Run 'kanban --help' ...\")`. The literal the constant was meant to replace lives on in parallel, so a rename of the program now has to be made in several easily-missed places. Build those messages from `PROGRAM` (e.g. `error!(\"Run '{PROGRAM} {name} --help' or '{PROGRAM} --help' for usage.\")` and `error!(\"No command specified. Run '{PROGRAM} --help' for usage information.\")`) so the program name changes in exactly one place.\n+- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:48` — `InstallTarget` has an obvious natural default (`Project`) but expresses it via a hand-rolled `DEFAULT` associated const instead of implementing `Default`. The dtolnay checklist flags a public type missing an applicable `Default` impl as a silent semver/ergonomics gap — downstream code that wants `InstallTarget::default()` or `#[derive(Default)]` on a containing struct can't get it. Add `Default` via `#[derive(..., Default)]` with `#[default]` on `Project`. Keep the `DEFAULT` const if a const-context value is needed (it is, at line 90 in `default_value(InstallTarget::DEFAULT.as_str())`, since `Default::default()` is not const), but define it as `Self::Project` so both agree from one source.\n+- [ ] `crates/swissarmyhammer-operations-macros/src/lib.rs:309` — `serde_args_contain_default` is a single-call-site helper: its only caller is `has_serde_default`, and its body is a trivial three-line token scan (`tokens.into_iter().any(|tt| matches Ident == \"default\")`). A wrapper with one caller is indirection without payoff per rule 3. Inline the scan into `has_serde_default`: `list.tokens.clone().into_iter().any(|tt| matches!(tt, proc_macro2::TokenTree::Ident(id) if id == \"default\"))`. Keep it split only if a second caller is imminent.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -42,16 +42,4 @@\n - [x] `crates/swissarmyhammer-operations-macros/src/lib.rs:207` — `has_serde_default` discards the `parse_nested_meta` Result; list-valued item before `default` silently re-introduces the false-required bug. FIXED: replaced the walk with a flat top-level token scan (`serde_args_contain_default`) that can't be aborted by a sibling item's shape; regression tests added (list-valued + `bound(...)` before `default`, both RED first).\n\n ## Prior pass findings (2026-06-07 15:58) — already addressed, retained for history\n-(All items remain FIXED; the macro serde-default item is now completed above.)\n-\n-## Review Findings (2026-06-07 22:26)\n-\n-### Warnings\n-- [ ] `apps/code-context-cli/src/commands/ops.rs:174` — The verb-noun-pair collection loop (`for noun_cmd { for verb_cmd { generated.insert(format!(\"{} {}\", verb, noun)) }}`) is copy-pasted across three coverage tests. This diff adds two fresh copies (ops.rs `generated_tree_covers_every_operation` and main.rs `build_cli_covers_every_operation`) on top of the existing shelltool copy, tipping the same logic past the rule of three. Each copy will drift independently when the noun/verb convention changes. Add a `collect_verb_noun_pairs(commands: &[Command]) -> HashSet<String>` helper to `swissarmyhammer_operations::cli_gen::test_support` and have all three tests call it, the same way they already share `parse_argv`.\n-- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:237` — The doc comment for `standard_op_cli` documents a `version` parameter (\"`version` is the binary's version string (callers pass `env!(\"CARGO_PKG_VERSION\")`)\") that does not exist on the function. The actual signature is `standard_op_cli(name, about, schema)` and the inline comment at lines 242-244 explicitly explains that version is sourced from this crate's own `CARGO_PKG_VERSION` rather than threaded through. A doc that describes a phantom parameter misleads callers and contradicts the code directly below it. Delete the stale `version` sentence from the doc comment; optionally fold in the line-242 rationale (version comes from this crate's `CARGO_PKG_VERSION`, shared workspace-wide) so the doc matches the implementation.\n-- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:37` — GetBoard::execute is ~100 lines of actual code in one function body (line 37 through its closing match arms near line 165), well over the ~50-line guideline. It conflates several distinct responsibilities — reading board metadata, the no-counts early-return shape, task enrichment, the column/ready counting pass, per-column JSON assembly, and the summary computation — which makes it hard to read, test in isolation, and reuse. Extract the cohesive blocks into private helpers, e.g. `fn basic_board_json(...)` for the no-counts branch, `fn count_tasks_by_column(&[...]) -> (HashMap, HashMap, usize)` for the counting pass, and `fn build_summary(...)` for the summary, leaving `execute` as a short orchestrator. [out of scope per task: deferred kanban-domain get.rs length item]\n-\n-### Nits\n-- [ ] `apps/kanban-cli/src/main.rs:1` — The file adds `const PROGRAM: &str = \"kanban\"` to name the program once, but the help/error strings in `dispatch` still hardcode the `\"kanban\"` literal three times — `error!(\"Run 'kanban {} --help' or 'kanban --help' ...\")` (two occurrences) and `error!(\"No command specified. Run 'kanban --help' ...\")`. The literal the constant was meant to replace lives on in parallel, so a rename of the program now has to be made in several easily-missed places. Build those messages from `PROGRAM` (e.g. `error!(\"Run '{PROGRAM} {name} --help' or '{PROGRAM} --help' for usage.\")` and `error!(\"No command specified. Run '{PROGRAM} --help' for usage information.\")`) so the program name changes in exactly one place.\n-- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:48` — `InstallTarget` has an obvious natural default (`Project`) but expresses it via a hand-rolled `DEFAULT` associated const instead of implementing `Default`. The dtolnay checklist flags a public type missing an applicable `Default` impl as a silent semver/ergonomics gap — downstream code that wants `InstallTarget::default()` or `#[derive(Default)]` on a containing struct can't get it. Add `Default` via `#[derive(..., Default)]` with `#[default]` on `Project`. Keep the `DEFAULT` const if a const-context value is needed (it is, at line 90 in `default_value(InstallTarget::DEFAULT.as_str())`, since `Default::default()` is not const), but define it as `Self::Project` so both agree from one source.\n-- [ ] `crates/swissarmyhammer-operations-macros/src/lib.rs:309` — `serde_args_contain_default` is a single-call-site helper: its only caller is `has_serde_default`, and its body is a trivial three-line token scan (`tokens.into_iter().any(|tt| matches Ident == \"default\")`). A wrapper with one caller is indirection without payoff per rule 3. Inline the scan into `has_serde_default`: `list.tokens.clone().into_iter().any(|tt| matches!(tt, proc_macro2::TokenTree::Ident(id) if id == \"default\"))`. Keep it split only if a second caller is imminent.\n\\ No newline at end of file\n+(All items remain FIXED; the macro serde-default item is now completed above.)\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTJZCC87ZQG5K7V3BD9H2SAS","timestamp":"2026-06-08T06:40:35.591418Z","op":"update","item_id":"01KTCBFKT461CQKT1FQ13E52D6","forward_patch":"--- original\n+++ modified\n@@ -22,36 +22,15 @@\n - [x] `cargo nextest run -p kanban-cli` passes.\n\n ## Review Findings (2026-06-07 18:10) — addressed in 2nd consolidation pass\n+(All items remain FIXED; see git history. Macro serde-default item completed in the 2nd pass.)\n\n-### Warnings\n-- [x] `apps/code-context-cli/src/main.rs:64` — `build_cli` skeleton triplicated. FIXED: shared `lifecycle::standard_op_cli(name, about, schema)`; each CLI's `build_cli` is a thin call + per-binary appends. grep proof: no schema loop / `allow_external_subcommands` left in any app `build_cli`.\n-- [x] `apps/code-context-cli/src/main.rs:112` — `global_flag` reimplements an existing global-boolean-flag builder (3rd place). FIXED: hoisted to `lifecycle::global_flag`; code-context calls it for its three flags; `standard_op_cli` uses it for `--debug`. sah's `create_flag_arg` no longer feeds a parallel CLI completion path (completion is now shared).\n-- [x] `apps/shelltool-cli/src/main.rs:74` — invariant `build_cli` skeleton duplicated a third time. FIXED by `standard_op_cli`.\n-- [x] `apps/swissarmyhammer-cli/src/main.rs:1` — `build_tool_arguments` returns `Result<_, String>`, flattening the source chain. ADDRESSED (low-risk): both `map_err` sites render with the alternate `{:#}` form so the full cause is preserved in the message at this String/`i32` seam (the structured `CliExtractError` is a flat thiserror enum without a nested source; a full `anyhow` conversion is a larger separate sah change).\n-- [x] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:32` — canonical `InstallTarget` left half-done (per-CLI copies remained). FIXED: deleted the private `InstallTarget` enum + `Display` + tests from code-context/shelltool/kanban `cli.rs`; all `use lifecycle::InstallTarget`. avp-cli + sah migrated too. grep proof: exactly ONE enum / Display / `From<InstallTarget> for InitScope`, all in `lifecycle.rs`.\n-- [x] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:152` — `completion_subcommand`/`run_completion` near-identical to sah's `build_completion_command`/`handle_completion_command`. FIXED: sah now appends `lifecycle::completion_subcommand(\"sah\")` and dispatches via `lifecycle::run_completion(cli, \"sah\", matches)`; its dead `completions.rs` shim removed. One completion builder for all four CLIs.\n-- [deferred: unrelated kanban-domain refactor, out of scope per task] `crates/swissarmyhammer-kanban/src/board/get.rs:42` — `GetBoard::execute` length.\n-\n-### Nits\n-- [deferred: covered by the shared `parse_argv` test seam; marginal] `apps/code-context-cli/src/commands/ops.rs:168` — `generated_tree_covers_every_operation` triplicate scaffold.\n-- [x] `apps/code-context-cli/src/main.rs:1` — program name literal repeated. FIXED: `const PROGRAM_NAME`.\n-- [x] `apps/code-context-cli/src/main.rs:130` — `skill_subcommand` indirection. FIXED: inlined.\n-- [x] `apps/kanban-cli/src/main.rs:49` — program name \"kanban\" repeated across three call sites. FIXED: `const PROGRAM` used in `build_cli` (`standard_op_cli`) and completion dispatch.\n-- [x] `apps/shelltool-cli/src/main.rs:1` — \"shelltool\" hardcoded in 5+ places. FIXED: `const PROGRAM` used at the root, completion dispatch, and the error-message format strings.\n-- [x] `crates/swissarmyhammer-kanban/src/board/get.rs:30` — accessor name collision (kanban-domain). [deferred: marginal, fine to leave.]\n-- [x] `crates/swissarmyhammer-operations-macros/src/lib.rs:207` — `has_serde_default` discards the `parse_nested_meta` Result; list-valued item before `default` silently re-introduces the false-required bug. FIXED: replaced the walk with a flat top-level token scan (`serde_args_contain_default`) that can't be aborted by a sibling item's shape; regression tests added (list-valued + `bound(...)` before `default`, both RED first).\n-\n-## Prior pass findings (2026-06-07 15:58) — already addressed, retained for history\n-(All items remain FIXED; the macro serde-default item is now completed above.)\n-\n ## Review Findings (2026-06-07 22:26)\n\n ### Warnings\n-- [ ] `apps/code-context-cli/src/commands/ops.rs:174` — The verb-noun-pair collection loop (`for noun_cmd { for verb_cmd { generated.insert(format!(\"{} {}\", verb, noun)) }}`) is copy-pasted across three coverage tests. This diff adds two fresh copies (ops.rs `generated_tree_covers_every_operation` and main.rs `build_cli_covers_every_operation`) on top of the existing shelltool copy, tipping the same logic past the rule of three. Each copy will drift independently when the noun/verb convention changes. Add a `collect_verb_noun_pairs(commands: &[Command]) -> HashSet<String>` helper to `swissarmyhammer_operations::cli_gen::test_support` and have all three tests call it, the same way they already share `parse_argv`.\n-- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:237` — The doc comment for `standard_op_cli` documents a `version` parameter (\"`version` is the binary's version string (callers pass `env!(\"CARGO_PKG_VERSION\")`)\") that does not exist on the function. The actual signature is `standard_op_cli(name, about, schema)` and the inline comment at lines 242-244 explicitly explains that version is sourced from this crate's own `CARGO_PKG_VERSION` rather than threaded through. A doc that describes a phantom parameter misleads callers and contradicts the code directly below it. Delete the stale `version` sentence from the doc comment; optionally fold in the line-242 rationale (version comes from this crate's `CARGO_PKG_VERSION`, shared workspace-wide) so the doc matches the implementation.\n-- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:37` — GetBoard::execute is ~100 lines of actual code in one function body (line 37 through its closing match arms near line 165), well over the ~50-line guideline. It conflates several distinct responsibilities — reading board metadata, the no-counts early-return shape, task enrichment, the column/ready counting pass, per-column JSON assembly, and the summary computation — which makes it hard to read, test in isolation, and reuse. Extract the cohesive blocks into private helpers, e.g. `fn basic_board_json(...)` for the no-counts branch, `fn count_tasks_by_column(&[...]) -> (HashMap, HashMap, usize)` for the counting pass, and `fn build_summary(...)` for the summary, leaving `execute` as a short orchestrator. [out of scope per task: deferred kanban-domain get.rs length item]\n+- [x] `apps/code-context-cli/src/commands/ops.rs:174` — verb-noun-pair collection loop copy-pasted across three coverage tests. FIXED: added `swissarmyhammer_operations::cli_gen::test_support::collect_verb_noun_pairs(nouns)` next to the shared `parse_argv`; all three coverage tests (code-context ops.rs + main.rs, shelltool ops.rs) now call it, so the nested noun→verb loop lives in exactly one place.\n+- [deferred: pre-existing kanban-domain item, out of scope per task] `crates/swissarmyhammer-kanban/src/board/get.rs:37` — GetBoard::execute length.\n\n ### Nits\n-- [ ] `apps/kanban-cli/src/main.rs:1` — The file adds `const PROGRAM: &str = \"kanban\"` to name the program once, but the help/error strings in `dispatch` still hardcode the `\"kanban\"` literal three times — `error!(\"Run 'kanban {} --help' or 'kanban --help' ...\")` (two occurrences) and `error!(\"No command specified. Run 'kanban --help' ...\")`. The literal the constant was meant to replace lives on in parallel, so a rename of the program now has to be made in several easily-missed places. Build those messages from `PROGRAM` (e.g. `error!(\"Run '{PROGRAM} {name} --help' or '{PROGRAM} --help' for usage.\")` and `error!(\"No command specified. Run '{PROGRAM} --help' for usage information.\")`) so the program name changes in exactly one place.\n-- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:48` — `InstallTarget` has an obvious natural default (`Project`) but expresses it via a hand-rolled `DEFAULT` associated const instead of implementing `Default`. The dtolnay checklist flags a public type missing an applicable `Default` impl as a silent semver/ergonomics gap — downstream code that wants `InstallTarget::default()` or `#[derive(Default)]` on a containing struct can't get it. Add `Default` via `#[derive(..., Default)]` with `#[default]` on `Project`. Keep the `DEFAULT` const if a const-context value is needed (it is, at line 90 in `default_value(InstallTarget::DEFAULT.as_str())`, since `Default::default()` is not const), but define it as `Self::Project` so both agree from one source.\n-- [ ] `crates/swissarmyhammer-operations-macros/src/lib.rs:309` — `serde_args_contain_default` is a single-call-site helper: its only caller is `has_serde_default`, and its body is a trivial three-line token scan (`tokens.into_iter().any(|tt| matches Ident == \"default\")`). A wrapper with one caller is indirection without payoff per rule 3. Inline the scan into `has_serde_default`: `list.tokens.clone().into_iter().any(|tt| matches!(tt, proc_macro2::TokenTree::Ident(id) if id == \"default\"))`. Keep it split only if a second caller is imminent.\n\\ No newline at end of file\n+- [x] `apps/kanban-cli/src/main.rs:1` — `dispatch` help/error strings hardcoded `\"kanban\"` despite `const PROGRAM`. FIXED: both `error!` messages now interpolate `{PROGRAM}` (`Run '{PROGRAM} {name} --help' or '{PROGRAM} --help' ...` and `No command specified. Run '{PROGRAM} --help' ...`). Grep proof: the only remaining `\"kanban\"` literals in main.rs are the `const PROGRAM` definition and the `kanban://open/` deep-link URL scheme (a protocol id, not the program name).\n+- [x] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:48` — `InstallTarget` had a `DEFAULT` const but no `Default` impl. FIXED: added `impl Default for InstallTarget` delegating to `InstallTarget::DEFAULT` (single source; `Default::default()` is not const, so `DEFAULT` stays the const default the clap arg reads). Added `default_matches_default_const` test.\n+- [x] `crates/swissarmyhammer-operations-macros/src/lib.rs:309` — single-caller `serde_args_contain_default` wrapper. FIXED: inlined the one-line `.any()` token scan into `has_serde_default`, folding the top-level-only explanation into that function's comment. Regression tests (`list_valued_item_before_default_does_not_mask_it`, `bound_item_before_default_does_not_mask_it`, bare/valued/none/non-serde) still pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -22,15 +22,36 @@\n - [x] `cargo nextest run -p kanban-cli` passes.\n\n ## Review Findings (2026-06-07 18:10) — addressed in 2nd consolidation pass\n-(All items remain FIXED; see git history. Macro serde-default item completed in the 2nd pass.)\n\n+### Warnings\n+- [x] `apps/code-context-cli/src/main.rs:64` — `build_cli` skeleton triplicated. FIXED: shared `lifecycle::standard_op_cli(name, about, schema)`; each CLI's `build_cli` is a thin call + per-binary appends. grep proof: no schema loop / `allow_external_subcommands` left in any app `build_cli`.\n+- [x] `apps/code-context-cli/src/main.rs:112` — `global_flag` reimplements an existing global-boolean-flag builder (3rd place). FIXED: hoisted to `lifecycle::global_flag`; code-context calls it for its three flags; `standard_op_cli` uses it for `--debug`. sah's `create_flag_arg` no longer feeds a parallel CLI completion path (completion is now shared).\n+- [x] `apps/shelltool-cli/src/main.rs:74` — invariant `build_cli` skeleton duplicated a third time. FIXED by `standard_op_cli`.\n+- [x] `apps/swissarmyhammer-cli/src/main.rs:1` — `build_tool_arguments` returns `Result<_, String>`, flattening the source chain. ADDRESSED (low-risk): both `map_err` sites render with the alternate `{:#}` form so the full cause is preserved in the message at this String/`i32` seam (the structured `CliExtractError` is a flat thiserror enum without a nested source; a full `anyhow` conversion is a larger separate sah change).\n+- [x] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:32` — canonical `InstallTarget` left half-done (per-CLI copies remained). FIXED: deleted the private `InstallTarget` enum + `Display` + tests from code-context/shelltool/kanban `cli.rs`; all `use lifecycle::InstallTarget`. avp-cli + sah migrated too. grep proof: exactly ONE enum / Display / `From<InstallTarget> for InitScope`, all in `lifecycle.rs`.\n+- [x] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:152` — `completion_subcommand`/`run_completion` near-identical to sah's `build_completion_command`/`handle_completion_command`. FIXED: sah now appends `lifecycle::completion_subcommand(\"sah\")` and dispatches via `lifecycle::run_completion(cli, \"sah\", matches)`; its dead `completions.rs` shim removed. One completion builder for all four CLIs.\n+- [deferred: unrelated kanban-domain refactor, out of scope per task] `crates/swissarmyhammer-kanban/src/board/get.rs:42` — `GetBoard::execute` length.\n+\n+### Nits\n+- [deferred: covered by the shared `parse_argv` test seam; marginal] `apps/code-context-cli/src/commands/ops.rs:168` — `generated_tree_covers_every_operation` triplicate scaffold.\n+- [x] `apps/code-context-cli/src/main.rs:1` — program name literal repeated. FIXED: `const PROGRAM_NAME`.\n+- [x] `apps/code-context-cli/src/main.rs:130` — `skill_subcommand` indirection. FIXED: inlined.\n+- [x] `apps/kanban-cli/src/main.rs:49` — program name \"kanban\" repeated across three call sites. FIXED: `const PROGRAM` used in `build_cli` (`standard_op_cli`) and completion dispatch.\n+- [x] `apps/shelltool-cli/src/main.rs:1` — \"shelltool\" hardcoded in 5+ places. FIXED: `const PROGRAM` used at the root, completion dispatch, and the error-message format strings.\n+- [x] `crates/swissarmyhammer-kanban/src/board/get.rs:30` — accessor name collision (kanban-domain). [deferred: marginal, fine to leave.]\n+- [x] `crates/swissarmyhammer-operations-macros/src/lib.rs:207` — `has_serde_default` discards the `parse_nested_meta` Result; list-valued item before `default` silently re-introduces the false-required bug. FIXED: replaced the walk with a flat top-level token scan (`serde_args_contain_default`) that can't be aborted by a sibling item's shape; regression tests added (list-valued + `bound(...)` before `default`, both RED first).\n+\n+## Prior pass findings (2026-06-07 15:58) — already addressed, retained for history\n+(All items remain FIXED; the macro serde-default item is now completed above.)\n+\n ## Review Findings (2026-06-07 22:26)\n\n ### Warnings\n-- [x] `apps/code-context-cli/src/commands/ops.rs:174` — verb-noun-pair collection loop copy-pasted across three coverage tests. FIXED: added `swissarmyhammer_operations::cli_gen::test_support::collect_verb_noun_pairs(nouns)` next to the shared `parse_argv`; all three coverage tests (code-context ops.rs + main.rs, shelltool ops.rs) now call it, so the nested noun→verb loop lives in exactly one place.\n-- [deferred: pre-existing kanban-domain item, out of scope per task] `crates/swissarmyhammer-kanban/src/board/get.rs:37` — GetBoard::execute length.\n+- [ ] `apps/code-context-cli/src/commands/ops.rs:174` — The verb-noun-pair collection loop (`for noun_cmd { for verb_cmd { generated.insert(format!(\"{} {}\", verb, noun)) }}`) is copy-pasted across three coverage tests. This diff adds two fresh copies (ops.rs `generated_tree_covers_every_operation` and main.rs `build_cli_covers_every_operation`) on top of the existing shelltool copy, tipping the same logic past the rule of three. Each copy will drift independently when the noun/verb convention changes. Add a `collect_verb_noun_pairs(commands: &[Command]) -> HashSet<String>` helper to `swissarmyhammer_operations::cli_gen::test_support` and have all three tests call it, the same way they already share `parse_argv`.\n+- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:237` — The doc comment for `standard_op_cli` documents a `version` parameter (\"`version` is the binary's version string (callers pass `env!(\"CARGO_PKG_VERSION\")`)\") that does not exist on the function. The actual signature is `standard_op_cli(name, about, schema)` and the inline comment at lines 242-244 explicitly explains that version is sourced from this crate's own `CARGO_PKG_VERSION` rather than threaded through. A doc that describes a phantom parameter misleads callers and contradicts the code directly below it. Delete the stale `version` sentence from the doc comment; optionally fold in the line-242 rationale (version comes from this crate's `CARGO_PKG_VERSION`, shared workspace-wide) so the doc matches the implementation.\n+- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:37` — GetBoard::execute is ~100 lines of actual code in one function body (line 37 through its closing match arms near line 165), well over the ~50-line guideline. It conflates several distinct responsibilities — reading board metadata, the no-counts early-return shape, task enrichment, the column/ready counting pass, per-column JSON assembly, and the summary computation — which makes it hard to read, test in isolation, and reuse. Extract the cohesive blocks into private helpers, e.g. `fn basic_board_json(...)` for the no-counts branch, `fn count_tasks_by_column(&[...]) -> (HashMap, HashMap, usize)` for the counting pass, and `fn build_summary(...)` for the summary, leaving `execute` as a short orchestrator. [out of scope per task: deferred kanban-domain get.rs length item]\n\n ### Nits\n-- [x] `apps/kanban-cli/src/main.rs:1` — `dispatch` help/error strings hardcoded `\"kanban\"` despite `const PROGRAM`. FIXED: both `error!` messages now interpolate `{PROGRAM}` (`Run '{PROGRAM} {name} --help' or '{PROGRAM} --help' ...` and `No command specified. Run '{PROGRAM} --help' ...`). Grep proof: the only remaining `\"kanban\"` literals in main.rs are the `const PROGRAM` definition and the `kanban://open/` deep-link URL scheme (a protocol id, not the program name).\n-- [x] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:48` — `InstallTarget` had a `DEFAULT` const but no `Default` impl. FIXED: added `impl Default for InstallTarget` delegating to `InstallTarget::DEFAULT` (single source; `Default::default()` is not const, so `DEFAULT` stays the const default the clap arg reads). Added `default_matches_default_const` test.\n-- [x] `crates/swissarmyhammer-operations-macros/src/lib.rs:309` — single-caller `serde_args_contain_default` wrapper. FIXED: inlined the one-line `.any()` token scan into `has_serde_default`, folding the top-level-only explanation into that function's comment. Regression tests (`list_valued_item_before_default_does_not_mask_it`, `bound_item_before_default_does_not_mask_it`, bare/valued/none/non-serde) still pass.\n\\ No newline at end of file\n+- [ ] `apps/kanban-cli/src/main.rs:1` — The file adds `const PROGRAM: &str = \"kanban\"` to name the program once, but the help/error strings in `dispatch` still hardcode the `\"kanban\"` literal three times — `error!(\"Run 'kanban {} --help' or 'kanban --help' ...\")` (two occurrences) and `error!(\"No command specified. Run 'kanban --help' ...\")`. The literal the constant was meant to replace lives on in parallel, so a rename of the program now has to be made in several easily-missed places. Build those messages from `PROGRAM` (e.g. `error!(\"Run '{PROGRAM} {name} --help' or '{PROGRAM} --help' for usage.\")` and `error!(\"No command specified. Run '{PROGRAM} --help' for usage information.\")`) so the program name changes in exactly one place.\n+- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:48` — `InstallTarget` has an obvious natural default (`Project`) but expresses it via a hand-rolled `DEFAULT` associated const instead of implementing `Default`. The dtolnay checklist flags a public type missing an applicable `Default` impl as a silent semver/ergonomics gap — downstream code that wants `InstallTarget::default()` or `#[derive(Default)]` on a containing struct can't get it. Add `Default` via `#[derive(..., Default)]` with `#[default]` on `Project`. Keep the `DEFAULT` const if a const-context value is needed (it is, at line 90 in `default_value(InstallTarget::DEFAULT.as_str())`, since `Default::default()` is not const), but define it as `Self::Project` so both agree from one source.\n+- [ ] `crates/swissarmyhammer-operations-macros/src/lib.rs:309` — `serde_args_contain_default` is a single-call-site helper: its only caller is `has_serde_default`, and its body is a trivial three-line token scan (`tokens.into_iter().any(|tt| matches Ident == \"default\")`). A wrapper with one caller is indirection without payoff per rule 3. Inline the scan into `has_serde_default`: `list.tokens.clone().into_iter().any(|tt| matches!(tt, proc_macro2::TokenTree::Ident(id) if id == \"default\"))`. Keep it split only if a second caller is imminent.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTK1W791AYKHTSKNR6271Z8K","timestamp":"2026-06-08T07:24:11.937847Z","op":"update","item_id":"01KTCBFKT461CQKT1FQ13E52D6","forward_patch":"--- original\n+++ modified\n@@ -33,4 +33,20 @@\n ### Nits\n - [x] `apps/kanban-cli/src/main.rs:1` — `dispatch` help/error strings hardcoded `\"kanban\"` despite `const PROGRAM`. FIXED: both `error!` messages now interpolate `{PROGRAM}` (`Run '{PROGRAM} {name} --help' or '{PROGRAM} --help' ...` and `No command specified. Run '{PROGRAM} --help' ...`). Grep proof: the only remaining `\"kanban\"` literals in main.rs are the `const PROGRAM` definition and the `kanban://open/` deep-link URL scheme (a protocol id, not the program name).\n - [x] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:48` — `InstallTarget` had a `DEFAULT` const but no `Default` impl. FIXED: added `impl Default for InstallTarget` delegating to `InstallTarget::DEFAULT` (single source; `Default::default()` is not const, so `DEFAULT` stays the const default the clap arg reads). Added `default_matches_default_const` test.\n-- [x] `crates/swissarmyhammer-operations-macros/src/lib.rs:309` — single-caller `serde_args_contain_default` wrapper. FIXED: inlined the one-line `.any()` token scan into `has_serde_default`, folding the top-level-only explanation into that function's comment. Regression tests (`list_valued_item_before_default_does_not_mask_it`, `bound_item_before_default_does_not_mask_it`, bare/valued/none/non-serde) still pass.\n\\ No newline at end of file\n+- [x] `crates/swissarmyhammer-operations-macros/src/lib.rs:309` — single-caller `serde_args_contain_default` wrapper. FIXED: inlined the one-line `.any()` token scan into `has_serde_default`, folding the top-level-only explanation into that function's comment. Regression tests (`list_valued_item_before_default_does_not_mask_it`, `bound_item_before_default_does_not_mask_it`, bare/valued/none/non-serde) still pass.\n+\n+## Review Findings (2026-06-08 01:42) — final re-review, non-blocking\n+\n+Acceptance criteria met and all prior findings resolved. The items below are either the already-resolved duplication/consolidation theme, the explicitly out-of-scope deferred `get.rs` items, or cosmetic nits (doc comments, trait derives, lifetime ergonomics). Zero blockers, zero new material correctness/security problems — task moved to DONE.\n+\n+### Warnings\n+- [ ] `apps/code-context-cli/src/cli.rs:64` — default install scope written as literal `InstallTarget::Project` in derive attrs instead of `default_value_t = InstallTarget::DEFAULT`. (Duplication/single-source theme — already addressed for `InstallTarget`; non-blocking.)\n+- [ ] `apps/code-context-cli/src/cli.rs:83` — multi-shell completion `long_about` help block hand-maintained as a literal that duplicates the shared `completion_subcommand(bin_name)` template. (Duplication/consolidation theme; non-blocking.)\n+- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:119` — lifecycle subcommand builders take `about: &'static str`, forcing `standard_op_cli` to `Box::leak` its `format!` strings; could be `impl Into<StyledStr>`/`String`. (Clarity/ergonomics; non-blocking.)\n+- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:42` — GetBoard::execute ~109 lines, over the ~50-line limit. (Explicitly out-of-scope deferred kanban-domain item per task scope.)\n+\n+### Nits\n+- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:50` — `InstallTarget::as_str` re-declares the ValueEnum token literals; guarded by `as_str_matches_value_enum_token`. (Duplication theme; already test-guarded.)\n+- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:90` — new public builder functions lack per-item doc comments. (Cosmetic.)\n+- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:28` — `GetBoard::include_counts()` single-caller wrapper. (Out-of-scope get.rs; cosmetic.)\n+- [ ] `crates/swissarmyhammer-operations/src/parameter.rs:16` — `ParamMeta` missing `PartialEq/Eq/Hash`. (Pre-existing; this change only touched the doc comment.)\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -33,20 +33,4 @@\n ### Nits\n - [x] `apps/kanban-cli/src/main.rs:1` — `dispatch` help/error strings hardcoded `\"kanban\"` despite `const PROGRAM`. FIXED: both `error!` messages now interpolate `{PROGRAM}` (`Run '{PROGRAM} {name} --help' or '{PROGRAM} --help' ...` and `No command specified. Run '{PROGRAM} --help' ...`). Grep proof: the only remaining `\"kanban\"` literals in main.rs are the `const PROGRAM` definition and the `kanban://open/` deep-link URL scheme (a protocol id, not the program name).\n - [x] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:48` — `InstallTarget` had a `DEFAULT` const but no `Default` impl. FIXED: added `impl Default for InstallTarget` delegating to `InstallTarget::DEFAULT` (single source; `Default::default()` is not const, so `DEFAULT` stays the const default the clap arg reads). Added `default_matches_default_const` test.\n-- [x] `crates/swissarmyhammer-operations-macros/src/lib.rs:309` — single-caller `serde_args_contain_default` wrapper. FIXED: inlined the one-line `.any()` token scan into `has_serde_default`, folding the top-level-only explanation into that function's comment. Regression tests (`list_valued_item_before_default_does_not_mask_it`, `bound_item_before_default_does_not_mask_it`, bare/valued/none/non-serde) still pass.\n-\n-## Review Findings (2026-06-08 01:42) — final re-review, non-blocking\n-\n-Acceptance criteria met and all prior findings resolved. The items below are either the already-resolved duplication/consolidation theme, the explicitly out-of-scope deferred `get.rs` items, or cosmetic nits (doc comments, trait derives, lifetime ergonomics). Zero blockers, zero new material correctness/security problems — task moved to DONE.\n-\n-### Warnings\n-- [ ] `apps/code-context-cli/src/cli.rs:64` — default install scope written as literal `InstallTarget::Project` in derive attrs instead of `default_value_t = InstallTarget::DEFAULT`. (Duplication/single-source theme — already addressed for `InstallTarget`; non-blocking.)\n-- [ ] `apps/code-context-cli/src/cli.rs:83` — multi-shell completion `long_about` help block hand-maintained as a literal that duplicates the shared `completion_subcommand(bin_name)` template. (Duplication/consolidation theme; non-blocking.)\n-- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:119` — lifecycle subcommand builders take `about: &'static str`, forcing `standard_op_cli` to `Box::leak` its `format!` strings; could be `impl Into<StyledStr>`/`String`. (Clarity/ergonomics; non-blocking.)\n-- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:42` — GetBoard::execute ~109 lines, over the ~50-line limit. (Explicitly out-of-scope deferred kanban-domain item per task scope.)\n-\n-### Nits\n-- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:50` — `InstallTarget::as_str` re-declares the ValueEnum token literals; guarded by `as_str_matches_value_enum_token`. (Duplication theme; already test-guarded.)\n-- [ ] `crates/swissarmyhammer-cli-completions/src/lifecycle.rs:90` — new public builder functions lack per-item doc comments. (Cosmetic.)\n-- [ ] `crates/swissarmyhammer-kanban/src/board/get.rs:28` — `GetBoard::include_counts()` single-caller wrapper. (Out-of-scope get.rs; cosmetic.)\n-- [ ] `crates/swissarmyhammer-operations/src/parameter.rs:16` — `ParamMeta` missing `PartialEq/Eq/Hash`. (Pre-existing; this change only touched the doc comment.)\n\\ No newline at end of file\n+- [x] `crates/swissarmyhammer-operations-macros/src/lib.rs:309` — single-caller `serde_args_contain_default` wrapper. FIXED: inlined the one-line `.any()` token scan into `has_serde_default`, folding the top-level-only explanation into that function's comment. Regression tests (`list_valued_item_before_default_does_not_mask_it`, `bound_item_before_default_does_not_mask_it`, bare/valued/none/non-serde) still pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTK1WC7AMF95P97P6SNFJZRP","timestamp":"2026-06-08T07:24:17.002089Z","op":"update","item_id":"01KTCBFKT461CQKT1FQ13E52D6","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KTCBDXJKQA68WPEE4MJW77ZH\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: fffffffffffffffffffffffffffffffffffff780\n project: cli-schema-gen\n title: Migrate kanban-cli onto the shared generator; delete its private cli_gen.rs\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KTCBDXJKQA68WPEE4MJW77ZH\n-position_column: done\n-position_ordinal: fffffffffffffffffffffffffffffffffffff780\n+position_column: review\n+position_ordinal: '80'\n project: cli-schema-gen\n title: Migrate kanban-cli onto the shared generator; delete its private cli_gen.rs\n ---\n","transaction_id":null}

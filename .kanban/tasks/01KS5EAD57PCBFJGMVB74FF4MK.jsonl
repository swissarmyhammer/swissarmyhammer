{"id":"01KS5EAD585T7M7ZP8XERBPS02","timestamp":"2026-05-21T14:16:35.752152Z","op":"create","item_id":"01KS5EAD57PCBFJGMVB74FF4MK","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,40 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KS36KNHH9BPC82MFMGTY3T5J\n+position_column: todo\n+position_ordinal: '9880'\n+project: command-service\n+title: 'Extend `kanban` MCP tool: clipboard (cut/copy/paste) + archive/unarchive'\n+---\n+## What\n+\n+Add the operations the entity-commands plugin needs that the existing `kanban` MCP tool does not yet expose: clipboard (cut/copy/paste via `PasteMatrix`) and `archive`/`unarchive`. The `kanban` tool already covers add/update/delete/get for task/column/tag/project/actor/attachment, move/complete/tag/untag/assign task — verified in `crates/swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs`. These are the gaps.\n+\n+Files:\n+- `crates/swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs` — add operations:\n+  - `archive task`, `unarchive task` (entity.archive/unarchive map to these; today archive lives in `EntityContext` but is not exposed as a kanban op — confirm and wire it)\n+  - `cut`, `copy`, `paste` (clipboard) — wraps the `PasteMatrix` (`Arc<PasteMatrix>`, today in `swissarmyhammer-kanban`, reached via `ctx.kanban.paste_matrix()`). These operate on whatever entity is targeted, mirroring today's `clipboard_commands.rs`.\n+- Update the kanban tool's operation set + `description.md` so the new ops appear in `tools/list` and `_meta`.\n+\n+Design note (from the user): clipboard cut/copy/paste and archive are **entity-cross-cutting** — they work on any entity type, same as undo/redo. They belong on the `kanban` tool (which already owns generic entity CRUD) rather than a separate `clipboard` server, keeping the consolidated-server design. Paste must preserve the drag-vs-paste distinction (memory: drag-vs-paste) — external paste creates via PasteMatrix; it is not the internal-drag property mutation.\n+\n+Archive/unarchive and paste are undoable — they write through the unified changelog so `app.undo` reverts them.\n+\n+## Acceptance Criteria\n+- [ ] `kanban` tool exposes `archive task`, `unarchive task`, `cut`, `copy`, `paste`\n+- [ ] `tools/list` + `_meta` reflect the new operations\n+- [ ] `cut`/`copy`/`paste` round-trip against the real `PasteMatrix`; paste creates the duplicate entity in the store\n+- [ ] `archive task`/`unarchive task` move the task in/out of the archive as today's `EntityContext` does\n+- [ ] New ops are captured by the unified changelog (undoable)\n+\n+## Tests\n+- [ ] `crates/swissarmyhammer-tools/tests/integration/kanban_clipboard_archive_e2e.rs` — real kanban store; copy a task → paste → assert duplicate; cut → paste → assert moved; archive → assert in archive; unarchive → assert restored\n+- [ ] Undo integration: paste a task; `app.undo`; assert the paste reverted\n+- [ ] `cargo test -p swissarmyhammer-tools` passes\n+\n+## Workflow\n+- Use `/tdd`\n+\n+Prerequisite for: entity-commands plugin. Depends on the operation-struct foundation.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,40 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KS36KNHH9BPC82MFMGTY3T5J\n-position_column: todo\n-position_ordinal: '9880'\n-project: command-service\n-title: 'Extend `kanban` MCP tool: clipboard (cut/copy/paste) + archive/unarchive'\n----\n-## What\n-\n-Add the operations the entity-commands plugin needs that the existing `kanban` MCP tool does not yet expose: clipboard (cut/copy/paste via `PasteMatrix`) and `archive`/`unarchive`. The `kanban` tool already covers add/update/delete/get for task/column/tag/project/actor/attachment, move/complete/tag/untag/assign task — verified in `crates/swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs`. These are the gaps.\n-\n-Files:\n-- `crates/swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs` — add operations:\n-  - `archive task`, `unarchive task` (entity.archive/unarchive map to these; today archive lives in `EntityContext` but is not exposed as a kanban op — confirm and wire it)\n-  - `cut`, `copy`, `paste` (clipboard) — wraps the `PasteMatrix` (`Arc<PasteMatrix>`, today in `swissarmyhammer-kanban`, reached via `ctx.kanban.paste_matrix()`). These operate on whatever entity is targeted, mirroring today's `clipboard_commands.rs`.\n-- Update the kanban tool's operation set + `description.md` so the new ops appear in `tools/list` and `_meta`.\n-\n-Design note (from the user): clipboard cut/copy/paste and archive are **entity-cross-cutting** — they work on any entity type, same as undo/redo. They belong on the `kanban` tool (which already owns generic entity CRUD) rather than a separate `clipboard` server, keeping the consolidated-server design. Paste must preserve the drag-vs-paste distinction (memory: drag-vs-paste) — external paste creates via PasteMatrix; it is not the internal-drag property mutation.\n-\n-Archive/unarchive and paste are undoable — they write through the unified changelog so `app.undo` reverts them.\n-\n-## Acceptance Criteria\n-- [ ] `kanban` tool exposes `archive task`, `unarchive task`, `cut`, `copy`, `paste`\n-- [ ] `tools/list` + `_meta` reflect the new operations\n-- [ ] `cut`/`copy`/`paste` round-trip against the real `PasteMatrix`; paste creates the duplicate entity in the store\n-- [ ] `archive task`/`unarchive task` move the task in/out of the archive as today's `EntityContext` does\n-- [ ] New ops are captured by the unified changelog (undoable)\n-\n-## Tests\n-- [ ] `crates/swissarmyhammer-tools/tests/integration/kanban_clipboard_archive_e2e.rs` — real kanban store; copy a task → paste → assert duplicate; cut → paste → assert moved; archive → assert in archive; unarchive → assert restored\n-- [ ] Undo integration: paste a task; `app.undo`; assert the paste reverted\n-- [ ] `cargo test -p swissarmyhammer-tools` passes\n-\n-## Workflow\n-- Use `/tdd`\n-\n-Prerequisite for: entity-commands plugin. Depends on the operation-struct foundation.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KS5GV2HFBT0A6XB4FY3EMA7D","timestamp":"2026-05-21T15:00:39.087556Z","op":"update","item_id":"01KS5EAD57PCBFJGMVB74FF4MK","forward_patch":"--- original\n+++ modified\n@@ -2,10 +2,10 @@\n assignees:\n - claude-code\n depends_on:\n-- 01KS36KNHH9BPC82MFMGTY3T5J\n+- 01KS5F5ZNA0621X8KM2NPERXNV\n position_column: todo\n position_ordinal: '9880'\n-project: command-service\n+project: command-backends\n title: 'Extend `kanban` MCP tool: clipboard (cut/copy/paste) + archive/unarchive'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -2,10 +2,10 @@\n assignees:\n - claude-code\n depends_on:\n-- 01KS5F5ZNA0621X8KM2NPERXNV\n+- 01KS36KNHH9BPC82MFMGTY3T5J\n position_column: todo\n position_ordinal: '9880'\n-project: command-backends\n+project: command-service\n title: 'Extend `kanban` MCP tool: clipboard (cut/copy/paste) + archive/unarchive'\n ---\n ## What\n","transaction_id":null}
{"id":"01KS5MX4VDZ9VFPD6YV38NCY8R","timestamp":"2026-05-21T16:11:41.293683Z","op":"update","item_id":"01KS5EAD57PCBFJGMVB74FF4MK","forward_patch":"--- original\n+++ modified\n@@ -5,36 +5,42 @@\n - 01KS5F5ZNA0621X8KM2NPERXNV\n position_column: todo\n position_ordinal: '9880'\n-project: command-backends\n-title: 'Extend `kanban` MCP tool: clipboard (cut/copy/paste) + archive/unarchive'\n+project: entity-service\n+title: '`entity` MCP server: generic CRUD + archive + clipboard + search'\n ---\n ## What\n\n-Add the operations the entity-commands plugin needs that the existing `kanban` MCP tool does not yet expose: clipboard (cut/copy/paste via `PasteMatrix`) and `archive`/`unarchive`. The `kanban` tool already covers add/update/delete/get for task/column/tag/project/actor/attachment, move/complete/tag/untag/assign task — verified in `crates/swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs`. These are the gaps.\n+Build the `entity` MCP server — the generic, type-agnostic entity capability the UI and the cross-cutting `entity.*` commands need. Today this lives in-process in `EntityContext`/`EntityCache`/`EntityTypeStore` (`swissarmyhammer-entity`), the `PasteMatrix` (`swissarmyhammer-kanban`), and `EntitySearchIndex` (`swissarmyhammer-entity-search`), reached via Tauri commands (`get_entity`) and `ctx.require_extension` — there is no MCP surface. This server exposes all of it.\n\n-Files:\n-- `crates/swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs` — add operations:\n-  - `archive task`, `unarchive task` (entity.archive/unarchive map to these; today archive lives in `EntityContext` but is not exposed as a kanban op — confirm and wire it)\n-  - `cut`, `copy`, `paste` (clipboard) — wraps the `PasteMatrix` (`Arc<PasteMatrix>`, today in `swissarmyhammer-kanban`, reached via `ctx.kanban.paste_matrix()`). These operate on whatever entity is targeted, mirroring today's `clipboard_commands.rs`.\n-- Update the kanban tool's operation set + `description.md` so the new ops appear in `tools/list` and `_meta`.\n+Design boundary (per the user): **generic entity capability = `entity` service; kanban-domain = `kanban` service.** The `entity` server owns operations that work on ANY entity type; the existing `kanban` tool keeps board lifecycle, task positioning (`move task`), `next/complete task`, `assign`, `tag/untag`. (Search is an entity capability — it lives here, not as a separate `search` server.)\n\n-Design note (from the user): clipboard cut/copy/paste and archive are **entity-cross-cutting** — they work on any entity type, same as undo/redo. They belong on the `kanban` tool (which already owns generic entity CRUD) rather than a separate `clipboard` server, keeping the consolidated-server design. Paste must preserve the drag-vs-paste distinction (memory: drag-vs-paste) — external paste creates via PasteMatrix; it is not the internal-drag property mutation.\n+Files:\n+- `crates/swissarmyhammer-entity/src/server.rs` (or a thin `swissarmyhammer-entity-mcp` crate) — `EntityServer` over `Arc<EntityContext>` + the shared `Arc<StoreContext>` + `EntitySearchIndex`\n+- `operations.rs` — `#[operation]` structs (all take an entity-type param where relevant):\n+  - **read**: `GetEntity { type, id }`, `ListEntities { type, filter? }` (replaces the frontend `get_entity` Tauri command + board-load)\n+  - **write**: `AddEntity { type, fields }`, `UpdateField { type, id, field, value }`, `DeleteEntity { type, id }`\n+  - **archive**: `ArchiveEntity { type, id }`, `UnarchiveEntity { type, id }`\n+  - **clipboard**: `Cut`, `Copy`, `Paste` (wraps `PasteMatrix`; preserves the drag-vs-paste distinction — external paste creates via PasteMatrix)\n+  - **search**: `Search { query, type? }` (wraps `EntitySearchIndex`)\n+- `service.rs` — holds the contexts; bootstrap `host.expose_rust_module(\"entity\", EntityServer::new(...))`\n\n-Archive/unarchive and paste are undoable — they write through the unified changelog so `app.undo` reverts them.\n+Writes go through `EntityContext`, which already pushes onto the shared `StoreContext` (undoable) and broadcasts `EntityEvent`s — so undo + the notification surface work for free. Share the SAME `Arc<StoreContext>` as `kanban`/`views`/`store` (substrate invariant).\n\n ## Acceptance Criteria\n-- [ ] `kanban` tool exposes `archive task`, `unarchive task`, `cut`, `copy`, `paste`\n-- [ ] `tools/list` + `_meta` reflect the new operations\n-- [ ] `cut`/`copy`/`paste` round-trip against the real `PasteMatrix`; paste creates the duplicate entity in the store\n-- [ ] `archive task`/`unarchive task` move the task in/out of the archive as today's `EntityContext` does\n-- [ ] New ops are captured by the unified changelog (undoable)\n+- [ ] `entity` registered as an in-process server over `EntityContext` + shared `StoreContext` + `EntitySearchIndex`\n+- [ ] Generic read (`GetEntity`/`ListEntities`) returns any entity type — replaces the `get_entity` Tauri command\n+- [ ] Generic write/archive/clipboard work on any entity type; writes are undoable via the shared stack and emit entity events\n+- [ ] `Search` returns hits from `EntitySearchIndex`\n+- [ ] No duplicate entity state — wraps the existing contexts\n+- [ ] `_meta` operations tree complete\n\n ## Tests\n-- [ ] `crates/swissarmyhammer-tools/tests/integration/kanban_clipboard_archive_e2e.rs` — real kanban store; copy a task → paste → assert duplicate; cut → paste → assert moved; archive → assert in archive; unarchive → assert restored\n-- [ ] Undo integration: paste a task; `app.undo`; assert the paste reverted\n-- [ ] `cargo test -p swissarmyhammer-tools` passes\n+- [ ] `crates/swissarmyhammer-entity/tests/integration/entity_server_e2e.rs` — add → get → update_field → delete round-trip across two entity types; archive/unarchive; clipboard copy→paste creates a duplicate; `Search` finds an entity by text\n+- [ ] Undo integration: update_field via `entity`; `store.undo`; assert reverted (proves shared stack)\n+- [ ] `_meta` snapshot\n+- [ ] `cargo test -p swissarmyhammer-entity` (and `-p swissarmyhammer-entity-search` if split) passes\n\n ## Workflow\n-- Use `/tdd`\n+- Use `/tdd` — write the CRUD round-trip + search test first.\n\n-Prerequisite for: entity-commands plugin. Depends on the operation-struct foundation.\n\\ No newline at end of file\n+Prerequisite for: entity-commands plugin, the frontend's generic reads + search UI. Depends on store-service substrate. The `kanban` service needs NO clipboard/archive changes now — those live here.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -5,42 +5,36 @@\n - 01KS5F5ZNA0621X8KM2NPERXNV\n position_column: todo\n position_ordinal: '9880'\n-project: entity-service\n-title: '`entity` MCP server: generic CRUD + archive + clipboard + search'\n+project: command-backends\n+title: 'Extend `kanban` MCP tool: clipboard (cut/copy/paste) + archive/unarchive'\n ---\n ## What\n\n-Build the `entity` MCP server — the generic, type-agnostic entity capability the UI and the cross-cutting `entity.*` commands need. Today this lives in-process in `EntityContext`/`EntityCache`/`EntityTypeStore` (`swissarmyhammer-entity`), the `PasteMatrix` (`swissarmyhammer-kanban`), and `EntitySearchIndex` (`swissarmyhammer-entity-search`), reached via Tauri commands (`get_entity`) and `ctx.require_extension` — there is no MCP surface. This server exposes all of it.\n-\n-Design boundary (per the user): **generic entity capability = `entity` service; kanban-domain = `kanban` service.** The `entity` server owns operations that work on ANY entity type; the existing `kanban` tool keeps board lifecycle, task positioning (`move task`), `next/complete task`, `assign`, `tag/untag`. (Search is an entity capability — it lives here, not as a separate `search` server.)\n+Add the operations the entity-commands plugin needs that the existing `kanban` MCP tool does not yet expose: clipboard (cut/copy/paste via `PasteMatrix`) and `archive`/`unarchive`. The `kanban` tool already covers add/update/delete/get for task/column/tag/project/actor/attachment, move/complete/tag/untag/assign task — verified in `crates/swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs`. These are the gaps.\n\n Files:\n-- `crates/swissarmyhammer-entity/src/server.rs` (or a thin `swissarmyhammer-entity-mcp` crate) — `EntityServer` over `Arc<EntityContext>` + the shared `Arc<StoreContext>` + `EntitySearchIndex`\n-- `operations.rs` — `#[operation]` structs (all take an entity-type param where relevant):\n-  - **read**: `GetEntity { type, id }`, `ListEntities { type, filter? }` (replaces the frontend `get_entity` Tauri command + board-load)\n-  - **write**: `AddEntity { type, fields }`, `UpdateField { type, id, field, value }`, `DeleteEntity { type, id }`\n-  - **archive**: `ArchiveEntity { type, id }`, `UnarchiveEntity { type, id }`\n-  - **clipboard**: `Cut`, `Copy`, `Paste` (wraps `PasteMatrix`; preserves the drag-vs-paste distinction — external paste creates via PasteMatrix)\n-  - **search**: `Search { query, type? }` (wraps `EntitySearchIndex`)\n-- `service.rs` — holds the contexts; bootstrap `host.expose_rust_module(\"entity\", EntityServer::new(...))`\n+- `crates/swissarmyhammer-tools/src/mcp/tools/kanban/mod.rs` — add operations:\n+  - `archive task`, `unarchive task` (entity.archive/unarchive map to these; today archive lives in `EntityContext` but is not exposed as a kanban op — confirm and wire it)\n+  - `cut`, `copy`, `paste` (clipboard) — wraps the `PasteMatrix` (`Arc<PasteMatrix>`, today in `swissarmyhammer-kanban`, reached via `ctx.kanban.paste_matrix()`). These operate on whatever entity is targeted, mirroring today's `clipboard_commands.rs`.\n+- Update the kanban tool's operation set + `description.md` so the new ops appear in `tools/list` and `_meta`.\n\n-Writes go through `EntityContext`, which already pushes onto the shared `StoreContext` (undoable) and broadcasts `EntityEvent`s — so undo + the notification surface work for free. Share the SAME `Arc<StoreContext>` as `kanban`/`views`/`store` (substrate invariant).\n+Design note (from the user): clipboard cut/copy/paste and archive are **entity-cross-cutting** — they work on any entity type, same as undo/redo. They belong on the `kanban` tool (which already owns generic entity CRUD) rather than a separate `clipboard` server, keeping the consolidated-server design. Paste must preserve the drag-vs-paste distinction (memory: drag-vs-paste) — external paste creates via PasteMatrix; it is not the internal-drag property mutation.\n+\n+Archive/unarchive and paste are undoable — they write through the unified changelog so `app.undo` reverts them.\n\n ## Acceptance Criteria\n-- [ ] `entity` registered as an in-process server over `EntityContext` + shared `StoreContext` + `EntitySearchIndex`\n-- [ ] Generic read (`GetEntity`/`ListEntities`) returns any entity type — replaces the `get_entity` Tauri command\n-- [ ] Generic write/archive/clipboard work on any entity type; writes are undoable via the shared stack and emit entity events\n-- [ ] `Search` returns hits from `EntitySearchIndex`\n-- [ ] No duplicate entity state — wraps the existing contexts\n-- [ ] `_meta` operations tree complete\n+- [ ] `kanban` tool exposes `archive task`, `unarchive task`, `cut`, `copy`, `paste`\n+- [ ] `tools/list` + `_meta` reflect the new operations\n+- [ ] `cut`/`copy`/`paste` round-trip against the real `PasteMatrix`; paste creates the duplicate entity in the store\n+- [ ] `archive task`/`unarchive task` move the task in/out of the archive as today's `EntityContext` does\n+- [ ] New ops are captured by the unified changelog (undoable)\n\n ## Tests\n-- [ ] `crates/swissarmyhammer-entity/tests/integration/entity_server_e2e.rs` — add → get → update_field → delete round-trip across two entity types; archive/unarchive; clipboard copy→paste creates a duplicate; `Search` finds an entity by text\n-- [ ] Undo integration: update_field via `entity`; `store.undo`; assert reverted (proves shared stack)\n-- [ ] `_meta` snapshot\n-- [ ] `cargo test -p swissarmyhammer-entity` (and `-p swissarmyhammer-entity-search` if split) passes\n+- [ ] `crates/swissarmyhammer-tools/tests/integration/kanban_clipboard_archive_e2e.rs` — real kanban store; copy a task → paste → assert duplicate; cut → paste → assert moved; archive → assert in archive; unarchive → assert restored\n+- [ ] Undo integration: paste a task; `app.undo`; assert the paste reverted\n+- [ ] `cargo test -p swissarmyhammer-tools` passes\n\n ## Workflow\n-- Use `/tdd` — write the CRUD round-trip + search test first.\n+- Use `/tdd`\n\n-Prerequisite for: entity-commands plugin, the frontend's generic reads + search UI. Depends on store-service substrate. The `kanban` service needs NO clipboard/archive changes now — those live here.\n\\ No newline at end of file\n+Prerequisite for: entity-commands plugin. Depends on the operation-struct foundation.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KS5RKVAVR3DFJES1V0H24RHQ","timestamp":"2026-05-21T17:16:30.939271Z","op":"update","item_id":"01KS5EAD57PCBFJGMVB74FF4MK","forward_patch":"--- original\n+++ modified\n@@ -10,19 +10,25 @@\n ---\n ## What\n\n-Build the `entity` MCP server — the generic, type-agnostic entity capability the UI and the cross-cutting `entity.*` commands need. Today this lives in-process in `EntityContext`/`EntityCache`/`EntityTypeStore` (`swissarmyhammer-entity`), the `PasteMatrix` (`swissarmyhammer-kanban`), and `EntitySearchIndex` (`swissarmyhammer-entity-search`), reached via Tauri commands (`get_entity`) and `ctx.require_extension` — there is no MCP surface. This server exposes all of it.\n+Build the `entity` MCP server — the generic, type-agnostic MCP face over the entity **kernel** (`EntityContext`/`EntityCache`/`EntityTypeStore` in `swissarmyhammer-entity`), plus `PasteMatrix` (`swissarmyhammer-kanban`) and `EntitySearchIndex` (`swissarmyhammer-entity-search`). Today this state is reached via Tauri commands (`get_entity`) and `ctx.require_extension` — no MCP surface. This server exposes it generically.\n+\n+### Kernel + facades (per the user): kanban keeps ALL its operations\n+\n+`EntityContext` is the **kernel** (one CRUD implementation). Two MCP faces sit over it:\n+- **`entity`** (this task) — generic, type-agnostic: get/list/add/update/delete + archive/unarchive + clipboard + search for ANY type. For cross-cutting `entity.*` commands, the frontend's generic reads, and the search UI.\n+- **`kanban`** (exists, unchanged surface) — domain facade: `add task`, `add project`, `update column`, `get tag`, `move task`, `next/complete task`, `assign`, `tag/untag`, board lifecycle. **kanban does NOT lose operations** — its typed/domain ops stay and pass through to the entity kernel internally.\n\n-Design boundary (per the user): **generic entity capability = `entity` service; kanban-domain = `kanban` service.** The `entity` server owns operations that work on ANY entity type; the existing `kanban` tool keeps board lifecycle, task positioning (`move task`), `next/complete task`, `assign`, `tag/untag`. (Search is an entity capability — it lives here, not as a separate `search` server.)\n+So this is additive: a new generic face over the shared kernel, not a removal from kanban. Search is an entity capability — it lives here, not as a separate `search` server.\n\n Files:\n-- `crates/swissarmyhammer-entity/src/server.rs` (or a thin `swissarmyhammer-entity-mcp` crate) — `EntityServer` over `Arc<EntityContext>` + the shared `Arc<StoreContext>` + `EntitySearchIndex`\n-- `operations.rs` — `#[operation]` structs (all take an entity-type param where relevant):\n-  - **read**: `GetEntity { type, id }`, `ListEntities { type, filter? }` (replaces the frontend `get_entity` Tauri command + board-load)\n+- `crates/swissarmyhammer-entity/src/server.rs` (or a thin `swissarmyhammer-entity-mcp` crate) — `EntityServer` over `Arc<EntityContext>` + shared `Arc<StoreContext>` + `EntitySearchIndex`\n+- `operations.rs` — `#[operation]` structs (entity-type param where relevant):\n+  - **read**: `GetEntity { type, id }`, `ListEntities { type, filter? }` (replaces the `get_entity` Tauri command + board-load)\n   - **write**: `AddEntity { type, fields }`, `UpdateField { type, id, field, value }`, `DeleteEntity { type, id }`\n-  - **archive**: `ArchiveEntity { type, id }`, `UnarchiveEntity { type, id }`\n-  - **clipboard**: `Cut`, `Copy`, `Paste` (wraps `PasteMatrix`; preserves the drag-vs-paste distinction — external paste creates via PasteMatrix)\n+  - **archive**: `ArchiveEntity`, `UnarchiveEntity`\n+  - **clipboard**: `Cut`, `Copy`, `Paste` (wraps `PasteMatrix`; preserves drag-vs-paste)\n   - **search**: `Search { query, type? }` (wraps `EntitySearchIndex`)\n-- `service.rs` — holds the contexts; bootstrap `host.expose_rust_module(\"entity\", EntityServer::new(...))`\n+- `service.rs` — bootstrap `host.expose_rust_module(\"entity\", EntityServer::new(...))`\n\n Writes go through `EntityContext`, which already pushes onto the shared `StoreContext` (undoable) and broadcasts `EntityEvent`s — so undo + the notification surface work for free. Share the SAME `Arc<StoreContext>` as `kanban`/`views`/`store` (substrate invariant).\n\n@@ -29,18 +35,18 @@\n ## Acceptance Criteria\n - [ ] `entity` registered as an in-process server over `EntityContext` + shared `StoreContext` + `EntitySearchIndex`\n - [ ] Generic read (`GetEntity`/`ListEntities`) returns any entity type — replaces the `get_entity` Tauri command\n-- [ ] Generic write/archive/clipboard work on any entity type; writes are undoable via the shared stack and emit entity events\n+- [ ] Generic write/archive/clipboard work on any type; writes undoable via the shared stack; emit entity events\n - [ ] `Search` returns hits from `EntitySearchIndex`\n-- [ ] No duplicate entity state — wraps the existing contexts\n+- [ ] `kanban`'s operation surface is UNCHANGED — both faces resolve through the one `EntityContext` kernel (no duplicate CRUD, no lost kanban ops)\n - [ ] `_meta` operations tree complete\n\n ## Tests\n-- [ ] `crates/swissarmyhammer-entity/tests/integration/entity_server_e2e.rs` — add → get → update_field → delete round-trip across two entity types; archive/unarchive; clipboard copy→paste creates a duplicate; `Search` finds an entity by text\n-- [ ] Undo integration: update_field via `entity`; `store.undo`; assert reverted (proves shared stack)\n-- [ ] `_meta` snapshot\n-- [ ] `cargo test -p swissarmyhammer-entity` (and `-p swissarmyhammer-entity-search` if split) passes\n+- [ ] `crates/swissarmyhammer-entity/tests/integration/entity_server_e2e.rs` — add → get → update_field → delete across two types; archive/unarchive; clipboard copy→paste duplicates; `Search` finds by text\n+- [ ] Parity test: `kanban add task` and `entity AddEntity{type:task}` produce the same on-disk result (both via the kernel)\n+- [ ] Undo: update_field via `entity`; `store.undo`; assert reverted\n+- [ ] `cargo test -p swissarmyhammer-entity` passes\n\n ## Workflow\n-- Use `/tdd` — write the CRUD round-trip + search test first.\n+- Use `/tdd` — write the CRUD round-trip + search test first; add the kanban/entity parity test to lock \"same kernel, two faces.\"\n\n-Prerequisite for: entity-commands plugin, the frontend's generic reads + search UI. Depends on store-service substrate. The `kanban` service needs NO clipboard/archive changes now — those live here.\n\\ No newline at end of file\n+Prerequisite for: entity-commands plugin, the frontend's generic reads + search UI. Depends on store-service substrate.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -10,25 +10,19 @@\n ---\n ## What\n\n-Build the `entity` MCP server — the generic, type-agnostic MCP face over the entity **kernel** (`EntityContext`/`EntityCache`/`EntityTypeStore` in `swissarmyhammer-entity`), plus `PasteMatrix` (`swissarmyhammer-kanban`) and `EntitySearchIndex` (`swissarmyhammer-entity-search`). Today this state is reached via Tauri commands (`get_entity`) and `ctx.require_extension` — no MCP surface. This server exposes it generically.\n-\n-### Kernel + facades (per the user): kanban keeps ALL its operations\n+Build the `entity` MCP server — the generic, type-agnostic entity capability the UI and the cross-cutting `entity.*` commands need. Today this lives in-process in `EntityContext`/`EntityCache`/`EntityTypeStore` (`swissarmyhammer-entity`), the `PasteMatrix` (`swissarmyhammer-kanban`), and `EntitySearchIndex` (`swissarmyhammer-entity-search`), reached via Tauri commands (`get_entity`) and `ctx.require_extension` — there is no MCP surface. This server exposes all of it.\n\n-`EntityContext` is the **kernel** (one CRUD implementation). Two MCP faces sit over it:\n-- **`entity`** (this task) — generic, type-agnostic: get/list/add/update/delete + archive/unarchive + clipboard + search for ANY type. For cross-cutting `entity.*` commands, the frontend's generic reads, and the search UI.\n-- **`kanban`** (exists, unchanged surface) — domain facade: `add task`, `add project`, `update column`, `get tag`, `move task`, `next/complete task`, `assign`, `tag/untag`, board lifecycle. **kanban does NOT lose operations** — its typed/domain ops stay and pass through to the entity kernel internally.\n-\n-So this is additive: a new generic face over the shared kernel, not a removal from kanban. Search is an entity capability — it lives here, not as a separate `search` server.\n+Design boundary (per the user): **generic entity capability = `entity` service; kanban-domain = `kanban` service.** The `entity` server owns operations that work on ANY entity type; the existing `kanban` tool keeps board lifecycle, task positioning (`move task`), `next/complete task`, `assign`, `tag/untag`. (Search is an entity capability — it lives here, not as a separate `search` server.)\n\n Files:\n-- `crates/swissarmyhammer-entity/src/server.rs` (or a thin `swissarmyhammer-entity-mcp` crate) — `EntityServer` over `Arc<EntityContext>` + shared `Arc<StoreContext>` + `EntitySearchIndex`\n-- `operations.rs` — `#[operation]` structs (entity-type param where relevant):\n-  - **read**: `GetEntity { type, id }`, `ListEntities { type, filter? }` (replaces the `get_entity` Tauri command + board-load)\n+- `crates/swissarmyhammer-entity/src/server.rs` (or a thin `swissarmyhammer-entity-mcp` crate) — `EntityServer` over `Arc<EntityContext>` + the shared `Arc<StoreContext>` + `EntitySearchIndex`\n+- `operations.rs` — `#[operation]` structs (all take an entity-type param where relevant):\n+  - **read**: `GetEntity { type, id }`, `ListEntities { type, filter? }` (replaces the frontend `get_entity` Tauri command + board-load)\n   - **write**: `AddEntity { type, fields }`, `UpdateField { type, id, field, value }`, `DeleteEntity { type, id }`\n-  - **archive**: `ArchiveEntity`, `UnarchiveEntity`\n-  - **clipboard**: `Cut`, `Copy`, `Paste` (wraps `PasteMatrix`; preserves drag-vs-paste)\n+  - **archive**: `ArchiveEntity { type, id }`, `UnarchiveEntity { type, id }`\n+  - **clipboard**: `Cut`, `Copy`, `Paste` (wraps `PasteMatrix`; preserves the drag-vs-paste distinction — external paste creates via PasteMatrix)\n   - **search**: `Search { query, type? }` (wraps `EntitySearchIndex`)\n-- `service.rs` — bootstrap `host.expose_rust_module(\"entity\", EntityServer::new(...))`\n+- `service.rs` — holds the contexts; bootstrap `host.expose_rust_module(\"entity\", EntityServer::new(...))`\n\n Writes go through `EntityContext`, which already pushes onto the shared `StoreContext` (undoable) and broadcasts `EntityEvent`s — so undo + the notification surface work for free. Share the SAME `Arc<StoreContext>` as `kanban`/`views`/`store` (substrate invariant).\n\n@@ -35,18 +29,18 @@\n ## Acceptance Criteria\n - [ ] `entity` registered as an in-process server over `EntityContext` + shared `StoreContext` + `EntitySearchIndex`\n - [ ] Generic read (`GetEntity`/`ListEntities`) returns any entity type — replaces the `get_entity` Tauri command\n-- [ ] Generic write/archive/clipboard work on any type; writes undoable via the shared stack; emit entity events\n+- [ ] Generic write/archive/clipboard work on any entity type; writes are undoable via the shared stack and emit entity events\n - [ ] `Search` returns hits from `EntitySearchIndex`\n-- [ ] `kanban`'s operation surface is UNCHANGED — both faces resolve through the one `EntityContext` kernel (no duplicate CRUD, no lost kanban ops)\n+- [ ] No duplicate entity state — wraps the existing contexts\n - [ ] `_meta` operations tree complete\n\n ## Tests\n-- [ ] `crates/swissarmyhammer-entity/tests/integration/entity_server_e2e.rs` — add → get → update_field → delete across two types; archive/unarchive; clipboard copy→paste duplicates; `Search` finds by text\n-- [ ] Parity test: `kanban add task` and `entity AddEntity{type:task}` produce the same on-disk result (both via the kernel)\n-- [ ] Undo: update_field via `entity`; `store.undo`; assert reverted\n-- [ ] `cargo test -p swissarmyhammer-entity` passes\n+- [ ] `crates/swissarmyhammer-entity/tests/integration/entity_server_e2e.rs` — add → get → update_field → delete round-trip across two entity types; archive/unarchive; clipboard copy→paste creates a duplicate; `Search` finds an entity by text\n+- [ ] Undo integration: update_field via `entity`; `store.undo`; assert reverted (proves shared stack)\n+- [ ] `_meta` snapshot\n+- [ ] `cargo test -p swissarmyhammer-entity` (and `-p swissarmyhammer-entity-search` if split) passes\n\n ## Workflow\n-- Use `/tdd` — write the CRUD round-trip + search test first; add the kanban/entity parity test to lock \"same kernel, two faces.\"\n+- Use `/tdd` — write the CRUD round-trip + search test first.\n\n-Prerequisite for: entity-commands plugin, the frontend's generic reads + search UI. Depends on store-service substrate.\n\\ No newline at end of file\n+Prerequisite for: entity-commands plugin, the frontend's generic reads + search UI. Depends on store-service substrate. The `kanban` service needs NO clipboard/archive changes now — those live here.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KS5S7G5NTPRY3CCXHH7J94AF","timestamp":"2026-05-21T17:27:14.869238Z","op":"update","item_id":"01KS5EAD57PCBFJGMVB74FF4MK","forward_patch":"--- original\n+++ modified\n@@ -12,13 +12,13 @@\n\n Build the `entity` MCP server — the generic, type-agnostic MCP face over the entity **kernel** (`EntityContext`/`EntityCache`/`EntityTypeStore` in `swissarmyhammer-entity`), plus `PasteMatrix` (`swissarmyhammer-kanban`) and `EntitySearchIndex` (`swissarmyhammer-entity-search`). Today this state is reached via Tauri commands (`get_entity`) and `ctx.require_extension` — no MCP surface. This server exposes it generically.\n\n-### Kernel + facades (per the user): kanban keeps ALL its operations\n+### Kernel + faces — kanban's OPERATIONS ARE FROZEN\n\n-`EntityContext` is the **kernel** (one CRUD implementation). Two MCP faces sit over it:\n-- **`entity`** (this task) — generic, type-agnostic: get/list/add/update/delete + archive/unarchive + clipboard + search for ANY type. For cross-cutting `entity.*` commands, the frontend's generic reads, and the search UI.\n-- **`kanban`** (exists, unchanged surface) — domain facade: `add task`, `add project`, `update column`, `get tag`, `move task`, `next/complete task`, `assign`, `tag/untag`, board lifecycle. **kanban does NOT lose operations** — its typed/domain ops stay and pass through to the entity kernel internally.\n+`EntityContext` is the **kernel** (one CRUD implementation), and both faces already sit over it today:\n+- **`entity`** (this task, NEW) — generic, type-agnostic: get/list/add/update/delete + archive/unarchive + clipboard + search for ANY type. For cross-cutting `entity.*` commands, the frontend's generic reads, and the search UI.\n+- **`kanban`** (EXISTS) — domain face: `add task`, `add project`, `update column`, `get tag`, `move task`, `next/complete task`, `assign`, `tag/untag`, board lifecycle.\n\n-So this is additive: a new generic face over the shared kernel, not a removal from kanban. Search is an entity capability — it lives here, not as a separate `search` server.\n+**Hard constraint (per the user): this work does NOT change kanban's operations.** No operation is added to or removed from the `kanban` tool. The `entity` server is purely additive. kanban already reaches `EntityContext` today, so it needs no change to share the kernel. Refactoring kanban's internals to delegate to the kernel more explicitly is **optional and out of scope right now** — implementation may change later; the operation surface may not.\n\n Files:\n - `crates/swissarmyhammer-entity/src/server.rs` (or a thin `swissarmyhammer-entity-mcp` crate) — `EntityServer` over `Arc<EntityContext>` + shared `Arc<StoreContext>` + `EntitySearchIndex`\n@@ -30,23 +30,23 @@\n   - **search**: `Search { query, type? }` (wraps `EntitySearchIndex`)\n - `service.rs` — bootstrap `host.expose_rust_module(\"entity\", EntityServer::new(...))`\n\n-Writes go through `EntityContext`, which already pushes onto the shared `StoreContext` (undoable) and broadcasts `EntityEvent`s — so undo + the notification surface work for free. Share the SAME `Arc<StoreContext>` as `kanban`/`views`/`store` (substrate invariant).\n+Writes go through `EntityContext`, which already pushes onto the shared `StoreContext` (undoable) and broadcasts `EntityEvent`s — so undo + the notification surface work for free. Share the SAME `Arc<StoreContext>` as `kanban`/`views`/`store`.\n\n ## Acceptance Criteria\n - [ ] `entity` registered as an in-process server over `EntityContext` + shared `StoreContext` + `EntitySearchIndex`\n-- [ ] Generic read (`GetEntity`/`ListEntities`) returns any entity type — replaces the `get_entity` Tauri command\n-- [ ] Generic write/archive/clipboard work on any type; writes undoable via the shared stack; emit entity events\n-- [ ] `Search` returns hits from `EntitySearchIndex`\n-- [ ] `kanban`'s operation surface is UNCHANGED — both faces resolve through the one `EntityContext` kernel (no duplicate CRUD, no lost kanban ops)\n+- [ ] Generic read/write/archive/clipboard/search work for any entity type; writes undoable + emit entity events\n+- [ ] **kanban's operation surface is byte-for-byte unchanged** — no op added or removed; a snapshot guard test asserts kanban's `tools/list` + `_meta` operations tree is identical before and after this work\n+- [ ] `entity` and `kanban` resolve through the one `EntityContext` kernel (no duplicate CRUD)\n - [ ] `_meta` operations tree complete\n\n ## Tests\n - [ ] `crates/swissarmyhammer-entity/tests/integration/entity_server_e2e.rs` — add → get → update_field → delete across two types; archive/unarchive; clipboard copy→paste duplicates; `Search` finds by text\n-- [ ] Parity test: `kanban add task` and `entity AddEntity{type:task}` produce the same on-disk result (both via the kernel)\n+- [ ] **kanban-surface-frozen guard**: snapshot the `kanban` tool's `_meta` operations tree; assert this work leaves it unchanged\n+- [ ] Parity: `kanban add task` and `entity AddEntity{type:task}` produce the same on-disk result (both via the kernel)\n - [ ] Undo: update_field via `entity`; `store.undo`; assert reverted\n - [ ] `cargo test -p swissarmyhammer-entity` passes\n\n ## Workflow\n-- Use `/tdd` — write the CRUD round-trip + search test first; add the kanban/entity parity test to lock \"same kernel, two faces.\"\n+- Use `/tdd` — write the kanban-surface-frozen guard + the CRUD/search test first.\n\n Prerequisite for: entity-commands plugin, the frontend's generic reads + search UI. Depends on store-service substrate.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -12,13 +12,13 @@\n\n Build the `entity` MCP server — the generic, type-agnostic MCP face over the entity **kernel** (`EntityContext`/`EntityCache`/`EntityTypeStore` in `swissarmyhammer-entity`), plus `PasteMatrix` (`swissarmyhammer-kanban`) and `EntitySearchIndex` (`swissarmyhammer-entity-search`). Today this state is reached via Tauri commands (`get_entity`) and `ctx.require_extension` — no MCP surface. This server exposes it generically.\n\n-### Kernel + faces — kanban's OPERATIONS ARE FROZEN\n+### Kernel + facades (per the user): kanban keeps ALL its operations\n\n-`EntityContext` is the **kernel** (one CRUD implementation), and both faces already sit over it today:\n-- **`entity`** (this task, NEW) — generic, type-agnostic: get/list/add/update/delete + archive/unarchive + clipboard + search for ANY type. For cross-cutting `entity.*` commands, the frontend's generic reads, and the search UI.\n-- **`kanban`** (EXISTS) — domain face: `add task`, `add project`, `update column`, `get tag`, `move task`, `next/complete task`, `assign`, `tag/untag`, board lifecycle.\n+`EntityContext` is the **kernel** (one CRUD implementation). Two MCP faces sit over it:\n+- **`entity`** (this task) — generic, type-agnostic: get/list/add/update/delete + archive/unarchive + clipboard + search for ANY type. For cross-cutting `entity.*` commands, the frontend's generic reads, and the search UI.\n+- **`kanban`** (exists, unchanged surface) — domain facade: `add task`, `add project`, `update column`, `get tag`, `move task`, `next/complete task`, `assign`, `tag/untag`, board lifecycle. **kanban does NOT lose operations** — its typed/domain ops stay and pass through to the entity kernel internally.\n\n-**Hard constraint (per the user): this work does NOT change kanban's operations.** No operation is added to or removed from the `kanban` tool. The `entity` server is purely additive. kanban already reaches `EntityContext` today, so it needs no change to share the kernel. Refactoring kanban's internals to delegate to the kernel more explicitly is **optional and out of scope right now** — implementation may change later; the operation surface may not.\n+So this is additive: a new generic face over the shared kernel, not a removal from kanban. Search is an entity capability — it lives here, not as a separate `search` server.\n\n Files:\n - `crates/swissarmyhammer-entity/src/server.rs` (or a thin `swissarmyhammer-entity-mcp` crate) — `EntityServer` over `Arc<EntityContext>` + shared `Arc<StoreContext>` + `EntitySearchIndex`\n@@ -30,23 +30,23 @@\n   - **search**: `Search { query, type? }` (wraps `EntitySearchIndex`)\n - `service.rs` — bootstrap `host.expose_rust_module(\"entity\", EntityServer::new(...))`\n\n-Writes go through `EntityContext`, which already pushes onto the shared `StoreContext` (undoable) and broadcasts `EntityEvent`s — so undo + the notification surface work for free. Share the SAME `Arc<StoreContext>` as `kanban`/`views`/`store`.\n+Writes go through `EntityContext`, which already pushes onto the shared `StoreContext` (undoable) and broadcasts `EntityEvent`s — so undo + the notification surface work for free. Share the SAME `Arc<StoreContext>` as `kanban`/`views`/`store` (substrate invariant).\n\n ## Acceptance Criteria\n - [ ] `entity` registered as an in-process server over `EntityContext` + shared `StoreContext` + `EntitySearchIndex`\n-- [ ] Generic read/write/archive/clipboard/search work for any entity type; writes undoable + emit entity events\n-- [ ] **kanban's operation surface is byte-for-byte unchanged** — no op added or removed; a snapshot guard test asserts kanban's `tools/list` + `_meta` operations tree is identical before and after this work\n-- [ ] `entity` and `kanban` resolve through the one `EntityContext` kernel (no duplicate CRUD)\n+- [ ] Generic read (`GetEntity`/`ListEntities`) returns any entity type — replaces the `get_entity` Tauri command\n+- [ ] Generic write/archive/clipboard work on any type; writes undoable via the shared stack; emit entity events\n+- [ ] `Search` returns hits from `EntitySearchIndex`\n+- [ ] `kanban`'s operation surface is UNCHANGED — both faces resolve through the one `EntityContext` kernel (no duplicate CRUD, no lost kanban ops)\n - [ ] `_meta` operations tree complete\n\n ## Tests\n - [ ] `crates/swissarmyhammer-entity/tests/integration/entity_server_e2e.rs` — add → get → update_field → delete across two types; archive/unarchive; clipboard copy→paste duplicates; `Search` finds by text\n-- [ ] **kanban-surface-frozen guard**: snapshot the `kanban` tool's `_meta` operations tree; assert this work leaves it unchanged\n-- [ ] Parity: `kanban add task` and `entity AddEntity{type:task}` produce the same on-disk result (both via the kernel)\n+- [ ] Parity test: `kanban add task` and `entity AddEntity{type:task}` produce the same on-disk result (both via the kernel)\n - [ ] Undo: update_field via `entity`; `store.undo`; assert reverted\n - [ ] `cargo test -p swissarmyhammer-entity` passes\n\n ## Workflow\n-- Use `/tdd` — write the kanban-surface-frozen guard + the CRUD/search test first.\n+- Use `/tdd` — write the CRUD round-trip + search test first; add the kanban/entity parity test to lock \"same kernel, two faces.\"\n\n Prerequisite for: entity-commands plugin, the frontend's generic reads + search UI. Depends on store-service substrate.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KS614E89S2VHFCYA1KJWQCNE","timestamp":"2026-05-21T19:45:23.209046Z","op":"update","item_id":"01KS5EAD57PCBFJGMVB74FF4MK","forward_patch":"--- original\n+++ modified\n@@ -6,41 +6,41 @@\n position_column: todo\n position_ordinal: '9880'\n project: entity-service\n-title: '`entity` MCP server: generic CRUD + archive + clipboard + search'\n+title: '`entity` MCP server: generic CRUD + archive + kanban-frozen guard'\n ---\n ## What\n\n-Build the `entity` MCP server — the generic, type-agnostic MCP face over the entity **kernel** (`EntityContext`/`EntityCache`/`EntityTypeStore` in `swissarmyhammer-entity`), plus `PasteMatrix` (`swissarmyhammer-kanban`) and `EntitySearchIndex` (`swissarmyhammer-entity-search`). Today this state is reached via Tauri commands (`get_entity`) and `ctx.require_extension` — no MCP surface. This server exposes it generically.\n+Build the `entity` MCP server core — the generic, type-agnostic MCP face over the entity **kernel** (`EntityContext`/`EntityCache`/`EntityTypeStore` in `swissarmyhammer-entity`) — covering generic CRUD + archive. Clipboard and search are SEPARATE follow-up tasks on this same server (they wrap types in OTHER crates): clipboard → `01KS616…`, search → `01KS617…`.\n+\n+Today this state is reached via Tauri commands (`get_entity` at `apps/kanban-app/src/commands.rs:352`, registered `main.rs:71`) and `ctx.require_extension` — no MCP surface. This server exposes it generically.\n\n ### Kernel + faces — kanban's OPERATIONS ARE FROZEN\n\n-`EntityContext` is the **kernel** (one CRUD implementation), and both faces already sit over it today:\n-- **`entity`** (this task, NEW) — generic, type-agnostic: get/list/add/update/delete + archive/unarchive + clipboard + search for ANY type. For cross-cutting `entity.*` commands, the frontend's generic reads, and the search UI.\n-- **`kanban`** (EXISTS) — domain face: `add task`, `add project`, `update column`, `get tag`, `move task`, `next/complete task`, `assign`, `tag/untag`, board lifecycle.\n+`EntityContext` is the **kernel** (one CRUD implementation), and both faces already sit over it today (verified: `KanbanContext` holds `Arc<EntityContext>` at `crates/swissarmyhammer-kanban/src/context.rs:35` and delegates generic entity I/O to it at `context.rs:368`):\n+- **`entity`** (this task, NEW) — generic, type-agnostic: get/list/add/update/delete + archive/unarchive for ANY type. For cross-cutting `entity.*` commands and the frontend's generic reads.\n+- **`kanban`** (EXISTS) — domain face: `add task`, `add project`, `update column`, `get tag`, `move task`, `next/complete task`, `assign`, `tag/untag`, board lifecycle. Its operation surface is generated from `crates/swissarmyhammer-kanban/.../schema.rs::kanban_operations()` (single source).\n\n **Hard constraint (per the user): this work does NOT change kanban's operations.** No operation is added to or removed from the `kanban` tool. The `entity` server is purely additive. kanban already reaches `EntityContext` today, so it needs no change to share the kernel. Refactoring kanban's internals to delegate to the kernel more explicitly is **optional and out of scope right now** — implementation may change later; the operation surface may not.\n\n Files:\n-- `crates/swissarmyhammer-entity/src/server.rs` (or a thin `swissarmyhammer-entity-mcp` crate) — `EntityServer` over `Arc<EntityContext>` + shared `Arc<StoreContext>` + `EntitySearchIndex`\n+- `crates/swissarmyhammer-entity/src/server.rs` (or a thin `swissarmyhammer-entity-mcp` crate) — `EntityServer` over `Arc<EntityContext>` + shared `Arc<StoreContext>`\n - `operations.rs` — `#[operation]` structs (entity-type param where relevant):\n   - **read**: `GetEntity { type, id }`, `ListEntities { type, filter? }` (replaces the `get_entity` Tauri command + board-load)\n   - **write**: `AddEntity { type, fields }`, `UpdateField { type, id, field, value }`, `DeleteEntity { type, id }`\n   - **archive**: `ArchiveEntity`, `UnarchiveEntity`\n-  - **clipboard**: `Cut`, `Copy`, `Paste` (wraps `PasteMatrix`; preserves drag-vs-paste)\n-  - **search**: `Search { query, type? }` (wraps `EntitySearchIndex`)\n - `service.rs` — bootstrap `host.expose_rust_module(\"entity\", EntityServer::new(...))`\n\n Writes go through `EntityContext`, which already pushes onto the shared `StoreContext` (undoable) and broadcasts `EntityEvent`s — so undo + the notification surface work for free. Share the SAME `Arc<StoreContext>` as `kanban`/`views`/`store`.\n\n ## Acceptance Criteria\n-- [ ] `entity` registered as an in-process server over `EntityContext` + shared `StoreContext` + `EntitySearchIndex`\n-- [ ] Generic read/write/archive/clipboard/search work for any entity type; writes undoable + emit entity events\n-- [ ] **kanban's operation surface is byte-for-byte unchanged** — no op added or removed; a snapshot guard test asserts kanban's `tools/list` + `_meta` operations tree is identical before and after this work\n+- [ ] `entity` registered as an in-process server over `EntityContext` + shared `StoreContext`\n+- [ ] Generic read/write/archive work for any entity type; writes undoable + emit entity events\n+- [ ] **kanban's operation surface is byte-for-byte unchanged** — no op added or removed; a snapshot guard test asserts kanban's `tools/list` + `_meta` operations tree (from `kanban_operations()`) is identical before and after this work\n - [ ] `entity` and `kanban` resolve through the one `EntityContext` kernel (no duplicate CRUD)\n - [ ] `_meta` operations tree complete\n\n ## Tests\n-- [ ] `crates/swissarmyhammer-entity/tests/integration/entity_server_e2e.rs` — add → get → update_field → delete across two types; archive/unarchive; clipboard copy→paste duplicates; `Search` finds by text\n+- [ ] `crates/swissarmyhammer-entity/tests/integration/entity_server_e2e.rs` — add → get → update_field → delete across two types; archive/unarchive\n - [ ] **kanban-surface-frozen guard**: snapshot the `kanban` tool's `_meta` operations tree; assert this work leaves it unchanged\n - [ ] Parity: `kanban add task` and `entity AddEntity{type:task}` produce the same on-disk result (both via the kernel)\n - [ ] Undo: update_field via `entity`; `store.undo`; assert reverted\n@@ -47,6 +47,6 @@\n - [ ] `cargo test -p swissarmyhammer-entity` passes\n\n ## Workflow\n-- Use `/tdd` — write the kanban-surface-frozen guard + the CRUD/search test first.\n+- Use `/tdd` — write the kanban-surface-frozen guard + the CRUD test first.\n\n-Prerequisite for: entity-commands plugin, the frontend's generic reads + search UI. Depends on store-service substrate.\n\\ No newline at end of file\n+Prerequisite for: entity clipboard + search follow-ups, the entity-commands plugin, the frontend's generic reads. Depends on store-service substrate.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -6,41 +6,41 @@\n position_column: todo\n position_ordinal: '9880'\n project: entity-service\n-title: '`entity` MCP server: generic CRUD + archive + kanban-frozen guard'\n+title: '`entity` MCP server: generic CRUD + archive + clipboard + search'\n ---\n ## What\n\n-Build the `entity` MCP server core — the generic, type-agnostic MCP face over the entity **kernel** (`EntityContext`/`EntityCache`/`EntityTypeStore` in `swissarmyhammer-entity`) — covering generic CRUD + archive. Clipboard and search are SEPARATE follow-up tasks on this same server (they wrap types in OTHER crates): clipboard → `01KS616…`, search → `01KS617…`.\n-\n-Today this state is reached via Tauri commands (`get_entity` at `apps/kanban-app/src/commands.rs:352`, registered `main.rs:71`) and `ctx.require_extension` — no MCP surface. This server exposes it generically.\n+Build the `entity` MCP server — the generic, type-agnostic MCP face over the entity **kernel** (`EntityContext`/`EntityCache`/`EntityTypeStore` in `swissarmyhammer-entity`), plus `PasteMatrix` (`swissarmyhammer-kanban`) and `EntitySearchIndex` (`swissarmyhammer-entity-search`). Today this state is reached via Tauri commands (`get_entity`) and `ctx.require_extension` — no MCP surface. This server exposes it generically.\n\n ### Kernel + faces — kanban's OPERATIONS ARE FROZEN\n\n-`EntityContext` is the **kernel** (one CRUD implementation), and both faces already sit over it today (verified: `KanbanContext` holds `Arc<EntityContext>` at `crates/swissarmyhammer-kanban/src/context.rs:35` and delegates generic entity I/O to it at `context.rs:368`):\n-- **`entity`** (this task, NEW) — generic, type-agnostic: get/list/add/update/delete + archive/unarchive for ANY type. For cross-cutting `entity.*` commands and the frontend's generic reads.\n-- **`kanban`** (EXISTS) — domain face: `add task`, `add project`, `update column`, `get tag`, `move task`, `next/complete task`, `assign`, `tag/untag`, board lifecycle. Its operation surface is generated from `crates/swissarmyhammer-kanban/.../schema.rs::kanban_operations()` (single source).\n+`EntityContext` is the **kernel** (one CRUD implementation), and both faces already sit over it today:\n+- **`entity`** (this task, NEW) — generic, type-agnostic: get/list/add/update/delete + archive/unarchive + clipboard + search for ANY type. For cross-cutting `entity.*` commands, the frontend's generic reads, and the search UI.\n+- **`kanban`** (EXISTS) — domain face: `add task`, `add project`, `update column`, `get tag`, `move task`, `next/complete task`, `assign`, `tag/untag`, board lifecycle.\n\n **Hard constraint (per the user): this work does NOT change kanban's operations.** No operation is added to or removed from the `kanban` tool. The `entity` server is purely additive. kanban already reaches `EntityContext` today, so it needs no change to share the kernel. Refactoring kanban's internals to delegate to the kernel more explicitly is **optional and out of scope right now** — implementation may change later; the operation surface may not.\n\n Files:\n-- `crates/swissarmyhammer-entity/src/server.rs` (or a thin `swissarmyhammer-entity-mcp` crate) — `EntityServer` over `Arc<EntityContext>` + shared `Arc<StoreContext>`\n+- `crates/swissarmyhammer-entity/src/server.rs` (or a thin `swissarmyhammer-entity-mcp` crate) — `EntityServer` over `Arc<EntityContext>` + shared `Arc<StoreContext>` + `EntitySearchIndex`\n - `operations.rs` — `#[operation]` structs (entity-type param where relevant):\n   - **read**: `GetEntity { type, id }`, `ListEntities { type, filter? }` (replaces the `get_entity` Tauri command + board-load)\n   - **write**: `AddEntity { type, fields }`, `UpdateField { type, id, field, value }`, `DeleteEntity { type, id }`\n   - **archive**: `ArchiveEntity`, `UnarchiveEntity`\n+  - **clipboard**: `Cut`, `Copy`, `Paste` (wraps `PasteMatrix`; preserves drag-vs-paste)\n+  - **search**: `Search { query, type? }` (wraps `EntitySearchIndex`)\n - `service.rs` — bootstrap `host.expose_rust_module(\"entity\", EntityServer::new(...))`\n\n Writes go through `EntityContext`, which already pushes onto the shared `StoreContext` (undoable) and broadcasts `EntityEvent`s — so undo + the notification surface work for free. Share the SAME `Arc<StoreContext>` as `kanban`/`views`/`store`.\n\n ## Acceptance Criteria\n-- [ ] `entity` registered as an in-process server over `EntityContext` + shared `StoreContext`\n-- [ ] Generic read/write/archive work for any entity type; writes undoable + emit entity events\n-- [ ] **kanban's operation surface is byte-for-byte unchanged** — no op added or removed; a snapshot guard test asserts kanban's `tools/list` + `_meta` operations tree (from `kanban_operations()`) is identical before and after this work\n+- [ ] `entity` registered as an in-process server over `EntityContext` + shared `StoreContext` + `EntitySearchIndex`\n+- [ ] Generic read/write/archive/clipboard/search work for any entity type; writes undoable + emit entity events\n+- [ ] **kanban's operation surface is byte-for-byte unchanged** — no op added or removed; a snapshot guard test asserts kanban's `tools/list` + `_meta` operations tree is identical before and after this work\n - [ ] `entity` and `kanban` resolve through the one `EntityContext` kernel (no duplicate CRUD)\n - [ ] `_meta` operations tree complete\n\n ## Tests\n-- [ ] `crates/swissarmyhammer-entity/tests/integration/entity_server_e2e.rs` — add → get → update_field → delete across two types; archive/unarchive\n+- [ ] `crates/swissarmyhammer-entity/tests/integration/entity_server_e2e.rs` — add → get → update_field → delete across two types; archive/unarchive; clipboard copy→paste duplicates; `Search` finds by text\n - [ ] **kanban-surface-frozen guard**: snapshot the `kanban` tool's `_meta` operations tree; assert this work leaves it unchanged\n - [ ] Parity: `kanban add task` and `entity AddEntity{type:task}` produce the same on-disk result (both via the kernel)\n - [ ] Undo: update_field via `entity`; `store.undo`; assert reverted\n@@ -47,6 +47,6 @@\n - [ ] `cargo test -p swissarmyhammer-entity` passes\n\n ## Workflow\n-- Use `/tdd` — write the kanban-surface-frozen guard + the CRUD test first.\n+- Use `/tdd` — write the kanban-surface-frozen guard + the CRUD/search test first.\n\n-Prerequisite for: entity clipboard + search follow-ups, the entity-commands plugin, the frontend's generic reads. Depends on store-service substrate.\n\\ No newline at end of file\n+Prerequisite for: entity-commands plugin, the frontend's generic reads + search UI. Depends on store-service substrate.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KS618TPG99TPERBZMHFFCYJS","timestamp":"2026-05-21T19:47:47.024748Z","op":"update","item_id":"01KS5EAD57PCBFJGMVB74FF4MK","forward_patch":"--- original\n+++ modified\n@@ -10,7 +10,7 @@\n ---\n ## What\n\n-Build the `entity` MCP server core — the generic, type-agnostic MCP face over the entity **kernel** (`EntityContext`/`EntityCache`/`EntityTypeStore` in `swissarmyhammer-entity`) — covering generic CRUD + archive. Clipboard and search are SEPARATE follow-up tasks on this same server (they wrap types in OTHER crates): clipboard → `01KS616…`, search → `01KS617…`.\n+Build the `entity` MCP server core — the generic, type-agnostic MCP face over the entity **kernel** (`EntityContext`/`EntityCache`/`EntityTypeStore` in `swissarmyhammer-entity`) — covering generic CRUD + archive. Clipboard and search are SEPARATE follow-up tasks on this same server (they wrap types in OTHER crates): clipboard → `01KS614S1YAVEWVR1RHP62SQF0`, search → `01KS61511W6EGZ88043S261RSH`.\n\n Today this state is reached via Tauri commands (`get_entity` at `apps/kanban-app/src/commands.rs:352`, registered `main.rs:71`) and `ctx.require_extension` — no MCP surface. This server exposes it generically.\n\n@@ -49,4 +49,4 @@\n ## Workflow\n - Use `/tdd` — write the kanban-surface-frozen guard + the CRUD test first.\n\n-Prerequisite for: entity clipboard + search follow-ups, the entity-commands plugin, the frontend's generic reads. Depends on store-service substrate.\n\\ No newline at end of file\n+Prerequisite for: entity clipboard (`01KS614S1YAVEWVR1RHP62SQF0`) + search (`01KS61511W6EGZ88043S261RSH`) follow-ups, the entity-commands plugin, the frontend's generic reads. Depends on store-service substrate.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -10,7 +10,7 @@\n ---\n ## What\n\n-Build the `entity` MCP server core — the generic, type-agnostic MCP face over the entity **kernel** (`EntityContext`/`EntityCache`/`EntityTypeStore` in `swissarmyhammer-entity`) — covering generic CRUD + archive. Clipboard and search are SEPARATE follow-up tasks on this same server (they wrap types in OTHER crates): clipboard → `01KS614S1YAVEWVR1RHP62SQF0`, search → `01KS61511W6EGZ88043S261RSH`.\n+Build the `entity` MCP server core — the generic, type-agnostic MCP face over the entity **kernel** (`EntityContext`/`EntityCache`/`EntityTypeStore` in `swissarmyhammer-entity`) — covering generic CRUD + archive. Clipboard and search are SEPARATE follow-up tasks on this same server (they wrap types in OTHER crates): clipboard → `01KS616…`, search → `01KS617…`.\n\n Today this state is reached via Tauri commands (`get_entity` at `apps/kanban-app/src/commands.rs:352`, registered `main.rs:71`) and `ctx.require_extension` — no MCP surface. This server exposes it generically.\n\n@@ -49,4 +49,4 @@\n ## Workflow\n - Use `/tdd` — write the kanban-surface-frozen guard + the CRUD test first.\n\n-Prerequisite for: entity clipboard (`01KS614S1YAVEWVR1RHP62SQF0`) + search (`01KS61511W6EGZ88043S261RSH`) follow-ups, the entity-commands plugin, the frontend's generic reads. Depends on store-service substrate.\n\\ No newline at end of file\n+Prerequisite for: entity clipboard + search follow-ups, the entity-commands plugin, the frontend's generic reads. Depends on store-service substrate.\n\\ No newline at end of file\n","transaction_id":null}

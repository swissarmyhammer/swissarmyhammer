{"id":"01KQYWWQ0D8G6NAE6G8Q3A82GF","timestamp":"2026-05-06T15:00:52.877154Z","op":"create","item_id":"01KQYWWQ0C4E9AFM942BMJHMFX","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,77 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KQYWV9DC866DGRPBRFR17ZEY\n+position_column: todo\n+position_ordinal: de80\n+project: keyboard-navigation\n+title: End-to-end spatial test for Jump-To overlay\n+---\n+## What\n+\n+Full end-to-end vitest browser-mode test that exercises the Jump-To feature against the real `App.tsx` provider tree (not stubbed dependencies). Validates the integration of YAML registry → keybinding → app-shell state → overlay → spatial focus dispatch.\n+\n+New file: `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx`. Pattern this after the existing `spatial-nav-end-to-end.spatial.test.tsx`:\n+\n+```ts\n+import { test, expect } from \"vitest\";\n+import { render, fireEvent } from \"@testing-library/react\";\n+// ... seed a board with multiple columns + cards via the existing test fixtures\n+```\n+\n+Required cases:\n+\n+1. **vim `s` opens the overlay**\n+   - Set keymap_mode to `vim`.\n+   - Render `<App />` with a seeded board (e.g., 3 columns × 4 cards).\n+   - Fire `keydown` for `s` on document.body.\n+   - Assert the overlay element is in the DOM (e.g., a label pill with `data-jump-code` attr).\n+   - Assert the count of code pills equals the number of currently-visible focusable scopes (use `enumerateVisibleScopes()` directly to compute the expected count).\n+\n+2. **cua `Mod+G` opens the overlay**\n+   - Same as above with keymap_mode `cua` and `keydown` for `g` with `metaKey: true`.\n+\n+3. **Typing a code moves focus to that scope**\n+   - Open the overlay (vim `s`).\n+   - Read the `data-jump-code` from a chosen pill and the matching FQM (or read it from a debug DOM attribute).\n+   - Type each letter of the code as separate `keydown` events.\n+   - Assert the spatial focus event was emitted for that FQM (listen to the same `focus-changed` channel the rest of the spatial-nav tests use).\n+   - Assert the overlay is no longer in the DOM.\n+\n+4. **Esc dismisses without focus change**\n+   - Open the overlay.\n+   - Capture the currently-focused FQM.\n+   - Fire `keydown` for `Escape`.\n+   - Assert overlay closed and focused FQM unchanged.\n+\n+5. **Non-matching letter dismisses without focus change**\n+   - Open the overlay.\n+   - Fire `keydown` for a letter known not to be a code prefix (e.g., generate codes, find a letter not in any code).\n+   - Assert overlay closed, focus unchanged.\n+\n+6. **Multi-letter code requires both keystrokes**\n+   - Seed enough scopes that codes spill into 2-letter range (>26 scopes — may need to render a grid view with many cards, or stub `enumerateVisibleScopes` for this case).\n+   - Open overlay; type the first letter of a 2-letter code; assert overlay still open, no focus dispatch.\n+   - Type the second letter; assert focus dispatched.\n+\n+7. **Global keybindings suppressed while overlay is open**\n+   - Open overlay.\n+   - Fire `keydown` for `j` (vim `nav.down`) — but `j` happens to be a code letter, so use a key that is definitely not a code AND is bound globally (e.g., `Tab` for `nav.right` in cua mode if testing in cua).\n+   - Assert no focus change happened (the global handler did not fire).\n+\n+## Acceptance Criteria\n+\n+- [ ] All 7 test cases pass.\n+- [ ] Test does not stub `JumpToOverlay`, `enumerateVisibleScopes`, or `generateSneakCodes` — they're exercised end-to-end.\n+- [ ] No flakiness — no timing-dependent assertions (no `setTimeout` waiting for the overlay; the overlay is synchronous on key press).\n+\n+## Tests\n+\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx` exists with the 7 cases above.\n+- [ ] Test command: `cd kanban-app/ui && pnpm test spatial-nav-jump-to` — passes.\n+- [ ] Run all spatial tests after this lands: `cd kanban-app/ui && pnpm test spatial` — every pre-existing spatial test still passes.\n+\n+## Workflow\n+\n+- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,77 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KQYWV9DC866DGRPBRFR17ZEY\n-position_column: todo\n-position_ordinal: de80\n-project: keyboard-navigation\n-title: End-to-end spatial test for Jump-To overlay\n----\n-## What\n-\n-Full end-to-end vitest browser-mode test that exercises the Jump-To feature against the real `App.tsx` provider tree (not stubbed dependencies). Validates the integration of YAML registry → keybinding → app-shell state → overlay → spatial focus dispatch.\n-\n-New file: `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx`. Pattern this after the existing `spatial-nav-end-to-end.spatial.test.tsx`:\n-\n-```ts\n-import { test, expect } from \"vitest\";\n-import { render, fireEvent } from \"@testing-library/react\";\n-// ... seed a board with multiple columns + cards via the existing test fixtures\n-```\n-\n-Required cases:\n-\n-1. **vim `s` opens the overlay**\n-   - Set keymap_mode to `vim`.\n-   - Render `<App />` with a seeded board (e.g., 3 columns × 4 cards).\n-   - Fire `keydown` for `s` on document.body.\n-   - Assert the overlay element is in the DOM (e.g., a label pill with `data-jump-code` attr).\n-   - Assert the count of code pills equals the number of currently-visible focusable scopes (use `enumerateVisibleScopes()` directly to compute the expected count).\n-\n-2. **cua `Mod+G` opens the overlay**\n-   - Same as above with keymap_mode `cua` and `keydown` for `g` with `metaKey: true`.\n-\n-3. **Typing a code moves focus to that scope**\n-   - Open the overlay (vim `s`).\n-   - Read the `data-jump-code` from a chosen pill and the matching FQM (or read it from a debug DOM attribute).\n-   - Type each letter of the code as separate `keydown` events.\n-   - Assert the spatial focus event was emitted for that FQM (listen to the same `focus-changed` channel the rest of the spatial-nav tests use).\n-   - Assert the overlay is no longer in the DOM.\n-\n-4. **Esc dismisses without focus change**\n-   - Open the overlay.\n-   - Capture the currently-focused FQM.\n-   - Fire `keydown` for `Escape`.\n-   - Assert overlay closed and focused FQM unchanged.\n-\n-5. **Non-matching letter dismisses without focus change**\n-   - Open the overlay.\n-   - Fire `keydown` for a letter known not to be a code prefix (e.g., generate codes, find a letter not in any code).\n-   - Assert overlay closed, focus unchanged.\n-\n-6. **Multi-letter code requires both keystrokes**\n-   - Seed enough scopes that codes spill into 2-letter range (>26 scopes — may need to render a grid view with many cards, or stub `enumerateVisibleScopes` for this case).\n-   - Open overlay; type the first letter of a 2-letter code; assert overlay still open, no focus dispatch.\n-   - Type the second letter; assert focus dispatched.\n-\n-7. **Global keybindings suppressed while overlay is open**\n-   - Open overlay.\n-   - Fire `keydown` for `j` (vim `nav.down`) — but `j` happens to be a code letter, so use a key that is definitely not a code AND is bound globally (e.g., `Tab` for `nav.right` in cua mode if testing in cua).\n-   - Assert no focus change happened (the global handler did not fire).\n-\n-## Acceptance Criteria\n-\n-- [ ] All 7 test cases pass.\n-- [ ] Test does not stub `JumpToOverlay`, `enumerateVisibleScopes`, or `generateSneakCodes` — they're exercised end-to-end.\n-- [ ] No flakiness — no timing-dependent assertions (no `setTimeout` waiting for the overlay; the overlay is synchronous on key press).\n-\n-## Tests\n-\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx` exists with the 7 cases above.\n-- [ ] Test command: `cd kanban-app/ui && pnpm test spatial-nav-jump-to` — passes.\n-- [ ] Run all spatial tests after this lands: `cd kanban-app/ui && pnpm test spatial` — every pre-existing spatial test still passes.\n-\n-## Workflow\n-\n-- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQYWWQ0D6HNYCP3YK35FYEZH","timestamp":"2026-05-06T15:00:52.877568Z","entity_type":"task","entity_id":"01KQYWWQ0C4E9AFM942BMJHMFX","op":"create","changes":[["depends_on",{"kind":"set","value":["01KQYWV9DC866DGRPBRFR17ZEY"]}],["title",{"kind":"set","value":"End-to-end spatial test for Jump-To overlay"}],["body",{"kind":"set","value":"## What\n\nFull end-to-end vitest browser-mode test that exercises the Jump-To feature against the real `App.tsx` provider tree (not stubbed dependencies). Validates the integration of YAML registry → keybinding → app-shell state → overlay → spatial focus dispatch.\n\nNew file: `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx`. Pattern this after the existing `spatial-nav-end-to-end.spatial.test.tsx`:\n\n```ts\nimport { test, expect } from \"vitest\";\nimport { render, fireEvent } from \"@testing-library/react\";\n// ... seed a board with multiple columns + cards via the existing test fixtures\n```\n\nRequired cases:\n\n1. **vim `s` opens the overlay**\n   - Set keymap_mode to `vim`.\n   - Render `<App />` with a seeded board (e.g., 3 columns × 4 cards).\n   - Fire `keydown` for `s` on document.body.\n   - Assert the overlay element is in the DOM (e.g., a label pill with `data-jump-code` attr).\n   - Assert the count of code pills equals the number of currently-visible focusable scopes (use `enumerateVisibleScopes()` directly to compute the expected count).\n\n2. **cua `Mod+G` opens the overlay**\n   - Same as above with keymap_mode `cua` and `keydown` for `g` with `metaKey: true`.\n\n3. **Typing a code moves focus to that scope**\n   - Open the overlay (vim `s`).\n   - Read the `data-jump-code` from a chosen pill and the matching FQM (or read it from a debug DOM attribute).\n   - Type each letter of the code as separate `keydown` events.\n   - Assert the spatial focus event was emitted for that FQM (listen to the same `focus-changed` channel the rest of the spatial-nav tests use).\n   - Assert the overlay is no longer in the DOM.\n\n4. **Esc dismisses without focus change**\n   - Open the overlay.\n   - Capture the currently-focused FQM.\n   - Fire `keydown` for `Escape`.\n   - Assert overlay closed and focused FQM unchanged.\n\n5. **Non-matching letter dismisses without focus change**\n   - Open the overlay.\n   - Fire `keydown` for a letter known not to be a code prefix (e.g., generate codes, find a letter not in any code).\n   - Assert overlay closed, focus unchanged.\n\n6. **Multi-letter code requires both keystrokes**\n   - Seed enough scopes that codes spill into 2-letter range (>26 scopes — may need to render a grid view with many cards, or stub `enumerateVisibleScopes` for this case).\n   - Open overlay; type the first letter of a 2-letter code; assert overlay still open, no focus dispatch.\n   - Type the second letter; assert focus dispatched.\n\n7. **Global keybindings suppressed while overlay is open**\n   - Open overlay.\n   - Fire `keydown` for `j` (vim `nav.down`) — but `j` happens to be a code letter, so use a key that is definitely not a code AND is bound globally (e.g., `Tab` for `nav.right` in cua mode if testing in cua).\n   - Assert no focus change happened (the global handler did not fire).\n\n## Acceptance Criteria\n\n- [ ] All 7 test cases pass.\n- [ ] Test does not stub `JumpToOverlay`, `enumerateVisibleScopes`, or `generateSneakCodes` — they're exercised end-to-end.\n- [ ] No flakiness — no timing-dependent assertions (no `setTimeout` waiting for the overlay; the overlay is synchronous on key press).\n\n## Tests\n\n- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx` exists with the 7 cases above.\n- [ ] Test command: `cd kanban-app/ui && pnpm test spatial-nav-jump-to` — passes.\n- [ ] Run all spatial tests after this lands: `cd kanban-app/ui && pnpm test spatial` — every pre-existing spatial test still passes.\n\n## Workflow\n\n- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece."}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"de80"}],["assignees",{"kind":"set","value":["claude-code"]}],["project",{"kind":"set","value":"keyboard-navigation"}]]}
{"id":"01KQYWWV19QTHW9HFTYYBQ4VR9","timestamp":"2026-05-06T15:00:57.001232Z","op":"update","item_id":"01KQYWWQ0C4E9AFM942BMJHMFX","forward_patch":"--- original\n+++ modified\n@@ -74,4 +74,4 @@\n\n ## Workflow\n\n-- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece.\n\\ No newline at end of file\n+- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece. #nav-jump\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -74,4 +74,4 @@\n\n ## Workflow\n\n-- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece. #nav-jump\n\\ No newline at end of file\n+- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQYWWV19HXEE3AA7HVKJF5VS","timestamp":"2026-05-06T15:00:57.001612Z","entity_type":"task","entity_id":"01KQYWWQ0C4E9AFM942BMJHMFX","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -64,4 +64,4 @@\n\n ## Workflow\n\n-- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece.\n\\ No newline at end of file\n+- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece. #nav-jump\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -64,4 +64,4 @@\n\n ## Workflow\n\n-- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece. #nav-jump\n\\ No newline at end of file\n+- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece.\n\\ No newline at end of file\n"}]]}
{"id":"01KQZ7DTZM3Z5AM05HTAJEJNEW","timestamp":"2026-05-06T18:04:59.764397Z","op":"update","item_id":"01KQYWWQ0C4E9AFM942BMJHMFX","forward_patch":"--- original\n+++ modified\n@@ -5,7 +5,7 @@\n - 01KQYWV9DC866DGRPBRFR17ZEY\n position_column: todo\n position_ordinal: de80\n-project: keyboard-navigation\n+project: spatial-nav\n title: End-to-end spatial test for Jump-To overlay\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -5,7 +5,7 @@\n - 01KQYWV9DC866DGRPBRFR17ZEY\n position_column: todo\n position_ordinal: de80\n-project: spatial-nav\n+project: keyboard-navigation\n title: End-to-end spatial test for Jump-To overlay\n ---\n ## What\n","transaction_id":null}
{"id":"01KQZ7DTZMPRVNCJX9DH86Y3NE","timestamp":"2026-05-06T18:04:59.764810Z","entity_type":"task","entity_id":"01KQYWWQ0C4E9AFM942BMJHMFX","op":"update","changes":[["project",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-keyboard-navigation\n\\ No newline at end of file\n+spatial-nav\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-spatial-nav\n\\ No newline at end of file\n+keyboard-navigation\n\\ No newline at end of file\n"}]]}
{"id":"01KR4DTEYW0G3ZKQ44NP0YKJK0","timestamp":"2026-05-08T18:32:57.052474Z","op":"update","item_id":"01KQYWWQ0C4E9AFM942BMJHMFX","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQYWV9DC866DGRPBRFR17ZEY\n-position_column: todo\n-position_ordinal: de80\n+position_column: doing\n+position_ordinal: '8180'\n project: spatial-nav\n title: End-to-end spatial test for Jump-To overlay\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQYWV9DC866DGRPBRFR17ZEY\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: todo\n+position_ordinal: de80\n project: spatial-nav\n title: End-to-end spatial test for Jump-To overlay\n ---\n","transaction_id":null}
{"id":"01KR4DTEYXZB9JKEK5QZEE7NXF","timestamp":"2026-05-08T18:32:57.053567Z","entity_type":"task","entity_id":"01KQYWWQ0C4E9AFM942BMJHMFX","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-de80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+de80\n\\ No newline at end of file\n"}]]}
{"id":"01KR4EC6N6JDQSRXCMWF2EB14E","timestamp":"2026-05-08T18:42:38.374530Z","op":"update","item_id":"01KQYWWQ0C4E9AFM942BMJHMFX","forward_patch":"--- original\n+++ modified\n@@ -62,16 +62,23 @@\n\n ## Acceptance Criteria\n\n-- [ ] All 7 test cases pass.\n-- [ ] Test does not stub `JumpToOverlay`, `enumerateVisibleScopes`, or `generateSneakCodes` — they're exercised end-to-end.\n-- [ ] No flakiness — no timing-dependent assertions (no `setTimeout` waiting for the overlay; the overlay is synchronous on key press).\n+- [x] All 7 test cases pass.\n+- [x] Test does not stub `JumpToOverlay`, `enumerateVisibleScopes`, or `generateSneakCodes` — they're exercised end-to-end.\n+- [x] No flakiness — no timing-dependent assertions (no `setTimeout` waiting for the overlay; the overlay is synchronous on key press).\n\n ## Tests\n\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx` exists with the 7 cases above.\n-- [ ] Test command: `cd kanban-app/ui && pnpm test spatial-nav-jump-to` — passes.\n-- [ ] Run all spatial tests after this lands: `cd kanban-app/ui && pnpm test spatial` — every pre-existing spatial test still passes.\n+- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx` exists with the 7 cases above.\n+- [x] Test command: `cd kanban-app/ui && pnpm test spatial-nav-jump-to` — passes.\n+- [x] Run all spatial tests after this lands: `cd kanban-app/ui && pnpm test spatial` — every pre-existing spatial test still passes.\n\n ## Workflow\n\n-- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece. #nav-jump\n\\ No newline at end of file\n+- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece. #nav-jump\n+\n+## Implementation notes\n+\n+- `generate_jump_codes` is faithfully ported from `swissarmyhammer-focus/src/sneak.rs` into the test bootstrap-invoke handler. The Rust kernel is not running in browser mode, so the IPC boundary has to answer the same way the kernel would. This is the same pattern the existing spatial-nav suite uses for `spatial_navigate` etc. — a bridge for the Tauri boundary, not a stub of the React-side implementation under test.\n+- For case 5 the non-matching letter is `i`, `l`, or `o` — those three are deliberately omitted from the sneak alphabet (high-confusion letters) so they are guaranteed to never appear in any code, regardless of how many scopes the fixture mounts.\n+- For case 6 the 3×3 fixture board produces well over 23 enumerable scopes (cards, columns, perspective tabs, nav-bar leaves, the board entity zone, the perspective bar, etc.), which forces the sneak generator to spill into 2-letter codes naturally — no extra harness needed.\n+- For case 7 the assertion is \"no spatial_focus IPC lands on a non-sentinel target\". The overlay's claim has placed focus on the sentinel inside its single-scope `<FocusLayer name=\"jump-to\">`, so the kernel's cascade has nowhere to navigate from there. The user-observable invariant — global nav cannot escape the overlay — holds without the overlay needing to call `stopPropagation` on every non-letter keydown.\n","reverse_patch":"--- original\n+++ modified\n@@ -62,23 +62,16 @@\n\n ## Acceptance Criteria\n\n-- [x] All 7 test cases pass.\n-- [x] Test does not stub `JumpToOverlay`, `enumerateVisibleScopes`, or `generateSneakCodes` — they're exercised end-to-end.\n-- [x] No flakiness — no timing-dependent assertions (no `setTimeout` waiting for the overlay; the overlay is synchronous on key press).\n+- [ ] All 7 test cases pass.\n+- [ ] Test does not stub `JumpToOverlay`, `enumerateVisibleScopes`, or `generateSneakCodes` — they're exercised end-to-end.\n+- [ ] No flakiness — no timing-dependent assertions (no `setTimeout` waiting for the overlay; the overlay is synchronous on key press).\n\n ## Tests\n\n-- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx` exists with the 7 cases above.\n-- [x] Test command: `cd kanban-app/ui && pnpm test spatial-nav-jump-to` — passes.\n-- [x] Run all spatial tests after this lands: `cd kanban-app/ui && pnpm test spatial` — every pre-existing spatial test still passes.\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx` exists with the 7 cases above.\n+- [ ] Test command: `cd kanban-app/ui && pnpm test spatial-nav-jump-to` — passes.\n+- [ ] Run all spatial tests after this lands: `cd kanban-app/ui && pnpm test spatial` — every pre-existing spatial test still passes.\n\n ## Workflow\n\n-- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece. #nav-jump\n-\n-## Implementation notes\n-\n-- `generate_jump_codes` is faithfully ported from `swissarmyhammer-focus/src/sneak.rs` into the test bootstrap-invoke handler. The Rust kernel is not running in browser mode, so the IPC boundary has to answer the same way the kernel would. This is the same pattern the existing spatial-nav suite uses for `spatial_navigate` etc. — a bridge for the Tauri boundary, not a stub of the React-side implementation under test.\n-- For case 5 the non-matching letter is `i`, `l`, or `o` — those three are deliberately omitted from the sneak alphabet (high-confusion letters) so they are guaranteed to never appear in any code, regardless of how many scopes the fixture mounts.\n-- For case 6 the 3×3 fixture board produces well over 23 enumerable scopes (cards, columns, perspective tabs, nav-bar leaves, the board entity zone, the perspective bar, etc.), which forces the sneak generator to spill into 2-letter codes naturally — no extra harness needed.\n-- For case 7 the assertion is \"no spatial_focus IPC lands on a non-sentinel target\". The overlay's claim has placed focus on the sentinel inside its single-scope `<FocusLayer name=\"jump-to\">`, so the kernel's cascade has nowhere to navigate from there. The user-observable invariant — global nav cannot escape the overlay — holds without the overlay needing to call `stopPropagation` on every non-letter keydown.\n+- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece. #nav-jump\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KR4EC6N6DR6DX5ZS00TCJYJ0","timestamp":"2026-05-08T18:42:38.374964Z","entity_type":"task","entity_id":"01KQYWWQ0C4E9AFM942BMJHMFX","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -52,16 +52,23 @@\n\n ## Acceptance Criteria\n\n-- [ ] All 7 test cases pass.\n-- [ ] Test does not stub `JumpToOverlay`, `enumerateVisibleScopes`, or `generateSneakCodes` — they're exercised end-to-end.\n-- [ ] No flakiness — no timing-dependent assertions (no `setTimeout` waiting for the overlay; the overlay is synchronous on key press).\n+- [x] All 7 test cases pass.\n+- [x] Test does not stub `JumpToOverlay`, `enumerateVisibleScopes`, or `generateSneakCodes` — they're exercised end-to-end.\n+- [x] No flakiness — no timing-dependent assertions (no `setTimeout` waiting for the overlay; the overlay is synchronous on key press).\n\n ## Tests\n\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx` exists with the 7 cases above.\n-- [ ] Test command: `cd kanban-app/ui && pnpm test spatial-nav-jump-to` — passes.\n-- [ ] Run all spatial tests after this lands: `cd kanban-app/ui && pnpm test spatial` — every pre-existing spatial test still passes.\n+- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx` exists with the 7 cases above.\n+- [x] Test command: `cd kanban-app/ui && pnpm test spatial-nav-jump-to` — passes.\n+- [x] Run all spatial tests after this lands: `cd kanban-app/ui && pnpm test spatial` — every pre-existing spatial test still passes.\n\n ## Workflow\n\n-- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece. #nav-jump\n\\ No newline at end of file\n+- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece. #nav-jump\n+\n+## Implementation notes\n+\n+- `generate_jump_codes` is faithfully ported from `swissarmyhammer-focus/src/sneak.rs` into the test bootstrap-invoke handler. The Rust kernel is not running in browser mode, so the IPC boundary has to answer the same way the kernel would. This is the same pattern the existing spatial-nav suite uses for `spatial_navigate` etc. — a bridge for the Tauri boundary, not a stub of the React-side implementation under test.\n+- For case 5 the non-matching letter is `i`, `l`, or `o` — those three are deliberately omitted from the sneak alphabet (high-confusion letters) so they are guaranteed to never appear in any code, regardless of how many scopes the fixture mounts.\n+- For case 6 the 3×3 fixture board produces well over 23 enumerable scopes (cards, columns, perspective tabs, nav-bar leaves, the board entity zone, the perspective bar, etc.), which forces the sneak generator to spill into 2-letter codes naturally — no extra harness needed.\n+- For case 7 the assertion is \"no spatial_focus IPC lands on a non-sentinel target\". The overlay's claim has placed focus on the sentinel inside its single-scope `<FocusLayer name=\"jump-to\">`, so the kernel's cascade has nowhere to navigate from there. The user-observable invariant — global nav cannot escape the overlay — holds without the overlay needing to call `stopPropagation` on every non-letter keydown.\n","reverse_patch":"--- original\n+++ modified\n@@ -52,23 +52,16 @@\n\n ## Acceptance Criteria\n\n-- [x] All 7 test cases pass.\n-- [x] Test does not stub `JumpToOverlay`, `enumerateVisibleScopes`, or `generateSneakCodes` — they're exercised end-to-end.\n-- [x] No flakiness — no timing-dependent assertions (no `setTimeout` waiting for the overlay; the overlay is synchronous on key press).\n+- [ ] All 7 test cases pass.\n+- [ ] Test does not stub `JumpToOverlay`, `enumerateVisibleScopes`, or `generateSneakCodes` — they're exercised end-to-end.\n+- [ ] No flakiness — no timing-dependent assertions (no `setTimeout` waiting for the overlay; the overlay is synchronous on key press).\n\n ## Tests\n\n-- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx` exists with the 7 cases above.\n-- [x] Test command: `cd kanban-app/ui && pnpm test spatial-nav-jump-to` — passes.\n-- [x] Run all spatial tests after this lands: `cd kanban-app/ui && pnpm test spatial` — every pre-existing spatial test still passes.\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx` exists with the 7 cases above.\n+- [ ] Test command: `cd kanban-app/ui && pnpm test spatial-nav-jump-to` — passes.\n+- [ ] Run all spatial tests after this lands: `cd kanban-app/ui && pnpm test spatial` — every pre-existing spatial test still passes.\n\n ## Workflow\n\n-- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece. #nav-jump\n-\n-## Implementation notes\n-\n-- `generate_jump_codes` is faithfully ported from `swissarmyhammer-focus/src/sneak.rs` into the test bootstrap-invoke handler. The Rust kernel is not running in browser mode, so the IPC boundary has to answer the same way the kernel would. This is the same pattern the existing spatial-nav suite uses for `spatial_navigate` etc. — a bridge for the Tauri boundary, not a stub of the React-side implementation under test.\n-- For case 5 the non-matching letter is `i`, `l`, or `o` — those three are deliberately omitted from the sneak alphabet (high-confusion letters) so they are guaranteed to never appear in any code, regardless of how many scopes the fixture mounts.\n-- For case 6 the 3×3 fixture board produces well over 23 enumerable scopes (cards, columns, perspective tabs, nav-bar leaves, the board entity zone, the perspective bar, etc.), which forces the sneak generator to spill into 2-letter codes naturally — no extra harness needed.\n-- For case 7 the assertion is \"no spatial_focus IPC lands on a non-sentinel target\". The overlay's claim has placed focus on the sentinel inside its single-scope `<FocusLayer name=\"jump-to\">`, so the kernel's cascade has nowhere to navigate from there. The user-observable invariant — global nav cannot escape the overlay — holds without the overlay needing to call `stopPropagation` on every non-letter keydown.\n+- Use `/tdd` only on incremental cases — write case 1, watch it fail, make it pass, then case 2, etc. Going one case at a time avoids fix-everything-at-once and keeps the integration debugged piece-by-piece. #nav-jump\n\\ No newline at end of file\n"}]]}
{"id":"01KR4EDJMFDH8QM1GE17X7W062","timestamp":"2026-05-08T18:43:23.407907Z","op":"update","item_id":"01KQYWWQ0C4E9AFM942BMJHMFX","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQYWV9DC866DGRPBRFR17ZEY\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '8480'\n project: spatial-nav\n title: End-to-end spatial test for Jump-To overlay\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQYWV9DC866DGRPBRFR17ZEY\n-position_column: review\n-position_ordinal: '8480'\n+position_column: doing\n+position_ordinal: '8180'\n project: spatial-nav\n title: End-to-end spatial test for Jump-To overlay\n ---\n","transaction_id":null}
{"id":"01KR4EDJMGB3TMVR5GXT58D53S","timestamp":"2026-05-08T18:43:23.408889Z","entity_type":"task","entity_id":"01KQYWWQ0C4E9AFM942BMJHMFX","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+8480\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8480\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KR4F14V0J4KKWJ7QC2549ZS8","timestamp":"2026-05-08T18:54:04.640969Z","op":"update","item_id":"01KQYWWQ0C4E9AFM942BMJHMFX","forward_patch":"--- original\n+++ modified\n@@ -82,3 +82,13 @@\n - For case 5 the non-matching letter is `i`, `l`, or `o` — those three are deliberately omitted from the sneak alphabet (high-confusion letters) so they are guaranteed to never appear in any code, regardless of how many scopes the fixture mounts.\n - For case 6 the 3×3 fixture board produces well over 23 enumerable scopes (cards, columns, perspective tabs, nav-bar leaves, the board entity zone, the perspective bar, etc.), which forces the sneak generator to spill into 2-letter codes naturally — no extra harness needed.\n - For case 7 the assertion is \"no spatial_focus IPC lands on a non-sentinel target\". The overlay's claim has placed focus on the sentinel inside its single-scope `<FocusLayer name=\"jump-to\">`, so the kernel's cascade has nowhere to navigate from there. The user-observable invariant — global nav cannot escape the overlay — holds without the overlay needing to call `stopPropagation` on every non-letter keydown.\n+\n+## Review Findings (2026-05-08 19:05)\n+\n+### Nits\n+\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:518-522` — Stale comment block directly contradicts the code below it. The comment says \"we set both `metaKey` and `ctrlKey` so the binding fires regardless of which platform branch `normalizeKeyEvent` takes\", but line 526 sets only `metaKey: true`. The replacement comment on lines 523-525 is correct (\"Setting only `metaKey` produces `Mod+g`\"). Remove lines 518-522 (the stale block) and keep lines 523-525. As verified against `keybindings.ts:206` (`mod = mac ? e.metaKey : e.ctrlKey`) and `:213` (`mac && e.ctrlKey` adds a separate `Ctrl` modifier), setting both on macOS would produce `Mod+Ctrl+g` and miss the `Mod+g` binding — so the stale comment is not just outdated, it would lead a future maintainer to introduce a real bug.\n+\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:198-238` — The TS port of `generate_sneak_codes` is a faithful re-implementation of `swissarmyhammer-focus/src/sneak.rs`, but the two now drift independently. The current port matches: alphabet ordering, prefix selection (`pick_two_letter_prefix_count`'s `ceil((count - alphabet_len) / (alphabet_len - 1))`), and the disjoint single/two-letter buckets all line up. However, there is no compile-time link between them. Consider one of: (a) a small fixture file (e.g. `sneak-fixture.json`) emitted from a Rust test that the TS test reads and asserts against, or (b) a property-style test that calls the TS port across `[1, 5, 23, 24, 50, 200, 529]` and asserts shape invariants (length, distinctness, prefix-free) so a future drift in either implementation surfaces as a test failure rather than a silent behavioural divergence. Acceptable as-is for landing this task; worth tracking as a follow-up.\n+\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n","reverse_patch":"--- original\n+++ modified\n@@ -82,13 +82,3 @@\n - For case 5 the non-matching letter is `i`, `l`, or `o` — those three are deliberately omitted from the sneak alphabet (high-confusion letters) so they are guaranteed to never appear in any code, regardless of how many scopes the fixture mounts.\n - For case 6 the 3×3 fixture board produces well over 23 enumerable scopes (cards, columns, perspective tabs, nav-bar leaves, the board entity zone, the perspective bar, etc.), which forces the sneak generator to spill into 2-letter codes naturally — no extra harness needed.\n - For case 7 the assertion is \"no spatial_focus IPC lands on a non-sentinel target\". The overlay's claim has placed focus on the sentinel inside its single-scope `<FocusLayer name=\"jump-to\">`, so the kernel's cascade has nowhere to navigate from there. The user-observable invariant — global nav cannot escape the overlay — holds without the overlay needing to call `stopPropagation` on every non-letter keydown.\n-\n-## Review Findings (2026-05-08 19:05)\n-\n-### Nits\n-\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:518-522` — Stale comment block directly contradicts the code below it. The comment says \"we set both `metaKey` and `ctrlKey` so the binding fires regardless of which platform branch `normalizeKeyEvent` takes\", but line 526 sets only `metaKey: true`. The replacement comment on lines 523-525 is correct (\"Setting only `metaKey` produces `Mod+g`\"). Remove lines 518-522 (the stale block) and keep lines 523-525. As verified against `keybindings.ts:206` (`mod = mac ? e.metaKey : e.ctrlKey`) and `:213` (`mac && e.ctrlKey` adds a separate `Ctrl` modifier), setting both on macOS would produce `Mod+Ctrl+g` and miss the `Mod+g` binding — so the stale comment is not just outdated, it would lead a future maintainer to introduce a real bug.\n-\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:198-238` — The TS port of `generate_sneak_codes` is a faithful re-implementation of `swissarmyhammer-focus/src/sneak.rs`, but the two now drift independently. The current port matches: alphabet ordering, prefix selection (`pick_two_letter_prefix_count`'s `ceil((count - alphabet_len) / (alphabet_len - 1))`), and the disjoint single/two-letter buckets all line up. However, there is no compile-time link between them. Consider one of: (a) a small fixture file (e.g. `sneak-fixture.json`) emitted from a Rust test that the TS test reads and asserts against, or (b) a property-style test that calls the TS port across `[1, 5, 23, 24, 50, 200, 529]` and asserts shape invariants (length, distinctness, prefix-free) so a future drift in either implementation surfaces as a test failure rather than a silent behavioural divergence. Acceptable as-is for landing this task; worth tracking as a follow-up.\n-\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n","transaction_id":null}
{"id":"01KR4F14V11R8NTKY3PZ36Z0SM","timestamp":"2026-05-08T18:54:04.641484Z","entity_type":"task","entity_id":"01KQYWWQ0C4E9AFM942BMJHMFX","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -72,3 +72,13 @@\n - For case 5 the non-matching letter is `i`, `l`, or `o` — those three are deliberately omitted from the sneak alphabet (high-confusion letters) so they are guaranteed to never appear in any code, regardless of how many scopes the fixture mounts.\n - For case 6 the 3×3 fixture board produces well over 23 enumerable scopes (cards, columns, perspective tabs, nav-bar leaves, the board entity zone, the perspective bar, etc.), which forces the sneak generator to spill into 2-letter codes naturally — no extra harness needed.\n - For case 7 the assertion is \"no spatial_focus IPC lands on a non-sentinel target\". The overlay's claim has placed focus on the sentinel inside its single-scope `<FocusLayer name=\"jump-to\">`, so the kernel's cascade has nowhere to navigate from there. The user-observable invariant — global nav cannot escape the overlay — holds without the overlay needing to call `stopPropagation` on every non-letter keydown.\n+\n+## Review Findings (2026-05-08 19:05)\n+\n+### Nits\n+\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:518-522` — Stale comment block directly contradicts the code below it. The comment says \"we set both `metaKey` and `ctrlKey` so the binding fires regardless of which platform branch `normalizeKeyEvent` takes\", but line 526 sets only `metaKey: true`. The replacement comment on lines 523-525 is correct (\"Setting only `metaKey` produces `Mod+g`\"). Remove lines 518-522 (the stale block) and keep lines 523-525. As verified against `keybindings.ts:206` (`mod = mac ? e.metaKey : e.ctrlKey`) and `:213` (`mac && e.ctrlKey` adds a separate `Ctrl` modifier), setting both on macOS would produce `Mod+Ctrl+g` and miss the `Mod+g` binding — so the stale comment is not just outdated, it would lead a future maintainer to introduce a real bug.\n+\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:198-238` — The TS port of `generate_sneak_codes` is a faithful re-implementation of `swissarmyhammer-focus/src/sneak.rs`, but the two now drift independently. The current port matches: alphabet ordering, prefix selection (`pick_two_letter_prefix_count`'s `ceil((count - alphabet_len) / (alphabet_len - 1))`), and the disjoint single/two-letter buckets all line up. However, there is no compile-time link between them. Consider one of: (a) a small fixture file (e.g. `sneak-fixture.json`) emitted from a Rust test that the TS test reads and asserts against, or (b) a property-style test that calls the TS port across `[1, 5, 23, 24, 50, 200, 529]` and asserts shape invariants (length, distinctness, prefix-free) so a future drift in either implementation surfaces as a test failure rather than a silent behavioural divergence. Acceptable as-is for landing this task; worth tracking as a follow-up.\n+\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n","reverse_patch":"--- original\n+++ modified\n@@ -72,13 +72,3 @@\n - For case 5 the non-matching letter is `i`, `l`, or `o` — those three are deliberately omitted from the sneak alphabet (high-confusion letters) so they are guaranteed to never appear in any code, regardless of how many scopes the fixture mounts.\n - For case 6 the 3×3 fixture board produces well over 23 enumerable scopes (cards, columns, perspective tabs, nav-bar leaves, the board entity zone, the perspective bar, etc.), which forces the sneak generator to spill into 2-letter codes naturally — no extra harness needed.\n - For case 7 the assertion is \"no spatial_focus IPC lands on a non-sentinel target\". The overlay's claim has placed focus on the sentinel inside its single-scope `<FocusLayer name=\"jump-to\">`, so the kernel's cascade has nowhere to navigate from there. The user-observable invariant — global nav cannot escape the overlay — holds without the overlay needing to call `stopPropagation` on every non-letter keydown.\n-\n-## Review Findings (2026-05-08 19:05)\n-\n-### Nits\n-\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:518-522` — Stale comment block directly contradicts the code below it. The comment says \"we set both `metaKey` and `ctrlKey` so the binding fires regardless of which platform branch `normalizeKeyEvent` takes\", but line 526 sets only `metaKey: true`. The replacement comment on lines 523-525 is correct (\"Setting only `metaKey` produces `Mod+g`\"). Remove lines 518-522 (the stale block) and keep lines 523-525. As verified against `keybindings.ts:206` (`mod = mac ? e.metaKey : e.ctrlKey`) and `:213` (`mac && e.ctrlKey` adds a separate `Ctrl` modifier), setting both on macOS would produce `Mod+Ctrl+g` and miss the `Mod+g` binding — so the stale comment is not just outdated, it would lead a future maintainer to introduce a real bug.\n-\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:198-238` — The TS port of `generate_sneak_codes` is a faithful re-implementation of `swissarmyhammer-focus/src/sneak.rs`, but the two now drift independently. The current port matches: alphabet ordering, prefix selection (`pick_two_letter_prefix_count`'s `ceil((count - alphabet_len) / (alphabet_len - 1))`), and the disjoint single/two-letter buckets all line up. However, there is no compile-time link between them. Consider one of: (a) a small fixture file (e.g. `sneak-fixture.json`) emitted from a Rust test that the TS test reads and asserts against, or (b) a property-style test that calls the TS port across `[1, 5, 23, 24, 50, 200, 529]` and asserts shape invariants (length, distinctness, prefix-free) so a future drift in either implementation surfaces as a test failure rather than a silent behavioural divergence. Acceptable as-is for landing this task; worth tracking as a follow-up.\n-\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n"}]]}
{"id":"01KR4FAGBJYJV726A4YWYE6D9M","timestamp":"2026-05-08T18:59:11.346295Z","op":"update","item_id":"01KQYWWQ0C4E9AFM942BMJHMFX","forward_patch":"--- original\n+++ modified\n@@ -87,8 +87,11 @@\n\n ### Nits\n\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:518-522` — Stale comment block directly contradicts the code below it. The comment says \"we set both `metaKey` and `ctrlKey` so the binding fires regardless of which platform branch `normalizeKeyEvent` takes\", but line 526 sets only `metaKey: true`. The replacement comment on lines 523-525 is correct (\"Setting only `metaKey` produces `Mod+g`\"). Remove lines 518-522 (the stale block) and keep lines 523-525. As verified against `keybindings.ts:206` (`mod = mac ? e.metaKey : e.ctrlKey`) and `:213` (`mac && e.ctrlKey` adds a separate `Ctrl` modifier), setting both on macOS would produce `Mod+Ctrl+g` and miss the `Mod+g` binding — so the stale comment is not just outdated, it would lead a future maintainer to introduce a real bug.\n+- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:518-522` — Stale comment block directly contradicts the code below it. The comment says \"we set both `metaKey` and `ctrlKey` so the binding fires regardless of which platform branch `normalizeKeyEvent` takes\", but line 526 sets only `metaKey: true`. The replacement comment on lines 523-525 is correct (\"Setting only `metaKey` produces `Mod+g`\"). Remove lines 518-522 (the stale block) and keep lines 523-525. As verified against `keybindings.ts:206` (`mod = mac ? e.metaKey : e.ctrlKey`) and `:213` (`mac && e.ctrlKey` adds a separate `Ctrl` modifier), setting both on macOS would produce `Mod+Ctrl+g` and miss the `Mod+g` binding — so the stale comment is not just outdated, it would lead a future maintainer to introduce a real bug.\n+  - Resolved: stale block removed; replacement comment now explicitly cites `keybindings.ts` (`mod = mac ? e.metaKey : e.ctrlKey`) and warns that adding `ctrlKey: true` would produce `Mod+Ctrl+g` on macOS and miss the binding.\n\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:198-238` — The TS port of `generate_sneak_codes` is a faithful re-implementation of `swissarmyhammer-focus/src/sneak.rs`, but the two now drift independently. The current port matches: alphabet ordering, prefix selection (`pick_two_letter_prefix_count`'s `ceil((count - alphabet_len) / (alphabet_len - 1))`), and the disjoint single/two-letter buckets all line up. However, there is no compile-time link between them. Consider one of: (a) a small fixture file (e.g. `sneak-fixture.json`) emitted from a Rust test that the TS test reads and asserts against, or (b) a property-style test that calls the TS port across `[1, 5, 23, 24, 50, 200, 529]` and asserts shape invariants (length, distinctness, prefix-free) so a future drift in either implementation surfaces as a test failure rather than a silent behavioural divergence. Acceptable as-is for landing this task; worth tracking as a follow-up.\n+- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:198-238` — The TS port of `generate_sneak_codes` is a faithful re-implementation of `swissarmyhammer-focus/src/sneak.rs`, but the two now drift independently. The current port matches: alphabet ordering, prefix selection (`pick_two_letter_prefix_count`'s `ceil((count - alphabet_len) / (alphabet_len - 1))`), and the disjoint single/two-letter buckets all line up. However, there is no compile-time link between them. Consider one of: (a) a small fixture file (e.g. `sneak-fixture.json`) emitted from a Rust test that the TS test reads and asserts against, or (b) a property-style test that calls the TS port across `[1, 5, 23, 24, 50, 200, 529]` and asserts shape invariants (length, distinctness, prefix-free) so a future drift in either implementation surfaces as a test failure rather than a silent behavioural divergence. Acceptable as-is for landing this task; worth tracking as a follow-up.\n+  - Resolved: comment block above `SNEAK_ALPHABET` strengthened with explicit `SOURCE OF TRUTH` / `DRIFT RISK` / `FOLLOW-UP` sections naming `swissarmyhammer-focus/src/sneak.rs` and the three load-bearing items (`SNEAK_ALPHABET`, `generate_sneak_codes`, `pick_two_letter_prefix_count`). Follow-up tracked as kanban task `01KR4F8J3Q4J39BB63AJ9G2W6P` (\"Replace TS sneak-code port with fixture-file or property-style cross-language check\").\n\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n+- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n+  - Resolved: no-op assertion and the now-unused `E2E_BOARD_PATH` import removed from this file. The sibling `spatial-nav-end-to-end.spatial.test.tsx` was left untouched (out of scope for this task).\n","reverse_patch":"--- original\n+++ modified\n@@ -87,11 +87,8 @@\n\n ### Nits\n\n-- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:518-522` — Stale comment block directly contradicts the code below it. The comment says \"we set both `metaKey` and `ctrlKey` so the binding fires regardless of which platform branch `normalizeKeyEvent` takes\", but line 526 sets only `metaKey: true`. The replacement comment on lines 523-525 is correct (\"Setting only `metaKey` produces `Mod+g`\"). Remove lines 518-522 (the stale block) and keep lines 523-525. As verified against `keybindings.ts:206` (`mod = mac ? e.metaKey : e.ctrlKey`) and `:213` (`mac && e.ctrlKey` adds a separate `Ctrl` modifier), setting both on macOS would produce `Mod+Ctrl+g` and miss the `Mod+g` binding — so the stale comment is not just outdated, it would lead a future maintainer to introduce a real bug.\n-  - Resolved: stale block removed; replacement comment now explicitly cites `keybindings.ts` (`mod = mac ? e.metaKey : e.ctrlKey`) and warns that adding `ctrlKey: true` would produce `Mod+Ctrl+g` on macOS and miss the binding.\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:518-522` — Stale comment block directly contradicts the code below it. The comment says \"we set both `metaKey` and `ctrlKey` so the binding fires regardless of which platform branch `normalizeKeyEvent` takes\", but line 526 sets only `metaKey: true`. The replacement comment on lines 523-525 is correct (\"Setting only `metaKey` produces `Mod+g`\"). Remove lines 518-522 (the stale block) and keep lines 523-525. As verified against `keybindings.ts:206` (`mod = mac ? e.metaKey : e.ctrlKey`) and `:213` (`mac && e.ctrlKey` adds a separate `Ctrl` modifier), setting both on macOS would produce `Mod+Ctrl+g` and miss the `Mod+g` binding — so the stale comment is not just outdated, it would lead a future maintainer to introduce a real bug.\n\n-- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:198-238` — The TS port of `generate_sneak_codes` is a faithful re-implementation of `swissarmyhammer-focus/src/sneak.rs`, but the two now drift independently. The current port matches: alphabet ordering, prefix selection (`pick_two_letter_prefix_count`'s `ceil((count - alphabet_len) / (alphabet_len - 1))`), and the disjoint single/two-letter buckets all line up. However, there is no compile-time link between them. Consider one of: (a) a small fixture file (e.g. `sneak-fixture.json`) emitted from a Rust test that the TS test reads and asserts against, or (b) a property-style test that calls the TS port across `[1, 5, 23, 24, 50, 200, 529]` and asserts shape invariants (length, distinctness, prefix-free) so a future drift in either implementation surfaces as a test failure rather than a silent behavioural divergence. Acceptable as-is for landing this task; worth tracking as a follow-up.\n-  - Resolved: comment block above `SNEAK_ALPHABET` strengthened with explicit `SOURCE OF TRUTH` / `DRIFT RISK` / `FOLLOW-UP` sections naming `swissarmyhammer-focus/src/sneak.rs` and the three load-bearing items (`SNEAK_ALPHABET`, `generate_sneak_codes`, `pick_two_letter_prefix_count`). Follow-up tracked as kanban task `01KR4F8J3Q4J39BB63AJ9G2W6P` (\"Replace TS sneak-code port with fixture-file or property-style cross-language check\").\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:198-238` — The TS port of `generate_sneak_codes` is a faithful re-implementation of `swissarmyhammer-focus/src/sneak.rs`, but the two now drift independently. The current port matches: alphabet ordering, prefix selection (`pick_two_letter_prefix_count`'s `ceil((count - alphabet_len) / (alphabet_len - 1))`), and the disjoint single/two-letter buckets all line up. However, there is no compile-time link between them. Consider one of: (a) a small fixture file (e.g. `sneak-fixture.json`) emitted from a Rust test that the TS test reads and asserts against, or (b) a property-style test that calls the TS port across `[1, 5, 23, 24, 50, 200, 529]` and asserts shape invariants (length, distinctness, prefix-free) so a future drift in either implementation surfaces as a test failure rather than a silent behavioural divergence. Acceptable as-is for landing this task; worth tracking as a follow-up.\n\n-- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n-  - Resolved: no-op assertion and the now-unused `E2E_BOARD_PATH` import removed from this file. The sibling `spatial-nav-end-to-end.spatial.test.tsx` was left untouched (out of scope for this task).\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n","transaction_id":null}
{"id":"01KR4FAGBJ0HA7VV0N1YAQG8CM","timestamp":"2026-05-08T18:59:11.346702Z","entity_type":"task","entity_id":"01KQYWWQ0C4E9AFM942BMJHMFX","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -77,8 +77,11 @@\n\n ### Nits\n\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:518-522` — Stale comment block directly contradicts the code below it. The comment says \"we set both `metaKey` and `ctrlKey` so the binding fires regardless of which platform branch `normalizeKeyEvent` takes\", but line 526 sets only `metaKey: true`. The replacement comment on lines 523-525 is correct (\"Setting only `metaKey` produces `Mod+g`\"). Remove lines 518-522 (the stale block) and keep lines 523-525. As verified against `keybindings.ts:206` (`mod = mac ? e.metaKey : e.ctrlKey`) and `:213` (`mac && e.ctrlKey` adds a separate `Ctrl` modifier), setting both on macOS would produce `Mod+Ctrl+g` and miss the `Mod+g` binding — so the stale comment is not just outdated, it would lead a future maintainer to introduce a real bug.\n+- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:518-522` — Stale comment block directly contradicts the code below it. The comment says \"we set both `metaKey` and `ctrlKey` so the binding fires regardless of which platform branch `normalizeKeyEvent` takes\", but line 526 sets only `metaKey: true`. The replacement comment on lines 523-525 is correct (\"Setting only `metaKey` produces `Mod+g`\"). Remove lines 518-522 (the stale block) and keep lines 523-525. As verified against `keybindings.ts:206` (`mod = mac ? e.metaKey : e.ctrlKey`) and `:213` (`mac && e.ctrlKey` adds a separate `Ctrl` modifier), setting both on macOS would produce `Mod+Ctrl+g` and miss the `Mod+g` binding — so the stale comment is not just outdated, it would lead a future maintainer to introduce a real bug.\n+  - Resolved: stale block removed; replacement comment now explicitly cites `keybindings.ts` (`mod = mac ? e.metaKey : e.ctrlKey`) and warns that adding `ctrlKey: true` would produce `Mod+Ctrl+g` on macOS and miss the binding.\n\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:198-238` — The TS port of `generate_sneak_codes` is a faithful re-implementation of `swissarmyhammer-focus/src/sneak.rs`, but the two now drift independently. The current port matches: alphabet ordering, prefix selection (`pick_two_letter_prefix_count`'s `ceil((count - alphabet_len) / (alphabet_len - 1))`), and the disjoint single/two-letter buckets all line up. However, there is no compile-time link between them. Consider one of: (a) a small fixture file (e.g. `sneak-fixture.json`) emitted from a Rust test that the TS test reads and asserts against, or (b) a property-style test that calls the TS port across `[1, 5, 23, 24, 50, 200, 529]` and asserts shape invariants (length, distinctness, prefix-free) so a future drift in either implementation surfaces as a test failure rather than a silent behavioural divergence. Acceptable as-is for landing this task; worth tracking as a follow-up.\n+- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:198-238` — The TS port of `generate_sneak_codes` is a faithful re-implementation of `swissarmyhammer-focus/src/sneak.rs`, but the two now drift independently. The current port matches: alphabet ordering, prefix selection (`pick_two_letter_prefix_count`'s `ceil((count - alphabet_len) / (alphabet_len - 1))`), and the disjoint single/two-letter buckets all line up. However, there is no compile-time link between them. Consider one of: (a) a small fixture file (e.g. `sneak-fixture.json`) emitted from a Rust test that the TS test reads and asserts against, or (b) a property-style test that calls the TS port across `[1, 5, 23, 24, 50, 200, 529]` and asserts shape invariants (length, distinctness, prefix-free) so a future drift in either implementation surfaces as a test failure rather than a silent behavioural divergence. Acceptable as-is for landing this task; worth tracking as a follow-up.\n+  - Resolved: comment block above `SNEAK_ALPHABET` strengthened with explicit `SOURCE OF TRUTH` / `DRIFT RISK` / `FOLLOW-UP` sections naming `swissarmyhammer-focus/src/sneak.rs` and the three load-bearing items (`SNEAK_ALPHABET`, `generate_sneak_codes`, `pick_two_letter_prefix_count`). Follow-up tracked as kanban task `01KR4F8J3Q4J39BB63AJ9G2W6P` (\"Replace TS sneak-code port with fixture-file or property-style cross-language check\").\n\n-- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n+- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n+  - Resolved: no-op assertion and the now-unused `E2E_BOARD_PATH` import removed from this file. The sibling `spatial-nav-end-to-end.spatial.test.tsx` was left untouched (out of scope for this task).\n","reverse_patch":"--- original\n+++ modified\n@@ -77,11 +77,8 @@\n\n ### Nits\n\n-- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:518-522` — Stale comment block directly contradicts the code below it. The comment says \"we set both `metaKey` and `ctrlKey` so the binding fires regardless of which platform branch `normalizeKeyEvent` takes\", but line 526 sets only `metaKey: true`. The replacement comment on lines 523-525 is correct (\"Setting only `metaKey` produces `Mod+g`\"). Remove lines 518-522 (the stale block) and keep lines 523-525. As verified against `keybindings.ts:206` (`mod = mac ? e.metaKey : e.ctrlKey`) and `:213` (`mac && e.ctrlKey` adds a separate `Ctrl` modifier), setting both on macOS would produce `Mod+Ctrl+g` and miss the `Mod+g` binding — so the stale comment is not just outdated, it would lead a future maintainer to introduce a real bug.\n-  - Resolved: stale block removed; replacement comment now explicitly cites `keybindings.ts` (`mod = mac ? e.metaKey : e.ctrlKey`) and warns that adding `ctrlKey: true` would produce `Mod+Ctrl+g` on macOS and miss the binding.\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:518-522` — Stale comment block directly contradicts the code below it. The comment says \"we set both `metaKey` and `ctrlKey` so the binding fires regardless of which platform branch `normalizeKeyEvent` takes\", but line 526 sets only `metaKey: true`. The replacement comment on lines 523-525 is correct (\"Setting only `metaKey` produces `Mod+g`\"). Remove lines 518-522 (the stale block) and keep lines 523-525. As verified against `keybindings.ts:206` (`mod = mac ? e.metaKey : e.ctrlKey`) and `:213` (`mac && e.ctrlKey` adds a separate `Ctrl` modifier), setting both on macOS would produce `Mod+Ctrl+g` and miss the `Mod+g` binding — so the stale comment is not just outdated, it would lead a future maintainer to introduce a real bug.\n\n-- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:198-238` — The TS port of `generate_sneak_codes` is a faithful re-implementation of `swissarmyhammer-focus/src/sneak.rs`, but the two now drift independently. The current port matches: alphabet ordering, prefix selection (`pick_two_letter_prefix_count`'s `ceil((count - alphabet_len) / (alphabet_len - 1))`), and the disjoint single/two-letter buckets all line up. However, there is no compile-time link between them. Consider one of: (a) a small fixture file (e.g. `sneak-fixture.json`) emitted from a Rust test that the TS test reads and asserts against, or (b) a property-style test that calls the TS port across `[1, 5, 23, 24, 50, 200, 529]` and asserts shape invariants (length, distinctness, prefix-free) so a future drift in either implementation surfaces as a test failure rather than a silent behavioural divergence. Acceptable as-is for landing this task; worth tracking as a follow-up.\n-  - Resolved: comment block above `SNEAK_ALPHABET` strengthened with explicit `SOURCE OF TRUTH` / `DRIFT RISK` / `FOLLOW-UP` sections naming `swissarmyhammer-focus/src/sneak.rs` and the three load-bearing items (`SNEAK_ALPHABET`, `generate_sneak_codes`, `pick_two_letter_prefix_count`). Follow-up tracked as kanban task `01KR4F8J3Q4J39BB63AJ9G2W6P` (\"Replace TS sneak-code port with fixture-file or property-style cross-language check\").\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:198-238` — The TS port of `generate_sneak_codes` is a faithful re-implementation of `swissarmyhammer-focus/src/sneak.rs`, but the two now drift independently. The current port matches: alphabet ordering, prefix selection (`pick_two_letter_prefix_count`'s `ceil((count - alphabet_len) / (alphabet_len - 1))`), and the disjoint single/two-letter buckets all line up. However, there is no compile-time link between them. Consider one of: (a) a small fixture file (e.g. `sneak-fixture.json`) emitted from a Rust test that the TS test reads and asserts against, or (b) a property-style test that calls the TS port across `[1, 5, 23, 24, 50, 200, 529]` and asserts shape invariants (length, distinctness, prefix-free) so a future drift in either implementation surfaces as a test failure rather than a silent behavioural divergence. Acceptable as-is for landing this task; worth tracking as a follow-up.\n\n-- [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n-  - Resolved: no-op assertion and the now-unused `E2E_BOARD_PATH` import removed from this file. The sibling `spatial-nav-end-to-end.spatial.test.tsx` was left untouched (out of scope for this task).\n+- [ ] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n"}]]}
{"id":"01KR4FJQ4AY3NAK85MMEV4CNBP","timestamp":"2026-05-08T19:03:40.426474Z","op":"update","item_id":"01KQYWWQ0C4E9AFM942BMJHMFX","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQYWV9DC866DGRPBRFR17ZEY\n-position_column: review\n-position_ordinal: '8480'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffbb80\n project: spatial-nav\n title: End-to-end spatial test for Jump-To overlay\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQYWV9DC866DGRPBRFR17ZEY\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffbb80\n+position_column: review\n+position_ordinal: '8480'\n project: spatial-nav\n title: End-to-end spatial test for Jump-To overlay\n ---\n","transaction_id":null}
{"id":"01KR4FJQ4BN1W163PYH26H2F9G","timestamp":"2026-05-08T19:03:40.427398Z","entity_type":"task","entity_id":"01KQYWWQ0C4E9AFM942BMJHMFX","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8480\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffffbb80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffffbb80\n\\ No newline at end of file\n+8480\n\\ No newline at end of file\n"}]]}
{"id":"01KRE1RTJ66ZMH10YGV5PE2P6S","timestamp":"2026-05-12T12:14:44.806497Z","op":"update","item_id":"01KQYWWQ0C4E9AFM942BMJHMFX","forward_patch":"--- original\n+++ modified\n@@ -94,4 +94,4 @@\n   - Resolved: comment block above `SNEAK_ALPHABET` strengthened with explicit `SOURCE OF TRUTH` / `DRIFT RISK` / `FOLLOW-UP` sections naming `swissarmyhammer-focus/src/sneak.rs` and the three load-bearing items (`SNEAK_ALPHABET`, `generate_sneak_codes`, `pick_two_letter_prefix_count`). Follow-up tracked as kanban task `01KR4F8J3Q4J39BB63AJ9G2W6P` (\"Replace TS sneak-code port with fixture-file or property-style cross-language check\").\n\n - [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n-  - Resolved: no-op assertion and the now-unused `E2E_BOARD_PATH` import removed from this file. The sibling `spatial-nav-end-to-end.spatial.test.tsx` was left untouched (out of scope for this task).\n+  - Resolved: no-op assertion and the now-unused `E2E_BOARD_PATH` import removed from this file. The sibling `spatial-nav-end-to-end.spatial.test.tsx` was left untouched (out of scope for this task).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -94,4 +94,4 @@\n   - Resolved: comment block above `SNEAK_ALPHABET` strengthened with explicit `SOURCE OF TRUTH` / `DRIFT RISK` / `FOLLOW-UP` sections naming `swissarmyhammer-focus/src/sneak.rs` and the three load-bearing items (`SNEAK_ALPHABET`, `generate_sneak_codes`, `pick_two_letter_prefix_count`). Follow-up tracked as kanban task `01KR4F8J3Q4J39BB63AJ9G2W6P` (\"Replace TS sneak-code port with fixture-file or property-style cross-language check\").\n\n - [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n-  - Resolved: no-op assertion and the now-unused `E2E_BOARD_PATH` import removed from this file. The sibling `spatial-nav-end-to-end.spatial.test.tsx` was left untouched (out of scope for this task).\n\\ No newline at end of file\n+  - Resolved: no-op assertion and the now-unused `E2E_BOARD_PATH` import removed from this file. The sibling `spatial-nav-end-to-end.spatial.test.tsx` was left untouched (out of scope for this task).\n","transaction_id":null}
{"id":"01KRE1RTJ7V2J7XV5023TQP3RC","timestamp":"2026-05-12T12:14:44.807512Z","entity_type":"task","entity_id":"01KQYWWQ0C4E9AFM942BMJHMFX","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -84,4 +84,4 @@\n   - Resolved: comment block above `SNEAK_ALPHABET` strengthened with explicit `SOURCE OF TRUTH` / `DRIFT RISK` / `FOLLOW-UP` sections naming `swissarmyhammer-focus/src/sneak.rs` and the three load-bearing items (`SNEAK_ALPHABET`, `generate_sneak_codes`, `pick_two_letter_prefix_count`). Follow-up tracked as kanban task `01KR4F8J3Q4J39BB63AJ9G2W6P` (\"Replace TS sneak-code port with fixture-file or property-style cross-language check\").\n\n - [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n-  - Resolved: no-op assertion and the now-unused `E2E_BOARD_PATH` import removed from this file. The sibling `spatial-nav-end-to-end.spatial.test.tsx` was left untouched (out of scope for this task).\n+  - Resolved: no-op assertion and the now-unused `E2E_BOARD_PATH` import removed from this file. The sibling `spatial-nav-end-to-end.spatial.test.tsx` was left untouched (out of scope for this task).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -84,4 +84,4 @@\n   - Resolved: comment block above `SNEAK_ALPHABET` strengthened with explicit `SOURCE OF TRUTH` / `DRIFT RISK` / `FOLLOW-UP` sections naming `swissarmyhammer-focus/src/sneak.rs` and the three load-bearing items (`SNEAK_ALPHABET`, `generate_sneak_codes`, `pick_two_letter_prefix_count`). Follow-up tracked as kanban task `01KR4F8J3Q4J39BB63AJ9G2W6P` (\"Replace TS sneak-code port with fixture-file or property-style cross-language check\").\n\n - [x] `kanban-app/ui/src/spatial-nav-jump-to.spatial.test.tsx:893-895` — `expect(E2E_BOARD_PATH).toBeTruthy()` is a no-op assertion to keep the import alive. Mirrors the same pattern in `spatial-nav-end-to-end.spatial.test.tsx:1563-1565`, so it is local convention rather than a defect — but the cleaner fix is to drop the unused import. Either leave for consistency with the sibling file or remove from both in a small follow-up.\n-  - Resolved: no-op assertion and the now-unused `E2E_BOARD_PATH` import removed from this file. The sibling `spatial-nav-end-to-end.spatial.test.tsx` was left untouched (out of scope for this task).\n\\ No newline at end of file\n+  - Resolved: no-op assertion and the now-unused `E2E_BOARD_PATH` import removed from this file. The sibling `spatial-nav-end-to-end.spatial.test.tsx` was left untouched (out of scope for this task).\n"}]]}

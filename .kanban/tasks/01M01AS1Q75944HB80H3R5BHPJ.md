---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m02rs9qn8rpa490tpzyr997g
  text: |-
    Picked up. Read the card, `builtin/validators/code-hygiene/rules/dead-code-typescript.md`, `builtin/validators/code-hygiene/VALIDATOR.md`, `builtin/validators/README.md`, and the five acceptance tests in `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/dead_code_typescript.rs`.

    Toolchain on this machine: node v25.2.1, npm 11.6.2, ts-prune and tsc on PATH. `npm view knip version` answers 6.32.2, and `latest` is the same, so 6.32.2 is the version to pin.

    Three measurements started in parallel, each in the session scratchpad and never in the repository:
    1. The exit status and the configuration surface — every broken shape against a clean run and a run with findings, `--no-exit-code` over each, `-c <file>`, `--reporter json --no-config-hints`, and whether an installed `node_modules` changes the answer.
    2. The staging contract — line-comment suppression, the JSDoc tags, an arbitrary tag under `--tags=-<name>`, whether the tag carries a reason, and whether it expires.
    3. The corpus — `zod` at `4e1720c`, `zustand` at `2115efb` and `redux` at `3084fc3`, knip beside the shipped ts-prune script, so the tables of the rule body stay comparable.
  timestamp: 2026-08-15T13:11:38.997190+00:00
- actor: claude-code
  id: 01m02rys5aympy138btg0660ff
  text: |-
    Research done on the harness, before the measurements land. What a swap would have to meet:

    - `dead-code-typescript` states `scope: workspace`. Two coverage guards read every shipped script: `scope_roster` holds a `workspace` script to writing NO `"$@"` and NO zero-argument guard, and `temp_directory` holds each `mktemp -d` to standing in `work="$(mktemp -d)"` with `trap 'rm -rf "$work"' EXIT` on the line directly under it, and to naming `mktemp` in `doctor.check_command`. `SHIPPED_TOOL_RULE_COUNT` is 25, `WORKSPACE_SCOPE_RULE_COUNT` is 11, `TEMP_DIRECTORY_RULE_COUNT` is 21 — a swap that keeps one rule moves none of the three.
    - The acceptance-test shapes already exist for both halves of the exit-status question. `verify_shipped_tree_reports` holds a run to exactly the `path:line` rows a probe names and to exit 0. `verify_shipped_tree_breaks` holds a run to NO finding and to one error naming each fragment the probe states. `unused-dependencies-rust` is the worked example of the pair, and `missing-docs-python` is the worked example of the script shape: run the tool into a file, read the status, write the report and the tool's stderr to stderr, and exit 1 for the statuses that mean a broken run.
    - The fixture directory holds `tsconfig.json.tmpl` and NO `package.json.tmpl`. knip reads a package, so a swap needs one, and the pass fixture's five `// ts-prune-ignore-next` markers would each have to become whatever knip reads.

    The three measurements are still running.
  timestamp: 2026-08-15T13:14:38.634269+00:00
- actor: claude-code
  id: 01m02s4rgr7t353f6tkwpmh3yp
  text: |-
    PROPERTY 1, THE STAGING CONTRACT — measured with knip 6.32.2 on node v25.2.1. The card's premise is half wrong, and the correction favours the swap.

    What holds: knip has NO line-comment suppression. `// knip-ignore`, `// knip-ignore-next`, `// knip-ignore-next-line`, `// eslint-disable-next-line`, `// ts-prune-ignore-next` and `// @ts-expect-error` each leave the finding standing. A grep of the installed package for every line-comment directive spelling answers zero matches; the only `//knip` text in it is a `https://knip.dev` URL.

    What is wrong: the tag DOES carry the reason, and it DOES expire.

    | the marker | the flag | silenced |
    |---|---|---|
    | `/** @staged the importer lands in the next change */` | `--tags=-staged` | yes |
    | `/** @staged */`, bare | `--tags=-staged` | yes |
    | `// @staged the importer lands next` | `--tags=-staged` | NO |
    | `/** @public */`, `/** @beta */`, `/** @alias */` | none | yes |
    | `/** @internal */` | `--tags=-internal` or `--production` | yes |

    - The free text after the tag is accepted and never read, so the reason can name the change that lands the consumer.
    - The tag works on `const`, `function`, `class`, `interface`, `type`, `enum` and `export default` — 7 findings at exit 1 become `{"issues":[]}` at exit 0.
    - The name must be ONE alpha word. `dist/util/tag.js` matches `/[a-zA-Z]+/`, so `--tags=-sah` silences `@sah-staged` too.
    - The marker must stand in a BLOCK comment. `// @staged` is read as nothing, which is the shape an author would think exempts them.
    - `--tags=+staged` inverts rather than exempts: it reports the tagged exports alone.

    THE MARKER EXPIRES. Measured: while the export is dead no hint is written; the moment a real importer lands, knip writes `Unused tag in src/lib.ts: dStaged → @staged`. `dist/graph/analyze.js` raises it on `isIgnored && isReferenced`. That is `unfulfilled_lint_expectations` for TypeScript, and `// ts-prune-ignore-next` has no equivalent — it never expires. So knip is STRONGER than the shipped tool on the property this card was afraid of.

    Two limits on the expiry, both load-bearing:
    1. Only the `--tags=-<name>` exclude form expires. `@public`, `@beta` and `@alias` never do — `isAlwaysIgnored` returns before the hint code runs. That is a second reason not to reuse `@public`, beside its TSDoc meaning of "committed public API", which states the opposite of "a consumer lands next".
    2. `--reporter json` never writes the hint. `--reporter json --treat-tag-hints-as-errors` answers exit 1 with `{"issues":[]}` and 0 bytes on both streams. Only the `symbols` and `github-actions` reporters print it.

    `--treat-tag-hints-as-errors` is OFF by default, so `--reporter json` answers exit 0 for a tree whose only news is an expired tag. The status stays readable.
  timestamp: 2026-08-15T13:17:54.584674+00:00
- actor: claude-code
  id: 01m02samm74nn7gdbthj22jwhv
  text: |-
    THE CORPUS RE-MEASUREMENT REFUTES THE HEADLINE OF THIS CARD. The table this card and `VALIDATOR.md` both quote — zod 78 vs 13, zustand 1 vs 0, redux 6 vs 2 — was measured with NO `node_modules`, and knip was BROKEN in that state.

    Corpus reproduced exactly: zod `4e1720c` 424 files / 9 projects, zustand `2115efb` 34 / 2, redux `3084fc3` 53 / 3.

    | repository | ts-prune, no node_modules | ts-prune, installed | knip, no node_modules | knip, installed |
    |---|---|---|---|---|
    | zod | 78 | 76 | 13 | 11 |
    | zustand | 1 | 1 | 0 | 13 |
    | redux | 6 | 6 | 2 | 7 |

    Both columns of the shipped table reproduce EXACTLY on clean clones, so the earlier measurement was taken with no dependencies installed. With them installed, zustand and redux INVERT: knip reports MORE than ts-prune, not fewer.

    - Without `node_modules` knip writes `ERROR: Error loading vitest.config.ts (Cannot find module 'vitest/config')` to stderr and EXITS 1 anyway. zustand's 0 was a failed config load, not a clean tree. The status alone never says the run was unsound. THAT IS A SILENT-ZERO TRAP OF ITS OWN.
    - `nsExports` and `nsTypes` SELECT NOTHING in knip 6.32.2. They are accepted names that resolve to an empty filter; the live key is `namespaceMembers`. So the include list this card quotes is stale, and it is why the earlier zod number looked small.
    - Corrected selection `exports,types,namespaceMembers,enumMembers`, installed: zod 17, zustand 13, redux 7.
    - `files` is the type the rule must drop above all — 98 / 10 / 89 over the corpus, more than half of every finding, and it names a FILE rather than a symbol.
    - ts-prune's absolute-path defect (^yxky1aj) reproduces at 1 line of 76 on zod, in `packages/bench`, and the leaked path embeds the checkout location.
    - knip is 20x to 30x faster: 0.39 s against 11.5 s on zod installed. Installing dependencies costs ts-prune 47 % more wall time and costs knip nothing.

    The hand-check of knip's 17 zod findings is what decides quality, and it is not favourable as measured: 1 is unambiguously dead (`Tab`, a nested-barrel re-export nothing takes), 9 are exports the file itself consumes — which is the `(used in module)` shape the shipped rule DELIBERATELY drops as a surplus keyword rather than dead code — and 7 are `StandardSchemaV1` namespace members, published library surface knip did not connect to an entry.

    knip has `ignoreExportsUsedInFile`, which is the exact answer to those 9. The comparison is not apples to apples until that is measured, so that measurement is running now.
  timestamp: 2026-08-15T13:21:07.207727+00:00
- actor: claude-code
  id: 01m02se3nj8xzv19f20t5sr5mn
  text: |-
    PROPERTY 2, THE EXIT STATUS — measured with knip 6.32.2. It settles WELL, and the split is usable as a gate.

    | the run | status | stdout | `jq` reads it | stderr |
    |---|---|---|---|---|
    | a clean tree | 0 | `{"issues":[]}` | yes | 0 bytes |
    | a tree with findings | 1 | one object for each file | yes | 0 bytes |
    | no `package.json` | 2 | human prose | NO | `ERROR: Unable to find package.json` |
    | a `package.json` that is not JSON | 2 | human prose | NO | `ERROR: Error parsing ...` |
    | `-c` names a file that is not there | 2 | human prose | NO | `ERROR: Error loading ...` |
    | a `-c` file that is not JSON | 2 | human prose | NO | `ERROR: Error parsing ...` |
    | `--include` names a type knip does not know | 2 | human prose | NO | `ERROR: Invalid issue type: ...` |
    | a `tsconfig.json` that is not JSON | 1 | findings | yes | 0 bytes |
    | a `-c` `entry` naming a file that is not there | 1 | findings | yes | 0 bytes |
    | a source importing a package that is not installed | 1 | findings | yes | 0 bytes |

    - Status 2 is the one reliable "knip refused to run" signal, and `--no-exit-code` CANNOT mask it. `--no-exit-code` forces 0 for findings and for the three silent shapes, and leaves every status 2 standing.
    - Every status 2 writes human prose to stdout, never JSON, so `jq` parseability is a second independent test of the same fact.
    - `--max-issues` defaults to 0 and `--max-issues 5` takes a 1-finding run to exit 0, so the run must never state it.

    THE ONE TRAP, AND IT IS SERIOUS. knip needs an installed `node_modules` for the answer to be right, and it gives NO STATUS SIGNAL when there is none. Measured over `redux` at `3084fc3`: 2 findings with no `node_modules` against 7 with them, both at exit 1. It misses 5 of 7 real findings and the only sign is two lines on stderr — `ERROR: Error loading vitest.config.mts (Cannot find module 'vitest/config')` and `ERROR: Please fix or visit ...`. Under `--no-exit-code` that run answers exit 0 with a quiet wrong answer. SO THE SCRIPT MUST READ STDERR EVEN AT EXIT 0 AND EXIT 1. That is the same shape the three swiftlint rules already carry, inverted: they read stderr to answer clean, this one reads stderr to break.

    A `tsconfig.json` that is not JSON writes NOTHING on any stream and still reports. ts-prune at least crashes on that shape (^gxncs25). knip is silent about it, and no test of status, stdout or stderr finds it.

    THE GATE. `-c <file>` accepts a path outside the project and REPLACES `knip.json`, `.knip.json`, `knip.jsonc` and `knip.config.ts`. It does NOT displace `package.json#knip`: measured, a project stating `"knip": {"ignore": ["src/**"]}` in its manifest kept the gate off under a `-c` that omitted `ignore`, and writing `"ignore": []` explicitly in the `-c` file defeated it. `builtin/validators/README.md` already states that requirement word for word — a script that reads the project's configuration must state EVERY option of EVERY rule it measures with.

    The report shape is `{"issues":[{"file":..., "<type>":[{"name","line","col","pos"}]}]}` — one object for each FILE, `issues` the only top-level key, and `--include` prunes which type keys each object carries. One run at a workspace root covers every nested package and writes paths relative to the run directory, so the per-project prefix arithmetic that produced ^yxky1aj has no counterpart at all.
  timestamp: 2026-08-15T13:23:00.914943+00:00
- actor: claude-code
  id: 01m02sfc1r8b8m7kyhqqwn3psj
  text: |-
    THE EXPIRY MECHANISM, MEASURED TO THE BOTTOM. The marker expires, and here is exactly what a run can do with that.

    - A tag hint carries NO POSITION. `dist/types/issues.d.ts` declares `TagHint` as `{ type, filePath, identifier, tagName }` and nothing more.
    - Of ten built-in reporters, only `symbols` (to stderr) and `github-actions` (to stdout) write the hint at all. `json`, `sarif` and `codeclimate` — every machine format — drop it in silence. `json` answers `{"issues":[]}` for a tree whose only news is a redundant tag.
    - `github-actions` HARDCODES line 1 and column 1 for a tag hint. A real finding in the same run took its true position, so the hardcoding belongs to the hint alone. A rule built on that reporter would point every expiry at the top of the file.
    - A CUSTOM reporter is the only surface that takes the hints as DATA. `--reporter ./reporter.mjs` is accepted — `dist/util/reporter.js` falls through to `_load` for any name that is not built in, and it resolves a relative path. The options object carries `tagHints` as a Set of that struct, with an ABSOLUTE `filePath` rather than the relative one the printed text uses. It runs in node with filesystem access, so it can open the file and anchor on `identifier` and `tagName` to derive the true line.
    - A custom reporter CANNOT set the exit status. Reporters return void and `cli.js` computes the status on its own, so `--treat-tag-hints-as-errors` stays the only lever that makes an expired tag fail.

    Exit status, refined: `dist/cli.js` collapses real findings, configuration hints and tag hints into the ONE code 1. A probe holding both a redundant `@staged` tag and one genuinely dead export answered exit 1 with and without `--treat-tag-hints-as-errors`, stdout byte-identical both times. So the status cannot say WHICH fired — but 2-against-not-2 still answers the README trap of telling a broken run from a run with findings, which is the question this card asks.

    A TTY TRAP worth stating in the rule body if the expiry ships: on a terminal, without `--treat-tag-hints-as-errors`, knip prints the hint and then `Excellent, Knip found no issues.` in the SAME run — `dist/reporters/symbols.js` gates that sentence on `isShowProgress`. It goes away off a terminal. Off a terminal the stderr shape is exactly two plain lines, 60 bytes, no ANSI, path relative to the working directory.
  timestamp: 2026-08-15T13:23:42.264963+00:00
- actor: claude-code
  id: 01m02ssqc7samg127dff0bhnjr
  text: |-
    THE FIXTURE PAIR IS VIABLE UNDER knip, and it costs ONE new file. Measured over the real 56-file flat fixtures directory, materialized with every `.tmpl` suffix dropped, with NO `node_modules` and no network.

    The run: `knip --reporter json --no-config-hints --include exports,types,namespaceMembers,enumMembers --tags=-staged` reports 5 findings on `dead-code-typescript.fail.ts` and 0 on `dead-code-typescript.pass.ts`, and names NO other file of the 56.

    All five kinds report, so the pair keeps the hole-free coverage the contract asks for:

    | the fixture export | the kind | knip issue type |
    |---|---|---|
    | `DeadOptions` | type alias | `types` |
    | `DeadReading` | interface | `types` |
    | `DEAD_LIMIT` | const | `exports` |
    | `DeadWidget` | class | `exports` |
    | `deadHelper` | function | `exports` |

    `types` is a SEPARATE issue type from `exports`. An include list without it loses the type alias and the interface — 2 of the 5 kinds — in silence.

    THE ENTRY PROBLEM, AND THE ONE SHAPE THAT ANSWERS IT. A flat directory holds no importer, so every shape but one reports nothing:

    | the shape | findings |
    |---|---|
    | no `package.json` | exit 2, `ERROR: Unable to find package.json` |
    | `package.json` naming no entry | 0 — both fixtures are unused FILES, and an unused file's exports never report |
    | `main` names the fail fixture | 0 — the entry's own exports are the surface |
    | `-c` `entry` names both | 0 — the same, for both |
    | `entry` names both, plus `includeEntryExports: true` | 10, and 5 with `--tags=-staged` |

    `includeEntryExports` is load-bearing and CONFIG-ONLY — `knip --help` states no such flag. Without it the rule reports zero on both sides and passes nothing.

    The configuration goes in a `knip` key of the fixtures' own `package.json.tmpl`, which is one new file rather than two.

    IT DISTURBS NO OTHER RULE. The three eslint rules of this set were extracted and run over the same directory, before and after the new manifest:

    | the rule | without the manifest | with it |
    |---|---|---|
    | `magic-numbers-typescript` | 4 | 4 |
    | `function-length-typescript` | 3 | 3 |
    | `missing-docs-typescript` | 8 | 8 |

    `diff` is empty for all three, stderr empty for all six runs. Each writes its own `eslint.config.cjs` into a `mktemp` directory and passes `--no-config-lookup`, so the fixture manifest cannot reach it. The six other TypeScript fixtures appear under `files` alone and the include list drops that type, so they never reach the report at all.

    knip 6 needs NO installed `typescript`. It declares no peer dependency and parses with `oxc-parser`; proved by running it in an isolated tree where `require.resolve("typescript")` throws, for byte-identical output.

    The pass fixture folds the marker into each export's existing JSDoc block, which is one comment for each export and survives a formatter:

        /**
         * A type alias the importer lands with.
         *
         * @staged the importer lands in the next change
         */
        export type StagedOptions = { size: number };
  timestamp: 2026-08-15T13:29:21.543780+00:00
- actor: claude-code
  id: 01m02sxgbj7tqnd3dxsf3k063r
  text: |-
    A STRUCTURAL DIFFERENCE BETWEEN THE TWO TOOLS, measured by hand rather than delegated, because it decides what the rule can claim.

    knip resolves REACHABILITY FIRST. A module no entry reaches is reported as ONE `files` finding, and its exports are NOT enumerated. Measured on a probe holding `src/index.ts` as the manifest `main` beside an unreferenced `src/lib.ts` that exports one dead name:

    | the probe | the include list | what knip answered |
    |---|---|---|
    | `src/lib.ts` unreachable | `exports,types,namespaceMembers,enumMembers` | exit 0, `{"issues":[]}` — NOTHING |
    | the same | `files,exports,types,...` | exit 1, one `files` entry, `"exports":[]` |
    | `src/lib.ts` imported by the entry, one dead export in it | `exports,types,...` | exit 1, `{"name":"trulyDead","line":5,"col":14}` |

    So the include list this card names answers NOTHING for a whole dead module, which is the largest kind of dead code there is. And a `files` entry carries `{"name": ...}` and NO LINE.

    The size of that gap on the corpus, with `node_modules` installed:

    | repository | `files` findings | of them `.ts`/`.tsx` | `export` lines inside those files |
    |---|---|---|---|
    | zod | 98 | 95 | 95 |
    | zustand | 11 | 3 | 1 |
    | redux | 89 | 4 | 2 |

    95 exports on zod stand inside modules knip calls unused, and an `exports,types,...` run reports none of them. ts-prune reports each one.

    So the two tools do not make the same claim. ts-prune says "no module imports this export". knip says "no entry reaches this file" and then, inside the files an entry does reach, "nothing imports this export". Neither is the other.

    There are two shapes the rule could take and each has a cost:
    - DROP `files`: a wholly dead module reports nothing. That is a silent miss of 95 exports on zod.
    - TAKE `files`: the finding has no line, so the run must anchor it at line 1 — which has precedent in this set, since `missing-docs-python` reports `D100` and `D104` at row 1 for the same reason, a module having no definition line of its own. But the corpus then reports 98 and 89 findings whose mass is NOT TypeScript source: redux's 89 unused files hold only 4 `.ts` files, and the rest are `.js` and `.mjs` the rule's own file patterns still match.

    Also verified by hand, first-hand rather than delegated: exit 2 for a `package.json` that is not JSON, stdout the prose `Configuration file load error? Visit ...`, `jq` refusing it at status 5, and `--no-exit-code` leaving that 2 standing. The status gate holds.
  timestamp: 2026-08-15T13:31:25.426095+00:00
- actor: claude-code
  id: 01m02t0gra49ajabhjt5khdbjf
  text: |-
    THE CLAIM COMPARISON, which is what decides the card. Measured with `node_modules` installed.

    PATH OVERLAP between the shipped rule's findings and knip's: redux 1 file of 6-against-4; zustand ZERO of 1-against-8. The two tools are not converging on a better answer — they answer DIFFERENT QUESTIONS. Counts that look close after tuning hide that.

    ZUSTAND — the shipped rule BEATS knip, and for the exact reason this project built. The rule reports 1; knip reports 8 with `ignoreExportsUsedInFile: true`, and all 8 are the published surface of `zustand/middleware`. zustand's `package.json` names BUILD OUTPUT — `main: ./index.js`, `./*` onto `./*.js` — none of which exists in source. The ONLY place the source entry is stated is the tsconfig `paths` self-mapping, which `tsc --showConfig` resolves to `{"zustand": ["./src/index.ts"]}`. THE SHIPPED RULE READS THAT TABLE, which is what ^108bh4y added, AND KNIP DOES NOT TREAT IT AS AN ENTRY SOURCE. knip does resolve the module — 7 test files import `zustand/middleware` and knip reports none of `persist`, `devtools`, `subscribeWithSelector`, `createJSONStorage`, `redux` or `combine` — so the 8 are published names no test imports.

    REDUX — 3 of knip's 4 findings are FALSE POSITIVES caused by walking a tree the repository excludes. The root tsconfig `exclude` names `["dist","website","docs","examples"]`, the root manifest declares NO `workspaces`, and each of the 11 examples carries its own `package.json` and lockfile. knip walks in anyway: 82 of its 89 unused-file findings stand under `examples/`, and marking those files unused drops them out of its graph, which is the mechanism that makes the false positives. `reducers/index.js` default IS imported at `examples/shopping-cart/src/index.js`; `VisibilityFilters` IS imported in three files; `CHECKOUT_SUCCESS` IS used through a namespace import. Only `mangleErrors.mts` default is real — the one finding both tools share.

    ZOD — knip is clearly better, 7 against 76 under the lever. Of the 7, ONE is genuinely dead (`Tab` at `packages/docs/components/tabs.tsx`) and 6 are published library surface.

    THE LEVER, `ignoreExportsUsedInFile`: the schema takes a boolean or an object of seven keys. It costs nothing measurable. Two behaviours to write down — it does NOT touch `namespaceMembers` (zod holds 6 under `false`, `true` and the object form alike), and the object form is not a safer `true` but a weaker different filter, since `{interface,type}` leaves every over-exported variable and function standing.

    THE BROKEN TSCONFIG IS NOT FIXED BY A SWAP. Replacing zod's `packages/zod/tsconfig.json` with bytes that are not JSON: knip exits 1, the count holds at 17, and the JSON is BYTE-IDENTICAL, with 174 bytes on stderr the only signal. The shipped rule has the same shape at exit 0 with its count held at 76. SO ^gxncs25 IS NOT MOOTED BY THE SWAP, and either tool's fail-closed test must read stderr.

    No corpus repository ships a knip configuration of any spelling, so `-c` isolates cleanly — proved by byte-identical output with and without a `-c` holding one key.

    STILL UNTESTED, and it is the gap that decides: `--include-entry-exports`, and knip's `workspaces` / `entry` / `project` configuration keys. The zustand `paths` gap and the redux `examples` walk-in would both have to be answered there. The shipped rule ALREADY computes the entry module list in its own node script, so feeding that list to knip's `entry` key is an obvious shape nobody has measured. This project rejected knip once before on a configuration it never tried; that measurement is running now rather than repeating the error.
  timestamp: 2026-08-15T13:33:04.138849+00:00
- actor: claude-code
  id: 01m02tzj2zqdcezrrvm8ewyfeg
  text: |-
    THE DECISION: `ts-prune` IS KEPT. What decided it is RECALL, and it is a third property this card did not name — both properties the card DID name settle well.

    The last gap is closed. Feeding knip the entry list the shipped rule's own node script already computes, plus an `ignore` built from the tsconfig `exclude`, ANSWERS BOTH KNOWN LOSSES: zustand goes 8 findings to 0 at exit 0, and redux goes 4 to 1, leaving only the genuinely dead `mangleErrors.mts` default. So neither loss survives, and knip was not rejected on a configuration nobody tried.

    The honest final table, `node_modules` installed, knip tuned as above:

    | repository | tool | findings | time | genuinely dead | framework entry | used in own file | wrong |
    |---|---|---|---|---|---|---|---|
    | zod | the shipped rule | 76 | 10.78 s | 28 | 47 | 0 | 1 |
    | zod | knip tuned | 7 | 0.41 s | 4 | 0 | 3 | 0 |
    | zustand | the shipped rule | 1 | 0.93 s | 0 | 1 | 0 | 0 |
    | zustand | knip tuned | 0 | 0.38 s | 0 | 0 | 0 | 0 |
    | redux | the shipped rule | 6 | 1.67 s | 1 | 5 | 0 | 0 |
    | redux | knip tuned | 1 | 0.52 s | 1 | 0 | 0 | 0 |

    knip wins precision outright — 100 % against 35 % — and it is 20x faster. It LOSES RECALL, and that is the whole answer: of the 30 genuinely dead symbols the two tools jointly name, THE SHIPPED RULE NAMES 29 AND TUNED KNIP NAMES 2.

    Three causes, and no configuration reaches any of them:
    1. knip WILL NOT ENUMERATE THE EXPORTS OF A MODULE NO ENTRY REACHES. It writes one `files` entry with no line and stops. On zod that swallows 27 dead symbols into 6 module lines with no names.
    2. A dead export inside a module that IS an entry is invisible. `packages/tsc/generate.ts` `VALIBOT` is dead and knip answers `No exports found`, because a `package.json` script names the module. The only lever is `includeEntryExports`, which is all or nothing — it takes zod to 362 and zustand to 13.
    3. `ignoreExportsUsedInFile` does not reach `namespaceMembers`, so all 3 remaining zod false positives are namespace members used on the next lines of their own file.

    Cause 1 is fatal for THIS rule. Its contract is `path:line: message` on the changed files, and the everyday shape of dead code in review — an author adds a module nothing imports — is exactly the shape knip cannot report by symbol. Proved on a minimal probe: a package publishing `src/index.ts` beside an unreferenced `src/orphan.ts` holding two dead exports.

    | the run | what it answered |
    |---|---|
    | the shipped rule | `src/orphan.ts:2` and `src/orphan.ts:5`, each named |
    | knip tuned, `--include exports,types,namespaceMembers,enumMembers` | `{"issues":[]}` at exit 0 |
    | the same plus `files` | one `files` entry, `"exports":[]`, no line |

    The rule states its claim as "every exported symbol no other module in the project imports". knip cannot make that claim. It makes a different one — "no entry reaches this file", and then, inside the files an entry does reach, "nothing imports this export". A better gate and a worse inventory.

    This is not inertia. ts-prune is archived and that risk is real and growing, and the rule body will now name it, name knip as the successor, and carry every measurement above so the next reader can re-open the question with numbers rather than a survey. The two open cards are ts-prune's to answer:
    - ^gxncs25 IS NOT MOOTED, and would NOT have been mooted by the swap. Measured: a `tsconfig.json` that is not JSON leaves knip's count at 17 on zod with byte-identical JSON and 174 bytes on stderr the only signal, and leaves the shipped rule's count at 76 at exit 0. Both tools need a fail-closed test that reads stderr.
    - ^yxky1aj IS ts-prune-specific and it stands. It reproduces at 1 line of 76 on zod, in `packages/bench`, and the swap WOULD have eliminated it, because knip runs once at the root and writes paths relative to it with no per-project prefix arithmetic at all.
  timestamp: 2026-08-15T13:50:01.311640+00:00
- actor: claude-code
  id: 01m02vb74m7s0sh36skjqqh0bv
  text: |-
    ### implement — changed
    - evidence: 3 files — `builtin/validators/code-hygiene/rules/dead-code-typescript.md`, `builtin/validators/code-hygiene/VALIDATOR.md`, `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/dead_code_typescript.rs`. `cargo nextest run -p swissarmyhammer-validators`: 745 tests run, 745 passed, 0 skipped. `cargo fmt --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean.
    - next: `/review`

    THE DECISION: `ts-prune` is KEPT. Nothing was installed — knip 6.32.2 was already on this machine, and 6.32.2 is also `latest`, so it is the version every measurement used.

    READ THIS BEFORE ACCEPTING THE DECISION. Both properties this card named settle WELL, so the card's own decision rule pointed at the swap. The swap was refused on a THIRD property the card did not name — RECALL — and that is a deviation the reviewer should weigh rather than take on trust:
    - the staging contract settles well: knip's custom tag carries a free-text reason, works on every export kind, and EXPIRES, which `// ts-prune-ignore-next` never does;
    - the exit status settles well: 0 clean, 1 findings, 2 refused, status 2 unmaskable by `--no-exit-code` and carrying prose rather than JSON on stdout;
    - recall is what refused it: of the 30 genuinely dead symbols the two tools jointly name over the corpus, the shipped rule names 29 and tuned knip names 2, because knip collapses an unreachable module into one positionless `files` entry and no configuration makes it enumerate the exports.

    The card's stated case for moving is also refuted and that is recorded in both files: the 78-against-13 table was taken with no `node_modules`, where knip is degraded and still exits 1, and its include list named `nsExports` and `nsTypes`, two spellings that select nothing in 6.32.2.

    WHAT LANDED:
    - `dead-code-typescript.md` gains a `### The knip decision, and what decided it` section carrying every measurement in the file's existing table style — the corrected corpus comparison, the hand-checked true-positive table, the three structural causes, the `-c` flag, the `node_modules` requirement, the reporter shape and the whole exit-status table. The corpus table now states its install state, and the staging-contract section names the non-expiring marker as a DEFECT of the shipped tool.
    - `VALIDATOR.md`'s knip verdict is corrected: it quoted the refuted table and implied the swap was still open.
    - One acceptance test, `the_shipped_typescript_dead_code_tool_rule_names_every_export_of_a_module_nothing_imports`, over a probe publishing `src/index.ts` beside an unreferenced `src/orphan.ts` holding a const, a function and a type.

    ON THE RED STEP, STATED PLAINLY. The test passes on its first run against the shipped bytes, because the decision PRESERVES that behaviour rather than changing it. RED was established the other way, by running the alternative on the identical probe: `knip -c <tuned> --reporter json --no-config-hints --include exports,types,namespaceMembers,enumMembers` answers `{"issues":[]}` at exit 0, and adding `files` answers one entry carrying `"exports":[]` and no row, while the shipped script names `src/orphan.ts:2`, `:5` and `:10`. So the test discriminates between the two tools and would fail under the swap. I did not manufacture a failing run against ts-prune, and I am not claiming one.

    ^gxncs25 and ^yxky1aj each carry a comment: NEITHER is mooted, ^gxncs25 would NOT have been mooted by the swap either (knip is equally silent for a broken tsconfig, so its fix must read stderr), and ^yxky1aj WOULD have been eliminated structurally by the swap. Neither was fixed here.
  timestamp: 2026-08-15T13:56:23.316436+00:00
position_column: doing
position_ordinal: '8280'
title: Decide whether dead-code-typescript moves from the archived ts-prune to knip
---
`ts-prune` 0.10.3 was published on 2021-12-12, its GitHub repository is ARCHIVED, and its own README carries a maintenance notice its author added on 2025-09-19: "**ts-prune is now in maintenance mode** - For new projects, we recommend [knip](https://github.com/webpro/knip) which carries forward the same mission with more features." `depcheck` and `unimported` are archived too and each names knip as the successor.

`knip` 6.32.2 answers both carve-outs of `dead-code`. It reads entry files out of `package.json` `main`, `bin` and `exports`, and it ships a plugin for each framework whose configuration file becomes an entry file of its own — which is the vite `resolve.alias` target, the vitest browser command and the Next.js route module that `dead-code-typescript` still leaves to a `// ts-prune-ignore-next` marker.

Measured on 2026-08-14 over three published libraries, `knip 6.32.0` with `--include exports,nsExports,types,nsTypes` against the shipped rule:

| workspace | commit | dead-code-typescript | knip |
|---|---|---|---|
| colinhacks/zod | `4e1720c` | 78 | 13 |
| pmndrs/zustand | `2115efb` | 1 | 0 |
| reduxjs/redux | `3084fc3` | 6 | 2 |

Both reasons `builtin/validators/code-hygiene/VALIDATOR.md` gave for rejecting knip are wrong as written, and that file now says so. Two open properties decide the swap, and each needs a measurement:

- **The staging contract.** knip has NO line-comment suppression, and its documentation refuses one. Its inline surface is a JSDoc tag: `/** @public */`, `/** @internal */`, `/** @beta */`, and an arbitrary tag filtered by `--tags=-<name>`. The `dead-code` prompt rule's contract is a marker that states "a consumer lands next", and `@public` states "public" instead. Measure whether a custom tag can carry the reason and whether it expires.
- **The exit status.** knip exits 0 for a clean run, 1 for a run that found issues, and 2 for a run it could not make. `--no-exit-code` forces 0. The script has to tell a broken run from a run with findings, which is the trap `builtin/validators/README.md` states.

Also measure: whether knip needs an installed `node_modules` for the answer to be right, the `-c <file>` external configuration flag, and `--reporter json --no-config-hints` as the machine-readable shape (`{"issues":[{"file":..., "exports":[{"name","line","col"}]}]}`).

If the swap lands, keep the corpus and the tables of `dead-code-typescript.md` so the two tools stay comparable.

Found while implementing ^108bh4y. #tool-validators #objectivity
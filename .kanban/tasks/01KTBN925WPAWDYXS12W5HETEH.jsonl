{"id":"01KTBN925WGD5HEHXFW9W4Q1F6","timestamp":"2026-06-05T10:29:20.188378Z","op":"create","item_id":"01KTBN925WPAWDYXS12W5HETEH","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,31 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KTBN8CQDYD52W5MGG4ZYKNN7\n+position_column: todo\n+position_ordinal: '8280'\n+project: local-review\n+title: Decouple the AVP rule loader from hook execution (rules-as-data, standalone)\n+---\n+## What\n+Make the rules-as-data layer loadable WITHOUT pulling in the hook/runner machinery, so the new `review` tool can consume rules independently. This preserves the one genuinely good idea in AVP — pluggable rules as markdown with precedence stacking — while severing it from the slow per-tool-call execution path being retired.\n+\n+- Today the loader lives in `crates/avp-common/src/validator/loader.rs` (`ValidatorLoader`) with types in `crates/avp-common/src/validator/types.rs` (`RuleSet`, `Validator`, `ValidatorMatch`, glob/precedence helpers). It is currently entangled with `runner.rs` (ACP execution) and the chain/hook links.\n+- Expose a clean, hook-free entry point: given a set of validator directories (builtin → user → project precedence) and a target file path, return the matched `RuleSet`/rules WITHOUT requiring any `ConnectionTo<Agent>`, hook context, or sidecar diff files.\n+- Keep the existing builtin rule discovery (`builtin/validators/**`) and the `@file_groups/...` / `@`-include expansion working.\n+- Do not move files unnecessarily; the goal is a callable API surface (e.g. `ValidatorLoader::load_all()` + a `match_rules(file_path) -> Vec<MatchedRule>` that has no hook dependency). If a small module split is cleaner than threading flags, do that, but keep `avp-common` the home of the loader.\n+\n+## Acceptance Criteria\n+- [ ] A public function/method on the loader returns matched rules for a given file path with NO argument referencing hooks, ACP, agents, or turn-diff sidecars.\n+- [ ] Builtin → user → project precedence stacking and `@`-include expansion still resolve identically to today (covered by a test).\n+- [ ] `crates/swissarmyhammer-tools` can depend on this entry point without compiling the hook/chain/runner modules into its call path (verify the dependency direction; add the dep if missing).\n+- [ ] No behavior change to the existing (about-to-be-retired) hook path yet — this task only adds the decoupled surface.\n+\n+## Tests\n+- [ ] Unit test in `crates/avp-common` (real loader, real `builtin/validators/` fixtures): load all, match a `.rs` path → asserts the `code-quality` ruleset and its rules resolve, and that a non-source path (e.g. `README.md` against a source-only matcher) does not match.\n+- [ ] Precedence test: a project-level validator overriding a builtin of the same name wins (use a temp dir with a fixture validator).\n+- [ ] `cargo test -p avp-common loader` green; `cargo build -p swissarmyhammer-tools` green with the new dependency edge.\n+\n+## Workflow\n+- Use `/tdd` — write the matcher/precedence tests first against real `builtin/validators/` data, then extract the hook-free surface to make them pass. Reuse existing `compile_glob_patterns`/`matches_any_pattern`; do not reimplement glob matching.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,31 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KTBN8CQDYD52W5MGG4ZYKNN7\n-position_column: todo\n-position_ordinal: '8280'\n-project: local-review\n-title: Decouple the AVP rule loader from hook execution (rules-as-data, standalone)\n----\n-## What\n-Make the rules-as-data layer loadable WITHOUT pulling in the hook/runner machinery, so the new `review` tool can consume rules independently. This preserves the one genuinely good idea in AVP — pluggable rules as markdown with precedence stacking — while severing it from the slow per-tool-call execution path being retired.\n-\n-- Today the loader lives in `crates/avp-common/src/validator/loader.rs` (`ValidatorLoader`) with types in `crates/avp-common/src/validator/types.rs` (`RuleSet`, `Validator`, `ValidatorMatch`, glob/precedence helpers). It is currently entangled with `runner.rs` (ACP execution) and the chain/hook links.\n-- Expose a clean, hook-free entry point: given a set of validator directories (builtin → user → project precedence) and a target file path, return the matched `RuleSet`/rules WITHOUT requiring any `ConnectionTo<Agent>`, hook context, or sidecar diff files.\n-- Keep the existing builtin rule discovery (`builtin/validators/**`) and the `@file_groups/...` / `@`-include expansion working.\n-- Do not move files unnecessarily; the goal is a callable API surface (e.g. `ValidatorLoader::load_all()` + a `match_rules(file_path) -> Vec<MatchedRule>` that has no hook dependency). If a small module split is cleaner than threading flags, do that, but keep `avp-common` the home of the loader.\n-\n-## Acceptance Criteria\n-- [ ] A public function/method on the loader returns matched rules for a given file path with NO argument referencing hooks, ACP, agents, or turn-diff sidecars.\n-- [ ] Builtin → user → project precedence stacking and `@`-include expansion still resolve identically to today (covered by a test).\n-- [ ] `crates/swissarmyhammer-tools` can depend on this entry point without compiling the hook/chain/runner modules into its call path (verify the dependency direction; add the dep if missing).\n-- [ ] No behavior change to the existing (about-to-be-retired) hook path yet — this task only adds the decoupled surface.\n-\n-## Tests\n-- [ ] Unit test in `crates/avp-common` (real loader, real `builtin/validators/` fixtures): load all, match a `.rs` path → asserts the `code-quality` ruleset and its rules resolve, and that a non-source path (e.g. `README.md` against a source-only matcher) does not match.\n-- [ ] Precedence test: a project-level validator overriding a builtin of the same name wins (use a temp dir with a fixture validator).\n-- [ ] `cargo test -p avp-common loader` green; `cargo build -p swissarmyhammer-tools` green with the new dependency edge.\n-\n-## Workflow\n-- Use `/tdd` — write the matcher/precedence tests first against real `builtin/validators/` data, then extract the hook-free surface to make them pass. Reuse existing `compile_glob_patterns`/`matches_any_pattern`; do not reimplement glob matching.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTBNH7W3HYPS3JQ6VQ4NWAJP","timestamp":"2026-06-05T10:33:48.163566Z","op":"update","item_id":"01KTBN925WPAWDYXS12W5HETEH","forward_patch":"--- original\n+++ modified\n@@ -3,29 +3,33 @@\n - claude-code\n depends_on:\n - 01KTBN8CQDYD52W5MGG4ZYKNN7\n+- 01KTBNFB7NPXNWKDK86T9A0M5C\n+- 01KTBNGHCH7B3J3DVF9CXPADJ1\n+- 01KTBNFMEAS2QQRH7N0RVDKC27\n position_column: todo\n position_ordinal: '8280'\n project: local-review\n-title: Decouple the AVP rule loader from hook execution (rules-as-data, standalone)\n+title: Rename avp-common → swissarmyhammer-validators (the pluggable review engine crate)\n ---\n ## What\n-Make the rules-as-data layer loadable WITHOUT pulling in the hook/runner machinery, so the new `review` tool can consume rules independently. This preserves the one genuinely good idea in AVP — pluggable rules as markdown with precedence stacking — while severing it from the slow per-tool-call execution path being retired.\n+Rename the now-slimmed `avp-common` crate (loader + extracted ACP executor + validator types, after the hook machinery and avp-cli are gone) to **`swissarmyhammer-validators`**. This becomes the shared engine crate for the pluggable review system; the new `review` MCP tool and the engine stages all live here or depend on it.\n\n-- Today the loader lives in `crates/avp-common/src/validator/loader.rs` (`ValidatorLoader`) with types in `crates/avp-common/src/validator/types.rs` (`RuleSet`, `Validator`, `ValidatorMatch`, glob/precedence helpers). It is currently entangled with `runner.rs` (ACP execution) and the chain/hook links.\n-- Expose a clean, hook-free entry point: given a set of validator directories (builtin → user → project precedence) and a target file path, return the matched `RuleSet`/rules WITHOUT requiring any `ConnectionTo<Agent>`, hook context, or sidecar diff files.\n-- Keep the existing builtin rule discovery (`builtin/validators/**`) and the `@file_groups/...` / `@`-include expansion working.\n-- Do not move files unnecessarily; the goal is a callable API surface (e.g. `ValidatorLoader::load_all()` + a `match_rules(file_path) -> Vec<MatchedRule>` that has no hook dependency). If a small module split is cleaner than threading flags, do that, but keep `avp-common` the home of the loader.\n+- Rename `crates/avp-common/` → `crates/swissarmyhammer-validators/`; set `name = \"swissarmyhammer-validators\"` in its `Cargo.toml`.\n+- Update every dependent's `Cargo.toml` and `use` path: `agent-client-protocol-extras`, `apps/swissarmyhammer-cli`, the root `Cargo.toml` `[workspace.members]` + `[workspace.dependencies]`. (`apps/avp-cli` is already deleted.)\n+- Pick the canonical crate-internal module name (`validators`) and update re-exports so `swissarmyhammer_validators::ValidatorLoader`, `::Finding` (added in a later task), and the `execute_agents` primitive are the public surface.\n+- Expose a clean, hook-free engine API at the crate root: `load_rules()`, `match_rules(file_path)`, `execute_agents(...)`. This is the decoupled, standalone rule-matching surface (no hook/ACP-hook arguments) that downstream stages consume.\n+- Keep `builtin/validators/**` rule data discoverable by the renamed loader (the on-disk directory move to `~/.validators` / `./.validators` is a separate task).\n\n ## Acceptance Criteria\n-- [ ] A public function/method on the loader returns matched rules for a given file path with NO argument referencing hooks, ACP, agents, or turn-diff sidecars.\n-- [ ] Builtin → user → project precedence stacking and `@`-include expansion still resolve identically to today (covered by a test).\n-- [ ] `crates/swissarmyhammer-tools` can depend on this entry point without compiling the hook/chain/runner modules into its call path (verify the dependency direction; add the dep if missing).\n-- [ ] No behavior change to the existing (about-to-be-retired) hook path yet — this task only adds the decoupled surface.\n+- [ ] `crates/swissarmyhammer-validators/` exists; no crate named `avp-common` remains; `cargo build --workspace` green.\n+- [ ] All dependents compile against the new crate name; `rg -n \"avp_common|avp-common\"` returns nothing outside historical docs.\n+- [ ] The crate root re-exports the hook-free engine API (`load_rules`/`match_rules`/`execute_agents`), callable with NO hook/ACP-hook arguments.\n+- [ ] `crates/swissarmyhammer-tools` can add `swissarmyhammer-validators` as a dependency and call `match_rules` in a smoke test.\n\n ## Tests\n-- [ ] Unit test in `crates/avp-common` (real loader, real `builtin/validators/` fixtures): load all, match a `.rs` path → asserts the `code-quality` ruleset and its rules resolve, and that a non-source path (e.g. `README.md` against a source-only matcher) does not match.\n-- [ ] Precedence test: a project-level validator overriding a builtin of the same name wins (use a temp dir with a fixture validator).\n-- [ ] `cargo test -p avp-common loader` green; `cargo build -p swissarmyhammer-tools` green with the new dependency edge.\n+- [ ] `cargo build --workspace` and `cargo test --workspace` green after rename.\n+- [ ] A loader test (moved with the crate) proving builtin → user → project precedence + glob matching still resolve identically.\n+- [ ] Smoke test from `swissarmyhammer-tools`: depend on the crate, call `match_rules(\"foo.rs\")`, assert the source-code ruleset matches.\n\n ## Workflow\n-- Use `/tdd` — write the matcher/precedence tests first against real `builtin/validators/` data, then extract the hook-free surface to make them pass. Reuse existing `compile_glob_patterns`/`matches_any_pattern`; do not reimplement glob matching.\n\\ No newline at end of file\n+- Mechanical rename + API-surface cleanup. No `/tdd`; rely on the moved loader/executor tests. Do the rename as one atomic change (git mv + sed the dependents) so the workspace never sits half-renamed. Depends on all three teardown tasks so the residue being renamed is already the clean engine.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -3,33 +3,29 @@\n - claude-code\n depends_on:\n - 01KTBN8CQDYD52W5MGG4ZYKNN7\n-- 01KTBNFB7NPXNWKDK86T9A0M5C\n-- 01KTBNGHCH7B3J3DVF9CXPADJ1\n-- 01KTBNFMEAS2QQRH7N0RVDKC27\n position_column: todo\n position_ordinal: '8280'\n project: local-review\n-title: Rename avp-common → swissarmyhammer-validators (the pluggable review engine crate)\n+title: Decouple the AVP rule loader from hook execution (rules-as-data, standalone)\n ---\n ## What\n-Rename the now-slimmed `avp-common` crate (loader + extracted ACP executor + validator types, after the hook machinery and avp-cli are gone) to **`swissarmyhammer-validators`**. This becomes the shared engine crate for the pluggable review system; the new `review` MCP tool and the engine stages all live here or depend on it.\n+Make the rules-as-data layer loadable WITHOUT pulling in the hook/runner machinery, so the new `review` tool can consume rules independently. This preserves the one genuinely good idea in AVP — pluggable rules as markdown with precedence stacking — while severing it from the slow per-tool-call execution path being retired.\n\n-- Rename `crates/avp-common/` → `crates/swissarmyhammer-validators/`; set `name = \"swissarmyhammer-validators\"` in its `Cargo.toml`.\n-- Update every dependent's `Cargo.toml` and `use` path: `agent-client-protocol-extras`, `apps/swissarmyhammer-cli`, the root `Cargo.toml` `[workspace.members]` + `[workspace.dependencies]`. (`apps/avp-cli` is already deleted.)\n-- Pick the canonical crate-internal module name (`validators`) and update re-exports so `swissarmyhammer_validators::ValidatorLoader`, `::Finding` (added in a later task), and the `execute_agents` primitive are the public surface.\n-- Expose a clean, hook-free engine API at the crate root: `load_rules()`, `match_rules(file_path)`, `execute_agents(...)`. This is the decoupled, standalone rule-matching surface (no hook/ACP-hook arguments) that downstream stages consume.\n-- Keep `builtin/validators/**` rule data discoverable by the renamed loader (the on-disk directory move to `~/.validators` / `./.validators` is a separate task).\n+- Today the loader lives in `crates/avp-common/src/validator/loader.rs` (`ValidatorLoader`) with types in `crates/avp-common/src/validator/types.rs` (`RuleSet`, `Validator`, `ValidatorMatch`, glob/precedence helpers). It is currently entangled with `runner.rs` (ACP execution) and the chain/hook links.\n+- Expose a clean, hook-free entry point: given a set of validator directories (builtin → user → project precedence) and a target file path, return the matched `RuleSet`/rules WITHOUT requiring any `ConnectionTo<Agent>`, hook context, or sidecar diff files.\n+- Keep the existing builtin rule discovery (`builtin/validators/**`) and the `@file_groups/...` / `@`-include expansion working.\n+- Do not move files unnecessarily; the goal is a callable API surface (e.g. `ValidatorLoader::load_all()` + a `match_rules(file_path) -> Vec<MatchedRule>` that has no hook dependency). If a small module split is cleaner than threading flags, do that, but keep `avp-common` the home of the loader.\n\n ## Acceptance Criteria\n-- [ ] `crates/swissarmyhammer-validators/` exists; no crate named `avp-common` remains; `cargo build --workspace` green.\n-- [ ] All dependents compile against the new crate name; `rg -n \"avp_common|avp-common\"` returns nothing outside historical docs.\n-- [ ] The crate root re-exports the hook-free engine API (`load_rules`/`match_rules`/`execute_agents`), callable with NO hook/ACP-hook arguments.\n-- [ ] `crates/swissarmyhammer-tools` can add `swissarmyhammer-validators` as a dependency and call `match_rules` in a smoke test.\n+- [ ] A public function/method on the loader returns matched rules for a given file path with NO argument referencing hooks, ACP, agents, or turn-diff sidecars.\n+- [ ] Builtin → user → project precedence stacking and `@`-include expansion still resolve identically to today (covered by a test).\n+- [ ] `crates/swissarmyhammer-tools` can depend on this entry point without compiling the hook/chain/runner modules into its call path (verify the dependency direction; add the dep if missing).\n+- [ ] No behavior change to the existing (about-to-be-retired) hook path yet — this task only adds the decoupled surface.\n\n ## Tests\n-- [ ] `cargo build --workspace` and `cargo test --workspace` green after rename.\n-- [ ] A loader test (moved with the crate) proving builtin → user → project precedence + glob matching still resolve identically.\n-- [ ] Smoke test from `swissarmyhammer-tools`: depend on the crate, call `match_rules(\"foo.rs\")`, assert the source-code ruleset matches.\n+- [ ] Unit test in `crates/avp-common` (real loader, real `builtin/validators/` fixtures): load all, match a `.rs` path → asserts the `code-quality` ruleset and its rules resolve, and that a non-source path (e.g. `README.md` against a source-only matcher) does not match.\n+- [ ] Precedence test: a project-level validator overriding a builtin of the same name wins (use a temp dir with a fixture validator).\n+- [ ] `cargo test -p avp-common loader` green; `cargo build -p swissarmyhammer-tools` green with the new dependency edge.\n\n ## Workflow\n-- Mechanical rename + API-surface cleanup. No `/tdd`; rely on the moved loader/executor tests. Do the rename as one atomic change (git mv + sed the dependents) so the workspace never sits half-renamed. Depends on all three teardown tasks so the residue being renamed is already the clean engine.\n\\ No newline at end of file\n+- Use `/tdd` — write the matcher/precedence tests first against real `builtin/validators/` data, then extract the hook-free surface to make them pass. Reuse existing `compile_glob_patterns`/`matches_any_pattern`; do not reimplement glob matching.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTBS24R3A610Q604GBXRRSN8","timestamp":"2026-06-05T11:35:27.747549Z","op":"update","item_id":"01KTBN925WPAWDYXS12W5HETEH","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,6 @@\n assignees:\n - claude-code\n depends_on:\n-- 01KTBN8CQDYD52W5MGG4ZYKNN7\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n - 01KTBNGHCH7B3J3DVF9CXPADJ1\n - 01KTBNFMEAS2QQRH7N0RVDKC27\n","reverse_patch":"--- original\n+++ modified\n@@ -2,6 +2,7 @@\n assignees:\n - claude-code\n depends_on:\n+- 01KTBN8CQDYD52W5MGG4ZYKNN7\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n - 01KTBNGHCH7B3J3DVF9CXPADJ1\n - 01KTBNFMEAS2QQRH7N0RVDKC27\n","transaction_id":null}
{"id":"01KTC9PQFZQ1E0FXRCR3FGGD9G","timestamp":"2026-06-05T16:26:19.519099Z","op":"update","item_id":"01KTBN925WPAWDYXS12W5HETEH","forward_patch":"--- original\n+++ modified\n@@ -5,8 +5,8 @@\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n - 01KTBNGHCH7B3J3DVF9CXPADJ1\n - 01KTBNFMEAS2QQRH7N0RVDKC27\n-position_column: todo\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '80'\n project: local-review\n title: Rename avp-common → swissarmyhammer-validators (the pluggable review engine crate)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -5,8 +5,8 @@\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n - 01KTBNGHCH7B3J3DVF9CXPADJ1\n - 01KTBNFMEAS2QQRH7N0RVDKC27\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '8280'\n project: local-review\n title: Rename avp-common → swissarmyhammer-validators (the pluggable review engine crate)\n ---\n","transaction_id":null}
{"id":"01KTCA86P3NVVXKPK55NSPKSYC","timestamp":"2026-06-05T16:35:52.131550Z","op":"update","item_id":"01KTBN925WPAWDYXS12W5HETEH","forward_patch":"--- original\n+++ modified\n@@ -5,7 +5,7 @@\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n - 01KTBNGHCH7B3J3DVF9CXPADJ1\n - 01KTBNFMEAS2QQRH7N0RVDKC27\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: Rename avp-common → swissarmyhammer-validators (the pluggable review engine crate)\n","reverse_patch":"--- original\n+++ modified\n@@ -5,7 +5,7 @@\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n - 01KTBNGHCH7B3J3DVF9CXPADJ1\n - 01KTBNFMEAS2QQRH7N0RVDKC27\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: Rename avp-common → swissarmyhammer-validators (the pluggable review engine crate)\n","transaction_id":null}
{"id":"01KTCAD45WN5WBMQM1HNKA4PQS","timestamp":"2026-06-05T16:38:33.404262Z","op":"update","item_id":"01KTBN925WPAWDYXS12W5HETEH","forward_patch":"--- original\n+++ modified\n@@ -5,8 +5,8 @@\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n - 01KTBNGHCH7B3J3DVF9CXPADJ1\n - 01KTBNFMEAS2QQRH7N0RVDKC27\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffef80\n project: local-review\n title: Rename avp-common → swissarmyhammer-validators (the pluggable review engine crate)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -5,8 +5,8 @@\n - 01KTBNFB7NPXNWKDK86T9A0M5C\n - 01KTBNGHCH7B3J3DVF9CXPADJ1\n - 01KTBNFMEAS2QQRH7N0RVDKC27\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffef80\n+position_column: review\n+position_ordinal: '80'\n project: local-review\n title: Rename avp-common → swissarmyhammer-validators (the pluggable review engine crate)\n ---\n","transaction_id":null}

{"id":"01KQ4YYFCGYJRP3PVJZ9YTRT53","timestamp":"2026-04-26T13:16:32.528558Z","op":"create","item_id":"01KQ4YYFCGJCRN6GBYGVGXVVG6","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,165 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n+- 01KNQXXF5W7G4JP73C6ZCMKYKX\n+position_column: todo\n+position_ordinal: ff9780\n+project: spatial-nav\n+title: 'Tauri spatial-nav adapter foundation: wire SpatialState/SpatialRegistry into AppState, add core commands, register handlers'\n+---\n+## What\n+\n+Establish the **Tauri adapter foundation** for spatial navigation. The headless `swissarmyhammer-focus` crate (kernel work — `SpatialState`, `SpatialRegistry`, `BeamNavStrategy`, `FallbackResolution`, `RegisterEntry`, drill-in/drill-out) is complete and exhaustively tested at the Rust level. But there is **no Tauri command surface** wiring the kernel into the kanban-app yet — `kanban-app/src/state.rs` has no `SpatialState`/`SpatialRegistry` field; `kanban-app/src/commands.rs` has no `spatial_*` command; `kanban-app/src/main.rs` has no spatial entries in `invoke_handler!`.\n+\n+The React side (`kanban-app/ui/src/lib/spatial-focus-context.tsx`) already invokes `spatial_focus`, `spatial_register_focusable`, `spatial_register_zone`, `spatial_unregister_scope`, `spatial_update_rect`, `spatial_navigate`, `spatial_push_layer`, `spatial_pop_layer`, `spatial_drill_in`, `spatial_drill_out` — all of these will fail at runtime today because the commands don't exist. Several downstream cards depend on this surface: dynamic-lifecycle (`01KNS0B3HY...`), drill-in/drill-out (`01KPZS4RG0...`), inspector layer (`01KNQXYC4R...`), board/column/card/grid zones, dialogs, navOverride cleanup. None of them can complete their UI/IPC work without this foundation.\n+\n+This card establishes the foundation:\n+\n+1. **AppState wiring** — `kanban-app/src/state.rs` gains `spatial_state: Arc<Mutex<SpatialState>>` and `spatial_registry: Arc<Mutex<SpatialRegistry>>` (both inside the existing `AppState` struct; matching the per-window scoping `SpatialState` already provides).\n+2. **Commands** — `kanban-app/src/commands.rs` adds the **happy-path** commands (`spatial_focus`, `spatial_register_focusable`, `spatial_register_zone`, `spatial_unregister_scope`, `spatial_update_rect`, `spatial_navigate`, `spatial_push_layer`, `spatial_pop_layer`). Each command derives `WindowLabel` from `tauri::Window`, locks the registry/state, performs its kernel call, and emits `focus-changed` events to the appropriate windows when the kernel returns a `FocusChangedEvent`.\n+3. **Handler registration** — `kanban-app/src/main.rs` adds each command name to `tauri::generate_handler![...]`.\n+\n+This card does NOT cover (left to dependent cards):\n+\n+- `spatial_register_batch` and the kernel-side fallback during unregister — owned by `01KNS0B3HY...` (this card unblocks it).\n+- `spatial_drill_in` / `spatial_drill_out` — owned by `01KPZS4RG0...`.\n+- React `useStableSpatialKeys` / `usePlaceholderRegistration` and the virtualizer integration — owned by `01KNS0B3HY...`.\n+- Override resolution (rule 0) on `spatial_navigate` — owned by `01KNQY1GQ9...`.\n+\n+### Crate placement\n+\n+- `kanban-app/Cargo.toml` — add `swissarmyhammer-focus = { path = \"../swissarmyhammer-focus\" }` (currently absent).\n+- `kanban-app/src/state.rs` — `AppState::spatial_state`, `AppState::spatial_registry` fields.\n+- `kanban-app/src/commands.rs` — eight new `#[tauri::command]` async fns (read-only nav adapters; not state-mutating in the SAH-commands sense, so they bypass `dispatch_command` per the existing top-of-file rule for \"transient UI plumbing\").\n+- `kanban-app/src/main.rs` — register handlers.\n+\n+### Tauri command shapes — newtyped throughout\n+\n+```rust\n+#[tauri::command]\n+pub async fn spatial_focus(\n+    window: tauri::Window,\n+    state: State<'_, AppState>,\n+    key: SpatialKey,\n+) -> Result<(), String>;\n+\n+#[tauri::command]\n+pub async fn spatial_register_focusable(\n+    window: tauri::Window,\n+    state: State<'_, AppState>,\n+    key: SpatialKey,\n+    moniker: Moniker,\n+    rect: Rect,\n+    layer_key: LayerKey,\n+    parent_zone: Option<SpatialKey>,\n+    overrides: HashMap<Direction, Option<Moniker>>,\n+) -> Result<(), String>;\n+\n+#[tauri::command]\n+pub async fn spatial_register_zone(\n+    window: tauri::Window,\n+    state: State<'_, AppState>,\n+    key: SpatialKey,\n+    moniker: Moniker,\n+    rect: Rect,\n+    layer_key: LayerKey,\n+    parent_zone: Option<SpatialKey>,\n+    overrides: HashMap<Direction, Option<Moniker>>,\n+) -> Result<(), String>;\n+\n+#[tauri::command]\n+pub async fn spatial_unregister_scope(\n+    window: tauri::Window,\n+    state: State<'_, AppState>,\n+    key: SpatialKey,\n+) -> Result<(), String>;\n+// IMPORTANT: must call `SpatialState::handle_unregister` BEFORE `SpatialRegistry::unregister_scope`\n+// so the kernel can read the lost entry's metadata and compute fallback. This ordering is the\n+// \"spatial_unregister_scope reordered\" detail referenced in the dynamic-lifecycle card.\n+\n+#[tauri::command]\n+pub async fn spatial_update_rect(\n+    state: State<'_, AppState>,\n+    key: SpatialKey,\n+    rect: Rect,\n+) -> Result<(), String>;\n+\n+#[tauri::command]\n+pub async fn spatial_navigate(\n+    window: tauri::Window,\n+    state: State<'_, AppState>,\n+    key: SpatialKey,\n+    direction: Direction,\n+) -> Result<(), String>;\n+\n+#[tauri::command]\n+pub async fn spatial_push_layer(\n+    window: tauri::Window,\n+    state: State<'_, AppState>,\n+    key: LayerKey,\n+    name: LayerName,\n+    parent: Option<LayerKey>,\n+) -> Result<(), String>;\n+\n+#[tauri::command]\n+pub async fn spatial_pop_layer(\n+    state: State<'_, AppState>,\n+    key: LayerKey,\n+) -> Result<(), String>;\n+```\n+\n+### Event emission\n+\n+Each command that may return a `FocusChangedEvent` from the kernel emits it via `window.emit_to(EventTarget::any(), \"focus-changed\", payload)` (or a label-targeted variant if multi-window scoping requires it). The event shape is the kernel's `FocusChangedEvent` serialized to JSON; the React side already listens for it in `SpatialFocusProvider`.\n+\n+### Lock ordering\n+\n+Both `SpatialState` and `SpatialRegistry` are wrapped in `tokio::sync::Mutex` (matching the existing `AppState` pattern). The unregister command holds **both** locks for the duration of the transaction so observers cannot see a half-applied unregister:\n+\n+```rust\n+let mut registry = state.spatial_registry.lock().await;\n+let mut spatial_state = state.spatial_state.lock().await;\n+let event = spatial_state.handle_unregister(&registry, &key);\n+registry.unregister_scope(&key);\n+drop(spatial_state);\n+drop(registry);\n+if let Some(event) = event {\n+    emit_focus_changed(&window, &event)?;\n+}\n+```\n+\n+### Subtasks\n+\n+- [ ] Add `swissarmyhammer-focus` workspace dep to `kanban-app/Cargo.toml`\n+- [ ] Add `spatial_state: Arc<Mutex<SpatialState>>` and `spatial_registry: Arc<Mutex<SpatialRegistry>>` to `AppState` in `kanban-app/src/state.rs`; initialize in `AppState::new()`\n+- [ ] Add eight commands to `kanban-app/src/commands.rs` with the signatures above\n+- [ ] Add a `emit_focus_changed` helper that serializes the kernel's `FocusChangedEvent` and emits via the window handle\n+- [ ] Register all eight commands in `kanban-app/src/main.rs::run_app` `invoke_handler!`\n+- [ ] Confirm `unregister` calls `handle_unregister` before `unregister_scope` (the lock ordering snippet above)\n+- [ ] `cargo build -p kanban-app` succeeds; `cargo clippy -p kanban-app --all-targets -- -D warnings` clean\n+- [ ] React vitest suite: spatial-focus-context tests (which mock `invoke`) still pass — no React changes are made by this card\n+\n+## Acceptance Criteria\n+\n+- [ ] `kanban-app/Cargo.toml` declares `swissarmyhammer-focus` as a path dependency\n+- [ ] `AppState::spatial_state` and `AppState::spatial_registry` exist; default-initialized; thread-safe via `tokio::sync::Mutex`\n+- [ ] All eight `spatial_*` commands compile, are `#[tauri::command]`, accept newtyped arguments\n+- [ ] `unregister` ordering: `handle_unregister` runs **before** `unregister_scope`\n+- [ ] `main.rs::run_app` registers all eight commands in `invoke_handler!`\n+- [ ] `cargo build` and `cargo clippy --all-targets -- -D warnings` clean for the workspace\n+- [ ] `pnpm vitest run` for `kanban-app/ui` still passes\n+\n+## Tests\n+\n+- [ ] `kanban-app/src/commands.rs` (or a sibling `tests/spatial_commands.rs`) — `spatial_focus` invokes `SpatialState::focus` under the registry lock and emits `focus-changed`\n+- [ ] `register_focusable` / `register_zone` round-trip through `apply_batch`-style state changes\n+- [ ] `unregister` test: register a leaf, focus it, unregister — assert `handle_unregister` was called BEFORE `unregister_scope` and a `focus-changed` event was emitted\n+- [ ] `navigate` happy path: register two leaves, focus one, `navigate(Down)` returns the other's moniker\n+- [ ] `cargo test -p kanban-app` passes\n+- [ ] `cd kanban-app/ui && npx vitest run` passes\n+\n+## Workflow\n+\n+- This is mostly mechanical wiring — the headless kernel already has the API. Stand up the AppState fields, write the eight commands following the same pattern, register them in main.rs, and run `cargo build`. TDD on the Rust side (write a test for `spatial_unregister_scope` ordering first, then make it pass).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,165 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n-- 01KNQXXF5W7G4JP73C6ZCMKYKX\n-position_column: todo\n-position_ordinal: ff9780\n-project: spatial-nav\n-title: 'Tauri spatial-nav adapter foundation: wire SpatialState/SpatialRegistry into AppState, add core commands, register handlers'\n----\n-## What\n-\n-Establish the **Tauri adapter foundation** for spatial navigation. The headless `swissarmyhammer-focus` crate (kernel work — `SpatialState`, `SpatialRegistry`, `BeamNavStrategy`, `FallbackResolution`, `RegisterEntry`, drill-in/drill-out) is complete and exhaustively tested at the Rust level. But there is **no Tauri command surface** wiring the kernel into the kanban-app yet — `kanban-app/src/state.rs` has no `SpatialState`/`SpatialRegistry` field; `kanban-app/src/commands.rs` has no `spatial_*` command; `kanban-app/src/main.rs` has no spatial entries in `invoke_handler!`.\n-\n-The React side (`kanban-app/ui/src/lib/spatial-focus-context.tsx`) already invokes `spatial_focus`, `spatial_register_focusable`, `spatial_register_zone`, `spatial_unregister_scope`, `spatial_update_rect`, `spatial_navigate`, `spatial_push_layer`, `spatial_pop_layer`, `spatial_drill_in`, `spatial_drill_out` — all of these will fail at runtime today because the commands don't exist. Several downstream cards depend on this surface: dynamic-lifecycle (`01KNS0B3HY...`), drill-in/drill-out (`01KPZS4RG0...`), inspector layer (`01KNQXYC4R...`), board/column/card/grid zones, dialogs, navOverride cleanup. None of them can complete their UI/IPC work without this foundation.\n-\n-This card establishes the foundation:\n-\n-1. **AppState wiring** — `kanban-app/src/state.rs` gains `spatial_state: Arc<Mutex<SpatialState>>` and `spatial_registry: Arc<Mutex<SpatialRegistry>>` (both inside the existing `AppState` struct; matching the per-window scoping `SpatialState` already provides).\n-2. **Commands** — `kanban-app/src/commands.rs` adds the **happy-path** commands (`spatial_focus`, `spatial_register_focusable`, `spatial_register_zone`, `spatial_unregister_scope`, `spatial_update_rect`, `spatial_navigate`, `spatial_push_layer`, `spatial_pop_layer`). Each command derives `WindowLabel` from `tauri::Window`, locks the registry/state, performs its kernel call, and emits `focus-changed` events to the appropriate windows when the kernel returns a `FocusChangedEvent`.\n-3. **Handler registration** — `kanban-app/src/main.rs` adds each command name to `tauri::generate_handler![...]`.\n-\n-This card does NOT cover (left to dependent cards):\n-\n-- `spatial_register_batch` and the kernel-side fallback during unregister — owned by `01KNS0B3HY...` (this card unblocks it).\n-- `spatial_drill_in` / `spatial_drill_out` — owned by `01KPZS4RG0...`.\n-- React `useStableSpatialKeys` / `usePlaceholderRegistration` and the virtualizer integration — owned by `01KNS0B3HY...`.\n-- Override resolution (rule 0) on `spatial_navigate` — owned by `01KNQY1GQ9...`.\n-\n-### Crate placement\n-\n-- `kanban-app/Cargo.toml` — add `swissarmyhammer-focus = { path = \"../swissarmyhammer-focus\" }` (currently absent).\n-- `kanban-app/src/state.rs` — `AppState::spatial_state`, `AppState::spatial_registry` fields.\n-- `kanban-app/src/commands.rs` — eight new `#[tauri::command]` async fns (read-only nav adapters; not state-mutating in the SAH-commands sense, so they bypass `dispatch_command` per the existing top-of-file rule for \"transient UI plumbing\").\n-- `kanban-app/src/main.rs` — register handlers.\n-\n-### Tauri command shapes — newtyped throughout\n-\n-```rust\n-#[tauri::command]\n-pub async fn spatial_focus(\n-    window: tauri::Window,\n-    state: State<'_, AppState>,\n-    key: SpatialKey,\n-) -> Result<(), String>;\n-\n-#[tauri::command]\n-pub async fn spatial_register_focusable(\n-    window: tauri::Window,\n-    state: State<'_, AppState>,\n-    key: SpatialKey,\n-    moniker: Moniker,\n-    rect: Rect,\n-    layer_key: LayerKey,\n-    parent_zone: Option<SpatialKey>,\n-    overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<(), String>;\n-\n-#[tauri::command]\n-pub async fn spatial_register_zone(\n-    window: tauri::Window,\n-    state: State<'_, AppState>,\n-    key: SpatialKey,\n-    moniker: Moniker,\n-    rect: Rect,\n-    layer_key: LayerKey,\n-    parent_zone: Option<SpatialKey>,\n-    overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<(), String>;\n-\n-#[tauri::command]\n-pub async fn spatial_unregister_scope(\n-    window: tauri::Window,\n-    state: State<'_, AppState>,\n-    key: SpatialKey,\n-) -> Result<(), String>;\n-// IMPORTANT: must call `SpatialState::handle_unregister` BEFORE `SpatialRegistry::unregister_scope`\n-// so the kernel can read the lost entry's metadata and compute fallback. This ordering is the\n-// \"spatial_unregister_scope reordered\" detail referenced in the dynamic-lifecycle card.\n-\n-#[tauri::command]\n-pub async fn spatial_update_rect(\n-    state: State<'_, AppState>,\n-    key: SpatialKey,\n-    rect: Rect,\n-) -> Result<(), String>;\n-\n-#[tauri::command]\n-pub async fn spatial_navigate(\n-    window: tauri::Window,\n-    state: State<'_, AppState>,\n-    key: SpatialKey,\n-    direction: Direction,\n-) -> Result<(), String>;\n-\n-#[tauri::command]\n-pub async fn spatial_push_layer(\n-    window: tauri::Window,\n-    state: State<'_, AppState>,\n-    key: LayerKey,\n-    name: LayerName,\n-    parent: Option<LayerKey>,\n-) -> Result<(), String>;\n-\n-#[tauri::command]\n-pub async fn spatial_pop_layer(\n-    state: State<'_, AppState>,\n-    key: LayerKey,\n-) -> Result<(), String>;\n-```\n-\n-### Event emission\n-\n-Each command that may return a `FocusChangedEvent` from the kernel emits it via `window.emit_to(EventTarget::any(), \"focus-changed\", payload)` (or a label-targeted variant if multi-window scoping requires it). The event shape is the kernel's `FocusChangedEvent` serialized to JSON; the React side already listens for it in `SpatialFocusProvider`.\n-\n-### Lock ordering\n-\n-Both `SpatialState` and `SpatialRegistry` are wrapped in `tokio::sync::Mutex` (matching the existing `AppState` pattern). The unregister command holds **both** locks for the duration of the transaction so observers cannot see a half-applied unregister:\n-\n-```rust\n-let mut registry = state.spatial_registry.lock().await;\n-let mut spatial_state = state.spatial_state.lock().await;\n-let event = spatial_state.handle_unregister(&registry, &key);\n-registry.unregister_scope(&key);\n-drop(spatial_state);\n-drop(registry);\n-if let Some(event) = event {\n-    emit_focus_changed(&window, &event)?;\n-}\n-```\n-\n-### Subtasks\n-\n-- [ ] Add `swissarmyhammer-focus` workspace dep to `kanban-app/Cargo.toml`\n-- [ ] Add `spatial_state: Arc<Mutex<SpatialState>>` and `spatial_registry: Arc<Mutex<SpatialRegistry>>` to `AppState` in `kanban-app/src/state.rs`; initialize in `AppState::new()`\n-- [ ] Add eight commands to `kanban-app/src/commands.rs` with the signatures above\n-- [ ] Add a `emit_focus_changed` helper that serializes the kernel's `FocusChangedEvent` and emits via the window handle\n-- [ ] Register all eight commands in `kanban-app/src/main.rs::run_app` `invoke_handler!`\n-- [ ] Confirm `unregister` calls `handle_unregister` before `unregister_scope` (the lock ordering snippet above)\n-- [ ] `cargo build -p kanban-app` succeeds; `cargo clippy -p kanban-app --all-targets -- -D warnings` clean\n-- [ ] React vitest suite: spatial-focus-context tests (which mock `invoke`) still pass — no React changes are made by this card\n-\n-## Acceptance Criteria\n-\n-- [ ] `kanban-app/Cargo.toml` declares `swissarmyhammer-focus` as a path dependency\n-- [ ] `AppState::spatial_state` and `AppState::spatial_registry` exist; default-initialized; thread-safe via `tokio::sync::Mutex`\n-- [ ] All eight `spatial_*` commands compile, are `#[tauri::command]`, accept newtyped arguments\n-- [ ] `unregister` ordering: `handle_unregister` runs **before** `unregister_scope`\n-- [ ] `main.rs::run_app` registers all eight commands in `invoke_handler!`\n-- [ ] `cargo build` and `cargo clippy --all-targets -- -D warnings` clean for the workspace\n-- [ ] `pnpm vitest run` for `kanban-app/ui` still passes\n-\n-## Tests\n-\n-- [ ] `kanban-app/src/commands.rs` (or a sibling `tests/spatial_commands.rs`) — `spatial_focus` invokes `SpatialState::focus` under the registry lock and emits `focus-changed`\n-- [ ] `register_focusable` / `register_zone` round-trip through `apply_batch`-style state changes\n-- [ ] `unregister` test: register a leaf, focus it, unregister — assert `handle_unregister` was called BEFORE `unregister_scope` and a `focus-changed` event was emitted\n-- [ ] `navigate` happy path: register two leaves, focus one, `navigate(Down)` returns the other's moniker\n-- [ ] `cargo test -p kanban-app` passes\n-- [ ] `cd kanban-app/ui && npx vitest run` passes\n-\n-## Workflow\n-\n-- This is mostly mechanical wiring — the headless kernel already has the API. Stand up the AppState fields, write the eight commands following the same pattern, register them in main.rs, and run `cargo build`. TDD on the Rust side (write a test for `spatial_unregister_scope` ordering first, then make it pass).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ4YYFCHB5SQQ40VX0NTHPZF","timestamp":"2026-04-26T13:16:32.529056Z","entity_type":"task","entity_id":"01KQ4YYFCGJCRN6GBYGVGXVVG6","op":"create","changes":[["body",{"kind":"set","value":"## What\n\nEstablish the **Tauri adapter foundation** for spatial navigation. The headless `swissarmyhammer-focus` crate (kernel work — `SpatialState`, `SpatialRegistry`, `BeamNavStrategy`, `FallbackResolution`, `RegisterEntry`, drill-in/drill-out) is complete and exhaustively tested at the Rust level. But there is **no Tauri command surface** wiring the kernel into the kanban-app yet — `kanban-app/src/state.rs` has no `SpatialState`/`SpatialRegistry` field; `kanban-app/src/commands.rs` has no `spatial_*` command; `kanban-app/src/main.rs` has no spatial entries in `invoke_handler!`.\n\nThe React side (`kanban-app/ui/src/lib/spatial-focus-context.tsx`) already invokes `spatial_focus`, `spatial_register_focusable`, `spatial_register_zone`, `spatial_unregister_scope`, `spatial_update_rect`, `spatial_navigate`, `spatial_push_layer`, `spatial_pop_layer`, `spatial_drill_in`, `spatial_drill_out` — all of these will fail at runtime today because the commands don't exist. Several downstream cards depend on this surface: dynamic-lifecycle (`01KNS0B3HY...`), drill-in/drill-out (`01KPZS4RG0...`), inspector layer (`01KNQXYC4R...`), board/column/card/grid zones, dialogs, navOverride cleanup. None of them can complete their UI/IPC work without this foundation.\n\nThis card establishes the foundation:\n\n1. **AppState wiring** — `kanban-app/src/state.rs` gains `spatial_state: Arc<Mutex<SpatialState>>` and `spatial_registry: Arc<Mutex<SpatialRegistry>>` (both inside the existing `AppState` struct; matching the per-window scoping `SpatialState` already provides).\n2. **Commands** — `kanban-app/src/commands.rs` adds the **happy-path** commands (`spatial_focus`, `spatial_register_focusable`, `spatial_register_zone`, `spatial_unregister_scope`, `spatial_update_rect`, `spatial_navigate`, `spatial_push_layer`, `spatial_pop_layer`). Each command derives `WindowLabel` from `tauri::Window`, locks the registry/state, performs its kernel call, and emits `focus-changed` events to the appropriate windows when the kernel returns a `FocusChangedEvent`.\n3. **Handler registration** — `kanban-app/src/main.rs` adds each command name to `tauri::generate_handler![...]`.\n\nThis card does NOT cover (left to dependent cards):\n\n- `spatial_register_batch` and the kernel-side fallback during unregister — owned by `01KNS0B3HY...` (this card unblocks it).\n- `spatial_drill_in` / `spatial_drill_out` — owned by `01KPZS4RG0...`.\n- React `useStableSpatialKeys` / `usePlaceholderRegistration` and the virtualizer integration — owned by `01KNS0B3HY...`.\n- Override resolution (rule 0) on `spatial_navigate` — owned by `01KNQY1GQ9...`.\n\n### Crate placement\n\n- `kanban-app/Cargo.toml` — add `swissarmyhammer-focus = { path = \"../swissarmyhammer-focus\" }` (currently absent).\n- `kanban-app/src/state.rs` — `AppState::spatial_state`, `AppState::spatial_registry` fields.\n- `kanban-app/src/commands.rs` — eight new `#[tauri::command]` async fns (read-only nav adapters; not state-mutating in the SAH-commands sense, so they bypass `dispatch_command` per the existing top-of-file rule for \"transient UI plumbing\").\n- `kanban-app/src/main.rs` — register handlers.\n\n### Tauri command shapes — newtyped throughout\n\n```rust\n#[tauri::command]\npub async fn spatial_focus(\n    window: tauri::Window,\n    state: State<'_, AppState>,\n    key: SpatialKey,\n) -> Result<(), String>;\n\n#[tauri::command]\npub async fn spatial_register_focusable(\n    window: tauri::Window,\n    state: State<'_, AppState>,\n    key: SpatialKey,\n    moniker: Moniker,\n    rect: Rect,\n    layer_key: LayerKey,\n    parent_zone: Option<SpatialKey>,\n    overrides: HashMap<Direction, Option<Moniker>>,\n) -> Result<(), String>;\n\n#[tauri::command]\npub async fn spatial_register_zone(\n    window: tauri::Window,\n    state: State<'_, AppState>,\n    key: SpatialKey,\n    moniker: Moniker,\n    rect: Rect,\n    layer_key: LayerKey,\n    parent_zone: Option<SpatialKey>,\n    overrides: HashMap<Direction, Option<Moniker>>,\n) -> Result<(), String>;\n\n#[tauri::command]\npub async fn spatial_unregister_scope(\n    window: tauri::Window,\n    state: State<'_, AppState>,\n    key: SpatialKey,\n) -> Result<(), String>;\n// IMPORTANT: must call `SpatialState::handle_unregister` BEFORE `SpatialRegistry::unregister_scope`\n// so the kernel can read the lost entry's metadata and compute fallback. This ordering is the\n// \"spatial_unregister_scope reordered\" detail referenced in the dynamic-lifecycle card.\n\n#[tauri::command]\npub async fn spatial_update_rect(\n    state: State<'_, AppState>,\n    key: SpatialKey,\n    rect: Rect,\n) -> Result<(), String>;\n\n#[tauri::command]\npub async fn spatial_navigate(\n    window: tauri::Window,\n    state: State<'_, AppState>,\n    key: SpatialKey,\n    direction: Direction,\n) -> Result<(), String>;\n\n#[tauri::command]\npub async fn spatial_push_layer(\n    window: tauri::Window,\n    state: State<'_, AppState>,\n    key: LayerKey,\n    name: LayerName,\n    parent: Option<LayerKey>,\n) -> Result<(), String>;\n\n#[tauri::command]\npub async fn spatial_pop_layer(\n    state: State<'_, AppState>,\n    key: LayerKey,\n) -> Result<(), String>;\n```\n\n### Event emission\n\nEach command that may return a `FocusChangedEvent` from the kernel emits it via `window.emit_to(EventTarget::any(), \"focus-changed\", payload)` (or a label-targeted variant if multi-window scoping requires it). The event shape is the kernel's `FocusChangedEvent` serialized to JSON; the React side already listens for it in `SpatialFocusProvider`.\n\n### Lock ordering\n\nBoth `SpatialState` and `SpatialRegistry` are wrapped in `tokio::sync::Mutex` (matching the existing `AppState` pattern). The unregister command holds **both** locks for the duration of the transaction so observers cannot see a half-applied unregister:\n\n```rust\nlet mut registry = state.spatial_registry.lock().await;\nlet mut spatial_state = state.spatial_state.lock().await;\nlet event = spatial_state.handle_unregister(&registry, &key);\nregistry.unregister_scope(&key);\ndrop(spatial_state);\ndrop(registry);\nif let Some(event) = event {\n    emit_focus_changed(&window, &event)?;\n}\n```\n\n### Subtasks\n\n- [ ] Add `swissarmyhammer-focus` workspace dep to `kanban-app/Cargo.toml`\n- [ ] Add `spatial_state: Arc<Mutex<SpatialState>>` and `spatial_registry: Arc<Mutex<SpatialRegistry>>` to `AppState` in `kanban-app/src/state.rs`; initialize in `AppState::new()`\n- [ ] Add eight commands to `kanban-app/src/commands.rs` with the signatures above\n- [ ] Add a `emit_focus_changed` helper that serializes the kernel's `FocusChangedEvent` and emits via the window handle\n- [ ] Register all eight commands in `kanban-app/src/main.rs::run_app` `invoke_handler!`\n- [ ] Confirm `unregister` calls `handle_unregister` before `unregister_scope` (the lock ordering snippet above)\n- [ ] `cargo build -p kanban-app` succeeds; `cargo clippy -p kanban-app --all-targets -- -D warnings` clean\n- [ ] React vitest suite: spatial-focus-context tests (which mock `invoke`) still pass — no React changes are made by this card\n\n## Acceptance Criteria\n\n- [ ] `kanban-app/Cargo.toml` declares `swissarmyhammer-focus` as a path dependency\n- [ ] `AppState::spatial_state` and `AppState::spatial_registry` exist; default-initialized; thread-safe via `tokio::sync::Mutex`\n- [ ] All eight `spatial_*` commands compile, are `#[tauri::command]`, accept newtyped arguments\n- [ ] `unregister` ordering: `handle_unregister` runs **before** `unregister_scope`\n- [ ] `main.rs::run_app` registers all eight commands in `invoke_handler!`\n- [ ] `cargo build` and `cargo clippy --all-targets -- -D warnings` clean for the workspace\n- [ ] `pnpm vitest run` for `kanban-app/ui` still passes\n\n## Tests\n\n- [ ] `kanban-app/src/commands.rs` (or a sibling `tests/spatial_commands.rs`) — `spatial_focus` invokes `SpatialState::focus` under the registry lock and emits `focus-changed`\n- [ ] `register_focusable` / `register_zone` round-trip through `apply_batch`-style state changes\n- [ ] `unregister` test: register a leaf, focus it, unregister — assert `handle_unregister` was called BEFORE `unregister_scope` and a `focus-changed` event was emitted\n- [ ] `navigate` happy path: register two leaves, focus one, `navigate(Down)` returns the other's moniker\n- [ ] `cargo test -p kanban-app` passes\n- [ ] `cd kanban-app/ui && npx vitest run` passes\n\n## Workflow\n\n- This is mostly mechanical wiring — the headless kernel already has the API. Stand up the AppState fields, write the eight commands following the same pattern, register them in main.rs, and run `cargo build`. TDD on the Rust side (write a test for `spatial_unregister_scope` ordering first, then make it pass)."}],["assignees",{"kind":"set","value":["claude-code"]}],["project",{"kind":"set","value":"spatial-nav"}],["position_column",{"kind":"set","value":"todo"}],["title",{"kind":"set","value":"Tauri spatial-nav adapter foundation: wire SpatialState/SpatialRegistry into AppState, add core commands, register handlers"}],["position_ordinal",{"kind":"set","value":"ff9780"}],["depends_on",{"kind":"set","value":["01KQ2E7RPBPJ8T8KZX39N2SZ0A","01KNQXXF5W7G4JP73C6ZCMKYKX"]}]]}
{"id":"01KQ50QC08XGAYRYF69G1DXWKF","timestamp":"2026-04-26T13:47:36.840441Z","op":"update","item_id":"01KQ4YYFCGJCRN6GBYGVGXVVG6","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n - 01KNQXXF5W7G4JP73C6ZCMKYKX\n-position_column: todo\n-position_ordinal: ff9780\n+position_column: doing\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Tauri spatial-nav adapter foundation: wire SpatialState/SpatialRegistry into AppState, add core commands, register handlers'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n - 01KNQXXF5W7G4JP73C6ZCMKYKX\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: todo\n+position_ordinal: ff9780\n project: spatial-nav\n title: 'Tauri spatial-nav adapter foundation: wire SpatialState/SpatialRegistry into AppState, add core commands, register handlers'\n ---\n","transaction_id":null}
{"id":"01KQ50QC0D8M32X5F8XXPDH6FW","timestamp":"2026-04-26T13:47:36.845078Z","entity_type":"task","entity_id":"01KQ4YYFCGJCRN6GBYGVGXVVG6","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ff9780\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+ff9780\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5193T2YR59AYJS8N2KV77R","timestamp":"2026-04-26T13:57:18.274839Z","op":"update","item_id":"01KQ4YYFCGJCRN6GBYGVGXVVG6","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n - 01KNQXXF5W7G4JP73C6ZCMKYKX\n-position_column: doing\n+position_column: review\n position_ordinal: '8280'\n project: spatial-nav\n title: 'Tauri spatial-nav adapter foundation: wire SpatialState/SpatialRegistry into AppState, add core commands, register handlers'\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n - 01KNQXXF5W7G4JP73C6ZCMKYKX\n-position_column: review\n+position_column: doing\n position_ordinal: '8280'\n project: spatial-nav\n title: 'Tauri spatial-nav adapter foundation: wire SpatialState/SpatialRegistry into AppState, add core commands, register handlers'\n","transaction_id":null}
{"id":"01KQ5193T3T0Q8Z08QB34ZFC8D","timestamp":"2026-04-26T13:57:18.275487Z","entity_type":"task","entity_id":"01KQ4YYFCGJCRN6GBYGVGXVVG6","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KQ51VTCM9AA5QD31A86NDTT8","timestamp":"2026-04-26T14:07:31.220059Z","op":"update","item_id":"01KQ4YYFCGJCRN6GBYGVGXVVG6","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n - 01KNQXXF5W7G4JP73C6ZCMKYKX\n-position_column: review\n-position_ordinal: '8280'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffb680\n project: spatial-nav\n title: 'Tauri spatial-nav adapter foundation: wire SpatialState/SpatialRegistry into AppState, add core commands, register handlers'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n - 01KNQXXF5W7G4JP73C6ZCMKYKX\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffb680\n+position_column: review\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Tauri spatial-nav adapter foundation: wire SpatialState/SpatialRegistry into AppState, add core commands, register handlers'\n ---\n","transaction_id":null}
{"id":"01KQ51VTCMQM3070XDX3850PAV","timestamp":"2026-04-26T14:07:31.220942Z","entity_type":"task","entity_id":"01KQ4YYFCGJCRN6GBYGVGXVVG6","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+ffffffffffffffffffffffffb680\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffb680\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5NTG2QX0B3GQZK3XBS7HW5","timestamp":"2026-04-26T19:56:19.415901Z","op":"update","item_id":"01KQ4YYFCGJCRN6GBYGVGXVVG6","forward_patch":"--- original\n+++ modified\n@@ -5,7 +5,7 @@\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n - 01KNQXXF5W7G4JP73C6ZCMKYKX\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffb680\n+position_ordinal: ffffffffffffffffffffffffffffffffac80\n project: spatial-nav\n title: 'Tauri spatial-nav adapter foundation: wire SpatialState/SpatialRegistry into AppState, add core commands, register handlers'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -5,7 +5,7 @@\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n - 01KNQXXF5W7G4JP73C6ZCMKYKX\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffac80\n+position_ordinal: ffffffffffffffffffffffffb680\n project: spatial-nav\n title: 'Tauri spatial-nav adapter foundation: wire SpatialState/SpatialRegistry into AppState, add core commands, register handlers'\n ---\n","transaction_id":null}
{"id":"01KQ5NTG2RCY3FT48YYDXARQ42","timestamp":"2026-04-26T19:56:19.416307Z","entity_type":"task","entity_id":"01KQ4YYFCGJCRN6GBYGVGXVVG6","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffb680\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffac80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffac80\n\\ No newline at end of file\n+ffffffffffffffffffffffffb680\n\\ No newline at end of file\n"}]]}

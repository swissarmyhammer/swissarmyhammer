{"id":"01KN4C4R95BH3PV2M9AG2NJCZS","timestamp":"2026-04-01T11:16:31.653080Z","entity_type":"task","entity_id":"01KN4C4R94N21004BP3YNEAJSK","op":"create","changes":[["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\nTeach `EntityContext::write()` and `EntityContext::read()` to handle `FieldType::Attachment` fields by copying files into the entity store and deriving metadata from the stored files on read.\n\n### Storage layout\nWhen an entity has a field of `kind: attachment`, the field value in the YAML is a ULID (or list of ULIDs for `multiple: true`). The actual file is stored at:\n```\n.kanban/{entity_type}s/.attachments/{ulid}-{sanitized_original_name}\n```\ne.g., `.kanban/tasks/.attachments/01abc123-screenshot.png`\n\nThe `.attachments/` subdirectory lives inside the entity type's directory, prefixed with `.` to distinguish from entity files.\n\n### Write path (`EntityContext::write()` / `apply_validation()`)\nWhen writing an entity, for each attachment field:\n1. If the value is a **path string** (not a ULID), treat it as a source file to attach:\n   - Validate the source file exists\n   - Check file size against `max_bytes` from the field definition → error if exceeded\n   - Generate a ULID for this attachment\n   - Sanitize the original filename\n   - Copy the file to `.attachments/{ulid}-{sanitized_name}` (atomic: temp + rename)\n   - Replace the field value with the ULID\n2. If the value is already a ULID (existing attachment), leave it alone\n3. For `multiple: true`, the value is an array — process each element\n\n### Read path (`EntityContext::read()` / `apply_compute()`)\nWhen reading an entity, for each attachment field:\n1. Glob `.attachments/{ulid}-*` to find the stored file\n2. Derive metadata and return a rich JSON object:\n   ```json\n   { \"id\": \"01abc...\", \"name\": \"screenshot.png\", \"size\": 12345, \"mime_type\": \"image/png\" }\n   ```\n3. For `multiple: true`, return an array of these objects\n\n### Delete path\nWhen an attachment ULID is removed from a field (old value had it, new value doesn't), delete the stored file (or move to `.trash`).\n\n### Files to modify\n- `swissarmyhammer-entity/src/context.rs` — attachment handling in `write()` and `apply_validation()`, metadata derivation in `read()` / `apply_compute()`\n- `swissarmyhammer-entity/src/io.rs` — helper functions for attachment file I/O (copy, glob, derive metadata)\n\n## Acceptance Criteria\n- [ ] Writing a path string to an attachment field copies the file and stores the ULID\n- [ ] Reading an attachment field returns derived metadata (name, size, mime_type)\n- [ ] File size exceeding `max_bytes` → validation error before copy\n- [ ] Removing a ULID from the field deletes the stored file\n- [ ] Atomic file copy (temp + rename)\n- [ ] `multiple: true` fields handle arrays of attachments\n- [ ] Source file not found → clear error\n\n## Tests\n- [ ] Test: write entity with attachment path → file copied to `.attachments/`, field becomes ULID\n- [ ] Test: read entity with attachment ULID → returns metadata object\n- [ ] Test: write entity with file exceeding max_bytes → validation error\n- [ ] Test: update entity removing an attachment → stored file deleted\n- [ ] Test: multiple attachments — add, read, remove one\n- [ ] Run: `cargo test -p swissarmyhammer-entity` — all pass"}],["depends_on",{"kind":"set","value":["01KN4C401J0HY3FB0GBJSMD3MJ"]}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"8180"}],["title",{"kind":"set","value":"Entity layer: file storage for attachment fields"}]],"transaction_id":"01KN4C4PV2XZ6ZYVFP60XCE3A2"}
{"id":"01KN4C920R6N664JKEXH0ZHY25","timestamp":"2026-04-01T11:18:52.696533Z","entity_type":"task","entity_id":"01KN4C4R94N21004BP3YNEAJSK","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,18 @@\n Teach `EntityContext::write()` and `EntityContext::read()` to handle `FieldType::Attachment` fields by copying files into the entity store and deriving metadata from the stored files on read.\n\n ### Storage layout\n-When an entity has a field of `kind: attachment`, the field value in the YAML is a ULID (or list of ULIDs for `multiple: true`). The actual file is stored at:\n+When an entity has a field of `kind: attachment`, the field value in the YAML is the filename within `.attachments/`:\n+```yaml\n+# single\n+avatar: 01abc123-photo.jpg\n+\n+# multiple\n+attachments:\n+  - 01abc123-screenshot.png\n+  - 01def456-spec.pdf\n+```\n+\n+The actual files live at:\n ```\n .kanban/{entity_type}s/.attachments/{ulid}-{sanitized_original_name}\n ```\n@@ -13,27 +24,36 @@\n\n ### Write path (`EntityContext::write()` / `apply_validation()`)\n When writing an entity, for each attachment field:\n-1. If the value is a **path string** (not a ULID), treat it as a source file to attach:\n+1. If the value is a **path string** (not a `{ulid}-{name}` pattern), treat it as a source file to attach:\n    - Validate the source file exists\n    - Check file size against `max_bytes` from the field definition → error if exceeded\n    - Generate a ULID for this attachment\n    - Sanitize the original filename\n    - Copy the file to `.attachments/{ulid}-{sanitized_name}` (atomic: temp + rename)\n-   - Replace the field value with the ULID\n-2. If the value is already a ULID (existing attachment), leave it alone\n+   - Replace the field value with the filename (`{ulid}-{sanitized_name}`)\n+2. If the value already matches the `{ulid}-{name}` pattern (existing attachment), leave it alone\n 3. For `multiple: true`, the value is an array — process each element\n\n ### Read path (`EntityContext::read()` / `apply_compute()`)\n When reading an entity, for each attachment field:\n-1. Glob `.attachments/{ulid}-*` to find the stored file\n-2. Derive metadata and return a rich JSON object:\n+1. Resolve the stored filename to an absolute path: `{entity_dir}/.attachments/{filename}`\n+2. Stat the file for size via `fs::metadata`\n+3. Derive name (everything after first `-`), MIME type (from extension)\n+4. Return a rich JSON object with the resolved path for content access:\n    ```json\n-   { \"id\": \"01abc...\", \"name\": \"screenshot.png\", \"size\": 12345, \"mime_type\": \"image/png\" }\n+   {\n+     \"id\": \"01abc123\",\n+     \"name\": \"screenshot.png\",\n+     \"size\": 12345,\n+     \"mime_type\": \"image/png\",\n+     \"path\": \"/absolute/path/to/.kanban/tasks/.attachments/01abc123-screenshot.png\"\n+   }\n    ```\n-3. For `multiple: true`, return an array of these objects\n+5. For `multiple: true`, return an array of these objects\n+6. The `path` field gives callers the resolved filesystem location — they read/stream/serve the bytes however they want. No base64 encoding in the JSON.\n\n ### Delete path\n-When an attachment ULID is removed from a field (old value had it, new value doesn't), delete the stored file (or move to `.trash`).\n+When an attachment filename is removed from a field (old value had it, new value doesn't), delete the stored file (or move to `.trash`).\n\n ### Files to modify\n - `swissarmyhammer-entity/src/context.rs` — attachment handling in `write()` and `apply_validation()`, metadata derivation in `read()` / `apply_compute()`\n@@ -40,17 +60,18 @@\n - `swissarmyhammer-entity/src/io.rs` — helper functions for attachment file I/O (copy, glob, derive metadata)\n\n ## Acceptance Criteria\n-- [ ] Writing a path string to an attachment field copies the file and stores the ULID\n-- [ ] Reading an attachment field returns derived metadata (name, size, mime_type)\n+- [ ] Writing a path string to an attachment field copies the file and stores the filename\n+- [ ] Reading an attachment field returns metadata object with absolute `path` for content access\n - [ ] File size exceeding `max_bytes` → validation error before copy\n-- [ ] Removing a ULID from the field deletes the stored file\n+- [ ] Removing a filename from the field deletes the stored file\n - [ ] Atomic file copy (temp + rename)\n - [ ] `multiple: true` fields handle arrays of attachments\n - [ ] Source file not found → clear error\n\n ## Tests\n-- [ ] Test: write entity with attachment path → file copied to `.attachments/`, field becomes ULID\n-- [ ] Test: read entity with attachment ULID → returns metadata object\n+- [ ] Test: write entity with attachment path → file copied to `.attachments/`, field becomes `{ulid}-{name}`\n+- [ ] Test: read entity with attachment → returns metadata object including absolute `path`\n+- [ ] Test: read the file at the returned `path` → content matches original source\n - [ ] Test: write entity with file exceeding max_bytes → validation error\n - [ ] Test: update entity removing an attachment → stored file deleted\n - [ ] Test: multiple attachments — add, read, remove one\n","reverse_patch":"--- original\n+++ modified\n@@ -3,18 +3,7 @@\n Teach `EntityContext::write()` and `EntityContext::read()` to handle `FieldType::Attachment` fields by copying files into the entity store and deriving metadata from the stored files on read.\n\n ### Storage layout\n-When an entity has a field of `kind: attachment`, the field value in the YAML is the filename within `.attachments/`:\n-```yaml\n-# single\n-avatar: 01abc123-photo.jpg\n-\n-# multiple\n-attachments:\n-  - 01abc123-screenshot.png\n-  - 01def456-spec.pdf\n-```\n-\n-The actual files live at:\n+When an entity has a field of `kind: attachment`, the field value in the YAML is a ULID (or list of ULIDs for `multiple: true`). The actual file is stored at:\n ```\n .kanban/{entity_type}s/.attachments/{ulid}-{sanitized_original_name}\n ```\n@@ -24,36 +13,27 @@\n\n ### Write path (`EntityContext::write()` / `apply_validation()`)\n When writing an entity, for each attachment field:\n-1. If the value is a **path string** (not a `{ulid}-{name}` pattern), treat it as a source file to attach:\n+1. If the value is a **path string** (not a ULID), treat it as a source file to attach:\n    - Validate the source file exists\n    - Check file size against `max_bytes` from the field definition → error if exceeded\n    - Generate a ULID for this attachment\n    - Sanitize the original filename\n    - Copy the file to `.attachments/{ulid}-{sanitized_name}` (atomic: temp + rename)\n-   - Replace the field value with the filename (`{ulid}-{sanitized_name}`)\n-2. If the value already matches the `{ulid}-{name}` pattern (existing attachment), leave it alone\n+   - Replace the field value with the ULID\n+2. If the value is already a ULID (existing attachment), leave it alone\n 3. For `multiple: true`, the value is an array — process each element\n\n ### Read path (`EntityContext::read()` / `apply_compute()`)\n When reading an entity, for each attachment field:\n-1. Resolve the stored filename to an absolute path: `{entity_dir}/.attachments/{filename}`\n-2. Stat the file for size via `fs::metadata`\n-3. Derive name (everything after first `-`), MIME type (from extension)\n-4. Return a rich JSON object with the resolved path for content access:\n+1. Glob `.attachments/{ulid}-*` to find the stored file\n+2. Derive metadata and return a rich JSON object:\n    ```json\n-   {\n-     \"id\": \"01abc123\",\n-     \"name\": \"screenshot.png\",\n-     \"size\": 12345,\n-     \"mime_type\": \"image/png\",\n-     \"path\": \"/absolute/path/to/.kanban/tasks/.attachments/01abc123-screenshot.png\"\n-   }\n+   { \"id\": \"01abc...\", \"name\": \"screenshot.png\", \"size\": 12345, \"mime_type\": \"image/png\" }\n    ```\n-5. For `multiple: true`, return an array of these objects\n-6. The `path` field gives callers the resolved filesystem location — they read/stream/serve the bytes however they want. No base64 encoding in the JSON.\n+3. For `multiple: true`, return an array of these objects\n\n ### Delete path\n-When an attachment filename is removed from a field (old value had it, new value doesn't), delete the stored file (or move to `.trash`).\n+When an attachment ULID is removed from a field (old value had it, new value doesn't), delete the stored file (or move to `.trash`).\n\n ### Files to modify\n - `swissarmyhammer-entity/src/context.rs` — attachment handling in `write()` and `apply_validation()`, metadata derivation in `read()` / `apply_compute()`\n@@ -60,18 +40,17 @@\n - `swissarmyhammer-entity/src/io.rs` — helper functions for attachment file I/O (copy, glob, derive metadata)\n\n ## Acceptance Criteria\n-- [ ] Writing a path string to an attachment field copies the file and stores the filename\n-- [ ] Reading an attachment field returns metadata object with absolute `path` for content access\n+- [ ] Writing a path string to an attachment field copies the file and stores the ULID\n+- [ ] Reading an attachment field returns derived metadata (name, size, mime_type)\n - [ ] File size exceeding `max_bytes` → validation error before copy\n-- [ ] Removing a filename from the field deletes the stored file\n+- [ ] Removing a ULID from the field deletes the stored file\n - [ ] Atomic file copy (temp + rename)\n - [ ] `multiple: true` fields handle arrays of attachments\n - [ ] Source file not found → clear error\n\n ## Tests\n-- [ ] Test: write entity with attachment path → file copied to `.attachments/`, field becomes `{ulid}-{name}`\n-- [ ] Test: read entity with attachment → returns metadata object including absolute `path`\n-- [ ] Test: read the file at the returned `path` → content matches original source\n+- [ ] Test: write entity with attachment path → file copied to `.attachments/`, field becomes ULID\n+- [ ] Test: read entity with attachment ULID → returns metadata object\n - [ ] Test: write entity with file exceeding max_bytes → validation error\n - [ ] Test: update entity removing an attachment → stored file deleted\n - [ ] Test: multiple attachments — add, read, remove one\n"}]],"transaction_id":"01KN4C920C6SQTKZC2164CK183"}
{"id":"01KN4CS8HG6A2ZECB0Y21F5FDC","timestamp":"2026-04-01T11:27:43.664419Z","entity_type":"task","entity_id":"01KN4C4R94N21004BP3YNEAJSK","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,6 @@\n ## What\n\n-Teach `EntityContext::write()` and `EntityContext::read()` to handle `FieldType::Attachment` fields by copying files into the entity store and deriving metadata from the stored files on read.\n+Teach `EntityContext::write()`, `EntityContext::read()`, and `EntityContext::delete()` to handle `FieldType::Attachment` fields by copying files into the entity store, deriving metadata on read, and trashing attachment files on entity delete.\n\n ### Storage layout\n When an entity has a field of `kind: attachment`, the field value in the YAML is the filename within `.attachments/`:\n@@ -22,9 +22,11 @@\n\n The `.attachments/` subdirectory lives inside the entity type's directory, prefixed with `.` to distinguish from entity files.\n\n-### Write path (`EntityContext::write()` / `apply_validation()`)\n-When writing an entity, for each attachment field:\n-1. If the value is a **path string** (not a `{ulid}-{name}` pattern), treat it as a source file to attach:\n+### Write path (`apply_validation()`)\n+New infrastructure — `apply_validation` currently only branches on `Computed`. This adds a second type-specific branch for `Attachment`:\n+\n+For each attachment field:\n+1. If the value is a **path string** pointing to a file that exists on the filesystem, treat it as a source file to attach:\n    - Validate the source file exists\n    - Check file size against `max_bytes` from the field definition → error if exceeded\n    - Generate a ULID for this attachment\n@@ -31,11 +33,14 @@\n    - Sanitize the original filename\n    - Copy the file to `.attachments/{ulid}-{sanitized_name}` (atomic: temp + rename)\n    - Replace the field value with the filename (`{ulid}-{sanitized_name}`)\n-2. If the value already matches the `{ulid}-{name}` pattern (existing attachment), leave it alone\n+2. If the value is a string that matches a file already in `.attachments/`, leave it alone (existing attachment). **Detection**: check if `.attachments/{value}` exists — don't rely on pattern matching the ULID format.\n 3. For `multiple: true`, the value is an array — process each element\n+4. When updating: diff old vs new attachment lists. Any filenames present in old but absent in new → move the attachment file to `.attachments/.trash/`\n+\n+### Read path (`apply_compute()`)\n+New infrastructure — `apply_compute` currently only handles `Computed`. This adds a branch for `Attachment`:\n\n-### Read path (`EntityContext::read()` / `apply_compute()`)\n-When reading an entity, for each attachment field:\n+For each attachment field:\n 1. Resolve the stored filename to an absolute path: `{entity_dir}/.attachments/{filename}`\n 2. Stat the file for size via `fs::metadata`\n 3. Derive name (everything after first `-`), MIME type (from extension)\n@@ -52,18 +57,22 @@\n 5. For `multiple: true`, return an array of these objects\n 6. The `path` field gives callers the resolved filesystem location — they read/stream/serve the bytes however they want. No base64 encoding in the JSON.\n\n-### Delete path\n-When an attachment filename is removed from a field (old value had it, new value doesn't), delete the stored file (or move to `.trash`).\n+### Delete path (`EntityContext::delete()`)\n+New infrastructure — `delete()` currently just trashes the YAML + changelog. Add attachment awareness:\n+\n+When deleting an entity, check its fields for `FieldType::Attachment`. For each attachment filename found, move the file to `.attachments/.trash/` (not hard-delete — matches entity trash semantics). This ensures entity delete cascades to attachment file cleanup without leaking blobs.\n\n ### Files to modify\n-- `swissarmyhammer-entity/src/context.rs` — attachment handling in `write()` and `apply_validation()`, metadata derivation in `read()` / `apply_compute()`\n-- `swissarmyhammer-entity/src/io.rs` — helper functions for attachment file I/O (copy, glob, derive metadata)\n+- `swissarmyhammer-entity/src/context.rs` — attachment handling in `apply_validation()`, metadata derivation in `apply_compute()`, cascade trash in `delete()`\n+- `swissarmyhammer-entity/src/io.rs` — helper functions for attachment file I/O (copy, trash, stat, derive metadata)\n\n ## Acceptance Criteria\n-- [ ] Writing a path string to an attachment field copies the file and stores the filename\n+- [ ] Writing a filesystem path to an attachment field copies the file and stores the filename\n+- [ ] Writing a filename that already exists in `.attachments/` leaves it alone\n - [ ] Reading an attachment field returns metadata object with absolute `path` for content access\n - [ ] File size exceeding `max_bytes` → validation error before copy\n-- [ ] Removing a filename from the field deletes the stored file\n+- [ ] Removing a filename from the field moves the stored file to `.attachments/.trash/`\n+- [ ] Deleting an entity with attachment fields moves all attachment files to `.attachments/.trash/`\n - [ ] Atomic file copy (temp + rename)\n - [ ] `multiple: true` fields handle arrays of attachments\n - [ ] Source file not found → clear error\n@@ -70,9 +79,11 @@\n\n ## Tests\n - [ ] Test: write entity with attachment path → file copied to `.attachments/`, field becomes `{ulid}-{name}`\n+- [ ] Test: write entity with existing attachment filename → file untouched, no re-copy\n - [ ] Test: read entity with attachment → returns metadata object including absolute `path`\n - [ ] Test: read the file at the returned `path` → content matches original source\n - [ ] Test: write entity with file exceeding max_bytes → validation error\n-- [ ] Test: update entity removing an attachment → stored file deleted\n+- [ ] Test: update entity removing an attachment → stored file moved to `.attachments/.trash/`\n+- [ ] Test: delete entity with attachments → attachment files moved to `.attachments/.trash/`\n - [ ] Test: multiple attachments — add, read, remove one\n - [ ] Run: `cargo test -p swissarmyhammer-entity` — all pass\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,6 +1,6 @@\n ## What\n\n-Teach `EntityContext::write()`, `EntityContext::read()`, and `EntityContext::delete()` to handle `FieldType::Attachment` fields by copying files into the entity store, deriving metadata on read, and trashing attachment files on entity delete.\n+Teach `EntityContext::write()` and `EntityContext::read()` to handle `FieldType::Attachment` fields by copying files into the entity store and deriving metadata from the stored files on read.\n\n ### Storage layout\n When an entity has a field of `kind: attachment`, the field value in the YAML is the filename within `.attachments/`:\n@@ -22,11 +22,9 @@\n\n The `.attachments/` subdirectory lives inside the entity type's directory, prefixed with `.` to distinguish from entity files.\n\n-### Write path (`apply_validation()`)\n-New infrastructure — `apply_validation` currently only branches on `Computed`. This adds a second type-specific branch for `Attachment`:\n-\n-For each attachment field:\n-1. If the value is a **path string** pointing to a file that exists on the filesystem, treat it as a source file to attach:\n+### Write path (`EntityContext::write()` / `apply_validation()`)\n+When writing an entity, for each attachment field:\n+1. If the value is a **path string** (not a `{ulid}-{name}` pattern), treat it as a source file to attach:\n    - Validate the source file exists\n    - Check file size against `max_bytes` from the field definition → error if exceeded\n    - Generate a ULID for this attachment\n@@ -33,14 +31,11 @@\n    - Sanitize the original filename\n    - Copy the file to `.attachments/{ulid}-{sanitized_name}` (atomic: temp + rename)\n    - Replace the field value with the filename (`{ulid}-{sanitized_name}`)\n-2. If the value is a string that matches a file already in `.attachments/`, leave it alone (existing attachment). **Detection**: check if `.attachments/{value}` exists — don't rely on pattern matching the ULID format.\n+2. If the value already matches the `{ulid}-{name}` pattern (existing attachment), leave it alone\n 3. For `multiple: true`, the value is an array — process each element\n-4. When updating: diff old vs new attachment lists. Any filenames present in old but absent in new → move the attachment file to `.attachments/.trash/`\n-\n-### Read path (`apply_compute()`)\n-New infrastructure — `apply_compute` currently only handles `Computed`. This adds a branch for `Attachment`:\n\n-For each attachment field:\n+### Read path (`EntityContext::read()` / `apply_compute()`)\n+When reading an entity, for each attachment field:\n 1. Resolve the stored filename to an absolute path: `{entity_dir}/.attachments/{filename}`\n 2. Stat the file for size via `fs::metadata`\n 3. Derive name (everything after first `-`), MIME type (from extension)\n@@ -57,22 +52,18 @@\n 5. For `multiple: true`, return an array of these objects\n 6. The `path` field gives callers the resolved filesystem location — they read/stream/serve the bytes however they want. No base64 encoding in the JSON.\n\n-### Delete path (`EntityContext::delete()`)\n-New infrastructure — `delete()` currently just trashes the YAML + changelog. Add attachment awareness:\n-\n-When deleting an entity, check its fields for `FieldType::Attachment`. For each attachment filename found, move the file to `.attachments/.trash/` (not hard-delete — matches entity trash semantics). This ensures entity delete cascades to attachment file cleanup without leaking blobs.\n+### Delete path\n+When an attachment filename is removed from a field (old value had it, new value doesn't), delete the stored file (or move to `.trash`).\n\n ### Files to modify\n-- `swissarmyhammer-entity/src/context.rs` — attachment handling in `apply_validation()`, metadata derivation in `apply_compute()`, cascade trash in `delete()`\n-- `swissarmyhammer-entity/src/io.rs` — helper functions for attachment file I/O (copy, trash, stat, derive metadata)\n+- `swissarmyhammer-entity/src/context.rs` — attachment handling in `write()` and `apply_validation()`, metadata derivation in `read()` / `apply_compute()`\n+- `swissarmyhammer-entity/src/io.rs` — helper functions for attachment file I/O (copy, glob, derive metadata)\n\n ## Acceptance Criteria\n-- [ ] Writing a filesystem path to an attachment field copies the file and stores the filename\n-- [ ] Writing a filename that already exists in `.attachments/` leaves it alone\n+- [ ] Writing a path string to an attachment field copies the file and stores the filename\n - [ ] Reading an attachment field returns metadata object with absolute `path` for content access\n - [ ] File size exceeding `max_bytes` → validation error before copy\n-- [ ] Removing a filename from the field moves the stored file to `.attachments/.trash/`\n-- [ ] Deleting an entity with attachment fields moves all attachment files to `.attachments/.trash/`\n+- [ ] Removing a filename from the field deletes the stored file\n - [ ] Atomic file copy (temp + rename)\n - [ ] `multiple: true` fields handle arrays of attachments\n - [ ] Source file not found → clear error\n@@ -79,11 +70,9 @@\n\n ## Tests\n - [ ] Test: write entity with attachment path → file copied to `.attachments/`, field becomes `{ulid}-{name}`\n-- [ ] Test: write entity with existing attachment filename → file untouched, no re-copy\n - [ ] Test: read entity with attachment → returns metadata object including absolute `path`\n - [ ] Test: read the file at the returned `path` → content matches original source\n - [ ] Test: write entity with file exceeding max_bytes → validation error\n-- [ ] Test: update entity removing an attachment → stored file moved to `.attachments/.trash/`\n-- [ ] Test: delete entity with attachments → attachment files moved to `.attachments/.trash/`\n+- [ ] Test: update entity removing an attachment → stored file deleted\n - [ ] Test: multiple attachments — add, read, remove one\n - [ ] Run: `cargo test -p swissarmyhammer-entity` — all pass\n\\ No newline at end of file\n"}]],"transaction_id":"01KN4CS8H5X0CPN4T7CEBAX09D"}
{"id":"01KN4CZWR231KMZ5PZYSREGXH1","timestamp":"2026-04-01T11:31:20.962894Z","entity_type":"task","entity_id":"01KN4C4R94N21004BP3YNEAJSK","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]],"transaction_id":"01KN4CZV9ASGSQM7H5R46VZK5P"}
{"id":"01KN4DQ3CGB38WEB30BS46WF9N","timestamp":"2026-04-01T11:44:01.424460Z","entity_type":"task","entity_id":"01KN4C4R94N21004BP3YNEAJSK","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffffffb180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffb180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]],"transaction_id":"01KN4DQ1X1ZS9H1B3HJ6BZ329T"}
{"id":"01KQ5NTCX9PMWS1GG4K9SXAA4T","timestamp":"2026-04-26T19:56:16.169430Z","op":"update","item_id":"01KN4C4R94N21004BP3YNEAJSK","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KN4C401J0HY3FB0GBJSMD3MJ\n position_column: done\n-position_ordinal: ffffffffffffffffb180\n+position_ordinal: ffffffffffffffffffa380\n title: 'Entity layer: file storage for attachment fields'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KN4C401J0HY3FB0GBJSMD3MJ\n position_column: done\n-position_ordinal: ffffffffffffffffffa380\n+position_ordinal: ffffffffffffffffb180\n title: 'Entity layer: file storage for attachment fields'\n ---\n ## What\n","transaction_id":null}
{"id":"01KQ5NTCX9HJKA22FQQH0D9Y5Q","timestamp":"2026-04-26T19:56:16.169753Z","entity_type":"task","entity_id":"01KN4C4R94N21004BP3YNEAJSK","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffb180\n\\ No newline at end of file\n+ffffffffffffffffffa380\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffa380\n\\ No newline at end of file\n+ffffffffffffffffb180\n\\ No newline at end of file\n"}]]}

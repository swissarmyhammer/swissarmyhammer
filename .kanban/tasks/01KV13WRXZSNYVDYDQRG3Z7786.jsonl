{"id":"01KV13WRXZQWC45CMBE7FHPF8Y","timestamp":"2026-06-13T18:28:49.215598Z","op":"create","item_id":"01KV13WRXZSNYVDYDQRG3Z7786","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,40 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '9e80'\n+project: local-review\n+title: 'fix(llama-agent): wire pin-on-save through the prime path so criterion-3 born-pinned save is structural, not budget-only'\n+---\n+## What\n+\n+Close the prime→pin eviction race STRUCTURALLY (atomically), not just by budget headroom.\n+\n+Today (after task 01KV0KFAD6V4E9HR9BZ9QMPZ3T) the race is closed only by the RAM-scaled byte budget (`max(2 GiB, total/4)`) plus the 64-entry floor so `evict()` does not fire during priming. That makes the race improbable on a high-RAM box but NOT impossible: on a constrained box (RAM clamped to the 2 GiB floor + large per-entry KV blobs) enough concurrent saves during priming can still push `cur_bytes() > max_bytes` and evict a not-yet-pinned prefix before its pin lands — the exact `failed to pin primed prefix state` failure.\n+\n+The atomic born-pinned mechanism ALREADY EXISTS as the spec/seam: `crates/llama-agent/src/queue.rs` `insert_pinned` → `insert_inner(.., pin_on_save = true)` makes an entry born pinned so it is never an eviction candidate from the moment its bytes land. It is `#[cfg(test)]`-only today, exercised by the two store-level race tests (`pin_on_save_survives_concurrent_eviction_race`, `post_save_pin_loses_the_race_without_pin_on_save`). Those tests are the spec for this follow-up — keep them.\n+\n+## Why it is a cross-crate change\n+\n+The prime is two round-trips and the save site does not know the caller intends to pin:\n+- `prime_validator_prefix` (crates/swissarmyhammer-validators/src/review/fleet.rs) calls `pool.submit_primed(prefix)`; the prime turn's generation-path finalize-save happens INSIDE the queue worker DURING that turn.\n+- The pin is a SEPARATE `session/pin` ACP extension call made AFTER the turn completes (`pool.pin_session_scoped`).\n+\n+So the worker's finalize-save currently passes `pin_on_save = false` (the sole production `insert_inner` caller is `insert`). Threading a \"pin on save\" intent requires a cross-crate change:\n+`run_validator_fleet`/`prime_validator_prefix` (validators) → `submit_primed` carries a pin-on-save intent → ACP `session/prompt` (or a new `session_fork` ext field) in `agent-client-protocol-extras` → queue worker finalize-save → `insert_inner(.., pin_on_save = true)`.\n+\n+## Acceptance Criteria\n+\n+- [ ] A pin requested for a just-saved primed prefix CANNOT be defeated by another session's concurrent save, structurally (born-pinned at save time), independent of the byte/entry budget — i.e. the race is closed even when the budget is clamped to the 2 GiB / 64-entry floor and per-entry blobs are large.\n+- [ ] The production prime path (`prime_validator_prefix` → `submit_primed` → ACP → queue worker finalize-save) reaches `insert_inner(.., pin_on_save = true)`; `insert_pinned` (or an equivalent born-pinned save) is no longer `#[cfg(test)]`-only dead code on the production path.\n+- [ ] The two store-level race tests (`pin_on_save_survives_concurrent_eviction_race`, `post_save_pin_loses_the_race_without_pin_on_save`) still pass and are joined by an end-to-end fleet test (scripted agent, no real model, <10s) asserting the primed prefix is born pinned through the production path.\n+- [ ] When pin-on-save lands, the separate post-turn `session/pin` confirm in `prime_validator_prefix` can be simplified (idempotent re-pin or status-only confirm) — do not leave two competing pin protocols.\n+\n+## Workflow\n+- Use `/tdd`. Extend the existing born-pinned store tests; add a fleet-level test through the scripted agent. No real model. Unit tests <10s.\n+\n+## Context / Seam\n+- Born-pinned seam: `crates/llama-agent/src/queue.rs` `insert_inner` `pin_on_save` param + `insert_pinned`.\n+- Prime path: `crates/swissarmyhammer-validators/src/review/fleet.rs` `prime_validator_prefix`.\n+- ACP fork/pin contract: `crates/agent-client-protocol-extras` (`session_fork`).\n+- Predecessor task (budget-headroom mitigation that this completes): 01KV0KFAD6V4E9HR9BZ9QMPZ3T.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,40 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '9e80'\n-project: local-review\n-title: 'fix(llama-agent): wire pin-on-save through the prime path so criterion-3 born-pinned save is structural, not budget-only'\n----\n-## What\n-\n-Close the prime→pin eviction race STRUCTURALLY (atomically), not just by budget headroom.\n-\n-Today (after task 01KV0KFAD6V4E9HR9BZ9QMPZ3T) the race is closed only by the RAM-scaled byte budget (`max(2 GiB, total/4)`) plus the 64-entry floor so `evict()` does not fire during priming. That makes the race improbable on a high-RAM box but NOT impossible: on a constrained box (RAM clamped to the 2 GiB floor + large per-entry KV blobs) enough concurrent saves during priming can still push `cur_bytes() > max_bytes` and evict a not-yet-pinned prefix before its pin lands — the exact `failed to pin primed prefix state` failure.\n-\n-The atomic born-pinned mechanism ALREADY EXISTS as the spec/seam: `crates/llama-agent/src/queue.rs` `insert_pinned` → `insert_inner(.., pin_on_save = true)` makes an entry born pinned so it is never an eviction candidate from the moment its bytes land. It is `#[cfg(test)]`-only today, exercised by the two store-level race tests (`pin_on_save_survives_concurrent_eviction_race`, `post_save_pin_loses_the_race_without_pin_on_save`). Those tests are the spec for this follow-up — keep them.\n-\n-## Why it is a cross-crate change\n-\n-The prime is two round-trips and the save site does not know the caller intends to pin:\n-- `prime_validator_prefix` (crates/swissarmyhammer-validators/src/review/fleet.rs) calls `pool.submit_primed(prefix)`; the prime turn's generation-path finalize-save happens INSIDE the queue worker DURING that turn.\n-- The pin is a SEPARATE `session/pin` ACP extension call made AFTER the turn completes (`pool.pin_session_scoped`).\n-\n-So the worker's finalize-save currently passes `pin_on_save = false` (the sole production `insert_inner` caller is `insert`). Threading a \"pin on save\" intent requires a cross-crate change:\n-`run_validator_fleet`/`prime_validator_prefix` (validators) → `submit_primed` carries a pin-on-save intent → ACP `session/prompt` (or a new `session_fork` ext field) in `agent-client-protocol-extras` → queue worker finalize-save → `insert_inner(.., pin_on_save = true)`.\n-\n-## Acceptance Criteria\n-\n-- [ ] A pin requested for a just-saved primed prefix CANNOT be defeated by another session's concurrent save, structurally (born-pinned at save time), independent of the byte/entry budget — i.e. the race is closed even when the budget is clamped to the 2 GiB / 64-entry floor and per-entry blobs are large.\n-- [ ] The production prime path (`prime_validator_prefix` → `submit_primed` → ACP → queue worker finalize-save) reaches `insert_inner(.., pin_on_save = true)`; `insert_pinned` (or an equivalent born-pinned save) is no longer `#[cfg(test)]`-only dead code on the production path.\n-- [ ] The two store-level race tests (`pin_on_save_survives_concurrent_eviction_race`, `post_save_pin_loses_the_race_without_pin_on_save`) still pass and are joined by an end-to-end fleet test (scripted agent, no real model, <10s) asserting the primed prefix is born pinned through the production path.\n-- [ ] When pin-on-save lands, the separate post-turn `session/pin` confirm in `prime_validator_prefix` can be simplified (idempotent re-pin or status-only confirm) — do not leave two competing pin protocols.\n-\n-## Workflow\n-- Use `/tdd`. Extend the existing born-pinned store tests; add a fleet-level test through the scripted agent. No real model. Unit tests <10s.\n-\n-## Context / Seam\n-- Born-pinned seam: `crates/llama-agent/src/queue.rs` `insert_inner` `pin_on_save` param + `insert_pinned`.\n-- Prime path: `crates/swissarmyhammer-validators/src/review/fleet.rs` `prime_validator_prefix`.\n-- ACP fork/pin contract: `crates/agent-client-protocol-extras` (`session_fork`).\n-- Predecessor task (budget-headroom mitigation that this completes): 01KV0KFAD6V4E9HR9BZ9QMPZ3T.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KV17XM60TTQGAAJH9YBWCYKT","timestamp":"2026-06-13T19:39:11.424089Z","op":"update","item_id":"01KV13WRXZSNYVDYDQRG3Z7786","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: '9e80'\n+position_column: doing\n+position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): wire pin-on-save through the prime path so criterion-3 born-pinned save is structural, not budget-only'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '9e80'\n project: local-review\n title: 'fix(llama-agent): wire pin-on-save through the prime path so criterion-3 born-pinned save is structural, not budget-only'\n ---\n","transaction_id":null}
{"id":"01KV18ZSER09H8Q2BDV21BY6P9","timestamp":"2026-06-13T19:57:50.936148Z","op":"update","item_id":"01KV13WRXZSNYVDYDQRG3Z7786","forward_patch":"--- original\n+++ modified\n@@ -25,10 +25,23 @@\n\n ## Acceptance Criteria\n\n-- [ ] A pin requested for a just-saved primed prefix CANNOT be defeated by another session's concurrent save, structurally (born-pinned at save time), independent of the byte/entry budget — i.e. the race is closed even when the budget is clamped to the 2 GiB / 64-entry floor and per-entry blobs are large.\n-- [ ] The production prime path (`prime_validator_prefix` → `submit_primed` → ACP → queue worker finalize-save) reaches `insert_inner(.., pin_on_save = true)`; `insert_pinned` (or an equivalent born-pinned save) is no longer `#[cfg(test)]`-only dead code on the production path.\n-- [ ] The two store-level race tests (`pin_on_save_survives_concurrent_eviction_race`, `post_save_pin_loses_the_race_without_pin_on_save`) still pass and are joined by an end-to-end fleet test (scripted agent, no real model, <10s) asserting the primed prefix is born pinned through the production path.\n-- [ ] When pin-on-save lands, the separate post-turn `session/pin` confirm in `prime_validator_prefix` can be simplified (idempotent re-pin or status-only confirm) — do not leave two competing pin protocols.\n+- [x] A pin requested for a just-saved primed prefix CANNOT be defeated by another session's concurrent save, structurally (born-pinned at save time), independent of the byte/entry budget — i.e. the race is closed even when the budget is clamped to the 2 GiB / 64-entry floor and per-entry blobs are large.\n+- [x] The production prime path (`prime_validator_prefix` → `submit_primed` → ACP → queue worker finalize-save) reaches `insert_inner(.., pin_on_save = true)`; `insert_pinned` (or an equivalent born-pinned save) is no longer `#[cfg(test)]`-only dead code on the production path.\n+- [x] The two store-level race tests (`pin_on_save_survives_concurrent_eviction_race`, `post_save_pin_loses_the_race_without_pin_on_save`) still pass and are joined by an end-to-end fleet test (scripted agent, no real model, <10s) asserting the primed prefix is born pinned through the production path.\n+- [x] When pin-on-save lands, the separate post-turn `session/pin` confirm in `prime_validator_prefix` can be simplified (idempotent re-pin or status-only confirm) — do not leave two competing pin protocols.\n+\n+## Resolution\n+\n+Threaded a \"pin on save\" intent over ACP `_meta` (mirroring `MAX_TOKENS_META_KEY`), not a new `session_fork` ext field — the prime is a normal `session/prompt` turn whose save happens inside the queue worker during that turn, so the `_meta` channel is the consistent place. Chain:\n+\n+- `agent-client-protocol-extras/src/lib.rs`: new `PIN_ON_SAVE_META_KEY` wire constant.\n+- validators `pool.rs`: `submit_primed` jobs carry `pin_on_save: true` (plain `submit`/`submit_forked` carry `false`); `run_prompt` sets the `_meta` key on the prime turn only.\n+- llama-agent `acp/server.rs`: `extract_request_pin_on_save(meta)` → `GenerationRequest.pin_on_save` (new field).\n+- llama-agent `queue.rs`: streaming prompt-boundary save threads `request.pin_on_save` into `save_prompt_boundary_state` → `insert_inner(.., pin_on_save)` — the born-pinned path is now production-reachable.\n+- fleet.rs `prime_validator_prefix`: doc + log clarified that the prefix is born pinned at save; `pin_session_scoped` is now an idempotent re-pin/confirm that returns the unpin guard (one pin protocol, not two). SessionPinGuard/unpin lifecycle preserved.\n+- claude-agent unaffected: it ignores the prompt `_meta` key (pin = no-op).\n+\n+TDD: extended born-pinned store tests (kept), added `extract_request_pin_on_save` unit tests, a pool-level `_meta`-intent test, and a fleet-level born-pinned-through-production test (red-green-red verified). `cargo test -p llama-agent -p swissarmyhammer-validators -p agent-client-protocol-extras -p claude-agent` green; clippy --all-targets -D warnings clean on all four.\n\n ## Workflow\n - Use `/tdd`. Extend the existing born-pinned store tests; add a fleet-level test through the scripted agent. No real model. Unit tests <10s.\n","reverse_patch":"--- original\n+++ modified\n@@ -25,23 +25,10 @@\n\n ## Acceptance Criteria\n\n-- [x] A pin requested for a just-saved primed prefix CANNOT be defeated by another session's concurrent save, structurally (born-pinned at save time), independent of the byte/entry budget — i.e. the race is closed even when the budget is clamped to the 2 GiB / 64-entry floor and per-entry blobs are large.\n-- [x] The production prime path (`prime_validator_prefix` → `submit_primed` → ACP → queue worker finalize-save) reaches `insert_inner(.., pin_on_save = true)`; `insert_pinned` (or an equivalent born-pinned save) is no longer `#[cfg(test)]`-only dead code on the production path.\n-- [x] The two store-level race tests (`pin_on_save_survives_concurrent_eviction_race`, `post_save_pin_loses_the_race_without_pin_on_save`) still pass and are joined by an end-to-end fleet test (scripted agent, no real model, <10s) asserting the primed prefix is born pinned through the production path.\n-- [x] When pin-on-save lands, the separate post-turn `session/pin` confirm in `prime_validator_prefix` can be simplified (idempotent re-pin or status-only confirm) — do not leave two competing pin protocols.\n-\n-## Resolution\n-\n-Threaded a \"pin on save\" intent over ACP `_meta` (mirroring `MAX_TOKENS_META_KEY`), not a new `session_fork` ext field — the prime is a normal `session/prompt` turn whose save happens inside the queue worker during that turn, so the `_meta` channel is the consistent place. Chain:\n-\n-- `agent-client-protocol-extras/src/lib.rs`: new `PIN_ON_SAVE_META_KEY` wire constant.\n-- validators `pool.rs`: `submit_primed` jobs carry `pin_on_save: true` (plain `submit`/`submit_forked` carry `false`); `run_prompt` sets the `_meta` key on the prime turn only.\n-- llama-agent `acp/server.rs`: `extract_request_pin_on_save(meta)` → `GenerationRequest.pin_on_save` (new field).\n-- llama-agent `queue.rs`: streaming prompt-boundary save threads `request.pin_on_save` into `save_prompt_boundary_state` → `insert_inner(.., pin_on_save)` — the born-pinned path is now production-reachable.\n-- fleet.rs `prime_validator_prefix`: doc + log clarified that the prefix is born pinned at save; `pin_session_scoped` is now an idempotent re-pin/confirm that returns the unpin guard (one pin protocol, not two). SessionPinGuard/unpin lifecycle preserved.\n-- claude-agent unaffected: it ignores the prompt `_meta` key (pin = no-op).\n-\n-TDD: extended born-pinned store tests (kept), added `extract_request_pin_on_save` unit tests, a pool-level `_meta`-intent test, and a fleet-level born-pinned-through-production test (red-green-red verified). `cargo test -p llama-agent -p swissarmyhammer-validators -p agent-client-protocol-extras -p claude-agent` green; clippy --all-targets -D warnings clean on all four.\n+- [ ] A pin requested for a just-saved primed prefix CANNOT be defeated by another session's concurrent save, structurally (born-pinned at save time), independent of the byte/entry budget — i.e. the race is closed even when the budget is clamped to the 2 GiB / 64-entry floor and per-entry blobs are large.\n+- [ ] The production prime path (`prime_validator_prefix` → `submit_primed` → ACP → queue worker finalize-save) reaches `insert_inner(.., pin_on_save = true)`; `insert_pinned` (or an equivalent born-pinned save) is no longer `#[cfg(test)]`-only dead code on the production path.\n+- [ ] The two store-level race tests (`pin_on_save_survives_concurrent_eviction_race`, `post_save_pin_loses_the_race_without_pin_on_save`) still pass and are joined by an end-to-end fleet test (scripted agent, no real model, <10s) asserting the primed prefix is born pinned through the production path.\n+- [ ] When pin-on-save lands, the separate post-turn `session/pin` confirm in `prime_validator_prefix` can be simplified (idempotent re-pin or status-only confirm) — do not leave two competing pin protocols.\n\n ## Workflow\n - Use `/tdd`. Extend the existing born-pinned store tests; add a fleet-level test through the scripted agent. No real model. Unit tests <10s.\n","transaction_id":null}
{"id":"01KV18ZY747YJ9F2XFKD43HKQP","timestamp":"2026-06-13T19:57:55.812740Z","op":"update","item_id":"01KV13WRXZSNYVDYDQRG3Z7786","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): wire pin-on-save through the prime path so criterion-3 born-pinned save is structural, not budget-only'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): wire pin-on-save through the prime path so criterion-3 born-pinned save is structural, not budget-only'\n","transaction_id":null}
{"id":"01KV1ADE97M6ZXXSD04CZQVQ44","timestamp":"2026-06-13T20:22:46.823986Z","op":"update","item_id":"01KV13WRXZSNYVDYDQRG3Z7786","forward_patch":"--- original\n+++ modified\n@@ -50,4 +50,21 @@\n - Born-pinned seam: `crates/llama-agent/src/queue.rs` `insert_inner` `pin_on_save` param + `insert_pinned`.\n - Prime path: `crates/swissarmyhammer-validators/src/review/fleet.rs` `prime_validator_prefix`.\n - ACP fork/pin contract: `crates/agent-client-protocol-extras` (`session_fork`).\n-- Predecessor task (budget-headroom mitigation that this completes): 01KV0KFAD6V4E9HR9BZ9QMPZ3T.\n\\ No newline at end of file\n+- Predecessor task (budget-headroom mitigation that this completes): 01KV0KFAD6V4E9HR9BZ9QMPZ3T.\n+\n+## Review Findings (2026-06-13 15:04)\n+\n+> ⚠️ 7/45 review tasks failed — results are INCOMPLETE.\n+\n+### Warnings\n+- [ ] `crates/llama-agent/src/acp/server.rs:2565` — `prompt_inner` is well over 200 lines of actual code — it inlines token-budget computation, the streaming/segment-broadcast loop, tool-call extraction and execution, the runaway-loop guard, and stop-reason resolution in one body. Its length makes the agentic control flow hard to follow and impossible to unit-test piece by piece (the loop body's distinct phases can only be exercised through a full prompt turn). Decompose the loop body into named steps — e.g. `compute_turn_max_tokens(...)`, a `stream_turn(...)` that returns `(generated_text, finish_reason, turn_tokens)`, and an `execute_tool_round(...)` returning the per-step `AgenticStep` — so `prompt_inner` becomes a readable orchestration of those phases. The agentic-loop-guard logic is already factored behind `AGENTIC_LOOP_LIMITS.evaluate`; apply the same extraction to the streaming and tool-execution phases.\n+- [ ] `crates/llama-agent/src/agent.rs:992` — `generate` is ~236 lines of actual code (a single tool-calling loop with inline message bookkeeping and extensive logging), far over the ~50-line guideline. A function this long is hard to read, test in isolation, and reuse — the loop body, the assistant/tool message construction, and the tool-result formatting are independent concerns fused into one frame. Extract the per-iteration message bookkeeping into helpers — e.g. an `append_assistant_tool_round(&mut session, generated_text, &tool_results)` (the tool-result `format!`/`serde_json::to_string` formatting and the assistant+tool `messages.push` blocks) — mirroring the already-extracted `append_tool_round` used by the bounded loop, leaving `generate` as the loop skeleton.\n+- [ ] `crates/llama-agent/src/queue.rs:2089` — New `save_prompt_boundary_state` re-implements the context-state snapshot sequence already owned by the live `save_session_state` (still called on the batch path at queue.rs:1896) instead of extending it. The identical, and riskiest, block — `get_state_size()` → `vec![0u8; state_size]` → `unsafe copy_state_data` → 0-bytes-written warn+return → `truncate(bytes_written)` → lock → insert → info! — now exists in two copies. A future fix to the unsafe snapshot/zero-write handling must be made twice or silently diverges between the two save points. Extract the shared snapshot into one helper, e.g. `fn snapshot_ctx_state(worker_id, request_id, ctx: &LlamaContext) -> Option<Vec<u8>>` returning `None` on the 0-write case, and have both savers call it; then parameterize the insert with `pin_on_save` (default `false`) so `save_session_state` becomes the `pin_on_save=false` / `None`-draft case rather than a parallel copy. If `save_session_state` is intended to be retired once the batch path also moves to a prompt-boundary save, say so in a TODO so the duplication is visibly temporary.\n+- [ ] `crates/llama-agent/src/queue.rs:2197` — `process_streaming_request_sync` runs from line 2197 to 2435 with well over 50 lines of actual code (~130+ non-comment, non-blank lines). It mixes prompt rendering, context creation, KV-cache prep, tokenization, MTP detection, and a large `if use_mtp { match { Ok => if/else, Err } } else` branch — and the body contains three near-identical ~22-line `GenerationHelper::generate_stream_with_borrowed_model_and_template_offset(...)`/`save_prompt_boundary_state` call blocks (lines 2337-2362, 2369-2393, 2397-2419). A function this long with copy-pasted arms is hard to read, test in isolation, and keep the three save-closure call sites in sync. Extract the repeated standard-streaming arm into a private helper (e.g. `run_standard_stream(worker_id, &request_id, session, &mut ctx, &prompt, request, &stream_sender, cancellation_token, model_manager, template_token_count, session_state_cache, &prompt_tokens_for_save, pin_on_save)`) that builds the `save_prompt_boundary_state` closure once, and pull the MTP branch into `run_mtp_stream(...)`. `process_streaming_request_sync` then reduces to setup + `if use_mtp { run_mtp_stream(...) } else { run_standard_stream(...) }`, collapsing the three duplicated call blocks into one definition and bringing the function under ~50 lines.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:332` — `prime_validator_prefix` runs 64 lines of actual code (fleet.rs:337–414, excluding its 10 comment-only and 2 blank lines), over the ~50-line guideline. It is three independent fallible steps — submit the prime turn, confirm saved state, pin the session — each with its own error/log branch inlined, so the orchestrating control flow is buried among the per-step handling. Extract each phase into a small helper that owns its own warn-and-return — e.g. `submit_prime(pool, name) -> Option<SessionTurn>`, `confirm_saved_state(pool, name, &turn) -> Option<StateStatus>`, `pin_prefix(pool, name, &turn, &status) -> Option<SessionPinGuard>`. `prime_validator_prefix` then reads as three `?`-style steps. The verbose `tracing` calls move with their step (keep them full — do not shorten log payloads), so each helper stays small and the orchestrator drops well under 50 lines.\n+\n+### Nits\n+- [ ] `crates/llama-agent/src/queue.rs:1` — test_streaming_worker_released_after_turn hardcodes its drain poll budget inline (`for _ in 0..50` with `sleep(from_millis(10))`) while the sibling `worker_lifecycle_tests` module already names the same concept as `POLL_ATTEMPTS`/`POLL_INTERVAL` and exposes `await_queue_drained` helpers. The two will drift independently when CI timing is retuned. Reuse (or hoist to a shared test-util scope) the named poll constants / `await_queue_drained` helper instead of a second inline magic-number loop, so the wait budget tunes in one place.\n+- [ ] `crates/llama-agent/src/queue.rs:3158` — The new test's worker-drain poll loop hardcodes a retry count of 50 and a 10ms interval (a 500ms wait budget) as bare literals. These are behaviour-configuring magic numbers, and the file already establishes the opposite convention — the worker_lifecycle_tests module names exactly these knobs (POLL_ATTEMPTS, POLL_INTERVAL, SETTLE_DELAY) so a slow-CI retune is a single edit. Bare literals here drift from that convention and make the wait budget opaque. Replace `for _ in 0..50` / `from_millis(10)` with named constants (e.g. reuse or hoist POLL_ATTEMPTS / POLL_INTERVAL so the drain-wait budget is named and tuned in one place like the rest of the file's tests).\n+- [ ] `crates/llama-agent/src/validation/generation_request/session_validator.rs:35` — create_test_request() hardcodes the generation-config values max_tokens=100, temperature=0.7, and top_p=0.9 as bare literals. These are exactly the 'Configuration Values' and 'Percentages/Ratios' categories the validator targets; as unnamed literals there is no signal whether the test depends on these specific values or whether any valid request would do, and the same triple is duplicated across both copies of the tests module. Hoist to named test constants, e.g. `const TEST_MAX_TOKENS: u32 = 100; const TEST_TEMPERATURE: f32 = 0.7; const TEST_TOP_P: f32 = 0.9;`, so the fixture documents that these are arbitrary-but-valid sample values.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1` — The wait_for() poll helper encodes a ~3s timeout as two unnamed literals — the loop bound `0..300` (a retry count) and `from_millis(10)` (the interval). The effective timeout is the product of two magic numbers, so the documented '~3s' budget can only be changed by editing two coupled literals, and the relationship is not expressed in code. Name the interval and total, deriving the count: e.g. `const POLL_INTERVAL: Duration = Duration::from_millis(10); const POLL_TIMEOUT: Duration = Duration::from_secs(3);` and loop `for _ in 0..(POLL_TIMEOUT.as_millis() / POLL_INTERVAL.as_millis())`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -50,21 +50,4 @@\n - Born-pinned seam: `crates/llama-agent/src/queue.rs` `insert_inner` `pin_on_save` param + `insert_pinned`.\n - Prime path: `crates/swissarmyhammer-validators/src/review/fleet.rs` `prime_validator_prefix`.\n - ACP fork/pin contract: `crates/agent-client-protocol-extras` (`session_fork`).\n-- Predecessor task (budget-headroom mitigation that this completes): 01KV0KFAD6V4E9HR9BZ9QMPZ3T.\n-\n-## Review Findings (2026-06-13 15:04)\n-\n-> ⚠️ 7/45 review tasks failed — results are INCOMPLETE.\n-\n-### Warnings\n-- [ ] `crates/llama-agent/src/acp/server.rs:2565` — `prompt_inner` is well over 200 lines of actual code — it inlines token-budget computation, the streaming/segment-broadcast loop, tool-call extraction and execution, the runaway-loop guard, and stop-reason resolution in one body. Its length makes the agentic control flow hard to follow and impossible to unit-test piece by piece (the loop body's distinct phases can only be exercised through a full prompt turn). Decompose the loop body into named steps — e.g. `compute_turn_max_tokens(...)`, a `stream_turn(...)` that returns `(generated_text, finish_reason, turn_tokens)`, and an `execute_tool_round(...)` returning the per-step `AgenticStep` — so `prompt_inner` becomes a readable orchestration of those phases. The agentic-loop-guard logic is already factored behind `AGENTIC_LOOP_LIMITS.evaluate`; apply the same extraction to the streaming and tool-execution phases.\n-- [ ] `crates/llama-agent/src/agent.rs:992` — `generate` is ~236 lines of actual code (a single tool-calling loop with inline message bookkeeping and extensive logging), far over the ~50-line guideline. A function this long is hard to read, test in isolation, and reuse — the loop body, the assistant/tool message construction, and the tool-result formatting are independent concerns fused into one frame. Extract the per-iteration message bookkeeping into helpers — e.g. an `append_assistant_tool_round(&mut session, generated_text, &tool_results)` (the tool-result `format!`/`serde_json::to_string` formatting and the assistant+tool `messages.push` blocks) — mirroring the already-extracted `append_tool_round` used by the bounded loop, leaving `generate` as the loop skeleton.\n-- [ ] `crates/llama-agent/src/queue.rs:2089` — New `save_prompt_boundary_state` re-implements the context-state snapshot sequence already owned by the live `save_session_state` (still called on the batch path at queue.rs:1896) instead of extending it. The identical, and riskiest, block — `get_state_size()` → `vec![0u8; state_size]` → `unsafe copy_state_data` → 0-bytes-written warn+return → `truncate(bytes_written)` → lock → insert → info! — now exists in two copies. A future fix to the unsafe snapshot/zero-write handling must be made twice or silently diverges between the two save points. Extract the shared snapshot into one helper, e.g. `fn snapshot_ctx_state(worker_id, request_id, ctx: &LlamaContext) -> Option<Vec<u8>>` returning `None` on the 0-write case, and have both savers call it; then parameterize the insert with `pin_on_save` (default `false`) so `save_session_state` becomes the `pin_on_save=false` / `None`-draft case rather than a parallel copy. If `save_session_state` is intended to be retired once the batch path also moves to a prompt-boundary save, say so in a TODO so the duplication is visibly temporary.\n-- [ ] `crates/llama-agent/src/queue.rs:2197` — `process_streaming_request_sync` runs from line 2197 to 2435 with well over 50 lines of actual code (~130+ non-comment, non-blank lines). It mixes prompt rendering, context creation, KV-cache prep, tokenization, MTP detection, and a large `if use_mtp { match { Ok => if/else, Err } } else` branch — and the body contains three near-identical ~22-line `GenerationHelper::generate_stream_with_borrowed_model_and_template_offset(...)`/`save_prompt_boundary_state` call blocks (lines 2337-2362, 2369-2393, 2397-2419). A function this long with copy-pasted arms is hard to read, test in isolation, and keep the three save-closure call sites in sync. Extract the repeated standard-streaming arm into a private helper (e.g. `run_standard_stream(worker_id, &request_id, session, &mut ctx, &prompt, request, &stream_sender, cancellation_token, model_manager, template_token_count, session_state_cache, &prompt_tokens_for_save, pin_on_save)`) that builds the `save_prompt_boundary_state` closure once, and pull the MTP branch into `run_mtp_stream(...)`. `process_streaming_request_sync` then reduces to setup + `if use_mtp { run_mtp_stream(...) } else { run_standard_stream(...) }`, collapsing the three duplicated call blocks into one definition and bringing the function under ~50 lines.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:332` — `prime_validator_prefix` runs 64 lines of actual code (fleet.rs:337–414, excluding its 10 comment-only and 2 blank lines), over the ~50-line guideline. It is three independent fallible steps — submit the prime turn, confirm saved state, pin the session — each with its own error/log branch inlined, so the orchestrating control flow is buried among the per-step handling. Extract each phase into a small helper that owns its own warn-and-return — e.g. `submit_prime(pool, name) -> Option<SessionTurn>`, `confirm_saved_state(pool, name, &turn) -> Option<StateStatus>`, `pin_prefix(pool, name, &turn, &status) -> Option<SessionPinGuard>`. `prime_validator_prefix` then reads as three `?`-style steps. The verbose `tracing` calls move with their step (keep them full — do not shorten log payloads), so each helper stays small and the orchestrator drops well under 50 lines.\n-\n-### Nits\n-- [ ] `crates/llama-agent/src/queue.rs:1` — test_streaming_worker_released_after_turn hardcodes its drain poll budget inline (`for _ in 0..50` with `sleep(from_millis(10))`) while the sibling `worker_lifecycle_tests` module already names the same concept as `POLL_ATTEMPTS`/`POLL_INTERVAL` and exposes `await_queue_drained` helpers. The two will drift independently when CI timing is retuned. Reuse (or hoist to a shared test-util scope) the named poll constants / `await_queue_drained` helper instead of a second inline magic-number loop, so the wait budget tunes in one place.\n-- [ ] `crates/llama-agent/src/queue.rs:3158` — The new test's worker-drain poll loop hardcodes a retry count of 50 and a 10ms interval (a 500ms wait budget) as bare literals. These are behaviour-configuring magic numbers, and the file already establishes the opposite convention — the worker_lifecycle_tests module names exactly these knobs (POLL_ATTEMPTS, POLL_INTERVAL, SETTLE_DELAY) so a slow-CI retune is a single edit. Bare literals here drift from that convention and make the wait budget opaque. Replace `for _ in 0..50` / `from_millis(10)` with named constants (e.g. reuse or hoist POLL_ATTEMPTS / POLL_INTERVAL so the drain-wait budget is named and tuned in one place like the rest of the file's tests).\n-- [ ] `crates/llama-agent/src/validation/generation_request/session_validator.rs:35` — create_test_request() hardcodes the generation-config values max_tokens=100, temperature=0.7, and top_p=0.9 as bare literals. These are exactly the 'Configuration Values' and 'Percentages/Ratios' categories the validator targets; as unnamed literals there is no signal whether the test depends on these specific values or whether any valid request would do, and the same triple is duplicated across both copies of the tests module. Hoist to named test constants, e.g. `const TEST_MAX_TOKENS: u32 = 100; const TEST_TEMPERATURE: f32 = 0.7; const TEST_TOP_P: f32 = 0.9;`, so the fixture documents that these are arbitrary-but-valid sample values.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1` — The wait_for() poll helper encodes a ~3s timeout as two unnamed literals — the loop bound `0..300` (a retry count) and `from_millis(10)` (the interval). The effective timeout is the product of two magic numbers, so the documented '~3s' budget can only be changed by editing two coupled literals, and the relationship is not expressed in code. Name the interval and total, deriving the count: e.g. `const POLL_INTERVAL: Duration = Duration::from_millis(10); const POLL_TIMEOUT: Duration = Duration::from_secs(3);` and loop `for _ in 0..(POLL_TIMEOUT.as_millis() / POLL_INTERVAL.as_millis())`.\n\\ No newline at end of file\n+- Predecessor task (budget-headroom mitigation that this completes): 01KV0KFAD6V4E9HR9BZ9QMPZ3T.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KV1AEKZH88Y1SRVNYM52MJZR","timestamp":"2026-06-13T20:23:25.425768Z","op":"update","item_id":"01KV13WRXZSNYVDYDQRG3Z7786","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): wire pin-on-save through the prime path so criterion-3 born-pinned save is structural, not budget-only'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): wire pin-on-save through the prime path so criterion-3 born-pinned save is structural, not budget-only'\n","transaction_id":null}
{"id":"01KV1B719GJ8KD3Q1GVKJMSTMC","timestamp":"2026-06-13T20:36:45.488565Z","op":"update","item_id":"01KV13WRXZSNYVDYDQRG3Z7786","forward_patch":"--- original\n+++ modified\n@@ -57,14 +57,14 @@\n > ⚠️ 7/45 review tasks failed — results are INCOMPLETE.\n\n ### Warnings\n-- [ ] `crates/llama-agent/src/acp/server.rs:2565` — `prompt_inner` is well over 200 lines of actual code — it inlines token-budget computation, the streaming/segment-broadcast loop, tool-call extraction and execution, the runaway-loop guard, and stop-reason resolution in one body. Its length makes the agentic control flow hard to follow and impossible to unit-test piece by piece (the loop body's distinct phases can only be exercised through a full prompt turn). Decompose the loop body into named steps — e.g. `compute_turn_max_tokens(...)`, a `stream_turn(...)` that returns `(generated_text, finish_reason, turn_tokens)`, and an `execute_tool_round(...)` returning the per-step `AgenticStep` — so `prompt_inner` becomes a readable orchestration of those phases. The agentic-loop-guard logic is already factored behind `AGENTIC_LOOP_LIMITS.evaluate`; apply the same extraction to the streaming and tool-execution phases.\n-- [ ] `crates/llama-agent/src/agent.rs:992` — `generate` is ~236 lines of actual code (a single tool-calling loop with inline message bookkeeping and extensive logging), far over the ~50-line guideline. A function this long is hard to read, test in isolation, and reuse — the loop body, the assistant/tool message construction, and the tool-result formatting are independent concerns fused into one frame. Extract the per-iteration message bookkeeping into helpers — e.g. an `append_assistant_tool_round(&mut session, generated_text, &tool_results)` (the tool-result `format!`/`serde_json::to_string` formatting and the assistant+tool `messages.push` blocks) — mirroring the already-extracted `append_tool_round` used by the bounded loop, leaving `generate` as the loop skeleton.\n-- [ ] `crates/llama-agent/src/queue.rs:2089` — New `save_prompt_boundary_state` re-implements the context-state snapshot sequence already owned by the live `save_session_state` (still called on the batch path at queue.rs:1896) instead of extending it. The identical, and riskiest, block — `get_state_size()` → `vec![0u8; state_size]` → `unsafe copy_state_data` → 0-bytes-written warn+return → `truncate(bytes_written)` → lock → insert → info! — now exists in two copies. A future fix to the unsafe snapshot/zero-write handling must be made twice or silently diverges between the two save points. Extract the shared snapshot into one helper, e.g. `fn snapshot_ctx_state(worker_id, request_id, ctx: &LlamaContext) -> Option<Vec<u8>>` returning `None` on the 0-write case, and have both savers call it; then parameterize the insert with `pin_on_save` (default `false`) so `save_session_state` becomes the `pin_on_save=false` / `None`-draft case rather than a parallel copy. If `save_session_state` is intended to be retired once the batch path also moves to a prompt-boundary save, say so in a TODO so the duplication is visibly temporary.\n-- [ ] `crates/llama-agent/src/queue.rs:2197` — `process_streaming_request_sync` runs from line 2197 to 2435 with well over 50 lines of actual code (~130+ non-comment, non-blank lines). It mixes prompt rendering, context creation, KV-cache prep, tokenization, MTP detection, and a large `if use_mtp { match { Ok => if/else, Err } } else` branch — and the body contains three near-identical ~22-line `GenerationHelper::generate_stream_with_borrowed_model_and_template_offset(...)`/`save_prompt_boundary_state` call blocks (lines 2337-2362, 2369-2393, 2397-2419). A function this long with copy-pasted arms is hard to read, test in isolation, and keep the three save-closure call sites in sync. Extract the repeated standard-streaming arm into a private helper (e.g. `run_standard_stream(worker_id, &request_id, session, &mut ctx, &prompt, request, &stream_sender, cancellation_token, model_manager, template_token_count, session_state_cache, &prompt_tokens_for_save, pin_on_save)`) that builds the `save_prompt_boundary_state` closure once, and pull the MTP branch into `run_mtp_stream(...)`. `process_streaming_request_sync` then reduces to setup + `if use_mtp { run_mtp_stream(...) } else { run_standard_stream(...) }`, collapsing the three duplicated call blocks into one definition and bringing the function under ~50 lines.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:332` — `prime_validator_prefix` runs 64 lines of actual code (fleet.rs:337–414, excluding its 10 comment-only and 2 blank lines), over the ~50-line guideline. It is three independent fallible steps — submit the prime turn, confirm saved state, pin the session — each with its own error/log branch inlined, so the orchestrating control flow is buried among the per-step handling. Extract each phase into a small helper that owns its own warn-and-return — e.g. `submit_prime(pool, name) -> Option<SessionTurn>`, `confirm_saved_state(pool, name, &turn) -> Option<StateStatus>`, `pin_prefix(pool, name, &turn, &status) -> Option<SessionPinGuard>`. `prime_validator_prefix` then reads as three `?`-style steps. The verbose `tracing` calls move with their step (keep them full — do not shorten log payloads), so each helper stays small and the orchestrator drops well under 50 lines.\n+- [x] `crates/llama-agent/src/acp/server.rs:2565` — `prompt_inner` over 200 lines. RESOLVED — DEFERRED with rationale. This function is pre-existing-large; this task added exactly one line of logic (`let pin_on_save = extract_request_pin_on_save(...)`) plus one struct-field initializer. The suggested decomposition (`compute_turn_max_tokens` / `stream_turn` / `execute_tool_round`) is a large, risky refactor of agentic control flow this task did not touch — out of scope per the project's altitude norms (do not balloon a pin-on-save task into a control-flow refactor). The pin-on-save inspection IS already factored into the unit-testable `extract_request_pin_on_save`, mirroring `extract_request_max_tokens`.\n+- [x] `crates/llama-agent/src/agent.rs:992` — `generate` ~236 lines. RESOLVED — DEFERRED with rationale. This task did NOT touch `generate`'s body; the only agent.rs changes are `pin_on_save: ...` field initializers in scattered `GenerationRequest` struct literals (git diff: +10 lines, all struct-field additions). Extracting `append_assistant_tool_round` would be an unrelated refactor of a pre-existing-large function — out of scope.\n+- [x] `crates/llama-agent/src/queue.rs:2089` — `save_prompt_boundary_state` vs `save_session_state` snapshot duplication. RESOLVED — FIXED. Evaluated against the no-duplicate-but-different rule: the two savers are legitimately distinct call paths (batch post-generation vs. streaming pre-generation prompt-boundary) with distinct insert semantics (`insert` always-unpinned vs. `insert_inner(.., pin_on_save)` born-pinnable), so they are NOT collapsed. But the identical, riskiest block — the `unsafe copy_state_data` snapshot sequence (`get_state_size` → zeroed buffer → `copy_state_data` → 0-write guard → `truncate`) — WAS duplicated; extracted into a single owner `fn snapshot_ctx_state(worker_id, request_id, ctx) -> Option<Vec<u8>>` (`None` on the 0-write case). Both savers call through it, so a future fix to the unsafe handling lives in one place. Added a doc block on `save_session_state` documenting why both savers exist; dropped the now-unneeded `&mut` on its ctx.\n+- [x] `crates/llama-agent/src/queue.rs:2197` — `process_streaming_request_sync` oversized + three near-identical standard-stream arms. RESOLVED — FIXED (in scope: this task added the three `pin_on_save`-threading save closures). Extracted `fn run_standard_stream(..)` that builds the `save_prompt_boundary_state` closure ONCE; the three duplicated ~22-line blocks (non-MTP, draft-not-ready fallback, draft-create-error fallback) now collapse to single calls, so `pin_on_save` is threaded in one place and cannot drift.\n+- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:332` — `prime_validator_prefix` 64 lines / three inlined fallible phases. RESOLVED — FIXED (in scope: this task reworked this function). Extracted `submit_prime` / `confirm_saved_state` / `pin_prefix`, each owning its own warn-and-return and full `tracing` payloads; `prime_validator_prefix` is now a 3-line orchestrator.\n\n ### Nits\n-- [ ] `crates/llama-agent/src/queue.rs:1` — test_streaming_worker_released_after_turn hardcodes its drain poll budget inline (`for _ in 0..50` with `sleep(from_millis(10))`) while the sibling `worker_lifecycle_tests` module already names the same concept as `POLL_ATTEMPTS`/`POLL_INTERVAL` and exposes `await_queue_drained` helpers. The two will drift independently when CI timing is retuned. Reuse (or hoist to a shared test-util scope) the named poll constants / `await_queue_drained` helper instead of a second inline magic-number loop, so the wait budget tunes in one place.\n-- [ ] `crates/llama-agent/src/queue.rs:3158` — The new test's worker-drain poll loop hardcodes a retry count of 50 and a 10ms interval (a 500ms wait budget) as bare literals. These are behaviour-configuring magic numbers, and the file already establishes the opposite convention — the worker_lifecycle_tests module names exactly these knobs (POLL_ATTEMPTS, POLL_INTERVAL, SETTLE_DELAY) so a slow-CI retune is a single edit. Bare literals here drift from that convention and make the wait budget opaque. Replace `for _ in 0..50` / `from_millis(10)` with named constants (e.g. reuse or hoist POLL_ATTEMPTS / POLL_INTERVAL so the drain-wait budget is named and tuned in one place like the rest of the file's tests).\n-- [ ] `crates/llama-agent/src/validation/generation_request/session_validator.rs:35` — create_test_request() hardcodes the generation-config values max_tokens=100, temperature=0.7, and top_p=0.9 as bare literals. These are exactly the 'Configuration Values' and 'Percentages/Ratios' categories the validator targets; as unnamed literals there is no signal whether the test depends on these specific values or whether any valid request would do, and the same triple is duplicated across both copies of the tests module. Hoist to named test constants, e.g. `const TEST_MAX_TOKENS: u32 = 100; const TEST_TEMPERATURE: f32 = 0.7; const TEST_TOP_P: f32 = 0.9;`, so the fixture documents that these are arbitrary-but-valid sample values.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1` — The wait_for() poll helper encodes a ~3s timeout as two unnamed literals — the loop bound `0..300` (a retry count) and `from_millis(10)` (the interval). The effective timeout is the product of two magic numbers, so the documented '~3s' budget can only be changed by editing two coupled literals, and the relationship is not expressed in code. Name the interval and total, deriving the count: e.g. `const POLL_INTERVAL: Duration = Duration::from_millis(10); const POLL_TIMEOUT: Duration = Duration::from_secs(3);` and loop `for _ in 0..(POLL_TIMEOUT.as_millis() / POLL_INTERVAL.as_millis())`.\n\\ No newline at end of file\n+- [x] `crates/llama-agent/src/queue.rs:1` — inline drain poll budget in `test_streaming_worker_released_after_turn`. RESOLVED — FIXED. Added named `DRAIN_POLL_ATTEMPTS` / `DRAIN_POLL_INTERVAL` consts in the outer `tests` module (the `worker_lifecycle_tests` consts are in a nested module not visible here) and used them in the loop.\n+- [x] `crates/llama-agent/src/queue.rs:3158` — same drain-poll loop magic numbers (this anchor and :1 both point at the single `test_streaming_worker_released_after_turn` drain loop). RESOLVED — FIXED by the named `DRAIN_POLL_ATTEMPTS` / `DRAIN_POLL_INTERVAL` consts above.\n+- [x] `crates/llama-agent/src/validation/generation_request/session_validator.rs:35` — `create_test_request()` magic literals. RESOLVED — FIXED. Hoisted `TEST_MAX_TOKENS` / `TEST_TEMPERATURE` / `TEST_TOP_P` named consts documenting they are arbitrary-but-valid sample values.\n+- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:1` — `wait_for()` ~3s timeout as two coupled literals. RESOLVED — FIXED. Named `POLL_INTERVAL` (10ms) and `POLL_TIMEOUT` (3s) and derived the retry count from their ratio, so the wait budget is expressed once.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -57,14 +57,14 @@\n > ⚠️ 7/45 review tasks failed — results are INCOMPLETE.\n\n ### Warnings\n-- [x] `crates/llama-agent/src/acp/server.rs:2565` — `prompt_inner` over 200 lines. RESOLVED — DEFERRED with rationale. This function is pre-existing-large; this task added exactly one line of logic (`let pin_on_save = extract_request_pin_on_save(...)`) plus one struct-field initializer. The suggested decomposition (`compute_turn_max_tokens` / `stream_turn` / `execute_tool_round`) is a large, risky refactor of agentic control flow this task did not touch — out of scope per the project's altitude norms (do not balloon a pin-on-save task into a control-flow refactor). The pin-on-save inspection IS already factored into the unit-testable `extract_request_pin_on_save`, mirroring `extract_request_max_tokens`.\n-- [x] `crates/llama-agent/src/agent.rs:992` — `generate` ~236 lines. RESOLVED — DEFERRED with rationale. This task did NOT touch `generate`'s body; the only agent.rs changes are `pin_on_save: ...` field initializers in scattered `GenerationRequest` struct literals (git diff: +10 lines, all struct-field additions). Extracting `append_assistant_tool_round` would be an unrelated refactor of a pre-existing-large function — out of scope.\n-- [x] `crates/llama-agent/src/queue.rs:2089` — `save_prompt_boundary_state` vs `save_session_state` snapshot duplication. RESOLVED — FIXED. Evaluated against the no-duplicate-but-different rule: the two savers are legitimately distinct call paths (batch post-generation vs. streaming pre-generation prompt-boundary) with distinct insert semantics (`insert` always-unpinned vs. `insert_inner(.., pin_on_save)` born-pinnable), so they are NOT collapsed. But the identical, riskiest block — the `unsafe copy_state_data` snapshot sequence (`get_state_size` → zeroed buffer → `copy_state_data` → 0-write guard → `truncate`) — WAS duplicated; extracted into a single owner `fn snapshot_ctx_state(worker_id, request_id, ctx) -> Option<Vec<u8>>` (`None` on the 0-write case). Both savers call through it, so a future fix to the unsafe handling lives in one place. Added a doc block on `save_session_state` documenting why both savers exist; dropped the now-unneeded `&mut` on its ctx.\n-- [x] `crates/llama-agent/src/queue.rs:2197` — `process_streaming_request_sync` oversized + three near-identical standard-stream arms. RESOLVED — FIXED (in scope: this task added the three `pin_on_save`-threading save closures). Extracted `fn run_standard_stream(..)` that builds the `save_prompt_boundary_state` closure ONCE; the three duplicated ~22-line blocks (non-MTP, draft-not-ready fallback, draft-create-error fallback) now collapse to single calls, so `pin_on_save` is threaded in one place and cannot drift.\n-- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:332` — `prime_validator_prefix` 64 lines / three inlined fallible phases. RESOLVED — FIXED (in scope: this task reworked this function). Extracted `submit_prime` / `confirm_saved_state` / `pin_prefix`, each owning its own warn-and-return and full `tracing` payloads; `prime_validator_prefix` is now a 3-line orchestrator.\n+- [ ] `crates/llama-agent/src/acp/server.rs:2565` — `prompt_inner` is well over 200 lines of actual code — it inlines token-budget computation, the streaming/segment-broadcast loop, tool-call extraction and execution, the runaway-loop guard, and stop-reason resolution in one body. Its length makes the agentic control flow hard to follow and impossible to unit-test piece by piece (the loop body's distinct phases can only be exercised through a full prompt turn). Decompose the loop body into named steps — e.g. `compute_turn_max_tokens(...)`, a `stream_turn(...)` that returns `(generated_text, finish_reason, turn_tokens)`, and an `execute_tool_round(...)` returning the per-step `AgenticStep` — so `prompt_inner` becomes a readable orchestration of those phases. The agentic-loop-guard logic is already factored behind `AGENTIC_LOOP_LIMITS.evaluate`; apply the same extraction to the streaming and tool-execution phases.\n+- [ ] `crates/llama-agent/src/agent.rs:992` — `generate` is ~236 lines of actual code (a single tool-calling loop with inline message bookkeeping and extensive logging), far over the ~50-line guideline. A function this long is hard to read, test in isolation, and reuse — the loop body, the assistant/tool message construction, and the tool-result formatting are independent concerns fused into one frame. Extract the per-iteration message bookkeeping into helpers — e.g. an `append_assistant_tool_round(&mut session, generated_text, &tool_results)` (the tool-result `format!`/`serde_json::to_string` formatting and the assistant+tool `messages.push` blocks) — mirroring the already-extracted `append_tool_round` used by the bounded loop, leaving `generate` as the loop skeleton.\n+- [ ] `crates/llama-agent/src/queue.rs:2089` — New `save_prompt_boundary_state` re-implements the context-state snapshot sequence already owned by the live `save_session_state` (still called on the batch path at queue.rs:1896) instead of extending it. The identical, and riskiest, block — `get_state_size()` → `vec![0u8; state_size]` → `unsafe copy_state_data` → 0-bytes-written warn+return → `truncate(bytes_written)` → lock → insert → info! — now exists in two copies. A future fix to the unsafe snapshot/zero-write handling must be made twice or silently diverges between the two save points. Extract the shared snapshot into one helper, e.g. `fn snapshot_ctx_state(worker_id, request_id, ctx: &LlamaContext) -> Option<Vec<u8>>` returning `None` on the 0-write case, and have both savers call it; then parameterize the insert with `pin_on_save` (default `false`) so `save_session_state` becomes the `pin_on_save=false` / `None`-draft case rather than a parallel copy. If `save_session_state` is intended to be retired once the batch path also moves to a prompt-boundary save, say so in a TODO so the duplication is visibly temporary.\n+- [ ] `crates/llama-agent/src/queue.rs:2197` — `process_streaming_request_sync` runs from line 2197 to 2435 with well over 50 lines of actual code (~130+ non-comment, non-blank lines). It mixes prompt rendering, context creation, KV-cache prep, tokenization, MTP detection, and a large `if use_mtp { match { Ok => if/else, Err } } else` branch — and the body contains three near-identical ~22-line `GenerationHelper::generate_stream_with_borrowed_model_and_template_offset(...)`/`save_prompt_boundary_state` call blocks (lines 2337-2362, 2369-2393, 2397-2419). A function this long with copy-pasted arms is hard to read, test in isolation, and keep the three save-closure call sites in sync. Extract the repeated standard-streaming arm into a private helper (e.g. `run_standard_stream(worker_id, &request_id, session, &mut ctx, &prompt, request, &stream_sender, cancellation_token, model_manager, template_token_count, session_state_cache, &prompt_tokens_for_save, pin_on_save)`) that builds the `save_prompt_boundary_state` closure once, and pull the MTP branch into `run_mtp_stream(...)`. `process_streaming_request_sync` then reduces to setup + `if use_mtp { run_mtp_stream(...) } else { run_standard_stream(...) }`, collapsing the three duplicated call blocks into one definition and bringing the function under ~50 lines.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:332` — `prime_validator_prefix` runs 64 lines of actual code (fleet.rs:337–414, excluding its 10 comment-only and 2 blank lines), over the ~50-line guideline. It is three independent fallible steps — submit the prime turn, confirm saved state, pin the session — each with its own error/log branch inlined, so the orchestrating control flow is buried among the per-step handling. Extract each phase into a small helper that owns its own warn-and-return — e.g. `submit_prime(pool, name) -> Option<SessionTurn>`, `confirm_saved_state(pool, name, &turn) -> Option<StateStatus>`, `pin_prefix(pool, name, &turn, &status) -> Option<SessionPinGuard>`. `prime_validator_prefix` then reads as three `?`-style steps. The verbose `tracing` calls move with their step (keep them full — do not shorten log payloads), so each helper stays small and the orchestrator drops well under 50 lines.\n\n ### Nits\n-- [x] `crates/llama-agent/src/queue.rs:1` — inline drain poll budget in `test_streaming_worker_released_after_turn`. RESOLVED — FIXED. Added named `DRAIN_POLL_ATTEMPTS` / `DRAIN_POLL_INTERVAL` consts in the outer `tests` module (the `worker_lifecycle_tests` consts are in a nested module not visible here) and used them in the loop.\n-- [x] `crates/llama-agent/src/queue.rs:3158` — same drain-poll loop magic numbers (this anchor and :1 both point at the single `test_streaming_worker_released_after_turn` drain loop). RESOLVED — FIXED by the named `DRAIN_POLL_ATTEMPTS` / `DRAIN_POLL_INTERVAL` consts above.\n-- [x] `crates/llama-agent/src/validation/generation_request/session_validator.rs:35` — `create_test_request()` magic literals. RESOLVED — FIXED. Hoisted `TEST_MAX_TOKENS` / `TEST_TEMPERATURE` / `TEST_TOP_P` named consts documenting they are arbitrary-but-valid sample values.\n-- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:1` — `wait_for()` ~3s timeout as two coupled literals. RESOLVED — FIXED. Named `POLL_INTERVAL` (10ms) and `POLL_TIMEOUT` (3s) and derived the retry count from their ratio, so the wait budget is expressed once.\n\\ No newline at end of file\n+- [ ] `crates/llama-agent/src/queue.rs:1` — test_streaming_worker_released_after_turn hardcodes its drain poll budget inline (`for _ in 0..50` with `sleep(from_millis(10))`) while the sibling `worker_lifecycle_tests` module already names the same concept as `POLL_ATTEMPTS`/`POLL_INTERVAL` and exposes `await_queue_drained` helpers. The two will drift independently when CI timing is retuned. Reuse (or hoist to a shared test-util scope) the named poll constants / `await_queue_drained` helper instead of a second inline magic-number loop, so the wait budget tunes in one place.\n+- [ ] `crates/llama-agent/src/queue.rs:3158` — The new test's worker-drain poll loop hardcodes a retry count of 50 and a 10ms interval (a 500ms wait budget) as bare literals. These are behaviour-configuring magic numbers, and the file already establishes the opposite convention — the worker_lifecycle_tests module names exactly these knobs (POLL_ATTEMPTS, POLL_INTERVAL, SETTLE_DELAY) so a slow-CI retune is a single edit. Bare literals here drift from that convention and make the wait budget opaque. Replace `for _ in 0..50` / `from_millis(10)` with named constants (e.g. reuse or hoist POLL_ATTEMPTS / POLL_INTERVAL so the drain-wait budget is named and tuned in one place like the rest of the file's tests).\n+- [ ] `crates/llama-agent/src/validation/generation_request/session_validator.rs:35` — create_test_request() hardcodes the generation-config values max_tokens=100, temperature=0.7, and top_p=0.9 as bare literals. These are exactly the 'Configuration Values' and 'Percentages/Ratios' categories the validator targets; as unnamed literals there is no signal whether the test depends on these specific values or whether any valid request would do, and the same triple is duplicated across both copies of the tests module. Hoist to named test constants, e.g. `const TEST_MAX_TOKENS: u32 = 100; const TEST_TEMPERATURE: f32 = 0.7; const TEST_TOP_P: f32 = 0.9;`, so the fixture documents that these are arbitrary-but-valid sample values.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1` — The wait_for() poll helper encodes a ~3s timeout as two unnamed literals — the loop bound `0..300` (a retry count) and `from_millis(10)` (the interval). The effective timeout is the product of two magic numbers, so the documented '~3s' budget can only be changed by editing two coupled literals, and the relationship is not expressed in code. Name the interval and total, deriving the count: e.g. `const POLL_INTERVAL: Duration = Duration::from_millis(10); const POLL_TIMEOUT: Duration = Duration::from_secs(3);` and loop `for _ in 0..(POLL_TIMEOUT.as_millis() / POLL_INTERVAL.as_millis())`.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KV1B78HCW48SNN9DW7TK5EJP","timestamp":"2026-06-13T20:36:52.908288Z","op":"update","item_id":"01KV13WRXZSNYVDYDQRG3Z7786","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): wire pin-on-save through the prime path so criterion-3 born-pinned save is structural, not budget-only'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): wire pin-on-save through the prime path so criterion-3 born-pinned save is structural, not budget-only'\n","transaction_id":null}
{"id":"01KV1BVM7DXZFFX9Y5NFTAQK10","timestamp":"2026-06-13T20:48:00.237699Z","op":"update","item_id":"01KV13WRXZSNYVDYDQRG3Z7786","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffffa780\n project: local-review\n title: 'fix(llama-agent): wire pin-on-save through the prime path so criterion-3 born-pinned save is structural, not budget-only'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffffa780\n+position_column: review\n+position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): wire pin-on-save through the prime path so criterion-3 born-pinned save is structural, not budget-only'\n ---\n","transaction_id":null}

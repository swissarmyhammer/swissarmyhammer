{"id":"01KSJD05CN3TQM05TQ9VAGNZTB","timestamp":"2026-05-26T15:03:39.157624Z","op":"create","item_id":"01KSJD05CNB1KFD880WQBN3XJY","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,38 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '8380'\n+title: Fix `test_dispatch_execute_command_empty_op` — order-dependent, passes in isolation, fails in full crate suite\n+---\n+## What\n+\n+`crates/swissarmyhammer-tools/src/mcp/tools/shell/`'s `test_dispatch_execute_command_empty_op` passes when run alone (`cargo test -p swissarmyhammer-tools test_dispatch_execute_command_empty_op` → 1 passed) but fails when run as part of the full crate suite (`cargo test -p swissarmyhammer-tools`). This is the classic shared-global-state failure pattern: another test in the suite mutates a global that this one reads — `CWD`, an env var, a static `OnceCell` / `Mutex`, a fixed-port listener, or a shared temp file.\n+\n+Fix the underlying isolation, not the symptom. Per the project's `test-isolation-raii` rule, do **not** add production APIs to fix test env problems — use existing isolation primitives (`#[serial_test::serial(...)]`, `IsolatedTestEnvironment`, `CurrentDirGuard`, `tempfile::TempDir`) or refactor the test to own the state it depends on.\n+\n+Investigation steps for the implementer:\n+1. Locate the failing test in `crates/swissarmyhammer-tools/src/mcp/tools/shell/` (search for `test_dispatch_execute_command_empty_op`).\n+2. Identify what global state it reads (likely the working directory, an env var like `PATH`/`SHELL`, or a dispatch registry static).\n+3. Find the test(s) in the same crate that mutate that state. Common suspects: anything that does `std::env::set_var`, `set_current_dir`, or initializes a `OnceCell`.\n+4. Apply the smallest correct isolation:\n+   - If it's CWD: `CurrentDirGuard` (or the project's RAII equivalent) on the offender.\n+   - If it's env: `#[serial_test::serial(<key>)]` on every test that touches the var, with RAII restore on the mutators.\n+   - If it's a static `OnceCell` or shared resource: serialize the tests that depend on it with a shared serial-test key.\n+5. Confirm the fix is real: run the failing test inside the full crate suite repeatedly to verify deterministic green. A single passing run is not enough — run it in a loop (`for i in 1..=10; cargo test -p swissarmyhammer-tools test_dispatch_execute_command_empty_op …`) under the full-suite invocation.\n+\n+This is verified pre-existing on `b7ba81dd7`, surfaced by the `test_loop` after recent work; the file isn't in any recent diff.\n+\n+## Acceptance Criteria\n+- [ ] `cargo test -p swissarmyhammer-tools` passes 10 consecutive runs without that test failing.\n+- [ ] The isolation fix uses existing primitives (no new prod API to support tests).\n+- [ ] Test still passes in isolation (`cargo test -p swissarmyhammer-tools test_dispatch_execute_command_empty_op`).\n+- [ ] No `#[ignore]` / `#[allow]` band-aids; no `--test-threads=1` workaround.\n+- [ ] `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` clean.\n+\n+## Tests\n+- [ ] The previously-failing test passes in the full-suite invocation, repeatedly.\n+- [ ] Whichever other test mutates the shared state still passes.\n+\n+## Workflow\n+- Investigate first to find the offender; fix at the root, not the symptom.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,38 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '8380'\n-title: Fix `test_dispatch_execute_command_empty_op` — order-dependent, passes in isolation, fails in full crate suite\n----\n-## What\n-\n-`crates/swissarmyhammer-tools/src/mcp/tools/shell/`'s `test_dispatch_execute_command_empty_op` passes when run alone (`cargo test -p swissarmyhammer-tools test_dispatch_execute_command_empty_op` → 1 passed) but fails when run as part of the full crate suite (`cargo test -p swissarmyhammer-tools`). This is the classic shared-global-state failure pattern: another test in the suite mutates a global that this one reads — `CWD`, an env var, a static `OnceCell` / `Mutex`, a fixed-port listener, or a shared temp file.\n-\n-Fix the underlying isolation, not the symptom. Per the project's `test-isolation-raii` rule, do **not** add production APIs to fix test env problems — use existing isolation primitives (`#[serial_test::serial(...)]`, `IsolatedTestEnvironment`, `CurrentDirGuard`, `tempfile::TempDir`) or refactor the test to own the state it depends on.\n-\n-Investigation steps for the implementer:\n-1. Locate the failing test in `crates/swissarmyhammer-tools/src/mcp/tools/shell/` (search for `test_dispatch_execute_command_empty_op`).\n-2. Identify what global state it reads (likely the working directory, an env var like `PATH`/`SHELL`, or a dispatch registry static).\n-3. Find the test(s) in the same crate that mutate that state. Common suspects: anything that does `std::env::set_var`, `set_current_dir`, or initializes a `OnceCell`.\n-4. Apply the smallest correct isolation:\n-   - If it's CWD: `CurrentDirGuard` (or the project's RAII equivalent) on the offender.\n-   - If it's env: `#[serial_test::serial(<key>)]` on every test that touches the var, with RAII restore on the mutators.\n-   - If it's a static `OnceCell` or shared resource: serialize the tests that depend on it with a shared serial-test key.\n-5. Confirm the fix is real: run the failing test inside the full crate suite repeatedly to verify deterministic green. A single passing run is not enough — run it in a loop (`for i in 1..=10; cargo test -p swissarmyhammer-tools test_dispatch_execute_command_empty_op …`) under the full-suite invocation.\n-\n-This is verified pre-existing on `b7ba81dd7`, surfaced by the `test_loop` after recent work; the file isn't in any recent diff.\n-\n-## Acceptance Criteria\n-- [ ] `cargo test -p swissarmyhammer-tools` passes 10 consecutive runs without that test failing.\n-- [ ] The isolation fix uses existing primitives (no new prod API to support tests).\n-- [ ] Test still passes in isolation (`cargo test -p swissarmyhammer-tools test_dispatch_execute_command_empty_op`).\n-- [ ] No `#[ignore]` / `#[allow]` band-aids; no `--test-threads=1` workaround.\n-- [ ] `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` clean.\n-\n-## Tests\n-- [ ] The previously-failing test passes in the full-suite invocation, repeatedly.\n-- [ ] Whichever other test mutates the shared state still passes.\n-\n-## Workflow\n-- Investigate first to find the offender; fix at the root, not the symptom.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSJD0FD0MRHT33QSVVAVJFNB","timestamp":"2026-05-26T15:03:49.408541Z","op":"update","item_id":"01KSJD05CNB1KFD880WQBN3XJY","forward_patch":"--- original\n+++ modified\n@@ -35,4 +35,4 @@\n - [ ] Whichever other test mutates the shared state still passes.\n\n ## Workflow\n-- Investigate first to find the offender; fix at the root, not the symptom.\n\\ No newline at end of file\n+- Investigate first to find the offender; fix at the root, not the symptom. #test-failure\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -35,4 +35,4 @@\n - [ ] Whichever other test mutates the shared state still passes.\n\n ## Workflow\n-- Investigate first to find the offender; fix at the root, not the symptom. #test-failure\n\\ No newline at end of file\n+- Investigate first to find the offender; fix at the root, not the symptom.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSJF67AMJAW2W1VH2YCJQF07","timestamp":"2026-05-26T15:41:54.900719Z","op":"update","item_id":"01KSJD05CNB1KFD880WQBN3XJY","forward_patch":"--- original\n+++ modified\n@@ -3,36 +3,42 @@\n - claude-code\n position_column: todo\n position_ordinal: '8380'\n-title: Fix `test_dispatch_execute_command_empty_op` — order-dependent, passes in isolation, fails in full crate suite\n+title: Fix `swissarmyhammer-tools` test parallel isolation — rotating failures from HOME/CWD/TempDir leakage\n ---\n ## What\n\n-`crates/swissarmyhammer-tools/src/mcp/tools/shell/`'s `test_dispatch_execute_command_empty_op` passes when run alone (`cargo test -p swissarmyhammer-tools test_dispatch_execute_command_empty_op` → 1 passed) but fails when run as part of the full crate suite (`cargo test -p swissarmyhammer-tools`). This is the classic shared-global-state failure pattern: another test in the suite mutates a global that this one reads — `CWD`, an env var, a static `OnceCell` / `Mutex`, a fixed-port listener, or a shared temp file.\n+The `swissarmyhammer-tools` lib test binary has parallel-execution test pollution: **a different test rotates as the failure each run**. Across recent invocations the failing test has been:\n+\n+- `mcp::tools::shell::tests::test_dispatch_execute_command_empty_op`\n+- `mcp::tool_config::tests::test_watcher_deleted_file_reverts_to_all_enabled`\n+- `test_utils::tests::test_isolated_test_environment_drop_restores_home`\n+\n+All three pass in isolation. The pattern is classic shared-global-state racing — HOME, CWD, env vars, a `OnceCell` / `Mutex` static, or shared `TempDir` paths leaking across threads in the same test binary.\n\n-Fix the underlying isolation, not the symptom. Per the project's `test-isolation-raii` rule, do **not** add production APIs to fix test env problems — use existing isolation primitives (`#[serial_test::serial(...)]`, `IsolatedTestEnvironment`, `CurrentDirGuard`, `tempfile::TempDir`) or refactor the test to own the state it depends on.\n+Fix the underlying isolation, not the symptom. Per the project's `test-isolation-raii` rule, do **not** add production APIs to fix test env problems — use existing isolation primitives (`#[serial_test::serial(<key>)]`, `IsolatedTestEnvironment`, `CurrentDirGuard`, `tempfile::TempDir`) or refactor the tests to own the state they depend on.\n\n Investigation steps for the implementer:\n-1. Locate the failing test in `crates/swissarmyhammer-tools/src/mcp/tools/shell/` (search for `test_dispatch_execute_command_empty_op`).\n-2. Identify what global state it reads (likely the working directory, an env var like `PATH`/`SHELL`, or a dispatch registry static).\n-3. Find the test(s) in the same crate that mutate that state. Common suspects: anything that does `std::env::set_var`, `set_current_dir`, or initializes a `OnceCell`.\n+1. Locate each currently-known failing test (paths above). Read each.\n+2. Identify the shared global state (likely `HOME` env var, CWD, a `OnceCell` / static `Mutex`, a fixed-port listener, or a shared on-disk path).\n+3. Find the test(s) in the same crate that mutate that state without proper RAII restore — common suspects: anything that does `std::env::set_var`, `std::env::set_current_dir`, or initializes a `OnceCell` from a non-isolated source.\n 4. Apply the smallest correct isolation:\n-   - If it's CWD: `CurrentDirGuard` (or the project's RAII equivalent) on the offender.\n-   - If it's env: `#[serial_test::serial(<key>)]` on every test that touches the var, with RAII restore on the mutators.\n-   - If it's a static `OnceCell` or shared resource: serialize the tests that depend on it with a shared serial-test key.\n-5. Confirm the fix is real: run the failing test inside the full crate suite repeatedly to verify deterministic green. A single passing run is not enough — run it in a loop (`for i in 1..=10; cargo test -p swissarmyhammer-tools test_dispatch_execute_command_empty_op …`) under the full-suite invocation.\n+   - CWD → `CurrentDirGuard` on the mutator(s); ensure readers serialize on the same key.\n+   - HOME / env → `#[serial_test::serial(home_env)]` (or whichever key the project uses) on every test that touches the var, with RAII restore on mutators (`IsolatedTestEnvironment` does this already).\n+   - Static `OnceCell` / shared resource → serialize the tests that depend on it with a shared `serial_test` key.\n+5. Verify deterministically: run `cargo test -p swissarmyhammer-tools` 10 consecutive times under the full-suite invocation and confirm zero failures across all runs. A single passing run is not enough.\n\n-This is verified pre-existing on `b7ba81dd7`, surfaced by the `test_loop` after recent work; the file isn't in any recent diff.\n+If different tests keep rotating after each fix, that means a still-unprotected mutator exists — keep going until the full suite is reliably green.\n\n ## Acceptance Criteria\n-- [ ] `cargo test -p swissarmyhammer-tools` passes 10 consecutive runs without that test failing.\n-- [ ] The isolation fix uses existing primitives (no new prod API to support tests).\n-- [ ] Test still passes in isolation (`cargo test -p swissarmyhammer-tools test_dispatch_execute_command_empty_op`).\n+- [ ] `cargo test -p swissarmyhammer-tools` is green for 10 consecutive full-suite runs.\n+- [ ] Each previously-failing test still passes in isolation.\n+- [ ] Isolation fixes use existing primitives (no new prod API to support tests).\n - [ ] No `#[ignore]` / `#[allow]` band-aids; no `--test-threads=1` workaround.\n - [ ] `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` clean.\n\n ## Tests\n-- [ ] The previously-failing test passes in the full-suite invocation, repeatedly.\n-- [ ] Whichever other test mutates the shared state still passes.\n+- [ ] All three currently-known tests (`test_dispatch_execute_command_empty_op`, `test_watcher_deleted_file_reverts_to_all_enabled`, `test_isolated_test_environment_drop_restores_home`) pass in the full-suite invocation, repeatedly.\n+- [ ] Whichever other tests mutate the shared state still pass.\n\n ## Workflow\n-- Investigate first to find the offender; fix at the root, not the symptom. #test-failure\n\\ No newline at end of file\n+- Investigate first to find the offender(s); fix at the root, not the symptom. Document which global was racing and which tests now serialize on which key.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -3,42 +3,36 @@\n - claude-code\n position_column: todo\n position_ordinal: '8380'\n-title: Fix `swissarmyhammer-tools` test parallel isolation — rotating failures from HOME/CWD/TempDir leakage\n+title: Fix `test_dispatch_execute_command_empty_op` — order-dependent, passes in isolation, fails in full crate suite\n ---\n ## What\n\n-The `swissarmyhammer-tools` lib test binary has parallel-execution test pollution: **a different test rotates as the failure each run**. Across recent invocations the failing test has been:\n+`crates/swissarmyhammer-tools/src/mcp/tools/shell/`'s `test_dispatch_execute_command_empty_op` passes when run alone (`cargo test -p swissarmyhammer-tools test_dispatch_execute_command_empty_op` → 1 passed) but fails when run as part of the full crate suite (`cargo test -p swissarmyhammer-tools`). This is the classic shared-global-state failure pattern: another test in the suite mutates a global that this one reads — `CWD`, an env var, a static `OnceCell` / `Mutex`, a fixed-port listener, or a shared temp file.\n\n-- `mcp::tools::shell::tests::test_dispatch_execute_command_empty_op`\n-- `mcp::tool_config::tests::test_watcher_deleted_file_reverts_to_all_enabled`\n-- `test_utils::tests::test_isolated_test_environment_drop_restores_home`\n-\n-All three pass in isolation. The pattern is classic shared-global-state racing — HOME, CWD, env vars, a `OnceCell` / `Mutex` static, or shared `TempDir` paths leaking across threads in the same test binary.\n-\n-Fix the underlying isolation, not the symptom. Per the project's `test-isolation-raii` rule, do **not** add production APIs to fix test env problems — use existing isolation primitives (`#[serial_test::serial(<key>)]`, `IsolatedTestEnvironment`, `CurrentDirGuard`, `tempfile::TempDir`) or refactor the tests to own the state they depend on.\n+Fix the underlying isolation, not the symptom. Per the project's `test-isolation-raii` rule, do **not** add production APIs to fix test env problems — use existing isolation primitives (`#[serial_test::serial(...)]`, `IsolatedTestEnvironment`, `CurrentDirGuard`, `tempfile::TempDir`) or refactor the test to own the state it depends on.\n\n Investigation steps for the implementer:\n-1. Locate each currently-known failing test (paths above). Read each.\n-2. Identify the shared global state (likely `HOME` env var, CWD, a `OnceCell` / static `Mutex`, a fixed-port listener, or a shared on-disk path).\n-3. Find the test(s) in the same crate that mutate that state without proper RAII restore — common suspects: anything that does `std::env::set_var`, `std::env::set_current_dir`, or initializes a `OnceCell` from a non-isolated source.\n+1. Locate the failing test in `crates/swissarmyhammer-tools/src/mcp/tools/shell/` (search for `test_dispatch_execute_command_empty_op`).\n+2. Identify what global state it reads (likely the working directory, an env var like `PATH`/`SHELL`, or a dispatch registry static).\n+3. Find the test(s) in the same crate that mutate that state. Common suspects: anything that does `std::env::set_var`, `set_current_dir`, or initializes a `OnceCell`.\n 4. Apply the smallest correct isolation:\n-   - CWD → `CurrentDirGuard` on the mutator(s); ensure readers serialize on the same key.\n-   - HOME / env → `#[serial_test::serial(home_env)]` (or whichever key the project uses) on every test that touches the var, with RAII restore on mutators (`IsolatedTestEnvironment` does this already).\n-   - Static `OnceCell` / shared resource → serialize the tests that depend on it with a shared `serial_test` key.\n-5. Verify deterministically: run `cargo test -p swissarmyhammer-tools` 10 consecutive times under the full-suite invocation and confirm zero failures across all runs. A single passing run is not enough.\n+   - If it's CWD: `CurrentDirGuard` (or the project's RAII equivalent) on the offender.\n+   - If it's env: `#[serial_test::serial(<key>)]` on every test that touches the var, with RAII restore on the mutators.\n+   - If it's a static `OnceCell` or shared resource: serialize the tests that depend on it with a shared serial-test key.\n+5. Confirm the fix is real: run the failing test inside the full crate suite repeatedly to verify deterministic green. A single passing run is not enough — run it in a loop (`for i in 1..=10; cargo test -p swissarmyhammer-tools test_dispatch_execute_command_empty_op …`) under the full-suite invocation.\n\n-If different tests keep rotating after each fix, that means a still-unprotected mutator exists — keep going until the full suite is reliably green.\n+This is verified pre-existing on `b7ba81dd7`, surfaced by the `test_loop` after recent work; the file isn't in any recent diff.\n\n ## Acceptance Criteria\n-- [ ] `cargo test -p swissarmyhammer-tools` is green for 10 consecutive full-suite runs.\n-- [ ] Each previously-failing test still passes in isolation.\n-- [ ] Isolation fixes use existing primitives (no new prod API to support tests).\n+- [ ] `cargo test -p swissarmyhammer-tools` passes 10 consecutive runs without that test failing.\n+- [ ] The isolation fix uses existing primitives (no new prod API to support tests).\n+- [ ] Test still passes in isolation (`cargo test -p swissarmyhammer-tools test_dispatch_execute_command_empty_op`).\n - [ ] No `#[ignore]` / `#[allow]` band-aids; no `--test-threads=1` workaround.\n - [ ] `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` clean.\n\n ## Tests\n-- [ ] All three currently-known tests (`test_dispatch_execute_command_empty_op`, `test_watcher_deleted_file_reverts_to_all_enabled`, `test_isolated_test_environment_drop_restores_home`) pass in the full-suite invocation, repeatedly.\n-- [ ] Whichever other tests mutate the shared state still pass.\n+- [ ] The previously-failing test passes in the full-suite invocation, repeatedly.\n+- [ ] Whichever other test mutates the shared state still passes.\n\n ## Workflow\n-- Investigate first to find the offender(s); fix at the root, not the symptom. Document which global was racing and which tests now serialize on which key.\n\\ No newline at end of file\n+- Investigate first to find the offender; fix at the root, not the symptom. #test-failure\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSJF6QZJV6JMNBBF9GY7WXXP","timestamp":"2026-05-26T15:42:11.954154Z","op":"update","item_id":"01KSJD05CNB1KFD880WQBN3XJY","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '80'\n title: Fix `swissarmyhammer-tools` test parallel isolation — rotating failures from HOME/CWD/TempDir leakage\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '8380'\n title: Fix `swissarmyhammer-tools` test parallel isolation — rotating failures from HOME/CWD/TempDir leakage\n ---\n ## What\n","transaction_id":null}
{"id":"01KSJJE58BDG9VEPDZEBE9FH6C","timestamp":"2026-05-26T16:38:40.651704Z","op":"update","item_id":"01KSJD05CNB1KFD880WQBN3XJY","forward_patch":"--- original\n+++ modified\n@@ -15,30 +15,50 @@\n\n All three pass in isolation. The pattern is classic shared-global-state racing — HOME, CWD, env vars, a `OnceCell` / `Mutex` static, or shared `TempDir` paths leaking across threads in the same test binary.\n\n-Fix the underlying isolation, not the symptom. Per the project's `test-isolation-raii` rule, do **not** add production APIs to fix test env problems — use existing isolation primitives (`#[serial_test::serial(<key>)]`, `IsolatedTestEnvironment`, `CurrentDirGuard`, `tempfile::TempDir`) or refactor the tests to own the state they depend on.\n+## Root Cause Analysis (resolved)\n+\n+Three distinct race conditions, all read-side. None of them was a missing mutator RAII restore — the mutators were already protected; the *readers* were unprotected.\n+\n+### Race 1 — `test_dispatch_execute_command_empty_op`\n+\n+`execute_op` (in `crates/swissarmyhammer-tools/src/mcp/tools/shell/test_helpers.rs`) calls into the shell tool with no `working_directory` arg. That falls through to `prepare_working_directory(None)` in `process.rs`, which reads `std::env::current_dir()` and then checks `work_dir.exists()`. Tests like `test_health_check_project_config_valid` use `CurrentDirGuard::new(tmp.path())` which sets CWD to a `TempDir`. Between this test's `current_dir()` read and the `work_dir.exists()` check, the other test can drop its guard and delete its `TempDir`, leaving us with `WorkingDirectoryError`. `TestCommandBuilder::new` already handles this by defaulting `working_directory` to `/tmp`; the dispatch helpers did not.\n+\n+### Race 2 — `test_watcher_deleted_file_reverts_to_all_enabled`\n+\n+`ToolConfigWatcher::check_and_reload` calls `load_merged_tool_config()` on reload. That function reads the *process-global* HOME via `dirs::home_dir()` and the process-global CWD via `find_git_repository_root()` — it ignores the watcher's stored paths for the actual load (the stored paths are only used for mtime tracking). If another parallel test had set HOME to a fake home that contains a `.sah/tools.yaml` disabling shell, or CWD to a workspace whose `.sah/tools.yaml` disabled shell, the reload would pick that up and the assertion `registry.is_tool_enabled(\"shell\")` would fail.\n+\n+### Race 3 — `test_isolated_test_environment_drop_restores_home`\n+\n+The test read `HOME` *outside* of `HOME_ENV_LOCK`, then dropped an `IsolatedTestEnvironment`, then read `HOME` again. When another test was inside its own `IsolatedTestEnvironment` at the time of the first read, the test observed that other test's fake HOME, then later — after the other test had restored the *real* HOME — saw a different value and failed.\n\n-Investigation steps for the implementer:\n-1. Locate each currently-known failing test (paths above). Read each.\n-2. Identify the shared global state (likely `HOME` env var, CWD, a `OnceCell` / static `Mutex`, a fixed-port listener, or a shared on-disk path).\n-3. Find the test(s) in the same crate that mutate that state without proper RAII restore — common suspects: anything that does `std::env::set_var`, `std::env::set_current_dir`, or initializes a `OnceCell` from a non-isolated source.\n-4. Apply the smallest correct isolation:\n-   - CWD → `CurrentDirGuard` on the mutator(s); ensure readers serialize on the same key.\n-   - HOME / env → `#[serial_test::serial(home_env)]` (or whichever key the project uses) on every test that touches the var, with RAII restore on mutators (`IsolatedTestEnvironment` does this already).\n-   - Static `OnceCell` / shared resource → serialize the tests that depend on it with a shared `serial_test` key.\n-5. Verify deterministically: run `cargo test -p swissarmyhammer-tools` 10 consecutive times under the full-suite invocation and confirm zero failures across all runs. A single passing run is not enough.\n+## Fix Summary\n+\n+Smallest correct isolation, using existing primitives:\n+\n+1. **`execute_op` / `run_command_with`** (`shell/test_helpers.rs`): default `working_directory` to `/tmp` for execute-command dispatches, matching `TestCommandBuilder::new`. Removes the dependence on a stable process CWD.\n+2. **`test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled`** (`tool_config.rs`): hold an `IsolatedTestEnvironment` for the lifetime of the reload (gives HOME isolation via the existing global lock), and chdir into a clean temp dir under `#[serial_test::serial(cwd)]`.\n+3. **`test_isolated_test_environment_drop_restores_home`** (`test_utils.rs`): rewrite to atomically capture HOME under the `HOME_ENV_LOCK`, stamp a sentinel, drop the lock so `IsolatedTestEnvironment::new` can acquire it, drop the env, then re-acquire the lock to read the restored HOME. Also exposed `acquire_home_env_lock()` (test-utility, symmetric to the existing `acquire_semantic_db_lock()`).\n\n-If different tests keep rotating after each fix, that means a still-unprotected mutator exists — keep going until the full suite is reliably green.\n+No production API was added to fix test env problems — only test-utility additions and test-only isolation primitives.\n\n ## Acceptance Criteria\n-- [ ] `cargo test -p swissarmyhammer-tools` is green for 10 consecutive full-suite runs.\n-- [ ] Each previously-failing test still passes in isolation.\n-- [ ] Isolation fixes use existing primitives (no new prod API to support tests).\n-- [ ] No `#[ignore]` / `#[allow]` band-aids; no `--test-threads=1` workaround.\n-- [ ] `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` clean.\n+- [x] `cargo test -p swissarmyhammer-tools` is green for 10 consecutive full-suite runs.\n+- [x] Each previously-failing test still passes in isolation.\n+- [x] Isolation fixes use existing primitives (no new prod API to support tests).\n+- [x] No `#[ignore]` / `#[allow]` band-aids; no `--test-threads=1` workaround.\n+- [x] `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` clean.\n\n ## Tests\n-- [ ] All three currently-known tests (`test_dispatch_execute_command_empty_op`, `test_watcher_deleted_file_reverts_to_all_enabled`, `test_isolated_test_environment_drop_restores_home`) pass in the full-suite invocation, repeatedly.\n-- [ ] Whichever other tests mutate the shared state still pass.\n+- [x] All three currently-known tests (`test_dispatch_execute_command_empty_op`, `test_watcher_deleted_file_reverts_to_all_enabled`, `test_isolated_test_environment_drop_restores_home`) pass in the full-suite invocation, repeatedly.\n+- [x] Whichever other tests mutate the shared state still pass.\n\n-## Workflow\n-- Investigate first to find the offender(s); fix at the root, not the symptom. Document which global was racing and which tests now serialize on which key.\n\\ No newline at end of file\n+## Files Changed\n+- `crates/swissarmyhammer-tools/src/mcp/tools/shell/test_helpers.rs` — default `working_directory` to `/tmp` in `execute_op` / `execute_op_with` / `run_command_with`.\n+- `crates/swissarmyhammer-tools/src/mcp/tool_config.rs` — add `IsolatedTestEnvironment` + `#[serial(cwd)]` + `CwdGuard` to the two watcher tests that trigger an actual reload.\n+- `crates/swissarmyhammer-common/src/test_utils.rs` — expose `acquire_home_env_lock()`; rewrite `test_isolated_test_environment_drop_restores_home` to use the lock + sentinel pattern.\n+\n+## Verification\n+- 10 consecutive runs of `cargo test -p swissarmyhammer-tools --lib --tests`: all green.\n+- Each named test passes in isolation.\n+- `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` clean.\n+- `cargo clippy -p swissarmyhammer-common --all-targets -- -D warnings` clean.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -15,50 +15,30 @@\n\n All three pass in isolation. The pattern is classic shared-global-state racing — HOME, CWD, env vars, a `OnceCell` / `Mutex` static, or shared `TempDir` paths leaking across threads in the same test binary.\n\n-## Root Cause Analysis (resolved)\n+Fix the underlying isolation, not the symptom. Per the project's `test-isolation-raii` rule, do **not** add production APIs to fix test env problems — use existing isolation primitives (`#[serial_test::serial(<key>)]`, `IsolatedTestEnvironment`, `CurrentDirGuard`, `tempfile::TempDir`) or refactor the tests to own the state they depend on.\n\n-Three distinct race conditions, all read-side. None of them was a missing mutator RAII restore — the mutators were already protected; the *readers* were unprotected.\n-\n-### Race 1 — `test_dispatch_execute_command_empty_op`\n+Investigation steps for the implementer:\n+1. Locate each currently-known failing test (paths above). Read each.\n+2. Identify the shared global state (likely `HOME` env var, CWD, a `OnceCell` / static `Mutex`, a fixed-port listener, or a shared on-disk path).\n+3. Find the test(s) in the same crate that mutate that state without proper RAII restore — common suspects: anything that does `std::env::set_var`, `std::env::set_current_dir`, or initializes a `OnceCell` from a non-isolated source.\n+4. Apply the smallest correct isolation:\n+   - CWD → `CurrentDirGuard` on the mutator(s); ensure readers serialize on the same key.\n+   - HOME / env → `#[serial_test::serial(home_env)]` (or whichever key the project uses) on every test that touches the var, with RAII restore on mutators (`IsolatedTestEnvironment` does this already).\n+   - Static `OnceCell` / shared resource → serialize the tests that depend on it with a shared `serial_test` key.\n+5. Verify deterministically: run `cargo test -p swissarmyhammer-tools` 10 consecutive times under the full-suite invocation and confirm zero failures across all runs. A single passing run is not enough.\n\n-`execute_op` (in `crates/swissarmyhammer-tools/src/mcp/tools/shell/test_helpers.rs`) calls into the shell tool with no `working_directory` arg. That falls through to `prepare_working_directory(None)` in `process.rs`, which reads `std::env::current_dir()` and then checks `work_dir.exists()`. Tests like `test_health_check_project_config_valid` use `CurrentDirGuard::new(tmp.path())` which sets CWD to a `TempDir`. Between this test's `current_dir()` read and the `work_dir.exists()` check, the other test can drop its guard and delete its `TempDir`, leaving us with `WorkingDirectoryError`. `TestCommandBuilder::new` already handles this by defaulting `working_directory` to `/tmp`; the dispatch helpers did not.\n-\n-### Race 2 — `test_watcher_deleted_file_reverts_to_all_enabled`\n-\n-`ToolConfigWatcher::check_and_reload` calls `load_merged_tool_config()` on reload. That function reads the *process-global* HOME via `dirs::home_dir()` and the process-global CWD via `find_git_repository_root()` — it ignores the watcher's stored paths for the actual load (the stored paths are only used for mtime tracking). If another parallel test had set HOME to a fake home that contains a `.sah/tools.yaml` disabling shell, or CWD to a workspace whose `.sah/tools.yaml` disabled shell, the reload would pick that up and the assertion `registry.is_tool_enabled(\"shell\")` would fail.\n-\n-### Race 3 — `test_isolated_test_environment_drop_restores_home`\n-\n-The test read `HOME` *outside* of `HOME_ENV_LOCK`, then dropped an `IsolatedTestEnvironment`, then read `HOME` again. When another test was inside its own `IsolatedTestEnvironment` at the time of the first read, the test observed that other test's fake HOME, then later — after the other test had restored the *real* HOME — saw a different value and failed.\n-\n-## Fix Summary\n-\n-Smallest correct isolation, using existing primitives:\n-\n-1. **`execute_op` / `run_command_with`** (`shell/test_helpers.rs`): default `working_directory` to `/tmp` for execute-command dispatches, matching `TestCommandBuilder::new`. Removes the dependence on a stable process CWD.\n-2. **`test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled`** (`tool_config.rs`): hold an `IsolatedTestEnvironment` for the lifetime of the reload (gives HOME isolation via the existing global lock), and chdir into a clean temp dir under `#[serial_test::serial(cwd)]`.\n-3. **`test_isolated_test_environment_drop_restores_home`** (`test_utils.rs`): rewrite to atomically capture HOME under the `HOME_ENV_LOCK`, stamp a sentinel, drop the lock so `IsolatedTestEnvironment::new` can acquire it, drop the env, then re-acquire the lock to read the restored HOME. Also exposed `acquire_home_env_lock()` (test-utility, symmetric to the existing `acquire_semantic_db_lock()`).\n-\n-No production API was added to fix test env problems — only test-utility additions and test-only isolation primitives.\n+If different tests keep rotating after each fix, that means a still-unprotected mutator exists — keep going until the full suite is reliably green.\n\n ## Acceptance Criteria\n-- [x] `cargo test -p swissarmyhammer-tools` is green for 10 consecutive full-suite runs.\n-- [x] Each previously-failing test still passes in isolation.\n-- [x] Isolation fixes use existing primitives (no new prod API to support tests).\n-- [x] No `#[ignore]` / `#[allow]` band-aids; no `--test-threads=1` workaround.\n-- [x] `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` clean.\n+- [ ] `cargo test -p swissarmyhammer-tools` is green for 10 consecutive full-suite runs.\n+- [ ] Each previously-failing test still passes in isolation.\n+- [ ] Isolation fixes use existing primitives (no new prod API to support tests).\n+- [ ] No `#[ignore]` / `#[allow]` band-aids; no `--test-threads=1` workaround.\n+- [ ] `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` clean.\n\n ## Tests\n-- [x] All three currently-known tests (`test_dispatch_execute_command_empty_op`, `test_watcher_deleted_file_reverts_to_all_enabled`, `test_isolated_test_environment_drop_restores_home`) pass in the full-suite invocation, repeatedly.\n-- [x] Whichever other tests mutate the shared state still pass.\n-\n-## Files Changed\n-- `crates/swissarmyhammer-tools/src/mcp/tools/shell/test_helpers.rs` — default `working_directory` to `/tmp` in `execute_op` / `execute_op_with` / `run_command_with`.\n-- `crates/swissarmyhammer-tools/src/mcp/tool_config.rs` — add `IsolatedTestEnvironment` + `#[serial(cwd)]` + `CwdGuard` to the two watcher tests that trigger an actual reload.\n-- `crates/swissarmyhammer-common/src/test_utils.rs` — expose `acquire_home_env_lock()`; rewrite `test_isolated_test_environment_drop_restores_home` to use the lock + sentinel pattern.\n+- [ ] All three currently-known tests (`test_dispatch_execute_command_empty_op`, `test_watcher_deleted_file_reverts_to_all_enabled`, `test_isolated_test_environment_drop_restores_home`) pass in the full-suite invocation, repeatedly.\n+- [ ] Whichever other tests mutate the shared state still pass.\n\n-## Verification\n-- 10 consecutive runs of `cargo test -p swissarmyhammer-tools --lib --tests`: all green.\n-- Each named test passes in isolation.\n-- `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` clean.\n-- `cargo clippy -p swissarmyhammer-common --all-targets -- -D warnings` clean.\n\\ No newline at end of file\n+## Workflow\n+- Investigate first to find the offender(s); fix at the root, not the symptom. Document which global was racing and which tests now serialize on which key.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSJJEEW5GRREMX66VPS6R2VT","timestamp":"2026-05-26T16:38:50.501783Z","op":"update","item_id":"01KSJD05CNB1KFD880WQBN3XJY","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Fix `swissarmyhammer-tools` test parallel isolation — rotating failures from HOME/CWD/TempDir leakage\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Fix `swissarmyhammer-tools` test parallel isolation — rotating failures from HOME/CWD/TempDir leakage\n ---\n","transaction_id":null}
{"id":"01KSJK8VNNXFRK8VJZTMTB17Z3","timestamp":"2026-05-26T16:53:15.573031Z","op":"update","item_id":"01KSJD05CNB1KFD880WQBN3XJY","forward_patch":"--- original\n+++ modified\n@@ -61,4 +61,9 @@\n - 10 consecutive runs of `cargo test -p swissarmyhammer-tools --lib --tests`: all green.\n - Each named test passes in isolation.\n - `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` clean.\n-- `cargo clippy -p swissarmyhammer-common --all-targets -- -D warnings` clean.\n\\ No newline at end of file\n+- `cargo clippy -p swissarmyhammer-common --all-targets -- -D warnings` clean.\n+\n+## Review Findings (2026-05-26 17:35)\n+\n+### Warnings\n+- [ ] `crates/swissarmyhammer-tools/src/mcp/tool_config.rs:294` — The watcher tests introduce a new one-off `CwdGuard(PathBuf)` instead of using the canonical `swissarmyhammer_common::test_utils::CurrentDirGuard` already imported in the same `mod tests` (via `IsolatedTestEnvironment`). This duplicates the established RAII primitive, contradicts the project's `test-isolation-raii` rule (which names `CurrentDirGuard` explicitly), and even diverges from this task's own description which promised \"watcher tests now wrap `IsolatedTestEnvironment` + `CurrentDirGuard`\". The canonical guard is strictly stronger: it validates the target dir exists before chdir, recovers from invalid CWD on entry, and falls back to `CARGO_MANIFEST_DIR` on restore failure — none of which the local `CwdGuard` does. Replace the local `CwdGuard` struct + manual `set_current_dir` calls in `test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled` with `let _cwd = CurrentDirGuard::new(cwd_dir.path()).expect(\"chdir guard\");` and delete the `CwdGuard` definition (lines 286-300). The `#[serial(cwd)]` attribute can stay — `CurrentDirGuard`'s internal lock is compatible with the serial token and provides defense in depth.\n","reverse_patch":"--- original\n+++ modified\n@@ -61,9 +61,4 @@\n - 10 consecutive runs of `cargo test -p swissarmyhammer-tools --lib --tests`: all green.\n - Each named test passes in isolation.\n - `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` clean.\n-- `cargo clippy -p swissarmyhammer-common --all-targets -- -D warnings` clean.\n-\n-## Review Findings (2026-05-26 17:35)\n-\n-### Warnings\n-- [ ] `crates/swissarmyhammer-tools/src/mcp/tool_config.rs:294` — The watcher tests introduce a new one-off `CwdGuard(PathBuf)` instead of using the canonical `swissarmyhammer_common::test_utils::CurrentDirGuard` already imported in the same `mod tests` (via `IsolatedTestEnvironment`). This duplicates the established RAII primitive, contradicts the project's `test-isolation-raii` rule (which names `CurrentDirGuard` explicitly), and even diverges from this task's own description which promised \"watcher tests now wrap `IsolatedTestEnvironment` + `CurrentDirGuard`\". The canonical guard is strictly stronger: it validates the target dir exists before chdir, recovers from invalid CWD on entry, and falls back to `CARGO_MANIFEST_DIR` on restore failure — none of which the local `CwdGuard` does. Replace the local `CwdGuard` struct + manual `set_current_dir` calls in `test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled` with `let _cwd = CurrentDirGuard::new(cwd_dir.path()).expect(\"chdir guard\");` and delete the `CwdGuard` definition (lines 286-300). The `#[serial(cwd)]` attribute can stay — `CurrentDirGuard`'s internal lock is compatible with the serial token and provides defense in depth.\n+- `cargo clippy -p swissarmyhammer-common --all-targets -- -D warnings` clean.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSJK9MGMTT46RM51DZ0XMS7Y","timestamp":"2026-05-26T16:53:41.012872Z","op":"update","item_id":"01KSJD05CNB1KFD880WQBN3XJY","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Fix `swissarmyhammer-tools` test parallel isolation — rotating failures from HOME/CWD/TempDir leakage\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Fix `swissarmyhammer-tools` test parallel isolation — rotating failures from HOME/CWD/TempDir leakage\n ---\n","transaction_id":null}
{"id":"01KSJKDP8TG9E6V5YSBZEEH63V","timestamp":"2026-05-26T16:55:53.882434Z","op":"update","item_id":"01KSJD05CNB1KFD880WQBN3XJY","forward_patch":"--- original\n+++ modified\n@@ -36,7 +36,7 @@\n Smallest correct isolation, using existing primitives:\n\n 1. **`execute_op` / `run_command_with`** (`shell/test_helpers.rs`): default `working_directory` to `/tmp` for execute-command dispatches, matching `TestCommandBuilder::new`. Removes the dependence on a stable process CWD.\n-2. **`test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled`** (`tool_config.rs`): hold an `IsolatedTestEnvironment` for the lifetime of the reload (gives HOME isolation via the existing global lock), and chdir into a clean temp dir under `#[serial_test::serial(cwd)]`.\n+2. **`test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled`** (`tool_config.rs`): hold an `IsolatedTestEnvironment` for the lifetime of the reload (gives HOME isolation via the existing global lock), and chdir into a clean temp dir under `#[serial_test::serial(cwd)]` using the canonical `CurrentDirGuard`.\n 3. **`test_isolated_test_environment_drop_restores_home`** (`test_utils.rs`): rewrite to atomically capture HOME under the `HOME_ENV_LOCK`, stamp a sentinel, drop the lock so `IsolatedTestEnvironment::new` can acquire it, drop the env, then re-acquire the lock to read the restored HOME. Also exposed `acquire_home_env_lock()` (test-utility, symmetric to the existing `acquire_semantic_db_lock()`).\n\n No production API was added to fix test env problems — only test-utility additions and test-only isolation primitives.\n@@ -54,7 +54,7 @@\n\n ## Files Changed\n - `crates/swissarmyhammer-tools/src/mcp/tools/shell/test_helpers.rs` — default `working_directory` to `/tmp` in `execute_op` / `execute_op_with` / `run_command_with`.\n-- `crates/swissarmyhammer-tools/src/mcp/tool_config.rs` — add `IsolatedTestEnvironment` + `#[serial(cwd)]` + `CwdGuard` to the two watcher tests that trigger an actual reload.\n+- `crates/swissarmyhammer-tools/src/mcp/tool_config.rs` — add `IsolatedTestEnvironment` + `#[serial(cwd)]` + `CurrentDirGuard` to the two watcher tests that trigger an actual reload.\n - `crates/swissarmyhammer-common/src/test_utils.rs` — expose `acquire_home_env_lock()`; rewrite `test_isolated_test_environment_drop_restores_home` to use the lock + sentinel pattern.\n\n ## Verification\n@@ -66,4 +66,7 @@\n ## Review Findings (2026-05-26 17:35)\n\n ### Warnings\n-- [ ] `crates/swissarmyhammer-tools/src/mcp/tool_config.rs:294` — The watcher tests introduce a new one-off `CwdGuard(PathBuf)` instead of using the canonical `swissarmyhammer_common::test_utils::CurrentDirGuard` already imported in the same `mod tests` (via `IsolatedTestEnvironment`). This duplicates the established RAII primitive, contradicts the project's `test-isolation-raii` rule (which names `CurrentDirGuard` explicitly), and even diverges from this task's own description which promised \"watcher tests now wrap `IsolatedTestEnvironment` + `CurrentDirGuard`\". The canonical guard is strictly stronger: it validates the target dir exists before chdir, recovers from invalid CWD on entry, and falls back to `CARGO_MANIFEST_DIR` on restore failure — none of which the local `CwdGuard` does. Replace the local `CwdGuard` struct + manual `set_current_dir` calls in `test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled` with `let _cwd = CurrentDirGuard::new(cwd_dir.path()).expect(\"chdir guard\");` and delete the `CwdGuard` definition (lines 286-300). The `#[serial(cwd)]` attribute can stay — `CurrentDirGuard`'s internal lock is compatible with the serial token and provides defense in depth.\n+- [x] `crates/swissarmyhammer-tools/src/mcp/tool_config.rs:294` — The watcher tests introduce a new one-off `CwdGuard(PathBuf)` instead of using the canonical `swissarmyhammer_common::test_utils::CurrentDirGuard` already imported in the same `mod tests` (via `IsolatedTestEnvironment`). This duplicates the established RAII primitive, contradicts the project's `test-isolation-raii` rule (which names `CurrentDirGuard` explicitly), and even diverges from this task's own description which promised \"watcher tests now wrap `IsolatedTestEnvironment` + `CurrentDirGuard`\". The canonical guard is strictly stronger: it validates the target dir exists before chdir, recovers from invalid CWD on entry, and falls back to `CARGO_MANIFEST_DIR` on restore failure — none of which the local `CwdGuard` does. Replace the local `CwdGuard` struct + manual `set_current_dir` calls in `test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled` with `let _cwd = CurrentDirGuard::new(cwd_dir.path()).expect(\"chdir guard\");` and delete the `CwdGuard` definition (lines 286-300). The `#[serial(cwd)]` attribute can stay — `CurrentDirGuard`'s internal lock is compatible with the serial token and provides defense in depth.\n+\n+### Resolution (2026-05-26)\n+Replaced local `CwdGuard` struct + manual `std::env::set_current_dir` calls in both `test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled` with `CurrentDirGuard::new(cwd_dir.path()).expect(\"chdir guard\")`. Deleted the local `CwdGuard` definition (former lines 286-300). Import line in `mod tests` updated to `use swissarmyhammer_common::test_utils::{CurrentDirGuard, IsolatedTestEnvironment};`. `#[serial_test::serial(cwd)]` retained on both tests for defense in depth. Both tests pass; `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` is clean.\n","reverse_patch":"--- original\n+++ modified\n@@ -36,7 +36,7 @@\n Smallest correct isolation, using existing primitives:\n\n 1. **`execute_op` / `run_command_with`** (`shell/test_helpers.rs`): default `working_directory` to `/tmp` for execute-command dispatches, matching `TestCommandBuilder::new`. Removes the dependence on a stable process CWD.\n-2. **`test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled`** (`tool_config.rs`): hold an `IsolatedTestEnvironment` for the lifetime of the reload (gives HOME isolation via the existing global lock), and chdir into a clean temp dir under `#[serial_test::serial(cwd)]` using the canonical `CurrentDirGuard`.\n+2. **`test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled`** (`tool_config.rs`): hold an `IsolatedTestEnvironment` for the lifetime of the reload (gives HOME isolation via the existing global lock), and chdir into a clean temp dir under `#[serial_test::serial(cwd)]`.\n 3. **`test_isolated_test_environment_drop_restores_home`** (`test_utils.rs`): rewrite to atomically capture HOME under the `HOME_ENV_LOCK`, stamp a sentinel, drop the lock so `IsolatedTestEnvironment::new` can acquire it, drop the env, then re-acquire the lock to read the restored HOME. Also exposed `acquire_home_env_lock()` (test-utility, symmetric to the existing `acquire_semantic_db_lock()`).\n\n No production API was added to fix test env problems — only test-utility additions and test-only isolation primitives.\n@@ -54,7 +54,7 @@\n\n ## Files Changed\n - `crates/swissarmyhammer-tools/src/mcp/tools/shell/test_helpers.rs` — default `working_directory` to `/tmp` in `execute_op` / `execute_op_with` / `run_command_with`.\n-- `crates/swissarmyhammer-tools/src/mcp/tool_config.rs` — add `IsolatedTestEnvironment` + `#[serial(cwd)]` + `CurrentDirGuard` to the two watcher tests that trigger an actual reload.\n+- `crates/swissarmyhammer-tools/src/mcp/tool_config.rs` — add `IsolatedTestEnvironment` + `#[serial(cwd)]` + `CwdGuard` to the two watcher tests that trigger an actual reload.\n - `crates/swissarmyhammer-common/src/test_utils.rs` — expose `acquire_home_env_lock()`; rewrite `test_isolated_test_environment_drop_restores_home` to use the lock + sentinel pattern.\n\n ## Verification\n@@ -66,7 +66,4 @@\n ## Review Findings (2026-05-26 17:35)\n\n ### Warnings\n-- [x] `crates/swissarmyhammer-tools/src/mcp/tool_config.rs:294` — The watcher tests introduce a new one-off `CwdGuard(PathBuf)` instead of using the canonical `swissarmyhammer_common::test_utils::CurrentDirGuard` already imported in the same `mod tests` (via `IsolatedTestEnvironment`). This duplicates the established RAII primitive, contradicts the project's `test-isolation-raii` rule (which names `CurrentDirGuard` explicitly), and even diverges from this task's own description which promised \"watcher tests now wrap `IsolatedTestEnvironment` + `CurrentDirGuard`\". The canonical guard is strictly stronger: it validates the target dir exists before chdir, recovers from invalid CWD on entry, and falls back to `CARGO_MANIFEST_DIR` on restore failure — none of which the local `CwdGuard` does. Replace the local `CwdGuard` struct + manual `set_current_dir` calls in `test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled` with `let _cwd = CurrentDirGuard::new(cwd_dir.path()).expect(\"chdir guard\");` and delete the `CwdGuard` definition (lines 286-300). The `#[serial(cwd)]` attribute can stay — `CurrentDirGuard`'s internal lock is compatible with the serial token and provides defense in depth.\n-\n-### Resolution (2026-05-26)\n-Replaced local `CwdGuard` struct + manual `std::env::set_current_dir` calls in both `test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled` with `CurrentDirGuard::new(cwd_dir.path()).expect(\"chdir guard\")`. Deleted the local `CwdGuard` definition (former lines 286-300). Import line in `mod tests` updated to `use swissarmyhammer_common::test_utils::{CurrentDirGuard, IsolatedTestEnvironment};`. `#[serial_test::serial(cwd)]` retained on both tests for defense in depth. Both tests pass; `cargo clippy -p swissarmyhammer-tools --all-targets -- -D warnings` is clean.\n+- [ ] `crates/swissarmyhammer-tools/src/mcp/tool_config.rs:294` — The watcher tests introduce a new one-off `CwdGuard(PathBuf)` instead of using the canonical `swissarmyhammer_common::test_utils::CurrentDirGuard` already imported in the same `mod tests` (via `IsolatedTestEnvironment`). This duplicates the established RAII primitive, contradicts the project's `test-isolation-raii` rule (which names `CurrentDirGuard` explicitly), and even diverges from this task's own description which promised \"watcher tests now wrap `IsolatedTestEnvironment` + `CurrentDirGuard`\". The canonical guard is strictly stronger: it validates the target dir exists before chdir, recovers from invalid CWD on entry, and falls back to `CARGO_MANIFEST_DIR` on restore failure — none of which the local `CwdGuard` does. Replace the local `CwdGuard` struct + manual `set_current_dir` calls in `test_watcher_detects_file_change` and `test_watcher_deleted_file_reverts_to_all_enabled` with `let _cwd = CurrentDirGuard::new(cwd_dir.path()).expect(\"chdir guard\");` and delete the `CwdGuard` definition (lines 286-300). The `#[serial(cwd)]` attribute can stay — `CurrentDirGuard`'s internal lock is compatible with the serial token and provides defense in depth.\n","transaction_id":null}
{"id":"01KSJKDXK6NJNRDMG6DJEQ195Q","timestamp":"2026-05-26T16:56:01.382129Z","op":"update","item_id":"01KSJD05CNB1KFD880WQBN3XJY","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Fix `swissarmyhammer-tools` test parallel isolation — rotating failures from HOME/CWD/TempDir leakage\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Fix `swissarmyhammer-tools` test parallel isolation — rotating failures from HOME/CWD/TempDir leakage\n ---\n","transaction_id":null}
{"id":"01KSJKHYM4FE8068BA0E7AV4FB","timestamp":"2026-05-26T16:58:13.508754Z","op":"update","item_id":"01KSJD05CNB1KFD880WQBN3XJY","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffff8f80\n title: Fix `swissarmyhammer-tools` test parallel isolation — rotating failures from HOME/CWD/TempDir leakage\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffff8f80\n+position_column: review\n+position_ordinal: '80'\n title: Fix `swissarmyhammer-tools` test parallel isolation — rotating failures from HOME/CWD/TempDir leakage\n ---\n ## What\n","transaction_id":null}

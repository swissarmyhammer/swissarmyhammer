{"id":"01KTED5F8EG1ATYKYX0HN3ZSN2","timestamp":"2026-06-06T12:05:17.198137Z","op":"create","item_id":"01KTED5F8DQ2XH5BB0WK1MRR3P","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,32 @@\n+---\n+position_column: todo\n+position_ordinal: d580\n+project: ui-command-cleanup\n+title: Card B — Generalize a webview command handler-bus\n+---\n+## What\n+FOUNDATION. Extract and generalize the existing AI module-bus (`apps/kanban-app/ui/src/ai/commands.ts` — `registerAiCommandHandlers` / `triggerAi*` / handler map keyed by purpose) into a REUSABLE webview command handler registry keyed by **plugin command id**. Webview-only command behaviors (overlay open-state, grid manipulation, field/pressable activation, editor drill-in) register a handler against the plugin command id; `useDispatchCommand` invokes the local handler for ids marked \"handled in webview\" instead of routing to a backend op.\n+\n+New module: `apps/kanban-app/ui/src/lib/webview-command-bus.ts` (mirror the ai/commands.ts doc-comment style and cleanup-function semantics):\n+- `registerWebviewCommandHandler(commandId: string, handler: (ctx: CommandContext) => void | Promise<unknown>): () => void` — install/replace by id, returns an unmount cleanup that only clears the slot it still owns (copy the ownership-guarded delete from `registerAiCommandHandlers`).\n+- `getWebviewCommandHandler(commandId)` / `hasWebviewCommandHandler(commandId)` — lookup used by the dispatch path.\n+- `resetWebviewCommandBusForTest()` — test isolation (mirror `resetAiCommandsForTest`).\n+\n+Define how a plugin-defined command marks \"handled in webview\" vs \"backend execute\": a plugin command whose `execute` returns a sentinel (recommend a typed `{ webview: true }` marker, or no backend op at all) is dispatched to the bus. Decide the marker in `apps/kanban-app/ui/src/lib/command-scope.tsx` where `useDispatchCommand`/`runBackendDispatch` live — add the bus lookup BEFORE backend dispatch: if `hasWebviewCommandHandler(id)`, run the handler and skip the backend. Keep `resolveCommand`'s existing execute fast-path untouched in THIS card (it is removed later in Card I once no scope execs remain).\n+\n+Do NOT migrate any specific behavior here — only the mechanism. Cards C/D/E/F and nav.jump (Card A) depend on this.\n+\n+## Acceptance Criteria\n+- [ ] `webview-command-bus.ts` exists with register (id-keyed, ownership-guarded cleanup), lookup, has-check, and test-reset.\n+- [ ] `useDispatchCommand` consults the bus before backend dispatch; a registered webview handler short-circuits the backend call for that id.\n+- [ ] The bus is independent of AI specifics — generic over any plugin command id.\n+- [ ] `ai/commands.ts` either re-expressed on top of the generic bus OR left as-is with a doc note that it is the precedent (do not regress AI behavior — keep its tests green).\n+- [ ] No command is DEFINED by the bus; it only routes execution of plugin-owned ids.\n+\n+## Tests\n+- [ ] New unit test `apps/kanban-app/ui/src/lib/webview-command-bus.test.ts`: register handler for an id → lookup returns it; cleanup clears only the owned slot; a remount registration is not wiped by an older cleanup (mirror the ai/commands ownership test).\n+- [ ] Dispatch test (extend `apps/kanban-app/ui/src/lib/command-scope` test or add `command-scope.webview-bus.test.tsx`): a command id with a registered webview handler dispatches to the handler and does NOT call the backend; an id without a handler falls through to backend dispatch.\n+- [ ] `npm test` (vitest) for the new/changed UI test files is green.\n+\n+## Workflow\n+- Use `/tdd` — write the failing bus + dispatch tests first, then implement. Automated tests only (vitest/browser harness).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,32 +0,0 @@\n----\n-position_column: todo\n-position_ordinal: d580\n-project: ui-command-cleanup\n-title: Card B — Generalize a webview command handler-bus\n----\n-## What\n-FOUNDATION. Extract and generalize the existing AI module-bus (`apps/kanban-app/ui/src/ai/commands.ts` — `registerAiCommandHandlers` / `triggerAi*` / handler map keyed by purpose) into a REUSABLE webview command handler registry keyed by **plugin command id**. Webview-only command behaviors (overlay open-state, grid manipulation, field/pressable activation, editor drill-in) register a handler against the plugin command id; `useDispatchCommand` invokes the local handler for ids marked \"handled in webview\" instead of routing to a backend op.\n-\n-New module: `apps/kanban-app/ui/src/lib/webview-command-bus.ts` (mirror the ai/commands.ts doc-comment style and cleanup-function semantics):\n-- `registerWebviewCommandHandler(commandId: string, handler: (ctx: CommandContext) => void | Promise<unknown>): () => void` — install/replace by id, returns an unmount cleanup that only clears the slot it still owns (copy the ownership-guarded delete from `registerAiCommandHandlers`).\n-- `getWebviewCommandHandler(commandId)` / `hasWebviewCommandHandler(commandId)` — lookup used by the dispatch path.\n-- `resetWebviewCommandBusForTest()` — test isolation (mirror `resetAiCommandsForTest`).\n-\n-Define how a plugin-defined command marks \"handled in webview\" vs \"backend execute\": a plugin command whose `execute` returns a sentinel (recommend a typed `{ webview: true }` marker, or no backend op at all) is dispatched to the bus. Decide the marker in `apps/kanban-app/ui/src/lib/command-scope.tsx` where `useDispatchCommand`/`runBackendDispatch` live — add the bus lookup BEFORE backend dispatch: if `hasWebviewCommandHandler(id)`, run the handler and skip the backend. Keep `resolveCommand`'s existing execute fast-path untouched in THIS card (it is removed later in Card I once no scope execs remain).\n-\n-Do NOT migrate any specific behavior here — only the mechanism. Cards C/D/E/F and nav.jump (Card A) depend on this.\n-\n-## Acceptance Criteria\n-- [ ] `webview-command-bus.ts` exists with register (id-keyed, ownership-guarded cleanup), lookup, has-check, and test-reset.\n-- [ ] `useDispatchCommand` consults the bus before backend dispatch; a registered webview handler short-circuits the backend call for that id.\n-- [ ] The bus is independent of AI specifics — generic over any plugin command id.\n-- [ ] `ai/commands.ts` either re-expressed on top of the generic bus OR left as-is with a doc note that it is the precedent (do not regress AI behavior — keep its tests green).\n-- [ ] No command is DEFINED by the bus; it only routes execution of plugin-owned ids.\n-\n-## Tests\n-- [ ] New unit test `apps/kanban-app/ui/src/lib/webview-command-bus.test.ts`: register handler for an id → lookup returns it; cleanup clears only the owned slot; a remount registration is not wiped by an older cleanup (mirror the ai/commands ownership test).\n-- [ ] Dispatch test (extend `apps/kanban-app/ui/src/lib/command-scope` test or add `command-scope.webview-bus.test.tsx`): a command id with a registered webview handler dispatches to the handler and does NOT call the backend; an id without a handler falls through to backend dispatch.\n-- [ ] `npm test` (vitest) for the new/changed UI test files is green.\n-\n-## Workflow\n-- Use `/tdd` — write the failing bus + dispatch tests first, then implement. Automated tests only (vitest/browser harness).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTEDQ9FFRPRGJ5QPNRYCHB4K","timestamp":"2026-06-06T12:15:01.103623Z","op":"update","item_id":"01KTED5F8DQ2XH5BB0WK1MRR3P","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,6 @@\n ---\n-position_column: todo\n-position_ordinal: d580\n+position_column: doing\n+position_ordinal: '80'\n project: ui-command-cleanup\n title: Card B — Generalize a webview command handler-bus\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,6 +1,6 @@\n ---\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: d580\n project: ui-command-cleanup\n title: Card B — Generalize a webview command handler-bus\n ---\n","transaction_id":null}
{"id":"01KTEE0ECY7D29XJ23X3GEH1FZ","timestamp":"2026-06-06T12:20:01.054129Z","op":"update","item_id":"01KTED5F8DQ2XH5BB0WK1MRR3P","forward_patch":"--- original\n+++ modified\n@@ -16,17 +16,27 @@\n\n Do NOT migrate any specific behavior here — only the mechanism. Cards C/D/E/F and nav.jump (Card A) depend on this.\n\n+## Design notes (as built)\n+- **Bus API** (`apps/kanban-app/ui/src/lib/webview-command-bus.ts`): module-level `Map<string, WebviewCommandHandler>`. `WebviewCommandHandler = (opts: DispatchOptions) => void | Promise<unknown>` — the handler receives the same `DispatchOptions` the dispatcher was called with (this is the webview equivalent of the service's `CommandContext`; reusing the existing type avoids inventing a parallel ctx shape and keeps the per-call args available). `registerWebviewCommandHandler` / `getWebviewCommandHandler` / `hasWebviewCommandHandler` / `resetWebviewCommandBusForTest`. Ownership-guarded cleanup copied verbatim from `registerAiCommandHandlers` (cleanup only deletes the slot if it still holds the same handler ref → StrictMode/HMR remount safe).\n+- **Marker decision (refined):** the \"handled in webview\" signal is simply **a registered handler existing for the id** — `hasWebviewCommandHandler(id)`. NO command-metadata field was added; no `{ webview: true }` sentinel is needed. Plugins still DEFINE the command (id/name/keys/menu); the webview just supplies the runtime handler. This is minimal and matches the precedent (ai/commands.ts also keys purely on a registered handler being present).\n+- **Dispatch integration** (`command-scope.tsx::useDispatchCommand`): bus lookup sits AFTER the existing `resolveCommand` `execute` fast-path (left untouched per the card — scope execs still win, removed in Card I) and BEFORE `runBackendDispatch`. A registered handler short-circuits the backend `execute command` call.\n+- `ai/commands.ts` left AS-IS (its fixed-key purpose-bus is the precedent); its 23 tests stay green — no AI behavior regressed.\n+\n ## Acceptance Criteria\n-- [ ] `webview-command-bus.ts` exists with register (id-keyed, ownership-guarded cleanup), lookup, has-check, and test-reset.\n-- [ ] `useDispatchCommand` consults the bus before backend dispatch; a registered webview handler short-circuits the backend call for that id.\n-- [ ] The bus is independent of AI specifics — generic over any plugin command id.\n-- [ ] `ai/commands.ts` either re-expressed on top of the generic bus OR left as-is with a doc note that it is the precedent (do not regress AI behavior — keep its tests green).\n-- [ ] No command is DEFINED by the bus; it only routes execution of plugin-owned ids.\n+- [x] `webview-command-bus.ts` exists with register (id-keyed, ownership-guarded cleanup), lookup, has-check, and test-reset.\n+- [x] `useDispatchCommand` consults the bus before backend dispatch; a registered webview handler short-circuits the backend call for that id.\n+- [x] The bus is independent of AI specifics — generic over any plugin command id.\n+- [x] `ai/commands.ts` left as-is as the documented precedent (its tests stay green; no AI behavior regressed).\n+- [x] No command is DEFINED by the bus; it only routes execution of plugin-owned ids.\n\n ## Tests\n-- [ ] New unit test `apps/kanban-app/ui/src/lib/webview-command-bus.test.ts`: register handler for an id → lookup returns it; cleanup clears only the owned slot; a remount registration is not wiped by an older cleanup (mirror the ai/commands ownership test).\n-- [ ] Dispatch test (extend `apps/kanban-app/ui/src/lib/command-scope` test or add `command-scope.webview-bus.test.tsx`): a command id with a registered webview handler dispatches to the handler and does NOT call the backend; an id without a handler falls through to backend dispatch.\n-- [ ] `npm test` (vitest) for the new/changed UI test files is green.\n+- [x] New unit test `apps/kanban-app/ui/src/lib/webview-command-bus.test.ts`: register handler for an id → lookup returns it; cleanup clears only the owned slot; a remount registration is not wiped by an older cleanup (mirror the ai/commands ownership test). (6 tests)\n+- [x] Dispatch test `apps/kanban-app/ui/src/lib/command-scope.webview-bus.test.tsx`: a command id with a registered webview handler dispatches to the handler and does NOT call the backend; an id without a handler falls through to backend dispatch; unregister falls back to backend; scope execute still wins. (5 tests)\n+- [x] vitest for the new/changed UI test files is green; `tsc --noEmit` clean. RED proven first (suites failed to import the missing module), GREEN after.\n+\n+## Test status (final)\n+- New + changed files: 4 files, 70 tests, all pass (webview-command-bus 6, command-scope.webview-bus 5, command-scope 36-ish, ai/commands 23). tsc --noEmit clean.\n+- Full UI suite: 93 failed | 2342 passed (2435) — the 93 are the documented PRE-EXISTING baseline (jump-to-overlay `startsWith`, spatial-nav, focus-scope). NONE reference `command-scope` or `webview` files. Zero new failures introduced.\n\n ## Workflow\n - Use `/tdd` — write the failing bus + dispatch tests first, then implement. Automated tests only (vitest/browser harness).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -16,27 +16,17 @@\n\n Do NOT migrate any specific behavior here — only the mechanism. Cards C/D/E/F and nav.jump (Card A) depend on this.\n\n-## Design notes (as built)\n-- **Bus API** (`apps/kanban-app/ui/src/lib/webview-command-bus.ts`): module-level `Map<string, WebviewCommandHandler>`. `WebviewCommandHandler = (opts: DispatchOptions) => void | Promise<unknown>` — the handler receives the same `DispatchOptions` the dispatcher was called with (this is the webview equivalent of the service's `CommandContext`; reusing the existing type avoids inventing a parallel ctx shape and keeps the per-call args available). `registerWebviewCommandHandler` / `getWebviewCommandHandler` / `hasWebviewCommandHandler` / `resetWebviewCommandBusForTest`. Ownership-guarded cleanup copied verbatim from `registerAiCommandHandlers` (cleanup only deletes the slot if it still holds the same handler ref → StrictMode/HMR remount safe).\n-- **Marker decision (refined):** the \"handled in webview\" signal is simply **a registered handler existing for the id** — `hasWebviewCommandHandler(id)`. NO command-metadata field was added; no `{ webview: true }` sentinel is needed. Plugins still DEFINE the command (id/name/keys/menu); the webview just supplies the runtime handler. This is minimal and matches the precedent (ai/commands.ts also keys purely on a registered handler being present).\n-- **Dispatch integration** (`command-scope.tsx::useDispatchCommand`): bus lookup sits AFTER the existing `resolveCommand` `execute` fast-path (left untouched per the card — scope execs still win, removed in Card I) and BEFORE `runBackendDispatch`. A registered handler short-circuits the backend `execute command` call.\n-- `ai/commands.ts` left AS-IS (its fixed-key purpose-bus is the precedent); its 23 tests stay green — no AI behavior regressed.\n-\n ## Acceptance Criteria\n-- [x] `webview-command-bus.ts` exists with register (id-keyed, ownership-guarded cleanup), lookup, has-check, and test-reset.\n-- [x] `useDispatchCommand` consults the bus before backend dispatch; a registered webview handler short-circuits the backend call for that id.\n-- [x] The bus is independent of AI specifics — generic over any plugin command id.\n-- [x] `ai/commands.ts` left as-is as the documented precedent (its tests stay green; no AI behavior regressed).\n-- [x] No command is DEFINED by the bus; it only routes execution of plugin-owned ids.\n+- [ ] `webview-command-bus.ts` exists with register (id-keyed, ownership-guarded cleanup), lookup, has-check, and test-reset.\n+- [ ] `useDispatchCommand` consults the bus before backend dispatch; a registered webview handler short-circuits the backend call for that id.\n+- [ ] The bus is independent of AI specifics — generic over any plugin command id.\n+- [ ] `ai/commands.ts` either re-expressed on top of the generic bus OR left as-is with a doc note that it is the precedent (do not regress AI behavior — keep its tests green).\n+- [ ] No command is DEFINED by the bus; it only routes execution of plugin-owned ids.\n\n ## Tests\n-- [x] New unit test `apps/kanban-app/ui/src/lib/webview-command-bus.test.ts`: register handler for an id → lookup returns it; cleanup clears only the owned slot; a remount registration is not wiped by an older cleanup (mirror the ai/commands ownership test). (6 tests)\n-- [x] Dispatch test `apps/kanban-app/ui/src/lib/command-scope.webview-bus.test.tsx`: a command id with a registered webview handler dispatches to the handler and does NOT call the backend; an id without a handler falls through to backend dispatch; unregister falls back to backend; scope execute still wins. (5 tests)\n-- [x] vitest for the new/changed UI test files is green; `tsc --noEmit` clean. RED proven first (suites failed to import the missing module), GREEN after.\n-\n-## Test status (final)\n-- New + changed files: 4 files, 70 tests, all pass (webview-command-bus 6, command-scope.webview-bus 5, command-scope 36-ish, ai/commands 23). tsc --noEmit clean.\n-- Full UI suite: 93 failed | 2342 passed (2435) — the 93 are the documented PRE-EXISTING baseline (jump-to-overlay `startsWith`, spatial-nav, focus-scope). NONE reference `command-scope` or `webview` files. Zero new failures introduced.\n+- [ ] New unit test `apps/kanban-app/ui/src/lib/webview-command-bus.test.ts`: register handler for an id → lookup returns it; cleanup clears only the owned slot; a remount registration is not wiped by an older cleanup (mirror the ai/commands ownership test).\n+- [ ] Dispatch test (extend `apps/kanban-app/ui/src/lib/command-scope` test or add `command-scope.webview-bus.test.tsx`): a command id with a registered webview handler dispatches to the handler and does NOT call the backend; an id without a handler falls through to backend dispatch.\n+- [ ] `npm test` (vitest) for the new/changed UI test files is green.\n\n ## Workflow\n - Use `/tdd` — write the failing bus + dispatch tests first, then implement. Automated tests only (vitest/browser harness).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTEE0M214MZQEJ8Q0FPVB7CK","timestamp":"2026-06-06T12:20:06.849292Z","op":"update","item_id":"01KTED5F8DQ2XH5BB0WK1MRR3P","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,6 @@\n ---\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8280'\n project: ui-command-cleanup\n title: Card B — Generalize a webview command handler-bus\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,6 +1,6 @@\n ---\n-position_column: review\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '80'\n project: ui-command-cleanup\n title: Card B — Generalize a webview command handler-bus\n ---\n","transaction_id":null}
{"id":"01KTEEN2B39KYXNWYGPZ1KJCW7","timestamp":"2026-06-06T12:31:16.835297Z","op":"update","item_id":"01KTED5F8DQ2XH5BB0WK1MRR3P","forward_patch":"--- original\n+++ modified\n@@ -16,27 +16,22 @@\n\n Do NOT migrate any specific behavior here — only the mechanism. Cards C/D/E/F and nav.jump (Card A) depend on this.\n\n-## Design notes (as built)\n-- **Bus API** (`apps/kanban-app/ui/src/lib/webview-command-bus.ts`): module-level `Map<string, WebviewCommandHandler>`. `WebviewCommandHandler = (opts: DispatchOptions) => void | Promise<unknown>` — the handler receives the same `DispatchOptions` the dispatcher was called with (this is the webview equivalent of the service's `CommandContext`; reusing the existing type avoids inventing a parallel ctx shape and keeps the per-call args available). `registerWebviewCommandHandler` / `getWebviewCommandHandler` / `hasWebviewCommandHandler` / `resetWebviewCommandBusForTest`. Ownership-guarded cleanup copied verbatim from `registerAiCommandHandlers` (cleanup only deletes the slot if it still holds the same handler ref → StrictMode/HMR remount safe).\n-- **Marker decision (refined):** the \"handled in webview\" signal is simply **a registered handler existing for the id** — `hasWebviewCommandHandler(id)`. NO command-metadata field was added; no `{ webview: true }` sentinel is needed. Plugins still DEFINE the command (id/name/keys/menu); the webview just supplies the runtime handler. This is minimal and matches the precedent (ai/commands.ts also keys purely on a registered handler being present).\n-- **Dispatch integration** (`command-scope.tsx::useDispatchCommand`): bus lookup sits AFTER the existing `resolveCommand` `execute` fast-path (left untouched per the card — scope execs still win, removed in Card I) and BEFORE `runBackendDispatch`. A registered handler short-circuits the backend `execute command` call.\n-- `ai/commands.ts` left AS-IS (its fixed-key purpose-bus is the precedent); its 23 tests stay green — no AI behavior regressed.\n+## Handler invariant (the guardrail downstream cards must keep)\n+A bus handler is PURE PRESENTATION — it may touch live webview state (DOM focus, editor instance, grid handle, edit-mode, onPress) and nothing else. It MUST NOT do a durable mutation inline (no store write, no constructing a CommandDef, no MCP transport). Durable effects route BACK through `useDispatchCommand` to a backend-op plugin command (e.g. grid.deleteRow → `${entity}.archive`, board.newTask → `entity.addTask`). This keeps all command logic in Rust and stops the bus becoming a client-side command-logic dumping ground. The invariant is documented in `webview-command-bus.ts` and enforced mechanically by `webview-command-bus.guard.node.test.ts` (fails when a handler-registration site also imports `@/lib/mcp-transport`).\n\n ## Acceptance Criteria\n - [x] `webview-command-bus.ts` exists with register (id-keyed, ownership-guarded cleanup), lookup, has-check, and test-reset.\n - [x] `useDispatchCommand` consults the bus before backend dispatch; a registered webview handler short-circuits the backend call for that id.\n - [x] The bus is independent of AI specifics — generic over any plugin command id.\n-- [x] `ai/commands.ts` left as-is as the documented precedent (its tests stay green; no AI behavior regressed).\n+- [ ] `ai/commands.ts` either re-expressed on top of the generic bus OR left as-is with a doc note that it is the precedent (do not regress AI behavior — keep its tests green).\n - [x] No command is DEFINED by the bus; it only routes execution of plugin-owned ids.\n+- [x] GUARD shipped: the presentation-only invariant is documented in the bus module and enforced by `webview-command-bus.guard.node.test.ts` (self-proving detector + live scan; green). Cards C–F each carry a matching acceptance line.\n\n ## Tests\n-- [x] New unit test `apps/kanban-app/ui/src/lib/webview-command-bus.test.ts`: register handler for an id → lookup returns it; cleanup clears only the owned slot; a remount registration is not wiped by an older cleanup (mirror the ai/commands ownership test). (6 tests)\n-- [x] Dispatch test `apps/kanban-app/ui/src/lib/command-scope.webview-bus.test.tsx`: a command id with a registered webview handler dispatches to the handler and does NOT call the backend; an id without a handler falls through to backend dispatch; unregister falls back to backend; scope execute still wins. (5 tests)\n-- [x] vitest for the new/changed UI test files is green; `tsc --noEmit` clean. RED proven first (suites failed to import the missing module), GREEN after.\n-\n-## Test status (final)\n-- New + changed files: 4 files, 70 tests, all pass (webview-command-bus 6, command-scope.webview-bus 5, command-scope 36-ish, ai/commands 23). tsc --noEmit clean.\n-- Full UI suite: 93 failed | 2342 passed (2435) — the 93 are the documented PRE-EXISTING baseline (jump-to-overlay `startsWith`, spatial-nav, focus-scope). NONE reference `command-scope` or `webview` files. Zero new failures introduced.\n+- [x] New unit test `apps/kanban-app/ui/src/lib/webview-command-bus.test.ts`: register handler for an id → lookup returns it; cleanup clears only the owned slot; a remount registration is not wiped by an older cleanup (mirror the ai/commands ownership test).\n+- [ ] Dispatch test (extend `apps/kanban-app/ui/src/lib/command-scope` test or add `command-scope.webview-bus.test.tsx`): a command id with a registered webview handler dispatches to the handler and does NOT call the backend; an id without a handler falls through to backend dispatch.\n+- [x] Guard test `apps/kanban-app/ui/src/lib/webview-command-bus.guard.node.test.ts`: detector is unit-proven (flags an mcp-transport import, ignores unrelated imports), the bus module stays transport-free, and no handler-registration site imports the transport. `npm test` green.\n+- [ ] `npm test` (vitest) for the new/changed UI test files is green.\n\n ## Workflow\n - Use `/tdd` — write the failing bus + dispatch tests first, then implement. Automated tests only (vitest/browser harness).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -16,22 +16,27 @@\n\n Do NOT migrate any specific behavior here — only the mechanism. Cards C/D/E/F and nav.jump (Card A) depend on this.\n\n-## Handler invariant (the guardrail downstream cards must keep)\n-A bus handler is PURE PRESENTATION — it may touch live webview state (DOM focus, editor instance, grid handle, edit-mode, onPress) and nothing else. It MUST NOT do a durable mutation inline (no store write, no constructing a CommandDef, no MCP transport). Durable effects route BACK through `useDispatchCommand` to a backend-op plugin command (e.g. grid.deleteRow → `${entity}.archive`, board.newTask → `entity.addTask`). This keeps all command logic in Rust and stops the bus becoming a client-side command-logic dumping ground. The invariant is documented in `webview-command-bus.ts` and enforced mechanically by `webview-command-bus.guard.node.test.ts` (fails when a handler-registration site also imports `@/lib/mcp-transport`).\n+## Design notes (as built)\n+- **Bus API** (`apps/kanban-app/ui/src/lib/webview-command-bus.ts`): module-level `Map<string, WebviewCommandHandler>`. `WebviewCommandHandler = (opts: DispatchOptions) => void | Promise<unknown>` — the handler receives the same `DispatchOptions` the dispatcher was called with (this is the webview equivalent of the service's `CommandContext`; reusing the existing type avoids inventing a parallel ctx shape and keeps the per-call args available). `registerWebviewCommandHandler` / `getWebviewCommandHandler` / `hasWebviewCommandHandler` / `resetWebviewCommandBusForTest`. Ownership-guarded cleanup copied verbatim from `registerAiCommandHandlers` (cleanup only deletes the slot if it still holds the same handler ref → StrictMode/HMR remount safe).\n+- **Marker decision (refined):** the \"handled in webview\" signal is simply **a registered handler existing for the id** — `hasWebviewCommandHandler(id)`. NO command-metadata field was added; no `{ webview: true }` sentinel is needed. Plugins still DEFINE the command (id/name/keys/menu); the webview just supplies the runtime handler. This is minimal and matches the precedent (ai/commands.ts also keys purely on a registered handler being present).\n+- **Dispatch integration** (`command-scope.tsx::useDispatchCommand`): bus lookup sits AFTER the existing `resolveCommand` `execute` fast-path (left untouched per the card — scope execs still win, removed in Card I) and BEFORE `runBackendDispatch`. A registered handler short-circuits the backend `execute command` call.\n+- `ai/commands.ts` left AS-IS (its fixed-key purpose-bus is the precedent); its 23 tests stay green — no AI behavior regressed.\n\n ## Acceptance Criteria\n - [x] `webview-command-bus.ts` exists with register (id-keyed, ownership-guarded cleanup), lookup, has-check, and test-reset.\n - [x] `useDispatchCommand` consults the bus before backend dispatch; a registered webview handler short-circuits the backend call for that id.\n - [x] The bus is independent of AI specifics — generic over any plugin command id.\n-- [ ] `ai/commands.ts` either re-expressed on top of the generic bus OR left as-is with a doc note that it is the precedent (do not regress AI behavior — keep its tests green).\n+- [x] `ai/commands.ts` left as-is as the documented precedent (its tests stay green; no AI behavior regressed).\n - [x] No command is DEFINED by the bus; it only routes execution of plugin-owned ids.\n-- [x] GUARD shipped: the presentation-only invariant is documented in the bus module and enforced by `webview-command-bus.guard.node.test.ts` (self-proving detector + live scan; green). Cards C–F each carry a matching acceptance line.\n\n ## Tests\n-- [x] New unit test `apps/kanban-app/ui/src/lib/webview-command-bus.test.ts`: register handler for an id → lookup returns it; cleanup clears only the owned slot; a remount registration is not wiped by an older cleanup (mirror the ai/commands ownership test).\n-- [ ] Dispatch test (extend `apps/kanban-app/ui/src/lib/command-scope` test or add `command-scope.webview-bus.test.tsx`): a command id with a registered webview handler dispatches to the handler and does NOT call the backend; an id without a handler falls through to backend dispatch.\n-- [x] Guard test `apps/kanban-app/ui/src/lib/webview-command-bus.guard.node.test.ts`: detector is unit-proven (flags an mcp-transport import, ignores unrelated imports), the bus module stays transport-free, and no handler-registration site imports the transport. `npm test` green.\n-- [ ] `npm test` (vitest) for the new/changed UI test files is green.\n+- [x] New unit test `apps/kanban-app/ui/src/lib/webview-command-bus.test.ts`: register handler for an id → lookup returns it; cleanup clears only the owned slot; a remount registration is not wiped by an older cleanup (mirror the ai/commands ownership test). (6 tests)\n+- [x] Dispatch test `apps/kanban-app/ui/src/lib/command-scope.webview-bus.test.tsx`: a command id with a registered webview handler dispatches to the handler and does NOT call the backend; an id without a handler falls through to backend dispatch; unregister falls back to backend; scope execute still wins. (5 tests)\n+- [x] vitest for the new/changed UI test files is green; `tsc --noEmit` clean. RED proven first (suites failed to import the missing module), GREEN after.\n+\n+## Test status (final)\n+- New + changed files: 4 files, 70 tests, all pass (webview-command-bus 6, command-scope.webview-bus 5, command-scope 36-ish, ai/commands 23). tsc --noEmit clean.\n+- Full UI suite: 93 failed | 2342 passed (2435) — the 93 are the documented PRE-EXISTING baseline (jump-to-overlay `startsWith`, spatial-nav, focus-scope). NONE reference `command-scope` or `webview` files. Zero new failures introduced.\n\n ## Workflow\n - Use `/tdd` — write the failing bus + dispatch tests first, then implement. Automated tests only (vitest/browser harness).\n\\ No newline at end of file\n","transaction_id":null}

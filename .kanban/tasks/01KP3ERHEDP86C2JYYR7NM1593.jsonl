{"id":"01KQ5NTG5SKSP774KE6JTZ3QT3","timestamp":"2026-04-26T19:56:19.513776Z","op":"update","item_id":"01KP3ERHEDP86C2JYYR7NM1593","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - wballard\n position_column: todo\n-position_ordinal: c680\n+position_ordinal: '8680'\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - wballard\n position_column: todo\n-position_ordinal: '8680'\n+position_ordinal: c680\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n ## What\n","transaction_id":null}
{"id":"01KQ5NTG5TBJFRF4GGFS4GV249","timestamp":"2026-04-26T19:56:19.514222Z","entity_type":"task","entity_id":"01KP3ERHEDP86C2JYYR7NM1593","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-c680\n\\ No newline at end of file\n+8680\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8680\n\\ No newline at end of file\n+c680\n\\ No newline at end of file\n"}]]}
{"id":"01KRK6ATE57KNM672K764EQR0Q","timestamp":"2026-05-14T12:10:40.965693Z","op":"update","item_id":"01KP3ERHEDP86C2JYYR7NM1593","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - wballard\n position_column: todo\n-position_ordinal: '8680'\n+position_ordinal: fd80\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - wballard\n position_column: todo\n-position_ordinal: fd80\n+position_ordinal: '8680'\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n ## What\n","transaction_id":null}
{"id":"01KRKVK4W6760TWBJRH8C34YR3","timestamp":"2026-05-14T18:22:13.894437Z","op":"update","item_id":"01KP3ERHEDP86C2JYYR7NM1593","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - wballard\n position_column: todo\n-position_ordinal: fd80\n+position_ordinal: '80'\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - wballard\n position_column: todo\n-position_ordinal: '80'\n+position_ordinal: fd80\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n ## What\n","transaction_id":null}
{"id":"01KRM2Z40DV65CXEJWX8YQQREE","timestamp":"2026-05-14T20:31:06.253366Z","op":"update","item_id":"01KP3ERHEDP86C2JYYR7NM1593","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - wballard\n-position_column: todo\n+position_column: doing\n position_ordinal: '80'\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - wballard\n-position_column: doing\n+position_column: todo\n position_ordinal: '80'\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n","transaction_id":null}
{"id":"01KRM2Z40E97RXG6Q08JNYVV5Q","timestamp":"2026-05-14T20:31:06.254530Z","entity_type":"task","entity_id":"01KP3ERHEDP86C2JYYR7NM1593","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}]]}
{"id":"01KRM4YR0ET1NS36JN7ZRQGTZM","timestamp":"2026-05-14T21:05:51.118079Z","op":"update","item_id":"01KP3ERHEDP86C2JYYR7NM1593","forward_patch":"--- original\n+++ modified\n@@ -71,21 +71,21 @@\n\n ## Acceptance Criteria\n\n-- [ ] `perspective.switch` command exists in `perspective.yaml` and is handled by `SwitchPerspectiveCmd`; `ui.perspective.set` is removed (both YAML and handler).\n-- [ ] Backend atomically updates `active_perspective_id` and `filtered_task_ids` on the window in a single `UIStateChange`; exactly one `ui-state-changed` event fires per click.\n-- [ ] Clicking a perspective tab in the running app visibly shows the indeterminate progress bar under `NavBar` for the duration of the filter evaluation.\n-- [ ] The previously-needed `useEffect` in `perspective-container.tsx` that called `refreshEntities(boardPath, activeFilter)` on perspective change is gone; perspective change no longer triggers a second roundtrip from the frontend.\n-- [ ] Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`; switching perspectives updates what is visible without any frontend-side filter call.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `bun run test` (in `kanban-app/ui`) pass. `bun run typecheck` passes.\n+- [x] `perspective.switch` command exists in `perspective.yaml` and is handled by `SwitchPerspectiveCmd`; `ui.perspective.set` is removed (both YAML and handler). (Note: the prior id was already `perspective.set`, relocated from `ui.perspective.set` in 01KPY02X405QTP5ACH67THHSN8; this card removed `perspective.set` and replaced it with `perspective.switch`.)\n+- [x] Backend atomically updates `active_perspective_id` and `filtered_task_ids` on the window in a single `UIStateChange`; exactly one `ui-state-changed` event fires per click.\n+- [x] Clicking a perspective tab in the running app visibly shows the indeterminate progress bar under `NavBar` for the duration of the filter evaluation.\n+- [x] The previously-needed `useEffect` in `perspective-container.tsx` that called `refreshEntities(boardPath, activeFilter)` on perspective change is gone; perspective change no longer triggers a second roundtrip from the frontend.\n+- [x] Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`; switching perspectives updates what is visible without any frontend-side filter call.\n+- [x] `cargo test -p swissarmyhammer-kanban` and `bun run test` (in `kanban-app/ui`) pass. `bun run typecheck` passes. (This project actually uses `npm` for the UI workspace — equivalents `npx vitest run` and `npx tsc --noEmit` were run and are all green.)\n\n ## Tests\n\n-- [ ] `swissarmyhammer-kanban/src/commands/perspective_commands.rs` — add unit tests for `SwitchPerspectiveCmd` covering: (a) sets `active_perspective_id`, (b) writes filtered task ids matching the perspective's filter, (c) both changes land in one `UIStateChange`, (d) unknown perspective id returns a clean `ExecutionFailed` error.\n-- [ ] Rust integration test (same crate or `swissarmyhammer-kanban/tests/`) that dispatches `perspective.switch` via the command registry end-to-end and asserts the resulting UIState snapshot.\n-- [ ] `kanban-app/ui/src/lib/perspective-context.test.tsx` — update the existing \"dispatches ui.perspective.set to backend\" test to assert dispatch of `perspective.switch` with `{ args: { perspective_id } }` and no `\"ui.perspective.set\"` IPC. Delete tests that assert the old command name.\n-- [ ] Add a test in `kanban-app/ui/src/components/perspective-container.test.tsx` (create if absent) that mounts `PerspectiveContainer` and asserts it does NOT call `refreshEntities` on perspective change — the filtered list comes from UIState.\n-- [ ] Add an integration-style test in `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` that clicks a tab, mocks the `dispatch_command` invoke to pend for ~50ms, and asserts `role=\"progressbar\"` is visible during the pending window. (This replaces the older \"doesn't show busy\" smoke concern with a real assertion.)\n-- [ ] Commands to run: `cargo nextest run -p swissarmyhammer-kanban`, `bun run test -- perspective-context perspective-container perspective-tab-bar`, `bun run typecheck`. All green.\n+- [x] `swissarmyhammer-kanban/src/commands/perspective_commands.rs` — add unit tests for `SwitchPerspectiveCmd` covering: (a) sets `active_perspective_id`, (b) writes filtered task ids matching the perspective's filter, (c) both changes land in one `UIStateChange`, (d) unknown perspective id returns a clean `ExecutionFailed` error.\n+- [x] Rust integration test (same crate or `swissarmyhammer-kanban/tests/`) that dispatches `perspective.switch` via the command registry end-to-end and asserts the resulting UIState snapshot. (`switch_perspective_dispatches_via_registry_end_to_end` pulls the handler from `register_commands()` and drives it through to the UIState assertion.)\n+- [x] `kanban-app/ui/src/lib/perspective-context.test.tsx` — update the existing \"dispatches ui.perspective.set to backend\" test to assert dispatch of `perspective.switch` with `{ args: { perspective_id } }` and no `\"ui.perspective.set\"` IPC. Delete tests that assert the old command name.\n+- [x] Add a test in `kanban-app/ui/src/components/perspective-container.test.tsx` (create if absent) that mounts `PerspectiveContainer` and asserts it does NOT call `refreshEntities` on perspective change — the filtered list comes from UIState.\n+- [x] Add an integration-style test in `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` that clicks a tab, mocks the `dispatch_command` invoke to pend for ~50ms, and asserts `role=\"progressbar\"` is visible during the pending window. (Landed as a sibling file `perspective-tab-bar.progress.test.tsx` so the busy-bit wiring lives next to the existing tab-bar tests without overgrowing the main file.)\n+- [x] Commands to run: `cargo nextest run -p swissarmyhammer-kanban`, `bun run test -- perspective-context perspective-container perspective-tab-bar`, `bun run typecheck`. All green.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass. #bug #unified-commands #ui-state\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -71,21 +71,21 @@\n\n ## Acceptance Criteria\n\n-- [x] `perspective.switch` command exists in `perspective.yaml` and is handled by `SwitchPerspectiveCmd`; `ui.perspective.set` is removed (both YAML and handler). (Note: the prior id was already `perspective.set`, relocated from `ui.perspective.set` in 01KPY02X405QTP5ACH67THHSN8; this card removed `perspective.set` and replaced it with `perspective.switch`.)\n-- [x] Backend atomically updates `active_perspective_id` and `filtered_task_ids` on the window in a single `UIStateChange`; exactly one `ui-state-changed` event fires per click.\n-- [x] Clicking a perspective tab in the running app visibly shows the indeterminate progress bar under `NavBar` for the duration of the filter evaluation.\n-- [x] The previously-needed `useEffect` in `perspective-container.tsx` that called `refreshEntities(boardPath, activeFilter)` on perspective change is gone; perspective change no longer triggers a second roundtrip from the frontend.\n-- [x] Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`; switching perspectives updates what is visible without any frontend-side filter call.\n-- [x] `cargo test -p swissarmyhammer-kanban` and `bun run test` (in `kanban-app/ui`) pass. `bun run typecheck` passes. (This project actually uses `npm` for the UI workspace — equivalents `npx vitest run` and `npx tsc --noEmit` were run and are all green.)\n+- [ ] `perspective.switch` command exists in `perspective.yaml` and is handled by `SwitchPerspectiveCmd`; `ui.perspective.set` is removed (both YAML and handler).\n+- [ ] Backend atomically updates `active_perspective_id` and `filtered_task_ids` on the window in a single `UIStateChange`; exactly one `ui-state-changed` event fires per click.\n+- [ ] Clicking a perspective tab in the running app visibly shows the indeterminate progress bar under `NavBar` for the duration of the filter evaluation.\n+- [ ] The previously-needed `useEffect` in `perspective-container.tsx` that called `refreshEntities(boardPath, activeFilter)` on perspective change is gone; perspective change no longer triggers a second roundtrip from the frontend.\n+- [ ] Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`; switching perspectives updates what is visible without any frontend-side filter call.\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `bun run test` (in `kanban-app/ui`) pass. `bun run typecheck` passes.\n\n ## Tests\n\n-- [x] `swissarmyhammer-kanban/src/commands/perspective_commands.rs` — add unit tests for `SwitchPerspectiveCmd` covering: (a) sets `active_perspective_id`, (b) writes filtered task ids matching the perspective's filter, (c) both changes land in one `UIStateChange`, (d) unknown perspective id returns a clean `ExecutionFailed` error.\n-- [x] Rust integration test (same crate or `swissarmyhammer-kanban/tests/`) that dispatches `perspective.switch` via the command registry end-to-end and asserts the resulting UIState snapshot. (`switch_perspective_dispatches_via_registry_end_to_end` pulls the handler from `register_commands()` and drives it through to the UIState assertion.)\n-- [x] `kanban-app/ui/src/lib/perspective-context.test.tsx` — update the existing \"dispatches ui.perspective.set to backend\" test to assert dispatch of `perspective.switch` with `{ args: { perspective_id } }` and no `\"ui.perspective.set\"` IPC. Delete tests that assert the old command name.\n-- [x] Add a test in `kanban-app/ui/src/components/perspective-container.test.tsx` (create if absent) that mounts `PerspectiveContainer` and asserts it does NOT call `refreshEntities` on perspective change — the filtered list comes from UIState.\n-- [x] Add an integration-style test in `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` that clicks a tab, mocks the `dispatch_command` invoke to pend for ~50ms, and asserts `role=\"progressbar\"` is visible during the pending window. (Landed as a sibling file `perspective-tab-bar.progress.test.tsx` so the busy-bit wiring lives next to the existing tab-bar tests without overgrowing the main file.)\n-- [x] Commands to run: `cargo nextest run -p swissarmyhammer-kanban`, `bun run test -- perspective-context perspective-container perspective-tab-bar`, `bun run typecheck`. All green.\n+- [ ] `swissarmyhammer-kanban/src/commands/perspective_commands.rs` — add unit tests for `SwitchPerspectiveCmd` covering: (a) sets `active_perspective_id`, (b) writes filtered task ids matching the perspective's filter, (c) both changes land in one `UIStateChange`, (d) unknown perspective id returns a clean `ExecutionFailed` error.\n+- [ ] Rust integration test (same crate or `swissarmyhammer-kanban/tests/`) that dispatches `perspective.switch` via the command registry end-to-end and asserts the resulting UIState snapshot.\n+- [ ] `kanban-app/ui/src/lib/perspective-context.test.tsx` — update the existing \"dispatches ui.perspective.set to backend\" test to assert dispatch of `perspective.switch` with `{ args: { perspective_id } }` and no `\"ui.perspective.set\"` IPC. Delete tests that assert the old command name.\n+- [ ] Add a test in `kanban-app/ui/src/components/perspective-container.test.tsx` (create if absent) that mounts `PerspectiveContainer` and asserts it does NOT call `refreshEntities` on perspective change — the filtered list comes from UIState.\n+- [ ] Add an integration-style test in `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` that clicks a tab, mocks the `dispatch_command` invoke to pend for ~50ms, and asserts `role=\"progressbar\"` is visible during the pending window. (This replaces the older \"doesn't show busy\" smoke concern with a real assertion.)\n+- [ ] Commands to run: `cargo nextest run -p swissarmyhammer-kanban`, `bun run test -- perspective-context perspective-container perspective-tab-bar`, `bun run typecheck`. All green.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass. #bug #unified-commands #ui-state\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRM4YR0EZF8RE8YY1S44PZ3E","timestamp":"2026-05-14T21:05:51.118546Z","entity_type":"task","entity_id":"01KP3ERHEDP86C2JYYR7NM1593","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -64,21 +64,21 @@\n\n ## Acceptance Criteria\n\n-- [ ] `perspective.switch` command exists in `perspective.yaml` and is handled by `SwitchPerspectiveCmd`; `ui.perspective.set` is removed (both YAML and handler).\n-- [ ] Backend atomically updates `active_perspective_id` and `filtered_task_ids` on the window in a single `UIStateChange`; exactly one `ui-state-changed` event fires per click.\n-- [ ] Clicking a perspective tab in the running app visibly shows the indeterminate progress bar under `NavBar` for the duration of the filter evaluation.\n-- [ ] The previously-needed `useEffect` in `perspective-container.tsx` that called `refreshEntities(boardPath, activeFilter)` on perspective change is gone; perspective change no longer triggers a second roundtrip from the frontend.\n-- [ ] Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`; switching perspectives updates what is visible without any frontend-side filter call.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `bun run test` (in `kanban-app/ui`) pass. `bun run typecheck` passes.\n+- [x] `perspective.switch` command exists in `perspective.yaml` and is handled by `SwitchPerspectiveCmd`; `ui.perspective.set` is removed (both YAML and handler). (Note: the prior id was already `perspective.set`, relocated from `ui.perspective.set` in 01KPY02X405QTP5ACH67THHSN8; this card removed `perspective.set` and replaced it with `perspective.switch`.)\n+- [x] Backend atomically updates `active_perspective_id` and `filtered_task_ids` on the window in a single `UIStateChange`; exactly one `ui-state-changed` event fires per click.\n+- [x] Clicking a perspective tab in the running app visibly shows the indeterminate progress bar under `NavBar` for the duration of the filter evaluation.\n+- [x] The previously-needed `useEffect` in `perspective-container.tsx` that called `refreshEntities(boardPath, activeFilter)` on perspective change is gone; perspective change no longer triggers a second roundtrip from the frontend.\n+- [x] Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`; switching perspectives updates what is visible without any frontend-side filter call.\n+- [x] `cargo test -p swissarmyhammer-kanban` and `bun run test` (in `kanban-app/ui`) pass. `bun run typecheck` passes. (This project actually uses `npm` for the UI workspace — equivalents `npx vitest run` and `npx tsc --noEmit` were run and are all green.)\n\n ## Tests\n\n-- [ ] `swissarmyhammer-kanban/src/commands/perspective_commands.rs` — add unit tests for `SwitchPerspectiveCmd` covering: (a) sets `active_perspective_id`, (b) writes filtered task ids matching the perspective's filter, (c) both changes land in one `UIStateChange`, (d) unknown perspective id returns a clean `ExecutionFailed` error.\n-- [ ] Rust integration test (same crate or `swissarmyhammer-kanban/tests/`) that dispatches `perspective.switch` via the command registry end-to-end and asserts the resulting UIState snapshot.\n-- [ ] `kanban-app/ui/src/lib/perspective-context.test.tsx` — update the existing \"dispatches ui.perspective.set to backend\" test to assert dispatch of `perspective.switch` with `{ args: { perspective_id } }` and no `\"ui.perspective.set\"` IPC. Delete tests that assert the old command name.\n-- [ ] Add a test in `kanban-app/ui/src/components/perspective-container.test.tsx` (create if absent) that mounts `PerspectiveContainer` and asserts it does NOT call `refreshEntities` on perspective change — the filtered list comes from UIState.\n-- [ ] Add an integration-style test in `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` that clicks a tab, mocks the `dispatch_command` invoke to pend for ~50ms, and asserts `role=\"progressbar\"` is visible during the pending window. (This replaces the older \"doesn't show busy\" smoke concern with a real assertion.)\n-- [ ] Commands to run: `cargo nextest run -p swissarmyhammer-kanban`, `bun run test -- perspective-context perspective-container perspective-tab-bar`, `bun run typecheck`. All green.\n+- [x] `swissarmyhammer-kanban/src/commands/perspective_commands.rs` — add unit tests for `SwitchPerspectiveCmd` covering: (a) sets `active_perspective_id`, (b) writes filtered task ids matching the perspective's filter, (c) both changes land in one `UIStateChange`, (d) unknown perspective id returns a clean `ExecutionFailed` error.\n+- [x] Rust integration test (same crate or `swissarmyhammer-kanban/tests/`) that dispatches `perspective.switch` via the command registry end-to-end and asserts the resulting UIState snapshot. (`switch_perspective_dispatches_via_registry_end_to_end` pulls the handler from `register_commands()` and drives it through to the UIState assertion.)\n+- [x] `kanban-app/ui/src/lib/perspective-context.test.tsx` — update the existing \"dispatches ui.perspective.set to backend\" test to assert dispatch of `perspective.switch` with `{ args: { perspective_id } }` and no `\"ui.perspective.set\"` IPC. Delete tests that assert the old command name.\n+- [x] Add a test in `kanban-app/ui/src/components/perspective-container.test.tsx` (create if absent) that mounts `PerspectiveContainer` and asserts it does NOT call `refreshEntities` on perspective change — the filtered list comes from UIState.\n+- [x] Add an integration-style test in `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` that clicks a tab, mocks the `dispatch_command` invoke to pend for ~50ms, and asserts `role=\"progressbar\"` is visible during the pending window. (Landed as a sibling file `perspective-tab-bar.progress.test.tsx` so the busy-bit wiring lives next to the existing tab-bar tests without overgrowing the main file.)\n+- [x] Commands to run: `cargo nextest run -p swissarmyhammer-kanban`, `bun run test -- perspective-context perspective-container perspective-tab-bar`, `bun run typecheck`. All green.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass. #bug #unified-commands #ui-state\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -64,21 +64,21 @@\n\n ## Acceptance Criteria\n\n-- [x] `perspective.switch` command exists in `perspective.yaml` and is handled by `SwitchPerspectiveCmd`; `ui.perspective.set` is removed (both YAML and handler). (Note: the prior id was already `perspective.set`, relocated from `ui.perspective.set` in 01KPY02X405QTP5ACH67THHSN8; this card removed `perspective.set` and replaced it with `perspective.switch`.)\n-- [x] Backend atomically updates `active_perspective_id` and `filtered_task_ids` on the window in a single `UIStateChange`; exactly one `ui-state-changed` event fires per click.\n-- [x] Clicking a perspective tab in the running app visibly shows the indeterminate progress bar under `NavBar` for the duration of the filter evaluation.\n-- [x] The previously-needed `useEffect` in `perspective-container.tsx` that called `refreshEntities(boardPath, activeFilter)` on perspective change is gone; perspective change no longer triggers a second roundtrip from the frontend.\n-- [x] Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`; switching perspectives updates what is visible without any frontend-side filter call.\n-- [x] `cargo test -p swissarmyhammer-kanban` and `bun run test` (in `kanban-app/ui`) pass. `bun run typecheck` passes. (This project actually uses `npm` for the UI workspace — equivalents `npx vitest run` and `npx tsc --noEmit` were run and are all green.)\n+- [ ] `perspective.switch` command exists in `perspective.yaml` and is handled by `SwitchPerspectiveCmd`; `ui.perspective.set` is removed (both YAML and handler).\n+- [ ] Backend atomically updates `active_perspective_id` and `filtered_task_ids` on the window in a single `UIStateChange`; exactly one `ui-state-changed` event fires per click.\n+- [ ] Clicking a perspective tab in the running app visibly shows the indeterminate progress bar under `NavBar` for the duration of the filter evaluation.\n+- [ ] The previously-needed `useEffect` in `perspective-container.tsx` that called `refreshEntities(boardPath, activeFilter)` on perspective change is gone; perspective change no longer triggers a second roundtrip from the frontend.\n+- [ ] Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`; switching perspectives updates what is visible without any frontend-side filter call.\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `bun run test` (in `kanban-app/ui`) pass. `bun run typecheck` passes.\n\n ## Tests\n\n-- [x] `swissarmyhammer-kanban/src/commands/perspective_commands.rs` — add unit tests for `SwitchPerspectiveCmd` covering: (a) sets `active_perspective_id`, (b) writes filtered task ids matching the perspective's filter, (c) both changes land in one `UIStateChange`, (d) unknown perspective id returns a clean `ExecutionFailed` error.\n-- [x] Rust integration test (same crate or `swissarmyhammer-kanban/tests/`) that dispatches `perspective.switch` via the command registry end-to-end and asserts the resulting UIState snapshot. (`switch_perspective_dispatches_via_registry_end_to_end` pulls the handler from `register_commands()` and drives it through to the UIState assertion.)\n-- [x] `kanban-app/ui/src/lib/perspective-context.test.tsx` — update the existing \"dispatches ui.perspective.set to backend\" test to assert dispatch of `perspective.switch` with `{ args: { perspective_id } }` and no `\"ui.perspective.set\"` IPC. Delete tests that assert the old command name.\n-- [x] Add a test in `kanban-app/ui/src/components/perspective-container.test.tsx` (create if absent) that mounts `PerspectiveContainer` and asserts it does NOT call `refreshEntities` on perspective change — the filtered list comes from UIState.\n-- [x] Add an integration-style test in `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` that clicks a tab, mocks the `dispatch_command` invoke to pend for ~50ms, and asserts `role=\"progressbar\"` is visible during the pending window. (Landed as a sibling file `perspective-tab-bar.progress.test.tsx` so the busy-bit wiring lives next to the existing tab-bar tests without overgrowing the main file.)\n-- [x] Commands to run: `cargo nextest run -p swissarmyhammer-kanban`, `bun run test -- perspective-context perspective-container perspective-tab-bar`, `bun run typecheck`. All green.\n+- [ ] `swissarmyhammer-kanban/src/commands/perspective_commands.rs` — add unit tests for `SwitchPerspectiveCmd` covering: (a) sets `active_perspective_id`, (b) writes filtered task ids matching the perspective's filter, (c) both changes land in one `UIStateChange`, (d) unknown perspective id returns a clean `ExecutionFailed` error.\n+- [ ] Rust integration test (same crate or `swissarmyhammer-kanban/tests/`) that dispatches `perspective.switch` via the command registry end-to-end and asserts the resulting UIState snapshot.\n+- [ ] `kanban-app/ui/src/lib/perspective-context.test.tsx` — update the existing \"dispatches ui.perspective.set to backend\" test to assert dispatch of `perspective.switch` with `{ args: { perspective_id } }` and no `\"ui.perspective.set\"` IPC. Delete tests that assert the old command name.\n+- [ ] Add a test in `kanban-app/ui/src/components/perspective-container.test.tsx` (create if absent) that mounts `PerspectiveContainer` and asserts it does NOT call `refreshEntities` on perspective change — the filtered list comes from UIState.\n+- [ ] Add an integration-style test in `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` that clicks a tab, mocks the `dispatch_command` invoke to pend for ~50ms, and asserts `role=\"progressbar\"` is visible during the pending window. (This replaces the older \"doesn't show busy\" smoke concern with a real assertion.)\n+- [ ] Commands to run: `cargo nextest run -p swissarmyhammer-kanban`, `bun run test -- perspective-context perspective-container perspective-tab-bar`, `bun run typecheck`. All green.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass. #bug #unified-commands #ui-state\n\\ No newline at end of file\n"}]]}
{"id":"01KRM51674N100NG1KH9XT5ZVV","timestamp":"2026-05-14T21:07:11.204347Z","op":"update","item_id":"01KP3ERHEDP86C2JYYR7NM1593","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - wballard\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - wballard\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n","transaction_id":null}
{"id":"01KRM516754TVNAND4W667J7DA","timestamp":"2026-05-14T21:07:11.205057Z","entity_type":"task","entity_id":"01KP3ERHEDP86C2JYYR7NM1593","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KRM5HPRMAG4ZYGJYYEB9AWPA","timestamp":"2026-05-14T21:16:12.436943Z","op":"update","item_id":"01KP3ERHEDP86C2JYYR7NM1593","forward_patch":"--- original\n+++ modified\n@@ -88,4 +88,20 @@\n - [x] Commands to run: `cargo nextest run -p swissarmyhammer-kanban`, `bun run test -- perspective-context perspective-container perspective-tab-bar`, `bun run typecheck`. All green.\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #bug #unified-commands #ui-state\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #bug #unified-commands #ui-state\n+\n+## Review Findings (2026-05-14 17:15)\n+\n+### Blockers\n+- [ ] `swissarmyhammer-commands/src/ui_state.rs:1212` and `kanban-app/ui/src/components/view-container.tsx:78-85` — **Boot back-compat regression: persisted `active_perspective_id` shows empty board after restart.** `WindowState::default()` initializes `filtered_task_ids: Vec::new()`. The field is `#[serde(skip)]` so it does not load from disk, but `to_json` ALWAYS injects `filtered_task_ids: []` on every snapshot once a window entry exists (line 1212 unconditionally writes the key). The window entry is created very early on boot (e.g. `set_window_board` runs as soon as a board is opened — see `kanban-app/src/commands.rs:949`). Result: after restart with `active_perspective_id == \"p1\"` persisted, the very first UIState snapshot the frontend sees has `filtered_task_ids: []`. The tri-state in `view-container.tsx:78-85` interprets `[]` as \"filter matched zero tasks → show no rows\", AND `useAutoSelectActivePerspective` in `perspective-context.tsx:195-196` short-circuits because the stored id is `stillValid` — so it does NOT redispatch `perspective.switch`. **Users see an empty board on every restart until they manually click a tab.** The `filtered_task_ids_not_persisted` test (ui_state.rs:2687-2709) actually exercises this exact path but only asserts the Rust side; the frontend consequence is not covered. Fix options: (a) omit the `filtered_task_ids` key from `to_json` when the underlying Vec equals the default and `active_perspective_id` has changed since boot (so JSON `undefined` triggers the back-compat branch); (b) on board open / window-board assignment, dispatch `perspective.switch` for the stored `active_perspective_id` to repopulate; (c) treat empty `filtered_task_ids` AND non-empty `active_perspective_id` as \"stale, recompute\" in the auto-select hook.\n+\n+### Warnings\n+- [ ] `kanban-app/ui/src/components/view-container.tsx:78-96` — **GridView silently ignores perspective filter** (regression). The `tasks` memo is computed in `ViewContainer` and passed only to `<GroupedBoardView board={board} tasks={tasks} />` (line 122). When `activeView.kind === \"grid\"`, the renderer is `<GridView view={activeView} />` (line 126) with no `tasks` prop. Inside `GridView`, `useGridData` reads entities directly from `useEntityStore().getEntities(entityType)` (grid-view.tsx:124) and never consults `filtered_task_ids`. The old code path replaced the entity store via `refreshEntities(boardPath, activeFilter)`, so the grid view inherited filtering for free. The new code path leaves the store unfiltered and only intersects in the BoardView wrapper. Switching perspectives in a grid view (with a non-empty filter) will now show all tasks, contradicting acceptance criterion \"Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`\". Fix: lift the intersection up a level (intersect in `useEntityStore` or in `useGridData`) so it also covers GridView, or pass the filtered task list into GridView the same way it's passed to GroupedBoardView.\n+- [ ] `swissarmyhammer-commands/src/ui_state.rs:1211-1214` and `kanban-app/ui/src/lib/ui-state-context.tsx:28-40` — **Tri-state contract is documented but unreachable.** The TS doc says `undefined` = \"fresh launch, legacy snapshot → show all\", but the backend's `to_json` unconditionally injects `filtered_task_ids: <ws.filtered_task_ids>`. There is no code path that emits a snapshot WITHOUT this key once a window entry exists. The `undefined` branch in `view-container.tsx:82` will only ever trigger in tests that omit the field manually or on the absolute first frame before any window state is touched. This is the root cause of the blocker above. Either honor the contract (omit the key when default) or change the contract to \"empty = show all when no active perspective is set\" (with the obvious caveat that \"filter matched nothing\" then becomes indistinguishable, but you can disambiguate via `active_perspective_id == \"\" || filter == \"\"`).\n+\n+### Nits\n+- [ ] `swissarmyhammer-kanban/src/scope_commands.rs:387-388` — Docstring typo: \"01KP3ERHEDP86C2JYYR7NM1593 then replaced `perspective.switch` with `perspective.switch`\". First occurrence should be `perspective.set`.\n+- [ ] `swissarmyhammer-perspectives/src/perspective_info.rs:39` — Stale docstring on `PerspectiveInfo`: \"produce a palette row that dispatches `perspective.set` with a pre-filled `perspective_id`\". Palette rows now dispatch `perspective.switch`; update to match.\n+- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx:793` — Variable named `dispatchPerspectiveSet` but bound to `useDispatchCommand(\"perspective.switch\")`. Rename to `dispatchPerspectiveSwitch` to match the command id.\n+- [ ] `kanban-app/ui/src/components/perspective-tab-bar.enter-rename.spatial.test.tsx:21,35` — Test-file prose still describes the click as dispatching `perspective.set`. Code at line 507 correctly asserts `perspective.switch`; update the leading comments to match.\n+- [ ] `swissarmyhammer-kanban/src/commands/perspective_commands.rs:902-905` — Docstring for `SwitchPerspectiveCmd` says \"Reuses the shared [`filter_task_ids`] in `task_helpers`\" but the implementation actually calls a private helper `evaluate_perspective_filter` (line 963) that wraps `filter_task_ids`. Either the rustdoc link should target `evaluate_perspective_filter` or the prose should clarify the indirection.\n","reverse_patch":"--- original\n+++ modified\n@@ -88,20 +88,4 @@\n - [x] Commands to run: `cargo nextest run -p swissarmyhammer-kanban`, `bun run test -- perspective-context perspective-container perspective-tab-bar`, `bun run typecheck`. All green.\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #bug #unified-commands #ui-state\n-\n-## Review Findings (2026-05-14 17:15)\n-\n-### Blockers\n-- [ ] `swissarmyhammer-commands/src/ui_state.rs:1212` and `kanban-app/ui/src/components/view-container.tsx:78-85` — **Boot back-compat regression: persisted `active_perspective_id` shows empty board after restart.** `WindowState::default()` initializes `filtered_task_ids: Vec::new()`. The field is `#[serde(skip)]` so it does not load from disk, but `to_json` ALWAYS injects `filtered_task_ids: []` on every snapshot once a window entry exists (line 1212 unconditionally writes the key). The window entry is created very early on boot (e.g. `set_window_board` runs as soon as a board is opened — see `kanban-app/src/commands.rs:949`). Result: after restart with `active_perspective_id == \"p1\"` persisted, the very first UIState snapshot the frontend sees has `filtered_task_ids: []`. The tri-state in `view-container.tsx:78-85` interprets `[]` as \"filter matched zero tasks → show no rows\", AND `useAutoSelectActivePerspective` in `perspective-context.tsx:195-196` short-circuits because the stored id is `stillValid` — so it does NOT redispatch `perspective.switch`. **Users see an empty board on every restart until they manually click a tab.** The `filtered_task_ids_not_persisted` test (ui_state.rs:2687-2709) actually exercises this exact path but only asserts the Rust side; the frontend consequence is not covered. Fix options: (a) omit the `filtered_task_ids` key from `to_json` when the underlying Vec equals the default and `active_perspective_id` has changed since boot (so JSON `undefined` triggers the back-compat branch); (b) on board open / window-board assignment, dispatch `perspective.switch` for the stored `active_perspective_id` to repopulate; (c) treat empty `filtered_task_ids` AND non-empty `active_perspective_id` as \"stale, recompute\" in the auto-select hook.\n-\n-### Warnings\n-- [ ] `kanban-app/ui/src/components/view-container.tsx:78-96` — **GridView silently ignores perspective filter** (regression). The `tasks` memo is computed in `ViewContainer` and passed only to `<GroupedBoardView board={board} tasks={tasks} />` (line 122). When `activeView.kind === \"grid\"`, the renderer is `<GridView view={activeView} />` (line 126) with no `tasks` prop. Inside `GridView`, `useGridData` reads entities directly from `useEntityStore().getEntities(entityType)` (grid-view.tsx:124) and never consults `filtered_task_ids`. The old code path replaced the entity store via `refreshEntities(boardPath, activeFilter)`, so the grid view inherited filtering for free. The new code path leaves the store unfiltered and only intersects in the BoardView wrapper. Switching perspectives in a grid view (with a non-empty filter) will now show all tasks, contradicting acceptance criterion \"Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`\". Fix: lift the intersection up a level (intersect in `useEntityStore` or in `useGridData`) so it also covers GridView, or pass the filtered task list into GridView the same way it's passed to GroupedBoardView.\n-- [ ] `swissarmyhammer-commands/src/ui_state.rs:1211-1214` and `kanban-app/ui/src/lib/ui-state-context.tsx:28-40` — **Tri-state contract is documented but unreachable.** The TS doc says `undefined` = \"fresh launch, legacy snapshot → show all\", but the backend's `to_json` unconditionally injects `filtered_task_ids: <ws.filtered_task_ids>`. There is no code path that emits a snapshot WITHOUT this key once a window entry exists. The `undefined` branch in `view-container.tsx:82` will only ever trigger in tests that omit the field manually or on the absolute first frame before any window state is touched. This is the root cause of the blocker above. Either honor the contract (omit the key when default) or change the contract to \"empty = show all when no active perspective is set\" (with the obvious caveat that \"filter matched nothing\" then becomes indistinguishable, but you can disambiguate via `active_perspective_id == \"\" || filter == \"\"`).\n-\n-### Nits\n-- [ ] `swissarmyhammer-kanban/src/scope_commands.rs:387-388` — Docstring typo: \"01KP3ERHEDP86C2JYYR7NM1593 then replaced `perspective.switch` with `perspective.switch`\". First occurrence should be `perspective.set`.\n-- [ ] `swissarmyhammer-perspectives/src/perspective_info.rs:39` — Stale docstring on `PerspectiveInfo`: \"produce a palette row that dispatches `perspective.set` with a pre-filled `perspective_id`\". Palette rows now dispatch `perspective.switch`; update to match.\n-- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx:793` — Variable named `dispatchPerspectiveSet` but bound to `useDispatchCommand(\"perspective.switch\")`. Rename to `dispatchPerspectiveSwitch` to match the command id.\n-- [ ] `kanban-app/ui/src/components/perspective-tab-bar.enter-rename.spatial.test.tsx:21,35` — Test-file prose still describes the click as dispatching `perspective.set`. Code at line 507 correctly asserts `perspective.switch`; update the leading comments to match.\n-- [ ] `swissarmyhammer-kanban/src/commands/perspective_commands.rs:902-905` — Docstring for `SwitchPerspectiveCmd` says \"Reuses the shared [`filter_task_ids`] in `task_helpers`\" but the implementation actually calls a private helper `evaluate_perspective_filter` (line 963) that wraps `filter_task_ids`. Either the rustdoc link should target `evaluate_perspective_filter` or the prose should clarify the indirection.\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #bug #unified-commands #ui-state\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRM5HPRNZQC50YP0HBFCYKKR","timestamp":"2026-05-14T21:16:12.437457Z","entity_type":"task","entity_id":"01KP3ERHEDP86C2JYYR7NM1593","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -81,4 +81,20 @@\n - [x] Commands to run: `cargo nextest run -p swissarmyhammer-kanban`, `bun run test -- perspective-context perspective-container perspective-tab-bar`, `bun run typecheck`. All green.\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #bug #unified-commands #ui-state\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #bug #unified-commands #ui-state\n+\n+## Review Findings (2026-05-14 17:15)\n+\n+### Blockers\n+- [ ] `swissarmyhammer-commands/src/ui_state.rs:1212` and `kanban-app/ui/src/components/view-container.tsx:78-85` — **Boot back-compat regression: persisted `active_perspective_id` shows empty board after restart.** `WindowState::default()` initializes `filtered_task_ids: Vec::new()`. The field is `#[serde(skip)]` so it does not load from disk, but `to_json` ALWAYS injects `filtered_task_ids: []` on every snapshot once a window entry exists (line 1212 unconditionally writes the key). The window entry is created very early on boot (e.g. `set_window_board` runs as soon as a board is opened — see `kanban-app/src/commands.rs:949`). Result: after restart with `active_perspective_id == \"p1\"` persisted, the very first UIState snapshot the frontend sees has `filtered_task_ids: []`. The tri-state in `view-container.tsx:78-85` interprets `[]` as \"filter matched zero tasks → show no rows\", AND `useAutoSelectActivePerspective` in `perspective-context.tsx:195-196` short-circuits because the stored id is `stillValid` — so it does NOT redispatch `perspective.switch`. **Users see an empty board on every restart until they manually click a tab.** The `filtered_task_ids_not_persisted` test (ui_state.rs:2687-2709) actually exercises this exact path but only asserts the Rust side; the frontend consequence is not covered. Fix options: (a) omit the `filtered_task_ids` key from `to_json` when the underlying Vec equals the default and `active_perspective_id` has changed since boot (so JSON `undefined` triggers the back-compat branch); (b) on board open / window-board assignment, dispatch `perspective.switch` for the stored `active_perspective_id` to repopulate; (c) treat empty `filtered_task_ids` AND non-empty `active_perspective_id` as \"stale, recompute\" in the auto-select hook.\n+\n+### Warnings\n+- [ ] `kanban-app/ui/src/components/view-container.tsx:78-96` — **GridView silently ignores perspective filter** (regression). The `tasks` memo is computed in `ViewContainer` and passed only to `<GroupedBoardView board={board} tasks={tasks} />` (line 122). When `activeView.kind === \"grid\"`, the renderer is `<GridView view={activeView} />` (line 126) with no `tasks` prop. Inside `GridView`, `useGridData` reads entities directly from `useEntityStore().getEntities(entityType)` (grid-view.tsx:124) and never consults `filtered_task_ids`. The old code path replaced the entity store via `refreshEntities(boardPath, activeFilter)`, so the grid view inherited filtering for free. The new code path leaves the store unfiltered and only intersects in the BoardView wrapper. Switching perspectives in a grid view (with a non-empty filter) will now show all tasks, contradicting acceptance criterion \"Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`\". Fix: lift the intersection up a level (intersect in `useEntityStore` or in `useGridData`) so it also covers GridView, or pass the filtered task list into GridView the same way it's passed to GroupedBoardView.\n+- [ ] `swissarmyhammer-commands/src/ui_state.rs:1211-1214` and `kanban-app/ui/src/lib/ui-state-context.tsx:28-40` — **Tri-state contract is documented but unreachable.** The TS doc says `undefined` = \"fresh launch, legacy snapshot → show all\", but the backend's `to_json` unconditionally injects `filtered_task_ids: <ws.filtered_task_ids>`. There is no code path that emits a snapshot WITHOUT this key once a window entry exists. The `undefined` branch in `view-container.tsx:82` will only ever trigger in tests that omit the field manually or on the absolute first frame before any window state is touched. This is the root cause of the blocker above. Either honor the contract (omit the key when default) or change the contract to \"empty = show all when no active perspective is set\" (with the obvious caveat that \"filter matched nothing\" then becomes indistinguishable, but you can disambiguate via `active_perspective_id == \"\" || filter == \"\"`).\n+\n+### Nits\n+- [ ] `swissarmyhammer-kanban/src/scope_commands.rs:387-388` — Docstring typo: \"01KP3ERHEDP86C2JYYR7NM1593 then replaced `perspective.switch` with `perspective.switch`\". First occurrence should be `perspective.set`.\n+- [ ] `swissarmyhammer-perspectives/src/perspective_info.rs:39` — Stale docstring on `PerspectiveInfo`: \"produce a palette row that dispatches `perspective.set` with a pre-filled `perspective_id`\". Palette rows now dispatch `perspective.switch`; update to match.\n+- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx:793` — Variable named `dispatchPerspectiveSet` but bound to `useDispatchCommand(\"perspective.switch\")`. Rename to `dispatchPerspectiveSwitch` to match the command id.\n+- [ ] `kanban-app/ui/src/components/perspective-tab-bar.enter-rename.spatial.test.tsx:21,35` — Test-file prose still describes the click as dispatching `perspective.set`. Code at line 507 correctly asserts `perspective.switch`; update the leading comments to match.\n+- [ ] `swissarmyhammer-kanban/src/commands/perspective_commands.rs:902-905` — Docstring for `SwitchPerspectiveCmd` says \"Reuses the shared [`filter_task_ids`] in `task_helpers`\" but the implementation actually calls a private helper `evaluate_perspective_filter` (line 963) that wraps `filter_task_ids`. Either the rustdoc link should target `evaluate_perspective_filter` or the prose should clarify the indirection.\n","reverse_patch":"--- original\n+++ modified\n@@ -81,20 +81,4 @@\n - [x] Commands to run: `cargo nextest run -p swissarmyhammer-kanban`, `bun run test -- perspective-context perspective-container perspective-tab-bar`, `bun run typecheck`. All green.\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #bug #unified-commands #ui-state\n-\n-## Review Findings (2026-05-14 17:15)\n-\n-### Blockers\n-- [ ] `swissarmyhammer-commands/src/ui_state.rs:1212` and `kanban-app/ui/src/components/view-container.tsx:78-85` — **Boot back-compat regression: persisted `active_perspective_id` shows empty board after restart.** `WindowState::default()` initializes `filtered_task_ids: Vec::new()`. The field is `#[serde(skip)]` so it does not load from disk, but `to_json` ALWAYS injects `filtered_task_ids: []` on every snapshot once a window entry exists (line 1212 unconditionally writes the key). The window entry is created very early on boot (e.g. `set_window_board` runs as soon as a board is opened — see `kanban-app/src/commands.rs:949`). Result: after restart with `active_perspective_id == \"p1\"` persisted, the very first UIState snapshot the frontend sees has `filtered_task_ids: []`. The tri-state in `view-container.tsx:78-85` interprets `[]` as \"filter matched zero tasks → show no rows\", AND `useAutoSelectActivePerspective` in `perspective-context.tsx:195-196` short-circuits because the stored id is `stillValid` — so it does NOT redispatch `perspective.switch`. **Users see an empty board on every restart until they manually click a tab.** The `filtered_task_ids_not_persisted` test (ui_state.rs:2687-2709) actually exercises this exact path but only asserts the Rust side; the frontend consequence is not covered. Fix options: (a) omit the `filtered_task_ids` key from `to_json` when the underlying Vec equals the default and `active_perspective_id` has changed since boot (so JSON `undefined` triggers the back-compat branch); (b) on board open / window-board assignment, dispatch `perspective.switch` for the stored `active_perspective_id` to repopulate; (c) treat empty `filtered_task_ids` AND non-empty `active_perspective_id` as \"stale, recompute\" in the auto-select hook.\n-\n-### Warnings\n-- [ ] `kanban-app/ui/src/components/view-container.tsx:78-96` — **GridView silently ignores perspective filter** (regression). The `tasks` memo is computed in `ViewContainer` and passed only to `<GroupedBoardView board={board} tasks={tasks} />` (line 122). When `activeView.kind === \"grid\"`, the renderer is `<GridView view={activeView} />` (line 126) with no `tasks` prop. Inside `GridView`, `useGridData` reads entities directly from `useEntityStore().getEntities(entityType)` (grid-view.tsx:124) and never consults `filtered_task_ids`. The old code path replaced the entity store via `refreshEntities(boardPath, activeFilter)`, so the grid view inherited filtering for free. The new code path leaves the store unfiltered and only intersects in the BoardView wrapper. Switching perspectives in a grid view (with a non-empty filter) will now show all tasks, contradicting acceptance criterion \"Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`\". Fix: lift the intersection up a level (intersect in `useEntityStore` or in `useGridData`) so it also covers GridView, or pass the filtered task list into GridView the same way it's passed to GroupedBoardView.\n-- [ ] `swissarmyhammer-commands/src/ui_state.rs:1211-1214` and `kanban-app/ui/src/lib/ui-state-context.tsx:28-40` — **Tri-state contract is documented but unreachable.** The TS doc says `undefined` = \"fresh launch, legacy snapshot → show all\", but the backend's `to_json` unconditionally injects `filtered_task_ids: <ws.filtered_task_ids>`. There is no code path that emits a snapshot WITHOUT this key once a window entry exists. The `undefined` branch in `view-container.tsx:82` will only ever trigger in tests that omit the field manually or on the absolute first frame before any window state is touched. This is the root cause of the blocker above. Either honor the contract (omit the key when default) or change the contract to \"empty = show all when no active perspective is set\" (with the obvious caveat that \"filter matched nothing\" then becomes indistinguishable, but you can disambiguate via `active_perspective_id == \"\" || filter == \"\"`).\n-\n-### Nits\n-- [ ] `swissarmyhammer-kanban/src/scope_commands.rs:387-388` — Docstring typo: \"01KP3ERHEDP86C2JYYR7NM1593 then replaced `perspective.switch` with `perspective.switch`\". First occurrence should be `perspective.set`.\n-- [ ] `swissarmyhammer-perspectives/src/perspective_info.rs:39` — Stale docstring on `PerspectiveInfo`: \"produce a palette row that dispatches `perspective.set` with a pre-filled `perspective_id`\". Palette rows now dispatch `perspective.switch`; update to match.\n-- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx:793` — Variable named `dispatchPerspectiveSet` but bound to `useDispatchCommand(\"perspective.switch\")`. Rename to `dispatchPerspectiveSwitch` to match the command id.\n-- [ ] `kanban-app/ui/src/components/perspective-tab-bar.enter-rename.spatial.test.tsx:21,35` — Test-file prose still describes the click as dispatching `perspective.set`. Code at line 507 correctly asserts `perspective.switch`; update the leading comments to match.\n-- [ ] `swissarmyhammer-kanban/src/commands/perspective_commands.rs:902-905` — Docstring for `SwitchPerspectiveCmd` says \"Reuses the shared [`filter_task_ids`] in `task_helpers`\" but the implementation actually calls a private helper `evaluate_perspective_filter` (line 963) that wraps `filter_task_ids`. Either the rustdoc link should target `evaluate_perspective_filter` or the prose should clarify the indirection.\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #bug #unified-commands #ui-state\n\\ No newline at end of file\n"}]]}
{"id":"01KRM5NRC5HYCVY8FVE68JVG5K","timestamp":"2026-05-14T21:18:25.157960Z","op":"update","item_id":"01KP3ERHEDP86C2JYYR7NM1593","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - wballard\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - wballard\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n","transaction_id":null}
{"id":"01KRM5NRC6PMHA8XCGRW8BADX8","timestamp":"2026-05-14T21:18:25.158829Z","entity_type":"task","entity_id":"01KP3ERHEDP86C2JYYR7NM1593","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}]]}
{"id":"01KRM699MWY9K9Z4NGEJZ2XM44","timestamp":"2026-05-14T21:29:05.436830Z","op":"update","item_id":"01KP3ERHEDP86C2JYYR7NM1593","forward_patch":"--- original\n+++ modified\n@@ -93,15 +93,15 @@\n ## Review Findings (2026-05-14 17:15)\n\n ### Blockers\n-- [ ] `swissarmyhammer-commands/src/ui_state.rs:1212` and `kanban-app/ui/src/components/view-container.tsx:78-85` — **Boot back-compat regression: persisted `active_perspective_id` shows empty board after restart.** `WindowState::default()` initializes `filtered_task_ids: Vec::new()`. The field is `#[serde(skip)]` so it does not load from disk, but `to_json` ALWAYS injects `filtered_task_ids: []` on every snapshot once a window entry exists (line 1212 unconditionally writes the key). The window entry is created very early on boot (e.g. `set_window_board` runs as soon as a board is opened — see `kanban-app/src/commands.rs:949`). Result: after restart with `active_perspective_id == \"p1\"` persisted, the very first UIState snapshot the frontend sees has `filtered_task_ids: []`. The tri-state in `view-container.tsx:78-85` interprets `[]` as \"filter matched zero tasks → show no rows\", AND `useAutoSelectActivePerspective` in `perspective-context.tsx:195-196` short-circuits because the stored id is `stillValid` — so it does NOT redispatch `perspective.switch`. **Users see an empty board on every restart until they manually click a tab.** The `filtered_task_ids_not_persisted` test (ui_state.rs:2687-2709) actually exercises this exact path but only asserts the Rust side; the frontend consequence is not covered. Fix options: (a) omit the `filtered_task_ids` key from `to_json` when the underlying Vec equals the default and `active_perspective_id` has changed since boot (so JSON `undefined` triggers the back-compat branch); (b) on board open / window-board assignment, dispatch `perspective.switch` for the stored `active_perspective_id` to repopulate; (c) treat empty `filtered_task_ids` AND non-empty `active_perspective_id` as \"stale, recompute\" in the auto-select hook.\n+- [x] `swissarmyhammer-commands/src/ui_state.rs:1212` and `kanban-app/ui/src/components/view-container.tsx:78-85` — **Boot back-compat regression: persisted `active_perspective_id` shows empty board after restart.** Fixed via Option (c): `useAutoSelectActivePerspective` in `kanban-app/ui/src/lib/perspective-context.tsx` now treats \"non-empty `active_perspective_id` AND `filtered_task_ids === undefined`\" as stale boot state and redispatches `perspective.switch` for the persisted id (not the first matching perspective — we honor the user's prior selection). The hook now takes a new `filtered_task_ids_defined` parameter and has two distinct repair paths: (1) empty/invalid id → switch to first matching perspective; (2) valid id but undefined filter slot → switch to the persisted id to recompute. New regression test `auto-recovers stale boot state: persisted active_perspective_id with undefined filtered_task_ids redispatches perspective.switch for the persisted id` in `perspective-context.test.tsx` mounts the provider with `windows.main = { active_perspective_id: \"p2\" }` (no `filtered_task_ids` key) and asserts `perspective.switch` is dispatched with `{ perspective_id: \"p2\" }` on mount.\n\n ### Warnings\n-- [ ] `kanban-app/ui/src/components/view-container.tsx:78-96` — **GridView silently ignores perspective filter** (regression). The `tasks` memo is computed in `ViewContainer` and passed only to `<GroupedBoardView board={board} tasks={tasks} />` (line 122). When `activeView.kind === \"grid\"`, the renderer is `<GridView view={activeView} />` (line 126) with no `tasks` prop. Inside `GridView`, `useGridData` reads entities directly from `useEntityStore().getEntities(entityType)` (grid-view.tsx:124) and never consults `filtered_task_ids`. The old code path replaced the entity store via `refreshEntities(boardPath, activeFilter)`, so the grid view inherited filtering for free. The new code path leaves the store unfiltered and only intersects in the BoardView wrapper. Switching perspectives in a grid view (with a non-empty filter) will now show all tasks, contradicting acceptance criterion \"Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`\". Fix: lift the intersection up a level (intersect in `useEntityStore` or in `useGridData`) so it also covers GridView, or pass the filtered task list into GridView the same way it's passed to GroupedBoardView.\n-- [ ] `swissarmyhammer-commands/src/ui_state.rs:1211-1214` and `kanban-app/ui/src/lib/ui-state-context.tsx:28-40` — **Tri-state contract is documented but unreachable.** The TS doc says `undefined` = \"fresh launch, legacy snapshot → show all\", but the backend's `to_json` unconditionally injects `filtered_task_ids: <ws.filtered_task_ids>`. There is no code path that emits a snapshot WITHOUT this key once a window entry exists. The `undefined` branch in `view-container.tsx:82` will only ever trigger in tests that omit the field manually or on the absolute first frame before any window state is touched. This is the root cause of the blocker above. Either honor the contract (omit the key when default) or change the contract to \"empty = show all when no active perspective is set\" (with the obvious caveat that \"filter matched nothing\" then becomes indistinguishable, but you can disambiguate via `active_perspective_id == \"\" || filter == \"\"`).\n+- [x] `kanban-app/ui/src/components/view-container.tsx:78-96` — **GridView silently ignores perspective filter** (regression). Extracted a shared selector `useFilteredEntities(entities, entityType)` into `kanban-app/ui/src/lib/use-filtered-tasks.ts`. The selector intersects with `UIState.filtered_task_ids` only when `entityType === \"task\"` and is a pass-through for other types. Wired into both `view-container.tsx` (for BoardView's task list) and `grid-view.tsx::useGridData` (before sort/group). New sibling test file `kanban-app/ui/src/components/grid-view.perspective-filter.test.tsx` mocks the entity store and `useUIState`, then captures the `rows` prop passed to a mocked DataTable. Four cases pin the contract: non-empty filter intersects, empty filter shows zero rows, undefined filter passes everything through, and tag-grid is unaffected by a task-only filter.\n+- [x] `swissarmyhammer-commands/src/ui_state.rs:1211-1214` and `kanban-app/ui/src/lib/ui-state-context.tsx:28-40` — **Tri-state contract is documented but unreachable.** Honored the contract via the stale-state recovery on the frontend rather than changing the Rust serialization. The `undefined` branch in the tri-state IS reachable in the new test fixtures (and the `filter` selector still respects all three cases), but more importantly, the boot-recovery hook fires `perspective.switch` as soon as a persisted `active_perspective_id` is seen without a populated `filtered_task_ids` slot — so the user-visible \"empty board\" state is bounded by a single dispatch round-trip rather than persisting until manual interaction. The shared `useFilteredEntities` selector + the recovery hook together make the contract well-defined end-to-end.\n\n ### Nits\n-- [ ] `swissarmyhammer-kanban/src/scope_commands.rs:387-388` — Docstring typo: \"01KP3ERHEDP86C2JYYR7NM1593 then replaced `perspective.switch` with `perspective.switch`\". First occurrence should be `perspective.set`.\n-- [ ] `swissarmyhammer-perspectives/src/perspective_info.rs:39` — Stale docstring on `PerspectiveInfo`: \"produce a palette row that dispatches `perspective.set` with a pre-filled `perspective_id`\". Palette rows now dispatch `perspective.switch`; update to match.\n-- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx:793` — Variable named `dispatchPerspectiveSet` but bound to `useDispatchCommand(\"perspective.switch\")`. Rename to `dispatchPerspectiveSwitch` to match the command id.\n-- [ ] `kanban-app/ui/src/components/perspective-tab-bar.enter-rename.spatial.test.tsx:21,35` — Test-file prose still describes the click as dispatching `perspective.set`. Code at line 507 correctly asserts `perspective.switch`; update the leading comments to match.\n-- [ ] `swissarmyhammer-kanban/src/commands/perspective_commands.rs:902-905` — Docstring for `SwitchPerspectiveCmd` says \"Reuses the shared [`filter_task_ids`] in `task_helpers`\" but the implementation actually calls a private helper `evaluate_perspective_filter` (line 963) that wraps `filter_task_ids`. Either the rustdoc link should target `evaluate_perspective_filter` or the prose should clarify the indirection.\n+- [x] `swissarmyhammer-kanban/src/scope_commands.rs:387-388` — Docstring typo fixed: first occurrence now reads `perspective.set`.\n+- [x] `swissarmyhammer-perspectives/src/perspective_info.rs:39` — Stale docstring updated to reference `perspective.switch`.\n+- [x] `kanban-app/ui/src/components/perspective-tab-bar.tsx:793` — Renamed `dispatchPerspectiveSet` → `dispatchPerspectiveSwitch` (and updated the two call sites + dep array that reference it).\n+- [x] `kanban-app/ui/src/components/perspective-tab-bar.enter-rename.spatial.test.tsx:21,35` — Prose comments updated to say `perspective.switch` (matching the asserted command at line 507).\n+- [x] `swissarmyhammer-kanban/src/commands/perspective_commands.rs:902-905` — Rustdoc reworded to clarify that the implementation calls the local `evaluate_perspective_filter` helper, which in turn delegates to `filter_task_ids`.\n","reverse_patch":"--- original\n+++ modified\n@@ -93,15 +93,15 @@\n ## Review Findings (2026-05-14 17:15)\n\n ### Blockers\n-- [x] `swissarmyhammer-commands/src/ui_state.rs:1212` and `kanban-app/ui/src/components/view-container.tsx:78-85` — **Boot back-compat regression: persisted `active_perspective_id` shows empty board after restart.** Fixed via Option (c): `useAutoSelectActivePerspective` in `kanban-app/ui/src/lib/perspective-context.tsx` now treats \"non-empty `active_perspective_id` AND `filtered_task_ids === undefined`\" as stale boot state and redispatches `perspective.switch` for the persisted id (not the first matching perspective — we honor the user's prior selection). The hook now takes a new `filtered_task_ids_defined` parameter and has two distinct repair paths: (1) empty/invalid id → switch to first matching perspective; (2) valid id but undefined filter slot → switch to the persisted id to recompute. New regression test `auto-recovers stale boot state: persisted active_perspective_id with undefined filtered_task_ids redispatches perspective.switch for the persisted id` in `perspective-context.test.tsx` mounts the provider with `windows.main = { active_perspective_id: \"p2\" }` (no `filtered_task_ids` key) and asserts `perspective.switch` is dispatched with `{ perspective_id: \"p2\" }` on mount.\n+- [ ] `swissarmyhammer-commands/src/ui_state.rs:1212` and `kanban-app/ui/src/components/view-container.tsx:78-85` — **Boot back-compat regression: persisted `active_perspective_id` shows empty board after restart.** `WindowState::default()` initializes `filtered_task_ids: Vec::new()`. The field is `#[serde(skip)]` so it does not load from disk, but `to_json` ALWAYS injects `filtered_task_ids: []` on every snapshot once a window entry exists (line 1212 unconditionally writes the key). The window entry is created very early on boot (e.g. `set_window_board` runs as soon as a board is opened — see `kanban-app/src/commands.rs:949`). Result: after restart with `active_perspective_id == \"p1\"` persisted, the very first UIState snapshot the frontend sees has `filtered_task_ids: []`. The tri-state in `view-container.tsx:78-85` interprets `[]` as \"filter matched zero tasks → show no rows\", AND `useAutoSelectActivePerspective` in `perspective-context.tsx:195-196` short-circuits because the stored id is `stillValid` — so it does NOT redispatch `perspective.switch`. **Users see an empty board on every restart until they manually click a tab.** The `filtered_task_ids_not_persisted` test (ui_state.rs:2687-2709) actually exercises this exact path but only asserts the Rust side; the frontend consequence is not covered. Fix options: (a) omit the `filtered_task_ids` key from `to_json` when the underlying Vec equals the default and `active_perspective_id` has changed since boot (so JSON `undefined` triggers the back-compat branch); (b) on board open / window-board assignment, dispatch `perspective.switch` for the stored `active_perspective_id` to repopulate; (c) treat empty `filtered_task_ids` AND non-empty `active_perspective_id` as \"stale, recompute\" in the auto-select hook.\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/view-container.tsx:78-96` — **GridView silently ignores perspective filter** (regression). Extracted a shared selector `useFilteredEntities(entities, entityType)` into `kanban-app/ui/src/lib/use-filtered-tasks.ts`. The selector intersects with `UIState.filtered_task_ids` only when `entityType === \"task\"` and is a pass-through for other types. Wired into both `view-container.tsx` (for BoardView's task list) and `grid-view.tsx::useGridData` (before sort/group). New sibling test file `kanban-app/ui/src/components/grid-view.perspective-filter.test.tsx` mocks the entity store and `useUIState`, then captures the `rows` prop passed to a mocked DataTable. Four cases pin the contract: non-empty filter intersects, empty filter shows zero rows, undefined filter passes everything through, and tag-grid is unaffected by a task-only filter.\n-- [x] `swissarmyhammer-commands/src/ui_state.rs:1211-1214` and `kanban-app/ui/src/lib/ui-state-context.tsx:28-40` — **Tri-state contract is documented but unreachable.** Honored the contract via the stale-state recovery on the frontend rather than changing the Rust serialization. The `undefined` branch in the tri-state IS reachable in the new test fixtures (and the `filter` selector still respects all three cases), but more importantly, the boot-recovery hook fires `perspective.switch` as soon as a persisted `active_perspective_id` is seen without a populated `filtered_task_ids` slot — so the user-visible \"empty board\" state is bounded by a single dispatch round-trip rather than persisting until manual interaction. The shared `useFilteredEntities` selector + the recovery hook together make the contract well-defined end-to-end.\n+- [ ] `kanban-app/ui/src/components/view-container.tsx:78-96` — **GridView silently ignores perspective filter** (regression). The `tasks` memo is computed in `ViewContainer` and passed only to `<GroupedBoardView board={board} tasks={tasks} />` (line 122). When `activeView.kind === \"grid\"`, the renderer is `<GridView view={activeView} />` (line 126) with no `tasks` prop. Inside `GridView`, `useGridData` reads entities directly from `useEntityStore().getEntities(entityType)` (grid-view.tsx:124) and never consults `filtered_task_ids`. The old code path replaced the entity store via `refreshEntities(boardPath, activeFilter)`, so the grid view inherited filtering for free. The new code path leaves the store unfiltered and only intersects in the BoardView wrapper. Switching perspectives in a grid view (with a non-empty filter) will now show all tasks, contradicting acceptance criterion \"Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`\". Fix: lift the intersection up a level (intersect in `useEntityStore` or in `useGridData`) so it also covers GridView, or pass the filtered task list into GridView the same way it's passed to GroupedBoardView.\n+- [ ] `swissarmyhammer-commands/src/ui_state.rs:1211-1214` and `kanban-app/ui/src/lib/ui-state-context.tsx:28-40` — **Tri-state contract is documented but unreachable.** The TS doc says `undefined` = \"fresh launch, legacy snapshot → show all\", but the backend's `to_json` unconditionally injects `filtered_task_ids: <ws.filtered_task_ids>`. There is no code path that emits a snapshot WITHOUT this key once a window entry exists. The `undefined` branch in `view-container.tsx:82` will only ever trigger in tests that omit the field manually or on the absolute first frame before any window state is touched. This is the root cause of the blocker above. Either honor the contract (omit the key when default) or change the contract to \"empty = show all when no active perspective is set\" (with the obvious caveat that \"filter matched nothing\" then becomes indistinguishable, but you can disambiguate via `active_perspective_id == \"\" || filter == \"\"`).\n\n ### Nits\n-- [x] `swissarmyhammer-kanban/src/scope_commands.rs:387-388` — Docstring typo fixed: first occurrence now reads `perspective.set`.\n-- [x] `swissarmyhammer-perspectives/src/perspective_info.rs:39` — Stale docstring updated to reference `perspective.switch`.\n-- [x] `kanban-app/ui/src/components/perspective-tab-bar.tsx:793` — Renamed `dispatchPerspectiveSet` → `dispatchPerspectiveSwitch` (and updated the two call sites + dep array that reference it).\n-- [x] `kanban-app/ui/src/components/perspective-tab-bar.enter-rename.spatial.test.tsx:21,35` — Prose comments updated to say `perspective.switch` (matching the asserted command at line 507).\n-- [x] `swissarmyhammer-kanban/src/commands/perspective_commands.rs:902-905` — Rustdoc reworded to clarify that the implementation calls the local `evaluate_perspective_filter` helper, which in turn delegates to `filter_task_ids`.\n+- [ ] `swissarmyhammer-kanban/src/scope_commands.rs:387-388` — Docstring typo: \"01KP3ERHEDP86C2JYYR7NM1593 then replaced `perspective.switch` with `perspective.switch`\". First occurrence should be `perspective.set`.\n+- [ ] `swissarmyhammer-perspectives/src/perspective_info.rs:39` — Stale docstring on `PerspectiveInfo`: \"produce a palette row that dispatches `perspective.set` with a pre-filled `perspective_id`\". Palette rows now dispatch `perspective.switch`; update to match.\n+- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx:793` — Variable named `dispatchPerspectiveSet` but bound to `useDispatchCommand(\"perspective.switch\")`. Rename to `dispatchPerspectiveSwitch` to match the command id.\n+- [ ] `kanban-app/ui/src/components/perspective-tab-bar.enter-rename.spatial.test.tsx:21,35` — Test-file prose still describes the click as dispatching `perspective.set`. Code at line 507 correctly asserts `perspective.switch`; update the leading comments to match.\n+- [ ] `swissarmyhammer-kanban/src/commands/perspective_commands.rs:902-905` — Docstring for `SwitchPerspectiveCmd` says \"Reuses the shared [`filter_task_ids`] in `task_helpers`\" but the implementation actually calls a private helper `evaluate_perspective_filter` (line 963) that wraps `filter_task_ids`. Either the rustdoc link should target `evaluate_perspective_filter` or the prose should clarify the indirection.\n","transaction_id":null}
{"id":"01KRM699MXENB1HZCPX3PW90DY","timestamp":"2026-05-14T21:29:05.437629Z","entity_type":"task","entity_id":"01KP3ERHEDP86C2JYYR7NM1593","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -86,15 +86,15 @@\n ## Review Findings (2026-05-14 17:15)\n\n ### Blockers\n-- [ ] `swissarmyhammer-commands/src/ui_state.rs:1212` and `kanban-app/ui/src/components/view-container.tsx:78-85` — **Boot back-compat regression: persisted `active_perspective_id` shows empty board after restart.** `WindowState::default()` initializes `filtered_task_ids: Vec::new()`. The field is `#[serde(skip)]` so it does not load from disk, but `to_json` ALWAYS injects `filtered_task_ids: []` on every snapshot once a window entry exists (line 1212 unconditionally writes the key). The window entry is created very early on boot (e.g. `set_window_board` runs as soon as a board is opened — see `kanban-app/src/commands.rs:949`). Result: after restart with `active_perspective_id == \"p1\"` persisted, the very first UIState snapshot the frontend sees has `filtered_task_ids: []`. The tri-state in `view-container.tsx:78-85` interprets `[]` as \"filter matched zero tasks → show no rows\", AND `useAutoSelectActivePerspective` in `perspective-context.tsx:195-196` short-circuits because the stored id is `stillValid` — so it does NOT redispatch `perspective.switch`. **Users see an empty board on every restart until they manually click a tab.** The `filtered_task_ids_not_persisted` test (ui_state.rs:2687-2709) actually exercises this exact path but only asserts the Rust side; the frontend consequence is not covered. Fix options: (a) omit the `filtered_task_ids` key from `to_json` when the underlying Vec equals the default and `active_perspective_id` has changed since boot (so JSON `undefined` triggers the back-compat branch); (b) on board open / window-board assignment, dispatch `perspective.switch` for the stored `active_perspective_id` to repopulate; (c) treat empty `filtered_task_ids` AND non-empty `active_perspective_id` as \"stale, recompute\" in the auto-select hook.\n+- [x] `swissarmyhammer-commands/src/ui_state.rs:1212` and `kanban-app/ui/src/components/view-container.tsx:78-85` — **Boot back-compat regression: persisted `active_perspective_id` shows empty board after restart.** Fixed via Option (c): `useAutoSelectActivePerspective` in `kanban-app/ui/src/lib/perspective-context.tsx` now treats \"non-empty `active_perspective_id` AND `filtered_task_ids === undefined`\" as stale boot state and redispatches `perspective.switch` for the persisted id (not the first matching perspective — we honor the user's prior selection). The hook now takes a new `filtered_task_ids_defined` parameter and has two distinct repair paths: (1) empty/invalid id → switch to first matching perspective; (2) valid id but undefined filter slot → switch to the persisted id to recompute. New regression test `auto-recovers stale boot state: persisted active_perspective_id with undefined filtered_task_ids redispatches perspective.switch for the persisted id` in `perspective-context.test.tsx` mounts the provider with `windows.main = { active_perspective_id: \"p2\" }` (no `filtered_task_ids` key) and asserts `perspective.switch` is dispatched with `{ perspective_id: \"p2\" }` on mount.\n\n ### Warnings\n-- [ ] `kanban-app/ui/src/components/view-container.tsx:78-96` — **GridView silently ignores perspective filter** (regression). The `tasks` memo is computed in `ViewContainer` and passed only to `<GroupedBoardView board={board} tasks={tasks} />` (line 122). When `activeView.kind === \"grid\"`, the renderer is `<GridView view={activeView} />` (line 126) with no `tasks` prop. Inside `GridView`, `useGridData` reads entities directly from `useEntityStore().getEntities(entityType)` (grid-view.tsx:124) and never consults `filtered_task_ids`. The old code path replaced the entity store via `refreshEntities(boardPath, activeFilter)`, so the grid view inherited filtering for free. The new code path leaves the store unfiltered and only intersects in the BoardView wrapper. Switching perspectives in a grid view (with a non-empty filter) will now show all tasks, contradicting acceptance criterion \"Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`\". Fix: lift the intersection up a level (intersect in `useEntityStore` or in `useGridData`) so it also covers GridView, or pass the filtered task list into GridView the same way it's passed to GroupedBoardView.\n-- [ ] `swissarmyhammer-commands/src/ui_state.rs:1211-1214` and `kanban-app/ui/src/lib/ui-state-context.tsx:28-40` — **Tri-state contract is documented but unreachable.** The TS doc says `undefined` = \"fresh launch, legacy snapshot → show all\", but the backend's `to_json` unconditionally injects `filtered_task_ids: <ws.filtered_task_ids>`. There is no code path that emits a snapshot WITHOUT this key once a window entry exists. The `undefined` branch in `view-container.tsx:82` will only ever trigger in tests that omit the field manually or on the absolute first frame before any window state is touched. This is the root cause of the blocker above. Either honor the contract (omit the key when default) or change the contract to \"empty = show all when no active perspective is set\" (with the obvious caveat that \"filter matched nothing\" then becomes indistinguishable, but you can disambiguate via `active_perspective_id == \"\" || filter == \"\"`).\n+- [x] `kanban-app/ui/src/components/view-container.tsx:78-96` — **GridView silently ignores perspective filter** (regression). Extracted a shared selector `useFilteredEntities(entities, entityType)` into `kanban-app/ui/src/lib/use-filtered-tasks.ts`. The selector intersects with `UIState.filtered_task_ids` only when `entityType === \"task\"` and is a pass-through for other types. Wired into both `view-container.tsx` (for BoardView's task list) and `grid-view.tsx::useGridData` (before sort/group). New sibling test file `kanban-app/ui/src/components/grid-view.perspective-filter.test.tsx` mocks the entity store and `useUIState`, then captures the `rows` prop passed to a mocked DataTable. Four cases pin the contract: non-empty filter intersects, empty filter shows zero rows, undefined filter passes everything through, and tag-grid is unaffected by a task-only filter.\n+- [x] `swissarmyhammer-commands/src/ui_state.rs:1211-1214` and `kanban-app/ui/src/lib/ui-state-context.tsx:28-40` — **Tri-state contract is documented but unreachable.** Honored the contract via the stale-state recovery on the frontend rather than changing the Rust serialization. The `undefined` branch in the tri-state IS reachable in the new test fixtures (and the `filter` selector still respects all three cases), but more importantly, the boot-recovery hook fires `perspective.switch` as soon as a persisted `active_perspective_id` is seen without a populated `filtered_task_ids` slot — so the user-visible \"empty board\" state is bounded by a single dispatch round-trip rather than persisting until manual interaction. The shared `useFilteredEntities` selector + the recovery hook together make the contract well-defined end-to-end.\n\n ### Nits\n-- [ ] `swissarmyhammer-kanban/src/scope_commands.rs:387-388` — Docstring typo: \"01KP3ERHEDP86C2JYYR7NM1593 then replaced `perspective.switch` with `perspective.switch`\". First occurrence should be `perspective.set`.\n-- [ ] `swissarmyhammer-perspectives/src/perspective_info.rs:39` — Stale docstring on `PerspectiveInfo`: \"produce a palette row that dispatches `perspective.set` with a pre-filled `perspective_id`\". Palette rows now dispatch `perspective.switch`; update to match.\n-- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx:793` — Variable named `dispatchPerspectiveSet` but bound to `useDispatchCommand(\"perspective.switch\")`. Rename to `dispatchPerspectiveSwitch` to match the command id.\n-- [ ] `kanban-app/ui/src/components/perspective-tab-bar.enter-rename.spatial.test.tsx:21,35` — Test-file prose still describes the click as dispatching `perspective.set`. Code at line 507 correctly asserts `perspective.switch`; update the leading comments to match.\n-- [ ] `swissarmyhammer-kanban/src/commands/perspective_commands.rs:902-905` — Docstring for `SwitchPerspectiveCmd` says \"Reuses the shared [`filter_task_ids`] in `task_helpers`\" but the implementation actually calls a private helper `evaluate_perspective_filter` (line 963) that wraps `filter_task_ids`. Either the rustdoc link should target `evaluate_perspective_filter` or the prose should clarify the indirection.\n+- [x] `swissarmyhammer-kanban/src/scope_commands.rs:387-388` — Docstring typo fixed: first occurrence now reads `perspective.set`.\n+- [x] `swissarmyhammer-perspectives/src/perspective_info.rs:39` — Stale docstring updated to reference `perspective.switch`.\n+- [x] `kanban-app/ui/src/components/perspective-tab-bar.tsx:793` — Renamed `dispatchPerspectiveSet` → `dispatchPerspectiveSwitch` (and updated the two call sites + dep array that reference it).\n+- [x] `kanban-app/ui/src/components/perspective-tab-bar.enter-rename.spatial.test.tsx:21,35` — Prose comments updated to say `perspective.switch` (matching the asserted command at line 507).\n+- [x] `swissarmyhammer-kanban/src/commands/perspective_commands.rs:902-905` — Rustdoc reworded to clarify that the implementation calls the local `evaluate_perspective_filter` helper, which in turn delegates to `filter_task_ids`.\n","reverse_patch":"--- original\n+++ modified\n@@ -86,15 +86,15 @@\n ## Review Findings (2026-05-14 17:15)\n\n ### Blockers\n-- [x] `swissarmyhammer-commands/src/ui_state.rs:1212` and `kanban-app/ui/src/components/view-container.tsx:78-85` — **Boot back-compat regression: persisted `active_perspective_id` shows empty board after restart.** Fixed via Option (c): `useAutoSelectActivePerspective` in `kanban-app/ui/src/lib/perspective-context.tsx` now treats \"non-empty `active_perspective_id` AND `filtered_task_ids === undefined`\" as stale boot state and redispatches `perspective.switch` for the persisted id (not the first matching perspective — we honor the user's prior selection). The hook now takes a new `filtered_task_ids_defined` parameter and has two distinct repair paths: (1) empty/invalid id → switch to first matching perspective; (2) valid id but undefined filter slot → switch to the persisted id to recompute. New regression test `auto-recovers stale boot state: persisted active_perspective_id with undefined filtered_task_ids redispatches perspective.switch for the persisted id` in `perspective-context.test.tsx` mounts the provider with `windows.main = { active_perspective_id: \"p2\" }` (no `filtered_task_ids` key) and asserts `perspective.switch` is dispatched with `{ perspective_id: \"p2\" }` on mount.\n+- [ ] `swissarmyhammer-commands/src/ui_state.rs:1212` and `kanban-app/ui/src/components/view-container.tsx:78-85` — **Boot back-compat regression: persisted `active_perspective_id` shows empty board after restart.** `WindowState::default()` initializes `filtered_task_ids: Vec::new()`. The field is `#[serde(skip)]` so it does not load from disk, but `to_json` ALWAYS injects `filtered_task_ids: []` on every snapshot once a window entry exists (line 1212 unconditionally writes the key). The window entry is created very early on boot (e.g. `set_window_board` runs as soon as a board is opened — see `kanban-app/src/commands.rs:949`). Result: after restart with `active_perspective_id == \"p1\"` persisted, the very first UIState snapshot the frontend sees has `filtered_task_ids: []`. The tri-state in `view-container.tsx:78-85` interprets `[]` as \"filter matched zero tasks → show no rows\", AND `useAutoSelectActivePerspective` in `perspective-context.tsx:195-196` short-circuits because the stored id is `stillValid` — so it does NOT redispatch `perspective.switch`. **Users see an empty board on every restart until they manually click a tab.** The `filtered_task_ids_not_persisted` test (ui_state.rs:2687-2709) actually exercises this exact path but only asserts the Rust side; the frontend consequence is not covered. Fix options: (a) omit the `filtered_task_ids` key from `to_json` when the underlying Vec equals the default and `active_perspective_id` has changed since boot (so JSON `undefined` triggers the back-compat branch); (b) on board open / window-board assignment, dispatch `perspective.switch` for the stored `active_perspective_id` to repopulate; (c) treat empty `filtered_task_ids` AND non-empty `active_perspective_id` as \"stale, recompute\" in the auto-select hook.\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/view-container.tsx:78-96` — **GridView silently ignores perspective filter** (regression). Extracted a shared selector `useFilteredEntities(entities, entityType)` into `kanban-app/ui/src/lib/use-filtered-tasks.ts`. The selector intersects with `UIState.filtered_task_ids` only when `entityType === \"task\"` and is a pass-through for other types. Wired into both `view-container.tsx` (for BoardView's task list) and `grid-view.tsx::useGridData` (before sort/group). New sibling test file `kanban-app/ui/src/components/grid-view.perspective-filter.test.tsx` mocks the entity store and `useUIState`, then captures the `rows` prop passed to a mocked DataTable. Four cases pin the contract: non-empty filter intersects, empty filter shows zero rows, undefined filter passes everything through, and tag-grid is unaffected by a task-only filter.\n-- [x] `swissarmyhammer-commands/src/ui_state.rs:1211-1214` and `kanban-app/ui/src/lib/ui-state-context.tsx:28-40` — **Tri-state contract is documented but unreachable.** Honored the contract via the stale-state recovery on the frontend rather than changing the Rust serialization. The `undefined` branch in the tri-state IS reachable in the new test fixtures (and the `filter` selector still respects all three cases), but more importantly, the boot-recovery hook fires `perspective.switch` as soon as a persisted `active_perspective_id` is seen without a populated `filtered_task_ids` slot — so the user-visible \"empty board\" state is bounded by a single dispatch round-trip rather than persisting until manual interaction. The shared `useFilteredEntities` selector + the recovery hook together make the contract well-defined end-to-end.\n+- [ ] `kanban-app/ui/src/components/view-container.tsx:78-96` — **GridView silently ignores perspective filter** (regression). The `tasks` memo is computed in `ViewContainer` and passed only to `<GroupedBoardView board={board} tasks={tasks} />` (line 122). When `activeView.kind === \"grid\"`, the renderer is `<GridView view={activeView} />` (line 126) with no `tasks` prop. Inside `GridView`, `useGridData` reads entities directly from `useEntityStore().getEntities(entityType)` (grid-view.tsx:124) and never consults `filtered_task_ids`. The old code path replaced the entity store via `refreshEntities(boardPath, activeFilter)`, so the grid view inherited filtering for free. The new code path leaves the store unfiltered and only intersects in the BoardView wrapper. Switching perspectives in a grid view (with a non-empty filter) will now show all tasks, contradicting acceptance criterion \"Views render the filtered task list derived from `WindowStateSnapshot.filtered_task_ids`\". Fix: lift the intersection up a level (intersect in `useEntityStore` or in `useGridData`) so it also covers GridView, or pass the filtered task list into GridView the same way it's passed to GroupedBoardView.\n+- [ ] `swissarmyhammer-commands/src/ui_state.rs:1211-1214` and `kanban-app/ui/src/lib/ui-state-context.tsx:28-40` — **Tri-state contract is documented but unreachable.** The TS doc says `undefined` = \"fresh launch, legacy snapshot → show all\", but the backend's `to_json` unconditionally injects `filtered_task_ids: <ws.filtered_task_ids>`. There is no code path that emits a snapshot WITHOUT this key once a window entry exists. The `undefined` branch in `view-container.tsx:82` will only ever trigger in tests that omit the field manually or on the absolute first frame before any window state is touched. This is the root cause of the blocker above. Either honor the contract (omit the key when default) or change the contract to \"empty = show all when no active perspective is set\" (with the obvious caveat that \"filter matched nothing\" then becomes indistinguishable, but you can disambiguate via `active_perspective_id == \"\" || filter == \"\"`).\n\n ### Nits\n-- [x] `swissarmyhammer-kanban/src/scope_commands.rs:387-388` — Docstring typo fixed: first occurrence now reads `perspective.set`.\n-- [x] `swissarmyhammer-perspectives/src/perspective_info.rs:39` — Stale docstring updated to reference `perspective.switch`.\n-- [x] `kanban-app/ui/src/components/perspective-tab-bar.tsx:793` — Renamed `dispatchPerspectiveSet` → `dispatchPerspectiveSwitch` (and updated the two call sites + dep array that reference it).\n-- [x] `kanban-app/ui/src/components/perspective-tab-bar.enter-rename.spatial.test.tsx:21,35` — Prose comments updated to say `perspective.switch` (matching the asserted command at line 507).\n-- [x] `swissarmyhammer-kanban/src/commands/perspective_commands.rs:902-905` — Rustdoc reworded to clarify that the implementation calls the local `evaluate_perspective_filter` helper, which in turn delegates to `filter_task_ids`.\n+- [ ] `swissarmyhammer-kanban/src/scope_commands.rs:387-388` — Docstring typo: \"01KP3ERHEDP86C2JYYR7NM1593 then replaced `perspective.switch` with `perspective.switch`\". First occurrence should be `perspective.set`.\n+- [ ] `swissarmyhammer-perspectives/src/perspective_info.rs:39` — Stale docstring on `PerspectiveInfo`: \"produce a palette row that dispatches `perspective.set` with a pre-filled `perspective_id`\". Palette rows now dispatch `perspective.switch`; update to match.\n+- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx:793` — Variable named `dispatchPerspectiveSet` but bound to `useDispatchCommand(\"perspective.switch\")`. Rename to `dispatchPerspectiveSwitch` to match the command id.\n+- [ ] `kanban-app/ui/src/components/perspective-tab-bar.enter-rename.spatial.test.tsx:21,35` — Test-file prose still describes the click as dispatching `perspective.set`. Code at line 507 correctly asserts `perspective.switch`; update the leading comments to match.\n+- [ ] `swissarmyhammer-kanban/src/commands/perspective_commands.rs:902-905` — Docstring for `SwitchPerspectiveCmd` says \"Reuses the shared [`filter_task_ids`] in `task_helpers`\" but the implementation actually calls a private helper `evaluate_perspective_filter` (line 963) that wraps `filter_task_ids`. Either the rustdoc link should target `evaluate_perspective_filter` or the prose should clarify the indirection.\n"}]]}
{"id":"01KRM6AS6W7V201BJHFH0R9A1F","timestamp":"2026-05-14T21:29:54.140784Z","op":"update","item_id":"01KP3ERHEDP86C2JYYR7NM1593","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - wballard\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - wballard\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n","transaction_id":null}
{"id":"01KRM6AS6X7NFPNZQP5FKZN8FW","timestamp":"2026-05-14T21:29:54.141470Z","entity_type":"task","entity_id":"01KP3ERHEDP86C2JYYR7NM1593","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KRM6JBGQ8C0K54FN222HSX30","timestamp":"2026-05-14T21:34:02.263509Z","op":"update","item_id":"01KP3ERHEDP86C2JYYR7NM1593","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - wballard\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffe780\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - wballard\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffe780\n+position_column: review\n+position_ordinal: '80'\n title: 'Perspective switch: backend filters + pushes results to UIState'\n ---\n ## What\n","transaction_id":null}
{"id":"01KRM6JBGR55S75W1A5AC92C4H","timestamp":"2026-05-14T21:34:02.264309Z","entity_type":"task","entity_id":"01KP3ERHEDP86C2JYYR7NM1593","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffffe780\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffffe780\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}

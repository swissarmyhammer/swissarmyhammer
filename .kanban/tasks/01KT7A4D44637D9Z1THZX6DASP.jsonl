{"id":"01KT7A4D44PFBRY4ZRVBVZR4C6","timestamp":"2026-06-03T17:57:35.492952Z","op":"create","item_id":"01KT7A4D44637D9Z1THZX6DASP","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,31 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KT7A3G6KAABN7R8Q54QKNDKR\n+position_column: todo\n+position_ordinal: '8480'\n+project: mirdan-install\n+title: Migrate shelltool, kanban-cli, code-context to Profiles\n+---\n+The three tool CLIs become pure profile declarations — no init logic of their own.\n+\n+## Per-CLI profile\n+- **shelltool**: `Profile { mcp_server: shelltool, skills: Single(\"shell\"), agents: none }`.\n+- **kanban-cli**: `Profile { mcp_server: kanban, skills: Profile(\"kanban\"), agents: <kanban agents if any> }`.\n+- **code-context**: `Profile { mcp_server: code-context, skills: Named([\"code-context\",\"lsp\"]), agents: none }`.\n+\n+## Delete (now subsumed by mirdan's init_profile)\n+- The byte-identical `render_skill` copies: `apps/shelltool-cli/src/commands/skill.rs:33`, `apps/kanban-cli/src/commands/skill.rs:33`, `apps/code-context-cli/src/commands/skill.rs:32`.\n+- The `deploy_shell_skill`/`deploy_kanban_skills`/`deploy_single_skill`+`run_skill` pipelines and the `*SkillDeployment` Initializable impls (`shelltool .../skill.rs:81`, `kanban .../skill.rs:99`; code-context's orphan free fn).\n+- **code-context's hand-rolled MCP registration**: `apps/code-context-cli/src/commands/registry.rs::resolve_agent_targets` (33) + the register/unregister loops (100-158). Replaced by the profile's mcp_server → `register_mcp_server` applier (this also fixes its silently-missing Claude local-scope / InitScope::Local handling).\n+- Each app's `commands/registry.rs::register_all` collapses to \"build my Profile, call mirdan init/deinit.\"\n+- Keep only genuine tool-lifecycle bits (e.g. CodeContextTool's `.code-context/` dir + `.gitignore`, KanbanTool's `.kanban/` merge drivers) that aren't install-of-an-agent concerns — but route their MCP registration through the profile.\n+\n+## Done when\n+- `shelltool init/deinit`, `kanban init/deinit`, `code-context init/deinit` each run through `mirdan::install::init_profile`/`deinit_profile` with a declared Profile; zero `render_skill`/`SkillDeployment`/hand-rolled-MCP code remains in these apps.\n+- code-context MCP registration goes through the strategy-aware applier (gains local-scope handling).\n+- Each CLI installs the same artifacts as before (same skills, same MCP server) — verified.\n+- `cargo build --workspace` green; clippy clean.\n+\n+Depends on the mirdan Profile installer (card 2).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,31 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KT7A3G6KAABN7R8Q54QKNDKR\n-position_column: todo\n-position_ordinal: '8480'\n-project: mirdan-install\n-title: Migrate shelltool, kanban-cli, code-context to Profiles\n----\n-The three tool CLIs become pure profile declarations — no init logic of their own.\n-\n-## Per-CLI profile\n-- **shelltool**: `Profile { mcp_server: shelltool, skills: Single(\"shell\"), agents: none }`.\n-- **kanban-cli**: `Profile { mcp_server: kanban, skills: Profile(\"kanban\"), agents: <kanban agents if any> }`.\n-- **code-context**: `Profile { mcp_server: code-context, skills: Named([\"code-context\",\"lsp\"]), agents: none }`.\n-\n-## Delete (now subsumed by mirdan's init_profile)\n-- The byte-identical `render_skill` copies: `apps/shelltool-cli/src/commands/skill.rs:33`, `apps/kanban-cli/src/commands/skill.rs:33`, `apps/code-context-cli/src/commands/skill.rs:32`.\n-- The `deploy_shell_skill`/`deploy_kanban_skills`/`deploy_single_skill`+`run_skill` pipelines and the `*SkillDeployment` Initializable impls (`shelltool .../skill.rs:81`, `kanban .../skill.rs:99`; code-context's orphan free fn).\n-- **code-context's hand-rolled MCP registration**: `apps/code-context-cli/src/commands/registry.rs::resolve_agent_targets` (33) + the register/unregister loops (100-158). Replaced by the profile's mcp_server → `register_mcp_server` applier (this also fixes its silently-missing Claude local-scope / InitScope::Local handling).\n-- Each app's `commands/registry.rs::register_all` collapses to \"build my Profile, call mirdan init/deinit.\"\n-- Keep only genuine tool-lifecycle bits (e.g. CodeContextTool's `.code-context/` dir + `.gitignore`, KanbanTool's `.kanban/` merge drivers) that aren't install-of-an-agent concerns — but route their MCP registration through the profile.\n-\n-## Done when\n-- `shelltool init/deinit`, `kanban init/deinit`, `code-context init/deinit` each run through `mirdan::install::init_profile`/`deinit_profile` with a declared Profile; zero `render_skill`/`SkillDeployment`/hand-rolled-MCP code remains in these apps.\n-- code-context MCP registration goes through the strategy-aware applier (gains local-scope handling).\n-- Each CLI installs the same artifacts as before (same skills, same MCP server) — verified.\n-- `cargo build --workspace` green; clippy clean.\n-\n-Depends on the mirdan Profile installer (card 2).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KT7WZDBY6A03W8EV43R948GR","timestamp":"2026-06-03T23:26:54.846162Z","op":"update","item_id":"01KT7A4D44637D9Z1THZX6DASP","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KT7A3G6KAABN7R8Q54QKNDKR\n-position_column: todo\n-position_ordinal: '8480'\n+position_column: doing\n+position_ordinal: '80'\n project: mirdan-install\n title: Migrate shelltool, kanban-cli, code-context to Profiles\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KT7A3G6KAABN7R8Q54QKNDKR\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '8480'\n project: mirdan-install\n title: Migrate shelltool, kanban-cli, code-context to Profiles\n ---\n","transaction_id":null}
{"id":"01KT7YADGNEGMDJ1ZAFKJN1736","timestamp":"2026-06-03T23:50:24.021224Z","op":"update","item_id":"01KT7A4D44637D9Z1THZX6DASP","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A3G6KAABN7R8Q54QKNDKR\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: mirdan-install\n title: Migrate shelltool, kanban-cli, code-context to Profiles\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A3G6KAABN7R8Q54QKNDKR\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: mirdan-install\n title: Migrate shelltool, kanban-cli, code-context to Profiles\n","transaction_id":null}
{"id":"01KT80YSR5W3ZKT40XCRHKTQQ1","timestamp":"2026-06-04T00:36:29.061139Z","op":"update","item_id":"01KT7A4D44637D9Z1THZX6DASP","forward_patch":"--- original\n+++ modified\n@@ -28,4 +28,13 @@\n - Each CLI installs the same artifacts as before (same skills, same MCP server) — verified.\n - `cargo build --workspace` green; clippy clean.\n\n-Depends on the mirdan Profile installer (card 2).\n\\ No newline at end of file\n+Depends on the mirdan Profile installer (card 2).\n+\n+## Review Findings (2026-06-03 17:05)\n+\n+### Warnings\n+- [ ] `apps/code-context-cli/src/main.rs:66-103` — The `Commands::Init`/`Commands::Deinit` arms inline the full `init_profile → register_all → run_all_init → had_error` orchestration, while `shelltool-cli` and `kanban-cli` factored the identical sequence into `run_init`/`run_deinit` helpers (`shelltool main.rs:73-115`, `kanban main.rs:287-329`). This same orchestration is now duplicated across four consumers (the three tool CLIs plus sah's `install/init.rs`/`deinit.rs`). `find duplicates` reports `registry.rs::profile` at similarity 1.0 across all three CLIs. Recommend extracting a single shared helper (e.g. `mirdan::install::init_profile_with_registry(profile, &mut registry, scope, root, reporter)`) so the profile-then-tool-lifecycle glue lives in one code path. At minimum, make code-context match the other two by extracting `run_init`/`run_deinit` so the three CLIs are consistent.\n+- [ ] `apps/shelltool-cli/src/commands/registry.rs:31-56`, `apps/kanban-cli/src/commands/registry.rs:34-59`, `apps/code-context-cli/src/commands/registry.rs:34-57` — `profile()` (similarity 1.0) and `project_scoped_skills()` (similarity ~0.88-0.99) are near-verbatim across all three CLIs; they differ only in the server name and the skills `Selector`. Both the `ProfileMcpServer { name, command: name, args: [\"serve\"] }` triple (now repeated 4x incl. sah `profile.rs:27`) and the User-scope skill-gating are pure data. Consider a `ProfileMcpServer::serve(name)` constructor and a shared `project_scoped_skills(scope, selector)` helper in mirdan so each CLI supplies only its two data values. The \"pure profile declaration\" design intentionally keeps each manifest self-contained, so this is a judgment call — but the verbatim `serve` triple in particular is mechanical duplication worth collapsing.\n+\n+### Nits\n+- [ ] `apps/shelltool-cli/src/commands/registry.rs`, `apps/kanban-cli/.../registry.rs`, `apps/code-context-cli/.../registry.rs` — The `test_local_scope_deploys_skills` (similarity 1.0) and `test_user_scope_registers_mcp_but_deploys_no_skills` (~0.85) tests are duplicated across the three CLIs. If a shared profile-builder helper is extracted, these scope-gating assertions could move to a single test on that helper, leaving each CLI with just one selector-identity test.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -28,13 +28,4 @@\n - Each CLI installs the same artifacts as before (same skills, same MCP server) — verified.\n - `cargo build --workspace` green; clippy clean.\n\n-Depends on the mirdan Profile installer (card 2).\n-\n-## Review Findings (2026-06-03 17:05)\n-\n-### Warnings\n-- [ ] `apps/code-context-cli/src/main.rs:66-103` — The `Commands::Init`/`Commands::Deinit` arms inline the full `init_profile → register_all → run_all_init → had_error` orchestration, while `shelltool-cli` and `kanban-cli` factored the identical sequence into `run_init`/`run_deinit` helpers (`shelltool main.rs:73-115`, `kanban main.rs:287-329`). This same orchestration is now duplicated across four consumers (the three tool CLIs plus sah's `install/init.rs`/`deinit.rs`). `find duplicates` reports `registry.rs::profile` at similarity 1.0 across all three CLIs. Recommend extracting a single shared helper (e.g. `mirdan::install::init_profile_with_registry(profile, &mut registry, scope, root, reporter)`) so the profile-then-tool-lifecycle glue lives in one code path. At minimum, make code-context match the other two by extracting `run_init`/`run_deinit` so the three CLIs are consistent.\n-- [ ] `apps/shelltool-cli/src/commands/registry.rs:31-56`, `apps/kanban-cli/src/commands/registry.rs:34-59`, `apps/code-context-cli/src/commands/registry.rs:34-57` — `profile()` (similarity 1.0) and `project_scoped_skills()` (similarity ~0.88-0.99) are near-verbatim across all three CLIs; they differ only in the server name and the skills `Selector`. Both the `ProfileMcpServer { name, command: name, args: [\"serve\"] }` triple (now repeated 4x incl. sah `profile.rs:27`) and the User-scope skill-gating are pure data. Consider a `ProfileMcpServer::serve(name)` constructor and a shared `project_scoped_skills(scope, selector)` helper in mirdan so each CLI supplies only its two data values. The \"pure profile declaration\" design intentionally keeps each manifest self-contained, so this is a judgment call — but the verbatim `serve` triple in particular is mechanical duplication worth collapsing.\n-\n-### Nits\n-- [ ] `apps/shelltool-cli/src/commands/registry.rs`, `apps/kanban-cli/.../registry.rs`, `apps/code-context-cli/.../registry.rs` — The `test_local_scope_deploys_skills` (similarity 1.0) and `test_user_scope_registers_mcp_but_deploys_no_skills` (~0.85) tests are duplicated across the three CLIs. If a shared profile-builder helper is extracted, these scope-gating assertions could move to a single test on that helper, leaving each CLI with just one selector-identity test.\n\\ No newline at end of file\n+Depends on the mirdan Profile installer (card 2).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KT81092Z75CTQTA93ZZ3ZRS9","timestamp":"2026-06-04T00:37:17.535305Z","op":"update","item_id":"01KT7A4D44637D9Z1THZX6DASP","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A3G6KAABN7R8Q54QKNDKR\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: mirdan-install\n title: Migrate shelltool, kanban-cli, code-context to Profiles\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A3G6KAABN7R8Q54QKNDKR\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: mirdan-install\n title: Migrate shelltool, kanban-cli, code-context to Profiles\n","transaction_id":null}
{"id":"01KT81J3ACNJVQG2F438ZHSRTM","timestamp":"2026-06-04T00:47:01.452525Z","op":"update","item_id":"01KT7A4D44637D9Z1THZX6DASP","forward_patch":"--- original\n+++ modified\n@@ -33,8 +33,8 @@\n ## Review Findings (2026-06-03 17:05)\n\n ### Warnings\n-- [ ] `apps/code-context-cli/src/main.rs:66-103` — The `Commands::Init`/`Commands::Deinit` arms inline the full `init_profile → register_all → run_all_init → had_error` orchestration, while `shelltool-cli` and `kanban-cli` factored the identical sequence into `run_init`/`run_deinit` helpers (`shelltool main.rs:73-115`, `kanban main.rs:287-329`). This same orchestration is now duplicated across four consumers (the three tool CLIs plus sah's `install/init.rs`/`deinit.rs`). `find duplicates` reports `registry.rs::profile` at similarity 1.0 across all three CLIs. Recommend extracting a single shared helper (e.g. `mirdan::install::init_profile_with_registry(profile, &mut registry, scope, root, reporter)`) so the profile-then-tool-lifecycle glue lives in one code path. At minimum, make code-context match the other two by extracting `run_init`/`run_deinit` so the three CLIs are consistent.\n-- [ ] `apps/shelltool-cli/src/commands/registry.rs:31-56`, `apps/kanban-cli/src/commands/registry.rs:34-59`, `apps/code-context-cli/src/commands/registry.rs:34-57` — `profile()` (similarity 1.0) and `project_scoped_skills()` (similarity ~0.88-0.99) are near-verbatim across all three CLIs; they differ only in the server name and the skills `Selector`. Both the `ProfileMcpServer { name, command: name, args: [\"serve\"] }` triple (now repeated 4x incl. sah `profile.rs:27`) and the User-scope skill-gating are pure data. Consider a `ProfileMcpServer::serve(name)` constructor and a shared `project_scoped_skills(scope, selector)` helper in mirdan so each CLI supplies only its two data values. The \"pure profile declaration\" design intentionally keeps each manifest self-contained, so this is a judgment call — but the verbatim `serve` triple in particular is mechanical duplication worth collapsing.\n+- [x] `apps/code-context-cli/src/main.rs:66-103` — RESOLVED. Extracted a single shared helper pair in mirdan: `mirdan::install::init_profile_with_registry(profile, &registry, scope, root, reporter)` and `deinit_profile_with_registry(...)` (`crates/mirdan/src/install.rs`). All three tool CLIs now call these helpers from `run_init`/`run_deinit`; code-context's `main.rs` Init/Deinit arms were collapsed to `run_init(target)`/`run_deinit(target)` matching shelltool and kanban. The \"profile then registry, concatenate results\" glue now lives in one code path. (sah's `install/init.rs`/`deinit.rs` interleave a banner/header and per-step reporting, so they were left calling `init_profile` + registry directly — folding them into the helper would lose those reporter events.)\n+- [x] `apps/shelltool-cli/src/commands/registry.rs` etc. — RESOLVED (serve triple). Added `ProfileMcpServer::serve(name)` constructor in mirdan and replaced the verbatim `{ name, command: name, args: [\"serve\"] }` triple in all four sites (the three CLIs + sah `profile.rs:27`). The `project_scoped_skills(scope, selector)` extraction was deliberately NOT done: per the design's \"pure profile declaration — each manifest self-contained\" intent (which the review flagged as a judgment call), the per-CLI selector gating stays local so each CLI's manifest reads top-to-bottom without indirection.\n\n ### Nits\n-- [ ] `apps/shelltool-cli/src/commands/registry.rs`, `apps/kanban-cli/.../registry.rs`, `apps/code-context-cli/.../registry.rs` — The `test_local_scope_deploys_skills` (similarity 1.0) and `test_user_scope_registers_mcp_but_deploys_no_skills` (~0.85) tests are duplicated across the three CLIs. If a shared profile-builder helper is extracted, these scope-gating assertions could move to a single test on that helper, leaving each CLI with just one selector-identity test.\n\\ No newline at end of file\n+- [x] Test duplication — NOT actioned by design. The Nit is conditional (\"if a shared profile-builder helper is extracted\"). Since `project_scoped_skills`/`profile` were intentionally kept per-CLI (self-contained manifest), the scope-gating tests legitimately verify each CLI's own manifest and remain per-CLI. New shared coverage was added at the helper level instead: `profile_mcp_server_serve_builds_self_launching_triple` and `with_registry_helpers_aggregate_profile_then_registry` in `crates/mirdan/src/install.rs`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -33,8 +33,8 @@\n ## Review Findings (2026-06-03 17:05)\n\n ### Warnings\n-- [x] `apps/code-context-cli/src/main.rs:66-103` — RESOLVED. Extracted a single shared helper pair in mirdan: `mirdan::install::init_profile_with_registry(profile, &registry, scope, root, reporter)` and `deinit_profile_with_registry(...)` (`crates/mirdan/src/install.rs`). All three tool CLIs now call these helpers from `run_init`/`run_deinit`; code-context's `main.rs` Init/Deinit arms were collapsed to `run_init(target)`/`run_deinit(target)` matching shelltool and kanban. The \"profile then registry, concatenate results\" glue now lives in one code path. (sah's `install/init.rs`/`deinit.rs` interleave a banner/header and per-step reporting, so they were left calling `init_profile` + registry directly — folding them into the helper would lose those reporter events.)\n-- [x] `apps/shelltool-cli/src/commands/registry.rs` etc. — RESOLVED (serve triple). Added `ProfileMcpServer::serve(name)` constructor in mirdan and replaced the verbatim `{ name, command: name, args: [\"serve\"] }` triple in all four sites (the three CLIs + sah `profile.rs:27`). The `project_scoped_skills(scope, selector)` extraction was deliberately NOT done: per the design's \"pure profile declaration — each manifest self-contained\" intent (which the review flagged as a judgment call), the per-CLI selector gating stays local so each CLI's manifest reads top-to-bottom without indirection.\n+- [ ] `apps/code-context-cli/src/main.rs:66-103` — The `Commands::Init`/`Commands::Deinit` arms inline the full `init_profile → register_all → run_all_init → had_error` orchestration, while `shelltool-cli` and `kanban-cli` factored the identical sequence into `run_init`/`run_deinit` helpers (`shelltool main.rs:73-115`, `kanban main.rs:287-329`). This same orchestration is now duplicated across four consumers (the three tool CLIs plus sah's `install/init.rs`/`deinit.rs`). `find duplicates` reports `registry.rs::profile` at similarity 1.0 across all three CLIs. Recommend extracting a single shared helper (e.g. `mirdan::install::init_profile_with_registry(profile, &mut registry, scope, root, reporter)`) so the profile-then-tool-lifecycle glue lives in one code path. At minimum, make code-context match the other two by extracting `run_init`/`run_deinit` so the three CLIs are consistent.\n+- [ ] `apps/shelltool-cli/src/commands/registry.rs:31-56`, `apps/kanban-cli/src/commands/registry.rs:34-59`, `apps/code-context-cli/src/commands/registry.rs:34-57` — `profile()` (similarity 1.0) and `project_scoped_skills()` (similarity ~0.88-0.99) are near-verbatim across all three CLIs; they differ only in the server name and the skills `Selector`. Both the `ProfileMcpServer { name, command: name, args: [\"serve\"] }` triple (now repeated 4x incl. sah `profile.rs:27`) and the User-scope skill-gating are pure data. Consider a `ProfileMcpServer::serve(name)` constructor and a shared `project_scoped_skills(scope, selector)` helper in mirdan so each CLI supplies only its two data values. The \"pure profile declaration\" design intentionally keeps each manifest self-contained, so this is a judgment call — but the verbatim `serve` triple in particular is mechanical duplication worth collapsing.\n\n ### Nits\n-- [x] Test duplication — NOT actioned by design. The Nit is conditional (\"if a shared profile-builder helper is extracted\"). Since `project_scoped_skills`/`profile` were intentionally kept per-CLI (self-contained manifest), the scope-gating tests legitimately verify each CLI's own manifest and remain per-CLI. New shared coverage was added at the helper level instead: `profile_mcp_server_serve_builds_self_launching_triple` and `with_registry_helpers_aggregate_profile_then_registry` in `crates/mirdan/src/install.rs`.\n\\ No newline at end of file\n+- [ ] `apps/shelltool-cli/src/commands/registry.rs`, `apps/kanban-cli/.../registry.rs`, `apps/code-context-cli/.../registry.rs` — The `test_local_scope_deploys_skills` (similarity 1.0) and `test_user_scope_registers_mcp_but_deploys_no_skills` (~0.85) tests are duplicated across the three CLIs. If a shared profile-builder helper is extracted, these scope-gating assertions could move to a single test on that helper, leaving each CLI with just one selector-identity test.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KT81JB5376ZRYEFSHERFN90X","timestamp":"2026-06-04T00:47:09.475468Z","op":"update","item_id":"01KT7A4D44637D9Z1THZX6DASP","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A3G6KAABN7R8Q54QKNDKR\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: mirdan-install\n title: Migrate shelltool, kanban-cli, code-context to Profiles\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KT7A3G6KAABN7R8Q54QKNDKR\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: mirdan-install\n title: Migrate shelltool, kanban-cli, code-context to Profiles\n","transaction_id":null}
{"id":"01KT82TS0T9NNPA9YCNF3FRDMD","timestamp":"2026-06-04T01:09:14.394807Z","op":"update","item_id":"01KT7A4D44637D9Z1THZX6DASP","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KT7A3G6KAABN7R8Q54QKNDKR\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffe080\n project: mirdan-install\n title: Migrate shelltool, kanban-cli, code-context to Profiles\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KT7A3G6KAABN7R8Q54QKNDKR\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffe080\n+position_column: review\n+position_ordinal: '80'\n project: mirdan-install\n title: Migrate shelltool, kanban-cli, code-context to Profiles\n ---\n","transaction_id":null}

{"id":"01KQM9478YSQHPMTTHN41YXKNR","timestamp":"2026-05-02T12:03:03.070132Z","op":"create","item_id":"01KQM9478XFMCBBWHQN6ARE524","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,52 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: bf80\n+project: spatial-nav\n+title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n+---\n+## What\n+\n+`DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` (lines 173–192) wraps the `<GripVertical>` button in a `<FocusScope moniker={asSegment(`card.drag-handle:${entityId}`)}>` leaf. The intent (per the in-source doc-comment, lines 159–172) was to give the spatial-nav graph a navigable atom inside the card zone for the drag-grip affordance.\n+\n+But drag-and-drop on cards is **mouse-only**. `kanban-app/ui/src/components/board-view.tsx` line 588 wires `@dnd-kit` with `useSensor(PointerSensor, …)` — no `KeyboardSensor`. The drag handle has no keyboard activation story: pressing Enter / Space on the focused leaf does nothing useful (no `onClick` action, drag handlers come from `dragHandleProps` and only respond to pointer events). The leaf adds a tab/arrow stop that lands the user on a button they can't actually use without a mouse.\n+\n+### Approach\n+\n+Edit `entity-card.tsx` only. In `DragHandle` (lines 173–192):\n+\n+1. Remove the `<FocusScope moniker={asSegment(`card.drag-handle:${entityId}`)}>` wrapper. The component returns the `<button>` directly.\n+2. Remove the now-unused `entityId` prop and update the single call site at line 121 to drop the `entityId={entity.id}` arg.\n+3. Update the doc-comment (lines 159–172) to state explicitly: \"The drag handle is mouse-only — `@dnd-kit` is configured with `PointerSensor` and no `KeyboardSensor` (see `board-view.tsx::useSensor(PointerSensor, …)`). It is intentionally NOT a `<FocusScope>` because there is nothing the keyboard user could do once focus landed there.\"\n+4. The `onClick={(e) => e.stopPropagation()}` stays — it prevents click-bubble to the card body (which would dispatch `ui.inspect` via the `<Inspectable>` wrapper). Mouse activation of the drag handle is the only pointer story; keyboard is out of scope.\n+\n+### Knock-on changes\n+\n+- `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx` lines 283–306 (and the file's docstring at lines 28–30) currently pin the drag-handle leaf as a `card.drag-handle:{id}` scope under the card zone. Update both:\n+  - The doc-comment list of \"inner leaf scopes\" should read just \"the inspect-button\" (drag-handle removed).\n+  - The `it(\"the drag-handle leaf registers as a scope under the card zone\")` test must invert: assert that **no** `spatial_register_scope` call has `segment === \"card.drag-handle:task-1\"`.\n+  - The neighbouring test `it(\"the inspect-button leaf registers as a scope under the card zone\")` (or equivalent) stays as-is — inspect IS keyboard-activatable (Space → inspect via `<Inspectable>`).\n+\n+### Why not keep it focusable but invisible to keyboard?\n+\n+The cleanest answer is \"don't register what cannot be acted on.\" A focusable element with no keyboard action is a tab-stop trap. The card's other inner leaf (`InspectButton`) has a real keyboard action (Space/Enter → dispatch `ui.inspect`); the drag handle has none.\n+\n+## Acceptance Criteria\n+- [ ] `DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` no longer wraps its `<button>` in a `<FocusScope>`. The component takes only `dragHandleProps` and returns `<button … onClick={…} {...dragHandleProps}><GripVertical /></button>`.\n+- [ ] The single call site (line ~120) is updated — no `entityId` prop passed.\n+- [ ] Doc-comment on `DragHandle` is rewritten to explain the mouse-only rationale and reference `board-view.tsx::useSensor(PointerSensor, …)` as the source of truth.\n+- [ ] `mockInvoke(\"spatial_register_scope\", …)` calls during card mount no longer include any segment matching `^card\\.drag-handle:`.\n+- [ ] Drag-and-drop still works: `dragHandleProps` are still spread onto the `<button>`, so dnd-kit's pointer sensor still picks up grip drags.\n+- [ ] Click on the drag handle still does NOT bubble to the card body's inspect handler (the `onClick={(e) => e.stopPropagation()}` is preserved).\n+\n+## Tests\n+- [ ] Update `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx`:\n+  - Rewrite `it(\"the drag-handle leaf registers as a scope under the card zone\")` (line ~283) to `it(\"the drag-handle does NOT register as a scope\")`. Assert that `mockInvoke` was called zero times with `cmd === \"spatial_register_scope\"` and `args.segment` matching `/^card\\.drag-handle:/`.\n+  - Update the file's top-of-file docstring (lines 28–30) to remove \"drag-handle\" from the list of inner leaf scopes.\n+- [ ] Existing card spatial tests stay green: `entity-card.spatial.test.tsx`, `entity-card.test.tsx`, `entity-card.field-icon-inside-zone.browser.test.tsx`, `column-view.spatial-nav.test.tsx`, `board-view.cross-column-nav.spatial.test.tsx`. In particular, the inspect-button leaf assertion in `scope-leaf.spatial.test.tsx` must remain green (inspect IS keyboard-actionable; only the drag handle is being demoted).\n+- [ ] Existing drag-drop tests stay green: `column-dragover.browser.test.tsx`, `sortable-task-card.test.tsx`, `sortable-column.tsx`'s tests. Confirm the pointer-driven drag path is unaffected by removing the FocusScope (the FocusScope was a sibling of the drag handlers, not in their event path).\n+- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/entity-card src/components/sortable-task-card src/components/column-dragover` and confirm green.\n+\n+## Workflow\n+- Use `/tdd` — invert the existing scope-leaf assertion first (failing test demanding the drag handle NOT be a scope), watch it fail, then remove the wrapper from `DragHandle` and confirm it passes. Final pass: drag-drop browser tests stay green.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,52 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: bf80\n-project: spatial-nav\n-title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n----\n-## What\n-\n-`DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` (lines 173–192) wraps the `<GripVertical>` button in a `<FocusScope moniker={asSegment(`card.drag-handle:${entityId}`)}>` leaf. The intent (per the in-source doc-comment, lines 159–172) was to give the spatial-nav graph a navigable atom inside the card zone for the drag-grip affordance.\n-\n-But drag-and-drop on cards is **mouse-only**. `kanban-app/ui/src/components/board-view.tsx` line 588 wires `@dnd-kit` with `useSensor(PointerSensor, …)` — no `KeyboardSensor`. The drag handle has no keyboard activation story: pressing Enter / Space on the focused leaf does nothing useful (no `onClick` action, drag handlers come from `dragHandleProps` and only respond to pointer events). The leaf adds a tab/arrow stop that lands the user on a button they can't actually use without a mouse.\n-\n-### Approach\n-\n-Edit `entity-card.tsx` only. In `DragHandle` (lines 173–192):\n-\n-1. Remove the `<FocusScope moniker={asSegment(`card.drag-handle:${entityId}`)}>` wrapper. The component returns the `<button>` directly.\n-2. Remove the now-unused `entityId` prop and update the single call site at line 121 to drop the `entityId={entity.id}` arg.\n-3. Update the doc-comment (lines 159–172) to state explicitly: \"The drag handle is mouse-only — `@dnd-kit` is configured with `PointerSensor` and no `KeyboardSensor` (see `board-view.tsx::useSensor(PointerSensor, …)`). It is intentionally NOT a `<FocusScope>` because there is nothing the keyboard user could do once focus landed there.\"\n-4. The `onClick={(e) => e.stopPropagation()}` stays — it prevents click-bubble to the card body (which would dispatch `ui.inspect` via the `<Inspectable>` wrapper). Mouse activation of the drag handle is the only pointer story; keyboard is out of scope.\n-\n-### Knock-on changes\n-\n-- `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx` lines 283–306 (and the file's docstring at lines 28–30) currently pin the drag-handle leaf as a `card.drag-handle:{id}` scope under the card zone. Update both:\n-  - The doc-comment list of \"inner leaf scopes\" should read just \"the inspect-button\" (drag-handle removed).\n-  - The `it(\"the drag-handle leaf registers as a scope under the card zone\")` test must invert: assert that **no** `spatial_register_scope` call has `segment === \"card.drag-handle:task-1\"`.\n-  - The neighbouring test `it(\"the inspect-button leaf registers as a scope under the card zone\")` (or equivalent) stays as-is — inspect IS keyboard-activatable (Space → inspect via `<Inspectable>`).\n-\n-### Why not keep it focusable but invisible to keyboard?\n-\n-The cleanest answer is \"don't register what cannot be acted on.\" A focusable element with no keyboard action is a tab-stop trap. The card's other inner leaf (`InspectButton`) has a real keyboard action (Space/Enter → dispatch `ui.inspect`); the drag handle has none.\n-\n-## Acceptance Criteria\n-- [ ] `DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` no longer wraps its `<button>` in a `<FocusScope>`. The component takes only `dragHandleProps` and returns `<button … onClick={…} {...dragHandleProps}><GripVertical /></button>`.\n-- [ ] The single call site (line ~120) is updated — no `entityId` prop passed.\n-- [ ] Doc-comment on `DragHandle` is rewritten to explain the mouse-only rationale and reference `board-view.tsx::useSensor(PointerSensor, …)` as the source of truth.\n-- [ ] `mockInvoke(\"spatial_register_scope\", …)` calls during card mount no longer include any segment matching `^card\\.drag-handle:`.\n-- [ ] Drag-and-drop still works: `dragHandleProps` are still spread onto the `<button>`, so dnd-kit's pointer sensor still picks up grip drags.\n-- [ ] Click on the drag handle still does NOT bubble to the card body's inspect handler (the `onClick={(e) => e.stopPropagation()}` is preserved).\n-\n-## Tests\n-- [ ] Update `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx`:\n-  - Rewrite `it(\"the drag-handle leaf registers as a scope under the card zone\")` (line ~283) to `it(\"the drag-handle does NOT register as a scope\")`. Assert that `mockInvoke` was called zero times with `cmd === \"spatial_register_scope\"` and `args.segment` matching `/^card\\.drag-handle:/`.\n-  - Update the file's top-of-file docstring (lines 28–30) to remove \"drag-handle\" from the list of inner leaf scopes.\n-- [ ] Existing card spatial tests stay green: `entity-card.spatial.test.tsx`, `entity-card.test.tsx`, `entity-card.field-icon-inside-zone.browser.test.tsx`, `column-view.spatial-nav.test.tsx`, `board-view.cross-column-nav.spatial.test.tsx`. In particular, the inspect-button leaf assertion in `scope-leaf.spatial.test.tsx` must remain green (inspect IS keyboard-actionable; only the drag handle is being demoted).\n-- [ ] Existing drag-drop tests stay green: `column-dragover.browser.test.tsx`, `sortable-task-card.test.tsx`, `sortable-column.tsx`'s tests. Confirm the pointer-driven drag path is unaffected by removing the FocusScope (the FocusScope was a sibling of the drag handlers, not in their event path).\n-- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/entity-card src/components/sortable-task-card src/components/column-dragover` and confirm green.\n-\n-## Workflow\n-- Use `/tdd` — invert the existing scope-leaf assertion first (failing test demanding the drag handle NOT be a scope), watch it fail, then remove the wrapper from `DragHandle` and confirm it passes. Final pass: drag-drop browser tests stay green.\n","transaction_id":null}
{"id":"01KQM9478Y9YM4G22NFCEAGSNK","timestamp":"2026-05-02T12:03:03.070688Z","entity_type":"task","entity_id":"01KQM9478XFMCBBWHQN6ARE524","op":"create","changes":[["project",{"kind":"set","value":"spatial-nav"}],["position_ordinal",{"kind":"set","value":"bf80"}],["position_column",{"kind":"set","value":"todo"}],["title",{"kind":"set","value":"Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed"}],["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\n`DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` (lines 173–192) wraps the `<GripVertical>` button in a `<FocusScope moniker={asSegment(`card.drag-handle:${entityId}`)}>` leaf. The intent (per the in-source doc-comment, lines 159–172) was to give the spatial-nav graph a navigable atom inside the card zone for the drag-grip affordance.\n\nBut drag-and-drop on cards is **mouse-only**. `kanban-app/ui/src/components/board-view.tsx` line 588 wires `@dnd-kit` with `useSensor(PointerSensor, …)` — no `KeyboardSensor`. The drag handle has no keyboard activation story: pressing Enter / Space on the focused leaf does nothing useful (no `onClick` action, drag handlers come from `dragHandleProps` and only respond to pointer events). The leaf adds a tab/arrow stop that lands the user on a button they can't actually use without a mouse.\n\n### Approach\n\nEdit `entity-card.tsx` only. In `DragHandle` (lines 173–192):\n\n1. Remove the `<FocusScope moniker={asSegment(`card.drag-handle:${entityId}`)}>` wrapper. The component returns the `<button>` directly.\n2. Remove the now-unused `entityId` prop and update the single call site at line 121 to drop the `entityId={entity.id}` arg.\n3. Update the doc-comment (lines 159–172) to state explicitly: \"The drag handle is mouse-only — `@dnd-kit` is configured with `PointerSensor` and no `KeyboardSensor` (see `board-view.tsx::useSensor(PointerSensor, …)`). It is intentionally NOT a `<FocusScope>` because there is nothing the keyboard user could do once focus landed there.\"\n4. The `onClick={(e) => e.stopPropagation()}` stays — it prevents click-bubble to the card body (which would dispatch `ui.inspect` via the `<Inspectable>` wrapper). Mouse activation of the drag handle is the only pointer story; keyboard is out of scope.\n\n### Knock-on changes\n\n- `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx` lines 283–306 (and the file's docstring at lines 28–30) currently pin the drag-handle leaf as a `card.drag-handle:{id}` scope under the card zone. Update both:\n  - The doc-comment list of \"inner leaf scopes\" should read just \"the inspect-button\" (drag-handle removed).\n  - The `it(\"the drag-handle leaf registers as a scope under the card zone\")` test must invert: assert that **no** `spatial_register_scope` call has `segment === \"card.drag-handle:task-1\"`.\n  - The neighbouring test `it(\"the inspect-button leaf registers as a scope under the card zone\")` (or equivalent) stays as-is — inspect IS keyboard-activatable (Space → inspect via `<Inspectable>`).\n\n### Why not keep it focusable but invisible to keyboard?\n\nThe cleanest answer is \"don't register what cannot be acted on.\" A focusable element with no keyboard action is a tab-stop trap. The card's other inner leaf (`InspectButton`) has a real keyboard action (Space/Enter → dispatch `ui.inspect`); the drag handle has none.\n\n## Acceptance Criteria\n- [ ] `DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` no longer wraps its `<button>` in a `<FocusScope>`. The component takes only `dragHandleProps` and returns `<button … onClick={…} {...dragHandleProps}><GripVertical /></button>`.\n- [ ] The single call site (line ~120) is updated — no `entityId` prop passed.\n- [ ] Doc-comment on `DragHandle` is rewritten to explain the mouse-only rationale and reference `board-view.tsx::useSensor(PointerSensor, …)` as the source of truth.\n- [ ] `mockInvoke(\"spatial_register_scope\", …)` calls during card mount no longer include any segment matching `^card\\.drag-handle:`.\n- [ ] Drag-and-drop still works: `dragHandleProps` are still spread onto the `<button>`, so dnd-kit's pointer sensor still picks up grip drags.\n- [ ] Click on the drag handle still does NOT bubble to the card body's inspect handler (the `onClick={(e) => e.stopPropagation()}` is preserved).\n\n## Tests\n- [ ] Update `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx`:\n  - Rewrite `it(\"the drag-handle leaf registers as a scope under the card zone\")` (line ~283) to `it(\"the drag-handle does NOT register as a scope\")`. Assert that `mockInvoke` was called zero times with `cmd === \"spatial_register_scope\"` and `args.segment` matching `/^card\\.drag-handle:/`.\n  - Update the file's top-of-file docstring (lines 28–30) to remove \"drag-handle\" from the list of inner leaf scopes.\n- [ ] Existing card spatial tests stay green: `entity-card.spatial.test.tsx`, `entity-card.test.tsx`, `entity-card.field-icon-inside-zone.browser.test.tsx`, `column-view.spatial-nav.test.tsx`, `board-view.cross-column-nav.spatial.test.tsx`. In particular, the inspect-button leaf assertion in `scope-leaf.spatial.test.tsx` must remain green (inspect IS keyboard-actionable; only the drag handle is being demoted).\n- [ ] Existing drag-drop tests stay green: `column-dragover.browser.test.tsx`, `sortable-task-card.test.tsx`, `sortable-column.tsx`'s tests. Confirm the pointer-driven drag path is unaffected by removing the FocusScope (the FocusScope was a sibling of the drag handlers, not in their event path).\n- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/entity-card src/components/sortable-task-card src/components/column-dragover` and confirm green.\n\n## Workflow\n- Use `/tdd` — invert the existing scope-leaf assertion first (failing test demanding the drag handle NOT be a scope), watch it fail, then remove the wrapper from `DragHandle` and confirm it passes. Final pass: drag-drop browser tests stay green.\n"}]]}
{"id":"01KQPTNKXD9MFBVGN9DVNXB7CW","timestamp":"2026-05-03T11:48:07.725601Z","op":"update","item_id":"01KQM9478XFMCBBWHQN6ARE524","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: todo\n-position_ordinal: bf80\n+position_ordinal: ac80\n project: spatial-nav\n title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: todo\n-position_ordinal: ac80\n+position_ordinal: bf80\n project: spatial-nav\n title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n ---\n","transaction_id":null}
{"id":"01KQPTNKXESHR32R44JF05KWGM","timestamp":"2026-05-03T11:48:07.726363Z","entity_type":"task","entity_id":"01KQM9478XFMCBBWHQN6ARE524","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-bf80\n\\ No newline at end of file\n+ac80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ac80\n\\ No newline at end of file\n+bf80\n\\ No newline at end of file\n"}]]}
{"id":"01KQPTTNTXKBNC0YA03MY57S89","timestamp":"2026-05-03T11:50:53.533504Z","op":"update","item_id":"01KQM9478XFMCBBWHQN6ARE524","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: ac80\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: ac80\n project: spatial-nav\n title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n ---\n","transaction_id":null}
{"id":"01KQPTTNTYBEF066TGP4G40XGC","timestamp":"2026-05-03T11:50:53.534360Z","entity_type":"task","entity_id":"01KQM9478XFMCBBWHQN6ARE524","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ac80\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ac80\n\\ No newline at end of file\n"}]]}
{"id":"01KQPTZVS7A68BPCT2SGKJ7VTB","timestamp":"2026-05-03T11:53:43.463048Z","op":"update","item_id":"01KQM9478XFMCBBWHQN6ARE524","forward_patch":"--- original\n+++ modified\n@@ -33,20 +33,20 @@\n The cleanest answer is \"don't register what cannot be acted on.\" A focusable element with no keyboard action is a tab-stop trap. The card's other inner leaf (`InspectButton`) has a real keyboard action (Space/Enter → dispatch `ui.inspect`); the drag handle has none.\n\n ## Acceptance Criteria\n-- [ ] `DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` no longer wraps its `<button>` in a `<FocusScope>`. The component takes only `dragHandleProps` and returns `<button … onClick={…} {...dragHandleProps}><GripVertical /></button>`.\n-- [ ] The single call site (line ~120) is updated — no `entityId` prop passed.\n-- [ ] Doc-comment on `DragHandle` is rewritten to explain the mouse-only rationale and reference `board-view.tsx::useSensor(PointerSensor, …)` as the source of truth.\n-- [ ] `mockInvoke(\"spatial_register_scope\", …)` calls during card mount no longer include any segment matching `^card\\.drag-handle:`.\n-- [ ] Drag-and-drop still works: `dragHandleProps` are still spread onto the `<button>`, so dnd-kit's pointer sensor still picks up grip drags.\n-- [ ] Click on the drag handle still does NOT bubble to the card body's inspect handler (the `onClick={(e) => e.stopPropagation()}` is preserved).\n+- [x] `DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` no longer wraps its `<button>` in a `<FocusScope>`. The component takes only `dragHandleProps` and returns `<button … onClick={…} {...dragHandleProps}><GripVertical /></button>`.\n+- [x] The single call site (line ~120) is updated — no `entityId` prop passed.\n+- [x] Doc-comment on `DragHandle` is rewritten to explain the mouse-only rationale and reference `board-view.tsx::useSensor(PointerSensor, …)` as the source of truth.\n+- [x] `mockInvoke(\"spatial_register_scope\", …)` calls during card mount no longer include any segment matching `^card\\.drag-handle:`.\n+- [x] Drag-and-drop still works: `dragHandleProps` are still spread onto the `<button>`, so dnd-kit's pointer sensor still picks up grip drags.\n+- [x] Click on the drag handle still does NOT bubble to the card body's inspect handler (the `onClick={(e) => e.stopPropagation()}` is preserved).\n\n ## Tests\n-- [ ] Update `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx`:\n+- [x] Update `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx`:\n   - Rewrite `it(\"the drag-handle leaf registers as a scope under the card zone\")` (line ~283) to `it(\"the drag-handle does NOT register as a scope\")`. Assert that `mockInvoke` was called zero times with `cmd === \"spatial_register_scope\"` and `args.segment` matching `/^card\\.drag-handle:/`.\n   - Update the file's top-of-file docstring (lines 28–30) to remove \"drag-handle\" from the list of inner leaf scopes.\n-- [ ] Existing card spatial tests stay green: `entity-card.spatial.test.tsx`, `entity-card.test.tsx`, `entity-card.field-icon-inside-zone.browser.test.tsx`, `column-view.spatial-nav.test.tsx`, `board-view.cross-column-nav.spatial.test.tsx`. In particular, the inspect-button leaf assertion in `scope-leaf.spatial.test.tsx` must remain green (inspect IS keyboard-actionable; only the drag handle is being demoted).\n-- [ ] Existing drag-drop tests stay green: `column-dragover.browser.test.tsx`, `sortable-task-card.test.tsx`, `sortable-column.tsx`'s tests. Confirm the pointer-driven drag path is unaffected by removing the FocusScope (the FocusScope was a sibling of the drag handlers, not in their event path).\n-- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/entity-card src/components/sortable-task-card src/components/column-dragover` and confirm green.\n+- [x] Existing card spatial tests stay green: `entity-card.spatial.test.tsx`, `entity-card.test.tsx`, `entity-card.field-icon-inside-zone.browser.test.tsx`, `column-view.spatial-nav.test.tsx`, `board-view.cross-column-nav.spatial.test.tsx`. In particular, the inspect-button leaf assertion in `scope-leaf.spatial.test.tsx` must remain green (inspect IS keyboard-actionable; only the drag handle is being demoted).\n+- [x] Existing drag-drop tests stay green: `column-dragover.browser.test.tsx`, `sortable-task-card.test.tsx`, `sortable-column.tsx`'s tests. Confirm the pointer-driven drag path is unaffected by removing the FocusScope (the FocusScope was a sibling of the drag handlers, not in their event path).\n+- [x] Run `cd kanban-app/ui && pnpm vitest run src/components/entity-card src/components/sortable-task-card src/components/column-dragover` and confirm green.\n\n ## Workflow\n - Use `/tdd` — invert the existing scope-leaf assertion first (failing test demanding the drag handle NOT be a scope), watch it fail, then remove the wrapper from `DragHandle` and confirm it passes. Final pass: drag-drop browser tests stay green.\n","reverse_patch":"--- original\n+++ modified\n@@ -33,20 +33,20 @@\n The cleanest answer is \"don't register what cannot be acted on.\" A focusable element with no keyboard action is a tab-stop trap. The card's other inner leaf (`InspectButton`) has a real keyboard action (Space/Enter → dispatch `ui.inspect`); the drag handle has none.\n\n ## Acceptance Criteria\n-- [x] `DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` no longer wraps its `<button>` in a `<FocusScope>`. The component takes only `dragHandleProps` and returns `<button … onClick={…} {...dragHandleProps}><GripVertical /></button>`.\n-- [x] The single call site (line ~120) is updated — no `entityId` prop passed.\n-- [x] Doc-comment on `DragHandle` is rewritten to explain the mouse-only rationale and reference `board-view.tsx::useSensor(PointerSensor, …)` as the source of truth.\n-- [x] `mockInvoke(\"spatial_register_scope\", …)` calls during card mount no longer include any segment matching `^card\\.drag-handle:`.\n-- [x] Drag-and-drop still works: `dragHandleProps` are still spread onto the `<button>`, so dnd-kit's pointer sensor still picks up grip drags.\n-- [x] Click on the drag handle still does NOT bubble to the card body's inspect handler (the `onClick={(e) => e.stopPropagation()}` is preserved).\n+- [ ] `DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` no longer wraps its `<button>` in a `<FocusScope>`. The component takes only `dragHandleProps` and returns `<button … onClick={…} {...dragHandleProps}><GripVertical /></button>`.\n+- [ ] The single call site (line ~120) is updated — no `entityId` prop passed.\n+- [ ] Doc-comment on `DragHandle` is rewritten to explain the mouse-only rationale and reference `board-view.tsx::useSensor(PointerSensor, …)` as the source of truth.\n+- [ ] `mockInvoke(\"spatial_register_scope\", …)` calls during card mount no longer include any segment matching `^card\\.drag-handle:`.\n+- [ ] Drag-and-drop still works: `dragHandleProps` are still spread onto the `<button>`, so dnd-kit's pointer sensor still picks up grip drags.\n+- [ ] Click on the drag handle still does NOT bubble to the card body's inspect handler (the `onClick={(e) => e.stopPropagation()}` is preserved).\n\n ## Tests\n-- [x] Update `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx`:\n+- [ ] Update `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx`:\n   - Rewrite `it(\"the drag-handle leaf registers as a scope under the card zone\")` (line ~283) to `it(\"the drag-handle does NOT register as a scope\")`. Assert that `mockInvoke` was called zero times with `cmd === \"spatial_register_scope\"` and `args.segment` matching `/^card\\.drag-handle:/`.\n   - Update the file's top-of-file docstring (lines 28–30) to remove \"drag-handle\" from the list of inner leaf scopes.\n-- [x] Existing card spatial tests stay green: `entity-card.spatial.test.tsx`, `entity-card.test.tsx`, `entity-card.field-icon-inside-zone.browser.test.tsx`, `column-view.spatial-nav.test.tsx`, `board-view.cross-column-nav.spatial.test.tsx`. In particular, the inspect-button leaf assertion in `scope-leaf.spatial.test.tsx` must remain green (inspect IS keyboard-actionable; only the drag handle is being demoted).\n-- [x] Existing drag-drop tests stay green: `column-dragover.browser.test.tsx`, `sortable-task-card.test.tsx`, `sortable-column.tsx`'s tests. Confirm the pointer-driven drag path is unaffected by removing the FocusScope (the FocusScope was a sibling of the drag handlers, not in their event path).\n-- [x] Run `cd kanban-app/ui && pnpm vitest run src/components/entity-card src/components/sortable-task-card src/components/column-dragover` and confirm green.\n+- [ ] Existing card spatial tests stay green: `entity-card.spatial.test.tsx`, `entity-card.test.tsx`, `entity-card.field-icon-inside-zone.browser.test.tsx`, `column-view.spatial-nav.test.tsx`, `board-view.cross-column-nav.spatial.test.tsx`. In particular, the inspect-button leaf assertion in `scope-leaf.spatial.test.tsx` must remain green (inspect IS keyboard-actionable; only the drag handle is being demoted).\n+- [ ] Existing drag-drop tests stay green: `column-dragover.browser.test.tsx`, `sortable-task-card.test.tsx`, `sortable-column.tsx`'s tests. Confirm the pointer-driven drag path is unaffected by removing the FocusScope (the FocusScope was a sibling of the drag handlers, not in their event path).\n+- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/entity-card src/components/sortable-task-card src/components/column-dragover` and confirm green.\n\n ## Workflow\n - Use `/tdd` — invert the existing scope-leaf assertion first (failing test demanding the drag handle NOT be a scope), watch it fail, then remove the wrapper from `DragHandle` and confirm it passes. Final pass: drag-drop browser tests stay green.\n","transaction_id":null}
{"id":"01KQPTZVSB0JZ4Y6EPGJQBJS5Y","timestamp":"2026-05-03T11:53:43.467720Z","entity_type":"task","entity_id":"01KQM9478XFMCBBWHQN6ARE524","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -25,20 +25,20 @@\n The cleanest answer is \"don't register what cannot be acted on.\" A focusable element with no keyboard action is a tab-stop trap. The card's other inner leaf (`InspectButton`) has a real keyboard action (Space/Enter → dispatch `ui.inspect`); the drag handle has none.\n\n ## Acceptance Criteria\n-- [ ] `DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` no longer wraps its `<button>` in a `<FocusScope>`. The component takes only `dragHandleProps` and returns `<button … onClick={…} {...dragHandleProps}><GripVertical /></button>`.\n-- [ ] The single call site (line ~120) is updated — no `entityId` prop passed.\n-- [ ] Doc-comment on `DragHandle` is rewritten to explain the mouse-only rationale and reference `board-view.tsx::useSensor(PointerSensor, …)` as the source of truth.\n-- [ ] `mockInvoke(\"spatial_register_scope\", …)` calls during card mount no longer include any segment matching `^card\\.drag-handle:`.\n-- [ ] Drag-and-drop still works: `dragHandleProps` are still spread onto the `<button>`, so dnd-kit's pointer sensor still picks up grip drags.\n-- [ ] Click on the drag handle still does NOT bubble to the card body's inspect handler (the `onClick={(e) => e.stopPropagation()}` is preserved).\n+- [x] `DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` no longer wraps its `<button>` in a `<FocusScope>`. The component takes only `dragHandleProps` and returns `<button … onClick={…} {...dragHandleProps}><GripVertical /></button>`.\n+- [x] The single call site (line ~120) is updated — no `entityId` prop passed.\n+- [x] Doc-comment on `DragHandle` is rewritten to explain the mouse-only rationale and reference `board-view.tsx::useSensor(PointerSensor, …)` as the source of truth.\n+- [x] `mockInvoke(\"spatial_register_scope\", …)` calls during card mount no longer include any segment matching `^card\\.drag-handle:`.\n+- [x] Drag-and-drop still works: `dragHandleProps` are still spread onto the `<button>`, so dnd-kit's pointer sensor still picks up grip drags.\n+- [x] Click on the drag handle still does NOT bubble to the card body's inspect handler (the `onClick={(e) => e.stopPropagation()}` is preserved).\n\n ## Tests\n-- [ ] Update `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx`:\n+- [x] Update `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx`:\n   - Rewrite `it(\"the drag-handle leaf registers as a scope under the card zone\")` (line ~283) to `it(\"the drag-handle does NOT register as a scope\")`. Assert that `mockInvoke` was called zero times with `cmd === \"spatial_register_scope\"` and `args.segment` matching `/^card\\.drag-handle:/`.\n   - Update the file's top-of-file docstring (lines 28–30) to remove \"drag-handle\" from the list of inner leaf scopes.\n-- [ ] Existing card spatial tests stay green: `entity-card.spatial.test.tsx`, `entity-card.test.tsx`, `entity-card.field-icon-inside-zone.browser.test.tsx`, `column-view.spatial-nav.test.tsx`, `board-view.cross-column-nav.spatial.test.tsx`. In particular, the inspect-button leaf assertion in `scope-leaf.spatial.test.tsx` must remain green (inspect IS keyboard-actionable; only the drag handle is being demoted).\n-- [ ] Existing drag-drop tests stay green: `column-dragover.browser.test.tsx`, `sortable-task-card.test.tsx`, `sortable-column.tsx`'s tests. Confirm the pointer-driven drag path is unaffected by removing the FocusScope (the FocusScope was a sibling of the drag handlers, not in their event path).\n-- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/entity-card src/components/sortable-task-card src/components/column-dragover` and confirm green.\n+- [x] Existing card spatial tests stay green: `entity-card.spatial.test.tsx`, `entity-card.test.tsx`, `entity-card.field-icon-inside-zone.browser.test.tsx`, `column-view.spatial-nav.test.tsx`, `board-view.cross-column-nav.spatial.test.tsx`. In particular, the inspect-button leaf assertion in `scope-leaf.spatial.test.tsx` must remain green (inspect IS keyboard-actionable; only the drag handle is being demoted).\n+- [x] Existing drag-drop tests stay green: `column-dragover.browser.test.tsx`, `sortable-task-card.test.tsx`, `sortable-column.tsx`'s tests. Confirm the pointer-driven drag path is unaffected by removing the FocusScope (the FocusScope was a sibling of the drag handlers, not in their event path).\n+- [x] Run `cd kanban-app/ui && pnpm vitest run src/components/entity-card src/components/sortable-task-card src/components/column-dragover` and confirm green.\n\n ## Workflow\n - Use `/tdd` — invert the existing scope-leaf assertion first (failing test demanding the drag handle NOT be a scope), watch it fail, then remove the wrapper from `DragHandle` and confirm it passes. Final pass: drag-drop browser tests stay green.\n","reverse_patch":"--- original\n+++ modified\n@@ -25,20 +25,20 @@\n The cleanest answer is \"don't register what cannot be acted on.\" A focusable element with no keyboard action is a tab-stop trap. The card's other inner leaf (`InspectButton`) has a real keyboard action (Space/Enter → dispatch `ui.inspect`); the drag handle has none.\n\n ## Acceptance Criteria\n-- [x] `DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` no longer wraps its `<button>` in a `<FocusScope>`. The component takes only `dragHandleProps` and returns `<button … onClick={…} {...dragHandleProps}><GripVertical /></button>`.\n-- [x] The single call site (line ~120) is updated — no `entityId` prop passed.\n-- [x] Doc-comment on `DragHandle` is rewritten to explain the mouse-only rationale and reference `board-view.tsx::useSensor(PointerSensor, …)` as the source of truth.\n-- [x] `mockInvoke(\"spatial_register_scope\", …)` calls during card mount no longer include any segment matching `^card\\.drag-handle:`.\n-- [x] Drag-and-drop still works: `dragHandleProps` are still spread onto the `<button>`, so dnd-kit's pointer sensor still picks up grip drags.\n-- [x] Click on the drag handle still does NOT bubble to the card body's inspect handler (the `onClick={(e) => e.stopPropagation()}` is preserved).\n+- [ ] `DragHandle` in `kanban-app/ui/src/components/entity-card.tsx` no longer wraps its `<button>` in a `<FocusScope>`. The component takes only `dragHandleProps` and returns `<button … onClick={…} {...dragHandleProps}><GripVertical /></button>`.\n+- [ ] The single call site (line ~120) is updated — no `entityId` prop passed.\n+- [ ] Doc-comment on `DragHandle` is rewritten to explain the mouse-only rationale and reference `board-view.tsx::useSensor(PointerSensor, …)` as the source of truth.\n+- [ ] `mockInvoke(\"spatial_register_scope\", …)` calls during card mount no longer include any segment matching `^card\\.drag-handle:`.\n+- [ ] Drag-and-drop still works: `dragHandleProps` are still spread onto the `<button>`, so dnd-kit's pointer sensor still picks up grip drags.\n+- [ ] Click on the drag handle still does NOT bubble to the card body's inspect handler (the `onClick={(e) => e.stopPropagation()}` is preserved).\n\n ## Tests\n-- [x] Update `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx`:\n+- [ ] Update `kanban-app/ui/src/components/entity-card.scope-leaf.spatial.test.tsx`:\n   - Rewrite `it(\"the drag-handle leaf registers as a scope under the card zone\")` (line ~283) to `it(\"the drag-handle does NOT register as a scope\")`. Assert that `mockInvoke` was called zero times with `cmd === \"spatial_register_scope\"` and `args.segment` matching `/^card\\.drag-handle:/`.\n   - Update the file's top-of-file docstring (lines 28–30) to remove \"drag-handle\" from the list of inner leaf scopes.\n-- [x] Existing card spatial tests stay green: `entity-card.spatial.test.tsx`, `entity-card.test.tsx`, `entity-card.field-icon-inside-zone.browser.test.tsx`, `column-view.spatial-nav.test.tsx`, `board-view.cross-column-nav.spatial.test.tsx`. In particular, the inspect-button leaf assertion in `scope-leaf.spatial.test.tsx` must remain green (inspect IS keyboard-actionable; only the drag handle is being demoted).\n-- [x] Existing drag-drop tests stay green: `column-dragover.browser.test.tsx`, `sortable-task-card.test.tsx`, `sortable-column.tsx`'s tests. Confirm the pointer-driven drag path is unaffected by removing the FocusScope (the FocusScope was a sibling of the drag handlers, not in their event path).\n-- [x] Run `cd kanban-app/ui && pnpm vitest run src/components/entity-card src/components/sortable-task-card src/components/column-dragover` and confirm green.\n+- [ ] Existing card spatial tests stay green: `entity-card.spatial.test.tsx`, `entity-card.test.tsx`, `entity-card.field-icon-inside-zone.browser.test.tsx`, `column-view.spatial-nav.test.tsx`, `board-view.cross-column-nav.spatial.test.tsx`. In particular, the inspect-button leaf assertion in `scope-leaf.spatial.test.tsx` must remain green (inspect IS keyboard-actionable; only the drag handle is being demoted).\n+- [ ] Existing drag-drop tests stay green: `column-dragover.browser.test.tsx`, `sortable-task-card.test.tsx`, `sortable-column.tsx`'s tests. Confirm the pointer-driven drag path is unaffected by removing the FocusScope (the FocusScope was a sibling of the drag handlers, not in their event path).\n+- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/entity-card src/components/sortable-task-card src/components/column-dragover` and confirm green.\n\n ## Workflow\n - Use `/tdd` — invert the existing scope-leaf assertion first (failing test demanding the drag handle NOT be a scope), watch it fail, then remove the wrapper from `DragHandle` and confirm it passes. Final pass: drag-drop browser tests stay green.\n"}]]}
{"id":"01KQPV12358WVT4EH87F3A6C39","timestamp":"2026-05-03T11:54:22.693630Z","op":"update","item_id":"01KQM9478XFMCBBWHQN6ARE524","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: spatial-nav\n title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: spatial-nav\n title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n","transaction_id":null}
{"id":"01KQPV1236867N3YS4TBZWBKC5","timestamp":"2026-05-03T11:54:22.694193Z","entity_type":"task","entity_id":"01KQM9478XFMCBBWHQN6ARE524","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KQPVD3QQWBX9CXMM05BSR8G1","timestamp":"2026-05-03T12:00:57.591313Z","op":"update","item_id":"01KQM9478XFMCBBWHQN6ARE524","forward_patch":"--- original\n+++ modified\n@@ -50,3 +50,8 @@\n\n ## Workflow\n - Use `/tdd` — invert the existing scope-leaf assertion first (failing test demanding the drag handle NOT be a scope), watch it fail, then remove the wrapper from `DragHandle` and confirm it passes. Final pass: drag-drop browser tests stay green.\n+\n+## Review Findings (2026-05-02 12:30)\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/components/entity-card.in-zone-nav.spatial.test.tsx` (the `ArrowLeft from card.inspect:{id} lands on the title field zone` test, the block guarded by `if (dragNode)`) — Now that `DragHandle` no longer registers a `<FocusScope>`, no element carries `data-segment='card.drag-handle:task-1'`, so `dragNode` is always `null` and the entire `if (dragNode) { expect(...).toBeNull() }` branch is dead code. Either delete the dead conditional outright (the primary `titleNode` assertion still pins the desired ArrowLeft outcome), or replace it with a positive assertion that no element carries that `data-segment` attribute. While there, refresh the test's docstring (the `ArrowLeft from card.inspect:{id}` block) and the inline comment that begins \"Crucially, focus must NOT be on the drag-handle leaf\" — both still describe a \"drag-handle leaf\" that no longer exists, so the pre-fix counterfactual is no longer reproducible and the wording is stale.\n","reverse_patch":"--- original\n+++ modified\n@@ -50,8 +50,3 @@\n\n ## Workflow\n - Use `/tdd` — invert the existing scope-leaf assertion first (failing test demanding the drag handle NOT be a scope), watch it fail, then remove the wrapper from `DragHandle` and confirm it passes. Final pass: drag-drop browser tests stay green.\n-\n-## Review Findings (2026-05-02 12:30)\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/components/entity-card.in-zone-nav.spatial.test.tsx` (the `ArrowLeft from card.inspect:{id} lands on the title field zone` test, the block guarded by `if (dragNode)`) — Now that `DragHandle` no longer registers a `<FocusScope>`, no element carries `data-segment='card.drag-handle:task-1'`, so `dragNode` is always `null` and the entire `if (dragNode) { expect(...).toBeNull() }` branch is dead code. Either delete the dead conditional outright (the primary `titleNode` assertion still pins the desired ArrowLeft outcome), or replace it with a positive assertion that no element carries that `data-segment` attribute. While there, refresh the test's docstring (the `ArrowLeft from card.inspect:{id}` block) and the inline comment that begins \"Crucially, focus must NOT be on the drag-handle leaf\" — both still describe a \"drag-handle leaf\" that no longer exists, so the pre-fix counterfactual is no longer reproducible and the wording is stale.\n","transaction_id":null}
{"id":"01KQPVD3QQJWGE52AXGCHJCZPV","timestamp":"2026-05-03T12:00:57.591733Z","entity_type":"task","entity_id":"01KQM9478XFMCBBWHQN6ARE524","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -42,3 +42,8 @@\n\n ## Workflow\n - Use `/tdd` — invert the existing scope-leaf assertion first (failing test demanding the drag handle NOT be a scope), watch it fail, then remove the wrapper from `DragHandle` and confirm it passes. Final pass: drag-drop browser tests stay green.\n+\n+## Review Findings (2026-05-02 12:30)\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/components/entity-card.in-zone-nav.spatial.test.tsx` (the `ArrowLeft from card.inspect:{id} lands on the title field zone` test, the block guarded by `if (dragNode)`) — Now that `DragHandle` no longer registers a `<FocusScope>`, no element carries `data-segment='card.drag-handle:task-1'`, so `dragNode` is always `null` and the entire `if (dragNode) { expect(...).toBeNull() }` branch is dead code. Either delete the dead conditional outright (the primary `titleNode` assertion still pins the desired ArrowLeft outcome), or replace it with a positive assertion that no element carries that `data-segment` attribute. While there, refresh the test's docstring (the `ArrowLeft from card.inspect:{id}` block) and the inline comment that begins \"Crucially, focus must NOT be on the drag-handle leaf\" — both still describe a \"drag-handle leaf\" that no longer exists, so the pre-fix counterfactual is no longer reproducible and the wording is stale.\n","reverse_patch":"--- original\n+++ modified\n@@ -42,8 +42,3 @@\n\n ## Workflow\n - Use `/tdd` — invert the existing scope-leaf assertion first (failing test demanding the drag handle NOT be a scope), watch it fail, then remove the wrapper from `DragHandle` and confirm it passes. Final pass: drag-drop browser tests stay green.\n-\n-## Review Findings (2026-05-02 12:30)\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/components/entity-card.in-zone-nav.spatial.test.tsx` (the `ArrowLeft from card.inspect:{id} lands on the title field zone` test, the block guarded by `if (dragNode)`) — Now that `DragHandle` no longer registers a `<FocusScope>`, no element carries `data-segment='card.drag-handle:task-1'`, so `dragNode` is always `null` and the entire `if (dragNode) { expect(...).toBeNull() }` branch is dead code. Either delete the dead conditional outright (the primary `titleNode` assertion still pins the desired ArrowLeft outcome), or replace it with a positive assertion that no element carries that `data-segment` attribute. While there, refresh the test's docstring (the `ArrowLeft from card.inspect:{id}` block) and the inline comment that begins \"Crucially, focus must NOT be on the drag-handle leaf\" — both still describe a \"drag-handle leaf\" that no longer exists, so the pre-fix counterfactual is no longer reproducible and the wording is stale.\n"}]]}
{"id":"01KQPVFTRW53VMG8151V275G7T","timestamp":"2026-05-03T12:02:26.716539Z","op":"update","item_id":"01KQM9478XFMCBBWHQN6ARE524","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: spatial-nav\n title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: spatial-nav\n title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n","transaction_id":null}
{"id":"01KQPVFTRXX1YMB8TN700BENC1","timestamp":"2026-05-03T12:02:26.717395Z","entity_type":"task","entity_id":"01KQM9478XFMCBBWHQN6ARE524","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}]]}
{"id":"01KQPVK7YF0NNDR839ZJN2MH0J","timestamp":"2026-05-03T12:04:18.511260Z","op":"update","item_id":"01KQM9478XFMCBBWHQN6ARE524","forward_patch":"--- original\n+++ modified\n@@ -54,4 +54,7 @@\n ## Review Findings (2026-05-02 12:30)\n\n ### Nits\n-- [ ] `kanban-app/ui/src/components/entity-card.in-zone-nav.spatial.test.tsx` (the `ArrowLeft from card.inspect:{id} lands on the title field zone` test, the block guarded by `if (dragNode)`) — Now that `DragHandle` no longer registers a `<FocusScope>`, no element carries `data-segment='card.drag-handle:task-1'`, so `dragNode` is always `null` and the entire `if (dragNode) { expect(...).toBeNull() }` branch is dead code. Either delete the dead conditional outright (the primary `titleNode` assertion still pins the desired ArrowLeft outcome), or replace it with a positive assertion that no element carries that `data-segment` attribute. While there, refresh the test's docstring (the `ArrowLeft from card.inspect:{id}` block) and the inline comment that begins \"Crucially, focus must NOT be on the drag-handle leaf\" — both still describe a \"drag-handle leaf\" that no longer exists, so the pre-fix counterfactual is no longer reproducible and the wording is stale.\n+- [x] `kanban-app/ui/src/components/entity-card.in-zone-nav.spatial.test.tsx` (the `ArrowLeft from card.inspect:{id} lands on the title field zone` test, the block guarded by `if (dragNode)`) — Now that `DragHandle` no longer registers a `<FocusScope>`, no element carries `data-segment='card.drag-handle:task-1'`, so `dragNode` is always `null` and the entire `if (dragNode) { expect(...).toBeNull() }` branch is dead code. Either delete the dead conditional outright (the primary `titleNode` assertion still pins the desired ArrowLeft outcome), or replace it with a positive assertion that no element carries that `data-segment` attribute. While there, refresh the test's docstring (the `ArrowLeft from card.inspect:{id}` block) and the inline comment that begins \"Crucially, focus must NOT be on the drag-handle leaf\" — both still describe a \"drag-handle leaf\" that no longer exists, so the pre-fix counterfactual is no longer reproducible and the wording is stale.\n+\n+### Resolution (2026-05-03)\n+Replaced the dead `if (dragNode) { ... }` conditional in `entity-card.in-zone-nav.spatial.test.tsx` with a positive `expect(dragNode).toBeNull()` assertion that pins the post-fix invariant: the drag handle is intentionally absent from the spatial-nav DOM. Refreshed the test docstring on `it(\"ArrowLeft from card.inspect:{id} lands on the title field zone\")` and the inline comment to describe the current state (drag handle is mouse-only, not a `<FocusScope>`) and to drop the now-unreproducible \"jumping over the title to the drag-handle leaf\" pre-fix narrative. Also refreshed the `TASK_SCHEMA` docstring (the `[drag-handle leaf, title field zone, inspect leaf]` list) to read `[title field zone, inspect leaf]` with a parenthetical noting why the drag handle isn't in the list. Verified with `pnpm vitest run src/components/entity-card` — 58/58 pass.\n","reverse_patch":"--- original\n+++ modified\n@@ -54,7 +54,4 @@\n ## Review Findings (2026-05-02 12:30)\n\n ### Nits\n-- [x] `kanban-app/ui/src/components/entity-card.in-zone-nav.spatial.test.tsx` (the `ArrowLeft from card.inspect:{id} lands on the title field zone` test, the block guarded by `if (dragNode)`) — Now that `DragHandle` no longer registers a `<FocusScope>`, no element carries `data-segment='card.drag-handle:task-1'`, so `dragNode` is always `null` and the entire `if (dragNode) { expect(...).toBeNull() }` branch is dead code. Either delete the dead conditional outright (the primary `titleNode` assertion still pins the desired ArrowLeft outcome), or replace it with a positive assertion that no element carries that `data-segment` attribute. While there, refresh the test's docstring (the `ArrowLeft from card.inspect:{id}` block) and the inline comment that begins \"Crucially, focus must NOT be on the drag-handle leaf\" — both still describe a \"drag-handle leaf\" that no longer exists, so the pre-fix counterfactual is no longer reproducible and the wording is stale.\n-\n-### Resolution (2026-05-03)\n-Replaced the dead `if (dragNode) { ... }` conditional in `entity-card.in-zone-nav.spatial.test.tsx` with a positive `expect(dragNode).toBeNull()` assertion that pins the post-fix invariant: the drag handle is intentionally absent from the spatial-nav DOM. Refreshed the test docstring on `it(\"ArrowLeft from card.inspect:{id} lands on the title field zone\")` and the inline comment to describe the current state (drag handle is mouse-only, not a `<FocusScope>`) and to drop the now-unreproducible \"jumping over the title to the drag-handle leaf\" pre-fix narrative. Also refreshed the `TASK_SCHEMA` docstring (the `[drag-handle leaf, title field zone, inspect leaf]` list) to read `[title field zone, inspect leaf]` with a parenthetical noting why the drag handle isn't in the list. Verified with `pnpm vitest run src/components/entity-card` — 58/58 pass.\n+- [ ] `kanban-app/ui/src/components/entity-card.in-zone-nav.spatial.test.tsx` (the `ArrowLeft from card.inspect:{id} lands on the title field zone` test, the block guarded by `if (dragNode)`) — Now that `DragHandle` no longer registers a `<FocusScope>`, no element carries `data-segment='card.drag-handle:task-1'`, so `dragNode` is always `null` and the entire `if (dragNode) { expect(...).toBeNull() }` branch is dead code. Either delete the dead conditional outright (the primary `titleNode` assertion still pins the desired ArrowLeft outcome), or replace it with a positive assertion that no element carries that `data-segment` attribute. While there, refresh the test's docstring (the `ArrowLeft from card.inspect:{id}` block) and the inline comment that begins \"Crucially, focus must NOT be on the drag-handle leaf\" — both still describe a \"drag-handle leaf\" that no longer exists, so the pre-fix counterfactual is no longer reproducible and the wording is stale.\n","transaction_id":null}
{"id":"01KQPVK7YF23HVQ9XHP0ANG3CG","timestamp":"2026-05-03T12:04:18.511752Z","entity_type":"task","entity_id":"01KQM9478XFMCBBWHQN6ARE524","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -46,4 +46,7 @@\n ## Review Findings (2026-05-02 12:30)\n\n ### Nits\n-- [ ] `kanban-app/ui/src/components/entity-card.in-zone-nav.spatial.test.tsx` (the `ArrowLeft from card.inspect:{id} lands on the title field zone` test, the block guarded by `if (dragNode)`) — Now that `DragHandle` no longer registers a `<FocusScope>`, no element carries `data-segment='card.drag-handle:task-1'`, so `dragNode` is always `null` and the entire `if (dragNode) { expect(...).toBeNull() }` branch is dead code. Either delete the dead conditional outright (the primary `titleNode` assertion still pins the desired ArrowLeft outcome), or replace it with a positive assertion that no element carries that `data-segment` attribute. While there, refresh the test's docstring (the `ArrowLeft from card.inspect:{id}` block) and the inline comment that begins \"Crucially, focus must NOT be on the drag-handle leaf\" — both still describe a \"drag-handle leaf\" that no longer exists, so the pre-fix counterfactual is no longer reproducible and the wording is stale.\n+- [x] `kanban-app/ui/src/components/entity-card.in-zone-nav.spatial.test.tsx` (the `ArrowLeft from card.inspect:{id} lands on the title field zone` test, the block guarded by `if (dragNode)`) — Now that `DragHandle` no longer registers a `<FocusScope>`, no element carries `data-segment='card.drag-handle:task-1'`, so `dragNode` is always `null` and the entire `if (dragNode) { expect(...).toBeNull() }` branch is dead code. Either delete the dead conditional outright (the primary `titleNode` assertion still pins the desired ArrowLeft outcome), or replace it with a positive assertion that no element carries that `data-segment` attribute. While there, refresh the test's docstring (the `ArrowLeft from card.inspect:{id}` block) and the inline comment that begins \"Crucially, focus must NOT be on the drag-handle leaf\" — both still describe a \"drag-handle leaf\" that no longer exists, so the pre-fix counterfactual is no longer reproducible and the wording is stale.\n+\n+### Resolution (2026-05-03)\n+Replaced the dead `if (dragNode) { ... }` conditional in `entity-card.in-zone-nav.spatial.test.tsx` with a positive `expect(dragNode).toBeNull()` assertion that pins the post-fix invariant: the drag handle is intentionally absent from the spatial-nav DOM. Refreshed the test docstring on `it(\"ArrowLeft from card.inspect:{id} lands on the title field zone\")` and the inline comment to describe the current state (drag handle is mouse-only, not a `<FocusScope>`) and to drop the now-unreproducible \"jumping over the title to the drag-handle leaf\" pre-fix narrative. Also refreshed the `TASK_SCHEMA` docstring (the `[drag-handle leaf, title field zone, inspect leaf]` list) to read `[title field zone, inspect leaf]` with a parenthetical noting why the drag handle isn't in the list. Verified with `pnpm vitest run src/components/entity-card` — 58/58 pass.\n","reverse_patch":"--- original\n+++ modified\n@@ -46,7 +46,4 @@\n ## Review Findings (2026-05-02 12:30)\n\n ### Nits\n-- [x] `kanban-app/ui/src/components/entity-card.in-zone-nav.spatial.test.tsx` (the `ArrowLeft from card.inspect:{id} lands on the title field zone` test, the block guarded by `if (dragNode)`) — Now that `DragHandle` no longer registers a `<FocusScope>`, no element carries `data-segment='card.drag-handle:task-1'`, so `dragNode` is always `null` and the entire `if (dragNode) { expect(...).toBeNull() }` branch is dead code. Either delete the dead conditional outright (the primary `titleNode` assertion still pins the desired ArrowLeft outcome), or replace it with a positive assertion that no element carries that `data-segment` attribute. While there, refresh the test's docstring (the `ArrowLeft from card.inspect:{id}` block) and the inline comment that begins \"Crucially, focus must NOT be on the drag-handle leaf\" — both still describe a \"drag-handle leaf\" that no longer exists, so the pre-fix counterfactual is no longer reproducible and the wording is stale.\n-\n-### Resolution (2026-05-03)\n-Replaced the dead `if (dragNode) { ... }` conditional in `entity-card.in-zone-nav.spatial.test.tsx` with a positive `expect(dragNode).toBeNull()` assertion that pins the post-fix invariant: the drag handle is intentionally absent from the spatial-nav DOM. Refreshed the test docstring on `it(\"ArrowLeft from card.inspect:{id} lands on the title field zone\")` and the inline comment to describe the current state (drag handle is mouse-only, not a `<FocusScope>`) and to drop the now-unreproducible \"jumping over the title to the drag-handle leaf\" pre-fix narrative. Also refreshed the `TASK_SCHEMA` docstring (the `[drag-handle leaf, title field zone, inspect leaf]` list) to read `[title field zone, inspect leaf]` with a parenthetical noting why the drag handle isn't in the list. Verified with `pnpm vitest run src/components/entity-card` — 58/58 pass.\n+- [ ] `kanban-app/ui/src/components/entity-card.in-zone-nav.spatial.test.tsx` (the `ArrowLeft from card.inspect:{id} lands on the title field zone` test, the block guarded by `if (dragNode)`) — Now that `DragHandle` no longer registers a `<FocusScope>`, no element carries `data-segment='card.drag-handle:task-1'`, so `dragNode` is always `null` and the entire `if (dragNode) { expect(...).toBeNull() }` branch is dead code. Either delete the dead conditional outright (the primary `titleNode` assertion still pins the desired ArrowLeft outcome), or replace it with a positive assertion that no element carries that `data-segment` attribute. While there, refresh the test's docstring (the `ArrowLeft from card.inspect:{id}` block) and the inline comment that begins \"Crucially, focus must NOT be on the drag-handle leaf\" — both still describe a \"drag-handle leaf\" that no longer exists, so the pre-fix counterfactual is no longer reproducible and the wording is stale.\n"}]]}
{"id":"01KQPVMFK9KSEMJGBT91YPA6VS","timestamp":"2026-05-03T12:04:59.113253Z","op":"update","item_id":"01KQM9478XFMCBBWHQN6ARE524","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: spatial-nav\n title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: spatial-nav\n title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n","transaction_id":null}
{"id":"01KQPVMFKAAWZBJEG4AK9J29EZ","timestamp":"2026-05-03T12:04:59.114095Z","entity_type":"task","entity_id":"01KQM9478XFMCBBWHQN6ARE524","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KQPVVQMPW4YA1KF7NJ3BRMV3","timestamp":"2026-05-03T12:08:56.726324Z","op":"update","item_id":"01KQM9478XFMCBBWHQN6ARE524","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: fffffffffffffffffffffffffffffffff580\n project: spatial-nav\n title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: fffffffffffffffffffffffffffffffff580\n+position_column: review\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Card drag handle: drop the FocusScope wrapper — handle is mouse-only, no keyboard target needed'\n ---\n","transaction_id":null}
{"id":"01KQPVVQMPY86GTM7GAP9RVNEY","timestamp":"2026-05-03T12:08:56.726809Z","entity_type":"task","entity_id":"01KQM9478XFMCBBWHQN6ARE524","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+fffffffffffffffffffffffffffffffff580\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-fffffffffffffffffffffffffffffffff580\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}

{"id":"01KNESK2TM6D34EFJGJVZ9FVJQ","timestamp":"2026-04-05T12:23:57.012384Z","entity_type":"task","entity_id":"01KNESK2TKEJ3GZWMF3M5PTK3N","op":"create","changes":[["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\nImplement `workspace_symbol_live` — live workspace symbol search via LSP. Supplements the existing indexed `search_symbol` op. Finds symbols the indexer hasn't reached yet or in files that changed since last index.\n\n### `workspace_symbol_live` — `workspace/symbol`\n- New file: `swissarmyhammer-code-context/src/ops/workspace_symbol_live.rs`\n- Takes `LspContext` + `WorkspaceSymbolLiveOptions { query, max_results }`\n- Sends `workspace/symbol` request with the query string\n- Parses `SymbolInformation[]` response\n- Converts to existing `SymbolLocation` type (reuse from `get_symbol.rs`) for consistency with indexed results\n- No special enrichment needed — this IS the live data\n\n### MCP registration\n- Add to `code_context` tool as `workspace symbol live` operation\n- Note: this is distinct from existing `search symbol` (which queries the index)\n\n### Files to create/modify\n- `swissarmyhammer-code-context/src/ops/workspace_symbol_live.rs` — new\n- `swissarmyhammer-code-context/src/ops/mod.rs` — re-export\n- `swissarmyhammer-code-context/src/lib.rs` — re-export\n- `swissarmyhammer-tools/src/mcp/tools/code_context/mod.rs` — registration + handler\n\n## Acceptance Criteria\n- [ ] Returns live symbol search results from the LSP server\n- [ ] Results use the same `SymbolLocation` type as indexed `search_symbol` for consistency\n- [ ] Respects `max_results` limit\n- [ ] Works even when the index is empty or stale\n- [ ] Appears as MCP operation under `code_context` tool\n\n## Tests\n- [ ] Unit test: `workspace/symbol` response parsing (SymbolInformation format)\n- [ ] Unit test: conversion to `SymbolLocation` type\n- [ ] Unit test: `max_results` truncation\n- [ ] Integration test: missing LSP client returns error\n- [ ] `cargo nextest run -p swissarmyhammer-code-context` passes\n\n## Workflow\n- Use `/tdd` — write failing tests first, then implement to make them pass.\n\n#lsp-live"}],["depends_on",{"kind":"set","value":["01KNESD0630BPJG3BK84KEJMJP"]}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"8980"}],["position_swimlane",{"kind":"set","value":"lsp-live"}],["title",{"kind":"set","value":"LSP-T3C: workspace_symbol_live op"}]],"transaction_id":"01KNESK0RZF3C644D48ARC1YRX"}
{"id":"01KNF0ATMN2Y0A8QAK9PWMFHWX","timestamp":"2026-04-05T14:21:46.517234Z","entity_type":"task","entity_id":"01KNESK2TKEJ3GZWMF3M5PTK3N","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,18 +1,17 @@\n ## What\n\n-Implement `workspace_symbol_live` — live workspace symbol search via LSP. Supplements the existing indexed `search_symbol` op. Finds symbols the indexer hasn't reached yet or in files that changed since last index.\n+Implement `workspace_symbol_live` — live workspace symbol search via LSP. Uses layered resolution. Supplements the existing indexed `search_symbol` op.\n\n-### `workspace_symbol_live` — `workspace/symbol`\n+### `workspace_symbol_live` — layered\n - New file: `swissarmyhammer-code-context/src/ops/workspace_symbol_live.rs`\n-- Takes `LspContext` + `WorkspaceSymbolLiveOptions { query, max_results }`\n-- Sends `workspace/symbol` request with the query string\n-- Parses `SymbolInformation[]` response\n-- Converts to existing `SymbolLocation` type (reuse from `get_symbol.rs`) for consistency with indexed results\n-- No special enrichment needed — this IS the live data\n+- Takes `LayeredContext` + `WorkspaceSymbolLiveOptions { query, max_results }`\n+- **Layer 1 (live LSP)**: `workspace/symbol` request. Most current — finds symbols the indexer hasn't reached yet.\n+- **Layer 2 (LSP index)**: Query `lsp_symbols` with fuzzy name match. This is essentially what existing `search_symbol` does.\n+- **Layer 3 (tree-sitter)**: Query `ts_chunks` for symbol names matching the query. Basic but always available.\n+- Returns results using the existing `SymbolLocation` type for consistency with indexed search.\n\n-### MCP registration\n-- Add to `code_context` tool as `workspace symbol live` operation\n-- Note: this is distinct from existing `search symbol` (which queries the index)\n+### Design note\n+This op and the existing `search_symbol` could potentially be merged — `search_symbol` could itself become layered, trying live LSP first and falling back to index. Consider whether this should replace `search_symbol` rather than being a separate op. If kept separate, the distinction is: `search_symbol` = index query (fast, offline), `workspace_symbol_live` = tries live first (most current).\n\n ### Files to create/modify\n - `swissarmyhammer-code-context/src/ops/workspace_symbol_live.rs` — new\n@@ -21,17 +20,18 @@\n - `swissarmyhammer-tools/src/mcp/tools/code_context/mod.rs` — registration + handler\n\n ## Acceptance Criteria\n-- [ ] Returns live symbol search results from the LSP server\n-- [ ] Results use the same `SymbolLocation` type as indexed `search_symbol` for consistency\n+- [ ] Returns live symbol results when LSP daemon running\n+- [ ] Falls back to LSP index symbol query when no live LSP\n+- [ ] Falls back to tree-sitter chunk symbols as last resort\n+- [ ] Results use `SymbolLocation` type for consistency with `search_symbol`\n - [ ] Respects `max_results` limit\n-- [ ] Works even when the index is empty or stale\n - [ ] Appears as MCP operation under `code_context` tool\n\n ## Tests\n - [ ] Unit test: `workspace/symbol` response parsing (SymbolInformation format)\n-- [ ] Unit test: conversion to `SymbolLocation` type\n+- [ ] Unit test: fallback to LSP index query\n+- [ ] Unit test: fallback to tree-sitter symbols\n - [ ] Unit test: `max_results` truncation\n-- [ ] Integration test: missing LSP client returns error\n - [ ] `cargo nextest run -p swissarmyhammer-code-context` passes\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -1,17 +1,18 @@\n ## What\n\n-Implement `workspace_symbol_live` — live workspace symbol search via LSP. Uses layered resolution. Supplements the existing indexed `search_symbol` op.\n+Implement `workspace_symbol_live` — live workspace symbol search via LSP. Supplements the existing indexed `search_symbol` op. Finds symbols the indexer hasn't reached yet or in files that changed since last index.\n\n-### `workspace_symbol_live` — layered\n+### `workspace_symbol_live` — `workspace/symbol`\n - New file: `swissarmyhammer-code-context/src/ops/workspace_symbol_live.rs`\n-- Takes `LayeredContext` + `WorkspaceSymbolLiveOptions { query, max_results }`\n-- **Layer 1 (live LSP)**: `workspace/symbol` request. Most current — finds symbols the indexer hasn't reached yet.\n-- **Layer 2 (LSP index)**: Query `lsp_symbols` with fuzzy name match. This is essentially what existing `search_symbol` does.\n-- **Layer 3 (tree-sitter)**: Query `ts_chunks` for symbol names matching the query. Basic but always available.\n-- Returns results using the existing `SymbolLocation` type for consistency with indexed search.\n+- Takes `LspContext` + `WorkspaceSymbolLiveOptions { query, max_results }`\n+- Sends `workspace/symbol` request with the query string\n+- Parses `SymbolInformation[]` response\n+- Converts to existing `SymbolLocation` type (reuse from `get_symbol.rs`) for consistency with indexed results\n+- No special enrichment needed — this IS the live data\n\n-### Design note\n-This op and the existing `search_symbol` could potentially be merged — `search_symbol` could itself become layered, trying live LSP first and falling back to index. Consider whether this should replace `search_symbol` rather than being a separate op. If kept separate, the distinction is: `search_symbol` = index query (fast, offline), `workspace_symbol_live` = tries live first (most current).\n+### MCP registration\n+- Add to `code_context` tool as `workspace symbol live` operation\n+- Note: this is distinct from existing `search symbol` (which queries the index)\n\n ### Files to create/modify\n - `swissarmyhammer-code-context/src/ops/workspace_symbol_live.rs` — new\n@@ -20,18 +21,17 @@\n - `swissarmyhammer-tools/src/mcp/tools/code_context/mod.rs` — registration + handler\n\n ## Acceptance Criteria\n-- [ ] Returns live symbol results when LSP daemon running\n-- [ ] Falls back to LSP index symbol query when no live LSP\n-- [ ] Falls back to tree-sitter chunk symbols as last resort\n-- [ ] Results use `SymbolLocation` type for consistency with `search_symbol`\n+- [ ] Returns live symbol search results from the LSP server\n+- [ ] Results use the same `SymbolLocation` type as indexed `search_symbol` for consistency\n - [ ] Respects `max_results` limit\n+- [ ] Works even when the index is empty or stale\n - [ ] Appears as MCP operation under `code_context` tool\n\n ## Tests\n - [ ] Unit test: `workspace/symbol` response parsing (SymbolInformation format)\n-- [ ] Unit test: fallback to LSP index query\n-- [ ] Unit test: fallback to tree-sitter symbols\n+- [ ] Unit test: conversion to `SymbolLocation` type\n - [ ] Unit test: `max_results` truncation\n+- [ ] Integration test: missing LSP client returns error\n - [ ] `cargo nextest run -p swissarmyhammer-code-context` passes\n\n ## Workflow\n"}]],"transaction_id":"01KNF0ATM933SVPV6CWY0CCPZT"}
{"id":"01KNF0GY54PC5A0GDWKQGMM2NR","timestamp":"2026-04-05T14:25:06.724544Z","entity_type":"task","entity_id":"01KNESK2TKEJ3GZWMF3M5PTK3N","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,17 +1,17 @@\n ## What\n\n-Implement `workspace_symbol_live` — live workspace symbol search via LSP. Uses layered resolution. Supplements the existing indexed `search_symbol` op.\n+Implement `workspace_symbol_live` — live workspace symbol search. Uses layered resolution via `LayeredContext`.\n\n ### `workspace_symbol_live` — layered\n - New file: `swissarmyhammer-code-context/src/ops/workspace_symbol_live.rs`\n-- Takes `LayeredContext` + `WorkspaceSymbolLiveOptions { query, max_results }`\n-- **Layer 1 (live LSP)**: `workspace/symbol` request. Most current — finds symbols the indexer hasn't reached yet.\n-- **Layer 2 (LSP index)**: Query `lsp_symbols` with fuzzy name match. This is essentially what existing `search_symbol` does.\n-- **Layer 3 (tree-sitter)**: Query `ts_chunks` for symbol names matching the query. Basic but always available.\n-- Returns results using the existing `SymbolLocation` type for consistency with indexed search.\n+- Takes `&LayeredContext` + `WorkspaceSymbolLiveOptions { query, max_results }`\n+- **Layer 1 (live LSP)**: `ctx.lsp_request(\"workspace/symbol\", ...)`. Most current — finds symbols the indexer hasn't reached yet.\n+- **Layer 2 (LSP index)**: `ctx.lsp_symbols_by_name(query, max_results)`. Same data as existing `search_symbol` op but accessed through `LayeredContext`.\n+- **Layer 3 (tree-sitter)**: `ctx.ts_chunks_matching(query, max_results)`. Basic text matching against chunk symbol names.\n+- Returns `SymbolLocation` type for consistency with indexed search.\n\n ### Design note\n-This op and the existing `search_symbol` could potentially be merged — `search_symbol` could itself become layered, trying live LSP first and falling back to index. Consider whether this should replace `search_symbol` rather than being a separate op. If kept separate, the distinction is: `search_symbol` = index query (fast, offline), `workspace_symbol_live` = tries live first (most current).\n+Consider whether this should eventually replace `search_symbol` rather than coexist. With `LayeredContext`, `search_symbol` is just \"layer 2 only\" — this op adds layers 1 and 3. For now, keep separate; merge later when existing ops migrate to `LayeredContext`.\n\n ### Files to create/modify\n - `swissarmyhammer-code-context/src/ops/workspace_symbol_live.rs` — new\n@@ -20,17 +20,16 @@\n - `swissarmyhammer-tools/src/mcp/tools/code_context/mod.rs` — registration + handler\n\n ## Acceptance Criteria\n-- [ ] Returns live symbol results when LSP daemon running\n-- [ ] Falls back to LSP index symbol query when no live LSP\n-- [ ] Falls back to tree-sitter chunk symbols as last resort\n-- [ ] Results use `SymbolLocation` type for consistency with `search_symbol`\n-- [ ] Respects `max_results` limit\n-- [ ] Appears as MCP operation under `code_context` tool\n+- [ ] Returns live results via `ctx.lsp_request()` when available\n+- [ ] Falls back to `ctx.lsp_symbols_by_name()` when no live LSP\n+- [ ] Falls back to `ctx.ts_chunks_matching()` as last resort\n+- [ ] Results use `SymbolLocation`/`SymbolInfo` type\n+- [ ] Respects `max_results`\n\n ## Tests\n - [ ] Unit test: `workspace/symbol` response parsing (SymbolInformation format)\n-- [ ] Unit test: fallback to LSP index query\n-- [ ] Unit test: fallback to tree-sitter symbols\n+- [ ] Unit test: fallback to `lsp_symbols_by_name()`\n+- [ ] Unit test: fallback to `ts_chunks_matching()`\n - [ ] Unit test: `max_results` truncation\n - [ ] `cargo nextest run -p swissarmyhammer-code-context` passes\n\n","reverse_patch":"--- original\n+++ modified\n@@ -1,17 +1,17 @@\n ## What\n\n-Implement `workspace_symbol_live` — live workspace symbol search. Uses layered resolution via `LayeredContext`.\n+Implement `workspace_symbol_live` — live workspace symbol search via LSP. Uses layered resolution. Supplements the existing indexed `search_symbol` op.\n\n ### `workspace_symbol_live` — layered\n - New file: `swissarmyhammer-code-context/src/ops/workspace_symbol_live.rs`\n-- Takes `&LayeredContext` + `WorkspaceSymbolLiveOptions { query, max_results }`\n-- **Layer 1 (live LSP)**: `ctx.lsp_request(\"workspace/symbol\", ...)`. Most current — finds symbols the indexer hasn't reached yet.\n-- **Layer 2 (LSP index)**: `ctx.lsp_symbols_by_name(query, max_results)`. Same data as existing `search_symbol` op but accessed through `LayeredContext`.\n-- **Layer 3 (tree-sitter)**: `ctx.ts_chunks_matching(query, max_results)`. Basic text matching against chunk symbol names.\n-- Returns `SymbolLocation` type for consistency with indexed search.\n+- Takes `LayeredContext` + `WorkspaceSymbolLiveOptions { query, max_results }`\n+- **Layer 1 (live LSP)**: `workspace/symbol` request. Most current — finds symbols the indexer hasn't reached yet.\n+- **Layer 2 (LSP index)**: Query `lsp_symbols` with fuzzy name match. This is essentially what existing `search_symbol` does.\n+- **Layer 3 (tree-sitter)**: Query `ts_chunks` for symbol names matching the query. Basic but always available.\n+- Returns results using the existing `SymbolLocation` type for consistency with indexed search.\n\n ### Design note\n-Consider whether this should eventually replace `search_symbol` rather than coexist. With `LayeredContext`, `search_symbol` is just \"layer 2 only\" — this op adds layers 1 and 3. For now, keep separate; merge later when existing ops migrate to `LayeredContext`.\n+This op and the existing `search_symbol` could potentially be merged — `search_symbol` could itself become layered, trying live LSP first and falling back to index. Consider whether this should replace `search_symbol` rather than being a separate op. If kept separate, the distinction is: `search_symbol` = index query (fast, offline), `workspace_symbol_live` = tries live first (most current).\n\n ### Files to create/modify\n - `swissarmyhammer-code-context/src/ops/workspace_symbol_live.rs` — new\n@@ -20,16 +20,17 @@\n - `swissarmyhammer-tools/src/mcp/tools/code_context/mod.rs` — registration + handler\n\n ## Acceptance Criteria\n-- [ ] Returns live results via `ctx.lsp_request()` when available\n-- [ ] Falls back to `ctx.lsp_symbols_by_name()` when no live LSP\n-- [ ] Falls back to `ctx.ts_chunks_matching()` as last resort\n-- [ ] Results use `SymbolLocation`/`SymbolInfo` type\n-- [ ] Respects `max_results`\n+- [ ] Returns live symbol results when LSP daemon running\n+- [ ] Falls back to LSP index symbol query when no live LSP\n+- [ ] Falls back to tree-sitter chunk symbols as last resort\n+- [ ] Results use `SymbolLocation` type for consistency with `search_symbol`\n+- [ ] Respects `max_results` limit\n+- [ ] Appears as MCP operation under `code_context` tool\n\n ## Tests\n - [ ] Unit test: `workspace/symbol` response parsing (SymbolInformation format)\n-- [ ] Unit test: fallback to `lsp_symbols_by_name()`\n-- [ ] Unit test: fallback to `ts_chunks_matching()`\n+- [ ] Unit test: fallback to LSP index query\n+- [ ] Unit test: fallback to tree-sitter symbols\n - [ ] Unit test: `max_results` truncation\n - [ ] `cargo nextest run -p swissarmyhammer-code-context` passes\n\n"}]],"transaction_id":"01KNF0GY4S738C6MRKNKRC6YQ6"}

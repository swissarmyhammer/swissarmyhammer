{"id":"01KQ4YF38P7CH04YX9AKK7800D","timestamp":"2026-04-26T13:08:08.598026Z","op":"create","item_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,55 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: ff9580\n+project: spatial-nav\n+title: 'Board columns: restore scrollability and row virtualization (regression)'\n+---\n+## What\n+\n+Regression: board columns are no longer scrollable, and row virtualization in `column-view.tsx` appears non-functional. Likely introduced during the spatial-nav refactor of `column-view.tsx` (FocusZone wrap, `useStableSpatialKeys`, `usePlaceholderRegistration`, neighbor-moniker prop removal, layout class changes).\n+\n+## Repro\n+\n+1. Open the kanban app on a board with a column containing many tasks (more than the virtualization threshold).\n+2. Observe: column does not scroll vertically — overflow content is clipped or pushes layout.\n+3. Observe: all task cards render, even off-screen ones (no placeholder rows / virtualizer windowing).\n+\n+## Likely cause\n+\n+When `column-view.tsx` was wrapped in `<FocusZone moniker={asMoniker(columnMoniker)}>`, the new wrapper `&lt;div&gt;` may have:\n+\n+- Dropped `overflow-y-auto` / `min-h-0` / `flex-1` classes that the inner virtualized list relies on for height + scroll context.\n+- Broken the flex chain so the inner scrollable area no longer has a constrained height (height collapses or grows unbounded).\n+- The `useVirtualizer` ref may now be on a parent that doesn't actually scroll, so visible-range computation always returns the full list.\n+\n+Cross-check `BoardSpatialZone` in `board-view.tsx`, `PerspectiveSpatialZone` in `perspective-container.tsx`, `ViewSpatialZone` in `view-container.tsx` — they each forward layout classes (`flex flex-col flex-1 min-h-0 min-w-0` etc). The column FocusZone may be missing analogous classes.\n+\n+## Files\n+\n+- `kanban-app/ui/src/components/column-view.tsx` (FocusZone wrap + className forwarding; `useVirtualizer` scroll element)\n+- `kanban-app/ui/src/components/board-view.tsx` (column container layout, if affected)\n+\n+## Acceptance Criteria\n+\n+- [ ] Each board column scrolls vertically when its task list exceeds visible height\n+- [ ] Row virtualization windowing is active: only ~visible cards mount as primitives; off-screen rows register via `spatial_register_batch` placeholders\n+- [ ] Layout classes preserved: column header stays at top, card list takes remaining height with overflow-y-auto\n+- [ ] No regression in card click / drag / drop / keyboard nav\n+- [ ] `pnpm vitest run` passes\n+- [ ] Manual: open a board with 50+ cards in one column, scroll, confirm visual scrollability and DOM-element count is bounded\n+\n+## Tests\n+\n+- [ ] `column-view.test.tsx` — assert the scroll container has `overflow-y-auto` (or equivalent) class\n+- [ ] `column-view.test.tsx` — virtualization threshold test: with N &gt; threshold cards, assert mounted card count &lt; N\n+- [ ] Browser test (if grid has one, mirror it for board column) — scroll the column, observe `data-virtual-row` rendering\n+\n+## Workflow\n+\n+- Use `/tdd` — write a failing test for scroll container className first, then for virtualization windowing.\n+\n+## Origin\n+\n+User-reported regression on 2026-04-26 during `/finish $spatial-nav` recovery work. Spun out so the spatial-nav scope can finish without blocking on this fix.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,55 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: ff9580\n-project: spatial-nav\n-title: 'Board columns: restore scrollability and row virtualization (regression)'\n----\n-## What\n-\n-Regression: board columns are no longer scrollable, and row virtualization in `column-view.tsx` appears non-functional. Likely introduced during the spatial-nav refactor of `column-view.tsx` (FocusZone wrap, `useStableSpatialKeys`, `usePlaceholderRegistration`, neighbor-moniker prop removal, layout class changes).\n-\n-## Repro\n-\n-1. Open the kanban app on a board with a column containing many tasks (more than the virtualization threshold).\n-2. Observe: column does not scroll vertically — overflow content is clipped or pushes layout.\n-3. Observe: all task cards render, even off-screen ones (no placeholder rows / virtualizer windowing).\n-\n-## Likely cause\n-\n-When `column-view.tsx` was wrapped in `<FocusZone moniker={asMoniker(columnMoniker)}>`, the new wrapper `&lt;div&gt;` may have:\n-\n-- Dropped `overflow-y-auto` / `min-h-0` / `flex-1` classes that the inner virtualized list relies on for height + scroll context.\n-- Broken the flex chain so the inner scrollable area no longer has a constrained height (height collapses or grows unbounded).\n-- The `useVirtualizer` ref may now be on a parent that doesn't actually scroll, so visible-range computation always returns the full list.\n-\n-Cross-check `BoardSpatialZone` in `board-view.tsx`, `PerspectiveSpatialZone` in `perspective-container.tsx`, `ViewSpatialZone` in `view-container.tsx` — they each forward layout classes (`flex flex-col flex-1 min-h-0 min-w-0` etc). The column FocusZone may be missing analogous classes.\n-\n-## Files\n-\n-- `kanban-app/ui/src/components/column-view.tsx` (FocusZone wrap + className forwarding; `useVirtualizer` scroll element)\n-- `kanban-app/ui/src/components/board-view.tsx` (column container layout, if affected)\n-\n-## Acceptance Criteria\n-\n-- [ ] Each board column scrolls vertically when its task list exceeds visible height\n-- [ ] Row virtualization windowing is active: only ~visible cards mount as primitives; off-screen rows register via `spatial_register_batch` placeholders\n-- [ ] Layout classes preserved: column header stays at top, card list takes remaining height with overflow-y-auto\n-- [ ] No regression in card click / drag / drop / keyboard nav\n-- [ ] `pnpm vitest run` passes\n-- [ ] Manual: open a board with 50+ cards in one column, scroll, confirm visual scrollability and DOM-element count is bounded\n-\n-## Tests\n-\n-- [ ] `column-view.test.tsx` — assert the scroll container has `overflow-y-auto` (or equivalent) class\n-- [ ] `column-view.test.tsx` — virtualization threshold test: with N &gt; threshold cards, assert mounted card count &lt; N\n-- [ ] Browser test (if grid has one, mirror it for board column) — scroll the column, observe `data-virtual-row` rendering\n-\n-## Workflow\n-\n-- Use `/tdd` — write a failing test for scroll container className first, then for virtualization windowing.\n-\n-## Origin\n-\n-User-reported regression on 2026-04-26 during `/finish $spatial-nav` recovery work. Spun out so the spatial-nav scope can finish without blocking on this fix.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ4YF38PYE0WZKJC42EFFVE1","timestamp":"2026-04-26T13:08:08.598508Z","entity_type":"task","entity_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","op":"create","changes":[["title",{"kind":"set","value":"Board columns: restore scrollability and row virtualization (regression)"}],["body",{"kind":"set","value":"## What\n\nRegression: board columns are no longer scrollable, and row virtualization in `column-view.tsx` appears non-functional. Likely introduced during the spatial-nav refactor of `column-view.tsx` (FocusZone wrap, `useStableSpatialKeys`, `usePlaceholderRegistration`, neighbor-moniker prop removal, layout class changes).\n\n## Repro\n\n1. Open the kanban app on a board with a column containing many tasks (more than the virtualization threshold).\n2. Observe: column does not scroll vertically — overflow content is clipped or pushes layout.\n3. Observe: all task cards render, even off-screen ones (no placeholder rows / virtualizer windowing).\n\n## Likely cause\n\nWhen `column-view.tsx` was wrapped in `<FocusZone moniker={asMoniker(columnMoniker)}>`, the new wrapper `&lt;div&gt;` may have:\n\n- Dropped `overflow-y-auto` / `min-h-0` / `flex-1` classes that the inner virtualized list relies on for height + scroll context.\n- Broken the flex chain so the inner scrollable area no longer has a constrained height (height collapses or grows unbounded).\n- The `useVirtualizer` ref may now be on a parent that doesn't actually scroll, so visible-range computation always returns the full list.\n\nCross-check `BoardSpatialZone` in `board-view.tsx`, `PerspectiveSpatialZone` in `perspective-container.tsx`, `ViewSpatialZone` in `view-container.tsx` — they each forward layout classes (`flex flex-col flex-1 min-h-0 min-w-0` etc). The column FocusZone may be missing analogous classes.\n\n## Files\n\n- `kanban-app/ui/src/components/column-view.tsx` (FocusZone wrap + className forwarding; `useVirtualizer` scroll element)\n- `kanban-app/ui/src/components/board-view.tsx` (column container layout, if affected)\n\n## Acceptance Criteria\n\n- [ ] Each board column scrolls vertically when its task list exceeds visible height\n- [ ] Row virtualization windowing is active: only ~visible cards mount as primitives; off-screen rows register via `spatial_register_batch` placeholders\n- [ ] Layout classes preserved: column header stays at top, card list takes remaining height with overflow-y-auto\n- [ ] No regression in card click / drag / drop / keyboard nav\n- [ ] `pnpm vitest run` passes\n- [ ] Manual: open a board with 50+ cards in one column, scroll, confirm visual scrollability and DOM-element count is bounded\n\n## Tests\n\n- [ ] `column-view.test.tsx` — assert the scroll container has `overflow-y-auto` (or equivalent) class\n- [ ] `column-view.test.tsx` — virtualization threshold test: with N &gt; threshold cards, assert mounted card count &lt; N\n- [ ] Browser test (if grid has one, mirror it for board column) — scroll the column, observe `data-virtual-row` rendering\n\n## Workflow\n\n- Use `/tdd` — write a failing test for scroll container className first, then for virtualization windowing.\n\n## Origin\n\nUser-reported regression on 2026-04-26 during `/finish $spatial-nav` recovery work. Spun out so the spatial-nav scope can finish without blocking on this fix."}],["position_ordinal",{"kind":"set","value":"ff9580"}],["assignees",{"kind":"set","value":["claude-code"]}],["position_column",{"kind":"set","value":"todo"}],["project",{"kind":"set","value":"spatial-nav"}]]}
{"id":"01KQ4YZRVNRWJBE88F5XA1TC17","timestamp":"2026-04-26T13:17:14.997264Z","op":"update","item_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,8 @@\n ---\n assignees:\n - claude-code\n+depends_on:\n+- 01KQ4YYFCGJCRN6GBYGVGXVVG6\n position_column: todo\n position_ordinal: ff9580\n project: spatial-nav\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,6 @@\n ---\n assignees:\n - claude-code\n-depends_on:\n-- 01KQ4YYFCGJCRN6GBYGVGXVVG6\n position_column: todo\n position_ordinal: ff9580\n project: spatial-nav\n","transaction_id":null}
{"id":"01KQ4YZRVYE5E638HP8Y91Y3WT","timestamp":"2026-04-26T13:17:15.006393Z","entity_type":"task","entity_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","op":"update","changes":[["depends_on",{"kind":"set","value":["01KQ4YYFCGJCRN6GBYGVGXVVG6"]}]]}
{"id":"01KQ58J6EM0WAP4HMA9H7KPDK9","timestamp":"2026-04-26T16:04:35.924799Z","op":"update","item_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ4YYFCGJCRN6GBYGVGXVVG6\n-position_column: todo\n-position_ordinal: ff9580\n+position_column: doing\n+position_ordinal: '8480'\n project: spatial-nav\n title: 'Board columns: restore scrollability and row virtualization (regression)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ4YYFCGJCRN6GBYGVGXVVG6\n-position_column: doing\n-position_ordinal: '8480'\n+position_column: todo\n+position_ordinal: ff9580\n project: spatial-nav\n title: 'Board columns: restore scrollability and row virtualization (regression)'\n ---\n","transaction_id":null}
{"id":"01KQ58J6EPGQYV6HH729GD10K9","timestamp":"2026-04-26T16:04:35.926188Z","entity_type":"task","entity_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ff9580\n\\ No newline at end of file\n+8480\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8480\n\\ No newline at end of file\n+ff9580\n\\ No newline at end of file\n"}]]}
{"id":"01KQ59PBV32SBKABBFCRV4QKDN","timestamp":"2026-04-26T16:24:21.091786Z","op":"update","item_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","forward_patch":"--- original\n+++ modified\n@@ -18,35 +18,40 @@\n 2. Observe: column does not scroll vertically — overflow content is clipped or pushes layout.\n 3. Observe: all task cards render, even off-screen ones (no placeholder rows / virtualizer windowing).\n\n-## Likely cause\n+## Root Cause (confirmed)\n+\n+`<FocusScope kind=\"zone\">` (post-spatial-nav refactor) renders TWO divs: the outer primitive (`<FocusZone>`) which receives `className`, and an inner `FocusScopeBody` chrome wrapper for right-click / double-click / scrollIntoView. That inner `FocusScopeBody` is a plain block `<div>` with no styling — it sits BETWEEN the FocusZone (with `flex flex-col flex-1 min-h-0`) and the column body. A block-display child breaks the flex chain: `flex-1` / `min-h-0` cannot propagate into ColumnBody, so VirtualizedCardList's scroll container loses its constrained height, the virtualizer windows against an unbounded container, and overflow no longer triggers a scrollbar.\n+\n+Pre-refactor `FocusScope` was a single `FocusHighlight` div, so the chain was preserved.\n\n-When `column-view.tsx` was wrapped in `<FocusZone moniker={asMoniker(columnMoniker)}>`, the new wrapper `&lt;div&gt;` may have:\n+## Fix\n\n-- Dropped `overflow-y-auto` / `min-h-0` / `flex-1` classes that the inner virtualized list relies on for height + scroll context.\n-- Broken the flex chain so the inner scrollable area no longer has a constrained height (height collapses or grows unbounded).\n-- The `useVirtualizer` ref may now be on a parent that doesn't actually scroll, so visible-range computation always returns the full list.\n+Establish the flex chain BELOW `FocusScopeBody` using absolute positioning (column-view.tsx only — no changes to focus-scope.tsx so the parallel inspector-layout fix is independent):\n\n-Cross-check `BoardSpatialZone` in `board-view.tsx`, `PerspectiveSpatialZone` in `perspective-container.tsx`, `ViewSpatialZone` in `view-container.tsx` — they each forward layout classes (`flex flex-col flex-1 min-h-0 min-w-0` etc). The column FocusZone may be missing analogous classes.\n+- Outer `<FocusScope>` is a `position: relative` flex item (`flex-1 min-h-0 ...`) sized by `SortableColumn`'s flex column parent.\n+- An inner `<div className=\"absolute inset-0 flex flex-col min-w-0\">` escapes `FocusScopeBody`'s static-block layout to re-establish the flex column chain. `inset-0` resolves against the nearest non-static ancestor (the FocusZone div), so the absolute wrapper fills the column's full allocated box regardless of the chrome wrapper between them.\n+- `ColumnBody` simplified to a `<>` fragment (no redundant inner div) so `ColumnHeader` and `VirtualizedCardList` are direct flex-col children of the absolute wrapper.\n\n ## Files\n\n-- `kanban-app/ui/src/components/column-view.tsx` (FocusZone wrap + className forwarding; `useVirtualizer` scroll element)\n-- `kanban-app/ui/src/components/board-view.tsx` (column container layout, if affected)\n+- `kanban-app/ui/src/components/column-view.tsx` (the fix)\n+- `kanban-app/ui/src/components/column-view.test.tsx` (regression tests)\n\n ## Acceptance Criteria\n\n-- [ ] Each board column scrolls vertically when its task list exceeds visible height\n-- [ ] Row virtualization windowing is active: only ~visible cards mount as primitives; off-screen rows register via `spatial_register_batch` placeholders\n-- [ ] Layout classes preserved: column header stays at top, card list takes remaining height with overflow-y-auto\n-- [ ] No regression in card click / drag / drop / keyboard nav\n-- [ ] `pnpm vitest run` passes\n-- [ ] Manual: open a board with 50+ cards in one column, scroll, confirm visual scrollability and DOM-element count is bounded\n+- [x] Each board column scrolls vertically when its task list exceeds visible height\n+- [x] Row virtualization windowing is active: only ~visible cards mount as primitives; off-screen rows register via `spatial_register_batch` placeholders\n+- [x] Layout classes preserved: column header stays at top, card list takes remaining height with overflow-y-auto\n+- [x] No regression in card click / drag / drop / keyboard nav (full suite green: 1567 tests)\n+- [x] `pnpm vitest run` passes (143 test files, 1567 tests, 0 failures)\n+- [ ] Manual: open a board with 50+ cards in one column, scroll, confirm visual scrollability and DOM-element count is bounded (deferred to user verification)\n\n ## Tests\n\n-- [ ] `column-view.test.tsx` — assert the scroll container has `overflow-y-auto` (or equivalent) class\n-- [ ] `column-view.test.tsx` — virtualization threshold test: with N &gt; threshold cards, assert mounted card count &lt; N\n-- [ ] Browser test (if grid has one, mirror it for board column) — scroll the column, observe `data-virtual-row` rendering\n+- [x] `column-view.test.tsx` — assert the scroll container has `overflow-y-auto` class (walks parent chain from drop-zone)\n+- [x] `column-view.test.tsx` — virtualization threshold test: with N > threshold cards, assert mounted card count < N (uses `stubScrollViewport` pattern from `data-table.virtualized.test.tsx` since the vitest browser project doesn't bundle Tailwind)\n+- [x] `column-view.test.tsx` — pin the absolute-positioned inner wrapper class structure (`absolute inset-0 flex flex-col`) so a future refactor cannot silently regress\n+- [x] `column-view.test.tsx` — pin that small lists (below threshold) render all cards directly\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -18,40 +18,35 @@\n 2. Observe: column does not scroll vertically — overflow content is clipped or pushes layout.\n 3. Observe: all task cards render, even off-screen ones (no placeholder rows / virtualizer windowing).\n\n-## Root Cause (confirmed)\n+## Likely cause\n\n-`<FocusScope kind=\"zone\">` (post-spatial-nav refactor) renders TWO divs: the outer primitive (`<FocusZone>`) which receives `className`, and an inner `FocusScopeBody` chrome wrapper for right-click / double-click / scrollIntoView. That inner `FocusScopeBody` is a plain block `<div>` with no styling — it sits BETWEEN the FocusZone (with `flex flex-col flex-1 min-h-0`) and the column body. A block-display child breaks the flex chain: `flex-1` / `min-h-0` cannot propagate into ColumnBody, so VirtualizedCardList's scroll container loses its constrained height, the virtualizer windows against an unbounded container, and overflow no longer triggers a scrollbar.\n+When `column-view.tsx` was wrapped in `<FocusZone moniker={asMoniker(columnMoniker)}>`, the new wrapper `&lt;div&gt;` may have:\n\n-Pre-refactor `FocusScope` was a single `FocusHighlight` div, so the chain was preserved.\n+- Dropped `overflow-y-auto` / `min-h-0` / `flex-1` classes that the inner virtualized list relies on for height + scroll context.\n+- Broken the flex chain so the inner scrollable area no longer has a constrained height (height collapses or grows unbounded).\n+- The `useVirtualizer` ref may now be on a parent that doesn't actually scroll, so visible-range computation always returns the full list.\n\n-## Fix\n+Cross-check `BoardSpatialZone` in `board-view.tsx`, `PerspectiveSpatialZone` in `perspective-container.tsx`, `ViewSpatialZone` in `view-container.tsx` — they each forward layout classes (`flex flex-col flex-1 min-h-0 min-w-0` etc). The column FocusZone may be missing analogous classes.\n\n-Establish the flex chain BELOW `FocusScopeBody` using absolute positioning (column-view.tsx only — no changes to focus-scope.tsx so the parallel inspector-layout fix is independent):\n-\n-- Outer `<FocusScope>` is a `position: relative` flex item (`flex-1 min-h-0 ...`) sized by `SortableColumn`'s flex column parent.\n-- An inner `<div className=\"absolute inset-0 flex flex-col min-w-0\">` escapes `FocusScopeBody`'s static-block layout to re-establish the flex column chain. `inset-0` resolves against the nearest non-static ancestor (the FocusZone div), so the absolute wrapper fills the column's full allocated box regardless of the chrome wrapper between them.\n-- `ColumnBody` simplified to a `<>` fragment (no redundant inner div) so `ColumnHeader` and `VirtualizedCardList` are direct flex-col children of the absolute wrapper.\n-\n ## Files\n\n-- `kanban-app/ui/src/components/column-view.tsx` (the fix)\n-- `kanban-app/ui/src/components/column-view.test.tsx` (regression tests)\n+- `kanban-app/ui/src/components/column-view.tsx` (FocusZone wrap + className forwarding; `useVirtualizer` scroll element)\n+- `kanban-app/ui/src/components/board-view.tsx` (column container layout, if affected)\n\n ## Acceptance Criteria\n\n-- [x] Each board column scrolls vertically when its task list exceeds visible height\n-- [x] Row virtualization windowing is active: only ~visible cards mount as primitives; off-screen rows register via `spatial_register_batch` placeholders\n-- [x] Layout classes preserved: column header stays at top, card list takes remaining height with overflow-y-auto\n-- [x] No regression in card click / drag / drop / keyboard nav (full suite green: 1567 tests)\n-- [x] `pnpm vitest run` passes (143 test files, 1567 tests, 0 failures)\n-- [ ] Manual: open a board with 50+ cards in one column, scroll, confirm visual scrollability and DOM-element count is bounded (deferred to user verification)\n+- [ ] Each board column scrolls vertically when its task list exceeds visible height\n+- [ ] Row virtualization windowing is active: only ~visible cards mount as primitives; off-screen rows register via `spatial_register_batch` placeholders\n+- [ ] Layout classes preserved: column header stays at top, card list takes remaining height with overflow-y-auto\n+- [ ] No regression in card click / drag / drop / keyboard nav\n+- [ ] `pnpm vitest run` passes\n+- [ ] Manual: open a board with 50+ cards in one column, scroll, confirm visual scrollability and DOM-element count is bounded\n\n ## Tests\n\n-- [x] `column-view.test.tsx` — assert the scroll container has `overflow-y-auto` class (walks parent chain from drop-zone)\n-- [x] `column-view.test.tsx` — virtualization threshold test: with N > threshold cards, assert mounted card count < N (uses `stubScrollViewport` pattern from `data-table.virtualized.test.tsx` since the vitest browser project doesn't bundle Tailwind)\n-- [x] `column-view.test.tsx` — pin the absolute-positioned inner wrapper class structure (`absolute inset-0 flex flex-col`) so a future refactor cannot silently regress\n-- [x] `column-view.test.tsx` — pin that small lists (below threshold) render all cards directly\n+- [ ] `column-view.test.tsx` — assert the scroll container has `overflow-y-auto` (or equivalent) class\n+- [ ] `column-view.test.tsx` — virtualization threshold test: with N &gt; threshold cards, assert mounted card count &lt; N\n+- [ ] Browser test (if grid has one, mirror it for board column) — scroll the column, observe `data-virtual-row` rendering\n\n ## Workflow\n\n","transaction_id":null}
{"id":"01KQ59PBV9638TR5J92FDZ5T82","timestamp":"2026-04-26T16:24:21.097320Z","entity_type":"task","entity_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -8,35 +8,40 @@\n 2. Observe: column does not scroll vertically — overflow content is clipped or pushes layout.\n 3. Observe: all task cards render, even off-screen ones (no placeholder rows / virtualizer windowing).\n\n-## Likely cause\n+## Root Cause (confirmed)\n+\n+`<FocusScope kind=\"zone\">` (post-spatial-nav refactor) renders TWO divs: the outer primitive (`<FocusZone>`) which receives `className`, and an inner `FocusScopeBody` chrome wrapper for right-click / double-click / scrollIntoView. That inner `FocusScopeBody` is a plain block `<div>` with no styling — it sits BETWEEN the FocusZone (with `flex flex-col flex-1 min-h-0`) and the column body. A block-display child breaks the flex chain: `flex-1` / `min-h-0` cannot propagate into ColumnBody, so VirtualizedCardList's scroll container loses its constrained height, the virtualizer windows against an unbounded container, and overflow no longer triggers a scrollbar.\n+\n+Pre-refactor `FocusScope` was a single `FocusHighlight` div, so the chain was preserved.\n\n-When `column-view.tsx` was wrapped in `<FocusZone moniker={asMoniker(columnMoniker)}>`, the new wrapper `&lt;div&gt;` may have:\n+## Fix\n\n-- Dropped `overflow-y-auto` / `min-h-0` / `flex-1` classes that the inner virtualized list relies on for height + scroll context.\n-- Broken the flex chain so the inner scrollable area no longer has a constrained height (height collapses or grows unbounded).\n-- The `useVirtualizer` ref may now be on a parent that doesn't actually scroll, so visible-range computation always returns the full list.\n+Establish the flex chain BELOW `FocusScopeBody` using absolute positioning (column-view.tsx only — no changes to focus-scope.tsx so the parallel inspector-layout fix is independent):\n\n-Cross-check `BoardSpatialZone` in `board-view.tsx`, `PerspectiveSpatialZone` in `perspective-container.tsx`, `ViewSpatialZone` in `view-container.tsx` — they each forward layout classes (`flex flex-col flex-1 min-h-0 min-w-0` etc). The column FocusZone may be missing analogous classes.\n+- Outer `<FocusScope>` is a `position: relative` flex item (`flex-1 min-h-0 ...`) sized by `SortableColumn`'s flex column parent.\n+- An inner `<div className=\"absolute inset-0 flex flex-col min-w-0\">` escapes `FocusScopeBody`'s static-block layout to re-establish the flex column chain. `inset-0` resolves against the nearest non-static ancestor (the FocusZone div), so the absolute wrapper fills the column's full allocated box regardless of the chrome wrapper between them.\n+- `ColumnBody` simplified to a `<>` fragment (no redundant inner div) so `ColumnHeader` and `VirtualizedCardList` are direct flex-col children of the absolute wrapper.\n\n ## Files\n\n-- `kanban-app/ui/src/components/column-view.tsx` (FocusZone wrap + className forwarding; `useVirtualizer` scroll element)\n-- `kanban-app/ui/src/components/board-view.tsx` (column container layout, if affected)\n+- `kanban-app/ui/src/components/column-view.tsx` (the fix)\n+- `kanban-app/ui/src/components/column-view.test.tsx` (regression tests)\n\n ## Acceptance Criteria\n\n-- [ ] Each board column scrolls vertically when its task list exceeds visible height\n-- [ ] Row virtualization windowing is active: only ~visible cards mount as primitives; off-screen rows register via `spatial_register_batch` placeholders\n-- [ ] Layout classes preserved: column header stays at top, card list takes remaining height with overflow-y-auto\n-- [ ] No regression in card click / drag / drop / keyboard nav\n-- [ ] `pnpm vitest run` passes\n-- [ ] Manual: open a board with 50+ cards in one column, scroll, confirm visual scrollability and DOM-element count is bounded\n+- [x] Each board column scrolls vertically when its task list exceeds visible height\n+- [x] Row virtualization windowing is active: only ~visible cards mount as primitives; off-screen rows register via `spatial_register_batch` placeholders\n+- [x] Layout classes preserved: column header stays at top, card list takes remaining height with overflow-y-auto\n+- [x] No regression in card click / drag / drop / keyboard nav (full suite green: 1567 tests)\n+- [x] `pnpm vitest run` passes (143 test files, 1567 tests, 0 failures)\n+- [ ] Manual: open a board with 50+ cards in one column, scroll, confirm visual scrollability and DOM-element count is bounded (deferred to user verification)\n\n ## Tests\n\n-- [ ] `column-view.test.tsx` — assert the scroll container has `overflow-y-auto` (or equivalent) class\n-- [ ] `column-view.test.tsx` — virtualization threshold test: with N &gt; threshold cards, assert mounted card count &lt; N\n-- [ ] Browser test (if grid has one, mirror it for board column) — scroll the column, observe `data-virtual-row` rendering\n+- [x] `column-view.test.tsx` — assert the scroll container has `overflow-y-auto` class (walks parent chain from drop-zone)\n+- [x] `column-view.test.tsx` — virtualization threshold test: with N > threshold cards, assert mounted card count < N (uses `stubScrollViewport` pattern from `data-table.virtualized.test.tsx` since the vitest browser project doesn't bundle Tailwind)\n+- [x] `column-view.test.tsx` — pin the absolute-positioned inner wrapper class structure (`absolute inset-0 flex flex-col`) so a future refactor cannot silently regress\n+- [x] `column-view.test.tsx` — pin that small lists (below threshold) render all cards directly\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -8,40 +8,35 @@\n 2. Observe: column does not scroll vertically — overflow content is clipped or pushes layout.\n 3. Observe: all task cards render, even off-screen ones (no placeholder rows / virtualizer windowing).\n\n-## Root Cause (confirmed)\n+## Likely cause\n\n-`<FocusScope kind=\"zone\">` (post-spatial-nav refactor) renders TWO divs: the outer primitive (`<FocusZone>`) which receives `className`, and an inner `FocusScopeBody` chrome wrapper for right-click / double-click / scrollIntoView. That inner `FocusScopeBody` is a plain block `<div>` with no styling — it sits BETWEEN the FocusZone (with `flex flex-col flex-1 min-h-0`) and the column body. A block-display child breaks the flex chain: `flex-1` / `min-h-0` cannot propagate into ColumnBody, so VirtualizedCardList's scroll container loses its constrained height, the virtualizer windows against an unbounded container, and overflow no longer triggers a scrollbar.\n+When `column-view.tsx` was wrapped in `<FocusZone moniker={asMoniker(columnMoniker)}>`, the new wrapper `&lt;div&gt;` may have:\n\n-Pre-refactor `FocusScope` was a single `FocusHighlight` div, so the chain was preserved.\n+- Dropped `overflow-y-auto` / `min-h-0` / `flex-1` classes that the inner virtualized list relies on for height + scroll context.\n+- Broken the flex chain so the inner scrollable area no longer has a constrained height (height collapses or grows unbounded).\n+- The `useVirtualizer` ref may now be on a parent that doesn't actually scroll, so visible-range computation always returns the full list.\n\n-## Fix\n+Cross-check `BoardSpatialZone` in `board-view.tsx`, `PerspectiveSpatialZone` in `perspective-container.tsx`, `ViewSpatialZone` in `view-container.tsx` — they each forward layout classes (`flex flex-col flex-1 min-h-0 min-w-0` etc). The column FocusZone may be missing analogous classes.\n\n-Establish the flex chain BELOW `FocusScopeBody` using absolute positioning (column-view.tsx only — no changes to focus-scope.tsx so the parallel inspector-layout fix is independent):\n-\n-- Outer `<FocusScope>` is a `position: relative` flex item (`flex-1 min-h-0 ...`) sized by `SortableColumn`'s flex column parent.\n-- An inner `<div className=\"absolute inset-0 flex flex-col min-w-0\">` escapes `FocusScopeBody`'s static-block layout to re-establish the flex column chain. `inset-0` resolves against the nearest non-static ancestor (the FocusZone div), so the absolute wrapper fills the column's full allocated box regardless of the chrome wrapper between them.\n-- `ColumnBody` simplified to a `<>` fragment (no redundant inner div) so `ColumnHeader` and `VirtualizedCardList` are direct flex-col children of the absolute wrapper.\n-\n ## Files\n\n-- `kanban-app/ui/src/components/column-view.tsx` (the fix)\n-- `kanban-app/ui/src/components/column-view.test.tsx` (regression tests)\n+- `kanban-app/ui/src/components/column-view.tsx` (FocusZone wrap + className forwarding; `useVirtualizer` scroll element)\n+- `kanban-app/ui/src/components/board-view.tsx` (column container layout, if affected)\n\n ## Acceptance Criteria\n\n-- [x] Each board column scrolls vertically when its task list exceeds visible height\n-- [x] Row virtualization windowing is active: only ~visible cards mount as primitives; off-screen rows register via `spatial_register_batch` placeholders\n-- [x] Layout classes preserved: column header stays at top, card list takes remaining height with overflow-y-auto\n-- [x] No regression in card click / drag / drop / keyboard nav (full suite green: 1567 tests)\n-- [x] `pnpm vitest run` passes (143 test files, 1567 tests, 0 failures)\n-- [ ] Manual: open a board with 50+ cards in one column, scroll, confirm visual scrollability and DOM-element count is bounded (deferred to user verification)\n+- [ ] Each board column scrolls vertically when its task list exceeds visible height\n+- [ ] Row virtualization windowing is active: only ~visible cards mount as primitives; off-screen rows register via `spatial_register_batch` placeholders\n+- [ ] Layout classes preserved: column header stays at top, card list takes remaining height with overflow-y-auto\n+- [ ] No regression in card click / drag / drop / keyboard nav\n+- [ ] `pnpm vitest run` passes\n+- [ ] Manual: open a board with 50+ cards in one column, scroll, confirm visual scrollability and DOM-element count is bounded\n\n ## Tests\n\n-- [x] `column-view.test.tsx` — assert the scroll container has `overflow-y-auto` class (walks parent chain from drop-zone)\n-- [x] `column-view.test.tsx` — virtualization threshold test: with N > threshold cards, assert mounted card count < N (uses `stubScrollViewport` pattern from `data-table.virtualized.test.tsx` since the vitest browser project doesn't bundle Tailwind)\n-- [x] `column-view.test.tsx` — pin the absolute-positioned inner wrapper class structure (`absolute inset-0 flex flex-col`) so a future refactor cannot silently regress\n-- [x] `column-view.test.tsx` — pin that small lists (below threshold) render all cards directly\n+- [ ] `column-view.test.tsx` — assert the scroll container has `overflow-y-auto` (or equivalent) class\n+- [ ] `column-view.test.tsx` — virtualization threshold test: with N &gt; threshold cards, assert mounted card count &lt; N\n+- [ ] Browser test (if grid has one, mirror it for board column) — scroll the column, observe `data-virtual-row` rendering\n\n ## Workflow\n\n"}]]}
{"id":"01KQ59PTG18Y6C74X8KFZV49QF","timestamp":"2026-04-26T16:24:36.097716Z","op":"update","item_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ4YYFCGJCRN6GBYGVGXVVG6\n-position_column: doing\n-position_ordinal: '8480'\n+position_column: review\n+position_ordinal: '8380'\n project: spatial-nav\n title: 'Board columns: restore scrollability and row virtualization (regression)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ4YYFCGJCRN6GBYGVGXVVG6\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '8480'\n project: spatial-nav\n title: 'Board columns: restore scrollability and row virtualization (regression)'\n ---\n","transaction_id":null}
{"id":"01KQ59PTG2P612175PT10FNDW8","timestamp":"2026-04-26T16:24:36.098192Z","entity_type":"task","entity_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8480\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+8480\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5A9VNBSTHP7R19R6N891V8","timestamp":"2026-04-26T16:34:59.883692Z","op":"update","item_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ4YYFCGJCRN6GBYGVGXVVG6\n-position_column: review\n-position_ordinal: '8380'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffbd80\n project: spatial-nav\n title: 'Board columns: restore scrollability and row virtualization (regression)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ4YYFCGJCRN6GBYGVGXVVG6\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffbd80\n+position_column: review\n+position_ordinal: '8380'\n project: spatial-nav\n title: 'Board columns: restore scrollability and row virtualization (regression)'\n ---\n","transaction_id":null}
{"id":"01KQ5A9VNDSEH7BCTK660043NN","timestamp":"2026-04-26T16:34:59.885412Z","entity_type":"task","entity_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+ffffffffffffffffffffffffbd80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffbd80\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5NTG3QF13EEN28MF0S0ZB5","timestamp":"2026-04-26T19:56:19.447343Z","op":"update","item_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KQ4YYFCGJCRN6GBYGVGXVVG6\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffbd80\n+position_ordinal: ffffffffffffffffffffffffffffffffb380\n project: spatial-nav\n title: 'Board columns: restore scrollability and row virtualization (regression)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KQ4YYFCGJCRN6GBYGVGXVVG6\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffb380\n+position_ordinal: ffffffffffffffffffffffffbd80\n project: spatial-nav\n title: 'Board columns: restore scrollability and row virtualization (regression)'\n ---\n","transaction_id":null}
{"id":"01KQ5NTG3QGCQ51KRWVNG3XVBQ","timestamp":"2026-04-26T19:56:19.447712Z","entity_type":"task","entity_id":"01KQ4YF38NJ0BN6FM5EVDVRH1N","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffbd80\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffb380\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffb380\n\\ No newline at end of file\n+ffffffffffffffffffffffffbd80\n\\ No newline at end of file\n"}]]}

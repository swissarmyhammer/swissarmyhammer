---
assignees:
- claude-code
position_column: done
position_ordinal: ffffffffd780
title: CrossWindowDropOverlay always renders even when no drag session active
---
**kanban-app/ui/src/components/board-view.tsx:458-462**\n\nThe `CrossWindowDropOverlay` is always mounted in the JSX tree. It returns `null` internally when no session is active, but it still calls `useDragSession()` and `useState` on every render of `BoardView`. This is a minor performance concern.\n\n**Suggestion:** This is fine as-is — React hooks are cheap. No action needed. Nit-level only.
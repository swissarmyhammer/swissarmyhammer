{"id":"01KQSDP4ZKQ2323BWHHVVJ10JR","timestamp":"2026-05-04T11:58:57.011229Z","op":"create","item_id":"01KQSDP4ZJY5ERAJ68TFPVFRRE","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,119 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KQQSXM2PEYR1WAQ7QXW3B8ME\n+- 01KQQTXDHP3XBHZ8G40AC4FG4D\n+- 01KQQTY2HZBX7M9TW95862JVQ3\n+- 01KQQTYKS63RPM62PPVJ7S7J60\n+- 01KQQTZ7PSXEQF1WWX14ST8WRT\n+position_column: todo\n+position_ordinal: cc80\n+project: spatial-nav\n+title: 'Spatial-nav follow-up: collapse FocusZone into FocusScope (single primitive with optional children)'\n+---\n+## Reference\n+\n+Follow-up to the spatial-nav redesign (design `01KQQSXM2PEYR1WAQ7QXW3B8ME`). The geometric cardinal algorithm and drill / first / last operations have landed; the type-level `FocusZone` / `FocusScope` split was kept during the redesign as a deliberate staging choice. This task collapses them into one primitive.\n+\n+**Decision:** keep `FocusScope` as the single primitive name. Delete `FocusZone` entirely. `FocusScope` already accepts children (its prop type has `children: ReactNode`). The \"is this a container or a leaf?\" question becomes \"does it have registered children?\" — answered by querying the registry, not by a type tag.\n+\n+This is **pure refactor** — no behaviour change. The geometric algorithm doesn't distinguish kind; `showFocusBar={false}` is still a per-instance prop; last-focused memory is still keyed by FQM; drill / first / last still work on the focused scope's children.\n+\n+## What\n+\n+### Rust kernel side (`swissarmyhammer-focus`)\n+\n+- `src/scope.rs`:\n+  - Delete the `FocusZone` struct.\n+  - `RegisteredScope` (the enum or wrapper that distinguishes zones from leaves today) collapses to `FocusScope` only. If it's currently `enum RegisteredScope { Scope(FocusScope), Zone(FocusZone) }`, replace with a struct alias or directly with `FocusScope`.\n+  - Drop `RegisteredScope::is_zone()` — replace callers with `registry.has_children(fq)` or equivalent.\n+  - Whatever per-zone fields don't already exist on `FocusScope` (e.g. `showFocusBar` flag) move onto `FocusScope`.\n+\n+- `src/registry.rs`:\n+  - Collapse `register_scope` and `register_zone` IPC commands into a single `register_scope` (or keep both as transition aliases — recommend drop entirely; the React side is updated in this same PR).\n+  - Internal storage: one collection, not two. `zones_iter` / `leaves_iter` either go away or get re-implemented as filters over `entries_in_layer` (`with_children` / `without_children`).\n+  - Children query: add `fn children_of(&self, parent_fq: &FullyQualifiedMoniker) -> impl Iterator<Item = &FocusScope>` or similar — used by drill-in / first / last.\n+\n+- `src/navigate.rs`:\n+  - The leaf-tie-break in `geometric_pick` currently uses `is_zone()` to prefer leaves. Rewrite as \"prefer scopes with no registered children\" using the new `children_of` query.\n+  - Drill-in / first / last already operate on children — confirm they don't depend on `is_zone()` after the rename.\n+\n+- `src/types.rs`:\n+  - Sweep any `Direction` / `Pixels` etc. for stale references.\n+\n+- `tests/*`:\n+  - Every `is_zone()` / `RegisteredScope::Zone(...)` callsite needs updating. Fixture builders (`tests/fixtures/mod.rs`) become \"register a scope; if it has children later, it acts like a zone.\"\n+  - Tests asserting `spatial_register_zone` IPC name become `spatial_register_scope`.\n+\n+### TypeScript / React side (`kanban-app/ui`)\n+\n+- `src/components/focus-zone.tsx`:\n+  - Delete the file. Move any unique logic into `focus-scope.tsx`.\n+  - All exports (`FocusZone`, `FocusZoneProps`, `FocusZoneContext`, `useParentZoneFq`, etc.) — the type re-exports become `FocusScope*`. `FocusZoneContext` becomes `FocusScopeContext` (or stays as the existing `FocusScopeContext` if there's already overlap — verify).\n+\n+- `src/components/focus-scope.tsx`:\n+  - Confirm it accepts children (it does — see existing `FocusScopeProps.children`).\n+  - Add any zone-only props that need to migrate (e.g. zone-specific layout class handling).\n+\n+- `src/components/use-track-rect-on-ancestor-scroll.ts` and `src/lib/spatial-focus-context.tsx`:\n+  - Update `spatial_register_zone` IPC adapter to alias / point at `spatial_register_scope`.\n+\n+- **Sweep all callers** — replace `<FocusZone …>` with `<FocusScope …>`, replace `import { FocusZone, FocusZoneProps } from \"@/components/focus-zone\"` with the equivalent import from `focus-scope`. Known callers (run a grep to confirm complete list):\n+  - `nav-bar.tsx` (the outer `<FocusZone moniker=\"ui:navbar\">` and its `<FocusZone moniker=\"ui:navbar.board-selector\">`)\n+  - `board-view.tsx` (`BoardSpatialZone` → `<FocusZone moniker=\"ui:board\">`)\n+  - `view-container.tsx` (`ViewSpatialZone` — note: a separate task `01KQPW61ZNS010FV8PV844KY0K` may delete this entirely; coordinate)\n+  - `perspective-container.tsx` (`PerspectiveSpatialZone` → `<FocusZone moniker=\"ui:perspective\">`)\n+  - `perspective-tab-bar.tsx` (`PerspectiveBarSpatialZone`)\n+  - `left-nav.tsx` (`<FocusZone moniker=\"ui:left-nav\">`)\n+  - `column-view.tsx` (column body zones)\n+  - `grid-view.tsx` (`GridSpatialZone`)\n+  - `entity-card.tsx` (card zones)\n+  - `data-table.tsx`\n+  - `fields/field.tsx` (`Field` mounts a `<FocusZone>`)\n+  - `board-selector.tsx`\n+  - `inspectable.tsx`\n+  - `inspectors-container.tsx`\n+  - Any others — verified by `grep -rn \"FocusZone\" kanban-app/ui/src --include='*.ts*'`\n+\n+- **Sweep all tests** that assert on `<FocusZone>`, `FocusZoneContext`, `useParentZoneFq`, or `spatial_register_zone` IPC. Many test files (board-view.guards.node.test.ts, perspective-spatial-nav.guards.node.test.ts, etc.) will need updates.\n+\n+### IPC contract\n+\n+- After this PR, the IPC surface from React to Rust has only `spatial_register_scope` (no more `spatial_register_zone`). The kernel decides \"leaf vs container\" by inspecting children.\n+- Update `kanban-app/src/commands.rs` (the Tauri command bridge) accordingly.\n+\n+### Documentation\n+\n+- `swissarmyhammer-focus/README.md`: rewrite the prose that distinguishes zones from leaves. New framing: \"All registered things are `FocusScope`s. A scope with no children behaves as a leaf; a scope with children behaves as a container.\"\n+\n+## Acceptance Criteria\n+\n+- [ ] `grep -r \"FocusZone\" swissarmyhammer-focus/src kanban-app/ui/src` returns no source-code matches (test fixtures and historical task descriptions excluded).\n+- [ ] `grep -r \"spatial_register_zone\" swissarmyhammer-focus/src kanban-app/ui/src kanban-app/src` returns no source-code matches.\n+- [ ] `is_zone()` / `RegisteredScope::Zone` enum variants are gone from the kernel.\n+- [ ] `cargo test -p swissarmyhammer-focus` passes.\n+- [ ] `pnpm -C kanban-app/ui test` passes (full suite — this refactor touches many files).\n+- [ ] `pnpm -C kanban-app/ui typecheck` passes.\n+- [ ] `cargo build --workspace` passes.\n+- [ ] No behaviour change observable in the running app: the four cross-zone bug fixes from the redesign still work; in-card and in-row navigation works; drill / first / last work; focus indicator still paints; `showFocusBar={false}` containers still suppress the visible bar.\n+- [ ] README rewritten to reflect single-primitive model.\n+\n+## Tests\n+\n+- [ ] No new tests required (pure refactor) — but the existing test suite must pass after the sweep. Any test that explicitly asserted \"this is a zone, not a leaf\" needs to either become \"this has children\" or be deleted.\n+- [ ] Re-run the four cross-zone regression assertions from `swissarmyhammer-focus/tests/cross_zone_geometric_nav.rs` and confirm green.\n+- [ ] Re-run the drill / first / last assertions and confirm green.\n+- [ ] Run `cargo test -p swissarmyhammer-focus && pnpm -C kanban-app/ui test && pnpm -C kanban-app/ui typecheck` and confirm everything green.\n+\n+## Workflow\n+\n+- This task is large in line count (30+ files) but **not** in conceptual surface — it's a mechanical rename + delete + IPC consolidation. One PR is fine because the pieces (kernel struct collapse, React component collapse, IPC unification) are interlocking — splitting them creates incompatible intermediate states.\n+- Sequence within the PR:\n+  1. Kernel side first: collapse `FocusZone` into `FocusScope` in Rust, update IPC command names, run `cargo test -p swissarmyhammer-focus`.\n+  2. Update IPC adapter in React (`spatial-focus-context.tsx`, `commands.rs`).\n+  3. Sweep React callers: rename `<FocusZone>` → `<FocusScope>` and update imports. Use mechanical find/replace; verify each file builds.\n+  4. Sweep tests on both sides.\n+  5. Run full test suite. Fix any genuine fallout (likely tests that asserted on `is_zone()` or `data-moniker=\"ui:...\"` patterns that no longer differentiate).\n+  6. Rewrite README.\n+- No `/tdd` for this one — it's a refactor with no new behaviour to test. The existing test suite is the safety net.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,119 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KQQSXM2PEYR1WAQ7QXW3B8ME\n-- 01KQQTXDHP3XBHZ8G40AC4FG4D\n-- 01KQQTY2HZBX7M9TW95862JVQ3\n-- 01KQQTYKS63RPM62PPVJ7S7J60\n-- 01KQQTZ7PSXEQF1WWX14ST8WRT\n-position_column: todo\n-position_ordinal: cc80\n-project: spatial-nav\n-title: 'Spatial-nav follow-up: collapse FocusZone into FocusScope (single primitive with optional children)'\n----\n-## Reference\n-\n-Follow-up to the spatial-nav redesign (design `01KQQSXM2PEYR1WAQ7QXW3B8ME`). The geometric cardinal algorithm and drill / first / last operations have landed; the type-level `FocusZone` / `FocusScope` split was kept during the redesign as a deliberate staging choice. This task collapses them into one primitive.\n-\n-**Decision:** keep `FocusScope` as the single primitive name. Delete `FocusZone` entirely. `FocusScope` already accepts children (its prop type has `children: ReactNode`). The \"is this a container or a leaf?\" question becomes \"does it have registered children?\" — answered by querying the registry, not by a type tag.\n-\n-This is **pure refactor** — no behaviour change. The geometric algorithm doesn't distinguish kind; `showFocusBar={false}` is still a per-instance prop; last-focused memory is still keyed by FQM; drill / first / last still work on the focused scope's children.\n-\n-## What\n-\n-### Rust kernel side (`swissarmyhammer-focus`)\n-\n-- `src/scope.rs`:\n-  - Delete the `FocusZone` struct.\n-  - `RegisteredScope` (the enum or wrapper that distinguishes zones from leaves today) collapses to `FocusScope` only. If it's currently `enum RegisteredScope { Scope(FocusScope), Zone(FocusZone) }`, replace with a struct alias or directly with `FocusScope`.\n-  - Drop `RegisteredScope::is_zone()` — replace callers with `registry.has_children(fq)` or equivalent.\n-  - Whatever per-zone fields don't already exist on `FocusScope` (e.g. `showFocusBar` flag) move onto `FocusScope`.\n-\n-- `src/registry.rs`:\n-  - Collapse `register_scope` and `register_zone` IPC commands into a single `register_scope` (or keep both as transition aliases — recommend drop entirely; the React side is updated in this same PR).\n-  - Internal storage: one collection, not two. `zones_iter` / `leaves_iter` either go away or get re-implemented as filters over `entries_in_layer` (`with_children` / `without_children`).\n-  - Children query: add `fn children_of(&self, parent_fq: &FullyQualifiedMoniker) -> impl Iterator<Item = &FocusScope>` or similar — used by drill-in / first / last.\n-\n-- `src/navigate.rs`:\n-  - The leaf-tie-break in `geometric_pick` currently uses `is_zone()` to prefer leaves. Rewrite as \"prefer scopes with no registered children\" using the new `children_of` query.\n-  - Drill-in / first / last already operate on children — confirm they don't depend on `is_zone()` after the rename.\n-\n-- `src/types.rs`:\n-  - Sweep any `Direction` / `Pixels` etc. for stale references.\n-\n-- `tests/*`:\n-  - Every `is_zone()` / `RegisteredScope::Zone(...)` callsite needs updating. Fixture builders (`tests/fixtures/mod.rs`) become \"register a scope; if it has children later, it acts like a zone.\"\n-  - Tests asserting `spatial_register_zone` IPC name become `spatial_register_scope`.\n-\n-### TypeScript / React side (`kanban-app/ui`)\n-\n-- `src/components/focus-zone.tsx`:\n-  - Delete the file. Move any unique logic into `focus-scope.tsx`.\n-  - All exports (`FocusZone`, `FocusZoneProps`, `FocusZoneContext`, `useParentZoneFq`, etc.) — the type re-exports become `FocusScope*`. `FocusZoneContext` becomes `FocusScopeContext` (or stays as the existing `FocusScopeContext` if there's already overlap — verify).\n-\n-- `src/components/focus-scope.tsx`:\n-  - Confirm it accepts children (it does — see existing `FocusScopeProps.children`).\n-  - Add any zone-only props that need to migrate (e.g. zone-specific layout class handling).\n-\n-- `src/components/use-track-rect-on-ancestor-scroll.ts` and `src/lib/spatial-focus-context.tsx`:\n-  - Update `spatial_register_zone` IPC adapter to alias / point at `spatial_register_scope`.\n-\n-- **Sweep all callers** — replace `<FocusZone …>` with `<FocusScope …>`, replace `import { FocusZone, FocusZoneProps } from \"@/components/focus-zone\"` with the equivalent import from `focus-scope`. Known callers (run a grep to confirm complete list):\n-  - `nav-bar.tsx` (the outer `<FocusZone moniker=\"ui:navbar\">` and its `<FocusZone moniker=\"ui:navbar.board-selector\">`)\n-  - `board-view.tsx` (`BoardSpatialZone` → `<FocusZone moniker=\"ui:board\">`)\n-  - `view-container.tsx` (`ViewSpatialZone` — note: a separate task `01KQPW61ZNS010FV8PV844KY0K` may delete this entirely; coordinate)\n-  - `perspective-container.tsx` (`PerspectiveSpatialZone` → `<FocusZone moniker=\"ui:perspective\">`)\n-  - `perspective-tab-bar.tsx` (`PerspectiveBarSpatialZone`)\n-  - `left-nav.tsx` (`<FocusZone moniker=\"ui:left-nav\">`)\n-  - `column-view.tsx` (column body zones)\n-  - `grid-view.tsx` (`GridSpatialZone`)\n-  - `entity-card.tsx` (card zones)\n-  - `data-table.tsx`\n-  - `fields/field.tsx` (`Field` mounts a `<FocusZone>`)\n-  - `board-selector.tsx`\n-  - `inspectable.tsx`\n-  - `inspectors-container.tsx`\n-  - Any others — verified by `grep -rn \"FocusZone\" kanban-app/ui/src --include='*.ts*'`\n-\n-- **Sweep all tests** that assert on `<FocusZone>`, `FocusZoneContext`, `useParentZoneFq`, or `spatial_register_zone` IPC. Many test files (board-view.guards.node.test.ts, perspective-spatial-nav.guards.node.test.ts, etc.) will need updates.\n-\n-### IPC contract\n-\n-- After this PR, the IPC surface from React to Rust has only `spatial_register_scope` (no more `spatial_register_zone`). The kernel decides \"leaf vs container\" by inspecting children.\n-- Update `kanban-app/src/commands.rs` (the Tauri command bridge) accordingly.\n-\n-### Documentation\n-\n-- `swissarmyhammer-focus/README.md`: rewrite the prose that distinguishes zones from leaves. New framing: \"All registered things are `FocusScope`s. A scope with no children behaves as a leaf; a scope with children behaves as a container.\"\n-\n-## Acceptance Criteria\n-\n-- [ ] `grep -r \"FocusZone\" swissarmyhammer-focus/src kanban-app/ui/src` returns no source-code matches (test fixtures and historical task descriptions excluded).\n-- [ ] `grep -r \"spatial_register_zone\" swissarmyhammer-focus/src kanban-app/ui/src kanban-app/src` returns no source-code matches.\n-- [ ] `is_zone()` / `RegisteredScope::Zone` enum variants are gone from the kernel.\n-- [ ] `cargo test -p swissarmyhammer-focus` passes.\n-- [ ] `pnpm -C kanban-app/ui test` passes (full suite — this refactor touches many files).\n-- [ ] `pnpm -C kanban-app/ui typecheck` passes.\n-- [ ] `cargo build --workspace` passes.\n-- [ ] No behaviour change observable in the running app: the four cross-zone bug fixes from the redesign still work; in-card and in-row navigation works; drill / first / last work; focus indicator still paints; `showFocusBar={false}` containers still suppress the visible bar.\n-- [ ] README rewritten to reflect single-primitive model.\n-\n-## Tests\n-\n-- [ ] No new tests required (pure refactor) — but the existing test suite must pass after the sweep. Any test that explicitly asserted \"this is a zone, not a leaf\" needs to either become \"this has children\" or be deleted.\n-- [ ] Re-run the four cross-zone regression assertions from `swissarmyhammer-focus/tests/cross_zone_geometric_nav.rs` and confirm green.\n-- [ ] Re-run the drill / first / last assertions and confirm green.\n-- [ ] Run `cargo test -p swissarmyhammer-focus && pnpm -C kanban-app/ui test && pnpm -C kanban-app/ui typecheck` and confirm everything green.\n-\n-## Workflow\n-\n-- This task is large in line count (30+ files) but **not** in conceptual surface — it's a mechanical rename + delete + IPC consolidation. One PR is fine because the pieces (kernel struct collapse, React component collapse, IPC unification) are interlocking — splitting them creates incompatible intermediate states.\n-- Sequence within the PR:\n-  1. Kernel side first: collapse `FocusZone` into `FocusScope` in Rust, update IPC command names, run `cargo test -p swissarmyhammer-focus`.\n-  2. Update IPC adapter in React (`spatial-focus-context.tsx`, `commands.rs`).\n-  3. Sweep React callers: rename `<FocusZone>` → `<FocusScope>` and update imports. Use mechanical find/replace; verify each file builds.\n-  4. Sweep tests on both sides.\n-  5. Run full test suite. Fix any genuine fallout (likely tests that asserted on `is_zone()` or `data-moniker=\"ui:...\"` patterns that no longer differentiate).\n-  6. Rewrite README.\n-- No `/tdd` for this one — it's a refactor with no new behaviour to test. The existing test suite is the safety net.\n","transaction_id":null}
{"id":"01KQSDP4ZK7N4TNKK72GKKA6RC","timestamp":"2026-05-04T11:58:57.011761Z","entity_type":"task","entity_id":"01KQSDP4ZJY5ERAJ68TFPVFRRE","op":"create","changes":[["position_column",{"kind":"set","value":"todo"}],["depends_on",{"kind":"set","value":["01KQQSXM2PEYR1WAQ7QXW3B8ME","01KQQTXDHP3XBHZ8G40AC4FG4D","01KQQTY2HZBX7M9TW95862JVQ3","01KQQTYKS63RPM62PPVJ7S7J60","01KQQTZ7PSXEQF1WWX14ST8WRT"]}],["position_ordinal",{"kind":"set","value":"cc80"}],["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## Reference\n\nFollow-up to the spatial-nav redesign (design `01KQQSXM2PEYR1WAQ7QXW3B8ME`). The geometric cardinal algorithm and drill / first / last operations have landed; the type-level `FocusZone` / `FocusScope` split was kept during the redesign as a deliberate staging choice. This task collapses them into one primitive.\n\n**Decision:** keep `FocusScope` as the single primitive name. Delete `FocusZone` entirely. `FocusScope` already accepts children (its prop type has `children: ReactNode`). The \"is this a container or a leaf?\" question becomes \"does it have registered children?\" — answered by querying the registry, not by a type tag.\n\nThis is **pure refactor** — no behaviour change. The geometric algorithm doesn't distinguish kind; `showFocusBar={false}` is still a per-instance prop; last-focused memory is still keyed by FQM; drill / first / last still work on the focused scope's children.\n\n## What\n\n### Rust kernel side (`swissarmyhammer-focus`)\n\n- `src/scope.rs`:\n  - Delete the `FocusZone` struct.\n  - `RegisteredScope` (the enum or wrapper that distinguishes zones from leaves today) collapses to `FocusScope` only. If it's currently `enum RegisteredScope { Scope(FocusScope), Zone(FocusZone) }`, replace with a struct alias or directly with `FocusScope`.\n  - Drop `RegisteredScope::is_zone()` — replace callers with `registry.has_children(fq)` or equivalent.\n  - Whatever per-zone fields don't already exist on `FocusScope` (e.g. `showFocusBar` flag) move onto `FocusScope`.\n\n- `src/registry.rs`:\n  - Collapse `register_scope` and `register_zone` IPC commands into a single `register_scope` (or keep both as transition aliases — recommend drop entirely; the React side is updated in this same PR).\n  - Internal storage: one collection, not two. `zones_iter` / `leaves_iter` either go away or get re-implemented as filters over `entries_in_layer` (`with_children` / `without_children`).\n  - Children query: add `fn children_of(&self, parent_fq: &FullyQualifiedMoniker) -> impl Iterator<Item = &FocusScope>` or similar — used by drill-in / first / last.\n\n- `src/navigate.rs`:\n  - The leaf-tie-break in `geometric_pick` currently uses `is_zone()` to prefer leaves. Rewrite as \"prefer scopes with no registered children\" using the new `children_of` query.\n  - Drill-in / first / last already operate on children — confirm they don't depend on `is_zone()` after the rename.\n\n- `src/types.rs`:\n  - Sweep any `Direction` / `Pixels` etc. for stale references.\n\n- `tests/*`:\n  - Every `is_zone()` / `RegisteredScope::Zone(...)` callsite needs updating. Fixture builders (`tests/fixtures/mod.rs`) become \"register a scope; if it has children later, it acts like a zone.\"\n  - Tests asserting `spatial_register_zone` IPC name become `spatial_register_scope`.\n\n### TypeScript / React side (`kanban-app/ui`)\n\n- `src/components/focus-zone.tsx`:\n  - Delete the file. Move any unique logic into `focus-scope.tsx`.\n  - All exports (`FocusZone`, `FocusZoneProps`, `FocusZoneContext`, `useParentZoneFq`, etc.) — the type re-exports become `FocusScope*`. `FocusZoneContext` becomes `FocusScopeContext` (or stays as the existing `FocusScopeContext` if there's already overlap — verify).\n\n- `src/components/focus-scope.tsx`:\n  - Confirm it accepts children (it does — see existing `FocusScopeProps.children`).\n  - Add any zone-only props that need to migrate (e.g. zone-specific layout class handling).\n\n- `src/components/use-track-rect-on-ancestor-scroll.ts` and `src/lib/spatial-focus-context.tsx`:\n  - Update `spatial_register_zone` IPC adapter to alias / point at `spatial_register_scope`.\n\n- **Sweep all callers** — replace `<FocusZone …>` with `<FocusScope …>`, replace `import { FocusZone, FocusZoneProps } from \"@/components/focus-zone\"` with the equivalent import from `focus-scope`. Known callers (run a grep to confirm complete list):\n  - `nav-bar.tsx` (the outer `<FocusZone moniker=\"ui:navbar\">` and its `<FocusZone moniker=\"ui:navbar.board-selector\">`)\n  - `board-view.tsx` (`BoardSpatialZone` → `<FocusZone moniker=\"ui:board\">`)\n  - `view-container.tsx` (`ViewSpatialZone` — note: a separate task `01KQPW61ZNS010FV8PV844KY0K` may delete this entirely; coordinate)\n  - `perspective-container.tsx` (`PerspectiveSpatialZone` → `<FocusZone moniker=\"ui:perspective\">`)\n  - `perspective-tab-bar.tsx` (`PerspectiveBarSpatialZone`)\n  - `left-nav.tsx` (`<FocusZone moniker=\"ui:left-nav\">`)\n  - `column-view.tsx` (column body zones)\n  - `grid-view.tsx` (`GridSpatialZone`)\n  - `entity-card.tsx` (card zones)\n  - `data-table.tsx`\n  - `fields/field.tsx` (`Field` mounts a `<FocusZone>`)\n  - `board-selector.tsx`\n  - `inspectable.tsx`\n  - `inspectors-container.tsx`\n  - Any others — verified by `grep -rn \"FocusZone\" kanban-app/ui/src --include='*.ts*'`\n\n- **Sweep all tests** that assert on `<FocusZone>`, `FocusZoneContext`, `useParentZoneFq`, or `spatial_register_zone` IPC. Many test files (board-view.guards.node.test.ts, perspective-spatial-nav.guards.node.test.ts, etc.) will need updates.\n\n### IPC contract\n\n- After this PR, the IPC surface from React to Rust has only `spatial_register_scope` (no more `spatial_register_zone`). The kernel decides \"leaf vs container\" by inspecting children.\n- Update `kanban-app/src/commands.rs` (the Tauri command bridge) accordingly.\n\n### Documentation\n\n- `swissarmyhammer-focus/README.md`: rewrite the prose that distinguishes zones from leaves. New framing: \"All registered things are `FocusScope`s. A scope with no children behaves as a leaf; a scope with children behaves as a container.\"\n\n## Acceptance Criteria\n\n- [ ] `grep -r \"FocusZone\" swissarmyhammer-focus/src kanban-app/ui/src` returns no source-code matches (test fixtures and historical task descriptions excluded).\n- [ ] `grep -r \"spatial_register_zone\" swissarmyhammer-focus/src kanban-app/ui/src kanban-app/src` returns no source-code matches.\n- [ ] `is_zone()` / `RegisteredScope::Zone` enum variants are gone from the kernel.\n- [ ] `cargo test -p swissarmyhammer-focus` passes.\n- [ ] `pnpm -C kanban-app/ui test` passes (full suite — this refactor touches many files).\n- [ ] `pnpm -C kanban-app/ui typecheck` passes.\n- [ ] `cargo build --workspace` passes.\n- [ ] No behaviour change observable in the running app: the four cross-zone bug fixes from the redesign still work; in-card and in-row navigation works; drill / first / last work; focus indicator still paints; `showFocusBar={false}` containers still suppress the visible bar.\n- [ ] README rewritten to reflect single-primitive model.\n\n## Tests\n\n- [ ] No new tests required (pure refactor) — but the existing test suite must pass after the sweep. Any test that explicitly asserted \"this is a zone, not a leaf\" needs to either become \"this has children\" or be deleted.\n- [ ] Re-run the four cross-zone regression assertions from `swissarmyhammer-focus/tests/cross_zone_geometric_nav.rs` and confirm green.\n- [ ] Re-run the drill / first / last assertions and confirm green.\n- [ ] Run `cargo test -p swissarmyhammer-focus && pnpm -C kanban-app/ui test && pnpm -C kanban-app/ui typecheck` and confirm everything green.\n\n## Workflow\n\n- This task is large in line count (30+ files) but **not** in conceptual surface — it's a mechanical rename + delete + IPC consolidation. One PR is fine because the pieces (kernel struct collapse, React component collapse, IPC unification) are interlocking — splitting them creates incompatible intermediate states.\n- Sequence within the PR:\n  1. Kernel side first: collapse `FocusZone` into `FocusScope` in Rust, update IPC command names, run `cargo test -p swissarmyhammer-focus`.\n  2. Update IPC adapter in React (`spatial-focus-context.tsx`, `commands.rs`).\n  3. Sweep React callers: rename `<FocusZone>` → `<FocusScope>` and update imports. Use mechanical find/replace; verify each file builds.\n  4. Sweep tests on both sides.\n  5. Run full test suite. Fix any genuine fallout (likely tests that asserted on `is_zone()` or `data-moniker=\"ui:...\"` patterns that no longer differentiate).\n  6. Rewrite README.\n- No `/tdd` for this one — it's a refactor with no new behaviour to test. The existing test suite is the safety net.\n"}],["title",{"kind":"set","value":"Spatial-nav follow-up: collapse FocusZone into FocusScope (single primitive with optional children)"}],["project",{"kind":"set","value":"spatial-nav"}]]}
{"id":"01KQSDP99N70Y1RSDZ9T6G5FVJ","timestamp":"2026-05-04T11:59:01.429964Z","op":"update","item_id":"01KQSDP4ZJY5ERAJ68TFPVFRRE","forward_patch":"--- original\n+++ modified\n@@ -117,3 +117,4 @@\n   5. Run full test suite. Fix any genuine fallout (likely tests that asserted on `is_zone()` or `data-moniker=\"ui:...\"` patterns that no longer differentiate).\n   6. Rewrite README.\n - No `/tdd` for this one — it's a refactor with no new behaviour to test. The existing test suite is the safety net.\n+#spatial-nav-redesign\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -117,4 +117,3 @@\n   5. Run full test suite. Fix any genuine fallout (likely tests that asserted on `is_zone()` or `data-moniker=\"ui:...\"` patterns that no longer differentiate).\n   6. Rewrite README.\n - No `/tdd` for this one — it's a refactor with no new behaviour to test. The existing test suite is the safety net.\n-#spatial-nav-redesign\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQSDP99PPS2CTCF49ME33WES","timestamp":"2026-05-04T11:59:01.430431Z","entity_type":"task","entity_id":"01KQSDP4ZJY5ERAJ68TFPVFRRE","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -103,3 +103,4 @@\n   5. Run full test suite. Fix any genuine fallout (likely tests that asserted on `is_zone()` or `data-moniker=\"ui:...\"` patterns that no longer differentiate).\n   6. Rewrite README.\n - No `/tdd` for this one — it's a refactor with no new behaviour to test. The existing test suite is the safety net.\n+#spatial-nav-redesign\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -103,4 +103,3 @@\n   5. Run full test suite. Fix any genuine fallout (likely tests that asserted on `is_zone()` or `data-moniker=\"ui:...\"` patterns that no longer differentiate).\n   6. Rewrite README.\n - No `/tdd` for this one — it's a refactor with no new behaviour to test. The existing test suite is the safety net.\n-#spatial-nav-redesign\n\\ No newline at end of file\n"}]]}
{"id":"01KQSDTNR5CQAX1M91NKKYEBGN","timestamp":"2026-05-04T12:01:25.253358Z","op":"update","item_id":"01KQSDP4ZJY5ERAJ68TFPVFRRE","forward_patch":"--- original\n+++ modified\n@@ -7,8 +7,8 @@\n - 01KQQTY2HZBX7M9TW95862JVQ3\n - 01KQQTYKS63RPM62PPVJ7S7J60\n - 01KQQTZ7PSXEQF1WWX14ST8WRT\n-position_column: todo\n-position_ordinal: cc80\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial-nav follow-up: collapse FocusZone into FocusScope (single primitive with optional children)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -7,8 +7,8 @@\n - 01KQQTY2HZBX7M9TW95862JVQ3\n - 01KQQTYKS63RPM62PPVJ7S7J60\n - 01KQQTZ7PSXEQF1WWX14ST8WRT\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: cc80\n project: spatial-nav\n title: 'Spatial-nav follow-up: collapse FocusZone into FocusScope (single primitive with optional children)'\n ---\n","transaction_id":null}
{"id":"01KQSDTNR5ZMFFBXVKYSC3SZY8","timestamp":"2026-05-04T12:01:25.253805Z","entity_type":"task","entity_id":"01KQSDP4ZJY5ERAJ68TFPVFRRE","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-cc80\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+cc80\n\\ No newline at end of file\n"}]]}
{"id":"01KQSE4799QGSAN0M6B62QFT4W","timestamp":"2026-05-04T12:06:38.121459Z","op":"update","item_id":"01KQSDP4ZJY5ERAJ68TFPVFRRE","forward_patch":"--- original\n+++ modified\n@@ -7,8 +7,8 @@\n - 01KQQTY2HZBX7M9TW95862JVQ3\n - 01KQQTYKS63RPM62PPVJ7S7J60\n - 01KQQTZ7PSXEQF1WWX14ST8WRT\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: ce80\n project: spatial-nav\n title: 'Spatial-nav follow-up: collapse FocusZone into FocusScope (single primitive with optional children)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -7,8 +7,8 @@\n - 01KQQTY2HZBX7M9TW95862JVQ3\n - 01KQQTYKS63RPM62PPVJ7S7J60\n - 01KQQTZ7PSXEQF1WWX14ST8WRT\n-position_column: todo\n-position_ordinal: ce80\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial-nav follow-up: collapse FocusZone into FocusScope (single primitive with optional children)'\n ---\n","transaction_id":null}
{"id":"01KQSE479AZBJMEKTWJYFQH2M8","timestamp":"2026-05-04T12:06:38.122301Z","entity_type":"task","entity_id":"01KQSDP4ZJY5ERAJ68TFPVFRRE","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ce80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ce80\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQSEJE60MAZS854T2A11TWAD","timestamp":"2026-05-04T12:14:23.936763Z","op":"update","item_id":"01KQSDP4ZJY5ERAJ68TFPVFRRE","forward_patch":"--- original\n+++ modified\n@@ -12,109 +12,40 @@\n project: spatial-nav\n title: 'Spatial-nav follow-up: collapse FocusZone into FocusScope (single primitive with optional children)'\n ---\n-## Reference\n-\n-Follow-up to the spatial-nav redesign (design `01KQQSXM2PEYR1WAQ7QXW3B8ME`). The geometric cardinal algorithm and drill / first / last operations have landed; the type-level `FocusZone` / `FocusScope` split was kept during the redesign as a deliberate staging choice. This task collapses them into one primitive.\n-\n-**Decision:** keep `FocusScope` as the single primitive name. Delete `FocusZone` entirely. `FocusScope` already accepts children (its prop type has `children: ReactNode`). The \"is this a container or a leaf?\" question becomes \"does it have registered children?\" — answered by querying the registry, not by a type tag.\n-\n-This is **pure refactor** — no behaviour change. The geometric algorithm doesn't distinguish kind; `showFocusBar={false}` is still a per-instance prop; last-focused memory is still keyed by FQM; drill / first / last still work on the focused scope's children.\n-\n-## What\n-\n-### Rust kernel side (`swissarmyhammer-focus`)\n-\n-- `src/scope.rs`:\n-  - Delete the `FocusZone` struct.\n-  - `RegisteredScope` (the enum or wrapper that distinguishes zones from leaves today) collapses to `FocusScope` only. If it's currently `enum RegisteredScope { Scope(FocusScope), Zone(FocusZone) }`, replace with a struct alias or directly with `FocusScope`.\n-  - Drop `RegisteredScope::is_zone()` — replace callers with `registry.has_children(fq)` or equivalent.\n-  - Whatever per-zone fields don't already exist on `FocusScope` (e.g. `showFocusBar` flag) move onto `FocusScope`.\n-\n-- `src/registry.rs`:\n-  - Collapse `register_scope` and `register_zone` IPC commands into a single `register_scope` (or keep both as transition aliases — recommend drop entirely; the React side is updated in this same PR).\n-  - Internal storage: one collection, not two. `zones_iter` / `leaves_iter` either go away or get re-implemented as filters over `entries_in_layer` (`with_children` / `without_children`).\n-  - Children query: add `fn children_of(&self, parent_fq: &FullyQualifiedMoniker) -> impl Iterator<Item = &FocusScope>` or similar — used by drill-in / first / last.\n-\n-- `src/navigate.rs`:\n-  - The leaf-tie-break in `geometric_pick` currently uses `is_zone()` to prefer leaves. Rewrite as \"prefer scopes with no registered children\" using the new `children_of` query.\n-  - Drill-in / first / last already operate on children — confirm they don't depend on `is_zone()` after the rename.\n-\n-- `src/types.rs`:\n-  - Sweep any `Direction` / `Pixels` etc. for stale references.\n+## STATUS: Split into 4 sub-tasks (2026-05-04)\n\n-- `tests/*`:\n-  - Every `is_zone()` / `RegisteredScope::Zone(...)` callsite needs updating. Fixture builders (`tests/fixtures/mod.rs`) become \"register a scope; if it has children later, it acts like a zone.\"\n-  - Tests asserting `spatial_register_zone` IPC name become `spatial_register_scope`.\n+The single-PR collapse was attempted by the implementer and reported as too large for a single execution context — measured at 143 source files affected, with intermediate states that don't compile. Per user direction, this task is now a **planning placeholder**. Execution work is split across four sequential sub-tasks, each landing a coherent compile-clean state:\n\n-### TypeScript / React side (`kanban-app/ui`)\n-\n-- `src/components/focus-zone.tsx`:\n-  - Delete the file. Move any unique logic into `focus-scope.tsx`.\n-  - All exports (`FocusZone`, `FocusZoneProps`, `FocusZoneContext`, `useParentZoneFq`, etc.) — the type re-exports become `FocusScope*`. `FocusZoneContext` becomes `FocusScopeContext` (or stays as the existing `FocusScopeContext` if there's already overlap — verify).\n-\n-- `src/components/focus-scope.tsx`:\n-  - Confirm it accepts children (it does — see existing `FocusScopeProps.children`).\n-  - Add any zone-only props that need to migrate (e.g. zone-specific layout class handling).\n-\n-- `src/components/use-track-rect-on-ancestor-scroll.ts` and `src/lib/spatial-focus-context.tsx`:\n-  - Update `spatial_register_zone` IPC adapter to alias / point at `spatial_register_scope`.\n+| Order | Sub-task | ID | Net |\n+|---|---|---|---|\n+| A | Kernel only — collapse `FocusZone`/`is_zone` in scope/registry/navigate/lib | `01KQSEA6J8BCE1CAQ1S9XK7TFF` | `cargo test -p swissarmyhammer-focus` standalone green; other crates intentionally break |\n+| B | Tauri IPC + React adapter — unified `register_scope` surface | `01KQSEC2KJ1K1CVTHYNXGZZG2C` | `cargo build --workspace` clean; adapter typecheck passes |\n+| C | React component sweep — `<FocusZone>` JSX → `<FocusScope>`; delete `focus-zone.tsx` | `01KQSEDYSJT9J8Y1N8JYX7TQ12` | Component-tree typecheck passes; tests still partial |\n+| D | Test sweep + README rewrite — ~120 test files, single-primitive prose | `01KQSEFZ8VQ67KFA0B4QE84Z2X` | Full vitest + clippy + tsc clean; final acceptance grep checks pass |\n\n-- **Sweep all callers** — replace `<FocusZone …>` with `<FocusScope …>`, replace `import { FocusZone, FocusZoneProps } from \"@/components/focus-zone\"` with the equivalent import from `focus-scope`. Known callers (run a grep to confirm complete list):\n-  - `nav-bar.tsx` (the outer `<FocusZone moniker=\"ui:navbar\">` and its `<FocusZone moniker=\"ui:navbar.board-selector\">`)\n-  - `board-view.tsx` (`BoardSpatialZone` → `<FocusZone moniker=\"ui:board\">`)\n-  - `view-container.tsx` (`ViewSpatialZone` — note: a separate task `01KQPW61ZNS010FV8PV844KY0K` may delete this entirely; coordinate)\n-  - `perspective-container.tsx` (`PerspectiveSpatialZone` → `<FocusZone moniker=\"ui:perspective\">`)\n-  - `perspective-tab-bar.tsx` (`PerspectiveBarSpatialZone`)\n-  - `left-nav.tsx` (`<FocusZone moniker=\"ui:left-nav\">`)\n-  - `column-view.tsx` (column body zones)\n-  - `grid-view.tsx` (`GridSpatialZone`)\n-  - `entity-card.tsx` (card zones)\n-  - `data-table.tsx`\n-  - `fields/field.tsx` (`Field` mounts a `<FocusZone>`)\n-  - `board-selector.tsx`\n-  - `inspectable.tsx`\n-  - `inspectors-container.tsx`\n-  - Any others — verified by `grep -rn \"FocusZone\" kanban-app/ui/src --include='*.ts*'`\n+Decisions approved by user (carried into all four sub-tasks):\n\n-- **Sweep all tests** that assert on `<FocusZone>`, `FocusZoneContext`, `useParentZoneFq`, or `spatial_register_zone` IPC. Many test files (board-view.guards.node.test.ts, perspective-spatial-nav.guards.node.test.ts, etc.) will need updates.\n+1. Wire-format `RegisterEntry` discriminator (`kind: \"scope\"|\"zone\"`) is dropped (intra-process IPC, no external consumers).\n+2. `last_focused` moves onto every `FocusScope`, defaulting to `None`.\n+3. The `scope-not-leaf` validation path (~150 LOC) becomes vacuous and is deleted.\n+4. `ChildScope`, `FocusEntry`, `ScopeKind`, `BatchRegisterError::KindMismatch` all collapse or disappear.\n\n-### IPC contract\n+---\n\n-- After this PR, the IPC surface from React to Rust has only `spatial_register_scope` (no more `spatial_register_zone`). The kernel decides \"leaf vs container\" by inspecting children.\n-- Update `kanban-app/src/commands.rs` (the Tauri command bridge) accordingly.\n+## Original task body (for reference)\n\n-### Documentation\n+(Below kept verbatim from the original filing — the four sub-tasks restate the relevant pieces inline so an implementer reading only their sub-task has full context.)\n\n-- `swissarmyhammer-focus/README.md`: rewrite the prose that distinguishes zones from leaves. New framing: \"All registered things are `FocusScope`s. A scope with no children behaves as a leaf; a scope with children behaves as a container.\"\n+---\n\n-## Acceptance Criteria\n+## Reference\n\n-- [ ] `grep -r \"FocusZone\" swissarmyhammer-focus/src kanban-app/ui/src` returns no source-code matches (test fixtures and historical task descriptions excluded).\n-- [ ] `grep -r \"spatial_register_zone\" swissarmyhammer-focus/src kanban-app/ui/src kanban-app/src` returns no source-code matches.\n-- [ ] `is_zone()` / `RegisteredScope::Zone` enum variants are gone from the kernel.\n-- [ ] `cargo test -p swissarmyhammer-focus` passes.\n-- [ ] `pnpm -C kanban-app/ui test` passes (full suite — this refactor touches many files).\n-- [ ] `pnpm -C kanban-app/ui typecheck` passes.\n-- [ ] `cargo build --workspace` passes.\n-- [ ] No behaviour change observable in the running app: the four cross-zone bug fixes from the redesign still work; in-card and in-row navigation works; drill / first / last work; focus indicator still paints; `showFocusBar={false}` containers still suppress the visible bar.\n-- [ ] README rewritten to reflect single-primitive model.\n+Follow-up to the spatial-nav redesign (design `01KQQSXM2PEYR1WAQ7QXW3B8ME`). The geometric cardinal algorithm and drill / first / last operations have landed; the type-level `FocusZone` / `FocusScope` split was kept during the redesign as a deliberate staging choice. This task collapses them into one primitive.\n\n-## Tests\n+**Decision:** keep `FocusScope` as the single primitive name. Delete `FocusZone` entirely. `FocusScope` already accepts children (its prop type has `children: ReactNode`). The \"is this a container or a leaf?\" question becomes \"does it have registered children?\" — answered by querying the registry, not by a type tag.\n\n-- [ ] No new tests required (pure refactor) — but the existing test suite must pass after the sweep. Any test that explicitly asserted \"this is a zone, not a leaf\" needs to either become \"this has children\" or be deleted.\n-- [ ] Re-run the four cross-zone regression assertions from `swissarmyhammer-focus/tests/cross_zone_geometric_nav.rs` and confirm green.\n-- [ ] Re-run the drill / first / last assertions and confirm green.\n-- [ ] Run `cargo test -p swissarmyhammer-focus && pnpm -C kanban-app/ui test && pnpm -C kanban-app/ui typecheck` and confirm everything green.\n+This is **pure refactor** — no behaviour change. The geometric algorithm doesn't distinguish kind; `showFocusBar={false}` is still a per-instance prop; last-focused memory is still keyed by FQM; drill / first / last still work on the focused scope's children.\n\n-## Workflow\n+(See sub-tasks A/B/C/D for the full file lists and acceptance criteria.)\n\n-- This task is large in line count (30+ files) but **not** in conceptual surface — it's a mechanical rename + delete + IPC consolidation. One PR is fine because the pieces (kernel struct collapse, React component collapse, IPC unification) are interlocking — splitting them creates incompatible intermediate states.\n-- Sequence within the PR:\n-  1. Kernel side first: collapse `FocusZone` into `FocusScope` in Rust, update IPC command names, run `cargo test -p swissarmyhammer-focus`.\n-  2. Update IPC adapter in React (`spatial-focus-context.tsx`, `commands.rs`).\n-  3. Sweep React callers: rename `<FocusZone>` → `<FocusScope>` and update imports. Use mechanical find/replace; verify each file builds.\n-  4. Sweep tests on both sides.\n-  5. Run full test suite. Fix any genuine fallout (likely tests that asserted on `is_zone()` or `data-moniker=\"ui:...\"` patterns that no longer differentiate).\n-  6. Rewrite README.\n-- No `/tdd` for this one — it's a refactor with no new behaviour to test. The existing test suite is the safety net.\n #spatial-nav-redesign\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -12,40 +12,109 @@\n project: spatial-nav\n title: 'Spatial-nav follow-up: collapse FocusZone into FocusScope (single primitive with optional children)'\n ---\n-## STATUS: Split into 4 sub-tasks (2026-05-04)\n+## Reference\n\n-The single-PR collapse was attempted by the implementer and reported as too large for a single execution context — measured at 143 source files affected, with intermediate states that don't compile. Per user direction, this task is now a **planning placeholder**. Execution work is split across four sequential sub-tasks, each landing a coherent compile-clean state:\n+Follow-up to the spatial-nav redesign (design `01KQQSXM2PEYR1WAQ7QXW3B8ME`). The geometric cardinal algorithm and drill / first / last operations have landed; the type-level `FocusZone` / `FocusScope` split was kept during the redesign as a deliberate staging choice. This task collapses them into one primitive.\n\n-| Order | Sub-task | ID | Net |\n-|---|---|---|---|\n-| A | Kernel only — collapse `FocusZone`/`is_zone` in scope/registry/navigate/lib | `01KQSEA6J8BCE1CAQ1S9XK7TFF` | `cargo test -p swissarmyhammer-focus` standalone green; other crates intentionally break |\n-| B | Tauri IPC + React adapter — unified `register_scope` surface | `01KQSEC2KJ1K1CVTHYNXGZZG2C` | `cargo build --workspace` clean; adapter typecheck passes |\n-| C | React component sweep — `<FocusZone>` JSX → `<FocusScope>`; delete `focus-zone.tsx` | `01KQSEDYSJT9J8Y1N8JYX7TQ12` | Component-tree typecheck passes; tests still partial |\n-| D | Test sweep + README rewrite — ~120 test files, single-primitive prose | `01KQSEFZ8VQ67KFA0B4QE84Z2X` | Full vitest + clippy + tsc clean; final acceptance grep checks pass |\n+**Decision:** keep `FocusScope` as the single primitive name. Delete `FocusZone` entirely. `FocusScope` already accepts children (its prop type has `children: ReactNode`). The \"is this a container or a leaf?\" question becomes \"does it have registered children?\" — answered by querying the registry, not by a type tag.\n\n-Decisions approved by user (carried into all four sub-tasks):\n+This is **pure refactor** — no behaviour change. The geometric algorithm doesn't distinguish kind; `showFocusBar={false}` is still a per-instance prop; last-focused memory is still keyed by FQM; drill / first / last still work on the focused scope's children.\n\n-1. Wire-format `RegisterEntry` discriminator (`kind: \"scope\"|\"zone\"`) is dropped (intra-process IPC, no external consumers).\n-2. `last_focused` moves onto every `FocusScope`, defaulting to `None`.\n-3. The `scope-not-leaf` validation path (~150 LOC) becomes vacuous and is deleted.\n-4. `ChildScope`, `FocusEntry`, `ScopeKind`, `BatchRegisterError::KindMismatch` all collapse or disappear.\n+## What\n\n----\n+### Rust kernel side (`swissarmyhammer-focus`)\n\n-## Original task body (for reference)\n+- `src/scope.rs`:\n+  - Delete the `FocusZone` struct.\n+  - `RegisteredScope` (the enum or wrapper that distinguishes zones from leaves today) collapses to `FocusScope` only. If it's currently `enum RegisteredScope { Scope(FocusScope), Zone(FocusZone) }`, replace with a struct alias or directly with `FocusScope`.\n+  - Drop `RegisteredScope::is_zone()` — replace callers with `registry.has_children(fq)` or equivalent.\n+  - Whatever per-zone fields don't already exist on `FocusScope` (e.g. `showFocusBar` flag) move onto `FocusScope`.\n\n-(Below kept verbatim from the original filing — the four sub-tasks restate the relevant pieces inline so an implementer reading only their sub-task has full context.)\n+- `src/registry.rs`:\n+  - Collapse `register_scope` and `register_zone` IPC commands into a single `register_scope` (or keep both as transition aliases — recommend drop entirely; the React side is updated in this same PR).\n+  - Internal storage: one collection, not two. `zones_iter` / `leaves_iter` either go away or get re-implemented as filters over `entries_in_layer` (`with_children` / `without_children`).\n+  - Children query: add `fn children_of(&self, parent_fq: &FullyQualifiedMoniker) -> impl Iterator<Item = &FocusScope>` or similar — used by drill-in / first / last.\n\n----\n+- `src/navigate.rs`:\n+  - The leaf-tie-break in `geometric_pick` currently uses `is_zone()` to prefer leaves. Rewrite as \"prefer scopes with no registered children\" using the new `children_of` query.\n+  - Drill-in / first / last already operate on children — confirm they don't depend on `is_zone()` after the rename.\n+\n+- `src/types.rs`:\n+  - Sweep any `Direction` / `Pixels` etc. for stale references.\n\n-## Reference\n+- `tests/*`:\n+  - Every `is_zone()` / `RegisteredScope::Zone(...)` callsite needs updating. Fixture builders (`tests/fixtures/mod.rs`) become \"register a scope; if it has children later, it acts like a zone.\"\n+  - Tests asserting `spatial_register_zone` IPC name become `spatial_register_scope`.\n\n-Follow-up to the spatial-nav redesign (design `01KQQSXM2PEYR1WAQ7QXW3B8ME`). The geometric cardinal algorithm and drill / first / last operations have landed; the type-level `FocusZone` / `FocusScope` split was kept during the redesign as a deliberate staging choice. This task collapses them into one primitive.\n+### TypeScript / React side (`kanban-app/ui`)\n\n-**Decision:** keep `FocusScope` as the single primitive name. Delete `FocusZone` entirely. `FocusScope` already accepts children (its prop type has `children: ReactNode`). The \"is this a container or a leaf?\" question becomes \"does it have registered children?\" — answered by querying the registry, not by a type tag.\n+- `src/components/focus-zone.tsx`:\n+  - Delete the file. Move any unique logic into `focus-scope.tsx`.\n+  - All exports (`FocusZone`, `FocusZoneProps`, `FocusZoneContext`, `useParentZoneFq`, etc.) — the type re-exports become `FocusScope*`. `FocusZoneContext` becomes `FocusScopeContext` (or stays as the existing `FocusScopeContext` if there's already overlap — verify).\n\n-This is **pure refactor** — no behaviour change. The geometric algorithm doesn't distinguish kind; `showFocusBar={false}` is still a per-instance prop; last-focused memory is still keyed by FQM; drill / first / last still work on the focused scope's children.\n+- `src/components/focus-scope.tsx`:\n+  - Confirm it accepts children (it does — see existing `FocusScopeProps.children`).\n+  - Add any zone-only props that need to migrate (e.g. zone-specific layout class handling).\n\n-(See sub-tasks A/B/C/D for the full file lists and acceptance criteria.)\n+- `src/components/use-track-rect-on-ancestor-scroll.ts` and `src/lib/spatial-focus-context.tsx`:\n+  - Update `spatial_register_zone` IPC adapter to alias / point at `spatial_register_scope`.\n\n+- **Sweep all callers** — replace `<FocusZone …>` with `<FocusScope …>`, replace `import { FocusZone, FocusZoneProps } from \"@/components/focus-zone\"` with the equivalent import from `focus-scope`. Known callers (run a grep to confirm complete list):\n+  - `nav-bar.tsx` (the outer `<FocusZone moniker=\"ui:navbar\">` and its `<FocusZone moniker=\"ui:navbar.board-selector\">`)\n+  - `board-view.tsx` (`BoardSpatialZone` → `<FocusZone moniker=\"ui:board\">`)\n+  - `view-container.tsx` (`ViewSpatialZone` — note: a separate task `01KQPW61ZNS010FV8PV844KY0K` may delete this entirely; coordinate)\n+  - `perspective-container.tsx` (`PerspectiveSpatialZone` → `<FocusZone moniker=\"ui:perspective\">`)\n+  - `perspective-tab-bar.tsx` (`PerspectiveBarSpatialZone`)\n+  - `left-nav.tsx` (`<FocusZone moniker=\"ui:left-nav\">`)\n+  - `column-view.tsx` (column body zones)\n+  - `grid-view.tsx` (`GridSpatialZone`)\n+  - `entity-card.tsx` (card zones)\n+  - `data-table.tsx`\n+  - `fields/field.tsx` (`Field` mounts a `<FocusZone>`)\n+  - `board-selector.tsx`\n+  - `inspectable.tsx`\n+  - `inspectors-container.tsx`\n+  - Any others — verified by `grep -rn \"FocusZone\" kanban-app/ui/src --include='*.ts*'`\n+\n+- **Sweep all tests** that assert on `<FocusZone>`, `FocusZoneContext`, `useParentZoneFq`, or `spatial_register_zone` IPC. Many test files (board-view.guards.node.test.ts, perspective-spatial-nav.guards.node.test.ts, etc.) will need updates.\n+\n+### IPC contract\n+\n+- After this PR, the IPC surface from React to Rust has only `spatial_register_scope` (no more `spatial_register_zone`). The kernel decides \"leaf vs container\" by inspecting children.\n+- Update `kanban-app/src/commands.rs` (the Tauri command bridge) accordingly.\n+\n+### Documentation\n+\n+- `swissarmyhammer-focus/README.md`: rewrite the prose that distinguishes zones from leaves. New framing: \"All registered things are `FocusScope`s. A scope with no children behaves as a leaf; a scope with children behaves as a container.\"\n+\n+## Acceptance Criteria\n+\n+- [ ] `grep -r \"FocusZone\" swissarmyhammer-focus/src kanban-app/ui/src` returns no source-code matches (test fixtures and historical task descriptions excluded).\n+- [ ] `grep -r \"spatial_register_zone\" swissarmyhammer-focus/src kanban-app/ui/src kanban-app/src` returns no source-code matches.\n+- [ ] `is_zone()` / `RegisteredScope::Zone` enum variants are gone from the kernel.\n+- [ ] `cargo test -p swissarmyhammer-focus` passes.\n+- [ ] `pnpm -C kanban-app/ui test` passes (full suite — this refactor touches many files).\n+- [ ] `pnpm -C kanban-app/ui typecheck` passes.\n+- [ ] `cargo build --workspace` passes.\n+- [ ] No behaviour change observable in the running app: the four cross-zone bug fixes from the redesign still work; in-card and in-row navigation works; drill / first / last work; focus indicator still paints; `showFocusBar={false}` containers still suppress the visible bar.\n+- [ ] README rewritten to reflect single-primitive model.\n+\n+## Tests\n+\n+- [ ] No new tests required (pure refactor) — but the existing test suite must pass after the sweep. Any test that explicitly asserted \"this is a zone, not a leaf\" needs to either become \"this has children\" or be deleted.\n+- [ ] Re-run the four cross-zone regression assertions from `swissarmyhammer-focus/tests/cross_zone_geometric_nav.rs` and confirm green.\n+- [ ] Re-run the drill / first / last assertions and confirm green.\n+- [ ] Run `cargo test -p swissarmyhammer-focus && pnpm -C kanban-app/ui test && pnpm -C kanban-app/ui typecheck` and confirm everything green.\n+\n+## Workflow\n+\n+- This task is large in line count (30+ files) but **not** in conceptual surface — it's a mechanical rename + delete + IPC consolidation. One PR is fine because the pieces (kernel struct collapse, React component collapse, IPC unification) are interlocking — splitting them creates incompatible intermediate states.\n+- Sequence within the PR:\n+  1. Kernel side first: collapse `FocusZone` into `FocusScope` in Rust, update IPC command names, run `cargo test -p swissarmyhammer-focus`.\n+  2. Update IPC adapter in React (`spatial-focus-context.tsx`, `commands.rs`).\n+  3. Sweep React callers: rename `<FocusZone>` → `<FocusScope>` and update imports. Use mechanical find/replace; verify each file builds.\n+  4. Sweep tests on both sides.\n+  5. Run full test suite. Fix any genuine fallout (likely tests that asserted on `is_zone()` or `data-moniker=\"ui:...\"` patterns that no longer differentiate).\n+  6. Rewrite README.\n+- No `/tdd` for this one — it's a refactor with no new behaviour to test. The existing test suite is the safety net.\n #spatial-nav-redesign\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQSEJE61CV02CBWXWJ2WED5K","timestamp":"2026-05-04T12:14:23.937410Z","entity_type":"task","entity_id":"01KQSDP4ZJY5ERAJ68TFPVFRRE","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,106 +1,37 @@\n-## Reference\n-\n-Follow-up to the spatial-nav redesign (design `01KQQSXM2PEYR1WAQ7QXW3B8ME`). The geometric cardinal algorithm and drill / first / last operations have landed; the type-level `FocusZone` / `FocusScope` split was kept during the redesign as a deliberate staging choice. This task collapses them into one primitive.\n-\n-**Decision:** keep `FocusScope` as the single primitive name. Delete `FocusZone` entirely. `FocusScope` already accepts children (its prop type has `children: ReactNode`). The \"is this a container or a leaf?\" question becomes \"does it have registered children?\" — answered by querying the registry, not by a type tag.\n-\n-This is **pure refactor** — no behaviour change. The geometric algorithm doesn't distinguish kind; `showFocusBar={false}` is still a per-instance prop; last-focused memory is still keyed by FQM; drill / first / last still work on the focused scope's children.\n-\n-## What\n-\n-### Rust kernel side (`swissarmyhammer-focus`)\n-\n-- `src/scope.rs`:\n-  - Delete the `FocusZone` struct.\n-  - `RegisteredScope` (the enum or wrapper that distinguishes zones from leaves today) collapses to `FocusScope` only. If it's currently `enum RegisteredScope { Scope(FocusScope), Zone(FocusZone) }`, replace with a struct alias or directly with `FocusScope`.\n-  - Drop `RegisteredScope::is_zone()` — replace callers with `registry.has_children(fq)` or equivalent.\n-  - Whatever per-zone fields don't already exist on `FocusScope` (e.g. `showFocusBar` flag) move onto `FocusScope`.\n-\n-- `src/registry.rs`:\n-  - Collapse `register_scope` and `register_zone` IPC commands into a single `register_scope` (or keep both as transition aliases — recommend drop entirely; the React side is updated in this same PR).\n-  - Internal storage: one collection, not two. `zones_iter` / `leaves_iter` either go away or get re-implemented as filters over `entries_in_layer` (`with_children` / `without_children`).\n-  - Children query: add `fn children_of(&self, parent_fq: &FullyQualifiedMoniker) -> impl Iterator<Item = &FocusScope>` or similar — used by drill-in / first / last.\n-\n-- `src/navigate.rs`:\n-  - The leaf-tie-break in `geometric_pick` currently uses `is_zone()` to prefer leaves. Rewrite as \"prefer scopes with no registered children\" using the new `children_of` query.\n-  - Drill-in / first / last already operate on children — confirm they don't depend on `is_zone()` after the rename.\n-\n-- `src/types.rs`:\n-  - Sweep any `Direction` / `Pixels` etc. for stale references.\n+## STATUS: Split into 4 sub-tasks (2026-05-04)\n\n-- `tests/*`:\n-  - Every `is_zone()` / `RegisteredScope::Zone(...)` callsite needs updating. Fixture builders (`tests/fixtures/mod.rs`) become \"register a scope; if it has children later, it acts like a zone.\"\n-  - Tests asserting `spatial_register_zone` IPC name become `spatial_register_scope`.\n+The single-PR collapse was attempted by the implementer and reported as too large for a single execution context — measured at 143 source files affected, with intermediate states that don't compile. Per user direction, this task is now a **planning placeholder**. Execution work is split across four sequential sub-tasks, each landing a coherent compile-clean state:\n\n-### TypeScript / React side (`kanban-app/ui`)\n-\n-- `src/components/focus-zone.tsx`:\n-  - Delete the file. Move any unique logic into `focus-scope.tsx`.\n-  - All exports (`FocusZone`, `FocusZoneProps`, `FocusZoneContext`, `useParentZoneFq`, etc.) — the type re-exports become `FocusScope*`. `FocusZoneContext` becomes `FocusScopeContext` (or stays as the existing `FocusScopeContext` if there's already overlap — verify).\n-\n-- `src/components/focus-scope.tsx`:\n-  - Confirm it accepts children (it does — see existing `FocusScopeProps.children`).\n-  - Add any zone-only props that need to migrate (e.g. zone-specific layout class handling).\n-\n-- `src/components/use-track-rect-on-ancestor-scroll.ts` and `src/lib/spatial-focus-context.tsx`:\n-  - Update `spatial_register_zone` IPC adapter to alias / point at `spatial_register_scope`.\n+| Order | Sub-task | ID | Net |\n+|---|---|---|---|\n+| A | Kernel only — collapse `FocusZone`/`is_zone` in scope/registry/navigate/lib | `01KQSEA6J8BCE1CAQ1S9XK7TFF` | `cargo test -p swissarmyhammer-focus` standalone green; other crates intentionally break |\n+| B | Tauri IPC + React adapter — unified `register_scope` surface | `01KQSEC2KJ1K1CVTHYNXGZZG2C` | `cargo build --workspace` clean; adapter typecheck passes |\n+| C | React component sweep — `<FocusZone>` JSX → `<FocusScope>`; delete `focus-zone.tsx` | `01KQSEDYSJT9J8Y1N8JYX7TQ12` | Component-tree typecheck passes; tests still partial |\n+| D | Test sweep + README rewrite — ~120 test files, single-primitive prose | `01KQSEFZ8VQ67KFA0B4QE84Z2X` | Full vitest + clippy + tsc clean; final acceptance grep checks pass |\n\n-- **Sweep all callers** — replace `<FocusZone …>` with `<FocusScope …>`, replace `import { FocusZone, FocusZoneProps } from \"@/components/focus-zone\"` with the equivalent import from `focus-scope`. Known callers (run a grep to confirm complete list):\n-  - `nav-bar.tsx` (the outer `<FocusZone moniker=\"ui:navbar\">` and its `<FocusZone moniker=\"ui:navbar.board-selector\">`)\n-  - `board-view.tsx` (`BoardSpatialZone` → `<FocusZone moniker=\"ui:board\">`)\n-  - `view-container.tsx` (`ViewSpatialZone` — note: a separate task `01KQPW61ZNS010FV8PV844KY0K` may delete this entirely; coordinate)\n-  - `perspective-container.tsx` (`PerspectiveSpatialZone` → `<FocusZone moniker=\"ui:perspective\">`)\n-  - `perspective-tab-bar.tsx` (`PerspectiveBarSpatialZone`)\n-  - `left-nav.tsx` (`<FocusZone moniker=\"ui:left-nav\">`)\n-  - `column-view.tsx` (column body zones)\n-  - `grid-view.tsx` (`GridSpatialZone`)\n-  - `entity-card.tsx` (card zones)\n-  - `data-table.tsx`\n-  - `fields/field.tsx` (`Field` mounts a `<FocusZone>`)\n-  - `board-selector.tsx`\n-  - `inspectable.tsx`\n-  - `inspectors-container.tsx`\n-  - Any others — verified by `grep -rn \"FocusZone\" kanban-app/ui/src --include='*.ts*'`\n+Decisions approved by user (carried into all four sub-tasks):\n\n-- **Sweep all tests** that assert on `<FocusZone>`, `FocusZoneContext`, `useParentZoneFq`, or `spatial_register_zone` IPC. Many test files (board-view.guards.node.test.ts, perspective-spatial-nav.guards.node.test.ts, etc.) will need updates.\n+1. Wire-format `RegisterEntry` discriminator (`kind: \"scope\"|\"zone\"`) is dropped (intra-process IPC, no external consumers).\n+2. `last_focused` moves onto every `FocusScope`, defaulting to `None`.\n+3. The `scope-not-leaf` validation path (~150 LOC) becomes vacuous and is deleted.\n+4. `ChildScope`, `FocusEntry`, `ScopeKind`, `BatchRegisterError::KindMismatch` all collapse or disappear.\n\n-### IPC contract\n+---\n\n-- After this PR, the IPC surface from React to Rust has only `spatial_register_scope` (no more `spatial_register_zone`). The kernel decides \"leaf vs container\" by inspecting children.\n-- Update `kanban-app/src/commands.rs` (the Tauri command bridge) accordingly.\n+## Original task body (for reference)\n\n-### Documentation\n+(Below kept verbatim from the original filing — the four sub-tasks restate the relevant pieces inline so an implementer reading only their sub-task has full context.)\n\n-- `swissarmyhammer-focus/README.md`: rewrite the prose that distinguishes zones from leaves. New framing: \"All registered things are `FocusScope`s. A scope with no children behaves as a leaf; a scope with children behaves as a container.\"\n+---\n\n-## Acceptance Criteria\n+## Reference\n\n-- [ ] `grep -r \"FocusZone\" swissarmyhammer-focus/src kanban-app/ui/src` returns no source-code matches (test fixtures and historical task descriptions excluded).\n-- [ ] `grep -r \"spatial_register_zone\" swissarmyhammer-focus/src kanban-app/ui/src kanban-app/src` returns no source-code matches.\n-- [ ] `is_zone()` / `RegisteredScope::Zone` enum variants are gone from the kernel.\n-- [ ] `cargo test -p swissarmyhammer-focus` passes.\n-- [ ] `pnpm -C kanban-app/ui test` passes (full suite — this refactor touches many files).\n-- [ ] `pnpm -C kanban-app/ui typecheck` passes.\n-- [ ] `cargo build --workspace` passes.\n-- [ ] No behaviour change observable in the running app: the four cross-zone bug fixes from the redesign still work; in-card and in-row navigation works; drill / first / last work; focus indicator still paints; `showFocusBar={false}` containers still suppress the visible bar.\n-- [ ] README rewritten to reflect single-primitive model.\n+Follow-up to the spatial-nav redesign (design `01KQQSXM2PEYR1WAQ7QXW3B8ME`). The geometric cardinal algorithm and drill / first / last operations have landed; the type-level `FocusZone` / `FocusScope` split was kept during the redesign as a deliberate staging choice. This task collapses them into one primitive.\n\n-## Tests\n+**Decision:** keep `FocusScope` as the single primitive name. Delete `FocusZone` entirely. `FocusScope` already accepts children (its prop type has `children: ReactNode`). The \"is this a container or a leaf?\" question becomes \"does it have registered children?\" — answered by querying the registry, not by a type tag.\n\n-- [ ] No new tests required (pure refactor) — but the existing test suite must pass after the sweep. Any test that explicitly asserted \"this is a zone, not a leaf\" needs to either become \"this has children\" or be deleted.\n-- [ ] Re-run the four cross-zone regression assertions from `swissarmyhammer-focus/tests/cross_zone_geometric_nav.rs` and confirm green.\n-- [ ] Re-run the drill / first / last assertions and confirm green.\n-- [ ] Run `cargo test -p swissarmyhammer-focus && pnpm -C kanban-app/ui test && pnpm -C kanban-app/ui typecheck` and confirm everything green.\n+This is **pure refactor** — no behaviour change. The geometric algorithm doesn't distinguish kind; `showFocusBar={false}` is still a per-instance prop; last-focused memory is still keyed by FQM; drill / first / last still work on the focused scope's children.\n\n-## Workflow\n+(See sub-tasks A/B/C/D for the full file lists and acceptance criteria.)\n\n-- This task is large in line count (30+ files) but **not** in conceptual surface — it's a mechanical rename + delete + IPC consolidation. One PR is fine because the pieces (kernel struct collapse, React component collapse, IPC unification) are interlocking — splitting them creates incompatible intermediate states.\n-- Sequence within the PR:\n-  1. Kernel side first: collapse `FocusZone` into `FocusScope` in Rust, update IPC command names, run `cargo test -p swissarmyhammer-focus`.\n-  2. Update IPC adapter in React (`spatial-focus-context.tsx`, `commands.rs`).\n-  3. Sweep React callers: rename `<FocusZone>` → `<FocusScope>` and update imports. Use mechanical find/replace; verify each file builds.\n-  4. Sweep tests on both sides.\n-  5. Run full test suite. Fix any genuine fallout (likely tests that asserted on `is_zone()` or `data-moniker=\"ui:...\"` patterns that no longer differentiate).\n-  6. Rewrite README.\n-- No `/tdd` for this one — it's a refactor with no new behaviour to test. The existing test suite is the safety net.\n #spatial-nav-redesign\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,37 +1,106 @@\n-## STATUS: Split into 4 sub-tasks (2026-05-04)\n+## Reference\n\n-The single-PR collapse was attempted by the implementer and reported as too large for a single execution context — measured at 143 source files affected, with intermediate states that don't compile. Per user direction, this task is now a **planning placeholder**. Execution work is split across four sequential sub-tasks, each landing a coherent compile-clean state:\n+Follow-up to the spatial-nav redesign (design `01KQQSXM2PEYR1WAQ7QXW3B8ME`). The geometric cardinal algorithm and drill / first / last operations have landed; the type-level `FocusZone` / `FocusScope` split was kept during the redesign as a deliberate staging choice. This task collapses them into one primitive.\n\n-| Order | Sub-task | ID | Net |\n-|---|---|---|---|\n-| A | Kernel only — collapse `FocusZone`/`is_zone` in scope/registry/navigate/lib | `01KQSEA6J8BCE1CAQ1S9XK7TFF` | `cargo test -p swissarmyhammer-focus` standalone green; other crates intentionally break |\n-| B | Tauri IPC + React adapter — unified `register_scope` surface | `01KQSEC2KJ1K1CVTHYNXGZZG2C` | `cargo build --workspace` clean; adapter typecheck passes |\n-| C | React component sweep — `<FocusZone>` JSX → `<FocusScope>`; delete `focus-zone.tsx` | `01KQSEDYSJT9J8Y1N8JYX7TQ12` | Component-tree typecheck passes; tests still partial |\n-| D | Test sweep + README rewrite — ~120 test files, single-primitive prose | `01KQSEFZ8VQ67KFA0B4QE84Z2X` | Full vitest + clippy + tsc clean; final acceptance grep checks pass |\n+**Decision:** keep `FocusScope` as the single primitive name. Delete `FocusZone` entirely. `FocusScope` already accepts children (its prop type has `children: ReactNode`). The \"is this a container or a leaf?\" question becomes \"does it have registered children?\" — answered by querying the registry, not by a type tag.\n\n-Decisions approved by user (carried into all four sub-tasks):\n+This is **pure refactor** — no behaviour change. The geometric algorithm doesn't distinguish kind; `showFocusBar={false}` is still a per-instance prop; last-focused memory is still keyed by FQM; drill / first / last still work on the focused scope's children.\n\n-1. Wire-format `RegisterEntry` discriminator (`kind: \"scope\"|\"zone\"`) is dropped (intra-process IPC, no external consumers).\n-2. `last_focused` moves onto every `FocusScope`, defaulting to `None`.\n-3. The `scope-not-leaf` validation path (~150 LOC) becomes vacuous and is deleted.\n-4. `ChildScope`, `FocusEntry`, `ScopeKind`, `BatchRegisterError::KindMismatch` all collapse or disappear.\n+## What\n\n----\n+### Rust kernel side (`swissarmyhammer-focus`)\n\n-## Original task body (for reference)\n+- `src/scope.rs`:\n+  - Delete the `FocusZone` struct.\n+  - `RegisteredScope` (the enum or wrapper that distinguishes zones from leaves today) collapses to `FocusScope` only. If it's currently `enum RegisteredScope { Scope(FocusScope), Zone(FocusZone) }`, replace with a struct alias or directly with `FocusScope`.\n+  - Drop `RegisteredScope::is_zone()` — replace callers with `registry.has_children(fq)` or equivalent.\n+  - Whatever per-zone fields don't already exist on `FocusScope` (e.g. `showFocusBar` flag) move onto `FocusScope`.\n\n-(Below kept verbatim from the original filing — the four sub-tasks restate the relevant pieces inline so an implementer reading only their sub-task has full context.)\n+- `src/registry.rs`:\n+  - Collapse `register_scope` and `register_zone` IPC commands into a single `register_scope` (or keep both as transition aliases — recommend drop entirely; the React side is updated in this same PR).\n+  - Internal storage: one collection, not two. `zones_iter` / `leaves_iter` either go away or get re-implemented as filters over `entries_in_layer` (`with_children` / `without_children`).\n+  - Children query: add `fn children_of(&self, parent_fq: &FullyQualifiedMoniker) -> impl Iterator<Item = &FocusScope>` or similar — used by drill-in / first / last.\n\n----\n+- `src/navigate.rs`:\n+  - The leaf-tie-break in `geometric_pick` currently uses `is_zone()` to prefer leaves. Rewrite as \"prefer scopes with no registered children\" using the new `children_of` query.\n+  - Drill-in / first / last already operate on children — confirm they don't depend on `is_zone()` after the rename.\n+\n+- `src/types.rs`:\n+  - Sweep any `Direction` / `Pixels` etc. for stale references.\n\n-## Reference\n+- `tests/*`:\n+  - Every `is_zone()` / `RegisteredScope::Zone(...)` callsite needs updating. Fixture builders (`tests/fixtures/mod.rs`) become \"register a scope; if it has children later, it acts like a zone.\"\n+  - Tests asserting `spatial_register_zone` IPC name become `spatial_register_scope`.\n\n-Follow-up to the spatial-nav redesign (design `01KQQSXM2PEYR1WAQ7QXW3B8ME`). The geometric cardinal algorithm and drill / first / last operations have landed; the type-level `FocusZone` / `FocusScope` split was kept during the redesign as a deliberate staging choice. This task collapses them into one primitive.\n+### TypeScript / React side (`kanban-app/ui`)\n\n-**Decision:** keep `FocusScope` as the single primitive name. Delete `FocusZone` entirely. `FocusScope` already accepts children (its prop type has `children: ReactNode`). The \"is this a container or a leaf?\" question becomes \"does it have registered children?\" — answered by querying the registry, not by a type tag.\n+- `src/components/focus-zone.tsx`:\n+  - Delete the file. Move any unique logic into `focus-scope.tsx`.\n+  - All exports (`FocusZone`, `FocusZoneProps`, `FocusZoneContext`, `useParentZoneFq`, etc.) — the type re-exports become `FocusScope*`. `FocusZoneContext` becomes `FocusScopeContext` (or stays as the existing `FocusScopeContext` if there's already overlap — verify).\n\n-This is **pure refactor** — no behaviour change. The geometric algorithm doesn't distinguish kind; `showFocusBar={false}` is still a per-instance prop; last-focused memory is still keyed by FQM; drill / first / last still work on the focused scope's children.\n+- `src/components/focus-scope.tsx`:\n+  - Confirm it accepts children (it does — see existing `FocusScopeProps.children`).\n+  - Add any zone-only props that need to migrate (e.g. zone-specific layout class handling).\n\n-(See sub-tasks A/B/C/D for the full file lists and acceptance criteria.)\n+- `src/components/use-track-rect-on-ancestor-scroll.ts` and `src/lib/spatial-focus-context.tsx`:\n+  - Update `spatial_register_zone` IPC adapter to alias / point at `spatial_register_scope`.\n\n+- **Sweep all callers** — replace `<FocusZone …>` with `<FocusScope …>`, replace `import { FocusZone, FocusZoneProps } from \"@/components/focus-zone\"` with the equivalent import from `focus-scope`. Known callers (run a grep to confirm complete list):\n+  - `nav-bar.tsx` (the outer `<FocusZone moniker=\"ui:navbar\">` and its `<FocusZone moniker=\"ui:navbar.board-selector\">`)\n+  - `board-view.tsx` (`BoardSpatialZone` → `<FocusZone moniker=\"ui:board\">`)\n+  - `view-container.tsx` (`ViewSpatialZone` — note: a separate task `01KQPW61ZNS010FV8PV844KY0K` may delete this entirely; coordinate)\n+  - `perspective-container.tsx` (`PerspectiveSpatialZone` → `<FocusZone moniker=\"ui:perspective\">`)\n+  - `perspective-tab-bar.tsx` (`PerspectiveBarSpatialZone`)\n+  - `left-nav.tsx` (`<FocusZone moniker=\"ui:left-nav\">`)\n+  - `column-view.tsx` (column body zones)\n+  - `grid-view.tsx` (`GridSpatialZone`)\n+  - `entity-card.tsx` (card zones)\n+  - `data-table.tsx`\n+  - `fields/field.tsx` (`Field` mounts a `<FocusZone>`)\n+  - `board-selector.tsx`\n+  - `inspectable.tsx`\n+  - `inspectors-container.tsx`\n+  - Any others — verified by `grep -rn \"FocusZone\" kanban-app/ui/src --include='*.ts*'`\n+\n+- **Sweep all tests** that assert on `<FocusZone>`, `FocusZoneContext`, `useParentZoneFq`, or `spatial_register_zone` IPC. Many test files (board-view.guards.node.test.ts, perspective-spatial-nav.guards.node.test.ts, etc.) will need updates.\n+\n+### IPC contract\n+\n+- After this PR, the IPC surface from React to Rust has only `spatial_register_scope` (no more `spatial_register_zone`). The kernel decides \"leaf vs container\" by inspecting children.\n+- Update `kanban-app/src/commands.rs` (the Tauri command bridge) accordingly.\n+\n+### Documentation\n+\n+- `swissarmyhammer-focus/README.md`: rewrite the prose that distinguishes zones from leaves. New framing: \"All registered things are `FocusScope`s. A scope with no children behaves as a leaf; a scope with children behaves as a container.\"\n+\n+## Acceptance Criteria\n+\n+- [ ] `grep -r \"FocusZone\" swissarmyhammer-focus/src kanban-app/ui/src` returns no source-code matches (test fixtures and historical task descriptions excluded).\n+- [ ] `grep -r \"spatial_register_zone\" swissarmyhammer-focus/src kanban-app/ui/src kanban-app/src` returns no source-code matches.\n+- [ ] `is_zone()` / `RegisteredScope::Zone` enum variants are gone from the kernel.\n+- [ ] `cargo test -p swissarmyhammer-focus` passes.\n+- [ ] `pnpm -C kanban-app/ui test` passes (full suite — this refactor touches many files).\n+- [ ] `pnpm -C kanban-app/ui typecheck` passes.\n+- [ ] `cargo build --workspace` passes.\n+- [ ] No behaviour change observable in the running app: the four cross-zone bug fixes from the redesign still work; in-card and in-row navigation works; drill / first / last work; focus indicator still paints; `showFocusBar={false}` containers still suppress the visible bar.\n+- [ ] README rewritten to reflect single-primitive model.\n+\n+## Tests\n+\n+- [ ] No new tests required (pure refactor) — but the existing test suite must pass after the sweep. Any test that explicitly asserted \"this is a zone, not a leaf\" needs to either become \"this has children\" or be deleted.\n+- [ ] Re-run the four cross-zone regression assertions from `swissarmyhammer-focus/tests/cross_zone_geometric_nav.rs` and confirm green.\n+- [ ] Re-run the drill / first / last assertions and confirm green.\n+- [ ] Run `cargo test -p swissarmyhammer-focus && pnpm -C kanban-app/ui test && pnpm -C kanban-app/ui typecheck` and confirm everything green.\n+\n+## Workflow\n+\n+- This task is large in line count (30+ files) but **not** in conceptual surface — it's a mechanical rename + delete + IPC consolidation. One PR is fine because the pieces (kernel struct collapse, React component collapse, IPC unification) are interlocking — splitting them creates incompatible intermediate states.\n+- Sequence within the PR:\n+  1. Kernel side first: collapse `FocusZone` into `FocusScope` in Rust, update IPC command names, run `cargo test -p swissarmyhammer-focus`.\n+  2. Update IPC adapter in React (`spatial-focus-context.tsx`, `commands.rs`).\n+  3. Sweep React callers: rename `<FocusZone>` → `<FocusScope>` and update imports. Use mechanical find/replace; verify each file builds.\n+  4. Sweep tests on both sides.\n+  5. Run full test suite. Fix any genuine fallout (likely tests that asserted on `is_zone()` or `data-moniker=\"ui:...\"` patterns that no longer differentiate).\n+  6. Rewrite README.\n+- No `/tdd` for this one — it's a refactor with no new behaviour to test. The existing test suite is the safety net.\n #spatial-nav-redesign\n\\ No newline at end of file\n"}]]}
{"id":"01KQSEKN56Y3SM86ABVXA87V3S","timestamp":"2026-05-04T12:15:03.846725Z","op":"update","item_id":"01KQSDP4ZJY5ERAJ68TFPVFRRE","forward_patch":"--- original\n+++ modified\n@@ -7,8 +7,8 @@\n - 01KQQTY2HZBX7M9TW95862JVQ3\n - 01KQQTYKS63RPM62PPVJ7S7J60\n - 01KQQTZ7PSXEQF1WWX14ST8WRT\n-position_column: todo\n-position_ordinal: ce80\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffff8780\n project: spatial-nav\n title: 'Spatial-nav follow-up: collapse FocusZone into FocusScope (single primitive with optional children)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -7,8 +7,8 @@\n - 01KQQTY2HZBX7M9TW95862JVQ3\n - 01KQQTYKS63RPM62PPVJ7S7J60\n - 01KQQTZ7PSXEQF1WWX14ST8WRT\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffff8780\n+position_column: todo\n+position_ordinal: ce80\n project: spatial-nav\n title: 'Spatial-nav follow-up: collapse FocusZone into FocusScope (single primitive with optional children)'\n ---\n","transaction_id":null}
{"id":"01KQSEKN571GKN3QSR34CWW297","timestamp":"2026-05-04T12:15:03.847550Z","entity_type":"task","entity_id":"01KQSDP4ZJY5ERAJ68TFPVFRRE","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ce80\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffff8780\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffff8780\n\\ No newline at end of file\n+ce80\n\\ No newline at end of file\n"}]]}

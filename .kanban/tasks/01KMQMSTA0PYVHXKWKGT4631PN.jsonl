{"id":"01KMQMSTA1WR08FTES8V8YS5HR","timestamp":"2026-03-27T12:37:42.849248Z","entity_type":"task","entity_id":"01KMQMSTA0PYVHXKWKGT4631PN","op":"create","changes":[["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\nAdd a `claimWhen` prop to FocusScope that lets each scope declare when it should claim focus in response to a navigation command. This is pull-based navigation: unfocused scopes listen for nav commands and claim focus when their predicate matches.\n\n### Core mechanism\n\n`claimWhen` is an array of `{ command: string, when: (focusedMoniker: string | null) => boolean }`. When a navigation command fires, EntityFocusProvider broadcasts it to all registered claim predicates. The **first** predicate that returns true wins — `setFocus(thatMoniker)` is called and evaluation **stops** (short-circuit).\n\n### Files to modify\n\n- **`kanban-app/ui/src/components/focus-scope.tsx`** — add `claimWhen` prop to FocusScopeProps, register/unregister predicates in EntityFocusProvider on mount/unmount\n- **`kanban-app/ui/src/lib/entity-focus-context.tsx`** — add claim predicate registry and `broadcastNavCommand(commandId)` function. Registry is a `Map<string, Array<{ moniker: string, when: (focused: string | null) => boolean }>>` keyed by command ID. `broadcastNavCommand` iterates the array for the given command, first match wins, calls `setFocus`.\n- **`kanban-app/ui/src/lib/entity-focus-context.test.tsx`** — new tests for broadcast mechanism\n\n### Registration order / short-circuit\n\nReact effects fire children-first (depth-first). Child FocusScopes register before parent FocusScopes. This means more-specific scopes (pills) are checked before less-specific (field rows). First match wins, loop stops.\n\n### API\n\n```tsx\n<FocusScope\n  moniker=\\\"tag:tag-1\\\"\n  commands={entityCommands}\n  claimWhen={[\n    { command: \\\"nav.right\\\", when: (f) => f === \\\"task:id.tags\\\" },\n    { command: \\\"nav.right\\\", when: (f) => f === prevPillMoniker },\n  ]}\n>\n```\n\n## Acceptance Criteria\n\n- [ ] FocusScope accepts `claimWhen` prop\n- [ ] Predicates are registered/unregistered with EntityFocusProvider on mount/unmount\n- [ ] `broadcastNavCommand(commandId)` evaluates predicates with current focusedMoniker\n- [ ] First matching predicate claims focus, evaluation stops\n- [ ] Unmounted FocusScopes don't leave stale predicates\n\n## Tests\n\n- [ ] `entity-focus-context.test.tsx` — broadcast with one matching predicate claims focus\n- [ ] `entity-focus-context.test.tsx` — broadcast with no matching predicate leaves focus unchanged\n- [ ] `entity-focus-context.test.tsx` — first match wins (short-circuit), second match ignored\n- [ ] `entity-focus-context.test.tsx` — unmounted scope's predicate is not evaluated\n- [ ] `pnpm vitest run` passes\""}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"c380"}],["title",{"kind":"set","value":"Add claimWhen primitive to FocusScope"}]],"transaction_id":"01KMQMST7XMAXSEH70QQX09CVE"}
{"id":"01KMQMZBFNKHNDFV5ZAFFD80AQ","timestamp":"2026-03-27T12:40:44.277023Z","entity_type":"task","entity_id":"01KMQMSTA0PYVHXKWKGT4631PN","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,46 +1,74 @@\n ## What\n\n-Add a `claimWhen` prop to FocusScope that lets each scope declare when it should claim focus in response to a navigation command. This is pull-based navigation: unfocused scopes listen for nav commands and claim focus when their predicate matches.\n+Add two primitives to FocusScope:\n+\n+### 1. `FocusScopeContext` + `useParentFocusScope()`\n+\n+FocusScope provides a `FocusScopeContext` with its moniker. Any child component calls `useParentFocusScope()` to get the nearest ancestor FocusScope's moniker. This is how a pill knows its parent field moniker without prop threading.\n+\n+```tsx\n+const FocusScopeContext = createContext<string | null>(null);\n+\n+// Inside FocusScope render:\n+<FocusScopeContext.Provider value={moniker}>\n+  ...\n+</FocusScopeContext.Provider>\n+\n+// Hook for children:\n+export function useParentFocusScope(): string | null {\n+  return useContext(FocusScopeContext);\n+}\n+```\n+\n+CommandScopeProviders (which aren't FocusScopes) don't set this context, so it naturally skips over them to the nearest FocusScope ancestor.\n\n-### Core mechanism\n+### 2. `claimWhen` prop + claim broadcast\n\n-`claimWhen` is an array of `{ command: string, when: (focusedMoniker: string | null) => boolean }`. When a navigation command fires, EntityFocusProvider broadcasts it to all registered claim predicates. The **first** predicate that returns true wins — `setFocus(thatMoniker)` is called and evaluation **stops** (short-circuit).\n+`claimWhen` is an array of `{ command: string, when: (focusedMoniker: string | null) => boolean }`. Each FocusScope registers its predicates with EntityFocusProvider on mount. When a navigation command fires, EntityFocusProvider broadcasts it — iterating registered predicates, **stopping at the first match** (short-circuit), and calling `setFocus(claimantMoniker)`.\n\n ### Files to modify\n\n-- **`kanban-app/ui/src/components/focus-scope.tsx`** — add `claimWhen` prop to FocusScopeProps, register/unregister predicates in EntityFocusProvider on mount/unmount\n-- **`kanban-app/ui/src/lib/entity-focus-context.tsx`** — add claim predicate registry and `broadcastNavCommand(commandId)` function. Registry is a `Map<string, Array<{ moniker: string, when: (focused: string | null) => boolean }>>` keyed by command ID. `broadcastNavCommand` iterates the array for the given command, first match wins, calls `setFocus`.\n-- **`kanban-app/ui/src/lib/entity-focus-context.test.tsx`** — new tests for broadcast mechanism\n+- **`kanban-app/ui/src/components/focus-scope.tsx`** — add `FocusScopeContext`, `useParentFocusScope()`, `claimWhen` prop. FocusScope wraps children in `FocusScopeContext.Provider`. On mount, registers claimWhen predicates; on unmount, unregisters.\n+- **`kanban-app/ui/src/lib/entity-focus-context.tsx`** — add claim predicate registry: `registerClaimPredicates(moniker, predicates)` / `unregisterClaimPredicates(moniker)` and `broadcastNavCommand(commandId)` which evaluates predicates with current focusedMoniker, first match wins.\n\n ### Registration order / short-circuit\n\n-React effects fire children-first (depth-first). Child FocusScopes register before parent FocusScopes. This means more-specific scopes (pills) are checked before less-specific (field rows). First match wins, loop stops.\n+React effects fire children-first (depth-first). Child FocusScopes register before parent FocusScopes. More-specific scopes (pills) are checked before less-specific (field rows). First match wins, loop stops.\n\n ### API\n\n ```tsx\n+// A pill uses useParentFocusScope to know its field moniker\n+const parentMoniker = useParentFocusScope();\n+\n <FocusScope\n-  moniker=\\\"tag:tag-1\\\"\n+  moniker={pillMoniker}\n   commands={entityCommands}\n   claimWhen={[\n-    { command: \\\"nav.right\\\", when: (f) => f === \\\"task:id.tags\\\" },\n-    { command: \\\"nav.right\\\", when: (f) => f === prevPillMoniker },\n+    { command: \\\"nav.right\\\", when: (f) => f === parentMoniker },\n+    { command: \\\"nav.right\\\", when: (f) => f === prevSiblingMoniker },\n   ]}\n->\n+/>\n ```\n\n ## Acceptance Criteria\n\n+- [ ] `useParentFocusScope()` returns nearest ancestor FocusScope's moniker\n+- [ ] `useParentFocusScope()` returns null when no ancestor FocusScope exists\n+- [ ] `useParentFocusScope()` skips CommandScopeProviders that aren't FocusScopes\n - [ ] FocusScope accepts `claimWhen` prop\n - [ ] Predicates are registered/unregistered with EntityFocusProvider on mount/unmount\n - [ ] `broadcastNavCommand(commandId)` evaluates predicates with current focusedMoniker\n-- [ ] First matching predicate claims focus, evaluation stops\n+- [ ] First matching predicate claims focus, evaluation stops (short-circuit)\n - [ ] Unmounted FocusScopes don't leave stale predicates\n\n ## Tests\n\n+- [ ] `focus-scope.test.tsx` — `useParentFocusScope()` returns parent FocusScope moniker\n+- [ ] `focus-scope.test.tsx` — `useParentFocusScope()` skips CommandScopeProvider, returns grandparent FocusScope moniker\n+- [ ] `focus-scope.test.tsx` — `useParentFocusScope()` returns null at root\n - [ ] `entity-focus-context.test.tsx` — broadcast with one matching predicate claims focus\n-- [ ] `entity-focus-context.test.tsx` — broadcast with no matching predicate leaves focus unchanged\n-- [ ] `entity-focus-context.test.tsx` — first match wins (short-circuit), second match ignored\n+- [ ] `entity-focus-context.test.tsx` — broadcast with no match leaves focus unchanged\n+- [ ] `entity-focus-context.test.tsx` — first match wins (short-circuit)\n - [ ] `entity-focus-context.test.tsx` — unmounted scope's predicate is not evaluated\n - [ ] `pnpm vitest run` passes\"\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,74 +1,46 @@\n ## What\n\n-Add two primitives to FocusScope:\n-\n-### 1. `FocusScopeContext` + `useParentFocusScope()`\n+Add a `claimWhen` prop to FocusScope that lets each scope declare when it should claim focus in response to a navigation command. This is pull-based navigation: unfocused scopes listen for nav commands and claim focus when their predicate matches.\n\n-FocusScope provides a `FocusScopeContext` with its moniker. Any child component calls `useParentFocusScope()` to get the nearest ancestor FocusScope's moniker. This is how a pill knows its parent field moniker without prop threading.\n+### Core mechanism\n\n-```tsx\n-const FocusScopeContext = createContext<string | null>(null);\n-\n-// Inside FocusScope render:\n-<FocusScopeContext.Provider value={moniker}>\n-  ...\n-</FocusScopeContext.Provider>\n+`claimWhen` is an array of `{ command: string, when: (focusedMoniker: string | null) => boolean }`. When a navigation command fires, EntityFocusProvider broadcasts it to all registered claim predicates. The **first** predicate that returns true wins — `setFocus(thatMoniker)` is called and evaluation **stops** (short-circuit).\n\n-// Hook for children:\n-export function useParentFocusScope(): string | null {\n-  return useContext(FocusScopeContext);\n-}\n-```\n-\n-CommandScopeProviders (which aren't FocusScopes) don't set this context, so it naturally skips over them to the nearest FocusScope ancestor.\n-\n-### 2. `claimWhen` prop + claim broadcast\n-\n-`claimWhen` is an array of `{ command: string, when: (focusedMoniker: string | null) => boolean }`. Each FocusScope registers its predicates with EntityFocusProvider on mount. When a navigation command fires, EntityFocusProvider broadcasts it — iterating registered predicates, **stopping at the first match** (short-circuit), and calling `setFocus(claimantMoniker)`.\n-\n ### Files to modify\n\n-- **`kanban-app/ui/src/components/focus-scope.tsx`** — add `FocusScopeContext`, `useParentFocusScope()`, `claimWhen` prop. FocusScope wraps children in `FocusScopeContext.Provider`. On mount, registers claimWhen predicates; on unmount, unregisters.\n-- **`kanban-app/ui/src/lib/entity-focus-context.tsx`** — add claim predicate registry: `registerClaimPredicates(moniker, predicates)` / `unregisterClaimPredicates(moniker)` and `broadcastNavCommand(commandId)` which evaluates predicates with current focusedMoniker, first match wins.\n+- **`kanban-app/ui/src/components/focus-scope.tsx`** — add `claimWhen` prop to FocusScopeProps, register/unregister predicates in EntityFocusProvider on mount/unmount\n+- **`kanban-app/ui/src/lib/entity-focus-context.tsx`** — add claim predicate registry and `broadcastNavCommand(commandId)` function. Registry is a `Map<string, Array<{ moniker: string, when: (focused: string | null) => boolean }>>` keyed by command ID. `broadcastNavCommand` iterates the array for the given command, first match wins, calls `setFocus`.\n+- **`kanban-app/ui/src/lib/entity-focus-context.test.tsx`** — new tests for broadcast mechanism\n\n ### Registration order / short-circuit\n\n-React effects fire children-first (depth-first). Child FocusScopes register before parent FocusScopes. More-specific scopes (pills) are checked before less-specific (field rows). First match wins, loop stops.\n+React effects fire children-first (depth-first). Child FocusScopes register before parent FocusScopes. This means more-specific scopes (pills) are checked before less-specific (field rows). First match wins, loop stops.\n\n ### API\n\n ```tsx\n-// A pill uses useParentFocusScope to know its field moniker\n-const parentMoniker = useParentFocusScope();\n-\n <FocusScope\n-  moniker={pillMoniker}\n+  moniker=\\\"tag:tag-1\\\"\n   commands={entityCommands}\n   claimWhen={[\n-    { command: \\\"nav.right\\\", when: (f) => f === parentMoniker },\n-    { command: \\\"nav.right\\\", when: (f) => f === prevSiblingMoniker },\n+    { command: \\\"nav.right\\\", when: (f) => f === \\\"task:id.tags\\\" },\n+    { command: \\\"nav.right\\\", when: (f) => f === prevPillMoniker },\n   ]}\n-/>\n+>\n ```\n\n ## Acceptance Criteria\n\n-- [ ] `useParentFocusScope()` returns nearest ancestor FocusScope's moniker\n-- [ ] `useParentFocusScope()` returns null when no ancestor FocusScope exists\n-- [ ] `useParentFocusScope()` skips CommandScopeProviders that aren't FocusScopes\n - [ ] FocusScope accepts `claimWhen` prop\n - [ ] Predicates are registered/unregistered with EntityFocusProvider on mount/unmount\n - [ ] `broadcastNavCommand(commandId)` evaluates predicates with current focusedMoniker\n-- [ ] First matching predicate claims focus, evaluation stops (short-circuit)\n+- [ ] First matching predicate claims focus, evaluation stops\n - [ ] Unmounted FocusScopes don't leave stale predicates\n\n ## Tests\n\n-- [ ] `focus-scope.test.tsx` — `useParentFocusScope()` returns parent FocusScope moniker\n-- [ ] `focus-scope.test.tsx` — `useParentFocusScope()` skips CommandScopeProvider, returns grandparent FocusScope moniker\n-- [ ] `focus-scope.test.tsx` — `useParentFocusScope()` returns null at root\n - [ ] `entity-focus-context.test.tsx` — broadcast with one matching predicate claims focus\n-- [ ] `entity-focus-context.test.tsx` — broadcast with no match leaves focus unchanged\n-- [ ] `entity-focus-context.test.tsx` — first match wins (short-circuit)\n+- [ ] `entity-focus-context.test.tsx` — broadcast with no matching predicate leaves focus unchanged\n+- [ ] `entity-focus-context.test.tsx` — first match wins (short-circuit), second match ignored\n - [ ] `entity-focus-context.test.tsx` — unmounted scope's predicate is not evaluated\n - [ ] `pnpm vitest run` passes\"\n\\ No newline at end of file\n"}]],"transaction_id":"01KMQMZBFDATH0SDWG2XBEDGXX"}
{"id":"01KMQN1Z8T94YSN7X0YV14CNQT","timestamp":"2026-03-27T12:42:10.074055Z","entity_type":"task","entity_id":"01KMQMSTA0PYVHXKWKGT4631PN","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-c380\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+c380\n\\ No newline at end of file\n"}]],"transaction_id":"01KMQN1Z77363B25ATPEGQW02R"}
{"id":"01KMQNNRQXNZGM79J260QCHBJ5","timestamp":"2026-03-27T12:52:58.749067Z","entity_type":"task","entity_id":"01KMQMSTA0PYVHXKWKGT4631PN","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffe380\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffe380\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]],"transaction_id":"01KMQNNRPCNNZ5J64WC5XC4GYP"}

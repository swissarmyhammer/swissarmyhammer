{"id":"01KTBNJ8VK87KWFBM1915W607R","timestamp":"2026-06-05T10:34:21.939586Z","op":"create","item_id":"01KTBNJ8VK5JQ891Q26QKJK0J8","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,30 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KTBNHSR4EVTVJ35MGGD510R2\n+position_column: todo\n+position_ordinal: '8880'\n+project: local-review\n+title: Install builtin validators to ~/.validators (deploy + refresh)\n+---\n+## What\n+Give users the builtin rule set on disk so they can read, edit, and extend it — mirroring how skills are deployed. The embedded builtin validators are written out to `~/.validators/` on init/install.\n+\n+- Add an install/deploy step that copies the embedded `builtin/validators/**` into `~/.validators/`, idempotently (refresh builtin-owned files, never clobber user-authored validators or user edits to a non-builtin set).\n+- Wire it into the existing init/install path. Per the active `mirdan-install` direction, prefer routing this through mirdan's Profile-driven installer rather than a bespoke deployer — reuse the skills-deployment mechanism (store + symlink/copy), do not invent a parallel one. Confirm the right home during implementation against the design doc.\n+- Builtin remains the lowest-precedence layer even when materialized on disk; user/project edits still win via the loader precedence.\n+- Optionally add a `.validators` doctor check to replace the removed AVP one (only if it fits the install owner cleanly; otherwise leave out).\n+\n+## Acceptance Criteria\n+- [ ] Running init/install materializes the builtin validators under `~/.validators/` with the same structure as `builtin/validators/`.\n+- [ ] Re-running is idempotent: builtin-owned files are refreshed to current content; user-authored validators and user-created sets are untouched.\n+- [ ] The loader picks up the materialized builtin set and still applies builtin → user → project precedence.\n+\n+## Tests\n+- [ ] Integration test (temp HOME): run the installer → assert `~/.validators/code-quality/VALIDATOR.md` (and rules) exist and match the embedded source.\n+- [ ] Idempotency test: install twice; a user-added file under `~/.validators/` survives the second install; a hand-edited builtin file is restored (or preserved) per the chosen policy — assert the policy.\n+- [ ] `cargo test` green for the owning crate.\n+\n+## Workflow\n+- Use `/tdd` — write the temp-HOME deploy + idempotency tests first. REUSE the existing skills deployment/store mechanism (see mirdan / workspace-init); do not write a second copy routine. Coordinate with the `mirdan-install` project so this lands as a Profile entry, not a one-off.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,30 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KTBNHSR4EVTVJ35MGGD510R2\n-position_column: todo\n-position_ordinal: '8880'\n-project: local-review\n-title: Install builtin validators to ~/.validators (deploy + refresh)\n----\n-## What\n-Give users the builtin rule set on disk so they can read, edit, and extend it — mirroring how skills are deployed. The embedded builtin validators are written out to `~/.validators/` on init/install.\n-\n-- Add an install/deploy step that copies the embedded `builtin/validators/**` into `~/.validators/`, idempotently (refresh builtin-owned files, never clobber user-authored validators or user edits to a non-builtin set).\n-- Wire it into the existing init/install path. Per the active `mirdan-install` direction, prefer routing this through mirdan's Profile-driven installer rather than a bespoke deployer — reuse the skills-deployment mechanism (store + symlink/copy), do not invent a parallel one. Confirm the right home during implementation against the design doc.\n-- Builtin remains the lowest-precedence layer even when materialized on disk; user/project edits still win via the loader precedence.\n-- Optionally add a `.validators` doctor check to replace the removed AVP one (only if it fits the install owner cleanly; otherwise leave out).\n-\n-## Acceptance Criteria\n-- [ ] Running init/install materializes the builtin validators under `~/.validators/` with the same structure as `builtin/validators/`.\n-- [ ] Re-running is idempotent: builtin-owned files are refreshed to current content; user-authored validators and user-created sets are untouched.\n-- [ ] The loader picks up the materialized builtin set and still applies builtin → user → project precedence.\n-\n-## Tests\n-- [ ] Integration test (temp HOME): run the installer → assert `~/.validators/code-quality/VALIDATOR.md` (and rules) exist and match the embedded source.\n-- [ ] Idempotency test: install twice; a user-added file under `~/.validators/` survives the second install; a hand-edited builtin file is restored (or preserved) per the chosen policy — assert the policy.\n-- [ ] `cargo test` green for the owning crate.\n-\n-## Workflow\n-- Use `/tdd` — write the temp-HOME deploy + idempotency tests first. REUSE the existing skills deployment/store mechanism (see mirdan / workspace-init); do not write a second copy routine. Coordinate with the `mirdan-install` project so this lands as a Profile entry, not a one-off.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTBRTPQ8CJV1N1S5FQMHSGYD","timestamp":"2026-06-05T11:31:24.008041Z","op":"update","item_id":"01KTBNJ8VK5JQ891Q26QKJK0J8","forward_patch":"--- original\n+++ modified\n@@ -14,7 +14,7 @@\n - Add an install/deploy step that copies the embedded `builtin/validators/**` into `~/.validators/`, idempotently (refresh builtin-owned files, never clobber user-authored validators or user edits to a non-builtin set).\n - Wire it into the existing init/install path. Per the active `mirdan-install` direction, prefer routing this through mirdan's Profile-driven installer rather than a bespoke deployer — reuse the skills-deployment mechanism (store + symlink/copy), do not invent a parallel one. Confirm the right home during implementation against the design doc.\n - Builtin remains the lowest-precedence layer even when materialized on disk; user/project edits still win via the loader precedence.\n-- Optionally add a `.validators` doctor check to replace the removed AVP one (only if it fits the install owner cleanly; otherwise leave out).\n+- The validators doctor check is NOT here — it is the dedicated \"Review tool Doctorable\" task (`check validators` via the standard `Doctorable` trait into `sah doctor`). Do not add a separate one.\n\n ## Acceptance Criteria\n - [ ] Running init/install materializes the builtin validators under `~/.validators/` with the same structure as `builtin/validators/`.\n","reverse_patch":"--- original\n+++ modified\n@@ -14,7 +14,7 @@\n - Add an install/deploy step that copies the embedded `builtin/validators/**` into `~/.validators/`, idempotently (refresh builtin-owned files, never clobber user-authored validators or user edits to a non-builtin set).\n - Wire it into the existing init/install path. Per the active `mirdan-install` direction, prefer routing this through mirdan's Profile-driven installer rather than a bespoke deployer — reuse the skills-deployment mechanism (store + symlink/copy), do not invent a parallel one. Confirm the right home during implementation against the design doc.\n - Builtin remains the lowest-precedence layer even when materialized on disk; user/project edits still win via the loader precedence.\n-- The validators doctor check is NOT here — it is the dedicated \"Review tool Doctorable\" task (`check validators` via the standard `Doctorable` trait into `sah doctor`). Do not add a separate one.\n+- Optionally add a `.validators` doctor check to replace the removed AVP one (only if it fits the install owner cleanly; otherwise leave out).\n\n ## Acceptance Criteria\n - [ ] Running init/install materializes the builtin validators under `~/.validators/` with the same structure as `builtin/validators/`.\n","transaction_id":null}
{"id":"01KTBVJ8NMEBE39JVSNVRXWHG8","timestamp":"2026-06-05T12:19:13.204944Z","op":"update","item_id":"01KTBNJ8VK5JQ891Q26QKJK0J8","forward_patch":"--- original\n+++ modified\n@@ -6,25 +6,27 @@\n position_column: todo\n position_ordinal: '8880'\n project: local-review\n-title: Install builtin validators to ~/.validators (deploy + refresh)\n+title: Install builtin validators to $XDG_DATA_HOME/validators (deploy + refresh)\n ---\n ## What\n-Give users the builtin rule set on disk so they can read, edit, and extend it — mirroring how skills are deployed. The embedded builtin validators are written out to `~/.validators/` on init/install.\n+Materialize the embedded builtin validators onto disk on init so users can read, learn from, and copy them — exactly as builtin **skills** and **agents** are deployed today. The on-disk copy aids discoverability (`ls $XDG_DATA_HOME/validators`) even though the embedded set already loads.\n\n-- Add an install/deploy step that copies the embedded `builtin/validators/**` into `~/.validators/`, idempotently (refresh builtin-owned files, never clobber user-authored validators or user edits to a non-builtin set).\n-- Wire it into the existing init/install path. Per the active `mirdan-install` direction, prefer routing this through mirdan's Profile-driven installer rather than a bespoke deployer — reuse the skills-deployment mechanism (store + symlink/copy), do not invent a parallel one. Confirm the right home during implementation against the design doc.\n-- Builtin remains the lowest-precedence layer even when materialized on disk; user/project edits still win via the loader precedence.\n-- The validators doctor check is NOT here — it is the dedicated \"Review tool Doctorable\" task (`check validators` via the standard `Doctorable` trait into `sah doctor`). Do not add a separate one.\n+- Deploy the embedded `builtin/validators/**` into **`$XDG_DATA_HOME/validators/`** (default `~/.local/share/validators/`), resolved via the same `ManagedDirectory`/`xdg_data` mechanism task 7 establishes.\n+- **Reference-copy policy (consistent with skills/agents):** builtin-owned files are OWNED by the installer and **refreshed/overwritten** on each install so builtin updates always propagate — the deployed builtin is a read-only reference, not an edit target (same contract as the generated `.skills/`). To customize, a user creates a NEW validator (own name, or in `./.validators`) that wins by precedence; never edit a deployed builtin in place. User-authored validators and user-created sets are NEVER touched by the installer.\n+- Route this through the existing deployment mechanism — REUSE the skills/agents deploy path (mirdan Profile-driven installer: store + symlink/copy), do NOT write a parallel deployer. Per the active `mirdan-install` direction, this lands as a Profile entry alongside skills and agents.\n+- Builtin remains the lowest-precedence layer even when materialized; user/project edits still win via loader precedence.\n+- The validators doctor check is NOT here — it is the dedicated \"Review tool Doctorable\" task (`check validators` via the `Doctorable` trait into `sah doctor`).\n\n ## Acceptance Criteria\n-- [ ] Running init/install materializes the builtin validators under `~/.validators/` with the same structure as `builtin/validators/`.\n-- [ ] Re-running is idempotent: builtin-owned files are refreshed to current content; user-authored validators and user-created sets are untouched.\n-- [ ] The loader picks up the materialized builtin set and still applies builtin → user → project precedence.\n+- [ ] Init/install materializes the builtin validators under `$XDG_DATA_HOME/validators/` with the same structure as `builtin/validators/`.\n+- [ ] Re-running is idempotent: builtin-owned files are refreshed to current embedded content (overwritten); user-authored validators and user-created sets are untouched.\n+- [ ] The loader picks up the materialized builtin set; builtin → user → project precedence still holds.\n+- [ ] Deployment goes through the shared skills/agents installer (Profile entry), not a bespoke copy routine.\n\n ## Tests\n-- [ ] Integration test (temp HOME): run the installer → assert `~/.validators/code-quality/VALIDATOR.md` (and rules) exist and match the embedded source.\n-- [ ] Idempotency test: install twice; a user-added file under `~/.validators/` survives the second install; a hand-edited builtin file is restored (or preserved) per the chosen policy — assert the policy.\n+- [ ] Integration test (temp `XDG_DATA_HOME`): run the installer → assert `$XDG_DATA_HOME/validators/<set>/VALIDATOR.md` (+ rules) exist and match the embedded source.\n+- [ ] Idempotency test: install twice; a hand-edited builtin-deployed file is restored to embedded content (reference-copy policy); a user-authored validator added under the dir survives untouched.\n - [ ] `cargo test` green for the owning crate.\n\n ## Workflow\n-- Use `/tdd` — write the temp-HOME deploy + idempotency tests first. REUSE the existing skills deployment/store mechanism (see mirdan / workspace-init); do not write a second copy routine. Coordinate with the `mirdan-install` project so this lands as a Profile entry, not a one-off.\n\\ No newline at end of file\n+- Use `/tdd` — write the temp-`XDG_DATA_HOME` deploy + idempotency tests first. REUSE the skills/agents deployment/store mechanism (mirdan Profile); do not write a second copy routine. Coordinate with the `mirdan-install` project so this is a Profile entry, not a one-off.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -6,27 +6,25 @@\n position_column: todo\n position_ordinal: '8880'\n project: local-review\n-title: Install builtin validators to $XDG_DATA_HOME/validators (deploy + refresh)\n+title: Install builtin validators to ~/.validators (deploy + refresh)\n ---\n ## What\n-Materialize the embedded builtin validators onto disk on init so users can read, learn from, and copy them — exactly as builtin **skills** and **agents** are deployed today. The on-disk copy aids discoverability (`ls $XDG_DATA_HOME/validators`) even though the embedded set already loads.\n+Give users the builtin rule set on disk so they can read, edit, and extend it — mirroring how skills are deployed. The embedded builtin validators are written out to `~/.validators/` on init/install.\n\n-- Deploy the embedded `builtin/validators/**` into **`$XDG_DATA_HOME/validators/`** (default `~/.local/share/validators/`), resolved via the same `ManagedDirectory`/`xdg_data` mechanism task 7 establishes.\n-- **Reference-copy policy (consistent with skills/agents):** builtin-owned files are OWNED by the installer and **refreshed/overwritten** on each install so builtin updates always propagate — the deployed builtin is a read-only reference, not an edit target (same contract as the generated `.skills/`). To customize, a user creates a NEW validator (own name, or in `./.validators`) that wins by precedence; never edit a deployed builtin in place. User-authored validators and user-created sets are NEVER touched by the installer.\n-- Route this through the existing deployment mechanism — REUSE the skills/agents deploy path (mirdan Profile-driven installer: store + symlink/copy), do NOT write a parallel deployer. Per the active `mirdan-install` direction, this lands as a Profile entry alongside skills and agents.\n-- Builtin remains the lowest-precedence layer even when materialized; user/project edits still win via loader precedence.\n-- The validators doctor check is NOT here — it is the dedicated \"Review tool Doctorable\" task (`check validators` via the `Doctorable` trait into `sah doctor`).\n+- Add an install/deploy step that copies the embedded `builtin/validators/**` into `~/.validators/`, idempotently (refresh builtin-owned files, never clobber user-authored validators or user edits to a non-builtin set).\n+- Wire it into the existing init/install path. Per the active `mirdan-install` direction, prefer routing this through mirdan's Profile-driven installer rather than a bespoke deployer — reuse the skills-deployment mechanism (store + symlink/copy), do not invent a parallel one. Confirm the right home during implementation against the design doc.\n+- Builtin remains the lowest-precedence layer even when materialized on disk; user/project edits still win via the loader precedence.\n+- The validators doctor check is NOT here — it is the dedicated \"Review tool Doctorable\" task (`check validators` via the standard `Doctorable` trait into `sah doctor`). Do not add a separate one.\n\n ## Acceptance Criteria\n-- [ ] Init/install materializes the builtin validators under `$XDG_DATA_HOME/validators/` with the same structure as `builtin/validators/`.\n-- [ ] Re-running is idempotent: builtin-owned files are refreshed to current embedded content (overwritten); user-authored validators and user-created sets are untouched.\n-- [ ] The loader picks up the materialized builtin set; builtin → user → project precedence still holds.\n-- [ ] Deployment goes through the shared skills/agents installer (Profile entry), not a bespoke copy routine.\n+- [ ] Running init/install materializes the builtin validators under `~/.validators/` with the same structure as `builtin/validators/`.\n+- [ ] Re-running is idempotent: builtin-owned files are refreshed to current content; user-authored validators and user-created sets are untouched.\n+- [ ] The loader picks up the materialized builtin set and still applies builtin → user → project precedence.\n\n ## Tests\n-- [ ] Integration test (temp `XDG_DATA_HOME`): run the installer → assert `$XDG_DATA_HOME/validators/<set>/VALIDATOR.md` (+ rules) exist and match the embedded source.\n-- [ ] Idempotency test: install twice; a hand-edited builtin-deployed file is restored to embedded content (reference-copy policy); a user-authored validator added under the dir survives untouched.\n+- [ ] Integration test (temp HOME): run the installer → assert `~/.validators/code-quality/VALIDATOR.md` (and rules) exist and match the embedded source.\n+- [ ] Idempotency test: install twice; a user-added file under `~/.validators/` survives the second install; a hand-edited builtin file is restored (or preserved) per the chosen policy — assert the policy.\n - [ ] `cargo test` green for the owning crate.\n\n ## Workflow\n-- Use `/tdd` — write the temp-`XDG_DATA_HOME` deploy + idempotency tests first. REUSE the skills/agents deployment/store mechanism (mirdan Profile); do not write a second copy routine. Coordinate with the `mirdan-install` project so this is a Profile entry, not a one-off.\n\\ No newline at end of file\n+- Use `/tdd` — write the temp-HOME deploy + idempotency tests first. REUSE the existing skills deployment/store mechanism (see mirdan / workspace-init); do not write a second copy routine. Coordinate with the `mirdan-install` project so this lands as a Profile entry, not a one-off.\n\\ No newline at end of file\n","transaction_id":null}

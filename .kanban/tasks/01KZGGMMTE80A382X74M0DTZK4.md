---
assignees:
- claude-code
position_column: todo
position_ordinal: ffa080
title: 'validator findings: crates/claude-agent/src/agent_prompt_handling.rs (pre-existing)'
---
Pre-existing validator findings surfaced by `review file crates/claude-agent/src/agent_prompt_handling.rs` on 2026-08-08. None of these lines belong to card ^811xj0q (warm prefix reuse telemetry); that card added `fold_cache_usage`/`attach_cache_usage` and the scripted-CLI tests, whose own two findings stayed on ^811xj0q. Filed separately so ^811xj0q is not blocked by code it never touched.

Findings whose subject is refactoring test code that already existed were dropped per the review skill's standing exception.

## Review Findings (2026-08-08 05:27)

- [ ] `crates/claude-agent/src/agent_prompt_handling.rs:253` — Invalid params error mapping pattern repeated; 5-line `.map_err()` block at 253-258 follows same structure as reported decode errors at lines 1271, 1295 — part of 3-instance invalid_params duplication. Extract to helper function: `fn map_invalid_params_error(action: &str, description: &str) -> impl Fn(E) -> agent_client_protocol::Error` to unify all 3 invalid_params error handlers (lines 253, 1271, 1295).
- [ ] `crates/claude-agent/src/agent_prompt_handling.rs:295` — Session update error mapping pattern repeated 4 times; 5-line `.map_err()` block follows identical structure at lines 757, 859, 1446 differing only in action/description strings. Extract to helper function: `fn map_session_error(action: &str, description: &str) -> impl Fn(String) -> agent_client_protocol::Error` to eliminate 4 near-identical error handlers.
- [ ] `crates/claude-agent/src/agent_prompt_handling.rs:757` — Session update error mapping pattern (see finding at line 295 for details — part of 4-instance duplication). See extraction suggestion in finding at line 295.
- [ ] `crates/claude-agent/src/agent_prompt_handling.rs:809` — Stop reason mapping uses a match statement over string values that should be a data table. The arms match on known Claude stop_reason strings ("max_tokens", "end_turn") and differ only in the StopReason enum constant returned. This creates a single source of truth problem: if new stop reasons are added by Claude, both the match arms and the fallback logic must be maintained in parallel. Replace with a data-driven mapping: `const STOP_REASON_MAP: &[(StopReason, &str)] = &[(StopReason::MaxTokens, "max_tokens"), (StopReason::EndTurn, "end_turn")]`. Lookup the string in the table; default to EndTurn if not found. This isolates stop reason values in one place and makes adding new reasons a data change.
- [ ] `crates/claude-agent/src/agent_prompt_handling.rs:829` — Tool kind detection uses an if-chain over string patterns that should be a data table. Each arm differs only in the ToolKind constant returned, making this a case of parallel arms that should be unified into a map or slice of (patterns, kind) tuples. Maintainers must keep patterns in sync across arms when adding tool types. Replace the if-chain with a data structure: `const TOOL_PATTERNS: &[(ToolKind, &[&str])] = &[(ToolKind::Read, &["read"]), (ToolKind::Edit, &["write", "edit"]), ...]`. Iterate and match, or use a lazy_static/once_cell map for substring patterns. This centralizes the patterns in one place and makes adding new tool types a data change, not a code change.
- [ ] `crates/claude-agent/src/agent_prompt_handling.rs:859` — Session update error mapping pattern (see finding at line 295 for details — part of 4-instance duplication). See extraction suggestion in finding at line 295.
- [ ] `crates/claude-agent/src/agent_prompt_handling.rs:961` — Internal error mapping pattern repeated; 5-line `.map_err()` block at 961-966 follows same structure as reported session update errors — part of 7-instance duplication of identical pattern. Use the same extracted helper function for all 7 instances (see finding at line 341).
- [ ] `crates/claude-agent/src/agent_prompt_handling.rs:1243` — Lines 1243-1255 re-implement validation logic that already exists in the validate_streaming_content method (lines 225-242). The identical validation code appears twice: once extracted in validate_streaming_content, and once inlined in handle_non_streaming_prompt. The streaming path calls the extracted method; the non-streaming path duplicates its implementation instead of reusing. Replace lines 1243-1255 with a single call: `self.validate_streaming_content(session_id, request)?;`.
- [ ] `crates/claude-agent/src/agent_prompt_handling.rs:1271` — Decode error mapping pattern repeated; 5-line `.map_err()` block at 1271-1276 (image) duplicated at 1295-1300 (audio) differing only by 'image' vs 'audio'. Extract to helper function: `fn map_decode_error(content_type: &str) -> impl Fn(E) -> agent_client_protocol::Error` to handle both image and audio decoding errors uniformly.
- [ ] `crates/claude-agent/src/agent_prompt_handling.rs:1295` — Decode error mapping pattern (see finding at line 1271 for details — part of 2-instance duplication). See extraction suggestion in finding at line 1271.
- [ ] `crates/claude-agent/src/agent_prompt_handling.rs:1446` — Session update error mapping pattern (see finding at line 295 for details — part of 4-instance duplication). See extraction suggestion in finding at line 295.
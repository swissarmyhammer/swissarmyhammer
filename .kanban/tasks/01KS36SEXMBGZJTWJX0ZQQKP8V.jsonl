{"id":"01KS36SEXM340YV6R901RVQ8WX","timestamp":"2026-05-20T17:26:31.604818Z","op":"create","item_id":"01KS36SEXMBGZJTWJX0ZQQKP8V","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,34 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KS36QGEVVP064EKW0JDGD94B\n+position_column: todo\n+position_ordinal: '8980'\n+project: command-service\n+title: 'Builtin plugin: file/board/window commands (port file.yaml)'\n+---\n+## What\n+\n+Port `crates/swissarmyhammer-kanban/builtin/commands/file.yaml` (5 commands: `file.switchBoard`, `file.closeBoard`, `file.newBoard`, `file.openBoard`, `window.new`) to a builtin TypeScript plugin.\n+\n+Files:\n+- `builtin/plugins/file-commands/index.ts` — registers all 5\n+\n+These commands cross into the host shell (board file management, window creation), so several will route through the forthcoming `window` MCP server (defined in a later task) rather than the kanban server. That's fine — at registration time the plugin just declares the command; the callback target is decided at call time.\n+\n+Sequencing note: this plugin can register the commands before the `window` MCP server exists; the `execute` callbacks will fail at runtime if `window` isn't registered yet, but that's caught by the frontend e2e tests, not by the plugin-load tests. Ordering with the `window` server task is loose — the cut-over task gates the final user-visible behavior.\n+\n+## Acceptance Criteria\n+- [ ] `builtin/plugins/file-commands/` discoverable\n+- [ ] All 5 commands appear in `list command` with original metadata\n+- [ ] `execute` for each, when its downstream MCP server is registered, produces the same effect as today's YAML version\n+- [ ] Metadata (keys, scope, params, menu placement) matches the YAML baseline exactly\n+\n+## Tests\n+- [ ] `crates/swissarmyhammer-command-service/tests/integration/builtin_file_commands_e2e.rs` — load plugin; assert all 5 registered with metadata fidelity; execute `file.newBoard` (which only touches the store, no window needed) and assert the new board file appears\n+- [ ] Metadata-fidelity tests for each command\n+- [ ] `cargo test -p swissarmyhammer-command-service --test integration builtin_file_commands_e2e` passes\n+\n+## Workflow\n+- Use `/tdd`\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,34 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KS36QGEVVP064EKW0JDGD94B\n-position_column: todo\n-position_ordinal: '8980'\n-project: command-service\n-title: 'Builtin plugin: file/board/window commands (port file.yaml)'\n----\n-## What\n-\n-Port `crates/swissarmyhammer-kanban/builtin/commands/file.yaml` (5 commands: `file.switchBoard`, `file.closeBoard`, `file.newBoard`, `file.openBoard`, `window.new`) to a builtin TypeScript plugin.\n-\n-Files:\n-- `builtin/plugins/file-commands/index.ts` — registers all 5\n-\n-These commands cross into the host shell (board file management, window creation), so several will route through the forthcoming `window` MCP server (defined in a later task) rather than the kanban server. That's fine — at registration time the plugin just declares the command; the callback target is decided at call time.\n-\n-Sequencing note: this plugin can register the commands before the `window` MCP server exists; the `execute` callbacks will fail at runtime if `window` isn't registered yet, but that's caught by the frontend e2e tests, not by the plugin-load tests. Ordering with the `window` server task is loose — the cut-over task gates the final user-visible behavior.\n-\n-## Acceptance Criteria\n-- [ ] `builtin/plugins/file-commands/` discoverable\n-- [ ] All 5 commands appear in `list command` with original metadata\n-- [ ] `execute` for each, when its downstream MCP server is registered, produces the same effect as today's YAML version\n-- [ ] Metadata (keys, scope, params, menu placement) matches the YAML baseline exactly\n-\n-## Tests\n-- [ ] `crates/swissarmyhammer-command-service/tests/integration/builtin_file_commands_e2e.rs` — load plugin; assert all 5 registered with metadata fidelity; execute `file.newBoard` (which only touches the store, no window needed) and assert the new board file appears\n-- [ ] Metadata-fidelity tests for each command\n-- [ ] `cargo test -p swissarmyhammer-command-service --test integration builtin_file_commands_e2e` passes\n-\n-## Workflow\n-- Use `/tdd`\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KS377YZVKJ8G5F5X4ZBEFDRM","timestamp":"2026-05-20T17:34:26.811473Z","op":"update","item_id":"01KS36SEXMBGZJTWJX0ZQQKP8V","forward_patch":"--- original\n+++ modified\n@@ -6,27 +6,29 @@\n position_column: todo\n position_ordinal: '8980'\n project: command-service\n-title: 'Builtin plugin: file/board/window commands (port file.yaml)'\n+title: 'Builtin plugin: file/board commands (port file.yaml)'\n ---\n ## What\n\n-Port `crates/swissarmyhammer-kanban/builtin/commands/file.yaml` (5 commands: `file.switchBoard`, `file.closeBoard`, `file.newBoard`, `file.openBoard`, `window.new`) to a builtin TypeScript plugin.\n+Port `crates/swissarmyhammer-kanban/builtin/commands/file.yaml` (4 commands: `file.switchBoard`, `file.closeBoard`, `file.newBoard`, `file.openBoard`) to a builtin TypeScript plugin.\n+\n+(NOTE: `window.new` lives in `ui.yaml`, not `file.yaml` — it is handled by the ui-commands plugin task.)\n\n Files:\n-- `builtin/plugins/file-commands/index.ts` — registers all 5\n+- `builtin/plugins/file-commands/index.ts` — registers all 4\n\n-These commands cross into the host shell (board file management, window creation), so several will route through the forthcoming `window` MCP server (defined in a later task) rather than the kanban server. That's fine — at registration time the plugin just declares the command; the callback target is decided at call time.\n+These commands cross into the host shell (board file management), so several will route through the `window` MCP server (defined in a later task) for OS file dialogs, plus the kanban server for new-board creation. That's fine — at registration time the plugin just declares the command; the callback target is decided at call time.\n\n Sequencing note: this plugin can register the commands before the `window` MCP server exists; the `execute` callbacks will fail at runtime if `window` isn't registered yet, but that's caught by the frontend e2e tests, not by the plugin-load tests. Ordering with the `window` server task is loose — the cut-over task gates the final user-visible behavior.\n\n ## Acceptance Criteria\n - [ ] `builtin/plugins/file-commands/` discoverable\n-- [ ] All 5 commands appear in `list command` with original metadata\n+- [ ] All 4 commands appear in `list command` with original metadata\n - [ ] `execute` for each, when its downstream MCP server is registered, produces the same effect as today's YAML version\n - [ ] Metadata (keys, scope, params, menu placement) matches the YAML baseline exactly\n\n ## Tests\n-- [ ] `crates/swissarmyhammer-command-service/tests/integration/builtin_file_commands_e2e.rs` — load plugin; assert all 5 registered with metadata fidelity; execute `file.newBoard` (which only touches the store, no window needed) and assert the new board file appears\n+- [ ] `crates/swissarmyhammer-command-service/tests/integration/builtin_file_commands_e2e.rs` — load plugin; assert all 4 registered with metadata fidelity; execute `file.newBoard` (which only touches the store) and assert the new board file appears\n - [ ] Metadata-fidelity tests for each command\n - [ ] `cargo test -p swissarmyhammer-command-service --test integration builtin_file_commands_e2e` passes\n\n","reverse_patch":"--- original\n+++ modified\n@@ -6,29 +6,27 @@\n position_column: todo\n position_ordinal: '8980'\n project: command-service\n-title: 'Builtin plugin: file/board commands (port file.yaml)'\n+title: 'Builtin plugin: file/board/window commands (port file.yaml)'\n ---\n ## What\n\n-Port `crates/swissarmyhammer-kanban/builtin/commands/file.yaml` (4 commands: `file.switchBoard`, `file.closeBoard`, `file.newBoard`, `file.openBoard`) to a builtin TypeScript plugin.\n-\n-(NOTE: `window.new` lives in `ui.yaml`, not `file.yaml` — it is handled by the ui-commands plugin task.)\n+Port `crates/swissarmyhammer-kanban/builtin/commands/file.yaml` (5 commands: `file.switchBoard`, `file.closeBoard`, `file.newBoard`, `file.openBoard`, `window.new`) to a builtin TypeScript plugin.\n\n Files:\n-- `builtin/plugins/file-commands/index.ts` — registers all 4\n+- `builtin/plugins/file-commands/index.ts` — registers all 5\n\n-These commands cross into the host shell (board file management), so several will route through the `window` MCP server (defined in a later task) for OS file dialogs, plus the kanban server for new-board creation. That's fine — at registration time the plugin just declares the command; the callback target is decided at call time.\n+These commands cross into the host shell (board file management, window creation), so several will route through the forthcoming `window` MCP server (defined in a later task) rather than the kanban server. That's fine — at registration time the plugin just declares the command; the callback target is decided at call time.\n\n Sequencing note: this plugin can register the commands before the `window` MCP server exists; the `execute` callbacks will fail at runtime if `window` isn't registered yet, but that's caught by the frontend e2e tests, not by the plugin-load tests. Ordering with the `window` server task is loose — the cut-over task gates the final user-visible behavior.\n\n ## Acceptance Criteria\n - [ ] `builtin/plugins/file-commands/` discoverable\n-- [ ] All 4 commands appear in `list command` with original metadata\n+- [ ] All 5 commands appear in `list command` with original metadata\n - [ ] `execute` for each, when its downstream MCP server is registered, produces the same effect as today's YAML version\n - [ ] Metadata (keys, scope, params, menu placement) matches the YAML baseline exactly\n\n ## Tests\n-- [ ] `crates/swissarmyhammer-command-service/tests/integration/builtin_file_commands_e2e.rs` — load plugin; assert all 4 registered with metadata fidelity; execute `file.newBoard` (which only touches the store) and assert the new board file appears\n+- [ ] `crates/swissarmyhammer-command-service/tests/integration/builtin_file_commands_e2e.rs` — load plugin; assert all 5 registered with metadata fidelity; execute `file.newBoard` (which only touches the store, no window needed) and assert the new board file appears\n - [ ] Metadata-fidelity tests for each command\n - [ ] `cargo test -p swissarmyhammer-command-service --test integration builtin_file_commands_e2e` passes\n\n","transaction_id":null}
{"id":"01KS37DQ32D3891PVV6S9R7JKA","timestamp":"2026-05-20T17:37:35.330324Z","op":"update","item_id":"01KS36SEXMBGZJTWJX0ZQQKP8V","forward_patch":"--- original\n+++ modified\n@@ -15,7 +15,7 @@\n (NOTE: `window.new` lives in `ui.yaml`, not `file.yaml` — it is handled by the ui-commands plugin task.)\n\n Files:\n-- `builtin/plugins/file-commands/index.ts` — registers all 4\n+- `builtin/plugins/file-commands/index.ts` — entry; `load()` calls `ensureServices(this, [\"commands\", \"window\"])` then `registerCommands(this, [...])` with all 4 commands. The `window` service is needed for `file.openBoard` (OS file dialog).\n\n These commands cross into the host shell (board file management), so several will route through the `window` MCP server (defined in a later task) for OS file dialogs, plus the kanban server for new-board creation. That's fine — at registration time the plugin just declares the command; the callback target is decided at call time.\n\n@@ -26,6 +26,7 @@\n - [ ] All 4 commands appear in `list command` with original metadata\n - [ ] `execute` for each, when its downstream MCP server is registered, produces the same effect as today's YAML version\n - [ ] Metadata (keys, scope, params, menu placement) matches the YAML baseline exactly\n+- [ ] `load()` calls `ensureServices` before `registerCommands` (the convention)\n\n ## Tests\n - [ ] `crates/swissarmyhammer-command-service/tests/integration/builtin_file_commands_e2e.rs` — load plugin; assert all 4 registered with metadata fidelity; execute `file.newBoard` (which only touches the store) and assert the new board file appears\n","reverse_patch":"--- original\n+++ modified\n@@ -15,7 +15,7 @@\n (NOTE: `window.new` lives in `ui.yaml`, not `file.yaml` — it is handled by the ui-commands plugin task.)\n\n Files:\n-- `builtin/plugins/file-commands/index.ts` — entry; `load()` calls `ensureServices(this, [\"commands\", \"window\"])` then `registerCommands(this, [...])` with all 4 commands. The `window` service is needed for `file.openBoard` (OS file dialog).\n+- `builtin/plugins/file-commands/index.ts` — registers all 4\n\n These commands cross into the host shell (board file management), so several will route through the `window` MCP server (defined in a later task) for OS file dialogs, plus the kanban server for new-board creation. That's fine — at registration time the plugin just declares the command; the callback target is decided at call time.\n\n@@ -26,7 +26,6 @@\n - [ ] All 4 commands appear in `list command` with original metadata\n - [ ] `execute` for each, when its downstream MCP server is registered, produces the same effect as today's YAML version\n - [ ] Metadata (keys, scope, params, menu placement) matches the YAML baseline exactly\n-- [ ] `load()` calls `ensureServices` before `registerCommands` (the convention)\n\n ## Tests\n - [ ] `crates/swissarmyhammer-command-service/tests/integration/builtin_file_commands_e2e.rs` — load plugin; assert all 4 registered with metadata fidelity; execute `file.newBoard` (which only touches the store) and assert the new board file appears\n","transaction_id":null}
{"id":"01KS3BKK7RVQ327EESYCWB29D2","timestamp":"2026-05-20T18:50:42.296098Z","op":"update","item_id":"01KS36SEXMBGZJTWJX0ZQQKP8V","forward_patch":"--- original\n+++ modified\n@@ -3,6 +3,7 @@\n - claude-code\n depends_on:\n - 01KS36QGEVVP064EKW0JDGD94B\n+- 01KS3BK37R2P0RYSYSXGZTS0Z3\n position_column: todo\n position_ordinal: '8980'\n project: command-service\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,6 @@\n - claude-code\n depends_on:\n - 01KS36QGEVVP064EKW0JDGD94B\n-- 01KS3BK37R2P0RYSYSXGZTS0Z3\n position_column: todo\n position_ordinal: '8980'\n project: command-service\n","transaction_id":null}
{"id":"01KS5ED3VP7PWQH4YV0M63TCSR","timestamp":"2026-05-21T14:18:04.534183Z","op":"update","item_id":"01KS36SEXMBGZJTWJX0ZQQKP8V","forward_patch":"--- original\n+++ modified\n@@ -4,6 +4,7 @@\n depends_on:\n - 01KS36QGEVVP064EKW0JDGD94B\n - 01KS3BK37R2P0RYSYSXGZTS0Z3\n+- 01KS36VTN9K8C41P20SJ2WQA6X\n position_column: todo\n position_ordinal: '8980'\n project: command-service\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,6 @@\n depends_on:\n - 01KS36QGEVVP064EKW0JDGD94B\n - 01KS3BK37R2P0RYSYSXGZTS0Z3\n-- 01KS36VTN9K8C41P20SJ2WQA6X\n position_column: todo\n position_ordinal: '8980'\n project: command-service\n","transaction_id":null}
{"id":"01KS5GVKKDJWZACQV4C4WBWK1T","timestamp":"2026-05-21T15:00:56.557410Z","op":"update","item_id":"01KS36SEXMBGZJTWJX0ZQQKP8V","forward_patch":"--- original\n+++ modified\n@@ -7,7 +7,7 @@\n - 01KS36VTN9K8C41P20SJ2WQA6X\n position_column: todo\n position_ordinal: '8980'\n-project: command-service\n+project: builtin-commands\n title: 'Builtin plugin: file/board commands (port file.yaml)'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -7,7 +7,7 @@\n - 01KS36VTN9K8C41P20SJ2WQA6X\n position_column: todo\n position_ordinal: '8980'\n-project: builtin-commands\n+project: command-service\n title: 'Builtin plugin: file/board commands (port file.yaml)'\n ---\n ## What\n","transaction_id":null}
{"id":"01KS617RN89E87PEHEYYZGAVZQ","timestamp":"2026-05-21T19:47:12.168271Z","op":"update","item_id":"01KS36SEXMBGZJTWJX0ZQQKP8V","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KS36QGEVVP064EKW0JDGD94B\n - 01KS3BK37R2P0RYSYSXGZTS0Z3\n-- 01KS36VTN9K8C41P20SJ2WQA6X\n+- 01KS612DV4W0N1X1RPXWAKMT4B\n position_column: todo\n position_ordinal: '8980'\n project: builtin-commands\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KS36QGEVVP064EKW0JDGD94B\n - 01KS3BK37R2P0RYSYSXGZTS0Z3\n-- 01KS612DV4W0N1X1RPXWAKMT4B\n+- 01KS36VTN9K8C41P20SJ2WQA6X\n position_column: todo\n position_ordinal: '8980'\n project: builtin-commands\n","transaction_id":null}
{"id":"01KSR75YCCS0E6HCNEMKKPHP2J","timestamp":"2026-05-28T21:17:23.724230Z","op":"update","item_id":"01KS36SEXMBGZJTWJX0ZQQKP8V","forward_patch":"--- original\n+++ modified\n@@ -5,8 +5,8 @@\n - 01KS36QGEVVP064EKW0JDGD94B\n - 01KS3BK37R2P0RYSYSXGZTS0Z3\n - 01KS612DV4W0N1X1RPXWAKMT4B\n-position_column: todo\n-position_ordinal: '8980'\n+position_column: doing\n+position_ordinal: '8180'\n project: builtin-commands\n title: 'Builtin plugin: file/board commands (port file.yaml)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -5,8 +5,8 @@\n - 01KS36QGEVVP064EKW0JDGD94B\n - 01KS3BK37R2P0RYSYSXGZTS0Z3\n - 01KS612DV4W0N1X1RPXWAKMT4B\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: todo\n+position_ordinal: '8980'\n project: builtin-commands\n title: 'Builtin plugin: file/board commands (port file.yaml)'\n ---\n","transaction_id":null}
{"id":"01KSR7BSHMR9MTKVKEBWD2VDG4","timestamp":"2026-05-28T21:20:35.380733Z","op":"update","item_id":"01KS36SEXMBGZJTWJX0ZQQKP8V","forward_patch":"--- original\n+++ modified\n@@ -5,8 +5,8 @@\n - 01KS36QGEVVP064EKW0JDGD94B\n - 01KS3BK37R2P0RYSYSXGZTS0Z3\n - 01KS612DV4W0N1X1RPXWAKMT4B\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '80'\n project: builtin-commands\n title: 'Builtin plugin: file/board commands (port file.yaml)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -5,8 +5,8 @@\n - 01KS36QGEVVP064EKW0JDGD94B\n - 01KS3BK37R2P0RYSYSXGZTS0Z3\n - 01KS612DV4W0N1X1RPXWAKMT4B\n-position_column: review\n-position_ordinal: '80'\n+position_column: doing\n+position_ordinal: '8180'\n project: builtin-commands\n title: 'Builtin plugin: file/board commands (port file.yaml)'\n ---\n","transaction_id":null}

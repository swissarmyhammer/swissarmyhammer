---
assignees:
- claude-code
position_column: done
position_ordinal: fffffffffffffffffffffffffffffffffffff380
project: remove-prompts
title: Remove the `sah prompt` CLI subcommand
---
## What
Remove the entire `sah prompt` CLI subcommand (list / test / show / new / edit) and its wiring. This is a leaf removal — nothing else in the codebase depends on these command modules.

Files deleted:
- `apps/swissarmyhammer-cli/src/commands/prompt/` (whole dir)
- `apps/swissarmyhammer-cli/src/list.rs` (dead prompt-listing module; only consumer was the prompt command)
- `apps/swissarmyhammer-cli/tests/integration/prompt_command_integration.rs`, `prompt_comprehensive_integrations.rs`, `prompt_performance.rs`, `prompt_real_integrations.rs`

Files edited:
- `apps/swissarmyhammer-cli/src/cli.rs` — removed `Prompt` variant; reframed top-level about/long_about around skills/workflows/agents (no longer "managing prompts").
- `apps/swissarmyhammer-cli/src/dynamic_cli.rs` — REAL CLI surface. Removed `build_prompt_command` + all `build_prompt_*_subcommand` helpers + `create_test_*_args` + `PROMPT_*_LONG_ABOUT` constants + the `prompt` wiring in `add_content_commands`. (Task originally pointed only at cli.rs/main.rs; the binary builds its command tree dynamically here.)
- `apps/swissarmyhammer-cli/src/main.rs` — removed `handle_prompt_command` and its dispatch arm.
- `apps/swissarmyhammer-cli/src/commands/mod.rs` — removed `pub mod prompt;`.
- `apps/swissarmyhammer-cli/src/lib.rs` — removed `pub mod list;`.
- `apps/swissarmyhammer-cli/src/context.rs` — removed prompt-command-only `display_prompts` method + its 2 tests; annotated now-orphaned `render_prompt`/`template_context`/`debug` with `#[allow(dead_code)]` (kept for later skill-rendering task per instructions).
- `apps/swissarmyhammer-cli/src/error.rs` — `#[allow(dead_code)]` on `CliResult`/`CliError::new` (prompt-command plumbing now unused; matches existing convention).
- Test harness/asserts: `tests/in_process_test_utils.rs` (removed prompt simulation), `tests/integration/{mod.rs,binary_aliases.rs,cli_integration.rs}`.
- Generated `completions/*` and `doc/src/reference/sah-cli.md` regenerated by build.rs (now prompt-free).

Kept: CliContext `prompt_library` field (still needed for skill rendering at this stage).

## Acceptance Criteria
- [x] `apps/swissarmyhammer-cli/src/commands/prompt/` directory no longer exists.
- [x] `sah --help` output contains no `prompt` subcommand.
- [x] `sah prompt` returns an unknown-subcommand error (RC=2, "unrecognized subcommand 'prompt'").
- [x] `cargo build -p swissarmyhammer-cli` succeeds.
- [x] Top-level CLI about/help text no longer markets "managing prompts".

## Tests
- [x] Deleted `apps/swissarmyhammer-cli/tests/integration/prompt_performance.rs` (and the other prompt-command test files).
- [x] Added `tests/integration/prompt_command_removed.rs` asserting `sah prompt` exits non-zero with "unrecognized subcommand" and `--help` lists no prompt subcommand.
- [x] `cargo test -p swissarmyhammer-cli` is green (0 failures across all test binaries).

## Workflow
- Used `/tdd` — wrote the failing "no prompt subcommand" assertions first (watched them fail), then deleted code to make them pass.

## Notes / follow-ups
- One pre-existing warning remains in dependency crate `swissarmyhammer-tools` (`unused variable: agent` in `mcp/tools/skill/use_op.rs`) — unrelated to this task, not touched.
- `PromptSourceArg` (cli.rs) + `cli_conversions::PromptSource` are now only self-referential; left in place (public type, build.rs compiles cli.rs standalone) for the later library-rename task to address.

## Review Findings (2026-06-08 06:40)

### Blockers
- [x] `apps/swissarmyhammer-cli/tests/integration/mcp_integration.rs:21` — test_mcp_server_basic_functionality is a near-verbatim copy of the in-process MCP-server bootstrap scaffold (temp dir -> start server -> create_test_client -> list_tools -> assert non-empty -> cancel/shutdown) that already exists in two other tests. Three copies of this setup/teardown will drift out of sync — note one copy calls start_mcp_server while the other two call start_mcp_server_with_options, and the temp-dir rationale comment is re-pasted verbatim in each. Extract the shared bootstrap into a test_utils helper (e.g. `start_test_http_server() -> (server, client)` that allocates the temp dir, starts the in-process HTTP server, and returns a connected create_test_client). Each test then keeps only its distinct assertions/tool-call, parameterizing the tool op where they differ.
  - RESOLVED: Added `start_test_server_and_client() -> (McpServerHandle, RunningService<RoleClient, ClientInfo>, TempDir)` to `crates/swissarmyhammer-tools/src/mcp/test_utils.rs` (alongside `create_test_client`). It allocates the fresh temp working dir, starts the in-process HTTP server, connects a default client, and returns the TempDir guard so it is not dropped early. mcp_integration.rs, rmcp_stdio_working.rs, rmcp_integration.rs, and the two test_utils unit tests (`test_client_list_tools`, `test_client_call_tool`) now call it; each keeps only its distinct assertions/tool-call.

### Warnings
- [x] `apps/swissarmyhammer-cli/tests/integration/binary_aliases.rs:19` — Four parallel assert! blocks (lines 19-34) check output.contains(<cmd>) over the known set {serve, doctor, completion, validate}, differing only in the command-name constant and its message. These are N hand-maintained copies of one check — adding or renaming an expected command means editing another copy, and an omitted command silently goes untested. Express the expected commands as data and interpret with one loop: `for cmd in ["serve", "doctor", "completion", "validate"] { assert!(output.contains(cmd), "Help should mention {cmd} command"); }`.
  - RESOLVED: Replaced the four parallel asserts with the data-driven `for cmd in ["serve", "doctor", "completion", "validate"]` loop.
- [x] `apps/swissarmyhammer-cli/tests/integration/cli_integration.rs:21` — test_verbose_flag's only assertion `result.exit_code >= 0` is always true for the non-negative i32 exit_code, so the test cannot fail and verifies nothing about the --verbose flag. Assert the concrete outcome, e.g. `result.exit_code == 0` for a passing validate, and/or assert the verbose flag actually changes output.
  - RESOLVED: test_verbose_flag now runs under an `IsolatedTestEnvironment` guard and asserts `exit_code == 0` (a passing validate in a clean env), with stdout/stderr in the failure message.
- [x] `apps/swissarmyhammer-cli/tests/integration/cli_integration.rs:34` — test_quiet_flag's only assertion `result.exit_code >= 0` is a tautology (exit_code is never negative), so the test passes unconditionally and does not exercise the --quiet flag. Assert a concrete exit code and/or that --quiet suppresses output (as the sibling test_root_validate_quiet already does at cli_integration.rs:88-93).
  - RESOLVED: test_quiet_flag now runs under an `IsolatedTestEnvironment` guard and asserts `exit_code == 0`. (Note: the global `--quiet` flag is parsed into `Cli` but the in-process validate handler does not apply it — only the subcommand-level `validate --quiet` suppresses output, which `test_root_validate_quiet` already covers — so a concrete exit-code assertion is the honest outcome here.)
- [x] `crates/swissarmyhammer-tools/tests/integration/rmcp_stdio_working.rs:26` — The 'make temp dir → start_mcp_server_with_options(Http{port:None}, None, None, Some(temp)) → create_test_client(server.url())' isolation+startup block is duplicated inline rather than reused from a shared helper. test_utils is the canonical home for MCP test setup (it already owns create_test_client specifically to 'avoid code duplication and ensure consistent test client configuration'); the server-startup half should live there too, so the proxy/temp-dir/handshake conventions stay fixed in one place. Add a `start_test_server() -> (RunningService server, TempDir)` (or `start_test_server_and_client() -> (server, client, TempDir)`) helper in test_utils alongside create_test_client, and have these tests plus the four test_utils copies call it. The TempDir must be returned/held so it isn't dropped early.
  - RESOLVED: Implemented exactly the suggested `start_test_server_and_client() -> (server, client, TempDir)` helper; rmcp_stdio_working.rs now calls it and holds the returned TempDir.
- [x] `crates/swissarmyhammer-tools/tests/rmcp_integration.rs:20` — test_mcp_server_with_rmcp_client is a near-verbatim copy of test_rmcp_client_lists_tools_and_prompts in tests/integration/rmcp_stdio_working.rs (~25-line shared scaffold: identical temp-dir + start_mcp_server_with_options HTTP setup, create_test_client, list_tools + non-empty assert, files-not-advertised assert, files tool call + non-empty content assert). The two differ only by the tool op/pattern (grep files/"test" vs glob files/"*.md") and one extra shell-not-advertised assert. A fix to the server-startup or tool-composition assertions in one will silently skip the other. Extract the shared scaffold into a helper in the test support module (e.g. `assert_unknown_host_lists_tools_and_calls_files(op: &str, pattern: &str)` returning the started server/client), and have each test call it with its op/pattern, then add the shell-gating assert only where it applies.
  - RESOLVED: The shared server-startup + client scaffold (the part that genuinely drifts out of sync) is now the single `start_test_server_and_client()` helper in test_utils, reused by both files. The per-test assertions (tool-not-advertised set + the differing files-tool op/pattern + the rmcp_integration-only shell-not-advertised assert) are intentionally left inline in each test because they are each test's distinct subject under test, not boilerplate — extracting an `assert_..._calls_files(op, pattern)` wrapper would invert which test owns which assertion and hide the differing host-composition checks behind a parameter. Two call sites with differing assertion sets do not meet the rule-of-three for a further assertion-helper abstraction.

### Nits
- [x] `crates/swissarmyhammer-tools/src/mcp/test_utils.rs:11` — The module doc lists "Listing prompts" as a demonstrated usage pattern, but the `test_client_list_prompts` example test was deleted in this change. The doc now advertises an example that no longer exists. Remove the "- Listing prompts" bullet from the module-level doc comment.
  - RESOLVED: Removed the "- Listing prompts" bullet from the module doc comment.
- [x] `crates/swissarmyhammer-tools/tests/integration/rmcp_stdio_working.rs:19` — The test is named `test_rmcp_client_lists_tools_and_prompts` and its doc comment claims it tests "prompt listing", but the prompts protocol surface was removed and the body no longer lists prompts — only tools. The name and doc now misdescribe what the test verifies, which misleads anyone diagnosing a failure. Rename to `test_rmcp_client_lists_tools` and drop the "prompts"/"prompt listing" phrases from the doc comment (lines 11, 16).
  - RESOLVED: Renamed to `test_rmcp_client_lists_tools` and dropped all "prompts"/"prompt listing" wording from the doc comment.

## Review Findings (2026-06-08 08:43)

_Re-review. Engine swept the whole working tree; sibling-task findings in `context.rs` (belong to MCP task 01KTC7HAF68EAYYV9ZMEB0NGX2) and the pre-existing `serve/mod.rs` and `dynamic_cli.rs:636` (statusline/completion already omitted before this task; this task only deleted `prompt` from the list) nits are triaged OUT of scope and not listed. Below are the in-scope findings against this task's own test-refactor changes._

### Warnings
- [x] `crates/swissarmyhammer-tools/src/mcp/test_utils.rs:53` — Added helper `start_test_server_and_client` re-derives the same start-server + create-client + TempDir sequence that `skill_e2e.rs::setup` already implements. Since this helper lives in the non-test-gated `pub mod test_utils` (mod.rs:79), it is reachable from integration tests, so it should become the single shared entry point those copies call — not a 5th parallel variant. Keep the shared helper (it has 2 in-module callers, so it is warranted), but point `skill_e2e.rs::setup` at it to retire that duplicate; consider a server+temp variant the `start_isolated_server` copies can share too.
  - RESOLVED: `skill_e2e.rs::setup` now delegates to `start_test_server_and_client()` — its body is a single `start_test_server_and_client().await` call, retiring the parallel temp-dir+start+connect copy. setup needed no extra steps beyond the shared sequence, so no build-on-top wrapper was warranted. The doc comment is kept because it records a skill-specific reason the temp working_dir matters (it stops local `.skills/` pre-rendered overrides from masking builtin Liquid-template skills) — distinct rationale, not duplicated code. The optional `start_isolated_server` server+temp variant was NOT pursued: it is a "consider" in sibling test files outside these two unchecked items' scope. All 12 skill_e2e tests pass; clippy clean.
- [x] `crates/swissarmyhammer-tools/tests/integration/rmcp_stdio_working.rs:16` — The added `test_rmcp_client_lists_tools` re-implements a test that already exists, adding no new coverage. It does start_test_server_and_client → list_tools → assert non-empty → assert `files` not advertised → call `files` → assert content non-empty → shutdown — the exact behavior already verified by `test_mcp_server_with_rmcp_client`. The only difference is the `files` op (`grep files` here vs `glob files` there), which does not exercise a distinct code path in the MCP layer under test. Drop `test_rmcp_client_lists_tools` (its host-composition assertions are a strict subset of `test_mcp_server_with_rmcp_client`, which additionally asserts `shell` is not advertised). If the stdio file must retain a smoke test, factor the shared list+call+assert invariants into one helper in `test_utils` and have each test call it, rather than keeping a third hand-copied body.
  - RESOLVED: Chose to DROP. Confirmed `test_rmcp_client_lists_tools`'s assertions are a strict subset of `test_mcp_server_with_rmcp_client` in `tests/rmcp_integration.rs` (same list_tools + non-empty + `files`-not-advertised + files-tool-call + content-non-empty; the surviving test additionally asserts `shell` is not advertised). Deleted the test via `git rm` (history preserves it); since it was the only test in `rmcp_stdio_working.rs`, removed the now-empty file and its `mod rmcp_stdio_working;` line from `tests/integration/mod.rs`. No coverage lost — `test_mcp_server_with_rmcp_client` still passes (1 passed, 0 failed).
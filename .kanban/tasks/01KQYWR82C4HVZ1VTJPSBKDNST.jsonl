{"id":"01KQYWR82C55Z7S0MQ8VQ856SR","timestamp":"2026-05-06T14:58:26.508863Z","op":"create","item_id":"01KQYWR82C4HVZ1VTJPSBKDNST","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,62 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: e180\n+project: keyboard-navigation\n+title: Sneak code generator (prefix-free, ergonomic alphabet)\n+---\n+## What\n+\n+Pure utility module that produces short, prefix-free key codes for the Jump-To overlay — the same idea as vim-sneak/jumpy/AceJump labels.\n+\n+Create `kanban-app/ui/src/lib/sneak-codes.ts` exporting:\n+\n+```ts\n+/**\n+ * Generate `count` distinct key codes such that no code is a prefix of another.\n+ * Codes are drawn from an ergonomic alphabet biased toward home-row keys.\n+ * Returns codes ordered by ergonomic priority (shortest + easiest first).\n+ */\n+export function generateSneakCodes(count: number): string[];\n+\n+/** The alphabet used, exposed for tests. */\n+export const SNEAK_ALPHABET: readonly string[];\n+```\n+\n+Algorithm:\n+\n+1. Alphabet (in this priority order — home row first, then top row, then bottom row, skipping easily-confused keys): `[\"a\",\"s\",\"d\",\"f\",\"j\",\"k\",\"l\",\"g\",\"h\",\"w\",\"e\",\"r\",\"u\",\"i\",\"o\",\"p\",\"q\",\"t\",\"y\",\"z\",\"x\",\"c\",\"v\",\"n\",\"m\",\"b\"]` (26 letters minus none, but you can drop `i` if it visually conflicts with `1`/`l` — pick a final list and document the choice).\n+2. If `count <= alphabet.length`, return single-letter codes from the front of the alphabet.\n+3. If `count > alphabet.length`: split codes into two-letter prefixes. Reserve the last K letters of the alphabet as \"two-letter prefixes\": for each prefix letter X, codes `X+a`, `X+s`, `X+d`, ... become valid two-letter codes. Single-letter codes use the first `alphabet.length - K` letters; two-letter codes use combinations starting with the last K letters. Choose K to fit `count` codes (smallest K such that `(alphabet.length - K) + K * alphabet.length >= count`).\n+4. The resulting set is prefix-free by construction: a single-letter code never starts a two-letter code (different prefix bucket).\n+5. Codes are case-insensitive in matching but emit lowercase. The overlay handles case folding when matching keystrokes.\n+\n+Edge cases:\n+- `count === 0` → return `[]`.\n+- `count === 1` → return `[\"a\"]`.\n+- `count > alphabet.length ** 2` (more than ~676 targets) → throw an `Error(\"too many jump targets\")`. The Jump-To overlay caller should never hit this; if it does, the bug is upstream.\n+\n+This is pure logic — no DOM, no React, no async.\n+\n+## Acceptance Criteria\n+\n+- [ ] `generateSneakCodes(0).length === 0`.\n+- [ ] `generateSneakCodes(N)` returns N distinct strings for any `1 <= N <= 676`.\n+- [ ] No element of the returned array is a prefix of any other element.\n+- [ ] Codes for small N use the home-row letters first.\n+- [ ] `generateSneakCodes(N+1)` throws when N is the maximum capacity.\n+\n+## Tests\n+\n+- [ ] New test `kanban-app/ui/src/lib/sneak-codes.test.ts`:\n+  - `generates_empty_for_zero_count`\n+  - `generates_distinct_codes` — for N in [1, 5, 10, 26, 50, 200, 600], every pair is distinct.\n+  - `prefix_free_invariant` — for the same range of N, no code is a prefix of any other (use a brute-force check: for each pair, neither `startsWith` the other).\n+  - `single_letter_codes_use_home_row_first` — for N=4, returned codes are `[\"a\",\"s\",\"d\",\"f\"]` (or your chosen first four).\n+  - `throws_when_count_exceeds_capacity` — `generateSneakCodes(677)` throws.\n+- [ ] Test command: `cd kanban-app/ui && pnpm test sneak-codes` — passes.\n+\n+## Workflow\n+\n+- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,62 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: e180\n-project: keyboard-navigation\n-title: Sneak code generator (prefix-free, ergonomic alphabet)\n----\n-## What\n-\n-Pure utility module that produces short, prefix-free key codes for the Jump-To overlay — the same idea as vim-sneak/jumpy/AceJump labels.\n-\n-Create `kanban-app/ui/src/lib/sneak-codes.ts` exporting:\n-\n-```ts\n-/**\n- * Generate `count` distinct key codes such that no code is a prefix of another.\n- * Codes are drawn from an ergonomic alphabet biased toward home-row keys.\n- * Returns codes ordered by ergonomic priority (shortest + easiest first).\n- */\n-export function generateSneakCodes(count: number): string[];\n-\n-/** The alphabet used, exposed for tests. */\n-export const SNEAK_ALPHABET: readonly string[];\n-```\n-\n-Algorithm:\n-\n-1. Alphabet (in this priority order — home row first, then top row, then bottom row, skipping easily-confused keys): `[\"a\",\"s\",\"d\",\"f\",\"j\",\"k\",\"l\",\"g\",\"h\",\"w\",\"e\",\"r\",\"u\",\"i\",\"o\",\"p\",\"q\",\"t\",\"y\",\"z\",\"x\",\"c\",\"v\",\"n\",\"m\",\"b\"]` (26 letters minus none, but you can drop `i` if it visually conflicts with `1`/`l` — pick a final list and document the choice).\n-2. If `count <= alphabet.length`, return single-letter codes from the front of the alphabet.\n-3. If `count > alphabet.length`: split codes into two-letter prefixes. Reserve the last K letters of the alphabet as \"two-letter prefixes\": for each prefix letter X, codes `X+a`, `X+s`, `X+d`, ... become valid two-letter codes. Single-letter codes use the first `alphabet.length - K` letters; two-letter codes use combinations starting with the last K letters. Choose K to fit `count` codes (smallest K such that `(alphabet.length - K) + K * alphabet.length >= count`).\n-4. The resulting set is prefix-free by construction: a single-letter code never starts a two-letter code (different prefix bucket).\n-5. Codes are case-insensitive in matching but emit lowercase. The overlay handles case folding when matching keystrokes.\n-\n-Edge cases:\n-- `count === 0` → return `[]`.\n-- `count === 1` → return `[\"a\"]`.\n-- `count > alphabet.length ** 2` (more than ~676 targets) → throw an `Error(\"too many jump targets\")`. The Jump-To overlay caller should never hit this; if it does, the bug is upstream.\n-\n-This is pure logic — no DOM, no React, no async.\n-\n-## Acceptance Criteria\n-\n-- [ ] `generateSneakCodes(0).length === 0`.\n-- [ ] `generateSneakCodes(N)` returns N distinct strings for any `1 <= N <= 676`.\n-- [ ] No element of the returned array is a prefix of any other element.\n-- [ ] Codes for small N use the home-row letters first.\n-- [ ] `generateSneakCodes(N+1)` throws when N is the maximum capacity.\n-\n-## Tests\n-\n-- [ ] New test `kanban-app/ui/src/lib/sneak-codes.test.ts`:\n-  - `generates_empty_for_zero_count`\n-  - `generates_distinct_codes` — for N in [1, 5, 10, 26, 50, 200, 600], every pair is distinct.\n-  - `prefix_free_invariant` — for the same range of N, no code is a prefix of any other (use a brute-force check: for each pair, neither `startsWith` the other).\n-  - `single_letter_codes_use_home_row_first` — for N=4, returned codes are `[\"a\",\"s\",\"d\",\"f\"]` (or your chosen first four).\n-  - `throws_when_count_exceeds_capacity` — `generateSneakCodes(677)` throws.\n-- [ ] Test command: `cd kanban-app/ui && pnpm test sneak-codes` — passes.\n-\n-## Workflow\n-\n-- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQYWR82DM8RBTV2TGDK0Z3E4","timestamp":"2026-05-06T14:58:26.509675Z","entity_type":"task","entity_id":"01KQYWR82C4HVZ1VTJPSBKDNST","op":"create","changes":[["body",{"kind":"set","value":"## What\n\nPure utility module that produces short, prefix-free key codes for the Jump-To overlay — the same idea as vim-sneak/jumpy/AceJump labels.\n\nCreate `kanban-app/ui/src/lib/sneak-codes.ts` exporting:\n\n```ts\n/**\n * Generate `count` distinct key codes such that no code is a prefix of another.\n * Codes are drawn from an ergonomic alphabet biased toward home-row keys.\n * Returns codes ordered by ergonomic priority (shortest + easiest first).\n */\nexport function generateSneakCodes(count: number): string[];\n\n/** The alphabet used, exposed for tests. */\nexport const SNEAK_ALPHABET: readonly string[];\n```\n\nAlgorithm:\n\n1. Alphabet (in this priority order — home row first, then top row, then bottom row, skipping easily-confused keys): `[\"a\",\"s\",\"d\",\"f\",\"j\",\"k\",\"l\",\"g\",\"h\",\"w\",\"e\",\"r\",\"u\",\"i\",\"o\",\"p\",\"q\",\"t\",\"y\",\"z\",\"x\",\"c\",\"v\",\"n\",\"m\",\"b\"]` (26 letters minus none, but you can drop `i` if it visually conflicts with `1`/`l` — pick a final list and document the choice).\n2. If `count <= alphabet.length`, return single-letter codes from the front of the alphabet.\n3. If `count > alphabet.length`: split codes into two-letter prefixes. Reserve the last K letters of the alphabet as \"two-letter prefixes\": for each prefix letter X, codes `X+a`, `X+s`, `X+d`, ... become valid two-letter codes. Single-letter codes use the first `alphabet.length - K` letters; two-letter codes use combinations starting with the last K letters. Choose K to fit `count` codes (smallest K such that `(alphabet.length - K) + K * alphabet.length >= count`).\n4. The resulting set is prefix-free by construction: a single-letter code never starts a two-letter code (different prefix bucket).\n5. Codes are case-insensitive in matching but emit lowercase. The overlay handles case folding when matching keystrokes.\n\nEdge cases:\n- `count === 0` → return `[]`.\n- `count === 1` → return `[\"a\"]`.\n- `count > alphabet.length ** 2` (more than ~676 targets) → throw an `Error(\"too many jump targets\")`. The Jump-To overlay caller should never hit this; if it does, the bug is upstream.\n\nThis is pure logic — no DOM, no React, no async.\n\n## Acceptance Criteria\n\n- [ ] `generateSneakCodes(0).length === 0`.\n- [ ] `generateSneakCodes(N)` returns N distinct strings for any `1 <= N <= 676`.\n- [ ] No element of the returned array is a prefix of any other element.\n- [ ] Codes for small N use the home-row letters first.\n- [ ] `generateSneakCodes(N+1)` throws when N is the maximum capacity.\n\n## Tests\n\n- [ ] New test `kanban-app/ui/src/lib/sneak-codes.test.ts`:\n  - `generates_empty_for_zero_count`\n  - `generates_distinct_codes` — for N in [1, 5, 10, 26, 50, 200, 600], every pair is distinct.\n  - `prefix_free_invariant` — for the same range of N, no code is a prefix of any other (use a brute-force check: for each pair, neither `startsWith` the other).\n  - `single_letter_codes_use_home_row_first` — for N=4, returned codes are `[\"a\",\"s\",\"d\",\"f\"]` (or your chosen first four).\n  - `throws_when_count_exceeds_capacity` — `generateSneakCodes(677)` throws.\n- [ ] Test command: `cd kanban-app/ui && pnpm test sneak-codes` — passes.\n\n## Workflow\n\n- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run."}],["position_column",{"kind":"set","value":"todo"}],["title",{"kind":"set","value":"Sneak code generator (prefix-free, ergonomic alphabet)"}],["position_ordinal",{"kind":"set","value":"e180"}],["assignees",{"kind":"set","value":["claude-code"]}],["project",{"kind":"set","value":"keyboard-navigation"}]]}
{"id":"01KQYWRBKZHMWBY5ME7886ZT6G","timestamp":"2026-05-06T14:58:30.143525Z","op":"update","item_id":"01KQYWR82C4HVZ1VTJPSBKDNST","forward_patch":"--- original\n+++ modified\n@@ -59,4 +59,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run.\n\\ No newline at end of file\n+- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run. #nav-jump\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -59,4 +59,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run. #nav-jump\n\\ No newline at end of file\n+- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQYWRBKZTBJCMG6Q51V86CMF","timestamp":"2026-05-06T14:58:30.143931Z","entity_type":"task","entity_id":"01KQYWR82C4HVZ1VTJPSBKDNST","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -51,4 +51,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run.\n\\ No newline at end of file\n+- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run. #nav-jump\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -51,4 +51,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run. #nav-jump\n\\ No newline at end of file\n+- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run.\n\\ No newline at end of file\n"}]]}
{"id":"01KQZ3K3GWN5T1KYWJAF1JTAST","timestamp":"2026-05-06T16:57:58.044306Z","op":"update","item_id":"01KQYWR82C4HVZ1VTJPSBKDNST","forward_patch":"--- original\n+++ modified\n@@ -4,59 +4,101 @@\n position_column: todo\n position_ordinal: e180\n project: keyboard-navigation\n-title: Sneak code generator (prefix-free, ergonomic alphabet)\n+title: Sneak code generator in swissarmyhammer-focus (pure Rust + Tauri command)\n ---\n ## What\n\n-Pure utility module that produces short, prefix-free key codes for the Jump-To overlay — the same idea as vim-sneak/jumpy/AceJump labels.\n+Pure algorithm that produces short, prefix-free key codes for the Jump-To overlay — vim-sneak / jumpy / AceJump labels. Lives in **`swissarmyhammer-focus`** as Rust; exposed to the React frontend via a Tauri command in `kanban-app/src/commands.rs`.\n\n-Create `kanban-app/ui/src/lib/sneak-codes.ts` exporting:\n+Architectural fit: spatial-nav vocabulary is Rust-authoritative in this workspace (`Direction`, `FullyQualifiedMoniker`, etc. are defined in Rust and mirrored in TypeScript). The sneak code algorithm is generic spatial-nav infrastructure with no domain or UI knowledge — putting it in `swissarmyhammer-focus` keeps the layering consistent and means any future consumer (mirdan-app) gets it via dep, not by copy-pasting TypeScript.\n\n-```ts\n-/**\n- * Generate `count` distinct key codes such that no code is a prefix of another.\n- * Codes are drawn from an ergonomic alphabet biased toward home-row keys.\n- * Returns codes ordered by ergonomic priority (shortest + easiest first).\n- */\n-export function generateSneakCodes(count: number): string[];\n+### Steps\n+\n+1. **Create `swissarmyhammer-focus/src/sneak.rs`** with:\n+\n+   ```rust\n+   /// Ergonomic alphabet, ordered by priority — home row first, then top\n+   /// row, then bottom row. Skips letters with high visual confusion\n+   /// (`i`/`1`/`l`, `o`/`0`).\n+   pub const SNEAK_ALPHABET: &[char] = &[\n+       'a','s','d','f','j','k','g','h','w','e','r','u',\n+       'p','q','t','y','z','x','c','v','n','m','b',\n+   ]; // pick a final 23-letter list; document the choices\n+\n+   /// Generate `count` distinct, prefix-free key codes drawn from\n+   /// [`SNEAK_ALPHABET`]. Codes are ordered by ergonomic priority\n+   /// (shortest + easiest first). Returned codes are lowercase strings.\n+   ///\n+   /// # Errors\n+   ///\n+   /// Returns an error when `count > SNEAK_ALPHABET.len().pow(2)` —\n+   /// today the cap is around 529, well above any realistic Jump-To\n+   /// target count. Hitting it indicates an upstream bug.\n+   pub fn generate_sneak_codes(count: usize) -> Result<Vec<String>, SneakError>;\n+\n+   #[derive(Debug, thiserror::Error)]\n+   pub enum SneakError {\n+       #[error(\"too many jump targets: {0} exceeds capacity {1}\")]\n+       TooManyTargets(usize, usize),\n+   }\n+   ```\n+\n+   Algorithm:\n+   - `count == 0` → empty vec.\n+   - If `count <= alphabet.len()` → single-letter codes from the front of the alphabet.\n+   - Else split codes into two-letter prefixes. Reserve the last `K` letters of the alphabet as \"two-letter prefixes\": for each prefix letter `X`, codes `Xa`, `Xs`, `Xd`, ... become valid two-letter codes. Single-letter codes use the first `alphabet.len() - K` letters; two-letter codes use combinations starting with the last `K` letters. Choose `K` to fit `count` codes (smallest `K` such that `(alphabet.len() - K) + K * alphabet.len() >= count`).\n+   - Prefix-free by construction: a single-letter code never starts a two-letter code (different prefix bucket).\n+   - Pure logic; no I/O, no `unsafe`, no dependencies beyond `thiserror`.\n+\n+2. **Expose via lib.rs**: `pub mod sneak; pub use sneak::{generate_sneak_codes, SNEAK_ALPHABET, SneakError};`\n+\n+3. **Tauri command in `kanban-app/src/commands.rs`**:\n+\n+   ```rust\n+   #[tauri::command]\n+   pub fn generate_jump_codes(count: usize) -> Result<Vec<String>, String> {\n+       swissarmyhammer_focus::generate_sneak_codes(count)\n+           .map_err(|e| e.to_string())\n+   }\n+   ```\n\n-/** The alphabet used, exposed for tests. */\n-export const SNEAK_ALPHABET: readonly string[];\n-```\n+   Register it in the Tauri builder alongside the other commands. Read `kanban-app/src/commands.rs:173+` for the existing `#[tauri::command]` registration pattern and the warning banner at the top of the file about state-mutating commands. Sneak code generation is pure (no state mutation), so it's a clean addition.\n\n-Algorithm:\n+4. **Frontend bindings**: in `kanban-app/ui/src/lib/sneak-codes.ts` (a thin wrapper, not the algorithm itself):\n\n-1. Alphabet (in this priority order — home row first, then top row, then bottom row, skipping easily-confused keys): `[\"a\",\"s\",\"d\",\"f\",\"j\",\"k\",\"l\",\"g\",\"h\",\"w\",\"e\",\"r\",\"u\",\"i\",\"o\",\"p\",\"q\",\"t\",\"y\",\"z\",\"x\",\"c\",\"v\",\"n\",\"m\",\"b\"]` (26 letters minus none, but you can drop `i` if it visually conflicts with `1`/`l` — pick a final list and document the choice).\n-2. If `count <= alphabet.length`, return single-letter codes from the front of the alphabet.\n-3. If `count > alphabet.length`: split codes into two-letter prefixes. Reserve the last K letters of the alphabet as \"two-letter prefixes\": for each prefix letter X, codes `X+a`, `X+s`, `X+d`, ... become valid two-letter codes. Single-letter codes use the first `alphabet.length - K` letters; two-letter codes use combinations starting with the last K letters. Choose K to fit `count` codes (smallest K such that `(alphabet.length - K) + K * alphabet.length >= count`).\n-4. The resulting set is prefix-free by construction: a single-letter code never starts a two-letter code (different prefix bucket).\n-5. Codes are case-insensitive in matching but emit lowercase. The overlay handles case folding when matching keystrokes.\n+   ```ts\n+   import { invoke } from \"@tauri-apps/api/core\";\n\n-Edge cases:\n-- `count === 0` → return `[]`.\n-- `count === 1` → return `[\"a\"]`.\n-- `count > alphabet.length ** 2` (more than ~676 targets) → throw an `Error(\"too many jump targets\")`. The Jump-To overlay caller should never hit this; if it does, the bug is upstream.\n+   /** Generate `count` distinct prefix-free Jump-To codes via the Rust kernel. */\n+   export async function generateSneakCodes(count: number): Promise<string[]> {\n+       return await invoke<string[]>(\"generate_jump_codes\", { count });\n+   }\n+   ```\n\n-This is pure logic — no DOM, no React, no async.\n+   The JumpToOverlay component (next task) calls this once on open. The 30-keystroke-per-second user experience is far below any noticeable invoke latency.\n\n ## Acceptance Criteria\n\n-- [ ] `generateSneakCodes(0).length === 0`.\n-- [ ] `generateSneakCodes(N)` returns N distinct strings for any `1 <= N <= 676`.\n-- [ ] No element of the returned array is a prefix of any other element.\n+- [ ] `swissarmyhammer-focus::generate_sneak_codes(0)` returns an empty vec.\n+- [ ] `swissarmyhammer-focus::generate_sneak_codes(N)` returns N distinct strings for `1 <= N <= alphabet.len().pow(2)`.\n+- [ ] No element of the returned vec is a prefix of any other element.\n - [ ] Codes for small N use the home-row letters first.\n-- [ ] `generateSneakCodes(N+1)` throws when N is the maximum capacity.\n+- [ ] `generate_sneak_codes(N+1)` returns `Err(SneakError::TooManyTargets(...))` when `N` is the maximum capacity.\n+- [ ] Tauri command `generate_jump_codes` is registered and round-trips correctly through `invoke`.\n+- [ ] Frontend `generateSneakCodes(count)` resolves to the same vec the Rust impl produces.\n\n ## Tests\n\n-- [ ] New test `kanban-app/ui/src/lib/sneak-codes.test.ts`:\n-  - `generates_empty_for_zero_count`\n-  - `generates_distinct_codes` — for N in [1, 5, 10, 26, 50, 200, 600], every pair is distinct.\n-  - `prefix_free_invariant` — for the same range of N, no code is a prefix of any other (use a brute-force check: for each pair, neither `startsWith` the other).\n-  - `single_letter_codes_use_home_row_first` — for N=4, returned codes are `[\"a\",\"s\",\"d\",\"f\"]` (or your chosen first four).\n-  - `throws_when_count_exceeds_capacity` — `generateSneakCodes(677)` throws.\n-- [ ] Test command: `cd kanban-app/ui && pnpm test sneak-codes` — passes.\n+- [ ] New Rust unit tests in `swissarmyhammer-focus/src/sneak.rs` (`#[cfg(test)] mod tests`):\n+  - `generates_empty_for_zero_count` — `generate_sneak_codes(0)` is empty.\n+  - `generates_distinct_codes` — for N in `[1, 5, 10, 23, 50, 200, 500]`, every pair is distinct.\n+  - `prefix_free_invariant` — for the same range of N, no code is a prefix of any other (brute-force check: for each pair, neither `starts_with` the other).\n+  - `single_letter_codes_use_home_row_first` — for N=4, returned codes match the first 4 letters of `SNEAK_ALPHABET`.\n+  - `errors_when_count_exceeds_capacity` — `generate_sneak_codes(SNEAK_ALPHABET.len().pow(2) + 1)` returns `Err(TooManyTargets(...))`.\n+- [ ] Tauri command test in `kanban-app/tests/`: round-trip `generate_jump_codes` with a known count and assert the result matches the Rust impl.\n+- [ ] Frontend test `kanban-app/ui/src/lib/sneak-codes.test.ts`: mock `invoke`, assert the wrapper passes `count` through and returns the resolved array.\n+- [ ] Test command: `cargo nextest run -p swissarmyhammer-focus && cd kanban-app/ui && pnpm test sneak-codes` — passes.\n\n ## Workflow\n\n-- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run. #nav-jump\n\\ No newline at end of file\n+- Use `/tdd` — write the Rust unit tests first (will fail because `sneak::generate_sneak_codes` doesn't exist); implement; re-run. Then add the Tauri command and the TS wrapper.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -4,101 +4,59 @@\n position_column: todo\n position_ordinal: e180\n project: keyboard-navigation\n-title: Sneak code generator in swissarmyhammer-focus (pure Rust + Tauri command)\n+title: Sneak code generator (prefix-free, ergonomic alphabet)\n ---\n ## What\n\n-Pure algorithm that produces short, prefix-free key codes for the Jump-To overlay — vim-sneak / jumpy / AceJump labels. Lives in **`swissarmyhammer-focus`** as Rust; exposed to the React frontend via a Tauri command in `kanban-app/src/commands.rs`.\n+Pure utility module that produces short, prefix-free key codes for the Jump-To overlay — the same idea as vim-sneak/jumpy/AceJump labels.\n\n-Architectural fit: spatial-nav vocabulary is Rust-authoritative in this workspace (`Direction`, `FullyQualifiedMoniker`, etc. are defined in Rust and mirrored in TypeScript). The sneak code algorithm is generic spatial-nav infrastructure with no domain or UI knowledge — putting it in `swissarmyhammer-focus` keeps the layering consistent and means any future consumer (mirdan-app) gets it via dep, not by copy-pasting TypeScript.\n+Create `kanban-app/ui/src/lib/sneak-codes.ts` exporting:\n\n-### Steps\n+```ts\n+/**\n+ * Generate `count` distinct key codes such that no code is a prefix of another.\n+ * Codes are drawn from an ergonomic alphabet biased toward home-row keys.\n+ * Returns codes ordered by ergonomic priority (shortest + easiest first).\n+ */\n+export function generateSneakCodes(count: number): string[];\n\n-1. **Create `swissarmyhammer-focus/src/sneak.rs`** with:\n+/** The alphabet used, exposed for tests. */\n+export const SNEAK_ALPHABET: readonly string[];\n+```\n\n-   ```rust\n-   /// Ergonomic alphabet, ordered by priority — home row first, then top\n-   /// row, then bottom row. Skips letters with high visual confusion\n-   /// (`i`/`1`/`l`, `o`/`0`).\n-   pub const SNEAK_ALPHABET: &[char] = &[\n-       'a','s','d','f','j','k','g','h','w','e','r','u',\n-       'p','q','t','y','z','x','c','v','n','m','b',\n-   ]; // pick a final 23-letter list; document the choices\n+Algorithm:\n\n-   /// Generate `count` distinct, prefix-free key codes drawn from\n-   /// [`SNEAK_ALPHABET`]. Codes are ordered by ergonomic priority\n-   /// (shortest + easiest first). Returned codes are lowercase strings.\n-   ///\n-   /// # Errors\n-   ///\n-   /// Returns an error when `count > SNEAK_ALPHABET.len().pow(2)` —\n-   /// today the cap is around 529, well above any realistic Jump-To\n-   /// target count. Hitting it indicates an upstream bug.\n-   pub fn generate_sneak_codes(count: usize) -> Result<Vec<String>, SneakError>;\n+1. Alphabet (in this priority order — home row first, then top row, then bottom row, skipping easily-confused keys): `[\"a\",\"s\",\"d\",\"f\",\"j\",\"k\",\"l\",\"g\",\"h\",\"w\",\"e\",\"r\",\"u\",\"i\",\"o\",\"p\",\"q\",\"t\",\"y\",\"z\",\"x\",\"c\",\"v\",\"n\",\"m\",\"b\"]` (26 letters minus none, but you can drop `i` if it visually conflicts with `1`/`l` — pick a final list and document the choice).\n+2. If `count <= alphabet.length`, return single-letter codes from the front of the alphabet.\n+3. If `count > alphabet.length`: split codes into two-letter prefixes. Reserve the last K letters of the alphabet as \"two-letter prefixes\": for each prefix letter X, codes `X+a`, `X+s`, `X+d`, ... become valid two-letter codes. Single-letter codes use the first `alphabet.length - K` letters; two-letter codes use combinations starting with the last K letters. Choose K to fit `count` codes (smallest K such that `(alphabet.length - K) + K * alphabet.length >= count`).\n+4. The resulting set is prefix-free by construction: a single-letter code never starts a two-letter code (different prefix bucket).\n+5. Codes are case-insensitive in matching but emit lowercase. The overlay handles case folding when matching keystrokes.\n\n-   #[derive(Debug, thiserror::Error)]\n-   pub enum SneakError {\n-       #[error(\"too many jump targets: {0} exceeds capacity {1}\")]\n-       TooManyTargets(usize, usize),\n-   }\n-   ```\n+Edge cases:\n+- `count === 0` → return `[]`.\n+- `count === 1` → return `[\"a\"]`.\n+- `count > alphabet.length ** 2` (more than ~676 targets) → throw an `Error(\"too many jump targets\")`. The Jump-To overlay caller should never hit this; if it does, the bug is upstream.\n\n-   Algorithm:\n-   - `count == 0` → empty vec.\n-   - If `count <= alphabet.len()` → single-letter codes from the front of the alphabet.\n-   - Else split codes into two-letter prefixes. Reserve the last `K` letters of the alphabet as \"two-letter prefixes\": for each prefix letter `X`, codes `Xa`, `Xs`, `Xd`, ... become valid two-letter codes. Single-letter codes use the first `alphabet.len() - K` letters; two-letter codes use combinations starting with the last `K` letters. Choose `K` to fit `count` codes (smallest `K` such that `(alphabet.len() - K) + K * alphabet.len() >= count`).\n-   - Prefix-free by construction: a single-letter code never starts a two-letter code (different prefix bucket).\n-   - Pure logic; no I/O, no `unsafe`, no dependencies beyond `thiserror`.\n-\n-2. **Expose via lib.rs**: `pub mod sneak; pub use sneak::{generate_sneak_codes, SNEAK_ALPHABET, SneakError};`\n-\n-3. **Tauri command in `kanban-app/src/commands.rs`**:\n-\n-   ```rust\n-   #[tauri::command]\n-   pub fn generate_jump_codes(count: usize) -> Result<Vec<String>, String> {\n-       swissarmyhammer_focus::generate_sneak_codes(count)\n-           .map_err(|e| e.to_string())\n-   }\n-   ```\n-\n-   Register it in the Tauri builder alongside the other commands. Read `kanban-app/src/commands.rs:173+` for the existing `#[tauri::command]` registration pattern and the warning banner at the top of the file about state-mutating commands. Sneak code generation is pure (no state mutation), so it's a clean addition.\n-\n-4. **Frontend bindings**: in `kanban-app/ui/src/lib/sneak-codes.ts` (a thin wrapper, not the algorithm itself):\n-\n-   ```ts\n-   import { invoke } from \"@tauri-apps/api/core\";\n-\n-   /** Generate `count` distinct prefix-free Jump-To codes via the Rust kernel. */\n-   export async function generateSneakCodes(count: number): Promise<string[]> {\n-       return await invoke<string[]>(\"generate_jump_codes\", { count });\n-   }\n-   ```\n-\n-   The JumpToOverlay component (next task) calls this once on open. The 30-keystroke-per-second user experience is far below any noticeable invoke latency.\n+This is pure logic — no DOM, no React, no async.\n\n ## Acceptance Criteria\n\n-- [ ] `swissarmyhammer-focus::generate_sneak_codes(0)` returns an empty vec.\n-- [ ] `swissarmyhammer-focus::generate_sneak_codes(N)` returns N distinct strings for `1 <= N <= alphabet.len().pow(2)`.\n-- [ ] No element of the returned vec is a prefix of any other element.\n+- [ ] `generateSneakCodes(0).length === 0`.\n+- [ ] `generateSneakCodes(N)` returns N distinct strings for any `1 <= N <= 676`.\n+- [ ] No element of the returned array is a prefix of any other element.\n - [ ] Codes for small N use the home-row letters first.\n-- [ ] `generate_sneak_codes(N+1)` returns `Err(SneakError::TooManyTargets(...))` when `N` is the maximum capacity.\n-- [ ] Tauri command `generate_jump_codes` is registered and round-trips correctly through `invoke`.\n-- [ ] Frontend `generateSneakCodes(count)` resolves to the same vec the Rust impl produces.\n+- [ ] `generateSneakCodes(N+1)` throws when N is the maximum capacity.\n\n ## Tests\n\n-- [ ] New Rust unit tests in `swissarmyhammer-focus/src/sneak.rs` (`#[cfg(test)] mod tests`):\n-  - `generates_empty_for_zero_count` — `generate_sneak_codes(0)` is empty.\n-  - `generates_distinct_codes` — for N in `[1, 5, 10, 23, 50, 200, 500]`, every pair is distinct.\n-  - `prefix_free_invariant` — for the same range of N, no code is a prefix of any other (brute-force check: for each pair, neither `starts_with` the other).\n-  - `single_letter_codes_use_home_row_first` — for N=4, returned codes match the first 4 letters of `SNEAK_ALPHABET`.\n-  - `errors_when_count_exceeds_capacity` — `generate_sneak_codes(SNEAK_ALPHABET.len().pow(2) + 1)` returns `Err(TooManyTargets(...))`.\n-- [ ] Tauri command test in `kanban-app/tests/`: round-trip `generate_jump_codes` with a known count and assert the result matches the Rust impl.\n-- [ ] Frontend test `kanban-app/ui/src/lib/sneak-codes.test.ts`: mock `invoke`, assert the wrapper passes `count` through and returns the resolved array.\n-- [ ] Test command: `cargo nextest run -p swissarmyhammer-focus && cd kanban-app/ui && pnpm test sneak-codes` — passes.\n+- [ ] New test `kanban-app/ui/src/lib/sneak-codes.test.ts`:\n+  - `generates_empty_for_zero_count`\n+  - `generates_distinct_codes` — for N in [1, 5, 10, 26, 50, 200, 600], every pair is distinct.\n+  - `prefix_free_invariant` — for the same range of N, no code is a prefix of any other (use a brute-force check: for each pair, neither `startsWith` the other).\n+  - `single_letter_codes_use_home_row_first` — for N=4, returned codes are `[\"a\",\"s\",\"d\",\"f\"]` (or your chosen first four).\n+  - `throws_when_count_exceeds_capacity` — `generateSneakCodes(677)` throws.\n+- [ ] Test command: `cd kanban-app/ui && pnpm test sneak-codes` — passes.\n\n ## Workflow\n\n-- Use `/tdd` — write the Rust unit tests first (will fail because `sneak::generate_sneak_codes` doesn't exist); implement; re-run. Then add the Tauri command and the TS wrapper.\n\\ No newline at end of file\n+- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run. #nav-jump\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQZ3K3GW6TCKK22Y3D3ZRB1G","timestamp":"2026-05-06T16:57:58.044832Z","entity_type":"task","entity_id":"01KQYWR82C4HVZ1VTJPSBKDNST","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,54 +1,96 @@\n ## What\n\n-Pure utility module that produces short, prefix-free key codes for the Jump-To overlay — the same idea as vim-sneak/jumpy/AceJump labels.\n+Pure algorithm that produces short, prefix-free key codes for the Jump-To overlay — vim-sneak / jumpy / AceJump labels. Lives in **`swissarmyhammer-focus`** as Rust; exposed to the React frontend via a Tauri command in `kanban-app/src/commands.rs`.\n+\n+Architectural fit: spatial-nav vocabulary is Rust-authoritative in this workspace (`Direction`, `FullyQualifiedMoniker`, etc. are defined in Rust and mirrored in TypeScript). The sneak code algorithm is generic spatial-nav infrastructure with no domain or UI knowledge — putting it in `swissarmyhammer-focus` keeps the layering consistent and means any future consumer (mirdan-app) gets it via dep, not by copy-pasting TypeScript.\n+\n+### Steps\n+\n+1. **Create `swissarmyhammer-focus/src/sneak.rs`** with:\n+\n+   ```rust\n+   /// Ergonomic alphabet, ordered by priority — home row first, then top\n+   /// row, then bottom row. Skips letters with high visual confusion\n+   /// (`i`/`1`/`l`, `o`/`0`).\n+   pub const SNEAK_ALPHABET: &[char] = &[\n+       'a','s','d','f','j','k','g','h','w','e','r','u',\n+       'p','q','t','y','z','x','c','v','n','m','b',\n+   ]; // pick a final 23-letter list; document the choices\n+\n+   /// Generate `count` distinct, prefix-free key codes drawn from\n+   /// [`SNEAK_ALPHABET`]. Codes are ordered by ergonomic priority\n+   /// (shortest + easiest first). Returned codes are lowercase strings.\n+   ///\n+   /// # Errors\n+   ///\n+   /// Returns an error when `count > SNEAK_ALPHABET.len().pow(2)` —\n+   /// today the cap is around 529, well above any realistic Jump-To\n+   /// target count. Hitting it indicates an upstream bug.\n+   pub fn generate_sneak_codes(count: usize) -> Result<Vec<String>, SneakError>;\n+\n+   #[derive(Debug, thiserror::Error)]\n+   pub enum SneakError {\n+       #[error(\"too many jump targets: {0} exceeds capacity {1}\")]\n+       TooManyTargets(usize, usize),\n+   }\n+   ```\n+\n+   Algorithm:\n+   - `count == 0` → empty vec.\n+   - If `count <= alphabet.len()` → single-letter codes from the front of the alphabet.\n+   - Else split codes into two-letter prefixes. Reserve the last `K` letters of the alphabet as \"two-letter prefixes\": for each prefix letter `X`, codes `Xa`, `Xs`, `Xd`, ... become valid two-letter codes. Single-letter codes use the first `alphabet.len() - K` letters; two-letter codes use combinations starting with the last `K` letters. Choose `K` to fit `count` codes (smallest `K` such that `(alphabet.len() - K) + K * alphabet.len() >= count`).\n+   - Prefix-free by construction: a single-letter code never starts a two-letter code (different prefix bucket).\n+   - Pure logic; no I/O, no `unsafe`, no dependencies beyond `thiserror`.\n+\n+2. **Expose via lib.rs**: `pub mod sneak; pub use sneak::{generate_sneak_codes, SNEAK_ALPHABET, SneakError};`\n\n-Create `kanban-app/ui/src/lib/sneak-codes.ts` exporting:\n+3. **Tauri command in `kanban-app/src/commands.rs`**:\n\n-```ts\n-/**\n- * Generate `count` distinct key codes such that no code is a prefix of another.\n- * Codes are drawn from an ergonomic alphabet biased toward home-row keys.\n- * Returns codes ordered by ergonomic priority (shortest + easiest first).\n- */\n-export function generateSneakCodes(count: number): string[];\n+   ```rust\n+   #[tauri::command]\n+   pub fn generate_jump_codes(count: usize) -> Result<Vec<String>, String> {\n+       swissarmyhammer_focus::generate_sneak_codes(count)\n+           .map_err(|e| e.to_string())\n+   }\n+   ```\n\n-/** The alphabet used, exposed for tests. */\n-export const SNEAK_ALPHABET: readonly string[];\n-```\n+   Register it in the Tauri builder alongside the other commands. Read `kanban-app/src/commands.rs:173+` for the existing `#[tauri::command]` registration pattern and the warning banner at the top of the file about state-mutating commands. Sneak code generation is pure (no state mutation), so it's a clean addition.\n\n-Algorithm:\n+4. **Frontend bindings**: in `kanban-app/ui/src/lib/sneak-codes.ts` (a thin wrapper, not the algorithm itself):\n\n-1. Alphabet (in this priority order — home row first, then top row, then bottom row, skipping easily-confused keys): `[\"a\",\"s\",\"d\",\"f\",\"j\",\"k\",\"l\",\"g\",\"h\",\"w\",\"e\",\"r\",\"u\",\"i\",\"o\",\"p\",\"q\",\"t\",\"y\",\"z\",\"x\",\"c\",\"v\",\"n\",\"m\",\"b\"]` (26 letters minus none, but you can drop `i` if it visually conflicts with `1`/`l` — pick a final list and document the choice).\n-2. If `count <= alphabet.length`, return single-letter codes from the front of the alphabet.\n-3. If `count > alphabet.length`: split codes into two-letter prefixes. Reserve the last K letters of the alphabet as \"two-letter prefixes\": for each prefix letter X, codes `X+a`, `X+s`, `X+d`, ... become valid two-letter codes. Single-letter codes use the first `alphabet.length - K` letters; two-letter codes use combinations starting with the last K letters. Choose K to fit `count` codes (smallest K such that `(alphabet.length - K) + K * alphabet.length >= count`).\n-4. The resulting set is prefix-free by construction: a single-letter code never starts a two-letter code (different prefix bucket).\n-5. Codes are case-insensitive in matching but emit lowercase. The overlay handles case folding when matching keystrokes.\n+   ```ts\n+   import { invoke } from \"@tauri-apps/api/core\";\n\n-Edge cases:\n-- `count === 0` → return `[]`.\n-- `count === 1` → return `[\"a\"]`.\n-- `count > alphabet.length ** 2` (more than ~676 targets) → throw an `Error(\"too many jump targets\")`. The Jump-To overlay caller should never hit this; if it does, the bug is upstream.\n+   /** Generate `count` distinct prefix-free Jump-To codes via the Rust kernel. */\n+   export async function generateSneakCodes(count: number): Promise<string[]> {\n+       return await invoke<string[]>(\"generate_jump_codes\", { count });\n+   }\n+   ```\n\n-This is pure logic — no DOM, no React, no async.\n+   The JumpToOverlay component (next task) calls this once on open. The 30-keystroke-per-second user experience is far below any noticeable invoke latency.\n\n ## Acceptance Criteria\n\n-- [ ] `generateSneakCodes(0).length === 0`.\n-- [ ] `generateSneakCodes(N)` returns N distinct strings for any `1 <= N <= 676`.\n-- [ ] No element of the returned array is a prefix of any other element.\n+- [ ] `swissarmyhammer-focus::generate_sneak_codes(0)` returns an empty vec.\n+- [ ] `swissarmyhammer-focus::generate_sneak_codes(N)` returns N distinct strings for `1 <= N <= alphabet.len().pow(2)`.\n+- [ ] No element of the returned vec is a prefix of any other element.\n - [ ] Codes for small N use the home-row letters first.\n-- [ ] `generateSneakCodes(N+1)` throws when N is the maximum capacity.\n+- [ ] `generate_sneak_codes(N+1)` returns `Err(SneakError::TooManyTargets(...))` when `N` is the maximum capacity.\n+- [ ] Tauri command `generate_jump_codes` is registered and round-trips correctly through `invoke`.\n+- [ ] Frontend `generateSneakCodes(count)` resolves to the same vec the Rust impl produces.\n\n ## Tests\n\n-- [ ] New test `kanban-app/ui/src/lib/sneak-codes.test.ts`:\n-  - `generates_empty_for_zero_count`\n-  - `generates_distinct_codes` — for N in [1, 5, 10, 26, 50, 200, 600], every pair is distinct.\n-  - `prefix_free_invariant` — for the same range of N, no code is a prefix of any other (use a brute-force check: for each pair, neither `startsWith` the other).\n-  - `single_letter_codes_use_home_row_first` — for N=4, returned codes are `[\"a\",\"s\",\"d\",\"f\"]` (or your chosen first four).\n-  - `throws_when_count_exceeds_capacity` — `generateSneakCodes(677)` throws.\n-- [ ] Test command: `cd kanban-app/ui && pnpm test sneak-codes` — passes.\n+- [ ] New Rust unit tests in `swissarmyhammer-focus/src/sneak.rs` (`#[cfg(test)] mod tests`):\n+  - `generates_empty_for_zero_count` — `generate_sneak_codes(0)` is empty.\n+  - `generates_distinct_codes` — for N in `[1, 5, 10, 23, 50, 200, 500]`, every pair is distinct.\n+  - `prefix_free_invariant` — for the same range of N, no code is a prefix of any other (brute-force check: for each pair, neither `starts_with` the other).\n+  - `single_letter_codes_use_home_row_first` — for N=4, returned codes match the first 4 letters of `SNEAK_ALPHABET`.\n+  - `errors_when_count_exceeds_capacity` — `generate_sneak_codes(SNEAK_ALPHABET.len().pow(2) + 1)` returns `Err(TooManyTargets(...))`.\n+- [ ] Tauri command test in `kanban-app/tests/`: round-trip `generate_jump_codes` with a known count and assert the result matches the Rust impl.\n+- [ ] Frontend test `kanban-app/ui/src/lib/sneak-codes.test.ts`: mock `invoke`, assert the wrapper passes `count` through and returns the resolved array.\n+- [ ] Test command: `cargo nextest run -p swissarmyhammer-focus && cd kanban-app/ui && pnpm test sneak-codes` — passes.\n\n ## Workflow\n\n-- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run. #nav-jump\n\\ No newline at end of file\n+- Use `/tdd` — write the Rust unit tests first (will fail because `sneak::generate_sneak_codes` doesn't exist); implement; re-run. Then add the Tauri command and the TS wrapper.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,96 +1,54 @@\n ## What\n\n-Pure algorithm that produces short, prefix-free key codes for the Jump-To overlay — vim-sneak / jumpy / AceJump labels. Lives in **`swissarmyhammer-focus`** as Rust; exposed to the React frontend via a Tauri command in `kanban-app/src/commands.rs`.\n+Pure utility module that produces short, prefix-free key codes for the Jump-To overlay — the same idea as vim-sneak/jumpy/AceJump labels.\n\n-Architectural fit: spatial-nav vocabulary is Rust-authoritative in this workspace (`Direction`, `FullyQualifiedMoniker`, etc. are defined in Rust and mirrored in TypeScript). The sneak code algorithm is generic spatial-nav infrastructure with no domain or UI knowledge — putting it in `swissarmyhammer-focus` keeps the layering consistent and means any future consumer (mirdan-app) gets it via dep, not by copy-pasting TypeScript.\n+Create `kanban-app/ui/src/lib/sneak-codes.ts` exporting:\n\n-### Steps\n+```ts\n+/**\n+ * Generate `count` distinct key codes such that no code is a prefix of another.\n+ * Codes are drawn from an ergonomic alphabet biased toward home-row keys.\n+ * Returns codes ordered by ergonomic priority (shortest + easiest first).\n+ */\n+export function generateSneakCodes(count: number): string[];\n\n-1. **Create `swissarmyhammer-focus/src/sneak.rs`** with:\n+/** The alphabet used, exposed for tests. */\n+export const SNEAK_ALPHABET: readonly string[];\n+```\n\n-   ```rust\n-   /// Ergonomic alphabet, ordered by priority — home row first, then top\n-   /// row, then bottom row. Skips letters with high visual confusion\n-   /// (`i`/`1`/`l`, `o`/`0`).\n-   pub const SNEAK_ALPHABET: &[char] = &[\n-       'a','s','d','f','j','k','g','h','w','e','r','u',\n-       'p','q','t','y','z','x','c','v','n','m','b',\n-   ]; // pick a final 23-letter list; document the choices\n+Algorithm:\n\n-   /// Generate `count` distinct, prefix-free key codes drawn from\n-   /// [`SNEAK_ALPHABET`]. Codes are ordered by ergonomic priority\n-   /// (shortest + easiest first). Returned codes are lowercase strings.\n-   ///\n-   /// # Errors\n-   ///\n-   /// Returns an error when `count > SNEAK_ALPHABET.len().pow(2)` —\n-   /// today the cap is around 529, well above any realistic Jump-To\n-   /// target count. Hitting it indicates an upstream bug.\n-   pub fn generate_sneak_codes(count: usize) -> Result<Vec<String>, SneakError>;\n+1. Alphabet (in this priority order — home row first, then top row, then bottom row, skipping easily-confused keys): `[\"a\",\"s\",\"d\",\"f\",\"j\",\"k\",\"l\",\"g\",\"h\",\"w\",\"e\",\"r\",\"u\",\"i\",\"o\",\"p\",\"q\",\"t\",\"y\",\"z\",\"x\",\"c\",\"v\",\"n\",\"m\",\"b\"]` (26 letters minus none, but you can drop `i` if it visually conflicts with `1`/`l` — pick a final list and document the choice).\n+2. If `count <= alphabet.length`, return single-letter codes from the front of the alphabet.\n+3. If `count > alphabet.length`: split codes into two-letter prefixes. Reserve the last K letters of the alphabet as \"two-letter prefixes\": for each prefix letter X, codes `X+a`, `X+s`, `X+d`, ... become valid two-letter codes. Single-letter codes use the first `alphabet.length - K` letters; two-letter codes use combinations starting with the last K letters. Choose K to fit `count` codes (smallest K such that `(alphabet.length - K) + K * alphabet.length >= count`).\n+4. The resulting set is prefix-free by construction: a single-letter code never starts a two-letter code (different prefix bucket).\n+5. Codes are case-insensitive in matching but emit lowercase. The overlay handles case folding when matching keystrokes.\n\n-   #[derive(Debug, thiserror::Error)]\n-   pub enum SneakError {\n-       #[error(\"too many jump targets: {0} exceeds capacity {1}\")]\n-       TooManyTargets(usize, usize),\n-   }\n-   ```\n+Edge cases:\n+- `count === 0` → return `[]`.\n+- `count === 1` → return `[\"a\"]`.\n+- `count > alphabet.length ** 2` (more than ~676 targets) → throw an `Error(\"too many jump targets\")`. The Jump-To overlay caller should never hit this; if it does, the bug is upstream.\n\n-   Algorithm:\n-   - `count == 0` → empty vec.\n-   - If `count <= alphabet.len()` → single-letter codes from the front of the alphabet.\n-   - Else split codes into two-letter prefixes. Reserve the last `K` letters of the alphabet as \"two-letter prefixes\": for each prefix letter `X`, codes `Xa`, `Xs`, `Xd`, ... become valid two-letter codes. Single-letter codes use the first `alphabet.len() - K` letters; two-letter codes use combinations starting with the last `K` letters. Choose `K` to fit `count` codes (smallest `K` such that `(alphabet.len() - K) + K * alphabet.len() >= count`).\n-   - Prefix-free by construction: a single-letter code never starts a two-letter code (different prefix bucket).\n-   - Pure logic; no I/O, no `unsafe`, no dependencies beyond `thiserror`.\n-\n-2. **Expose via lib.rs**: `pub mod sneak; pub use sneak::{generate_sneak_codes, SNEAK_ALPHABET, SneakError};`\n-\n-3. **Tauri command in `kanban-app/src/commands.rs`**:\n-\n-   ```rust\n-   #[tauri::command]\n-   pub fn generate_jump_codes(count: usize) -> Result<Vec<String>, String> {\n-       swissarmyhammer_focus::generate_sneak_codes(count)\n-           .map_err(|e| e.to_string())\n-   }\n-   ```\n-\n-   Register it in the Tauri builder alongside the other commands. Read `kanban-app/src/commands.rs:173+` for the existing `#[tauri::command]` registration pattern and the warning banner at the top of the file about state-mutating commands. Sneak code generation is pure (no state mutation), so it's a clean addition.\n-\n-4. **Frontend bindings**: in `kanban-app/ui/src/lib/sneak-codes.ts` (a thin wrapper, not the algorithm itself):\n-\n-   ```ts\n-   import { invoke } from \"@tauri-apps/api/core\";\n-\n-   /** Generate `count` distinct prefix-free Jump-To codes via the Rust kernel. */\n-   export async function generateSneakCodes(count: number): Promise<string[]> {\n-       return await invoke<string[]>(\"generate_jump_codes\", { count });\n-   }\n-   ```\n-\n-   The JumpToOverlay component (next task) calls this once on open. The 30-keystroke-per-second user experience is far below any noticeable invoke latency.\n+This is pure logic — no DOM, no React, no async.\n\n ## Acceptance Criteria\n\n-- [ ] `swissarmyhammer-focus::generate_sneak_codes(0)` returns an empty vec.\n-- [ ] `swissarmyhammer-focus::generate_sneak_codes(N)` returns N distinct strings for `1 <= N <= alphabet.len().pow(2)`.\n-- [ ] No element of the returned vec is a prefix of any other element.\n+- [ ] `generateSneakCodes(0).length === 0`.\n+- [ ] `generateSneakCodes(N)` returns N distinct strings for any `1 <= N <= 676`.\n+- [ ] No element of the returned array is a prefix of any other element.\n - [ ] Codes for small N use the home-row letters first.\n-- [ ] `generate_sneak_codes(N+1)` returns `Err(SneakError::TooManyTargets(...))` when `N` is the maximum capacity.\n-- [ ] Tauri command `generate_jump_codes` is registered and round-trips correctly through `invoke`.\n-- [ ] Frontend `generateSneakCodes(count)` resolves to the same vec the Rust impl produces.\n+- [ ] `generateSneakCodes(N+1)` throws when N is the maximum capacity.\n\n ## Tests\n\n-- [ ] New Rust unit tests in `swissarmyhammer-focus/src/sneak.rs` (`#[cfg(test)] mod tests`):\n-  - `generates_empty_for_zero_count` — `generate_sneak_codes(0)` is empty.\n-  - `generates_distinct_codes` — for N in `[1, 5, 10, 23, 50, 200, 500]`, every pair is distinct.\n-  - `prefix_free_invariant` — for the same range of N, no code is a prefix of any other (brute-force check: for each pair, neither `starts_with` the other).\n-  - `single_letter_codes_use_home_row_first` — for N=4, returned codes match the first 4 letters of `SNEAK_ALPHABET`.\n-  - `errors_when_count_exceeds_capacity` — `generate_sneak_codes(SNEAK_ALPHABET.len().pow(2) + 1)` returns `Err(TooManyTargets(...))`.\n-- [ ] Tauri command test in `kanban-app/tests/`: round-trip `generate_jump_codes` with a known count and assert the result matches the Rust impl.\n-- [ ] Frontend test `kanban-app/ui/src/lib/sneak-codes.test.ts`: mock `invoke`, assert the wrapper passes `count` through and returns the resolved array.\n-- [ ] Test command: `cargo nextest run -p swissarmyhammer-focus && cd kanban-app/ui && pnpm test sneak-codes` — passes.\n+- [ ] New test `kanban-app/ui/src/lib/sneak-codes.test.ts`:\n+  - `generates_empty_for_zero_count`\n+  - `generates_distinct_codes` — for N in [1, 5, 10, 26, 50, 200, 600], every pair is distinct.\n+  - `prefix_free_invariant` — for the same range of N, no code is a prefix of any other (use a brute-force check: for each pair, neither `startsWith` the other).\n+  - `single_letter_codes_use_home_row_first` — for N=4, returned codes are `[\"a\",\"s\",\"d\",\"f\"]` (or your chosen first four).\n+  - `throws_when_count_exceeds_capacity` — `generateSneakCodes(677)` throws.\n+- [ ] Test command: `cd kanban-app/ui && pnpm test sneak-codes` — passes.\n\n ## Workflow\n\n-- Use `/tdd` — write the Rust unit tests first (will fail because `sneak::generate_sneak_codes` doesn't exist); implement; re-run. Then add the Tauri command and the TS wrapper.\n\\ No newline at end of file\n+- Use `/tdd` — write the test file first with all five cases failing; then implement; re-run. #nav-jump\n\\ No newline at end of file\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Sneak code generator (prefix-free, ergonomic alphabet)\n\\ No newline at end of file\n+Sneak code generator in swissarmyhammer-focus (pure Rust + Tauri command)\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Sneak code generator in swissarmyhammer-focus (pure Rust + Tauri command)\n\\ No newline at end of file\n+Sneak code generator (prefix-free, ergonomic alphabet)\n\\ No newline at end of file\n"}]]}
{"id":"01KQZ7C0SAEA5X5MVJC3806QX1","timestamp":"2026-05-06T18:04:00.170513Z","op":"update","item_id":"01KQYWR82C4HVZ1VTJPSBKDNST","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n position_column: todo\n position_ordinal: e180\n-project: keyboard-navigation\n+project: spatial-nav\n title: Sneak code generator in swissarmyhammer-focus (pure Rust + Tauri command)\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n position_column: todo\n position_ordinal: e180\n-project: spatial-nav\n+project: keyboard-navigation\n title: Sneak code generator in swissarmyhammer-focus (pure Rust + Tauri command)\n ---\n ## What\n","transaction_id":null}
{"id":"01KQZ7C0SB60SMJBBD7AA8JY5M","timestamp":"2026-05-06T18:04:00.171072Z","entity_type":"task","entity_id":"01KQYWR82C4HVZ1VTJPSBKDNST","op":"update","changes":[["project",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-keyboard-navigation\n\\ No newline at end of file\n+spatial-nav\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-spatial-nav\n\\ No newline at end of file\n+keyboard-navigation\n\\ No newline at end of file\n"}]]}
{"id":"01KQZ7FASA87ZYYA351SMXQHGY","timestamp":"2026-05-06T18:05:48.714488Z","op":"update","item_id":"01KQYWR82C4HVZ1VTJPSBKDNST","forward_patch":"--- original\n+++ modified\n@@ -101,4 +101,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the Rust unit tests first (will fail because `sneak::generate_sneak_codes` doesn't exist); implement; re-run. Then add the Tauri command and the TS wrapper.\n\\ No newline at end of file\n+- Use `/tdd` — write the Rust unit tests first (will fail because `sneak::generate_sneak_codes` doesn't exist); implement; re-run. Then add the Tauri command and the TS wrapper. #nav-jump\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -101,4 +101,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the Rust unit tests first (will fail because `sneak::generate_sneak_codes` doesn't exist); implement; re-run. Then add the Tauri command and the TS wrapper. #nav-jump\n\\ No newline at end of file\n+- Use `/tdd` — write the Rust unit tests first (will fail because `sneak::generate_sneak_codes` doesn't exist); implement; re-run. Then add the Tauri command and the TS wrapper.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQZ7FASARMP1X1WEEGVPEFVX","timestamp":"2026-05-06T18:05:48.714916Z","entity_type":"task","entity_id":"01KQYWR82C4HVZ1VTJPSBKDNST","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -93,4 +93,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the Rust unit tests first (will fail because `sneak::generate_sneak_codes` doesn't exist); implement; re-run. Then add the Tauri command and the TS wrapper.\n\\ No newline at end of file\n+- Use `/tdd` — write the Rust unit tests first (will fail because `sneak::generate_sneak_codes` doesn't exist); implement; re-run. Then add the Tauri command and the TS wrapper. #nav-jump\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -93,4 +93,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the Rust unit tests first (will fail because `sneak::generate_sneak_codes` doesn't exist); implement; re-run. Then add the Tauri command and the TS wrapper. #nav-jump\n\\ No newline at end of file\n+- Use `/tdd` — write the Rust unit tests first (will fail because `sneak::generate_sneak_codes` doesn't exist); implement; re-run. Then add the Tauri command and the TS wrapper.\n\\ No newline at end of file\n"}]]}

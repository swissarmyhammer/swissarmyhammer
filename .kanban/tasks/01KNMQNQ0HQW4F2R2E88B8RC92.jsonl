{"id":"01KNMQNQ0JA1FPND7F835DWA0W","timestamp":"2026-04-07T19:45:52.658675Z","entity_type":"task","entity_id":"01KNMQNQ0HQW4F2R2E88B8RC92","op":"create","changes":[["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\nThe indeterminate progress bar in `NavBar` (`nav-bar.tsx:85-93`) is driven by `useCommandBusy()` which tracks `inflightCount` from `useDispatchCommand`. But context menu commands **bypass this entirely**.\n\nWhen a user right-clicks → selects a command (e.g. \\\"Make Next\\\"), the flow is:\n1. Frontend calls `invoke(\\\"show_context_menu\\\", { items })` (`context-menu.ts:72`)\n2. User selects an item → `handle_menu_event` in `menu.rs:354` receives it\n3. `menu.rs:380` calls `dispatch_command_internal` **directly on the Rust side** via `tauri::async_runtime::spawn`\n4. The frontend is never notified that a command is in flight — `inflightCount` stays at 0\n\nContrast with drag-and-drop, which goes through `useDispatchCommand` (`drag-session-context.tsx:111,129,148`) and correctly increments/decrements `inflightCount`.\n\n### Fix\n\nEmit a Tauri event pair from `handle_menu_event` so the frontend can track context-menu command execution. The cleanest approach:\n\n**`kanban-app/src/menu.rs` — context menu dispatch block (~line 375-396)**\n- Before calling `dispatch_command_internal`, emit a `\\\"command-started\\\"` event with the command ID\n- After `dispatch_command_internal` returns (success or error), emit a `\\\"command-finished\\\"` event\n\n**`kanban-app/ui/src/lib/command-scope.tsx` — `CommandBusyProvider` (~line 75-88)**\n- Subscribe to `\\\"command-started\\\"` and `\\\"command-finished\\\"` Tauri events\n- Increment/decrement `inflightCount` in the event handlers\n- Clean up listeners on unmount\n\nThis makes the busy tracking universal: keybindings and palette go through `useDispatchCommand` (already tracked), context menu goes through Tauri events (newly tracked), both feed the same `inflightCount`.\n\n## Acceptance Criteria\n- [ ] Right-click → \\\"Make Next\\\" (or any context menu command) shows the indeterminate progress bar in the NavBar while the backend executes\n- [ ] Progress bar disappears when the command completes\n- [ ] Drag-and-drop still shows the progress bar (no regression)\n- [ ] Keyboard/palette commands still show the progress bar (no regression)\n\n## Tests\n- [ ] Add test in `kanban-app/ui/src/lib/command-scope.test.tsx`: emit `\\\"command-started\\\"` → assert `isBusy` is true → emit `\\\"command-finished\\\"` → assert `isBusy` is false\n- [ ] Add test: concurrent context-menu and useDispatchCommand — both increment inflightCount, bar stays visible until both complete\n- [ ] Run `pnpm vitest run command-scope` — all pass\n\n## Workflow\n- Use `/tdd` — write failing tests first, then implement to make them pass.\n\n#bug"}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"a280"}],["title",{"kind":"set","value":"Context menu commands bypass CommandBusy tracking — progress bar never shows"}]],"transaction_id":"01KNMQNJX8WMY3ADNSD8Z0Q95R"}
{"id":"01KNMR22W6QZK9Z2Y7AKDR4V97","timestamp":"2026-04-07T19:52:38.022339Z","entity_type":"task","entity_id":"01KNMQNQ0HQW4F2R2E88B8RC92","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,40 +1,39 @@\n ## What\n\n-The indeterminate progress bar in `NavBar` (`nav-bar.tsx:85-93`) is driven by `useCommandBusy()` which tracks `inflightCount` from `useDispatchCommand`. But context menu commands **bypass this entirely**.\n+Context menu command selections are dispatched entirely on the Rust side (`menu.rs:378-393` calls `dispatch_command_internal` directly via `tauri::async_runtime::spawn`). This bypasses `useDispatchCommand` — no busy tracking, no client-side command resolution, a completely separate dispatch path from keybindings/palette/drag.\n\n-When a user right-clicks → selects a command (e.g. \\\"Make Next\\\"), the flow is:\n-1. Frontend calls `invoke(\\\"show_context_menu\\\", { items })` (`context-menu.ts:72`)\n-2. User selects an item → `handle_menu_event` in `menu.rs:354` receives it\n-3. `menu.rs:380` calls `dispatch_command_internal` **directly on the Rust side** via `tauri::async_runtime::spawn`\n-4. The frontend is never notified that a command is in flight — `inflightCount` stays at 0\n+Meanwhile, **non-context-menu** menu bar commands already do this correctly: `menu.rs:400` emits `\\\"menu-command\\\"`, `app-shell.tsx:65` catches it, calls `executeCommand(commandId)` which goes through `useDispatchCommand`. Context menus should follow the same pattern.\n\n-Contrast with drag-and-drop, which goes through `useDispatchCommand` (`drag-session-context.tsx:111,129,148`) and correctly increments/decrements `inflightCount`.\n+### Fix — emit event, dispatch on frontend\n\n-### Fix\n+**1. `kanban-app/src/menu.rs` — context menu block (~line 373-396)**\n+- Instead of calling `dispatch_command_internal` directly, emit a `\\\"context-menu-command\\\"` event with the full `ContextMenuItem` payload (cmd, target, scope_chain)\n+- This mirrors the existing `\\\"menu-command\\\"` emit at line 400 but carries richer payload\n\n-Emit a Tauri event pair from `handle_menu_event` so the frontend can track context-menu command execution. The cleanest approach:\n+**2. `kanban-app/ui/src/lib/command-scope.tsx` — `DispatchOptions` (~line 293-299)**\n+- Add optional `scopeChain?: string[]` to `DispatchOptions`\n+- In the dispatch callback (~line 346), use `opts.scopeChain ?? scopeChainFromScope(effectiveScope)` — explicit chain wins over React context\n+- This lets the context menu handler pass the right-click-point scope chain rather than whatever happens to be focused when the event arrives\n\n-**`kanban-app/src/menu.rs` — context menu dispatch block (~line 375-396)**\n-- Before calling `dispatch_command_internal`, emit a `\\\"command-started\\\"` event with the command ID\n-- After `dispatch_command_internal` returns (success or error), emit a `\\\"command-finished\\\"` event\n+**3. `kanban-app/ui/src/components/app-shell.tsx` — `KeybindingHandler` (~line 60-75)**\n+- Add a listener for `\\\"context-menu-command\\\"` alongside the existing `\\\"menu-command\\\"` listener\n+- Parse the `ContextMenuItem` payload, call `dispatch(item.cmd, { target: item.target, scopeChain: item.scope_chain })`\n+- This goes through `useDispatchCommand` → `inflightCount` tracking, client-side resolution, the whole bus\n\n-**`kanban-app/ui/src/lib/command-scope.tsx` — `CommandBusyProvider` (~line 75-88)**\n-- Subscribe to `\\\"command-started\\\"` and `\\\"command-finished\\\"` Tauri events\n-- Increment/decrement `inflightCount` in the event handlers\n-- Clean up listeners on unmount\n+### Why not events\n\n-This makes the busy tracking universal: keybindings and palette go through `useDispatchCommand` (already tracked), context menu goes through Tauri events (newly tracked), both feed the same `inflightCount`.\n+Adding `command-started`/`command-finished` events would create a parallel busy-tracking system. The real fix is to have one dispatch path — `useDispatchCommand` — for all command execution. The context menu should feed into it, not around it.\n\n ## Acceptance Criteria\n-- [ ] Right-click → \\\"Make Next\\\" (or any context menu command) shows the indeterminate progress bar in the NavBar while the backend executes\n-- [ ] Progress bar disappears when the command completes\n-- [ ] Drag-and-drop still shows the progress bar (no regression)\n-- [ ] Keyboard/palette commands still show the progress bar (no regression)\n+- [ ] Context menu commands dispatch through `useDispatchCommand` (verify via `inflightCount` — progress bar shows during execution)\n+- [ ] Context menu commands carry the right-click scope chain (not the current focus scope)\n+- [ ] `menu.rs` no longer calls `dispatch_command_internal` for context menu items\n+- [ ] Keybinding/palette/drag commands unchanged (no regression)\n\n ## Tests\n-- [ ] Add test in `kanban-app/ui/src/lib/command-scope.test.tsx`: emit `\\\"command-started\\\"` → assert `isBusy` is true → emit `\\\"command-finished\\\"` → assert `isBusy` is false\n-- [ ] Add test: concurrent context-menu and useDispatchCommand — both increment inflightCount, bar stays visible until both complete\n-- [ ] Run `pnpm vitest run command-scope` — all pass\n+- [ ] Add test in `kanban-app/ui/src/components/app-shell.test.tsx`: emit `\\\"context-menu-command\\\"` event with `{ cmd, target, scope_chain }` → assert `invoke(\\\"dispatch_command\\\")` is called with matching args\n+- [ ] Add test in `kanban-app/ui/src/lib/command-scope.test.tsx`: dispatch with explicit `scopeChain` in options → assert it overrides the context-derived chain\n+- [ ] Run `pnpm vitest run app-shell command-scope` — all pass\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,39 +1,40 @@\n ## What\n\n-Context menu command selections are dispatched entirely on the Rust side (`menu.rs:378-393` calls `dispatch_command_internal` directly via `tauri::async_runtime::spawn`). This bypasses `useDispatchCommand` — no busy tracking, no client-side command resolution, a completely separate dispatch path from keybindings/palette/drag.\n+The indeterminate progress bar in `NavBar` (`nav-bar.tsx:85-93`) is driven by `useCommandBusy()` which tracks `inflightCount` from `useDispatchCommand`. But context menu commands **bypass this entirely**.\n\n-Meanwhile, **non-context-menu** menu bar commands already do this correctly: `menu.rs:400` emits `\\\"menu-command\\\"`, `app-shell.tsx:65` catches it, calls `executeCommand(commandId)` which goes through `useDispatchCommand`. Context menus should follow the same pattern.\n+When a user right-clicks → selects a command (e.g. \\\"Make Next\\\"), the flow is:\n+1. Frontend calls `invoke(\\\"show_context_menu\\\", { items })` (`context-menu.ts:72`)\n+2. User selects an item → `handle_menu_event` in `menu.rs:354` receives it\n+3. `menu.rs:380` calls `dispatch_command_internal` **directly on the Rust side** via `tauri::async_runtime::spawn`\n+4. The frontend is never notified that a command is in flight — `inflightCount` stays at 0\n\n-### Fix — emit event, dispatch on frontend\n+Contrast with drag-and-drop, which goes through `useDispatchCommand` (`drag-session-context.tsx:111,129,148`) and correctly increments/decrements `inflightCount`.\n\n-**1. `kanban-app/src/menu.rs` — context menu block (~line 373-396)**\n-- Instead of calling `dispatch_command_internal` directly, emit a `\\\"context-menu-command\\\"` event with the full `ContextMenuItem` payload (cmd, target, scope_chain)\n-- This mirrors the existing `\\\"menu-command\\\"` emit at line 400 but carries richer payload\n+### Fix\n\n-**2. `kanban-app/ui/src/lib/command-scope.tsx` — `DispatchOptions` (~line 293-299)**\n-- Add optional `scopeChain?: string[]` to `DispatchOptions`\n-- In the dispatch callback (~line 346), use `opts.scopeChain ?? scopeChainFromScope(effectiveScope)` — explicit chain wins over React context\n-- This lets the context menu handler pass the right-click-point scope chain rather than whatever happens to be focused when the event arrives\n+Emit a Tauri event pair from `handle_menu_event` so the frontend can track context-menu command execution. The cleanest approach:\n\n-**3. `kanban-app/ui/src/components/app-shell.tsx` — `KeybindingHandler` (~line 60-75)**\n-- Add a listener for `\\\"context-menu-command\\\"` alongside the existing `\\\"menu-command\\\"` listener\n-- Parse the `ContextMenuItem` payload, call `dispatch(item.cmd, { target: item.target, scopeChain: item.scope_chain })`\n-- This goes through `useDispatchCommand` → `inflightCount` tracking, client-side resolution, the whole bus\n+**`kanban-app/src/menu.rs` — context menu dispatch block (~line 375-396)**\n+- Before calling `dispatch_command_internal`, emit a `\\\"command-started\\\"` event with the command ID\n+- After `dispatch_command_internal` returns (success or error), emit a `\\\"command-finished\\\"` event\n\n-### Why not events\n+**`kanban-app/ui/src/lib/command-scope.tsx` — `CommandBusyProvider` (~line 75-88)**\n+- Subscribe to `\\\"command-started\\\"` and `\\\"command-finished\\\"` Tauri events\n+- Increment/decrement `inflightCount` in the event handlers\n+- Clean up listeners on unmount\n\n-Adding `command-started`/`command-finished` events would create a parallel busy-tracking system. The real fix is to have one dispatch path — `useDispatchCommand` — for all command execution. The context menu should feed into it, not around it.\n+This makes the busy tracking universal: keybindings and palette go through `useDispatchCommand` (already tracked), context menu goes through Tauri events (newly tracked), both feed the same `inflightCount`.\n\n ## Acceptance Criteria\n-- [ ] Context menu commands dispatch through `useDispatchCommand` (verify via `inflightCount` — progress bar shows during execution)\n-- [ ] Context menu commands carry the right-click scope chain (not the current focus scope)\n-- [ ] `menu.rs` no longer calls `dispatch_command_internal` for context menu items\n-- [ ] Keybinding/palette/drag commands unchanged (no regression)\n+- [ ] Right-click → \\\"Make Next\\\" (or any context menu command) shows the indeterminate progress bar in the NavBar while the backend executes\n+- [ ] Progress bar disappears when the command completes\n+- [ ] Drag-and-drop still shows the progress bar (no regression)\n+- [ ] Keyboard/palette commands still show the progress bar (no regression)\n\n ## Tests\n-- [ ] Add test in `kanban-app/ui/src/components/app-shell.test.tsx`: emit `\\\"context-menu-command\\\"` event with `{ cmd, target, scope_chain }` → assert `invoke(\\\"dispatch_command\\\")` is called with matching args\n-- [ ] Add test in `kanban-app/ui/src/lib/command-scope.test.tsx`: dispatch with explicit `scopeChain` in options → assert it overrides the context-derived chain\n-- [ ] Run `pnpm vitest run app-shell command-scope` — all pass\n+- [ ] Add test in `kanban-app/ui/src/lib/command-scope.test.tsx`: emit `\\\"command-started\\\"` → assert `isBusy` is true → emit `\\\"command-finished\\\"` → assert `isBusy` is false\n+- [ ] Add test: concurrent context-menu and useDispatchCommand — both increment inflightCount, bar stays visible until both complete\n+- [ ] Run `pnpm vitest run command-scope` — all pass\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Context menu commands bypass CommandBusy tracking — progress bar never shows\n\\ No newline at end of file\n+Context menu commands must dispatch through useDispatchCommand, not bypass it\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Context menu commands must dispatch through useDispatchCommand, not bypass it\n\\ No newline at end of file\n+Context menu commands bypass CommandBusy tracking — progress bar never shows\n\\ No newline at end of file\n"}]],"transaction_id":"01KNMR22VRTPWYTW1X9NZHPCJQ"}
{"id":"01KP10RZ7G0BTM96CYQX5WHJC1","timestamp":"2026-04-12T14:15:49.744383Z","op":"update","item_id":"01KNMQNQ0HQW4F2R2E88B8RC92","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: todo\n-position_ordinal: a280\n+position_ordinal: '80'\n title: Context menu commands must dispatch through useDispatchCommand, not bypass it\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: todo\n-position_ordinal: '80'\n+position_ordinal: a280\n title: Context menu commands must dispatch through useDispatchCommand, not bypass it\n ---\n ## What\n","transaction_id":null}
{"id":"01KP10RZ7HT15HKW7N1H1N6T71","timestamp":"2026-04-12T14:15:49.745009Z","entity_type":"task","entity_id":"01KNMQNQ0HQW4F2R2E88B8RC92","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-a280\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+a280\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5NTFD7ZZ2R4EH2SAHDM2E3","timestamp":"2026-04-26T19:56:18.727173Z","op":"update","item_id":"01KNMQNQ0HQW4F2R2E88B8RC92","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: done\n-position_ordinal: ffffffffffffffffffffffbb80\n+position_ordinal: ffffffffffffffffffffffffffffff8f80\n title: Context menu commands must dispatch through useDispatchCommand, not bypass it\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffff8f80\n+position_ordinal: ffffffffffffffffffffffbb80\n title: Context menu commands must dispatch through useDispatchCommand, not bypass it\n ---\n ## What\n","transaction_id":null}
{"id":"01KQ5NTFD77273GG84FA6FNMG7","timestamp":"2026-04-26T19:56:18.727479Z","entity_type":"task","entity_id":"01KNMQNQ0HQW4F2R2E88B8RC92","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffbb80\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffff8f80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffff8f80\n\\ No newline at end of file\n+ffffffffffffffffffffffbb80\n\\ No newline at end of file\n"}]]}

---
assignees:
- claude-code
position_column: done
position_ordinal: ffffffffffffffffffffffa780
title: Add tests for LspDaemon restart_with_backoff and shutdown
---
daemon.rs:344-345, 395-396\n\nCoverage: 74.6% (153/205 lines)\n\nUncovered lines: 344-345, 395-396\n\nFunctions:\n1. `restart_with_backoff()` (lines 333-351) — lines 344-345 are the info log + sleep before restart\n2. `shutdown()` (lines 370-407) — lines 395-396 are the Ok(Err(e)) path: graceful shutdown returned error\n\nTest scenarios:\n- restart_with_backoff with low failure count → verify sleep + restart called\n- shutdown where graceful_shutdown returns Err → verify warning logged, state = NotStarted\n\n#coverage-gap\n\n=== RESOLUTION ===\nAdded 6 tests:\n- test_restart_with_backoff_info_log_path_attempt_2: attempt=2 → 4s backoff, verifies sleep + restart path\n- test_restart_with_backoff_success_path: successful restart via python mock, confirms Ok(()) + Running state + consecutive_failures reset\n- test_restart_with_backoff_at_attempt_zero_returns_binary_not_found: confirms error propagation from underlying start()\n- test_shutdown_timeout_path_emits_shutting_down_then_not_started: covers the Err(_) timeout branch (5s sleep process refuses stdin close) — final state NotStarted\n- test_shutdown_clears_client_with_installed_value: verifies client clearing on shutdown with no child\n- test_shutdown_is_idempotent_when_not_started: idempotent shutdown\n\nSKIPPED GAP:\n- shutdown Ok(Err(e)) path (395-396): graceful_shutdown only returns Err when child.wait().await fails. wait() on a child that exists only fails if the OS wait4 syscall fails (basically never in practice) or if the child handle has been invalidated. All other errors in graceful_shutdown (send_jsonrpc_message failures) are swallowed with `let _ =`. Triggering this portably requires mocking tokio::process::Child, which would require refactoring production code. The Ok(Err) branch behaves identically to Err(_) (both log warn and continue to NotStarted), so the timeout test covers the branch's observable behavior.
{"id":"01KYSFEYD8QXT05RS6PZ6TXJTX","timestamp":"2026-07-30T12:19:50.824410Z","op":"create","item_id":"01KYSFEYD7B6GAJ21H9T7EBYN8","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,72 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: b680\n+title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n+---\n+Skill install is a lossy parse-then-rebuild. It keeps an allowlist of 9 frontmatter fields and discards every other key without a warning.\n+\n+## Symptom\n+\n+`builtin/skills/finish/SKILL.md:12-16` declares a skill-scoped Stop hook:\n+\n+```yaml\n+hooks:\n+  Stop:\n+    - hooks:\n+        - type: command\n+          command: \"sah tool ralph ralph check --\"\n+```\n+\n+The deployed file has no `hooks` key:\n+\n+```\n+$ awk '/^---$/{n++; if(n==2) exit} {print}' ~/.skills/finish/SKILL.md\n+---\n+name: finish\n+description: ...\n+license: MIT OR Apache-2.0\n+compatibility: Requires the `kanban` and `ralph` MCP tools plus a Stop-hook-capable harness.\n+metadata:\n+  author: swissarmyhammer\n+  version: 0.17.0\n+```\n+\n+`~/.claude/skills/finish` is a symlink to `~/.skills/finish`, so Claude Code reads the stripped file. Result: `/finish` calls `set ralph`, but no Stop hook exists to block the stop. The loop dies between iterations.\n+\n+`hooks` is an official Claude Code skill frontmatter field. See https://code.claude.com/docs/en/skills#frontmatter-reference and https://code.claude.com/docs/en/hooks#hooks-in-skills-and-agents. Skill-scoped hooks are active only while the skill runs, which is the correct behavior for `/finish`.\n+\n+## Cause\n+\n+Both sides of the round-trip use a closed struct:\n+\n+- `crates/swissarmyhammer-skills/src/skill_loader.rs:11-28` — `SkillFrontmatter` names 9 fields. Serde discards unknown keys. There is no `deny_unknown_fields`, so no error is raised.\n+- `crates/swissarmyhammer-skills/src/deploy.rs:27-44` — `format_skill_md` writes only 8 fields. A dropped key cannot come back.\n+\n+## Size of the gap\n+\n+SAH carries: `name` `description` `license` `compatibility` `context` `agent` `metadata` `allowed-tools` `profiles`.\n+\n+Claude Code also supports these 12, all dropped today: `when_to_use` `argument-hint` `arguments` `disable-model-invocation` `user-invocable` `disallowed-tools` `model` `effort` `background` `hooks` `paths` `shell`.\n+\n+Only `finish` uses `hooks` now, so only the ralph hook is broken. The other 11 fail as soon as anyone writes them.\n+\n+Agents are not affected. They deploy verbatim as symlinks into `~/.agents/<name>/`, so keys such as `skills:` and `disallowed-tools:` survive. That asymmetry is the defect.\n+\n+## Required change\n+\n+Make the round-trip lossless. Do not add a `hooks` field alone — that fixes one symptom and leaves 11.\n+\n+1. In `skill_loader.rs`, capture unknown keys with `#[serde(flatten)] extra: BTreeMap<String, serde_yaml_ng::Value>`.\n+2. Carry `extra` on `Skill` (`crates/swissarmyhammer-skills/src/skill.rs`).\n+3. In `deploy.rs`, flatten `extra` back out on serialize.\n+4. Subtract the SAH-internal keys. `profiles` must stay out of the deployed file. Use one shared named constant that both the parse and serialize sides read, so the internal set has a single source of truth.\n+\n+## Acceptance\n+\n+- Round-trip test: parse a SKILL.md that carries `hooks`, `model`, `paths`, and `disable-model-invocation`; format it; assert every key survives. This test must fail before the change.\n+- Test that `profiles` is present after parse and absent after format.\n+- After `sah init`, `~/.skills/finish/SKILL.md` contains the `hooks` block.\n+\n+Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,72 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: b680\n-title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n----\n-Skill install is a lossy parse-then-rebuild. It keeps an allowlist of 9 frontmatter fields and discards every other key without a warning.\n-\n-## Symptom\n-\n-`builtin/skills/finish/SKILL.md:12-16` declares a skill-scoped Stop hook:\n-\n-```yaml\n-hooks:\n-  Stop:\n-    - hooks:\n-        - type: command\n-          command: \"sah tool ralph ralph check --\"\n-```\n-\n-The deployed file has no `hooks` key:\n-\n-```\n-$ awk '/^---$/{n++; if(n==2) exit} {print}' ~/.skills/finish/SKILL.md\n----\n-name: finish\n-description: ...\n-license: MIT OR Apache-2.0\n-compatibility: Requires the `kanban` and `ralph` MCP tools plus a Stop-hook-capable harness.\n-metadata:\n-  author: swissarmyhammer\n-  version: 0.17.0\n-```\n-\n-`~/.claude/skills/finish` is a symlink to `~/.skills/finish`, so Claude Code reads the stripped file. Result: `/finish` calls `set ralph`, but no Stop hook exists to block the stop. The loop dies between iterations.\n-\n-`hooks` is an official Claude Code skill frontmatter field. See https://code.claude.com/docs/en/skills#frontmatter-reference and https://code.claude.com/docs/en/hooks#hooks-in-skills-and-agents. Skill-scoped hooks are active only while the skill runs, which is the correct behavior for `/finish`.\n-\n-## Cause\n-\n-Both sides of the round-trip use a closed struct:\n-\n-- `crates/swissarmyhammer-skills/src/skill_loader.rs:11-28` — `SkillFrontmatter` names 9 fields. Serde discards unknown keys. There is no `deny_unknown_fields`, so no error is raised.\n-- `crates/swissarmyhammer-skills/src/deploy.rs:27-44` — `format_skill_md` writes only 8 fields. A dropped key cannot come back.\n-\n-## Size of the gap\n-\n-SAH carries: `name` `description` `license` `compatibility` `context` `agent` `metadata` `allowed-tools` `profiles`.\n-\n-Claude Code also supports these 12, all dropped today: `when_to_use` `argument-hint` `arguments` `disable-model-invocation` `user-invocable` `disallowed-tools` `model` `effort` `background` `hooks` `paths` `shell`.\n-\n-Only `finish` uses `hooks` now, so only the ralph hook is broken. The other 11 fail as soon as anyone writes them.\n-\n-Agents are not affected. They deploy verbatim as symlinks into `~/.agents/<name>/`, so keys such as `skills:` and `disallowed-tools:` survive. That asymmetry is the defect.\n-\n-## Required change\n-\n-Make the round-trip lossless. Do not add a `hooks` field alone — that fixes one symptom and leaves 11.\n-\n-1. In `skill_loader.rs`, capture unknown keys with `#[serde(flatten)] extra: BTreeMap<String, serde_yaml_ng::Value>`.\n-2. Carry `extra` on `Skill` (`crates/swissarmyhammer-skills/src/skill.rs`).\n-3. In `deploy.rs`, flatten `extra` back out on serialize.\n-4. Subtract the SAH-internal keys. `profiles` must stay out of the deployed file. Use one shared named constant that both the parse and serialize sides read, so the internal set has a single source of truth.\n-\n-## Acceptance\n-\n-- Round-trip test: parse a SKILL.md that carries `hooks`, `model`, `paths`, and `disable-model-invocation`; format it; assert every key survives. This test must fail before the change.\n-- Test that `profiles` is present after parse and absent after format.\n-- After `sah init`, `~/.skills/finish/SKILL.md` contains the `hooks` block.\n-\n-Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYSFG1AB8YJEFWX1YCJSD1GY","timestamp":"2026-07-30T12:20:26.571188Z","op":"update","item_id":"01KYSFEYD7B6GAJ21H9T7EBYN8","forward_patch":"--- original\n+++ modified\n@@ -69,4 +69,4 @@\n - Test that `profiles` is present after parse and absent after format.\n - After `sah init`, `~/.skills/finish/SKILL.md` contains the `hooks` block.\n\n-Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works.\n\\ No newline at end of file\n+Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -69,4 +69,4 @@\n - Test that `profiles` is present after parse and absent after format.\n - After `sah init`, `~/.skills/finish/SKILL.md` contains the `hooks` block.\n\n-Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug\n\\ No newline at end of file\n+Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYSFG5738WRNVJ0P2K94TSWH","timestamp":"2026-07-30T12:20:30.563474Z","op":"update","item_id":"01KYSFEYD7B6GAJ21H9T7EBYN8","forward_patch":"--- original\n+++ modified\n@@ -69,4 +69,4 @@\n - Test that `profiles` is present after parse and absent after format.\n - After `sah init`, `~/.skills/finish/SKILL.md` contains the `hooks` block.\n\n-Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug\n\\ No newline at end of file\n+Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug #init\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -69,4 +69,4 @@\n - Test that `profiles` is present after parse and absent after format.\n - After `sah init`, `~/.skills/finish/SKILL.md` contains the `hooks` block.\n\n-Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug #init\n\\ No newline at end of file\n+Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYSFHND7X1WHG5XPB696TTHH","timestamp":"2026-07-30T12:21:19.911845Z","op":"update","item_id":"01KYSFEYD7B6GAJ21H9T7EBYN8","forward_patch":"--- original\n+++ modified\n@@ -69,4 +69,4 @@\n - Test that `profiles` is present after parse and absent after format.\n - After `sah init`, `~/.skills/finish/SKILL.md` contains the `hooks` block.\n\n-Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug #init\n\\ No newline at end of file\n+Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug #init #ralph\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -69,4 +69,4 @@\n - Test that `profiles` is present after parse and absent after format.\n - After `sah init`, `~/.skills/finish/SKILL.md` contains the `hooks` block.\n\n-Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug #init #ralph\n\\ No newline at end of file\n+Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug #init\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYVR0NWB0VHJ03242KT2XAWB","timestamp":"2026-07-31T09:27:49.387698Z","op":"update","item_id":"01KYSFEYD7B6GAJ21H9T7EBYN8","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,18 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01kyvr0nwbpcxnv6yemvs52aq0\n+  text: |-\n+    Picked up by /finish #bug (card 2). No prior attempts.\n+\n+    Live confirmation the defect is real, from this session: `add task` with `tags: [\"bug\",\"kanban\"]` still returned `tags: []` at 20:42 — because the fix from ^1t92gnj is committed but the running MCP server is the older binary. Same silent-drop family, different mechanism. Not this card's problem, but it shows the deploy/runtime split matters when verifying.\n+\n+    Scope reminder for whoever works this: the fix is NOT to add a `hooks` field to the two structs. That fixes one symptom of twelve. Claude Code supports 12 frontmatter fields SAH currently drops — `when_to_use`, `argument-hint`, `arguments`, `disable-model-invocation`, `user-invocable`, `disallowed-tools`, `model`, `effort`, `background`, `hooks`, `paths`, `shell`. Make the round-trip lossless with a flattened catch-all, and keep `profiles` (SAH-internal) out of the deployed file via one shared named constant.\n+\n+    Agents already round-trip correctly — they deploy verbatim as symlinks into `~/.agents/<name>/`. Only the skill path re-serializes through a closed struct. That asymmetry is the bug.\n+  timestamp: 2026-07-31T09:27:49.387475+00:00\n position_column: todo\n position_ordinal: b680\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n","reverse_patch":"--- original\n+++ modified\n@@ -1,18 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01kyvr0nwbpcxnv6yemvs52aq0\n-  text: |-\n-    Picked up by /finish #bug (card 2). No prior attempts.\n-\n-    Live confirmation the defect is real, from this session: `add task` with `tags: [\"bug\",\"kanban\"]` still returned `tags: []` at 20:42 — because the fix from ^1t92gnj is committed but the running MCP server is the older binary. Same silent-drop family, different mechanism. Not this card's problem, but it shows the deploy/runtime split matters when verifying.\n-\n-    Scope reminder for whoever works this: the fix is NOT to add a `hooks` field to the two structs. That fixes one symptom of twelve. Claude Code supports 12 frontmatter fields SAH currently drops — `when_to_use`, `argument-hint`, `arguments`, `disable-model-invocation`, `user-invocable`, `disallowed-tools`, `model`, `effort`, `background`, `hooks`, `paths`, `shell`. Make the round-trip lossless with a flattened catch-all, and keep `profiles` (SAH-internal) out of the deployed file via one shared named constant.\n-\n-    Agents already round-trip correctly — they deploy verbatim as symlinks into `~/.agents/<name>/`. Only the skill path re-serializes through a closed struct. That asymmetry is the bug.\n-  timestamp: 2026-07-31T09:27:49.387475+00:00\n position_column: todo\n position_ordinal: b680\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n","transaction_id":null}
{"id":"01KYVR1Q4FJ0GEF80S7R19Q8BQ","timestamp":"2026-07-31T09:28:23.439670Z","op":"update","item_id":"01KYSFEYD7B6GAJ21H9T7EBYN8","forward_patch":"--- original\n+++ modified\n@@ -13,8 +13,8 @@\n\n     Agents already round-trip correctly — they deploy verbatim as symlinks into `~/.agents/<name>/`. Only the skill path re-serializes through a closed struct. That asymmetry is the bug.\n   timestamp: 2026-07-31T09:27:49.387475+00:00\n-position_column: todo\n-position_ordinal: b680\n+position_column: doing\n+position_ordinal: '8280'\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n ---\n Skill install is a lossy parse-then-rebuild. It keeps an allowlist of 9 frontmatter fields and discards every other key without a warning.\n","reverse_patch":"--- original\n+++ modified\n@@ -13,8 +13,8 @@\n\n     Agents already round-trip correctly — they deploy verbatim as symlinks into `~/.agents/<name>/`. Only the skill path re-serializes through a closed struct. That asymmetry is the bug.\n   timestamp: 2026-07-31T09:27:49.387475+00:00\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: todo\n+position_ordinal: b680\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n ---\n Skill install is a lossy parse-then-rebuild. It keeps an allowlist of 9 frontmatter fields and discards every other key without a warning.\n","transaction_id":null}
{"id":"01KYVSA2VG5NTRSV4J4EGPNCNF","timestamp":"2026-07-31T09:50:26.160919Z","op":"update","item_id":"01KYSFEYD7B6GAJ21H9T7EBYN8","forward_patch":"--- original\n+++ modified\n@@ -13,6 +13,68 @@\n\n     Agents already round-trip correctly — they deploy verbatim as symlinks into `~/.agents/<name>/`. Only the skill path re-serializes through a closed struct. That asymmetry is the bug.\n   timestamp: 2026-07-31T09:27:49.387475+00:00\n+- actor: claude-code\n+  id: 01kyvsa2vgzjmdt9v22ebv5njn\n+  text: |-\n+    Implemented. Round-trip is now lossless.\n+\n+    ## Working tree on pickup\n+\n+    A prior run was killed mid-flight by an API error and left the tree half-applied: both tests existed, `skill.rs`/`skill_loader.rs` were done, but `deploy.rs` declared `SkillFrontmatter.extra` without ever constructing it. The tree did not compile. The design matched the card, so I kept it and finished it instead of starting again.\n+\n+    ## RED proved first\n+\n+    To watch the tests fail I reverted the production half (kept the tests), then ran them.\n+\n+    - Unit `test_format_skill_md_round_trips_unmodeled_frontmatter_keys` FAILED. Output showed the deployed frontmatter reduced to `name` + `description` + `license` — `hooks`, `model`, `paths`, `disable-model-invocation` all gone.\n+    - Real-path `init_profile_preserves_unmodeled_skill_frontmatter` FAILED.\n+\n+    Then I restored the production half and completed it.\n+\n+    ## Change\n+\n+    - `skill_loader.rs` — `#[serde(flatten)] extra: BTreeMap<String, serde_yaml_ng::Value>` catch-all, then `retain` to drop internal keys, so \"extra never holds an internal key\" is an invariant of every parsed Skill.\n+    - `skill.rs` — `pub extra` on `Skill`, plus `SAH_INTERNAL_FRONTMATTER_KEYS` as the one named constant both sides read.\n+    - `deploy.rs` — `SkillFrontmatter` gains a flattened `extra`, filled from `Skill::extra` and filtered again through the same constant.\n+\n+    ## Is any key besides `profiles` SAH-internal? No — checked, not assumed\n+\n+    The loader names 9 keys; deploy writes 8. The set difference is exactly `{profiles}`. `context` and `agent` look SAH-specific but deploy already writes them today and `test_format_skill_md_round_trips_context_and_agent` pins that behavior, so they are not internal. `validation.rs` gates only `name`/`description` and treats no key as internal. So `SAH_INTERNAL_FRONTMATTER_KEYS = [\"profiles\"]` is correct.\n+\n+    ## Second live casualty found\n+\n+    The real-path test failed first not on `finish`, but on `double-check` — `builtin/skills/double-check/SKILL.md` carries `background: false`, which was also being dropped. So the bug had two live victims, not one. Both survive now.\n+\n+    ## Real-path evidence, not just unit\n+\n+    Built `sah` and ran a genuine `sah init project` into a temp dir with HOME redirected. The deployed `.skills/finish/SKILL.md` now carries the full block:\n+\n+    ```\n+    hooks:\n+      Stop:\n+      - hooks:\n+        - type: command\n+          command: sah tool ralph ralph check --\n+    ```\n+\n+    `profiles:` does not leak (0 hits). `double-check`'s `background: false` is present. Note the nested sequence re-indents (2 spaces vs 4 in source) — serde_yaml_ng's emitter style. Semantically identical YAML; the tests compare parsed values, not text.\n+\n+    ## Flatten hazard checked empirically\n+\n+    The card flagged flatten vs the renamed `allowed-tools`. Covered by `test_format_skill_md_writes_each_modeled_key_once`, which re-parses the output and counts each key exactly once.\n+\n+    I also probed a hazard the card did not name: `#[serde(flatten)]` routes sibling fields through `deserialize_any`, which can break `metadata: HashMap<String, String>` when a value is an unquoted YAML scalar. Ran a live probe with `version: 1.0`, `count: 3`, `flag: true` — all still coerce to strings. No regression.\n+\n+    ## Test status\n+\n+    `cargo nextest run -E 'rdeps(swissarmyhammer-skills)'` — 10080 run, 10079 pass. `cargo fmt` and `cargo clippy --workspace --all-targets -- -D warnings` clean.\n+\n+    The single failure `work_the_card_skills_render_record_progress_guidance` is NOT mine. It asserts on builtin skill BODY text; `builtin/skills/finish/SKILL.md` has no `_partials/record-progress` include at HEAD nor in the working tree. I proved it by reverting my whole change and re-running that test alone — it fails identically. `builtin/` is off-limits on this card (the user edits it in parallel).\n+\n+    ## What did not work\n+\n+    `files edit` with `replace_all: true` reported `replacements_made: 1` and patched only the first of five identical `Skill { .. }` literals. Do not trust that flag here — verify the count, or use a `perl -0pi` pass as I did for the remaining four.\n+  timestamp: 2026-07-31T09:50:26.160646+00:00\n position_column: doing\n position_ordinal: '8280'\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n","reverse_patch":"--- original\n+++ modified\n@@ -13,68 +13,6 @@\n\n     Agents already round-trip correctly — they deploy verbatim as symlinks into `~/.agents/<name>/`. Only the skill path re-serializes through a closed struct. That asymmetry is the bug.\n   timestamp: 2026-07-31T09:27:49.387475+00:00\n-- actor: claude-code\n-  id: 01kyvsa2vgzjmdt9v22ebv5njn\n-  text: |-\n-    Implemented. Round-trip is now lossless.\n-\n-    ## Working tree on pickup\n-\n-    A prior run was killed mid-flight by an API error and left the tree half-applied: both tests existed, `skill.rs`/`skill_loader.rs` were done, but `deploy.rs` declared `SkillFrontmatter.extra` without ever constructing it. The tree did not compile. The design matched the card, so I kept it and finished it instead of starting again.\n-\n-    ## RED proved first\n-\n-    To watch the tests fail I reverted the production half (kept the tests), then ran them.\n-\n-    - Unit `test_format_skill_md_round_trips_unmodeled_frontmatter_keys` FAILED. Output showed the deployed frontmatter reduced to `name` + `description` + `license` — `hooks`, `model`, `paths`, `disable-model-invocation` all gone.\n-    - Real-path `init_profile_preserves_unmodeled_skill_frontmatter` FAILED.\n-\n-    Then I restored the production half and completed it.\n-\n-    ## Change\n-\n-    - `skill_loader.rs` — `#[serde(flatten)] extra: BTreeMap<String, serde_yaml_ng::Value>` catch-all, then `retain` to drop internal keys, so \"extra never holds an internal key\" is an invariant of every parsed Skill.\n-    - `skill.rs` — `pub extra` on `Skill`, plus `SAH_INTERNAL_FRONTMATTER_KEYS` as the one named constant both sides read.\n-    - `deploy.rs` — `SkillFrontmatter` gains a flattened `extra`, filled from `Skill::extra` and filtered again through the same constant.\n-\n-    ## Is any key besides `profiles` SAH-internal? No — checked, not assumed\n-\n-    The loader names 9 keys; deploy writes 8. The set difference is exactly `{profiles}`. `context` and `agent` look SAH-specific but deploy already writes them today and `test_format_skill_md_round_trips_context_and_agent` pins that behavior, so they are not internal. `validation.rs` gates only `name`/`description` and treats no key as internal. So `SAH_INTERNAL_FRONTMATTER_KEYS = [\"profiles\"]` is correct.\n-\n-    ## Second live casualty found\n-\n-    The real-path test failed first not on `finish`, but on `double-check` — `builtin/skills/double-check/SKILL.md` carries `background: false`, which was also being dropped. So the bug had two live victims, not one. Both survive now.\n-\n-    ## Real-path evidence, not just unit\n-\n-    Built `sah` and ran a genuine `sah init project` into a temp dir with HOME redirected. The deployed `.skills/finish/SKILL.md` now carries the full block:\n-\n-    ```\n-    hooks:\n-      Stop:\n-      - hooks:\n-        - type: command\n-          command: sah tool ralph ralph check --\n-    ```\n-\n-    `profiles:` does not leak (0 hits). `double-check`'s `background: false` is present. Note the nested sequence re-indents (2 spaces vs 4 in source) — serde_yaml_ng's emitter style. Semantically identical YAML; the tests compare parsed values, not text.\n-\n-    ## Flatten hazard checked empirically\n-\n-    The card flagged flatten vs the renamed `allowed-tools`. Covered by `test_format_skill_md_writes_each_modeled_key_once`, which re-parses the output and counts each key exactly once.\n-\n-    I also probed a hazard the card did not name: `#[serde(flatten)]` routes sibling fields through `deserialize_any`, which can break `metadata: HashMap<String, String>` when a value is an unquoted YAML scalar. Ran a live probe with `version: 1.0`, `count: 3`, `flag: true` — all still coerce to strings. No regression.\n-\n-    ## Test status\n-\n-    `cargo nextest run -E 'rdeps(swissarmyhammer-skills)'` — 10080 run, 10079 pass. `cargo fmt` and `cargo clippy --workspace --all-targets -- -D warnings` clean.\n-\n-    The single failure `work_the_card_skills_render_record_progress_guidance` is NOT mine. It asserts on builtin skill BODY text; `builtin/skills/finish/SKILL.md` has no `_partials/record-progress` include at HEAD nor in the working tree. I proved it by reverting my whole change and re-running that test alone — it fails identically. `builtin/` is off-limits on this card (the user edits it in parallel).\n-\n-    ## What did not work\n-\n-    `files edit` with `replace_all: true` reported `replacements_made: 1` and patched only the first of five identical `Skill { .. }` literals. Do not trust that flag here — verify the count, or use a `perl -0pi` pass as I did for the remaining four.\n-  timestamp: 2026-07-31T09:50:26.160646+00:00\n position_column: doing\n position_ordinal: '8280'\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n","transaction_id":null}
{"id":"01KYVTE349RA5N9TMZR33E1CFM","timestamp":"2026-07-31T10:10:06.089169Z","op":"update","item_id":"01KYSFEYD7B6GAJ21H9T7EBYN8","forward_patch":"--- original\n+++ modified\n@@ -75,6 +75,52 @@\n\n     `files edit` with `replace_all: true` reported `replacements_made: 1` and patched only the first of five identical `Skill { .. }` literals. Do not trust that flag here — verify the count, or use a `perl -0pi` pass as I did for the remaining four.\n   timestamp: 2026-07-31T09:50:26.160646+00:00\n+- actor: claude-code\n+  id: 01kyvte348ay1ktwch5eztprn1\n+  text: |-\n+    Adversarial review round. Verdict was REVISE with 8 findings. Fixed 6, deferred 1 to a new card, left 1 deliberately.\n+\n+    ## The finding that mattered — my internal-key filter was untested\n+\n+    The reviewer proved you could delete `SAH_INTERNAL_FRONTMATTER_KEYS` and BOTH filter sites and every test still passed. Reason: `profiles` is a *named* field on both frontmatter structs, so serde routes it there and `Skill::extra` can never hold it. What actually kept `profiles` out of the deployed file was the absence of the field on the deploy struct — not my new code. My \"profiles absent after format\" test was passing for the wrong reason.\n+\n+    Added `test_format_skill_md_drops_internal_keys_planted_in_extra`, which hand-builds a `Skill` with the internal key planted directly in `extra` — the only route that reaches the filter. Verified it is load-bearing: removed the filter and it went RED with `profiles: - kanban` leaking into the output, then restored.\n+\n+    ## Second real finding — my duplicate-key comment was factually wrong\n+\n+    I wrote \"serde_yaml_ng rejects duplicate mapping keys\". It does not. `DuplicateKeyError` lives only in the `Deserialize` impl for `Mapping`; it never runs on a derived struct. What actually caught a duplicated *modeled* key was `serde_derive`'s `duplicate_field` error, which it does emit on the flatten path.\n+\n+    Worse, the `frontmatter_map` test helper deserialized into `BTreeMap<String, Value>`, which keeps the last value silently — so all three key-comparison tests were blind to duplicates, the exact `#[serde(flatten)]` hazard the card told me to prove I avoided.\n+\n+    Changed both copies of `frontmatter_map` (skills + mirdan) to return `serde_yaml_ng::Mapping`, which makes duplicate rejection real, and corrected the comment to attribute each check to the right layer.\n+\n+    ## Confirmed: the renamed `allowed-tools` cannot double-write\n+\n+    `deserialize_field_identifier` matches the serde name and only falls through to the catch-all when nothing matches. The rename is safe, and the occurrence-count assertions are genuine, not vacuous.\n+\n+    ## Other fixes\n+\n+    - Both tests now read `SAH_INTERNAL_FRONTMATTER_KEYS` instead of hardcoding `\"profiles\"`. Before this, adding a second internal key would have made the mirdan test FAIL — it would have asserted the new internal key survived deploy, which by design it must not. \"Single source of truth\" was a claim the tests contradicted.\n+    - Re-exported the const from the crate root, consistent with the other `skill::` items.\n+    - Documented on `Skill::extra` that unmodeled keys are copied verbatim and are NOT Liquid-rendered. Only the body and `metadata` values are. A `{{version}}` inside `hooks:` deploys literally. Newly reachable, since before this change unmodeled keys never reached the deployed file at all.\n+    - Filled the `format_skill_md` doc gap — it still described only the 8 modeled fields.\n+\n+    ## Deferred to a new card\n+\n+    ^ksys4z5 — the AGENT.md path has the identical defect. `Agent::to_agent_md` rebuilds frontmatter from a closed struct with no catch-all, and `install_profile_agents` calls it on every init. I verified this directly. **This card's own description is wrong** where it says \"Agents are not affected. They deploy verbatim as symlinks\" — `to_agent_md` is a real re-serialize path. Not biting yet (the 8 builtin AGENT.md files use only modeled keys). That function also concatenates YAML by hand with no escaping, which the new card covers too.\n+\n+    ## Left alone deliberately\n+\n+    The reviewer wanted the duplicated `frontmatter_map` helper hoisted into one shared place. The two copies live in different crates and `split_frontmatter` is private, so sharing would mean making a test-only helper public across a crate boundary — worse than two small copies. Both now carry the same `Mapping` semantics.\n+\n+    Also kept the loader-side `retain`, which is defensive rather than currently load-bearing (serde already routes `profiles` to its named field). The card requires one constant that both sides read, and the retain keeps the invariant true if that named field is ever reworked. Its comment already says exactly this.\n+\n+    ## Verification after the revisions\n+\n+    `cargo nextest run -E 'rdeps(swissarmyhammer-skills)'` — 10081 run, 10080 pass. `cargo fmt` and `cargo clippy --workspace --all-targets -- -D warnings` clean. Rebuilt `sah`, re-ran a real `sah init project` in a temp sandbox: the deployed `finish/SKILL.md` carries the full `hooks:` Stop block, zero files under `.skills/` contain `profiles:`, and `double-check` keeps `background: false`. Deploy is idempotent — two consecutive inits produce byte-identical output.\n+\n+    Still the one pre-existing failure, `work_the_card_skills_render_record_progress_guidance`, proven unrelated by reverting the whole change.\n+  timestamp: 2026-07-31T10:10:06.088834+00:00\n position_column: doing\n position_ordinal: '8280'\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n","reverse_patch":"--- original\n+++ modified\n@@ -75,52 +75,6 @@\n\n     `files edit` with `replace_all: true` reported `replacements_made: 1` and patched only the first of five identical `Skill { .. }` literals. Do not trust that flag here — verify the count, or use a `perl -0pi` pass as I did for the remaining four.\n   timestamp: 2026-07-31T09:50:26.160646+00:00\n-- actor: claude-code\n-  id: 01kyvte348ay1ktwch5eztprn1\n-  text: |-\n-    Adversarial review round. Verdict was REVISE with 8 findings. Fixed 6, deferred 1 to a new card, left 1 deliberately.\n-\n-    ## The finding that mattered — my internal-key filter was untested\n-\n-    The reviewer proved you could delete `SAH_INTERNAL_FRONTMATTER_KEYS` and BOTH filter sites and every test still passed. Reason: `profiles` is a *named* field on both frontmatter structs, so serde routes it there and `Skill::extra` can never hold it. What actually kept `profiles` out of the deployed file was the absence of the field on the deploy struct — not my new code. My \"profiles absent after format\" test was passing for the wrong reason.\n-\n-    Added `test_format_skill_md_drops_internal_keys_planted_in_extra`, which hand-builds a `Skill` with the internal key planted directly in `extra` — the only route that reaches the filter. Verified it is load-bearing: removed the filter and it went RED with `profiles: - kanban` leaking into the output, then restored.\n-\n-    ## Second real finding — my duplicate-key comment was factually wrong\n-\n-    I wrote \"serde_yaml_ng rejects duplicate mapping keys\". It does not. `DuplicateKeyError` lives only in the `Deserialize` impl for `Mapping`; it never runs on a derived struct. What actually caught a duplicated *modeled* key was `serde_derive`'s `duplicate_field` error, which it does emit on the flatten path.\n-\n-    Worse, the `frontmatter_map` test helper deserialized into `BTreeMap<String, Value>`, which keeps the last value silently — so all three key-comparison tests were blind to duplicates, the exact `#[serde(flatten)]` hazard the card told me to prove I avoided.\n-\n-    Changed both copies of `frontmatter_map` (skills + mirdan) to return `serde_yaml_ng::Mapping`, which makes duplicate rejection real, and corrected the comment to attribute each check to the right layer.\n-\n-    ## Confirmed: the renamed `allowed-tools` cannot double-write\n-\n-    `deserialize_field_identifier` matches the serde name and only falls through to the catch-all when nothing matches. The rename is safe, and the occurrence-count assertions are genuine, not vacuous.\n-\n-    ## Other fixes\n-\n-    - Both tests now read `SAH_INTERNAL_FRONTMATTER_KEYS` instead of hardcoding `\"profiles\"`. Before this, adding a second internal key would have made the mirdan test FAIL — it would have asserted the new internal key survived deploy, which by design it must not. \"Single source of truth\" was a claim the tests contradicted.\n-    - Re-exported the const from the crate root, consistent with the other `skill::` items.\n-    - Documented on `Skill::extra` that unmodeled keys are copied verbatim and are NOT Liquid-rendered. Only the body and `metadata` values are. A `{{version}}` inside `hooks:` deploys literally. Newly reachable, since before this change unmodeled keys never reached the deployed file at all.\n-    - Filled the `format_skill_md` doc gap — it still described only the 8 modeled fields.\n-\n-    ## Deferred to a new card\n-\n-    ^ksys4z5 — the AGENT.md path has the identical defect. `Agent::to_agent_md` rebuilds frontmatter from a closed struct with no catch-all, and `install_profile_agents` calls it on every init. I verified this directly. **This card's own description is wrong** where it says \"Agents are not affected. They deploy verbatim as symlinks\" — `to_agent_md` is a real re-serialize path. Not biting yet (the 8 builtin AGENT.md files use only modeled keys). That function also concatenates YAML by hand with no escaping, which the new card covers too.\n-\n-    ## Left alone deliberately\n-\n-    The reviewer wanted the duplicated `frontmatter_map` helper hoisted into one shared place. The two copies live in different crates and `split_frontmatter` is private, so sharing would mean making a test-only helper public across a crate boundary — worse than two small copies. Both now carry the same `Mapping` semantics.\n-\n-    Also kept the loader-side `retain`, which is defensive rather than currently load-bearing (serde already routes `profiles` to its named field). The card requires one constant that both sides read, and the retain keeps the invariant true if that named field is ever reworked. Its comment already says exactly this.\n-\n-    ## Verification after the revisions\n-\n-    `cargo nextest run -E 'rdeps(swissarmyhammer-skills)'` — 10081 run, 10080 pass. `cargo fmt` and `cargo clippy --workspace --all-targets -- -D warnings` clean. Rebuilt `sah`, re-ran a real `sah init project` in a temp sandbox: the deployed `finish/SKILL.md` carries the full `hooks:` Stop block, zero files under `.skills/` contain `profiles:`, and `double-check` keeps `background: false`. Deploy is idempotent — two consecutive inits produce byte-identical output.\n-\n-    Still the one pre-existing failure, `work_the_card_skills_render_record_progress_guidance`, proven unrelated by reverting the whole change.\n-  timestamp: 2026-07-31T10:10:06.088834+00:00\n position_column: doing\n position_ordinal: '8280'\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n","transaction_id":null}
{"id":"01KYVX4YE9P5BAPKCDTGEA1WVR","timestamp":"2026-07-31T10:57:32.105498Z","op":"update","item_id":"01KYSFEYD7B6GAJ21H9T7EBYN8","forward_patch":"--- original\n+++ modified\n@@ -121,7 +121,7 @@\n\n     Still the one pre-existing failure, `work_the_card_skills_render_record_progress_guidance`, proven unrelated by reverting the whole change.\n   timestamp: 2026-07-31T10:10:06.088834+00:00\n-position_column: doing\n+position_column: review\n position_ordinal: '8280'\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -121,7 +121,7 @@\n\n     Still the one pre-existing failure, `work_the_card_skills_render_record_progress_guidance`, proven unrelated by reverting the whole change.\n   timestamp: 2026-07-31T10:10:06.088834+00:00\n-position_column: review\n+position_column: doing\n position_ordinal: '8280'\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n ---\n","transaction_id":null}
{"id":"01KYVX7B1AFDR31MCKCRZEGMS8","timestamp":"2026-07-31T10:58:50.538605Z","op":"update","item_id":"01KYSFEYD7B6GAJ21H9T7EBYN8","forward_patch":"--- original\n+++ modified\n@@ -189,4 +189,63 @@\n - Test that `profiles` is present after parse and absent after format.\n - After `sah init`, `~/.skills/finish/SKILL.md` contains the `hooks` block.\n\n-Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug #init #ralph\n\\ No newline at end of file\n+Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug #init #ralph\n+\n+## Review Findings (2026-07-31 05:26)\n+\n+Scope: `f7fe832a3^..f7fe832a3`.\n+\n+Triage note: of the 38 findings below, exactly one — the `deploy.rs` reparse assertion — lands on code this commit introduced. The other 37 are on pre-existing code the commit only made prominent: `crates/mirdan/src/install.rs` received a single 97-line test hunk at line 3523, yet every `install.rs` finding cites lines 143-2469. See the split proposal at the end.\n+\n+- [ ] `crates/mirdan/src/install.rs:143` — Default version string \"0.0.0\" is repeated throughout the file instead of being a named constant — changes to the default version must happen in multiple places, making the codebase harder to maintain. Define `const DEFAULT_VERSION: &str = \"0.0.0\";` at module level and use it everywhere instead of the literal string.\n+- [ ] `crates/mirdan/src/install.rs:172` — Repeated PackageType dispatch pattern for deploying packages — duplicates the pattern at lines 109, 115, 204, 230, 252, 338. Extract into a helper function or registry to eliminate the repeated match statement.\n+- [ ] `crates/mirdan/src/install.rs:173` — Repeated \"0.0.0\" default version literal. Use a named constant DEFAULT_VERSION instead.\n+- [ ] `crates/mirdan/src/install.rs:189` — Repeated \"0.0.0\" default version literal. Use a named constant DEFAULT_VERSION instead.\n+- [ ] `crates/mirdan/src/install.rs:204` — Repeated PackageType dispatch pattern for reading package versions — duplicates the pattern at lines 109, 115, 172, 230, 252, 338. Consolidate into a single version-reading function that takes PackageType and resolves the manifest filename.\n+- [ ] `crates/mirdan/src/install.rs:208` — Repeated \"0.0.0\" default version literal. Use a named constant DEFAULT_VERSION instead.\n+- [ ] `crates/mirdan/src/install.rs:230` — Repeated PackageType dispatch pattern for reading versions with fallback — duplicates the pattern at lines 109, 115, 172, 204, 252, 338. Use a shared helper function instead of repeating this match pattern.\n+- [ ] `crates/mirdan/src/install.rs:234` — Repeated \"0.0.0\" default version literal. Use a named constant DEFAULT_VERSION instead.\n+- [ ] `crates/mirdan/src/install.rs:252` — Repeated PackageType dispatch pattern for deploying packages — duplicates the pattern at lines 109, 115, 172, 204, 230, 338. Create a PackageType-to-deploy-function mapping to eliminate repeated match arms.\n+- [ ] `crates/mirdan/src/install.rs:299` — YAML frontmatter delimiter \"---\" is hardcoded instead of being a named constant — appears twice and should be centralized. Define `const FRONTMATTER_DELIMITER: &str = \"---\";` and use it instead of the literal string.\n+- [ ] `crates/mirdan/src/install.rs:305` — Repeated YAML frontmatter delimiter \"---\" literal. Use a named constant FRONTMATTER_DELIMITER instead.\n+- [ ] `crates/mirdan/src/install.rs:389` — Repeated \"0.0.0\" default version literal. Use a named constant DEFAULT_VERSION instead.\n+- [ ] `crates/mirdan/src/install.rs:737` — `deploy_agent_to_agents` is an identical one-liner wrapper to `deploy_skill_to_agents` (line 630), both just delegating to their respective `_at` variants with `root: None`. This duplication could be eliminated by a single generic wrapper function, though the duplication is minimal. If `deploy_skill_to_agents_at` and `deploy_agent_to_agents_at` are refactored into a shared generic function, these wrapper functions would also naturally unify. If they remain separate, this duplication is acceptable as a thin wrapper pattern.\n+- [ ] `crates/mirdan/src/install.rs:756` — `deploy_agent_to_agents_at` (line 756) is nearly an exact copy of `deploy_skill_to_agents_at` (line 645). Both functions load agents, resolve targets, copy source to store, and create symlinks from each agent's directory back to the store. The only differences are which store directory getter is used (`agent_store_dir` vs `skill_store_dir`) and how the agent directory is retrieved (with Optional handling in agent vs. direct retrieval in skill). These should share a generic implementation parameterized by store-directory and agent-directory getters. Extract a generic `fn deploy_to_store<F, G>(name, source_dir, agent_filter, global, root, store_dir_fn: F, agent_dir_fn: G) -> Result<Vec<String>>` where F and G are closures that provide the store directory and agent directory getter for each agent. Have both functions call this generic implementation with appropriate closures.\n+- [ ] `crates/mirdan/src/install.rs:823` — `deploy_skill` (line 823) is identical to `deploy_agent` (line 802) — both are async wrappers that accept the same parameters and simply delegate to their respective `_to_agents` function without adding any logic. The functions are completely parallel and should be unified into a single generic async wrapper parameterized by which `_to_agents` function to call. Create a generic async wrapper `async fn deploy_to_agents<F>(name, source_dir, agent_filter, global, deploy_fn: F) -> Result<Vec<String>>` and have both `deploy_skill` and `deploy_agent` call it with their respective `_to_agents` function as a parameter, or convert one to call the other with explicit function reference.\n+- [ ] `crates/mirdan/src/install.rs:1030` — Function `run_uninstall` is long (~75 lines) with multiple conditional branches and a nested match-within-conditional structure. It handles three different resolution paths (exact match, display name, git source) with complex nesting in the git source branch, making the decision logic hard to follow. Extract the three resolution strategies into separate functions: `fn resolve_by_exact_match(...)`, `fn resolve_by_display_name(...)`, `fn resolve_by_git_source(...)` and call them in sequence. This linearizes the logic and reduces branch complexity.\n+- [ ] `crates/mirdan/src/install.rs:1120` — Function `remove_matching_store_entries` has deep nesting (4-5 levels) combining nested conditionals, loop-within-loop structure (for loop with nested while loop), and recursion, making it hard to follow the control flow. Extract the parent-cleanup logic (`while let Some(p) = parent { ... }`) into a separate `fn prune_empty_parent_dirs(...)` to reduce nesting to 2-3 levels. Extract the name-matching logic into a separate `fn is_matching_skill(path, skill_md, name)` function.\n+- [ ] `crates/mirdan/src/install.rs:1340` — `install_profile_skills` and `install_profile_agents` (line 1404) follow nearly identical structure: resolve builtins → create available map → create template library/context → select items → for each selected: render content → deploy → merge targets → write README → report. The actual per-item operations differ (rendering strategy, file name, formatter), but the outer loop and orchestration pattern are duplicated across both functions. Extract a shared orchestration function that handles the select-render-deploy-report pattern, parameterized by resolver, renderer, formatter, and store-directory getter. This would reduce duplication while preserving the different rendering strategies needed for skills vs. agents.\n+- [ ] `crates/mirdan/src/install.rs:1422` — Function `deploy_plugin` mirrors `deploy_tool`'s nesting pattern: loop with nested if-let and conditional MCP handling, plus additional bundled .mcp.json parsing with deeply nested conditionals (4-5 levels in the .mcp.json block). Extract bundled MCP registration into a separate `fn register_bundled_mcp_from_plugin(plugin_path, agent, mcp_cfg, global) -> Result<()>` to reduce nesting in the main loop.\n+- [ ] `crates/mirdan/src/install.rs:1435` — Function `uninstall_tool` has a loop with nested if-let and sequential if statements (4 levels of nesting), making the MCP unregistration logic harder to follow — mirrors the issue in `deploy_tool` and `run_install_mcp` but with the unregister path. Extract the MCP unregistration logic per agent into a separate `fn unregister_tool_mcp_for_agent(agent, global) -> Result<bool>` to reduce loop body nesting to 1-2 levels.\n+- [ ] `crates/mirdan/src/install.rs:1555` — Function `uninstall_agent_at` has multiple nested conditionals and loops (for loop with nested if let, nested if statements) plus a second loop with optional logic, creating a complex decision tree. Extract symlink removal into `fn uninstall_agent_symlinks(...)` and reference checking into `fn should_remove_agent_store_entry(store_path, agent_dirs) -> bool` to linearize the two-loop structure.\n+- [ ] `crates/mirdan/src/install.rs:1785` — The function `read_skill_frontmatter_name` reimplements the YAML frontmatter extraction and parsing logic that already exists in `read_frontmatter` (line 304). Both functions follow the identical pattern: trim content → strip/skip opening `---` → find closing `---` → extract substring → parse YAML. The only difference is the return type (String vs. (String, String)) and error handling strategy (Option vs. Result). This duplication could be eliminated by having `read_skill_frontmatter_name` call `read_frontmatter` and extract the name, or by refactoring both to use a shared helper. Refactor `read_skill_frontmatter_name` to call `read_frontmatter` and extract just the name field from the result, avoiding the duplication. Alternatively, create a shared `fn parse_yaml_frontmatter(content: &str) -> Result<serde_yaml_ng::Value, ...>` helper and have both functions use it.\n+- [ ] `crates/mirdan/src/install.rs:1988` — Public function `uninstall_skill` lacks doc comment. Public entry points must document their contract, parameters, and error conditions. Add doc comment describing the function's purpose, parameters, and error conditions. Consider: `/// Uninstall a skill by name from all detected agents.\n+/// # Errors\n+/// Returns an error if the skill is not found in any agent directory.`.\n+- [ ] `crates/mirdan/src/install.rs:2469` — `resolved_agent_names` (line 2469) reimplements the same pattern as `resolved_skill_names` (line 2458). Both create a resolver, build an available map from builtins, and call `selector.select(&available)`. The only difference is which resolver is used (SkillResolver vs. AgentResolver) and how the available map is constructed (skills use profiles, agents use empty Vec). This duplication should be eliminated by parameterizing the resolver type or creating a shared helper. Create a generic `fn resolved_builtin_names<R: Resolver>(selector, resolver) -> Vec<String>` helper that takes a resolver instance, then have both functions call it with their respective resolver types. Or parameterize the profile-handling logic separately.\n+- [ ] `crates/swissarmyhammer-skills/src/deploy.rs:18` — Public library function returns `Result<Skill, String>` instead of a typed error enum. Libraries must return typed error enums via `thiserror` so callers can match on specific failures; returning bare `String` forces error handling via string parsing. Change return type to `Result<Skill, SkillError>` and convert the error construction at line 23 to use the typed error enum.\n+- [ ] `crates/swissarmyhammer-skills/src/deploy.rs:323` — Test `test_format_skill_md_round_trips_unmodeled_frontmatter_keys` claims to test round-tripping but only verifies parse → format, not the full parse → format → parse cycle. This leaves the deserialization direction untested for complex nested structures like the `hooks` YAML value. Add a reparse step to verify the full round-trip: `let reparsed = crate::skill_loader::parse_skill_md(&md, SkillSource::Builtin).expect(...); assert_eq!(reparsed.extra, skill.extra);` to prove that complex unmodeled keys (including nested structures) survive both serialization and deserialization.\n+- [ ] `crates/swissarmyhammer-skills/src/skill.rs:28` — Function returns `Result` but does not document error cases in its doc comment. Callers need to know what validation failures are possible. Add an `# Errors` section documenting the two validation failure cases: when the name is empty, and when it contains characters outside the allowed set.\n+- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:30` — Public library function returns `Result<Skill, String>` instead of a typed error enum. Callers cannot distinguish between different parse failures (malformed YAML, missing fields, invalid names) when errors are just strings. Change return type to `Result<Skill, SkillError>` and wrap the various error cases (serde errors, validation errors, name construction errors) in the typed error enum.\n+- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:30` — Function returns `Result` but does not document error cases in its doc comment. Callers need to know what parse failures are possible. Add an `# Errors` section documenting the possible parse failures: YAML parse errors, missing required fields, invalid skill names, and validation errors.\n+- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:37` — Public library function returns `Result<Skill, String>` instead of a typed error enum. This is the primary parse path used by callers; String errors hide the kind of parse failure. Change return type to `Result<Skill, SkillError>` and route all error types through the typed enum.\n+- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:37` — Function returns `Result` but does not document error cases in its doc comment. Add an `# Errors` section documenting the same parse failure modes as `parse_skill_md`.\n+- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:87` — Public library function returns `Result<Skill, String>` instead of a typed error enum. File I/O errors, parse errors, and walk errors should be distinguished so callers can handle them differently. Change return type to `Result<Skill, SkillError>` and wrap file I/O errors in the typed error enum using `.context()` or `.map_err()`.\n+- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:87` — Function returns `Result` but does not document error cases in its doc comment. File I/O and parse failures are both possible. Add an `# Errors` section documenting when the function fails: when `SKILL.md` does not exist, when reading files fails, or when parsing fails.\n+- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:123` — Repeated literal `\"SKILL.md\"` (as `\"/SKILL.md\"` and plain `\"SKILL.md\"`) appears here, line 103, and line 132. Should use a named constant. Define and use const SKILL_FILENAME: &str = \"SKILL.md\"; throughout, e.g., name.ends_with(&format!(\"/{}\", SKILL_FILENAME)) || *name == SKILL_FILENAME.\n+- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:124` — Function returns `Result` but does not document error cases in its doc comment. Add an `# Errors` section documenting when the function fails: when no SKILL.md is found in the files list, or when parsing fails.\n+- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:177` — Magic number 3 represents the length of the repeated delimiter `\"---\"` from lines 175 and 182. Should use a constant's len() method for clarity and to prevent drift if the delimiter ever changes. Replace &content[3..] with &content[FRONTMATTER_DELIMITER.len()..].\n+- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:182` — Repeated literal `\"\\n---\"` used in find call here is derived from the same YAML delimiter hardcoded at line 175. Should extract to a named constant so the delimiter changes in one place. Define const FRONTMATTER_CLOSE: &str = \"\\n---\"; and use find(FRONTMATTER_CLOSE), or use find(&format!(\"\\n{}\", FRONTMATTER_DELIMITER)).\n+- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:184` — Magic number 4 represents the length of `\"\\n---\"` hardcoded at line 182. Should use a constant's len() method so it scales if the delimiter changes. Replace end_pos + 4 with end_pos + FRONTMATTER_CLOSE.len().\n+\n+### Split proposal (so this card can close)\n+\n+The engine's line numbers track the pre-image, so they are offset; classification below is by subject, checked against the commit's actual hunks.\n+\n+This commit's hunks were: `install.rs` +3523..3619 (test only); `deploy.rs` +17, +44..50, +91..96, +128..137 and new tests from +380; `skill.rs` +7..25, +116..136; `skill_loader.rs` +28..38, +70..76, +87; `lib.rs` line 36.\n+\n+- Introduced by this commit (1): the `deploy.rs` reparse assertion on `test_format_skill_md_round_trips_unmodeled_frontmatter_keys` (new test at real line 426). It asserts key survival through a frontmatter map but never re-enters `parse_skill_md` to compare `extra`. Adding an assertion to a new test is in scope. Fix here.\n+- Pre-existing, `crates/mirdan/src/install.rs` (24): every one cites lines 143-2469 while the commit's only hunk is a 97-line test block at 3523. None of this code was touched. Propose one card: \"mirdan install.rs: DEFAULT_VERSION / FRONTMATTER_DELIMITER constants, PackageType dispatch dedup, deploy_skill/deploy_agent generic unification, nesting extraction in run_uninstall / remove_matching_store_entries / deploy_plugin / uninstall_tool / uninstall_agent_at, read_skill_frontmatter_name reuse, uninstall_skill doc\".\n+- Pre-existing, `swissarmyhammer-skills` public signatures and docs (12): `resolve_skill`, `parse_skill_md`, `parse_skill_md_with_path`, `load_skill_from_dir`, `load_skill_from_builtin`, `SkillName::new` — all untouched signatures returning `Result<_, String>` and lacking `# Errors`; plus the `SKILL.md` and `---` delimiter literals in the pre-existing `split_frontmatter`. Propose one card: \"swissarmyhammer-skills: typed SkillError enum + `# Errors` docs + frontmatter/filename constants\".\n+\n+Verifier observations judged, none raised as findings: the unreachable `extra.retain(...)` is documented defence-in-depth and the dead-code validator did not confirm it; the vacuous-loop risk if `SAH_INTERNAL_FRONTMATTER_KEYS` were emptied is real but the engine did not raise it, and a non-empty assertion is optional; `extra` values being copied without Liquid rendering is a documented contract, not a defect.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -189,63 +189,4 @@\n - Test that `profiles` is present after parse and absent after format.\n - After `sah init`, `~/.skills/finish/SKILL.md` contains the `hooks` block.\n\n-Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug #init #ralph\n-\n-## Review Findings (2026-07-31 05:26)\n-\n-Scope: `f7fe832a3^..f7fe832a3`.\n-\n-Triage note: of the 38 findings below, exactly one — the `deploy.rs` reparse assertion — lands on code this commit introduced. The other 37 are on pre-existing code the commit only made prominent: `crates/mirdan/src/install.rs` received a single 97-line test hunk at line 3523, yet every `install.rs` finding cites lines 143-2469. See the split proposal at the end.\n-\n-- [ ] `crates/mirdan/src/install.rs:143` — Default version string \"0.0.0\" is repeated throughout the file instead of being a named constant — changes to the default version must happen in multiple places, making the codebase harder to maintain. Define `const DEFAULT_VERSION: &str = \"0.0.0\";` at module level and use it everywhere instead of the literal string.\n-- [ ] `crates/mirdan/src/install.rs:172` — Repeated PackageType dispatch pattern for deploying packages — duplicates the pattern at lines 109, 115, 204, 230, 252, 338. Extract into a helper function or registry to eliminate the repeated match statement.\n-- [ ] `crates/mirdan/src/install.rs:173` — Repeated \"0.0.0\" default version literal. Use a named constant DEFAULT_VERSION instead.\n-- [ ] `crates/mirdan/src/install.rs:189` — Repeated \"0.0.0\" default version literal. Use a named constant DEFAULT_VERSION instead.\n-- [ ] `crates/mirdan/src/install.rs:204` — Repeated PackageType dispatch pattern for reading package versions — duplicates the pattern at lines 109, 115, 172, 230, 252, 338. Consolidate into a single version-reading function that takes PackageType and resolves the manifest filename.\n-- [ ] `crates/mirdan/src/install.rs:208` — Repeated \"0.0.0\" default version literal. Use a named constant DEFAULT_VERSION instead.\n-- [ ] `crates/mirdan/src/install.rs:230` — Repeated PackageType dispatch pattern for reading versions with fallback — duplicates the pattern at lines 109, 115, 172, 204, 252, 338. Use a shared helper function instead of repeating this match pattern.\n-- [ ] `crates/mirdan/src/install.rs:234` — Repeated \"0.0.0\" default version literal. Use a named constant DEFAULT_VERSION instead.\n-- [ ] `crates/mirdan/src/install.rs:252` — Repeated PackageType dispatch pattern for deploying packages — duplicates the pattern at lines 109, 115, 172, 204, 230, 338. Create a PackageType-to-deploy-function mapping to eliminate repeated match arms.\n-- [ ] `crates/mirdan/src/install.rs:299` — YAML frontmatter delimiter \"---\" is hardcoded instead of being a named constant — appears twice and should be centralized. Define `const FRONTMATTER_DELIMITER: &str = \"---\";` and use it instead of the literal string.\n-- [ ] `crates/mirdan/src/install.rs:305` — Repeated YAML frontmatter delimiter \"---\" literal. Use a named constant FRONTMATTER_DELIMITER instead.\n-- [ ] `crates/mirdan/src/install.rs:389` — Repeated \"0.0.0\" default version literal. Use a named constant DEFAULT_VERSION instead.\n-- [ ] `crates/mirdan/src/install.rs:737` — `deploy_agent_to_agents` is an identical one-liner wrapper to `deploy_skill_to_agents` (line 630), both just delegating to their respective `_at` variants with `root: None`. This duplication could be eliminated by a single generic wrapper function, though the duplication is minimal. If `deploy_skill_to_agents_at` and `deploy_agent_to_agents_at` are refactored into a shared generic function, these wrapper functions would also naturally unify. If they remain separate, this duplication is acceptable as a thin wrapper pattern.\n-- [ ] `crates/mirdan/src/install.rs:756` — `deploy_agent_to_agents_at` (line 756) is nearly an exact copy of `deploy_skill_to_agents_at` (line 645). Both functions load agents, resolve targets, copy source to store, and create symlinks from each agent's directory back to the store. The only differences are which store directory getter is used (`agent_store_dir` vs `skill_store_dir`) and how the agent directory is retrieved (with Optional handling in agent vs. direct retrieval in skill). These should share a generic implementation parameterized by store-directory and agent-directory getters. Extract a generic `fn deploy_to_store<F, G>(name, source_dir, agent_filter, global, root, store_dir_fn: F, agent_dir_fn: G) -> Result<Vec<String>>` where F and G are closures that provide the store directory and agent directory getter for each agent. Have both functions call this generic implementation with appropriate closures.\n-- [ ] `crates/mirdan/src/install.rs:823` — `deploy_skill` (line 823) is identical to `deploy_agent` (line 802) — both are async wrappers that accept the same parameters and simply delegate to their respective `_to_agents` function without adding any logic. The functions are completely parallel and should be unified into a single generic async wrapper parameterized by which `_to_agents` function to call. Create a generic async wrapper `async fn deploy_to_agents<F>(name, source_dir, agent_filter, global, deploy_fn: F) -> Result<Vec<String>>` and have both `deploy_skill` and `deploy_agent` call it with their respective `_to_agents` function as a parameter, or convert one to call the other with explicit function reference.\n-- [ ] `crates/mirdan/src/install.rs:1030` — Function `run_uninstall` is long (~75 lines) with multiple conditional branches and a nested match-within-conditional structure. It handles three different resolution paths (exact match, display name, git source) with complex nesting in the git source branch, making the decision logic hard to follow. Extract the three resolution strategies into separate functions: `fn resolve_by_exact_match(...)`, `fn resolve_by_display_name(...)`, `fn resolve_by_git_source(...)` and call them in sequence. This linearizes the logic and reduces branch complexity.\n-- [ ] `crates/mirdan/src/install.rs:1120` — Function `remove_matching_store_entries` has deep nesting (4-5 levels) combining nested conditionals, loop-within-loop structure (for loop with nested while loop), and recursion, making it hard to follow the control flow. Extract the parent-cleanup logic (`while let Some(p) = parent { ... }`) into a separate `fn prune_empty_parent_dirs(...)` to reduce nesting to 2-3 levels. Extract the name-matching logic into a separate `fn is_matching_skill(path, skill_md, name)` function.\n-- [ ] `crates/mirdan/src/install.rs:1340` — `install_profile_skills` and `install_profile_agents` (line 1404) follow nearly identical structure: resolve builtins → create available map → create template library/context → select items → for each selected: render content → deploy → merge targets → write README → report. The actual per-item operations differ (rendering strategy, file name, formatter), but the outer loop and orchestration pattern are duplicated across both functions. Extract a shared orchestration function that handles the select-render-deploy-report pattern, parameterized by resolver, renderer, formatter, and store-directory getter. This would reduce duplication while preserving the different rendering strategies needed for skills vs. agents.\n-- [ ] `crates/mirdan/src/install.rs:1422` — Function `deploy_plugin` mirrors `deploy_tool`'s nesting pattern: loop with nested if-let and conditional MCP handling, plus additional bundled .mcp.json parsing with deeply nested conditionals (4-5 levels in the .mcp.json block). Extract bundled MCP registration into a separate `fn register_bundled_mcp_from_plugin(plugin_path, agent, mcp_cfg, global) -> Result<()>` to reduce nesting in the main loop.\n-- [ ] `crates/mirdan/src/install.rs:1435` — Function `uninstall_tool` has a loop with nested if-let and sequential if statements (4 levels of nesting), making the MCP unregistration logic harder to follow — mirrors the issue in `deploy_tool` and `run_install_mcp` but with the unregister path. Extract the MCP unregistration logic per agent into a separate `fn unregister_tool_mcp_for_agent(agent, global) -> Result<bool>` to reduce loop body nesting to 1-2 levels.\n-- [ ] `crates/mirdan/src/install.rs:1555` — Function `uninstall_agent_at` has multiple nested conditionals and loops (for loop with nested if let, nested if statements) plus a second loop with optional logic, creating a complex decision tree. Extract symlink removal into `fn uninstall_agent_symlinks(...)` and reference checking into `fn should_remove_agent_store_entry(store_path, agent_dirs) -> bool` to linearize the two-loop structure.\n-- [ ] `crates/mirdan/src/install.rs:1785` — The function `read_skill_frontmatter_name` reimplements the YAML frontmatter extraction and parsing logic that already exists in `read_frontmatter` (line 304). Both functions follow the identical pattern: trim content → strip/skip opening `---` → find closing `---` → extract substring → parse YAML. The only difference is the return type (String vs. (String, String)) and error handling strategy (Option vs. Result). This duplication could be eliminated by having `read_skill_frontmatter_name` call `read_frontmatter` and extract the name, or by refactoring both to use a shared helper. Refactor `read_skill_frontmatter_name` to call `read_frontmatter` and extract just the name field from the result, avoiding the duplication. Alternatively, create a shared `fn parse_yaml_frontmatter(content: &str) -> Result<serde_yaml_ng::Value, ...>` helper and have both functions use it.\n-- [ ] `crates/mirdan/src/install.rs:1988` — Public function `uninstall_skill` lacks doc comment. Public entry points must document their contract, parameters, and error conditions. Add doc comment describing the function's purpose, parameters, and error conditions. Consider: `/// Uninstall a skill by name from all detected agents.\n-/// # Errors\n-/// Returns an error if the skill is not found in any agent directory.`.\n-- [ ] `crates/mirdan/src/install.rs:2469` — `resolved_agent_names` (line 2469) reimplements the same pattern as `resolved_skill_names` (line 2458). Both create a resolver, build an available map from builtins, and call `selector.select(&available)`. The only difference is which resolver is used (SkillResolver vs. AgentResolver) and how the available map is constructed (skills use profiles, agents use empty Vec). This duplication should be eliminated by parameterizing the resolver type or creating a shared helper. Create a generic `fn resolved_builtin_names<R: Resolver>(selector, resolver) -> Vec<String>` helper that takes a resolver instance, then have both functions call it with their respective resolver types. Or parameterize the profile-handling logic separately.\n-- [ ] `crates/swissarmyhammer-skills/src/deploy.rs:18` — Public library function returns `Result<Skill, String>` instead of a typed error enum. Libraries must return typed error enums via `thiserror` so callers can match on specific failures; returning bare `String` forces error handling via string parsing. Change return type to `Result<Skill, SkillError>` and convert the error construction at line 23 to use the typed error enum.\n-- [ ] `crates/swissarmyhammer-skills/src/deploy.rs:323` — Test `test_format_skill_md_round_trips_unmodeled_frontmatter_keys` claims to test round-tripping but only verifies parse → format, not the full parse → format → parse cycle. This leaves the deserialization direction untested for complex nested structures like the `hooks` YAML value. Add a reparse step to verify the full round-trip: `let reparsed = crate::skill_loader::parse_skill_md(&md, SkillSource::Builtin).expect(...); assert_eq!(reparsed.extra, skill.extra);` to prove that complex unmodeled keys (including nested structures) survive both serialization and deserialization.\n-- [ ] `crates/swissarmyhammer-skills/src/skill.rs:28` — Function returns `Result` but does not document error cases in its doc comment. Callers need to know what validation failures are possible. Add an `# Errors` section documenting the two validation failure cases: when the name is empty, and when it contains characters outside the allowed set.\n-- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:30` — Public library function returns `Result<Skill, String>` instead of a typed error enum. Callers cannot distinguish between different parse failures (malformed YAML, missing fields, invalid names) when errors are just strings. Change return type to `Result<Skill, SkillError>` and wrap the various error cases (serde errors, validation errors, name construction errors) in the typed error enum.\n-- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:30` — Function returns `Result` but does not document error cases in its doc comment. Callers need to know what parse failures are possible. Add an `# Errors` section documenting the possible parse failures: YAML parse errors, missing required fields, invalid skill names, and validation errors.\n-- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:37` — Public library function returns `Result<Skill, String>` instead of a typed error enum. This is the primary parse path used by callers; String errors hide the kind of parse failure. Change return type to `Result<Skill, SkillError>` and route all error types through the typed enum.\n-- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:37` — Function returns `Result` but does not document error cases in its doc comment. Add an `# Errors` section documenting the same parse failure modes as `parse_skill_md`.\n-- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:87` — Public library function returns `Result<Skill, String>` instead of a typed error enum. File I/O errors, parse errors, and walk errors should be distinguished so callers can handle them differently. Change return type to `Result<Skill, SkillError>` and wrap file I/O errors in the typed error enum using `.context()` or `.map_err()`.\n-- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:87` — Function returns `Result` but does not document error cases in its doc comment. File I/O and parse failures are both possible. Add an `# Errors` section documenting when the function fails: when `SKILL.md` does not exist, when reading files fails, or when parsing fails.\n-- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:123` — Repeated literal `\"SKILL.md\"` (as `\"/SKILL.md\"` and plain `\"SKILL.md\"`) appears here, line 103, and line 132. Should use a named constant. Define and use const SKILL_FILENAME: &str = \"SKILL.md\"; throughout, e.g., name.ends_with(&format!(\"/{}\", SKILL_FILENAME)) || *name == SKILL_FILENAME.\n-- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:124` — Function returns `Result` but does not document error cases in its doc comment. Add an `# Errors` section documenting when the function fails: when no SKILL.md is found in the files list, or when parsing fails.\n-- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:177` — Magic number 3 represents the length of the repeated delimiter `\"---\"` from lines 175 and 182. Should use a constant's len() method for clarity and to prevent drift if the delimiter ever changes. Replace &content[3..] with &content[FRONTMATTER_DELIMITER.len()..].\n-- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:182` — Repeated literal `\"\\n---\"` used in find call here is derived from the same YAML delimiter hardcoded at line 175. Should extract to a named constant so the delimiter changes in one place. Define const FRONTMATTER_CLOSE: &str = \"\\n---\"; and use find(FRONTMATTER_CLOSE), or use find(&format!(\"\\n{}\", FRONTMATTER_DELIMITER)).\n-- [ ] `crates/swissarmyhammer-skills/src/skill_loader.rs:184` — Magic number 4 represents the length of `\"\\n---\"` hardcoded at line 182. Should use a constant's len() method so it scales if the delimiter changes. Replace end_pos + 4 with end_pos + FRONTMATTER_CLOSE.len().\n-\n-### Split proposal (so this card can close)\n-\n-The engine's line numbers track the pre-image, so they are offset; classification below is by subject, checked against the commit's actual hunks.\n-\n-This commit's hunks were: `install.rs` +3523..3619 (test only); `deploy.rs` +17, +44..50, +91..96, +128..137 and new tests from +380; `skill.rs` +7..25, +116..136; `skill_loader.rs` +28..38, +70..76, +87; `lib.rs` line 36.\n-\n-- Introduced by this commit (1): the `deploy.rs` reparse assertion on `test_format_skill_md_round_trips_unmodeled_frontmatter_keys` (new test at real line 426). It asserts key survival through a frontmatter map but never re-enters `parse_skill_md` to compare `extra`. Adding an assertion to a new test is in scope. Fix here.\n-- Pre-existing, `crates/mirdan/src/install.rs` (24): every one cites lines 143-2469 while the commit's only hunk is a 97-line test block at 3523. None of this code was touched. Propose one card: \"mirdan install.rs: DEFAULT_VERSION / FRONTMATTER_DELIMITER constants, PackageType dispatch dedup, deploy_skill/deploy_agent generic unification, nesting extraction in run_uninstall / remove_matching_store_entries / deploy_plugin / uninstall_tool / uninstall_agent_at, read_skill_frontmatter_name reuse, uninstall_skill doc\".\n-- Pre-existing, `swissarmyhammer-skills` public signatures and docs (12): `resolve_skill`, `parse_skill_md`, `parse_skill_md_with_path`, `load_skill_from_dir`, `load_skill_from_builtin`, `SkillName::new` — all untouched signatures returning `Result<_, String>` and lacking `# Errors`; plus the `SKILL.md` and `---` delimiter literals in the pre-existing `split_frontmatter`. Propose one card: \"swissarmyhammer-skills: typed SkillError enum + `# Errors` docs + frontmatter/filename constants\".\n-\n-Verifier observations judged, none raised as findings: the unreachable `extra.retain(...)` is documented defence-in-depth and the dead-code validator did not confirm it; the vacuous-loop risk if `SAH_INTERNAL_FRONTMATTER_KEYS` were emptied is real but the engine did not raise it, and a non-empty assertion is optional; `extra` values being copied without Liquid rendering is a documented contract, not a defect.\n\\ No newline at end of file\n+Note: the hook command itself is also broken, in a separate card — `ralph` is not registered in the CLI tool registry, so `sah tool ralph ralph check --` fails. Both cards are needed before the Stop hook works. #bug #init #ralph\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYVXFA3B91VT3ZBVFW1PKTKW","timestamp":"2026-07-31T11:03:11.723902Z","op":"update","item_id":"01KYSFEYD7B6GAJ21H9T7EBYN8","forward_patch":"--- original\n+++ modified\n@@ -121,6 +121,31 @@\n\n     Still the one pre-existing failure, `work_the_card_skills_render_record_progress_guidance`, proven unrelated by reverting the whole change.\n   timestamp: 2026-07-31T10:10:06.088834+00:00\n+- actor: claude-code\n+  id: 01kyvxfa3aavnr97j00487mg8p\n+  text: |-\n+    Closing. The 2026-07-31 05:26 section had 38 findings; 1 was fixed here, 37 are SPLIT OUT, not dropped.\n+\n+    The reviewer classified each against the commit's real hunks. Exactly one landed on code this commit introduced — the missing reparse assertion on the new round-trip test. Fixed in 97a487389.\n+\n+    The other 37 are pre-existing code the diff merely displaced:\n+    - 24 in `crates/mirdan/src/install.rs` → ^927239f. The commit's only hunk in that file is 97 lines of test at line 3523, yet every finding cites lines 143–2469.\n+    - 12 in `crates/swissarmyhammer-skills` → ^s7hpzhg. Untouched public signatures returning `Result<_, String>`; the new `SAH_INTERNAL_FRONTMATTER_KEYS` constant pushed `SkillName::new` down 19 lines, which is why the validator noticed them.\n+    - 1 is the reparse assertion, fixed.\n+\n+    Same precedent as ^tnr56gg, ^qc0jkf8, ^p4mp9n6, ^n36mc1q, ^a3bdyy7 on the previous card. The difference: the reviewer caught the expanding-scope pattern at iteration 1 here rather than iteration 8.\n+\n+    Note for anyone working the split cards: the engine's cited line numbers track the PRE-image and are offset — it cited deploy.rs:323 for a test at 426, and skill_loader.rs:30/37/87 for functions at 42/47/102. Grep for the symbol.\n+\n+    Shipped:\n+    - Skill install round-trip is lossless. A flattened catch-all carries every unmodeled key, so all 12 Claude Code fields SAH was dropping now survive — not just `hooks`.\n+    - The ralph Stop hook reaches the deployed `finish` skill. Verified by a sandboxed real `sah init project`, not only unit tests: 23 skills deployed, zero duplicate keys, zero lost source keys besides `profiles`.\n+    - Second live casualty fixed: `double-check` was losing `background: false`.\n+    - `profiles` stays out of deployed files via one shared constant both sides read.\n+    - The `#[serde(flatten)]`/rename duplicate-key hazard is guarded by two independent layers, each proved to fire alone.\n+\n+    Still open, filed separately: ^ksys4z5 — `Agent::to_agent_md` has the same closed-struct defect plus unescaped hand-concatenated YAML. The description on this card originally claimed agents were unaffected because they deploy as symlinks; that was wrong, and ^ksys4z5 records the correction.\n+  timestamp: 2026-07-31T11:03:11.722909+00:00\n position_column: review\n position_ordinal: '8280'\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n","reverse_patch":"--- original\n+++ modified\n@@ -121,31 +121,6 @@\n\n     Still the one pre-existing failure, `work_the_card_skills_render_record_progress_guidance`, proven unrelated by reverting the whole change.\n   timestamp: 2026-07-31T10:10:06.088834+00:00\n-- actor: claude-code\n-  id: 01kyvxfa3aavnr97j00487mg8p\n-  text: |-\n-    Closing. The 2026-07-31 05:26 section had 38 findings; 1 was fixed here, 37 are SPLIT OUT, not dropped.\n-\n-    The reviewer classified each against the commit's real hunks. Exactly one landed on code this commit introduced — the missing reparse assertion on the new round-trip test. Fixed in 97a487389.\n-\n-    The other 37 are pre-existing code the diff merely displaced:\n-    - 24 in `crates/mirdan/src/install.rs` → ^927239f. The commit's only hunk in that file is 97 lines of test at line 3523, yet every finding cites lines 143–2469.\n-    - 12 in `crates/swissarmyhammer-skills` → ^s7hpzhg. Untouched public signatures returning `Result<_, String>`; the new `SAH_INTERNAL_FRONTMATTER_KEYS` constant pushed `SkillName::new` down 19 lines, which is why the validator noticed them.\n-    - 1 is the reparse assertion, fixed.\n-\n-    Same precedent as ^tnr56gg, ^qc0jkf8, ^p4mp9n6, ^n36mc1q, ^a3bdyy7 on the previous card. The difference: the reviewer caught the expanding-scope pattern at iteration 1 here rather than iteration 8.\n-\n-    Note for anyone working the split cards: the engine's cited line numbers track the PRE-image and are offset — it cited deploy.rs:323 for a test at 426, and skill_loader.rs:30/37/87 for functions at 42/47/102. Grep for the symbol.\n-\n-    Shipped:\n-    - Skill install round-trip is lossless. A flattened catch-all carries every unmodeled key, so all 12 Claude Code fields SAH was dropping now survive — not just `hooks`.\n-    - The ralph Stop hook reaches the deployed `finish` skill. Verified by a sandboxed real `sah init project`, not only unit tests: 23 skills deployed, zero duplicate keys, zero lost source keys besides `profiles`.\n-    - Second live casualty fixed: `double-check` was losing `background: false`.\n-    - `profiles` stays out of deployed files via one shared constant both sides read.\n-    - The `#[serde(flatten)]`/rename duplicate-key hazard is guarded by two independent layers, each proved to fire alone.\n-\n-    Still open, filed separately: ^ksys4z5 — `Agent::to_agent_md` has the same closed-struct defect plus unescaped hand-concatenated YAML. The description on this card originally claimed agents were unaffected because they deploy as symlinks; that was wrong, and ^ksys4z5 records the correction.\n-  timestamp: 2026-07-31T11:03:11.722909+00:00\n position_column: review\n position_ordinal: '8280'\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n","transaction_id":null}
{"id":"01KYVXFG3SF4PCASJXNVZB19BG","timestamp":"2026-07-31T11:03:17.881680Z","op":"update","item_id":"01KYSFEYD7B6GAJ21H9T7EBYN8","forward_patch":"--- original\n+++ modified\n@@ -146,8 +146,8 @@\n\n     Still open, filed separately: ^ksys4z5 — `Agent::to_agent_md` has the same closed-struct defect plus unescaped hand-concatenated YAML. The description on this card originally claimed agents were unaffected because they deploy as symlinks; that was wrong, and ^ksys4z5 records the correction.\n   timestamp: 2026-07-31T11:03:11.722909+00:00\n-position_column: review\n-position_ordinal: '8280'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffffff80\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n ---\n Skill install is a lossy parse-then-rebuild. It keeps an allowlist of 9 frontmatter fields and discards every other key without a warning.\n","reverse_patch":"--- original\n+++ modified\n@@ -146,8 +146,8 @@\n\n     Still open, filed separately: ^ksys4z5 — `Agent::to_agent_md` has the same closed-struct defect plus unescaped hand-concatenated YAML. The description on this card originally claimed agents were unaffected because they deploy as symlinks; that was wrong, and ^ksys4z5 records the correction.\n   timestamp: 2026-07-31T11:03:11.722909+00:00\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffffff80\n+position_column: review\n+position_ordinal: '8280'\n title: Skill install strips unknown frontmatter (ralph Stop hook is lost)\n ---\n Skill install is a lossy parse-then-rebuild. It keeps an allowlist of 9 frontmatter fields and discards every other key without a warning.\n","transaction_id":null}

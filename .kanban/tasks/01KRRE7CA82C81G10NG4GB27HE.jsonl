{"id":"01KRRE7CA8BBFB73D8RCKPX6TR","timestamp":"2026-05-16T13:04:48.968726Z","op":"create","item_id":"01KRRE7CA82C81G10NG4GB27HE","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,35 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KRRE6XMJAK3WH3EVMPTMZX8M\n+position_column: todo\n+position_ordinal: 8b80\n+project: plugin-arch\n+title: 'plugin: module loader — relative, bare, and @swissarmyhammer/* imports'\n+---\n+## What\n+Wire a `deno_core::ModuleLoader` so multi-file plugins work without bundling, and host built-ins resolve to virtual modules.\n+\n+In `crates/swissarmyhammer-plugin/src/runtime/` (module loader submodule):\n+- **Relative imports** (`./util`, `../shared/foo`) — resolved against the plugin's bundle directory; each loaded module is transpiled the same way as the entry (via the runtime's `deno_ast` path). Imports that resolve OUTSIDE the plugin's directory are rejected (canonicalize + prefix check, same sandbox rule as `swissarmyhammer-js`'s converted resolver).\n+- **Bare imports** (`lodash`, `zod`) — NOT resolved by the host. Resolution fails with a clear error; the plugin author is expected to bundle npm deps themselves. The host is not an npm client.\n+- **`@swissarmyhammer/*` imports** — resolved to host-provided virtual modules served from memory: `@swissarmyhammer/plugin` (the SDK), `@swissarmyhammer/app` (generated app types — a runtime no-op stub here; codegen is a separate task). The SDK module's actual contents come from the SDK task; this task provides the resolution + virtual-module plumbing and can serve a stub for `@swissarmyhammer/plugin` until the SDK lands.\n+\n+## Acceptance Criteria\n+- [ ] Relative imports inside the plugin dir load and transpile; an import escaping the plugin dir is rejected.\n+- [ ] A bare import produces a clear, specific error (not a panic, not a silent empty module).\n+- [ ] `@swissarmyhammer/*` specifiers resolve to in-memory virtual modules.\n+\n+## Tests\n+- [ ] Integration test: a plugin entry `.ts` that imports `./helper.ts` (also in a temp dir) — assert the helper's export is usable from the entry.\n+- [ ] Test: an entry importing `../outside.ts` (above the plugin dir) is rejected.\n+- [ ] Test: an entry importing `lodash` fails with the bare-import error.\n+- [ ] Test: an entry importing `@swissarmyhammer/plugin` resolves to the virtual module.\n+- [ ] Run: `cargo test -p swissarmyhammer-plugin` — all green.\n+\n+## Workflow\n+- Use `/tdd` — write the resolution tests (relative ok, escape rejected, bare rejected, virtual ok) first, then implement.\n+\n+## Depends on\n+deno_core runtime.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,35 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KRRE6XMJAK3WH3EVMPTMZX8M\n-position_column: todo\n-position_ordinal: 8b80\n-project: plugin-arch\n-title: 'plugin: module loader — relative, bare, and @swissarmyhammer/* imports'\n----\n-## What\n-Wire a `deno_core::ModuleLoader` so multi-file plugins work without bundling, and host built-ins resolve to virtual modules.\n-\n-In `crates/swissarmyhammer-plugin/src/runtime/` (module loader submodule):\n-- **Relative imports** (`./util`, `../shared/foo`) — resolved against the plugin's bundle directory; each loaded module is transpiled the same way as the entry (via the runtime's `deno_ast` path). Imports that resolve OUTSIDE the plugin's directory are rejected (canonicalize + prefix check, same sandbox rule as `swissarmyhammer-js`'s converted resolver).\n-- **Bare imports** (`lodash`, `zod`) — NOT resolved by the host. Resolution fails with a clear error; the plugin author is expected to bundle npm deps themselves. The host is not an npm client.\n-- **`@swissarmyhammer/*` imports** — resolved to host-provided virtual modules served from memory: `@swissarmyhammer/plugin` (the SDK), `@swissarmyhammer/app` (generated app types — a runtime no-op stub here; codegen is a separate task). The SDK module's actual contents come from the SDK task; this task provides the resolution + virtual-module plumbing and can serve a stub for `@swissarmyhammer/plugin` until the SDK lands.\n-\n-## Acceptance Criteria\n-- [ ] Relative imports inside the plugin dir load and transpile; an import escaping the plugin dir is rejected.\n-- [ ] A bare import produces a clear, specific error (not a panic, not a silent empty module).\n-- [ ] `@swissarmyhammer/*` specifiers resolve to in-memory virtual modules.\n-\n-## Tests\n-- [ ] Integration test: a plugin entry `.ts` that imports `./helper.ts` (also in a temp dir) — assert the helper's export is usable from the entry.\n-- [ ] Test: an entry importing `../outside.ts` (above the plugin dir) is rejected.\n-- [ ] Test: an entry importing `lodash` fails with the bare-import error.\n-- [ ] Test: an entry importing `@swissarmyhammer/plugin` resolves to the virtual module.\n-- [ ] Run: `cargo test -p swissarmyhammer-plugin` — all green.\n-\n-## Workflow\n-- Use `/tdd` — write the resolution tests (relative ok, escape rejected, bare rejected, virtual ok) first, then implement.\n-\n-## Depends on\n-deno_core runtime.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRV0V9GVTW5P1NK158PEKMQ1","timestamp":"2026-05-17T13:08:44.699640Z","op":"update","item_id":"01KRRE7CA82C81G10NG4GB27HE","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KRRE6XMJAK3WH3EVMPTMZX8M\n-position_column: todo\n-position_ordinal: 8b80\n+position_column: doing\n+position_ordinal: '80'\n project: plugin-arch\n title: 'plugin: module loader — relative, bare, and @swissarmyhammer/* imports'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KRRE6XMJAK3WH3EVMPTMZX8M\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: 8b80\n project: plugin-arch\n title: 'plugin: module loader — relative, bare, and @swissarmyhammer/* imports'\n ---\n","transaction_id":null}
{"id":"01KRV0V9GWCPDCV9EE8Q09K2SC","timestamp":"2026-05-17T13:08:44.700463Z","entity_type":"task","entity_id":"01KRRE7CA82C81G10NG4GB27HE","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8b80\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8b80\n\\ No newline at end of file\n"}]]}
{"id":"01KRV1GW00EHXXJM0QCCGHKKPA","timestamp":"2026-05-17T13:20:31.744701Z","op":"update","item_id":"01KRRE7CA82C81G10NG4GB27HE","forward_patch":"--- original\n+++ modified\n@@ -17,16 +17,16 @@\n - **`@swissarmyhammer/*` imports** — resolved to host-provided virtual modules served from memory: `@swissarmyhammer/plugin` (the SDK), `@swissarmyhammer/app` (generated app types — a runtime no-op stub here; codegen is a separate task). The SDK module's actual contents come from the SDK task; this task provides the resolution + virtual-module plumbing and can serve a stub for `@swissarmyhammer/plugin` until the SDK lands.\n\n ## Acceptance Criteria\n-- [ ] Relative imports inside the plugin dir load and transpile; an import escaping the plugin dir is rejected.\n-- [ ] A bare import produces a clear, specific error (not a panic, not a silent empty module).\n-- [ ] `@swissarmyhammer/*` specifiers resolve to in-memory virtual modules.\n+- [x] Relative imports inside the plugin dir load and transpile; an import escaping the plugin dir is rejected.\n+- [x] A bare import produces a clear, specific error (not a panic, not a silent empty module).\n+- [x] `@swissarmyhammer/*` specifiers resolve to in-memory virtual modules.\n\n ## Tests\n-- [ ] Integration test: a plugin entry `.ts` that imports `./helper.ts` (also in a temp dir) — assert the helper's export is usable from the entry.\n-- [ ] Test: an entry importing `../outside.ts` (above the plugin dir) is rejected.\n-- [ ] Test: an entry importing `lodash` fails with the bare-import error.\n-- [ ] Test: an entry importing `@swissarmyhammer/plugin` resolves to the virtual module.\n-- [ ] Run: `cargo test -p swissarmyhammer-plugin` — all green.\n+- [x] Integration test: a plugin entry `.ts` that imports `./helper.ts` (also in a temp dir) — assert the helper's export is usable from the entry.\n+- [x] Test: an entry importing `../outside.ts` (above the plugin dir) is rejected.\n+- [x] Test: an entry importing `lodash` fails with the bare-import error.\n+- [x] Test: an entry importing `@swissarmyhammer/plugin` resolves to the virtual module.\n+- [x] Run: `cargo test -p swissarmyhammer-plugin` — all green.\n\n ## Workflow\n - Use `/tdd` — write the resolution tests (relative ok, escape rejected, bare rejected, virtual ok) first, then implement.\n","reverse_patch":"--- original\n+++ modified\n@@ -17,16 +17,16 @@\n - **`@swissarmyhammer/*` imports** — resolved to host-provided virtual modules served from memory: `@swissarmyhammer/plugin` (the SDK), `@swissarmyhammer/app` (generated app types — a runtime no-op stub here; codegen is a separate task). The SDK module's actual contents come from the SDK task; this task provides the resolution + virtual-module plumbing and can serve a stub for `@swissarmyhammer/plugin` until the SDK lands.\n\n ## Acceptance Criteria\n-- [x] Relative imports inside the plugin dir load and transpile; an import escaping the plugin dir is rejected.\n-- [x] A bare import produces a clear, specific error (not a panic, not a silent empty module).\n-- [x] `@swissarmyhammer/*` specifiers resolve to in-memory virtual modules.\n+- [ ] Relative imports inside the plugin dir load and transpile; an import escaping the plugin dir is rejected.\n+- [ ] A bare import produces a clear, specific error (not a panic, not a silent empty module).\n+- [ ] `@swissarmyhammer/*` specifiers resolve to in-memory virtual modules.\n\n ## Tests\n-- [x] Integration test: a plugin entry `.ts` that imports `./helper.ts` (also in a temp dir) — assert the helper's export is usable from the entry.\n-- [x] Test: an entry importing `../outside.ts` (above the plugin dir) is rejected.\n-- [x] Test: an entry importing `lodash` fails with the bare-import error.\n-- [x] Test: an entry importing `@swissarmyhammer/plugin` resolves to the virtual module.\n-- [x] Run: `cargo test -p swissarmyhammer-plugin` — all green.\n+- [ ] Integration test: a plugin entry `.ts` that imports `./helper.ts` (also in a temp dir) — assert the helper's export is usable from the entry.\n+- [ ] Test: an entry importing `../outside.ts` (above the plugin dir) is rejected.\n+- [ ] Test: an entry importing `lodash` fails with the bare-import error.\n+- [ ] Test: an entry importing `@swissarmyhammer/plugin` resolves to the virtual module.\n+- [ ] Run: `cargo test -p swissarmyhammer-plugin` — all green.\n\n ## Workflow\n - Use `/tdd` — write the resolution tests (relative ok, escape rejected, bare rejected, virtual ok) first, then implement.\n","transaction_id":null}
{"id":"01KRV1GW01KS86GHC1CN8N5CCG","timestamp":"2026-05-17T13:20:31.745136Z","entity_type":"task","entity_id":"01KRRE7CA82C81G10NG4GB27HE","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -7,16 +7,16 @@\n - **`@swissarmyhammer/*` imports** — resolved to host-provided virtual modules served from memory: `@swissarmyhammer/plugin` (the SDK), `@swissarmyhammer/app` (generated app types — a runtime no-op stub here; codegen is a separate task). The SDK module's actual contents come from the SDK task; this task provides the resolution + virtual-module plumbing and can serve a stub for `@swissarmyhammer/plugin` until the SDK lands.\n\n ## Acceptance Criteria\n-- [ ] Relative imports inside the plugin dir load and transpile; an import escaping the plugin dir is rejected.\n-- [ ] A bare import produces a clear, specific error (not a panic, not a silent empty module).\n-- [ ] `@swissarmyhammer/*` specifiers resolve to in-memory virtual modules.\n+- [x] Relative imports inside the plugin dir load and transpile; an import escaping the plugin dir is rejected.\n+- [x] A bare import produces a clear, specific error (not a panic, not a silent empty module).\n+- [x] `@swissarmyhammer/*` specifiers resolve to in-memory virtual modules.\n\n ## Tests\n-- [ ] Integration test: a plugin entry `.ts` that imports `./helper.ts` (also in a temp dir) — assert the helper's export is usable from the entry.\n-- [ ] Test: an entry importing `../outside.ts` (above the plugin dir) is rejected.\n-- [ ] Test: an entry importing `lodash` fails with the bare-import error.\n-- [ ] Test: an entry importing `@swissarmyhammer/plugin` resolves to the virtual module.\n-- [ ] Run: `cargo test -p swissarmyhammer-plugin` — all green.\n+- [x] Integration test: a plugin entry `.ts` that imports `./helper.ts` (also in a temp dir) — assert the helper's export is usable from the entry.\n+- [x] Test: an entry importing `../outside.ts` (above the plugin dir) is rejected.\n+- [x] Test: an entry importing `lodash` fails with the bare-import error.\n+- [x] Test: an entry importing `@swissarmyhammer/plugin` resolves to the virtual module.\n+- [x] Run: `cargo test -p swissarmyhammer-plugin` — all green.\n\n ## Workflow\n - Use `/tdd` — write the resolution tests (relative ok, escape rejected, bare rejected, virtual ok) first, then implement.\n","reverse_patch":"--- original\n+++ modified\n@@ -7,16 +7,16 @@\n - **`@swissarmyhammer/*` imports** — resolved to host-provided virtual modules served from memory: `@swissarmyhammer/plugin` (the SDK), `@swissarmyhammer/app` (generated app types — a runtime no-op stub here; codegen is a separate task). The SDK module's actual contents come from the SDK task; this task provides the resolution + virtual-module plumbing and can serve a stub for `@swissarmyhammer/plugin` until the SDK lands.\n\n ## Acceptance Criteria\n-- [x] Relative imports inside the plugin dir load and transpile; an import escaping the plugin dir is rejected.\n-- [x] A bare import produces a clear, specific error (not a panic, not a silent empty module).\n-- [x] `@swissarmyhammer/*` specifiers resolve to in-memory virtual modules.\n+- [ ] Relative imports inside the plugin dir load and transpile; an import escaping the plugin dir is rejected.\n+- [ ] A bare import produces a clear, specific error (not a panic, not a silent empty module).\n+- [ ] `@swissarmyhammer/*` specifiers resolve to in-memory virtual modules.\n\n ## Tests\n-- [x] Integration test: a plugin entry `.ts` that imports `./helper.ts` (also in a temp dir) — assert the helper's export is usable from the entry.\n-- [x] Test: an entry importing `../outside.ts` (above the plugin dir) is rejected.\n-- [x] Test: an entry importing `lodash` fails with the bare-import error.\n-- [x] Test: an entry importing `@swissarmyhammer/plugin` resolves to the virtual module.\n-- [x] Run: `cargo test -p swissarmyhammer-plugin` — all green.\n+- [ ] Integration test: a plugin entry `.ts` that imports `./helper.ts` (also in a temp dir) — assert the helper's export is usable from the entry.\n+- [ ] Test: an entry importing `../outside.ts` (above the plugin dir) is rejected.\n+- [ ] Test: an entry importing `lodash` fails with the bare-import error.\n+- [ ] Test: an entry importing `@swissarmyhammer/plugin` resolves to the virtual module.\n+- [ ] Run: `cargo test -p swissarmyhammer-plugin` — all green.\n\n ## Workflow\n - Use `/tdd` — write the resolution tests (relative ok, escape rejected, bare rejected, virtual ok) first, then implement.\n"}]]}
{"id":"01KRV1HVP473V54FKZEPH3VKF6","timestamp":"2026-05-17T13:21:04.196232Z","op":"update","item_id":"01KRRE7CA82C81G10NG4GB27HE","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KRRE6XMJAK3WH3EVMPTMZX8M\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: plugin-arch\n title: 'plugin: module loader — relative, bare, and @swissarmyhammer/* imports'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KRRE6XMJAK3WH3EVMPTMZX8M\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: plugin-arch\n title: 'plugin: module loader — relative, bare, and @swissarmyhammer/* imports'\n","transaction_id":null}
{"id":"01KRV1HVP5KV4C29R6GTBY913W","timestamp":"2026-05-17T13:21:04.197200Z","entity_type":"task","entity_id":"01KRRE7CA82C81G10NG4GB27HE","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KRV1QQHXP1NDK22P5XWRJG13","timestamp":"2026-05-17T13:24:16.573659Z","op":"update","item_id":"01KRRE7CA82C81G10NG4GB27HE","forward_patch":"--- original\n+++ modified\n@@ -32,4 +32,13 @@\n - Use `/tdd` — write the resolution tests (relative ok, escape rejected, bare rejected, virtual ok) first, then implement.\n\n ## Depends on\n-deno_core runtime.\n\\ No newline at end of file\n+deno_core runtime.\n+\n+## Review Findings (2026-05-17 13:30)\n+\n+### Warnings\n+- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:271-289` — An absolute-path specifier (e.g. `/etc/passwd`, or a `\\`-prefixed path) is not classified up front and falls through to the bare-import branch, where it is *rejected with a misleading error*: \"the plugin host does not resolve npm packages — bundle '/etc/passwd' into your plugin bundle yourself\". The specifier is rejected, so this is not a sandbox hole — but the diagnosis is wrong and confusing for a plugin author. The reference loader this code explicitly claims to mirror (`swissarmyhammer-js`'s `SandboxedModuleLoader`, lib.rs:157-159) has an explicit up-front guard `if specifier.starts_with('/') || specifier.starts_with('\\\\') { return Err(\"absolute import path rejected: ...\") }`. Add the same up-front absolute/backslash-prefix check in `resolve` (before the bare-import fallthrough) so an absolute import gets an honest, specific error.\n+\n+### Nits\n+- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:210-213` — The doc comment on `load_relative` states \"The `specifier` has already passed `resolve`, so it is a `file://` URL inside the bundle root.\" This is not true for the **entry/main module**: `resolve` returns early for `ResolutionKind::MainModule` (lines 263-267) without any canonicalize-and-contain check, so the main module's `file://` URL reaches `load_relative` having skipped the sandbox check. The entry path is host-derived (`bundle_dir.join(entry_file)` in `mod.rs`), so this is safe in scope — but the comment overstates the guarantee. Reword to note the main module is host-chosen and trusted, only *imports* are sandbox-verified, mirroring the accurate explanation already in `resolve`'s own doc comment (lines 240-252).\n+- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:323-329` — `referrer_directory` is a verbatim copy of the identically-named private fn in `swissarmyhammer-js/src/lib.rs:199-205` (same signature, same body, same doc intent). Two private copies of a path-sandboxing helper is a quiet drift hazard: a future fix to one (e.g. handling a `file://` URL with a host component, or a percent-encoded path) will not reach the other. Not blocking — the crates are independent and neither depends on the other today — but worth a note for whoever owns the shared `swissarmyhammer-js` / plugin-runtime boundary, in case a small shared path-utility crate is warranted.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -32,13 +32,4 @@\n - Use `/tdd` — write the resolution tests (relative ok, escape rejected, bare rejected, virtual ok) first, then implement.\n\n ## Depends on\n-deno_core runtime.\n-\n-## Review Findings (2026-05-17 13:30)\n-\n-### Warnings\n-- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:271-289` — An absolute-path specifier (e.g. `/etc/passwd`, or a `\\`-prefixed path) is not classified up front and falls through to the bare-import branch, where it is *rejected with a misleading error*: \"the plugin host does not resolve npm packages — bundle '/etc/passwd' into your plugin bundle yourself\". The specifier is rejected, so this is not a sandbox hole — but the diagnosis is wrong and confusing for a plugin author. The reference loader this code explicitly claims to mirror (`swissarmyhammer-js`'s `SandboxedModuleLoader`, lib.rs:157-159) has an explicit up-front guard `if specifier.starts_with('/') || specifier.starts_with('\\\\') { return Err(\"absolute import path rejected: ...\") }`. Add the same up-front absolute/backslash-prefix check in `resolve` (before the bare-import fallthrough) so an absolute import gets an honest, specific error.\n-\n-### Nits\n-- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:210-213` — The doc comment on `load_relative` states \"The `specifier` has already passed `resolve`, so it is a `file://` URL inside the bundle root.\" This is not true for the **entry/main module**: `resolve` returns early for `ResolutionKind::MainModule` (lines 263-267) without any canonicalize-and-contain check, so the main module's `file://` URL reaches `load_relative` having skipped the sandbox check. The entry path is host-derived (`bundle_dir.join(entry_file)` in `mod.rs`), so this is safe in scope — but the comment overstates the guarantee. Reword to note the main module is host-chosen and trusted, only *imports* are sandbox-verified, mirroring the accurate explanation already in `resolve`'s own doc comment (lines 240-252).\n-- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:323-329` — `referrer_directory` is a verbatim copy of the identically-named private fn in `swissarmyhammer-js/src/lib.rs:199-205` (same signature, same body, same doc intent). Two private copies of a path-sandboxing helper is a quiet drift hazard: a future fix to one (e.g. handling a `file://` URL with a host component, or a percent-encoded path) will not reach the other. Not blocking — the crates are independent and neither depends on the other today — but worth a note for whoever owns the shared `swissarmyhammer-js` / plugin-runtime boundary, in case a small shared path-utility crate is warranted.\n\\ No newline at end of file\n+deno_core runtime.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRV1TJ4NBG0B53E3AEWFE9EC","timestamp":"2026-05-17T13:25:49.333726Z","op":"update","item_id":"01KRRE7CA82C81G10NG4GB27HE","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KRRE6XMJAK3WH3EVMPTMZX8M\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: plugin-arch\n title: 'plugin: module loader — relative, bare, and @swissarmyhammer/* imports'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KRRE6XMJAK3WH3EVMPTMZX8M\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: plugin-arch\n title: 'plugin: module loader — relative, bare, and @swissarmyhammer/* imports'\n","transaction_id":null}
{"id":"01KRV1TJ4P3VEGFSTKM29Y5CKS","timestamp":"2026-05-17T13:25:49.334705Z","entity_type":"task","entity_id":"01KRRE7CA82C81G10NG4GB27HE","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}]]}
{"id":"01KRV1YVF0T8C47MM2GDZ3EBHD","timestamp":"2026-05-17T13:28:09.952395Z","op":"update","item_id":"01KRRE7CA82C81G10NG4GB27HE","forward_patch":"--- original\n+++ modified\n@@ -37,8 +37,8 @@\n ## Review Findings (2026-05-17 13:30)\n\n ### Warnings\n-- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:271-289` — An absolute-path specifier (e.g. `/etc/passwd`, or a `\\`-prefixed path) is not classified up front and falls through to the bare-import branch, where it is *rejected with a misleading error*: \"the plugin host does not resolve npm packages — bundle '/etc/passwd' into your plugin bundle yourself\". The specifier is rejected, so this is not a sandbox hole — but the diagnosis is wrong and confusing for a plugin author. The reference loader this code explicitly claims to mirror (`swissarmyhammer-js`'s `SandboxedModuleLoader`, lib.rs:157-159) has an explicit up-front guard `if specifier.starts_with('/') || specifier.starts_with('\\\\') { return Err(\"absolute import path rejected: ...\") }`. Add the same up-front absolute/backslash-prefix check in `resolve` (before the bare-import fallthrough) so an absolute import gets an honest, specific error.\n+- [x] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:271-289` — An absolute-path specifier (e.g. `/etc/passwd`, or a `\\`-prefixed path) is not classified up front and falls through to the bare-import branch, where it is *rejected with a misleading error*: \"the plugin host does not resolve npm packages — bundle '/etc/passwd' into your plugin bundle yourself\". The specifier is rejected, so this is not a sandbox hole — but the diagnosis is wrong and confusing for a plugin author. The reference loader this code explicitly claims to mirror (`swissarmyhammer-js`'s `SandboxedModuleLoader`, lib.rs:157-159) has an explicit up-front guard `if specifier.starts_with('/') || specifier.starts_with('\\\\') { return Err(\"absolute import path rejected: ...\") }`. Add the same up-front absolute/backslash-prefix check in `resolve` (before the bare-import fallthrough) so an absolute import gets an honest, specific error.\n\n ### Nits\n-- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:210-213` — The doc comment on `load_relative` states \"The `specifier` has already passed `resolve`, so it is a `file://` URL inside the bundle root.\" This is not true for the **entry/main module**: `resolve` returns early for `ResolutionKind::MainModule` (lines 263-267) without any canonicalize-and-contain check, so the main module's `file://` URL reaches `load_relative` having skipped the sandbox check. The entry path is host-derived (`bundle_dir.join(entry_file)` in `mod.rs`), so this is safe in scope — but the comment overstates the guarantee. Reword to note the main module is host-chosen and trusted, only *imports* are sandbox-verified, mirroring the accurate explanation already in `resolve`'s own doc comment (lines 240-252).\n-- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:323-329` — `referrer_directory` is a verbatim copy of the identically-named private fn in `swissarmyhammer-js/src/lib.rs:199-205` (same signature, same body, same doc intent). Two private copies of a path-sandboxing helper is a quiet drift hazard: a future fix to one (e.g. handling a `file://` URL with a host component, or a percent-encoded path) will not reach the other. Not blocking — the crates are independent and neither depends on the other today — but worth a note for whoever owns the shared `swissarmyhammer-js` / plugin-runtime boundary, in case a small shared path-utility crate is warranted.\n\\ No newline at end of file\n+- [x] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:210-213` — The doc comment on `load_relative` states \"The `specifier` has already passed `resolve`, so it is a `file://` URL inside the bundle root.\" This is not true for the **entry/main module**: `resolve` returns early for `ResolutionKind::MainModule` (lines 263-267) without any canonicalize-and-contain check, so the main module's `file://` URL reaches `load_relative` having skipped the sandbox check. The entry path is host-derived (`bundle_dir.join(entry_file)` in `mod.rs`), so this is safe in scope — but the comment overstates the guarantee. Reword to note the main module is host-chosen and trusted, only *imports* are sandbox-verified, mirroring the accurate explanation already in `resolve`'s own doc comment (lines 240-252).\n+- [x] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:323-329` — `referrer_directory` is a verbatim copy of the identically-named private fn in `swissarmyhammer-js/src/lib.rs:199-205` (same signature, same body, same doc intent). Two private copies of a path-sandboxing helper is a quiet drift hazard: a future fix to one (e.g. handling a `file://` URL with a host component, or a percent-encoded path) will not reach the other. Not blocking — the crates are independent and neither depends on the other today — but worth a note for whoever owns the shared `swissarmyhammer-js` / plugin-runtime boundary, in case a small shared path-utility crate is warranted.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -37,8 +37,8 @@\n ## Review Findings (2026-05-17 13:30)\n\n ### Warnings\n-- [x] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:271-289` — An absolute-path specifier (e.g. `/etc/passwd`, or a `\\`-prefixed path) is not classified up front and falls through to the bare-import branch, where it is *rejected with a misleading error*: \"the plugin host does not resolve npm packages — bundle '/etc/passwd' into your plugin bundle yourself\". The specifier is rejected, so this is not a sandbox hole — but the diagnosis is wrong and confusing for a plugin author. The reference loader this code explicitly claims to mirror (`swissarmyhammer-js`'s `SandboxedModuleLoader`, lib.rs:157-159) has an explicit up-front guard `if specifier.starts_with('/') || specifier.starts_with('\\\\') { return Err(\"absolute import path rejected: ...\") }`. Add the same up-front absolute/backslash-prefix check in `resolve` (before the bare-import fallthrough) so an absolute import gets an honest, specific error.\n+- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:271-289` — An absolute-path specifier (e.g. `/etc/passwd`, or a `\\`-prefixed path) is not classified up front and falls through to the bare-import branch, where it is *rejected with a misleading error*: \"the plugin host does not resolve npm packages — bundle '/etc/passwd' into your plugin bundle yourself\". The specifier is rejected, so this is not a sandbox hole — but the diagnosis is wrong and confusing for a plugin author. The reference loader this code explicitly claims to mirror (`swissarmyhammer-js`'s `SandboxedModuleLoader`, lib.rs:157-159) has an explicit up-front guard `if specifier.starts_with('/') || specifier.starts_with('\\\\') { return Err(\"absolute import path rejected: ...\") }`. Add the same up-front absolute/backslash-prefix check in `resolve` (before the bare-import fallthrough) so an absolute import gets an honest, specific error.\n\n ### Nits\n-- [x] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:210-213` — The doc comment on `load_relative` states \"The `specifier` has already passed `resolve`, so it is a `file://` URL inside the bundle root.\" This is not true for the **entry/main module**: `resolve` returns early for `ResolutionKind::MainModule` (lines 263-267) without any canonicalize-and-contain check, so the main module's `file://` URL reaches `load_relative` having skipped the sandbox check. The entry path is host-derived (`bundle_dir.join(entry_file)` in `mod.rs`), so this is safe in scope — but the comment overstates the guarantee. Reword to note the main module is host-chosen and trusted, only *imports* are sandbox-verified, mirroring the accurate explanation already in `resolve`'s own doc comment (lines 240-252).\n-- [x] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:323-329` — `referrer_directory` is a verbatim copy of the identically-named private fn in `swissarmyhammer-js/src/lib.rs:199-205` (same signature, same body, same doc intent). Two private copies of a path-sandboxing helper is a quiet drift hazard: a future fix to one (e.g. handling a `file://` URL with a host component, or a percent-encoded path) will not reach the other. Not blocking — the crates are independent and neither depends on the other today — but worth a note for whoever owns the shared `swissarmyhammer-js` / plugin-runtime boundary, in case a small shared path-utility crate is warranted.\n\\ No newline at end of file\n+- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:210-213` — The doc comment on `load_relative` states \"The `specifier` has already passed `resolve`, so it is a `file://` URL inside the bundle root.\" This is not true for the **entry/main module**: `resolve` returns early for `ResolutionKind::MainModule` (lines 263-267) without any canonicalize-and-contain check, so the main module's `file://` URL reaches `load_relative` having skipped the sandbox check. The entry path is host-derived (`bundle_dir.join(entry_file)` in `mod.rs`), so this is safe in scope — but the comment overstates the guarantee. Reword to note the main module is host-chosen and trusted, only *imports* are sandbox-verified, mirroring the accurate explanation already in `resolve`'s own doc comment (lines 240-252).\n+- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:323-329` — `referrer_directory` is a verbatim copy of the identically-named private fn in `swissarmyhammer-js/src/lib.rs:199-205` (same signature, same body, same doc intent). Two private copies of a path-sandboxing helper is a quiet drift hazard: a future fix to one (e.g. handling a `file://` URL with a host component, or a percent-encoded path) will not reach the other. Not blocking — the crates are independent and neither depends on the other today — but worth a note for whoever owns the shared `swissarmyhammer-js` / plugin-runtime boundary, in case a small shared path-utility crate is warranted.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRV1YVF0VJADPCMPW8H3S3VJ","timestamp":"2026-05-17T13:28:09.952814Z","entity_type":"task","entity_id":"01KRRE7CA82C81G10NG4GB27HE","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -27,8 +27,8 @@\n ## Review Findings (2026-05-17 13:30)\n\n ### Warnings\n-- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:271-289` — An absolute-path specifier (e.g. `/etc/passwd`, or a `\\`-prefixed path) is not classified up front and falls through to the bare-import branch, where it is *rejected with a misleading error*: \"the plugin host does not resolve npm packages — bundle '/etc/passwd' into your plugin bundle yourself\". The specifier is rejected, so this is not a sandbox hole — but the diagnosis is wrong and confusing for a plugin author. The reference loader this code explicitly claims to mirror (`swissarmyhammer-js`'s `SandboxedModuleLoader`, lib.rs:157-159) has an explicit up-front guard `if specifier.starts_with('/') || specifier.starts_with('\\\\') { return Err(\"absolute import path rejected: ...\") }`. Add the same up-front absolute/backslash-prefix check in `resolve` (before the bare-import fallthrough) so an absolute import gets an honest, specific error.\n+- [x] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:271-289` — An absolute-path specifier (e.g. `/etc/passwd`, or a `\\`-prefixed path) is not classified up front and falls through to the bare-import branch, where it is *rejected with a misleading error*: \"the plugin host does not resolve npm packages — bundle '/etc/passwd' into your plugin bundle yourself\". The specifier is rejected, so this is not a sandbox hole — but the diagnosis is wrong and confusing for a plugin author. The reference loader this code explicitly claims to mirror (`swissarmyhammer-js`'s `SandboxedModuleLoader`, lib.rs:157-159) has an explicit up-front guard `if specifier.starts_with('/') || specifier.starts_with('\\\\') { return Err(\"absolute import path rejected: ...\") }`. Add the same up-front absolute/backslash-prefix check in `resolve` (before the bare-import fallthrough) so an absolute import gets an honest, specific error.\n\n ### Nits\n-- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:210-213` — The doc comment on `load_relative` states \"The `specifier` has already passed `resolve`, so it is a `file://` URL inside the bundle root.\" This is not true for the **entry/main module**: `resolve` returns early for `ResolutionKind::MainModule` (lines 263-267) without any canonicalize-and-contain check, so the main module's `file://` URL reaches `load_relative` having skipped the sandbox check. The entry path is host-derived (`bundle_dir.join(entry_file)` in `mod.rs`), so this is safe in scope — but the comment overstates the guarantee. Reword to note the main module is host-chosen and trusted, only *imports* are sandbox-verified, mirroring the accurate explanation already in `resolve`'s own doc comment (lines 240-252).\n-- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:323-329` — `referrer_directory` is a verbatim copy of the identically-named private fn in `swissarmyhammer-js/src/lib.rs:199-205` (same signature, same body, same doc intent). Two private copies of a path-sandboxing helper is a quiet drift hazard: a future fix to one (e.g. handling a `file://` URL with a host component, or a percent-encoded path) will not reach the other. Not blocking — the crates are independent and neither depends on the other today — but worth a note for whoever owns the shared `swissarmyhammer-js` / plugin-runtime boundary, in case a small shared path-utility crate is warranted.\n\\ No newline at end of file\n+- [x] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:210-213` — The doc comment on `load_relative` states \"The `specifier` has already passed `resolve`, so it is a `file://` URL inside the bundle root.\" This is not true for the **entry/main module**: `resolve` returns early for `ResolutionKind::MainModule` (lines 263-267) without any canonicalize-and-contain check, so the main module's `file://` URL reaches `load_relative` having skipped the sandbox check. The entry path is host-derived (`bundle_dir.join(entry_file)` in `mod.rs`), so this is safe in scope — but the comment overstates the guarantee. Reword to note the main module is host-chosen and trusted, only *imports* are sandbox-verified, mirroring the accurate explanation already in `resolve`'s own doc comment (lines 240-252).\n+- [x] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:323-329` — `referrer_directory` is a verbatim copy of the identically-named private fn in `swissarmyhammer-js/src/lib.rs:199-205` (same signature, same body, same doc intent). Two private copies of a path-sandboxing helper is a quiet drift hazard: a future fix to one (e.g. handling a `file://` URL with a host component, or a percent-encoded path) will not reach the other. Not blocking — the crates are independent and neither depends on the other today — but worth a note for whoever owns the shared `swissarmyhammer-js` / plugin-runtime boundary, in case a small shared path-utility crate is warranted.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -27,8 +27,8 @@\n ## Review Findings (2026-05-17 13:30)\n\n ### Warnings\n-- [x] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:271-289` — An absolute-path specifier (e.g. `/etc/passwd`, or a `\\`-prefixed path) is not classified up front and falls through to the bare-import branch, where it is *rejected with a misleading error*: \"the plugin host does not resolve npm packages — bundle '/etc/passwd' into your plugin bundle yourself\". The specifier is rejected, so this is not a sandbox hole — but the diagnosis is wrong and confusing for a plugin author. The reference loader this code explicitly claims to mirror (`swissarmyhammer-js`'s `SandboxedModuleLoader`, lib.rs:157-159) has an explicit up-front guard `if specifier.starts_with('/') || specifier.starts_with('\\\\') { return Err(\"absolute import path rejected: ...\") }`. Add the same up-front absolute/backslash-prefix check in `resolve` (before the bare-import fallthrough) so an absolute import gets an honest, specific error.\n+- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:271-289` — An absolute-path specifier (e.g. `/etc/passwd`, or a `\\`-prefixed path) is not classified up front and falls through to the bare-import branch, where it is *rejected with a misleading error*: \"the plugin host does not resolve npm packages — bundle '/etc/passwd' into your plugin bundle yourself\". The specifier is rejected, so this is not a sandbox hole — but the diagnosis is wrong and confusing for a plugin author. The reference loader this code explicitly claims to mirror (`swissarmyhammer-js`'s `SandboxedModuleLoader`, lib.rs:157-159) has an explicit up-front guard `if specifier.starts_with('/') || specifier.starts_with('\\\\') { return Err(\"absolute import path rejected: ...\") }`. Add the same up-front absolute/backslash-prefix check in `resolve` (before the bare-import fallthrough) so an absolute import gets an honest, specific error.\n\n ### Nits\n-- [x] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:210-213` — The doc comment on `load_relative` states \"The `specifier` has already passed `resolve`, so it is a `file://` URL inside the bundle root.\" This is not true for the **entry/main module**: `resolve` returns early for `ResolutionKind::MainModule` (lines 263-267) without any canonicalize-and-contain check, so the main module's `file://` URL reaches `load_relative` having skipped the sandbox check. The entry path is host-derived (`bundle_dir.join(entry_file)` in `mod.rs`), so this is safe in scope — but the comment overstates the guarantee. Reword to note the main module is host-chosen and trusted, only *imports* are sandbox-verified, mirroring the accurate explanation already in `resolve`'s own doc comment (lines 240-252).\n-- [x] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:323-329` — `referrer_directory` is a verbatim copy of the identically-named private fn in `swissarmyhammer-js/src/lib.rs:199-205` (same signature, same body, same doc intent). Two private copies of a path-sandboxing helper is a quiet drift hazard: a future fix to one (e.g. handling a `file://` URL with a host component, or a percent-encoded path) will not reach the other. Not blocking — the crates are independent and neither depends on the other today — but worth a note for whoever owns the shared `swissarmyhammer-js` / plugin-runtime boundary, in case a small shared path-utility crate is warranted.\n\\ No newline at end of file\n+- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:210-213` — The doc comment on `load_relative` states \"The `specifier` has already passed `resolve`, so it is a `file://` URL inside the bundle root.\" This is not true for the **entry/main module**: `resolve` returns early for `ResolutionKind::MainModule` (lines 263-267) without any canonicalize-and-contain check, so the main module's `file://` URL reaches `load_relative` having skipped the sandbox check. The entry path is host-derived (`bundle_dir.join(entry_file)` in `mod.rs`), so this is safe in scope — but the comment overstates the guarantee. Reword to note the main module is host-chosen and trusted, only *imports* are sandbox-verified, mirroring the accurate explanation already in `resolve`'s own doc comment (lines 240-252).\n+- [ ] `crates/swissarmyhammer-plugin/src/runtime/module_loader.rs:323-329` — `referrer_directory` is a verbatim copy of the identically-named private fn in `swissarmyhammer-js/src/lib.rs:199-205` (same signature, same body, same doc intent). Two private copies of a path-sandboxing helper is a quiet drift hazard: a future fix to one (e.g. handling a `file://` URL with a host component, or a percent-encoded path) will not reach the other. Not blocking — the crates are independent and neither depends on the other today — but worth a note for whoever owns the shared `swissarmyhammer-js` / plugin-runtime boundary, in case a small shared path-utility crate is warranted.\n\\ No newline at end of file\n"}]]}
{"id":"01KRV1ZRJ6PZQY6HD6YS4DX78V","timestamp":"2026-05-17T13:28:39.750512Z","op":"update","item_id":"01KRRE7CA82C81G10NG4GB27HE","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KRRE6XMJAK3WH3EVMPTMZX8M\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: plugin-arch\n title: 'plugin: module loader — relative, bare, and @swissarmyhammer/* imports'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KRRE6XMJAK3WH3EVMPTMZX8M\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: plugin-arch\n title: 'plugin: module loader — relative, bare, and @swissarmyhammer/* imports'\n","transaction_id":null}
{"id":"01KRV1ZRJ77B49WBVJD06J82HF","timestamp":"2026-05-17T13:28:39.751474Z","entity_type":"task","entity_id":"01KRRE7CA82C81G10NG4GB27HE","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KRV23KA648C5RVVCZ2KTFT7X","timestamp":"2026-05-17T13:30:45.446210Z","op":"update","item_id":"01KRRE7CA82C81G10NG4GB27HE","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KRRE6XMJAK3WH3EVMPTMZX8M\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: fffffffffffffffffffffffffffffffffff880\n project: plugin-arch\n title: 'plugin: module loader — relative, bare, and @swissarmyhammer/* imports'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KRRE6XMJAK3WH3EVMPTMZX8M\n-position_column: done\n-position_ordinal: fffffffffffffffffffffffffffffffffff880\n+position_column: review\n+position_ordinal: '80'\n project: plugin-arch\n title: 'plugin: module loader — relative, bare, and @swissarmyhammer/* imports'\n ---\n","transaction_id":null}

---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01ky0hsd4rnrp1g5y6fraw6qeg
  text: 'Finish loop started (single-task mode). Iteration 1: delegating to /implement. Plan per card: ReviewProgressEvent in swissarmyhammer-validators threaded through run_review_over_agent → run_fleet; MCP bridge in review_op.rs via spawn_drain_task gated on progress_token; unit test in fleet/tests.rs + e2e review_progress_notifications_test.rs.'
  timestamp: 2026-07-20T19:59:29.944482+00:00
- actor: claude-code
  id: 01ky0j4fhn73r0w4e4nphjzfdv
  text: 'Picked up; research done. Call chain confirmed: run_review_over_agent (drive.rs) → run_pipeline_in_connection → run_review (synthesize.rs, loops batches) → run_fleet (fleet.rs). Plan: (1) ReviewProgressEvent + ReviewProgressSender alias in fleet.rs, threaded as Option through run_review_over_agent/run_pipeline_in_connection (owned) and run_review/run_fleet (Option<&sender>); Planned{total_pairs} emitted per batch after plan_fan_out (bridge accumulates totals), PairStarted per (validator,file) in submit_fan_out, PairDone per pair in collect_fan_out after each task resolves (covers failed + monolithic-fallback since it is one emit site). (2) Bridge in review_op.rs: spawn_review_progress_bridge(context) honoring token+sink > token+peer > none (code_context precedence), two channels: engine events → mapping task (cumulative planned/completed state, monotonic progress) → ProgressNotificationParam channel → existing spawn_drain_task/spawn_in_process_drain_task; spawned in execute_review on the outer runtime, only the UnboundedSender crosses into spawn_blocking. TDD: fleet unit test (2 validators, val-ok 2 files + val-bad 1 file with ScriptedReply::Error → 3 pairs, Planned+3 started+3 done), mapping unit tests in review_op.rs, e2e HTTP test reusing tests/integration/review_fixture.rs via #[path] like review_global_subscriber.rs, using handle.server().set_review_factories.'
  timestamp: 2026-07-20T20:05:32.853422+00:00
- actor: claude-code
  id: 01ky0k6jn0bajjfgxfcdgtnv5y
  text: 'Implementation landed, all green. Engine: ReviewProgressEvent + ReviewProgressSender in fleet.rs (no rmcp dep), threaded run_review_over_agent → run_pipeline_in_connection → run_review → run_fleet; Planned{total_pairs} per batch after plan_fan_out, PairStarted per (validator,file) in submit_fan_out, PairDone per pair at the single resolution point in collect_fan_out (covers success, monolithic fallback, fork-failed, dropped, unparseable — one emit site after `match collected`). Bridge: review_op.rs spawn_review_progress_bridge(context) with token+sink > token+peer > none precedence; two channels (engine events → mapping task with cumulative planned/completed counters → ProgressNotificationParam → existing spawn_drain_task/spawn_in_process_drain_task); execute_review spawns it on the outer runtime BEFORE spawn_blocking, threads only the sync sender in, and awaits the drain after the run (also on the early-error path, since the sender is moved and dropped either way). Messages: "Planned N (validator, file) review pairs" / "Reviewing {file} against {validator}" / "Reviewed {file} against {validator}" — full paths, never truncated. TDD: fleet unit test watched RED (0 events) then GREEN; mapping tests watched RED (missing fn) then GREEN. Tests green: cargo nextest run -p swissarmyhammer-validators review → 141 passed; -p swissarmyhammer-tools --test review_progress_notifications_test → 1 passed (e2e HTTP, token echo, monotonic, per-pair started/done, progress==total); full validators crate 306 passed; tools -E ''test(review)'' 36 passed. cargo fmt clean; cargo clippy --all-targets -D warnings clean on both crates. Also updated the one external caller crates/swissarmyhammer-agent/tests/review_real_model_e2e.rs (passes None); cargo check --workspace --all-targets green. Awaiting double-check verdict.'
  timestamp: 2026-07-20T20:24:10.144574+00:00
- actor: claude-code
  id: 01ky0kewdggsymcz9r7cavbxdz
  text: 'really-done complete: verification commands re-run green (cargo nextest run -p swissarmyhammer-validators review → 141 passed; cargo nextest run -p swissarmyhammer-tools --test review_progress_notifications_test → 1 passed) and the adversarial double-check agent returned PASS. Its pass confirmed: single PairDone emit site covers every resolution path (fork-failed fallback, dropped channel, unparseable) and its count equals the Planned total by construction; monotonicity holds under any Planned/PairDone interleaving (completed only increments, total = planned.max(progress)); no sender clone survives the pipeline so the awaited drain cannot hang (early-error and panic paths drop it too); all None call sites byte-identical to pre-change behavior; validators crate gained no rmcp dependency (Cargo.toml untouched). Noted on-contract: PairStarted repeats the current progress value (non-decreasing, matching the code_context precedent). Work complete and green — leaving the task in doing for /review.'
  timestamp: 2026-07-20T20:28:42.288211+00:00
- actor: claude-code
  id: 01ky0kg0ft6rdn43vfgyzev050
  text: 'Iteration 1 implement landed green. ReviewProgressEvent (Planned/PairStarted/PairDone) + ReviewProgressSender in fleet.rs, threaded through synthesize.rs run_review + drive.rs run_review_over_agent (None = unchanged behavior); MCP bridge spawn_review_progress_bridge in review_op.rs (sink > peer precedence, monotonic cumulative counters, drain awaited after run so notifications flush before the result); execute_review spawns bridge on outer runtime before spawn_blocking. Tests: fleet unit (2 validators × 3 files incl. failing task), review_op mapping/gating units, new e2e review_progress_notifications_test.rs. cargo nextest: validators 306 passed, tools review tests 36 passed, e2e 1 passed; fmt+clippy clean. Next: /test then checkpoint /commit then /review.'
  timestamp: 2026-07-20T20:29:19.226900+00:00
- actor: claude-code
  id: 01ky0rgeh98nt6e239p2cdta67
  text: 'Test-gate note: the rdeps(swissarmyhammer-validators) sweep was stopped after ~1h — it descended into swissarmyhammer-agent''s real-model agent_tests binary (llama/qwen, minutes per test). Lesson for this card''s gates: scope to `cargo nextest run -p swissarmyhammer-validators -p swissarmyhammer-tools`; the only swissarmyhammer-agent change is a one-line test-callsite (None param) covered by cargo check --workspace --all-targets. Re-running the leaner gate now.'
  timestamp: 2026-07-20T21:56:56.489743+00:00
- actor: claude-code
  id: 01ky0rvakrqwcddpn0p63st5sj
  text: 'Lean test gate GREEN (1725 passed on validators+tools, fmt/clippy clean). Checkpoint committed: 5a176d4 "feat(review): emit notifications/progress per validator x file pair during fan-out (^jn2wjd5)". Sending to /review scoped to HEAD~1..HEAD.'
  timestamp: 2026-07-20T22:02:52.920831+00:00
- actor: claude-code
  id: 01ky0tzsfr8b4wyjjzzqvmevxg
  text: 'Review findings pass: pulled back from review to doing, fixed all 14 findings, flipped them to [x]. What changed: (1) manual Debug for ReviewTool in tools review/mod.rs rendering the trait-object factories by presence/absence (Some("AgentFactory")/None), with a new unit test review_tool_debug_summarizes_factory_presence watched RED (E0277) then GREEN; (2) #[derive(Debug)] on ReviewProgressBridge in review_op.rs (tokio UnboundedSender/JoinHandle both impl Debug); (3) documented the `progress` param in run_review_over_agent''s docstring (drive.rs) and, same root cause, in run_review''s (synthesize.rs); (4-5) extracted CHANGE_PURPOSE_HEADER const in fleet.rs, used by render_fleet_prompt + render_run_prime (placed next to PRIME_HANDOFF/VALIDATOR_HEADER per the existing pattern); (6-14) synthesize.rs encapsulation: FleetTally.attempted/.failed, all five ReviewCounts fields, and ReviewReport.markdown/.counts are now private with doc''d getters; kept FleetTally::new, added ReviewReport::into_markdown so ReviewResponse::from moves the markdown out without a clone. Call sites updated: check_review_completeness + ReviewResponse::from (getters), review_op.rs test fixture report_with_tally now builds via synthesize(vec![], &FleetTally::new(..), "now") — the one public construction path — drive.rs tests (report.markdown()/counts().findings()/confirmed()), and swissarmyhammer-agent/tests/review_real_model_e2e.rs. Wire shape unchanged: ReviewResponse/ReviewCountsView serialize the same field names/values (no serde on the engine types). Gates: cargo nextest run -p swissarmyhammer-validators -p swissarmyhammer-tools → 1726 passed / 0 failed; cargo clippy (both crates, --all-targets, -D warnings) clean; cargo check -p swissarmyhammer-agent --all-targets clean; cargo fmt --check clean. Awaiting double-check verdict; task stays in doing.'
  timestamp: 2026-07-20T22:40:16.376564+00:00
- actor: claude-code
  id: 01ky0v106w4q8q63hxr4fk7yrt
  text: 'The original heavy gate (rdeps sweep) eventually completed after being redirected to targeted scope: GREEN — 1818 run, 1817 passed, fmt/clippy clean. Sole failure = known Qwen3-0.6B real-model flake in review_real_model_e2e (cold-HF-cache timeouts + intermittent post-synthesis connect_with teardown hang after a spurious tool call; 3 consecutive passes after cache warm; progress param is None in that test so this change is not in the hang path). Filed as ^nmms6bb (#test-failure, marked intermittent). Iteration 2 (14 review findings) in progress.'
  timestamp: 2026-07-20T22:40:56.028829+00:00
- actor: claude-code
  id: 01ky0v4tknz7g4nmx82he5cyhx
  text: 'Double-check verdict: REVISE with two minor findings, both now resolved. (1) FIXED — the ReviewTool Debug test only exercised embedder_factory''s None arm; extended review_tool_debug_summarizes_factory_presence to also chain .with_embedder_factory(mock_embedder_factory()) and assert "embedder_factory: Some". Re-verified: cargo nextest -p swissarmyhammer-tools -E ''test(/tools::review::tests/)'' → 17/17 passed; clippy --all-targets -D warnings clean; fmt clean. (2) PROCEED WITH JUSTIFICATION — the extra `progress` docstring paragraph on run_review (synthesize.rs) goes beyond finding 3''s named function (run_review_over_agent in drive.rs). Kept deliberately: run_review is the sibling public entry point with the identical newly-added `progress` parameter, so leaving it undocumented reproduces the exact class of gap the finding flags; doc-only, zero behavior impact. Everything else the double-check verified clean: zero remaining direct field reads/constructions of ReviewReport/ReviewCounts/FleetTally outside synthesize.rs, wire shape byte-identical, completeness tests unaffected by the synthesize-built fixture, diff scoped to exactly the finding files. All 14 findings [x], task stays in doing for /review.'
  timestamp: 2026-07-20T22:43:01.365973+00:00
- actor: claude-code
  id: 01ky0x3b1zndengs90rb73q2rk
  text: |-
    Fixed all 13 findings from the 2026-07-20 17:46 review sweep (encapsulation + typed errors), at root, in review_op.rs and fleet.rs.

    review_op.rs:
    - AgentHandle: fields private; added `new(agent, notification_rx)` + `into_parts()` (the driver consumes both halves by value, so a by-value split, not borrowing getters) + a manual Debug (DynConnectTo is not Debug).
    - EmbedderFactory now returns `Result<Arc<dyn TextEmbedder>, EmbedderError>` — new `EmbedderError { Resolve, Load }` thiserror enum; `shared_embedder` and `default_embedder_factory` migrated.
    - ReviewRequest: fields private; `ReviewRequest::new(scope)` + `with_backend/with_validators/with_concurrency/with_batch_size` builder (matches the ReviewTool builder pattern) + getters.
    - run_review_request: `repo_path: &Path`, `now: &str` (owned clones taken only at the spawn_blocking boundary), returns `Result<ReviewReport, ReviewError>` — new thiserror enum with GateClosed/Runtime/Join/ValidatorLoad/IndexMissing/IndexOpen/IndexConfigure/Embedder/Agent/Pipeline/Incomplete variants. Display strings preserved verbatim from the old format!() messages (the real-model e2e's `incomplete review:` / model-unavailable substring matches still hold; Agent(String) is `{0}` pass-through so factory messages are unchanged on the wire). open_index_connection and check_review_completeness now return ReviewError too.
    - ReviewProgressBridge: fields private + `into_parts()` (both halves used by value in execute_review).
    - ReviewResponse + ReviewCountsView: fields private with getters; serde serializes the private fields by name so the wire shape is byte-identical (guarded by a new unit test asserting the JSON keys/values plus getters and Copy/Eq semantics). ReviewCountsView now derives Debug, Clone, Copy, PartialEq, Eq, Serialize.

    fleet.rs:
    - FleetConfig: `batch_size` private + `FleetConfig::new(batch_size)` + `batch_size()` getter; Default delegates to new(DEFAULT_BATCH_SIZE).
    - FleetOutcome: `findings` and `prime` private + `findings()`/`prime()` getters + `into_parts(self) -> (Vec<Finding>, Option<SessionPinGuard>)` for the by-value consumption in run_review (findings move to build_candidates, prime released via unpin_prefix_session).

    Call sites migrated (every out-of-crate site found by workspace grep): tools review/mod.rs execute_review (builder, into_parts, &Path/&str, error `.to_string()` into rmcp ErrorData); tools review/tests.rs (AgentHandle::new, ReviewRequest builder, borrowed args); tests/integration/review_fixture.rs (AgentHandle::new); swissarmyhammer-agent/src/lib.rs review_agent_factory (AgentHandle::new); swissarmyhammer-agent/tests/review_real_model_e2e.rs (builder, &Path/&str, err.to_string()); validators synthesize.rs (batch_size() getter, into_parts instead of struct destructure); drive.rs test (FleetConfig::new); fleet/tests.rs (batch_size() getter). AgentFactory's `Result<AgentHandle, String>` deliberately unchanged — the review flagged only EmbedderFactory and run_review_request, and the ReviewError::Agent(String) variant documents the seam.

    Both files swept: zero remaining pub fields on pub structs (enum variant payloads excepted).

    Gates: cargo nextest run -p swissarmyhammer-validators -p swissarmyhammer-tools → 1727/1727 passed; cargo fmt applied; cargo clippy (both crates, --all-targets, -D warnings) clean; cargo check --workspace --all-targets clean.
  timestamp: 2026-07-20T23:17:09.823410+00:00
position_column: doing
position_ordinal: '8280'
title: 'Review MCP tool: emit notifications/progress per validator×file pair during the fan-out'
---
## What

The `review` MCP tool currently returns one batch result at the end and emits **zero** MCP notifications while it runs. The server already has all the progress plumbing — `crates/swissarmyhammer-tools/src/mcp/progress.rs` (`spawn_drain_task`, `spawn_in_process_drain_task`) and `ToolContext.progress_token` / `ToolContext.peer` / `ToolContext.progress_sink` in `crates/swissarmyhammer-tools/src/mcp/tool_registry.rs` — but only `code_context` `op: "rebuild index"` is wired to it. Wire the review pipeline to it so a client that passes `_meta.progressToken` gets frequent `notifications/progress` as the review evaluates.

**Granularity:** the engine's fan-out unit (`plan_fan_out` in `crates/swissarmyhammer-validators/src/review/fleet.rs`) is one `ValidatorTask` per validator (RuleSet) × file batch; all of a validator's rules go to the agent in one prompt, so the finest real evaluation grain is the **(validator, file) pair**. Emit one progress event per pair — on task submission (`submit_fan_out`) for every file the task covers ("reviewing <file> against <validator>") and on task completion (`collect_fan_out`, including failed/degraded tasks) — with `total` = the number of planned (validator, file) pairs.

Implementation shape:

1. **Engine events (`swissarmyhammer-validators`, no rmcp dependency):** define a small `ReviewProgressEvent` (e.g. `Planned { total_pairs }`, `PairStarted { validator, file }`, `PairDone { validator, file }`) next to the fleet code, and thread an `Option<tokio::sync::mpsc::UnboundedSender<ReviewProgressEvent>>` through `run_review_over_agent` (`crates/swissarmyhammer-validators/src/review/drive.rs`) into `run_fleet` / `submit_fan_out` / `collect_fan_out` (`fleet.rs`). `None` = today's behavior. UnboundedSender::send is sync, so it works inside the nested runtime.
2. **MCP bridge (`swissarmyhammer-tools`):** in `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs`, when a progress token is present, create the channel, map each `ReviewProgressEvent` to a `ProgressNotificationParam` (echoing the token, monotonic `progress` = completed-pair count, `total` = planned pairs, human `message` naming the validator and file — full paths, never truncated), and forward via the existing `spawn_drain_task(peer, rx)` (or `context.progress_sink` when set, matching the code_context precedence). **Runtime caveat:** `run_review_request` runs the pipeline inside `spawn_blocking` with a nested current-thread runtime — spawn the drain task on the outer runtime *before* entering `spawn_blocking` and hand only the sender in.
3. **Wiring:** `execute_review` in `crates/swissarmyhammer-tools/src/mcp/tools/review/mod.rs` already receives `&ToolContext`; pass the token/peer/sink (or the constructed sender) into `run_review_request`. No token and no sink → pass `None`, emit nothing (same contract as rebuild index's NoopReporter).

Out of scope: progress for the verify/synthesize stages (can be a follow-up event variant later); changing the returned `ReviewReport` shape.

Subtasks:
- [x] `ReviewProgressEvent` + optional sender threaded through `run_review_over_agent` → `run_fleet` (drive.rs, fleet.rs)
- [x] Emit `Planned` after `plan_fan_out`, `PairStarted` per (validator, file) in `submit_fan_out`, `PairDone` per pair in `collect_fan_out` — including failed/monolithic-fallback tasks so progress always reaches `total`
- [x] Bridge events → `ProgressNotificationParam` in `review_op.rs`, drained through `spawn_drain_task` / `progress_sink`, gated on `progress_token`
- [x] Pass context through `execute_review` in tools `review/mod.rs`
- [x] Unit + e2e tests (below)

## Acceptance Criteria
- [ ] A `tools/call` for `review` (`op: "review working"`) carrying `_meta.progressToken` receives at least one `notifications/progress` per planned (validator, file) pair before the final result arrives
- [ ] Every notification echoes the request's progress token; `progress` is monotonically non-decreasing on the wire and reaches `total` (the planned pair count) even when fan-out tasks fail
- [ ] Each notification `message` names the validator and the file path, untruncated
- [ ] A call with no `progressToken` (and no in-process sink) emits zero progress notifications and returns the identical `ReviewReport` as today
- [ ] `swissarmyhammer-validators` gains no rmcp/MCP dependency; event emission with a `None` sender is a no-op on the existing code path

## Tests
- [x] Unit test in `crates/swissarmyhammer-validators/src/review/fleet/tests.rs` (scripted agent, existing harness): run the fleet with a progress sender over 2 validators × known files; assert one `Planned` with the correct pair total, and `PairStarted`/`PairDone` for every (validator, file) pair including a deliberately failing task
- [x] New e2e `crates/swissarmyhammer-tools/tests/review_progress_notifications_test.rs` modeled on `rebuild_index_progress_notifications_test.rs`: real in-process HTTP MCP server + scripted review agent, client overrides `on_progress`, calls `review` with a `progressToken`; assert ≥1 notification per pair, token echo, monotonic progress
- [x] Run: `cargo test -p swissarmyhammer-validators review::fleet` and `cargo test -p swissarmyhammer-tools --test review_progress_notifications_test` — both green; keep unit tests under 10s (scripted agents only, no real model)

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass. #review #mcp #progress

## Review Findings (2026-07-20 17:03)

- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/mod.rs:110` — ReviewTool is a public struct with non-empty representation (three fields) but lacks a Debug implementation, violating the rule that all public types with non-empty representation must implement Debug. Add a Debug implementation to ReviewTool, either by implementing it manually to avoid trait object issues or refactoring to use Debuggable wrapper types.
- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:133` — ReviewProgressBridge is a public struct with non-empty representation but lacks a Debug implementation. Both its fields (ReviewProgressSender and JoinHandle<()>) implement Debug, so deriving Debug is straightforward and required. Add #[derive(Debug)] to the ReviewProgressBridge struct definition.
- [x] `crates/swissarmyhammer-validators/src/review/drive.rs:76` — The `progress` parameter (newly added to run_review_over_agent) is not documented in the public function's docstring, violating the requirement that parameters of public functions be documented. Add documentation for the `progress` parameter in the function docstring, explaining its purpose (enabling progress notifications for the review run) and when it should be Some vs None.
- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:673` — Repeated literal "# Change purpose\n\n" appears in multiple functions; extract to a named constant so it changes in one place. Define `const CHANGE_PURPOSE_HEADER: &str = "# Change purpose\n\n";` at the module level and use it in both functions.
- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:708` — Repeated literal "# Change purpose\n\n" appears in multiple functions; extract to a named constant so it changes in one place. Define `const CHANGE_PURPOSE_HEADER: &str = "# Change purpose\n\n";` at the module level and use it in both functions.
- [x] `crates/swissarmyhammer-validators/src/review/synthesize.rs:37` — `FleetTally::attempted` is a public field, making it a permanent commitment; adding validation or computed properties later breaks downstream code that directly mutates this field. Make the field private and expose it via a getter method `pub fn attempted(&self) -> usize`; allow construction via `FleetTally::new(attempted, failed)`.
- [x] `crates/swissarmyhammer-validators/src/review/synthesize.rs:39` — `FleetTally::failed` is a public field, making it a permanent commitment; future versions cannot add validation or derived logic without breaking code that directly accesses or mutates it. Make the field private and expose it via `pub fn failed(&self) -> usize`; use `FleetTally::new(attempted, failed)` for construction.
- [x] `crates/swissarmyhammer-validators/src/review/synthesize.rs:60` — `ReviewCounts::findings` is a public field, preventing future validation, derived computation, or refactoring without breaking callers that directly access this field. Make the field private and expose it via a getter method `pub fn findings(&self) -> usize`.
- [x] `crates/swissarmyhammer-validators/src/review/synthesize.rs:62` — `ReviewCounts::confirmed` is a public field, creating a permanent API commitment that prevents future refactoring or validation logic. Make the field private and expose it via `pub fn confirmed(&self) -> usize`.
- [x] `crates/swissarmyhammer-validators/src/review/synthesize.rs:64` — `ReviewCounts::refuted` is a public field, making it a permanent part of the public API that cannot be refactored or validated later. Make the field private and expose it via `pub fn refuted(&self) -> usize`.
- [x] `crates/swissarmyhammer-validators/src/review/synthesize.rs:66` — `ReviewCounts::tasks_attempted` is a public field that cannot be changed later without breaking callers; direct field access is a semver hazard. Make the field private and expose it via `pub fn tasks_attempted(&self) -> usize`.
- [x] `crates/swissarmyhammer-validators/src/review/synthesize.rs:68` — `ReviewCounts::tasks_failed` is a public field that locks the struct layout; adding validation or derived logic later would be a breaking change for callers accessing this directly. Make the field private and expose it via `pub fn tasks_failed(&self) -> usize`.
- [x] `crates/swissarmyhammer-validators/src/review/synthesize.rs:78` — `ReviewReport::markdown` is a public field, making it a permanent API commitment; future changes (e.g., structured rendering, caching) require callers to update. Make the field private and expose it via `pub fn markdown(&self) -> &str`.
- [x] `crates/swissarmyhammer-validators/src/review/synthesize.rs:80` — `ReviewReport::counts` is a public field that locks the struct design; future refactorings (e.g., splitting counts into multiple structures) would break any caller that directly accesses this field. Make the field private and expose it via `pub fn counts(&self) -> &ReviewCounts`.

*Note: 3 additional engine findings against pre-existing test helpers in `crates/swissarmyhammer-validators/src/review/fleet/tests.rs` (`loader_with`, `forking_agent`, `agent_with_fork_mode` signature refactors) were dropped per the existing-test refactor exception — those helpers were not touched by this commit.*

## Review Findings (2026-07-20 17:46)

- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:27` — Public struct `AgentHandle` exposes public fields (`agent` and `notification_rx`), which are permanent API commitments that cannot be changed without breaking downstream code. Make the fields private and expose them through getter methods. For example: `pub fn agent(&self) -> &DynConnectTo<Client>` and `pub fn notification_rx(&self) -> &broadcast::Receiver<SessionNotification>`.
- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:44` — Type alias `EmbedderFactory` exposes a public factory function that returns `Result<Arc<dyn TextEmbedder>, String>`, using untyped string errors instead of a typed error enum. Define a typed error enum and update the type alias to return `Result<Arc<dyn TextEmbedder>, EmbedderError>` (or appropriate enum name) instead of `Result<..., String>`.
- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:162` — Public struct `ReviewRequest` exposes five public fields (`scope`, `backend`, `validators`, `concurrency`, `batch_size`), which are permanent API commitments that cannot be changed without breaking downstream code. Make all fields private and expose them through getter methods. Provide a constructor function or builder pattern to allow construction by callers.
- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:222` — Public function `run_review_request` accepts `repo_path: PathBuf`, a concrete type, instead of a generic reference like `&Path` or `impl AsRef<Path>`. Change the parameter to `repo_path: &Path` to accept any path type without requiring callers to own a PathBuf. Matches the signature of the downstream `run_review_over_agent` (drive.rs:54) which already uses `repo_path: &Path`.
- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:225` — Public function `run_review_request` accepts `now: String`, a concrete owned type, instead of a borrowed reference `&str`. Change the parameter to `now: &str` to avoid requiring callers to own a String. Matches the signature of the downstream `run_review_over_agent` (drive.rs:61) which already uses `now: &str`.
- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:226` — Public function `run_review_request` returns `Result<ReviewReport, String>`, but library code must use typed error enums, not untyped strings. Define a typed error enum (via `thiserror`) and return `Result<ReviewReport, ReviewError>` (or appropriate enum name) instead of `Result<..., String>`.
- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:322` — Public struct `ReviewProgressBridge` exposes two public fields (`sender` and `drain`), which are permanent API commitments that cannot be changed without breaking downstream code. Make the fields private and expose them through getter methods or conversion methods (`pub fn sender(&self)`, `pub fn into_sender(self)`, `pub fn drain(&self)`, `pub fn into_drain(self)`) to preserve flexibility for future changes.
- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:449` — Public struct `ReviewResponse` exposes public fields (`markdown` and `counts`), which are permanent API commitments that cannot be changed without breaking downstream code. Make the fields private and expose them through getter methods. Preserve serde serialization using `#[serde(rename)]` if needed on the getter implementations or via struct-level serde annotations.
- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:459` — Public struct `ReviewCountsView` exposes five public fields (`findings`, `confirmed`, `refuted`, `attempted`, `failed`), which are permanent API commitments that cannot be changed without breaking downstream code. Make all fields private and expose them through getter methods (`pub fn findings(&self) -> usize`, etc.). Preserve serde serialization via struct-level serde annotations if needed.
- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:459` — Public struct `ReviewCountsView` does not derive `Clone`, `PartialEq`, or `Eq`, even though all its fields (five `usize` fields) are `Clone`, `PartialEq`, and `Eq`. Add derives: `#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize)]` to ReviewCountsView to match the pattern in synthesize.rs where ReviewCounts derives all these traits.
- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:51` — Public struct `FleetConfig` exposes public field `batch_size`, which is a permanent API commitment that cannot be changed without breaking downstream code. Make the field private and expose it through a getter method `pub fn batch_size(&self) -> usize`. Provide a constructor or builder pattern for construction.
- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:60` — Public struct `FleetOutcome` exposes public field `findings` (Vec<Finding>), which is a permanent API commitment that cannot be changed without breaking downstream code. Make the field private and expose it through a getter method `pub fn findings(&self) -> &[Finding]`. Maintain consistency with the already-private fields.
- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:73` — Public struct `FleetOutcome` exposes public field `prime` (Option<SessionPinGuard>), which is a permanent API commitment that cannot be changed without breaking downstream code. Make the field private and expose it through a getter method `pub fn prime(&self) -> Option<&SessionPinGuard>`. Maintain consistency with the already-private fields.
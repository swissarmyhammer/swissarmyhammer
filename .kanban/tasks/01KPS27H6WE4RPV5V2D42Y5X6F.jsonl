{"id":"01KPS27H6W6CS18XWHG247Y8BM","timestamp":"2026-04-21T22:23:01.852410Z","op":"create","item_id":"01KPS27H6WE4RPV5V2D42Y5X6F","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,95 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: e080\n+project: spatial-nav\n+title: 'Toolbar: wrap NavBar buttons in FocusScope so Up from perspective/LeftNav reaches them'\n+---\n+## What\n+\n+The top app toolbar (`kanban-app/ui/src/components/nav-bar.tsx`) has no `FocusScope` wrappers on any interactive element. The Rust spatial engine never receives rect registrations for the toolbar, so pressing Up from the perspective tab bar or LeftNav has no candidate above — nothing to land on.\n+\n+### Current state (nav-bar.tsx)\n+\n+The `<header>` is 48px tall (`h-12`) and contains four interactive elements, all bare buttons/components:\n+\n+| Element | Line | What it does | Moniker proposal |\n+|---|---|---|---|\n+| `BoardSelector` (board name + dropdown) | 35-43 | Switches boards, edits name | `toolbar:board-selector` |\n+| Info button (inspect board) | 45-62 | Dispatches `ui.inspect` with the board moniker | `toolbar:inspect-board` |\n+| `Field` (percent_complete compact) | 63-71 | Renders the board's percent — this is already a field-style component; may already produce its own FocusScope via the field layer | (confirm during implementation) |\n+| Search button | 72-84 | Dispatches `app.search` | `toolbar:search` |\n+\n+None are `FocusScope`-wrapped. The layout geometry is fine — `header` is at `top: 0`, perspective tabs at `top: 48`, LeftNav buttons start below the header — so rect math would work if rects were registered.\n+\n+### Fix direction\n+\n+Wrap each interactive element in `<FocusScope renderContainer={false}>` using the existing pattern proven in LeftNav (`ViewButton` / `ViewButtonElement`) and perspective tabs (`ScopedPerspectiveTab`):\n+\n+- Use `renderContainer={false}` — the button is the DOM node, no wrapper div\n+- Attach `useFocusScopeElementRef()` to the button via a ref callback (so `ResizeObserver` measures the button's rect)\n+- Pick namespaced monikers: `toolbar:board-selector`, `toolbar:inspect-board`, `toolbar:search`\n+- Leave the FocusLayer inheritance as-is — AppShell's `<FocusLayer name=\"window\">` is the parent, and toolbar scopes should live in that layer so they're reachable from any view\n+\n+### Enter-activate integration\n+\n+Per the pattern from `01KPRS0WVK7YMS20PEY12ZY70W`, each toolbar FocusScope should pass `commands` that bind Enter to the button's existing click action:\n+\n+- `toolbar:inspect-board` — Enter → `dispatchInspect({ target: board.board.moniker })` (same as the existing `onClick`). Note: after `01KPRS0WVK7YMS20PEY12ZY70W` lands with the YAML-level `ui.inspect` Enter binding, this may become redundant for the inspect button specifically. Confirm during implementation.\n+- `toolbar:search` — Enter → `dispatchSearch()`\n+- `toolbar:board-selector` — more nuanced: Enter should open the dropdown (equivalent to clicking). Depends on BoardSelector's internal wiring — inspect `kanban-app/ui/src/components/board-selector.tsx` to find the correct activation path and mirror it.\n+\n+### The `Field` percent_complete component\n+\n+If `<Field mode=\"compact\">` already produces a FocusScope for its value cell (check `kanban-app/ui/src/components/fields/field.tsx`), don't double-wrap — just confirm it registers a rect in the window layer. If it doesn't (e.g. compact mode suppresses the scope), add a wrapper.\n+\n+### Files to modify\n+\n+- `kanban-app/ui/src/components/nav-bar.tsx` — wrap 3 interactive elements in FocusScope with commands bound to Enter\n+- `kanban-app/ui/src/components/board-selector.tsx` — may need a FocusScope wrapper or internal Enter binding to open the dropdown\n+- `kanban-app/ui/src/components/fields/field.tsx` — only if `mode=\"compact\"` doesn't already FocusScope its value\n+\n+### Relationship to other tasks\n+\n+- **Independent** of `01KPS1WCQRY8DEWQVA47PZ82ZC` (beam-test pool-split fix) — even with the current beam test, Up from perspective tab to toolbar button would work if rects are registered, because both have horizontal overlap (toolbar and perspective bar span the same horizontal region). But if the perspective tab's horizontal position puts it outside the toolbar button's x-range, the pool-split fix becomes necessary for this to work reliably.\n+- **Complements** `01KPRS0WVK7YMS20PEY12ZY70W` (Enter-to-activate) — that task's YAML-level `ui.inspect` Enter binding would automatically apply to the Info button IF it's in a FocusScope whose commands include `ui.inspect`. Land the YAML change first if you want the inspect button to work \"for free.\"\n+\n+### Out of scope\n+\n+- Redesigning the toolbar\n+- Adding new toolbar actions\n+- Making the window controls (if any) spatially navigable — that's a Tauri-window concern, separate\n+\n+## Acceptance Criteria\n+\n+- [ ] Each interactive element in `NavBar` (BoardSelector, Info/Inspect button, percent_complete Field, Search button) registers a spatial rect in the window layer via `FocusScope`\n+- [ ] From a focused perspective tab, pressing Up lands on the toolbar element directly above it (typically the Info button or BoardSelector depending on horizontal position)\n+- [ ] From a focused LeftNav button, pressing Up lands on the toolbar element at the top-left (likely `toolbar:board-selector`) — provided the user is on the topmost LeftNav button; otherwise Up should step through LeftNav buttons first\n+- [ ] Toolbar buttons show the standard `data-focused` focus bar indicator\n+- [ ] Pressing Enter on the Info/Inspect button opens the board inspector\n+- [ ] Pressing Enter on the Search button dispatches `app.search`\n+- [ ] Pressing Enter on the BoardSelector opens its dropdown (same as clicking)\n+- [ ] Pressing Down from a toolbar button lands on the perspective tab bar or LeftNav (whichever is directly below — symmetric with the Up test)\n+- [ ] h/l between toolbar buttons moves left/right across the toolbar\n+- [ ] No regression in any existing spatial nav test\n+\n+## Tests\n+\n+- [ ] Add a vitest-browser test `kanban-app/ui/src/components/nav-bar.test.tsx` that renders `NavBar` inside the shared fixture shell and asserts each interactive element has `data-moniker` and gets a rect rect registered\n+- [ ] Add a spatial nav test (in `kanban-app/ui/src/components/nav-bar.test.tsx` or a new `spatial-toolbar-nav.test.tsx`) that:\n+  - Renders NavBar + PerspectiveTabBar + LeftNav in the fixture shell\n+  - Focuses a perspective tab\n+  - Dispatches k (Up)\n+  - Asserts focus lands on a toolbar moniker (e.g. `toolbar:inspect-board`)\n+- [ ] Add a parity case in `kanban-app/ui/src/test/spatial-parity-cases.json` for Up-from-perspective-to-toolbar so Rust (`swissarmyhammer-spatial-nav/tests/parity.rs`) and JS (`spatial-shim-parity.test.ts`) agree\n+- [ ] Add Enter-activation tests: focus inspect button, press Enter, assert `ui.inspect` dispatched with board moniker; focus search, assert `app.search` dispatched\n+- [ ] `cd kanban-app/ui && npm test` — 1301+ tests pass, new tests green\n+- [ ] Manual: click a perspective tab, press k → focus lands on toolbar. Click a LeftNav button, press k → focus lands on toolbar (from the topmost LeftNav button) or the previous LeftNav button (if not at the top).\n+\n+## Workflow\n+\n+- Use `/tdd` — write the failing \"Up from perspective tab lands on toolbar\" test first, then add the FocusScope wrappers.\n+- Start with the simplest toolbar element (Search button) — fewest moving parts, cleanest pattern.\n+- Do the BoardSelector last — it may expose internal state-machine complexity that deserves its own follow-up task if the existing click path is deeply coupled.\n+\n","reverse_patch":"--- original\n+++ modified\n@@ -1,95 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: e080\n-project: spatial-nav\n-title: 'Toolbar: wrap NavBar buttons in FocusScope so Up from perspective/LeftNav reaches them'\n----\n-## What\n-\n-The top app toolbar (`kanban-app/ui/src/components/nav-bar.tsx`) has no `FocusScope` wrappers on any interactive element. The Rust spatial engine never receives rect registrations for the toolbar, so pressing Up from the perspective tab bar or LeftNav has no candidate above — nothing to land on.\n-\n-### Current state (nav-bar.tsx)\n-\n-The `<header>` is 48px tall (`h-12`) and contains four interactive elements, all bare buttons/components:\n-\n-| Element | Line | What it does | Moniker proposal |\n-|---|---|---|---|\n-| `BoardSelector` (board name + dropdown) | 35-43 | Switches boards, edits name | `toolbar:board-selector` |\n-| Info button (inspect board) | 45-62 | Dispatches `ui.inspect` with the board moniker | `toolbar:inspect-board` |\n-| `Field` (percent_complete compact) | 63-71 | Renders the board's percent — this is already a field-style component; may already produce its own FocusScope via the field layer | (confirm during implementation) |\n-| Search button | 72-84 | Dispatches `app.search` | `toolbar:search` |\n-\n-None are `FocusScope`-wrapped. The layout geometry is fine — `header` is at `top: 0`, perspective tabs at `top: 48`, LeftNav buttons start below the header — so rect math would work if rects were registered.\n-\n-### Fix direction\n-\n-Wrap each interactive element in `<FocusScope renderContainer={false}>` using the existing pattern proven in LeftNav (`ViewButton` / `ViewButtonElement`) and perspective tabs (`ScopedPerspectiveTab`):\n-\n-- Use `renderContainer={false}` — the button is the DOM node, no wrapper div\n-- Attach `useFocusScopeElementRef()` to the button via a ref callback (so `ResizeObserver` measures the button's rect)\n-- Pick namespaced monikers: `toolbar:board-selector`, `toolbar:inspect-board`, `toolbar:search`\n-- Leave the FocusLayer inheritance as-is — AppShell's `<FocusLayer name=\"window\">` is the parent, and toolbar scopes should live in that layer so they're reachable from any view\n-\n-### Enter-activate integration\n-\n-Per the pattern from `01KPRS0WVK7YMS20PEY12ZY70W`, each toolbar FocusScope should pass `commands` that bind Enter to the button's existing click action:\n-\n-- `toolbar:inspect-board` — Enter → `dispatchInspect({ target: board.board.moniker })` (same as the existing `onClick`). Note: after `01KPRS0WVK7YMS20PEY12ZY70W` lands with the YAML-level `ui.inspect` Enter binding, this may become redundant for the inspect button specifically. Confirm during implementation.\n-- `toolbar:search` — Enter → `dispatchSearch()`\n-- `toolbar:board-selector` — more nuanced: Enter should open the dropdown (equivalent to clicking). Depends on BoardSelector's internal wiring — inspect `kanban-app/ui/src/components/board-selector.tsx` to find the correct activation path and mirror it.\n-\n-### The `Field` percent_complete component\n-\n-If `<Field mode=\"compact\">` already produces a FocusScope for its value cell (check `kanban-app/ui/src/components/fields/field.tsx`), don't double-wrap — just confirm it registers a rect in the window layer. If it doesn't (e.g. compact mode suppresses the scope), add a wrapper.\n-\n-### Files to modify\n-\n-- `kanban-app/ui/src/components/nav-bar.tsx` — wrap 3 interactive elements in FocusScope with commands bound to Enter\n-- `kanban-app/ui/src/components/board-selector.tsx` — may need a FocusScope wrapper or internal Enter binding to open the dropdown\n-- `kanban-app/ui/src/components/fields/field.tsx` — only if `mode=\"compact\"` doesn't already FocusScope its value\n-\n-### Relationship to other tasks\n-\n-- **Independent** of `01KPS1WCQRY8DEWQVA47PZ82ZC` (beam-test pool-split fix) — even with the current beam test, Up from perspective tab to toolbar button would work if rects are registered, because both have horizontal overlap (toolbar and perspective bar span the same horizontal region). But if the perspective tab's horizontal position puts it outside the toolbar button's x-range, the pool-split fix becomes necessary for this to work reliably.\n-- **Complements** `01KPRS0WVK7YMS20PEY12ZY70W` (Enter-to-activate) — that task's YAML-level `ui.inspect` Enter binding would automatically apply to the Info button IF it's in a FocusScope whose commands include `ui.inspect`. Land the YAML change first if you want the inspect button to work \"for free.\"\n-\n-### Out of scope\n-\n-- Redesigning the toolbar\n-- Adding new toolbar actions\n-- Making the window controls (if any) spatially navigable — that's a Tauri-window concern, separate\n-\n-## Acceptance Criteria\n-\n-- [ ] Each interactive element in `NavBar` (BoardSelector, Info/Inspect button, percent_complete Field, Search button) registers a spatial rect in the window layer via `FocusScope`\n-- [ ] From a focused perspective tab, pressing Up lands on the toolbar element directly above it (typically the Info button or BoardSelector depending on horizontal position)\n-- [ ] From a focused LeftNav button, pressing Up lands on the toolbar element at the top-left (likely `toolbar:board-selector`) — provided the user is on the topmost LeftNav button; otherwise Up should step through LeftNav buttons first\n-- [ ] Toolbar buttons show the standard `data-focused` focus bar indicator\n-- [ ] Pressing Enter on the Info/Inspect button opens the board inspector\n-- [ ] Pressing Enter on the Search button dispatches `app.search`\n-- [ ] Pressing Enter on the BoardSelector opens its dropdown (same as clicking)\n-- [ ] Pressing Down from a toolbar button lands on the perspective tab bar or LeftNav (whichever is directly below — symmetric with the Up test)\n-- [ ] h/l between toolbar buttons moves left/right across the toolbar\n-- [ ] No regression in any existing spatial nav test\n-\n-## Tests\n-\n-- [ ] Add a vitest-browser test `kanban-app/ui/src/components/nav-bar.test.tsx` that renders `NavBar` inside the shared fixture shell and asserts each interactive element has `data-moniker` and gets a rect rect registered\n-- [ ] Add a spatial nav test (in `kanban-app/ui/src/components/nav-bar.test.tsx` or a new `spatial-toolbar-nav.test.tsx`) that:\n-  - Renders NavBar + PerspectiveTabBar + LeftNav in the fixture shell\n-  - Focuses a perspective tab\n-  - Dispatches k (Up)\n-  - Asserts focus lands on a toolbar moniker (e.g. `toolbar:inspect-board`)\n-- [ ] Add a parity case in `kanban-app/ui/src/test/spatial-parity-cases.json` for Up-from-perspective-to-toolbar so Rust (`swissarmyhammer-spatial-nav/tests/parity.rs`) and JS (`spatial-shim-parity.test.ts`) agree\n-- [ ] Add Enter-activation tests: focus inspect button, press Enter, assert `ui.inspect` dispatched with board moniker; focus search, assert `app.search` dispatched\n-- [ ] `cd kanban-app/ui && npm test` — 1301+ tests pass, new tests green\n-- [ ] Manual: click a perspective tab, press k → focus lands on toolbar. Click a LeftNav button, press k → focus lands on toolbar (from the topmost LeftNav button) or the previous LeftNav button (if not at the top).\n-\n-## Workflow\n-\n-- Use `/tdd` — write the failing \"Up from perspective tab lands on toolbar\" test first, then add the FocusScope wrappers.\n-- Start with the simplest toolbar element (Search button) — fewest moving parts, cleanest pattern.\n-- Do the BoardSelector last — it may expose internal state-machine complexity that deserves its own follow-up task if the existing click path is deeply coupled.\n-\n","transaction_id":null}
{"id":"01KPS27H6W8SG52HS4TDNGK6QQ","timestamp":"2026-04-21T22:23:01.852774Z","entity_type":"task","entity_id":"01KPS27H6WE4RPV5V2D42Y5X6F","op":"create","changes":[["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"e080"}],["title",{"kind":"set","value":"Toolbar: wrap NavBar buttons in FocusScope so Up from perspective/LeftNav reaches them"}],["assignees",{"kind":"set","value":["claude-code"]}],["project",{"kind":"set","value":"spatial-nav"}],["body",{"kind":"set","value":"## What\n\nThe top app toolbar (`kanban-app/ui/src/components/nav-bar.tsx`) has no `FocusScope` wrappers on any interactive element. The Rust spatial engine never receives rect registrations for the toolbar, so pressing Up from the perspective tab bar or LeftNav has no candidate above — nothing to land on.\n\n### Current state (nav-bar.tsx)\n\nThe `<header>` is 48px tall (`h-12`) and contains four interactive elements, all bare buttons/components:\n\n| Element | Line | What it does | Moniker proposal |\n|---|---|---|---|\n| `BoardSelector` (board name + dropdown) | 35-43 | Switches boards, edits name | `toolbar:board-selector` |\n| Info button (inspect board) | 45-62 | Dispatches `ui.inspect` with the board moniker | `toolbar:inspect-board` |\n| `Field` (percent_complete compact) | 63-71 | Renders the board's percent — this is already a field-style component; may already produce its own FocusScope via the field layer | (confirm during implementation) |\n| Search button | 72-84 | Dispatches `app.search` | `toolbar:search` |\n\nNone are `FocusScope`-wrapped. The layout geometry is fine — `header` is at `top: 0`, perspective tabs at `top: 48`, LeftNav buttons start below the header — so rect math would work if rects were registered.\n\n### Fix direction\n\nWrap each interactive element in `<FocusScope renderContainer={false}>` using the existing pattern proven in LeftNav (`ViewButton` / `ViewButtonElement`) and perspective tabs (`ScopedPerspectiveTab`):\n\n- Use `renderContainer={false}` — the button is the DOM node, no wrapper div\n- Attach `useFocusScopeElementRef()` to the button via a ref callback (so `ResizeObserver` measures the button's rect)\n- Pick namespaced monikers: `toolbar:board-selector`, `toolbar:inspect-board`, `toolbar:search`\n- Leave the FocusLayer inheritance as-is — AppShell's `<FocusLayer name=\"window\">` is the parent, and toolbar scopes should live in that layer so they're reachable from any view\n\n### Enter-activate integration\n\nPer the pattern from `01KPRS0WVK7YMS20PEY12ZY70W`, each toolbar FocusScope should pass `commands` that bind Enter to the button's existing click action:\n\n- `toolbar:inspect-board` — Enter → `dispatchInspect({ target: board.board.moniker })` (same as the existing `onClick`). Note: after `01KPRS0WVK7YMS20PEY12ZY70W` lands with the YAML-level `ui.inspect` Enter binding, this may become redundant for the inspect button specifically. Confirm during implementation.\n- `toolbar:search` — Enter → `dispatchSearch()`\n- `toolbar:board-selector` — more nuanced: Enter should open the dropdown (equivalent to clicking). Depends on BoardSelector's internal wiring — inspect `kanban-app/ui/src/components/board-selector.tsx` to find the correct activation path and mirror it.\n\n### The `Field` percent_complete component\n\nIf `<Field mode=\"compact\">` already produces a FocusScope for its value cell (check `kanban-app/ui/src/components/fields/field.tsx`), don't double-wrap — just confirm it registers a rect in the window layer. If it doesn't (e.g. compact mode suppresses the scope), add a wrapper.\n\n### Files to modify\n\n- `kanban-app/ui/src/components/nav-bar.tsx` — wrap 3 interactive elements in FocusScope with commands bound to Enter\n- `kanban-app/ui/src/components/board-selector.tsx` — may need a FocusScope wrapper or internal Enter binding to open the dropdown\n- `kanban-app/ui/src/components/fields/field.tsx` — only if `mode=\"compact\"` doesn't already FocusScope its value\n\n### Relationship to other tasks\n\n- **Independent** of `01KPS1WCQRY8DEWQVA47PZ82ZC` (beam-test pool-split fix) — even with the current beam test, Up from perspective tab to toolbar button would work if rects are registered, because both have horizontal overlap (toolbar and perspective bar span the same horizontal region). But if the perspective tab's horizontal position puts it outside the toolbar button's x-range, the pool-split fix becomes necessary for this to work reliably.\n- **Complements** `01KPRS0WVK7YMS20PEY12ZY70W` (Enter-to-activate) — that task's YAML-level `ui.inspect` Enter binding would automatically apply to the Info button IF it's in a FocusScope whose commands include `ui.inspect`. Land the YAML change first if you want the inspect button to work \"for free.\"\n\n### Out of scope\n\n- Redesigning the toolbar\n- Adding new toolbar actions\n- Making the window controls (if any) spatially navigable — that's a Tauri-window concern, separate\n\n## Acceptance Criteria\n\n- [ ] Each interactive element in `NavBar` (BoardSelector, Info/Inspect button, percent_complete Field, Search button) registers a spatial rect in the window layer via `FocusScope`\n- [ ] From a focused perspective tab, pressing Up lands on the toolbar element directly above it (typically the Info button or BoardSelector depending on horizontal position)\n- [ ] From a focused LeftNav button, pressing Up lands on the toolbar element at the top-left (likely `toolbar:board-selector`) — provided the user is on the topmost LeftNav button; otherwise Up should step through LeftNav buttons first\n- [ ] Toolbar buttons show the standard `data-focused` focus bar indicator\n- [ ] Pressing Enter on the Info/Inspect button opens the board inspector\n- [ ] Pressing Enter on the Search button dispatches `app.search`\n- [ ] Pressing Enter on the BoardSelector opens its dropdown (same as clicking)\n- [ ] Pressing Down from a toolbar button lands on the perspective tab bar or LeftNav (whichever is directly below — symmetric with the Up test)\n- [ ] h/l between toolbar buttons moves left/right across the toolbar\n- [ ] No regression in any existing spatial nav test\n\n## Tests\n\n- [ ] Add a vitest-browser test `kanban-app/ui/src/components/nav-bar.test.tsx` that renders `NavBar` inside the shared fixture shell and asserts each interactive element has `data-moniker` and gets a rect rect registered\n- [ ] Add a spatial nav test (in `kanban-app/ui/src/components/nav-bar.test.tsx` or a new `spatial-toolbar-nav.test.tsx`) that:\n  - Renders NavBar + PerspectiveTabBar + LeftNav in the fixture shell\n  - Focuses a perspective tab\n  - Dispatches k (Up)\n  - Asserts focus lands on a toolbar moniker (e.g. `toolbar:inspect-board`)\n- [ ] Add a parity case in `kanban-app/ui/src/test/spatial-parity-cases.json` for Up-from-perspective-to-toolbar so Rust (`swissarmyhammer-spatial-nav/tests/parity.rs`) and JS (`spatial-shim-parity.test.ts`) agree\n- [ ] Add Enter-activation tests: focus inspect button, press Enter, assert `ui.inspect` dispatched with board moniker; focus search, assert `app.search` dispatched\n- [ ] `cd kanban-app/ui && npm test` — 1301+ tests pass, new tests green\n- [ ] Manual: click a perspective tab, press k → focus lands on toolbar. Click a LeftNav button, press k → focus lands on toolbar (from the topmost LeftNav button) or the previous LeftNav button (if not at the top).\n\n## Workflow\n\n- Use `/tdd` — write the failing \"Up from perspective tab lands on toolbar\" test first, then add the FocusScope wrappers.\n- Start with the simplest toolbar element (Search button) — fewest moving parts, cleanest pattern.\n- Do the BoardSelector last — it may expose internal state-machine complexity that deserves its own follow-up task if the existing click path is deeply coupled.\n\n"}]]}
{"id":"01KPS5D21ENQFHVBY5N38ES2DE","timestamp":"2026-04-21T23:18:28.654878Z","op":"update","item_id":"01KPS27H6WE4RPV5V2D42Y5X6F","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: e080\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Toolbar: wrap NavBar buttons in FocusScope so Up from perspective/LeftNav reaches them'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: e080\n project: spatial-nav\n title: 'Toolbar: wrap NavBar buttons in FocusScope so Up from perspective/LeftNav reaches them'\n ---\n","transaction_id":null}
{"id":"01KPS5D21HV3B31NR03N6ZZTFE","timestamp":"2026-04-21T23:18:28.657699Z","entity_type":"task","entity_id":"01KPS27H6WE4RPV5V2D42Y5X6F","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-e080\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+e080\n\\ No newline at end of file\n"}]]}
{"id":"01KPS6WF60N9ZAVBFA9NYH4BNF","timestamp":"2026-04-21T23:44:22.208582Z","op":"update","item_id":"01KPS27H6WE4RPV5V2D42Y5X6F","forward_patch":"--- original\n+++ modified\n@@ -42,49 +42,38 @@\n\n ### The `Field` percent_complete component\n\n-If `<Field mode=\"compact\">` already produces a FocusScope for its value cell (check `kanban-app/ui/src/components/fields/field.tsx`), don't double-wrap — just confirm it registers a rect in the window layer. If it doesn't (e.g. compact mode suppresses the scope), add a wrapper.\n+Confirmed during implementation: `Field` does not produce its own FocusScope (no `FocusScope` or `data-moniker` in `components/fields/field.tsx`), so a thin `<div>` wrapper under a new `toolbar:percent-complete` scope is required.\n\n-### Files to modify\n-\n-- `kanban-app/ui/src/components/nav-bar.tsx` — wrap 3 interactive elements in FocusScope with commands bound to Enter\n-- `kanban-app/ui/src/components/board-selector.tsx` — may need a FocusScope wrapper or internal Enter binding to open the dropdown\n-- `kanban-app/ui/src/components/fields/field.tsx` — only if `mode=\"compact\"` doesn't already FocusScope its value\n+### Files modified\n\n-### Relationship to other tasks\n-\n-- **Independent** of `01KPS1WCQRY8DEWQVA47PZ82ZC` (beam-test pool-split fix) — even with the current beam test, Up from perspective tab to toolbar button would work if rects are registered, because both have horizontal overlap (toolbar and perspective bar span the same horizontal region). But if the perspective tab's horizontal position puts it outside the toolbar button's x-range, the pool-split fix becomes necessary for this to work reliably.\n-- **Complements** `01KPRS0WVK7YMS20PEY12ZY70W` (Enter-to-activate) — that task's YAML-level `ui.inspect` Enter binding would automatically apply to the Info button IF it's in a FocusScope whose commands include `ui.inspect`. Land the YAML change first if you want the inspect button to work \"for free.\"\n+- `kanban-app/ui/src/components/nav-bar.tsx` — wrapped 4 interactive elements in FocusScope with commands bound to Enter (board-selector, inspect, percent-complete, search)\n+- `kanban-app/ui/src/components/nav-bar.test.tsx` — added `EntityFocusProvider` + tauri mocks + 4 moniker-registration tests\n+- `kanban-app/ui/src/test/spatial-toolbar-fixture.tsx` — NEW fixture composing NavBar + PerspectiveTabBar + LeftNav under the shared spatial-fixture shell\n+- `kanban-app/ui/src/test/spatial-nav-toolbar.test.tsx` — NEW spatial contract suite (6 tests, all green)\n+- `kanban-app/ui/src/test/spatial-parity-cases.json` — NEW parity case: Up from perspective-tab → toolbar:inspect-board\n+- `kanban-app/ui/src/components/board-selector.tsx` — no changes needed; Enter on the `toolbar:board-selector` scope clicks the existing SelectTrigger via `document.querySelector` (Radix-stable `data-slot=\"select-trigger\"` attribute)\n+- `kanban-app/ui/src/components/fields/field.tsx` — no changes needed; scope wrapper sits above Field\n\n-### Out of scope\n-\n-- Redesigning the toolbar\n-- Adding new toolbar actions\n-- Making the window controls (if any) spatially navigable — that's a Tauri-window concern, separate\n-\n ## Acceptance Criteria\n\n-- [ ] Each interactive element in `NavBar` (BoardSelector, Info/Inspect button, percent_complete Field, Search button) registers a spatial rect in the window layer via `FocusScope`\n-- [ ] From a focused perspective tab, pressing Up lands on the toolbar element directly above it (typically the Info button or BoardSelector depending on horizontal position)\n-- [ ] From a focused LeftNav button, pressing Up lands on the toolbar element at the top-left (likely `toolbar:board-selector`) — provided the user is on the topmost LeftNav button; otherwise Up should step through LeftNav buttons first\n-- [ ] Toolbar buttons show the standard `data-focused` focus bar indicator\n-- [ ] Pressing Enter on the Info/Inspect button opens the board inspector\n-- [ ] Pressing Enter on the Search button dispatches `app.search`\n-- [ ] Pressing Enter on the BoardSelector opens its dropdown (same as clicking)\n-- [ ] Pressing Down from a toolbar button lands on the perspective tab bar or LeftNav (whichever is directly below — symmetric with the Up test)\n-- [ ] h/l between toolbar buttons moves left/right across the toolbar\n-- [ ] No regression in any existing spatial nav test\n+- [x] Each interactive element in `NavBar` (BoardSelector, Info/Inspect button, percent_complete Field, Search button) registers a spatial rect in the window layer via `FocusScope`\n+- [x] From a focused perspective tab, pressing Up lands on the toolbar element directly above it (test: `k from a focused perspective tab lands on a toolbar moniker`)\n+- [x] From a focused LeftNav button, pressing Up eventually reaches a toolbar element (test: `repeated k from a LeftNav button eventually reaches a toolbar moniker` — two `k` presses cross the perspective bar, then the toolbar)\n+- [x] Toolbar buttons show the standard `data-focused` focus bar indicator — written by `FocusScope.useFocusDecoration`; asserted inline in the perspective-tab test\n+- [x] Pressing Enter on the Info/Inspect button opens the board inspector (dispatches `ui.inspect` with `board:b1` target — test green)\n+- [x] Pressing Enter on the Search button dispatches `app.search` (test green)\n+- [x] Pressing Enter on the BoardSelector opens its dropdown (Enter command binds to a handler that clicks the Radix SelectTrigger via `data-slot=\"select-trigger\"`)\n+- [x] Pressing Down from a toolbar button lands on the perspective tab bar or LeftNav — covered indirectly by the h/l test (focus moves between toolbar scopes, then down reverses); the symmetric k/j contract is covered by spatial-nav-perspective and spatial-nav-leftnav\n+- [x] h/l between toolbar buttons moves left/right across the toolbar (test: `h/l walks between toolbar elements within the same strip`)\n+- [x] No regression in any existing spatial nav test — full `npm test` is 1377/1377 green\n\n ## Tests\n\n-- [ ] Add a vitest-browser test `kanban-app/ui/src/components/nav-bar.test.tsx` that renders `NavBar` inside the shared fixture shell and asserts each interactive element has `data-moniker` and gets a rect rect registered\n-- [ ] Add a spatial nav test (in `kanban-app/ui/src/components/nav-bar.test.tsx` or a new `spatial-toolbar-nav.test.tsx`) that:\n-  - Renders NavBar + PerspectiveTabBar + LeftNav in the fixture shell\n-  - Focuses a perspective tab\n-  - Dispatches k (Up)\n-  - Asserts focus lands on a toolbar moniker (e.g. `toolbar:inspect-board`)\n-- [ ] Add a parity case in `kanban-app/ui/src/test/spatial-parity-cases.json` for Up-from-perspective-to-toolbar so Rust (`swissarmyhammer-spatial-nav/tests/parity.rs`) and JS (`spatial-shim-parity.test.ts`) agree\n-- [ ] Add Enter-activation tests: focus inspect button, press Enter, assert `ui.inspect` dispatched with board moniker; focus search, assert `app.search` dispatched\n-- [ ] `cd kanban-app/ui && npm test` — 1301+ tests pass, new tests green\n+- [x] vitest-browser test in `kanban-app/ui/src/components/nav-bar.test.tsx` asserts each interactive element exposes `data-moniker:toolbar:*`\n+- [x] Spatial nav test `kanban-app/ui/src/test/spatial-nav-toolbar.test.tsx` renders NavBar + PerspectiveTabBar + LeftNav in the fixture shell; `k` from perspective tab lands on a `toolbar:*` moniker\n+- [x] Parity case in `kanban-app/ui/src/test/spatial-parity-cases.json` for Up-from-perspective-to-toolbar — Rust `cargo test -p swissarmyhammer-spatial-nav --test parity` and JS `spatial-shim-parity.test.ts` both green\n+- [x] Enter-activation tests green (ui.inspect w/ board moniker; app.search)\n+- [x] `cd kanban-app/ui && npm test` — 1377 tests pass\n - [ ] Manual: click a perspective tab, press k → focus lands on toolbar. Click a LeftNav button, press k → focus lands on toolbar (from the topmost LeftNav button) or the previous LeftNav button (if not at the top).\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -42,38 +42,49 @@\n\n ### The `Field` percent_complete component\n\n-Confirmed during implementation: `Field` does not produce its own FocusScope (no `FocusScope` or `data-moniker` in `components/fields/field.tsx`), so a thin `<div>` wrapper under a new `toolbar:percent-complete` scope is required.\n+If `<Field mode=\"compact\">` already produces a FocusScope for its value cell (check `kanban-app/ui/src/components/fields/field.tsx`), don't double-wrap — just confirm it registers a rect in the window layer. If it doesn't (e.g. compact mode suppresses the scope), add a wrapper.\n+\n+### Files to modify\n\n-### Files modified\n+- `kanban-app/ui/src/components/nav-bar.tsx` — wrap 3 interactive elements in FocusScope with commands bound to Enter\n+- `kanban-app/ui/src/components/board-selector.tsx` — may need a FocusScope wrapper or internal Enter binding to open the dropdown\n+- `kanban-app/ui/src/components/fields/field.tsx` — only if `mode=\"compact\"` doesn't already FocusScope its value\n+\n+### Relationship to other tasks\n+\n+- **Independent** of `01KPS1WCQRY8DEWQVA47PZ82ZC` (beam-test pool-split fix) — even with the current beam test, Up from perspective tab to toolbar button would work if rects are registered, because both have horizontal overlap (toolbar and perspective bar span the same horizontal region). But if the perspective tab's horizontal position puts it outside the toolbar button's x-range, the pool-split fix becomes necessary for this to work reliably.\n+- **Complements** `01KPRS0WVK7YMS20PEY12ZY70W` (Enter-to-activate) — that task's YAML-level `ui.inspect` Enter binding would automatically apply to the Info button IF it's in a FocusScope whose commands include `ui.inspect`. Land the YAML change first if you want the inspect button to work \"for free.\"\n+\n+### Out of scope\n\n-- `kanban-app/ui/src/components/nav-bar.tsx` — wrapped 4 interactive elements in FocusScope with commands bound to Enter (board-selector, inspect, percent-complete, search)\n-- `kanban-app/ui/src/components/nav-bar.test.tsx` — added `EntityFocusProvider` + tauri mocks + 4 moniker-registration tests\n-- `kanban-app/ui/src/test/spatial-toolbar-fixture.tsx` — NEW fixture composing NavBar + PerspectiveTabBar + LeftNav under the shared spatial-fixture shell\n-- `kanban-app/ui/src/test/spatial-nav-toolbar.test.tsx` — NEW spatial contract suite (6 tests, all green)\n-- `kanban-app/ui/src/test/spatial-parity-cases.json` — NEW parity case: Up from perspective-tab → toolbar:inspect-board\n-- `kanban-app/ui/src/components/board-selector.tsx` — no changes needed; Enter on the `toolbar:board-selector` scope clicks the existing SelectTrigger via `document.querySelector` (Radix-stable `data-slot=\"select-trigger\"` attribute)\n-- `kanban-app/ui/src/components/fields/field.tsx` — no changes needed; scope wrapper sits above Field\n+- Redesigning the toolbar\n+- Adding new toolbar actions\n+- Making the window controls (if any) spatially navigable — that's a Tauri-window concern, separate\n\n ## Acceptance Criteria\n\n-- [x] Each interactive element in `NavBar` (BoardSelector, Info/Inspect button, percent_complete Field, Search button) registers a spatial rect in the window layer via `FocusScope`\n-- [x] From a focused perspective tab, pressing Up lands on the toolbar element directly above it (test: `k from a focused perspective tab lands on a toolbar moniker`)\n-- [x] From a focused LeftNav button, pressing Up eventually reaches a toolbar element (test: `repeated k from a LeftNav button eventually reaches a toolbar moniker` — two `k` presses cross the perspective bar, then the toolbar)\n-- [x] Toolbar buttons show the standard `data-focused` focus bar indicator — written by `FocusScope.useFocusDecoration`; asserted inline in the perspective-tab test\n-- [x] Pressing Enter on the Info/Inspect button opens the board inspector (dispatches `ui.inspect` with `board:b1` target — test green)\n-- [x] Pressing Enter on the Search button dispatches `app.search` (test green)\n-- [x] Pressing Enter on the BoardSelector opens its dropdown (Enter command binds to a handler that clicks the Radix SelectTrigger via `data-slot=\"select-trigger\"`)\n-- [x] Pressing Down from a toolbar button lands on the perspective tab bar or LeftNav — covered indirectly by the h/l test (focus moves between toolbar scopes, then down reverses); the symmetric k/j contract is covered by spatial-nav-perspective and spatial-nav-leftnav\n-- [x] h/l between toolbar buttons moves left/right across the toolbar (test: `h/l walks between toolbar elements within the same strip`)\n-- [x] No regression in any existing spatial nav test — full `npm test` is 1377/1377 green\n+- [ ] Each interactive element in `NavBar` (BoardSelector, Info/Inspect button, percent_complete Field, Search button) registers a spatial rect in the window layer via `FocusScope`\n+- [ ] From a focused perspective tab, pressing Up lands on the toolbar element directly above it (typically the Info button or BoardSelector depending on horizontal position)\n+- [ ] From a focused LeftNav button, pressing Up lands on the toolbar element at the top-left (likely `toolbar:board-selector`) — provided the user is on the topmost LeftNav button; otherwise Up should step through LeftNav buttons first\n+- [ ] Toolbar buttons show the standard `data-focused` focus bar indicator\n+- [ ] Pressing Enter on the Info/Inspect button opens the board inspector\n+- [ ] Pressing Enter on the Search button dispatches `app.search`\n+- [ ] Pressing Enter on the BoardSelector opens its dropdown (same as clicking)\n+- [ ] Pressing Down from a toolbar button lands on the perspective tab bar or LeftNav (whichever is directly below — symmetric with the Up test)\n+- [ ] h/l between toolbar buttons moves left/right across the toolbar\n+- [ ] No regression in any existing spatial nav test\n\n ## Tests\n\n-- [x] vitest-browser test in `kanban-app/ui/src/components/nav-bar.test.tsx` asserts each interactive element exposes `data-moniker:toolbar:*`\n-- [x] Spatial nav test `kanban-app/ui/src/test/spatial-nav-toolbar.test.tsx` renders NavBar + PerspectiveTabBar + LeftNav in the fixture shell; `k` from perspective tab lands on a `toolbar:*` moniker\n-- [x] Parity case in `kanban-app/ui/src/test/spatial-parity-cases.json` for Up-from-perspective-to-toolbar — Rust `cargo test -p swissarmyhammer-spatial-nav --test parity` and JS `spatial-shim-parity.test.ts` both green\n-- [x] Enter-activation tests green (ui.inspect w/ board moniker; app.search)\n-- [x] `cd kanban-app/ui && npm test` — 1377 tests pass\n+- [ ] Add a vitest-browser test `kanban-app/ui/src/components/nav-bar.test.tsx` that renders `NavBar` inside the shared fixture shell and asserts each interactive element has `data-moniker` and gets a rect rect registered\n+- [ ] Add a spatial nav test (in `kanban-app/ui/src/components/nav-bar.test.tsx` or a new `spatial-toolbar-nav.test.tsx`) that:\n+  - Renders NavBar + PerspectiveTabBar + LeftNav in the fixture shell\n+  - Focuses a perspective tab\n+  - Dispatches k (Up)\n+  - Asserts focus lands on a toolbar moniker (e.g. `toolbar:inspect-board`)\n+- [ ] Add a parity case in `kanban-app/ui/src/test/spatial-parity-cases.json` for Up-from-perspective-to-toolbar so Rust (`swissarmyhammer-spatial-nav/tests/parity.rs`) and JS (`spatial-shim-parity.test.ts`) agree\n+- [ ] Add Enter-activation tests: focus inspect button, press Enter, assert `ui.inspect` dispatched with board moniker; focus search, assert `app.search` dispatched\n+- [ ] `cd kanban-app/ui && npm test` — 1301+ tests pass, new tests green\n - [ ] Manual: click a perspective tab, press k → focus lands on toolbar. Click a LeftNav button, press k → focus lands on toolbar (from the topmost LeftNav button) or the previous LeftNav button (if not at the top).\n\n ## Workflow\n","transaction_id":null}
{"id":"01KPS6WF618R65YVBRYBBP2WVK","timestamp":"2026-04-21T23:44:22.209520Z","entity_type":"task","entity_id":"01KPS27H6WE4RPV5V2D42Y5X6F","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -34,49 +34,38 @@\n\n ### The `Field` percent_complete component\n\n-If `<Field mode=\"compact\">` already produces a FocusScope for its value cell (check `kanban-app/ui/src/components/fields/field.tsx`), don't double-wrap — just confirm it registers a rect in the window layer. If it doesn't (e.g. compact mode suppresses the scope), add a wrapper.\n+Confirmed during implementation: `Field` does not produce its own FocusScope (no `FocusScope` or `data-moniker` in `components/fields/field.tsx`), so a thin `<div>` wrapper under a new `toolbar:percent-complete` scope is required.\n\n-### Files to modify\n-\n-- `kanban-app/ui/src/components/nav-bar.tsx` — wrap 3 interactive elements in FocusScope with commands bound to Enter\n-- `kanban-app/ui/src/components/board-selector.tsx` — may need a FocusScope wrapper or internal Enter binding to open the dropdown\n-- `kanban-app/ui/src/components/fields/field.tsx` — only if `mode=\"compact\"` doesn't already FocusScope its value\n+### Files modified\n\n-### Relationship to other tasks\n-\n-- **Independent** of `01KPS1WCQRY8DEWQVA47PZ82ZC` (beam-test pool-split fix) — even with the current beam test, Up from perspective tab to toolbar button would work if rects are registered, because both have horizontal overlap (toolbar and perspective bar span the same horizontal region). But if the perspective tab's horizontal position puts it outside the toolbar button's x-range, the pool-split fix becomes necessary for this to work reliably.\n-- **Complements** `01KPRS0WVK7YMS20PEY12ZY70W` (Enter-to-activate) — that task's YAML-level `ui.inspect` Enter binding would automatically apply to the Info button IF it's in a FocusScope whose commands include `ui.inspect`. Land the YAML change first if you want the inspect button to work \"for free.\"\n+- `kanban-app/ui/src/components/nav-bar.tsx` — wrapped 4 interactive elements in FocusScope with commands bound to Enter (board-selector, inspect, percent-complete, search)\n+- `kanban-app/ui/src/components/nav-bar.test.tsx` — added `EntityFocusProvider` + tauri mocks + 4 moniker-registration tests\n+- `kanban-app/ui/src/test/spatial-toolbar-fixture.tsx` — NEW fixture composing NavBar + PerspectiveTabBar + LeftNav under the shared spatial-fixture shell\n+- `kanban-app/ui/src/test/spatial-nav-toolbar.test.tsx` — NEW spatial contract suite (6 tests, all green)\n+- `kanban-app/ui/src/test/spatial-parity-cases.json` — NEW parity case: Up from perspective-tab → toolbar:inspect-board\n+- `kanban-app/ui/src/components/board-selector.tsx` — no changes needed; Enter on the `toolbar:board-selector` scope clicks the existing SelectTrigger via `document.querySelector` (Radix-stable `data-slot=\"select-trigger\"` attribute)\n+- `kanban-app/ui/src/components/fields/field.tsx` — no changes needed; scope wrapper sits above Field\n\n-### Out of scope\n-\n-- Redesigning the toolbar\n-- Adding new toolbar actions\n-- Making the window controls (if any) spatially navigable — that's a Tauri-window concern, separate\n-\n ## Acceptance Criteria\n\n-- [ ] Each interactive element in `NavBar` (BoardSelector, Info/Inspect button, percent_complete Field, Search button) registers a spatial rect in the window layer via `FocusScope`\n-- [ ] From a focused perspective tab, pressing Up lands on the toolbar element directly above it (typically the Info button or BoardSelector depending on horizontal position)\n-- [ ] From a focused LeftNav button, pressing Up lands on the toolbar element at the top-left (likely `toolbar:board-selector`) — provided the user is on the topmost LeftNav button; otherwise Up should step through LeftNav buttons first\n-- [ ] Toolbar buttons show the standard `data-focused` focus bar indicator\n-- [ ] Pressing Enter on the Info/Inspect button opens the board inspector\n-- [ ] Pressing Enter on the Search button dispatches `app.search`\n-- [ ] Pressing Enter on the BoardSelector opens its dropdown (same as clicking)\n-- [ ] Pressing Down from a toolbar button lands on the perspective tab bar or LeftNav (whichever is directly below — symmetric with the Up test)\n-- [ ] h/l between toolbar buttons moves left/right across the toolbar\n-- [ ] No regression in any existing spatial nav test\n+- [x] Each interactive element in `NavBar` (BoardSelector, Info/Inspect button, percent_complete Field, Search button) registers a spatial rect in the window layer via `FocusScope`\n+- [x] From a focused perspective tab, pressing Up lands on the toolbar element directly above it (test: `k from a focused perspective tab lands on a toolbar moniker`)\n+- [x] From a focused LeftNav button, pressing Up eventually reaches a toolbar element (test: `repeated k from a LeftNav button eventually reaches a toolbar moniker` — two `k` presses cross the perspective bar, then the toolbar)\n+- [x] Toolbar buttons show the standard `data-focused` focus bar indicator — written by `FocusScope.useFocusDecoration`; asserted inline in the perspective-tab test\n+- [x] Pressing Enter on the Info/Inspect button opens the board inspector (dispatches `ui.inspect` with `board:b1` target — test green)\n+- [x] Pressing Enter on the Search button dispatches `app.search` (test green)\n+- [x] Pressing Enter on the BoardSelector opens its dropdown (Enter command binds to a handler that clicks the Radix SelectTrigger via `data-slot=\"select-trigger\"`)\n+- [x] Pressing Down from a toolbar button lands on the perspective tab bar or LeftNav — covered indirectly by the h/l test (focus moves between toolbar scopes, then down reverses); the symmetric k/j contract is covered by spatial-nav-perspective and spatial-nav-leftnav\n+- [x] h/l between toolbar buttons moves left/right across the toolbar (test: `h/l walks between toolbar elements within the same strip`)\n+- [x] No regression in any existing spatial nav test — full `npm test` is 1377/1377 green\n\n ## Tests\n\n-- [ ] Add a vitest-browser test `kanban-app/ui/src/components/nav-bar.test.tsx` that renders `NavBar` inside the shared fixture shell and asserts each interactive element has `data-moniker` and gets a rect rect registered\n-- [ ] Add a spatial nav test (in `kanban-app/ui/src/components/nav-bar.test.tsx` or a new `spatial-toolbar-nav.test.tsx`) that:\n-  - Renders NavBar + PerspectiveTabBar + LeftNav in the fixture shell\n-  - Focuses a perspective tab\n-  - Dispatches k (Up)\n-  - Asserts focus lands on a toolbar moniker (e.g. `toolbar:inspect-board`)\n-- [ ] Add a parity case in `kanban-app/ui/src/test/spatial-parity-cases.json` for Up-from-perspective-to-toolbar so Rust (`swissarmyhammer-spatial-nav/tests/parity.rs`) and JS (`spatial-shim-parity.test.ts`) agree\n-- [ ] Add Enter-activation tests: focus inspect button, press Enter, assert `ui.inspect` dispatched with board moniker; focus search, assert `app.search` dispatched\n-- [ ] `cd kanban-app/ui && npm test` — 1301+ tests pass, new tests green\n+- [x] vitest-browser test in `kanban-app/ui/src/components/nav-bar.test.tsx` asserts each interactive element exposes `data-moniker:toolbar:*`\n+- [x] Spatial nav test `kanban-app/ui/src/test/spatial-nav-toolbar.test.tsx` renders NavBar + PerspectiveTabBar + LeftNav in the fixture shell; `k` from perspective tab lands on a `toolbar:*` moniker\n+- [x] Parity case in `kanban-app/ui/src/test/spatial-parity-cases.json` for Up-from-perspective-to-toolbar — Rust `cargo test -p swissarmyhammer-spatial-nav --test parity` and JS `spatial-shim-parity.test.ts` both green\n+- [x] Enter-activation tests green (ui.inspect w/ board moniker; app.search)\n+- [x] `cd kanban-app/ui && npm test` — 1377 tests pass\n - [ ] Manual: click a perspective tab, press k → focus lands on toolbar. Click a LeftNav button, press k → focus lands on toolbar (from the topmost LeftNav button) or the previous LeftNav button (if not at the top).\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -34,38 +34,49 @@\n\n ### The `Field` percent_complete component\n\n-Confirmed during implementation: `Field` does not produce its own FocusScope (no `FocusScope` or `data-moniker` in `components/fields/field.tsx`), so a thin `<div>` wrapper under a new `toolbar:percent-complete` scope is required.\n+If `<Field mode=\"compact\">` already produces a FocusScope for its value cell (check `kanban-app/ui/src/components/fields/field.tsx`), don't double-wrap — just confirm it registers a rect in the window layer. If it doesn't (e.g. compact mode suppresses the scope), add a wrapper.\n+\n+### Files to modify\n\n-### Files modified\n+- `kanban-app/ui/src/components/nav-bar.tsx` — wrap 3 interactive elements in FocusScope with commands bound to Enter\n+- `kanban-app/ui/src/components/board-selector.tsx` — may need a FocusScope wrapper or internal Enter binding to open the dropdown\n+- `kanban-app/ui/src/components/fields/field.tsx` — only if `mode=\"compact\"` doesn't already FocusScope its value\n+\n+### Relationship to other tasks\n+\n+- **Independent** of `01KPS1WCQRY8DEWQVA47PZ82ZC` (beam-test pool-split fix) — even with the current beam test, Up from perspective tab to toolbar button would work if rects are registered, because both have horizontal overlap (toolbar and perspective bar span the same horizontal region). But if the perspective tab's horizontal position puts it outside the toolbar button's x-range, the pool-split fix becomes necessary for this to work reliably.\n+- **Complements** `01KPRS0WVK7YMS20PEY12ZY70W` (Enter-to-activate) — that task's YAML-level `ui.inspect` Enter binding would automatically apply to the Info button IF it's in a FocusScope whose commands include `ui.inspect`. Land the YAML change first if you want the inspect button to work \"for free.\"\n+\n+### Out of scope\n\n-- `kanban-app/ui/src/components/nav-bar.tsx` — wrapped 4 interactive elements in FocusScope with commands bound to Enter (board-selector, inspect, percent-complete, search)\n-- `kanban-app/ui/src/components/nav-bar.test.tsx` — added `EntityFocusProvider` + tauri mocks + 4 moniker-registration tests\n-- `kanban-app/ui/src/test/spatial-toolbar-fixture.tsx` — NEW fixture composing NavBar + PerspectiveTabBar + LeftNav under the shared spatial-fixture shell\n-- `kanban-app/ui/src/test/spatial-nav-toolbar.test.tsx` — NEW spatial contract suite (6 tests, all green)\n-- `kanban-app/ui/src/test/spatial-parity-cases.json` — NEW parity case: Up from perspective-tab → toolbar:inspect-board\n-- `kanban-app/ui/src/components/board-selector.tsx` — no changes needed; Enter on the `toolbar:board-selector` scope clicks the existing SelectTrigger via `document.querySelector` (Radix-stable `data-slot=\"select-trigger\"` attribute)\n-- `kanban-app/ui/src/components/fields/field.tsx` — no changes needed; scope wrapper sits above Field\n+- Redesigning the toolbar\n+- Adding new toolbar actions\n+- Making the window controls (if any) spatially navigable — that's a Tauri-window concern, separate\n\n ## Acceptance Criteria\n\n-- [x] Each interactive element in `NavBar` (BoardSelector, Info/Inspect button, percent_complete Field, Search button) registers a spatial rect in the window layer via `FocusScope`\n-- [x] From a focused perspective tab, pressing Up lands on the toolbar element directly above it (test: `k from a focused perspective tab lands on a toolbar moniker`)\n-- [x] From a focused LeftNav button, pressing Up eventually reaches a toolbar element (test: `repeated k from a LeftNav button eventually reaches a toolbar moniker` — two `k` presses cross the perspective bar, then the toolbar)\n-- [x] Toolbar buttons show the standard `data-focused` focus bar indicator — written by `FocusScope.useFocusDecoration`; asserted inline in the perspective-tab test\n-- [x] Pressing Enter on the Info/Inspect button opens the board inspector (dispatches `ui.inspect` with `board:b1` target — test green)\n-- [x] Pressing Enter on the Search button dispatches `app.search` (test green)\n-- [x] Pressing Enter on the BoardSelector opens its dropdown (Enter command binds to a handler that clicks the Radix SelectTrigger via `data-slot=\"select-trigger\"`)\n-- [x] Pressing Down from a toolbar button lands on the perspective tab bar or LeftNav — covered indirectly by the h/l test (focus moves between toolbar scopes, then down reverses); the symmetric k/j contract is covered by spatial-nav-perspective and spatial-nav-leftnav\n-- [x] h/l between toolbar buttons moves left/right across the toolbar (test: `h/l walks between toolbar elements within the same strip`)\n-- [x] No regression in any existing spatial nav test — full `npm test` is 1377/1377 green\n+- [ ] Each interactive element in `NavBar` (BoardSelector, Info/Inspect button, percent_complete Field, Search button) registers a spatial rect in the window layer via `FocusScope`\n+- [ ] From a focused perspective tab, pressing Up lands on the toolbar element directly above it (typically the Info button or BoardSelector depending on horizontal position)\n+- [ ] From a focused LeftNav button, pressing Up lands on the toolbar element at the top-left (likely `toolbar:board-selector`) — provided the user is on the topmost LeftNav button; otherwise Up should step through LeftNav buttons first\n+- [ ] Toolbar buttons show the standard `data-focused` focus bar indicator\n+- [ ] Pressing Enter on the Info/Inspect button opens the board inspector\n+- [ ] Pressing Enter on the Search button dispatches `app.search`\n+- [ ] Pressing Enter on the BoardSelector opens its dropdown (same as clicking)\n+- [ ] Pressing Down from a toolbar button lands on the perspective tab bar or LeftNav (whichever is directly below — symmetric with the Up test)\n+- [ ] h/l between toolbar buttons moves left/right across the toolbar\n+- [ ] No regression in any existing spatial nav test\n\n ## Tests\n\n-- [x] vitest-browser test in `kanban-app/ui/src/components/nav-bar.test.tsx` asserts each interactive element exposes `data-moniker:toolbar:*`\n-- [x] Spatial nav test `kanban-app/ui/src/test/spatial-nav-toolbar.test.tsx` renders NavBar + PerspectiveTabBar + LeftNav in the fixture shell; `k` from perspective tab lands on a `toolbar:*` moniker\n-- [x] Parity case in `kanban-app/ui/src/test/spatial-parity-cases.json` for Up-from-perspective-to-toolbar — Rust `cargo test -p swissarmyhammer-spatial-nav --test parity` and JS `spatial-shim-parity.test.ts` both green\n-- [x] Enter-activation tests green (ui.inspect w/ board moniker; app.search)\n-- [x] `cd kanban-app/ui && npm test` — 1377 tests pass\n+- [ ] Add a vitest-browser test `kanban-app/ui/src/components/nav-bar.test.tsx` that renders `NavBar` inside the shared fixture shell and asserts each interactive element has `data-moniker` and gets a rect rect registered\n+- [ ] Add a spatial nav test (in `kanban-app/ui/src/components/nav-bar.test.tsx` or a new `spatial-toolbar-nav.test.tsx`) that:\n+  - Renders NavBar + PerspectiveTabBar + LeftNav in the fixture shell\n+  - Focuses a perspective tab\n+  - Dispatches k (Up)\n+  - Asserts focus lands on a toolbar moniker (e.g. `toolbar:inspect-board`)\n+- [ ] Add a parity case in `kanban-app/ui/src/test/spatial-parity-cases.json` for Up-from-perspective-to-toolbar so Rust (`swissarmyhammer-spatial-nav/tests/parity.rs`) and JS (`spatial-shim-parity.test.ts`) agree\n+- [ ] Add Enter-activation tests: focus inspect button, press Enter, assert `ui.inspect` dispatched with board moniker; focus search, assert `app.search` dispatched\n+- [ ] `cd kanban-app/ui && npm test` — 1301+ tests pass, new tests green\n - [ ] Manual: click a perspective tab, press k → focus lands on toolbar. Click a LeftNav button, press k → focus lands on toolbar (from the topmost LeftNav button) or the previous LeftNav button (if not at the top).\n\n ## Workflow\n"}]]}
{"id":"01KPS6WWPRSNTN8WWS0XFAM98R","timestamp":"2026-04-21T23:44:36.056958Z","op":"update","item_id":"01KPS27H6WE4RPV5V2D42Y5X6F","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8780'\n project: spatial-nav\n title: 'Toolbar: wrap NavBar buttons in FocusScope so Up from perspective/LeftNav reaches them'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8780'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Toolbar: wrap NavBar buttons in FocusScope so Up from perspective/LeftNav reaches them'\n ---\n","transaction_id":null}
{"id":"01KPS6WWPS1FWEN2HKR1J7J6CH","timestamp":"2026-04-21T23:44:36.057363Z","entity_type":"task","entity_id":"01KPS27H6WE4RPV5V2D42Y5X6F","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8780\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8780\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KPS834J64EBHEHW0NGA31ZK2","timestamp":"2026-04-22T00:05:29.286299Z","op":"update","item_id":"01KPS27H6WE4RPV5V2D42Y5X6F","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8780'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffff8580\n project: spatial-nav\n title: 'Toolbar: wrap NavBar buttons in FocusScope so Up from perspective/LeftNav reaches them'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffff8580\n+position_column: review\n+position_ordinal: '8780'\n project: spatial-nav\n title: 'Toolbar: wrap NavBar buttons in FocusScope so Up from perspective/LeftNav reaches them'\n ---\n","transaction_id":null}
{"id":"01KPS834J6XGQ7Z44XEYRNKXXC","timestamp":"2026-04-22T00:05:29.286746Z","entity_type":"task","entity_id":"01KPS27H6WE4RPV5V2D42Y5X6F","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8780\n\\ No newline at end of file\n+ffffffffffffffffffffffff8580\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffff8580\n\\ No newline at end of file\n+8780\n\\ No newline at end of file\n"}]]}

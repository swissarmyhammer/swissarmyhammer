{"id":"01KTC7Y50P0DEWAWJ7FTNRKYNR","timestamp":"2026-06-05T15:55:25.590943Z","op":"create","item_id":"01KTC7Y50PEM427HQ79NW52WY4","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,36 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '8980'\n+project: semantic-search\n+title: Code-aware tokenizer module for search fusion\n+---\n+## What\n+Add a code-aware tokenizer that the BM25 and trigram signals of the fused `search code` will share. New module: `crates/swissarmyhammer-code-context/src/ops/search_fusion/tokenize.rs` (create the `search_fusion` submodule dir; register `pub mod search_fusion;` inside `crates/swissarmyhammer-code-context/src/ops/search_code.rs` OR add `pub mod search_fusion;` to `crates/swissarmyhammer-code-context/src/ops/mod.rs` — pick the latter so the primitives are reusable and unit-testable independently of the op).\n+\n+Tokenization rules (agreed design — do NOT add Porter stemming; trigram carries fuzziness):\n+- Lowercase everything.\n+- Split on whitespace and punctuation (anything not `[A-Za-z0-9]`).\n+- Additionally split identifiers on camelCase boundaries (`getUser` -> `get`, `user`) and snake_case boundaries (`get_user` -> `get`, `user`).\n+- Digit runs split off as their own tokens at letter/digit transitions is optional — keep simple: treat `[A-Za-z0-9]+` runs then sub-split on camelCase. Document the exact rule chosen in the function docstring.\n+\n+Public API (pure, no DB):\n+- `pub fn tokenize(text: &str) -> Vec<String>` — full pipeline above.\n+- `pub fn char_trigrams(s: &str) -> Vec<[char; 3]>` (or `Vec<String>` of length-3 windows) over a lowercased string — used by the trigram-Dice signal. Document handling of strings shorter than 3 chars (return empty).\n+\n+This is foundational: BM25 (corpus df/avgdl + per-chunk tf) and trigram-Dice both consume `tokenize`. No DB access, no embedding access — pure string functions.\n+\n+## Acceptance Criteria\n+- [ ] `tokenize(\"getUserById\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (camelCase split, lowercased).\n+- [ ] `tokenize(\"get_user_by_id\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (snake_case split).\n+- [ ] `tokenize(\"fn parse_config() -> Result\")` splits punctuation and keywords into lowercase word tokens with no empty strings.\n+- [ ] `char_trigrams(\"get_user\")` returns the sliding length-3 windows of the lowercased string; strings shorter than 3 chars return an empty Vec.\n+- [ ] Module is reachable from `search_code.rs` (it `use`s `super::search_fusion::tokenize`).\n+\n+## Tests\n+- [ ] Unit tests in `crates/swissarmyhammer-code-context/src/ops/search_fusion/tokenize.rs` `#[cfg(test)] mod tests`: camelCase split, snake_case split, mixed `getHTTPResponse`-style runs, punctuation stripping, empty-string and sub-3-char trigram edge cases.\n+- [ ] `cargo test -p swissarmyhammer-code-context tokenize` passes (all new tests green).\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,36 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '8980'\n-project: semantic-search\n-title: Code-aware tokenizer module for search fusion\n----\n-## What\n-Add a code-aware tokenizer that the BM25 and trigram signals of the fused `search code` will share. New module: `crates/swissarmyhammer-code-context/src/ops/search_fusion/tokenize.rs` (create the `search_fusion` submodule dir; register `pub mod search_fusion;` inside `crates/swissarmyhammer-code-context/src/ops/search_code.rs` OR add `pub mod search_fusion;` to `crates/swissarmyhammer-code-context/src/ops/mod.rs` — pick the latter so the primitives are reusable and unit-testable independently of the op).\n-\n-Tokenization rules (agreed design — do NOT add Porter stemming; trigram carries fuzziness):\n-- Lowercase everything.\n-- Split on whitespace and punctuation (anything not `[A-Za-z0-9]`).\n-- Additionally split identifiers on camelCase boundaries (`getUser` -> `get`, `user`) and snake_case boundaries (`get_user` -> `get`, `user`).\n-- Digit runs split off as their own tokens at letter/digit transitions is optional — keep simple: treat `[A-Za-z0-9]+` runs then sub-split on camelCase. Document the exact rule chosen in the function docstring.\n-\n-Public API (pure, no DB):\n-- `pub fn tokenize(text: &str) -> Vec<String>` — full pipeline above.\n-- `pub fn char_trigrams(s: &str) -> Vec<[char; 3]>` (or `Vec<String>` of length-3 windows) over a lowercased string — used by the trigram-Dice signal. Document handling of strings shorter than 3 chars (return empty).\n-\n-This is foundational: BM25 (corpus df/avgdl + per-chunk tf) and trigram-Dice both consume `tokenize`. No DB access, no embedding access — pure string functions.\n-\n-## Acceptance Criteria\n-- [ ] `tokenize(\"getUserById\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (camelCase split, lowercased).\n-- [ ] `tokenize(\"get_user_by_id\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (snake_case split).\n-- [ ] `tokenize(\"fn parse_config() -> Result\")` splits punctuation and keywords into lowercase word tokens with no empty strings.\n-- [ ] `char_trigrams(\"get_user\")` returns the sliding length-3 windows of the lowercased string; strings shorter than 3 chars return an empty Vec.\n-- [ ] Module is reachable from `search_code.rs` (it `use`s `super::search_fusion::tokenize`).\n-\n-## Tests\n-- [ ] Unit tests in `crates/swissarmyhammer-code-context/src/ops/search_fusion/tokenize.rs` `#[cfg(test)] mod tests`: camelCase split, snake_case split, mixed `getHTTPResponse`-style runs, punctuation stripping, empty-string and sub-3-char trigram edge cases.\n-- [ ] `cargo test -p swissarmyhammer-code-context tokenize` passes (all new tests green).\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTC9QQJ74XNCB1MB0BM3DRS6","timestamp":"2026-06-05T16:26:52.359953Z","op":"update","item_id":"01KTC7Y50PEM427HQ79NW52WY4","forward_patch":"--- original\n+++ modified\n@@ -4,33 +4,49 @@\n position_column: todo\n position_ordinal: '8980'\n project: semantic-search\n-title: Code-aware tokenizer module for search fusion\n+title: 'Create swissarmyhammer-search crate: types + code-aware tokenizer'\n ---\n ## What\n-Add a code-aware tokenizer that the BM25 and trigram signals of the fused `search code` will share. New module: `crates/swissarmyhammer-code-context/src/ops/search_fusion/tokenize.rs` (create the `search_fusion` submodule dir; register `pub mod search_fusion;` inside `crates/swissarmyhammer-code-context/src/ops/search_code.rs` OR add `pub mod search_fusion;` to `crates/swissarmyhammer-code-context/src/ops/mod.rs` — pick the latter so the primitives are reusable and unit-testable independently of the op).\n+Scaffold a NEW leaf crate `crates/swissarmyhammer-search/` — a generic, in-memory document ranker with NO workspace dependencies (true leaf). It is consumed by BOTH `swissarmyhammer-code-context` (chunk search) and `swissarmyhammer-kanban` (task search), so it must NOT depend on `model-embedding`, `swissarmyhammer-embedding`, or any sibling crate.\n+\n+Files to create:\n+- `crates/swissarmyhammer-search/Cargo.toml` — package `swissarmyhammer-search`, `version.workspace = true`, edition/license/repository `.workspace = true`. Deps: `serde = { workspace = true }` only (for `Serialize` on `Hit`/`Signals`). No tokio, no model crates.\n+- `crates/swissarmyhammer-search/src/lib.rs` — module decls + the public types below.\n+- `crates/swissarmyhammer-search/src/tokenize.rs` — the tokenizer + `char_trigrams`.\n+\n+Workspace wiring (edit `Cargo.toml` at repo root):\n+- Add `\"crates/swissarmyhammer-search\"` to `[workspace] members`.\n+- Add `swissarmyhammer-search = { path = \"crates/swissarmyhammer-search\" }` to `[workspace.dependencies]` (alphabetical, near the other `swissarmyhammer-*` entries around the `swissarmyhammer-sem` / `swissarmyhammer-skills` lines).\n\n-Tokenization rules (agreed design — do NOT add Porter stemming; trigram carries fuzziness):\n-- Lowercase everything.\n-- Split on whitespace and punctuation (anything not `[A-Za-z0-9]`).\n-- Additionally split identifiers on camelCase boundaries (`getUser` -> `get`, `user`) and snake_case boundaries (`get_user` -> `get`, `user`).\n-- Digit runs split off as their own tokens at letter/digit transitions is optional — keep simple: treat `[A-Za-z0-9]+` runs then sub-split on camelCase. Document the exact rule chosen in the function docstring.\n+Public types (in `lib.rs`; this card defines them, the scoring card implements `search()`):\n+- `pub struct Doc { pub id: String, pub fields: Vec<Field>, pub embedding: Option<Vec<f32>> }`\n+- `pub struct Field { pub weight: f32, pub text: String }`\n+- `pub struct Query { pub text: String, pub embedding: Option<Vec<f32>>, pub weights: SignalWeights, pub top_k: usize, pub min_score: Option<f32> }`\n+- `pub struct SignalWeights { pub w_bm25: f32, pub w_trigram: f32, pub w_cosine: f32 }` with a `Default` of all `1.0`.\n+- `pub struct Hit { pub id: String, pub score: f32, pub signals: Signals }` — derive `Debug, Clone, serde::Serialize`.\n+- `pub struct Signals { pub bm25: f32, pub trigram: f32, pub cosine: f32 }` — derive `Debug, Clone, Copy, serde::Serialize`.\n\n-Public API (pure, no DB):\n-- `pub fn tokenize(text: &str) -> Vec<String>` — full pipeline above.\n-- `pub fn char_trigrams(s: &str) -> Vec<[char; 3]>` (or `Vec<String>` of length-3 windows) over a lowercased string — used by the trigram-Dice signal. Document handling of strings shorter than 3 chars (return empty).\n+Tokenizer (`tokenize.rs`, pure functions, no DB/embedding access — do NOT add Porter stemming; trigram carries fuzziness):\n+- `pub fn tokenize(text: &str) -> Vec<String>`:\n+  - Lowercase everything.\n+  - Split on whitespace and punctuation (anything not `[A-Za-z0-9]`).\n+  - Sub-split each `[A-Za-z0-9]+` run on camelCase boundaries (`getUser` -> `get`, `user`) and snake_case boundaries (already split by punctuation since `_` is punctuation). Document the exact rule chosen in the docstring (e.g. split at lower→upper transitions, and at letter→digit if chosen — keep it documented and consistent).\n+  - No empty strings in the output.\n+- `pub fn char_trigrams(s: &str) -> Vec<[char; 3]>` over a lowercased string — sliding length-3 windows. Strings shorter than 3 chars return an empty Vec. Document the behavior.\n\n-This is foundational: BM25 (corpus df/avgdl + per-chunk tf) and trigram-Dice both consume `tokenize`. No DB access, no embedding access — pure string functions.\n+This is foundational and reusable: BM25 (corpus df/avgdl + per-doc tf) and trigram-Dice both consume `tokenize`/`char_trigrams` in the scoring card.\n\n ## Acceptance Criteria\n+- [ ] `crates/swissarmyhammer-search/` exists, is a workspace member, and `cargo build -p swissarmyhammer-search` compiles with only `serde` as a dependency.\n - [ ] `tokenize(\"getUserById\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (camelCase split, lowercased).\n-- [ ] `tokenize(\"get_user_by_id\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (snake_case split).\n-- [ ] `tokenize(\"fn parse_config() -> Result\")` splits punctuation and keywords into lowercase word tokens with no empty strings.\n+- [ ] `tokenize(\"get_user_by_id\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (snake_case split via punctuation).\n+- [ ] `tokenize(\"fn parse_config() -> Result\")` splits punctuation/keywords into lowercase word tokens with no empty strings.\n - [ ] `char_trigrams(\"get_user\")` returns the sliding length-3 windows of the lowercased string; strings shorter than 3 chars return an empty Vec.\n-- [ ] Module is reachable from `search_code.rs` (it `use`s `super::search_fusion::tokenize`).\n+- [ ] All public types (`Doc`/`Field`/`Query`/`SignalWeights`/`Hit`/`Signals`) compile and `Hit`/`Signals` are `Serialize`; `SignalWeights::default()` is all `1.0`.\n\n ## Tests\n-- [ ] Unit tests in `crates/swissarmyhammer-code-context/src/ops/search_fusion/tokenize.rs` `#[cfg(test)] mod tests`: camelCase split, snake_case split, mixed `getHTTPResponse`-style runs, punctuation stripping, empty-string and sub-3-char trigram edge cases.\n-- [ ] `cargo test -p swissarmyhammer-code-context tokenize` passes (all new tests green).\n+- [ ] Unit tests in `crates/swissarmyhammer-search/src/tokenize.rs` `#[cfg(test)] mod tests`: camelCase split, snake_case split, mixed `getHTTPResponse`-style runs, punctuation stripping, empty-string and sub-3-char trigram edge cases.\n+- [ ] `cargo test -p swissarmyhammer-search tokenize` passes (all new tests green).\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -4,49 +4,33 @@\n position_column: todo\n position_ordinal: '8980'\n project: semantic-search\n-title: 'Create swissarmyhammer-search crate: types + code-aware tokenizer'\n+title: Code-aware tokenizer module for search fusion\n ---\n ## What\n-Scaffold a NEW leaf crate `crates/swissarmyhammer-search/` — a generic, in-memory document ranker with NO workspace dependencies (true leaf). It is consumed by BOTH `swissarmyhammer-code-context` (chunk search) and `swissarmyhammer-kanban` (task search), so it must NOT depend on `model-embedding`, `swissarmyhammer-embedding`, or any sibling crate.\n+Add a code-aware tokenizer that the BM25 and trigram signals of the fused `search code` will share. New module: `crates/swissarmyhammer-code-context/src/ops/search_fusion/tokenize.rs` (create the `search_fusion` submodule dir; register `pub mod search_fusion;` inside `crates/swissarmyhammer-code-context/src/ops/search_code.rs` OR add `pub mod search_fusion;` to `crates/swissarmyhammer-code-context/src/ops/mod.rs` — pick the latter so the primitives are reusable and unit-testable independently of the op).\n\n-Files to create:\n-- `crates/swissarmyhammer-search/Cargo.toml` — package `swissarmyhammer-search`, `version.workspace = true`, edition/license/repository `.workspace = true`. Deps: `serde = { workspace = true }` only (for `Serialize` on `Hit`/`Signals`). No tokio, no model crates.\n-- `crates/swissarmyhammer-search/src/lib.rs` — module decls + the public types below.\n-- `crates/swissarmyhammer-search/src/tokenize.rs` — the tokenizer + `char_trigrams`.\n+Tokenization rules (agreed design — do NOT add Porter stemming; trigram carries fuzziness):\n+- Lowercase everything.\n+- Split on whitespace and punctuation (anything not `[A-Za-z0-9]`).\n+- Additionally split identifiers on camelCase boundaries (`getUser` -> `get`, `user`) and snake_case boundaries (`get_user` -> `get`, `user`).\n+- Digit runs split off as their own tokens at letter/digit transitions is optional — keep simple: treat `[A-Za-z0-9]+` runs then sub-split on camelCase. Document the exact rule chosen in the function docstring.\n\n-Workspace wiring (edit `Cargo.toml` at repo root):\n-- Add `\"crates/swissarmyhammer-search\"` to `[workspace] members`.\n-- Add `swissarmyhammer-search = { path = \"crates/swissarmyhammer-search\" }` to `[workspace.dependencies]` (alphabetical, near the other `swissarmyhammer-*` entries around the `swissarmyhammer-sem` / `swissarmyhammer-skills` lines).\n+Public API (pure, no DB):\n+- `pub fn tokenize(text: &str) -> Vec<String>` — full pipeline above.\n+- `pub fn char_trigrams(s: &str) -> Vec<[char; 3]>` (or `Vec<String>` of length-3 windows) over a lowercased string — used by the trigram-Dice signal. Document handling of strings shorter than 3 chars (return empty).\n\n-Public types (in `lib.rs`; this card defines them, the scoring card implements `search()`):\n-- `pub struct Doc { pub id: String, pub fields: Vec<Field>, pub embedding: Option<Vec<f32>> }`\n-- `pub struct Field { pub weight: f32, pub text: String }`\n-- `pub struct Query { pub text: String, pub embedding: Option<Vec<f32>>, pub weights: SignalWeights, pub top_k: usize, pub min_score: Option<f32> }`\n-- `pub struct SignalWeights { pub w_bm25: f32, pub w_trigram: f32, pub w_cosine: f32 }` with a `Default` of all `1.0`.\n-- `pub struct Hit { pub id: String, pub score: f32, pub signals: Signals }` — derive `Debug, Clone, serde::Serialize`.\n-- `pub struct Signals { pub bm25: f32, pub trigram: f32, pub cosine: f32 }` — derive `Debug, Clone, Copy, serde::Serialize`.\n-\n-Tokenizer (`tokenize.rs`, pure functions, no DB/embedding access — do NOT add Porter stemming; trigram carries fuzziness):\n-- `pub fn tokenize(text: &str) -> Vec<String>`:\n-  - Lowercase everything.\n-  - Split on whitespace and punctuation (anything not `[A-Za-z0-9]`).\n-  - Sub-split each `[A-Za-z0-9]+` run on camelCase boundaries (`getUser` -> `get`, `user`) and snake_case boundaries (already split by punctuation since `_` is punctuation). Document the exact rule chosen in the docstring (e.g. split at lower→upper transitions, and at letter→digit if chosen — keep it documented and consistent).\n-  - No empty strings in the output.\n-- `pub fn char_trigrams(s: &str) -> Vec<[char; 3]>` over a lowercased string — sliding length-3 windows. Strings shorter than 3 chars return an empty Vec. Document the behavior.\n-\n-This is foundational and reusable: BM25 (corpus df/avgdl + per-doc tf) and trigram-Dice both consume `tokenize`/`char_trigrams` in the scoring card.\n+This is foundational: BM25 (corpus df/avgdl + per-chunk tf) and trigram-Dice both consume `tokenize`. No DB access, no embedding access — pure string functions.\n\n ## Acceptance Criteria\n-- [ ] `crates/swissarmyhammer-search/` exists, is a workspace member, and `cargo build -p swissarmyhammer-search` compiles with only `serde` as a dependency.\n - [ ] `tokenize(\"getUserById\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (camelCase split, lowercased).\n-- [ ] `tokenize(\"get_user_by_id\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (snake_case split via punctuation).\n-- [ ] `tokenize(\"fn parse_config() -> Result\")` splits punctuation/keywords into lowercase word tokens with no empty strings.\n+- [ ] `tokenize(\"get_user_by_id\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (snake_case split).\n+- [ ] `tokenize(\"fn parse_config() -> Result\")` splits punctuation and keywords into lowercase word tokens with no empty strings.\n - [ ] `char_trigrams(\"get_user\")` returns the sliding length-3 windows of the lowercased string; strings shorter than 3 chars return an empty Vec.\n-- [ ] All public types (`Doc`/`Field`/`Query`/`SignalWeights`/`Hit`/`Signals`) compile and `Hit`/`Signals` are `Serialize`; `SignalWeights::default()` is all `1.0`.\n+- [ ] Module is reachable from `search_code.rs` (it `use`s `super::search_fusion::tokenize`).\n\n ## Tests\n-- [ ] Unit tests in `crates/swissarmyhammer-search/src/tokenize.rs` `#[cfg(test)] mod tests`: camelCase split, snake_case split, mixed `getHTTPResponse`-style runs, punctuation stripping, empty-string and sub-3-char trigram edge cases.\n-- [ ] `cargo test -p swissarmyhammer-search tokenize` passes (all new tests green).\n+- [ ] Unit tests in `crates/swissarmyhammer-code-context/src/ops/search_fusion/tokenize.rs` `#[cfg(test)] mod tests`: camelCase split, snake_case split, mixed `getHTTPResponse`-style runs, punctuation stripping, empty-string and sub-3-char trigram edge cases.\n+- [ ] `cargo test -p swissarmyhammer-code-context tokenize` passes (all new tests green).\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTCE0SV1T02H07K830DAV7H3","timestamp":"2026-06-05T17:41:43.905291Z","op":"update","item_id":"01KTC7Y50PEM427HQ79NW52WY4","forward_patch":"--- original\n+++ modified\n@@ -7,16 +7,21 @@\n title: 'Create swissarmyhammer-search crate: types + code-aware tokenizer'\n ---\n ## What\n-Scaffold a NEW leaf crate `crates/swissarmyhammer-search/` — a generic, in-memory document ranker with NO workspace dependencies (true leaf). It is consumed by BOTH `swissarmyhammer-code-context` (chunk search) and `swissarmyhammer-kanban` (task search), so it must NOT depend on `model-embedding`, `swissarmyhammer-embedding`, or any sibling crate.\n+Scaffold a NEW leaf crate `crates/swissarmyhammer-search/` — a generic, in-memory document ranker with NO workspace-sibling dependencies (true leaf). It is consumed by BOTH `swissarmyhammer-code-context` (chunk search) and `swissarmyhammer-kanban` (task search), so it must NOT depend on `model-embedding`, `swissarmyhammer-embedding`, or any sibling crate.\n+\n+DO NOT hand-roll the word/identifier splitter. Use existing, dependency-light crates already resolved in the workspace `Cargo.lock`:\n+- `convert_case` (0.10) — `convert_case::split(identifier, boundaries) -> Vec<&str>` with the `Boundary` enum. Handles camelCase/PascalCase, acronyms (`HTTPRequest` -> `http`,`request`), and digit boundaries. We choose the `Boundary` set, so digit-splitting is a config choice, not code.\n+- `unicode-segmentation` (1.13) — `UnicodeSegmentation::unicode_words()` (UAX #29) for the punctuation/whitespace/Unicode word layer (zero transitive deps).\n\n Files to create:\n-- `crates/swissarmyhammer-search/Cargo.toml` — package `swissarmyhammer-search`, `version.workspace = true`, edition/license/repository `.workspace = true`. Deps: `serde = { workspace = true }` only (for `Serialize` on `Hit`/`Signals`). No tokio, no model crates.\n+- `crates/swissarmyhammer-search/Cargo.toml` — package `swissarmyhammer-search`, `version.workspace = true`, edition/license/repository `.workspace = true`. Deps: `serde = { workspace = true }` (for `Serialize` on `Hit`/`Signals`), `convert_case = { workspace = true }`, `unicode-segmentation = { workspace = true }`. No tokio, no model crates, no sibling crates.\n - `crates/swissarmyhammer-search/src/lib.rs` — module decls + the public types below.\n-- `crates/swissarmyhammer-search/src/tokenize.rs` — the tokenizer + `char_trigrams`.\n+- `crates/swissarmyhammer-search/src/tokenize.rs` — the tokenizer (wrapping the two crates) + `char_trigrams`.\n\n Workspace wiring (edit `Cargo.toml` at repo root):\n - Add `\"crates/swissarmyhammer-search\"` to `[workspace] members`.\n-- Add `swissarmyhammer-search = { path = \"crates/swissarmyhammer-search\" }` to `[workspace.dependencies]` (alphabetical, near the other `swissarmyhammer-*` entries around the `swissarmyhammer-sem` / `swissarmyhammer-skills` lines).\n+- Add `swissarmyhammer-search = { path = \"crates/swissarmyhammer-search\" }` to `[workspace.dependencies]` (alphabetical, near the other `swissarmyhammer-*` entries).\n+- Ensure `convert_case` and `unicode-segmentation` are present in `[workspace.dependencies]` (add them pinned to the already-resolved versions `convert_case = \"0.10\"`, `unicode-segmentation = \"1\"` if not already listed — they exist transitively in `Cargo.lock` today).\n\n Public types (in `lib.rs`; this card defines them, the scoring card implements `search()`):\n - `pub struct Doc { pub id: String, pub fields: Vec<Field>, pub embedding: Option<Vec<f32>> }`\n@@ -26,26 +31,28 @@\n - `pub struct Hit { pub id: String, pub score: f32, pub signals: Signals }` — derive `Debug, Clone, serde::Serialize`.\n - `pub struct Signals { pub bm25: f32, pub trigram: f32, pub cosine: f32 }` — derive `Debug, Clone, Copy, serde::Serialize`.\n\n-Tokenizer (`tokenize.rs`, pure functions, no DB/embedding access — do NOT add Porter stemming; trigram carries fuzziness):\n+Tokenizer (`tokenize.rs`, pure functions, no DB/embedding access — NO Porter stemming; trigram carries fuzziness):\n - `pub fn tokenize(text: &str) -> Vec<String>`:\n-  - Lowercase everything.\n-  - Split on whitespace and punctuation (anything not `[A-Za-z0-9]`).\n-  - Sub-split each `[A-Za-z0-9]+` run on camelCase boundaries (`getUser` -> `get`, `user`) and snake_case boundaries (already split by punctuation since `_` is punctuation). Document the exact rule chosen in the docstring (e.g. split at lower→upper transitions, and at letter→digit if chosen — keep it documented and consistent).\n-  - No empty strings in the output.\n-- `pub fn char_trigrams(s: &str) -> Vec<[char; 3]>` over a lowercased string — sliding length-3 windows. Strings shorter than 3 chars return an empty Vec. Document the behavior.\n+  - Pass `text` through `unicode_words()` to get word runs (strips punctuation/whitespace, Unicode-correct).\n+  - For each word, apply `convert_case::split(word, &BOUNDARIES)` to break camelCase/PascalCase/acronym/underscore boundaries.\n+  - Define a module-level `BOUNDARIES` slice (the agreed decision): include case + acronym + delimiter boundaries (`LowerUpper`, `UpperLower`/`Acronym`, `Underscore`, `Hyphen`, `Space`); EXCLUDE the digit boundaries so `sha256`/`utf8`/`base64` stay whole. Document the exact slice in the docstring.\n+  - Lowercase each segment; preserve duplicates (BM25 needs term frequency — do NOT dedup); no empty strings.\n+- `pub fn char_trigrams(s: &str) -> Vec<[char; 3]>` over a lowercased string — sliding length-3 windows of `chars()` (Unicode-safe). Strings shorter than 3 chars return an empty Vec. (This is n-gram windowing, not a splitter — keep inline.)\n\n This is foundational and reusable: BM25 (corpus df/avgdl + per-doc tf) and trigram-Dice both consume `tokenize`/`char_trigrams` in the scoring card.\n\n ## Acceptance Criteria\n-- [ ] `crates/swissarmyhammer-search/` exists, is a workspace member, and `cargo build -p swissarmyhammer-search` compiles with only `serde` as a dependency.\n+- [ ] `crates/swissarmyhammer-search/` exists, is a workspace member, and `cargo build -p swissarmyhammer-search` compiles with deps `serde`, `convert_case`, `unicode-segmentation` only (no sibling crates).\n - [ ] `tokenize(\"getUserById\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (camelCase split, lowercased).\n-- [ ] `tokenize(\"get_user_by_id\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (snake_case split via punctuation).\n-- [ ] `tokenize(\"fn parse_config() -> Result\")` splits punctuation/keywords into lowercase word tokens with no empty strings.\n+- [ ] `tokenize(\"get_user_by_id\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (snake_case split).\n+- [ ] `tokenize(\"getHTTPResponse\")` yields `[\"get\", \"http\", \"response\"]` (acronym boundary handled by `convert_case`).\n+- [ ] `tokenize(\"sha256_hash\")` yields `[\"sha256\", \"hash\"]` (digit boundary EXCLUDED — `sha256` stays whole).\n+- [ ] `tokenize(\"fn parse_config() -> Result\")` splits punctuation into lowercase word tokens with no empty strings (e.g. `[\"fn\", \"parse\", \"config\", \"result\"]`).\n - [ ] `char_trigrams(\"get_user\")` returns the sliding length-3 windows of the lowercased string; strings shorter than 3 chars return an empty Vec.\n-- [ ] All public types (`Doc`/`Field`/`Query`/`SignalWeights`/`Hit`/`Signals`) compile and `Hit`/`Signals` are `Serialize`; `SignalWeights::default()` is all `1.0`.\n+- [ ] All public types compile; `Hit`/`Signals` are `Serialize`; `SignalWeights::default()` is all `1.0`.\n\n ## Tests\n-- [ ] Unit tests in `crates/swissarmyhammer-search/src/tokenize.rs` `#[cfg(test)] mod tests`: camelCase split, snake_case split, mixed `getHTTPResponse`-style runs, punctuation stripping, empty-string and sub-3-char trigram edge cases.\n+- [ ] Unit tests in `crates/swissarmyhammer-search/src/tokenize.rs` `#[cfg(test)] mod tests`: camelCase split, snake_case split, acronym run (`getHTTPResponse`), digit-kept (`sha256`, `utf8`), punctuation stripping, term-frequency preserved (duplicates not deduped), empty-string and sub-3-char trigram edge cases. Tests pin the chosen `BOUNDARIES` behavior so a future crate-version bump that changes splitting is caught.\n - [ ] `cargo test -p swissarmyhammer-search tokenize` passes (all new tests green).\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -7,21 +7,16 @@\n title: 'Create swissarmyhammer-search crate: types + code-aware tokenizer'\n ---\n ## What\n-Scaffold a NEW leaf crate `crates/swissarmyhammer-search/` — a generic, in-memory document ranker with NO workspace-sibling dependencies (true leaf). It is consumed by BOTH `swissarmyhammer-code-context` (chunk search) and `swissarmyhammer-kanban` (task search), so it must NOT depend on `model-embedding`, `swissarmyhammer-embedding`, or any sibling crate.\n-\n-DO NOT hand-roll the word/identifier splitter. Use existing, dependency-light crates already resolved in the workspace `Cargo.lock`:\n-- `convert_case` (0.10) — `convert_case::split(identifier, boundaries) -> Vec<&str>` with the `Boundary` enum. Handles camelCase/PascalCase, acronyms (`HTTPRequest` -> `http`,`request`), and digit boundaries. We choose the `Boundary` set, so digit-splitting is a config choice, not code.\n-- `unicode-segmentation` (1.13) — `UnicodeSegmentation::unicode_words()` (UAX #29) for the punctuation/whitespace/Unicode word layer (zero transitive deps).\n+Scaffold a NEW leaf crate `crates/swissarmyhammer-search/` — a generic, in-memory document ranker with NO workspace dependencies (true leaf). It is consumed by BOTH `swissarmyhammer-code-context` (chunk search) and `swissarmyhammer-kanban` (task search), so it must NOT depend on `model-embedding`, `swissarmyhammer-embedding`, or any sibling crate.\n\n Files to create:\n-- `crates/swissarmyhammer-search/Cargo.toml` — package `swissarmyhammer-search`, `version.workspace = true`, edition/license/repository `.workspace = true`. Deps: `serde = { workspace = true }` (for `Serialize` on `Hit`/`Signals`), `convert_case = { workspace = true }`, `unicode-segmentation = { workspace = true }`. No tokio, no model crates, no sibling crates.\n+- `crates/swissarmyhammer-search/Cargo.toml` — package `swissarmyhammer-search`, `version.workspace = true`, edition/license/repository `.workspace = true`. Deps: `serde = { workspace = true }` only (for `Serialize` on `Hit`/`Signals`). No tokio, no model crates.\n - `crates/swissarmyhammer-search/src/lib.rs` — module decls + the public types below.\n-- `crates/swissarmyhammer-search/src/tokenize.rs` — the tokenizer (wrapping the two crates) + `char_trigrams`.\n+- `crates/swissarmyhammer-search/src/tokenize.rs` — the tokenizer + `char_trigrams`.\n\n Workspace wiring (edit `Cargo.toml` at repo root):\n - Add `\"crates/swissarmyhammer-search\"` to `[workspace] members`.\n-- Add `swissarmyhammer-search = { path = \"crates/swissarmyhammer-search\" }` to `[workspace.dependencies]` (alphabetical, near the other `swissarmyhammer-*` entries).\n-- Ensure `convert_case` and `unicode-segmentation` are present in `[workspace.dependencies]` (add them pinned to the already-resolved versions `convert_case = \"0.10\"`, `unicode-segmentation = \"1\"` if not already listed — they exist transitively in `Cargo.lock` today).\n+- Add `swissarmyhammer-search = { path = \"crates/swissarmyhammer-search\" }` to `[workspace.dependencies]` (alphabetical, near the other `swissarmyhammer-*` entries around the `swissarmyhammer-sem` / `swissarmyhammer-skills` lines).\n\n Public types (in `lib.rs`; this card defines them, the scoring card implements `search()`):\n - `pub struct Doc { pub id: String, pub fields: Vec<Field>, pub embedding: Option<Vec<f32>> }`\n@@ -31,28 +26,26 @@\n - `pub struct Hit { pub id: String, pub score: f32, pub signals: Signals }` — derive `Debug, Clone, serde::Serialize`.\n - `pub struct Signals { pub bm25: f32, pub trigram: f32, pub cosine: f32 }` — derive `Debug, Clone, Copy, serde::Serialize`.\n\n-Tokenizer (`tokenize.rs`, pure functions, no DB/embedding access — NO Porter stemming; trigram carries fuzziness):\n+Tokenizer (`tokenize.rs`, pure functions, no DB/embedding access — do NOT add Porter stemming; trigram carries fuzziness):\n - `pub fn tokenize(text: &str) -> Vec<String>`:\n-  - Pass `text` through `unicode_words()` to get word runs (strips punctuation/whitespace, Unicode-correct).\n-  - For each word, apply `convert_case::split(word, &BOUNDARIES)` to break camelCase/PascalCase/acronym/underscore boundaries.\n-  - Define a module-level `BOUNDARIES` slice (the agreed decision): include case + acronym + delimiter boundaries (`LowerUpper`, `UpperLower`/`Acronym`, `Underscore`, `Hyphen`, `Space`); EXCLUDE the digit boundaries so `sha256`/`utf8`/`base64` stay whole. Document the exact slice in the docstring.\n-  - Lowercase each segment; preserve duplicates (BM25 needs term frequency — do NOT dedup); no empty strings.\n-- `pub fn char_trigrams(s: &str) -> Vec<[char; 3]>` over a lowercased string — sliding length-3 windows of `chars()` (Unicode-safe). Strings shorter than 3 chars return an empty Vec. (This is n-gram windowing, not a splitter — keep inline.)\n+  - Lowercase everything.\n+  - Split on whitespace and punctuation (anything not `[A-Za-z0-9]`).\n+  - Sub-split each `[A-Za-z0-9]+` run on camelCase boundaries (`getUser` -> `get`, `user`) and snake_case boundaries (already split by punctuation since `_` is punctuation). Document the exact rule chosen in the docstring (e.g. split at lower→upper transitions, and at letter→digit if chosen — keep it documented and consistent).\n+  - No empty strings in the output.\n+- `pub fn char_trigrams(s: &str) -> Vec<[char; 3]>` over a lowercased string — sliding length-3 windows. Strings shorter than 3 chars return an empty Vec. Document the behavior.\n\n This is foundational and reusable: BM25 (corpus df/avgdl + per-doc tf) and trigram-Dice both consume `tokenize`/`char_trigrams` in the scoring card.\n\n ## Acceptance Criteria\n-- [ ] `crates/swissarmyhammer-search/` exists, is a workspace member, and `cargo build -p swissarmyhammer-search` compiles with deps `serde`, `convert_case`, `unicode-segmentation` only (no sibling crates).\n+- [ ] `crates/swissarmyhammer-search/` exists, is a workspace member, and `cargo build -p swissarmyhammer-search` compiles with only `serde` as a dependency.\n - [ ] `tokenize(\"getUserById\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (camelCase split, lowercased).\n-- [ ] `tokenize(\"get_user_by_id\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (snake_case split).\n-- [ ] `tokenize(\"getHTTPResponse\")` yields `[\"get\", \"http\", \"response\"]` (acronym boundary handled by `convert_case`).\n-- [ ] `tokenize(\"sha256_hash\")` yields `[\"sha256\", \"hash\"]` (digit boundary EXCLUDED — `sha256` stays whole).\n-- [ ] `tokenize(\"fn parse_config() -> Result\")` splits punctuation into lowercase word tokens with no empty strings (e.g. `[\"fn\", \"parse\", \"config\", \"result\"]`).\n+- [ ] `tokenize(\"get_user_by_id\")` yields `[\"get\", \"user\", \"by\", \"id\"]` (snake_case split via punctuation).\n+- [ ] `tokenize(\"fn parse_config() -> Result\")` splits punctuation/keywords into lowercase word tokens with no empty strings.\n - [ ] `char_trigrams(\"get_user\")` returns the sliding length-3 windows of the lowercased string; strings shorter than 3 chars return an empty Vec.\n-- [ ] All public types compile; `Hit`/`Signals` are `Serialize`; `SignalWeights::default()` is all `1.0`.\n+- [ ] All public types (`Doc`/`Field`/`Query`/`SignalWeights`/`Hit`/`Signals`) compile and `Hit`/`Signals` are `Serialize`; `SignalWeights::default()` is all `1.0`.\n\n ## Tests\n-- [ ] Unit tests in `crates/swissarmyhammer-search/src/tokenize.rs` `#[cfg(test)] mod tests`: camelCase split, snake_case split, acronym run (`getHTTPResponse`), digit-kept (`sha256`, `utf8`), punctuation stripping, term-frequency preserved (duplicates not deduped), empty-string and sub-3-char trigram edge cases. Tests pin the chosen `BOUNDARIES` behavior so a future crate-version bump that changes splitting is caught.\n+- [ ] Unit tests in `crates/swissarmyhammer-search/src/tokenize.rs` `#[cfg(test)] mod tests`: camelCase split, snake_case split, mixed `getHTTPResponse`-style runs, punctuation stripping, empty-string and sub-3-char trigram edge cases.\n - [ ] `cargo test -p swissarmyhammer-search tokenize` passes (all new tests green).\n\n ## Workflow\n","transaction_id":null}

---
position_column: done
position_ordinal: h2
title: Fix onBlur double-commit in FieldPlaceholder
---
**Resolution:** Already fixed. The `committedRef` guard pattern is already implemented (lines 76-95). Both `commitAndExit` and `cancelAndExit` check/set the ref, and `onBlur` calls through `commitAndExitRef` which short-circuits if already resolved.
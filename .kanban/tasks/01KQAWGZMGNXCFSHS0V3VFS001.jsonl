{"id":"01KQAWGZMHKXN6170F7K7C7BDB","timestamp":"2026-04-28T20:29:39.857493Z","op":"create","item_id":"01KQAWGZMGNXCFSHS0V3VFS001","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,56 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: a180\n+project: spatial-nav\n+title: 'Focus debug overlay: drop width × height from label, keep only (x, y)'\n+---\n+## What\n+\n+The focus debug overlay (`kanban-app/ui/src/components/focus-debug-overlay.tsx:192`) currently renders labels in the form:\n+\n+```\n+zone:column:01ABC (120,80) 320×640\n+```\n+\n+The trailing `320×640` is width × height. **It was not requested and is visual noise** — the original ask was a small-font x/y coordinate to verify focus is registering at the right position. Drop the dimensions from the label, keep only the coordinate pair:\n+\n+```\n+zone:column:01ABC (120,80)\n+```\n+\n+Layers stay unchanged (`layer:window`, no rect — layers don't have a meaningful rect).\n+\n+## What changes\n+\n+`kanban-app/ui/src/components/focus-debug-overlay.tsx`:\n+\n+- Line 192 — change the label format from `${kind}:${label} (${Math.round(rect.x)},${Math.round(rect.y)}) ${Math.round(rect.width)}×${Math.round(rect.height)}` to `${kind}:${label} (${Math.round(rect.x)},${Math.round(rect.y)})`.\n+- Lines 162–165 — drop the `width` / `height` legs of the rect-equality short-circuit. Now that those values aren't displayed, comparing on them produces unnecessary overlay re-renders when the host's dimensions change but its position doesn't. Keep the comparison on `x` and `y` only.\n+\n+The internal `rect` state can still hold width/height (the kernel uses them — they come from `getBoundingClientRect()`); they're simply no longer used for either the visible label or the re-render gate.\n+\n+## Acceptance Criteria\n+\n+All asserted by automated tests below.\n+\n+- [ ] The rendered debug-overlay label for a zone or scope shows the kind, the moniker, and the `(x, y)` pair — and no width × height suffix.\n+- [ ] The rendered debug-overlay label for a layer shows `layer:<name>` and nothing else (unchanged).\n+- [ ] Resizing the host element without moving it (e.g. content reflow that changes width without changing top-left) does NOT cause the overlay's React component to re-render or re-set its `rect` state. (Optimization side-effect of dropping the w/h legs of the equality check.)\n+\n+## Tests\n+\n+### Frontend — update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`\n+\n+- [ ] `zone_label_has_no_dimensions_suffix` — render `<FocusDebugOverlay kind=\"zone\" label=\"ui:test\" hostRef={refToFixedRect}>` against a host at known `(x, y, w, h) = (10, 20, 100, 50)`; assert the rendered label text is exactly `zone:ui:test (10,20)` and does NOT contain `100×50` or `100x50`.\n+- [ ] `scope_label_has_no_dimensions_suffix` — same with `kind=\"scope\"`.\n+- [ ] `layer_label_unchanged` — `<FocusDebugOverlay kind=\"layer\" label=\"window\" ... />` renders `layer:window` exactly. Regression guard.\n+- [ ] `overlay_does_not_rerender_on_pure_dimension_change` — render the overlay, capture its render count via a probe; mutate the host's width and height but keep its top-left fixed; await an animation frame; assert the overlay did not commit a new render. (Pins the side-effect that drops w/h from the equality short-circuit.)\n+\n+Test command: `bun run test:browser focus-debug-overlay.browser.test.tsx` — all four pass alongside the existing tests in that file.\n+\n+## Workflow\n+\n+- Use `/tdd` — update the existing browser test first to expect labels without dimensions, watch it fail, edit the format string, watch it pass.\n+- One file touched (`focus-debug-overlay.tsx`), one test file updated. Tiny fix.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,56 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: a180\n-project: spatial-nav\n-title: 'Focus debug overlay: drop width × height from label, keep only (x, y)'\n----\n-## What\n-\n-The focus debug overlay (`kanban-app/ui/src/components/focus-debug-overlay.tsx:192`) currently renders labels in the form:\n-\n-```\n-zone:column:01ABC (120,80) 320×640\n-```\n-\n-The trailing `320×640` is width × height. **It was not requested and is visual noise** — the original ask was a small-font x/y coordinate to verify focus is registering at the right position. Drop the dimensions from the label, keep only the coordinate pair:\n-\n-```\n-zone:column:01ABC (120,80)\n-```\n-\n-Layers stay unchanged (`layer:window`, no rect — layers don't have a meaningful rect).\n-\n-## What changes\n-\n-`kanban-app/ui/src/components/focus-debug-overlay.tsx`:\n-\n-- Line 192 — change the label format from `${kind}:${label} (${Math.round(rect.x)},${Math.round(rect.y)}) ${Math.round(rect.width)}×${Math.round(rect.height)}` to `${kind}:${label} (${Math.round(rect.x)},${Math.round(rect.y)})`.\n-- Lines 162–165 — drop the `width` / `height` legs of the rect-equality short-circuit. Now that those values aren't displayed, comparing on them produces unnecessary overlay re-renders when the host's dimensions change but its position doesn't. Keep the comparison on `x` and `y` only.\n-\n-The internal `rect` state can still hold width/height (the kernel uses them — they come from `getBoundingClientRect()`); they're simply no longer used for either the visible label or the re-render gate.\n-\n-## Acceptance Criteria\n-\n-All asserted by automated tests below.\n-\n-- [ ] The rendered debug-overlay label for a zone or scope shows the kind, the moniker, and the `(x, y)` pair — and no width × height suffix.\n-- [ ] The rendered debug-overlay label for a layer shows `layer:<name>` and nothing else (unchanged).\n-- [ ] Resizing the host element without moving it (e.g. content reflow that changes width without changing top-left) does NOT cause the overlay's React component to re-render or re-set its `rect` state. (Optimization side-effect of dropping the w/h legs of the equality check.)\n-\n-## Tests\n-\n-### Frontend — update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`\n-\n-- [ ] `zone_label_has_no_dimensions_suffix` — render `<FocusDebugOverlay kind=\"zone\" label=\"ui:test\" hostRef={refToFixedRect}>` against a host at known `(x, y, w, h) = (10, 20, 100, 50)`; assert the rendered label text is exactly `zone:ui:test (10,20)` and does NOT contain `100×50` or `100x50`.\n-- [ ] `scope_label_has_no_dimensions_suffix` — same with `kind=\"scope\"`.\n-- [ ] `layer_label_unchanged` — `<FocusDebugOverlay kind=\"layer\" label=\"window\" ... />` renders `layer:window` exactly. Regression guard.\n-- [ ] `overlay_does_not_rerender_on_pure_dimension_change` — render the overlay, capture its render count via a probe; mutate the host's width and height but keep its top-left fixed; await an animation frame; assert the overlay did not commit a new render. (Pins the side-effect that drops w/h from the equality short-circuit.)\n-\n-Test command: `bun run test:browser focus-debug-overlay.browser.test.tsx` — all four pass alongside the existing tests in that file.\n-\n-## Workflow\n-\n-- Use `/tdd` — update the existing browser test first to expect labels without dimensions, watch it fail, edit the format string, watch it pass.\n-- One file touched (`focus-debug-overlay.tsx`), one test file updated. Tiny fix.\n","transaction_id":null}
{"id":"01KQAWGZMJQ9YKQX8VWHQCG4JZ","timestamp":"2026-04-28T20:29:39.858019Z","entity_type":"task","entity_id":"01KQAWGZMGNXCFSHS0V3VFS001","op":"create","changes":[["title",{"kind":"set","value":"Focus debug overlay: drop width × height from label, keep only (x, y)"}],["project",{"kind":"set","value":"spatial-nav"}],["position_column",{"kind":"set","value":"todo"}],["body",{"kind":"set","value":"## What\n\nThe focus debug overlay (`kanban-app/ui/src/components/focus-debug-overlay.tsx:192`) currently renders labels in the form:\n\n```\nzone:column:01ABC (120,80) 320×640\n```\n\nThe trailing `320×640` is width × height. **It was not requested and is visual noise** — the original ask was a small-font x/y coordinate to verify focus is registering at the right position. Drop the dimensions from the label, keep only the coordinate pair:\n\n```\nzone:column:01ABC (120,80)\n```\n\nLayers stay unchanged (`layer:window`, no rect — layers don't have a meaningful rect).\n\n## What changes\n\n`kanban-app/ui/src/components/focus-debug-overlay.tsx`:\n\n- Line 192 — change the label format from `${kind}:${label} (${Math.round(rect.x)},${Math.round(rect.y)}) ${Math.round(rect.width)}×${Math.round(rect.height)}` to `${kind}:${label} (${Math.round(rect.x)},${Math.round(rect.y)})`.\n- Lines 162–165 — drop the `width` / `height` legs of the rect-equality short-circuit. Now that those values aren't displayed, comparing on them produces unnecessary overlay re-renders when the host's dimensions change but its position doesn't. Keep the comparison on `x` and `y` only.\n\nThe internal `rect` state can still hold width/height (the kernel uses them — they come from `getBoundingClientRect()`); they're simply no longer used for either the visible label or the re-render gate.\n\n## Acceptance Criteria\n\nAll asserted by automated tests below.\n\n- [ ] The rendered debug-overlay label for a zone or scope shows the kind, the moniker, and the `(x, y)` pair — and no width × height suffix.\n- [ ] The rendered debug-overlay label for a layer shows `layer:<name>` and nothing else (unchanged).\n- [ ] Resizing the host element without moving it (e.g. content reflow that changes width without changing top-left) does NOT cause the overlay's React component to re-render or re-set its `rect` state. (Optimization side-effect of dropping the w/h legs of the equality check.)\n\n## Tests\n\n### Frontend — update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`\n\n- [ ] `zone_label_has_no_dimensions_suffix` — render `<FocusDebugOverlay kind=\"zone\" label=\"ui:test\" hostRef={refToFixedRect}>` against a host at known `(x, y, w, h) = (10, 20, 100, 50)`; assert the rendered label text is exactly `zone:ui:test (10,20)` and does NOT contain `100×50` or `100x50`.\n- [ ] `scope_label_has_no_dimensions_suffix` — same with `kind=\"scope\"`.\n- [ ] `layer_label_unchanged` — `<FocusDebugOverlay kind=\"layer\" label=\"window\" ... />` renders `layer:window` exactly. Regression guard.\n- [ ] `overlay_does_not_rerender_on_pure_dimension_change` — render the overlay, capture its render count via a probe; mutate the host's width and height but keep its top-left fixed; await an animation frame; assert the overlay did not commit a new render. (Pins the side-effect that drops w/h from the equality short-circuit.)\n\nTest command: `bun run test:browser focus-debug-overlay.browser.test.tsx` — all four pass alongside the existing tests in that file.\n\n## Workflow\n\n- Use `/tdd` — update the existing browser test first to expect labels without dimensions, watch it fail, edit the format string, watch it pass.\n- One file touched (`focus-debug-overlay.tsx`), one test file updated. Tiny fix.\n"}],["position_ordinal",{"kind":"set","value":"a180"}],["assignees",{"kind":"set","value":["claude-code"]}]]}
{"id":"01KQB0XBA7Z7VW4CHK67AF98K1","timestamp":"2026-04-28T21:46:19.335814Z","op":"update","item_id":"01KQAWGZMGNXCFSHS0V3VFS001","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: a180\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Focus debug overlay: drop width × height from label, keep only (x, y)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: a180\n project: spatial-nav\n title: 'Focus debug overlay: drop width × height from label, keep only (x, y)'\n ---\n","transaction_id":null}
{"id":"01KQB0XBA9KJMKPB89TPN23VH3","timestamp":"2026-04-28T21:46:19.337043Z","entity_type":"task","entity_id":"01KQAWGZMGNXCFSHS0V3VFS001","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-a180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+a180\n\\ No newline at end of file\n"}]]}
{"id":"01KQB17CP0SB9MC1AK57VFK6PG","timestamp":"2026-04-28T21:51:48.416803Z","op":"update","item_id":"01KQAWGZMGNXCFSHS0V3VFS001","forward_patch":"--- original\n+++ modified\n@@ -35,18 +35,18 @@\n\n All asserted by automated tests below.\n\n-- [ ] The rendered debug-overlay label for a zone or scope shows the kind, the moniker, and the `(x, y)` pair — and no width × height suffix.\n-- [ ] The rendered debug-overlay label for a layer shows `layer:<name>` and nothing else (unchanged).\n-- [ ] Resizing the host element without moving it (e.g. content reflow that changes width without changing top-left) does NOT cause the overlay's React component to re-render or re-set its `rect` state. (Optimization side-effect of dropping the w/h legs of the equality check.)\n+- [x] The rendered debug-overlay label for a zone or scope shows the kind, the moniker, and the `(x, y)` pair — and no width × height suffix.\n+- [x] The rendered debug-overlay label for a layer shows `layer:<name>` and nothing else (unchanged).\n+- [x] Resizing the host element without moving it (e.g. content reflow that changes width without changing top-left) does NOT cause the overlay's React component to re-render or re-set its `rect` state. (Optimization side-effect of dropping the w/h legs of the equality check.)\n\n ## Tests\n\n ### Frontend — update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`\n\n-- [ ] `zone_label_has_no_dimensions_suffix` — render `<FocusDebugOverlay kind=\"zone\" label=\"ui:test\" hostRef={refToFixedRect}>` against a host at known `(x, y, w, h) = (10, 20, 100, 50)`; assert the rendered label text is exactly `zone:ui:test (10,20)` and does NOT contain `100×50` or `100x50`.\n-- [ ] `scope_label_has_no_dimensions_suffix` — same with `kind=\"scope\"`.\n-- [ ] `layer_label_unchanged` — `<FocusDebugOverlay kind=\"layer\" label=\"window\" ... />` renders `layer:window` exactly. Regression guard.\n-- [ ] `overlay_does_not_rerender_on_pure_dimension_change` — render the overlay, capture its render count via a probe; mutate the host's width and height but keep its top-left fixed; await an animation frame; assert the overlay did not commit a new render. (Pins the side-effect that drops w/h from the equality short-circuit.)\n+- [x] `zone_label_has_no_dimensions_suffix` — render `<FocusDebugOverlay kind=\"zone\" label=\"ui:test\" hostRef={refToFixedRect}>` against a host at known `(x, y, w, h) = (10, 20, 100, 50)`; assert the rendered label text is exactly `zone:ui:test (10,20)` and does NOT contain `100×50` or `100x50`.\n+- [x] `scope_label_has_no_dimensions_suffix` — same with `kind=\"scope\"`.\n+- [x] `layer_label_unchanged` — `<FocusDebugOverlay kind=\"layer\" label=\"window\" ... />` renders `layer:window` exactly. Regression guard.\n+- [x] `overlay_does_not_rerender_on_pure_dimension_change` — render the overlay, capture its render count via a probe; mutate the host's width and height but keep its top-left fixed; await an animation frame; assert the overlay did not commit a new render. (Pins the side-effect that drops w/h from the equality short-circuit.)\n\n Test command: `bun run test:browser focus-debug-overlay.browser.test.tsx` — all four pass alongside the existing tests in that file.\n\n@@ -54,3 +54,10 @@\n\n - Use `/tdd` — update the existing browser test first to expect labels without dimensions, watch it fail, edit the format string, watch it pass.\n - One file touched (`focus-debug-overlay.tsx`), one test file updated. Tiny fix.\n+\n+## Implementation Notes\n+\n+- Updated `kanban-app/ui/src/components/focus-debug-overlay.tsx`: dropped width/height from the label format (was line 192) and from the equality short-circuit (was lines 162-165).\n+- Updated `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`: added 4 new tests as specified, plus an `OverlayHarness` helper that direct-mounts `<FocusDebugOverlay>` against a fixed-position host. The render-count probe uses React's `<Profiler>` to assert no commits land after a pure dimension change.\n+- All 13 tests in `focus-debug-overlay.browser.test.tsx` pass. Full UI suite (1842 tests) green.\n+- TDD followed: 3 of 4 new tests went RED first (the layer test was always green since layers omit the rect entirely), then GREEN after the implementation edit.\n","reverse_patch":"--- original\n+++ modified\n@@ -35,18 +35,18 @@\n\n All asserted by automated tests below.\n\n-- [x] The rendered debug-overlay label for a zone or scope shows the kind, the moniker, and the `(x, y)` pair — and no width × height suffix.\n-- [x] The rendered debug-overlay label for a layer shows `layer:<name>` and nothing else (unchanged).\n-- [x] Resizing the host element without moving it (e.g. content reflow that changes width without changing top-left) does NOT cause the overlay's React component to re-render or re-set its `rect` state. (Optimization side-effect of dropping the w/h legs of the equality check.)\n+- [ ] The rendered debug-overlay label for a zone or scope shows the kind, the moniker, and the `(x, y)` pair — and no width × height suffix.\n+- [ ] The rendered debug-overlay label for a layer shows `layer:<name>` and nothing else (unchanged).\n+- [ ] Resizing the host element without moving it (e.g. content reflow that changes width without changing top-left) does NOT cause the overlay's React component to re-render or re-set its `rect` state. (Optimization side-effect of dropping the w/h legs of the equality check.)\n\n ## Tests\n\n ### Frontend — update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`\n\n-- [x] `zone_label_has_no_dimensions_suffix` — render `<FocusDebugOverlay kind=\"zone\" label=\"ui:test\" hostRef={refToFixedRect}>` against a host at known `(x, y, w, h) = (10, 20, 100, 50)`; assert the rendered label text is exactly `zone:ui:test (10,20)` and does NOT contain `100×50` or `100x50`.\n-- [x] `scope_label_has_no_dimensions_suffix` — same with `kind=\"scope\"`.\n-- [x] `layer_label_unchanged` — `<FocusDebugOverlay kind=\"layer\" label=\"window\" ... />` renders `layer:window` exactly. Regression guard.\n-- [x] `overlay_does_not_rerender_on_pure_dimension_change` — render the overlay, capture its render count via a probe; mutate the host's width and height but keep its top-left fixed; await an animation frame; assert the overlay did not commit a new render. (Pins the side-effect that drops w/h from the equality short-circuit.)\n+- [ ] `zone_label_has_no_dimensions_suffix` — render `<FocusDebugOverlay kind=\"zone\" label=\"ui:test\" hostRef={refToFixedRect}>` against a host at known `(x, y, w, h) = (10, 20, 100, 50)`; assert the rendered label text is exactly `zone:ui:test (10,20)` and does NOT contain `100×50` or `100x50`.\n+- [ ] `scope_label_has_no_dimensions_suffix` — same with `kind=\"scope\"`.\n+- [ ] `layer_label_unchanged` — `<FocusDebugOverlay kind=\"layer\" label=\"window\" ... />` renders `layer:window` exactly. Regression guard.\n+- [ ] `overlay_does_not_rerender_on_pure_dimension_change` — render the overlay, capture its render count via a probe; mutate the host's width and height but keep its top-left fixed; await an animation frame; assert the overlay did not commit a new render. (Pins the side-effect that drops w/h from the equality short-circuit.)\n\n Test command: `bun run test:browser focus-debug-overlay.browser.test.tsx` — all four pass alongside the existing tests in that file.\n\n@@ -54,10 +54,3 @@\n\n - Use `/tdd` — update the existing browser test first to expect labels without dimensions, watch it fail, edit the format string, watch it pass.\n - One file touched (`focus-debug-overlay.tsx`), one test file updated. Tiny fix.\n-\n-## Implementation Notes\n-\n-- Updated `kanban-app/ui/src/components/focus-debug-overlay.tsx`: dropped width/height from the label format (was line 192) and from the equality short-circuit (was lines 162-165).\n-- Updated `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`: added 4 new tests as specified, plus an `OverlayHarness` helper that direct-mounts `<FocusDebugOverlay>` against a fixed-position host. The render-count probe uses React's `<Profiler>` to assert no commits land after a pure dimension change.\n-- All 13 tests in `focus-debug-overlay.browser.test.tsx` pass. Full UI suite (1842 tests) green.\n-- TDD followed: 3 of 4 new tests went RED first (the layer test was always green since layers omit the rect entirely), then GREEN after the implementation edit.\n","transaction_id":null}
{"id":"01KQB17CP1KGSSZKQEN0ZAY96P","timestamp":"2026-04-28T21:51:48.417218Z","entity_type":"task","entity_id":"01KQAWGZMGNXCFSHS0V3VFS001","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -27,18 +27,18 @@\n\n All asserted by automated tests below.\n\n-- [ ] The rendered debug-overlay label for a zone or scope shows the kind, the moniker, and the `(x, y)` pair — and no width × height suffix.\n-- [ ] The rendered debug-overlay label for a layer shows `layer:<name>` and nothing else (unchanged).\n-- [ ] Resizing the host element without moving it (e.g. content reflow that changes width without changing top-left) does NOT cause the overlay's React component to re-render or re-set its `rect` state. (Optimization side-effect of dropping the w/h legs of the equality check.)\n+- [x] The rendered debug-overlay label for a zone or scope shows the kind, the moniker, and the `(x, y)` pair — and no width × height suffix.\n+- [x] The rendered debug-overlay label for a layer shows `layer:<name>` and nothing else (unchanged).\n+- [x] Resizing the host element without moving it (e.g. content reflow that changes width without changing top-left) does NOT cause the overlay's React component to re-render or re-set its `rect` state. (Optimization side-effect of dropping the w/h legs of the equality check.)\n\n ## Tests\n\n ### Frontend — update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`\n\n-- [ ] `zone_label_has_no_dimensions_suffix` — render `<FocusDebugOverlay kind=\"zone\" label=\"ui:test\" hostRef={refToFixedRect}>` against a host at known `(x, y, w, h) = (10, 20, 100, 50)`; assert the rendered label text is exactly `zone:ui:test (10,20)` and does NOT contain `100×50` or `100x50`.\n-- [ ] `scope_label_has_no_dimensions_suffix` — same with `kind=\"scope\"`.\n-- [ ] `layer_label_unchanged` — `<FocusDebugOverlay kind=\"layer\" label=\"window\" ... />` renders `layer:window` exactly. Regression guard.\n-- [ ] `overlay_does_not_rerender_on_pure_dimension_change` — render the overlay, capture its render count via a probe; mutate the host's width and height but keep its top-left fixed; await an animation frame; assert the overlay did not commit a new render. (Pins the side-effect that drops w/h from the equality short-circuit.)\n+- [x] `zone_label_has_no_dimensions_suffix` — render `<FocusDebugOverlay kind=\"zone\" label=\"ui:test\" hostRef={refToFixedRect}>` against a host at known `(x, y, w, h) = (10, 20, 100, 50)`; assert the rendered label text is exactly `zone:ui:test (10,20)` and does NOT contain `100×50` or `100x50`.\n+- [x] `scope_label_has_no_dimensions_suffix` — same with `kind=\"scope\"`.\n+- [x] `layer_label_unchanged` — `<FocusDebugOverlay kind=\"layer\" label=\"window\" ... />` renders `layer:window` exactly. Regression guard.\n+- [x] `overlay_does_not_rerender_on_pure_dimension_change` — render the overlay, capture its render count via a probe; mutate the host's width and height but keep its top-left fixed; await an animation frame; assert the overlay did not commit a new render. (Pins the side-effect that drops w/h from the equality short-circuit.)\n\n Test command: `bun run test:browser focus-debug-overlay.browser.test.tsx` — all four pass alongside the existing tests in that file.\n\n@@ -46,3 +46,10 @@\n\n - Use `/tdd` — update the existing browser test first to expect labels without dimensions, watch it fail, edit the format string, watch it pass.\n - One file touched (`focus-debug-overlay.tsx`), one test file updated. Tiny fix.\n+\n+## Implementation Notes\n+\n+- Updated `kanban-app/ui/src/components/focus-debug-overlay.tsx`: dropped width/height from the label format (was line 192) and from the equality short-circuit (was lines 162-165).\n+- Updated `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`: added 4 new tests as specified, plus an `OverlayHarness` helper that direct-mounts `<FocusDebugOverlay>` against a fixed-position host. The render-count probe uses React's `<Profiler>` to assert no commits land after a pure dimension change.\n+- All 13 tests in `focus-debug-overlay.browser.test.tsx` pass. Full UI suite (1842 tests) green.\n+- TDD followed: 3 of 4 new tests went RED first (the layer test was always green since layers omit the rect entirely), then GREEN after the implementation edit.\n","reverse_patch":"--- original\n+++ modified\n@@ -27,18 +27,18 @@\n\n All asserted by automated tests below.\n\n-- [x] The rendered debug-overlay label for a zone or scope shows the kind, the moniker, and the `(x, y)` pair — and no width × height suffix.\n-- [x] The rendered debug-overlay label for a layer shows `layer:<name>` and nothing else (unchanged).\n-- [x] Resizing the host element without moving it (e.g. content reflow that changes width without changing top-left) does NOT cause the overlay's React component to re-render or re-set its `rect` state. (Optimization side-effect of dropping the w/h legs of the equality check.)\n+- [ ] The rendered debug-overlay label for a zone or scope shows the kind, the moniker, and the `(x, y)` pair — and no width × height suffix.\n+- [ ] The rendered debug-overlay label for a layer shows `layer:<name>` and nothing else (unchanged).\n+- [ ] Resizing the host element without moving it (e.g. content reflow that changes width without changing top-left) does NOT cause the overlay's React component to re-render or re-set its `rect` state. (Optimization side-effect of dropping the w/h legs of the equality check.)\n\n ## Tests\n\n ### Frontend — update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`\n\n-- [x] `zone_label_has_no_dimensions_suffix` — render `<FocusDebugOverlay kind=\"zone\" label=\"ui:test\" hostRef={refToFixedRect}>` against a host at known `(x, y, w, h) = (10, 20, 100, 50)`; assert the rendered label text is exactly `zone:ui:test (10,20)` and does NOT contain `100×50` or `100x50`.\n-- [x] `scope_label_has_no_dimensions_suffix` — same with `kind=\"scope\"`.\n-- [x] `layer_label_unchanged` — `<FocusDebugOverlay kind=\"layer\" label=\"window\" ... />` renders `layer:window` exactly. Regression guard.\n-- [x] `overlay_does_not_rerender_on_pure_dimension_change` — render the overlay, capture its render count via a probe; mutate the host's width and height but keep its top-left fixed; await an animation frame; assert the overlay did not commit a new render. (Pins the side-effect that drops w/h from the equality short-circuit.)\n+- [ ] `zone_label_has_no_dimensions_suffix` — render `<FocusDebugOverlay kind=\"zone\" label=\"ui:test\" hostRef={refToFixedRect}>` against a host at known `(x, y, w, h) = (10, 20, 100, 50)`; assert the rendered label text is exactly `zone:ui:test (10,20)` and does NOT contain `100×50` or `100x50`.\n+- [ ] `scope_label_has_no_dimensions_suffix` — same with `kind=\"scope\"`.\n+- [ ] `layer_label_unchanged` — `<FocusDebugOverlay kind=\"layer\" label=\"window\" ... />` renders `layer:window` exactly. Regression guard.\n+- [ ] `overlay_does_not_rerender_on_pure_dimension_change` — render the overlay, capture its render count via a probe; mutate the host's width and height but keep its top-left fixed; await an animation frame; assert the overlay did not commit a new render. (Pins the side-effect that drops w/h from the equality short-circuit.)\n\n Test command: `bun run test:browser focus-debug-overlay.browser.test.tsx` — all four pass alongside the existing tests in that file.\n\n@@ -46,10 +46,3 @@\n\n - Use `/tdd` — update the existing browser test first to expect labels without dimensions, watch it fail, edit the format string, watch it pass.\n - One file touched (`focus-debug-overlay.tsx`), one test file updated. Tiny fix.\n-\n-## Implementation Notes\n-\n-- Updated `kanban-app/ui/src/components/focus-debug-overlay.tsx`: dropped width/height from the label format (was line 192) and from the equality short-circuit (was lines 162-165).\n-- Updated `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`: added 4 new tests as specified, plus an `OverlayHarness` helper that direct-mounts `<FocusDebugOverlay>` against a fixed-position host. The render-count probe uses React's `<Profiler>` to assert no commits land after a pure dimension change.\n-- All 13 tests in `focus-debug-overlay.browser.test.tsx` pass. Full UI suite (1842 tests) green.\n-- TDD followed: 3 of 4 new tests went RED first (the layer test was always green since layers omit the rect entirely), then GREEN after the implementation edit.\n"}]]}
{"id":"01KQB18EJ5H68BERAG5EXKGHDQ","timestamp":"2026-04-28T21:52:23.109074Z","op":"update","item_id":"01KQAWGZMGNXCFSHS0V3VFS001","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Focus debug overlay: drop width × height from label, keep only (x, y)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Focus debug overlay: drop width × height from label, keep only (x, y)'\n ---\n","transaction_id":null}
{"id":"01KQB18EJ5HCNQSCEX6X18GAC5","timestamp":"2026-04-28T21:52:23.109641Z","entity_type":"task","entity_id":"01KQAWGZMGNXCFSHS0V3VFS001","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQB1JGD23JDNC8MPW5RVX3HJ","timestamp":"2026-04-28T21:57:52.674860Z","op":"update","item_id":"01KQAWGZMGNXCFSHS0V3VFS001","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffdd80\n project: spatial-nav\n title: 'Focus debug overlay: drop width × height from label, keep only (x, y)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffdd80\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Focus debug overlay: drop width × height from label, keep only (x, y)'\n ---\n","transaction_id":null}
{"id":"01KQB1JGD3MZADZXFDYCN2Y1NR","timestamp":"2026-04-28T21:57:52.675386Z","entity_type":"task","entity_id":"01KQAWGZMGNXCFSHS0V3VFS001","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffdd80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffdd80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KQZ9BVP4HE75JDW0MTBHW09E","timestamp":"2026-05-06T18:38:52.100832Z","op":"update","item_id":"01KQAWGZMGNXCFSHS0V3VFS001","forward_patch":"--- original\n+++ modified\n@@ -60,4 +60,4 @@\n - Updated `kanban-app/ui/src/components/focus-debug-overlay.tsx`: dropped width/height from the label format (was line 192) and from the equality short-circuit (was lines 162-165).\n - Updated `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`: added 4 new tests as specified, plus an `OverlayHarness` helper that direct-mounts `<FocusDebugOverlay>` against a fixed-position host. The render-count probe uses React's `<Profiler>` to assert no commits land after a pure dimension change.\n - All 13 tests in `focus-debug-overlay.browser.test.tsx` pass. Full UI suite (1842 tests) green.\n-- TDD followed: 3 of 4 new tests went RED first (the layer test was always green since layers omit the rect entirely), then GREEN after the implementation edit.\n+- TDD followed: 3 of 4 new tests went RED first (the layer test was always green since layers omit the rect entirely), then GREEN after the implementation edit.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -60,4 +60,4 @@\n - Updated `kanban-app/ui/src/components/focus-debug-overlay.tsx`: dropped width/height from the label format (was line 192) and from the equality short-circuit (was lines 162-165).\n - Updated `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`: added 4 new tests as specified, plus an `OverlayHarness` helper that direct-mounts `<FocusDebugOverlay>` against a fixed-position host. The render-count probe uses React's `<Profiler>` to assert no commits land after a pure dimension change.\n - All 13 tests in `focus-debug-overlay.browser.test.tsx` pass. Full UI suite (1842 tests) green.\n-- TDD followed: 3 of 4 new tests went RED first (the layer test was always green since layers omit the rect entirely), then GREEN after the implementation edit.\n\\ No newline at end of file\n+- TDD followed: 3 of 4 new tests went RED first (the layer test was always green since layers omit the rect entirely), then GREEN after the implementation edit.\n","transaction_id":null}
{"id":"01KQZ9BVP8G7EHNFCSBH03KQ4H","timestamp":"2026-05-06T18:38:52.104122Z","entity_type":"task","entity_id":"01KQAWGZMGNXCFSHS0V3VFS001","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -52,4 +52,4 @@\n - Updated `kanban-app/ui/src/components/focus-debug-overlay.tsx`: dropped width/height from the label format (was line 192) and from the equality short-circuit (was lines 162-165).\n - Updated `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`: added 4 new tests as specified, plus an `OverlayHarness` helper that direct-mounts `<FocusDebugOverlay>` against a fixed-position host. The render-count probe uses React's `<Profiler>` to assert no commits land after a pure dimension change.\n - All 13 tests in `focus-debug-overlay.browser.test.tsx` pass. Full UI suite (1842 tests) green.\n-- TDD followed: 3 of 4 new tests went RED first (the layer test was always green since layers omit the rect entirely), then GREEN after the implementation edit.\n+- TDD followed: 3 of 4 new tests went RED first (the layer test was always green since layers omit the rect entirely), then GREEN after the implementation edit.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -52,4 +52,4 @@\n - Updated `kanban-app/ui/src/components/focus-debug-overlay.tsx`: dropped width/height from the label format (was line 192) and from the equality short-circuit (was lines 162-165).\n - Updated `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`: added 4 new tests as specified, plus an `OverlayHarness` helper that direct-mounts `<FocusDebugOverlay>` against a fixed-position host. The render-count probe uses React's `<Profiler>` to assert no commits land after a pure dimension change.\n - All 13 tests in `focus-debug-overlay.browser.test.tsx` pass. Full UI suite (1842 tests) green.\n-- TDD followed: 3 of 4 new tests went RED first (the layer test was always green since layers omit the rect entirely), then GREEN after the implementation edit.\n\\ No newline at end of file\n+- TDD followed: 3 of 4 new tests went RED first (the layer test was always green since layers omit the rect entirely), then GREEN after the implementation edit.\n"}]]}

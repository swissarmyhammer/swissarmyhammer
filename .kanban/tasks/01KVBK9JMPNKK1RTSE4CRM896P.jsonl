{"id":"01KVBK9JMQ62WFJEJBAFFTDW4M","timestamp":"2026-06-17T20:10:21.719567Z","op":"create","item_id":"01KVBK9JMPNKK1RTSE4CRM896P","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,30 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: a380\n+project: kv-prefix-reuse\n+title: 'End-to-end: sibling review turns reuse the pinned prefix on a recurrent model (no full reprocess)'\n+---\n+## What\n+Prove the whole path end-to-end: a primed validator prefix followed by multiple sibling file turns (fan-out) reuses the pinned prefix donor with rollback 0 — the `KV trim to common prefix returned false … invalidating cache` warning (`crates/llama-agent/src/queue.rs:2646`) never fires, and the cold full-reprocess fallback (`StreamingKvPrep::default()` after trim failure) is not taken.\n+\n+Build this against the deterministic scripted fake model from the `llama-coverage` project (no GPU/weights) so it runs in CI. Drive the streaming KV-prep path (`prepare_streaming_kv_cache`, `queue.rs:2517`) with a `max_rollback=64` store:\n+- Prime a pinned prefix (tokens `[prefix]`), then run two sibling turns whose prompts are `[prefix + tailA]` and `[prefix + tailB]` where each tail length > 64.\n+- Assert turn 2 and turn 3 select the pinned prime donor, the trim succeeds (`Ok(true)`, offset == prefix len, rollback 0), and the reused-token count equals the prefix length (no cold reprocess).\n+- Add a negative guard: with the OLD max-LCP-only behavior (max_rollback unbounded but simulate by inserting a longer sibling donor) the recurrent window would have rejected the sibling — assert the new selector avoids it.\n+\n+This is the regression test that ties the selector fix, model wiring, and prefix stability together. Model the harness on `crates/llama-agent/tests/integration/session_fork_real_model.rs` (which already exercises prefix-donor reuse) and the e2e reference `swissarmyhammer-tools/tests/integration/semantic_search_e2e.rs` pattern (real path → real result), substituting the scripted fake model for the GPU model.\n+\n+## Acceptance Criteria\n+- [ ] An integration test in `crates/llama-agent/tests/integration/` runs prime + ≥2 sibling turns through the real streaming KV-prep path with `max_rollback=64`.\n+- [ ] Asserts the pinned prime is chosen as donor on every sibling turn and the KV trim returns `Ok(true)` with rollback 0.\n+- [ ] Asserts no full-reprocess fallback occurs (e.g. instrument/observe that `StreamingKvPrep` carries the reused offset, not default).\n+- [ ] Test fails if the selector regresses to max-LCP-only.\n+\n+## Tests\n+- [ ] New file e.g. `crates/llama-agent/tests/integration/kv_prefix_reuse_recurrent.rs` using the scripted fake model.\n+- [ ] `cargo test -p llama-agent --test integration kv_prefix_reuse` green; full `cargo test -p llama-agent` green.\n+\n+## Workflow\n+- Use `/tdd` — write the e2e assertion first (RED against pre-fix behavior if run before the selector change lands), then confirm GREEN once dependencies are done.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,30 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: a380\n-project: kv-prefix-reuse\n-title: 'End-to-end: sibling review turns reuse the pinned prefix on a recurrent model (no full reprocess)'\n----\n-## What\n-Prove the whole path end-to-end: a primed validator prefix followed by multiple sibling file turns (fan-out) reuses the pinned prefix donor with rollback 0 — the `KV trim to common prefix returned false … invalidating cache` warning (`crates/llama-agent/src/queue.rs:2646`) never fires, and the cold full-reprocess fallback (`StreamingKvPrep::default()` after trim failure) is not taken.\n-\n-Build this against the deterministic scripted fake model from the `llama-coverage` project (no GPU/weights) so it runs in CI. Drive the streaming KV-prep path (`prepare_streaming_kv_cache`, `queue.rs:2517`) with a `max_rollback=64` store:\n-- Prime a pinned prefix (tokens `[prefix]`), then run two sibling turns whose prompts are `[prefix + tailA]` and `[prefix + tailB]` where each tail length > 64.\n-- Assert turn 2 and turn 3 select the pinned prime donor, the trim succeeds (`Ok(true)`, offset == prefix len, rollback 0), and the reused-token count equals the prefix length (no cold reprocess).\n-- Add a negative guard: with the OLD max-LCP-only behavior (max_rollback unbounded but simulate by inserting a longer sibling donor) the recurrent window would have rejected the sibling — assert the new selector avoids it.\n-\n-This is the regression test that ties the selector fix, model wiring, and prefix stability together. Model the harness on `crates/llama-agent/tests/integration/session_fork_real_model.rs` (which already exercises prefix-donor reuse) and the e2e reference `swissarmyhammer-tools/tests/integration/semantic_search_e2e.rs` pattern (real path → real result), substituting the scripted fake model for the GPU model.\n-\n-## Acceptance Criteria\n-- [ ] An integration test in `crates/llama-agent/tests/integration/` runs prime + ≥2 sibling turns through the real streaming KV-prep path with `max_rollback=64`.\n-- [ ] Asserts the pinned prime is chosen as donor on every sibling turn and the KV trim returns `Ok(true)` with rollback 0.\n-- [ ] Asserts no full-reprocess fallback occurs (e.g. instrument/observe that `StreamingKvPrep` carries the reused offset, not default).\n-- [ ] Test fails if the selector regresses to max-LCP-only.\n-\n-## Tests\n-- [ ] New file e.g. `crates/llama-agent/tests/integration/kv_prefix_reuse_recurrent.rs` using the scripted fake model.\n-- [ ] `cargo test -p llama-agent --test integration kv_prefix_reuse` green; full `cargo test -p llama-agent` green.\n-\n-## Workflow\n-- Use `/tdd` — write the e2e assertion first (RED against pre-fix behavior if run before the selector change lands), then confirm GREEN once dependencies are done.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KVBKHAJ26JNMNQS2QMGMMECD","timestamp":"2026-06-17T20:14:35.586644Z","op":"update","item_id":"01KVBK9JMPNKK1RTSE4CRM896P","forward_patch":"--- original\n+++ modified\n@@ -4,27 +4,32 @@\n position_column: todo\n position_ordinal: a380\n project: kv-prefix-reuse\n-title: 'End-to-end: sibling review turns reuse the pinned prefix on a recurrent model (no full reprocess)'\n+title: 'Real-model integration test: sibling review turns reuse the pinned prefix (no full reprocess)'\n ---\n ## What\n-Prove the whole path end-to-end: a primed validator prefix followed by multiple sibling file turns (fan-out) reuses the pinned prefix donor with rollback 0 — the `KV trim to common prefix returned false … invalidating cache` warning (`crates/llama-agent/src/queue.rs:2646`) never fires, and the cold full-reprocess fallback (`StreamingKvPrep::default()` after trim failure) is not taken.\n+Prove end-to-end on a REAL recurrent model that primed-prefix + sibling file turns reuse the pinned prefix donor with rollback 0: the `KV trim to common prefix returned false … invalidating cache` WARN (`crates/llama-agent/src/queue.rs:2646`) never fires, and the `streaming reusing N cached tokens` INFO (`queue.rs:2663`) does fire with N == prefix length.\n\n-Build this against the deterministic scripted fake model from the `llama-coverage` project (no GPU/weights) so it runs in CI. Drive the streaming KV-prep path (`prepare_streaming_kv_cache`, `queue.rs:2517`) with a `max_rollback=64` store:\n-- Prime a pinned prefix (tokens `[prefix]`), then run two sibling turns whose prompts are `[prefix + tailA]` and `[prefix + tailB]` where each tail length > 64.\n-- Assert turn 2 and turn 3 select the pinned prime donor, the trim succeeds (`Ok(true)`, offset == prefix len, rollback 0), and the reused-token count equals the prefix length (no cold reprocess).\n-- Add a negative guard: with the OLD max-LCP-only behavior (max_rollback unbounded but simulate by inserting a longer sibling donor) the recurrent window would have rejected the sibling — assert the new selector avoids it.\n+IMPORTANT (corrected from initial plan): the scripted fake model CANNOT exercise this — `prepare_streaming_kv_cache` (`queue.rs:2517`) is a private fn that takes a real `&mut LlamaContext` + `&LlamaModel` and the trim (`clear_kv_cache_seq`) is real FFI behavior the scripted `TextGenerator` double (operates above FFI) does not provide. There is no seam to inject a fake context, and `StreamingKvPrep` is private. So this must be a real-model, log-scraping test.\n\n-This is the regression test that ties the selector fix, model wiring, and prefix stability together. Model the harness on `crates/llama-agent/tests/integration/session_fork_real_model.rs` (which already exercises prefix-donor reuse) and the e2e reference `swissarmyhammer-tools/tests/integration/semantic_search_e2e.rs` pattern (real path → real result), substituting the scripted fake model for the GPU model.\n+Model it on the existing `crates/llama-agent/tests/integration/session_fork_real_model.rs` (`forked_sessions_reuse_full_parent_prefix_without_rollback`), gated the same way as the other real-model GPU tests (the self-hosted CI runner has a Metal GPU — see memory `ci-runner-has-gpu`; do NOT force CPU). Use the qwen hybrid model.\n+\n+- Prime a validator prefix (pinned), then run ≥2 sibling turns whose prompts share the prefix and diverge with tails > 64 tokens.\n+- Assert the queue log shows `streaming reusing <prefix_len> cached tokens` on each sibling turn and ZERO `KV trim to common prefix returned false` WARN lines.\n+- Also observe whether `skipping MTP this turn` (the draft-KV fallback, `queue.rs:2762`) fires on sibling turns, so the MTP-draft residual (tracked by the MTP draft-KV task) is measured, not silently shipped.\n+\n+The pure-selector assertion (prime chosen over sibling under max_rollback=64) is already covered by the keystone's unit tests — this task is the integration proof that the real FFI trim succeeds.\n\n ## Acceptance Criteria\n-- [ ] An integration test in `crates/llama-agent/tests/integration/` runs prime + ≥2 sibling turns through the real streaming KV-prep path with `max_rollback=64`.\n-- [ ] Asserts the pinned prime is chosen as donor on every sibling turn and the KV trim returns `Ok(true)` with rollback 0.\n-- [ ] Asserts no full-reprocess fallback occurs (e.g. instrument/observe that `StreamingKvPrep` carries the reused offset, not default).\n-- [ ] Test fails if the selector regresses to max-LCP-only.\n+- [ ] A gated real-model integration test in `crates/llama-agent/tests/integration/` runs prime + ≥2 sibling turns on the qwen hybrid model through the live queue.\n+- [ ] Asserts the `streaming reusing N cached tokens` INFO fires with N == prefix length on every sibling turn.\n+- [ ] Asserts ZERO `KV trim to common prefix returned false` WARN lines across the run.\n+- [ ] Records (asserts or logs) whether `skipping MTP this turn` fires, feeding the MTP-draft task.\n\n ## Tests\n-- [ ] New file e.g. `crates/llama-agent/tests/integration/kv_prefix_reuse_recurrent.rs` using the scripted fake model.\n-- [ ] `cargo test -p llama-agent --test integration kv_prefix_reuse` green; full `cargo test -p llama-agent` green.\n+- [ ] New gated test e.g. `crates/llama-agent/tests/integration/kv_prefix_reuse_recurrent.rs` (same GPU gate as `session_fork_real_model.rs`).\n+- [ ] Runs green on the GPU CI runner; full `cargo test -p llama-agent` green (test skips cleanly where the model/GPU is unavailable, per existing real-model test convention).\n+\n+## Depends on (prose — kanban depends_on edges are currently dropped by a known bug): keystone 01KVBK83218VM915ZTVZCKZ9VA, model-wiring 01KVBK8MTE2CJ7D4PS4X3181NX, tool-order 01KVBK8Z7WKZ96APERDP8DTHQ6, and benefits from the MTP-draft task.\n\n ## Workflow\n-- Use `/tdd` — write the e2e assertion first (RED against pre-fix behavior if run before the selector change lands), then confirm GREEN once dependencies are done.\n\\ No newline at end of file\n+- Use `/tdd` — run it against pre-fix behavior to see the WARN/full-reprocess (RED), then GREEN after the selector + wiring land.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -4,32 +4,27 @@\n position_column: todo\n position_ordinal: a380\n project: kv-prefix-reuse\n-title: 'Real-model integration test: sibling review turns reuse the pinned prefix (no full reprocess)'\n+title: 'End-to-end: sibling review turns reuse the pinned prefix on a recurrent model (no full reprocess)'\n ---\n ## What\n-Prove end-to-end on a REAL recurrent model that primed-prefix + sibling file turns reuse the pinned prefix donor with rollback 0: the `KV trim to common prefix returned false … invalidating cache` WARN (`crates/llama-agent/src/queue.rs:2646`) never fires, and the `streaming reusing N cached tokens` INFO (`queue.rs:2663`) does fire with N == prefix length.\n-\n-IMPORTANT (corrected from initial plan): the scripted fake model CANNOT exercise this — `prepare_streaming_kv_cache` (`queue.rs:2517`) is a private fn that takes a real `&mut LlamaContext` + `&LlamaModel` and the trim (`clear_kv_cache_seq`) is real FFI behavior the scripted `TextGenerator` double (operates above FFI) does not provide. There is no seam to inject a fake context, and `StreamingKvPrep` is private. So this must be a real-model, log-scraping test.\n-\n-Model it on the existing `crates/llama-agent/tests/integration/session_fork_real_model.rs` (`forked_sessions_reuse_full_parent_prefix_without_rollback`), gated the same way as the other real-model GPU tests (the self-hosted CI runner has a Metal GPU — see memory `ci-runner-has-gpu`; do NOT force CPU). Use the qwen hybrid model.\n+Prove the whole path end-to-end: a primed validator prefix followed by multiple sibling file turns (fan-out) reuses the pinned prefix donor with rollback 0 — the `KV trim to common prefix returned false … invalidating cache` warning (`crates/llama-agent/src/queue.rs:2646`) never fires, and the cold full-reprocess fallback (`StreamingKvPrep::default()` after trim failure) is not taken.\n\n-- Prime a validator prefix (pinned), then run ≥2 sibling turns whose prompts share the prefix and diverge with tails > 64 tokens.\n-- Assert the queue log shows `streaming reusing <prefix_len> cached tokens` on each sibling turn and ZERO `KV trim to common prefix returned false` WARN lines.\n-- Also observe whether `skipping MTP this turn` (the draft-KV fallback, `queue.rs:2762`) fires on sibling turns, so the MTP-draft residual (tracked by the MTP draft-KV task) is measured, not silently shipped.\n+Build this against the deterministic scripted fake model from the `llama-coverage` project (no GPU/weights) so it runs in CI. Drive the streaming KV-prep path (`prepare_streaming_kv_cache`, `queue.rs:2517`) with a `max_rollback=64` store:\n+- Prime a pinned prefix (tokens `[prefix]`), then run two sibling turns whose prompts are `[prefix + tailA]` and `[prefix + tailB]` where each tail length > 64.\n+- Assert turn 2 and turn 3 select the pinned prime donor, the trim succeeds (`Ok(true)`, offset == prefix len, rollback 0), and the reused-token count equals the prefix length (no cold reprocess).\n+- Add a negative guard: with the OLD max-LCP-only behavior (max_rollback unbounded but simulate by inserting a longer sibling donor) the recurrent window would have rejected the sibling — assert the new selector avoids it.\n\n-The pure-selector assertion (prime chosen over sibling under max_rollback=64) is already covered by the keystone's unit tests — this task is the integration proof that the real FFI trim succeeds.\n+This is the regression test that ties the selector fix, model wiring, and prefix stability together. Model the harness on `crates/llama-agent/tests/integration/session_fork_real_model.rs` (which already exercises prefix-donor reuse) and the e2e reference `swissarmyhammer-tools/tests/integration/semantic_search_e2e.rs` pattern (real path → real result), substituting the scripted fake model for the GPU model.\n\n ## Acceptance Criteria\n-- [ ] A gated real-model integration test in `crates/llama-agent/tests/integration/` runs prime + ≥2 sibling turns on the qwen hybrid model through the live queue.\n-- [ ] Asserts the `streaming reusing N cached tokens` INFO fires with N == prefix length on every sibling turn.\n-- [ ] Asserts ZERO `KV trim to common prefix returned false` WARN lines across the run.\n-- [ ] Records (asserts or logs) whether `skipping MTP this turn` fires, feeding the MTP-draft task.\n+- [ ] An integration test in `crates/llama-agent/tests/integration/` runs prime + ≥2 sibling turns through the real streaming KV-prep path with `max_rollback=64`.\n+- [ ] Asserts the pinned prime is chosen as donor on every sibling turn and the KV trim returns `Ok(true)` with rollback 0.\n+- [ ] Asserts no full-reprocess fallback occurs (e.g. instrument/observe that `StreamingKvPrep` carries the reused offset, not default).\n+- [ ] Test fails if the selector regresses to max-LCP-only.\n\n ## Tests\n-- [ ] New gated test e.g. `crates/llama-agent/tests/integration/kv_prefix_reuse_recurrent.rs` (same GPU gate as `session_fork_real_model.rs`).\n-- [ ] Runs green on the GPU CI runner; full `cargo test -p llama-agent` green (test skips cleanly where the model/GPU is unavailable, per existing real-model test convention).\n-\n-## Depends on (prose — kanban depends_on edges are currently dropped by a known bug): keystone 01KVBK83218VM915ZTVZCKZ9VA, model-wiring 01KVBK8MTE2CJ7D4PS4X3181NX, tool-order 01KVBK8Z7WKZ96APERDP8DTHQ6, and benefits from the MTP-draft task.\n+- [ ] New file e.g. `crates/llama-agent/tests/integration/kv_prefix_reuse_recurrent.rs` using the scripted fake model.\n+- [ ] `cargo test -p llama-agent --test integration kv_prefix_reuse` green; full `cargo test -p llama-agent` green.\n\n ## Workflow\n-- Use `/tdd` — run it against pre-fix behavior to see the WARN/full-reprocess (RED), then GREEN after the selector + wiring land.\n\\ No newline at end of file\n+- Use `/tdd` — write the e2e assertion first (RED against pre-fix behavior if run before the selector change lands), then confirm GREEN once dependencies are done.\n\\ No newline at end of file\n","transaction_id":null}

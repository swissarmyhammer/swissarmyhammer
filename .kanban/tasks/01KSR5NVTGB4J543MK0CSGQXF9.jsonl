{"id":"01KSR5NVTGV2QPYA1Z6A3FS6ZG","timestamp":"2026-05-28T20:51:08.240656Z","op":"create","item_id":"01KSR5NVTGB4J543MK0CSGQXF9","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,26 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '80'\n+project: ai-panel\n+title: Verify llama-agent uses Metal GPU on macOS (small-model test)\n+---\n+## Why\n+User ran qwen-27B in the kanban GUI and it was very slow; suspects it ran on CPU despite the Mac having Metal. We must not guess — prove with a test whether the model loads with Metal GPU offload.\n+\n+## Findings so far\n+- `llama-cpp-2` is built with `features = [\"metal\", \"sampler\"]` (workspace Cargo.toml) — Metal IS compiled in.\n+- `ModelManager::default_model_params` (crates/llama-agent/src/model.rs:132) sets `n_gpu_layers = i32::MAX` when `LLAMA_N_GPU_LAYERS` is unset (offload all). Nothing in the app sets it to 0 (only CI yaml + tests).\n+- So it *should* offload to Metal — but unverified at runtime.\n+\n+## What\n+Add a macOS-only integration test using the small `qwen-0.6b-test` model (`TEST_MODEL_REPO`/`TEST_MODEL_FILE`) that proves Metal is actually used:\n+- Build a `ModelManager` with `debug=true` so llama.cpp's C++ logs route to `tracing` (via `send_logs_to_tracing`).\n+- Capture tracing output during `load_model()` and assert it shows the Metal backend initializing AND layers offloaded to GPU (e.g. log lines containing \"Metal\" and \"offloaded … layers to GPU\", with offloaded == total).\n+- `#[cfg(target_os = \"macos\")]`. Lives in the `llama-embedding-serial` nextest group (llama-agent already is) so the global tracing subscriber is safe.\n+\n+## Acceptance Criteria\n+- [ ] A macOS test loads qwen-0.6b-test and asserts Metal is the active backend with all layers offloaded.\n+- [ ] No timing-based assertion (capture the backend/offload signal directly).\n+- [ ] If the test reveals we are NOT on Metal, fix the cause (build feature, env, or params) so it is.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,26 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '80'\n-project: ai-panel\n-title: Verify llama-agent uses Metal GPU on macOS (small-model test)\n----\n-## Why\n-User ran qwen-27B in the kanban GUI and it was very slow; suspects it ran on CPU despite the Mac having Metal. We must not guess — prove with a test whether the model loads with Metal GPU offload.\n-\n-## Findings so far\n-- `llama-cpp-2` is built with `features = [\"metal\", \"sampler\"]` (workspace Cargo.toml) — Metal IS compiled in.\n-- `ModelManager::default_model_params` (crates/llama-agent/src/model.rs:132) sets `n_gpu_layers = i32::MAX` when `LLAMA_N_GPU_LAYERS` is unset (offload all). Nothing in the app sets it to 0 (only CI yaml + tests).\n-- So it *should* offload to Metal — but unverified at runtime.\n-\n-## What\n-Add a macOS-only integration test using the small `qwen-0.6b-test` model (`TEST_MODEL_REPO`/`TEST_MODEL_FILE`) that proves Metal is actually used:\n-- Build a `ModelManager` with `debug=true` so llama.cpp's C++ logs route to `tracing` (via `send_logs_to_tracing`).\n-- Capture tracing output during `load_model()` and assert it shows the Metal backend initializing AND layers offloaded to GPU (e.g. log lines containing \"Metal\" and \"offloaded … layers to GPU\", with offloaded == total).\n-- `#[cfg(target_os = \"macos\")]`. Lives in the `llama-embedding-serial` nextest group (llama-agent already is) so the global tracing subscriber is safe.\n-\n-## Acceptance Criteria\n-- [ ] A macOS test loads qwen-0.6b-test and asserts Metal is the active backend with all layers offloaded.\n-- [ ] No timing-based assertion (capture the backend/offload signal directly).\n-- [ ] If the test reveals we are NOT on Metal, fix the cause (build feature, env, or params) so it is.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSR5QNC6YX9CGRC73Z1BTWYC","timestamp":"2026-05-28T20:52:07.174226Z","op":"update","item_id":"01KSR5NVTGB4J543MK0CSGQXF9","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n+position_column: doing\n position_ordinal: '80'\n project: ai-panel\n title: Verify llama-agent uses Metal GPU on macOS (small-model test)\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: todo\n position_ordinal: '80'\n project: ai-panel\n title: Verify llama-agent uses Metal GPU on macOS (small-model test)\n","transaction_id":null}
{"id":"01KSR64RWAJKGY4E71R5V6EVKZ","timestamp":"2026-05-28T20:59:16.746446Z","op":"update","item_id":"01KSR5NVTGB4J543MK0CSGQXF9","forward_patch":"--- original\n+++ modified\n@@ -6,21 +6,29 @@\n project: ai-panel\n title: Verify llama-agent uses Metal GPU on macOS (small-model test)\n ---\n-## Why\n-User ran qwen-27B in the kanban GUI and it was very slow; suspects it ran on CPU despite the Mac having Metal. We must not guess — prove with a test whether the model loads with Metal GPU offload.\n+## DONE (2026-05-28) — Metal CONFIRMED in use\n+\n+The user's CPU suspicion was wrong, now proven not guessed. New macOS-only test `crates/llama-agent/tests/integration/metal_gpu.rs::qwen_small_model_loads_on_metal_gpu` loads `qwen-0.6b-test` and the captured llama.cpp logs show:\n+- `ggml_metal_device_init: GPU name: MTL0 (Apple M5 Max)`\n+- `load_tensors: offloaded 29/29 layers to GPU`\n\n-## Findings so far\n-- `llama-cpp-2` is built with `features = [\"metal\", \"sampler\"]` (workspace Cargo.toml) — Metal IS compiled in.\n-- `ModelManager::default_model_params` (crates/llama-agent/src/model.rs:132) sets `n_gpu_layers = i32::MAX` when `LLAMA_N_GPU_LAYERS` is unset (offload all). Nothing in the app sets it to 0 (only CI yaml + tests).\n-- So it *should* offload to Metal — but unverified at runtime.\n+So the model runs fully on the Metal GPU. The GUI slowness on the 27B was NOT CPU — it was the 28k-token prompt (10 kanban MCP tool schemas rendered into a 108k-char prompt) on a 27B with a 262k context window (prefill cost), plus Qwen3 thinking mode.\n+\n+### How the test works\n+- Builds a `ModelManager` with `debug=true` (un-suppresses llama.cpp native logging).\n+- Those ggml/llama logs go straight to the C `stderr` fd — NOT through the `tracing` bridge (an in-process tracing subscriber captured nothing, which is why the first cut failed even though Metal was clearly working). So the test redirects fd 2 to a temp file across the load (`StderrCapture` RAII via `libc::dup`/`dup2`) and reads it back.\n+- Hard assertion: parse `offloaded N/N layers to GPU` and require N==total>0 (every layer on the GPU — on macOS the only GPU backend is Metal, so this == running on Metal). This line fires on every load. Also asserts \"metal\" appears (the ggml device-init line; reliable under nextest's process-per-test).\n+- No timing assertion.\n+- Added `libc` dev-dep.\n+\n+### Verification\n+- `cargo test -p llama-agent --test agent_tests integration::metal_gpu` → ok.\n+- `cargo nextest run -E 'test(integration::metal_gpu)'` → 1 passed (2.2s). clippy clean.\n\n-## What\n-Add a macOS-only integration test using the small `qwen-0.6b-test` model (`TEST_MODEL_REPO`/`TEST_MODEL_FILE`) that proves Metal is actually used:\n-- Build a `ModelManager` with `debug=true` so llama.cpp's C++ logs route to `tracing` (via `send_logs_to_tracing`).\n-- Capture tracing output during `load_model()` and assert it shows the Metal backend initializing AND layers offloaded to GPU (e.g. log lines containing \"Metal\" and \"offloaded … layers to GPU\", with offloaded == total).\n-- `#[cfg(target_os = \"macos\")]`. Lives in the `llama-embedding-serial` nextest group (llama-agent already is) so the global tracing subscriber is safe.\n+### Why it found nothing to fix\n+Metal was already correctly enabled: `llama-cpp-2` built with `features=[\"metal\",\"sampler\"]`, and `default_model_params` defaults `n_gpu_layers` to `i32::MAX` (offload all) when `LLAMA_N_GPU_LAYERS` is unset. The test now guards that this stays true.\n\n-## Acceptance Criteria\n-- [ ] A macOS test loads qwen-0.6b-test and asserts Metal is the active backend with all layers offloaded.\n-- [ ] No timing-based assertion (capture the backend/offload signal directly).\n-- [ ] If the test reveals we are NOT on Metal, fix the cause (build feature, env, or params) so it is.\n\\ No newline at end of file\n+### Acceptance criteria\n+- [x] macOS test loads qwen-0.6b-test and asserts Metal active with all layers offloaded (29/29).\n+- [x] No timing-based assertion (parses the offload-count log line directly).\n+- [x] Investigated the \"is it CPU?\" concern — it is NOT; Metal is in use, nothing to fix there. (Real slowness cause = prompt size, tracked separately.)\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -6,29 +6,21 @@\n project: ai-panel\n title: Verify llama-agent uses Metal GPU on macOS (small-model test)\n ---\n-## DONE (2026-05-28) — Metal CONFIRMED in use\n+## Why\n+User ran qwen-27B in the kanban GUI and it was very slow; suspects it ran on CPU despite the Mac having Metal. We must not guess — prove with a test whether the model loads with Metal GPU offload.\n\n-The user's CPU suspicion was wrong, now proven not guessed. New macOS-only test `crates/llama-agent/tests/integration/metal_gpu.rs::qwen_small_model_loads_on_metal_gpu` loads `qwen-0.6b-test` and the captured llama.cpp logs show:\n-- `ggml_metal_device_init: GPU name: MTL0 (Apple M5 Max)`\n-- `load_tensors: offloaded 29/29 layers to GPU`\n+## Findings so far\n+- `llama-cpp-2` is built with `features = [\"metal\", \"sampler\"]` (workspace Cargo.toml) — Metal IS compiled in.\n+- `ModelManager::default_model_params` (crates/llama-agent/src/model.rs:132) sets `n_gpu_layers = i32::MAX` when `LLAMA_N_GPU_LAYERS` is unset (offload all). Nothing in the app sets it to 0 (only CI yaml + tests).\n+- So it *should* offload to Metal — but unverified at runtime.\n\n-So the model runs fully on the Metal GPU. The GUI slowness on the 27B was NOT CPU — it was the 28k-token prompt (10 kanban MCP tool schemas rendered into a 108k-char prompt) on a 27B with a 262k context window (prefill cost), plus Qwen3 thinking mode.\n+## What\n+Add a macOS-only integration test using the small `qwen-0.6b-test` model (`TEST_MODEL_REPO`/`TEST_MODEL_FILE`) that proves Metal is actually used:\n+- Build a `ModelManager` with `debug=true` so llama.cpp's C++ logs route to `tracing` (via `send_logs_to_tracing`).\n+- Capture tracing output during `load_model()` and assert it shows the Metal backend initializing AND layers offloaded to GPU (e.g. log lines containing \"Metal\" and \"offloaded … layers to GPU\", with offloaded == total).\n+- `#[cfg(target_os = \"macos\")]`. Lives in the `llama-embedding-serial` nextest group (llama-agent already is) so the global tracing subscriber is safe.\n\n-### How the test works\n-- Builds a `ModelManager` with `debug=true` (un-suppresses llama.cpp native logging).\n-- Those ggml/llama logs go straight to the C `stderr` fd — NOT through the `tracing` bridge (an in-process tracing subscriber captured nothing, which is why the first cut failed even though Metal was clearly working). So the test redirects fd 2 to a temp file across the load (`StderrCapture` RAII via `libc::dup`/`dup2`) and reads it back.\n-- Hard assertion: parse `offloaded N/N layers to GPU` and require N==total>0 (every layer on the GPU — on macOS the only GPU backend is Metal, so this == running on Metal). This line fires on every load. Also asserts \"metal\" appears (the ggml device-init line; reliable under nextest's process-per-test).\n-- No timing assertion.\n-- Added `libc` dev-dep.\n-\n-### Verification\n-- `cargo test -p llama-agent --test agent_tests integration::metal_gpu` → ok.\n-- `cargo nextest run -E 'test(integration::metal_gpu)'` → 1 passed (2.2s). clippy clean.\n-\n-### Why it found nothing to fix\n-Metal was already correctly enabled: `llama-cpp-2` built with `features=[\"metal\",\"sampler\"]`, and `default_model_params` defaults `n_gpu_layers` to `i32::MAX` (offload all) when `LLAMA_N_GPU_LAYERS` is unset. The test now guards that this stays true.\n-\n-### Acceptance criteria\n-- [x] macOS test loads qwen-0.6b-test and asserts Metal active with all layers offloaded (29/29).\n-- [x] No timing-based assertion (parses the offload-count log line directly).\n-- [x] Investigated the \"is it CPU?\" concern — it is NOT; Metal is in use, nothing to fix there. (Real slowness cause = prompt size, tracked separately.)\n\\ No newline at end of file\n+## Acceptance Criteria\n+- [ ] A macOS test loads qwen-0.6b-test and asserts Metal is the active backend with all layers offloaded.\n+- [ ] No timing-based assertion (capture the backend/offload signal directly).\n+- [ ] If the test reveals we are NOT on Metal, fix the cause (build feature, env, or params) so it is.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSR64X75HYSWJN1264NRHQ2C","timestamp":"2026-05-28T20:59:21.189411Z","op":"update","item_id":"01KSR5NVTGB4J543MK0CSGQXF9","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffc580\n project: ai-panel\n title: Verify llama-agent uses Metal GPU on macOS (small-model test)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffc580\n+position_column: doing\n+position_ordinal: '80'\n project: ai-panel\n title: Verify llama-agent uses Metal GPU on macOS (small-model test)\n ---\n","transaction_id":null}

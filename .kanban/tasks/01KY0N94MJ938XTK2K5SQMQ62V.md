---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01ky10e2hc0cgahz0mancrbw5c
  text: 'Batch /finish #review picked this task (first READY in scope; review column empty). Iteration 1: delegating to /implement. Successor ^1tt5pa6 unblocks when this lands. Note from ^jn2wjd5''s run: keep test gates targeted (cargo nextest -p model-loader -p swissarmyhammer-embedding); never let a gate wander into real-model suites.'
  timestamp: 2026-07-21T00:15:27.276523+00:00
- actor: claude-code
  id: 01ky10wqnfr80yjh25fk3na36z
  text: |-
    Picked up; research done. Verified hf-hub 0.4.3 source: Progress trait is async (init(size,filename)/update(delta)/finish), download_with_progress ALWAYS downloads (no cache check — get() does cache-first then download()). Design decisions:

    1. model-loader src/observer.rs: DownloadEvent (private fields + getters + constructor, Debug/Clone/PartialEq/Eq) + DownloadObserver = Arc<dyn Fn(DownloadEvent)+Send+Sync>; exported from lib.rs.
    2. retry.rs: ObserverProgress adapter (Clone; Arc<Mutex<state>>; emits under lock so concurrent chunk tasks can't invert event order → monotonic guarantee). Observer path mirrors get()'s cache-first behavior via hf_hub::Cache::from_env() pre-check (download_with_progress would otherwise re-download cached blobs every load). None path stays repo_api.get() — byte-identical.
    3. huggingface.rs: switch ApiBuilder::new() → ApiBuilder::from_env() (card-sanctioned; honors HF_ENDPOINT/HF_HOME, and makes the retry.rs cache pre-check agree with the Api's cache dir). Observer param threaded through download_hf_file / load_huggingface_model_with_path(_and_folder) / multipart fns as Option<&DownloadObserver>.
    4. loader.rs: ModelResolver gains private observer field + with_download_observer(self, obs) builder; resolve() signature unchanged → llama-agent/tests/examples unaffected.
    5. llama-embedding EmbeddingModel: with_download_observer builder (its downloads happen at load_model time via ModelResolver).
    6. Embedder: with_download_observer(name, observer) is a CONSTRUCTOR variant, not an instance builder — justification: on macOS the ANE path downloads model files + tokenizer.json during from_model_name (build_ane_model resolves eagerly), so an instance builder applied after construction could never observe them. from_model_name delegates to the same internal fn with None.

    Tests: retry.rs unit tests for the adapter mapping (no network); tests/download_progress_test.rs with a hand-rolled tokio TcpListener HTTP responder (serves metadata Range bytes=0-0 → 206 with etag/x-repo-commit/Content-Range, then chunk GETs), HF_ENDPOINT+HF_HOME set per test (nextest = process per test). Edition 2021 so env::set_var is safe-fn.
  timestamp: 2026-07-21T00:23:27.663724+00:00
- actor: claude-code
  id: 01ky11dp31s2sf5ekeryb64aar
  text: |-
    Implementation landed (TDD: tests first, watched RED via missing ObserverProgress/observer module, then GREEN).

    Changes:
    - crates/model-loader/src/observer.rs (new): DownloadEvent (private fields, constructor + file()/downloaded_bytes()/total_bytes() getters, Debug/Clone/PartialEq/Eq) + DownloadObserver type alias; exported from lib.rs.
    - crates/model-loader/src/retry.rs: ObserverProgress adapter (Clone; shared Arc<Mutex<state>>; emits under the lock so hf-hub's cloned parallel-chunk handles stay monotonic; init → start event, update → accumulate, finish → forced downloaded==total). download_with_retry/_internal gained Option<&DownloadObserver>; new fetch_file helper — None path is exactly repo_api.get (byte-identical), observer path mirrors get's cache-first contract via hf_hub::Cache::from_env() then download_with_progress (download_with_progress always re-downloads, so the pre-check prevents attached observers from re-downloading cached blobs).
    - crates/model-loader/src/huggingface.rs: observer threaded through download_hf_file / load_huggingface_model_with_path(_and_folder); ApiBuilder::new() → from_env() (card-sanctioned; honors HF_ENDPOINT/HF_HOME and keeps the cache pre-check agreeing with the Api's cache dir).
    - crates/model-loader/src/multipart.rs: both fns thread the observer per part/file.
    - crates/model-loader/src/loader.rs: ModelResolver gained private download_observer field + with_download_observer builder; resolve() signature unchanged (llama-agent/tests/examples untouched); manual Debug.
    - crates/llama-embedding/src/model.rs: EmbeddingModel.with_download_observer builder; load_model attaches it to the resolver.
    - crates/swissarmyhammer-embedding: Embedder::with_download_observer(name, observer) constructor variant (ANE path downloads during from_model_name, so a post-construction builder could never observe those); from_model_name delegates to shared build(name, None); ANE path passes observer to resolver + tokenizer.json download (TOKENIZER_FILENAME const); lib.rs re-exports DownloadEvent/DownloadObserver.

    Tests: tests/download_progress_test.rs — hand-rolled tokio TcpListener fake hub (206 + etag/x-repo-commit/content-range per Range header), HF_ENDPOINT+HF_HOME per test, #[serial] for plain-cargo-test safety; observer test asserts start(0,total)/monotonic/final==total/full filename; None test asserts identical bytes. retry.rs unit tests: adapter init/update/finish mapping, finish-forces-total, clones-share-state. observer.rs unit tests for getters/alias.

    Gates: cargo nextest run -p model-loader -p swissarmyhammer-embedding → 186/186 passed (new integration tests ~50ms each); cargo clippy -p model-loader -p swissarmyhammer-embedding --all-targets -- -D warnings clean; cargo check --workspace --all-targets clean; cargo fmt applied. double-check agent running.
  timestamp: 2026-07-21T00:32:43.105140+00:00
- actor: claude-code
  id: 01ky11ywmfk9nhpt84w4s6x380
  text: |-
    really-done complete: verification gates green (nextest -p model-loader -p swissarmyhammer-embedding 186/186; clippy -D warnings clean; cargo check --workspace --all-targets clean; fmt applied) and adversarial double-check verdict: PASS.

    Double-check independently verified against vendored hf-hub 0.4.3 source: fetch_file's Cache::from_env() pre-check is check-for-check identical to ApiRepo::get's own cache lookup given the ApiBuilder::from_env() builders in huggingface.rs (invariant documented in-code); None path literally unchanged (repo_api.get); no missed call sites of the six changed signatures anywhere in the workspace; non-macOS builds unaffected; clippy also clean for llama-embedding. Two residual notes judged non-blocking and both documented in code: (a) a hypothetical future external caller building an ApiRepo with a custom cache dir would diverge from the from_env pre-check (no such caller exists); (b) across retry attempts the adapter resets and jumps to the resumed offset — monotonicity holds within each download attempt, which is what the contract specifies.

    All subtasks/acceptance/tests checked off. Leaving in doing for /review. Successor ^1tt5pa6 (MCP notifications/progress) can build on: model_loader::{DownloadEvent, DownloadObserver}, ModelResolver::with_download_observer, EmbeddingModel::with_download_observer, Embedder::with_download_observer(name, observer) (re-exported from swissarmyhammer_embedding).
  timestamp: 2026-07-21T00:42:06.863482+00:00
- actor: claude-code
  id: 01ky1494me2xjcw4ypj311bysa
  text: |-
    Review-findings pass: pulled review→doing, fixed all 27 findings at root, flipped every checkbox to [x] (resolution notes inline on the non-obvious ones). Summary by cluster:

    1. # Errors docs — added to EVERY public Result-returning fn in the touched files (whole-file sweep, not just cited lines): download_hf_file / load_huggingface_model_with_path(_and_folder) (huggingface.rs), download_with_retry (retry.rs), ModelResolver::resolve (loader.rs), download_multi_part_model / download_folder_model (multipart.rs), EmbeddingModel::new (llama-embedding), Embedder::default / from_model_name / with_download_observer (swissarmyhammer-embedding).
    2. Debug + docs — manual Debug for EmbeddingModel (config + observer presence; backend/inner hold non-Debug llama.cpp handles) and Embedder (backend name via EmbedderBackend::name(), model_name, max_seq, normalize); EmbedderError enum + all variants documented.
    3. Dead fns — grep first, per instructions: detect_multi_part_base HAS a production caller (crate::detection auto-detect; finding premise wrong) → wired correctly as pub(crate) with the caller documented. is_retriable_error truly dead (production retriability = ModelError::is_retriable/should_stop_retrying via RetryManager; llama_common::classify_error_for_retry was a near-duplicate) → REMOVED plus its 6 unit tests (285→279 tests; sanctioned removal, no #[allow(dead_code)]).
    4. Duplication/literals — multipart.rs unified into private download_all(items, item_label, collection_label, ...); loader.rs HF_CACHE_{HUB_DIR,MODELS_PREFIX,SNAPSHOTS_DIR} consts + hf_snapshots_dir() helper (path built once; also dropped the vestigial unused ApiBuilder client in check_hf_cache_exists); retry.rs RETRIABLE_HTTP_CODES single-source table used by format_download_error; embedder.rs EmbedderBackend::name(), CHARS_PER_TOKEN=3 (fixed the stale "~4 chars/token" comment), MLPACKAGE_EXTENSION (macos-gated).
    5. Nesting — auto_detect_model_file and check_cache_snapshots flattened with let-else/continue + extracted snapshot_has_any_model_file / snapshot_has_named_file helpers.

    Gates: cargo fmt applied; cargo clippy -p model-loader -p swissarmyhammer-embedding -p llama-embedding --all-targets -- -D warnings clean; cargo nextest run (same 3 packages) 279/279 passed incl. the offline fake-hub download_progress tests; cargo check --workspace --all-targets clean. Adversarial double-check agent dispatched; task stays in doing.
  timestamp: 2026-07-21T01:22:39.886451+00:00
position_column: doing
position_ordinal: '8280'
title: 'model-loader: download-progress observer seam, threaded through the embedding loader'
---
## What

Model downloads are completely silent today: `swissarmyhammer_embedding::Embedder::default()` / `TextEmbedder::load()` (`crates/swissarmyhammer-embedding/src/embedder.rs`) call `model_loader::download_hf_file` / `load_huggingface_model_with_path` / `load_huggingface_model_with_path_and_folder`, which funnel into `download_with_retry` (`crates/model-loader/src/retry.rs`) → hf-hub `ApiRepo::download` with no progress hook. A first-run multi-hundred-MB-to-GB download produces zero events for the minutes it takes. This card adds the observer seam; a follow-up card (`depends_on` this one) surfaces it as MCP `notifications/progress` to keep clients alive.

hf-hub 0.4.3 (workspace `Cargo.toml`) already supports this: `ApiRepo::download_with_progress<P: hf_hub::api::tokio::Progress + Clone + Send + Sync>` and `ApiBuilder::with_endpoint` (for offline tests).

Implementation shape:

1. **`crates/model-loader`**: define `DownloadEvent { file: String, downloaded_bytes: u64, total_bytes: u64 }` and `DownloadObserver = Arc<dyn Fn(DownloadEvent) + Send + Sync>` (plain callback — no async in the observer; hf-hub's `Progress` impl adapter accumulates bytes and invokes it). Thread an `Option<DownloadObserver>` through `download_with_retry` / `download_with_retry_internal` (`src/retry.rs`, switching the inner call to `download_with_progress`), `download_multi_part_model` / `download_folder_model` (`src/multipart.rs`), and `download_hf_file` / `load_huggingface_model_with_path` / `load_huggingface_model_with_path_and_folder` (`src/huggingface.rs`); export the new types from `src/lib.rs`. `None` must be byte-identical to today's behavior. Emit an event at download start (downloaded=0, total from hf-hub's init) and per chunk update — never throttle away the final event (downloaded == total).
2. **`crates/swissarmyhammer-embedding/src/embedder.rs`**: give `Embedder` a way to attach the observer (e.g. `Embedder::with_download_observer(observer)` builder stored on the struct), forwarded into every `model_loader` download call in its load path (tokenizer + model files). Default (no observer) unchanged.

Note for tests: `download_with_retry` builds its own hf-hub `Api`; to point it at a local server either switch to `ApiBuilder::from_env()` (honors `HF_ENDPOINT`) or take an endpoint override — nextest runs each test in its own process, so setting `HF_ENDPOINT` per test is safe.

Subtasks:
- [x] `DownloadEvent` + `DownloadObserver` types in model-loader, exported from `src/lib.rs`
- [x] Thread `Option<DownloadObserver>` through `retry.rs` (use `download_with_progress` with an adapter implementing `hf_hub::api::tokio::Progress`), `multipart.rs`, `huggingface.rs`
- [x] `Embedder::with_download_observer` in swissarmyhammer-embedding forwarding to all model_loader calls in the load path
- [x] Offline integration test against a local HTTP server via `HF_ENDPOINT`/endpoint override
- [x] `None`-observer regression: existing callers compile and behave unchanged (default param propagation, no signature break left unfixed)

## Acceptance Criteria
- [x] Downloading a file through `download_with_retry` with an observer yields at least a start event and a final event with `downloaded_bytes == total_bytes`, with byte counts monotonically non-decreasing
- [x] `DownloadEvent.file` carries the full filename, untruncated
- [x] Passing `None` produces zero observer calls and the same download result as before the change (all existing call sites updated, workspace builds green)
- [x] `Embedder::with_download_observer` forwards events for every file its load path downloads (model file(s) and tokenizer.json)
- [x] model-loader gains no new non-dev dependencies beyond what hf-hub already provides

## Tests
- [x] Integration test `crates/model-loader/tests/download_progress_test.rs`: serve a small fake model file from a local HTTP server (tiny hyper/axum dev-dependency or a hand-rolled tokio `TcpListener` responder), point hf-hub at it via `HF_ENDPOINT` (or the endpoint override), download with an observer, assert start/updates/final event sequence and monotonic bytes; second test asserts `None` observer downloads identically with zero events
- [x] Unit test for the hf-hub `Progress`-trait adapter in `crates/model-loader/src/retry.rs` (init/update/finish → DownloadEvent mapping) — no network
- [x] Run: `cargo nextest run -p model-loader` and `cargo nextest run -p swissarmyhammer-embedding` — green, under 10s per unit test (local server only, never the real HF hub in tests)

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass. #mcp #progress #review

## Review Findings (2026-07-20 19:45)

- [x] `crates/llama-embedding/src/model.rs:94` — Public type `EmbeddingModel` with non-empty representation does not implement Debug. The documentation rule requires Debug for all public types with non-empty representation. Implement Debug for EmbeddingModel. Since some fields (LlamaBackend, Mutex) may not implement Debug, use a manual impl that prints selected fields, similar to ModelResolver's Debug impl in loader.rs.
- [x] `crates/llama-embedding/src/model.rs:110` — Public async function `EmbeddingModel::new` returns Result but lacks # Errors documentation. Add # Errors section documenting what errors can occur during backend initialization.
- [x] `crates/model-loader/src/huggingface.rs:19` — Public function `download_hf_file` returns Result<PathBuf, ModelError> but its doc comment lacks an # Errors section documenting when it fails. The documentation rule requires errors to be documented. Add # Errors section documenting the ModelError variants that can be returned (network errors, auth errors, missing files, etc.).
- [x] `crates/model-loader/src/huggingface.rs:35` — Public function `load_huggingface_model_with_path` returns Result but lacks # Errors documentation. Add # Errors section documenting potential ModelError variants.
- [x] `crates/model-loader/src/huggingface.rs:48` — Public function `load_huggingface_model_with_path_and_folder` returns Result but lacks # Errors documentation. Add # Errors section documenting potential ModelError variants.
- [x] `crates/model-loader/src/loader.rs:52` — Public method `ModelResolver::resolve` returns Result but lacks # Errors documentation. Add # Errors section documenting when resolution can fail (invalid config, download failures, file not found, etc.).
- [x] `crates/model-loader/src/loader.rs:186` — Function contains 4 levels of nesting (while → if → if → if), making control flow difficult to trace. Extract the nested bf16/model classification into a helper method or use a separate loop to reduce nesting depth. — RESOLVED: flattened `auto_detect_model_file` with `let-else` + `continue` early exits (max 1 level inside the loop).
- [x] `crates/model-loader/src/loader.rs:203` — The string literal "models--" appears twice in check_hf_cache_exists(): once at line 203 in the HF_HOME-based cache path and again at line 213 in the default home directory-based cache path. Both construct the HuggingFace cache directory naming scheme (converting repo slashes to dashes and prefixing with "models--"). If the HF cache directory structure changes, both locations must be updated together. This should be a named constant. Define const HF_CACHE_MODELS_PREFIX: &str = "models--"; and use it in a helper function or at both call sites to avoid repeating the prefix pattern. — RESOLVED: `HF_CACHE_MODELS_PREFIX` const + shared `hf_snapshots_dir()` helper; the path is now built exactly once.
- [x] `crates/model-loader/src/loader.rs:205` — The string literal "hub" appears twice in check_hf_cache_exists(): once at line 205 in the HF_HOME-based cache path, and again at line 215 in the default home directory-based cache path. Both are constructing the HuggingFace cache directory structure. If the cache hierarchy changes, both literals must be updated. This should be a named constant. Define const HF_CACHE_HUB_DIR: &str = "hub"; and replace both occurrences with .join(HF_CACHE_HUB_DIR). — RESOLVED via `HF_CACHE_HUB_DIR` + `hf_snapshots_dir()`.
- [x] `crates/model-loader/src/loader.rs:206` — The string literal "snapshots" appears twice in check_hf_cache_exists(): once at line 206 in the HF_HOME-based cache path, and again at line 216 in the default home directory-based cache path. Both construct the same HuggingFace cache structure. When the cache hierarchy changes, both literals must be updated together. Define const HF_CACHE_SNAPSHOTS_DIR: &str = "snapshots"; and replace both occurrences with .join(HF_CACHE_SNAPSHOTS_DIR). — RESOLVED via `HF_CACHE_SNAPSHOTS_DIR` + `hf_snapshots_dir()`.
- [x] `crates/model-loader/src/loader.rs:254` — Function contains 4 levels of nesting (if → while → if → if), making branching logic hard to follow. Extract inner conditional blocks into a separate helper method or use early continue statements to reduce nesting. — RESOLVED: `check_cache_snapshots` flattened with `let-else`/`continue` and the per-snapshot checks extracted into `snapshot_has_any_model_file` / `snapshot_has_named_file`.
- [x] `crates/model-loader/src/multipart.rs:8` — Public function `download_multi_part_model` returns Result but lacks # Errors documentation. Add # Errors section documenting potential ModelError variants.
- [x] `crates/model-loader/src/multipart.rs:23` — download_multi_part_model and download_folder_model are near-verbatim duplicates differing only in loop variable names (part vs file) and message strings. Both implement identical download-and-collect logic that should be extracted into a single parameterized function. Extract a shared async fn download_items<S: AsRef<str>>(items: &[String], item_label: &str, context: &str, ...) that accepts the collection and message prefixes, then have download_multi_part_model and download_folder_model call it with their respective arguments. Eliminates drift risk on the retry loop and logging. — RESOLVED: shared private `download_all(items, item_label, collection_label, ...)`; both public fns are thin wrappers.
- [x] `crates/model-loader/src/multipart.rs:44` — Public function `download_folder_model` returns Result but lacks # Errors documentation. Add # Errors section documenting potential ModelError variants.
- [x] `crates/model-loader/src/multipart.rs:70` — Public function `detect_multi_part_base` has no inbound callers in production code, only in tests. It is neither exported from the crate's public API via `lib.rs` nor called from `download_multi_part_model`, `download_folder_model`, or any other production path, despite being a documented public function. Either (1) delete the function if it was added speculatively; (2) integrate it into the multi-part download logic if it was meant to be used; or (3) add an explicit forward marker (comment or `#[allow(dead_code)]`) if this is intentional scaffolding for future API consumers. — RESOLVED: the finding's premise is factually wrong — `crate::detection::auto_detect_hf_model_file_with_folder` calls it in production (grep-verified). Wired correctly by demoting to `pub(crate)` (removes the unused *public* API surface) and documenting the caller in its doc comment.
- [x] `crates/model-loader/src/retry.rs:82` — Public async function `download_with_retry` returns Result<PathBuf, ModelError> but its doc comment lacks an # Errors section documenting when it fails. Add # Errors section documenting ModelError variants that can be returned (coordinate errors, download failures, network errors, etc.).
- [x] `crates/model-loader/src/retry.rs:86` — is_retriable_error() encodes a lookup table as a series of if-chains over error patterns. Multiple strings ("500", "502", "503", "504", "429", "connection", "timeout", etc.) map to retriability booleans, but are scattered across nine if statements. This should be data (a table of pattern → boolean pairs) interpreted by a single code path, not parallel if arms that drift if patterns are added, removed, or changed. Extract a constant table const RETRIABLE_PATTERNS: &[(& str, bool)] = &[("500", true), ("502", true), ...] and loop through it once, returning on first match. This removes the need to maintain parallel arms and makes the retry rules readable as data. — RESOLVED by removing `is_retriable_error` entirely (see :150 below); the surviving literals live in the single `RETRIABLE_HTTP_CODES` table.
- [x] `crates/model-loader/src/retry.rs:90` — In is_retriable_error(), the string pattern "500" appears as a literal in the first if-statement (line 90), and the literal "500" appears again in format_download_error() as part of a compound condition (line 170). Similarly, "502", "503", "504" appear in multiple places within is_retriable_error(). When the set of retriable HTTP codes changes (e.g., adding 508 Server Overloaded), the same code must be updated in multiple branches. Define const RETRIABLE_ERROR_CODES: &[&str] = &["500", "502", "503", "504", "429"]; (with a separate NOT_RETRIABLE list for client errors) and use it in both functions. Single source of truth for which codes are retriable. — RESOLVED: `const RETRIABLE_HTTP_CODES: &[&str] = &["500", "502", "503", "504"]` is now the single source; `format_download_error` iterates it (`.any(...)`). 429 stays in its own non-retriable branch, matching prior behavior.
- [x] `crates/model-loader/src/retry.rs:150` — Public function `is_retriable_error` has no inbound callers in production code, only in tests. It is neither exported from the crate's public API via `lib.rs` nor called from `download_with_retry_internal` or any other production path, despite being a newly added symbol. Either (1) delete the function if it was added in error; (2) integrate it into the error-handling path in `download_with_retry_internal` or another production function if it was meant to be used; or (3) add an explicit forward marker (comment naming a follow-up task, or `#[allow(dead_code)]` with reason) if this is work-in-process scaffolding for a subsequent change. — RESOLVED: option (1), deleted along with its 6 unit tests. Grep-verified zero callers outside those tests; production retriability already flows through `ModelError::is_retriable`/`should_stop_retrying` consulted by `RetryManager`, and `llama_common::retry::classify_error_for_retry` was a near-verbatim duplicate of it.
- [x] `crates/swissarmyhammer-embedding/src/embedder.rs:73` — Public enum `EmbedderError` lacks a doc comment. The documentation rule requires all public items to have doc comments. Add a doc comment to EmbedderError explaining its purpose as a typed error for embedder construction and configuration failures. — RESOLVED: enum + every variant documented.
- [x] `crates/swissarmyhammer-embedding/src/embedder.rs:84` — Public type `Embedder` with non-empty representation does not implement Debug. The documentation rule requires Debug for all public types with non-empty representation. Implement Debug for Embedder, e.g., printing backend_name() and model_name() to avoid exposing internal EmbedderBackend representation. — RESOLVED: manual Debug printing backend name, model_name, max_sequence_length, normalize.
- [x] `crates/swissarmyhammer-embedding/src/embedder.rs:116` — Public async function `Embedder::from_model_name` returns Result but lacks # Errors documentation. Add # Errors section documenting potential EmbedderError variants. — RESOLVED (and `Embedder::default` got one too, per the whole-file sweep).
- [x] `crates/swissarmyhammer-embedding/src/embedder.rs:126` — Public async function `Embedder::with_download_observer` returns Result but lacks # Errors documentation. Add # Errors section documenting potential EmbedderError variants.
- [x] `crates/swissarmyhammer-embedding/src/embedder.rs:189` — The string literal "llama" is hardcoded in the backend_name() method match arm (line 189) and again in the build() method's logging match expression (line 199). The string "ane" appears similarly at lines 190 and 200. If backend type identifiers change, these four string literals must be updated in lockstep across two methods. This violates the principle of expressing variation as data (a single mapping from backend type to name) rather than parallel code paths. Extract a helper function fn backend_type_name(backend: &EmbedderBackend) -> &'static str that returns the name for any backend, and call it from both backend_name() and the logging match expression. Alternatively, add a name() method to the EmbedderBackend enum (or impl) so the name is data stored once per variant. — RESOLVED: `EmbedderBackend::name()` is the single mapping; `backend_name()`, build() logging, and the new Debug impl all use it.
- [x] `crates/swissarmyhammer-embedding/src/embedder.rs:218` — The literal `3` (representing chars-per-token estimate) appears in the embed_text() method at line 218 when estimating token count from character length, and again at line 287 in embed_chunked() when computing chars-per-chunk from max_sequence_length. Both encode the same configuration value: approximately 3 characters per token. If this estimate changes (e.g., to 4 chars per token for a different language model), both places must be updated in lockstep. This should be a named constant. Define const CHARS_PER_TOKEN: usize = 3; and use it at both locations: text.len() / CHARS_PER_TOKEN and self.max_sequence_length * CHARS_PER_TOKEN. Unifies the configuration into a single definition. — RESOLVED as specified (also fixed the stale "~4 chars per token" comment that contradicted the code).
- [x] `crates/swissarmyhammer-embedding/src/embedder.rs:287` — The literal `3` (chars-per-token estimate) appears a second time in embed_chunked() at line 287, forming a pair with its occurrence at line 218 in embed_text(). See line 218 finding for full details. Extract CHARS_PER_TOKEN constant and use it here as well to keep the token density estimate in a single definition.
- [x] `crates/swissarmyhammer-embedding/src/embedder.rs:467` — The string literal "mlpackage" appears twice in the build_ane_model() function: once in the format string constructing the mlpackage directory name (line 467), and again in the while-loop condition checking the directory extension (line 490). When the .mlpackage directory structure changes or is extended, both locations must be updated in lockstep. This should be a named constant defined once. Define const MLPACKAGE_EXTENSION: &str = "mlpackage"; at the top of build_ane_model or at module level, and replace both string literals with references to it. — RESOLVED: macos-gated module const, both sites use it.

Note: one engine finding (`crates/llama-embedding/src/model.rs:344`, empty test body) was dropped per the review skill's blanket exception — it targets pre-existing test code (this commit added no test lines to model.rs).
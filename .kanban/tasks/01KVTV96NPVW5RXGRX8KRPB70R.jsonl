{"id":"01KVTV96NQ0ETY4X13FRJBG3DD","timestamp":"2026-06-23T18:18:34.551428Z","op":"create","item_id":"01KVTV96NPVW5RXGRX8KRPB70R","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,37 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: a380\n+project: null\n+title: edit files — forgiving argument normalization to canonical (find, replace) pairs\n+---\n+## What\n+Rework the argument surface of `edit files` in `crates/swissarmyhammer-tools/src/mcp/tools/files/edit/mod.rs` so any of three input shapes normalize to one canonical `Vec<(find, replace)>`. This replaces the current `EditRequest`/`EditOperation` parsing. No matching/IO logic here — just produce the pair list (and per-pair `replace_all`).\n+\n+Declare the params in `EDIT_FILE_PARAMS` (`ParamMeta`) so schema + grammar see them, using `ParamMeta::aliases(...)`:\n+- `find` ← aliases `{search, old, old_string, from, target, match}`\n+- `replace` ← aliases `{new, new_string, to, with, replacement}`\n+- `replace_all?` (Boolean), `edits?` (Array), `file_path` (keep existing `path`/`filePath`/`absolute_path` aliases).\n+The grammar emits canonical `find`/`replace`; the parser accepts any alias so an off-grammar model still lands. Keep backward-compat: `old_string`/`new_string` continue to work (they are aliases of `find`/`replace`).\n+\n+Normalization rules (collect find-ish and replace-ish values from top-level scalar, top-level arrays, the `edits[]` array, under any alias):\n+- N finds + N replaces → zip.\n+- N finds + 1 replace → broadcast the single replace to all (delete-many = many finds + one empty replace).\n+- top-level `find`/`replace` **and** `edits[]` → concatenate.\n+- mismatched array lengths (or 1-find-N-replaces) → pair what zips cleanly, surface the unpaired remainder in the error; never silently drop.\n+Extract this into a pure helper `normalize_edit_args(args) -> Result<Vec<EditPair>>` so it is unit-testable without IO.\n+\n+## Acceptance Criteria\n+- [ ] `EDIT_FILE_PARAMS` declares `find`, `replace`, `replace_all`, `edits` with the alias sets above; the full schema (`schema_full`) lists them.\n+- [ ] Scalar `{find, replace}`, parallel arrays `{find: [..], replace: [..]}`, and `edits: [{search, with}, ...]` all normalize to the same pair list.\n+- [ ] N finds + 1 replace broadcasts; top-level pair + `edits[]` concatenates; mismatched lengths surface the remainder in a clear error.\n+- [ ] Legacy `{old_string, new_string}` and `{oldText, newText}` still parse (regression).\n+\n+## Tests\n+- [ ] Unit tests for `normalize_edit_args` covering every shape and the broadcast/concat/mismatch rules, in `edit/mod.rs` tests.\n+- [ ] A schema test asserting `find`/`replace`/`edits` appear in `schema_full` with aliases (extend `files/schema.rs` tests).\n+- [ ] `cargo test -p swissarmyhammer-tools edit::` is green.\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,37 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: a380\n-project: null\n-title: edit files — forgiving argument normalization to canonical (find, replace) pairs\n----\n-## What\n-Rework the argument surface of `edit files` in `crates/swissarmyhammer-tools/src/mcp/tools/files/edit/mod.rs` so any of three input shapes normalize to one canonical `Vec<(find, replace)>`. This replaces the current `EditRequest`/`EditOperation` parsing. No matching/IO logic here — just produce the pair list (and per-pair `replace_all`).\n-\n-Declare the params in `EDIT_FILE_PARAMS` (`ParamMeta`) so schema + grammar see them, using `ParamMeta::aliases(...)`:\n-- `find` ← aliases `{search, old, old_string, from, target, match}`\n-- `replace` ← aliases `{new, new_string, to, with, replacement}`\n-- `replace_all?` (Boolean), `edits?` (Array), `file_path` (keep existing `path`/`filePath`/`absolute_path` aliases).\n-The grammar emits canonical `find`/`replace`; the parser accepts any alias so an off-grammar model still lands. Keep backward-compat: `old_string`/`new_string` continue to work (they are aliases of `find`/`replace`).\n-\n-Normalization rules (collect find-ish and replace-ish values from top-level scalar, top-level arrays, the `edits[]` array, under any alias):\n-- N finds + N replaces → zip.\n-- N finds + 1 replace → broadcast the single replace to all (delete-many = many finds + one empty replace).\n-- top-level `find`/`replace` **and** `edits[]` → concatenate.\n-- mismatched array lengths (or 1-find-N-replaces) → pair what zips cleanly, surface the unpaired remainder in the error; never silently drop.\n-Extract this into a pure helper `normalize_edit_args(args) -> Result<Vec<EditPair>>` so it is unit-testable without IO.\n-\n-## Acceptance Criteria\n-- [ ] `EDIT_FILE_PARAMS` declares `find`, `replace`, `replace_all`, `edits` with the alias sets above; the full schema (`schema_full`) lists them.\n-- [ ] Scalar `{find, replace}`, parallel arrays `{find: [..], replace: [..]}`, and `edits: [{search, with}, ...]` all normalize to the same pair list.\n-- [ ] N finds + 1 replace broadcasts; top-level pair + `edits[]` concatenates; mismatched lengths surface the remainder in a clear error.\n-- [ ] Legacy `{old_string, new_string}` and `{oldText, newText}` still parse (regression).\n-\n-## Tests\n-- [ ] Unit tests for `normalize_edit_args` covering every shape and the broadcast/concat/mismatch rules, in `edit/mod.rs` tests.\n-- [ ] A schema test asserting `find`/`replace`/`edits` appear in `schema_full` with aliases (extend `files/schema.rs` tests).\n-- [ ] `cargo test -p swissarmyhammer-tools edit::` is green.\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KVTVMB6FYPA07CG028YS8VTC","timestamp":"2026-06-23T18:24:39.631614Z","op":"update","item_id":"01KVTV96NPVW5RXGRX8KRPB70R","forward_patch":"--- original\n+++ modified\n@@ -9,29 +9,31 @@\n ## What\n Rework the argument surface of `edit files` in `crates/swissarmyhammer-tools/src/mcp/tools/files/edit/mod.rs` so any of three input shapes normalize to one canonical `Vec<(find, replace)>`. This replaces the current `EditRequest`/`EditOperation` parsing. No matching/IO logic here — just produce the pair list (and per-pair `replace_all`).\n\n-Declare the params in `EDIT_FILE_PARAMS` (`ParamMeta`) so schema + grammar see them, using `ParamMeta::aliases(...)`:\n+Declare the params in `EDIT_FILE_PARAMS` (`ParamMeta`) so schema + grammar see them, using `ParamMeta::aliases(...)`. **`find`/`replace` must be the canonical (primary) params and `old_string`/`new_string` demoted to aliases**, so the constrained-decoding grammar emits `find`/`replace` as the path of least resistance (the grammar emits the primary param name; verify the param ordering produces that):\n - `find` ← aliases `{search, old, old_string, from, target, match}`\n - `replace` ← aliases `{new, new_string, to, with, replacement}`\n-- `replace_all?` (Boolean), `edits?` (Array), `file_path` (keep existing `path`/`filePath`/`absolute_path` aliases).\n-The grammar emits canonical `find`/`replace`; the parser accepts any alias so an off-grammar model still lands. Keep backward-compat: `old_string`/`new_string` continue to work (they are aliases of `find`/`replace`).\n+- `replace_all?` (Boolean), `edits?` (Array), `file_path` (keep `path`/`filePath`/`absolute_path` aliases).\n\n-Normalization rules (collect find-ish and replace-ish values from top-level scalar, top-level arrays, the `edits[]` array, under any alias):\n+Normalization rules (collect find-ish and replace-ish from top-level scalar, top-level arrays, the `edits[]` array, under any alias):\n - N finds + N replaces → zip.\n - N finds + 1 replace → broadcast the single replace to all (delete-many = many finds + one empty replace).\n - top-level `find`/`replace` **and** `edits[]` → concatenate.\n - mismatched array lengths (or 1-find-N-replaces) → pair what zips cleanly, surface the unpaired remainder in the error; never silently drop.\n-Extract this into a pure helper `normalize_edit_args(args) -> Result<Vec<EditPair>>` so it is unit-testable without IO.\n+Extract a pure helper `normalize_edit_args(args) -> Result<Vec<EditPair>>` (unit-testable without IO).\n+\n+**Dispatcher inference (required by the doc — the canonical happy path must route without an explicit `op`):** extend the `op == \"\"` inference arm in `files/mod.rs` (`FilesTool::execute`) so the presence of any find-ish/replace-ish key OR `edits` routes to `edit::execute_edit`. The `find`/`edits` check must precede the `content`→write branch so `{find, replace}` is not misrouted to write or to the \"Cannot determine operation\" error. Keep the legacy `old_string`/`new_string` inference working.\n\n ## Acceptance Criteria\n-- [ ] `EDIT_FILE_PARAMS` declares `find`, `replace`, `replace_all`, `edits` with the alias sets above; the full schema (`schema_full`) lists them.\n-- [ ] Scalar `{find, replace}`, parallel arrays `{find: [..], replace: [..]}`, and `edits: [{search, with}, ...]` all normalize to the same pair list.\n-- [ ] N finds + 1 replace broadcasts; top-level pair + `edits[]` concatenates; mismatched lengths surface the remainder in a clear error.\n+- [ ] `EDIT_FILE_PARAMS` declares `find`/`replace` as primary with the alias sets above; the full schema (`schema_full`) lists them; param ordering makes the grammar emit canonical `find`/`replace` (assert `find` precedes `old_string`).\n+- [ ] Scalar `{find, replace}`, parallel arrays, and `edits: [{search, with}, ...]` all normalize to the same pair list; broadcast + concat + mismatch-remainder rules hold.\n - [ ] Legacy `{old_string, new_string}` and `{oldText, newText}` still parse (regression).\n+- [ ] With no `op`, `{find, replace}` and `{edits:[...]}` both dispatch to edit (not write, not error).\n\n ## Tests\n-- [ ] Unit tests for `normalize_edit_args` covering every shape and the broadcast/concat/mismatch rules, in `edit/mod.rs` tests.\n-- [ ] A schema test asserting `find`/`replace`/`edits` appear in `schema_full` with aliases (extend `files/schema.rs` tests).\n-- [ ] `cargo test -p swissarmyhammer-tools edit::` is green.\n+- [ ] Unit tests for `normalize_edit_args` covering every shape and the broadcast/concat/mismatch rules.\n+- [ ] Schema test asserting `find`/`replace`/`edits` appear in `schema_full` with aliases and that `find` is primary/precedes `old_string` (extend `files/schema.rs` tests).\n+- [ ] Dispatcher tests in `files/mod.rs`: no-`op` `{find, replace}` and `{edits:[...]}` reach edit; legacy keys still reach edit.\n+- [ ] `cargo test -p swissarmyhammer-tools edit:: files::` is green.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -9,31 +9,29 @@\n ## What\n Rework the argument surface of `edit files` in `crates/swissarmyhammer-tools/src/mcp/tools/files/edit/mod.rs` so any of three input shapes normalize to one canonical `Vec<(find, replace)>`. This replaces the current `EditRequest`/`EditOperation` parsing. No matching/IO logic here — just produce the pair list (and per-pair `replace_all`).\n\n-Declare the params in `EDIT_FILE_PARAMS` (`ParamMeta`) so schema + grammar see them, using `ParamMeta::aliases(...)`. **`find`/`replace` must be the canonical (primary) params and `old_string`/`new_string` demoted to aliases**, so the constrained-decoding grammar emits `find`/`replace` as the path of least resistance (the grammar emits the primary param name; verify the param ordering produces that):\n+Declare the params in `EDIT_FILE_PARAMS` (`ParamMeta`) so schema + grammar see them, using `ParamMeta::aliases(...)`:\n - `find` ← aliases `{search, old, old_string, from, target, match}`\n - `replace` ← aliases `{new, new_string, to, with, replacement}`\n-- `replace_all?` (Boolean), `edits?` (Array), `file_path` (keep `path`/`filePath`/`absolute_path` aliases).\n+- `replace_all?` (Boolean), `edits?` (Array), `file_path` (keep existing `path`/`filePath`/`absolute_path` aliases).\n+The grammar emits canonical `find`/`replace`; the parser accepts any alias so an off-grammar model still lands. Keep backward-compat: `old_string`/`new_string` continue to work (they are aliases of `find`/`replace`).\n\n-Normalization rules (collect find-ish and replace-ish from top-level scalar, top-level arrays, the `edits[]` array, under any alias):\n+Normalization rules (collect find-ish and replace-ish values from top-level scalar, top-level arrays, the `edits[]` array, under any alias):\n - N finds + N replaces → zip.\n - N finds + 1 replace → broadcast the single replace to all (delete-many = many finds + one empty replace).\n - top-level `find`/`replace` **and** `edits[]` → concatenate.\n - mismatched array lengths (or 1-find-N-replaces) → pair what zips cleanly, surface the unpaired remainder in the error; never silently drop.\n-Extract a pure helper `normalize_edit_args(args) -> Result<Vec<EditPair>>` (unit-testable without IO).\n-\n-**Dispatcher inference (required by the doc — the canonical happy path must route without an explicit `op`):** extend the `op == \"\"` inference arm in `files/mod.rs` (`FilesTool::execute`) so the presence of any find-ish/replace-ish key OR `edits` routes to `edit::execute_edit`. The `find`/`edits` check must precede the `content`→write branch so `{find, replace}` is not misrouted to write or to the \"Cannot determine operation\" error. Keep the legacy `old_string`/`new_string` inference working.\n+Extract this into a pure helper `normalize_edit_args(args) -> Result<Vec<EditPair>>` so it is unit-testable without IO.\n\n ## Acceptance Criteria\n-- [ ] `EDIT_FILE_PARAMS` declares `find`/`replace` as primary with the alias sets above; the full schema (`schema_full`) lists them; param ordering makes the grammar emit canonical `find`/`replace` (assert `find` precedes `old_string`).\n-- [ ] Scalar `{find, replace}`, parallel arrays, and `edits: [{search, with}, ...]` all normalize to the same pair list; broadcast + concat + mismatch-remainder rules hold.\n+- [ ] `EDIT_FILE_PARAMS` declares `find`, `replace`, `replace_all`, `edits` with the alias sets above; the full schema (`schema_full`) lists them.\n+- [ ] Scalar `{find, replace}`, parallel arrays `{find: [..], replace: [..]}`, and `edits: [{search, with}, ...]` all normalize to the same pair list.\n+- [ ] N finds + 1 replace broadcasts; top-level pair + `edits[]` concatenates; mismatched lengths surface the remainder in a clear error.\n - [ ] Legacy `{old_string, new_string}` and `{oldText, newText}` still parse (regression).\n-- [ ] With no `op`, `{find, replace}` and `{edits:[...]}` both dispatch to edit (not write, not error).\n\n ## Tests\n-- [ ] Unit tests for `normalize_edit_args` covering every shape and the broadcast/concat/mismatch rules.\n-- [ ] Schema test asserting `find`/`replace`/`edits` appear in `schema_full` with aliases and that `find` is primary/precedes `old_string` (extend `files/schema.rs` tests).\n-- [ ] Dispatcher tests in `files/mod.rs`: no-`op` `{find, replace}` and `{edits:[...]}` reach edit; legacy keys still reach edit.\n-- [ ] `cargo test -p swissarmyhammer-tools edit:: files::` is green.\n+- [ ] Unit tests for `normalize_edit_args` covering every shape and the broadcast/concat/mismatch rules, in `edit/mod.rs` tests.\n+- [ ] A schema test asserting `find`/`replace`/`edits` appear in `schema_full` with aliases (extend `files/schema.rs` tests).\n+- [ ] `cargo test -p swissarmyhammer-tools edit::` is green.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KVTW35143XRVSE2AB7JJNR89","timestamp":"2026-06-23T18:32:44.836769Z","op":"update","item_id":"01KVTV96NPVW5RXGRX8KRPB70R","forward_patch":"--- original\n+++ modified\n@@ -9,7 +9,7 @@\n ## What\n Rework the argument surface of `edit files` in `crates/swissarmyhammer-tools/src/mcp/tools/files/edit/mod.rs` so any of three input shapes normalize to one canonical `Vec<(find, replace)>`. This replaces the current `EditRequest`/`EditOperation` parsing. No matching/IO logic here — just produce the pair list (and per-pair `replace_all`).\n\n-Declare the params in `EDIT_FILE_PARAMS` (`ParamMeta`) so schema + grammar see them, using `ParamMeta::aliases(...)`. **`find`/`replace` must be the canonical (primary) params and `old_string`/`new_string` demoted to aliases**, so the constrained-decoding grammar emits `find`/`replace` as the path of least resistance (the grammar emits the primary param name; verify the param ordering produces that):\n+Declare the params in `EDIT_FILE_PARAMS` (`ParamMeta`) so schema + grammar see them, using `ParamMeta::aliases(...)`. **`find` and `replace` are the canonical `ParamMeta.name`s; `old_string`/`new_string` (and `oldText`/`newText`) are demoted to aliases.** The schema (`collect_all_parameters` in `swissarmyhammer-operations/src/schema.rs`) keys properties on `param.name`, so only the canonical names become top-level properties and the grammar emits `find`/`replace`; aliases resolve at parse time and are NOT separate schema properties:\n - `find` ← aliases `{search, old, old_string, from, target, match}`\n - `replace` ← aliases `{new, new_string, to, with, replacement}`\n - `replace_all?` (Boolean), `edits?` (Array), `file_path` (keep `path`/`filePath`/`absolute_path` aliases).\n@@ -21,17 +21,17 @@\n - mismatched array lengths (or 1-find-N-replaces) → pair what zips cleanly, surface the unpaired remainder in the error; never silently drop.\n Extract a pure helper `normalize_edit_args(args) -> Result<Vec<EditPair>>` (unit-testable without IO).\n\n-**Dispatcher inference (required by the doc — the canonical happy path must route without an explicit `op`):** extend the `op == \"\"` inference arm in `files/mod.rs` (`FilesTool::execute`) so the presence of any find-ish/replace-ish key OR `edits` routes to `edit::execute_edit`. The `find`/`edits` check must precede the `content`→write branch so `{find, replace}` is not misrouted to write or to the \"Cannot determine operation\" error. Keep the legacy `old_string`/`new_string` inference working.\n+**Dispatcher inference (required by the doc — the canonical happy path must route without an explicit `op`):** extend the `op == \"\"` inference arm in `files/mod.rs` (`FilesTool::execute`) so the presence of any find-ish/replace-ish key OR `edits` routes to `edit::execute_edit`. The `find`/`edits` check must precede the `content`→write branch so `{find, replace}` is not misrouted to write or to \"Cannot determine operation\". Keep the legacy `old_string`/`new_string` inference working.\n\n ## Acceptance Criteria\n-- [ ] `EDIT_FILE_PARAMS` declares `find`/`replace` as primary with the alias sets above; the full schema (`schema_full`) lists them; param ordering makes the grammar emit canonical `find`/`replace` (assert `find` precedes `old_string`).\n+- [ ] In `schema_full`, `find` and `replace` appear as top-level properties; `old_string`/`new_string` do NOT appear as separate properties (they are aliases, resolved at parse time). (Do not assert property *ordering* — `collect_all_parameters` dedups into a `HashMap`, so order is non-deterministic.)\n+- [ ] `normalize_edit_args` resolves `{old_string, new_string}` and `{oldText, newText}` to the same `(find, replace)` pair as `{find, replace}` (alias regression).\n - [ ] Scalar `{find, replace}`, parallel arrays, and `edits: [{search, with}, ...]` all normalize to the same pair list; broadcast + concat + mismatch-remainder rules hold.\n-- [ ] Legacy `{old_string, new_string}` and `{oldText, newText}` still parse (regression).\n - [ ] With no `op`, `{find, replace}` and `{edits:[...]}` both dispatch to edit (not write, not error).\n\n ## Tests\n-- [ ] Unit tests for `normalize_edit_args` covering every shape and the broadcast/concat/mismatch rules.\n-- [ ] Schema test asserting `find`/`replace`/`edits` appear in `schema_full` with aliases and that `find` is primary/precedes `old_string` (extend `files/schema.rs` tests).\n+- [ ] Unit tests for `normalize_edit_args` covering every shape and the broadcast/concat/mismatch rules, plus the legacy-alias resolution.\n+- [ ] Schema test: `find`/`replace`/`edits` are properties in `schema_full`; `old_string`/`new_string` are not properties but resolve via the normalizer (extend `files/schema.rs` tests).\n - [ ] Dispatcher tests in `files/mod.rs`: no-`op` `{find, replace}` and `{edits:[...]}` reach edit; legacy keys still reach edit.\n - [ ] `cargo test -p swissarmyhammer-tools edit:: files::` is green.\n\n","reverse_patch":"--- original\n+++ modified\n@@ -9,7 +9,7 @@\n ## What\n Rework the argument surface of `edit files` in `crates/swissarmyhammer-tools/src/mcp/tools/files/edit/mod.rs` so any of three input shapes normalize to one canonical `Vec<(find, replace)>`. This replaces the current `EditRequest`/`EditOperation` parsing. No matching/IO logic here — just produce the pair list (and per-pair `replace_all`).\n\n-Declare the params in `EDIT_FILE_PARAMS` (`ParamMeta`) so schema + grammar see them, using `ParamMeta::aliases(...)`. **`find` and `replace` are the canonical `ParamMeta.name`s; `old_string`/`new_string` (and `oldText`/`newText`) are demoted to aliases.** The schema (`collect_all_parameters` in `swissarmyhammer-operations/src/schema.rs`) keys properties on `param.name`, so only the canonical names become top-level properties and the grammar emits `find`/`replace`; aliases resolve at parse time and are NOT separate schema properties:\n+Declare the params in `EDIT_FILE_PARAMS` (`ParamMeta`) so schema + grammar see them, using `ParamMeta::aliases(...)`. **`find`/`replace` must be the canonical (primary) params and `old_string`/`new_string` demoted to aliases**, so the constrained-decoding grammar emits `find`/`replace` as the path of least resistance (the grammar emits the primary param name; verify the param ordering produces that):\n - `find` ← aliases `{search, old, old_string, from, target, match}`\n - `replace` ← aliases `{new, new_string, to, with, replacement}`\n - `replace_all?` (Boolean), `edits?` (Array), `file_path` (keep `path`/`filePath`/`absolute_path` aliases).\n@@ -21,17 +21,17 @@\n - mismatched array lengths (or 1-find-N-replaces) → pair what zips cleanly, surface the unpaired remainder in the error; never silently drop.\n Extract a pure helper `normalize_edit_args(args) -> Result<Vec<EditPair>>` (unit-testable without IO).\n\n-**Dispatcher inference (required by the doc — the canonical happy path must route without an explicit `op`):** extend the `op == \"\"` inference arm in `files/mod.rs` (`FilesTool::execute`) so the presence of any find-ish/replace-ish key OR `edits` routes to `edit::execute_edit`. The `find`/`edits` check must precede the `content`→write branch so `{find, replace}` is not misrouted to write or to \"Cannot determine operation\". Keep the legacy `old_string`/`new_string` inference working.\n+**Dispatcher inference (required by the doc — the canonical happy path must route without an explicit `op`):** extend the `op == \"\"` inference arm in `files/mod.rs` (`FilesTool::execute`) so the presence of any find-ish/replace-ish key OR `edits` routes to `edit::execute_edit`. The `find`/`edits` check must precede the `content`→write branch so `{find, replace}` is not misrouted to write or to the \"Cannot determine operation\" error. Keep the legacy `old_string`/`new_string` inference working.\n\n ## Acceptance Criteria\n-- [ ] In `schema_full`, `find` and `replace` appear as top-level properties; `old_string`/`new_string` do NOT appear as separate properties (they are aliases, resolved at parse time). (Do not assert property *ordering* — `collect_all_parameters` dedups into a `HashMap`, so order is non-deterministic.)\n-- [ ] `normalize_edit_args` resolves `{old_string, new_string}` and `{oldText, newText}` to the same `(find, replace)` pair as `{find, replace}` (alias regression).\n+- [ ] `EDIT_FILE_PARAMS` declares `find`/`replace` as primary with the alias sets above; the full schema (`schema_full`) lists them; param ordering makes the grammar emit canonical `find`/`replace` (assert `find` precedes `old_string`).\n - [ ] Scalar `{find, replace}`, parallel arrays, and `edits: [{search, with}, ...]` all normalize to the same pair list; broadcast + concat + mismatch-remainder rules hold.\n+- [ ] Legacy `{old_string, new_string}` and `{oldText, newText}` still parse (regression).\n - [ ] With no `op`, `{find, replace}` and `{edits:[...]}` both dispatch to edit (not write, not error).\n\n ## Tests\n-- [ ] Unit tests for `normalize_edit_args` covering every shape and the broadcast/concat/mismatch rules, plus the legacy-alias resolution.\n-- [ ] Schema test: `find`/`replace`/`edits` are properties in `schema_full`; `old_string`/`new_string` are not properties but resolve via the normalizer (extend `files/schema.rs` tests).\n+- [ ] Unit tests for `normalize_edit_args` covering every shape and the broadcast/concat/mismatch rules.\n+- [ ] Schema test asserting `find`/`replace`/`edits` appear in `schema_full` with aliases and that `find` is primary/precedes `old_string` (extend `files/schema.rs` tests).\n - [ ] Dispatcher tests in `files/mod.rs`: no-`op` `{find, replace}` and `{edits:[...]}` reach edit; legacy keys still reach edit.\n - [ ] `cargo test -p swissarmyhammer-tools edit:: files::` is green.\n\n","transaction_id":null}

---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kzp3eeztnwbc1v79mp8xasns
  text: |-
    Research: I checked all five files by hand. Every one is an orphan. No `mod` declaration names them, and no `include!` or `#[path]` reaches them.

    - `crates/swissarmyhammer/src/lib.rs` declares only `pub mod plan_utils;`. There is no `mod security`.
    - `crates/markdowndown/src/lib.rs` declares types, client, converters, frontmatter, detection, config, utils. There is no `mod error` and no `mod fetch`.
    - `crates/swissarmyhammer-common/src/lib.rs` has no `mod sample_avp_test`.
    - `crates/swissarmyhammer-tools/src/mcp/mod.rs` has no `mod notifications`.
    - The four `include!` sites in the workspace all point at `OUT_DIR` build output. The three `#[path]` sites all point at `tests/integration/review_fixture.rs`.

    Each orphan is superseded, broken, or empty:

    - `security.rs` does not even parse. It has `SwissArmyHammerError::Other { message: "...".to_string(), ));` — a bad edit that mixed a struct variant with a tuple variant. The live code is in `swissarmyhammer-templating/src/security.rs`, `claude-agent/src/path_validator.rs`, and `swissarmyhammer-common/src/validation`.
    - `markdowndown/src/error.rs` declares an `Error` enum. The crate uses `MarkdownError` in `types.rs`.
    - `markdowndown/src/fetch.rs` declares `convert_url_with_config`. The live copy is in `markdowndown/src/lib.rs`, and that is the one `swissarmyhammer-web/src/fetch.rs` calls.
    - `sample_avp_test.rs` has no reference anywhere in the tree.
    - `notifications.rs` is 0 bytes.

    Decision: delete all five. Nothing lost a `mod` line — each one is a dead copy of code that lives somewhere else.
  timestamp: 2026-08-10T15:07:50.650861+00:00
- actor: claude-code
  id: 01kzp3ynsm0zshkrfytbepsm2r
  text: |-
    ### implement — changed
    - evidence: deleted 5 files — crates/swissarmyhammer/src/security.rs, crates/markdowndown/src/error.rs, crates/markdowndown/src/fetch.rs, crates/swissarmyhammer-common/src/sample_avp_test.rs, crates/swissarmyhammer-tools/src/mcp/notifications.rs. `cargo fmt --all` clean, no other file changed. `cargo clippy --workspace --all-targets -- -D warnings` finished with no warning. `cargo nextest run --workspace`: 14024 tests, 14020 passed, 4 failed, 0 skipped.
    - the 4 failures are not mine. I put all five files back with `git cat-file blob HEAD:<path>` and ran `review_e2e_sha_range_confirms_the_same_defects` again. It failed the same way, and cargo did not recompile anything (1.5 s total run), which proves the orphan files never reach the compiler. I then deleted them again. New card ^bh5ncd0 holds the four failures.
    - the known swift working-directory race (`the_swift_package_root_restores_the_directory_before_it_removes_it`) passed in this run.
    - next: /review
  timestamp: 2026-08-10T15:16:41.908311+00:00
- actor: claude-code
  id: 01kzp4871h1q4eryb90xcewxnn
  text: |-
    ### review — clean
    - evidence: review sha HEAD~1..HEAD (d9462b38f) — 0 findings, 0 confirmed, 0 refuted, 8 validators attempted, 0 failed. No `mod`, `include!`, or `#[path]` statement names the five deleted files. `cargo check -p markdowndown -p swissarmyhammer -p swissarmyhammer-common -p swissarmyhammer-tools --all-targets` — exit 0, zero warnings.
    - next: none. Card moved to done.
  timestamp: 2026-08-10T15:21:54.481358+00:00
- actor: claude-code
  id: 01kzp48zc249qtz2hzqc33adcj
  text: |
    ### finish iteration 1 — clean
    - implement: changed — 5 orphan files deleted; each claim verified first (no `mod`, no `include!`, no `#[path]` names them)
    - test: green for this change — `cargo nextest run --workspace` 14024 run, 14020 passed, 0 skipped; the 4 failures are pre-existing and proven so by a restore-and-rerun (cargo recompiled nothing, which shows the orphan files never reach the compiler); filed as ^bh5ncd0
    - commit: d9462b38f
    - review: clean — `review sha HEAD~1..HEAD`, 0 findings, 8 validators attempted, 0 failed
    - note: `crates/swissarmyhammer/src/security.rs` did not parse. It mixed a struct variant with a tuple variant in one `return Err(...)`. This shows how long the file was out of the build.
  timestamp: 2026-08-10T15:22:19.394375+00:00
position_column: done
position_ordinal: ffffffffffffffffffffffffffffffffffffffffd680
title: delete the five orphan Rust modules nothing compiles
---
The `dead-code-rust` tool rule's orphan-module scan (^0v7nysz) found five `.rs` files in this workspace that no `mod` declaration names, so nothing compiles them. Every one was hand-checked and every one is real:

- [x] `crates/swissarmyhammer/src/security.rs` — 15 KB of path-validation and resource-limit code nothing compiles — Deleted. It does not parse (`SwissArmyHammerError::Other { message: ... ));`), and the live code is in `swissarmyhammer-templating/src/security.rs`, `claude-agent/src/path_validator.rs`, and `swissarmyhammer-common/src/validation`.
- [x] `crates/markdowndown/src/error.rs` — Deleted. It declares an `Error` enum that nothing uses; the crate uses `MarkdownError` in `types.rs`.
- [x] `crates/markdowndown/src/fetch.rs` — Deleted. Its `convert_url_with_config` is a stale copy of the live one in `markdowndown/src/lib.rs`, which is what `swissarmyhammer-web/src/fetch.rs` calls.
- [x] `crates/swissarmyhammer-common/src/sample_avp_test.rs` — Deleted. No file in the tree names it.
- [x] `crates/swissarmyhammer-tools/src/mcp/notifications.rs` — an empty file — Deleted. It is 0 bytes.

Reproduce with the `dead-code-rust` rule's own script, or by hand:

```sh
grep -rn 'mod security\b' crates/swissarmyhammer --include='*.rs'
```

For each file, decide: wire it in with a `mod` declaration, or delete it. A file nothing compiles has no suppression available — naming it is the only fix, and deleting it is the other.

The rule reports only findings in changed files, so these five never block a review until someone touches the file. That is why they are their own card.

#tool-validators #dead-code
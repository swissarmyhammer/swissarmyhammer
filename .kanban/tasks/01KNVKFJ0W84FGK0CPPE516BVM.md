---
assignees:
- claude-code
position_column: done
position_ordinal: ffffffffffffffffffffffa680
title: Add tests for LspDaemon health check failure paths
---
daemon.rs:272, 315-323\n\nCoverage: 74.6% (153/205 lines)\n\nUncovered lines: 272, 315-318, 321-323\n\nFunction: `is_healthy()` — two failure paths:\n1. Line 272: `child.try_wait()` but no child exists (early return false)\n2. Lines 315-318: process exited unexpectedly (Ok(Some(status))) — clears client, records failure\n3. Lines 321-323: try_wait error (Err(e)) — clears client, records failure\n\nTest scenarios:\n- Call is_healthy when no child process → false\n- Process exited with status → state transition, client cleared\n- try_wait returns Err → state transition, client cleared\n\n#coverage-gap\n\n=== RESOLUTION ===\nAdded 4 tests strengthening health_check coverage:\n- test_health_check_exited_process_full_contract: verifies full contract (child cleared, client cleared with a real LspJsonRpcClient installed, Failed state reason mentions 'process exited', consecutive_failures = 1)\n- test_health_check_none_path_does_not_mutate_state: None early-return path doesn't mutate state or failure counter\n- test_health_check_live_process_no_side_effects: live process path returns true idempotently with no side effects\n- test_health_check_failed_attempts_tracks_consecutive_failures: Failed.attempts payload tracks consecutive_failures counter\n\nSKIPPED GAP:\n- try_wait Err branch (lines 315-318 at time of writing, 321-323 in card coords): tokio Child::try_wait only returns Err if the child handle has been detached or the OS wait4 syscall fails (e.g. EINTR). Triggering this portably is effectively impossible without a custom Child mock, which would require refactoring production code to accept a trait-bound handle. The two failure branches (Ok(Some) and Err) are functionally identical (both clear client, drop child, record failure, return false), so coverage of the Ok(Some) branch verifies the behavior contract; the Err branch is dead-code-adjacent from a test-reachability perspective.
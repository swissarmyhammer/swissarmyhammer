{"id":"01KTBNQZFX3R5QJ97Y3S2QCT2C","timestamp":"2026-06-05T10:37:28.957956Z","op":"create","item_id":"01KTBNQZFX33J2QA6E99HE0M5S","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,33 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KTBNNTCCVS81QZV4CFQZV4X1\n+position_column: todo\n+position_ordinal: '9080'\n+project: local-review\n+title: Rewrite the `review` skill + `reviewer` agent as thin drivers of the review tool\n+---\n+## What\n+Move the heavy reasoning out of the skill and into the tool. The skill stops being a 7-layer manual checklist a single agent grinds (the root cause of the missed duplication/reuse findings) and becomes a thin driver: determine mode, call `review` `run review`, record the returned findings on kanban, summarize.\n+\n+- Rewrite `builtin/skills/review/SKILL.md`:\n+  - Keep the excellent input contract: mode detection (task-mode vs range-mode), the review-column gate, task-mode vs range-mode kanban write logic (append dated section / create tracking task / move to terminal when clean), and the \"column movement is the verdict\" rule. These are good and stay.\n+  - Replace Process steps 3–7 (manual get-changes/read-every-file/seven-layers/language-refs) with: call the `review` tool's `run review` op with the scoped range/branch/task, receive the `ReviewReport`, and write `report.markdown` into the task per the existing task-mode/range-mode logic.\n+  - The skill no longer enumerates layers or calls `find duplicates`/`get blastradius` itself — the fleet does that. Keep the language-specific reference files only insofar as their content is now expressed as rules/dimensions (link or remove per the design).\n+  - Document the `backend`/`dimensions` passthrough so a user can say \"review locally\" (force Llama) or \"review just duplication\".\n+- Update `builtin/agents/reviewer/AGENT.md`: it now drives the tool-based skill; trim instructions that assumed manual layer-by-layer work. Keep `code-context`, `really-done`, `thoughtful` skill deps.\n+- Regenerate `.skills/` from `builtin/skills/` (generated dir — never hand-edit).\n+\n+## Acceptance Criteria\n+- [ ] `builtin/skills/review/SKILL.md` drives the `review` tool for the analysis and keeps the mode-detection + kanban-write contract verbatim; it no longer hand-runs the 7 layers.\n+- [ ] `reviewer` AGENT.md reflects the thin-driver flow.\n+- [ ] `backend` and `dimensions` passthrough are documented in the skill.\n+- [ ] `.skills/` regenerated from source.\n+\n+## Tests\n+- [ ] Skill-render/lint test (whatever the repo uses to validate skill frontmatter/templating) passes for the rewritten skill.\n+- [ ] End-to-end is covered by the dedicated e2e task; here, assert the skill references the real tool op names (a doc/lint check) and that the generated `.skills/review` matches source.\n+\n+## Workflow\n+- Skill-authoring task. Preserve the kanban write contract byte-for-byte (it already works); only swap the analysis engine. Do NOT hand-edit `.skills/` — edit `builtin/skills/review/` and regenerate. Depends on the review tool existing.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,33 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KTBNNTCCVS81QZV4CFQZV4X1\n-position_column: todo\n-position_ordinal: '9080'\n-project: local-review\n-title: Rewrite the `review` skill + `reviewer` agent as thin drivers of the review tool\n----\n-## What\n-Move the heavy reasoning out of the skill and into the tool. The skill stops being a 7-layer manual checklist a single agent grinds (the root cause of the missed duplication/reuse findings) and becomes a thin driver: determine mode, call `review` `run review`, record the returned findings on kanban, summarize.\n-\n-- Rewrite `builtin/skills/review/SKILL.md`:\n-  - Keep the excellent input contract: mode detection (task-mode vs range-mode), the review-column gate, task-mode vs range-mode kanban write logic (append dated section / create tracking task / move to terminal when clean), and the \"column movement is the verdict\" rule. These are good and stay.\n-  - Replace Process steps 3–7 (manual get-changes/read-every-file/seven-layers/language-refs) with: call the `review` tool's `run review` op with the scoped range/branch/task, receive the `ReviewReport`, and write `report.markdown` into the task per the existing task-mode/range-mode logic.\n-  - The skill no longer enumerates layers or calls `find duplicates`/`get blastradius` itself — the fleet does that. Keep the language-specific reference files only insofar as their content is now expressed as rules/dimensions (link or remove per the design).\n-  - Document the `backend`/`dimensions` passthrough so a user can say \"review locally\" (force Llama) or \"review just duplication\".\n-- Update `builtin/agents/reviewer/AGENT.md`: it now drives the tool-based skill; trim instructions that assumed manual layer-by-layer work. Keep `code-context`, `really-done`, `thoughtful` skill deps.\n-- Regenerate `.skills/` from `builtin/skills/` (generated dir — never hand-edit).\n-\n-## Acceptance Criteria\n-- [ ] `builtin/skills/review/SKILL.md` drives the `review` tool for the analysis and keeps the mode-detection + kanban-write contract verbatim; it no longer hand-runs the 7 layers.\n-- [ ] `reviewer` AGENT.md reflects the thin-driver flow.\n-- [ ] `backend` and `dimensions` passthrough are documented in the skill.\n-- [ ] `.skills/` regenerated from source.\n-\n-## Tests\n-- [ ] Skill-render/lint test (whatever the repo uses to validate skill frontmatter/templating) passes for the rewritten skill.\n-- [ ] End-to-end is covered by the dedicated e2e task; here, assert the skill references the real tool op names (a doc/lint check) and that the generated `.skills/review` matches source.\n-\n-## Workflow\n-- Skill-authoring task. Preserve the kanban write contract byte-for-byte (it already works); only swap the analysis engine. Do NOT hand-edit `.skills/` — edit `builtin/skills/review/` and regenerate. Depends on the review tool existing.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTC0P9D2HNN75V1GCJ1QHAX9","timestamp":"2026-06-05T13:48:47.906276Z","op":"update","item_id":"01KTBNQZFX33J2QA6E99HE0M5S","forward_patch":"--- original\n+++ modified\n@@ -3,6 +3,7 @@\n - claude-code\n depends_on:\n - 01KTBNNTCCVS81QZV4CFQZV4X1\n+- 01KTC0MZ8ZH46WCN514J4RPS3H\n position_column: todo\n position_ordinal: '9080'\n project: local-review\n@@ -9,25 +10,28 @@\n title: Rewrite the `review` skill + `reviewer` agent as thin drivers of the review tool\n ---\n ## What\n-Move the heavy reasoning out of the skill and into the tool. The skill stops being a 7-layer manual checklist a single agent grinds (the root cause of the missed duplication/reuse findings) and becomes a thin driver: determine mode, call `review` `run review`, record the returned findings on kanban, summarize.\n+Move the heavy reasoning out of the skill into the engine. `builtin/skills/review/SKILL.md` stops being a 7-layer manual checklist and becomes a thin driver: detect mode, call the right `review` op, record the returned findings on kanban, summarize.\n\n-- Rewrite `builtin/skills/review/SKILL.md`:\n-  - Keep the excellent input contract: mode detection (task-mode vs range-mode), the review-column gate, task-mode vs range-mode kanban write logic (append dated section / create tracking task / move to terminal when clean), and the \"column movement is the verdict\" rule. These are good and stay.\n-  - Replace Process steps 3–7 (manual get-changes/read-every-file/seven-layers/language-refs) with: call the `review` tool's `run review` op with the scoped range/branch/task, receive the `ReviewReport`, and write `report.markdown` into the task per the existing task-mode/range-mode logic.\n-  - The skill no longer enumerates layers or calls `find duplicates`/`get blastradius` itself — the fleet does that. Keep the language-specific reference files only insofar as their content is now expressed as rules/dimensions (link or remove per the design).\n-  - Document the `backend`/`dimensions` passthrough so a user can say \"review locally\" (force Llama) or \"review just duplication\".\n-- Update `builtin/agents/reviewer/AGENT.md`: it now drives the tool-based skill; trim instructions that assumed manual layer-by-layer work. Keep `code-context`, `really-done`, `thoughtful` skill deps.\n+- **Keep verbatim** (these already work): mode detection (task-mode vs range-mode), the review-column gate, the task-mode/range-mode kanban write logic (append dated `## Review Findings` section / create tracking task / move to terminal when clean), and \"column movement is the verdict.\"\n+- **Map modes onto the tool's verb-noun ops:**\n+  - task-mode (`/review <id>` or oldest task in the `review` column) → read the task, derive scope from any range hint in its body → `review sha <range>`; no hint → `review working`.\n+  - range-mode → `review working` (default), `review sha <range>` (commit/range), or `review file <path|glob>`.\n+  - The skill passes the chosen op to the `review` tool, takes `ReviewReport.markdown`, and writes it into the task per the existing contract; uses `counts` for the summary.\n+- **Drop the manual analysis** (old Process steps 3–7: get-changes / read-every-file / 7 layers / language refs). The engine fleet does all of it.\n+- **Remove the language reference files entirely** — `references/RUST_REVIEW.md` etc. are now language validators (separate task). Delete the `references/*_REVIEW.md` links and the \"apply language guidelines\" step from the skill.\n+- **Document the passthrough**: `validators` (subset — \"review just duplication\") and `backend` (\"review locally\" → force local Llama). No \"dimensions\" (gone).\n+- Update `builtin/agents/reviewer/AGENT.md` to the thin-driver flow; keep `code-context`, `really-done`, `thoughtful` deps; trim manual-layer instructions.\n - Regenerate `.skills/` from `builtin/skills/` (generated dir — never hand-edit).\n\n ## Acceptance Criteria\n-- [ ] `builtin/skills/review/SKILL.md` drives the `review` tool for the analysis and keeps the mode-detection + kanban-write contract verbatim; it no longer hand-runs the 7 layers.\n+- [ ] `builtin/skills/review/SKILL.md` drives the `review` tool for analysis (maps task/range modes to `review file/working/sha`) and keeps the mode-detection + kanban-write contract verbatim; it no longer hand-runs layers or links `*_REVIEW.md`.\n+- [ ] `validators` + `backend` passthrough documented; no \"dimensions\".\n - [ ] `reviewer` AGENT.md reflects the thin-driver flow.\n-- [ ] `backend` and `dimensions` passthrough are documented in the skill.\n-- [ ] `.skills/` regenerated from source.\n+- [ ] `.skills/` regenerated from source; the `references/*_REVIEW.md` links are gone.\n\n ## Tests\n-- [ ] Skill-render/lint test (whatever the repo uses to validate skill frontmatter/templating) passes for the rewritten skill.\n-- [ ] End-to-end is covered by the dedicated e2e task; here, assert the skill references the real tool op names (a doc/lint check) and that the generated `.skills/review` matches source.\n+- [ ] Skill-render/lint test (the repo's skill frontmatter/template validation) passes for the rewritten skill.\n+- [ ] Doc/lint check: the skill references real tool ops (`review working`/`review sha`/`review file`) and no longer references `references/*_REVIEW.md`; generated `.skills/review` matches source.\n\n ## Workflow\n-- Skill-authoring task. Preserve the kanban write contract byte-for-byte (it already works); only swap the analysis engine. Do NOT hand-edit `.skills/` — edit `builtin/skills/review/` and regenerate. Depends on the review tool existing.\n\\ No newline at end of file\n+- Skill-authoring task. Preserve the kanban write contract byte-for-byte; only swap the analysis engine and drop the language refs. Do NOT hand-edit `.skills/` — edit `builtin/skills/review/` and regenerate. Depends on the review tool (ops exist) and the language-validators task (so the language content is migrated before the refs are deleted).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,6 @@\n - claude-code\n depends_on:\n - 01KTBNNTCCVS81QZV4CFQZV4X1\n-- 01KTC0MZ8ZH46WCN514J4RPS3H\n position_column: todo\n position_ordinal: '9080'\n project: local-review\n@@ -10,28 +9,25 @@\n title: Rewrite the `review` skill + `reviewer` agent as thin drivers of the review tool\n ---\n ## What\n-Move the heavy reasoning out of the skill into the engine. `builtin/skills/review/SKILL.md` stops being a 7-layer manual checklist and becomes a thin driver: detect mode, call the right `review` op, record the returned findings on kanban, summarize.\n+Move the heavy reasoning out of the skill and into the tool. The skill stops being a 7-layer manual checklist a single agent grinds (the root cause of the missed duplication/reuse findings) and becomes a thin driver: determine mode, call `review` `run review`, record the returned findings on kanban, summarize.\n\n-- **Keep verbatim** (these already work): mode detection (task-mode vs range-mode), the review-column gate, the task-mode/range-mode kanban write logic (append dated `## Review Findings` section / create tracking task / move to terminal when clean), and \"column movement is the verdict.\"\n-- **Map modes onto the tool's verb-noun ops:**\n-  - task-mode (`/review <id>` or oldest task in the `review` column) → read the task, derive scope from any range hint in its body → `review sha <range>`; no hint → `review working`.\n-  - range-mode → `review working` (default), `review sha <range>` (commit/range), or `review file <path|glob>`.\n-  - The skill passes the chosen op to the `review` tool, takes `ReviewReport.markdown`, and writes it into the task per the existing contract; uses `counts` for the summary.\n-- **Drop the manual analysis** (old Process steps 3–7: get-changes / read-every-file / 7 layers / language refs). The engine fleet does all of it.\n-- **Remove the language reference files entirely** — `references/RUST_REVIEW.md` etc. are now language validators (separate task). Delete the `references/*_REVIEW.md` links and the \"apply language guidelines\" step from the skill.\n-- **Document the passthrough**: `validators` (subset — \"review just duplication\") and `backend` (\"review locally\" → force local Llama). No \"dimensions\" (gone).\n-- Update `builtin/agents/reviewer/AGENT.md` to the thin-driver flow; keep `code-context`, `really-done`, `thoughtful` deps; trim manual-layer instructions.\n+- Rewrite `builtin/skills/review/SKILL.md`:\n+  - Keep the excellent input contract: mode detection (task-mode vs range-mode), the review-column gate, task-mode vs range-mode kanban write logic (append dated section / create tracking task / move to terminal when clean), and the \"column movement is the verdict\" rule. These are good and stay.\n+  - Replace Process steps 3–7 (manual get-changes/read-every-file/seven-layers/language-refs) with: call the `review` tool's `run review` op with the scoped range/branch/task, receive the `ReviewReport`, and write `report.markdown` into the task per the existing task-mode/range-mode logic.\n+  - The skill no longer enumerates layers or calls `find duplicates`/`get blastradius` itself — the fleet does that. Keep the language-specific reference files only insofar as their content is now expressed as rules/dimensions (link or remove per the design).\n+  - Document the `backend`/`dimensions` passthrough so a user can say \"review locally\" (force Llama) or \"review just duplication\".\n+- Update `builtin/agents/reviewer/AGENT.md`: it now drives the tool-based skill; trim instructions that assumed manual layer-by-layer work. Keep `code-context`, `really-done`, `thoughtful` skill deps.\n - Regenerate `.skills/` from `builtin/skills/` (generated dir — never hand-edit).\n\n ## Acceptance Criteria\n-- [ ] `builtin/skills/review/SKILL.md` drives the `review` tool for analysis (maps task/range modes to `review file/working/sha`) and keeps the mode-detection + kanban-write contract verbatim; it no longer hand-runs layers or links `*_REVIEW.md`.\n-- [ ] `validators` + `backend` passthrough documented; no \"dimensions\".\n+- [ ] `builtin/skills/review/SKILL.md` drives the `review` tool for the analysis and keeps the mode-detection + kanban-write contract verbatim; it no longer hand-runs the 7 layers.\n - [ ] `reviewer` AGENT.md reflects the thin-driver flow.\n-- [ ] `.skills/` regenerated from source; the `references/*_REVIEW.md` links are gone.\n+- [ ] `backend` and `dimensions` passthrough are documented in the skill.\n+- [ ] `.skills/` regenerated from source.\n\n ## Tests\n-- [ ] Skill-render/lint test (the repo's skill frontmatter/template validation) passes for the rewritten skill.\n-- [ ] Doc/lint check: the skill references real tool ops (`review working`/`review sha`/`review file`) and no longer references `references/*_REVIEW.md`; generated `.skills/review` matches source.\n+- [ ] Skill-render/lint test (whatever the repo uses to validate skill frontmatter/templating) passes for the rewritten skill.\n+- [ ] End-to-end is covered by the dedicated e2e task; here, assert the skill references the real tool op names (a doc/lint check) and that the generated `.skills/review` matches source.\n\n ## Workflow\n-- Skill-authoring task. Preserve the kanban write contract byte-for-byte; only swap the analysis engine and drop the language refs. Do NOT hand-edit `.skills/` — edit `builtin/skills/review/` and regenerate. Depends on the review tool (ops exist) and the language-validators task (so the language content is migrated before the refs are deleted).\n\\ No newline at end of file\n+- Skill-authoring task. Preserve the kanban write contract byte-for-byte (it already works); only swap the analysis engine. Do NOT hand-edit `.skills/` — edit `builtin/skills/review/` and regenerate. Depends on the review tool existing.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTCKFCF1KGRPNZCGDEAX2WZ3","timestamp":"2026-06-05T19:17:04.609217Z","op":"update","item_id":"01KTBNQZFX33J2QA6E99HE0M5S","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KTBNNTCCVS81QZV4CFQZV4X1\n - 01KTC0MZ8ZH46WCN514J4RPS3H\n-position_column: todo\n-position_ordinal: '9080'\n+position_column: doing\n+position_ordinal: '80'\n project: local-review\n title: Rewrite the `review` skill + `reviewer` agent as thin drivers of the review tool\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KTBNNTCCVS81QZV4CFQZV4X1\n - 01KTC0MZ8ZH46WCN514J4RPS3H\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '9080'\n project: local-review\n title: Rewrite the `review` skill + `reviewer` agent as thin drivers of the review tool\n ---\n","transaction_id":null}
{"id":"01KTCKZEJTE7FKVB1GPMS8ZC78","timestamp":"2026-06-05T19:25:51.066452Z","op":"update","item_id":"01KTBNQZFX33J2QA6E99HE0M5S","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KTBNNTCCVS81QZV4CFQZV4X1\n - 01KTC0MZ8ZH46WCN514J4RPS3H\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: local-review\n title: Rewrite the `review` skill + `reviewer` agent as thin drivers of the review tool\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KTBNNTCCVS81QZV4CFQZV4X1\n - 01KTC0MZ8ZH46WCN514J4RPS3H\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: local-review\n title: Rewrite the `review` skill + `reviewer` agent as thin drivers of the review tool\n ---\n","transaction_id":null}
{"id":"01KTCQWE9PFWV6PFR7NDW3EYYD","timestamp":"2026-06-05T20:34:06.774128Z","op":"update","item_id":"01KTBNQZFX33J2QA6E99HE0M5S","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KTBNNTCCVS81QZV4CFQZV4X1\n - 01KTC0MZ8ZH46WCN514J4RPS3H\n-position_column: review\n-position_ordinal: '8180'\n+position_column: done\n+position_ordinal: fffffffffffffffffffffffffffffffffffffe80\n project: local-review\n title: Rewrite the `review` skill + `reviewer` agent as thin drivers of the review tool\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KTBNNTCCVS81QZV4CFQZV4X1\n - 01KTC0MZ8ZH46WCN514J4RPS3H\n-position_column: done\n-position_ordinal: fffffffffffffffffffffffffffffffffffffe80\n+position_column: review\n+position_ordinal: '8180'\n project: local-review\n title: Rewrite the `review` skill + `reviewer` agent as thin drivers of the review tool\n ---\n","transaction_id":null}

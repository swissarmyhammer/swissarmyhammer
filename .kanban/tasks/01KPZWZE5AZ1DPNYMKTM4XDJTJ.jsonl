{"id":"01KPZWZE5BAGMR3VB9NS9P76BZ","timestamp":"2026-04-24T14:05:54.731285Z","op":"create","item_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,129 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KPZWY4B79QJFF6XFEG1JR4RJ\n+position_column: todo\n+position_ordinal: ff8d80\n+project: spatial-nav\n+title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n+---\n+## What\n+\n+Refactor the existing `<FocusScope>` (in `kanban-app/ui/src/components/focus-scope.tsx`) so that it composes the primitives (`<Focusable>` or `<FocusZone>`) added in the primitives card. `FocusScope` keeps its existing entity-oriented responsibilities — CommandScopeProvider, click-to-focus routing, data attributes, context menu, focus bar — but delegates **registration / rect measurement / keys** to whichever primitive fits.\n+\n+### Peer-type summary\n+\n+| Rust type           | React component       | Registration           |\n+|---------------------|-----------------------|------------------------|\n+| `struct Focusable`  | `<Focusable>`         | leaf, direct           |\n+| `struct FocusZone`  | `<FocusZone>`         | zone, direct           |\n+| `struct FocusLayer` | `<FocusLayer>`        | layer, direct          |\n+| `enum FocusScope`   | `<FocusScope>`        | composite → wraps primitive |\n+\n+On the Rust side, `enum FocusScope` is the stored sum type. On React, `<FocusScope>` is the entity-aware wrapper most call sites use. Direct use of primitives is also fine for non-entity chrome.\n+\n+### Props\n+\n+```typescript\n+import type { Moniker, NavOverride } from \"@/types/spatial\";\n+\n+interface FocusScopeProps {\n+  moniker: Moniker;\n+  kind?: \"leaf\" | \"zone\";          // default \"leaf\"\n+  navOverride?: NavOverride;\n+  commands?: CommandDef[];          // existing — passed to CommandScopeProvider\n+  showFocusBar?: boolean;           // existing\n+  handleEvents?: boolean;           // existing\n+  children: React.ReactNode;\n+  // plus existing passthrough HTMLAttributes\n+}\n+```\n+\n+### Structure\n+\n+```typescript\n+export function FocusScope({ moniker, kind = \"leaf\", navOverride, commands, ...rest }: FocusScopeProps) {\n+  const Primitive = kind === \"zone\" ? FocusZone : Focusable;\n+\n+  return (\n+    <FocusScopeContext.Provider value={moniker}>\n+      <CommandScopeProvider commands={commands ?? EMPTY_COMMANDS}>\n+        <Primitive moniker={moniker} navOverride={navOverride} {...rest}>\n+          {/* existing focus-bar / context-menu chrome */}\n+          <FocusHighlight>{children}</FocusHighlight>\n+        </Primitive>\n+      </CommandScopeProvider>\n+    </FocusScopeContext.Provider>\n+  );\n+}\n+```\n+\n+The primitive handles: key generation, rect measurement, Tauri registration, claim callback, data-moniker/data-focused attrs, click-to-focus. `FocusScope` stacks the entity plumbing on top.\n+\n+### What moves where\n+\n+| Concern                                     | Before (in FocusScope) | After                                   |\n+|---------------------------------------------|------------------------|-----------------------------------------|\n+| ULID key generation                         | FocusScope             | Primitive                               |\n+| ResizeObserver + `spatial_register_*`       | (did not exist)        | Primitive                               |\n+| Unregister on unmount                       | (did not exist)        | Primitive                               |\n+| data-moniker / data-focused attrs           | FocusScope             | Primitive                               |\n+| Click → focus dispatch                      | FocusScope             | Primitive (`spatial_focus` invoke)      |\n+| CommandScopeProvider                        | FocusScope             | FocusScope (unchanged)                  |\n+| FocusHighlight / focus bar visual           | FocusScope             | FocusScope (unchanged)                  |\n+| Context menu                                | FocusScope             | FocusScope (unchanged)                  |\n+| Legacy `claimWhen` predicate registration   | FocusScope             | **Deleted** (see card `01KNQY1GQ9...`)  |\n+| `claimWhen` → `navOverride` rename          | —                      | **FocusScope passes through to primitive** |\n+\n+### Usage parity\n+\n+After refactor, every existing call site of `<FocusScope moniker=\"...\">` keeps working. Zone-ness is opt-in via the new `kind` prop:\n+\n+```tsx\n+// Leaf — default, unchanged for existing call sites\n+<FocusScope moniker={Moniker(`task:${id}.title`)}>\n+  {title}\n+</FocusScope>\n+\n+// Zone — new, for columns, cards, field rows\n+<FocusScope moniker={Moniker(`task:${id}`)} kind=\"zone\">\n+  <TaskCardBody />\n+</FocusScope>\n+\n+// Non-entity chrome can skip FocusScope entirely and use primitives:\n+<FocusZone moniker={Moniker(\"ui:toolbar.actions\")}>\n+  <FiltersButton />\n+  <NewTaskButton />\n+</FocusZone>\n+```\n+\n+### Subtasks\n+- [ ] Add `kind: \"leaf\" | \"zone\"` prop to FocusScope (default `\"leaf\"`)\n+- [ ] Internally delegate to `<Focusable>` or `<FocusZone>` based on `kind`\n+- [ ] Remove key-generation, ResizeObserver, and registration logic from FocusScope (now in primitives)\n+- [ ] Keep CommandScopeProvider, FocusHighlight, context menu, entity-focus behaviors\n+- [ ] Pass `navOverride` through to the primitive\n+- [ ] Ensure existing `<FocusScope moniker=\"...\">` call sites compile and behave the same as before (default kind = leaf)\n+- [ ] Delete any `claimWhen` wiring that the primitives don't cover (final removal happens in card `01KNQY1GQ9...`)\n+\n+## Acceptance Criteria\n+- [ ] `<FocusScope>` wraps exactly one primitive based on `kind`\n+- [ ] Every existing call site of `<FocusScope>` compiles unchanged (default kind is `\"leaf\"`)\n+- [ ] `moniker` prop is typed as `Moniker` (branded); TS rejects raw `string` at call sites\n+- [ ] CommandScopeProvider, context menu, FocusHighlight behaviors unchanged\n+- [ ] Click-to-focus now goes through the primitive's `spatial_focus` invoke (not the old `setFocus(moniker)` path)\n+- [ ] No duplicate registration — primitive registers once, FocusScope does not register separately\n+- [ ] `pnpm vitest run` passes\n+\n+## Tests\n+- [ ] `focus-scope.test.tsx` — `<FocusScope moniker={Moniker(...)}>` renders a `<Focusable>` when `kind` omitted\n+- [ ] `focus-scope.test.tsx` — `<FocusScope kind=\"zone\">` renders a `<FocusZone>`\n+- [ ] `focus-scope.test.tsx` — `navOverride` prop forwarded to the primitive\n+- [ ] `focus-scope.test.tsx` — CommandScopeProvider sees `commands` prop as before\n+- [ ] `focus-scope.test.tsx` — click on the rendered element invokes `spatial_focus` with the primitive's key\n+- [ ] Existing tests in `focus-scope.test.tsx` still pass without modification beyond type updates\n+- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,129 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KPZWY4B79QJFF6XFEG1JR4RJ\n-position_column: todo\n-position_ordinal: ff8d80\n-project: spatial-nav\n-title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n----\n-## What\n-\n-Refactor the existing `<FocusScope>` (in `kanban-app/ui/src/components/focus-scope.tsx`) so that it composes the primitives (`<Focusable>` or `<FocusZone>`) added in the primitives card. `FocusScope` keeps its existing entity-oriented responsibilities — CommandScopeProvider, click-to-focus routing, data attributes, context menu, focus bar — but delegates **registration / rect measurement / keys** to whichever primitive fits.\n-\n-### Peer-type summary\n-\n-| Rust type           | React component       | Registration           |\n-|---------------------|-----------------------|------------------------|\n-| `struct Focusable`  | `<Focusable>`         | leaf, direct           |\n-| `struct FocusZone`  | `<FocusZone>`         | zone, direct           |\n-| `struct FocusLayer` | `<FocusLayer>`        | layer, direct          |\n-| `enum FocusScope`   | `<FocusScope>`        | composite → wraps primitive |\n-\n-On the Rust side, `enum FocusScope` is the stored sum type. On React, `<FocusScope>` is the entity-aware wrapper most call sites use. Direct use of primitives is also fine for non-entity chrome.\n-\n-### Props\n-\n-```typescript\n-import type { Moniker, NavOverride } from \"@/types/spatial\";\n-\n-interface FocusScopeProps {\n-  moniker: Moniker;\n-  kind?: \"leaf\" | \"zone\";          // default \"leaf\"\n-  navOverride?: NavOverride;\n-  commands?: CommandDef[];          // existing — passed to CommandScopeProvider\n-  showFocusBar?: boolean;           // existing\n-  handleEvents?: boolean;           // existing\n-  children: React.ReactNode;\n-  // plus existing passthrough HTMLAttributes\n-}\n-```\n-\n-### Structure\n-\n-```typescript\n-export function FocusScope({ moniker, kind = \"leaf\", navOverride, commands, ...rest }: FocusScopeProps) {\n-  const Primitive = kind === \"zone\" ? FocusZone : Focusable;\n-\n-  return (\n-    <FocusScopeContext.Provider value={moniker}>\n-      <CommandScopeProvider commands={commands ?? EMPTY_COMMANDS}>\n-        <Primitive moniker={moniker} navOverride={navOverride} {...rest}>\n-          {/* existing focus-bar / context-menu chrome */}\n-          <FocusHighlight>{children}</FocusHighlight>\n-        </Primitive>\n-      </CommandScopeProvider>\n-    </FocusScopeContext.Provider>\n-  );\n-}\n-```\n-\n-The primitive handles: key generation, rect measurement, Tauri registration, claim callback, data-moniker/data-focused attrs, click-to-focus. `FocusScope` stacks the entity plumbing on top.\n-\n-### What moves where\n-\n-| Concern                                     | Before (in FocusScope) | After                                   |\n-|---------------------------------------------|------------------------|-----------------------------------------|\n-| ULID key generation                         | FocusScope             | Primitive                               |\n-| ResizeObserver + `spatial_register_*`       | (did not exist)        | Primitive                               |\n-| Unregister on unmount                       | (did not exist)        | Primitive                               |\n-| data-moniker / data-focused attrs           | FocusScope             | Primitive                               |\n-| Click → focus dispatch                      | FocusScope             | Primitive (`spatial_focus` invoke)      |\n-| CommandScopeProvider                        | FocusScope             | FocusScope (unchanged)                  |\n-| FocusHighlight / focus bar visual           | FocusScope             | FocusScope (unchanged)                  |\n-| Context menu                                | FocusScope             | FocusScope (unchanged)                  |\n-| Legacy `claimWhen` predicate registration   | FocusScope             | **Deleted** (see card `01KNQY1GQ9...`)  |\n-| `claimWhen` → `navOverride` rename          | —                      | **FocusScope passes through to primitive** |\n-\n-### Usage parity\n-\n-After refactor, every existing call site of `<FocusScope moniker=\"...\">` keeps working. Zone-ness is opt-in via the new `kind` prop:\n-\n-```tsx\n-// Leaf — default, unchanged for existing call sites\n-<FocusScope moniker={Moniker(`task:${id}.title`)}>\n-  {title}\n-</FocusScope>\n-\n-// Zone — new, for columns, cards, field rows\n-<FocusScope moniker={Moniker(`task:${id}`)} kind=\"zone\">\n-  <TaskCardBody />\n-</FocusScope>\n-\n-// Non-entity chrome can skip FocusScope entirely and use primitives:\n-<FocusZone moniker={Moniker(\"ui:toolbar.actions\")}>\n-  <FiltersButton />\n-  <NewTaskButton />\n-</FocusZone>\n-```\n-\n-### Subtasks\n-- [ ] Add `kind: \"leaf\" | \"zone\"` prop to FocusScope (default `\"leaf\"`)\n-- [ ] Internally delegate to `<Focusable>` or `<FocusZone>` based on `kind`\n-- [ ] Remove key-generation, ResizeObserver, and registration logic from FocusScope (now in primitives)\n-- [ ] Keep CommandScopeProvider, FocusHighlight, context menu, entity-focus behaviors\n-- [ ] Pass `navOverride` through to the primitive\n-- [ ] Ensure existing `<FocusScope moniker=\"...\">` call sites compile and behave the same as before (default kind = leaf)\n-- [ ] Delete any `claimWhen` wiring that the primitives don't cover (final removal happens in card `01KNQY1GQ9...`)\n-\n-## Acceptance Criteria\n-- [ ] `<FocusScope>` wraps exactly one primitive based on `kind`\n-- [ ] Every existing call site of `<FocusScope>` compiles unchanged (default kind is `\"leaf\"`)\n-- [ ] `moniker` prop is typed as `Moniker` (branded); TS rejects raw `string` at call sites\n-- [ ] CommandScopeProvider, context menu, FocusHighlight behaviors unchanged\n-- [ ] Click-to-focus now goes through the primitive's `spatial_focus` invoke (not the old `setFocus(moniker)` path)\n-- [ ] No duplicate registration — primitive registers once, FocusScope does not register separately\n-- [ ] `pnpm vitest run` passes\n-\n-## Tests\n-- [ ] `focus-scope.test.tsx` — `<FocusScope moniker={Moniker(...)}>` renders a `<Focusable>` when `kind` omitted\n-- [ ] `focus-scope.test.tsx` — `<FocusScope kind=\"zone\">` renders a `<FocusZone>`\n-- [ ] `focus-scope.test.tsx` — `navOverride` prop forwarded to the primitive\n-- [ ] `focus-scope.test.tsx` — CommandScopeProvider sees `commands` prop as before\n-- [ ] `focus-scope.test.tsx` — click on the rendered element invokes `spatial_focus` with the primitive's key\n-- [ ] Existing tests in `focus-scope.test.tsx` still pass without modification beyond type updates\n-- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPZWZE5B1JJVFKS68YZGGARB","timestamp":"2026-04-24T14:05:54.731749Z","entity_type":"task","entity_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","op":"create","changes":[["project",{"kind":"set","value":"spatial-nav"}],["title",{"kind":"set","value":"FocusScope wrapper refactor: entity-aware composite over the primitives"}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"ff8d80"}],["depends_on",{"kind":"set","value":["01KPZWY4B79QJFF6XFEG1JR4RJ"]}],["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\nRefactor the existing `<FocusScope>` (in `kanban-app/ui/src/components/focus-scope.tsx`) so that it composes the primitives (`<Focusable>` or `<FocusZone>`) added in the primitives card. `FocusScope` keeps its existing entity-oriented responsibilities — CommandScopeProvider, click-to-focus routing, data attributes, context menu, focus bar — but delegates **registration / rect measurement / keys** to whichever primitive fits.\n\n### Peer-type summary\n\n| Rust type           | React component       | Registration           |\n|---------------------|-----------------------|------------------------|\n| `struct Focusable`  | `<Focusable>`         | leaf, direct           |\n| `struct FocusZone`  | `<FocusZone>`         | zone, direct           |\n| `struct FocusLayer` | `<FocusLayer>`        | layer, direct          |\n| `enum FocusScope`   | `<FocusScope>`        | composite → wraps primitive |\n\nOn the Rust side, `enum FocusScope` is the stored sum type. On React, `<FocusScope>` is the entity-aware wrapper most call sites use. Direct use of primitives is also fine for non-entity chrome.\n\n### Props\n\n```typescript\nimport type { Moniker, NavOverride } from \"@/types/spatial\";\n\ninterface FocusScopeProps {\n  moniker: Moniker;\n  kind?: \"leaf\" | \"zone\";          // default \"leaf\"\n  navOverride?: NavOverride;\n  commands?: CommandDef[];          // existing — passed to CommandScopeProvider\n  showFocusBar?: boolean;           // existing\n  handleEvents?: boolean;           // existing\n  children: React.ReactNode;\n  // plus existing passthrough HTMLAttributes\n}\n```\n\n### Structure\n\n```typescript\nexport function FocusScope({ moniker, kind = \"leaf\", navOverride, commands, ...rest }: FocusScopeProps) {\n  const Primitive = kind === \"zone\" ? FocusZone : Focusable;\n\n  return (\n    <FocusScopeContext.Provider value={moniker}>\n      <CommandScopeProvider commands={commands ?? EMPTY_COMMANDS}>\n        <Primitive moniker={moniker} navOverride={navOverride} {...rest}>\n          {/* existing focus-bar / context-menu chrome */}\n          <FocusHighlight>{children}</FocusHighlight>\n        </Primitive>\n      </CommandScopeProvider>\n    </FocusScopeContext.Provider>\n  );\n}\n```\n\nThe primitive handles: key generation, rect measurement, Tauri registration, claim callback, data-moniker/data-focused attrs, click-to-focus. `FocusScope` stacks the entity plumbing on top.\n\n### What moves where\n\n| Concern                                     | Before (in FocusScope) | After                                   |\n|---------------------------------------------|------------------------|-----------------------------------------|\n| ULID key generation                         | FocusScope             | Primitive                               |\n| ResizeObserver + `spatial_register_*`       | (did not exist)        | Primitive                               |\n| Unregister on unmount                       | (did not exist)        | Primitive                               |\n| data-moniker / data-focused attrs           | FocusScope             | Primitive                               |\n| Click → focus dispatch                      | FocusScope             | Primitive (`spatial_focus` invoke)      |\n| CommandScopeProvider                        | FocusScope             | FocusScope (unchanged)                  |\n| FocusHighlight / focus bar visual           | FocusScope             | FocusScope (unchanged)                  |\n| Context menu                                | FocusScope             | FocusScope (unchanged)                  |\n| Legacy `claimWhen` predicate registration   | FocusScope             | **Deleted** (see card `01KNQY1GQ9...`)  |\n| `claimWhen` → `navOverride` rename          | —                      | **FocusScope passes through to primitive** |\n\n### Usage parity\n\nAfter refactor, every existing call site of `<FocusScope moniker=\"...\">` keeps working. Zone-ness is opt-in via the new `kind` prop:\n\n```tsx\n// Leaf — default, unchanged for existing call sites\n<FocusScope moniker={Moniker(`task:${id}.title`)}>\n  {title}\n</FocusScope>\n\n// Zone — new, for columns, cards, field rows\n<FocusScope moniker={Moniker(`task:${id}`)} kind=\"zone\">\n  <TaskCardBody />\n</FocusScope>\n\n// Non-entity chrome can skip FocusScope entirely and use primitives:\n<FocusZone moniker={Moniker(\"ui:toolbar.actions\")}>\n  <FiltersButton />\n  <NewTaskButton />\n</FocusZone>\n```\n\n### Subtasks\n- [ ] Add `kind: \"leaf\" | \"zone\"` prop to FocusScope (default `\"leaf\"`)\n- [ ] Internally delegate to `<Focusable>` or `<FocusZone>` based on `kind`\n- [ ] Remove key-generation, ResizeObserver, and registration logic from FocusScope (now in primitives)\n- [ ] Keep CommandScopeProvider, FocusHighlight, context menu, entity-focus behaviors\n- [ ] Pass `navOverride` through to the primitive\n- [ ] Ensure existing `<FocusScope moniker=\"...\">` call sites compile and behave the same as before (default kind = leaf)\n- [ ] Delete any `claimWhen` wiring that the primitives don't cover (final removal happens in card `01KNQY1GQ9...`)\n\n## Acceptance Criteria\n- [ ] `<FocusScope>` wraps exactly one primitive based on `kind`\n- [ ] Every existing call site of `<FocusScope>` compiles unchanged (default kind is `\"leaf\"`)\n- [ ] `moniker` prop is typed as `Moniker` (branded); TS rejects raw `string` at call sites\n- [ ] CommandScopeProvider, context menu, FocusHighlight behaviors unchanged\n- [ ] Click-to-focus now goes through the primitive's `spatial_focus` invoke (not the old `setFocus(moniker)` path)\n- [ ] No duplicate registration — primitive registers once, FocusScope does not register separately\n- [ ] `pnpm vitest run` passes\n\n## Tests\n- [ ] `focus-scope.test.tsx` — `<FocusScope moniker={Moniker(...)}>` renders a `<Focusable>` when `kind` omitted\n- [ ] `focus-scope.test.tsx` — `<FocusScope kind=\"zone\">` renders a `<FocusZone>`\n- [ ] `focus-scope.test.tsx` — `navOverride` prop forwarded to the primitive\n- [ ] `focus-scope.test.tsx` — CommandScopeProvider sees `commands` prop as before\n- [ ] `focus-scope.test.tsx` — click on the rendered element invokes `spatial_focus` with the primitive's key\n- [ ] Existing tests in `focus-scope.test.tsx` still pass without modification beyond type updates\n- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n## Workflow\n- Use `/tdd` — write failing tests first, then implement to make them pass."}]]}
{"id":"01KQ4XHPXP0097D4Y3AECAQ6KE","timestamp":"2026-04-26T12:52:05.686884Z","op":"update","item_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KPZWY4B79QJFF6XFEG1JR4RJ\n-position_column: todo\n-position_ordinal: ff8d80\n+position_column: doing\n+position_ordinal: '8380'\n project: spatial-nav\n title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KPZWY4B79QJFF6XFEG1JR4RJ\n-position_column: doing\n-position_ordinal: '8380'\n+position_column: todo\n+position_ordinal: ff8d80\n project: spatial-nav\n title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n ---\n","transaction_id":null}
{"id":"01KQ4XHPXQVAR0HBF0JSB1T98Z","timestamp":"2026-04-26T12:52:05.687808Z","entity_type":"task","entity_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ff8d80\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+ff8d80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ50C2FRCCAJF10ZJ69C7A2T","timestamp":"2026-04-26T13:41:26.648988Z","op":"update","item_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","forward_patch":"--- original\n+++ modified\n@@ -101,31 +101,51 @@\n ```\n\n ### Subtasks\n-- [ ] Add `kind: \"leaf\" | \"zone\"` prop to FocusScope (default `\"leaf\"`)\n-- [ ] Internally delegate to `<Focusable>` or `<FocusZone>` based on `kind`\n-- [ ] Remove key-generation, ResizeObserver, and registration logic from FocusScope (now in primitives)\n-- [ ] Keep CommandScopeProvider, FocusHighlight, context menu, entity-focus behaviors\n-- [ ] Pass `navOverride` through to the primitive\n-- [ ] Ensure existing `<FocusScope moniker=\"...\">` call sites compile and behave the same as before (default kind = leaf)\n-- [ ] Delete any `claimWhen` wiring that the primitives don't cover (final removal happens in card `01KNQY1GQ9...`)\n+- [x] Add `kind: \"leaf\" | \"zone\"` prop to FocusScope (default `\"leaf\"`)\n+- [x] Internally delegate to `<Focusable>` or `<FocusZone>` based on `kind`\n+- [x] Remove key-generation, ResizeObserver, and registration logic from FocusScope (now in primitives)\n+- [x] Keep CommandScopeProvider, FocusHighlight, context menu, entity-focus behaviors\n+- [x] Pass `navOverride` through to the primitive\n+- [x] Ensure existing `<FocusScope moniker=\"...\">` call sites compile and behave the same as before (default kind = leaf)\n+- [x] Delete any `claimWhen` wiring that the primitives don't cover (final removal happens in card `01KNQY1GQ9...`)\n\n ## Acceptance Criteria\n-- [ ] `<FocusScope>` wraps exactly one primitive based on `kind`\n-- [ ] Every existing call site of `<FocusScope>` compiles unchanged (default kind is `\"leaf\"`)\n-- [ ] `moniker` prop is typed as `Moniker` (branded); TS rejects raw `string` at call sites\n-- [ ] CommandScopeProvider, context menu, FocusHighlight behaviors unchanged\n-- [ ] Click-to-focus now goes through the primitive's `spatial_focus` invoke (not the old `setFocus(moniker)` path)\n-- [ ] No duplicate registration — primitive registers once, FocusScope does not register separately\n-- [ ] `pnpm vitest run` passes\n+- [x] `<FocusScope>` wraps exactly one primitive based on `kind`\n+- [x] Every existing call site of `<FocusScope>` compiles unchanged (default kind is `\"leaf\"`)\n+- [x] `moniker` prop is typed as `Moniker` (branded); TS rejects raw `string` at call sites\n+- [x] CommandScopeProvider, context menu, FocusHighlight behaviors unchanged\n+- [x] Click-to-focus now goes through the primitive's `spatial_focus` invoke (not the old `setFocus(moniker)` path)\n+- [x] No duplicate registration — primitive registers once, FocusScope does not register separately\n+- [x] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `focus-scope.test.tsx` — `<FocusScope moniker={Moniker(...)}>` renders a `<Focusable>` when `kind` omitted\n-- [ ] `focus-scope.test.tsx` — `<FocusScope kind=\"zone\">` renders a `<FocusZone>`\n-- [ ] `focus-scope.test.tsx` — `navOverride` prop forwarded to the primitive\n-- [ ] `focus-scope.test.tsx` — CommandScopeProvider sees `commands` prop as before\n-- [ ] `focus-scope.test.tsx` — click on the rendered element invokes `spatial_focus` with the primitive's key\n-- [ ] Existing tests in `focus-scope.test.tsx` still pass without modification beyond type updates\n-- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n+- [x] `focus-scope.test.tsx` — `<FocusScope moniker={Moniker(...)}>` renders a `<Focusable>` when `kind` omitted\n+- [x] `focus-scope.test.tsx` — `<FocusScope kind=\"zone\">` renders a `<FocusZone>`\n+- [x] `focus-scope.test.tsx` — `navOverride` prop forwarded to the primitive\n+- [x] `focus-scope.test.tsx` — CommandScopeProvider sees `commands` prop as before\n+- [x] `focus-scope.test.tsx` — click on the rendered element invokes `spatial_focus` with the primitive's key\n+- [x] Existing tests in `focus-scope.test.tsx` still pass without modification beyond type updates\n+- [x] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Implementation Notes (2026-04-26)\n+\n+The implementation was already in place in working tree when this task was picked up. Verification work:\n+\n+1. Verified `focus-scope.tsx` correctly composes `<Focusable>` (kind=\"leaf\") or `<FocusZone>` (kind=\"zone\"), with a no-FocusLayer fallback to a plain div. Implementation matches spec.\n+2. Added five new tests in `focus-scope.test.tsx` under a `primitive composition` describe block:\n+   - renders `<Focusable>` when `kind` is omitted (verifies `spatial_register_focusable` call)\n+   - renders `<FocusZone>` when `kind=\"zone\"` (verifies `spatial_register_zone` call)\n+   - forwards `navOverride` to the primitive registration's `overrides` field\n+   - click invokes `spatial_focus` with the primitive's minted key\n+   - falls back to a plain div when no `<FocusLayer>` ancestor is mounted\n+3. The existing 25 tests still pass with only type-update changes (raw string monikers wrapped with `asMoniker(...)`).\n+4. Updated all call sites listed in the task to use `asMoniker(...)`:\n+   - `entity-card.tsx`, `column-view.tsx`, `data-table.tsx`, `entity-inspector.tsx`, `mention-view.tsx`, `inspector-focus-bridge.tsx`, `command-palette.tsx`, `avatar.tsx`, `board-view.tsx`, `attachment-display.tsx`\n+5. Updated test files: `app-shell.test.tsx`, `attachment-display.test.tsx`, `badge-list-nav.test.tsx`, `mention-view.test.tsx`, `entity-focus-context.test.tsx`.\n+6. `entity-card.tsx` simplified: switched from `<FocusScope>` + `SpatialZoneIfAvailable` (which double-registered as both leaf and zone in production) to `<FocusScope kind=\"zone\">`. The `<FocusScope>` composite now does the right thing in both production (composes `<FocusZone>`) and test isolation (falls back to plain div without a `<FocusLayer>` ancestor) — no manual zone-availability check needed. Removes a workaround the new composite eliminated.\n+7. `store-container.tsx` switched from `<FocusScope renderContainer={false}>` to `<CommandScopeProvider>`. The store scope is purely structural (contributes a moniker to the scope chain so the backend resolves the store path) — there is no entity to focus, no spatial-nav rect, and no DOM surface. Sibling structural containers (`AppModeContainer`, `WindowContainer`, `BoardContainer`) follow the same pattern. This also avoids transitively pulling the spatial primitives' Tauri-event imports into tests that mount `StoreContainer` in isolation.\n+\n+Test result: 138 test files, 1513 tests, all passing. `tsc --noEmit` clean.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -101,51 +101,31 @@\n ```\n\n ### Subtasks\n-- [x] Add `kind: \"leaf\" | \"zone\"` prop to FocusScope (default `\"leaf\"`)\n-- [x] Internally delegate to `<Focusable>` or `<FocusZone>` based on `kind`\n-- [x] Remove key-generation, ResizeObserver, and registration logic from FocusScope (now in primitives)\n-- [x] Keep CommandScopeProvider, FocusHighlight, context menu, entity-focus behaviors\n-- [x] Pass `navOverride` through to the primitive\n-- [x] Ensure existing `<FocusScope moniker=\"...\">` call sites compile and behave the same as before (default kind = leaf)\n-- [x] Delete any `claimWhen` wiring that the primitives don't cover (final removal happens in card `01KNQY1GQ9...`)\n+- [ ] Add `kind: \"leaf\" | \"zone\"` prop to FocusScope (default `\"leaf\"`)\n+- [ ] Internally delegate to `<Focusable>` or `<FocusZone>` based on `kind`\n+- [ ] Remove key-generation, ResizeObserver, and registration logic from FocusScope (now in primitives)\n+- [ ] Keep CommandScopeProvider, FocusHighlight, context menu, entity-focus behaviors\n+- [ ] Pass `navOverride` through to the primitive\n+- [ ] Ensure existing `<FocusScope moniker=\"...\">` call sites compile and behave the same as before (default kind = leaf)\n+- [ ] Delete any `claimWhen` wiring that the primitives don't cover (final removal happens in card `01KNQY1GQ9...`)\n\n ## Acceptance Criteria\n-- [x] `<FocusScope>` wraps exactly one primitive based on `kind`\n-- [x] Every existing call site of `<FocusScope>` compiles unchanged (default kind is `\"leaf\"`)\n-- [x] `moniker` prop is typed as `Moniker` (branded); TS rejects raw `string` at call sites\n-- [x] CommandScopeProvider, context menu, FocusHighlight behaviors unchanged\n-- [x] Click-to-focus now goes through the primitive's `spatial_focus` invoke (not the old `setFocus(moniker)` path)\n-- [x] No duplicate registration — primitive registers once, FocusScope does not register separately\n-- [x] `pnpm vitest run` passes\n+- [ ] `<FocusScope>` wraps exactly one primitive based on `kind`\n+- [ ] Every existing call site of `<FocusScope>` compiles unchanged (default kind is `\"leaf\"`)\n+- [ ] `moniker` prop is typed as `Moniker` (branded); TS rejects raw `string` at call sites\n+- [ ] CommandScopeProvider, context menu, FocusHighlight behaviors unchanged\n+- [ ] Click-to-focus now goes through the primitive's `spatial_focus` invoke (not the old `setFocus(moniker)` path)\n+- [ ] No duplicate registration — primitive registers once, FocusScope does not register separately\n+- [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [x] `focus-scope.test.tsx` — `<FocusScope moniker={Moniker(...)}>` renders a `<Focusable>` when `kind` omitted\n-- [x] `focus-scope.test.tsx` — `<FocusScope kind=\"zone\">` renders a `<FocusZone>`\n-- [x] `focus-scope.test.tsx` — `navOverride` prop forwarded to the primitive\n-- [x] `focus-scope.test.tsx` — CommandScopeProvider sees `commands` prop as before\n-- [x] `focus-scope.test.tsx` — click on the rendered element invokes `spatial_focus` with the primitive's key\n-- [x] Existing tests in `focus-scope.test.tsx` still pass without modification beyond type updates\n-- [x] Run `cd kanban-app/ui && npx vitest run` — all pass\n+- [ ] `focus-scope.test.tsx` — `<FocusScope moniker={Moniker(...)}>` renders a `<Focusable>` when `kind` omitted\n+- [ ] `focus-scope.test.tsx` — `<FocusScope kind=\"zone\">` renders a `<FocusZone>`\n+- [ ] `focus-scope.test.tsx` — `navOverride` prop forwarded to the primitive\n+- [ ] `focus-scope.test.tsx` — CommandScopeProvider sees `commands` prop as before\n+- [ ] `focus-scope.test.tsx` — click on the rendered element invokes `spatial_focus` with the primitive's key\n+- [ ] Existing tests in `focus-scope.test.tsx` still pass without modification beyond type updates\n+- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Implementation Notes (2026-04-26)\n-\n-The implementation was already in place in working tree when this task was picked up. Verification work:\n-\n-1. Verified `focus-scope.tsx` correctly composes `<Focusable>` (kind=\"leaf\") or `<FocusZone>` (kind=\"zone\"), with a no-FocusLayer fallback to a plain div. Implementation matches spec.\n-2. Added five new tests in `focus-scope.test.tsx` under a `primitive composition` describe block:\n-   - renders `<Focusable>` when `kind` is omitted (verifies `spatial_register_focusable` call)\n-   - renders `<FocusZone>` when `kind=\"zone\"` (verifies `spatial_register_zone` call)\n-   - forwards `navOverride` to the primitive registration's `overrides` field\n-   - click invokes `spatial_focus` with the primitive's minted key\n-   - falls back to a plain div when no `<FocusLayer>` ancestor is mounted\n-3. The existing 25 tests still pass with only type-update changes (raw string monikers wrapped with `asMoniker(...)`).\n-4. Updated all call sites listed in the task to use `asMoniker(...)`:\n-   - `entity-card.tsx`, `column-view.tsx`, `data-table.tsx`, `entity-inspector.tsx`, `mention-view.tsx`, `inspector-focus-bridge.tsx`, `command-palette.tsx`, `avatar.tsx`, `board-view.tsx`, `attachment-display.tsx`\n-5. Updated test files: `app-shell.test.tsx`, `attachment-display.test.tsx`, `badge-list-nav.test.tsx`, `mention-view.test.tsx`, `entity-focus-context.test.tsx`.\n-6. `entity-card.tsx` simplified: switched from `<FocusScope>` + `SpatialZoneIfAvailable` (which double-registered as both leaf and zone in production) to `<FocusScope kind=\"zone\">`. The `<FocusScope>` composite now does the right thing in both production (composes `<FocusZone>`) and test isolation (falls back to plain div without a `<FocusLayer>` ancestor) — no manual zone-availability check needed. Removes a workaround the new composite eliminated.\n-7. `store-container.tsx` switched from `<FocusScope renderContainer={false}>` to `<CommandScopeProvider>`. The store scope is purely structural (contributes a moniker to the scope chain so the backend resolves the store path) — there is no entity to focus, no spatial-nav rect, and no DOM surface. Sibling structural containers (`AppModeContainer`, `WindowContainer`, `BoardContainer`) follow the same pattern. This also avoids transitively pulling the spatial primitives' Tauri-event imports into tests that mount `StoreContainer` in isolation.\n-\n-Test result: 138 test files, 1513 tests, all passing. `tsc --noEmit` clean.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ50C2FSPNP947KR55ZFBK6F","timestamp":"2026-04-26T13:41:26.649733Z","entity_type":"task","entity_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -91,31 +91,51 @@\n ```\n\n ### Subtasks\n-- [ ] Add `kind: \"leaf\" | \"zone\"` prop to FocusScope (default `\"leaf\"`)\n-- [ ] Internally delegate to `<Focusable>` or `<FocusZone>` based on `kind`\n-- [ ] Remove key-generation, ResizeObserver, and registration logic from FocusScope (now in primitives)\n-- [ ] Keep CommandScopeProvider, FocusHighlight, context menu, entity-focus behaviors\n-- [ ] Pass `navOverride` through to the primitive\n-- [ ] Ensure existing `<FocusScope moniker=\"...\">` call sites compile and behave the same as before (default kind = leaf)\n-- [ ] Delete any `claimWhen` wiring that the primitives don't cover (final removal happens in card `01KNQY1GQ9...`)\n+- [x] Add `kind: \"leaf\" | \"zone\"` prop to FocusScope (default `\"leaf\"`)\n+- [x] Internally delegate to `<Focusable>` or `<FocusZone>` based on `kind`\n+- [x] Remove key-generation, ResizeObserver, and registration logic from FocusScope (now in primitives)\n+- [x] Keep CommandScopeProvider, FocusHighlight, context menu, entity-focus behaviors\n+- [x] Pass `navOverride` through to the primitive\n+- [x] Ensure existing `<FocusScope moniker=\"...\">` call sites compile and behave the same as before (default kind = leaf)\n+- [x] Delete any `claimWhen` wiring that the primitives don't cover (final removal happens in card `01KNQY1GQ9...`)\n\n ## Acceptance Criteria\n-- [ ] `<FocusScope>` wraps exactly one primitive based on `kind`\n-- [ ] Every existing call site of `<FocusScope>` compiles unchanged (default kind is `\"leaf\"`)\n-- [ ] `moniker` prop is typed as `Moniker` (branded); TS rejects raw `string` at call sites\n-- [ ] CommandScopeProvider, context menu, FocusHighlight behaviors unchanged\n-- [ ] Click-to-focus now goes through the primitive's `spatial_focus` invoke (not the old `setFocus(moniker)` path)\n-- [ ] No duplicate registration — primitive registers once, FocusScope does not register separately\n-- [ ] `pnpm vitest run` passes\n+- [x] `<FocusScope>` wraps exactly one primitive based on `kind`\n+- [x] Every existing call site of `<FocusScope>` compiles unchanged (default kind is `\"leaf\"`)\n+- [x] `moniker` prop is typed as `Moniker` (branded); TS rejects raw `string` at call sites\n+- [x] CommandScopeProvider, context menu, FocusHighlight behaviors unchanged\n+- [x] Click-to-focus now goes through the primitive's `spatial_focus` invoke (not the old `setFocus(moniker)` path)\n+- [x] No duplicate registration — primitive registers once, FocusScope does not register separately\n+- [x] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `focus-scope.test.tsx` — `<FocusScope moniker={Moniker(...)}>` renders a `<Focusable>` when `kind` omitted\n-- [ ] `focus-scope.test.tsx` — `<FocusScope kind=\"zone\">` renders a `<FocusZone>`\n-- [ ] `focus-scope.test.tsx` — `navOverride` prop forwarded to the primitive\n-- [ ] `focus-scope.test.tsx` — CommandScopeProvider sees `commands` prop as before\n-- [ ] `focus-scope.test.tsx` — click on the rendered element invokes `spatial_focus` with the primitive's key\n-- [ ] Existing tests in `focus-scope.test.tsx` still pass without modification beyond type updates\n-- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n+- [x] `focus-scope.test.tsx` — `<FocusScope moniker={Moniker(...)}>` renders a `<Focusable>` when `kind` omitted\n+- [x] `focus-scope.test.tsx` — `<FocusScope kind=\"zone\">` renders a `<FocusZone>`\n+- [x] `focus-scope.test.tsx` — `navOverride` prop forwarded to the primitive\n+- [x] `focus-scope.test.tsx` — CommandScopeProvider sees `commands` prop as before\n+- [x] `focus-scope.test.tsx` — click on the rendered element invokes `spatial_focus` with the primitive's key\n+- [x] Existing tests in `focus-scope.test.tsx` still pass without modification beyond type updates\n+- [x] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Implementation Notes (2026-04-26)\n+\n+The implementation was already in place in working tree when this task was picked up. Verification work:\n+\n+1. Verified `focus-scope.tsx` correctly composes `<Focusable>` (kind=\"leaf\") or `<FocusZone>` (kind=\"zone\"), with a no-FocusLayer fallback to a plain div. Implementation matches spec.\n+2. Added five new tests in `focus-scope.test.tsx` under a `primitive composition` describe block:\n+   - renders `<Focusable>` when `kind` is omitted (verifies `spatial_register_focusable` call)\n+   - renders `<FocusZone>` when `kind=\"zone\"` (verifies `spatial_register_zone` call)\n+   - forwards `navOverride` to the primitive registration's `overrides` field\n+   - click invokes `spatial_focus` with the primitive's minted key\n+   - falls back to a plain div when no `<FocusLayer>` ancestor is mounted\n+3. The existing 25 tests still pass with only type-update changes (raw string monikers wrapped with `asMoniker(...)`).\n+4. Updated all call sites listed in the task to use `asMoniker(...)`:\n+   - `entity-card.tsx`, `column-view.tsx`, `data-table.tsx`, `entity-inspector.tsx`, `mention-view.tsx`, `inspector-focus-bridge.tsx`, `command-palette.tsx`, `avatar.tsx`, `board-view.tsx`, `attachment-display.tsx`\n+5. Updated test files: `app-shell.test.tsx`, `attachment-display.test.tsx`, `badge-list-nav.test.tsx`, `mention-view.test.tsx`, `entity-focus-context.test.tsx`.\n+6. `entity-card.tsx` simplified: switched from `<FocusScope>` + `SpatialZoneIfAvailable` (which double-registered as both leaf and zone in production) to `<FocusScope kind=\"zone\">`. The `<FocusScope>` composite now does the right thing in both production (composes `<FocusZone>`) and test isolation (falls back to plain div without a `<FocusLayer>` ancestor) — no manual zone-availability check needed. Removes a workaround the new composite eliminated.\n+7. `store-container.tsx` switched from `<FocusScope renderContainer={false}>` to `<CommandScopeProvider>`. The store scope is purely structural (contributes a moniker to the scope chain so the backend resolves the store path) — there is no entity to focus, no spatial-nav rect, and no DOM surface. Sibling structural containers (`AppModeContainer`, `WindowContainer`, `BoardContainer`) follow the same pattern. This also avoids transitively pulling the spatial primitives' Tauri-event imports into tests that mount `StoreContainer` in isolation.\n+\n+Test result: 138 test files, 1513 tests, all passing. `tsc --noEmit` clean.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -91,51 +91,31 @@\n ```\n\n ### Subtasks\n-- [x] Add `kind: \"leaf\" | \"zone\"` prop to FocusScope (default `\"leaf\"`)\n-- [x] Internally delegate to `<Focusable>` or `<FocusZone>` based on `kind`\n-- [x] Remove key-generation, ResizeObserver, and registration logic from FocusScope (now in primitives)\n-- [x] Keep CommandScopeProvider, FocusHighlight, context menu, entity-focus behaviors\n-- [x] Pass `navOverride` through to the primitive\n-- [x] Ensure existing `<FocusScope moniker=\"...\">` call sites compile and behave the same as before (default kind = leaf)\n-- [x] Delete any `claimWhen` wiring that the primitives don't cover (final removal happens in card `01KNQY1GQ9...`)\n+- [ ] Add `kind: \"leaf\" | \"zone\"` prop to FocusScope (default `\"leaf\"`)\n+- [ ] Internally delegate to `<Focusable>` or `<FocusZone>` based on `kind`\n+- [ ] Remove key-generation, ResizeObserver, and registration logic from FocusScope (now in primitives)\n+- [ ] Keep CommandScopeProvider, FocusHighlight, context menu, entity-focus behaviors\n+- [ ] Pass `navOverride` through to the primitive\n+- [ ] Ensure existing `<FocusScope moniker=\"...\">` call sites compile and behave the same as before (default kind = leaf)\n+- [ ] Delete any `claimWhen` wiring that the primitives don't cover (final removal happens in card `01KNQY1GQ9...`)\n\n ## Acceptance Criteria\n-- [x] `<FocusScope>` wraps exactly one primitive based on `kind`\n-- [x] Every existing call site of `<FocusScope>` compiles unchanged (default kind is `\"leaf\"`)\n-- [x] `moniker` prop is typed as `Moniker` (branded); TS rejects raw `string` at call sites\n-- [x] CommandScopeProvider, context menu, FocusHighlight behaviors unchanged\n-- [x] Click-to-focus now goes through the primitive's `spatial_focus` invoke (not the old `setFocus(moniker)` path)\n-- [x] No duplicate registration — primitive registers once, FocusScope does not register separately\n-- [x] `pnpm vitest run` passes\n+- [ ] `<FocusScope>` wraps exactly one primitive based on `kind`\n+- [ ] Every existing call site of `<FocusScope>` compiles unchanged (default kind is `\"leaf\"`)\n+- [ ] `moniker` prop is typed as `Moniker` (branded); TS rejects raw `string` at call sites\n+- [ ] CommandScopeProvider, context menu, FocusHighlight behaviors unchanged\n+- [ ] Click-to-focus now goes through the primitive's `spatial_focus` invoke (not the old `setFocus(moniker)` path)\n+- [ ] No duplicate registration — primitive registers once, FocusScope does not register separately\n+- [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [x] `focus-scope.test.tsx` — `<FocusScope moniker={Moniker(...)}>` renders a `<Focusable>` when `kind` omitted\n-- [x] `focus-scope.test.tsx` — `<FocusScope kind=\"zone\">` renders a `<FocusZone>`\n-- [x] `focus-scope.test.tsx` — `navOverride` prop forwarded to the primitive\n-- [x] `focus-scope.test.tsx` — CommandScopeProvider sees `commands` prop as before\n-- [x] `focus-scope.test.tsx` — click on the rendered element invokes `spatial_focus` with the primitive's key\n-- [x] Existing tests in `focus-scope.test.tsx` still pass without modification beyond type updates\n-- [x] Run `cd kanban-app/ui && npx vitest run` — all pass\n+- [ ] `focus-scope.test.tsx` — `<FocusScope moniker={Moniker(...)}>` renders a `<Focusable>` when `kind` omitted\n+- [ ] `focus-scope.test.tsx` — `<FocusScope kind=\"zone\">` renders a `<FocusZone>`\n+- [ ] `focus-scope.test.tsx` — `navOverride` prop forwarded to the primitive\n+- [ ] `focus-scope.test.tsx` — CommandScopeProvider sees `commands` prop as before\n+- [ ] `focus-scope.test.tsx` — click on the rendered element invokes `spatial_focus` with the primitive's key\n+- [ ] Existing tests in `focus-scope.test.tsx` still pass without modification beyond type updates\n+- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Implementation Notes (2026-04-26)\n-\n-The implementation was already in place in working tree when this task was picked up. Verification work:\n-\n-1. Verified `focus-scope.tsx` correctly composes `<Focusable>` (kind=\"leaf\") or `<FocusZone>` (kind=\"zone\"), with a no-FocusLayer fallback to a plain div. Implementation matches spec.\n-2. Added five new tests in `focus-scope.test.tsx` under a `primitive composition` describe block:\n-   - renders `<Focusable>` when `kind` is omitted (verifies `spatial_register_focusable` call)\n-   - renders `<FocusZone>` when `kind=\"zone\"` (verifies `spatial_register_zone` call)\n-   - forwards `navOverride` to the primitive registration's `overrides` field\n-   - click invokes `spatial_focus` with the primitive's minted key\n-   - falls back to a plain div when no `<FocusLayer>` ancestor is mounted\n-3. The existing 25 tests still pass with only type-update changes (raw string monikers wrapped with `asMoniker(...)`).\n-4. Updated all call sites listed in the task to use `asMoniker(...)`:\n-   - `entity-card.tsx`, `column-view.tsx`, `data-table.tsx`, `entity-inspector.tsx`, `mention-view.tsx`, `inspector-focus-bridge.tsx`, `command-palette.tsx`, `avatar.tsx`, `board-view.tsx`, `attachment-display.tsx`\n-5. Updated test files: `app-shell.test.tsx`, `attachment-display.test.tsx`, `badge-list-nav.test.tsx`, `mention-view.test.tsx`, `entity-focus-context.test.tsx`.\n-6. `entity-card.tsx` simplified: switched from `<FocusScope>` + `SpatialZoneIfAvailable` (which double-registered as both leaf and zone in production) to `<FocusScope kind=\"zone\">`. The `<FocusScope>` composite now does the right thing in both production (composes `<FocusZone>`) and test isolation (falls back to plain div without a `<FocusLayer>` ancestor) — no manual zone-availability check needed. Removes a workaround the new composite eliminated.\n-7. `store-container.tsx` switched from `<FocusScope renderContainer={false}>` to `<CommandScopeProvider>`. The store scope is purely structural (contributes a moniker to the scope chain so the backend resolves the store path) — there is no entity to focus, no spatial-nav rect, and no DOM surface. Sibling structural containers (`AppModeContainer`, `WindowContainer`, `BoardContainer`) follow the same pattern. This also avoids transitively pulling the spatial primitives' Tauri-event imports into tests that mount `StoreContainer` in isolation.\n-\n-Test result: 138 test files, 1513 tests, all passing. `tsc --noEmit` clean.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ50CH77CQ4RMBY2ERYZSVE0","timestamp":"2026-04-26T13:41:41.735342Z","op":"update","item_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KPZWY4B79QJFF6XFEG1JR4RJ\n-position_column: doing\n-position_ordinal: '8380'\n+position_column: review\n+position_ordinal: '8480'\n project: spatial-nav\n title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KPZWY4B79QJFF6XFEG1JR4RJ\n-position_column: review\n-position_ordinal: '8480'\n+position_column: doing\n+position_ordinal: '8380'\n project: spatial-nav\n title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n ---\n","transaction_id":null}
{"id":"01KQ50CH78QDBS5G8G9NYAM2QS","timestamp":"2026-04-26T13:41:41.736384Z","entity_type":"task","entity_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+8480\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8480\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n"}]]}
{"id":"01KQ513EHKKVK6XHA2VETNSNDK","timestamp":"2026-04-26T13:54:12.659904Z","op":"update","item_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","forward_patch":"--- original\n+++ modified\n@@ -148,4 +148,10 @@\n 6. `entity-card.tsx` simplified: switched from `<FocusScope>` + `SpatialZoneIfAvailable` (which double-registered as both leaf and zone in production) to `<FocusScope kind=\"zone\">`. The `<FocusScope>` composite now does the right thing in both production (composes `<FocusZone>`) and test isolation (falls back to plain div without a `<FocusLayer>` ancestor) — no manual zone-availability check needed. Removes a workaround the new composite eliminated.\n 7. `store-container.tsx` switched from `<FocusScope renderContainer={false}>` to `<CommandScopeProvider>`. The store scope is purely structural (contributes a moniker to the scope chain so the backend resolves the store path) — there is no entity to focus, no spatial-nav rect, and no DOM surface. Sibling structural containers (`AppModeContainer`, `WindowContainer`, `BoardContainer`) follow the same pattern. This also avoids transitively pulling the spatial primitives' Tauri-event imports into tests that mount `StoreContainer` in isolation.\n\n-Test result: 138 test files, 1513 tests, all passing. `tsc --noEmit` clean.\n\\ No newline at end of file\n+Test result: 138 test files, 1513 tests, all passing. `tsc --noEmit` clean.\n+\n+## Review Findings (2026-04-26 08:53)\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/components/data-table.tsx:830` — Doc comment on `GridCellScope` claims `FocusScope` \"renders as a `<td>` element (via the underlying FocusHighlight)\". Both clauses are now stale: this refactor replaced the inner `FocusHighlight` with an in-file `FocusScopeBody` (which deliberately does not emit `data-focused` so the primitive owns it), and `FocusScope` does not render as a `<td>` — it renders inside the `TableCell`. Suggest: drop the parenthetical and reword to something like \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\"\n+- [ ] `kanban-app/ui/src/components/store-container.test.tsx:71,80` — Test name `\"does not render a wrapping container div (renderContainer=false)\"` and the inline comment \"FocusScope with renderContainer=false should not add a FocusHighlight wrapper\" both refer to the pre-refactor implementation. `StoreContainer` now uses `CommandScopeProvider` directly, so there is no `renderContainer` flag and no `FocusHighlight` involved. The assertion (`querySelector(\"[data-moniker]\") === null`) still holds for the right reason — `CommandScopeProvider` is a pure context provider — so the test is still meaningful, but the wording should be updated to reflect the new structure (e.g. `\"renders no DOM wrapper — CommandScopeProvider is context-only\"`).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -148,10 +148,4 @@\n 6. `entity-card.tsx` simplified: switched from `<FocusScope>` + `SpatialZoneIfAvailable` (which double-registered as both leaf and zone in production) to `<FocusScope kind=\"zone\">`. The `<FocusScope>` composite now does the right thing in both production (composes `<FocusZone>`) and test isolation (falls back to plain div without a `<FocusLayer>` ancestor) — no manual zone-availability check needed. Removes a workaround the new composite eliminated.\n 7. `store-container.tsx` switched from `<FocusScope renderContainer={false}>` to `<CommandScopeProvider>`. The store scope is purely structural (contributes a moniker to the scope chain so the backend resolves the store path) — there is no entity to focus, no spatial-nav rect, and no DOM surface. Sibling structural containers (`AppModeContainer`, `WindowContainer`, `BoardContainer`) follow the same pattern. This also avoids transitively pulling the spatial primitives' Tauri-event imports into tests that mount `StoreContainer` in isolation.\n\n-Test result: 138 test files, 1513 tests, all passing. `tsc --noEmit` clean.\n-\n-## Review Findings (2026-04-26 08:53)\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/components/data-table.tsx:830` — Doc comment on `GridCellScope` claims `FocusScope` \"renders as a `<td>` element (via the underlying FocusHighlight)\". Both clauses are now stale: this refactor replaced the inner `FocusHighlight` with an in-file `FocusScopeBody` (which deliberately does not emit `data-focused` so the primitive owns it), and `FocusScope` does not render as a `<td>` — it renders inside the `TableCell`. Suggest: drop the parenthetical and reword to something like \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\"\n-- [ ] `kanban-app/ui/src/components/store-container.test.tsx:71,80` — Test name `\"does not render a wrapping container div (renderContainer=false)\"` and the inline comment \"FocusScope with renderContainer=false should not add a FocusHighlight wrapper\" both refer to the pre-refactor implementation. `StoreContainer` now uses `CommandScopeProvider` directly, so there is no `renderContainer` flag and no `FocusHighlight` involved. The assertion (`querySelector(\"[data-moniker]\") === null`) still holds for the right reason — `CommandScopeProvider` is a pure context provider — so the test is still meaningful, but the wording should be updated to reflect the new structure (e.g. `\"renders no DOM wrapper — CommandScopeProvider is context-only\"`).\n\\ No newline at end of file\n+Test result: 138 test files, 1513 tests, all passing. `tsc --noEmit` clean.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ513EHM301VHEKBT9JGDYYN","timestamp":"2026-04-26T13:54:12.660634Z","entity_type":"task","entity_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -138,4 +138,10 @@\n 6. `entity-card.tsx` simplified: switched from `<FocusScope>` + `SpatialZoneIfAvailable` (which double-registered as both leaf and zone in production) to `<FocusScope kind=\"zone\">`. The `<FocusScope>` composite now does the right thing in both production (composes `<FocusZone>`) and test isolation (falls back to plain div without a `<FocusLayer>` ancestor) — no manual zone-availability check needed. Removes a workaround the new composite eliminated.\n 7. `store-container.tsx` switched from `<FocusScope renderContainer={false}>` to `<CommandScopeProvider>`. The store scope is purely structural (contributes a moniker to the scope chain so the backend resolves the store path) — there is no entity to focus, no spatial-nav rect, and no DOM surface. Sibling structural containers (`AppModeContainer`, `WindowContainer`, `BoardContainer`) follow the same pattern. This also avoids transitively pulling the spatial primitives' Tauri-event imports into tests that mount `StoreContainer` in isolation.\n\n-Test result: 138 test files, 1513 tests, all passing. `tsc --noEmit` clean.\n\\ No newline at end of file\n+Test result: 138 test files, 1513 tests, all passing. `tsc --noEmit` clean.\n+\n+## Review Findings (2026-04-26 08:53)\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/components/data-table.tsx:830` — Doc comment on `GridCellScope` claims `FocusScope` \"renders as a `<td>` element (via the underlying FocusHighlight)\". Both clauses are now stale: this refactor replaced the inner `FocusHighlight` with an in-file `FocusScopeBody` (which deliberately does not emit `data-focused` so the primitive owns it), and `FocusScope` does not render as a `<td>` — it renders inside the `TableCell`. Suggest: drop the parenthetical and reword to something like \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\"\n+- [ ] `kanban-app/ui/src/components/store-container.test.tsx:71,80` — Test name `\"does not render a wrapping container div (renderContainer=false)\"` and the inline comment \"FocusScope with renderContainer=false should not add a FocusHighlight wrapper\" both refer to the pre-refactor implementation. `StoreContainer` now uses `CommandScopeProvider` directly, so there is no `renderContainer` flag and no `FocusHighlight` involved. The assertion (`querySelector(\"[data-moniker]\") === null`) still holds for the right reason — `CommandScopeProvider` is a pure context provider — so the test is still meaningful, but the wording should be updated to reflect the new structure (e.g. `\"renders no DOM wrapper — CommandScopeProvider is context-only\"`).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -138,10 +138,4 @@\n 6. `entity-card.tsx` simplified: switched from `<FocusScope>` + `SpatialZoneIfAvailable` (which double-registered as both leaf and zone in production) to `<FocusScope kind=\"zone\">`. The `<FocusScope>` composite now does the right thing in both production (composes `<FocusZone>`) and test isolation (falls back to plain div without a `<FocusLayer>` ancestor) — no manual zone-availability check needed. Removes a workaround the new composite eliminated.\n 7. `store-container.tsx` switched from `<FocusScope renderContainer={false}>` to `<CommandScopeProvider>`. The store scope is purely structural (contributes a moniker to the scope chain so the backend resolves the store path) — there is no entity to focus, no spatial-nav rect, and no DOM surface. Sibling structural containers (`AppModeContainer`, `WindowContainer`, `BoardContainer`) follow the same pattern. This also avoids transitively pulling the spatial primitives' Tauri-event imports into tests that mount `StoreContainer` in isolation.\n\n-Test result: 138 test files, 1513 tests, all passing. `tsc --noEmit` clean.\n-\n-## Review Findings (2026-04-26 08:53)\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/components/data-table.tsx:830` — Doc comment on `GridCellScope` claims `FocusScope` \"renders as a `<td>` element (via the underlying FocusHighlight)\". Both clauses are now stale: this refactor replaced the inner `FocusHighlight` with an in-file `FocusScopeBody` (which deliberately does not emit `data-focused` so the primitive owns it), and `FocusScope` does not render as a `<td>` — it renders inside the `TableCell`. Suggest: drop the parenthetical and reword to something like \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\"\n-- [ ] `kanban-app/ui/src/components/store-container.test.tsx:71,80` — Test name `\"does not render a wrapping container div (renderContainer=false)\"` and the inline comment \"FocusScope with renderContainer=false should not add a FocusHighlight wrapper\" both refer to the pre-refactor implementation. `StoreContainer` now uses `CommandScopeProvider` directly, so there is no `renderContainer` flag and no `FocusHighlight` involved. The assertion (`querySelector(\"[data-moniker]\") === null`) still holds for the right reason — `CommandScopeProvider` is a pure context provider — so the test is still meaningful, but the wording should be updated to reflect the new structure (e.g. `\"renders no DOM wrapper — CommandScopeProvider is context-only\"`).\n\\ No newline at end of file\n+Test result: 138 test files, 1513 tests, all passing. `tsc --noEmit` clean.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ515DJSWGNSQQNPB9R1NSB3","timestamp":"2026-04-26T13:55:17.209097Z","op":"update","item_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KPZWY4B79QJFF6XFEG1JR4RJ\n-position_column: review\n-position_ordinal: '8480'\n+position_column: doing\n+position_ordinal: '8380'\n project: spatial-nav\n title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KPZWY4B79QJFF6XFEG1JR4RJ\n-position_column: doing\n-position_ordinal: '8380'\n+position_column: review\n+position_ordinal: '8480'\n project: spatial-nav\n title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n ---\n","transaction_id":null}
{"id":"01KQ515DJS8GKP2G9F3Q4QQCYA","timestamp":"2026-04-26T13:55:17.209674Z","entity_type":"task","entity_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8480\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+8480\n\\ No newline at end of file\n"}]]}
{"id":"01KQ51BB4GWD1D1MFD2KYSBSMN","timestamp":"2026-04-26T13:58:31.312764Z","op":"update","item_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","forward_patch":"--- original\n+++ modified\n@@ -153,5 +153,17 @@\n ## Review Findings (2026-04-26 08:53)\n\n ### Nits\n-- [ ] `kanban-app/ui/src/components/data-table.tsx:830` — Doc comment on `GridCellScope` claims `FocusScope` \"renders as a `<td>` element (via the underlying FocusHighlight)\". Both clauses are now stale: this refactor replaced the inner `FocusHighlight` with an in-file `FocusScopeBody` (which deliberately does not emit `data-focused` so the primitive owns it), and `FocusScope` does not render as a `<td>` — it renders inside the `TableCell`. Suggest: drop the parenthetical and reword to something like \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\"\n-- [ ] `kanban-app/ui/src/components/store-container.test.tsx:71,80` — Test name `\"does not render a wrapping container div (renderContainer=false)\"` and the inline comment \"FocusScope with renderContainer=false should not add a FocusHighlight wrapper\" both refer to the pre-refactor implementation. `StoreContainer` now uses `CommandScopeProvider` directly, so there is no `renderContainer` flag and no `FocusHighlight` involved. The assertion (`querySelector(\"[data-moniker]\") === null`) still holds for the right reason — `CommandScopeProvider` is a pure context provider — so the test is still meaningful, but the wording should be updated to reflect the new structure (e.g. `\"renders no DOM wrapper — CommandScopeProvider is context-only\"`).\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/components/data-table.tsx:830` — Doc comment on `GridCellScope` claims `FocusScope` \"renders as a `<td>` element (via the underlying FocusHighlight)\". Both clauses are now stale: this refactor replaced the inner `FocusHighlight` with an in-file `FocusScopeBody` (which deliberately does not emit `data-focused` so the primitive owns it), and `FocusScope` does not render as a `<td>` — it renders inside the `TableCell`. Suggest: drop the parenthetical and reword to something like \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\"\n+- [x] `kanban-app/ui/src/components/store-container.test.tsx:71,80` — Test name `\"does not render a wrapping container div (renderContainer=false)\"` and the inline comment \"FocusScope with renderContainer=false should not add a FocusHighlight wrapper\" both refer to the pre-refactor implementation. `StoreContainer` now uses `CommandScopeProvider` directly, so there is no `renderContainer` flag and no `FocusHighlight` involved. The assertion (`querySelector(\"[data-moniker]\") === null`) still holds for the right reason — `CommandScopeProvider` is a pure context provider — so the test is still meaningful, but the wording should be updated to reflect the new structure (e.g. `\"renders no DOM wrapper — CommandScopeProvider is context-only\"`).\n+\n+### Nit Resolution (2026-04-26 08:58)\n+\n+Both nits addressed:\n+\n+1. **`data-table.tsx`** — Replaced the stale doc comment on `GridCellScope` with the suggested wording: \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\" Param tags retained.\n+2. **`store-container.test.tsx`** — Renamed the test to `\"renders no DOM wrapper — CommandScopeProvider is context-only\"` and rewrote the inline comment to reflect that `StoreContainer` is now a pure context provider (`CommandScopeProvider`), not a `FocusScope` with `renderContainer=false`. The `querySelector(\"[data-moniker]\") === null` assertion is unchanged — it still asserts the right thing, just with accurate justification.\n+\n+Verification:\n+- `npx vitest run src/components/store-container.test.tsx src/components/data-table.test.tsx` — 14 / 14 pass.\n+- `npx vitest run` (full UI suite) — 138 files, 1513 tests, all passing (no regression).\n+- `npx tsc --noEmit` — clean.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -153,17 +153,5 @@\n ## Review Findings (2026-04-26 08:53)\n\n ### Nits\n-- [x] `kanban-app/ui/src/components/data-table.tsx:830` — Doc comment on `GridCellScope` claims `FocusScope` \"renders as a `<td>` element (via the underlying FocusHighlight)\". Both clauses are now stale: this refactor replaced the inner `FocusHighlight` with an in-file `FocusScopeBody` (which deliberately does not emit `data-focused` so the primitive owns it), and `FocusScope` does not render as a `<td>` — it renders inside the `TableCell`. Suggest: drop the parenthetical and reword to something like \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\"\n-- [x] `kanban-app/ui/src/components/store-container.test.tsx:71,80` — Test name `\"does not render a wrapping container div (renderContainer=false)\"` and the inline comment \"FocusScope with renderContainer=false should not add a FocusHighlight wrapper\" both refer to the pre-refactor implementation. `StoreContainer` now uses `CommandScopeProvider` directly, so there is no `renderContainer` flag and no `FocusHighlight` involved. The assertion (`querySelector(\"[data-moniker]\") === null`) still holds for the right reason — `CommandScopeProvider` is a pure context provider — so the test is still meaningful, but the wording should be updated to reflect the new structure (e.g. `\"renders no DOM wrapper — CommandScopeProvider is context-only\"`).\n-\n-### Nit Resolution (2026-04-26 08:58)\n-\n-Both nits addressed:\n-\n-1. **`data-table.tsx`** — Replaced the stale doc comment on `GridCellScope` with the suggested wording: \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\" Param tags retained.\n-2. **`store-container.test.tsx`** — Renamed the test to `\"renders no DOM wrapper — CommandScopeProvider is context-only\"` and rewrote the inline comment to reflect that `StoreContainer` is now a pure context provider (`CommandScopeProvider`), not a `FocusScope` with `renderContainer=false`. The `querySelector(\"[data-moniker]\") === null` assertion is unchanged — it still asserts the right thing, just with accurate justification.\n-\n-Verification:\n-- `npx vitest run src/components/store-container.test.tsx src/components/data-table.test.tsx` — 14 / 14 pass.\n-- `npx vitest run` (full UI suite) — 138 files, 1513 tests, all passing (no regression).\n-- `npx tsc --noEmit` — clean.\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/components/data-table.tsx:830` — Doc comment on `GridCellScope` claims `FocusScope` \"renders as a `<td>` element (via the underlying FocusHighlight)\". Both clauses are now stale: this refactor replaced the inner `FocusHighlight` with an in-file `FocusScopeBody` (which deliberately does not emit `data-focused` so the primitive owns it), and `FocusScope` does not render as a `<td>` — it renders inside the `TableCell`. Suggest: drop the parenthetical and reword to something like \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\"\n+- [ ] `kanban-app/ui/src/components/store-container.test.tsx:71,80` — Test name `\"does not render a wrapping container div (renderContainer=false)\"` and the inline comment \"FocusScope with renderContainer=false should not add a FocusHighlight wrapper\" both refer to the pre-refactor implementation. `StoreContainer` now uses `CommandScopeProvider` directly, so there is no `renderContainer` flag and no `FocusHighlight` involved. The assertion (`querySelector(\"[data-moniker]\") === null`) still holds for the right reason — `CommandScopeProvider` is a pure context provider — so the test is still meaningful, but the wording should be updated to reflect the new structure (e.g. `\"renders no DOM wrapper — CommandScopeProvider is context-only\"`).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ51BB4HDAMHJHE4NX1FZV41","timestamp":"2026-04-26T13:58:31.313238Z","entity_type":"task","entity_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -143,5 +143,17 @@\n ## Review Findings (2026-04-26 08:53)\n\n ### Nits\n-- [ ] `kanban-app/ui/src/components/data-table.tsx:830` — Doc comment on `GridCellScope` claims `FocusScope` \"renders as a `<td>` element (via the underlying FocusHighlight)\". Both clauses are now stale: this refactor replaced the inner `FocusHighlight` with an in-file `FocusScopeBody` (which deliberately does not emit `data-focused` so the primitive owns it), and `FocusScope` does not render as a `<td>` — it renders inside the `TableCell`. Suggest: drop the parenthetical and reword to something like \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\"\n-- [ ] `kanban-app/ui/src/components/store-container.test.tsx:71,80` — Test name `\"does not render a wrapping container div (renderContainer=false)\"` and the inline comment \"FocusScope with renderContainer=false should not add a FocusHighlight wrapper\" both refer to the pre-refactor implementation. `StoreContainer` now uses `CommandScopeProvider` directly, so there is no `renderContainer` flag and no `FocusHighlight` involved. The assertion (`querySelector(\"[data-moniker]\") === null`) still holds for the right reason — `CommandScopeProvider` is a pure context provider — so the test is still meaningful, but the wording should be updated to reflect the new structure (e.g. `\"renders no DOM wrapper — CommandScopeProvider is context-only\"`).\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/components/data-table.tsx:830` — Doc comment on `GridCellScope` claims `FocusScope` \"renders as a `<td>` element (via the underlying FocusHighlight)\". Both clauses are now stale: this refactor replaced the inner `FocusHighlight` with an in-file `FocusScopeBody` (which deliberately does not emit `data-focused` so the primitive owns it), and `FocusScope` does not render as a `<td>` — it renders inside the `TableCell`. Suggest: drop the parenthetical and reword to something like \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\"\n+- [x] `kanban-app/ui/src/components/store-container.test.tsx:71,80` — Test name `\"does not render a wrapping container div (renderContainer=false)\"` and the inline comment \"FocusScope with renderContainer=false should not add a FocusHighlight wrapper\" both refer to the pre-refactor implementation. `StoreContainer` now uses `CommandScopeProvider` directly, so there is no `renderContainer` flag and no `FocusHighlight` involved. The assertion (`querySelector(\"[data-moniker]\") === null`) still holds for the right reason — `CommandScopeProvider` is a pure context provider — so the test is still meaningful, but the wording should be updated to reflect the new structure (e.g. `\"renders no DOM wrapper — CommandScopeProvider is context-only\"`).\n+\n+### Nit Resolution (2026-04-26 08:58)\n+\n+Both nits addressed:\n+\n+1. **`data-table.tsx`** — Replaced the stale doc comment on `GridCellScope` with the suggested wording: \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\" Param tags retained.\n+2. **`store-container.test.tsx`** — Renamed the test to `\"renders no DOM wrapper — CommandScopeProvider is context-only\"` and rewrote the inline comment to reflect that `StoreContainer` is now a pure context provider (`CommandScopeProvider`), not a `FocusScope` with `renderContainer=false`. The `querySelector(\"[data-moniker]\") === null` assertion is unchanged — it still asserts the right thing, just with accurate justification.\n+\n+Verification:\n+- `npx vitest run src/components/store-container.test.tsx src/components/data-table.test.tsx` — 14 / 14 pass.\n+- `npx vitest run` (full UI suite) — 138 files, 1513 tests, all passing (no regression).\n+- `npx tsc --noEmit` — clean.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -143,17 +143,5 @@\n ## Review Findings (2026-04-26 08:53)\n\n ### Nits\n-- [x] `kanban-app/ui/src/components/data-table.tsx:830` — Doc comment on `GridCellScope` claims `FocusScope` \"renders as a `<td>` element (via the underlying FocusHighlight)\". Both clauses are now stale: this refactor replaced the inner `FocusHighlight` with an in-file `FocusScopeBody` (which deliberately does not emit `data-focused` so the primitive owns it), and `FocusScope` does not render as a `<td>` — it renders inside the `TableCell`. Suggest: drop the parenthetical and reword to something like \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\"\n-- [x] `kanban-app/ui/src/components/store-container.test.tsx:71,80` — Test name `\"does not render a wrapping container div (renderContainer=false)\"` and the inline comment \"FocusScope with renderContainer=false should not add a FocusHighlight wrapper\" both refer to the pre-refactor implementation. `StoreContainer` now uses `CommandScopeProvider` directly, so there is no `renderContainer` flag and no `FocusHighlight` involved. The assertion (`querySelector(\"[data-moniker]\") === null`) still holds for the right reason — `CommandScopeProvider` is a pure context provider — so the test is still meaningful, but the wording should be updated to reflect the new structure (e.g. `\"renders no DOM wrapper — CommandScopeProvider is context-only\"`).\n-\n-### Nit Resolution (2026-04-26 08:58)\n-\n-Both nits addressed:\n-\n-1. **`data-table.tsx`** — Replaced the stale doc comment on `GridCellScope` with the suggested wording: \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\" Param tags retained.\n-2. **`store-container.test.tsx`** — Renamed the test to `\"renders no DOM wrapper — CommandScopeProvider is context-only\"` and rewrote the inline comment to reflect that `StoreContainer` is now a pure context provider (`CommandScopeProvider`), not a `FocusScope` with `renderContainer=false`. The `querySelector(\"[data-moniker]\") === null` assertion is unchanged — it still asserts the right thing, just with accurate justification.\n-\n-Verification:\n-- `npx vitest run src/components/store-container.test.tsx src/components/data-table.test.tsx` — 14 / 14 pass.\n-- `npx vitest run` (full UI suite) — 138 files, 1513 tests, all passing (no regression).\n-- `npx tsc --noEmit` — clean.\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/components/data-table.tsx:830` — Doc comment on `GridCellScope` claims `FocusScope` \"renders as a `<td>` element (via the underlying FocusHighlight)\". Both clauses are now stale: this refactor replaced the inner `FocusHighlight` with an in-file `FocusScopeBody` (which deliberately does not emit `data-focused` so the primitive owns it), and `FocusScope` does not render as a `<td>` — it renders inside the `TableCell`. Suggest: drop the parenthetical and reword to something like \"Wraps `children` in a `FocusScope` mounted inside a `TableCell` so the row/cell carries the entity-focus + claim-predicate plumbing without breaking table HTML structure.\"\n+- [ ] `kanban-app/ui/src/components/store-container.test.tsx:71,80` — Test name `\"does not render a wrapping container div (renderContainer=false)\"` and the inline comment \"FocusScope with renderContainer=false should not add a FocusHighlight wrapper\" both refer to the pre-refactor implementation. `StoreContainer` now uses `CommandScopeProvider` directly, so there is no `renderContainer` flag and no `FocusHighlight` involved. The assertion (`querySelector(\"[data-moniker]\") === null`) still holds for the right reason — `CommandScopeProvider` is a pure context provider — so the test is still meaningful, but the wording should be updated to reflect the new structure (e.g. `\"renders no DOM wrapper — CommandScopeProvider is context-only\"`).\n\\ No newline at end of file\n"}]]}
{"id":"01KQ51BS79R9W1W7EEQQYNG08F","timestamp":"2026-04-26T13:58:45.737864Z","op":"update","item_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KPZWY4B79QJFF6XFEG1JR4RJ\n-position_column: doing\n+position_column: review\n position_ordinal: '8380'\n project: spatial-nav\n title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KPZWY4B79QJFF6XFEG1JR4RJ\n-position_column: review\n+position_column: doing\n position_ordinal: '8380'\n project: spatial-nav\n title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n","transaction_id":null}
{"id":"01KQ51BS7AFHSKQTGDB4B2VTAN","timestamp":"2026-04-26T13:58:45.738579Z","entity_type":"task","entity_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KQ51PRVEPC14TKY9E94A8JST","timestamp":"2026-04-26T14:04:45.806122Z","op":"update","item_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KPZWY4B79QJFF6XFEG1JR4RJ\n-position_column: review\n-position_ordinal: '8380'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffb580\n project: spatial-nav\n title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KPZWY4B79QJFF6XFEG1JR4RJ\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffb580\n+position_column: review\n+position_ordinal: '8380'\n project: spatial-nav\n title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n ---\n","transaction_id":null}
{"id":"01KQ51PRVEY21QRSQXP3CYMGVR","timestamp":"2026-04-26T14:04:45.806665Z","entity_type":"task","entity_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+ffffffffffffffffffffffffb580\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffb580\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5NTG2J01RB78WJ9HP0S70H","timestamp":"2026-04-26T19:56:19.410634Z","op":"update","item_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KPZWY4B79QJFF6XFEG1JR4RJ\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffb580\n+position_ordinal: ffffffffffffffffffffffffffffffffab80\n project: spatial-nav\n title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KPZWY4B79QJFF6XFEG1JR4RJ\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffab80\n+position_ordinal: ffffffffffffffffffffffffb580\n project: spatial-nav\n title: 'FocusScope wrapper refactor: entity-aware composite over the primitives'\n ---\n","transaction_id":null}
{"id":"01KQ5NTG2K7TRSPGRM48V6Z6E1","timestamp":"2026-04-26T19:56:19.411163Z","entity_type":"task","entity_id":"01KPZWZE5AZ1DPNYMKTM4XDJTJ","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffb580\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffab80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffab80\n\\ No newline at end of file\n+ffffffffffffffffffffffffb580\n\\ No newline at end of file\n"}]]}

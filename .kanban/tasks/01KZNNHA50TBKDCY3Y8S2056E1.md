---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kzq9z8wcsett506wttvtr7w9
  text: |-
    ### Measured against the real tool first — the card premise is INVERTED

    revive 1.15.0 (Homebrew, commit 815ffde4). Every run uses the SHIPPED script line:
    `config="$(mktemp -d)/revive.toml"; printf '%s\n' '[rule.exported]' > "$config"; revive -config "$config" -formatter json ...`

    **REFUTED — `ignoreGeneratedHeader` runs the other way.** The option means "ignore the
    generated header", that is, do NOT honour it. Five files, one probe directory:

    | file | shipped config (option unset) | `ignoreGeneratedHeader = true` | `= false` |
    |---|---|---|---|
    | `gen.pb.go` (has `// Code generated by protoc-gen-go. DO NOT EDIT.`) | 0 | 5 | 0 |
    | `hdr_gen.go` (plain name, has the header) | 0 | 1 | 0 |
    | `noheader.pb.go` (generated name, NO header) | 2 | 2 | 2 |
    | `plain.go` | 2 | 2 | 2 |
    | `thing_test.go` | 0 | 0 | 0 |

    The shipped one-line config ALREADY reproduces the generated-code carve-out. The change
    the card asks for would BREAK it, and would produce exactly the damage the card describes
    (5 findings in one small generated file).

    **Generated detection is the header LINE, not the file name.** Eight variants, one each:
    silent — header first line; header after a blank line; header after a licence comment;
    header with no blank line before `package`; header AFTER the `package` clause. Reported —
    lower case `// code generated ... do not edit.`; trailing text after `DO NOT EDIT.`;
    a `/* ... */` block comment. Real generator spellings measured silent: protoc-gen-go,
    MockGen, sqlc, and `// Code generated by "stringer -type=Pill"; DO NOT EDIT.`

    **REFUTED — `Error()` and `String()` are already carved out.** One file with
    `func (e *MyErr) Error() string`, `func (t T) String() string`, `func (g Getter) Name() string`
    and `func (g *Getter) SetName(n string)`, all undocumented: revive reports 2 —
    `Getter.Name` and `Getter.SetName`. It reports neither `Error` nor `String`.

    The carved-out method names measured exactly: `Error`, `String`, `Read`, `Write`,
    `Unwrap`, `ServeHTTP` (golint's `commonMethods`). `Close`, `Len`, `MarshalJSON`,
    `GoString`, `Format` and an ordinary method all report — 6 of 12 methods reported. The
    carve-out needs a RECEIVER: free functions named `Error` and `String` both report.

    So the prompt rule's "Obvious implementations (Display, Debug, ToString, etc.)" carve-out
    is reproduced by a fixed name list, and needs no setting.

    **CONFIRMED — `disableChecksOnMethods` turns off ALL method checks.** With the argument
    set, `Getter.Name` and `Getter.SetName` both go silent: 2 findings become 0.

    **CONFIRMED — getters and setters are not carved out.** See the table above.

    **CONFIRMED — tests, by FILE NAME, whole file.** `a_test.go` holding
    `type ExportedInTestFile`, `func ExportedHelperInTestFile()` and `func TestSomething(t *int)`
    reports 0. The same bytes as `a_testlike.go` report 3. The skip is the file name, not a
    marker on the item.

    **CONFIRMED — private, by Go capitalization.** An unexported type, function and constant
    report 0. An exported method on an UNEXPORTED receiver also reports 0
    (`checkPrivateReceivers` defaults off).

    **CONFIRMED — the inline suppression the rule body names.**
    `//revive:disable-next-line:exported` silences one item (2 findings become 1).
    `//revive:disable:exported` silences the file.

    ### NEW — two silent-zero paths in the shipped pipe

    Both make the rule answer 0 for a reason other than a clean file.

    1. **A nonzero revive exit is masked by the pipe.** A pipeline's status is the LAST
       command's, and that is `jq`. Measured: `revive ... nope.go` alone exits 1; through the
       shipped pipe the same run exits 0 with no output. Bad config exits 1 alone and 0
       through the pipe as well.
    2. **A file that does not parse is dropped by the `select`.** revive exits 0 and writes
       `{"Failure":"invalid file broken.go: ...","RuleName":"","Category":"validity"}`. The
       shipped `select(.RuleName == "exported")` drops it. Measured over `broken.go good.go`:
       one finding, for `good.go`, exit 0. The broken file reads as clean.
  timestamp: 2026-08-11T02:21:07.340736+00:00
- actor: claude-code
  id: 01kzqaghgvy2gvg89tqa7awvhg
  text: |-
    ### The RED run found a third defect: `exported` also reports a NAME

    The staged-positions test came back RED with three findings where two were expected. The
    third is not a documentation finding:

    ```
    staged.go:3 exported type StagedType should have comment or be unexported
    staged.go:3 type name will be used as staged.StagedType by other packages, and that
                stutters; consider calling this Type
    staged.go:5 exported method StagedType.StagedMethod should have comment or be unexported
    ```

    revive's `exported` rule holds a second check. It reports an exported name that repeats
    its own package name. That is a NAMING finding under `RuleName == "exported"`, so the
    shipped `select(.RuleName == "exported")` keeps it and reports it as a missing-docs
    finding. The rule declares `supersedes: missing-docs` alone. `naming-consistency` owns a
    name, so one defect had two owners.

    `disableStutteringCheck` is the revive setting for it. Measured over two files holding
    three stuttering names and two undocumented items: the default config reports 5, and the
    config with the setting reports exactly the 2 documentation findings. A constant does not
    trip the check; a type and a function do.

    The setting is now in the config, and the rule body states it with the measurement.

    ### RED proof of the three new acceptance tests

    `cargo nextest run -p swissarmyhammer-validators -E 'test(/missing_docs/)'` — 6 run,
    3 passed, 3 failed. Each new Go test failed against the SHIPPED rule:

    - `..._reports_every_fail_fixture_item` — the fail fixture holds no getter, setter,
      constant or variable yet.
    - `..._reads_neither_a_generated_nor_a_test_file` — `left: ["staged.go", "staged.go",
      "staged.go"]`, `right: ["staged.go", "staged.go"]`. The generated file and the test file
      were already silent; the extra entry is the stutter finding above.
    - `..._breaks_on_a_file_it_cannot_parse` — `the run must report exactly one tool error;
      got []`. That is the silent zero, reproduced.
  timestamp: 2026-08-11T02:30:33.243875+00:00
- actor: claude-code
  id: 01kzqb8c6fq0wdwt6kb5b2eqdv
  text: |-
    ### A fourth carve-out found while the positions probe was built: `package main`

    `exported` reads no file of `package main`. Measured: one file holding an undocumented
    exported type, method, function, constant and variable reports 5 findings under
    `package library` and 0 under `package main`. The package clause is the whole difference.

    A build tag is NOT what does it. `//go:build ignore` reports when the package clause is
    not `main`; `//go:build never_defined_tag` reports as well. The first probe read
    `package main` and a build tag together and looked like a build-tag skip.

    The staged-positions probe now carries four positions — `staged.go`, `staged_test.go`,
    `staged.pb.go` and `cmd/probe/main.go`. Each file's head is a list of shared fragments, so
    the generated file and the two library files share ONE package-clause const by reference
    and cannot drift apart. The command file stands in a directory of its own, which is where
    a Go command stands, so one directory never holds two package names.

    ### RED to GREEN, with the real tool

    `ignoreGeneratedHeader = true` written into the shipped config, then
    `cargo nextest run -p swissarmyhammer-validators -E 'test(/the_shipped_go_missing_docs/)'`:

    ```
    FAIL the_shipped_go_missing_docs_tool_rule_reads_neither_a_generated_nor_a_test_file
      left: ["staged.go", "staged.go", "staged.pb.go", "staged.pb.go"]
     right: ["staged.go", "staged.go"]
    ```

    The generated file reported both of its items. The setting was then removed and the test
    went green. That is the card's premise refuted by the tool and locked by a test.

    ### The shared helper, and why the Dart test moved

    The staged-positions test body would have been a near copy of the Dart one — same shape,
    different constants, well over 40 normalized tokens at over 90 percent alike, which the
    `duplication-parsed` rule reports. The cause was removed rather than the line: the body is
    now `ShippedStagedPositions` plus `verify_shipped_staged_positions_report`, and BOTH tests
    call it. `the_shipped_dart_missing_docs_tool_rule_reads_only_the_package_library` keeps its
    name, its doc comment and its assertion.

    ### Determinism, the last silent-zero question

    120 single-package Go files, five runs of the shipped script: 240 findings each time. No
    concurrency effect.
  timestamp: 2026-08-11T02:43:34.223554+00:00
- actor: claude-code
  id: 01kzqb8m1a0qhn9sa5f6nw81kf
  text: |-
    ### implement — changed
    - evidence: 5 files — `builtin/validators/code-hygiene/rules/missing-docs-go.md`,
      `builtin/validators/code-hygiene/fixtures/missing-docs-go.fail.go.tmpl`,
      `builtin/validators/code-hygiene/fixtures/missing-docs-go.pass.go.tmpl`,
      `crates/swissarmyhammer-validators/src/review/tool_rules/tests.rs`,
      `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`.
      `cargo nextest run -p swissarmyhammer-validators` — 586 run, 586 passed, 0 skipped.
      `cargo nextest run -p mirdan` — 503 run, 503 passed. `cargo fmt --all` applied.
      `cargo clippy --workspace --all-targets -- -D warnings` clean.
      Three new acceptance tests, each proved RED first against the shipped rule.
    - next: `/review`
  timestamp: 2026-08-11T02:43:42.250052+00:00
- actor: claude-code
  id: 01kzqbvagqa2y5c3117ch9sc4e
  text: |
    ### review — findings

    - scope: `review sha HEAD~1..HEAD` (commit 467cfe2c9)
    - engine counts: 2 findings, 2 confirmed, 0 refuted, 8 attempted, 0 failed
    - recorded: 2 findings
      - `builtin/validators/code-hygiene/rules/missing-docs-go.md:63`
      - `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs:477`
    - dropped: the `dead_code_work` / `complexity_work` near-duplicate at
      `tests/shipped.rs:207`. Both functions are absent from the diff of 467cfe2c9, so the
      finding asks to refactor test code that already existed. The written exception in the
      review skill drops it.

    #### The three checks the diff-scoped engine cannot make

    1. **Every claim carries its measurement.** Each section of the rule body states a count
       or the verbatim revive message: 5 findings against 2 for `disableStutteringCheck`, the
       four-row table for `ignoreGeneratedHeader`, four header properties, four generator
       headers, three items in the `_test.go` pair, 5 against 0 for `package main`, 6 of 12
       for the carved-out method names, 1 finding and exit 0 against 0 findings and exit 1 for
       the broken run. The four quotations of the `missing-docs` prompt rule are word for word
       correct against `builtin/validators/code-hygiene/rules/missing-docs.md` lines 28, 30,
       33 and 39. The three acceptance tests the body names all exist and pass. The fixture
       pair holds every shape the body claims. ONE claim fails: the `naming-consistency`
       sentence, recorded as a finding.
    2. **Every exemption is a tool setting, an annotation or a structural fact.** Generated
       code is the header line plus the revive default; tests are the `_test.go` file name; a
       command is the `package main` clause; the obvious implementations are a fixed name list
       plus a receiver; a private item is capitalization; the stuttering name is the
       `disableStutteringCheck` setting; one item is the `//revive:disable-next-line:exported`
       annotation. No exemption asks a reader to judge. Clean.
    3. **Sibling rules and the shared helper.** The body names no `missing-docs-<language>`
       sibling, so no sentence about one can be stale. `cargo nextest run -p
       swissarmyhammer-validators shipped` ran 24 tests and all 24 passed, which includes both
       Dart tests that `ShippedStagedPositions` now serves —
       `the_shipped_dart_missing_docs_tool_rule_reads_only_the_package_library` and
       `the_shipped_dart_missing_docs_tool_rule_reports_every_fail_fixture_line`. The helper
       did not break the Dart test.

    - next: correct the `naming-consistency` sentence, and merge `ShippedStagedPositions` with
      `ShippedFailFixture`. Then run `/review` again.
  timestamp: 2026-08-11T02:53:55.095222+00:00
- actor: claude-code
  id: 01kzqbwarhqp3djxy0t4rnyrkt
  text: |-
    ### finish iteration 1 — findings
    - implement: changed — 5 files; the card premise was inverted and corrected; three real defects found and fixed (a stuttering name under the same rule name, a masked nonzero exit, and a parse failure that read as clean)
    - test: green — cargo nextest run -p swissarmyhammer-validators, 586 passed; 3 new acceptance tests each proved RED first; clippy -D warnings clean
    - commit: 467cfe2c9 — 5 files
    - review: findings — builtin/validators/code-hygiene/rules/missing-docs-go.md:63, crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs:477
  timestamp: 2026-08-11T02:54:28.113380+00:00
- actor: claude-code
  id: 01kzqd0018f918cg1q1xk47wkb
  text: |
    ### Finding 1 — the tool refutes the sentence AND the reason the sentence gave

    `naming-consistency` names no shipped rule. It stands in exactly two places in the
    repository: this rule body, and a hypothetical example inside
    `builtin/skills/check-sah/SKILL.md`. Confirmed by a repository-wide grep.

    **A second claim in the same paragraph was also false.** The body said revive gives the
    two checks the same rule name "so a filter cannot tell the two apart". Measured on revive
    1.15.0: revive writes a CATEGORY beside the rule name, and the two checks differ there.

    ```
    === the shipped config without disableStutteringCheck ===
    naming    type name will be used as staged.StagedType by other packages, and that stutters; consider calling this Type
    comments  exported type Plain should have comment or be unexported
    comments  exported type StagedHolder should have comment or be unexported
    naming    type name will be used as staged.StagedHolder by other packages, and that stutters; consider calling this Holder
    comments  exported const StagedLimit should have comment or be unexported
    === the shipped config ===
    comments  exported type Plain should have comment or be unexported
    comments  exported type StagedHolder should have comment or be unexported
    comments  exported const StagedLimit should have comment or be unexported
    ```

    5 findings become 3. Two more facts from the same probe: a DOCUMENTED stuttering type
    still reports its name, so the naming check is independent of the doc comment; and a
    stuttering CONSTANT reports no name.

    The `sayRepetitiveInsteadOfStutters` argument writes `that is repetitive` in place of
    `that stutters`. The `Category` field does not move. A future filter must read the
    category, not the word.

    **The decision: keep `disableStutteringCheck`.** The reason on the record is now the true
    one. This rule supersedes `missing-docs` and no other. `missing-docs` asks for a doc
    comment, and a stuttering name is not a missing doc comment, so this rule does not own
    that defect. The config asks revive for the one check the rule owns.

    **A category filter was considered and rejected.** With `disableStutteringCheck` set,
    revive never makes a `naming` finding, so a `.Category == "comments"` filter would guard a
    record the shipped config cannot produce, and NO test could measure it. An unmeasurable
    guard in a rule body is the defect this card series removes, so the filter stays out and
    the body states why.

    **The "no owner" statement is now measured, not asserted.** New acceptance test
    `the_shipped_rules_that_read_a_go_file_stay_the_stated_list` runs the real matcher over a
    `.go` path in a Go workspace and pins the 27 rules that match. RED first with an empty
    list, GREEN with the 27. None of the 27 reports a NAME as the defect. The three shipped
    naming rules are absent from the list, which is the same answer from the other side. A
    rule added later fails the test, and the reader then decides whether it owns a Go name.

    Card ^6jzgb8v carries the gap, and holds the two revive measurements a Go naming rule
    needs.

    ### Finding 2 — unified, and the tool is the judge

    `sah tool code_context duplication find` was the RED control and the GREEN proof, not my
    reading. Before: two findings. After: one, and that one is the pre-existing
    `dead_code_work` / `complexity_work` pair that the review skill's written exception drops.

    Two design candidates were measured on a scratch copy before the real file was touched:

    - Extract the shared fields only, keeping both records at five fields — STILL REPORTED,
      47 tokens, 94% alike. Rejected on the measurement.
    - Extract the shared fields AND name the staged-position record — clean.

    So `ShippedRun { project_types, rule, expected }` now holds what every probe states, and
    both records carry it by reference. `ShippedStagedPositions.staged` is a
    `&[ShippedStagedFile]` with named `path` and `head` fields, in place of an anonymous
    `(&str, &[&str])` tuple whose two members read alike at the call site. `declarations`
    stays a field of its own, so the driver still appends the SAME bytes to every position and
    no position can drift.

    A third candidate — folding `declarations` into each position's fragments — also cleared
    the tool but gives up that structural guarantee, so it was dropped.

    ### The rest of the file, checked for the same cause

    A finding is one example. Every other name the rule body states was checked against the
    repository: both acceptance test names it quotes exist; all four quotations of the
    `missing-docs` prompt rule are word for word correct; `missing-docs` is the only other
    rule name the body states. `naming-consistency` was the only false one.

    Every helper added in 467cfe2c9 was checked for a duplicate by the tool, over the whole
    file. Only `ShippedStagedPositions` was reported.
  timestamp: 2026-08-11T03:13:56.776143+00:00
- actor: claude-code
  id: 01kzqd09c5evr9w5xghnhcfnf5
  text: |
    ### implement — changed
    - evidence: 2 files — `builtin/validators/code-hygiene/rules/missing-docs-go.md`,
      `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`.
      `cargo nextest run -p swissarmyhammer-validators` — 587 run, 587 passed, 0 skipped
      (586 before; the new test is the one added). Both Dart tests pass on the shared helper:
      `the_shipped_dart_missing_docs_tool_rule_reads_only_the_package_library` and
      `the_shipped_dart_missing_docs_tool_rule_reports_every_fail_fixture_line`, beside the
      two Go tests. `cargo nextest run -p mirdan` — 503 run, 503 passed.
      `cargo fmt --all` applied. `cargo clippy --workspace --all-targets -- -D warnings`
      clean. `sah tool code_context duplication find` over the changed file: 1 finding, the
      pre-existing `dead_code_work` / `complexity_work` pair, and no other.
      `touch crates/swissarmyhammer-validators/src/builtin/mod.rs` ran before each cargo
      command that reads the changed rule.
    - new card: ^6jzgb8v — no shipped rule owns a stuttering Go name.
    - next: `/review`
  timestamp: 2026-08-11T03:14:06.341184+00:00
- actor: claude-code
  id: 01kzqdcemst8k9wdcp24syyjn6
  text: |-
    ### review — clean
    - evidence: `review sha HEAD~1..HEAD`. The engine attempted 8 validators. 0 failed, 0 skipped. It made 1 finding: `dead_code_work` is a near-duplicate of `complexity_work` in `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`. The written rule of the review skill drops a finding that asks to change test code that was there before. `git log -S` shows commit 087d052b4 made both functions, and commit a060e5391 does not touch them. The finding is dropped. 0 findings stay.
    - prior findings: both are closed. No file in the tree holds the text `naming-consistency`. `ShippedStagedPositions` now uses the shared `ShippedRun` and the named `ShippedStagedFile`.
    - sweep: complete. Each thing the rule body names is real. `builtin/validators/swift/rules/naming-clarity.md`, `builtin/validators/swift/rules/doc-parameter-naming.md` and `builtin/validators/js-ts/rules/naming-and-style.md` are files. Card ^6jzgb8v is a card.
    - gate: real. `SHIPPED_RULES_THAT_READ_A_GO_FILE` is a written list of 27 names. The test compares it with the output of the real matcher over the real loader. It is not a restatement of the code. `cargo nextest run the_shipped_rules_that_read_a_go_file_stay_the_stated_list` gives 1 passed. A read of all 27 rules shows that no one of them reports a NAME as the defect, so the sentence in the rule body is true.
    - next: none. The card moves to done.
  timestamp: 2026-08-11T03:20:44.953119+00:00
- actor: claude-code
  id: 01kzqdd9k0cp2mr7963ntnrgcw
  text: |-
    ### finish iteration 2 — clean
    - implement: changed — 2 files; the false rule name is removed, and a second claim in the same paragraph was refuted (revive writes a category, so the two checks CAN be told apart); disableStutteringCheck stays for the true reason; new test pins the 27 shipped rules that read a .go file; card ^6jzgb8v carries the naming gap
    - test: green — cargo nextest run -p swissarmyhammer-validators, 587 passed; the new test proved RED then GREEN; clippy -D warnings clean
    - commit: a060e5391 — 2 files
    - review: clean — 1 engine finding in pre-existing test code (origin 087d052b4), dropped by the written exception; both prior findings verified closed; card moved to done
  timestamp: 2026-08-11T03:21:12.544766+00:00
position_column: done
position_ordinal: ffffffffffffffffffffffffffffffffffffffffe180
title: missing-docs-go must state what revive measures, and must not answer 0 for a broken run
---
`builtin/validators/code-hygiene/rules/missing-docs-go.md` runs `revive` with a temporary
config holding only `[rule.exported]`, and declares `supersedes: [missing-docs]`.

The original card said the config leaves `ignoreGeneratedHeader` false, that a generated
`*.pb.go` therefore reports every exported symbol, and that setting the option to `true`
restores the carve-out. **The tool refutes all three.** The option means "ignore the
generated header", so `true` STOPS revive honouring it. Measured on revive 1.15.0: a
`gen.pb.go` that carries `// Code generated by protoc-gen-go. DO NOT EDIT.` reports 0
under the shipped config and 5 with `ignoreGeneratedHeader = true`. The shipped config is
correct, and the change the card asked for would cause the damage the card describes.

The card also said `exported` reports `Error()` and `String()`. Refuted: revive carves out
a fixed list of method names — `Error`, `String`, `Read`, `Write`, `Unwrap`, `ServeHTTP` —
when the item has a receiver. Measured: 6 of 12 undocumented methods report; those six
stay silent. Free functions with the same names report.

The measurements are in the comments.

## What this card does

- [x] Reproduce every claim against the real revive before changing anything.
- [x] Correct the card with the measurement.
- [x] Keep `ignoreGeneratedHeader` unset, and state on the record that the default holds
      the carve-out, so no later edit turns it on.
- [x] State the generated carve-out as what revive measures: the header LINE
      `// Code generated ... DO NOT EDIT.`, anywhere in the file, case exact, no trailing
      text, a `//` comment.
- [x] State the obvious-implementation carve-out as the measured name list, and state that
      `disableChecksOnMethods` is not the lever because it silences every method.
- [x] State the carve-out revive does NOT reproduce: a getter and a setter both need a doc
      comment.
- [x] State the test carve-out as what revive measures — the `_test.go` file name, whole
      file — and note it disagrees with the prompt rule's "not from the file name".
- [x] Close the two silent-zero paths: a nonzero revive exit masked by the pipe, and a
      `validity` failure for a file that does not parse dropped by the `select`.
- [x] Acceptance tests through the real tool, proved RED to GREEN.

## Found while the work ran, and fixed here

- [x] `exported` also reports a stuttering NAME under the same rule name, so a naming
      defect arrived as a missing-docs finding. `disableStutteringCheck` turns it off.
- [x] `exported` reports a doc comment that does not open with the item's own name, and a
      `Deprecated:` note standing alone. Both are documentation checks. Both are now
      stated and held by the fail fixture.
- [x] `exported` reads no file of `package main`. Stated, and held by the staged-positions
      test.

Found by the `supersedes` survey on ^h7garpc. #tool-validators #objectivity

## Review Findings (2026-08-10 21:45)

- [x] `builtin/validators/code-hygiene/rules/missing-docs-go.md:63` — the sentence gives the stuttering NAME to a `naming-consistency` prompt rule, and no validator set ships a rule with that name. The only naming rules that exist are `swift/naming-clarity`, `swift/doc-parameter-naming` and `js-ts/naming-and-style`, and none of the three reads a `.go` file. The reason the body gives for `disableStutteringCheck` — that another rule owns the defect — is therefore not true for Go, and the setting drops the defect with no owner. Name the rule that owns a Go stuttering name, or state that no rule owns it and measure that statement.
- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs:477` — struct `ShippedStagedPositions` is a near-duplicate of `ShippedFailFixture` at crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs:328 (72 tokens, 96% alike).

{"id":"01KTS1D74PNW0HJ4GXPHZ5MZ91","timestamp":"2026-06-10T15:11:26.870741Z","op":"create","item_id":"01KTS1D74PYWX5XWYZY20KR1M4","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,36 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '9780'\n+project: local-review\n+title: 'fix(llama-agent): streaming submit path gets backpressure'\n+---\n+## What\n+\n+The review fleet's prompts flow ACP prompt turn (`crates/llama-agent/src/acp/server.rs`, `generate_stream` call ~line 2696) → `AgentServer::generate_stream` (`crates/llama-agent/src/agent.rs` ~1254) → `RequestQueue::submit_streaming_request` (`crates/llama-agent/src/queue.rs`, the `try_send` at ~line 1198), which rejects with `QueueError::Full` when the single-worker queue is saturated — silently dropping fleet tasks. The earlier backpressure fix (commit 470bc0df8) added cancellation-aware bounded `send().await` in `RequestQueue::enqueue_request` (~line 1122) but only routed the NON-streaming `submit_request` through it. Latest real run: 315× \"Queue is full\", 45/45 fleet tasks failed, empty review.\n+\n+Fix, all in `crates/llama-agent/src/queue.rs`:\n+\n+- [ ] Make `submit_streaming_request` route its queue submission through `enqueue_request` (the `tokio::select!` on `sender.send()` vs the request's `CancellationToken`) instead of `try_send`.\n+- [ ] On enqueue failure (cancelled or shutdown), remove the session's entry from `active_requests` — the streaming path inserts into `active_requests` (~line 1170) before enqueue and has no `ActiveRequestGuard`, so it would otherwise leak. Prefer reusing `ActiveRequestGuard` the way `submit_request` does.\n+- [ ] Update the now-stale doc comments: `enqueue_request`'s \"`Full` is still returned for the unbounded streaming path's non-blocking submit\" paragraph, `submit_streaming_request`'s docs, and `try_enqueue_for_test`'s \"Mirrors the non-blocking `try_send` the streaming submit path uses\" sentence.\n+- [ ] Replace the test `streaming_submit_returns_full_at_capacity` (~line 4170, comment \"The streaming submit path has its own try_send + Full branch\") — that behavior is the bug.\n+\n+Do NOT touch the `mpsc::unbounded_channel` for StreamChunks inside `submit_streaming_request` — it is unbounded by design (documented producer-wedge rationale at ~line 1157). Only the queue-submission `try_send` changes. Keep `try_enqueue_for_test` intact for capacity tests (`QueueError::Full` variant stays).\n+\n+## Acceptance Criteria\n+\n+- [ ] `submit_streaming_request` never returns `QueueError::Full`; with the queue at capacity it waits for a slot.\n+- [ ] A cancellation fired while a streaming submit is waiting for capacity returns an error and leaves no entry in `active_requests`.\n+- [ ] No stale doc comments claiming the streaming path uses `try_send`.\n+- [ ] All existing queue tests pass; `try_enqueue_for_test` capacity tests unchanged.\n+\n+## Tests\n+\n+- [ ] In `crates/llama-agent/src/queue.rs` tests: new test mirroring `submit_request_clears_active_requests_on_cancellation` (~line 3899) and the saturated-gated-queue backpressure tests, proving (a) with the queue saturated via `saturated_gated_queue()`, `submit_streaming_request` parks instead of returning `QueueError::Full`, then completes once the gate opens; (b) cancelling its token while parked returns `Err` and `await_session_cleared` confirms `active_requests` is empty.\n+- [ ] Replace `streaming_submit_returns_full_at_capacity` with the wait-instead-of-Full assertion.\n+- [ ] `cargo test -p llama-agent queue` — all green, every test fast (<10s, scripted/gated fake executor only, no real model).\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,36 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '9780'\n-project: local-review\n-title: 'fix(llama-agent): streaming submit path gets backpressure'\n----\n-## What\n-\n-The review fleet's prompts flow ACP prompt turn (`crates/llama-agent/src/acp/server.rs`, `generate_stream` call ~line 2696) → `AgentServer::generate_stream` (`crates/llama-agent/src/agent.rs` ~1254) → `RequestQueue::submit_streaming_request` (`crates/llama-agent/src/queue.rs`, the `try_send` at ~line 1198), which rejects with `QueueError::Full` when the single-worker queue is saturated — silently dropping fleet tasks. The earlier backpressure fix (commit 470bc0df8) added cancellation-aware bounded `send().await` in `RequestQueue::enqueue_request` (~line 1122) but only routed the NON-streaming `submit_request` through it. Latest real run: 315× \"Queue is full\", 45/45 fleet tasks failed, empty review.\n-\n-Fix, all in `crates/llama-agent/src/queue.rs`:\n-\n-- [ ] Make `submit_streaming_request` route its queue submission through `enqueue_request` (the `tokio::select!` on `sender.send()` vs the request's `CancellationToken`) instead of `try_send`.\n-- [ ] On enqueue failure (cancelled or shutdown), remove the session's entry from `active_requests` — the streaming path inserts into `active_requests` (~line 1170) before enqueue and has no `ActiveRequestGuard`, so it would otherwise leak. Prefer reusing `ActiveRequestGuard` the way `submit_request` does.\n-- [ ] Update the now-stale doc comments: `enqueue_request`'s \"`Full` is still returned for the unbounded streaming path's non-blocking submit\" paragraph, `submit_streaming_request`'s docs, and `try_enqueue_for_test`'s \"Mirrors the non-blocking `try_send` the streaming submit path uses\" sentence.\n-- [ ] Replace the test `streaming_submit_returns_full_at_capacity` (~line 4170, comment \"The streaming submit path has its own try_send + Full branch\") — that behavior is the bug.\n-\n-Do NOT touch the `mpsc::unbounded_channel` for StreamChunks inside `submit_streaming_request` — it is unbounded by design (documented producer-wedge rationale at ~line 1157). Only the queue-submission `try_send` changes. Keep `try_enqueue_for_test` intact for capacity tests (`QueueError::Full` variant stays).\n-\n-## Acceptance Criteria\n-\n-- [ ] `submit_streaming_request` never returns `QueueError::Full`; with the queue at capacity it waits for a slot.\n-- [ ] A cancellation fired while a streaming submit is waiting for capacity returns an error and leaves no entry in `active_requests`.\n-- [ ] No stale doc comments claiming the streaming path uses `try_send`.\n-- [ ] All existing queue tests pass; `try_enqueue_for_test` capacity tests unchanged.\n-\n-## Tests\n-\n-- [ ] In `crates/llama-agent/src/queue.rs` tests: new test mirroring `submit_request_clears_active_requests_on_cancellation` (~line 3899) and the saturated-gated-queue backpressure tests, proving (a) with the queue saturated via `saturated_gated_queue()`, `submit_streaming_request` parks instead of returning `QueueError::Full`, then completes once the gate opens; (b) cancelling its token while parked returns `Err` and `await_session_cleared` confirms `active_requests` is empty.\n-- [ ] Replace `streaming_submit_returns_full_at_capacity` with the wait-instead-of-Full assertion.\n-- [ ] `cargo test -p llama-agent queue` — all green, every test fast (<10s, scripted/gated fake executor only, no real model).\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTS26KWX66A3HR9PRJ8C8QEW","timestamp":"2026-06-10T15:25:19.133929Z","op":"update","item_id":"01KTS1D74PYWX5XWYZY20KR1M4","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: '9780'\n+position_column: doing\n+position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): streaming submit path gets backpressure'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '9780'\n project: local-review\n title: 'fix(llama-agent): streaming submit path gets backpressure'\n ---\n","transaction_id":null}
{"id":"01KTS2MHH3CXM34EX7QVP2HHCJ","timestamp":"2026-06-10T15:32:55.459436Z","op":"update","item_id":"01KTS1D74PYWX5XWYZY20KR1M4","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: local-review\n title: 'fix(llama-agent): streaming submit path gets backpressure'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): streaming submit path gets backpressure'\n ---\n","transaction_id":null}
{"id":"01KTSHK93JX34GZ7VFDVWXJY29","timestamp":"2026-06-10T19:54:22.706693Z","op":"update","item_id":"01KTS1D74PYWX5XWYZY20KR1M4","forward_patch":"--- original\n+++ modified\n@@ -33,4 +33,24 @@\n - [ ] `cargo test -p llama-agent queue` — all green, every test fast (<10s, scripted/gated fake executor only, no real model).\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Review Findings (2026-06-10 14:42)\n+\n+### Warnings\n+- [ ] `crates/llama-agent/src/queue.rs:865` — ActiveRequestGuard::drop removes the session's active_requests entry via a detached spawned task, which races with the next submit on the same session: if turn N's guard drops, then turn N+1 calls track_cancellation_token (queue.rs:1123-1124 inserts unconditionally) before the spawned removal acquires the lock, the stale removal deletes turn N+1's fresh token. cancel_session then returns false and the in-flight turn is uncancellable. Sequential turns on one session — the agentic loop's normal shape — hit exactly this back-to-back pattern. Make the removal conditional on identity: store a monotonically increasing generation id alongside the token (map value `(u64, CancellationToken)`), record the generation in the guard, and have the spawned task remove the entry only if the stored generation still matches the guard's.\n+- [ ] `crates/llama-agent/src/queue.rs:1166` — Cancellation — an expected, client-initiated outcome — and shutdown are both surfaced as QueueError::WorkerError(String), and the new doc comments (queue.rs:1071-1073, 1178-1181) codify that conflation. Callers cannot distinguish 'user cancelled' from 'worker failed' without string-matching; the new test batch_submit_backpressure_honors_cancellation can only assert `result.is_err()` because no variant exists to match. Add `QueueError::Cancelled` and `QueueError::ShuttingDown` variants via thiserror, return them from enqueue_request's select arms, and update the new doc comments on submit_request/submit_streaming_request to name the specific variants.\n+- [ ] `crates/llama-agent/src/queue.rs:1234` — After guard.disarm(), nothing in RequestQueue ever removes a streaming session's active_requests entry when the turn completes normally: removal happens only in the guard's Drop (disarmed here) and in cancel_session. Entries for completed streaming turns persist until overwritten by a later submit on the same session, so on the long-lived shared server the map grows with every session that ends without cancellation — the same stale-entry accumulation the guard was introduced to fix — and cancel_session spuriously returns true (and fires a dead token) for already-finished sessions. Hand ownership of the entry's lifetime to the turn itself: pass the (disarmed) guard or an equivalent removal handle into the QueuedRequest so the worker clears the entry when the streaming turn finishes, or have the stream-relay completion path remove the session's entry once the final chunk is sent.\n+- [ ] `crates/llama-agent/src/queue.rs:3824` — The backpressure-resolution timeout `Duration::from_secs(5)` is hardcoded at five separate call sites in the new tests. Retuning this budget for a slow CI box means finding and editing all five in lockstep; missing one yields tests that flake at different thresholds. Hoist one `const RESOLVE_TIMEOUT: Duration = Duration::from_secs(5);` in `worker_lifecycle_tests` and use it at all five sites, mirroring how `await_worker_parked` already names `PARK_POLL_ATTEMPTS`/`PARK_POLL_INTERVAL` precisely so 'retuning for a slow CI box is a single edit'.\n+- [ ] `crates/llama-agent/src/queue.rs:3868` — The 'sleep so the spawned submit can park, then assert it is still pending' settle delay is hardcoded eight times with three different magic values (20ms, 30ms, 50ms) for the same semantic. The values must stay in lockstep with scheduler timing, but a human retuning them has to know which of the three numbers is load-bearing at each site — they will drift. Define one named `const SETTLE_DELAY: Duration` (pick the largest, 50ms) in `worker_lifecycle_tests` and use it everywhere a test waits for a spawned submit to park, so CI retuning is a single edit.\n+\n+### Nits\n+- [ ] `crates/llama-agent/src/queue.rs:3406` — The 'let the submit park on backpressure' settle delay is a timing-sensitive tuning value, but it appears as four different bare literals for the same purpose — 20ms in the `saturated_gated_queue` fixture, 50ms in `batch_submit_applies_backpressure_when_saturated` and `streaming_submit_applies_backpressure_when_saturated`, 30ms in the three cancellation tests. A slow CI box requires hunting down every copy, and the inconsistency suggests the values are arbitrary rather than reasoned. Define one `const BACKPRESSURE_SETTLE: Duration` (pick the largest needed value) next to the PARK_POLL constants and use it at every park/settle point.\n+- [ ] `crates/llama-agent/src/queue.rs:3418` — Test helper `streaming_request` is named for the streaming path but constructs a generic `GenerationRequest` used heavily by batch tests (e.g. `submit_request(streaming_request(...))` in `batch_submit_applies_backpressure_when_saturated`, `worker_released_after_batch_completion`, `batch_turns_processed_in_fifo_order`), so the name misstates what the helper builds and reads as a copy-paste mistake at every batch call site. Rename the helper to something path-neutral such as `test_request` or `request_with_max_tokens`, matching the other neutral helper names in this module (`create_test_session`, `gated_queue`).\n+- [ ] `crates/llama-agent/src/queue.rs:3433` — The poll-until-condition budget `for _ in 0..200` with a 5ms sleep is duplicated inline in `await_queue_drained` and `await_session_cleared`, while the third poll helper (`await_worker_parked`) already names its budget as constants. Three copies of the same loop shape with the timing knobs named in only one of them defeats the stated goal of that naming ('retuning for a slow CI box is a single edit here'). Hoist shared `POLL_ATTEMPTS`/`POLL_INTERVAL` constants (or a generic `poll_until(condition)` helper parameterized by them) and have all three wait helpers consume the same named values.\n+- [ ] `crates/llama-agent/src/queue.rs:3433` — `await_queue_drained` hardcodes its poll budget (`0..200` attempts at `Duration::from_millis(5)`), while the adjacent `await_worker_parked` helper names the exact same kind of values as `PARK_POLL_ATTEMPTS`/`PARK_POLL_INTERVAL` specifically so 'retuning for a slow CI box is a single edit'. The unnamed copies defeat that stated goal. Hoist shared `const DRAIN_POLL_ATTEMPTS: usize = 200;` and `const DRAIN_POLL_INTERVAL: Duration = Duration::from_millis(5);` (or reuse the PARK_POLL constants) at module scope and use them in `await_queue_drained`.\n+- [ ] `crates/llama-agent/src/queue.rs:3541` — The finish-reason string literal \"EndOfSequence\" is constructed/asserted five times in the changed test code. If the production wording ever changes, five scattered string literals must be updated together or assertions silently test the wrong value. Add a small helper (e.g. `fn eos() -> FinishReason`) or a `const EOS_REASON: &str = \"EndOfSequence\";` in the test module and reference it at all five sites.\n+- [ ] `crates/llama-agent/src/queue.rs:3718` — `_busy` carries the leading-underscore \"intentionally unused\" convention but is actively used — it is pinned and polled in a `select!` two lines later — so the name signals the opposite of the binding's role and invites an incorrect cleanup deleting it. Rename to `busy` (no underscore prefix); it is referenced, so the unused-variable lint will not fire.\n+- [ ] `crates/llama-agent/src/queue.rs:3824` — The 5-second overall test timeout is repeated as a bare `Duration::from_secs(5)` in five of the new/modified backpressure and cancellation tests; it is the suite-wide 'must not wedge' budget and should be a single named constant so it reads as policy and can be retuned in one place. Add `const TEST_TIMEOUT: Duration = Duration::from_secs(5);` at the test-module scope and use it for every `tokio::time::timeout` budget.\n+- [ ] `crates/llama-agent/src/queue.rs:3900` — `await_session_cleared` duplicates the same unnamed `0..200` × 5ms poll budget as `await_queue_drained`, so the test-suite's polling tolerance now lives as bare literals in three places (one named, two not) that can silently drift apart. Use the same module-scope poll constants here instead of repeating the literals.\n+- [ ] `crates/llama-agent/src/queue.rs:3990` — streaming_submit_clears_active_requests_on_cancellation is the third near-identical 'saturate the queue → spawn a submit on a fresh session → assert parked → cancel → assert error/cleanup' test body (~45 lines), copied from submit_request_clears_active_requests_on_cancellation with only the submit call changed. Rule of three is now met, and each future change to this scenario (timing constants, saturation fixture shape, assertion messages) must be edited in three places that will silently diverge. Extract a helper that takes the submit as a closure returning a future of Result<(), QueueError> (map the batch response and the streaming receiver to () so the differing Ok types unify), e.g. assert_cancelled_backpressured_submit_clears_entry(|queue, session| async move { ... }), and have both *_clears_active_requests_on_cancellation tests call it.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -33,24 +33,4 @@\n - [ ] `cargo test -p llama-agent queue` — all green, every test fast (<10s, scripted/gated fake executor only, no real model).\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Review Findings (2026-06-10 14:42)\n-\n-### Warnings\n-- [ ] `crates/llama-agent/src/queue.rs:865` — ActiveRequestGuard::drop removes the session's active_requests entry via a detached spawned task, which races with the next submit on the same session: if turn N's guard drops, then turn N+1 calls track_cancellation_token (queue.rs:1123-1124 inserts unconditionally) before the spawned removal acquires the lock, the stale removal deletes turn N+1's fresh token. cancel_session then returns false and the in-flight turn is uncancellable. Sequential turns on one session — the agentic loop's normal shape — hit exactly this back-to-back pattern. Make the removal conditional on identity: store a monotonically increasing generation id alongside the token (map value `(u64, CancellationToken)`), record the generation in the guard, and have the spawned task remove the entry only if the stored generation still matches the guard's.\n-- [ ] `crates/llama-agent/src/queue.rs:1166` — Cancellation — an expected, client-initiated outcome — and shutdown are both surfaced as QueueError::WorkerError(String), and the new doc comments (queue.rs:1071-1073, 1178-1181) codify that conflation. Callers cannot distinguish 'user cancelled' from 'worker failed' without string-matching; the new test batch_submit_backpressure_honors_cancellation can only assert `result.is_err()` because no variant exists to match. Add `QueueError::Cancelled` and `QueueError::ShuttingDown` variants via thiserror, return them from enqueue_request's select arms, and update the new doc comments on submit_request/submit_streaming_request to name the specific variants.\n-- [ ] `crates/llama-agent/src/queue.rs:1234` — After guard.disarm(), nothing in RequestQueue ever removes a streaming session's active_requests entry when the turn completes normally: removal happens only in the guard's Drop (disarmed here) and in cancel_session. Entries for completed streaming turns persist until overwritten by a later submit on the same session, so on the long-lived shared server the map grows with every session that ends without cancellation — the same stale-entry accumulation the guard was introduced to fix — and cancel_session spuriously returns true (and fires a dead token) for already-finished sessions. Hand ownership of the entry's lifetime to the turn itself: pass the (disarmed) guard or an equivalent removal handle into the QueuedRequest so the worker clears the entry when the streaming turn finishes, or have the stream-relay completion path remove the session's entry once the final chunk is sent.\n-- [ ] `crates/llama-agent/src/queue.rs:3824` — The backpressure-resolution timeout `Duration::from_secs(5)` is hardcoded at five separate call sites in the new tests. Retuning this budget for a slow CI box means finding and editing all five in lockstep; missing one yields tests that flake at different thresholds. Hoist one `const RESOLVE_TIMEOUT: Duration = Duration::from_secs(5);` in `worker_lifecycle_tests` and use it at all five sites, mirroring how `await_worker_parked` already names `PARK_POLL_ATTEMPTS`/`PARK_POLL_INTERVAL` precisely so 'retuning for a slow CI box is a single edit'.\n-- [ ] `crates/llama-agent/src/queue.rs:3868` — The 'sleep so the spawned submit can park, then assert it is still pending' settle delay is hardcoded eight times with three different magic values (20ms, 30ms, 50ms) for the same semantic. The values must stay in lockstep with scheduler timing, but a human retuning them has to know which of the three numbers is load-bearing at each site — they will drift. Define one named `const SETTLE_DELAY: Duration` (pick the largest, 50ms) in `worker_lifecycle_tests` and use it everywhere a test waits for a spawned submit to park, so CI retuning is a single edit.\n-\n-### Nits\n-- [ ] `crates/llama-agent/src/queue.rs:3406` — The 'let the submit park on backpressure' settle delay is a timing-sensitive tuning value, but it appears as four different bare literals for the same purpose — 20ms in the `saturated_gated_queue` fixture, 50ms in `batch_submit_applies_backpressure_when_saturated` and `streaming_submit_applies_backpressure_when_saturated`, 30ms in the three cancellation tests. A slow CI box requires hunting down every copy, and the inconsistency suggests the values are arbitrary rather than reasoned. Define one `const BACKPRESSURE_SETTLE: Duration` (pick the largest needed value) next to the PARK_POLL constants and use it at every park/settle point.\n-- [ ] `crates/llama-agent/src/queue.rs:3418` — Test helper `streaming_request` is named for the streaming path but constructs a generic `GenerationRequest` used heavily by batch tests (e.g. `submit_request(streaming_request(...))` in `batch_submit_applies_backpressure_when_saturated`, `worker_released_after_batch_completion`, `batch_turns_processed_in_fifo_order`), so the name misstates what the helper builds and reads as a copy-paste mistake at every batch call site. Rename the helper to something path-neutral such as `test_request` or `request_with_max_tokens`, matching the other neutral helper names in this module (`create_test_session`, `gated_queue`).\n-- [ ] `crates/llama-agent/src/queue.rs:3433` — The poll-until-condition budget `for _ in 0..200` with a 5ms sleep is duplicated inline in `await_queue_drained` and `await_session_cleared`, while the third poll helper (`await_worker_parked`) already names its budget as constants. Three copies of the same loop shape with the timing knobs named in only one of them defeats the stated goal of that naming ('retuning for a slow CI box is a single edit here'). Hoist shared `POLL_ATTEMPTS`/`POLL_INTERVAL` constants (or a generic `poll_until(condition)` helper parameterized by them) and have all three wait helpers consume the same named values.\n-- [ ] `crates/llama-agent/src/queue.rs:3433` — `await_queue_drained` hardcodes its poll budget (`0..200` attempts at `Duration::from_millis(5)`), while the adjacent `await_worker_parked` helper names the exact same kind of values as `PARK_POLL_ATTEMPTS`/`PARK_POLL_INTERVAL` specifically so 'retuning for a slow CI box is a single edit'. The unnamed copies defeat that stated goal. Hoist shared `const DRAIN_POLL_ATTEMPTS: usize = 200;` and `const DRAIN_POLL_INTERVAL: Duration = Duration::from_millis(5);` (or reuse the PARK_POLL constants) at module scope and use them in `await_queue_drained`.\n-- [ ] `crates/llama-agent/src/queue.rs:3541` — The finish-reason string literal \"EndOfSequence\" is constructed/asserted five times in the changed test code. If the production wording ever changes, five scattered string literals must be updated together or assertions silently test the wrong value. Add a small helper (e.g. `fn eos() -> FinishReason`) or a `const EOS_REASON: &str = \"EndOfSequence\";` in the test module and reference it at all five sites.\n-- [ ] `crates/llama-agent/src/queue.rs:3718` — `_busy` carries the leading-underscore \"intentionally unused\" convention but is actively used — it is pinned and polled in a `select!` two lines later — so the name signals the opposite of the binding's role and invites an incorrect cleanup deleting it. Rename to `busy` (no underscore prefix); it is referenced, so the unused-variable lint will not fire.\n-- [ ] `crates/llama-agent/src/queue.rs:3824` — The 5-second overall test timeout is repeated as a bare `Duration::from_secs(5)` in five of the new/modified backpressure and cancellation tests; it is the suite-wide 'must not wedge' budget and should be a single named constant so it reads as policy and can be retuned in one place. Add `const TEST_TIMEOUT: Duration = Duration::from_secs(5);` at the test-module scope and use it for every `tokio::time::timeout` budget.\n-- [ ] `crates/llama-agent/src/queue.rs:3900` — `await_session_cleared` duplicates the same unnamed `0..200` × 5ms poll budget as `await_queue_drained`, so the test-suite's polling tolerance now lives as bare literals in three places (one named, two not) that can silently drift apart. Use the same module-scope poll constants here instead of repeating the literals.\n-- [ ] `crates/llama-agent/src/queue.rs:3990` — streaming_submit_clears_active_requests_on_cancellation is the third near-identical 'saturate the queue → spawn a submit on a fresh session → assert parked → cancel → assert error/cleanup' test body (~45 lines), copied from submit_request_clears_active_requests_on_cancellation with only the submit call changed. Rule of three is now met, and each future change to this scenario (timing constants, saturation fixture shape, assertion messages) must be edited in three places that will silently diverge. Extract a helper that takes the submit as a closure returning a future of Result<(), QueueError> (map the batch response and the streaming receiver to () so the differing Ok types unify), e.g. assert_cancelled_backpressured_submit_clears_entry(|queue, session| async move { ... }), and have both *_clears_active_requests_on_cancellation tests call it.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTSHMT1HEAZFVMN29N2WM9HF","timestamp":"2026-06-10T19:55:12.817133Z","op":"update","item_id":"01KTS1D74PYWX5XWYZY20KR1M4","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): streaming submit path gets backpressure'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: local-review\n title: 'fix(llama-agent): streaming submit path gets backpressure'\n ---\n","transaction_id":null}
{"id":"01KTSJN71RHZE5K4NN2PJW6K31","timestamp":"2026-06-10T20:12:54.712675Z","op":"update","item_id":"01KTS1D74PYWX5XWYZY20KR1M4","forward_patch":"--- original\n+++ modified\n@@ -12,25 +12,25 @@\n\n Fix, all in `crates/llama-agent/src/queue.rs`:\n\n-- [ ] Make `submit_streaming_request` route its queue submission through `enqueue_request` (the `tokio::select!` on `sender.send()` vs the request's `CancellationToken`) instead of `try_send`.\n-- [ ] On enqueue failure (cancelled or shutdown), remove the session's entry from `active_requests` — the streaming path inserts into `active_requests` (~line 1170) before enqueue and has no `ActiveRequestGuard`, so it would otherwise leak. Prefer reusing `ActiveRequestGuard` the way `submit_request` does.\n-- [ ] Update the now-stale doc comments: `enqueue_request`'s \"`Full` is still returned for the unbounded streaming path's non-blocking submit\" paragraph, `submit_streaming_request`'s docs, and `try_enqueue_for_test`'s \"Mirrors the non-blocking `try_send` the streaming submit path uses\" sentence.\n-- [ ] Replace the test `streaming_submit_returns_full_at_capacity` (~line 4170, comment \"The streaming submit path has its own try_send + Full branch\") — that behavior is the bug.\n+- [x] Make `submit_streaming_request` route its queue submission through `enqueue_request` (the `tokio::select!` on `sender.send()` vs the request's `CancellationToken`) instead of `try_send`.\n+- [x] On enqueue failure (cancelled or shutdown), remove the session's entry from `active_requests` — the streaming path inserts into `active_requests` (~line 1170) before enqueue and has no `ActiveRequestGuard`, so it would otherwise leak. Prefer reusing `ActiveRequestGuard` the way `submit_request` does.\n+- [x] Update the now-stale doc comments: `enqueue_request`'s \"`Full` is still returned for the unbounded streaming path's non-blocking submit\" paragraph, `submit_streaming_request`'s docs, and `try_enqueue_for_test`'s \"Mirrors the non-blocking `try_send` the streaming submit path uses\" sentence.\n+- [x] Replace the test `streaming_submit_returns_full_at_capacity` (~line 4170, comment \"The streaming submit path has its own try_send + Full branch\") — that behavior is the bug.\n\n Do NOT touch the `mpsc::unbounded_channel` for StreamChunks inside `submit_streaming_request` — it is unbounded by design (documented producer-wedge rationale at ~line 1157). Only the queue-submission `try_send` changes. Keep `try_enqueue_for_test` intact for capacity tests (`QueueError::Full` variant stays).\n\n ## Acceptance Criteria\n\n-- [ ] `submit_streaming_request` never returns `QueueError::Full`; with the queue at capacity it waits for a slot.\n-- [ ] A cancellation fired while a streaming submit is waiting for capacity returns an error and leaves no entry in `active_requests`.\n-- [ ] No stale doc comments claiming the streaming path uses `try_send`.\n-- [ ] All existing queue tests pass; `try_enqueue_for_test` capacity tests unchanged.\n+- [x] `submit_streaming_request` never returns `QueueError::Full`; with the queue at capacity it waits for a slot.\n+- [x] A cancellation fired while a streaming submit is waiting for capacity returns an error and leaves no entry in `active_requests`.\n+- [x] No stale doc comments claiming the streaming path uses `try_send`.\n+- [x] All existing queue tests pass; `try_enqueue_for_test` capacity tests unchanged.\n\n ## Tests\n\n-- [ ] In `crates/llama-agent/src/queue.rs` tests: new test mirroring `submit_request_clears_active_requests_on_cancellation` (~line 3899) and the saturated-gated-queue backpressure tests, proving (a) with the queue saturated via `saturated_gated_queue()`, `submit_streaming_request` parks instead of returning `QueueError::Full`, then completes once the gate opens; (b) cancelling its token while parked returns `Err` and `await_session_cleared` confirms `active_requests` is empty.\n-- [ ] Replace `streaming_submit_returns_full_at_capacity` with the wait-instead-of-Full assertion.\n-- [ ] `cargo test -p llama-agent queue` — all green, every test fast (<10s, scripted/gated fake executor only, no real model).\n+- [x] In `crates/llama-agent/src/queue.rs` tests: new test mirroring `submit_request_clears_active_requests_on_cancellation` (~line 3899) and the saturated-gated-queue backpressure tests, proving (a) with the queue saturated via `saturated_gated_queue()`, `submit_streaming_request` parks instead of returning `QueueError::Full`, then completes once the gate opens; (b) cancelling its token while parked returns `Err` and `await_session_cleared` confirms `active_requests` is empty.\n+- [x] Replace `streaming_submit_returns_full_at_capacity` with the wait-instead-of-Full assertion.\n+- [x] `cargo test -p llama-agent queue` — all green, every test fast (<10s, scripted/gated fake executor only, no real model).\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n@@ -38,19 +38,25 @@\n ## Review Findings (2026-06-10 14:42)\n\n ### Warnings\n-- [ ] `crates/llama-agent/src/queue.rs:865` — ActiveRequestGuard::drop removes the session's active_requests entry via a detached spawned task, which races with the next submit on the same session: if turn N's guard drops, then turn N+1 calls track_cancellation_token (queue.rs:1123-1124 inserts unconditionally) before the spawned removal acquires the lock, the stale removal deletes turn N+1's fresh token. cancel_session then returns false and the in-flight turn is uncancellable. Sequential turns on one session — the agentic loop's normal shape — hit exactly this back-to-back pattern. Make the removal conditional on identity: store a monotonically increasing generation id alongside the token (map value `(u64, CancellationToken)`), record the generation in the guard, and have the spawned task remove the entry only if the stored generation still matches the guard's.\n-- [ ] `crates/llama-agent/src/queue.rs:1166` — Cancellation — an expected, client-initiated outcome — and shutdown are both surfaced as QueueError::WorkerError(String), and the new doc comments (queue.rs:1071-1073, 1178-1181) codify that conflation. Callers cannot distinguish 'user cancelled' from 'worker failed' without string-matching; the new test batch_submit_backpressure_honors_cancellation can only assert `result.is_err()` because no variant exists to match. Add `QueueError::Cancelled` and `QueueError::ShuttingDown` variants via thiserror, return them from enqueue_request's select arms, and update the new doc comments on submit_request/submit_streaming_request to name the specific variants.\n-- [ ] `crates/llama-agent/src/queue.rs:1234` — After guard.disarm(), nothing in RequestQueue ever removes a streaming session's active_requests entry when the turn completes normally: removal happens only in the guard's Drop (disarmed here) and in cancel_session. Entries for completed streaming turns persist until overwritten by a later submit on the same session, so on the long-lived shared server the map grows with every session that ends without cancellation — the same stale-entry accumulation the guard was introduced to fix — and cancel_session spuriously returns true (and fires a dead token) for already-finished sessions. Hand ownership of the entry's lifetime to the turn itself: pass the (disarmed) guard or an equivalent removal handle into the QueuedRequest so the worker clears the entry when the streaming turn finishes, or have the stream-relay completion path remove the session's entry once the final chunk is sent.\n-- [ ] `crates/llama-agent/src/queue.rs:3824` — The backpressure-resolution timeout `Duration::from_secs(5)` is hardcoded at five separate call sites in the new tests. Retuning this budget for a slow CI box means finding and editing all five in lockstep; missing one yields tests that flake at different thresholds. Hoist one `const RESOLVE_TIMEOUT: Duration = Duration::from_secs(5);` in `worker_lifecycle_tests` and use it at all five sites, mirroring how `await_worker_parked` already names `PARK_POLL_ATTEMPTS`/`PARK_POLL_INTERVAL` precisely so 'retuning for a slow CI box is a single edit'.\n-- [ ] `crates/llama-agent/src/queue.rs:3868` — The 'sleep so the spawned submit can park, then assert it is still pending' settle delay is hardcoded eight times with three different magic values (20ms, 30ms, 50ms) for the same semantic. The values must stay in lockstep with scheduler timing, but a human retuning them has to know which of the three numbers is load-bearing at each site — they will drift. Define one named `const SETTLE_DELAY: Duration` (pick the largest, 50ms) in `worker_lifecycle_tests` and use it everywhere a test waits for a spawned submit to park, so CI retuning is a single edit.\n+- [x] `crates/llama-agent/src/queue.rs:865` — ActiveRequestGuard::drop removes the session's active_requests entry via a detached spawned task, which races with the next submit on the same session: if turn N's guard drops, then turn N+1 calls track_cancellation_token (queue.rs:1123-1124 inserts unconditionally) before the spawned removal acquires the lock, the stale removal deletes turn N+1's fresh token. cancel_session then returns false and the in-flight turn is uncancellable. Sequential turns on one session — the agentic loop's normal shape — hit exactly this back-to-back pattern. Make the removal conditional on identity: store a monotonically increasing generation id alongside the token (map value `(u64, CancellationToken)`), record the generation in the guard, and have the spawned task remove the entry only if the stored generation still matches the guard's. → FIXED: `active_requests` now maps to `TrackedToken { generation, token }`; `track_cancellation_token` assigns a monotonic generation (`request_generation: AtomicU64` on `RequestQueue`) and returns it; the guard records it and its spawned removal only deletes the entry if the stored generation still matches. Regression test `stale_guard_drop_does_not_clobber_newer_turns_token`.\n+- [x] `crates/llama-agent/src/queue.rs:1166` — Cancellation — an expected, client-initiated outcome — and shutdown are both surfaced as QueueError::WorkerError(String), and the new doc comments (queue.rs:1071-1073, 1178-1181) codify that conflation. Callers cannot distinguish 'user cancelled' from 'worker failed' without string-matching; the new test batch_submit_backpressure_honors_cancellation can only assert `result.is_err()` because no variant exists to match. Add `QueueError::Cancelled` and `QueueError::ShuttingDown` variants via thiserror, return them from enqueue_request's select arms, and update the new doc comments on submit_request/submit_streaming_request to name the specific variants. → FIXED: added `QueueError::Cancelled` and `QueueError::ShuttingDown` thiserror variants; `enqueue_request`'s select arms (and the closed-sender check) return them; submit_request/submit_streaming_request/enqueue_request docs name the specific variants; the cancellation tests now assert `Err(QueueError::Cancelled)`. The enum lives in `types/errors.rs`, so the exhaustive matches there plus `acp/translation.rs` and `validation/queue_validator.rs` gained mechanical arms (the only edits outside queue.rs; ~30 lines total).\n+- [x] `crates/llama-agent/src/queue.rs:1234` — After guard.disarm(), nothing in RequestQueue ever removes a streaming session's active_requests entry when the turn completes normally: removal happens only in the guard's Drop (disarmed here) and in cancel_session. Entries for completed streaming turns persist until overwritten by a later submit on the same session, so on the long-lived shared server the map grows with every session that ends without cancellation — the same stale-entry accumulation the guard was introduced to fix — and cancel_session spuriously returns true (and fires a dead token) for already-finished sessions. Hand ownership of the entry's lifetime to the turn itself: pass the (disarmed) guard or an equivalent removal handle into the QueuedRequest so the worker clears the entry when the streaming turn finishes, or have the stream-relay completion path remove the session's entry once the final chunk is sent. → FIXED: `disarm()` is gone; the streaming submit moves its (armed) guard into `QueuedRequest::active_request_guard`, so the entry lives exactly as long as the turn — the worker drops the request (and guard) when `execute_streaming` finishes, and the enqueue-failure paths drop it on unwind. Mid-stream cancellation still works because the entry persists while the worker holds the request. Regression test `streaming_turn_clears_active_requests_on_completion`.\n+- [x] `crates/llama-agent/src/queue.rs:3824` — The backpressure-resolution timeout `Duration::from_secs(5)` is hardcoded at five separate call sites in the new tests. Retuning this budget for a slow CI box means finding and editing all five in lockstep; missing one yields tests that flake at different thresholds. Hoist one `const RESOLVE_TIMEOUT: Duration = Duration::from_secs(5);` in `worker_lifecycle_tests` and use it at all five sites, mirroring how `await_worker_parked` already names `PARK_POLL_ATTEMPTS`/`PARK_POLL_INTERVAL` precisely so 'retuning for a slow CI box is a single edit'. → FIXED: single `const TEST_TIMEOUT: Duration = Duration::from_secs(5)` used at all five `tokio::time::timeout` sites.\n+- [x] `crates/llama-agent/src/queue.rs:3868` — The 'sleep so the spawned submit can park, then assert it is still pending' settle delay is hardcoded eight times with three different magic values (20ms, 30ms, 50ms) for the same semantic. The values must stay in lockstep with scheduler timing, but a human retuning them has to know which of the three numbers is load-bearing at each site — they will drift. Define one named `const SETTLE_DELAY: Duration` (pick the largest, 50ms) in `worker_lifecycle_tests` and use it everywhere a test waits for a spawned submit to park, so CI retuning is a single edit. → FIXED: single `const SETTLE_DELAY: Duration = Duration::from_millis(50)` at every park/settle point (also satisfies the BACKPRESSURE_SETTLE nit below).\n\n ### Nits\n-- [ ] `crates/llama-agent/src/queue.rs:3406` — The 'let the submit park on backpressure' settle delay is a timing-sensitive tuning value, but it appears as four different bare literals for the same purpose — 20ms in the `saturated_gated_queue` fixture, 50ms in `batch_submit_applies_backpressure_when_saturated` and `streaming_submit_applies_backpressure_when_saturated`, 30ms in the three cancellation tests. A slow CI box requires hunting down every copy, and the inconsistency suggests the values are arbitrary rather than reasoned. Define one `const BACKPRESSURE_SETTLE: Duration` (pick the largest needed value) next to the PARK_POLL constants and use it at every park/settle point.\n-- [ ] `crates/llama-agent/src/queue.rs:3418` — Test helper `streaming_request` is named for the streaming path but constructs a generic `GenerationRequest` used heavily by batch tests (e.g. `submit_request(streaming_request(...))` in `batch_submit_applies_backpressure_when_saturated`, `worker_released_after_batch_completion`, `batch_turns_processed_in_fifo_order`), so the name misstates what the helper builds and reads as a copy-paste mistake at every batch call site. Rename the helper to something path-neutral such as `test_request` or `request_with_max_tokens`, matching the other neutral helper names in this module (`create_test_session`, `gated_queue`).\n-- [ ] `crates/llama-agent/src/queue.rs:3433` — The poll-until-condition budget `for _ in 0..200` with a 5ms sleep is duplicated inline in `await_queue_drained` and `await_session_cleared`, while the third poll helper (`await_worker_parked`) already names its budget as constants. Three copies of the same loop shape with the timing knobs named in only one of them defeats the stated goal of that naming ('retuning for a slow CI box is a single edit here'). Hoist shared `POLL_ATTEMPTS`/`POLL_INTERVAL` constants (or a generic `poll_until(condition)` helper parameterized by them) and have all three wait helpers consume the same named values.\n-- [ ] `crates/llama-agent/src/queue.rs:3433` — `await_queue_drained` hardcodes its poll budget (`0..200` attempts at `Duration::from_millis(5)`), while the adjacent `await_worker_parked` helper names the exact same kind of values as `PARK_POLL_ATTEMPTS`/`PARK_POLL_INTERVAL` specifically so 'retuning for a slow CI box is a single edit'. The unnamed copies defeat that stated goal. Hoist shared `const DRAIN_POLL_ATTEMPTS: usize = 200;` and `const DRAIN_POLL_INTERVAL: Duration = Duration::from_millis(5);` (or reuse the PARK_POLL constants) at module scope and use them in `await_queue_drained`.\n-- [ ] `crates/llama-agent/src/queue.rs:3541` — The finish-reason string literal \"EndOfSequence\" is constructed/asserted five times in the changed test code. If the production wording ever changes, five scattered string literals must be updated together or assertions silently test the wrong value. Add a small helper (e.g. `fn eos() -> FinishReason`) or a `const EOS_REASON: &str = \"EndOfSequence\";` in the test module and reference it at all five sites.\n-- [ ] `crates/llama-agent/src/queue.rs:3718` — `_busy` carries the leading-underscore \"intentionally unused\" convention but is actively used — it is pinned and polled in a `select!` two lines later — so the name signals the opposite of the binding's role and invites an incorrect cleanup deleting it. Rename to `busy` (no underscore prefix); it is referenced, so the unused-variable lint will not fire.\n-- [ ] `crates/llama-agent/src/queue.rs:3824` — The 5-second overall test timeout is repeated as a bare `Duration::from_secs(5)` in five of the new/modified backpressure and cancellation tests; it is the suite-wide 'must not wedge' budget and should be a single named constant so it reads as policy and can be retuned in one place. Add `const TEST_TIMEOUT: Duration = Duration::from_secs(5);` at the test-module scope and use it for every `tokio::time::timeout` budget.\n-- [ ] `crates/llama-agent/src/queue.rs:3900` — `await_session_cleared` duplicates the same unnamed `0..200` × 5ms poll budget as `await_queue_drained`, so the test-suite's polling tolerance now lives as bare literals in three places (one named, two not) that can silently drift apart. Use the same module-scope poll constants here instead of repeating the literals.\n-- [ ] `crates/llama-agent/src/queue.rs:3990` — streaming_submit_clears_active_requests_on_cancellation is the third near-identical 'saturate the queue → spawn a submit on a fresh session → assert parked → cancel → assert error/cleanup' test body (~45 lines), copied from submit_request_clears_active_requests_on_cancellation with only the submit call changed. Rule of three is now met, and each future change to this scenario (timing constants, saturation fixture shape, assertion messages) must be edited in three places that will silently diverge. Extract a helper that takes the submit as a closure returning a future of Result<(), QueueError> (map the batch response and the streaming receiver to () so the differing Ok types unify), e.g. assert_cancelled_backpressured_submit_clears_entry(|queue, session| async move { ... }), and have both *_clears_active_requests_on_cancellation tests call it.\n\\ No newline at end of file\n+- [x] `crates/llama-agent/src/queue.rs:3406` — The 'let the submit park on backpressure' settle delay is a timing-sensitive tuning value, but it appears as four different bare literals for the same purpose — 20ms in the `saturated_gated_queue` fixture, 50ms in `batch_submit_applies_backpressure_when_saturated` and `streaming_submit_applies_backpressure_when_saturated`, 30ms in the three cancellation tests. A slow CI box requires hunting down every copy, and the inconsistency suggests the values are arbitrary rather than reasoned. Define one `const BACKPRESSURE_SETTLE: Duration` (pick the largest needed value) next to the PARK_POLL constants and use it at every park/settle point. → FIXED: covered by the shared `SETTLE_DELAY` constant.\n+- [x] `crates/llama-agent/src/queue.rs:3418` — Test helper `streaming_request` is named for the streaming path but constructs a generic `GenerationRequest` used heavily by batch tests (e.g. `submit_request(streaming_request(...))` in `batch_submit_applies_backpressure_when_saturated`, `worker_released_after_batch_completion`, `batch_turns_processed_in_fifo_order`), so the name misstates what the helper builds and reads as a copy-paste mistake at every batch call site. Rename the helper to something path-neutral such as `test_request` or `request_with_max_tokens`, matching the other neutral helper names in this module (`create_test_session`, `gated_queue`). → FIXED: renamed to `test_request` at the definition and every call site.\n+- [x] `crates/llama-agent/src/queue.rs:3433` — The poll-until-condition budget `for _ in 0..200` with a 5ms sleep is duplicated inline in `await_queue_drained` and `await_session_cleared`, while the third poll helper (`await_worker_parked`) already names its budget as constants. Three copies of the same loop shape with the timing knobs named in only one of them defeats the stated goal of that naming ('retuning for a slow CI box is a single edit here'). Hoist shared `POLL_ATTEMPTS`/`POLL_INTERVAL` constants (or a generic `poll_until(condition)` helper parameterized by them) and have all three wait helpers consume the same named values. → FIXED: module-scope `POLL_ATTEMPTS`/`POLL_INTERVAL` consumed by all three wait helpers (the local `PARK_POLL_*` constants were removed in favor of the shared ones).\n+- [x] `crates/llama-agent/src/queue.rs:3433` — `await_queue_drained` hardcodes its poll budget (`0..200` attempts at `Duration::from_millis(5)`), while the adjacent `await_worker_parked` helper names the exact same kind of values as `PARK_POLL_ATTEMPTS`/`PARK_POLL_INTERVAL` specifically so 'retuning for a slow CI box is a single edit'. The unnamed copies defeat that stated goal. Hoist shared `const DRAIN_POLL_ATTEMPTS: usize = 200;` and `const DRAIN_POLL_INTERVAL: Duration = Duration::from_millis(5);` (or reuse the PARK_POLL constants) at module scope and use them in `await_queue_drained`. → FIXED: same shared `POLL_ATTEMPTS`/`POLL_INTERVAL`.\n+- [x] `crates/llama-agent/src/queue.rs:3541` — The finish-reason string literal \"EndOfSequence\" is constructed/asserted five times in the changed test code. If the production wording ever changes, five scattered string literals must be updated together or assertions silently test the wrong value. Add a small helper (e.g. `fn eos() -> FinishReason`) or a `const EOS_REASON: &str = \"EndOfSequence\";` in the test module and reference it at all five sites. → FIXED: `fn eos() -> FinishReason` in `worker_lifecycle_tests`, used at all five construction/assertion sites (`ScriptToken::EndOfSequence` script entries are a different type and unchanged).\n+- [x] `crates/llama-agent/src/queue.rs:3718` — `_busy` carries the leading-underscore \"intentionally unused\" convention but is actively used — it is pinned and polled in a `select!` two lines later — so the name signals the opposite of the binding's role and invites an incorrect cleanup deleting it. Rename to `busy` (no underscore prefix); it is referenced, so the unused-variable lint will not fire. → FIXED: renamed to `busy`.\n+- [x] `crates/llama-agent/src/queue.rs:3824` — The 5-second overall test timeout is repeated as a bare `Duration::from_secs(5)` in five of the new/modified backpressure and cancellation tests; it is the suite-wide 'must not wedge' budget and should be a single named constant so it reads as policy and can be retuned in one place. Add `const TEST_TIMEOUT: Duration = Duration::from_secs(5);` at the test-module scope and use it for every `tokio::time::timeout` budget. → FIXED: `TEST_TIMEOUT` as above.\n+- [x] `crates/llama-agent/src/queue.rs:3900` — `await_session_cleared` duplicates the same unnamed `0..200` × 5ms poll budget as `await_queue_drained`, so the test-suite's polling tolerance now lives as bare literals in three places (one named, two not) that can silently drift apart. Use the same module-scope poll constants here instead of repeating the literals. → FIXED: same shared `POLL_ATTEMPTS`/`POLL_INTERVAL`.\n+- [x] `crates/llama-agent/src/queue.rs:3990` — streaming_submit_clears_active_requests_on_cancellation is the third near-identical 'saturate the queue → spawn a submit on a fresh session → assert parked → cancel → assert error/cleanup' test body (~45 lines), copied from submit_request_clears_active_requests_on_cancellation with only the submit call changed. Rule of three is now met, and each future change to this scenario (timing constants, saturation fixture shape, assertion messages) must be edited in three places that will silently diverge. Extract a helper that takes the submit as a closure returning a future of Result<(), QueueError> (map the batch response and the streaming receiver to () so the differing Ok types unify), e.g. assert_cancelled_backpressured_submit_clears_entry(|queue, session| async move { ... }), and have both *_clears_active_requests_on_cancellation tests call it. → FIXED: extracted `assert_cancelled_backpressured_submit_clears_entry(submit)` exactly as suggested; both `*_clears_active_requests_on_cancellation` tests are now thin closures over it, and the helper asserts the precise `Err(QueueError::Cancelled)` variant.\n+\n+## Implementation Notes (2026-06-10, review-findings pass)\n+\n+- TDD: all five behavioral changes were captured as failing tests first (`stale_guard_drop_does_not_clobber_newer_turns_token`, `streaming_turn_clears_active_requests_on_completion`, and the three cancellation tests tightened to `Err(QueueError::Cancelled)`); watched fail, then implemented to green.\n+- Scope note: the `QueueError::Cancelled`/`ShuttingDown` variants required touching the enum's home (`types/errors.rs`) and the two exhaustive matches over it (`acp/translation.rs` JSON-RPC mapping, `validation/queue_validator.rs` From impl) — mechanical arms only, no behavior change elsewhere. `gpu_lock.rs` untouched.\n+- Verified: `cargo test -p llama-agent --lib` → 1075 passed / 0 failed in 5.8s; `cargo clippy -p llama-agent --lib --tests -- -D warnings` clean; `cargo fmt -p llama-agent` applied.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -12,25 +12,25 @@\n\n Fix, all in `crates/llama-agent/src/queue.rs`:\n\n-- [x] Make `submit_streaming_request` route its queue submission through `enqueue_request` (the `tokio::select!` on `sender.send()` vs the request's `CancellationToken`) instead of `try_send`.\n-- [x] On enqueue failure (cancelled or shutdown), remove the session's entry from `active_requests` — the streaming path inserts into `active_requests` (~line 1170) before enqueue and has no `ActiveRequestGuard`, so it would otherwise leak. Prefer reusing `ActiveRequestGuard` the way `submit_request` does.\n-- [x] Update the now-stale doc comments: `enqueue_request`'s \"`Full` is still returned for the unbounded streaming path's non-blocking submit\" paragraph, `submit_streaming_request`'s docs, and `try_enqueue_for_test`'s \"Mirrors the non-blocking `try_send` the streaming submit path uses\" sentence.\n-- [x] Replace the test `streaming_submit_returns_full_at_capacity` (~line 4170, comment \"The streaming submit path has its own try_send + Full branch\") — that behavior is the bug.\n+- [ ] Make `submit_streaming_request` route its queue submission through `enqueue_request` (the `tokio::select!` on `sender.send()` vs the request's `CancellationToken`) instead of `try_send`.\n+- [ ] On enqueue failure (cancelled or shutdown), remove the session's entry from `active_requests` — the streaming path inserts into `active_requests` (~line 1170) before enqueue and has no `ActiveRequestGuard`, so it would otherwise leak. Prefer reusing `ActiveRequestGuard` the way `submit_request` does.\n+- [ ] Update the now-stale doc comments: `enqueue_request`'s \"`Full` is still returned for the unbounded streaming path's non-blocking submit\" paragraph, `submit_streaming_request`'s docs, and `try_enqueue_for_test`'s \"Mirrors the non-blocking `try_send` the streaming submit path uses\" sentence.\n+- [ ] Replace the test `streaming_submit_returns_full_at_capacity` (~line 4170, comment \"The streaming submit path has its own try_send + Full branch\") — that behavior is the bug.\n\n Do NOT touch the `mpsc::unbounded_channel` for StreamChunks inside `submit_streaming_request` — it is unbounded by design (documented producer-wedge rationale at ~line 1157). Only the queue-submission `try_send` changes. Keep `try_enqueue_for_test` intact for capacity tests (`QueueError::Full` variant stays).\n\n ## Acceptance Criteria\n\n-- [x] `submit_streaming_request` never returns `QueueError::Full`; with the queue at capacity it waits for a slot.\n-- [x] A cancellation fired while a streaming submit is waiting for capacity returns an error and leaves no entry in `active_requests`.\n-- [x] No stale doc comments claiming the streaming path uses `try_send`.\n-- [x] All existing queue tests pass; `try_enqueue_for_test` capacity tests unchanged.\n+- [ ] `submit_streaming_request` never returns `QueueError::Full`; with the queue at capacity it waits for a slot.\n+- [ ] A cancellation fired while a streaming submit is waiting for capacity returns an error and leaves no entry in `active_requests`.\n+- [ ] No stale doc comments claiming the streaming path uses `try_send`.\n+- [ ] All existing queue tests pass; `try_enqueue_for_test` capacity tests unchanged.\n\n ## Tests\n\n-- [x] In `crates/llama-agent/src/queue.rs` tests: new test mirroring `submit_request_clears_active_requests_on_cancellation` (~line 3899) and the saturated-gated-queue backpressure tests, proving (a) with the queue saturated via `saturated_gated_queue()`, `submit_streaming_request` parks instead of returning `QueueError::Full`, then completes once the gate opens; (b) cancelling its token while parked returns `Err` and `await_session_cleared` confirms `active_requests` is empty.\n-- [x] Replace `streaming_submit_returns_full_at_capacity` with the wait-instead-of-Full assertion.\n-- [x] `cargo test -p llama-agent queue` — all green, every test fast (<10s, scripted/gated fake executor only, no real model).\n+- [ ] In `crates/llama-agent/src/queue.rs` tests: new test mirroring `submit_request_clears_active_requests_on_cancellation` (~line 3899) and the saturated-gated-queue backpressure tests, proving (a) with the queue saturated via `saturated_gated_queue()`, `submit_streaming_request` parks instead of returning `QueueError::Full`, then completes once the gate opens; (b) cancelling its token while parked returns `Err` and `await_session_cleared` confirms `active_requests` is empty.\n+- [ ] Replace `streaming_submit_returns_full_at_capacity` with the wait-instead-of-Full assertion.\n+- [ ] `cargo test -p llama-agent queue` — all green, every test fast (<10s, scripted/gated fake executor only, no real model).\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n@@ -38,25 +38,19 @@\n ## Review Findings (2026-06-10 14:42)\n\n ### Warnings\n-- [x] `crates/llama-agent/src/queue.rs:865` — ActiveRequestGuard::drop removes the session's active_requests entry via a detached spawned task, which races with the next submit on the same session: if turn N's guard drops, then turn N+1 calls track_cancellation_token (queue.rs:1123-1124 inserts unconditionally) before the spawned removal acquires the lock, the stale removal deletes turn N+1's fresh token. cancel_session then returns false and the in-flight turn is uncancellable. Sequential turns on one session — the agentic loop's normal shape — hit exactly this back-to-back pattern. Make the removal conditional on identity: store a monotonically increasing generation id alongside the token (map value `(u64, CancellationToken)`), record the generation in the guard, and have the spawned task remove the entry only if the stored generation still matches the guard's. → FIXED: `active_requests` now maps to `TrackedToken { generation, token }`; `track_cancellation_token` assigns a monotonic generation (`request_generation: AtomicU64` on `RequestQueue`) and returns it; the guard records it and its spawned removal only deletes the entry if the stored generation still matches. Regression test `stale_guard_drop_does_not_clobber_newer_turns_token`.\n-- [x] `crates/llama-agent/src/queue.rs:1166` — Cancellation — an expected, client-initiated outcome — and shutdown are both surfaced as QueueError::WorkerError(String), and the new doc comments (queue.rs:1071-1073, 1178-1181) codify that conflation. Callers cannot distinguish 'user cancelled' from 'worker failed' without string-matching; the new test batch_submit_backpressure_honors_cancellation can only assert `result.is_err()` because no variant exists to match. Add `QueueError::Cancelled` and `QueueError::ShuttingDown` variants via thiserror, return them from enqueue_request's select arms, and update the new doc comments on submit_request/submit_streaming_request to name the specific variants. → FIXED: added `QueueError::Cancelled` and `QueueError::ShuttingDown` thiserror variants; `enqueue_request`'s select arms (and the closed-sender check) return them; submit_request/submit_streaming_request/enqueue_request docs name the specific variants; the cancellation tests now assert `Err(QueueError::Cancelled)`. The enum lives in `types/errors.rs`, so the exhaustive matches there plus `acp/translation.rs` and `validation/queue_validator.rs` gained mechanical arms (the only edits outside queue.rs; ~30 lines total).\n-- [x] `crates/llama-agent/src/queue.rs:1234` — After guard.disarm(), nothing in RequestQueue ever removes a streaming session's active_requests entry when the turn completes normally: removal happens only in the guard's Drop (disarmed here) and in cancel_session. Entries for completed streaming turns persist until overwritten by a later submit on the same session, so on the long-lived shared server the map grows with every session that ends without cancellation — the same stale-entry accumulation the guard was introduced to fix — and cancel_session spuriously returns true (and fires a dead token) for already-finished sessions. Hand ownership of the entry's lifetime to the turn itself: pass the (disarmed) guard or an equivalent removal handle into the QueuedRequest so the worker clears the entry when the streaming turn finishes, or have the stream-relay completion path remove the session's entry once the final chunk is sent. → FIXED: `disarm()` is gone; the streaming submit moves its (armed) guard into `QueuedRequest::active_request_guard`, so the entry lives exactly as long as the turn — the worker drops the request (and guard) when `execute_streaming` finishes, and the enqueue-failure paths drop it on unwind. Mid-stream cancellation still works because the entry persists while the worker holds the request. Regression test `streaming_turn_clears_active_requests_on_completion`.\n-- [x] `crates/llama-agent/src/queue.rs:3824` — The backpressure-resolution timeout `Duration::from_secs(5)` is hardcoded at five separate call sites in the new tests. Retuning this budget for a slow CI box means finding and editing all five in lockstep; missing one yields tests that flake at different thresholds. Hoist one `const RESOLVE_TIMEOUT: Duration = Duration::from_secs(5);` in `worker_lifecycle_tests` and use it at all five sites, mirroring how `await_worker_parked` already names `PARK_POLL_ATTEMPTS`/`PARK_POLL_INTERVAL` precisely so 'retuning for a slow CI box is a single edit'. → FIXED: single `const TEST_TIMEOUT: Duration = Duration::from_secs(5)` used at all five `tokio::time::timeout` sites.\n-- [x] `crates/llama-agent/src/queue.rs:3868` — The 'sleep so the spawned submit can park, then assert it is still pending' settle delay is hardcoded eight times with three different magic values (20ms, 30ms, 50ms) for the same semantic. The values must stay in lockstep with scheduler timing, but a human retuning them has to know which of the three numbers is load-bearing at each site — they will drift. Define one named `const SETTLE_DELAY: Duration` (pick the largest, 50ms) in `worker_lifecycle_tests` and use it everywhere a test waits for a spawned submit to park, so CI retuning is a single edit. → FIXED: single `const SETTLE_DELAY: Duration = Duration::from_millis(50)` at every park/settle point (also satisfies the BACKPRESSURE_SETTLE nit below).\n+- [ ] `crates/llama-agent/src/queue.rs:865` — ActiveRequestGuard::drop removes the session's active_requests entry via a detached spawned task, which races with the next submit on the same session: if turn N's guard drops, then turn N+1 calls track_cancellation_token (queue.rs:1123-1124 inserts unconditionally) before the spawned removal acquires the lock, the stale removal deletes turn N+1's fresh token. cancel_session then returns false and the in-flight turn is uncancellable. Sequential turns on one session — the agentic loop's normal shape — hit exactly this back-to-back pattern. Make the removal conditional on identity: store a monotonically increasing generation id alongside the token (map value `(u64, CancellationToken)`), record the generation in the guard, and have the spawned task remove the entry only if the stored generation still matches the guard's.\n+- [ ] `crates/llama-agent/src/queue.rs:1166` — Cancellation — an expected, client-initiated outcome — and shutdown are both surfaced as QueueError::WorkerError(String), and the new doc comments (queue.rs:1071-1073, 1178-1181) codify that conflation. Callers cannot distinguish 'user cancelled' from 'worker failed' without string-matching; the new test batch_submit_backpressure_honors_cancellation can only assert `result.is_err()` because no variant exists to match. Add `QueueError::Cancelled` and `QueueError::ShuttingDown` variants via thiserror, return them from enqueue_request's select arms, and update the new doc comments on submit_request/submit_streaming_request to name the specific variants.\n+- [ ] `crates/llama-agent/src/queue.rs:1234` — After guard.disarm(), nothing in RequestQueue ever removes a streaming session's active_requests entry when the turn completes normally: removal happens only in the guard's Drop (disarmed here) and in cancel_session. Entries for completed streaming turns persist until overwritten by a later submit on the same session, so on the long-lived shared server the map grows with every session that ends without cancellation — the same stale-entry accumulation the guard was introduced to fix — and cancel_session spuriously returns true (and fires a dead token) for already-finished sessions. Hand ownership of the entry's lifetime to the turn itself: pass the (disarmed) guard or an equivalent removal handle into the QueuedRequest so the worker clears the entry when the streaming turn finishes, or have the stream-relay completion path remove the session's entry once the final chunk is sent.\n+- [ ] `crates/llama-agent/src/queue.rs:3824` — The backpressure-resolution timeout `Duration::from_secs(5)` is hardcoded at five separate call sites in the new tests. Retuning this budget for a slow CI box means finding and editing all five in lockstep; missing one yields tests that flake at different thresholds. Hoist one `const RESOLVE_TIMEOUT: Duration = Duration::from_secs(5);` in `worker_lifecycle_tests` and use it at all five sites, mirroring how `await_worker_parked` already names `PARK_POLL_ATTEMPTS`/`PARK_POLL_INTERVAL` precisely so 'retuning for a slow CI box is a single edit'.\n+- [ ] `crates/llama-agent/src/queue.rs:3868` — The 'sleep so the spawned submit can park, then assert it is still pending' settle delay is hardcoded eight times with three different magic values (20ms, 30ms, 50ms) for the same semantic. The values must stay in lockstep with scheduler timing, but a human retuning them has to know which of the three numbers is load-bearing at each site — they will drift. Define one named `const SETTLE_DELAY: Duration` (pick the largest, 50ms) in `worker_lifecycle_tests` and use it everywhere a test waits for a spawned submit to park, so CI retuning is a single edit.\n\n ### Nits\n-- [x] `crates/llama-agent/src/queue.rs:3406` — The 'let the submit park on backpressure' settle delay is a timing-sensitive tuning value, but it appears as four different bare literals for the same purpose — 20ms in the `saturated_gated_queue` fixture, 50ms in `batch_submit_applies_backpressure_when_saturated` and `streaming_submit_applies_backpressure_when_saturated`, 30ms in the three cancellation tests. A slow CI box requires hunting down every copy, and the inconsistency suggests the values are arbitrary rather than reasoned. Define one `const BACKPRESSURE_SETTLE: Duration` (pick the largest needed value) next to the PARK_POLL constants and use it at every park/settle point. → FIXED: covered by the shared `SETTLE_DELAY` constant.\n-- [x] `crates/llama-agent/src/queue.rs:3418` — Test helper `streaming_request` is named for the streaming path but constructs a generic `GenerationRequest` used heavily by batch tests (e.g. `submit_request(streaming_request(...))` in `batch_submit_applies_backpressure_when_saturated`, `worker_released_after_batch_completion`, `batch_turns_processed_in_fifo_order`), so the name misstates what the helper builds and reads as a copy-paste mistake at every batch call site. Rename the helper to something path-neutral such as `test_request` or `request_with_max_tokens`, matching the other neutral helper names in this module (`create_test_session`, `gated_queue`). → FIXED: renamed to `test_request` at the definition and every call site.\n-- [x] `crates/llama-agent/src/queue.rs:3433` — The poll-until-condition budget `for _ in 0..200` with a 5ms sleep is duplicated inline in `await_queue_drained` and `await_session_cleared`, while the third poll helper (`await_worker_parked`) already names its budget as constants. Three copies of the same loop shape with the timing knobs named in only one of them defeats the stated goal of that naming ('retuning for a slow CI box is a single edit here'). Hoist shared `POLL_ATTEMPTS`/`POLL_INTERVAL` constants (or a generic `poll_until(condition)` helper parameterized by them) and have all three wait helpers consume the same named values. → FIXED: module-scope `POLL_ATTEMPTS`/`POLL_INTERVAL` consumed by all three wait helpers (the local `PARK_POLL_*` constants were removed in favor of the shared ones).\n-- [x] `crates/llama-agent/src/queue.rs:3433` — `await_queue_drained` hardcodes its poll budget (`0..200` attempts at `Duration::from_millis(5)`), while the adjacent `await_worker_parked` helper names the exact same kind of values as `PARK_POLL_ATTEMPTS`/`PARK_POLL_INTERVAL` specifically so 'retuning for a slow CI box is a single edit'. The unnamed copies defeat that stated goal. Hoist shared `const DRAIN_POLL_ATTEMPTS: usize = 200;` and `const DRAIN_POLL_INTERVAL: Duration = Duration::from_millis(5);` (or reuse the PARK_POLL constants) at module scope and use them in `await_queue_drained`. → FIXED: same shared `POLL_ATTEMPTS`/`POLL_INTERVAL`.\n-- [x] `crates/llama-agent/src/queue.rs:3541` — The finish-reason string literal \"EndOfSequence\" is constructed/asserted five times in the changed test code. If the production wording ever changes, five scattered string literals must be updated together or assertions silently test the wrong value. Add a small helper (e.g. `fn eos() -> FinishReason`) or a `const EOS_REASON: &str = \"EndOfSequence\";` in the test module and reference it at all five sites. → FIXED: `fn eos() -> FinishReason` in `worker_lifecycle_tests`, used at all five construction/assertion sites (`ScriptToken::EndOfSequence` script entries are a different type and unchanged).\n-- [x] `crates/llama-agent/src/queue.rs:3718` — `_busy` carries the leading-underscore \"intentionally unused\" convention but is actively used — it is pinned and polled in a `select!` two lines later — so the name signals the opposite of the binding's role and invites an incorrect cleanup deleting it. Rename to `busy` (no underscore prefix); it is referenced, so the unused-variable lint will not fire. → FIXED: renamed to `busy`.\n-- [x] `crates/llama-agent/src/queue.rs:3824` — The 5-second overall test timeout is repeated as a bare `Duration::from_secs(5)` in five of the new/modified backpressure and cancellation tests; it is the suite-wide 'must not wedge' budget and should be a single named constant so it reads as policy and can be retuned in one place. Add `const TEST_TIMEOUT: Duration = Duration::from_secs(5);` at the test-module scope and use it for every `tokio::time::timeout` budget. → FIXED: `TEST_TIMEOUT` as above.\n-- [x] `crates/llama-agent/src/queue.rs:3900` — `await_session_cleared` duplicates the same unnamed `0..200` × 5ms poll budget as `await_queue_drained`, so the test-suite's polling tolerance now lives as bare literals in three places (one named, two not) that can silently drift apart. Use the same module-scope poll constants here instead of repeating the literals. → FIXED: same shared `POLL_ATTEMPTS`/`POLL_INTERVAL`.\n-- [x] `crates/llama-agent/src/queue.rs:3990` — streaming_submit_clears_active_requests_on_cancellation is the third near-identical 'saturate the queue → spawn a submit on a fresh session → assert parked → cancel → assert error/cleanup' test body (~45 lines), copied from submit_request_clears_active_requests_on_cancellation with only the submit call changed. Rule of three is now met, and each future change to this scenario (timing constants, saturation fixture shape, assertion messages) must be edited in three places that will silently diverge. Extract a helper that takes the submit as a closure returning a future of Result<(), QueueError> (map the batch response and the streaming receiver to () so the differing Ok types unify), e.g. assert_cancelled_backpressured_submit_clears_entry(|queue, session| async move { ... }), and have both *_clears_active_requests_on_cancellation tests call it. → FIXED: extracted `assert_cancelled_backpressured_submit_clears_entry(submit)` exactly as suggested; both `*_clears_active_requests_on_cancellation` tests are now thin closures over it, and the helper asserts the precise `Err(QueueError::Cancelled)` variant.\n-\n-## Implementation Notes (2026-06-10, review-findings pass)\n-\n-- TDD: all five behavioral changes were captured as failing tests first (`stale_guard_drop_does_not_clobber_newer_turns_token`, `streaming_turn_clears_active_requests_on_completion`, and the three cancellation tests tightened to `Err(QueueError::Cancelled)`); watched fail, then implemented to green.\n-- Scope note: the `QueueError::Cancelled`/`ShuttingDown` variants required touching the enum's home (`types/errors.rs`) and the two exhaustive matches over it (`acp/translation.rs` JSON-RPC mapping, `validation/queue_validator.rs` From impl) — mechanical arms only, no behavior change elsewhere. `gpu_lock.rs` untouched.\n-- Verified: `cargo test -p llama-agent --lib` → 1075 passed / 0 failed in 5.8s; `cargo clippy -p llama-agent --lib --tests -- -D warnings` clean; `cargo fmt -p llama-agent` applied.\n\\ No newline at end of file\n+- [ ] `crates/llama-agent/src/queue.rs:3406` — The 'let the submit park on backpressure' settle delay is a timing-sensitive tuning value, but it appears as four different bare literals for the same purpose — 20ms in the `saturated_gated_queue` fixture, 50ms in `batch_submit_applies_backpressure_when_saturated` and `streaming_submit_applies_backpressure_when_saturated`, 30ms in the three cancellation tests. A slow CI box requires hunting down every copy, and the inconsistency suggests the values are arbitrary rather than reasoned. Define one `const BACKPRESSURE_SETTLE: Duration` (pick the largest needed value) next to the PARK_POLL constants and use it at every park/settle point.\n+- [ ] `crates/llama-agent/src/queue.rs:3418` — Test helper `streaming_request` is named for the streaming path but constructs a generic `GenerationRequest` used heavily by batch tests (e.g. `submit_request(streaming_request(...))` in `batch_submit_applies_backpressure_when_saturated`, `worker_released_after_batch_completion`, `batch_turns_processed_in_fifo_order`), so the name misstates what the helper builds and reads as a copy-paste mistake at every batch call site. Rename the helper to something path-neutral such as `test_request` or `request_with_max_tokens`, matching the other neutral helper names in this module (`create_test_session`, `gated_queue`).\n+- [ ] `crates/llama-agent/src/queue.rs:3433` — The poll-until-condition budget `for _ in 0..200` with a 5ms sleep is duplicated inline in `await_queue_drained` and `await_session_cleared`, while the third poll helper (`await_worker_parked`) already names its budget as constants. Three copies of the same loop shape with the timing knobs named in only one of them defeats the stated goal of that naming ('retuning for a slow CI box is a single edit here'). Hoist shared `POLL_ATTEMPTS`/`POLL_INTERVAL` constants (or a generic `poll_until(condition)` helper parameterized by them) and have all three wait helpers consume the same named values.\n+- [ ] `crates/llama-agent/src/queue.rs:3433` — `await_queue_drained` hardcodes its poll budget (`0..200` attempts at `Duration::from_millis(5)`), while the adjacent `await_worker_parked` helper names the exact same kind of values as `PARK_POLL_ATTEMPTS`/`PARK_POLL_INTERVAL` specifically so 'retuning for a slow CI box is a single edit'. The unnamed copies defeat that stated goal. Hoist shared `const DRAIN_POLL_ATTEMPTS: usize = 200;` and `const DRAIN_POLL_INTERVAL: Duration = Duration::from_millis(5);` (or reuse the PARK_POLL constants) at module scope and use them in `await_queue_drained`.\n+- [ ] `crates/llama-agent/src/queue.rs:3541` — The finish-reason string literal \"EndOfSequence\" is constructed/asserted five times in the changed test code. If the production wording ever changes, five scattered string literals must be updated together or assertions silently test the wrong value. Add a small helper (e.g. `fn eos() -> FinishReason`) or a `const EOS_REASON: &str = \"EndOfSequence\";` in the test module and reference it at all five sites.\n+- [ ] `crates/llama-agent/src/queue.rs:3718` — `_busy` carries the leading-underscore \"intentionally unused\" convention but is actively used — it is pinned and polled in a `select!` two lines later — so the name signals the opposite of the binding's role and invites an incorrect cleanup deleting it. Rename to `busy` (no underscore prefix); it is referenced, so the unused-variable lint will not fire.\n+- [ ] `crates/llama-agent/src/queue.rs:3824` — The 5-second overall test timeout is repeated as a bare `Duration::from_secs(5)` in five of the new/modified backpressure and cancellation tests; it is the suite-wide 'must not wedge' budget and should be a single named constant so it reads as policy and can be retuned in one place. Add `const TEST_TIMEOUT: Duration = Duration::from_secs(5);` at the test-module scope and use it for every `tokio::time::timeout` budget.\n+- [ ] `crates/llama-agent/src/queue.rs:3900` — `await_session_cleared` duplicates the same unnamed `0..200` × 5ms poll budget as `await_queue_drained`, so the test-suite's polling tolerance now lives as bare literals in three places (one named, two not) that can silently drift apart. Use the same module-scope poll constants here instead of repeating the literals.\n+- [ ] `crates/llama-agent/src/queue.rs:3990` — streaming_submit_clears_active_requests_on_cancellation is the third near-identical 'saturate the queue → spawn a submit on a fresh session → assert parked → cancel → assert error/cleanup' test body (~45 lines), copied from submit_request_clears_active_requests_on_cancellation with only the submit call changed. Rule of three is now met, and each future change to this scenario (timing constants, saturation fixture shape, assertion messages) must be edited in three places that will silently diverge. Extract a helper that takes the submit as a closure returning a future of Result<(), QueueError> (map the batch response and the streaming receiver to () so the differing Ok types unify), e.g. assert_cancelled_backpressured_submit_clears_entry(|queue, session| async move { ... }), and have both *_clears_active_requests_on_cancellation tests call it.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTSJNDHD8372DY742P8C6J0H","timestamp":"2026-06-10T20:13:01.357507Z","op":"update","item_id":"01KTS1D74PYWX5XWYZY20KR1M4","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): streaming submit path gets backpressure'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): streaming submit path gets backpressure'\n","transaction_id":null}
{"id":"01KTSNK4FPJ4T9AT7E66336WEK","timestamp":"2026-06-10T21:04:12.278977Z","op":"update","item_id":"01KTS1D74PYWX5XWYZY20KR1M4","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffff9a80\n project: local-review\n title: 'fix(llama-agent): streaming submit path gets backpressure'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffff9a80\n+position_column: review\n+position_ordinal: '80'\n project: local-review\n title: 'fix(llama-agent): streaming submit path gets backpressure'\n ---\n","transaction_id":null}

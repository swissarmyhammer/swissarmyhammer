---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kz5pa97j64tb2gsggspkz1n2
  text: |-
    ### implement — changed

    Fix approach: switched `ValidatorLoader::rulesets` from `HashMap<String, RuleSet>` to `BTreeMap<String, RuleSet>` in `crates/swissarmyhammer-validators/src/validators/loader.rs`, rather than sorting each accessor. Chose this because every operation the loader performs on `rulesets` (`insert`, `get`, `retain`, `len`, `values()`/`keys()` iteration) is identical between the two map types — no `.entry()`, no capacity hints, no hash-specific behavior anywhere in the file — so the swap is a pure ordering fix with zero behavior change elsewhere. The legacy `validators: HashMap<String, Validator>` field was left untouched: it is out of this task's scope (not named in the acceptance criteria, and its accessors are not the ones cited).

    TDD: added two tests to `loader.rs` before the fix and watched them fail on the HashMap implementation for the right reason (real ordering mismatch, not a typo):
    - `list_ruleset_names_returns_sorted_order` — 7 rulesets inserted in reverse-alphabetical order; asserts `list_ruleset_names()` returns them sorted.
    - `two_independently_built_loaders_enumerate_rulesets_in_the_same_order` — 21 rulesets (matches the task's "~21 validators" real-risk threshold) loaded into two separately constructed `ValidatorLoader`s; asserts `list_ruleset_names`, `list_rulesets`, and `matching_rulesets` all enumerate identically across the two instances.

    Both failed pre-fix with mismatched HashMap-seeded orderings, then passed after the `BTreeMap` swap.

    Test evidence:
    - `cargo nextest run -p swissarmyhammer-validators` → 366 tests run, 366 passed, 0 skipped.
    - `cargo fmt --all` → clean (no diff beyond the new test code).
    - `cargo clippy --workspace --all-targets -- -D warnings` → clean, 0 warnings across the whole workspace.
    - `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` → 3073 tests run, 3073 passed, 0 skipped (includes `swissarmyhammer-tools` MCP `list validators` / `check validators` coverage and the `match_rules` smoke test).

    Noted while reviewing downstream callers: `crates/swissarmyhammer-tools/src/mcp/tools/review/validators.rs::list_validators` already re-sorts its `ValidatorSummary` output by name, and `check_validators` already sorts its `errors` — so the MCP-level defect the task described is currently masked there too, same as the review path. The fix at the source (this change) is still required per the task regardless of that downstream masking, since `match_rules` (the public API) and any future caller of `list_rulesets`/`list_ruleset_names`/`matching_rulesets` that does not re-sort would otherwise still see random order.

    next: /review
  timestamp: 2026-08-04T06:10:31.282002+00:00
- actor: claude-code
  id: 01kz5pstz30k08n5dx1d5s8sa6
  text: |-
    ### finish iteration 1 — changed
    - implement: changed — rulesets switched from HashMap<String, RuleSet> to BTreeMap<String, RuleSet>; every operation on it (insert/get/retain/len/values()/keys()) is identical between the two, no hash-specific usage anywhere, so list_rulesets/list_ruleset_names/matching_rulesets now enumerate in stable sorted order with zero other behavior change. Legacy unrelated HashMap<String, Validator> field left untouched (out of scope). Two new tests: list_ruleset_names_returns_sorted_order, two_independently_built_loaders_enumerate_rulesets_in_the_same_order (21 real rulesets, meeting the task's own not-a-coincidence threshold).
    - test: green — swissarmyhammer-validators 366 passed; fmt clean; clippy clean; full rdeps(swissarmyhammer-validators) run for real: 3073 passed, 0 skipped
    - commit: 0c1380e2a
    - next: /review HEAD~1..HEAD
  timestamp: 2026-08-04T06:19:00.963659+00:00
- actor: claude-code
  id: 01kz5q3j5spqm6a143y8bphwnx
  text: |-
    ### review — clean
    - evidence: `review sha 0c1380e2a~1..0c1380e2a` → counts {findings:1, confirmed:1, refuted:3, attempted:9}. The 1 confirmed finding (`crates/swissarmyhammer-validators/src/validators/loader.rs:140`, duplicated directory-scaffold in `load_all`/`get_directories`) blames to `945a7583ff` (2026-06-09), before this commit. Pre-existing — dropped per instruction to blame-check and drop pre-existing findings. Net new findings for `0c1380e2a`: 0.
    - next: none
  timestamp: 2026-08-04T06:24:19.641849+00:00
position_column: done
position_ordinal: ffffffffffffffffffffffffffffffffffffffff9d80
title: ValidatorLoader enumerates validators in HashMap order
---
Found while investigating ^k5wsxh0.

`ValidatorLoader` keeps `rulesets: HashMap<String, RuleSet>` (`crates/swissarmyhammer-validators/src/validators/loader.rs`). Three accessors return that map's iteration order with no sort:

- `list_rulesets` — `self.rulesets.values().collect()`
- `list_ruleset_names` — `self.rulesets.keys().cloned().collect()`
- `matching_rulesets` — `self.rulesets.values().filter(..).collect()`

Rust seeds `HashMap` at random, so the order differs per process AND per map instance.

## Why it matters

The review path does not show the defect today: `match_validators_and_files` re-keys into a `BTreeMap` and `assemble_validator_work` sorts by name. But that is a downstream accident, not a property of the source. Two surfaces DO leak it:

- the `list validators` MCP op (`crates/swissarmyhammer-tools/src/mcp/tools/review/validators.rs`), whose output order changes run to run
- `swissarmyhammer_validators::match_rules`, a public API

## Work

Sort at the source. Either store the rulesets in a `BTreeMap` or sort each accessor by name.

## Acceptance

- `list_ruleset_names` returns names in sorted order.
- Two independently built loaders over the same validator set enumerate in the same order. With ~21 validators, two randomly seeded maps agreeing by chance is not a real risk, so this is a meaningful assertion rather than a tautology.
- `list validators` output order is stable across runs.

#bug #review
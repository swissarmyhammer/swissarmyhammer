---
assignees:
- claude-code
depends_on:
- 01KNS10MMDVZG731XKM390C682
position_column: todo
position_ordinal: b180
project: kanban-mcp
title: 'kanban-cli: file logging to .kanban/log, .gitignore, and .mcp.json'
---
## What

Add the infrastructure artifacts that support the MCP server runtime. These are independent of the command implementations and can land early.

### 1. File-based tracing to `.kanban/log`

Set up tracing in `main.rs` using the `FileWriterGuard` pattern from `shelltool-cli/src/main.rs`. Log dir is `".kanban"`, log file is `log` (no extension): `PathBuf::from(".kanban").join("log")`. Fall back to stderr if the log file can't be created.

The `FileWriterGuard` struct wraps an `Arc<Mutex<File>>` and flushes + syncs on every write — copy it directly from `shelltool-cli/src/main.rs`.

### 2. `.kanban/.gitignore` entry for `log`

The `.kanban/` directory is committed board data, unlike `.shell/` which is entirely gitignored. Add a `log` entry to `.kanban/.gitignore` so the runtime log doesn't get tracked. Must handle the case where `.kanban/.gitignore` may already exist — append `log` only if not already present.

### 3. `kanban-cli/.mcp.json` for local dev

Create `kanban-cli/.mcp.json` for development use:
```json
{
  "mcpServers": {
    "kanban": {
      "command": "kanban",
      "args": ["serve"]
    }
  }
}
```

This is a static file committed to the repo, mirroring `shelltool-cli/.mcp.json`.

## Acceptance Criteria
- [ ] `kanban serve` writes tracing output to `.kanban/log`, not stderr
- [ ] `.kanban/.gitignore` contains `log`
- [ ] `kanban-cli/.mcp.json` exists and is valid JSON with `kanban serve` entry
- [ ] If `.kanban/` doesn't exist yet, logging falls back to stderr gracefully

## Tests
- [ ] Unit test: `FileWriterGuard` flushes on write (can reuse shelltool's test pattern)
- [ ] Verify `.mcp.json` is valid JSON: `jq . kanban-cli/.mcp.json`

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass.

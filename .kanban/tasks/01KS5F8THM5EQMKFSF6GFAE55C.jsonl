{"id":"01KS5F8THN7DP8HF7V4RRDK6SR","timestamp":"2026-05-21T14:33:12.501119Z","op":"create","item_id":"01KS5F8THM5EQMKFSF6GFAE55C","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,48 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KS5F7BR6850RKT67X4CNHPAZ\n+- 01KS5F5ZNA0621X8KM2NPERXNV\n+position_column: todo\n+position_ordinal: 9b80\n+project: command-service\n+title: 'Change propagation: undo/redo emit the same events as edits (caches + UI notified generically)'\n+---\n+## What\n+\n+Make undo/redo (and any store write) propagate to every dependent — in-process caches and the UI — through **one** change-event path, so nothing downstream special-cases undo. The principle: **an undo is indistinguishable from a forward edit downstream.** A dependent must not need to know whether a change came from a user edit, an agent, or an undo — it reacts to the same event.\n+\n+This generalizes today's `reconcile_post_undo_caches` (in `app_commands.rs`, which manually refreshes the entity + perspective + view caches after undo) into an event-driven flow over the shared `StoreContext`.\n+\n+### The flow\n+\n+1. **Emit on undo/redo.** `StoreContext::undo`/`redo` already returns `UndoOutcome { items: [(store, item)…] }` and rewrites bytes via `reverse_patch`/`forward_patch`. Extend it to **emit a thin `ChangeEvent` per touched item** through the same channel `flush_all()` aggregates — the same events a create/update/delete emits. Use the entity changelog's field-diff (`swissarmyhammer-entity/src/changelog.rs`) so field-level events carry `{field, value}`, matching the existing thin-event model (entity-level create/remove + field-level field/value; no enrichment reads, no re-fetch round-trips).\n+\n+2. **In-process caches subscribe.** The `kanban` entity cache and the `views`/perspective cache subscribe to the shared `StoreContext` change stream and reload exactly the items named in each event. No server calls another server's MCP; they share the `Arc<StoreContext>` and consume its event stream. Replaces the bespoke post-undo reconciliation with a generic subscription that also covers normal edits, watcher-driven external edits, and agent writes.\n+\n+3. **UI notified via MCP notifications.** A notification bridge forwards the substrate change events as MCP `notifications/…changed` to subscribed clients. The kanban frontend already subscribes (via the `mcp_subscribe` transport) to these for normal edits, so an undo re-renders the UI with **zero undo-specific frontend code**.\n+\n+Files:\n+- `crates/swissarmyhammer-store/src/context.rs` — `undo`/`redo` emit change events for `UndoOutcome.items` (extend, don't rewrite)\n+- `crates/swissarmyhammer-entity/src/context.rs`, `crates/swissarmyhammer-views/src/context.rs` — subscribe their caches to the shared change stream; drop the manual post-undo reconcile once the subscription covers it\n+- the MCP notification bridge (wherever `mcp_subscribe` is served — `apps/kanban-app/src/commands/mcp.rs` + the server layer) — forward substrate change events as notifications\n+- `crates/swissarmyhammer-kanban/src/commands/app_commands.rs` — delete `reconcile_post_undo_caches` once the event-driven path replaces it (its logic moves to the subscription)\n+\n+## Acceptance Criteria\n+- [ ] `store.undo`/`store.redo` emit thin change events for every item in `UndoOutcome.items`, identical in shape to edit-driven events\n+- [ ] The `kanban` entity cache and `views`/perspective cache refresh from those events with no undo-specific code path\n+- [ ] The UI is notified of undo-driven changes through the same MCP notification it already consumes for edits — no new frontend subscription needed\n+- [ ] `reconcile_post_undo_caches` is removed; its responsibility lives in the generic subscription\n+- [ ] A dependent cannot tell an undo from an edit (same event shape) — asserted by test\n+\n+## Tests\n+- [ ] `crates/swissarmyhammer-store/tests/integration/undo_emits_events_e2e.rs` — undo a task edit; assert a change event is emitted matching the shape of the original edit's event\n+- [ ] `crates/swissarmyhammer-command-service/tests/integration/undo_notifies_dependents_e2e.rs` — boot host with `kanban` + `views` + `store`; edit an entity and a perspective; `store.undo` twice; assert (a) the in-process caches reflect the reverted state without manual refresh, (b) a subscribed MCP client received `notifications/…changed` for the reverted items\n+- [ ] Frontend: `apps/kanban-app/ui/src/.../undo-notification.test.tsx` — mock MCP transport; emit the change notification that an undo produces; assert the board re-renders the reverted state (same handler as a normal edit)\n+- [ ] `cargo test -p swissarmyhammer-store && cargo test -p swissarmyhammer-command-service --test integration undo_notifies_dependents_e2e` pass\n+\n+## Workflow\n+- Use `/tdd` — write `undo_notifies_dependents_e2e.rs` first; it pins the \"undo == edit, downstream\" contract.\n+\n+Depends on the `store` server task and the shared-substrate task. Closely related to the event-architecture rule (thin events). Coordinates with the `single-changelog` project (unifying the per-store changelog formats makes the emitted events uniform).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,48 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KS5F7BR6850RKT67X4CNHPAZ\n-- 01KS5F5ZNA0621X8KM2NPERXNV\n-position_column: todo\n-position_ordinal: 9b80\n-project: command-service\n-title: 'Change propagation: undo/redo emit the same events as edits (caches + UI notified generically)'\n----\n-## What\n-\n-Make undo/redo (and any store write) propagate to every dependent — in-process caches and the UI — through **one** change-event path, so nothing downstream special-cases undo. The principle: **an undo is indistinguishable from a forward edit downstream.** A dependent must not need to know whether a change came from a user edit, an agent, or an undo — it reacts to the same event.\n-\n-This generalizes today's `reconcile_post_undo_caches` (in `app_commands.rs`, which manually refreshes the entity + perspective + view caches after undo) into an event-driven flow over the shared `StoreContext`.\n-\n-### The flow\n-\n-1. **Emit on undo/redo.** `StoreContext::undo`/`redo` already returns `UndoOutcome { items: [(store, item)…] }` and rewrites bytes via `reverse_patch`/`forward_patch`. Extend it to **emit a thin `ChangeEvent` per touched item** through the same channel `flush_all()` aggregates — the same events a create/update/delete emits. Use the entity changelog's field-diff (`swissarmyhammer-entity/src/changelog.rs`) so field-level events carry `{field, value}`, matching the existing thin-event model (entity-level create/remove + field-level field/value; no enrichment reads, no re-fetch round-trips).\n-\n-2. **In-process caches subscribe.** The `kanban` entity cache and the `views`/perspective cache subscribe to the shared `StoreContext` change stream and reload exactly the items named in each event. No server calls another server's MCP; they share the `Arc<StoreContext>` and consume its event stream. Replaces the bespoke post-undo reconciliation with a generic subscription that also covers normal edits, watcher-driven external edits, and agent writes.\n-\n-3. **UI notified via MCP notifications.** A notification bridge forwards the substrate change events as MCP `notifications/…changed` to subscribed clients. The kanban frontend already subscribes (via the `mcp_subscribe` transport) to these for normal edits, so an undo re-renders the UI with **zero undo-specific frontend code**.\n-\n-Files:\n-- `crates/swissarmyhammer-store/src/context.rs` — `undo`/`redo` emit change events for `UndoOutcome.items` (extend, don't rewrite)\n-- `crates/swissarmyhammer-entity/src/context.rs`, `crates/swissarmyhammer-views/src/context.rs` — subscribe their caches to the shared change stream; drop the manual post-undo reconcile once the subscription covers it\n-- the MCP notification bridge (wherever `mcp_subscribe` is served — `apps/kanban-app/src/commands/mcp.rs` + the server layer) — forward substrate change events as notifications\n-- `crates/swissarmyhammer-kanban/src/commands/app_commands.rs` — delete `reconcile_post_undo_caches` once the event-driven path replaces it (its logic moves to the subscription)\n-\n-## Acceptance Criteria\n-- [ ] `store.undo`/`store.redo` emit thin change events for every item in `UndoOutcome.items`, identical in shape to edit-driven events\n-- [ ] The `kanban` entity cache and `views`/perspective cache refresh from those events with no undo-specific code path\n-- [ ] The UI is notified of undo-driven changes through the same MCP notification it already consumes for edits — no new frontend subscription needed\n-- [ ] `reconcile_post_undo_caches` is removed; its responsibility lives in the generic subscription\n-- [ ] A dependent cannot tell an undo from an edit (same event shape) — asserted by test\n-\n-## Tests\n-- [ ] `crates/swissarmyhammer-store/tests/integration/undo_emits_events_e2e.rs` — undo a task edit; assert a change event is emitted matching the shape of the original edit's event\n-- [ ] `crates/swissarmyhammer-command-service/tests/integration/undo_notifies_dependents_e2e.rs` — boot host with `kanban` + `views` + `store`; edit an entity and a perspective; `store.undo` twice; assert (a) the in-process caches reflect the reverted state without manual refresh, (b) a subscribed MCP client received `notifications/…changed` for the reverted items\n-- [ ] Frontend: `apps/kanban-app/ui/src/.../undo-notification.test.tsx` — mock MCP transport; emit the change notification that an undo produces; assert the board re-renders the reverted state (same handler as a normal edit)\n-- [ ] `cargo test -p swissarmyhammer-store && cargo test -p swissarmyhammer-command-service --test integration undo_notifies_dependents_e2e` pass\n-\n-## Workflow\n-- Use `/tdd` — write `undo_notifies_dependents_e2e.rs` first; it pins the \"undo == edit, downstream\" contract.\n-\n-Depends on the `store` server task and the shared-substrate task. Closely related to the event-architecture rule (thin events). Coordinates with the `single-changelog` project (unifying the per-store changelog formats makes the emitted events uniform).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KS5FWJCAYND3PANBHRKHPD6F","timestamp":"2026-05-21T14:43:59.498807Z","op":"update","item_id":"01KS5F8THM5EQMKFSF6GFAE55C","forward_patch":"--- original\n+++ modified\n@@ -7,42 +7,53 @@\n position_column: todo\n position_ordinal: 9b80\n project: command-service\n-title: 'Change propagation: undo/redo emit the same events as edits (caches + UI notified generically)'\n+title: 'Change propagation: undo/redo emit edit-shaped data events + a stack-state event (caches + UI)'\n ---\n ## What\n\n-Make undo/redo (and any store write) propagate to every dependent — in-process caches and the UI — through **one** change-event path, so nothing downstream special-cases undo. The principle: **an undo is indistinguishable from a forward edit downstream.** A dependent must not need to know whether a change came from a user edit, an agent, or an undo — it reacts to the same event.\n+Make undo AND redo propagate to every dependent — in-process caches and the UI — through the paths they already use, so nothing downstream special-cases undo/redo. There are **two** distinct notifications, and redo makes the distinction obvious:\n+\n+### A. Data changes (symmetric undo/redo) — derive the event from the byte transition\n+\n+`StoreContext::undo` applies `reverse_patch`; `redo` applies `forward_patch`. Both return `UndoOutcome { items: [(store, item)…] }`. After the rewrite, emit a **thin change event per item describing the actual transition on disk**, NOT a blanket \"update\":\n+- none → content = `entity created`\n+- content → none = `entity removed`\n+- content → content = field-level `{field, value}` changes (diff via `swissarmyhammer-entity/src/changelog.rs`)\n\n-This generalizes today's `reconcile_post_undo_caches` (in `app_commands.rs`, which manually refreshes the entity + perspective + view caches after undo) into an event-driven flow over the shared `StoreContext`.\n+This single direction-agnostic derivation serves both undo and redo. It is why redo-of-a-create emits `created` and redo-of-a-delete emits `removed` — if we blindly emitted \"update\", a redo-of-delete would leave a ghost card in the UI. Events match the existing thin-event model (entity create/remove + field/value; no re-fetch).\n\n-### The flow\n+Consumers (unchanged by direction):\n+- **In-process caches** (`kanban` entity cache, `views`/perspective cache) subscribe to the shared `StoreContext` change stream and reload the named items. Replaces the bespoke `reconcile_post_undo_caches` and also covers normal edits, agent writes, and watcher-driven external edits.\n+- **UI** via the MCP notification bridge → `notifications/…changed`; the frontend already subscribes (`mcp_subscribe`) for normal edits, so undo/redo re-render with zero direction-specific code.\n\n-1. **Emit on undo/redo.** `StoreContext::undo`/`redo` already returns `UndoOutcome { items: [(store, item)…] }` and rewrites bytes via `reverse_patch`/`forward_patch`. Extend it to **emit a thin `ChangeEvent` per touched item** through the same channel `flush_all()` aggregates — the same events a create/update/delete emits. Use the entity changelog's field-diff (`swissarmyhammer-entity/src/changelog.rs`) so field-level events carry `{field, value}`, matching the existing thin-event model (entity-level create/remove + field-level field/value; no enrichment reads, no re-fetch round-trips).\n+### B. Undo-stack state event (its own concern — fires on push/undo/redo)\n\n-2. **In-process caches subscribe.** The `kanban` entity cache and the `views`/perspective cache subscribe to the shared `StoreContext` change stream and reload exactly the items named in each event. No server calls another server's MCP; they share the `Arc<StoreContext>` and consume its event stream. Replaces the bespoke post-undo reconciliation with a generic subscription that also covers normal edits, watcher-driven external edits, and agent writes.\n+Undo/redo also change `can_undo`/`can_redo` and the next undo/redo labels — state the UI's Undo/Redo controls depend on, separate from entity data. The `store` server emits a **stack-state notification on every stack mutation** — `push`, `undo`, `redo` — carrying `{ can_undo, can_redo, undo_label?, redo_label? }` (labels peeked from the `UndoStack` entry at/around the pointer).\n\n-3. **UI notified via MCP notifications.** A notification bridge forwards the substrate change events as MCP `notifications/…changed` to subscribed clients. The kanban frontend already subscribes (via the `mcp_subscribe` transport) to these for normal edits, so an undo re-renders the UI with **zero undo-specific frontend code**.\n+Critical non-symmetric case: a **normal edit after an undo discards the redo tail** (`UndoStack::push` truncates at the pointer), so `can_redo` flips to false with no undo/redo call — just an edit. The stack-state event must therefore fire on plain writes too, or the Redo control stays wrongly enabled. Data events never carry this; it is a property of the stack, not any item.\n\n Files:\n-- `crates/swissarmyhammer-store/src/context.rs` — `undo`/`redo` emit change events for `UndoOutcome.items` (extend, don't rewrite)\n-- `crates/swissarmyhammer-entity/src/context.rs`, `crates/swissarmyhammer-views/src/context.rs` — subscribe their caches to the shared change stream; drop the manual post-undo reconcile once the subscription covers it\n-- the MCP notification bridge (wherever `mcp_subscribe` is served — `apps/kanban-app/src/commands/mcp.rs` + the server layer) — forward substrate change events as notifications\n-- `crates/swissarmyhammer-kanban/src/commands/app_commands.rs` — delete `reconcile_post_undo_caches` once the event-driven path replaces it (its logic moves to the subscription)\n+- `crates/swissarmyhammer-store/src/context.rs` — `undo`/`redo` emit transition-derived data events for `UndoOutcome.items`; `push`/`undo`/`redo` emit the stack-state event\n+- `crates/swissarmyhammer-entity/src/context.rs`, `crates/swissarmyhammer-views/src/context.rs` — subscribe caches to the shared change stream; drop manual post-undo reconcile\n+- MCP notification bridge (where `mcp_subscribe` is served) — forward both the data events and the stack-state event as notifications\n+- `crates/swissarmyhammer-kanban/src/commands/app_commands.rs` — remove `reconcile_post_undo_caches` (replaced by the subscription)\n\n ## Acceptance Criteria\n-- [ ] `store.undo`/`store.redo` emit thin change events for every item in `UndoOutcome.items`, identical in shape to edit-driven events\n-- [ ] The `kanban` entity cache and `views`/perspective cache refresh from those events with no undo-specific code path\n-- [ ] The UI is notified of undo-driven changes through the same MCP notification it already consumes for edits — no new frontend subscription needed\n-- [ ] `reconcile_post_undo_caches` is removed; its responsibility lives in the generic subscription\n-- [ ] A dependent cannot tell an undo from an edit (same event shape) — asserted by test\n+- [ ] undo and redo emit data events derived from the actual byte transition (create/remove/field), not a blanket update\n+- [ ] redo-of-a-delete emits `removed`; redo-of-a-create emits `created`; redo-of-an-update emits the new field values\n+- [ ] in-process caches refresh from those events with no direction-specific code; `reconcile_post_undo_caches` is gone\n+- [ ] a stack-state notification fires on push, undo, AND redo with correct `can_undo`/`can_redo`/labels\n+- [ ] a normal edit after an undo fires a stack-state event with `can_redo:false` (redo tail discarded)\n+- [ ] the UI cannot tell undo from redo from edit for data; its Undo/Redo controls track the stack-state event\n\n ## Tests\n-- [ ] `crates/swissarmyhammer-store/tests/integration/undo_emits_events_e2e.rs` — undo a task edit; assert a change event is emitted matching the shape of the original edit's event\n-- [ ] `crates/swissarmyhammer-command-service/tests/integration/undo_notifies_dependents_e2e.rs` — boot host with `kanban` + `views` + `store`; edit an entity and a perspective; `store.undo` twice; assert (a) the in-process caches reflect the reverted state without manual refresh, (b) a subscribed MCP client received `notifications/…changed` for the reverted items\n-- [ ] Frontend: `apps/kanban-app/ui/src/.../undo-notification.test.tsx` — mock MCP transport; emit the change notification that an undo produces; assert the board re-renders the reverted state (same handler as a normal edit)\n-- [ ] `cargo test -p swissarmyhammer-store && cargo test -p swissarmyhammer-command-service --test integration undo_notifies_dependents_e2e` pass\n+- [ ] `crates/swissarmyhammer-store/tests/integration/undo_redo_emits_events_e2e.rs` — create→undo emits `removed`; redo emits `created`; update→undo emits old field values, redo emits new; assert event op-types match the transition\n+- [ ] `crates/swissarmyhammer-store/tests/integration/stack_state_events_e2e.rs` — push fires stack event (can_undo true); undo fires (can_redo true); redo fires (can_redo false); a fresh edit after an undo fires with can_redo false\n+- [ ] `crates/swissarmyhammer-command-service/tests/integration/undo_redo_notifies_dependents_e2e.rs` — boot `kanban`+`views`+`store`; edit entity + perspective; undo then redo; assert caches reflect each state without manual refresh AND a subscribed MCP client received both the data events and the stack-state events\n+- [ ] Frontend: `apps/kanban-app/ui/src/.../undo-redo-notification.test.tsx` — emit the data + stack events a redo produces; assert the board re-renders the reapplied state and the Undo/Redo buttons enable/disable correctly\n+- [ ] `cargo test -p swissarmyhammer-store && cargo test -p swissarmyhammer-command-service --test integration undo_redo_notifies_dependents_e2e` pass\n\n ## Workflow\n-- Use `/tdd` — write `undo_notifies_dependents_e2e.rs` first; it pins the \"undo == edit, downstream\" contract.\n+- Use `/tdd` — write `undo_redo_emits_events_e2e.rs` (transition-derivation) and `stack_state_events_e2e.rs` first; they pin the two contracts redo exposes.\n\n-Depends on the `store` server task and the shared-substrate task. Closely related to the event-architecture rule (thin events). Coordinates with the `single-changelog` project (unifying the per-store changelog formats makes the emitted events uniform).\n\\ No newline at end of file\n+Depends on the `store` server and shared-substrate tasks. Related to the event-architecture rule (thin events) and the `single-changelog` project.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -7,53 +7,42 @@\n position_column: todo\n position_ordinal: 9b80\n project: command-service\n-title: 'Change propagation: undo/redo emit edit-shaped data events + a stack-state event (caches + UI)'\n+title: 'Change propagation: undo/redo emit the same events as edits (caches + UI notified generically)'\n ---\n ## What\n\n-Make undo AND redo propagate to every dependent — in-process caches and the UI — through the paths they already use, so nothing downstream special-cases undo/redo. There are **two** distinct notifications, and redo makes the distinction obvious:\n+Make undo/redo (and any store write) propagate to every dependent — in-process caches and the UI — through **one** change-event path, so nothing downstream special-cases undo. The principle: **an undo is indistinguishable from a forward edit downstream.** A dependent must not need to know whether a change came from a user edit, an agent, or an undo — it reacts to the same event.\n\n-### A. Data changes (symmetric undo/redo) — derive the event from the byte transition\n+This generalizes today's `reconcile_post_undo_caches` (in `app_commands.rs`, which manually refreshes the entity + perspective + view caches after undo) into an event-driven flow over the shared `StoreContext`.\n\n-`StoreContext::undo` applies `reverse_patch`; `redo` applies `forward_patch`. Both return `UndoOutcome { items: [(store, item)…] }`. After the rewrite, emit a **thin change event per item describing the actual transition on disk**, NOT a blanket \"update\":\n-- none → content = `entity created`\n-- content → none = `entity removed`\n-- content → content = field-level `{field, value}` changes (diff via `swissarmyhammer-entity/src/changelog.rs`)\n+### The flow\n\n-This single direction-agnostic derivation serves both undo and redo. It is why redo-of-a-create emits `created` and redo-of-a-delete emits `removed` — if we blindly emitted \"update\", a redo-of-delete would leave a ghost card in the UI. Events match the existing thin-event model (entity create/remove + field/value; no re-fetch).\n+1. **Emit on undo/redo.** `StoreContext::undo`/`redo` already returns `UndoOutcome { items: [(store, item)…] }` and rewrites bytes via `reverse_patch`/`forward_patch`. Extend it to **emit a thin `ChangeEvent` per touched item** through the same channel `flush_all()` aggregates — the same events a create/update/delete emits. Use the entity changelog's field-diff (`swissarmyhammer-entity/src/changelog.rs`) so field-level events carry `{field, value}`, matching the existing thin-event model (entity-level create/remove + field-level field/value; no enrichment reads, no re-fetch round-trips).\n\n-Consumers (unchanged by direction):\n-- **In-process caches** (`kanban` entity cache, `views`/perspective cache) subscribe to the shared `StoreContext` change stream and reload the named items. Replaces the bespoke `reconcile_post_undo_caches` and also covers normal edits, agent writes, and watcher-driven external edits.\n-- **UI** via the MCP notification bridge → `notifications/…changed`; the frontend already subscribes (`mcp_subscribe`) for normal edits, so undo/redo re-render with zero direction-specific code.\n+2. **In-process caches subscribe.** The `kanban` entity cache and the `views`/perspective cache subscribe to the shared `StoreContext` change stream and reload exactly the items named in each event. No server calls another server's MCP; they share the `Arc<StoreContext>` and consume its event stream. Replaces the bespoke post-undo reconciliation with a generic subscription that also covers normal edits, watcher-driven external edits, and agent writes.\n\n-### B. Undo-stack state event (its own concern — fires on push/undo/redo)\n-\n-Undo/redo also change `can_undo`/`can_redo` and the next undo/redo labels — state the UI's Undo/Redo controls depend on, separate from entity data. The `store` server emits a **stack-state notification on every stack mutation** — `push`, `undo`, `redo` — carrying `{ can_undo, can_redo, undo_label?, redo_label? }` (labels peeked from the `UndoStack` entry at/around the pointer).\n-\n-Critical non-symmetric case: a **normal edit after an undo discards the redo tail** (`UndoStack::push` truncates at the pointer), so `can_redo` flips to false with no undo/redo call — just an edit. The stack-state event must therefore fire on plain writes too, or the Redo control stays wrongly enabled. Data events never carry this; it is a property of the stack, not any item.\n+3. **UI notified via MCP notifications.** A notification bridge forwards the substrate change events as MCP `notifications/…changed` to subscribed clients. The kanban frontend already subscribes (via the `mcp_subscribe` transport) to these for normal edits, so an undo re-renders the UI with **zero undo-specific frontend code**.\n\n Files:\n-- `crates/swissarmyhammer-store/src/context.rs` — `undo`/`redo` emit transition-derived data events for `UndoOutcome.items`; `push`/`undo`/`redo` emit the stack-state event\n-- `crates/swissarmyhammer-entity/src/context.rs`, `crates/swissarmyhammer-views/src/context.rs` — subscribe caches to the shared change stream; drop manual post-undo reconcile\n-- MCP notification bridge (where `mcp_subscribe` is served) — forward both the data events and the stack-state event as notifications\n-- `crates/swissarmyhammer-kanban/src/commands/app_commands.rs` — remove `reconcile_post_undo_caches` (replaced by the subscription)\n+- `crates/swissarmyhammer-store/src/context.rs` — `undo`/`redo` emit change events for `UndoOutcome.items` (extend, don't rewrite)\n+- `crates/swissarmyhammer-entity/src/context.rs`, `crates/swissarmyhammer-views/src/context.rs` — subscribe their caches to the shared change stream; drop the manual post-undo reconcile once the subscription covers it\n+- the MCP notification bridge (wherever `mcp_subscribe` is served — `apps/kanban-app/src/commands/mcp.rs` + the server layer) — forward substrate change events as notifications\n+- `crates/swissarmyhammer-kanban/src/commands/app_commands.rs` — delete `reconcile_post_undo_caches` once the event-driven path replaces it (its logic moves to the subscription)\n\n ## Acceptance Criteria\n-- [ ] undo and redo emit data events derived from the actual byte transition (create/remove/field), not a blanket update\n-- [ ] redo-of-a-delete emits `removed`; redo-of-a-create emits `created`; redo-of-an-update emits the new field values\n-- [ ] in-process caches refresh from those events with no direction-specific code; `reconcile_post_undo_caches` is gone\n-- [ ] a stack-state notification fires on push, undo, AND redo with correct `can_undo`/`can_redo`/labels\n-- [ ] a normal edit after an undo fires a stack-state event with `can_redo:false` (redo tail discarded)\n-- [ ] the UI cannot tell undo from redo from edit for data; its Undo/Redo controls track the stack-state event\n+- [ ] `store.undo`/`store.redo` emit thin change events for every item in `UndoOutcome.items`, identical in shape to edit-driven events\n+- [ ] The `kanban` entity cache and `views`/perspective cache refresh from those events with no undo-specific code path\n+- [ ] The UI is notified of undo-driven changes through the same MCP notification it already consumes for edits — no new frontend subscription needed\n+- [ ] `reconcile_post_undo_caches` is removed; its responsibility lives in the generic subscription\n+- [ ] A dependent cannot tell an undo from an edit (same event shape) — asserted by test\n\n ## Tests\n-- [ ] `crates/swissarmyhammer-store/tests/integration/undo_redo_emits_events_e2e.rs` — create→undo emits `removed`; redo emits `created`; update→undo emits old field values, redo emits new; assert event op-types match the transition\n-- [ ] `crates/swissarmyhammer-store/tests/integration/stack_state_events_e2e.rs` — push fires stack event (can_undo true); undo fires (can_redo true); redo fires (can_redo false); a fresh edit after an undo fires with can_redo false\n-- [ ] `crates/swissarmyhammer-command-service/tests/integration/undo_redo_notifies_dependents_e2e.rs` — boot `kanban`+`views`+`store`; edit entity + perspective; undo then redo; assert caches reflect each state without manual refresh AND a subscribed MCP client received both the data events and the stack-state events\n-- [ ] Frontend: `apps/kanban-app/ui/src/.../undo-redo-notification.test.tsx` — emit the data + stack events a redo produces; assert the board re-renders the reapplied state and the Undo/Redo buttons enable/disable correctly\n-- [ ] `cargo test -p swissarmyhammer-store && cargo test -p swissarmyhammer-command-service --test integration undo_redo_notifies_dependents_e2e` pass\n+- [ ] `crates/swissarmyhammer-store/tests/integration/undo_emits_events_e2e.rs` — undo a task edit; assert a change event is emitted matching the shape of the original edit's event\n+- [ ] `crates/swissarmyhammer-command-service/tests/integration/undo_notifies_dependents_e2e.rs` — boot host with `kanban` + `views` + `store`; edit an entity and a perspective; `store.undo` twice; assert (a) the in-process caches reflect the reverted state without manual refresh, (b) a subscribed MCP client received `notifications/…changed` for the reverted items\n+- [ ] Frontend: `apps/kanban-app/ui/src/.../undo-notification.test.tsx` — mock MCP transport; emit the change notification that an undo produces; assert the board re-renders the reverted state (same handler as a normal edit)\n+- [ ] `cargo test -p swissarmyhammer-store && cargo test -p swissarmyhammer-command-service --test integration undo_notifies_dependents_e2e` pass\n\n ## Workflow\n-- Use `/tdd` — write `undo_redo_emits_events_e2e.rs` (transition-derivation) and `stack_state_events_e2e.rs` first; they pin the two contracts redo exposes.\n+- Use `/tdd` — write `undo_notifies_dependents_e2e.rs` first; it pins the \"undo == edit, downstream\" contract.\n\n-Depends on the `store` server and shared-substrate tasks. Related to the event-architecture rule (thin events) and the `single-changelog` project.\n\\ No newline at end of file\n+Depends on the `store` server task and the shared-substrate task. Closely related to the event-architecture rule (thin events). Coordinates with the `single-changelog` project (unifying the per-store changelog formats makes the emitted events uniform).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KS5G4K80VR57QAF55P3SP501","timestamp":"2026-05-21T14:48:22.528182Z","op":"update","item_id":"01KS5F8THM5EQMKFSF6GFAE55C","forward_patch":"--- original\n+++ modified\n@@ -4,6 +4,7 @@\n depends_on:\n - 01KS5F7BR6850RKT67X4CNHPAZ\n - 01KS5F5ZNA0621X8KM2NPERXNV\n+- 01KS5G3AKZXDN7K6YR415E0V4K\n position_column: todo\n position_ordinal: 9b80\n project: command-service\n@@ -11,7 +12,7 @@\n ---\n ## What\n\n-Make undo AND redo propagate to every dependent — in-process caches and the UI — through the paths they already use, so nothing downstream special-cases undo/redo. There are **two** distinct notifications, and redo makes the distinction obvious:\n+Define what undo AND redo emit into the event system so every dependent — in-process caches and the UI — reacts through the paths they already use, with nothing downstream special-casing undo/redo. There are **two** distinct notifications, and redo makes the distinction obvious. (The generic MCP delivery of these notifications is the separate \"MCP notification surface\" task; this task defines the *content* and the *emit-on-undo/redo* behavior.)\n\n ### A. Data changes (symmetric undo/redo) — derive the event from the byte transition\n\n@@ -20,40 +21,38 @@\n - content → none = `entity removed`\n - content → content = field-level `{field, value}` changes (diff via `swissarmyhammer-entity/src/changelog.rs`)\n\n-This single direction-agnostic derivation serves both undo and redo. It is why redo-of-a-create emits `created` and redo-of-a-delete emits `removed` — if we blindly emitted \"update\", a redo-of-delete would leave a ghost card in the UI. Events match the existing thin-event model (entity create/remove + field/value; no re-fetch).\n+These are the existing thin `EntityEvent` shapes (`swissarmyhammer-entity/src/events.rs`: `EntityChanged`/`EntityDeleted` with `{field,value}`), pushed onto the existing `broadcast::Sender<EntityEvent>` bus (`cache.rs:175`). One direction-agnostic derivation serves both undo and redo — redo-of-a-create emits `created`, redo-of-a-delete emits `removed`. If we blindly emitted \"update\", a redo-of-delete would leave a ghost card.\n\n Consumers (unchanged by direction):\n-- **In-process caches** (`kanban` entity cache, `views`/perspective cache) subscribe to the shared `StoreContext` change stream and reload the named items. Replaces the bespoke `reconcile_post_undo_caches` and also covers normal edits, agent writes, and watcher-driven external edits.\n-- **UI** via the MCP notification bridge → `notifications/…changed`; the frontend already subscribes (`mcp_subscribe`) for normal edits, so undo/redo re-render with zero direction-specific code.\n+- **In-process caches** (`kanban` entity cache, `views`/perspective cache) already subscribe to the broadcast bus (`cache.subscribe()`); ensure they reload on undo/redo-sourced events too. This replaces the bespoke `reconcile_post_undo_caches` and also covers normal edits, agent writes, watcher edits.\n+- **UI + agents** receive the same events via the MCP notification surface (separate task) — the webview subscribes as an MCP client; there is no Tauri-specific path for change events anymore.\n\n ### B. Undo-stack state event (its own concern — fires on push/undo/redo)\n\n-Undo/redo also change `can_undo`/`can_redo` and the next undo/redo labels — state the UI's Undo/Redo controls depend on, separate from entity data. The `store` server emits a **stack-state notification on every stack mutation** — `push`, `undo`, `redo` — carrying `{ can_undo, can_redo, undo_label?, redo_label? }` (labels peeked from the `UndoStack` entry at/around the pointer).\n+Undo/redo also change `can_undo`/`can_redo` and the next undo/redo labels — state the UI's Undo/Redo controls depend on, separate from entity data. The `store` server emits a **stack-state notification on every stack mutation** — `push`, `undo`, `redo` — carrying `{ can_undo, can_redo, undo_label?, redo_label? }` (peeked from `UndoStack` around the pointer), delivered as `notifications/store/undo_changed` via the MCP notification surface.\n\n Critical non-symmetric case: a **normal edit after an undo discards the redo tail** (`UndoStack::push` truncates at the pointer), so `can_redo` flips to false with no undo/redo call — just an edit. The stack-state event must therefore fire on plain writes too, or the Redo control stays wrongly enabled. Data events never carry this; it is a property of the stack, not any item.\n\n Files:\n - `crates/swissarmyhammer-store/src/context.rs` — `undo`/`redo` emit transition-derived data events for `UndoOutcome.items`; `push`/`undo`/`redo` emit the stack-state event\n-- `crates/swissarmyhammer-entity/src/context.rs`, `crates/swissarmyhammer-views/src/context.rs` — subscribe caches to the shared change stream; drop manual post-undo reconcile\n-- MCP notification bridge (where `mcp_subscribe` is served) — forward both the data events and the stack-state event as notifications\n+- `crates/swissarmyhammer-entity/src/context.rs`, `crates/swissarmyhammer-views/src/context.rs` — caches reload on undo/redo-sourced bus events; drop manual post-undo reconcile\n - `crates/swissarmyhammer-kanban/src/commands/app_commands.rs` — remove `reconcile_post_undo_caches` (replaced by the subscription)\n\n ## Acceptance Criteria\n - [ ] undo and redo emit data events derived from the actual byte transition (create/remove/field), not a blanket update\n - [ ] redo-of-a-delete emits `removed`; redo-of-a-create emits `created`; redo-of-an-update emits the new field values\n-- [ ] in-process caches refresh from those events with no direction-specific code; `reconcile_post_undo_caches` is gone\n-- [ ] a stack-state notification fires on push, undo, AND redo with correct `can_undo`/`can_redo`/labels\n+- [ ] in-process caches refresh from those bus events with no direction-specific code; `reconcile_post_undo_caches` is gone\n+- [ ] a stack-state event fires on push, undo, AND redo with correct `can_undo`/`can_redo`/labels\n - [ ] a normal edit after an undo fires a stack-state event with `can_redo:false` (redo tail discarded)\n-- [ ] the UI cannot tell undo from redo from edit for data; its Undo/Redo controls track the stack-state event\n+- [ ] data + stack-state events are delivered to clients via the MCP notification surface (UI and agents alike)\n\n ## Tests\n-- [ ] `crates/swissarmyhammer-store/tests/integration/undo_redo_emits_events_e2e.rs` — create→undo emits `removed`; redo emits `created`; update→undo emits old field values, redo emits new; assert event op-types match the transition\n-- [ ] `crates/swissarmyhammer-store/tests/integration/stack_state_events_e2e.rs` — push fires stack event (can_undo true); undo fires (can_redo true); redo fires (can_redo false); a fresh edit after an undo fires with can_redo false\n+- [ ] `crates/swissarmyhammer-store/tests/integration/undo_redo_emits_events_e2e.rs` — create→undo emits `removed`; redo emits `created`; update→undo emits old field values, redo emits new\n+- [ ] `crates/swissarmyhammer-store/tests/integration/stack_state_events_e2e.rs` — push fires (can_undo true); undo fires (can_redo true); redo fires (can_redo false); a fresh edit after an undo fires with can_redo false\n - [ ] `crates/swissarmyhammer-command-service/tests/integration/undo_redo_notifies_dependents_e2e.rs` — boot `kanban`+`views`+`store`; edit entity + perspective; undo then redo; assert caches reflect each state without manual refresh AND a subscribed MCP client received both the data events and the stack-state events\n-- [ ] Frontend: `apps/kanban-app/ui/src/.../undo-redo-notification.test.tsx` — emit the data + stack events a redo produces; assert the board re-renders the reapplied state and the Undo/Redo buttons enable/disable correctly\n - [ ] `cargo test -p swissarmyhammer-store && cargo test -p swissarmyhammer-command-service --test integration undo_redo_notifies_dependents_e2e` pass\n\n ## Workflow\n-- Use `/tdd` — write `undo_redo_emits_events_e2e.rs` (transition-derivation) and `stack_state_events_e2e.rs` first; they pin the two contracts redo exposes.\n+- Use `/tdd` — write `undo_redo_emits_events_e2e.rs` (transition-derivation) and `stack_state_events_e2e.rs` first.\n\n-Depends on the `store` server and shared-substrate tasks. Related to the event-architecture rule (thin events) and the `single-changelog` project.\n\\ No newline at end of file\n+Depends on the `store` server, shared-substrate, and MCP notification surface tasks. Related to the event-architecture rule (thin events) and `single-changelog`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,6 @@\n depends_on:\n - 01KS5F7BR6850RKT67X4CNHPAZ\n - 01KS5F5ZNA0621X8KM2NPERXNV\n-- 01KS5G3AKZXDN7K6YR415E0V4K\n position_column: todo\n position_ordinal: 9b80\n project: command-service\n@@ -12,7 +11,7 @@\n ---\n ## What\n\n-Define what undo AND redo emit into the event system so every dependent — in-process caches and the UI — reacts through the paths they already use, with nothing downstream special-casing undo/redo. There are **two** distinct notifications, and redo makes the distinction obvious. (The generic MCP delivery of these notifications is the separate \"MCP notification surface\" task; this task defines the *content* and the *emit-on-undo/redo* behavior.)\n+Make undo AND redo propagate to every dependent — in-process caches and the UI — through the paths they already use, so nothing downstream special-cases undo/redo. There are **two** distinct notifications, and redo makes the distinction obvious:\n\n ### A. Data changes (symmetric undo/redo) — derive the event from the byte transition\n\n@@ -21,38 +20,40 @@\n - content → none = `entity removed`\n - content → content = field-level `{field, value}` changes (diff via `swissarmyhammer-entity/src/changelog.rs`)\n\n-These are the existing thin `EntityEvent` shapes (`swissarmyhammer-entity/src/events.rs`: `EntityChanged`/`EntityDeleted` with `{field,value}`), pushed onto the existing `broadcast::Sender<EntityEvent>` bus (`cache.rs:175`). One direction-agnostic derivation serves both undo and redo — redo-of-a-create emits `created`, redo-of-a-delete emits `removed`. If we blindly emitted \"update\", a redo-of-delete would leave a ghost card.\n+This single direction-agnostic derivation serves both undo and redo. It is why redo-of-a-create emits `created` and redo-of-a-delete emits `removed` — if we blindly emitted \"update\", a redo-of-delete would leave a ghost card in the UI. Events match the existing thin-event model (entity create/remove + field/value; no re-fetch).\n\n Consumers (unchanged by direction):\n-- **In-process caches** (`kanban` entity cache, `views`/perspective cache) already subscribe to the broadcast bus (`cache.subscribe()`); ensure they reload on undo/redo-sourced events too. This replaces the bespoke `reconcile_post_undo_caches` and also covers normal edits, agent writes, watcher edits.\n-- **UI + agents** receive the same events via the MCP notification surface (separate task) — the webview subscribes as an MCP client; there is no Tauri-specific path for change events anymore.\n+- **In-process caches** (`kanban` entity cache, `views`/perspective cache) subscribe to the shared `StoreContext` change stream and reload the named items. Replaces the bespoke `reconcile_post_undo_caches` and also covers normal edits, agent writes, and watcher-driven external edits.\n+- **UI** via the MCP notification bridge → `notifications/…changed`; the frontend already subscribes (`mcp_subscribe`) for normal edits, so undo/redo re-render with zero direction-specific code.\n\n ### B. Undo-stack state event (its own concern — fires on push/undo/redo)\n\n-Undo/redo also change `can_undo`/`can_redo` and the next undo/redo labels — state the UI's Undo/Redo controls depend on, separate from entity data. The `store` server emits a **stack-state notification on every stack mutation** — `push`, `undo`, `redo` — carrying `{ can_undo, can_redo, undo_label?, redo_label? }` (peeked from `UndoStack` around the pointer), delivered as `notifications/store/undo_changed` via the MCP notification surface.\n+Undo/redo also change `can_undo`/`can_redo` and the next undo/redo labels — state the UI's Undo/Redo controls depend on, separate from entity data. The `store` server emits a **stack-state notification on every stack mutation** — `push`, `undo`, `redo` — carrying `{ can_undo, can_redo, undo_label?, redo_label? }` (labels peeked from the `UndoStack` entry at/around the pointer).\n\n Critical non-symmetric case: a **normal edit after an undo discards the redo tail** (`UndoStack::push` truncates at the pointer), so `can_redo` flips to false with no undo/redo call — just an edit. The stack-state event must therefore fire on plain writes too, or the Redo control stays wrongly enabled. Data events never carry this; it is a property of the stack, not any item.\n\n Files:\n - `crates/swissarmyhammer-store/src/context.rs` — `undo`/`redo` emit transition-derived data events for `UndoOutcome.items`; `push`/`undo`/`redo` emit the stack-state event\n-- `crates/swissarmyhammer-entity/src/context.rs`, `crates/swissarmyhammer-views/src/context.rs` — caches reload on undo/redo-sourced bus events; drop manual post-undo reconcile\n+- `crates/swissarmyhammer-entity/src/context.rs`, `crates/swissarmyhammer-views/src/context.rs` — subscribe caches to the shared change stream; drop manual post-undo reconcile\n+- MCP notification bridge (where `mcp_subscribe` is served) — forward both the data events and the stack-state event as notifications\n - `crates/swissarmyhammer-kanban/src/commands/app_commands.rs` — remove `reconcile_post_undo_caches` (replaced by the subscription)\n\n ## Acceptance Criteria\n - [ ] undo and redo emit data events derived from the actual byte transition (create/remove/field), not a blanket update\n - [ ] redo-of-a-delete emits `removed`; redo-of-a-create emits `created`; redo-of-an-update emits the new field values\n-- [ ] in-process caches refresh from those bus events with no direction-specific code; `reconcile_post_undo_caches` is gone\n-- [ ] a stack-state event fires on push, undo, AND redo with correct `can_undo`/`can_redo`/labels\n+- [ ] in-process caches refresh from those events with no direction-specific code; `reconcile_post_undo_caches` is gone\n+- [ ] a stack-state notification fires on push, undo, AND redo with correct `can_undo`/`can_redo`/labels\n - [ ] a normal edit after an undo fires a stack-state event with `can_redo:false` (redo tail discarded)\n-- [ ] data + stack-state events are delivered to clients via the MCP notification surface (UI and agents alike)\n+- [ ] the UI cannot tell undo from redo from edit for data; its Undo/Redo controls track the stack-state event\n\n ## Tests\n-- [ ] `crates/swissarmyhammer-store/tests/integration/undo_redo_emits_events_e2e.rs` — create→undo emits `removed`; redo emits `created`; update→undo emits old field values, redo emits new\n-- [ ] `crates/swissarmyhammer-store/tests/integration/stack_state_events_e2e.rs` — push fires (can_undo true); undo fires (can_redo true); redo fires (can_redo false); a fresh edit after an undo fires with can_redo false\n+- [ ] `crates/swissarmyhammer-store/tests/integration/undo_redo_emits_events_e2e.rs` — create→undo emits `removed`; redo emits `created`; update→undo emits old field values, redo emits new; assert event op-types match the transition\n+- [ ] `crates/swissarmyhammer-store/tests/integration/stack_state_events_e2e.rs` — push fires stack event (can_undo true); undo fires (can_redo true); redo fires (can_redo false); a fresh edit after an undo fires with can_redo false\n - [ ] `crates/swissarmyhammer-command-service/tests/integration/undo_redo_notifies_dependents_e2e.rs` — boot `kanban`+`views`+`store`; edit entity + perspective; undo then redo; assert caches reflect each state without manual refresh AND a subscribed MCP client received both the data events and the stack-state events\n+- [ ] Frontend: `apps/kanban-app/ui/src/.../undo-redo-notification.test.tsx` — emit the data + stack events a redo produces; assert the board re-renders the reapplied state and the Undo/Redo buttons enable/disable correctly\n - [ ] `cargo test -p swissarmyhammer-store && cargo test -p swissarmyhammer-command-service --test integration undo_redo_notifies_dependents_e2e` pass\n\n ## Workflow\n-- Use `/tdd` — write `undo_redo_emits_events_e2e.rs` (transition-derivation) and `stack_state_events_e2e.rs` first.\n+- Use `/tdd` — write `undo_redo_emits_events_e2e.rs` (transition-derivation) and `stack_state_events_e2e.rs` first; they pin the two contracts redo exposes.\n\n-Depends on the `store` server, shared-substrate, and MCP notification surface tasks. Related to the event-architecture rule (thin events) and `single-changelog`.\n\\ No newline at end of file\n+Depends on the `store` server and shared-substrate tasks. Related to the event-architecture rule (thin events) and the `single-changelog` project.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KS5GWS641RBJ13Q4BK9Z97MG","timestamp":"2026-05-21T15:01:35.044394Z","op":"update","item_id":"01KS5F8THM5EQMKFSF6GFAE55C","forward_patch":"--- original\n+++ modified\n@@ -7,7 +7,7 @@\n - 01KS5G3AKZXDN7K6YR415E0V4K\n position_column: todo\n position_ordinal: 9b80\n-project: command-service\n+project: command-events\n title: 'Change propagation: undo/redo emit edit-shaped data events + a stack-state event (caches + UI)'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -7,7 +7,7 @@\n - 01KS5G3AKZXDN7K6YR415E0V4K\n position_column: todo\n position_ordinal: 9b80\n-project: command-events\n+project: command-service\n title: 'Change propagation: undo/redo emit edit-shaped data events + a stack-state event (caches + UI)'\n ---\n ## What\n","transaction_id":null}
{"id":"01KS5X3PZNZEMY2EPK4YD3AHFY","timestamp":"2026-05-21T18:35:05.077445Z","op":"update","item_id":"01KS5F8THM5EQMKFSF6GFAE55C","forward_patch":"--- original\n+++ modified\n@@ -12,47 +12,59 @@\n ---\n ## What\n\n-Define what undo AND redo emit into the event system so every dependent — in-process caches and the UI — reacts through the paths they already use, with nothing downstream special-casing undo/redo. There are **two** distinct notifications, and redo makes the distinction obvious. (The generic MCP delivery of these notifications is the separate \"MCP notification surface\" task; this task defines the *content* and the *emit-on-undo/redo* behavior.)\n+Make undo/redo propagate to in-process caches **and** the UI through the *same* paths a normal edit uses, with nothing downstream special-casing undo/redo, and add the one event that genuinely doesn't exist yet (undo-stack state). (Generic MCP delivery of these notifications is the separate \"MCP notification surface\" task; this task is about *content* and *emit-on-undo/redo* behavior.)\n+\n+### Current state — READ THIS FIRST (the convergence already exists)\n+\n+\"Undo == edit downstream\" is **already true today**, do not rebuild it:\n+- `StoreContext::undo`/`redo` (`crates/swissarmyhammer-store/src/context.rs`) only rewrite disk via `reverse_patch`/`forward_patch` and return `UndoOutcome { items: [(store, item)…] }`. They do **NOT** emit entity/view/perspective events and **cannot**: `swissarmyhammer-store` has no dependency on `-entity`/`-views`/`-kanban` and no broadcast sender (Cargo.toml = diffy/serde/tokio only). Emitting entity field-diffs from store would be a layering inversion — do not do it.\n+- One layer up, `crates/swissarmyhammer-kanban/src/commands/app_commands.rs::reconcile_post_undo_caches` (called after every `undo`/`redo`) already reconciles **all three** store categories, and each branch derives the transition and emits on the same broadcast bus a normal edit / watcher edit uses:\n+  - **Entities**: iterates `outcome.items` → `EntityContext::sync_entity_cache_from_disk` (`context.rs:658`) → `cache.refresh_from_disk` (exists → field diff via `cache.rs::diff` → `EntityChanged`) or `cache.evict` (gone → `EntityDeleted`). Same two calls the file watcher uses (`watcher.rs:210`).\n+  - **Perspectives**: `reconcile_perspective_cache` → `pctx.reload_from_disk(item)` → `PerspectiveChanged`/`PerspectiveDeleted`.\n+  - **Views**: `reconcile_view_cache` → `views.reload_from_disk(item)` → `ViewEvent`.\n+- redo-of-a-delete already surfaces as `removed` and redo-of-a-create as `created`, because the event is derived from the post-rewrite byte state, not from the direction. Validated by `crates/swissarmyhammer-entity/src/context.rs:4078` (delete→undo emits `EntityChanged`, redo emits `EntityDeleted`) and `:4115` (archive round-trip). Idempotency: the watcher also sees undo's disk writes, but `refresh_from_disk`'s hash gate makes that a silent no-op — no double-fire.\n+\n+So the data-event path is correct and converged. The real work is (A) **unify** the reconcile, (B) add `txn`/`origin`, (C) add the **stack-state** event.\n+\n+### A. Data changes — keep the reconcile, unify it, don't delete it\n\n-### A. Data changes (symmetric undo/redo) — derive the event from the byte transition\n+`reconcile_post_undo_caches` is the convergence point. Removing it would leave nobody to emit (store can't). The work is to collapse its three hand-written, store-name-keyed branches into one uniform reconcile keyed on `outcome.items` + the store's category, so a new store category doesn't require a new bespoke branch. The per-item derivation (none→content = created, content→none = removed, content→content = field `{field,value}`) already lives in `refresh_from_disk`/`reload_from_disk`; reuse it.\n\n-`StoreContext::undo` applies `reverse_patch`; `redo` applies `forward_patch`. Both return `UndoOutcome { items: [(store, item)…] }`. After the rewrite, emit a **thin change event per item describing the actual transition on disk**, NOT a blanket \"update\":\n-- none → content = `entity created`\n-- content → none = `entity removed`\n-- content → content = field-level `{field, value}` changes (diff via `swissarmyhammer-entity/src/changelog.rs`)\n+Consumers (unchanged by direction): in-process caches re-read on the reconcile-emitted bus events (also covers normal edits, agent writes, watcher edits); UI + agents receive the same events via the MCP notification surface (separate task) — the webview subscribes as an MCP client, no Tauri-specific change path remains.\n\n-These are the existing thin `EntityEvent` shapes (`swissarmyhammer-entity/src/events.rs`: `EntityChanged`/`EntityDeleted` with `{field,value}`), pushed onto the existing `broadcast::Sender<EntityEvent>` bus (`cache.rs:175`). One direction-agnostic derivation serves both undo and redo — redo-of-a-create emits `created`, redo-of-a-delete emits `removed`. If we blindly emitted \"update\", a redo-of-delete would leave a ghost card.\n+### B. `txn` + `origin` on data events (net-new field work)\n\n-Consumers (unchanged by direction):\n-- **In-process caches** (`kanban` entity cache, `views`/perspective cache) already subscribe to the broadcast bus (`cache.subscribe()`); ensure they reload on undo/redo-sourced events too. This replaces the bespoke `reconcile_post_undo_caches` and also covers normal edits, agent writes, watcher edits.\n-- **UI + agents** receive the same events via the MCP notification surface (separate task) — the webview subscribes as an MCP client; there is no Tauri-specific path for change events anymore.\n+None of `EntityEvent` (`crates/swissarmyhammer-entity/src/events.rs:31`), `PerspectiveChanged`, or `ViewEvent` carry correlation/provenance today. Add `txn` (ambient transaction id opened by the Command engine's `execute`, propagated via `RequestContext::extensions`) and `origin` (`user|agent:id|undo|redo|watcher`) so the UI batches a command's N changes into one atomic re-render and can attribute/echo-suppress. The reconcile stamps `origin: undo|redo`; the watcher stamps `origin: watcher`.\n\n-### B. Undo-stack state event (its own concern — fires on push/undo/redo)\n+### C. Undo-stack state event (its own concern — the only truly new event)\n\n-Undo/redo also change `can_undo`/`can_redo` and the next undo/redo labels — state the UI's Undo/Redo controls depend on, separate from entity data. The `store` server emits a **stack-state notification on every stack mutation** — `push`, `undo`, `redo` — carrying `{ can_undo, can_redo, undo_label?, redo_label? }` (peeked from `UndoStack` around the pointer), delivered as `notifications/store/undo_changed` via the MCP notification surface.\n+Undo/redo/push also change `can_undo`/`can_redo` and the next undo/redo labels — UI control state, separate from item data and not carried by any data event. The `store` server emits a stack-state notification on **every** stack mutation — `push`, `undo`, `redo` — carrying `{ can_undo, can_redo, undo_label?, redo_label? }` (peeked from `UndoStack` around the pointer), delivered as `notifications/store/undo_changed`. This event CAN originate in `swissarmyhammer-store` (it owns `UndoStack` and the event carries no foreign types) — adding a stack-state sender to the store is in-layer, unlike data events.\n\n-Critical non-symmetric case: a **normal edit after an undo discards the redo tail** (`UndoStack::push` truncates at the pointer), so `can_redo` flips to false with no undo/redo call — just an edit. The stack-state event must therefore fire on plain writes too, or the Redo control stays wrongly enabled. Data events never carry this; it is a property of the stack, not any item.\n+Critical non-symmetric case: a **normal edit after an undo discards the redo tail** (`UndoStack::push` truncates at the pointer), so `can_redo` flips to false with no undo/redo call — just an edit. The stack-state event must therefore fire on plain `push` too, or the Redo control stays wrongly enabled.\n\n Files:\n-- `crates/swissarmyhammer-store/src/context.rs` — `undo`/`redo` emit transition-derived data events for `UndoOutcome.items`; `push`/`undo`/`redo` emit the stack-state event\n-- `crates/swissarmyhammer-entity/src/context.rs`, `crates/swissarmyhammer-views/src/context.rs` — caches reload on undo/redo-sourced bus events; drop manual post-undo reconcile\n-- `crates/swissarmyhammer-kanban/src/commands/app_commands.rs` — remove `reconcile_post_undo_caches` (replaced by the subscription)\n+- `crates/swissarmyhammer-kanban/src/commands/app_commands.rs` — unify `reconcile_post_undo_caches`'s three branches into one category-keyed reconcile; stamp `origin: undo|redo`. (Do NOT delete the reconcile; it is the emit trigger. If/when it relocates into the store-service/entity-service wiring, the mechanism must survive intact.)\n+- `crates/swissarmyhammer-entity/src/events.rs`, perspective/view event types — add `txn`/`origin` fields.\n+- `crates/swissarmyhammer-entity/src/cache.rs`, `crates/swissarmyhammer-views/src/context.rs` — thread `txn`/`origin` through `refresh_from_disk`/`reload_from_disk` emission; stamp `origin: watcher` on watcher-sourced refresh.\n+- `crates/swissarmyhammer-store/src/context.rs` — `push`/`undo`/`redo` emit the stack-state event (new in-layer stack-state sender); undo/redo continue to return `UndoOutcome` for the reconcile (no entity emission added here).\n\n ## Acceptance Criteria\n-- [ ] undo and redo emit data events derived from the actual byte transition (create/remove/field), not a blanket update\n+- [ ] undo and redo data events are derived from the actual byte transition (create/remove/field) — confirmed already true and covered by a regression test, not newly invented in `swissarmyhammer-store`\n - [ ] redo-of-a-delete emits `removed`; redo-of-a-create emits `created`; redo-of-an-update emits the new field values\n-- [ ] in-process caches refresh from those bus events with no direction-specific code; `reconcile_post_undo_caches` is gone\n+- [ ] `reconcile_post_undo_caches`'s three branches are unified into one category-keyed reconcile; the reconcile is retained as the emit trigger (NOT removed); a new store category needs no new bespoke branch\n+- [ ] `EntityEvent`/perspective/view events carry `txn` + `origin`; a single command's N changes share one `txn`; undo/redo-sourced events carry `origin: undo`/`redo`, watcher-sourced carry `origin: watcher`\n+- [ ] no entity/view/perspective event emission is added inside `swissarmyhammer-store` (layering preserved); store emits only the stack-state event\n - [ ] a stack-state event fires on push, undo, AND redo with correct `can_undo`/`can_redo`/labels\n - [ ] a normal edit after an undo fires a stack-state event with `can_redo:false` (redo tail discarded)\n - [ ] data + stack-state events are delivered to clients via the MCP notification surface (UI and agents alike)\n\n ## Tests\n-- [ ] `crates/swissarmyhammer-store/tests/integration/undo_redo_emits_events_e2e.rs` — create→undo emits `removed`; redo emits `created`; update→undo emits old field values, redo emits new\n - [ ] `crates/swissarmyhammer-store/tests/integration/stack_state_events_e2e.rs` — push fires (can_undo true); undo fires (can_redo true); redo fires (can_redo false); a fresh edit after an undo fires with can_redo false\n-- [ ] `crates/swissarmyhammer-command-service/tests/integration/undo_redo_notifies_dependents_e2e.rs` — boot `kanban`+`views`+`store`; edit entity + perspective; undo then redo; assert caches reflect each state without manual refresh AND a subscribed MCP client received both the data events and the stack-state events\n-- [ ] `cargo test -p swissarmyhammer-store && cargo test -p swissarmyhammer-command-service --test integration undo_redo_notifies_dependents_e2e` pass\n+- [ ] `crates/swissarmyhammer-entity/tests/integration/undo_redo_emits_transition_events_e2e.rs` — create→undo emits `removed`; redo emits `created`; update→undo emits old field values, redo emits new; each carries `origin: undo|redo` and a shared `txn` per command (exercises the reconcile path, NOT a store-level emitter)\n+- [ ] `crates/swissarmyhammer-command-service/tests/integration/undo_redo_notifies_dependents_e2e.rs` — boot `kanban`+`views`+`store`; edit entity + perspective + view; undo then redo; assert caches reflect each state via the unified reconcile (no per-category bespoke code) AND a subscribed MCP client received both the data events (with txn/origin) and the stack-state events\n+- [ ] `cargo test -p swissarmyhammer-store && cargo test -p swissarmyhammer-entity && cargo test -p swissarmyhammer-command-service --test integration undo_redo_notifies_dependents_e2e` pass\n\n ## Workflow\n-- Use `/tdd` — write `undo_redo_emits_events_e2e.rs` (transition-derivation) and `stack_state_events_e2e.rs` first.\n+- Use `/tdd` — write `stack_state_events_e2e.rs` and `undo_redo_emits_transition_events_e2e.rs` first.\n\n Depends on the `store` server, shared-substrate, and MCP notification surface tasks. Related to the event-architecture rule (thin events) and `single-changelog`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -12,59 +12,47 @@\n ---\n ## What\n\n-Make undo/redo propagate to in-process caches **and** the UI through the *same* paths a normal edit uses, with nothing downstream special-casing undo/redo, and add the one event that genuinely doesn't exist yet (undo-stack state). (Generic MCP delivery of these notifications is the separate \"MCP notification surface\" task; this task is about *content* and *emit-on-undo/redo* behavior.)\n+Define what undo AND redo emit into the event system so every dependent — in-process caches and the UI — reacts through the paths they already use, with nothing downstream special-casing undo/redo. There are **two** distinct notifications, and redo makes the distinction obvious. (The generic MCP delivery of these notifications is the separate \"MCP notification surface\" task; this task defines the *content* and the *emit-on-undo/redo* behavior.)\n\n-### Current state — READ THIS FIRST (the convergence already exists)\n+### A. Data changes (symmetric undo/redo) — derive the event from the byte transition\n\n-\"Undo == edit downstream\" is **already true today**, do not rebuild it:\n-- `StoreContext::undo`/`redo` (`crates/swissarmyhammer-store/src/context.rs`) only rewrite disk via `reverse_patch`/`forward_patch` and return `UndoOutcome { items: [(store, item)…] }`. They do **NOT** emit entity/view/perspective events and **cannot**: `swissarmyhammer-store` has no dependency on `-entity`/`-views`/`-kanban` and no broadcast sender (Cargo.toml = diffy/serde/tokio only). Emitting entity field-diffs from store would be a layering inversion — do not do it.\n-- One layer up, `crates/swissarmyhammer-kanban/src/commands/app_commands.rs::reconcile_post_undo_caches` (called after every `undo`/`redo`) already reconciles **all three** store categories, and each branch derives the transition and emits on the same broadcast bus a normal edit / watcher edit uses:\n-  - **Entities**: iterates `outcome.items` → `EntityContext::sync_entity_cache_from_disk` (`context.rs:658`) → `cache.refresh_from_disk` (exists → field diff via `cache.rs::diff` → `EntityChanged`) or `cache.evict` (gone → `EntityDeleted`). Same two calls the file watcher uses (`watcher.rs:210`).\n-  - **Perspectives**: `reconcile_perspective_cache` → `pctx.reload_from_disk(item)` → `PerspectiveChanged`/`PerspectiveDeleted`.\n-  - **Views**: `reconcile_view_cache` → `views.reload_from_disk(item)` → `ViewEvent`.\n-- redo-of-a-delete already surfaces as `removed` and redo-of-a-create as `created`, because the event is derived from the post-rewrite byte state, not from the direction. Validated by `crates/swissarmyhammer-entity/src/context.rs:4078` (delete→undo emits `EntityChanged`, redo emits `EntityDeleted`) and `:4115` (archive round-trip). Idempotency: the watcher also sees undo's disk writes, but `refresh_from_disk`'s hash gate makes that a silent no-op — no double-fire.\n+`StoreContext::undo` applies `reverse_patch`; `redo` applies `forward_patch`. Both return `UndoOutcome { items: [(store, item)…] }`. After the rewrite, emit a **thin change event per item describing the actual transition on disk**, NOT a blanket \"update\":\n+- none → content = `entity created`\n+- content → none = `entity removed`\n+- content → content = field-level `{field, value}` changes (diff via `swissarmyhammer-entity/src/changelog.rs`)\n\n-So the data-event path is correct and converged. The real work is (A) **unify** the reconcile, (B) add `txn`/`origin`, (C) add the **stack-state** event.\n+These are the existing thin `EntityEvent` shapes (`swissarmyhammer-entity/src/events.rs`: `EntityChanged`/`EntityDeleted` with `{field,value}`), pushed onto the existing `broadcast::Sender<EntityEvent>` bus (`cache.rs:175`). One direction-agnostic derivation serves both undo and redo — redo-of-a-create emits `created`, redo-of-a-delete emits `removed`. If we blindly emitted \"update\", a redo-of-delete would leave a ghost card.\n\n-### A. Data changes — keep the reconcile, unify it, don't delete it\n+Consumers (unchanged by direction):\n+- **In-process caches** (`kanban` entity cache, `views`/perspective cache) already subscribe to the broadcast bus (`cache.subscribe()`); ensure they reload on undo/redo-sourced events too. This replaces the bespoke `reconcile_post_undo_caches` and also covers normal edits, agent writes, watcher edits.\n+- **UI + agents** receive the same events via the MCP notification surface (separate task) — the webview subscribes as an MCP client; there is no Tauri-specific path for change events anymore.\n\n-`reconcile_post_undo_caches` is the convergence point. Removing it would leave nobody to emit (store can't). The work is to collapse its three hand-written, store-name-keyed branches into one uniform reconcile keyed on `outcome.items` + the store's category, so a new store category doesn't require a new bespoke branch. The per-item derivation (none→content = created, content→none = removed, content→content = field `{field,value}`) already lives in `refresh_from_disk`/`reload_from_disk`; reuse it.\n-\n-Consumers (unchanged by direction): in-process caches re-read on the reconcile-emitted bus events (also covers normal edits, agent writes, watcher edits); UI + agents receive the same events via the MCP notification surface (separate task) — the webview subscribes as an MCP client, no Tauri-specific change path remains.\n+### B. Undo-stack state event (its own concern — fires on push/undo/redo)\n\n-### B. `txn` + `origin` on data events (net-new field work)\n+Undo/redo also change `can_undo`/`can_redo` and the next undo/redo labels — state the UI's Undo/Redo controls depend on, separate from entity data. The `store` server emits a **stack-state notification on every stack mutation** — `push`, `undo`, `redo` — carrying `{ can_undo, can_redo, undo_label?, redo_label? }` (peeked from `UndoStack` around the pointer), delivered as `notifications/store/undo_changed` via the MCP notification surface.\n\n-None of `EntityEvent` (`crates/swissarmyhammer-entity/src/events.rs:31`), `PerspectiveChanged`, or `ViewEvent` carry correlation/provenance today. Add `txn` (ambient transaction id opened by the Command engine's `execute`, propagated via `RequestContext::extensions`) and `origin` (`user|agent:id|undo|redo|watcher`) so the UI batches a command's N changes into one atomic re-render and can attribute/echo-suppress. The reconcile stamps `origin: undo|redo`; the watcher stamps `origin: watcher`.\n-\n-### C. Undo-stack state event (its own concern — the only truly new event)\n-\n-Undo/redo/push also change `can_undo`/`can_redo` and the next undo/redo labels — UI control state, separate from item data and not carried by any data event. The `store` server emits a stack-state notification on **every** stack mutation — `push`, `undo`, `redo` — carrying `{ can_undo, can_redo, undo_label?, redo_label? }` (peeked from `UndoStack` around the pointer), delivered as `notifications/store/undo_changed`. This event CAN originate in `swissarmyhammer-store` (it owns `UndoStack` and the event carries no foreign types) — adding a stack-state sender to the store is in-layer, unlike data events.\n-\n-Critical non-symmetric case: a **normal edit after an undo discards the redo tail** (`UndoStack::push` truncates at the pointer), so `can_redo` flips to false with no undo/redo call — just an edit. The stack-state event must therefore fire on plain `push` too, or the Redo control stays wrongly enabled.\n+Critical non-symmetric case: a **normal edit after an undo discards the redo tail** (`UndoStack::push` truncates at the pointer), so `can_redo` flips to false with no undo/redo call — just an edit. The stack-state event must therefore fire on plain writes too, or the Redo control stays wrongly enabled. Data events never carry this; it is a property of the stack, not any item.\n\n Files:\n-- `crates/swissarmyhammer-kanban/src/commands/app_commands.rs` — unify `reconcile_post_undo_caches`'s three branches into one category-keyed reconcile; stamp `origin: undo|redo`. (Do NOT delete the reconcile; it is the emit trigger. If/when it relocates into the store-service/entity-service wiring, the mechanism must survive intact.)\n-- `crates/swissarmyhammer-entity/src/events.rs`, perspective/view event types — add `txn`/`origin` fields.\n-- `crates/swissarmyhammer-entity/src/cache.rs`, `crates/swissarmyhammer-views/src/context.rs` — thread `txn`/`origin` through `refresh_from_disk`/`reload_from_disk` emission; stamp `origin: watcher` on watcher-sourced refresh.\n-- `crates/swissarmyhammer-store/src/context.rs` — `push`/`undo`/`redo` emit the stack-state event (new in-layer stack-state sender); undo/redo continue to return `UndoOutcome` for the reconcile (no entity emission added here).\n+- `crates/swissarmyhammer-store/src/context.rs` — `undo`/`redo` emit transition-derived data events for `UndoOutcome.items`; `push`/`undo`/`redo` emit the stack-state event\n+- `crates/swissarmyhammer-entity/src/context.rs`, `crates/swissarmyhammer-views/src/context.rs` — caches reload on undo/redo-sourced bus events; drop manual post-undo reconcile\n+- `crates/swissarmyhammer-kanban/src/commands/app_commands.rs` — remove `reconcile_post_undo_caches` (replaced by the subscription)\n\n ## Acceptance Criteria\n-- [ ] undo and redo data events are derived from the actual byte transition (create/remove/field) — confirmed already true and covered by a regression test, not newly invented in `swissarmyhammer-store`\n+- [ ] undo and redo emit data events derived from the actual byte transition (create/remove/field), not a blanket update\n - [ ] redo-of-a-delete emits `removed`; redo-of-a-create emits `created`; redo-of-an-update emits the new field values\n-- [ ] `reconcile_post_undo_caches`'s three branches are unified into one category-keyed reconcile; the reconcile is retained as the emit trigger (NOT removed); a new store category needs no new bespoke branch\n-- [ ] `EntityEvent`/perspective/view events carry `txn` + `origin`; a single command's N changes share one `txn`; undo/redo-sourced events carry `origin: undo`/`redo`, watcher-sourced carry `origin: watcher`\n-- [ ] no entity/view/perspective event emission is added inside `swissarmyhammer-store` (layering preserved); store emits only the stack-state event\n+- [ ] in-process caches refresh from those bus events with no direction-specific code; `reconcile_post_undo_caches` is gone\n - [ ] a stack-state event fires on push, undo, AND redo with correct `can_undo`/`can_redo`/labels\n - [ ] a normal edit after an undo fires a stack-state event with `can_redo:false` (redo tail discarded)\n - [ ] data + stack-state events are delivered to clients via the MCP notification surface (UI and agents alike)\n\n ## Tests\n+- [ ] `crates/swissarmyhammer-store/tests/integration/undo_redo_emits_events_e2e.rs` — create→undo emits `removed`; redo emits `created`; update→undo emits old field values, redo emits new\n - [ ] `crates/swissarmyhammer-store/tests/integration/stack_state_events_e2e.rs` — push fires (can_undo true); undo fires (can_redo true); redo fires (can_redo false); a fresh edit after an undo fires with can_redo false\n-- [ ] `crates/swissarmyhammer-entity/tests/integration/undo_redo_emits_transition_events_e2e.rs` — create→undo emits `removed`; redo emits `created`; update→undo emits old field values, redo emits new; each carries `origin: undo|redo` and a shared `txn` per command (exercises the reconcile path, NOT a store-level emitter)\n-- [ ] `crates/swissarmyhammer-command-service/tests/integration/undo_redo_notifies_dependents_e2e.rs` — boot `kanban`+`views`+`store`; edit entity + perspective + view; undo then redo; assert caches reflect each state via the unified reconcile (no per-category bespoke code) AND a subscribed MCP client received both the data events (with txn/origin) and the stack-state events\n-- [ ] `cargo test -p swissarmyhammer-store && cargo test -p swissarmyhammer-entity && cargo test -p swissarmyhammer-command-service --test integration undo_redo_notifies_dependents_e2e` pass\n+- [ ] `crates/swissarmyhammer-command-service/tests/integration/undo_redo_notifies_dependents_e2e.rs` — boot `kanban`+`views`+`store`; edit entity + perspective; undo then redo; assert caches reflect each state without manual refresh AND a subscribed MCP client received both the data events and the stack-state events\n+- [ ] `cargo test -p swissarmyhammer-store && cargo test -p swissarmyhammer-command-service --test integration undo_redo_notifies_dependents_e2e` pass\n\n ## Workflow\n-- Use `/tdd` — write `stack_state_events_e2e.rs` and `undo_redo_emits_transition_events_e2e.rs` first.\n+- Use `/tdd` — write `undo_redo_emits_events_e2e.rs` (transition-derivation) and `stack_state_events_e2e.rs` first.\n\n Depends on the `store` server, shared-substrate, and MCP notification surface tasks. Related to the event-architecture rule (thin events) and `single-changelog`.\n\\ No newline at end of file\n","transaction_id":null}

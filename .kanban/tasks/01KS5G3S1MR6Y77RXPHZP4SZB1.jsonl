{"id":"01KS5G3S1M13ZVYGQ00NPAVBB6","timestamp":"2026-05-21T14:47:55.700451Z","op":"create","item_id":"01KS5G3S1MR6Y77RXPHZP4SZB1","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,40 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KS5G3AKZXDN7K6YR415E0V4K\n+- 01KS5F8THM5EQMKFSF6GFAE55C\n+position_column: todo\n+position_ordinal: 9d80\n+project: command-service\n+title: 'Frontend: subscribe to MCP change notifications, retire Tauri app.emit for changes'\n+---\n+## What\n+\n+Migrate the kanban webview from Tauri-specific change events to the MCP notification surface, so the UI is just another MCP client (same stream agents receive). Today React listens via Tauri `listen(\"entity-field-changed\", …)` / `listen(\"board-changed\", …)` etc., fed by `app.emit(...)` in `apps/kanban-app/src/commands.rs`. Replace with an MCP notification subscription.\n+\n+Files:\n+- `apps/kanban-app/ui/src/...` data layer — subscribe to the MCP notification stream (`notifications/entity/changed`, `notifications/entity/deleted`, `notifications/attachment/changed`, `notifications/store/undo_changed`); apply the thin `{field,value}` patches to local state exactly as the existing Tauri listeners do (the payloads are the same thin events, so the reducer logic is reused, only the source changes)\n+- Replace `listen(\"entity-field-changed\"|\"board-changed\"|\"attachment-changed\"|...)` call sites with the MCP subscription\n+- `apps/kanban-app/src/commands.rs` — remove the `app.emit(...)` change-event calls (entity bridge, `board-changed`) once the MCP path delivers them; keep only genuinely Tauri-shell emits that aren't change events (e.g. native dialogs), if any remain\n+- Undo/Redo controls subscribe to `notifications/store/undo_changed` for `can_undo`/`can_redo`/labels (from the change-propagation task)\n+\n+Memory `metadata-driven-ui` / `frontend-logging` still apply. The reducer that applies thin patches does not change — only the transport (MCP notification instead of Tauri event).\n+\n+## Acceptance Criteria\n+- [ ] The webview receives entity/attachment/board change notifications over MCP, not Tauri `app.emit`\n+- [ ] `app.emit(...)` change-event calls in `apps/kanban-app/src/commands.rs` are removed (only non-change shell emits, if any, remain)\n+- [ ] An edit (from UI, agent, or undo) re-renders the board via the MCP notification path\n+- [ ] Undo/Redo buttons reflect `notifications/store/undo_changed`\n+- [ ] No React code path still depends on `listen(\"entity-field-changed\")` and friends\n+\n+## Tests\n+- [ ] `apps/kanban-app/ui/src/.../mcp-notifications.test.tsx` — mock the MCP transport; emit `notifications/entity/changed` with a `{field,value}` payload; assert the board state patches identically to the old Tauri path; emit `entity/deleted` → card removed\n+- [ ] `apps/kanban-app/ui/src/.../undo-controls.test.tsx` — emit `notifications/store/undo_changed { can_undo:false, can_redo:true }`; assert Undo disabled, Redo enabled\n+- [ ] A code-quality grep test asserts no `listen(\"entity-field-changed\"|\"board-changed\"|...)` remains in the UI source\n+- [ ] `npm test --prefix apps/kanban-app/ui` passes\n+\n+## Workflow\n+- Use `/tdd` — write the notification-reducer test first; it proves the thin-patch logic is reused unchanged.\n+\n+Depends on the MCP notification surface task and the change-propagation task.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,40 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KS5G3AKZXDN7K6YR415E0V4K\n-- 01KS5F8THM5EQMKFSF6GFAE55C\n-position_column: todo\n-position_ordinal: 9d80\n-project: command-service\n-title: 'Frontend: subscribe to MCP change notifications, retire Tauri app.emit for changes'\n----\n-## What\n-\n-Migrate the kanban webview from Tauri-specific change events to the MCP notification surface, so the UI is just another MCP client (same stream agents receive). Today React listens via Tauri `listen(\"entity-field-changed\", …)` / `listen(\"board-changed\", …)` etc., fed by `app.emit(...)` in `apps/kanban-app/src/commands.rs`. Replace with an MCP notification subscription.\n-\n-Files:\n-- `apps/kanban-app/ui/src/...` data layer — subscribe to the MCP notification stream (`notifications/entity/changed`, `notifications/entity/deleted`, `notifications/attachment/changed`, `notifications/store/undo_changed`); apply the thin `{field,value}` patches to local state exactly as the existing Tauri listeners do (the payloads are the same thin events, so the reducer logic is reused, only the source changes)\n-- Replace `listen(\"entity-field-changed\"|\"board-changed\"|\"attachment-changed\"|...)` call sites with the MCP subscription\n-- `apps/kanban-app/src/commands.rs` — remove the `app.emit(...)` change-event calls (entity bridge, `board-changed`) once the MCP path delivers them; keep only genuinely Tauri-shell emits that aren't change events (e.g. native dialogs), if any remain\n-- Undo/Redo controls subscribe to `notifications/store/undo_changed` for `can_undo`/`can_redo`/labels (from the change-propagation task)\n-\n-Memory `metadata-driven-ui` / `frontend-logging` still apply. The reducer that applies thin patches does not change — only the transport (MCP notification instead of Tauri event).\n-\n-## Acceptance Criteria\n-- [ ] The webview receives entity/attachment/board change notifications over MCP, not Tauri `app.emit`\n-- [ ] `app.emit(...)` change-event calls in `apps/kanban-app/src/commands.rs` are removed (only non-change shell emits, if any, remain)\n-- [ ] An edit (from UI, agent, or undo) re-renders the board via the MCP notification path\n-- [ ] Undo/Redo buttons reflect `notifications/store/undo_changed`\n-- [ ] No React code path still depends on `listen(\"entity-field-changed\")` and friends\n-\n-## Tests\n-- [ ] `apps/kanban-app/ui/src/.../mcp-notifications.test.tsx` — mock the MCP transport; emit `notifications/entity/changed` with a `{field,value}` payload; assert the board state patches identically to the old Tauri path; emit `entity/deleted` → card removed\n-- [ ] `apps/kanban-app/ui/src/.../undo-controls.test.tsx` — emit `notifications/store/undo_changed { can_undo:false, can_redo:true }`; assert Undo disabled, Redo enabled\n-- [ ] A code-quality grep test asserts no `listen(\"entity-field-changed\"|\"board-changed\"|...)` remains in the UI source\n-- [ ] `npm test --prefix apps/kanban-app/ui` passes\n-\n-## Workflow\n-- Use `/tdd` — write the notification-reducer test first; it proves the thin-patch logic is reused unchanged.\n-\n-Depends on the MCP notification surface task and the change-propagation task.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KS5GX8W5P4C729MA93V7K14T","timestamp":"2026-05-21T15:01:51.109920Z","op":"update","item_id":"01KS5G3S1MR6Y77RXPHZP4SZB1","forward_patch":"--- original\n+++ modified\n@@ -6,35 +6,40 @@\n - 01KS5F8THM5EQMKFSF6GFAE55C\n position_column: todo\n position_ordinal: 9d80\n-project: command-service\n-title: 'Frontend: subscribe to MCP change notifications, retire Tauri app.emit for changes'\n+project: command-events\n+title: 'Frontend: subscribe to MCP change notifications (reuse reducer + add txn batching)'\n ---\n ## What\n\n-Migrate the kanban webview from Tauri-specific change events to the MCP notification surface, so the UI is just another MCP client (same stream agents receive). Today React listens via Tauri `listen(\"entity-field-changed\", …)` / `listen(\"board-changed\", …)` etc., fed by `app.emit(...)` in `apps/kanban-app/src/commands.rs`. Replace with an MCP notification subscription.\n+Migrate the kanban webview from Tauri-specific change events to the MCP notification surface, so the UI is just another MCP client (same stream agents receive). The existing entity/field reload reducer is solid — **keep it; only change its input source** from Tauri `listen(\"entity-field-changed\", …)` to the MCP `notifications/store/changed` subscription. Add transaction batching so a command's N changes apply as one atomic re-render.\n\n-Files:\n-- `apps/kanban-app/ui/src/...` data layer — subscribe to the MCP notification stream (`notifications/entity/changed`, `notifications/entity/deleted`, `notifications/attachment/changed`, `notifications/store/undo_changed`); apply the thin `{field,value}` patches to local state exactly as the existing Tauri listeners do (the payloads are the same thin events, so the reducer logic is reused, only the source changes)\n-- Replace `listen(\"entity-field-changed\"|\"board-changed\"|\"attachment-changed\"|...)` call sites with the MCP subscription\n-- `apps/kanban-app/src/commands.rs` — remove the `app.emit(...)` change-event calls (entity bridge, `board-changed`) once the MCP path delivers them; keep only genuinely Tauri-shell emits that aren't change events (e.g. native dialogs), if any remain\n-- Undo/Redo controls subscribe to `notifications/store/undo_changed` for `can_undo`/`can_redo`/labels (from the change-propagation task)\n+Files (`apps/kanban-app/ui/src/...`):\n+- Data layer — subscribe to the MCP notification planes and route into the **existing reducer**:\n+  - `notifications/store/changed { store, item, op, changes?, txn, origin }` → apply field patches (entities) or reload item (views/perspectives) using today's reducer logic, unchanged\n+  - `notifications/store/undo_changed` → Undo/Redo control state\n+  - `notifications/ui_state/changed` → UI state\n+  - `notifications/commands/changed` → palette/menu refresh (already wired in the dispatch task)\n+- **Txn batching**: buffer `store/changed` events by `txn` and flush as one atomic state update per transaction, so a multi-write command (or an undo of one) re-renders once, not N times. A short coalescing window keyed on `txn`; flush on txn change or a microtask tick.\n+- Remove the Tauri `listen(\"entity-field-changed\"|\"board-changed\"|\"attachment-changed\"|...)` call sites\n+- `apps/kanban-app/src/commands.rs` — remove the `app.emit(...)` change-event calls (handled in the cut-over Tauri-migration task if any remain)\n\n-Memory `metadata-driven-ui` / `frontend-logging` still apply. The reducer that applies thin patches does not change — only the transport (MCP notification instead of Tauri event).\n+Memory `metadata-driven-ui`, `frontend-logging` apply. The reducer that applies thin patches does NOT change — only the source (MCP) and the batching wrapper.\n\n ## Acceptance Criteria\n-- [ ] The webview receives entity/attachment/board change notifications over MCP, not Tauri `app.emit`\n-- [ ] `app.emit(...)` change-event calls in `apps/kanban-app/src/commands.rs` are removed (only non-change shell emits, if any, remain)\n-- [ ] An edit (from UI, agent, or undo) re-renders the board via the MCP notification path\n-- [ ] Undo/Redo buttons reflect `notifications/store/undo_changed`\n+- [ ] The webview receives all change planes over MCP, not Tauri `app.emit`\n+- [ ] The existing entity/field reducer is reused unchanged; only its input source changed\n+- [ ] A command's N `store/changed` events sharing a `txn` apply as ONE atomic re-render (txn batching)\n+- [ ] Undo/Redo controls reflect `notifications/store/undo_changed`\n - [ ] No React code path still depends on `listen(\"entity-field-changed\")` and friends\n\n ## Tests\n-- [ ] `apps/kanban-app/ui/src/.../mcp-notifications.test.tsx` — mock the MCP transport; emit `notifications/entity/changed` with a `{field,value}` payload; assert the board state patches identically to the old Tauri path; emit `entity/deleted` → card removed\n-- [ ] `apps/kanban-app/ui/src/.../undo-controls.test.tsx` — emit `notifications/store/undo_changed { can_undo:false, can_redo:true }`; assert Undo disabled, Redo enabled\n-- [ ] A code-quality grep test asserts no `listen(\"entity-field-changed\"|\"board-changed\"|...)` remains in the UI source\n+- [ ] `apps/kanban-app/ui/src/.../mcp-notifications.test.tsx` — emit `store/changed` with `{field,value}`; assert the board patches identically to the old Tauri path (reducer reused); emit `op:\"removed\"` → card removed\n+- [ ] `apps/kanban-app/ui/src/.../txn-batching.test.tsx` — emit 3 `store/changed` sharing one `txn`; assert exactly one render flush; a 4th with a new `txn` flushes separately\n+- [ ] `apps/kanban-app/ui/src/.../undo-controls.test.tsx` — emit `undo_changed { can_undo:false, can_redo:true }`; assert Undo disabled, Redo enabled\n+- [ ] grep test: no `listen(\"entity-field-changed\"|\"board-changed\"|...)` remains\n - [ ] `npm test --prefix apps/kanban-app/ui` passes\n\n ## Workflow\n-- Use `/tdd` — write the notification-reducer test first; it proves the thin-patch logic is reused unchanged.\n+- Use `/tdd` — write the txn-batching test first; it pins the atomic-per-command-render contract while proving the reducer is reused.\n\n-Depends on the MCP notification surface task and the change-propagation task.\n\\ No newline at end of file\n+Depends on the MCP notification surface + change-propagation tasks.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -6,40 +6,35 @@\n - 01KS5F8THM5EQMKFSF6GFAE55C\n position_column: todo\n position_ordinal: 9d80\n-project: command-events\n-title: 'Frontend: subscribe to MCP change notifications (reuse reducer + add txn batching)'\n+project: command-service\n+title: 'Frontend: subscribe to MCP change notifications, retire Tauri app.emit for changes'\n ---\n ## What\n\n-Migrate the kanban webview from Tauri-specific change events to the MCP notification surface, so the UI is just another MCP client (same stream agents receive). The existing entity/field reload reducer is solid — **keep it; only change its input source** from Tauri `listen(\"entity-field-changed\", …)` to the MCP `notifications/store/changed` subscription. Add transaction batching so a command's N changes apply as one atomic re-render.\n+Migrate the kanban webview from Tauri-specific change events to the MCP notification surface, so the UI is just another MCP client (same stream agents receive). Today React listens via Tauri `listen(\"entity-field-changed\", …)` / `listen(\"board-changed\", …)` etc., fed by `app.emit(...)` in `apps/kanban-app/src/commands.rs`. Replace with an MCP notification subscription.\n\n-Files (`apps/kanban-app/ui/src/...`):\n-- Data layer — subscribe to the MCP notification planes and route into the **existing reducer**:\n-  - `notifications/store/changed { store, item, op, changes?, txn, origin }` → apply field patches (entities) or reload item (views/perspectives) using today's reducer logic, unchanged\n-  - `notifications/store/undo_changed` → Undo/Redo control state\n-  - `notifications/ui_state/changed` → UI state\n-  - `notifications/commands/changed` → palette/menu refresh (already wired in the dispatch task)\n-- **Txn batching**: buffer `store/changed` events by `txn` and flush as one atomic state update per transaction, so a multi-write command (or an undo of one) re-renders once, not N times. A short coalescing window keyed on `txn`; flush on txn change or a microtask tick.\n-- Remove the Tauri `listen(\"entity-field-changed\"|\"board-changed\"|\"attachment-changed\"|...)` call sites\n-- `apps/kanban-app/src/commands.rs` — remove the `app.emit(...)` change-event calls (handled in the cut-over Tauri-migration task if any remain)\n+Files:\n+- `apps/kanban-app/ui/src/...` data layer — subscribe to the MCP notification stream (`notifications/entity/changed`, `notifications/entity/deleted`, `notifications/attachment/changed`, `notifications/store/undo_changed`); apply the thin `{field,value}` patches to local state exactly as the existing Tauri listeners do (the payloads are the same thin events, so the reducer logic is reused, only the source changes)\n+- Replace `listen(\"entity-field-changed\"|\"board-changed\"|\"attachment-changed\"|...)` call sites with the MCP subscription\n+- `apps/kanban-app/src/commands.rs` — remove the `app.emit(...)` change-event calls (entity bridge, `board-changed`) once the MCP path delivers them; keep only genuinely Tauri-shell emits that aren't change events (e.g. native dialogs), if any remain\n+- Undo/Redo controls subscribe to `notifications/store/undo_changed` for `can_undo`/`can_redo`/labels (from the change-propagation task)\n\n-Memory `metadata-driven-ui`, `frontend-logging` apply. The reducer that applies thin patches does NOT change — only the source (MCP) and the batching wrapper.\n+Memory `metadata-driven-ui` / `frontend-logging` still apply. The reducer that applies thin patches does not change — only the transport (MCP notification instead of Tauri event).\n\n ## Acceptance Criteria\n-- [ ] The webview receives all change planes over MCP, not Tauri `app.emit`\n-- [ ] The existing entity/field reducer is reused unchanged; only its input source changed\n-- [ ] A command's N `store/changed` events sharing a `txn` apply as ONE atomic re-render (txn batching)\n-- [ ] Undo/Redo controls reflect `notifications/store/undo_changed`\n+- [ ] The webview receives entity/attachment/board change notifications over MCP, not Tauri `app.emit`\n+- [ ] `app.emit(...)` change-event calls in `apps/kanban-app/src/commands.rs` are removed (only non-change shell emits, if any, remain)\n+- [ ] An edit (from UI, agent, or undo) re-renders the board via the MCP notification path\n+- [ ] Undo/Redo buttons reflect `notifications/store/undo_changed`\n - [ ] No React code path still depends on `listen(\"entity-field-changed\")` and friends\n\n ## Tests\n-- [ ] `apps/kanban-app/ui/src/.../mcp-notifications.test.tsx` — emit `store/changed` with `{field,value}`; assert the board patches identically to the old Tauri path (reducer reused); emit `op:\"removed\"` → card removed\n-- [ ] `apps/kanban-app/ui/src/.../txn-batching.test.tsx` — emit 3 `store/changed` sharing one `txn`; assert exactly one render flush; a 4th with a new `txn` flushes separately\n-- [ ] `apps/kanban-app/ui/src/.../undo-controls.test.tsx` — emit `undo_changed { can_undo:false, can_redo:true }`; assert Undo disabled, Redo enabled\n-- [ ] grep test: no `listen(\"entity-field-changed\"|\"board-changed\"|...)` remains\n+- [ ] `apps/kanban-app/ui/src/.../mcp-notifications.test.tsx` — mock the MCP transport; emit `notifications/entity/changed` with a `{field,value}` payload; assert the board state patches identically to the old Tauri path; emit `entity/deleted` → card removed\n+- [ ] `apps/kanban-app/ui/src/.../undo-controls.test.tsx` — emit `notifications/store/undo_changed { can_undo:false, can_redo:true }`; assert Undo disabled, Redo enabled\n+- [ ] A code-quality grep test asserts no `listen(\"entity-field-changed\"|\"board-changed\"|...)` remains in the UI source\n - [ ] `npm test --prefix apps/kanban-app/ui` passes\n\n ## Workflow\n-- Use `/tdd` — write the txn-batching test first; it pins the atomic-per-command-render contract while proving the reducer is reused.\n+- Use `/tdd` — write the notification-reducer test first; it proves the thin-patch logic is reused unchanged.\n\n-Depends on the MCP notification surface + change-propagation tasks.\n\\ No newline at end of file\n+Depends on the MCP notification surface task and the change-propagation task.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSR7V5XXSRDGFBBX92Z1FQTJ","timestamp":"2026-05-28T21:28:59.581883Z","op":"update","item_id":"01KS5G3S1MR6Y77RXPHZP4SZB1","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KS5G3AKZXDN7K6YR415E0V4K\n - 01KS5F8THM5EQMKFSF6GFAE55C\n-position_column: todo\n-position_ordinal: 9d80\n+position_column: doing\n+position_ordinal: '80'\n project: command-events\n title: 'Frontend: subscribe to MCP change notifications (reuse reducer + add txn batching)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KS5G3AKZXDN7K6YR415E0V4K\n - 01KS5F8THM5EQMKFSF6GFAE55C\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: 9d80\n project: command-events\n title: 'Frontend: subscribe to MCP change notifications (reuse reducer + add txn batching)'\n ---\n","transaction_id":null}

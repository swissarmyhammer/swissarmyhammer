{"id":"01KPNWMXGS5RRZPFEAGW82HFC2","timestamp":"2026-04-20T16:47:45.689351Z","op":"create","item_id":"01KPNWMXGSRDV29QP1D6Z6ED8D","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,50 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: e580\n+project: spatial-nav\n+title: 'Layout: board columns don''t fill the available vertical space'\n+---\n+## What\n+\n+In the board view, each column's card area visually occupies only the bottom portion of the space between the column header and the bottom of the viewport — reported by user: \"columns are not 'filling' the column, it's like just the bottom half of the visible column space.\"\n+\n+This is a pure layout / CSS bug, unrelated to spatial navigation. It does affect spatial nav indirectly because a short column area means fewer visible card rects for the beam test, so nav below visible range scrolls less smoothly than it should.\n+\n+### Reproduction\n+\n+1. Open a board with 3+ cards per column.\n+2. Observe that the card list is bottom-aligned within the column — empty space above the first card, no empty space below the last card.\n+3. Compare to the expected behavior: card list is top-aligned, cards stack from the column header downward, empty space is below.\n+\n+### TDD — failing test\n+\n+`kanban-app/ui/src/components/board-view.test.tsx` (or a new `board-view.layout.test.tsx`):\n+\n+```tsx\n+it(\"column card list starts at the top of the column, not the bottom\", () => {\n+  const { container } = render(<BoardViewWithColumns cards={[c1, c2, c3]} />);\n+  const column = container.querySelector('[data-testid=\"column-body\"]') as HTMLElement;\n+  const firstCard = column.querySelector('[data-testid^=\"card:\"]') as HTMLElement;\n+  const columnRect = column.getBoundingClientRect();\n+  const cardRect = firstCard.getBoundingClientRect();\n+  // First card's top should be within ~8px (column padding) of the column's top.\n+  expect(cardRect.top - columnRect.top).toBeLessThan(16);\n+});\n+```\n+\n+Or better: jsdom can't reliably measure flexbox, so consider a CSS smoke — use `getComputedStyle` to assert the column body has `flex-direction: column` and `justify-content: flex-start` (or no `flex-end`).\n+\n+### Likely cause (not verified)\n+\n+Something in the column's flex container is setting `justify-content: flex-end` or similar, making children pile to the bottom. Or the card list has `margin-top: auto` somewhere.\n+\n+Grep `board-view.tsx`, `column-view.tsx`, and their CSS classes for `flex-end`, `end`, `margin-top: auto`, `items-end`, etc. This is a small, localized fix.\n+\n+### Acceptance\n+\n+- [ ] Failing test that asserts top-alignment of the first card in a column\n+- [ ] Fix: adjust the flex/layout class so cards start at the top of the column body\n+- [ ] Manual verification that the board looks right in the running app\n+- [ ] No regression in column header position or drag-and-drop drop zones\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,50 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: e580\n-project: spatial-nav\n-title: 'Layout: board columns don''t fill the available vertical space'\n----\n-## What\n-\n-In the board view, each column's card area visually occupies only the bottom portion of the space between the column header and the bottom of the viewport — reported by user: \"columns are not 'filling' the column, it's like just the bottom half of the visible column space.\"\n-\n-This is a pure layout / CSS bug, unrelated to spatial navigation. It does affect spatial nav indirectly because a short column area means fewer visible card rects for the beam test, so nav below visible range scrolls less smoothly than it should.\n-\n-### Reproduction\n-\n-1. Open a board with 3+ cards per column.\n-2. Observe that the card list is bottom-aligned within the column — empty space above the first card, no empty space below the last card.\n-3. Compare to the expected behavior: card list is top-aligned, cards stack from the column header downward, empty space is below.\n-\n-### TDD — failing test\n-\n-`kanban-app/ui/src/components/board-view.test.tsx` (or a new `board-view.layout.test.tsx`):\n-\n-```tsx\n-it(\"column card list starts at the top of the column, not the bottom\", () => {\n-  const { container } = render(<BoardViewWithColumns cards={[c1, c2, c3]} />);\n-  const column = container.querySelector('[data-testid=\"column-body\"]') as HTMLElement;\n-  const firstCard = column.querySelector('[data-testid^=\"card:\"]') as HTMLElement;\n-  const columnRect = column.getBoundingClientRect();\n-  const cardRect = firstCard.getBoundingClientRect();\n-  // First card's top should be within ~8px (column padding) of the column's top.\n-  expect(cardRect.top - columnRect.top).toBeLessThan(16);\n-});\n-```\n-\n-Or better: jsdom can't reliably measure flexbox, so consider a CSS smoke — use `getComputedStyle` to assert the column body has `flex-direction: column` and `justify-content: flex-start` (or no `flex-end`).\n-\n-### Likely cause (not verified)\n-\n-Something in the column's flex container is setting `justify-content: flex-end` or similar, making children pile to the bottom. Or the card list has `margin-top: auto` somewhere.\n-\n-Grep `board-view.tsx`, `column-view.tsx`, and their CSS classes for `flex-end`, `end`, `margin-top: auto`, `items-end`, etc. This is a small, localized fix.\n-\n-### Acceptance\n-\n-- [ ] Failing test that asserts top-alignment of the first card in a column\n-- [ ] Fix: adjust the flex/layout class so cards start at the top of the column body\n-- [ ] Manual verification that the board looks right in the running app\n-- [ ] No regression in column header position or drag-and-drop drop zones\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPNWMXGS0F2H7671HYM9X3RM","timestamp":"2026-04-20T16:47:45.689706Z","entity_type":"task","entity_id":"01KPNWMXGSRDV29QP1D6Z6ED8D","op":"create","changes":[["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"e580"}],["assignees",{"kind":"set","value":["claude-code"]}],["project",{"kind":"set","value":"spatial-nav"}],["title",{"kind":"set","value":"Layout: board columns don't fill the available vertical space"}],["body",{"kind":"set","value":"## What\n\nIn the board view, each column's card area visually occupies only the bottom portion of the space between the column header and the bottom of the viewport — reported by user: \"columns are not 'filling' the column, it's like just the bottom half of the visible column space.\"\n\nThis is a pure layout / CSS bug, unrelated to spatial navigation. It does affect spatial nav indirectly because a short column area means fewer visible card rects for the beam test, so nav below visible range scrolls less smoothly than it should.\n\n### Reproduction\n\n1. Open a board with 3+ cards per column.\n2. Observe that the card list is bottom-aligned within the column — empty space above the first card, no empty space below the last card.\n3. Compare to the expected behavior: card list is top-aligned, cards stack from the column header downward, empty space is below.\n\n### TDD — failing test\n\n`kanban-app/ui/src/components/board-view.test.tsx` (or a new `board-view.layout.test.tsx`):\n\n```tsx\nit(\"column card list starts at the top of the column, not the bottom\", () => {\n  const { container } = render(<BoardViewWithColumns cards={[c1, c2, c3]} />);\n  const column = container.querySelector('[data-testid=\"column-body\"]') as HTMLElement;\n  const firstCard = column.querySelector('[data-testid^=\"card:\"]') as HTMLElement;\n  const columnRect = column.getBoundingClientRect();\n  const cardRect = firstCard.getBoundingClientRect();\n  // First card's top should be within ~8px (column padding) of the column's top.\n  expect(cardRect.top - columnRect.top).toBeLessThan(16);\n});\n```\n\nOr better: jsdom can't reliably measure flexbox, so consider a CSS smoke — use `getComputedStyle` to assert the column body has `flex-direction: column` and `justify-content: flex-start` (or no `flex-end`).\n\n### Likely cause (not verified)\n\nSomething in the column's flex container is setting `justify-content: flex-end` or similar, making children pile to the bottom. Or the card list has `margin-top: auto` somewhere.\n\nGrep `board-view.tsx`, `column-view.tsx`, and their CSS classes for `flex-end`, `end`, `margin-top: auto`, `items-end`, etc. This is a small, localized fix.\n\n### Acceptance\n\n- [ ] Failing test that asserts top-alignment of the first card in a column\n- [ ] Fix: adjust the flex/layout class so cards start at the top of the column body\n- [ ] Manual verification that the board looks right in the running app\n- [ ] No regression in column header position or drag-and-drop drop zones"}]]}
{"id":"01KPNYRK3S0VABX244KV4RGMZC","timestamp":"2026-04-20T17:24:43.257506Z","op":"update","item_id":"01KPNWMXGSRDV29QP1D6Z6ED8D","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: e580\n+position_column: doing\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Layout: board columns don''t fill the available vertical space'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: todo\n+position_ordinal: e580\n project: spatial-nav\n title: 'Layout: board columns don''t fill the available vertical space'\n ---\n","transaction_id":null}
{"id":"01KPNYRK4EYKVXJE2B2G0ZXV49","timestamp":"2026-04-20T17:24:43.278890Z","entity_type":"task","entity_id":"01KPNWMXGSRDV29QP1D6Z6ED8D","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-e580\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+e580\n\\ No newline at end of file\n"}]]}
{"id":"01KPNZA787F87MKKS2KGMC3QQ2","timestamp":"2026-04-20T17:34:20.935765Z","op":"update","item_id":"01KPNWMXGSRDV29QP1D6Z6ED8D","forward_patch":"--- original\n+++ modified\n@@ -18,33 +18,27 @@\n 2. Observe that the card list is bottom-aligned within the column — empty space above the first card, no empty space below the last card.\n 3. Compare to the expected behavior: card list is top-aligned, cards stack from the column header downward, empty space is below.\n\n-### TDD — failing test\n+### Root cause\n\n-`kanban-app/ui/src/components/board-view.test.tsx` (or a new `board-view.layout.test.tsx`):\n+`ColumnHeader` in `kanban-app/ui/src/components/column-view.tsx` wrapped its single header row in a redundant `<div className=\"flex flex-col min-h-0 min-w-0 flex-1\">`. Because that wrapper carried `flex-1`, it became a flex sibling of the card list (`VirtualizedCardList`, also `flex-1`) inside the column's `flex flex-col` scope. The two `flex-1` siblings split the column's vertical space 50/50, leaving the card list pinned to the bottom half.\n+\n+The wrapper was vestigial from commit 6684a43e3 — before ColumnHeader was extracted into its own function, the enclosing div contained both the header and the card list, so `flex-1` on it made sense. After the extraction the wrapper served no purpose and caused the split.\n\n-```tsx\n-it(\"column card list starts at the top of the column, not the bottom\", () => {\n-  const { container } = render(<BoardViewWithColumns cards={[c1, c2, c3]} />);\n-  const column = container.querySelector('[data-testid=\"column-body\"]') as HTMLElement;\n-  const firstCard = column.querySelector('[data-testid^=\"card:\"]') as HTMLElement;\n-  const columnRect = column.getBoundingClientRect();\n-  const cardRect = firstCard.getBoundingClientRect();\n-  // First card's top should be within ~8px (column padding) of the column's top.\n-  expect(cardRect.top - columnRect.top).toBeLessThan(16);\n-});\n-```\n+### Fix\n\n-Or better: jsdom can't reliably measure flexbox, so consider a CSS smoke — use `getComputedStyle` to assert the column body has `flex-direction: column` and `justify-content: flex-start` (or no `flex-end`).\n+Remove the `flex-col flex-1` wrapper entirely; let the `.column-header-focus` row be a direct child of the column's FocusScope container. It sizes to its content height, and the card list's `flex-1` then claims all remaining vertical space.\n\n-### Likely cause (not verified)\n+### Acceptance\n\n-Something in the column's flex container is setting `justify-content: flex-end` or similar, making children pile to the bottom. Or the card list has `margin-top: auto` somewhere.\n+- [x] Failing test that asserts top-alignment of the first card in a column\n+- [x] Fix: adjust the flex/layout class so cards start at the top of the column body\n+- [x] Manual verification that the board looks right in the running app\n+- [x] No regression in column header position or drag-and-drop drop zones\n\n-Grep `board-view.tsx`, `column-view.tsx`, and their CSS classes for `flex-end`, `end`, `margin-top: auto`, `items-end`, etc. This is a small, localized fix.\n+### Tests\n\n-### Acceptance\n+Added `describe(\"ColumnView layout\")` in `kanban-app/ui/src/components/column-view.test.tsx`:\n+- `card list is the only flex-1 child of the column scope` — asserts structural invariant\n+- `column header row is a direct child of the column scope` — confirms the wrapper is gone\n\n-- [ ] Failing test that asserts top-alignment of the first card in a column\n-- [ ] Fix: adjust the flex/layout class so cards start at the top of the column body\n-- [ ] Manual verification that the board looks right in the running app\n-- [ ] No regression in column header position or drag-and-drop drop zones\n\\ No newline at end of file\n+Both tests fail against the old layout and pass against the fix. Full UI test suite (1268 tests) passes.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -18,27 +18,33 @@\n 2. Observe that the card list is bottom-aligned within the column — empty space above the first card, no empty space below the last card.\n 3. Compare to the expected behavior: card list is top-aligned, cards stack from the column header downward, empty space is below.\n\n-### Root cause\n+### TDD — failing test\n\n-`ColumnHeader` in `kanban-app/ui/src/components/column-view.tsx` wrapped its single header row in a redundant `<div className=\"flex flex-col min-h-0 min-w-0 flex-1\">`. Because that wrapper carried `flex-1`, it became a flex sibling of the card list (`VirtualizedCardList`, also `flex-1`) inside the column's `flex flex-col` scope. The two `flex-1` siblings split the column's vertical space 50/50, leaving the card list pinned to the bottom half.\n+`kanban-app/ui/src/components/board-view.test.tsx` (or a new `board-view.layout.test.tsx`):\n\n-The wrapper was vestigial from commit 6684a43e3 — before ColumnHeader was extracted into its own function, the enclosing div contained both the header and the card list, so `flex-1` on it made sense. After the extraction the wrapper served no purpose and caused the split.\n+```tsx\n+it(\"column card list starts at the top of the column, not the bottom\", () => {\n+  const { container } = render(<BoardViewWithColumns cards={[c1, c2, c3]} />);\n+  const column = container.querySelector('[data-testid=\"column-body\"]') as HTMLElement;\n+  const firstCard = column.querySelector('[data-testid^=\"card:\"]') as HTMLElement;\n+  const columnRect = column.getBoundingClientRect();\n+  const cardRect = firstCard.getBoundingClientRect();\n+  // First card's top should be within ~8px (column padding) of the column's top.\n+  expect(cardRect.top - columnRect.top).toBeLessThan(16);\n+});\n+```\n\n-### Fix\n+Or better: jsdom can't reliably measure flexbox, so consider a CSS smoke — use `getComputedStyle` to assert the column body has `flex-direction: column` and `justify-content: flex-start` (or no `flex-end`).\n\n-Remove the `flex-col flex-1` wrapper entirely; let the `.column-header-focus` row be a direct child of the column's FocusScope container. It sizes to its content height, and the card list's `flex-1` then claims all remaining vertical space.\n+### Likely cause (not verified)\n\n-### Acceptance\n+Something in the column's flex container is setting `justify-content: flex-end` or similar, making children pile to the bottom. Or the card list has `margin-top: auto` somewhere.\n\n-- [x] Failing test that asserts top-alignment of the first card in a column\n-- [x] Fix: adjust the flex/layout class so cards start at the top of the column body\n-- [x] Manual verification that the board looks right in the running app\n-- [x] No regression in column header position or drag-and-drop drop zones\n-\n-### Tests\n+Grep `board-view.tsx`, `column-view.tsx`, and their CSS classes for `flex-end`, `end`, `margin-top: auto`, `items-end`, etc. This is a small, localized fix.\n\n-Added `describe(\"ColumnView layout\")` in `kanban-app/ui/src/components/column-view.test.tsx`:\n-- `card list is the only flex-1 child of the column scope` — asserts structural invariant\n-- `column header row is a direct child of the column scope` — confirms the wrapper is gone\n+### Acceptance\n\n-Both tests fail against the old layout and pass against the fix. Full UI test suite (1268 tests) passes.\n\\ No newline at end of file\n+- [ ] Failing test that asserts top-alignment of the first card in a column\n+- [ ] Fix: adjust the flex/layout class so cards start at the top of the column body\n+- [ ] Manual verification that the board looks right in the running app\n+- [ ] No regression in column header position or drag-and-drop drop zones\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPNZA7885MGHPBBNC3SC4XWH","timestamp":"2026-04-20T17:34:20.936327Z","entity_type":"task","entity_id":"01KPNWMXGSRDV29QP1D6Z6ED8D","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -10,33 +10,27 @@\n 2. Observe that the card list is bottom-aligned within the column — empty space above the first card, no empty space below the last card.\n 3. Compare to the expected behavior: card list is top-aligned, cards stack from the column header downward, empty space is below.\n\n-### TDD — failing test\n+### Root cause\n\n-`kanban-app/ui/src/components/board-view.test.tsx` (or a new `board-view.layout.test.tsx`):\n+`ColumnHeader` in `kanban-app/ui/src/components/column-view.tsx` wrapped its single header row in a redundant `<div className=\"flex flex-col min-h-0 min-w-0 flex-1\">`. Because that wrapper carried `flex-1`, it became a flex sibling of the card list (`VirtualizedCardList`, also `flex-1`) inside the column's `flex flex-col` scope. The two `flex-1` siblings split the column's vertical space 50/50, leaving the card list pinned to the bottom half.\n+\n+The wrapper was vestigial from commit 6684a43e3 — before ColumnHeader was extracted into its own function, the enclosing div contained both the header and the card list, so `flex-1` on it made sense. After the extraction the wrapper served no purpose and caused the split.\n\n-```tsx\n-it(\"column card list starts at the top of the column, not the bottom\", () => {\n-  const { container } = render(<BoardViewWithColumns cards={[c1, c2, c3]} />);\n-  const column = container.querySelector('[data-testid=\"column-body\"]') as HTMLElement;\n-  const firstCard = column.querySelector('[data-testid^=\"card:\"]') as HTMLElement;\n-  const columnRect = column.getBoundingClientRect();\n-  const cardRect = firstCard.getBoundingClientRect();\n-  // First card's top should be within ~8px (column padding) of the column's top.\n-  expect(cardRect.top - columnRect.top).toBeLessThan(16);\n-});\n-```\n+### Fix\n\n-Or better: jsdom can't reliably measure flexbox, so consider a CSS smoke — use `getComputedStyle` to assert the column body has `flex-direction: column` and `justify-content: flex-start` (or no `flex-end`).\n+Remove the `flex-col flex-1` wrapper entirely; let the `.column-header-focus` row be a direct child of the column's FocusScope container. It sizes to its content height, and the card list's `flex-1` then claims all remaining vertical space.\n\n-### Likely cause (not verified)\n+### Acceptance\n\n-Something in the column's flex container is setting `justify-content: flex-end` or similar, making children pile to the bottom. Or the card list has `margin-top: auto` somewhere.\n+- [x] Failing test that asserts top-alignment of the first card in a column\n+- [x] Fix: adjust the flex/layout class so cards start at the top of the column body\n+- [x] Manual verification that the board looks right in the running app\n+- [x] No regression in column header position or drag-and-drop drop zones\n\n-Grep `board-view.tsx`, `column-view.tsx`, and their CSS classes for `flex-end`, `end`, `margin-top: auto`, `items-end`, etc. This is a small, localized fix.\n+### Tests\n\n-### Acceptance\n+Added `describe(\"ColumnView layout\")` in `kanban-app/ui/src/components/column-view.test.tsx`:\n+- `card list is the only flex-1 child of the column scope` — asserts structural invariant\n+- `column header row is a direct child of the column scope` — confirms the wrapper is gone\n\n-- [ ] Failing test that asserts top-alignment of the first card in a column\n-- [ ] Fix: adjust the flex/layout class so cards start at the top of the column body\n-- [ ] Manual verification that the board looks right in the running app\n-- [ ] No regression in column header position or drag-and-drop drop zones\n\\ No newline at end of file\n+Both tests fail against the old layout and pass against the fix. Full UI test suite (1268 tests) passes.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -10,27 +10,33 @@\n 2. Observe that the card list is bottom-aligned within the column — empty space above the first card, no empty space below the last card.\n 3. Compare to the expected behavior: card list is top-aligned, cards stack from the column header downward, empty space is below.\n\n-### Root cause\n+### TDD — failing test\n\n-`ColumnHeader` in `kanban-app/ui/src/components/column-view.tsx` wrapped its single header row in a redundant `<div className=\"flex flex-col min-h-0 min-w-0 flex-1\">`. Because that wrapper carried `flex-1`, it became a flex sibling of the card list (`VirtualizedCardList`, also `flex-1`) inside the column's `flex flex-col` scope. The two `flex-1` siblings split the column's vertical space 50/50, leaving the card list pinned to the bottom half.\n+`kanban-app/ui/src/components/board-view.test.tsx` (or a new `board-view.layout.test.tsx`):\n\n-The wrapper was vestigial from commit 6684a43e3 — before ColumnHeader was extracted into its own function, the enclosing div contained both the header and the card list, so `flex-1` on it made sense. After the extraction the wrapper served no purpose and caused the split.\n+```tsx\n+it(\"column card list starts at the top of the column, not the bottom\", () => {\n+  const { container } = render(<BoardViewWithColumns cards={[c1, c2, c3]} />);\n+  const column = container.querySelector('[data-testid=\"column-body\"]') as HTMLElement;\n+  const firstCard = column.querySelector('[data-testid^=\"card:\"]') as HTMLElement;\n+  const columnRect = column.getBoundingClientRect();\n+  const cardRect = firstCard.getBoundingClientRect();\n+  // First card's top should be within ~8px (column padding) of the column's top.\n+  expect(cardRect.top - columnRect.top).toBeLessThan(16);\n+});\n+```\n\n-### Fix\n+Or better: jsdom can't reliably measure flexbox, so consider a CSS smoke — use `getComputedStyle` to assert the column body has `flex-direction: column` and `justify-content: flex-start` (or no `flex-end`).\n\n-Remove the `flex-col flex-1` wrapper entirely; let the `.column-header-focus` row be a direct child of the column's FocusScope container. It sizes to its content height, and the card list's `flex-1` then claims all remaining vertical space.\n+### Likely cause (not verified)\n\n-### Acceptance\n+Something in the column's flex container is setting `justify-content: flex-end` or similar, making children pile to the bottom. Or the card list has `margin-top: auto` somewhere.\n\n-- [x] Failing test that asserts top-alignment of the first card in a column\n-- [x] Fix: adjust the flex/layout class so cards start at the top of the column body\n-- [x] Manual verification that the board looks right in the running app\n-- [x] No regression in column header position or drag-and-drop drop zones\n-\n-### Tests\n+Grep `board-view.tsx`, `column-view.tsx`, and their CSS classes for `flex-end`, `end`, `margin-top: auto`, `items-end`, etc. This is a small, localized fix.\n\n-Added `describe(\"ColumnView layout\")` in `kanban-app/ui/src/components/column-view.test.tsx`:\n-- `card list is the only flex-1 child of the column scope` — asserts structural invariant\n-- `column header row is a direct child of the column scope` — confirms the wrapper is gone\n+### Acceptance\n\n-Both tests fail against the old layout and pass against the fix. Full UI test suite (1268 tests) passes.\n\\ No newline at end of file\n+- [ ] Failing test that asserts top-alignment of the first card in a column\n+- [ ] Fix: adjust the flex/layout class so cards start at the top of the column body\n+- [ ] Manual verification that the board looks right in the running app\n+- [ ] No regression in column header position or drag-and-drop drop zones\n\\ No newline at end of file\n"}]]}
{"id":"01KPNZAQFK7P8B86JAT6THJZNC","timestamp":"2026-04-20T17:34:37.555744Z","op":"update","item_id":"01KPNWMXGSRDV29QP1D6Z6ED8D","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '8680'\n project: spatial-nav\n title: 'Layout: board columns don''t fill the available vertical space'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8680'\n+position_column: doing\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Layout: board columns don''t fill the available vertical space'\n ---\n","transaction_id":null}
{"id":"01KPNZAQFMZP2KW9QGNT0P030G","timestamp":"2026-04-20T17:34:37.556251Z","entity_type":"task","entity_id":"01KPNWMXGSRDV29QP1D6Z6ED8D","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+8680\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8680\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KPP0EG71SVHGTVCVN2Z7T1VA","timestamp":"2026-04-20T17:54:09.761290Z","op":"update","item_id":"01KPNWMXGSRDV29QP1D6Z6ED8D","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8680'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffee80\n project: spatial-nav\n title: 'Layout: board columns don''t fill the available vertical space'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffee80\n+position_column: review\n+position_ordinal: '8680'\n project: spatial-nav\n title: 'Layout: board columns don''t fill the available vertical space'\n ---\n","transaction_id":null}
{"id":"01KPP0EG71XAW39CTVJ5FAX1MM","timestamp":"2026-04-20T17:54:09.761900Z","entity_type":"task","entity_id":"01KPNWMXGSRDV29QP1D6Z6ED8D","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8680\n\\ No newline at end of file\n+ffffffffffffffffffffffee80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffee80\n\\ No newline at end of file\n+8680\n\\ No newline at end of file\n"}]]}

{"id":"01KQ7K7KZN3NQWE19ZPDKW6Z2C","timestamp":"2026-04-27T13:49:32.533942Z","op":"create","item_id":"01KQ7K7KZNR3EHS9SY0XY79NYE","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,172 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KQ7GM77B1E6YH8Z893K05VKY\n+position_column: todo\n+position_ordinal: a880\n+project: spatial-nav\n+title: 'Refactor: replace inspectOnDoubleClick prop with &lt;Inspectable&gt; wrapper component'\n+---\n+## What\n+\n+Once the `inspectOnDoubleClick?: boolean` fix from `01KQ7GM77B1E6YH8Z893K05VKY` lands and stops the perspective-tab-opens-inspector bug, replace that boolean prop with a dedicated `<Inspectable>` wrapper component. The boolean is the right user-visible fix; the wrapper is the right architecture.\n+\n+### Why this is its own task\n+\n+The boolean prop on `<FocusScope>` / `<FocusZone>` works, but it has design smells that the wrapper-component approach eliminates:\n+\n+- **Domain leakage.** `<FocusScope>` and `<FocusZone>` are generic spatial primitives. Carrying an `inspectOnDoubleClick` prop means the primitive imports `useDispatchCommand(\"ui.inspect\")` — kanban-domain knowledge — into the spatial-nav infrastructure.\n+- **Implicit naming.** `inspectOnDoubleClick={true}` on a `<FocusScope moniker=\"task:01\">` is a flag whose meaning is buried in conventions. `<Inspectable moniker=\"task:01\">` *names* the architectural concept (\"this DOM subtree is an inspectable entity\").\n+- **Composability.** A future change that needs both inspect-on-dblclick and a focus zone (e.g. an editable card) gets `<Inspectable><FocusZone>…</FocusZone></Inspectable>` cleanly. The boolean has to be duplicated on both `<FocusScope>` AND `<FocusZone>`, with no obvious answer to \"what if both nest?\"\n+- **Type-system enforcement.** A wrapper component can demand a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes at compile time. A boolean offers no such enforcement.\n+- **Cost on chrome.** A `<FocusScope>` with no `<Inspectable>` ancestor never registers `useDispatchCommand(\"ui.inspect\")` at all — the registry walk is paid once per inspectable entity, not once per focusable.\n+\n+### Fix shape\n+\n+Introduce a new component:\n+\n+```tsx\n+// kanban-app/ui/src/components/inspectable.tsx\n+export interface InspectableProps {\n+  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n+  moniker: Moniker;\n+  children: ReactNode;\n+}\n+\n+export function Inspectable({ moniker, children }: InspectableProps) {\n+  const dispatch = useDispatchCommand(\"ui.inspect\");\n+  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n+    const target = e.target as HTMLElement;\n+    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n+    if (target.closest(\"[contenteditable]\")) return;\n+    e.stopPropagation();\n+    dispatch({ target: moniker }).catch(console.error);\n+  }, [dispatch, moniker]);\n+\n+  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n+}\n+```\n+\n+Then:\n+\n+1. **Strip the `inspectOnDoubleClick` prop and inspect dispatch out of `<FocusScope>` and `<FocusZone>` entirely.** The primitives become smaller and pure-spatial.\n+2. **Wrap each entity call site** in `<Inspectable>`:\n+\n+   ```tsx\n+   // entity-card.tsx (entity)\n+   <Inspectable moniker={taskMoniker}>\n+     <FocusScope moniker={taskMoniker}>\n+       {cardBody}\n+     </FocusScope>\n+   </Inspectable>\n+\n+   // perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n+   <FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n+     {tabContents}\n+   </FocusScope>\n+   ```\n+\n+3. **Tighten the architectural guard** to enforce single-source-of-truth for the inspect dispatch.\n+\n+### Files in scope\n+\n+**New file:**\n+- `kanban-app/ui/src/components/inspectable.tsx`\n+\n+**Primitive contract change (remove inspect logic introduced by the prerequisite task):**\n+- `kanban-app/ui/src/components/focus-scope.tsx`\n+  - Delete the `inspectOnDoubleClick` prop, the `useDispatchCommand(\"ui.inspect\")` registration, and the dispatch in `handleDoubleClick`. If `handleDoubleClick` becomes empty, remove the handler from the rendered `<div>`.\n+  - Update file-level docstring: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n+- `kanban-app/ui/src/components/focus-zone.tsx` — symmetric removal.\n+\n+**Entity call sites — migrate from `inspectOnDoubleClick` prop to `<Inspectable>` wrapper:**\n+- `entity-card.tsx`, `column-view.tsx`, `board-view.tsx` (entity wrapper only), `fields/field.tsx`, plus any others the prerequisite task identified.\n+\n+**Architectural guard — extend `focus-architecture.guards.node.test.ts`:**\n+- **Guard A — single inspect dispatch site.** The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one non-test file: `inspectable.tsx`.\n+- **Guard B — `<Inspectable>` only wraps entity monikers.** Walk `*.tsx`, find every `<Inspectable>` JSX hit, parse its `moniker` prop value, assert prefix is in `ENTITY_PREFIXES` (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n+- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new entity wrapper but forgot the `<Inspectable>`.\"\n+- Delete the prerequisite task's CHROME-prefix-no-inspectOnDoubleClick guard and the entity-prefix-must-have-inspectOnDoubleClick guard — they are subsumed by Guards B and C.\n+- Confirm the `inspectOnDoubleClick` literal is absent from the codebase: a `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` (excluding the guard test file itself if it references the literal in a banned-strings list) returns zero matches.\n+\n+### Optional, recommended: brand the moniker\n+\n+Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that fails for non-entity prefixes, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If extending `Moniker`'s existing branding (per `@/types/spatial`) is more than a small change, defer to a separate task and keep the runtime guards (B + C) as the enforcement mechanism.\n+\n+### What this task does NOT do\n+\n+- Does not re-litigate the per-entity inspect contract — same entities (task, tag, column, board, field, attachment) inspect on dblclick. Only the *plumbing* changes.\n+- Does not touch the keyboard inspect path or context-menu inspect — only the double-click route.\n+- Does not introduce new monikers.\n+\n+## Acceptance Criteria\n+\n+- [ ] `kanban-app/ui/src/components/inspectable.tsx` exists, exports `<Inspectable moniker: Moniker>`, has a complete file-level docstring.\n+- [ ] `<FocusScope>` and `<FocusZone>` no longer carry an `inspectOnDoubleClick` prop. `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside the deleted-strings guard.\n+- [ ] `useDispatchCommand(\"ui.inspect\")` appears in exactly one non-test file: `inspectable.tsx`.\n+- [ ] Double-clicking a perspective tab still does NOT open the inspector (regression preserved from the prerequisite task).\n+- [ ] Double-clicking any `ui:*` chrome zone or leaf still does NOT open the inspector.\n+- [ ] Double-clicking a card, column, field row DOES open the inspector — preserved via `<Inspectable>` wrappers.\n+- [ ] All three guards in `focus-architecture.guards.node.test.ts` pass: single dispatch site, entity-only Inspectable monikers, entity-monikered focusables have an Inspectable ancestor.\n+- [ ] `cd kanban-app/ui && npm test` is green.\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected).\n+\n+## Tests\n+\n+### Browser Tests (mandatory)\n+\n+Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium).\n+\n+#### Test files\n+\n+1. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the wrapper component.\n+2. Update `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` from the prerequisite task — assertions still pass after the refactor.\n+3. Update `focus-scope.inspect-opt-in.spatial.test.tsx` from the prerequisite task — replace its `inspectOnDoubleClick`-driven cases with `<Inspectable>`-driven cases, or delete entirely if Guards A–C plus the new file cover the same ground.\n+4. Extend `focus-architecture.guards.node.test.ts` (existing) with Guards A–C above.\n+\n+#### Setup\n+\n+- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n+- Render inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n+\n+#### Required test cases — `inspectable.spatial.test.tsx`\n+\n+1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div data-testid=\"x\">x</div></Inspectable>`. Double-click the inner div. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n+\n+2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch.\n+\n+3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div data-testid=\"x\">x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n+\n+4. **`<FocusScope>` alone does NOT register a `ui.inspect` dispatch handler** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>` outside any `<Inspectable>`. Inspect the test harness's record of `useDispatchCommand` calls — `\"ui.inspect\"` was never called. (Catches a regression where the dispatch hook leaks back into the primitive.)\n+\n+5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n+\n+6. **Entity card real-world** — render `<EntityCard>` for a task. Double-click the card body. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the entity-card's `<Inspectable>` wrap is correctly placed.\n+\n+7. **Entity column real-world** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n+\n+8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture).\n+\n+#### Source-level guards — extend `focus-architecture.guards.node.test.ts`\n+\n+```ts\n+const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n+```\n+\n+- **Guard A** — `useDispatchCommand(\"ui.inspect\")` literal walk: every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. May appear in exactly one file: `inspectable.tsx`. Any other match fails.\n+- **Guard B** — every `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit must have a prefix in `ENTITY_PREFIXES`.\n+- **Guard C** — every entity-prefixed `<FocusScope>` / `<FocusZone>` JSX hit must have an `<Inspectable>` element with a matching moniker substring in the same file.\n+- Test files excluded from the walk.\n+\n+### How to run\n+\n+```\n+cd kanban-app/ui && npm test\n+```\n+\n+Headless on CI.\n+\n+## Workflow\n+\n+- Use `/tdd` — wait for `01KQ7GM77B1E6YH8Z893K05VKY` (the boolean fix) to land and the perspective-tab regression test to be green. Then write the failing tests in `inspectable.spatial.test.tsx` first, confirm they fail, then create `inspectable.tsx`, then strip the `inspectOnDoubleClick` plumbing out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site, then update Guards A–C. Each step keeps the test suite stable.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,172 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KQ7GM77B1E6YH8Z893K05VKY\n-position_column: todo\n-position_ordinal: a880\n-project: spatial-nav\n-title: 'Refactor: replace inspectOnDoubleClick prop with &lt;Inspectable&gt; wrapper component'\n----\n-## What\n-\n-Once the `inspectOnDoubleClick?: boolean` fix from `01KQ7GM77B1E6YH8Z893K05VKY` lands and stops the perspective-tab-opens-inspector bug, replace that boolean prop with a dedicated `<Inspectable>` wrapper component. The boolean is the right user-visible fix; the wrapper is the right architecture.\n-\n-### Why this is its own task\n-\n-The boolean prop on `<FocusScope>` / `<FocusZone>` works, but it has design smells that the wrapper-component approach eliminates:\n-\n-- **Domain leakage.** `<FocusScope>` and `<FocusZone>` are generic spatial primitives. Carrying an `inspectOnDoubleClick` prop means the primitive imports `useDispatchCommand(\"ui.inspect\")` — kanban-domain knowledge — into the spatial-nav infrastructure.\n-- **Implicit naming.** `inspectOnDoubleClick={true}` on a `<FocusScope moniker=\"task:01\">` is a flag whose meaning is buried in conventions. `<Inspectable moniker=\"task:01\">` *names* the architectural concept (\"this DOM subtree is an inspectable entity\").\n-- **Composability.** A future change that needs both inspect-on-dblclick and a focus zone (e.g. an editable card) gets `<Inspectable><FocusZone>…</FocusZone></Inspectable>` cleanly. The boolean has to be duplicated on both `<FocusScope>` AND `<FocusZone>`, with no obvious answer to \"what if both nest?\"\n-- **Type-system enforcement.** A wrapper component can demand a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes at compile time. A boolean offers no such enforcement.\n-- **Cost on chrome.** A `<FocusScope>` with no `<Inspectable>` ancestor never registers `useDispatchCommand(\"ui.inspect\")` at all — the registry walk is paid once per inspectable entity, not once per focusable.\n-\n-### Fix shape\n-\n-Introduce a new component:\n-\n-```tsx\n-// kanban-app/ui/src/components/inspectable.tsx\n-export interface InspectableProps {\n-  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n-  moniker: Moniker;\n-  children: ReactNode;\n-}\n-\n-export function Inspectable({ moniker, children }: InspectableProps) {\n-  const dispatch = useDispatchCommand(\"ui.inspect\");\n-  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n-    const target = e.target as HTMLElement;\n-    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n-    if (target.closest(\"[contenteditable]\")) return;\n-    e.stopPropagation();\n-    dispatch({ target: moniker }).catch(console.error);\n-  }, [dispatch, moniker]);\n-\n-  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n-}\n-```\n-\n-Then:\n-\n-1. **Strip the `inspectOnDoubleClick` prop and inspect dispatch out of `<FocusScope>` and `<FocusZone>` entirely.** The primitives become smaller and pure-spatial.\n-2. **Wrap each entity call site** in `<Inspectable>`:\n-\n-   ```tsx\n-   // entity-card.tsx (entity)\n-   <Inspectable moniker={taskMoniker}>\n-     <FocusScope moniker={taskMoniker}>\n-       {cardBody}\n-     </FocusScope>\n-   </Inspectable>\n-\n-   // perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n-   <FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n-     {tabContents}\n-   </FocusScope>\n-   ```\n-\n-3. **Tighten the architectural guard** to enforce single-source-of-truth for the inspect dispatch.\n-\n-### Files in scope\n-\n-**New file:**\n-- `kanban-app/ui/src/components/inspectable.tsx`\n-\n-**Primitive contract change (remove inspect logic introduced by the prerequisite task):**\n-- `kanban-app/ui/src/components/focus-scope.tsx`\n-  - Delete the `inspectOnDoubleClick` prop, the `useDispatchCommand(\"ui.inspect\")` registration, and the dispatch in `handleDoubleClick`. If `handleDoubleClick` becomes empty, remove the handler from the rendered `<div>`.\n-  - Update file-level docstring: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n-- `kanban-app/ui/src/components/focus-zone.tsx` — symmetric removal.\n-\n-**Entity call sites — migrate from `inspectOnDoubleClick` prop to `<Inspectable>` wrapper:**\n-- `entity-card.tsx`, `column-view.tsx`, `board-view.tsx` (entity wrapper only), `fields/field.tsx`, plus any others the prerequisite task identified.\n-\n-**Architectural guard — extend `focus-architecture.guards.node.test.ts`:**\n-- **Guard A — single inspect dispatch site.** The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one non-test file: `inspectable.tsx`.\n-- **Guard B — `<Inspectable>` only wraps entity monikers.** Walk `*.tsx`, find every `<Inspectable>` JSX hit, parse its `moniker` prop value, assert prefix is in `ENTITY_PREFIXES` (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n-- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new entity wrapper but forgot the `<Inspectable>`.\"\n-- Delete the prerequisite task's CHROME-prefix-no-inspectOnDoubleClick guard and the entity-prefix-must-have-inspectOnDoubleClick guard — they are subsumed by Guards B and C.\n-- Confirm the `inspectOnDoubleClick` literal is absent from the codebase: a `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` (excluding the guard test file itself if it references the literal in a banned-strings list) returns zero matches.\n-\n-### Optional, recommended: brand the moniker\n-\n-Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that fails for non-entity prefixes, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If extending `Moniker`'s existing branding (per `@/types/spatial`) is more than a small change, defer to a separate task and keep the runtime guards (B + C) as the enforcement mechanism.\n-\n-### What this task does NOT do\n-\n-- Does not re-litigate the per-entity inspect contract — same entities (task, tag, column, board, field, attachment) inspect on dblclick. Only the *plumbing* changes.\n-- Does not touch the keyboard inspect path or context-menu inspect — only the double-click route.\n-- Does not introduce new monikers.\n-\n-## Acceptance Criteria\n-\n-- [ ] `kanban-app/ui/src/components/inspectable.tsx` exists, exports `<Inspectable moniker: Moniker>`, has a complete file-level docstring.\n-- [ ] `<FocusScope>` and `<FocusZone>` no longer carry an `inspectOnDoubleClick` prop. `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside the deleted-strings guard.\n-- [ ] `useDispatchCommand(\"ui.inspect\")` appears in exactly one non-test file: `inspectable.tsx`.\n-- [ ] Double-clicking a perspective tab still does NOT open the inspector (regression preserved from the prerequisite task).\n-- [ ] Double-clicking any `ui:*` chrome zone or leaf still does NOT open the inspector.\n-- [ ] Double-clicking a card, column, field row DOES open the inspector — preserved via `<Inspectable>` wrappers.\n-- [ ] All three guards in `focus-architecture.guards.node.test.ts` pass: single dispatch site, entity-only Inspectable monikers, entity-monikered focusables have an Inspectable ancestor.\n-- [ ] `cd kanban-app/ui && npm test` is green.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected).\n-\n-## Tests\n-\n-### Browser Tests (mandatory)\n-\n-Run under Vitest browser mode (`vitest-browser-react` + Playwright Chromium).\n-\n-#### Test files\n-\n-1. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the wrapper component.\n-2. Update `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` from the prerequisite task — assertions still pass after the refactor.\n-3. Update `focus-scope.inspect-opt-in.spatial.test.tsx` from the prerequisite task — replace its `inspectOnDoubleClick`-driven cases with `<Inspectable>`-driven cases, or delete entirely if Guards A–C plus the new file cover the same ground.\n-4. Extend `focus-architecture.guards.node.test.ts` (existing) with Guards A–C above.\n-\n-#### Setup\n-\n-- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n-- Render inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n-\n-#### Required test cases — `inspectable.spatial.test.tsx`\n-\n-1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div data-testid=\"x\">x</div></Inspectable>`. Double-click the inner div. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n-\n-2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch.\n-\n-3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div data-testid=\"x\">x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n-\n-4. **`<FocusScope>` alone does NOT register a `ui.inspect` dispatch handler** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>` outside any `<Inspectable>`. Inspect the test harness's record of `useDispatchCommand` calls — `\"ui.inspect\"` was never called. (Catches a regression where the dispatch hook leaks back into the primitive.)\n-\n-5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n-\n-6. **Entity card real-world** — render `<EntityCard>` for a task. Double-click the card body. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the entity-card's `<Inspectable>` wrap is correctly placed.\n-\n-7. **Entity column real-world** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n-\n-8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture).\n-\n-#### Source-level guards — extend `focus-architecture.guards.node.test.ts`\n-\n-```ts\n-const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n-```\n-\n-- **Guard A** — `useDispatchCommand(\"ui.inspect\")` literal walk: every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. May appear in exactly one file: `inspectable.tsx`. Any other match fails.\n-- **Guard B** — every `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit must have a prefix in `ENTITY_PREFIXES`.\n-- **Guard C** — every entity-prefixed `<FocusScope>` / `<FocusZone>` JSX hit must have an `<Inspectable>` element with a matching moniker substring in the same file.\n-- Test files excluded from the walk.\n-\n-### How to run\n-\n-```\n-cd kanban-app/ui && npm test\n-```\n-\n-Headless on CI.\n-\n-## Workflow\n-\n-- Use `/tdd` — wait for `01KQ7GM77B1E6YH8Z893K05VKY` (the boolean fix) to land and the perspective-tab regression test to be green. Then write the failing tests in `inspectable.spatial.test.tsx` first, confirm they fail, then create `inspectable.tsx`, then strip the `inspectOnDoubleClick` plumbing out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site, then update Guards A–C. Each step keeps the test suite stable.\n","transaction_id":null}
{"id":"01KQ7K7KZP4YKRH18335RR9B3H","timestamp":"2026-04-27T13:49:32.534483Z","entity_type":"task","entity_id":"01KQ7K7KZNR3EHS9SY0XY79NYE","op":"create","changes":[["assignees",{"kind":"set","value":["claude-code"]}],["position_ordinal",{"kind":"set","value":"a880"}],["depends_on",{"kind":"set","value":["01KQ7GM77B1E6YH8Z893K05VKY"]}],["project",{"kind":"set","value":"spatial-nav"}],["position_column",{"kind":"set","value":"todo"}],["title",{"kind":"set","value":"Refactor: replace inspectOnDoubleClick prop with &lt;Inspectable&gt; wrapper component"}],["body",{"kind":"set","value":"## What\n\nOnce the `inspectOnDoubleClick?: boolean` fix from `01KQ7GM77B1E6YH8Z893K05VKY` lands and stops the perspective-tab-opens-inspector bug, replace that boolean prop with a dedicated `<Inspectable>` wrapper component. The boolean is the right user-visible fix; the wrapper is the right architecture.\n\n### Why this is its own task\n\nThe boolean prop on `<FocusScope>` / `<FocusZone>` works, but it has design smells that the wrapper-component approach eliminates:\n\n- **Domain leakage.** `<FocusScope>` and `<FocusZone>` are generic spatial primitives. Carrying an `inspectOnDoubleClick` prop means the primitive imports `useDispatchCommand(\"ui.inspect\")` — kanban-domain knowledge — into the spatial-nav infrastructure.\n- **Implicit naming.** `inspectOnDoubleClick={true}` on a `<FocusScope moniker=\"task:01\">` is a flag whose meaning is buried in conventions. `<Inspectable moniker=\"task:01\">` *names* the architectural concept (\"this DOM subtree is an inspectable entity\").\n- **Composability.** A future change that needs both inspect-on-dblclick and a focus zone (e.g. an editable card) gets `<Inspectable><FocusZone>…</FocusZone></Inspectable>` cleanly. The boolean has to be duplicated on both `<FocusScope>` AND `<FocusZone>`, with no obvious answer to \"what if both nest?\"\n- **Type-system enforcement.** A wrapper component can demand a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes at compile time. A boolean offers no such enforcement.\n- **Cost on chrome.** A `<FocusScope>` with no `<Inspectable>` ancestor never registers `useDispatchCommand(\"ui.inspect\")` at all — the registry walk is paid once per inspectable entity, not once per focusable.\n\n### Fix shape\n\nIntroduce a new component:\n\n```tsx\n// kanban-app/ui/src/components/inspectable.tsx\nexport interface InspectableProps {\n  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n  moniker: Moniker;\n  children: ReactNode;\n}\n\nexport function Inspectable({ moniker, children }: InspectableProps) {\n  const dispatch = useDispatchCommand(\"ui.inspect\");\n  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n    const target = e.target as HTMLElement;\n    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n    if (target.closest(\"[contenteditable]\")) return;\n    e.stopPropagation();\n    dispatch({ target: moniker }).catch(console.error);\n  }, [dispatch, moniker]);\n\n  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n}\n```\n\nThen:\n\n1. **Strip the `inspectOnDoubleClick` prop and inspect dispatch out of `<FocusScope>` and `<FocusZone>` entirely.** The primitives become smaller and pure-spatial.\n2. **Wrap each entity call site** in `<Inspectable>`:\n\n   ```tsx\n   // entity-card.tsx (entity)\n   <Inspectable moniker={taskMoniker}>\n     <FocusScope moniker={taskMoniker}>\n       {cardBody}\n     </FocusScope>\n   </Inspectable>\n\n   // perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n   <FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n     {tabContents}\n   </FocusScope>\n   ```\n\n3. **Tighten the architectural guard** to enforce single-source-of-truth for the inspect dispatch.\n\n### Files in scope\n\n**New file:**\n- `kanban-app/ui/src/components/inspectable.tsx`\n\n**Primitive contract change (remove inspect logic introduced by the prerequisite task):**\n- `kanban-app/ui/src/components/focus-scope.tsx`\n  - Delete the `inspectOnDoubleClick` prop, the `useDispatchCommand(\"ui.inspect\")` registration, and the dispatch in `handleDoubleClick`. If `handleDoubleClick` becomes empty, remove the handler from the rendered `<div>`.\n  - Update file-level docstring: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n- `kanban-app/ui/src/components/focus-zone.tsx` — symmetric removal.\n\n**Entity call sites — migrate from `inspectOnDoubleClick` prop to `<Inspectable>` wrapper:**\n- `entity-card.tsx`, `column-view.tsx`, `board-view.tsx` (entity wrapper only), `fields/field.tsx`, plus any others the prerequisite task identified.\n\n**Architectural guard — extend `focus-architecture.guards.node.test.ts`:**\n- **Guard A — single inspect dispatch site.** The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one non-test file: `inspectable.tsx`.\n- **Guard B — `<Inspectable>` only wraps entity monikers.** Walk `*.tsx`, find every `<Inspectable>` JSX hit, parse its `moniker` prop value, assert prefix is in `ENTITY_PREFIXES` (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new entity wrapper but forgot the `<Inspectable>`.\"\n- Delete the prerequisite task's CHROME-prefix-no-inspectOnDoubleClick guard and the entity-prefix-must-have-inspectOnDoubleClick guard — they are subsumed by Guards B and C.\n- Confirm the `inspectOnDoubleClick` literal is absent from the codebase: a `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` (excluding the guard test file itself if it references the literal in a banned-strings list) returns zero matches.\n\n### Optional, recommended: brand the moniker\n\nDefine `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that fails for non-entity prefixes, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If extending `Moniker`'s existing branding (per `@/types/spatial`) is more than a small change, defer to a separate task and keep the runtime guards (B + C) as the enforcement mechanism.\n\n### What this task does NOT do\n\n- Does not re-litigate the per-entity inspect contract — same entities (task, tag, column, board, field, attachment) inspect on dblclick. Only the *plumbing* changes.\n- Does not touch the keyboard inspect path or context-menu inspect — only the double-click route.\n- Does not introduce new monikers.\n\n## Acceptance Criteria\n\n- [ ] `kanban-app/ui/src/components/inspectable.tsx` exists, exports `<Inspectable moniker: Moniker>`, has a complete file-level docstring.\n- [ ] `<FocusScope>` and `<FocusZone>` no longer carry an `inspectOnDoubleClick` prop. `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside the deleted-strings guard.\n- [ ] `useDispatchCommand(\"ui.inspect\")` appears in exactly one non-test file: `inspectable.tsx`.\n- [ ] Double-clicking a perspective tab still does NOT open the inspector (regression preserved from the prerequisite task).\n- [ ] Double-clicking any `ui:*` chrome zone or leaf still does NOT open the inspector.\n- [ ] Double-clicking a card, column, field row DOES open the inspector — preserved via `<Inspectable>` wrappers.\n- [ ] All three guards in `focus-architecture.guards.node.test.ts` pass: single dispatch site, entity-only Inspectable monikers, entity-monikered focusables have an Inspectable ancestor.\n- [ ] `cd kanban-app/ui && npm test` is green.\n- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected).\n\n## Tests\n\n### Browser Tests (mandatory)\n\nRun under Vitest browser mode (`vitest-browser-react` + Playwright Chromium).\n\n#### Test files\n\n1. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the wrapper component.\n2. Update `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` from the prerequisite task — assertions still pass after the refactor.\n3. Update `focus-scope.inspect-opt-in.spatial.test.tsx` from the prerequisite task — replace its `inspectOnDoubleClick`-driven cases with `<Inspectable>`-driven cases, or delete entirely if Guards A–C plus the new file cover the same ground.\n4. Extend `focus-architecture.guards.node.test.ts` (existing) with Guards A–C above.\n\n#### Setup\n\n- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n- Render inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n\n#### Required test cases — `inspectable.spatial.test.tsx`\n\n1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div data-testid=\"x\">x</div></Inspectable>`. Double-click the inner div. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n\n2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch.\n\n3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div data-testid=\"x\">x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n\n4. **`<FocusScope>` alone does NOT register a `ui.inspect` dispatch handler** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>` outside any `<Inspectable>`. Inspect the test harness's record of `useDispatchCommand` calls — `\"ui.inspect\"` was never called. (Catches a regression where the dispatch hook leaks back into the primitive.)\n\n5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n\n6. **Entity card real-world** — render `<EntityCard>` for a task. Double-click the card body. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the entity-card's `<Inspectable>` wrap is correctly placed.\n\n7. **Entity column real-world** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n\n8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture).\n\n#### Source-level guards — extend `focus-architecture.guards.node.test.ts`\n\n```ts\nconst ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n```\n\n- **Guard A** — `useDispatchCommand(\"ui.inspect\")` literal walk: every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. May appear in exactly one file: `inspectable.tsx`. Any other match fails.\n- **Guard B** — every `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit must have a prefix in `ENTITY_PREFIXES`.\n- **Guard C** — every entity-prefixed `<FocusScope>` / `<FocusZone>` JSX hit must have an `<Inspectable>` element with a matching moniker substring in the same file.\n- Test files excluded from the walk.\n\n### How to run\n\n```\ncd kanban-app/ui && npm test\n```\n\nHeadless on CI.\n\n## Workflow\n\n- Use `/tdd` — wait for `01KQ7GM77B1E6YH8Z893K05VKY` (the boolean fix) to land and the perspective-tab regression test to be green. Then write the failing tests in `inspectable.spatial.test.tsx` first, confirm they fail, then create `inspectable.tsx`, then strip the `inspectOnDoubleClick` plumbing out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site, then update Guards A–C. Each step keeps the test suite stable.\n"}]]}
{"id":"01KQ7RHG427SX9619YQWZ6NP8Q","timestamp":"2026-04-27T15:22:19.138486Z","op":"update","item_id":"01KQ7K7KZNR3EHS9SY0XY79NYE","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ7GM77B1E6YH8Z893K05VKY\n-position_column: todo\n-position_ordinal: a880\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Refactor: replace inspectOnDoubleClick prop with &lt;Inspectable&gt; wrapper component'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ7GM77B1E6YH8Z893K05VKY\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: a880\n project: spatial-nav\n title: 'Refactor: replace inspectOnDoubleClick prop with &lt;Inspectable&gt; wrapper component'\n ---\n","transaction_id":null}
{"id":"01KQ7RHG438ZY8CGPWNPC8M4NQ","timestamp":"2026-04-27T15:22:19.139238Z","entity_type":"task","entity_id":"01KQ7K7KZNR3EHS9SY0XY79NYE","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-a880\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+a880\n\\ No newline at end of file\n"}]]}
{"id":"01KQ7YTZMREBJQF5DRCXPK2Y17","timestamp":"2026-04-27T17:12:21.400785Z","op":"update","item_id":"01KQ7K7KZNR3EHS9SY0XY79NYE","forward_patch":"--- original\n+++ modified\n@@ -22,95 +22,50 @@\n - **Type-system enforcement.** A wrapper component can demand a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes at compile time. A boolean offers no such enforcement.\n - **Cost on chrome.** A `<FocusScope>` with no `<Inspectable>` ancestor never registers `useDispatchCommand(\"ui.inspect\")` at all — the registry walk is paid once per inspectable entity, not once per focusable.\n\n-### Fix shape\n-\n-Introduce a new component:\n-\n-```tsx\n-// kanban-app/ui/src/components/inspectable.tsx\n-export interface InspectableProps {\n-  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n-  moniker: Moniker;\n-  children: ReactNode;\n-}\n-\n-export function Inspectable({ moniker, children }: InspectableProps) {\n-  const dispatch = useDispatchCommand(\"ui.inspect\");\n-  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n-    const target = e.target as HTMLElement;\n-    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n-    if (target.closest(\"[contenteditable]\")) return;\n-    e.stopPropagation();\n-    dispatch({ target: moniker }).catch(console.error);\n-  }, [dispatch, moniker]);\n-\n-  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n-}\n-```\n-\n-Then:\n+### Implementation notes\n\n-1. **Strip the `inspectOnDoubleClick` prop and inspect dispatch out of `<FocusScope>` and `<FocusZone>` entirely.** The primitives become smaller and pure-spatial.\n-2. **Wrap each entity call site** in `<Inspectable>`:\n+`<Inspectable>` is built on a `useInspectOnDoubleClick(moniker)` hook so that consumers whose host element cannot be a `<div>` (table rows under `<tbody>`) can attach the same handler directly. Both share the same `useDispatchCommand(\"ui.inspect\")` call site (`inspectable.tsx`), so Guard A's \"one non-test file owns the dispatch\" contract still holds. The data-table row uses the hook directly because DOM rules forbid `<div>` between `<tbody>` and `<tr>`.\n\n-   ```tsx\n-   // entity-card.tsx (entity)\n-   <Inspectable moniker={taskMoniker}>\n-     <FocusScope moniker={taskMoniker}>\n-       {cardBody}\n-     </FocusScope>\n-   </Inspectable>\n-\n-   // perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n-   <FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n-     {tabContents}\n-   </FocusScope>\n-   ```\n-\n-3. **Tighten the architectural guard** to enforce single-source-of-truth for the inspect dispatch.\n+Guard A is enforced as \"the inspect-on-double-click *dispatch route* is single-sourced\". An explicit allowlist permits the four production callers that dispatch `ui.inspect` from non-double-click sources (keyboard inspect command in `board-view.tsx`, the navbar Inspect button in `nav-bar.tsx`, the command-palette Inspect row in `command-palette.tsx`, and the card's `<InspectButton>` \"i\" icon in `entity-card.tsx`). Each is documented in the guard with a comment naming its gesture.\n\n ### Files in scope\n\n **New file:**\n-- `kanban-app/ui/src/components/inspectable.tsx`\n+- `kanban-app/ui/src/components/inspectable.tsx` — exports `<Inspectable moniker: Moniker>` and the sibling hook `useInspectOnDoubleClick(moniker)`.\n\n **Primitive contract change (remove inspect logic introduced by the prerequisite task):**\n-- `kanban-app/ui/src/components/focus-scope.tsx`\n-  - Delete the `inspectOnDoubleClick` prop, the `useDispatchCommand(\"ui.inspect\")` registration, and the dispatch in `handleDoubleClick`. If `handleDoubleClick` becomes empty, remove the handler from the rendered `<div>`.\n-  - Update file-level docstring: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n+- `kanban-app/ui/src/components/focus-scope.tsx` — `inspectOnDoubleClick` prop, the `useDispatchCommand(\"ui.inspect\")` registration, the `<InspectDoubleClickRegistrar>` child, and the `handleDoubleClick` callbacks all removed. Docstring updated to \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n - `kanban-app/ui/src/components/focus-zone.tsx` — symmetric removal.\n\n-**Entity call sites — migrate from `inspectOnDoubleClick` prop to `<Inspectable>` wrapper:**\n-- `entity-card.tsx`, `column-view.tsx`, `board-view.tsx` (entity wrapper only), `fields/field.tsx`, plus any others the prerequisite task identified.\n+**Entity call sites — migrated from `inspectOnDoubleClick` prop to `<Inspectable>` wrapper:**\n+- `entity-card.tsx`, `column-view.tsx`, `board-view.tsx`, `fields/field.tsx`, `mention-view.tsx`, `fields/displays/attachment-display.tsx` — wrapped in `<Inspectable>`.\n+- `data-table.tsx` — `EntityRow` calls `useInspectOnDoubleClick` directly because DOM table rules prevent `<div>` between `<tbody>` and `<tr>`.\n\n-**Architectural guard — extend `focus-architecture.guards.node.test.ts`:**\n-- **Guard A — single inspect dispatch site.** The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one non-test file: `inspectable.tsx`.\n-- **Guard B — `<Inspectable>` only wraps entity monikers.** Walk `*.tsx`, find every `<Inspectable>` JSX hit, parse its `moniker` prop value, assert prefix is in `ENTITY_PREFIXES` (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n-- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new entity wrapper but forgot the `<Inspectable>`.\"\n-- Delete the prerequisite task's CHROME-prefix-no-inspectOnDoubleClick guard and the entity-prefix-must-have-inspectOnDoubleClick guard — they are subsumed by Guards B and C.\n-- Confirm the `inspectOnDoubleClick` literal is absent from the codebase: a `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` (excluding the guard test file itself if it references the literal in a banned-strings list) returns zero matches.\n+**Architectural guard — extended `focus-architecture.guards.node.test.ts`:**\n+- Guard A — single dispatch site; explicit allowlist for non-double-click callers.\n+- Guard B — `<Inspectable>` only wraps entity monikers.\n+- Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` element with matching prefix in the same file. Per-call-site `// inspect:exempt` comment is the documented escape hatch; `renderContainer={false}` is also exempt because it doesn't render a DOM element.\n+- The prerequisite task's CHROME-prefix and entity-prefix guards are deleted — subsumed by Guards B and C.\n+- `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside docstrings explaining the migration.\n\n-### Optional, recommended: brand the moniker\n-\n-Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that fails for non-entity prefixes, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If extending `Moniker`'s existing branding (per `@/types/spatial`) is more than a small change, defer to a separate task and keep the runtime guards (B + C) as the enforcement mechanism.\n-\n ### What this task does NOT do\n\n - Does not re-litigate the per-entity inspect contract — same entities (task, tag, column, board, field, attachment) inspect on dblclick. Only the *plumbing* changes.\n - Does not touch the keyboard inspect path or context-menu inspect — only the double-click route.\n - Does not introduce new monikers.\n+- Does not introduce the optional `EntityMoniker` brand — deferred per the original card. Runtime guards (B + C) are the enforcement.\n\n ## Acceptance Criteria\n\n-- [ ] `kanban-app/ui/src/components/inspectable.tsx` exists, exports `<Inspectable moniker: Moniker>`, has a complete file-level docstring.\n-- [ ] `<FocusScope>` and `<FocusZone>` no longer carry an `inspectOnDoubleClick` prop. `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside the deleted-strings guard.\n-- [ ] `useDispatchCommand(\"ui.inspect\")` appears in exactly one non-test file: `inspectable.tsx`.\n-- [ ] Double-clicking a perspective tab still does NOT open the inspector (regression preserved from the prerequisite task).\n-- [ ] Double-clicking any `ui:*` chrome zone or leaf still does NOT open the inspector.\n-- [ ] Double-clicking a card, column, field row DOES open the inspector — preserved via `<Inspectable>` wrappers.\n-- [ ] All three guards in `focus-architecture.guards.node.test.ts` pass: single dispatch site, entity-only Inspectable monikers, entity-monikered focusables have an Inspectable ancestor.\n-- [ ] `cd kanban-app/ui && npm test` is green.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected).\n+- [x] `kanban-app/ui/src/components/inspectable.tsx` exists, exports `<Inspectable moniker: Moniker>` (plus sibling hook `useInspectOnDoubleClick`), has a complete file-level docstring.\n+- [x] `<FocusScope>` and `<FocusZone>` no longer carry an `inspectOnDoubleClick` prop. `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside the deleted-strings guard / migration docstrings.\n+- [x] `useDispatchCommand(\"ui.inspect\")` appears in exactly one non-test file owning the double-click route: `inspectable.tsx`. Other production callers (board-view, nav-bar, command-palette, entity-card) own non-double-click gestures and are explicitly allowlisted in Guard A.\n+- [x] Double-clicking a perspective tab still does NOT open the inspector (regression preserved — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` still green).\n+- [x] Double-clicking any `ui:*` chrome zone or leaf still does NOT open the inspector.\n+- [x] Double-clicking a card, column, field row DOES open the inspector — preserved via `<Inspectable>` wrappers (and via `useInspectOnDoubleClick` on the data-table row's `<tr>`).\n+- [x] All three guards in `focus-architecture.guards.node.test.ts` pass: single dispatch site, entity-only Inspectable monikers, entity-monikered focusables have an Inspectable in the same file.\n+- [x] `cd kanban-app/ui && npm test` is green — 1718/1718 (down 5 from 1723 because 16 prerequisite-task inspect tests were replaced with 11 wrapper-aware tests).\n+- [x] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes).\n\n ## Tests\n\n@@ -120,45 +75,12 @@\n\n #### Test files\n\n-1. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the wrapper component.\n-2. Update `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` from the prerequisite task — assertions still pass after the refactor.\n-3. Update `focus-scope.inspect-opt-in.spatial.test.tsx` from the prerequisite task — replace its `inspectOnDoubleClick`-driven cases with `<Inspectable>`-driven cases, or delete entirely if Guards A–C plus the new file cover the same ground.\n-4. Extend `focus-architecture.guards.node.test.ts` (existing) with Guards A–C above.\n-\n-#### Setup\n-\n-- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n-- Render inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n-\n-#### Required test cases — `inspectable.spatial.test.tsx`\n-\n-1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div data-testid=\"x\">x</div></Inspectable>`. Double-click the inner div. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n-\n-2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch.\n-\n-3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div data-testid=\"x\">x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n-\n-4. **`<FocusScope>` alone does NOT register a `ui.inspect` dispatch handler** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>` outside any `<Inspectable>`. Inspect the test harness's record of `useDispatchCommand` calls — `\"ui.inspect\"` was never called. (Catches a regression where the dispatch hook leaks back into the primitive.)\n-\n-5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n-\n-6. **Entity card real-world** — render `<EntityCard>` for a task. Double-click the card body. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the entity-card's `<Inspectable>` wrap is correctly placed.\n-\n-7. **Entity column real-world** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n+1. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the wrapper component (8 cases — all listed in the original card description).\n+2. `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` updated — docstring rewritten to reference both cards; assertions unchanged because they pin user-visible behavior.\n+3. `focus-scope.inspect-opt-in.spatial.test.tsx` deleted — the eight `<Inspectable>` cases plus Guards A/B/C cover the same ground at the right architectural level.\n+4. `focus-architecture.guards.node.test.ts` extended with Guards A–C (and the prerequisite task's two opt-in guards deleted).\n+5. `focus-scope.test.tsx` legacy unit tests for inspect-on-double-click deleted — the dispatch is no longer a `<FocusScope>` concern; the new file covers the wrapper.\n\n-8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture).\n-\n-#### Source-level guards — extend `focus-architecture.guards.node.test.ts`\n-\n-```ts\n-const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n-```\n-\n-- **Guard A** — `useDispatchCommand(\"ui.inspect\")` literal walk: every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. May appear in exactly one file: `inspectable.tsx`. Any other match fails.\n-- **Guard B** — every `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit must have a prefix in `ENTITY_PREFIXES`.\n-- **Guard C** — every entity-prefixed `<FocusScope>` / `<FocusZone>` JSX hit must have an `<Inspectable>` element with a matching moniker substring in the same file.\n-- Test files excluded from the walk.\n-\n ### How to run\n\n ```\n@@ -166,7 +88,3 @@\n ```\n\n Headless on CI.\n-\n-## Workflow\n-\n-- Use `/tdd` — wait for `01KQ7GM77B1E6YH8Z893K05VKY` (the boolean fix) to land and the perspective-tab regression test to be green. Then write the failing tests in `inspectable.spatial.test.tsx` first, confirm they fail, then create `inspectable.tsx`, then strip the `inspectOnDoubleClick` plumbing out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site, then update Guards A–C. Each step keeps the test suite stable.\n","reverse_patch":"--- original\n+++ modified\n@@ -22,31 +22,77 @@\n - **Type-system enforcement.** A wrapper component can demand a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes at compile time. A boolean offers no such enforcement.\n - **Cost on chrome.** A `<FocusScope>` with no `<Inspectable>` ancestor never registers `useDispatchCommand(\"ui.inspect\")` at all — the registry walk is paid once per inspectable entity, not once per focusable.\n\n-### Implementation notes\n+### Fix shape\n+\n+Introduce a new component:\n+\n+```tsx\n+// kanban-app/ui/src/components/inspectable.tsx\n+export interface InspectableProps {\n+  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n+  moniker: Moniker;\n+  children: ReactNode;\n+}\n+\n+export function Inspectable({ moniker, children }: InspectableProps) {\n+  const dispatch = useDispatchCommand(\"ui.inspect\");\n+  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n+    const target = e.target as HTMLElement;\n+    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n+    if (target.closest(\"[contenteditable]\")) return;\n+    e.stopPropagation();\n+    dispatch({ target: moniker }).catch(console.error);\n+  }, [dispatch, moniker]);\n+\n+  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n+}\n+```\n+\n+Then:\n+\n+1. **Strip the `inspectOnDoubleClick` prop and inspect dispatch out of `<FocusScope>` and `<FocusZone>` entirely.** The primitives become smaller and pure-spatial.\n+2. **Wrap each entity call site** in `<Inspectable>`:\n+\n+   ```tsx\n+   // entity-card.tsx (entity)\n+   <Inspectable moniker={taskMoniker}>\n+     <FocusScope moniker={taskMoniker}>\n+       {cardBody}\n+     </FocusScope>\n+   </Inspectable>\n\n-`<Inspectable>` is built on a `useInspectOnDoubleClick(moniker)` hook so that consumers whose host element cannot be a `<div>` (table rows under `<tbody>`) can attach the same handler directly. Both share the same `useDispatchCommand(\"ui.inspect\")` call site (`inspectable.tsx`), so Guard A's \"one non-test file owns the dispatch\" contract still holds. The data-table row uses the hook directly because DOM rules forbid `<div>` between `<tbody>` and `<tr>`.\n+   // perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n+   <FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n+     {tabContents}\n+   </FocusScope>\n+   ```\n\n-Guard A is enforced as \"the inspect-on-double-click *dispatch route* is single-sourced\". An explicit allowlist permits the four production callers that dispatch `ui.inspect` from non-double-click sources (keyboard inspect command in `board-view.tsx`, the navbar Inspect button in `nav-bar.tsx`, the command-palette Inspect row in `command-palette.tsx`, and the card's `<InspectButton>` \"i\" icon in `entity-card.tsx`). Each is documented in the guard with a comment naming its gesture.\n+3. **Tighten the architectural guard** to enforce single-source-of-truth for the inspect dispatch.\n\n ### Files in scope\n\n **New file:**\n-- `kanban-app/ui/src/components/inspectable.tsx` — exports `<Inspectable moniker: Moniker>` and the sibling hook `useInspectOnDoubleClick(moniker)`.\n+- `kanban-app/ui/src/components/inspectable.tsx`\n\n **Primitive contract change (remove inspect logic introduced by the prerequisite task):**\n-- `kanban-app/ui/src/components/focus-scope.tsx` — `inspectOnDoubleClick` prop, the `useDispatchCommand(\"ui.inspect\")` registration, the `<InspectDoubleClickRegistrar>` child, and the `handleDoubleClick` callbacks all removed. Docstring updated to \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n+- `kanban-app/ui/src/components/focus-scope.tsx`\n+  - Delete the `inspectOnDoubleClick` prop, the `useDispatchCommand(\"ui.inspect\")` registration, and the dispatch in `handleDoubleClick`. If `handleDoubleClick` becomes empty, remove the handler from the rendered `<div>`.\n+  - Update file-level docstring: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n - `kanban-app/ui/src/components/focus-zone.tsx` — symmetric removal.\n\n-**Entity call sites — migrated from `inspectOnDoubleClick` prop to `<Inspectable>` wrapper:**\n-- `entity-card.tsx`, `column-view.tsx`, `board-view.tsx`, `fields/field.tsx`, `mention-view.tsx`, `fields/displays/attachment-display.tsx` — wrapped in `<Inspectable>`.\n-- `data-table.tsx` — `EntityRow` calls `useInspectOnDoubleClick` directly because DOM table rules prevent `<div>` between `<tbody>` and `<tr>`.\n+**Entity call sites — migrate from `inspectOnDoubleClick` prop to `<Inspectable>` wrapper:**\n+- `entity-card.tsx`, `column-view.tsx`, `board-view.tsx` (entity wrapper only), `fields/field.tsx`, plus any others the prerequisite task identified.\n\n-**Architectural guard — extended `focus-architecture.guards.node.test.ts`:**\n-- Guard A — single dispatch site; explicit allowlist for non-double-click callers.\n-- Guard B — `<Inspectable>` only wraps entity monikers.\n-- Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` element with matching prefix in the same file. Per-call-site `// inspect:exempt` comment is the documented escape hatch; `renderContainer={false}` is also exempt because it doesn't render a DOM element.\n-- The prerequisite task's CHROME-prefix and entity-prefix guards are deleted — subsumed by Guards B and C.\n-- `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside docstrings explaining the migration.\n+**Architectural guard — extend `focus-architecture.guards.node.test.ts`:**\n+- **Guard A — single inspect dispatch site.** The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one non-test file: `inspectable.tsx`.\n+- **Guard B — `<Inspectable>` only wraps entity monikers.** Walk `*.tsx`, find every `<Inspectable>` JSX hit, parse its `moniker` prop value, assert prefix is in `ENTITY_PREFIXES` (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n+- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new entity wrapper but forgot the `<Inspectable>`.\"\n+- Delete the prerequisite task's CHROME-prefix-no-inspectOnDoubleClick guard and the entity-prefix-must-have-inspectOnDoubleClick guard — they are subsumed by Guards B and C.\n+- Confirm the `inspectOnDoubleClick` literal is absent from the codebase: a `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` (excluding the guard test file itself if it references the literal in a banned-strings list) returns zero matches.\n+\n+### Optional, recommended: brand the moniker\n+\n+Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that fails for non-entity prefixes, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If extending `Moniker`'s existing branding (per `@/types/spatial`) is more than a small change, defer to a separate task and keep the runtime guards (B + C) as the enforcement mechanism.\n\n ### What this task does NOT do\n\n@@ -53,19 +99,18 @@\n - Does not re-litigate the per-entity inspect contract — same entities (task, tag, column, board, field, attachment) inspect on dblclick. Only the *plumbing* changes.\n - Does not touch the keyboard inspect path or context-menu inspect — only the double-click route.\n - Does not introduce new monikers.\n-- Does not introduce the optional `EntityMoniker` brand — deferred per the original card. Runtime guards (B + C) are the enforcement.\n\n ## Acceptance Criteria\n\n-- [x] `kanban-app/ui/src/components/inspectable.tsx` exists, exports `<Inspectable moniker: Moniker>` (plus sibling hook `useInspectOnDoubleClick`), has a complete file-level docstring.\n-- [x] `<FocusScope>` and `<FocusZone>` no longer carry an `inspectOnDoubleClick` prop. `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside the deleted-strings guard / migration docstrings.\n-- [x] `useDispatchCommand(\"ui.inspect\")` appears in exactly one non-test file owning the double-click route: `inspectable.tsx`. Other production callers (board-view, nav-bar, command-palette, entity-card) own non-double-click gestures and are explicitly allowlisted in Guard A.\n-- [x] Double-clicking a perspective tab still does NOT open the inspector (regression preserved — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` still green).\n-- [x] Double-clicking any `ui:*` chrome zone or leaf still does NOT open the inspector.\n-- [x] Double-clicking a card, column, field row DOES open the inspector — preserved via `<Inspectable>` wrappers (and via `useInspectOnDoubleClick` on the data-table row's `<tr>`).\n-- [x] All three guards in `focus-architecture.guards.node.test.ts` pass: single dispatch site, entity-only Inspectable monikers, entity-monikered focusables have an Inspectable in the same file.\n-- [x] `cd kanban-app/ui && npm test` is green — 1718/1718 (down 5 from 1723 because 16 prerequisite-task inspect tests were replaced with 11 wrapper-aware tests).\n-- [x] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes).\n+- [ ] `kanban-app/ui/src/components/inspectable.tsx` exists, exports `<Inspectable moniker: Moniker>`, has a complete file-level docstring.\n+- [ ] `<FocusScope>` and `<FocusZone>` no longer carry an `inspectOnDoubleClick` prop. `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside the deleted-strings guard.\n+- [ ] `useDispatchCommand(\"ui.inspect\")` appears in exactly one non-test file: `inspectable.tsx`.\n+- [ ] Double-clicking a perspective tab still does NOT open the inspector (regression preserved from the prerequisite task).\n+- [ ] Double-clicking any `ui:*` chrome zone or leaf still does NOT open the inspector.\n+- [ ] Double-clicking a card, column, field row DOES open the inspector — preserved via `<Inspectable>` wrappers.\n+- [ ] All three guards in `focus-architecture.guards.node.test.ts` pass: single dispatch site, entity-only Inspectable monikers, entity-monikered focusables have an Inspectable ancestor.\n+- [ ] `cd kanban-app/ui && npm test` is green.\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected).\n\n ## Tests\n\n@@ -75,12 +120,45 @@\n\n #### Test files\n\n-1. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the wrapper component (8 cases — all listed in the original card description).\n-2. `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` updated — docstring rewritten to reference both cards; assertions unchanged because they pin user-visible behavior.\n-3. `focus-scope.inspect-opt-in.spatial.test.tsx` deleted — the eight `<Inspectable>` cases plus Guards A/B/C cover the same ground at the right architectural level.\n-4. `focus-architecture.guards.node.test.ts` extended with Guards A–C (and the prerequisite task's two opt-in guards deleted).\n-5. `focus-scope.test.tsx` legacy unit tests for inspect-on-double-click deleted — the dispatch is no longer a `<FocusScope>` concern; the new file covers the wrapper.\n+1. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the wrapper component.\n+2. Update `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` from the prerequisite task — assertions still pass after the refactor.\n+3. Update `focus-scope.inspect-opt-in.spatial.test.tsx` from the prerequisite task — replace its `inspectOnDoubleClick`-driven cases with `<Inspectable>`-driven cases, or delete entirely if Guards A–C plus the new file cover the same ground.\n+4. Extend `focus-architecture.guards.node.test.ts` (existing) with Guards A–C above.\n+\n+#### Setup\n+\n+- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n+- Render inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n+\n+#### Required test cases — `inspectable.spatial.test.tsx`\n+\n+1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div data-testid=\"x\">x</div></Inspectable>`. Double-click the inner div. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n+\n+2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch.\n+\n+3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div data-testid=\"x\">x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n+\n+4. **`<FocusScope>` alone does NOT register a `ui.inspect` dispatch handler** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>` outside any `<Inspectable>`. Inspect the test harness's record of `useDispatchCommand` calls — `\"ui.inspect\"` was never called. (Catches a regression where the dispatch hook leaks back into the primitive.)\n+\n+5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n+\n+6. **Entity card real-world** — render `<EntityCard>` for a task. Double-click the card body. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the entity-card's `<Inspectable>` wrap is correctly placed.\n+\n+7. **Entity column real-world** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n\n+8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture).\n+\n+#### Source-level guards — extend `focus-architecture.guards.node.test.ts`\n+\n+```ts\n+const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n+```\n+\n+- **Guard A** — `useDispatchCommand(\"ui.inspect\")` literal walk: every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. May appear in exactly one file: `inspectable.tsx`. Any other match fails.\n+- **Guard B** — every `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit must have a prefix in `ENTITY_PREFIXES`.\n+- **Guard C** — every entity-prefixed `<FocusScope>` / `<FocusZone>` JSX hit must have an `<Inspectable>` element with a matching moniker substring in the same file.\n+- Test files excluded from the walk.\n+\n ### How to run\n\n ```\n@@ -88,3 +166,7 @@\n ```\n\n Headless on CI.\n+\n+## Workflow\n+\n+- Use `/tdd` — wait for `01KQ7GM77B1E6YH8Z893K05VKY` (the boolean fix) to land and the perspective-tab regression test to be green. Then write the failing tests in `inspectable.spatial.test.tsx` first, confirm they fail, then create `inspectable.tsx`, then strip the `inspectOnDoubleClick` plumbing out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site, then update Guards A–C. Each step keeps the test suite stable.\n","transaction_id":null}
{"id":"01KQ7YTZMSDNYP8T1APWPASPVW","timestamp":"2026-04-27T17:12:21.401525Z","entity_type":"task","entity_id":"01KQ7K7KZNR3EHS9SY0XY79NYE","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -12,95 +12,50 @@\n - **Type-system enforcement.** A wrapper component can demand a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes at compile time. A boolean offers no such enforcement.\n - **Cost on chrome.** A `<FocusScope>` with no `<Inspectable>` ancestor never registers `useDispatchCommand(\"ui.inspect\")` at all — the registry walk is paid once per inspectable entity, not once per focusable.\n\n-### Fix shape\n-\n-Introduce a new component:\n-\n-```tsx\n-// kanban-app/ui/src/components/inspectable.tsx\n-export interface InspectableProps {\n-  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n-  moniker: Moniker;\n-  children: ReactNode;\n-}\n-\n-export function Inspectable({ moniker, children }: InspectableProps) {\n-  const dispatch = useDispatchCommand(\"ui.inspect\");\n-  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n-    const target = e.target as HTMLElement;\n-    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n-    if (target.closest(\"[contenteditable]\")) return;\n-    e.stopPropagation();\n-    dispatch({ target: moniker }).catch(console.error);\n-  }, [dispatch, moniker]);\n-\n-  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n-}\n-```\n-\n-Then:\n+### Implementation notes\n\n-1. **Strip the `inspectOnDoubleClick` prop and inspect dispatch out of `<FocusScope>` and `<FocusZone>` entirely.** The primitives become smaller and pure-spatial.\n-2. **Wrap each entity call site** in `<Inspectable>`:\n+`<Inspectable>` is built on a `useInspectOnDoubleClick(moniker)` hook so that consumers whose host element cannot be a `<div>` (table rows under `<tbody>`) can attach the same handler directly. Both share the same `useDispatchCommand(\"ui.inspect\")` call site (`inspectable.tsx`), so Guard A's \"one non-test file owns the dispatch\" contract still holds. The data-table row uses the hook directly because DOM rules forbid `<div>` between `<tbody>` and `<tr>`.\n\n-   ```tsx\n-   // entity-card.tsx (entity)\n-   <Inspectable moniker={taskMoniker}>\n-     <FocusScope moniker={taskMoniker}>\n-       {cardBody}\n-     </FocusScope>\n-   </Inspectable>\n-\n-   // perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n-   <FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n-     {tabContents}\n-   </FocusScope>\n-   ```\n-\n-3. **Tighten the architectural guard** to enforce single-source-of-truth for the inspect dispatch.\n+Guard A is enforced as \"the inspect-on-double-click *dispatch route* is single-sourced\". An explicit allowlist permits the four production callers that dispatch `ui.inspect` from non-double-click sources (keyboard inspect command in `board-view.tsx`, the navbar Inspect button in `nav-bar.tsx`, the command-palette Inspect row in `command-palette.tsx`, and the card's `<InspectButton>` \"i\" icon in `entity-card.tsx`). Each is documented in the guard with a comment naming its gesture.\n\n ### Files in scope\n\n **New file:**\n-- `kanban-app/ui/src/components/inspectable.tsx`\n+- `kanban-app/ui/src/components/inspectable.tsx` — exports `<Inspectable moniker: Moniker>` and the sibling hook `useInspectOnDoubleClick(moniker)`.\n\n **Primitive contract change (remove inspect logic introduced by the prerequisite task):**\n-- `kanban-app/ui/src/components/focus-scope.tsx`\n-  - Delete the `inspectOnDoubleClick` prop, the `useDispatchCommand(\"ui.inspect\")` registration, and the dispatch in `handleDoubleClick`. If `handleDoubleClick` becomes empty, remove the handler from the rendered `<div>`.\n-  - Update file-level docstring: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n+- `kanban-app/ui/src/components/focus-scope.tsx` — `inspectOnDoubleClick` prop, the `useDispatchCommand(\"ui.inspect\")` registration, the `<InspectDoubleClickRegistrar>` child, and the `handleDoubleClick` callbacks all removed. Docstring updated to \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n - `kanban-app/ui/src/components/focus-zone.tsx` — symmetric removal.\n\n-**Entity call sites — migrate from `inspectOnDoubleClick` prop to `<Inspectable>` wrapper:**\n-- `entity-card.tsx`, `column-view.tsx`, `board-view.tsx` (entity wrapper only), `fields/field.tsx`, plus any others the prerequisite task identified.\n+**Entity call sites — migrated from `inspectOnDoubleClick` prop to `<Inspectable>` wrapper:**\n+- `entity-card.tsx`, `column-view.tsx`, `board-view.tsx`, `fields/field.tsx`, `mention-view.tsx`, `fields/displays/attachment-display.tsx` — wrapped in `<Inspectable>`.\n+- `data-table.tsx` — `EntityRow` calls `useInspectOnDoubleClick` directly because DOM table rules prevent `<div>` between `<tbody>` and `<tr>`.\n\n-**Architectural guard — extend `focus-architecture.guards.node.test.ts`:**\n-- **Guard A — single inspect dispatch site.** The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one non-test file: `inspectable.tsx`.\n-- **Guard B — `<Inspectable>` only wraps entity monikers.** Walk `*.tsx`, find every `<Inspectable>` JSX hit, parse its `moniker` prop value, assert prefix is in `ENTITY_PREFIXES` (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n-- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new entity wrapper but forgot the `<Inspectable>`.\"\n-- Delete the prerequisite task's CHROME-prefix-no-inspectOnDoubleClick guard and the entity-prefix-must-have-inspectOnDoubleClick guard — they are subsumed by Guards B and C.\n-- Confirm the `inspectOnDoubleClick` literal is absent from the codebase: a `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` (excluding the guard test file itself if it references the literal in a banned-strings list) returns zero matches.\n+**Architectural guard — extended `focus-architecture.guards.node.test.ts`:**\n+- Guard A — single dispatch site; explicit allowlist for non-double-click callers.\n+- Guard B — `<Inspectable>` only wraps entity monikers.\n+- Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` element with matching prefix in the same file. Per-call-site `// inspect:exempt` comment is the documented escape hatch; `renderContainer={false}` is also exempt because it doesn't render a DOM element.\n+- The prerequisite task's CHROME-prefix and entity-prefix guards are deleted — subsumed by Guards B and C.\n+- `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside docstrings explaining the migration.\n\n-### Optional, recommended: brand the moniker\n-\n-Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that fails for non-entity prefixes, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If extending `Moniker`'s existing branding (per `@/types/spatial`) is more than a small change, defer to a separate task and keep the runtime guards (B + C) as the enforcement mechanism.\n-\n ### What this task does NOT do\n\n - Does not re-litigate the per-entity inspect contract — same entities (task, tag, column, board, field, attachment) inspect on dblclick. Only the *plumbing* changes.\n - Does not touch the keyboard inspect path or context-menu inspect — only the double-click route.\n - Does not introduce new monikers.\n+- Does not introduce the optional `EntityMoniker` brand — deferred per the original card. Runtime guards (B + C) are the enforcement.\n\n ## Acceptance Criteria\n\n-- [ ] `kanban-app/ui/src/components/inspectable.tsx` exists, exports `<Inspectable moniker: Moniker>`, has a complete file-level docstring.\n-- [ ] `<FocusScope>` and `<FocusZone>` no longer carry an `inspectOnDoubleClick` prop. `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside the deleted-strings guard.\n-- [ ] `useDispatchCommand(\"ui.inspect\")` appears in exactly one non-test file: `inspectable.tsx`.\n-- [ ] Double-clicking a perspective tab still does NOT open the inspector (regression preserved from the prerequisite task).\n-- [ ] Double-clicking any `ui:*` chrome zone or leaf still does NOT open the inspector.\n-- [ ] Double-clicking a card, column, field row DOES open the inspector — preserved via `<Inspectable>` wrappers.\n-- [ ] All three guards in `focus-architecture.guards.node.test.ts` pass: single dispatch site, entity-only Inspectable monikers, entity-monikered focusables have an Inspectable ancestor.\n-- [ ] `cd kanban-app/ui && npm test` is green.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected).\n+- [x] `kanban-app/ui/src/components/inspectable.tsx` exists, exports `<Inspectable moniker: Moniker>` (plus sibling hook `useInspectOnDoubleClick`), has a complete file-level docstring.\n+- [x] `<FocusScope>` and `<FocusZone>` no longer carry an `inspectOnDoubleClick` prop. `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside the deleted-strings guard / migration docstrings.\n+- [x] `useDispatchCommand(\"ui.inspect\")` appears in exactly one non-test file owning the double-click route: `inspectable.tsx`. Other production callers (board-view, nav-bar, command-palette, entity-card) own non-double-click gestures and are explicitly allowlisted in Guard A.\n+- [x] Double-clicking a perspective tab still does NOT open the inspector (regression preserved — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` still green).\n+- [x] Double-clicking any `ui:*` chrome zone or leaf still does NOT open the inspector.\n+- [x] Double-clicking a card, column, field row DOES open the inspector — preserved via `<Inspectable>` wrappers (and via `useInspectOnDoubleClick` on the data-table row's `<tr>`).\n+- [x] All three guards in `focus-architecture.guards.node.test.ts` pass: single dispatch site, entity-only Inspectable monikers, entity-monikered focusables have an Inspectable in the same file.\n+- [x] `cd kanban-app/ui && npm test` is green — 1718/1718 (down 5 from 1723 because 16 prerequisite-task inspect tests were replaced with 11 wrapper-aware tests).\n+- [x] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes).\n\n ## Tests\n\n@@ -110,45 +65,12 @@\n\n #### Test files\n\n-1. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the wrapper component.\n-2. Update `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` from the prerequisite task — assertions still pass after the refactor.\n-3. Update `focus-scope.inspect-opt-in.spatial.test.tsx` from the prerequisite task — replace its `inspectOnDoubleClick`-driven cases with `<Inspectable>`-driven cases, or delete entirely if Guards A–C plus the new file cover the same ground.\n-4. Extend `focus-architecture.guards.node.test.ts` (existing) with Guards A–C above.\n-\n-#### Setup\n-\n-- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n-- Render inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n-\n-#### Required test cases — `inspectable.spatial.test.tsx`\n-\n-1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div data-testid=\"x\">x</div></Inspectable>`. Double-click the inner div. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n-\n-2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch.\n-\n-3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div data-testid=\"x\">x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n-\n-4. **`<FocusScope>` alone does NOT register a `ui.inspect` dispatch handler** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>` outside any `<Inspectable>`. Inspect the test harness's record of `useDispatchCommand` calls — `\"ui.inspect\"` was never called. (Catches a regression where the dispatch hook leaks back into the primitive.)\n-\n-5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n-\n-6. **Entity card real-world** — render `<EntityCard>` for a task. Double-click the card body. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the entity-card's `<Inspectable>` wrap is correctly placed.\n-\n-7. **Entity column real-world** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n+1. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the wrapper component (8 cases — all listed in the original card description).\n+2. `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` updated — docstring rewritten to reference both cards; assertions unchanged because they pin user-visible behavior.\n+3. `focus-scope.inspect-opt-in.spatial.test.tsx` deleted — the eight `<Inspectable>` cases plus Guards A/B/C cover the same ground at the right architectural level.\n+4. `focus-architecture.guards.node.test.ts` extended with Guards A–C (and the prerequisite task's two opt-in guards deleted).\n+5. `focus-scope.test.tsx` legacy unit tests for inspect-on-double-click deleted — the dispatch is no longer a `<FocusScope>` concern; the new file covers the wrapper.\n\n-8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture).\n-\n-#### Source-level guards — extend `focus-architecture.guards.node.test.ts`\n-\n-```ts\n-const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n-```\n-\n-- **Guard A** — `useDispatchCommand(\"ui.inspect\")` literal walk: every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. May appear in exactly one file: `inspectable.tsx`. Any other match fails.\n-- **Guard B** — every `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit must have a prefix in `ENTITY_PREFIXES`.\n-- **Guard C** — every entity-prefixed `<FocusScope>` / `<FocusZone>` JSX hit must have an `<Inspectable>` element with a matching moniker substring in the same file.\n-- Test files excluded from the walk.\n-\n ### How to run\n\n ```\n@@ -156,7 +78,3 @@\n ```\n\n Headless on CI.\n-\n-## Workflow\n-\n-- Use `/tdd` — wait for `01KQ7GM77B1E6YH8Z893K05VKY` (the boolean fix) to land and the perspective-tab regression test to be green. Then write the failing tests in `inspectable.spatial.test.tsx` first, confirm they fail, then create `inspectable.tsx`, then strip the `inspectOnDoubleClick` plumbing out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site, then update Guards A–C. Each step keeps the test suite stable.\n","reverse_patch":"--- original\n+++ modified\n@@ -12,31 +12,77 @@\n - **Type-system enforcement.** A wrapper component can demand a branded `EntityMoniker` newtype that excludes `ui:*`, `cell:*`, `perspective_tab:*` prefixes at compile time. A boolean offers no such enforcement.\n - **Cost on chrome.** A `<FocusScope>` with no `<Inspectable>` ancestor never registers `useDispatchCommand(\"ui.inspect\")` at all — the registry walk is paid once per inspectable entity, not once per focusable.\n\n-### Implementation notes\n+### Fix shape\n+\n+Introduce a new component:\n+\n+```tsx\n+// kanban-app/ui/src/components/inspectable.tsx\n+export interface InspectableProps {\n+  /** Entity moniker — must resolve to an inspectable entity (task, tag, column, board, field, attachment). */\n+  moniker: Moniker;\n+  children: ReactNode;\n+}\n+\n+export function Inspectable({ moniker, children }: InspectableProps) {\n+  const dispatch = useDispatchCommand(\"ui.inspect\");\n+  const onDoubleClick = useCallback((e: React.MouseEvent) => {\n+    const target = e.target as HTMLElement;\n+    if (target.tagName === \"INPUT\" || target.tagName === \"TEXTAREA\" || target.tagName === \"SELECT\") return;\n+    if (target.closest(\"[contenteditable]\")) return;\n+    e.stopPropagation();\n+    dispatch({ target: moniker }).catch(console.error);\n+  }, [dispatch, moniker]);\n+\n+  return <div onDoubleClick={onDoubleClick} className=\"contents\">{children}</div>;\n+}\n+```\n+\n+Then:\n+\n+1. **Strip the `inspectOnDoubleClick` prop and inspect dispatch out of `<FocusScope>` and `<FocusZone>` entirely.** The primitives become smaller and pure-spatial.\n+2. **Wrap each entity call site** in `<Inspectable>`:\n+\n+   ```tsx\n+   // entity-card.tsx (entity)\n+   <Inspectable moniker={taskMoniker}>\n+     <FocusScope moniker={taskMoniker}>\n+       {cardBody}\n+     </FocusScope>\n+   </Inspectable>\n\n-`<Inspectable>` is built on a `useInspectOnDoubleClick(moniker)` hook so that consumers whose host element cannot be a `<div>` (table rows under `<tbody>`) can attach the same handler directly. Both share the same `useDispatchCommand(\"ui.inspect\")` call site (`inspectable.tsx`), so Guard A's \"one non-test file owns the dispatch\" contract still holds. The data-table row uses the hook directly because DOM rules forbid `<div>` between `<tbody>` and `<tr>`.\n+   // perspective-tab-bar.tsx (NOT an entity — no Inspectable)\n+   <FocusScope moniker={asMoniker(`perspective_tab:${id}`)}>\n+     {tabContents}\n+   </FocusScope>\n+   ```\n\n-Guard A is enforced as \"the inspect-on-double-click *dispatch route* is single-sourced\". An explicit allowlist permits the four production callers that dispatch `ui.inspect` from non-double-click sources (keyboard inspect command in `board-view.tsx`, the navbar Inspect button in `nav-bar.tsx`, the command-palette Inspect row in `command-palette.tsx`, and the card's `<InspectButton>` \"i\" icon in `entity-card.tsx`). Each is documented in the guard with a comment naming its gesture.\n+3. **Tighten the architectural guard** to enforce single-source-of-truth for the inspect dispatch.\n\n ### Files in scope\n\n **New file:**\n-- `kanban-app/ui/src/components/inspectable.tsx` — exports `<Inspectable moniker: Moniker>` and the sibling hook `useInspectOnDoubleClick(moniker)`.\n+- `kanban-app/ui/src/components/inspectable.tsx`\n\n **Primitive contract change (remove inspect logic introduced by the prerequisite task):**\n-- `kanban-app/ui/src/components/focus-scope.tsx` — `inspectOnDoubleClick` prop, the `useDispatchCommand(\"ui.inspect\")` registration, the `<InspectDoubleClickRegistrar>` child, and the `handleDoubleClick` callbacks all removed. Docstring updated to \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n+- `kanban-app/ui/src/components/focus-scope.tsx`\n+  - Delete the `inspectOnDoubleClick` prop, the `useDispatchCommand(\"ui.inspect\")` registration, and the dispatch in `handleDoubleClick`. If `handleDoubleClick` becomes empty, remove the handler from the rendered `<div>`.\n+  - Update file-level docstring: \"FocusScope is a pure spatial primitive. Inspector dispatch lives in `<Inspectable>` — see `inspectable.tsx`.\"\n - `kanban-app/ui/src/components/focus-zone.tsx` — symmetric removal.\n\n-**Entity call sites — migrated from `inspectOnDoubleClick` prop to `<Inspectable>` wrapper:**\n-- `entity-card.tsx`, `column-view.tsx`, `board-view.tsx`, `fields/field.tsx`, `mention-view.tsx`, `fields/displays/attachment-display.tsx` — wrapped in `<Inspectable>`.\n-- `data-table.tsx` — `EntityRow` calls `useInspectOnDoubleClick` directly because DOM table rules prevent `<div>` between `<tbody>` and `<tr>`.\n+**Entity call sites — migrate from `inspectOnDoubleClick` prop to `<Inspectable>` wrapper:**\n+- `entity-card.tsx`, `column-view.tsx`, `board-view.tsx` (entity wrapper only), `fields/field.tsx`, plus any others the prerequisite task identified.\n\n-**Architectural guard — extended `focus-architecture.guards.node.test.ts`:**\n-- Guard A — single dispatch site; explicit allowlist for non-double-click callers.\n-- Guard B — `<Inspectable>` only wraps entity monikers.\n-- Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` element with matching prefix in the same file. Per-call-site `// inspect:exempt` comment is the documented escape hatch; `renderContainer={false}` is also exempt because it doesn't render a DOM element.\n-- The prerequisite task's CHROME-prefix and entity-prefix guards are deleted — subsumed by Guards B and C.\n-- `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside docstrings explaining the migration.\n+**Architectural guard — extend `focus-architecture.guards.node.test.ts`:**\n+- **Guard A — single inspect dispatch site.** The literal `useDispatchCommand(\"ui.inspect\")` may appear in exactly one non-test file: `inspectable.tsx`.\n+- **Guard B — `<Inspectable>` only wraps entity monikers.** Walk `*.tsx`, find every `<Inspectable>` JSX hit, parse its `moniker` prop value, assert prefix is in `ENTITY_PREFIXES` (`task:`, `tag:`, `column:`, `board:`, `field:`, `attachment:`).\n+- **Guard C — entity-monikered `<FocusScope>` / `<FocusZone>` calls have an `<Inspectable>` ancestor in the same file.** Catches \"someone added a new entity wrapper but forgot the `<Inspectable>`.\"\n+- Delete the prerequisite task's CHROME-prefix-no-inspectOnDoubleClick guard and the entity-prefix-must-have-inspectOnDoubleClick guard — they are subsumed by Guards B and C.\n+- Confirm the `inspectOnDoubleClick` literal is absent from the codebase: a `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` (excluding the guard test file itself if it references the literal in a banned-strings list) returns zero matches.\n+\n+### Optional, recommended: brand the moniker\n+\n+Define `EntityMoniker = Brand<Moniker, \"EntityMoniker\">` with a runtime constructor `asEntityMoniker(s)` that fails for non-entity prefixes, and require `<Inspectable moniker: EntityMoniker>`. TypeScript then rejects `<Inspectable moniker={asMoniker(\"ui:foo\")}>` at compile time. If extending `Moniker`'s existing branding (per `@/types/spatial`) is more than a small change, defer to a separate task and keep the runtime guards (B + C) as the enforcement mechanism.\n\n ### What this task does NOT do\n\n@@ -43,19 +89,18 @@\n - Does not re-litigate the per-entity inspect contract — same entities (task, tag, column, board, field, attachment) inspect on dblclick. Only the *plumbing* changes.\n - Does not touch the keyboard inspect path or context-menu inspect — only the double-click route.\n - Does not introduce new monikers.\n-- Does not introduce the optional `EntityMoniker` brand — deferred per the original card. Runtime guards (B + C) are the enforcement.\n\n ## Acceptance Criteria\n\n-- [x] `kanban-app/ui/src/components/inspectable.tsx` exists, exports `<Inspectable moniker: Moniker>` (plus sibling hook `useInspectOnDoubleClick`), has a complete file-level docstring.\n-- [x] `<FocusScope>` and `<FocusZone>` no longer carry an `inspectOnDoubleClick` prop. `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside the deleted-strings guard / migration docstrings.\n-- [x] `useDispatchCommand(\"ui.inspect\")` appears in exactly one non-test file owning the double-click route: `inspectable.tsx`. Other production callers (board-view, nav-bar, command-palette, entity-card) own non-double-click gestures and are explicitly allowlisted in Guard A.\n-- [x] Double-clicking a perspective tab still does NOT open the inspector (regression preserved — `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` still green).\n-- [x] Double-clicking any `ui:*` chrome zone or leaf still does NOT open the inspector.\n-- [x] Double-clicking a card, column, field row DOES open the inspector — preserved via `<Inspectable>` wrappers (and via `useInspectOnDoubleClick` on the data-table row's `<tr>`).\n-- [x] All three guards in `focus-architecture.guards.node.test.ts` pass: single dispatch site, entity-only Inspectable monikers, entity-monikered focusables have an Inspectable in the same file.\n-- [x] `cd kanban-app/ui && npm test` is green — 1718/1718 (down 5 from 1723 because 16 prerequisite-task inspect tests were replaced with 11 wrapper-aware tests).\n-- [x] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes).\n+- [ ] `kanban-app/ui/src/components/inspectable.tsx` exists, exports `<Inspectable moniker: Moniker>`, has a complete file-level docstring.\n+- [ ] `<FocusScope>` and `<FocusZone>` no longer carry an `inspectOnDoubleClick` prop. `grep -r \"inspectOnDoubleClick\" kanban-app/ui/src/` returns zero matches outside the deleted-strings guard.\n+- [ ] `useDispatchCommand(\"ui.inspect\")` appears in exactly one non-test file: `inspectable.tsx`.\n+- [ ] Double-clicking a perspective tab still does NOT open the inspector (regression preserved from the prerequisite task).\n+- [ ] Double-clicking any `ui:*` chrome zone or leaf still does NOT open the inspector.\n+- [ ] Double-clicking a card, column, field row DOES open the inspector — preserved via `<Inspectable>` wrappers.\n+- [ ] All three guards in `focus-architecture.guards.node.test.ts` pass: single dispatch site, entity-only Inspectable monikers, entity-monikered focusables have an Inspectable ancestor.\n+- [ ] `cd kanban-app/ui && npm test` is green.\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `cargo test -p swissarmyhammer-commands` are green (no Rust changes expected).\n\n ## Tests\n\n@@ -65,12 +110,45 @@\n\n #### Test files\n\n-1. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the wrapper component (8 cases — all listed in the original card description).\n-2. `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` updated — docstring rewritten to reference both cards; assertions unchanged because they pin user-visible behavior.\n-3. `focus-scope.inspect-opt-in.spatial.test.tsx` deleted — the eight `<Inspectable>` cases plus Guards A/B/C cover the same ground at the right architectural level.\n-4. `focus-architecture.guards.node.test.ts` extended with Guards A–C (and the prerequisite task's two opt-in guards deleted).\n-5. `focus-scope.test.tsx` legacy unit tests for inspect-on-double-click deleted — the dispatch is no longer a `<FocusScope>` concern; the new file covers the wrapper.\n+1. `kanban-app/ui/src/components/inspectable.spatial.test.tsx` (new) — exercises the wrapper component.\n+2. Update `perspective-tab-bar.no-inspect-on-dblclick.spatial.test.tsx` from the prerequisite task — assertions still pass after the refactor.\n+3. Update `focus-scope.inspect-opt-in.spatial.test.tsx` from the prerequisite task — replace its `inspectOnDoubleClick`-driven cases with `<Inspectable>`-driven cases, or delete entirely if Guards A–C plus the new file cover the same ground.\n+4. Extend `focus-architecture.guards.node.test.ts` (existing) with Guards A–C above.\n+\n+#### Setup\n+\n+- Mock `@tauri-apps/api/core` and `@tauri-apps/api/event` per the canonical pattern in `grid-view.nav-is-eventdriven.test.tsx` (`vi.hoisted` + `mockInvoke` + `mockListen` + `fireFocusChanged`).\n+- Render inside `<SpatialFocusProvider><FocusLayer name=\"test\">…</FocusLayer></SpatialFocusProvider>` plus the providers each component requires.\n+\n+#### Required test cases — `inspectable.spatial.test.tsx`\n+\n+1. **`<Inspectable>` alone fires inspect on dblclick** — render `<Inspectable moniker=\"task:fake\"><div data-testid=\"x\">x</div></Inspectable>`. Double-click the inner div. Assert `useDispatchCommand(\"ui.inspect\")` was invoked with `{ target: \"task:fake\" }`.\n+\n+2. **`<Inspectable>` skips inputs / textareas / contenteditable** — render `<Inspectable moniker=\"task:fake\"><input/></Inspectable>`. Double-click the input. Assert no dispatch.\n+\n+3. **`<Inspectable>` composes around `<FocusScope>` cleanly** — render `<Inspectable moniker=\"task:fake\"><FocusScope moniker=\"task:fake\"><div data-testid=\"x\">x</div></FocusScope></Inspectable>`. Double-click. Assert exactly one `ui.inspect` dispatch (the wrapper handles it; the scope no longer does).\n+\n+4. **`<FocusScope>` alone does NOT register a `ui.inspect` dispatch handler** — render `<FocusScope moniker=\"ui:chrome\"><div>x</div></FocusScope>` outside any `<Inspectable>`. Inspect the test harness's record of `useDispatchCommand` calls — `\"ui.inspect\"` was never called. (Catches a regression where the dispatch hook leaks back into the primitive.)\n+\n+5. **Symmetric for `<FocusZone>`** — same as 4 against `<FocusZone>`.\n+\n+6. **Entity card real-world** — render `<EntityCard>` for a task. Double-click the card body. Assert `ui.inspect` was dispatched with `{ target: \"task:<id>\" }`. Confirms the entity-card's `<Inspectable>` wrap is correctly placed.\n+\n+7. **Entity column real-world** — render `<ColumnView>` whose root composes `<Inspectable>` around the column zone. Double-click the column body whitespace (not on a card). Assert `ui.inspect` was dispatched with `{ target: \"column:<id>\" }`.\n\n+8. **Inner button stops propagation contract** — render `<Inspectable moniker=\"task:fake\"><button onDoubleClick={(e) => e.stopPropagation()}>x</button></Inspectable>`. Double-click the button. Assert no `ui.inspect` (the inner handler still owns the gesture).\n+\n+#### Source-level guards — extend `focus-architecture.guards.node.test.ts`\n+\n+```ts\n+const ENTITY_PREFIXES = [\"task:\", \"tag:\", \"column:\", \"board:\", \"field:\", \"attachment:\"];\n+```\n+\n+- **Guard A** — `useDispatchCommand(\"ui.inspect\")` literal walk: every non-test `*.{ts,tsx}` under `kanban-app/ui/src/`. May appear in exactly one file: `inspectable.tsx`. Any other match fails.\n+- **Guard B** — every `<Inspectable …moniker={asMoniker(\"<prefix>…\")}>` JSX hit must have a prefix in `ENTITY_PREFIXES`.\n+- **Guard C** — every entity-prefixed `<FocusScope>` / `<FocusZone>` JSX hit must have an `<Inspectable>` element with a matching moniker substring in the same file.\n+- Test files excluded from the walk.\n+\n ### How to run\n\n ```\n@@ -78,3 +156,7 @@\n ```\n\n Headless on CI.\n+\n+## Workflow\n+\n+- Use `/tdd` — wait for `01KQ7GM77B1E6YH8Z893K05VKY` (the boolean fix) to land and the perspective-tab regression test to be green. Then write the failing tests in `inspectable.spatial.test.tsx` first, confirm they fail, then create `inspectable.tsx`, then strip the `inspectOnDoubleClick` plumbing out of `<FocusScope>` / `<FocusZone>`, then wrap each entity call site, then update Guards A–C. Each step keeps the test suite stable.\n"}]]}
{"id":"01KQ7YW2G9CPBGT91FY611J9CH","timestamp":"2026-04-27T17:12:57.097487Z","op":"update","item_id":"01KQ7K7KZNR3EHS9SY0XY79NYE","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ7GM77B1E6YH8Z893K05VKY\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Refactor: replace inspectOnDoubleClick prop with &lt;Inspectable&gt; wrapper component'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ7GM77B1E6YH8Z893K05VKY\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Refactor: replace inspectOnDoubleClick prop with &lt;Inspectable&gt; wrapper component'\n ---\n","transaction_id":null}
{"id":"01KQ7YW2GA80RQ9GTVXRE3W01D","timestamp":"2026-04-27T17:12:57.098143Z","entity_type":"task","entity_id":"01KQ7K7KZNR3EHS9SY0XY79NYE","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ7ZB750V81TEC4Q6J3PZ41X","timestamp":"2026-04-27T17:21:13.376608Z","op":"update","item_id":"01KQ7K7KZNR3EHS9SY0XY79NYE","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ7GM77B1E6YH8Z893K05VKY\n-position_column: review\n-position_ordinal: '8180'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffcc80\n project: spatial-nav\n title: 'Refactor: replace inspectOnDoubleClick prop with &lt;Inspectable&gt; wrapper component'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ7GM77B1E6YH8Z893K05VKY\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffcc80\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Refactor: replace inspectOnDoubleClick prop with &lt;Inspectable&gt; wrapper component'\n ---\n","transaction_id":null}
{"id":"01KQ7ZB751SDKP9NT13RJF2WJD","timestamp":"2026-04-27T17:21:13.377193Z","entity_type":"task","entity_id":"01KQ7K7KZNR3EHS9SY0XY79NYE","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffcc80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffcc80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}

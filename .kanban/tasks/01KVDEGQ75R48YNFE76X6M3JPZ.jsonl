{"id":"01KVDEGQ754VHE8GRWSWSC9E71","timestamp":"2026-06-18T13:25:21.765772Z","op":"create","item_id":"01KVDEGQ75R48YNFE76X6M3JPZ","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,34 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: b080\n+project: diagnostics\n+title: 'No orphaned rust-analyzer: kill-on-exit for the owned LSP child + remove the start_lsp_server spawn-to-verify leak'\n+---\n+## Why (evidence, 2026-06-18)\n+Inspecting the running machine found **98 live `rust-analyzer` servers, 96 of them orphaned** (PPID=1, reparented to launchd) — their owning process exited but the server was never killed. Only 2 were still children of live `sah serve`. This is a process-lifecycle leak, **distinct from** the cross-process arbitration tasks (`^7a5h2bj` leader-per-workdir, `^ref6nj4` multiplexer): orphans accumulate even with correct leader gating, and the dominant source is a probe that spawns with no leader involved at all.\n+\n+## Two confirmed root causes\n+1. **`start_lsp_server` / `spawn_and_verify` is a spawn-to-verify leak.** `crates/swissarmyhammer-lsp/src/server_spec.rs` — `spawn_and_verify` launches a real `rust-analyzer`, `sleep(100ms)`, calls `try_wait()`, returns `Result<(), LspError>`, and **drops the `std::process::Child` without killing it**. The verified server is never attached to anything → orphaned every call. `start_lsp_server` returns only `LspServerHandle { started: true }`. It is called from `server_spec` unit tests and `code-context/tests/integration_test.rs` (and re-exported), so **every `cargo test` run leaks one or more** — this, plus the wedged test runs, explains the bulk of the 96.\n+2. **No kill-on-exit on the synchronous client path.** `LspJsonRpcClient::new(stdin, stdout)` only borrows the pipes — its doc says *\"The caller retains ownership of the `Child` handle for lifecycle management.\"* The async `LspDaemon` path uses `.kill_on_drop(true)` + explicit `child.kill()`, but the std `Child` held by the session/supervisor caller has no Drop/kill, so on process exit (or SIGKILL of `sah serve`) the server orphans.\n+\n+## What\n+- Replace the spawn-to-verify availability check with a **non-spawning** probe: resolve the executable on disk/PATH (we already have `resolve_executable`/`find_executable`/`detect_rust_analyzer`) and report availability without launching a throwaway server. If a liveness spawn is truly required, it must own the child and kill it before returning.\n+- Guarantee the **one owned session child is killed when the session/owning process drops** — either route the production session through the `kill_on_drop`-managed path, or give the std-`Child` holder an explicit Drop that kills+reaps. No rust-analyzer should outlive the process that spawned it.\n+- Make the tests that call `start_lsp_server` / spawn real servers **not leak** (own + kill the handle, mirroring the `MockLsp` kill-on-drop guard already added to code-context test_fixtures).\n+\n+## Scope boundary (avoid overlap)\n+- `^7a5h2bj` owns the *leader-handoff* orphan case (try_promote re-spawn). This task owns the **probe leak + general kill-on-exit + test hygiene**, which apply with zero leaders involved. Cross-reference, do not duplicate.\n+\n+## Acceptance Criteria\n+- [ ] Availability/\"can we start rust-analyzer\" checks do not spawn a throwaway server (or own+kill it if they must).\n+- [ ] The owned LSP child is killed+reaped on session drop / process exit; nothing reparents to launchd.\n+- [ ] Tests exercising real LSP servers leave **zero** orphaned `rust-analyzer` after the run.\n+\n+## Tests\n+- [ ] Unit: a probe/availability check spawns no surviving child (assert process count unchanged, or that the returned handle owns a child that is killed on drop).\n+- [ ] Regression: after `cargo test -p swissarmyhammer-lsp` and `-p swissarmyhammer-code-context`, `ps -eo ppid,command | grep bin/rust-analyzer | grep -v proc-macro` shows no PPID=1 orphans attributable to the run.\n+\n+## Workflow\n+Use `/tdd`. #diagnostics\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,34 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: b080\n-project: diagnostics\n-title: 'No orphaned rust-analyzer: kill-on-exit for the owned LSP child + remove the start_lsp_server spawn-to-verify leak'\n----\n-## Why (evidence, 2026-06-18)\n-Inspecting the running machine found **98 live `rust-analyzer` servers, 96 of them orphaned** (PPID=1, reparented to launchd) — their owning process exited but the server was never killed. Only 2 were still children of live `sah serve`. This is a process-lifecycle leak, **distinct from** the cross-process arbitration tasks (`^7a5h2bj` leader-per-workdir, `^ref6nj4` multiplexer): orphans accumulate even with correct leader gating, and the dominant source is a probe that spawns with no leader involved at all.\n-\n-## Two confirmed root causes\n-1. **`start_lsp_server` / `spawn_and_verify` is a spawn-to-verify leak.** `crates/swissarmyhammer-lsp/src/server_spec.rs` — `spawn_and_verify` launches a real `rust-analyzer`, `sleep(100ms)`, calls `try_wait()`, returns `Result<(), LspError>`, and **drops the `std::process::Child` without killing it**. The verified server is never attached to anything → orphaned every call. `start_lsp_server` returns only `LspServerHandle { started: true }`. It is called from `server_spec` unit tests and `code-context/tests/integration_test.rs` (and re-exported), so **every `cargo test` run leaks one or more** — this, plus the wedged test runs, explains the bulk of the 96.\n-2. **No kill-on-exit on the synchronous client path.** `LspJsonRpcClient::new(stdin, stdout)` only borrows the pipes — its doc says *\"The caller retains ownership of the `Child` handle for lifecycle management.\"* The async `LspDaemon` path uses `.kill_on_drop(true)` + explicit `child.kill()`, but the std `Child` held by the session/supervisor caller has no Drop/kill, so on process exit (or SIGKILL of `sah serve`) the server orphans.\n-\n-## What\n-- Replace the spawn-to-verify availability check with a **non-spawning** probe: resolve the executable on disk/PATH (we already have `resolve_executable`/`find_executable`/`detect_rust_analyzer`) and report availability without launching a throwaway server. If a liveness spawn is truly required, it must own the child and kill it before returning.\n-- Guarantee the **one owned session child is killed when the session/owning process drops** — either route the production session through the `kill_on_drop`-managed path, or give the std-`Child` holder an explicit Drop that kills+reaps. No rust-analyzer should outlive the process that spawned it.\n-- Make the tests that call `start_lsp_server` / spawn real servers **not leak** (own + kill the handle, mirroring the `MockLsp` kill-on-drop guard already added to code-context test_fixtures).\n-\n-## Scope boundary (avoid overlap)\n-- `^7a5h2bj` owns the *leader-handoff* orphan case (try_promote re-spawn). This task owns the **probe leak + general kill-on-exit + test hygiene**, which apply with zero leaders involved. Cross-reference, do not duplicate.\n-\n-## Acceptance Criteria\n-- [ ] Availability/\"can we start rust-analyzer\" checks do not spawn a throwaway server (or own+kill it if they must).\n-- [ ] The owned LSP child is killed+reaped on session drop / process exit; nothing reparents to launchd.\n-- [ ] Tests exercising real LSP servers leave **zero** orphaned `rust-analyzer` after the run.\n-\n-## Tests\n-- [ ] Unit: a probe/availability check spawns no surviving child (assert process count unchanged, or that the returned handle owns a child that is killed on drop).\n-- [ ] Regression: after `cargo test -p swissarmyhammer-lsp` and `-p swissarmyhammer-code-context`, `ps -eo ppid,command | grep bin/rust-analyzer | grep -v proc-macro` shows no PPID=1 orphans attributable to the run.\n-\n-## Workflow\n-Use `/tdd`. #diagnostics\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KVEV0WKYRWBY56WQNMABY9C5","timestamp":"2026-06-19T02:23:08.926815Z","op":"update","item_id":"01KVDEGQ75R48YNFE76X6M3JPZ","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: b080\n+position_column: doing\n+position_ordinal: '8180'\n project: diagnostics\n title: 'No orphaned rust-analyzer: kill-on-exit for the owned LSP child + remove the start_lsp_server spawn-to-verify leak'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: todo\n+position_ordinal: b080\n project: diagnostics\n title: 'No orphaned rust-analyzer: kill-on-exit for the owned LSP child + remove the start_lsp_server spawn-to-verify leak'\n ---\n","transaction_id":null}
{"id":"01KVEV11K9FB2VZK7AYGM093YZ","timestamp":"2026-06-19T02:23:14.025123Z","op":"update","item_id":"01KVDEGQ75R48YNFE76X6M3JPZ","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,11 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01kvev11k8k4w2cq488bw0pk4h\n+  text: 'Picked up by /finish $diagnostics, after ^affvtqf → done. Process-lifecycle leak fix (96 orphaned rust-analyzers evidenced), distinct from ^7a5h2bj (leader-handoff orphan, already done). Two root causes: (1) server_spec.rs spawn_and_verify launches rust-analyzer, sleep+try_wait, drops std::process::Child WITHOUT killing → orphan every call (called from server_spec unit tests + code-context integration_test.rs, so every cargo test leaks). Fix = non-spawning probe via resolve_executable/find_executable/detect_rust_analyzer (own+kill if a liveness spawn is truly required). (2) LspJsonRpcClient::new only borrows pipes; the std Child held by session/supervisor has no Drop/kill (async LspDaemon already uses kill_on_drop(true)+child.kill()). Fix = guarantee the one owned session child is killed+reaped on session/process drop (route through kill_on_drop path or give the std-Child holder an explicit Drop that kills+reaps). (3) Tests calling start_lsp_server must not leak — mirror the MockLsp kill-on-drop guard added to code-context test_fixtures. Tests: unit (probe spawns no surviving child) + regression (no PPID=1 orphans after cargo test -p swissarmyhammer-lsp / -p swissarmyhammer-code-context). Driving implement → test → review.'\n+  timestamp: 2026-06-19T02:23:14.024816+00:00\n position_column: doing\n position_ordinal: '8180'\n project: diagnostics\n","reverse_patch":"--- original\n+++ modified\n@@ -1,11 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01kvev11k8k4w2cq488bw0pk4h\n-  text: 'Picked up by /finish $diagnostics, after ^affvtqf → done. Process-lifecycle leak fix (96 orphaned rust-analyzers evidenced), distinct from ^7a5h2bj (leader-handoff orphan, already done). Two root causes: (1) server_spec.rs spawn_and_verify launches rust-analyzer, sleep+try_wait, drops std::process::Child WITHOUT killing → orphan every call (called from server_spec unit tests + code-context integration_test.rs, so every cargo test leaks). Fix = non-spawning probe via resolve_executable/find_executable/detect_rust_analyzer (own+kill if a liveness spawn is truly required). (2) LspJsonRpcClient::new only borrows pipes; the std Child held by session/supervisor has no Drop/kill (async LspDaemon already uses kill_on_drop(true)+child.kill()). Fix = guarantee the one owned session child is killed+reaped on session/process drop (route through kill_on_drop path or give the std-Child holder an explicit Drop that kills+reaps). (3) Tests calling start_lsp_server must not leak — mirror the MockLsp kill-on-drop guard added to code-context test_fixtures. Tests: unit (probe spawns no surviving child) + regression (no PPID=1 orphans after cargo test -p swissarmyhammer-lsp / -p swissarmyhammer-code-context). Driving implement → test → review.'\n-  timestamp: 2026-06-19T02:23:14.024816+00:00\n position_column: doing\n position_ordinal: '8180'\n project: diagnostics\n","transaction_id":null}
{"id":"01KVEV5M47HHKKZEKDQH4F3CWQ","timestamp":"2026-06-19T02:25:44.071665Z","op":"update","item_id":"01KVDEGQ75R48YNFE76X6M3JPZ","forward_patch":"--- original\n+++ modified\n@@ -6,6 +6,10 @@\n   id: 01kvev11k8k4w2cq488bw0pk4h\n   text: 'Picked up by /finish $diagnostics, after ^affvtqf → done. Process-lifecycle leak fix (96 orphaned rust-analyzers evidenced), distinct from ^7a5h2bj (leader-handoff orphan, already done). Two root causes: (1) server_spec.rs spawn_and_verify launches rust-analyzer, sleep+try_wait, drops std::process::Child WITHOUT killing → orphan every call (called from server_spec unit tests + code-context integration_test.rs, so every cargo test leaks). Fix = non-spawning probe via resolve_executable/find_executable/detect_rust_analyzer (own+kill if a liveness spawn is truly required). (2) LspJsonRpcClient::new only borrows pipes; the std Child held by session/supervisor has no Drop/kill (async LspDaemon already uses kill_on_drop(true)+child.kill()). Fix = guarantee the one owned session child is killed+reaped on session/process drop (route through kill_on_drop path or give the std-Child holder an explicit Drop that kills+reaps). (3) Tests calling start_lsp_server must not leak — mirror the MockLsp kill-on-drop guard added to code-context test_fixtures. Tests: unit (probe spawns no surviving child) + regression (no PPID=1 orphans after cargo test -p swissarmyhammer-lsp / -p swissarmyhammer-code-context). Driving implement → test → review.'\n   timestamp: 2026-06-19T02:23:14.024816+00:00\n+- actor: claude-code\n+  id: 01kvev5m47knca044hr4w7mhfs\n+  text: 'Research done. Production lifecycle is already kill-on-drop-safe: the ONLY production holder of an LSP child is LspDaemon.child: Option<tokio::process::Child> spawned with .kill_on_drop(true) (daemon.rs start()), with graceful_shutdown + take()-based reaping in shutdown(). The std pipes handed to LspJsonRpcClient::new are extracted from that same tokio Child, which remains the owner — so there is NO separate production std-Child session holder to leak (root cause 2''s \"synchronous session caller\" is tests only; supervisor owns daemons in a HashMap, so supervisor drop -> daemon drop -> tokio kill_on_drop). The dominant real leak is root cause 1: server_spec.rs spawn_and_verify spawns a real rust-analyzer (via start_lsp_server(\"rust\",...)), sleeps 100ms, try_wait, and DROPS the std::process::Child without kill -> orphan. start_lsp_server is called ONLY from tests (server_spec unit tests + code-context integration_test.rs test_lsp_server_startup); no production caller. Plan: convert the availability check to a NON-SPAWNING probe (resolve_executable on disk/PATH), and fix server_spec unit tests that spawn long-lived real processes (sleep/rust-analyzer) to not leak. Mock python LSP spawns in lsp_communication/lsp_worker test mods are self-exiting python, not rust-analyzer; canonical MockLsp kill-on-drop guard already lives in code-context test_fixtures.'\n+  timestamp: 2026-06-19T02:25:44.071331+00:00\n position_column: doing\n position_ordinal: '8180'\n project: diagnostics\n","reverse_patch":"--- original\n+++ modified\n@@ -6,10 +6,6 @@\n   id: 01kvev11k8k4w2cq488bw0pk4h\n   text: 'Picked up by /finish $diagnostics, after ^affvtqf → done. Process-lifecycle leak fix (96 orphaned rust-analyzers evidenced), distinct from ^7a5h2bj (leader-handoff orphan, already done). Two root causes: (1) server_spec.rs spawn_and_verify launches rust-analyzer, sleep+try_wait, drops std::process::Child WITHOUT killing → orphan every call (called from server_spec unit tests + code-context integration_test.rs, so every cargo test leaks). Fix = non-spawning probe via resolve_executable/find_executable/detect_rust_analyzer (own+kill if a liveness spawn is truly required). (2) LspJsonRpcClient::new only borrows pipes; the std Child held by session/supervisor has no Drop/kill (async LspDaemon already uses kill_on_drop(true)+child.kill()). Fix = guarantee the one owned session child is killed+reaped on session/process drop (route through kill_on_drop path or give the std-Child holder an explicit Drop that kills+reaps). (3) Tests calling start_lsp_server must not leak — mirror the MockLsp kill-on-drop guard added to code-context test_fixtures. Tests: unit (probe spawns no surviving child) + regression (no PPID=1 orphans after cargo test -p swissarmyhammer-lsp / -p swissarmyhammer-code-context). Driving implement → test → review.'\n   timestamp: 2026-06-19T02:23:14.024816+00:00\n-- actor: claude-code\n-  id: 01kvev5m47knca044hr4w7mhfs\n-  text: 'Research done. Production lifecycle is already kill-on-drop-safe: the ONLY production holder of an LSP child is LspDaemon.child: Option<tokio::process::Child> spawned with .kill_on_drop(true) (daemon.rs start()), with graceful_shutdown + take()-based reaping in shutdown(). The std pipes handed to LspJsonRpcClient::new are extracted from that same tokio Child, which remains the owner — so there is NO separate production std-Child session holder to leak (root cause 2''s \"synchronous session caller\" is tests only; supervisor owns daemons in a HashMap, so supervisor drop -> daemon drop -> tokio kill_on_drop). The dominant real leak is root cause 1: server_spec.rs spawn_and_verify spawns a real rust-analyzer (via start_lsp_server(\"rust\",...)), sleeps 100ms, try_wait, and DROPS the std::process::Child without kill -> orphan. start_lsp_server is called ONLY from tests (server_spec unit tests + code-context integration_test.rs test_lsp_server_startup); no production caller. Plan: convert the availability check to a NON-SPAWNING probe (resolve_executable on disk/PATH), and fix server_spec unit tests that spawn long-lived real processes (sleep/rust-analyzer) to not leak. Mock python LSP spawns in lsp_communication/lsp_worker test mods are self-exiting python, not rust-analyzer; canonical MockLsp kill-on-drop guard already lives in code-context test_fixtures.'\n-  timestamp: 2026-06-19T02:25:44.071331+00:00\n position_column: doing\n position_ordinal: '8180'\n project: diagnostics\n","transaction_id":null}
{"id":"01KVEVRRX8EGBT5FE58Q37E2VX","timestamp":"2026-06-19T02:36:11.560531Z","op":"update","item_id":"01KVDEGQ75R48YNFE76X6M3JPZ","forward_patch":"--- original\n+++ modified\n@@ -10,6 +10,17 @@\n   id: 01kvev5m47knca044hr4w7mhfs\n   text: 'Research done. Production lifecycle is already kill-on-drop-safe: the ONLY production holder of an LSP child is LspDaemon.child: Option<tokio::process::Child> spawned with .kill_on_drop(true) (daemon.rs start()), with graceful_shutdown + take()-based reaping in shutdown(). The std pipes handed to LspJsonRpcClient::new are extracted from that same tokio Child, which remains the owner — so there is NO separate production std-Child session holder to leak (root cause 2''s \"synchronous session caller\" is tests only; supervisor owns daemons in a HashMap, so supervisor drop -> daemon drop -> tokio kill_on_drop). The dominant real leak is root cause 1: server_spec.rs spawn_and_verify spawns a real rust-analyzer (via start_lsp_server(\"rust\",...)), sleeps 100ms, try_wait, and DROPS the std::process::Child without kill -> orphan. start_lsp_server is called ONLY from tests (server_spec unit tests + code-context integration_test.rs test_lsp_server_startup); no production caller. Plan: convert the availability check to a NON-SPAWNING probe (resolve_executable on disk/PATH), and fix server_spec unit tests that spawn long-lived real processes (sleep/rust-analyzer) to not leak. Mock python LSP spawns in lsp_communication/lsp_worker test mods are self-exiting python, not rust-analyzer; canonical MockLsp kill-on-drop guard already lives in code-context test_fixtures.'\n   timestamp: 2026-06-19T02:25:44.071331+00:00\n+- actor: claude-code\n+  id: 01kvevrrx87s28w18jaaf8hw6v\n+  text: |-\n+    Implementation landed (TDD). Probe redesign: server_spec.rs spawn_and_verify (the spawn-to-verify leak) DELETED; the private spawn_server is replaced by NON-SPAWNING verify_server_available(config) which only resolves the executable on disk/PATH (resolve_executable -> find_executable) and returns Ok(()) if it resolves — no Command::spawn, no throwaway rust-analyzer. start_lsp_server keeps its (language, project_root) signature + LspServerHandle{started,error} availability contract (started=available, error=why-not); project_root is now _project_root (kept for API compat, probe needs no cwd). Rewrote the obsolete spawn-based unit tests (the sleep/true immediate-exit ones, which themselves leaked) to assert the non-spawning availability contract.\n+\n+    Kill-on-drop guarantee: production was ALREADY safe — the sole production LSP-child holder is LspDaemon.child: Option<tokio::process::Child> spawned with .kill_on_drop(true) (daemon.rs start()), with graceful_shutdown + take()-based reaping in shutdown(); the std pipes given to LspJsonRpcClient::new are extracted from that same tokio Child which remains the owner, and the supervisor owns daemons in a HashMap (supervisor drop -> daemon drop -> kill_on_drop). No separate production std-Child session holder exists, so root cause 2 needed no production code change. Test hygiene (root cause 3): promoted the existing MockLsp kill-on-drop guard into a single pub guard crate::testing::KillOnDrop (Deref/DerefMut/Drop kill+wait); test_fixtures::MockLsp is now `pub use ...KillOnDrop as MockLsp` (removed the duplicated Drop impl — ONE mechanism, no duplicate). Wrapped every real-rust-analyzer Command::new(\"rust-analyzer\").spawn() child in KillOnDrop in code-context/tests/integration_test.rs (3 sites) and swissarmyhammer-tools/tests/integration/code_context_ops_e2e.rs (1 site) so a panicking assertion can no longer leak them.\n+\n+    Regression: new unit test test_start_lsp_server_does_not_spawn_rust_analyzer asserts the live rust-analyzer count never RISES across start_lsp_server (after <= before tolerates unrelated churn-down). Red-green-red verified: a spawning probe -> FAIL (before=2, after=3); the non-spawning probe -> pass.\n+\n+    Verification: cargo test -p swissarmyhammer-lsp = 222 passed / 0 failed (no hang); cargo test -p swissarmyhammer-code-context = 655 passed / 0 failed incl. real-RA integration tests test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end. ORPHAN ACCEPTANCE (ps -eo ppid,command | grep '[b]in/rust-analyzer' | grep -v proc-macro | awk '$1==1'): killed pre-existing orphans -> before=0; after both suites -> AFTER=0 new PPID=1 orphans. clippy -D warnings clean on swissarmyhammer-lsp + swissarmyhammer-code-context + swissarmyhammer-tools(tests); cargo fmt --check clean; .config/nextest.toml untouched (fail-fast=false intact). Dead end avoided: did NOT touch the leader-handoff respawn case (^7a5h2bj scope) and did NOT add a second kill-on-drop/probe mechanism. Running really-done adversarial double-check, then -> review.\n+  timestamp: 2026-06-19T02:36:11.560153+00:00\n position_column: doing\n position_ordinal: '8180'\n project: diagnostics\n","reverse_patch":"--- original\n+++ modified\n@@ -10,17 +10,6 @@\n   id: 01kvev5m47knca044hr4w7mhfs\n   text: 'Research done. Production lifecycle is already kill-on-drop-safe: the ONLY production holder of an LSP child is LspDaemon.child: Option<tokio::process::Child> spawned with .kill_on_drop(true) (daemon.rs start()), with graceful_shutdown + take()-based reaping in shutdown(). The std pipes handed to LspJsonRpcClient::new are extracted from that same tokio Child, which remains the owner — so there is NO separate production std-Child session holder to leak (root cause 2''s \"synchronous session caller\" is tests only; supervisor owns daemons in a HashMap, so supervisor drop -> daemon drop -> tokio kill_on_drop). The dominant real leak is root cause 1: server_spec.rs spawn_and_verify spawns a real rust-analyzer (via start_lsp_server(\"rust\",...)), sleeps 100ms, try_wait, and DROPS the std::process::Child without kill -> orphan. start_lsp_server is called ONLY from tests (server_spec unit tests + code-context integration_test.rs test_lsp_server_startup); no production caller. Plan: convert the availability check to a NON-SPAWNING probe (resolve_executable on disk/PATH), and fix server_spec unit tests that spawn long-lived real processes (sleep/rust-analyzer) to not leak. Mock python LSP spawns in lsp_communication/lsp_worker test mods are self-exiting python, not rust-analyzer; canonical MockLsp kill-on-drop guard already lives in code-context test_fixtures.'\n   timestamp: 2026-06-19T02:25:44.071331+00:00\n-- actor: claude-code\n-  id: 01kvevrrx87s28w18jaaf8hw6v\n-  text: |-\n-    Implementation landed (TDD). Probe redesign: server_spec.rs spawn_and_verify (the spawn-to-verify leak) DELETED; the private spawn_server is replaced by NON-SPAWNING verify_server_available(config) which only resolves the executable on disk/PATH (resolve_executable -> find_executable) and returns Ok(()) if it resolves — no Command::spawn, no throwaway rust-analyzer. start_lsp_server keeps its (language, project_root) signature + LspServerHandle{started,error} availability contract (started=available, error=why-not); project_root is now _project_root (kept for API compat, probe needs no cwd). Rewrote the obsolete spawn-based unit tests (the sleep/true immediate-exit ones, which themselves leaked) to assert the non-spawning availability contract.\n-\n-    Kill-on-drop guarantee: production was ALREADY safe — the sole production LSP-child holder is LspDaemon.child: Option<tokio::process::Child> spawned with .kill_on_drop(true) (daemon.rs start()), with graceful_shutdown + take()-based reaping in shutdown(); the std pipes given to LspJsonRpcClient::new are extracted from that same tokio Child which remains the owner, and the supervisor owns daemons in a HashMap (supervisor drop -> daemon drop -> kill_on_drop). No separate production std-Child session holder exists, so root cause 2 needed no production code change. Test hygiene (root cause 3): promoted the existing MockLsp kill-on-drop guard into a single pub guard crate::testing::KillOnDrop (Deref/DerefMut/Drop kill+wait); test_fixtures::MockLsp is now `pub use ...KillOnDrop as MockLsp` (removed the duplicated Drop impl — ONE mechanism, no duplicate). Wrapped every real-rust-analyzer Command::new(\"rust-analyzer\").spawn() child in KillOnDrop in code-context/tests/integration_test.rs (3 sites) and swissarmyhammer-tools/tests/integration/code_context_ops_e2e.rs (1 site) so a panicking assertion can no longer leak them.\n-\n-    Regression: new unit test test_start_lsp_server_does_not_spawn_rust_analyzer asserts the live rust-analyzer count never RISES across start_lsp_server (after <= before tolerates unrelated churn-down). Red-green-red verified: a spawning probe -> FAIL (before=2, after=3); the non-spawning probe -> pass.\n-\n-    Verification: cargo test -p swissarmyhammer-lsp = 222 passed / 0 failed (no hang); cargo test -p swissarmyhammer-code-context = 655 passed / 0 failed incl. real-RA integration tests test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end. ORPHAN ACCEPTANCE (ps -eo ppid,command | grep '[b]in/rust-analyzer' | grep -v proc-macro | awk '$1==1'): killed pre-existing orphans -> before=0; after both suites -> AFTER=0 new PPID=1 orphans. clippy -D warnings clean on swissarmyhammer-lsp + swissarmyhammer-code-context + swissarmyhammer-tools(tests); cargo fmt --check clean; .config/nextest.toml untouched (fail-fast=false intact). Dead end avoided: did NOT touch the leader-handoff respawn case (^7a5h2bj scope) and did NOT add a second kill-on-drop/probe mechanism. Running really-done adversarial double-check, then -> review.\n-  timestamp: 2026-06-19T02:36:11.560153+00:00\n position_column: doing\n position_ordinal: '8180'\n project: diagnostics\n","transaction_id":null}
{"id":"01KVEW5MYRH4ESY4HJRMR6SZMY","timestamp":"2026-06-19T02:43:13.496994Z","op":"update","item_id":"01KVDEGQ75R48YNFE76X6M3JPZ","forward_patch":"--- original\n+++ modified\n@@ -21,6 +21,10 @@\n\n     Verification: cargo test -p swissarmyhammer-lsp = 222 passed / 0 failed (no hang); cargo test -p swissarmyhammer-code-context = 655 passed / 0 failed incl. real-RA integration tests test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end. ORPHAN ACCEPTANCE (ps -eo ppid,command | grep '[b]in/rust-analyzer' | grep -v proc-macro | awk '$1==1'): killed pre-existing orphans -> before=0; after both suites -> AFTER=0 new PPID=1 orphans. clippy -D warnings clean on swissarmyhammer-lsp + swissarmyhammer-code-context + swissarmyhammer-tools(tests); cargo fmt --check clean; .config/nextest.toml untouched (fail-fast=false intact). Dead end avoided: did NOT touch the leader-handoff respawn case (^7a5h2bj scope) and did NOT add a second kill-on-drop/probe mechanism. Running really-done adversarial double-check, then -> review.\n   timestamp: 2026-06-19T02:36:11.560153+00:00\n+- actor: claude-code\n+  id: 01kvew5myrv0q9ea48k3ts2jfp\n+  text: 'Adversarial double-check returned REVISE with ONE narrow finding (it independently corroborated root cause 2: only production LSP spawn is daemon.rs tokio kill_on_drop owned by the LSP_SUPERVISOR static; no production std-Child holder). Finding: lsp_worker.rs test mod had a SECOND mock-LSP spawn mechanism — helper spawn_mock_lsp_for_index_single_file returning a bare std::process::Child with manual let _ = child.kill(); child.wait(); across 3 call sites (a near-duplicate of test_fixtures::spawn_mock_lsp + leak-on-panic risk), contradicting the single-mechanism claim. FIXED: deleted spawn_mock_lsp_for_index_single_file and routed all 3 tests through the canonical crate::test_fixtures::spawn_mock_lsp(&[Null, response, Null]) (read didOpen / reply documentSymbol / read didClose), which returns the single MockLsp=KillOnDrop guard; removed all manual kill/wait (guard reaps on drop). Now ONE mock-LSP spawn path + ONE process guard crate-wide. Verification after the fix: cargo clippy -p swissarmyhammer-code-context --all-targets -D warnings clean; cargo fmt --check clean; cargo test -p swissarmyhammer-lsp -p swissarmyhammer-code-context = 222 + 655 = all pass, 0 failed, no hang, incl. real-RA test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end and the 3 reworked lsp_worker index_single_file tests. ORPHANS: before=0, after=0 (zero new PPID=1 rust-analyzer orphans). Re-running double-check (bounded, one re-check).'\n+  timestamp: 2026-06-19T02:43:13.496572+00:00\n position_column: doing\n position_ordinal: '8180'\n project: diagnostics\n","reverse_patch":"--- original\n+++ modified\n@@ -21,10 +21,6 @@\n\n     Verification: cargo test -p swissarmyhammer-lsp = 222 passed / 0 failed (no hang); cargo test -p swissarmyhammer-code-context = 655 passed / 0 failed incl. real-RA integration tests test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end. ORPHAN ACCEPTANCE (ps -eo ppid,command | grep '[b]in/rust-analyzer' | grep -v proc-macro | awk '$1==1'): killed pre-existing orphans -> before=0; after both suites -> AFTER=0 new PPID=1 orphans. clippy -D warnings clean on swissarmyhammer-lsp + swissarmyhammer-code-context + swissarmyhammer-tools(tests); cargo fmt --check clean; .config/nextest.toml untouched (fail-fast=false intact). Dead end avoided: did NOT touch the leader-handoff respawn case (^7a5h2bj scope) and did NOT add a second kill-on-drop/probe mechanism. Running really-done adversarial double-check, then -> review.\n   timestamp: 2026-06-19T02:36:11.560153+00:00\n-- actor: claude-code\n-  id: 01kvew5myrv0q9ea48k3ts2jfp\n-  text: 'Adversarial double-check returned REVISE with ONE narrow finding (it independently corroborated root cause 2: only production LSP spawn is daemon.rs tokio kill_on_drop owned by the LSP_SUPERVISOR static; no production std-Child holder). Finding: lsp_worker.rs test mod had a SECOND mock-LSP spawn mechanism — helper spawn_mock_lsp_for_index_single_file returning a bare std::process::Child with manual let _ = child.kill(); child.wait(); across 3 call sites (a near-duplicate of test_fixtures::spawn_mock_lsp + leak-on-panic risk), contradicting the single-mechanism claim. FIXED: deleted spawn_mock_lsp_for_index_single_file and routed all 3 tests through the canonical crate::test_fixtures::spawn_mock_lsp(&[Null, response, Null]) (read didOpen / reply documentSymbol / read didClose), which returns the single MockLsp=KillOnDrop guard; removed all manual kill/wait (guard reaps on drop). Now ONE mock-LSP spawn path + ONE process guard crate-wide. Verification after the fix: cargo clippy -p swissarmyhammer-code-context --all-targets -D warnings clean; cargo fmt --check clean; cargo test -p swissarmyhammer-lsp -p swissarmyhammer-code-context = 222 + 655 = all pass, 0 failed, no hang, incl. real-RA test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end and the 3 reworked lsp_worker index_single_file tests. ORPHANS: before=0, after=0 (zero new PPID=1 rust-analyzer orphans). Re-running double-check (bounded, one re-check).'\n-  timestamp: 2026-06-19T02:43:13.496572+00:00\n position_column: doing\n position_ordinal: '8180'\n project: diagnostics\n","transaction_id":null}
{"id":"01KVEWF6NS1HGZ3YQTXMC5Y04S","timestamp":"2026-06-19T02:48:26.553770Z","op":"update","item_id":"01KVDEGQ75R48YNFE76X6M3JPZ","forward_patch":"--- original\n+++ modified\n@@ -25,6 +25,13 @@\n   id: 01kvew5myrv0q9ea48k3ts2jfp\n   text: 'Adversarial double-check returned REVISE with ONE narrow finding (it independently corroborated root cause 2: only production LSP spawn is daemon.rs tokio kill_on_drop owned by the LSP_SUPERVISOR static; no production std-Child holder). Finding: lsp_worker.rs test mod had a SECOND mock-LSP spawn mechanism — helper spawn_mock_lsp_for_index_single_file returning a bare std::process::Child with manual let _ = child.kill(); child.wait(); across 3 call sites (a near-duplicate of test_fixtures::spawn_mock_lsp + leak-on-panic risk), contradicting the single-mechanism claim. FIXED: deleted spawn_mock_lsp_for_index_single_file and routed all 3 tests through the canonical crate::test_fixtures::spawn_mock_lsp(&[Null, response, Null]) (read didOpen / reply documentSymbol / read didClose), which returns the single MockLsp=KillOnDrop guard; removed all manual kill/wait (guard reaps on drop). Now ONE mock-LSP spawn path + ONE process guard crate-wide. Verification after the fix: cargo clippy -p swissarmyhammer-code-context --all-targets -D warnings clean; cargo fmt --check clean; cargo test -p swissarmyhammer-lsp -p swissarmyhammer-code-context = 222 + 655 = all pass, 0 failed, no hang, incl. real-RA test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end and the 3 reworked lsp_worker index_single_file tests. ORPHANS: before=0, after=0 (zero new PPID=1 rust-analyzer orphans). Re-running double-check (bounded, one re-check).'\n   timestamp: 2026-06-19T02:43:13.496572+00:00\n+- actor: claude-code\n+  id: 01kvewf6ns2am8nb72b0b12yn8\n+  text: |-\n+    Re-check (bounded second double-check) confirmed the lsp_worker.rs delta correct, but found the SAME anti-pattern in a third spot: lsp_communication.rs tests had its OWN bespoke spawn_mock_lsp(Vec<Value>) returning a bare std::process::Child + 5 manual child.kill()/child.wait() pairs (another duplicate mock-LSP mechanism, leak-on-panic). RESOLVED: replaced the bespoke python3-script helper with a thin local spawn_mock_lsp(Value) that delegates to crate::test_fixtures::spawn_mock_lsp(&[response]) and returns the single MockLsp/KillOnDrop guard; updated all 5 call sites (vec![x] -> x) and removed every manual kill/wait (guard reaps on drop). Crate-wide sweep now confirms exactly ONE python3 mock-LSP spawn (test_fixtures::spawn_mock_lsp) and ONE process guard (testing::KillOnDrop); no bare-Child-with-manual-kill remains in code-context src. Per really-done the adversarial gate is bounded to one re-check, which is done and acted on.\n+\n+    FINAL VERIFICATION (all fresh): cargo test -p swissarmyhammer-lsp -p swissarmyhammer-code-context = 222 + 655 all pass, 0 failed, no hang, incl. real-RA test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end and all reworked lsp_worker + lsp_communication tests. cargo clippy -p swissarmyhammer-lsp -p swissarmyhammer-code-context -p swissarmyhammer-tools --all-targets -D warnings = 0 warnings. cargo fmt --check = clean. ORPHAN ACCEPTANCE: before=0, AFTER=0 new PPID=1 rust-analyzer orphans across both suites (ps -eo ppid,command | grep '[b]in/rust-analyzer' | grep -v proc-macro | awk '$1==1'). .config/nextest.toml untouched (fail-fast=false). Moving to review.\n+  timestamp: 2026-06-19T02:48:26.553303+00:00\n position_column: doing\n position_ordinal: '8180'\n project: diagnostics\n","reverse_patch":"--- original\n+++ modified\n@@ -25,13 +25,6 @@\n   id: 01kvew5myrv0q9ea48k3ts2jfp\n   text: 'Adversarial double-check returned REVISE with ONE narrow finding (it independently corroborated root cause 2: only production LSP spawn is daemon.rs tokio kill_on_drop owned by the LSP_SUPERVISOR static; no production std-Child holder). Finding: lsp_worker.rs test mod had a SECOND mock-LSP spawn mechanism — helper spawn_mock_lsp_for_index_single_file returning a bare std::process::Child with manual let _ = child.kill(); child.wait(); across 3 call sites (a near-duplicate of test_fixtures::spawn_mock_lsp + leak-on-panic risk), contradicting the single-mechanism claim. FIXED: deleted spawn_mock_lsp_for_index_single_file and routed all 3 tests through the canonical crate::test_fixtures::spawn_mock_lsp(&[Null, response, Null]) (read didOpen / reply documentSymbol / read didClose), which returns the single MockLsp=KillOnDrop guard; removed all manual kill/wait (guard reaps on drop). Now ONE mock-LSP spawn path + ONE process guard crate-wide. Verification after the fix: cargo clippy -p swissarmyhammer-code-context --all-targets -D warnings clean; cargo fmt --check clean; cargo test -p swissarmyhammer-lsp -p swissarmyhammer-code-context = 222 + 655 = all pass, 0 failed, no hang, incl. real-RA test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end and the 3 reworked lsp_worker index_single_file tests. ORPHANS: before=0, after=0 (zero new PPID=1 rust-analyzer orphans). Re-running double-check (bounded, one re-check).'\n   timestamp: 2026-06-19T02:43:13.496572+00:00\n-- actor: claude-code\n-  id: 01kvewf6ns2am8nb72b0b12yn8\n-  text: |-\n-    Re-check (bounded second double-check) confirmed the lsp_worker.rs delta correct, but found the SAME anti-pattern in a third spot: lsp_communication.rs tests had its OWN bespoke spawn_mock_lsp(Vec<Value>) returning a bare std::process::Child + 5 manual child.kill()/child.wait() pairs (another duplicate mock-LSP mechanism, leak-on-panic). RESOLVED: replaced the bespoke python3-script helper with a thin local spawn_mock_lsp(Value) that delegates to crate::test_fixtures::spawn_mock_lsp(&[response]) and returns the single MockLsp/KillOnDrop guard; updated all 5 call sites (vec![x] -> x) and removed every manual kill/wait (guard reaps on drop). Crate-wide sweep now confirms exactly ONE python3 mock-LSP spawn (test_fixtures::spawn_mock_lsp) and ONE process guard (testing::KillOnDrop); no bare-Child-with-manual-kill remains in code-context src. Per really-done the adversarial gate is bounded to one re-check, which is done and acted on.\n-\n-    FINAL VERIFICATION (all fresh): cargo test -p swissarmyhammer-lsp -p swissarmyhammer-code-context = 222 + 655 all pass, 0 failed, no hang, incl. real-RA test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end and all reworked lsp_worker + lsp_communication tests. cargo clippy -p swissarmyhammer-lsp -p swissarmyhammer-code-context -p swissarmyhammer-tools --all-targets -D warnings = 0 warnings. cargo fmt --check = clean. ORPHAN ACCEPTANCE: before=0, AFTER=0 new PPID=1 rust-analyzer orphans across both suites (ps -eo ppid,command | grep '[b]in/rust-analyzer' | grep -v proc-macro | awk '$1==1'). .config/nextest.toml untouched (fail-fast=false). Moving to review.\n-  timestamp: 2026-06-19T02:48:26.553303+00:00\n position_column: doing\n position_ordinal: '8180'\n project: diagnostics\n","transaction_id":null}
{"id":"01KVEWFCNZEAKKD9KE78FFQ58Q","timestamp":"2026-06-19T02:48:32.703535Z","op":"update","item_id":"01KVDEGQ75R48YNFE76X6M3JPZ","forward_patch":"--- original\n+++ modified\n@@ -32,8 +32,8 @@\n\n     FINAL VERIFICATION (all fresh): cargo test -p swissarmyhammer-lsp -p swissarmyhammer-code-context = 222 + 655 all pass, 0 failed, no hang, incl. real-RA test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end and all reworked lsp_worker + lsp_communication tests. cargo clippy -p swissarmyhammer-lsp -p swissarmyhammer-code-context -p swissarmyhammer-tools --all-targets -D warnings = 0 warnings. cargo fmt --check = clean. ORPHAN ACCEPTANCE: before=0, AFTER=0 new PPID=1 rust-analyzer orphans across both suites (ps -eo ppid,command | grep '[b]in/rust-analyzer' | grep -v proc-macro | awk '$1==1'). .config/nextest.toml untouched (fail-fast=false). Moving to review.\n   timestamp: 2026-06-19T02:48:26.553303+00:00\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '80'\n project: diagnostics\n title: 'No orphaned rust-analyzer: kill-on-exit for the owned LSP child + remove the start_lsp_server spawn-to-verify leak'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -32,8 +32,8 @@\n\n     FINAL VERIFICATION (all fresh): cargo test -p swissarmyhammer-lsp -p swissarmyhammer-code-context = 222 + 655 all pass, 0 failed, no hang, incl. real-RA test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end and all reworked lsp_worker + lsp_communication tests. cargo clippy -p swissarmyhammer-lsp -p swissarmyhammer-code-context -p swissarmyhammer-tools --all-targets -D warnings = 0 warnings. cargo fmt --check = clean. ORPHAN ACCEPTANCE: before=0, AFTER=0 new PPID=1 rust-analyzer orphans across both suites (ps -eo ppid,command | grep '[b]in/rust-analyzer' | grep -v proc-macro | awk '$1==1'). .config/nextest.toml untouched (fail-fast=false). Moving to review.\n   timestamp: 2026-06-19T02:48:26.553303+00:00\n-position_column: review\n-position_ordinal: '80'\n+position_column: doing\n+position_ordinal: '8180'\n project: diagnostics\n title: 'No orphaned rust-analyzer: kill-on-exit for the owned LSP child + remove the start_lsp_server spawn-to-verify leak'\n ---\n","transaction_id":null}
{"id":"01KVEXBCGWHQDPDZR00ZSGRB4C","timestamp":"2026-06-19T03:03:50.044722Z","op":"update","item_id":"01KVDEGQ75R48YNFE76X6M3JPZ","forward_patch":"--- original\n+++ modified\n@@ -62,4 +62,26 @@\n - [ ] Regression: after `cargo test -p swissarmyhammer-lsp` and `-p swissarmyhammer-code-context`, `ps -eo ppid,command | grep bin/rust-analyzer | grep -v proc-macro` shows no PPID=1 orphans attributable to the run.\n\n ## Workflow\n-Use `/tdd`. #diagnostics\n\\ No newline at end of file\n+Use `/tdd`. #diagnostics\n+\n+## Review Findings (2026-06-18 21:49)\n+\n+Verdict: **clean — 0 blockers** (6 warnings, 2 nits, all non-blocking clarity/reuse/convention items). The load-bearing claims were verified by direct inspection and hold:\n+\n+- (a) **Probe contract weakening is acceptable.** `start_lsp_server` has **zero production callers** (grep: only `lib.rs` re-exports in both crates + `server_spec.rs` tests + `integration_test.rs:1621` test). `started=true` now means \"executable resolves on disk/PATH\" instead of \"process launched\". No caller relies on actual launch (production launch is owned solely by `LspDaemon`), so the weaker signal breaks nothing; `started`/`error` remain a coherent availability contract. The non-spawning `verify_server_available` → `resolve_executable` path never calls `Command::spawn`.\n+- (b) **Production root cause 2 is genuinely fixed (no unguarded production std-Child).** The only non-test rust-analyzer spawn is `daemon.rs:244` with `.kill_on_drop(true)`. The std pipes handed to `LspJsonRpcClient::new` are derived via `into_owned_fd()` from `child.stdin/stdout.take()`, but the tokio `Child` is retained as `self.child = Some(child)` — it stays the lifecycle owner. Daemons live in `LspSupervisorManager.daemons: HashMap`, owned by the long-lived supervisor. No separate production `std::process::Child` rust-analyzer holder lacking kill-on-drop exists. No production code change was required, correctly.\n+- **Consolidation is genuine (no duplicate-but-different mechanism).** ONE guard `crate::testing::KillOnDrop`; `MockLsp` is a pure `pub use ...KillOnDrop as MockLsp` re-export (duplicate Drop impl removed); all mock-LSP spawns route through `test_fixtures::spawn_mock_lsp`; the `lsp_communication.rs` local `spawn_mock_lsp(Value)` is a thin delegate. No bare-`Child` + manual `kill()/wait()` remains in code-context src.\n+- **Regression test is red-green meaningful.** `test_start_lsp_server_does_not_spawn_rust_analyzer` counts live rust-analyzers before/after with the same 200ms grace the old spawning code used and asserts `after <= before` — it detects a surviving child via process-count delta, not a trivial pass.\n+- **Hang-safe + in scope.** All 3 real-RA integration spawns wrapped in `KillOnDrop::new(...)`; change stays out of the `^7a5h2bj` leader-handoff respawn case.\n+\n+### Warnings\n+- [ ] `crates/swissarmyhammer-code-context/src/lib.rs:31` — Public module `testing` lacks a documentation comment. All public items require doc comments explaining their purpose and usage. Add a documentation comment such as: `/// Test infrastructure and helpers (primarily for internal test use).` This clarifies the module's purpose and that it is test-specific, despite being a public module.\n+- [ ] `crates/swissarmyhammer-code-context/src/lsp_worker.rs:600` — Test helper `create_test_db()` reimplements the same capability as `crate::test_fixtures::test_db()` — both create an in-memory connection, configure it, and build the schema. Should call the shared helper instead. Replace local `create_test_db()` with `crate::test_fixtures::test_db()`. For file insertion, replace `insert_test_file()` with either `crate::test_fixtures::insert_file()` (passing explicit flags) or `insert_file_simple()` (for unindexed files). Extract TEST_FILE_SIZE to test_fixtures if that constant is needed elsewhere.\n+- [ ] `crates/swissarmyhammer-code-context/src/testing.rs:16` — KillOnDrop reimplements a child process guard that already exists as ProcessGuard in swissarmyhammer-common. Both wrap std::process::Child, implement Deref/DerefMut, and kill the process on drop. The difference is ProcessGuard attempts graceful termination first, while KillOnDrop goes straight to SIGKILL—a real variation axis, but one that could be parameterized rather than duplicated. Extend ProcessGuard with a force_kill_on_drop(child) constructor or skip_graceful_termination parameter, so both the graceful-termination use case and the immediate-kill use case are served from one type. Otherwise, if ProcessGuard's dependency on swissarmyhammer-common is unwanted, document why code-context needs its own guard (architecture/dependency constraint) in a comment. (Reviewer note: the KillOnDrop doc already explains the non-blocking SIGKILL is a deliberate anti-deadlock choice vs ProcessGuard's blocking graceful path; non-blocking.)\n+- [ ] `crates/swissarmyhammer-code-context/src/testing.rs:29` — KillOnDrop is a new public type that does not implement Debug. The rule requires all public types to implement applicable traits; absence of Debug prevents downstream crates from adding it (orphan rule) and makes debugging harder. Implement Debug for KillOnDrop: `impl std::fmt::Debug for KillOnDrop { fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result { self.0.fmt(f) } }`.\n+- [ ] `crates/swissarmyhammer-lsp/src/server_spec.rs:105` — LspServerHandle couples two fields (started: bool and error: Option<String>) that represent a single logical state. The type system does not enforce the invariant that started=true implies error=None. An enum would encode this at compile time and prevent misuse. Replace coupled fields with an enum: `pub enum LspServerStartResult { Success, Failed(String) }` and use `pub result: LspServerStartResult` instead of `started` and `error` fields in LspServerHandle.\n+- [ ] `crates/swissarmyhammer-tools/tests/integration/code_context_ops_e2e.rs:555` — Function contains ~66 actual code lines, exceeding the ~50-line threshold. Long functions are harder to read, test, and reuse. Extract the LSP initialization sequence (lines 563–574), symbol-persistence logic (lines 592–611), and call-edge persistence logic (lines 617–634) into separate helper functions.\n+\n+### Nits\n+- [ ] `crates/swissarmyhammer-lsp/src/server_spec.rs:111` — Hardcoded timeout value 30 should be a named constant. Currently duplicates the `default_startup_timeout()` function's return value. Change `init_timeout: 30` to `init_timeout: default_startup_timeout()` or extract a named constant like `const DEFAULT_INIT_TIMEOUT: u64 = 30` and use it both in the Default impl and in `default_startup_timeout()`.\n+- [ ] `crates/swissarmyhammer-lsp/src/server_spec.rs:251` — Hardcoded grace period 200ms should be a named constant. This configures how long to wait before checking if a spawned process survived, and the comment indicates it was previously 100ms. Extract a constant like `const PROCESS_GRACE_PERIOD_MS: u64 = 200` and use `Duration::from_millis(PROCESS_GRACE_PERIOD_MS)`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -62,26 +62,4 @@\n - [ ] Regression: after `cargo test -p swissarmyhammer-lsp` and `-p swissarmyhammer-code-context`, `ps -eo ppid,command | grep bin/rust-analyzer | grep -v proc-macro` shows no PPID=1 orphans attributable to the run.\n\n ## Workflow\n-Use `/tdd`. #diagnostics\n-\n-## Review Findings (2026-06-18 21:49)\n-\n-Verdict: **clean — 0 blockers** (6 warnings, 2 nits, all non-blocking clarity/reuse/convention items). The load-bearing claims were verified by direct inspection and hold:\n-\n-- (a) **Probe contract weakening is acceptable.** `start_lsp_server` has **zero production callers** (grep: only `lib.rs` re-exports in both crates + `server_spec.rs` tests + `integration_test.rs:1621` test). `started=true` now means \"executable resolves on disk/PATH\" instead of \"process launched\". No caller relies on actual launch (production launch is owned solely by `LspDaemon`), so the weaker signal breaks nothing; `started`/`error` remain a coherent availability contract. The non-spawning `verify_server_available` → `resolve_executable` path never calls `Command::spawn`.\n-- (b) **Production root cause 2 is genuinely fixed (no unguarded production std-Child).** The only non-test rust-analyzer spawn is `daemon.rs:244` with `.kill_on_drop(true)`. The std pipes handed to `LspJsonRpcClient::new` are derived via `into_owned_fd()` from `child.stdin/stdout.take()`, but the tokio `Child` is retained as `self.child = Some(child)` — it stays the lifecycle owner. Daemons live in `LspSupervisorManager.daemons: HashMap`, owned by the long-lived supervisor. No separate production `std::process::Child` rust-analyzer holder lacking kill-on-drop exists. No production code change was required, correctly.\n-- **Consolidation is genuine (no duplicate-but-different mechanism).** ONE guard `crate::testing::KillOnDrop`; `MockLsp` is a pure `pub use ...KillOnDrop as MockLsp` re-export (duplicate Drop impl removed); all mock-LSP spawns route through `test_fixtures::spawn_mock_lsp`; the `lsp_communication.rs` local `spawn_mock_lsp(Value)` is a thin delegate. No bare-`Child` + manual `kill()/wait()` remains in code-context src.\n-- **Regression test is red-green meaningful.** `test_start_lsp_server_does_not_spawn_rust_analyzer` counts live rust-analyzers before/after with the same 200ms grace the old spawning code used and asserts `after <= before` — it detects a surviving child via process-count delta, not a trivial pass.\n-- **Hang-safe + in scope.** All 3 real-RA integration spawns wrapped in `KillOnDrop::new(...)`; change stays out of the `^7a5h2bj` leader-handoff respawn case.\n-\n-### Warnings\n-- [ ] `crates/swissarmyhammer-code-context/src/lib.rs:31` — Public module `testing` lacks a documentation comment. All public items require doc comments explaining their purpose and usage. Add a documentation comment such as: `/// Test infrastructure and helpers (primarily for internal test use).` This clarifies the module's purpose and that it is test-specific, despite being a public module.\n-- [ ] `crates/swissarmyhammer-code-context/src/lsp_worker.rs:600` — Test helper `create_test_db()` reimplements the same capability as `crate::test_fixtures::test_db()` — both create an in-memory connection, configure it, and build the schema. Should call the shared helper instead. Replace local `create_test_db()` with `crate::test_fixtures::test_db()`. For file insertion, replace `insert_test_file()` with either `crate::test_fixtures::insert_file()` (passing explicit flags) or `insert_file_simple()` (for unindexed files). Extract TEST_FILE_SIZE to test_fixtures if that constant is needed elsewhere.\n-- [ ] `crates/swissarmyhammer-code-context/src/testing.rs:16` — KillOnDrop reimplements a child process guard that already exists as ProcessGuard in swissarmyhammer-common. Both wrap std::process::Child, implement Deref/DerefMut, and kill the process on drop. The difference is ProcessGuard attempts graceful termination first, while KillOnDrop goes straight to SIGKILL—a real variation axis, but one that could be parameterized rather than duplicated. Extend ProcessGuard with a force_kill_on_drop(child) constructor or skip_graceful_termination parameter, so both the graceful-termination use case and the immediate-kill use case are served from one type. Otherwise, if ProcessGuard's dependency on swissarmyhammer-common is unwanted, document why code-context needs its own guard (architecture/dependency constraint) in a comment. (Reviewer note: the KillOnDrop doc already explains the non-blocking SIGKILL is a deliberate anti-deadlock choice vs ProcessGuard's blocking graceful path; non-blocking.)\n-- [ ] `crates/swissarmyhammer-code-context/src/testing.rs:29` — KillOnDrop is a new public type that does not implement Debug. The rule requires all public types to implement applicable traits; absence of Debug prevents downstream crates from adding it (orphan rule) and makes debugging harder. Implement Debug for KillOnDrop: `impl std::fmt::Debug for KillOnDrop { fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result { self.0.fmt(f) } }`.\n-- [ ] `crates/swissarmyhammer-lsp/src/server_spec.rs:105` — LspServerHandle couples two fields (started: bool and error: Option<String>) that represent a single logical state. The type system does not enforce the invariant that started=true implies error=None. An enum would encode this at compile time and prevent misuse. Replace coupled fields with an enum: `pub enum LspServerStartResult { Success, Failed(String) }` and use `pub result: LspServerStartResult` instead of `started` and `error` fields in LspServerHandle.\n-- [ ] `crates/swissarmyhammer-tools/tests/integration/code_context_ops_e2e.rs:555` — Function contains ~66 actual code lines, exceeding the ~50-line threshold. Long functions are harder to read, test, and reuse. Extract the LSP initialization sequence (lines 563–574), symbol-persistence logic (lines 592–611), and call-edge persistence logic (lines 617–634) into separate helper functions.\n-\n-### Nits\n-- [ ] `crates/swissarmyhammer-lsp/src/server_spec.rs:111` — Hardcoded timeout value 30 should be a named constant. Currently duplicates the `default_startup_timeout()` function's return value. Change `init_timeout: 30` to `init_timeout: default_startup_timeout()` or extract a named constant like `const DEFAULT_INIT_TIMEOUT: u64 = 30` and use it both in the Default impl and in `default_startup_timeout()`.\n-- [ ] `crates/swissarmyhammer-lsp/src/server_spec.rs:251` — Hardcoded grace period 200ms should be a named constant. This configures how long to wait before checking if a spawned process survived, and the comment indicates it was previously 100ms. Extract a constant like `const PROCESS_GRACE_PERIOD_MS: u64 = 200` and use `Duration::from_millis(PROCESS_GRACE_PERIOD_MS)`.\n\\ No newline at end of file\n+Use `/tdd`. #diagnostics\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KVEXBH8GB6KFA4G9DYP4JB9Z","timestamp":"2026-06-19T03:03:54.896980Z","op":"update","item_id":"01KVDEGQ75R48YNFE76X6M3JPZ","forward_patch":"--- original\n+++ modified\n@@ -32,8 +32,8 @@\n\n     FINAL VERIFICATION (all fresh): cargo test -p swissarmyhammer-lsp -p swissarmyhammer-code-context = 222 + 655 all pass, 0 failed, no hang, incl. real-RA test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end and all reworked lsp_worker + lsp_communication tests. cargo clippy -p swissarmyhammer-lsp -p swissarmyhammer-code-context -p swissarmyhammer-tools --all-targets -D warnings = 0 warnings. cargo fmt --check = clean. ORPHAN ACCEPTANCE: before=0, AFTER=0 new PPID=1 rust-analyzer orphans across both suites (ps -eo ppid,command | grep '[b]in/rust-analyzer' | grep -v proc-macro | awk '$1==1'). .config/nextest.toml untouched (fail-fast=false). Moving to review.\n   timestamp: 2026-06-19T02:48:26.553303+00:00\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffffc680\n project: diagnostics\n title: 'No orphaned rust-analyzer: kill-on-exit for the owned LSP child + remove the start_lsp_server spawn-to-verify leak'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -32,8 +32,8 @@\n\n     FINAL VERIFICATION (all fresh): cargo test -p swissarmyhammer-lsp -p swissarmyhammer-code-context = 222 + 655 all pass, 0 failed, no hang, incl. real-RA test_real_lsp_document_symbols + test_lsp_symbol_lookup_end_to_end and all reworked lsp_worker + lsp_communication tests. cargo clippy -p swissarmyhammer-lsp -p swissarmyhammer-code-context -p swissarmyhammer-tools --all-targets -D warnings = 0 warnings. cargo fmt --check = clean. ORPHAN ACCEPTANCE: before=0, AFTER=0 new PPID=1 rust-analyzer orphans across both suites (ps -eo ppid,command | grep '[b]in/rust-analyzer' | grep -v proc-macro | awk '$1==1'). .config/nextest.toml untouched (fail-fast=false). Moving to review.\n   timestamp: 2026-06-19T02:48:26.553303+00:00\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffffc680\n+position_column: review\n+position_ordinal: '80'\n project: diagnostics\n title: 'No orphaned rust-analyzer: kill-on-exit for the owned LSP child + remove the start_lsp_server spawn-to-verify leak'\n ---\n","transaction_id":null}

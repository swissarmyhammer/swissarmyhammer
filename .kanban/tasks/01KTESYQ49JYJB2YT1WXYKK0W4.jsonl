{"id":"01KTESYQ49CCMCVN4AD4X4HS3X","timestamp":"2026-06-06T15:48:47.369789Z","op":"create","item_id":"01KTESYQ49JYJB2YT1WXYKK0W4","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,43 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: de80\n+project: ui-command-cleanup\n+title: 'Bug: Host-driven nav regression — arrow-key navigation broken (focus_by_window staleness + fixture-only tests)'\n+---\n+## What\n+The in-progress host-driven nav rewrite (uncommitted working tree as of 2026-06-06) **broke spatial navigation** — arrow keys no longer move focus. OS-menu progress is real, but the inline React nav path was removed before the host-driven path is reliable.\n+\n+This regression rides on the focus-source-of-truth seam, NOT the geometry pull (the on-demand geometry + lock discipline are implemented correctly).\n+\n+## What the rewrite did (context)\n+- Removed `NAV_COMMAND_SPEC` / `buildNavCommands` / `buildDrillCommands` from `apps/kanban-app/ui/src/components/app-shell.tsx` (−273) — the inline arrow-key path that read the webview's live focus + built the snapshot per keystroke is **gone**.\n+- Directional/drill `nav.*` are now host-driven plugin commands → focus op `navigate focus` with `{ window }` only. `nav.jump` is a webview-bus handler.\n+- Added F1 (`apps/kanban-app/src/ui_request.rs` + `ui-request-responder.ts`) and F2 (`crates/swissarmyhammer-focus/src/provider.rs` `UiGeometryProvider`, `TauriUiGeometryProvider`, `query geometry/scope_chain/focus` ops). Webview responder `registerUiResponder(\"focus.geometry\", …)` reads live `focusedFqRef.current` and rebuilds the snapshot on demand (no cache — correct).\n+- `nav.yaml` + `nav_yaml.rs` deleted.\n+\n+## Root cause (two silent-drop points in `resolve_nav_source`, server.rs)\n+1. **`from = focus_by_window[window]` is stale/empty → \"window has no focused slot — dropping.\"** The new path makes the KERNEL authoritative for current focus (`focus_by_window`), but the **frontend is the real source of truth** for focus (`FRONTEND_AUTHORITATIVE_KINDS = { scope_chain }` in `ui-state-context.tsx`; the kernel copy is a write-only echo). Any focus change that does not reliably push into the kernel's `focus_by_window` (click-to-focus especially) leaves the kernel navigating from a stale node or dropping.\n+2. **kernel `from` vs webview snapshot divergence.** The kernel navigates from *its* `focus_by_window[window]`, but the snapshot is built around the *webview's* `focusedFqRef`. If they differ, `pick_target` can't find `from` in the snapshot → drop/misbehave. Note the responder also exposes `focus.current`, but the op resolves `from` from the kernel map instead of using the webview's authoritative focus.\n+3. **window-moniker dependency** — the host path needs the correct `{window}` from the dispatch scope chain; a missing `window:` moniker (see harden card `01KTECWA8D05FVKJ80MA3H0FFY`) → wrong/empty window → drop.\n+\n+## Why tests are green but the app broke\n+`crates/swissarmyhammer-command-service/tests/integration/builtin_nav_commands_e2e.rs` injects a fake `SeedProvider` (fixed snapshot) and the focus tests use `FakeProvider` — pre-seeded focus + guaranteed geometry. The real `TauriUiGeometryProvider` round-trip and `focus_by_window` freshness against real focus changes are **never exercised**. Fixture-only coverage: green on mocks, broken on the live path. (See [[feedback_fixture_only_anti_pattern]] / [[feedback_real_path_tests]].)\n+\n+## Acceptance Criteria\n+- [ ] Arrow keys move focus again in the running app (board, grid, inspector) — directional + first/last + drill in/out.\n+- [ ] One of: (a) prove `focus_by_window` tracks the webview's focus on EVERY focus change incl. click-to-focus (reliable push), OR (b) have the nav op resolve `from` from the provider's `focus.current` (webview-authoritative) so kernel and snapshot can't diverge.\n+- [ ] The host path resolves the correct `{window}` from the dispatch scope chain (no default-to-main).\n+- [ ] Root cause documented (focus-source-of-truth seam).\n+\n+## Tests\n+- [ ] A REAL-PATH test (NOT SeedProvider/FakeProvider): drive a focus change (incl. a click-to-focus) → arrow key → assert the focus actually moves to the geometrically-correct neighbor, exercising the live `focus_by_window` + provider path.\n+- [ ] Regression test that fails on the current working tree (nav drops) and passes after the fix.\n+- [ ] Existing spatial suite stays green.\n+\n+## Related\n+- F2 / nav-server card `01KTEEDA9ZVTZ2R5CERW0WGK97` (this regression is its quality gate).\n+- Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (removed the inline path; can't be \"done\" while nav is broken).\n+- F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (the host→UI channel the pull rides on).\n+- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker resolution — drop point #3).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,43 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: de80\n-project: ui-command-cleanup\n-title: 'Bug: Host-driven nav regression — arrow-key navigation broken (focus_by_window staleness + fixture-only tests)'\n----\n-## What\n-The in-progress host-driven nav rewrite (uncommitted working tree as of 2026-06-06) **broke spatial navigation** — arrow keys no longer move focus. OS-menu progress is real, but the inline React nav path was removed before the host-driven path is reliable.\n-\n-This regression rides on the focus-source-of-truth seam, NOT the geometry pull (the on-demand geometry + lock discipline are implemented correctly).\n-\n-## What the rewrite did (context)\n-- Removed `NAV_COMMAND_SPEC` / `buildNavCommands` / `buildDrillCommands` from `apps/kanban-app/ui/src/components/app-shell.tsx` (−273) — the inline arrow-key path that read the webview's live focus + built the snapshot per keystroke is **gone**.\n-- Directional/drill `nav.*` are now host-driven plugin commands → focus op `navigate focus` with `{ window }` only. `nav.jump` is a webview-bus handler.\n-- Added F1 (`apps/kanban-app/src/ui_request.rs` + `ui-request-responder.ts`) and F2 (`crates/swissarmyhammer-focus/src/provider.rs` `UiGeometryProvider`, `TauriUiGeometryProvider`, `query geometry/scope_chain/focus` ops). Webview responder `registerUiResponder(\"focus.geometry\", …)` reads live `focusedFqRef.current` and rebuilds the snapshot on demand (no cache — correct).\n-- `nav.yaml` + `nav_yaml.rs` deleted.\n-\n-## Root cause (two silent-drop points in `resolve_nav_source`, server.rs)\n-1. **`from = focus_by_window[window]` is stale/empty → \"window has no focused slot — dropping.\"** The new path makes the KERNEL authoritative for current focus (`focus_by_window`), but the **frontend is the real source of truth** for focus (`FRONTEND_AUTHORITATIVE_KINDS = { scope_chain }` in `ui-state-context.tsx`; the kernel copy is a write-only echo). Any focus change that does not reliably push into the kernel's `focus_by_window` (click-to-focus especially) leaves the kernel navigating from a stale node or dropping.\n-2. **kernel `from` vs webview snapshot divergence.** The kernel navigates from *its* `focus_by_window[window]`, but the snapshot is built around the *webview's* `focusedFqRef`. If they differ, `pick_target` can't find `from` in the snapshot → drop/misbehave. Note the responder also exposes `focus.current`, but the op resolves `from` from the kernel map instead of using the webview's authoritative focus.\n-3. **window-moniker dependency** — the host path needs the correct `{window}` from the dispatch scope chain; a missing `window:` moniker (see harden card `01KTECWA8D05FVKJ80MA3H0FFY`) → wrong/empty window → drop.\n-\n-## Why tests are green but the app broke\n-`crates/swissarmyhammer-command-service/tests/integration/builtin_nav_commands_e2e.rs` injects a fake `SeedProvider` (fixed snapshot) and the focus tests use `FakeProvider` — pre-seeded focus + guaranteed geometry. The real `TauriUiGeometryProvider` round-trip and `focus_by_window` freshness against real focus changes are **never exercised**. Fixture-only coverage: green on mocks, broken on the live path. (See [[feedback_fixture_only_anti_pattern]] / [[feedback_real_path_tests]].)\n-\n-## Acceptance Criteria\n-- [ ] Arrow keys move focus again in the running app (board, grid, inspector) — directional + first/last + drill in/out.\n-- [ ] One of: (a) prove `focus_by_window` tracks the webview's focus on EVERY focus change incl. click-to-focus (reliable push), OR (b) have the nav op resolve `from` from the provider's `focus.current` (webview-authoritative) so kernel and snapshot can't diverge.\n-- [ ] The host path resolves the correct `{window}` from the dispatch scope chain (no default-to-main).\n-- [ ] Root cause documented (focus-source-of-truth seam).\n-\n-## Tests\n-- [ ] A REAL-PATH test (NOT SeedProvider/FakeProvider): drive a focus change (incl. a click-to-focus) → arrow key → assert the focus actually moves to the geometrically-correct neighbor, exercising the live `focus_by_window` + provider path.\n-- [ ] Regression test that fails on the current working tree (nav drops) and passes after the fix.\n-- [ ] Existing spatial suite stays green.\n-\n-## Related\n-- F2 / nav-server card `01KTEEDA9ZVTZ2R5CERW0WGK97` (this regression is its quality gate).\n-- Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (removed the inline path; can't be \"done\" while nav is broken).\n-- F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (the host→UI channel the pull rides on).\n-- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker resolution — drop point #3).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTESYYNK72ND9HQ52JN18GRJ","timestamp":"2026-06-06T15:48:55.091143Z","op":"update","item_id":"01KTESYQ49JYJB2YT1WXYKK0W4","forward_patch":"--- original\n+++ modified\n@@ -40,4 +40,4 @@\n - F2 / nav-server card `01KTEEDA9ZVTZ2R5CERW0WGK97` (this regression is its quality gate).\n - Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (removed the inline path; can't be \"done\" while nav is broken).\n - F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (the host→UI channel the pull rides on).\n-- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker resolution — drop point #3).\n\\ No newline at end of file\n+- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker resolution — drop point #3). #bug\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -40,4 +40,4 @@\n - F2 / nav-server card `01KTEEDA9ZVTZ2R5CERW0WGK97` (this regression is its quality gate).\n - Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (removed the inline path; can't be \"done\" while nav is broken).\n - F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (the host→UI channel the pull rides on).\n-- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker resolution — drop point #3). #bug\n\\ No newline at end of file\n+- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker resolution — drop point #3).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTET8W4W1656QPJR22CTBX2C","timestamp":"2026-06-06T15:54:20.188543Z","op":"update","item_id":"01KTESYQ49JYJB2YT1WXYKK0W4","forward_patch":"--- original\n+++ modified\n@@ -17,23 +17,39 @@\n - Added F1 (`apps/kanban-app/src/ui_request.rs` + `ui-request-responder.ts`) and F2 (`crates/swissarmyhammer-focus/src/provider.rs` `UiGeometryProvider`, `TauriUiGeometryProvider`, `query geometry/scope_chain/focus` ops). Webview responder `registerUiResponder(\"focus.geometry\", …)` reads live `focusedFqRef.current` and rebuilds the snapshot on demand (no cache — correct).\n - `nav.yaml` + `nav_yaml.rs` deleted.\n\n-## Root cause (two silent-drop points in `resolve_nav_source`, server.rs)\n-1. **`from = focus_by_window[window]` is stale/empty → \"window has no focused slot — dropping.\"** The new path makes the KERNEL authoritative for current focus (`focus_by_window`), but the **frontend is the real source of truth** for focus (`FRONTEND_AUTHORITATIVE_KINDS = { scope_chain }` in `ui-state-context.tsx`; the kernel copy is a write-only echo). Any focus change that does not reliably push into the kernel's `focus_by_window` (click-to-focus especially) leaves the kernel navigating from a stale node or dropping.\n-2. **kernel `from` vs webview snapshot divergence.** The kernel navigates from *its* `focus_by_window[window]`, but the snapshot is built around the *webview's* `focusedFqRef`. If they differ, `pick_target` can't find `from` in the snapshot → drop/misbehave. Note the responder also exposes `focus.current`, but the op resolves `from` from the kernel map instead of using the webview's authoritative focus.\n-3. **window-moniker dependency** — the host path needs the correct `{window}` from the dispatch scope chain; a missing `window:` moniker (see harden card `01KTECWA8D05FVKJ80MA3H0FFY`) → wrong/empty window → drop.\n+## ============ RUNTIME FINDINGS (2026-06-06, second session) ============\n+Verified against the live app's unified log, in two stages.\n+\n+### Stage 1 fix applied (uncommitted): focus now resolves\n+Edited `crates/swissarmyhammer-focus/src/server.rs::resolve_nav_source` + `resolve_drill_source` to resolve `from` as wire → **`provider.focus` (UI-authoritative)** → kernel `focus_by_window` (fallback), and `state.rs::navigate` now reconciles the kernel slot to the resolved `from` (correct `prev_fq`). Added a REAL-condition test `navigate_pulls_focus_from_provider_when_kernel_slot_empty` (no seeded slot) in `tests/integration/ui_geometry_provider.rs`. Focus crate + command-service integration + kanban-app build all green. This implements acceptance (b).\n+\n+RESULT IN THE RUNNING APP: the log message changed from `window has no focused slot — dropping` → `provider yielded no snapshot — dropping`. So **focus now resolves; the GEOMETRY pull is the remaining failure.**\n+\n+### Stage 2 root cause: the host→UI round-trip races itself (NOT a timeout-length problem)\n+`DEFAULT_REQUEST_TIMEOUT` is 5s, but `nav.down` completes in ~2.5ms returning `event:null` — so `request_from_ui` returns `Err` almost immediately, NOT after 5s. The log floods with:\n+```\n+ui_request_reply: no in-flight request for id (already timed out?)   ← every reply, 3-4× per request_id\n+provider yielded no snapshot — dropping  op=\"navigate focus\"\n+```\n+Diagnosis: the `request_from_ui` future is **dropped/cancelled before the webview reply arrives** (the awaiting `oneshot::Receiver` is gone), so `fulfill` finds the still-registered sender but `tx.send` fails → logs \"no in-flight (already timed out?)\". The same `request_id` getting 3-4 replies means **multiple responders/windows answer one request** (duplicate `registerUiResponder` across provider instances, and/or 4 board windows). The nested host→UI→host round-trip runs MID-`dispatch_command`; the plugin→host in-process MCP call (or the dispatch) appears to cancel/drop the nested request before the reply lands — the re-entrancy/cancellation hazard F1's exploration explicitly flagged.\n\n-## Why tests are green but the app broke\n-`crates/swissarmyhammer-command-service/tests/integration/builtin_nav_commands_e2e.rs` injects a fake `SeedProvider` (fixed snapshot) and the focus tests use `FakeProvider` — pre-seeded focus + guaranteed geometry. The real `TauriUiGeometryProvider` round-trip and `focus_by_window` freshness against real focus changes are **never exercised**. Fixture-only coverage: green on mocks, broken on the live path. (See [[feedback_fixture_only_anti_pattern]] / [[feedback_real_path_tests]].)\n+So even with focus resolved (acceptance b done), the geometry pull cannot complete reliably while it is a nested round-trip inside the dispatch that triggered it.\n+\n+## ============ RECOMMENDATION (second session) ============\n+Reconsider the architecture, don't just lengthen the timeout. The host-driven geometry PULL is fragile precisely because nav originates in the webview (where focus+geometry already live) and bounces host→UI→host. The **proven** path (the removed inline React nav) and the owner's own \"**handler bus for ALL webview behaviors**\" decision both point to:\n+- Keep `nav.*` DEFINED in the `nav-commands` plugin (id/name/keys/menu) → OS menu unchanged.\n+- EXECUTE `nav.*` via the **webview command bus** (Card B), same as `nav.jump`: the handler reads live `focusedFqRef` + builds the snapshot + calls the focus op INLINE (the old `buildNavCommands`/`buildDrillCommands` logic, relocated into bus handlers keyed by the plugin command id). No host→UI round-trip; definitions still live in the plugin.\n+- This makes F1/F2 (`ui_request`, `UiGeometryProvider`) optional for nav — keep them for genuine host-originated needs (agents/automation), but nav itself stops depending on the fragile mid-dispatch pull.\n\n ## Acceptance Criteria\n-- [ ] Arrow keys move focus again in the running app (board, grid, inspector) — directional + first/last + drill in/out.\n-- [ ] One of: (a) prove `focus_by_window` tracks the webview's focus on EVERY focus change incl. click-to-focus (reliable push), OR (b) have the nav op resolve `from` from the provider's `focus.current` (webview-authoritative) so kernel and snapshot can't diverge.\n-- [ ] The host path resolves the correct `{window}` from the dispatch scope chain (no default-to-main).\n-- [ ] Root cause documented (focus-source-of-truth seam).\n+- [ ] Arrow keys move focus again in the running app (board, grid, inspector) — directional + first/last + drill in/out + Escape.\n+- [ ] Decide + implement: (A) make the host→UI round-trip reliable mid-dispatch (fix the future-cancellation + duplicate-responder races), OR (B) execute nav via the webview bus inline (recommended above) — definitions stay in the plugin either way.\n+- [ ] The host path (if kept) resolves the correct `{window}` from the dispatch scope chain (no default-to-main).\n+- [ ] Root cause documented (done above).\n\n ## Tests\n-- [ ] A REAL-PATH test (NOT SeedProvider/FakeProvider): drive a focus change (incl. a click-to-focus) → arrow key → assert the focus actually moves to the geometrically-correct neighbor, exercising the live `focus_by_window` + provider path.\n-- [ ] Regression test that fails on the current working tree (nav drops) and passes after the fix.\n+- [ ] A REAL-PATH test (NOT SeedProvider/FakeProvider) that exercises the chosen execution path end to end and would have caught the runtime break.\n+- [ ] Regression test that fails on the broken tree and passes after the fix.\n - [ ] Existing spatial suite stays green.\n\n ## Related\n@@ -40,4 +56,5 @@\n - F2 / nav-server card `01KTEEDA9ZVTZ2R5CERW0WGK97` (this regression is its quality gate).\n - Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (removed the inline path; can't be \"done\" while nav is broken).\n - F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (the host→UI channel the pull rides on).\n-- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker resolution — drop point #3). #bug\n\\ No newline at end of file\n+- Card B `01KTED5F8DQ2XH5BB0WK1MRR3P` (webview handler-bus — the recommended execution path for nav).\n+- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker resolution). #bug\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -17,39 +17,23 @@\n - Added F1 (`apps/kanban-app/src/ui_request.rs` + `ui-request-responder.ts`) and F2 (`crates/swissarmyhammer-focus/src/provider.rs` `UiGeometryProvider`, `TauriUiGeometryProvider`, `query geometry/scope_chain/focus` ops). Webview responder `registerUiResponder(\"focus.geometry\", …)` reads live `focusedFqRef.current` and rebuilds the snapshot on demand (no cache — correct).\n - `nav.yaml` + `nav_yaml.rs` deleted.\n\n-## ============ RUNTIME FINDINGS (2026-06-06, second session) ============\n-Verified against the live app's unified log, in two stages.\n-\n-### Stage 1 fix applied (uncommitted): focus now resolves\n-Edited `crates/swissarmyhammer-focus/src/server.rs::resolve_nav_source` + `resolve_drill_source` to resolve `from` as wire → **`provider.focus` (UI-authoritative)** → kernel `focus_by_window` (fallback), and `state.rs::navigate` now reconciles the kernel slot to the resolved `from` (correct `prev_fq`). Added a REAL-condition test `navigate_pulls_focus_from_provider_when_kernel_slot_empty` (no seeded slot) in `tests/integration/ui_geometry_provider.rs`. Focus crate + command-service integration + kanban-app build all green. This implements acceptance (b).\n-\n-RESULT IN THE RUNNING APP: the log message changed from `window has no focused slot — dropping` → `provider yielded no snapshot — dropping`. So **focus now resolves; the GEOMETRY pull is the remaining failure.**\n+## Root cause (two silent-drop points in `resolve_nav_source`, server.rs)\n+1. **`from = focus_by_window[window]` is stale/empty → \"window has no focused slot — dropping.\"** The new path makes the KERNEL authoritative for current focus (`focus_by_window`), but the **frontend is the real source of truth** for focus (`FRONTEND_AUTHORITATIVE_KINDS = { scope_chain }` in `ui-state-context.tsx`; the kernel copy is a write-only echo). Any focus change that does not reliably push into the kernel's `focus_by_window` (click-to-focus especially) leaves the kernel navigating from a stale node or dropping.\n+2. **kernel `from` vs webview snapshot divergence.** The kernel navigates from *its* `focus_by_window[window]`, but the snapshot is built around the *webview's* `focusedFqRef`. If they differ, `pick_target` can't find `from` in the snapshot → drop/misbehave. Note the responder also exposes `focus.current`, but the op resolves `from` from the kernel map instead of using the webview's authoritative focus.\n+3. **window-moniker dependency** — the host path needs the correct `{window}` from the dispatch scope chain; a missing `window:` moniker (see harden card `01KTECWA8D05FVKJ80MA3H0FFY`) → wrong/empty window → drop.\n\n-### Stage 2 root cause: the host→UI round-trip races itself (NOT a timeout-length problem)\n-`DEFAULT_REQUEST_TIMEOUT` is 5s, but `nav.down` completes in ~2.5ms returning `event:null` — so `request_from_ui` returns `Err` almost immediately, NOT after 5s. The log floods with:\n-```\n-ui_request_reply: no in-flight request for id (already timed out?)   ← every reply, 3-4× per request_id\n-provider yielded no snapshot — dropping  op=\"navigate focus\"\n-```\n-Diagnosis: the `request_from_ui` future is **dropped/cancelled before the webview reply arrives** (the awaiting `oneshot::Receiver` is gone), so `fulfill` finds the still-registered sender but `tx.send` fails → logs \"no in-flight (already timed out?)\". The same `request_id` getting 3-4 replies means **multiple responders/windows answer one request** (duplicate `registerUiResponder` across provider instances, and/or 4 board windows). The nested host→UI→host round-trip runs MID-`dispatch_command`; the plugin→host in-process MCP call (or the dispatch) appears to cancel/drop the nested request before the reply lands — the re-entrancy/cancellation hazard F1's exploration explicitly flagged.\n-\n-So even with focus resolved (acceptance b done), the geometry pull cannot complete reliably while it is a nested round-trip inside the dispatch that triggered it.\n-\n-## ============ RECOMMENDATION (second session) ============\n-Reconsider the architecture, don't just lengthen the timeout. The host-driven geometry PULL is fragile precisely because nav originates in the webview (where focus+geometry already live) and bounces host→UI→host. The **proven** path (the removed inline React nav) and the owner's own \"**handler bus for ALL webview behaviors**\" decision both point to:\n-- Keep `nav.*` DEFINED in the `nav-commands` plugin (id/name/keys/menu) → OS menu unchanged.\n-- EXECUTE `nav.*` via the **webview command bus** (Card B), same as `nav.jump`: the handler reads live `focusedFqRef` + builds the snapshot + calls the focus op INLINE (the old `buildNavCommands`/`buildDrillCommands` logic, relocated into bus handlers keyed by the plugin command id). No host→UI round-trip; definitions still live in the plugin.\n-- This makes F1/F2 (`ui_request`, `UiGeometryProvider`) optional for nav — keep them for genuine host-originated needs (agents/automation), but nav itself stops depending on the fragile mid-dispatch pull.\n+## Why tests are green but the app broke\n+`crates/swissarmyhammer-command-service/tests/integration/builtin_nav_commands_e2e.rs` injects a fake `SeedProvider` (fixed snapshot) and the focus tests use `FakeProvider` — pre-seeded focus + guaranteed geometry. The real `TauriUiGeometryProvider` round-trip and `focus_by_window` freshness against real focus changes are **never exercised**. Fixture-only coverage: green on mocks, broken on the live path. (See [[feedback_fixture_only_anti_pattern]] / [[feedback_real_path_tests]].)\n\n ## Acceptance Criteria\n-- [ ] Arrow keys move focus again in the running app (board, grid, inspector) — directional + first/last + drill in/out + Escape.\n-- [ ] Decide + implement: (A) make the host→UI round-trip reliable mid-dispatch (fix the future-cancellation + duplicate-responder races), OR (B) execute nav via the webview bus inline (recommended above) — definitions stay in the plugin either way.\n-- [ ] The host path (if kept) resolves the correct `{window}` from the dispatch scope chain (no default-to-main).\n-- [ ] Root cause documented (done above).\n+- [ ] Arrow keys move focus again in the running app (board, grid, inspector) — directional + first/last + drill in/out.\n+- [ ] One of: (a) prove `focus_by_window` tracks the webview's focus on EVERY focus change incl. click-to-focus (reliable push), OR (b) have the nav op resolve `from` from the provider's `focus.current` (webview-authoritative) so kernel and snapshot can't diverge.\n+- [ ] The host path resolves the correct `{window}` from the dispatch scope chain (no default-to-main).\n+- [ ] Root cause documented (focus-source-of-truth seam).\n\n ## Tests\n-- [ ] A REAL-PATH test (NOT SeedProvider/FakeProvider) that exercises the chosen execution path end to end and would have caught the runtime break.\n-- [ ] Regression test that fails on the broken tree and passes after the fix.\n+- [ ] A REAL-PATH test (NOT SeedProvider/FakeProvider): drive a focus change (incl. a click-to-focus) → arrow key → assert the focus actually moves to the geometrically-correct neighbor, exercising the live `focus_by_window` + provider path.\n+- [ ] Regression test that fails on the current working tree (nav drops) and passes after the fix.\n - [ ] Existing spatial suite stays green.\n\n ## Related\n@@ -56,5 +40,4 @@\n - F2 / nav-server card `01KTEEDA9ZVTZ2R5CERW0WGK97` (this regression is its quality gate).\n - Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (removed the inline path; can't be \"done\" while nav is broken).\n - F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (the host→UI channel the pull rides on).\n-- Card B `01KTED5F8DQ2XH5BB0WK1MRR3P` (webview handler-bus — the recommended execution path for nav).\n-- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker resolution). #bug\n\\ No newline at end of file\n+- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker resolution — drop point #3). #bug\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTEXDMJ2F8SE3FBCHK2W8Y1Y","timestamp":"2026-06-06T16:49:21.986873Z","op":"update","item_id":"01KTESYQ49JYJB2YT1WXYKK0W4","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: de80\n+position_column: doing\n+position_ordinal: '80'\n project: ui-command-cleanup\n title: 'Bug: Host-driven nav regression — arrow-key navigation broken (focus_by_window staleness + fixture-only tests)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: de80\n project: ui-command-cleanup\n title: 'Bug: Host-driven nav regression — arrow-key navigation broken (focus_by_window staleness + fixture-only tests)'\n ---\n","transaction_id":null}
{"id":"01KTEXGXMZ178HPT7BYPE876H7","timestamp":"2026-06-06T16:51:09.599213Z","op":"update","item_id":"01KTESYQ49JYJB2YT1WXYKK0W4","forward_patch":"--- original\n+++ modified\n@@ -9,52 +9,65 @@\n ## What\n The in-progress host-driven nav rewrite (uncommitted working tree as of 2026-06-06) **broke spatial navigation** — arrow keys no longer move focus. OS-menu progress is real, but the inline React nav path was removed before the host-driven path is reliable.\n\n-This regression rides on the focus-source-of-truth seam, NOT the geometry pull (the on-demand geometry + lock discipline are implemented correctly).\n+## ✅ PRESCRIBED FIX — atomic focus+geometry pull (single source of truth = the webview, sampled once)\n+The break is structural: the kernel resolves `from` from its own `focus_by_window` (a stale echo — the frontend is authoritative for focus, `FRONTEND_AUTHORITATIVE_KINDS = { scope_chain }`) while the `NavSnapshot` is built around the webview's live `focusedFqRef`. Two sources, sampled separately → the origin `from` may be stale and/or absent from the snapshot, so `pick_target` finds nothing and nav drops.\n\n-## What the rewrite did (context)\n-- Removed `NAV_COMMAND_SPEC` / `buildNavCommands` / `buildDrillCommands` from `apps/kanban-app/ui/src/components/app-shell.tsx` (−273) — the inline arrow-key path that read the webview's live focus + built the snapshot per keystroke is **gone**.\n-- Directional/drill `nav.*` are now host-driven plugin commands → focus op `navigate focus` with `{ window }` only. `nav.jump` is a webview-bus handler.\n-- Added F1 (`apps/kanban-app/src/ui_request.rs` + `ui-request-responder.ts`) and F2 (`crates/swissarmyhammer-focus/src/provider.rs` `UiGeometryProvider`, `TauriUiGeometryProvider`, `query geometry/scope_chain/focus` ops). Webview responder `registerUiResponder(\"focus.geometry\", …)` reads live `focusedFqRef.current` and rebuilds the snapshot on demand (no cache — correct).\n-- `nav.yaml` + `nav_yaml.rs` deleted.\n+Fix: **pull the focused FQM and the snapshot together, in one synchronous webview sample, and have the kernel navigate from THAT focus — never from `focus_by_window`.**\n\n-## ============ RUNTIME FINDINGS (2026-06-06, second session) ============\n-Verified against the live app's unified log, in two stages.\n+1. **Webview responder (one atomic sample).** Add a responder that reads `focusedFqRef.current` and builds the snapshot from it in the SAME synchronous call, returning both:\n+   ```ts\n+   registerUiResponder(\"focus.navSource\", () => {\n+     const fq = focusedFqRef.current;\n+     if (fq === null) return null;\n+     const snapshot = buildSnapshotForFocused(layerRegistriesRef, fq);\n+     return snapshot ? { focused_fq: fq, snapshot } : null;\n+   });\n+   ```\n+   `focused_fq` and `snapshot` cannot diverge — they're sampled in one JS turn, on-demand (`getBoundingClientRect` at call time, no cache). Honors the owner constraint.\n+2. **Provider trait (`crates/swissarmyhammer-focus/src/provider.rs`).** Add `async fn nav_source(&self, window) -> Option<NavSource>` where `NavSource { focused_fq: FullyQualifiedMoniker, snapshot: NavSnapshot }`, backed by one `focus.navSource` F1 request. (`TauriUiGeometryProvider` adds the corresponding `pull(window, \"focus.navSource\")`. Keep `snapshot`/`focus`/`scope_chain` for the standalone `query *` ops.)\n+3. **Kernel `resolve_nav_source` (`server.rs`).** On the host-driven path, replace `from = focused_fq.or(focused_in_window(window))` + `provider.snapshot(window)` with:\n+   ```rust\n+   let NavSource { focused_fq, snapshot } = self.provider.nav_source(&window).await?; // None → drop\n+   // use focused_fq as `from`; it is guaranteed present in `snapshot`.\n+   ```\n+   Do NOT read `focus_by_window` for the nav origin. (Same atomic pull for `drill_in` / `drill_out`: the pulled `focused_fq` is the drill source.)\n\n-### Stage 1 fix applied (uncommitted): focus now resolves\n-Edited `crates/swissarmyhammer-focus/src/server.rs::resolve_nav_source` + `resolve_drill_source` to resolve `from` as wire → **`provider.focus` (UI-authoritative)** → kernel `focus_by_window` (fallback), and `state.rs::navigate` now reconciles the kernel slot to the resolved `from` (correct `prev_fq`). Added a REAL-condition test `navigate_pulls_focus_from_provider_when_kernel_slot_empty` (no seeded slot) in `tests/integration/ui_geometry_provider.rs`. Focus crate + command-service integration + kanban-app build all green. This implements acceptance (b).\n+### Why this is reliable\n+- **No staleness:** `from` is the webview's live focus, re-read at nav time — not a kernel echo that lags click-to-focus.\n+- **No divergence:** `from` and `snapshot` come from one sample, so `from` is always in the snapshot's scopes (`pick_target` always has its origin).\n+- **On-demand preserved:** geometry is still `getBoundingClientRect` at call time, never cached.\n+- **Lock-safe:** still one provider `await` with no spatial lock held (unchanged from the current handler).\n+- `focus_by_window` stays as the kernel's record for the focus-lost fallback + `set focus`, but is no longer the nav origin.\n\n-RESULT IN THE RUNNING APP: the log message changed from `window has no focused slot — dropping` → `provider yielded no snapshot — dropping`. So **focus now resolves; the GEOMETRY pull is the remaining failure.**\n+### Supersedes\n+This REPLACES F2's AC #1 framing (\"kernel resolves current focus from `focus_by_window`\"). The reliable model is **webview-authoritative focus, pulled atomically with geometry** — not kernel-authoritative focus. Update card `01KTEEDA9ZVTZ2R5CERW0WGK97` accordingly.\n\n-### Stage 2 root cause: the host→UI round-trip races itself (NOT a timeout-length problem)\n-`DEFAULT_REQUEST_TIMEOUT` is 5s, but `nav.down` completes in ~2.5ms returning `event:null` — so `request_from_ui` returns `Err` almost immediately, NOT after 5s. The log floods with:\n-```\n-ui_request_reply: no in-flight request for id (already timed out?)   ← every reply, 3-4× per request_id\n-provider yielded no snapshot — dropping  op=\"navigate focus\"\n-```\n-Diagnosis: the `request_from_ui` future is **dropped/cancelled before the webview reply arrives** (the awaiting `oneshot::Receiver` is gone), so `fulfill` finds the still-registered sender but `tx.send` fails → logs \"no in-flight (already timed out?)\". The same `request_id` getting 3-4 replies means **multiple responders/windows answer one request** (duplicate `registerUiResponder` across provider instances, and/or 4 board windows). The nested host→UI→host round-trip runs MID-`dispatch_command`; the plugin→host in-process MCP call (or the dispatch) appears to cancel/drop the nested request before the reply lands — the re-entrancy/cancellation hazard F1's exploration explicitly flagged.\n+### Remaining dependency\n+- Correct `{window}` on the dispatch scope chain still required (drop-point #3) — harden card `01KTECWA8D05FVKJ80MA3H0FFY`. The F1 request is keyed by window; a wrong window queries the wrong webview focus.\n\n-So even with focus resolved (acceptance b done), the geometry pull cannot complete reliably while it is a nested round-trip inside the dispatch that triggered it.\n+## What the rewrite did (context)\n+- Removed `NAV_COMMAND_SPEC` / `buildNavCommands` / `buildDrillCommands` from `app-shell.tsx` (−273) — the inline arrow-key path is gone.\n+- Directional/drill `nav.*` are now host-driven plugin commands → focus op with `{ window }`. `nav.jump` is a webview-bus handler.\n+- Added F1 (`apps/kanban-app/src/ui_request.rs`) + F2 (`provider.rs` `UiGeometryProvider`, `TauriUiGeometryProvider`, `query geometry/scope_chain/focus`). Responder `focus.geometry` reads live `focusedFqRef` (on-demand, no cache — correct).\n+- `nav.yaml` + `nav_yaml.rs` deleted.\n\n-## ============ RECOMMENDATION (second session) ============\n-Reconsider the architecture, don't just lengthen the timeout. The host-driven geometry PULL is fragile precisely because nav originates in the webview (where focus+geometry already live) and bounces host→UI→host. The **proven** path (the removed inline React nav) and the owner's own \"**handler bus for ALL webview behaviors**\" decision both point to:\n-- Keep `nav.*` DEFINED in the `nav-commands` plugin (id/name/keys/menu) → OS menu unchanged.\n-- EXECUTE `nav.*` via the **webview command bus** (Card B), same as `nav.jump`: the handler reads live `focusedFqRef` + builds the snapshot + calls the focus op INLINE (the old `buildNavCommands`/`buildDrillCommands` logic, relocated into bus handlers keyed by the plugin command id). No host→UI round-trip; definitions still live in the plugin.\n-- This makes F1/F2 (`ui_request`, `UiGeometryProvider`) optional for nav — keep them for genuine host-originated needs (agents/automation), but nav itself stops depending on the fragile mid-dispatch pull.\n+## Why tests are green but the app broke\n+`builtin_nav_commands_e2e.rs` injects a fake `SeedProvider` (fixed snapshot, pre-seeded focus); the real `TauriUiGeometryProvider` round-trip + `focus_by_window` freshness are never exercised. Fixture-only: green on mocks, broken on the live path. (See [[feedback_fixture_only_anti_pattern]] / [[feedback_real_path_tests]].)\n\n ## Acceptance Criteria\n-- [ ] Arrow keys move focus again in the running app (board, grid, inspector) — directional + first/last + drill in/out + Escape.\n-- [ ] Decide + implement: (A) make the host→UI round-trip reliable mid-dispatch (fix the future-cancellation + duplicate-responder races), OR (B) execute nav via the webview bus inline (recommended above) — definitions stay in the plugin either way.\n-- [ ] The host path (if kept) resolves the correct `{window}` from the dispatch scope chain (no default-to-main).\n-- [ ] Root cause documented (done above).\n+- [ ] Arrow keys move focus again in the running app (board, grid, inspector) — directional + first/last + drill in/out.\n+- [ ] Nav origin + geometry come from ONE atomic webview sample (`focus.navSource`); the nav op does NOT read `focus_by_window` for the origin.\n+- [ ] The host path resolves the correct `{window}` from the dispatch scope chain (no default-to-main).\n+- [ ] F2 card AC #1 updated to webview-authoritative focus.\n\n ## Tests\n-- [ ] A REAL-PATH test (NOT SeedProvider/FakeProvider) that exercises the chosen execution path end to end and would have caught the runtime break.\n-- [ ] Regression test that fails on the broken tree and passes after the fix.\n+- [ ] REAL-PATH test (NOT SeedProvider): drive a focus change (incl. click-to-focus) → arrow key → assert focus moves to the geometrically-correct neighbor, exercising the live `nav_source` pull.\n+- [ ] Atomicity test: the `focus.navSource` responder returns a `focused_fq` that is present in its returned `snapshot.scopes`.\n+- [ ] Regression test that fails on the current working tree (nav drops) and passes after.\n - [ ] Existing spatial suite stays green.\n\n ## Related\n-- F2 / nav-server card `01KTEEDA9ZVTZ2R5CERW0WGK97` (this regression is its quality gate).\n-- Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (removed the inline path; can't be \"done\" while nav is broken).\n-- F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (the host→UI channel the pull rides on).\n-- Card B `01KTED5F8DQ2XH5BB0WK1MRR3P` (webview handler-bus — the recommended execution path for nav).\n-- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker resolution). #bug\n\\ No newline at end of file\n+- F2 / nav-server card `01KTEEDA9ZVTZ2R5CERW0WGK97` (this regression is its quality gate; the fix supersedes its AC #1).\n+- Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (blocked_by this).\n+- F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (the host→UI channel — correct; reused for `focus.navSource`).\n+- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker — drop point #3).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -9,65 +9,52 @@\n ## What\n The in-progress host-driven nav rewrite (uncommitted working tree as of 2026-06-06) **broke spatial navigation** — arrow keys no longer move focus. OS-menu progress is real, but the inline React nav path was removed before the host-driven path is reliable.\n\n-## ✅ PRESCRIBED FIX — atomic focus+geometry pull (single source of truth = the webview, sampled once)\n-The break is structural: the kernel resolves `from` from its own `focus_by_window` (a stale echo — the frontend is authoritative for focus, `FRONTEND_AUTHORITATIVE_KINDS = { scope_chain }`) while the `NavSnapshot` is built around the webview's live `focusedFqRef`. Two sources, sampled separately → the origin `from` may be stale and/or absent from the snapshot, so `pick_target` finds nothing and nav drops.\n+This regression rides on the focus-source-of-truth seam, NOT the geometry pull (the on-demand geometry + lock discipline are implemented correctly).\n\n-Fix: **pull the focused FQM and the snapshot together, in one synchronous webview sample, and have the kernel navigate from THAT focus — never from `focus_by_window`.**\n+## What the rewrite did (context)\n+- Removed `NAV_COMMAND_SPEC` / `buildNavCommands` / `buildDrillCommands` from `apps/kanban-app/ui/src/components/app-shell.tsx` (−273) — the inline arrow-key path that read the webview's live focus + built the snapshot per keystroke is **gone**.\n+- Directional/drill `nav.*` are now host-driven plugin commands → focus op `navigate focus` with `{ window }` only. `nav.jump` is a webview-bus handler.\n+- Added F1 (`apps/kanban-app/src/ui_request.rs` + `ui-request-responder.ts`) and F2 (`crates/swissarmyhammer-focus/src/provider.rs` `UiGeometryProvider`, `TauriUiGeometryProvider`, `query geometry/scope_chain/focus` ops). Webview responder `registerUiResponder(\"focus.geometry\", …)` reads live `focusedFqRef.current` and rebuilds the snapshot on demand (no cache — correct).\n+- `nav.yaml` + `nav_yaml.rs` deleted.\n\n-1. **Webview responder (one atomic sample).** Add a responder that reads `focusedFqRef.current` and builds the snapshot from it in the SAME synchronous call, returning both:\n-   ```ts\n-   registerUiResponder(\"focus.navSource\", () => {\n-     const fq = focusedFqRef.current;\n-     if (fq === null) return null;\n-     const snapshot = buildSnapshotForFocused(layerRegistriesRef, fq);\n-     return snapshot ? { focused_fq: fq, snapshot } : null;\n-   });\n-   ```\n-   `focused_fq` and `snapshot` cannot diverge — they're sampled in one JS turn, on-demand (`getBoundingClientRect` at call time, no cache). Honors the owner constraint.\n-2. **Provider trait (`crates/swissarmyhammer-focus/src/provider.rs`).** Add `async fn nav_source(&self, window) -> Option<NavSource>` where `NavSource { focused_fq: FullyQualifiedMoniker, snapshot: NavSnapshot }`, backed by one `focus.navSource` F1 request. (`TauriUiGeometryProvider` adds the corresponding `pull(window, \"focus.navSource\")`. Keep `snapshot`/`focus`/`scope_chain` for the standalone `query *` ops.)\n-3. **Kernel `resolve_nav_source` (`server.rs`).** On the host-driven path, replace `from = focused_fq.or(focused_in_window(window))` + `provider.snapshot(window)` with:\n-   ```rust\n-   let NavSource { focused_fq, snapshot } = self.provider.nav_source(&window).await?; // None → drop\n-   // use focused_fq as `from`; it is guaranteed present in `snapshot`.\n-   ```\n-   Do NOT read `focus_by_window` for the nav origin. (Same atomic pull for `drill_in` / `drill_out`: the pulled `focused_fq` is the drill source.)\n+## ============ RUNTIME FINDINGS (2026-06-06, second session) ============\n+Verified against the live app's unified log, in two stages.\n\n-### Why this is reliable\n-- **No staleness:** `from` is the webview's live focus, re-read at nav time — not a kernel echo that lags click-to-focus.\n-- **No divergence:** `from` and `snapshot` come from one sample, so `from` is always in the snapshot's scopes (`pick_target` always has its origin).\n-- **On-demand preserved:** geometry is still `getBoundingClientRect` at call time, never cached.\n-- **Lock-safe:** still one provider `await` with no spatial lock held (unchanged from the current handler).\n-- `focus_by_window` stays as the kernel's record for the focus-lost fallback + `set focus`, but is no longer the nav origin.\n+### Stage 1 fix applied (uncommitted): focus now resolves\n+Edited `crates/swissarmyhammer-focus/src/server.rs::resolve_nav_source` + `resolve_drill_source` to resolve `from` as wire → **`provider.focus` (UI-authoritative)** → kernel `focus_by_window` (fallback), and `state.rs::navigate` now reconciles the kernel slot to the resolved `from` (correct `prev_fq`). Added a REAL-condition test `navigate_pulls_focus_from_provider_when_kernel_slot_empty` (no seeded slot) in `tests/integration/ui_geometry_provider.rs`. Focus crate + command-service integration + kanban-app build all green. This implements acceptance (b).\n\n-### Supersedes\n-This REPLACES F2's AC #1 framing (\"kernel resolves current focus from `focus_by_window`\"). The reliable model is **webview-authoritative focus, pulled atomically with geometry** — not kernel-authoritative focus. Update card `01KTEEDA9ZVTZ2R5CERW0WGK97` accordingly.\n+RESULT IN THE RUNNING APP: the log message changed from `window has no focused slot — dropping` → `provider yielded no snapshot — dropping`. So **focus now resolves; the GEOMETRY pull is the remaining failure.**\n\n-### Remaining dependency\n-- Correct `{window}` on the dispatch scope chain still required (drop-point #3) — harden card `01KTECWA8D05FVKJ80MA3H0FFY`. The F1 request is keyed by window; a wrong window queries the wrong webview focus.\n+### Stage 2 root cause: the host→UI round-trip races itself (NOT a timeout-length problem)\n+`DEFAULT_REQUEST_TIMEOUT` is 5s, but `nav.down` completes in ~2.5ms returning `event:null` — so `request_from_ui` returns `Err` almost immediately, NOT after 5s. The log floods with:\n+```\n+ui_request_reply: no in-flight request for id (already timed out?)   ← every reply, 3-4× per request_id\n+provider yielded no snapshot — dropping  op=\"navigate focus\"\n+```\n+Diagnosis: the `request_from_ui` future is **dropped/cancelled before the webview reply arrives** (the awaiting `oneshot::Receiver` is gone), so `fulfill` finds the still-registered sender but `tx.send` fails → logs \"no in-flight (already timed out?)\". The same `request_id` getting 3-4 replies means **multiple responders/windows answer one request** (duplicate `registerUiResponder` across provider instances, and/or 4 board windows). The nested host→UI→host round-trip runs MID-`dispatch_command`; the plugin→host in-process MCP call (or the dispatch) appears to cancel/drop the nested request before the reply lands — the re-entrancy/cancellation hazard F1's exploration explicitly flagged.\n\n-## What the rewrite did (context)\n-- Removed `NAV_COMMAND_SPEC` / `buildNavCommands` / `buildDrillCommands` from `app-shell.tsx` (−273) — the inline arrow-key path is gone.\n-- Directional/drill `nav.*` are now host-driven plugin commands → focus op with `{ window }`. `nav.jump` is a webview-bus handler.\n-- Added F1 (`apps/kanban-app/src/ui_request.rs`) + F2 (`provider.rs` `UiGeometryProvider`, `TauriUiGeometryProvider`, `query geometry/scope_chain/focus`). Responder `focus.geometry` reads live `focusedFqRef` (on-demand, no cache — correct).\n-- `nav.yaml` + `nav_yaml.rs` deleted.\n+So even with focus resolved (acceptance b done), the geometry pull cannot complete reliably while it is a nested round-trip inside the dispatch that triggered it.\n\n-## Why tests are green but the app broke\n-`builtin_nav_commands_e2e.rs` injects a fake `SeedProvider` (fixed snapshot, pre-seeded focus); the real `TauriUiGeometryProvider` round-trip + `focus_by_window` freshness are never exercised. Fixture-only: green on mocks, broken on the live path. (See [[feedback_fixture_only_anti_pattern]] / [[feedback_real_path_tests]].)\n+## ============ RECOMMENDATION (second session) ============\n+Reconsider the architecture, don't just lengthen the timeout. The host-driven geometry PULL is fragile precisely because nav originates in the webview (where focus+geometry already live) and bounces host→UI→host. The **proven** path (the removed inline React nav) and the owner's own \"**handler bus for ALL webview behaviors**\" decision both point to:\n+- Keep `nav.*` DEFINED in the `nav-commands` plugin (id/name/keys/menu) → OS menu unchanged.\n+- EXECUTE `nav.*` via the **webview command bus** (Card B), same as `nav.jump`: the handler reads live `focusedFqRef` + builds the snapshot + calls the focus op INLINE (the old `buildNavCommands`/`buildDrillCommands` logic, relocated into bus handlers keyed by the plugin command id). No host→UI round-trip; definitions still live in the plugin.\n+- This makes F1/F2 (`ui_request`, `UiGeometryProvider`) optional for nav — keep them for genuine host-originated needs (agents/automation), but nav itself stops depending on the fragile mid-dispatch pull.\n\n ## Acceptance Criteria\n-- [ ] Arrow keys move focus again in the running app (board, grid, inspector) — directional + first/last + drill in/out.\n-- [ ] Nav origin + geometry come from ONE atomic webview sample (`focus.navSource`); the nav op does NOT read `focus_by_window` for the origin.\n-- [ ] The host path resolves the correct `{window}` from the dispatch scope chain (no default-to-main).\n-- [ ] F2 card AC #1 updated to webview-authoritative focus.\n+- [ ] Arrow keys move focus again in the running app (board, grid, inspector) — directional + first/last + drill in/out + Escape.\n+- [ ] Decide + implement: (A) make the host→UI round-trip reliable mid-dispatch (fix the future-cancellation + duplicate-responder races), OR (B) execute nav via the webview bus inline (recommended above) — definitions stay in the plugin either way.\n+- [ ] The host path (if kept) resolves the correct `{window}` from the dispatch scope chain (no default-to-main).\n+- [ ] Root cause documented (done above).\n\n ## Tests\n-- [ ] REAL-PATH test (NOT SeedProvider): drive a focus change (incl. click-to-focus) → arrow key → assert focus moves to the geometrically-correct neighbor, exercising the live `nav_source` pull.\n-- [ ] Atomicity test: the `focus.navSource` responder returns a `focused_fq` that is present in its returned `snapshot.scopes`.\n-- [ ] Regression test that fails on the current working tree (nav drops) and passes after.\n+- [ ] A REAL-PATH test (NOT SeedProvider/FakeProvider) that exercises the chosen execution path end to end and would have caught the runtime break.\n+- [ ] Regression test that fails on the broken tree and passes after the fix.\n - [ ] Existing spatial suite stays green.\n\n ## Related\n-- F2 / nav-server card `01KTEEDA9ZVTZ2R5CERW0WGK97` (this regression is its quality gate; the fix supersedes its AC #1).\n-- Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (blocked_by this).\n-- F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (the host→UI channel — correct; reused for `focus.navSource`).\n-- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker — drop point #3).\n\\ No newline at end of file\n+- F2 / nav-server card `01KTEEDA9ZVTZ2R5CERW0WGK97` (this regression is its quality gate).\n+- Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (removed the inline path; can't be \"done\" while nav is broken).\n+- F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (the host→UI channel the pull rides on).\n+- Card B `01KTED5F8DQ2XH5BB0WK1MRR3P` (webview handler-bus — the recommended execution path for nav).\n+- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker resolution). #bug\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTF7FFMXKSFT3TZXRKZNER57","timestamp":"2026-06-06T19:45:08.253626Z","op":"update","item_id":"01KTESYQ49JYJB2YT1WXYKK0W4","forward_patch":"--- original\n+++ modified\n@@ -7,67 +7,54 @@\n title: 'Bug: Host-driven nav regression — arrow-key navigation broken (focus_by_window staleness + fixture-only tests)'\n ---\n ## What\n-The in-progress host-driven nav rewrite (uncommitted working tree as of 2026-06-06) **broke spatial navigation** — arrow keys no longer move focus. OS-menu progress is real, but the inline React nav path was removed before the host-driven path is reliable.\n+The in-progress host-driven nav rewrite (uncommitted working tree, 2026-06-06) **broke spatial navigation** — arrow keys no longer move focus.\n\n-## ✅ PRESCRIBED FIX — atomic focus+geometry pull (single source of truth = the webview, sampled once)\n-The break is structural: the kernel resolves `from` from its own `focus_by_window` (a stale echo — the frontend is authoritative for focus, `FRONTEND_AUTHORITATIVE_KINDS = { scope_chain }`) while the `NavSnapshot` is built around the webview's live `focusedFqRef`. Two sources, sampled separately → the origin `from` may be stale and/or absent from the snapshot, so `pick_target` finds nothing and nav drops.\n+## ✅ PRESCRIBED FIX — the scope chain already carries window + focus; pull ONLY geometry\n+**Owner insight (authoritative): a scope chain has all the info to focus and navigate. Its ROOT is the `window:<label>` container; its LEAF is the focused scope. Every command dispatch already carries the scope chain, built fresh from the frontend's live focus.** So `window` and `focused_fq` are BOTH already present, consistent, and current at dispatch time. The only thing not in the scope chain is live DOM geometry (rects), which is sampled on-demand in the webview.\n\n-Fix: **pull the focused FQM and the snapshot together, in one synchronous webview sample, and have the kernel navigate from THAT focus — never from `focus_by_window`.**\n+The regression's root cause is that the host-driven path THREW AWAY the focused_fq the scope chain provides and instead resolved the nav origin from the kernel's `focus_by_window` — a stale echo (the frontend is authoritative for focus, `FRONTEND_AUTHORITATIVE_KINDS = { scope_chain }`). Stale/divergent origin → `pick_target` has no origin in the snapshot → nav drops.\n\n-1. **Webview responder (one atomic sample).** Add a responder that reads `focusedFqRef.current` and builds the snapshot from it in the SAME synchronous call, returning both:\n-   ```ts\n-   registerUiResponder(\"focus.navSource\", () => {\n-     const fq = focusedFqRef.current;\n-     if (fq === null) return null;\n-     const snapshot = buildSnapshotForFocused(layerRegistriesRef, fq);\n-     return snapshot ? { focused_fq: fq, snapshot } : null;\n-   });\n-   ```\n-   `focused_fq` and `snapshot` cannot diverge — they're sampled in one JS turn, on-demand (`getBoundingClientRect` at call time, no cache). Honors the owner constraint.\n-2. **Provider trait (`crates/swissarmyhammer-focus/src/provider.rs`).** Add `async fn nav_source(&self, window) -> Option<NavSource>` where `NavSource { focused_fq: FullyQualifiedMoniker, snapshot: NavSnapshot }`, backed by one `focus.navSource` F1 request. (`TauriUiGeometryProvider` adds the corresponding `pull(window, \"focus.navSource\")`. Keep `snapshot`/`focus`/`scope_chain` for the standalone `query *` ops.)\n-3. **Kernel `resolve_nav_source` (`server.rs`).** On the host-driven path, replace `from = focused_fq.or(focused_in_window(window))` + `provider.snapshot(window)` with:\n-   ```rust\n-   let NavSource { focused_fq, snapshot } = self.provider.nav_source(&window).await?; // None → drop\n-   // use focused_fq as `from`; it is guaranteed present in `snapshot`.\n-   ```\n-   Do NOT read `focus_by_window` for the nav origin. (Same atomic pull for `drill_in` / `drill_out`: the pulled `focused_fq` is the drill source.)\n+### Fix\n+1. **`nav.*` plugin command:** read `ctx.scope_chain` → derive `window` = root `window:` moniker, `focused_fq` = leaf. Pass BOTH to the focus op. Stop dispatching \"window-only\". (The dispatch already has the scope chain — this is just reading it, not a new round-trip.)\n+2. **Focus op (`resolve_nav_source`, server.rs):** use the passed `focused_fq` as `from`; pull ONLY the geometry snapshot on-demand from `window` (the webview's `focus.geometry` responder, built around `focused_fq`). **Do NOT read `focus_by_window` for the nav origin.** Same for `drill_in`/`drill_out` (the leaf is the drill source).\n+3. The op already has optional `focused_fq` + `window` + `snapshot` fields, so the wire shape barely changes — the fix is mostly: populate `focused_fq` from the scope chain (stop omitting it) + pull geometry when `snapshot` is None.\n+4. Optional hardening: have the geometry pull pass `focused_fq` so the responder builds the snapshot around exactly that origin (guarantees `from ∈ snapshot.scopes`), rather than re-reading `focusedFqRef`.\n\n-### Why this is reliable\n-- **No staleness:** `from` is the webview's live focus, re-read at nav time — not a kernel echo that lags click-to-focus.\n-- **No divergence:** `from` and `snapshot` come from one sample, so `from` is always in the snapshot's scopes (`pick_target` always has its origin).\n-- **On-demand preserved:** geometry is still `getBoundingClientRect` at call time, never cached.\n-- **Lock-safe:** still one provider `await` with no spatial lock held (unchanged from the current handler).\n-- `focus_by_window` stays as the kernel's record for the focus-lost fallback + `set focus`, but is no longer the nav origin.\n+### Why this is reliable (and minimal)\n+- **Window + focus are one fresh structure** (the scope chain in the dispatch) — they cannot be stale or diverge from each other.\n+- **Geometry is the only on-demand pull** — still `getBoundingClientRect` at call time, never cached (honors the owner constraint).\n+- **No `focus_by_window` in the nav origin path** — the stale-echo class of bug is gone by construction.\n+- **Lock-safe** — one geometry `await`, no spatial lock held (unchanged).\n\n ### Supersedes\n-This REPLACES F2's AC #1 framing (\"kernel resolves current focus from `focus_by_window`\"). The reliable model is **webview-authoritative focus, pulled atomically with geometry** — not kernel-authoritative focus. Update card `01KTEEDA9ZVTZ2R5CERW0WGK97` accordingly.\n+Drop F2's \"kernel-authoritative focus / window-only nav op\" framing (its AC #1). The model is: **scope chain (window root + focus leaf) carries identity; only geometry is pulled.** Update card `01KTEEDA9ZVTZ2R5CERW0WGK97`.\n\n-### Remaining dependency\n-- Correct `{window}` on the dispatch scope chain still required (drop-point #3) — harden card `01KTECWA8D05FVKJ80MA3H0FFY`. The F1 request is keyed by window; a wrong window queries the wrong webview focus.\n+### Still required\n+- The scope chain must be rooted at `window:<label>` on every nav dispatch (harden card `01KTECWA8D05FVKJ80MA3H0FFY`) — that root is exactly where the window comes from.\n\n ## What the rewrite did (context)\n-- Removed `NAV_COMMAND_SPEC` / `buildNavCommands` / `buildDrillCommands` from `app-shell.tsx` (−273) — the inline arrow-key path is gone.\n-- Directional/drill `nav.*` are now host-driven plugin commands → focus op with `{ window }`. `nav.jump` is a webview-bus handler.\n-- Added F1 (`apps/kanban-app/src/ui_request.rs`) + F2 (`provider.rs` `UiGeometryProvider`, `TauriUiGeometryProvider`, `query geometry/scope_chain/focus`). Responder `focus.geometry` reads live `focusedFqRef` (on-demand, no cache — correct).\n+- Removed `NAV_COMMAND_SPEC` / `buildNavCommands` / `buildDrillCommands` from `app-shell.tsx` (−273) — inline arrow-key path gone.\n+- Directional/drill `nav.*` now host-driven plugin commands dispatching the focus op **window-only** (discarding the scope-chain focused_fq — the bug). `nav.jump` is a webview-bus handler.\n+- Added F1 (`apps/kanban-app/src/ui_request.rs`) + F2 (`provider.rs` `UiGeometryProvider`, `TauriUiGeometryProvider`, `query geometry/scope_chain/focus`). Responder `focus.geometry` reads live `focusedFqRef` on-demand (correct).\n - `nav.yaml` + `nav_yaml.rs` deleted.\n\n ## Why tests are green but the app broke\n-`builtin_nav_commands_e2e.rs` injects a fake `SeedProvider` (fixed snapshot, pre-seeded focus); the real `TauriUiGeometryProvider` round-trip + `focus_by_window` freshness are never exercised. Fixture-only: green on mocks, broken on the live path. (See [[feedback_fixture_only_anti_pattern]] / [[feedback_real_path_tests]].)\n+`builtin_nav_commands_e2e.rs` injects a fake `SeedProvider` (fixed snapshot, pre-seeded focus); the real round-trip + `focus_by_window` freshness are never exercised. Fixture-only — green on mocks, broken live. (See [[feedback_fixture_only_anti_pattern]] / [[feedback_real_path_tests]].)\n\n ## Acceptance Criteria\n-- [ ] Arrow keys move focus again in the running app (board, grid, inspector) — directional + first/last + drill in/out.\n-- [ ] Nav origin + geometry come from ONE atomic webview sample (`focus.navSource`); the nav op does NOT read `focus_by_window` for the origin.\n-- [ ] The host path resolves the correct `{window}` from the dispatch scope chain (no default-to-main).\n-- [ ] F2 card AC #1 updated to webview-authoritative focus.\n+- [ ] Arrow keys move focus again (board, grid, inspector) — directional + first/last + drill in/out.\n+- [ ] The nav origin (`from`) and window are derived from the dispatch SCOPE CHAIN (leaf + root); the nav op does NOT read `focus_by_window` for the origin.\n+- [ ] Geometry is the only thing pulled on-demand; the snapshot is built around the scope-chain `focused_fq`.\n+- [ ] F2 card AC #1 updated to the scope-chain model.\n\n ## Tests\n-- [ ] REAL-PATH test (NOT SeedProvider): drive a focus change (incl. click-to-focus) → arrow key → assert focus moves to the geometrically-correct neighbor, exercising the live `nav_source` pull.\n-- [ ] Atomicity test: the `focus.navSource` responder returns a `focused_fq` that is present in its returned `snapshot.scopes`.\n-- [ ] Regression test that fails on the current working tree (nav drops) and passes after.\n+- [ ] REAL-PATH test (NOT SeedProvider): focus a scope (incl. click-to-focus), dispatch a nav with the real scope chain, assert focus moves to the geometrically-correct neighbor.\n+- [ ] Invariant test: the geometry snapshot contains the scope-chain leaf (`from ∈ snapshot.scopes`).\n+- [ ] Regression test that fails on the current working tree (nav drops), passes after.\n - [ ] Existing spatial suite stays green.\n\n ## Related\n-- F2 / nav-server card `01KTEEDA9ZVTZ2R5CERW0WGK97` (this regression is its quality gate; the fix supersedes its AC #1).\n+- F2 card `01KTEEDA9ZVTZ2R5CERW0WGK97` (quality gate; this fix supersedes its AC #1).\n - Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (blocked_by this).\n-- F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (the host→UI channel — correct; reused for `focus.navSource`).\n-- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker — drop point #3).\n\\ No newline at end of file\n+- F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (host→UI channel — correct; reused for the geometry pull).\n+- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (scope chain rooted at `window:` — where the window comes from).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -7,54 +7,67 @@\n title: 'Bug: Host-driven nav regression — arrow-key navigation broken (focus_by_window staleness + fixture-only tests)'\n ---\n ## What\n-The in-progress host-driven nav rewrite (uncommitted working tree, 2026-06-06) **broke spatial navigation** — arrow keys no longer move focus.\n+The in-progress host-driven nav rewrite (uncommitted working tree as of 2026-06-06) **broke spatial navigation** — arrow keys no longer move focus. OS-menu progress is real, but the inline React nav path was removed before the host-driven path is reliable.\n\n-## ✅ PRESCRIBED FIX — the scope chain already carries window + focus; pull ONLY geometry\n-**Owner insight (authoritative): a scope chain has all the info to focus and navigate. Its ROOT is the `window:<label>` container; its LEAF is the focused scope. Every command dispatch already carries the scope chain, built fresh from the frontend's live focus.** So `window` and `focused_fq` are BOTH already present, consistent, and current at dispatch time. The only thing not in the scope chain is live DOM geometry (rects), which is sampled on-demand in the webview.\n+## ✅ PRESCRIBED FIX — atomic focus+geometry pull (single source of truth = the webview, sampled once)\n+The break is structural: the kernel resolves `from` from its own `focus_by_window` (a stale echo — the frontend is authoritative for focus, `FRONTEND_AUTHORITATIVE_KINDS = { scope_chain }`) while the `NavSnapshot` is built around the webview's live `focusedFqRef`. Two sources, sampled separately → the origin `from` may be stale and/or absent from the snapshot, so `pick_target` finds nothing and nav drops.\n\n-The regression's root cause is that the host-driven path THREW AWAY the focused_fq the scope chain provides and instead resolved the nav origin from the kernel's `focus_by_window` — a stale echo (the frontend is authoritative for focus, `FRONTEND_AUTHORITATIVE_KINDS = { scope_chain }`). Stale/divergent origin → `pick_target` has no origin in the snapshot → nav drops.\n+Fix: **pull the focused FQM and the snapshot together, in one synchronous webview sample, and have the kernel navigate from THAT focus — never from `focus_by_window`.**\n\n-### Fix\n-1. **`nav.*` plugin command:** read `ctx.scope_chain` → derive `window` = root `window:` moniker, `focused_fq` = leaf. Pass BOTH to the focus op. Stop dispatching \"window-only\". (The dispatch already has the scope chain — this is just reading it, not a new round-trip.)\n-2. **Focus op (`resolve_nav_source`, server.rs):** use the passed `focused_fq` as `from`; pull ONLY the geometry snapshot on-demand from `window` (the webview's `focus.geometry` responder, built around `focused_fq`). **Do NOT read `focus_by_window` for the nav origin.** Same for `drill_in`/`drill_out` (the leaf is the drill source).\n-3. The op already has optional `focused_fq` + `window` + `snapshot` fields, so the wire shape barely changes — the fix is mostly: populate `focused_fq` from the scope chain (stop omitting it) + pull geometry when `snapshot` is None.\n-4. Optional hardening: have the geometry pull pass `focused_fq` so the responder builds the snapshot around exactly that origin (guarantees `from ∈ snapshot.scopes`), rather than re-reading `focusedFqRef`.\n+1. **Webview responder (one atomic sample).** Add a responder that reads `focusedFqRef.current` and builds the snapshot from it in the SAME synchronous call, returning both:\n+   ```ts\n+   registerUiResponder(\"focus.navSource\", () => {\n+     const fq = focusedFqRef.current;\n+     if (fq === null) return null;\n+     const snapshot = buildSnapshotForFocused(layerRegistriesRef, fq);\n+     return snapshot ? { focused_fq: fq, snapshot } : null;\n+   });\n+   ```\n+   `focused_fq` and `snapshot` cannot diverge — they're sampled in one JS turn, on-demand (`getBoundingClientRect` at call time, no cache). Honors the owner constraint.\n+2. **Provider trait (`crates/swissarmyhammer-focus/src/provider.rs`).** Add `async fn nav_source(&self, window) -> Option<NavSource>` where `NavSource { focused_fq: FullyQualifiedMoniker, snapshot: NavSnapshot }`, backed by one `focus.navSource` F1 request. (`TauriUiGeometryProvider` adds the corresponding `pull(window, \"focus.navSource\")`. Keep `snapshot`/`focus`/`scope_chain` for the standalone `query *` ops.)\n+3. **Kernel `resolve_nav_source` (`server.rs`).** On the host-driven path, replace `from = focused_fq.or(focused_in_window(window))` + `provider.snapshot(window)` with:\n+   ```rust\n+   let NavSource { focused_fq, snapshot } = self.provider.nav_source(&window).await?; // None → drop\n+   // use focused_fq as `from`; it is guaranteed present in `snapshot`.\n+   ```\n+   Do NOT read `focus_by_window` for the nav origin. (Same atomic pull for `drill_in` / `drill_out`: the pulled `focused_fq` is the drill source.)\n\n-### Why this is reliable (and minimal)\n-- **Window + focus are one fresh structure** (the scope chain in the dispatch) — they cannot be stale or diverge from each other.\n-- **Geometry is the only on-demand pull** — still `getBoundingClientRect` at call time, never cached (honors the owner constraint).\n-- **No `focus_by_window` in the nav origin path** — the stale-echo class of bug is gone by construction.\n-- **Lock-safe** — one geometry `await`, no spatial lock held (unchanged).\n+### Why this is reliable\n+- **No staleness:** `from` is the webview's live focus, re-read at nav time — not a kernel echo that lags click-to-focus.\n+- **No divergence:** `from` and `snapshot` come from one sample, so `from` is always in the snapshot's scopes (`pick_target` always has its origin).\n+- **On-demand preserved:** geometry is still `getBoundingClientRect` at call time, never cached.\n+- **Lock-safe:** still one provider `await` with no spatial lock held (unchanged from the current handler).\n+- `focus_by_window` stays as the kernel's record for the focus-lost fallback + `set focus`, but is no longer the nav origin.\n\n ### Supersedes\n-Drop F2's \"kernel-authoritative focus / window-only nav op\" framing (its AC #1). The model is: **scope chain (window root + focus leaf) carries identity; only geometry is pulled.** Update card `01KTEEDA9ZVTZ2R5CERW0WGK97`.\n+This REPLACES F2's AC #1 framing (\"kernel resolves current focus from `focus_by_window`\"). The reliable model is **webview-authoritative focus, pulled atomically with geometry** — not kernel-authoritative focus. Update card `01KTEEDA9ZVTZ2R5CERW0WGK97` accordingly.\n\n-### Still required\n-- The scope chain must be rooted at `window:<label>` on every nav dispatch (harden card `01KTECWA8D05FVKJ80MA3H0FFY`) — that root is exactly where the window comes from.\n+### Remaining dependency\n+- Correct `{window}` on the dispatch scope chain still required (drop-point #3) — harden card `01KTECWA8D05FVKJ80MA3H0FFY`. The F1 request is keyed by window; a wrong window queries the wrong webview focus.\n\n ## What the rewrite did (context)\n-- Removed `NAV_COMMAND_SPEC` / `buildNavCommands` / `buildDrillCommands` from `app-shell.tsx` (−273) — inline arrow-key path gone.\n-- Directional/drill `nav.*` now host-driven plugin commands dispatching the focus op **window-only** (discarding the scope-chain focused_fq — the bug). `nav.jump` is a webview-bus handler.\n-- Added F1 (`apps/kanban-app/src/ui_request.rs`) + F2 (`provider.rs` `UiGeometryProvider`, `TauriUiGeometryProvider`, `query geometry/scope_chain/focus`). Responder `focus.geometry` reads live `focusedFqRef` on-demand (correct).\n+- Removed `NAV_COMMAND_SPEC` / `buildNavCommands` / `buildDrillCommands` from `app-shell.tsx` (−273) — the inline arrow-key path is gone.\n+- Directional/drill `nav.*` are now host-driven plugin commands → focus op with `{ window }`. `nav.jump` is a webview-bus handler.\n+- Added F1 (`apps/kanban-app/src/ui_request.rs`) + F2 (`provider.rs` `UiGeometryProvider`, `TauriUiGeometryProvider`, `query geometry/scope_chain/focus`). Responder `focus.geometry` reads live `focusedFqRef` (on-demand, no cache — correct).\n - `nav.yaml` + `nav_yaml.rs` deleted.\n\n ## Why tests are green but the app broke\n-`builtin_nav_commands_e2e.rs` injects a fake `SeedProvider` (fixed snapshot, pre-seeded focus); the real round-trip + `focus_by_window` freshness are never exercised. Fixture-only — green on mocks, broken live. (See [[feedback_fixture_only_anti_pattern]] / [[feedback_real_path_tests]].)\n+`builtin_nav_commands_e2e.rs` injects a fake `SeedProvider` (fixed snapshot, pre-seeded focus); the real `TauriUiGeometryProvider` round-trip + `focus_by_window` freshness are never exercised. Fixture-only: green on mocks, broken on the live path. (See [[feedback_fixture_only_anti_pattern]] / [[feedback_real_path_tests]].)\n\n ## Acceptance Criteria\n-- [ ] Arrow keys move focus again (board, grid, inspector) — directional + first/last + drill in/out.\n-- [ ] The nav origin (`from`) and window are derived from the dispatch SCOPE CHAIN (leaf + root); the nav op does NOT read `focus_by_window` for the origin.\n-- [ ] Geometry is the only thing pulled on-demand; the snapshot is built around the scope-chain `focused_fq`.\n-- [ ] F2 card AC #1 updated to the scope-chain model.\n+- [ ] Arrow keys move focus again in the running app (board, grid, inspector) — directional + first/last + drill in/out.\n+- [ ] Nav origin + geometry come from ONE atomic webview sample (`focus.navSource`); the nav op does NOT read `focus_by_window` for the origin.\n+- [ ] The host path resolves the correct `{window}` from the dispatch scope chain (no default-to-main).\n+- [ ] F2 card AC #1 updated to webview-authoritative focus.\n\n ## Tests\n-- [ ] REAL-PATH test (NOT SeedProvider): focus a scope (incl. click-to-focus), dispatch a nav with the real scope chain, assert focus moves to the geometrically-correct neighbor.\n-- [ ] Invariant test: the geometry snapshot contains the scope-chain leaf (`from ∈ snapshot.scopes`).\n-- [ ] Regression test that fails on the current working tree (nav drops), passes after.\n+- [ ] REAL-PATH test (NOT SeedProvider): drive a focus change (incl. click-to-focus) → arrow key → assert focus moves to the geometrically-correct neighbor, exercising the live `nav_source` pull.\n+- [ ] Atomicity test: the `focus.navSource` responder returns a `focused_fq` that is present in its returned `snapshot.scopes`.\n+- [ ] Regression test that fails on the current working tree (nav drops) and passes after.\n - [ ] Existing spatial suite stays green.\n\n ## Related\n-- F2 card `01KTEEDA9ZVTZ2R5CERW0WGK97` (quality gate; this fix supersedes its AC #1).\n+- F2 / nav-server card `01KTEEDA9ZVTZ2R5CERW0WGK97` (this regression is its quality gate; the fix supersedes its AC #1).\n - Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (blocked_by this).\n-- F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (host→UI channel — correct; reused for the geometry pull).\n-- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (scope chain rooted at `window:` — where the window comes from).\n\\ No newline at end of file\n+- F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (the host→UI channel — correct; reused for `focus.navSource`).\n+- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (window-moniker — drop point #3).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTF7FXKMRQM5C0M1127YB5RM","timestamp":"2026-06-06T19:45:22.548359Z","op":"update","item_id":"01KTESYQ49JYJB2YT1WXYKK0W4","forward_patch":"--- original\n+++ modified\n@@ -57,4 +57,4 @@\n - F2 card `01KTEEDA9ZVTZ2R5CERW0WGK97` (quality gate; this fix supersedes its AC #1).\n - Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (blocked_by this).\n - F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (host→UI channel — correct; reused for the geometry pull).\n-- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (scope chain rooted at `window:` — where the window comes from).\n\\ No newline at end of file\n+- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (scope chain rooted at `window:` — where the window comes from). #bug\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -57,4 +57,4 @@\n - F2 card `01KTEEDA9ZVTZ2R5CERW0WGK97` (quality gate; this fix supersedes its AC #1).\n - Card A `01KTCQFH7AEQDZD0QETSMCMGP0` (blocked_by this).\n - F1 `01KTEFP1RMM2G65GR1PH9YQWEZ` (host→UI channel — correct; reused for the geometry pull).\n-- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (scope chain rooted at `window:` — where the window comes from). #bug\n\\ No newline at end of file\n+- Harden `01KTECWA8D05FVKJ80MA3H0FFY` (scope chain rooted at `window:` — where the window comes from).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTF7FYF1MJ01TJB4QVXESVK3","timestamp":"2026-06-06T19:45:23.425078Z","op":"update","item_id":"01KTESYQ49JYJB2YT1WXYKK0W4","forward_patch":"--- original\n+++ modified\n@@ -45,7 +45,7 @@\n - [ ] Arrow keys move focus again (board, grid, inspector) — directional + first/last + drill in/out.\n - [ ] The nav origin (`from`) and window are derived from the dispatch SCOPE CHAIN (leaf + root); the nav op does NOT read `focus_by_window` for the origin.\n - [ ] Geometry is the only thing pulled on-demand; the snapshot is built around the scope-chain `focused_fq`.\n-- [ ] F2 card AC #1 updated to the scope-chain model.\n+- [ ] F2 card AC updated to the scope-chain model.\n\n ## Tests\n - [ ] REAL-PATH test (NOT SeedProvider): focus a scope (incl. click-to-focus), dispatch a nav with the real scope chain, assert focus moves to the geometrically-correct neighbor.\n","reverse_patch":"--- original\n+++ modified\n@@ -45,7 +45,7 @@\n - [ ] Arrow keys move focus again (board, grid, inspector) — directional + first/last + drill in/out.\n - [ ] The nav origin (`from`) and window are derived from the dispatch SCOPE CHAIN (leaf + root); the nav op does NOT read `focus_by_window` for the origin.\n - [ ] Geometry is the only thing pulled on-demand; the snapshot is built around the scope-chain `focused_fq`.\n-- [ ] F2 card AC updated to the scope-chain model.\n+- [ ] F2 card AC #1 updated to the scope-chain model.\n\n ## Tests\n - [ ] REAL-PATH test (NOT SeedProvider): focus a scope (incl. click-to-focus), dispatch a nav with the real scope chain, assert focus moves to the geometrically-correct neighbor.\n","transaction_id":null}

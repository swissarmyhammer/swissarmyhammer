---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kw2kem9fvkspcmh5gezq4s62
  text: |-
    Implemented via TDD.

    - Added `AgentUseCase` enum (Root/Rules/Workflows/Expectations, `#[serde(rename_all="lowercase")]`) in new module `crates/swissarmyhammer-config/src/agent_use_case.rs`; re-exported as `swissarmyhammer_config::AgentUseCase`. Serde round-trip tests (lowercase strings) — verified RED by removing rename_all (got "unknown variant `root`"), then GREEN.
    - Extended `ToolContext` (tool_registry.rs) with `pub use_case_agents: Arc<HashMap<AgentUseCase, ModelConfig>>` (mirrors `agent_config: Arc<ModelConfig>`), defaulted to empty map in `ToolContext::new`. Added builder `with_use_case_agents` and resolver `get_agent_for_use_case(&self, AgentUseCase) -> &ModelConfig` that falls back to root `agent_config` when unconfigured. 3 resolver tests (configured / fallback / empty-map-default).
    - Only one struct-literal constructor exists (`ToolContext::new`); all test helpers route through it, so the empty-map default keeps every existing caller/test unaffected — no other constructor touched.
    - server.rs: documented that `use_case_agents` is intentionally left empty pending a config surface for the `agents` mapping; empty map => silent fallback to root.

    Verification: `cargo nextest -p config -p tools -E 'test(agent_use_case) or test(use_case) or test(AgentUseCase)'` => 7 passed. `cargo check --workspace` ok. `cargo clippy -p config -p tools --all-targets -- -D warnings` clean. `cargo fmt` applied.

    Did NOT build the ACP driver (separate task). Left in `doing` for review.
  timestamp: 2026-06-26T18:35:39.183390+00:00
depends_on:
- 01KW25YZ4MKNR09RXYR1B4S05T
position_column: doing
position_ordinal: '8280'
project: expect
title: Add AgentUseCase::Expectations + ToolContext agent resolution
---
## What
`expect` needs a *driving* agent distinct from the *grading* model. Per `ideas/rule_agent.md` and `ideas/expect.md:93`, introduce a minimal use-case-based agent selection and slot in `Expectations`. Keep scope tight: only what `expect` needs, reusing the existing injected-factory pattern rather than rebuilding agent construction.

- Add `AgentUseCase` enum to `crates/swissarmyhammer-config` (variants `Root`, `Rules`, `Workflows`, `Expectations`; `#[serde(rename_all="lowercase")]`). This is the design's chosen placement (rule_agent.md §"Design Decisions").
- Extend `ToolContext` (`crates/swissarmyhammer-tools/src/mcp/tool_registry.rs:259`) with `use_case_agents: Arc<HashMap<AgentUseCase, ModelConfig>>` and a resolver `get_agent_for_use_case(&self, AgentUseCase) -> &ModelConfig` that falls back to the existing `agent_config` (root) when unset. Default the map to empty so existing tools are unaffected.
- Wire population from config at server init (`server.rs`) — read the `[agent]`/`agents` mapping; silent fallback to root when a use case is unconfigured (rule_agent.md decision #3).
- This task does NOT build the ACP driver (that's the agent-seam task); it only provides resolution so `expect` can later ask `ToolContext` for its `Expectations` agent.

## Acceptance Criteria
- [ ] `AgentUseCase` exists in `swissarmyhammer-config`, serializes to `"root"|"rules"|"workflows"|"expectations"`.
- [ ] `ToolContext::get_agent_for_use_case(AgentUseCase::Expectations)` returns the configured agent or falls back to root `agent_config`.
- [ ] Existing tools/tests unaffected (empty map ⇒ everything resolves to root).
- [ ] `cargo build` of config + tools crates succeeds.

## Tests
- [ ] Unit test in `swissarmyhammer-config`: `AgentUseCase` serde round-trip incl. lowercase strings.
- [ ] Unit test for `ToolContext::get_agent_for_use_case`: configured use case returns its agent; unconfigured falls back to root.
- [ ] `cargo nextest run -p swissarmyhammer-config -p swissarmyhammer-tools agent_use_case` passes.

## Workflow
- Use `/tdd`.
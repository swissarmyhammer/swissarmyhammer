{"id":"01KQ1XWCWHKHM4BXSFWH5Q6GBP","timestamp":"2026-04-25T09:00:12.561505Z","op":"create","item_id":"01KQ1XWCWHT7C4T71T02V99AGP","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,58 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: ff9380\n+title: Virtualize DataTable rows with @tanstack/react-virtual\n+---\n+## What\n+\n+Virtualize the body rows of `DataTable` (the renderer behind every grid view) using `@tanstack/react-virtual`. Today every data row mounts as a real `<tr>`, so a view with 2k+ entities pays the cost of mounting 2k × N `<td>` + `Field` subtrees on every render — even though only ~20 rows are ever on screen. Virtualizing the rows is the single biggest perf win available for grid views.\n+\n+**Files:**\n+- Modify: `kanban-app/ui/src/components/data-table.tsx`\n+- Modify (probably): `kanban-app/ui/src/components/grid-view.tsx` — only if scroll-into-view for the cursor needs to switch from `cursorRef.scrollIntoView` to `virtualizer.scrollToIndex`. Keep `data-table.tsx`-internal if possible.\n+- Add test: `kanban-app/ui/src/components/data-table.virtualized.test.tsx`\n+\n+**Approach:**\n+\n+1. The dependency is already installed — `@tanstack/react-virtual ^3.13.23` (see `kanban-app/ui/package.json:30`, used by `column-view.tsx::VirtualColumn` for reference).\n+2. The scroll element is the existing outer `<div className=\"flex-1 overflow-auto min-h-0\">` at `data-table.tsx:95-98`. Attach a `ref` and pass via `getScrollElement`.\n+3. **Only virtualize body rows.** Leave the existing sticky header (`<TableHeader className=\"sticky top-0 z-[1] bg-muted/80 backdrop-blur-sm\">` at `data-table.tsx:100`) alone — it already pins via CSS sticky and must continue to do so. The user explicitly does NOT want column virtualization; all columns render every row.\n+4. Use `useVirtualizer` over `flatRows = table.getRowModel().rows` (which includes both data rows and TanStack group-header rows — both need to be virtualized as one flat list, since `dataRowIndices` already maps flat-row index → data-row index).\n+5. **Fixed row height.** Rows are uniform: every cell uses `px-3 py-1.5` (`data-table.tsx:558`) and renders `<Field mode=\"compact\" />`. Define a `ROW_HEIGHT` constant (start with `32` px — verify by measuring an actual rendered row in the running app and adjust) and use `estimateSize: () => ROW_HEIGHT`. Do NOT use `measureElement` — fixed height makes dynamic measurement unnecessary and avoids the layout-thrash cost. If grouping introduces a different header-row height, `estimateSize: (i) => flatRows[i].getIsGrouped() ? GROUP_ROW_HEIGHT : ROW_HEIGHT` is the escape hatch.\n+6. **`<table>` virtualization pattern (padding-row technique):** because we render inside a real `<table>`, render two empty `<tr>` spacers — one before the virtualized window with `style={{ height: paddingTop }}` and one after with `style={{ height: paddingBottom }}` — where `paddingTop = virtualItems[0]?.start ?? 0` and `paddingBottom = totalSize - (virtualItems[last]?.end ?? 0)`. Then map `virtualItems.map(vi => <DataTableRow key={flatRows[vi.index].id} row={flatRows[vi.index]} ri={vi.index} ... />)` — keep the existing `DataTableRow` memo signature unchanged.\n+7. **Cursor scroll-into-view:** the existing `useEffect` at `data-table.tsx:185-187` calls `cursorRef.current?.scrollIntoView()`. With virtualization, the cursor row may not be mounted at all when the user moves the cursor past the visible window. Replace (or augment) with `virtualizer.scrollToIndex(grid.cursor.row)` so the row scrolls into view *first*, then the existing `cursorRef.scrollIntoView` (when the row remounts) handles the inline/horizontal scroll. Be careful: `grid.cursor.row` is a *data-row* index, but the virtualizer indexes into `flatRows`. Use `dataRowIndices.indexOf(grid.cursor.row)` (or build the inverse map) to translate before calling `scrollToIndex`.\n+8. `overscan: 5` matches `column-view.tsx:844`.\n+9. Set `count: flatRows.length` so the virtualizer reacts to grouping/expansion changes.\n+\n+**Out of scope:**\n+- Column virtualization. Sticky columns stay; do not introduce horizontal virtualization.\n+- Changing `DataTableRow`/`DataBodyCell`/`GroupHeaderRow` internals — they should be rendered by the virtualizer wrapper without modification.\n+- Touching `column-view.tsx` (already virtualized).\n+- Changing `Table` / `TableBody` shadcn primitives (`kanban-app/ui/src/components/ui/table.tsx`).\n+\n+## Acceptance Criteria\n+\n+- [ ] With a 2000-row grid view rendered, only ~20–30 `<tr data-slot=\"table-row\">` elements exist in the DOM at any one time (initial viewport + overscan), not 2000+.\n+- [ ] Sticky header still pins to the top of the scroll container during scroll (verify visually in dev: `cd kanban-app && bun tauri dev`, scroll a long grid view).\n+- [ ] Cursor navigation (`j`/`k`/arrow keys) past the visible window scrolls the cursor row into view — the cell becomes mounted and visible, focus highlight is correct.\n+- [ ] Grouping still works: collapsing/expanding a group changes the visible row count and the virtualizer adjusts (`count` follows `flatRows.length`).\n+- [ ] Sort header click still works (re-sorts and the virtualized list reflects new order).\n+- [ ] Right-click on whitespace below the last row still fires `onContainerContextMenu` (the bottom padding `<tr>` must not swallow the event — give it `pointer-events: none` if needed, or attach the listener on the outer scroll `<div>` as today).\n+- [ ] No measurable regression on grid views with <50 rows (small grids should not pay overhead beyond a single `useVirtualizer` call).\n+- [ ] No new console warnings/errors during initial mount or scroll.\n+\n+## Tests\n+\n+- [ ] Add `kanban-app/ui/src/components/data-table.virtualized.test.tsx`. Mock the scroll container's `getBoundingClientRect` (or use `defineProperty` on `clientHeight`) so jsdom reports a finite height — `useVirtualizer` returns 0 items in jsdom otherwise. Reuse the mock setup from `data-table.test.tsx:9-50`.\n+- [ ] Test 1: render `DataTable` with 1000 rows, assert `container.querySelectorAll('tr[data-slot=\"table-row\"]').length` is well under 100 (e.g. `<= 50`). This proves rows are virtualized at all.\n+- [ ] Test 2: cursor scroll-into-view. Render with 1000 rows, programmatically advance `grid.cursor.row` to 500, assert `virtualizer.scrollToIndex` was called (spy on it) OR that the row at index 500 is now in the DOM after a `flushSync` / `await act(...)`. Pick whichever is easier given the existing `useGrid` test fixtures.\n+- [ ] Test 3: existing `data-table.test.tsx` row-structure assertions must still pass unchanged — virtualization should be invisible to those tests, which use small fixtures (<10 rows). Run `cd kanban-app/ui && bun run test data-table` and confirm green.\n+- [ ] Test 4: existing `grid-view.test.tsx` and `grid-view.stale-card-fields.test.tsx` must still pass. Run `cd kanban-app/ui && bun run test grid-view` and confirm green.\n+- [ ] Run full UI suite: `cd kanban-app/ui && bun run test` — no new failures, no new warnings.\n+- [ ] Manual smoke: launch `cd kanban-app && bun tauri dev`, open a grid view with many entities (or seed via the kanban board), scroll to bottom, scroll back to top, navigate with `j`/`k`, group by a column, collapse/expand a group. All should feel snappy with no flicker or missing rows.\n+\n+## Workflow\n+\n+- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,58 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: ff9380\n-title: Virtualize DataTable rows with @tanstack/react-virtual\n----\n-## What\n-\n-Virtualize the body rows of `DataTable` (the renderer behind every grid view) using `@tanstack/react-virtual`. Today every data row mounts as a real `<tr>`, so a view with 2k+ entities pays the cost of mounting 2k × N `<td>` + `Field` subtrees on every render — even though only ~20 rows are ever on screen. Virtualizing the rows is the single biggest perf win available for grid views.\n-\n-**Files:**\n-- Modify: `kanban-app/ui/src/components/data-table.tsx`\n-- Modify (probably): `kanban-app/ui/src/components/grid-view.tsx` — only if scroll-into-view for the cursor needs to switch from `cursorRef.scrollIntoView` to `virtualizer.scrollToIndex`. Keep `data-table.tsx`-internal if possible.\n-- Add test: `kanban-app/ui/src/components/data-table.virtualized.test.tsx`\n-\n-**Approach:**\n-\n-1. The dependency is already installed — `@tanstack/react-virtual ^3.13.23` (see `kanban-app/ui/package.json:30`, used by `column-view.tsx::VirtualColumn` for reference).\n-2. The scroll element is the existing outer `<div className=\"flex-1 overflow-auto min-h-0\">` at `data-table.tsx:95-98`. Attach a `ref` and pass via `getScrollElement`.\n-3. **Only virtualize body rows.** Leave the existing sticky header (`<TableHeader className=\"sticky top-0 z-[1] bg-muted/80 backdrop-blur-sm\">` at `data-table.tsx:100`) alone — it already pins via CSS sticky and must continue to do so. The user explicitly does NOT want column virtualization; all columns render every row.\n-4. Use `useVirtualizer` over `flatRows = table.getRowModel().rows` (which includes both data rows and TanStack group-header rows — both need to be virtualized as one flat list, since `dataRowIndices` already maps flat-row index → data-row index).\n-5. **Fixed row height.** Rows are uniform: every cell uses `px-3 py-1.5` (`data-table.tsx:558`) and renders `<Field mode=\"compact\" />`. Define a `ROW_HEIGHT` constant (start with `32` px — verify by measuring an actual rendered row in the running app and adjust) and use `estimateSize: () => ROW_HEIGHT`. Do NOT use `measureElement` — fixed height makes dynamic measurement unnecessary and avoids the layout-thrash cost. If grouping introduces a different header-row height, `estimateSize: (i) => flatRows[i].getIsGrouped() ? GROUP_ROW_HEIGHT : ROW_HEIGHT` is the escape hatch.\n-6. **`<table>` virtualization pattern (padding-row technique):** because we render inside a real `<table>`, render two empty `<tr>` spacers — one before the virtualized window with `style={{ height: paddingTop }}` and one after with `style={{ height: paddingBottom }}` — where `paddingTop = virtualItems[0]?.start ?? 0` and `paddingBottom = totalSize - (virtualItems[last]?.end ?? 0)`. Then map `virtualItems.map(vi => <DataTableRow key={flatRows[vi.index].id} row={flatRows[vi.index]} ri={vi.index} ... />)` — keep the existing `DataTableRow` memo signature unchanged.\n-7. **Cursor scroll-into-view:** the existing `useEffect` at `data-table.tsx:185-187` calls `cursorRef.current?.scrollIntoView()`. With virtualization, the cursor row may not be mounted at all when the user moves the cursor past the visible window. Replace (or augment) with `virtualizer.scrollToIndex(grid.cursor.row)` so the row scrolls into view *first*, then the existing `cursorRef.scrollIntoView` (when the row remounts) handles the inline/horizontal scroll. Be careful: `grid.cursor.row` is a *data-row* index, but the virtualizer indexes into `flatRows`. Use `dataRowIndices.indexOf(grid.cursor.row)` (or build the inverse map) to translate before calling `scrollToIndex`.\n-8. `overscan: 5` matches `column-view.tsx:844`.\n-9. Set `count: flatRows.length` so the virtualizer reacts to grouping/expansion changes.\n-\n-**Out of scope:**\n-- Column virtualization. Sticky columns stay; do not introduce horizontal virtualization.\n-- Changing `DataTableRow`/`DataBodyCell`/`GroupHeaderRow` internals — they should be rendered by the virtualizer wrapper without modification.\n-- Touching `column-view.tsx` (already virtualized).\n-- Changing `Table` / `TableBody` shadcn primitives (`kanban-app/ui/src/components/ui/table.tsx`).\n-\n-## Acceptance Criteria\n-\n-- [ ] With a 2000-row grid view rendered, only ~20–30 `<tr data-slot=\"table-row\">` elements exist in the DOM at any one time (initial viewport + overscan), not 2000+.\n-- [ ] Sticky header still pins to the top of the scroll container during scroll (verify visually in dev: `cd kanban-app && bun tauri dev`, scroll a long grid view).\n-- [ ] Cursor navigation (`j`/`k`/arrow keys) past the visible window scrolls the cursor row into view — the cell becomes mounted and visible, focus highlight is correct.\n-- [ ] Grouping still works: collapsing/expanding a group changes the visible row count and the virtualizer adjusts (`count` follows `flatRows.length`).\n-- [ ] Sort header click still works (re-sorts and the virtualized list reflects new order).\n-- [ ] Right-click on whitespace below the last row still fires `onContainerContextMenu` (the bottom padding `<tr>` must not swallow the event — give it `pointer-events: none` if needed, or attach the listener on the outer scroll `<div>` as today).\n-- [ ] No measurable regression on grid views with <50 rows (small grids should not pay overhead beyond a single `useVirtualizer` call).\n-- [ ] No new console warnings/errors during initial mount or scroll.\n-\n-## Tests\n-\n-- [ ] Add `kanban-app/ui/src/components/data-table.virtualized.test.tsx`. Mock the scroll container's `getBoundingClientRect` (or use `defineProperty` on `clientHeight`) so jsdom reports a finite height — `useVirtualizer` returns 0 items in jsdom otherwise. Reuse the mock setup from `data-table.test.tsx:9-50`.\n-- [ ] Test 1: render `DataTable` with 1000 rows, assert `container.querySelectorAll('tr[data-slot=\"table-row\"]').length` is well under 100 (e.g. `<= 50`). This proves rows are virtualized at all.\n-- [ ] Test 2: cursor scroll-into-view. Render with 1000 rows, programmatically advance `grid.cursor.row` to 500, assert `virtualizer.scrollToIndex` was called (spy on it) OR that the row at index 500 is now in the DOM after a `flushSync` / `await act(...)`. Pick whichever is easier given the existing `useGrid` test fixtures.\n-- [ ] Test 3: existing `data-table.test.tsx` row-structure assertions must still pass unchanged — virtualization should be invisible to those tests, which use small fixtures (<10 rows). Run `cd kanban-app/ui && bun run test data-table` and confirm green.\n-- [ ] Test 4: existing `grid-view.test.tsx` and `grid-view.stale-card-fields.test.tsx` must still pass. Run `cd kanban-app/ui && bun run test grid-view` and confirm green.\n-- [ ] Run full UI suite: `cd kanban-app/ui && bun run test` — no new failures, no new warnings.\n-- [ ] Manual smoke: launch `cd kanban-app && bun tauri dev`, open a grid view with many entities (or seed via the kanban board), scroll to bottom, scroll back to top, navigate with `j`/`k`, group by a column, collapse/expand a group. All should feel snappy with no flicker or missing rows.\n-\n-## Workflow\n-\n-- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ1XWCWH3REAECSCAJ1JTMK9","timestamp":"2026-04-25T09:00:12.561947Z","entity_type":"task","entity_id":"01KQ1XWCWHT7C4T71T02V99AGP","op":"create","changes":[["title",{"kind":"set","value":"Virtualize DataTable rows with @tanstack/react-virtual"}],["position_ordinal",{"kind":"set","value":"ff9380"}],["body",{"kind":"set","value":"## What\n\nVirtualize the body rows of `DataTable` (the renderer behind every grid view) using `@tanstack/react-virtual`. Today every data row mounts as a real `<tr>`, so a view with 2k+ entities pays the cost of mounting 2k × N `<td>` + `Field` subtrees on every render — even though only ~20 rows are ever on screen. Virtualizing the rows is the single biggest perf win available for grid views.\n\n**Files:**\n- Modify: `kanban-app/ui/src/components/data-table.tsx`\n- Modify (probably): `kanban-app/ui/src/components/grid-view.tsx` — only if scroll-into-view for the cursor needs to switch from `cursorRef.scrollIntoView` to `virtualizer.scrollToIndex`. Keep `data-table.tsx`-internal if possible.\n- Add test: `kanban-app/ui/src/components/data-table.virtualized.test.tsx`\n\n**Approach:**\n\n1. The dependency is already installed — `@tanstack/react-virtual ^3.13.23` (see `kanban-app/ui/package.json:30`, used by `column-view.tsx::VirtualColumn` for reference).\n2. The scroll element is the existing outer `<div className=\"flex-1 overflow-auto min-h-0\">` at `data-table.tsx:95-98`. Attach a `ref` and pass via `getScrollElement`.\n3. **Only virtualize body rows.** Leave the existing sticky header (`<TableHeader className=\"sticky top-0 z-[1] bg-muted/80 backdrop-blur-sm\">` at `data-table.tsx:100`) alone — it already pins via CSS sticky and must continue to do so. The user explicitly does NOT want column virtualization; all columns render every row.\n4. Use `useVirtualizer` over `flatRows = table.getRowModel().rows` (which includes both data rows and TanStack group-header rows — both need to be virtualized as one flat list, since `dataRowIndices` already maps flat-row index → data-row index).\n5. **Fixed row height.** Rows are uniform: every cell uses `px-3 py-1.5` (`data-table.tsx:558`) and renders `<Field mode=\"compact\" />`. Define a `ROW_HEIGHT` constant (start with `32` px — verify by measuring an actual rendered row in the running app and adjust) and use `estimateSize: () => ROW_HEIGHT`. Do NOT use `measureElement` — fixed height makes dynamic measurement unnecessary and avoids the layout-thrash cost. If grouping introduces a different header-row height, `estimateSize: (i) => flatRows[i].getIsGrouped() ? GROUP_ROW_HEIGHT : ROW_HEIGHT` is the escape hatch.\n6. **`<table>` virtualization pattern (padding-row technique):** because we render inside a real `<table>`, render two empty `<tr>` spacers — one before the virtualized window with `style={{ height: paddingTop }}` and one after with `style={{ height: paddingBottom }}` — where `paddingTop = virtualItems[0]?.start ?? 0` and `paddingBottom = totalSize - (virtualItems[last]?.end ?? 0)`. Then map `virtualItems.map(vi => <DataTableRow key={flatRows[vi.index].id} row={flatRows[vi.index]} ri={vi.index} ... />)` — keep the existing `DataTableRow` memo signature unchanged.\n7. **Cursor scroll-into-view:** the existing `useEffect` at `data-table.tsx:185-187` calls `cursorRef.current?.scrollIntoView()`. With virtualization, the cursor row may not be mounted at all when the user moves the cursor past the visible window. Replace (or augment) with `virtualizer.scrollToIndex(grid.cursor.row)` so the row scrolls into view *first*, then the existing `cursorRef.scrollIntoView` (when the row remounts) handles the inline/horizontal scroll. Be careful: `grid.cursor.row` is a *data-row* index, but the virtualizer indexes into `flatRows`. Use `dataRowIndices.indexOf(grid.cursor.row)` (or build the inverse map) to translate before calling `scrollToIndex`.\n8. `overscan: 5` matches `column-view.tsx:844`.\n9. Set `count: flatRows.length` so the virtualizer reacts to grouping/expansion changes.\n\n**Out of scope:**\n- Column virtualization. Sticky columns stay; do not introduce horizontal virtualization.\n- Changing `DataTableRow`/`DataBodyCell`/`GroupHeaderRow` internals — they should be rendered by the virtualizer wrapper without modification.\n- Touching `column-view.tsx` (already virtualized).\n- Changing `Table` / `TableBody` shadcn primitives (`kanban-app/ui/src/components/ui/table.tsx`).\n\n## Acceptance Criteria\n\n- [ ] With a 2000-row grid view rendered, only ~20–30 `<tr data-slot=\"table-row\">` elements exist in the DOM at any one time (initial viewport + overscan), not 2000+.\n- [ ] Sticky header still pins to the top of the scroll container during scroll (verify visually in dev: `cd kanban-app && bun tauri dev`, scroll a long grid view).\n- [ ] Cursor navigation (`j`/`k`/arrow keys) past the visible window scrolls the cursor row into view — the cell becomes mounted and visible, focus highlight is correct.\n- [ ] Grouping still works: collapsing/expanding a group changes the visible row count and the virtualizer adjusts (`count` follows `flatRows.length`).\n- [ ] Sort header click still works (re-sorts and the virtualized list reflects new order).\n- [ ] Right-click on whitespace below the last row still fires `onContainerContextMenu` (the bottom padding `<tr>` must not swallow the event — give it `pointer-events: none` if needed, or attach the listener on the outer scroll `<div>` as today).\n- [ ] No measurable regression on grid views with <50 rows (small grids should not pay overhead beyond a single `useVirtualizer` call).\n- [ ] No new console warnings/errors during initial mount or scroll.\n\n## Tests\n\n- [ ] Add `kanban-app/ui/src/components/data-table.virtualized.test.tsx`. Mock the scroll container's `getBoundingClientRect` (or use `defineProperty` on `clientHeight`) so jsdom reports a finite height — `useVirtualizer` returns 0 items in jsdom otherwise. Reuse the mock setup from `data-table.test.tsx:9-50`.\n- [ ] Test 1: render `DataTable` with 1000 rows, assert `container.querySelectorAll('tr[data-slot=\"table-row\"]').length` is well under 100 (e.g. `<= 50`). This proves rows are virtualized at all.\n- [ ] Test 2: cursor scroll-into-view. Render with 1000 rows, programmatically advance `grid.cursor.row` to 500, assert `virtualizer.scrollToIndex` was called (spy on it) OR that the row at index 500 is now in the DOM after a `flushSync` / `await act(...)`. Pick whichever is easier given the existing `useGrid` test fixtures.\n- [ ] Test 3: existing `data-table.test.tsx` row-structure assertions must still pass unchanged — virtualization should be invisible to those tests, which use small fixtures (<10 rows). Run `cd kanban-app/ui && bun run test data-table` and confirm green.\n- [ ] Test 4: existing `grid-view.test.tsx` and `grid-view.stale-card-fields.test.tsx` must still pass. Run `cd kanban-app/ui && bun run test grid-view` and confirm green.\n- [ ] Run full UI suite: `cd kanban-app/ui && bun run test` — no new failures, no new warnings.\n- [ ] Manual smoke: launch `cd kanban-app && bun tauri dev`, open a grid view with many entities (or seed via the kanban board), scroll to bottom, scroll back to top, navigate with `j`/`k`, group by a column, collapse/expand a group. All should feel snappy with no flicker or missing rows.\n\n## Workflow\n\n- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests."}],["assignees",{"kind":"set","value":["claude-code"]}],["position_column",{"kind":"set","value":"todo"}]]}
{"id":"01KQ1XWGRBJE92EQXR5WYDYDH0","timestamp":"2026-04-25T09:00:16.523655Z","op":"update","item_id":"01KQ1XWCWHT7C4T71T02V99AGP","forward_patch":"--- original\n+++ modified\n@@ -55,4 +55,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests.\n\\ No newline at end of file\n+- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -55,4 +55,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance\n\\ No newline at end of file\n+- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ1XWGRC7W8D2M6GKJZERCC9","timestamp":"2026-04-25T09:00:16.524150Z","entity_type":"task","entity_id":"01KQ1XWCWHT7C4T71T02V99AGP","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -48,4 +48,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests.\n\\ No newline at end of file\n+- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -48,4 +48,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance\n\\ No newline at end of file\n+- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ1XWH8HNV8TGWPFMZKR3NMF","timestamp":"2026-04-25T09:00:17.041257Z","op":"update","item_id":"01KQ1XWCWHT7C4T71T02V99AGP","forward_patch":"--- original\n+++ modified\n@@ -55,4 +55,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance\n\\ No newline at end of file\n+- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance #frontend\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -55,4 +55,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance #frontend\n\\ No newline at end of file\n+- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ1XWH8HY1YZTAY998K9YTX9","timestamp":"2026-04-25T09:00:17.041684Z","entity_type":"task","entity_id":"01KQ1XWCWHT7C4T71T02V99AGP","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -48,4 +48,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance\n\\ No newline at end of file\n+- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance #frontend\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -48,4 +48,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance #frontend\n\\ No newline at end of file\n+- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance\n\\ No newline at end of file\n"}]]}
{"id":"01KQ1XWHQ66NDQKT3CKY9XH6T2","timestamp":"2026-04-25T09:00:17.510252Z","op":"update","item_id":"01KQ1XWCWHT7C4T71T02V99AGP","forward_patch":"--- original\n+++ modified\n@@ -55,4 +55,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance #frontend\n\\ No newline at end of file\n+- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance #frontend #kanban-app\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -55,4 +55,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance #frontend #kanban-app\n\\ No newline at end of file\n+- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance #frontend\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ1XWHQ6NKK4V7QWPFE9457P","timestamp":"2026-04-25T09:00:17.510686Z","entity_type":"task","entity_id":"01KQ1XWCWHT7C4T71T02V99AGP","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -48,4 +48,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance #frontend\n\\ No newline at end of file\n+- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance #frontend #kanban-app\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -48,4 +48,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance #frontend #kanban-app\n\\ No newline at end of file\n+- Use `/tdd` — write the virtualization smoke test (test 1 above) first, watch it fail (it currently asserts 1000 rows are rendered), then implement the virtualizer wrapper to make it pass. Then layer in cursor-scroll handling and the remaining tests. #performance #frontend\n\\ No newline at end of file\n"}]]}
{"id":"01KQ1YH02YPZY2YQ830JSD4SK1","timestamp":"2026-04-25T09:11:27.582145Z","op":"update","item_id":"01KQ1XWCWHT7C4T71T02V99AGP","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: ff9380\n+position_column: doing\n+position_ordinal: '80'\n title: Virtualize DataTable rows with @tanstack/react-virtual\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: ff9380\n title: Virtualize DataTable rows with @tanstack/react-virtual\n ---\n ## What\n","transaction_id":null}
{"id":"01KQ1YH02YS9MMYEA2TKBCHZZ5","timestamp":"2026-04-25T09:11:27.582855Z","entity_type":"task","entity_id":"01KQ1XWCWHT7C4T71T02V99AGP","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ff9380\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ff9380\n\\ No newline at end of file\n"}]]}
{"id":"01KQ20HM3DG2TQCY2WJEPC2KKX","timestamp":"2026-04-25T09:46:45.229236Z","op":"update","item_id":"01KQ1XWCWHT7C4T71T02V99AGP","forward_patch":"--- original\n+++ modified\n@@ -34,24 +34,24 @@\n\n ## Acceptance Criteria\n\n-- [ ] With a 2000-row grid view rendered, only ~20–30 `<tr data-slot=\"table-row\">` elements exist in the DOM at any one time (initial viewport + overscan), not 2000+.\n-- [ ] Sticky header still pins to the top of the scroll container during scroll (verify visually in dev: `cd kanban-app && bun tauri dev`, scroll a long grid view).\n-- [ ] Cursor navigation (`j`/`k`/arrow keys) past the visible window scrolls the cursor row into view — the cell becomes mounted and visible, focus highlight is correct.\n-- [ ] Grouping still works: collapsing/expanding a group changes the visible row count and the virtualizer adjusts (`count` follows `flatRows.length`).\n-- [ ] Sort header click still works (re-sorts and the virtualized list reflects new order).\n-- [ ] Right-click on whitespace below the last row still fires `onContainerContextMenu` (the bottom padding `<tr>` must not swallow the event — give it `pointer-events: none` if needed, or attach the listener on the outer scroll `<div>` as today).\n-- [ ] No measurable regression on grid views with <50 rows (small grids should not pay overhead beyond a single `useVirtualizer` call).\n-- [ ] No new console warnings/errors during initial mount or scroll.\n+- [x] With a 2000-row grid view rendered, only ~20–30 `<tr data-slot=\"table-row\">` elements exist in the DOM at any one time (initial viewport + overscan), not 2000+.\n+- [x] Sticky header still pins to the top of the scroll container during scroll (verify visually in dev: `cd kanban-app && bun tauri dev`, scroll a long grid view).\n+- [x] Cursor navigation (`j`/`k`/arrow keys) past the visible window scrolls the cursor row into view — the cell becomes mounted and visible, focus highlight is correct.\n+- [x] Grouping still works: collapsing/expanding a group changes the visible row count and the virtualizer adjusts (`count` follows `flatRows.length`).\n+- [x] Sort header click still works (re-sorts and the virtualized list reflects new order).\n+- [x] Right-click on whitespace below the last row still fires `onContainerContextMenu` (the bottom padding `<tr>` must not swallow the event — give it `pointer-events: none` if needed, or attach the listener on the outer scroll `<div>` as today).\n+- [x] No measurable regression on grid views with <50 rows (small grids should not pay overhead beyond a single `useVirtualizer` call).\n+- [x] No new console warnings/errors during initial mount or scroll.\n\n ## Tests\n\n-- [ ] Add `kanban-app/ui/src/components/data-table.virtualized.test.tsx`. Mock the scroll container's `getBoundingClientRect` (or use `defineProperty` on `clientHeight`) so jsdom reports a finite height — `useVirtualizer` returns 0 items in jsdom otherwise. Reuse the mock setup from `data-table.test.tsx:9-50`.\n-- [ ] Test 1: render `DataTable` with 1000 rows, assert `container.querySelectorAll('tr[data-slot=\"table-row\"]').length` is well under 100 (e.g. `<= 50`). This proves rows are virtualized at all.\n-- [ ] Test 2: cursor scroll-into-view. Render with 1000 rows, programmatically advance `grid.cursor.row` to 500, assert `virtualizer.scrollToIndex` was called (spy on it) OR that the row at index 500 is now in the DOM after a `flushSync` / `await act(...)`. Pick whichever is easier given the existing `useGrid` test fixtures.\n-- [ ] Test 3: existing `data-table.test.tsx` row-structure assertions must still pass unchanged — virtualization should be invisible to those tests, which use small fixtures (<10 rows). Run `cd kanban-app/ui && bun run test data-table` and confirm green.\n-- [ ] Test 4: existing `grid-view.test.tsx` and `grid-view.stale-card-fields.test.tsx` must still pass. Run `cd kanban-app/ui && bun run test grid-view` and confirm green.\n-- [ ] Run full UI suite: `cd kanban-app/ui && bun run test` — no new failures, no new warnings.\n-- [ ] Manual smoke: launch `cd kanban-app && bun tauri dev`, open a grid view with many entities (or seed via the kanban board), scroll to bottom, scroll back to top, navigate with `j`/`k`, group by a column, collapse/expand a group. All should feel snappy with no flicker or missing rows.\n+- [x] Add `kanban-app/ui/src/components/data-table.virtualized.test.tsx`. Mock the scroll container's `getBoundingClientRect` (or use `defineProperty` on `clientHeight`) so jsdom reports a finite height — `useVirtualizer` returns 0 items in jsdom otherwise. Reuse the mock setup from `data-table.test.tsx:9-50`.\n+- [x] Test 1: render `DataTable` with 1000 rows, assert `container.querySelectorAll('tr[data-slot=\"table-row\"]').length` is well under 100 (e.g. `<= 50`). This proves rows are virtualized at all.\n+- [x] Test 2: cursor scroll-into-view. Render with 1000 rows, programmatically advance `grid.cursor.row` to 500, assert `virtualizer.scrollToIndex` was called (spy on it) OR that the row at index 500 is now in the DOM after a `flushSync` / `await act(...)`. Pick whichever is easier given the existing `useGrid` test fixtures.\n+- [x] Test 3: existing `data-table.test.tsx` row-structure assertions must still pass unchanged — virtualization should be invisible to those tests, which use small fixtures (<10 rows). Run `cd kanban-app/ui && bun run test data-table` and confirm green.\n+- [x] Test 4: existing `grid-view.test.tsx` and `grid-view.stale-card-fields.test.tsx` must still pass. Run `cd kanban-app/ui && bun run test grid-view` and confirm green.\n+- [x] Run full UI suite: `cd kanban-app/ui && bun run test` — no new failures, no new warnings.\n+- [x] Manual smoke: launch `cd kanban-app && bun tauri dev`, open a grid view with many entities (or seed via the kanban board), scroll to bottom, scroll back to top, navigate with `j`/`k`, group by a column, collapse/expand a group. All should feel snappy with no flicker or missing rows.\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -34,24 +34,24 @@\n\n ## Acceptance Criteria\n\n-- [x] With a 2000-row grid view rendered, only ~20–30 `<tr data-slot=\"table-row\">` elements exist in the DOM at any one time (initial viewport + overscan), not 2000+.\n-- [x] Sticky header still pins to the top of the scroll container during scroll (verify visually in dev: `cd kanban-app && bun tauri dev`, scroll a long grid view).\n-- [x] Cursor navigation (`j`/`k`/arrow keys) past the visible window scrolls the cursor row into view — the cell becomes mounted and visible, focus highlight is correct.\n-- [x] Grouping still works: collapsing/expanding a group changes the visible row count and the virtualizer adjusts (`count` follows `flatRows.length`).\n-- [x] Sort header click still works (re-sorts and the virtualized list reflects new order).\n-- [x] Right-click on whitespace below the last row still fires `onContainerContextMenu` (the bottom padding `<tr>` must not swallow the event — give it `pointer-events: none` if needed, or attach the listener on the outer scroll `<div>` as today).\n-- [x] No measurable regression on grid views with <50 rows (small grids should not pay overhead beyond a single `useVirtualizer` call).\n-- [x] No new console warnings/errors during initial mount or scroll.\n+- [ ] With a 2000-row grid view rendered, only ~20–30 `<tr data-slot=\"table-row\">` elements exist in the DOM at any one time (initial viewport + overscan), not 2000+.\n+- [ ] Sticky header still pins to the top of the scroll container during scroll (verify visually in dev: `cd kanban-app && bun tauri dev`, scroll a long grid view).\n+- [ ] Cursor navigation (`j`/`k`/arrow keys) past the visible window scrolls the cursor row into view — the cell becomes mounted and visible, focus highlight is correct.\n+- [ ] Grouping still works: collapsing/expanding a group changes the visible row count and the virtualizer adjusts (`count` follows `flatRows.length`).\n+- [ ] Sort header click still works (re-sorts and the virtualized list reflects new order).\n+- [ ] Right-click on whitespace below the last row still fires `onContainerContextMenu` (the bottom padding `<tr>` must not swallow the event — give it `pointer-events: none` if needed, or attach the listener on the outer scroll `<div>` as today).\n+- [ ] No measurable regression on grid views with <50 rows (small grids should not pay overhead beyond a single `useVirtualizer` call).\n+- [ ] No new console warnings/errors during initial mount or scroll.\n\n ## Tests\n\n-- [x] Add `kanban-app/ui/src/components/data-table.virtualized.test.tsx`. Mock the scroll container's `getBoundingClientRect` (or use `defineProperty` on `clientHeight`) so jsdom reports a finite height — `useVirtualizer` returns 0 items in jsdom otherwise. Reuse the mock setup from `data-table.test.tsx:9-50`.\n-- [x] Test 1: render `DataTable` with 1000 rows, assert `container.querySelectorAll('tr[data-slot=\"table-row\"]').length` is well under 100 (e.g. `<= 50`). This proves rows are virtualized at all.\n-- [x] Test 2: cursor scroll-into-view. Render with 1000 rows, programmatically advance `grid.cursor.row` to 500, assert `virtualizer.scrollToIndex` was called (spy on it) OR that the row at index 500 is now in the DOM after a `flushSync` / `await act(...)`. Pick whichever is easier given the existing `useGrid` test fixtures.\n-- [x] Test 3: existing `data-table.test.tsx` row-structure assertions must still pass unchanged — virtualization should be invisible to those tests, which use small fixtures (<10 rows). Run `cd kanban-app/ui && bun run test data-table` and confirm green.\n-- [x] Test 4: existing `grid-view.test.tsx` and `grid-view.stale-card-fields.test.tsx` must still pass. Run `cd kanban-app/ui && bun run test grid-view` and confirm green.\n-- [x] Run full UI suite: `cd kanban-app/ui && bun run test` — no new failures, no new warnings.\n-- [x] Manual smoke: launch `cd kanban-app && bun tauri dev`, open a grid view with many entities (or seed via the kanban board), scroll to bottom, scroll back to top, navigate with `j`/`k`, group by a column, collapse/expand a group. All should feel snappy with no flicker or missing rows.\n+- [ ] Add `kanban-app/ui/src/components/data-table.virtualized.test.tsx`. Mock the scroll container's `getBoundingClientRect` (or use `defineProperty` on `clientHeight`) so jsdom reports a finite height — `useVirtualizer` returns 0 items in jsdom otherwise. Reuse the mock setup from `data-table.test.tsx:9-50`.\n+- [ ] Test 1: render `DataTable` with 1000 rows, assert `container.querySelectorAll('tr[data-slot=\"table-row\"]').length` is well under 100 (e.g. `<= 50`). This proves rows are virtualized at all.\n+- [ ] Test 2: cursor scroll-into-view. Render with 1000 rows, programmatically advance `grid.cursor.row` to 500, assert `virtualizer.scrollToIndex` was called (spy on it) OR that the row at index 500 is now in the DOM after a `flushSync` / `await act(...)`. Pick whichever is easier given the existing `useGrid` test fixtures.\n+- [ ] Test 3: existing `data-table.test.tsx` row-structure assertions must still pass unchanged — virtualization should be invisible to those tests, which use small fixtures (<10 rows). Run `cd kanban-app/ui && bun run test data-table` and confirm green.\n+- [ ] Test 4: existing `grid-view.test.tsx` and `grid-view.stale-card-fields.test.tsx` must still pass. Run `cd kanban-app/ui && bun run test grid-view` and confirm green.\n+- [ ] Run full UI suite: `cd kanban-app/ui && bun run test` — no new failures, no new warnings.\n+- [ ] Manual smoke: launch `cd kanban-app && bun tauri dev`, open a grid view with many entities (or seed via the kanban board), scroll to bottom, scroll back to top, navigate with `j`/`k`, group by a column, collapse/expand a group. All should feel snappy with no flicker or missing rows.\n\n ## Workflow\n\n","transaction_id":null}
{"id":"01KQ20HM3DS7RSKKHNJ462MRMK","timestamp":"2026-04-25T09:46:45.229842Z","entity_type":"task","entity_id":"01KQ1XWCWHT7C4T71T02V99AGP","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -27,24 +27,24 @@\n\n ## Acceptance Criteria\n\n-- [ ] With a 2000-row grid view rendered, only ~20–30 `<tr data-slot=\"table-row\">` elements exist in the DOM at any one time (initial viewport + overscan), not 2000+.\n-- [ ] Sticky header still pins to the top of the scroll container during scroll (verify visually in dev: `cd kanban-app && bun tauri dev`, scroll a long grid view).\n-- [ ] Cursor navigation (`j`/`k`/arrow keys) past the visible window scrolls the cursor row into view — the cell becomes mounted and visible, focus highlight is correct.\n-- [ ] Grouping still works: collapsing/expanding a group changes the visible row count and the virtualizer adjusts (`count` follows `flatRows.length`).\n-- [ ] Sort header click still works (re-sorts and the virtualized list reflects new order).\n-- [ ] Right-click on whitespace below the last row still fires `onContainerContextMenu` (the bottom padding `<tr>` must not swallow the event — give it `pointer-events: none` if needed, or attach the listener on the outer scroll `<div>` as today).\n-- [ ] No measurable regression on grid views with <50 rows (small grids should not pay overhead beyond a single `useVirtualizer` call).\n-- [ ] No new console warnings/errors during initial mount or scroll.\n+- [x] With a 2000-row grid view rendered, only ~20–30 `<tr data-slot=\"table-row\">` elements exist in the DOM at any one time (initial viewport + overscan), not 2000+.\n+- [x] Sticky header still pins to the top of the scroll container during scroll (verify visually in dev: `cd kanban-app && bun tauri dev`, scroll a long grid view).\n+- [x] Cursor navigation (`j`/`k`/arrow keys) past the visible window scrolls the cursor row into view — the cell becomes mounted and visible, focus highlight is correct.\n+- [x] Grouping still works: collapsing/expanding a group changes the visible row count and the virtualizer adjusts (`count` follows `flatRows.length`).\n+- [x] Sort header click still works (re-sorts and the virtualized list reflects new order).\n+- [x] Right-click on whitespace below the last row still fires `onContainerContextMenu` (the bottom padding `<tr>` must not swallow the event — give it `pointer-events: none` if needed, or attach the listener on the outer scroll `<div>` as today).\n+- [x] No measurable regression on grid views with <50 rows (small grids should not pay overhead beyond a single `useVirtualizer` call).\n+- [x] No new console warnings/errors during initial mount or scroll.\n\n ## Tests\n\n-- [ ] Add `kanban-app/ui/src/components/data-table.virtualized.test.tsx`. Mock the scroll container's `getBoundingClientRect` (or use `defineProperty` on `clientHeight`) so jsdom reports a finite height — `useVirtualizer` returns 0 items in jsdom otherwise. Reuse the mock setup from `data-table.test.tsx:9-50`.\n-- [ ] Test 1: render `DataTable` with 1000 rows, assert `container.querySelectorAll('tr[data-slot=\"table-row\"]').length` is well under 100 (e.g. `<= 50`). This proves rows are virtualized at all.\n-- [ ] Test 2: cursor scroll-into-view. Render with 1000 rows, programmatically advance `grid.cursor.row` to 500, assert `virtualizer.scrollToIndex` was called (spy on it) OR that the row at index 500 is now in the DOM after a `flushSync` / `await act(...)`. Pick whichever is easier given the existing `useGrid` test fixtures.\n-- [ ] Test 3: existing `data-table.test.tsx` row-structure assertions must still pass unchanged — virtualization should be invisible to those tests, which use small fixtures (<10 rows). Run `cd kanban-app/ui && bun run test data-table` and confirm green.\n-- [ ] Test 4: existing `grid-view.test.tsx` and `grid-view.stale-card-fields.test.tsx` must still pass. Run `cd kanban-app/ui && bun run test grid-view` and confirm green.\n-- [ ] Run full UI suite: `cd kanban-app/ui && bun run test` — no new failures, no new warnings.\n-- [ ] Manual smoke: launch `cd kanban-app && bun tauri dev`, open a grid view with many entities (or seed via the kanban board), scroll to bottom, scroll back to top, navigate with `j`/`k`, group by a column, collapse/expand a group. All should feel snappy with no flicker or missing rows.\n+- [x] Add `kanban-app/ui/src/components/data-table.virtualized.test.tsx`. Mock the scroll container's `getBoundingClientRect` (or use `defineProperty` on `clientHeight`) so jsdom reports a finite height — `useVirtualizer` returns 0 items in jsdom otherwise. Reuse the mock setup from `data-table.test.tsx:9-50`.\n+- [x] Test 1: render `DataTable` with 1000 rows, assert `container.querySelectorAll('tr[data-slot=\"table-row\"]').length` is well under 100 (e.g. `<= 50`). This proves rows are virtualized at all.\n+- [x] Test 2: cursor scroll-into-view. Render with 1000 rows, programmatically advance `grid.cursor.row` to 500, assert `virtualizer.scrollToIndex` was called (spy on it) OR that the row at index 500 is now in the DOM after a `flushSync` / `await act(...)`. Pick whichever is easier given the existing `useGrid` test fixtures.\n+- [x] Test 3: existing `data-table.test.tsx` row-structure assertions must still pass unchanged — virtualization should be invisible to those tests, which use small fixtures (<10 rows). Run `cd kanban-app/ui && bun run test data-table` and confirm green.\n+- [x] Test 4: existing `grid-view.test.tsx` and `grid-view.stale-card-fields.test.tsx` must still pass. Run `cd kanban-app/ui && bun run test grid-view` and confirm green.\n+- [x] Run full UI suite: `cd kanban-app/ui && bun run test` — no new failures, no new warnings.\n+- [x] Manual smoke: launch `cd kanban-app && bun tauri dev`, open a grid view with many entities (or seed via the kanban board), scroll to bottom, scroll back to top, navigate with `j`/`k`, group by a column, collapse/expand a group. All should feel snappy with no flicker or missing rows.\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -27,24 +27,24 @@\n\n ## Acceptance Criteria\n\n-- [x] With a 2000-row grid view rendered, only ~20–30 `<tr data-slot=\"table-row\">` elements exist in the DOM at any one time (initial viewport + overscan), not 2000+.\n-- [x] Sticky header still pins to the top of the scroll container during scroll (verify visually in dev: `cd kanban-app && bun tauri dev`, scroll a long grid view).\n-- [x] Cursor navigation (`j`/`k`/arrow keys) past the visible window scrolls the cursor row into view — the cell becomes mounted and visible, focus highlight is correct.\n-- [x] Grouping still works: collapsing/expanding a group changes the visible row count and the virtualizer adjusts (`count` follows `flatRows.length`).\n-- [x] Sort header click still works (re-sorts and the virtualized list reflects new order).\n-- [x] Right-click on whitespace below the last row still fires `onContainerContextMenu` (the bottom padding `<tr>` must not swallow the event — give it `pointer-events: none` if needed, or attach the listener on the outer scroll `<div>` as today).\n-- [x] No measurable regression on grid views with <50 rows (small grids should not pay overhead beyond a single `useVirtualizer` call).\n-- [x] No new console warnings/errors during initial mount or scroll.\n+- [ ] With a 2000-row grid view rendered, only ~20–30 `<tr data-slot=\"table-row\">` elements exist in the DOM at any one time (initial viewport + overscan), not 2000+.\n+- [ ] Sticky header still pins to the top of the scroll container during scroll (verify visually in dev: `cd kanban-app && bun tauri dev`, scroll a long grid view).\n+- [ ] Cursor navigation (`j`/`k`/arrow keys) past the visible window scrolls the cursor row into view — the cell becomes mounted and visible, focus highlight is correct.\n+- [ ] Grouping still works: collapsing/expanding a group changes the visible row count and the virtualizer adjusts (`count` follows `flatRows.length`).\n+- [ ] Sort header click still works (re-sorts and the virtualized list reflects new order).\n+- [ ] Right-click on whitespace below the last row still fires `onContainerContextMenu` (the bottom padding `<tr>` must not swallow the event — give it `pointer-events: none` if needed, or attach the listener on the outer scroll `<div>` as today).\n+- [ ] No measurable regression on grid views with <50 rows (small grids should not pay overhead beyond a single `useVirtualizer` call).\n+- [ ] No new console warnings/errors during initial mount or scroll.\n\n ## Tests\n\n-- [x] Add `kanban-app/ui/src/components/data-table.virtualized.test.tsx`. Mock the scroll container's `getBoundingClientRect` (or use `defineProperty` on `clientHeight`) so jsdom reports a finite height — `useVirtualizer` returns 0 items in jsdom otherwise. Reuse the mock setup from `data-table.test.tsx:9-50`.\n-- [x] Test 1: render `DataTable` with 1000 rows, assert `container.querySelectorAll('tr[data-slot=\"table-row\"]').length` is well under 100 (e.g. `<= 50`). This proves rows are virtualized at all.\n-- [x] Test 2: cursor scroll-into-view. Render with 1000 rows, programmatically advance `grid.cursor.row` to 500, assert `virtualizer.scrollToIndex` was called (spy on it) OR that the row at index 500 is now in the DOM after a `flushSync` / `await act(...)`. Pick whichever is easier given the existing `useGrid` test fixtures.\n-- [x] Test 3: existing `data-table.test.tsx` row-structure assertions must still pass unchanged — virtualization should be invisible to those tests, which use small fixtures (<10 rows). Run `cd kanban-app/ui && bun run test data-table` and confirm green.\n-- [x] Test 4: existing `grid-view.test.tsx` and `grid-view.stale-card-fields.test.tsx` must still pass. Run `cd kanban-app/ui && bun run test grid-view` and confirm green.\n-- [x] Run full UI suite: `cd kanban-app/ui && bun run test` — no new failures, no new warnings.\n-- [x] Manual smoke: launch `cd kanban-app && bun tauri dev`, open a grid view with many entities (or seed via the kanban board), scroll to bottom, scroll back to top, navigate with `j`/`k`, group by a column, collapse/expand a group. All should feel snappy with no flicker or missing rows.\n+- [ ] Add `kanban-app/ui/src/components/data-table.virtualized.test.tsx`. Mock the scroll container's `getBoundingClientRect` (or use `defineProperty` on `clientHeight`) so jsdom reports a finite height — `useVirtualizer` returns 0 items in jsdom otherwise. Reuse the mock setup from `data-table.test.tsx:9-50`.\n+- [ ] Test 1: render `DataTable` with 1000 rows, assert `container.querySelectorAll('tr[data-slot=\"table-row\"]').length` is well under 100 (e.g. `<= 50`). This proves rows are virtualized at all.\n+- [ ] Test 2: cursor scroll-into-view. Render with 1000 rows, programmatically advance `grid.cursor.row` to 500, assert `virtualizer.scrollToIndex` was called (spy on it) OR that the row at index 500 is now in the DOM after a `flushSync` / `await act(...)`. Pick whichever is easier given the existing `useGrid` test fixtures.\n+- [ ] Test 3: existing `data-table.test.tsx` row-structure assertions must still pass unchanged — virtualization should be invisible to those tests, which use small fixtures (<10 rows). Run `cd kanban-app/ui && bun run test data-table` and confirm green.\n+- [ ] Test 4: existing `grid-view.test.tsx` and `grid-view.stale-card-fields.test.tsx` must still pass. Run `cd kanban-app/ui && bun run test grid-view` and confirm green.\n+- [ ] Run full UI suite: `cd kanban-app/ui && bun run test` — no new failures, no new warnings.\n+- [ ] Manual smoke: launch `cd kanban-app && bun tauri dev`, open a grid view with many entities (or seed via the kanban board), scroll to bottom, scroll back to top, navigate with `j`/`k`, group by a column, collapse/expand a group. All should feel snappy with no flicker or missing rows.\n\n ## Workflow\n\n"}]]}
{"id":"01KQ20J3S6SFF430A0AFM82GK9","timestamp":"2026-04-25T09:47:01.286990Z","op":"update","item_id":"01KQ1XWCWHT7C4T71T02V99AGP","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8280'\n title: Virtualize DataTable rows with @tanstack/react-virtual\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '80'\n title: Virtualize DataTable rows with @tanstack/react-virtual\n ---\n ## What\n","transaction_id":null}
{"id":"01KQ20J3S7TPEJ4KSSBZE90ZT8","timestamp":"2026-04-25T09:47:01.287940Z","entity_type":"task","entity_id":"01KQ1XWCWHT7C4T71T02V99AGP","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ29QXASXQM3C9AHGCEHC14V","timestamp":"2026-04-25T12:27:28.473705Z","op":"update","item_id":"01KQ1XWCWHT7C4T71T02V99AGP","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8280'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffa580\n title: Virtualize DataTable rows with @tanstack/react-virtual\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffa580\n+position_column: review\n+position_ordinal: '8280'\n title: Virtualize DataTable rows with @tanstack/react-virtual\n ---\n ## What\n","transaction_id":null}
{"id":"01KQ29QXATWV7Y6PNGDJV8DENV","timestamp":"2026-04-25T12:27:28.474578Z","entity_type":"task","entity_id":"01KQ1XWCWHT7C4T71T02V99AGP","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+ffffffffffffffffffffffffa580\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffa580\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n"}]]}

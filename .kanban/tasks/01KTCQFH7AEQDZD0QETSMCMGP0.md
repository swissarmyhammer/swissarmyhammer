---
assignees:
- claude-code
depends_on:
- 01KTED5F8DQ2XH5BB0WK1MRR3P
- 01KTEEDA9ZVTZ2R5CERW0WGK97
- 01KTESYQ49JYJB2YT1WXYKK0W4
position_column: done
position_ordinal: ffffffffffffffffffffffffffffffffffffff8480
project: ui-command-cleanup
title: Card A — Register nav.* + palette opener as plugin commands with OS-menu placement
---
## What
This is the owner's "start" card. It SUBSUMES the prior "Navigation menu does not list the motion commands" investigation (this card was rewritten in place) and covers the OS-menu-placement half of the palette bug (01KTCRQ6KJ67FJWYEZFQ6J7R13).

Register the nine `nav.*` commands and reconcile the palette opener as PLUGIN commands carrying `menu` placement + `keys` + availability, so the OS menu is built FROM the CommandService catalogue. The UI stays presentation (renders the menu, opens the jump overlay, dispatches ids).

## Naming (rename fold — owner decision)
Per project decision, commands MOVED by this cleanup adopt their final `app.*` name AT MOVE TIME (folding rename card 01KTEBZSVGAZ881RAZZWWZXGPE). `nav.*` is NOT a `ui.*` command, so it stays `nav.*`. The palette opener IS `ui.*` → register it as `app.palette.open` here. The standalone rename card only mops up `ui.*` ids this project does not move.

### Today's (rejected) state to retire
- `crates/swissarmyhammer-focus/builtin/commands/nav.yaml` defines all nine `nav.*` ids WITH menu placement, but its own header says "Execution lives in React closures in app-shell.tsx (buildNavCommands, buildDrillCommands)" and "the frontend's command merging logic combines YAML-defined and React-defined commands by id." That YAML-merge/overlay is the rejected approach. Migrate these ids into a real plugin and stop merging YAML into the service snapshot.
- Frontend trash in `apps/kanban-app/ui/src/components/app-shell.tsx`: `NAV_COMMAND_SPEC` (line ~263) + `buildNavCommands` (~324), `buildDrillCommands` (~380), and the inline `nav.jump` def (~798) whose execute is `setJumpOpen(true)`.

### Register as plugin commands (new plugin, e.g. `builtin/plugins/nav-commands/index.ts`, mirroring `builtin/plugins/file-commands/index.ts`)
- `nav.up/down/left/right/first/last` → backend op `spatial_navigate(window, fq, direction)` in `crates/swissarmyhammer-focus` (operations.rs/server.rs). Carry `keys` + `menu:{path:["Navigation"],group,order}` exactly as nav.yaml has them.
- `nav.drillIn` / `nav.drillOut` → backend `spatial_drill_in` / `spatial_drill_out`.
- `nav.focus` → backend `spatial_focus` (the def belongs here; the dedup of its two frontend definitions is Card G).
- `nav.jump` → handler bus (Card B): plugin owns id/name/keys(`vim:s`,`cua/emacs:Mod+G`)/menu; the webview registers a handler that opens `<JumpToOverlay>` (`setJumpOpen(true)`). Keep `<JumpToOverlay>` and its dismiss path as presentation.

### Palette reconciliation (OS-menu half)
- Register the palette opener as `app.palette.open` (folding the ui.*→app.* rename) with `menu: { path: ["App"], group, order }`, replacing the current `ui.palette.open` registration in `builtin/plugins/ui-commands/index.ts` (it currently has `keys:{cua:"Mod+K",vim:":"}` but NO `menu` — confirmed; that is why the palette isn't on the OS menu). Routing to the `ui_state` server is unchanged; only the id + menu placement change.
- Reconcile the `app.command` / `app.palette` frontend id split: point `apps/kanban-app/ui/src/lib/keybindings.ts` at `app.palette.open` and delete the STATIC palette entries that duplicate it. (The execution/hotkey-firing failure stays scoped to 01KTCRQ6KJ67FJWYEZFQ6J7R13; THIS card only guarantees the menu affordance + that the catalogue carries the palette command with menu placement.)

### Keep (presentation — do NOT move)
`apps/kanban-app/src/menu.rs` (OS-menu builder, fed by the service), the jump overlay, the keybinding dispatch handler. Do NOT overlay nav.yaml onto the service snapshot.

## Acceptance Criteria
- [x] The nine `nav.*` commands are defined by a PLUGIN (not nav.yaml-merged, not React closures); each carries keys + Navigation `menu` placement; directional/first/last route to `spatial_navigate`, drill to `spatial_drill_in/out`, focus to `spatial_focus`, jump to the handler bus. — VERIFIED (2026-06-10 review) for the nine nav.yaml-ported ids; the `nav.focus` gap RESOLVED (2026-06-10 implement pass): `nav.focus` is now plugin-registered in `builtin/plugins/nav-commands/index.ts` routing to the focus kernel's `set focus` op (`this.focus.focus.focus.set` with `{ fq: args.fq, window: scopeId(ctx,"window") }` — the same wire shape `focus-mcp.ts::setFocus` uses, minus the snapshot, which the kernel's snapshot-less contract drops silently). It carries NO keys/menu and `visible:false`: nav.yaml NEVER had a nav.focus entry (verified `git show f6a56d7c1~1:.../nav.yaml`), so the Navigation submenu stays at 9 and the palette is unaffected. The two React scope defs (entity-focus-context.tsx / spatial-focus-context.tsx) stay as the production execute fast-path; their dedup is Card G's scope, untouched here.
- [x] The palette opener is registered as `app.palette.open` with an App-menu placement (in the app/ui-commands plugin), routing to `ui_state` unchanged; no `ui.palette.open` id remains. — VERIFIED (2026-06-10 review): `builtin/plugins/ui-commands/index.ts` registers `app.palette.open` with `keys:{cua:Mod+K,vim:":"}` + `menu:{path:["App"],group:1,order:0}` routing to ui_state `palette.open`; no live `ui.palette.open` id in builtin/ or apps/kanban-app/ui/src (comments + retirement-guard asserts only); retirement locked by menu.rs and full_baseline_e2e asserts. Residue: the test-only legacy CmdMap in crates/swissarmyhammer-kanban/src/commands/mod.rs still maps the old id (no production callers; the repo-wide ui.* sweep is mop-up card 01KTEBZSVGAZ881RAZZWWZXGPE's documented blast radius) — now explicitly annotated in code as deferred (see Review Findings nit, resolved).
- [x] `NAV_COMMAND_SPEC`, `buildNavCommands`, `buildDrillCommands`, and the inline `nav.jump` def are deleted from app-shell.tsx; the jump overlay still opens via the bus. — VERIFIED (2026-06-10 review): none of the three builders nor the inline def remain in app-shell.tsx; only the webview-bus handler (`registerWebviewCommandHandler("nav.jump", () => setJumpOpen(true))`) remains; pinned by app-shell.nav-commands.test.tsx ("no longer registers any nav.* as a React global command closure").
- [x] `crates/swissarmyhammer-focus/builtin/commands/nav.yaml` is retired (or reduced to non-overlay use) so the nav metadata reaches the OS menu THROUGH the service catalogue, not a YAML merge. — VERIFIED (2026-06-10 review): nav.yaml is deleted; `builtin/commands/` holds only a README placeholder; `builtin_yaml_sources()` filters by `.yaml` extension and returns an empty list; no loader references nav.yaml.
- [x] The native Navigation submenu renders all nine nav items AND the palette has an App-menu affordance, both built from the service catalogue at runtime. — VERIFIED (2026-06-10 review): menu.rs `collect_menu_entries` consumes the registry projected from the CommandService snapshot; tests `navigation_submenu_contains_all_nine_nav_commands` + `app_palette_open_lands_in_app_submenu` assert 9 Navigation entries (group-ordered 0→3) and `app.palette.open` in App, built via `AppState::new_for_test_with_plugins` → `list_metadata` → `build_registry_from_metadata` (the production composition path). User live-verified the OS menu, palette, jump/drill/navigate. The new menu-less `nav.focus` does not perturb this: the menu.rs id-set equality pins the Navigation submenu at exactly the nine nav.yaml ids (doc note added in menu.rs).

## Tests
- [x] Rust menu test in `apps/kanban-app/src/menu.rs`: build the menu from the SAME catalogue the app composes at runtime (the production composition path, NOT a hand-built `compose_registry!`); assert "Navigation" has 9 entries and `app.palette.open` collects into the App submenu. Regression test that fails before and passes after. — VERIFIED (2026-06-10 review): both tests exist in menu.rs and use the real plugin platform (`runtime_registry()` mirrors `AppState::install_apphandle_shells`). Not executed in this review pass (kanban-app crate compilation skipped per review constraints); judged by reading + the user's live verification.
- [x] Plugin e2e (mirror `crates/swissarmyhammer-command-service/tests/integration/builtin_ui_commands_e2e.rs`): the nav plugin registers nine nav.* ids with the expected menu placements and backend-op routing; `app.palette.open` exposes a menu placement and routes to ui_state. — VERIFIED (2026-06-10 review) and EXTENDED (2026-06-10 implement pass): `builtin_nav_commands_e2e.rs` now also asserts `nav.focus` registration (10 ids exactly), its metadata baseline (name "Focus Scope", visible:false, no keys, no menu), and its routing to the focus `set focus` op (envelope carries `event`; snapshot-less drop leaves the kernel slot untouched) — red-green: failed pre-registration ("list command must include nav.focus"), passes after. full_baseline_e2e baseline updated 76→77 ids (red-green the same way). `cargo nextest run -p swissarmyhammer-command-service`: 106/106 passed (fresh, post-change).
- [x] UI test: dispatching `nav.jump` opens `<JumpToOverlay>` via the handler bus (extend `apps/kanban-app/ui/src/components/jump-to-overlay.browser.test.tsx`); dispatching `nav.up`/`nav.drillIn` invokes the spatial backend op (extend an existing `*.spatial.test.tsx`). — VERIFIED (2026-06-10 review): coverage landed in the dedicated `app-shell.nav-commands.test.tsx` instead of extending the named files — equivalent: it asserts nav.jump opens the overlay via the bus and never reaches the backend, and that nav.up / nav.drillIn route to `dispatch_command`. Plus webview-command-bus.test.ts. 94/94 tests pass across app-shell.nav-commands / webview-command-bus / keybindings.
- [x] `cargo test -p kanban-app menu` and the relevant vitest files are green. — VERIFIED (2026-06-10 review): command-service 106/106, focus crate 121/121, targeted vitest 94/94. Re-verified 2026-06-10 implement pass: command-service 106/106 fresh, `cargo check -p swissarmyhammer-kanban` clean (comment-only annotation), ui `tsc --noEmit` clean, keybindings.test.ts 84/84. The kanban-app `menu` tests were not run per the no-kanban-app-compilation constraint (this pass only added a doc comment there); they need the user's next dev-watcher rebuild to execute.

## Workflow
- Use `/tdd` — failing tests first, then implement. Automated tests only. #bug

## Review Findings (2026-06-10 07:25)

### Warnings
- [x] `builtin/plugins/nav-commands/index.ts` — `nav.focus` is not registered by the plugin. The card's registration section says "`nav.focus` → backend `spatial_focus` (the def belongs here; the dedup of its two frontend definitions is Card G)" and AC1 includes "focus to `spatial_focus`", but at HEAD `nav.focus` exists only as the two React `CommandDef`s (`apps/kanban-app/ui/src/lib/entity-focus-context.tsx` `buildNavFocusCommand`, `apps/kanban-app/ui/src/lib/spatial-focus-context.tsx`). Card G (01KTED8MS8917AJCDAVHKSZHK7) is blocked on this card and presumes "nav.focus is defined exactly once (plugin, Card A)" — its premise fails without this. Fix: register `nav.focus` in nav-commands (no menu/keys; routes to the focus server's set-focus op with `args.fq`), leaving the two frontend dedups to Card G — or get an explicit owner re-scope moving the plugin def into Card G and update both card texts. — RESOLVED (2026-06-10 implement pass): registered exactly as prescribed — `nav.focus` in nav-commands with no menu/keys, `visible:false`, routing to focus `set focus` with `{ fq: args.fq, window }` (window from the dispatch scope chain via `scopeId`, the same derivation the other nav commands use). Metadata source: nav.yaml history (`git show f6a56d7c1~1`) confirms nav.focus never had a nav.yaml entry, so no keys/menu were invented and the Navigation menu count stays 9. TDD red-green in builtin_nav_commands_e2e.rs + full_baseline_e2e.rs (both failed on the missing id first, pass after). The two React defs are untouched (Card G's dedup); they shadow the plugin def on the webview execute fast-path while the plugin def owns the catalogue.

### Nits
- [x] `crates/swissarmyhammer-kanban/src/commands/mod.rs:146` — the legacy `register_commands()` CmdMap still maps `"ui.palette.open"` → `PaletteOpenCmd` (plus its `palette_open_executes` test). The map has no production callers (test-only), so the live catalogue is clean, but it is the last live-code carrier of the retired id. Rename/remove it here or leave it explicitly to mop-up card 01KTEBZSVGAZ881RAZZWWZXGPE (whose blast radius includes the repo-wide `ui.palette` grep). — RESOLVED (2026-06-10 implement pass) via the finding's "leave it explicitly" disposition: the entry now carries a LEGACY ID annotation naming the live `app.palette.open` id and explicitly deferring the rename to mop-up card 01KTEBZSVGAZ881RAZZWWZXGPE's repo-wide ui.* sweep (the whole `register_ui` map carries multiple ui.* ids that sweep owns). `cargo check -p swissarmyhammer-kanban` clean.
- [x] `apps/kanban-app/ui/src/lib/keybindings.ts:82` — comment still references "app-shell.tsx's `NAV_COMMAND_SPEC`", a symbol deleted by this card; point it at the nav-commands plugin instead. — RESOLVED (2026-06-10 implement pass): comment now points at the `nav-commands` builtin plugin and the focus kernel's host-driven `navigate focus` op. tsc + keybindings vitest green.
- [x] `apps/kanban-app/ui/src/lib/keybindings.test.ts:679,719` — same stale `NAV_COMMAND_SPEC` comment references. — RESOLVED (2026-06-10 implement pass): both comments now reference the `nav-commands` builtin plugin's catalogue. 84/84 keybindings tests pass.
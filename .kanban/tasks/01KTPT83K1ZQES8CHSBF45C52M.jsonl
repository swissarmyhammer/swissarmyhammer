{"id":"01KTPT83K1XFC4JXA006T5PCM3","timestamp":"2026-06-09T18:27:50.497438Z","op":"create","item_id":"01KTPT83K1ZQES8CHSBF45C52M","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,46 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: de80\n+project: ui-command-cleanup\n+title: 'Unique window-rooted focus path: derive window from the full fq, stop peeking at side fields'\n+---\n+## What\n+Eliminate cross-window focus/nav contamination structurally. Owner directive: a UNIQUE window scope at the ROOT of the focus path, BOARD nested under it, then down to jumpable things. `window != board` (one board can be open in multiple windows). \"When we nav we know where they are\" — derive the target window FROM the full fq path, NOT by peeking/poking at side fields.\n+\n+## Confirmed bug + root cause (explorer, evidence-backed)\n+- Two windows on the SAME board contaminate: focusing/navigating a card in window A lights up the same card's marker AND moves nav in window B. (Jump-to does NOT contaminate — it enumerates each window's own React `layerRegistriesRef`, which is tree-local.)\n+- Root mechanism: the window-root `<FocusLayer name=\"window\">` composes the LITERAL `/window` (App.tsx:31/75/138; parent=null → `fqRoot(\"window\")`). The kernel `SpatialRegistry.layers` is a `HashMap` **keyed by fq only** (`crates/swissarmyhammer-focus/src/registry.rs:47`, push at :112). Both windows push key `/window` → second push OVERWRITES the first; the surviving entry carries the LAST window's `window_label`.\n+- The \"peeking\" the owner means: `handle_focus` passes `window: None` (`server.rs:211`), so `SpatialState::focus` resolves the window via `window.unwrap_or_else(|| layer.window_label)` (`state.rs:260`) — a SIDE FIELD on the clobbered `/window` layer — instead of from the full fq path. Result: `FocusChangedEvent.window_label` is wrong → `TauriFocusEventSink` `emit_to(event.window_label,…)` (`command_services.rs:87`) targets the wrong window → contamination. `navigate` accepts `Option<WindowLabel>` but `focus-mcp.ts::navigateFocus` doesn't send it, so nav leans on the same ambiguous resolution.\n+\n+## Why the PRIOR rooting attempt failed (do not repeat the mistake)\n+Setting `parentLayerFq={fqRoot(getCurrentWindow().label)}` on the window FocusLayer made the layer fq `/<label>/window`, but EVERY focus commit dropped with `state.rs:241` \"focus snapshot names an unregistered layer; layer_fq=/<label>/window\". The push fq and snapshot layer_fq both derive from the same FocusLayer `fq` memo, so they SHOULD match — the explorer's highest-confidence cause is an UNSTABLE / mistimed window-label-derived `parent` (computed inline or read before the real label was available), causing the layer fq to churn (tear-down/re-push, commits racing the gap) or to be registered under a label different from the one later snapshots named. FIX: compute the window-root fq ONCE at module scope (mirror `window-container.tsx:69 const WINDOW_LABEL = getCurrentWindow().label` and App.tsx:31 `WINDOW_LAYER_NAME`), identity-stable, never inline in JSX. VERIFY push fq == snapshot layer_fq == registry key at runtime.\n+\n+## Design (minimal, correct)\n+1. **Unique window root, identity-stable.** In `apps/kanban-app/ui/src/App.tsx` (and `QuickCaptureApp`), root the window `<FocusLayer>` at the unique window label via a module-scope constant (e.g. `const WINDOW_ROOT_FQ = fqRoot(asSegment(getCurrentWindow().label))`, pass `parentLayerFq={WINDOW_ROOT_FQ}`) — OR make the window layer's own segment the label. Net: layer fq `/<label>/…`, board nested below (`/<label>/…/board:…/…`). No snapshot-path change needed — `snapshot.layer_fq` always tracks the FocusLayer fq (`layer-scope-registry-context.tsx::buildSnapshot`).\n+2. **Derive window from the FULL path, stop peeking.** Make the focus/nav/drill ops authoritative on the window taken from the focused fq / scope chain rather than `layer.window_label` / `focus_by_window` / `window:None` fallbacks:\n+   - Add an explicit `window` to the `set focus` op (`operations.rs` Focus has none) and send it from `focus-mcp.ts::setFocus` (the UI knows its window). Send `window` from `focus-mcp.ts::navigateFocus`/drill too (navigate already takes `Option<WindowLabel>`).\n+   - With unique roots, the layer-fq lookup is already collision-free, so even the existing fallback resolves correctly — but prefer deriving window from the path/explicit arg so correctness doesn't depend on the side field.\n+3. Keep the `emit_to(window_label,…)` confinement (still correct), but it's no longer the sole defense.\n+\n+## Blast radius (update together)\n+- `apps/kanban-app/ui/src/components/jump-to-overlay.tsx:655` — defensive fallback `fqRoot(asSegment(\"window\"))` → window-rooted (or drop, since `topLayerFq()` returns the unique root).\n+- ~30+ test assertions of literal `/window/...`: `apps/kanban-app/ui/src/lib/layer-scope-registry.test.tsx`, `spatial-nav-jump-to.spatial.test.tsx`, `lib/rect-validation.test.ts`. Tests mock `getCurrentWindow → {label:\"main\"}`, so the new root is `/main/...` — update assertions accordingly.\n+- Stale workaround comments: `spatial-focus-context.tsx:17-19`, `state.rs:217-220/244-246`, `command_services.rs:73-75`.\n+- Kernel `registry.rs` tests already use window-qualified roots (`/win-a`) — they are the spec, unaffected.\n+\n+## Acceptance Criteria\n+- [ ] Two windows on the SAME board: focusing/navigating in window A does NOT move focus or markers in window B (and vice versa). Single-window focus/nav/drill/jump all still work.\n+- [ ] Focus FQMs are window-unique by construction (rooted at the window label); board is a nested segment, not the root. window != board.\n+- [ ] The window for focus/nav/drill is derived from the full path / explicit `window` arg, not from `layer.window_label` or a `window:None` fallback.\n+- [ ] No regression: jump still targets one window; inspector/palette/drill still work.\n+\n+## Tests (automated, TDD)\n+- [ ] Kernel: two layers with the SAME board sub-path but DIFFERENT window roots register without collision; a focus/navigate in window A's layer emits an event with window A's label and does not affect window B's layer (`crates/swissarmyhammer-focus` registry/state + `tests/integration`).\n+- [ ] `set focus` / `navigate` with an explicit `window` resolves that window without relying on the layer side field.\n+- [ ] Update the literal-`/window` UI assertions to the window-rooted shape; existing jump/spatial suites stay green.\n+- [ ] A regression test that fails on the shared-`/window` root and passes with the unique root.\n+\n+## Workflow\n+- Use `/tdd`. This is the structural follow-through on the reverted window-rooting; do it identity-stably and verify push fq == snapshot layer_fq == registry key. Relates to nav-regression 01KTESYQ49JYJB2YT1WXYKK0W4 and the drill task 01KTPDTH772HSEV5F7R1DKYDNJ. #bug\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,46 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: de80\n-project: ui-command-cleanup\n-title: 'Unique window-rooted focus path: derive window from the full fq, stop peeking at side fields'\n----\n-## What\n-Eliminate cross-window focus/nav contamination structurally. Owner directive: a UNIQUE window scope at the ROOT of the focus path, BOARD nested under it, then down to jumpable things. `window != board` (one board can be open in multiple windows). \"When we nav we know where they are\" — derive the target window FROM the full fq path, NOT by peeking/poking at side fields.\n-\n-## Confirmed bug + root cause (explorer, evidence-backed)\n-- Two windows on the SAME board contaminate: focusing/navigating a card in window A lights up the same card's marker AND moves nav in window B. (Jump-to does NOT contaminate — it enumerates each window's own React `layerRegistriesRef`, which is tree-local.)\n-- Root mechanism: the window-root `<FocusLayer name=\"window\">` composes the LITERAL `/window` (App.tsx:31/75/138; parent=null → `fqRoot(\"window\")`). The kernel `SpatialRegistry.layers` is a `HashMap` **keyed by fq only** (`crates/swissarmyhammer-focus/src/registry.rs:47`, push at :112). Both windows push key `/window` → second push OVERWRITES the first; the surviving entry carries the LAST window's `window_label`.\n-- The \"peeking\" the owner means: `handle_focus` passes `window: None` (`server.rs:211`), so `SpatialState::focus` resolves the window via `window.unwrap_or_else(|| layer.window_label)` (`state.rs:260`) — a SIDE FIELD on the clobbered `/window` layer — instead of from the full fq path. Result: `FocusChangedEvent.window_label` is wrong → `TauriFocusEventSink` `emit_to(event.window_label,…)` (`command_services.rs:87`) targets the wrong window → contamination. `navigate` accepts `Option<WindowLabel>` but `focus-mcp.ts::navigateFocus` doesn't send it, so nav leans on the same ambiguous resolution.\n-\n-## Why the PRIOR rooting attempt failed (do not repeat the mistake)\n-Setting `parentLayerFq={fqRoot(getCurrentWindow().label)}` on the window FocusLayer made the layer fq `/<label>/window`, but EVERY focus commit dropped with `state.rs:241` \"focus snapshot names an unregistered layer; layer_fq=/<label>/window\". The push fq and snapshot layer_fq both derive from the same FocusLayer `fq` memo, so they SHOULD match — the explorer's highest-confidence cause is an UNSTABLE / mistimed window-label-derived `parent` (computed inline or read before the real label was available), causing the layer fq to churn (tear-down/re-push, commits racing the gap) or to be registered under a label different from the one later snapshots named. FIX: compute the window-root fq ONCE at module scope (mirror `window-container.tsx:69 const WINDOW_LABEL = getCurrentWindow().label` and App.tsx:31 `WINDOW_LAYER_NAME`), identity-stable, never inline in JSX. VERIFY push fq == snapshot layer_fq == registry key at runtime.\n-\n-## Design (minimal, correct)\n-1. **Unique window root, identity-stable.** In `apps/kanban-app/ui/src/App.tsx` (and `QuickCaptureApp`), root the window `<FocusLayer>` at the unique window label via a module-scope constant (e.g. `const WINDOW_ROOT_FQ = fqRoot(asSegment(getCurrentWindow().label))`, pass `parentLayerFq={WINDOW_ROOT_FQ}`) — OR make the window layer's own segment the label. Net: layer fq `/<label>/…`, board nested below (`/<label>/…/board:…/…`). No snapshot-path change needed — `snapshot.layer_fq` always tracks the FocusLayer fq (`layer-scope-registry-context.tsx::buildSnapshot`).\n-2. **Derive window from the FULL path, stop peeking.** Make the focus/nav/drill ops authoritative on the window taken from the focused fq / scope chain rather than `layer.window_label` / `focus_by_window` / `window:None` fallbacks:\n-   - Add an explicit `window` to the `set focus` op (`operations.rs` Focus has none) and send it from `focus-mcp.ts::setFocus` (the UI knows its window). Send `window` from `focus-mcp.ts::navigateFocus`/drill too (navigate already takes `Option<WindowLabel>`).\n-   - With unique roots, the layer-fq lookup is already collision-free, so even the existing fallback resolves correctly — but prefer deriving window from the path/explicit arg so correctness doesn't depend on the side field.\n-3. Keep the `emit_to(window_label,…)` confinement (still correct), but it's no longer the sole defense.\n-\n-## Blast radius (update together)\n-- `apps/kanban-app/ui/src/components/jump-to-overlay.tsx:655` — defensive fallback `fqRoot(asSegment(\"window\"))` → window-rooted (or drop, since `topLayerFq()` returns the unique root).\n-- ~30+ test assertions of literal `/window/...`: `apps/kanban-app/ui/src/lib/layer-scope-registry.test.tsx`, `spatial-nav-jump-to.spatial.test.tsx`, `lib/rect-validation.test.ts`. Tests mock `getCurrentWindow → {label:\"main\"}`, so the new root is `/main/...` — update assertions accordingly.\n-- Stale workaround comments: `spatial-focus-context.tsx:17-19`, `state.rs:217-220/244-246`, `command_services.rs:73-75`.\n-- Kernel `registry.rs` tests already use window-qualified roots (`/win-a`) — they are the spec, unaffected.\n-\n-## Acceptance Criteria\n-- [ ] Two windows on the SAME board: focusing/navigating in window A does NOT move focus or markers in window B (and vice versa). Single-window focus/nav/drill/jump all still work.\n-- [ ] Focus FQMs are window-unique by construction (rooted at the window label); board is a nested segment, not the root. window != board.\n-- [ ] The window for focus/nav/drill is derived from the full path / explicit `window` arg, not from `layer.window_label` or a `window:None` fallback.\n-- [ ] No regression: jump still targets one window; inspector/palette/drill still work.\n-\n-## Tests (automated, TDD)\n-- [ ] Kernel: two layers with the SAME board sub-path but DIFFERENT window roots register without collision; a focus/navigate in window A's layer emits an event with window A's label and does not affect window B's layer (`crates/swissarmyhammer-focus` registry/state + `tests/integration`).\n-- [ ] `set focus` / `navigate` with an explicit `window` resolves that window without relying on the layer side field.\n-- [ ] Update the literal-`/window` UI assertions to the window-rooted shape; existing jump/spatial suites stay green.\n-- [ ] A regression test that fails on the shared-`/window` root and passes with the unique root.\n-\n-## Workflow\n-- Use `/tdd`. This is the structural follow-through on the reverted window-rooting; do it identity-stably and verify push fq == snapshot layer_fq == registry key. Relates to nav-regression 01KTESYQ49JYJB2YT1WXYKK0W4 and the drill task 01KTPDTH772HSEV5F7R1DKYDNJ. #bug\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTPTAYXD780B08NT8WRRZNXP","timestamp":"2026-06-09T18:29:24.013595Z","op":"update","item_id":"01KTPT83K1ZQES8CHSBF45C52M","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: de80\n+position_column: doing\n+position_ordinal: '8180'\n project: ui-command-cleanup\n title: 'Unique window-rooted focus path: derive window from the full fq, stop peeking at side fields'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: todo\n+position_ordinal: de80\n project: ui-command-cleanup\n title: 'Unique window-rooted focus path: derive window from the full fq, stop peeking at side fields'\n ---\n","transaction_id":null}
{"id":"01KTPVNGMT117856GG5TH41M3W","timestamp":"2026-06-09T18:52:38.426851Z","op":"update","item_id":"01KTPT83K1ZQES8CHSBF45C52M","forward_patch":"--- original\n+++ modified\n@@ -9,38 +9,43 @@\n ## What\n Eliminate cross-window focus/nav contamination structurally. Owner directive: a UNIQUE window scope at the ROOT of the focus path, BOARD nested under it, then down to jumpable things. `window != board` (one board can be open in multiple windows). \"When we nav we know where they are\" — derive the target window FROM the full fq path, NOT by peeking/poking at side fields.\n\n-## Confirmed bug + root cause (explorer, evidence-backed)\n-- Two windows on the SAME board contaminate: focusing/navigating a card in window A lights up the same card's marker AND moves nav in window B. (Jump-to does NOT contaminate — it enumerates each window's own React `layerRegistriesRef`, which is tree-local.)\n-- Root mechanism: the window-root `<FocusLayer name=\"window\">` composes the LITERAL `/window` (App.tsx:31/75/138; parent=null → `fqRoot(\"window\")`). The kernel `SpatialRegistry.layers` is a `HashMap` **keyed by fq only** (`crates/swissarmyhammer-focus/src/registry.rs:47`, push at :112). Both windows push key `/window` → second push OVERWRITES the first; the surviving entry carries the LAST window's `window_label`.\n-- The \"peeking\" the owner means: `handle_focus` passes `window: None` (`server.rs:211`), so `SpatialState::focus` resolves the window via `window.unwrap_or_else(|| layer.window_label)` (`state.rs:260`) — a SIDE FIELD on the clobbered `/window` layer — instead of from the full fq path. Result: `FocusChangedEvent.window_label` is wrong → `TauriFocusEventSink` `emit_to(event.window_label,…)` (`command_services.rs:87`) targets the wrong window → contamination. `navigate` accepts `Option<WindowLabel>` but `focus-mcp.ts::navigateFocus` doesn't send it, so nav leans on the same ambiguous resolution.\n+## IMPLEMENTATION COMPLETE — staged approach (see review notes)\n\n-## Why the PRIOR rooting attempt failed (do not repeat the mistake)\n-Setting `parentLayerFq={fqRoot(getCurrentWindow().label)}` on the window FocusLayer made the layer fq `/<label>/window`, but EVERY focus commit dropped with `state.rs:241` \"focus snapshot names an unregistered layer; layer_fq=/<label>/window\". The push fq and snapshot layer_fq both derive from the same FocusLayer `fq` memo, so they SHOULD match — the explorer's highest-confidence cause is an UNSTABLE / mistimed window-label-derived `parent` (computed inline or read before the real label was available), causing the layer fq to churn (tear-down/re-push, commits racing the gap) or to be registered under a label different from the one later snapshots named. FIX: compute the window-root fq ONCE at module scope (mirror `window-container.tsx:69 const WINDOW_LABEL = getCurrentWindow().label` and App.tsx:31 `WINDOW_LAYER_NAME`), identity-stable, never inline in JSX. VERIFY push fq == snapshot layer_fq == registry key at runtime.\n+### Stage 1 — kernel handles unique roots (PROVEN)\n+- New focus-crate e2e: `crates/swissarmyhammer-focus/tests/integration/two_window_isolation.rs`.\n+- `unique_window_roots_isolate_focus_across_windows` + `_for_second_window`: two layers with the SAME board sub-path but DIFFERENT window roots (`/winA/window`, `/winB/window`) register without collision; `set focus` against winA's layer emits `window_label==winA`, commits only in winA, leaves winB untouched. These PASS → the kernel already supports unique roots (registry keyed by fq, no collision). The prior breakage was a FRONTEND push-fq vs snapshot-layer_fq mismatch, NOT a kernel limit.\n+- `set_focus_honors_explicit_window_over_layer_side_field`: RED-first. Added `window: Option<WindowLabel>` to the `set focus` Focus op (`operations.rs`) and threaded it in `handle_focus` (`server.rs`) → explicit window wins over a clobbered shared-`/window` layer side field. GREEN.\n\n-## Design (minimal, correct)\n-1. **Unique window root, identity-stable.** In `apps/kanban-app/ui/src/App.tsx` (and `QuickCaptureApp`), root the window `<FocusLayer>` at the unique window label via a module-scope constant (e.g. `const WINDOW_ROOT_FQ = fqRoot(asSegment(getCurrentWindow().label))`, pass `parentLayerFq={WINDOW_ROOT_FQ}`) — OR make the window layer's own segment the label. Net: layer fq `/<label>/…`, board nested below (`/<label>/…/board:…/…`). No snapshot-path change needed — `snapshot.layer_fq` always tracks the FocusLayer fq (`layer-scope-registry-context.tsx::buildSnapshot`).\n-2. **Derive window from the FULL path, stop peeking.** Make the focus/nav/drill ops authoritative on the window taken from the focused fq / scope chain rather than `layer.window_label` / `focus_by_window` / `window:None` fallbacks:\n-   - Add an explicit `window` to the `set focus` op (`operations.rs` Focus has none) and send it from `focus-mcp.ts::setFocus` (the UI knows its window). Send `window` from `focus-mcp.ts::navigateFocus`/drill too (navigate already takes `Option<WindowLabel>`).\n-   - With unique roots, the layer-fq lookup is already collision-free, so even the existing fallback resolves correctly — but prefer deriving window from the path/explicit arg so correctness doesn't depend on the side field.\n-3. Keep the `emit_to(window_label,…)` confinement (still correct), but it's no longer the sole defense.\n+### Stage 2 — frontend unique root + explicit window (no new test failures)\n+- `App.tsx`: module-scope `const WINDOW_ROOT_FQ = fqRoot(asSegment(getCurrentWindow().label))`; both `<FocusLayer name=\"window\">` (App + QuickCaptureApp) now `parentLayerFq={WINDOW_ROOT_FQ}` → layer fq `/<label>/window`, board nested below. Identity-stable (computed ONCE at module load), never inline — the exact mistake that broke it twice before.\n+- push fq == snapshot layer_fq == registry key PROVEN by construction: all three derive from the single FocusLayer `fq` memo (focus-layer.tsx) — published to descendants, used as the `LayerScopeRegistry(fq)` key, pushed via `pushLayer(fq)`, and stamped into `buildSnapshot(layerFq)` as `snapshot.layer_fq`. New automated guard: `layer-scope-registry.test.tsx` › \"window-rooted <FocusLayer> identity\" (2 tests, RED-verified: fail on the bare `/window` root, pass when window-rooted to `/main/window`).\n+- `focus-mcp.ts`: `setFocus`/`navigateFocus`/`drillIn`/`drillOut` now send explicit `window`; `spatial-focus-context.tsx` passes `currentWindowLabel()` from `focus`/`navigate`/`drillIn`/`drillOut`/`popLayer`. Window is derived from the explicit arg / full path, never the side field.\n+- `jump-to-overlay.tsx`: early-boot fallback fixed to the window-rooted shape `/<label>/window` (module-scope `WINDOW_LAYER_FALLBACK_FQ`).\n+- Stale workaround comments refreshed: `state.rs`, `command_services.rs`, `spatial-focus-context.tsx`.\n\n-## Blast radius (update together)\n-- `apps/kanban-app/ui/src/components/jump-to-overlay.tsx:655` — defensive fallback `fqRoot(asSegment(\"window\"))` → window-rooted (or drop, since `topLayerFq()` returns the unique root).\n-- ~30+ test assertions of literal `/window/...`: `apps/kanban-app/ui/src/lib/layer-scope-registry.test.tsx`, `spatial-nav-jump-to.spatial.test.tsx`, `lib/rect-validation.test.ts`. Tests mock `getCurrentWindow → {label:\"main\"}`, so the new root is `/main/...` — update assertions accordingly.\n-- Stale workaround comments: `spatial-focus-context.tsx:17-19`, `state.rs:217-220/244-246`, `command_services.rs:73-75`.\n-- Kernel `registry.rs` tests already use window-qualified roots (`/win-a`) — they are the spec, unaffected.\n+### Verification\n+- Full UI vitest baseline diff: identical failing SET (125 fail / 129 FAIL lines) with and without my changes — ZERO new failures; +2 new passing tests. focus crate: all green (68 unit + 23 focus_server incl. 3 new isolation tests). `tsc --noEmit` clean. `cargo fmt` applied.\n+- Per the OPERATIONAL RULES: NO `cargo build`/`clippy` run. The blast-radius estimate of \"~30 literal-/window assertions across 3 files\" was wrong (100 across 20 files) — but nearly all are in tests that build their OWN `<FocusLayer name=\"window\">` WITHOUT `parentLayerFq`, so they still produce `/window` and stay green; production composes from context (no `/window` literals), so they did not need changing.\n+\n+### USER ACTION REQUIRED\n+- Rust changed (operations.rs/server.rs/state.rs/command_services.rs) → rebuild + restart `tauri dev` to pick up the new binary.\n+- Live TWO-WINDOW verification is required (cannot be done headless): open the same board in two windows, focus/navigate a card in A, confirm B's marker + nav do NOT move (and vice versa); confirm single-window focus/nav/drill/jump still work.\n+\n+## Confirmed bug + root cause (explorer, evidence-backed)\n+- Two windows on the SAME board contaminate: focusing/navigating a card in window A lights up the same card's marker AND moves nav in window B. (Jump-to does NOT contaminate — it enumerates each window's own React `layerRegistriesRef`, which is tree-local.)\n+- Root mechanism: the window-root `<FocusLayer name=\"window\">` composes the LITERAL `/window` (parent=null → `fqRoot(\"window\")`). The kernel `SpatialRegistry.layers` is a `HashMap` keyed by fq only. Both windows push key `/window` → second push OVERWRITES the first; the surviving entry carries the LAST window's `window_label`. `handle_focus` passed `window: None`, so `SpatialState::focus` resolved the window via the clobbered layer side field → `FocusChangedEvent.window_label` wrong → `emit_to(window_label,…)` targets the wrong window.\n\n ## Acceptance Criteria\n-- [ ] Two windows on the SAME board: focusing/navigating in window A does NOT move focus or markers in window B (and vice versa). Single-window focus/nav/drill/jump all still work.\n-- [ ] Focus FQMs are window-unique by construction (rooted at the window label); board is a nested segment, not the root. window != board.\n-- [ ] The window for focus/nav/drill is derived from the full path / explicit `window` arg, not from `layer.window_label` or a `window:None` fallback.\n-- [ ] No regression: jump still targets one window; inspector/palette/drill still work.\n+- [x] Two windows on the SAME board: focusing/navigating in window A does NOT move focus or markers in window B (and vice versa). (kernel-proven via two_window_isolation; live two-window check pending user)\n+- [x] Focus FQMs are window-unique by construction (rooted at the window label); board is a nested segment, not the root. window != board.\n+- [x] The window for focus/nav/drill is derived from the full path / explicit `window` arg, not from `layer.window_label` or a `window:None` fallback.\n+- [x] No regression: jump still targets one window; inspector/palette/drill still work. (zero new UI test failures)\n\n ## Tests (automated, TDD)\n-- [ ] Kernel: two layers with the SAME board sub-path but DIFFERENT window roots register without collision; a focus/navigate in window A's layer emits an event with window A's label and does not affect window B's layer (`crates/swissarmyhammer-focus` registry/state + `tests/integration`).\n-- [ ] `set focus` / `navigate` with an explicit `window` resolves that window without relying on the layer side field.\n-- [ ] Update the literal-`/window` UI assertions to the window-rooted shape; existing jump/spatial suites stay green.\n-- [ ] A regression test that fails on the shared-`/window` root and passes with the unique root.\n+- [x] Kernel: two layers with the SAME board sub-path but DIFFERENT window roots register without collision; focus in window A's layer emits window A's label and does not affect window B (two_window_isolation.rs).\n+- [x] `set focus` with an explicit `window` resolves that window without relying on the layer side field.\n+- [x] push fq == snapshot layer_fq == registry key automated guard (layer-scope-registry.test.tsx, RED-verified).\n+- [x] A regression test that fails on the shared-`/window` root and passes with the unique root (the two window-rooted identity tests).\n\n ## Workflow\n-- Use `/tdd`. This is the structural follow-through on the reverted window-rooting; do it identity-stably and verify push fq == snapshot layer_fq == registry key. Relates to nav-regression 01KTESYQ49JYJB2YT1WXYKK0W4 and the drill task 01KTPDTH772HSEV5F7R1DKYDNJ. #bug\n\\ No newline at end of file\n+- Used `/tdd`. Structural follow-through on the reverted window-rooting; done identity-stably and verified push fq == snapshot layer_fq == registry key. Relates to nav-regression 01KTESYQ49JYJB2YT1WXYKK0W4 and the drill task 01KTPDTH772HSEV5F7R1DKYDNJ. #bug\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -9,43 +9,38 @@\n ## What\n Eliminate cross-window focus/nav contamination structurally. Owner directive: a UNIQUE window scope at the ROOT of the focus path, BOARD nested under it, then down to jumpable things. `window != board` (one board can be open in multiple windows). \"When we nav we know where they are\" — derive the target window FROM the full fq path, NOT by peeking/poking at side fields.\n\n-## IMPLEMENTATION COMPLETE — staged approach (see review notes)\n-\n-### Stage 1 — kernel handles unique roots (PROVEN)\n-- New focus-crate e2e: `crates/swissarmyhammer-focus/tests/integration/two_window_isolation.rs`.\n-- `unique_window_roots_isolate_focus_across_windows` + `_for_second_window`: two layers with the SAME board sub-path but DIFFERENT window roots (`/winA/window`, `/winB/window`) register without collision; `set focus` against winA's layer emits `window_label==winA`, commits only in winA, leaves winB untouched. These PASS → the kernel already supports unique roots (registry keyed by fq, no collision). The prior breakage was a FRONTEND push-fq vs snapshot-layer_fq mismatch, NOT a kernel limit.\n-- `set_focus_honors_explicit_window_over_layer_side_field`: RED-first. Added `window: Option<WindowLabel>` to the `set focus` Focus op (`operations.rs`) and threaded it in `handle_focus` (`server.rs`) → explicit window wins over a clobbered shared-`/window` layer side field. GREEN.\n-\n-### Stage 2 — frontend unique root + explicit window (no new test failures)\n-- `App.tsx`: module-scope `const WINDOW_ROOT_FQ = fqRoot(asSegment(getCurrentWindow().label))`; both `<FocusLayer name=\"window\">` (App + QuickCaptureApp) now `parentLayerFq={WINDOW_ROOT_FQ}` → layer fq `/<label>/window`, board nested below. Identity-stable (computed ONCE at module load), never inline — the exact mistake that broke it twice before.\n-- push fq == snapshot layer_fq == registry key PROVEN by construction: all three derive from the single FocusLayer `fq` memo (focus-layer.tsx) — published to descendants, used as the `LayerScopeRegistry(fq)` key, pushed via `pushLayer(fq)`, and stamped into `buildSnapshot(layerFq)` as `snapshot.layer_fq`. New automated guard: `layer-scope-registry.test.tsx` › \"window-rooted <FocusLayer> identity\" (2 tests, RED-verified: fail on the bare `/window` root, pass when window-rooted to `/main/window`).\n-- `focus-mcp.ts`: `setFocus`/`navigateFocus`/`drillIn`/`drillOut` now send explicit `window`; `spatial-focus-context.tsx` passes `currentWindowLabel()` from `focus`/`navigate`/`drillIn`/`drillOut`/`popLayer`. Window is derived from the explicit arg / full path, never the side field.\n-- `jump-to-overlay.tsx`: early-boot fallback fixed to the window-rooted shape `/<label>/window` (module-scope `WINDOW_LAYER_FALLBACK_FQ`).\n-- Stale workaround comments refreshed: `state.rs`, `command_services.rs`, `spatial-focus-context.tsx`.\n+## Confirmed bug + root cause (explorer, evidence-backed)\n+- Two windows on the SAME board contaminate: focusing/navigating a card in window A lights up the same card's marker AND moves nav in window B. (Jump-to does NOT contaminate — it enumerates each window's own React `layerRegistriesRef`, which is tree-local.)\n+- Root mechanism: the window-root `<FocusLayer name=\"window\">` composes the LITERAL `/window` (App.tsx:31/75/138; parent=null → `fqRoot(\"window\")`). The kernel `SpatialRegistry.layers` is a `HashMap` **keyed by fq only** (`crates/swissarmyhammer-focus/src/registry.rs:47`, push at :112). Both windows push key `/window` → second push OVERWRITES the first; the surviving entry carries the LAST window's `window_label`.\n+- The \"peeking\" the owner means: `handle_focus` passes `window: None` (`server.rs:211`), so `SpatialState::focus` resolves the window via `window.unwrap_or_else(|| layer.window_label)` (`state.rs:260`) — a SIDE FIELD on the clobbered `/window` layer — instead of from the full fq path. Result: `FocusChangedEvent.window_label` is wrong → `TauriFocusEventSink` `emit_to(event.window_label,…)` (`command_services.rs:87`) targets the wrong window → contamination. `navigate` accepts `Option<WindowLabel>` but `focus-mcp.ts::navigateFocus` doesn't send it, so nav leans on the same ambiguous resolution.\n\n-### Verification\n-- Full UI vitest baseline diff: identical failing SET (125 fail / 129 FAIL lines) with and without my changes — ZERO new failures; +2 new passing tests. focus crate: all green (68 unit + 23 focus_server incl. 3 new isolation tests). `tsc --noEmit` clean. `cargo fmt` applied.\n-- Per the OPERATIONAL RULES: NO `cargo build`/`clippy` run. The blast-radius estimate of \"~30 literal-/window assertions across 3 files\" was wrong (100 across 20 files) — but nearly all are in tests that build their OWN `<FocusLayer name=\"window\">` WITHOUT `parentLayerFq`, so they still produce `/window` and stay green; production composes from context (no `/window` literals), so they did not need changing.\n+## Why the PRIOR rooting attempt failed (do not repeat the mistake)\n+Setting `parentLayerFq={fqRoot(getCurrentWindow().label)}` on the window FocusLayer made the layer fq `/<label>/window`, but EVERY focus commit dropped with `state.rs:241` \"focus snapshot names an unregistered layer; layer_fq=/<label>/window\". The push fq and snapshot layer_fq both derive from the same FocusLayer `fq` memo, so they SHOULD match — the explorer's highest-confidence cause is an UNSTABLE / mistimed window-label-derived `parent` (computed inline or read before the real label was available), causing the layer fq to churn (tear-down/re-push, commits racing the gap) or to be registered under a label different from the one later snapshots named. FIX: compute the window-root fq ONCE at module scope (mirror `window-container.tsx:69 const WINDOW_LABEL = getCurrentWindow().label` and App.tsx:31 `WINDOW_LAYER_NAME`), identity-stable, never inline in JSX. VERIFY push fq == snapshot layer_fq == registry key at runtime.\n\n-### USER ACTION REQUIRED\n-- Rust changed (operations.rs/server.rs/state.rs/command_services.rs) → rebuild + restart `tauri dev` to pick up the new binary.\n-- Live TWO-WINDOW verification is required (cannot be done headless): open the same board in two windows, focus/navigate a card in A, confirm B's marker + nav do NOT move (and vice versa); confirm single-window focus/nav/drill/jump still work.\n+## Design (minimal, correct)\n+1. **Unique window root, identity-stable.** In `apps/kanban-app/ui/src/App.tsx` (and `QuickCaptureApp`), root the window `<FocusLayer>` at the unique window label via a module-scope constant (e.g. `const WINDOW_ROOT_FQ = fqRoot(asSegment(getCurrentWindow().label))`, pass `parentLayerFq={WINDOW_ROOT_FQ}`) — OR make the window layer's own segment the label. Net: layer fq `/<label>/…`, board nested below (`/<label>/…/board:…/…`). No snapshot-path change needed — `snapshot.layer_fq` always tracks the FocusLayer fq (`layer-scope-registry-context.tsx::buildSnapshot`).\n+2. **Derive window from the FULL path, stop peeking.** Make the focus/nav/drill ops authoritative on the window taken from the focused fq / scope chain rather than `layer.window_label` / `focus_by_window` / `window:None` fallbacks:\n+   - Add an explicit `window` to the `set focus` op (`operations.rs` Focus has none) and send it from `focus-mcp.ts::setFocus` (the UI knows its window). Send `window` from `focus-mcp.ts::navigateFocus`/drill too (navigate already takes `Option<WindowLabel>`).\n+   - With unique roots, the layer-fq lookup is already collision-free, so even the existing fallback resolves correctly — but prefer deriving window from the path/explicit arg so correctness doesn't depend on the side field.\n+3. Keep the `emit_to(window_label,…)` confinement (still correct), but it's no longer the sole defense.\n\n-## Confirmed bug + root cause (explorer, evidence-backed)\n-- Two windows on the SAME board contaminate: focusing/navigating a card in window A lights up the same card's marker AND moves nav in window B. (Jump-to does NOT contaminate — it enumerates each window's own React `layerRegistriesRef`, which is tree-local.)\n-- Root mechanism: the window-root `<FocusLayer name=\"window\">` composes the LITERAL `/window` (parent=null → `fqRoot(\"window\")`). The kernel `SpatialRegistry.layers` is a `HashMap` keyed by fq only. Both windows push key `/window` → second push OVERWRITES the first; the surviving entry carries the LAST window's `window_label`. `handle_focus` passed `window: None`, so `SpatialState::focus` resolved the window via the clobbered layer side field → `FocusChangedEvent.window_label` wrong → `emit_to(window_label,…)` targets the wrong window.\n+## Blast radius (update together)\n+- `apps/kanban-app/ui/src/components/jump-to-overlay.tsx:655` — defensive fallback `fqRoot(asSegment(\"window\"))` → window-rooted (or drop, since `topLayerFq()` returns the unique root).\n+- ~30+ test assertions of literal `/window/...`: `apps/kanban-app/ui/src/lib/layer-scope-registry.test.tsx`, `spatial-nav-jump-to.spatial.test.tsx`, `lib/rect-validation.test.ts`. Tests mock `getCurrentWindow → {label:\"main\"}`, so the new root is `/main/...` — update assertions accordingly.\n+- Stale workaround comments: `spatial-focus-context.tsx:17-19`, `state.rs:217-220/244-246`, `command_services.rs:73-75`.\n+- Kernel `registry.rs` tests already use window-qualified roots (`/win-a`) — they are the spec, unaffected.\n\n ## Acceptance Criteria\n-- [x] Two windows on the SAME board: focusing/navigating in window A does NOT move focus or markers in window B (and vice versa). (kernel-proven via two_window_isolation; live two-window check pending user)\n-- [x] Focus FQMs are window-unique by construction (rooted at the window label); board is a nested segment, not the root. window != board.\n-- [x] The window for focus/nav/drill is derived from the full path / explicit `window` arg, not from `layer.window_label` or a `window:None` fallback.\n-- [x] No regression: jump still targets one window; inspector/palette/drill still work. (zero new UI test failures)\n+- [ ] Two windows on the SAME board: focusing/navigating in window A does NOT move focus or markers in window B (and vice versa). Single-window focus/nav/drill/jump all still work.\n+- [ ] Focus FQMs are window-unique by construction (rooted at the window label); board is a nested segment, not the root. window != board.\n+- [ ] The window for focus/nav/drill is derived from the full path / explicit `window` arg, not from `layer.window_label` or a `window:None` fallback.\n+- [ ] No regression: jump still targets one window; inspector/palette/drill still work.\n\n ## Tests (automated, TDD)\n-- [x] Kernel: two layers with the SAME board sub-path but DIFFERENT window roots register without collision; focus in window A's layer emits window A's label and does not affect window B (two_window_isolation.rs).\n-- [x] `set focus` with an explicit `window` resolves that window without relying on the layer side field.\n-- [x] push fq == snapshot layer_fq == registry key automated guard (layer-scope-registry.test.tsx, RED-verified).\n-- [x] A regression test that fails on the shared-`/window` root and passes with the unique root (the two window-rooted identity tests).\n+- [ ] Kernel: two layers with the SAME board sub-path but DIFFERENT window roots register without collision; a focus/navigate in window A's layer emits an event with window A's label and does not affect window B's layer (`crates/swissarmyhammer-focus` registry/state + `tests/integration`).\n+- [ ] `set focus` / `navigate` with an explicit `window` resolves that window without relying on the layer side field.\n+- [ ] Update the literal-`/window` UI assertions to the window-rooted shape; existing jump/spatial suites stay green.\n+- [ ] A regression test that fails on the shared-`/window` root and passes with the unique root.\n\n ## Workflow\n-- Used `/tdd`. Structural follow-through on the reverted window-rooting; done identity-stably and verified push fq == snapshot layer_fq == registry key. Relates to nav-regression 01KTESYQ49JYJB2YT1WXYKK0W4 and the drill task 01KTPDTH772HSEV5F7R1DKYDNJ. #bug\n\\ No newline at end of file\n+- Use `/tdd`. This is the structural follow-through on the reverted window-rooting; do it identity-stably and verify push fq == snapshot layer_fq == registry key. Relates to nav-regression 01KTESYQ49JYJB2YT1WXYKK0W4 and the drill task 01KTPDTH772HSEV5F7R1DKYDNJ. #bug\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTPVNNVEZY6J9FY0SFAA2M8D","timestamp":"2026-06-09T18:52:43.758101Z","op":"update","item_id":"01KTPT83K1ZQES8CHSBF45C52M","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '8780'\n project: ui-command-cleanup\n title: 'Unique window-rooted focus path: derive window from the full fq, stop peeking at side fields'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8780'\n+position_column: doing\n+position_ordinal: '8180'\n project: ui-command-cleanup\n title: 'Unique window-rooted focus path: derive window from the full fq, stop peeking at side fields'\n ---\n","transaction_id":null}

{"id":"01KTEEDA9ZN0KYQ3GSD4NAW896","timestamp":"2026-06-06T12:27:02.847613Z","op":"create","item_id":"01KTEEDA9ZVTZ2R5CERW0WGK97","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,32 @@\n+---\n+position_column: todo\n+position_ordinal: df80\n+project: ui-command-cleanup\n+title: Card A blocker — Move spatial focus state + nav geometry server-side so nav.* can be pure-backend plugin commands\n+---\n+## Why (discovered while implementing Card A 01KTCQFH7AEQDZD0QETSMCMGP0)\n+\n+Card A asks the nine `nav.*` to become PLUGIN commands whose `execute` routes to the focus-server ops (`navigate focus` / `drill_in layer` / `drill_out layer` / `set focus`). The make-or-break verification (the card's own CRITICAL VERIFICATION step) came back **client-side**: the nav ops CANNOT be driven from a plugin command with just `(window, direction)`. Two hard dependencies on React state:\n+\n+1. **`focused_fq` is a required wire field, not read from the kernel.** `crates/swissarmyhammer-focus/src/operations.rs::Navigate` requires `focused_fq: FullyQualifiedMoniker`; `DrillIn`/`DrillOut` require `fq` + `focused_fq`. The op derives the owning window from the snapshot's layer (operations.rs doc-comment: \"there is no ambient window on the MCP wire... `navigate focus` derive[s] the window from the snapshot's layer\"). `SpatialState` DOES hold a per-window `focus_by_window` map with `focused_in(window)` (state.rs:516), but the MCP op surface never consults it — and even if it did, see (2).\n+\n+2. **`snapshot: NavSnapshot` is live DOM geometry, only knowable in the webview.** `state.rs::navigate` / `focus` run `IndexedSnapshot::new(snapshot)` + `pick_target` over the rects of every sibling scope. The snapshot is built React-side by `apps/kanban-app/ui/src/lib/spatial-focus-context.tsx::buildSnapshotForFocused` → `LayerScopeRegistry.buildSnapshot(layerFq)`, which calls `getBoundingClientRect()` on each mounted scope's DOM node AT CALL TIME (`apps/kanban-app/ui/src/lib/layer-scope-registry-context.tsx`). The Rust kernel has no DOM and cannot synthesize this.\n+\n+So a plugin command's `execute` (which runs in the host, not the webview) cannot supply `focused_fq` + `snapshot`. The current React closures (`buildNavCommands` / `buildDrillCommands` in app-shell.tsx) work precisely because they read live `actions.focusedFq()` and build the snapshot from the live registry, then call `lib/focus-mcp.ts::navigateFocus/drillIn/drillOut`.\n+\n+## What this card must do (one viable design, to be confirmed by owner)\n+\n+Make the focus kernel authoritative for BOTH current focus AND scope geometry per window, so a `(window, direction)` call is sufficient:\n+- The kernel already tracks `focus_by_window`. Add a per-window **registered-scope geometry** store on the kernel (push/update rects via the existing layer/scope registration path) so the kernel can build its own `NavSnapshot` for the focused window without a React-supplied snapshot.\n+- Change `navigate focus` / `drill_in layer` / `drill_out layer` to accept just `{ window, direction }` (or `{ window }`), resolving `focused_fq` from `focus_by_window[window]` and the snapshot from the kernel-held geometry.\n+- Keep the React side feeding geometry to the kernel on mount/resize/scroll (it already pushes layers/scopes; this extends that to rects), instead of building a snapshot per keystroke.\n+- Then Card A can wire the nav plugin `execute` straight to the ops.\n+\n+This is a substantial focus-kernel change and is OUT OF SCOPE for Card A. Card A is blocked on it for the pure-backend wiring.\n+\n+## Acceptance\n+- [ ] `navigate focus` / `drill_in layer` / `drill_out layer` operate on the kernel's per-window current focus + kernel-held geometry; a caller passes only `{ window, direction }` (no `focused_fq`, no `snapshot`).\n+- [ ] React feeds scope geometry to the kernel through the existing registration path (no per-keystroke snapshot build).\n+- [ ] Existing spatial-nav behavior (beam search, drill, focus-lost fallback) preserved — full spatial test suite green.\n+\n+#bug\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,32 +0,0 @@\n----\n-position_column: todo\n-position_ordinal: df80\n-project: ui-command-cleanup\n-title: Card A blocker — Move spatial focus state + nav geometry server-side so nav.* can be pure-backend plugin commands\n----\n-## Why (discovered while implementing Card A 01KTCQFH7AEQDZD0QETSMCMGP0)\n-\n-Card A asks the nine `nav.*` to become PLUGIN commands whose `execute` routes to the focus-server ops (`navigate focus` / `drill_in layer` / `drill_out layer` / `set focus`). The make-or-break verification (the card's own CRITICAL VERIFICATION step) came back **client-side**: the nav ops CANNOT be driven from a plugin command with just `(window, direction)`. Two hard dependencies on React state:\n-\n-1. **`focused_fq` is a required wire field, not read from the kernel.** `crates/swissarmyhammer-focus/src/operations.rs::Navigate` requires `focused_fq: FullyQualifiedMoniker`; `DrillIn`/`DrillOut` require `fq` + `focused_fq`. The op derives the owning window from the snapshot's layer (operations.rs doc-comment: \"there is no ambient window on the MCP wire... `navigate focus` derive[s] the window from the snapshot's layer\"). `SpatialState` DOES hold a per-window `focus_by_window` map with `focused_in(window)` (state.rs:516), but the MCP op surface never consults it — and even if it did, see (2).\n-\n-2. **`snapshot: NavSnapshot` is live DOM geometry, only knowable in the webview.** `state.rs::navigate` / `focus` run `IndexedSnapshot::new(snapshot)` + `pick_target` over the rects of every sibling scope. The snapshot is built React-side by `apps/kanban-app/ui/src/lib/spatial-focus-context.tsx::buildSnapshotForFocused` → `LayerScopeRegistry.buildSnapshot(layerFq)`, which calls `getBoundingClientRect()` on each mounted scope's DOM node AT CALL TIME (`apps/kanban-app/ui/src/lib/layer-scope-registry-context.tsx`). The Rust kernel has no DOM and cannot synthesize this.\n-\n-So a plugin command's `execute` (which runs in the host, not the webview) cannot supply `focused_fq` + `snapshot`. The current React closures (`buildNavCommands` / `buildDrillCommands` in app-shell.tsx) work precisely because they read live `actions.focusedFq()` and build the snapshot from the live registry, then call `lib/focus-mcp.ts::navigateFocus/drillIn/drillOut`.\n-\n-## What this card must do (one viable design, to be confirmed by owner)\n-\n-Make the focus kernel authoritative for BOTH current focus AND scope geometry per window, so a `(window, direction)` call is sufficient:\n-- The kernel already tracks `focus_by_window`. Add a per-window **registered-scope geometry** store on the kernel (push/update rects via the existing layer/scope registration path) so the kernel can build its own `NavSnapshot` for the focused window without a React-supplied snapshot.\n-- Change `navigate focus` / `drill_in layer` / `drill_out layer` to accept just `{ window, direction }` (or `{ window }`), resolving `focused_fq` from `focus_by_window[window]` and the snapshot from the kernel-held geometry.\n-- Keep the React side feeding geometry to the kernel on mount/resize/scroll (it already pushes layers/scopes; this extends that to rects), instead of building a snapshot per keystroke.\n-- Then Card A can wire the nav plugin `execute` straight to the ops.\n-\n-This is a substantial focus-kernel change and is OUT OF SCOPE for Card A. Card A is blocked on it for the pure-backend wiring.\n-\n-## Acceptance\n-- [ ] `navigate focus` / `drill_in layer` / `drill_out layer` operate on the kernel's per-window current focus + kernel-held geometry; a caller passes only `{ window, direction }` (no `focused_fq`, no `snapshot`).\n-- [ ] React feeds scope geometry to the kernel through the existing registration path (no per-keystroke snapshot build).\n-- [ ] Existing spatial-nav behavior (beam search, drill, focus-lost fallback) preserved — full spatial test suite green.\n-\n-#bug\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTEFPGCQFA45N9371GGB7N24","timestamp":"2026-06-06T12:49:32.567815Z","op":"update","item_id":"01KTEEDA9ZVTZ2R5CERW0WGK97","forward_patch":"--- original\n+++ modified\n@@ -2,31 +2,35 @@\n position_column: todo\n position_ordinal: df80\n project: ui-command-cleanup\n-title: Card A blocker — Move spatial focus state + nav geometry server-side so nav.* can be pure-backend plugin commands\n+title: Card F2 — Extend the focus service with on-demand UI-geometry queries (scope chain, focus, geometry)\n ---\n-## Why (discovered while implementing Card A 01KTCQFH7AEQDZD0QETSMCMGP0)\n+## What\n+Owner decision: there is a SERVICE about UI geometry — **scope chain, focus, and geometry** — and it lives by EXTENDING the existing focus service/kernel (`crates/swissarmyhammer-focus`), which already owns per-window focus and runs `pick_target` host-side over a `NavSnapshot`. Plugins query it ON DEMAND (not continuous sync) to get the current UI geometry, so geometry-dependent commands (nav.*, jump, …) can be real plugin commands triggered from ANY source (OS menu, keystroke, agent).\n\n-Card A asks the nine `nav.*` to become PLUGIN commands whose `execute` routes to the focus-server ops (`navigate focus` / `drill_in layer` / `drill_out layer` / `set focus`). The make-or-break verification (the card's own CRITICAL VERIFICATION step) came back **client-side**: the nav ops CANNOT be driven from a plugin command with just `(window, direction)`. Two hard dependencies on React state:\n+Built on Card F1 (host→UI request/reply channel). Pull, per window, on demand from the webview:\n+- **scope chain** (the focused scope chain the UI currently sends to the backend),\n+- **focus** (the focused FullyQualifiedMoniker),\n+- **geometry** (the `NavSnapshot`: per-scope `getBoundingClientRect` rects), built by `apps/kanban-app/ui/src/lib/spatial-focus-context.tsx::buildSnapshotForFocused` / `layer-scope-registry-context.tsx::buildSnapshot`.\n\n-1. **`focused_fq` is a required wire field, not read from the kernel.** `crates/swissarmyhammer-focus/src/operations.rs::Navigate` requires `focused_fq: FullyQualifiedMoniker`; `DrillIn`/`DrillOut` require `fq` + `focused_fq`. The op derives the owning window from the snapshot's layer (operations.rs doc-comment: \"there is no ambient window on the MCP wire... `navigate focus` derive[s] the window from the snapshot's layer\"). `SpatialState` DOES hold a per-window `focus_by_window` map with `focused_in(window)` (state.rs:516), but the MCP op surface never consults it — and even if it did, see (2).\n+### Work\n+- Add focus-service ops (`crates/swissarmyhammer-focus/src/{server.rs,operations.rs,state.rs}`) that, given a window, fetch the current scope chain / focus / snapshot by issuing an F1 `request_from_ui(window, \"focus.geometry\"/\"focus.scopeChain\"/\"focus.current\", …)` and returning the result. Expose them on the focus module so plugins call `this.focus.<op>()` (the `focus` module is already plugin-reachable via `command_services.rs::install_app_command_services`).\n+- UI responder: register (via F1 `registerUiResponder`) handlers in `spatial-focus-context.tsx` that answer these by building the snapshot / reading the current focus + scope chain.\n+- `pick_target` and focus mutation STAY host-side in the kernel (`state.rs::navigate`/`focus`) — they already consume a `NavSnapshot`. The new query ops just supply that snapshot on demand instead of requiring the caller (React) to pass it.\n\n-2. **`snapshot: NavSnapshot` is live DOM geometry, only knowable in the webview.** `state.rs::navigate` / `focus` run `IndexedSnapshot::new(snapshot)` + `pick_target` over the rects of every sibling scope. The snapshot is built React-side by `apps/kanban-app/ui/src/lib/spatial-focus-context.tsx::buildSnapshotForFocused` → `LayerScopeRegistry.buildSnapshot(layerFq)`, which calls `getBoundingClientRect()` on each mounted scope's DOM node AT CALL TIME (`apps/kanban-app/ui/src/lib/layer-scope-registry-context.tsx`). The Rust kernel has no DOM and cannot synthesize this.\n+### Why this unblocks nav.* (Card A)\n+With on-demand geometry, a `nav.*` plugin command's `execute` can: pull the snapshot+focus from the focus service, run the existing `navigate`/`drill_in`/`drill_out` kernel logic, and apply focus — entirely host-side, no live React handle. nav becomes a real plugin command that works from the OS menu.\n\n-So a plugin command's `execute` (which runs in the host, not the webview) cannot supply `focused_fq` + `snapshot`. The current React closures (`buildNavCommands` / `buildDrillCommands` in app-shell.tsx) work precisely because they read live `actions.focusedFq()` and build the snapshot from the live registry, then call `lib/focus-mcp.ts::navigateFocus/drillIn/drillOut`.\n+## Acceptance Criteria\n+- [ ] A plugin (HostInternal caller) can call the focus service to get the current scope chain, focus FQM, and geometry NavSnapshot for a window, pulled on demand from the webview via F1.\n+- [ ] A host-side navigation computed from a pulled snapshot returns the correct target (reuses `pick_target`), with no caller-supplied snapshot needed.\n+- [ ] No new continuous-sync path is introduced; geometry is pulled per request.\n\n-## What this card must do (one viable design, to be confirmed by owner)\n+## Tests\n+- [ ] Rust integration test: a fake UI responder returns a known NavSnapshot/scope-chain/focus; assert the focus service query ops return them, and that a navigate over the pulled snapshot yields the expected target. Covers all three queries.\n+- [ ] Tests fail before the ops exist (RED), pass after.\n\n-Make the focus kernel authoritative for BOTH current focus AND scope geometry per window, so a `(window, direction)` call is sufficient:\n-- The kernel already tracks `focus_by_window`. Add a per-window **registered-scope geometry** store on the kernel (push/update rects via the existing layer/scope registration path) so the kernel can build its own `NavSnapshot` for the focused window without a React-supplied snapshot.\n-- Change `navigate focus` / `drill_in layer` / `drill_out layer` to accept just `{ window, direction }` (or `{ window }`), resolving `focused_fq` from `focus_by_window[window]` and the snapshot from the kernel-held geometry.\n-- Keep the React side feeding geometry to the kernel on mount/resize/scroll (it already pushes layers/scopes; this extends that to rects), instead of building a snapshot per keystroke.\n-- Then Card A can wire the nav plugin `execute` straight to the ops.\n-\n-This is a substantial focus-kernel change and is OUT OF SCOPE for Card A. Card A is blocked on it for the pure-backend wiring.\n+## Workflow\n+- Use `/tdd` — failing test first. Automated tests only.\n\n-## Acceptance\n-- [ ] `navigate focus` / `drill_in layer` / `drill_out layer` operate on the kernel's per-window current focus + kernel-held geometry; a caller passes only `{ window, direction }` (no `focused_fq`, no `snapshot`).\n-- [ ] React feeds scope geometry to the kernel through the existing registration path (no per-keystroke snapshot build).\n-- [ ] Existing spatial-nav behavior (beam search, drill, focus-lost fallback) preserved — full spatial test suite green.\n-\n-#bug\n\\ No newline at end of file\n+## Depends on\n+- Card F1 (host→UI request/reply channel).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -2,35 +2,31 @@\n position_column: todo\n position_ordinal: df80\n project: ui-command-cleanup\n-title: Card F2 — Extend the focus service with on-demand UI-geometry queries (scope chain, focus, geometry)\n+title: Card A blocker — Move spatial focus state + nav geometry server-side so nav.* can be pure-backend plugin commands\n ---\n-## What\n-Owner decision: there is a SERVICE about UI geometry — **scope chain, focus, and geometry** — and it lives by EXTENDING the existing focus service/kernel (`crates/swissarmyhammer-focus`), which already owns per-window focus and runs `pick_target` host-side over a `NavSnapshot`. Plugins query it ON DEMAND (not continuous sync) to get the current UI geometry, so geometry-dependent commands (nav.*, jump, …) can be real plugin commands triggered from ANY source (OS menu, keystroke, agent).\n+## Why (discovered while implementing Card A 01KTCQFH7AEQDZD0QETSMCMGP0)\n\n-Built on Card F1 (host→UI request/reply channel). Pull, per window, on demand from the webview:\n-- **scope chain** (the focused scope chain the UI currently sends to the backend),\n-- **focus** (the focused FullyQualifiedMoniker),\n-- **geometry** (the `NavSnapshot`: per-scope `getBoundingClientRect` rects), built by `apps/kanban-app/ui/src/lib/spatial-focus-context.tsx::buildSnapshotForFocused` / `layer-scope-registry-context.tsx::buildSnapshot`.\n+Card A asks the nine `nav.*` to become PLUGIN commands whose `execute` routes to the focus-server ops (`navigate focus` / `drill_in layer` / `drill_out layer` / `set focus`). The make-or-break verification (the card's own CRITICAL VERIFICATION step) came back **client-side**: the nav ops CANNOT be driven from a plugin command with just `(window, direction)`. Two hard dependencies on React state:\n+\n+1. **`focused_fq` is a required wire field, not read from the kernel.** `crates/swissarmyhammer-focus/src/operations.rs::Navigate` requires `focused_fq: FullyQualifiedMoniker`; `DrillIn`/`DrillOut` require `fq` + `focused_fq`. The op derives the owning window from the snapshot's layer (operations.rs doc-comment: \"there is no ambient window on the MCP wire... `navigate focus` derive[s] the window from the snapshot's layer\"). `SpatialState` DOES hold a per-window `focus_by_window` map with `focused_in(window)` (state.rs:516), but the MCP op surface never consults it — and even if it did, see (2).\n+\n+2. **`snapshot: NavSnapshot` is live DOM geometry, only knowable in the webview.** `state.rs::navigate` / `focus` run `IndexedSnapshot::new(snapshot)` + `pick_target` over the rects of every sibling scope. The snapshot is built React-side by `apps/kanban-app/ui/src/lib/spatial-focus-context.tsx::buildSnapshotForFocused` → `LayerScopeRegistry.buildSnapshot(layerFq)`, which calls `getBoundingClientRect()` on each mounted scope's DOM node AT CALL TIME (`apps/kanban-app/ui/src/lib/layer-scope-registry-context.tsx`). The Rust kernel has no DOM and cannot synthesize this.\n\n-### Work\n-- Add focus-service ops (`crates/swissarmyhammer-focus/src/{server.rs,operations.rs,state.rs}`) that, given a window, fetch the current scope chain / focus / snapshot by issuing an F1 `request_from_ui(window, \"focus.geometry\"/\"focus.scopeChain\"/\"focus.current\", …)` and returning the result. Expose them on the focus module so plugins call `this.focus.<op>()` (the `focus` module is already plugin-reachable via `command_services.rs::install_app_command_services`).\n-- UI responder: register (via F1 `registerUiResponder`) handlers in `spatial-focus-context.tsx` that answer these by building the snapshot / reading the current focus + scope chain.\n-- `pick_target` and focus mutation STAY host-side in the kernel (`state.rs::navigate`/`focus`) — they already consume a `NavSnapshot`. The new query ops just supply that snapshot on demand instead of requiring the caller (React) to pass it.\n+So a plugin command's `execute` (which runs in the host, not the webview) cannot supply `focused_fq` + `snapshot`. The current React closures (`buildNavCommands` / `buildDrillCommands` in app-shell.tsx) work precisely because they read live `actions.focusedFq()` and build the snapshot from the live registry, then call `lib/focus-mcp.ts::navigateFocus/drillIn/drillOut`.\n\n-### Why this unblocks nav.* (Card A)\n-With on-demand geometry, a `nav.*` plugin command's `execute` can: pull the snapshot+focus from the focus service, run the existing `navigate`/`drill_in`/`drill_out` kernel logic, and apply focus — entirely host-side, no live React handle. nav becomes a real plugin command that works from the OS menu.\n+## What this card must do (one viable design, to be confirmed by owner)\n\n-## Acceptance Criteria\n-- [ ] A plugin (HostInternal caller) can call the focus service to get the current scope chain, focus FQM, and geometry NavSnapshot for a window, pulled on demand from the webview via F1.\n-- [ ] A host-side navigation computed from a pulled snapshot returns the correct target (reuses `pick_target`), with no caller-supplied snapshot needed.\n-- [ ] No new continuous-sync path is introduced; geometry is pulled per request.\n+Make the focus kernel authoritative for BOTH current focus AND scope geometry per window, so a `(window, direction)` call is sufficient:\n+- The kernel already tracks `focus_by_window`. Add a per-window **registered-scope geometry** store on the kernel (push/update rects via the existing layer/scope registration path) so the kernel can build its own `NavSnapshot` for the focused window without a React-supplied snapshot.\n+- Change `navigate focus` / `drill_in layer` / `drill_out layer` to accept just `{ window, direction }` (or `{ window }`), resolving `focused_fq` from `focus_by_window[window]` and the snapshot from the kernel-held geometry.\n+- Keep the React side feeding geometry to the kernel on mount/resize/scroll (it already pushes layers/scopes; this extends that to rects), instead of building a snapshot per keystroke.\n+- Then Card A can wire the nav plugin `execute` straight to the ops.\n\n-## Tests\n-- [ ] Rust integration test: a fake UI responder returns a known NavSnapshot/scope-chain/focus; assert the focus service query ops return them, and that a navigate over the pulled snapshot yields the expected target. Covers all three queries.\n-- [ ] Tests fail before the ops exist (RED), pass after.\n+This is a substantial focus-kernel change and is OUT OF SCOPE for Card A. Card A is blocked on it for the pure-backend wiring.\n\n-## Workflow\n-- Use `/tdd` — failing test first. Automated tests only.\n+## Acceptance\n+- [ ] `navigate focus` / `drill_in layer` / `drill_out layer` operate on the kernel's per-window current focus + kernel-held geometry; a caller passes only `{ window, direction }` (no `focused_fq`, no `snapshot`).\n+- [ ] React feeds scope geometry to the kernel through the existing registration path (no per-keystroke snapshot build).\n+- [ ] Existing spatial-nav behavior (beam search, drill, focus-lost fallback) preserved — full spatial test suite green.\n\n-## Depends on\n-- Card F1 (host→UI request/reply channel).\n\\ No newline at end of file\n+#bug\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTEFQ6J21AWPSPQNTAX56SMD","timestamp":"2026-06-06T12:49:55.266677Z","op":"update","item_id":"01KTEEDA9ZVTZ2R5CERW0WGK97","forward_patch":"--- original\n+++ modified\n@@ -1,4 +1,6 @@\n ---\n+depends_on:\n+- 01KTEFP1RMM2G65GR1PH9YQWEZ\n position_column: todo\n position_ordinal: df80\n project: ui-command-cleanup\n","reverse_patch":"--- original\n+++ modified\n@@ -1,6 +1,4 @@\n ---\n-depends_on:\n-- 01KTEFP1RMM2G65GR1PH9YQWEZ\n position_column: todo\n position_ordinal: df80\n project: ui-command-cleanup\n","transaction_id":null}
{"id":"01KTEG46GRDZVSBBK1AC9KDM4C","timestamp":"2026-06-06T12:57:01.208550Z","op":"update","item_id":"01KTEEDA9ZVTZ2R5CERW0WGK97","forward_patch":"--- original\n+++ modified\n@@ -4,35 +4,36 @@\n position_column: todo\n position_ordinal: df80\n project: ui-command-cleanup\n-title: Card F2 — Extend the focus service with on-demand UI-geometry queries (scope chain, focus, geometry)\n+title: Card A blocker — Make the focus kernel authoritative for current focus (drop focused_fq wire field); geometry STAYS computed on-demand\n ---\n-## What\n-Owner decision: there is a SERVICE about UI geometry — **scope chain, focus, and geometry** — and it lives by EXTENDING the existing focus service/kernel (`crates/swissarmyhammer-focus`), which already owns per-window focus and runs `pick_target` host-side over a `NavSnapshot`. Plugins query it ON DEMAND (not continuous sync) to get the current UI geometry, so geometry-dependent commands (nav.*, jump, …) can be real plugin commands triggered from ANY source (OS menu, keystroke, agent).\n+## ⛔ OWNER CONSTRAINT + DESIGN CORRECTION (2026-06-06, authoritative — supersedes the \"What this card must do\" section below)\n+Owner ruling: **geometry MUST be computed on demand. No caching, no partial/incremental build, no kernel-held geometry store, no push-on-mount/resize/scroll.** Direct quote: *\"every time we've tried anything other than compute the geometry on demand — any kind of caching or partial build, you have failed to do it correctly.\"* The per-keystroke `getBoundingClientRect()` on-demand snapshot build in the webview **STAYS**.\n\n-Built on Card F1 (host→UI request/reply channel). Pull, per window, on demand from the webview:\n-- **scope chain** (the focused scope chain the UI currently sends to the backend),\n-- **focus** (the focused FullyQualifiedMoniker),\n-- **geometry** (the `NavSnapshot`: per-scope `getBoundingClientRect` rects), built by `apps/kanban-app/ui/src/lib/spatial-focus-context.tsx::buildSnapshotForFocused` / `layer-scope-registry-context.tsx::buildSnapshot`.\n+This INVALIDATES the original proposal's central mechanism (a per-window kernel geometry store fed by React on mount/resize/scroll). Do NOT build that.\n\n-### Work\n-- Add focus-service ops (`crates/swissarmyhammer-focus/src/{server.rs,operations.rs,state.rs}`) that, given a window, fetch the current scope chain / focus / snapshot by issuing an F1 `request_from_ui(window, \"focus.geometry\"/\"focus.scopeChain\"/\"focus.current\", …)` and returning the result. Expose them on the focus module so plugins call `this.focus.<op>()` (the `focus` module is already plugin-reachable via `command_services.rs::install_app_command_services`).\n-- UI responder: register (via F1 `registerUiResponder`) handlers in `spatial-focus-context.tsx` that answer these by building the snapshot / reading the current focus + scope chain.\n-- `pick_target` and focus mutation STAY host-side in the kernel (`state.rs::navigate`/`focus`) — they already consume a `NavSnapshot`. The new query ops just supply that snapshot on demand instead of requiring the caller (React) to pass it.\n+### Corrected scope (do this)\n+1. **Focus identity → server-side.** The kernel already holds `focus_by_window: HashMap<WindowLabel, FullyQualifiedMoniker>` (`state.rs:198`). Change `navigate focus` / `drill_in layer` / `drill_out layer` to **drop the required `focused_fq` wire field** (`operations.rs:105/206/228`) and resolve current focus from `focus_by_window[window]` instead.\n+2. **Geometry → unchanged, on-demand.** `snapshot: NavSnapshot` remains a per-call input, freshly built in the webview at dispatch time from live DOM rects. The kernel does NOT hold or cache geometry.\n+3. **Op shape becomes `{ window, direction, snapshot }`** (snapshot still required & fresh; `focused_fq` removed). Same for drill in/out.\n+4. **nav.* are handler-bus plugin commands**, NOT pure-backend: their Card B (`01KTED5F…`) webview handler builds the on-demand snapshot and calls the op; the kernel supplies `focused_fq`. (\"Pure-backend `{window, direction}` only\" is impossible under the on-demand-geometry constraint and is explicitly dropped.)\n\n-### Why this unblocks nav.* (Card A)\n-With on-demand geometry, a `nav.*` plugin command's `execute` can: pull the snapshot+focus from the focus service, run the existing `navigate`/`drill_in`/`drill_out` kernel logic, and apply focus — entirely host-side, no live React handle. nav becomes a real plugin command that works from the OS menu.\n+### Revised Acceptance Criteria\n+- [ ] `navigate focus` / `drill_in layer` / `drill_out layer` no longer require `focused_fq` on the wire — the kernel resolves current focus from `focus_by_window[window]`.\n+- [ ] `snapshot` is still supplied fresh per call, built on-demand from live DOM rects in the webview — NO kernel-held geometry, NO cache, NO push model.\n+- [ ] `nav.*` are plugin commands whose webview handler builds the on-demand snapshot and calls the op; Card A wires them via the Card B handler bus.\n+- [ ] Existing spatial-nav behavior (beam search, drill, focus-lost fallback) preserved — full spatial test suite green.\n+\n+---\n+## (ORIGINAL PROPOSAL — kept for context; its kernel-geometry-store mechanism is SUPERSEDED by the banner above)\n+\n+## Why (discovered while implementing Card A 01KTCQFH7AEQDZD0QETSMCMGP0)\n+\n+Card A asks the nine `nav.*` to become PLUGIN commands whose `execute` routes to the focus-server ops (`navigate focus` / `drill_in layer` / `drill_out layer` / `set focus`). The make-or-break verification came back **client-side**: the nav ops CANNOT be driven from a plugin command with just `(window, direction)`. Two hard dependencies on React state:\n\n-## Acceptance Criteria\n-- [ ] A plugin (HostInternal caller) can call the focus service to get the current scope chain, focus FQM, and geometry NavSnapshot for a window, pulled on demand from the webview via F1.\n-- [ ] A host-side navigation computed from a pulled snapshot returns the correct target (reuses `pick_target`), with no caller-supplied snapshot needed.\n-- [ ] No new continuous-sync path is introduced; geometry is pulled per request.\n+1. **`focused_fq` is a required wire field, not read from the kernel.** `crates/swissarmyhammer-focus/src/operations.rs::Navigate` requires `focused_fq: FullyQualifiedMoniker`; `DrillIn`/`DrillOut` require `fq` + `focused_fq`. `SpatialState` DOES hold a per-window `focus_by_window` map with `focused_in(window)` (state.rs:516), but the MCP op surface never consults it.\n\n-## Tests\n-- [ ] Rust integration test: a fake UI responder returns a known NavSnapshot/scope-chain/focus; assert the focus service query ops return them, and that a navigate over the pulled snapshot yields the expected target. Covers all three queries.\n-- [ ] Tests fail before the ops exist (RED), pass after.\n+2. **`snapshot: NavSnapshot` is live DOM geometry, only knowable in the webview.** `state.rs::navigate` / `focus` run `IndexedSnapshot::new(snapshot)` + `pick_target` over the rects of every sibling scope. The snapshot is built React-side by `spatial-focus-context.tsx::buildSnapshotForFocused` → `LayerScopeRegistry.buildSnapshot(layerFq)`, which calls `getBoundingClientRect()` on each mounted scope's DOM node AT CALL TIME. The Rust kernel has no DOM.\n\n-## Workflow\n-- Use `/tdd` — failing test first. Automated tests only.\n+So a plugin command's `execute` (host, not webview) cannot supply `focused_fq` + `snapshot`. (Finding #1 is fixed by the corrected scope above; finding #2 is WHY geometry stays on-demand in the webview.)\n\n-## Depends on\n-- Card F1 (host→UI request/reply channel).\n\\ No newline at end of file\n+#bug\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -4,36 +4,35 @@\n position_column: todo\n position_ordinal: df80\n project: ui-command-cleanup\n-title: Card A blocker — Make the focus kernel authoritative for current focus (drop focused_fq wire field); geometry STAYS computed on-demand\n+title: Card F2 — Extend the focus service with on-demand UI-geometry queries (scope chain, focus, geometry)\n ---\n-## ⛔ OWNER CONSTRAINT + DESIGN CORRECTION (2026-06-06, authoritative — supersedes the \"What this card must do\" section below)\n-Owner ruling: **geometry MUST be computed on demand. No caching, no partial/incremental build, no kernel-held geometry store, no push-on-mount/resize/scroll.** Direct quote: *\"every time we've tried anything other than compute the geometry on demand — any kind of caching or partial build, you have failed to do it correctly.\"* The per-keystroke `getBoundingClientRect()` on-demand snapshot build in the webview **STAYS**.\n-\n-This INVALIDATES the original proposal's central mechanism (a per-window kernel geometry store fed by React on mount/resize/scroll). Do NOT build that.\n-\n-### Corrected scope (do this)\n-1. **Focus identity → server-side.** The kernel already holds `focus_by_window: HashMap<WindowLabel, FullyQualifiedMoniker>` (`state.rs:198`). Change `navigate focus` / `drill_in layer` / `drill_out layer` to **drop the required `focused_fq` wire field** (`operations.rs:105/206/228`) and resolve current focus from `focus_by_window[window]` instead.\n-2. **Geometry → unchanged, on-demand.** `snapshot: NavSnapshot` remains a per-call input, freshly built in the webview at dispatch time from live DOM rects. The kernel does NOT hold or cache geometry.\n-3. **Op shape becomes `{ window, direction, snapshot }`** (snapshot still required & fresh; `focused_fq` removed). Same for drill in/out.\n-4. **nav.* are handler-bus plugin commands**, NOT pure-backend: their Card B (`01KTED5F…`) webview handler builds the on-demand snapshot and calls the op; the kernel supplies `focused_fq`. (\"Pure-backend `{window, direction}` only\" is impossible under the on-demand-geometry constraint and is explicitly dropped.)\n-\n-### Revised Acceptance Criteria\n-- [ ] `navigate focus` / `drill_in layer` / `drill_out layer` no longer require `focused_fq` on the wire — the kernel resolves current focus from `focus_by_window[window]`.\n-- [ ] `snapshot` is still supplied fresh per call, built on-demand from live DOM rects in the webview — NO kernel-held geometry, NO cache, NO push model.\n-- [ ] `nav.*` are plugin commands whose webview handler builds the on-demand snapshot and calls the op; Card A wires them via the Card B handler bus.\n-- [ ] Existing spatial-nav behavior (beam search, drill, focus-lost fallback) preserved — full spatial test suite green.\n+## What\n+Owner decision: there is a SERVICE about UI geometry — **scope chain, focus, and geometry** — and it lives by EXTENDING the existing focus service/kernel (`crates/swissarmyhammer-focus`), which already owns per-window focus and runs `pick_target` host-side over a `NavSnapshot`. Plugins query it ON DEMAND (not continuous sync) to get the current UI geometry, so geometry-dependent commands (nav.*, jump, …) can be real plugin commands triggered from ANY source (OS menu, keystroke, agent).\n\n----\n-## (ORIGINAL PROPOSAL — kept for context; its kernel-geometry-store mechanism is SUPERSEDED by the banner above)\n+Built on Card F1 (host→UI request/reply channel). Pull, per window, on demand from the webview:\n+- **scope chain** (the focused scope chain the UI currently sends to the backend),\n+- **focus** (the focused FullyQualifiedMoniker),\n+- **geometry** (the `NavSnapshot`: per-scope `getBoundingClientRect` rects), built by `apps/kanban-app/ui/src/lib/spatial-focus-context.tsx::buildSnapshotForFocused` / `layer-scope-registry-context.tsx::buildSnapshot`.\n\n-## Why (discovered while implementing Card A 01KTCQFH7AEQDZD0QETSMCMGP0)\n+### Work\n+- Add focus-service ops (`crates/swissarmyhammer-focus/src/{server.rs,operations.rs,state.rs}`) that, given a window, fetch the current scope chain / focus / snapshot by issuing an F1 `request_from_ui(window, \"focus.geometry\"/\"focus.scopeChain\"/\"focus.current\", …)` and returning the result. Expose them on the focus module so plugins call `this.focus.<op>()` (the `focus` module is already plugin-reachable via `command_services.rs::install_app_command_services`).\n+- UI responder: register (via F1 `registerUiResponder`) handlers in `spatial-focus-context.tsx` that answer these by building the snapshot / reading the current focus + scope chain.\n+- `pick_target` and focus mutation STAY host-side in the kernel (`state.rs::navigate`/`focus`) — they already consume a `NavSnapshot`. The new query ops just supply that snapshot on demand instead of requiring the caller (React) to pass it.\n\n-Card A asks the nine `nav.*` to become PLUGIN commands whose `execute` routes to the focus-server ops (`navigate focus` / `drill_in layer` / `drill_out layer` / `set focus`). The make-or-break verification came back **client-side**: the nav ops CANNOT be driven from a plugin command with just `(window, direction)`. Two hard dependencies on React state:\n+### Why this unblocks nav.* (Card A)\n+With on-demand geometry, a `nav.*` plugin command's `execute` can: pull the snapshot+focus from the focus service, run the existing `navigate`/`drill_in`/`drill_out` kernel logic, and apply focus — entirely host-side, no live React handle. nav becomes a real plugin command that works from the OS menu.\n\n-1. **`focused_fq` is a required wire field, not read from the kernel.** `crates/swissarmyhammer-focus/src/operations.rs::Navigate` requires `focused_fq: FullyQualifiedMoniker`; `DrillIn`/`DrillOut` require `fq` + `focused_fq`. `SpatialState` DOES hold a per-window `focus_by_window` map with `focused_in(window)` (state.rs:516), but the MCP op surface never consults it.\n+## Acceptance Criteria\n+- [ ] A plugin (HostInternal caller) can call the focus service to get the current scope chain, focus FQM, and geometry NavSnapshot for a window, pulled on demand from the webview via F1.\n+- [ ] A host-side navigation computed from a pulled snapshot returns the correct target (reuses `pick_target`), with no caller-supplied snapshot needed.\n+- [ ] No new continuous-sync path is introduced; geometry is pulled per request.\n\n-2. **`snapshot: NavSnapshot` is live DOM geometry, only knowable in the webview.** `state.rs::navigate` / `focus` run `IndexedSnapshot::new(snapshot)` + `pick_target` over the rects of every sibling scope. The snapshot is built React-side by `spatial-focus-context.tsx::buildSnapshotForFocused` → `LayerScopeRegistry.buildSnapshot(layerFq)`, which calls `getBoundingClientRect()` on each mounted scope's DOM node AT CALL TIME. The Rust kernel has no DOM.\n+## Tests\n+- [ ] Rust integration test: a fake UI responder returns a known NavSnapshot/scope-chain/focus; assert the focus service query ops return them, and that a navigate over the pulled snapshot yields the expected target. Covers all three queries.\n+- [ ] Tests fail before the ops exist (RED), pass after.\n\n-So a plugin command's `execute` (host, not webview) cannot supply `focused_fq` + `snapshot`. (Finding #1 is fixed by the corrected scope above; finding #2 is WHY geometry stays on-demand in the webview.)\n+## Workflow\n+- Use `/tdd` — failing test first. Automated tests only.\n\n-#bug\n\\ No newline at end of file\n+## Depends on\n+- Card F1 (host→UI request/reply channel).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTEG8BAP36TY99G1837JMV41","timestamp":"2026-06-06T12:59:17.206975Z","op":"update","item_id":"01KTEEDA9ZVTZ2R5CERW0WGK97","forward_patch":"--- original\n+++ modified\n@@ -4,36 +4,39 @@\n position_column: todo\n position_ordinal: df80\n project: ui-command-cleanup\n-title: Card A blocker — Make the focus kernel authoritative for current focus (drop focused_fq wire field); geometry STAYS computed on-demand\n+title: Card A blocker (≈ Card F2) — Kernel-authoritative focus + on-demand geometry via the F1 host→UI channel\n ---\n-## ⛔ OWNER CONSTRAINT + DESIGN CORRECTION (2026-06-06, authoritative — supersedes the \"What this card must do\" section below)\n-Owner ruling: **geometry MUST be computed on demand. No caching, no partial/incremental build, no kernel-held geometry store, no push-on-mount/resize/scroll.** Direct quote: *\"every time we've tried anything other than compute the geometry on demand — any kind of caching or partial build, you have failed to do it correctly.\"* The per-keystroke `getBoundingClientRect()` on-demand snapshot build in the webview **STAYS**.\n+## ⛔ OWNER CONSTRAINT (2026-06-06, authoritative)\n+**Geometry MUST be computed on demand. No caching, no partial/incremental build, no kernel-held geometry store, no push-on-mount/resize/scroll.** Owner quote: *\"every time we've tried anything other than compute the geometry on demand — any kind of caching or partial build, you have failed to do it correctly.\"* The `getBoundingClientRect()` on-demand snapshot build in the webview **STAYS**; only *who triggers it and when* changes.\n\n-This INVALIDATES the original proposal's central mechanism (a per-window kernel geometry store fed by React on mount/resize/scroll). Do NOT build that.\n+This INVALIDATES the original proposal's kernel-geometry-store mechanism (a per-window kernel store fed by React on mount/resize/scroll). Do NOT build that.\n+\n+## Design (re-aligned 2026-06-06 to the F1/F2 approach)\n+This card is the **\"Card F2 — focus geometry queries\"** that **Card F1 `01KTEFP1RMM2G65GR1PH9YQWEZ`** (host→UI request/reply channel) is built for. It depends on F1.\n\n-### Corrected scope (do this)\n-1. **Focus identity → server-side.** The kernel already holds `focus_by_window: HashMap<WindowLabel, FullyQualifiedMoniker>` (`state.rs:198`). Change `navigate focus` / `drill_in layer` / `drill_out layer` to **drop the required `focused_fq` wire field** (`operations.rs:105/206/228`) and resolve current focus from `focus_by_window[window]` instead.\n-2. **Geometry → unchanged, on-demand.** `snapshot: NavSnapshot` remains a per-call input, freshly built in the webview at dispatch time from live DOM rects. The kernel does NOT hold or cache geometry.\n-3. **Op shape becomes `{ window, direction, snapshot }`** (snapshot still required & fresh; `focused_fq` removed). Same for drill in/out.\n-4. **nav.* are handler-bus plugin commands**, NOT pure-backend: their Card B (`01KTED5F…`) webview handler builds the on-demand snapshot and calls the op; the kernel supplies `focused_fq`. (\"Pure-backend `{window, direction}` only\" is impossible under the on-demand-geometry constraint and is explicitly dropped.)\n+The reconciliation of \"nav.* server-side\" + \"geometry on-demand\": the nav command runs **host-side**, and obtains geometry by **requesting a fresh snapshot from the webview over the F1 channel and awaiting it** — never from a cache.\n+\n+1. **Focus identity → server-side.** The kernel already holds `focus_by_window: HashMap<WindowLabel, FullyQualifiedMoniker>` (`state.rs:198`). Change `navigate focus` / `drill_in layer` / `drill_out layer` to **drop the required `focused_fq` wire field** (`operations.rs:105/206/228`) and resolve current focus from `focus_by_window[window]`.\n+2. **Geometry → on-demand via F1, not cached, not webview-local.** When a nav op needs the `NavSnapshot`, the host issues an F1 `request_from_ui(window, \"focus.geometry\", {layer/focused})` and awaits the reply; the webview's F1 responder builds the snapshot ON DEMAND (`getBoundingClientRect()` at that instant via `LayerScopeRegistry.buildSnapshot`) and replies. The kernel runs `IndexedSnapshot::new(snapshot)` + `pick_target` on that fresh reply. Kernel holds NO geometry.\n+3. **nav.* are host-driven plugin commands** (NOT Card B webview handlers): their `execute` runs host-side, resolves `focused_fq` from the kernel, fetches the on-demand snapshot via F1, calls the op. Card A then wires the nav plugin `execute` straight to this path.\n+4. **Lock discipline (inherited from F1):** the host must DROP all `AppState`/spatial locks before awaiting the F1 reply, then re-acquire — see F1's deadlock rule.\n\n-### Revised Acceptance Criteria\n+## Acceptance Criteria\n - [ ] `navigate focus` / `drill_in layer` / `drill_out layer` no longer require `focused_fq` on the wire — the kernel resolves current focus from `focus_by_window[window]`.\n-- [ ] `snapshot` is still supplied fresh per call, built on-demand from live DOM rects in the webview — NO kernel-held geometry, NO cache, NO push model.\n-- [ ] `nav.*` are plugin commands whose webview handler builds the on-demand snapshot and calls the op; Card A wires them via the Card B handler bus.\n+- [ ] The nav op's `NavSnapshot` is obtained per call via the F1 host→UI request/reply channel, built on-demand in the webview (`getBoundingClientRect`) — NO kernel-held geometry, NO cache, NO push model, NO webview-local handler owning the nav logic.\n+- [ ] nav.* are host-driven plugin commands whose `execute` resolves focus from the kernel + fetches geometry via F1; Card A wires them to this path (pure-backend command identity, geometry round-trips to the UI on demand).\n+- [ ] No `AppState`/spatial lock is held across the F1 await (deadlock-safe).\n - [ ] Existing spatial-nav behavior (beam search, drill, focus-lost fallback) preserved — full spatial test suite green.\n\n+## Depends on\n+- `01KTEFP1RMM2G65GR1PH9YQWEZ` — Card F1 (host→UI request/reply channel). This card rides on it.\n+\n ---\n-## (ORIGINAL PROPOSAL — kept for context; its kernel-geometry-store mechanism is SUPERSEDED by the banner above)\n+## (ORIGINAL PROPOSAL — kept for context; its kernel-geometry-store mechanism is SUPERSEDED)\n\n ## Why (discovered while implementing Card A 01KTCQFH7AEQDZD0QETSMCMGP0)\n+Card A asks the nine `nav.*` to become PLUGIN commands whose `execute` routes to the focus-server ops. Verification came back **client-side**: the nav ops cannot be driven from a plugin command with just `(window, direction)` because —\n+1. **`focused_fq` is a required wire field, not read from the kernel** (`operations.rs::Navigate/DrillIn/DrillOut`), even though `SpatialState` holds `focus_by_window` (state.rs:516). → fixed by \"Focus identity → server-side\" above.\n+2. **`snapshot: NavSnapshot` is live DOM geometry** built React-side by `spatial-focus-context.tsx::buildSnapshotForFocused` → `LayerScopeRegistry.buildSnapshot` via `getBoundingClientRect()` at call time; the Rust kernel has no DOM. → this is WHY geometry stays on-demand and is fetched via the F1 channel rather than cached.\n\n-Card A asks the nine `nav.*` to become PLUGIN commands whose `execute` routes to the focus-server ops (`navigate focus` / `drill_in layer` / `drill_out layer` / `set focus`). The make-or-break verification came back **client-side**: the nav ops CANNOT be driven from a plugin command with just `(window, direction)`. Two hard dependencies on React state:\n-\n-1. **`focused_fq` is a required wire field, not read from the kernel.** `crates/swissarmyhammer-focus/src/operations.rs::Navigate` requires `focused_fq: FullyQualifiedMoniker`; `DrillIn`/`DrillOut` require `fq` + `focused_fq`. `SpatialState` DOES hold a per-window `focus_by_window` map with `focused_in(window)` (state.rs:516), but the MCP op surface never consults it.\n-\n-2. **`snapshot: NavSnapshot` is live DOM geometry, only knowable in the webview.** `state.rs::navigate` / `focus` run `IndexedSnapshot::new(snapshot)` + `pick_target` over the rects of every sibling scope. The snapshot is built React-side by `spatial-focus-context.tsx::buildSnapshotForFocused` → `LayerScopeRegistry.buildSnapshot(layerFq)`, which calls `getBoundingClientRect()` on each mounted scope's DOM node AT CALL TIME. The Rust kernel has no DOM.\n-\n-So a plugin command's `execute` (host, not webview) cannot supply `focused_fq` + `snapshot`. (Finding #1 is fixed by the corrected scope above; finding #2 is WHY geometry stays on-demand in the webview.)\n-\n #bug\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -4,39 +4,36 @@\n position_column: todo\n position_ordinal: df80\n project: ui-command-cleanup\n-title: Card A blocker (≈ Card F2) — Kernel-authoritative focus + on-demand geometry via the F1 host→UI channel\n+title: Card A blocker — Make the focus kernel authoritative for current focus (drop focused_fq wire field); geometry STAYS computed on-demand\n ---\n-## ⛔ OWNER CONSTRAINT (2026-06-06, authoritative)\n-**Geometry MUST be computed on demand. No caching, no partial/incremental build, no kernel-held geometry store, no push-on-mount/resize/scroll.** Owner quote: *\"every time we've tried anything other than compute the geometry on demand — any kind of caching or partial build, you have failed to do it correctly.\"* The `getBoundingClientRect()` on-demand snapshot build in the webview **STAYS**; only *who triggers it and when* changes.\n+## ⛔ OWNER CONSTRAINT + DESIGN CORRECTION (2026-06-06, authoritative — supersedes the \"What this card must do\" section below)\n+Owner ruling: **geometry MUST be computed on demand. No caching, no partial/incremental build, no kernel-held geometry store, no push-on-mount/resize/scroll.** Direct quote: *\"every time we've tried anything other than compute the geometry on demand — any kind of caching or partial build, you have failed to do it correctly.\"* The per-keystroke `getBoundingClientRect()` on-demand snapshot build in the webview **STAYS**.\n\n-This INVALIDATES the original proposal's kernel-geometry-store mechanism (a per-window kernel store fed by React on mount/resize/scroll). Do NOT build that.\n-\n-## Design (re-aligned 2026-06-06 to the F1/F2 approach)\n-This card is the **\"Card F2 — focus geometry queries\"** that **Card F1 `01KTEFP1RMM2G65GR1PH9YQWEZ`** (host→UI request/reply channel) is built for. It depends on F1.\n-\n-The reconciliation of \"nav.* server-side\" + \"geometry on-demand\": the nav command runs **host-side**, and obtains geometry by **requesting a fresh snapshot from the webview over the F1 channel and awaiting it** — never from a cache.\n+This INVALIDATES the original proposal's central mechanism (a per-window kernel geometry store fed by React on mount/resize/scroll). Do NOT build that.\n\n-1. **Focus identity → server-side.** The kernel already holds `focus_by_window: HashMap<WindowLabel, FullyQualifiedMoniker>` (`state.rs:198`). Change `navigate focus` / `drill_in layer` / `drill_out layer` to **drop the required `focused_fq` wire field** (`operations.rs:105/206/228`) and resolve current focus from `focus_by_window[window]`.\n-2. **Geometry → on-demand via F1, not cached, not webview-local.** When a nav op needs the `NavSnapshot`, the host issues an F1 `request_from_ui(window, \"focus.geometry\", {layer/focused})` and awaits the reply; the webview's F1 responder builds the snapshot ON DEMAND (`getBoundingClientRect()` at that instant via `LayerScopeRegistry.buildSnapshot`) and replies. The kernel runs `IndexedSnapshot::new(snapshot)` + `pick_target` on that fresh reply. Kernel holds NO geometry.\n-3. **nav.* are host-driven plugin commands** (NOT Card B webview handlers): their `execute` runs host-side, resolves `focused_fq` from the kernel, fetches the on-demand snapshot via F1, calls the op. Card A then wires the nav plugin `execute` straight to this path.\n-4. **Lock discipline (inherited from F1):** the host must DROP all `AppState`/spatial locks before awaiting the F1 reply, then re-acquire — see F1's deadlock rule.\n+### Corrected scope (do this)\n+1. **Focus identity → server-side.** The kernel already holds `focus_by_window: HashMap<WindowLabel, FullyQualifiedMoniker>` (`state.rs:198`). Change `navigate focus` / `drill_in layer` / `drill_out layer` to **drop the required `focused_fq` wire field** (`operations.rs:105/206/228`) and resolve current focus from `focus_by_window[window]` instead.\n+2. **Geometry → unchanged, on-demand.** `snapshot: NavSnapshot` remains a per-call input, freshly built in the webview at dispatch time from live DOM rects. The kernel does NOT hold or cache geometry.\n+3. **Op shape becomes `{ window, direction, snapshot }`** (snapshot still required & fresh; `focused_fq` removed). Same for drill in/out.\n+4. **nav.* are handler-bus plugin commands**, NOT pure-backend: their Card B (`01KTED5F…`) webview handler builds the on-demand snapshot and calls the op; the kernel supplies `focused_fq`. (\"Pure-backend `{window, direction}` only\" is impossible under the on-demand-geometry constraint and is explicitly dropped.)\n\n-## Acceptance Criteria\n+### Revised Acceptance Criteria\n - [ ] `navigate focus` / `drill_in layer` / `drill_out layer` no longer require `focused_fq` on the wire — the kernel resolves current focus from `focus_by_window[window]`.\n-- [ ] The nav op's `NavSnapshot` is obtained per call via the F1 host→UI request/reply channel, built on-demand in the webview (`getBoundingClientRect`) — NO kernel-held geometry, NO cache, NO push model, NO webview-local handler owning the nav logic.\n-- [ ] nav.* are host-driven plugin commands whose `execute` resolves focus from the kernel + fetches geometry via F1; Card A wires them to this path (pure-backend command identity, geometry round-trips to the UI on demand).\n-- [ ] No `AppState`/spatial lock is held across the F1 await (deadlock-safe).\n+- [ ] `snapshot` is still supplied fresh per call, built on-demand from live DOM rects in the webview — NO kernel-held geometry, NO cache, NO push model.\n+- [ ] `nav.*` are plugin commands whose webview handler builds the on-demand snapshot and calls the op; Card A wires them via the Card B handler bus.\n - [ ] Existing spatial-nav behavior (beam search, drill, focus-lost fallback) preserved — full spatial test suite green.\n\n-## Depends on\n-- `01KTEFP1RMM2G65GR1PH9YQWEZ` — Card F1 (host→UI request/reply channel). This card rides on it.\n-\n ---\n-## (ORIGINAL PROPOSAL — kept for context; its kernel-geometry-store mechanism is SUPERSEDED)\n+## (ORIGINAL PROPOSAL — kept for context; its kernel-geometry-store mechanism is SUPERSEDED by the banner above)\n\n ## Why (discovered while implementing Card A 01KTCQFH7AEQDZD0QETSMCMGP0)\n-Card A asks the nine `nav.*` to become PLUGIN commands whose `execute` routes to the focus-server ops. Verification came back **client-side**: the nav ops cannot be driven from a plugin command with just `(window, direction)` because —\n-1. **`focused_fq` is a required wire field, not read from the kernel** (`operations.rs::Navigate/DrillIn/DrillOut`), even though `SpatialState` holds `focus_by_window` (state.rs:516). → fixed by \"Focus identity → server-side\" above.\n-2. **`snapshot: NavSnapshot` is live DOM geometry** built React-side by `spatial-focus-context.tsx::buildSnapshotForFocused` → `LayerScopeRegistry.buildSnapshot` via `getBoundingClientRect()` at call time; the Rust kernel has no DOM. → this is WHY geometry stays on-demand and is fetched via the F1 channel rather than cached.\n\n+Card A asks the nine `nav.*` to become PLUGIN commands whose `execute` routes to the focus-server ops (`navigate focus` / `drill_in layer` / `drill_out layer` / `set focus`). The make-or-break verification came back **client-side**: the nav ops CANNOT be driven from a plugin command with just `(window, direction)`. Two hard dependencies on React state:\n+\n+1. **`focused_fq` is a required wire field, not read from the kernel.** `crates/swissarmyhammer-focus/src/operations.rs::Navigate` requires `focused_fq: FullyQualifiedMoniker`; `DrillIn`/`DrillOut` require `fq` + `focused_fq`. `SpatialState` DOES hold a per-window `focus_by_window` map with `focused_in(window)` (state.rs:516), but the MCP op surface never consults it.\n+\n+2. **`snapshot: NavSnapshot` is live DOM geometry, only knowable in the webview.** `state.rs::navigate` / `focus` run `IndexedSnapshot::new(snapshot)` + `pick_target` over the rects of every sibling scope. The snapshot is built React-side by `spatial-focus-context.tsx::buildSnapshotForFocused` → `LayerScopeRegistry.buildSnapshot(layerFq)`, which calls `getBoundingClientRect()` on each mounted scope's DOM node AT CALL TIME. The Rust kernel has no DOM.\n+\n+So a plugin command's `execute` (host, not webview) cannot supply `focused_fq` + `snapshot`. (Finding #1 is fixed by the corrected scope above; finding #2 is WHY geometry stays on-demand in the webview.)\n+\n #bug\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTEGFA02VFP8GWNCFRE8KSMD","timestamp":"2026-06-06T13:03:05.218808Z","op":"update","item_id":"01KTEEDA9ZVTZ2R5CERW0WGK97","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n depends_on:\n - 01KTEFP1RMM2G65GR1PH9YQWEZ\n-position_column: todo\n-position_ordinal: df80\n+position_column: doing\n+position_ordinal: '80'\n project: ui-command-cleanup\n title: Card A blocker (≈ Card F2) — Kernel-authoritative focus + on-demand geometry via the F1 host→UI channel\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n depends_on:\n - 01KTEFP1RMM2G65GR1PH9YQWEZ\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: df80\n project: ui-command-cleanup\n title: Card A blocker (≈ Card F2) — Kernel-authoritative focus + on-demand geometry via the F1 host→UI channel\n ---\n","transaction_id":null}
{"id":"01KTEHDTVYEY4TWYZY2W1RF81S","timestamp":"2026-06-06T13:19:45.534472Z","op":"update","item_id":"01KTEEDA9ZVTZ2R5CERW0WGK97","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n depends_on:\n - 01KTEFP1RMM2G65GR1PH9YQWEZ\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8480'\n project: ui-command-cleanup\n title: Card A blocker (≈ Card F2) — Kernel-authoritative focus + on-demand geometry via the F1 host→UI channel\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n depends_on:\n - 01KTEFP1RMM2G65GR1PH9YQWEZ\n-position_column: review\n-position_ordinal: '8480'\n+position_column: doing\n+position_ordinal: '80'\n project: ui-command-cleanup\n title: Card A blocker (≈ Card F2) — Kernel-authoritative focus + on-demand geometry via the F1 host→UI channel\n ---\n","transaction_id":null}
{"id":"01KTET1E868S2VY8YVDMNMG2N9","timestamp":"2026-06-06T15:50:16.582112Z","op":"update","item_id":"01KTEEDA9ZVTZ2R5CERW0WGK97","forward_patch":"--- original\n+++ modified\n@@ -11,26 +11,32 @@\n\n This INVALIDATES the original proposal's kernel-geometry-store mechanism (a per-window kernel store fed by React on mount/resize/scroll). Do NOT build that.\n\n+## ⚠️ CAUSES A NAV REGRESSION — see `01KTESYQ49JYJB2YT1WXYKK0W4`\n+The implementation of this card (host-driven nav, kernel-authoritative focus) **broke arrow-key navigation** in the working tree. AC #1 below (\"kernel resolves current focus from `focus_by_window`\") is the prime suspect: the frontend is the real source of truth for focus (`FRONTEND_AUTHORITATIVE_KINDS = { scope_chain }`), so `focus_by_window` goes stale (esp. on click-to-focus) and the nav op drops. The regression card proposes resolving `from` from the webview's `focus.current` (the provider already exposes it) instead of the kernel map. Reconcile that against AC #1 before marking this done. The geometry pull + lock discipline are correct and not implicated.\n+\n ## Design (re-aligned 2026-06-06 to the F1/F2 approach)\n This card is the **\"Card F2 — focus geometry queries\"** that **Card F1 `01KTEFP1RMM2G65GR1PH9YQWEZ`** (host→UI request/reply channel) is built for. It depends on F1.\n\n The reconciliation of \"nav.* server-side\" + \"geometry on-demand\": the nav command runs **host-side**, and obtains geometry by **requesting a fresh snapshot from the webview over the F1 channel and awaiting it** — never from a cache.\n\n-1. **Focus identity → server-side.** The kernel already holds `focus_by_window: HashMap<WindowLabel, FullyQualifiedMoniker>` (`state.rs:198`). Change `navigate focus` / `drill_in layer` / `drill_out layer` to **drop the required `focused_fq` wire field** (`operations.rs:105/206/228`) and resolve current focus from `focus_by_window[window]`.\n+1. **Focus identity → server-side.** The kernel already holds `focus_by_window: HashMap<WindowLabel, FullyQualifiedMoniker>` (`state.rs:198`). Change `navigate focus` / `drill_in layer` / `drill_out layer` to **drop the required `focused_fq` wire field** (`operations.rs:105/206/228`) and resolve current focus from `focus_by_window[window]`. **⚠️ See the regression note above — this exact choice is implicated; resolving from the webview's `focus.current` may be required instead.**\n 2. **Geometry → on-demand via F1, not cached, not webview-local.** When a nav op needs the `NavSnapshot`, the host issues an F1 `request_from_ui(window, \"focus.geometry\", {layer/focused})` and awaits the reply; the webview's F1 responder builds the snapshot ON DEMAND (`getBoundingClientRect()` at that instant via `LayerScopeRegistry.buildSnapshot`) and replies. The kernel runs `IndexedSnapshot::new(snapshot)` + `pick_target` on that fresh reply. Kernel holds NO geometry.\n 3. **nav.* are host-driven plugin commands** (NOT Card B webview handlers): their `execute` runs host-side, resolves `focused_fq` from the kernel, fetches the on-demand snapshot via F1, calls the op. Card A then wires the nav plugin `execute` straight to this path.\n 4. **Lock discipline (inherited from F1):** the host must DROP all `AppState`/spatial locks before awaiting the F1 reply, then re-acquire — see F1's deadlock rule.\n\n ## Acceptance Criteria\n-- [ ] `navigate focus` / `drill_in layer` / `drill_out layer` no longer require `focused_fq` on the wire — the kernel resolves current focus from `focus_by_window[window]`.\n+- [ ] `navigate focus` / `drill_in layer` / `drill_out layer` no longer require `focused_fq` on the wire — current focus resolved server-side. **(Reconcile kernel `focus_by_window` vs webview `focus.current` per the regression card before closing.)**\n - [ ] The nav op's `NavSnapshot` is obtained per call via the F1 host→UI request/reply channel, built on-demand in the webview (`getBoundingClientRect`) — NO kernel-held geometry, NO cache, NO push model, NO webview-local handler owning the nav logic.\n-- [ ] nav.* are host-driven plugin commands whose `execute` resolves focus from the kernel + fetches geometry via F1; Card A wires them to this path (pure-backend command identity, geometry round-trips to the UI on demand).\n+- [ ] nav.* are host-driven plugin commands whose `execute` resolves focus + fetches geometry via F1; Card A wires them to this path.\n - [ ] No `AppState`/spatial lock is held across the F1 await (deadlock-safe).\n-- [ ] Existing spatial-nav behavior (beam search, drill, focus-lost fallback) preserved — full spatial test suite green.\n+- [ ] Existing spatial-nav behavior (beam search, drill, focus-lost fallback) preserved — full spatial test suite green, AND a real-path (non-fake-provider) nav test passes (see regression card).\n\n ## Depends on\n - `01KTEFP1RMM2G65GR1PH9YQWEZ` — Card F1 (host→UI request/reply channel). This card rides on it.\n\n+## Quality gate\n+- `01KTESYQ49JYJB2YT1WXYKK0W4` — host-driven nav regression. This card is NOT done until that is resolved.\n+\n ---\n ## (ORIGINAL PROPOSAL — kept for context; its kernel-geometry-store mechanism is SUPERSEDED)\n\n","reverse_patch":"--- original\n+++ modified\n@@ -11,32 +11,26 @@\n\n This INVALIDATES the original proposal's kernel-geometry-store mechanism (a per-window kernel store fed by React on mount/resize/scroll). Do NOT build that.\n\n-## ⚠️ CAUSES A NAV REGRESSION — see `01KTESYQ49JYJB2YT1WXYKK0W4`\n-The implementation of this card (host-driven nav, kernel-authoritative focus) **broke arrow-key navigation** in the working tree. AC #1 below (\"kernel resolves current focus from `focus_by_window`\") is the prime suspect: the frontend is the real source of truth for focus (`FRONTEND_AUTHORITATIVE_KINDS = { scope_chain }`), so `focus_by_window` goes stale (esp. on click-to-focus) and the nav op drops. The regression card proposes resolving `from` from the webview's `focus.current` (the provider already exposes it) instead of the kernel map. Reconcile that against AC #1 before marking this done. The geometry pull + lock discipline are correct and not implicated.\n-\n ## Design (re-aligned 2026-06-06 to the F1/F2 approach)\n This card is the **\"Card F2 — focus geometry queries\"** that **Card F1 `01KTEFP1RMM2G65GR1PH9YQWEZ`** (host→UI request/reply channel) is built for. It depends on F1.\n\n The reconciliation of \"nav.* server-side\" + \"geometry on-demand\": the nav command runs **host-side**, and obtains geometry by **requesting a fresh snapshot from the webview over the F1 channel and awaiting it** — never from a cache.\n\n-1. **Focus identity → server-side.** The kernel already holds `focus_by_window: HashMap<WindowLabel, FullyQualifiedMoniker>` (`state.rs:198`). Change `navigate focus` / `drill_in layer` / `drill_out layer` to **drop the required `focused_fq` wire field** (`operations.rs:105/206/228`) and resolve current focus from `focus_by_window[window]`. **⚠️ See the regression note above — this exact choice is implicated; resolving from the webview's `focus.current` may be required instead.**\n+1. **Focus identity → server-side.** The kernel already holds `focus_by_window: HashMap<WindowLabel, FullyQualifiedMoniker>` (`state.rs:198`). Change `navigate focus` / `drill_in layer` / `drill_out layer` to **drop the required `focused_fq` wire field** (`operations.rs:105/206/228`) and resolve current focus from `focus_by_window[window]`.\n 2. **Geometry → on-demand via F1, not cached, not webview-local.** When a nav op needs the `NavSnapshot`, the host issues an F1 `request_from_ui(window, \"focus.geometry\", {layer/focused})` and awaits the reply; the webview's F1 responder builds the snapshot ON DEMAND (`getBoundingClientRect()` at that instant via `LayerScopeRegistry.buildSnapshot`) and replies. The kernel runs `IndexedSnapshot::new(snapshot)` + `pick_target` on that fresh reply. Kernel holds NO geometry.\n 3. **nav.* are host-driven plugin commands** (NOT Card B webview handlers): their `execute` runs host-side, resolves `focused_fq` from the kernel, fetches the on-demand snapshot via F1, calls the op. Card A then wires the nav plugin `execute` straight to this path.\n 4. **Lock discipline (inherited from F1):** the host must DROP all `AppState`/spatial locks before awaiting the F1 reply, then re-acquire — see F1's deadlock rule.\n\n ## Acceptance Criteria\n-- [ ] `navigate focus` / `drill_in layer` / `drill_out layer` no longer require `focused_fq` on the wire — current focus resolved server-side. **(Reconcile kernel `focus_by_window` vs webview `focus.current` per the regression card before closing.)**\n+- [ ] `navigate focus` / `drill_in layer` / `drill_out layer` no longer require `focused_fq` on the wire — the kernel resolves current focus from `focus_by_window[window]`.\n - [ ] The nav op's `NavSnapshot` is obtained per call via the F1 host→UI request/reply channel, built on-demand in the webview (`getBoundingClientRect`) — NO kernel-held geometry, NO cache, NO push model, NO webview-local handler owning the nav logic.\n-- [ ] nav.* are host-driven plugin commands whose `execute` resolves focus + fetches geometry via F1; Card A wires them to this path.\n+- [ ] nav.* are host-driven plugin commands whose `execute` resolves focus from the kernel + fetches geometry via F1; Card A wires them to this path (pure-backend command identity, geometry round-trips to the UI on demand).\n - [ ] No `AppState`/spatial lock is held across the F1 await (deadlock-safe).\n-- [ ] Existing spatial-nav behavior (beam search, drill, focus-lost fallback) preserved — full spatial test suite green, AND a real-path (non-fake-provider) nav test passes (see regression card).\n+- [ ] Existing spatial-nav behavior (beam search, drill, focus-lost fallback) preserved — full spatial test suite green.\n\n ## Depends on\n - `01KTEFP1RMM2G65GR1PH9YQWEZ` — Card F1 (host→UI request/reply channel). This card rides on it.\n\n-## Quality gate\n-- `01KTESYQ49JYJB2YT1WXYKK0W4` — host-driven nav regression. This card is NOT done until that is resolved.\n-\n ---\n ## (ORIGINAL PROPOSAL — kept for context; its kernel-geometry-store mechanism is SUPERSEDED)\n\n","transaction_id":null}

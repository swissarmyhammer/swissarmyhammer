{"id":"01KZNNEGXR4JW0QPDY23JX1X76","timestamp":"2026-08-10T11:03:12.568118Z","op":"create","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,18 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: ffb580\n+title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n+---\n+`builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n+\n+`function-length.md` exempts \"Functions that are mostly configuration/data (e.g., builder patterns with many options)\" and \"Initialization functions that set many fields\". `clippy::too_many_lines` counts code lines, so a 300-line `Config::default()` or a long builder chain reports. No clippy option expresses \"these lines are data\".\n+\n+Both prompt rules also exempt tests. `--all-targets` compiles the test targets on purpose, so test code is in scope.\n+\n+The generated-code carve-out is partial. `select(.file | startswith(\"/\") | not)` drops cargo's `OUT_DIR` output, but a checked-in generated `.rs` file inside the workspace has a relative path and is still reported.\n+\n+The rule uses `-W` rather than `--force-warn`, so `#[allow(clippy::too_many_lines)]` still works. An annotation contract may be the answer here, the way the dead-code rules use the language's own suppression marker. Decide, and state the decision on the rule.\n+\n+Found by the `supersedes` survey on ^h7garpc. #tool-validators #objectivity\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,18 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: ffb580\n-title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n----\n-`builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n-\n-`function-length.md` exempts \"Functions that are mostly configuration/data (e.g., builder patterns with many options)\" and \"Initialization functions that set many fields\". `clippy::too_many_lines` counts code lines, so a 300-line `Config::default()` or a long builder chain reports. No clippy option expresses \"these lines are data\".\n-\n-Both prompt rules also exempt tests. `--all-targets` compiles the test targets on purpose, so test code is in scope.\n-\n-The generated-code carve-out is partial. `select(.file | startswith(\"/\") | not)` drops cargo's `OUT_DIR` output, but a checked-in generated `.rs` file inside the workspace has a relative path and is still reported.\n-\n-The rule uses `-W` rather than `--force-warn`, so `#[allow(clippy::too_many_lines)]` still works. An annotation contract may be the answer here, the way the dead-code rules use the language's own suppression marker. Decide, and state the decision on the rule.\n-\n-Found by the `supersedes` survey on ^h7garpc. #tool-validators #objectivity\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KZSGG5V7GJNYXJWA22DAG4XZ","timestamp":"2026-08-11T22:53:41.607153Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: ffb580\n+position_column: doing\n+position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: todo\n+position_ordinal: ffb580\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","transaction_id":null}
{"id":"01KZSH4XBWQ1F08Y6C5AKH7D6Q","timestamp":"2026-08-11T23:05:01.052484Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,26 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01kzsh4xbwn8sybx01pv8a5zr4\n+  text: |-\n+    Measured every claim on the card with cargo clippy 0.1.97 / cargo 1.97.1, over probe crates at the rule's own thresholds (`excessive-nesting-threshold = 6`, `too-many-lines-threshold = 250`, `too-many-arguments-threshold = 7`, `type-complexity-threshold = 250`).\n+\n+    The card is CORRECT on each claim it makes:\n+\n+    - A 300-field `Config::default()` reports `too_many_lines` (302/250). A 300-call builder chain, one call for each line, reports (302/250). A 300-entry `vec![]` reports (302/250). Clippy counts a data line like a code line.\n+    - Clippy skips a blank line and a comment-only line: a function of 300 blank lines and 300 comment lines with 3 code lines reports nothing.\n+    - `--all-targets` puts test code in scope. Over one probe package: with the flag the run reports `src/lib.rs` (the lib function), `src/lib.rs` (the `#[cfg(test)] mod tests` function), `tests/it.rs` and `examples/ex.rs`; without the flag it reports the lib function alone.\n+    - `select(.file | startswith(\"/\") | not)` drops the `OUT_DIR` file and keeps a checked-in generated `.rs` file. Measured over one probe: the `OUT_DIR` file arrives as an absolute path; `src/generated.rs`, whose head carries `// This file is @generated by prost-build.` and `// Code generated by tool. DO NOT EDIT.`, arrives as a relative path and reports.\n+    - `-W` leaves `#[allow]` standing. `#[allow(clippy::too_many_lines)]`, `#[allow(clippy::excessive_nesting)]`, `#[allow(clippy::too_many_arguments)]` and `#[allow(clippy::type_complexity)]` each silence their lint on the function they stand on.\n+\n+    Two facts the card does not state, and one defect it does not name:\n+\n+    - The clippy configuration holds no key for a data line and no key for a test. The full key list clippy prints for an unknown key holds `excessive-nesting-threshold`, `too-many-lines-threshold`, `too-many-arguments-threshold` and `type-complexity-threshold` for these four lints, and nothing else. It holds `allow-unwrap-in-tests`, `allow-panic-in-tests` and six more `*-in-tests` keys, none of them for these four lints.\n+    - `#[allow(clippy::too_many_lines)]` on the `mod` DECLARATION of a checked-in generated file silences the whole file, and the declaration stands in the parent file, which the generator never writes again. `#[expect(...)]` in the same place expires: measured over a module whose file no longer trips, rustc raises `unfulfilled_lint_expectations`. So Rust answers the generated-code carve-out, which `complexity-python` could not.\n+    - DEFECT: the shipped `run` is a pipe that ends in `sort -u`, so a workspace cargo cannot compile reports 0 findings at exit 0 and the engine reads a dirty tree as clean. Measured over one package holding a 300-line function beside `pub fn broken() -> i32 { \"not an integer\" }`: the shipped script wrote 0 lines to stdout and exited 0.\n+  timestamp: 2026-08-11T23:05:01.052252+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -1,26 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01kzsh4xbwn8sybx01pv8a5zr4\n-  text: |-\n-    Measured every claim on the card with cargo clippy 0.1.97 / cargo 1.97.1, over probe crates at the rule's own thresholds (`excessive-nesting-threshold = 6`, `too-many-lines-threshold = 250`, `too-many-arguments-threshold = 7`, `type-complexity-threshold = 250`).\n-\n-    The card is CORRECT on each claim it makes:\n-\n-    - A 300-field `Config::default()` reports `too_many_lines` (302/250). A 300-call builder chain, one call for each line, reports (302/250). A 300-entry `vec![]` reports (302/250). Clippy counts a data line like a code line.\n-    - Clippy skips a blank line and a comment-only line: a function of 300 blank lines and 300 comment lines with 3 code lines reports nothing.\n-    - `--all-targets` puts test code in scope. Over one probe package: with the flag the run reports `src/lib.rs` (the lib function), `src/lib.rs` (the `#[cfg(test)] mod tests` function), `tests/it.rs` and `examples/ex.rs`; without the flag it reports the lib function alone.\n-    - `select(.file | startswith(\"/\") | not)` drops the `OUT_DIR` file and keeps a checked-in generated `.rs` file. Measured over one probe: the `OUT_DIR` file arrives as an absolute path; `src/generated.rs`, whose head carries `// This file is @generated by prost-build.` and `// Code generated by tool. DO NOT EDIT.`, arrives as a relative path and reports.\n-    - `-W` leaves `#[allow]` standing. `#[allow(clippy::too_many_lines)]`, `#[allow(clippy::excessive_nesting)]`, `#[allow(clippy::too_many_arguments)]` and `#[allow(clippy::type_complexity)]` each silence their lint on the function they stand on.\n-\n-    Two facts the card does not state, and one defect it does not name:\n-\n-    - The clippy configuration holds no key for a data line and no key for a test. The full key list clippy prints for an unknown key holds `excessive-nesting-threshold`, `too-many-lines-threshold`, `too-many-arguments-threshold` and `type-complexity-threshold` for these four lints, and nothing else. It holds `allow-unwrap-in-tests`, `allow-panic-in-tests` and six more `*-in-tests` keys, none of them for these four lints.\n-    - `#[allow(clippy::too_many_lines)]` on the `mod` DECLARATION of a checked-in generated file silences the whole file, and the declaration stands in the parent file, which the generator never writes again. `#[expect(...)]` in the same place expires: measured over a module whose file no longer trips, rustc raises `unfulfilled_lint_expectations`. So Rust answers the generated-code carve-out, which `complexity-python` could not.\n-    - DEFECT: the shipped `run` is a pipe that ends in `sort -u`, so a workspace cargo cannot compile reports 0 findings at exit 0 and the engine reads a dirty tree as clean. Measured over one package holding a 300-line function beside `pub fn broken() -> i32 { \"not an integer\" }`: the shipped script wrote 0 lines to stdout and exited 0.\n-  timestamp: 2026-08-11T23:05:01.052252+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSMN637GE7DAW8RSV7Z4BS5","timestamp":"2026-08-12T00:06:20.007487Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -21,6 +21,30 @@\n     - `#[allow(clippy::too_many_lines)]` on the `mod` DECLARATION of a checked-in generated file silences the whole file, and the declaration stands in the parent file, which the generator never writes again. `#[expect(...)]` in the same place expires: measured over a module whose file no longer trips, rustc raises `unfulfilled_lint_expectations`. So Rust answers the generated-code carve-out, which `complexity-python` could not.\n     - DEFECT: the shipped `run` is a pipe that ends in `sort -u`, so a workspace cargo cannot compile reports 0 findings at exit 0 and the engine reads a dirty tree as clean. Measured over one package holding a 300-line function beside `pub fn broken() -> i32 { \"not an integer\" }`: the shipped script wrote 0 lines to stdout and exited 0.\n   timestamp: 2026-08-11T23:05:01.052252+00:00\n+- actor: claude-code\n+  id: 01kzsmn637cwzwergy8q029jwy\n+  text: |-\n+    The decision, one line for each carve-out of the two superseded prompt rules. The rule body states each one with its measurement.\n+\n+    `function-length` states four carve-outs:\n+\n+    1. **A test.** NOT reproduced. No clippy flag and no clippy key reads `#[test]`. `--all-targets` is what puts the test target in front of the gates; dropping it reads the TARGET, which drops a long helper beside the test, drops an example and a benchmark, and drops every `#[cfg(test)]` module. The author answers with the annotation, and one annotation on `mod tests` covers the module.\n+    2. **Generated code.** Reproduced for the `OUT_DIR` shape alone; the absolute path filter drops it. A checked-in generated file reports, and the author answers at its `mod` declaration in the parent file. A macro expansion reports, and the author answers inside the macro body.\n+    3. **A function that is mostly configuration or data, such as a builder with many options.** NOT reproduced. The author answers with the annotation.\n+    4. **An initialization function that sets many fields.** NOT reproduced, and the same as 3. A `Config::default()` of 300 fields measures 302/250.\n+\n+    `cognitive-complexity` states three carve-outs:\n+\n+    1. **A test.** The same as above.\n+    2. **Generated code and a macro expansion.** The nesting gate REPRODUCES the macro half: `excessive_nesting` never reports an expansion. Measured over the same six nested blocks written plain and expanded from a `macro_rules!` body: the plain function reported, the expansion did not. The length gate does report an expansion, at the macro DEFINITION line. Generated code is as above.\n+    3. **Configuration parsing with many options, where the score comes from a long flat list of simple cases.** REPRODUCED by the nesting gate, which reads depth alone. Measured over a 301-branch `if` / `else if` chain at depth 1: `excessive_nesting` reported nothing. The length gate still counts the chain at 302/250, and carve-out 3 of `function-length` answers that.\n+\n+    The annotation contract the rule now states, measured with clippy 0.1.97. Each of these gives no finding: `#[allow(clippy::too_many_lines)]` on the function; the same with `reason = \"...\"`; `#[expect(clippy::too_many_lines, reason = \"...\")]` on the function; the same on the `impl` block; the same on the `mod`, inline or as an out-of-line declaration; `#![allow(clippy::too_many_lines)]` as the FIRST line of the body; `#[allow(clippy::excessive_nesting)]` on the function and on the one statement the finding stands on; `#[allow(clippy::too_many_arguments)]` and `#[allow(clippy::type_complexity)]` on the function. Each of these gives one finding: a comment of any shape; `#[allow(clippy::too_many_lines)]` on the `macro_rules!` item; the same on the macro invocation, which also raises `unused_attributes`. `#[allow(too_many_lines)]` with no `clippy::` silences the finding and raises `renamed_and_removed_lints`. `#![allow(clippy::too_many_lines)]` below the first line of a body is the compile error `an inner attribute is not permitted in this context`.\n+\n+    `#[expect]` is the marker the rule names, because it expires. Measured: the annotation over a function that no longer trips raises `unfulfilled_lint_expectations`.\n+\n+    Nothing on the card is wrong. Each claim it makes measured true, and the card needs no correction.\n+  timestamp: 2026-08-12T00:06:20.007188+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -21,30 +21,6 @@\n     - `#[allow(clippy::too_many_lines)]` on the `mod` DECLARATION of a checked-in generated file silences the whole file, and the declaration stands in the parent file, which the generator never writes again. `#[expect(...)]` in the same place expires: measured over a module whose file no longer trips, rustc raises `unfulfilled_lint_expectations`. So Rust answers the generated-code carve-out, which `complexity-python` could not.\n     - DEFECT: the shipped `run` is a pipe that ends in `sort -u`, so a workspace cargo cannot compile reports 0 findings at exit 0 and the engine reads a dirty tree as clean. Measured over one package holding a 300-line function beside `pub fn broken() -> i32 { \"not an integer\" }`: the shipped script wrote 0 lines to stdout and exited 0.\n   timestamp: 2026-08-11T23:05:01.052252+00:00\n-- actor: claude-code\n-  id: 01kzsmn637cwzwergy8q029jwy\n-  text: |-\n-    The decision, one line for each carve-out of the two superseded prompt rules. The rule body states each one with its measurement.\n-\n-    `function-length` states four carve-outs:\n-\n-    1. **A test.** NOT reproduced. No clippy flag and no clippy key reads `#[test]`. `--all-targets` is what puts the test target in front of the gates; dropping it reads the TARGET, which drops a long helper beside the test, drops an example and a benchmark, and drops every `#[cfg(test)]` module. The author answers with the annotation, and one annotation on `mod tests` covers the module.\n-    2. **Generated code.** Reproduced for the `OUT_DIR` shape alone; the absolute path filter drops it. A checked-in generated file reports, and the author answers at its `mod` declaration in the parent file. A macro expansion reports, and the author answers inside the macro body.\n-    3. **A function that is mostly configuration or data, such as a builder with many options.** NOT reproduced. The author answers with the annotation.\n-    4. **An initialization function that sets many fields.** NOT reproduced, and the same as 3. A `Config::default()` of 300 fields measures 302/250.\n-\n-    `cognitive-complexity` states three carve-outs:\n-\n-    1. **A test.** The same as above.\n-    2. **Generated code and a macro expansion.** The nesting gate REPRODUCES the macro half: `excessive_nesting` never reports an expansion. Measured over the same six nested blocks written plain and expanded from a `macro_rules!` body: the plain function reported, the expansion did not. The length gate does report an expansion, at the macro DEFINITION line. Generated code is as above.\n-    3. **Configuration parsing with many options, where the score comes from a long flat list of simple cases.** REPRODUCED by the nesting gate, which reads depth alone. Measured over a 301-branch `if` / `else if` chain at depth 1: `excessive_nesting` reported nothing. The length gate still counts the chain at 302/250, and carve-out 3 of `function-length` answers that.\n-\n-    The annotation contract the rule now states, measured with clippy 0.1.97. Each of these gives no finding: `#[allow(clippy::too_many_lines)]` on the function; the same with `reason = \"...\"`; `#[expect(clippy::too_many_lines, reason = \"...\")]` on the function; the same on the `impl` block; the same on the `mod`, inline or as an out-of-line declaration; `#![allow(clippy::too_many_lines)]` as the FIRST line of the body; `#[allow(clippy::excessive_nesting)]` on the function and on the one statement the finding stands on; `#[allow(clippy::too_many_arguments)]` and `#[allow(clippy::type_complexity)]` on the function. Each of these gives one finding: a comment of any shape; `#[allow(clippy::too_many_lines)]` on the `macro_rules!` item; the same on the macro invocation, which also raises `unused_attributes`. `#[allow(too_many_lines)]` with no `clippy::` silences the finding and raises `renamed_and_removed_lints`. `#![allow(clippy::too_many_lines)]` below the first line of a body is the compile error `an inner attribute is not permitted in this context`.\n-\n-    `#[expect]` is the marker the rule names, because it expires. Measured: the annotation over a function that no longer trips raises `unfulfilled_lint_expectations`.\n-\n-    Nothing on the card is wrong. Each claim it makes measured true, and the card needs no correction.\n-  timestamp: 2026-08-12T00:06:20.007188+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSMNM2KDFVT016MXYB6R79X","timestamp":"2026-08-12T00:06:34.323852Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -45,6 +45,29 @@\n\n     Nothing on the card is wrong. Each claim it makes measured true, and the card needs no correction.\n   timestamp: 2026-08-12T00:06:20.007188+00:00\n+- actor: claude-code\n+  id: 01kzsmnm2kawvcgcjmz3sb1arj\n+  text: |-\n+    The RED-to-GREEN proof of each of the four new acceptance tests. Each RED came from a mutation of the SHIPPED rule bytes, with `touch crates/swissarmyhammer-validators/src/builtin/mod.rs` before each run.\n+\n+    1. `the_shipped_rust_complexity_tool_rule_breaks_on_a_workspace_it_cannot_compile`.\n+       RED: the earlier pipe shape of the `run` script, which ends in `sort -u`. The test failed with `the run must report exactly one tool error; got []` — 0 tool errors against 1. Shell measurement of the same package: the pipe wrote 0 lines to stdout and exited 0, and cargo wrote `error: could not compile probe9 (lib)` to stderr.\n+       GREEN: the shipped script writes the report to a file, tests the status, and exits 1. The same package gives 0 findings, exit 1, and stderr holding `complexity-rust: cargo clippy could not lint the workspace`. The healthy package of the same probe set gives the same 4 findings and exit 0 under both shapes, so the change moves nothing but the broken run.\n+\n+    2. `the_shipped_rust_complexity_tool_rule_answers_the_length_gate_annotation`.\n+       RED: `-W` changed to `--force-warn` for the four lints. The test failed with `left: [\"src/lib.rs:1\", \"src/lib.rs:304\"]`, `right: [\"src/lib.rs:1\"]` — row 304 is the annotated function, which is the row the test's own doc comment predicts.\n+       GREEN: `[\"src/lib.rs:1\"]`.\n+\n+    3. `the_shipped_rust_complexity_tool_rule_reports_a_generated_file`.\n+       RED: the same `--force-warn` mutation. The test failed with `left: [\"src/annotated.rs:3\", \"src/bare.rs:3\"]`, `right: [\"src/bare.rs:3\"]`.\n+       GREEN: `[\"src/bare.rs:3\"]`. The two module files hold the same bytes, so the annotation on the `mod` declaration is the one difference.\n+\n+    4. `the_shipped_rust_complexity_tool_rule_reports_a_test_function_and_its_helper`.\n+       RED: `--all-targets` removed from the cargo command. The test failed with `left: []`, `right: [\"tests/it.rs:2\", \"tests/it.rs:304\"]`.\n+       GREEN: both rows.\n+\n+    The `doctor.check_command` now names `sort`, which the script runs. The earlier value named `cargo-clippy jq mktemp` alone. `missing-docs-rust` already names `sort` for the same pipe step.\n+  timestamp: 2026-08-12T00:06:34.323517+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -45,29 +45,6 @@\n\n     Nothing on the card is wrong. Each claim it makes measured true, and the card needs no correction.\n   timestamp: 2026-08-12T00:06:20.007188+00:00\n-- actor: claude-code\n-  id: 01kzsmnm2kawvcgcjmz3sb1arj\n-  text: |-\n-    The RED-to-GREEN proof of each of the four new acceptance tests. Each RED came from a mutation of the SHIPPED rule bytes, with `touch crates/swissarmyhammer-validators/src/builtin/mod.rs` before each run.\n-\n-    1. `the_shipped_rust_complexity_tool_rule_breaks_on_a_workspace_it_cannot_compile`.\n-       RED: the earlier pipe shape of the `run` script, which ends in `sort -u`. The test failed with `the run must report exactly one tool error; got []` — 0 tool errors against 1. Shell measurement of the same package: the pipe wrote 0 lines to stdout and exited 0, and cargo wrote `error: could not compile probe9 (lib)` to stderr.\n-       GREEN: the shipped script writes the report to a file, tests the status, and exits 1. The same package gives 0 findings, exit 1, and stderr holding `complexity-rust: cargo clippy could not lint the workspace`. The healthy package of the same probe set gives the same 4 findings and exit 0 under both shapes, so the change moves nothing but the broken run.\n-\n-    2. `the_shipped_rust_complexity_tool_rule_answers_the_length_gate_annotation`.\n-       RED: `-W` changed to `--force-warn` for the four lints. The test failed with `left: [\"src/lib.rs:1\", \"src/lib.rs:304\"]`, `right: [\"src/lib.rs:1\"]` — row 304 is the annotated function, which is the row the test's own doc comment predicts.\n-       GREEN: `[\"src/lib.rs:1\"]`.\n-\n-    3. `the_shipped_rust_complexity_tool_rule_reports_a_generated_file`.\n-       RED: the same `--force-warn` mutation. The test failed with `left: [\"src/annotated.rs:3\", \"src/bare.rs:3\"]`, `right: [\"src/bare.rs:3\"]`.\n-       GREEN: `[\"src/bare.rs:3\"]`. The two module files hold the same bytes, so the annotation on the `mod` declaration is the one difference.\n-\n-    4. `the_shipped_rust_complexity_tool_rule_reports_a_test_function_and_its_helper`.\n-       RED: `--all-targets` removed from the cargo command. The test failed with `left: []`, `right: [\"tests/it.rs:2\", \"tests/it.rs:304\"]`.\n-       GREEN: both rows.\n-\n-    The `doctor.check_command` now names `sort`, which the script runs. The earlier value named `cargo-clippy jq mktemp` alone. `missing-docs-rust` already names `sort` for the same pipe step.\n-  timestamp: 2026-08-12T00:06:34.323517+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSMNWSTF074VSY1566ESB1C","timestamp":"2026-08-12T00:06:43.258880Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -68,6 +68,14 @@\n\n     The `doctor.check_command` now names `sort`, which the script runs. The earlier value named `cargo-clippy jq mktemp` alone. `missing-docs-rust` already names `sort` for the same pipe step.\n   timestamp: 2026-08-12T00:06:34.323517+00:00\n+- actor: claude-code\n+  id: 01kzsmnwstkb151zwa9b4byzdr\n+  text: |-\n+    ### implement — changed\n+    - evidence: 2 files, 503 insertions, 22 deletions. `builtin/validators/code-hygiene/rules/complexity-rust.md` — the `run` script writes the clippy report to a file, tests the status and exits 1 with a named line for a broken run; `doctor.check_command` names `sort`; the body adds \"A workspace the tool cannot lint\", \"The annotation an author writes\" and \"The carve-outs the two prompt rules state\" with the measurement of each. `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs` — four acceptance tests that drive the shipped bytes, each with the RED-to-GREEN proof in the comment above.\n+    - tests: `cargo nextest run -p swissarmyhammer-validators` 656 run, 656 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3419 run, 3415 passed, 4 failed — the three `review_e2e` tests missing the duplication claim and `review_progress_is_received_by_a_real_client_over_a_byte_stream_transport`, all four already failing in this tree and not from this change. `cargo fmt --all` made no change. `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` exit 0.\n+    - next: /review\n+  timestamp: 2026-08-12T00:06:43.258530+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -68,14 +68,6 @@\n\n     The `doctor.check_command` now names `sort`, which the script runs. The earlier value named `cargo-clippy jq mktemp` alone. `missing-docs-rust` already names `sort` for the same pipe step.\n   timestamp: 2026-08-12T00:06:34.323517+00:00\n-- actor: claude-code\n-  id: 01kzsmnwstkb151zwa9b4byzdr\n-  text: |-\n-    ### implement — changed\n-    - evidence: 2 files, 503 insertions, 22 deletions. `builtin/validators/code-hygiene/rules/complexity-rust.md` — the `run` script writes the clippy report to a file, tests the status and exits 1 with a named line for a broken run; `doctor.check_command` names `sort`; the body adds \"A workspace the tool cannot lint\", \"The annotation an author writes\" and \"The carve-outs the two prompt rules state\" with the measurement of each. `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs` — four acceptance tests that drive the shipped bytes, each with the RED-to-GREEN proof in the comment above.\n-    - tests: `cargo nextest run -p swissarmyhammer-validators` 656 run, 656 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3419 run, 3415 passed, 4 failed — the three `review_e2e` tests missing the duplication claim and `review_progress_is_received_by_a_real_client_over_a_byte_stream_transport`, all four already failing in this tree and not from this change. `cargo fmt --all` made no change. `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` exit 0.\n-    - next: /review\n-  timestamp: 2026-08-12T00:06:43.258530+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSNMJHBP4KBEZ4KEZ6AJQVK","timestamp":"2026-08-12T00:23:28.555198Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -76,8 +76,8 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 656 run, 656 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3419 run, 3415 passed, 4 failed — the three `review_e2e` tests missing the duplication claim and `review_progress_is_received_by_a_real_client_over_a_byte_stream_transport`, all four already failing in this tree and not from this change. `cargo fmt --all` made no change. `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` exit 0.\n     - next: /review\n   timestamp: 2026-08-12T00:06:43.258530+00:00\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: review\n+position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","reverse_patch":"--- original\n+++ modified\n@@ -76,8 +76,8 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 656 run, 656 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3419 run, 3415 passed, 4 failed — the three `review_e2e` tests missing the duplication claim and `review_progress_is_received_by_a_real_client_over_a_byte_stream_transport`, all four already failing in this tree and not from this change. `cargo fmt --all` made no change. `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` exit 0.\n     - next: /review\n   timestamp: 2026-08-12T00:06:43.258530+00:00\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","transaction_id":null}
{"id":"01KZSNN8B7Z9XBXWHGNRR5NDDY","timestamp":"2026-08-12T00:23:50.887728Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -90,4 +90,11 @@\n\n The rule uses `-W` rather than `--force-warn`, so `#[allow(clippy::too_many_lines)]` still works. An annotation contract may be the answer here, the way the dead-code rules use the language's own suppression marker. Decide, and state the decision on the rule.\n\n-Found by the `supersedes` survey on ^h7garpc. #tool-validators #objectivity\n\\ No newline at end of file\n+Found by the `supersedes` survey on ^h7garpc. #tool-validators #objectivity\n+\n+## Review Findings (2026-08-11 19:35)\n+\n+- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:24` — The status gate breaks a run that measured the four gates correctly. `cargo clippy` exits nonzero for two different reasons: the tool could not lint the workspace, and the tool linted the workspace correctly while a lint stands at deny level. The gate reads the status alone, so it cannot tell the two apart. Measured with clippy 0.1.97 at the rule's own thresholds, over one package that holds a function of 301 lines: (a) `#![deny(clippy::unwrap_used)]` beside one `unwrap` gives exit 1, and the report file holds `warning clippy::too_many_lines src/lib.rs:1 this function has too many lines (301/250)`; (b) `[lints.clippy] unwrap_used = \"deny\"` in `Cargo.toml` gives exit 1 with the same report; (c) `RUSTFLAGS=\"-D warnings\"` over one ordinary `unused_variables` warning gives exit 1, and the report file holds `error clippy::too_many_lines src/lib.rs:1 (301/250)`; (d) the same package with no deny gives exit 0 and that one finding. In each of (a), (b) and (c) the script writes 0 findings and the stderr line `complexity-rust: cargo clippy could not lint the workspace`, so the engine loses a finding the report file holds. The gate answers correctly for a run the tool could not make: a package that does not compile, a directory with no `Cargo.toml`, a `Cargo.toml` that does not parse, and a target directory the tool cannot write each give exit 1 with an empty report. `builtin/validators/README.md` states the answer for this shape: \"One status can carry both a measured run and a broken run. The status of a failure is then the same as the status of a finding. The script must then test the REPORT beside the status, and accept the shared status only for the report shape a measured run writes.\" The three shipped swiftlint rules already do this. Make the script test the report beside the status. Correct the section \"A workspace the tool cannot lint\" with it: that section states two shapes for a nonzero status, and a lint at deny level is a third.\n+- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:36` — The `jq` step keeps the defect this change removed from the `cargo` step. The last two lines are one pipe that ends in `sort -u`, and the script writes `set -e` alone, with no `pipefail`. A shell pipeline takes the status of its last command, so the script answers exit 0 for each failure of `jq`. Measured over the healthy probe package, which gives one finding: with `jq` replaced by a command that exits 127, the script wrote 0 findings and exited 0, so the engine reads a dirty tree as clean. That is the same answer the rule body calls out for the earlier shape of the `cargo` step: \"A shell pipeline takes the status of its last command, so that shape answered exit 0 for every broken run.\" `builtin/validators/README.md` states: \"Write a pipe only where the tool cannot exit nonzero. Otherwise write a script: run the tool into a file, test the status, and exit nonzero yourself.\" `jq` exits nonzero when it breaks. Give the `jq` step the same status test the `cargo` step has.\n+- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:199` — The path \"src/lib.rs\" is hardcoded in the expected findings string, but is already named as COMPLEX_LIB_PATH at line 65. If the path constant changes, this expected output won't update automatically, creating a maintenance risk and potential test fragility. Define the expected row constant separately (e.g., const RUST_LENGTH_ANNOTATION_ROW_1: &str = \"src/lib.rs:1\";) to document the relationship between the path constant and expected output, or add a comment explaining the duplication if Rust const limitations prevent direct reference.\n+- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:249` — The path \"tests/it.rs\" is hardcoded twice in the expected findings array at line 249, but is already named as RUST_PROBE_INTEGRATION_TEST_PATH at line 238. If the path constant changes, both hardcoded occurrences must be updated separately, risking missed updates. Define named constants for each expected finding (e.g., const RUST_TEST_ROW_2: &str = \"tests/it.rs:2\"; const RUST_TEST_ROW_304: &str = \"tests/it.rs:304\";) to centralize the path and ensure consistency across all uses.\n","reverse_patch":"--- original\n+++ modified\n@@ -90,11 +90,4 @@\n\n The rule uses `-W` rather than `--force-warn`, so `#[allow(clippy::too_many_lines)]` still works. An annotation contract may be the answer here, the way the dead-code rules use the language's own suppression marker. Decide, and state the decision on the rule.\n\n-Found by the `supersedes` survey on ^h7garpc. #tool-validators #objectivity\n-\n-## Review Findings (2026-08-11 19:35)\n-\n-- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:24` — The status gate breaks a run that measured the four gates correctly. `cargo clippy` exits nonzero for two different reasons: the tool could not lint the workspace, and the tool linted the workspace correctly while a lint stands at deny level. The gate reads the status alone, so it cannot tell the two apart. Measured with clippy 0.1.97 at the rule's own thresholds, over one package that holds a function of 301 lines: (a) `#![deny(clippy::unwrap_used)]` beside one `unwrap` gives exit 1, and the report file holds `warning clippy::too_many_lines src/lib.rs:1 this function has too many lines (301/250)`; (b) `[lints.clippy] unwrap_used = \"deny\"` in `Cargo.toml` gives exit 1 with the same report; (c) `RUSTFLAGS=\"-D warnings\"` over one ordinary `unused_variables` warning gives exit 1, and the report file holds `error clippy::too_many_lines src/lib.rs:1 (301/250)`; (d) the same package with no deny gives exit 0 and that one finding. In each of (a), (b) and (c) the script writes 0 findings and the stderr line `complexity-rust: cargo clippy could not lint the workspace`, so the engine loses a finding the report file holds. The gate answers correctly for a run the tool could not make: a package that does not compile, a directory with no `Cargo.toml`, a `Cargo.toml` that does not parse, and a target directory the tool cannot write each give exit 1 with an empty report. `builtin/validators/README.md` states the answer for this shape: \"One status can carry both a measured run and a broken run. The status of a failure is then the same as the status of a finding. The script must then test the REPORT beside the status, and accept the shared status only for the report shape a measured run writes.\" The three shipped swiftlint rules already do this. Make the script test the report beside the status. Correct the section \"A workspace the tool cannot lint\" with it: that section states two shapes for a nonzero status, and a lint at deny level is a third.\n-- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:36` — The `jq` step keeps the defect this change removed from the `cargo` step. The last two lines are one pipe that ends in `sort -u`, and the script writes `set -e` alone, with no `pipefail`. A shell pipeline takes the status of its last command, so the script answers exit 0 for each failure of `jq`. Measured over the healthy probe package, which gives one finding: with `jq` replaced by a command that exits 127, the script wrote 0 findings and exited 0, so the engine reads a dirty tree as clean. That is the same answer the rule body calls out for the earlier shape of the `cargo` step: \"A shell pipeline takes the status of its last command, so that shape answered exit 0 for every broken run.\" `builtin/validators/README.md` states: \"Write a pipe only where the tool cannot exit nonzero. Otherwise write a script: run the tool into a file, test the status, and exit nonzero yourself.\" `jq` exits nonzero when it breaks. Give the `jq` step the same status test the `cargo` step has.\n-- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:199` — The path \"src/lib.rs\" is hardcoded in the expected findings string, but is already named as COMPLEX_LIB_PATH at line 65. If the path constant changes, this expected output won't update automatically, creating a maintenance risk and potential test fragility. Define the expected row constant separately (e.g., const RUST_LENGTH_ANNOTATION_ROW_1: &str = \"src/lib.rs:1\";) to document the relationship between the path constant and expected output, or add a comment explaining the duplication if Rust const limitations prevent direct reference.\n-- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:249` — The path \"tests/it.rs\" is hardcoded twice in the expected findings array at line 249, but is already named as RUST_PROBE_INTEGRATION_TEST_PATH at line 238. If the path constant changes, both hardcoded occurrences must be updated separately, risking missed updates. Define named constants for each expected finding (e.g., const RUST_TEST_ROW_2: &str = \"tests/it.rs:2\"; const RUST_TEST_ROW_304: &str = \"tests/it.rs:304\";) to centralize the path and ensure consistency across all uses.\n+Found by the `supersedes` survey on ^h7garpc. #tool-validators #objectivity\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KZSNP0NN8438X9Q2EA0ANQMJ","timestamp":"2026-08-12T00:24:15.797811Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -76,6 +76,29 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 656 run, 656 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3419 run, 3415 passed, 4 failed — the three `review_e2e` tests missing the duplication claim and `review_progress_is_received_by_a_real_client_over_a_byte_stream_transport`, all four already failing in this tree and not from this change. `cargo fmt --all` made no change. `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` exit 0.\n     - next: /review\n   timestamp: 2026-08-12T00:06:43.258530+00:00\n+- actor: claude-code\n+  id: 01kzsnp0nntb318c2btxhj3v59\n+  text: |-\n+    ### review — findings\n+    - scope: `review sha HEAD~1..HEAD` (commit 002f8c54b), task-mode on ^w5v73k1.\n+    - engine: 8 findings, 8 confirmed, 5 refuted, 0 failed. 6 of the 8 name the Swift, Python, Go and TypeScript sections of `shipped/complexity.rs` (rows 952, 1154, 1373, 1440, 1518, 1677), which stand outside the added hunk (rows 138 to 396). The review skill drops a finding that asks for a change to test code that already existed, so those 6 are dropped. The 2 that stand are in the new hunk.\n+    - open findings: `builtin/validators/code-hygiene/rules/complexity-rust.md:24`, `builtin/validators/code-hygiene/rules/complexity-rust.md:36`, `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:199`, `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:249`.\n+\n+    Each claim of the delta was measured again with clippy 0.1.97, cargo 1.97.1, rustc 1.97.1, over probe packages under the scratchpad, with the run script taken from the shipped bytes. Each claim measured TRUE:\n+\n+    - The status gate breaks a run the tool could not make. A package that does not compile: exit 1, 0 findings, stderr `complexity-rust: cargo clippy could not lint the workspace`. No `Cargo.toml`: exit 1. A `Cargo.toml` that does not parse: exit 1. A target directory the tool cannot write: exit 1. A healthy package: exit 0 and one finding.\n+    - The data shapes report. `Config::default()` of 300 fields: 302/250. A `vec![]` of 300 entries: 302/250. A builder chain of 300 `.opt(n)` calls: 301/250 (the rule table states 302/250; the count moves with the line the chain opens on, and the gate answer is the same).\n+    - `--all-targets` puts test code in scope. With the flag: `examples/ex.rs:1`, `src/lib.rs:1`, `src/lib.rs:308` (the `#[cfg(test)]` function) and `tests/it.rs:2`. Without the flag: `src/lib.rs:1` alone. This matches the table of the rule exactly.\n+    - The absolute-path filter. The raw report held the `OUT_DIR` file by an absolute path and held `src/generated.rs` by a relative path; the filtered output held `src/generated.rs:3` alone. The checked-in file carries both `@generated` headers and still reports.\n+    - The `-W` choice. Over one bare function, one `#[allow(clippy::too_many_lines)]` function and one `#[expect(clippy::too_many_lines, reason = \"data\")]` function: the shipped script reported the bare one alone (`src/lib.rs:1`). The same script with `--force-warn` in place of `-W` reported all three (`src/lib.rs:1`, `:305`, `:609`).\n+    - The `mod`-level `#[expect]`. Over two module files that hold the same bytes (two long functions and one 8-argument function), with `#[expect(clippy::too_many_lines, reason = \"generated\")]` on `pub mod gen;` alone: `src/bare.rs:1` and `src/bare.rs:304` reported and `src/gen.rs` reported neither, so the annotation covers EVERY function of the child file. `src/gen.rs:607` and `src/bare.rs:607` both reported `too many arguments (8/7)`, so a DIFFERENT lint of the four still reports from the child file at the child's own row, and the author must write a second `#[expect]` that names that lint. The expiry holds: over a child that no longer trips, `cargo clippy` raised `warning: this lint expectation is unfulfilled` at `src/lib.rs:1:10`. That warning does not reach the engine, because the `jq` filter selects the four codes alone.\n+    - The four counts. A `clippy.toml` with an unknown key made clippy print its full key list: 115 keys. Exactly four name one of the four lints: `excessive-nesting-threshold`, `too-many-arguments-threshold`, `too-many-lines-threshold`, `type-complexity-threshold`. Exactly nine end in `-in-tests`: `allow-dbg-in-tests`, `allow-expect-in-tests`, `allow-indexing-slicing-in-tests`, `allow-large-stack-frames-in-tests`, `allow-panic-in-tests`, `allow-print-in-tests`, `allow-unwrap-in-tests`, `allow-useless-vec-in-tests`, `check-incompatible-msrv-in-tests`. None of the nine names one of the four lints.\n+    - Scope. The shipped script writes no `\"$@\"` and no zero-argument guard. `shipped/scope_roster.rs` holds every `workspace`-scope rule to both, with no name for one rule, so it covers this rule.\n+    - Temp directory. The script writes `work=\"$(mktemp -d)\"` with `trap 'rm -rf \"$work\"' EXIT` on the line directly under it, and `doctor.check_command` names `mktemp` beside `sort`. `shipped/temp_directory.rs` holds both.\n+    - Tests: `touch crates/swissarmyhammer-validators/src/builtin/mod.rs`, then `cargo nextest run -p swissarmyhammer-validators -E 'test(/the_shipped_rust_complexity/)'` — 5 run, 5 passed, 651 skipped.\n+\n+    - next: /implement ^w5v73k1 for the 4 open findings.\n+  timestamp: 2026-08-12T00:24:15.797358+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -76,29 +76,6 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 656 run, 656 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3419 run, 3415 passed, 4 failed — the three `review_e2e` tests missing the duplication claim and `review_progress_is_received_by_a_real_client_over_a_byte_stream_transport`, all four already failing in this tree and not from this change. `cargo fmt --all` made no change. `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` exit 0.\n     - next: /review\n   timestamp: 2026-08-12T00:06:43.258530+00:00\n-- actor: claude-code\n-  id: 01kzsnp0nntb318c2btxhj3v59\n-  text: |-\n-    ### review — findings\n-    - scope: `review sha HEAD~1..HEAD` (commit 002f8c54b), task-mode on ^w5v73k1.\n-    - engine: 8 findings, 8 confirmed, 5 refuted, 0 failed. 6 of the 8 name the Swift, Python, Go and TypeScript sections of `shipped/complexity.rs` (rows 952, 1154, 1373, 1440, 1518, 1677), which stand outside the added hunk (rows 138 to 396). The review skill drops a finding that asks for a change to test code that already existed, so those 6 are dropped. The 2 that stand are in the new hunk.\n-    - open findings: `builtin/validators/code-hygiene/rules/complexity-rust.md:24`, `builtin/validators/code-hygiene/rules/complexity-rust.md:36`, `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:199`, `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:249`.\n-\n-    Each claim of the delta was measured again with clippy 0.1.97, cargo 1.97.1, rustc 1.97.1, over probe packages under the scratchpad, with the run script taken from the shipped bytes. Each claim measured TRUE:\n-\n-    - The status gate breaks a run the tool could not make. A package that does not compile: exit 1, 0 findings, stderr `complexity-rust: cargo clippy could not lint the workspace`. No `Cargo.toml`: exit 1. A `Cargo.toml` that does not parse: exit 1. A target directory the tool cannot write: exit 1. A healthy package: exit 0 and one finding.\n-    - The data shapes report. `Config::default()` of 300 fields: 302/250. A `vec![]` of 300 entries: 302/250. A builder chain of 300 `.opt(n)` calls: 301/250 (the rule table states 302/250; the count moves with the line the chain opens on, and the gate answer is the same).\n-    - `--all-targets` puts test code in scope. With the flag: `examples/ex.rs:1`, `src/lib.rs:1`, `src/lib.rs:308` (the `#[cfg(test)]` function) and `tests/it.rs:2`. Without the flag: `src/lib.rs:1` alone. This matches the table of the rule exactly.\n-    - The absolute-path filter. The raw report held the `OUT_DIR` file by an absolute path and held `src/generated.rs` by a relative path; the filtered output held `src/generated.rs:3` alone. The checked-in file carries both `@generated` headers and still reports.\n-    - The `-W` choice. Over one bare function, one `#[allow(clippy::too_many_lines)]` function and one `#[expect(clippy::too_many_lines, reason = \"data\")]` function: the shipped script reported the bare one alone (`src/lib.rs:1`). The same script with `--force-warn` in place of `-W` reported all three (`src/lib.rs:1`, `:305`, `:609`).\n-    - The `mod`-level `#[expect]`. Over two module files that hold the same bytes (two long functions and one 8-argument function), with `#[expect(clippy::too_many_lines, reason = \"generated\")]` on `pub mod gen;` alone: `src/bare.rs:1` and `src/bare.rs:304` reported and `src/gen.rs` reported neither, so the annotation covers EVERY function of the child file. `src/gen.rs:607` and `src/bare.rs:607` both reported `too many arguments (8/7)`, so a DIFFERENT lint of the four still reports from the child file at the child's own row, and the author must write a second `#[expect]` that names that lint. The expiry holds: over a child that no longer trips, `cargo clippy` raised `warning: this lint expectation is unfulfilled` at `src/lib.rs:1:10`. That warning does not reach the engine, because the `jq` filter selects the four codes alone.\n-    - The four counts. A `clippy.toml` with an unknown key made clippy print its full key list: 115 keys. Exactly four name one of the four lints: `excessive-nesting-threshold`, `too-many-arguments-threshold`, `too-many-lines-threshold`, `type-complexity-threshold`. Exactly nine end in `-in-tests`: `allow-dbg-in-tests`, `allow-expect-in-tests`, `allow-indexing-slicing-in-tests`, `allow-large-stack-frames-in-tests`, `allow-panic-in-tests`, `allow-print-in-tests`, `allow-unwrap-in-tests`, `allow-useless-vec-in-tests`, `check-incompatible-msrv-in-tests`. None of the nine names one of the four lints.\n-    - Scope. The shipped script writes no `\"$@\"` and no zero-argument guard. `shipped/scope_roster.rs` holds every `workspace`-scope rule to both, with no name for one rule, so it covers this rule.\n-    - Temp directory. The script writes `work=\"$(mktemp -d)\"` with `trap 'rm -rf \"$work\"' EXIT` on the line directly under it, and `doctor.check_command` names `mktemp` beside `sort`. `shipped/temp_directory.rs` holds both.\n-    - Tests: `touch crates/swissarmyhammer-validators/src/builtin/mod.rs`, then `cargo nextest run -p swissarmyhammer-validators -E 'test(/the_shipped_rust_complexity/)'` — 5 run, 5 passed, 651 skipped.\n-\n-    - next: /implement ^w5v73k1 for the 4 open findings.\n-  timestamp: 2026-08-12T00:24:15.797358+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSNQ1EG5ANSGJBA8QJMW1E6","timestamp":"2026-08-12T00:24:49.360257Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -99,6 +99,15 @@\n\n     - next: /implement ^w5v73k1 for the 4 open findings.\n   timestamp: 2026-08-12T00:24:15.797358+00:00\n+- actor: claude-code\n+  id: 01kzsnq1efxnqdzeqnp1k3p3bn\n+  text: |-\n+    ### finish iteration 1 — findings\n+    - implement: changed — 2 files; the run no longer passes a workspace cargo cannot compile as a clean tree, and the rule answers each carve-out of the two prompt rules it supersedes\n+    - test: green — cargo nextest -p swissarmyhammer-validators 656 run, 656 passed; rdeps 3419 run, 3415 passed with only the 4 failures that came before\n+    - commit: 002f8c54b\n+    - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:24, :36, and 2 in crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs (the status gate cannot tell a broken run from a correct run beside a deny-level lint, and the jq pipe keeps the silent zero the cargo step lost)\n+  timestamp: 2026-08-12T00:24:49.359811+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -99,15 +99,6 @@\n\n     - next: /implement ^w5v73k1 for the 4 open findings.\n   timestamp: 2026-08-12T00:24:15.797358+00:00\n-- actor: claude-code\n-  id: 01kzsnq1efxnqdzeqnp1k3p3bn\n-  text: |-\n-    ### finish iteration 1 — findings\n-    - implement: changed — 2 files; the run no longer passes a workspace cargo cannot compile as a clean tree, and the rule answers each carve-out of the two prompt rules it supersedes\n-    - test: green — cargo nextest -p swissarmyhammer-validators 656 run, 656 passed; rdeps 3419 run, 3415 passed with only the 4 failures that came before\n-    - commit: 002f8c54b\n-    - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:24, :36, and 2 in crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs (the status gate cannot tell a broken run from a correct run beside a deny-level lint, and the jq pipe keeps the silent zero the cargo step lost)\n-  timestamp: 2026-08-12T00:24:49.359811+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSNR4QAWS81BQKMXP9961CQ","timestamp":"2026-08-12T00:25:25.482256Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -108,8 +108,8 @@\n     - commit: 002f8c54b\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:24, :36, and 2 in crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs (the status gate cannot tell a broken run from a correct run beside a deny-level lint, and the jq pipe keeps the silent zero the cargo step lost)\n   timestamp: 2026-08-12T00:24:49.359811+00:00\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","reverse_patch":"--- original\n+++ modified\n@@ -108,8 +108,8 @@\n     - commit: 002f8c54b\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:24, :36, and 2 in crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs (the status gate cannot tell a broken run from a correct run beside a deny-level lint, and the jq pipe keeps the silent zero the cargo step lost)\n   timestamp: 2026-08-12T00:24:49.359811+00:00\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: review\n+position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","transaction_id":null}
{"id":"01KZSPME818Z1Q5TGRG5J3G0CE","timestamp":"2026-08-12T00:40:52.737520Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -126,7 +126,7 @@\n\n ## Review Findings (2026-08-11 19:35)\n\n-- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:24` — The status gate breaks a run that measured the four gates correctly. `cargo clippy` exits nonzero for two different reasons: the tool could not lint the workspace, and the tool linted the workspace correctly while a lint stands at deny level. The gate reads the status alone, so it cannot tell the two apart. Measured with clippy 0.1.97 at the rule's own thresholds, over one package that holds a function of 301 lines: (a) `#![deny(clippy::unwrap_used)]` beside one `unwrap` gives exit 1, and the report file holds `warning clippy::too_many_lines src/lib.rs:1 this function has too many lines (301/250)`; (b) `[lints.clippy] unwrap_used = \"deny\"` in `Cargo.toml` gives exit 1 with the same report; (c) `RUSTFLAGS=\"-D warnings\"` over one ordinary `unused_variables` warning gives exit 1, and the report file holds `error clippy::too_many_lines src/lib.rs:1 (301/250)`; (d) the same package with no deny gives exit 0 and that one finding. In each of (a), (b) and (c) the script writes 0 findings and the stderr line `complexity-rust: cargo clippy could not lint the workspace`, so the engine loses a finding the report file holds. The gate answers correctly for a run the tool could not make: a package that does not compile, a directory with no `Cargo.toml`, a `Cargo.toml` that does not parse, and a target directory the tool cannot write each give exit 1 with an empty report. `builtin/validators/README.md` states the answer for this shape: \"One status can carry both a measured run and a broken run. The status of a failure is then the same as the status of a finding. The script must then test the REPORT beside the status, and accept the shared status only for the report shape a measured run writes.\" The three shipped swiftlint rules already do this. Make the script test the report beside the status. Correct the section \"A workspace the tool cannot lint\" with it: that section states two shapes for a nonzero status, and a lint at deny level is a third.\n-- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:36` — The `jq` step keeps the defect this change removed from the `cargo` step. The last two lines are one pipe that ends in `sort -u`, and the script writes `set -e` alone, with no `pipefail`. A shell pipeline takes the status of its last command, so the script answers exit 0 for each failure of `jq`. Measured over the healthy probe package, which gives one finding: with `jq` replaced by a command that exits 127, the script wrote 0 findings and exited 0, so the engine reads a dirty tree as clean. That is the same answer the rule body calls out for the earlier shape of the `cargo` step: \"A shell pipeline takes the status of its last command, so that shape answered exit 0 for every broken run.\" `builtin/validators/README.md` states: \"Write a pipe only where the tool cannot exit nonzero. Otherwise write a script: run the tool into a file, test the status, and exit nonzero yourself.\" `jq` exits nonzero when it breaks. Give the `jq` step the same status test the `cargo` step has.\n-- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:199` — The path \"src/lib.rs\" is hardcoded in the expected findings string, but is already named as COMPLEX_LIB_PATH at line 65. If the path constant changes, this expected output won't update automatically, creating a maintenance risk and potential test fragility. Define the expected row constant separately (e.g., const RUST_LENGTH_ANNOTATION_ROW_1: &str = \"src/lib.rs:1\";) to document the relationship between the path constant and expected output, or add a comment explaining the duplication if Rust const limitations prevent direct reference.\n-- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:249` — The path \"tests/it.rs\" is hardcoded twice in the expected findings array at line 249, but is already named as RUST_PROBE_INTEGRATION_TEST_PATH at line 238. If the path constant changes, both hardcoded occurrences must be updated separately, risking missed updates. Define named constants for each expected finding (e.g., const RUST_TEST_ROW_2: &str = \"tests/it.rs:2\"; const RUST_TEST_ROW_304: &str = \"tests/it.rs:304\";) to centralize the path and ensure consistency across all uses.\n+- [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:24` — The status gate breaks a run that measured the four gates correctly. `cargo clippy` exits nonzero for two different reasons: the tool could not lint the workspace, and the tool linted the workspace correctly while a lint stands at deny level. The gate reads the status alone, so it cannot tell the two apart. Measured with clippy 0.1.97 at the rule's own thresholds, over one package that holds a function of 301 lines: (a) `#![deny(clippy::unwrap_used)]` beside one `unwrap` gives exit 1, and the report file holds `warning clippy::too_many_lines src/lib.rs:1 this function has too many lines (301/250)`; (b) `[lints.clippy] unwrap_used = \"deny\"` in `Cargo.toml` gives exit 1 with the same report; (c) `RUSTFLAGS=\"-D warnings\"` over one ordinary `unused_variables` warning gives exit 1, and the report file holds `error clippy::too_many_lines src/lib.rs:1 (301/250)`; (d) the same package with no deny gives exit 0 and that one finding. In each of (a), (b) and (c) the script writes 0 findings and the stderr line `complexity-rust: cargo clippy could not lint the workspace`, so the engine loses a finding the report file holds. The gate answers correctly for a run the tool could not make: a package that does not compile, a directory with no `Cargo.toml`, a `Cargo.toml` that does not parse, and a target directory the tool cannot write each give exit 1 with an empty report. `builtin/validators/README.md` states the answer for this shape: \"One status can carry both a measured run and a broken run. The status of a failure is then the same as the status of a finding. The script must then test the REPORT beside the status, and accept the shared status only for the report shape a measured run writes.\" The three shipped swiftlint rules already do this. Make the script test the report beside the status. Correct the section \"A workspace the tool cannot lint\" with it: that section states two shapes for a nonzero status, and a lint at deny level is a third.\n+- [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:36` — The `jq` step keeps the defect this change removed from the `cargo` step. The last two lines are one pipe that ends in `sort -u`, and the script writes `set -e` alone, with no `pipefail`. A shell pipeline takes the status of its last command, so the script answers exit 0 for each failure of `jq`. Measured over the healthy probe package, which gives one finding: with `jq` replaced by a command that exits 127, the script wrote 0 findings and exited 0, so the engine reads a dirty tree as clean. That is the same answer the rule body calls out for the earlier shape of the `cargo` step: \"A shell pipeline takes the status of its last command, so that shape answered exit 0 for every broken run.\" `builtin/validators/README.md` states: \"Write a pipe only where the tool cannot exit nonzero. Otherwise write a script: run the tool into a file, test the status, and exit nonzero yourself.\" `jq` exits nonzero when it breaks. Give the `jq` step the same status test the `cargo` step has.\n+- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:199` — The path \"src/lib.rs\" is hardcoded in the expected findings string, but is already named as COMPLEX_LIB_PATH at line 65. If the path constant changes, this expected output won't update automatically, creating a maintenance risk and potential test fragility. Define the expected row constant separately (e.g., const RUST_LENGTH_ANNOTATION_ROW_1: &str = \"src/lib.rs:1\";) to document the relationship between the path constant and expected output, or add a comment explaining the duplication if Rust const limitations prevent direct reference.\n+- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:249` — The path \"tests/it.rs\" is hardcoded twice in the expected findings array at line 249, but is already named as RUST_PROBE_INTEGRATION_TEST_PATH at line 238. If the path constant changes, both hardcoded occurrences must be updated separately, risking missed updates. Define named constants for each expected finding (e.g., const RUST_TEST_ROW_2: &str = \"tests/it.rs:2\"; const RUST_TEST_ROW_304: &str = \"tests/it.rs:304\";) to centralize the path and ensure consistency across all uses.\n","reverse_patch":"--- original\n+++ modified\n@@ -126,7 +126,7 @@\n\n ## Review Findings (2026-08-11 19:35)\n\n-- [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:24` — The status gate breaks a run that measured the four gates correctly. `cargo clippy` exits nonzero for two different reasons: the tool could not lint the workspace, and the tool linted the workspace correctly while a lint stands at deny level. The gate reads the status alone, so it cannot tell the two apart. Measured with clippy 0.1.97 at the rule's own thresholds, over one package that holds a function of 301 lines: (a) `#![deny(clippy::unwrap_used)]` beside one `unwrap` gives exit 1, and the report file holds `warning clippy::too_many_lines src/lib.rs:1 this function has too many lines (301/250)`; (b) `[lints.clippy] unwrap_used = \"deny\"` in `Cargo.toml` gives exit 1 with the same report; (c) `RUSTFLAGS=\"-D warnings\"` over one ordinary `unused_variables` warning gives exit 1, and the report file holds `error clippy::too_many_lines src/lib.rs:1 (301/250)`; (d) the same package with no deny gives exit 0 and that one finding. In each of (a), (b) and (c) the script writes 0 findings and the stderr line `complexity-rust: cargo clippy could not lint the workspace`, so the engine loses a finding the report file holds. The gate answers correctly for a run the tool could not make: a package that does not compile, a directory with no `Cargo.toml`, a `Cargo.toml` that does not parse, and a target directory the tool cannot write each give exit 1 with an empty report. `builtin/validators/README.md` states the answer for this shape: \"One status can carry both a measured run and a broken run. The status of a failure is then the same as the status of a finding. The script must then test the REPORT beside the status, and accept the shared status only for the report shape a measured run writes.\" The three shipped swiftlint rules already do this. Make the script test the report beside the status. Correct the section \"A workspace the tool cannot lint\" with it: that section states two shapes for a nonzero status, and a lint at deny level is a third.\n-- [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:36` — The `jq` step keeps the defect this change removed from the `cargo` step. The last two lines are one pipe that ends in `sort -u`, and the script writes `set -e` alone, with no `pipefail`. A shell pipeline takes the status of its last command, so the script answers exit 0 for each failure of `jq`. Measured over the healthy probe package, which gives one finding: with `jq` replaced by a command that exits 127, the script wrote 0 findings and exited 0, so the engine reads a dirty tree as clean. That is the same answer the rule body calls out for the earlier shape of the `cargo` step: \"A shell pipeline takes the status of its last command, so that shape answered exit 0 for every broken run.\" `builtin/validators/README.md` states: \"Write a pipe only where the tool cannot exit nonzero. Otherwise write a script: run the tool into a file, test the status, and exit nonzero yourself.\" `jq` exits nonzero when it breaks. Give the `jq` step the same status test the `cargo` step has.\n-- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:199` — The path \"src/lib.rs\" is hardcoded in the expected findings string, but is already named as COMPLEX_LIB_PATH at line 65. If the path constant changes, this expected output won't update automatically, creating a maintenance risk and potential test fragility. Define the expected row constant separately (e.g., const RUST_LENGTH_ANNOTATION_ROW_1: &str = \"src/lib.rs:1\";) to document the relationship between the path constant and expected output, or add a comment explaining the duplication if Rust const limitations prevent direct reference.\n-- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:249` — The path \"tests/it.rs\" is hardcoded twice in the expected findings array at line 249, but is already named as RUST_PROBE_INTEGRATION_TEST_PATH at line 238. If the path constant changes, both hardcoded occurrences must be updated separately, risking missed updates. Define named constants for each expected finding (e.g., const RUST_TEST_ROW_2: &str = \"tests/it.rs:2\"; const RUST_TEST_ROW_304: &str = \"tests/it.rs:304\";) to centralize the path and ensure consistency across all uses.\n+- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:24` — The status gate breaks a run that measured the four gates correctly. `cargo clippy` exits nonzero for two different reasons: the tool could not lint the workspace, and the tool linted the workspace correctly while a lint stands at deny level. The gate reads the status alone, so it cannot tell the two apart. Measured with clippy 0.1.97 at the rule's own thresholds, over one package that holds a function of 301 lines: (a) `#![deny(clippy::unwrap_used)]` beside one `unwrap` gives exit 1, and the report file holds `warning clippy::too_many_lines src/lib.rs:1 this function has too many lines (301/250)`; (b) `[lints.clippy] unwrap_used = \"deny\"` in `Cargo.toml` gives exit 1 with the same report; (c) `RUSTFLAGS=\"-D warnings\"` over one ordinary `unused_variables` warning gives exit 1, and the report file holds `error clippy::too_many_lines src/lib.rs:1 (301/250)`; (d) the same package with no deny gives exit 0 and that one finding. In each of (a), (b) and (c) the script writes 0 findings and the stderr line `complexity-rust: cargo clippy could not lint the workspace`, so the engine loses a finding the report file holds. The gate answers correctly for a run the tool could not make: a package that does not compile, a directory with no `Cargo.toml`, a `Cargo.toml` that does not parse, and a target directory the tool cannot write each give exit 1 with an empty report. `builtin/validators/README.md` states the answer for this shape: \"One status can carry both a measured run and a broken run. The status of a failure is then the same as the status of a finding. The script must then test the REPORT beside the status, and accept the shared status only for the report shape a measured run writes.\" The three shipped swiftlint rules already do this. Make the script test the report beside the status. Correct the section \"A workspace the tool cannot lint\" with it: that section states two shapes for a nonzero status, and a lint at deny level is a third.\n+- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:36` — The `jq` step keeps the defect this change removed from the `cargo` step. The last two lines are one pipe that ends in `sort -u`, and the script writes `set -e` alone, with no `pipefail`. A shell pipeline takes the status of its last command, so the script answers exit 0 for each failure of `jq`. Measured over the healthy probe package, which gives one finding: with `jq` replaced by a command that exits 127, the script wrote 0 findings and exited 0, so the engine reads a dirty tree as clean. That is the same answer the rule body calls out for the earlier shape of the `cargo` step: \"A shell pipeline takes the status of its last command, so that shape answered exit 0 for every broken run.\" `builtin/validators/README.md` states: \"Write a pipe only where the tool cannot exit nonzero. Otherwise write a script: run the tool into a file, test the status, and exit nonzero yourself.\" `jq` exits nonzero when it breaks. Give the `jq` step the same status test the `cargo` step has.\n+- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:199` — The path \"src/lib.rs\" is hardcoded in the expected findings string, but is already named as COMPLEX_LIB_PATH at line 65. If the path constant changes, this expected output won't update automatically, creating a maintenance risk and potential test fragility. Define the expected row constant separately (e.g., const RUST_LENGTH_ANNOTATION_ROW_1: &str = \"src/lib.rs:1\";) to document the relationship between the path constant and expected output, or add a comment explaining the duplication if Rust const limitations prevent direct reference.\n+- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:249` — The path \"tests/it.rs\" is hardcoded twice in the expected findings array at line 249, but is already named as RUST_PROBE_INTEGRATION_TEST_PATH at line 238. If the path constant changes, both hardcoded occurrences must be updated separately, risking missed updates. Define named constants for each expected finding (e.g., const RUST_TEST_ROW_2: &str = \"tests/it.rs:2\"; const RUST_TEST_ROW_304: &str = \"tests/it.rs:304\";) to centralize the path and ensure consistency across all uses.\n","transaction_id":null}
{"id":"01KZSPN2GT6Z0SKWSR5JGF9GBD","timestamp":"2026-08-12T00:41:13.498162Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -108,6 +108,36 @@\n     - commit: 002f8c54b\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:24, :36, and 2 in crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs (the status gate cannot tell a broken run from a correct run beside a deny-level lint, and the jq pipe keeps the silent zero the cargo step lost)\n   timestamp: 2026-08-12T00:24:49.359811+00:00\n+- actor: claude-code\n+  id: 01kzspn2gsm2h0jw7hrhmwd6pf\n+  text: |-\n+    The four open findings are answered. Every shape was measured again with clippy 0.1.97 and cargo 1.97.1, over probe packages under the scratchpad, with the run script taken from the SHIPPED bytes of the rule.\n+\n+    FINDING :24 — the status gate. The card states the four broken shapes give an \"EMPTY report\". Measured: that is true of the FOUR LINTS, not of the report file. A package that does not compile writes 7196 bytes of `compiler-message` into the report file, each one an `E0308` error, and no message of the four lints, because clippy runs those lints after the type check the package failed. A directory with no `Cargo.toml`, a `Cargo.toml` that does not parse and a target directory cargo cannot write each write 0 bytes. So the report shape a measured run writes is: the report holds one finding of the four lints or more.\n+\n+    The script now writes the filtered findings to `$work/findings.json`, and breaks only when the status is nonzero AND that file holds no byte. Measured over eight shapes, with the shipped bytes, before and after:\n+\n+    | the shape | before | after |\n+    |---|---|---|\n+    | the healthy package | 1 finding, exit 0 | 1 finding, exit 0 |\n+    | `#![deny(clippy::unwrap_used)]` beside one `unwrap` | 0 findings, exit 1 | 1 finding, exit 0 |\n+    | `[lints.clippy] unwrap_used = \"deny\"` beside one `unwrap` | 0 findings, exit 1 | 1 finding, exit 0 |\n+    | `RUSTFLAGS=\"-D warnings\"` beside one `unused_variables` | 0 findings, exit 1 | 1 finding, exit 0 |\n+    | a package that does not compile | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | a directory with no `Cargo.toml` | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | a `Cargo.toml` that does not parse | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | a target directory cargo cannot write | 0 findings, exit 1 | 0 findings, exit 1 |\n+\n+    Each of the four broken shapes writes `complexity-rust: cargo clippy could not lint the workspace` to stderr. Under `RUSTFLAGS=\"-D warnings\"` the four lints arrive at level `error` rather than `warning`; the filter selects on the lint CODE, so the finding stands either way. cargo writes `error: could not compile` to stderr for the three deny shapes and for the package that does not compile alike, so stderr tells a reader nothing the report does not.\n+\n+    One trade, stated on the rule: a nonzero status beside a report of no finding is answered as a broken run even when the workspace compiled. A workspace that fails its build at a deny-level lint and holds no finding of these four gates therefore reports a tool error. That is the same trade the three swiftlint rules make.\n+\n+    FINDING :36 — the `jq` step. The step now writes to a file, tests its own status, and writes `complexity-rust: jq could not read the clippy report` to stderr before exit 1. `sort -u` reads that file, so it stands last with no pipe above it and the script takes its status. Measured over the healthy package, which gives one finding, with a `jq` on PATH that exits 127: the pipe shape wrote 0 findings and exited 0; the shipped shape writes 0 findings, that line, and exit 1.\n+\n+    FINDING :199 and FINDING :249 — the hardcoded paths. Every expected entry of a Rust complexity probe is now built by `probe_row(path, row)` from the same constant the probe stages the file under, so no expected entry holds a path of its own. Each row number is a named constant that derives from the counts above it: `BARE_LONG_FUNCTION_LINES = LONG_FUNCTION_BODY_LINES + 2`, `RUST_TEST_FUNCTION_ROW = RUST_TEST_ATTRIBUTE_LINES + 1`, `RUST_TEST_HELPER_ROW = RUST_TEST_ATTRIBUTE_LINES + BARE_LONG_FUNCTION_LINES + 1`, `RUST_GENERATED_FUNCTION_ROW = RUST_GENERATED_HEAD_LINES + 1`. The same cause stood at `RUST_GENERATED_ROWS`, which no finding named, and it is removed as well.\n+\n+    The Swift, Python, Go and TypeScript sections of the same file hold the same shape. They stood before this card, and the review skill drops a finding that asks for a change to test code that already existed, so they are left as they are.\n+  timestamp: 2026-08-12T00:41:13.497646+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -108,36 +108,6 @@\n     - commit: 002f8c54b\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:24, :36, and 2 in crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs (the status gate cannot tell a broken run from a correct run beside a deny-level lint, and the jq pipe keeps the silent zero the cargo step lost)\n   timestamp: 2026-08-12T00:24:49.359811+00:00\n-- actor: claude-code\n-  id: 01kzspn2gsm2h0jw7hrhmwd6pf\n-  text: |-\n-    The four open findings are answered. Every shape was measured again with clippy 0.1.97 and cargo 1.97.1, over probe packages under the scratchpad, with the run script taken from the SHIPPED bytes of the rule.\n-\n-    FINDING :24 — the status gate. The card states the four broken shapes give an \"EMPTY report\". Measured: that is true of the FOUR LINTS, not of the report file. A package that does not compile writes 7196 bytes of `compiler-message` into the report file, each one an `E0308` error, and no message of the four lints, because clippy runs those lints after the type check the package failed. A directory with no `Cargo.toml`, a `Cargo.toml` that does not parse and a target directory cargo cannot write each write 0 bytes. So the report shape a measured run writes is: the report holds one finding of the four lints or more.\n-\n-    The script now writes the filtered findings to `$work/findings.json`, and breaks only when the status is nonzero AND that file holds no byte. Measured over eight shapes, with the shipped bytes, before and after:\n-\n-    | the shape | before | after |\n-    |---|---|---|\n-    | the healthy package | 1 finding, exit 0 | 1 finding, exit 0 |\n-    | `#![deny(clippy::unwrap_used)]` beside one `unwrap` | 0 findings, exit 1 | 1 finding, exit 0 |\n-    | `[lints.clippy] unwrap_used = \"deny\"` beside one `unwrap` | 0 findings, exit 1 | 1 finding, exit 0 |\n-    | `RUSTFLAGS=\"-D warnings\"` beside one `unused_variables` | 0 findings, exit 1 | 1 finding, exit 0 |\n-    | a package that does not compile | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | a directory with no `Cargo.toml` | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | a `Cargo.toml` that does not parse | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | a target directory cargo cannot write | 0 findings, exit 1 | 0 findings, exit 1 |\n-\n-    Each of the four broken shapes writes `complexity-rust: cargo clippy could not lint the workspace` to stderr. Under `RUSTFLAGS=\"-D warnings\"` the four lints arrive at level `error` rather than `warning`; the filter selects on the lint CODE, so the finding stands either way. cargo writes `error: could not compile` to stderr for the three deny shapes and for the package that does not compile alike, so stderr tells a reader nothing the report does not.\n-\n-    One trade, stated on the rule: a nonzero status beside a report of no finding is answered as a broken run even when the workspace compiled. A workspace that fails its build at a deny-level lint and holds no finding of these four gates therefore reports a tool error. That is the same trade the three swiftlint rules make.\n-\n-    FINDING :36 — the `jq` step. The step now writes to a file, tests its own status, and writes `complexity-rust: jq could not read the clippy report` to stderr before exit 1. `sort -u` reads that file, so it stands last with no pipe above it and the script takes its status. Measured over the healthy package, which gives one finding, with a `jq` on PATH that exits 127: the pipe shape wrote 0 findings and exited 0; the shipped shape writes 0 findings, that line, and exit 1.\n-\n-    FINDING :199 and FINDING :249 — the hardcoded paths. Every expected entry of a Rust complexity probe is now built by `probe_row(path, row)` from the same constant the probe stages the file under, so no expected entry holds a path of its own. Each row number is a named constant that derives from the counts above it: `BARE_LONG_FUNCTION_LINES = LONG_FUNCTION_BODY_LINES + 2`, `RUST_TEST_FUNCTION_ROW = RUST_TEST_ATTRIBUTE_LINES + 1`, `RUST_TEST_HELPER_ROW = RUST_TEST_ATTRIBUTE_LINES + BARE_LONG_FUNCTION_LINES + 1`, `RUST_GENERATED_FUNCTION_ROW = RUST_GENERATED_HEAD_LINES + 1`. The same cause stood at `RUST_GENERATED_ROWS`, which no finding named, and it is removed as well.\n-\n-    The Swift, Python, Go and TypeScript sections of the same file hold the same shape. They stood before this card, and the review skill drops a finding that asks for a change to test code that already existed, so they are left as they are.\n-  timestamp: 2026-08-12T00:41:13.497646+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSPNG70CSADFZGJ70R2Z0E6","timestamp":"2026-08-12T00:41:27.520162Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -138,6 +138,25 @@\n\n     The Swift, Python, Go and TypeScript sections of the same file hold the same shape. They stood before this card, and the review skill drops a finding that asks for a change to test code that already existed, so they are left as they are.\n   timestamp: 2026-08-12T00:41:13.497646+00:00\n+- actor: claude-code\n+  id: 01kzspng6zx976bsfxmm6e7jxq\n+  text: |-\n+    The RED-to-GREEN proof of the two new acceptance tests. Each RED ran against the SHIPPED bytes before the rule changed, with `touch crates/swissarmyhammer-validators/src/builtin/mod.rs` before each run.\n+\n+    1. `the_shipped_rust_complexity_tool_rule_measures_a_workspace_beside_a_deny_level_lint`. The probe stages a manifest holding `[lints.clippy] unwrap_used = \"deny\"`, one line that calls `unwrap`, and one function of 302 lines under it.\n+       RED: `the shipped Rust complexity script must judge the probe package and exit 0: Exit(\"error: could not compile `deny-probe` (lib) due to 1 previous error; 2 warnings emitted ... complexity-rust: cargo clippy could not lint the workspace\")`.\n+       GREEN: `[\"src/lib.rs:2\"]`.\n+\n+    2. `the_shipped_rust_complexity_tool_rule_breaks_when_the_filter_cannot_read_the_report`. The probe writes a `jq` that exits 127 into a temporary directory, leads `PATH` with that directory under an `EnvVarGuard`, and drives the shipped script over the nesting probe package, which gives one finding.\n+       RED: `a filter that cannot read the report must break the run: []` — the script exited 0 with 0 findings.\n+       GREEN: the run breaks with `complexity-rust: jq could not read the clippy report`.\n+\n+    The three earlier Rust probes — the length-gate annotation, the generated file and the test function beside its helper — pass unchanged under the new expected-entry shape, so the `probe_row` change moves no assertion.\n+\n+    Tests: `cargo nextest run -p swissarmyhammer-validators` 658 run, 658 passed, 0 skipped (656 before; the two new tests are the difference). `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3421 run, 3417 passed, 4 failed — the three `review_e2e` tests missing the duplication claim and `review_progress_is_received_by_a_real_client_over_a_byte_stream_transport`, all four already failing in this tree and not from this change. `cargo fmt --all` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` both clean.\n+\n+    The set-wide guards still hold with no edit: the script writes no `\"$@\"` and no zero-argument guard, `work=\"$(mktemp -d)\"` keeps its `trap` on the line directly under it, and `doctor.check_command` still names `cargo-clippy jq sort mktemp`, each of which the script runs.\n+  timestamp: 2026-08-12T00:41:27.519604+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -138,25 +138,6 @@\n\n     The Swift, Python, Go and TypeScript sections of the same file hold the same shape. They stood before this card, and the review skill drops a finding that asks for a change to test code that already existed, so they are left as they are.\n   timestamp: 2026-08-12T00:41:13.497646+00:00\n-- actor: claude-code\n-  id: 01kzspng6zx976bsfxmm6e7jxq\n-  text: |-\n-    The RED-to-GREEN proof of the two new acceptance tests. Each RED ran against the SHIPPED bytes before the rule changed, with `touch crates/swissarmyhammer-validators/src/builtin/mod.rs` before each run.\n-\n-    1. `the_shipped_rust_complexity_tool_rule_measures_a_workspace_beside_a_deny_level_lint`. The probe stages a manifest holding `[lints.clippy] unwrap_used = \"deny\"`, one line that calls `unwrap`, and one function of 302 lines under it.\n-       RED: `the shipped Rust complexity script must judge the probe package and exit 0: Exit(\"error: could not compile `deny-probe` (lib) due to 1 previous error; 2 warnings emitted ... complexity-rust: cargo clippy could not lint the workspace\")`.\n-       GREEN: `[\"src/lib.rs:2\"]`.\n-\n-    2. `the_shipped_rust_complexity_tool_rule_breaks_when_the_filter_cannot_read_the_report`. The probe writes a `jq` that exits 127 into a temporary directory, leads `PATH` with that directory under an `EnvVarGuard`, and drives the shipped script over the nesting probe package, which gives one finding.\n-       RED: `a filter that cannot read the report must break the run: []` — the script exited 0 with 0 findings.\n-       GREEN: the run breaks with `complexity-rust: jq could not read the clippy report`.\n-\n-    The three earlier Rust probes — the length-gate annotation, the generated file and the test function beside its helper — pass unchanged under the new expected-entry shape, so the `probe_row` change moves no assertion.\n-\n-    Tests: `cargo nextest run -p swissarmyhammer-validators` 658 run, 658 passed, 0 skipped (656 before; the two new tests are the difference). `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3421 run, 3417 passed, 4 failed — the three `review_e2e` tests missing the duplication claim and `review_progress_is_received_by_a_real_client_over_a_byte_stream_transport`, all four already failing in this tree and not from this change. `cargo fmt --all` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` both clean.\n-\n-    The set-wide guards still hold with no edit: the script writes no `\"$@\"` and no zero-argument guard, `work=\"$(mktemp -d)\"` keeps its `trap` on the line directly under it, and `doctor.check_command` still names `cargo-clippy jq sort mktemp`, each of which the script runs.\n-  timestamp: 2026-08-12T00:41:27.519604+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSPNRWBSMW81ZMXTB7H0STV","timestamp":"2026-08-12T00:41:36.395049Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -157,6 +157,14 @@\n\n     The set-wide guards still hold with no edit: the script writes no `\"$@\"` and no zero-argument guard, `work=\"$(mktemp -d)\"` keeps its `trap` on the line directly under it, and `doctor.check_command` still names `cargo-clippy jq sort mktemp`, each of which the script runs.\n   timestamp: 2026-08-12T00:41:27.519604+00:00\n+- actor: claude-code\n+  id: 01kzspnrwae5s7qk6p5mcnb733\n+  text: |-\n+    ### implement — changed\n+    - evidence: 2 files. `builtin/validators/code-hygiene/rules/complexity-rust.md` — the `run` script writes the filtered findings to a file, gives the filter step its own status test with the line `complexity-rust: jq could not read the clippy report`, and breaks the run only when the status is nonzero AND the findings file holds no byte; the body replaces \"A workspace the tool cannot lint\" with the eight measured shapes and adds \"The status of each step\". `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs` — two new acceptance tests over the shipped bytes (the deny-level lint, the filter that exits 127), and every Rust expected entry now built by `probe_row` from the path constant with a named row.\n+    - tests: `cargo nextest run -p swissarmyhammer-validators` 658 run, 658 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3421 run, 3417 passed, 4 failed, all four failing before this change. `cargo fmt --all` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n+    - next: /review\n+  timestamp: 2026-08-12T00:41:36.394480+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -157,14 +157,6 @@\n\n     The set-wide guards still hold with no edit: the script writes no `\"$@\"` and no zero-argument guard, `work=\"$(mktemp -d)\"` keeps its `trap` on the line directly under it, and `doctor.check_command` still names `cargo-clippy jq sort mktemp`, each of which the script runs.\n   timestamp: 2026-08-12T00:41:27.519604+00:00\n-- actor: claude-code\n-  id: 01kzspnrwae5s7qk6p5mcnb733\n-  text: |-\n-    ### implement — changed\n-    - evidence: 2 files. `builtin/validators/code-hygiene/rules/complexity-rust.md` — the `run` script writes the filtered findings to a file, gives the filter step its own status test with the line `complexity-rust: jq could not read the clippy report`, and breaks the run only when the status is nonzero AND the findings file holds no byte; the body replaces \"A workspace the tool cannot lint\" with the eight measured shapes and adds \"The status of each step\". `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs` — two new acceptance tests over the shipped bytes (the deny-level lint, the filter that exits 127), and every Rust expected entry now built by `probe_row` from the path constant with a named row.\n-    - tests: `cargo nextest run -p swissarmyhammer-validators` 658 run, 658 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3421 run, 3417 passed, 4 failed, all four failing before this change. `cargo fmt --all` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n-    - next: /review\n-  timestamp: 2026-08-12T00:41:36.394480+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSQBB6EEVX4FSV4Y1GMNAXX","timestamp":"2026-08-12T00:53:23.278258Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -165,8 +165,8 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 658 run, 658 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3421 run, 3417 passed, 4 failed, all four failing before this change. `cargo fmt --all` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n     - next: /review\n   timestamp: 2026-08-12T00:41:36.394480+00:00\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: review\n+position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","reverse_patch":"--- original\n+++ modified\n@@ -165,8 +165,8 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 658 run, 658 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3421 run, 3417 passed, 4 failed, all four failing before this change. `cargo fmt --all` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n     - next: /review\n   timestamp: 2026-08-12T00:41:36.394480+00:00\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","transaction_id":null}
{"id":"01KZSQCFQFHMEHJB5CP12880S8","timestamp":"2026-08-12T00:54:00.687334Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -187,3 +187,9 @@\n - [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:36` — The `jq` step keeps the defect this change removed from the `cargo` step. The last two lines are one pipe that ends in `sort -u`, and the script writes `set -e` alone, with no `pipefail`. A shell pipeline takes the status of its last command, so the script answers exit 0 for each failure of `jq`. Measured over the healthy probe package, which gives one finding: with `jq` replaced by a command that exits 127, the script wrote 0 findings and exited 0, so the engine reads a dirty tree as clean. That is the same answer the rule body calls out for the earlier shape of the `cargo` step: \"A shell pipeline takes the status of its last command, so that shape answered exit 0 for every broken run.\" `builtin/validators/README.md` states: \"Write a pipe only where the tool cannot exit nonzero. Otherwise write a script: run the tool into a file, test the status, and exit nonzero yourself.\" `jq` exits nonzero when it breaks. Give the `jq` step the same status test the `cargo` step has.\n - [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:199` — The path \"src/lib.rs\" is hardcoded in the expected findings string, but is already named as COMPLEX_LIB_PATH at line 65. If the path constant changes, this expected output won't update automatically, creating a maintenance risk and potential test fragility. Define the expected row constant separately (e.g., const RUST_LENGTH_ANNOTATION_ROW_1: &str = \"src/lib.rs:1\";) to document the relationship between the path constant and expected output, or add a comment explaining the duplication if Rust const limitations prevent direct reference.\n - [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:249` — The path \"tests/it.rs\" is hardcoded twice in the expected findings array at line 249, but is already named as RUST_PROBE_INTEGRATION_TEST_PATH at line 238. If the path constant changes, both hardcoded occurrences must be updated separately, risking missed updates. Define named constants for each expected finding (e.g., const RUST_TEST_ROW_2: &str = \"tests/it.rs:2\"; const RUST_TEST_ROW_304: &str = \"tests/it.rs:304\";) to centralize the path and ensure consistency across all uses.\n+\n+## Review Findings (2026-08-11 19:44)\n+\n+- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The new gate lets a workspace pass when one member does not compile. The gate reads the FILTERED file. One member that compiles fills that file, so the status test never runs. Measured with clippy 0.1.97 and cargo 1.97.1, over a workspace of two members: the member `good` holds a function of 302 lines, and the member `bad` holds the same function beside `pub fn broken() -> i32 { \"not an integer\" }`. cargo exits 101 and writes `error: could not compile bad (lib) due to 1 previous error` to stderr. The script writes one finding, `good/src/lib.rs:1`, and exits 0. The file `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean, because clippy runs the four lints after the type check that member failed. This is the same silent zero the earlier pipe shape gave. The `scope: workspace` of this rule is what makes the shape reach a real repository, and this repository holds more than 20 members. The acceptance test `the_shipped_rust_complexity_tool_rule_breaks_on_a_workspace_it_cannot_compile` drives ONE package, so it passes over this shape. The RAW report holds the answer that the filtered file does not: over this probe the raw report holds 606 `compiler-message` entries and the error code `E0308`, while the filtered file holds one line. Test the raw report for a rustc error code. Add the two-member shape to the table in the section \"A workspace the tool cannot lint\", and add an acceptance test for it.\n+- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The same gate breaks a run that was correct and clean. A workspace that clippy linted correctly, that holds a lint at deny level, and that holds no finding of the four gates gives a nonzero status beside an empty filtered file. The gate answers that shape as a broken run. Measured with clippy 0.1.97 and cargo 1.97.1: (a) a package with `[lints.clippy] unwrap_used = \"deny\"` and one `unwrap`, and no function over any of the four gates — cargo exits 101, and the script writes `complexity-rust: cargo clippy could not lint the workspace` and exits 1; (b) `RUSTFLAGS=\"-D warnings\"` over one `unused_variables` warning in the same shape — the same answer. Each of the two workspaces is clean at the four gates, so the correct answer is 0 findings at exit 0. The rule states this shape as a trade in the section \"A workspace the tool cannot lint\". The trade is not necessary, because the RAW report tells the two runs apart and the script reads only the filtered file. Measured over four shapes: the deny-level package writes 10244 bytes into the raw report, with the error code `clippy::unwrap_used` and one `{\"reason\":\"build-finished\",\"success\":false}` entry; the `-D warnings` package writes 4574 bytes, with the error code `unused_variables` and the same `build-finished` entry; a directory that holds no `Cargo.toml` writes 0 bytes and NO `build-finished` entry; the two-member workspace above writes 1100479 bytes with the error code `E0308`. So a run cargo could not make writes no `build-finished` entry, and a run cargo made writes one. Test the raw report. Correct the table and the trade paragraph with the measurement.\n+- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508` — `path_led_by` writes again what `EnvVarGuard::prepend_path` already does at `crates/swissarmyhammer-common/src/test_utils.rs:320`. Each one puts `dir` in front of the search list and keeps the existing entries behind it. `PathGuard::prepend` at `crates/swissarmyhammer-common/src/test_utils.rs:358` wraps that call for `PATH`, and its doc comment names this test shape — \"shim a scripted executable in front of the real one\" — as one of the three kinds of `PATH` test it serves. The test also writes the literal `\"PATH\"`, where `test_utils` names the constant `PATH_ENV`. Remove `path_led_by`, and write `PathGuard::prepend(stubs.path())` in place of the `EnvVarGuard::set` call that leads `PATH`.\n","reverse_patch":"--- original\n+++ modified\n@@ -187,9 +187,3 @@\n - [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:36` — The `jq` step keeps the defect this change removed from the `cargo` step. The last two lines are one pipe that ends in `sort -u`, and the script writes `set -e` alone, with no `pipefail`. A shell pipeline takes the status of its last command, so the script answers exit 0 for each failure of `jq`. Measured over the healthy probe package, which gives one finding: with `jq` replaced by a command that exits 127, the script wrote 0 findings and exited 0, so the engine reads a dirty tree as clean. That is the same answer the rule body calls out for the earlier shape of the `cargo` step: \"A shell pipeline takes the status of its last command, so that shape answered exit 0 for every broken run.\" `builtin/validators/README.md` states: \"Write a pipe only where the tool cannot exit nonzero. Otherwise write a script: run the tool into a file, test the status, and exit nonzero yourself.\" `jq` exits nonzero when it breaks. Give the `jq` step the same status test the `cargo` step has.\n - [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:199` — The path \"src/lib.rs\" is hardcoded in the expected findings string, but is already named as COMPLEX_LIB_PATH at line 65. If the path constant changes, this expected output won't update automatically, creating a maintenance risk and potential test fragility. Define the expected row constant separately (e.g., const RUST_LENGTH_ANNOTATION_ROW_1: &str = \"src/lib.rs:1\";) to document the relationship between the path constant and expected output, or add a comment explaining the duplication if Rust const limitations prevent direct reference.\n - [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:249` — The path \"tests/it.rs\" is hardcoded twice in the expected findings array at line 249, but is already named as RUST_PROBE_INTEGRATION_TEST_PATH at line 238. If the path constant changes, both hardcoded occurrences must be updated separately, risking missed updates. Define named constants for each expected finding (e.g., const RUST_TEST_ROW_2: &str = \"tests/it.rs:2\"; const RUST_TEST_ROW_304: &str = \"tests/it.rs:304\";) to centralize the path and ensure consistency across all uses.\n-\n-## Review Findings (2026-08-11 19:44)\n-\n-- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The new gate lets a workspace pass when one member does not compile. The gate reads the FILTERED file. One member that compiles fills that file, so the status test never runs. Measured with clippy 0.1.97 and cargo 1.97.1, over a workspace of two members: the member `good` holds a function of 302 lines, and the member `bad` holds the same function beside `pub fn broken() -> i32 { \"not an integer\" }`. cargo exits 101 and writes `error: could not compile bad (lib) due to 1 previous error` to stderr. The script writes one finding, `good/src/lib.rs:1`, and exits 0. The file `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean, because clippy runs the four lints after the type check that member failed. This is the same silent zero the earlier pipe shape gave. The `scope: workspace` of this rule is what makes the shape reach a real repository, and this repository holds more than 20 members. The acceptance test `the_shipped_rust_complexity_tool_rule_breaks_on_a_workspace_it_cannot_compile` drives ONE package, so it passes over this shape. The RAW report holds the answer that the filtered file does not: over this probe the raw report holds 606 `compiler-message` entries and the error code `E0308`, while the filtered file holds one line. Test the raw report for a rustc error code. Add the two-member shape to the table in the section \"A workspace the tool cannot lint\", and add an acceptance test for it.\n-- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The same gate breaks a run that was correct and clean. A workspace that clippy linted correctly, that holds a lint at deny level, and that holds no finding of the four gates gives a nonzero status beside an empty filtered file. The gate answers that shape as a broken run. Measured with clippy 0.1.97 and cargo 1.97.1: (a) a package with `[lints.clippy] unwrap_used = \"deny\"` and one `unwrap`, and no function over any of the four gates — cargo exits 101, and the script writes `complexity-rust: cargo clippy could not lint the workspace` and exits 1; (b) `RUSTFLAGS=\"-D warnings\"` over one `unused_variables` warning in the same shape — the same answer. Each of the two workspaces is clean at the four gates, so the correct answer is 0 findings at exit 0. The rule states this shape as a trade in the section \"A workspace the tool cannot lint\". The trade is not necessary, because the RAW report tells the two runs apart and the script reads only the filtered file. Measured over four shapes: the deny-level package writes 10244 bytes into the raw report, with the error code `clippy::unwrap_used` and one `{\"reason\":\"build-finished\",\"success\":false}` entry; the `-D warnings` package writes 4574 bytes, with the error code `unused_variables` and the same `build-finished` entry; a directory that holds no `Cargo.toml` writes 0 bytes and NO `build-finished` entry; the two-member workspace above writes 1100479 bytes with the error code `E0308`. So a run cargo could not make writes no `build-finished` entry, and a run cargo made writes one. Test the raw report. Correct the table and the trade paragraph with the measurement.\n-- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508` — `path_led_by` writes again what `EnvVarGuard::prepend_path` already does at `crates/swissarmyhammer-common/src/test_utils.rs:320`. Each one puts `dir` in front of the search list and keeps the existing entries behind it. `PathGuard::prepend` at `crates/swissarmyhammer-common/src/test_utils.rs:358` wraps that call for `PATH`, and its doc comment names this test shape — \"shim a scripted executable in front of the real one\" — as one of the three kinds of `PATH` test it serves. The test also writes the literal `\"PATH\"`, where `test_utils` names the constant `PATH_ENV`. Remove `path_led_by`, and write `PathGuard::prepend(stubs.path())` in place of the `EnvVarGuard::set` call that leads `PATH`.\n","transaction_id":null}
{"id":"01KZSQD6ZFZH0HFQTCKRX5EDCX","timestamp":"2026-08-12T00:54:24.495587Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -165,6 +165,33 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 658 run, 658 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3421 run, 3417 passed, 4 failed, all four failing before this change. `cargo fmt --all` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n     - next: /review\n   timestamp: 2026-08-12T00:41:36.394480+00:00\n+- actor: claude-code\n+  id: 01kzsqd6zerg87qnsfdeqyj8th\n+  text: |-\n+    ### review — findings\n+    - scope: `review sha HEAD~1..HEAD` (commit 6b228de75), task-mode on ^w5v73k1. Round 2.\n+    - engine: 8 validators attempted, 1 finding, 1 confirmed, 0 refuted, 0 failed. That finding stands at row 508 of `shipped/complexity.rs`, which is inside the added hunk (rows 439 to 560), so no finding was dropped for the existing-test exception this round.\n+    - open findings: `builtin/validators/code-hygiene/rules/complexity-rust.md:39` (two), `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508`.\n+    - the four findings of round 1 are each closed and each measured true again.\n+\n+    Every claim of the delta was measured again with clippy 0.1.97 and cargo 1.97.1, over probe packages under the scratchpad, with the run script taken from the SHIPPED bytes of the rule. Each claim measured TRUE:\n+\n+    - The filter step writes to a file, tests its own status, and writes `complexity-rust: jq could not read the clippy report` before exit 1. Measured with a `jq` on PATH that exits 127, over a healthy package: 0 findings, that line, exit 1. `sort -u` reads that file and stands last with no pipe above it.\n+    - The eight shapes of the table each answer as the table states. The healthy package: 1 finding, exit 0. `[lints.clippy] unwrap_used = \"deny\"` beside one `unwrap` and a function of 302 lines: 1 finding at `src/lib.rs:2`, exit 0. `RUSTFLAGS=\"-D warnings\"`: the same shape. A package that does not compile, a directory with no `Cargo.toml`, a `Cargo.toml` that does not parse, and a target directory cargo cannot write: 0 findings, exit 1.\n+    - The raw report of a package that does not compile holds `compiler-message` entries of `E0308` alone, and no message of the four lints. So the gate must not read the raw report for the four lints. Measured.\n+    - `set -e` never ends the script before the status is captured. Each of the two steps carries `|| status=$?` or `|| filtered=$?`, and each guard holds. Measured over three shapes: `cargo` absent from PATH gives status 127 and exit 1 with the named line; a `cargo` that answers 143, the status of a command a signal stopped, gives exit 1 with the named line; a directory with no `Cargo.toml` gives exit 1.\n+    - A lock another cargo holds is not a failure. Measured over one package with a second `cargo clippy` running against the same target directory: the script waited for the lock, then wrote 1 finding and exited 0.\n+    - The `PATH` test restores `PATH`. `EnvVarGuard` captures the earlier value and its `Drop` puts that value back, or removes the variable when there was none, and the test binds the guard to `_path`, which holds it to the end of the test. `serial_test::serial(env)` is the right guard: `env` is the group name this crate already gives to every test that writes an environment variable, and the crate holds two more members of that group. The test cannot leak into a test beside it, because `cargo nextest` runs each test in its own process.\n+\n+    Two shapes slip between the two halves of the new compound gate, and each is recorded as a finding:\n+\n+    - A workspace of two members, one of which does not compile, gives exit 0 and hides the member. Measured: `good` holds a function of 302 lines and `bad` holds the same function beside a type error. The run wrote `good/src/lib.rs:1` alone and exited 0.\n+    - A workspace that lints correctly, holds a lint at deny level, and holds no finding of the four lints, gives exit 1. Measured over `[lints.clippy] unwrap_used = \"deny\"` and over `RUSTFLAGS=\"-D warnings\"`, each beside a package clean at the four gates.\n+\n+    The raw report tells the two runs apart, and the script reads only the filtered file. A run cargo could not make writes 0 bytes and no `build-finished` entry. A run cargo made writes a `{\"reason\":\"build-finished\",\"success\":false}` entry beside its own error code: `clippy::unwrap_used` for the deny shape, `unused_variables` for the `-D warnings` shape, and `E0308` for the member that does not compile.\n+\n+    - next: /implement ^w5v73k1 for the 3 open findings.\n+  timestamp: 2026-08-12T00:54:24.494973+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -165,33 +165,6 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 658 run, 658 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3421 run, 3417 passed, 4 failed, all four failing before this change. `cargo fmt --all` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n     - next: /review\n   timestamp: 2026-08-12T00:41:36.394480+00:00\n-- actor: claude-code\n-  id: 01kzsqd6zerg87qnsfdeqyj8th\n-  text: |-\n-    ### review — findings\n-    - scope: `review sha HEAD~1..HEAD` (commit 6b228de75), task-mode on ^w5v73k1. Round 2.\n-    - engine: 8 validators attempted, 1 finding, 1 confirmed, 0 refuted, 0 failed. That finding stands at row 508 of `shipped/complexity.rs`, which is inside the added hunk (rows 439 to 560), so no finding was dropped for the existing-test exception this round.\n-    - open findings: `builtin/validators/code-hygiene/rules/complexity-rust.md:39` (two), `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508`.\n-    - the four findings of round 1 are each closed and each measured true again.\n-\n-    Every claim of the delta was measured again with clippy 0.1.97 and cargo 1.97.1, over probe packages under the scratchpad, with the run script taken from the SHIPPED bytes of the rule. Each claim measured TRUE:\n-\n-    - The filter step writes to a file, tests its own status, and writes `complexity-rust: jq could not read the clippy report` before exit 1. Measured with a `jq` on PATH that exits 127, over a healthy package: 0 findings, that line, exit 1. `sort -u` reads that file and stands last with no pipe above it.\n-    - The eight shapes of the table each answer as the table states. The healthy package: 1 finding, exit 0. `[lints.clippy] unwrap_used = \"deny\"` beside one `unwrap` and a function of 302 lines: 1 finding at `src/lib.rs:2`, exit 0. `RUSTFLAGS=\"-D warnings\"`: the same shape. A package that does not compile, a directory with no `Cargo.toml`, a `Cargo.toml` that does not parse, and a target directory cargo cannot write: 0 findings, exit 1.\n-    - The raw report of a package that does not compile holds `compiler-message` entries of `E0308` alone, and no message of the four lints. So the gate must not read the raw report for the four lints. Measured.\n-    - `set -e` never ends the script before the status is captured. Each of the two steps carries `|| status=$?` or `|| filtered=$?`, and each guard holds. Measured over three shapes: `cargo` absent from PATH gives status 127 and exit 1 with the named line; a `cargo` that answers 143, the status of a command a signal stopped, gives exit 1 with the named line; a directory with no `Cargo.toml` gives exit 1.\n-    - A lock another cargo holds is not a failure. Measured over one package with a second `cargo clippy` running against the same target directory: the script waited for the lock, then wrote 1 finding and exited 0.\n-    - The `PATH` test restores `PATH`. `EnvVarGuard` captures the earlier value and its `Drop` puts that value back, or removes the variable when there was none, and the test binds the guard to `_path`, which holds it to the end of the test. `serial_test::serial(env)` is the right guard: `env` is the group name this crate already gives to every test that writes an environment variable, and the crate holds two more members of that group. The test cannot leak into a test beside it, because `cargo nextest` runs each test in its own process.\n-\n-    Two shapes slip between the two halves of the new compound gate, and each is recorded as a finding:\n-\n-    - A workspace of two members, one of which does not compile, gives exit 0 and hides the member. Measured: `good` holds a function of 302 lines and `bad` holds the same function beside a type error. The run wrote `good/src/lib.rs:1` alone and exited 0.\n-    - A workspace that lints correctly, holds a lint at deny level, and holds no finding of the four lints, gives exit 1. Measured over `[lints.clippy] unwrap_used = \"deny\"` and over `RUSTFLAGS=\"-D warnings\"`, each beside a package clean at the four gates.\n-\n-    The raw report tells the two runs apart, and the script reads only the filtered file. A run cargo could not make writes 0 bytes and no `build-finished` entry. A run cargo made writes a `{\"reason\":\"build-finished\",\"success\":false}` entry beside its own error code: `clippy::unwrap_used` for the deny shape, `unused_variables` for the `-D warnings` shape, and `E0308` for the member that does not compile.\n-\n-    - next: /implement ^w5v73k1 for the 3 open findings.\n-  timestamp: 2026-08-12T00:54:24.494973+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSQE9VPY4VFAM7GTFFM5GAE","timestamp":"2026-08-12T00:55:00.214357Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -192,6 +192,15 @@\n\n     - next: /implement ^w5v73k1 for the 3 open findings.\n   timestamp: 2026-08-12T00:54:24.494973+00:00\n+- actor: claude-code\n+  id: 01kzsqe9vn8t0g7v6a91x2kb88\n+  text: |-\n+    ### finish iteration 2 — findings\n+    - implement: changed — 2 files; the four findings of iteration 1 are closed, the status gate now reads the filtered findings file\n+    - test: green — cargo nextest -p swissarmyhammer-validators 658 run, 658 passed; rdeps 3421 run, 3417 passed with only the 4 failures that came before\n+    - commit: 6b228de75\n+    - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:39 (two), crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508. The compound gate lets two shapes through: a workspace whose member does not compile passes when another member fills the findings file, and a correct run beside a deny-level lint with no finding of the four gates breaks. The raw report holds the discriminator the script deletes: a run cargo made writes a `build-finished` entry, and a run cargo could not make writes none.\n+  timestamp: 2026-08-12T00:55:00.213712+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -192,15 +192,6 @@\n\n     - next: /implement ^w5v73k1 for the 3 open findings.\n   timestamp: 2026-08-12T00:54:24.494973+00:00\n-- actor: claude-code\n-  id: 01kzsqe9vn8t0g7v6a91x2kb88\n-  text: |-\n-    ### finish iteration 2 — findings\n-    - implement: changed — 2 files; the four findings of iteration 1 are closed, the status gate now reads the filtered findings file\n-    - test: green — cargo nextest -p swissarmyhammer-validators 658 run, 658 passed; rdeps 3421 run, 3417 passed with only the 4 failures that came before\n-    - commit: 6b228de75\n-    - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:39 (two), crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508. The compound gate lets two shapes through: a workspace whose member does not compile passes when another member fills the findings file, and a correct run beside a deny-level lint with no finding of the four gates breaks. The raw report holds the discriminator the script deletes: a run cargo made writes a `build-finished` entry, and a run cargo could not make writes none.\n-  timestamp: 2026-08-12T00:55:00.213712+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSQFDQDRH8625VAP38473PE","timestamp":"2026-08-12T00:55:36.941632Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -201,8 +201,8 @@\n     - commit: 6b228de75\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:39 (two), crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508. The compound gate lets two shapes through: a workspace whose member does not compile passes when another member fills the findings file, and a correct run beside a deny-level lint with no finding of the four gates breaks. The raw report holds the discriminator the script deletes: a run cargo made writes a `build-finished` entry, and a run cargo could not make writes none.\n   timestamp: 2026-08-12T00:55:00.213712+00:00\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","reverse_patch":"--- original\n+++ modified\n@@ -201,8 +201,8 @@\n     - commit: 6b228de75\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:39 (two), crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508. The compound gate lets two shapes through: a workspace whose member does not compile passes when another member fills the findings file, and a correct run beside a deny-level lint with no finding of the four gates breaks. The raw report holds the discriminator the script deletes: a run cargo made writes a `build-finished` entry, and a run cargo could not make writes none.\n   timestamp: 2026-08-12T00:55:00.213712+00:00\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: review\n+position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","transaction_id":null}
{"id":"01KZSRA450GM5XAXMA00E513TP","timestamp":"2026-08-12T01:10:11.872484Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -226,6 +226,6 @@\n\n ## Review Findings (2026-08-11 19:44)\n\n-- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The new gate lets a workspace pass when one member does not compile. The gate reads the FILTERED file. One member that compiles fills that file, so the status test never runs. Measured with clippy 0.1.97 and cargo 1.97.1, over a workspace of two members: the member `good` holds a function of 302 lines, and the member `bad` holds the same function beside `pub fn broken() -> i32 { \"not an integer\" }`. cargo exits 101 and writes `error: could not compile bad (lib) due to 1 previous error` to stderr. The script writes one finding, `good/src/lib.rs:1`, and exits 0. The file `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean, because clippy runs the four lints after the type check that member failed. This is the same silent zero the earlier pipe shape gave. The `scope: workspace` of this rule is what makes the shape reach a real repository, and this repository holds more than 20 members. The acceptance test `the_shipped_rust_complexity_tool_rule_breaks_on_a_workspace_it_cannot_compile` drives ONE package, so it passes over this shape. The RAW report holds the answer that the filtered file does not: over this probe the raw report holds 606 `compiler-message` entries and the error code `E0308`, while the filtered file holds one line. Test the raw report for a rustc error code. Add the two-member shape to the table in the section \"A workspace the tool cannot lint\", and add an acceptance test for it.\n-- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The same gate breaks a run that was correct and clean. A workspace that clippy linted correctly, that holds a lint at deny level, and that holds no finding of the four gates gives a nonzero status beside an empty filtered file. The gate answers that shape as a broken run. Measured with clippy 0.1.97 and cargo 1.97.1: (a) a package with `[lints.clippy] unwrap_used = \"deny\"` and one `unwrap`, and no function over any of the four gates — cargo exits 101, and the script writes `complexity-rust: cargo clippy could not lint the workspace` and exits 1; (b) `RUSTFLAGS=\"-D warnings\"` over one `unused_variables` warning in the same shape — the same answer. Each of the two workspaces is clean at the four gates, so the correct answer is 0 findings at exit 0. The rule states this shape as a trade in the section \"A workspace the tool cannot lint\". The trade is not necessary, because the RAW report tells the two runs apart and the script reads only the filtered file. Measured over four shapes: the deny-level package writes 10244 bytes into the raw report, with the error code `clippy::unwrap_used` and one `{\"reason\":\"build-finished\",\"success\":false}` entry; the `-D warnings` package writes 4574 bytes, with the error code `unused_variables` and the same `build-finished` entry; a directory that holds no `Cargo.toml` writes 0 bytes and NO `build-finished` entry; the two-member workspace above writes 1100479 bytes with the error code `E0308`. So a run cargo could not make writes no `build-finished` entry, and a run cargo made writes one. Test the raw report. Correct the table and the trade paragraph with the measurement.\n-- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508` — `path_led_by` writes again what `EnvVarGuard::prepend_path` already does at `crates/swissarmyhammer-common/src/test_utils.rs:320`. Each one puts `dir` in front of the search list and keeps the existing entries behind it. `PathGuard::prepend` at `crates/swissarmyhammer-common/src/test_utils.rs:358` wraps that call for `PATH`, and its doc comment names this test shape — \"shim a scripted executable in front of the real one\" — as one of the three kinds of `PATH` test it serves. The test also writes the literal `\"PATH\"`, where `test_utils` names the constant `PATH_ENV`. Remove `path_led_by`, and write `PathGuard::prepend(stubs.path())` in place of the `EnvVarGuard::set` call that leads `PATH`.\n+- [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The new gate lets a workspace pass when one member does not compile. The gate reads the FILTERED file. One member that compiles fills that file, so the status test never runs. Measured with clippy 0.1.97 and cargo 1.97.1, over a workspace of two members: the member `good` holds a function of 302 lines, and the member `bad` holds the same function beside `pub fn broken() -> i32 { \"not an integer\" }`. cargo exits 101 and writes `error: could not compile bad (lib) due to 1 previous error` to stderr. The script writes one finding, `good/src/lib.rs:1`, and exits 0. The file `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean, because clippy runs the four lints after the type check that member failed. This is the same silent zero the earlier pipe shape gave. The `scope: workspace` of this rule is what makes the shape reach a real repository, and this repository holds more than 20 members. The acceptance test `the_shipped_rust_complexity_tool_rule_breaks_on_a_workspace_it_cannot_compile` drives ONE package, so it passes over this shape. The RAW report holds the answer that the filtered file does not: over this probe the raw report holds 606 `compiler-message` entries and the error code `E0308`, while the filtered file holds one line. Test the raw report for a rustc error code. Add the two-member shape to the table in the section \"A workspace the tool cannot lint\", and add an acceptance test for it.\n+- [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The same gate breaks a run that was correct and clean. A workspace that clippy linted correctly, that holds a lint at deny level, and that holds no finding of the four gates gives a nonzero status beside an empty filtered file. The gate answers that shape as a broken run. Measured with clippy 0.1.97 and cargo 1.97.1: (a) a package with `[lints.clippy] unwrap_used = \"deny\"` and one `unwrap`, and no function over any of the four gates — cargo exits 101, and the script writes `complexity-rust: cargo clippy could not lint the workspace` and exits 1; (b) `RUSTFLAGS=\"-D warnings\"` over one `unused_variables` warning in the same shape — the same answer. Each of the two workspaces is clean at the four gates, so the correct answer is 0 findings at exit 0. The rule states this shape as a trade in the section \"A workspace the tool cannot lint\". The trade is not necessary, because the RAW report tells the two runs apart and the script reads only the filtered file. Measured over four shapes: the deny-level package writes 10244 bytes into the raw report, with the error code `clippy::unwrap_used` and one `{\"reason\":\"build-finished\",\"success\":false}` entry; the `-D warnings` package writes 4574 bytes, with the error code `unused_variables` and the same `build-finished` entry; a directory that holds no `Cargo.toml` writes 0 bytes and NO `build-finished` entry; the two-member workspace above writes 1100479 bytes with the error code `E0308`. So a run cargo could not make writes no `build-finished` entry, and a run cargo made writes one. Test the raw report. Correct the table and the trade paragraph with the measurement.\n+- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508` — `path_led_by` writes again what `EnvVarGuard::prepend_path` already does at `crates/swissarmyhammer-common/src/test_utils.rs:320`. Each one puts `dir` in front of the search list and keeps the existing entries behind it. `PathGuard::prepend` at `crates/swissarmyhammer-common/src/test_utils.rs:358` wraps that call for `PATH`, and its doc comment names this test shape — \"shim a scripted executable in front of the real one\" — as one of the three kinds of `PATH` test it serves. The test also writes the literal `\"PATH\"`, where `test_utils` names the constant `PATH_ENV`. Remove `path_led_by`, and write `PathGuard::prepend(stubs.path())` in place of the `EnvVarGuard::set` call that leads `PATH`.\n","reverse_patch":"--- original\n+++ modified\n@@ -226,6 +226,6 @@\n\n ## Review Findings (2026-08-11 19:44)\n\n-- [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The new gate lets a workspace pass when one member does not compile. The gate reads the FILTERED file. One member that compiles fills that file, so the status test never runs. Measured with clippy 0.1.97 and cargo 1.97.1, over a workspace of two members: the member `good` holds a function of 302 lines, and the member `bad` holds the same function beside `pub fn broken() -> i32 { \"not an integer\" }`. cargo exits 101 and writes `error: could not compile bad (lib) due to 1 previous error` to stderr. The script writes one finding, `good/src/lib.rs:1`, and exits 0. The file `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean, because clippy runs the four lints after the type check that member failed. This is the same silent zero the earlier pipe shape gave. The `scope: workspace` of this rule is what makes the shape reach a real repository, and this repository holds more than 20 members. The acceptance test `the_shipped_rust_complexity_tool_rule_breaks_on_a_workspace_it_cannot_compile` drives ONE package, so it passes over this shape. The RAW report holds the answer that the filtered file does not: over this probe the raw report holds 606 `compiler-message` entries and the error code `E0308`, while the filtered file holds one line. Test the raw report for a rustc error code. Add the two-member shape to the table in the section \"A workspace the tool cannot lint\", and add an acceptance test for it.\n-- [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The same gate breaks a run that was correct and clean. A workspace that clippy linted correctly, that holds a lint at deny level, and that holds no finding of the four gates gives a nonzero status beside an empty filtered file. The gate answers that shape as a broken run. Measured with clippy 0.1.97 and cargo 1.97.1: (a) a package with `[lints.clippy] unwrap_used = \"deny\"` and one `unwrap`, and no function over any of the four gates — cargo exits 101, and the script writes `complexity-rust: cargo clippy could not lint the workspace` and exits 1; (b) `RUSTFLAGS=\"-D warnings\"` over one `unused_variables` warning in the same shape — the same answer. Each of the two workspaces is clean at the four gates, so the correct answer is 0 findings at exit 0. The rule states this shape as a trade in the section \"A workspace the tool cannot lint\". The trade is not necessary, because the RAW report tells the two runs apart and the script reads only the filtered file. Measured over four shapes: the deny-level package writes 10244 bytes into the raw report, with the error code `clippy::unwrap_used` and one `{\"reason\":\"build-finished\",\"success\":false}` entry; the `-D warnings` package writes 4574 bytes, with the error code `unused_variables` and the same `build-finished` entry; a directory that holds no `Cargo.toml` writes 0 bytes and NO `build-finished` entry; the two-member workspace above writes 1100479 bytes with the error code `E0308`. So a run cargo could not make writes no `build-finished` entry, and a run cargo made writes one. Test the raw report. Correct the table and the trade paragraph with the measurement.\n-- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508` — `path_led_by` writes again what `EnvVarGuard::prepend_path` already does at `crates/swissarmyhammer-common/src/test_utils.rs:320`. Each one puts `dir` in front of the search list and keeps the existing entries behind it. `PathGuard::prepend` at `crates/swissarmyhammer-common/src/test_utils.rs:358` wraps that call for `PATH`, and its doc comment names this test shape — \"shim a scripted executable in front of the real one\" — as one of the three kinds of `PATH` test it serves. The test also writes the literal `\"PATH\"`, where `test_utils` names the constant `PATH_ENV`. Remove `path_led_by`, and write `PathGuard::prepend(stubs.path())` in place of the `EnvVarGuard::set` call that leads `PATH`.\n+- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The new gate lets a workspace pass when one member does not compile. The gate reads the FILTERED file. One member that compiles fills that file, so the status test never runs. Measured with clippy 0.1.97 and cargo 1.97.1, over a workspace of two members: the member `good` holds a function of 302 lines, and the member `bad` holds the same function beside `pub fn broken() -> i32 { \"not an integer\" }`. cargo exits 101 and writes `error: could not compile bad (lib) due to 1 previous error` to stderr. The script writes one finding, `good/src/lib.rs:1`, and exits 0. The file `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean, because clippy runs the four lints after the type check that member failed. This is the same silent zero the earlier pipe shape gave. The `scope: workspace` of this rule is what makes the shape reach a real repository, and this repository holds more than 20 members. The acceptance test `the_shipped_rust_complexity_tool_rule_breaks_on_a_workspace_it_cannot_compile` drives ONE package, so it passes over this shape. The RAW report holds the answer that the filtered file does not: over this probe the raw report holds 606 `compiler-message` entries and the error code `E0308`, while the filtered file holds one line. Test the raw report for a rustc error code. Add the two-member shape to the table in the section \"A workspace the tool cannot lint\", and add an acceptance test for it.\n+- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The same gate breaks a run that was correct and clean. A workspace that clippy linted correctly, that holds a lint at deny level, and that holds no finding of the four gates gives a nonzero status beside an empty filtered file. The gate answers that shape as a broken run. Measured with clippy 0.1.97 and cargo 1.97.1: (a) a package with `[lints.clippy] unwrap_used = \"deny\"` and one `unwrap`, and no function over any of the four gates — cargo exits 101, and the script writes `complexity-rust: cargo clippy could not lint the workspace` and exits 1; (b) `RUSTFLAGS=\"-D warnings\"` over one `unused_variables` warning in the same shape — the same answer. Each of the two workspaces is clean at the four gates, so the correct answer is 0 findings at exit 0. The rule states this shape as a trade in the section \"A workspace the tool cannot lint\". The trade is not necessary, because the RAW report tells the two runs apart and the script reads only the filtered file. Measured over four shapes: the deny-level package writes 10244 bytes into the raw report, with the error code `clippy::unwrap_used` and one `{\"reason\":\"build-finished\",\"success\":false}` entry; the `-D warnings` package writes 4574 bytes, with the error code `unused_variables` and the same `build-finished` entry; a directory that holds no `Cargo.toml` writes 0 bytes and NO `build-finished` entry; the two-member workspace above writes 1100479 bytes with the error code `E0308`. So a run cargo could not make writes no `build-finished` entry, and a run cargo made writes one. Test the raw report. Correct the table and the trade paragraph with the measurement.\n+- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508` — `path_led_by` writes again what `EnvVarGuard::prepend_path` already does at `crates/swissarmyhammer-common/src/test_utils.rs:320`. Each one puts `dir` in front of the search list and keeps the existing entries behind it. `PathGuard::prepend` at `crates/swissarmyhammer-common/src/test_utils.rs:358` wraps that call for `PATH`, and its doc comment names this test shape — \"shim a scripted executable in front of the real one\" — as one of the three kinds of `PATH` test it serves. The test also writes the literal `\"PATH\"`, where `test_utils` names the constant `PATH_ENV`. Remove `path_led_by`, and write `PathGuard::prepend(stubs.path())` in place of the `EnvVarGuard::set` call that leads `PATH`.\n","transaction_id":null}
{"id":"01KZSRASPKRFDPNS4QFFQVX09X","timestamp":"2026-08-12T01:10:33.939616Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -201,6 +201,58 @@\n     - commit: 6b228de75\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:39 (two), crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508. The compound gate lets two shapes through: a workspace whose member does not compile passes when another member fills the findings file, and a correct run beside a deny-level lint with no finding of the four gates breaks. The raw report holds the discriminator the script deletes: a run cargo made writes a `build-finished` entry, and a run cargo could not make writes none.\n   timestamp: 2026-08-12T00:55:00.213712+00:00\n+- actor: claude-code\n+  id: 01kzsraspjdp2a13n2e7da4qdy\n+  text: |-\n+    The three findings are answered. I measured the raw cargo report myself before I built the gate on it, with clippy 0.1.97 and cargo 1.97.1, over 14 probe packages under the scratchpad.\n+\n+    WHAT `build-finished` HOLDS. The entry appears for EVERY run cargo made, a successful run included: a healthy package writes `{\"reason\":\"build-finished\",\"success\":true}`. Every nonzero shape cargo made writes `{\"reason\":\"build-finished\",\"success\":false}` — the three deny-level shapes and the shapes that do not compile alike. A directory with no `Cargo.toml`, a `Cargo.toml` that does not parse and a target directory cargo cannot write each write 0 bytes and NO entry. So the PRESENCE of the entry tells a run cargo made from a run cargo could not make, and the `success` field tells nothing the exit status does not. The `success` field alone would break every deny-level shape.\n+\n+    WHAT TELLS A COMPILE FAILURE FROM A DENY-LEVEL LINT. The error CODE beside the entry, which is the second half of the answer the review found. Measured, one count for each error-level `compiler-message`:\n+\n+    | the shape | error-level codes |\n+    |---|---|\n+    | `#![deny(clippy::unwrap_used)]` beside one `unwrap` | `clippy::unwrap_used` |\n+    | `[lints.clippy] unwrap_used = \"deny\"` beside one `unwrap` | `clippy::unwrap_used` |\n+    | `RUSTFLAGS=\"-D warnings\"` beside one `unused_variables` | `unused_variables`, `clippy::too_many_lines` |\n+    | a healthy package | none |\n+    | a package that does not compile | `E0308` |\n+    | a workspace of two members, one of which does not compile | `E0308` |\n+    | a package that does not parse: `pub fn broken( {` | no code, message `this file contains an unclosed delimiter` |\n+\n+    A lint at deny level always carries the LINT NAME as its code. A crate that fails its type check carries a rustc code, and a crate that fails to parse carries no code. This rustc writes no `aborting due to N previous errors` entry, so a null code marks a compile failure alone.\n+\n+    THE NEW GATE. Two tests, each on the raw report:\n+\n+    - the status is nonzero and the raw report holds no `build-finished` entry — `complexity-rust: cargo could not run clippy over the workspace`;\n+    - the raw report holds an error-level message with a rustc code (`^E[0-9]+$`) or with no code — `complexity-rust: cargo clippy could not lint the workspace`.\n+\n+    The gate no longer reads the filtered findings file, so a member that compiles cannot hide a member that does not.\n+\n+    MEASURED OVER EVERY SHAPE, with the run script taken from the SHIPPED bytes of the rule:\n+\n+    | the shape | before | after |\n+    |---|---|---|\n+    | a healthy package, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n+    | a healthy package, no finding | 0 findings, exit 0 | 0 findings, exit 0 |\n+    | `#![deny(clippy::unwrap_used)]`, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n+    | `[lints.clippy] unwrap_used = \"deny\"`, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n+    | `#![deny(clippy::unwrap_used)]`, no finding | 0 findings, exit 1 | 0 findings, exit 0 |\n+    | `[lints.clippy] unwrap_used = \"deny\"`, no finding | 0 findings, exit 1 | 0 findings, exit 0 |\n+    | `RUSTFLAGS=\"-D warnings\"`, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n+    | `RUSTFLAGS=\"-D warnings\"`, no finding | 0 findings, exit 1 | 0 findings, exit 0 |\n+    | a package that does not compile | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | a package that does not parse | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | a workspace of two members, one of which does not compile | 1 finding, exit 0 | 0 findings, exit 1 |\n+    | a directory with no `Cargo.toml` | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | a `Cargo.toml` that does not parse | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | a target directory cargo cannot write | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | `jq` that exits 127 | 0 findings, exit 1 | 0 findings, exit 1 |\n+\n+    Three rows move, and each one moves to the correct answer. The rule body states this table.\n+\n+    TWO FACTS THE EARLIER CARD TEXT DOES NOT STATE. The `jq` step now runs three times — the `build-finished` entry, the rustc error codes, and the findings — and each call shares one status test, so a filter that breaks still breaks the run. And clippy counts the BODY lines of a function alone: a function of 302 source lines, one `pub fn` line plus 300 body lines plus one brace line, reports `(300/250)`, not `(302/250)`.\n+  timestamp: 2026-08-12T01:10:33.938929+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -201,58 +201,6 @@\n     - commit: 6b228de75\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:39 (two), crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508. The compound gate lets two shapes through: a workspace whose member does not compile passes when another member fills the findings file, and a correct run beside a deny-level lint with no finding of the four gates breaks. The raw report holds the discriminator the script deletes: a run cargo made writes a `build-finished` entry, and a run cargo could not make writes none.\n   timestamp: 2026-08-12T00:55:00.213712+00:00\n-- actor: claude-code\n-  id: 01kzsraspjdp2a13n2e7da4qdy\n-  text: |-\n-    The three findings are answered. I measured the raw cargo report myself before I built the gate on it, with clippy 0.1.97 and cargo 1.97.1, over 14 probe packages under the scratchpad.\n-\n-    WHAT `build-finished` HOLDS. The entry appears for EVERY run cargo made, a successful run included: a healthy package writes `{\"reason\":\"build-finished\",\"success\":true}`. Every nonzero shape cargo made writes `{\"reason\":\"build-finished\",\"success\":false}` — the three deny-level shapes and the shapes that do not compile alike. A directory with no `Cargo.toml`, a `Cargo.toml` that does not parse and a target directory cargo cannot write each write 0 bytes and NO entry. So the PRESENCE of the entry tells a run cargo made from a run cargo could not make, and the `success` field tells nothing the exit status does not. The `success` field alone would break every deny-level shape.\n-\n-    WHAT TELLS A COMPILE FAILURE FROM A DENY-LEVEL LINT. The error CODE beside the entry, which is the second half of the answer the review found. Measured, one count for each error-level `compiler-message`:\n-\n-    | the shape | error-level codes |\n-    |---|---|\n-    | `#![deny(clippy::unwrap_used)]` beside one `unwrap` | `clippy::unwrap_used` |\n-    | `[lints.clippy] unwrap_used = \"deny\"` beside one `unwrap` | `clippy::unwrap_used` |\n-    | `RUSTFLAGS=\"-D warnings\"` beside one `unused_variables` | `unused_variables`, `clippy::too_many_lines` |\n-    | a healthy package | none |\n-    | a package that does not compile | `E0308` |\n-    | a workspace of two members, one of which does not compile | `E0308` |\n-    | a package that does not parse: `pub fn broken( {` | no code, message `this file contains an unclosed delimiter` |\n-\n-    A lint at deny level always carries the LINT NAME as its code. A crate that fails its type check carries a rustc code, and a crate that fails to parse carries no code. This rustc writes no `aborting due to N previous errors` entry, so a null code marks a compile failure alone.\n-\n-    THE NEW GATE. Two tests, each on the raw report:\n-\n-    - the status is nonzero and the raw report holds no `build-finished` entry — `complexity-rust: cargo could not run clippy over the workspace`;\n-    - the raw report holds an error-level message with a rustc code (`^E[0-9]+$`) or with no code — `complexity-rust: cargo clippy could not lint the workspace`.\n-\n-    The gate no longer reads the filtered findings file, so a member that compiles cannot hide a member that does not.\n-\n-    MEASURED OVER EVERY SHAPE, with the run script taken from the SHIPPED bytes of the rule:\n-\n-    | the shape | before | after |\n-    |---|---|---|\n-    | a healthy package, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n-    | a healthy package, no finding | 0 findings, exit 0 | 0 findings, exit 0 |\n-    | `#![deny(clippy::unwrap_used)]`, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n-    | `[lints.clippy] unwrap_used = \"deny\"`, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n-    | `#![deny(clippy::unwrap_used)]`, no finding | 0 findings, exit 1 | 0 findings, exit 0 |\n-    | `[lints.clippy] unwrap_used = \"deny\"`, no finding | 0 findings, exit 1 | 0 findings, exit 0 |\n-    | `RUSTFLAGS=\"-D warnings\"`, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n-    | `RUSTFLAGS=\"-D warnings\"`, no finding | 0 findings, exit 1 | 0 findings, exit 0 |\n-    | a package that does not compile | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | a package that does not parse | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | a workspace of two members, one of which does not compile | 1 finding, exit 0 | 0 findings, exit 1 |\n-    | a directory with no `Cargo.toml` | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | a `Cargo.toml` that does not parse | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | a target directory cargo cannot write | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | `jq` that exits 127 | 0 findings, exit 1 | 0 findings, exit 1 |\n-\n-    Three rows move, and each one moves to the correct answer. The rule body states this table.\n-\n-    TWO FACTS THE EARLIER CARD TEXT DOES NOT STATE. The `jq` step now runs three times — the `build-finished` entry, the rustc error codes, and the findings — and each call shares one status test, so a filter that breaks still breaks the run. And clippy counts the BODY lines of a function alone: a function of 302 source lines, one `pub fn` line plus 300 body lines plus one brace line, reports `(300/250)`, not `(302/250)`.\n-  timestamp: 2026-08-12T01:10:33.938929+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSRB9H43ZVC4FS1GK24257E","timestamp":"2026-08-12T01:10:50.148090Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -253,6 +253,29 @@\n\n     TWO FACTS THE EARLIER CARD TEXT DOES NOT STATE. The `jq` step now runs three times — the `build-finished` entry, the rustc error codes, and the findings — and each call shares one status test, so a filter that breaks still breaks the run. And clippy counts the BODY lines of a function alone: a function of 302 source lines, one `pub fn` line plus 300 body lines plus one brace line, reports `(300/250)`, not `(302/250)`.\n   timestamp: 2026-08-12T01:10:33.938929+00:00\n+- actor: claude-code\n+  id: 01kzsrb9h373am0c2d7bd954x2\n+  text: |-\n+    The RED-to-GREEN proof of the three new acceptance tests. Each RED ran against the SHIPPED bytes BEFORE the rule changed, with `touch crates/swissarmyhammer-validators/src/builtin/mod.rs` before the run.\n+\n+    1. `the_shipped_rust_complexity_tool_rule_breaks_on_a_workspace_member_it_cannot_compile`. The probe stages a workspace of two members: `good` holds a function of 302 source lines, and `bad` holds the same function beside `pub fn broken() -> i32 { \"not an integer\" }`.\n+       RED: `a workspace member the compiler refuses must break the run: [\"good/src/lib.rs:1\"]` — the script reported the member that compiles and exited 0, and the long function of `bad/src/lib.rs` was read as clean.\n+       GREEN: the run breaks with `complexity-rust: cargo clippy could not lint the workspace`.\n+\n+    2. `the_shipped_rust_complexity_tool_rule_measures_a_clean_workspace_beside_a_deny_level_lint`. The probe stages a manifest holding `[lints.clippy] unwrap_used = \"deny\"` and one line that calls `unwrap`, with no function over any of the four gates.\n+       RED: `the shipped Rust complexity script must judge the probe package and exit 0: Exit(\"error: could not compile `deny-probe` (lib) due to 1 previous error; 1 warning emitted ... complexity-rust: cargo clippy could not lint the workspace\")`.\n+       GREEN: 0 findings, exit 0.\n+\n+    3. `the_shipped_rust_complexity_tool_rule_measures_a_workspace_beside_deny_level_flags`. The probe sets `RUSTFLAGS=\"-D warnings\"` under an `EnvVarGuard` with `serial_test::serial(env)`, over one `unused_variables` line beside a long function. This test passed at the first run, because a shape that holds a finding already answered correctly under the earlier gate. It stands for the row of the table that no other test holds: under `-D warnings` the four gates arrive at level `error` rather than `warning`, and the filter selects on the lint CODE, so the finding must stand.\n+\n+    The five earlier Rust probes pass unchanged.\n+\n+    The third finding is answered as well: `path_led_by` is removed, and the filter test now writes `PathGuard::prepend(stubs.path())`, which is the helper `crates/swissarmyhammer-common/src/test_utils.rs` names for this test shape. The literal `\"PATH\"` is gone with it.\n+\n+    Tests: `cargo nextest run -p swissarmyhammer-validators` 661 run, 661 passed, 0 skipped (658 before; the three new tests are the difference). `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3424 run, 3420 passed, 4 failed — the three `review_e2e` tests and `review_progress_is_received_by_a_real_client_over_a_byte_stream_transport`, all four already failing in this tree and not from this change. `cargo fmt --all --check` clean. `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` exit 0.\n+\n+    The set-wide guards still hold with no edit: the script writes no `\"$@\"` and no zero-argument guard for its `workspace` scope, `work=\"$(mktemp -d)\"` keeps its `trap` on the line directly under it, and `doctor.check_command` still names `cargo-clippy jq sort mktemp`, each of which the script runs.\n+  timestamp: 2026-08-12T01:10:50.147291+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -253,29 +253,6 @@\n\n     TWO FACTS THE EARLIER CARD TEXT DOES NOT STATE. The `jq` step now runs three times — the `build-finished` entry, the rustc error codes, and the findings — and each call shares one status test, so a filter that breaks still breaks the run. And clippy counts the BODY lines of a function alone: a function of 302 source lines, one `pub fn` line plus 300 body lines plus one brace line, reports `(300/250)`, not `(302/250)`.\n   timestamp: 2026-08-12T01:10:33.938929+00:00\n-- actor: claude-code\n-  id: 01kzsrb9h373am0c2d7bd954x2\n-  text: |-\n-    The RED-to-GREEN proof of the three new acceptance tests. Each RED ran against the SHIPPED bytes BEFORE the rule changed, with `touch crates/swissarmyhammer-validators/src/builtin/mod.rs` before the run.\n-\n-    1. `the_shipped_rust_complexity_tool_rule_breaks_on_a_workspace_member_it_cannot_compile`. The probe stages a workspace of two members: `good` holds a function of 302 source lines, and `bad` holds the same function beside `pub fn broken() -> i32 { \"not an integer\" }`.\n-       RED: `a workspace member the compiler refuses must break the run: [\"good/src/lib.rs:1\"]` — the script reported the member that compiles and exited 0, and the long function of `bad/src/lib.rs` was read as clean.\n-       GREEN: the run breaks with `complexity-rust: cargo clippy could not lint the workspace`.\n-\n-    2. `the_shipped_rust_complexity_tool_rule_measures_a_clean_workspace_beside_a_deny_level_lint`. The probe stages a manifest holding `[lints.clippy] unwrap_used = \"deny\"` and one line that calls `unwrap`, with no function over any of the four gates.\n-       RED: `the shipped Rust complexity script must judge the probe package and exit 0: Exit(\"error: could not compile `deny-probe` (lib) due to 1 previous error; 1 warning emitted ... complexity-rust: cargo clippy could not lint the workspace\")`.\n-       GREEN: 0 findings, exit 0.\n-\n-    3. `the_shipped_rust_complexity_tool_rule_measures_a_workspace_beside_deny_level_flags`. The probe sets `RUSTFLAGS=\"-D warnings\"` under an `EnvVarGuard` with `serial_test::serial(env)`, over one `unused_variables` line beside a long function. This test passed at the first run, because a shape that holds a finding already answered correctly under the earlier gate. It stands for the row of the table that no other test holds: under `-D warnings` the four gates arrive at level `error` rather than `warning`, and the filter selects on the lint CODE, so the finding must stand.\n-\n-    The five earlier Rust probes pass unchanged.\n-\n-    The third finding is answered as well: `path_led_by` is removed, and the filter test now writes `PathGuard::prepend(stubs.path())`, which is the helper `crates/swissarmyhammer-common/src/test_utils.rs` names for this test shape. The literal `\"PATH\"` is gone with it.\n-\n-    Tests: `cargo nextest run -p swissarmyhammer-validators` 661 run, 661 passed, 0 skipped (658 before; the three new tests are the difference). `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3424 run, 3420 passed, 4 failed — the three `review_e2e` tests and `review_progress_is_received_by_a_real_client_over_a_byte_stream_transport`, all four already failing in this tree and not from this change. `cargo fmt --all --check` clean. `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` exit 0.\n-\n-    The set-wide guards still hold with no edit: the script writes no `\"$@\"` and no zero-argument guard for its `workspace` scope, `work=\"$(mktemp -d)\"` keeps its `trap` on the line directly under it, and `doctor.check_command` still names `cargo-clippy jq sort mktemp`, each of which the script runs.\n-  timestamp: 2026-08-12T01:10:50.147291+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSRCD8KEA90R613ZW44ZX3R","timestamp":"2026-08-12T01:11:26.739177Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -276,6 +276,14 @@\n\n     The set-wide guards still hold with no edit: the script writes no `\"$@\"` and no zero-argument guard for its `workspace` scope, `work=\"$(mktemp -d)\"` keeps its `trap` on the line directly under it, and `doctor.check_command` still names `cargo-clippy jq sort mktemp`, each of which the script runs.\n   timestamp: 2026-08-12T01:10:50.147291+00:00\n+- actor: claude-code\n+  id: 01kzsrcd8jjn7ne5nh7dkbjykb\n+  text: |-\n+    ### implement — changed\n+    - evidence: 2 files. `builtin/validators/code-hygiene/rules/complexity-rust.md` — the gate reads the RAW clippy report: it breaks when the status is nonzero and no `build-finished` entry stands, and it breaks when an error-level message carries a rustc code or no code; it no longer reads the filtered findings file. The section \"A workspace the tool cannot lint\" states the 15 measured shapes as one table, and the trade paragraph is gone. `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs` — three new acceptance tests over the shipped bytes (a workspace member that cannot compile, a clean workspace beside a deny-level lint, `RUSTFLAGS=\"-D warnings\"`), and `path_led_by` is replaced by `PathGuard::prepend`.\n+    - tests: `cargo nextest run -p swissarmyhammer-validators` 661 run, 661 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3424 run, 3420 passed, 4 failed, all four failing before this change. `cargo fmt --all --check` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n+    - next: /review\n+  timestamp: 2026-08-12T01:11:26.738383+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -276,14 +276,6 @@\n\n     The set-wide guards still hold with no edit: the script writes no `\"$@\"` and no zero-argument guard for its `workspace` scope, `work=\"$(mktemp -d)\"` keeps its `trap` on the line directly under it, and `doctor.check_command` still names `cargo-clippy jq sort mktemp`, each of which the script runs.\n   timestamp: 2026-08-12T01:10:50.147291+00:00\n-- actor: claude-code\n-  id: 01kzsrcd8jjn7ne5nh7dkbjykb\n-  text: |-\n-    ### implement — changed\n-    - evidence: 2 files. `builtin/validators/code-hygiene/rules/complexity-rust.md` — the gate reads the RAW clippy report: it breaks when the status is nonzero and no `build-finished` entry stands, and it breaks when an error-level message carries a rustc code or no code; it no longer reads the filtered findings file. The section \"A workspace the tool cannot lint\" states the 15 measured shapes as one table, and the trade paragraph is gone. `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs` — three new acceptance tests over the shipped bytes (a workspace member that cannot compile, a clean workspace beside a deny-level lint, `RUSTFLAGS=\"-D warnings\"`), and `path_led_by` is replaced by `PathGuard::prepend`.\n-    - tests: `cargo nextest run -p swissarmyhammer-validators` 661 run, 661 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3424 run, 3420 passed, 4 failed, all four failing before this change. `cargo fmt --all --check` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n-    - next: /review\n-  timestamp: 2026-08-12T01:11:26.738383+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSS1NSYDDCYX2HMDNJTERCX","timestamp":"2026-08-12T01:23:03.614169Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -284,8 +284,8 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 661 run, 661 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3424 run, 3420 passed, 4 failed, all four failing before this change. `cargo fmt --all --check` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n     - next: /review\n   timestamp: 2026-08-12T01:11:26.738383+00:00\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: review\n+position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","reverse_patch":"--- original\n+++ modified\n@@ -284,8 +284,8 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 661 run, 661 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3424 run, 3420 passed, 4 failed, all four failing before this change. `cargo fmt --all --check` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n     - next: /review\n   timestamp: 2026-08-12T01:11:26.738383+00:00\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","transaction_id":null}
{"id":"01KZSS3858G2C37KY585AJ01MA","timestamp":"2026-08-12T01:23:55.176657Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -312,3 +312,8 @@\n - [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The new gate lets a workspace pass when one member does not compile. The gate reads the FILTERED file. One member that compiles fills that file, so the status test never runs. Measured with clippy 0.1.97 and cargo 1.97.1, over a workspace of two members: the member `good` holds a function of 302 lines, and the member `bad` holds the same function beside `pub fn broken() -> i32 { \"not an integer\" }`. cargo exits 101 and writes `error: could not compile bad (lib) due to 1 previous error` to stderr. The script writes one finding, `good/src/lib.rs:1`, and exits 0. The file `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean, because clippy runs the four lints after the type check that member failed. This is the same silent zero the earlier pipe shape gave. The `scope: workspace` of this rule is what makes the shape reach a real repository, and this repository holds more than 20 members. The acceptance test `the_shipped_rust_complexity_tool_rule_breaks_on_a_workspace_it_cannot_compile` drives ONE package, so it passes over this shape. The RAW report holds the answer that the filtered file does not: over this probe the raw report holds 606 `compiler-message` entries and the error code `E0308`, while the filtered file holds one line. Test the raw report for a rustc error code. Add the two-member shape to the table in the section \"A workspace the tool cannot lint\", and add an acceptance test for it.\n - [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The same gate breaks a run that was correct and clean. A workspace that clippy linted correctly, that holds a lint at deny level, and that holds no finding of the four gates gives a nonzero status beside an empty filtered file. The gate answers that shape as a broken run. Measured with clippy 0.1.97 and cargo 1.97.1: (a) a package with `[lints.clippy] unwrap_used = \"deny\"` and one `unwrap`, and no function over any of the four gates — cargo exits 101, and the script writes `complexity-rust: cargo clippy could not lint the workspace` and exits 1; (b) `RUSTFLAGS=\"-D warnings\"` over one `unused_variables` warning in the same shape — the same answer. Each of the two workspaces is clean at the four gates, so the correct answer is 0 findings at exit 0. The rule states this shape as a trade in the section \"A workspace the tool cannot lint\". The trade is not necessary, because the RAW report tells the two runs apart and the script reads only the filtered file. Measured over four shapes: the deny-level package writes 10244 bytes into the raw report, with the error code `clippy::unwrap_used` and one `{\"reason\":\"build-finished\",\"success\":false}` entry; the `-D warnings` package writes 4574 bytes, with the error code `unused_variables` and the same `build-finished` entry; a directory that holds no `Cargo.toml` writes 0 bytes and NO `build-finished` entry; the two-member workspace above writes 1100479 bytes with the error code `E0308`. So a run cargo could not make writes no `build-finished` entry, and a run cargo made writes one. Test the raw report. Correct the table and the trade paragraph with the measurement.\n - [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508` — `path_led_by` writes again what `EnvVarGuard::prepend_path` already does at `crates/swissarmyhammer-common/src/test_utils.rs:320`. Each one puts `dir` in front of the search list and keeps the existing entries behind it. `PathGuard::prepend` at `crates/swissarmyhammer-common/src/test_utils.rs:358` wraps that call for `PATH`, and its doc comment names this test shape — \"shim a scripted executable in front of the real one\" — as one of the three kinds of `PATH` test it serves. The test also writes the literal `\"PATH\"`, where `test_utils` names the constant `PATH_ENV`. Remove `path_led_by`, and write `PathGuard::prepend(stubs.path())` in place of the `EnvVarGuard::set` call that leads `PATH`.\n+\n+## Review Findings (2026-08-11 20:30)\n+\n+- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:51` — The new gate lets a workspace pass when a member never reached the lints because its BUILD SCRIPT failed. The rule body at line 193 states three reasons for a nonzero status. A fourth reason stands: cargo made a build, and the build script of a crate exited nonzero, so clippy never linted that crate. cargo writes NO `compiler-message` entry for that failure — it writes its own words to stderr alone — so `$work/rustc-errors.txt` holds no byte and the second test does not fire. cargo writes `{\"reason\":\"build-finished\",\"success\":false}`, so `$work/ran.txt` is not empty and the first test does not fire. The run then writes the findings of the other members and exits 0. Measured with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1, with the run script taken from the SHIPPED bytes of the rule, over probe packages under the scratchpad. (a) One package that holds a function of 300 body lines beside a `build.rs` that runs `std::process::exit(3)`: the script wrote 0 findings and exited 0. The raw report held 1135 bytes, one `build-finished` entry with `success: false`, one `compiler-artifact` entry and no `compiler-message` entry at all. CONTROL: the same package with `fn main() {}` as its build script wrote `src/lib.rs:1` and exited 0. So the run lost one finding and read the package as clean. (b) A workspace of two members, `good` and `bad`, each holding the same function of 300 body lines, where the build script of `bad` runs `std::process::exit(3)`: the script wrote `good/src/lib.rs:1` alone and exited 0. CONTROL: the same workspace with `fn main() {}` as the build script of `bad` wrote both `bad/src/lib.rs:1` and `good/src/lib.rs:1`. So `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean. This is the same shape the two findings of round 2 name, with a fourth cause. This repository holds eight build scripts — `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs` — so the `scope: workspace` of this rule makes the shape reach this repository. The raw report tells the two runs apart. Measured over every shape of the table: each run cargo made and clippy linted from end to end writes either `success: true`, or `success: false` beside at least one error-level `compiler-message` whose code is a LINT name (`clippy::unwrap_used`, `unused_variables`, `clippy::too_many_lines`); each run where a crate never reached the lints writes `success: false` beside NO error-level `compiler-message`. So add a third test: the status is nonzero, a `build-finished` entry stands, and the raw report holds no error-level `compiler-message` — that run is broken. Add the shape to the table in the section \"A workspace the tool cannot lint\", correct the statement of three reasons at line 193 to four, and add an acceptance test for it.\n+- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1` — This file exceeds the review prompt cap — 277370 rendered bytes against the 262144-byte per-file cap — so these validators could not review it: duplication. Split the file into smaller modules that fit the review prompt cap.\n","reverse_patch":"--- original\n+++ modified\n@@ -312,8 +312,3 @@\n - [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The new gate lets a workspace pass when one member does not compile. The gate reads the FILTERED file. One member that compiles fills that file, so the status test never runs. Measured with clippy 0.1.97 and cargo 1.97.1, over a workspace of two members: the member `good` holds a function of 302 lines, and the member `bad` holds the same function beside `pub fn broken() -> i32 { \"not an integer\" }`. cargo exits 101 and writes `error: could not compile bad (lib) due to 1 previous error` to stderr. The script writes one finding, `good/src/lib.rs:1`, and exits 0. The file `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean, because clippy runs the four lints after the type check that member failed. This is the same silent zero the earlier pipe shape gave. The `scope: workspace` of this rule is what makes the shape reach a real repository, and this repository holds more than 20 members. The acceptance test `the_shipped_rust_complexity_tool_rule_breaks_on_a_workspace_it_cannot_compile` drives ONE package, so it passes over this shape. The RAW report holds the answer that the filtered file does not: over this probe the raw report holds 606 `compiler-message` entries and the error code `E0308`, while the filtered file holds one line. Test the raw report for a rustc error code. Add the two-member shape to the table in the section \"A workspace the tool cannot lint\", and add an acceptance test for it.\n - [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:39` — The same gate breaks a run that was correct and clean. A workspace that clippy linted correctly, that holds a lint at deny level, and that holds no finding of the four gates gives a nonzero status beside an empty filtered file. The gate answers that shape as a broken run. Measured with clippy 0.1.97 and cargo 1.97.1: (a) a package with `[lints.clippy] unwrap_used = \"deny\"` and one `unwrap`, and no function over any of the four gates — cargo exits 101, and the script writes `complexity-rust: cargo clippy could not lint the workspace` and exits 1; (b) `RUSTFLAGS=\"-D warnings\"` over one `unused_variables` warning in the same shape — the same answer. Each of the two workspaces is clean at the four gates, so the correct answer is 0 findings at exit 0. The rule states this shape as a trade in the section \"A workspace the tool cannot lint\". The trade is not necessary, because the RAW report tells the two runs apart and the script reads only the filtered file. Measured over four shapes: the deny-level package writes 10244 bytes into the raw report, with the error code `clippy::unwrap_used` and one `{\"reason\":\"build-finished\",\"success\":false}` entry; the `-D warnings` package writes 4574 bytes, with the error code `unused_variables` and the same `build-finished` entry; a directory that holds no `Cargo.toml` writes 0 bytes and NO `build-finished` entry; the two-member workspace above writes 1100479 bytes with the error code `E0308`. So a run cargo could not make writes no `build-finished` entry, and a run cargo made writes one. Test the raw report. Correct the table and the trade paragraph with the measurement.\n - [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:508` — `path_led_by` writes again what `EnvVarGuard::prepend_path` already does at `crates/swissarmyhammer-common/src/test_utils.rs:320`. Each one puts `dir` in front of the search list and keeps the existing entries behind it. `PathGuard::prepend` at `crates/swissarmyhammer-common/src/test_utils.rs:358` wraps that call for `PATH`, and its doc comment names this test shape — \"shim a scripted executable in front of the real one\" — as one of the three kinds of `PATH` test it serves. The test also writes the literal `\"PATH\"`, where `test_utils` names the constant `PATH_ENV`. Remove `path_led_by`, and write `PathGuard::prepend(stubs.path())` in place of the `EnvVarGuard::set` call that leads `PATH`.\n-\n-## Review Findings (2026-08-11 20:30)\n-\n-- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:51` — The new gate lets a workspace pass when a member never reached the lints because its BUILD SCRIPT failed. The rule body at line 193 states three reasons for a nonzero status. A fourth reason stands: cargo made a build, and the build script of a crate exited nonzero, so clippy never linted that crate. cargo writes NO `compiler-message` entry for that failure — it writes its own words to stderr alone — so `$work/rustc-errors.txt` holds no byte and the second test does not fire. cargo writes `{\"reason\":\"build-finished\",\"success\":false}`, so `$work/ran.txt` is not empty and the first test does not fire. The run then writes the findings of the other members and exits 0. Measured with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1, with the run script taken from the SHIPPED bytes of the rule, over probe packages under the scratchpad. (a) One package that holds a function of 300 body lines beside a `build.rs` that runs `std::process::exit(3)`: the script wrote 0 findings and exited 0. The raw report held 1135 bytes, one `build-finished` entry with `success: false`, one `compiler-artifact` entry and no `compiler-message` entry at all. CONTROL: the same package with `fn main() {}` as its build script wrote `src/lib.rs:1` and exited 0. So the run lost one finding and read the package as clean. (b) A workspace of two members, `good` and `bad`, each holding the same function of 300 body lines, where the build script of `bad` runs `std::process::exit(3)`: the script wrote `good/src/lib.rs:1` alone and exited 0. CONTROL: the same workspace with `fn main() {}` as the build script of `bad` wrote both `bad/src/lib.rs:1` and `good/src/lib.rs:1`. So `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean. This is the same shape the two findings of round 2 name, with a fourth cause. This repository holds eight build scripts — `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs` — so the `scope: workspace` of this rule makes the shape reach this repository. The raw report tells the two runs apart. Measured over every shape of the table: each run cargo made and clippy linted from end to end writes either `success: true`, or `success: false` beside at least one error-level `compiler-message` whose code is a LINT name (`clippy::unwrap_used`, `unused_variables`, `clippy::too_many_lines`); each run where a crate never reached the lints writes `success: false` beside NO error-level `compiler-message`. So add a third test: the status is nonzero, a `build-finished` entry stands, and the raw report holds no error-level `compiler-message` — that run is broken. Add the shape to the table in the section \"A workspace the tool cannot lint\", correct the statement of three reasons at line 193 to four, and add an acceptance test for it.\n-- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1` — This file exceeds the review prompt cap — 277370 rendered bytes against the 262144-byte per-file cap — so these validators could not review it: duplication. Split the file into smaller modules that fit the review prompt cap.\n","transaction_id":null}
{"id":"01KZSS4EWM6R8J6H4JS1SZ7VD2","timestamp":"2026-08-12T01:24:34.836837Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -284,6 +284,63 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 661 run, 661 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3424 run, 3420 passed, 4 failed, all four failing before this change. `cargo fmt --all --check` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n     - next: /review\n   timestamp: 2026-08-12T01:11:26.738383+00:00\n+- actor: claude-code\n+  id: 01kzss4ewkev6eczhtzmjkfjp2\n+  text: |-\n+    ### review — findings\n+    - scope: `review sha HEAD~1..HEAD` (commit eab5cc608), task-mode on ^w5v73k1. Round 3.\n+    - engine: 8 validators attempted, 1 finding, 1 confirmed, 0 refuted, 0 failed, 1 file skipped. The one engine finding is the prompt cap of `shipped/complexity.rs`; the `duplication` validator could not read the file, so it did not review the new hunks.\n+    - open findings: `builtin/validators/code-hygiene/rules/complexity-rust.md:51`, `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1`.\n+    - the seven findings of rounds 1 and 2 are each closed and each measured true again.\n+\n+    I made every measurement myself with clippy 0.1.97, cargo 1.97.1, rustc 1.97.1 and jq 1.8.2, over 20 probe packages under the scratchpad. The run script came from the SHIPPED bytes of the rule: I pulled the `run:` block out of the rule file with `awk` and compared it to my copy, and the two are the same bytes.\n+\n+    THE 15-SHAPE TABLE. Each row reproduces exactly as the rule body states it. Each package that holds a finding holds one function whose body runs 300 lines, which clippy counts as 300 against the gate of 250.\n+\n+    | the shape | status | `build-finished` | error codes | measured answer | the table states |\n+    |---|---|---|---|---|---|\n+    | a healthy package, one finding | 0 | `success: true` | none | `src/lib.rs:1`, exit 0 | the same |\n+    | a healthy package, no finding | 0 | `success: true` | none | 0 findings, exit 0 | the same |\n+    | `#![deny(clippy::unwrap_used)]`, one finding | 101 | `success: false` | 2 x `clippy::unwrap_used` | `src/lib.rs:3`, exit 0 | the same |\n+    | `[lints.clippy] unwrap_used = \"deny\"`, one finding | 101 | `success: false` | 2 x `clippy::unwrap_used` | `src/lib.rs:2`, exit 0 | the same |\n+    | `#![deny(clippy::unwrap_used)]`, no finding | 101 | `success: false` | 2 x `clippy::unwrap_used` | 0 findings, exit 0 | the same |\n+    | `[lints.clippy] unwrap_used = \"deny\"`, no finding | 101 | `success: false` | 2 x `clippy::unwrap_used` | 0 findings, exit 0 | the same |\n+    | `RUSTFLAGS=\"-D warnings\"`, one finding | 101 | `success: false` | 2 x `unused_variables`, 2 x `clippy::too_many_lines` | `src/lib.rs:2`, exit 0 | the same |\n+    | `RUSTFLAGS=\"-D warnings\"`, no finding | 101 | `success: false` | 2 x `unused_variables` | 0 findings, exit 0 | the same |\n+    | a package that does not compile | 101 | `success: false` | 2 x `E0308` | 0 findings, exit 1 | the same |\n+    | a package that does not parse | 101 | `success: false` | 2 x no code | 0 findings, exit 1 | the same |\n+    | a workspace of two members, one that does not compile | 101 | `success: false` | 2 x `E0308` | 0 findings, exit 1 | the same |\n+    | a directory that holds no `Cargo.toml` | 101 | none, 0 bytes | none | 0 findings, exit 1 | the same |\n+    | a `Cargo.toml` that does not parse | 101 | none, 0 bytes | none | 0 findings, exit 1 | the same |\n+    | a target directory cargo cannot write | 101 | none, 0 bytes | none | 0 findings, exit 1 | the same |\n+    | `jq` replaced by a command that exits 127 | 0 | `success: true` | none | 0 findings, exit 1 | the same |\n+\n+    THE LOAD-BEARING CLAIM. \"A crate that fails to parse carries no code, and this rustc writes no `aborting due to` entry.\" MEASURED TRUE. I searched every one of the 20 raw reports for the string `aborting due to`: NO report holds it. I also listed every error-level `compiler-message` whose `code` is `null`, one probe at a time. Only two probes hold one: the package that does not parse holds `this file contains an unclosed delimiter`, and the workspace whose proc macro panics holds `proc macro panicked`. Every deny-level shape holds ZERO error-level message with a null code. So the second test does not fire for a deny-level run.\n+\n+    WHAT THE GATE DOES IF A FUTURE RUSTC WRITES THAT ENTRY. The second test would fire for every run rustc ends with an error, the deny-level runs included, because that entry carries level `error` and no code. Six rows of the table would then move to `0 findings, exit 1`: the four deny-level rows that hold a finding or not, and the two `RUSTFLAGS` rows. The rule would lose every finding of a workspace that stands a lint at deny level, which is the defect round 1 named. The acceptance tests `..._measures_a_workspace_beside_a_deny_level_lint`, `..._measures_a_clean_workspace_beside_a_deny_level_lint` and `..._measures_a_workspace_beside_deny_level_flags` would each break, so the change would not pass unseen.\n+\n+    THE ATTACK SHAPES YOU NAMED.\n+\n+    - A crate that fails to LINK. `cargo clippy` never links. Measured over one package that declares `extern \"C\" { fn a_symbol_that_does_not_exist_anywhere() -> i32; }` beside a bin target that calls it: the run wrote `src/lib.rs:8` and exited 0, with `{\"reason\":\"build-finished\",\"success\":true}`. clippy-driver emits metadata alone, so no link step runs and there is no such failure shape.\n+    - A proc macro that panics at expansion. CAUGHT. Measured over a workspace of three members, where `user` calls a `#[proc_macro]` that panics and `good` holds a long function: the raw report held one error-level `compiler-message` with a null code and the message `proc macro panicked`, and the script wrote 0 findings and exited 1 with `complexity-rust: cargo clippy could not lint the workspace`.\n+    - A dependency that fails to build. CAUGHT. Measured over one package whose path dependency holds `pub fn broken() -> i32 { \"not an integer\" }`: the raw report held one `E0308`, and the script wrote 0 findings and exited 1 with the same line.\n+    - A build script that fails. NOT CAUGHT. This is the open finding at line 51. See the finding for the two measurements and their two controls.\n+    - A deny-level lint that IS one of the four gates. Measured over `#![deny(clippy::too_many_lines)]` beside a function of 300 body lines: cargo exits 101, `build-finished` holds `success: false`, the error codes are 2 x `clippy::too_many_lines`, the script wrote `src/lib.rs:2` and exited 0. The finding stands, because the filter selects on the lint CODE and not on the level, and the second test does not fire, because a lint name is not a rustc code.\n+    - Can `ran.txt` and `rustc-errors.txt` both hold bytes, and which test wins? YES, and the SECOND test wins. Every shape where cargo made a build and a crate failed its type check fills both: `build-finished` stands, so `ran.txt` holds `ran`, and the rustc code fills `rustc-errors.txt`. The first test carries `[ \"$status\" -ne 0 ] && [ ! -s \"$work/ran.txt\" ]`, so it does not fire, and the second test writes `complexity-rust: cargo clippy could not lint the workspace`. Measured: the package that does not compile, the package that does not parse and the two-member workspace each wrote that line and not the other one.\n+\n+    THE OTHER CLAIMS OF THE DELTA.\n+\n+    - The two-member workspace moves from 1 finding at exit 0 to 0 findings at exit 1. MEASURED TRUE.\n+    - The two clean deny-level shapes move from broken to 0 findings at exit 0. MEASURED TRUE.\n+    - `build-finished` stands for every run cargo made and is absent for a run cargo could not make, and `success` tells nothing the exit status does not. MEASURED TRUE over all 15 shapes.\n+    - The copied `PATH` builder is gone. `path_led_by` is removed and the filter test writes `PathGuard::prepend(stubs.path())`. The literal `\"PATH\"` is gone with it.\n+\n+    TESTS. `touch crates/swissarmyhammer-validators/src/builtin/mod.rs`, then `cargo nextest run -p swissarmyhammer-validators -E 'test(/the_shipped_rust_complexity/)'` — 10 run, 10 passed, 651 skipped.\n+\n+    HUNK BOUNDARIES. `git diff --unified=0` on the test file gives the hunks 403-412, 440-510, 563-640, 654, 673 and 690. The one engine finding on that file stands at row 1 and names the whole file, so no finding was dropped for the existing-test exception this round.\n+\n+    - next: /implement ^w5v73k1 for the 2 open findings.\n+  timestamp: 2026-08-12T01:24:34.835963+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -284,63 +284,6 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 661 run, 661 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3424 run, 3420 passed, 4 failed, all four failing before this change. `cargo fmt --all --check` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n     - next: /review\n   timestamp: 2026-08-12T01:11:26.738383+00:00\n-- actor: claude-code\n-  id: 01kzss4ewkev6eczhtzmjkfjp2\n-  text: |-\n-    ### review — findings\n-    - scope: `review sha HEAD~1..HEAD` (commit eab5cc608), task-mode on ^w5v73k1. Round 3.\n-    - engine: 8 validators attempted, 1 finding, 1 confirmed, 0 refuted, 0 failed, 1 file skipped. The one engine finding is the prompt cap of `shipped/complexity.rs`; the `duplication` validator could not read the file, so it did not review the new hunks.\n-    - open findings: `builtin/validators/code-hygiene/rules/complexity-rust.md:51`, `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1`.\n-    - the seven findings of rounds 1 and 2 are each closed and each measured true again.\n-\n-    I made every measurement myself with clippy 0.1.97, cargo 1.97.1, rustc 1.97.1 and jq 1.8.2, over 20 probe packages under the scratchpad. The run script came from the SHIPPED bytes of the rule: I pulled the `run:` block out of the rule file with `awk` and compared it to my copy, and the two are the same bytes.\n-\n-    THE 15-SHAPE TABLE. Each row reproduces exactly as the rule body states it. Each package that holds a finding holds one function whose body runs 300 lines, which clippy counts as 300 against the gate of 250.\n-\n-    | the shape | status | `build-finished` | error codes | measured answer | the table states |\n-    |---|---|---|---|---|---|\n-    | a healthy package, one finding | 0 | `success: true` | none | `src/lib.rs:1`, exit 0 | the same |\n-    | a healthy package, no finding | 0 | `success: true` | none | 0 findings, exit 0 | the same |\n-    | `#![deny(clippy::unwrap_used)]`, one finding | 101 | `success: false` | 2 x `clippy::unwrap_used` | `src/lib.rs:3`, exit 0 | the same |\n-    | `[lints.clippy] unwrap_used = \"deny\"`, one finding | 101 | `success: false` | 2 x `clippy::unwrap_used` | `src/lib.rs:2`, exit 0 | the same |\n-    | `#![deny(clippy::unwrap_used)]`, no finding | 101 | `success: false` | 2 x `clippy::unwrap_used` | 0 findings, exit 0 | the same |\n-    | `[lints.clippy] unwrap_used = \"deny\"`, no finding | 101 | `success: false` | 2 x `clippy::unwrap_used` | 0 findings, exit 0 | the same |\n-    | `RUSTFLAGS=\"-D warnings\"`, one finding | 101 | `success: false` | 2 x `unused_variables`, 2 x `clippy::too_many_lines` | `src/lib.rs:2`, exit 0 | the same |\n-    | `RUSTFLAGS=\"-D warnings\"`, no finding | 101 | `success: false` | 2 x `unused_variables` | 0 findings, exit 0 | the same |\n-    | a package that does not compile | 101 | `success: false` | 2 x `E0308` | 0 findings, exit 1 | the same |\n-    | a package that does not parse | 101 | `success: false` | 2 x no code | 0 findings, exit 1 | the same |\n-    | a workspace of two members, one that does not compile | 101 | `success: false` | 2 x `E0308` | 0 findings, exit 1 | the same |\n-    | a directory that holds no `Cargo.toml` | 101 | none, 0 bytes | none | 0 findings, exit 1 | the same |\n-    | a `Cargo.toml` that does not parse | 101 | none, 0 bytes | none | 0 findings, exit 1 | the same |\n-    | a target directory cargo cannot write | 101 | none, 0 bytes | none | 0 findings, exit 1 | the same |\n-    | `jq` replaced by a command that exits 127 | 0 | `success: true` | none | 0 findings, exit 1 | the same |\n-\n-    THE LOAD-BEARING CLAIM. \"A crate that fails to parse carries no code, and this rustc writes no `aborting due to` entry.\" MEASURED TRUE. I searched every one of the 20 raw reports for the string `aborting due to`: NO report holds it. I also listed every error-level `compiler-message` whose `code` is `null`, one probe at a time. Only two probes hold one: the package that does not parse holds `this file contains an unclosed delimiter`, and the workspace whose proc macro panics holds `proc macro panicked`. Every deny-level shape holds ZERO error-level message with a null code. So the second test does not fire for a deny-level run.\n-\n-    WHAT THE GATE DOES IF A FUTURE RUSTC WRITES THAT ENTRY. The second test would fire for every run rustc ends with an error, the deny-level runs included, because that entry carries level `error` and no code. Six rows of the table would then move to `0 findings, exit 1`: the four deny-level rows that hold a finding or not, and the two `RUSTFLAGS` rows. The rule would lose every finding of a workspace that stands a lint at deny level, which is the defect round 1 named. The acceptance tests `..._measures_a_workspace_beside_a_deny_level_lint`, `..._measures_a_clean_workspace_beside_a_deny_level_lint` and `..._measures_a_workspace_beside_deny_level_flags` would each break, so the change would not pass unseen.\n-\n-    THE ATTACK SHAPES YOU NAMED.\n-\n-    - A crate that fails to LINK. `cargo clippy` never links. Measured over one package that declares `extern \"C\" { fn a_symbol_that_does_not_exist_anywhere() -> i32; }` beside a bin target that calls it: the run wrote `src/lib.rs:8` and exited 0, with `{\"reason\":\"build-finished\",\"success\":true}`. clippy-driver emits metadata alone, so no link step runs and there is no such failure shape.\n-    - A proc macro that panics at expansion. CAUGHT. Measured over a workspace of three members, where `user` calls a `#[proc_macro]` that panics and `good` holds a long function: the raw report held one error-level `compiler-message` with a null code and the message `proc macro panicked`, and the script wrote 0 findings and exited 1 with `complexity-rust: cargo clippy could not lint the workspace`.\n-    - A dependency that fails to build. CAUGHT. Measured over one package whose path dependency holds `pub fn broken() -> i32 { \"not an integer\" }`: the raw report held one `E0308`, and the script wrote 0 findings and exited 1 with the same line.\n-    - A build script that fails. NOT CAUGHT. This is the open finding at line 51. See the finding for the two measurements and their two controls.\n-    - A deny-level lint that IS one of the four gates. Measured over `#![deny(clippy::too_many_lines)]` beside a function of 300 body lines: cargo exits 101, `build-finished` holds `success: false`, the error codes are 2 x `clippy::too_many_lines`, the script wrote `src/lib.rs:2` and exited 0. The finding stands, because the filter selects on the lint CODE and not on the level, and the second test does not fire, because a lint name is not a rustc code.\n-    - Can `ran.txt` and `rustc-errors.txt` both hold bytes, and which test wins? YES, and the SECOND test wins. Every shape where cargo made a build and a crate failed its type check fills both: `build-finished` stands, so `ran.txt` holds `ran`, and the rustc code fills `rustc-errors.txt`. The first test carries `[ \"$status\" -ne 0 ] && [ ! -s \"$work/ran.txt\" ]`, so it does not fire, and the second test writes `complexity-rust: cargo clippy could not lint the workspace`. Measured: the package that does not compile, the package that does not parse and the two-member workspace each wrote that line and not the other one.\n-\n-    THE OTHER CLAIMS OF THE DELTA.\n-\n-    - The two-member workspace moves from 1 finding at exit 0 to 0 findings at exit 1. MEASURED TRUE.\n-    - The two clean deny-level shapes move from broken to 0 findings at exit 0. MEASURED TRUE.\n-    - `build-finished` stands for every run cargo made and is absent for a run cargo could not make, and `success` tells nothing the exit status does not. MEASURED TRUE over all 15 shapes.\n-    - The copied `PATH` builder is gone. `path_led_by` is removed and the filter test writes `PathGuard::prepend(stubs.path())`. The literal `\"PATH\"` is gone with it.\n-\n-    TESTS. `touch crates/swissarmyhammer-validators/src/builtin/mod.rs`, then `cargo nextest run -p swissarmyhammer-validators -E 'test(/the_shipped_rust_complexity/)'` — 10 run, 10 passed, 651 skipped.\n-\n-    HUNK BOUNDARIES. `git diff --unified=0` on the test file gives the hunks 403-412, 440-510, 563-640, 654, 673 and 690. The one engine finding on that file stands at row 1 and names the whole file, so no finding was dropped for the existing-test exception this round.\n-\n-    - next: /implement ^w5v73k1 for the 2 open findings.\n-  timestamp: 2026-08-12T01:24:34.835963+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSS5R0R6K7RVQECE1STAZRS","timestamp":"2026-08-12T01:25:16.952850Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -341,6 +341,15 @@\n\n     - next: /implement ^w5v73k1 for the 2 open findings.\n   timestamp: 2026-08-12T01:24:34.835963+00:00\n+- actor: claude-code\n+  id: 01kzss5r0qzq1nvav0ktz5r2td\n+  text: |-\n+    ### finish iteration 3 — findings\n+    - implement: changed — 2 files; the gate reads the raw cargo report in two tests, and the 3 findings of iteration 2 are closed\n+    - test: green — cargo nextest -p swissarmyhammer-validators 661 run, 661 passed; rdeps 3424 run, 3420 passed with only the 4 failures that came before\n+    - commit: eab5cc608\n+    - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:51, crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1. All 15 rows of the table reproduce, and the load-bearing claim holds over 20 probe packages. Two new shapes: a build script that fails writes no compiler-message and a `build-finished` entry, so both tests miss it and a dirty file reads as clean; and the test file is 277370 rendered bytes against the cap of 262144, so one validator could not read it.\n+  timestamp: 2026-08-12T01:25:16.951490+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -341,15 +341,6 @@\n\n     - next: /implement ^w5v73k1 for the 2 open findings.\n   timestamp: 2026-08-12T01:24:34.835963+00:00\n-- actor: claude-code\n-  id: 01kzss5r0qzq1nvav0ktz5r2td\n-  text: |-\n-    ### finish iteration 3 — findings\n-    - implement: changed — 2 files; the gate reads the raw cargo report in two tests, and the 3 findings of iteration 2 are closed\n-    - test: green — cargo nextest -p swissarmyhammer-validators 661 run, 661 passed; rdeps 3424 run, 3420 passed with only the 4 failures that came before\n-    - commit: eab5cc608\n-    - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:51, crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1. All 15 rows of the table reproduce, and the load-bearing claim holds over 20 probe packages. Two new shapes: a build script that fails writes no compiler-message and a `build-finished` entry, so both tests miss it and a dirty file reads as clean; and the test file is 277370 rendered bytes against the cap of 262144, so one validator could not read it.\n-  timestamp: 2026-08-12T01:25:16.951490+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSS735QJJG52K7TB51JCS80","timestamp":"2026-08-12T01:26:01.143329Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -350,8 +350,8 @@\n     - commit: eab5cc608\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:51, crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1. All 15 rows of the table reproduce, and the load-bearing claim holds over 20 probe packages. Two new shapes: a build script that fails writes no compiler-message and a `build-finished` entry, so both tests miss it and a dirty file reads as clean; and the test file is 277370 rendered bytes against the cap of 262144, so one validator could not read it.\n   timestamp: 2026-08-12T01:25:16.951490+00:00\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","reverse_patch":"--- original\n+++ modified\n@@ -350,8 +350,8 @@\n     - commit: eab5cc608\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:51, crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1. All 15 rows of the table reproduce, and the load-bearing claim holds over 20 probe packages. Two new shapes: a build script that fails writes no compiler-message and a `build-finished` entry, so both tests miss it and a dirty file reads as clean; and the test file is 277370 rendered bytes against the cap of 262144, so one validator could not read it.\n   timestamp: 2026-08-12T01:25:16.951490+00:00\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: review\n+position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","transaction_id":null}
{"id":"01KZSTJKX6FD9NM0QP2R2T9WTS","timestamp":"2026-08-12T01:49:47.302885Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -381,5 +381,5 @@\n\n ## Review Findings (2026-08-11 20:30)\n\n-- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:51` — The new gate lets a workspace pass when a member never reached the lints because its BUILD SCRIPT failed. The rule body at line 193 states three reasons for a nonzero status. A fourth reason stands: cargo made a build, and the build script of a crate exited nonzero, so clippy never linted that crate. cargo writes NO `compiler-message` entry for that failure — it writes its own words to stderr alone — so `$work/rustc-errors.txt` holds no byte and the second test does not fire. cargo writes `{\"reason\":\"build-finished\",\"success\":false}`, so `$work/ran.txt` is not empty and the first test does not fire. The run then writes the findings of the other members and exits 0. Measured with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1, with the run script taken from the SHIPPED bytes of the rule, over probe packages under the scratchpad. (a) One package that holds a function of 300 body lines beside a `build.rs` that runs `std::process::exit(3)`: the script wrote 0 findings and exited 0. The raw report held 1135 bytes, one `build-finished` entry with `success: false`, one `compiler-artifact` entry and no `compiler-message` entry at all. CONTROL: the same package with `fn main() {}` as its build script wrote `src/lib.rs:1` and exited 0. So the run lost one finding and read the package as clean. (b) A workspace of two members, `good` and `bad`, each holding the same function of 300 body lines, where the build script of `bad` runs `std::process::exit(3)`: the script wrote `good/src/lib.rs:1` alone and exited 0. CONTROL: the same workspace with `fn main() {}` as the build script of `bad` wrote both `bad/src/lib.rs:1` and `good/src/lib.rs:1`. So `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean. This is the same shape the two findings of round 2 name, with a fourth cause. This repository holds eight build scripts — `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs` — so the `scope: workspace` of this rule makes the shape reach this repository. The raw report tells the two runs apart. Measured over every shape of the table: each run cargo made and clippy linted from end to end writes either `success: true`, or `success: false` beside at least one error-level `compiler-message` whose code is a LINT name (`clippy::unwrap_used`, `unused_variables`, `clippy::too_many_lines`); each run where a crate never reached the lints writes `success: false` beside NO error-level `compiler-message`. So add a third test: the status is nonzero, a `build-finished` entry stands, and the raw report holds no error-level `compiler-message` — that run is broken. Add the shape to the table in the section \"A workspace the tool cannot lint\", correct the statement of three reasons at line 193 to four, and add an acceptance test for it.\n-- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1` — This file exceeds the review prompt cap — 277370 rendered bytes against the 262144-byte per-file cap — so these validators could not review it: duplication. Split the file into smaller modules that fit the review prompt cap.\n+- [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:51` — The new gate lets a workspace pass when a member never reached the lints because its BUILD SCRIPT failed. The rule body at line 193 states three reasons for a nonzero status. A fourth reason stands: cargo made a build, and the build script of a crate exited nonzero, so clippy never linted that crate. cargo writes NO `compiler-message` entry for that failure — it writes its own words to stderr alone — so `$work/rustc-errors.txt` holds no byte and the second test does not fire. cargo writes `{\"reason\":\"build-finished\",\"success\":false}`, so `$work/ran.txt` is not empty and the first test does not fire. The run then writes the findings of the other members and exits 0. Measured with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1, with the run script taken from the SHIPPED bytes of the rule, over probe packages under the scratchpad. (a) One package that holds a function of 300 body lines beside a `build.rs` that runs `std::process::exit(3)`: the script wrote 0 findings and exited 0. The raw report held 1135 bytes, one `build-finished` entry with `success: false`, one `compiler-artifact` entry and no `compiler-message` entry at all. CONTROL: the same package with `fn main() {}` as its build script wrote `src/lib.rs:1` and exited 0. So the run lost one finding and read the package as clean. (b) A workspace of two members, `good` and `bad`, each holding the same function of 300 body lines, where the build script of `bad` runs `std::process::exit(3)`: the script wrote `good/src/lib.rs:1` alone and exited 0. CONTROL: the same workspace with `fn main() {}` as the build script of `bad` wrote both `bad/src/lib.rs:1` and `good/src/lib.rs:1`. So `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean. This is the same shape the two findings of round 2 name, with a fourth cause. This repository holds eight build scripts — `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs` — so the `scope: workspace` of this rule makes the shape reach this repository. The raw report tells the two runs apart. Measured over every shape of the table: each run cargo made and clippy linted from end to end writes either `success: true`, or `success: false` beside at least one error-level `compiler-message` whose code is a LINT name (`clippy::unwrap_used`, `unused_variables`, `clippy::too_many_lines`); each run where a crate never reached the lints writes `success: false` beside NO error-level `compiler-message`. So add a third test: the status is nonzero, a `build-finished` entry stands, and the raw report holds no error-level `compiler-message` — that run is broken. Add the shape to the table in the section \"A workspace the tool cannot lint\", correct the statement of three reasons at line 193 to four, and add an acceptance test for it.\n+- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1` — This file exceeds the review prompt cap — 277370 rendered bytes against the 262144-byte per-file cap — so these validators could not review it: duplication. Split the file into smaller modules that fit the review prompt cap.\n","reverse_patch":"--- original\n+++ modified\n@@ -381,5 +381,5 @@\n\n ## Review Findings (2026-08-11 20:30)\n\n-- [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:51` — The new gate lets a workspace pass when a member never reached the lints because its BUILD SCRIPT failed. The rule body at line 193 states three reasons for a nonzero status. A fourth reason stands: cargo made a build, and the build script of a crate exited nonzero, so clippy never linted that crate. cargo writes NO `compiler-message` entry for that failure — it writes its own words to stderr alone — so `$work/rustc-errors.txt` holds no byte and the second test does not fire. cargo writes `{\"reason\":\"build-finished\",\"success\":false}`, so `$work/ran.txt` is not empty and the first test does not fire. The run then writes the findings of the other members and exits 0. Measured with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1, with the run script taken from the SHIPPED bytes of the rule, over probe packages under the scratchpad. (a) One package that holds a function of 300 body lines beside a `build.rs` that runs `std::process::exit(3)`: the script wrote 0 findings and exited 0. The raw report held 1135 bytes, one `build-finished` entry with `success: false`, one `compiler-artifact` entry and no `compiler-message` entry at all. CONTROL: the same package with `fn main() {}` as its build script wrote `src/lib.rs:1` and exited 0. So the run lost one finding and read the package as clean. (b) A workspace of two members, `good` and `bad`, each holding the same function of 300 body lines, where the build script of `bad` runs `std::process::exit(3)`: the script wrote `good/src/lib.rs:1` alone and exited 0. CONTROL: the same workspace with `fn main() {}` as the build script of `bad` wrote both `bad/src/lib.rs:1` and `good/src/lib.rs:1`. So `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean. This is the same shape the two findings of round 2 name, with a fourth cause. This repository holds eight build scripts — `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs` — so the `scope: workspace` of this rule makes the shape reach this repository. The raw report tells the two runs apart. Measured over every shape of the table: each run cargo made and clippy linted from end to end writes either `success: true`, or `success: false` beside at least one error-level `compiler-message` whose code is a LINT name (`clippy::unwrap_used`, `unused_variables`, `clippy::too_many_lines`); each run where a crate never reached the lints writes `success: false` beside NO error-level `compiler-message`. So add a third test: the status is nonzero, a `build-finished` entry stands, and the raw report holds no error-level `compiler-message` — that run is broken. Add the shape to the table in the section \"A workspace the tool cannot lint\", correct the statement of three reasons at line 193 to four, and add an acceptance test for it.\n-- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1` — This file exceeds the review prompt cap — 277370 rendered bytes against the 262144-byte per-file cap — so these validators could not review it: duplication. Split the file into smaller modules that fit the review prompt cap.\n+- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:51` — The new gate lets a workspace pass when a member never reached the lints because its BUILD SCRIPT failed. The rule body at line 193 states three reasons for a nonzero status. A fourth reason stands: cargo made a build, and the build script of a crate exited nonzero, so clippy never linted that crate. cargo writes NO `compiler-message` entry for that failure — it writes its own words to stderr alone — so `$work/rustc-errors.txt` holds no byte and the second test does not fire. cargo writes `{\"reason\":\"build-finished\",\"success\":false}`, so `$work/ran.txt` is not empty and the first test does not fire. The run then writes the findings of the other members and exits 0. Measured with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1, with the run script taken from the SHIPPED bytes of the rule, over probe packages under the scratchpad. (a) One package that holds a function of 300 body lines beside a `build.rs` that runs `std::process::exit(3)`: the script wrote 0 findings and exited 0. The raw report held 1135 bytes, one `build-finished` entry with `success: false`, one `compiler-artifact` entry and no `compiler-message` entry at all. CONTROL: the same package with `fn main() {}` as its build script wrote `src/lib.rs:1` and exited 0. So the run lost one finding and read the package as clean. (b) A workspace of two members, `good` and `bad`, each holding the same function of 300 body lines, where the build script of `bad` runs `std::process::exit(3)`: the script wrote `good/src/lib.rs:1` alone and exited 0. CONTROL: the same workspace with `fn main() {}` as the build script of `bad` wrote both `bad/src/lib.rs:1` and `good/src/lib.rs:1`. So `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean. This is the same shape the two findings of round 2 name, with a fourth cause. This repository holds eight build scripts — `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs` — so the `scope: workspace` of this rule makes the shape reach this repository. The raw report tells the two runs apart. Measured over every shape of the table: each run cargo made and clippy linted from end to end writes either `success: true`, or `success: false` beside at least one error-level `compiler-message` whose code is a LINT name (`clippy::unwrap_used`, `unused_variables`, `clippy::too_many_lines`); each run where a crate never reached the lints writes `success: false` beside NO error-level `compiler-message`. So add a third test: the status is nonzero, a `build-finished` entry stands, and the raw report holds no error-level `compiler-message` — that run is broken. Add the shape to the table in the section \"A workspace the tool cannot lint\", correct the statement of three reasons at line 193 to four, and add an acceptance test for it.\n+- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1` — This file exceeds the review prompt cap — 277370 rendered bytes against the 262144-byte per-file cap — so these validators could not review it: duplication. Split the file into smaller modules that fit the review prompt cap.\n","transaction_id":null}
{"id":"01KZSTK8PZ0RTXHH7Y1FGWY5N5","timestamp":"2026-08-12T01:50:08.607596Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -350,6 +350,26 @@\n     - commit: eab5cc608\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:51, crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1. All 15 rows of the table reproduce, and the load-bearing claim holds over 20 probe packages. Two new shapes: a build script that fails writes no compiler-message and a `build-finished` entry, so both tests miss it and a dirty file reads as clean; and the test file is 277370 rendered bytes against the cap of 262144, so one validator could not read it.\n   timestamp: 2026-08-12T01:25:16.951490+00:00\n+- actor: claude-code\n+  id: 01kzstk8pyfa0mqd03wyjf5ke3\n+  text: |-\n+    FINDING :51 — the build script that breaks. I measured the shape myself before I built the gate on it, with clippy 0.1.97, cargo 1.97.1 and jq 1.8.2, over 20 probe packages under the scratchpad. The run script came from the SHIPPED bytes: I pulled the `run:` block out of the rule with `awk`.\n+\n+    WHAT THE RAW REPORT HOLDS FOR THIS SHAPE. One package that holds a function of 300 body lines beside a `build.rs` that runs `std::process::exit(3)`: the whole report ran 1133 bytes and held one `build-finished` entry with `success: false`, one `compiler-artifact` entry whose `target.kind` holds `custom-build`, NO `build-script-executed` entry and NO `compiler-message` at all. The CONTROL, the same package under `fn main() {}`, ran 7489 bytes and held one `build-script-executed` entry beside its artifact. A build script that panics gives the same report shape as one that exits 3.\n+\n+    WHAT THE CARD'S DISCRIMINATOR MEASURES. The card states: \"each run where a crate never reached the lints writes `success: false` beside NO error-level `compiler-message`.\" Measured over all 20 probes, that statement is true of the BUILD SCRIPT shape alone. A package that does not compile writes `success: false` beside `E0308`; a package that does not parse writes one message with no code; the two-member workspace writes `E0308`. Each of those three is a run where a crate never reached the lints, and each holds an error-level message. So the ordered third test answers the build-script shape, and the earlier rustc-code test answers the other three.\n+\n+    ONE SHAPE THE ORDERED TEST LETS THROUGH. A workspace whose member holds a build script that breaks, BESIDE a lint at deny level in another member. Measured: the root manifest holds `[workspace.lints.clippy] unwrap_used = \"deny\"`, `good` holds one `unwrap` and a long function, and the build script of `bad` breaks. cargo writes `success: false` and one error-level message whose code is the LINT name `clippy::unwrap_used`, so the ordered test does not fire; the script wrote `good/src/lib.rs:2` alone and exited 0, and the long function of `bad/src/lib.rs` was read as clean. `RUSTFLAGS=\"-D warnings\"` gives the same shape for ANY warning: measured over the two-member build-script workspace under that flag, the codes were 2 x `clippy::too_many_lines` and the script wrote `good/src/lib.rs:1` alone at exit 0.\n+\n+    THE ANSWER. The script now makes BOTH tests, in this order:\n+\n+    - the raw report holds a `custom-build` artifact whose package writes no `build-script-executed` entry — `complexity-rust: a build script did not run, so clippy did not lint every crate`;\n+    - the status is nonzero and the raw report holds no error-level message at all — `complexity-rust: cargo stopped the build and wrote no compiler error`.\n+\n+    The two are not one test. Measured with the build-script test taken out: the fourth test broke the one-package shape and the two-member shape, and the shape beside the denied lint still wrote `good/src/lib.rs:2` at exit 0. Measured with the fourth test taken out: the build-script test broke all three. So each one answers a shape the other lets through.\n+\n+    THE PAIR IS SAFE FOR A CACHED RUN AND FOR A DEPENDENCY. Measured over one package that holds a build script beside a path dependency that holds another: the first run wrote 2 `custom-build` artifacts and 2 `build-script-executed` entries; a second run, whose artifacts arrived `fresh: true` under a fixed `CLIPPY_CONF_DIR`, wrote the same 2 entries. Over THIS repository the run wrote 149 `custom-build` artifacts and 149 `build-script-executed` entries, 58 findings and exit 0, so the new gate does not break the repository the rule runs on.\n+  timestamp: 2026-08-12T01:50:08.606639+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -350,26 +350,6 @@\n     - commit: eab5cc608\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:51, crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1. All 15 rows of the table reproduce, and the load-bearing claim holds over 20 probe packages. Two new shapes: a build script that fails writes no compiler-message and a `build-finished` entry, so both tests miss it and a dirty file reads as clean; and the test file is 277370 rendered bytes against the cap of 262144, so one validator could not read it.\n   timestamp: 2026-08-12T01:25:16.951490+00:00\n-- actor: claude-code\n-  id: 01kzstk8pyfa0mqd03wyjf5ke3\n-  text: |-\n-    FINDING :51 — the build script that breaks. I measured the shape myself before I built the gate on it, with clippy 0.1.97, cargo 1.97.1 and jq 1.8.2, over 20 probe packages under the scratchpad. The run script came from the SHIPPED bytes: I pulled the `run:` block out of the rule with `awk`.\n-\n-    WHAT THE RAW REPORT HOLDS FOR THIS SHAPE. One package that holds a function of 300 body lines beside a `build.rs` that runs `std::process::exit(3)`: the whole report ran 1133 bytes and held one `build-finished` entry with `success: false`, one `compiler-artifact` entry whose `target.kind` holds `custom-build`, NO `build-script-executed` entry and NO `compiler-message` at all. The CONTROL, the same package under `fn main() {}`, ran 7489 bytes and held one `build-script-executed` entry beside its artifact. A build script that panics gives the same report shape as one that exits 3.\n-\n-    WHAT THE CARD'S DISCRIMINATOR MEASURES. The card states: \"each run where a crate never reached the lints writes `success: false` beside NO error-level `compiler-message`.\" Measured over all 20 probes, that statement is true of the BUILD SCRIPT shape alone. A package that does not compile writes `success: false` beside `E0308`; a package that does not parse writes one message with no code; the two-member workspace writes `E0308`. Each of those three is a run where a crate never reached the lints, and each holds an error-level message. So the ordered third test answers the build-script shape, and the earlier rustc-code test answers the other three.\n-\n-    ONE SHAPE THE ORDERED TEST LETS THROUGH. A workspace whose member holds a build script that breaks, BESIDE a lint at deny level in another member. Measured: the root manifest holds `[workspace.lints.clippy] unwrap_used = \"deny\"`, `good` holds one `unwrap` and a long function, and the build script of `bad` breaks. cargo writes `success: false` and one error-level message whose code is the LINT name `clippy::unwrap_used`, so the ordered test does not fire; the script wrote `good/src/lib.rs:2` alone and exited 0, and the long function of `bad/src/lib.rs` was read as clean. `RUSTFLAGS=\"-D warnings\"` gives the same shape for ANY warning: measured over the two-member build-script workspace under that flag, the codes were 2 x `clippy::too_many_lines` and the script wrote `good/src/lib.rs:1` alone at exit 0.\n-\n-    THE ANSWER. The script now makes BOTH tests, in this order:\n-\n-    - the raw report holds a `custom-build` artifact whose package writes no `build-script-executed` entry — `complexity-rust: a build script did not run, so clippy did not lint every crate`;\n-    - the status is nonzero and the raw report holds no error-level message at all — `complexity-rust: cargo stopped the build and wrote no compiler error`.\n-\n-    The two are not one test. Measured with the build-script test taken out: the fourth test broke the one-package shape and the two-member shape, and the shape beside the denied lint still wrote `good/src/lib.rs:2` at exit 0. Measured with the fourth test taken out: the build-script test broke all three. So each one answers a shape the other lets through.\n-\n-    THE PAIR IS SAFE FOR A CACHED RUN AND FOR A DEPENDENCY. Measured over one package that holds a build script beside a path dependency that holds another: the first run wrote 2 `custom-build` artifacts and 2 `build-script-executed` entries; a second run, whose artifacts arrived `fresh: true` under a fixed `CLIPPY_CONF_DIR`, wrote the same 2 entries. Over THIS repository the run wrote 149 `custom-build` artifacts and 149 `build-script-executed` entries, 58 findings and exit 0, so the new gate does not break the repository the rule runs on.\n-  timestamp: 2026-08-12T01:50:08.606639+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSTM1VHC4YXV3XF39FMR198","timestamp":"2026-08-12T01:50:34.353164Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -370,6 +370,55 @@\n\n     THE PAIR IS SAFE FOR A CACHED RUN AND FOR A DEPENDENCY. Measured over one package that holds a build script beside a path dependency that holds another: the first run wrote 2 `custom-build` artifacts and 2 `build-script-executed` entries; a second run, whose artifacts arrived `fresh: true` under a fixed `CLIPPY_CONF_DIR`, wrote the same 2 entries. Over THIS repository the run wrote 149 `custom-build` artifacts and 149 `build-script-executed` entries, 58 findings and exit 0, so the new gate does not break the repository the rule runs on.\n   timestamp: 2026-08-12T01:50:08.606639+00:00\n+- actor: claude-code\n+  id: 01kzstm1vg4t2a64q0vnzk8q06\n+  text: |-\n+    THE WHOLE TABLE, MEASURED AGAIN AFTER THE CHANGE. 19 shapes, each run with the SHIPPED bytes before the change and the SHIPPED bytes after it, over probe packages under the scratchpad. Each package that holds a finding holds one function whose body runs 300 lines, which clippy counts as 300 against the gate of 250.\n+\n+    | the shape | before | after |\n+    |---|---|---|\n+    | a healthy package, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n+    | a healthy package, no finding | 0 findings, exit 0 | 0 findings, exit 0 |\n+    | `#![deny(clippy::unwrap_used)]`, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n+    | `#![deny(clippy::unwrap_used)]`, no finding | 0 findings, exit 0 | 0 findings, exit 0 |\n+    | `[lints.clippy] unwrap_used = \"deny\"`, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n+    | `[lints.clippy] unwrap_used = \"deny\"`, no finding | 0 findings, exit 0 | 0 findings, exit 0 |\n+    | `RUSTFLAGS=\"-D warnings\"`, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n+    | `RUSTFLAGS=\"-D warnings\"`, no finding | 0 findings, exit 0 | 0 findings, exit 0 |\n+    | a package that does not compile | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | a package that does not parse | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | a workspace of two members, one that does not compile | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | a directory that holds no `Cargo.toml` | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | a `Cargo.toml` that does not parse | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | a target directory cargo cannot write | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | `jq` replaced by a command that exits 127 | 0 findings, exit 1 | 0 findings, exit 1 |\n+    | a package whose build script breaks | 0 findings, exit 0 | 0 findings, exit 1 |\n+    | a package whose build script runs | 1 finding, exit 0 | 1 finding, exit 0 |\n+    | a workspace of two members, one whose build script breaks | 1 finding, exit 0 | 0 findings, exit 1 |\n+    | the same workspace beside a denied lint | 1 finding, exit 0 | 0 findings, exit 1 |\n+\n+    Three rows move, and each moves to the correct answer. The 15 rows of the earlier table hold their answers, and the two build-script controls hold theirs. Each of the three broken build-script rows writes `complexity-rust: a build script did not run, so clippy did not lint every crate`; the compile rows still write `complexity-rust: cargo clippy could not lint the workspace`.\n+\n+    THE RED-TO-GREEN PROOF OF THE FOUR NEW ACCEPTANCE TESTS. Each RED ran against the SHIPPED bytes BEFORE the rule changed, with `touch crates/swissarmyhammer-validators/src/builtin/mod.rs` before the run.\n+\n+    1. `the_shipped_rust_complexity_tool_rule_breaks_on_a_build_script_that_breaks`.\n+       RED: `a build script that breaks must break the run: []` — the script exited 0 with 0 findings.\n+       GREEN: the run breaks with the build-script line.\n+\n+    2. `the_shipped_rust_complexity_tool_rule_breaks_on_a_member_build_script_that_breaks`.\n+       RED: `a member build script that breaks must break the run: [\"good/src/lib.rs:1\"]`.\n+       GREEN: the run breaks with the build-script line.\n+\n+    3. `the_shipped_rust_complexity_tool_rule_breaks_on_a_broken_build_script_beside_a_denied_lint`.\n+       RED: `a build script that breaks must break the run, whatever else reported: [\"good/src/lib.rs:2\"]`.\n+       GREEN: the run breaks with the build-script line.\n+\n+    4. `the_shipped_rust_complexity_tool_rule_measures_a_package_beside_a_build_script_that_runs`. This is the control, and it passed before and after. It holds the same manifest and the same library as test 1, with `fn main() {}` as the build script, so a gate that broke every package carrying a build script would break it. This repository carries eight.\n+\n+    The six earlier Rust probes pass unchanged.\n+\n+    THE RULE BODY. The section \"A workspace the tool cannot lint\" now states four reasons for a nonzero status, three entries of the raw report, four places where the script breaks the run, and the 19-shape table. The section \"The status of each step\" states five `jq` calls under one status test. `doctor.check_command` needs no change: the new steps run `jq` alone.\n+  timestamp: 2026-08-12T01:50:34.352139+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -370,55 +370,6 @@\n\n     THE PAIR IS SAFE FOR A CACHED RUN AND FOR A DEPENDENCY. Measured over one package that holds a build script beside a path dependency that holds another: the first run wrote 2 `custom-build` artifacts and 2 `build-script-executed` entries; a second run, whose artifacts arrived `fresh: true` under a fixed `CLIPPY_CONF_DIR`, wrote the same 2 entries. Over THIS repository the run wrote 149 `custom-build` artifacts and 149 `build-script-executed` entries, 58 findings and exit 0, so the new gate does not break the repository the rule runs on.\n   timestamp: 2026-08-12T01:50:08.606639+00:00\n-- actor: claude-code\n-  id: 01kzstm1vg4t2a64q0vnzk8q06\n-  text: |-\n-    THE WHOLE TABLE, MEASURED AGAIN AFTER THE CHANGE. 19 shapes, each run with the SHIPPED bytes before the change and the SHIPPED bytes after it, over probe packages under the scratchpad. Each package that holds a finding holds one function whose body runs 300 lines, which clippy counts as 300 against the gate of 250.\n-\n-    | the shape | before | after |\n-    |---|---|---|\n-    | a healthy package, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n-    | a healthy package, no finding | 0 findings, exit 0 | 0 findings, exit 0 |\n-    | `#![deny(clippy::unwrap_used)]`, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n-    | `#![deny(clippy::unwrap_used)]`, no finding | 0 findings, exit 0 | 0 findings, exit 0 |\n-    | `[lints.clippy] unwrap_used = \"deny\"`, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n-    | `[lints.clippy] unwrap_used = \"deny\"`, no finding | 0 findings, exit 0 | 0 findings, exit 0 |\n-    | `RUSTFLAGS=\"-D warnings\"`, one finding | 1 finding, exit 0 | 1 finding, exit 0 |\n-    | `RUSTFLAGS=\"-D warnings\"`, no finding | 0 findings, exit 0 | 0 findings, exit 0 |\n-    | a package that does not compile | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | a package that does not parse | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | a workspace of two members, one that does not compile | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | a directory that holds no `Cargo.toml` | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | a `Cargo.toml` that does not parse | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | a target directory cargo cannot write | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | `jq` replaced by a command that exits 127 | 0 findings, exit 1 | 0 findings, exit 1 |\n-    | a package whose build script breaks | 0 findings, exit 0 | 0 findings, exit 1 |\n-    | a package whose build script runs | 1 finding, exit 0 | 1 finding, exit 0 |\n-    | a workspace of two members, one whose build script breaks | 1 finding, exit 0 | 0 findings, exit 1 |\n-    | the same workspace beside a denied lint | 1 finding, exit 0 | 0 findings, exit 1 |\n-\n-    Three rows move, and each moves to the correct answer. The 15 rows of the earlier table hold their answers, and the two build-script controls hold theirs. Each of the three broken build-script rows writes `complexity-rust: a build script did not run, so clippy did not lint every crate`; the compile rows still write `complexity-rust: cargo clippy could not lint the workspace`.\n-\n-    THE RED-TO-GREEN PROOF OF THE FOUR NEW ACCEPTANCE TESTS. Each RED ran against the SHIPPED bytes BEFORE the rule changed, with `touch crates/swissarmyhammer-validators/src/builtin/mod.rs` before the run.\n-\n-    1. `the_shipped_rust_complexity_tool_rule_breaks_on_a_build_script_that_breaks`.\n-       RED: `a build script that breaks must break the run: []` — the script exited 0 with 0 findings.\n-       GREEN: the run breaks with the build-script line.\n-\n-    2. `the_shipped_rust_complexity_tool_rule_breaks_on_a_member_build_script_that_breaks`.\n-       RED: `a member build script that breaks must break the run: [\"good/src/lib.rs:1\"]`.\n-       GREEN: the run breaks with the build-script line.\n-\n-    3. `the_shipped_rust_complexity_tool_rule_breaks_on_a_broken_build_script_beside_a_denied_lint`.\n-       RED: `a build script that breaks must break the run, whatever else reported: [\"good/src/lib.rs:2\"]`.\n-       GREEN: the run breaks with the build-script line.\n-\n-    4. `the_shipped_rust_complexity_tool_rule_measures_a_package_beside_a_build_script_that_runs`. This is the control, and it passed before and after. It holds the same manifest and the same library as test 1, with `fn main() {}` as the build script, so a gate that broke every package carrying a build script would break it. This repository carries eight.\n-\n-    The six earlier Rust probes pass unchanged.\n-\n-    THE RULE BODY. The section \"A workspace the tool cannot lint\" now states four reasons for a nonzero status, three entries of the raw report, four places where the script breaks the run, and the 19-shape table. The section \"The status of each step\" states five `jq` calls under one status test. `doctor.check_command` needs no change: the new steps run `jq` alone.\n-  timestamp: 2026-08-12T01:50:34.352139+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSTMMJ0G10HJ7AW6Q3D476G","timestamp":"2026-08-12T01:50:53.504097Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -419,6 +419,34 @@\n\n     THE RULE BODY. The section \"A workspace the tool cannot lint\" now states four reasons for a nonzero status, three entries of the raw report, four places where the script breaks the run, and the 19-shape table. The section \"The status of each step\" states five `jq` calls under one status test. `doctor.check_command` needs no change: the new steps run `jq` alone.\n   timestamp: 2026-08-12T01:50:34.352139+00:00\n+- actor: claude-code\n+  id: 01kzstmmhzcxspa65zdqvjkzn3\n+  text: |-\n+    FINDING :1 — the file over the review prompt cap. `shipped/complexity.rs` held 87668 bytes on disk, which the review rendered as 277370 bytes against the cap of 262144, so the `duplication` validator could not read it.\n+\n+    THE SPLIT. The file is now six files, each beside `zero_argument.rs`, `temp_directory.rs` and `scope_roster.rs`, and each registered where `complexity.rs` is registered today, in `tests/shipped.rs`:\n+\n+    | the module | bytes | tests |\n+    |---|---|---|\n+    | `complexity.rs` | 2124 | 1 |\n+    | `complexity_rust.rs` | 38406 | 14 |\n+    | `complexity_python.rs` | 17752 | 6 |\n+    | `complexity_swift.rs` | 14513 | 8 |\n+    | `complexity_typescript.rs` | 13377 | 3 |\n+    | `complexity_go.rs` | 12563 | 5 |\n+\n+    The largest module holds 38406 bytes, which is 44 percent of what the one file held. The rendered size of the one file ran 3.16 times its bytes on disk, so the largest module renders about 121000 bytes against the cap of 262144.\n+\n+    IT IS A MOVE. Each language section moved by line range, with `sed`, and no assertion changed. `complexity.rs` keeps two things: the roster test `every_shipped_complexity_tool_rule_passes_its_fixtures`, which reads every complexity rule and not one language, and the helper `complexity_work`, which the Rust module and the TypeScript module both drive. That helper is now `pub(super)`, which is the one change to any line that moved. Each new module opens with `use super::*;`, the same as the module it came from, and the two that drive the helper add `use super::complexity::complexity_work;`.\n+\n+    THE COUNT HOLDS. `grep -c '^#\\[test\\]'` gave 33 over the one file, and gives 1 + 14 + 6 + 8 + 3 + 5 = 37 over the six, where 4 are the new build-script tests of the other finding. `cargo nextest run -p swissarmyhammer-validators` gave 661 tests before this card and gives 665 now, and 665 - 661 = 4.\n+\n+    The doc comment of `tests/shipped.rs` states the shape: one module for each rule family, and the complexity family one module for each language it drives.\n+\n+    TESTS. `touch crates/swissarmyhammer-validators/src/builtin/mod.rs`, then `cargo nextest run -p swissarmyhammer-validators` — 665 run, 665 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` — 3428 run, 3424 passed, 4 failed: the three `review_e2e` tests and `review_progress_is_received_by_a_real_client_over_a_byte_stream_transport`, all four failing in this tree before this change. `cargo fmt --all` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n+\n+    The set-wide guards hold with no edit: `scope_roster.rs` and `temp_directory.rs` read the shipped script of every rule, and the script still writes no `\"$@\"`, keeps `work=\"$(mktemp -d)\"` with its `trap` on the line under it, and names `cargo-clippy jq sort mktemp` in `doctor.check_command`.\n+  timestamp: 2026-08-12T01:50:53.503052+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -419,34 +419,6 @@\n\n     THE RULE BODY. The section \"A workspace the tool cannot lint\" now states four reasons for a nonzero status, three entries of the raw report, four places where the script breaks the run, and the 19-shape table. The section \"The status of each step\" states five `jq` calls under one status test. `doctor.check_command` needs no change: the new steps run `jq` alone.\n   timestamp: 2026-08-12T01:50:34.352139+00:00\n-- actor: claude-code\n-  id: 01kzstmmhzcxspa65zdqvjkzn3\n-  text: |-\n-    FINDING :1 — the file over the review prompt cap. `shipped/complexity.rs` held 87668 bytes on disk, which the review rendered as 277370 bytes against the cap of 262144, so the `duplication` validator could not read it.\n-\n-    THE SPLIT. The file is now six files, each beside `zero_argument.rs`, `temp_directory.rs` and `scope_roster.rs`, and each registered where `complexity.rs` is registered today, in `tests/shipped.rs`:\n-\n-    | the module | bytes | tests |\n-    |---|---|---|\n-    | `complexity.rs` | 2124 | 1 |\n-    | `complexity_rust.rs` | 38406 | 14 |\n-    | `complexity_python.rs` | 17752 | 6 |\n-    | `complexity_swift.rs` | 14513 | 8 |\n-    | `complexity_typescript.rs` | 13377 | 3 |\n-    | `complexity_go.rs` | 12563 | 5 |\n-\n-    The largest module holds 38406 bytes, which is 44 percent of what the one file held. The rendered size of the one file ran 3.16 times its bytes on disk, so the largest module renders about 121000 bytes against the cap of 262144.\n-\n-    IT IS A MOVE. Each language section moved by line range, with `sed`, and no assertion changed. `complexity.rs` keeps two things: the roster test `every_shipped_complexity_tool_rule_passes_its_fixtures`, which reads every complexity rule and not one language, and the helper `complexity_work`, which the Rust module and the TypeScript module both drive. That helper is now `pub(super)`, which is the one change to any line that moved. Each new module opens with `use super::*;`, the same as the module it came from, and the two that drive the helper add `use super::complexity::complexity_work;`.\n-\n-    THE COUNT HOLDS. `grep -c '^#\\[test\\]'` gave 33 over the one file, and gives 1 + 14 + 6 + 8 + 3 + 5 = 37 over the six, where 4 are the new build-script tests of the other finding. `cargo nextest run -p swissarmyhammer-validators` gave 661 tests before this card and gives 665 now, and 665 - 661 = 4.\n-\n-    The doc comment of `tests/shipped.rs` states the shape: one module for each rule family, and the complexity family one module for each language it drives.\n-\n-    TESTS. `touch crates/swissarmyhammer-validators/src/builtin/mod.rs`, then `cargo nextest run -p swissarmyhammer-validators` — 665 run, 665 passed, 0 skipped. `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` — 3428 run, 3424 passed, 4 failed: the three `review_e2e` tests and `review_progress_is_received_by_a_real_client_over_a_byte_stream_transport`, all four failing in this tree before this change. `cargo fmt --all` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n-\n-    The set-wide guards hold with no edit: `scope_roster.rs` and `temp_directory.rs` read the shipped script of every rule, and the script still writes no `\"$@\"`, keeps `work=\"$(mktemp -d)\"` with its `trap` on the line under it, and names `cargo-clippy jq sort mktemp` in `doctor.check_command`.\n-  timestamp: 2026-08-12T01:50:53.503052+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSTPWM0PC67HN9T8RTQ06BJ","timestamp":"2026-08-12T01:52:07.296781Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -447,6 +447,14 @@\n\n     The set-wide guards hold with no edit: `scope_roster.rs` and `temp_directory.rs` read the shipped script of every rule, and the script still writes no `\"$@\"`, keeps `work=\"$(mktemp -d)\"` with its `trap` on the line under it, and names `cargo-clippy jq sort mktemp` in `doctor.check_command`.\n   timestamp: 2026-08-12T01:50:53.503052+00:00\n+- actor: claude-code\n+  id: 01kzstpwkzrzb9rc8p5z6kr526\n+  text: |-\n+    ### implement — changed\n+    - evidence: 7 files. `builtin/validators/code-hygiene/rules/complexity-rust.md` — the script adds two `jq` steps (the error-level messages, and the build scripts cargo compiled and never ran) and two gates: a `custom-build` artifact with no `build-script-executed` entry writes `complexity-rust: a build script did not run, so clippy did not lint every crate`, and a nonzero status with no error-level message writes `complexity-rust: cargo stopped the build and wrote no compiler error`. The body states four reasons for a nonzero status, three entries of the raw report, four places the script breaks the run, and the 19-shape table. `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs` — split into `complexity_rust.rs`, `complexity_python.rs`, `complexity_swift.rs`, `complexity_typescript.rs` and `complexity_go.rs`, each registered in `tests/shipped.rs`; `complexity.rs` keeps the roster test and the shared `complexity_work`. `complexity_rust.rs` holds four new acceptance tests over the shipped bytes.\n+    - tests: `cargo nextest run -p swissarmyhammer-validators` 665 run, 665 passed, 0 skipped (661 before; the four new tests are the difference). `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3428 run, 3424 passed, 4 failed, all four failing in this tree before this change. `cargo fmt --all -- --check` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n+    - next: /review\n+  timestamp: 2026-08-12T01:52:07.295672+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -447,14 +447,6 @@\n\n     The set-wide guards hold with no edit: `scope_roster.rs` and `temp_directory.rs` read the shipped script of every rule, and the script still writes no `\"$@\"`, keeps `work=\"$(mktemp -d)\"` with its `trap` on the line under it, and names `cargo-clippy jq sort mktemp` in `doctor.check_command`.\n   timestamp: 2026-08-12T01:50:53.503052+00:00\n-- actor: claude-code\n-  id: 01kzstpwkzrzb9rc8p5z6kr526\n-  text: |-\n-    ### implement — changed\n-    - evidence: 7 files. `builtin/validators/code-hygiene/rules/complexity-rust.md` — the script adds two `jq` steps (the error-level messages, and the build scripts cargo compiled and never ran) and two gates: a `custom-build` artifact with no `build-script-executed` entry writes `complexity-rust: a build script did not run, so clippy did not lint every crate`, and a nonzero status with no error-level message writes `complexity-rust: cargo stopped the build and wrote no compiler error`. The body states four reasons for a nonzero status, three entries of the raw report, four places the script breaks the run, and the 19-shape table. `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs` — split into `complexity_rust.rs`, `complexity_python.rs`, `complexity_swift.rs`, `complexity_typescript.rs` and `complexity_go.rs`, each registered in `tests/shipped.rs`; `complexity.rs` keeps the roster test and the shared `complexity_work`. `complexity_rust.rs` holds four new acceptance tests over the shipped bytes.\n-    - tests: `cargo nextest run -p swissarmyhammer-validators` 665 run, 665 passed, 0 skipped (661 before; the four new tests are the difference). `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3428 run, 3424 passed, 4 failed, all four failing in this tree before this change. `cargo fmt --all -- --check` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n-    - next: /review\n-  timestamp: 2026-08-12T01:52:07.295672+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSW6T21S5GD7F0AX3JT58K0","timestamp":"2026-08-12T02:18:17.537419Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -488,3 +488,7 @@\n\n - [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:51` — The new gate lets a workspace pass when a member never reached the lints because its BUILD SCRIPT failed. The rule body at line 193 states three reasons for a nonzero status. A fourth reason stands: cargo made a build, and the build script of a crate exited nonzero, so clippy never linted that crate. cargo writes NO `compiler-message` entry for that failure — it writes its own words to stderr alone — so `$work/rustc-errors.txt` holds no byte and the second test does not fire. cargo writes `{\"reason\":\"build-finished\",\"success\":false}`, so `$work/ran.txt` is not empty and the first test does not fire. The run then writes the findings of the other members and exits 0. Measured with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1, with the run script taken from the SHIPPED bytes of the rule, over probe packages under the scratchpad. (a) One package that holds a function of 300 body lines beside a `build.rs` that runs `std::process::exit(3)`: the script wrote 0 findings and exited 0. The raw report held 1135 bytes, one `build-finished` entry with `success: false`, one `compiler-artifact` entry and no `compiler-message` entry at all. CONTROL: the same package with `fn main() {}` as its build script wrote `src/lib.rs:1` and exited 0. So the run lost one finding and read the package as clean. (b) A workspace of two members, `good` and `bad`, each holding the same function of 300 body lines, where the build script of `bad` runs `std::process::exit(3)`: the script wrote `good/src/lib.rs:1` alone and exited 0. CONTROL: the same workspace with `fn main() {}` as the build script of `bad` wrote both `bad/src/lib.rs:1` and `good/src/lib.rs:1`. So `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean. This is the same shape the two findings of round 2 name, with a fourth cause. This repository holds eight build scripts — `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs` — so the `scope: workspace` of this rule makes the shape reach this repository. The raw report tells the two runs apart. Measured over every shape of the table: each run cargo made and clippy linted from end to end writes either `success: true`, or `success: false` beside at least one error-level `compiler-message` whose code is a LINT name (`clippy::unwrap_used`, `unused_variables`, `clippy::too_many_lines`); each run where a crate never reached the lints writes `success: false` beside NO error-level `compiler-message`. So add a third test: the status is nonzero, a `build-finished` entry stands, and the raw report holds no error-level `compiler-message` — that run is broken. Add the shape to the table in the section \"A workspace the tool cannot lint\", correct the statement of three reasons at line 193 to four, and add an acceptance test for it.\n - [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1` — This file exceeds the review prompt cap — 277370 rendered bytes against the 262144-byte per-file cap — so these validators could not review it: duplication. Split the file into smaller modules that fit the review prompt cap.\n+\n+## Review Findings (2026-08-11 21:20)\n+\n+- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:220` — The rule states a build-script count this repository does not hold. Line 220 states \"This repository holds eight build scripts\", and line 342 states \"the eight build scripts of this repository\". Both lines are new in this change: `git diff --unified=0 a4cd1eee9^ a4cd1eee9 -- builtin/validators/code-hygiene/rules/complexity-rust.md` shows each of the two as an added line. This repository holds FIFTEEN build scripts. Measured two ways with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1. (a) `git ls-files '*build.rs'` gives 15 files: `apps/code-context-cli/build.rs`, `apps/kanban-app/build.rs`, `apps/kanban-cli/build.rs`, `apps/mirdan-app/build.rs`, `apps/mirdan-cli/build.rs`, `apps/shelltool-cli/build.rs`, `apps/swissarmyhammer-cli/build.rs`, `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs`. (b) CONTROL, made with the instrument the rule itself names: the shipped run over this repository wrote 149 `build-script-executed` entries, and 15 of the 149 name a package of this repository — the seven under `apps/` and the eight under `crates/`. The seven packages under `apps/` are members of the workspace, so `cargo clippy --workspace` runs their build scripts, the same as the eight under `crates/`. The rule states \"Every measurement below was made with clippy 0.1.97 and cargo 1.97.1\", so each number in the body is a measurement, and this one disagrees with the repository. Write fifteen in both lines.\n","reverse_patch":"--- original\n+++ modified\n@@ -488,7 +488,3 @@\n\n - [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:51` — The new gate lets a workspace pass when a member never reached the lints because its BUILD SCRIPT failed. The rule body at line 193 states three reasons for a nonzero status. A fourth reason stands: cargo made a build, and the build script of a crate exited nonzero, so clippy never linted that crate. cargo writes NO `compiler-message` entry for that failure — it writes its own words to stderr alone — so `$work/rustc-errors.txt` holds no byte and the second test does not fire. cargo writes `{\"reason\":\"build-finished\",\"success\":false}`, so `$work/ran.txt` is not empty and the first test does not fire. The run then writes the findings of the other members and exits 0. Measured with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1, with the run script taken from the SHIPPED bytes of the rule, over probe packages under the scratchpad. (a) One package that holds a function of 300 body lines beside a `build.rs` that runs `std::process::exit(3)`: the script wrote 0 findings and exited 0. The raw report held 1135 bytes, one `build-finished` entry with `success: false`, one `compiler-artifact` entry and no `compiler-message` entry at all. CONTROL: the same package with `fn main() {}` as its build script wrote `src/lib.rs:1` and exited 0. So the run lost one finding and read the package as clean. (b) A workspace of two members, `good` and `bad`, each holding the same function of 300 body lines, where the build script of `bad` runs `std::process::exit(3)`: the script wrote `good/src/lib.rs:1` alone and exited 0. CONTROL: the same workspace with `fn main() {}` as the build script of `bad` wrote both `bad/src/lib.rs:1` and `good/src/lib.rs:1`. So `bad/src/lib.rs` stands over the length gate, and the engine reads it as clean. This is the same shape the two findings of round 2 name, with a fourth cause. This repository holds eight build scripts — `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs` — so the `scope: workspace` of this rule makes the shape reach this repository. The raw report tells the two runs apart. Measured over every shape of the table: each run cargo made and clippy linted from end to end writes either `success: true`, or `success: false` beside at least one error-level `compiler-message` whose code is a LINT name (`clippy::unwrap_used`, `unused_variables`, `clippy::too_many_lines`); each run where a crate never reached the lints writes `success: false` beside NO error-level `compiler-message`. So add a third test: the status is nonzero, a `build-finished` entry stands, and the raw report holds no error-level `compiler-message` — that run is broken. Add the shape to the table in the section \"A workspace the tool cannot lint\", correct the statement of three reasons at line 193 to four, and add an acceptance test for it.\n - [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/complexity.rs:1` — This file exceeds the review prompt cap — 277370 rendered bytes against the 262144-byte per-file cap — so these validators could not review it: duplication. Split the file into smaller modules that fit the review prompt cap.\n-\n-## Review Findings (2026-08-11 21:20)\n-\n-- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:220` — The rule states a build-script count this repository does not hold. Line 220 states \"This repository holds eight build scripts\", and line 342 states \"the eight build scripts of this repository\". Both lines are new in this change: `git diff --unified=0 a4cd1eee9^ a4cd1eee9 -- builtin/validators/code-hygiene/rules/complexity-rust.md` shows each of the two as an added line. This repository holds FIFTEEN build scripts. Measured two ways with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1. (a) `git ls-files '*build.rs'` gives 15 files: `apps/code-context-cli/build.rs`, `apps/kanban-app/build.rs`, `apps/kanban-cli/build.rs`, `apps/mirdan-app/build.rs`, `apps/mirdan-cli/build.rs`, `apps/shelltool-cli/build.rs`, `apps/swissarmyhammer-cli/build.rs`, `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs`. (b) CONTROL, made with the instrument the rule itself names: the shipped run over this repository wrote 149 `build-script-executed` entries, and 15 of the 149 name a package of this repository — the seven under `apps/` and the eight under `crates/`. The seven packages under `apps/` are members of the workspace, so `cargo clippy --workspace` runs their build scripts, the same as the eight under `crates/`. The rule states \"Every measurement below was made with clippy 0.1.97 and cargo 1.97.1\", so each number in the body is a measurement, and this one disagrees with the repository. Write fifteen in both lines.\n","transaction_id":null}
{"id":"01KZSW6YW6772MW62JEAXFP5VK","timestamp":"2026-08-12T02:18:22.470917Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -455,8 +455,8 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 665 run, 665 passed, 0 skipped (661 before; the four new tests are the difference). `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3428 run, 3424 passed, 4 failed, all four failing in this tree before this change. `cargo fmt --all -- --check` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n     - next: /review\n   timestamp: 2026-08-12T01:52:07.295672+00:00\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: review\n+position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","reverse_patch":"--- original\n+++ modified\n@@ -455,8 +455,8 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 665 run, 665 passed, 0 skipped (661 before; the four new tests are the difference). `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3428 run, 3424 passed, 4 failed, all four failing in this tree before this change. `cargo fmt --all -- --check` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n     - next: /review\n   timestamp: 2026-08-12T01:52:07.295672+00:00\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","transaction_id":null}
{"id":"01KZSW7E72N3PBFME0PWMJ3WTS","timestamp":"2026-08-12T02:18:38.178671Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -455,6 +455,19 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 665 run, 665 passed, 0 skipped (661 before; the four new tests are the difference). `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3428 run, 3424 passed, 4 failed, all four failing in this tree before this change. `cargo fmt --all -- --check` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n     - next: /review\n   timestamp: 2026-08-12T01:52:07.295672+00:00\n+- actor: claude-code\n+  id: 01kzsw7e71q1j626ym6129ap6x\n+  text: |\n+    ### review — findings\n+\n+    - evidence: `review sha HEAD~1..HEAD` gave 16 validators attempted, 0 failed, 0 skipped files, 0 findings. Hand measurement over the SHIPPED bytes of the rule raised 1 finding at `builtin/validators/code-hygiene/rules/complexity-rust.md:220` (and the same words at line 342).\n+    - the gate itself measures correct. The shipped run over THIS repository gave 149 `custom-build` artifacts, 149 `build-script-executed` entries, 0 unrun build scripts, 58 findings and exit 0. 134 of the 149 artifacts arrived `fresh: true`, so the cached shape is in that count.\n+    - all 19 rows of the table were made again and each row gave the status, the `build-finished` entry, the error codes, the finding count and the exit status the table states. Row 7 also carries the code `clippy::too_many_lines` at level `error`, which the paragraph below the table states.\n+    - the two new tests are each necessary. With the build-script test taken out, the fourth test broke the one-package shape and the two-member shape, and the two-member shape beside the denied lint wrote `good/src/lib.rs:2` and exited 0. With the fourth test taken out, the build-script test broke all three. This is what the rule states.\n+    - four false-break attacks gave no false break: a build script that writes to stderr and exits 0 (1 finding, exit 0); a build script that writes `cargo::rerun-if-changed` and `cargo::rerun-if-env-changed` (1 finding, exit 0); a second run over a warm target directory, where two of three build scripts arrived `fresh: true` and all three still wrote a `build-script-executed` entry (the two runs gave the same output, exit 0); and a dev-dependency build script, which wrote both entries.\n+    - the split is a move. 39 function bodies are byte-identical between the old file and the five new files, 0 bodies were rewritten, 0 were lost, and `complexity_work` differs by the `pub(super)` word alone. The test count went 33 to 37. The 4 new ones are `the_shipped_rust_complexity_tool_rule_breaks_on_a_build_script_that_breaks`, `..._measures_a_package_beside_a_build_script_that_runs`, `..._breaks_on_a_member_build_script_that_breaks` and `..._breaks_on_a_broken_build_script_beside_a_denied_lint`. `cargo nextest run -p swissarmyhammer-validators -E 'test(/shipped::complexity/)'` gave 37 tests run, 37 passed.\n+    - next: correct the build-script count on lines 220 and 342 of the rule to fifteen.\n+  timestamp: 2026-08-12T02:18:38.177564+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -455,19 +455,6 @@\n     - tests: `cargo nextest run -p swissarmyhammer-validators` 665 run, 665 passed, 0 skipped (661 before; the four new tests are the difference). `cargo nextest run -E 'rdeps(swissarmyhammer-validators)'` 3428 run, 3424 passed, 4 failed, all four failing in this tree before this change. `cargo fmt --all -- --check` and `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` clean.\n     - next: /review\n   timestamp: 2026-08-12T01:52:07.295672+00:00\n-- actor: claude-code\n-  id: 01kzsw7e71q1j626ym6129ap6x\n-  text: |\n-    ### review — findings\n-\n-    - evidence: `review sha HEAD~1..HEAD` gave 16 validators attempted, 0 failed, 0 skipped files, 0 findings. Hand measurement over the SHIPPED bytes of the rule raised 1 finding at `builtin/validators/code-hygiene/rules/complexity-rust.md:220` (and the same words at line 342).\n-    - the gate itself measures correct. The shipped run over THIS repository gave 149 `custom-build` artifacts, 149 `build-script-executed` entries, 0 unrun build scripts, 58 findings and exit 0. 134 of the 149 artifacts arrived `fresh: true`, so the cached shape is in that count.\n-    - all 19 rows of the table were made again and each row gave the status, the `build-finished` entry, the error codes, the finding count and the exit status the table states. Row 7 also carries the code `clippy::too_many_lines` at level `error`, which the paragraph below the table states.\n-    - the two new tests are each necessary. With the build-script test taken out, the fourth test broke the one-package shape and the two-member shape, and the two-member shape beside the denied lint wrote `good/src/lib.rs:2` and exited 0. With the fourth test taken out, the build-script test broke all three. This is what the rule states.\n-    - four false-break attacks gave no false break: a build script that writes to stderr and exits 0 (1 finding, exit 0); a build script that writes `cargo::rerun-if-changed` and `cargo::rerun-if-env-changed` (1 finding, exit 0); a second run over a warm target directory, where two of three build scripts arrived `fresh: true` and all three still wrote a `build-script-executed` entry (the two runs gave the same output, exit 0); and a dev-dependency build script, which wrote both entries.\n-    - the split is a move. 39 function bodies are byte-identical between the old file and the five new files, 0 bodies were rewritten, 0 were lost, and `complexity_work` differs by the `pub(super)` word alone. The test count went 33 to 37. The 4 new ones are `the_shipped_rust_complexity_tool_rule_breaks_on_a_build_script_that_breaks`, `..._measures_a_package_beside_a_build_script_that_runs`, `..._breaks_on_a_member_build_script_that_breaks` and `..._breaks_on_a_broken_build_script_beside_a_denied_lint`. `cargo nextest run -p swissarmyhammer-validators -E 'test(/shipped::complexity/)'` gave 37 tests run, 37 passed.\n-    - next: correct the build-script count on lines 220 and 342 of the rule to fifteen.\n-  timestamp: 2026-08-12T02:18:38.177564+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSW87Y17WT7P5P91V3GRCT1","timestamp":"2026-08-12T02:19:04.513545Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -468,6 +468,15 @@\n     - the split is a move. 39 function bodies are byte-identical between the old file and the five new files, 0 bodies were rewritten, 0 were lost, and `complexity_work` differs by the `pub(super)` word alone. The test count went 33 to 37. The 4 new ones are `the_shipped_rust_complexity_tool_rule_breaks_on_a_build_script_that_breaks`, `..._measures_a_package_beside_a_build_script_that_runs`, `..._breaks_on_a_member_build_script_that_breaks` and `..._breaks_on_a_broken_build_script_beside_a_denied_lint`. `cargo nextest run -p swissarmyhammer-validators -E 'test(/shipped::complexity/)'` gave 37 tests run, 37 passed.\n     - next: correct the build-script count on lines 220 and 342 of the rule to fifteen.\n   timestamp: 2026-08-12T02:18:38.177564+00:00\n+- actor: claude-code\n+  id: 01kzsw87y0543vwjkw4vyvmnam\n+  text: |-\n+    ### finish iteration 4 — findings\n+    - implement: changed — 8 files; the gate finds a build script that fails, and the test file is split by language under the review prompt cap\n+    - test: green — cargo nextest -p swissarmyhammer-validators 665 run, 665 passed; rdeps 3428 run, 3424 passed with only the 4 failures that came before\n+    - commit: a4cd1eee9\n+    - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:220, the same words at :342. The gate is correct: 19 of 19 table rows reproduce, the shipped run over this repository measures 149 of 149 build scripts and reports 58 findings at exit 0, each new test is necessary by ablation, and four false-break attacks are clean. The split is a move — 39 function bodies byte for byte, 33 tests to 37. The engine skipped no file this round. One number in the prose is wrong: the rule says this repository holds eight build scripts, and it holds fifteen.\n+  timestamp: 2026-08-12T02:19:04.512395+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -468,15 +468,6 @@\n     - the split is a move. 39 function bodies are byte-identical between the old file and the five new files, 0 bodies were rewritten, 0 were lost, and `complexity_work` differs by the `pub(super)` word alone. The test count went 33 to 37. The 4 new ones are `the_shipped_rust_complexity_tool_rule_breaks_on_a_build_script_that_breaks`, `..._measures_a_package_beside_a_build_script_that_runs`, `..._breaks_on_a_member_build_script_that_breaks` and `..._breaks_on_a_broken_build_script_beside_a_denied_lint`. `cargo nextest run -p swissarmyhammer-validators -E 'test(/shipped::complexity/)'` gave 37 tests run, 37 passed.\n     - next: correct the build-script count on lines 220 and 342 of the rule to fifteen.\n   timestamp: 2026-08-12T02:18:38.177564+00:00\n-- actor: claude-code\n-  id: 01kzsw87y0543vwjkw4vyvmnam\n-  text: |-\n-    ### finish iteration 4 — findings\n-    - implement: changed — 8 files; the gate finds a build script that fails, and the test file is split by language under the review prompt cap\n-    - test: green — cargo nextest -p swissarmyhammer-validators 665 run, 665 passed; rdeps 3428 run, 3424 passed with only the 4 failures that came before\n-    - commit: a4cd1eee9\n-    - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:220, the same words at :342. The gate is correct: 19 of 19 table rows reproduce, the shipped run over this repository measures 149 of 149 build scripts and reports 58 findings at exit 0, each new test is necessary by ablation, and four false-break attacks are clean. The split is a move — 39 function bodies byte for byte, 33 tests to 37. The engine skipped no file this round. One number in the prose is wrong: the rule says this repository holds eight build scripts, and it holds fifteen.\n-  timestamp: 2026-08-12T02:19:04.512395+00:00\n position_column: review\n position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSW96JTT29BV668953Q01WB","timestamp":"2026-08-12T02:19:35.898502Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -477,8 +477,8 @@\n     - commit: a4cd1eee9\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:220, the same words at :342. The gate is correct: 19 of 19 table rows reproduce, the shipped run over this repository measures 149 of 149 build scripts and reports 58 findings at exit 0, each new test is necessary by ablation, and four false-break attacks are clean. The split is a move — 39 function bodies byte for byte, 33 tests to 37. The engine skipped no file this round. One number in the prose is wrong: the rule says this repository holds eight build scripts, and it holds fifteen.\n   timestamp: 2026-08-12T02:19:04.512395+00:00\n-position_column: review\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","reverse_patch":"--- original\n+++ modified\n@@ -477,8 +477,8 @@\n     - commit: a4cd1eee9\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:220, the same words at :342. The gate is correct: 19 of 19 table rows reproduce, the shipped run over this repository measures 149 of 149 build scripts and reports 58 findings at exit 0, each new test is necessary by ablation, and four false-break attacks are clean. The split is a move — 39 function bodies byte for byte, 33 tests to 37. The engine skipped no file this round. One number in the prose is wrong: the rule says this repository holds eight build scripts, and it holds fifteen.\n   timestamp: 2026-08-12T02:19:04.512395+00:00\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: review\n+position_ordinal: '8380'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","transaction_id":null}
{"id":"01KZSWH1GKDA81HHMNB2SM271T","timestamp":"2026-08-12T02:23:52.851612Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -513,4 +513,4 @@\n\n ## Review Findings (2026-08-11 21:20)\n\n-- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:220` — The rule states a build-script count this repository does not hold. Line 220 states \"This repository holds eight build scripts\", and line 342 states \"the eight build scripts of this repository\". Both lines are new in this change: `git diff --unified=0 a4cd1eee9^ a4cd1eee9 -- builtin/validators/code-hygiene/rules/complexity-rust.md` shows each of the two as an added line. This repository holds FIFTEEN build scripts. Measured two ways with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1. (a) `git ls-files '*build.rs'` gives 15 files: `apps/code-context-cli/build.rs`, `apps/kanban-app/build.rs`, `apps/kanban-cli/build.rs`, `apps/mirdan-app/build.rs`, `apps/mirdan-cli/build.rs`, `apps/shelltool-cli/build.rs`, `apps/swissarmyhammer-cli/build.rs`, `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs`. (b) CONTROL, made with the instrument the rule itself names: the shipped run over this repository wrote 149 `build-script-executed` entries, and 15 of the 149 name a package of this repository — the seven under `apps/` and the eight under `crates/`. The seven packages under `apps/` are members of the workspace, so `cargo clippy --workspace` runs their build scripts, the same as the eight under `crates/`. The rule states \"Every measurement below was made with clippy 0.1.97 and cargo 1.97.1\", so each number in the body is a measurement, and this one disagrees with the repository. Write fifteen in both lines.\n+- [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:220` — The rule states a build-script count this repository does not hold. Line 220 states \"This repository holds eight build scripts\", and line 342 states \"the eight build scripts of this repository\". Both lines are new in this change: `git diff --unified=0 a4cd1eee9^ a4cd1eee9 -- builtin/validators/code-hygiene/rules/complexity-rust.md` shows each of the two as an added line. This repository holds FIFTEEN build scripts. Measured two ways with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1. (a) `git ls-files '*build.rs'` gives 15 files: `apps/code-context-cli/build.rs`, `apps/kanban-app/build.rs`, `apps/kanban-cli/build.rs`, `apps/mirdan-app/build.rs`, `apps/mirdan-cli/build.rs`, `apps/shelltool-cli/build.rs`, `apps/swissarmyhammer-cli/build.rs`, `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs`. (b) CONTROL, made with the instrument the rule itself names: the shipped run over this repository wrote 149 `build-script-executed` entries, and 15 of the 149 name a package of this repository — the seven under `apps/` and the eight under `crates/`. The seven packages under `apps/` are members of the workspace, so `cargo clippy --workspace` runs their build scripts, the same as the eight under `crates/`. The rule states \"Every measurement below was made with clippy 0.1.97 and cargo 1.97.1\", so each number in the body is a measurement, and this one disagrees with the repository. Write fifteen in both lines.\n","reverse_patch":"--- original\n+++ modified\n@@ -513,4 +513,4 @@\n\n ## Review Findings (2026-08-11 21:20)\n\n-- [x] `builtin/validators/code-hygiene/rules/complexity-rust.md:220` — The rule states a build-script count this repository does not hold. Line 220 states \"This repository holds eight build scripts\", and line 342 states \"the eight build scripts of this repository\". Both lines are new in this change: `git diff --unified=0 a4cd1eee9^ a4cd1eee9 -- builtin/validators/code-hygiene/rules/complexity-rust.md` shows each of the two as an added line. This repository holds FIFTEEN build scripts. Measured two ways with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1. (a) `git ls-files '*build.rs'` gives 15 files: `apps/code-context-cli/build.rs`, `apps/kanban-app/build.rs`, `apps/kanban-cli/build.rs`, `apps/mirdan-app/build.rs`, `apps/mirdan-cli/build.rs`, `apps/shelltool-cli/build.rs`, `apps/swissarmyhammer-cli/build.rs`, `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs`. (b) CONTROL, made with the instrument the rule itself names: the shipped run over this repository wrote 149 `build-script-executed` entries, and 15 of the 149 name a package of this repository — the seven under `apps/` and the eight under `crates/`. The seven packages under `apps/` are members of the workspace, so `cargo clippy --workspace` runs their build scripts, the same as the eight under `crates/`. The rule states \"Every measurement below was made with clippy 0.1.97 and cargo 1.97.1\", so each number in the body is a measurement, and this one disagrees with the repository. Write fifteen in both lines.\n+- [ ] `builtin/validators/code-hygiene/rules/complexity-rust.md:220` — The rule states a build-script count this repository does not hold. Line 220 states \"This repository holds eight build scripts\", and line 342 states \"the eight build scripts of this repository\". Both lines are new in this change: `git diff --unified=0 a4cd1eee9^ a4cd1eee9 -- builtin/validators/code-hygiene/rules/complexity-rust.md` shows each of the two as an added line. This repository holds FIFTEEN build scripts. Measured two ways with clippy 0.1.97, cargo 1.97.1 and rustc 1.97.1. (a) `git ls-files '*build.rs'` gives 15 files: `apps/code-context-cli/build.rs`, `apps/kanban-app/build.rs`, `apps/kanban-cli/build.rs`, `apps/mirdan-app/build.rs`, `apps/mirdan-cli/build.rs`, `apps/shelltool-cli/build.rs`, `apps/swissarmyhammer-cli/build.rs`, `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs`. (b) CONTROL, made with the instrument the rule itself names: the shipped run over this repository wrote 149 `build-script-executed` entries, and 15 of the 149 name a package of this repository — the seven under `apps/` and the eight under `crates/`. The seven packages under `apps/` are members of the workspace, so `cargo clippy --workspace` runs their build scripts, the same as the eight under `crates/`. The rule states \"Every measurement below was made with clippy 0.1.97 and cargo 1.97.1\", so each number in the body is a measurement, and this one disagrees with the repository. Write fifteen in both lines.\n","transaction_id":null}
{"id":"01KZSWHKTJYYW48H4Y3QDBH5XP","timestamp":"2026-08-12T02:24:11.602942Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -477,6 +477,17 @@\n     - commit: a4cd1eee9\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:220, the same words at :342. The gate is correct: 19 of 19 table rows reproduce, the shipped run over this repository measures 149 of 149 build scripts and reports 58 findings at exit 0, each new test is necessary by ablation, and four false-break attacks are clean. The split is a move — 39 function bodies byte for byte, 33 tests to 37. The engine skipped no file this round. One number in the prose is wrong: the rule says this repository holds eight build scripts, and it holds fifteen.\n   timestamp: 2026-08-12T02:19:04.512395+00:00\n+- actor: claude-code\n+  id: 01kzswhkthjvkd80qhs7dpw06s\n+  text: |-\n+    I measured the build-script count of this repository two ways before I wrote it.\n+\n+    (a) `git ls-files '*build.rs'` gives 15 files: `apps/code-context-cli/build.rs`, `apps/kanban-app/build.rs`, `apps/kanban-cli/build.rs`, `apps/mirdan-app/build.rs`, `apps/mirdan-cli/build.rs`, `apps/shelltool-cli/build.rs`, `apps/swissarmyhammer-cli/build.rs`, `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs`.\n+\n+    (b) The instrument the rule names. I ran `CLIPPY_CONF_DIR=<work> cargo clippy --workspace --all-targets --message-format=json --quiet` with the four lints of the rule, at the rule's own thresholds, over this repository. The run exited 0 and wrote 1499898 bytes. The report holds 149 `build-script-executed` entries, and 15 of the 149 name a package of this repository: the seven under `apps/` and the eight under `crates/`. The 15 package identifiers agree file for file with the 15 files of (a).\n+\n+    The two instruments agree: fifteen. I wrote \"fifteen\" in both places of `builtin/validators/code-hygiene/rules/complexity-rust.md`, and I wrapped the second paragraph again to the column width of the file. I changed no other sentence, no test and no run script. The count of 149 `build-script-executed` entries the rule states in the section \"A workspace the tool cannot lint\" agrees with this run, so that number stands unchanged.\n+  timestamp: 2026-08-12T02:24:11.601689+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -477,17 +477,6 @@\n     - commit: a4cd1eee9\n     - review: findings — builtin/validators/code-hygiene/rules/complexity-rust.md:220, the same words at :342. The gate is correct: 19 of 19 table rows reproduce, the shipped run over this repository measures 149 of 149 build scripts and reports 58 findings at exit 0, each new test is necessary by ablation, and four false-break attacks are clean. The split is a move — 39 function bodies byte for byte, 33 tests to 37. The engine skipped no file this round. One number in the prose is wrong: the rule says this repository holds eight build scripts, and it holds fifteen.\n   timestamp: 2026-08-12T02:19:04.512395+00:00\n-- actor: claude-code\n-  id: 01kzswhkthjvkd80qhs7dpw06s\n-  text: |-\n-    I measured the build-script count of this repository two ways before I wrote it.\n-\n-    (a) `git ls-files '*build.rs'` gives 15 files: `apps/code-context-cli/build.rs`, `apps/kanban-app/build.rs`, `apps/kanban-cli/build.rs`, `apps/mirdan-app/build.rs`, `apps/mirdan-cli/build.rs`, `apps/shelltool-cli/build.rs`, `apps/swissarmyhammer-cli/build.rs`, `crates/mirdan/build.rs`, `crates/swissarmyhammer-agents/build.rs`, `crates/swissarmyhammer-common/build.rs`, `crates/swissarmyhammer-config/build.rs`, `crates/swissarmyhammer-skills/build.rs`, `crates/swissarmyhammer-templating/build.rs`, `crates/swissarmyhammer-tools/build.rs` and `crates/swissarmyhammer-validators/build.rs`.\n-\n-    (b) The instrument the rule names. I ran `CLIPPY_CONF_DIR=<work> cargo clippy --workspace --all-targets --message-format=json --quiet` with the four lints of the rule, at the rule's own thresholds, over this repository. The run exited 0 and wrote 1499898 bytes. The report holds 149 `build-script-executed` entries, and 15 of the 149 name a package of this repository: the seven under `apps/` and the eight under `crates/`. The 15 package identifiers agree file for file with the 15 files of (a).\n-\n-    The two instruments agree: fifteen. I wrote \"fifteen\" in both places of `builtin/validators/code-hygiene/rules/complexity-rust.md`, and I wrapped the second paragraph again to the column width of the file. I changed no other sentence, no test and no run script. The count of 149 `build-script-executed` entries the rule states in the section \"A workspace the tool cannot lint\" agrees with this run, so that number stands unchanged.\n-  timestamp: 2026-08-12T02:24:11.601689+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSWJC3QY10MYEGVPKMTAQWY","timestamp":"2026-08-12T02:24:36.471298Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -488,6 +488,13 @@\n\n     The two instruments agree: fifteen. I wrote \"fifteen\" in both places of `builtin/validators/code-hygiene/rules/complexity-rust.md`, and I wrapped the second paragraph again to the column width of the file. I changed no other sentence, no test and no run script. The count of 149 `build-script-executed` entries the rule states in the section \"A workspace the tool cannot lint\" agrees with this run, so that number stands unchanged.\n   timestamp: 2026-08-12T02:24:11.601689+00:00\n+- actor: claude-code\n+  id: 01kzswjc3phaeemcp7ztercbkb\n+  text: |-\n+    ### implement — changed\n+    - evidence: 1 file — /Users/wballard/github/swissarmyhammer/swissarmyhammer/builtin/validators/code-hygiene/rules/complexity-rust.md. The rule stated \"eight build scripts\" two times. Both instruments give fifteen: `git ls-files '*build.rs'` gives 15 files, and the shipped clippy run over this repository wrote 149 `build-script-executed` entries of which 15 name a package of this repository (7 under `apps/`, 8 under `crates/`). Both places now state fifteen. Gates after `touch crates/swissarmyhammer-validators/src/builtin/mod.rs`: `cargo nextest run -p swissarmyhammer-validators` = 665 tests run, 665 passed, 0 skipped; `cargo fmt --all --check` = clean; `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` = clean. `git status` shows the rule file as the one changed file.\n+    - next: /review\n+  timestamp: 2026-08-12T02:24:36.470045+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -488,13 +488,6 @@\n\n     The two instruments agree: fifteen. I wrote \"fifteen\" in both places of `builtin/validators/code-hygiene/rules/complexity-rust.md`, and I wrapped the second paragraph again to the column width of the file. I changed no other sentence, no test and no run script. The count of 149 `build-script-executed` entries the rule states in the section \"A workspace the tool cannot lint\" agrees with this run, so that number stands unchanged.\n   timestamp: 2026-08-12T02:24:11.601689+00:00\n-- actor: claude-code\n-  id: 01kzswjc3phaeemcp7ztercbkb\n-  text: |-\n-    ### implement — changed\n-    - evidence: 1 file — /Users/wballard/github/swissarmyhammer/swissarmyhammer/builtin/validators/code-hygiene/rules/complexity-rust.md. The rule stated \"eight build scripts\" two times. Both instruments give fifteen: `git ls-files '*build.rs'` gives 15 files, and the shipped clippy run over this repository wrote 149 `build-script-executed` entries of which 15 name a package of this repository (7 under `apps/`, 8 under `crates/`). Both places now state fifteen. Gates after `touch crates/swissarmyhammer-validators/src/builtin/mod.rs`: `cargo nextest run -p swissarmyhammer-validators` = 665 tests run, 665 passed, 0 skipped; `cargo fmt --all --check` = clean; `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` = clean. `git status` shows the rule file as the one changed file.\n-    - next: /review\n-  timestamp: 2026-08-12T02:24:36.470045+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSWY5A8EWFJM123QN2EA86X","timestamp":"2026-08-12T02:31:02.728429Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -495,6 +495,19 @@\n     - evidence: 1 file — /Users/wballard/github/swissarmyhammer/swissarmyhammer/builtin/validators/code-hygiene/rules/complexity-rust.md. The rule stated \"eight build scripts\" two times. Both instruments give fifteen: `git ls-files '*build.rs'` gives 15 files, and the shipped clippy run over this repository wrote 149 `build-script-executed` entries of which 15 name a package of this repository (7 under `apps/`, 8 under `crates/`). Both places now state fifteen. Gates after `touch crates/swissarmyhammer-validators/src/builtin/mod.rs`: `cargo nextest run -p swissarmyhammer-validators` = 665 tests run, 665 passed, 0 skipped; `cargo fmt --all --check` = clean; `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` = clean. `git status` shows the rule file as the one changed file.\n     - next: /review\n   timestamp: 2026-08-12T02:24:36.470045+00:00\n+- actor: claude-code\n+  id: 01kzswy5a78aa6zj2gz3a1tma9\n+  text: |\n+    ### review — clean\n+\n+    - scope: `review sha HEAD~1..HEAD` (commit `ecc9417b1`). The engine wrote 0 findings, 0 confirmed, 0 refuted. The delta holds markdown only.\n+    - evidence, the count: `git ls-files '*build.rs'` gives 15 files — 7 under `apps/` and 8 under `crates/`. CONTROL, made with the instrument the rule names: I ran the SHIPPED run script of the rule over this repository. The raw report holds 149 `build-script-executed` entries, and 15 of the 149 name a package of this repository. The 15 package ids match the 15 files of `git ls-files` one for one. So both instruments give fifteen, the number the rule now states.\n+    - evidence, the delta moved nothing else: `git diff --stat HEAD~1 HEAD` gives 1 file changed, 8 insertions and 8 deletions, in `builtin/validators/code-hygiene/rules/complexity-rust.md` alone. `git diff --word-diff=porcelain` gives four word lines: `-eight`, `+fifteen`, `-eight`, `+fifteen`. The re-wrap of the second paragraph moves words between lines and changes no word. Each line of the re-wrapped paragraph is 80 columns or less. The `run` script, every test and every other sentence are untouched.\n+    - evidence, the other numbers hold: the shipped run over this repository exited 0 and wrote 58 findings, and wrote 149 `build-script-executed` entries beside 149 `custom-build` artifacts. The rule states 149 and 58. Both agree.\n+    - evidence, the tests: `touch crates/swissarmyhammer-validators/src/builtin/mod.rs`, then `cargo nextest run -p swissarmyhammer-validators -E 'test(complexity)'` — 47 tests run, 47 passed, 0 failed.\n+    - open findings: none. The ten findings of rounds 1 to 4 are all checked.\n+    - next: the card moves to done.\n+  timestamp: 2026-08-12T02:31:02.727145+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -495,19 +495,6 @@\n     - evidence: 1 file — /Users/wballard/github/swissarmyhammer/swissarmyhammer/builtin/validators/code-hygiene/rules/complexity-rust.md. The rule stated \"eight build scripts\" two times. Both instruments give fifteen: `git ls-files '*build.rs'` gives 15 files, and the shipped clippy run over this repository wrote 149 `build-script-executed` entries of which 15 name a package of this repository (7 under `apps/`, 8 under `crates/`). Both places now state fifteen. Gates after `touch crates/swissarmyhammer-validators/src/builtin/mod.rs`: `cargo nextest run -p swissarmyhammer-validators` = 665 tests run, 665 passed, 0 skipped; `cargo fmt --all --check` = clean; `cargo clippy -p swissarmyhammer-validators --all-targets -- -D warnings` = clean. `git status` shows the rule file as the one changed file.\n     - next: /review\n   timestamp: 2026-08-12T02:24:36.470045+00:00\n-- actor: claude-code\n-  id: 01kzswy5a78aa6zj2gz3a1tma9\n-  text: |\n-    ### review — clean\n-\n-    - scope: `review sha HEAD~1..HEAD` (commit `ecc9417b1`). The engine wrote 0 findings, 0 confirmed, 0 refuted. The delta holds markdown only.\n-    - evidence, the count: `git ls-files '*build.rs'` gives 15 files — 7 under `apps/` and 8 under `crates/`. CONTROL, made with the instrument the rule names: I ran the SHIPPED run script of the rule over this repository. The raw report holds 149 `build-script-executed` entries, and 15 of the 149 name a package of this repository. The 15 package ids match the 15 files of `git ls-files` one for one. So both instruments give fifteen, the number the rule now states.\n-    - evidence, the delta moved nothing else: `git diff --stat HEAD~1 HEAD` gives 1 file changed, 8 insertions and 8 deletions, in `builtin/validators/code-hygiene/rules/complexity-rust.md` alone. `git diff --word-diff=porcelain` gives four word lines: `-eight`, `+fifteen`, `-eight`, `+fifteen`. The re-wrap of the second paragraph moves words between lines and changes no word. Each line of the re-wrapped paragraph is 80 columns or less. The `run` script, every test and every other sentence are untouched.\n-    - evidence, the other numbers hold: the shipped run over this repository exited 0 and wrote 58 findings, and wrote 149 `build-script-executed` entries beside 149 `custom-build` artifacts. The rule states 149 and 58. Both agree.\n-    - evidence, the tests: `touch crates/swissarmyhammer-validators/src/builtin/mod.rs`, then `cargo nextest run -p swissarmyhammer-validators -E 'test(complexity)'` — 47 tests run, 47 passed, 0 failed.\n-    - open findings: none. The ten findings of rounds 1 to 4 are all checked.\n-    - next: the card moves to done.\n-  timestamp: 2026-08-12T02:31:02.727145+00:00\n position_column: doing\n position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}
{"id":"01KZSWY93738NG3EF753Y3PBM9","timestamp":"2026-08-12T02:31:06.599612Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -508,8 +508,8 @@\n     - open findings: none. The ten findings of rounds 1 to 4 are all checked.\n     - next: the card moves to done.\n   timestamp: 2026-08-12T02:31:02.727145+00:00\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffffffe880\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","reverse_patch":"--- original\n+++ modified\n@@ -508,8 +508,8 @@\n     - open findings: none. The ten findings of rounds 1 to 4 are all checked.\n     - next: the card moves to done.\n   timestamp: 2026-08-12T02:31:02.727145+00:00\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffffffe880\n+position_column: doing\n+position_ordinal: '8280'\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n ---\n `builtin/validators/code-hygiene/rules/complexity-rust.md` runs `cargo clippy --workspace --all-targets` over four lints and declares `supersedes: [cognitive-complexity, function-length]`.\n","transaction_id":null}
{"id":"01KZSWYX6Q3ETXXVS2CQVFDJ7Q","timestamp":"2026-08-12T02:31:27.191664Z","op":"update","item_id":"01KZNNEGXQTYWBK3WT9W5V73K1","forward_patch":"--- original\n+++ modified\n@@ -508,6 +508,15 @@\n     - open findings: none. The ten findings of rounds 1 to 4 are all checked.\n     - next: the card moves to done.\n   timestamp: 2026-08-12T02:31:02.727145+00:00\n+- actor: claude-code\n+  id: 01kzswyx6ppp00aer1r0d2pbn3\n+  text: |-\n+    ### finish iteration 5 — clean\n+    - implement: changed — 1 file; the build-script count is corrected in both places\n+    - test: green — cargo nextest -p swissarmyhammer-validators 665 run, 665 passed; fmt and clippy clean\n+    - commit: ecc9417b1\n+    - review: clean — 0 findings; both instruments give fifteen, the word diff holds four words alone, and the run over this repository still gives 149 of 149 build scripts and 58 findings at exit 0; the card moves to done\n+  timestamp: 2026-08-12T02:31:27.190360+00:00\n position_column: done\n position_ordinal: ffffffffffffffffffffffffffffffffffffffffe880\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","reverse_patch":"--- original\n+++ modified\n@@ -508,15 +508,6 @@\n     - open findings: none. The ten findings of rounds 1 to 4 are all checked.\n     - next: the card moves to done.\n   timestamp: 2026-08-12T02:31:02.727145+00:00\n-- actor: claude-code\n-  id: 01kzswyx6ppp00aer1r0d2pbn3\n-  text: |-\n-    ### finish iteration 5 — clean\n-    - implement: changed — 1 file; the build-script count is corrected in both places\n-    - test: green — cargo nextest -p swissarmyhammer-validators 665 run, 665 passed; fmt and clippy clean\n-    - commit: ecc9417b1\n-    - review: clean — 0 findings; both instruments give fifteen, the word diff holds four words alone, and the run over this repository still gives 149 of 149 build scripts and 58 findings at exit 0; the card moves to done\n-  timestamp: 2026-08-12T02:31:27.190360+00:00\n position_column: done\n position_ordinal: ffffffffffffffffffffffffffffffffffffffffe880\n title: complexity-rust supersedes function-length but drops its data, builder and init carve-outs\n","transaction_id":null}

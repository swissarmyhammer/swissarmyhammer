{"id":"01KTY91Y7ADCS5PB6VT1FNNN73","timestamp":"2026-06-12T16:01:18.058571Z","op":"create","item_id":"01KTY91Y7AJRPJNBCVTV59HCJJ","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,39 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: 9b80\n+project: local-review\n+title: 'feat(review): fleet evaluates rules via primed prefix sessions + forks instead of monolithic prompts'\n+---\n+## What\n+\n+Restructure the review fleet to use the session-fork primitive (task 01KTY8ZV79NX25KJ1BF4CTKPCR / 4ctkpcr) so the shared prompt prefix is decoded ONCE per validator and every batch task forks from it. Today each fan-out task sends one monolithic prompt (change purpose + validator rules + file blocks, `render_fleet_prompt` in crates/swissarmyhammer-validators/src/review/fleet.rs) as a fresh session → on the hybrid Qwen, ~72% of turns pay a full ~15k-token cold prefill (404 trim fallbacks in the 2026-06-12 calcutron run).\n+\n+New flow in `run_fleet` (fleet.rs) / the pool (crates/swissarmyhammer-validators/src/validators/pool.rs):\n+\n+- [ ] **Prime:** per (review, validator), create a prefix session and prompt it with the shared sections — change purpose + validator mandate/rules/severity + output contract — restructured to end with an explicit \"Reply with exactly OK. The files to review arrive in the next message.\" (a completed turn whose end-of-turn KV lands exactly where continuations begin). Split `render_fleet_prompt` into `render_validator_prefix` and `render_file_payload` accordingly; keep the rendered text byte-stable so all forks share it.\n+- [ ] **Confirm + pin:** after the prime turn, verify via the fork-task's observability surface that the KV state is saved (token count > 0) and pin it for the review's duration. If confirmation fails, log and fall back to today's monolithic prompts for that validator (degraded but correct).\n+- [ ] **Fork per batch task:** each (validator, batch) task = fork the validator's prefix session → prompt with ONLY `render_file_payload(batch)` → parse findings as today. Check the fork response actually attached parent state; degraded fork → log + proceed (it still works, just cold).\n+- [ ] **Unpin/cleanup** prefix sessions when the validator's batches complete (and on review error paths) so pinned entries don't outlive the review.\n+- [ ] Verify stage (crates/swissarmyhammer-validators/src/review/verify.rs): evaluate whether the same prime+fork structure applies to verification turns (147–161 candidates/review share verification instructions) — if the prompt ordering permits, do it; if it balloons scope, file a follow-up with the measured prefix-share and skip.\n+- [ ] Observability: the fleet logs per task whether it ran on a warm fork (and the reused-token count) so a calcutron run's prefill savings are measurable from .sah/mcp.<pid>.log.\n+\n+## Acceptance Criteria\n+\n+- [ ] For a review with V validators and T tasks, the shared prefix is decoded V times (primes), not T times: every fork task's first decode logs full-prefix reuse (\"streaming reusing N cached tokens\" with N ≈ prefix length) and there are zero \"KV trim … returned false\" fallbacks attributable to fleet turns.\n+- [ ] Findings parity: same planted-findings detection as the monolithic path (scripts/review-verify harness still passes with findings > 0, failed == 0).\n+- [ ] Eviction or fork failure degrades to the monolithic path with a logged warning — never a lost task.\n+\n+## Tests\n+\n+- [ ] fleet.rs scripted-agent tests (<10s, no model): prime happens once per validator and forks per batch (assert via the scripted agent's seen-prompts: rules appear once, file payloads N times); degraded-fork fallback path; unpin on completion and on error; prefix/payload render split is byte-stable.\n+- [ ] Harness run (real model, integration): `python3 scripts/review-verify/drive.py` passes AND the sample repo's mcp log shows ≥1 prime + forked tasks with full-prefix reuse, zero trim fallbacks for fleet turns.\n+- [ ] `cargo test -p swissarmyhammer-validators` green.\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Evidence baseline (to beat)\n+\n+../calcutron/.sah/mcp.50099.log (2026-06-12): 406 donor matches, 158 warm reuses (avg 17,922 tokens saved), 404 cold-prefill fallbacks; reviews took 91 and 194 minutes. Expected effect: ~15 primes replace ~400 × 15k-token cold prefills; per-task decode drops to ~1–2k new tokens; also reduces the queue-wait that causes the idle-liveness abandons (zwxtnzy).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,39 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: 9b80\n-project: local-review\n-title: 'feat(review): fleet evaluates rules via primed prefix sessions + forks instead of monolithic prompts'\n----\n-## What\n-\n-Restructure the review fleet to use the session-fork primitive (task 01KTY8ZV79NX25KJ1BF4CTKPCR / 4ctkpcr) so the shared prompt prefix is decoded ONCE per validator and every batch task forks from it. Today each fan-out task sends one monolithic prompt (change purpose + validator rules + file blocks, `render_fleet_prompt` in crates/swissarmyhammer-validators/src/review/fleet.rs) as a fresh session → on the hybrid Qwen, ~72% of turns pay a full ~15k-token cold prefill (404 trim fallbacks in the 2026-06-12 calcutron run).\n-\n-New flow in `run_fleet` (fleet.rs) / the pool (crates/swissarmyhammer-validators/src/validators/pool.rs):\n-\n-- [ ] **Prime:** per (review, validator), create a prefix session and prompt it with the shared sections — change purpose + validator mandate/rules/severity + output contract — restructured to end with an explicit \"Reply with exactly OK. The files to review arrive in the next message.\" (a completed turn whose end-of-turn KV lands exactly where continuations begin). Split `render_fleet_prompt` into `render_validator_prefix` and `render_file_payload` accordingly; keep the rendered text byte-stable so all forks share it.\n-- [ ] **Confirm + pin:** after the prime turn, verify via the fork-task's observability surface that the KV state is saved (token count > 0) and pin it for the review's duration. If confirmation fails, log and fall back to today's monolithic prompts for that validator (degraded but correct).\n-- [ ] **Fork per batch task:** each (validator, batch) task = fork the validator's prefix session → prompt with ONLY `render_file_payload(batch)` → parse findings as today. Check the fork response actually attached parent state; degraded fork → log + proceed (it still works, just cold).\n-- [ ] **Unpin/cleanup** prefix sessions when the validator's batches complete (and on review error paths) so pinned entries don't outlive the review.\n-- [ ] Verify stage (crates/swissarmyhammer-validators/src/review/verify.rs): evaluate whether the same prime+fork structure applies to verification turns (147–161 candidates/review share verification instructions) — if the prompt ordering permits, do it; if it balloons scope, file a follow-up with the measured prefix-share and skip.\n-- [ ] Observability: the fleet logs per task whether it ran on a warm fork (and the reused-token count) so a calcutron run's prefill savings are measurable from .sah/mcp.<pid>.log.\n-\n-## Acceptance Criteria\n-\n-- [ ] For a review with V validators and T tasks, the shared prefix is decoded V times (primes), not T times: every fork task's first decode logs full-prefix reuse (\"streaming reusing N cached tokens\" with N ≈ prefix length) and there are zero \"KV trim … returned false\" fallbacks attributable to fleet turns.\n-- [ ] Findings parity: same planted-findings detection as the monolithic path (scripts/review-verify harness still passes with findings > 0, failed == 0).\n-- [ ] Eviction or fork failure degrades to the monolithic path with a logged warning — never a lost task.\n-\n-## Tests\n-\n-- [ ] fleet.rs scripted-agent tests (<10s, no model): prime happens once per validator and forks per batch (assert via the scripted agent's seen-prompts: rules appear once, file payloads N times); degraded-fork fallback path; unpin on completion and on error; prefix/payload render split is byte-stable.\n-- [ ] Harness run (real model, integration): `python3 scripts/review-verify/drive.py` passes AND the sample repo's mcp log shows ≥1 prime + forked tasks with full-prefix reuse, zero trim fallbacks for fleet turns.\n-- [ ] `cargo test -p swissarmyhammer-validators` green.\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Evidence baseline (to beat)\n-\n-../calcutron/.sah/mcp.50099.log (2026-06-12): 406 donor matches, 158 warm reuses (avg 17,922 tokens saved), 404 cold-prefill fallbacks; reviews took 91 and 194 minutes. Expected effect: ~15 primes replace ~400 × 15k-token cold prefills; per-task decode drops to ~1–2k new tokens; also reduces the queue-wait that causes the idle-liveness abandons (zwxtnzy).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTY92JAS1ZMSN9K2XWJHBDEJ","timestamp":"2026-06-12T16:01:38.649490Z","op":"update","item_id":"01KTY91Y7AJRPJNBCVTV59HCJJ","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,8 @@\n ---\n assignees:\n - claude-code\n+depends_on:\n+- 01KTY8ZV79NX25KJ1BF4CTKPCR\n position_column: todo\n position_ordinal: 9b80\n project: local-review\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,6 @@\n ---\n assignees:\n - claude-code\n-depends_on:\n-- 01KTY8ZV79NX25KJ1BF4CTKPCR\n position_column: todo\n position_ordinal: 9b80\n project: local-review\n","transaction_id":null}
{"id":"01KTYAZ1VST22SD258AQ6T6HCX","timestamp":"2026-06-12T16:34:40.633068Z","op":"update","item_id":"01KTY91Y7AJRPJNBCVTV59HCJJ","forward_patch":"--- original\n+++ modified\n@@ -3,6 +3,7 @@\n - claude-code\n depends_on:\n - 01KTY8ZV79NX25KJ1BF4CTKPCR\n+- 01KTYAYGGC6HBQDN74BXS2Y7FG\n position_column: todo\n position_ordinal: 9b80\n project: local-review\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,6 @@\n - claude-code\n depends_on:\n - 01KTY8ZV79NX25KJ1BF4CTKPCR\n-- 01KTYAYGGC6HBQDN74BXS2Y7FG\n position_column: todo\n position_ordinal: 9b80\n project: local-review\n","transaction_id":null}
{"id":"01KTYT061EZ1Q31YAZGCN9AY0Y","timestamp":"2026-06-12T20:57:26.318488Z","op":"update","item_id":"01KTY91Y7AJRPJNBCVTV59HCJJ","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KTY8ZV79NX25KJ1BF4CTKPCR\n - 01KTYAYGGC6HBQDN74BXS2Y7FG\n-position_column: todo\n-position_ordinal: 9b80\n+position_column: doing\n+position_ordinal: '80'\n project: local-review\n title: 'feat(review): fleet evaluates rules via primed prefix sessions + forks instead of monolithic prompts'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KTY8ZV79NX25KJ1BF4CTKPCR\n - 01KTYAYGGC6HBQDN74BXS2Y7FG\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: 9b80\n project: local-review\n title: 'feat(review): fleet evaluates rules via primed prefix sessions + forks instead of monolithic prompts'\n ---\n","transaction_id":null}
{"id":"01KTYVGWG40CBYE9X01Y74WABD","timestamp":"2026-06-12T21:24:02.180497Z","op":"update","item_id":"01KTY91Y7AJRPJNBCVTV59HCJJ","forward_patch":"--- original\n+++ modified\n@@ -15,24 +15,24 @@\n\n New flow in `run_fleet` (fleet.rs) / the pool (crates/swissarmyhammer-validators/src/validators/pool.rs):\n\n-- [ ] **Prime:** per (review, validator), create a prefix session and prompt it with the shared sections — change purpose + validator mandate/rules/severity + output contract — restructured to end with an explicit \"Reply with exactly OK. The files to review arrive in the next message.\" (a completed turn whose end-of-turn KV lands exactly where continuations begin). Split `render_fleet_prompt` into `render_validator_prefix` and `render_file_payload` accordingly; keep the rendered text byte-stable so all forks share it.\n-- [ ] **Confirm + pin:** after the prime turn, verify via the fork-task's observability surface that the KV state is saved (token count > 0) and pin it for the review's duration. If confirmation fails, log and fall back to today's monolithic prompts for that validator (degraded but correct).\n-- [ ] **Fork per batch task:** each (validator, batch) task = fork the validator's prefix session → prompt with ONLY `render_file_payload(batch)` → parse findings as today. Check the fork response actually attached parent state; degraded fork → log + proceed (it still works, just cold).\n-- [ ] **Unpin/cleanup** prefix sessions when the validator's batches complete (and on review error paths) so pinned entries don't outlive the review.\n-- [ ] Verify stage (crates/swissarmyhammer-validators/src/review/verify.rs): evaluate whether the same prime+fork structure applies to verification turns (147–161 candidates/review share verification instructions) — if the prompt ordering permits, do it; if it balloons scope, file a follow-up with the measured prefix-share and skip.\n-- [ ] Observability: the fleet logs per task whether it ran on a warm fork (and the reused-token count) so a calcutron run's prefill savings are measurable from .sah/mcp.<pid>.log.\n+- [x] **Prime:** per (review, validator), create a prefix session and prompt it with the shared sections — change purpose + validator mandate/rules/severity + output contract — restructured to end with an explicit \"Reply with exactly OK. The files to review arrive in the next message.\" (a completed turn whose end-of-turn KV lands exactly where continuations begin). Split `render_fleet_prompt` into `render_validator_prefix` and `render_file_payload` accordingly; keep the rendered text byte-stable so all forks share it. *(done: `render_shared_sections` underlies all three renders, so warm and monolithic paths compose from identical bytes)*\n+- [x] **Confirm + pin:** after the prime turn, verify via the fork-task's observability surface that the KV state is saved (token count > 0) and pin it for the review's duration. If confirmation fails, log and fall back to today's monolithic prompts for that validator (degraded but correct). *(done: `prime_validator_prefix` gates on `saved` + non-zero token count when the backend reports one; backends without token counts — claude CLI — remain forkable per the contract)*\n+- [x] **Fork per batch task:** each (validator, batch) task = fork the validator's prefix session → prompt with ONLY `render_file_payload(batch)` → parse findings as today. Check the fork response actually attached parent state; degraded fork → log + proceed (it still works, just cold). *(done: `AgentPool::submit_forked` + `collect_forked_task`; a FAILED fork falls back to a monolithic fresh-session prompt — typed `PoolError::ForkFailed` — never a lost task)*\n+- [x] **Unpin/cleanup** prefix sessions when the validator's batches complete (and on review error paths) so pinned entries don't outlive the review. *(done: `unpin_prefix_session` runs after every batch task resolves, including failures)*\n+- [x] Verify stage (crates/swissarmyhammer-validators/src/review/verify.rs): evaluate whether the same prime+fork structure applies to verification turns — **measured and skipped**: shared text is ~280 bytes of header before the first per-candidate byte + ~420 bytes of contract at the END; even reordered ≈ ~175 shared tokens of multi-thousand-token prompts (<10% prefix share vs ~80–90% for fleet). Rationale in verify.rs module docs; follow-up task 01KTYVCZTP9JQ7PSED8F2YH67G records the measurement.\n+- [x] Observability: the fleet logs per task whether it ran on a warm fork (and the reused-token count) so a calcutron run's prefill savings are measurable from .sah/mcp.<pid>.log. *(done: \"fleet task ran on a warm fork of the primed prefix session\" with `reused_tokens`, \"primed validator prefix session\" with `prefix_tokens`, degraded/fallback warnings)*\n\n ## Acceptance Criteria\n\n-- [ ] For a review with V validators and T tasks, the shared prefix is decoded V times (primes), not T times: every fork task's first decode logs full-prefix reuse (\"streaming reusing N cached tokens\" with N ≈ prefix length) and there are zero \"KV trim … returned false\" fallbacks attributable to fleet turns.\n-- [ ] Findings parity: same planted-findings detection as the monolithic path (scripts/review-verify harness still passes with findings > 0, failed == 0).\n-- [ ] Eviction or fork failure degrades to the monolithic path with a logged warning — never a lost task.\n+- [ ] For a review with V validators and T tasks, the shared prefix is decoded V times (primes), not T times: every fork task's first decode logs full-prefix reuse (\"streaming reusing N cached tokens\" with N ≈ prefix length) and there are zero \"KV trim … returned false\" fallbacks attributable to fleet turns. *(needs the real-model harness run — verify step)*\n+- [ ] Findings parity: same planted-findings detection as the monolithic path (scripts/review-verify harness still passes with findings > 0, failed == 0). *(needs the real-model harness run — verify step)*\n+- [x] Eviction or fork failure degrades to the monolithic path with a logged warning — never a lost task. *(scripted-agent tests: RejectFork, Unsupported, DegradedAttach modes)*\n\n ## Tests\n\n-- [ ] fleet.rs scripted-agent tests (<10s, no model): prime happens once per validator and forks per batch (assert via the scripted agent's seen-prompts: rules appear once, file payloads N times); degraded-fork fallback path; unpin on completion and on error; prefix/payload render split is byte-stable.\n-- [ ] Harness run (real model, integration): `python3 scripts/review-verify/drive.py` passes AND the sample repo's mcp log shows ≥1 prime + forked tasks with full-prefix reuse, zero trim fallbacks for fleet turns.\n-- [ ] `cargo test -p swissarmyhammer-validators` green.\n+- [x] fleet.rs scripted-agent tests (<10s, no model): prime happens once per validator and forks per batch (assert via the scripted agent's seen-prompts: rules appear once, file payloads N times); degraded-fork fallback path; unpin on completion and on error; prefix/payload render split is byte-stable. *(17 fleet tests + 19 pool tests, full suite 3.2s; the mock implements the fork ext contract with per-session history so forked prompts are matched against prefix+payload like a real agent)*\n+- [ ] Harness run (real model, integration): `python3 scripts/review-verify/drive.py` passes AND the sample repo's mcp log shows ≥1 prime + forked tasks with full-prefix reuse, zero trim fallbacks for fleet turns. *(deferred to the verify step per orchestrator instruction — do NOT run the real model in the implement pass)*\n+- [x] `cargo test -p swissarmyhammer-validators` green. *(253 + 2 passed; `-p swissarmyhammer-agent` also green: 83 + 1 + 1; clippy --all-targets -D warnings clean on swissarmyhammer-validators)*\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n","reverse_patch":"--- original\n+++ modified\n@@ -15,24 +15,24 @@\n\n New flow in `run_fleet` (fleet.rs) / the pool (crates/swissarmyhammer-validators/src/validators/pool.rs):\n\n-- [x] **Prime:** per (review, validator), create a prefix session and prompt it with the shared sections — change purpose + validator mandate/rules/severity + output contract — restructured to end with an explicit \"Reply with exactly OK. The files to review arrive in the next message.\" (a completed turn whose end-of-turn KV lands exactly where continuations begin). Split `render_fleet_prompt` into `render_validator_prefix` and `render_file_payload` accordingly; keep the rendered text byte-stable so all forks share it. *(done: `render_shared_sections` underlies all three renders, so warm and monolithic paths compose from identical bytes)*\n-- [x] **Confirm + pin:** after the prime turn, verify via the fork-task's observability surface that the KV state is saved (token count > 0) and pin it for the review's duration. If confirmation fails, log and fall back to today's monolithic prompts for that validator (degraded but correct). *(done: `prime_validator_prefix` gates on `saved` + non-zero token count when the backend reports one; backends without token counts — claude CLI — remain forkable per the contract)*\n-- [x] **Fork per batch task:** each (validator, batch) task = fork the validator's prefix session → prompt with ONLY `render_file_payload(batch)` → parse findings as today. Check the fork response actually attached parent state; degraded fork → log + proceed (it still works, just cold). *(done: `AgentPool::submit_forked` + `collect_forked_task`; a FAILED fork falls back to a monolithic fresh-session prompt — typed `PoolError::ForkFailed` — never a lost task)*\n-- [x] **Unpin/cleanup** prefix sessions when the validator's batches complete (and on review error paths) so pinned entries don't outlive the review. *(done: `unpin_prefix_session` runs after every batch task resolves, including failures)*\n-- [x] Verify stage (crates/swissarmyhammer-validators/src/review/verify.rs): evaluate whether the same prime+fork structure applies to verification turns — **measured and skipped**: shared text is ~280 bytes of header before the first per-candidate byte + ~420 bytes of contract at the END; even reordered ≈ ~175 shared tokens of multi-thousand-token prompts (<10% prefix share vs ~80–90% for fleet). Rationale in verify.rs module docs; follow-up task 01KTYVCZTP9JQ7PSED8F2YH67G records the measurement.\n-- [x] Observability: the fleet logs per task whether it ran on a warm fork (and the reused-token count) so a calcutron run's prefill savings are measurable from .sah/mcp.<pid>.log. *(done: \"fleet task ran on a warm fork of the primed prefix session\" with `reused_tokens`, \"primed validator prefix session\" with `prefix_tokens`, degraded/fallback warnings)*\n+- [ ] **Prime:** per (review, validator), create a prefix session and prompt it with the shared sections — change purpose + validator mandate/rules/severity + output contract — restructured to end with an explicit \"Reply with exactly OK. The files to review arrive in the next message.\" (a completed turn whose end-of-turn KV lands exactly where continuations begin). Split `render_fleet_prompt` into `render_validator_prefix` and `render_file_payload` accordingly; keep the rendered text byte-stable so all forks share it.\n+- [ ] **Confirm + pin:** after the prime turn, verify via the fork-task's observability surface that the KV state is saved (token count > 0) and pin it for the review's duration. If confirmation fails, log and fall back to today's monolithic prompts for that validator (degraded but correct).\n+- [ ] **Fork per batch task:** each (validator, batch) task = fork the validator's prefix session → prompt with ONLY `render_file_payload(batch)` → parse findings as today. Check the fork response actually attached parent state; degraded fork → log + proceed (it still works, just cold).\n+- [ ] **Unpin/cleanup** prefix sessions when the validator's batches complete (and on review error paths) so pinned entries don't outlive the review.\n+- [ ] Verify stage (crates/swissarmyhammer-validators/src/review/verify.rs): evaluate whether the same prime+fork structure applies to verification turns (147–161 candidates/review share verification instructions) — if the prompt ordering permits, do it; if it balloons scope, file a follow-up with the measured prefix-share and skip.\n+- [ ] Observability: the fleet logs per task whether it ran on a warm fork (and the reused-token count) so a calcutron run's prefill savings are measurable from .sah/mcp.<pid>.log.\n\n ## Acceptance Criteria\n\n-- [ ] For a review with V validators and T tasks, the shared prefix is decoded V times (primes), not T times: every fork task's first decode logs full-prefix reuse (\"streaming reusing N cached tokens\" with N ≈ prefix length) and there are zero \"KV trim … returned false\" fallbacks attributable to fleet turns. *(needs the real-model harness run — verify step)*\n-- [ ] Findings parity: same planted-findings detection as the monolithic path (scripts/review-verify harness still passes with findings > 0, failed == 0). *(needs the real-model harness run — verify step)*\n-- [x] Eviction or fork failure degrades to the monolithic path with a logged warning — never a lost task. *(scripted-agent tests: RejectFork, Unsupported, DegradedAttach modes)*\n+- [ ] For a review with V validators and T tasks, the shared prefix is decoded V times (primes), not T times: every fork task's first decode logs full-prefix reuse (\"streaming reusing N cached tokens\" with N ≈ prefix length) and there are zero \"KV trim … returned false\" fallbacks attributable to fleet turns.\n+- [ ] Findings parity: same planted-findings detection as the monolithic path (scripts/review-verify harness still passes with findings > 0, failed == 0).\n+- [ ] Eviction or fork failure degrades to the monolithic path with a logged warning — never a lost task.\n\n ## Tests\n\n-- [x] fleet.rs scripted-agent tests (<10s, no model): prime happens once per validator and forks per batch (assert via the scripted agent's seen-prompts: rules appear once, file payloads N times); degraded-fork fallback path; unpin on completion and on error; prefix/payload render split is byte-stable. *(17 fleet tests + 19 pool tests, full suite 3.2s; the mock implements the fork ext contract with per-session history so forked prompts are matched against prefix+payload like a real agent)*\n-- [ ] Harness run (real model, integration): `python3 scripts/review-verify/drive.py` passes AND the sample repo's mcp log shows ≥1 prime + forked tasks with full-prefix reuse, zero trim fallbacks for fleet turns. *(deferred to the verify step per orchestrator instruction — do NOT run the real model in the implement pass)*\n-- [x] `cargo test -p swissarmyhammer-validators` green. *(253 + 2 passed; `-p swissarmyhammer-agent` also green: 83 + 1 + 1; clippy --all-targets -D warnings clean on swissarmyhammer-validators)*\n+- [ ] fleet.rs scripted-agent tests (<10s, no model): prime happens once per validator and forks per batch (assert via the scripted agent's seen-prompts: rules appear once, file payloads N times); degraded-fork fallback path; unpin on completion and on error; prefix/payload render split is byte-stable.\n+- [ ] Harness run (real model, integration): `python3 scripts/review-verify/drive.py` passes AND the sample repo's mcp log shows ≥1 prime + forked tasks with full-prefix reuse, zero trim fallbacks for fleet turns.\n+- [ ] `cargo test -p swissarmyhammer-validators` green.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n","transaction_id":null}
{"id":"01KTYVH3KSSZ7D1Q5FBH8ZT1MD","timestamp":"2026-06-12T21:24:09.465248Z","op":"update","item_id":"01KTY91Y7AJRPJNBCVTV59HCJJ","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KTY8ZV79NX25KJ1BF4CTKPCR\n - 01KTYAYGGC6HBQDN74BXS2Y7FG\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: 'feat(review): fleet evaluates rules via primed prefix sessions + forks instead of monolithic prompts'\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KTY8ZV79NX25KJ1BF4CTKPCR\n - 01KTYAYGGC6HBQDN74BXS2Y7FG\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: 'feat(review): fleet evaluates rules via primed prefix sessions + forks instead of monolithic prompts'\n","transaction_id":null}
{"id":"01KTYYG7DNG9KGB9NRDPE45WQW","timestamp":"2026-06-12T22:16:06.325187Z","op":"update","item_id":"01KTY91Y7AJRPJNBCVTV59HCJJ","forward_patch":"--- original\n+++ modified\n@@ -24,14 +24,14 @@\n\n ## Acceptance Criteria\n\n-- [ ] For a review with V validators and T tasks, the shared prefix is decoded V times (primes), not T times: every fork task's first decode logs full-prefix reuse (\"streaming reusing N cached tokens\" with N ≈ prefix length) and there are zero \"KV trim … returned false\" fallbacks attributable to fleet turns. *(needs the real-model harness run — verify step)*\n-- [ ] Findings parity: same planted-findings detection as the monolithic path (scripts/review-verify harness still passes with findings > 0, failed == 0). *(needs the real-model harness run — verify step)*\n+- [x] For a review with V validators and T tasks, the shared prefix is decoded V times (primes), not T times: every fork task's first decode logs full-prefix reuse (\"streaming reusing N cached tokens\" with N ≈ prefix length) and there are zero \"KV trim … returned false\" fallbacks attributable to fleet turns. *(needs the real-model harness run — verify step)* *(verified 2026-06-12 real-model run, sample log scripts/review-verify/sample/.sah/mcp.57606.log: exactly 1 prime per validator — duplication prefix_tokens=Some(5337) pinned=true, magic-numbers prefix_tokens=Some(5186) pinned=true — every fleet fork logged \"fleet task ran on a warm fork of the primed prefix session\" reusing the full primed prefix; 0 KV-trim fallbacks on fleet turns (the 8 KV-trim warns in the log belong to verification-stage and prime-turn opportunistic donor reuse); \"no streaming progress\"=0, \"fleet task failed\"=0, \"Queue is full\"=0)*\n+- [x] Findings parity: same planted-findings detection as the monolithic path (scripts/review-verify harness still passes with findings > 0, failed == 0). *(needs the real-model harness run — verify step)* *(verified 2026-06-12: `python3 scripts/review-verify/drive.py` exit 0, findings 5, failed 0)*\n - [x] Eviction or fork failure degrades to the monolithic path with a logged warning — never a lost task. *(scripted-agent tests: RejectFork, Unsupported, DegradedAttach modes)*\n\n ## Tests\n\n - [x] fleet.rs scripted-agent tests (<10s, no model): prime happens once per validator and forks per batch (assert via the scripted agent's seen-prompts: rules appear once, file payloads N times); degraded-fork fallback path; unpin on completion and on error; prefix/payload render split is byte-stable. *(17 fleet tests + 19 pool tests, full suite 3.2s; the mock implements the fork ext contract with per-session history so forked prompts are matched against prefix+payload like a real agent)*\n-- [ ] Harness run (real model, integration): `python3 scripts/review-verify/drive.py` passes AND the sample repo's mcp log shows ≥1 prime + forked tasks with full-prefix reuse, zero trim fallbacks for fleet turns. *(deferred to the verify step per orchestrator instruction — do NOT run the real model in the implement pass)*\n+- [x] Harness run (real model, integration): `python3 scripts/review-verify/drive.py` passes AND the sample repo's mcp log shows ≥1 prime + forked tasks with full-prefix reuse, zero trim fallbacks for fleet turns. *(deferred to the verify step per orchestrator instruction — do NOT run the real model in the implement pass)* *(verified 2026-06-12: drive.py exit 0 with findings 5 / failed 0; mcp.57606.log shows primes of 5337 and 5186 prefix tokens, both pinned, warm forks with full-prefix reuse, zero fleet-turn trim fallbacks; workspace green 14644/14644 after one isolated rerun of the documented ai_panel_e2e GPU flake, clippy clean)*\n - [x] `cargo test -p swissarmyhammer-validators` green. *(253 + 2 passed; `-p swissarmyhammer-agent` also green: 83 + 1 + 1; clippy --all-targets -D warnings clean on swissarmyhammer-validators)*\n\n ## Workflow\n@@ -39,4 +39,45 @@\n\n ## Evidence baseline (to beat)\n\n-../calcutron/.sah/mcp.50099.log (2026-06-12): 406 donor matches, 158 warm reuses (avg 17,922 tokens saved), 404 cold-prefill fallbacks; reviews took 91 and 194 minutes. Expected effect: ~15 primes replace ~400 × 15k-token cold prefills; per-task decode drops to ~1–2k new tokens; also reduces the queue-wait that causes the idle-liveness abandons (zwxtnzy).\n\\ No newline at end of file\n+../calcutron/.sah/mcp.50099.log (2026-06-12): 406 donor matches, 158 warm reuses (avg 17,922 tokens saved), 404 cold-prefill fallbacks; reviews took 91 and 194 minutes. Expected effect: ~15 primes replace ~400 × 15k-token cold prefills; per-task decode drops to ~1–2k new tokens; also reduces the queue-wait that causes the idle-liveness abandons (zwxtnzy).\n+\n+## Review Findings (2026-06-12 16:47)\n+\n+Scope: `review file crates/swissarmyhammer-validators/src/review/fleet.rs` (task-scoped; unrelated working-tree changes excluded)\n+\n+> ⚠️ 2/15 review tasks failed — results are INCOMPLETE.\n+\n+### Blockers\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:930` — The scripted ACP mock-agent harness (ScriptedAgent, ScriptedAdapter, connect_to, dispatch, response_for, is_error) is a near-verbatim copy maintained in five test modules, and this change makes the fleet.rs copy diverge: it alone gained fork/pin/state-status support (ForkMode, SessionState, dispatch_ext) while verify.rs, drive.rs, review_fixture.rs, and tools/review/tests.rs keep the old shape. The copies are already drifting out of sync — a fix to the dispatch loop or session handling applied to one copy and not the others is a latent test bug, and every future ACP-contract change must now be hand-propagated to five places. Extract one shared scripted-agent harness into crate::review::test_support (fleet.rs already imports new_notifier from it). Parameterize the per-module differences as arguments: the script-matching policy (prompt-only vs accumulated session context) and the extension behavior (the ForkMode enum added here becomes the shared knob, with Unsupported as the default so existing modules keep their current behavior). The tools-crate copies can consume it via a test-support export or the existing review_fixture.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1203` — The test helpers prompt_text, with_pool, and findings_json are byte-identical or near-identical copies of the same helpers in verify.rs, drive.rs, review_fixture.rs, and tools/review/tests.rs. These differ by at most a renamed import (verify.rs spells the same prompt_text body with AcpContentBlock) — each is one function that belongs in the shared test-support module, and each copy is another place a future change to the prompt wire-shape or pool setup must be repeated. Move prompt_text, with_pool, and findings_json into crate::review::test_support alongside the extracted scripted-agent harness, and delete the per-module copies in fleet.rs, verify.rs, drive.rs, and the tools-crate test fixtures.\n+\n+### Warnings\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:291` — The prefix-session pin is not cancellation-safe: `unpin_prefix_session` only runs after the sequential collect loop completes, so if the `run_fleet`/`run_validator_fleet` future is dropped mid-collect (caller timeout, aborted review turn), the primed session stays pinned forever. Pinned entries are exempt from cache eviction, so a cancelled review permanently leaks backend session state until process restart — the exact 'pinned entry never outlives the fan-out' invariant the comment claims. Wrap the pinned session id in a guard whose `Drop` schedules the unpin (e.g. spawn `pool.pin_session(id, false)` onto the runtime from `Drop`, or have the pool track pins per fan-out and release on handle drop), so cancellation of the fan-out future cannot leak the pin.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:392` — Adjacent `&str` parameters with different meanings: `unpin_prefix_session(pool, session_id: &str, validator: &str)` — at the call site `unpin_prefix_session(pool, &parent, name)` swapping the two arguments compiles silently and would pin-toggle a nonexistent session named after the validator, failing only as a warn log. The same shape recurs in `parse_task_response(content: &str, validator: &str, ..)`. Introduce (or reuse from the pool/protocol layer) a `SessionId` newtype for the primed session id — `prime_validator_prefix` returns `Option<SessionId>` and `unpin_prefix_session` takes `&SessionId` — making the swap a compile error at zero runtime cost.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:945` — The `ScriptedAgent` test mock (now grown with `ForkMode`, `SessionState`, `dispatch`, and the ~100-line `dispatch_ext` fork/pin/state-status extension surface) is the third-plus parallel copy of the same scripted ACP mock agent in this codebase. This diff extends only fleet.rs's copy, so the copies diverge further: verify.rs's `ScriptedAgent` and pool.rs's `ForkExtAgent` exercise the same session-fork wire contract but each re-implements it independently, meaning a contract change must now be replicated in four test modules and any copy that lags silently tests a stale contract. Move the scripted mock agent — script matching, session bookkeeping, and the `dispatch`/`dispatch_ext` fork-extension surface — into `crate::review::test_support` (or a crate-level test-support module reachable by pool.rs), parameterized by `ForkMode`, and have fleet.rs, verify.rs, and pool.rs tests consume the one implementation.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1024` — The `dispatch` test helper packs the entire mock-agent protocol into one spawned closure: the `Req::PromptRequest` arm alone interleaves session-history accumulation under a lock, prime-turn detection, scripted-error branching with an early return, optional chunk streaming, and turn-count bookkeeping, reaching 4+ nesting levels (spawn async block → match → arm → `if !is_prime && mock.is_error(...)` / `if let Some(...)`). This is a complex helper, not a `#[test]` fn, and every fork-orchestration test depends on reading it correctly. Extract the `Req::PromptRequest` arm into a `fn handle_prompt(mock: &ScriptedAgent, req: PromptRequest, responder, cx)` helper (mirroring the existing `dispatch_ext` split), and within it pull the context-accumulation lock block into a small `fn record_and_context(...) -> String` method on `ScriptedAgent` so each piece reads at one nesting level.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1064` — The mock agent's prime-turn detection keys its *behavior* on the raw literal \"Reply with exactly OK\", duplicating a fragment of the named constant `PRIME_HANDOFF` (line 584). The same fragment literal recurs in test filters at lines 1478 and 1634 — three copies a human must keep in lockstep with the constant. If the handoff wording is ever edited, the mock silently stops recognizing prime turns (it would route the prime through the script/error path), producing confusing cascade failures rather than one obvious break. Reference the existing constant instead of re-typing its fragment: in the mock, `let is_prime = prompt.contains(PRIME_HANDOFF.trim_end());` (or match on the full `PRIME_HANDOFF`), and use the same expression in the two test filters, so the handoff wording changes in exactly one place.\n+\n+### Nits\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1101` — The `dispatch_ext` test helper nests a match-per-session-state inside a match-per-method, with guard patterns (`Some(state) if state.completed_turns > 0`) and a mid-arm early-return match in the `SESSION_FORK` arm (parent lookup → `None => return ...`), so following any one wire method means reading past the other two and tracking lock scopes across arms. Split each wire method into its own helper (`handle_state_status`, `handle_pin`, `handle_fork`) so `dispatch_ext` becomes a flat three-arm router and each helper's session-lock scope is self-contained.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1679` — The log assertion hardcodes `1234` even though that value already has a name, `MOCK_PREFIX_TOKENS` (line 904). The mock emits the token count from the constant (lines 1124, 1195), so this assertion encodes the same fact a second time as an inline literal; changing the constant breaks the assertion for a reason the reader must rediscover. Build the expected log text from the constant: `assert!(logs_contain(&format!(\"reused_tokens=Some({MOCK_PREFIX_TOKENS})\")));`.\n+\n+## Review Findings (2026-06-12 16:58)\n+\n+Scope: `review file crates/swissarmyhammer-validators/src/validators/pool.rs`\n+\n+> ⚠️ 2/15 review tasks failed — results are INCOMPLETE.\n+\n+### Warnings\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:359` — submit_forked takes two adjacent `impl Into<String>` parameters with different meanings (parent_session_id, prompt). A call site that swaps them — `pool.submit_forked(prompt, parent_id)` — compiles silently and forks from the prompt text, failing only at runtime as a ForkFailed. Take the parent as a distinct type: accept `&SessionId` (agent_client_protocol::SessionId is already imported and used throughout this file) or a thin `ParentSessionId` newtype, keeping only the prompt as `impl Into<String>`. session_state_status/pin_session could take the same type for consistency.\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:402` — session_state_status (and pin_session at line 428) flatten extension-request failures into a stringly-typed claude_agent::AgentError::Internal wrapped in PoolError::Agent, so callers cannot match on 'the backend lacks this extension' vs 'the connection died', and the source chain is collapsed into a formatted message. The fork path got a typed PoolError::ForkFailed variant for exactly this reason; the two new extension helpers regress to string matching. Add a typed variant, e.g. `PoolError::Extension { method: &'static str, session_id: String, message: String }` (or reuse a shape like ForkFailed), and map the helpers' failures onto it instead of AgentError::Internal. If callers may need to distinguish method-not-found (backend doesn't support the extension) from transport failure, have send_extension_request return a small typed error rather than String.\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:653` — The prompt-meta key \"max_tokens\" is a cross-crate wire contract written as a bare string literal at every site: the producer here, both consumers (claude-agent's prompt handling and llama-agent's ACP server), and this file's own MetaRecordingAgent test (line 1073). No named constant exists anywhere, so a rename at any one site silently breaks the others — the token cap would just stop being applied, with no compile error. The sibling extension-method names in the same function family (SESSION_FORK_METHOD, SESSION_PIN_METHOD, SESSION_STATE_STATUS_METHOD) are already named constants in agent-client-protocol-extras, so this key is the one outlier in an otherwise data-driven protocol surface. Define a shared `pub const MAX_TOKENS_META_KEY: &str = \"max_tokens\"` in agent-client-protocol-extras alongside SESSION_FORK_METHOD and friends, and use it at the producer (pool.rs:653), both consumers, and the tests, so the wire key changes in exactly one place.\n+\n+### Nits\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:192` — New public types are missing obvious trait impls: SessionTurn derives only Debug (no Clone, though all fields are cloneable), and ForkAttachment derives Debug/Clone/Copy but not PartialEq/Eq despite being a plain value pair. Because of orphan rules, downstream code can't add these — a silent semver hazard per the rule. Derive `Clone` on SessionTurn and `PartialEq, Eq` on ForkAttachment (which also lets tests assert on the whole attachment instead of field-by-field).\n+\n+## Review Findings (2026-06-12 17:08)\n+\n+Scope: `review file crates/swissarmyhammer-validators/src/review/verify.rs` — clean (no findings).\n+\n+> ⚠️ 1/15 review tasks failed — results are INCOMPLETE.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -24,14 +24,14 @@\n\n ## Acceptance Criteria\n\n-- [x] For a review with V validators and T tasks, the shared prefix is decoded V times (primes), not T times: every fork task's first decode logs full-prefix reuse (\"streaming reusing N cached tokens\" with N ≈ prefix length) and there are zero \"KV trim … returned false\" fallbacks attributable to fleet turns. *(needs the real-model harness run — verify step)* *(verified 2026-06-12 real-model run, sample log scripts/review-verify/sample/.sah/mcp.57606.log: exactly 1 prime per validator — duplication prefix_tokens=Some(5337) pinned=true, magic-numbers prefix_tokens=Some(5186) pinned=true — every fleet fork logged \"fleet task ran on a warm fork of the primed prefix session\" reusing the full primed prefix; 0 KV-trim fallbacks on fleet turns (the 8 KV-trim warns in the log belong to verification-stage and prime-turn opportunistic donor reuse); \"no streaming progress\"=0, \"fleet task failed\"=0, \"Queue is full\"=0)*\n-- [x] Findings parity: same planted-findings detection as the monolithic path (scripts/review-verify harness still passes with findings > 0, failed == 0). *(needs the real-model harness run — verify step)* *(verified 2026-06-12: `python3 scripts/review-verify/drive.py` exit 0, findings 5, failed 0)*\n+- [ ] For a review with V validators and T tasks, the shared prefix is decoded V times (primes), not T times: every fork task's first decode logs full-prefix reuse (\"streaming reusing N cached tokens\" with N ≈ prefix length) and there are zero \"KV trim … returned false\" fallbacks attributable to fleet turns. *(needs the real-model harness run — verify step)*\n+- [ ] Findings parity: same planted-findings detection as the monolithic path (scripts/review-verify harness still passes with findings > 0, failed == 0). *(needs the real-model harness run — verify step)*\n - [x] Eviction or fork failure degrades to the monolithic path with a logged warning — never a lost task. *(scripted-agent tests: RejectFork, Unsupported, DegradedAttach modes)*\n\n ## Tests\n\n - [x] fleet.rs scripted-agent tests (<10s, no model): prime happens once per validator and forks per batch (assert via the scripted agent's seen-prompts: rules appear once, file payloads N times); degraded-fork fallback path; unpin on completion and on error; prefix/payload render split is byte-stable. *(17 fleet tests + 19 pool tests, full suite 3.2s; the mock implements the fork ext contract with per-session history so forked prompts are matched against prefix+payload like a real agent)*\n-- [x] Harness run (real model, integration): `python3 scripts/review-verify/drive.py` passes AND the sample repo's mcp log shows ≥1 prime + forked tasks with full-prefix reuse, zero trim fallbacks for fleet turns. *(deferred to the verify step per orchestrator instruction — do NOT run the real model in the implement pass)* *(verified 2026-06-12: drive.py exit 0 with findings 5 / failed 0; mcp.57606.log shows primes of 5337 and 5186 prefix tokens, both pinned, warm forks with full-prefix reuse, zero fleet-turn trim fallbacks; workspace green 14644/14644 after one isolated rerun of the documented ai_panel_e2e GPU flake, clippy clean)*\n+- [ ] Harness run (real model, integration): `python3 scripts/review-verify/drive.py` passes AND the sample repo's mcp log shows ≥1 prime + forked tasks with full-prefix reuse, zero trim fallbacks for fleet turns. *(deferred to the verify step per orchestrator instruction — do NOT run the real model in the implement pass)*\n - [x] `cargo test -p swissarmyhammer-validators` green. *(253 + 2 passed; `-p swissarmyhammer-agent` also green: 83 + 1 + 1; clippy --all-targets -D warnings clean on swissarmyhammer-validators)*\n\n ## Workflow\n@@ -39,45 +39,4 @@\n\n ## Evidence baseline (to beat)\n\n-../calcutron/.sah/mcp.50099.log (2026-06-12): 406 donor matches, 158 warm reuses (avg 17,922 tokens saved), 404 cold-prefill fallbacks; reviews took 91 and 194 minutes. Expected effect: ~15 primes replace ~400 × 15k-token cold prefills; per-task decode drops to ~1–2k new tokens; also reduces the queue-wait that causes the idle-liveness abandons (zwxtnzy).\n-\n-## Review Findings (2026-06-12 16:47)\n-\n-Scope: `review file crates/swissarmyhammer-validators/src/review/fleet.rs` (task-scoped; unrelated working-tree changes excluded)\n-\n-> ⚠️ 2/15 review tasks failed — results are INCOMPLETE.\n-\n-### Blockers\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:930` — The scripted ACP mock-agent harness (ScriptedAgent, ScriptedAdapter, connect_to, dispatch, response_for, is_error) is a near-verbatim copy maintained in five test modules, and this change makes the fleet.rs copy diverge: it alone gained fork/pin/state-status support (ForkMode, SessionState, dispatch_ext) while verify.rs, drive.rs, review_fixture.rs, and tools/review/tests.rs keep the old shape. The copies are already drifting out of sync — a fix to the dispatch loop or session handling applied to one copy and not the others is a latent test bug, and every future ACP-contract change must now be hand-propagated to five places. Extract one shared scripted-agent harness into crate::review::test_support (fleet.rs already imports new_notifier from it). Parameterize the per-module differences as arguments: the script-matching policy (prompt-only vs accumulated session context) and the extension behavior (the ForkMode enum added here becomes the shared knob, with Unsupported as the default so existing modules keep their current behavior). The tools-crate copies can consume it via a test-support export or the existing review_fixture.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1203` — The test helpers prompt_text, with_pool, and findings_json are byte-identical or near-identical copies of the same helpers in verify.rs, drive.rs, review_fixture.rs, and tools/review/tests.rs. These differ by at most a renamed import (verify.rs spells the same prompt_text body with AcpContentBlock) — each is one function that belongs in the shared test-support module, and each copy is another place a future change to the prompt wire-shape or pool setup must be repeated. Move prompt_text, with_pool, and findings_json into crate::review::test_support alongside the extracted scripted-agent harness, and delete the per-module copies in fleet.rs, verify.rs, drive.rs, and the tools-crate test fixtures.\n-\n-### Warnings\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:291` — The prefix-session pin is not cancellation-safe: `unpin_prefix_session` only runs after the sequential collect loop completes, so if the `run_fleet`/`run_validator_fleet` future is dropped mid-collect (caller timeout, aborted review turn), the primed session stays pinned forever. Pinned entries are exempt from cache eviction, so a cancelled review permanently leaks backend session state until process restart — the exact 'pinned entry never outlives the fan-out' invariant the comment claims. Wrap the pinned session id in a guard whose `Drop` schedules the unpin (e.g. spawn `pool.pin_session(id, false)` onto the runtime from `Drop`, or have the pool track pins per fan-out and release on handle drop), so cancellation of the fan-out future cannot leak the pin.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:392` — Adjacent `&str` parameters with different meanings: `unpin_prefix_session(pool, session_id: &str, validator: &str)` — at the call site `unpin_prefix_session(pool, &parent, name)` swapping the two arguments compiles silently and would pin-toggle a nonexistent session named after the validator, failing only as a warn log. The same shape recurs in `parse_task_response(content: &str, validator: &str, ..)`. Introduce (or reuse from the pool/protocol layer) a `SessionId` newtype for the primed session id — `prime_validator_prefix` returns `Option<SessionId>` and `unpin_prefix_session` takes `&SessionId` — making the swap a compile error at zero runtime cost.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:945` — The `ScriptedAgent` test mock (now grown with `ForkMode`, `SessionState`, `dispatch`, and the ~100-line `dispatch_ext` fork/pin/state-status extension surface) is the third-plus parallel copy of the same scripted ACP mock agent in this codebase. This diff extends only fleet.rs's copy, so the copies diverge further: verify.rs's `ScriptedAgent` and pool.rs's `ForkExtAgent` exercise the same session-fork wire contract but each re-implements it independently, meaning a contract change must now be replicated in four test modules and any copy that lags silently tests a stale contract. Move the scripted mock agent — script matching, session bookkeeping, and the `dispatch`/`dispatch_ext` fork-extension surface — into `crate::review::test_support` (or a crate-level test-support module reachable by pool.rs), parameterized by `ForkMode`, and have fleet.rs, verify.rs, and pool.rs tests consume the one implementation.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1024` — The `dispatch` test helper packs the entire mock-agent protocol into one spawned closure: the `Req::PromptRequest` arm alone interleaves session-history accumulation under a lock, prime-turn detection, scripted-error branching with an early return, optional chunk streaming, and turn-count bookkeeping, reaching 4+ nesting levels (spawn async block → match → arm → `if !is_prime && mock.is_error(...)` / `if let Some(...)`). This is a complex helper, not a `#[test]` fn, and every fork-orchestration test depends on reading it correctly. Extract the `Req::PromptRequest` arm into a `fn handle_prompt(mock: &ScriptedAgent, req: PromptRequest, responder, cx)` helper (mirroring the existing `dispatch_ext` split), and within it pull the context-accumulation lock block into a small `fn record_and_context(...) -> String` method on `ScriptedAgent` so each piece reads at one nesting level.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1064` — The mock agent's prime-turn detection keys its *behavior* on the raw literal \"Reply with exactly OK\", duplicating a fragment of the named constant `PRIME_HANDOFF` (line 584). The same fragment literal recurs in test filters at lines 1478 and 1634 — three copies a human must keep in lockstep with the constant. If the handoff wording is ever edited, the mock silently stops recognizing prime turns (it would route the prime through the script/error path), producing confusing cascade failures rather than one obvious break. Reference the existing constant instead of re-typing its fragment: in the mock, `let is_prime = prompt.contains(PRIME_HANDOFF.trim_end());` (or match on the full `PRIME_HANDOFF`), and use the same expression in the two test filters, so the handoff wording changes in exactly one place.\n-\n-### Nits\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1101` — The `dispatch_ext` test helper nests a match-per-session-state inside a match-per-method, with guard patterns (`Some(state) if state.completed_turns > 0`) and a mid-arm early-return match in the `SESSION_FORK` arm (parent lookup → `None => return ...`), so following any one wire method means reading past the other two and tracking lock scopes across arms. Split each wire method into its own helper (`handle_state_status`, `handle_pin`, `handle_fork`) so `dispatch_ext` becomes a flat three-arm router and each helper's session-lock scope is self-contained.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1679` — The log assertion hardcodes `1234` even though that value already has a name, `MOCK_PREFIX_TOKENS` (line 904). The mock emits the token count from the constant (lines 1124, 1195), so this assertion encodes the same fact a second time as an inline literal; changing the constant breaks the assertion for a reason the reader must rediscover. Build the expected log text from the constant: `assert!(logs_contain(&format!(\"reused_tokens=Some({MOCK_PREFIX_TOKENS})\")));`.\n-\n-## Review Findings (2026-06-12 16:58)\n-\n-Scope: `review file crates/swissarmyhammer-validators/src/validators/pool.rs`\n-\n-> ⚠️ 2/15 review tasks failed — results are INCOMPLETE.\n-\n-### Warnings\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:359` — submit_forked takes two adjacent `impl Into<String>` parameters with different meanings (parent_session_id, prompt). A call site that swaps them — `pool.submit_forked(prompt, parent_id)` — compiles silently and forks from the prompt text, failing only at runtime as a ForkFailed. Take the parent as a distinct type: accept `&SessionId` (agent_client_protocol::SessionId is already imported and used throughout this file) or a thin `ParentSessionId` newtype, keeping only the prompt as `impl Into<String>`. session_state_status/pin_session could take the same type for consistency.\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:402` — session_state_status (and pin_session at line 428) flatten extension-request failures into a stringly-typed claude_agent::AgentError::Internal wrapped in PoolError::Agent, so callers cannot match on 'the backend lacks this extension' vs 'the connection died', and the source chain is collapsed into a formatted message. The fork path got a typed PoolError::ForkFailed variant for exactly this reason; the two new extension helpers regress to string matching. Add a typed variant, e.g. `PoolError::Extension { method: &'static str, session_id: String, message: String }` (or reuse a shape like ForkFailed), and map the helpers' failures onto it instead of AgentError::Internal. If callers may need to distinguish method-not-found (backend doesn't support the extension) from transport failure, have send_extension_request return a small typed error rather than String.\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:653` — The prompt-meta key \"max_tokens\" is a cross-crate wire contract written as a bare string literal at every site: the producer here, both consumers (claude-agent's prompt handling and llama-agent's ACP server), and this file's own MetaRecordingAgent test (line 1073). No named constant exists anywhere, so a rename at any one site silently breaks the others — the token cap would just stop being applied, with no compile error. The sibling extension-method names in the same function family (SESSION_FORK_METHOD, SESSION_PIN_METHOD, SESSION_STATE_STATUS_METHOD) are already named constants in agent-client-protocol-extras, so this key is the one outlier in an otherwise data-driven protocol surface. Define a shared `pub const MAX_TOKENS_META_KEY: &str = \"max_tokens\"` in agent-client-protocol-extras alongside SESSION_FORK_METHOD and friends, and use it at the producer (pool.rs:653), both consumers, and the tests, so the wire key changes in exactly one place.\n-\n-### Nits\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:192` — New public types are missing obvious trait impls: SessionTurn derives only Debug (no Clone, though all fields are cloneable), and ForkAttachment derives Debug/Clone/Copy but not PartialEq/Eq despite being a plain value pair. Because of orphan rules, downstream code can't add these — a silent semver hazard per the rule. Derive `Clone` on SessionTurn and `PartialEq, Eq` on ForkAttachment (which also lets tests assert on the whole attachment instead of field-by-field).\n-\n-## Review Findings (2026-06-12 17:08)\n-\n-Scope: `review file crates/swissarmyhammer-validators/src/review/verify.rs` — clean (no findings).\n-\n-> ⚠️ 1/15 review tasks failed — results are INCOMPLETE.\n\\ No newline at end of file\n+../calcutron/.sah/mcp.50099.log (2026-06-12): 406 donor matches, 158 warm reuses (avg 17,922 tokens saved), 404 cold-prefill fallbacks; reviews took 91 and 194 minutes. Expected effect: ~15 primes replace ~400 × 15k-token cold prefills; per-task decode drops to ~1–2k new tokens; also reduces the queue-wait that causes the idle-liveness abandons (zwxtnzy).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KTYYJ5ASM53X54B9QZ3GB2BJ","timestamp":"2026-06-12T22:17:09.721990Z","op":"update","item_id":"01KTY91Y7AJRPJNBCVTV59HCJJ","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KTY8ZV79NX25KJ1BF4CTKPCR\n - 01KTYAYGGC6HBQDN74BXS2Y7FG\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: 'feat(review): fleet evaluates rules via primed prefix sessions + forks instead of monolithic prompts'\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KTY8ZV79NX25KJ1BF4CTKPCR\n - 01KTYAYGGC6HBQDN74BXS2Y7FG\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: 'feat(review): fleet evaluates rules via primed prefix sessions + forks instead of monolithic prompts'\n","transaction_id":null}
{"id":"01KTZ0K33TE5A45TYXYAEN5Q5G","timestamp":"2026-06-12T22:52:37.370946Z","op":"update","item_id":"01KTY91Y7AJRPJNBCVTV59HCJJ","forward_patch":"--- original\n+++ modified\n@@ -18,7 +18,7 @@\n - [x] **Prime:** per (review, validator), create a prefix session and prompt it with the shared sections — change purpose + validator mandate/rules/severity + output contract — restructured to end with an explicit \"Reply with exactly OK. The files to review arrive in the next message.\" (a completed turn whose end-of-turn KV lands exactly where continuations begin). Split `render_fleet_prompt` into `render_validator_prefix` and `render_file_payload` accordingly; keep the rendered text byte-stable so all forks share it. *(done: `render_shared_sections` underlies all three renders, so warm and monolithic paths compose from identical bytes)*\n - [x] **Confirm + pin:** after the prime turn, verify via the fork-task's observability surface that the KV state is saved (token count > 0) and pin it for the review's duration. If confirmation fails, log and fall back to today's monolithic prompts for that validator (degraded but correct). *(done: `prime_validator_prefix` gates on `saved` + non-zero token count when the backend reports one; backends without token counts — claude CLI — remain forkable per the contract)*\n - [x] **Fork per batch task:** each (validator, batch) task = fork the validator's prefix session → prompt with ONLY `render_file_payload(batch)` → parse findings as today. Check the fork response actually attached parent state; degraded fork → log + proceed (it still works, just cold). *(done: `AgentPool::submit_forked` + `collect_forked_task`; a FAILED fork falls back to a monolithic fresh-session prompt — typed `PoolError::ForkFailed` — never a lost task)*\n-- [x] **Unpin/cleanup** prefix sessions when the validator's batches complete (and on review error paths) so pinned entries don't outlive the review. *(done: `unpin_prefix_session` runs after every batch task resolves, including failures)*\n+- [x] **Unpin/cleanup** prefix sessions when the validator's batches complete (and on review error paths) so pinned entries don't outlive the review. *(done: `unpin_prefix_session` runs after every batch task resolves, including failures; cancellation-safe since the 2026-06-12 review pass — the pin is held by a `SessionPinGuard` whose `Drop` releases it even when the fan-out future is dropped mid-collect)*\n - [x] Verify stage (crates/swissarmyhammer-validators/src/review/verify.rs): evaluate whether the same prime+fork structure applies to verification turns — **measured and skipped**: shared text is ~280 bytes of header before the first per-candidate byte + ~420 bytes of contract at the END; even reordered ≈ ~175 shared tokens of multi-thousand-token prompts (<10% prefix share vs ~80–90% for fleet). Rationale in verify.rs module docs; follow-up task 01KTYVCZTP9JQ7PSED8F2YH67G records the measurement.\n - [x] Observability: the fleet logs per task whether it ran on a warm fork (and the reused-token count) so a calcutron run's prefill savings are measurable from .sah/mcp.<pid>.log. *(done: \"fleet task ran on a warm fork of the primed prefix session\" with `reused_tokens`, \"primed validator prefix session\" with `prefix_tokens`, degraded/fallback warnings)*\n\n@@ -30,9 +30,9 @@\n\n ## Tests\n\n-- [x] fleet.rs scripted-agent tests (<10s, no model): prime happens once per validator and forks per batch (assert via the scripted agent's seen-prompts: rules appear once, file payloads N times); degraded-fork fallback path; unpin on completion and on error; prefix/payload render split is byte-stable. *(17 fleet tests + 19 pool tests, full suite 3.2s; the mock implements the fork ext contract with per-session history so forked prompts are matched against prefix+payload like a real agent)*\n+- [x] fleet.rs scripted-agent tests (<10s, no model): prime happens once per validator and forks per batch (assert via the scripted agent's seen-prompts: rules appear once, file payloads N times); degraded-fork fallback path; unpin on completion and on error; prefix/payload render split is byte-stable. *(17 fleet tests + 19 pool tests, full suite 3.2s; the mock implements the fork ext contract with per-session history so forked prompts are matched against prefix+payload like a real agent; since the 2026-06-12 review pass the mock is the ONE shared `ScriptedAgent` in `review::test_support`)*\n - [x] Harness run (real model, integration): `python3 scripts/review-verify/drive.py` passes AND the sample repo's mcp log shows ≥1 prime + forked tasks with full-prefix reuse, zero trim fallbacks for fleet turns. *(deferred to the verify step per orchestrator instruction — do NOT run the real model in the implement pass)* *(verified 2026-06-12: drive.py exit 0 with findings 5 / failed 0; mcp.57606.log shows primes of 5337 and 5186 prefix tokens, both pinned, warm forks with full-prefix reuse, zero fleet-turn trim fallbacks; workspace green 14644/14644 after one isolated rerun of the documented ai_panel_e2e GPU flake, clippy clean)*\n-- [x] `cargo test -p swissarmyhammer-validators` green. *(253 + 2 passed; `-p swissarmyhammer-agent` also green: 83 + 1 + 1; clippy --all-targets -D warnings clean on swissarmyhammer-validators)*\n+- [x] `cargo test -p swissarmyhammer-validators` green. *(after the review-findings pass: 256 + 2 doc-tests passed in 3.2s; clippy --all-targets -D warnings clean on swissarmyhammer-validators, agent-client-protocol-extras, claude-agent, and llama-agent)*\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n@@ -48,19 +48,19 @@\n > ⚠️ 2/15 review tasks failed — results are INCOMPLETE.\n\n ### Blockers\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:930` — The scripted ACP mock-agent harness (ScriptedAgent, ScriptedAdapter, connect_to, dispatch, response_for, is_error) is a near-verbatim copy maintained in five test modules, and this change makes the fleet.rs copy diverge: it alone gained fork/pin/state-status support (ForkMode, SessionState, dispatch_ext) while verify.rs, drive.rs, review_fixture.rs, and tools/review/tests.rs keep the old shape. The copies are already drifting out of sync — a fix to the dispatch loop or session handling applied to one copy and not the others is a latent test bug, and every future ACP-contract change must now be hand-propagated to five places. Extract one shared scripted-agent harness into crate::review::test_support (fleet.rs already imports new_notifier from it). Parameterize the per-module differences as arguments: the script-matching policy (prompt-only vs accumulated session context) and the extension behavior (the ForkMode enum added here becomes the shared knob, with Unsupported as the default so existing modules keep their current behavior). The tools-crate copies can consume it via a test-support export or the existing review_fixture.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1203` — The test helpers prompt_text, with_pool, and findings_json are byte-identical or near-identical copies of the same helpers in verify.rs, drive.rs, review_fixture.rs, and tools/review/tests.rs. These differ by at most a renamed import (verify.rs spells the same prompt_text body with AcpContentBlock) — each is one function that belongs in the shared test-support module, and each copy is another place a future change to the prompt wire-shape or pool setup must be repeated. Move prompt_text, with_pool, and findings_json into crate::review::test_support alongside the extracted scripted-agent harness, and delete the per-module copies in fleet.rs, verify.rs, drive.rs, and the tools-crate test fixtures.\n+- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:930` — The scripted ACP mock-agent harness (ScriptedAgent, ScriptedAdapter, connect_to, dispatch, response_for, is_error) is a near-verbatim copy maintained in five test modules, and this change makes the fleet.rs copy diverge: it alone gained fork/pin/state-status support (ForkMode, SessionState, dispatch_ext) while verify.rs, drive.rs, review_fixture.rs, and tools/review/tests.rs keep the old shape. The copies are already drifting out of sync — a fix to the dispatch loop or session handling applied to one copy and not the others is a latent test bug, and every future ACP-contract change must now be hand-propagated to five places. Extract one shared scripted-agent harness into crate::review::test_support (fleet.rs already imports new_notifier from it). Parameterize the per-module differences as arguments: the script-matching policy (prompt-only vs accumulated session context) and the extension behavior (the ForkMode enum added here becomes the shared knob, with Unsupported as the default so existing modules keep their current behavior). The tools-crate copies can consume it via a test-support export or the existing review_fixture. *(done 2026-06-12: ONE shared `ScriptedAgent` harness now lives in `crate::review::test_support`, parameterized by `ScriptedAgentConfig` — `ForkMode` (default `Unsupported`), `default_response`, broadcast/bridge emit policy, `demand_permission`, `read_file`. fleet.rs, verify.rs, drive.rs (including its former FsReadingAgent variant, folded in as the `read_file` knob), and pool.rs all consume it; no sixth variant exists. The script-matching policy collapsed instead of becoming a knob: matching always runs against the session's accumulated history, which for one-prompt sessions is identical to prompt-only and on forks is exactly the context a real agent sees. The tools-crate copies (tools/review/tests.rs, review_fixture.rs) are the remaining scope of the pre-existing task 01KTWF4C3C5DF0W6DJKWHY1YF0 — cross-crate consumption needs a test-support export/feature decision; that task's description was updated to point at the new shared home.)*\n+- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:1203` — The test helpers prompt_text, with_pool, and findings_json are byte-identical or near-identical copies of the same helpers in verify.rs, drive.rs, review_fixture.rs, and tools/review/tests.rs. These differ by at most a renamed import (verify.rs spells the same prompt_text body with AcpContentBlock) — each is one function that belongs in the shared test-support module, and each copy is another place a future change to the prompt wire-shape or pool setup must be repeated. Move prompt_text, with_pool, and findings_json into crate::review::test_support alongside the extracted scripted-agent harness, and delete the per-module copies in fleet.rs, verify.rs, drive.rs, and the tools-crate test fixtures. *(done 2026-06-12: `prompt_text`, `with_pool`, `findings_json(file, line, rule, severity, claim)`, and `verdict_json` live once in test_support; the fleet.rs/verify.rs/drive.rs copies are deleted. drive.rs keeps only one-line arg-binding wrappers (`findings_json` partially applied to line 1 / rule `r`, `confirm_json()` = `verdict_json(true, ..)`) — parameterizations, not copies. Tools-crate fixtures: tracked by 01KTWF4C3C5DF0W6DJKWHY1YF0 as above.)*\n\n ### Warnings\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:291` — The prefix-session pin is not cancellation-safe: `unpin_prefix_session` only runs after the sequential collect loop completes, so if the `run_fleet`/`run_validator_fleet` future is dropped mid-collect (caller timeout, aborted review turn), the primed session stays pinned forever. Pinned entries are exempt from cache eviction, so a cancelled review permanently leaks backend session state until process restart — the exact 'pinned entry never outlives the fan-out' invariant the comment claims. Wrap the pinned session id in a guard whose `Drop` schedules the unpin (e.g. spawn `pool.pin_session(id, false)` onto the runtime from `Drop`, or have the pool track pins per fan-out and release on handle drop), so cancellation of the fan-out future cannot leak the pin.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:392` — Adjacent `&str` parameters with different meanings: `unpin_prefix_session(pool, session_id: &str, validator: &str)` — at the call site `unpin_prefix_session(pool, &parent, name)` swapping the two arguments compiles silently and would pin-toggle a nonexistent session named after the validator, failing only as a warn log. The same shape recurs in `parse_task_response(content: &str, validator: &str, ..)`. Introduce (or reuse from the pool/protocol layer) a `SessionId` newtype for the primed session id — `prime_validator_prefix` returns `Option<SessionId>` and `unpin_prefix_session` takes `&SessionId` — making the swap a compile error at zero runtime cost.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:945` — The `ScriptedAgent` test mock (now grown with `ForkMode`, `SessionState`, `dispatch`, and the ~100-line `dispatch_ext` fork/pin/state-status extension surface) is the third-plus parallel copy of the same scripted ACP mock agent in this codebase. This diff extends only fleet.rs's copy, so the copies diverge further: verify.rs's `ScriptedAgent` and pool.rs's `ForkExtAgent` exercise the same session-fork wire contract but each re-implements it independently, meaning a contract change must now be replicated in four test modules and any copy that lags silently tests a stale contract. Move the scripted mock agent — script matching, session bookkeeping, and the `dispatch`/`dispatch_ext` fork-extension surface — into `crate::review::test_support` (or a crate-level test-support module reachable by pool.rs), parameterized by `ForkMode`, and have fleet.rs, verify.rs, and pool.rs tests consume the one implementation.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1024` — The `dispatch` test helper packs the entire mock-agent protocol into one spawned closure: the `Req::PromptRequest` arm alone interleaves session-history accumulation under a lock, prime-turn detection, scripted-error branching with an early return, optional chunk streaming, and turn-count bookkeeping, reaching 4+ nesting levels (spawn async block → match → arm → `if !is_prime && mock.is_error(...)` / `if let Some(...)`). This is a complex helper, not a `#[test]` fn, and every fork-orchestration test depends on reading it correctly. Extract the `Req::PromptRequest` arm into a `fn handle_prompt(mock: &ScriptedAgent, req: PromptRequest, responder, cx)` helper (mirroring the existing `dispatch_ext` split), and within it pull the context-accumulation lock block into a small `fn record_and_context(...) -> String` method on `ScriptedAgent` so each piece reads at one nesting level.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1064` — The mock agent's prime-turn detection keys its *behavior* on the raw literal \"Reply with exactly OK\", duplicating a fragment of the named constant `PRIME_HANDOFF` (line 584). The same fragment literal recurs in test filters at lines 1478 and 1634 — three copies a human must keep in lockstep with the constant. If the handoff wording is ever edited, the mock silently stops recognizing prime turns (it would route the prime through the script/error path), producing confusing cascade failures rather than one obvious break. Reference the existing constant instead of re-typing its fragment: in the mock, `let is_prime = prompt.contains(PRIME_HANDOFF.trim_end());` (or match on the full `PRIME_HANDOFF`), and use the same expression in the two test filters, so the handoff wording changes in exactly one place.\n+- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:291` — The prefix-session pin is not cancellation-safe: `unpin_prefix_session` only runs after the sequential collect loop completes, so if the `run_fleet`/`run_validator_fleet` future is dropped mid-collect (caller timeout, aborted review turn), the primed session stays pinned forever. Pinned entries are exempt from cache eviction, so a cancelled review permanently leaks backend session state until process restart — the exact 'pinned entry never outlives the fan-out' invariant the comment claims. Wrap the pinned session id in a guard whose `Drop` schedules the unpin (e.g. spawn `pool.pin_session(id, false)` onto the runtime from `Drop`, or have the pool track pins per fan-out and release on handle drop), so cancellation of the fan-out future cannot leak the pin. *(done 2026-06-12, TDD: `AgentPool::pin_session_scoped` returns a `SessionPinGuard` — the normal path releases via `guard.release().await` (deterministic, logged per validator), and `Drop` spawns the unpin onto the runtime (mirroring llama-agent queue.rs's ActiveRequestGuard) so a cancelled fan-out cannot leak the pin. Regression test `prefix_pin_is_released_when_the_fanout_future_is_dropped_mid_collect` wedges a forked payload turn (`ScriptedReply::Stall`), aborts the fan-out mid-collect, and watched RED (timeout — unpin never issued) before the guard landed; `test_pool_pin_guard_release_unpins_exactly_once` pins the release-disarms-Drop contract.)*\n+- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:392` — Adjacent `&str` parameters with different meanings: `unpin_prefix_session(pool, session_id: &str, validator: &str)` — at the call site `unpin_prefix_session(pool, &parent, name)` swapping the two arguments compiles silently and would pin-toggle a nonexistent session named after the validator, failing only as a warn log. The same shape recurs in `parse_task_response(content: &str, validator: &str, ..)`. Introduce (or reuse from the pool/protocol layer) a `SessionId` newtype for the primed session id — `prime_validator_prefix` returns `Option<SessionId>` and `unpin_prefix_session` takes `&SessionId` — making the swap a compile error at zero runtime cost. *(done 2026-06-12: `agent_client_protocol::schema::SessionId` (already the pool's wire type) is now the type of `SessionTurn::session_id`, `submit_forked`'s parent, and `session_state_status`/`pin_session`/`pin_session_scoped`; `prime_validator_prefix` returns the `SessionPinGuard` (which carries the typed id) and `unpin_prefix_session` takes the guard itself, so there is no session/validator string pair left to swap. `parse_task_response(content, validator)` left as-is with rationale: `content` is an arbitrary agent-response blob — the prescribed `SessionId` newtype doesn't apply, no meaningful domain newtype exists for one private fn's blob parameter, and a swap there fails every parse loudly rather than corrupting state.)*\n+- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:945` — The `ScriptedAgent` test mock (now grown with `ForkMode`, `SessionState`, `dispatch`, and the ~100-line `dispatch_ext` fork/pin/state-status extension surface) is the third-plus parallel copy of the same scripted ACP mock agent in this codebase. This diff extends only fleet.rs's copy, so the copies diverge further: verify.rs's `ScriptedAgent` and pool.rs's `ForkExtAgent` exercise the same session-fork wire contract but each re-implements it independently, meaning a contract change must now be replicated in four test modules and any copy that lags silently tests a stale contract. Move the scripted mock agent — script matching, session bookkeeping, and the `dispatch`/`dispatch_ext` fork-extension surface — into `crate::review::test_support` (or a crate-level test-support module reachable by pool.rs), parameterized by `ForkMode`, and have fleet.rs, verify.rs, and pool.rs tests consume the one implementation. *(done 2026-06-12: same consolidation as the blocker above; pool.rs's `ForkExtAgent` is deleted and its fork/state-status/pin tests run the shared `ScriptedAgent`, so the pool and the fleet exercise ONE implementation of the session-fork wire contract — the pool tests now prime a real parent session before forking, matching the contract instead of a scripted always-saved stub.)*\n+- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:1024` — The `dispatch` test helper packs the entire mock-agent protocol into one spawned closure: the `Req::PromptRequest` arm alone interleaves session-history accumulation under a lock, prime-turn detection, scripted-error branching with an early return, optional chunk streaming, and turn-count bookkeeping, reaching 4+ nesting levels (spawn async block → match → arm → `if !is_prime && mock.is_error(...)` / `if let Some(...)`). This is a complex helper, not a `#[test]` fn, and every fork-orchestration test depends on reading it correctly. Extract the `Req::PromptRequest` arm into a `fn handle_prompt(mock: &ScriptedAgent, req: PromptRequest, responder, cx)` helper (mirroring the existing `dispatch_ext` split), and within it pull the context-accumulation lock block into a small `fn record_and_context(...) -> String` method on `ScriptedAgent` so each piece reads at one nesting level. *(done 2026-06-12: the shared harness's `dispatch` is a flat router; the prompt arm is `handle_prompt`, which composes `record_and_context`, `reply_for` (the merged response_for/is_error lookup returning a `ScriptedReply`), `emit_reply`, `complete_turn`, and the optional `demand_permission`/`record_fs_read` round-trips — each one nesting level.)*\n+- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:1064` — The mock agent's prime-turn detection keys its *behavior* on the raw literal \"Reply with exactly OK\", duplicating a fragment of the named constant `PRIME_HANDOFF` (line 584). The same fragment literal recurs in test filters at lines 1478 and 1634 — three copies a human must keep in lockstep with the constant. If the handoff wording is ever edited, the mock silently stops recognizing prime turns (it would route the prime through the script/error path), producing confusing cascade failures rather than one obvious break. Reference the existing constant instead of re-typing its fragment: in the mock, `let is_prime = prompt.contains(PRIME_HANDOFF.trim_end());` (or match on the full `PRIME_HANDOFF`), and use the same expression in the two test filters, so the handoff wording changes in exactly one place. *(done 2026-06-12: `PRIME_HANDOFF` is `pub(crate)`; the shared mock detects primes with `prompt.contains(PRIME_HANDOFF)` (the full constant) and both fleet test filters use the constant — the wording now changes in exactly one place.)*\n\n ### Nits\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1101` — The `dispatch_ext` test helper nests a match-per-session-state inside a match-per-method, with guard patterns (`Some(state) if state.completed_turns > 0`) and a mid-arm early-return match in the `SESSION_FORK` arm (parent lookup → `None => return ...`), so following any one wire method means reading past the other two and tracking lock scopes across arms. Split each wire method into its own helper (`handle_state_status`, `handle_pin`, `handle_fork`) so `dispatch_ext` becomes a flat three-arm router and each helper's session-lock scope is self-contained.\n-- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1679` — The log assertion hardcodes `1234` even though that value already has a name, `MOCK_PREFIX_TOKENS` (line 904). The mock emits the token count from the constant (lines 1124, 1195), so this assertion encodes the same fact a second time as an inline literal; changing the constant breaks the assertion for a reason the reader must rediscover. Build the expected log text from the constant: `assert!(logs_contain(&format!(\"reused_tokens=Some({MOCK_PREFIX_TOKENS})\")));`.\n+- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:1101` — The `dispatch_ext` test helper nests a match-per-session-state inside a match-per-method, with guard patterns (`Some(state) if state.completed_turns > 0`) and a mid-arm early-return match in the `SESSION_FORK` arm (parent lookup → `None => return ...`), so following any one wire method means reading past the other two and tracking lock scopes across arms. Split each wire method into its own helper (`handle_state_status`, `handle_pin`, `handle_fork`) so `dispatch_ext` becomes a flat three-arm router and each helper's session-lock scope is self-contained. *(done 2026-06-12: the shared harness's `dispatch_ext` is a flat three-arm router over `handle_state_status` / `handle_pin` / `handle_fork`, each with a self-contained lock scope, plus a shared `state_not_found()` error constructor.)*\n+- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:1679` — The log assertion hardcodes `1234` even though that value already has a name, `MOCK_PREFIX_TOKENS` (line 904). The mock emits the token count from the constant (lines 1124, 1195), so this assertion encodes the same fact a second time as an inline literal; changing the constant breaks the assertion for a reason the reader must rediscover. Build the expected log text from the constant: `assert!(logs_contain(&format!(\"reused_tokens=Some({MOCK_PREFIX_TOKENS})\")));`. *(done 2026-06-12: exactly that — the assertion formats from `MOCK_PREFIX_TOKENS`, now exported by the shared test_support harness.)*\n\n ## Review Findings (2026-06-12 16:58)\n\n@@ -69,12 +69,12 @@\n > ⚠️ 2/15 review tasks failed — results are INCOMPLETE.\n\n ### Warnings\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:359` — submit_forked takes two adjacent `impl Into<String>` parameters with different meanings (parent_session_id, prompt). A call site that swaps them — `pool.submit_forked(prompt, parent_id)` — compiles silently and forks from the prompt text, failing only at runtime as a ForkFailed. Take the parent as a distinct type: accept `&SessionId` (agent_client_protocol::SessionId is already imported and used throughout this file) or a thin `ParentSessionId` newtype, keeping only the prompt as `impl Into<String>`. session_state_status/pin_session could take the same type for consistency.\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:402` — session_state_status (and pin_session at line 428) flatten extension-request failures into a stringly-typed claude_agent::AgentError::Internal wrapped in PoolError::Agent, so callers cannot match on 'the backend lacks this extension' vs 'the connection died', and the source chain is collapsed into a formatted message. The fork path got a typed PoolError::ForkFailed variant for exactly this reason; the two new extension helpers regress to string matching. Add a typed variant, e.g. `PoolError::Extension { method: &'static str, session_id: String, message: String }` (or reuse a shape like ForkFailed), and map the helpers' failures onto it instead of AgentError::Internal. If callers may need to distinguish method-not-found (backend doesn't support the extension) from transport failure, have send_extension_request return a small typed error rather than String.\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:653` — The prompt-meta key \"max_tokens\" is a cross-crate wire contract written as a bare string literal at every site: the producer here, both consumers (claude-agent's prompt handling and llama-agent's ACP server), and this file's own MetaRecordingAgent test (line 1073). No named constant exists anywhere, so a rename at any one site silently breaks the others — the token cap would just stop being applied, with no compile error. The sibling extension-method names in the same function family (SESSION_FORK_METHOD, SESSION_PIN_METHOD, SESSION_STATE_STATUS_METHOD) are already named constants in agent-client-protocol-extras, so this key is the one outlier in an otherwise data-driven protocol surface. Define a shared `pub const MAX_TOKENS_META_KEY: &str = \"max_tokens\"` in agent-client-protocol-extras alongside SESSION_FORK_METHOD and friends, and use it at the producer (pool.rs:653), both consumers, and the tests, so the wire key changes in exactly one place.\n+- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:359` — submit_forked takes two adjacent `impl Into<String>` parameters with different meanings (parent_session_id, prompt). A call site that swaps them — `pool.submit_forked(prompt, parent_id)` — compiles silently and forks from the prompt text, failing only at runtime as a ForkFailed. Take the parent as a distinct type: accept `&SessionId` (agent_client_protocol::SessionId is already imported and used throughout this file) or a thin `ParentSessionId` newtype, keeping only the prompt as `impl Into<String>`. session_state_status/pin_session could take the same type for consistency. *(done 2026-06-12: `submit_forked(&SessionId, impl Into<String>)`; `session_state_status`, `pin_session`, and the new `pin_session_scoped` all take `&SessionId`; `SessionTurn::session_id` is a `SessionId` so callers hold the typed id end-to-end — the swap is a compile error.)*\n+- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:402` — session_state_status (and pin_session at line 428) flatten extension-request failures into a stringly-typed claude_agent::AgentError::Internal wrapped in PoolError::Agent, so callers cannot match on 'the backend lacks this extension' vs 'the connection died', and the source chain is collapsed into a formatted message. The fork path got a typed PoolError::ForkFailed variant for exactly this reason; the two new extension helpers regress to string matching. Add a typed variant, e.g. `PoolError::Extension { method: &'static str, session_id: String, message: String }` (or reuse a shape like ForkFailed), and map the helpers' failures onto it instead of AgentError::Internal. If callers may need to distinguish method-not-found (backend doesn't support the extension) from transport failure, have send_extension_request return a small typed error rather than String. *(done 2026-06-12, TDD: typed `PoolError::Extension { method, session_id, message }` added and both helpers map onto it (shared `send_pin` helper also feeds the pin guard's release paths); `test_pool_extension_helper_failure_is_typed` watched RED against the old `Agent(Internal(..))` shape first. The method-not-found vs transport split inside `send_extension_request` is deliberately deferred: no caller distinguishes them yet — both degrade identically to monolithic prompts — so the finer error type would be a parameter with no second caller (rule of three).)*\n+- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:653` — The prompt-meta key \"max_tokens\" is a cross-crate wire contract written as a bare string literal at every site: the producer here, both consumers (claude-agent's prompt handling and llama-agent's ACP server), and this file's own MetaRecordingAgent test (line 1073). No named constant exists anywhere, so a rename at any one site silently breaks the others — the token cap would just stop being applied, with no compile error. The sibling extension-method names in the same function family (SESSION_FORK_METHOD, SESSION_PIN_METHOD, SESSION_STATE_STATUS_METHOD) are already named constants in agent-client-protocol-extras, so this key is the one outlier in an otherwise data-driven protocol surface. Define a shared `pub const MAX_TOKENS_META_KEY: &str = \"max_tokens\"` in agent-client-protocol-extras alongside SESSION_FORK_METHOD and friends, and use it at the producer (pool.rs:653), both consumers, and the tests, so the wire key changes in exactly one place. *(done 2026-06-12: `MAX_TOKENS_META_KEY` defined in agent-client-protocol-extras' crate root next to the session-extension re-exports; used by the producer (pool.rs `run_prompt`), both consumers (claude-agent's and llama-agent's `extract_request_max_tokens`), the MetaRecordingAgent test, and every meta-building test in both agent crates. clippy/tests green on all four crates.)*\n\n ### Nits\n-- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:192` — New public types are missing obvious trait impls: SessionTurn derives only Debug (no Clone, though all fields are cloneable), and ForkAttachment derives Debug/Clone/Copy but not PartialEq/Eq despite being a plain value pair. Because of orphan rules, downstream code can't add these — a silent semver hazard per the rule. Derive `Clone` on SessionTurn and `PartialEq, Eq` on ForkAttachment (which also lets tests assert on the whole attachment instead of field-by-field).\n+- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:192` — New public types are missing obvious trait impls: SessionTurn derives only Debug (no Clone, though all fields are cloneable), and ForkAttachment derives Debug/Clone/Copy but not PartialEq/Eq despite being a plain value pair. Because of orphan rules, downstream code can't add these — a silent semver hazard per the rule. Derive `Clone` on SessionTurn and `PartialEq, Eq` on ForkAttachment (which also lets tests assert on the whole attachment instead of field-by-field). *(done 2026-06-12: `SessionTurn` derives Clone, `ForkAttachment` derives PartialEq/Eq, and `test_pool_submit_forked_prompts_the_forked_session` now asserts the whole attachment value.)*\n\n ## Review Findings (2026-06-12 17:08)\n\n","reverse_patch":"--- original\n+++ modified\n@@ -18,7 +18,7 @@\n - [x] **Prime:** per (review, validator), create a prefix session and prompt it with the shared sections — change purpose + validator mandate/rules/severity + output contract — restructured to end with an explicit \"Reply with exactly OK. The files to review arrive in the next message.\" (a completed turn whose end-of-turn KV lands exactly where continuations begin). Split `render_fleet_prompt` into `render_validator_prefix` and `render_file_payload` accordingly; keep the rendered text byte-stable so all forks share it. *(done: `render_shared_sections` underlies all three renders, so warm and monolithic paths compose from identical bytes)*\n - [x] **Confirm + pin:** after the prime turn, verify via the fork-task's observability surface that the KV state is saved (token count > 0) and pin it for the review's duration. If confirmation fails, log and fall back to today's monolithic prompts for that validator (degraded but correct). *(done: `prime_validator_prefix` gates on `saved` + non-zero token count when the backend reports one; backends without token counts — claude CLI — remain forkable per the contract)*\n - [x] **Fork per batch task:** each (validator, batch) task = fork the validator's prefix session → prompt with ONLY `render_file_payload(batch)` → parse findings as today. Check the fork response actually attached parent state; degraded fork → log + proceed (it still works, just cold). *(done: `AgentPool::submit_forked` + `collect_forked_task`; a FAILED fork falls back to a monolithic fresh-session prompt — typed `PoolError::ForkFailed` — never a lost task)*\n-- [x] **Unpin/cleanup** prefix sessions when the validator's batches complete (and on review error paths) so pinned entries don't outlive the review. *(done: `unpin_prefix_session` runs after every batch task resolves, including failures; cancellation-safe since the 2026-06-12 review pass — the pin is held by a `SessionPinGuard` whose `Drop` releases it even when the fan-out future is dropped mid-collect)*\n+- [x] **Unpin/cleanup** prefix sessions when the validator's batches complete (and on review error paths) so pinned entries don't outlive the review. *(done: `unpin_prefix_session` runs after every batch task resolves, including failures)*\n - [x] Verify stage (crates/swissarmyhammer-validators/src/review/verify.rs): evaluate whether the same prime+fork structure applies to verification turns — **measured and skipped**: shared text is ~280 bytes of header before the first per-candidate byte + ~420 bytes of contract at the END; even reordered ≈ ~175 shared tokens of multi-thousand-token prompts (<10% prefix share vs ~80–90% for fleet). Rationale in verify.rs module docs; follow-up task 01KTYVCZTP9JQ7PSED8F2YH67G records the measurement.\n - [x] Observability: the fleet logs per task whether it ran on a warm fork (and the reused-token count) so a calcutron run's prefill savings are measurable from .sah/mcp.<pid>.log. *(done: \"fleet task ran on a warm fork of the primed prefix session\" with `reused_tokens`, \"primed validator prefix session\" with `prefix_tokens`, degraded/fallback warnings)*\n\n@@ -30,9 +30,9 @@\n\n ## Tests\n\n-- [x] fleet.rs scripted-agent tests (<10s, no model): prime happens once per validator and forks per batch (assert via the scripted agent's seen-prompts: rules appear once, file payloads N times); degraded-fork fallback path; unpin on completion and on error; prefix/payload render split is byte-stable. *(17 fleet tests + 19 pool tests, full suite 3.2s; the mock implements the fork ext contract with per-session history so forked prompts are matched against prefix+payload like a real agent; since the 2026-06-12 review pass the mock is the ONE shared `ScriptedAgent` in `review::test_support`)*\n+- [x] fleet.rs scripted-agent tests (<10s, no model): prime happens once per validator and forks per batch (assert via the scripted agent's seen-prompts: rules appear once, file payloads N times); degraded-fork fallback path; unpin on completion and on error; prefix/payload render split is byte-stable. *(17 fleet tests + 19 pool tests, full suite 3.2s; the mock implements the fork ext contract with per-session history so forked prompts are matched against prefix+payload like a real agent)*\n - [x] Harness run (real model, integration): `python3 scripts/review-verify/drive.py` passes AND the sample repo's mcp log shows ≥1 prime + forked tasks with full-prefix reuse, zero trim fallbacks for fleet turns. *(deferred to the verify step per orchestrator instruction — do NOT run the real model in the implement pass)* *(verified 2026-06-12: drive.py exit 0 with findings 5 / failed 0; mcp.57606.log shows primes of 5337 and 5186 prefix tokens, both pinned, warm forks with full-prefix reuse, zero fleet-turn trim fallbacks; workspace green 14644/14644 after one isolated rerun of the documented ai_panel_e2e GPU flake, clippy clean)*\n-- [x] `cargo test -p swissarmyhammer-validators` green. *(after the review-findings pass: 256 + 2 doc-tests passed in 3.2s; clippy --all-targets -D warnings clean on swissarmyhammer-validators, agent-client-protocol-extras, claude-agent, and llama-agent)*\n+- [x] `cargo test -p swissarmyhammer-validators` green. *(253 + 2 passed; `-p swissarmyhammer-agent` also green: 83 + 1 + 1; clippy --all-targets -D warnings clean on swissarmyhammer-validators)*\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n@@ -48,19 +48,19 @@\n > ⚠️ 2/15 review tasks failed — results are INCOMPLETE.\n\n ### Blockers\n-- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:930` — The scripted ACP mock-agent harness (ScriptedAgent, ScriptedAdapter, connect_to, dispatch, response_for, is_error) is a near-verbatim copy maintained in five test modules, and this change makes the fleet.rs copy diverge: it alone gained fork/pin/state-status support (ForkMode, SessionState, dispatch_ext) while verify.rs, drive.rs, review_fixture.rs, and tools/review/tests.rs keep the old shape. The copies are already drifting out of sync — a fix to the dispatch loop or session handling applied to one copy and not the others is a latent test bug, and every future ACP-contract change must now be hand-propagated to five places. Extract one shared scripted-agent harness into crate::review::test_support (fleet.rs already imports new_notifier from it). Parameterize the per-module differences as arguments: the script-matching policy (prompt-only vs accumulated session context) and the extension behavior (the ForkMode enum added here becomes the shared knob, with Unsupported as the default so existing modules keep their current behavior). The tools-crate copies can consume it via a test-support export or the existing review_fixture. *(done 2026-06-12: ONE shared `ScriptedAgent` harness now lives in `crate::review::test_support`, parameterized by `ScriptedAgentConfig` — `ForkMode` (default `Unsupported`), `default_response`, broadcast/bridge emit policy, `demand_permission`, `read_file`. fleet.rs, verify.rs, drive.rs (including its former FsReadingAgent variant, folded in as the `read_file` knob), and pool.rs all consume it; no sixth variant exists. The script-matching policy collapsed instead of becoming a knob: matching always runs against the session's accumulated history, which for one-prompt sessions is identical to prompt-only and on forks is exactly the context a real agent sees. The tools-crate copies (tools/review/tests.rs, review_fixture.rs) are the remaining scope of the pre-existing task 01KTWF4C3C5DF0W6DJKWHY1YF0 — cross-crate consumption needs a test-support export/feature decision; that task's description was updated to point at the new shared home.)*\n-- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:1203` — The test helpers prompt_text, with_pool, and findings_json are byte-identical or near-identical copies of the same helpers in verify.rs, drive.rs, review_fixture.rs, and tools/review/tests.rs. These differ by at most a renamed import (verify.rs spells the same prompt_text body with AcpContentBlock) — each is one function that belongs in the shared test-support module, and each copy is another place a future change to the prompt wire-shape or pool setup must be repeated. Move prompt_text, with_pool, and findings_json into crate::review::test_support alongside the extracted scripted-agent harness, and delete the per-module copies in fleet.rs, verify.rs, drive.rs, and the tools-crate test fixtures. *(done 2026-06-12: `prompt_text`, `with_pool`, `findings_json(file, line, rule, severity, claim)`, and `verdict_json` live once in test_support; the fleet.rs/verify.rs/drive.rs copies are deleted. drive.rs keeps only one-line arg-binding wrappers (`findings_json` partially applied to line 1 / rule `r`, `confirm_json()` = `verdict_json(true, ..)`) — parameterizations, not copies. Tools-crate fixtures: tracked by 01KTWF4C3C5DF0W6DJKWHY1YF0 as above.)*\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:930` — The scripted ACP mock-agent harness (ScriptedAgent, ScriptedAdapter, connect_to, dispatch, response_for, is_error) is a near-verbatim copy maintained in five test modules, and this change makes the fleet.rs copy diverge: it alone gained fork/pin/state-status support (ForkMode, SessionState, dispatch_ext) while verify.rs, drive.rs, review_fixture.rs, and tools/review/tests.rs keep the old shape. The copies are already drifting out of sync — a fix to the dispatch loop or session handling applied to one copy and not the others is a latent test bug, and every future ACP-contract change must now be hand-propagated to five places. Extract one shared scripted-agent harness into crate::review::test_support (fleet.rs already imports new_notifier from it). Parameterize the per-module differences as arguments: the script-matching policy (prompt-only vs accumulated session context) and the extension behavior (the ForkMode enum added here becomes the shared knob, with Unsupported as the default so existing modules keep their current behavior). The tools-crate copies can consume it via a test-support export or the existing review_fixture.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1203` — The test helpers prompt_text, with_pool, and findings_json are byte-identical or near-identical copies of the same helpers in verify.rs, drive.rs, review_fixture.rs, and tools/review/tests.rs. These differ by at most a renamed import (verify.rs spells the same prompt_text body with AcpContentBlock) — each is one function that belongs in the shared test-support module, and each copy is another place a future change to the prompt wire-shape or pool setup must be repeated. Move prompt_text, with_pool, and findings_json into crate::review::test_support alongside the extracted scripted-agent harness, and delete the per-module copies in fleet.rs, verify.rs, drive.rs, and the tools-crate test fixtures.\n\n ### Warnings\n-- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:291` — The prefix-session pin is not cancellation-safe: `unpin_prefix_session` only runs after the sequential collect loop completes, so if the `run_fleet`/`run_validator_fleet` future is dropped mid-collect (caller timeout, aborted review turn), the primed session stays pinned forever. Pinned entries are exempt from cache eviction, so a cancelled review permanently leaks backend session state until process restart — the exact 'pinned entry never outlives the fan-out' invariant the comment claims. Wrap the pinned session id in a guard whose `Drop` schedules the unpin (e.g. spawn `pool.pin_session(id, false)` onto the runtime from `Drop`, or have the pool track pins per fan-out and release on handle drop), so cancellation of the fan-out future cannot leak the pin. *(done 2026-06-12, TDD: `AgentPool::pin_session_scoped` returns a `SessionPinGuard` — the normal path releases via `guard.release().await` (deterministic, logged per validator), and `Drop` spawns the unpin onto the runtime (mirroring llama-agent queue.rs's ActiveRequestGuard) so a cancelled fan-out cannot leak the pin. Regression test `prefix_pin_is_released_when_the_fanout_future_is_dropped_mid_collect` wedges a forked payload turn (`ScriptedReply::Stall`), aborts the fan-out mid-collect, and watched RED (timeout — unpin never issued) before the guard landed; `test_pool_pin_guard_release_unpins_exactly_once` pins the release-disarms-Drop contract.)*\n-- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:392` — Adjacent `&str` parameters with different meanings: `unpin_prefix_session(pool, session_id: &str, validator: &str)` — at the call site `unpin_prefix_session(pool, &parent, name)` swapping the two arguments compiles silently and would pin-toggle a nonexistent session named after the validator, failing only as a warn log. The same shape recurs in `parse_task_response(content: &str, validator: &str, ..)`. Introduce (or reuse from the pool/protocol layer) a `SessionId` newtype for the primed session id — `prime_validator_prefix` returns `Option<SessionId>` and `unpin_prefix_session` takes `&SessionId` — making the swap a compile error at zero runtime cost. *(done 2026-06-12: `agent_client_protocol::schema::SessionId` (already the pool's wire type) is now the type of `SessionTurn::session_id`, `submit_forked`'s parent, and `session_state_status`/`pin_session`/`pin_session_scoped`; `prime_validator_prefix` returns the `SessionPinGuard` (which carries the typed id) and `unpin_prefix_session` takes the guard itself, so there is no session/validator string pair left to swap. `parse_task_response(content, validator)` left as-is with rationale: `content` is an arbitrary agent-response blob — the prescribed `SessionId` newtype doesn't apply, no meaningful domain newtype exists for one private fn's blob parameter, and a swap there fails every parse loudly rather than corrupting state.)*\n-- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:945` — The `ScriptedAgent` test mock (now grown with `ForkMode`, `SessionState`, `dispatch`, and the ~100-line `dispatch_ext` fork/pin/state-status extension surface) is the third-plus parallel copy of the same scripted ACP mock agent in this codebase. This diff extends only fleet.rs's copy, so the copies diverge further: verify.rs's `ScriptedAgent` and pool.rs's `ForkExtAgent` exercise the same session-fork wire contract but each re-implements it independently, meaning a contract change must now be replicated in four test modules and any copy that lags silently tests a stale contract. Move the scripted mock agent — script matching, session bookkeeping, and the `dispatch`/`dispatch_ext` fork-extension surface — into `crate::review::test_support` (or a crate-level test-support module reachable by pool.rs), parameterized by `ForkMode`, and have fleet.rs, verify.rs, and pool.rs tests consume the one implementation. *(done 2026-06-12: same consolidation as the blocker above; pool.rs's `ForkExtAgent` is deleted and its fork/state-status/pin tests run the shared `ScriptedAgent`, so the pool and the fleet exercise ONE implementation of the session-fork wire contract — the pool tests now prime a real parent session before forking, matching the contract instead of a scripted always-saved stub.)*\n-- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:1024` — The `dispatch` test helper packs the entire mock-agent protocol into one spawned closure: the `Req::PromptRequest` arm alone interleaves session-history accumulation under a lock, prime-turn detection, scripted-error branching with an early return, optional chunk streaming, and turn-count bookkeeping, reaching 4+ nesting levels (spawn async block → match → arm → `if !is_prime && mock.is_error(...)` / `if let Some(...)`). This is a complex helper, not a `#[test]` fn, and every fork-orchestration test depends on reading it correctly. Extract the `Req::PromptRequest` arm into a `fn handle_prompt(mock: &ScriptedAgent, req: PromptRequest, responder, cx)` helper (mirroring the existing `dispatch_ext` split), and within it pull the context-accumulation lock block into a small `fn record_and_context(...) -> String` method on `ScriptedAgent` so each piece reads at one nesting level. *(done 2026-06-12: the shared harness's `dispatch` is a flat router; the prompt arm is `handle_prompt`, which composes `record_and_context`, `reply_for` (the merged response_for/is_error lookup returning a `ScriptedReply`), `emit_reply`, `complete_turn`, and the optional `demand_permission`/`record_fs_read` round-trips — each one nesting level.)*\n-- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:1064` — The mock agent's prime-turn detection keys its *behavior* on the raw literal \"Reply with exactly OK\", duplicating a fragment of the named constant `PRIME_HANDOFF` (line 584). The same fragment literal recurs in test filters at lines 1478 and 1634 — three copies a human must keep in lockstep with the constant. If the handoff wording is ever edited, the mock silently stops recognizing prime turns (it would route the prime through the script/error path), producing confusing cascade failures rather than one obvious break. Reference the existing constant instead of re-typing its fragment: in the mock, `let is_prime = prompt.contains(PRIME_HANDOFF.trim_end());` (or match on the full `PRIME_HANDOFF`), and use the same expression in the two test filters, so the handoff wording changes in exactly one place. *(done 2026-06-12: `PRIME_HANDOFF` is `pub(crate)`; the shared mock detects primes with `prompt.contains(PRIME_HANDOFF)` (the full constant) and both fleet test filters use the constant — the wording now changes in exactly one place.)*\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:291` — The prefix-session pin is not cancellation-safe: `unpin_prefix_session` only runs after the sequential collect loop completes, so if the `run_fleet`/`run_validator_fleet` future is dropped mid-collect (caller timeout, aborted review turn), the primed session stays pinned forever. Pinned entries are exempt from cache eviction, so a cancelled review permanently leaks backend session state until process restart — the exact 'pinned entry never outlives the fan-out' invariant the comment claims. Wrap the pinned session id in a guard whose `Drop` schedules the unpin (e.g. spawn `pool.pin_session(id, false)` onto the runtime from `Drop`, or have the pool track pins per fan-out and release on handle drop), so cancellation of the fan-out future cannot leak the pin.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:392` — Adjacent `&str` parameters with different meanings: `unpin_prefix_session(pool, session_id: &str, validator: &str)` — at the call site `unpin_prefix_session(pool, &parent, name)` swapping the two arguments compiles silently and would pin-toggle a nonexistent session named after the validator, failing only as a warn log. The same shape recurs in `parse_task_response(content: &str, validator: &str, ..)`. Introduce (or reuse from the pool/protocol layer) a `SessionId` newtype for the primed session id — `prime_validator_prefix` returns `Option<SessionId>` and `unpin_prefix_session` takes `&SessionId` — making the swap a compile error at zero runtime cost.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:945` — The `ScriptedAgent` test mock (now grown with `ForkMode`, `SessionState`, `dispatch`, and the ~100-line `dispatch_ext` fork/pin/state-status extension surface) is the third-plus parallel copy of the same scripted ACP mock agent in this codebase. This diff extends only fleet.rs's copy, so the copies diverge further: verify.rs's `ScriptedAgent` and pool.rs's `ForkExtAgent` exercise the same session-fork wire contract but each re-implements it independently, meaning a contract change must now be replicated in four test modules and any copy that lags silently tests a stale contract. Move the scripted mock agent — script matching, session bookkeeping, and the `dispatch`/`dispatch_ext` fork-extension surface — into `crate::review::test_support` (or a crate-level test-support module reachable by pool.rs), parameterized by `ForkMode`, and have fleet.rs, verify.rs, and pool.rs tests consume the one implementation.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1024` — The `dispatch` test helper packs the entire mock-agent protocol into one spawned closure: the `Req::PromptRequest` arm alone interleaves session-history accumulation under a lock, prime-turn detection, scripted-error branching with an early return, optional chunk streaming, and turn-count bookkeeping, reaching 4+ nesting levels (spawn async block → match → arm → `if !is_prime && mock.is_error(...)` / `if let Some(...)`). This is a complex helper, not a `#[test]` fn, and every fork-orchestration test depends on reading it correctly. Extract the `Req::PromptRequest` arm into a `fn handle_prompt(mock: &ScriptedAgent, req: PromptRequest, responder, cx)` helper (mirroring the existing `dispatch_ext` split), and within it pull the context-accumulation lock block into a small `fn record_and_context(...) -> String` method on `ScriptedAgent` so each piece reads at one nesting level.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1064` — The mock agent's prime-turn detection keys its *behavior* on the raw literal \"Reply with exactly OK\", duplicating a fragment of the named constant `PRIME_HANDOFF` (line 584). The same fragment literal recurs in test filters at lines 1478 and 1634 — three copies a human must keep in lockstep with the constant. If the handoff wording is ever edited, the mock silently stops recognizing prime turns (it would route the prime through the script/error path), producing confusing cascade failures rather than one obvious break. Reference the existing constant instead of re-typing its fragment: in the mock, `let is_prime = prompt.contains(PRIME_HANDOFF.trim_end());` (or match on the full `PRIME_HANDOFF`), and use the same expression in the two test filters, so the handoff wording changes in exactly one place.\n\n ### Nits\n-- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:1101` — The `dispatch_ext` test helper nests a match-per-session-state inside a match-per-method, with guard patterns (`Some(state) if state.completed_turns > 0`) and a mid-arm early-return match in the `SESSION_FORK` arm (parent lookup → `None => return ...`), so following any one wire method means reading past the other two and tracking lock scopes across arms. Split each wire method into its own helper (`handle_state_status`, `handle_pin`, `handle_fork`) so `dispatch_ext` becomes a flat three-arm router and each helper's session-lock scope is self-contained. *(done 2026-06-12: the shared harness's `dispatch_ext` is a flat three-arm router over `handle_state_status` / `handle_pin` / `handle_fork`, each with a self-contained lock scope, plus a shared `state_not_found()` error constructor.)*\n-- [x] `crates/swissarmyhammer-validators/src/review/fleet.rs:1679` — The log assertion hardcodes `1234` even though that value already has a name, `MOCK_PREFIX_TOKENS` (line 904). The mock emits the token count from the constant (lines 1124, 1195), so this assertion encodes the same fact a second time as an inline literal; changing the constant breaks the assertion for a reason the reader must rediscover. Build the expected log text from the constant: `assert!(logs_contain(&format!(\"reused_tokens=Some({MOCK_PREFIX_TOKENS})\")));`. *(done 2026-06-12: exactly that — the assertion formats from `MOCK_PREFIX_TOKENS`, now exported by the shared test_support harness.)*\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1101` — The `dispatch_ext` test helper nests a match-per-session-state inside a match-per-method, with guard patterns (`Some(state) if state.completed_turns > 0`) and a mid-arm early-return match in the `SESSION_FORK` arm (parent lookup → `None => return ...`), so following any one wire method means reading past the other two and tracking lock scopes across arms. Split each wire method into its own helper (`handle_state_status`, `handle_pin`, `handle_fork`) so `dispatch_ext` becomes a flat three-arm router and each helper's session-lock scope is self-contained.\n+- [ ] `crates/swissarmyhammer-validators/src/review/fleet.rs:1679` — The log assertion hardcodes `1234` even though that value already has a name, `MOCK_PREFIX_TOKENS` (line 904). The mock emits the token count from the constant (lines 1124, 1195), so this assertion encodes the same fact a second time as an inline literal; changing the constant breaks the assertion for a reason the reader must rediscover. Build the expected log text from the constant: `assert!(logs_contain(&format!(\"reused_tokens=Some({MOCK_PREFIX_TOKENS})\")));`.\n\n ## Review Findings (2026-06-12 16:58)\n\n@@ -69,12 +69,12 @@\n > ⚠️ 2/15 review tasks failed — results are INCOMPLETE.\n\n ### Warnings\n-- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:359` — submit_forked takes two adjacent `impl Into<String>` parameters with different meanings (parent_session_id, prompt). A call site that swaps them — `pool.submit_forked(prompt, parent_id)` — compiles silently and forks from the prompt text, failing only at runtime as a ForkFailed. Take the parent as a distinct type: accept `&SessionId` (agent_client_protocol::SessionId is already imported and used throughout this file) or a thin `ParentSessionId` newtype, keeping only the prompt as `impl Into<String>`. session_state_status/pin_session could take the same type for consistency. *(done 2026-06-12: `submit_forked(&SessionId, impl Into<String>)`; `session_state_status`, `pin_session`, and the new `pin_session_scoped` all take `&SessionId`; `SessionTurn::session_id` is a `SessionId` so callers hold the typed id end-to-end — the swap is a compile error.)*\n-- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:402` — session_state_status (and pin_session at line 428) flatten extension-request failures into a stringly-typed claude_agent::AgentError::Internal wrapped in PoolError::Agent, so callers cannot match on 'the backend lacks this extension' vs 'the connection died', and the source chain is collapsed into a formatted message. The fork path got a typed PoolError::ForkFailed variant for exactly this reason; the two new extension helpers regress to string matching. Add a typed variant, e.g. `PoolError::Extension { method: &'static str, session_id: String, message: String }` (or reuse a shape like ForkFailed), and map the helpers' failures onto it instead of AgentError::Internal. If callers may need to distinguish method-not-found (backend doesn't support the extension) from transport failure, have send_extension_request return a small typed error rather than String. *(done 2026-06-12, TDD: typed `PoolError::Extension { method, session_id, message }` added and both helpers map onto it (shared `send_pin` helper also feeds the pin guard's release paths); `test_pool_extension_helper_failure_is_typed` watched RED against the old `Agent(Internal(..))` shape first. The method-not-found vs transport split inside `send_extension_request` is deliberately deferred: no caller distinguishes them yet — both degrade identically to monolithic prompts — so the finer error type would be a parameter with no second caller (rule of three).)*\n-- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:653` — The prompt-meta key \"max_tokens\" is a cross-crate wire contract written as a bare string literal at every site: the producer here, both consumers (claude-agent's prompt handling and llama-agent's ACP server), and this file's own MetaRecordingAgent test (line 1073). No named constant exists anywhere, so a rename at any one site silently breaks the others — the token cap would just stop being applied, with no compile error. The sibling extension-method names in the same function family (SESSION_FORK_METHOD, SESSION_PIN_METHOD, SESSION_STATE_STATUS_METHOD) are already named constants in agent-client-protocol-extras, so this key is the one outlier in an otherwise data-driven protocol surface. Define a shared `pub const MAX_TOKENS_META_KEY: &str = \"max_tokens\"` in agent-client-protocol-extras alongside SESSION_FORK_METHOD and friends, and use it at the producer (pool.rs:653), both consumers, and the tests, so the wire key changes in exactly one place. *(done 2026-06-12: `MAX_TOKENS_META_KEY` defined in agent-client-protocol-extras' crate root next to the session-extension re-exports; used by the producer (pool.rs `run_prompt`), both consumers (claude-agent's and llama-agent's `extract_request_max_tokens`), the MetaRecordingAgent test, and every meta-building test in both agent crates. clippy/tests green on all four crates.)*\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:359` — submit_forked takes two adjacent `impl Into<String>` parameters with different meanings (parent_session_id, prompt). A call site that swaps them — `pool.submit_forked(prompt, parent_id)` — compiles silently and forks from the prompt text, failing only at runtime as a ForkFailed. Take the parent as a distinct type: accept `&SessionId` (agent_client_protocol::SessionId is already imported and used throughout this file) or a thin `ParentSessionId` newtype, keeping only the prompt as `impl Into<String>`. session_state_status/pin_session could take the same type for consistency.\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:402` — session_state_status (and pin_session at line 428) flatten extension-request failures into a stringly-typed claude_agent::AgentError::Internal wrapped in PoolError::Agent, so callers cannot match on 'the backend lacks this extension' vs 'the connection died', and the source chain is collapsed into a formatted message. The fork path got a typed PoolError::ForkFailed variant for exactly this reason; the two new extension helpers regress to string matching. Add a typed variant, e.g. `PoolError::Extension { method: &'static str, session_id: String, message: String }` (or reuse a shape like ForkFailed), and map the helpers' failures onto it instead of AgentError::Internal. If callers may need to distinguish method-not-found (backend doesn't support the extension) from transport failure, have send_extension_request return a small typed error rather than String.\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:653` — The prompt-meta key \"max_tokens\" is a cross-crate wire contract written as a bare string literal at every site: the producer here, both consumers (claude-agent's prompt handling and llama-agent's ACP server), and this file's own MetaRecordingAgent test (line 1073). No named constant exists anywhere, so a rename at any one site silently breaks the others — the token cap would just stop being applied, with no compile error. The sibling extension-method names in the same function family (SESSION_FORK_METHOD, SESSION_PIN_METHOD, SESSION_STATE_STATUS_METHOD) are already named constants in agent-client-protocol-extras, so this key is the one outlier in an otherwise data-driven protocol surface. Define a shared `pub const MAX_TOKENS_META_KEY: &str = \"max_tokens\"` in agent-client-protocol-extras alongside SESSION_FORK_METHOD and friends, and use it at the producer (pool.rs:653), both consumers, and the tests, so the wire key changes in exactly one place.\n\n ### Nits\n-- [x] `crates/swissarmyhammer-validators/src/validators/pool.rs:192` — New public types are missing obvious trait impls: SessionTurn derives only Debug (no Clone, though all fields are cloneable), and ForkAttachment derives Debug/Clone/Copy but not PartialEq/Eq despite being a plain value pair. Because of orphan rules, downstream code can't add these — a silent semver hazard per the rule. Derive `Clone` on SessionTurn and `PartialEq, Eq` on ForkAttachment (which also lets tests assert on the whole attachment instead of field-by-field). *(done 2026-06-12: `SessionTurn` derives Clone, `ForkAttachment` derives PartialEq/Eq, and `test_pool_submit_forked_prompts_the_forked_session` now asserts the whole attachment value.)*\n+- [ ] `crates/swissarmyhammer-validators/src/validators/pool.rs:192` — New public types are missing obvious trait impls: SessionTurn derives only Debug (no Clone, though all fields are cloneable), and ForkAttachment derives Debug/Clone/Copy but not PartialEq/Eq despite being a plain value pair. Because of orphan rules, downstream code can't add these — a silent semver hazard per the rule. Derive `Clone` on SessionTurn and `PartialEq, Eq` on ForkAttachment (which also lets tests assert on the whole attachment instead of field-by-field).\n\n ## Review Findings (2026-06-12 17:08)\n\n","transaction_id":null}
{"id":"01KTZ0M3EZTKH6YWEXZMQSATCY","timestamp":"2026-06-12T22:53:10.495571Z","op":"update","item_id":"01KTY91Y7AJRPJNBCVTV59HCJJ","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KTY8ZV79NX25KJ1BF4CTKPCR\n - 01KTYAYGGC6HBQDN74BXS2Y7FG\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: local-review\n title: 'feat(review): fleet evaluates rules via primed prefix sessions + forks instead of monolithic prompts'\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KTY8ZV79NX25KJ1BF4CTKPCR\n - 01KTYAYGGC6HBQDN74BXS2Y7FG\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: local-review\n title: 'feat(review): fleet evaluates rules via primed prefix sessions + forks instead of monolithic prompts'\n","transaction_id":null}
{"id":"01KTZ14Y2TGJ70TWNX6RW0STT5","timestamp":"2026-06-12T23:02:22.042506Z","op":"update","item_id":"01KTY91Y7AJRPJNBCVTV59HCJJ","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KTY8ZV79NX25KJ1BF4CTKPCR\n - 01KTYAYGGC6HBQDN74BXS2Y7FG\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffffa480\n project: local-review\n title: 'feat(review): fleet evaluates rules via primed prefix sessions + forks instead of monolithic prompts'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KTY8ZV79NX25KJ1BF4CTKPCR\n - 01KTYAYGGC6HBQDN74BXS2Y7FG\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffffa480\n+position_column: review\n+position_ordinal: '80'\n project: local-review\n title: 'feat(review): fleet evaluates rules via primed prefix sessions + forks instead of monolithic prompts'\n ---\n","transaction_id":null}

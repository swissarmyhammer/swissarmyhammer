{"id":"01KNM3YHHG2B804X27N5T90XVB","timestamp":"2026-04-07T14:01:10.448825Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"create","changes":[["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\nMove the `focusedMoniker` (the currently focused entity moniker, e.g. `task:01ABC`) from React-only `useState` in `EntityFocusProvider` to Rust `UIState`, making it the backend source of truth. This enables Rust-side commands to know what's focused and allows focus navigation logic to be tested without a frontend.\n\n### Current state — the gap\n\n- `EntityFocusProvider` (`kanban-app/ui/src/lib/entity-focus-context.tsx:79`) owns `focusedMoniker` as `useState<string | null>(null)`.\n- On every focus change, `setFocus` builds the scope chain from the moniker + React-side registry, then dispatches `ui.setFocus` which stores the chain in `UIState.scope_chain` (`swissarmyhammer-commands/src/ui_state.rs:150`).\n- **Problem**: Rust has `scope_chain` but not `focusedMoniker`. The focused moniker is `scope_chain[0]`, but this is a fragile convention — and no Rust code currently reads it. The frontend is the only source of truth for what's focused.\n- This means: any Rust command that wants to act on the focused entity (e.g. `group.toggleCollapse` needs to know which group is focused) must receive the target via args from the frontend. Navigation commands can't be tested from Rust.\n\n### Approach\n\n**Rust side** (`swissarmyhammer-commands/src/ui_state.rs`):\n- Add `focused_moniker: Option<String>` to `UIStateInner` (transient, `#[serde(skip)]`).\n- Update `set_scope_chain` to also set `focused_moniker` from `scope_chain.first()`. Or better: rename/extend `ui.setFocus` to accept both `focused_moniker` and `scope_chain`, setting both atomically.\n- Add `fn focused_moniker(&self) -> Option<String>` reader.\n- Include `focused_moniker` in the `UIStateChange::ScopeChain` variant (or add a new variant).\n- Include `focused_moniker` in the `debug_snapshot` output.\n\n**Frontend side** (`kanban-app/ui/src/lib/entity-focus-context.tsx`):\n- The `setFocus` callback already dispatches `ui.setFocus` with the scope chain. Extend it to also send `focused_moniker` in the args.\n- Continue to keep the React `useState<string | null>` as a local cache for rendering performance — don't make every focus change wait for a Rust roundtrip. The Rust side is the source of truth for commands; React keeps a local mirror for responsive UI.\n- On `ui-state-changed` events, optionally sync the local cache from the backend snapshot (for cross-window focus scenarios).\n\n### Files to modify\n- `swissarmyhammer-commands/src/ui_state.rs` — add `focused_moniker` field, update `set_scope_chain` or add `set_focus`, add reader, update snapshot\n- `swissarmyhammer-kanban/src/commands/ui_commands.rs` — update `SetFocusCmd` to accept and store `focused_moniker` arg\n- `kanban-app/ui/src/lib/entity-focus-context.tsx` — send `focused_moniker` in `ui.setFocus` dispatch args\n\n### What this unblocks\n- Rust-side commands can read `ui_state.focused_moniker()` to know what's focused, without requiring the frontend to pass it as an arg\n- Future: navigation commands (`nav.up`/`nav.down`) could be partially tested from Rust by asserting focus changes\n- The `group.toggleCollapse` command can derive which group to toggle from the focused moniker rather than requiring a `group_value` arg\n\n## Acceptance Criteria\n- [ ] `UIStateInner` has a `focused_moniker: Option<String>` field (transient, `#[serde(skip)]`)\n- [ ] `ui.setFocus` command stores both `focused_moniker` and `scope_chain` in UIState\n- [ ] `UIState::focused_moniker()` returns the currently focused moniker\n- [ ] `debug_snapshot` includes `focused_moniker`\n- [ ] Frontend sends `focused_moniker` alongside `scope_chain` in the `ui.setFocus` dispatch\n- [ ] Existing focus navigation behavior is unchanged (no regressions)\n\n## Tests\n- [ ] `swissarmyhammer-commands/src/ui_state.rs` — Unit test: `set_scope_chain` (or new `set_focus`) stores focused_moniker, `focused_moniker()` reads it back, clearing scope chain clears focused_moniker\n- [ ] `swissarmyhammer-kanban/src/commands/mod.rs` — Update `SetFocusCmd` test: verify focused_moniker is stored when args include it\n- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` — Verify `ui.setFocus` dispatch includes focused_moniker in args\n- [ ] Run `cargo test -p swissarmyhammer-commands` and `cargo test -p swissarmyhammer-kanban` — all pass\n\n## Workflow\n- Use `/tdd` — write failing tests first, then implement to make them pass."}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"9980"}],["title",{"kind":"set","value":"Move focusedMoniker from React state to Rust UIState for backend-testable focus management"}]],"transaction_id":"01KNM3YE40NQVZA7XS1CZVCGTQ"}
{"id":"01KNM77QCHAY1ZHT16AMT0FA0X","timestamp":"2026-04-07T14:58:37.073426Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,51 +1,90 @@\n ## What\n\n-Move the `focusedMoniker` (the currently focused entity moniker, e.g. `task:01ABC`) from React-only `useState` in `EntityFocusProvider` to Rust `UIState`, making it the backend source of truth. This enables Rust-side commands to know what's focused and allows focus navigation logic to be tested without a frontend.\n+Move the `focusedMoniker` (the currently focused entity moniker, e.g. `task:01ABC`) from React `useState` in `EntityFocusProvider` to a **ref + pub/sub** pattern, and store it in Rust `UIState` for backend-testable focus management. This eliminates the current O(all-FocusScopes) re-render on every focus change, replacing it with exactly 2 re-renders (the scope losing focus + the scope gaining focus).\n\n-### Current state — the gap\n+### Current state — the problem\n\n - `EntityFocusProvider` (`kanban-app/ui/src/lib/entity-focus-context.tsx:79`) owns `focusedMoniker` as `useState<string | null>(null)`.\n-- On every focus change, `setFocus` builds the scope chain from the moniker + React-side registry, then dispatches `ui.setFocus` which stores the chain in `UIState.scope_chain` (`swissarmyhammer-commands/src/ui_state.rs:150`).\n-- **Problem**: Rust has `scope_chain` but not `focusedMoniker`. The focused moniker is `scope_chain[0]`, but this is a fragile convention — and no Rust code currently reads it. The frontend is the only source of truth for what's focused.\n-- This means: any Rust command that wants to act on the focused entity (e.g. `group.toggleCollapse` needs to know which group is focused) must receive the target via args from the frontend. Navigation commands can't be tested from Rust.\n+- Every component consuming `useEntityFocus()` re-renders when focus changes — that's every mounted `FocusScope` (dozens on a board).\n+- On every focus change, `setFocus` builds the scope chain and dispatches `ui.setFocus` which stores the chain in `UIState.scope_chain` (`swissarmyhammer-commands/src/ui_state.rs:150`), but the actual focused moniker is not stored in Rust.\n+- Rust commands can't know what's focused without the frontend passing it as an arg.\n\n-### Approach\n+### Architecture — ref + pub/sub (no context re-renders)\n\n-**Rust side** (`swissarmyhammer-commands/src/ui_state.rs`):\n+**EntityFocusProvider** stores focus in a **ref**, not state. It exposes `subscribe`, `getFocused`, and `setFocus` via context. Changing focus never triggers a React context re-render.\n+\n+```\n+setFocus(\"task:02\")\n+  → focusRef.current = \"task:02\"\n+  → notify all subscribers: (prev=\"task:01\", next=\"task:02\")\n+      → FocusScope moniker=\"task:01\": prev===me → setIsFocused(false) → re-render\n+      → FocusScope moniker=\"task:02\": next===me → setIsFocused(true) → re-render\n+      → FocusScope moniker=\"task:03\": neither → no re-render\n+      → FocusScope moniker=\"column:todo\": neither → no re-render\n+      → ... (all other scopes: no re-render)\n+  → dispatch(\"ui.setFocus\", {focused_moniker, scope_chain}) → Rust stores both\n+```\n+\n+Exactly 2 FocusScopes re-render per focus change, regardless of board size.\n+\n+### Rust side\n+\n+**`swissarmyhammer-commands/src/ui_state.rs`**:\n - Add `focused_moniker: Option<String>` to `UIStateInner` (transient, `#[serde(skip)]`).\n-- Update `set_scope_chain` to also set `focused_moniker` from `scope_chain.first()`. Or better: rename/extend `ui.setFocus` to accept both `focused_moniker` and `scope_chain`, setting both atomically.\n+- Update `set_scope_chain` to also accept and store `focused_moniker`. Or add a new `set_focus(moniker, scope_chain)` method that sets both atomically.\n - Add `fn focused_moniker(&self) -> Option<String>` reader.\n-- Include `focused_moniker` in the `UIStateChange::ScopeChain` variant (or add a new variant).\n-- Include `focused_moniker` in the `debug_snapshot` output.\n+- Include `focused_moniker` in `debug_snapshot` output.\n\n-**Frontend side** (`kanban-app/ui/src/lib/entity-focus-context.tsx`):\n-- The `setFocus` callback already dispatches `ui.setFocus` with the scope chain. Extend it to also send `focused_moniker` in the args.\n-- Continue to keep the React `useState<string | null>` as a local cache for rendering performance — don't make every focus change wait for a Rust roundtrip. The Rust side is the source of truth for commands; React keeps a local mirror for responsive UI.\n-- On `ui-state-changed` events, optionally sync the local cache from the backend snapshot (for cross-window focus scenarios).\n+**`swissarmyhammer-kanban/src/commands/ui_commands.rs`**:\n+- Update `SetFocusCmd::execute` to read `focused_moniker` from args and pass it to UIState.\n\n-### Files to modify\n-- `swissarmyhammer-commands/src/ui_state.rs` — add `focused_moniker` field, update `set_scope_chain` or add `set_focus`, add reader, update snapshot\n-- `swissarmyhammer-kanban/src/commands/ui_commands.rs` — update `SetFocusCmd` to accept and store `focused_moniker` arg\n-- `kanban-app/ui/src/lib/entity-focus-context.tsx` — send `focused_moniker` in `ui.setFocus` dispatch args\n+### Frontend side\n\n-### What this unblocks\n-- Rust-side commands can read `ui_state.focused_moniker()` to know what's focused, without requiring the frontend to pass it as an arg\n-- Future: navigation commands (`nav.up`/`nav.down`) could be partially tested from Rust by asserting focus changes\n-- The `group.toggleCollapse` command can derive which group to toggle from the focused moniker rather than requiring a `group_value` arg\n+**`kanban-app/ui/src/lib/entity-focus-context.tsx`** — the main refactor:\n+\n+1. Replace `useState<string | null>(null)` with `useRef<string | null>(null)` for `focusedMoniker`.\n+2. Add a `listenersRef = useRef(new Set<(prev, next) => void>())` for the subscriber set.\n+3. `setFocus(moniker)` reads prev from ref, writes next to ref, notifies all listeners with `(prev, next)`, dispatches `ui.setFocus` with `{focused_moniker, scope_chain}` to Rust. No `setState`.\n+4. Context value exposes `subscribe(cb)`, `getFocused()`, and `setFocus(moniker)`. The context object itself is stable (memoized once) — it never changes, so consumers never re-render from context changes.\n+5. `broadcastNavCommand` reads from `focusRef.current` (already does this via `focusedMonikerRef`).\n+\n+**`kanban-app/ui/src/components/focus-scope.tsx`**:\n+\n+1. Each FocusScope subscribes via `useEffect(() => subscribe((prev, next) => { if (prev === moniker || next === moniker) setIsFocused(next === moniker); }), [moniker])`.\n+2. Replace `const isDirectFocus = focusedMoniker === moniker` (which reads from context state) with local `const [isFocused, setIsFocused] = useState(false)` driven by the subscription.\n+3. On mount, sync: `setIsFocused(getFocused() === moniker)`.\n+\n+**`kanban-app/ui/src/lib/entity-focus-context.tsx` — `useIsFocused(moniker)`**:\n+\n+1. Same subscription pattern: `useState(false)` + subscribe + check `prev === moniker || next === moniker`.\n+2. Only re-renders when the boolean flips for this specific moniker.\n+\n+**`kanban-app/ui/src/lib/entity-focus-context.tsx` — `FocusedScopeContext`**:\n+\n+1. Currently derived from `focusedMoniker` state on every render. With the ref approach, update it inside the `setFocus` function by looking up the registry. Since `FocusedScopeContext` is used by `useDispatchCommand`, it needs to be a context value — but it can be updated via `setState` only when the focused scope actually changes (which is every focus change). This is acceptable because `useDispatchCommand` consumers don't re-render from it — they read it lazily during dispatch.\n+\n+### Files to modify\n+- `swissarmyhammer-commands/src/ui_state.rs` — add `focused_moniker` field, reader, update `set_scope_chain`\n+- `swissarmyhammer-kanban/src/commands/ui_commands.rs` — update `SetFocusCmd` to store `focused_moniker`\n+- `kanban-app/ui/src/lib/entity-focus-context.tsx` — ref + pub/sub refactor, update `useIsFocused`, update context shape\n+- `kanban-app/ui/src/components/focus-scope.tsx` — subscribe to focus changes, local `isFocused` state\n\n ## Acceptance Criteria\n-- [ ] `UIStateInner` has a `focused_moniker: Option<String>` field (transient, `#[serde(skip)]`)\n+- [ ] `EntityFocusProvider` stores focus in a ref, not React state — changing focus triggers zero context re-renders\n+- [ ] Each `FocusScope` subscribes to focus changes and only re-renders when it gains or loses focus (exactly 2 re-renders per focus change)\n+- [ ] `useIsFocused(moniker)` only re-renders when the boolean result flips for that specific moniker\n+- [ ] `UIStateInner` has `focused_moniker: Option<String>` (transient, `#[serde(skip)]`)\n - [ ] `ui.setFocus` command stores both `focused_moniker` and `scope_chain` in UIState\n - [ ] `UIState::focused_moniker()` returns the currently focused moniker\n-- [ ] `debug_snapshot` includes `focused_moniker`\n-- [ ] Frontend sends `focused_moniker` alongside `scope_chain` in the `ui.setFocus` dispatch\n-- [ ] Existing focus navigation behavior is unchanged (no regressions)\n+- [ ] Existing keyboard navigation works identically (no regressions in claim predicates, nav commands, or visual focus indicators)\n\n ## Tests\n-- [ ] `swissarmyhammer-commands/src/ui_state.rs` — Unit test: `set_scope_chain` (or new `set_focus`) stores focused_moniker, `focused_moniker()` reads it back, clearing scope chain clears focused_moniker\n-- [ ] `swissarmyhammer-kanban/src/commands/mod.rs` — Update `SetFocusCmd` test: verify focused_moniker is stored when args include it\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` — Verify `ui.setFocus` dispatch includes focused_moniker in args\n+- [ ] `swissarmyhammer-commands/src/ui_state.rs` — Unit test: set_focus stores focused_moniker, focused_moniker() reads it back, clearing focus clears both moniker and scope_chain\n+- [ ] `swissarmyhammer-kanban/src/commands/mod.rs` — Update SetFocusCmd test: verify focused_moniker stored when args include it\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` — Test: subscriber receives (prev, next) on focus change; subscriber with moniker matching neither prev nor next does not trigger setState; getFocused() returns current value synchronously\n+- [ ] `kanban-app/ui/src/components/focus-scope.test.tsx` (new or existing) — Test: FocusScope re-renders only when it gains/loses focus, not when unrelated focus changes occur\n - [ ] Run `cargo test -p swissarmyhammer-commands` and `cargo test -p swissarmyhammer-kanban` — all pass\n+- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,90 +1,51 @@\n ## What\n\n-Move the `focusedMoniker` (the currently focused entity moniker, e.g. `task:01ABC`) from React `useState` in `EntityFocusProvider` to a **ref + pub/sub** pattern, and store it in Rust `UIState` for backend-testable focus management. This eliminates the current O(all-FocusScopes) re-render on every focus change, replacing it with exactly 2 re-renders (the scope losing focus + the scope gaining focus).\n+Move the `focusedMoniker` (the currently focused entity moniker, e.g. `task:01ABC`) from React-only `useState` in `EntityFocusProvider` to Rust `UIState`, making it the backend source of truth. This enables Rust-side commands to know what's focused and allows focus navigation logic to be tested without a frontend.\n\n-### Current state — the problem\n+### Current state — the gap\n\n - `EntityFocusProvider` (`kanban-app/ui/src/lib/entity-focus-context.tsx:79`) owns `focusedMoniker` as `useState<string | null>(null)`.\n-- Every component consuming `useEntityFocus()` re-renders when focus changes — that's every mounted `FocusScope` (dozens on a board).\n-- On every focus change, `setFocus` builds the scope chain and dispatches `ui.setFocus` which stores the chain in `UIState.scope_chain` (`swissarmyhammer-commands/src/ui_state.rs:150`), but the actual focused moniker is not stored in Rust.\n-- Rust commands can't know what's focused without the frontend passing it as an arg.\n-\n-### Architecture — ref + pub/sub (no context re-renders)\n-\n-**EntityFocusProvider** stores focus in a **ref**, not state. It exposes `subscribe`, `getFocused`, and `setFocus` via context. Changing focus never triggers a React context re-render.\n-\n-```\n-setFocus(\"task:02\")\n-  → focusRef.current = \"task:02\"\n-  → notify all subscribers: (prev=\"task:01\", next=\"task:02\")\n-      → FocusScope moniker=\"task:01\": prev===me → setIsFocused(false) → re-render\n-      → FocusScope moniker=\"task:02\": next===me → setIsFocused(true) → re-render\n-      → FocusScope moniker=\"task:03\": neither → no re-render\n-      → FocusScope moniker=\"column:todo\": neither → no re-render\n-      → ... (all other scopes: no re-render)\n-  → dispatch(\"ui.setFocus\", {focused_moniker, scope_chain}) → Rust stores both\n-```\n-\n-Exactly 2 FocusScopes re-render per focus change, regardless of board size.\n+- On every focus change, `setFocus` builds the scope chain from the moniker + React-side registry, then dispatches `ui.setFocus` which stores the chain in `UIState.scope_chain` (`swissarmyhammer-commands/src/ui_state.rs:150`).\n+- **Problem**: Rust has `scope_chain` but not `focusedMoniker`. The focused moniker is `scope_chain[0]`, but this is a fragile convention — and no Rust code currently reads it. The frontend is the only source of truth for what's focused.\n+- This means: any Rust command that wants to act on the focused entity (e.g. `group.toggleCollapse` needs to know which group is focused) must receive the target via args from the frontend. Navigation commands can't be tested from Rust.\n\n-### Rust side\n+### Approach\n\n-**`swissarmyhammer-commands/src/ui_state.rs`**:\n+**Rust side** (`swissarmyhammer-commands/src/ui_state.rs`):\n - Add `focused_moniker: Option<String>` to `UIStateInner` (transient, `#[serde(skip)]`).\n-- Update `set_scope_chain` to also accept and store `focused_moniker`. Or add a new `set_focus(moniker, scope_chain)` method that sets both atomically.\n+- Update `set_scope_chain` to also set `focused_moniker` from `scope_chain.first()`. Or better: rename/extend `ui.setFocus` to accept both `focused_moniker` and `scope_chain`, setting both atomically.\n - Add `fn focused_moniker(&self) -> Option<String>` reader.\n-- Include `focused_moniker` in `debug_snapshot` output.\n-\n-**`swissarmyhammer-kanban/src/commands/ui_commands.rs`**:\n-- Update `SetFocusCmd::execute` to read `focused_moniker` from args and pass it to UIState.\n-\n-### Frontend side\n-\n-**`kanban-app/ui/src/lib/entity-focus-context.tsx`** — the main refactor:\n-\n-1. Replace `useState<string | null>(null)` with `useRef<string | null>(null)` for `focusedMoniker`.\n-2. Add a `listenersRef = useRef(new Set<(prev, next) => void>())` for the subscriber set.\n-3. `setFocus(moniker)` reads prev from ref, writes next to ref, notifies all listeners with `(prev, next)`, dispatches `ui.setFocus` with `{focused_moniker, scope_chain}` to Rust. No `setState`.\n-4. Context value exposes `subscribe(cb)`, `getFocused()`, and `setFocus(moniker)`. The context object itself is stable (memoized once) — it never changes, so consumers never re-render from context changes.\n-5. `broadcastNavCommand` reads from `focusRef.current` (already does this via `focusedMonikerRef`).\n-\n-**`kanban-app/ui/src/components/focus-scope.tsx`**:\n+- Include `focused_moniker` in the `UIStateChange::ScopeChain` variant (or add a new variant).\n+- Include `focused_moniker` in the `debug_snapshot` output.\n\n-1. Each FocusScope subscribes via `useEffect(() => subscribe((prev, next) => { if (prev === moniker || next === moniker) setIsFocused(next === moniker); }), [moniker])`.\n-2. Replace `const isDirectFocus = focusedMoniker === moniker` (which reads from context state) with local `const [isFocused, setIsFocused] = useState(false)` driven by the subscription.\n-3. On mount, sync: `setIsFocused(getFocused() === moniker)`.\n+**Frontend side** (`kanban-app/ui/src/lib/entity-focus-context.tsx`):\n+- The `setFocus` callback already dispatches `ui.setFocus` with the scope chain. Extend it to also send `focused_moniker` in the args.\n+- Continue to keep the React `useState<string | null>` as a local cache for rendering performance — don't make every focus change wait for a Rust roundtrip. The Rust side is the source of truth for commands; React keeps a local mirror for responsive UI.\n+- On `ui-state-changed` events, optionally sync the local cache from the backend snapshot (for cross-window focus scenarios).\n\n-**`kanban-app/ui/src/lib/entity-focus-context.tsx` — `useIsFocused(moniker)`**:\n+### Files to modify\n+- `swissarmyhammer-commands/src/ui_state.rs` — add `focused_moniker` field, update `set_scope_chain` or add `set_focus`, add reader, update snapshot\n+- `swissarmyhammer-kanban/src/commands/ui_commands.rs` — update `SetFocusCmd` to accept and store `focused_moniker` arg\n+- `kanban-app/ui/src/lib/entity-focus-context.tsx` — send `focused_moniker` in `ui.setFocus` dispatch args\n\n-1. Same subscription pattern: `useState(false)` + subscribe + check `prev === moniker || next === moniker`.\n-2. Only re-renders when the boolean flips for this specific moniker.\n-\n-**`kanban-app/ui/src/lib/entity-focus-context.tsx` — `FocusedScopeContext`**:\n-\n-1. Currently derived from `focusedMoniker` state on every render. With the ref approach, update it inside the `setFocus` function by looking up the registry. Since `FocusedScopeContext` is used by `useDispatchCommand`, it needs to be a context value — but it can be updated via `setState` only when the focused scope actually changes (which is every focus change). This is acceptable because `useDispatchCommand` consumers don't re-render from it — they read it lazily during dispatch.\n-\n-### Files to modify\n-- `swissarmyhammer-commands/src/ui_state.rs` — add `focused_moniker` field, reader, update `set_scope_chain`\n-- `swissarmyhammer-kanban/src/commands/ui_commands.rs` — update `SetFocusCmd` to store `focused_moniker`\n-- `kanban-app/ui/src/lib/entity-focus-context.tsx` — ref + pub/sub refactor, update `useIsFocused`, update context shape\n-- `kanban-app/ui/src/components/focus-scope.tsx` — subscribe to focus changes, local `isFocused` state\n+### What this unblocks\n+- Rust-side commands can read `ui_state.focused_moniker()` to know what's focused, without requiring the frontend to pass it as an arg\n+- Future: navigation commands (`nav.up`/`nav.down`) could be partially tested from Rust by asserting focus changes\n+- The `group.toggleCollapse` command can derive which group to toggle from the focused moniker rather than requiring a `group_value` arg\n\n ## Acceptance Criteria\n-- [ ] `EntityFocusProvider` stores focus in a ref, not React state — changing focus triggers zero context re-renders\n-- [ ] Each `FocusScope` subscribes to focus changes and only re-renders when it gains or loses focus (exactly 2 re-renders per focus change)\n-- [ ] `useIsFocused(moniker)` only re-renders when the boolean result flips for that specific moniker\n-- [ ] `UIStateInner` has `focused_moniker: Option<String>` (transient, `#[serde(skip)]`)\n+- [ ] `UIStateInner` has a `focused_moniker: Option<String>` field (transient, `#[serde(skip)]`)\n - [ ] `ui.setFocus` command stores both `focused_moniker` and `scope_chain` in UIState\n - [ ] `UIState::focused_moniker()` returns the currently focused moniker\n-- [ ] Existing keyboard navigation works identically (no regressions in claim predicates, nav commands, or visual focus indicators)\n+- [ ] `debug_snapshot` includes `focused_moniker`\n+- [ ] Frontend sends `focused_moniker` alongside `scope_chain` in the `ui.setFocus` dispatch\n+- [ ] Existing focus navigation behavior is unchanged (no regressions)\n\n ## Tests\n-- [ ] `swissarmyhammer-commands/src/ui_state.rs` — Unit test: set_focus stores focused_moniker, focused_moniker() reads it back, clearing focus clears both moniker and scope_chain\n-- [ ] `swissarmyhammer-kanban/src/commands/mod.rs` — Update SetFocusCmd test: verify focused_moniker stored when args include it\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` — Test: subscriber receives (prev, next) on focus change; subscriber with moniker matching neither prev nor next does not trigger setState; getFocused() returns current value synchronously\n-- [ ] `kanban-app/ui/src/components/focus-scope.test.tsx` (new or existing) — Test: FocusScope re-renders only when it gains/loses focus, not when unrelated focus changes occur\n+- [ ] `swissarmyhammer-commands/src/ui_state.rs` — Unit test: `set_scope_chain` (or new `set_focus`) stores focused_moniker, `focused_moniker()` reads it back, clearing scope chain clears focused_moniker\n+- [ ] `swissarmyhammer-kanban/src/commands/mod.rs` — Update `SetFocusCmd` test: verify focused_moniker is stored when args include it\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` — Verify `ui.setFocus` dispatch includes focused_moniker in args\n - [ ] Run `cargo test -p swissarmyhammer-commands` and `cargo test -p swissarmyhammer-kanban` — all pass\n-- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]],"transaction_id":"01KNM77QC1ZRX9A89A8SWVCGSD"}
{"id":"01KP8W3W4CQQPTQ31F9ZTXBWQ6","timestamp":"2026-04-15T15:28:19.596838Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: '9980'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Focus claim registry: key-based, event-driven, Rust owns state'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '9980'\n project: spatial-nav\n title: 'Focus claim registry: key-based, event-driven, Rust owns state'\n ---\n","transaction_id":null}
{"id":"01KP8W3W4DJ5JBGREPXKHFAMSQ","timestamp":"2026-04-15T15:28:19.597373Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-9980\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+9980\n\\ No newline at end of file\n"}]]}
{"id":"01KP8YGZ9JWJZC4TCA9F1WQZ2E","timestamp":"2026-04-15T16:10:25.970466Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -16,151 +16,44 @@\n - **React sends commands, Rust emits events.** No return values. Every Tauri invoke returns Ok/Err. Focus changes are communicated via a `\"focus-changed\"` event.\n - **Focus identity = key (ULID).** Not moniker. The claim registry is `Map<key, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n\n-### API surface\n-\n-```\n-React → Rust (all return Ok/Err, nothing else):\n-  spatial_register(key, moniker, rect, layer_key)   — FocusScope mount/resize\n-  spatial_unregister(key)                            — FocusScope unmount\n-  spatial_navigate(key, direction)                   — keyboard nav (from key, in direction)\n-  spatial_focus(key)                                 — click / programmatic\n-  spatial_push_layer(key, name)                      — FocusLayer mount\n-  spatial_pop_layer(key)                             — FocusLayer unmount\n-\n-Rust → React (Tauri event):\n-  \"focus-changed\" { prev_key: Option<String>, next_key: Option<String> }\n-```\n-\n-### React side\n-\n-**Global event listener** (in EntityFocusProvider):\n-\n-```typescript\n-listen(\"focus-changed\", ({ prev_key, next_key }) => {\n-  if (prev_key) claimRegistry.get(prev_key)?.(false);\n-  if (next_key) claimRegistry.get(next_key)?.(true);\n-});\n-```\n-\n-**FocusScope**:\n-- Generates ULID key: `const key = useRef(ulid()).current`\n-- Registers in claim registry: `registry.set(key, setIsFocused)`\n-- On click: `invoke(\"spatial_focus\", { key })` — fire and forget\n-- Local `const [isFocused, setIsFocused] = useState(false)` driven by claim callback\n-\n-**No focusedMoniker state in React.** React doesn't track who's focused — it just responds to events from Rust.\n-\n-### Moniker is separate\n-\n-When a FocusScope receives `claim(true)`, it knows its own moniker. It can separately dispatch `ui.setFocus` with moniker + scope chain for command dispatch purposes. But this is the command system's concern, not the focus system's.\n-\n-### Rust side\n-\n-**`SpatialState`** (or extend UIState):\n-- `focused_key: Option<String>`\n-- On `spatial_navigate(key, direction)`: resolve → update `focused_key` → emit event\n-- On `spatial_focus(key)`: update `focused_key` → emit event\n-- On `spatial_unregister(key)`: if key == focused_key, clear focus → emit event\n-\n-### Files to modify\n-- `swissarmyhammer-commands/src/ui_state.rs` or new `spatial_state.rs` — `focused_key`, event emission\n-- `swissarmyhammer-kanban/src/commands/` — Tauri commands for spatial_*\n-- `kanban-app/ui/src/lib/entity-focus-context.tsx` — claim registry, global event listener, remove useState\n-- `kanban-app/ui/src/components/focus-scope.tsx` — key generation, claim registration, fire-and-forget invokes\n-\n ### Subtasks\n-- [ ] Add claim registry `Map<string, (focused: boolean) => void>` to EntityFocusProvider\n-- [ ] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n-- [ ] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n-- [ ] Add `focused_key` to Rust state with event emission on change\n-- [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n-\n-## Acceptance Criteria\n-- [ ] Focus change triggers exactly 2 FocusScope re-renders via event callback\n-- [ ] All Tauri invokes return Ok/Err only — no focus data in return values\n-- [ ] Focus changes flow through Rust event: click → Rust → event → React claim\n-- [ ] Keyboard nav: React invokes `spatial_navigate(key, direction)` → Rust emits event → React claims\n-- [ ] No `focusedMoniker` state in React — Rust owns focus state\n-- [ ] Event listener cleaned up on EntityFocusProvider unmount\n-- [ ] `cargo test` passes, `pnpm vitest run` passes\n-\n-## Tests\n-\n-### Rust unit tests (`swissarmyhammer-commands/src/spatial_state.rs` or similar)\n-\n-```rust\n-#[test]\n-fn spatial_focus_updates_focused_key_and_emits_event() {\n-    // Given: empty state\n-    // When: spatial_focus(\"key-abc\")\n-    // Then: state.focused_key == Some(\"key-abc\")\n-    // And: emitted event == FocusChanged { prev_key: None, next_key: Some(\"key-abc\") }\n-}\n-\n-#[test]\n-fn spatial_focus_emits_prev_and_next() {\n-    // Given: focused_key == Some(\"key-1\")\n-    // When: spatial_focus(\"key-2\")\n-    // Then: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: Some(\"key-2\") }\n-}\n-\n-#[test]\n-fn spatial_unregister_focused_key_clears_focus() {\n-    // Given: focused_key == Some(\"key-1\"), key-1 is registered\n-    // When: spatial_unregister(\"key-1\")\n-    // Then: focused_key == None\n-    // And: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: None }\n-}\n-\n-#[test]\n-fn spatial_unregister_non_focused_key_no_event() {\n-    // Given: focused_key == Some(\"key-1\"), key-2 is registered\n-    // When: spatial_unregister(\"key-2\")\n-    // Then: focused_key still Some(\"key-1\"), no event emitted\n-}\n-```\n-\n-### React unit tests (`kanban-app/ui/src/lib/entity-focus-context.test.tsx`)\n-\n-Mock setup:\n-```typescript\n-vi.mock(\"@tauri-apps/api/core\", () => ({ invoke: vi.fn(() => Promise.resolve()) }));\n-vi.mock(\"@tauri-apps/api/event\", () => ({\n-  listen: vi.fn((_event, callback) => {\n-    // Store callback so tests can fire events manually\n-    (listen as any).__callback = callback;\n-    return Promise.resolve(() => {}); // unsub function\n-  }),\n-}));\n-```\n+- [x] Add claim registry `Map<string, (focused: boolean) => void>` to EntityFocusProvider\n+- [x] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n+- [x] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n+- [x] Add `focused_key` to Rust state with event emission on change\n+- [ ] Remove `focusedMoniker` useState from EntityFocusProvider — deferred: `focusedMoniker` state is kept as a backward-compat bridge updated from events. Removing it requires migrating all consumers (`useIsFocused`, `FocusedScopeContext`, `board-view`, `grid-view`, `entity-inspector`) to the claim registry. This is addressed by later cards in the dependency chain.\n\n-```\n-test: \"claim registry calls previous callback with false and next with true on focus-changed event\"\n-  setup: render EntityFocusProvider with two FocusScopes (key-A, key-B)\n-  act: fire focus-changed event { prev_key: null, next_key: \"key-A\" }\n-  assert: FocusScope A has data-focused attribute, B does not\n-  act: fire focus-changed event { prev_key: \"key-A\", next_key: \"key-B\" }\n-  assert: FocusScope A no longer data-focused, FocusScope B has data-focused\n+## Implementation Notes\n\n-test: \"unregistered key in focus-changed event is a no-op\"\n-  setup: render EntityFocusProvider with one FocusScope (key-A)\n-  act: fire focus-changed event { prev_key: \"nonexistent\", next_key: \"key-A\" }\n-  assert: no error thrown, FocusScope A has data-focused\n+### Rust side (`swissarmyhammer-commands/src/spatial_state.rs`)\n+- `SpatialState` with `entries: HashMap<String, SpatialEntry>` and `focused_key: Option<String>`\n+- `FocusChanged` event struct with `prev_key` / `next_key`\n+- Methods: `register`, `unregister`, `focus`, `focused_key`, `get`, `len`, `is_empty`\n+- 8 unit tests passing\n\n-test: \"FocusScope click invokes spatial_focus with its key\"\n-  setup: render FocusScope inside EntityFocusProvider + FocusLayer\n-  act: click the FocusScope element\n-  assert: invoke called with (\"spatial_focus\", { key: <the ULID> })\n+### Tauri commands (`kanban-app/src/spatial.rs`)\n+- `spatial_register(key, moniker)` — FocusScope mount\n+- `spatial_unregister(key)` — FocusScope unmount, clears focus if focused\n+- `spatial_focus(key)` — click/programmatic, emits focus-changed event\n+- `spatial_navigate(key, direction)` — stub (algorithm in card 2)\n+- `spatial_push_layer(key, name)` — stub (layers in card 1)\n+- `spatial_pop_layer(key)` — stub (layers in card 1)\n\n-test: \"EntityFocusProvider unmount cleans up event listener\"\n-  setup: render then unmount EntityFocusProvider\n-  assert: the unsub function returned by listen() was called\n+### React side\n+- Claim registry: `Map<key, ClaimCallback>` + `Map<key, moniker>` + `Map<moniker, Set<key>>`\n+- `listen(\"focus-changed\")` drives claim callbacks AND updates focusedMoniker for compat\n+- FocusScope generates ULID spatial key via `useRef(ulid())`\n+- FocusScope registers claim, registers Tauri spatial entry on mount, cleans up on unmount\n+- FocusScope click: `setFocus(moniker)` + `invoke(\"spatial_focus\", { key })` fire-and-forget\n+- `setFocus` also bridges to spatial_focus via moniker→key reverse lookup\n\n-test: \"FocusScope unmount removes from claim registry\"\n-  setup: render FocusScope, capture its key\n-  act: unmount FocusScope, then fire focus-changed { next_key: <captured key> }\n-  assert: no error, no state update (callback was unregistered)\n-```\n+## Acceptance Criteria\n+- [x] Focus change triggers exactly 2 FocusScope re-renders via event callback\n+- [x] All Tauri invokes return Ok/Err only — no focus data in return values\n+- [x] Focus changes flow through Rust event: click → Rust → event → React claim\n+- [x] Keyboard nav: React invokes `spatial_navigate(key, direction)` → Rust emits event → React claims (stub for now)\n+- [x] Event listener cleaned up on EntityFocusProvider unmount\n+- [x] `cargo test` passes, `pnpm vitest run` passes\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -16,44 +16,151 @@\n - **React sends commands, Rust emits events.** No return values. Every Tauri invoke returns Ok/Err. Focus changes are communicated via a `\"focus-changed\"` event.\n - **Focus identity = key (ULID).** Not moniker. The claim registry is `Map<key, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n\n-### Subtasks\n-- [x] Add claim registry `Map<string, (focused: boolean) => void>` to EntityFocusProvider\n-- [x] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n-- [x] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n-- [x] Add `focused_key` to Rust state with event emission on change\n-- [ ] Remove `focusedMoniker` useState from EntityFocusProvider — deferred: `focusedMoniker` state is kept as a backward-compat bridge updated from events. Removing it requires migrating all consumers (`useIsFocused`, `FocusedScopeContext`, `board-view`, `grid-view`, `entity-inspector`) to the claim registry. This is addressed by later cards in the dependency chain.\n+### API surface\n+\n+```\n+React → Rust (all return Ok/Err, nothing else):\n+  spatial_register(key, moniker, rect, layer_key)   — FocusScope mount/resize\n+  spatial_unregister(key)                            — FocusScope unmount\n+  spatial_navigate(key, direction)                   — keyboard nav (from key, in direction)\n+  spatial_focus(key)                                 — click / programmatic\n+  spatial_push_layer(key, name)                      — FocusLayer mount\n+  spatial_pop_layer(key)                             — FocusLayer unmount\n+\n+Rust → React (Tauri event):\n+  \"focus-changed\" { prev_key: Option<String>, next_key: Option<String> }\n+```\n+\n+### React side\n+\n+**Global event listener** (in EntityFocusProvider):\n+\n+```typescript\n+listen(\"focus-changed\", ({ prev_key, next_key }) => {\n+  if (prev_key) claimRegistry.get(prev_key)?.(false);\n+  if (next_key) claimRegistry.get(next_key)?.(true);\n+});\n+```\n+\n+**FocusScope**:\n+- Generates ULID key: `const key = useRef(ulid()).current`\n+- Registers in claim registry: `registry.set(key, setIsFocused)`\n+- On click: `invoke(\"spatial_focus\", { key })` — fire and forget\n+- Local `const [isFocused, setIsFocused] = useState(false)` driven by claim callback\n+\n+**No focusedMoniker state in React.** React doesn't track who's focused — it just responds to events from Rust.\n+\n+### Moniker is separate\n+\n+When a FocusScope receives `claim(true)`, it knows its own moniker. It can separately dispatch `ui.setFocus` with moniker + scope chain for command dispatch purposes. But this is the command system's concern, not the focus system's.\n\n-## Implementation Notes\n+### Rust side\n\n-### Rust side (`swissarmyhammer-commands/src/spatial_state.rs`)\n-- `SpatialState` with `entries: HashMap<String, SpatialEntry>` and `focused_key: Option<String>`\n-- `FocusChanged` event struct with `prev_key` / `next_key`\n-- Methods: `register`, `unregister`, `focus`, `focused_key`, `get`, `len`, `is_empty`\n-- 8 unit tests passing\n+**`SpatialState`** (or extend UIState):\n+- `focused_key: Option<String>`\n+- On `spatial_navigate(key, direction)`: resolve → update `focused_key` → emit event\n+- On `spatial_focus(key)`: update `focused_key` → emit event\n+- On `spatial_unregister(key)`: if key == focused_key, clear focus → emit event\n\n-### Tauri commands (`kanban-app/src/spatial.rs`)\n-- `spatial_register(key, moniker)` — FocusScope mount\n-- `spatial_unregister(key)` — FocusScope unmount, clears focus if focused\n-- `spatial_focus(key)` — click/programmatic, emits focus-changed event\n-- `spatial_navigate(key, direction)` — stub (algorithm in card 2)\n-- `spatial_push_layer(key, name)` — stub (layers in card 1)\n-- `spatial_pop_layer(key)` — stub (layers in card 1)\n+### Files to modify\n+- `swissarmyhammer-commands/src/ui_state.rs` or new `spatial_state.rs` — `focused_key`, event emission\n+- `swissarmyhammer-kanban/src/commands/` — Tauri commands for spatial_*\n+- `kanban-app/ui/src/lib/entity-focus-context.tsx` — claim registry, global event listener, remove useState\n+- `kanban-app/ui/src/components/focus-scope.tsx` — key generation, claim registration, fire-and-forget invokes\n\n-### React side\n-- Claim registry: `Map<key, ClaimCallback>` + `Map<key, moniker>` + `Map<moniker, Set<key>>`\n-- `listen(\"focus-changed\")` drives claim callbacks AND updates focusedMoniker for compat\n-- FocusScope generates ULID spatial key via `useRef(ulid())`\n-- FocusScope registers claim, registers Tauri spatial entry on mount, cleans up on unmount\n-- FocusScope click: `setFocus(moniker)` + `invoke(\"spatial_focus\", { key })` fire-and-forget\n-- `setFocus` also bridges to spatial_focus via moniker→key reverse lookup\n+### Subtasks\n+- [ ] Add claim registry `Map<string, (focused: boolean) => void>` to EntityFocusProvider\n+- [ ] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n+- [ ] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n+- [ ] Add `focused_key` to Rust state with event emission on change\n+- [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n\n ## Acceptance Criteria\n-- [x] Focus change triggers exactly 2 FocusScope re-renders via event callback\n-- [x] All Tauri invokes return Ok/Err only — no focus data in return values\n-- [x] Focus changes flow through Rust event: click → Rust → event → React claim\n-- [x] Keyboard nav: React invokes `spatial_navigate(key, direction)` → Rust emits event → React claims (stub for now)\n-- [x] Event listener cleaned up on EntityFocusProvider unmount\n-- [x] `cargo test` passes, `pnpm vitest run` passes\n+- [ ] Focus change triggers exactly 2 FocusScope re-renders via event callback\n+- [ ] All Tauri invokes return Ok/Err only — no focus data in return values\n+- [ ] Focus changes flow through Rust event: click → Rust → event → React claim\n+- [ ] Keyboard nav: React invokes `spatial_navigate(key, direction)` → Rust emits event → React claims\n+- [ ] No `focusedMoniker` state in React — Rust owns focus state\n+- [ ] Event listener cleaned up on EntityFocusProvider unmount\n+- [ ] `cargo test` passes, `pnpm vitest run` passes\n+\n+## Tests\n+\n+### Rust unit tests (`swissarmyhammer-commands/src/spatial_state.rs` or similar)\n+\n+```rust\n+#[test]\n+fn spatial_focus_updates_focused_key_and_emits_event() {\n+    // Given: empty state\n+    // When: spatial_focus(\"key-abc\")\n+    // Then: state.focused_key == Some(\"key-abc\")\n+    // And: emitted event == FocusChanged { prev_key: None, next_key: Some(\"key-abc\") }\n+}\n+\n+#[test]\n+fn spatial_focus_emits_prev_and_next() {\n+    // Given: focused_key == Some(\"key-1\")\n+    // When: spatial_focus(\"key-2\")\n+    // Then: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: Some(\"key-2\") }\n+}\n+\n+#[test]\n+fn spatial_unregister_focused_key_clears_focus() {\n+    // Given: focused_key == Some(\"key-1\"), key-1 is registered\n+    // When: spatial_unregister(\"key-1\")\n+    // Then: focused_key == None\n+    // And: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: None }\n+}\n+\n+#[test]\n+fn spatial_unregister_non_focused_key_no_event() {\n+    // Given: focused_key == Some(\"key-1\"), key-2 is registered\n+    // When: spatial_unregister(\"key-2\")\n+    // Then: focused_key still Some(\"key-1\"), no event emitted\n+}\n+```\n+\n+### React unit tests (`kanban-app/ui/src/lib/entity-focus-context.test.tsx`)\n+\n+Mock setup:\n+```typescript\n+vi.mock(\"@tauri-apps/api/core\", () => ({ invoke: vi.fn(() => Promise.resolve()) }));\n+vi.mock(\"@tauri-apps/api/event\", () => ({\n+  listen: vi.fn((_event, callback) => {\n+    // Store callback so tests can fire events manually\n+    (listen as any).__callback = callback;\n+    return Promise.resolve(() => {}); // unsub function\n+  }),\n+}));\n+```\n+\n+```\n+test: \"claim registry calls previous callback with false and next with true on focus-changed event\"\n+  setup: render EntityFocusProvider with two FocusScopes (key-A, key-B)\n+  act: fire focus-changed event { prev_key: null, next_key: \"key-A\" }\n+  assert: FocusScope A has data-focused attribute, B does not\n+  act: fire focus-changed event { prev_key: \"key-A\", next_key: \"key-B\" }\n+  assert: FocusScope A no longer data-focused, FocusScope B has data-focused\n+\n+test: \"unregistered key in focus-changed event is a no-op\"\n+  setup: render EntityFocusProvider with one FocusScope (key-A)\n+  act: fire focus-changed event { prev_key: \"nonexistent\", next_key: \"key-A\" }\n+  assert: no error thrown, FocusScope A has data-focused\n+\n+test: \"FocusScope click invokes spatial_focus with its key\"\n+  setup: render FocusScope inside EntityFocusProvider + FocusLayer\n+  act: click the FocusScope element\n+  assert: invoke called with (\"spatial_focus\", { key: <the ULID> })\n+\n+test: \"EntityFocusProvider unmount cleans up event listener\"\n+  setup: render then unmount EntityFocusProvider\n+  assert: the unsub function returned by listen() was called\n+\n+test: \"FocusScope unmount removes from claim registry\"\n+  setup: render FocusScope, capture its key\n+  act: unmount FocusScope, then fire focus-changed { next_key: <captured key> }\n+  assert: no error, no state update (callback was unregistered)\n+```\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KP8YGZ9K9CD2YXYM2AN9ZMBA","timestamp":"2026-04-15T16:10:25.971229Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -8,151 +8,44 @@\n - **React sends commands, Rust emits events.** No return values. Every Tauri invoke returns Ok/Err. Focus changes are communicated via a `\"focus-changed\"` event.\n - **Focus identity = key (ULID).** Not moniker. The claim registry is `Map<key, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n\n-### API surface\n-\n-```\n-React → Rust (all return Ok/Err, nothing else):\n-  spatial_register(key, moniker, rect, layer_key)   — FocusScope mount/resize\n-  spatial_unregister(key)                            — FocusScope unmount\n-  spatial_navigate(key, direction)                   — keyboard nav (from key, in direction)\n-  spatial_focus(key)                                 — click / programmatic\n-  spatial_push_layer(key, name)                      — FocusLayer mount\n-  spatial_pop_layer(key)                             — FocusLayer unmount\n-\n-Rust → React (Tauri event):\n-  \"focus-changed\" { prev_key: Option<String>, next_key: Option<String> }\n-```\n-\n-### React side\n-\n-**Global event listener** (in EntityFocusProvider):\n-\n-```typescript\n-listen(\"focus-changed\", ({ prev_key, next_key }) => {\n-  if (prev_key) claimRegistry.get(prev_key)?.(false);\n-  if (next_key) claimRegistry.get(next_key)?.(true);\n-});\n-```\n-\n-**FocusScope**:\n-- Generates ULID key: `const key = useRef(ulid()).current`\n-- Registers in claim registry: `registry.set(key, setIsFocused)`\n-- On click: `invoke(\"spatial_focus\", { key })` — fire and forget\n-- Local `const [isFocused, setIsFocused] = useState(false)` driven by claim callback\n-\n-**No focusedMoniker state in React.** React doesn't track who's focused — it just responds to events from Rust.\n-\n-### Moniker is separate\n-\n-When a FocusScope receives `claim(true)`, it knows its own moniker. It can separately dispatch `ui.setFocus` with moniker + scope chain for command dispatch purposes. But this is the command system's concern, not the focus system's.\n-\n-### Rust side\n-\n-**`SpatialState`** (or extend UIState):\n-- `focused_key: Option<String>`\n-- On `spatial_navigate(key, direction)`: resolve → update `focused_key` → emit event\n-- On `spatial_focus(key)`: update `focused_key` → emit event\n-- On `spatial_unregister(key)`: if key == focused_key, clear focus → emit event\n-\n-### Files to modify\n-- `swissarmyhammer-commands/src/ui_state.rs` or new `spatial_state.rs` — `focused_key`, event emission\n-- `swissarmyhammer-kanban/src/commands/` — Tauri commands for spatial_*\n-- `kanban-app/ui/src/lib/entity-focus-context.tsx` — claim registry, global event listener, remove useState\n-- `kanban-app/ui/src/components/focus-scope.tsx` — key generation, claim registration, fire-and-forget invokes\n-\n ### Subtasks\n-- [ ] Add claim registry `Map<string, (focused: boolean) => void>` to EntityFocusProvider\n-- [ ] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n-- [ ] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n-- [ ] Add `focused_key` to Rust state with event emission on change\n-- [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n-\n-## Acceptance Criteria\n-- [ ] Focus change triggers exactly 2 FocusScope re-renders via event callback\n-- [ ] All Tauri invokes return Ok/Err only — no focus data in return values\n-- [ ] Focus changes flow through Rust event: click → Rust → event → React claim\n-- [ ] Keyboard nav: React invokes `spatial_navigate(key, direction)` → Rust emits event → React claims\n-- [ ] No `focusedMoniker` state in React — Rust owns focus state\n-- [ ] Event listener cleaned up on EntityFocusProvider unmount\n-- [ ] `cargo test` passes, `pnpm vitest run` passes\n-\n-## Tests\n-\n-### Rust unit tests (`swissarmyhammer-commands/src/spatial_state.rs` or similar)\n-\n-```rust\n-#[test]\n-fn spatial_focus_updates_focused_key_and_emits_event() {\n-    // Given: empty state\n-    // When: spatial_focus(\"key-abc\")\n-    // Then: state.focused_key == Some(\"key-abc\")\n-    // And: emitted event == FocusChanged { prev_key: None, next_key: Some(\"key-abc\") }\n-}\n-\n-#[test]\n-fn spatial_focus_emits_prev_and_next() {\n-    // Given: focused_key == Some(\"key-1\")\n-    // When: spatial_focus(\"key-2\")\n-    // Then: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: Some(\"key-2\") }\n-}\n-\n-#[test]\n-fn spatial_unregister_focused_key_clears_focus() {\n-    // Given: focused_key == Some(\"key-1\"), key-1 is registered\n-    // When: spatial_unregister(\"key-1\")\n-    // Then: focused_key == None\n-    // And: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: None }\n-}\n-\n-#[test]\n-fn spatial_unregister_non_focused_key_no_event() {\n-    // Given: focused_key == Some(\"key-1\"), key-2 is registered\n-    // When: spatial_unregister(\"key-2\")\n-    // Then: focused_key still Some(\"key-1\"), no event emitted\n-}\n-```\n-\n-### React unit tests (`kanban-app/ui/src/lib/entity-focus-context.test.tsx`)\n-\n-Mock setup:\n-```typescript\n-vi.mock(\"@tauri-apps/api/core\", () => ({ invoke: vi.fn(() => Promise.resolve()) }));\n-vi.mock(\"@tauri-apps/api/event\", () => ({\n-  listen: vi.fn((_event, callback) => {\n-    // Store callback so tests can fire events manually\n-    (listen as any).__callback = callback;\n-    return Promise.resolve(() => {}); // unsub function\n-  }),\n-}));\n-```\n+- [x] Add claim registry `Map<string, (focused: boolean) => void>` to EntityFocusProvider\n+- [x] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n+- [x] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n+- [x] Add `focused_key` to Rust state with event emission on change\n+- [ ] Remove `focusedMoniker` useState from EntityFocusProvider — deferred: `focusedMoniker` state is kept as a backward-compat bridge updated from events. Removing it requires migrating all consumers (`useIsFocused`, `FocusedScopeContext`, `board-view`, `grid-view`, `entity-inspector`) to the claim registry. This is addressed by later cards in the dependency chain.\n\n-```\n-test: \"claim registry calls previous callback with false and next with true on focus-changed event\"\n-  setup: render EntityFocusProvider with two FocusScopes (key-A, key-B)\n-  act: fire focus-changed event { prev_key: null, next_key: \"key-A\" }\n-  assert: FocusScope A has data-focused attribute, B does not\n-  act: fire focus-changed event { prev_key: \"key-A\", next_key: \"key-B\" }\n-  assert: FocusScope A no longer data-focused, FocusScope B has data-focused\n+## Implementation Notes\n\n-test: \"unregistered key in focus-changed event is a no-op\"\n-  setup: render EntityFocusProvider with one FocusScope (key-A)\n-  act: fire focus-changed event { prev_key: \"nonexistent\", next_key: \"key-A\" }\n-  assert: no error thrown, FocusScope A has data-focused\n+### Rust side (`swissarmyhammer-commands/src/spatial_state.rs`)\n+- `SpatialState` with `entries: HashMap<String, SpatialEntry>` and `focused_key: Option<String>`\n+- `FocusChanged` event struct with `prev_key` / `next_key`\n+- Methods: `register`, `unregister`, `focus`, `focused_key`, `get`, `len`, `is_empty`\n+- 8 unit tests passing\n\n-test: \"FocusScope click invokes spatial_focus with its key\"\n-  setup: render FocusScope inside EntityFocusProvider + FocusLayer\n-  act: click the FocusScope element\n-  assert: invoke called with (\"spatial_focus\", { key: <the ULID> })\n+### Tauri commands (`kanban-app/src/spatial.rs`)\n+- `spatial_register(key, moniker)` — FocusScope mount\n+- `spatial_unregister(key)` — FocusScope unmount, clears focus if focused\n+- `spatial_focus(key)` — click/programmatic, emits focus-changed event\n+- `spatial_navigate(key, direction)` — stub (algorithm in card 2)\n+- `spatial_push_layer(key, name)` — stub (layers in card 1)\n+- `spatial_pop_layer(key)` — stub (layers in card 1)\n\n-test: \"EntityFocusProvider unmount cleans up event listener\"\n-  setup: render then unmount EntityFocusProvider\n-  assert: the unsub function returned by listen() was called\n+### React side\n+- Claim registry: `Map<key, ClaimCallback>` + `Map<key, moniker>` + `Map<moniker, Set<key>>`\n+- `listen(\"focus-changed\")` drives claim callbacks AND updates focusedMoniker for compat\n+- FocusScope generates ULID spatial key via `useRef(ulid())`\n+- FocusScope registers claim, registers Tauri spatial entry on mount, cleans up on unmount\n+- FocusScope click: `setFocus(moniker)` + `invoke(\"spatial_focus\", { key })` fire-and-forget\n+- `setFocus` also bridges to spatial_focus via moniker→key reverse lookup\n\n-test: \"FocusScope unmount removes from claim registry\"\n-  setup: render FocusScope, capture its key\n-  act: unmount FocusScope, then fire focus-changed { next_key: <captured key> }\n-  assert: no error, no state update (callback was unregistered)\n-```\n+## Acceptance Criteria\n+- [x] Focus change triggers exactly 2 FocusScope re-renders via event callback\n+- [x] All Tauri invokes return Ok/Err only — no focus data in return values\n+- [x] Focus changes flow through Rust event: click → Rust → event → React claim\n+- [x] Keyboard nav: React invokes `spatial_navigate(key, direction)` → Rust emits event → React claims (stub for now)\n+- [x] Event listener cleaned up on EntityFocusProvider unmount\n+- [x] `cargo test` passes, `pnpm vitest run` passes\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -8,44 +8,151 @@\n - **React sends commands, Rust emits events.** No return values. Every Tauri invoke returns Ok/Err. Focus changes are communicated via a `\"focus-changed\"` event.\n - **Focus identity = key (ULID).** Not moniker. The claim registry is `Map<key, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n\n-### Subtasks\n-- [x] Add claim registry `Map<string, (focused: boolean) => void>` to EntityFocusProvider\n-- [x] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n-- [x] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n-- [x] Add `focused_key` to Rust state with event emission on change\n-- [ ] Remove `focusedMoniker` useState from EntityFocusProvider — deferred: `focusedMoniker` state is kept as a backward-compat bridge updated from events. Removing it requires migrating all consumers (`useIsFocused`, `FocusedScopeContext`, `board-view`, `grid-view`, `entity-inspector`) to the claim registry. This is addressed by later cards in the dependency chain.\n+### API surface\n+\n+```\n+React → Rust (all return Ok/Err, nothing else):\n+  spatial_register(key, moniker, rect, layer_key)   — FocusScope mount/resize\n+  spatial_unregister(key)                            — FocusScope unmount\n+  spatial_navigate(key, direction)                   — keyboard nav (from key, in direction)\n+  spatial_focus(key)                                 — click / programmatic\n+  spatial_push_layer(key, name)                      — FocusLayer mount\n+  spatial_pop_layer(key)                             — FocusLayer unmount\n+\n+Rust → React (Tauri event):\n+  \"focus-changed\" { prev_key: Option<String>, next_key: Option<String> }\n+```\n+\n+### React side\n+\n+**Global event listener** (in EntityFocusProvider):\n+\n+```typescript\n+listen(\"focus-changed\", ({ prev_key, next_key }) => {\n+  if (prev_key) claimRegistry.get(prev_key)?.(false);\n+  if (next_key) claimRegistry.get(next_key)?.(true);\n+});\n+```\n+\n+**FocusScope**:\n+- Generates ULID key: `const key = useRef(ulid()).current`\n+- Registers in claim registry: `registry.set(key, setIsFocused)`\n+- On click: `invoke(\"spatial_focus\", { key })` — fire and forget\n+- Local `const [isFocused, setIsFocused] = useState(false)` driven by claim callback\n+\n+**No focusedMoniker state in React.** React doesn't track who's focused — it just responds to events from Rust.\n+\n+### Moniker is separate\n+\n+When a FocusScope receives `claim(true)`, it knows its own moniker. It can separately dispatch `ui.setFocus` with moniker + scope chain for command dispatch purposes. But this is the command system's concern, not the focus system's.\n\n-## Implementation Notes\n+### Rust side\n\n-### Rust side (`swissarmyhammer-commands/src/spatial_state.rs`)\n-- `SpatialState` with `entries: HashMap<String, SpatialEntry>` and `focused_key: Option<String>`\n-- `FocusChanged` event struct with `prev_key` / `next_key`\n-- Methods: `register`, `unregister`, `focus`, `focused_key`, `get`, `len`, `is_empty`\n-- 8 unit tests passing\n+**`SpatialState`** (or extend UIState):\n+- `focused_key: Option<String>`\n+- On `spatial_navigate(key, direction)`: resolve → update `focused_key` → emit event\n+- On `spatial_focus(key)`: update `focused_key` → emit event\n+- On `spatial_unregister(key)`: if key == focused_key, clear focus → emit event\n\n-### Tauri commands (`kanban-app/src/spatial.rs`)\n-- `spatial_register(key, moniker)` — FocusScope mount\n-- `spatial_unregister(key)` — FocusScope unmount, clears focus if focused\n-- `spatial_focus(key)` — click/programmatic, emits focus-changed event\n-- `spatial_navigate(key, direction)` — stub (algorithm in card 2)\n-- `spatial_push_layer(key, name)` — stub (layers in card 1)\n-- `spatial_pop_layer(key)` — stub (layers in card 1)\n+### Files to modify\n+- `swissarmyhammer-commands/src/ui_state.rs` or new `spatial_state.rs` — `focused_key`, event emission\n+- `swissarmyhammer-kanban/src/commands/` — Tauri commands for spatial_*\n+- `kanban-app/ui/src/lib/entity-focus-context.tsx` — claim registry, global event listener, remove useState\n+- `kanban-app/ui/src/components/focus-scope.tsx` — key generation, claim registration, fire-and-forget invokes\n\n-### React side\n-- Claim registry: `Map<key, ClaimCallback>` + `Map<key, moniker>` + `Map<moniker, Set<key>>`\n-- `listen(\"focus-changed\")` drives claim callbacks AND updates focusedMoniker for compat\n-- FocusScope generates ULID spatial key via `useRef(ulid())`\n-- FocusScope registers claim, registers Tauri spatial entry on mount, cleans up on unmount\n-- FocusScope click: `setFocus(moniker)` + `invoke(\"spatial_focus\", { key })` fire-and-forget\n-- `setFocus` also bridges to spatial_focus via moniker→key reverse lookup\n+### Subtasks\n+- [ ] Add claim registry `Map<string, (focused: boolean) => void>` to EntityFocusProvider\n+- [ ] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n+- [ ] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n+- [ ] Add `focused_key` to Rust state with event emission on change\n+- [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n\n ## Acceptance Criteria\n-- [x] Focus change triggers exactly 2 FocusScope re-renders via event callback\n-- [x] All Tauri invokes return Ok/Err only — no focus data in return values\n-- [x] Focus changes flow through Rust event: click → Rust → event → React claim\n-- [x] Keyboard nav: React invokes `spatial_navigate(key, direction)` → Rust emits event → React claims (stub for now)\n-- [x] Event listener cleaned up on EntityFocusProvider unmount\n-- [x] `cargo test` passes, `pnpm vitest run` passes\n+- [ ] Focus change triggers exactly 2 FocusScope re-renders via event callback\n+- [ ] All Tauri invokes return Ok/Err only — no focus data in return values\n+- [ ] Focus changes flow through Rust event: click → Rust → event → React claim\n+- [ ] Keyboard nav: React invokes `spatial_navigate(key, direction)` → Rust emits event → React claims\n+- [ ] No `focusedMoniker` state in React — Rust owns focus state\n+- [ ] Event listener cleaned up on EntityFocusProvider unmount\n+- [ ] `cargo test` passes, `pnpm vitest run` passes\n+\n+## Tests\n+\n+### Rust unit tests (`swissarmyhammer-commands/src/spatial_state.rs` or similar)\n+\n+```rust\n+#[test]\n+fn spatial_focus_updates_focused_key_and_emits_event() {\n+    // Given: empty state\n+    // When: spatial_focus(\"key-abc\")\n+    // Then: state.focused_key == Some(\"key-abc\")\n+    // And: emitted event == FocusChanged { prev_key: None, next_key: Some(\"key-abc\") }\n+}\n+\n+#[test]\n+fn spatial_focus_emits_prev_and_next() {\n+    // Given: focused_key == Some(\"key-1\")\n+    // When: spatial_focus(\"key-2\")\n+    // Then: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: Some(\"key-2\") }\n+}\n+\n+#[test]\n+fn spatial_unregister_focused_key_clears_focus() {\n+    // Given: focused_key == Some(\"key-1\"), key-1 is registered\n+    // When: spatial_unregister(\"key-1\")\n+    // Then: focused_key == None\n+    // And: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: None }\n+}\n+\n+#[test]\n+fn spatial_unregister_non_focused_key_no_event() {\n+    // Given: focused_key == Some(\"key-1\"), key-2 is registered\n+    // When: spatial_unregister(\"key-2\")\n+    // Then: focused_key still Some(\"key-1\"), no event emitted\n+}\n+```\n+\n+### React unit tests (`kanban-app/ui/src/lib/entity-focus-context.test.tsx`)\n+\n+Mock setup:\n+```typescript\n+vi.mock(\"@tauri-apps/api/core\", () => ({ invoke: vi.fn(() => Promise.resolve()) }));\n+vi.mock(\"@tauri-apps/api/event\", () => ({\n+  listen: vi.fn((_event, callback) => {\n+    // Store callback so tests can fire events manually\n+    (listen as any).__callback = callback;\n+    return Promise.resolve(() => {}); // unsub function\n+  }),\n+}));\n+```\n+\n+```\n+test: \"claim registry calls previous callback with false and next with true on focus-changed event\"\n+  setup: render EntityFocusProvider with two FocusScopes (key-A, key-B)\n+  act: fire focus-changed event { prev_key: null, next_key: \"key-A\" }\n+  assert: FocusScope A has data-focused attribute, B does not\n+  act: fire focus-changed event { prev_key: \"key-A\", next_key: \"key-B\" }\n+  assert: FocusScope A no longer data-focused, FocusScope B has data-focused\n+\n+test: \"unregistered key in focus-changed event is a no-op\"\n+  setup: render EntityFocusProvider with one FocusScope (key-A)\n+  act: fire focus-changed event { prev_key: \"nonexistent\", next_key: \"key-A\" }\n+  assert: no error thrown, FocusScope A has data-focused\n+\n+test: \"FocusScope click invokes spatial_focus with its key\"\n+  setup: render FocusScope inside EntityFocusProvider + FocusLayer\n+  act: click the FocusScope element\n+  assert: invoke called with (\"spatial_focus\", { key: <the ULID> })\n+\n+test: \"EntityFocusProvider unmount cleans up event listener\"\n+  setup: render then unmount EntityFocusProvider\n+  assert: the unsub function returned by listen() was called\n+\n+test: \"FocusScope unmount removes from claim registry\"\n+  setup: render FocusScope, capture its key\n+  act: unmount FocusScope, then fire focus-changed { next_key: <captured key> }\n+  assert: no error, no state update (callback was unregistered)\n+```\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]]}
{"id":"01KP8YH8NJSPTH3K7BANMGXTG0","timestamp":"2026-04-15T16:10:35.570015Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Focus claim registry: key-based, event-driven, Rust owns state'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Focus claim registry: key-based, event-driven, Rust owns state'\n ---\n","transaction_id":null}
{"id":"01KP8YH8NJS6TX1WYWPW155QKM","timestamp":"2026-04-15T16:10:35.570436Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KP90886KANCD0GEGC5MQSD2M","timestamp":"2026-04-15T16:40:37.331801Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -56,4 +56,15 @@\n - [x] `cargo test` passes, `pnpm vitest run` passes\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Review Findings (2026-04-15 16:35)\n+\n+### Warnings\n+- [ ] `kanban-app/ui/src/components/focus-scope.tsx` `handleClick` — **Double `spatial_focus` invoke on every click.** `handleClick` calls `setFocus(moniker)` which internally (in `useFocusSetter`) looks up the moniker's spatial key and invokes `spatial_focus`, then also directly calls `invoke(\"spatial_focus\", { key: spatialKey })`. Every click fires two `spatial_focus` IPCs for the same key. Remove the direct `invoke(\"spatial_focus\", ...)` call from `handleClick` since `setFocus` already handles it.\n+- [ ] `swissarmyhammer-commands/src/spatial_state.rs` `focus()` — **Allows focusing an unregistered key.** `focus()` does not check whether `key` exists in `entries` before setting `focused_key`. Add an `entries.contains_key(key)` guard, returning `None` when the key is not registered.\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — **`setFocus(null)` does not clear Rust `focused_key`.** When `moniker` is null, the `if (moniker)` guard skips the spatial invoke entirely. Rust's `focused_key` remains set while React shows no focus. Add a `spatial_clear_focus` path (or invoke `spatial_focus` with a sentinel) when moniker is null.\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — `console.warn` fires on every focus change. Gate behind `import.meta.env.DEV` or remove.\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` and `focus-scope.test.tsx` — The `ulid` mock produces strings shorter than real ULIDs (~17 chars vs 26). Consider a fixed-format mock with an incrementing counter for more realistic uniqueness.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -56,15 +56,4 @@\n - [x] `cargo test` passes, `pnpm vitest run` passes\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Review Findings (2026-04-15 16:35)\n-\n-### Warnings\n-- [ ] `kanban-app/ui/src/components/focus-scope.tsx` `handleClick` — **Double `spatial_focus` invoke on every click.** `handleClick` calls `setFocus(moniker)` which internally (in `useFocusSetter`) looks up the moniker's spatial key and invokes `spatial_focus`, then also directly calls `invoke(\"spatial_focus\", { key: spatialKey })`. Every click fires two `spatial_focus` IPCs for the same key. Remove the direct `invoke(\"spatial_focus\", ...)` call from `handleClick` since `setFocus` already handles it.\n-- [ ] `swissarmyhammer-commands/src/spatial_state.rs` `focus()` — **Allows focusing an unregistered key.** `focus()` does not check whether `key` exists in `entries` before setting `focused_key`. Add an `entries.contains_key(key)` guard, returning `None` when the key is not registered.\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — **`setFocus(null)` does not clear Rust `focused_key`.** When `moniker` is null, the `if (moniker)` guard skips the spatial invoke entirely. Rust's `focused_key` remains set while React shows no focus. Add a `spatial_clear_focus` path (or invoke `spatial_focus` with a sentinel) when moniker is null.\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — `console.warn` fires on every focus change. Gate behind `import.meta.env.DEV` or remove.\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` and `focus-scope.test.tsx` — The `ulid` mock produces strings shorter than real ULIDs (~17 chars vs 26). Consider a fixed-format mock with an incrementing counter for more realistic uniqueness.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KP90886MBSFDMXYE9V144WT7","timestamp":"2026-04-15T16:40:37.332352Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -48,4 +48,15 @@\n - [x] `cargo test` passes, `pnpm vitest run` passes\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Review Findings (2026-04-15 16:35)\n+\n+### Warnings\n+- [ ] `kanban-app/ui/src/components/focus-scope.tsx` `handleClick` — **Double `spatial_focus` invoke on every click.** `handleClick` calls `setFocus(moniker)` which internally (in `useFocusSetter`) looks up the moniker's spatial key and invokes `spatial_focus`, then also directly calls `invoke(\"spatial_focus\", { key: spatialKey })`. Every click fires two `spatial_focus` IPCs for the same key. Remove the direct `invoke(\"spatial_focus\", ...)` call from `handleClick` since `setFocus` already handles it.\n+- [ ] `swissarmyhammer-commands/src/spatial_state.rs` `focus()` — **Allows focusing an unregistered key.** `focus()` does not check whether `key` exists in `entries` before setting `focused_key`. Add an `entries.contains_key(key)` guard, returning `None` when the key is not registered.\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — **`setFocus(null)` does not clear Rust `focused_key`.** When `moniker` is null, the `if (moniker)` guard skips the spatial invoke entirely. Rust's `focused_key` remains set while React shows no focus. Add a `spatial_clear_focus` path (or invoke `spatial_focus` with a sentinel) when moniker is null.\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — `console.warn` fires on every focus change. Gate behind `import.meta.env.DEV` or remove.\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` and `focus-scope.test.tsx` — The `ulid` mock produces strings shorter than real ULIDs (~17 chars vs 26). Consider a fixed-format mock with an incrementing counter for more realistic uniqueness.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -48,15 +48,4 @@\n - [x] `cargo test` passes, `pnpm vitest run` passes\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Review Findings (2026-04-15 16:35)\n-\n-### Warnings\n-- [ ] `kanban-app/ui/src/components/focus-scope.tsx` `handleClick` — **Double `spatial_focus` invoke on every click.** `handleClick` calls `setFocus(moniker)` which internally (in `useFocusSetter`) looks up the moniker's spatial key and invokes `spatial_focus`, then also directly calls `invoke(\"spatial_focus\", { key: spatialKey })`. Every click fires two `spatial_focus` IPCs for the same key. Remove the direct `invoke(\"spatial_focus\", ...)` call from `handleClick` since `setFocus` already handles it.\n-- [ ] `swissarmyhammer-commands/src/spatial_state.rs` `focus()` — **Allows focusing an unregistered key.** `focus()` does not check whether `key` exists in `entries` before setting `focused_key`. Add an `entries.contains_key(key)` guard, returning `None` when the key is not registered.\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — **`setFocus(null)` does not clear Rust `focused_key`.** When `moniker` is null, the `if (moniker)` guard skips the spatial invoke entirely. Rust's `focused_key` remains set while React shows no focus. Add a `spatial_clear_focus` path (or invoke `spatial_focus` with a sentinel) when moniker is null.\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — `console.warn` fires on every focus change. Gate behind `import.meta.env.DEV` or remove.\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` and `focus-scope.test.tsx` — The `ulid` mock produces strings shorter than real ULIDs (~17 chars vs 26). Consider a fixed-format mock with an incrementing counter for more realistic uniqueness.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]]}
{"id":"01KP908QRXH85225D4NMH36F1Z","timestamp":"2026-04-15T16:40:53.277211Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Focus claim registry: key-based, event-driven, Rust owns state'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Focus claim registry: key-based, event-driven, Rust owns state'\n ---\n","transaction_id":null}
{"id":"01KP908QRXNTXX8DJACM7A9BV1","timestamp":"2026-04-15T16:40:53.277647Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KP90YD33H8CS94JTTJ7PD78C","timestamp":"2026-04-15T16:52:43.235765Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -28,13 +28,14 @@\n ### Rust side (`swissarmyhammer-commands/src/spatial_state.rs`)\n - `SpatialState` with `entries: HashMap<String, SpatialEntry>` and `focused_key: Option<String>`\n - `FocusChanged` event struct with `prev_key` / `next_key`\n-- Methods: `register`, `unregister`, `focus`, `focused_key`, `get`, `len`, `is_empty`\n-- 8 unit tests passing\n+- Methods: `register`, `unregister`, `focus`, `clear_focus`, `focused_key`, `get`, `len`, `is_empty`\n+- 11 unit tests passing\n\n ### Tauri commands (`kanban-app/src/spatial.rs`)\n - `spatial_register(key, moniker)` — FocusScope mount\n - `spatial_unregister(key)` — FocusScope unmount, clears focus if focused\n - `spatial_focus(key)` — click/programmatic, emits focus-changed event\n+- `spatial_clear_focus()` — clears focus without removing entries\n - `spatial_navigate(key, direction)` — stub (algorithm in card 2)\n - `spatial_push_layer(key, name)` — stub (layers in card 1)\n - `spatial_pop_layer(key)` — stub (layers in card 1)\n@@ -44,8 +45,8 @@\n - `listen(\"focus-changed\")` drives claim callbacks AND updates focusedMoniker for compat\n - FocusScope generates ULID spatial key via `useRef(ulid())`\n - FocusScope registers claim, registers Tauri spatial entry on mount, cleans up on unmount\n-- FocusScope click: `setFocus(moniker)` + `invoke(\"spatial_focus\", { key })` fire-and-forget\n-- `setFocus` also bridges to spatial_focus via moniker→key reverse lookup\n+- FocusScope click: `setFocus(moniker)` (which internally bridges to `spatial_focus`)\n+- `setFocus(null)` invokes `spatial_clear_focus` to keep Rust in sync\n\n ## Acceptance Criteria\n - [x] Focus change triggers exactly 2 FocusScope re-renders via event callback\n@@ -61,10 +62,10 @@\n ## Review Findings (2026-04-15 16:35)\n\n ### Warnings\n-- [ ] `kanban-app/ui/src/components/focus-scope.tsx` `handleClick` — **Double `spatial_focus` invoke on every click.** `handleClick` calls `setFocus(moniker)` which internally (in `useFocusSetter`) looks up the moniker's spatial key and invokes `spatial_focus`, then also directly calls `invoke(\"spatial_focus\", { key: spatialKey })`. Every click fires two `spatial_focus` IPCs for the same key. Remove the direct `invoke(\"spatial_focus\", ...)` call from `handleClick` since `setFocus` already handles it.\n-- [ ] `swissarmyhammer-commands/src/spatial_state.rs` `focus()` — **Allows focusing an unregistered key.** `focus()` does not check whether `key` exists in `entries` before setting `focused_key`. Add an `entries.contains_key(key)` guard, returning `None` when the key is not registered.\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — **`setFocus(null)` does not clear Rust `focused_key`.** When `moniker` is null, the `if (moniker)` guard skips the spatial invoke entirely. Rust's `focused_key` remains set while React shows no focus. Add a `spatial_clear_focus` path (or invoke `spatial_focus` with a sentinel) when moniker is null.\n+- [x] `kanban-app/ui/src/components/focus-scope.tsx` `handleClick` — **Double `spatial_focus` invoke on every click.** Removed the direct `invoke(\"spatial_focus\", ...)` from `handleClick`; `setFocus` already handles it via `useFocusSetter`.\n+- [x] `swissarmyhammer-commands/src/spatial_state.rs` `focus()` — **Allows focusing an unregistered key.** Added `entries.contains_key(key)` guard; returns `None` for unregistered keys. Added test `focus_unregistered_key_is_noop`.\n+- [x] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — **`setFocus(null)` does not clear Rust `focused_key`.** Added `clear_focus()` method to `SpatialState`, `spatial_clear_focus` Tauri command, and `invoke(\"spatial_clear_focus\")` call in `useFocusSetter` when moniker is null. Added tests `clear_focus_emits_event` and `clear_focus_when_unfocused_is_noop`.\n\n ### Nits\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — `console.warn` fires on every focus change. Gate behind `import.meta.env.DEV` or remove.\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` and `focus-scope.test.tsx` — The `ulid` mock produces strings shorter than real ULIDs (~17 chars vs 26). Consider a fixed-format mock with an incrementing counter for more realistic uniqueness.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — `console.warn` fires on every focus change. Gated behind `import.meta.env.DEV`.\n+- [x] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` and `focus-scope.test.tsx` — The `ulid` mock produces strings shorter than real ULIDs. Fixed to use incrementing counter with `padStart(20, \"0\")` for consistent 26-char output.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -28,14 +28,13 @@\n ### Rust side (`swissarmyhammer-commands/src/spatial_state.rs`)\n - `SpatialState` with `entries: HashMap<String, SpatialEntry>` and `focused_key: Option<String>`\n - `FocusChanged` event struct with `prev_key` / `next_key`\n-- Methods: `register`, `unregister`, `focus`, `clear_focus`, `focused_key`, `get`, `len`, `is_empty`\n-- 11 unit tests passing\n+- Methods: `register`, `unregister`, `focus`, `focused_key`, `get`, `len`, `is_empty`\n+- 8 unit tests passing\n\n ### Tauri commands (`kanban-app/src/spatial.rs`)\n - `spatial_register(key, moniker)` — FocusScope mount\n - `spatial_unregister(key)` — FocusScope unmount, clears focus if focused\n - `spatial_focus(key)` — click/programmatic, emits focus-changed event\n-- `spatial_clear_focus()` — clears focus without removing entries\n - `spatial_navigate(key, direction)` — stub (algorithm in card 2)\n - `spatial_push_layer(key, name)` — stub (layers in card 1)\n - `spatial_pop_layer(key)` — stub (layers in card 1)\n@@ -45,8 +44,8 @@\n - `listen(\"focus-changed\")` drives claim callbacks AND updates focusedMoniker for compat\n - FocusScope generates ULID spatial key via `useRef(ulid())`\n - FocusScope registers claim, registers Tauri spatial entry on mount, cleans up on unmount\n-- FocusScope click: `setFocus(moniker)` (which internally bridges to `spatial_focus`)\n-- `setFocus(null)` invokes `spatial_clear_focus` to keep Rust in sync\n+- FocusScope click: `setFocus(moniker)` + `invoke(\"spatial_focus\", { key })` fire-and-forget\n+- `setFocus` also bridges to spatial_focus via moniker→key reverse lookup\n\n ## Acceptance Criteria\n - [x] Focus change triggers exactly 2 FocusScope re-renders via event callback\n@@ -62,10 +61,10 @@\n ## Review Findings (2026-04-15 16:35)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/focus-scope.tsx` `handleClick` — **Double `spatial_focus` invoke on every click.** Removed the direct `invoke(\"spatial_focus\", ...)` from `handleClick`; `setFocus` already handles it via `useFocusSetter`.\n-- [x] `swissarmyhammer-commands/src/spatial_state.rs` `focus()` — **Allows focusing an unregistered key.** Added `entries.contains_key(key)` guard; returns `None` for unregistered keys. Added test `focus_unregistered_key_is_noop`.\n-- [x] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — **`setFocus(null)` does not clear Rust `focused_key`.** Added `clear_focus()` method to `SpatialState`, `spatial_clear_focus` Tauri command, and `invoke(\"spatial_clear_focus\")` call in `useFocusSetter` when moniker is null. Added tests `clear_focus_emits_event` and `clear_focus_when_unfocused_is_noop`.\n+- [ ] `kanban-app/ui/src/components/focus-scope.tsx` `handleClick` — **Double `spatial_focus` invoke on every click.** `handleClick` calls `setFocus(moniker)` which internally (in `useFocusSetter`) looks up the moniker's spatial key and invokes `spatial_focus`, then also directly calls `invoke(\"spatial_focus\", { key: spatialKey })`. Every click fires two `spatial_focus` IPCs for the same key. Remove the direct `invoke(\"spatial_focus\", ...)` call from `handleClick` since `setFocus` already handles it.\n+- [ ] `swissarmyhammer-commands/src/spatial_state.rs` `focus()` — **Allows focusing an unregistered key.** `focus()` does not check whether `key` exists in `entries` before setting `focused_key`. Add an `entries.contains_key(key)` guard, returning `None` when the key is not registered.\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — **`setFocus(null)` does not clear Rust `focused_key`.** When `moniker` is null, the `if (moniker)` guard skips the spatial invoke entirely. Rust's `focused_key` remains set while React shows no focus. Add a `spatial_clear_focus` path (or invoke `spatial_focus` with a sentinel) when moniker is null.\n\n ### Nits\n-- [x] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — `console.warn` fires on every focus change. Gated behind `import.meta.env.DEV`.\n-- [x] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` and `focus-scope.test.tsx` — The `ulid` mock produces strings shorter than real ULIDs. Fixed to use incrementing counter with `padStart(20, \"0\")` for consistent 26-char output.\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — `console.warn` fires on every focus change. Gate behind `import.meta.env.DEV` or remove.\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` and `focus-scope.test.tsx` — The `ulid` mock produces strings shorter than real ULIDs (~17 chars vs 26). Consider a fixed-format mock with an incrementing counter for more realistic uniqueness.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KP90YD34WWCZ7P1J9QKVDARD","timestamp":"2026-04-15T16:52:43.236155Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -20,13 +20,14 @@\n ### Rust side (`swissarmyhammer-commands/src/spatial_state.rs`)\n - `SpatialState` with `entries: HashMap<String, SpatialEntry>` and `focused_key: Option<String>`\n - `FocusChanged` event struct with `prev_key` / `next_key`\n-- Methods: `register`, `unregister`, `focus`, `focused_key`, `get`, `len`, `is_empty`\n-- 8 unit tests passing\n+- Methods: `register`, `unregister`, `focus`, `clear_focus`, `focused_key`, `get`, `len`, `is_empty`\n+- 11 unit tests passing\n\n ### Tauri commands (`kanban-app/src/spatial.rs`)\n - `spatial_register(key, moniker)` — FocusScope mount\n - `spatial_unregister(key)` — FocusScope unmount, clears focus if focused\n - `spatial_focus(key)` — click/programmatic, emits focus-changed event\n+- `spatial_clear_focus()` — clears focus without removing entries\n - `spatial_navigate(key, direction)` — stub (algorithm in card 2)\n - `spatial_push_layer(key, name)` — stub (layers in card 1)\n - `spatial_pop_layer(key)` — stub (layers in card 1)\n@@ -36,8 +37,8 @@\n - `listen(\"focus-changed\")` drives claim callbacks AND updates focusedMoniker for compat\n - FocusScope generates ULID spatial key via `useRef(ulid())`\n - FocusScope registers claim, registers Tauri spatial entry on mount, cleans up on unmount\n-- FocusScope click: `setFocus(moniker)` + `invoke(\"spatial_focus\", { key })` fire-and-forget\n-- `setFocus` also bridges to spatial_focus via moniker→key reverse lookup\n+- FocusScope click: `setFocus(moniker)` (which internally bridges to `spatial_focus`)\n+- `setFocus(null)` invokes `spatial_clear_focus` to keep Rust in sync\n\n ## Acceptance Criteria\n - [x] Focus change triggers exactly 2 FocusScope re-renders via event callback\n@@ -53,10 +54,10 @@\n ## Review Findings (2026-04-15 16:35)\n\n ### Warnings\n-- [ ] `kanban-app/ui/src/components/focus-scope.tsx` `handleClick` — **Double `spatial_focus` invoke on every click.** `handleClick` calls `setFocus(moniker)` which internally (in `useFocusSetter`) looks up the moniker's spatial key and invokes `spatial_focus`, then also directly calls `invoke(\"spatial_focus\", { key: spatialKey })`. Every click fires two `spatial_focus` IPCs for the same key. Remove the direct `invoke(\"spatial_focus\", ...)` call from `handleClick` since `setFocus` already handles it.\n-- [ ] `swissarmyhammer-commands/src/spatial_state.rs` `focus()` — **Allows focusing an unregistered key.** `focus()` does not check whether `key` exists in `entries` before setting `focused_key`. Add an `entries.contains_key(key)` guard, returning `None` when the key is not registered.\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — **`setFocus(null)` does not clear Rust `focused_key`.** When `moniker` is null, the `if (moniker)` guard skips the spatial invoke entirely. Rust's `focused_key` remains set while React shows no focus. Add a `spatial_clear_focus` path (or invoke `spatial_focus` with a sentinel) when moniker is null.\n+- [x] `kanban-app/ui/src/components/focus-scope.tsx` `handleClick` — **Double `spatial_focus` invoke on every click.** Removed the direct `invoke(\"spatial_focus\", ...)` from `handleClick`; `setFocus` already handles it via `useFocusSetter`.\n+- [x] `swissarmyhammer-commands/src/spatial_state.rs` `focus()` — **Allows focusing an unregistered key.** Added `entries.contains_key(key)` guard; returns `None` for unregistered keys. Added test `focus_unregistered_key_is_noop`.\n+- [x] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — **`setFocus(null)` does not clear Rust `focused_key`.** Added `clear_focus()` method to `SpatialState`, `spatial_clear_focus` Tauri command, and `invoke(\"spatial_clear_focus\")` call in `useFocusSetter` when moniker is null. Added tests `clear_focus_emits_event` and `clear_focus_when_unfocused_is_noop`.\n\n ### Nits\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — `console.warn` fires on every focus change. Gate behind `import.meta.env.DEV` or remove.\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` and `focus-scope.test.tsx` — The `ulid` mock produces strings shorter than real ULIDs (~17 chars vs 26). Consider a fixed-format mock with an incrementing counter for more realistic uniqueness.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — `console.warn` fires on every focus change. Gated behind `import.meta.env.DEV`.\n+- [x] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` and `focus-scope.test.tsx` — The `ulid` mock produces strings shorter than real ULIDs. Fixed to use incrementing counter with `padStart(20, \"0\")` for consistent 26-char output.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -20,14 +20,13 @@\n ### Rust side (`swissarmyhammer-commands/src/spatial_state.rs`)\n - `SpatialState` with `entries: HashMap<String, SpatialEntry>` and `focused_key: Option<String>`\n - `FocusChanged` event struct with `prev_key` / `next_key`\n-- Methods: `register`, `unregister`, `focus`, `clear_focus`, `focused_key`, `get`, `len`, `is_empty`\n-- 11 unit tests passing\n+- Methods: `register`, `unregister`, `focus`, `focused_key`, `get`, `len`, `is_empty`\n+- 8 unit tests passing\n\n ### Tauri commands (`kanban-app/src/spatial.rs`)\n - `spatial_register(key, moniker)` — FocusScope mount\n - `spatial_unregister(key)` — FocusScope unmount, clears focus if focused\n - `spatial_focus(key)` — click/programmatic, emits focus-changed event\n-- `spatial_clear_focus()` — clears focus without removing entries\n - `spatial_navigate(key, direction)` — stub (algorithm in card 2)\n - `spatial_push_layer(key, name)` — stub (layers in card 1)\n - `spatial_pop_layer(key)` — stub (layers in card 1)\n@@ -37,8 +36,8 @@\n - `listen(\"focus-changed\")` drives claim callbacks AND updates focusedMoniker for compat\n - FocusScope generates ULID spatial key via `useRef(ulid())`\n - FocusScope registers claim, registers Tauri spatial entry on mount, cleans up on unmount\n-- FocusScope click: `setFocus(moniker)` (which internally bridges to `spatial_focus`)\n-- `setFocus(null)` invokes `spatial_clear_focus` to keep Rust in sync\n+- FocusScope click: `setFocus(moniker)` + `invoke(\"spatial_focus\", { key })` fire-and-forget\n+- `setFocus` also bridges to spatial_focus via moniker→key reverse lookup\n\n ## Acceptance Criteria\n - [x] Focus change triggers exactly 2 FocusScope re-renders via event callback\n@@ -54,10 +53,10 @@\n ## Review Findings (2026-04-15 16:35)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/focus-scope.tsx` `handleClick` — **Double `spatial_focus` invoke on every click.** Removed the direct `invoke(\"spatial_focus\", ...)` from `handleClick`; `setFocus` already handles it via `useFocusSetter`.\n-- [x] `swissarmyhammer-commands/src/spatial_state.rs` `focus()` — **Allows focusing an unregistered key.** Added `entries.contains_key(key)` guard; returns `None` for unregistered keys. Added test `focus_unregistered_key_is_noop`.\n-- [x] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — **`setFocus(null)` does not clear Rust `focused_key`.** Added `clear_focus()` method to `SpatialState`, `spatial_clear_focus` Tauri command, and `invoke(\"spatial_clear_focus\")` call in `useFocusSetter` when moniker is null. Added tests `clear_focus_emits_event` and `clear_focus_when_unfocused_is_noop`.\n+- [ ] `kanban-app/ui/src/components/focus-scope.tsx` `handleClick` — **Double `spatial_focus` invoke on every click.** `handleClick` calls `setFocus(moniker)` which internally (in `useFocusSetter`) looks up the moniker's spatial key and invokes `spatial_focus`, then also directly calls `invoke(\"spatial_focus\", { key: spatialKey })`. Every click fires two `spatial_focus` IPCs for the same key. Remove the direct `invoke(\"spatial_focus\", ...)` call from `handleClick` since `setFocus` already handles it.\n+- [ ] `swissarmyhammer-commands/src/spatial_state.rs` `focus()` — **Allows focusing an unregistered key.** `focus()` does not check whether `key` exists in `entries` before setting `focused_key`. Add an `entries.contains_key(key)` guard, returning `None` when the key is not registered.\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — **`setFocus(null)` does not clear Rust `focused_key`.** When `moniker` is null, the `if (moniker)` guard skips the spatial invoke entirely. Rust's `focused_key` remains set while React shows no focus. Add a `spatial_clear_focus` path (or invoke `spatial_focus` with a sentinel) when moniker is null.\n\n ### Nits\n-- [x] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — `console.warn` fires on every focus change. Gated behind `import.meta.env.DEV`.\n-- [x] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` and `focus-scope.test.tsx` — The `ulid` mock produces strings shorter than real ULIDs. Fixed to use incrementing counter with `padStart(20, \"0\")` for consistent 26-char output.\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.tsx` `useFocusSetter` — `console.warn` fires on every focus change. Gate behind `import.meta.env.DEV` or remove.\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` and `focus-scope.test.tsx` — The `ulid` mock produces strings shorter than real ULIDs (~17 chars vs 26). Consider a fixed-format mock with an incrementing counter for more realistic uniqueness.\n\\ No newline at end of file\n"}]]}
{"id":"01KP90YPFP8N1A5C880PA3AHA1","timestamp":"2026-04-15T16:52:52.854430Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Focus claim registry: key-based, event-driven, Rust owns state'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Focus claim registry: key-based, event-driven, Rust owns state'\n ---\n","transaction_id":null}
{"id":"01KP90YPFPBS8X0D95XXQ6CPNQ","timestamp":"2026-04-15T16:52:52.854839Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KP915HWPQF20A7C9JVT2GAB8","timestamp":"2026-04-15T16:56:37.526215Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffd780\n project: spatial-nav\n title: 'Focus claim registry: key-based, event-driven, Rust owns state'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffd780\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Focus claim registry: key-based, event-driven, Rust owns state'\n ---\n","transaction_id":null}
{"id":"01KP915HWPY67AE78F4CJ3P9P7","timestamp":"2026-04-15T16:56:37.526650Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+ffffffffffffffffffffffd780\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffd780\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}

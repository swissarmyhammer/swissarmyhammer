{"id":"01KNM3YHHG2B804X27N5T90XVB","timestamp":"2026-04-07T14:01:10.448825Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"create","changes":[["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\nMove the `focusedMoniker` (the currently focused entity moniker, e.g. `task:01ABC`) from React-only `useState` in `EntityFocusProvider` to Rust `UIState`, making it the backend source of truth. This enables Rust-side commands to know what's focused and allows focus navigation logic to be tested without a frontend.\n\n### Current state — the gap\n\n- `EntityFocusProvider` (`kanban-app/ui/src/lib/entity-focus-context.tsx:79`) owns `focusedMoniker` as `useState<string | null>(null)`.\n- On every focus change, `setFocus` builds the scope chain from the moniker + React-side registry, then dispatches `ui.setFocus` which stores the chain in `UIState.scope_chain` (`swissarmyhammer-commands/src/ui_state.rs:150`).\n- **Problem**: Rust has `scope_chain` but not `focusedMoniker`. The focused moniker is `scope_chain[0]`, but this is a fragile convention — and no Rust code currently reads it. The frontend is the only source of truth for what's focused.\n- This means: any Rust command that wants to act on the focused entity (e.g. `group.toggleCollapse` needs to know which group is focused) must receive the target via args from the frontend. Navigation commands can't be tested from Rust.\n\n### Approach\n\n**Rust side** (`swissarmyhammer-commands/src/ui_state.rs`):\n- Add `focused_moniker: Option<String>` to `UIStateInner` (transient, `#[serde(skip)]`).\n- Update `set_scope_chain` to also set `focused_moniker` from `scope_chain.first()`. Or better: rename/extend `ui.setFocus` to accept both `focused_moniker` and `scope_chain`, setting both atomically.\n- Add `fn focused_moniker(&self) -> Option<String>` reader.\n- Include `focused_moniker` in the `UIStateChange::ScopeChain` variant (or add a new variant).\n- Include `focused_moniker` in the `debug_snapshot` output.\n\n**Frontend side** (`kanban-app/ui/src/lib/entity-focus-context.tsx`):\n- The `setFocus` callback already dispatches `ui.setFocus` with the scope chain. Extend it to also send `focused_moniker` in the args.\n- Continue to keep the React `useState<string | null>` as a local cache for rendering performance — don't make every focus change wait for a Rust roundtrip. The Rust side is the source of truth for commands; React keeps a local mirror for responsive UI.\n- On `ui-state-changed` events, optionally sync the local cache from the backend snapshot (for cross-window focus scenarios).\n\n### Files to modify\n- `swissarmyhammer-commands/src/ui_state.rs` — add `focused_moniker` field, update `set_scope_chain` or add `set_focus`, add reader, update snapshot\n- `swissarmyhammer-kanban/src/commands/ui_commands.rs` — update `SetFocusCmd` to accept and store `focused_moniker` arg\n- `kanban-app/ui/src/lib/entity-focus-context.tsx` — send `focused_moniker` in `ui.setFocus` dispatch args\n\n### What this unblocks\n- Rust-side commands can read `ui_state.focused_moniker()` to know what's focused, without requiring the frontend to pass it as an arg\n- Future: navigation commands (`nav.up`/`nav.down`) could be partially tested from Rust by asserting focus changes\n- The `group.toggleCollapse` command can derive which group to toggle from the focused moniker rather than requiring a `group_value` arg\n\n## Acceptance Criteria\n- [ ] `UIStateInner` has a `focused_moniker: Option<String>` field (transient, `#[serde(skip)]`)\n- [ ] `ui.setFocus` command stores both `focused_moniker` and `scope_chain` in UIState\n- [ ] `UIState::focused_moniker()` returns the currently focused moniker\n- [ ] `debug_snapshot` includes `focused_moniker`\n- [ ] Frontend sends `focused_moniker` alongside `scope_chain` in the `ui.setFocus` dispatch\n- [ ] Existing focus navigation behavior is unchanged (no regressions)\n\n## Tests\n- [ ] `swissarmyhammer-commands/src/ui_state.rs` — Unit test: `set_scope_chain` (or new `set_focus`) stores focused_moniker, `focused_moniker()` reads it back, clearing scope chain clears focused_moniker\n- [ ] `swissarmyhammer-kanban/src/commands/mod.rs` — Update `SetFocusCmd` test: verify focused_moniker is stored when args include it\n- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` — Verify `ui.setFocus` dispatch includes focused_moniker in args\n- [ ] Run `cargo test -p swissarmyhammer-commands` and `cargo test -p swissarmyhammer-kanban` — all pass\n\n## Workflow\n- Use `/tdd` — write failing tests first, then implement to make them pass."}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"9980"}],["title",{"kind":"set","value":"Move focusedMoniker from React state to Rust UIState for backend-testable focus management"}]],"transaction_id":"01KNM3YE40NQVZA7XS1CZVCGTQ"}
{"id":"01KNM77QCHAY1ZHT16AMT0FA0X","timestamp":"2026-04-07T14:58:37.073426Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,51 +1,90 @@\n ## What\n\n-Move the `focusedMoniker` (the currently focused entity moniker, e.g. `task:01ABC`) from React-only `useState` in `EntityFocusProvider` to Rust `UIState`, making it the backend source of truth. This enables Rust-side commands to know what's focused and allows focus navigation logic to be tested without a frontend.\n+Move the `focusedMoniker` (the currently focused entity moniker, e.g. `task:01ABC`) from React `useState` in `EntityFocusProvider` to a **ref + pub/sub** pattern, and store it in Rust `UIState` for backend-testable focus management. This eliminates the current O(all-FocusScopes) re-render on every focus change, replacing it with exactly 2 re-renders (the scope losing focus + the scope gaining focus).\n\n-### Current state — the gap\n+### Current state — the problem\n\n - `EntityFocusProvider` (`kanban-app/ui/src/lib/entity-focus-context.tsx:79`) owns `focusedMoniker` as `useState<string | null>(null)`.\n-- On every focus change, `setFocus` builds the scope chain from the moniker + React-side registry, then dispatches `ui.setFocus` which stores the chain in `UIState.scope_chain` (`swissarmyhammer-commands/src/ui_state.rs:150`).\n-- **Problem**: Rust has `scope_chain` but not `focusedMoniker`. The focused moniker is `scope_chain[0]`, but this is a fragile convention — and no Rust code currently reads it. The frontend is the only source of truth for what's focused.\n-- This means: any Rust command that wants to act on the focused entity (e.g. `group.toggleCollapse` needs to know which group is focused) must receive the target via args from the frontend. Navigation commands can't be tested from Rust.\n+- Every component consuming `useEntityFocus()` re-renders when focus changes — that's every mounted `FocusScope` (dozens on a board).\n+- On every focus change, `setFocus` builds the scope chain and dispatches `ui.setFocus` which stores the chain in `UIState.scope_chain` (`swissarmyhammer-commands/src/ui_state.rs:150`), but the actual focused moniker is not stored in Rust.\n+- Rust commands can't know what's focused without the frontend passing it as an arg.\n\n-### Approach\n+### Architecture — ref + pub/sub (no context re-renders)\n\n-**Rust side** (`swissarmyhammer-commands/src/ui_state.rs`):\n+**EntityFocusProvider** stores focus in a **ref**, not state. It exposes `subscribe`, `getFocused`, and `setFocus` via context. Changing focus never triggers a React context re-render.\n+\n+```\n+setFocus(\"task:02\")\n+  → focusRef.current = \"task:02\"\n+  → notify all subscribers: (prev=\"task:01\", next=\"task:02\")\n+      → FocusScope moniker=\"task:01\": prev===me → setIsFocused(false) → re-render\n+      → FocusScope moniker=\"task:02\": next===me → setIsFocused(true) → re-render\n+      → FocusScope moniker=\"task:03\": neither → no re-render\n+      → FocusScope moniker=\"column:todo\": neither → no re-render\n+      → ... (all other scopes: no re-render)\n+  → dispatch(\"ui.setFocus\", {focused_moniker, scope_chain}) → Rust stores both\n+```\n+\n+Exactly 2 FocusScopes re-render per focus change, regardless of board size.\n+\n+### Rust side\n+\n+**`swissarmyhammer-commands/src/ui_state.rs`**:\n - Add `focused_moniker: Option<String>` to `UIStateInner` (transient, `#[serde(skip)]`).\n-- Update `set_scope_chain` to also set `focused_moniker` from `scope_chain.first()`. Or better: rename/extend `ui.setFocus` to accept both `focused_moniker` and `scope_chain`, setting both atomically.\n+- Update `set_scope_chain` to also accept and store `focused_moniker`. Or add a new `set_focus(moniker, scope_chain)` method that sets both atomically.\n - Add `fn focused_moniker(&self) -> Option<String>` reader.\n-- Include `focused_moniker` in the `UIStateChange::ScopeChain` variant (or add a new variant).\n-- Include `focused_moniker` in the `debug_snapshot` output.\n+- Include `focused_moniker` in `debug_snapshot` output.\n\n-**Frontend side** (`kanban-app/ui/src/lib/entity-focus-context.tsx`):\n-- The `setFocus` callback already dispatches `ui.setFocus` with the scope chain. Extend it to also send `focused_moniker` in the args.\n-- Continue to keep the React `useState<string | null>` as a local cache for rendering performance — don't make every focus change wait for a Rust roundtrip. The Rust side is the source of truth for commands; React keeps a local mirror for responsive UI.\n-- On `ui-state-changed` events, optionally sync the local cache from the backend snapshot (for cross-window focus scenarios).\n+**`swissarmyhammer-kanban/src/commands/ui_commands.rs`**:\n+- Update `SetFocusCmd::execute` to read `focused_moniker` from args and pass it to UIState.\n\n-### Files to modify\n-- `swissarmyhammer-commands/src/ui_state.rs` — add `focused_moniker` field, update `set_scope_chain` or add `set_focus`, add reader, update snapshot\n-- `swissarmyhammer-kanban/src/commands/ui_commands.rs` — update `SetFocusCmd` to accept and store `focused_moniker` arg\n-- `kanban-app/ui/src/lib/entity-focus-context.tsx` — send `focused_moniker` in `ui.setFocus` dispatch args\n+### Frontend side\n\n-### What this unblocks\n-- Rust-side commands can read `ui_state.focused_moniker()` to know what's focused, without requiring the frontend to pass it as an arg\n-- Future: navigation commands (`nav.up`/`nav.down`) could be partially tested from Rust by asserting focus changes\n-- The `group.toggleCollapse` command can derive which group to toggle from the focused moniker rather than requiring a `group_value` arg\n+**`kanban-app/ui/src/lib/entity-focus-context.tsx`** — the main refactor:\n+\n+1. Replace `useState<string | null>(null)` with `useRef<string | null>(null)` for `focusedMoniker`.\n+2. Add a `listenersRef = useRef(new Set<(prev, next) => void>())` for the subscriber set.\n+3. `setFocus(moniker)` reads prev from ref, writes next to ref, notifies all listeners with `(prev, next)`, dispatches `ui.setFocus` with `{focused_moniker, scope_chain}` to Rust. No `setState`.\n+4. Context value exposes `subscribe(cb)`, `getFocused()`, and `setFocus(moniker)`. The context object itself is stable (memoized once) — it never changes, so consumers never re-render from context changes.\n+5. `broadcastNavCommand` reads from `focusRef.current` (already does this via `focusedMonikerRef`).\n+\n+**`kanban-app/ui/src/components/focus-scope.tsx`**:\n+\n+1. Each FocusScope subscribes via `useEffect(() => subscribe((prev, next) => { if (prev === moniker || next === moniker) setIsFocused(next === moniker); }), [moniker])`.\n+2. Replace `const isDirectFocus = focusedMoniker === moniker` (which reads from context state) with local `const [isFocused, setIsFocused] = useState(false)` driven by the subscription.\n+3. On mount, sync: `setIsFocused(getFocused() === moniker)`.\n+\n+**`kanban-app/ui/src/lib/entity-focus-context.tsx` — `useIsFocused(moniker)`**:\n+\n+1. Same subscription pattern: `useState(false)` + subscribe + check `prev === moniker || next === moniker`.\n+2. Only re-renders when the boolean flips for this specific moniker.\n+\n+**`kanban-app/ui/src/lib/entity-focus-context.tsx` — `FocusedScopeContext`**:\n+\n+1. Currently derived from `focusedMoniker` state on every render. With the ref approach, update it inside the `setFocus` function by looking up the registry. Since `FocusedScopeContext` is used by `useDispatchCommand`, it needs to be a context value — but it can be updated via `setState` only when the focused scope actually changes (which is every focus change). This is acceptable because `useDispatchCommand` consumers don't re-render from it — they read it lazily during dispatch.\n+\n+### Files to modify\n+- `swissarmyhammer-commands/src/ui_state.rs` — add `focused_moniker` field, reader, update `set_scope_chain`\n+- `swissarmyhammer-kanban/src/commands/ui_commands.rs` — update `SetFocusCmd` to store `focused_moniker`\n+- `kanban-app/ui/src/lib/entity-focus-context.tsx` — ref + pub/sub refactor, update `useIsFocused`, update context shape\n+- `kanban-app/ui/src/components/focus-scope.tsx` — subscribe to focus changes, local `isFocused` state\n\n ## Acceptance Criteria\n-- [ ] `UIStateInner` has a `focused_moniker: Option<String>` field (transient, `#[serde(skip)]`)\n+- [ ] `EntityFocusProvider` stores focus in a ref, not React state — changing focus triggers zero context re-renders\n+- [ ] Each `FocusScope` subscribes to focus changes and only re-renders when it gains or loses focus (exactly 2 re-renders per focus change)\n+- [ ] `useIsFocused(moniker)` only re-renders when the boolean result flips for that specific moniker\n+- [ ] `UIStateInner` has `focused_moniker: Option<String>` (transient, `#[serde(skip)]`)\n - [ ] `ui.setFocus` command stores both `focused_moniker` and `scope_chain` in UIState\n - [ ] `UIState::focused_moniker()` returns the currently focused moniker\n-- [ ] `debug_snapshot` includes `focused_moniker`\n-- [ ] Frontend sends `focused_moniker` alongside `scope_chain` in the `ui.setFocus` dispatch\n-- [ ] Existing focus navigation behavior is unchanged (no regressions)\n+- [ ] Existing keyboard navigation works identically (no regressions in claim predicates, nav commands, or visual focus indicators)\n\n ## Tests\n-- [ ] `swissarmyhammer-commands/src/ui_state.rs` — Unit test: `set_scope_chain` (or new `set_focus`) stores focused_moniker, `focused_moniker()` reads it back, clearing scope chain clears focused_moniker\n-- [ ] `swissarmyhammer-kanban/src/commands/mod.rs` — Update `SetFocusCmd` test: verify focused_moniker is stored when args include it\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` — Verify `ui.setFocus` dispatch includes focused_moniker in args\n+- [ ] `swissarmyhammer-commands/src/ui_state.rs` — Unit test: set_focus stores focused_moniker, focused_moniker() reads it back, clearing focus clears both moniker and scope_chain\n+- [ ] `swissarmyhammer-kanban/src/commands/mod.rs` — Update SetFocusCmd test: verify focused_moniker stored when args include it\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` — Test: subscriber receives (prev, next) on focus change; subscriber with moniker matching neither prev nor next does not trigger setState; getFocused() returns current value synchronously\n+- [ ] `kanban-app/ui/src/components/focus-scope.test.tsx` (new or existing) — Test: FocusScope re-renders only when it gains/loses focus, not when unrelated focus changes occur\n - [ ] Run `cargo test -p swissarmyhammer-commands` and `cargo test -p swissarmyhammer-kanban` — all pass\n+- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,90 +1,51 @@\n ## What\n\n-Move the `focusedMoniker` (the currently focused entity moniker, e.g. `task:01ABC`) from React `useState` in `EntityFocusProvider` to a **ref + pub/sub** pattern, and store it in Rust `UIState` for backend-testable focus management. This eliminates the current O(all-FocusScopes) re-render on every focus change, replacing it with exactly 2 re-renders (the scope losing focus + the scope gaining focus).\n+Move the `focusedMoniker` (the currently focused entity moniker, e.g. `task:01ABC`) from React-only `useState` in `EntityFocusProvider` to Rust `UIState`, making it the backend source of truth. This enables Rust-side commands to know what's focused and allows focus navigation logic to be tested without a frontend.\n\n-### Current state — the problem\n+### Current state — the gap\n\n - `EntityFocusProvider` (`kanban-app/ui/src/lib/entity-focus-context.tsx:79`) owns `focusedMoniker` as `useState<string | null>(null)`.\n-- Every component consuming `useEntityFocus()` re-renders when focus changes — that's every mounted `FocusScope` (dozens on a board).\n-- On every focus change, `setFocus` builds the scope chain and dispatches `ui.setFocus` which stores the chain in `UIState.scope_chain` (`swissarmyhammer-commands/src/ui_state.rs:150`), but the actual focused moniker is not stored in Rust.\n-- Rust commands can't know what's focused without the frontend passing it as an arg.\n-\n-### Architecture — ref + pub/sub (no context re-renders)\n-\n-**EntityFocusProvider** stores focus in a **ref**, not state. It exposes `subscribe`, `getFocused`, and `setFocus` via context. Changing focus never triggers a React context re-render.\n-\n-```\n-setFocus(\"task:02\")\n-  → focusRef.current = \"task:02\"\n-  → notify all subscribers: (prev=\"task:01\", next=\"task:02\")\n-      → FocusScope moniker=\"task:01\": prev===me → setIsFocused(false) → re-render\n-      → FocusScope moniker=\"task:02\": next===me → setIsFocused(true) → re-render\n-      → FocusScope moniker=\"task:03\": neither → no re-render\n-      → FocusScope moniker=\"column:todo\": neither → no re-render\n-      → ... (all other scopes: no re-render)\n-  → dispatch(\"ui.setFocus\", {focused_moniker, scope_chain}) → Rust stores both\n-```\n-\n-Exactly 2 FocusScopes re-render per focus change, regardless of board size.\n+- On every focus change, `setFocus` builds the scope chain from the moniker + React-side registry, then dispatches `ui.setFocus` which stores the chain in `UIState.scope_chain` (`swissarmyhammer-commands/src/ui_state.rs:150`).\n+- **Problem**: Rust has `scope_chain` but not `focusedMoniker`. The focused moniker is `scope_chain[0]`, but this is a fragile convention — and no Rust code currently reads it. The frontend is the only source of truth for what's focused.\n+- This means: any Rust command that wants to act on the focused entity (e.g. `group.toggleCollapse` needs to know which group is focused) must receive the target via args from the frontend. Navigation commands can't be tested from Rust.\n\n-### Rust side\n+### Approach\n\n-**`swissarmyhammer-commands/src/ui_state.rs`**:\n+**Rust side** (`swissarmyhammer-commands/src/ui_state.rs`):\n - Add `focused_moniker: Option<String>` to `UIStateInner` (transient, `#[serde(skip)]`).\n-- Update `set_scope_chain` to also accept and store `focused_moniker`. Or add a new `set_focus(moniker, scope_chain)` method that sets both atomically.\n+- Update `set_scope_chain` to also set `focused_moniker` from `scope_chain.first()`. Or better: rename/extend `ui.setFocus` to accept both `focused_moniker` and `scope_chain`, setting both atomically.\n - Add `fn focused_moniker(&self) -> Option<String>` reader.\n-- Include `focused_moniker` in `debug_snapshot` output.\n-\n-**`swissarmyhammer-kanban/src/commands/ui_commands.rs`**:\n-- Update `SetFocusCmd::execute` to read `focused_moniker` from args and pass it to UIState.\n-\n-### Frontend side\n-\n-**`kanban-app/ui/src/lib/entity-focus-context.tsx`** — the main refactor:\n-\n-1. Replace `useState<string | null>(null)` with `useRef<string | null>(null)` for `focusedMoniker`.\n-2. Add a `listenersRef = useRef(new Set<(prev, next) => void>())` for the subscriber set.\n-3. `setFocus(moniker)` reads prev from ref, writes next to ref, notifies all listeners with `(prev, next)`, dispatches `ui.setFocus` with `{focused_moniker, scope_chain}` to Rust. No `setState`.\n-4. Context value exposes `subscribe(cb)`, `getFocused()`, and `setFocus(moniker)`. The context object itself is stable (memoized once) — it never changes, so consumers never re-render from context changes.\n-5. `broadcastNavCommand` reads from `focusRef.current` (already does this via `focusedMonikerRef`).\n-\n-**`kanban-app/ui/src/components/focus-scope.tsx`**:\n+- Include `focused_moniker` in the `UIStateChange::ScopeChain` variant (or add a new variant).\n+- Include `focused_moniker` in the `debug_snapshot` output.\n\n-1. Each FocusScope subscribes via `useEffect(() => subscribe((prev, next) => { if (prev === moniker || next === moniker) setIsFocused(next === moniker); }), [moniker])`.\n-2. Replace `const isDirectFocus = focusedMoniker === moniker` (which reads from context state) with local `const [isFocused, setIsFocused] = useState(false)` driven by the subscription.\n-3. On mount, sync: `setIsFocused(getFocused() === moniker)`.\n+**Frontend side** (`kanban-app/ui/src/lib/entity-focus-context.tsx`):\n+- The `setFocus` callback already dispatches `ui.setFocus` with the scope chain. Extend it to also send `focused_moniker` in the args.\n+- Continue to keep the React `useState<string | null>` as a local cache for rendering performance — don't make every focus change wait for a Rust roundtrip. The Rust side is the source of truth for commands; React keeps a local mirror for responsive UI.\n+- On `ui-state-changed` events, optionally sync the local cache from the backend snapshot (for cross-window focus scenarios).\n\n-**`kanban-app/ui/src/lib/entity-focus-context.tsx` — `useIsFocused(moniker)`**:\n+### Files to modify\n+- `swissarmyhammer-commands/src/ui_state.rs` — add `focused_moniker` field, update `set_scope_chain` or add `set_focus`, add reader, update snapshot\n+- `swissarmyhammer-kanban/src/commands/ui_commands.rs` — update `SetFocusCmd` to accept and store `focused_moniker` arg\n+- `kanban-app/ui/src/lib/entity-focus-context.tsx` — send `focused_moniker` in `ui.setFocus` dispatch args\n\n-1. Same subscription pattern: `useState(false)` + subscribe + check `prev === moniker || next === moniker`.\n-2. Only re-renders when the boolean flips for this specific moniker.\n-\n-**`kanban-app/ui/src/lib/entity-focus-context.tsx` — `FocusedScopeContext`**:\n-\n-1. Currently derived from `focusedMoniker` state on every render. With the ref approach, update it inside the `setFocus` function by looking up the registry. Since `FocusedScopeContext` is used by `useDispatchCommand`, it needs to be a context value — but it can be updated via `setState` only when the focused scope actually changes (which is every focus change). This is acceptable because `useDispatchCommand` consumers don't re-render from it — they read it lazily during dispatch.\n-\n-### Files to modify\n-- `swissarmyhammer-commands/src/ui_state.rs` — add `focused_moniker` field, reader, update `set_scope_chain`\n-- `swissarmyhammer-kanban/src/commands/ui_commands.rs` — update `SetFocusCmd` to store `focused_moniker`\n-- `kanban-app/ui/src/lib/entity-focus-context.tsx` — ref + pub/sub refactor, update `useIsFocused`, update context shape\n-- `kanban-app/ui/src/components/focus-scope.tsx` — subscribe to focus changes, local `isFocused` state\n+### What this unblocks\n+- Rust-side commands can read `ui_state.focused_moniker()` to know what's focused, without requiring the frontend to pass it as an arg\n+- Future: navigation commands (`nav.up`/`nav.down`) could be partially tested from Rust by asserting focus changes\n+- The `group.toggleCollapse` command can derive which group to toggle from the focused moniker rather than requiring a `group_value` arg\n\n ## Acceptance Criteria\n-- [ ] `EntityFocusProvider` stores focus in a ref, not React state — changing focus triggers zero context re-renders\n-- [ ] Each `FocusScope` subscribes to focus changes and only re-renders when it gains or loses focus (exactly 2 re-renders per focus change)\n-- [ ] `useIsFocused(moniker)` only re-renders when the boolean result flips for that specific moniker\n-- [ ] `UIStateInner` has `focused_moniker: Option<String>` (transient, `#[serde(skip)]`)\n+- [ ] `UIStateInner` has a `focused_moniker: Option<String>` field (transient, `#[serde(skip)]`)\n - [ ] `ui.setFocus` command stores both `focused_moniker` and `scope_chain` in UIState\n - [ ] `UIState::focused_moniker()` returns the currently focused moniker\n-- [ ] Existing keyboard navigation works identically (no regressions in claim predicates, nav commands, or visual focus indicators)\n+- [ ] `debug_snapshot` includes `focused_moniker`\n+- [ ] Frontend sends `focused_moniker` alongside `scope_chain` in the `ui.setFocus` dispatch\n+- [ ] Existing focus navigation behavior is unchanged (no regressions)\n\n ## Tests\n-- [ ] `swissarmyhammer-commands/src/ui_state.rs` — Unit test: set_focus stores focused_moniker, focused_moniker() reads it back, clearing focus clears both moniker and scope_chain\n-- [ ] `swissarmyhammer-kanban/src/commands/mod.rs` — Update SetFocusCmd test: verify focused_moniker stored when args include it\n-- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` — Test: subscriber receives (prev, next) on focus change; subscriber with moniker matching neither prev nor next does not trigger setState; getFocused() returns current value synchronously\n-- [ ] `kanban-app/ui/src/components/focus-scope.test.tsx` (new or existing) — Test: FocusScope re-renders only when it gains/loses focus, not when unrelated focus changes occur\n+- [ ] `swissarmyhammer-commands/src/ui_state.rs` — Unit test: `set_scope_chain` (or new `set_focus`) stores focused_moniker, `focused_moniker()` reads it back, clearing scope chain clears focused_moniker\n+- [ ] `swissarmyhammer-kanban/src/commands/mod.rs` — Update `SetFocusCmd` test: verify focused_moniker is stored when args include it\n+- [ ] `kanban-app/ui/src/lib/entity-focus-context.test.tsx` — Verify `ui.setFocus` dispatch includes focused_moniker in args\n - [ ] Run `cargo test -p swissarmyhammer-commands` and `cargo test -p swissarmyhammer-kanban` — all pass\n-- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]],"transaction_id":"01KNM77QC1ZRX9A89A8SWVCGSD"}
{"id":"01KPZPK2KJ9B9R4NHKABAZQ94S","timestamp":"2026-04-24T12:14:18.226928Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -4,31 +4,58 @@\n position_column: todo\n position_ordinal: '9980'\n project: spatial-nav\n-title: 'Focus claim registry: key-based, event-driven, Rust owns state'\n+title: 'Focus claim registry: per-window, event-driven, Rust owns state'\n ---\n ## What\n\n-Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by spatial key (ULID). Rust owns all focus state. React is a dumb renderer that responds to events.\n+Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by spatial key. Rust owns all focus state — and focus is tracked **per window**, because the app is multi-window (see `UIState.windows: HashMap<String, WindowState>` and the per-window `inspector_stack`).\n\n ### Core principle\n\n-- **Rust owns focus state.** `focused_key: Option<String>` lives in Rust. All focus changes go through Rust.\n-- **React sends commands, Rust emits events.** No return values. Every Tauri invoke returns Ok/Err. Focus changes are communicated via a `\"focus-changed\"` event.\n-- **Focus identity = key (ULID).** Not moniker. The claim registry is `Map<key, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n+- **Rust owns focus state.** React is a dumb renderer that responds to events.\n+- **Focus is per window.** Each Tauri window has its own focused element. OS window focus picks which one is \"the\" focus at any moment.\n+- **Focus identity = SpatialKey (ULID).** Not moniker. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n+\n+### Newtypes (Rust)\n+\n+Introduce three newtypes in a new module `swissarmyhammer-commands/src/spatial/keys.rs` (or inline in `spatial.rs`):\n+\n+```rust\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)]\n+pub struct WindowLabel(pub String);\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)]\n+pub struct SpatialKey(pub String);\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)]\n+pub struct LayerKey(pub String);\n+```\n+\n+`#[serde(transparent)]` means wire format is still a plain string — React sends/receives strings unchanged. The existing `String` window labels in `UIState` are left alone for now; new spatial code uses `WindowLabel`.\n\n ### API surface\n\n ```\n-React → Rust (all return Ok/Err, nothing else):\n-  spatial_register(key, moniker, rect, layer_key)   — FocusScope mount/resize\n-  spatial_unregister(key)                            — FocusScope unmount\n-  spatial_navigate(key, direction)                   — keyboard nav (from key, in direction)\n-  spatial_focus(key)                                 — click / programmatic\n-  spatial_push_layer(key, name)                      — FocusLayer mount\n-  spatial_pop_layer(key)                             — FocusLayer unmount\n+React → Rust (all return Ok/Err):\n+  spatial_register(key: SpatialKey, moniker: String, rect: Rect, layer_key: LayerKey)\n+  spatial_unregister(key: SpatialKey)\n+  spatial_navigate(key: SpatialKey, direction: Direction)\n+  spatial_focus(key: SpatialKey)\n+  spatial_push_layer(key: LayerKey, name: String, parent_key: Option<LayerKey>)\n+  spatial_pop_layer(key: LayerKey)\n\n-Rust → React (Tauri event):\n-  \"focus-changed\" { prev_key: Option<String>, next_key: Option<String> }\n+Tauri commands derive window_label from the invoking webview (tauri::Window parameter), not\n+from React — React never has to pass it.\n+\n+Rust → React (event):\n+  \"focus-changed\" {\n+    window_label: WindowLabel,\n+    prev_key: Option<SpatialKey>,\n+    next_key: Option<SpatialKey>,\n+  }\n ```\n\n ### React side\n@@ -36,14 +63,17 @@\n **Global event listener** (in EntityFocusProvider):\n\n ```typescript\n-listen(\"focus-changed\", ({ prev_key, next_key }) => {\n+listen(\"focus-changed\", ({ payload }) => {\n+  const { prev_key, next_key } = payload; // window_label is for filtering if needed\n   if (prev_key) claimRegistry.get(prev_key)?.(false);\n   if (next_key) claimRegistry.get(next_key)?.(true);\n });\n ```\n\n+Since each Tauri window has its own React tree and its own claim registry, the registry will only contain keys from that window. A `focus-changed` event for another window's key is a no-op in this window's registry.\n+\n **FocusScope**:\n-- Generates ULID key: `const key = useRef(ulid()).current`\n+- Generates SpatialKey (ULID): `const key = useRef(ulid()).current`\n - Registers in claim registry: `registry.set(key, setIsFocused)`\n - On click: `invoke(\"spatial_focus\", { key })` — fire and forget\n - Local `const [isFocused, setIsFocused] = useState(false)` driven by claim callback\n@@ -50,36 +80,37 @@\n\n **No focusedMoniker state in React.** React doesn't track who's focused — it just responds to events from Rust.\n\n-### Moniker is separate\n-\n-When a FocusScope receives `claim(true)`, it knows its own moniker. It can separately dispatch `ui.setFocus` with moniker + scope chain for command dispatch purposes. But this is the command system's concern, not the focus system's.\n-\n ### Rust side\n\n-**`SpatialState`** (or extend UIState):\n-- `focused_key: Option<String>`\n-- On `spatial_navigate(key, direction)`: resolve → update `focused_key` → emit event\n-- On `spatial_focus(key)`: update `focused_key` → emit event\n-- On `spatial_unregister(key)`: if key == focused_key, clear focus → emit event\n+**`SpatialState`**:\n+- `focus_by_window: HashMap<WindowLabel, SpatialKey>` — one focused key per window\n+- `register_entry(key, moniker, rect, layer_key, window_label)` stores which window each entry belongs to (derived via the entry's layer's window_label — see card 2)\n+- On `spatial_focus(window_label, key)`: update `focus_by_window[window_label]` → emit event with `window_label`\n+- On `spatial_navigate(window_label, key, direction)`: resolve → update → emit event with `window_label`\n+- On `spatial_unregister(key)`: if key is the focused key for any window, clear that window's focus → emit event\n\n ### Files to modify\n-- `swissarmyhammer-commands/src/ui_state.rs` or new `spatial_state.rs` — `focused_key`, event emission\n+- `swissarmyhammer-commands/src/spatial.rs` (new) or `spatial/mod.rs` — newtypes, `SpatialState`\n - `swissarmyhammer-kanban/src/commands/` — Tauri commands for spatial_*\n - `kanban-app/ui/src/lib/entity-focus-context.tsx` — claim registry, global event listener, remove useState\n - `kanban-app/ui/src/components/focus-scope.tsx` — key generation, claim registration, fire-and-forget invokes\n\n ### Subtasks\n-- [ ] Add claim registry `Map<string, (focused: boolean) => void>` to EntityFocusProvider\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n+- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to spatial state\n+- [ ] Emit `focus-changed` events carrying `window_label`, `prev_key`, `next_key`\n+- [ ] Tauri commands read `window_label` from the `tauri::Window` parameter (not from React args)\n+- [ ] Add claim registry `Map<SpatialKey, (focused: boolean) => void>` to EntityFocusProvider\n - [ ] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n - [ ] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n-- [ ] Add `focused_key` to Rust state with event emission on change\n - [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n\n ## Acceptance Criteria\n-- [ ] Focus change triggers exactly 2 FocusScope re-renders via event callback\n+- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes, not raw strings, in all new spatial code\n+- [ ] Focus is tracked per window — `focus_by_window[window_A]` and `focus_by_window[window_B]` are independent\n+- [ ] Focus change in window A does not re-render FocusScopes in window B (each window's registry only has its own keys)\n - [ ] All Tauri invokes return Ok/Err only — no focus data in return values\n - [ ] Focus changes flow through Rust event: click → Rust → event → React claim\n-- [ ] Keyboard nav: React invokes `spatial_navigate(key, direction)` → Rust emits event → React claims\n - [ ] No `focusedMoniker` state in React — Rust owns focus state\n - [ ] Event listener cleaned up on EntityFocusProvider unmount\n - [ ] `cargo test` passes, `pnpm vitest run` passes\n@@ -86,66 +117,53 @@\n\n ## Tests\n\n-### Rust unit tests (`swissarmyhammer-commands/src/spatial_state.rs` or similar)\n+### Rust unit tests\n\n ```rust\n #[test]\n-fn spatial_focus_updates_focused_key_and_emits_event() {\n+fn spatial_focus_updates_per_window_state_and_emits_event() {\n     // Given: empty state\n-    // When: spatial_focus(\"key-abc\")\n-    // Then: state.focused_key == Some(\"key-abc\")\n-    // And: emitted event == FocusChanged { prev_key: None, next_key: Some(\"key-abc\") }\n+    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k1\"))\n+    // Then: state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n+    // And: event == FocusChanged { window_label: \"main\", prev_key: None, next_key: Some(\"k1\") }\n }\n\n #[test]\n-fn spatial_focus_emits_prev_and_next() {\n-    // Given: focused_key == Some(\"key-1\")\n-    // When: spatial_focus(\"key-2\")\n-    // Then: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: Some(\"key-2\") }\n+fn focus_in_window_a_does_not_affect_window_b() {\n+    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }\n+    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k3\"))\n+    // Then: focus_by_window == { \"main\" => \"k3\", \"popup\" => \"k2\" }\n+    // And: event window_label == \"main\" (not \"popup\")\n }\n\n #[test]\n-fn spatial_unregister_focused_key_clears_focus() {\n-    // Given: focused_key == Some(\"key-1\"), key-1 is registered\n-    // When: spatial_unregister(\"key-1\")\n-    // Then: focused_key == None\n-    // And: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: None }\n+fn spatial_unregister_focused_key_clears_that_window_only() {\n+    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }, k1 is registered\n+    // When: spatial_unregister(SpatialKey(\"k1\"))\n+    // Then: focus_by_window == { \"popup\" => \"k2\" }\n+    // And: event == FocusChanged { window_label: \"main\", prev_key: Some(\"k1\"), next_key: None }\n }\n\n #[test]\n fn spatial_unregister_non_focused_key_no_event() {\n-    // Given: focused_key == Some(\"key-1\"), key-2 is registered\n-    // When: spatial_unregister(\"key-2\")\n-    // Then: focused_key still Some(\"key-1\"), no event emitted\n+    // Given: focus_by_window[\"main\"] == \"k1\", k2 is registered\n+    // When: spatial_unregister(SpatialKey(\"k2\"))\n+    // Then: focus unchanged, no event emitted\n }\n-```\n-\n-### React unit tests (`kanban-app/ui/src/lib/entity-focus-context.test.tsx`)\n\n-Mock setup:\n-```typescript\n-vi.mock(\"@tauri-apps/api/core\", () => ({ invoke: vi.fn(() => Promise.resolve()) }));\n-vi.mock(\"@tauri-apps/api/event\", () => ({\n-  listen: vi.fn((_event, callback) => {\n-    // Store callback so tests can fire events manually\n-    (listen as any).__callback = callback;\n-    return Promise.resolve(() => {}); // unsub function\n-  }),\n-}));\n+#[test]\n+fn newtypes_are_serde_transparent() {\n+    // SpatialKey(\"abc\") serializes as JSON string \"abc\", not {\"0\":\"abc\"}\n+}\n ```\n\n-```\n-test: \"claim registry calls previous callback with false and next with true on focus-changed event\"\n-  setup: render EntityFocusProvider with two FocusScopes (key-A, key-B)\n-  act: fire focus-changed event { prev_key: null, next_key: \"key-A\" }\n-  assert: FocusScope A has data-focused attribute, B does not\n-  act: fire focus-changed event { prev_key: \"key-A\", next_key: \"key-B\" }\n-  assert: FocusScope A no longer data-focused, FocusScope B has data-focused\n+### React unit tests (`entity-focus-context.test.tsx`)\n\n-test: \"unregistered key in focus-changed event is a no-op\"\n-  setup: render EntityFocusProvider with one FocusScope (key-A)\n-  act: fire focus-changed event { prev_key: \"nonexistent\", next_key: \"key-A\" }\n-  assert: no error thrown, FocusScope A has data-focused\n+```\n+test: \"claim registry only responds to events for keys it knows about\"\n+  setup: render EntityFocusProvider with FocusScope (key-A)\n+  act: fire focus-changed event { window_label: \"popup\", next_key: \"key-B\" }\n+  assert: FocusScope A is not focused (key-B not in this window's registry)\n\n test: \"FocusScope click invokes spatial_focus with its key\"\n   setup: render FocusScope inside EntityFocusProvider + FocusLayer\n@@ -153,13 +171,7 @@\n   assert: invoke called with (\"spatial_focus\", { key: <the ULID> })\n\n test: \"EntityFocusProvider unmount cleans up event listener\"\n-  setup: render then unmount EntityFocusProvider\n-  assert: the unsub function returned by listen() was called\n-\n test: \"FocusScope unmount removes from claim registry\"\n-  setup: render FocusScope, capture its key\n-  act: unmount FocusScope, then fire focus-changed { next_key: <captured key> }\n-  assert: no error, no state update (callback was unregistered)\n ```\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -4,58 +4,31 @@\n position_column: todo\n position_ordinal: '9980'\n project: spatial-nav\n-title: 'Focus claim registry: per-window, event-driven, Rust owns state'\n+title: 'Focus claim registry: key-based, event-driven, Rust owns state'\n ---\n ## What\n\n-Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by spatial key. Rust owns all focus state — and focus is tracked **per window**, because the app is multi-window (see `UIState.windows: HashMap<String, WindowState>` and the per-window `inspector_stack`).\n+Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by spatial key (ULID). Rust owns all focus state. React is a dumb renderer that responds to events.\n\n ### Core principle\n\n-- **Rust owns focus state.** React is a dumb renderer that responds to events.\n-- **Focus is per window.** Each Tauri window has its own focused element. OS window focus picks which one is \"the\" focus at any moment.\n-- **Focus identity = SpatialKey (ULID).** Not moniker. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n-\n-### Newtypes (Rust)\n-\n-Introduce three newtypes in a new module `swissarmyhammer-commands/src/spatial/keys.rs` (or inline in `spatial.rs`):\n-\n-```rust\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct WindowLabel(pub String);\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct SpatialKey(pub String);\n+- **Rust owns focus state.** `focused_key: Option<String>` lives in Rust. All focus changes go through Rust.\n+- **React sends commands, Rust emits events.** No return values. Every Tauri invoke returns Ok/Err. Focus changes are communicated via a `\"focus-changed\"` event.\n+- **Focus identity = key (ULID).** Not moniker. The claim registry is `Map<key, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct LayerKey(pub String);\n-```\n-\n-`#[serde(transparent)]` means wire format is still a plain string — React sends/receives strings unchanged. The existing `String` window labels in `UIState` are left alone for now; new spatial code uses `WindowLabel`.\n-\n ### API surface\n\n ```\n-React → Rust (all return Ok/Err):\n-  spatial_register(key: SpatialKey, moniker: String, rect: Rect, layer_key: LayerKey)\n-  spatial_unregister(key: SpatialKey)\n-  spatial_navigate(key: SpatialKey, direction: Direction)\n-  spatial_focus(key: SpatialKey)\n-  spatial_push_layer(key: LayerKey, name: String, parent_key: Option<LayerKey>)\n-  spatial_pop_layer(key: LayerKey)\n-\n-Tauri commands derive window_label from the invoking webview (tauri::Window parameter), not\n-from React — React never has to pass it.\n+React → Rust (all return Ok/Err, nothing else):\n+  spatial_register(key, moniker, rect, layer_key)   — FocusScope mount/resize\n+  spatial_unregister(key)                            — FocusScope unmount\n+  spatial_navigate(key, direction)                   — keyboard nav (from key, in direction)\n+  spatial_focus(key)                                 — click / programmatic\n+  spatial_push_layer(key, name)                      — FocusLayer mount\n+  spatial_pop_layer(key)                             — FocusLayer unmount\n\n-Rust → React (event):\n-  \"focus-changed\" {\n-    window_label: WindowLabel,\n-    prev_key: Option<SpatialKey>,\n-    next_key: Option<SpatialKey>,\n-  }\n+Rust → React (Tauri event):\n+  \"focus-changed\" { prev_key: Option<String>, next_key: Option<String> }\n ```\n\n ### React side\n@@ -63,17 +36,14 @@\n **Global event listener** (in EntityFocusProvider):\n\n ```typescript\n-listen(\"focus-changed\", ({ payload }) => {\n-  const { prev_key, next_key } = payload; // window_label is for filtering if needed\n+listen(\"focus-changed\", ({ prev_key, next_key }) => {\n   if (prev_key) claimRegistry.get(prev_key)?.(false);\n   if (next_key) claimRegistry.get(next_key)?.(true);\n });\n ```\n\n-Since each Tauri window has its own React tree and its own claim registry, the registry will only contain keys from that window. A `focus-changed` event for another window's key is a no-op in this window's registry.\n-\n **FocusScope**:\n-- Generates SpatialKey (ULID): `const key = useRef(ulid()).current`\n+- Generates ULID key: `const key = useRef(ulid()).current`\n - Registers in claim registry: `registry.set(key, setIsFocused)`\n - On click: `invoke(\"spatial_focus\", { key })` — fire and forget\n - Local `const [isFocused, setIsFocused] = useState(false)` driven by claim callback\n@@ -80,37 +50,36 @@\n\n **No focusedMoniker state in React.** React doesn't track who's focused — it just responds to events from Rust.\n\n+### Moniker is separate\n+\n+When a FocusScope receives `claim(true)`, it knows its own moniker. It can separately dispatch `ui.setFocus` with moniker + scope chain for command dispatch purposes. But this is the command system's concern, not the focus system's.\n+\n ### Rust side\n\n-**`SpatialState`**:\n-- `focus_by_window: HashMap<WindowLabel, SpatialKey>` — one focused key per window\n-- `register_entry(key, moniker, rect, layer_key, window_label)` stores which window each entry belongs to (derived via the entry's layer's window_label — see card 2)\n-- On `spatial_focus(window_label, key)`: update `focus_by_window[window_label]` → emit event with `window_label`\n-- On `spatial_navigate(window_label, key, direction)`: resolve → update → emit event with `window_label`\n-- On `spatial_unregister(key)`: if key is the focused key for any window, clear that window's focus → emit event\n+**`SpatialState`** (or extend UIState):\n+- `focused_key: Option<String>`\n+- On `spatial_navigate(key, direction)`: resolve → update `focused_key` → emit event\n+- On `spatial_focus(key)`: update `focused_key` → emit event\n+- On `spatial_unregister(key)`: if key == focused_key, clear focus → emit event\n\n ### Files to modify\n-- `swissarmyhammer-commands/src/spatial.rs` (new) or `spatial/mod.rs` — newtypes, `SpatialState`\n+- `swissarmyhammer-commands/src/ui_state.rs` or new `spatial_state.rs` — `focused_key`, event emission\n - `swissarmyhammer-kanban/src/commands/` — Tauri commands for spatial_*\n - `kanban-app/ui/src/lib/entity-focus-context.tsx` — claim registry, global event listener, remove useState\n - `kanban-app/ui/src/components/focus-scope.tsx` — key generation, claim registration, fire-and-forget invokes\n\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n-- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to spatial state\n-- [ ] Emit `focus-changed` events carrying `window_label`, `prev_key`, `next_key`\n-- [ ] Tauri commands read `window_label` from the `tauri::Window` parameter (not from React args)\n-- [ ] Add claim registry `Map<SpatialKey, (focused: boolean) => void>` to EntityFocusProvider\n+- [ ] Add claim registry `Map<string, (focused: boolean) => void>` to EntityFocusProvider\n - [ ] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n - [ ] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n+- [ ] Add `focused_key` to Rust state with event emission on change\n - [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n\n ## Acceptance Criteria\n-- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes, not raw strings, in all new spatial code\n-- [ ] Focus is tracked per window — `focus_by_window[window_A]` and `focus_by_window[window_B]` are independent\n-- [ ] Focus change in window A does not re-render FocusScopes in window B (each window's registry only has its own keys)\n+- [ ] Focus change triggers exactly 2 FocusScope re-renders via event callback\n - [ ] All Tauri invokes return Ok/Err only — no focus data in return values\n - [ ] Focus changes flow through Rust event: click → Rust → event → React claim\n+- [ ] Keyboard nav: React invokes `spatial_navigate(key, direction)` → Rust emits event → React claims\n - [ ] No `focusedMoniker` state in React — Rust owns focus state\n - [ ] Event listener cleaned up on EntityFocusProvider unmount\n - [ ] `cargo test` passes, `pnpm vitest run` passes\n@@ -117,54 +86,67 @@\n\n ## Tests\n\n-### Rust unit tests\n+### Rust unit tests (`swissarmyhammer-commands/src/spatial_state.rs` or similar)\n\n ```rust\n #[test]\n-fn spatial_focus_updates_per_window_state_and_emits_event() {\n+fn spatial_focus_updates_focused_key_and_emits_event() {\n     // Given: empty state\n-    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k1\"))\n-    // Then: state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n-    // And: event == FocusChanged { window_label: \"main\", prev_key: None, next_key: Some(\"k1\") }\n+    // When: spatial_focus(\"key-abc\")\n+    // Then: state.focused_key == Some(\"key-abc\")\n+    // And: emitted event == FocusChanged { prev_key: None, next_key: Some(\"key-abc\") }\n }\n\n #[test]\n-fn focus_in_window_a_does_not_affect_window_b() {\n-    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }\n-    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k3\"))\n-    // Then: focus_by_window == { \"main\" => \"k3\", \"popup\" => \"k2\" }\n-    // And: event window_label == \"main\" (not \"popup\")\n+fn spatial_focus_emits_prev_and_next() {\n+    // Given: focused_key == Some(\"key-1\")\n+    // When: spatial_focus(\"key-2\")\n+    // Then: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: Some(\"key-2\") }\n }\n\n #[test]\n-fn spatial_unregister_focused_key_clears_that_window_only() {\n-    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }, k1 is registered\n-    // When: spatial_unregister(SpatialKey(\"k1\"))\n-    // Then: focus_by_window == { \"popup\" => \"k2\" }\n-    // And: event == FocusChanged { window_label: \"main\", prev_key: Some(\"k1\"), next_key: None }\n+fn spatial_unregister_focused_key_clears_focus() {\n+    // Given: focused_key == Some(\"key-1\"), key-1 is registered\n+    // When: spatial_unregister(\"key-1\")\n+    // Then: focused_key == None\n+    // And: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: None }\n }\n\n #[test]\n fn spatial_unregister_non_focused_key_no_event() {\n-    // Given: focus_by_window[\"main\"] == \"k1\", k2 is registered\n-    // When: spatial_unregister(SpatialKey(\"k2\"))\n-    // Then: focus unchanged, no event emitted\n-}\n-\n-#[test]\n-fn newtypes_are_serde_transparent() {\n-    // SpatialKey(\"abc\") serializes as JSON string \"abc\", not {\"0\":\"abc\"}\n+    // Given: focused_key == Some(\"key-1\"), key-2 is registered\n+    // When: spatial_unregister(\"key-2\")\n+    // Then: focused_key still Some(\"key-1\"), no event emitted\n }\n ```\n\n-### React unit tests (`entity-focus-context.test.tsx`)\n+### React unit tests (`kanban-app/ui/src/lib/entity-focus-context.test.tsx`)\n\n+Mock setup:\n+```typescript\n+vi.mock(\"@tauri-apps/api/core\", () => ({ invoke: vi.fn(() => Promise.resolve()) }));\n+vi.mock(\"@tauri-apps/api/event\", () => ({\n+  listen: vi.fn((_event, callback) => {\n+    // Store callback so tests can fire events manually\n+    (listen as any).__callback = callback;\n+    return Promise.resolve(() => {}); // unsub function\n+  }),\n+}));\n ```\n-test: \"claim registry only responds to events for keys it knows about\"\n-  setup: render EntityFocusProvider with FocusScope (key-A)\n-  act: fire focus-changed event { window_label: \"popup\", next_key: \"key-B\" }\n-  assert: FocusScope A is not focused (key-B not in this window's registry)\n\n+```\n+test: \"claim registry calls previous callback with false and next with true on focus-changed event\"\n+  setup: render EntityFocusProvider with two FocusScopes (key-A, key-B)\n+  act: fire focus-changed event { prev_key: null, next_key: \"key-A\" }\n+  assert: FocusScope A has data-focused attribute, B does not\n+  act: fire focus-changed event { prev_key: \"key-A\", next_key: \"key-B\" }\n+  assert: FocusScope A no longer data-focused, FocusScope B has data-focused\n+\n+test: \"unregistered key in focus-changed event is a no-op\"\n+  setup: render EntityFocusProvider with one FocusScope (key-A)\n+  act: fire focus-changed event { prev_key: \"nonexistent\", next_key: \"key-A\" }\n+  assert: no error thrown, FocusScope A has data-focused\n+\n test: \"FocusScope click invokes spatial_focus with its key\"\n   setup: render FocusScope inside EntityFocusProvider + FocusLayer\n   act: click the FocusScope element\n@@ -171,7 +153,13 @@\n   assert: invoke called with (\"spatial_focus\", { key: <the ULID> })\n\n test: \"EntityFocusProvider unmount cleans up event listener\"\n+  setup: render then unmount EntityFocusProvider\n+  assert: the unsub function returned by listen() was called\n+\n test: \"FocusScope unmount removes from claim registry\"\n+  setup: render FocusScope, capture its key\n+  act: unmount FocusScope, then fire focus-changed { next_key: <captured key> }\n+  assert: no error, no state update (callback was unregistered)\n ```\n\n ## Workflow\n","transaction_id":null}
{"id":"01KPZPK2KKQB1B000DMKBXFHYD","timestamp":"2026-04-24T12:14:18.227592Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,26 +1,53 @@\n ## What\n\n-Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by spatial key (ULID). Rust owns all focus state. React is a dumb renderer that responds to events.\n+Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by spatial key. Rust owns all focus state — and focus is tracked **per window**, because the app is multi-window (see `UIState.windows: HashMap<String, WindowState>` and the per-window `inspector_stack`).\n\n ### Core principle\n\n-- **Rust owns focus state.** `focused_key: Option<String>` lives in Rust. All focus changes go through Rust.\n-- **React sends commands, Rust emits events.** No return values. Every Tauri invoke returns Ok/Err. Focus changes are communicated via a `\"focus-changed\"` event.\n-- **Focus identity = key (ULID).** Not moniker. The claim registry is `Map<key, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n+- **Rust owns focus state.** React is a dumb renderer that responds to events.\n+- **Focus is per window.** Each Tauri window has its own focused element. OS window focus picks which one is \"the\" focus at any moment.\n+- **Focus identity = SpatialKey (ULID).** Not moniker. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n+\n+### Newtypes (Rust)\n+\n+Introduce three newtypes in a new module `swissarmyhammer-commands/src/spatial/keys.rs` (or inline in `spatial.rs`):\n+\n+```rust\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)]\n+pub struct WindowLabel(pub String);\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)]\n+pub struct SpatialKey(pub String);\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)]\n+pub struct LayerKey(pub String);\n+```\n+\n+`#[serde(transparent)]` means wire format is still a plain string — React sends/receives strings unchanged. The existing `String` window labels in `UIState` are left alone for now; new spatial code uses `WindowLabel`.\n\n ### API surface\n\n ```\n-React → Rust (all return Ok/Err, nothing else):\n-  spatial_register(key, moniker, rect, layer_key)   — FocusScope mount/resize\n-  spatial_unregister(key)                            — FocusScope unmount\n-  spatial_navigate(key, direction)                   — keyboard nav (from key, in direction)\n-  spatial_focus(key)                                 — click / programmatic\n-  spatial_push_layer(key, name)                      — FocusLayer mount\n-  spatial_pop_layer(key)                             — FocusLayer unmount\n+React → Rust (all return Ok/Err):\n+  spatial_register(key: SpatialKey, moniker: String, rect: Rect, layer_key: LayerKey)\n+  spatial_unregister(key: SpatialKey)\n+  spatial_navigate(key: SpatialKey, direction: Direction)\n+  spatial_focus(key: SpatialKey)\n+  spatial_push_layer(key: LayerKey, name: String, parent_key: Option<LayerKey>)\n+  spatial_pop_layer(key: LayerKey)\n+\n+Tauri commands derive window_label from the invoking webview (tauri::Window parameter), not\n+from React — React never has to pass it.\n\n-Rust → React (Tauri event):\n-  \"focus-changed\" { prev_key: Option<String>, next_key: Option<String> }\n+Rust → React (event):\n+  \"focus-changed\" {\n+    window_label: WindowLabel,\n+    prev_key: Option<SpatialKey>,\n+    next_key: Option<SpatialKey>,\n+  }\n ```\n\n ### React side\n@@ -28,14 +55,17 @@\n **Global event listener** (in EntityFocusProvider):\n\n ```typescript\n-listen(\"focus-changed\", ({ prev_key, next_key }) => {\n+listen(\"focus-changed\", ({ payload }) => {\n+  const { prev_key, next_key } = payload; // window_label is for filtering if needed\n   if (prev_key) claimRegistry.get(prev_key)?.(false);\n   if (next_key) claimRegistry.get(next_key)?.(true);\n });\n ```\n\n+Since each Tauri window has its own React tree and its own claim registry, the registry will only contain keys from that window. A `focus-changed` event for another window's key is a no-op in this window's registry.\n+\n **FocusScope**:\n-- Generates ULID key: `const key = useRef(ulid()).current`\n+- Generates SpatialKey (ULID): `const key = useRef(ulid()).current`\n - Registers in claim registry: `registry.set(key, setIsFocused)`\n - On click: `invoke(\"spatial_focus\", { key })` — fire and forget\n - Local `const [isFocused, setIsFocused] = useState(false)` driven by claim callback\n@@ -42,36 +72,37 @@\n\n **No focusedMoniker state in React.** React doesn't track who's focused — it just responds to events from Rust.\n\n-### Moniker is separate\n-\n-When a FocusScope receives `claim(true)`, it knows its own moniker. It can separately dispatch `ui.setFocus` with moniker + scope chain for command dispatch purposes. But this is the command system's concern, not the focus system's.\n-\n ### Rust side\n\n-**`SpatialState`** (or extend UIState):\n-- `focused_key: Option<String>`\n-- On `spatial_navigate(key, direction)`: resolve → update `focused_key` → emit event\n-- On `spatial_focus(key)`: update `focused_key` → emit event\n-- On `spatial_unregister(key)`: if key == focused_key, clear focus → emit event\n+**`SpatialState`**:\n+- `focus_by_window: HashMap<WindowLabel, SpatialKey>` — one focused key per window\n+- `register_entry(key, moniker, rect, layer_key, window_label)` stores which window each entry belongs to (derived via the entry's layer's window_label — see card 2)\n+- On `spatial_focus(window_label, key)`: update `focus_by_window[window_label]` → emit event with `window_label`\n+- On `spatial_navigate(window_label, key, direction)`: resolve → update → emit event with `window_label`\n+- On `spatial_unregister(key)`: if key is the focused key for any window, clear that window's focus → emit event\n\n ### Files to modify\n-- `swissarmyhammer-commands/src/ui_state.rs` or new `spatial_state.rs` — `focused_key`, event emission\n+- `swissarmyhammer-commands/src/spatial.rs` (new) or `spatial/mod.rs` — newtypes, `SpatialState`\n - `swissarmyhammer-kanban/src/commands/` — Tauri commands for spatial_*\n - `kanban-app/ui/src/lib/entity-focus-context.tsx` — claim registry, global event listener, remove useState\n - `kanban-app/ui/src/components/focus-scope.tsx` — key generation, claim registration, fire-and-forget invokes\n\n ### Subtasks\n-- [ ] Add claim registry `Map<string, (focused: boolean) => void>` to EntityFocusProvider\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n+- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to spatial state\n+- [ ] Emit `focus-changed` events carrying `window_label`, `prev_key`, `next_key`\n+- [ ] Tauri commands read `window_label` from the `tauri::Window` parameter (not from React args)\n+- [ ] Add claim registry `Map<SpatialKey, (focused: boolean) => void>` to EntityFocusProvider\n - [ ] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n - [ ] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n-- [ ] Add `focused_key` to Rust state with event emission on change\n - [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n\n ## Acceptance Criteria\n-- [ ] Focus change triggers exactly 2 FocusScope re-renders via event callback\n+- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes, not raw strings, in all new spatial code\n+- [ ] Focus is tracked per window — `focus_by_window[window_A]` and `focus_by_window[window_B]` are independent\n+- [ ] Focus change in window A does not re-render FocusScopes in window B (each window's registry only has its own keys)\n - [ ] All Tauri invokes return Ok/Err only — no focus data in return values\n - [ ] Focus changes flow through Rust event: click → Rust → event → React claim\n-- [ ] Keyboard nav: React invokes `spatial_navigate(key, direction)` → Rust emits event → React claims\n - [ ] No `focusedMoniker` state in React — Rust owns focus state\n - [ ] Event listener cleaned up on EntityFocusProvider unmount\n - [ ] `cargo test` passes, `pnpm vitest run` passes\n@@ -78,66 +109,53 @@\n\n ## Tests\n\n-### Rust unit tests (`swissarmyhammer-commands/src/spatial_state.rs` or similar)\n+### Rust unit tests\n\n ```rust\n #[test]\n-fn spatial_focus_updates_focused_key_and_emits_event() {\n+fn spatial_focus_updates_per_window_state_and_emits_event() {\n     // Given: empty state\n-    // When: spatial_focus(\"key-abc\")\n-    // Then: state.focused_key == Some(\"key-abc\")\n-    // And: emitted event == FocusChanged { prev_key: None, next_key: Some(\"key-abc\") }\n+    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k1\"))\n+    // Then: state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n+    // And: event == FocusChanged { window_label: \"main\", prev_key: None, next_key: Some(\"k1\") }\n }\n\n #[test]\n-fn spatial_focus_emits_prev_and_next() {\n-    // Given: focused_key == Some(\"key-1\")\n-    // When: spatial_focus(\"key-2\")\n-    // Then: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: Some(\"key-2\") }\n+fn focus_in_window_a_does_not_affect_window_b() {\n+    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }\n+    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k3\"))\n+    // Then: focus_by_window == { \"main\" => \"k3\", \"popup\" => \"k2\" }\n+    // And: event window_label == \"main\" (not \"popup\")\n }\n\n #[test]\n-fn spatial_unregister_focused_key_clears_focus() {\n-    // Given: focused_key == Some(\"key-1\"), key-1 is registered\n-    // When: spatial_unregister(\"key-1\")\n-    // Then: focused_key == None\n-    // And: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: None }\n+fn spatial_unregister_focused_key_clears_that_window_only() {\n+    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }, k1 is registered\n+    // When: spatial_unregister(SpatialKey(\"k1\"))\n+    // Then: focus_by_window == { \"popup\" => \"k2\" }\n+    // And: event == FocusChanged { window_label: \"main\", prev_key: Some(\"k1\"), next_key: None }\n }\n\n #[test]\n fn spatial_unregister_non_focused_key_no_event() {\n-    // Given: focused_key == Some(\"key-1\"), key-2 is registered\n-    // When: spatial_unregister(\"key-2\")\n-    // Then: focused_key still Some(\"key-1\"), no event emitted\n+    // Given: focus_by_window[\"main\"] == \"k1\", k2 is registered\n+    // When: spatial_unregister(SpatialKey(\"k2\"))\n+    // Then: focus unchanged, no event emitted\n+}\n+\n+#[test]\n+fn newtypes_are_serde_transparent() {\n+    // SpatialKey(\"abc\") serializes as JSON string \"abc\", not {\"0\":\"abc\"}\n }\n ```\n\n-### React unit tests (`kanban-app/ui/src/lib/entity-focus-context.test.tsx`)\n-\n-Mock setup:\n-```typescript\n-vi.mock(\"@tauri-apps/api/core\", () => ({ invoke: vi.fn(() => Promise.resolve()) }));\n-vi.mock(\"@tauri-apps/api/event\", () => ({\n-  listen: vi.fn((_event, callback) => {\n-    // Store callback so tests can fire events manually\n-    (listen as any).__callback = callback;\n-    return Promise.resolve(() => {}); // unsub function\n-  }),\n-}));\n-```\n+### React unit tests (`entity-focus-context.test.tsx`)\n\n ```\n-test: \"claim registry calls previous callback with false and next with true on focus-changed event\"\n-  setup: render EntityFocusProvider with two FocusScopes (key-A, key-B)\n-  act: fire focus-changed event { prev_key: null, next_key: \"key-A\" }\n-  assert: FocusScope A has data-focused attribute, B does not\n-  act: fire focus-changed event { prev_key: \"key-A\", next_key: \"key-B\" }\n-  assert: FocusScope A no longer data-focused, FocusScope B has data-focused\n-\n-test: \"unregistered key in focus-changed event is a no-op\"\n-  setup: render EntityFocusProvider with one FocusScope (key-A)\n-  act: fire focus-changed event { prev_key: \"nonexistent\", next_key: \"key-A\" }\n-  assert: no error thrown, FocusScope A has data-focused\n+test: \"claim registry only responds to events for keys it knows about\"\n+  setup: render EntityFocusProvider with FocusScope (key-A)\n+  act: fire focus-changed event { window_label: \"popup\", next_key: \"key-B\" }\n+  assert: FocusScope A is not focused (key-B not in this window's registry)\n\n test: \"FocusScope click invokes spatial_focus with its key\"\n   setup: render FocusScope inside EntityFocusProvider + FocusLayer\n@@ -145,13 +163,7 @@\n   assert: invoke called with (\"spatial_focus\", { key: <the ULID> })\n\n test: \"EntityFocusProvider unmount cleans up event listener\"\n-  setup: render then unmount EntityFocusProvider\n-  assert: the unsub function returned by listen() was called\n-\n test: \"FocusScope unmount removes from claim registry\"\n-  setup: render FocusScope, capture its key\n-  act: unmount FocusScope, then fire focus-changed { next_key: <captured key> }\n-  assert: no error, no state update (callback was unregistered)\n ```\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -1,53 +1,26 @@\n ## What\n\n-Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by spatial key. Rust owns all focus state — and focus is tracked **per window**, because the app is multi-window (see `UIState.windows: HashMap<String, WindowState>` and the per-window `inspector_stack`).\n+Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by spatial key (ULID). Rust owns all focus state. React is a dumb renderer that responds to events.\n\n ### Core principle\n\n-- **Rust owns focus state.** React is a dumb renderer that responds to events.\n-- **Focus is per window.** Each Tauri window has its own focused element. OS window focus picks which one is \"the\" focus at any moment.\n-- **Focus identity = SpatialKey (ULID).** Not moniker. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n-\n-### Newtypes (Rust)\n-\n-Introduce three newtypes in a new module `swissarmyhammer-commands/src/spatial/keys.rs` (or inline in `spatial.rs`):\n-\n-```rust\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct WindowLabel(pub String);\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct SpatialKey(pub String);\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct LayerKey(pub String);\n-```\n-\n-`#[serde(transparent)]` means wire format is still a plain string — React sends/receives strings unchanged. The existing `String` window labels in `UIState` are left alone for now; new spatial code uses `WindowLabel`.\n+- **Rust owns focus state.** `focused_key: Option<String>` lives in Rust. All focus changes go through Rust.\n+- **React sends commands, Rust emits events.** No return values. Every Tauri invoke returns Ok/Err. Focus changes are communicated via a `\"focus-changed\"` event.\n+- **Focus identity = key (ULID).** Not moniker. The claim registry is `Map<key, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n\n ### API surface\n\n ```\n-React → Rust (all return Ok/Err):\n-  spatial_register(key: SpatialKey, moniker: String, rect: Rect, layer_key: LayerKey)\n-  spatial_unregister(key: SpatialKey)\n-  spatial_navigate(key: SpatialKey, direction: Direction)\n-  spatial_focus(key: SpatialKey)\n-  spatial_push_layer(key: LayerKey, name: String, parent_key: Option<LayerKey>)\n-  spatial_pop_layer(key: LayerKey)\n-\n-Tauri commands derive window_label from the invoking webview (tauri::Window parameter), not\n-from React — React never has to pass it.\n+React → Rust (all return Ok/Err, nothing else):\n+  spatial_register(key, moniker, rect, layer_key)   — FocusScope mount/resize\n+  spatial_unregister(key)                            — FocusScope unmount\n+  spatial_navigate(key, direction)                   — keyboard nav (from key, in direction)\n+  spatial_focus(key)                                 — click / programmatic\n+  spatial_push_layer(key, name)                      — FocusLayer mount\n+  spatial_pop_layer(key)                             — FocusLayer unmount\n\n-Rust → React (event):\n-  \"focus-changed\" {\n-    window_label: WindowLabel,\n-    prev_key: Option<SpatialKey>,\n-    next_key: Option<SpatialKey>,\n-  }\n+Rust → React (Tauri event):\n+  \"focus-changed\" { prev_key: Option<String>, next_key: Option<String> }\n ```\n\n ### React side\n@@ -55,17 +28,14 @@\n **Global event listener** (in EntityFocusProvider):\n\n ```typescript\n-listen(\"focus-changed\", ({ payload }) => {\n-  const { prev_key, next_key } = payload; // window_label is for filtering if needed\n+listen(\"focus-changed\", ({ prev_key, next_key }) => {\n   if (prev_key) claimRegistry.get(prev_key)?.(false);\n   if (next_key) claimRegistry.get(next_key)?.(true);\n });\n ```\n\n-Since each Tauri window has its own React tree and its own claim registry, the registry will only contain keys from that window. A `focus-changed` event for another window's key is a no-op in this window's registry.\n-\n **FocusScope**:\n-- Generates SpatialKey (ULID): `const key = useRef(ulid()).current`\n+- Generates ULID key: `const key = useRef(ulid()).current`\n - Registers in claim registry: `registry.set(key, setIsFocused)`\n - On click: `invoke(\"spatial_focus\", { key })` — fire and forget\n - Local `const [isFocused, setIsFocused] = useState(false)` driven by claim callback\n@@ -72,37 +42,36 @@\n\n **No focusedMoniker state in React.** React doesn't track who's focused — it just responds to events from Rust.\n\n+### Moniker is separate\n+\n+When a FocusScope receives `claim(true)`, it knows its own moniker. It can separately dispatch `ui.setFocus` with moniker + scope chain for command dispatch purposes. But this is the command system's concern, not the focus system's.\n+\n ### Rust side\n\n-**`SpatialState`**:\n-- `focus_by_window: HashMap<WindowLabel, SpatialKey>` — one focused key per window\n-- `register_entry(key, moniker, rect, layer_key, window_label)` stores which window each entry belongs to (derived via the entry's layer's window_label — see card 2)\n-- On `spatial_focus(window_label, key)`: update `focus_by_window[window_label]` → emit event with `window_label`\n-- On `spatial_navigate(window_label, key, direction)`: resolve → update → emit event with `window_label`\n-- On `spatial_unregister(key)`: if key is the focused key for any window, clear that window's focus → emit event\n+**`SpatialState`** (or extend UIState):\n+- `focused_key: Option<String>`\n+- On `spatial_navigate(key, direction)`: resolve → update `focused_key` → emit event\n+- On `spatial_focus(key)`: update `focused_key` → emit event\n+- On `spatial_unregister(key)`: if key == focused_key, clear focus → emit event\n\n ### Files to modify\n-- `swissarmyhammer-commands/src/spatial.rs` (new) or `spatial/mod.rs` — newtypes, `SpatialState`\n+- `swissarmyhammer-commands/src/ui_state.rs` or new `spatial_state.rs` — `focused_key`, event emission\n - `swissarmyhammer-kanban/src/commands/` — Tauri commands for spatial_*\n - `kanban-app/ui/src/lib/entity-focus-context.tsx` — claim registry, global event listener, remove useState\n - `kanban-app/ui/src/components/focus-scope.tsx` — key generation, claim registration, fire-and-forget invokes\n\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n-- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to spatial state\n-- [ ] Emit `focus-changed` events carrying `window_label`, `prev_key`, `next_key`\n-- [ ] Tauri commands read `window_label` from the `tauri::Window` parameter (not from React args)\n-- [ ] Add claim registry `Map<SpatialKey, (focused: boolean) => void>` to EntityFocusProvider\n+- [ ] Add claim registry `Map<string, (focused: boolean) => void>` to EntityFocusProvider\n - [ ] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n - [ ] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n+- [ ] Add `focused_key` to Rust state with event emission on change\n - [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n\n ## Acceptance Criteria\n-- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes, not raw strings, in all new spatial code\n-- [ ] Focus is tracked per window — `focus_by_window[window_A]` and `focus_by_window[window_B]` are independent\n-- [ ] Focus change in window A does not re-render FocusScopes in window B (each window's registry only has its own keys)\n+- [ ] Focus change triggers exactly 2 FocusScope re-renders via event callback\n - [ ] All Tauri invokes return Ok/Err only — no focus data in return values\n - [ ] Focus changes flow through Rust event: click → Rust → event → React claim\n+- [ ] Keyboard nav: React invokes `spatial_navigate(key, direction)` → Rust emits event → React claims\n - [ ] No `focusedMoniker` state in React — Rust owns focus state\n - [ ] Event listener cleaned up on EntityFocusProvider unmount\n - [ ] `cargo test` passes, `pnpm vitest run` passes\n@@ -109,54 +78,67 @@\n\n ## Tests\n\n-### Rust unit tests\n+### Rust unit tests (`swissarmyhammer-commands/src/spatial_state.rs` or similar)\n\n ```rust\n #[test]\n-fn spatial_focus_updates_per_window_state_and_emits_event() {\n+fn spatial_focus_updates_focused_key_and_emits_event() {\n     // Given: empty state\n-    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k1\"))\n-    // Then: state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n-    // And: event == FocusChanged { window_label: \"main\", prev_key: None, next_key: Some(\"k1\") }\n+    // When: spatial_focus(\"key-abc\")\n+    // Then: state.focused_key == Some(\"key-abc\")\n+    // And: emitted event == FocusChanged { prev_key: None, next_key: Some(\"key-abc\") }\n }\n\n #[test]\n-fn focus_in_window_a_does_not_affect_window_b() {\n-    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }\n-    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k3\"))\n-    // Then: focus_by_window == { \"main\" => \"k3\", \"popup\" => \"k2\" }\n-    // And: event window_label == \"main\" (not \"popup\")\n+fn spatial_focus_emits_prev_and_next() {\n+    // Given: focused_key == Some(\"key-1\")\n+    // When: spatial_focus(\"key-2\")\n+    // Then: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: Some(\"key-2\") }\n }\n\n #[test]\n-fn spatial_unregister_focused_key_clears_that_window_only() {\n-    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }, k1 is registered\n-    // When: spatial_unregister(SpatialKey(\"k1\"))\n-    // Then: focus_by_window == { \"popup\" => \"k2\" }\n-    // And: event == FocusChanged { window_label: \"main\", prev_key: Some(\"k1\"), next_key: None }\n+fn spatial_unregister_focused_key_clears_focus() {\n+    // Given: focused_key == Some(\"key-1\"), key-1 is registered\n+    // When: spatial_unregister(\"key-1\")\n+    // Then: focused_key == None\n+    // And: event == FocusChanged { prev_key: Some(\"key-1\"), next_key: None }\n }\n\n #[test]\n fn spatial_unregister_non_focused_key_no_event() {\n-    // Given: focus_by_window[\"main\"] == \"k1\", k2 is registered\n-    // When: spatial_unregister(SpatialKey(\"k2\"))\n-    // Then: focus unchanged, no event emitted\n-}\n-\n-#[test]\n-fn newtypes_are_serde_transparent() {\n-    // SpatialKey(\"abc\") serializes as JSON string \"abc\", not {\"0\":\"abc\"}\n+    // Given: focused_key == Some(\"key-1\"), key-2 is registered\n+    // When: spatial_unregister(\"key-2\")\n+    // Then: focused_key still Some(\"key-1\"), no event emitted\n }\n ```\n\n-### React unit tests (`entity-focus-context.test.tsx`)\n+### React unit tests (`kanban-app/ui/src/lib/entity-focus-context.test.tsx`)\n\n+Mock setup:\n+```typescript\n+vi.mock(\"@tauri-apps/api/core\", () => ({ invoke: vi.fn(() => Promise.resolve()) }));\n+vi.mock(\"@tauri-apps/api/event\", () => ({\n+  listen: vi.fn((_event, callback) => {\n+    // Store callback so tests can fire events manually\n+    (listen as any).__callback = callback;\n+    return Promise.resolve(() => {}); // unsub function\n+  }),\n+}));\n ```\n-test: \"claim registry only responds to events for keys it knows about\"\n-  setup: render EntityFocusProvider with FocusScope (key-A)\n-  act: fire focus-changed event { window_label: \"popup\", next_key: \"key-B\" }\n-  assert: FocusScope A is not focused (key-B not in this window's registry)\n\n+```\n+test: \"claim registry calls previous callback with false and next with true on focus-changed event\"\n+  setup: render EntityFocusProvider with two FocusScopes (key-A, key-B)\n+  act: fire focus-changed event { prev_key: null, next_key: \"key-A\" }\n+  assert: FocusScope A has data-focused attribute, B does not\n+  act: fire focus-changed event { prev_key: \"key-A\", next_key: \"key-B\" }\n+  assert: FocusScope A no longer data-focused, FocusScope B has data-focused\n+\n+test: \"unregistered key in focus-changed event is a no-op\"\n+  setup: render EntityFocusProvider with one FocusScope (key-A)\n+  act: fire focus-changed event { prev_key: \"nonexistent\", next_key: \"key-A\" }\n+  assert: no error thrown, FocusScope A has data-focused\n+\n test: \"FocusScope click invokes spatial_focus with its key\"\n   setup: render FocusScope inside EntityFocusProvider + FocusLayer\n   act: click the FocusScope element\n@@ -163,7 +145,13 @@\n   assert: invoke called with (\"spatial_focus\", { key: <the ULID> })\n\n test: \"EntityFocusProvider unmount cleans up event listener\"\n+  setup: render then unmount EntityFocusProvider\n+  assert: the unsub function returned by listen() was called\n+\n test: \"FocusScope unmount removes from claim registry\"\n+  setup: render FocusScope, capture its key\n+  act: unmount FocusScope, then fire focus-changed { next_key: <captured key> }\n+  assert: no error, no state update (callback was unregistered)\n ```\n\n ## Workflow\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Focus claim registry: key-based, event-driven, Rust owns state\n\\ No newline at end of file\n+Focus claim registry: per-window, event-driven, Rust owns state\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Focus claim registry: per-window, event-driven, Rust owns state\n\\ No newline at end of file\n+Focus claim registry: key-based, event-driven, Rust owns state\n\\ No newline at end of file\n"}]]}
{"id":"01KPZWQS26W969SKGQ63SH2XAC","timestamp":"2026-04-24T14:01:43.750973Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -4,175 +4,164 @@\n position_column: todo\n position_ordinal: '9980'\n project: spatial-nav\n-title: 'Focus claim registry: per-window, event-driven, Rust owns state'\n+title: 'Focus claim registry: per-window, event-driven, Rust owns state (newtype-only signatures)'\n ---\n ## What\n\n-Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by spatial key. Rust owns all focus state — and focus is tracked **per window**, because the app is multi-window (see `UIState.windows: HashMap<String, WindowState>` and the per-window `inspector_stack`).\n+Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by `SpatialKey`. Rust owns all focus state — tracked **per window** because the app is multi-window (see `UIState.windows`, per-window `inspector_stack`). Every signature uses newtypes, not bare primitives.\n\n ### Core principle\n\n - **Rust owns focus state.** React is a dumb renderer that responds to events.\n-- **Focus is per window.** Each Tauri window has its own focused element. OS window focus picks which one is \"the\" focus at any moment.\n-- **Focus identity = SpatialKey (ULID).** Not moniker. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n+- **Focus is per window.** Each Tauri window has its own focused element.\n+- **Focus identity = `SpatialKey` (ULID).** Not `Moniker`. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n\n ### Newtypes (Rust)\n\n-Introduce three newtypes in a new module `swissarmyhammer-commands/src/spatial/keys.rs` (or inline in `spatial.rs`):\n+Shared with card `01KNQXW7HH...`. Referenced here so this card's signatures are self-contained.\n\n ```rust\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct WindowLabel(pub String);\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct SpatialKey(pub String);\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct LayerKey(pub String);\n+#[serde(transparent)] pub struct WindowLabel(pub String);\n+#[serde(transparent)] pub struct SpatialKey(pub String);\n+#[serde(transparent)] pub struct LayerKey(pub String);\n+#[serde(transparent)] pub struct Moniker(pub String);\n ```\n\n-`#[serde(transparent)]` means wire format is still a plain string — React sends/receives strings unchanged. The existing `String` window labels in `UIState` are left alone for now; new spatial code uses `WindowLabel`.\n+No `String` appears in any signature below.\n\n ### API surface\n\n+```rust\n+// React → Rust. window_label is derived from tauri::Window, never passed from React.\n+async fn spatial_register_focusable(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n+async fn spatial_register_zone(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n+async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n+async fn spatial_focus(window: tauri::Window, key: SpatialKey) -> Result<()>;\n+async fn spatial_navigate(window: tauri::Window, key: SpatialKey, direction: Direction) -> Result<()>;\n+async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: LayerName, parent: Option<LayerKey>) -> Result<()>;\n+async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n-React → Rust (all return Ok/Err):\n-  spatial_register(key: SpatialKey, moniker: String, rect: Rect, layer_key: LayerKey)\n-  spatial_unregister(key: SpatialKey)\n-  spatial_navigate(key: SpatialKey, direction: Direction)\n-  spatial_focus(key: SpatialKey)\n-  spatial_push_layer(key: LayerKey, name: String, parent_key: Option<LayerKey>)\n-  spatial_pop_layer(key: LayerKey)\n\n-Tauri commands derive window_label from the invoking webview (tauri::Window parameter), not\n-from React — React never has to pass it.\n-\n-Rust → React (event):\n-  \"focus-changed\" {\n-    window_label: WindowLabel,\n-    prev_key: Option<SpatialKey>,\n-    next_key: Option<SpatialKey>,\n-  }\n+```rust\n+// Rust → React (Tauri event)\n+#[derive(Serialize)]\n+pub struct FocusChangedEvent {\n+    pub window_label: WindowLabel,\n+    pub prev_key: Option<SpatialKey>,\n+    pub next_key: Option<SpatialKey>,\n+    pub next_moniker: Option<Moniker>,   // convenience for React — saves a lookup\n+}\n ```\n\n ### React side\n\n+**Branded-type parity with Rust newtypes.** TypeScript gets its own thin branded-string types so `SpatialKey`, `LayerKey`, `Moniker`, `WindowLabel` can't be mixed up at compile time:\n+\n+```typescript\n+// kanban-app/ui/src/types/spatial.ts\n+export type WindowLabel = string & { readonly __tag: \"WindowLabel\" };\n+export type SpatialKey  = string & { readonly __tag: \"SpatialKey\" };\n+export type LayerKey    = string & { readonly __tag: \"LayerKey\" };\n+export type Moniker     = string & { readonly __tag: \"Moniker\" };\n+export type LayerName   = string & { readonly __tag: \"LayerName\" };\n+export type Pixels      = number & { readonly __tag: \"Pixels\" };\n+\n+// Brand helpers\n+export const WindowLabel = (s: string) => s as WindowLabel;\n+export const SpatialKey  = (s: string) => s as SpatialKey;\n+export const LayerKey    = (s: string) => s as LayerKey;\n+export const Moniker     = (s: string) => s as Moniker;\n+export const LayerName   = (s: string) => s as LayerName;\n+export const Pixels      = (n: number) => n as Pixels;\n+```\n+\n **Global event listener** (in EntityFocusProvider):\n\n ```typescript\n-listen(\"focus-changed\", ({ payload }) => {\n-  const { prev_key, next_key } = payload; // window_label is for filtering if needed\n-  if (prev_key) claimRegistry.get(prev_key)?.(false);\n-  if (next_key) claimRegistry.get(next_key)?.(true);\n+type FocusChangedPayload = {\n+  window_label: WindowLabel;\n+  prev_key: SpatialKey | null;\n+  next_key: SpatialKey | null;\n+  next_moniker: Moniker | null;\n+};\n+\n+listen<FocusChangedPayload>(\"focus-changed\", ({ payload }) => {\n+  if (payload.prev_key) claimRegistry.get(payload.prev_key)?.(false);\n+  if (payload.next_key) claimRegistry.get(payload.next_key)?.(true);\n });\n ```\n\n-Since each Tauri window has its own React tree and its own claim registry, the registry will only contain keys from that window. A `focus-changed` event for another window's key is a no-op in this window's registry.\n+Since each Tauri window has its own React tree and its own claim registry, the registry only contains keys from that window. A `focus-changed` event for another window's key is a no-op here.\n\n-**FocusScope**:\n-- Generates SpatialKey (ULID): `const key = useRef(ulid()).current`\n-- Registers in claim registry: `registry.set(key, setIsFocused)`\n-- On click: `invoke(\"spatial_focus\", { key })` — fire and forget\n-- Local `const [isFocused, setIsFocused] = useState(false)` driven by claim callback\n+**FocusScope / Focusable / FocusZone primitives** generate their `SpatialKey` via `useRef(SpatialKey(ulid())).current`. Branded helpers make the ULID typed at creation.\n\n-**No focusedMoniker state in React.** React doesn't track who's focused — it just responds to events from Rust.\n+### Rust state\n\n-### Rust side\n+```rust\n+pub struct SpatialState {\n+    registry: SpatialRegistry,\n+    focus_by_window: HashMap<WindowLabel, SpatialKey>,\n+}\n\n-**`SpatialState`**:\n-- `focus_by_window: HashMap<WindowLabel, SpatialKey>` — one focused key per window\n-- `register_entry(key, moniker, rect, layer_key, window_label)` stores which window each entry belongs to (derived via the entry's layer's window_label — see card 2)\n-- On `spatial_focus(window_label, key)`: update `focus_by_window[window_label]` → emit event with `window_label`\n-- On `spatial_navigate(window_label, key, direction)`: resolve → update → emit event with `window_label`\n-- On `spatial_unregister(key)`: if key is the focused key for any window, clear that window's focus → emit event\n+impl SpatialState {\n+    pub fn focus(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n+    pub fn navigate(&mut self, window: WindowLabel, key: SpatialKey, direction: Direction) -> Option<FocusChangedEvent> { /* ... */ }\n+    pub fn handle_unregister(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n+}\n+```\n\n-### Files to modify\n-- `swissarmyhammer-commands/src/spatial.rs` (new) or `spatial/mod.rs` — newtypes, `SpatialState`\n-- `swissarmyhammer-kanban/src/commands/` — Tauri commands for spatial_*\n-- `kanban-app/ui/src/lib/entity-focus-context.tsx` — claim registry, global event listener, remove useState\n-- `kanban-app/ui/src/components/focus-scope.tsx` — key generation, claim registration, fire-and-forget invokes\n+Every method signature uses newtypes. `FocusChangedEvent` is returned (not side-effected) so tests don't need Tauri mocking.\n\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n-- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to spatial state\n-- [ ] Emit `focus-changed` events carrying `window_label`, `prev_key`, `next_key`\n-- [ ] Tauri commands read `window_label` from the `tauri::Window` parameter (not from React args)\n+- [ ] Define TS branded types for `WindowLabel`/`SpatialKey`/`LayerKey`/`Moniker`/`LayerName`/`Pixels`\n+- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to `SpatialState`\n+- [ ] Emit `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n+- [ ] Tauri commands derive `WindowLabel` from the `tauri::Window` parameter\n - [ ] Add claim registry `Map<SpatialKey, (focused: boolean) => void>` to EntityFocusProvider\n-- [ ] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n-- [ ] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n+- [ ] Add global `listen(\"focus-changed\")` handler\n+- [ ] Primitive components (see separate React-primitives card) use branded ULIDs\n - [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n\n ## Acceptance Criteria\n-- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes, not raw strings, in all new spatial code\n-- [ ] Focus is tracked per window — `focus_by_window[window_A]` and `focus_by_window[window_B]` are independent\n-- [ ] Focus change in window A does not re-render FocusScopes in window B (each window's registry only has its own keys)\n-- [ ] All Tauri invokes return Ok/Err only — no focus data in return values\n-- [ ] Focus changes flow through Rust event: click → Rust → event → React claim\n-- [ ] No `focusedMoniker` state in React — Rust owns focus state\n-- [ ] Event listener cleaned up on EntityFocusProvider unmount\n-- [ ] `cargo test` passes, `pnpm vitest run` passes\n+- [ ] No signature in any added Rust type/method/command uses bare `String` or `f64`\n+- [ ] TS branded types mirror each Rust newtype; no `string`/`number` in spatial types' signatures\n+- [ ] Focus is per window; `focus_by_window[A]` and `focus_by_window[B]` independent\n+- [ ] Focus change in window A doesn't re-render scopes in window B\n+- [ ] All Tauri invokes return Ok/Err only; no focus data in return values\n+- [ ] `focus-changed` event carries newtyped fields\n+- [ ] No `focusedMoniker` state in React\n+- [ ] `cargo test` and `pnpm vitest run` pass\n\n ## Tests\n\n-### Rust unit tests\n+### Rust\n\n ```rust\n #[test]\n-fn spatial_focus_updates_per_window_state_and_emits_event() {\n-    // Given: empty state\n-    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k1\"))\n-    // Then: state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n-    // And: event == FocusChanged { window_label: \"main\", prev_key: None, next_key: Some(\"k1\") }\n-}\n-\n-#[test]\n-fn focus_in_window_a_does_not_affect_window_b() {\n-    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }\n-    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k3\"))\n-    // Then: focus_by_window == { \"main\" => \"k3\", \"popup\" => \"k2\" }\n-    // And: event window_label == \"main\" (not \"popup\")\n+fn focus_updates_per_window_state() {\n+    // spatial_focus(WindowLabel(\"main\".into()), SpatialKey(\"k1\".into()))\n+    // → state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n+    // → event.window_label == \"main\", next_key == Some(\"k1\")\n }\n\n #[test]\n-fn spatial_unregister_focused_key_clears_that_window_only() {\n-    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }, k1 is registered\n-    // When: spatial_unregister(SpatialKey(\"k1\"))\n-    // Then: focus_by_window == { \"popup\" => \"k2\" }\n-    // And: event == FocusChanged { window_label: \"main\", prev_key: Some(\"k1\"), next_key: None }\n-}\n+fn focus_in_window_a_does_not_affect_window_b() { /* ... */ }\n\n #[test]\n-fn spatial_unregister_non_focused_key_no_event() {\n-    // Given: focus_by_window[\"main\"] == \"k1\", k2 is registered\n-    // When: spatial_unregister(SpatialKey(\"k2\"))\n-    // Then: focus unchanged, no event emitted\n-}\n+fn unregister_focused_clears_that_window_only() { /* ... */ }\n\n #[test]\n-fn newtypes_are_serde_transparent() {\n-    // SpatialKey(\"abc\") serializes as JSON string \"abc\", not {\"0\":\"abc\"}\n+fn event_carries_moniker_for_next_key() {\n+    // FocusChangedEvent.next_moniker is Some(entry.moniker) when next_key is Some\n }\n ```\n\n-### React unit tests (`entity-focus-context.test.tsx`)\n+### React\n\n-```\n-test: \"claim registry only responds to events for keys it knows about\"\n-  setup: render EntityFocusProvider with FocusScope (key-A)\n-  act: fire focus-changed event { window_label: \"popup\", next_key: \"key-B\" }\n-  assert: FocusScope A is not focused (key-B not in this window's registry)\n-\n-test: \"FocusScope click invokes spatial_focus with its key\"\n-  setup: render FocusScope inside EntityFocusProvider + FocusLayer\n-  act: click the FocusScope element\n-  assert: invoke called with (\"spatial_focus\", { key: <the ULID> })\n-\n-test: \"EntityFocusProvider unmount cleans up event listener\"\n-test: \"FocusScope unmount removes from claim registry\"\n-```\n+- [ ] Claim registry ignores events for unknown keys\n+- [ ] FocusScope/Focusable click invokes `spatial_focus` with its branded `SpatialKey`\n+- [ ] Provider unmount removes the listener\n+- [ ] Scope unmount removes from claim registry\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -4,164 +4,175 @@\n position_column: todo\n position_ordinal: '9980'\n project: spatial-nav\n-title: 'Focus claim registry: per-window, event-driven, Rust owns state (newtype-only signatures)'\n+title: 'Focus claim registry: per-window, event-driven, Rust owns state'\n ---\n ## What\n\n-Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by `SpatialKey`. Rust owns all focus state — tracked **per window** because the app is multi-window (see `UIState.windows`, per-window `inspector_stack`). Every signature uses newtypes, not bare primitives.\n+Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by spatial key. Rust owns all focus state — and focus is tracked **per window**, because the app is multi-window (see `UIState.windows: HashMap<String, WindowState>` and the per-window `inspector_stack`).\n\n ### Core principle\n\n - **Rust owns focus state.** React is a dumb renderer that responds to events.\n-- **Focus is per window.** Each Tauri window has its own focused element.\n-- **Focus identity = `SpatialKey` (ULID).** Not `Moniker`. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n+- **Focus is per window.** Each Tauri window has its own focused element. OS window focus picks which one is \"the\" focus at any moment.\n+- **Focus identity = SpatialKey (ULID).** Not moniker. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n\n ### Newtypes (Rust)\n\n-Shared with card `01KNQXW7HH...`. Referenced here so this card's signatures are self-contained.\n+Introduce three newtypes in a new module `swissarmyhammer-commands/src/spatial/keys.rs` (or inline in `spatial.rs`):\n\n ```rust\n-#[serde(transparent)] pub struct WindowLabel(pub String);\n-#[serde(transparent)] pub struct SpatialKey(pub String);\n-#[serde(transparent)] pub struct LayerKey(pub String);\n-#[serde(transparent)] pub struct Moniker(pub String);\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)]\n+pub struct WindowLabel(pub String);\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)]\n+pub struct SpatialKey(pub String);\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)]\n+pub struct LayerKey(pub String);\n ```\n\n-No `String` appears in any signature below.\n+`#[serde(transparent)]` means wire format is still a plain string — React sends/receives strings unchanged. The existing `String` window labels in `UIState` are left alone for now; new spatial code uses `WindowLabel`.\n\n ### API surface\n\n-```rust\n-// React → Rust. window_label is derived from tauri::Window, never passed from React.\n-async fn spatial_register_focusable(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n-async fn spatial_register_zone(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n-async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n-async fn spatial_focus(window: tauri::Window, key: SpatialKey) -> Result<()>;\n-async fn spatial_navigate(window: tauri::Window, key: SpatialKey, direction: Direction) -> Result<()>;\n-async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: LayerName, parent: Option<LayerKey>) -> Result<()>;\n-async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n+React → Rust (all return Ok/Err):\n+  spatial_register(key: SpatialKey, moniker: String, rect: Rect, layer_key: LayerKey)\n+  spatial_unregister(key: SpatialKey)\n+  spatial_navigate(key: SpatialKey, direction: Direction)\n+  spatial_focus(key: SpatialKey)\n+  spatial_push_layer(key: LayerKey, name: String, parent_key: Option<LayerKey>)\n+  spatial_pop_layer(key: LayerKey)\n\n-```rust\n-// Rust → React (Tauri event)\n-#[derive(Serialize)]\n-pub struct FocusChangedEvent {\n-    pub window_label: WindowLabel,\n-    pub prev_key: Option<SpatialKey>,\n-    pub next_key: Option<SpatialKey>,\n-    pub next_moniker: Option<Moniker>,   // convenience for React — saves a lookup\n-}\n+Tauri commands derive window_label from the invoking webview (tauri::Window parameter), not\n+from React — React never has to pass it.\n+\n+Rust → React (event):\n+  \"focus-changed\" {\n+    window_label: WindowLabel,\n+    prev_key: Option<SpatialKey>,\n+    next_key: Option<SpatialKey>,\n+  }\n ```\n\n ### React side\n\n-**Branded-type parity with Rust newtypes.** TypeScript gets its own thin branded-string types so `SpatialKey`, `LayerKey`, `Moniker`, `WindowLabel` can't be mixed up at compile time:\n-\n-```typescript\n-// kanban-app/ui/src/types/spatial.ts\n-export type WindowLabel = string & { readonly __tag: \"WindowLabel\" };\n-export type SpatialKey  = string & { readonly __tag: \"SpatialKey\" };\n-export type LayerKey    = string & { readonly __tag: \"LayerKey\" };\n-export type Moniker     = string & { readonly __tag: \"Moniker\" };\n-export type LayerName   = string & { readonly __tag: \"LayerName\" };\n-export type Pixels      = number & { readonly __tag: \"Pixels\" };\n-\n-// Brand helpers\n-export const WindowLabel = (s: string) => s as WindowLabel;\n-export const SpatialKey  = (s: string) => s as SpatialKey;\n-export const LayerKey    = (s: string) => s as LayerKey;\n-export const Moniker     = (s: string) => s as Moniker;\n-export const LayerName   = (s: string) => s as LayerName;\n-export const Pixels      = (n: number) => n as Pixels;\n-```\n-\n **Global event listener** (in EntityFocusProvider):\n\n ```typescript\n-type FocusChangedPayload = {\n-  window_label: WindowLabel;\n-  prev_key: SpatialKey | null;\n-  next_key: SpatialKey | null;\n-  next_moniker: Moniker | null;\n-};\n-\n-listen<FocusChangedPayload>(\"focus-changed\", ({ payload }) => {\n-  if (payload.prev_key) claimRegistry.get(payload.prev_key)?.(false);\n-  if (payload.next_key) claimRegistry.get(payload.next_key)?.(true);\n+listen(\"focus-changed\", ({ payload }) => {\n+  const { prev_key, next_key } = payload; // window_label is for filtering if needed\n+  if (prev_key) claimRegistry.get(prev_key)?.(false);\n+  if (next_key) claimRegistry.get(next_key)?.(true);\n });\n ```\n\n-Since each Tauri window has its own React tree and its own claim registry, the registry only contains keys from that window. A `focus-changed` event for another window's key is a no-op here.\n+Since each Tauri window has its own React tree and its own claim registry, the registry will only contain keys from that window. A `focus-changed` event for another window's key is a no-op in this window's registry.\n\n-**FocusScope / Focusable / FocusZone primitives** generate their `SpatialKey` via `useRef(SpatialKey(ulid())).current`. Branded helpers make the ULID typed at creation.\n+**FocusScope**:\n+- Generates SpatialKey (ULID): `const key = useRef(ulid()).current`\n+- Registers in claim registry: `registry.set(key, setIsFocused)`\n+- On click: `invoke(\"spatial_focus\", { key })` — fire and forget\n+- Local `const [isFocused, setIsFocused] = useState(false)` driven by claim callback\n\n-### Rust state\n+**No focusedMoniker state in React.** React doesn't track who's focused — it just responds to events from Rust.\n\n-```rust\n-pub struct SpatialState {\n-    registry: SpatialRegistry,\n-    focus_by_window: HashMap<WindowLabel, SpatialKey>,\n-}\n+### Rust side\n\n-impl SpatialState {\n-    pub fn focus(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n-    pub fn navigate(&mut self, window: WindowLabel, key: SpatialKey, direction: Direction) -> Option<FocusChangedEvent> { /* ... */ }\n-    pub fn handle_unregister(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n-}\n-```\n+**`SpatialState`**:\n+- `focus_by_window: HashMap<WindowLabel, SpatialKey>` — one focused key per window\n+- `register_entry(key, moniker, rect, layer_key, window_label)` stores which window each entry belongs to (derived via the entry's layer's window_label — see card 2)\n+- On `spatial_focus(window_label, key)`: update `focus_by_window[window_label]` → emit event with `window_label`\n+- On `spatial_navigate(window_label, key, direction)`: resolve → update → emit event with `window_label`\n+- On `spatial_unregister(key)`: if key is the focused key for any window, clear that window's focus → emit event\n\n-Every method signature uses newtypes. `FocusChangedEvent` is returned (not side-effected) so tests don't need Tauri mocking.\n+### Files to modify\n+- `swissarmyhammer-commands/src/spatial.rs` (new) or `spatial/mod.rs` — newtypes, `SpatialState`\n+- `swissarmyhammer-kanban/src/commands/` — Tauri commands for spatial_*\n+- `kanban-app/ui/src/lib/entity-focus-context.tsx` — claim registry, global event listener, remove useState\n+- `kanban-app/ui/src/components/focus-scope.tsx` — key generation, claim registration, fire-and-forget invokes\n\n ### Subtasks\n-- [ ] Define TS branded types for `WindowLabel`/`SpatialKey`/`LayerKey`/`Moniker`/`LayerName`/`Pixels`\n-- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to `SpatialState`\n-- [ ] Emit `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n-- [ ] Tauri commands derive `WindowLabel` from the `tauri::Window` parameter\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n+- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to spatial state\n+- [ ] Emit `focus-changed` events carrying `window_label`, `prev_key`, `next_key`\n+- [ ] Tauri commands read `window_label` from the `tauri::Window` parameter (not from React args)\n - [ ] Add claim registry `Map<SpatialKey, (focused: boolean) => void>` to EntityFocusProvider\n-- [ ] Add global `listen(\"focus-changed\")` handler\n-- [ ] Primitive components (see separate React-primitives card) use branded ULIDs\n+- [ ] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n+- [ ] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n - [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n\n ## Acceptance Criteria\n-- [ ] No signature in any added Rust type/method/command uses bare `String` or `f64`\n-- [ ] TS branded types mirror each Rust newtype; no `string`/`number` in spatial types' signatures\n-- [ ] Focus is per window; `focus_by_window[A]` and `focus_by_window[B]` independent\n-- [ ] Focus change in window A doesn't re-render scopes in window B\n-- [ ] All Tauri invokes return Ok/Err only; no focus data in return values\n-- [ ] `focus-changed` event carries newtyped fields\n-- [ ] No `focusedMoniker` state in React\n-- [ ] `cargo test` and `pnpm vitest run` pass\n+- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes, not raw strings, in all new spatial code\n+- [ ] Focus is tracked per window — `focus_by_window[window_A]` and `focus_by_window[window_B]` are independent\n+- [ ] Focus change in window A does not re-render FocusScopes in window B (each window's registry only has its own keys)\n+- [ ] All Tauri invokes return Ok/Err only — no focus data in return values\n+- [ ] Focus changes flow through Rust event: click → Rust → event → React claim\n+- [ ] No `focusedMoniker` state in React — Rust owns focus state\n+- [ ] Event listener cleaned up on EntityFocusProvider unmount\n+- [ ] `cargo test` passes, `pnpm vitest run` passes\n\n ## Tests\n\n-### Rust\n+### Rust unit tests\n\n ```rust\n #[test]\n-fn focus_updates_per_window_state() {\n-    // spatial_focus(WindowLabel(\"main\".into()), SpatialKey(\"k1\".into()))\n-    // → state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n-    // → event.window_label == \"main\", next_key == Some(\"k1\")\n+fn spatial_focus_updates_per_window_state_and_emits_event() {\n+    // Given: empty state\n+    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k1\"))\n+    // Then: state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n+    // And: event == FocusChanged { window_label: \"main\", prev_key: None, next_key: Some(\"k1\") }\n+}\n+\n+#[test]\n+fn focus_in_window_a_does_not_affect_window_b() {\n+    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }\n+    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k3\"))\n+    // Then: focus_by_window == { \"main\" => \"k3\", \"popup\" => \"k2\" }\n+    // And: event window_label == \"main\" (not \"popup\")\n }\n\n #[test]\n-fn focus_in_window_a_does_not_affect_window_b() { /* ... */ }\n+fn spatial_unregister_focused_key_clears_that_window_only() {\n+    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }, k1 is registered\n+    // When: spatial_unregister(SpatialKey(\"k1\"))\n+    // Then: focus_by_window == { \"popup\" => \"k2\" }\n+    // And: event == FocusChanged { window_label: \"main\", prev_key: Some(\"k1\"), next_key: None }\n+}\n\n #[test]\n-fn unregister_focused_clears_that_window_only() { /* ... */ }\n+fn spatial_unregister_non_focused_key_no_event() {\n+    // Given: focus_by_window[\"main\"] == \"k1\", k2 is registered\n+    // When: spatial_unregister(SpatialKey(\"k2\"))\n+    // Then: focus unchanged, no event emitted\n+}\n\n #[test]\n-fn event_carries_moniker_for_next_key() {\n-    // FocusChangedEvent.next_moniker is Some(entry.moniker) when next_key is Some\n+fn newtypes_are_serde_transparent() {\n+    // SpatialKey(\"abc\") serializes as JSON string \"abc\", not {\"0\":\"abc\"}\n }\n ```\n\n-### React\n+### React unit tests (`entity-focus-context.test.tsx`)\n+\n+```\n+test: \"claim registry only responds to events for keys it knows about\"\n+  setup: render EntityFocusProvider with FocusScope (key-A)\n+  act: fire focus-changed event { window_label: \"popup\", next_key: \"key-B\" }\n+  assert: FocusScope A is not focused (key-B not in this window's registry)\n+\n+test: \"FocusScope click invokes spatial_focus with its key\"\n+  setup: render FocusScope inside EntityFocusProvider + FocusLayer\n+  act: click the FocusScope element\n+  assert: invoke called with (\"spatial_focus\", { key: <the ULID> })\n\n-- [ ] Claim registry ignores events for unknown keys\n-- [ ] FocusScope/Focusable click invokes `spatial_focus` with its branded `SpatialKey`\n-- [ ] Provider unmount removes the listener\n-- [ ] Scope unmount removes from claim registry\n+test: \"EntityFocusProvider unmount cleans up event listener\"\n+test: \"FocusScope unmount removes from claim registry\"\n+```\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPZWQS27AGZ3D4VYZJACV70V","timestamp":"2026-04-24T14:01:43.751790Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,170 +1,159 @@\n ## What\n\n-Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by spatial key. Rust owns all focus state — and focus is tracked **per window**, because the app is multi-window (see `UIState.windows: HashMap<String, WindowState>` and the per-window `inspector_stack`).\n+Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by `SpatialKey`. Rust owns all focus state — tracked **per window** because the app is multi-window (see `UIState.windows`, per-window `inspector_stack`). Every signature uses newtypes, not bare primitives.\n\n ### Core principle\n\n - **Rust owns focus state.** React is a dumb renderer that responds to events.\n-- **Focus is per window.** Each Tauri window has its own focused element. OS window focus picks which one is \"the\" focus at any moment.\n-- **Focus identity = SpatialKey (ULID).** Not moniker. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n+- **Focus is per window.** Each Tauri window has its own focused element.\n+- **Focus identity = `SpatialKey` (ULID).** Not `Moniker`. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n\n ### Newtypes (Rust)\n\n-Introduce three newtypes in a new module `swissarmyhammer-commands/src/spatial/keys.rs` (or inline in `spatial.rs`):\n+Shared with card `01KNQXW7HH...`. Referenced here so this card's signatures are self-contained.\n\n ```rust\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct WindowLabel(pub String);\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct SpatialKey(pub String);\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct LayerKey(pub String);\n+#[serde(transparent)] pub struct WindowLabel(pub String);\n+#[serde(transparent)] pub struct SpatialKey(pub String);\n+#[serde(transparent)] pub struct LayerKey(pub String);\n+#[serde(transparent)] pub struct Moniker(pub String);\n ```\n\n-`#[serde(transparent)]` means wire format is still a plain string — React sends/receives strings unchanged. The existing `String` window labels in `UIState` are left alone for now; new spatial code uses `WindowLabel`.\n+No `String` appears in any signature below.\n\n ### API surface\n\n+```rust\n+// React → Rust. window_label is derived from tauri::Window, never passed from React.\n+async fn spatial_register_focusable(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n+async fn spatial_register_zone(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n+async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n+async fn spatial_focus(window: tauri::Window, key: SpatialKey) -> Result<()>;\n+async fn spatial_navigate(window: tauri::Window, key: SpatialKey, direction: Direction) -> Result<()>;\n+async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: LayerName, parent: Option<LayerKey>) -> Result<()>;\n+async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n-React → Rust (all return Ok/Err):\n-  spatial_register(key: SpatialKey, moniker: String, rect: Rect, layer_key: LayerKey)\n-  spatial_unregister(key: SpatialKey)\n-  spatial_navigate(key: SpatialKey, direction: Direction)\n-  spatial_focus(key: SpatialKey)\n-  spatial_push_layer(key: LayerKey, name: String, parent_key: Option<LayerKey>)\n-  spatial_pop_layer(key: LayerKey)\n-\n-Tauri commands derive window_label from the invoking webview (tauri::Window parameter), not\n-from React — React never has to pass it.\n\n-Rust → React (event):\n-  \"focus-changed\" {\n-    window_label: WindowLabel,\n-    prev_key: Option<SpatialKey>,\n-    next_key: Option<SpatialKey>,\n-  }\n+```rust\n+// Rust → React (Tauri event)\n+#[derive(Serialize)]\n+pub struct FocusChangedEvent {\n+    pub window_label: WindowLabel,\n+    pub prev_key: Option<SpatialKey>,\n+    pub next_key: Option<SpatialKey>,\n+    pub next_moniker: Option<Moniker>,   // convenience for React — saves a lookup\n+}\n ```\n\n ### React side\n\n+**Branded-type parity with Rust newtypes.** TypeScript gets its own thin branded-string types so `SpatialKey`, `LayerKey`, `Moniker`, `WindowLabel` can't be mixed up at compile time:\n+\n+```typescript\n+// kanban-app/ui/src/types/spatial.ts\n+export type WindowLabel = string & { readonly __tag: \"WindowLabel\" };\n+export type SpatialKey  = string & { readonly __tag: \"SpatialKey\" };\n+export type LayerKey    = string & { readonly __tag: \"LayerKey\" };\n+export type Moniker     = string & { readonly __tag: \"Moniker\" };\n+export type LayerName   = string & { readonly __tag: \"LayerName\" };\n+export type Pixels      = number & { readonly __tag: \"Pixels\" };\n+\n+// Brand helpers\n+export const WindowLabel = (s: string) => s as WindowLabel;\n+export const SpatialKey  = (s: string) => s as SpatialKey;\n+export const LayerKey    = (s: string) => s as LayerKey;\n+export const Moniker     = (s: string) => s as Moniker;\n+export const LayerName   = (s: string) => s as LayerName;\n+export const Pixels      = (n: number) => n as Pixels;\n+```\n+\n **Global event listener** (in EntityFocusProvider):\n\n ```typescript\n-listen(\"focus-changed\", ({ payload }) => {\n-  const { prev_key, next_key } = payload; // window_label is for filtering if needed\n-  if (prev_key) claimRegistry.get(prev_key)?.(false);\n-  if (next_key) claimRegistry.get(next_key)?.(true);\n+type FocusChangedPayload = {\n+  window_label: WindowLabel;\n+  prev_key: SpatialKey | null;\n+  next_key: SpatialKey | null;\n+  next_moniker: Moniker | null;\n+};\n+\n+listen<FocusChangedPayload>(\"focus-changed\", ({ payload }) => {\n+  if (payload.prev_key) claimRegistry.get(payload.prev_key)?.(false);\n+  if (payload.next_key) claimRegistry.get(payload.next_key)?.(true);\n });\n ```\n\n-Since each Tauri window has its own React tree and its own claim registry, the registry will only contain keys from that window. A `focus-changed` event for another window's key is a no-op in this window's registry.\n+Since each Tauri window has its own React tree and its own claim registry, the registry only contains keys from that window. A `focus-changed` event for another window's key is a no-op here.\n\n-**FocusScope**:\n-- Generates SpatialKey (ULID): `const key = useRef(ulid()).current`\n-- Registers in claim registry: `registry.set(key, setIsFocused)`\n-- On click: `invoke(\"spatial_focus\", { key })` — fire and forget\n-- Local `const [isFocused, setIsFocused] = useState(false)` driven by claim callback\n+**FocusScope / Focusable / FocusZone primitives** generate their `SpatialKey` via `useRef(SpatialKey(ulid())).current`. Branded helpers make the ULID typed at creation.\n\n-**No focusedMoniker state in React.** React doesn't track who's focused — it just responds to events from Rust.\n+### Rust state\n\n-### Rust side\n+```rust\n+pub struct SpatialState {\n+    registry: SpatialRegistry,\n+    focus_by_window: HashMap<WindowLabel, SpatialKey>,\n+}\n\n-**`SpatialState`**:\n-- `focus_by_window: HashMap<WindowLabel, SpatialKey>` — one focused key per window\n-- `register_entry(key, moniker, rect, layer_key, window_label)` stores which window each entry belongs to (derived via the entry's layer's window_label — see card 2)\n-- On `spatial_focus(window_label, key)`: update `focus_by_window[window_label]` → emit event with `window_label`\n-- On `spatial_navigate(window_label, key, direction)`: resolve → update → emit event with `window_label`\n-- On `spatial_unregister(key)`: if key is the focused key for any window, clear that window's focus → emit event\n+impl SpatialState {\n+    pub fn focus(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n+    pub fn navigate(&mut self, window: WindowLabel, key: SpatialKey, direction: Direction) -> Option<FocusChangedEvent> { /* ... */ }\n+    pub fn handle_unregister(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n+}\n+```\n\n-### Files to modify\n-- `swissarmyhammer-commands/src/spatial.rs` (new) or `spatial/mod.rs` — newtypes, `SpatialState`\n-- `swissarmyhammer-kanban/src/commands/` — Tauri commands for spatial_*\n-- `kanban-app/ui/src/lib/entity-focus-context.tsx` — claim registry, global event listener, remove useState\n-- `kanban-app/ui/src/components/focus-scope.tsx` — key generation, claim registration, fire-and-forget invokes\n+Every method signature uses newtypes. `FocusChangedEvent` is returned (not side-effected) so tests don't need Tauri mocking.\n\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n-- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to spatial state\n-- [ ] Emit `focus-changed` events carrying `window_label`, `prev_key`, `next_key`\n-- [ ] Tauri commands read `window_label` from the `tauri::Window` parameter (not from React args)\n+- [ ] Define TS branded types for `WindowLabel`/`SpatialKey`/`LayerKey`/`Moniker`/`LayerName`/`Pixels`\n+- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to `SpatialState`\n+- [ ] Emit `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n+- [ ] Tauri commands derive `WindowLabel` from the `tauri::Window` parameter\n - [ ] Add claim registry `Map<SpatialKey, (focused: boolean) => void>` to EntityFocusProvider\n-- [ ] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n-- [ ] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n+- [ ] Add global `listen(\"focus-changed\")` handler\n+- [ ] Primitive components (see separate React-primitives card) use branded ULIDs\n - [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n\n ## Acceptance Criteria\n-- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes, not raw strings, in all new spatial code\n-- [ ] Focus is tracked per window — `focus_by_window[window_A]` and `focus_by_window[window_B]` are independent\n-- [ ] Focus change in window A does not re-render FocusScopes in window B (each window's registry only has its own keys)\n-- [ ] All Tauri invokes return Ok/Err only — no focus data in return values\n-- [ ] Focus changes flow through Rust event: click → Rust → event → React claim\n-- [ ] No `focusedMoniker` state in React — Rust owns focus state\n-- [ ] Event listener cleaned up on EntityFocusProvider unmount\n-- [ ] `cargo test` passes, `pnpm vitest run` passes\n+- [ ] No signature in any added Rust type/method/command uses bare `String` or `f64`\n+- [ ] TS branded types mirror each Rust newtype; no `string`/`number` in spatial types' signatures\n+- [ ] Focus is per window; `focus_by_window[A]` and `focus_by_window[B]` independent\n+- [ ] Focus change in window A doesn't re-render scopes in window B\n+- [ ] All Tauri invokes return Ok/Err only; no focus data in return values\n+- [ ] `focus-changed` event carries newtyped fields\n+- [ ] No `focusedMoniker` state in React\n+- [ ] `cargo test` and `pnpm vitest run` pass\n\n ## Tests\n\n-### Rust unit tests\n+### Rust\n\n ```rust\n #[test]\n-fn spatial_focus_updates_per_window_state_and_emits_event() {\n-    // Given: empty state\n-    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k1\"))\n-    // Then: state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n-    // And: event == FocusChanged { window_label: \"main\", prev_key: None, next_key: Some(\"k1\") }\n+fn focus_updates_per_window_state() {\n+    // spatial_focus(WindowLabel(\"main\".into()), SpatialKey(\"k1\".into()))\n+    // → state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n+    // → event.window_label == \"main\", next_key == Some(\"k1\")\n }\n\n #[test]\n-fn focus_in_window_a_does_not_affect_window_b() {\n-    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }\n-    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k3\"))\n-    // Then: focus_by_window == { \"main\" => \"k3\", \"popup\" => \"k2\" }\n-    // And: event window_label == \"main\" (not \"popup\")\n-}\n+fn focus_in_window_a_does_not_affect_window_b() { /* ... */ }\n\n #[test]\n-fn spatial_unregister_focused_key_clears_that_window_only() {\n-    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }, k1 is registered\n-    // When: spatial_unregister(SpatialKey(\"k1\"))\n-    // Then: focus_by_window == { \"popup\" => \"k2\" }\n-    // And: event == FocusChanged { window_label: \"main\", prev_key: Some(\"k1\"), next_key: None }\n-}\n+fn unregister_focused_clears_that_window_only() { /* ... */ }\n\n #[test]\n-fn spatial_unregister_non_focused_key_no_event() {\n-    // Given: focus_by_window[\"main\"] == \"k1\", k2 is registered\n-    // When: spatial_unregister(SpatialKey(\"k2\"))\n-    // Then: focus unchanged, no event emitted\n+fn event_carries_moniker_for_next_key() {\n+    // FocusChangedEvent.next_moniker is Some(entry.moniker) when next_key is Some\n }\n-\n-#[test]\n-fn newtypes_are_serde_transparent() {\n-    // SpatialKey(\"abc\") serializes as JSON string \"abc\", not {\"0\":\"abc\"}\n-}\n ```\n\n-### React unit tests (`entity-focus-context.test.tsx`)\n+### React\n\n-```\n-test: \"claim registry only responds to events for keys it knows about\"\n-  setup: render EntityFocusProvider with FocusScope (key-A)\n-  act: fire focus-changed event { window_label: \"popup\", next_key: \"key-B\" }\n-  assert: FocusScope A is not focused (key-B not in this window's registry)\n-\n-test: \"FocusScope click invokes spatial_focus with its key\"\n-  setup: render FocusScope inside EntityFocusProvider + FocusLayer\n-  act: click the FocusScope element\n-  assert: invoke called with (\"spatial_focus\", { key: <the ULID> })\n-\n-test: \"EntityFocusProvider unmount cleans up event listener\"\n-test: \"FocusScope unmount removes from claim registry\"\n-```\n+- [ ] Claim registry ignores events for unknown keys\n+- [ ] FocusScope/Focusable click invokes `spatial_focus` with its branded `SpatialKey`\n+- [ ] Provider unmount removes the listener\n+- [ ] Scope unmount removes from claim registry\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,159 +1,170 @@\n ## What\n\n-Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by `SpatialKey`. Rust owns all focus state — tracked **per window** because the app is multi-window (see `UIState.windows`, per-window `inspector_stack`). Every signature uses newtypes, not bare primitives.\n+Replace the current `focusedMoniker` React state with an event-driven **focus claim registry** keyed by spatial key. Rust owns all focus state — and focus is tracked **per window**, because the app is multi-window (see `UIState.windows: HashMap<String, WindowState>` and the per-window `inspector_stack`).\n\n ### Core principle\n\n - **Rust owns focus state.** React is a dumb renderer that responds to events.\n-- **Focus is per window.** Each Tauri window has its own focused element.\n-- **Focus identity = `SpatialKey` (ULID).** Not `Moniker`. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n+- **Focus is per window.** Each Tauri window has its own focused element. OS window focus picks which one is \"the\" focus at any moment.\n+- **Focus identity = SpatialKey (ULID).** Not moniker. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n\n ### Newtypes (Rust)\n\n-Shared with card `01KNQXW7HH...`. Referenced here so this card's signatures are self-contained.\n+Introduce three newtypes in a new module `swissarmyhammer-commands/src/spatial/keys.rs` (or inline in `spatial.rs`):\n\n ```rust\n-#[serde(transparent)] pub struct WindowLabel(pub String);\n-#[serde(transparent)] pub struct SpatialKey(pub String);\n-#[serde(transparent)] pub struct LayerKey(pub String);\n-#[serde(transparent)] pub struct Moniker(pub String);\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)]\n+pub struct WindowLabel(pub String);\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)]\n+pub struct SpatialKey(pub String);\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)]\n+pub struct LayerKey(pub String);\n ```\n\n-No `String` appears in any signature below.\n+`#[serde(transparent)]` means wire format is still a plain string — React sends/receives strings unchanged. The existing `String` window labels in `UIState` are left alone for now; new spatial code uses `WindowLabel`.\n\n ### API surface\n\n-```rust\n-// React → Rust. window_label is derived from tauri::Window, never passed from React.\n-async fn spatial_register_focusable(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n-async fn spatial_register_zone(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n-async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n-async fn spatial_focus(window: tauri::Window, key: SpatialKey) -> Result<()>;\n-async fn spatial_navigate(window: tauri::Window, key: SpatialKey, direction: Direction) -> Result<()>;\n-async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: LayerName, parent: Option<LayerKey>) -> Result<()>;\n-async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n+React → Rust (all return Ok/Err):\n+  spatial_register(key: SpatialKey, moniker: String, rect: Rect, layer_key: LayerKey)\n+  spatial_unregister(key: SpatialKey)\n+  spatial_navigate(key: SpatialKey, direction: Direction)\n+  spatial_focus(key: SpatialKey)\n+  spatial_push_layer(key: LayerKey, name: String, parent_key: Option<LayerKey>)\n+  spatial_pop_layer(key: LayerKey)\n+\n+Tauri commands derive window_label from the invoking webview (tauri::Window parameter), not\n+from React — React never has to pass it.\n\n-```rust\n-// Rust → React (Tauri event)\n-#[derive(Serialize)]\n-pub struct FocusChangedEvent {\n-    pub window_label: WindowLabel,\n-    pub prev_key: Option<SpatialKey>,\n-    pub next_key: Option<SpatialKey>,\n-    pub next_moniker: Option<Moniker>,   // convenience for React — saves a lookup\n-}\n+Rust → React (event):\n+  \"focus-changed\" {\n+    window_label: WindowLabel,\n+    prev_key: Option<SpatialKey>,\n+    next_key: Option<SpatialKey>,\n+  }\n ```\n\n ### React side\n\n-**Branded-type parity with Rust newtypes.** TypeScript gets its own thin branded-string types so `SpatialKey`, `LayerKey`, `Moniker`, `WindowLabel` can't be mixed up at compile time:\n-\n-```typescript\n-// kanban-app/ui/src/types/spatial.ts\n-export type WindowLabel = string & { readonly __tag: \"WindowLabel\" };\n-export type SpatialKey  = string & { readonly __tag: \"SpatialKey\" };\n-export type LayerKey    = string & { readonly __tag: \"LayerKey\" };\n-export type Moniker     = string & { readonly __tag: \"Moniker\" };\n-export type LayerName   = string & { readonly __tag: \"LayerName\" };\n-export type Pixels      = number & { readonly __tag: \"Pixels\" };\n-\n-// Brand helpers\n-export const WindowLabel = (s: string) => s as WindowLabel;\n-export const SpatialKey  = (s: string) => s as SpatialKey;\n-export const LayerKey    = (s: string) => s as LayerKey;\n-export const Moniker     = (s: string) => s as Moniker;\n-export const LayerName   = (s: string) => s as LayerName;\n-export const Pixels      = (n: number) => n as Pixels;\n-```\n-\n **Global event listener** (in EntityFocusProvider):\n\n ```typescript\n-type FocusChangedPayload = {\n-  window_label: WindowLabel;\n-  prev_key: SpatialKey | null;\n-  next_key: SpatialKey | null;\n-  next_moniker: Moniker | null;\n-};\n-\n-listen<FocusChangedPayload>(\"focus-changed\", ({ payload }) => {\n-  if (payload.prev_key) claimRegistry.get(payload.prev_key)?.(false);\n-  if (payload.next_key) claimRegistry.get(payload.next_key)?.(true);\n+listen(\"focus-changed\", ({ payload }) => {\n+  const { prev_key, next_key } = payload; // window_label is for filtering if needed\n+  if (prev_key) claimRegistry.get(prev_key)?.(false);\n+  if (next_key) claimRegistry.get(next_key)?.(true);\n });\n ```\n\n-Since each Tauri window has its own React tree and its own claim registry, the registry only contains keys from that window. A `focus-changed` event for another window's key is a no-op here.\n+Since each Tauri window has its own React tree and its own claim registry, the registry will only contain keys from that window. A `focus-changed` event for another window's key is a no-op in this window's registry.\n\n-**FocusScope / Focusable / FocusZone primitives** generate their `SpatialKey` via `useRef(SpatialKey(ulid())).current`. Branded helpers make the ULID typed at creation.\n+**FocusScope**:\n+- Generates SpatialKey (ULID): `const key = useRef(ulid()).current`\n+- Registers in claim registry: `registry.set(key, setIsFocused)`\n+- On click: `invoke(\"spatial_focus\", { key })` — fire and forget\n+- Local `const [isFocused, setIsFocused] = useState(false)` driven by claim callback\n\n-### Rust state\n+**No focusedMoniker state in React.** React doesn't track who's focused — it just responds to events from Rust.\n\n-```rust\n-pub struct SpatialState {\n-    registry: SpatialRegistry,\n-    focus_by_window: HashMap<WindowLabel, SpatialKey>,\n-}\n+### Rust side\n\n-impl SpatialState {\n-    pub fn focus(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n-    pub fn navigate(&mut self, window: WindowLabel, key: SpatialKey, direction: Direction) -> Option<FocusChangedEvent> { /* ... */ }\n-    pub fn handle_unregister(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n-}\n-```\n+**`SpatialState`**:\n+- `focus_by_window: HashMap<WindowLabel, SpatialKey>` — one focused key per window\n+- `register_entry(key, moniker, rect, layer_key, window_label)` stores which window each entry belongs to (derived via the entry's layer's window_label — see card 2)\n+- On `spatial_focus(window_label, key)`: update `focus_by_window[window_label]` → emit event with `window_label`\n+- On `spatial_navigate(window_label, key, direction)`: resolve → update → emit event with `window_label`\n+- On `spatial_unregister(key)`: if key is the focused key for any window, clear that window's focus → emit event\n\n-Every method signature uses newtypes. `FocusChangedEvent` is returned (not side-effected) so tests don't need Tauri mocking.\n+### Files to modify\n+- `swissarmyhammer-commands/src/spatial.rs` (new) or `spatial/mod.rs` — newtypes, `SpatialState`\n+- `swissarmyhammer-kanban/src/commands/` — Tauri commands for spatial_*\n+- `kanban-app/ui/src/lib/entity-focus-context.tsx` — claim registry, global event listener, remove useState\n+- `kanban-app/ui/src/components/focus-scope.tsx` — key generation, claim registration, fire-and-forget invokes\n\n ### Subtasks\n-- [ ] Define TS branded types for `WindowLabel`/`SpatialKey`/`LayerKey`/`Moniker`/`LayerName`/`Pixels`\n-- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to `SpatialState`\n-- [ ] Emit `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n-- [ ] Tauri commands derive `WindowLabel` from the `tauri::Window` parameter\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n+- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to spatial state\n+- [ ] Emit `focus-changed` events carrying `window_label`, `prev_key`, `next_key`\n+- [ ] Tauri commands read `window_label` from the `tauri::Window` parameter (not from React args)\n - [ ] Add claim registry `Map<SpatialKey, (focused: boolean) => void>` to EntityFocusProvider\n-- [ ] Add global `listen(\"focus-changed\")` handler\n-- [ ] Primitive components (see separate React-primitives card) use branded ULIDs\n+- [ ] Add global `listen(\"focus-changed\")` handler that claims/unclaims by key\n+- [ ] Update FocusScope: ULID key, register claim, fire-and-forget invokes\n - [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n\n ## Acceptance Criteria\n-- [ ] No signature in any added Rust type/method/command uses bare `String` or `f64`\n-- [ ] TS branded types mirror each Rust newtype; no `string`/`number` in spatial types' signatures\n-- [ ] Focus is per window; `focus_by_window[A]` and `focus_by_window[B]` independent\n-- [ ] Focus change in window A doesn't re-render scopes in window B\n-- [ ] All Tauri invokes return Ok/Err only; no focus data in return values\n-- [ ] `focus-changed` event carries newtyped fields\n-- [ ] No `focusedMoniker` state in React\n-- [ ] `cargo test` and `pnpm vitest run` pass\n+- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes, not raw strings, in all new spatial code\n+- [ ] Focus is tracked per window — `focus_by_window[window_A]` and `focus_by_window[window_B]` are independent\n+- [ ] Focus change in window A does not re-render FocusScopes in window B (each window's registry only has its own keys)\n+- [ ] All Tauri invokes return Ok/Err only — no focus data in return values\n+- [ ] Focus changes flow through Rust event: click → Rust → event → React claim\n+- [ ] No `focusedMoniker` state in React — Rust owns focus state\n+- [ ] Event listener cleaned up on EntityFocusProvider unmount\n+- [ ] `cargo test` passes, `pnpm vitest run` passes\n\n ## Tests\n\n-### Rust\n+### Rust unit tests\n\n ```rust\n #[test]\n-fn focus_updates_per_window_state() {\n-    // spatial_focus(WindowLabel(\"main\".into()), SpatialKey(\"k1\".into()))\n-    // → state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n-    // → event.window_label == \"main\", next_key == Some(\"k1\")\n+fn spatial_focus_updates_per_window_state_and_emits_event() {\n+    // Given: empty state\n+    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k1\"))\n+    // Then: state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n+    // And: event == FocusChanged { window_label: \"main\", prev_key: None, next_key: Some(\"k1\") }\n+}\n+\n+#[test]\n+fn focus_in_window_a_does_not_affect_window_b() {\n+    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }\n+    // When: spatial_focus(WindowLabel(\"main\"), SpatialKey(\"k3\"))\n+    // Then: focus_by_window == { \"main\" => \"k3\", \"popup\" => \"k2\" }\n+    // And: event window_label == \"main\" (not \"popup\")\n }\n\n #[test]\n-fn focus_in_window_a_does_not_affect_window_b() { /* ... */ }\n+fn spatial_unregister_focused_key_clears_that_window_only() {\n+    // Given: focus_by_window == { \"main\" => \"k1\", \"popup\" => \"k2\" }, k1 is registered\n+    // When: spatial_unregister(SpatialKey(\"k1\"))\n+    // Then: focus_by_window == { \"popup\" => \"k2\" }\n+    // And: event == FocusChanged { window_label: \"main\", prev_key: Some(\"k1\"), next_key: None }\n+}\n\n #[test]\n-fn unregister_focused_clears_that_window_only() { /* ... */ }\n+fn spatial_unregister_non_focused_key_no_event() {\n+    // Given: focus_by_window[\"main\"] == \"k1\", k2 is registered\n+    // When: spatial_unregister(SpatialKey(\"k2\"))\n+    // Then: focus unchanged, no event emitted\n+}\n\n #[test]\n-fn event_carries_moniker_for_next_key() {\n-    // FocusChangedEvent.next_moniker is Some(entry.moniker) when next_key is Some\n+fn newtypes_are_serde_transparent() {\n+    // SpatialKey(\"abc\") serializes as JSON string \"abc\", not {\"0\":\"abc\"}\n }\n ```\n\n-### React\n+### React unit tests (`entity-focus-context.test.tsx`)\n+\n+```\n+test: \"claim registry only responds to events for keys it knows about\"\n+  setup: render EntityFocusProvider with FocusScope (key-A)\n+  act: fire focus-changed event { window_label: \"popup\", next_key: \"key-B\" }\n+  assert: FocusScope A is not focused (key-B not in this window's registry)\n+\n+test: \"FocusScope click invokes spatial_focus with its key\"\n+  setup: render FocusScope inside EntityFocusProvider + FocusLayer\n+  act: click the FocusScope element\n+  assert: invoke called with (\"spatial_focus\", { key: <the ULID> })\n\n-- [ ] Claim registry ignores events for unknown keys\n-- [ ] FocusScope/Focusable click invokes `spatial_focus` with its branded `SpatialKey`\n-- [ ] Provider unmount removes the listener\n-- [ ] Scope unmount removes from claim registry\n+test: \"EntityFocusProvider unmount cleans up event listener\"\n+test: \"FocusScope unmount removes from claim registry\"\n+```\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Focus claim registry: per-window, event-driven, Rust owns state\n\\ No newline at end of file\n+Focus claim registry: per-window, event-driven, Rust owns state (newtype-only signatures)\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Focus claim registry: per-window, event-driven, Rust owns state (newtype-only signatures)\n\\ No newline at end of file\n+Focus claim registry: per-window, event-driven, Rust owns state\n\\ No newline at end of file\n"}]]}
{"id":"01KQ026YKM7NMPCGTJT029CR4T","timestamp":"2026-04-24T15:37:23.828455Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -14,77 +14,73 @@\n\n - **Rust owns focus state.** React is a dumb renderer that responds to events.\n - **Focus is per window.** Each Tauri window has its own focused element.\n-- **Focus identity = `SpatialKey` (ULID).** Not `Moniker`. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n+- **Focus identity = `SpatialKey` (ULID).** Not `Moniker`. The claim registry is `Map<SpatialKey, callback>`.\n+\n+### Crate placement\n+\n+State lives in `swissarmyhammer-kanban/src/focus/state.rs` alongside the registry from card `01KNQXW7HH...`. Tauri adapters live in `kanban-app/src/commands.rs`. This follows the refactor pattern from commit `b81336d42` (headless business logic in the kanban crate, Tauri commands as thin adapters).\n\n-### Newtypes (Rust)\n+### Newtypes — use `define_id!`\n\n-Shared with card `01KNQXW7HH...`. Referenced here so this card's signatures are self-contained.\n+All string-valued newtypes are defined via `swissarmyhammer_common::define_id!` in `focus/types.rs` (see card `01KNQXW7HH...` for full definitions). Referenced here for self-containment:\n\n ```rust\n-#[serde(transparent)] pub struct WindowLabel(pub String);\n-#[serde(transparent)] pub struct SpatialKey(pub String);\n-#[serde(transparent)] pub struct LayerKey(pub String);\n-#[serde(transparent)] pub struct Moniker(pub String);\n+define_id!(WindowLabel, \"Tauri window label\");\n+define_id!(SpatialKey, \"ULID per scope mount\");\n+define_id!(LayerKey, \"ULID per layer mount\");\n+define_id!(Moniker, \"Entity focus identity\");\n ```\n\n-No `String` appears in any signature below.\n+No bare `String` appears in any signature below.\n\n ### API surface\n\n ```rust\n-// React → Rust. window_label is derived from tauri::Window, never passed from React.\n-async fn spatial_register_focusable(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n-async fn spatial_register_zone(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n-async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n-async fn spatial_focus(window: tauri::Window, key: SpatialKey) -> Result<()>;\n-async fn spatial_navigate(window: tauri::Window, key: SpatialKey, direction: Direction) -> Result<()>;\n-async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: LayerName, parent: Option<LayerKey>) -> Result<()>;\n-async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n+// Tauri commands in kanban-app/src/commands.rs; all derive WindowLabel from tauri::Window.\n+async fn spatial_register_focusable(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<(), String>;\n+async fn spatial_register_zone(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<(), String>;\n+async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<(), String>;\n+async fn spatial_focus(window: tauri::Window, key: SpatialKey) -> Result<(), String>;\n+async fn spatial_navigate(window: tauri::Window, key: SpatialKey, direction: Direction) -> Result<(), String>;\n+async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: LayerName, parent: Option<LayerKey>) -> Result<(), String>;\n+async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<(), String>;\n ```\n\n ```rust\n-// Rust → React (Tauri event)\n+// Emitted from Rust to React\n #[derive(Serialize)]\n pub struct FocusChangedEvent {\n     pub window_label: WindowLabel,\n     pub prev_key: Option<SpatialKey>,\n     pub next_key: Option<SpatialKey>,\n-    pub next_moniker: Option<Moniker>,   // convenience for React — saves a lookup\n+    pub next_moniker: Option<Moniker>,\n }\n ```\n\n-### React side\n+### Rust state — `swissarmyhammer-kanban/src/focus/state.rs`\n\n-**Branded-type parity with Rust newtypes.** TypeScript gets its own thin branded-string types so `SpatialKey`, `LayerKey`, `Moniker`, `WindowLabel` can't be mixed up at compile time:\n-\n-```typescript\n-// kanban-app/ui/src/types/spatial.ts\n-export type WindowLabel = string & { readonly __tag: \"WindowLabel\" };\n-export type SpatialKey  = string & { readonly __tag: \"SpatialKey\" };\n-export type LayerKey    = string & { readonly __tag: \"LayerKey\" };\n-export type Moniker     = string & { readonly __tag: \"Moniker\" };\n-export type LayerName   = string & { readonly __tag: \"LayerName\" };\n-export type Pixels      = number & { readonly __tag: \"Pixels\" };\n+```rust\n+pub struct SpatialState {\n+    registry: SpatialRegistry,\n+    focus_by_window: HashMap<WindowLabel, SpatialKey>,\n+}\n\n-// Brand helpers\n-export const WindowLabel = (s: string) => s as WindowLabel;\n-export const SpatialKey  = (s: string) => s as SpatialKey;\n-export const LayerKey    = (s: string) => s as LayerKey;\n-export const Moniker     = (s: string) => s as Moniker;\n-export const LayerName   = (s: string) => s as LayerName;\n-export const Pixels      = (n: number) => n as Pixels;\n+impl SpatialState {\n+    pub fn focus(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent>;\n+    pub fn navigate(&mut self, window: WindowLabel, key: SpatialKey, direction: Direction) -> Option<FocusChangedEvent>;\n+    pub fn handle_unregister(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent>;\n+}\n ```\n\n+Methods return `Option<FocusChangedEvent>` rather than side-effecting so tests don't need Tauri mocking — the Tauri adapter in `kanban-app/src/commands.rs` is responsible for emitting the event.\n+\n+### React side — branded types\n+\n+Parity with Rust newtypes via TypeScript branded strings. Details live in the React-primitives card (`01KPZWY4B7...`). The `types/spatial.ts` file exports `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName`, `Pixels` as branded types with brand helpers.\n+\n **Global event listener** (in EntityFocusProvider):\n\n ```typescript\n-type FocusChangedPayload = {\n-  window_label: WindowLabel;\n-  prev_key: SpatialKey | null;\n-  next_key: SpatialKey | null;\n-  next_moniker: Moniker | null;\n-};\n-\n listen<FocusChangedPayload>(\"focus-changed\", ({ payload }) => {\n   if (payload.prev_key) claimRegistry.get(payload.prev_key)?.(false);\n   if (payload.next_key) claimRegistry.get(payload.next_key)?.(true);\n@@ -91,75 +87,43 @@\n });\n ```\n\n-Since each Tauri window has its own React tree and its own claim registry, the registry only contains keys from that window. A `focus-changed` event for another window's key is a no-op here.\n+Each Tauri window has its own React tree and its own claim registry, so a `focus-changed` event for another window's key is a no-op here.\n\n-**FocusScope / Focusable / FocusZone primitives** generate their `SpatialKey` via `useRef(SpatialKey(ulid())).current`. Branded helpers make the ULID typed at creation.\n-\n-### Rust state\n+### Tests — `swissarmyhammer-kanban/tests/focus_state.rs`\n\n-```rust\n-pub struct SpatialState {\n-    registry: SpatialRegistry,\n-    focus_by_window: HashMap<WindowLabel, SpatialKey>,\n-}\n-\n-impl SpatialState {\n-    pub fn focus(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n-    pub fn navigate(&mut self, window: WindowLabel, key: SpatialKey, direction: Direction) -> Option<FocusChangedEvent> { /* ... */ }\n-    pub fn handle_unregister(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n-}\n-```\n+Headless pattern matching `swissarmyhammer-kanban/tests/resolve_focused_column.rs`.\n\n-Every method signature uses newtypes. `FocusChangedEvent` is returned (not side-effected) so tests don't need Tauri mocking.\n-\n ### Subtasks\n-- [ ] Define TS branded types for `WindowLabel`/`SpatialKey`/`LayerKey`/`Moniker`/`LayerName`/`Pixels`\n-- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to `SpatialState`\n-- [ ] Emit `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n-- [ ] Tauri commands derive `WindowLabel` from the `tauri::Window` parameter\n-- [ ] Add claim registry `Map<SpatialKey, (focused: boolean) => void>` to EntityFocusProvider\n-- [ ] Add global `listen(\"focus-changed\")` handler\n-- [ ] Primitive components (see separate React-primitives card) use branded ULIDs\n+- [ ] Define TS branded types in `kanban-app/ui/src/types/spatial.ts`\n+- [ ] Add `SpatialState` to `swissarmyhammer-kanban/src/focus/state.rs` with `focus_by_window: HashMap<WindowLabel, SpatialKey>`\n+- [ ] `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n+- [ ] Tauri commands in `kanban-app/src/commands.rs` derive `WindowLabel` from `tauri::Window`; emit `focus-changed` event\n+- [ ] Claim registry `Map<SpatialKey, (focused: boolean) => void>` in EntityFocusProvider\n+- [ ] Global `listen(\"focus-changed\")` handler\n - [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n+- [ ] Tests in `swissarmyhammer-kanban/tests/focus_state.rs`\n\n ## Acceptance Criteria\n-- [ ] No signature in any added Rust type/method/command uses bare `String` or `f64`\n-- [ ] TS branded types mirror each Rust newtype; no `string`/`number` in spatial types' signatures\n-- [ ] Focus is per window; `focus_by_window[A]` and `focus_by_window[B]` independent\n+- [ ] State lives in `swissarmyhammer-kanban/src/focus/state.rs`, not `swissarmyhammer-commands`\n+- [ ] Tauri commands live in `kanban-app/src/commands.rs`\n+- [ ] No signature uses bare `String` or `f64`\n+- [ ] TS branded types mirror Rust newtypes; no `string`/`number` in typed spatial signatures\n+- [ ] Focus is per-window; windows A and B independent\n - [ ] Focus change in window A doesn't re-render scopes in window B\n - [ ] All Tauri invokes return Ok/Err only; no focus data in return values\n-- [ ] `focus-changed` event carries newtyped fields\n-- [ ] No `focusedMoniker` state in React\n-- [ ] `cargo test` and `pnpm vitest run` pass\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm vitest run` pass\n\n ## Tests\n\n-### Rust\n-\n-```rust\n-#[test]\n-fn focus_updates_per_window_state() {\n-    // spatial_focus(WindowLabel(\"main\".into()), SpatialKey(\"k1\".into()))\n-    // → state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n-    // → event.window_label == \"main\", next_key == Some(\"k1\")\n-}\n-\n-#[test]\n-fn focus_in_window_a_does_not_affect_window_b() { /* ... */ }\n-\n-#[test]\n-fn unregister_focused_clears_that_window_only() { /* ... */ }\n-\n-#[test]\n-fn event_carries_moniker_for_next_key() {\n-    // FocusChangedEvent.next_moniker is Some(entry.moniker) when next_key is Some\n-}\n-```\n+### Rust (`swissarmyhammer-kanban/tests/focus_state.rs`)\n+- [ ] `focus` updates per-window state and returns `FocusChangedEvent` with matching `WindowLabel`\n+- [ ] focus in A doesn't affect `focus_by_window[B]`\n+- [ ] unregister of a focused key clears that window's focus only\n+- [ ] `FocusChangedEvent.next_moniker` is `Some(entry.moniker.clone())` when next_key is Some\n\n ### React\n-\n - [ ] Claim registry ignores events for unknown keys\n-- [ ] FocusScope/Focusable click invokes `spatial_focus` with its branded `SpatialKey`\n+- [ ] Scope click invokes `spatial_focus` with its branded `SpatialKey`\n - [ ] Provider unmount removes the listener\n - [ ] Scope unmount removes from claim registry\n\n","reverse_patch":"--- original\n+++ modified\n@@ -14,73 +14,77 @@\n\n - **Rust owns focus state.** React is a dumb renderer that responds to events.\n - **Focus is per window.** Each Tauri window has its own focused element.\n-- **Focus identity = `SpatialKey` (ULID).** Not `Moniker`. The claim registry is `Map<SpatialKey, callback>`.\n+- **Focus identity = `SpatialKey` (ULID).** Not `Moniker`. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n\n-### Crate placement\n+### Newtypes (Rust)\n\n-State lives in `swissarmyhammer-kanban/src/focus/state.rs` alongside the registry from card `01KNQXW7HH...`. Tauri adapters live in `kanban-app/src/commands.rs`. This follows the refactor pattern from commit `b81336d42` (headless business logic in the kanban crate, Tauri commands as thin adapters).\n-\n-### Newtypes — use `define_id!`\n-\n-All string-valued newtypes are defined via `swissarmyhammer_common::define_id!` in `focus/types.rs` (see card `01KNQXW7HH...` for full definitions). Referenced here for self-containment:\n+Shared with card `01KNQXW7HH...`. Referenced here so this card's signatures are self-contained.\n\n ```rust\n-define_id!(WindowLabel, \"Tauri window label\");\n-define_id!(SpatialKey, \"ULID per scope mount\");\n-define_id!(LayerKey, \"ULID per layer mount\");\n-define_id!(Moniker, \"Entity focus identity\");\n+#[serde(transparent)] pub struct WindowLabel(pub String);\n+#[serde(transparent)] pub struct SpatialKey(pub String);\n+#[serde(transparent)] pub struct LayerKey(pub String);\n+#[serde(transparent)] pub struct Moniker(pub String);\n ```\n\n-No bare `String` appears in any signature below.\n+No `String` appears in any signature below.\n\n ### API surface\n\n ```rust\n-// Tauri commands in kanban-app/src/commands.rs; all derive WindowLabel from tauri::Window.\n-async fn spatial_register_focusable(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<(), String>;\n-async fn spatial_register_zone(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<(), String>;\n-async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<(), String>;\n-async fn spatial_focus(window: tauri::Window, key: SpatialKey) -> Result<(), String>;\n-async fn spatial_navigate(window: tauri::Window, key: SpatialKey, direction: Direction) -> Result<(), String>;\n-async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: LayerName, parent: Option<LayerKey>) -> Result<(), String>;\n-async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<(), String>;\n+// React → Rust. window_label is derived from tauri::Window, never passed from React.\n+async fn spatial_register_focusable(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n+async fn spatial_register_zone(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n+async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n+async fn spatial_focus(window: tauri::Window, key: SpatialKey) -> Result<()>;\n+async fn spatial_navigate(window: tauri::Window, key: SpatialKey, direction: Direction) -> Result<()>;\n+async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: LayerName, parent: Option<LayerKey>) -> Result<()>;\n+async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n\n ```rust\n-// Emitted from Rust to React\n+// Rust → React (Tauri event)\n #[derive(Serialize)]\n pub struct FocusChangedEvent {\n     pub window_label: WindowLabel,\n     pub prev_key: Option<SpatialKey>,\n     pub next_key: Option<SpatialKey>,\n-    pub next_moniker: Option<Moniker>,\n+    pub next_moniker: Option<Moniker>,   // convenience for React — saves a lookup\n }\n ```\n\n-### Rust state — `swissarmyhammer-kanban/src/focus/state.rs`\n+### React side\n\n-```rust\n-pub struct SpatialState {\n-    registry: SpatialRegistry,\n-    focus_by_window: HashMap<WindowLabel, SpatialKey>,\n-}\n+**Branded-type parity with Rust newtypes.** TypeScript gets its own thin branded-string types so `SpatialKey`, `LayerKey`, `Moniker`, `WindowLabel` can't be mixed up at compile time:\n\n-impl SpatialState {\n-    pub fn focus(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent>;\n-    pub fn navigate(&mut self, window: WindowLabel, key: SpatialKey, direction: Direction) -> Option<FocusChangedEvent>;\n-    pub fn handle_unregister(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent>;\n-}\n-```\n-\n-Methods return `Option<FocusChangedEvent>` rather than side-effecting so tests don't need Tauri mocking — the Tauri adapter in `kanban-app/src/commands.rs` is responsible for emitting the event.\n-\n-### React side — branded types\n+```typescript\n+// kanban-app/ui/src/types/spatial.ts\n+export type WindowLabel = string & { readonly __tag: \"WindowLabel\" };\n+export type SpatialKey  = string & { readonly __tag: \"SpatialKey\" };\n+export type LayerKey    = string & { readonly __tag: \"LayerKey\" };\n+export type Moniker     = string & { readonly __tag: \"Moniker\" };\n+export type LayerName   = string & { readonly __tag: \"LayerName\" };\n+export type Pixels      = number & { readonly __tag: \"Pixels\" };\n\n-Parity with Rust newtypes via TypeScript branded strings. Details live in the React-primitives card (`01KPZWY4B7...`). The `types/spatial.ts` file exports `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName`, `Pixels` as branded types with brand helpers.\n+// Brand helpers\n+export const WindowLabel = (s: string) => s as WindowLabel;\n+export const SpatialKey  = (s: string) => s as SpatialKey;\n+export const LayerKey    = (s: string) => s as LayerKey;\n+export const Moniker     = (s: string) => s as Moniker;\n+export const LayerName   = (s: string) => s as LayerName;\n+export const Pixels      = (n: number) => n as Pixels;\n+```\n\n **Global event listener** (in EntityFocusProvider):\n\n ```typescript\n+type FocusChangedPayload = {\n+  window_label: WindowLabel;\n+  prev_key: SpatialKey | null;\n+  next_key: SpatialKey | null;\n+  next_moniker: Moniker | null;\n+};\n+\n listen<FocusChangedPayload>(\"focus-changed\", ({ payload }) => {\n   if (payload.prev_key) claimRegistry.get(payload.prev_key)?.(false);\n   if (payload.next_key) claimRegistry.get(payload.next_key)?.(true);\n@@ -87,43 +91,75 @@\n });\n ```\n\n-Each Tauri window has its own React tree and its own claim registry, so a `focus-changed` event for another window's key is a no-op here.\n+Since each Tauri window has its own React tree and its own claim registry, the registry only contains keys from that window. A `focus-changed` event for another window's key is a no-op here.\n+\n+**FocusScope / Focusable / FocusZone primitives** generate their `SpatialKey` via `useRef(SpatialKey(ulid())).current`. Branded helpers make the ULID typed at creation.\n+\n+### Rust state\n+\n+```rust\n+pub struct SpatialState {\n+    registry: SpatialRegistry,\n+    focus_by_window: HashMap<WindowLabel, SpatialKey>,\n+}\n\n-### Tests — `swissarmyhammer-kanban/tests/focus_state.rs`\n+impl SpatialState {\n+    pub fn focus(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n+    pub fn navigate(&mut self, window: WindowLabel, key: SpatialKey, direction: Direction) -> Option<FocusChangedEvent> { /* ... */ }\n+    pub fn handle_unregister(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n+}\n+```\n\n-Headless pattern matching `swissarmyhammer-kanban/tests/resolve_focused_column.rs`.\n+Every method signature uses newtypes. `FocusChangedEvent` is returned (not side-effected) so tests don't need Tauri mocking.\n\n ### Subtasks\n-- [ ] Define TS branded types in `kanban-app/ui/src/types/spatial.ts`\n-- [ ] Add `SpatialState` to `swissarmyhammer-kanban/src/focus/state.rs` with `focus_by_window: HashMap<WindowLabel, SpatialKey>`\n-- [ ] `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n-- [ ] Tauri commands in `kanban-app/src/commands.rs` derive `WindowLabel` from `tauri::Window`; emit `focus-changed` event\n-- [ ] Claim registry `Map<SpatialKey, (focused: boolean) => void>` in EntityFocusProvider\n-- [ ] Global `listen(\"focus-changed\")` handler\n+- [ ] Define TS branded types for `WindowLabel`/`SpatialKey`/`LayerKey`/`Moniker`/`LayerName`/`Pixels`\n+- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to `SpatialState`\n+- [ ] Emit `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n+- [ ] Tauri commands derive `WindowLabel` from the `tauri::Window` parameter\n+- [ ] Add claim registry `Map<SpatialKey, (focused: boolean) => void>` to EntityFocusProvider\n+- [ ] Add global `listen(\"focus-changed\")` handler\n+- [ ] Primitive components (see separate React-primitives card) use branded ULIDs\n - [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n-- [ ] Tests in `swissarmyhammer-kanban/tests/focus_state.rs`\n\n ## Acceptance Criteria\n-- [ ] State lives in `swissarmyhammer-kanban/src/focus/state.rs`, not `swissarmyhammer-commands`\n-- [ ] Tauri commands live in `kanban-app/src/commands.rs`\n-- [ ] No signature uses bare `String` or `f64`\n-- [ ] TS branded types mirror Rust newtypes; no `string`/`number` in typed spatial signatures\n-- [ ] Focus is per-window; windows A and B independent\n+- [ ] No signature in any added Rust type/method/command uses bare `String` or `f64`\n+- [ ] TS branded types mirror each Rust newtype; no `string`/`number` in spatial types' signatures\n+- [ ] Focus is per window; `focus_by_window[A]` and `focus_by_window[B]` independent\n - [ ] Focus change in window A doesn't re-render scopes in window B\n - [ ] All Tauri invokes return Ok/Err only; no focus data in return values\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm vitest run` pass\n+- [ ] `focus-changed` event carries newtyped fields\n+- [ ] No `focusedMoniker` state in React\n+- [ ] `cargo test` and `pnpm vitest run` pass\n\n ## Tests\n\n-### Rust (`swissarmyhammer-kanban/tests/focus_state.rs`)\n-- [ ] `focus` updates per-window state and returns `FocusChangedEvent` with matching `WindowLabel`\n-- [ ] focus in A doesn't affect `focus_by_window[B]`\n-- [ ] unregister of a focused key clears that window's focus only\n-- [ ] `FocusChangedEvent.next_moniker` is `Some(entry.moniker.clone())` when next_key is Some\n+### Rust\n\n+```rust\n+#[test]\n+fn focus_updates_per_window_state() {\n+    // spatial_focus(WindowLabel(\"main\".into()), SpatialKey(\"k1\".into()))\n+    // → state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n+    // → event.window_label == \"main\", next_key == Some(\"k1\")\n+}\n+\n+#[test]\n+fn focus_in_window_a_does_not_affect_window_b() { /* ... */ }\n+\n+#[test]\n+fn unregister_focused_clears_that_window_only() { /* ... */ }\n+\n+#[test]\n+fn event_carries_moniker_for_next_key() {\n+    // FocusChangedEvent.next_moniker is Some(entry.moniker) when next_key is Some\n+}\n+```\n+\n ### React\n+\n - [ ] Claim registry ignores events for unknown keys\n-- [ ] Scope click invokes `spatial_focus` with its branded `SpatialKey`\n+- [ ] FocusScope/Focusable click invokes `spatial_focus` with its branded `SpatialKey`\n - [ ] Provider unmount removes the listener\n - [ ] Scope unmount removes from claim registry\n\n","transaction_id":null}
{"id":"01KQ026YKNP0C7ACC5M336QFZV","timestamp":"2026-04-24T15:37:23.829440Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -6,77 +6,73 @@\n\n - **Rust owns focus state.** React is a dumb renderer that responds to events.\n - **Focus is per window.** Each Tauri window has its own focused element.\n-- **Focus identity = `SpatialKey` (ULID).** Not `Moniker`. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n+- **Focus identity = `SpatialKey` (ULID).** Not `Moniker`. The claim registry is `Map<SpatialKey, callback>`.\n+\n+### Crate placement\n+\n+State lives in `swissarmyhammer-kanban/src/focus/state.rs` alongside the registry from card `01KNQXW7HH...`. Tauri adapters live in `kanban-app/src/commands.rs`. This follows the refactor pattern from commit `b81336d42` (headless business logic in the kanban crate, Tauri commands as thin adapters).\n\n-### Newtypes (Rust)\n+### Newtypes — use `define_id!`\n\n-Shared with card `01KNQXW7HH...`. Referenced here so this card's signatures are self-contained.\n+All string-valued newtypes are defined via `swissarmyhammer_common::define_id!` in `focus/types.rs` (see card `01KNQXW7HH...` for full definitions). Referenced here for self-containment:\n\n ```rust\n-#[serde(transparent)] pub struct WindowLabel(pub String);\n-#[serde(transparent)] pub struct SpatialKey(pub String);\n-#[serde(transparent)] pub struct LayerKey(pub String);\n-#[serde(transparent)] pub struct Moniker(pub String);\n+define_id!(WindowLabel, \"Tauri window label\");\n+define_id!(SpatialKey, \"ULID per scope mount\");\n+define_id!(LayerKey, \"ULID per layer mount\");\n+define_id!(Moniker, \"Entity focus identity\");\n ```\n\n-No `String` appears in any signature below.\n+No bare `String` appears in any signature below.\n\n ### API surface\n\n ```rust\n-// React → Rust. window_label is derived from tauri::Window, never passed from React.\n-async fn spatial_register_focusable(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n-async fn spatial_register_zone(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n-async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n-async fn spatial_focus(window: tauri::Window, key: SpatialKey) -> Result<()>;\n-async fn spatial_navigate(window: tauri::Window, key: SpatialKey, direction: Direction) -> Result<()>;\n-async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: LayerName, parent: Option<LayerKey>) -> Result<()>;\n-async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n+// Tauri commands in kanban-app/src/commands.rs; all derive WindowLabel from tauri::Window.\n+async fn spatial_register_focusable(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<(), String>;\n+async fn spatial_register_zone(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<(), String>;\n+async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<(), String>;\n+async fn spatial_focus(window: tauri::Window, key: SpatialKey) -> Result<(), String>;\n+async fn spatial_navigate(window: tauri::Window, key: SpatialKey, direction: Direction) -> Result<(), String>;\n+async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: LayerName, parent: Option<LayerKey>) -> Result<(), String>;\n+async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<(), String>;\n ```\n\n ```rust\n-// Rust → React (Tauri event)\n+// Emitted from Rust to React\n #[derive(Serialize)]\n pub struct FocusChangedEvent {\n     pub window_label: WindowLabel,\n     pub prev_key: Option<SpatialKey>,\n     pub next_key: Option<SpatialKey>,\n-    pub next_moniker: Option<Moniker>,   // convenience for React — saves a lookup\n+    pub next_moniker: Option<Moniker>,\n }\n ```\n\n-### React side\n+### Rust state — `swissarmyhammer-kanban/src/focus/state.rs`\n\n-**Branded-type parity with Rust newtypes.** TypeScript gets its own thin branded-string types so `SpatialKey`, `LayerKey`, `Moniker`, `WindowLabel` can't be mixed up at compile time:\n-\n-```typescript\n-// kanban-app/ui/src/types/spatial.ts\n-export type WindowLabel = string & { readonly __tag: \"WindowLabel\" };\n-export type SpatialKey  = string & { readonly __tag: \"SpatialKey\" };\n-export type LayerKey    = string & { readonly __tag: \"LayerKey\" };\n-export type Moniker     = string & { readonly __tag: \"Moniker\" };\n-export type LayerName   = string & { readonly __tag: \"LayerName\" };\n-export type Pixels      = number & { readonly __tag: \"Pixels\" };\n+```rust\n+pub struct SpatialState {\n+    registry: SpatialRegistry,\n+    focus_by_window: HashMap<WindowLabel, SpatialKey>,\n+}\n\n-// Brand helpers\n-export const WindowLabel = (s: string) => s as WindowLabel;\n-export const SpatialKey  = (s: string) => s as SpatialKey;\n-export const LayerKey    = (s: string) => s as LayerKey;\n-export const Moniker     = (s: string) => s as Moniker;\n-export const LayerName   = (s: string) => s as LayerName;\n-export const Pixels      = (n: number) => n as Pixels;\n+impl SpatialState {\n+    pub fn focus(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent>;\n+    pub fn navigate(&mut self, window: WindowLabel, key: SpatialKey, direction: Direction) -> Option<FocusChangedEvent>;\n+    pub fn handle_unregister(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent>;\n+}\n ```\n\n+Methods return `Option<FocusChangedEvent>` rather than side-effecting so tests don't need Tauri mocking — the Tauri adapter in `kanban-app/src/commands.rs` is responsible for emitting the event.\n+\n+### React side — branded types\n+\n+Parity with Rust newtypes via TypeScript branded strings. Details live in the React-primitives card (`01KPZWY4B7...`). The `types/spatial.ts` file exports `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName`, `Pixels` as branded types with brand helpers.\n+\n **Global event listener** (in EntityFocusProvider):\n\n ```typescript\n-type FocusChangedPayload = {\n-  window_label: WindowLabel;\n-  prev_key: SpatialKey | null;\n-  next_key: SpatialKey | null;\n-  next_moniker: Moniker | null;\n-};\n-\n listen<FocusChangedPayload>(\"focus-changed\", ({ payload }) => {\n   if (payload.prev_key) claimRegistry.get(payload.prev_key)?.(false);\n   if (payload.next_key) claimRegistry.get(payload.next_key)?.(true);\n@@ -83,75 +79,43 @@\n });\n ```\n\n-Since each Tauri window has its own React tree and its own claim registry, the registry only contains keys from that window. A `focus-changed` event for another window's key is a no-op here.\n+Each Tauri window has its own React tree and its own claim registry, so a `focus-changed` event for another window's key is a no-op here.\n\n-**FocusScope / Focusable / FocusZone primitives** generate their `SpatialKey` via `useRef(SpatialKey(ulid())).current`. Branded helpers make the ULID typed at creation.\n-\n-### Rust state\n+### Tests — `swissarmyhammer-kanban/tests/focus_state.rs`\n\n-```rust\n-pub struct SpatialState {\n-    registry: SpatialRegistry,\n-    focus_by_window: HashMap<WindowLabel, SpatialKey>,\n-}\n-\n-impl SpatialState {\n-    pub fn focus(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n-    pub fn navigate(&mut self, window: WindowLabel, key: SpatialKey, direction: Direction) -> Option<FocusChangedEvent> { /* ... */ }\n-    pub fn handle_unregister(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n-}\n-```\n+Headless pattern matching `swissarmyhammer-kanban/tests/resolve_focused_column.rs`.\n\n-Every method signature uses newtypes. `FocusChangedEvent` is returned (not side-effected) so tests don't need Tauri mocking.\n-\n ### Subtasks\n-- [ ] Define TS branded types for `WindowLabel`/`SpatialKey`/`LayerKey`/`Moniker`/`LayerName`/`Pixels`\n-- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to `SpatialState`\n-- [ ] Emit `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n-- [ ] Tauri commands derive `WindowLabel` from the `tauri::Window` parameter\n-- [ ] Add claim registry `Map<SpatialKey, (focused: boolean) => void>` to EntityFocusProvider\n-- [ ] Add global `listen(\"focus-changed\")` handler\n-- [ ] Primitive components (see separate React-primitives card) use branded ULIDs\n+- [ ] Define TS branded types in `kanban-app/ui/src/types/spatial.ts`\n+- [ ] Add `SpatialState` to `swissarmyhammer-kanban/src/focus/state.rs` with `focus_by_window: HashMap<WindowLabel, SpatialKey>`\n+- [ ] `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n+- [ ] Tauri commands in `kanban-app/src/commands.rs` derive `WindowLabel` from `tauri::Window`; emit `focus-changed` event\n+- [ ] Claim registry `Map<SpatialKey, (focused: boolean) => void>` in EntityFocusProvider\n+- [ ] Global `listen(\"focus-changed\")` handler\n - [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n+- [ ] Tests in `swissarmyhammer-kanban/tests/focus_state.rs`\n\n ## Acceptance Criteria\n-- [ ] No signature in any added Rust type/method/command uses bare `String` or `f64`\n-- [ ] TS branded types mirror each Rust newtype; no `string`/`number` in spatial types' signatures\n-- [ ] Focus is per window; `focus_by_window[A]` and `focus_by_window[B]` independent\n+- [ ] State lives in `swissarmyhammer-kanban/src/focus/state.rs`, not `swissarmyhammer-commands`\n+- [ ] Tauri commands live in `kanban-app/src/commands.rs`\n+- [ ] No signature uses bare `String` or `f64`\n+- [ ] TS branded types mirror Rust newtypes; no `string`/`number` in typed spatial signatures\n+- [ ] Focus is per-window; windows A and B independent\n - [ ] Focus change in window A doesn't re-render scopes in window B\n - [ ] All Tauri invokes return Ok/Err only; no focus data in return values\n-- [ ] `focus-changed` event carries newtyped fields\n-- [ ] No `focusedMoniker` state in React\n-- [ ] `cargo test` and `pnpm vitest run` pass\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm vitest run` pass\n\n ## Tests\n\n-### Rust\n-\n-```rust\n-#[test]\n-fn focus_updates_per_window_state() {\n-    // spatial_focus(WindowLabel(\"main\".into()), SpatialKey(\"k1\".into()))\n-    // → state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n-    // → event.window_label == \"main\", next_key == Some(\"k1\")\n-}\n-\n-#[test]\n-fn focus_in_window_a_does_not_affect_window_b() { /* ... */ }\n-\n-#[test]\n-fn unregister_focused_clears_that_window_only() { /* ... */ }\n-\n-#[test]\n-fn event_carries_moniker_for_next_key() {\n-    // FocusChangedEvent.next_moniker is Some(entry.moniker) when next_key is Some\n-}\n-```\n+### Rust (`swissarmyhammer-kanban/tests/focus_state.rs`)\n+- [ ] `focus` updates per-window state and returns `FocusChangedEvent` with matching `WindowLabel`\n+- [ ] focus in A doesn't affect `focus_by_window[B]`\n+- [ ] unregister of a focused key clears that window's focus only\n+- [ ] `FocusChangedEvent.next_moniker` is `Some(entry.moniker.clone())` when next_key is Some\n\n ### React\n-\n - [ ] Claim registry ignores events for unknown keys\n-- [ ] FocusScope/Focusable click invokes `spatial_focus` with its branded `SpatialKey`\n+- [ ] Scope click invokes `spatial_focus` with its branded `SpatialKey`\n - [ ] Provider unmount removes the listener\n - [ ] Scope unmount removes from claim registry\n\n","reverse_patch":"--- original\n+++ modified\n@@ -6,73 +6,77 @@\n\n - **Rust owns focus state.** React is a dumb renderer that responds to events.\n - **Focus is per window.** Each Tauri window has its own focused element.\n-- **Focus identity = `SpatialKey` (ULID).** Not `Moniker`. The claim registry is `Map<SpatialKey, callback>`.\n+- **Focus identity = `SpatialKey` (ULID).** Not `Moniker`. The claim registry is `Map<SpatialKey, callback>`. Monikers are metadata for command dispatch, separate from focus mechanics.\n\n-### Crate placement\n+### Newtypes (Rust)\n\n-State lives in `swissarmyhammer-kanban/src/focus/state.rs` alongside the registry from card `01KNQXW7HH...`. Tauri adapters live in `kanban-app/src/commands.rs`. This follows the refactor pattern from commit `b81336d42` (headless business logic in the kanban crate, Tauri commands as thin adapters).\n-\n-### Newtypes — use `define_id!`\n-\n-All string-valued newtypes are defined via `swissarmyhammer_common::define_id!` in `focus/types.rs` (see card `01KNQXW7HH...` for full definitions). Referenced here for self-containment:\n+Shared with card `01KNQXW7HH...`. Referenced here so this card's signatures are self-contained.\n\n ```rust\n-define_id!(WindowLabel, \"Tauri window label\");\n-define_id!(SpatialKey, \"ULID per scope mount\");\n-define_id!(LayerKey, \"ULID per layer mount\");\n-define_id!(Moniker, \"Entity focus identity\");\n+#[serde(transparent)] pub struct WindowLabel(pub String);\n+#[serde(transparent)] pub struct SpatialKey(pub String);\n+#[serde(transparent)] pub struct LayerKey(pub String);\n+#[serde(transparent)] pub struct Moniker(pub String);\n ```\n\n-No bare `String` appears in any signature below.\n+No `String` appears in any signature below.\n\n ### API surface\n\n ```rust\n-// Tauri commands in kanban-app/src/commands.rs; all derive WindowLabel from tauri::Window.\n-async fn spatial_register_focusable(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<(), String>;\n-async fn spatial_register_zone(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<(), String>;\n-async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<(), String>;\n-async fn spatial_focus(window: tauri::Window, key: SpatialKey) -> Result<(), String>;\n-async fn spatial_navigate(window: tauri::Window, key: SpatialKey, direction: Direction) -> Result<(), String>;\n-async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: LayerName, parent: Option<LayerKey>) -> Result<(), String>;\n-async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<(), String>;\n+// React → Rust. window_label is derived from tauri::Window, never passed from React.\n+async fn spatial_register_focusable(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n+async fn spatial_register_zone(window: tauri::Window, key: SpatialKey, /* ... */) -> Result<()>;\n+async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n+async fn spatial_focus(window: tauri::Window, key: SpatialKey) -> Result<()>;\n+async fn spatial_navigate(window: tauri::Window, key: SpatialKey, direction: Direction) -> Result<()>;\n+async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: LayerName, parent: Option<LayerKey>) -> Result<()>;\n+async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n\n ```rust\n-// Emitted from Rust to React\n+// Rust → React (Tauri event)\n #[derive(Serialize)]\n pub struct FocusChangedEvent {\n     pub window_label: WindowLabel,\n     pub prev_key: Option<SpatialKey>,\n     pub next_key: Option<SpatialKey>,\n-    pub next_moniker: Option<Moniker>,\n+    pub next_moniker: Option<Moniker>,   // convenience for React — saves a lookup\n }\n ```\n\n-### Rust state — `swissarmyhammer-kanban/src/focus/state.rs`\n+### React side\n\n-```rust\n-pub struct SpatialState {\n-    registry: SpatialRegistry,\n-    focus_by_window: HashMap<WindowLabel, SpatialKey>,\n-}\n+**Branded-type parity with Rust newtypes.** TypeScript gets its own thin branded-string types so `SpatialKey`, `LayerKey`, `Moniker`, `WindowLabel` can't be mixed up at compile time:\n\n-impl SpatialState {\n-    pub fn focus(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent>;\n-    pub fn navigate(&mut self, window: WindowLabel, key: SpatialKey, direction: Direction) -> Option<FocusChangedEvent>;\n-    pub fn handle_unregister(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent>;\n-}\n-```\n-\n-Methods return `Option<FocusChangedEvent>` rather than side-effecting so tests don't need Tauri mocking — the Tauri adapter in `kanban-app/src/commands.rs` is responsible for emitting the event.\n-\n-### React side — branded types\n+```typescript\n+// kanban-app/ui/src/types/spatial.ts\n+export type WindowLabel = string & { readonly __tag: \"WindowLabel\" };\n+export type SpatialKey  = string & { readonly __tag: \"SpatialKey\" };\n+export type LayerKey    = string & { readonly __tag: \"LayerKey\" };\n+export type Moniker     = string & { readonly __tag: \"Moniker\" };\n+export type LayerName   = string & { readonly __tag: \"LayerName\" };\n+export type Pixels      = number & { readonly __tag: \"Pixels\" };\n\n-Parity with Rust newtypes via TypeScript branded strings. Details live in the React-primitives card (`01KPZWY4B7...`). The `types/spatial.ts` file exports `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName`, `Pixels` as branded types with brand helpers.\n+// Brand helpers\n+export const WindowLabel = (s: string) => s as WindowLabel;\n+export const SpatialKey  = (s: string) => s as SpatialKey;\n+export const LayerKey    = (s: string) => s as LayerKey;\n+export const Moniker     = (s: string) => s as Moniker;\n+export const LayerName   = (s: string) => s as LayerName;\n+export const Pixels      = (n: number) => n as Pixels;\n+```\n\n **Global event listener** (in EntityFocusProvider):\n\n ```typescript\n+type FocusChangedPayload = {\n+  window_label: WindowLabel;\n+  prev_key: SpatialKey | null;\n+  next_key: SpatialKey | null;\n+  next_moniker: Moniker | null;\n+};\n+\n listen<FocusChangedPayload>(\"focus-changed\", ({ payload }) => {\n   if (payload.prev_key) claimRegistry.get(payload.prev_key)?.(false);\n   if (payload.next_key) claimRegistry.get(payload.next_key)?.(true);\n@@ -79,43 +83,75 @@\n });\n ```\n\n-Each Tauri window has its own React tree and its own claim registry, so a `focus-changed` event for another window's key is a no-op here.\n+Since each Tauri window has its own React tree and its own claim registry, the registry only contains keys from that window. A `focus-changed` event for another window's key is a no-op here.\n+\n+**FocusScope / Focusable / FocusZone primitives** generate their `SpatialKey` via `useRef(SpatialKey(ulid())).current`. Branded helpers make the ULID typed at creation.\n+\n+### Rust state\n+\n+```rust\n+pub struct SpatialState {\n+    registry: SpatialRegistry,\n+    focus_by_window: HashMap<WindowLabel, SpatialKey>,\n+}\n\n-### Tests — `swissarmyhammer-kanban/tests/focus_state.rs`\n+impl SpatialState {\n+    pub fn focus(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n+    pub fn navigate(&mut self, window: WindowLabel, key: SpatialKey, direction: Direction) -> Option<FocusChangedEvent> { /* ... */ }\n+    pub fn handle_unregister(&mut self, window: WindowLabel, key: SpatialKey) -> Option<FocusChangedEvent> { /* ... */ }\n+}\n+```\n\n-Headless pattern matching `swissarmyhammer-kanban/tests/resolve_focused_column.rs`.\n+Every method signature uses newtypes. `FocusChangedEvent` is returned (not side-effected) so tests don't need Tauri mocking.\n\n ### Subtasks\n-- [ ] Define TS branded types in `kanban-app/ui/src/types/spatial.ts`\n-- [ ] Add `SpatialState` to `swissarmyhammer-kanban/src/focus/state.rs` with `focus_by_window: HashMap<WindowLabel, SpatialKey>`\n-- [ ] `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n-- [ ] Tauri commands in `kanban-app/src/commands.rs` derive `WindowLabel` from `tauri::Window`; emit `focus-changed` event\n-- [ ] Claim registry `Map<SpatialKey, (focused: boolean) => void>` in EntityFocusProvider\n-- [ ] Global `listen(\"focus-changed\")` handler\n+- [ ] Define TS branded types for `WindowLabel`/`SpatialKey`/`LayerKey`/`Moniker`/`LayerName`/`Pixels`\n+- [ ] Add `focus_by_window: HashMap<WindowLabel, SpatialKey>` to `SpatialState`\n+- [ ] Emit `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n+- [ ] Tauri commands derive `WindowLabel` from the `tauri::Window` parameter\n+- [ ] Add claim registry `Map<SpatialKey, (focused: boolean) => void>` to EntityFocusProvider\n+- [ ] Add global `listen(\"focus-changed\")` handler\n+- [ ] Primitive components (see separate React-primitives card) use branded ULIDs\n - [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n-- [ ] Tests in `swissarmyhammer-kanban/tests/focus_state.rs`\n\n ## Acceptance Criteria\n-- [ ] State lives in `swissarmyhammer-kanban/src/focus/state.rs`, not `swissarmyhammer-commands`\n-- [ ] Tauri commands live in `kanban-app/src/commands.rs`\n-- [ ] No signature uses bare `String` or `f64`\n-- [ ] TS branded types mirror Rust newtypes; no `string`/`number` in typed spatial signatures\n-- [ ] Focus is per-window; windows A and B independent\n+- [ ] No signature in any added Rust type/method/command uses bare `String` or `f64`\n+- [ ] TS branded types mirror each Rust newtype; no `string`/`number` in spatial types' signatures\n+- [ ] Focus is per window; `focus_by_window[A]` and `focus_by_window[B]` independent\n - [ ] Focus change in window A doesn't re-render scopes in window B\n - [ ] All Tauri invokes return Ok/Err only; no focus data in return values\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm vitest run` pass\n+- [ ] `focus-changed` event carries newtyped fields\n+- [ ] No `focusedMoniker` state in React\n+- [ ] `cargo test` and `pnpm vitest run` pass\n\n ## Tests\n\n-### Rust (`swissarmyhammer-kanban/tests/focus_state.rs`)\n-- [ ] `focus` updates per-window state and returns `FocusChangedEvent` with matching `WindowLabel`\n-- [ ] focus in A doesn't affect `focus_by_window[B]`\n-- [ ] unregister of a focused key clears that window's focus only\n-- [ ] `FocusChangedEvent.next_moniker` is `Some(entry.moniker.clone())` when next_key is Some\n+### Rust\n\n+```rust\n+#[test]\n+fn focus_updates_per_window_state() {\n+    // spatial_focus(WindowLabel(\"main\".into()), SpatialKey(\"k1\".into()))\n+    // → state.focus_by_window[WindowLabel(\"main\")] == SpatialKey(\"k1\")\n+    // → event.window_label == \"main\", next_key == Some(\"k1\")\n+}\n+\n+#[test]\n+fn focus_in_window_a_does_not_affect_window_b() { /* ... */ }\n+\n+#[test]\n+fn unregister_focused_clears_that_window_only() { /* ... */ }\n+\n+#[test]\n+fn event_carries_moniker_for_next_key() {\n+    // FocusChangedEvent.next_moniker is Some(entry.moniker) when next_key is Some\n+}\n+```\n+\n ### React\n+\n - [ ] Claim registry ignores events for unknown keys\n-- [ ] Scope click invokes `spatial_focus` with its branded `SpatialKey`\n+- [ ] FocusScope/Focusable click invokes `spatial_focus` with its branded `SpatialKey`\n - [ ] Provider unmount removes the listener\n - [ ] Scope unmount removes from claim registry\n\n"}]]}
{"id":"01KQ2B8WGMBSA11VG61Z1NEMN3","timestamp":"2026-04-25T12:54:13.268778Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: '9980'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Focus claim registry: per-window, event-driven, Rust owns state (newtype-only signatures)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '9980'\n project: spatial-nav\n title: 'Focus claim registry: per-window, event-driven, Rust owns state (newtype-only signatures)'\n ---\n","transaction_id":null}
{"id":"01KQ2B8WGNNTV13Q6VSJ25FNMP","timestamp":"2026-04-25T12:54:13.269979Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-9980\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+9980\n\\ No newline at end of file\n"}]]}
{"id":"01KQ2CEADFT9VW3KAV5E0J54Q8","timestamp":"2026-04-25T13:14:39.919992Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -94,38 +94,45 @@\n Headless pattern matching `swissarmyhammer-kanban/tests/resolve_focused_column.rs`.\n\n ### Subtasks\n-- [ ] Define TS branded types in `kanban-app/ui/src/types/spatial.ts`\n-- [ ] Add `SpatialState` to `swissarmyhammer-kanban/src/focus/state.rs` with `focus_by_window: HashMap<WindowLabel, SpatialKey>`\n-- [ ] `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n-- [ ] Tauri commands in `kanban-app/src/commands.rs` derive `WindowLabel` from `tauri::Window`; emit `focus-changed` event\n-- [ ] Claim registry `Map<SpatialKey, (focused: boolean) => void>` in EntityFocusProvider\n-- [ ] Global `listen(\"focus-changed\")` handler\n-- [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n-- [ ] Tests in `swissarmyhammer-kanban/tests/focus_state.rs`\n+- [x] Define TS branded types in `kanban-app/ui/src/types/spatial.ts`\n+- [x] Add `SpatialState` to `swissarmyhammer-kanban/src/focus/state.rs` with `focus_by_window: HashMap<WindowLabel, SpatialKey>`\n+- [x] `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n+- [x] Tauri commands in `kanban-app/src/commands.rs` derive `WindowLabel` from `tauri::Window`; emit `focus-changed` event\n+- [x] Claim registry `Map<SpatialKey, (focused: boolean) => void>` (in `kanban-app/ui/src/lib/spatial-focus-context.tsx`)\n+- [x] Global `listen(\"focus-changed\")` handler\n+- [ ] Remove `focusedMoniker` useState from EntityFocusProvider — DEFERRED. The existing `EntityFocusProvider` does not in fact hold the focused moniker in `useState` — it uses `useSyncExternalStore` against an in-memory `FocusStore`, which 51 production files depend on through `useFocusActions`, `useIsDirectFocus`, `useFocusedMoniker`, `useFocusedScope`, `useEntityFocus`, etc. Removing the existing store would be a breaking refactor across the entire UI and lies beyond the scope of building the claim registry infrastructure. The new `SpatialFocusProvider` lives alongside the existing one; consumers migrate to the new claim registry as a separate piece of work.\n+- [x] Tests in `swissarmyhammer-kanban/tests/focus_state.rs`\n\n ## Acceptance Criteria\n-- [ ] State lives in `swissarmyhammer-kanban/src/focus/state.rs`, not `swissarmyhammer-commands`\n-- [ ] Tauri commands live in `kanban-app/src/commands.rs`\n-- [ ] No signature uses bare `String` or `f64`\n-- [ ] TS branded types mirror Rust newtypes; no `string`/`number` in typed spatial signatures\n-- [ ] Focus is per-window; windows A and B independent\n-- [ ] Focus change in window A doesn't re-render scopes in window B\n-- [ ] All Tauri invokes return Ok/Err only; no focus data in return values\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm vitest run` pass\n+- [x] State lives in `swissarmyhammer-kanban/src/focus/state.rs`, not `swissarmyhammer-commands`\n+- [x] Tauri commands live in `kanban-app/src/commands.rs`\n+- [x] No signature uses bare `String` or `f64`\n+- [x] TS branded types mirror Rust newtypes; no `string`/`number` in typed spatial signatures\n+- [x] Focus is per-window; windows A and B independent\n+- [x] Focus change in window A doesn't re-render scopes in window B\n+- [x] All Tauri invokes return Ok/Err only; no focus data in return values\n+- [x] `cargo test -p swissarmyhammer-kanban` and `pnpm vitest run` pass\n\n ## Tests\n\n ### Rust (`swissarmyhammer-kanban/tests/focus_state.rs`)\n-- [ ] `focus` updates per-window state and returns `FocusChangedEvent` with matching `WindowLabel`\n-- [ ] focus in A doesn't affect `focus_by_window[B]`\n-- [ ] unregister of a focused key clears that window's focus only\n-- [ ] `FocusChangedEvent.next_moniker` is `Some(entry.moniker.clone())` when next_key is Some\n+- [x] `focus` updates per-window state and returns `FocusChangedEvent` with matching `WindowLabel`\n+- [x] focus in A doesn't affect `focus_by_window[B]`\n+- [x] unregister of a focused key clears that window's focus only\n+- [x] `FocusChangedEvent.next_moniker` is `Some(entry.moniker.clone())` when next_key is Some\n+\n+### React (in `kanban-app/ui/src/lib/spatial-focus-context.test.tsx`)\n+- [x] Claim registry ignores events for unknown keys\n+- [x] Scope click invokes `spatial_focus` with its branded `SpatialKey`\n+- [x] Provider unmount removes the listener\n+- [x] Scope unmount removes from claim registry\n+\n+## Implementation notes\n\n-### React\n-- [ ] Claim registry ignores events for unknown keys\n-- [ ] Scope click invokes `spatial_focus` with its branded `SpatialKey`\n-- [ ] Provider unmount removes the listener\n-- [ ] Scope unmount removes from claim registry\n+- The claim registry is implemented in a new `SpatialFocusProvider` (in `kanban-app/ui/src/lib/spatial-focus-context.tsx`) rather than woven into the existing `EntityFocusProvider`. The two providers serve different keyspaces — `Moniker` for entity focus, `SpatialKey` for spatial-nav scopes — and keeping them separate keeps each context narrow.\n+- The full `SpatialRegistry` (Focusable / FocusZone / FocusLayer / FocusScope enum, beam search) is owned by card `01KNQXW7HH...` and lands on top of the `SpatialEntry` table introduced here without breaking the public surface.\n+- `SpatialState::navigate` is wired through the public surface but currently returns `None` — beam search lives in card `01KNQXXF5W...`.\n+- `spatial_push_layer` / `spatial_pop_layer` are no-ops at the kanban-crate level pending the layer forest from card `01KNQXW7HH...`; the IPC surface is pinned now so the frontend can register its keymap entries today.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -94,45 +94,38 @@\n Headless pattern matching `swissarmyhammer-kanban/tests/resolve_focused_column.rs`.\n\n ### Subtasks\n-- [x] Define TS branded types in `kanban-app/ui/src/types/spatial.ts`\n-- [x] Add `SpatialState` to `swissarmyhammer-kanban/src/focus/state.rs` with `focus_by_window: HashMap<WindowLabel, SpatialKey>`\n-- [x] `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n-- [x] Tauri commands in `kanban-app/src/commands.rs` derive `WindowLabel` from `tauri::Window`; emit `focus-changed` event\n-- [x] Claim registry `Map<SpatialKey, (focused: boolean) => void>` (in `kanban-app/ui/src/lib/spatial-focus-context.tsx`)\n-- [x] Global `listen(\"focus-changed\")` handler\n-- [ ] Remove `focusedMoniker` useState from EntityFocusProvider — DEFERRED. The existing `EntityFocusProvider` does not in fact hold the focused moniker in `useState` — it uses `useSyncExternalStore` against an in-memory `FocusStore`, which 51 production files depend on through `useFocusActions`, `useIsDirectFocus`, `useFocusedMoniker`, `useFocusedScope`, `useEntityFocus`, etc. Removing the existing store would be a breaking refactor across the entire UI and lies beyond the scope of building the claim registry infrastructure. The new `SpatialFocusProvider` lives alongside the existing one; consumers migrate to the new claim registry as a separate piece of work.\n-- [x] Tests in `swissarmyhammer-kanban/tests/focus_state.rs`\n+- [ ] Define TS branded types in `kanban-app/ui/src/types/spatial.ts`\n+- [ ] Add `SpatialState` to `swissarmyhammer-kanban/src/focus/state.rs` with `focus_by_window: HashMap<WindowLabel, SpatialKey>`\n+- [ ] `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n+- [ ] Tauri commands in `kanban-app/src/commands.rs` derive `WindowLabel` from `tauri::Window`; emit `focus-changed` event\n+- [ ] Claim registry `Map<SpatialKey, (focused: boolean) => void>` in EntityFocusProvider\n+- [ ] Global `listen(\"focus-changed\")` handler\n+- [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n+- [ ] Tests in `swissarmyhammer-kanban/tests/focus_state.rs`\n\n ## Acceptance Criteria\n-- [x] State lives in `swissarmyhammer-kanban/src/focus/state.rs`, not `swissarmyhammer-commands`\n-- [x] Tauri commands live in `kanban-app/src/commands.rs`\n-- [x] No signature uses bare `String` or `f64`\n-- [x] TS branded types mirror Rust newtypes; no `string`/`number` in typed spatial signatures\n-- [x] Focus is per-window; windows A and B independent\n-- [x] Focus change in window A doesn't re-render scopes in window B\n-- [x] All Tauri invokes return Ok/Err only; no focus data in return values\n-- [x] `cargo test -p swissarmyhammer-kanban` and `pnpm vitest run` pass\n+- [ ] State lives in `swissarmyhammer-kanban/src/focus/state.rs`, not `swissarmyhammer-commands`\n+- [ ] Tauri commands live in `kanban-app/src/commands.rs`\n+- [ ] No signature uses bare `String` or `f64`\n+- [ ] TS branded types mirror Rust newtypes; no `string`/`number` in typed spatial signatures\n+- [ ] Focus is per-window; windows A and B independent\n+- [ ] Focus change in window A doesn't re-render scopes in window B\n+- [ ] All Tauri invokes return Ok/Err only; no focus data in return values\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm vitest run` pass\n\n ## Tests\n\n ### Rust (`swissarmyhammer-kanban/tests/focus_state.rs`)\n-- [x] `focus` updates per-window state and returns `FocusChangedEvent` with matching `WindowLabel`\n-- [x] focus in A doesn't affect `focus_by_window[B]`\n-- [x] unregister of a focused key clears that window's focus only\n-- [x] `FocusChangedEvent.next_moniker` is `Some(entry.moniker.clone())` when next_key is Some\n+- [ ] `focus` updates per-window state and returns `FocusChangedEvent` with matching `WindowLabel`\n+- [ ] focus in A doesn't affect `focus_by_window[B]`\n+- [ ] unregister of a focused key clears that window's focus only\n+- [ ] `FocusChangedEvent.next_moniker` is `Some(entry.moniker.clone())` when next_key is Some\n\n-### React (in `kanban-app/ui/src/lib/spatial-focus-context.test.tsx`)\n-- [x] Claim registry ignores events for unknown keys\n-- [x] Scope click invokes `spatial_focus` with its branded `SpatialKey`\n-- [x] Provider unmount removes the listener\n-- [x] Scope unmount removes from claim registry\n-\n-## Implementation notes\n-\n-- The claim registry is implemented in a new `SpatialFocusProvider` (in `kanban-app/ui/src/lib/spatial-focus-context.tsx`) rather than woven into the existing `EntityFocusProvider`. The two providers serve different keyspaces — `Moniker` for entity focus, `SpatialKey` for spatial-nav scopes — and keeping them separate keeps each context narrow.\n-- The full `SpatialRegistry` (Focusable / FocusZone / FocusLayer / FocusScope enum, beam search) is owned by card `01KNQXW7HH...` and lands on top of the `SpatialEntry` table introduced here without breaking the public surface.\n-- `SpatialState::navigate` is wired through the public surface but currently returns `None` — beam search lives in card `01KNQXXF5W...`.\n-- `spatial_push_layer` / `spatial_pop_layer` are no-ops at the kanban-crate level pending the layer forest from card `01KNQXW7HH...`; the IPC surface is pinned now so the frontend can register its keymap entries today.\n+### React\n+- [ ] Claim registry ignores events for unknown keys\n+- [ ] Scope click invokes `spatial_focus` with its branded `SpatialKey`\n+- [ ] Provider unmount removes the listener\n+- [ ] Scope unmount removes from claim registry\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ2CEADGJRBS5R5RFW8SYM49","timestamp":"2026-04-25T13:14:39.920622Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -86,38 +86,45 @@\n Headless pattern matching `swissarmyhammer-kanban/tests/resolve_focused_column.rs`.\n\n ### Subtasks\n-- [ ] Define TS branded types in `kanban-app/ui/src/types/spatial.ts`\n-- [ ] Add `SpatialState` to `swissarmyhammer-kanban/src/focus/state.rs` with `focus_by_window: HashMap<WindowLabel, SpatialKey>`\n-- [ ] `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n-- [ ] Tauri commands in `kanban-app/src/commands.rs` derive `WindowLabel` from `tauri::Window`; emit `focus-changed` event\n-- [ ] Claim registry `Map<SpatialKey, (focused: boolean) => void>` in EntityFocusProvider\n-- [ ] Global `listen(\"focus-changed\")` handler\n-- [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n-- [ ] Tests in `swissarmyhammer-kanban/tests/focus_state.rs`\n+- [x] Define TS branded types in `kanban-app/ui/src/types/spatial.ts`\n+- [x] Add `SpatialState` to `swissarmyhammer-kanban/src/focus/state.rs` with `focus_by_window: HashMap<WindowLabel, SpatialKey>`\n+- [x] `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n+- [x] Tauri commands in `kanban-app/src/commands.rs` derive `WindowLabel` from `tauri::Window`; emit `focus-changed` event\n+- [x] Claim registry `Map<SpatialKey, (focused: boolean) => void>` (in `kanban-app/ui/src/lib/spatial-focus-context.tsx`)\n+- [x] Global `listen(\"focus-changed\")` handler\n+- [ ] Remove `focusedMoniker` useState from EntityFocusProvider — DEFERRED. The existing `EntityFocusProvider` does not in fact hold the focused moniker in `useState` — it uses `useSyncExternalStore` against an in-memory `FocusStore`, which 51 production files depend on through `useFocusActions`, `useIsDirectFocus`, `useFocusedMoniker`, `useFocusedScope`, `useEntityFocus`, etc. Removing the existing store would be a breaking refactor across the entire UI and lies beyond the scope of building the claim registry infrastructure. The new `SpatialFocusProvider` lives alongside the existing one; consumers migrate to the new claim registry as a separate piece of work.\n+- [x] Tests in `swissarmyhammer-kanban/tests/focus_state.rs`\n\n ## Acceptance Criteria\n-- [ ] State lives in `swissarmyhammer-kanban/src/focus/state.rs`, not `swissarmyhammer-commands`\n-- [ ] Tauri commands live in `kanban-app/src/commands.rs`\n-- [ ] No signature uses bare `String` or `f64`\n-- [ ] TS branded types mirror Rust newtypes; no `string`/`number` in typed spatial signatures\n-- [ ] Focus is per-window; windows A and B independent\n-- [ ] Focus change in window A doesn't re-render scopes in window B\n-- [ ] All Tauri invokes return Ok/Err only; no focus data in return values\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm vitest run` pass\n+- [x] State lives in `swissarmyhammer-kanban/src/focus/state.rs`, not `swissarmyhammer-commands`\n+- [x] Tauri commands live in `kanban-app/src/commands.rs`\n+- [x] No signature uses bare `String` or `f64`\n+- [x] TS branded types mirror Rust newtypes; no `string`/`number` in typed spatial signatures\n+- [x] Focus is per-window; windows A and B independent\n+- [x] Focus change in window A doesn't re-render scopes in window B\n+- [x] All Tauri invokes return Ok/Err only; no focus data in return values\n+- [x] `cargo test -p swissarmyhammer-kanban` and `pnpm vitest run` pass\n\n ## Tests\n\n ### Rust (`swissarmyhammer-kanban/tests/focus_state.rs`)\n-- [ ] `focus` updates per-window state and returns `FocusChangedEvent` with matching `WindowLabel`\n-- [ ] focus in A doesn't affect `focus_by_window[B]`\n-- [ ] unregister of a focused key clears that window's focus only\n-- [ ] `FocusChangedEvent.next_moniker` is `Some(entry.moniker.clone())` when next_key is Some\n+- [x] `focus` updates per-window state and returns `FocusChangedEvent` with matching `WindowLabel`\n+- [x] focus in A doesn't affect `focus_by_window[B]`\n+- [x] unregister of a focused key clears that window's focus only\n+- [x] `FocusChangedEvent.next_moniker` is `Some(entry.moniker.clone())` when next_key is Some\n+\n+### React (in `kanban-app/ui/src/lib/spatial-focus-context.test.tsx`)\n+- [x] Claim registry ignores events for unknown keys\n+- [x] Scope click invokes `spatial_focus` with its branded `SpatialKey`\n+- [x] Provider unmount removes the listener\n+- [x] Scope unmount removes from claim registry\n+\n+## Implementation notes\n\n-### React\n-- [ ] Claim registry ignores events for unknown keys\n-- [ ] Scope click invokes `spatial_focus` with its branded `SpatialKey`\n-- [ ] Provider unmount removes the listener\n-- [ ] Scope unmount removes from claim registry\n+- The claim registry is implemented in a new `SpatialFocusProvider` (in `kanban-app/ui/src/lib/spatial-focus-context.tsx`) rather than woven into the existing `EntityFocusProvider`. The two providers serve different keyspaces — `Moniker` for entity focus, `SpatialKey` for spatial-nav scopes — and keeping them separate keeps each context narrow.\n+- The full `SpatialRegistry` (Focusable / FocusZone / FocusLayer / FocusScope enum, beam search) is owned by card `01KNQXW7HH...` and lands on top of the `SpatialEntry` table introduced here without breaking the public surface.\n+- `SpatialState::navigate` is wired through the public surface but currently returns `None` — beam search lives in card `01KNQXXF5W...`.\n+- `spatial_push_layer` / `spatial_pop_layer` are no-ops at the kanban-crate level pending the layer forest from card `01KNQXW7HH...`; the IPC surface is pinned now so the frontend can register its keymap entries today.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -86,45 +86,38 @@\n Headless pattern matching `swissarmyhammer-kanban/tests/resolve_focused_column.rs`.\n\n ### Subtasks\n-- [x] Define TS branded types in `kanban-app/ui/src/types/spatial.ts`\n-- [x] Add `SpatialState` to `swissarmyhammer-kanban/src/focus/state.rs` with `focus_by_window: HashMap<WindowLabel, SpatialKey>`\n-- [x] `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n-- [x] Tauri commands in `kanban-app/src/commands.rs` derive `WindowLabel` from `tauri::Window`; emit `focus-changed` event\n-- [x] Claim registry `Map<SpatialKey, (focused: boolean) => void>` (in `kanban-app/ui/src/lib/spatial-focus-context.tsx`)\n-- [x] Global `listen(\"focus-changed\")` handler\n-- [ ] Remove `focusedMoniker` useState from EntityFocusProvider — DEFERRED. The existing `EntityFocusProvider` does not in fact hold the focused moniker in `useState` — it uses `useSyncExternalStore` against an in-memory `FocusStore`, which 51 production files depend on through `useFocusActions`, `useIsDirectFocus`, `useFocusedMoniker`, `useFocusedScope`, `useEntityFocus`, etc. Removing the existing store would be a breaking refactor across the entire UI and lies beyond the scope of building the claim registry infrastructure. The new `SpatialFocusProvider` lives alongside the existing one; consumers migrate to the new claim registry as a separate piece of work.\n-- [x] Tests in `swissarmyhammer-kanban/tests/focus_state.rs`\n+- [ ] Define TS branded types in `kanban-app/ui/src/types/spatial.ts`\n+- [ ] Add `SpatialState` to `swissarmyhammer-kanban/src/focus/state.rs` with `focus_by_window: HashMap<WindowLabel, SpatialKey>`\n+- [ ] `FocusChangedEvent { window_label, prev_key, next_key, next_moniker }` — all newtyped\n+- [ ] Tauri commands in `kanban-app/src/commands.rs` derive `WindowLabel` from `tauri::Window`; emit `focus-changed` event\n+- [ ] Claim registry `Map<SpatialKey, (focused: boolean) => void>` in EntityFocusProvider\n+- [ ] Global `listen(\"focus-changed\")` handler\n+- [ ] Remove `focusedMoniker` useState from EntityFocusProvider\n+- [ ] Tests in `swissarmyhammer-kanban/tests/focus_state.rs`\n\n ## Acceptance Criteria\n-- [x] State lives in `swissarmyhammer-kanban/src/focus/state.rs`, not `swissarmyhammer-commands`\n-- [x] Tauri commands live in `kanban-app/src/commands.rs`\n-- [x] No signature uses bare `String` or `f64`\n-- [x] TS branded types mirror Rust newtypes; no `string`/`number` in typed spatial signatures\n-- [x] Focus is per-window; windows A and B independent\n-- [x] Focus change in window A doesn't re-render scopes in window B\n-- [x] All Tauri invokes return Ok/Err only; no focus data in return values\n-- [x] `cargo test -p swissarmyhammer-kanban` and `pnpm vitest run` pass\n+- [ ] State lives in `swissarmyhammer-kanban/src/focus/state.rs`, not `swissarmyhammer-commands`\n+- [ ] Tauri commands live in `kanban-app/src/commands.rs`\n+- [ ] No signature uses bare `String` or `f64`\n+- [ ] TS branded types mirror Rust newtypes; no `string`/`number` in typed spatial signatures\n+- [ ] Focus is per-window; windows A and B independent\n+- [ ] Focus change in window A doesn't re-render scopes in window B\n+- [ ] All Tauri invokes return Ok/Err only; no focus data in return values\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm vitest run` pass\n\n ## Tests\n\n ### Rust (`swissarmyhammer-kanban/tests/focus_state.rs`)\n-- [x] `focus` updates per-window state and returns `FocusChangedEvent` with matching `WindowLabel`\n-- [x] focus in A doesn't affect `focus_by_window[B]`\n-- [x] unregister of a focused key clears that window's focus only\n-- [x] `FocusChangedEvent.next_moniker` is `Some(entry.moniker.clone())` when next_key is Some\n+- [ ] `focus` updates per-window state and returns `FocusChangedEvent` with matching `WindowLabel`\n+- [ ] focus in A doesn't affect `focus_by_window[B]`\n+- [ ] unregister of a focused key clears that window's focus only\n+- [ ] `FocusChangedEvent.next_moniker` is `Some(entry.moniker.clone())` when next_key is Some\n\n-### React (in `kanban-app/ui/src/lib/spatial-focus-context.test.tsx`)\n-- [x] Claim registry ignores events for unknown keys\n-- [x] Scope click invokes `spatial_focus` with its branded `SpatialKey`\n-- [x] Provider unmount removes the listener\n-- [x] Scope unmount removes from claim registry\n-\n-## Implementation notes\n-\n-- The claim registry is implemented in a new `SpatialFocusProvider` (in `kanban-app/ui/src/lib/spatial-focus-context.tsx`) rather than woven into the existing `EntityFocusProvider`. The two providers serve different keyspaces — `Moniker` for entity focus, `SpatialKey` for spatial-nav scopes — and keeping them separate keeps each context narrow.\n-- The full `SpatialRegistry` (Focusable / FocusZone / FocusLayer / FocusScope enum, beam search) is owned by card `01KNQXW7HH...` and lands on top of the `SpatialEntry` table introduced here without breaking the public surface.\n-- `SpatialState::navigate` is wired through the public surface but currently returns `None` — beam search lives in card `01KNQXXF5W...`.\n-- `spatial_push_layer` / `spatial_pop_layer` are no-ops at the kanban-crate level pending the layer forest from card `01KNQXW7HH...`; the IPC surface is pinned now so the frontend can register its keymap entries today.\n+### React\n+- [ ] Claim registry ignores events for unknown keys\n+- [ ] Scope click invokes `spatial_focus` with its branded `SpatialKey`\n+- [ ] Provider unmount removes the listener\n+- [ ] Scope unmount removes from claim registry\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ2CETHKB7PE4QY7P7VGGCM9","timestamp":"2026-04-25T13:14:56.435049Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Focus claim registry: per-window, event-driven, Rust owns state (newtype-only signatures)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Focus claim registry: per-window, event-driven, Rust owns state (newtype-only signatures)'\n ---\n","transaction_id":null}
{"id":"01KQ2CETHKN7F2GXWZ4M678KMT","timestamp":"2026-04-25T13:14:56.435951Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ2D2QV60KEYKM2EJQJR7D2X","timestamp":"2026-04-25T13:25:49.030639Z","op":"update","item_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8280'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffa680\n project: spatial-nav\n title: 'Focus claim registry: per-window, event-driven, Rust owns state (newtype-only signatures)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffa680\n+position_column: review\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Focus claim registry: per-window, event-driven, Rust owns state (newtype-only signatures)'\n ---\n","transaction_id":null}
{"id":"01KQ2D2QV7QWYK9RNHZ1CXY98D","timestamp":"2026-04-25T13:25:49.031555Z","entity_type":"task","entity_id":"01KNM3YHHFJ3PTXZHD9EFKVBS6","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+ffffffffffffffffffffffffa680\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffa680\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n"}]]}

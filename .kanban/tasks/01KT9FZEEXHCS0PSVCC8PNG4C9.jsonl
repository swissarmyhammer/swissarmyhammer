{"id":"01KT9FZEEX3R9Q7DHDMX1M4P86","timestamp":"2026-06-04T14:18:13.341375Z","op":"create","item_id":"01KT9FZEEXHCS0PSVCC8PNG4C9","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,19 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KT9FYTVE2CMAGZQW29G1M6Q6\n+position_column: todo\n+position_ordinal: b580\n+project: plugin-arch\n+title: 'Docs: document the SDK event-subscription API (.on); remove stale \"inert/RESERVED\" language'\n+---\n+Document the event-subscription API now that it is real. Depends on the SDK surface card.\n+\n+## Scope\n+- `ideas/plugins/plugin-architecture.md`: add an \"Events / subscriptions\" section — the `this.<server>.on(event, cb)` surface, the `notifications/<server>/<event>` method-naming convention, the bridge→isolate delivery path (NotificationBridge → per-host pump → invoke_plugin_callback), auto-cleanup on unload, and the cached-flag pattern for reactive `available()` (the canonical consumer).\n+- `ideas/plugins/command-service.md`: cross-link the cached-flag/`available` pattern it already references to the now-real `.on()` API.\n+- Code comments: remove \"intentionally inert\" / \"event API not implemented in this SDK task\" / \"wired by a later task\" language in sdk/plugin.ts and anywhere else that calls the event API reserved/unimplemented.\n+\n+## Acceptance\n+Docs describe the real API and no stale \"RESERVED/inert/not implemented\" claims about the event surface remain in code or specs.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,19 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KT9FYTVE2CMAGZQW29G1M6Q6\n-position_column: todo\n-position_ordinal: b580\n-project: plugin-arch\n-title: 'Docs: document the SDK event-subscription API (.on); remove stale \"inert/RESERVED\" language'\n----\n-Document the event-subscription API now that it is real. Depends on the SDK surface card.\n-\n-## Scope\n-- `ideas/plugins/plugin-architecture.md`: add an \"Events / subscriptions\" section — the `this.<server>.on(event, cb)` surface, the `notifications/<server>/<event>` method-naming convention, the bridge→isolate delivery path (NotificationBridge → per-host pump → invoke_plugin_callback), auto-cleanup on unload, and the cached-flag pattern for reactive `available()` (the canonical consumer).\n-- `ideas/plugins/command-service.md`: cross-link the cached-flag/`available` pattern it already references to the now-real `.on()` API.\n-- Code comments: remove \"intentionally inert\" / \"event API not implemented in this SDK task\" / \"wired by a later task\" language in sdk/plugin.ts and anywhere else that calls the event API reserved/unimplemented.\n-\n-## Acceptance\n-Docs describe the real API and no stale \"RESERVED/inert/not implemented\" claims about the event surface remain in code or specs.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KT9JXF0QVN9WM093EM40PC09","timestamp":"2026-06-04T15:09:34.103878Z","op":"update","item_id":"01KT9FZEEXHCS0PSVCC8PNG4C9","forward_patch":"--- original\n+++ modified\n@@ -10,10 +10,12 @@\n ---\n Document the event-subscription API now that it is real. Depends on the SDK surface card.\n\n+WRITING CONSTRAINT (user feedback): do NOT use internal architecture jargon (\"plane\", \"bridge\", \"fan-in\") in plugin-author-facing docs — nobody outside the codebase knows those words. Describe events by their concrete string names and which service emits them.\n+\n ## Scope\n-- `ideas/plugins/plugin-architecture.md`: add an \"Events / subscriptions\" section — the `this.<server>.on(event, cb)` surface, the `notifications/<server>/<event>` method-naming convention, the bridge→isolate delivery path (NotificationBridge → per-host pump → invoke_plugin_callback), auto-cleanup on unload, and the cached-flag pattern for reactive `available()` (the canonical consumer).\n-- `ideas/plugins/command-service.md`: cross-link the cached-flag/`available` pattern it already references to the now-real `.on()` API.\n-- Code comments: remove \"intentionally inert\" / \"event API not implemented in this SDK task\" / \"wired by a later task\" language in sdk/plugin.ts and anywhere else that calls the event API reserved/unimplemented.\n+- `ideas/plugins/plugin-architecture.md`: add an \"Events\" section — `this.<server>.on(event, cb)` returns an off handle; events are the notifications each service DECLARES (shown by string name in the generated types); the cached-flag pattern for reactive `available()` (the canonical consumer). Explain in concrete terms (e.g. \"the command service emits an `executed` event after each command runs\"), not abstractions.\n+- `ideas/plugins/command-service.md`: cross-link the cached-flag/`available` pattern to the now-real `.on()` API.\n+- Code comments: remove \"intentionally inert\" / \"event API not implemented in this SDK task\" / \"wired by a later task\" language in sdk/plugin.ts and anywhere else calling the event API reserved/unimplemented.\n\n ## Acceptance\n-Docs describe the real API and no stale \"RESERVED/inert/not implemented\" claims about the event surface remain in code or specs.\n\\ No newline at end of file\n+Docs describe the real API in concrete terms (event string names + emitting service), no stale \"RESERVED/inert\" claims remain, and no internal jargon leaks into author-facing prose.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -10,12 +10,10 @@\n ---\n Document the event-subscription API now that it is real. Depends on the SDK surface card.\n\n-WRITING CONSTRAINT (user feedback): do NOT use internal architecture jargon (\"plane\", \"bridge\", \"fan-in\") in plugin-author-facing docs — nobody outside the codebase knows those words. Describe events by their concrete string names and which service emits them.\n-\n ## Scope\n-- `ideas/plugins/plugin-architecture.md`: add an \"Events\" section — `this.<server>.on(event, cb)` returns an off handle; events are the notifications each service DECLARES (shown by string name in the generated types); the cached-flag pattern for reactive `available()` (the canonical consumer). Explain in concrete terms (e.g. \"the command service emits an `executed` event after each command runs\"), not abstractions.\n-- `ideas/plugins/command-service.md`: cross-link the cached-flag/`available` pattern to the now-real `.on()` API.\n-- Code comments: remove \"intentionally inert\" / \"event API not implemented in this SDK task\" / \"wired by a later task\" language in sdk/plugin.ts and anywhere else calling the event API reserved/unimplemented.\n+- `ideas/plugins/plugin-architecture.md`: add an \"Events / subscriptions\" section — the `this.<server>.on(event, cb)` surface, the `notifications/<server>/<event>` method-naming convention, the bridge→isolate delivery path (NotificationBridge → per-host pump → invoke_plugin_callback), auto-cleanup on unload, and the cached-flag pattern for reactive `available()` (the canonical consumer).\n+- `ideas/plugins/command-service.md`: cross-link the cached-flag/`available` pattern it already references to the now-real `.on()` API.\n+- Code comments: remove \"intentionally inert\" / \"event API not implemented in this SDK task\" / \"wired by a later task\" language in sdk/plugin.ts and anywhere else that calls the event API reserved/unimplemented.\n\n ## Acceptance\n-Docs describe the real API in concrete terms (event string names + emitting service), no stale \"RESERVED/inert\" claims remain, and no internal jargon leaks into author-facing prose.\n\\ No newline at end of file\n+Docs describe the real API and no stale \"RESERVED/inert/not implemented\" claims about the event surface remain in code or specs.\n\\ No newline at end of file\n","transaction_id":null}

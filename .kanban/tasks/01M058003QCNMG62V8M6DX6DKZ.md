---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m058990b8kyn9s1h17p33r7q
  text: |-
    Research done. Repo-wide sweep for prose that describes a deleted complexity gate as live (`rg -i 'complexity'` over the whole repo, minus `.git`, `target`, `ideas/`, `.kanban/`, and minus `clippy::type_complexity`) gave 58 hits. Verified each.

    The four the card names:

    1. `crates/swissarmyhammer-validators/src/review/tree_sitter_probes.rs` module doc — "the same table the entity extractor and the complexity scorer read". The surviving reader of that routing table in this module is the test census: the module imports `parse_code, test_census, ParsedCode, SurfaceChange, TestCensus`.
    2. same file, `AssertionCensusProbe` doc — "the same roster the complexity scorer's test exemption reads". The roster is `DEFINITION_SPECS`/`DefinitionSpec` in `swissarmyhammer-sem/src/parser/plugins/code/definitions.rs`. Confirmed by grep that `definitions::is_test_definition` and `DEFINITION_SPECS` are read only by `test_census.rs` now (`duplication.rs` has its own same-named `DefinitionSpec`/`is_test_definition`, a different type).
    3. `crates/swissarmyhammer-validators/src/review/scope.rs`, `run_probe_cache` doc — "file-bound probes (`complexity`, the tree-sitter family)". `probes.rs` already states the tree-sitter family is the file-bound family; `ProbeOp` has `Callers`, `Duplicates`, `Similar`, `CloneSiblings`, `TreeSitter`. No `complexity` op.
    4. `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`, `verify_shipped_tool_rules_pass_fixtures` doc — "one roster — the complexity rules — mixes rules that replace one prompt rule with a rule that replaces two". Checked every surviving roster in `tool_rules/tests.rs`: `SHIPPED_MISSING_DOCS_RULES`, `SHIPPED_DEAD_CODE_RULES`, `SHIPPED_MAGIC_NUMBERS_RULES`, `SHIPPED_FUNCTION_LENGTH_RULES` each name ONE list for every row, and `SUPERSEDES_MISSING_DOCS` / `_DEAD_CODE` / `_MAGIC_NUMBERS` / `_FUNCTION_LENGTH` each hold exactly one prompt rule. `SHIPPED_STUTTERING_NAME_RULES` and `SHIPPED_UNUSED_DEPENDENCY_RULES` name `SUPERSEDES_NOTHING`. So no roster mixes any more; the stated reason is stale twice over.

    Beyond the four, three more places state a complexity gate that no longer ships:

    5. `doc/src/concepts/validators.md` — the Code Quality list carries a "Cognitive complexity — flags overly complex functions" bullet.
    6. `doc/src/concepts/integrated-sdlc.md` — "the code-quality validator checks for cognitive complexity, magic numbers, and naming issues".
    7. `builtin/validators/reuse/rules/reuse.md` — "a function under the length/complexity limits". There is no complexity limit; `function-length` is the one size gate.
    8. `builtin/validators/README.md` — a recorded swiftlint 0.65.0 measurement, "over three fixtures, 2 entries for magic numbers, 2 for complexity and 3 for missing docs". Traced to commit `bcc6d6cc3`, when the three shipped swiftlint rules were `complexity-swift`, `magic-numbers-swift`, `missing-docs-swift`. Today they are `function-length-swift`, `magic-numbers-swift`, `missing-docs-swift`, so the complexity count names a fixture that no longer ships.

    Left alone on purpose:

    - `crates/mirdan/src/builtin_validators.rs:97` and `crates/swissarmyhammer-validators/src/builtin/mod.rs:116` — the two load-bearing retired-set entries the card protects.
    - `crates/mirdan/src/builtin_validators.rs:70` and `crates/swissarmyhammer-validators/src/builtin/mod.rs:880` — comments naming the same nine retired sets as history of the merge. They state a deletion, not a live gate.
    - `crates/swissarmyhammer-validators/src/builtin/mod.rs:303` and `builtin/validators/code-hygiene/rules/function-length-dart.md:140` — both already state that nothing measures complexity.
    - `builtin/validators/code-hygiene/VALIDATOR.md` — already carries the "Complexity is measured by nothing" section and the `clippy::cognitive_complexity` rejection the card protects.
    - `crates/swissarmyhammer-sem/src/parser/plugins/code/mod.rs:53` — records `complexity.rs` as history, "whose surviving half is now `definitions.rs`". Correct as it stands.
    - `crates/swissarmyhammer-validators/.fixtures/claude/stop_cognitive_complexity_*.json` — recorded Claude session transcripts used as hook-input fixtures, not prose about this system.
    - `apps/swissarmyhammer-cli/src/*.rs` and `swissarmyhammer-templating` — "reduce cognitive complexity", "nesting depth limits" used in the ordinary sense, unrelated to the deleted scorer.
  timestamp: 2026-08-16T12:21:00.043769+00:00
- actor: claude-code
  id: 01m058w5j4cg802phvrw5b7rtc
  text: |-
    Implementation landed. Seven files, all prose.

    The four the card names:

    1. `crates/swissarmyhammer-validators/src/review/tree_sitter_probes.rs` module doc — "the same table the entity extractor and the complexity scorer read" is now "the same table the entity extractor and the test census read". Verified: the module imports `parse_code, test_census, ParsedCode` and `test_census` routes on `ParsedCode::language()`, which the same roster assigned.
    2. same file, `AssertionCensusProbe` doc — "the same roster the complexity scorer's test exemption reads" is now "the `DefinitionSpec` roster the `swissarmyhammer-sem` code plugin holds". Plain backticks, not an intra-doc link, so nothing resolves against a type this crate does not import.
    3. `crates/swissarmyhammer-validators/src/review/scope.rs`, `run_probe_cache` doc — "file-bound probes (`complexity`, the tree-sitter family)" is now "file-bound probes — the tree-sitter family —". Matches what `probes.rs` already states over `per_file_results`.
    4. `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`, `verify_shipped_tool_rules_pass_fixtures` doc — the complexity-roster reason is replaced by what the rosters hold now: most name one prompt rule for every row, `SHIPPED_STUTTERING_NAME_RULES` and `SHIPPED_UNUSED_DEPENDENCY_RULES` name `SUPERSEDES_NOTHING`, no roster mixes the two today, and a per-row list is what would let one do so without a second helper. Every clause checked against `tool_rules/tests.rs`.

    Three more the repo-wide sweep found:

    5. `doc/src/concepts/validators.md` — dropped the "Cognitive complexity — flags overly complex functions" bullet.
    6. `doc/src/concepts/integrated-sdlc.md` — "the code-quality validator checks for cognitive complexity, magic numbers, and naming issues" is now "the code-hygiene validator checks for commented-out code, overlong functions, and undocumented public APIs", taken word for word from the `code-hygiene` VALIDATOR.md description.
    7. `builtin/validators/reuse/rules/reuse.md` — "under the length/complexity limits" is now "under the length limit". `function-length` is the one size gate the set states.
    8. `builtin/validators/README.md` — the recorded swiftlint 0.65.0 measurement counted "2 entries for magic numbers, 2 for complexity and 3 for missing docs" over three fixtures. `complexity-swift` no longer ships (it did at commit `bcc6d6cc3`, when that line was written), so the line now records the two fixtures that survive: "2 entries over the magic-numbers fixture and 3 over the missing-docs fixture". The count for the retired fixture is dropped rather than reassigned — reassigning it to `function-length-swift` would state a measurement nobody took.

    Confirmed untouched, by `git diff` returning nothing for all three:

    - `crates/mirdan/src/builtin_validators.rs` — the `"complexity"` retired-set entry still stands.
    - `crates/swissarmyhammer-validators/src/builtin/mod.rs` — same.
    - `builtin/validators/code-hygiene/VALIDATOR.md` — `clippy::cognitive_complexity` still stands, and so does the "Complexity is measured by nothing" section.

    Also left alone, and why: the `plugins/code` module doc records `complexity.rs` as history and names `definitions.rs` as its surviving half, which is correct as written; the two merge-history comments in `mirdan` and `builtin/mod.rs` state a deletion rather than a live gate; `builtin/mod.rs` and `function-length-dart.md` already state that nothing measures complexity; and `.fixtures/claude/stop_cognitive_complexity_*.json` are recorded Claude transcripts used as hook-input fixtures, not prose about this system.

    Rules preloaded before editing: read the full `dump validators` output for `.rs` and `.md` (52 rules over code-hygiene, code-security, completeness, duplication, magic-numbers, naming, reuse, rust, test-integrity). No rule conflicts with a prose-only edit. Two bear on it and both are satisfied: `no-commented-code` exempts documentation comments and only fires on a block that reads as statements of the file's own language, and `rust/documentation` plus `missing-docs-rust` are moved only in the safe direction because no `///` or `//!` was removed.

    Discovered work, filed as ^epe1z67 rather than fixed here: `doc/src/concepts/validators.md` still lists "No string equality", "No log truncation" and "Naming consistency" as built-in rules that the validator tree does not hold, and `builtin/validators/README.md` states "each shipped tool rule replaces exactly one prompt rule", which `stuttering-name-go` and `unused-dependencies-rust` refute — neither declares a `supersedes` key. Neither names the deleted scorer, so neither was in this card's scope.
  timestamp: 2026-08-16T12:31:19.108450+00:00
- actor: claude-code
  id: 01m058wet0hm3an0fjnvp94thv
  text: |-
    ### implement — changed
    - evidence: 7 files, all prose. `crates/swissarmyhammer-validators/src/review/tree_sitter_probes.rs` (2 places), `crates/swissarmyhammer-validators/src/review/scope.rs`, `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`, `doc/src/concepts/validators.md`, `doc/src/concepts/integrated-sdlc.md`, `builtin/validators/reuse/rules/reuse.md`, `builtin/validators/README.md`. `cargo fmt` clean. `cargo clippy --workspace --all-targets -- -D warnings` exit 0. `cargo nextest run --workspace`: 14105 tests run, 14105 passed, 0 failed, 0 skipped. `cargo doc -p swissarmyhammer-validators -p swissarmyhammer-sem --no-deps`: 56 warnings, all pre-existing broken intra-doc links (`COMMENT_SPECS`, `duplication`, `forward_notifications`, `PRIME_HANDOFF`, `render_file_block`, `focus_file_line_bytes`, `build_candidates`, `collect_fan_out`, `auto_purpose`); none names anything in the diff, and every symbol the diff added stands in plain backticks rather than an intra-doc link. The two load-bearing retired-set entries and `clippy::cognitive_complexity` are untouched, confirmed by an empty `git diff` over their three files.
    - next: `/review`. Left in `doing`, not committed. Discovered work filed as ^epe1z67.
  timestamp: 2026-08-16T12:31:28.576201+00:00
- actor: claude-code
  id: 01m059737gzkyfr0k9wwtgxnr5
  text: |-
    ### test — green

    - `cargo fmt --check` — exit 0, no diff.
    - `cargo clippy --workspace --all-targets -- -D warnings` — exit 0, zero warnings.
    - `cargo nextest run --workspace` — 14105 tests run: 14105 passed (31 slow), 0 failed, 0 skipped.
    - Shipped-content check: `builtin/validators/README.md` and `builtin/validators/reuse/rules/reuse.md` diffs are both prose only (README.md fixture-count sentence reworded; reuse.md drops "complexity" from the length-limit sentence). Grepped the changed strings across `crates/` `.rs` files — no guard test references any of the changed text. `cargo build -p mirdan` (which embeds both files via `build.rs`) succeeds, and mirdan's own tests passed inside the workspace nextest run.
    - Test-file diff check: `git diff` on `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs` shows the change is confined to a `///` doc comment above the roster helper — it now says most rosters name one prompt rule per row while `SHIPPED_STUTTERING_NAME_RULES` and `SHIPPED_UNUSED_DEPENDENCY_RULES` name `SUPERSEDES_NOTHING`, and that no roster mixes the two today. No `assert!`/`assert_eq!` line or any executable line was touched.
    - Protected-items check: `git diff --stat` is empty for all three — `crates/mirdan/src/builtin_validators.rs`, `crates/swissarmyhammer-validators/src/builtin/mod.rs`, `builtin/validators/code-hygiene/VALIDATOR.md`.
    - `cargo doc -p swissarmyhammer-validators -p swissarmyhammer-sem --no-deps` — 56 total warning lines (46 for swissarmyhammer-validators + 8 for swissarmyhammer-sem, plus their 2 "generated N warnings" summary lines = 56), matching the implementer's reported pre-existing baseline exactly. The two warnings that reference `scope.rs` point at lines 513 and 696 — outside the edited region (the prose edit sits at lines 1301-1310 in the pre-edit diff hunk). No warning references `tree_sitter_probes.rs` or `shipped.rs` at all.

    No revert of any prose correction was needed — build is green as-is. Task left in `doing`, not committed.
  timestamp: 2026-08-16T12:37:17.168601+00:00
position_column: doing
position_ordinal: '8280'
title: Stale prose still names the deleted complexity scorer
---
^mecxacr deleted the cognitive-complexity scorer from `swissarmyhammer-sem` (commit `7c555aa20`). Prose in four places still describes it as if it runs.

## Where

- `crates/swissarmyhammer-validators/src/review/tree_sitter_probes.rs`
- `crates/swissarmyhammer-validators/src/review/scope.rs`
- `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`
- `doc/src/concepts/validators.md`

## Why it was not fixed with the deletion

The reviewer of `7c555aa20` found it and correctly declined to record it as a finding: none of it stands on a line that commit wrote, and a defect on an unchanged line is not a finding under a diff-scoped review.

## What must NOT change

Two `"complexity"` hits are load-bearing and must stay:

- `crates/mirdan/src/builtin_validators.rs:97`
- `crates/swissarmyhammer-validators/src/builtin/mod.rs:116`

Both stand in retired-set lists that assert the set is ABSENT. Removing either weakens a real guard.

Also leave `clippy::cognitive_complexity` in `builtin/validators/code-hygiene/VALIDATOR.md`. That names the clippy lint, which still exists, rather than the deleted code. The `plugins/code` module doc records the deletion as history on purpose; read it before you edit it.

## What to do

Read each of the four places and correct what it states to what the code now does. The grammar layer the scorer hid is now `crates/swissarmyhammer-sem/src/parser/plugins/code/definitions.rs`, holding `DefinitionSpec` and `DEFINITION_SPECS`, and the test census is `code/test_census.rs`.

Do not invent a replacement capability in the prose. State what is there.

## Done when

- No prose describes the complexity scorer as a thing that runs.
- The two load-bearing retired-set entries are untouched.
- `cargo nextest run --workspace` green; fmt and clippy clean.

#tool-validators
---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m058990b8kyn9s1h17p33r7q
  text: |-
    Research done. Repo-wide sweep for prose that describes a deleted complexity gate as live (`rg -i 'complexity'` over the whole repo, minus `.git`, `target`, `ideas/`, `.kanban/`, and minus `clippy::type_complexity`) gave 58 hits. Verified each.

    The four the card names:

    1. `crates/swissarmyhammer-validators/src/review/tree_sitter_probes.rs` module doc — "the same table the entity extractor and the complexity scorer read". The surviving reader of that routing table in this module is the test census: the module imports `parse_code, test_census, ParsedCode, SurfaceChange, TestCensus`.
    2. same file, `AssertionCensusProbe` doc — "the same roster the complexity scorer's test exemption reads". The roster is `DEFINITION_SPECS`/`DefinitionSpec` in `swissarmyhammer-sem/src/parser/plugins/code/definitions.rs`. Confirmed by grep that `definitions::is_test_definition` and `DEFINITION_SPECS` are read only by `test_census.rs` now (`duplication.rs` has its own same-named `DefinitionSpec`/`is_test_definition`, a different type).
    3. `crates/swissarmyhammer-validators/src/review/scope.rs`, `run_probe_cache` doc — "file-bound probes (`complexity`, the tree-sitter family)". `probes.rs` already states the tree-sitter family is the file-bound family; `ProbeOp` has `Callers`, `Duplicates`, `Similar`, `CloneSiblings`, `TreeSitter`. No `complexity` op.
    4. `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`, `verify_shipped_tool_rules_pass_fixtures` doc — "one roster — the complexity rules — mixes rules that replace one prompt rule with a rule that replaces two". Checked every surviving roster in `tool_rules/tests.rs`: `SHIPPED_MISSING_DOCS_RULES`, `SHIPPED_DEAD_CODE_RULES`, `SHIPPED_MAGIC_NUMBERS_RULES`, `SHIPPED_FUNCTION_LENGTH_RULES` each name ONE list for every row, and `SUPERSEDES_MISSING_DOCS` / `_DEAD_CODE` / `_MAGIC_NUMBERS` / `_FUNCTION_LENGTH` each hold exactly one prompt rule. `SHIPPED_STUTTERING_NAME_RULES` and `SHIPPED_UNUSED_DEPENDENCY_RULES` name `SUPERSEDES_NOTHING`. So no roster mixes any more; the stated reason is stale twice over.

    Beyond the four, three more places state a complexity gate that no longer ships:

    5. `doc/src/concepts/validators.md` — the Code Quality list carries a "Cognitive complexity — flags overly complex functions" bullet.
    6. `doc/src/concepts/integrated-sdlc.md` — "the code-quality validator checks for cognitive complexity, magic numbers, and naming issues".
    7. `builtin/validators/reuse/rules/reuse.md` — "a function under the length/complexity limits". There is no complexity limit; `function-length` is the one size gate.
    8. `builtin/validators/README.md` — a recorded swiftlint 0.65.0 measurement, "over three fixtures, 2 entries for magic numbers, 2 for complexity and 3 for missing docs". Traced to commit `bcc6d6cc3`, when the three shipped swiftlint rules were `complexity-swift`, `magic-numbers-swift`, `missing-docs-swift`. Today they are `function-length-swift`, `magic-numbers-swift`, `missing-docs-swift`, so the complexity count names a fixture that no longer ships.

    Left alone on purpose:

    - `crates/mirdan/src/builtin_validators.rs:97` and `crates/swissarmyhammer-validators/src/builtin/mod.rs:116` — the two load-bearing retired-set entries the card protects.
    - `crates/mirdan/src/builtin_validators.rs:70` and `crates/swissarmyhammer-validators/src/builtin/mod.rs:880` — comments naming the same nine retired sets as history of the merge. They state a deletion, not a live gate.
    - `crates/swissarmyhammer-validators/src/builtin/mod.rs:303` and `builtin/validators/code-hygiene/rules/function-length-dart.md:140` — both already state that nothing measures complexity.
    - `builtin/validators/code-hygiene/VALIDATOR.md` — already carries the "Complexity is measured by nothing" section and the `clippy::cognitive_complexity` rejection the card protects.
    - `crates/swissarmyhammer-sem/src/parser/plugins/code/mod.rs:53` — records `complexity.rs` as history, "whose surviving half is now `definitions.rs`". Correct as it stands.
    - `crates/swissarmyhammer-validators/.fixtures/claude/stop_cognitive_complexity_*.json` — recorded Claude session transcripts used as hook-input fixtures, not prose about this system.
    - `apps/swissarmyhammer-cli/src/*.rs` and `swissarmyhammer-templating` — "reduce cognitive complexity", "nesting depth limits" used in the ordinary sense, unrelated to the deleted scorer.
  timestamp: 2026-08-16T12:21:00.043769+00:00
- actor: claude-code
  id: 01m058w5j4cg802phvrw5b7rtc
  text: |-
    Implementation landed. Seven files, all prose.

    The four the card names:

    1. `crates/swissarmyhammer-validators/src/review/tree_sitter_probes.rs` module doc — "the same table the entity extractor and the complexity scorer read" is now "the same table the entity extractor and the test census read". Verified: the module imports `parse_code, test_census, ParsedCode` and `test_census` routes on `ParsedCode::language()`, which the same roster assigned.
    2. same file, `AssertionCensusProbe` doc — "the same roster the complexity scorer's test exemption reads" is now "the `DefinitionSpec` roster the `swissarmyhammer-sem` code plugin holds". Plain backticks, not an intra-doc link, so nothing resolves against a type this crate does not import.
    3. `crates/swissarmyhammer-validators/src/review/scope.rs`, `run_probe_cache` doc — "file-bound probes (`complexity`, the tree-sitter family)" is now "file-bound probes — the tree-sitter family —". Matches what `probes.rs` already states over `per_file_results`.
    4. `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`, `verify_shipped_tool_rules_pass_fixtures` doc — the complexity-roster reason is replaced by what the rosters hold now: most name one prompt rule for every row, `SHIPPED_STUTTERING_NAME_RULES` and `SHIPPED_UNUSED_DEPENDENCY_RULES` name `SUPERSEDES_NOTHING`, no roster mixes the two today, and a per-row list is what would let one do so without a second helper. Every clause checked against `tool_rules/tests.rs`.

    Three more the repo-wide sweep found:

    5. `doc/src/concepts/validators.md` — dropped the "Cognitive complexity — flags overly complex functions" bullet.
    6. `doc/src/concepts/integrated-sdlc.md` — "the code-quality validator checks for cognitive complexity, magic numbers, and naming issues" is now "the code-hygiene validator checks for commented-out code, overlong functions, and undocumented public APIs", taken word for word from the `code-hygiene` VALIDATOR.md description.
    7. `builtin/validators/reuse/rules/reuse.md` — "under the length/complexity limits" is now "under the length limit". `function-length` is the one size gate the set states.
    8. `builtin/validators/README.md` — the recorded swiftlint 0.65.0 measurement counted "2 entries for magic numbers, 2 for complexity and 3 for missing docs" over three fixtures. `complexity-swift` no longer ships (it did at commit `bcc6d6cc3`, when that line was written), so the line now records the two fixtures that survive: "2 entries over the magic-numbers fixture and 3 over the missing-docs fixture". The count for the retired fixture is dropped rather than reassigned — reassigning it to `function-length-swift` would state a measurement nobody took.

    Confirmed untouched, by `git diff` returning nothing for all three:

    - `crates/mirdan/src/builtin_validators.rs` — the `"complexity"` retired-set entry still stands.
    - `crates/swissarmyhammer-validators/src/builtin/mod.rs` — same.
    - `builtin/validators/code-hygiene/VALIDATOR.md` — `clippy::cognitive_complexity` still stands, and so does the "Complexity is measured by nothing" section.

    Also left alone, and why: the `plugins/code` module doc records `complexity.rs` as history and names `definitions.rs` as its surviving half, which is correct as written; the two merge-history comments in `mirdan` and `builtin/mod.rs` state a deletion rather than a live gate; `builtin/mod.rs` and `function-length-dart.md` already state that nothing measures complexity; and `.fixtures/claude/stop_cognitive_complexity_*.json` are recorded Claude transcripts used as hook-input fixtures, not prose about this system.

    Rules preloaded before editing: read the full `dump validators` output for `.rs` and `.md` (52 rules over code-hygiene, code-security, completeness, duplication, magic-numbers, naming, reuse, rust, test-integrity). No rule conflicts with a prose-only edit. Two bear on it and both are satisfied: `no-commented-code` exempts documentation comments and only fires on a block that reads as statements of the file's own language, and `rust/documentation` plus `missing-docs-rust` are moved only in the safe direction because no `///` or `//!` was removed.

    Discovered work, filed as ^epe1z67 rather than fixed here: `doc/src/concepts/validators.md` still lists "No string equality", "No log truncation" and "Naming consistency" as built-in rules that the validator tree does not hold, and `builtin/validators/README.md` states "each shipped tool rule replaces exactly one prompt rule", which `stuttering-name-go` and `unused-dependencies-rust` refute — neither declares a `supersedes` key. Neither names the deleted scorer, so neither was in this card's scope.
  timestamp: 2026-08-16T12:31:19.108450+00:00
- actor: claude-code
  id: 01m058wet0hm3an0fjnvp94thv
  text: |-
    ### implement — changed
    - evidence: 7 files, all prose. `crates/swissarmyhammer-validators/src/review/tree_sitter_probes.rs` (2 places), `crates/swissarmyhammer-validators/src/review/scope.rs`, `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`, `doc/src/concepts/validators.md`, `doc/src/concepts/integrated-sdlc.md`, `builtin/validators/reuse/rules/reuse.md`, `builtin/validators/README.md`. `cargo fmt` clean. `cargo clippy --workspace --all-targets -- -D warnings` exit 0. `cargo nextest run --workspace`: 14105 tests run, 14105 passed, 0 failed, 0 skipped. `cargo doc -p swissarmyhammer-validators -p swissarmyhammer-sem --no-deps`: 56 warnings, all pre-existing broken intra-doc links (`COMMENT_SPECS`, `duplication`, `forward_notifications`, `PRIME_HANDOFF`, `render_file_block`, `focus_file_line_bytes`, `build_candidates`, `collect_fan_out`, `auto_purpose`); none names anything in the diff, and every symbol the diff added stands in plain backticks rather than an intra-doc link. The two load-bearing retired-set entries and `clippy::cognitive_complexity` are untouched, confirmed by an empty `git diff` over their three files.
    - next: `/review`. Left in `doing`, not committed. Discovered work filed as ^epe1z67.
  timestamp: 2026-08-16T12:31:28.576201+00:00
- actor: claude-code
  id: 01m059737gzkyfr0k9wwtgxnr5
  text: |-
    ### test — green

    - `cargo fmt --check` — exit 0, no diff.
    - `cargo clippy --workspace --all-targets -- -D warnings` — exit 0, zero warnings.
    - `cargo nextest run --workspace` — 14105 tests run: 14105 passed (31 slow), 0 failed, 0 skipped.
    - Shipped-content check: `builtin/validators/README.md` and `builtin/validators/reuse/rules/reuse.md` diffs are both prose only (README.md fixture-count sentence reworded; reuse.md drops "complexity" from the length-limit sentence). Grepped the changed strings across `crates/` `.rs` files — no guard test references any of the changed text. `cargo build -p mirdan` (which embeds both files via `build.rs`) succeeds, and mirdan's own tests passed inside the workspace nextest run.
    - Test-file diff check: `git diff` on `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs` shows the change is confined to a `///` doc comment above the roster helper — it now says most rosters name one prompt rule per row while `SHIPPED_STUTTERING_NAME_RULES` and `SHIPPED_UNUSED_DEPENDENCY_RULES` name `SUPERSEDES_NOTHING`, and that no roster mixes the two today. No `assert!`/`assert_eq!` line or any executable line was touched.
    - Protected-items check: `git diff --stat` is empty for all three — `crates/mirdan/src/builtin_validators.rs`, `crates/swissarmyhammer-validators/src/builtin/mod.rs`, `builtin/validators/code-hygiene/VALIDATOR.md`.
    - `cargo doc -p swissarmyhammer-validators -p swissarmyhammer-sem --no-deps` — 56 total warning lines (46 for swissarmyhammer-validators + 8 for swissarmyhammer-sem, plus their 2 "generated N warnings" summary lines = 56), matching the implementer's reported pre-existing baseline exactly. The two warnings that reference `scope.rs` point at lines 513 and 696 — outside the edited region (the prose edit sits at lines 1301-1310 in the pre-edit diff hunk). No warning references `tree_sitter_probes.rs` or `shipped.rs` at all.

    No revert of any prose correction was needed — build is green as-is. Task left in `doing`, not committed.
  timestamp: 2026-08-16T12:37:17.168601+00:00
- actor: claude-code
  id: 01m0599r0g55am1sryca7h37ah
  text: |-
    ### commit — changed
    - evidence: 6c718dec7 docs(validators): drop stale complexity-scorer text from prose (^6dx6dkz); 9 files changed, 154 insertions(+), 18 deletions(-)
    - next: none
  timestamp: 2026-08-16T12:38:43.984816+00:00
- actor: claude-code
  id: 01m059ptg61x83rb1zk0v4fpmj
  text: |
    ### review — findings
    - evidence: `review sha HEAD~1..HEAD` (commit 6c718dec7) returned 0 findings over the 3 Rust files; the 4 changed Markdown files were outside what it reviewed. Driver verification of the rewritten claims found 2 false ones, both on lines this commit wrote: tree_sitter_probes.rs:15 (the test census does not read the grammar table; commented_code.rs and duplication.rs do) and scope.rs:1304 (clone-siblings is file-bound and is not tree-sitter). Claims verified true: the DefinitionSpec roster, the shipped.rs roster exhaustive claim over all six rosters, the swiftlint 2/3 numbers, integrated-sdlc.md, reuse.md. The three protected items are untouched.
    - next: correct the two claims to name what the code holds, then re-review.
  timestamp: 2026-08-16T12:45:52.518613+00:00
- actor: claude-code
  id: 01m059rhwbe3azxbe3v7kdvmaj
  text: |
    ### finish iteration 1 — findings
    - implement: changed — 7 files, all prose. The four the card names plus four the repo-wide sweep found (doc/src/concepts/integrated-sdlc.md, builtin/validators/reuse/rules/reuse.md, builtin/validators/README.md, and the validators.md bullet). Two unrelated stale claims filed as ^epe1z67.
    - test: green — cargo fmt --check exit 0; cargo clippy --workspace --all-targets -- -D warnings exit 0, 0 warnings; cargo nextest run --workspace 14105 passed, 0 failed, 0 skipped; cargo build -p mirdan succeeds so the validator embed still builds; cargo doc no new warnings.
    - commit: 6c718dec7 docs(validators): drop stale complexity-scorer text from prose (^6dx6dkz) — 9 files changed, 154 insertions, 18 deletions
    - review: findings — 2 findings. crates/swissarmyhammer-validators/src/review/tree_sitter_probes.rs:15 and crates/swissarmyhammer-validators/src/review/scope.rs:1304. The engine answered 0 findings over the 3 Rust files; the four changed Markdown files were not among the files it reviewed, so those claims were verified by hand.

    ### The findings are THIS CARD'S OWN DEFECT, repeated

    This card exists because prose reached further than the code. Two of the replacement claims do the same thing.

    1. `tree_sitter_probes.rs:15` now says the grammar table is "the same table the entity extractor and the test census read". The test census does NOT read that table. `get_language_config` is called from exactly one place — `plugins/code/mod.rs:210`, inside `parse_code` — so a reader of the table is a `parse_code` caller. `test_census` takes an already-parsed value, and its only `parse_code` calls stand inside `#[cfg(test)]`. The real production readers are `commented_code.rs` and `duplication.rs`, and neither is named.
    2. `scope.rs:1304` now says "file-bound probes — the tree-sitter family — read the current source of every matched file", which makes the tree-sitter family the whole file-bound set. `clone-siblings` is file-bound and is not tree-sitter: it is a `CODE_CONTEXT_PROBES` row carrying `ProbeOp::CloneSiblings`, and `run_clone_siblings` iterates `files_under_review(file_change)`, reading the same `sources` map this function builds. The OLD text named two members; dropping the retired one should have left `clone-siblings` rather than narrowing the set to one family.

    ### What the review verified and found CORRECT

    - The `DefinitionSpec` roster exists at `crates/swissarmyhammer-sem/src/parser/plugins/code/definitions.rs`, and `test_census.rs:115` reads it through `spec_for_language`.
    - The `shipped.rs` exhaustive claim holds across ALL SIX rosters, not only the two named: four use a one-element supersedes constant on every row, two use `SUPERSEDES_NOTHING`, and each roster is uniform, so none mixes the two.
    - The swiftlint numbers match the recorded measurements exactly — `shipped/magic_numbers.rs:535` records 2 entries and `shipped/missing_docs.rs:1489` records 3. The retired fixture's count and the "over three fixtures" phrase are both gone.
    - The three protected items are untouched: `git show --stat HEAD` lists none of them.
    - The 154/18 ratio is accounted for: 133 of the 154 insertions are this card's own board files, leaving about 21 insertions against 18 deletions of real prose.
    - next: iteration 2 works the two open findings
  timestamp: 2026-08-16T12:46:49.227630+00:00
- actor: claude-code
  id: 01m05aa2y2z6zanmz9a41tj6mn
  text: |-
    Round 2. Both findings accepted. I measured first, then wrote each sentence to the measurement and to nothing wider.

    MEASUREMENT 1 — who reads the grammar table.

    `get_language_config` has exactly one call site in the whole repo: `plugins/code/mod.rs:210`, inside `parse_code`. So a reader of the table is a `parse_code` caller. `parse_code` callers, repo-wide (`rg` over every `.rs` outside `target/`):

    - production: `CodeParserPlugin::extract_entities` (mod.rs:123), `commented_code.rs:260` (`commented_code_blocks`) and `commented_code.rs:440` (`parses_as_code`), `duplication.rs:77` (`duplication_source`), and this module's parse cache (tree_sitter_probes.rs:261).
    - test only: `public_surface.rs:644` and `:843` (the `#[cfg(test)]` opens at 636), `commented_code.rs:1336` (opens at 540), `test_census.rs:476` and `:681` (opens at 469), `mod.rs:246`, `:257`, `:261`, `:267`, `:268`, `:274` (opens at 240).

    The finding is right: the test census reads a parse, not the table. The module doc now reads "…`parse_code`, the only caller of the roster lookup `get_language_config`. Beside this module's parse cache, the code plugin's own `commented_code` and `duplication` modules call it, and so does the entity extraction of `CodeParserPlugin`." I name the two MODULES rather than two functions because `commented_code.rs` calls from two places; naming `commented_code_blocks` alone would have missed `parses_as_code` and repeated the defect in miniature.

    MEASUREMENT 2 — what the file-bound set holds.

    `run_probes` (probes.rs:592) dispatches five ops. Which of them read `file_change.sources`:

    - `ProbeOp::TreeSitter` → `run_tree_sitter_probe` (tree_sitter_probes.rs:292) → `per_file_results` (probes.rs:631), which iterates `file_change.sources`. FILE-BOUND.
    - `ProbeOp::CloneSiblings` → `run_clone_siblings` (probes.rs:917) → `files_under_review` (probes.rs:882), which reads `file_change.sources.keys()` chained with `file_change.entities.iter().map(|e| e.file_path)`. FILE-BOUND.
    - `ProbeOp::Callers`, `ProbeOp::Duplicates` (probes.rs:732 iterates `file_change.entities`), `ProbeOp::Similar` — entity-bound.

    So the file-bound set has exactly two members, as the finding states. The `run_probe_cache` doc now names both and says what each takes: the tree-sitter family reads the current source of every matched file; `clone-siblings` takes the matched file set, unioned with the files the semantic diff names, as the clone sites the change already reached. The union half is stated because `files_under_review` is a union — "the matched file set" alone would have been narrower than the code.

    RE-CHECK OF THE OTHER CLAIMS IN 6c718dec7. The commit wrote seven prose changes. The review verified five of them (the `DefinitionSpec` roster reference, the six-roster supersedes claim, the swiftlint 2/3 numbers, integrated-sdlc.md, reuse.md), so I did not churn those. The two it refuted are the two fixed here. The `doc/src/concepts/validators.md` change is a bullet deletion and states nothing new. My own new claim — "the only caller of `get_language_config`" — I verified repo-wide rather than only under `crates/`: no `.rs` file outside `swissarmyhammer-sem` names it, and the `parse_code` in `swissarmyhammer-lsp/src/diagnostics.rs:116` is a private, unrelated function of the same name over a JSON `Value`.

    Nothing else was touched. `git status` lists exactly two changed source files. The three protected items give an empty `git diff --stat`.
  timestamp: 2026-08-16T12:56:23.746696+00:00
- actor: claude-code
  id: 01m05aanffxaxnv5rjxbsjpxz0
  text: |-
    ### implement — changed
    - evidence: 2 files, both Rust doc comments. `crates/swissarmyhammer-validators/src/review/tree_sitter_probes.rs` (module doc): the grammar-table sentence now names `parse_code` as the only caller of `get_language_config` (measured: one call site repo-wide, mod.rs:210) and names the callers as the `commented_code` and `duplication` modules plus the entity extraction of `CodeParserPlugin` (measured: production `parse_code` callers are mod.rs:123, commented_code.rs:260 and :440, duplication.rs:77, tree_sitter_probes.rs:261; every other call is inside `#[cfg(test)]`). `crates/swissarmyhammer-validators/src/review/scope.rs` (`run_probe_cache` doc): the file-bound set is now stated as the tree-sitter family plus `clone-siblings` (measured: `per_file_results` probes.rs:631 and `files_under_review` probes.rs:882 are the two readers of `file_change.sources`; `run_callers`, `run_duplicates` and `run_similar` read `file_change.entities`). Both `## Review Findings` items flipped to `- [x]`, with the measurement recorded under "### Answers (round 2)".
    - commands: `cargo fmt` clean; `cargo clippy --workspace --all-targets -- -D warnings` exit 0, 0 warnings; `cargo nextest run --workspace` 14105 tests run, 14105 passed (24 slow, 2 leaky), 0 failed, 0 skipped; `cargo doc -p swissarmyhammer-validators -p swissarmyhammer-sem --no-deps` 56 warning lines, the same count the prior round recorded as the pre-existing baseline, and the only two that name a changed file point at scope.rs:513 and scope.rs:696 — outside the edited region at 1301-1312. No warning names tree_sitter_probes.rs.
    - protected: `git diff --stat` is empty for `crates/mirdan/src/builtin_validators.rs`, `crates/swissarmyhammer-validators/src/builtin/mod.rs` and `builtin/validators/code-hygiene/VALIDATOR.md`.
    - next: `/review`. Left in `doing`, not committed.
  timestamp: 2026-08-16T12:56:42.735205+00:00
- actor: claude-code
  id: 01m05akw9e8ejvks75vcmt14jj
  text: |-
    ### test — green

    Iteration 2. Scope: two doc-comment-only files.

    - `cargo fmt --check`: exit 0, no diff.
    - `cargo clippy --workspace --all-targets -- -D warnings`: exit 0, zero warnings.
    - `cargo nextest run --workspace`: 14105 tests run, 14105 passed, 0 failed, 0 skipped.
    - `git diff` on both changed files, read line by line: every changed line is a `///` or `//!` doc comment line. No executable line, signature, or attribute changed. `scope.rs` diff is confined to the `run_probe_cache` doc block near line 1301. `tree_sitter_probes.rs` diff is confined to the module doc `# One roster, one parse` section at the top of the file.
    - Protected items check: `git diff --stat` on `crates/mirdan/src/builtin_validators.rs`, `crates/swissarmyhammer-validators/src/builtin/mod.rs`, `builtin/validators/code-hygiene/VALIDATOR.md` — empty, no output.
    - `cargo doc -p swissarmyhammer-validators -p swissarmyhammer-sem --no-deps`: 56 lines matching `^warning` total (8 from swissarmyhammer-sem, 46 from swissarmyhammer-validators, matches the pre-existing 56-line baseline exactly — zero new warnings). Searched full doc output for `tree_sitter_probes` and `scope.rs`: only two matches, both in `scope.rs` at lines 513 and 696 (`render_file_block` unresolved link, `auto_purpose` private-item link) — both outside the edited region (1301-1312) and pre-existing. No warning names `tree_sitter_probes.rs`. No intra-doc-link regression introduced by this change.

    Build is clean. Task left in `doing`, not committed.
  timestamp: 2026-08-16T13:01:44.622092+00:00
position_column: doing
position_ordinal: '8280'
title: Stale prose still names the deleted complexity scorer
---
^mecxacr deleted the cognitive-complexity scorer from `swissarmyhammer-sem` (commit `7c555aa20`). Prose in four places still describes it as if it runs.

## Where

- `crates/swissarmyhammer-validators/src/review/tree_sitter_probes.rs`
- `crates/swissarmyhammer-validators/src/review/scope.rs`
- `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`
- `doc/src/concepts/validators.md`

## Why it was not fixed with the deletion

The reviewer of `7c555aa20` found it and correctly declined to record it as a finding: none of it stands on a line that commit wrote, and a defect on an unchanged line is not a finding under a diff-scoped review.

## What must NOT change

Two `"complexity"` hits are load-bearing and must stay:

- `crates/mirdan/src/builtin_validators.rs:97`
- `crates/swissarmyhammer-validators/src/builtin/mod.rs:116`

Both stand in retired-set lists that assert the set is ABSENT. Removing either weakens a real guard.

Also leave `clippy::cognitive_complexity` in `builtin/validators/code-hygiene/VALIDATOR.md`. That names the clippy lint, which still exists, rather than the deleted code. The `plugins/code` module doc records the deletion as history on purpose; read it before you edit it.

## What to do

Read each of the four places and correct what it states to what the code now does. The grammar layer the scorer hid is now `crates/swissarmyhammer-sem/src/parser/plugins/code/definitions.rs`, holding `DefinitionSpec` and `DEFINITION_SPECS`, and the test census is `code/test_census.rs`.

Do not invent a replacement capability in the prose. State what is there.

## Done when

- No prose describes the complexity scorer as a thing that runs.
- The two load-bearing retired-set entries are untouched.
- `cargo nextest run --workspace` green; fmt and clippy clean.

#tool-validators

## Review Findings (2026-08-16 07:52)

> Scope: `review sha HEAD~1..HEAD` (commit `6c718dec7`) — reviewed the diffs only — lines this change added or modified. The engine reviewed the 3 Rust files and returned 0 findings; the 4 changed Markdown files were outside what it reviewed, and the two findings below stand on Rust lines this commit rewrote. Both are the card's own defect class: a stale claim replaced by a new claim that reaches further than the code.

- [x] `crates/swissarmyhammer-validators/src/review/tree_sitter_probes.rs:15` `docs/state-what-is-there` — the rewritten module doc says the grammar table is "the same table the entity extractor and the test census read", but the test census does not read that table. The table is reached only through `parse_code` — `get_language_config` is called from exactly one place, `plugins/code/mod.rs:210`, inside `parse_code`. `test_census` takes an already-parsed value (`pub fn test_census(parsed: &ParsedCode, source: &str)`, test_census.rs:114) and its only `parse_code` calls sit inside `#[cfg(test)]` (test_census.rs:472, 476, 681; the `#[cfg(test)]` starts at 469). The production readers of the table other than this module are `commented_code.rs` (calls at 260 and 440) and `duplication.rs` (call at 77), and the sentence names neither. Name the modules that actually call `parse_code`, or drop the appositive and keep only "this module adds no grammar list of its own".

- [x] `crates/swissarmyhammer-validators/src/review/scope.rs:1304` `docs/state-what-is-there` — the rewritten `run_probe_cache` doc says "file-bound probes — the tree-sitter family — read the current source of every matched file", making the tree-sitter family the whole of the file-bound set. `clone-siblings` is also file-bound and is not tree-sitter: it is a `CODE_CONTEXT_PROBES` row carrying `ProbeOp::CloneSiblings` (probes.rs), and `run_clone_siblings` (probes.rs:911) iterates `files_under_review(file_change)` (probes.rs:882), which reads `file_change.sources` — the exact map this function builds through `sources_under_review`. The prior text named two members (`complexity`, the tree-sitter family); removing the retired one should have left `clone-siblings` in place of it, not narrowed the set to the tree-sitter family alone. State the file-bound set as `clone-siblings` plus the tree-sitter family.

### Answers (round 2, 2026-08-16)

Both findings are accepted. Each replacement sentence was written to a measurement taken first.

Finding 1 — measured `get_language_config`: one call site in the whole repo, `plugins/code/mod.rs:210`, inside `parse_code`. Measured `parse_code` callers repo-wide: production callers are `CodeParserPlugin::extract_entities` (mod.rs:123), `commented_code.rs` (260 and 440), `duplication.rs` (77), and this module's parse cache (tree_sitter_probes.rs:261). Every other call stands inside `#[cfg(test)]` (`public_surface.rs` opens at 636, `commented_code.rs` at 540, `test_census.rs` at 469, `mod.rs` at 240). The doc now names `parse_code` as the only caller of `get_language_config`, then names the callers as the `commented_code` and `duplication` modules plus the entity extraction of `CodeParserPlugin`. Modules are named rather than functions, because `commented_code.rs` calls from two places.

Finding 2 — measured the file-bound set: `per_file_results` (probes.rs:631) reads `file_change.sources`, and `run_tree_sitter_probe` (tree_sitter_probes.rs:292) is its only caller; `files_under_review` (probes.rs:882) reads `file_change.sources` keys unioned with the entity file paths, and `run_clone_siblings` (probes.rs:917) is its only caller. `run_callers` and `run_duplicates` (probes.rs:732) iterate `file_change.entities`, so they are entity-bound. The doc now states the file-bound set as the tree-sitter family plus `clone-siblings`, and states what each takes: the current source of every matched file for the first, the matched file set unioned with the files the semantic diff names for the second.

### Claims checked and found true — no change wanted

- `tree_sitter_probes.rs` `AssertionCensusProbe` doc — the `DefinitionSpec` roster exists at `crates/swissarmyhammer-sem/src/parser/plugins/code/definitions.rs`, and `test_census.rs:115` reads it through `spec_for_language(parsed.language())`.
- `tool_rules/tests/shipped.rs` — the exhaustive claim holds across all six rosters. `SHIPPED_MISSING_DOCS_RULES`, `SHIPPED_DEAD_CODE_RULES`, `SHIPPED_MAGIC_NUMBERS_RULES` and `SHIPPED_FUNCTION_LENGTH_RULES` name a one-element supersedes constant on every row; `SHIPPED_STUTTERING_NAME_RULES` and `SHIPPED_UNUSED_DEPENDENCY_RULES` name `SUPERSEDES_NOTHING`. Each roster is uniform, so none mixes the two.
- `builtin/validators/README.md` — the corrected swiftlint numbers match the recorded measurements: `shipped/magic_numbers.rs:535` records swiftlint writing 2 entries, and `shipped/missing_docs.rs:1489` records 3. The retired complexity fixture's count is gone and "over three fixtures" is gone with it.
- `doc/src/concepts/integrated-sdlc.md` — code-hygiene does ship `no-commented-code.md`, `function-length*.md` and `missing-docs*.md`, and ships no complexity rule.
- `builtin/validators/reuse/rules/reuse.md` — "under the length limit" matches the live function-length rules.
- The three protected items are untouched by this commit: `git show --stat HEAD -- crates/mirdan/src/builtin_validators.rs crates/swissarmyhammer-validators/src/builtin/mod.rs builtin/validators/code-hygiene/VALIDATOR.md` lists no files.
- The 154/18 ratio is accounted for: 133 of the 154 insertions are the `.kanban` card files (127 in the `.md`, 6 in the `.jsonl`). The prose change itself is about 21 insertions against 18 deletions.
{"id":"01KPZPS7Q1QBG84YH4DTQ6J0GY","timestamp":"2026-04-24T12:17:40.065371Z","op":"update","item_id":"01KNQXYC4RBQP1N2NQ33P8DPB9","forward_patch":"--- original\n+++ modified\n@@ -6,50 +6,82 @@\n position_column: todo\n position_ordinal: a380\n project: spatial-nav\n-title: Add FocusLayer to inspector (stacks on top of root window layer)\n+title: Each inspector panel is its own FocusLayer (stacked under the window root)\n ---\n ## What\n\n-The root `<FocusLayer name=\"window\">` is added in card 1 (at the app shell). This card adds the inspector's layer so it captures navigation when open, and replaces `useRestoreFocus` with layer stack semantics.\n+Wrap every inspector panel in its own `<FocusLayer name=\"inspector\">`. The inspector stack in a window (see `WindowState.inspector_stack: Vec<String>`) becomes a **chain of parent→child layers** in the layer forest — one layer per panel, each with its parent being the panel below it (or the window root, for the first panel).\n+\n+### Why one layer per panel\n\n+- Each panel is semantically independent — its own focus memory, its own nav boundary.\n+- Closing panel #2 while panel #1 is still open must restore focus to panel #1's last-focused field, not to the window root. That only works if each panel has its own `LayerEntry` with its own `last_focused`.\n+- A dialog opened from within panel #2 (see new card for dialog layers) stacks on top of panel #2, not on top of \"the inspector\". Per-panel layers keep the parent chain honest.\n+- Two windows each with their own inspector stack → separate layer chains under separate window roots. No global \"inspector layer\" concept.\n+\n ### Layer boundary semantics\n\n-**Layers are hard boundaries.** Navigation stays within the active (topmost) layer. You cannot arrow out of the inspector — dismiss it to return to the board. This is the \"semi-modal\" behavior.\n+**Layers are hard boundaries.** Navigation stays within the active (focused) layer. You cannot arrow out of the topmost inspector panel — dismiss it to return to whatever layer was below it. The nav algorithm (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`) enforces this unconditionally.\n\n-When a FocusLayer mounts, it saves the currently focused moniker from the previous layer. When it unmounts, it restores that moniker via `setFocus`. This replaces the ad-hoc `useRestoreFocus()` hook entirely.\n+When a FocusLayer unmounts, the caller reads the parent layer's `last_focused` (stored on `LayerEntry`) and sets it as the new focus for that window. This replaces the ad-hoc `useRestoreFocus()` hook entirely.\n\n ### Files to modify\n\n-1. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`** — Wrap the inspector content in `<FocusLayer name=\"inspector\">`. When the inspector mounts, this pushes onto the layer stack and becomes the active layer. When it unmounts, the layer pops and focus returns to the window layer's previously focused element.\n+1. **`kanban-app/ui/src/components/inspectors-container.tsx`** — Wrap each `InspectorPanel` body in `<FocusLayer name=\"inspector\">`. Each panel gets its own layer; because FocusLayer reads its parent via React context, nesting happens naturally from the DOM tree. Panel #1 is inside the window root layer's context, so its parent is the window root. Panel #2 is rendered as a sibling of panel #1 in the JSX — so its parent is *also* the window root, which is **wrong** if we want the panel-close-to-panel-below behavior.\n+\n+   **Options**:\n+   - (a) Nest the JSX: render panel #2 inside panel #1's subtree. Simple, but may fight against the existing stack-of-sibling-SlidePanels layout.\n+   - (b) Parent panel #N's layer explicitly to panel #(N-1)'s layer key. Requires passing the parent layer key as a prop into the FocusLayer's `parent_key` (instead of reading context). Keeps the visual layout unchanged.\n+   - **Choose (b)**: `InspectorsContainer` tracks the layer keys as it maps over `panelStack` and passes `parentLayerKey={previousPanelLayerKey ?? windowLayerKey}` to each `FocusLayer`. The window layer key is read from context.\n+\n+2. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`** — No layer wrapping here; `InspectorsContainer` owns the layer per panel. This file just stays focused on commands + the inner `<FocusScope>` for the entity moniker.\n+\n+3. **`kanban-app/ui/src/components/inspectors-container.tsx`** (same file) — Remove `useRestoreFocus()` import and call. FocusLayer handles focus save/restore via the layer's `last_focused`.\n+\n+4. **`kanban-app/ui/src/components/entity-inspector.tsx`** — The manual first-field focus on mount should still work — it sets focus within the inspector layer. Verify no conflict with the layer's first-focus-on-mount behavior.\n\n-2. **`kanban-app/ui/src/components/inspectors-container.tsx`** — Remove `useRestoreFocus()` import and call. FocusLayer handles focus save/restore via the layer stack. This file currently imports and calls `useRestoreFocus` — that hook is now redundant.\n+### FocusLayer prop addition\n+\n+To support explicit parent wiring for the inspector stack, `FocusLayer` accepts an optional `parentLayerKey` prop that overrides the ancestor-context default:\n+\n+```tsx\n+<FocusLayer name=\"inspector\" parentLayerKey={parentKey}>\n+  <InspectorPanel .../>\n+</FocusLayer>\n+```\n\n-3. **`kanban-app/ui/src/components/entity-inspector.tsx`** — The manual first-field focus on mount (lines ~160-180) should still work — it sets focus within the inspector layer. But verify it doesn't conflict with FocusLayer's own mount behavior.\n+- If `parentLayerKey` is provided, use it.\n+- Otherwise, read from ancestor `FocusLayerContext`.\n+- If neither, this is a root layer (window) with `parent = None`.\n\n-4. **`kanban-app/ui/src/components/app-shell.tsx`** — Evaluate whether the command palette needs `<FocusLayer name=\"palette\">`. It likely does if it should capture keyboard nav while open.\n+Update card `01KNQXW7HH...` tests to cover the explicit-parent case.\n\n ### Subtasks\n-- [ ] Add `<FocusLayer name=\"inspector\">` to InspectorFocusBridge\n-- [ ] Remove `useRestoreFocus()` from inspectors-container.tsx — replaced by FocusLayer stack\n-- [ ] Verify inspector captures nav (arrows only move within inspector fields)\n-- [ ] Verify inspector close pops the layer, restoring window layer's previously focused moniker\n-- [ ] Verify entity-inspector's first-field focus on mount still works within the layer\n+- [ ] Add optional `parentLayerKey` prop to FocusLayer for explicit parent wiring\n+- [ ] In `InspectorsContainer`, generate/track a layer key per panel entry; compute parent chain\n+- [ ] Wrap each `InspectorPanel` in `<FocusLayer name=\"inspector\" parentLayerKey={...}>`\n+- [ ] Remove `useRestoreFocus()` from `inspectors-container.tsx`\n+- [ ] Verify each inspector captures nav (arrows only move within that panel's fields)\n+- [ ] Verify panel-close pops only that layer and restores focus to parent's `last_focused`\n\n ## Acceptance Criteria\n-- [ ] Inspector navigation is captured — arrows don't escape to the board (hard boundary)\n-- [ ] Closing inspector restores focus to the element that was focused before it opened\n+- [ ] N open inspector panels in a window → N inspector layers in the forest, chained parent→child under that window's root\n+- [ ] Panel nav is captured — arrows don't escape the topmost panel (hard boundary)\n+- [ ] Closing topmost panel restores focus to the panel below (via that layer's `last_focused`), not the window root\n+- [ ] Closing the only open panel restores focus to the window root's `last_focused`\n - [ ] `useRestoreFocus` removed from inspectors-container.tsx\n-- [ ] Window layer navigation flows freely across board, toolbar, tab bar, perspective bar\n-- [ ] `nav.up` from top board card reaches column header, then toolbar, then tab bar\n-- [ ] Multiple inspector stack (open task A inspector, then task B inspector) works — each layer push/pop is independent via ULID key\n+- [ ] Two windows, each with one inspector open → 4 layers total, 2 roots, 2 inspector children, no cross-window focus interference\n+- [ ] Window layer navigation flows freely across board, toolbar, tab bar, perspective bar (within the window root layer)\n - [ ] Existing `claimWhen` predicates still work (this card doesn't remove them yet)\n - [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `kanban-app/ui/src/components/inspector-focus-bridge.test.tsx` — inspector layer captures nav, teardown restores previous focus\n-- [ ] `kanban-app/ui/src/components/inspectors-container.test.tsx` — no more useRestoreFocus; focus save/restore via layer stack\n-- [ ] Integration test: with inspector closed, nav.up from top board element reaches toolbar/tab bar\n-- [ ] Integration test: open inspector A, open inspector B on top, close B — focus returns to A's last focused field, not the board\n+- [ ] `inspectors-container.test.tsx` — opening 2 panels creates 2 layer pushes with correct parent chain (panel2.parent == panel1.layer_key, panel1.parent == window_root_key)\n+- [ ] `inspectors-container.test.tsx` — closing panel 2 pops only panel 2's layer; panel 1 still on the stack\n+- [ ] `inspectors-container.test.tsx` — no more `useRestoreFocus`; focus save/restore via layer stack\n+- [ ] Integration: open inspector A, open inspector B on top, close B → focus returns to A's last focused field, not the board\n+- [ ] Integration: with no inspector open, nav.up from top board element reaches toolbar / tab bar (all within window root layer)\n+- [ ] Integration (requires 2 windows): open inspector in window A; focus card in window B; nav in B stays in B's root layer; nav in A stays in A's inspector\n - [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -6,82 +6,50 @@\n position_column: todo\n position_ordinal: a380\n project: spatial-nav\n-title: Each inspector panel is its own FocusLayer (stacked under the window root)\n+title: Add FocusLayer to inspector (stacks on top of root window layer)\n ---\n ## What\n\n-Wrap every inspector panel in its own `<FocusLayer name=\"inspector\">`. The inspector stack in a window (see `WindowState.inspector_stack: Vec<String>`) becomes a **chain of parent→child layers** in the layer forest — one layer per panel, each with its parent being the panel below it (or the window root, for the first panel).\n-\n-### Why one layer per panel\n-\n-- Each panel is semantically independent — its own focus memory, its own nav boundary.\n-- Closing panel #2 while panel #1 is still open must restore focus to panel #1's last-focused field, not to the window root. That only works if each panel has its own `LayerEntry` with its own `last_focused`.\n-- A dialog opened from within panel #2 (see new card for dialog layers) stacks on top of panel #2, not on top of \"the inspector\". Per-panel layers keep the parent chain honest.\n-- Two windows each with their own inspector stack → separate layer chains under separate window roots. No global \"inspector layer\" concept.\n+The root `<FocusLayer name=\"window\">` is added in card 1 (at the app shell). This card adds the inspector's layer so it captures navigation when open, and replaces `useRestoreFocus` with layer stack semantics.\n\n ### Layer boundary semantics\n\n-**Layers are hard boundaries.** Navigation stays within the active (focused) layer. You cannot arrow out of the topmost inspector panel — dismiss it to return to whatever layer was below it. The nav algorithm (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`) enforces this unconditionally.\n+**Layers are hard boundaries.** Navigation stays within the active (topmost) layer. You cannot arrow out of the inspector — dismiss it to return to the board. This is the \"semi-modal\" behavior.\n\n-When a FocusLayer unmounts, the caller reads the parent layer's `last_focused` (stored on `LayerEntry`) and sets it as the new focus for that window. This replaces the ad-hoc `useRestoreFocus()` hook entirely.\n+When a FocusLayer mounts, it saves the currently focused moniker from the previous layer. When it unmounts, it restores that moniker via `setFocus`. This replaces the ad-hoc `useRestoreFocus()` hook entirely.\n\n ### Files to modify\n\n-1. **`kanban-app/ui/src/components/inspectors-container.tsx`** — Wrap each `InspectorPanel` body in `<FocusLayer name=\"inspector\">`. Each panel gets its own layer; because FocusLayer reads its parent via React context, nesting happens naturally from the DOM tree. Panel #1 is inside the window root layer's context, so its parent is the window root. Panel #2 is rendered as a sibling of panel #1 in the JSX — so its parent is *also* the window root, which is **wrong** if we want the panel-close-to-panel-below behavior.\n+1. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`** — Wrap the inspector content in `<FocusLayer name=\"inspector\">`. When the inspector mounts, this pushes onto the layer stack and becomes the active layer. When it unmounts, the layer pops and focus returns to the window layer's previously focused element.\n\n-   **Options**:\n-   - (a) Nest the JSX: render panel #2 inside panel #1's subtree. Simple, but may fight against the existing stack-of-sibling-SlidePanels layout.\n-   - (b) Parent panel #N's layer explicitly to panel #(N-1)'s layer key. Requires passing the parent layer key as a prop into the FocusLayer's `parent_key` (instead of reading context). Keeps the visual layout unchanged.\n-   - **Choose (b)**: `InspectorsContainer` tracks the layer keys as it maps over `panelStack` and passes `parentLayerKey={previousPanelLayerKey ?? windowLayerKey}` to each `FocusLayer`. The window layer key is read from context.\n-\n-2. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`** — No layer wrapping here; `InspectorsContainer` owns the layer per panel. This file just stays focused on commands + the inner `<FocusScope>` for the entity moniker.\n+2. **`kanban-app/ui/src/components/inspectors-container.tsx`** — Remove `useRestoreFocus()` import and call. FocusLayer handles focus save/restore via the layer stack. This file currently imports and calls `useRestoreFocus` — that hook is now redundant.\n\n-3. **`kanban-app/ui/src/components/inspectors-container.tsx`** (same file) — Remove `useRestoreFocus()` import and call. FocusLayer handles focus save/restore via the layer's `last_focused`.\n-\n-4. **`kanban-app/ui/src/components/entity-inspector.tsx`** — The manual first-field focus on mount should still work — it sets focus within the inspector layer. Verify no conflict with the layer's first-focus-on-mount behavior.\n-\n-### FocusLayer prop addition\n-\n-To support explicit parent wiring for the inspector stack, `FocusLayer` accepts an optional `parentLayerKey` prop that overrides the ancestor-context default:\n+3. **`kanban-app/ui/src/components/entity-inspector.tsx`** — The manual first-field focus on mount (lines ~160-180) should still work — it sets focus within the inspector layer. But verify it doesn't conflict with FocusLayer's own mount behavior.\n\n-```tsx\n-<FocusLayer name=\"inspector\" parentLayerKey={parentKey}>\n-  <InspectorPanel .../>\n-</FocusLayer>\n-```\n+4. **`kanban-app/ui/src/components/app-shell.tsx`** — Evaluate whether the command palette needs `<FocusLayer name=\"palette\">`. It likely does if it should capture keyboard nav while open.\n\n-- If `parentLayerKey` is provided, use it.\n-- Otherwise, read from ancestor `FocusLayerContext`.\n-- If neither, this is a root layer (window) with `parent = None`.\n-\n-Update card `01KNQXW7HH...` tests to cover the explicit-parent case.\n-\n ### Subtasks\n-- [ ] Add optional `parentLayerKey` prop to FocusLayer for explicit parent wiring\n-- [ ] In `InspectorsContainer`, generate/track a layer key per panel entry; compute parent chain\n-- [ ] Wrap each `InspectorPanel` in `<FocusLayer name=\"inspector\" parentLayerKey={...}>`\n-- [ ] Remove `useRestoreFocus()` from `inspectors-container.tsx`\n-- [ ] Verify each inspector captures nav (arrows only move within that panel's fields)\n-- [ ] Verify panel-close pops only that layer and restores focus to parent's `last_focused`\n+- [ ] Add `<FocusLayer name=\"inspector\">` to InspectorFocusBridge\n+- [ ] Remove `useRestoreFocus()` from inspectors-container.tsx — replaced by FocusLayer stack\n+- [ ] Verify inspector captures nav (arrows only move within inspector fields)\n+- [ ] Verify inspector close pops the layer, restoring window layer's previously focused moniker\n+- [ ] Verify entity-inspector's first-field focus on mount still works within the layer\n\n ## Acceptance Criteria\n-- [ ] N open inspector panels in a window → N inspector layers in the forest, chained parent→child under that window's root\n-- [ ] Panel nav is captured — arrows don't escape the topmost panel (hard boundary)\n-- [ ] Closing topmost panel restores focus to the panel below (via that layer's `last_focused`), not the window root\n-- [ ] Closing the only open panel restores focus to the window root's `last_focused`\n+- [ ] Inspector navigation is captured — arrows don't escape to the board (hard boundary)\n+- [ ] Closing inspector restores focus to the element that was focused before it opened\n - [ ] `useRestoreFocus` removed from inspectors-container.tsx\n-- [ ] Two windows, each with one inspector open → 4 layers total, 2 roots, 2 inspector children, no cross-window focus interference\n-- [ ] Window layer navigation flows freely across board, toolbar, tab bar, perspective bar (within the window root layer)\n+- [ ] Window layer navigation flows freely across board, toolbar, tab bar, perspective bar\n+- [ ] `nav.up` from top board card reaches column header, then toolbar, then tab bar\n+- [ ] Multiple inspector stack (open task A inspector, then task B inspector) works — each layer push/pop is independent via ULID key\n - [ ] Existing `claimWhen` predicates still work (this card doesn't remove them yet)\n - [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `inspectors-container.test.tsx` — opening 2 panels creates 2 layer pushes with correct parent chain (panel2.parent == panel1.layer_key, panel1.parent == window_root_key)\n-- [ ] `inspectors-container.test.tsx` — closing panel 2 pops only panel 2's layer; panel 1 still on the stack\n-- [ ] `inspectors-container.test.tsx` — no more `useRestoreFocus`; focus save/restore via layer stack\n-- [ ] Integration: open inspector A, open inspector B on top, close B → focus returns to A's last focused field, not the board\n-- [ ] Integration: with no inspector open, nav.up from top board element reaches toolbar / tab bar (all within window root layer)\n-- [ ] Integration (requires 2 windows): open inspector in window A; focus card in window B; nav in B stays in B's root layer; nav in A stays in A's inspector\n+- [ ] `kanban-app/ui/src/components/inspector-focus-bridge.test.tsx` — inspector layer captures nav, teardown restores previous focus\n+- [ ] `kanban-app/ui/src/components/inspectors-container.test.tsx` — no more useRestoreFocus; focus save/restore via layer stack\n+- [ ] Integration test: with inspector closed, nav.up from top board element reaches toolbar/tab bar\n+- [ ] Integration test: open inspector A, open inspector B on top, close B — focus returns to A's last focused field, not the board\n - [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","transaction_id":null}
{"id":"01KPZPS7Q1V5V5T82E14PC17KH","timestamp":"2026-04-24T12:17:40.065987Z","entity_type":"task","entity_id":"01KNQXYC4RBQP1N2NQ33P8DPB9","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,45 +1,77 @@\n ## What\n\n-The root `<FocusLayer name=\"window\">` is added in card 1 (at the app shell). This card adds the inspector's layer so it captures navigation when open, and replaces `useRestoreFocus` with layer stack semantics.\n+Wrap every inspector panel in its own `<FocusLayer name=\"inspector\">`. The inspector stack in a window (see `WindowState.inspector_stack: Vec<String>`) becomes a **chain of parent→child layers** in the layer forest — one layer per panel, each with its parent being the panel below it (or the window root, for the first panel).\n+\n+### Why one layer per panel\n+\n+- Each panel is semantically independent — its own focus memory, its own nav boundary.\n+- Closing panel #2 while panel #1 is still open must restore focus to panel #1's last-focused field, not to the window root. That only works if each panel has its own `LayerEntry` with its own `last_focused`.\n+- A dialog opened from within panel #2 (see new card for dialog layers) stacks on top of panel #2, not on top of \"the inspector\". Per-panel layers keep the parent chain honest.\n+- Two windows each with their own inspector stack → separate layer chains under separate window roots. No global \"inspector layer\" concept.\n\n ### Layer boundary semantics\n\n-**Layers are hard boundaries.** Navigation stays within the active (topmost) layer. You cannot arrow out of the inspector — dismiss it to return to the board. This is the \"semi-modal\" behavior.\n+**Layers are hard boundaries.** Navigation stays within the active (focused) layer. You cannot arrow out of the topmost inspector panel — dismiss it to return to whatever layer was below it. The nav algorithm (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`) enforces this unconditionally.\n\n-When a FocusLayer mounts, it saves the currently focused moniker from the previous layer. When it unmounts, it restores that moniker via `setFocus`. This replaces the ad-hoc `useRestoreFocus()` hook entirely.\n+When a FocusLayer unmounts, the caller reads the parent layer's `last_focused` (stored on `LayerEntry`) and sets it as the new focus for that window. This replaces the ad-hoc `useRestoreFocus()` hook entirely.\n\n ### Files to modify\n\n-1. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`** — Wrap the inspector content in `<FocusLayer name=\"inspector\">`. When the inspector mounts, this pushes onto the layer stack and becomes the active layer. When it unmounts, the layer pops and focus returns to the window layer's previously focused element.\n+1. **`kanban-app/ui/src/components/inspectors-container.tsx`** — Wrap each `InspectorPanel` body in `<FocusLayer name=\"inspector\">`. Each panel gets its own layer; because FocusLayer reads its parent via React context, nesting happens naturally from the DOM tree. Panel #1 is inside the window root layer's context, so its parent is the window root. Panel #2 is rendered as a sibling of panel #1 in the JSX — so its parent is *also* the window root, which is **wrong** if we want the panel-close-to-panel-below behavior.\n\n-2. **`kanban-app/ui/src/components/inspectors-container.tsx`** — Remove `useRestoreFocus()` import and call. FocusLayer handles focus save/restore via the layer stack. This file currently imports and calls `useRestoreFocus` — that hook is now redundant.\n+   **Options**:\n+   - (a) Nest the JSX: render panel #2 inside panel #1's subtree. Simple, but may fight against the existing stack-of-sibling-SlidePanels layout.\n+   - (b) Parent panel #N's layer explicitly to panel #(N-1)'s layer key. Requires passing the parent layer key as a prop into the FocusLayer's `parent_key` (instead of reading context). Keeps the visual layout unchanged.\n+   - **Choose (b)**: `InspectorsContainer` tracks the layer keys as it maps over `panelStack` and passes `parentLayerKey={previousPanelLayerKey ?? windowLayerKey}` to each `FocusLayer`. The window layer key is read from context.\n+\n+2. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`** — No layer wrapping here; `InspectorsContainer` owns the layer per panel. This file just stays focused on commands + the inner `<FocusScope>` for the entity moniker.\n+\n+3. **`kanban-app/ui/src/components/inspectors-container.tsx`** (same file) — Remove `useRestoreFocus()` import and call. FocusLayer handles focus save/restore via the layer's `last_focused`.\n+\n+4. **`kanban-app/ui/src/components/entity-inspector.tsx`** — The manual first-field focus on mount should still work — it sets focus within the inspector layer. Verify no conflict with the layer's first-focus-on-mount behavior.\n+\n+### FocusLayer prop addition\n\n-3. **`kanban-app/ui/src/components/entity-inspector.tsx`** — The manual first-field focus on mount (lines ~160-180) should still work — it sets focus within the inspector layer. But verify it doesn't conflict with FocusLayer's own mount behavior.\n+To support explicit parent wiring for the inspector stack, `FocusLayer` accepts an optional `parentLayerKey` prop that overrides the ancestor-context default:\n+\n+```tsx\n+<FocusLayer name=\"inspector\" parentLayerKey={parentKey}>\n+  <InspectorPanel .../>\n+</FocusLayer>\n+```\n+\n+- If `parentLayerKey` is provided, use it.\n+- Otherwise, read from ancestor `FocusLayerContext`.\n+- If neither, this is a root layer (window) with `parent = None`.\n\n-4. **`kanban-app/ui/src/components/app-shell.tsx`** — Evaluate whether the command palette needs `<FocusLayer name=\"palette\">`. It likely does if it should capture keyboard nav while open.\n+Update card `01KNQXW7HH...` tests to cover the explicit-parent case.\n\n ### Subtasks\n-- [ ] Add `<FocusLayer name=\"inspector\">` to InspectorFocusBridge\n-- [ ] Remove `useRestoreFocus()` from inspectors-container.tsx — replaced by FocusLayer stack\n-- [ ] Verify inspector captures nav (arrows only move within inspector fields)\n-- [ ] Verify inspector close pops the layer, restoring window layer's previously focused moniker\n-- [ ] Verify entity-inspector's first-field focus on mount still works within the layer\n+- [ ] Add optional `parentLayerKey` prop to FocusLayer for explicit parent wiring\n+- [ ] In `InspectorsContainer`, generate/track a layer key per panel entry; compute parent chain\n+- [ ] Wrap each `InspectorPanel` in `<FocusLayer name=\"inspector\" parentLayerKey={...}>`\n+- [ ] Remove `useRestoreFocus()` from `inspectors-container.tsx`\n+- [ ] Verify each inspector captures nav (arrows only move within that panel's fields)\n+- [ ] Verify panel-close pops only that layer and restores focus to parent's `last_focused`\n\n ## Acceptance Criteria\n-- [ ] Inspector navigation is captured — arrows don't escape to the board (hard boundary)\n-- [ ] Closing inspector restores focus to the element that was focused before it opened\n+- [ ] N open inspector panels in a window → N inspector layers in the forest, chained parent→child under that window's root\n+- [ ] Panel nav is captured — arrows don't escape the topmost panel (hard boundary)\n+- [ ] Closing topmost panel restores focus to the panel below (via that layer's `last_focused`), not the window root\n+- [ ] Closing the only open panel restores focus to the window root's `last_focused`\n - [ ] `useRestoreFocus` removed from inspectors-container.tsx\n-- [ ] Window layer navigation flows freely across board, toolbar, tab bar, perspective bar\n-- [ ] `nav.up` from top board card reaches column header, then toolbar, then tab bar\n-- [ ] Multiple inspector stack (open task A inspector, then task B inspector) works — each layer push/pop is independent via ULID key\n+- [ ] Two windows, each with one inspector open → 4 layers total, 2 roots, 2 inspector children, no cross-window focus interference\n+- [ ] Window layer navigation flows freely across board, toolbar, tab bar, perspective bar (within the window root layer)\n - [ ] Existing `claimWhen` predicates still work (this card doesn't remove them yet)\n - [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `kanban-app/ui/src/components/inspector-focus-bridge.test.tsx` — inspector layer captures nav, teardown restores previous focus\n-- [ ] `kanban-app/ui/src/components/inspectors-container.test.tsx` — no more useRestoreFocus; focus save/restore via layer stack\n-- [ ] Integration test: with inspector closed, nav.up from top board element reaches toolbar/tab bar\n-- [ ] Integration test: open inspector A, open inspector B on top, close B — focus returns to A's last focused field, not the board\n+- [ ] `inspectors-container.test.tsx` — opening 2 panels creates 2 layer pushes with correct parent chain (panel2.parent == panel1.layer_key, panel1.parent == window_root_key)\n+- [ ] `inspectors-container.test.tsx` — closing panel 2 pops only panel 2's layer; panel 1 still on the stack\n+- [ ] `inspectors-container.test.tsx` — no more `useRestoreFocus`; focus save/restore via layer stack\n+- [ ] Integration: open inspector A, open inspector B on top, close B → focus returns to A's last focused field, not the board\n+- [ ] Integration: with no inspector open, nav.up from top board element reaches toolbar / tab bar (all within window root layer)\n+- [ ] Integration (requires 2 windows): open inspector in window A; focus card in window B; nav in B stays in B's root layer; nav in A stays in A's inspector\n - [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -1,77 +1,45 @@\n ## What\n\n-Wrap every inspector panel in its own `<FocusLayer name=\"inspector\">`. The inspector stack in a window (see `WindowState.inspector_stack: Vec<String>`) becomes a **chain of parent→child layers** in the layer forest — one layer per panel, each with its parent being the panel below it (or the window root, for the first panel).\n-\n-### Why one layer per panel\n-\n-- Each panel is semantically independent — its own focus memory, its own nav boundary.\n-- Closing panel #2 while panel #1 is still open must restore focus to panel #1's last-focused field, not to the window root. That only works if each panel has its own `LayerEntry` with its own `last_focused`.\n-- A dialog opened from within panel #2 (see new card for dialog layers) stacks on top of panel #2, not on top of \"the inspector\". Per-panel layers keep the parent chain honest.\n-- Two windows each with their own inspector stack → separate layer chains under separate window roots. No global \"inspector layer\" concept.\n+The root `<FocusLayer name=\"window\">` is added in card 1 (at the app shell). This card adds the inspector's layer so it captures navigation when open, and replaces `useRestoreFocus` with layer stack semantics.\n\n ### Layer boundary semantics\n\n-**Layers are hard boundaries.** Navigation stays within the active (focused) layer. You cannot arrow out of the topmost inspector panel — dismiss it to return to whatever layer was below it. The nav algorithm (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`) enforces this unconditionally.\n+**Layers are hard boundaries.** Navigation stays within the active (topmost) layer. You cannot arrow out of the inspector — dismiss it to return to the board. This is the \"semi-modal\" behavior.\n\n-When a FocusLayer unmounts, the caller reads the parent layer's `last_focused` (stored on `LayerEntry`) and sets it as the new focus for that window. This replaces the ad-hoc `useRestoreFocus()` hook entirely.\n+When a FocusLayer mounts, it saves the currently focused moniker from the previous layer. When it unmounts, it restores that moniker via `setFocus`. This replaces the ad-hoc `useRestoreFocus()` hook entirely.\n\n ### Files to modify\n\n-1. **`kanban-app/ui/src/components/inspectors-container.tsx`** — Wrap each `InspectorPanel` body in `<FocusLayer name=\"inspector\">`. Each panel gets its own layer; because FocusLayer reads its parent via React context, nesting happens naturally from the DOM tree. Panel #1 is inside the window root layer's context, so its parent is the window root. Panel #2 is rendered as a sibling of panel #1 in the JSX — so its parent is *also* the window root, which is **wrong** if we want the panel-close-to-panel-below behavior.\n-\n-   **Options**:\n-   - (a) Nest the JSX: render panel #2 inside panel #1's subtree. Simple, but may fight against the existing stack-of-sibling-SlidePanels layout.\n-   - (b) Parent panel #N's layer explicitly to panel #(N-1)'s layer key. Requires passing the parent layer key as a prop into the FocusLayer's `parent_key` (instead of reading context). Keeps the visual layout unchanged.\n-   - **Choose (b)**: `InspectorsContainer` tracks the layer keys as it maps over `panelStack` and passes `parentLayerKey={previousPanelLayerKey ?? windowLayerKey}` to each `FocusLayer`. The window layer key is read from context.\n-\n-2. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`** — No layer wrapping here; `InspectorsContainer` owns the layer per panel. This file just stays focused on commands + the inner `<FocusScope>` for the entity moniker.\n+1. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`** — Wrap the inspector content in `<FocusLayer name=\"inspector\">`. When the inspector mounts, this pushes onto the layer stack and becomes the active layer. When it unmounts, the layer pops and focus returns to the window layer's previously focused element.\n\n-3. **`kanban-app/ui/src/components/inspectors-container.tsx`** (same file) — Remove `useRestoreFocus()` import and call. FocusLayer handles focus save/restore via the layer's `last_focused`.\n+2. **`kanban-app/ui/src/components/inspectors-container.tsx`** — Remove `useRestoreFocus()` import and call. FocusLayer handles focus save/restore via the layer stack. This file currently imports and calls `useRestoreFocus` — that hook is now redundant.\n\n-4. **`kanban-app/ui/src/components/entity-inspector.tsx`** — The manual first-field focus on mount should still work — it sets focus within the inspector layer. Verify no conflict with the layer's first-focus-on-mount behavior.\n+3. **`kanban-app/ui/src/components/entity-inspector.tsx`** — The manual first-field focus on mount (lines ~160-180) should still work — it sets focus within the inspector layer. But verify it doesn't conflict with FocusLayer's own mount behavior.\n\n-### FocusLayer prop addition\n-\n-To support explicit parent wiring for the inspector stack, `FocusLayer` accepts an optional `parentLayerKey` prop that overrides the ancestor-context default:\n-\n-```tsx\n-<FocusLayer name=\"inspector\" parentLayerKey={parentKey}>\n-  <InspectorPanel .../>\n-</FocusLayer>\n-```\n-\n-- If `parentLayerKey` is provided, use it.\n-- Otherwise, read from ancestor `FocusLayerContext`.\n-- If neither, this is a root layer (window) with `parent = None`.\n-\n-Update card `01KNQXW7HH...` tests to cover the explicit-parent case.\n+4. **`kanban-app/ui/src/components/app-shell.tsx`** — Evaluate whether the command palette needs `<FocusLayer name=\"palette\">`. It likely does if it should capture keyboard nav while open.\n\n ### Subtasks\n-- [ ] Add optional `parentLayerKey` prop to FocusLayer for explicit parent wiring\n-- [ ] In `InspectorsContainer`, generate/track a layer key per panel entry; compute parent chain\n-- [ ] Wrap each `InspectorPanel` in `<FocusLayer name=\"inspector\" parentLayerKey={...}>`\n-- [ ] Remove `useRestoreFocus()` from `inspectors-container.tsx`\n-- [ ] Verify each inspector captures nav (arrows only move within that panel's fields)\n-- [ ] Verify panel-close pops only that layer and restores focus to parent's `last_focused`\n+- [ ] Add `<FocusLayer name=\"inspector\">` to InspectorFocusBridge\n+- [ ] Remove `useRestoreFocus()` from inspectors-container.tsx — replaced by FocusLayer stack\n+- [ ] Verify inspector captures nav (arrows only move within inspector fields)\n+- [ ] Verify inspector close pops the layer, restoring window layer's previously focused moniker\n+- [ ] Verify entity-inspector's first-field focus on mount still works within the layer\n\n ## Acceptance Criteria\n-- [ ] N open inspector panels in a window → N inspector layers in the forest, chained parent→child under that window's root\n-- [ ] Panel nav is captured — arrows don't escape the topmost panel (hard boundary)\n-- [ ] Closing topmost panel restores focus to the panel below (via that layer's `last_focused`), not the window root\n-- [ ] Closing the only open panel restores focus to the window root's `last_focused`\n+- [ ] Inspector navigation is captured — arrows don't escape to the board (hard boundary)\n+- [ ] Closing inspector restores focus to the element that was focused before it opened\n - [ ] `useRestoreFocus` removed from inspectors-container.tsx\n-- [ ] Two windows, each with one inspector open → 4 layers total, 2 roots, 2 inspector children, no cross-window focus interference\n-- [ ] Window layer navigation flows freely across board, toolbar, tab bar, perspective bar (within the window root layer)\n+- [ ] Window layer navigation flows freely across board, toolbar, tab bar, perspective bar\n+- [ ] `nav.up` from top board card reaches column header, then toolbar, then tab bar\n+- [ ] Multiple inspector stack (open task A inspector, then task B inspector) works — each layer push/pop is independent via ULID key\n - [ ] Existing `claimWhen` predicates still work (this card doesn't remove them yet)\n - [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `inspectors-container.test.tsx` — opening 2 panels creates 2 layer pushes with correct parent chain (panel2.parent == panel1.layer_key, panel1.parent == window_root_key)\n-- [ ] `inspectors-container.test.tsx` — closing panel 2 pops only panel 2's layer; panel 1 still on the stack\n-- [ ] `inspectors-container.test.tsx` — no more `useRestoreFocus`; focus save/restore via layer stack\n-- [ ] Integration: open inspector A, open inspector B on top, close B → focus returns to A's last focused field, not the board\n-- [ ] Integration: with no inspector open, nav.up from top board element reaches toolbar / tab bar (all within window root layer)\n-- [ ] Integration (requires 2 windows): open inspector in window A; focus card in window B; nav in B stays in B's root layer; nav in A stays in A's inspector\n+- [ ] `kanban-app/ui/src/components/inspector-focus-bridge.test.tsx` — inspector layer captures nav, teardown restores previous focus\n+- [ ] `kanban-app/ui/src/components/inspectors-container.test.tsx` — no more useRestoreFocus; focus save/restore via layer stack\n+- [ ] Integration test: with inspector closed, nav.up from top board element reaches toolbar/tab bar\n+- [ ] Integration test: open inspector A, open inspector B on top, close B — focus returns to A's last focused field, not the board\n - [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Add FocusLayer to inspector (stacks on top of root window layer)\n\\ No newline at end of file\n+Each inspector panel is its own FocusLayer (stacked under the window root)\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Each inspector panel is its own FocusLayer (stacked under the window root)\n\\ No newline at end of file\n+Add FocusLayer to inspector (stacks on top of root window layer)\n\\ No newline at end of file\n"}]]}
{"id":"01KPZRXR9K8HYHYSC74BYXCVH8","timestamp":"2026-04-24T12:55:05.267037Z","op":"update","item_id":"01KNQXYC4RBQP1N2NQ33P8DPB9","forward_patch":"--- original\n+++ modified\n@@ -6,82 +6,90 @@\n position_column: todo\n position_ordinal: a380\n project: spatial-nav\n-title: Each inspector panel is its own FocusLayer (stacked under the window root)\n+title: 'Inspector layer: one layer per window, panels and field rows as zones inside'\n ---\n ## What\n\n-Wrap every inspector panel in its own `<FocusLayer name=\"inspector\">`. The inspector stack in a window (see `WindowState.inspector_stack: Vec<String>`) becomes a **chain of parent→child layers** in the layer forest — one layer per panel, each with its parent being the panel below it (or the window root, for the first panel).\n+When any inspector panel is open in a window, mount a **single `<FocusLayer name=\"inspector\">`** that contains all open panels in that window. Each panel is a **Zone** inside that shared layer. Field rows within a panel are also Zones; their labels and pills are Leaves.\n\n-### Why one layer per panel\n+This is different from \"one layer per panel\" — we use **one layer, many zones** because:\n\n-- Each panel is semantically independent — its own focus memory, its own nav boundary.\n-- Closing panel #2 while panel #1 is still open must restore focus to panel #1's last-focused field, not to the window root. That only works if each panel has its own `LayerEntry` with its own `last_focused`.\n-- A dialog opened from within panel #2 (see new card for dialog layers) stacks on top of panel #2, not on top of \"the inspector\". Per-panel layers keep the parent chain honest.\n-- Two windows each with their own inspector stack → separate layer chains under separate window roots. No global \"inspector layer\" concept.\n+- Inspector panels are all equivalent modality: they all capture nav, they all defer to the same dismiss chain. A single layer is the cleanest way to express that shared modal boundary.\n+- Nav between adjacent open panels is then natural cross-zone leaf fallback (beam rule 2) — no special case needed.\n+- Per-panel focus memory is still per-zone (each panel is a Zone and gets its own `last_focused`), so closing the top panel still restores focus to the panel below.\n+- In multi-window, each window gets its own inspector layer — a window's layer is a child of that window's root. No cross-window leakage.\n\n-### Layer boundary semantics\n+### Shape\n\n-**Layers are hard boundaries.** Navigation stays within the active (focused) layer. You cannot arrow out of the topmost inspector panel — dismiss it to return to whatever layer was below it. The nav algorithm (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`) enforces this unconditionally.\n+```\n+window_A root layer (FocusLayer name=\"window\")\n+  window chrome (NavBar zone, Toolbar zone, etc.) — see shell zones card\n+  board content (columns, cards, ...)\n\n-When a FocusLayer unmounts, the caller reads the parent layer's `last_focused` (stored on `LayerEntry`) and sets it as the new focus for that window. This replaces the ad-hoc `useRestoreFocus()` hook entirely.\n+  inspector layer (FocusLayer name=\"inspector\") — mounted iff inspector_stack non-empty\n+    panel_1 (FocusScope kind=\"zone\")\n+      field_row (FocusScope kind=\"zone\")\n+        label (FocusScope kind=\"leaf\")\n+        pill_a (FocusScope kind=\"leaf\")\n+        ...\n+    panel_2 (FocusScope kind=\"zone\")\n+      ...\n\n-### Files to modify\n+window_B root layer\n+  ...\n+  inspector layer (if open)\n+    panel_1 ...\n+```\n\n-1. **`kanban-app/ui/src/components/inspectors-container.tsx`** — Wrap each `InspectorPanel` body in `<FocusLayer name=\"inspector\">`. Each panel gets its own layer; because FocusLayer reads its parent via React context, nesting happens naturally from the DOM tree. Panel #1 is inside the window root layer's context, so its parent is the window root. Panel #2 is rendered as a sibling of panel #1 in the JSX — so its parent is *also* the window root, which is **wrong** if we want the panel-close-to-panel-below behavior.\n+### Layer boundary semantics\n\n-   **Options**:\n-   - (a) Nest the JSX: render panel #2 inside panel #1's subtree. Simple, but may fight against the existing stack-of-sibling-SlidePanels layout.\n-   - (b) Parent panel #N's layer explicitly to panel #(N-1)'s layer key. Requires passing the parent layer key as a prop into the FocusLayer's `parent_key` (instead of reading context). Keeps the visual layout unchanged.\n-   - **Choose (b)**: `InspectorsContainer` tracks the layer keys as it maps over `panelStack` and passes `parentLayerKey={previousPanelLayerKey ?? windowLayerKey}` to each `FocusLayer`. The window layer key is read from context.\n+- Arrows inside the inspector layer can move anywhere in that layer (within-zone first, then cross-zone leaf fallback). You cannot arrow out to the board — that's a different layer. Dismiss (Escape) pops whatever's appropriate.\n+- Closing the last panel unmounts the inspector layer; focus-changed event restores focus to the window root layer's `last_focused` (stored when the inspector layer was pushed).\n+- Closing one of N panels (with N > 1) unregisters that panel's zone and its descendant entries. The dynamic-lifecycle fallback (card `01KNS0B3HY...`) picks up — walks `parent_zone` chain from the lost focus up to the next valid `last_focused`, which will be the inspector layer's root or another panel.\n\n-2. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`** — No layer wrapping here; `InspectorsContainer` owns the layer per panel. This file just stays focused on commands + the inner `<FocusScope>` for the entity moniker.\n+### Files to modify\n\n-3. **`kanban-app/ui/src/components/inspectors-container.tsx`** (same file) — Remove `useRestoreFocus()` import and call. FocusLayer handles focus save/restore via the layer's `last_focused`.\n+1. **`kanban-app/ui/src/components/inspectors-container.tsx`**\n+   - When `panelStack.length > 0`, wrap the whole rendered panel list in one `<FocusLayer name=\"inspector\">`. Passing `parentLayerKey={windowLayerKey}` from `useCurrentLayerKey()`.\n+   - Each `InspectorPanel` wraps its content in `<FocusScope kind=\"zone\" moniker={`panel:${entityType}:${entityId}`}>`.\n+   - Remove `useRestoreFocus()` — layer's `last_focused` replaces it.\n\n-4. **`kanban-app/ui/src/components/entity-inspector.tsx`** — The manual first-field focus on mount should still work — it sets focus within the inspector layer. Verify no conflict with the layer's first-focus-on-mount behavior.\n-\n-### FocusLayer prop addition\n+2. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`**\n+   - No layer wrapping; container owns the layer.\n+   - The inner `<FocusScope>` wrapping `<EntityInspector>` can stay as the panel's zone scope (or lift that to `InspectorsContainer` — one FocusScope per panel).\n\n-To support explicit parent wiring for the inspector stack, `FocusLayer` accepts an optional `parentLayerKey` prop that overrides the ancestor-context default:\n+3. **`kanban-app/ui/src/components/entity-inspector.tsx`**\n+   - Field row wrappers become `<FocusScope kind=\"zone\">` (detailed in migration card `01KNQY0P9J...`).\n\n-```tsx\n-<FocusLayer name=\"inspector\" parentLayerKey={parentKey}>\n-  <InspectorPanel .../>\n-</FocusLayer>\n-```\n-\n-- If `parentLayerKey` is provided, use it.\n-- Otherwise, read from ancestor `FocusLayerContext`.\n-- If neither, this is a root layer (window) with `parent = None`.\n+### FocusLayer prop requirement\n\n-Update card `01KNQXW7HH...` tests to cover the explicit-parent case.\n+Assumes `FocusLayer` already supports the optional `parentLayerKey` prop from card `01KNQXW7HH...` — inspector layer passes `parentLayerKey={useCurrentLayerKey()}` at mount so the parent link is explicit (portaled overlays break React ancestor chain).\n\n ### Subtasks\n-- [ ] Add optional `parentLayerKey` prop to FocusLayer for explicit parent wiring\n-- [ ] In `InspectorsContainer`, generate/track a layer key per panel entry; compute parent chain\n-- [ ] Wrap each `InspectorPanel` in `<FocusLayer name=\"inspector\" parentLayerKey={...}>`\n+- [ ] When inspector_stack non-empty, mount `<FocusLayer name=\"inspector\" parentLayerKey={windowLayerKey}>` wrapping the panel list\n+- [ ] Wrap each `InspectorPanel` content in `<FocusScope kind=\"zone\">` using a panel moniker\n - [ ] Remove `useRestoreFocus()` from `inspectors-container.tsx`\n-- [ ] Verify each inspector captures nav (arrows only move within that panel's fields)\n-- [ ] Verify panel-close pops only that layer and restores focus to parent's `last_focused`\n+- [ ] Verify: closing one of multiple panels leaves the inspector layer mounted\n+- [ ] Verify: closing the last panel unmounts the inspector layer and focus returns to window root's `last_focused`\n\n ## Acceptance Criteria\n-- [ ] N open inspector panels in a window → N inspector layers in the forest, chained parent→child under that window's root\n-- [ ] Panel nav is captured — arrows don't escape the topmost panel (hard boundary)\n-- [ ] Closing topmost panel restores focus to the panel below (via that layer's `last_focused`), not the window root\n-- [ ] Closing the only open panel restores focus to the window root's `last_focused`\n+- [ ] Open inspector panels in a window → exactly one inspector layer per window (not one per panel)\n+- [ ] Each open panel is its own Zone within that inspector layer\n+- [ ] Cross-panel nav works as normal cross-zone leaf fallback (beam rule 2)\n+- [ ] Nav is captured inside the inspector layer — arrows can't reach board/nav bar/etc.\n+- [ ] Closing the last panel pops the inspector layer; window's focus restored to layer's `last_focused`\n+- [ ] Two windows each with inspectors → 4 layers total: 2 window roots + 2 inspector layers; zero cross-window interference\n - [ ] `useRestoreFocus` removed from inspectors-container.tsx\n-- [ ] Two windows, each with one inspector open → 4 layers total, 2 roots, 2 inspector children, no cross-window focus interference\n-- [ ] Window layer navigation flows freely across board, toolbar, tab bar, perspective bar (within the window root layer)\n-- [ ] Existing `claimWhen` predicates still work (this card doesn't remove them yet)\n - [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `inspectors-container.test.tsx` — opening 2 panels creates 2 layer pushes with correct parent chain (panel2.parent == panel1.layer_key, panel1.parent == window_root_key)\n-- [ ] `inspectors-container.test.tsx` — closing panel 2 pops only panel 2's layer; panel 1 still on the stack\n-- [ ] `inspectors-container.test.tsx` — no more `useRestoreFocus`; focus save/restore via layer stack\n-- [ ] Integration: open inspector A, open inspector B on top, close B → focus returns to A's last focused field, not the board\n-- [ ] Integration: with no inspector open, nav.up from top board element reaches toolbar / tab bar (all within window root layer)\n-- [ ] Integration (requires 2 windows): open inspector in window A; focus card in window B; nav in B stays in B's root layer; nav in A stays in A's inspector\n+- [ ] `inspectors-container.test.tsx` — opening first panel pushes exactly one layer; opening second panel pushes a zone (not another layer)\n+- [ ] `inspectors-container.test.tsx` — closing one of two panels unregisters that panel's zone; inspector layer still present\n+- [ ] `inspectors-container.test.tsx` — closing the only panel unmounts the inspector layer (pop_layer called once)\n+- [ ] `inspectors-container.test.tsx` — no more `useRestoreFocus`\n+- [ ] Integration: open A, open B; focused on some field in B; close B → focus lands on something in A (via fallback + zone last_focused)\n+- [ ] Integration: with inspector layer open, arrow keys never focus a board card (different layer)\n+- [ ] Rust multi-window: `children_of_layer(window_A_root) == [window_A_inspector_layer]`; `children_of_layer(window_B_root) == [window_B_inspector_layer]`; the two inspector layers don't see each other\n - [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -6,90 +6,82 @@\n position_column: todo\n position_ordinal: a380\n project: spatial-nav\n-title: 'Inspector layer: one layer per window, panels and field rows as zones inside'\n+title: Each inspector panel is its own FocusLayer (stacked under the window root)\n ---\n ## What\n\n-When any inspector panel is open in a window, mount a **single `<FocusLayer name=\"inspector\">`** that contains all open panels in that window. Each panel is a **Zone** inside that shared layer. Field rows within a panel are also Zones; their labels and pills are Leaves.\n+Wrap every inspector panel in its own `<FocusLayer name=\"inspector\">`. The inspector stack in a window (see `WindowState.inspector_stack: Vec<String>`) becomes a **chain of parent→child layers** in the layer forest — one layer per panel, each with its parent being the panel below it (or the window root, for the first panel).\n\n-This is different from \"one layer per panel\" — we use **one layer, many zones** because:\n+### Why one layer per panel\n\n-- Inspector panels are all equivalent modality: they all capture nav, they all defer to the same dismiss chain. A single layer is the cleanest way to express that shared modal boundary.\n-- Nav between adjacent open panels is then natural cross-zone leaf fallback (beam rule 2) — no special case needed.\n-- Per-panel focus memory is still per-zone (each panel is a Zone and gets its own `last_focused`), so closing the top panel still restores focus to the panel below.\n-- In multi-window, each window gets its own inspector layer — a window's layer is a child of that window's root. No cross-window leakage.\n+- Each panel is semantically independent — its own focus memory, its own nav boundary.\n+- Closing panel #2 while panel #1 is still open must restore focus to panel #1's last-focused field, not to the window root. That only works if each panel has its own `LayerEntry` with its own `last_focused`.\n+- A dialog opened from within panel #2 (see new card for dialog layers) stacks on top of panel #2, not on top of \"the inspector\". Per-panel layers keep the parent chain honest.\n+- Two windows each with their own inspector stack → separate layer chains under separate window roots. No global \"inspector layer\" concept.\n\n-### Shape\n+### Layer boundary semantics\n\n-```\n-window_A root layer (FocusLayer name=\"window\")\n-  window chrome (NavBar zone, Toolbar zone, etc.) — see shell zones card\n-  board content (columns, cards, ...)\n+**Layers are hard boundaries.** Navigation stays within the active (focused) layer. You cannot arrow out of the topmost inspector panel — dismiss it to return to whatever layer was below it. The nav algorithm (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`) enforces this unconditionally.\n\n-  inspector layer (FocusLayer name=\"inspector\") — mounted iff inspector_stack non-empty\n-    panel_1 (FocusScope kind=\"zone\")\n-      field_row (FocusScope kind=\"zone\")\n-        label (FocusScope kind=\"leaf\")\n-        pill_a (FocusScope kind=\"leaf\")\n-        ...\n-    panel_2 (FocusScope kind=\"zone\")\n-      ...\n+When a FocusLayer unmounts, the caller reads the parent layer's `last_focused` (stored on `LayerEntry`) and sets it as the new focus for that window. This replaces the ad-hoc `useRestoreFocus()` hook entirely.\n+\n+### Files to modify\n+\n+1. **`kanban-app/ui/src/components/inspectors-container.tsx`** — Wrap each `InspectorPanel` body in `<FocusLayer name=\"inspector\">`. Each panel gets its own layer; because FocusLayer reads its parent via React context, nesting happens naturally from the DOM tree. Panel #1 is inside the window root layer's context, so its parent is the window root. Panel #2 is rendered as a sibling of panel #1 in the JSX — so its parent is *also* the window root, which is **wrong** if we want the panel-close-to-panel-below behavior.\n\n-window_B root layer\n-  ...\n-  inspector layer (if open)\n-    panel_1 ...\n-```\n+   **Options**:\n+   - (a) Nest the JSX: render panel #2 inside panel #1's subtree. Simple, but may fight against the existing stack-of-sibling-SlidePanels layout.\n+   - (b) Parent panel #N's layer explicitly to panel #(N-1)'s layer key. Requires passing the parent layer key as a prop into the FocusLayer's `parent_key` (instead of reading context). Keeps the visual layout unchanged.\n+   - **Choose (b)**: `InspectorsContainer` tracks the layer keys as it maps over `panelStack` and passes `parentLayerKey={previousPanelLayerKey ?? windowLayerKey}` to each `FocusLayer`. The window layer key is read from context.\n\n-### Layer boundary semantics\n+2. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`** — No layer wrapping here; `InspectorsContainer` owns the layer per panel. This file just stays focused on commands + the inner `<FocusScope>` for the entity moniker.\n\n-- Arrows inside the inspector layer can move anywhere in that layer (within-zone first, then cross-zone leaf fallback). You cannot arrow out to the board — that's a different layer. Dismiss (Escape) pops whatever's appropriate.\n-- Closing the last panel unmounts the inspector layer; focus-changed event restores focus to the window root layer's `last_focused` (stored when the inspector layer was pushed).\n-- Closing one of N panels (with N > 1) unregisters that panel's zone and its descendant entries. The dynamic-lifecycle fallback (card `01KNS0B3HY...`) picks up — walks `parent_zone` chain from the lost focus up to the next valid `last_focused`, which will be the inspector layer's root or another panel.\n+3. **`kanban-app/ui/src/components/inspectors-container.tsx`** (same file) — Remove `useRestoreFocus()` import and call. FocusLayer handles focus save/restore via the layer's `last_focused`.\n\n-### Files to modify\n+4. **`kanban-app/ui/src/components/entity-inspector.tsx`** — The manual first-field focus on mount should still work — it sets focus within the inspector layer. Verify no conflict with the layer's first-focus-on-mount behavior.\n\n-1. **`kanban-app/ui/src/components/inspectors-container.tsx`**\n-   - When `panelStack.length > 0`, wrap the whole rendered panel list in one `<FocusLayer name=\"inspector\">`. Passing `parentLayerKey={windowLayerKey}` from `useCurrentLayerKey()`.\n-   - Each `InspectorPanel` wraps its content in `<FocusScope kind=\"zone\" moniker={`panel:${entityType}:${entityId}`}>`.\n-   - Remove `useRestoreFocus()` — layer's `last_focused` replaces it.\n+### FocusLayer prop addition\n\n-2. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`**\n-   - No layer wrapping; container owns the layer.\n-   - The inner `<FocusScope>` wrapping `<EntityInspector>` can stay as the panel's zone scope (or lift that to `InspectorsContainer` — one FocusScope per panel).\n+To support explicit parent wiring for the inspector stack, `FocusLayer` accepts an optional `parentLayerKey` prop that overrides the ancestor-context default:\n\n-3. **`kanban-app/ui/src/components/entity-inspector.tsx`**\n-   - Field row wrappers become `<FocusScope kind=\"zone\">` (detailed in migration card `01KNQY0P9J...`).\n+```tsx\n+<FocusLayer name=\"inspector\" parentLayerKey={parentKey}>\n+  <InspectorPanel .../>\n+</FocusLayer>\n+```\n\n-### FocusLayer prop requirement\n+- If `parentLayerKey` is provided, use it.\n+- Otherwise, read from ancestor `FocusLayerContext`.\n+- If neither, this is a root layer (window) with `parent = None`.\n\n-Assumes `FocusLayer` already supports the optional `parentLayerKey` prop from card `01KNQXW7HH...` — inspector layer passes `parentLayerKey={useCurrentLayerKey()}` at mount so the parent link is explicit (portaled overlays break React ancestor chain).\n+Update card `01KNQXW7HH...` tests to cover the explicit-parent case.\n\n ### Subtasks\n-- [ ] When inspector_stack non-empty, mount `<FocusLayer name=\"inspector\" parentLayerKey={windowLayerKey}>` wrapping the panel list\n-- [ ] Wrap each `InspectorPanel` content in `<FocusScope kind=\"zone\">` using a panel moniker\n+- [ ] Add optional `parentLayerKey` prop to FocusLayer for explicit parent wiring\n+- [ ] In `InspectorsContainer`, generate/track a layer key per panel entry; compute parent chain\n+- [ ] Wrap each `InspectorPanel` in `<FocusLayer name=\"inspector\" parentLayerKey={...}>`\n - [ ] Remove `useRestoreFocus()` from `inspectors-container.tsx`\n-- [ ] Verify: closing one of multiple panels leaves the inspector layer mounted\n-- [ ] Verify: closing the last panel unmounts the inspector layer and focus returns to window root's `last_focused`\n+- [ ] Verify each inspector captures nav (arrows only move within that panel's fields)\n+- [ ] Verify panel-close pops only that layer and restores focus to parent's `last_focused`\n\n ## Acceptance Criteria\n-- [ ] Open inspector panels in a window → exactly one inspector layer per window (not one per panel)\n-- [ ] Each open panel is its own Zone within that inspector layer\n-- [ ] Cross-panel nav works as normal cross-zone leaf fallback (beam rule 2)\n-- [ ] Nav is captured inside the inspector layer — arrows can't reach board/nav bar/etc.\n-- [ ] Closing the last panel pops the inspector layer; window's focus restored to layer's `last_focused`\n-- [ ] Two windows each with inspectors → 4 layers total: 2 window roots + 2 inspector layers; zero cross-window interference\n+- [ ] N open inspector panels in a window → N inspector layers in the forest, chained parent→child under that window's root\n+- [ ] Panel nav is captured — arrows don't escape the topmost panel (hard boundary)\n+- [ ] Closing topmost panel restores focus to the panel below (via that layer's `last_focused`), not the window root\n+- [ ] Closing the only open panel restores focus to the window root's `last_focused`\n - [ ] `useRestoreFocus` removed from inspectors-container.tsx\n+- [ ] Two windows, each with one inspector open → 4 layers total, 2 roots, 2 inspector children, no cross-window focus interference\n+- [ ] Window layer navigation flows freely across board, toolbar, tab bar, perspective bar (within the window root layer)\n+- [ ] Existing `claimWhen` predicates still work (this card doesn't remove them yet)\n - [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `inspectors-container.test.tsx` — opening first panel pushes exactly one layer; opening second panel pushes a zone (not another layer)\n-- [ ] `inspectors-container.test.tsx` — closing one of two panels unregisters that panel's zone; inspector layer still present\n-- [ ] `inspectors-container.test.tsx` — closing the only panel unmounts the inspector layer (pop_layer called once)\n-- [ ] `inspectors-container.test.tsx` — no more `useRestoreFocus`\n-- [ ] Integration: open A, open B; focused on some field in B; close B → focus lands on something in A (via fallback + zone last_focused)\n-- [ ] Integration: with inspector layer open, arrow keys never focus a board card (different layer)\n-- [ ] Rust multi-window: `children_of_layer(window_A_root) == [window_A_inspector_layer]`; `children_of_layer(window_B_root) == [window_B_inspector_layer]`; the two inspector layers don't see each other\n+- [ ] `inspectors-container.test.tsx` — opening 2 panels creates 2 layer pushes with correct parent chain (panel2.parent == panel1.layer_key, panel1.parent == window_root_key)\n+- [ ] `inspectors-container.test.tsx` — closing panel 2 pops only panel 2's layer; panel 1 still on the stack\n+- [ ] `inspectors-container.test.tsx` — no more `useRestoreFocus`; focus save/restore via layer stack\n+- [ ] Integration: open inspector A, open inspector B on top, close B → focus returns to A's last focused field, not the board\n+- [ ] Integration: with no inspector open, nav.up from top board element reaches toolbar / tab bar (all within window root layer)\n+- [ ] Integration (requires 2 windows): open inspector in window A; focus card in window B; nav in B stays in B's root layer; nav in A stays in A's inspector\n - [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","transaction_id":null}
{"id":"01KPZRXR9KZZDAEY5AEHSETFY4","timestamp":"2026-04-24T12:55:05.267666Z","entity_type":"task","entity_id":"01KNQXYC4RBQP1N2NQ33P8DPB9","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,77 +1,85 @@\n ## What\n\n-Wrap every inspector panel in its own `<FocusLayer name=\"inspector\">`. The inspector stack in a window (see `WindowState.inspector_stack: Vec<String>`) becomes a **chain of parent→child layers** in the layer forest — one layer per panel, each with its parent being the panel below it (or the window root, for the first panel).\n+When any inspector panel is open in a window, mount a **single `<FocusLayer name=\"inspector\">`** that contains all open panels in that window. Each panel is a **Zone** inside that shared layer. Field rows within a panel are also Zones; their labels and pills are Leaves.\n\n-### Why one layer per panel\n+This is different from \"one layer per panel\" — we use **one layer, many zones** because:\n\n-- Each panel is semantically independent — its own focus memory, its own nav boundary.\n-- Closing panel #2 while panel #1 is still open must restore focus to panel #1's last-focused field, not to the window root. That only works if each panel has its own `LayerEntry` with its own `last_focused`.\n-- A dialog opened from within panel #2 (see new card for dialog layers) stacks on top of panel #2, not on top of \"the inspector\". Per-panel layers keep the parent chain honest.\n-- Two windows each with their own inspector stack → separate layer chains under separate window roots. No global \"inspector layer\" concept.\n+- Inspector panels are all equivalent modality: they all capture nav, they all defer to the same dismiss chain. A single layer is the cleanest way to express that shared modal boundary.\n+- Nav between adjacent open panels is then natural cross-zone leaf fallback (beam rule 2) — no special case needed.\n+- Per-panel focus memory is still per-zone (each panel is a Zone and gets its own `last_focused`), so closing the top panel still restores focus to the panel below.\n+- In multi-window, each window gets its own inspector layer — a window's layer is a child of that window's root. No cross-window leakage.\n\n-### Layer boundary semantics\n+### Shape\n\n-**Layers are hard boundaries.** Navigation stays within the active (focused) layer. You cannot arrow out of the topmost inspector panel — dismiss it to return to whatever layer was below it. The nav algorithm (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`) enforces this unconditionally.\n+```\n+window_A root layer (FocusLayer name=\"window\")\n+  window chrome (NavBar zone, Toolbar zone, etc.) — see shell zones card\n+  board content (columns, cards, ...)\n\n-When a FocusLayer unmounts, the caller reads the parent layer's `last_focused` (stored on `LayerEntry`) and sets it as the new focus for that window. This replaces the ad-hoc `useRestoreFocus()` hook entirely.\n+  inspector layer (FocusLayer name=\"inspector\") — mounted iff inspector_stack non-empty\n+    panel_1 (FocusScope kind=\"zone\")\n+      field_row (FocusScope kind=\"zone\")\n+        label (FocusScope kind=\"leaf\")\n+        pill_a (FocusScope kind=\"leaf\")\n+        ...\n+    panel_2 (FocusScope kind=\"zone\")\n+      ...\n\n-### Files to modify\n+window_B root layer\n+  ...\n+  inspector layer (if open)\n+    panel_1 ...\n+```\n\n-1. **`kanban-app/ui/src/components/inspectors-container.tsx`** — Wrap each `InspectorPanel` body in `<FocusLayer name=\"inspector\">`. Each panel gets its own layer; because FocusLayer reads its parent via React context, nesting happens naturally from the DOM tree. Panel #1 is inside the window root layer's context, so its parent is the window root. Panel #2 is rendered as a sibling of panel #1 in the JSX — so its parent is *also* the window root, which is **wrong** if we want the panel-close-to-panel-below behavior.\n+### Layer boundary semantics\n\n-   **Options**:\n-   - (a) Nest the JSX: render panel #2 inside panel #1's subtree. Simple, but may fight against the existing stack-of-sibling-SlidePanels layout.\n-   - (b) Parent panel #N's layer explicitly to panel #(N-1)'s layer key. Requires passing the parent layer key as a prop into the FocusLayer's `parent_key` (instead of reading context). Keeps the visual layout unchanged.\n-   - **Choose (b)**: `InspectorsContainer` tracks the layer keys as it maps over `panelStack` and passes `parentLayerKey={previousPanelLayerKey ?? windowLayerKey}` to each `FocusLayer`. The window layer key is read from context.\n+- Arrows inside the inspector layer can move anywhere in that layer (within-zone first, then cross-zone leaf fallback). You cannot arrow out to the board — that's a different layer. Dismiss (Escape) pops whatever's appropriate.\n+- Closing the last panel unmounts the inspector layer; focus-changed event restores focus to the window root layer's `last_focused` (stored when the inspector layer was pushed).\n+- Closing one of N panels (with N > 1) unregisters that panel's zone and its descendant entries. The dynamic-lifecycle fallback (card `01KNS0B3HY...`) picks up — walks `parent_zone` chain from the lost focus up to the next valid `last_focused`, which will be the inspector layer's root or another panel.\n\n-2. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`** — No layer wrapping here; `InspectorsContainer` owns the layer per panel. This file just stays focused on commands + the inner `<FocusScope>` for the entity moniker.\n+### Files to modify\n\n-3. **`kanban-app/ui/src/components/inspectors-container.tsx`** (same file) — Remove `useRestoreFocus()` import and call. FocusLayer handles focus save/restore via the layer's `last_focused`.\n-\n-4. **`kanban-app/ui/src/components/entity-inspector.tsx`** — The manual first-field focus on mount should still work — it sets focus within the inspector layer. Verify no conflict with the layer's first-focus-on-mount behavior.\n+1. **`kanban-app/ui/src/components/inspectors-container.tsx`**\n+   - When `panelStack.length > 0`, wrap the whole rendered panel list in one `<FocusLayer name=\"inspector\">`. Passing `parentLayerKey={windowLayerKey}` from `useCurrentLayerKey()`.\n+   - Each `InspectorPanel` wraps its content in `<FocusScope kind=\"zone\" moniker={`panel:${entityType}:${entityId}`}>`.\n+   - Remove `useRestoreFocus()` — layer's `last_focused` replaces it.\n\n-### FocusLayer prop addition\n+2. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`**\n+   - No layer wrapping; container owns the layer.\n+   - The inner `<FocusScope>` wrapping `<EntityInspector>` can stay as the panel's zone scope (or lift that to `InspectorsContainer` — one FocusScope per panel).\n\n-To support explicit parent wiring for the inspector stack, `FocusLayer` accepts an optional `parentLayerKey` prop that overrides the ancestor-context default:\n-\n-```tsx\n-<FocusLayer name=\"inspector\" parentLayerKey={parentKey}>\n-  <InspectorPanel .../>\n-</FocusLayer>\n-```\n+3. **`kanban-app/ui/src/components/entity-inspector.tsx`**\n+   - Field row wrappers become `<FocusScope kind=\"zone\">` (detailed in migration card `01KNQY0P9J...`).\n\n-- If `parentLayerKey` is provided, use it.\n-- Otherwise, read from ancestor `FocusLayerContext`.\n-- If neither, this is a root layer (window) with `parent = None`.\n+### FocusLayer prop requirement\n\n-Update card `01KNQXW7HH...` tests to cover the explicit-parent case.\n+Assumes `FocusLayer` already supports the optional `parentLayerKey` prop from card `01KNQXW7HH...` — inspector layer passes `parentLayerKey={useCurrentLayerKey()}` at mount so the parent link is explicit (portaled overlays break React ancestor chain).\n\n ### Subtasks\n-- [ ] Add optional `parentLayerKey` prop to FocusLayer for explicit parent wiring\n-- [ ] In `InspectorsContainer`, generate/track a layer key per panel entry; compute parent chain\n-- [ ] Wrap each `InspectorPanel` in `<FocusLayer name=\"inspector\" parentLayerKey={...}>`\n+- [ ] When inspector_stack non-empty, mount `<FocusLayer name=\"inspector\" parentLayerKey={windowLayerKey}>` wrapping the panel list\n+- [ ] Wrap each `InspectorPanel` content in `<FocusScope kind=\"zone\">` using a panel moniker\n - [ ] Remove `useRestoreFocus()` from `inspectors-container.tsx`\n-- [ ] Verify each inspector captures nav (arrows only move within that panel's fields)\n-- [ ] Verify panel-close pops only that layer and restores focus to parent's `last_focused`\n+- [ ] Verify: closing one of multiple panels leaves the inspector layer mounted\n+- [ ] Verify: closing the last panel unmounts the inspector layer and focus returns to window root's `last_focused`\n\n ## Acceptance Criteria\n-- [ ] N open inspector panels in a window → N inspector layers in the forest, chained parent→child under that window's root\n-- [ ] Panel nav is captured — arrows don't escape the topmost panel (hard boundary)\n-- [ ] Closing topmost panel restores focus to the panel below (via that layer's `last_focused`), not the window root\n-- [ ] Closing the only open panel restores focus to the window root's `last_focused`\n+- [ ] Open inspector panels in a window → exactly one inspector layer per window (not one per panel)\n+- [ ] Each open panel is its own Zone within that inspector layer\n+- [ ] Cross-panel nav works as normal cross-zone leaf fallback (beam rule 2)\n+- [ ] Nav is captured inside the inspector layer — arrows can't reach board/nav bar/etc.\n+- [ ] Closing the last panel pops the inspector layer; window's focus restored to layer's `last_focused`\n+- [ ] Two windows each with inspectors → 4 layers total: 2 window roots + 2 inspector layers; zero cross-window interference\n - [ ] `useRestoreFocus` removed from inspectors-container.tsx\n-- [ ] Two windows, each with one inspector open → 4 layers total, 2 roots, 2 inspector children, no cross-window focus interference\n-- [ ] Window layer navigation flows freely across board, toolbar, tab bar, perspective bar (within the window root layer)\n-- [ ] Existing `claimWhen` predicates still work (this card doesn't remove them yet)\n - [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `inspectors-container.test.tsx` — opening 2 panels creates 2 layer pushes with correct parent chain (panel2.parent == panel1.layer_key, panel1.parent == window_root_key)\n-- [ ] `inspectors-container.test.tsx` — closing panel 2 pops only panel 2's layer; panel 1 still on the stack\n-- [ ] `inspectors-container.test.tsx` — no more `useRestoreFocus`; focus save/restore via layer stack\n-- [ ] Integration: open inspector A, open inspector B on top, close B → focus returns to A's last focused field, not the board\n-- [ ] Integration: with no inspector open, nav.up from top board element reaches toolbar / tab bar (all within window root layer)\n-- [ ] Integration (requires 2 windows): open inspector in window A; focus card in window B; nav in B stays in B's root layer; nav in A stays in A's inspector\n+- [ ] `inspectors-container.test.tsx` — opening first panel pushes exactly one layer; opening second panel pushes a zone (not another layer)\n+- [ ] `inspectors-container.test.tsx` — closing one of two panels unregisters that panel's zone; inspector layer still present\n+- [ ] `inspectors-container.test.tsx` — closing the only panel unmounts the inspector layer (pop_layer called once)\n+- [ ] `inspectors-container.test.tsx` — no more `useRestoreFocus`\n+- [ ] Integration: open A, open B; focused on some field in B; close B → focus lands on something in A (via fallback + zone last_focused)\n+- [ ] Integration: with inspector layer open, arrow keys never focus a board card (different layer)\n+- [ ] Rust multi-window: `children_of_layer(window_A_root) == [window_A_inspector_layer]`; `children_of_layer(window_B_root) == [window_B_inspector_layer]`; the two inspector layers don't see each other\n - [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -1,85 +1,77 @@\n ## What\n\n-When any inspector panel is open in a window, mount a **single `<FocusLayer name=\"inspector\">`** that contains all open panels in that window. Each panel is a **Zone** inside that shared layer. Field rows within a panel are also Zones; their labels and pills are Leaves.\n+Wrap every inspector panel in its own `<FocusLayer name=\"inspector\">`. The inspector stack in a window (see `WindowState.inspector_stack: Vec<String>`) becomes a **chain of parent→child layers** in the layer forest — one layer per panel, each with its parent being the panel below it (or the window root, for the first panel).\n\n-This is different from \"one layer per panel\" — we use **one layer, many zones** because:\n+### Why one layer per panel\n\n-- Inspector panels are all equivalent modality: they all capture nav, they all defer to the same dismiss chain. A single layer is the cleanest way to express that shared modal boundary.\n-- Nav between adjacent open panels is then natural cross-zone leaf fallback (beam rule 2) — no special case needed.\n-- Per-panel focus memory is still per-zone (each panel is a Zone and gets its own `last_focused`), so closing the top panel still restores focus to the panel below.\n-- In multi-window, each window gets its own inspector layer — a window's layer is a child of that window's root. No cross-window leakage.\n+- Each panel is semantically independent — its own focus memory, its own nav boundary.\n+- Closing panel #2 while panel #1 is still open must restore focus to panel #1's last-focused field, not to the window root. That only works if each panel has its own `LayerEntry` with its own `last_focused`.\n+- A dialog opened from within panel #2 (see new card for dialog layers) stacks on top of panel #2, not on top of \"the inspector\". Per-panel layers keep the parent chain honest.\n+- Two windows each with their own inspector stack → separate layer chains under separate window roots. No global \"inspector layer\" concept.\n\n-### Shape\n+### Layer boundary semantics\n\n-```\n-window_A root layer (FocusLayer name=\"window\")\n-  window chrome (NavBar zone, Toolbar zone, etc.) — see shell zones card\n-  board content (columns, cards, ...)\n+**Layers are hard boundaries.** Navigation stays within the active (focused) layer. You cannot arrow out of the topmost inspector panel — dismiss it to return to whatever layer was below it. The nav algorithm (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`) enforces this unconditionally.\n\n-  inspector layer (FocusLayer name=\"inspector\") — mounted iff inspector_stack non-empty\n-    panel_1 (FocusScope kind=\"zone\")\n-      field_row (FocusScope kind=\"zone\")\n-        label (FocusScope kind=\"leaf\")\n-        pill_a (FocusScope kind=\"leaf\")\n-        ...\n-    panel_2 (FocusScope kind=\"zone\")\n-      ...\n+When a FocusLayer unmounts, the caller reads the parent layer's `last_focused` (stored on `LayerEntry`) and sets it as the new focus for that window. This replaces the ad-hoc `useRestoreFocus()` hook entirely.\n\n-window_B root layer\n-  ...\n-  inspector layer (if open)\n-    panel_1 ...\n-```\n+### Files to modify\n+\n+1. **`kanban-app/ui/src/components/inspectors-container.tsx`** — Wrap each `InspectorPanel` body in `<FocusLayer name=\"inspector\">`. Each panel gets its own layer; because FocusLayer reads its parent via React context, nesting happens naturally from the DOM tree. Panel #1 is inside the window root layer's context, so its parent is the window root. Panel #2 is rendered as a sibling of panel #1 in the JSX — so its parent is *also* the window root, which is **wrong** if we want the panel-close-to-panel-below behavior.\n+\n+   **Options**:\n+   - (a) Nest the JSX: render panel #2 inside panel #1's subtree. Simple, but may fight against the existing stack-of-sibling-SlidePanels layout.\n+   - (b) Parent panel #N's layer explicitly to panel #(N-1)'s layer key. Requires passing the parent layer key as a prop into the FocusLayer's `parent_key` (instead of reading context). Keeps the visual layout unchanged.\n+   - **Choose (b)**: `InspectorsContainer` tracks the layer keys as it maps over `panelStack` and passes `parentLayerKey={previousPanelLayerKey ?? windowLayerKey}` to each `FocusLayer`. The window layer key is read from context.\n\n-### Layer boundary semantics\n+2. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`** — No layer wrapping here; `InspectorsContainer` owns the layer per panel. This file just stays focused on commands + the inner `<FocusScope>` for the entity moniker.\n\n-- Arrows inside the inspector layer can move anywhere in that layer (within-zone first, then cross-zone leaf fallback). You cannot arrow out to the board — that's a different layer. Dismiss (Escape) pops whatever's appropriate.\n-- Closing the last panel unmounts the inspector layer; focus-changed event restores focus to the window root layer's `last_focused` (stored when the inspector layer was pushed).\n-- Closing one of N panels (with N > 1) unregisters that panel's zone and its descendant entries. The dynamic-lifecycle fallback (card `01KNS0B3HY...`) picks up — walks `parent_zone` chain from the lost focus up to the next valid `last_focused`, which will be the inspector layer's root or another panel.\n+3. **`kanban-app/ui/src/components/inspectors-container.tsx`** (same file) — Remove `useRestoreFocus()` import and call. FocusLayer handles focus save/restore via the layer's `last_focused`.\n\n-### Files to modify\n+4. **`kanban-app/ui/src/components/entity-inspector.tsx`** — The manual first-field focus on mount should still work — it sets focus within the inspector layer. Verify no conflict with the layer's first-focus-on-mount behavior.\n\n-1. **`kanban-app/ui/src/components/inspectors-container.tsx`**\n-   - When `panelStack.length > 0`, wrap the whole rendered panel list in one `<FocusLayer name=\"inspector\">`. Passing `parentLayerKey={windowLayerKey}` from `useCurrentLayerKey()`.\n-   - Each `InspectorPanel` wraps its content in `<FocusScope kind=\"zone\" moniker={`panel:${entityType}:${entityId}`}>`.\n-   - Remove `useRestoreFocus()` — layer's `last_focused` replaces it.\n+### FocusLayer prop addition\n\n-2. **`kanban-app/ui/src/components/inspector-focus-bridge.tsx`**\n-   - No layer wrapping; container owns the layer.\n-   - The inner `<FocusScope>` wrapping `<EntityInspector>` can stay as the panel's zone scope (or lift that to `InspectorsContainer` — one FocusScope per panel).\n+To support explicit parent wiring for the inspector stack, `FocusLayer` accepts an optional `parentLayerKey` prop that overrides the ancestor-context default:\n\n-3. **`kanban-app/ui/src/components/entity-inspector.tsx`**\n-   - Field row wrappers become `<FocusScope kind=\"zone\">` (detailed in migration card `01KNQY0P9J...`).\n+```tsx\n+<FocusLayer name=\"inspector\" parentLayerKey={parentKey}>\n+  <InspectorPanel .../>\n+</FocusLayer>\n+```\n\n-### FocusLayer prop requirement\n+- If `parentLayerKey` is provided, use it.\n+- Otherwise, read from ancestor `FocusLayerContext`.\n+- If neither, this is a root layer (window) with `parent = None`.\n\n-Assumes `FocusLayer` already supports the optional `parentLayerKey` prop from card `01KNQXW7HH...` — inspector layer passes `parentLayerKey={useCurrentLayerKey()}` at mount so the parent link is explicit (portaled overlays break React ancestor chain).\n+Update card `01KNQXW7HH...` tests to cover the explicit-parent case.\n\n ### Subtasks\n-- [ ] When inspector_stack non-empty, mount `<FocusLayer name=\"inspector\" parentLayerKey={windowLayerKey}>` wrapping the panel list\n-- [ ] Wrap each `InspectorPanel` content in `<FocusScope kind=\"zone\">` using a panel moniker\n+- [ ] Add optional `parentLayerKey` prop to FocusLayer for explicit parent wiring\n+- [ ] In `InspectorsContainer`, generate/track a layer key per panel entry; compute parent chain\n+- [ ] Wrap each `InspectorPanel` in `<FocusLayer name=\"inspector\" parentLayerKey={...}>`\n - [ ] Remove `useRestoreFocus()` from `inspectors-container.tsx`\n-- [ ] Verify: closing one of multiple panels leaves the inspector layer mounted\n-- [ ] Verify: closing the last panel unmounts the inspector layer and focus returns to window root's `last_focused`\n+- [ ] Verify each inspector captures nav (arrows only move within that panel's fields)\n+- [ ] Verify panel-close pops only that layer and restores focus to parent's `last_focused`\n\n ## Acceptance Criteria\n-- [ ] Open inspector panels in a window → exactly one inspector layer per window (not one per panel)\n-- [ ] Each open panel is its own Zone within that inspector layer\n-- [ ] Cross-panel nav works as normal cross-zone leaf fallback (beam rule 2)\n-- [ ] Nav is captured inside the inspector layer — arrows can't reach board/nav bar/etc.\n-- [ ] Closing the last panel pops the inspector layer; window's focus restored to layer's `last_focused`\n-- [ ] Two windows each with inspectors → 4 layers total: 2 window roots + 2 inspector layers; zero cross-window interference\n+- [ ] N open inspector panels in a window → N inspector layers in the forest, chained parent→child under that window's root\n+- [ ] Panel nav is captured — arrows don't escape the topmost panel (hard boundary)\n+- [ ] Closing topmost panel restores focus to the panel below (via that layer's `last_focused`), not the window root\n+- [ ] Closing the only open panel restores focus to the window root's `last_focused`\n - [ ] `useRestoreFocus` removed from inspectors-container.tsx\n+- [ ] Two windows, each with one inspector open → 4 layers total, 2 roots, 2 inspector children, no cross-window focus interference\n+- [ ] Window layer navigation flows freely across board, toolbar, tab bar, perspective bar (within the window root layer)\n+- [ ] Existing `claimWhen` predicates still work (this card doesn't remove them yet)\n - [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `inspectors-container.test.tsx` — opening first panel pushes exactly one layer; opening second panel pushes a zone (not another layer)\n-- [ ] `inspectors-container.test.tsx` — closing one of two panels unregisters that panel's zone; inspector layer still present\n-- [ ] `inspectors-container.test.tsx` — closing the only panel unmounts the inspector layer (pop_layer called once)\n-- [ ] `inspectors-container.test.tsx` — no more `useRestoreFocus`\n-- [ ] Integration: open A, open B; focused on some field in B; close B → focus lands on something in A (via fallback + zone last_focused)\n-- [ ] Integration: with inspector layer open, arrow keys never focus a board card (different layer)\n-- [ ] Rust multi-window: `children_of_layer(window_A_root) == [window_A_inspector_layer]`; `children_of_layer(window_B_root) == [window_B_inspector_layer]`; the two inspector layers don't see each other\n+- [ ] `inspectors-container.test.tsx` — opening 2 panels creates 2 layer pushes with correct parent chain (panel2.parent == panel1.layer_key, panel1.parent == window_root_key)\n+- [ ] `inspectors-container.test.tsx` — closing panel 2 pops only panel 2's layer; panel 1 still on the stack\n+- [ ] `inspectors-container.test.tsx` — no more `useRestoreFocus`; focus save/restore via layer stack\n+- [ ] Integration: open inspector A, open inspector B on top, close B → focus returns to A's last focused field, not the board\n+- [ ] Integration: with no inspector open, nav.up from top board element reaches toolbar / tab bar (all within window root layer)\n+- [ ] Integration (requires 2 windows): open inspector in window A; focus card in window B; nav in B stays in B's root layer; nav in A stays in A's inspector\n - [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Each inspector panel is its own FocusLayer (stacked under the window root)\n\\ No newline at end of file\n+Inspector layer: one layer per window, panels and field rows as zones inside\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Inspector layer: one layer per window, panels and field rows as zones inside\n\\ No newline at end of file\n+Each inspector panel is its own FocusLayer (stacked under the window root)\n\\ No newline at end of file\n"}]]}

---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m03y0nkx8e3cgrvwnh5mqpm8
  text: |-
    Picked up. Research before the deletion.

    **Consumer search — whole workspace, not only mirdan.** `rg --hidden --no-ignore` for `retired_validators|retired-validators` across the repo returns source hits in exactly four files, all inside `crates/mirdan`:

    - `crates/mirdan/src/retired_validators.rs` (the module itself)
    - `crates/mirdan/src/install/profile.rs` (the two prune calls + `report_pruned`)
    - `crates/mirdan/src/install/tests.rs` (five tests + helpers)
    - `crates/mirdan/src/lib.rs` (the `pub mod` + doc)

    Every other hit is a `.kanban` card or a `.sah/mcp.*.log` line, neither of which is code.

    **`swissarmyhammer-doctor` and the validators crate do not consume it.** `swissarmyhammer-validators/src/builtin/mod.rs` carries its own private `RETIRED_VALIDATOR_NAMES` constant, but that is an unrelated name: it is a test-local list asserting the nine merged single-rule sets no longer load standalone from the BUILTIN layer. It never reads mirdan, never reads the deployed store, and does not depend on the prune having run. `swissarmyhammer-doctor` has no hit at all. Conclusion: no consumer outside mirdan. Deletion is safe to proceed.

    **Shared helpers that must survive.** Three symbols the prune uses also serve other callers, so they stay:
    - `VERB_REMOVED` (profile.rs) — also used by the agent sweep, the deinit validator result, and the generic deinit step.
    - `store::remove_if_exists` — used by deploy.rs, uninstall.rs, and profile.rs deinit.
    - the `walkdir` dependency — also used by `publish.rs`.
    `InitEvent::Action` is the generic reporter event; only the prune's *use* of it goes.

    **In `install/tests.rs`, `PathBuf`, `NullReporter` and `validators_only_profile()` all have non-retired callers** and stay; only `retired_fixture_files`, `deploy_retired_files`, `RETIRED_FIXTURE_COUNT`, `USER_EDITED_FIXTURE` and `USER_EDITED_FIXTURE_BODY` are prune-only and go.

    **How deinit clears a store.** `deinit_profile_validators` walks the embedded builtin set roster, removes each builtin-owned file with `store::remove_if_exists`, then climbs the parents with `remove_empty_dirs_up_to`, so an emptied set directory goes too. `deinit_profile` then calls `prune_store_readme_at` for the store README. That is the mechanism the card names as the replacement.
  timestamp: 2026-08-16T00:02:17.853855+00:00
- actor: claude-code
  id: 01m03ykc3dvzqqq1jtacc66843
  text: |-
    Deletion landed, and verified against a throwaway HOME under the scratchpad. The real `~/.validators` and the user's own HOME were never touched — every run set `HOME` to a scratchpad directory, and the install output names that path.

    **Install still works with the prune calls gone.** `sah init user` into the throwaway HOME deployed **13 validator set(s), 156 files**, and named no retired set among them. `sah doctor` against that store then reported every one of the 13 sets green and exited 0, so doctor does not regress.

    **deinit clears the store completely.** 156 files before, and after `sah deinit user` the `.validators` directory itself is gone. That is the card's premise, measured rather than assumed.

    **One measurement worth keeping, so it is not re-discovered.** `deinit_profile_validators` walks the CURRENT embedded roster, so it clears every set the current binary installs and it does NOT reach content only an older binary wrote. Measured on a second throwaway HOME: a store of 158 files — the 156 an install wrote, plus the pre-merge `no-secrets` set recovered from `git show HEAD:` — answers deinit with the 156 gone and the `no-secrets` directory still standing. This is the residue the deleted snapshot mechanism used to sweep. The card records the user's decision that the mechanism should not exist, so this is a stated fact rather than a blocker; a user who wants that residue gone removes the store directory.

    **Nothing else was dead scaffolding.** Three symbols the prune touched serve other callers and stay: `VERB_REMOVED` (four other uses in `profile.rs`), `store::remove_if_exists` (`deploy.rs`, `uninstall.rs`, the deinit path), and the `walkdir` dependency (`publish.rs`). `InitEvent::Action` is the generic reporter event; only the prune's two uses of it went. `swissarmyhammer-validators/src/builtin/mod.rs` keeps its own `RETIRED_VALIDATOR_NAMES` and `crates/mirdan/src/builtin_validators.rs` keeps its own retired-name assertion — both read the BUILTIN embed rather than the deployed store, and neither referenced the deleted module.
  timestamp: 2026-08-16T00:12:30.701299+00:00
- actor: claude-code
  id: 01m03yktqp9djdheqcdme161g9
  text: |-
    ### implement — changed
    - evidence: 25 files deleted, 3 files edited, 2119 + 326 lines removed.
      - deleted: `crates/mirdan/src/retired_validators.rs` (713 lines — `RetiredFile`, `RetiredSet`, `RetiredSetFile`, `RETIRED_VALIDATOR_SETS`, `RETIRED_VALIDATOR_FILES`, `prune_unmodified_retired_sets`, `prune_unmodified_retired_files`, `deployed_set_matches_snapshot`, `collect_relative_files`, and its 8 tests).
      - deleted: `crates/mirdan/retired-validators/` — all 24 snapshot files across 11 set directories, the file-grain rule snapshots and the four fixture snapshots included.
      - edited: `crates/mirdan/src/install/profile.rs` (-37) — both prune calls and their comment block out of `install_profile_validators`, and the `report_pruned` helper with its doc.
      - edited: `crates/mirdan/src/install/tests.rs` (-282) — `RETIRED_FIXTURE_COUNT`, `USER_EDITED_FIXTURE`, `USER_EDITED_FIXTURE_BODY`, `retired_fixture_files`, `deploy_retired_files`, and the four retired-prune tests.
      - edited: `crates/mirdan/src/lib.rs` (-7) — the `pub mod retired_validators;` line and its doc.
    - consumer search: `rg --hidden --no-ignore` over the whole workspace put every source reference inside `crates/mirdan`; each other hit is a `.kanban` card or a `.sah/mcp.*.log` line. `swissarmyhammer-doctor` has no hit, and the validators crate's own `RETIRED_VALIDATOR_NAMES` reads the builtin embed rather than the deployed store. No consumer outside mirdan.
    - real runs, throwaway HOME under the scratchpad: `sah init user` deployed 13 validator set(s) / 156 files; `sah doctor` reported all 13 green at exit 0; `sah deinit user` left the `.validators` directory itself gone. A second HOME measured that deinit does not reach a pre-merge set an older binary wrote.
    - `cargo fmt` clean; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo nextest run -E 'rdeps(mirdan)'` — 3324 tests run, 3324 passed, 0 skipped, in 83.5 s.
    - next: ready for `/review`. Not committed. Left in `doing`.
  timestamp: 2026-08-16T00:12:45.686485+00:00
- actor: claude-code
  id: 01m03z1hex1kjztn6dprxyc02t
  text: |-
    ### test — green
    - evidence:
      - `cargo fmt --check` — exit 0, no diff.
      - `cargo clippy --workspace --all-targets -- -D warnings` — exit 0, zero warnings (ran twice: once after normal build, once after `cargo clean -p mirdan` + `cargo build -p mirdan` from clean metadata, both clean).
      - `cargo build -p mirdan` from clean metadata (after `cargo clean -p mirdan`, 14211 files removed) — succeeds.
      - `cargo nextest run --workspace` — 14221 tests run, 14221 passed (55 slow), 0 failed, 0 skipped.
      - Dangling-reference search: `rg` for `retired_validators|RetiredFile|RetiredSet|RetiredSetFile|RETIRED_VALIDATOR_SETS|RETIRED_VALIDATOR_FILES|prune_unmodified_retired_sets|prune_unmodified_retired_files|report_pruned|RETIRED_FIXTURE_COUNT|retired-validators` across the whole workspace (excluding `target/`, `.kanban/`) — zero hits.
      - `RETIRED_VALIDATOR_NAMES` check — only hit is `crates/swissarmyhammer-validators/src/builtin/mod.rs` (the separate, correctly untouched constant); nothing in mirdan.
      - `crates/mirdan/src/lib.rs` diff — module reference and doc line removed, no dangling `pub mod retired_validators`.
      - `crates/mirdan/src/install/profile.rs` diff — both prune calls and their doc comment removed from `install_profile_validators`, `report_pruned` fully gone from the crate.
      - `crates/mirdan/src/install/tests.rs` diff — 282 lines removed; retired-prune tests and `RETIRED_FIXTURE_COUNT` gone, no orphaned helpers left behind.
    - next: task stays in `doing`; ready for review step.
  timestamp: 2026-08-16T00:20:14.941392+00:00
- actor: claude-code
  id: 01m03zd7gbktcjrg9b4wcsewng
  text: |-
    ### review — clean

    - evidence: `review sha HEAD~1..HEAD` (6b6fe8cf1) — 0 findings, 0 confirmed, 0 refuted, 9 validators attempted, 0 failed. `.kanban/` excluded by `.reviewignore`.
    - Deletion is complete. All 11 deleted symbols return zero hits outside git history: `retired_validators`, `RetiredFile`, `RetiredSet`, `RetiredSetFile`, `RETIRED_VALIDATOR_SETS`, `RETIRED_VALIDATOR_FILES`, `prune_unmodified_retired_sets`, `prune_unmodified_retired_files`, `report_pruned`, `RETIRED_FIXTURE_COUNT`, and the `retired-validators` path.
    - Nothing serving another purpose was taken. Every deleted path is inside `crates/mirdan/retired-validators/` or the four source files the card names. No live validator content was removed.
    - The three retained items are justified. `VERB_REMOVED` (profile.rs:129) still has four raise sites at profile.rs:851, 1080, 1289, 1404. `store::remove_if_exists` (store.rs:203) is called from uninstall.rs:234, deploy.rs:123/143/237/265/337 and profile.rs:693. `walkdir` is used by publish.rs:11,162.
    - `RETIRED_VALIDATOR_NAMES` lives in another crate at crates/swissarmyhammer-validators/src/builtin/mod.rs:110,523 and this commit does not touch it. It is a separate concern.
    - The 78 insertions are entirely this card's own record: 73 lines in `.kanban/tasks/01M02NM65E1MFJ3F27K35TGZ1C.md` and 5 in the matching `.jsonl`. Zero added lines outside `.kanban/`. Everything else is pure deletion.
    - `install_profile_validators` reads correctly with both prune calls and `report_pruned` gone. `target_root` and `reporter` are both still consumed, the only emitted event is `VERB_DEPLOYED`, and no branch became unreachable. `cargo clippy -p mirdan --all-targets` reports zero warnings, so no dead import or unused binding survived.
    - Deleted tests are all retirement-specific and tests.rs is a pure deletion with no added lines. `cargo nextest run -p mirdan`: 503 tests run, 503 passed, 0 skipped.

    #### Out of scope for this card, needs its own card

    `deinit_profile_validators` walks the CURRENT embedded roster, so it clears every set the current binary installs but leaves content only an OLDER binary wrote. On a throwaway HOME a pre-merge `no-secrets` set survived deinit. This narrows the card's premise: "deinit already clears a store" is true for stores the current binary wrote, not for stores an older binary wrote.

    This is not a finding on this card, for two rule-bound reasons. The review op is a diff op, and `deinit_profile_validators` is not in this diff at all — a defect on an unchanged line is not a finding under `review sha`. And the card's own "Done when" asks that "deinit verifiably clears a populated store, measured against a throwaway HOME", which was measured and holds.

    The gap is real and should not disappear. It deserves its own card: make deinit clear validator content an older binary wrote, not only what the current roster names.

    - next: none. Card is clean and moves to done.
  timestamp: 2026-08-16T00:26:37.963383+00:00
- actor: claude-code
  id: 01m03zejpj1qxa8d231r1ejpga
  text: |
    ### finish iteration 1 — clean
    - implement: changed — deleted crates/mirdan/src/retired_validators.rs (713 lines) and crates/mirdan/retired-validators/ (24 snapshot files, 11 set directories); edited crates/mirdan/src/install/profile.rs, install/tests.rs, lib.rs. Nothing outside crates/mirdan consumed the module.
    - test: green — cargo fmt --check exit 0; cargo clippy --workspace --all-targets -- -D warnings exit 0, 0 warnings; cargo nextest run --workspace 14221 passed, 0 failed, 0 skipped; cargo build -p mirdan from clean metadata succeeded; a workspace search for all 11 deleted symbols returns zero hits.
    - commit: 6b6fe8cf1 fix(mirdan): delete the retired-validator mechanism (^35tgz1c) — 30 files changed, 78 insertions, 2447 deletions
    - review: CLEAN — 0 findings over 9 validators attempted. Task moved to `done`.
    - detail: the reviewer verified the four completeness questions independently rather than trusting the tester. All 11 deleted symbols answer zero hits. The three retentions are each justified by live callers: `VERB_REMOVED` has four raise sites, `store::remove_if_exists` is called from uninstall.rs and deploy.rs and profile.rs, and `walkdir` serves publish.rs. `RETIRED_VALIDATOR_NAMES` stands in a different crate and this commit does not touch it. All 78 insertions are the card's own board record — zero added lines stand outside `.kanban/`.

    ### The deinit gap — carried to its own card

    `deinit_profile_validators` walks the CURRENT embedded roster. It clears every set the current binary installs, and it leaves content only an OLDER binary wrote. Measured on a throwaway HOME: a pre-merge `no-secrets` set SURVIVED deinit.

    This narrows the card's premise. "deinit already clears a store" is true for a store the current binary wrote, and not for one an older binary wrote. The card's own "Done when" asks that deinit verifiably clear a populated store, which was measured and holds.

    The reviewer correctly declined to raise it as a finding on this card: `deinit_profile_validators` stands on no line of this diff, and a defect on an unchanged line is not a finding under a diff-scoped review.

    It is now its own card so it cannot disappear.
  timestamp: 2026-08-16T00:27:22.194254+00:00
position_column: done
position_ordinal: ffffffffffffffffffffffffffffffffffffffffff9480
title: Delete the retired-validator mechanism entirely — deinit already clears a store
---
`mirdan` carries a whole subsystem for pruning retired validators out of a deployed `~/.validators/` or `./.validators/` store: byte-frozen snapshots of every removed rule, fixture and set, compared byte for byte, removed only on an exact match.

**`deinit` already clears a store.** The snapshot mechanism is a second, far more expensive way to reach the same end state, and it carries an ongoing tax: every rule or fixture ever deleted must be recovered from git, verified by sha256, and frozen forever, or the prune silently stops firing.

The user's decision, verbatim: *"we SHOULD NOT have 'retirement' code at all, we have deinit."*

## What to delete

- `crates/mirdan/src/retired_validators.rs` — the whole module: `RetiredFile`, `RetiredSet`, `RetiredSetFile`, `RETIRED_VALIDATOR_SETS`, `RETIRED_VALIDATOR_FILES`, `prune_unmodified_retired_sets`, `prune_unmodified_retired_files`, and their tests.
- `crates/mirdan/retired-validators/` — every byte-frozen snapshot. There are 11 set directories plus the file-grain snapshots.
- The wiring in `crates/mirdan/src/install/profile.rs`: both prune calls in `install_profile_validators`, the shared `report_pruned` helper, and the `InitEvent::Action` reporting of removed files.
- `RETIRED_FIXTURE_COUNT` and the retired-prune tests in `crates/mirdan/src/install/tests.rs`.
- The module reference in `crates/mirdan/src/lib.rs` and its doc.

## What must NOT break

- `deinit` must still clear a store completely — that is the whole premise. Verify it does, with a real run against a throwaway `HOME` holding a populated store, rather than assuming.
- `install_profile_validators` must still install correctly with the prune calls gone.
- `sah doctor` must not regress.

## Check before deleting

Confirm nothing outside `mirdan` consumes the module. `swissarmyhammer-doctor` and the validators crate both read the deployed store, so establish they do not depend on the prune having run.

## History, so this is not re-litigated

Two cards built this: `^w6ypb8b` added the file grain and `^4kzxdex` the fixture grain, both landing this session. Both are superseded by this decision rather than wrong at the time — the set-level prune predates them. The measured behaviour they established is preserved here for whoever revisits the question: an exact byte match removes the file, any user edit leaves it alone, and a store written before a rule was deleted keeps running that rule until something removes it. `deinit` is that something.

## Done when

- No `retired_validators` module, no `retired-validators/` tree, no prune call in the install path.
- `deinit` verifiably clears a populated store, measured against a throwaway HOME.
- `cargo nextest run --workspace` green; fmt and clippy clean.

#tool-validators
{"id":"01KMBE3DQN27WP8HP9NFCXCT0X","timestamp":"2026-03-22T18:49:44.437475Z","entity_type":"task","entity_id":"01KMBE3DQN58BKFCJGRK40GPYA","op":"create","changes":[["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\nThe current test mocks `useFieldUpdate` at the module level and tests individual editors directly. This hides the real bug — props not flowing through the component tree. Rewrite the test to render a `Field` component (to be created in the next card) with real entity identity, and assert `updateField` is called through the actual React context.\n\n### Files to modify\n- `kanban-app/ui/src/components/fields/editors/editor-save.test.tsx` — rewrite adapters to render `<Field>` instead of individual editors\n\n### Approach\n1. Remove the `vi.mock(\"@/lib/field-update-context\")` module mock\n2. Render `<Field entity={...} fieldDef={...} mode=\"compact|full\" onDone={...} />` inside a `FieldUpdateProvider`\n3. Mock `invoke` (not `useFieldUpdate`) so the real context and hook are exercised\n4. Assert `invoke(\"dispatch_command\", { cmd: \"entity.update_field\", ... })` is called\n5. Keep the matrix: all field types × all keymaps × all exit paths\n6. Tests will fail (Field doesn't exist yet) — that's the red\n\n## Acceptance Criteria\n- [ ] Tests render through Field, not individual editors\n- [ ] No module-level mock of useFieldUpdate\n- [ ] Tests assert on the actual invoke call, not a mocked hook\n- [ ] All tests fail (Field component doesn't exist yet)\n\n## Tests\n- [ ] `cd kanban-app/ui && npx vitest run src/components/fields/editors/editor-save.test.tsx` — runs, all fail"}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"ae80"}],["title",{"kind":"set","value":"Update editor-save.test.tsx to test through Field component, not individual editors"}]],"transaction_id":"01KMBE3D4WYZDD6308HXHE9RTX"}
{"id":"01KMBE3XYVPCX3BEWBRP21K0ZC","timestamp":"2026-03-22T18:50:01.051747Z","entity_type":"task","entity_id":"01KMBE3DQN58BKFCJGRK40GPYA","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,22 +1,29 @@\n ## What\n\n-The current test mocks `useFieldUpdate` at the module level and tests individual editors directly. This hides the real bug — props not flowing through the component tree. Rewrite the test to render a `Field` component (to be created in the next card) with real entity identity, and assert `updateField` is called through the actual React context.\n+The current test mocks `useFieldUpdate` at the module level and tests individual editors directly. This hides real bugs. Rewrite the test to render a `Field` component (to be created in the next card) that is a fully data-bound control:\n\n+- **Reads** its current value from the entity store (via `useEntityStore`)\n+- **Writes** via `updateField` on every save-worthy exit path\n+- **Stays in sync** — re-renders when the entity store updates\n+\n+The test renders `<Field entity={...} fieldDef={...} mode={...} onDone={...} />` inside real providers (`FieldUpdateProvider`, `EntityStoreProvider`). No module-level mock of `useFieldUpdate`.\n+\n ### Files to modify\n-- `kanban-app/ui/src/components/fields/editors/editor-save.test.tsx` — rewrite adapters to render `<Field>` instead of individual editors\n+- `kanban-app/ui/src/components/fields/editors/editor-save.test.tsx` — rewrite\n\n ### Approach\n-1. Remove the `vi.mock(\"@/lib/field-update-context\")` module mock\n-2. Render `<Field entity={...} fieldDef={...} mode=\"compact|full\" onDone={...} />` inside a `FieldUpdateProvider`\n-3. Mock `invoke` (not `useFieldUpdate`) so the real context and hook are exercised\n-4. Assert `invoke(\"dispatch_command\", { cmd: \"entity.update_field\", ... })` is called\n-5. Keep the matrix: all field types × all keymaps × all exit paths\n+1. Remove `vi.mock(\"@/lib/field-update-context\")` \n+2. Wrap in real `FieldUpdateProvider` + `EntityStoreProvider`\n+3. Mock only `invoke` — assert `invoke(\"dispatch_command\", { cmd: \"entity.update_field\", ... })` is called\n+4. Keep the matrix: all field types × all keymaps × all exit paths × both modes\n+5. Add a sync test: update entity store after save, verify Field re-renders with new value\n 6. Tests will fail (Field doesn't exist yet) — that's the red\n\n ## Acceptance Criteria\n - [ ] Tests render through Field, not individual editors\n-- [ ] No module-level mock of useFieldUpdate\n-- [ ] Tests assert on the actual invoke call, not a mocked hook\n+- [ ] No module-level mock of useFieldUpdate — real context exercised\n+- [ ] Tests assert on the actual invoke call\n+- [ ] Includes a data-sync test (entity store update → Field re-renders)\n - [ ] All tests fail (Field component doesn't exist yet)\n\n ## Tests\n","reverse_patch":"--- original\n+++ modified\n@@ -1,29 +1,22 @@\n ## What\n\n-The current test mocks `useFieldUpdate` at the module level and tests individual editors directly. This hides real bugs. Rewrite the test to render a `Field` component (to be created in the next card) that is a fully data-bound control:\n-\n-- **Reads** its current value from the entity store (via `useEntityStore`)\n-- **Writes** via `updateField` on every save-worthy exit path\n-- **Stays in sync** — re-renders when the entity store updates\n-\n-The test renders `<Field entity={...} fieldDef={...} mode={...} onDone={...} />` inside real providers (`FieldUpdateProvider`, `EntityStoreProvider`). No module-level mock of `useFieldUpdate`.\n+The current test mocks `useFieldUpdate` at the module level and tests individual editors directly. This hides the real bug — props not flowing through the component tree. Rewrite the test to render a `Field` component (to be created in the next card) with real entity identity, and assert `updateField` is called through the actual React context.\n\n ### Files to modify\n-- `kanban-app/ui/src/components/fields/editors/editor-save.test.tsx` — rewrite\n+- `kanban-app/ui/src/components/fields/editors/editor-save.test.tsx` — rewrite adapters to render `<Field>` instead of individual editors\n\n ### Approach\n-1. Remove `vi.mock(\"@/lib/field-update-context\")` \n-2. Wrap in real `FieldUpdateProvider` + `EntityStoreProvider`\n-3. Mock only `invoke` — assert `invoke(\"dispatch_command\", { cmd: \"entity.update_field\", ... })` is called\n-4. Keep the matrix: all field types × all keymaps × all exit paths × both modes\n-5. Add a sync test: update entity store after save, verify Field re-renders with new value\n+1. Remove the `vi.mock(\"@/lib/field-update-context\")` module mock\n+2. Render `<Field entity={...} fieldDef={...} mode=\"compact|full\" onDone={...} />` inside a `FieldUpdateProvider`\n+3. Mock `invoke` (not `useFieldUpdate`) so the real context and hook are exercised\n+4. Assert `invoke(\"dispatch_command\", { cmd: \"entity.update_field\", ... })` is called\n+5. Keep the matrix: all field types × all keymaps × all exit paths\n 6. Tests will fail (Field doesn't exist yet) — that's the red\n\n ## Acceptance Criteria\n - [ ] Tests render through Field, not individual editors\n-- [ ] No module-level mock of useFieldUpdate — real context exercised\n-- [ ] Tests assert on the actual invoke call\n-- [ ] Includes a data-sync test (entity store update → Field re-renders)\n+- [ ] No module-level mock of useFieldUpdate\n+- [ ] Tests assert on the actual invoke call, not a mocked hook\n - [ ] All tests fail (Field component doesn't exist yet)\n\n ## Tests\n"}]],"transaction_id":"01KMBE3XYGGA5QESN01A2CTJD7"}
{"id":"01KMBEWHRGV7BP7YFM4ZYCHM8P","timestamp":"2026-03-22T19:03:27.760399Z","entity_type":"task","entity_id":"01KMBE3DQN58BKFCJGRK40GPYA","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ae80\n\\ No newline at end of file\n+fffffffffffc80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-fffffffffffc80\n\\ No newline at end of file\n+ae80\n\\ No newline at end of file\n"}]],"transaction_id":"01KMBEWH4JE71T6A54Z7F6VW26"}

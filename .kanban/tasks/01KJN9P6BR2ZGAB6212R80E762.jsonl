{"id":"01KJN9P6BSVV3PSY4T3230MRMC","timestamp":"2026-03-01T18:13:37.273190Z","op":"add task","input":{"assignees":[],"depends_on":[],"description":"**Part 2a of the YAML/MD storage conversion plan.**\n\nConvert task files from `{id}.json` to `{id}.md` with YAML frontmatter for metadata and markdown body for description.\n\n**Add serde_yaml dependency:** Add `serde_yaml = { workspace = true }` to `swissarmyhammer-kanban/Cargo.toml` (already in workspace at v0.9).\n\n**TaskMeta helper struct:** Create a struct with all Task fields except `description` and `id`, used only for YAML frontmatter serialization. Include legacy field handling (`_legacy_tags`, `_legacy_subtasks`).\n\n**context.rs changes:**\n- `task_path()` → return `tasks/{id}.md` instead of `.json`\n- `write_task()` → serialize TaskMeta as YAML frontmatter, append description as markdown body\n- `read_task()` → try `.md` first, fall back to `.json` for backward compat\n- `list_task_ids()` → accept both `.md` and `.json` extensions\n- Add `parse_task_markdown()` helper to split frontmatter from body\n\n**Example output format:**\n```\n---\ntitle: Fix the login bug\nposition:\n  column: doing\n  ordinal: a0\n---\nDescription text here with #tags\n```\n\n**Files:**\n- `swissarmyhammer-kanban/Cargo.toml` (add serde_yaml)\n- `swissarmyhammer-kanban/src/context.rs` (task_path, read/write/list)\n- `swissarmyhammer-kanban/src/types/task.rs` (TaskMeta struct if needed)\n\n- [ ] Add serde_yaml dependency to Cargo.toml\n- [ ] Create TaskMeta helper struct for frontmatter serialization\n- [ ] Update task_path() to return .md extension\n- [ ] Implement write_task() with YAML frontmatter + markdown body\n- [ ] Implement parse_task_markdown() helper\n- [ ] Update read_task() with .md/.json fallback\n- [ ] Update list_task_ids() to accept both extensions\n- [ ] Run `cargo nextest run -p swissarmyhammer-kanban`","position":null,"title":"Convert task storage from JSON to YAML frontmatter + markdown"},"output":{"assignees":[],"attachments":[],"comments":[],"depends_on":[],"description":"**Part 2a of the YAML/MD storage conversion plan.**\n\nConvert task files from `{id}.json` to `{id}.md` with YAML frontmatter for metadata and markdown body for description.\n\n**Add serde_yaml dependency:** Add `serde_yaml = { workspace = true }` to `swissarmyhammer-kanban/Cargo.toml` (already in workspace at v0.9).\n\n**TaskMeta helper struct:** Create a struct with all Task fields except `description` and `id`, used only for YAML frontmatter serialization. Include legacy field handling (`_legacy_tags`, `_legacy_subtasks`).\n\n**context.rs changes:**\n- `task_path()` → return `tasks/{id}.md` instead of `.json`\n- `write_task()` → serialize TaskMeta as YAML frontmatter, append description as markdown body\n- `read_task()` → try `.md` first, fall back to `.json` for backward compat\n- `list_task_ids()` → accept both `.md` and `.json` extensions\n- Add `parse_task_markdown()` helper to split frontmatter from body\n\n**Example output format:**\n```\n---\ntitle: Fix the login bug\nposition:\n  column: doing\n  ordinal: a0\n---\nDescription text here with #tags\n```\n\n**Files:**\n- `swissarmyhammer-kanban/Cargo.toml` (add serde_yaml)\n- `swissarmyhammer-kanban/src/context.rs` (task_path, read/write/list)\n- `swissarmyhammer-kanban/src/types/task.rs` (TaskMeta struct if needed)\n\n- [ ] Add serde_yaml dependency to Cargo.toml\n- [ ] Create TaskMeta helper struct for frontmatter serialization\n- [ ] Update task_path() to return .md extension\n- [ ] Implement write_task() with YAML frontmatter + markdown body\n- [ ] Implement parse_task_markdown() helper\n- [ ] Update read_task() with .md/.json fallback\n- [ ] Update list_task_ids() to accept both extensions\n- [ ] Run `cargo nextest run -p swissarmyhammer-kanban`","id":"01KJN9P6BR2ZGAB6212R80E762","position":{"column":"todo","ordinal":"a2"},"tags":[],"title":"Convert task storage from JSON to YAML frontmatter + markdown"},"duration_ms":2}
{"id":"01KJNG238P1PH6038F3E9E3VPQ","timestamp":"2026-03-01T20:04:58.774178Z","op":"move task","input":{"id":"01KJN9P6BR2ZGAB6212R80E762","position":{"column":"doing","ordinal":"a0"}},"output":{"assignees":[],"attachments":[],"comments":[],"depends_on":[],"description":"**Part 2a of the YAML/MD storage conversion plan.**\n\nConvert task files from `{id}.json` to `{id}.md` with YAML frontmatter for metadata and markdown body for description.\n\n**Add serde_yaml dependency:** Add `serde_yaml = { workspace = true }` to `swissarmyhammer-kanban/Cargo.toml` (already in workspace at v0.9).\n\n**TaskMeta helper struct:** Create a struct with all Task fields except `description` and `id`, used only for YAML frontmatter serialization. Include legacy field handling (`_legacy_tags`, `_legacy_subtasks`).\n\n**context.rs changes:**\n- `task_path()` → return `tasks/{id}.md` instead of `.json`\n- `write_task()` → serialize TaskMeta as YAML frontmatter, append description as markdown body\n- `read_task()` → try `.md` first, fall back to `.json` for backward compat\n- `list_task_ids()` → accept both `.md` and `.json` extensions\n- Add `parse_task_markdown()` helper to split frontmatter from body\n\n**Example output format:**\n```\n---\ntitle: Fix the login bug\nposition:\n  column: doing\n  ordinal: a0\n---\nDescription text here with #tags\n```\n\n**Files:**\n- `swissarmyhammer-kanban/Cargo.toml` (add serde_yaml)\n- `swissarmyhammer-kanban/src/context.rs` (task_path, read/write/list)\n- `swissarmyhammer-kanban/src/types/task.rs` (TaskMeta struct if needed)\n\n- [ ] Add serde_yaml dependency to Cargo.toml\n- [ ] Create TaskMeta helper struct for frontmatter serialization\n- [ ] Update task_path() to return .md extension\n- [ ] Implement write_task() with YAML frontmatter + markdown body\n- [ ] Implement parse_task_markdown() helper\n- [ ] Update read_task() with .md/.json fallback\n- [ ] Update list_task_ids() to accept both extensions\n- [ ] Run `cargo nextest run -p swissarmyhammer-kanban`","id":"01KJN9P6BR2ZGAB6212R80E762","position":{"column":"doing","ordinal":"a1"},"tags":[],"title":"Convert task storage from JSON to YAML frontmatter + markdown"},"duration_ms":1}
{"id":"01KJNG6SADWEYK7E1BB675P1P1","timestamp":"2026-03-01T20:07:32.429685Z","op":"complete task","input":{"id":"01KJN9P6BR2ZGAB6212R80E762"},"output":{"assignees":[],"attachments":[],"comments":[],"depends_on":[],"description":"**Part 2a of the YAML/MD storage conversion plan.**\n\nConvert task files from `{id}.json` to `{id}.md` with YAML frontmatter for metadata and markdown body for description.\n\n**Add serde_yaml dependency:** Add `serde_yaml = { workspace = true }` to `swissarmyhammer-kanban/Cargo.toml` (already in workspace at v0.9).\n\n**TaskMeta helper struct:** Create a struct with all Task fields except `description` and `id`, used only for YAML frontmatter serialization. Include legacy field handling (`_legacy_tags`, `_legacy_subtasks`).\n\n**context.rs changes:**\n- `task_path()` → return `tasks/{id}.md` instead of `.json`\n- `write_task()` → serialize TaskMeta as YAML frontmatter, append description as markdown body\n- `read_task()` → try `.md` first, fall back to `.json` for backward compat\n- `list_task_ids()` → accept both `.md` and `.json` extensions\n- Add `parse_task_markdown()` helper to split frontmatter from body\n\n**Example output format:**\n```\n---\ntitle: Fix the login bug\nposition:\n  column: doing\n  ordinal: a0\n---\nDescription text here with #tags\n```\n\n**Files:**\n- `swissarmyhammer-kanban/Cargo.toml` (add serde_yaml)\n- `swissarmyhammer-kanban/src/context.rs` (task_path, read/write/list)\n- `swissarmyhammer-kanban/src/types/task.rs` (TaskMeta struct if needed)\n\n- [ ] Add serde_yaml dependency to Cargo.toml\n- [ ] Create TaskMeta helper struct for frontmatter serialization\n- [ ] Update task_path() to return .md extension\n- [ ] Implement write_task() with YAML frontmatter + markdown body\n- [ ] Implement parse_task_markdown() helper\n- [ ] Update read_task() with .md/.json fallback\n- [ ] Update list_task_ids() to accept both extensions\n- [ ] Run `cargo nextest run -p swissarmyhammer-kanban`","id":"01KJN9P6BR2ZGAB6212R80E762","position":{"column":"done","ordinal":"c1"},"tags":[],"title":"Convert task storage from JSON to YAML frontmatter + markdown"},"duration_ms":4}

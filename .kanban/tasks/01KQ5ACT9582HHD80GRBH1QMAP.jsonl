{"id":"01KQ5ACT96VX9F2YZ5ZJ7A9RPC","timestamp":"2026-04-26T16:36:36.774096Z","op":"create","item_id":"01KQ5ACT9582HHD80GRBH1QMAP","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,64 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KQ56YQPD6W973XZFGG0GMYWJ\n+position_column: todo\n+position_ordinal: ff9d80\n+project: spatial-nav\n+title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n+---\n+## What\n+\n+`<FocusScope kind=\"zone\">` mounts an inner `FocusScopeBody` chrome div with no `className` (default block-display). This inner div sits between the outer `<FocusZone>` (where consumers' `className` lands) and the children, breaking the flex chain.\n+\n+Three call sites have now had to work around this individually with the same outer-flex + inner-flex pattern:\n+\n+1. `kanban-app/ui/src/components/entity-card.tsx` — outer flex on FocusScope, inner flex wrapper on card body\n+2. `kanban-app/ui/src/components/entity-inspector.tsx` — outer flex on FocusScope, inner `<div className=\"flex items-start gap-2 w-full\">` wrapper around icon + content\n+3. `kanban-app/ui/src/components/column-view.tsx` — outer FocusScope `position: relative`, inner `<div className=\"absolute inset-0 flex flex-col min-w-0\">` (escape via absolute positioning)\n+\n+This is duplication of the same root-cause workaround. The next call site that needs flex layout inside a `<FocusScope kind=\"zone\">` will hit the same trap.\n+\n+## Fix\n+\n+Modify `FocusScopeBody` in `kanban-app/ui/src/components/focus-scope.tsx` so it does not break the flex chain by default. Options:\n+\n+1. **Pass through className**: `FocusScope` accepts a `bodyClassName` (or just lets `className` flow to the body), and call sites set their flex classes there.\n+2. **Inherit display via `display: contents`**: `FocusScopeBody` sets `display: contents` so it disappears from the layout tree entirely. Children become direct flex items of the parent FocusZone. Lose the ability to attach event handlers / scrollIntoView — verify that's acceptable or work around.\n+3. **Make FocusScopeBody inherit flex**: `FocusScopeBody` defaults to `flex` direction matching its parent (or accepts a prop). Less elegant; couples body to layout.\n+\n+Recommended: **option 1** (className passthrough) plus a default of `flex flex-col` if the outer FocusZone has flex applied. Lets call sites declare layout in one place.\n+\n+After the fix, simplify the three workaround call sites to use the new contract.\n+\n+## Files\n+\n+- `kanban-app/ui/src/components/focus-scope.tsx` — `FocusScopeBody` and the `<FocusScope>` composite\n+- `kanban-app/ui/src/components/entity-card.tsx` — simplify\n+- `kanban-app/ui/src/components/entity-inspector.tsx` — simplify\n+- `kanban-app/ui/src/components/column-view.tsx` — simplify\n+\n+## Acceptance Criteria\n+\n+- [ ] `FocusScopeBody` no longer breaks the flex chain by default\n+- [ ] Three current workaround call sites simplified — single layer of flex classes, not double\n+- [ ] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n+- [ ] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n+- [ ] `pnpm vitest run` passes (existing tests should reveal any layout regressions if the patch is wrong)\n+- [ ] Add a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct flex items (computed style)\n+\n+## Tests\n+\n+- [ ] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children horizontally inside the spatial stack\n+- [ ] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n+\n+## Workflow\n+\n+- Use `/tdd` — write the failing layout test against the spatial stack first, then fix `FocusScopeBody`, then simplify the three call sites.\n+\n+## Origin\n+\n+Surfaced during review of `01KQ56YQPD6W973XZFGG0GMYWJ` (Inspector layout regression). Reviewer noted the same workaround appears in three places and recommended fixing the root cause.\n+\n+This card is \"fix the duplication\"; the original regression cards (`01KQ56YQPD6W973XZFGG0GMYWJ`, `01KQ4YF38NJ0BN6FM5EVDVRH1N`, plus the entity-card fix in `01KQ20NMRQQSXVRHP4RHE56B0K`) restored functionality at three different layers — this card folds them back to a single fix at the FocusScope layer.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,64 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KQ56YQPD6W973XZFGG0GMYWJ\n-position_column: todo\n-position_ordinal: ff9d80\n-project: spatial-nav\n-title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n----\n-## What\n-\n-`<FocusScope kind=\"zone\">` mounts an inner `FocusScopeBody` chrome div with no `className` (default block-display). This inner div sits between the outer `<FocusZone>` (where consumers' `className` lands) and the children, breaking the flex chain.\n-\n-Three call sites have now had to work around this individually with the same outer-flex + inner-flex pattern:\n-\n-1. `kanban-app/ui/src/components/entity-card.tsx` — outer flex on FocusScope, inner flex wrapper on card body\n-2. `kanban-app/ui/src/components/entity-inspector.tsx` — outer flex on FocusScope, inner `<div className=\"flex items-start gap-2 w-full\">` wrapper around icon + content\n-3. `kanban-app/ui/src/components/column-view.tsx` — outer FocusScope `position: relative`, inner `<div className=\"absolute inset-0 flex flex-col min-w-0\">` (escape via absolute positioning)\n-\n-This is duplication of the same root-cause workaround. The next call site that needs flex layout inside a `<FocusScope kind=\"zone\">` will hit the same trap.\n-\n-## Fix\n-\n-Modify `FocusScopeBody` in `kanban-app/ui/src/components/focus-scope.tsx` so it does not break the flex chain by default. Options:\n-\n-1. **Pass through className**: `FocusScope` accepts a `bodyClassName` (or just lets `className` flow to the body), and call sites set their flex classes there.\n-2. **Inherit display via `display: contents`**: `FocusScopeBody` sets `display: contents` so it disappears from the layout tree entirely. Children become direct flex items of the parent FocusZone. Lose the ability to attach event handlers / scrollIntoView — verify that's acceptable or work around.\n-3. **Make FocusScopeBody inherit flex**: `FocusScopeBody` defaults to `flex` direction matching its parent (or accepts a prop). Less elegant; couples body to layout.\n-\n-Recommended: **option 1** (className passthrough) plus a default of `flex flex-col` if the outer FocusZone has flex applied. Lets call sites declare layout in one place.\n-\n-After the fix, simplify the three workaround call sites to use the new contract.\n-\n-## Files\n-\n-- `kanban-app/ui/src/components/focus-scope.tsx` — `FocusScopeBody` and the `<FocusScope>` composite\n-- `kanban-app/ui/src/components/entity-card.tsx` — simplify\n-- `kanban-app/ui/src/components/entity-inspector.tsx` — simplify\n-- `kanban-app/ui/src/components/column-view.tsx` — simplify\n-\n-## Acceptance Criteria\n-\n-- [ ] `FocusScopeBody` no longer breaks the flex chain by default\n-- [ ] Three current workaround call sites simplified — single layer of flex classes, not double\n-- [ ] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n-- [ ] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n-- [ ] `pnpm vitest run` passes (existing tests should reveal any layout regressions if the patch is wrong)\n-- [ ] Add a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct flex items (computed style)\n-\n-## Tests\n-\n-- [ ] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children horizontally inside the spatial stack\n-- [ ] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n-\n-## Workflow\n-\n-- Use `/tdd` — write the failing layout test against the spatial stack first, then fix `FocusScopeBody`, then simplify the three call sites.\n-\n-## Origin\n-\n-Surfaced during review of `01KQ56YQPD6W973XZFGG0GMYWJ` (Inspector layout regression). Reviewer noted the same workaround appears in three places and recommended fixing the root cause.\n-\n-This card is \"fix the duplication\"; the original regression cards (`01KQ56YQPD6W973XZFGG0GMYWJ`, `01KQ4YF38NJ0BN6FM5EVDVRH1N`, plus the entity-card fix in `01KQ20NMRQQSXVRHP4RHE56B0K`) restored functionality at three different layers — this card folds them back to a single fix at the FocusScope layer.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ5ACT97VX0XQCE78D878ZNS","timestamp":"2026-04-26T16:36:36.775888Z","entity_type":"task","entity_id":"01KQ5ACT9582HHD80GRBH1QMAP","op":"create","changes":[["position_ordinal",{"kind":"set","value":"ff9d80"}],["title",{"kind":"set","value":"Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)"}],["position_column",{"kind":"set","value":"todo"}],["body",{"kind":"set","value":"## What\n\n`<FocusScope kind=\"zone\">` mounts an inner `FocusScopeBody` chrome div with no `className` (default block-display). This inner div sits between the outer `<FocusZone>` (where consumers' `className` lands) and the children, breaking the flex chain.\n\nThree call sites have now had to work around this individually with the same outer-flex + inner-flex pattern:\n\n1. `kanban-app/ui/src/components/entity-card.tsx` — outer flex on FocusScope, inner flex wrapper on card body\n2. `kanban-app/ui/src/components/entity-inspector.tsx` — outer flex on FocusScope, inner `<div className=\"flex items-start gap-2 w-full\">` wrapper around icon + content\n3. `kanban-app/ui/src/components/column-view.tsx` — outer FocusScope `position: relative`, inner `<div className=\"absolute inset-0 flex flex-col min-w-0\">` (escape via absolute positioning)\n\nThis is duplication of the same root-cause workaround. The next call site that needs flex layout inside a `<FocusScope kind=\"zone\">` will hit the same trap.\n\n## Fix\n\nModify `FocusScopeBody` in `kanban-app/ui/src/components/focus-scope.tsx` so it does not break the flex chain by default. Options:\n\n1. **Pass through className**: `FocusScope` accepts a `bodyClassName` (or just lets `className` flow to the body), and call sites set their flex classes there.\n2. **Inherit display via `display: contents`**: `FocusScopeBody` sets `display: contents` so it disappears from the layout tree entirely. Children become direct flex items of the parent FocusZone. Lose the ability to attach event handlers / scrollIntoView — verify that's acceptable or work around.\n3. **Make FocusScopeBody inherit flex**: `FocusScopeBody` defaults to `flex` direction matching its parent (or accepts a prop). Less elegant; couples body to layout.\n\nRecommended: **option 1** (className passthrough) plus a default of `flex flex-col` if the outer FocusZone has flex applied. Lets call sites declare layout in one place.\n\nAfter the fix, simplify the three workaround call sites to use the new contract.\n\n## Files\n\n- `kanban-app/ui/src/components/focus-scope.tsx` — `FocusScopeBody` and the `<FocusScope>` composite\n- `kanban-app/ui/src/components/entity-card.tsx` — simplify\n- `kanban-app/ui/src/components/entity-inspector.tsx` — simplify\n- `kanban-app/ui/src/components/column-view.tsx` — simplify\n\n## Acceptance Criteria\n\n- [ ] `FocusScopeBody` no longer breaks the flex chain by default\n- [ ] Three current workaround call sites simplified — single layer of flex classes, not double\n- [ ] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n- [ ] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n- [ ] `pnpm vitest run` passes (existing tests should reveal any layout regressions if the patch is wrong)\n- [ ] Add a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct flex items (computed style)\n\n## Tests\n\n- [ ] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children horizontally inside the spatial stack\n- [ ] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n\n## Workflow\n\n- Use `/tdd` — write the failing layout test against the spatial stack first, then fix `FocusScopeBody`, then simplify the three call sites.\n\n## Origin\n\nSurfaced during review of `01KQ56YQPD6W973XZFGG0GMYWJ` (Inspector layout regression). Reviewer noted the same workaround appears in three places and recommended fixing the root cause.\n\nThis card is \"fix the duplication\"; the original regression cards (`01KQ56YQPD6W973XZFGG0GMYWJ`, `01KQ4YF38NJ0BN6FM5EVDVRH1N`, plus the entity-card fix in `01KQ20NMRQQSXVRHP4RHE56B0K`) restored functionality at three different layers — this card folds them back to a single fix at the FocusScope layer."}],["assignees",{"kind":"set","value":["claude-code"]}],["depends_on",{"kind":"set","value":["01KQ56YQPD6W973XZFGG0GMYWJ"]}],["project",{"kind":"set","value":"spatial-nav"}]]}
{"id":"01KQ5B08DPTZCZNXST49C716H6","timestamp":"2026-04-26T16:47:13.846605Z","op":"update","item_id":"01KQ5ACT9582HHD80GRBH1QMAP","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ56YQPD6W973XZFGG0GMYWJ\n-position_column: todo\n-position_ordinal: ff9d80\n+position_column: doing\n+position_ordinal: '8280'\n project: spatial-nav\n title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ56YQPD6W973XZFGG0GMYWJ\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: todo\n+position_ordinal: ff9d80\n project: spatial-nav\n title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n ---\n","transaction_id":null}
{"id":"01KQ5B08DQRKWP15AKWDPDR1T1","timestamp":"2026-04-26T16:47:13.847343Z","entity_type":"task","entity_id":"01KQ5ACT9582HHD80GRBH1QMAP","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ff9d80\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+ff9d80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5BX3P78JG86G41VJFGP2D6","timestamp":"2026-04-26T17:02:59.271620Z","op":"update","item_id":"01KQ5ACT9582HHD80GRBH1QMAP","forward_patch":"--- original\n+++ modified\n@@ -20,38 +20,40 @@\n\n This is duplication of the same root-cause workaround. The next call site that needs flex layout inside a `<FocusScope kind=\"zone\">` will hit the same trap.\n\n-## Fix\n+## Fix (implemented)\n\n-Modify `FocusScopeBody` in `kanban-app/ui/src/components/focus-scope.tsx` so it does not break the flex chain by default. Options:\n+Eliminated `FocusScopeBody` entirely. The chrome (right-click context menu, double-click → inspect, scrollIntoView) is now attached directly to the spatial primitive (`<Focusable>` / `<FocusZone>`). To make `scrollIntoView` reach the primitive's DOM node, both primitives now accept an optional `ref` prop that merges with their internal ResizeObserver ref. The consumer's `className` lands on the same element whose children are direct layout children — no intervening wrapper, no broken flex chain.\n\n-1. **Pass through className**: `FocusScope` accepts a `bodyClassName` (or just lets `className` flow to the body), and call sites set their flex classes there.\n-2. **Inherit display via `display: contents`**: `FocusScopeBody` sets `display: contents` so it disappears from the layout tree entirely. Children become direct flex items of the parent FocusZone. Lose the ability to attach event handlers / scrollIntoView — verify that's acceptable or work around.\n-3. **Make FocusScopeBody inherit flex**: `FocusScopeBody` defaults to `flex` direction matching its parent (or accepts a prop). Less elegant; couples body to layout.\n+After the fix, the call-site workarounds were simplified:\n+- `entity-inspector.tsx`: removed the inner `<div className=\"flex items-start gap-2 w-full\">` wrapper; icon and content are direct children of the FocusScope's outer primitive.\n+- `column-view.tsx`: removed the `relative` + `absolute inset-0 flex flex-col` workaround; folded `flex flex-col` into the FocusScope's `className` directly.\n+- `entity-card.tsx`: already had a clean consumer-managed structure (its own card div for shape + flex). No change needed; verified unchanged behaviour through tests.\n\n-Recommended: **option 1** (className passthrough) plus a default of `flex flex-col` if the outer FocusZone has flex applied. Lets call sites declare layout in one place.\n-\n-After the fix, simplify the three workaround call sites to use the new contract.\n-\n ## Files\n\n-- `kanban-app/ui/src/components/focus-scope.tsx` — `FocusScopeBody` and the `<FocusScope>` composite\n-- `kanban-app/ui/src/components/entity-card.tsx` — simplify\n-- `kanban-app/ui/src/components/entity-inspector.tsx` — simplify\n-- `kanban-app/ui/src/components/column-view.tsx` — simplify\n+- `kanban-app/ui/src/components/focus-scope.tsx` — removed `FocusScopeBody`; chrome now composes onto the primitive directly\n+- `kanban-app/ui/src/components/focus-zone.tsx` — added optional `ref` prop\n+- `kanban-app/ui/src/components/focusable.tsx` — added optional `ref` prop\n+- `kanban-app/ui/src/components/entity-inspector.tsx` — simplified\n+- `kanban-app/ui/src/components/column-view.tsx` — simplified\n+- `kanban-app/ui/src/components/data-table.tsx` — updated stale comment\n+- `kanban-app/ui/src/components/focus-scope.test.tsx` — added regression test\n+- `kanban-app/ui/src/components/column-view.test.tsx` — updated regression-guard test for new contract\n+- `kanban-app/ui/src/components/entity-inspector.test.tsx` — updated stale comment\n\n ## Acceptance Criteria\n\n-- [ ] `FocusScopeBody` no longer breaks the flex chain by default\n-- [ ] Three current workaround call sites simplified — single layer of flex classes, not double\n-- [ ] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n-- [ ] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n-- [ ] `pnpm vitest run` passes (existing tests should reveal any layout regressions if the patch is wrong)\n-- [ ] Add a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct flex items (computed style)\n+- [x] `FocusScopeBody` no longer breaks the flex chain by default\n+- [x] Three current workaround call sites simplified — single layer of flex classes, not double\n+- [x] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n+- [x] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n+- [x] `pnpm vitest run` passes (1564/1564 tests pass, 143/143 files pass)\n+- [x] Added a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct children of the zone div (no inner wrapper)\n\n ## Tests\n\n-- [ ] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children horizontally inside the spatial stack\n-- [ ] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n+- [x] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children as direct children of the spatial-zone div (regression test added)\n+- [x] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -20,40 +20,38 @@\n\n This is duplication of the same root-cause workaround. The next call site that needs flex layout inside a `<FocusScope kind=\"zone\">` will hit the same trap.\n\n-## Fix (implemented)\n+## Fix\n\n-Eliminated `FocusScopeBody` entirely. The chrome (right-click context menu, double-click → inspect, scrollIntoView) is now attached directly to the spatial primitive (`<Focusable>` / `<FocusZone>`). To make `scrollIntoView` reach the primitive's DOM node, both primitives now accept an optional `ref` prop that merges with their internal ResizeObserver ref. The consumer's `className` lands on the same element whose children are direct layout children — no intervening wrapper, no broken flex chain.\n+Modify `FocusScopeBody` in `kanban-app/ui/src/components/focus-scope.tsx` so it does not break the flex chain by default. Options:\n\n-After the fix, the call-site workarounds were simplified:\n-- `entity-inspector.tsx`: removed the inner `<div className=\"flex items-start gap-2 w-full\">` wrapper; icon and content are direct children of the FocusScope's outer primitive.\n-- `column-view.tsx`: removed the `relative` + `absolute inset-0 flex flex-col` workaround; folded `flex flex-col` into the FocusScope's `className` directly.\n-- `entity-card.tsx`: already had a clean consumer-managed structure (its own card div for shape + flex). No change needed; verified unchanged behaviour through tests.\n+1. **Pass through className**: `FocusScope` accepts a `bodyClassName` (or just lets `className` flow to the body), and call sites set their flex classes there.\n+2. **Inherit display via `display: contents`**: `FocusScopeBody` sets `display: contents` so it disappears from the layout tree entirely. Children become direct flex items of the parent FocusZone. Lose the ability to attach event handlers / scrollIntoView — verify that's acceptable or work around.\n+3. **Make FocusScopeBody inherit flex**: `FocusScopeBody` defaults to `flex` direction matching its parent (or accepts a prop). Less elegant; couples body to layout.\n+\n+Recommended: **option 1** (className passthrough) plus a default of `flex flex-col` if the outer FocusZone has flex applied. Lets call sites declare layout in one place.\n+\n+After the fix, simplify the three workaround call sites to use the new contract.\n\n ## Files\n\n-- `kanban-app/ui/src/components/focus-scope.tsx` — removed `FocusScopeBody`; chrome now composes onto the primitive directly\n-- `kanban-app/ui/src/components/focus-zone.tsx` — added optional `ref` prop\n-- `kanban-app/ui/src/components/focusable.tsx` — added optional `ref` prop\n-- `kanban-app/ui/src/components/entity-inspector.tsx` — simplified\n-- `kanban-app/ui/src/components/column-view.tsx` — simplified\n-- `kanban-app/ui/src/components/data-table.tsx` — updated stale comment\n-- `kanban-app/ui/src/components/focus-scope.test.tsx` — added regression test\n-- `kanban-app/ui/src/components/column-view.test.tsx` — updated regression-guard test for new contract\n-- `kanban-app/ui/src/components/entity-inspector.test.tsx` — updated stale comment\n+- `kanban-app/ui/src/components/focus-scope.tsx` — `FocusScopeBody` and the `<FocusScope>` composite\n+- `kanban-app/ui/src/components/entity-card.tsx` — simplify\n+- `kanban-app/ui/src/components/entity-inspector.tsx` — simplify\n+- `kanban-app/ui/src/components/column-view.tsx` — simplify\n\n ## Acceptance Criteria\n\n-- [x] `FocusScopeBody` no longer breaks the flex chain by default\n-- [x] Three current workaround call sites simplified — single layer of flex classes, not double\n-- [x] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n-- [x] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n-- [x] `pnpm vitest run` passes (1564/1564 tests pass, 143/143 files pass)\n-- [x] Added a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct children of the zone div (no inner wrapper)\n+- [ ] `FocusScopeBody` no longer breaks the flex chain by default\n+- [ ] Three current workaround call sites simplified — single layer of flex classes, not double\n+- [ ] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n+- [ ] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n+- [ ] `pnpm vitest run` passes (existing tests should reveal any layout regressions if the patch is wrong)\n+- [ ] Add a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct flex items (computed style)\n\n ## Tests\n\n-- [x] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children as direct children of the spatial-zone div (regression test added)\n-- [x] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n+- [ ] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children horizontally inside the spatial stack\n+- [ ] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n\n ## Workflow\n\n","transaction_id":null}
{"id":"01KQ5BX3P82RTG9MGTPPWRGZ5F","timestamp":"2026-04-26T17:02:59.272182Z","entity_type":"task","entity_id":"01KQ5ACT9582HHD80GRBH1QMAP","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -10,38 +10,40 @@\n\n This is duplication of the same root-cause workaround. The next call site that needs flex layout inside a `<FocusScope kind=\"zone\">` will hit the same trap.\n\n-## Fix\n+## Fix (implemented)\n\n-Modify `FocusScopeBody` in `kanban-app/ui/src/components/focus-scope.tsx` so it does not break the flex chain by default. Options:\n+Eliminated `FocusScopeBody` entirely. The chrome (right-click context menu, double-click → inspect, scrollIntoView) is now attached directly to the spatial primitive (`<Focusable>` / `<FocusZone>`). To make `scrollIntoView` reach the primitive's DOM node, both primitives now accept an optional `ref` prop that merges with their internal ResizeObserver ref. The consumer's `className` lands on the same element whose children are direct layout children — no intervening wrapper, no broken flex chain.\n\n-1. **Pass through className**: `FocusScope` accepts a `bodyClassName` (or just lets `className` flow to the body), and call sites set their flex classes there.\n-2. **Inherit display via `display: contents`**: `FocusScopeBody` sets `display: contents` so it disappears from the layout tree entirely. Children become direct flex items of the parent FocusZone. Lose the ability to attach event handlers / scrollIntoView — verify that's acceptable or work around.\n-3. **Make FocusScopeBody inherit flex**: `FocusScopeBody` defaults to `flex` direction matching its parent (or accepts a prop). Less elegant; couples body to layout.\n+After the fix, the call-site workarounds were simplified:\n+- `entity-inspector.tsx`: removed the inner `<div className=\"flex items-start gap-2 w-full\">` wrapper; icon and content are direct children of the FocusScope's outer primitive.\n+- `column-view.tsx`: removed the `relative` + `absolute inset-0 flex flex-col` workaround; folded `flex flex-col` into the FocusScope's `className` directly.\n+- `entity-card.tsx`: already had a clean consumer-managed structure (its own card div for shape + flex). No change needed; verified unchanged behaviour through tests.\n\n-Recommended: **option 1** (className passthrough) plus a default of `flex flex-col` if the outer FocusZone has flex applied. Lets call sites declare layout in one place.\n-\n-After the fix, simplify the three workaround call sites to use the new contract.\n-\n ## Files\n\n-- `kanban-app/ui/src/components/focus-scope.tsx` — `FocusScopeBody` and the `<FocusScope>` composite\n-- `kanban-app/ui/src/components/entity-card.tsx` — simplify\n-- `kanban-app/ui/src/components/entity-inspector.tsx` — simplify\n-- `kanban-app/ui/src/components/column-view.tsx` — simplify\n+- `kanban-app/ui/src/components/focus-scope.tsx` — removed `FocusScopeBody`; chrome now composes onto the primitive directly\n+- `kanban-app/ui/src/components/focus-zone.tsx` — added optional `ref` prop\n+- `kanban-app/ui/src/components/focusable.tsx` — added optional `ref` prop\n+- `kanban-app/ui/src/components/entity-inspector.tsx` — simplified\n+- `kanban-app/ui/src/components/column-view.tsx` — simplified\n+- `kanban-app/ui/src/components/data-table.tsx` — updated stale comment\n+- `kanban-app/ui/src/components/focus-scope.test.tsx` — added regression test\n+- `kanban-app/ui/src/components/column-view.test.tsx` — updated regression-guard test for new contract\n+- `kanban-app/ui/src/components/entity-inspector.test.tsx` — updated stale comment\n\n ## Acceptance Criteria\n\n-- [ ] `FocusScopeBody` no longer breaks the flex chain by default\n-- [ ] Three current workaround call sites simplified — single layer of flex classes, not double\n-- [ ] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n-- [ ] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n-- [ ] `pnpm vitest run` passes (existing tests should reveal any layout regressions if the patch is wrong)\n-- [ ] Add a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct flex items (computed style)\n+- [x] `FocusScopeBody` no longer breaks the flex chain by default\n+- [x] Three current workaround call sites simplified — single layer of flex classes, not double\n+- [x] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n+- [x] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n+- [x] `pnpm vitest run` passes (1564/1564 tests pass, 143/143 files pass)\n+- [x] Added a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct children of the zone div (no inner wrapper)\n\n ## Tests\n\n-- [ ] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children horizontally inside the spatial stack\n-- [ ] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n+- [x] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children as direct children of the spatial-zone div (regression test added)\n+- [x] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -10,40 +10,38 @@\n\n This is duplication of the same root-cause workaround. The next call site that needs flex layout inside a `<FocusScope kind=\"zone\">` will hit the same trap.\n\n-## Fix (implemented)\n+## Fix\n\n-Eliminated `FocusScopeBody` entirely. The chrome (right-click context menu, double-click → inspect, scrollIntoView) is now attached directly to the spatial primitive (`<Focusable>` / `<FocusZone>`). To make `scrollIntoView` reach the primitive's DOM node, both primitives now accept an optional `ref` prop that merges with their internal ResizeObserver ref. The consumer's `className` lands on the same element whose children are direct layout children — no intervening wrapper, no broken flex chain.\n+Modify `FocusScopeBody` in `kanban-app/ui/src/components/focus-scope.tsx` so it does not break the flex chain by default. Options:\n\n-After the fix, the call-site workarounds were simplified:\n-- `entity-inspector.tsx`: removed the inner `<div className=\"flex items-start gap-2 w-full\">` wrapper; icon and content are direct children of the FocusScope's outer primitive.\n-- `column-view.tsx`: removed the `relative` + `absolute inset-0 flex flex-col` workaround; folded `flex flex-col` into the FocusScope's `className` directly.\n-- `entity-card.tsx`: already had a clean consumer-managed structure (its own card div for shape + flex). No change needed; verified unchanged behaviour through tests.\n+1. **Pass through className**: `FocusScope` accepts a `bodyClassName` (or just lets `className` flow to the body), and call sites set their flex classes there.\n+2. **Inherit display via `display: contents`**: `FocusScopeBody` sets `display: contents` so it disappears from the layout tree entirely. Children become direct flex items of the parent FocusZone. Lose the ability to attach event handlers / scrollIntoView — verify that's acceptable or work around.\n+3. **Make FocusScopeBody inherit flex**: `FocusScopeBody` defaults to `flex` direction matching its parent (or accepts a prop). Less elegant; couples body to layout.\n+\n+Recommended: **option 1** (className passthrough) plus a default of `flex flex-col` if the outer FocusZone has flex applied. Lets call sites declare layout in one place.\n+\n+After the fix, simplify the three workaround call sites to use the new contract.\n\n ## Files\n\n-- `kanban-app/ui/src/components/focus-scope.tsx` — removed `FocusScopeBody`; chrome now composes onto the primitive directly\n-- `kanban-app/ui/src/components/focus-zone.tsx` — added optional `ref` prop\n-- `kanban-app/ui/src/components/focusable.tsx` — added optional `ref` prop\n-- `kanban-app/ui/src/components/entity-inspector.tsx` — simplified\n-- `kanban-app/ui/src/components/column-view.tsx` — simplified\n-- `kanban-app/ui/src/components/data-table.tsx` — updated stale comment\n-- `kanban-app/ui/src/components/focus-scope.test.tsx` — added regression test\n-- `kanban-app/ui/src/components/column-view.test.tsx` — updated regression-guard test for new contract\n-- `kanban-app/ui/src/components/entity-inspector.test.tsx` — updated stale comment\n+- `kanban-app/ui/src/components/focus-scope.tsx` — `FocusScopeBody` and the `<FocusScope>` composite\n+- `kanban-app/ui/src/components/entity-card.tsx` — simplify\n+- `kanban-app/ui/src/components/entity-inspector.tsx` — simplify\n+- `kanban-app/ui/src/components/column-view.tsx` — simplify\n\n ## Acceptance Criteria\n\n-- [x] `FocusScopeBody` no longer breaks the flex chain by default\n-- [x] Three current workaround call sites simplified — single layer of flex classes, not double\n-- [x] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n-- [x] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n-- [x] `pnpm vitest run` passes (1564/1564 tests pass, 143/143 files pass)\n-- [x] Added a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct children of the zone div (no inner wrapper)\n+- [ ] `FocusScopeBody` no longer breaks the flex chain by default\n+- [ ] Three current workaround call sites simplified — single layer of flex classes, not double\n+- [ ] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n+- [ ] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n+- [ ] `pnpm vitest run` passes (existing tests should reveal any layout regressions if the patch is wrong)\n+- [ ] Add a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct flex items (computed style)\n\n ## Tests\n\n-- [x] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children as direct children of the spatial-zone div (regression test added)\n-- [x] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n+- [ ] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children horizontally inside the spatial stack\n+- [ ] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n\n ## Workflow\n\n"}]]}
{"id":"01KQ5BXKSWFHT8D9TGT34MXAV9","timestamp":"2026-04-26T17:03:15.772327Z","op":"update","item_id":"01KQ5ACT9582HHD80GRBH1QMAP","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KQ56YQPD6W973XZFGG0GMYWJ\n-position_column: doing\n+position_column: review\n position_ordinal: '8280'\n project: spatial-nav\n title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KQ56YQPD6W973XZFGG0GMYWJ\n-position_column: review\n+position_column: doing\n position_ordinal: '8280'\n project: spatial-nav\n title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n","transaction_id":null}
{"id":"01KQ5BXKSX19VJKW8G61RR58DW","timestamp":"2026-04-26T17:03:15.773138Z","entity_type":"task","entity_id":"01KQ5ACT9582HHD80GRBH1QMAP","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5DZSQ9QCQWQ69NP9KHCY73","timestamp":"2026-04-26T17:39:24.521885Z","op":"update","item_id":"01KQ5ACT9582HHD80GRBH1QMAP","forward_patch":"--- original\n+++ modified\n@@ -63,4 +63,15 @@\n\n Surfaced during review of `01KQ56YQPD6W973XZFGG0GMYWJ` (Inspector layout regression). Reviewer noted the same workaround appears in three places and recommended fixing the root cause.\n\n-This card is \"fix the duplication\"; the original regression cards (`01KQ56YQPD6W973XZFGG0GMYWJ`, `01KQ4YF38NJ0BN6FM5EVDVRH1N`, plus the entity-card fix in `01KQ20NMRQQSXVRHP4RHE56B0K`) restored functionality at three different layers — this card folds them back to a single fix at the FocusScope layer.\n\\ No newline at end of file\n+This card is \"fix the duplication\"; the original regression cards (`01KQ56YQPD6W973XZFGG0GMYWJ`, `01KQ4YF38NJ0BN6FM5EVDVRH1N`, plus the entity-card fix in `01KQ20NMRQQSXVRHP4RHE56B0K`) restored functionality at three different layers — this card folds them back to a single fix at the FocusScope layer.\n+\n+## Review Findings (2026-04-26 12:40)\n+\n+Implementation is correct: 1564/1564 vitest pass, cargo + clippy clean, tsc clean. The regression test pins the no-inner-wrapper contract for `kind=\"zone\"` FocusScopes. The architectural decision (route chrome onto the primitive directly) is sound — fewer DOM nodes and the consumer's `className` lands where layout actually applies.\n+\n+### Nits\n+\n+- [ ] `kanban-app/ui/src/components/focus-scope.tsx:167` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\".\n+- [ ] `kanban-app/ui/src/components/entity-inspector.test.tsx:296-310` — `renderInspectorWithSpatial` docstring says the helper exists because the no-spatial-context fallback \"hides the layout regression… the icon + content stack vertically only when `FocusScopeBody` (a className-less `<div>`) sits between the FocusZone primitive and the children.\" `FocusScopeBody` is gone, so the rationale is wrong. The helper is still useful (it covers the spatial primitive path that the fallback doesn't exercise), but the docstring should be rewritten to state that purpose without referencing the removed wrapper.\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense.\n+- [ ] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — The new optional `ref` prop on the primitives has no isolated unit test. The merged callback-ref path (write internal `.current` AND forward to the external `Ref<HTMLDivElement>`, supporting both function refs and `RefObject` refs) is exercised end-to-end via `<FocusScope>`'s scrollIntoView path, but a direct test on each primitive would pin the contract so a future refactor of the primitive can't silently break ref forwarding.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -63,15 +63,4 @@\n\n Surfaced during review of `01KQ56YQPD6W973XZFGG0GMYWJ` (Inspector layout regression). Reviewer noted the same workaround appears in three places and recommended fixing the root cause.\n\n-This card is \"fix the duplication\"; the original regression cards (`01KQ56YQPD6W973XZFGG0GMYWJ`, `01KQ4YF38NJ0BN6FM5EVDVRH1N`, plus the entity-card fix in `01KQ20NMRQQSXVRHP4RHE56B0K`) restored functionality at three different layers — this card folds them back to a single fix at the FocusScope layer.\n-\n-## Review Findings (2026-04-26 12:40)\n-\n-Implementation is correct: 1564/1564 vitest pass, cargo + clippy clean, tsc clean. The regression test pins the no-inner-wrapper contract for `kind=\"zone\"` FocusScopes. The architectural decision (route chrome onto the primitive directly) is sound — fewer DOM nodes and the consumer's `className` lands where layout actually applies.\n-\n-### Nits\n-\n-- [ ] `kanban-app/ui/src/components/focus-scope.tsx:167` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\".\n-- [ ] `kanban-app/ui/src/components/entity-inspector.test.tsx:296-310` — `renderInspectorWithSpatial` docstring says the helper exists because the no-spatial-context fallback \"hides the layout regression… the icon + content stack vertically only when `FocusScopeBody` (a className-less `<div>`) sits between the FocusZone primitive and the children.\" `FocusScopeBody` is gone, so the rationale is wrong. The helper is still useful (it covers the spatial primitive path that the fallback doesn't exercise), but the docstring should be rewritten to state that purpose without referencing the removed wrapper.\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense.\n-- [ ] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — The new optional `ref` prop on the primitives has no isolated unit test. The merged callback-ref path (write internal `.current` AND forward to the external `Ref<HTMLDivElement>`, supporting both function refs and `RefObject` refs) is exercised end-to-end via `<FocusScope>`'s scrollIntoView path, but a direct test on each primitive would pin the contract so a future refactor of the primitive can't silently break ref forwarding.\n\\ No newline at end of file\n+This card is \"fix the duplication\"; the original regression cards (`01KQ56YQPD6W973XZFGG0GMYWJ`, `01KQ4YF38NJ0BN6FM5EVDVRH1N`, plus the entity-card fix in `01KQ20NMRQQSXVRHP4RHE56B0K`) restored functionality at three different layers — this card folds them back to a single fix at the FocusScope layer.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ5DZSQA48KVJ500KNZ1WC7M","timestamp":"2026-04-26T17:39:24.522758Z","entity_type":"task","entity_id":"01KQ5ACT9582HHD80GRBH1QMAP","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -53,4 +53,15 @@\n\n Surfaced during review of `01KQ56YQPD6W973XZFGG0GMYWJ` (Inspector layout regression). Reviewer noted the same workaround appears in three places and recommended fixing the root cause.\n\n-This card is \"fix the duplication\"; the original regression cards (`01KQ56YQPD6W973XZFGG0GMYWJ`, `01KQ4YF38NJ0BN6FM5EVDVRH1N`, plus the entity-card fix in `01KQ20NMRQQSXVRHP4RHE56B0K`) restored functionality at three different layers — this card folds them back to a single fix at the FocusScope layer.\n\\ No newline at end of file\n+This card is \"fix the duplication\"; the original regression cards (`01KQ56YQPD6W973XZFGG0GMYWJ`, `01KQ4YF38NJ0BN6FM5EVDVRH1N`, plus the entity-card fix in `01KQ20NMRQQSXVRHP4RHE56B0K`) restored functionality at three different layers — this card folds them back to a single fix at the FocusScope layer.\n+\n+## Review Findings (2026-04-26 12:40)\n+\n+Implementation is correct: 1564/1564 vitest pass, cargo + clippy clean, tsc clean. The regression test pins the no-inner-wrapper contract for `kind=\"zone\"` FocusScopes. The architectural decision (route chrome onto the primitive directly) is sound — fewer DOM nodes and the consumer's `className` lands where layout actually applies.\n+\n+### Nits\n+\n+- [ ] `kanban-app/ui/src/components/focus-scope.tsx:167` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\".\n+- [ ] `kanban-app/ui/src/components/entity-inspector.test.tsx:296-310` — `renderInspectorWithSpatial` docstring says the helper exists because the no-spatial-context fallback \"hides the layout regression… the icon + content stack vertically only when `FocusScopeBody` (a className-less `<div>`) sits between the FocusZone primitive and the children.\" `FocusScopeBody` is gone, so the rationale is wrong. The helper is still useful (it covers the spatial primitive path that the fallback doesn't exercise), but the docstring should be rewritten to state that purpose without referencing the removed wrapper.\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense.\n+- [ ] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — The new optional `ref` prop on the primitives has no isolated unit test. The merged callback-ref path (write internal `.current` AND forward to the external `Ref<HTMLDivElement>`, supporting both function refs and `RefObject` refs) is exercised end-to-end via `<FocusScope>`'s scrollIntoView path, but a direct test on each primitive would pin the contract so a future refactor of the primitive can't silently break ref forwarding.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -53,15 +53,4 @@\n\n Surfaced during review of `01KQ56YQPD6W973XZFGG0GMYWJ` (Inspector layout regression). Reviewer noted the same workaround appears in three places and recommended fixing the root cause.\n\n-This card is \"fix the duplication\"; the original regression cards (`01KQ56YQPD6W973XZFGG0GMYWJ`, `01KQ4YF38NJ0BN6FM5EVDVRH1N`, plus the entity-card fix in `01KQ20NMRQQSXVRHP4RHE56B0K`) restored functionality at three different layers — this card folds them back to a single fix at the FocusScope layer.\n-\n-## Review Findings (2026-04-26 12:40)\n-\n-Implementation is correct: 1564/1564 vitest pass, cargo + clippy clean, tsc clean. The regression test pins the no-inner-wrapper contract for `kind=\"zone\"` FocusScopes. The architectural decision (route chrome onto the primitive directly) is sound — fewer DOM nodes and the consumer's `className` lands where layout actually applies.\n-\n-### Nits\n-\n-- [ ] `kanban-app/ui/src/components/focus-scope.tsx:167` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\".\n-- [ ] `kanban-app/ui/src/components/entity-inspector.test.tsx:296-310` — `renderInspectorWithSpatial` docstring says the helper exists because the no-spatial-context fallback \"hides the layout regression… the icon + content stack vertically only when `FocusScopeBody` (a className-less `<div>`) sits between the FocusZone primitive and the children.\" `FocusScopeBody` is gone, so the rationale is wrong. The helper is still useful (it covers the spatial primitive path that the fallback doesn't exercise), but the docstring should be rewritten to state that purpose without referencing the removed wrapper.\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense.\n-- [ ] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — The new optional `ref` prop on the primitives has no isolated unit test. The merged callback-ref path (write internal `.current` AND forward to the external `Ref<HTMLDivElement>`, supporting both function refs and `RefObject` refs) is exercised end-to-end via `<FocusScope>`'s scrollIntoView path, but a direct test on each primitive would pin the contract so a future refactor of the primitive can't silently break ref forwarding.\n\\ No newline at end of file\n+This card is \"fix the duplication\"; the original regression cards (`01KQ56YQPD6W973XZFGG0GMYWJ`, `01KQ4YF38NJ0BN6FM5EVDVRH1N`, plus the entity-card fix in `01KQ20NMRQQSXVRHP4RHE56B0K`) restored functionality at three different layers — this card folds them back to a single fix at the FocusScope layer.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5E8CXNCG5AS88ESWQPBA04","timestamp":"2026-04-26T17:44:06.325547Z","op":"update","item_id":"01KQ5ACT9582HHD80GRBH1QMAP","forward_patch":"--- original\n+++ modified\n@@ -47,7 +47,7 @@\n - [x] Three current workaround call sites simplified — single layer of flex classes, not double\n - [x] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n - [x] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n-- [x] `pnpm vitest run` passes (1564/1564 tests pass, 143/143 files pass)\n+- [x] `pnpm vitest run` passes (1568/1568 tests pass, 143/143 files pass)\n - [x] Added a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct children of the zone div (no inner wrapper)\n\n ## Tests\n@@ -54,6 +54,7 @@\n\n - [x] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children as direct children of the spatial-zone div (regression test added)\n - [x] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n+- [x] `focusable.test.tsx`, `focus-zone.test.tsx` — isolated unit tests pinning the merged callback-ref contract for the optional `ref` prop (RefObject and callback-ref forms; covers mount-time forwarding and unmount-time null cleanup; verifies the primitive's internal ref still drives spatial registration).\n\n ## Workflow\n\n@@ -71,7 +72,12 @@\n\n ### Nits\n\n-- [ ] `kanban-app/ui/src/components/focus-scope.tsx:167` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\".\n-- [ ] `kanban-app/ui/src/components/entity-inspector.test.tsx:296-310` — `renderInspectorWithSpatial` docstring says the helper exists because the no-spatial-context fallback \"hides the layout regression… the icon + content stack vertically only when `FocusScopeBody` (a className-less `<div>`) sits between the FocusZone primitive and the children.\" `FocusScopeBody` is gone, so the rationale is wrong. The helper is still useful (it covers the spatial primitive path that the fallback doesn't exercise), but the docstring should be rewritten to state that purpose without referencing the removed wrapper.\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense.\n-- [ ] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — The new optional `ref` prop on the primitives has no isolated unit test. The merged callback-ref path (write internal `.current` AND forward to the external `Ref<HTMLDivElement>`, supporting both function refs and `RefObject` refs) is exercised end-to-end via `<FocusScope>`'s scrollIntoView path, but a direct test on each primitive would pin the contract so a future refactor of the primitive can't silently break ref forwarding.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/components/focus-scope.tsx` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\". **Fixed.**\n+- [x] `kanban-app/ui/src/components/entity-inspector.test.tsx` — `renderInspectorWithSpatial` docstring rewrites the rationale without referencing the removed `FocusScopeBody`. The helper's purpose is now stated as exercising the production primitive code path (ResizeObserver/click + className-on-primitive contract) that the no-spatial-context fallback `<div>` does not. **Fixed.**\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense. **DEFERRED to dynamic-lifecycle agent (parallel-safety: this task agent must avoid column-view.tsx).** Suggested rewrite: replace \", which the FocusScopeBody fix in a parallel card also edits\" with \".\" (drop trailing clause) or with \" (the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\".\n+- [x] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — Added isolated unit tests on each primitive for the new optional `ref` prop. Both `RefObject` and callback-ref forms are exercised; the tests assert the external ref points at the same `<div>` that carries `data-moniker` (i.e. the primitive's internal-ref target), survive mount/unmount, and verify the internal ref still drives `spatial_register_*`. **Fixed.**\n+\n+### Verification\n+\n+- `pnpm vitest run` → 1568/1568 pass, 143/143 files pass (up +4 from new ref tests).\n+- `pnpm tsc --noEmit` → clean.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -47,7 +47,7 @@\n - [x] Three current workaround call sites simplified — single layer of flex classes, not double\n - [x] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n - [x] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n-- [x] `pnpm vitest run` passes (1568/1568 tests pass, 143/143 files pass)\n+- [x] `pnpm vitest run` passes (1564/1564 tests pass, 143/143 files pass)\n - [x] Added a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct children of the zone div (no inner wrapper)\n\n ## Tests\n@@ -54,7 +54,6 @@\n\n - [x] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children as direct children of the spatial-zone div (regression test added)\n - [x] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n-- [x] `focusable.test.tsx`, `focus-zone.test.tsx` — isolated unit tests pinning the merged callback-ref contract for the optional `ref` prop (RefObject and callback-ref forms; covers mount-time forwarding and unmount-time null cleanup; verifies the primitive's internal ref still drives spatial registration).\n\n ## Workflow\n\n@@ -72,12 +71,7 @@\n\n ### Nits\n\n-- [x] `kanban-app/ui/src/components/focus-scope.tsx` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\". **Fixed.**\n-- [x] `kanban-app/ui/src/components/entity-inspector.test.tsx` — `renderInspectorWithSpatial` docstring rewrites the rationale without referencing the removed `FocusScopeBody`. The helper's purpose is now stated as exercising the production primitive code path (ResizeObserver/click + className-on-primitive contract) that the no-spatial-context fallback `<div>` does not. **Fixed.**\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense. **DEFERRED to dynamic-lifecycle agent (parallel-safety: this task agent must avoid column-view.tsx).** Suggested rewrite: replace \", which the FocusScopeBody fix in a parallel card also edits\" with \".\" (drop trailing clause) or with \" (the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\".\n-- [x] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — Added isolated unit tests on each primitive for the new optional `ref` prop. Both `RefObject` and callback-ref forms are exercised; the tests assert the external ref points at the same `<div>` that carries `data-moniker` (i.e. the primitive's internal-ref target), survive mount/unmount, and verify the internal ref still drives `spatial_register_*`. **Fixed.**\n-\n-### Verification\n-\n-- `pnpm vitest run` → 1568/1568 pass, 143/143 files pass (up +4 from new ref tests).\n-- `pnpm tsc --noEmit` → clean.\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/components/focus-scope.tsx:167` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\".\n+- [ ] `kanban-app/ui/src/components/entity-inspector.test.tsx:296-310` — `renderInspectorWithSpatial` docstring says the helper exists because the no-spatial-context fallback \"hides the layout regression… the icon + content stack vertically only when `FocusScopeBody` (a className-less `<div>`) sits between the FocusZone primitive and the children.\" `FocusScopeBody` is gone, so the rationale is wrong. The helper is still useful (it covers the spatial primitive path that the fallback doesn't exercise), but the docstring should be rewritten to state that purpose without referencing the removed wrapper.\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense.\n+- [ ] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — The new optional `ref` prop on the primitives has no isolated unit test. The merged callback-ref path (write internal `.current` AND forward to the external `Ref<HTMLDivElement>`, supporting both function refs and `RefObject` refs) is exercised end-to-end via `<FocusScope>`'s scrollIntoView path, but a direct test on each primitive would pin the contract so a future refactor of the primitive can't silently break ref forwarding.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ5E8CXNWQ6FK6PAKH08F0AF","timestamp":"2026-04-26T17:44:06.325945Z","entity_type":"task","entity_id":"01KQ5ACT9582HHD80GRBH1QMAP","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -37,7 +37,7 @@\n - [x] Three current workaround call sites simplified — single layer of flex classes, not double\n - [x] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n - [x] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n-- [x] `pnpm vitest run` passes (1564/1564 tests pass, 143/143 files pass)\n+- [x] `pnpm vitest run` passes (1568/1568 tests pass, 143/143 files pass)\n - [x] Added a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct children of the zone div (no inner wrapper)\n\n ## Tests\n@@ -44,6 +44,7 @@\n\n - [x] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children as direct children of the spatial-zone div (regression test added)\n - [x] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n+- [x] `focusable.test.tsx`, `focus-zone.test.tsx` — isolated unit tests pinning the merged callback-ref contract for the optional `ref` prop (RefObject and callback-ref forms; covers mount-time forwarding and unmount-time null cleanup; verifies the primitive's internal ref still drives spatial registration).\n\n ## Workflow\n\n@@ -61,7 +62,12 @@\n\n ### Nits\n\n-- [ ] `kanban-app/ui/src/components/focus-scope.tsx:167` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\".\n-- [ ] `kanban-app/ui/src/components/entity-inspector.test.tsx:296-310` — `renderInspectorWithSpatial` docstring says the helper exists because the no-spatial-context fallback \"hides the layout regression… the icon + content stack vertically only when `FocusScopeBody` (a className-less `<div>`) sits between the FocusZone primitive and the children.\" `FocusScopeBody` is gone, so the rationale is wrong. The helper is still useful (it covers the spatial primitive path that the fallback doesn't exercise), but the docstring should be rewritten to state that purpose without referencing the removed wrapper.\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense.\n-- [ ] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — The new optional `ref` prop on the primitives has no isolated unit test. The merged callback-ref path (write internal `.current` AND forward to the external `Ref<HTMLDivElement>`, supporting both function refs and `RefObject` refs) is exercised end-to-end via `<FocusScope>`'s scrollIntoView path, but a direct test on each primitive would pin the contract so a future refactor of the primitive can't silently break ref forwarding.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/components/focus-scope.tsx` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\". **Fixed.**\n+- [x] `kanban-app/ui/src/components/entity-inspector.test.tsx` — `renderInspectorWithSpatial` docstring rewrites the rationale without referencing the removed `FocusScopeBody`. The helper's purpose is now stated as exercising the production primitive code path (ResizeObserver/click + className-on-primitive contract) that the no-spatial-context fallback `<div>` does not. **Fixed.**\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense. **DEFERRED to dynamic-lifecycle agent (parallel-safety: this task agent must avoid column-view.tsx).** Suggested rewrite: replace \", which the FocusScopeBody fix in a parallel card also edits\" with \".\" (drop trailing clause) or with \" (the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\".\n+- [x] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — Added isolated unit tests on each primitive for the new optional `ref` prop. Both `RefObject` and callback-ref forms are exercised; the tests assert the external ref points at the same `<div>` that carries `data-moniker` (i.e. the primitive's internal-ref target), survive mount/unmount, and verify the internal ref still drives `spatial_register_*`. **Fixed.**\n+\n+### Verification\n+\n+- `pnpm vitest run` → 1568/1568 pass, 143/143 files pass (up +4 from new ref tests).\n+- `pnpm tsc --noEmit` → clean.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -37,7 +37,7 @@\n - [x] Three current workaround call sites simplified — single layer of flex classes, not double\n - [x] Existing layout (cards, inspector field rows, column scroll) unchanged visually\n - [x] Spatial-nav zone registration / focus / context menu / right-click / scrollIntoView all still work\n-- [x] `pnpm vitest run` passes (1568/1568 tests pass, 143/143 files pass)\n+- [x] `pnpm vitest run` passes (1564/1564 tests pass, 143/143 files pass)\n - [x] Added a test that mounts `<FocusScope kind=\"zone\" className=\"flex flex-row\">` inside a SpatialFocusProvider+FocusLayer and asserts children are direct children of the zone div (no inner wrapper)\n\n ## Tests\n@@ -44,7 +44,6 @@\n\n - [x] `focus-scope.test.tsx` — flex parent test: `<FocusScope kind=\"zone\" className=\"flex flex-row\">{a}{b}</FocusScope>` lays children as direct children of the spatial-zone div (regression test added)\n - [x] Existing entity-card, entity-inspector, column-view tests still pass after the call-site simplifications\n-- [x] `focusable.test.tsx`, `focus-zone.test.tsx` — isolated unit tests pinning the merged callback-ref contract for the optional `ref` prop (RefObject and callback-ref forms; covers mount-time forwarding and unmount-time null cleanup; verifies the primitive's internal ref still drives spatial registration).\n\n ## Workflow\n\n@@ -62,12 +61,7 @@\n\n ### Nits\n\n-- [x] `kanban-app/ui/src/components/focus-scope.tsx` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\". **Fixed.**\n-- [x] `kanban-app/ui/src/components/entity-inspector.test.tsx` — `renderInspectorWithSpatial` docstring rewrites the rationale without referencing the removed `FocusScopeBody`. The helper's purpose is now stated as exercising the production primitive code path (ResizeObserver/click + className-on-primitive contract) that the no-spatial-context fallback `<div>` does not. **Fixed.**\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense. **DEFERRED to dynamic-lifecycle agent (parallel-safety: this task agent must avoid column-view.tsx).** Suggested rewrite: replace \", which the FocusScopeBody fix in a parallel card also edits\" with \".\" (drop trailing clause) or with \" (the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\".\n-- [x] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — Added isolated unit tests on each primitive for the new optional `ref` prop. Both `RefObject` and callback-ref forms are exercised; the tests assert the external ref points at the same `<div>` that carries `data-moniker` (i.e. the primitive's internal-ref target), survive mount/unmount, and verify the internal ref still drives `spatial_register_*`. **Fixed.**\n-\n-### Verification\n-\n-- `pnpm vitest run` → 1568/1568 pass, 143/143 files pass (up +4 from new ref tests).\n-- `pnpm tsc --noEmit` → clean.\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/components/focus-scope.tsx:167` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\".\n+- [ ] `kanban-app/ui/src/components/entity-inspector.test.tsx:296-310` — `renderInspectorWithSpatial` docstring says the helper exists because the no-spatial-context fallback \"hides the layout regression… the icon + content stack vertically only when `FocusScopeBody` (a className-less `<div>`) sits between the FocusZone primitive and the children.\" `FocusScopeBody` is gone, so the rationale is wrong. The helper is still useful (it covers the spatial primitive path that the fallback doesn't exercise), but the docstring should be rewritten to state that purpose without referencing the removed wrapper.\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense.\n+- [ ] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — The new optional `ref` prop on the primitives has no isolated unit test. The merged callback-ref path (write internal `.current` AND forward to the external `Ref<HTMLDivElement>`, supporting both function refs and `RefObject` refs) is exercised end-to-end via `<FocusScope>`'s scrollIntoView path, but a direct test on each primitive would pin the contract so a future refactor of the primitive can't silently break ref forwarding.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5F0YFJZN3B0TCY158GREPA","timestamp":"2026-04-26T17:57:30.738785Z","op":"update","item_id":"01KQ5ACT9582HHD80GRBH1QMAP","forward_patch":"--- original\n+++ modified\n@@ -80,4 +80,24 @@\n ### Verification\n\n - `pnpm vitest run` → 1568/1568 pass, 143/143 files pass (up +4 from new ref tests).\n-- `pnpm tsc --noEmit` → clean.\n\\ No newline at end of file\n+- `pnpm tsc --noEmit` → clean.\n+\n+## Review Findings (2026-04-26 12:55)\n+\n+Third-pass review. The two checked nits from the prior pass are confirmed fixed in tree:\n+\n+- `focus-scope.tsx` — the comment near `useIsDirectFocus(moniker)` now reads \"Drives the scroll-into-view effect in `FocusScopeChrome`…\", matching the relocated effect.\n+- `entity-inspector.test.tsx` — the `renderInspectorWithSpatial` docstring now describes the production primitive code path (ResizeObserver/click + className-on-primitive contract) without referencing the removed `FocusScopeBody`.\n+- `focusable.test.tsx`, `focus-zone.test.tsx` — each primitive has two new isolated tests for the optional `ref` prop (RefObject + callback-ref). Both verify the external ref points at the same `<div>` that carries `data-moniker`, that callback-ref cleanup nulls on unmount, and that the primitive's internal ref still drives `spatial_register_focusable` / `spatial_register_zone`. The tests are tightly scoped and don't duplicate the contract already exercised in `focus-scope.test.tsx`.\n+\n+The remaining `FocusScopeBody` references in `entity-inspector.tsx`, `column-view.test.tsx`, `focus-scope.tsx`, and `focus-scope.test.tsx` are all historical/explanatory documentation in regression-test docstrings and design comments — they describe the pre-fix state to motivate the regression guards. Those are intentional and should stay.\n+\n+Verification re-run on this branch:\n+\n+- `pnpm vitest run` → 1571/1571 pass, 143/143 files pass.\n+- `pnpm tsc --noEmit` → clean.\n+- `cargo clippy --all-targets --all-features` → clean.\n+\n+### Outstanding\n+\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:941` — Carryover from prior pass. Comment still reads \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" This task has now landed, so the \"parallel card\" reference is no longer accurate. The original reviewer deferred this to the dynamic-lifecycle agent for parallel-safety; that justification still applies if the dynamic-lifecycle work is in flight. Leave the carryover open for the dynamic-lifecycle agent (or a follow-up sweep) — do not advance this task to `done` until that comment lands.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -80,24 +80,4 @@\n ### Verification\n\n - `pnpm vitest run` → 1568/1568 pass, 143/143 files pass (up +4 from new ref tests).\n-- `pnpm tsc --noEmit` → clean.\n-\n-## Review Findings (2026-04-26 12:55)\n-\n-Third-pass review. The two checked nits from the prior pass are confirmed fixed in tree:\n-\n-- `focus-scope.tsx` — the comment near `useIsDirectFocus(moniker)` now reads \"Drives the scroll-into-view effect in `FocusScopeChrome`…\", matching the relocated effect.\n-- `entity-inspector.test.tsx` — the `renderInspectorWithSpatial` docstring now describes the production primitive code path (ResizeObserver/click + className-on-primitive contract) without referencing the removed `FocusScopeBody`.\n-- `focusable.test.tsx`, `focus-zone.test.tsx` — each primitive has two new isolated tests for the optional `ref` prop (RefObject + callback-ref). Both verify the external ref points at the same `<div>` that carries `data-moniker`, that callback-ref cleanup nulls on unmount, and that the primitive's internal ref still drives `spatial_register_focusable` / `spatial_register_zone`. The tests are tightly scoped and don't duplicate the contract already exercised in `focus-scope.test.tsx`.\n-\n-The remaining `FocusScopeBody` references in `entity-inspector.tsx`, `column-view.test.tsx`, `focus-scope.tsx`, and `focus-scope.test.tsx` are all historical/explanatory documentation in regression-test docstrings and design comments — they describe the pre-fix state to motivate the regression guards. Those are intentional and should stay.\n-\n-Verification re-run on this branch:\n-\n-- `pnpm vitest run` → 1571/1571 pass, 143/143 files pass.\n-- `pnpm tsc --noEmit` → clean.\n-- `cargo clippy --all-targets --all-features` → clean.\n-\n-### Outstanding\n-\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:941` — Carryover from prior pass. Comment still reads \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" This task has now landed, so the \"parallel card\" reference is no longer accurate. The original reviewer deferred this to the dynamic-lifecycle agent for parallel-safety; that justification still applies if the dynamic-lifecycle work is in flight. Leave the carryover open for the dynamic-lifecycle agent (or a follow-up sweep) — do not advance this task to `done` until that comment lands.\n\\ No newline at end of file\n+- `pnpm tsc --noEmit` → clean.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ5F0YFKJXP67EA80JJPGYVM","timestamp":"2026-04-26T17:57:30.739318Z","entity_type":"task","entity_id":"01KQ5ACT9582HHD80GRBH1QMAP","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -70,4 +70,24 @@\n ### Verification\n\n - `pnpm vitest run` → 1568/1568 pass, 143/143 files pass (up +4 from new ref tests).\n-- `pnpm tsc --noEmit` → clean.\n\\ No newline at end of file\n+- `pnpm tsc --noEmit` → clean.\n+\n+## Review Findings (2026-04-26 12:55)\n+\n+Third-pass review. The two checked nits from the prior pass are confirmed fixed in tree:\n+\n+- `focus-scope.tsx` — the comment near `useIsDirectFocus(moniker)` now reads \"Drives the scroll-into-view effect in `FocusScopeChrome`…\", matching the relocated effect.\n+- `entity-inspector.test.tsx` — the `renderInspectorWithSpatial` docstring now describes the production primitive code path (ResizeObserver/click + className-on-primitive contract) without referencing the removed `FocusScopeBody`.\n+- `focusable.test.tsx`, `focus-zone.test.tsx` — each primitive has two new isolated tests for the optional `ref` prop (RefObject + callback-ref). Both verify the external ref points at the same `<div>` that carries `data-moniker`, that callback-ref cleanup nulls on unmount, and that the primitive's internal ref still drives `spatial_register_focusable` / `spatial_register_zone`. The tests are tightly scoped and don't duplicate the contract already exercised in `focus-scope.test.tsx`.\n+\n+The remaining `FocusScopeBody` references in `entity-inspector.tsx`, `column-view.test.tsx`, `focus-scope.tsx`, and `focus-scope.test.tsx` are all historical/explanatory documentation in regression-test docstrings and design comments — they describe the pre-fix state to motivate the regression guards. Those are intentional and should stay.\n+\n+Verification re-run on this branch:\n+\n+- `pnpm vitest run` → 1571/1571 pass, 143/143 files pass.\n+- `pnpm tsc --noEmit` → clean.\n+- `cargo clippy --all-targets --all-features` → clean.\n+\n+### Outstanding\n+\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:941` — Carryover from prior pass. Comment still reads \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" This task has now landed, so the \"parallel card\" reference is no longer accurate. The original reviewer deferred this to the dynamic-lifecycle agent for parallel-safety; that justification still applies if the dynamic-lifecycle work is in flight. Leave the carryover open for the dynamic-lifecycle agent (or a follow-up sweep) — do not advance this task to `done` until that comment lands.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -70,24 +70,4 @@\n ### Verification\n\n - `pnpm vitest run` → 1568/1568 pass, 143/143 files pass (up +4 from new ref tests).\n-- `pnpm tsc --noEmit` → clean.\n-\n-## Review Findings (2026-04-26 12:55)\n-\n-Third-pass review. The two checked nits from the prior pass are confirmed fixed in tree:\n-\n-- `focus-scope.tsx` — the comment near `useIsDirectFocus(moniker)` now reads \"Drives the scroll-into-view effect in `FocusScopeChrome`…\", matching the relocated effect.\n-- `entity-inspector.test.tsx` — the `renderInspectorWithSpatial` docstring now describes the production primitive code path (ResizeObserver/click + className-on-primitive contract) without referencing the removed `FocusScopeBody`.\n-- `focusable.test.tsx`, `focus-zone.test.tsx` — each primitive has two new isolated tests for the optional `ref` prop (RefObject + callback-ref). Both verify the external ref points at the same `<div>` that carries `data-moniker`, that callback-ref cleanup nulls on unmount, and that the primitive's internal ref still drives `spatial_register_focusable` / `spatial_register_zone`. The tests are tightly scoped and don't duplicate the contract already exercised in `focus-scope.test.tsx`.\n-\n-The remaining `FocusScopeBody` references in `entity-inspector.tsx`, `column-view.test.tsx`, `focus-scope.tsx`, and `focus-scope.test.tsx` are all historical/explanatory documentation in regression-test docstrings and design comments — they describe the pre-fix state to motivate the regression guards. Those are intentional and should stay.\n-\n-Verification re-run on this branch:\n-\n-- `pnpm vitest run` → 1571/1571 pass, 143/143 files pass.\n-- `pnpm tsc --noEmit` → clean.\n-- `cargo clippy --all-targets --all-features` → clean.\n-\n-### Outstanding\n-\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:941` — Carryover from prior pass. Comment still reads \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" This task has now landed, so the \"parallel card\" reference is no longer accurate. The original reviewer deferred this to the dynamic-lifecycle agent for parallel-safety; that justification still applies if the dynamic-lifecycle work is in flight. Leave the carryover open for the dynamic-lifecycle agent (or a follow-up sweep) — do not advance this task to `done` until that comment lands.\n\\ No newline at end of file\n+- `pnpm tsc --noEmit` → clean.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5F2Y3ZJGK8H8Y11W37E2HR","timestamp":"2026-04-26T17:58:35.903150Z","op":"update","item_id":"01KQ5ACT9582HHD80GRBH1QMAP","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KQ56YQPD6W973XZFGG0GMYWJ\n-position_column: review\n+position_column: doing\n position_ordinal: '8280'\n project: spatial-nav\n title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KQ56YQPD6W973XZFGG0GMYWJ\n-position_column: doing\n+position_column: review\n position_ordinal: '8280'\n project: spatial-nav\n title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n","transaction_id":null}
{"id":"01KQ5F2Y40JCS4A30AQY5PY4QR","timestamp":"2026-04-26T17:58:35.904161Z","entity_type":"task","entity_id":"01KQ5ACT9582HHD80GRBH1QMAP","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5F5PTJXCMRPYQ856ZP09XN","timestamp":"2026-04-26T18:00:06.738584Z","op":"update","item_id":"01KQ5ACT9582HHD80GRBH1QMAP","forward_patch":"--- original\n+++ modified\n@@ -74,7 +74,7 @@\n\n - [x] `kanban-app/ui/src/components/focus-scope.tsx` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\". **Fixed.**\n - [x] `kanban-app/ui/src/components/entity-inspector.test.tsx` — `renderInspectorWithSpatial` docstring rewrites the rationale without referencing the removed `FocusScopeBody`. The helper's purpose is now stated as exercising the production primitive code path (ResizeObserver/click + className-on-primitive contract) that the no-spatial-context fallback `<div>` does not. **Fixed.**\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense. **DEFERRED to dynamic-lifecycle agent (parallel-safety: this task agent must avoid column-view.tsx).** Suggested rewrite: replace \", which the FocusScopeBody fix in a parallel card also edits\" with \".\" (drop trailing clause) or with \" (the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\".\n+- [x] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense. **DEFERRED to dynamic-lifecycle agent (parallel-safety: this task agent must avoid column-view.tsx).** Suggested rewrite: replace \", which the FocusScopeBody fix in a parallel card also edits\" with \".\" (drop trailing clause) or with \" (the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\". **Fixed.**\n - [x] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — Added isolated unit tests on each primitive for the new optional `ref` prop. Both `RefObject` and callback-ref forms are exercised; the tests assert the external ref points at the same `<div>` that carries `data-moniker` (i.e. the primitive's internal-ref target), survive mount/unmount, and verify the internal ref still drives `spatial_register_*`. **Fixed.**\n\n ### Verification\n@@ -100,4 +100,4 @@\n\n ### Outstanding\n\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:941` — Carryover from prior pass. Comment still reads \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" This task has now landed, so the \"parallel card\" reference is no longer accurate. The original reviewer deferred this to the dynamic-lifecycle agent for parallel-safety; that justification still applies if the dynamic-lifecycle work is in flight. Leave the carryover open for the dynamic-lifecycle agent (or a follow-up sweep) — do not advance this task to `done` until that comment lands.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/components/column-view.tsx:941` — Carryover from prior pass. Comment still reads \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" This task has now landed, so the \"parallel card\" reference is no longer accurate. The original reviewer deferred this to the dynamic-lifecycle agent for parallel-safety; that justification still applies if the dynamic-lifecycle work is in flight. Leave the carryover open for the dynamic-lifecycle agent (or a follow-up sweep) — do not advance this task to `done` until that comment lands. **Fixed (2026-04-26):** comment rewritten to past-tense — trailing clause now reads \"(the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\".\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -74,7 +74,7 @@\n\n - [x] `kanban-app/ui/src/components/focus-scope.tsx` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\". **Fixed.**\n - [x] `kanban-app/ui/src/components/entity-inspector.test.tsx` — `renderInspectorWithSpatial` docstring rewrites the rationale without referencing the removed `FocusScopeBody`. The helper's purpose is now stated as exercising the production primitive code path (ResizeObserver/click + className-on-primitive contract) that the no-spatial-context fallback `<div>` does not. **Fixed.**\n-- [x] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense. **DEFERRED to dynamic-lifecycle agent (parallel-safety: this task agent must avoid column-view.tsx).** Suggested rewrite: replace \", which the FocusScopeBody fix in a parallel card also edits\" with \".\" (drop trailing clause) or with \" (the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\". **Fixed.**\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense. **DEFERRED to dynamic-lifecycle agent (parallel-safety: this task agent must avoid column-view.tsx).** Suggested rewrite: replace \", which the FocusScopeBody fix in a parallel card also edits\" with \".\" (drop trailing clause) or with \" (the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\".\n - [x] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — Added isolated unit tests on each primitive for the new optional `ref` prop. Both `RefObject` and callback-ref forms are exercised; the tests assert the external ref points at the same `<div>` that carries `data-moniker` (i.e. the primitive's internal-ref target), survive mount/unmount, and verify the internal ref still drives `spatial_register_*`. **Fixed.**\n\n ### Verification\n@@ -100,4 +100,4 @@\n\n ### Outstanding\n\n-- [x] `kanban-app/ui/src/components/column-view.tsx:941` — Carryover from prior pass. Comment still reads \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" This task has now landed, so the \"parallel card\" reference is no longer accurate. The original reviewer deferred this to the dynamic-lifecycle agent for parallel-safety; that justification still applies if the dynamic-lifecycle work is in flight. Leave the carryover open for the dynamic-lifecycle agent (or a follow-up sweep) — do not advance this task to `done` until that comment lands. **Fixed (2026-04-26):** comment rewritten to past-tense — trailing clause now reads \"(the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\".\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:941` — Carryover from prior pass. Comment still reads \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" This task has now landed, so the \"parallel card\" reference is no longer accurate. The original reviewer deferred this to the dynamic-lifecycle agent for parallel-safety; that justification still applies if the dynamic-lifecycle work is in flight. Leave the carryover open for the dynamic-lifecycle agent (or a follow-up sweep) — do not advance this task to `done` until that comment lands.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ5F5PTKQ7Q7SK08QRCV0BH7","timestamp":"2026-04-26T18:00:06.739060Z","entity_type":"task","entity_id":"01KQ5ACT9582HHD80GRBH1QMAP","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -64,7 +64,7 @@\n\n - [x] `kanban-app/ui/src/components/focus-scope.tsx` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\". **Fixed.**\n - [x] `kanban-app/ui/src/components/entity-inspector.test.tsx` — `renderInspectorWithSpatial` docstring rewrites the rationale without referencing the removed `FocusScopeBody`. The helper's purpose is now stated as exercising the production primitive code path (ResizeObserver/click + className-on-primitive contract) that the no-spatial-context fallback `<div>` does not. **Fixed.**\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense. **DEFERRED to dynamic-lifecycle agent (parallel-safety: this task agent must avoid column-view.tsx).** Suggested rewrite: replace \", which the FocusScopeBody fix in a parallel card also edits\" with \".\" (drop trailing clause) or with \" (the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\".\n+- [x] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense. **DEFERRED to dynamic-lifecycle agent (parallel-safety: this task agent must avoid column-view.tsx).** Suggested rewrite: replace \", which the FocusScopeBody fix in a parallel card also edits\" with \".\" (drop trailing clause) or with \" (the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\". **Fixed.**\n - [x] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — Added isolated unit tests on each primitive for the new optional `ref` prop. Both `RefObject` and callback-ref forms are exercised; the tests assert the external ref points at the same `<div>` that carries `data-moniker` (i.e. the primitive's internal-ref target), survive mount/unmount, and verify the internal ref still drives `spatial_register_*`. **Fixed.**\n\n ### Verification\n@@ -90,4 +90,4 @@\n\n ### Outstanding\n\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:941` — Carryover from prior pass. Comment still reads \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" This task has now landed, so the \"parallel card\" reference is no longer accurate. The original reviewer deferred this to the dynamic-lifecycle agent for parallel-safety; that justification still applies if the dynamic-lifecycle work is in flight. Leave the carryover open for the dynamic-lifecycle agent (or a follow-up sweep) — do not advance this task to `done` until that comment lands.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/components/column-view.tsx:941` — Carryover from prior pass. Comment still reads \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" This task has now landed, so the \"parallel card\" reference is no longer accurate. The original reviewer deferred this to the dynamic-lifecycle agent for parallel-safety; that justification still applies if the dynamic-lifecycle work is in flight. Leave the carryover open for the dynamic-lifecycle agent (or a follow-up sweep) — do not advance this task to `done` until that comment lands. **Fixed (2026-04-26):** comment rewritten to past-tense — trailing clause now reads \"(the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\".\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -64,7 +64,7 @@\n\n - [x] `kanban-app/ui/src/components/focus-scope.tsx` — Stale comment references the removed `FocusScopeBody`: \"Drives the inner FocusScopeBody's scroll-into-view effect…\". The scroll-into-view effect now lives in `FocusScopeChrome`. Update to \"Drives the scroll-into-view effect in `FocusScopeChrome`…\". **Fixed.**\n - [x] `kanban-app/ui/src/components/entity-inspector.test.tsx` — `renderInspectorWithSpatial` docstring rewrites the rationale without referencing the removed `FocusScopeBody`. The helper's purpose is now stated as exercising the production primitive code path (ResizeObserver/click + className-on-primitive contract) that the no-spatial-context fallback `<div>` does not. **Fixed.**\n-- [x] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense. **DEFERRED to dynamic-lifecycle agent (parallel-safety: this task agent must avoid column-view.tsx).** Suggested rewrite: replace \", which the FocusScopeBody fix in a parallel card also edits\" with \".\" (drop trailing clause) or with \" (the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\". **Fixed.**\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:886` — Comment says hooks live in the column body so they sit alongside the virtualizer \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" The \"parallel card\" is this very task. After this lands the reference to a parallel in-flight fix is misleading. Either drop the trailing clause or rephrase to past tense. **DEFERRED to dynamic-lifecycle agent (parallel-safety: this task agent must avoid column-view.tsx).** Suggested rewrite: replace \", which the FocusScopeBody fix in a parallel card also edits\" with \".\" (drop trailing clause) or with \" (the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\".\n - [x] `kanban-app/ui/src/components/focusable.test.tsx`, `focus-zone.test.tsx` — Added isolated unit tests on each primitive for the new optional `ref` prop. Both `RefObject` and callback-ref forms are exercised; the tests assert the external ref points at the same `<div>` that carries `data-moniker` (i.e. the primitive's internal-ref target), survive mount/unmount, and verify the internal ref still drives `spatial_register_*`. **Fixed.**\n\n ### Verification\n@@ -90,4 +90,4 @@\n\n ### Outstanding\n\n-- [x] `kanban-app/ui/src/components/column-view.tsx:941` — Carryover from prior pass. Comment still reads \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" This task has now landed, so the \"parallel card\" reference is no longer accurate. The original reviewer deferred this to the dynamic-lifecycle agent for parallel-safety; that justification still applies if the dynamic-lifecycle work is in flight. Leave the carryover open for the dynamic-lifecycle agent (or a follow-up sweep) — do not advance this task to `done` until that comment lands. **Fixed (2026-04-26):** comment rewritten to past-tense — trailing clause now reads \"(the FocusScope wrap was rewritten when `FocusScopeBody` was removed)\".\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:941` — Carryover from prior pass. Comment still reads \"keeping the outer `<FocusScope>` wrap untouched, which the FocusScopeBody fix in a parallel card also edits.\" This task has now landed, so the \"parallel card\" reference is no longer accurate. The original reviewer deferred this to the dynamic-lifecycle agent for parallel-safety; that justification still applies if the dynamic-lifecycle work is in flight. Leave the carryover open for the dynamic-lifecycle agent (or a follow-up sweep) — do not advance this task to `done` until that comment lands.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5F676TC0VWCWQ1NMZ3R9WB","timestamp":"2026-04-26T18:00:23.514728Z","op":"update","item_id":"01KQ5ACT9582HHD80GRBH1QMAP","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KQ56YQPD6W973XZFGG0GMYWJ\n-position_column: doing\n+position_column: review\n position_ordinal: '8280'\n project: spatial-nav\n title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KQ56YQPD6W973XZFGG0GMYWJ\n-position_column: review\n+position_column: doing\n position_ordinal: '8280'\n project: spatial-nav\n title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n","transaction_id":null}
{"id":"01KQ5F676VTG7B3SQHGV7N3BQ2","timestamp":"2026-04-26T18:00:23.515414Z","entity_type":"task","entity_id":"01KQ5ACT9582HHD80GRBH1QMAP","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5FAYB77SV71W15SN09VAAG","timestamp":"2026-04-26T18:02:58.279484Z","op":"update","item_id":"01KQ5ACT9582HHD80GRBH1QMAP","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ56YQPD6W973XZFGG0GMYWJ\n-position_column: review\n-position_ordinal: '8280'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffc280\n project: spatial-nav\n title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KQ56YQPD6W973XZFGG0GMYWJ\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffc280\n+position_column: review\n+position_ordinal: '8280'\n project: spatial-nav\n title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n ---\n","transaction_id":null}
{"id":"01KQ5FAYB8VG1TAY84KQJHCQTZ","timestamp":"2026-04-26T18:02:58.280627Z","entity_type":"task","entity_id":"01KQ5ACT9582HHD80GRBH1QMAP","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+ffffffffffffffffffffffffc280\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffc280\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5NTG4GZTAV3Z87JFTG0H0K","timestamp":"2026-04-26T19:56:19.472357Z","op":"update","item_id":"01KQ5ACT9582HHD80GRBH1QMAP","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KQ56YQPD6W973XZFGG0GMYWJ\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffc280\n+position_ordinal: ffffffffffffffffffffffffffffffffb880\n project: spatial-nav\n title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KQ56YQPD6W973XZFGG0GMYWJ\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffb880\n+position_ordinal: ffffffffffffffffffffffffc280\n project: spatial-nav\n title: Fix FocusScopeBody flex chain breakage at the source (remove outer-flex + inner-flex duplication)\n ---\n","transaction_id":null}
{"id":"01KQ5NTG4HE73B0FWEZ5VF0X8X","timestamp":"2026-04-26T19:56:19.473063Z","entity_type":"task","entity_id":"01KQ5ACT9582HHD80GRBH1QMAP","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffc280\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffb880\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffb880\n\\ No newline at end of file\n+ffffffffffffffffffffffffc280\n\\ No newline at end of file\n"}]]}

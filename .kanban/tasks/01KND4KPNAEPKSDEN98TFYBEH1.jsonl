{"id":"01KND4KPNBQMXT26SS2R8Z7FKZ","timestamp":"2026-04-04T20:58:02.795254Z","entity_type":"task","entity_id":"01KND4KPNAEPKSDEN98TFYBEH1","op":"create","changes":[["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\nWire virtual tags into the task enrichment pipeline so that virtual tag slugs appear in a task's `tags` array alongside body-parsed tags.\n\nThe key insight: virtual tags should NOT be injected into `ParseBodyTags.compute()` because that handler only knows about individual task fields, not the full task list. Instead, virtual tags should be merged in `enrich_all_task_entities()` which already has the full task list and terminal column ID.\n\n**Files to modify:**\n- `swissarmyhammer-kanban/src/task_helpers.rs` — in `enrich_all_task_entities()`:\n  1. Accept a `&VirtualTagRegistry` parameter\n  2. After computing ready/blocked/blocks, evaluate virtual tags for each task\n  3. Read existing body-parsed tags from `entity.get_string_list(\\\"tags\\\")`\n  4. Append matching virtual tag slugs\n  5. Write merged list back: `entity.set(\\\"tags\\\", json!(merged_tags))`\n- All callers of `enrich_all_task_entities` — pass the registry\n- `swissarmyhammer-kanban/src/task_helpers.rs` — also update `enrich_task_entity()` (single-task variant) and `task_entity_to_rich_json()` if they produce tags independently\n\nThe `apply()` path in `ParseBodyTags` must filter out virtual tag slugs before writing back to body (they're computed, not stored). This prevents virtual tags from being appended to body text when the multi-select editor writes back.\n\n**Files to modify for apply filtering:**\n- `swissarmyhammer-kanban/src/derive_handlers.rs` — `ParseBodyTags::apply()` should accept a set of virtual tag slugs (or check the registry) and exclude them from the body-mutation diff\n\n## Acceptance Criteria\n- [ ] `enrich_all_task_entities` accepts and uses a `VirtualTagRegistry`\n- [ ] A task with no deps has `READY` in its tags (once READY strategy is registered)\n- [ ] A task with unmet deps has `BLOCKED` in its tags\n- [ ] Virtual tags appear alongside body-parsed tags in the merged list\n- [ ] `ParseBodyTags::apply()` does not write virtual tag slugs into the body\n- [ ] All callers pass the registry\n\n## Tests\n- [ ] Unit test: enrich with mock strategy, verify virtual tag appears in tags array\n- [ ] Unit test: apply() with virtual tag in desired set, verify it's NOT added to body\n- [ ] Integration test: create task with dependency, verify BLOCKED tag appears\n- [ ] `cargo nextest run -p swissarmyhammer-kanban` passes\n\n## Workflow\n- Use `/tdd` — write failing tests first, then implement to make them pass.\n\n#virtual-tags"}],["depends_on",{"kind":"set","value":["01KND4JJV437GTZ7QFQ3JBM2F1"]}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"8380"}],["title",{"kind":"set","value":"VT-4: Merge virtual tags into task enrichment pipeline"}]],"transaction_id":"01KND4KMZA67PN5D7P3JXBH9DX"}
{"id":"01KNERDGBT809GF9V0DM5JP5C9","timestamp":"2026-04-05T12:03:25.690338Z","entity_type":"task","entity_id":"01KND4KPNAEPKSDEN98TFYBEH1","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,36 +1,40 @@\n ## What\n\n-Wire virtual tags into the task enrichment pipeline so that virtual tag slugs appear in a task's `tags` array alongside body-parsed tags.\n+Wire virtual tags into the task enrichment pipeline as two separate fields — `virtual_tags` (display) and `filter_tags` (union for filtering). The existing `tags` field stays untouched (body-parsed only, editable).\n\n-The key insight: virtual tags should NOT be injected into `ParseBodyTags.compute()` because that handler only knows about individual task fields, not the full task list. Instead, virtual tags should be merged in `enrich_all_task_entities()` which already has the full task list and terminal column ID.\n+**Key principle**: `tags` is body-parsed and editable. `virtual_tags` is computed from strategies and display-only. `filter_tags = tags ∪ virtual_tags` and is hidden — used only for backend filtering.\n\n **Files to modify:**\n - `swissarmyhammer-kanban/src/task_helpers.rs` — in `enrich_all_task_entities()`:\n   1. Accept a `&VirtualTagRegistry` parameter\n   2. After computing ready/blocked/blocks, evaluate virtual tags for each task\n-  3. Read existing body-parsed tags from `entity.get_string_list(\\\"tags\\\")`\n-  4. Append matching virtual tag slugs\n-  5. Write merged list back: `entity.set(\\\"tags\\\", json!(merged_tags))`\n-- All callers of `enrich_all_task_entities` — pass the registry\n-- `swissarmyhammer-kanban/src/task_helpers.rs` — also update `enrich_task_entity()` (single-task variant) and `task_entity_to_rich_json()` if they produce tags independently\n+  3. Set `virtual_tags` field on entity with matching virtual tag slugs\n+  4. Read `tags` (body-parsed) and `virtual_tags`, compute union, set `filter_tags`\n+- `swissarmyhammer-kanban/src/task_helpers.rs` — also update `enrich_task_entity()` (single-task variant) to accept and use registry\n+- All callers of `enrich_all_task_entities` — pass the registry (main caller: `kanban-app/src/commands.rs:226`)\n\n-The `apply()` path in `ParseBodyTags` must filter out virtual tag slugs before writing back to body (they're computed, not stored). This prevents virtual tags from being appended to body text when the multi-select editor writes back.\n+**Files to modify for filtering:**\n+- `swissarmyhammer-kanban/src/task/list.rs` — change tag filter from `task_tags(t)` to reading `filter_tags` field\n+- `swissarmyhammer-kanban/src/task/next.rs` — same change for tag filtering\n+- `swissarmyhammer-kanban/src/task_helpers.rs` — `task_entity_to_rich_json()` should include `virtual_tags` and `filter_tags` in output\n\n-**Files to modify for apply filtering:**\n-- `swissarmyhammer-kanban/src/derive_handlers.rs` — `ParseBodyTags::apply()` should accept a set of virtual tag slugs (or check the registry) and exclude them from the body-mutation diff\n+**No changes to `ParseBodyTags`** — the derive handler stays pure (body-only). Virtual tags are injected at the enrichment layer, not the derive layer.\n\n ## Acceptance Criteria\n - [ ] `enrich_all_task_entities` accepts and uses a `VirtualTagRegistry`\n-- [ ] A task with no deps has `READY` in its tags (once READY strategy is registered)\n-- [ ] A task with unmet deps has `BLOCKED` in its tags\n-- [ ] Virtual tags appear alongside body-parsed tags in the merged list\n-- [ ] `ParseBodyTags::apply()` does not write virtual tag slugs into the body\n+- [ ] `virtual_tags` field set on each entity with matching virtual tag slugs\n+- [ ] `filter_tags` field set as union of `tags` + `virtual_tags`\n+- [ ] `tags` field is NOT modified (still body-parsed only)\n+- [ ] `list tasks --tag BLOCKED` finds tasks with the BLOCKED virtual tag via `filter_tags`\n+- [ ] `next task --tag READY` finds ready tasks via `filter_tags`\n - [ ] All callers pass the registry\n\n ## Tests\n-- [ ] Unit test: enrich with mock strategy, verify virtual tag appears in tags array\n-- [ ] Unit test: apply() with virtual tag in desired set, verify it's NOT added to body\n-- [ ] Integration test: create task with dependency, verify BLOCKED tag appears\n+- [ ] Unit test: enrich with mock strategy, verify `virtual_tags` field contains strategy slug\n+- [ ] Unit test: enrich, verify `filter_tags` is union of `tags` and `virtual_tags`\n+- [ ] Unit test: enrich, verify `tags` field unchanged (no virtual slugs)\n+- [ ] Integration test: `list tasks --tag BLOCKED` returns blocked task\n+- [ ] Integration test: `list tasks --tag bug` still works (body-parsed tag)\n - [ ] `cargo nextest run -p swissarmyhammer-kanban` passes\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -1,40 +1,36 @@\n ## What\n\n-Wire virtual tags into the task enrichment pipeline as two separate fields — `virtual_tags` (display) and `filter_tags` (union for filtering). The existing `tags` field stays untouched (body-parsed only, editable).\n+Wire virtual tags into the task enrichment pipeline so that virtual tag slugs appear in a task's `tags` array alongside body-parsed tags.\n\n-**Key principle**: `tags` is body-parsed and editable. `virtual_tags` is computed from strategies and display-only. `filter_tags = tags ∪ virtual_tags` and is hidden — used only for backend filtering.\n+The key insight: virtual tags should NOT be injected into `ParseBodyTags.compute()` because that handler only knows about individual task fields, not the full task list. Instead, virtual tags should be merged in `enrich_all_task_entities()` which already has the full task list and terminal column ID.\n\n **Files to modify:**\n - `swissarmyhammer-kanban/src/task_helpers.rs` — in `enrich_all_task_entities()`:\n   1. Accept a `&VirtualTagRegistry` parameter\n   2. After computing ready/blocked/blocks, evaluate virtual tags for each task\n-  3. Set `virtual_tags` field on entity with matching virtual tag slugs\n-  4. Read `tags` (body-parsed) and `virtual_tags`, compute union, set `filter_tags`\n-- `swissarmyhammer-kanban/src/task_helpers.rs` — also update `enrich_task_entity()` (single-task variant) to accept and use registry\n-- All callers of `enrich_all_task_entities` — pass the registry (main caller: `kanban-app/src/commands.rs:226`)\n+  3. Read existing body-parsed tags from `entity.get_string_list(\\\"tags\\\")`\n+  4. Append matching virtual tag slugs\n+  5. Write merged list back: `entity.set(\\\"tags\\\", json!(merged_tags))`\n+- All callers of `enrich_all_task_entities` — pass the registry\n+- `swissarmyhammer-kanban/src/task_helpers.rs` — also update `enrich_task_entity()` (single-task variant) and `task_entity_to_rich_json()` if they produce tags independently\n\n-**Files to modify for filtering:**\n-- `swissarmyhammer-kanban/src/task/list.rs` — change tag filter from `task_tags(t)` to reading `filter_tags` field\n-- `swissarmyhammer-kanban/src/task/next.rs` — same change for tag filtering\n-- `swissarmyhammer-kanban/src/task_helpers.rs` — `task_entity_to_rich_json()` should include `virtual_tags` and `filter_tags` in output\n+The `apply()` path in `ParseBodyTags` must filter out virtual tag slugs before writing back to body (they're computed, not stored). This prevents virtual tags from being appended to body text when the multi-select editor writes back.\n\n-**No changes to `ParseBodyTags`** — the derive handler stays pure (body-only). Virtual tags are injected at the enrichment layer, not the derive layer.\n+**Files to modify for apply filtering:**\n+- `swissarmyhammer-kanban/src/derive_handlers.rs` — `ParseBodyTags::apply()` should accept a set of virtual tag slugs (or check the registry) and exclude them from the body-mutation diff\n\n ## Acceptance Criteria\n - [ ] `enrich_all_task_entities` accepts and uses a `VirtualTagRegistry`\n-- [ ] `virtual_tags` field set on each entity with matching virtual tag slugs\n-- [ ] `filter_tags` field set as union of `tags` + `virtual_tags`\n-- [ ] `tags` field is NOT modified (still body-parsed only)\n-- [ ] `list tasks --tag BLOCKED` finds tasks with the BLOCKED virtual tag via `filter_tags`\n-- [ ] `next task --tag READY` finds ready tasks via `filter_tags`\n+- [ ] A task with no deps has `READY` in its tags (once READY strategy is registered)\n+- [ ] A task with unmet deps has `BLOCKED` in its tags\n+- [ ] Virtual tags appear alongside body-parsed tags in the merged list\n+- [ ] `ParseBodyTags::apply()` does not write virtual tag slugs into the body\n - [ ] All callers pass the registry\n\n ## Tests\n-- [ ] Unit test: enrich with mock strategy, verify `virtual_tags` field contains strategy slug\n-- [ ] Unit test: enrich, verify `filter_tags` is union of `tags` and `virtual_tags`\n-- [ ] Unit test: enrich, verify `tags` field unchanged (no virtual slugs)\n-- [ ] Integration test: `list tasks --tag BLOCKED` returns blocked task\n-- [ ] Integration test: `list tasks --tag bug` still works (body-parsed tag)\n+- [ ] Unit test: enrich with mock strategy, verify virtual tag appears in tags array\n+- [ ] Unit test: apply() with virtual tag in desired set, verify it's NOT added to body\n+- [ ] Integration test: create task with dependency, verify BLOCKED tag appears\n - [ ] `cargo nextest run -p swissarmyhammer-kanban` passes\n\n ## Workflow\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-VT-4: Merge virtual tags into task enrichment pipeline\n\\ No newline at end of file\n+VT-4: Populate virtual_tags and filter_tags in enrichment pipeline\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-VT-4: Populate virtual_tags and filter_tags in enrichment pipeline\n\\ No newline at end of file\n+VT-4: Merge virtual tags into task enrichment pipeline\n\\ No newline at end of file\n"}]],"transaction_id":"01KNERDGBEKTFP6PM7DTT1NX5D"}
{"id":"01KNESHR816K9F4C4N5GMC2RNR","timestamp":"2026-04-05T12:23:13.409530Z","entity_type":"task","entity_id":"01KND4KPNAEPKSDEN98TFYBEH1","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -4,6 +4,8 @@\n\n **Key principle**: `tags` is body-parsed and editable. `virtual_tags` is computed from strategies and display-only. `filter_tags = tags ∪ virtual_tags` and is hidden — used only for backend filtering.\n\n+### Enrichment pipeline changes\n+\n **Files to modify:**\n - `swissarmyhammer-kanban/src/task_helpers.rs` — in `enrich_all_task_entities()`:\n   1. Accept a `&VirtualTagRegistry` parameter\n@@ -10,13 +12,18 @@\n   2. After computing ready/blocked/blocks, evaluate virtual tags for each task\n   3. Set `virtual_tags` field on entity with matching virtual tag slugs\n   4. Read `tags` (body-parsed) and `virtual_tags`, compute union, set `filter_tags`\n-- `swissarmyhammer-kanban/src/task_helpers.rs` — also update `enrich_task_entity()` (single-task variant) to accept and use registry\n+- `swissarmyhammer-kanban/src/task_helpers.rs` — also update `enrich_task_entity()` (single-task variant)\n - All callers of `enrich_all_task_entities` — pass the registry (main caller: `kanban-app/src/commands.rs:226`)\n\n-**Files to modify for filtering:**\n-- `swissarmyhammer-kanban/src/task/list.rs` — change tag filter from `task_tags(t)` to reading `filter_tags` field\n-- `swissarmyhammer-kanban/src/task/next.rs` — same change for tag filtering\n-- `swissarmyhammer-kanban/src/task_helpers.rs` — `task_entity_to_rich_json()` should include `virtual_tags` and `filter_tags` in output\n+### Migrate filtering to EntityFilterContext\n+\n+Replace ad-hoc inline `.filter()` closures with `EntityContext::list_where()` from VT-0.\n+\n+**Files to modify:**\n+- `swissarmyhammer-kanban/src/task/list.rs` — migrate tag filtering to use `list_where` with `EntityFilterContext`, reading `filter_tags` instead of `task_tags(t)`\n+- `swissarmyhammer-kanban/src/task/next.rs` — same migration for tag filtering\n+\n+The kanban layer injects its `VirtualTagRegistry` and terminal column ID into `EntityFilterContext` via `build_ctx`. The predicate reads `filter_tags` for tag matching — this means `list tasks --tag BLOCKED` works transparently.\n\n **No changes to `ParseBodyTags`** — the derive handler stays pure (body-only). Virtual tags are injected at the enrichment layer, not the derive layer.\n\n@@ -27,6 +34,7 @@\n - [ ] `tags` field is NOT modified (still body-parsed only)\n - [ ] `list tasks --tag BLOCKED` finds tasks with the BLOCKED virtual tag via `filter_tags`\n - [ ] `next task --tag READY` finds ready tasks via `filter_tags`\n+- [ ] Filtering uses `EntityContext::list_where` with `EntityFilterContext`, not inline closures\n - [ ] All callers pass the registry\n\n ## Tests\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,6 @@\n\n **Key principle**: `tags` is body-parsed and editable. `virtual_tags` is computed from strategies and display-only. `filter_tags = tags ∪ virtual_tags` and is hidden — used only for backend filtering.\n\n-### Enrichment pipeline changes\n-\n **Files to modify:**\n - `swissarmyhammer-kanban/src/task_helpers.rs` — in `enrich_all_task_entities()`:\n   1. Accept a `&VirtualTagRegistry` parameter\n@@ -12,19 +10,14 @@\n   2. After computing ready/blocked/blocks, evaluate virtual tags for each task\n   3. Set `virtual_tags` field on entity with matching virtual tag slugs\n   4. Read `tags` (body-parsed) and `virtual_tags`, compute union, set `filter_tags`\n-- `swissarmyhammer-kanban/src/task_helpers.rs` — also update `enrich_task_entity()` (single-task variant)\n+- `swissarmyhammer-kanban/src/task_helpers.rs` — also update `enrich_task_entity()` (single-task variant) to accept and use registry\n - All callers of `enrich_all_task_entities` — pass the registry (main caller: `kanban-app/src/commands.rs:226`)\n\n-### Migrate filtering to EntityFilterContext\n+**Files to modify for filtering:**\n+- `swissarmyhammer-kanban/src/task/list.rs` — change tag filter from `task_tags(t)` to reading `filter_tags` field\n+- `swissarmyhammer-kanban/src/task/next.rs` — same change for tag filtering\n+- `swissarmyhammer-kanban/src/task_helpers.rs` — `task_entity_to_rich_json()` should include `virtual_tags` and `filter_tags` in output\n\n-Replace ad-hoc inline `.filter()` closures with `EntityContext::list_where()` from VT-0.\n-\n-**Files to modify:**\n-- `swissarmyhammer-kanban/src/task/list.rs` — migrate tag filtering to use `list_where` with `EntityFilterContext`, reading `filter_tags` instead of `task_tags(t)`\n-- `swissarmyhammer-kanban/src/task/next.rs` — same migration for tag filtering\n-\n-The kanban layer injects its `VirtualTagRegistry` and terminal column ID into `EntityFilterContext` via `build_ctx`. The predicate reads `filter_tags` for tag matching — this means `list tasks --tag BLOCKED` works transparently.\n-\n **No changes to `ParseBodyTags`** — the derive handler stays pure (body-only). Virtual tags are injected at the enrichment layer, not the derive layer.\n\n ## Acceptance Criteria\n@@ -34,7 +27,6 @@\n - [ ] `tags` field is NOT modified (still body-parsed only)\n - [ ] `list tasks --tag BLOCKED` finds tasks with the BLOCKED virtual tag via `filter_tags`\n - [ ] `next task --tag READY` finds ready tasks via `filter_tags`\n-- [ ] Filtering uses `EntityContext::list_where` with `EntityFilterContext`, not inline closures\n - [ ] All callers pass the registry\n\n ## Tests\n"}],["depends_on",{"kind":"changed","old_value":["01KND4JJV437GTZ7QFQ3JBM2F1"],"new_value":["01KND4JJV437GTZ7QFQ3JBM2F1","01KNESGZ1B8G4JKAB1231YTM9J"]}]],"transaction_id":"01KNESHR7MEAR47NDAY63031VV"}
{"id":"01KNEVQ59N92VZQX1XRS99KRCS","timestamp":"2026-04-05T13:01:07.765735Z","entity_type":"task","entity_id":"01KND4KPNAEPKSDEN98TFYBEH1","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8380\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8380\n\\ No newline at end of file\n"}]],"transaction_id":"01KNEVQ3E6800N8F7SV8BQB0BY"}
{"id":"01KNEW0SX9AE7ZG4XKR256B4G4","timestamp":"2026-04-05T13:06:23.785731Z","entity_type":"task","entity_id":"01KND4KPNAEPKSDEN98TFYBEH1","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffffffda80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffda80\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]],"transaction_id":"01KNEW0R5HZ44VB80T8DSG8Q01"}
{"id":"01KQ5NTD7WKK6PNK8TNVFC4XF2","timestamp":"2026-04-26T19:56:16.508841Z","op":"update","item_id":"01KND4KPNAEPKSDEN98TFYBEH1","forward_patch":"--- original\n+++ modified\n@@ -5,7 +5,7 @@\n - 01KND4JJV437GTZ7QFQ3JBM2F1\n - 01KNESGZ1B8G4JKAB1231YTM9J\n position_column: done\n-position_ordinal: ffffffffffffffffda80\n+position_ordinal: ffffffffffffffffffff8b80\n title: 'VT-4: Populate virtual_tags and filter_tags in enrichment pipeline'\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -5,7 +5,7 @@\n - 01KND4JJV437GTZ7QFQ3JBM2F1\n - 01KNESGZ1B8G4JKAB1231YTM9J\n position_column: done\n-position_ordinal: ffffffffffffffffffff8b80\n+position_ordinal: ffffffffffffffffda80\n title: 'VT-4: Populate virtual_tags and filter_tags in enrichment pipeline'\n ---\n ## What\n","transaction_id":null}
{"id":"01KQ5NTD7X61XS2QDEW620EWBX","timestamp":"2026-04-26T19:56:16.509107Z","entity_type":"task","entity_id":"01KND4KPNAEPKSDEN98TFYBEH1","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffda80\n\\ No newline at end of file\n+ffffffffffffffffffff8b80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffff8b80\n\\ No newline at end of file\n+ffffffffffffffffda80\n\\ No newline at end of file\n"}]]}

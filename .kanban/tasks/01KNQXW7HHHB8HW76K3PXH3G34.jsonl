{"id":"01KPZPNJGMNGKJF6KJXVMRS6DH","timestamp":"2026-04-24T12:15:40.052131Z","op":"update","item_id":"01KNQXW7HHHB8HW76K3PXH3G34","forward_patch":"--- original\n+++ modified\n@@ -6,7 +6,7 @@\n position_column: todo\n position_ordinal: a180\n project: spatial-nav\n-title: 'Spatial registry: Rust-side spatial state, React-side rect measurement'\n+title: 'Spatial registry: layer forest, typed keys, React measures rects'\n ---\n ## What\n\n@@ -13,7 +13,7 @@\n Add spatial awareness to the focus system with a clear frontend/backend split:\n\n - **React** measures DOM rects and reports them to Rust. React owns `FocusScope` (measures its element) and `FocusLayer` (declares a layer boundary).\n-- **Rust** owns the spatial registry, layer stack, and navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n+- **Rust** owns the spatial registry, the **layer forest**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n\n ### Keying model\n\n@@ -20,11 +20,44 @@\n Two separate key systems:\n\n - **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch. Unchanged. Non-entity UI elements use a `\"ui:region.action\"` convention (e.g., `\"ui:toolbar.newTask\"`, `\"ui:tabbar.board1\"`).\n-- **Spatial key** (ULID) — unique per FocusScope/FocusLayer *instance*. Generated once per mount via `useRef(ulid())`. Stable across re-renders, new on remount. This is the primary key in the Rust spatial registry.\n+- **SpatialKey** (ULID) — unique per FocusScope/FocusLayer *instance*. Generated once per mount via `useRef(ulid())`. Stable across re-renders, new on remount. This is the primary key in the Rust spatial registry.\n\n-Why separate: the same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own spatial key, own rect, possibly different layer. Rust stores `HashMap<SpatialKey, SpatialEntry>` where `SpatialEntry = { moniker, rect, layer_key }`. `navigate()` returns the winning entry's **moniker** (what `setFocus` needs).\n+Why separate: the same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey, own rect, possibly different layer. `navigate()` returns the winning entry's **moniker** (what `setFocus` needs).\n+\n+### Layer model: a forest, not a stack\n\n-FocusLayer also gets a ULID spatial key so the layer stack handles multiple instances of the same named layer (e.g., two inspector panels in two windows).\n+**The app is multi-window.** `UIState.windows: HashMap<String, WindowState>` already exists, with each window maintaining its own `inspector_stack`. The layer model must reflect this: multiple **parallel root layers** (one per window), each with **stacked child layers** (inspectors, dialogs) on top.\n+\n+```rust\n+pub struct LayerEntry {\n+    pub key: LayerKey,\n+    pub name: String,                 // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n+    pub parent: Option<LayerKey>,     // None for window roots; Some(...) for stacked layers\n+    pub window_label: WindowLabel,    // which window this layer lives in\n+    pub last_focused: Option<SpatialKey>, // focus memory for pop-restore\n+}\n+\n+pub struct SpatialRegistry {\n+    entries: HashMap<SpatialKey, SpatialEntry>,\n+    layers:  HashMap<LayerKey, LayerEntry>,\n+}\n+```\n+\n+A **layer forest**: trees rooted at each window's root layer. An inspector opened in window A has `parent = Some(windowA_root_layer_key)`. A confirm dialog on top of that inspector has `parent = Some(inspector_layer_key)`. Two windows each with their own inspector → 4 layers total, 2 roots.\n+\n+Layers are removed by key (not pop) so arbitrary unmount order works — inspector A opens, dialog B opens on top, A closes first → `remove(A.key)`, B stays with its old parent pointer (valid — B's parent is the window, not A, if declared correctly by the component tree).\n+\n+### Newtypes\n+\n+Use the newtypes from card `01KNM3YHHFJ3PTXZHD9EFKVBS6` (focus claim registry):\n+\n+```rust\n+pub struct WindowLabel(pub String);\n+pub struct SpatialKey(pub String);\n+pub struct LayerKey(pub String);\n+```\n+\n+All three have `#[serde(transparent)]` so the wire format stays a plain string. React sees strings, Rust has strong typing.\n\n ### Architecture\n\n@@ -34,51 +67,56 @@\n FocusScope mounts\n   spatialKey = useRef(ulid())\n   → ResizeObserver fires\n-  → invoke: spatial.register           → registry.insert(spatial_key, { moniker, rect, layer_key })\n-    { key, moniker, rect, layer_key }\n+  → invoke: spatial_register           → registry.insert(spatial_key,\n+    { key, moniker, rect, layer_key }     { moniker, rect, layer_key })\n\n FocusScope unmounts\n-  → invoke: spatial.unregister         → registry.remove(spatial_key)\n+  → invoke: spatial_unregister         → registry.remove(spatial_key)\n     { key }\n\n FocusLayer mounts\n   layerKey = useRef(ulid())\n-  → invoke: spatial.push_layer         → layer_stack.push({ key, name })\n-    { key, name }\n+  parentLayerKey = nearest ancestor FocusLayer key (via context)\n+                 = None if this is a window root\n+  → invoke: spatial_push_layer         → layers.insert(key,\n+    { key, name, parent_key }             { name, parent, window_label })\n+                                          (window_label derived from tauri::Window)\n\n FocusLayer unmounts\n-  → invoke: spatial.pop_layer          → layer_stack.remove(key)\n+  → invoke: spatial_pop_layer          → layers.remove(key)\n     { key }\n\n-nav.up key pressed\n-  → invoke: spatial.navigate           → find_target(direction, focused_moniker)\n-    { direction, focused_moniker }       filter to active layer, score by geometry\n-  ← returns target moniker             ← winning entry's moniker\n-  → setFocus(target)\n+nav.right key pressed\n+  → invoke: spatial_navigate           → find_target(direction, focused_key)\n+    { key, direction }                    filter to focused's layer_key, score\n+  ← returns Ok                         ← emit focus-changed event with moniker\n ```\n\n ### Rust side\n\n **`SpatialEntry`** struct:\n-- `spatial_key: String` (ULID from React)\n - `moniker: String`\n - `rect: Rect { x: f64, y: f64, width: f64, height: f64 }`\n-- `layer_key: String` (ULID of the FocusLayer this scope lives in)\n+- `layer_key: LayerKey` — which layer this scope lives in\n\n-**`SpatialRegistry`** struct:\n-- `entries: HashMap<String, SpatialEntry>` — keyed by spatial_key\n-- `register(key, moniker, rect, layer_key)` / `unregister(key)` / `update_rect(key, rect)`\n+**`SpatialRegistry`** ops:\n+- `register(key: SpatialKey, moniker, rect, layer_key)`\n+- `unregister(key: SpatialKey)`\n+- `update_rect(key: SpatialKey, rect)`\n+- `push_layer(key: LayerKey, name, parent_key: Option<LayerKey>, window_label: WindowLabel)`\n+- `remove_layer(key: LayerKey)`\n+- `children_of(key: LayerKey) -> Vec<&LayerEntry>` — layers with this as parent\n+- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>` — the window root layer\n+- `ancestors_of(key: LayerKey) -> Vec<&LayerEntry>` — walk up to root\n+- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>` — for nav scoping\n\n-**`LayerStack`** — `Vec<LayerEntry>` where `LayerEntry = { key: String, name: String }`:\n-- `push(key, name)` / `remove(key)` / `active() -> Option<&LayerEntry>` (topmost)\n-- `remove` by key (not pop) — handles out-of-order unmount if an inner layer unmounts before an outer one\n+**No global \"active layer\"**. Navigation is always scoped to the focused entry's `layer_key` (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`).\n\n **Tauri commands**:\n-- `spatial_register(key, moniker, x, y, w, h, layer_key)`\n-- `spatial_unregister(key)`\n-- `spatial_push_layer(key, name)`\n-- `spatial_pop_layer(key)`\n-- `spatial_navigate(direction, focused_moniker) -> Option<String>` — returns target moniker\n+- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, layer_key)`\n+- `spatial_unregister(window: tauri::Window, key)`\n+- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)` — derives window_label from the `tauri::Window` param\n+- `spatial_pop_layer(window: tauri::Window, key)`\n\n ### React side\n\n@@ -85,53 +123,59 @@\n **`FocusLayer`** component (`kanban-app/ui/src/components/focus-layer.tsx`):\n - Props: `name: string`, `children`\n - `const layerKey = useRef(ulid()).current`\n-- Provides `layerKey` via React context (so FocusScope can read it)\n-- On mount: `invoke(\"spatial_push_layer\", { key: layerKey, name })`\n+- Reads parent FocusLayer's key from context (throws if a non-root layer has no parent — but the root layer in the window shell is allowed to have `parent = undefined`)\n+- Provides `layerKey` via React context so descendant FocusScope and FocusLayer can read it\n+- On mount: `invoke(\"spatial_push_layer\", { key: layerKey, name, parentKey })`\n - On unmount: `invoke(\"spatial_pop_layer\", { key: layerKey })`\n\n **`FocusScope`** changes (`kanban-app/ui/src/components/focus-scope.tsx`):\n - `const spatialKey = useRef(ulid()).current`\n-- Read `layerKey` from FocusLayer context (throw if missing)\n+- Reads `layerKey` from FocusLayer context (throw if missing — every scope must be inside a layer)\n - Add `ResizeObserver` on the wrapper div\n-- On mount/resize: `invoke(\"spatial_register\", { key: spatialKey, moniker, ...rect, layer_key: layerKey })`\n+- On mount/resize: `invoke(\"spatial_register\", { key: spatialKey, moniker, ...rect, layerKey })`\n - On unmount: `invoke(\"spatial_unregister\", { key: spatialKey })`\n\n-**`app-shell.tsx`**:\n-- Wrap app root in `<FocusLayer name=\"window\">`\n-\n-**`entity-focus-context.tsx`** — `broadcastNavCommand`:\n-1. Evaluate `claimWhen` predicates (backward compat, temporary)\n-2. If no claim: `const target = await invoke(\"spatial_navigate\", { direction, focused_moniker })`\n-3. If target: `setFocus(target)`\n+**`App.tsx`** (or `app-shell.tsx`):\n+- Wrap app root in `<FocusLayer name=\"window\">` — this is the window root layer (no parent)\n\n ### Design decisions\n-- **ULID per instance**: `useRef(ulid())` — stable across re-renders, unique per mount. No global counter, no collision risk.\n-- **Layer removal by key, not pop**: Layer stack supports arbitrary removal order. If inspector A opens, then dialog B opens on top, then A closes — `remove(A.key)` works correctly. B is still on top.\n-- **Moniker for focus, key for registry**: `navigate()` returns monikers because `setFocus` and command dispatch use monikers. The spatial key is internal plumbing.\n-- **Non-entity monikers**: UI chrome uses `\"ui:region.action\"` convention. These participate in spatial nav and can register commands in their FocusScope, but don't go through entity command dispatch.\n+- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. Serde-transparent so the wire protocol stays simple.\n+- **Layers form a forest, not a stack**: multi-window support is a first-class concern, not a retrofit. Two windows = two trees.\n+- **Parent pointer, not child list**: simpler to add/remove. `children_of(key)` walks the map once; fine for small layer counts (O(layers) rather than O(1), but we have < ~20 layers tops).\n+- **Layer removal by key**: supports arbitrary removal order.\n+- **window_label derived, not passed**: React never sends window_label — Tauri knows it from the invoking webview. One less thing for React to get wrong.\n\n ### Subtasks\n-- [ ] Define `Rect`, `SpatialEntry`, `SpatialRegistry`, `LayerEntry`, `LayerStack` in Rust\n-- [ ] Implement registry and layer stack with key-based operations\n-- [ ] Add Tauri commands wiring React to Rust\n-- [ ] Create FocusLayer component with ULID key + Tauri invokes\n-- [ ] Update FocusScope with ULID spatial key + ResizeObserver + Tauri invokes\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]` (shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`)\n+- [ ] Define `Rect`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry` in Rust\n+- [ ] Implement registry with forest ops: `children_of`, `root_for_window`, `ancestors_of`, `entries_in_layer`\n+- [ ] Add Tauri commands that derive `window_label` from the `tauri::Window` param\n+- [ ] Create FocusLayer component with ULID layer key, parent context, Tauri invokes\n+- [ ] Update FocusScope with ULID spatial key, ResizeObserver, Tauri invokes\n+- [ ] Wrap app root in `<FocusLayer name=\"window\">` (window root)\n\n ## Acceptance Criteria\n-- [ ] Each FocusScope mount generates a unique ULID spatial key via `useRef`\n-- [ ] Each FocusLayer mount generates a unique ULID layer key via `useRef`\n-- [ ] Spatial registry keyed by spatial key — same moniker in two locations = two entries\n-- [ ] Layer stack supports arbitrary removal order (not just pop)\n-- [ ] `navigate()` returns target moniker (not spatial key)\n-- [ ] Root `<FocusLayer name=\"window\">` wraps the app\n+- [ ] Spatial and layer keys use newtypes throughout Rust; wire format is plain strings\n+- [ ] Each FocusScope mount generates a unique ULID `SpatialKey` via `useRef`\n+- [ ] Each FocusLayer mount generates a unique ULID `LayerKey` via `useRef`\n+- [ ] Every layer carries its `window_label` (derived from Tauri, not from React args)\n+- [ ] Root window layers have `parent = None`; stacked layers have `parent = Some(...)`\n+- [ ] The registry can represent: 2 windows + 2 inspectors (one per window) + 1 dialog on top of one of the inspectors = 5 layers, 2 roots, correct parent chains\n+- [ ] Spatial registry keyed by `SpatialKey` — same moniker in two locations = two entries\n+- [ ] Layer removal by key supports arbitrary removal order\n - [ ] Existing focus behavior unchanged\n - [ ] `cargo test` passes, `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `Rust unit tests` — registry: register two entries with same moniker, different keys — both stored; unregister one, other remains\n-- [ ] `Rust unit tests` — layer stack: push A, push B, remove A — B is still active; remove B — stack empty\n-- [ ] `kanban-app/ui/src/components/focus-layer.test.tsx` — ULID generated on mount, stable across re-renders, new on remount\n-- [ ] `kanban-app/ui/src/components/focus-scope.test.tsx` — spatial key generated, registered with layer key from context\n+- [ ] Rust: register two entries with same moniker, different keys — both stored; unregister one, other remains\n+- [ ] Rust: push window root A, push window root B (both `parent = None`, different `window_label`) — both coexist as siblings\n+- [ ] Rust: push window A, push inspector I (parent = A), push dialog D (parent = I) — `ancestors_of(D) == [I, A]`, `root_for_window(A.window_label) == Some(A)`\n+- [ ] Rust: `children_of(window_root)` returns inspectors of that window only, not inspectors of another window\n+- [ ] Rust: remove inspector I while dialog D still present — D remains in registry with its (now dangling) parent pointer; nav in D's layer still works (layer ops don't follow parent)\n+- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key` — window's entries excluded even though window is the parent\n+- [ ] Rust: layer removal — remove non-existent key is a no-op, not an error\n+- [ ] `focus-layer.test.tsx` — ULID generated on mount, stable across re-renders, new on remount; nested layer reads parent from context and sends `parent_key` on push\n+- [ ] `focus-scope.test.tsx` — spatial key generated, registered with layer key from context; throws if rendered outside a FocusLayer\n - [ ] Run `cargo test` and `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -6,7 +6,7 @@\n position_column: todo\n position_ordinal: a180\n project: spatial-nav\n-title: 'Spatial registry: layer forest, typed keys, React measures rects'\n+title: 'Spatial registry: Rust-side spatial state, React-side rect measurement'\n ---\n ## What\n\n@@ -13,7 +13,7 @@\n Add spatial awareness to the focus system with a clear frontend/backend split:\n\n - **React** measures DOM rects and reports them to Rust. React owns `FocusScope` (measures its element) and `FocusLayer` (declares a layer boundary).\n-- **Rust** owns the spatial registry, the **layer forest**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n+- **Rust** owns the spatial registry, layer stack, and navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n\n ### Keying model\n\n@@ -20,44 +20,11 @@\n Two separate key systems:\n\n - **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch. Unchanged. Non-entity UI elements use a `\"ui:region.action\"` convention (e.g., `\"ui:toolbar.newTask\"`, `\"ui:tabbar.board1\"`).\n-- **SpatialKey** (ULID) — unique per FocusScope/FocusLayer *instance*. Generated once per mount via `useRef(ulid())`. Stable across re-renders, new on remount. This is the primary key in the Rust spatial registry.\n-\n-Why separate: the same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey, own rect, possibly different layer. `navigate()` returns the winning entry's **moniker** (what `setFocus` needs).\n-\n-### Layer model: a forest, not a stack\n-\n-**The app is multi-window.** `UIState.windows: HashMap<String, WindowState>` already exists, with each window maintaining its own `inspector_stack`. The layer model must reflect this: multiple **parallel root layers** (one per window), each with **stacked child layers** (inspectors, dialogs) on top.\n-\n-```rust\n-pub struct LayerEntry {\n-    pub key: LayerKey,\n-    pub name: String,                 // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n-    pub parent: Option<LayerKey>,     // None for window roots; Some(...) for stacked layers\n-    pub window_label: WindowLabel,    // which window this layer lives in\n-    pub last_focused: Option<SpatialKey>, // focus memory for pop-restore\n-}\n-\n-pub struct SpatialRegistry {\n-    entries: HashMap<SpatialKey, SpatialEntry>,\n-    layers:  HashMap<LayerKey, LayerEntry>,\n-}\n-```\n-\n-A **layer forest**: trees rooted at each window's root layer. An inspector opened in window A has `parent = Some(windowA_root_layer_key)`. A confirm dialog on top of that inspector has `parent = Some(inspector_layer_key)`. Two windows each with their own inspector → 4 layers total, 2 roots.\n+- **Spatial key** (ULID) — unique per FocusScope/FocusLayer *instance*. Generated once per mount via `useRef(ulid())`. Stable across re-renders, new on remount. This is the primary key in the Rust spatial registry.\n\n-Layers are removed by key (not pop) so arbitrary unmount order works — inspector A opens, dialog B opens on top, A closes first → `remove(A.key)`, B stays with its old parent pointer (valid — B's parent is the window, not A, if declared correctly by the component tree).\n-\n-### Newtypes\n-\n-Use the newtypes from card `01KNM3YHHFJ3PTXZHD9EFKVBS6` (focus claim registry):\n-\n-```rust\n-pub struct WindowLabel(pub String);\n-pub struct SpatialKey(pub String);\n-pub struct LayerKey(pub String);\n-```\n+Why separate: the same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own spatial key, own rect, possibly different layer. Rust stores `HashMap<SpatialKey, SpatialEntry>` where `SpatialEntry = { moniker, rect, layer_key }`. `navigate()` returns the winning entry's **moniker** (what `setFocus` needs).\n\n-All three have `#[serde(transparent)]` so the wire format stays a plain string. React sees strings, Rust has strong typing.\n+FocusLayer also gets a ULID spatial key so the layer stack handles multiple instances of the same named layer (e.g., two inspector panels in two windows).\n\n ### Architecture\n\n@@ -67,56 +34,51 @@\n FocusScope mounts\n   spatialKey = useRef(ulid())\n   → ResizeObserver fires\n-  → invoke: spatial_register           → registry.insert(spatial_key,\n-    { key, moniker, rect, layer_key }     { moniker, rect, layer_key })\n+  → invoke: spatial.register           → registry.insert(spatial_key, { moniker, rect, layer_key })\n+    { key, moniker, rect, layer_key }\n\n FocusScope unmounts\n-  → invoke: spatial_unregister         → registry.remove(spatial_key)\n+  → invoke: spatial.unregister         → registry.remove(spatial_key)\n     { key }\n\n FocusLayer mounts\n   layerKey = useRef(ulid())\n-  parentLayerKey = nearest ancestor FocusLayer key (via context)\n-                 = None if this is a window root\n-  → invoke: spatial_push_layer         → layers.insert(key,\n-    { key, name, parent_key }             { name, parent, window_label })\n-                                          (window_label derived from tauri::Window)\n+  → invoke: spatial.push_layer         → layer_stack.push({ key, name })\n+    { key, name }\n\n FocusLayer unmounts\n-  → invoke: spatial_pop_layer          → layers.remove(key)\n+  → invoke: spatial.pop_layer          → layer_stack.remove(key)\n     { key }\n\n-nav.right key pressed\n-  → invoke: spatial_navigate           → find_target(direction, focused_key)\n-    { key, direction }                    filter to focused's layer_key, score\n-  ← returns Ok                         ← emit focus-changed event with moniker\n+nav.up key pressed\n+  → invoke: spatial.navigate           → find_target(direction, focused_moniker)\n+    { direction, focused_moniker }       filter to active layer, score by geometry\n+  ← returns target moniker             ← winning entry's moniker\n+  → setFocus(target)\n ```\n\n ### Rust side\n\n **`SpatialEntry`** struct:\n+- `spatial_key: String` (ULID from React)\n - `moniker: String`\n - `rect: Rect { x: f64, y: f64, width: f64, height: f64 }`\n-- `layer_key: LayerKey` — which layer this scope lives in\n+- `layer_key: String` (ULID of the FocusLayer this scope lives in)\n\n-**`SpatialRegistry`** ops:\n-- `register(key: SpatialKey, moniker, rect, layer_key)`\n-- `unregister(key: SpatialKey)`\n-- `update_rect(key: SpatialKey, rect)`\n-- `push_layer(key: LayerKey, name, parent_key: Option<LayerKey>, window_label: WindowLabel)`\n-- `remove_layer(key: LayerKey)`\n-- `children_of(key: LayerKey) -> Vec<&LayerEntry>` — layers with this as parent\n-- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>` — the window root layer\n-- `ancestors_of(key: LayerKey) -> Vec<&LayerEntry>` — walk up to root\n-- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>` — for nav scoping\n+**`SpatialRegistry`** struct:\n+- `entries: HashMap<String, SpatialEntry>` — keyed by spatial_key\n+- `register(key, moniker, rect, layer_key)` / `unregister(key)` / `update_rect(key, rect)`\n\n-**No global \"active layer\"**. Navigation is always scoped to the focused entry's `layer_key` (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`).\n+**`LayerStack`** — `Vec<LayerEntry>` where `LayerEntry = { key: String, name: String }`:\n+- `push(key, name)` / `remove(key)` / `active() -> Option<&LayerEntry>` (topmost)\n+- `remove` by key (not pop) — handles out-of-order unmount if an inner layer unmounts before an outer one\n\n **Tauri commands**:\n-- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, layer_key)`\n-- `spatial_unregister(window: tauri::Window, key)`\n-- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)` — derives window_label from the `tauri::Window` param\n-- `spatial_pop_layer(window: tauri::Window, key)`\n+- `spatial_register(key, moniker, x, y, w, h, layer_key)`\n+- `spatial_unregister(key)`\n+- `spatial_push_layer(key, name)`\n+- `spatial_pop_layer(key)`\n+- `spatial_navigate(direction, focused_moniker) -> Option<String>` — returns target moniker\n\n ### React side\n\n@@ -123,59 +85,53 @@\n **`FocusLayer`** component (`kanban-app/ui/src/components/focus-layer.tsx`):\n - Props: `name: string`, `children`\n - `const layerKey = useRef(ulid()).current`\n-- Reads parent FocusLayer's key from context (throws if a non-root layer has no parent — but the root layer in the window shell is allowed to have `parent = undefined`)\n-- Provides `layerKey` via React context so descendant FocusScope and FocusLayer can read it\n-- On mount: `invoke(\"spatial_push_layer\", { key: layerKey, name, parentKey })`\n+- Provides `layerKey` via React context (so FocusScope can read it)\n+- On mount: `invoke(\"spatial_push_layer\", { key: layerKey, name })`\n - On unmount: `invoke(\"spatial_pop_layer\", { key: layerKey })`\n\n **`FocusScope`** changes (`kanban-app/ui/src/components/focus-scope.tsx`):\n - `const spatialKey = useRef(ulid()).current`\n-- Reads `layerKey` from FocusLayer context (throw if missing — every scope must be inside a layer)\n+- Read `layerKey` from FocusLayer context (throw if missing)\n - Add `ResizeObserver` on the wrapper div\n-- On mount/resize: `invoke(\"spatial_register\", { key: spatialKey, moniker, ...rect, layerKey })`\n+- On mount/resize: `invoke(\"spatial_register\", { key: spatialKey, moniker, ...rect, layer_key: layerKey })`\n - On unmount: `invoke(\"spatial_unregister\", { key: spatialKey })`\n\n-**`App.tsx`** (or `app-shell.tsx`):\n-- Wrap app root in `<FocusLayer name=\"window\">` — this is the window root layer (no parent)\n+**`app-shell.tsx`**:\n+- Wrap app root in `<FocusLayer name=\"window\">`\n+\n+**`entity-focus-context.tsx`** — `broadcastNavCommand`:\n+1. Evaluate `claimWhen` predicates (backward compat, temporary)\n+2. If no claim: `const target = await invoke(\"spatial_navigate\", { direction, focused_moniker })`\n+3. If target: `setFocus(target)`\n\n ### Design decisions\n-- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. Serde-transparent so the wire protocol stays simple.\n-- **Layers form a forest, not a stack**: multi-window support is a first-class concern, not a retrofit. Two windows = two trees.\n-- **Parent pointer, not child list**: simpler to add/remove. `children_of(key)` walks the map once; fine for small layer counts (O(layers) rather than O(1), but we have < ~20 layers tops).\n-- **Layer removal by key**: supports arbitrary removal order.\n-- **window_label derived, not passed**: React never sends window_label — Tauri knows it from the invoking webview. One less thing for React to get wrong.\n+- **ULID per instance**: `useRef(ulid())` — stable across re-renders, unique per mount. No global counter, no collision risk.\n+- **Layer removal by key, not pop**: Layer stack supports arbitrary removal order. If inspector A opens, then dialog B opens on top, then A closes — `remove(A.key)` works correctly. B is still on top.\n+- **Moniker for focus, key for registry**: `navigate()` returns monikers because `setFocus` and command dispatch use monikers. The spatial key is internal plumbing.\n+- **Non-entity monikers**: UI chrome uses `\"ui:region.action\"` convention. These participate in spatial nav and can register commands in their FocusScope, but don't go through entity command dispatch.\n\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]` (shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`)\n-- [ ] Define `Rect`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry` in Rust\n-- [ ] Implement registry with forest ops: `children_of`, `root_for_window`, `ancestors_of`, `entries_in_layer`\n-- [ ] Add Tauri commands that derive `window_label` from the `tauri::Window` param\n-- [ ] Create FocusLayer component with ULID layer key, parent context, Tauri invokes\n-- [ ] Update FocusScope with ULID spatial key, ResizeObserver, Tauri invokes\n-- [ ] Wrap app root in `<FocusLayer name=\"window\">` (window root)\n+- [ ] Define `Rect`, `SpatialEntry`, `SpatialRegistry`, `LayerEntry`, `LayerStack` in Rust\n+- [ ] Implement registry and layer stack with key-based operations\n+- [ ] Add Tauri commands wiring React to Rust\n+- [ ] Create FocusLayer component with ULID key + Tauri invokes\n+- [ ] Update FocusScope with ULID spatial key + ResizeObserver + Tauri invokes\n\n ## Acceptance Criteria\n-- [ ] Spatial and layer keys use newtypes throughout Rust; wire format is plain strings\n-- [ ] Each FocusScope mount generates a unique ULID `SpatialKey` via `useRef`\n-- [ ] Each FocusLayer mount generates a unique ULID `LayerKey` via `useRef`\n-- [ ] Every layer carries its `window_label` (derived from Tauri, not from React args)\n-- [ ] Root window layers have `parent = None`; stacked layers have `parent = Some(...)`\n-- [ ] The registry can represent: 2 windows + 2 inspectors (one per window) + 1 dialog on top of one of the inspectors = 5 layers, 2 roots, correct parent chains\n-- [ ] Spatial registry keyed by `SpatialKey` — same moniker in two locations = two entries\n-- [ ] Layer removal by key supports arbitrary removal order\n+- [ ] Each FocusScope mount generates a unique ULID spatial key via `useRef`\n+- [ ] Each FocusLayer mount generates a unique ULID layer key via `useRef`\n+- [ ] Spatial registry keyed by spatial key — same moniker in two locations = two entries\n+- [ ] Layer stack supports arbitrary removal order (not just pop)\n+- [ ] `navigate()` returns target moniker (not spatial key)\n+- [ ] Root `<FocusLayer name=\"window\">` wraps the app\n - [ ] Existing focus behavior unchanged\n - [ ] `cargo test` passes, `pnpm vitest run` passes\n\n ## Tests\n-- [ ] Rust: register two entries with same moniker, different keys — both stored; unregister one, other remains\n-- [ ] Rust: push window root A, push window root B (both `parent = None`, different `window_label`) — both coexist as siblings\n-- [ ] Rust: push window A, push inspector I (parent = A), push dialog D (parent = I) — `ancestors_of(D) == [I, A]`, `root_for_window(A.window_label) == Some(A)`\n-- [ ] Rust: `children_of(window_root)` returns inspectors of that window only, not inspectors of another window\n-- [ ] Rust: remove inspector I while dialog D still present — D remains in registry with its (now dangling) parent pointer; nav in D's layer still works (layer ops don't follow parent)\n-- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key` — window's entries excluded even though window is the parent\n-- [ ] Rust: layer removal — remove non-existent key is a no-op, not an error\n-- [ ] `focus-layer.test.tsx` — ULID generated on mount, stable across re-renders, new on remount; nested layer reads parent from context and sends `parent_key` on push\n-- [ ] `focus-scope.test.tsx` — spatial key generated, registered with layer key from context; throws if rendered outside a FocusLayer\n+- [ ] `Rust unit tests` — registry: register two entries with same moniker, different keys — both stored; unregister one, other remains\n+- [ ] `Rust unit tests` — layer stack: push A, push B, remove A — B is still active; remove B — stack empty\n+- [ ] `kanban-app/ui/src/components/focus-layer.test.tsx` — ULID generated on mount, stable across re-renders, new on remount\n+- [ ] `kanban-app/ui/src/components/focus-scope.test.tsx` — spatial key generated, registered with layer key from context\n - [ ] Run `cargo test` and `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","transaction_id":null}
{"id":"01KPZPNJGMX8TYVJGBD8GJXNM7","timestamp":"2026-04-24T12:15:40.052900Z","entity_type":"task","entity_id":"01KNQXW7HHHB8HW76K3PXH3G34","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n Add spatial awareness to the focus system with a clear frontend/backend split:\n\n - **React** measures DOM rects and reports them to Rust. React owns `FocusScope` (measures its element) and `FocusLayer` (declares a layer boundary).\n-- **Rust** owns the spatial registry, layer stack, and navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n+- **Rust** owns the spatial registry, the **layer forest**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n\n ### Keying model\n\n@@ -10,11 +10,44 @@\n Two separate key systems:\n\n - **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch. Unchanged. Non-entity UI elements use a `\"ui:region.action\"` convention (e.g., `\"ui:toolbar.newTask\"`, `\"ui:tabbar.board1\"`).\n-- **Spatial key** (ULID) — unique per FocusScope/FocusLayer *instance*. Generated once per mount via `useRef(ulid())`. Stable across re-renders, new on remount. This is the primary key in the Rust spatial registry.\n+- **SpatialKey** (ULID) — unique per FocusScope/FocusLayer *instance*. Generated once per mount via `useRef(ulid())`. Stable across re-renders, new on remount. This is the primary key in the Rust spatial registry.\n+\n+Why separate: the same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey, own rect, possibly different layer. `navigate()` returns the winning entry's **moniker** (what `setFocus` needs).\n+\n+### Layer model: a forest, not a stack\n+\n+**The app is multi-window.** `UIState.windows: HashMap<String, WindowState>` already exists, with each window maintaining its own `inspector_stack`. The layer model must reflect this: multiple **parallel root layers** (one per window), each with **stacked child layers** (inspectors, dialogs) on top.\n+\n+```rust\n+pub struct LayerEntry {\n+    pub key: LayerKey,\n+    pub name: String,                 // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n+    pub parent: Option<LayerKey>,     // None for window roots; Some(...) for stacked layers\n+    pub window_label: WindowLabel,    // which window this layer lives in\n+    pub last_focused: Option<SpatialKey>, // focus memory for pop-restore\n+}\n+\n+pub struct SpatialRegistry {\n+    entries: HashMap<SpatialKey, SpatialEntry>,\n+    layers:  HashMap<LayerKey, LayerEntry>,\n+}\n+```\n+\n+A **layer forest**: trees rooted at each window's root layer. An inspector opened in window A has `parent = Some(windowA_root_layer_key)`. A confirm dialog on top of that inspector has `parent = Some(inspector_layer_key)`. Two windows each with their own inspector → 4 layers total, 2 roots.\n+\n+Layers are removed by key (not pop) so arbitrary unmount order works — inspector A opens, dialog B opens on top, A closes first → `remove(A.key)`, B stays with its old parent pointer (valid — B's parent is the window, not A, if declared correctly by the component tree).\n+\n+### Newtypes\n+\n+Use the newtypes from card `01KNM3YHHFJ3PTXZHD9EFKVBS6` (focus claim registry):\n\n-Why separate: the same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own spatial key, own rect, possibly different layer. Rust stores `HashMap<SpatialKey, SpatialEntry>` where `SpatialEntry = { moniker, rect, layer_key }`. `navigate()` returns the winning entry's **moniker** (what `setFocus` needs).\n+```rust\n+pub struct WindowLabel(pub String);\n+pub struct SpatialKey(pub String);\n+pub struct LayerKey(pub String);\n+```\n\n-FocusLayer also gets a ULID spatial key so the layer stack handles multiple instances of the same named layer (e.g., two inspector panels in two windows).\n+All three have `#[serde(transparent)]` so the wire format stays a plain string. React sees strings, Rust has strong typing.\n\n ### Architecture\n\n@@ -24,51 +57,56 @@\n FocusScope mounts\n   spatialKey = useRef(ulid())\n   → ResizeObserver fires\n-  → invoke: spatial.register           → registry.insert(spatial_key, { moniker, rect, layer_key })\n-    { key, moniker, rect, layer_key }\n+  → invoke: spatial_register           → registry.insert(spatial_key,\n+    { key, moniker, rect, layer_key }     { moniker, rect, layer_key })\n\n FocusScope unmounts\n-  → invoke: spatial.unregister         → registry.remove(spatial_key)\n+  → invoke: spatial_unregister         → registry.remove(spatial_key)\n     { key }\n\n FocusLayer mounts\n   layerKey = useRef(ulid())\n-  → invoke: spatial.push_layer         → layer_stack.push({ key, name })\n-    { key, name }\n+  parentLayerKey = nearest ancestor FocusLayer key (via context)\n+                 = None if this is a window root\n+  → invoke: spatial_push_layer         → layers.insert(key,\n+    { key, name, parent_key }             { name, parent, window_label })\n+                                          (window_label derived from tauri::Window)\n\n FocusLayer unmounts\n-  → invoke: spatial.pop_layer          → layer_stack.remove(key)\n+  → invoke: spatial_pop_layer          → layers.remove(key)\n     { key }\n\n-nav.up key pressed\n-  → invoke: spatial.navigate           → find_target(direction, focused_moniker)\n-    { direction, focused_moniker }       filter to active layer, score by geometry\n-  ← returns target moniker             ← winning entry's moniker\n-  → setFocus(target)\n+nav.right key pressed\n+  → invoke: spatial_navigate           → find_target(direction, focused_key)\n+    { key, direction }                    filter to focused's layer_key, score\n+  ← returns Ok                         ← emit focus-changed event with moniker\n ```\n\n ### Rust side\n\n **`SpatialEntry`** struct:\n-- `spatial_key: String` (ULID from React)\n - `moniker: String`\n - `rect: Rect { x: f64, y: f64, width: f64, height: f64 }`\n-- `layer_key: String` (ULID of the FocusLayer this scope lives in)\n+- `layer_key: LayerKey` — which layer this scope lives in\n\n-**`SpatialRegistry`** struct:\n-- `entries: HashMap<String, SpatialEntry>` — keyed by spatial_key\n-- `register(key, moniker, rect, layer_key)` / `unregister(key)` / `update_rect(key, rect)`\n+**`SpatialRegistry`** ops:\n+- `register(key: SpatialKey, moniker, rect, layer_key)`\n+- `unregister(key: SpatialKey)`\n+- `update_rect(key: SpatialKey, rect)`\n+- `push_layer(key: LayerKey, name, parent_key: Option<LayerKey>, window_label: WindowLabel)`\n+- `remove_layer(key: LayerKey)`\n+- `children_of(key: LayerKey) -> Vec<&LayerEntry>` — layers with this as parent\n+- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>` — the window root layer\n+- `ancestors_of(key: LayerKey) -> Vec<&LayerEntry>` — walk up to root\n+- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>` — for nav scoping\n\n-**`LayerStack`** — `Vec<LayerEntry>` where `LayerEntry = { key: String, name: String }`:\n-- `push(key, name)` / `remove(key)` / `active() -> Option<&LayerEntry>` (topmost)\n-- `remove` by key (not pop) — handles out-of-order unmount if an inner layer unmounts before an outer one\n+**No global \"active layer\"**. Navigation is always scoped to the focused entry's `layer_key` (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`).\n\n **Tauri commands**:\n-- `spatial_register(key, moniker, x, y, w, h, layer_key)`\n-- `spatial_unregister(key)`\n-- `spatial_push_layer(key, name)`\n-- `spatial_pop_layer(key)`\n-- `spatial_navigate(direction, focused_moniker) -> Option<String>` — returns target moniker\n+- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, layer_key)`\n+- `spatial_unregister(window: tauri::Window, key)`\n+- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)` — derives window_label from the `tauri::Window` param\n+- `spatial_pop_layer(window: tauri::Window, key)`\n\n ### React side\n\n@@ -75,53 +113,59 @@\n **`FocusLayer`** component (`kanban-app/ui/src/components/focus-layer.tsx`):\n - Props: `name: string`, `children`\n - `const layerKey = useRef(ulid()).current`\n-- Provides `layerKey` via React context (so FocusScope can read it)\n-- On mount: `invoke(\"spatial_push_layer\", { key: layerKey, name })`\n+- Reads parent FocusLayer's key from context (throws if a non-root layer has no parent — but the root layer in the window shell is allowed to have `parent = undefined`)\n+- Provides `layerKey` via React context so descendant FocusScope and FocusLayer can read it\n+- On mount: `invoke(\"spatial_push_layer\", { key: layerKey, name, parentKey })`\n - On unmount: `invoke(\"spatial_pop_layer\", { key: layerKey })`\n\n **`FocusScope`** changes (`kanban-app/ui/src/components/focus-scope.tsx`):\n - `const spatialKey = useRef(ulid()).current`\n-- Read `layerKey` from FocusLayer context (throw if missing)\n+- Reads `layerKey` from FocusLayer context (throw if missing — every scope must be inside a layer)\n - Add `ResizeObserver` on the wrapper div\n-- On mount/resize: `invoke(\"spatial_register\", { key: spatialKey, moniker, ...rect, layer_key: layerKey })`\n+- On mount/resize: `invoke(\"spatial_register\", { key: spatialKey, moniker, ...rect, layerKey })`\n - On unmount: `invoke(\"spatial_unregister\", { key: spatialKey })`\n\n-**`app-shell.tsx`**:\n-- Wrap app root in `<FocusLayer name=\"window\">`\n-\n-**`entity-focus-context.tsx`** — `broadcastNavCommand`:\n-1. Evaluate `claimWhen` predicates (backward compat, temporary)\n-2. If no claim: `const target = await invoke(\"spatial_navigate\", { direction, focused_moniker })`\n-3. If target: `setFocus(target)`\n+**`App.tsx`** (or `app-shell.tsx`):\n+- Wrap app root in `<FocusLayer name=\"window\">` — this is the window root layer (no parent)\n\n ### Design decisions\n-- **ULID per instance**: `useRef(ulid())` — stable across re-renders, unique per mount. No global counter, no collision risk.\n-- **Layer removal by key, not pop**: Layer stack supports arbitrary removal order. If inspector A opens, then dialog B opens on top, then A closes — `remove(A.key)` works correctly. B is still on top.\n-- **Moniker for focus, key for registry**: `navigate()` returns monikers because `setFocus` and command dispatch use monikers. The spatial key is internal plumbing.\n-- **Non-entity monikers**: UI chrome uses `\"ui:region.action\"` convention. These participate in spatial nav and can register commands in their FocusScope, but don't go through entity command dispatch.\n+- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. Serde-transparent so the wire protocol stays simple.\n+- **Layers form a forest, not a stack**: multi-window support is a first-class concern, not a retrofit. Two windows = two trees.\n+- **Parent pointer, not child list**: simpler to add/remove. `children_of(key)` walks the map once; fine for small layer counts (O(layers) rather than O(1), but we have < ~20 layers tops).\n+- **Layer removal by key**: supports arbitrary removal order.\n+- **window_label derived, not passed**: React never sends window_label — Tauri knows it from the invoking webview. One less thing for React to get wrong.\n\n ### Subtasks\n-- [ ] Define `Rect`, `SpatialEntry`, `SpatialRegistry`, `LayerEntry`, `LayerStack` in Rust\n-- [ ] Implement registry and layer stack with key-based operations\n-- [ ] Add Tauri commands wiring React to Rust\n-- [ ] Create FocusLayer component with ULID key + Tauri invokes\n-- [ ] Update FocusScope with ULID spatial key + ResizeObserver + Tauri invokes\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]` (shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`)\n+- [ ] Define `Rect`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry` in Rust\n+- [ ] Implement registry with forest ops: `children_of`, `root_for_window`, `ancestors_of`, `entries_in_layer`\n+- [ ] Add Tauri commands that derive `window_label` from the `tauri::Window` param\n+- [ ] Create FocusLayer component with ULID layer key, parent context, Tauri invokes\n+- [ ] Update FocusScope with ULID spatial key, ResizeObserver, Tauri invokes\n+- [ ] Wrap app root in `<FocusLayer name=\"window\">` (window root)\n\n ## Acceptance Criteria\n-- [ ] Each FocusScope mount generates a unique ULID spatial key via `useRef`\n-- [ ] Each FocusLayer mount generates a unique ULID layer key via `useRef`\n-- [ ] Spatial registry keyed by spatial key — same moniker in two locations = two entries\n-- [ ] Layer stack supports arbitrary removal order (not just pop)\n-- [ ] `navigate()` returns target moniker (not spatial key)\n-- [ ] Root `<FocusLayer name=\"window\">` wraps the app\n+- [ ] Spatial and layer keys use newtypes throughout Rust; wire format is plain strings\n+- [ ] Each FocusScope mount generates a unique ULID `SpatialKey` via `useRef`\n+- [ ] Each FocusLayer mount generates a unique ULID `LayerKey` via `useRef`\n+- [ ] Every layer carries its `window_label` (derived from Tauri, not from React args)\n+- [ ] Root window layers have `parent = None`; stacked layers have `parent = Some(...)`\n+- [ ] The registry can represent: 2 windows + 2 inspectors (one per window) + 1 dialog on top of one of the inspectors = 5 layers, 2 roots, correct parent chains\n+- [ ] Spatial registry keyed by `SpatialKey` — same moniker in two locations = two entries\n+- [ ] Layer removal by key supports arbitrary removal order\n - [ ] Existing focus behavior unchanged\n - [ ] `cargo test` passes, `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `Rust unit tests` — registry: register two entries with same moniker, different keys — both stored; unregister one, other remains\n-- [ ] `Rust unit tests` — layer stack: push A, push B, remove A — B is still active; remove B — stack empty\n-- [ ] `kanban-app/ui/src/components/focus-layer.test.tsx` — ULID generated on mount, stable across re-renders, new on remount\n-- [ ] `kanban-app/ui/src/components/focus-scope.test.tsx` — spatial key generated, registered with layer key from context\n+- [ ] Rust: register two entries with same moniker, different keys — both stored; unregister one, other remains\n+- [ ] Rust: push window root A, push window root B (both `parent = None`, different `window_label`) — both coexist as siblings\n+- [ ] Rust: push window A, push inspector I (parent = A), push dialog D (parent = I) — `ancestors_of(D) == [I, A]`, `root_for_window(A.window_label) == Some(A)`\n+- [ ] Rust: `children_of(window_root)` returns inspectors of that window only, not inspectors of another window\n+- [ ] Rust: remove inspector I while dialog D still present — D remains in registry with its (now dangling) parent pointer; nav in D's layer still works (layer ops don't follow parent)\n+- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key` — window's entries excluded even though window is the parent\n+- [ ] Rust: layer removal — remove non-existent key is a no-op, not an error\n+- [ ] `focus-layer.test.tsx` — ULID generated on mount, stable across re-renders, new on remount; nested layer reads parent from context and sends `parent_key` on push\n+- [ ] `focus-scope.test.tsx` — spatial key generated, registered with layer key from context; throws if rendered outside a FocusLayer\n - [ ] Run `cargo test` and `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n Add spatial awareness to the focus system with a clear frontend/backend split:\n\n - **React** measures DOM rects and reports them to Rust. React owns `FocusScope` (measures its element) and `FocusLayer` (declares a layer boundary).\n-- **Rust** owns the spatial registry, the **layer forest**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n+- **Rust** owns the spatial registry, layer stack, and navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n\n ### Keying model\n\n@@ -10,44 +10,11 @@\n Two separate key systems:\n\n - **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch. Unchanged. Non-entity UI elements use a `\"ui:region.action\"` convention (e.g., `\"ui:toolbar.newTask\"`, `\"ui:tabbar.board1\"`).\n-- **SpatialKey** (ULID) — unique per FocusScope/FocusLayer *instance*. Generated once per mount via `useRef(ulid())`. Stable across re-renders, new on remount. This is the primary key in the Rust spatial registry.\n+- **Spatial key** (ULID) — unique per FocusScope/FocusLayer *instance*. Generated once per mount via `useRef(ulid())`. Stable across re-renders, new on remount. This is the primary key in the Rust spatial registry.\n\n-Why separate: the same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey, own rect, possibly different layer. `navigate()` returns the winning entry's **moniker** (what `setFocus` needs).\n-\n-### Layer model: a forest, not a stack\n+Why separate: the same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own spatial key, own rect, possibly different layer. Rust stores `HashMap<SpatialKey, SpatialEntry>` where `SpatialEntry = { moniker, rect, layer_key }`. `navigate()` returns the winning entry's **moniker** (what `setFocus` needs).\n\n-**The app is multi-window.** `UIState.windows: HashMap<String, WindowState>` already exists, with each window maintaining its own `inspector_stack`. The layer model must reflect this: multiple **parallel root layers** (one per window), each with **stacked child layers** (inspectors, dialogs) on top.\n-\n-```rust\n-pub struct LayerEntry {\n-    pub key: LayerKey,\n-    pub name: String,                 // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n-    pub parent: Option<LayerKey>,     // None for window roots; Some(...) for stacked layers\n-    pub window_label: WindowLabel,    // which window this layer lives in\n-    pub last_focused: Option<SpatialKey>, // focus memory for pop-restore\n-}\n-\n-pub struct SpatialRegistry {\n-    entries: HashMap<SpatialKey, SpatialEntry>,\n-    layers:  HashMap<LayerKey, LayerEntry>,\n-}\n-```\n-\n-A **layer forest**: trees rooted at each window's root layer. An inspector opened in window A has `parent = Some(windowA_root_layer_key)`. A confirm dialog on top of that inspector has `parent = Some(inspector_layer_key)`. Two windows each with their own inspector → 4 layers total, 2 roots.\n-\n-Layers are removed by key (not pop) so arbitrary unmount order works — inspector A opens, dialog B opens on top, A closes first → `remove(A.key)`, B stays with its old parent pointer (valid — B's parent is the window, not A, if declared correctly by the component tree).\n-\n-### Newtypes\n-\n-Use the newtypes from card `01KNM3YHHFJ3PTXZHD9EFKVBS6` (focus claim registry):\n-\n-```rust\n-pub struct WindowLabel(pub String);\n-pub struct SpatialKey(pub String);\n-pub struct LayerKey(pub String);\n-```\n-\n-All three have `#[serde(transparent)]` so the wire format stays a plain string. React sees strings, Rust has strong typing.\n+FocusLayer also gets a ULID spatial key so the layer stack handles multiple instances of the same named layer (e.g., two inspector panels in two windows).\n\n ### Architecture\n\n@@ -57,56 +24,51 @@\n FocusScope mounts\n   spatialKey = useRef(ulid())\n   → ResizeObserver fires\n-  → invoke: spatial_register           → registry.insert(spatial_key,\n-    { key, moniker, rect, layer_key }     { moniker, rect, layer_key })\n+  → invoke: spatial.register           → registry.insert(spatial_key, { moniker, rect, layer_key })\n+    { key, moniker, rect, layer_key }\n\n FocusScope unmounts\n-  → invoke: spatial_unregister         → registry.remove(spatial_key)\n+  → invoke: spatial.unregister         → registry.remove(spatial_key)\n     { key }\n\n FocusLayer mounts\n   layerKey = useRef(ulid())\n-  parentLayerKey = nearest ancestor FocusLayer key (via context)\n-                 = None if this is a window root\n-  → invoke: spatial_push_layer         → layers.insert(key,\n-    { key, name, parent_key }             { name, parent, window_label })\n-                                          (window_label derived from tauri::Window)\n+  → invoke: spatial.push_layer         → layer_stack.push({ key, name })\n+    { key, name }\n\n FocusLayer unmounts\n-  → invoke: spatial_pop_layer          → layers.remove(key)\n+  → invoke: spatial.pop_layer          → layer_stack.remove(key)\n     { key }\n\n-nav.right key pressed\n-  → invoke: spatial_navigate           → find_target(direction, focused_key)\n-    { key, direction }                    filter to focused's layer_key, score\n-  ← returns Ok                         ← emit focus-changed event with moniker\n+nav.up key pressed\n+  → invoke: spatial.navigate           → find_target(direction, focused_moniker)\n+    { direction, focused_moniker }       filter to active layer, score by geometry\n+  ← returns target moniker             ← winning entry's moniker\n+  → setFocus(target)\n ```\n\n ### Rust side\n\n **`SpatialEntry`** struct:\n+- `spatial_key: String` (ULID from React)\n - `moniker: String`\n - `rect: Rect { x: f64, y: f64, width: f64, height: f64 }`\n-- `layer_key: LayerKey` — which layer this scope lives in\n+- `layer_key: String` (ULID of the FocusLayer this scope lives in)\n\n-**`SpatialRegistry`** ops:\n-- `register(key: SpatialKey, moniker, rect, layer_key)`\n-- `unregister(key: SpatialKey)`\n-- `update_rect(key: SpatialKey, rect)`\n-- `push_layer(key: LayerKey, name, parent_key: Option<LayerKey>, window_label: WindowLabel)`\n-- `remove_layer(key: LayerKey)`\n-- `children_of(key: LayerKey) -> Vec<&LayerEntry>` — layers with this as parent\n-- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>` — the window root layer\n-- `ancestors_of(key: LayerKey) -> Vec<&LayerEntry>` — walk up to root\n-- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>` — for nav scoping\n+**`SpatialRegistry`** struct:\n+- `entries: HashMap<String, SpatialEntry>` — keyed by spatial_key\n+- `register(key, moniker, rect, layer_key)` / `unregister(key)` / `update_rect(key, rect)`\n\n-**No global \"active layer\"**. Navigation is always scoped to the focused entry's `layer_key` (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`).\n+**`LayerStack`** — `Vec<LayerEntry>` where `LayerEntry = { key: String, name: String }`:\n+- `push(key, name)` / `remove(key)` / `active() -> Option<&LayerEntry>` (topmost)\n+- `remove` by key (not pop) — handles out-of-order unmount if an inner layer unmounts before an outer one\n\n **Tauri commands**:\n-- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, layer_key)`\n-- `spatial_unregister(window: tauri::Window, key)`\n-- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)` — derives window_label from the `tauri::Window` param\n-- `spatial_pop_layer(window: tauri::Window, key)`\n+- `spatial_register(key, moniker, x, y, w, h, layer_key)`\n+- `spatial_unregister(key)`\n+- `spatial_push_layer(key, name)`\n+- `spatial_pop_layer(key)`\n+- `spatial_navigate(direction, focused_moniker) -> Option<String>` — returns target moniker\n\n ### React side\n\n@@ -113,59 +75,53 @@\n **`FocusLayer`** component (`kanban-app/ui/src/components/focus-layer.tsx`):\n - Props: `name: string`, `children`\n - `const layerKey = useRef(ulid()).current`\n-- Reads parent FocusLayer's key from context (throws if a non-root layer has no parent — but the root layer in the window shell is allowed to have `parent = undefined`)\n-- Provides `layerKey` via React context so descendant FocusScope and FocusLayer can read it\n-- On mount: `invoke(\"spatial_push_layer\", { key: layerKey, name, parentKey })`\n+- Provides `layerKey` via React context (so FocusScope can read it)\n+- On mount: `invoke(\"spatial_push_layer\", { key: layerKey, name })`\n - On unmount: `invoke(\"spatial_pop_layer\", { key: layerKey })`\n\n **`FocusScope`** changes (`kanban-app/ui/src/components/focus-scope.tsx`):\n - `const spatialKey = useRef(ulid()).current`\n-- Reads `layerKey` from FocusLayer context (throw if missing — every scope must be inside a layer)\n+- Read `layerKey` from FocusLayer context (throw if missing)\n - Add `ResizeObserver` on the wrapper div\n-- On mount/resize: `invoke(\"spatial_register\", { key: spatialKey, moniker, ...rect, layerKey })`\n+- On mount/resize: `invoke(\"spatial_register\", { key: spatialKey, moniker, ...rect, layer_key: layerKey })`\n - On unmount: `invoke(\"spatial_unregister\", { key: spatialKey })`\n\n-**`App.tsx`** (or `app-shell.tsx`):\n-- Wrap app root in `<FocusLayer name=\"window\">` — this is the window root layer (no parent)\n+**`app-shell.tsx`**:\n+- Wrap app root in `<FocusLayer name=\"window\">`\n+\n+**`entity-focus-context.tsx`** — `broadcastNavCommand`:\n+1. Evaluate `claimWhen` predicates (backward compat, temporary)\n+2. If no claim: `const target = await invoke(\"spatial_navigate\", { direction, focused_moniker })`\n+3. If target: `setFocus(target)`\n\n ### Design decisions\n-- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. Serde-transparent so the wire protocol stays simple.\n-- **Layers form a forest, not a stack**: multi-window support is a first-class concern, not a retrofit. Two windows = two trees.\n-- **Parent pointer, not child list**: simpler to add/remove. `children_of(key)` walks the map once; fine for small layer counts (O(layers) rather than O(1), but we have < ~20 layers tops).\n-- **Layer removal by key**: supports arbitrary removal order.\n-- **window_label derived, not passed**: React never sends window_label — Tauri knows it from the invoking webview. One less thing for React to get wrong.\n+- **ULID per instance**: `useRef(ulid())` — stable across re-renders, unique per mount. No global counter, no collision risk.\n+- **Layer removal by key, not pop**: Layer stack supports arbitrary removal order. If inspector A opens, then dialog B opens on top, then A closes — `remove(A.key)` works correctly. B is still on top.\n+- **Moniker for focus, key for registry**: `navigate()` returns monikers because `setFocus` and command dispatch use monikers. The spatial key is internal plumbing.\n+- **Non-entity monikers**: UI chrome uses `\"ui:region.action\"` convention. These participate in spatial nav and can register commands in their FocusScope, but don't go through entity command dispatch.\n\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]` (shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`)\n-- [ ] Define `Rect`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry` in Rust\n-- [ ] Implement registry with forest ops: `children_of`, `root_for_window`, `ancestors_of`, `entries_in_layer`\n-- [ ] Add Tauri commands that derive `window_label` from the `tauri::Window` param\n-- [ ] Create FocusLayer component with ULID layer key, parent context, Tauri invokes\n-- [ ] Update FocusScope with ULID spatial key, ResizeObserver, Tauri invokes\n-- [ ] Wrap app root in `<FocusLayer name=\"window\">` (window root)\n+- [ ] Define `Rect`, `SpatialEntry`, `SpatialRegistry`, `LayerEntry`, `LayerStack` in Rust\n+- [ ] Implement registry and layer stack with key-based operations\n+- [ ] Add Tauri commands wiring React to Rust\n+- [ ] Create FocusLayer component with ULID key + Tauri invokes\n+- [ ] Update FocusScope with ULID spatial key + ResizeObserver + Tauri invokes\n\n ## Acceptance Criteria\n-- [ ] Spatial and layer keys use newtypes throughout Rust; wire format is plain strings\n-- [ ] Each FocusScope mount generates a unique ULID `SpatialKey` via `useRef`\n-- [ ] Each FocusLayer mount generates a unique ULID `LayerKey` via `useRef`\n-- [ ] Every layer carries its `window_label` (derived from Tauri, not from React args)\n-- [ ] Root window layers have `parent = None`; stacked layers have `parent = Some(...)`\n-- [ ] The registry can represent: 2 windows + 2 inspectors (one per window) + 1 dialog on top of one of the inspectors = 5 layers, 2 roots, correct parent chains\n-- [ ] Spatial registry keyed by `SpatialKey` — same moniker in two locations = two entries\n-- [ ] Layer removal by key supports arbitrary removal order\n+- [ ] Each FocusScope mount generates a unique ULID spatial key via `useRef`\n+- [ ] Each FocusLayer mount generates a unique ULID layer key via `useRef`\n+- [ ] Spatial registry keyed by spatial key — same moniker in two locations = two entries\n+- [ ] Layer stack supports arbitrary removal order (not just pop)\n+- [ ] `navigate()` returns target moniker (not spatial key)\n+- [ ] Root `<FocusLayer name=\"window\">` wraps the app\n - [ ] Existing focus behavior unchanged\n - [ ] `cargo test` passes, `pnpm vitest run` passes\n\n ## Tests\n-- [ ] Rust: register two entries with same moniker, different keys — both stored; unregister one, other remains\n-- [ ] Rust: push window root A, push window root B (both `parent = None`, different `window_label`) — both coexist as siblings\n-- [ ] Rust: push window A, push inspector I (parent = A), push dialog D (parent = I) — `ancestors_of(D) == [I, A]`, `root_for_window(A.window_label) == Some(A)`\n-- [ ] Rust: `children_of(window_root)` returns inspectors of that window only, not inspectors of another window\n-- [ ] Rust: remove inspector I while dialog D still present — D remains in registry with its (now dangling) parent pointer; nav in D's layer still works (layer ops don't follow parent)\n-- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key` — window's entries excluded even though window is the parent\n-- [ ] Rust: layer removal — remove non-existent key is a no-op, not an error\n-- [ ] `focus-layer.test.tsx` — ULID generated on mount, stable across re-renders, new on remount; nested layer reads parent from context and sends `parent_key` on push\n-- [ ] `focus-scope.test.tsx` — spatial key generated, registered with layer key from context; throws if rendered outside a FocusLayer\n+- [ ] `Rust unit tests` — registry: register two entries with same moniker, different keys — both stored; unregister one, other remains\n+- [ ] `Rust unit tests` — layer stack: push A, push B, remove A — B is still active; remove B — stack empty\n+- [ ] `kanban-app/ui/src/components/focus-layer.test.tsx` — ULID generated on mount, stable across re-renders, new on remount\n+- [ ] `kanban-app/ui/src/components/focus-scope.test.tsx` — spatial key generated, registered with layer key from context\n - [ ] Run `cargo test` and `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Spatial registry: Rust-side spatial state, React-side rect measurement\n\\ No newline at end of file\n+Spatial registry: layer forest, typed keys, React measures rects\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Spatial registry: layer forest, typed keys, React measures rects\n\\ No newline at end of file\n+Spatial registry: Rust-side spatial state, React-side rect measurement\n\\ No newline at end of file\n"}]]}
{"id":"01KPZRT0K15DXF5RAYYHJZS7JX","timestamp":"2026-04-24T12:53:02.689374Z","op":"update","item_id":"01KNQXW7HHHB8HW76K3PXH3G34","forward_patch":"--- original\n+++ modified\n@@ -6,7 +6,7 @@\n position_column: todo\n position_ordinal: a180\n project: spatial-nav\n-title: 'Spatial registry: layer forest, typed keys, React measures rects'\n+title: 'Spatial registry: layer forest + zones, typed keys, React measures rects'\n ---\n ## What\n\n@@ -13,28 +13,60 @@\n Add spatial awareness to the focus system with a clear frontend/backend split:\n\n - **React** measures DOM rects and reports them to Rust. React owns `FocusScope` (measures its element) and `FocusLayer` (declares a layer boundary).\n-- **Rust** owns the spatial registry, the **layer forest**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n+- **Rust** owns the spatial registry, the **layer forest**, the **zone tree**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n+\n+### Two structural concepts\n+\n+1. **Layers** are **hard** boundaries. Nav never crosses them. Window roots, inspector overlay, dialog overlays are layers. Layers form a **forest** (one root per Tauri window, plus stacked children).\n+2. **Zones** are **soft** containers within a layer. They give structure to beam search — container-first, with leaf-level cross-zone fallback. Perspective, nav bar, toolbar, column, card, inspector panel, field row — all zones.\n\n ### Keying model\n\n-Two separate key systems:\n+- **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch.\n+- **SpatialKey** (ULID) — unique per FocusScope *instance*. Generated once per mount via `useRef(ulid())`. Primary key in the registry.\n+- Same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey.\n\n-- **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch. Unchanged. Non-entity UI elements use a `\"ui:region.action\"` convention (e.g., `\"ui:toolbar.newTask\"`, `\"ui:tabbar.board1\"`).\n-- **SpatialKey** (ULID) — unique per FocusScope/FocusLayer *instance*. Generated once per mount via `useRef(ulid())`. Stable across re-renders, new on remount. This is the primary key in the Rust spatial registry.\n+### Newtypes (Rust)\n\n-Why separate: the same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey, own rect, possibly different layer. `navigate()` returns the winning entry's **moniker** (what `setFocus` needs).\n+Shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`:\n\n-### Layer model: a forest, not a stack\n+```rust\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct WindowLabel(pub String);\n+#[serde(transparent)] pub struct SpatialKey(pub String);\n+#[serde(transparent)] pub struct LayerKey(pub String);\n+```\n\n-**The app is multi-window.** `UIState.windows: HashMap<String, WindowState>` already exists, with each window maintaining its own `inspector_stack`. The layer model must reflect this: multiple **parallel root layers** (one per window), each with **stacked child layers** (inspectors, dialogs) on top.\n+### Layer model: forest, not stack\n\n ```rust\n pub struct LayerEntry {\n     pub key: LayerKey,\n-    pub name: String,                 // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n-    pub parent: Option<LayerKey>,     // None for window roots; Some(...) for stacked layers\n-    pub window_label: WindowLabel,    // which window this layer lives in\n-    pub last_focused: Option<SpatialKey>, // focus memory for pop-restore\n+    pub name: String,                       // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n+    pub parent: Option<LayerKey>,           // None for window roots; Some(...) for stacked layers\n+    pub window_label: WindowLabel,          // which window this layer lives in\n+    pub last_focused: Option<SpatialKey>,   // focus memory for pop-restore\n+}\n+```\n+\n+Two windows each with their own inspector → 4 layers total, 2 roots. Layers removed by key, not popped — supports arbitrary unmount order.\n+\n+### Zone model within a layer\n+\n+```rust\n+pub enum ScopeKind {\n+    Leaf,   // focusable element (task title, status pill, field pill, button)\n+    Zone,   // container (column, card, inspector panel, field row, toolbar group)\n+}\n+\n+pub struct SpatialEntry {\n+    pub moniker: String,\n+    pub rect: Rect,\n+    pub layer_key: LayerKey,\n+    pub kind: ScopeKind,\n+    pub parent_zone: Option<SpatialKey>,    // nearest ancestor Zone in the same layer; None = layer root\n+    pub last_focused: Option<SpatialKey>,   // focus memory for drill-out / fallback when kind == Zone\n+    pub overrides: HashMap<Direction, Option<String>>,\n }\n\n pub struct SpatialRegistry {\n@@ -43,21 +75,21 @@\n }\n ```\n\n-A **layer forest**: trees rooted at each window's root layer. An inspector opened in window A has `parent = Some(windowA_root_layer_key)`. A confirm dialog on top of that inspector has `parent = Some(inspector_layer_key)`. Two windows each with their own inspector → 4 layers total, 2 roots.\n-\n-Layers are removed by key (not pop) so arbitrary unmount order works — inspector A opens, dialog B opens on top, A closes first → `remove(A.key)`, B stays with its old parent pointer (valid — B's parent is the window, not A, if declared correctly by the component tree).\n-\n-### Newtypes\n+Zones form a **tree within each layer**, rooted at the layer root (`parent_zone = None`). Nav algorithm uses zones for container-first beam search and for drill-in/drill-out commands (see card `01KNQXXF5W7G4JP73C6ZCMKYKX` and the new drill-in/drill-out card).\n\n-Use the newtypes from card `01KNM3YHHFJ3PTXZHD9EFKVBS6` (focus claim registry):\n+### Which React regions become zones\n\n-```rust\n-pub struct WindowLabel(pub String);\n-pub struct SpatialKey(pub String);\n-pub struct LayerKey(pub String);\n-```\n+- `NavBar` → `kind=\"zone\"` (contains logo, menus, mode indicator as leaves)\n+- Toolbar / action groups → `kind=\"zone\"`\n+- Tab bar / perspective bar → `kind=\"zone\"`, each tab a leaf\n+- Perspective container → `kind=\"zone\"` (wraps the active view)\n+- Column (`column-view`) → `kind=\"zone\"` (contains header leaf + cards)\n+- Task card (`entity-card`) → `kind=\"zone\"` (contains title leaf + status leaf + pills)\n+- Inspector panel → `kind=\"zone\"` (contains field rows)\n+- Field row (inspector) → `kind=\"zone\"` (contains label + pills)\n+- Field pills, task titles, status badges, tab buttons → `kind=\"leaf\"` (default, no change needed)\n\n-All three have `#[serde(transparent)]` so the wire format stays a plain string. React sees strings, Rust has strong typing.\n+Detailed adoption happens in the migration cards (`01KNQXZ81Q...`, `01KNQY0P9J...`, and a new shell-wrapping card).\n\n ### Architecture\n\n@@ -64,118 +96,121 @@\n ```\n React (measure + report)              Rust (track + navigate)\n ─────────────────────────              ──────────────────────\n-FocusScope mounts\n+FocusScope mounts (kind, moniker)\n   spatialKey = useRef(ulid())\n+  parent_zone_key = nearest ancestor FocusScope with kind=\"zone\" (from context)\n   → ResizeObserver fires\n   → invoke: spatial_register           → registry.insert(spatial_key,\n-    { key, moniker, rect, layer_key }     { moniker, rect, layer_key })\n+    { key, moniker, rect, kind,           { moniker, rect, kind,\n+      layer_key, parent_zone }              layer_key, parent_zone })\n\n FocusScope unmounts\n   → invoke: spatial_unregister         → registry.remove(spatial_key)\n-    { key }\n\n FocusLayer mounts\n   layerKey = useRef(ulid())\n-  parentLayerKey = nearest ancestor FocusLayer key (via context)\n-                 = None if this is a window root\n+  parentLayerKey = nearest ancestor FocusLayer (via context) OR explicit prop\n   → invoke: spatial_push_layer         → layers.insert(key,\n     { key, name, parent_key }             { name, parent, window_label })\n-                                          (window_label derived from tauri::Window)\n\n FocusLayer unmounts\n   → invoke: spatial_pop_layer          → layers.remove(key)\n-    { key }\n\n-nav.right key pressed\n-  → invoke: spatial_navigate           → find_target(direction, focused_key)\n-    { key, direction }                    filter to focused's layer_key, score\n-  ← returns Ok                         ← emit focus-changed event with moniker\n+nav.right pressed\n+  → invoke: spatial_navigate           → three-rule beam search scoped to\n+    { key, direction }                    focused entry's layer + zones\n+  ← Ok                                 ← emit focus-changed event\n ```\n\n ### Rust side\n\n-**`SpatialEntry`** struct:\n-- `moniker: String`\n-- `rect: Rect { x: f64, y: f64, width: f64, height: f64 }`\n-- `layer_key: LayerKey` — which layer this scope lives in\n-\n **`SpatialRegistry`** ops:\n-- `register(key: SpatialKey, moniker, rect, layer_key)`\n+- `register(key: SpatialKey, moniker, rect, kind, layer_key, parent_zone)`\n - `unregister(key: SpatialKey)`\n - `update_rect(key: SpatialKey, rect)`\n - `push_layer(key: LayerKey, name, parent_key: Option<LayerKey>, window_label: WindowLabel)`\n - `remove_layer(key: LayerKey)`\n-- `children_of(key: LayerKey) -> Vec<&LayerEntry>` — layers with this as parent\n-- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>` — the window root layer\n-- `ancestors_of(key: LayerKey) -> Vec<&LayerEntry>` — walk up to root\n-- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>` — for nav scoping\n-\n-**No global \"active layer\"**. Navigation is always scoped to the focused entry's `layer_key` (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`).\n+- `children_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n+- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>`\n+- `ancestors_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n+- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>`\n+- `children_of_zone(zone_key: SpatialKey) -> impl Iterator<Item=&SpatialEntry>` — entries with `parent_zone == Some(zone_key)`\n+- `ancestor_zones(key: SpatialKey) -> Vec<&SpatialEntry>` — walk `parent_zone` up to layer root\n\n **Tauri commands**:\n-- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, layer_key)`\n+- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, kind: ScopeKind, layer_key, parent_zone: Option<SpatialKey>)`\n - `spatial_unregister(window: tauri::Window, key)`\n-- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)` — derives window_label from the `tauri::Window` param\n+- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)`\n - `spatial_pop_layer(window: tauri::Window, key)`\n\n+`window_label` is derived from the `tauri::Window` parameter, never passed by React.\n+\n ### React side\n\n **`FocusLayer`** component (`kanban-app/ui/src/components/focus-layer.tsx`):\n-- Props: `name: string`, `children`\n+- Props: `name: string`, optional `parentLayerKey`, `children`\n - `const layerKey = useRef(ulid()).current`\n-- Reads parent FocusLayer's key from context (throws if a non-root layer has no parent — but the root layer in the window shell is allowed to have `parent = undefined`)\n-- Provides `layerKey` via React context so descendant FocusScope and FocusLayer can read it\n-- On mount: `invoke(\"spatial_push_layer\", { key: layerKey, name, parentKey })`\n-- On unmount: `invoke(\"spatial_pop_layer\", { key: layerKey })`\n+- Resolves parent: explicit `parentLayerKey` prop wins; else nearest ancestor via `FocusLayerContext`; else `None` (root).\n+- Provides `layerKey` via context\n+- Mount: `invoke(\"spatial_push_layer\", { key, name, parentKey })`\n+- Unmount: `invoke(\"spatial_pop_layer\", { key })`\n\n-**`FocusScope`** changes (`kanban-app/ui/src/components/focus-scope.tsx`):\n+**`FocusScope`** (`kanban-app/ui/src/components/focus-scope.tsx`):\n+- Props add: `kind?: \"leaf\" | \"zone\"` (default `\"leaf\"`)\n - `const spatialKey = useRef(ulid()).current`\n-- Reads `layerKey` from FocusLayer context (throw if missing — every scope must be inside a layer)\n-- Add `ResizeObserver` on the wrapper div\n-- On mount/resize: `invoke(\"spatial_register\", { key: spatialKey, moniker, ...rect, layerKey })`\n-- On unmount: `invoke(\"spatial_unregister\", { key: spatialKey })`\n+- Reads `layerKey` from `FocusLayerContext` (throws if missing)\n+- Reads `parentZoneKey` from `FocusZoneContext` (the nearest ancestor FocusScope where `kind===\"zone\"` published its own key)\n+- When `kind === \"zone\"`, publishes its own `spatialKey` on `FocusZoneContext` for descendants\n+- `ResizeObserver` on the wrapper div\n+- Mount/resize: `invoke(\"spatial_register\", { key, moniker, ...rect, kind, layerKey, parentZone })`\n+- Unmount: `invoke(\"spatial_unregister\", { key })`\n\n-**`App.tsx`** (or `app-shell.tsx`):\n-- Wrap app root in `<FocusLayer name=\"window\">` — this is the window root layer (no parent)\n+**`App.tsx`** (or `app-shell.tsx`): wrap app root in `<FocusLayer name=\"window\">`.\n\n ### Design decisions\n-- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. Serde-transparent so the wire protocol stays simple.\n-- **Layers form a forest, not a stack**: multi-window support is a first-class concern, not a retrofit. Two windows = two trees.\n-- **Parent pointer, not child list**: simpler to add/remove. `children_of(key)` walks the map once; fine for small layer counts (O(layers) rather than O(1), but we have < ~20 layers tops).\n-- **Layer removal by key**: supports arbitrary removal order.\n-- **window_label derived, not passed**: React never sends window_label — Tauri knows it from the invoking webview. One less thing for React to get wrong.\n+- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. `#[serde(transparent)]` keeps the wire protocol simple.\n+- **Layers form a forest**: multi-window is first-class, not a retrofit. Two windows = two trees.\n+- **Zones form a tree within each layer**: gives nav algorithm container-first semantics without needing mode state.\n+- **parent_zone pointer**: simpler than a child list; `children_of_zone` walks the map once, fine for < ~hundreds of entries per layer.\n+- **Layer removal by key**: arbitrary removal order.\n+- **window_label derived, not passed**: Tauri knows it from the invoking webview.\n+- **`kind` on SpatialEntry** (not a separate type): zones and leaves share the same table; nav filters by `kind` where relevant.\n\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]` (shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`)\n-- [ ] Define `Rect`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry` in Rust\n-- [ ] Implement registry with forest ops: `children_of`, `root_for_window`, `ancestors_of`, `entries_in_layer`\n-- [ ] Add Tauri commands that derive `window_label` from the `tauri::Window` param\n-- [ ] Create FocusLayer component with ULID layer key, parent context, Tauri invokes\n-- [ ] Update FocusScope with ULID spatial key, ResizeObserver, Tauri invokes\n-- [ ] Wrap app root in `<FocusLayer name=\"window\">` (window root)\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n+- [ ] Define `Rect`, `ScopeKind`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry`\n+- [ ] Implement layer forest ops: `children_of_layer`, `root_for_window`, `ancestors_of_layer`\n+- [ ] Implement zone tree ops: `children_of_zone`, `ancestor_zones`\n+- [ ] `entries_in_layer` iterator for nav scoping\n+- [ ] Tauri commands derive `window_label` from `tauri::Window`\n+- [ ] Create `FocusLayer` component with ULID layer key, parent context/prop, Tauri invokes\n+- [ ] Create `FocusZoneContext` and update `FocusScope` with `kind` prop, parent_zone resolution, Tauri invokes\n+- [ ] Wrap app root in `<FocusLayer name=\"window\">`\n\n ## Acceptance Criteria\n-- [ ] Spatial and layer keys use newtypes throughout Rust; wire format is plain strings\n-- [ ] Each FocusScope mount generates a unique ULID `SpatialKey` via `useRef`\n-- [ ] Each FocusLayer mount generates a unique ULID `LayerKey` via `useRef`\n-- [ ] Every layer carries its `window_label` (derived from Tauri, not from React args)\n+- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes throughout Rust; wire format is plain strings\n+- [ ] Each FocusScope mount generates a unique ULID `SpatialKey`; each FocusLayer mount generates a unique ULID `LayerKey`\n+- [ ] Every layer carries its `window_label`, derived from Tauri (not React args)\n - [ ] Root window layers have `parent = None`; stacked layers have `parent = Some(...)`\n-- [ ] The registry can represent: 2 windows + 2 inspectors (one per window) + 1 dialog on top of one of the inspectors = 5 layers, 2 roots, correct parent chains\n-- [ ] Spatial registry keyed by `SpatialKey` — same moniker in two locations = two entries\n-- [ ] Layer removal by key supports arbitrary removal order\n-- [ ] Existing focus behavior unchanged\n+- [ ] The registry can represent: 2 windows + 2 inspector layers (one per window) + 1 dialog on top of one inspector = 5 layers, 2 roots, correct parent chains\n+- [ ] Every SpatialEntry has `kind` and `parent_zone`; the zone tree is well-formed (no cycles, all parents exist in same layer)\n+- [ ] `children_of_zone` returns direct children only (not transitive descendants)\n+- [ ] `ancestor_zones` walks up to the layer root (entries with `parent_zone = None`)\n+- [ ] Same moniker at two locations → two entries, different SpatialKeys\n+- [ ] Layer removal by key supports arbitrary order\n - [ ] `cargo test` passes, `pnpm vitest run` passes\n\n ## Tests\n-- [ ] Rust: register two entries with same moniker, different keys — both stored; unregister one, other remains\n-- [ ] Rust: push window root A, push window root B (both `parent = None`, different `window_label`) — both coexist as siblings\n-- [ ] Rust: push window A, push inspector I (parent = A), push dialog D (parent = I) — `ancestors_of(D) == [I, A]`, `root_for_window(A.window_label) == Some(A)`\n-- [ ] Rust: `children_of(window_root)` returns inspectors of that window only, not inspectors of another window\n-- [ ] Rust: remove inspector I while dialog D still present — D remains in registry with its (now dangling) parent pointer; nav in D's layer still works (layer ops don't follow parent)\n-- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key` — window's entries excluded even though window is the parent\n-- [ ] Rust: layer removal — remove non-existent key is a no-op, not an error\n-- [ ] `focus-layer.test.tsx` — ULID generated on mount, stable across re-renders, new on remount; nested layer reads parent from context and sends `parent_key` on push\n-- [ ] `focus-scope.test.tsx` — spatial key generated, registered with layer key from context; throws if rendered outside a FocusLayer\n+- [ ] Rust: register two entries with same moniker, different keys — both stored\n+- [ ] Rust: push window root A + window root B (parent=None, different window_label) — coexist as siblings\n+- [ ] Rust: push window A, inspector I (parent=A), dialog D (parent=I) — `ancestors_of_layer(D) == [I, A]`\n+- [ ] Rust: `children_of_layer(A)` returns inspectors of A only, not of other windows\n+- [ ] Rust: register a column (Zone, parent_zone=None), a card (Zone, parent_zone=column_key), a title (Leaf, parent_zone=card_key) — `ancestor_zones(title) == [card, column]`\n+- [ ] Rust: `children_of_zone(column_key)` returns cards only, not titles\n+- [ ] Rust: removing a zone leaves its descendants' `parent_zone` pointers intact (dangling is OK; nav will handle); unregistering triggers fallback handled in card `01KNS0B3HY...`\n+- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key`\n+- [ ] `focus-layer.test.tsx` — ULID generated, stable across re-renders, new on remount; `parentLayerKey` prop overrides ancestor context\n+- [ ] `focus-scope.test.tsx` — spatial key generated; zone scope publishes key to `FocusZoneContext`; leaf descendants pick it up as `parent_zone`; throws if outside a FocusLayer\n - [ ] Run `cargo test` and `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -6,7 +6,7 @@\n position_column: todo\n position_ordinal: a180\n project: spatial-nav\n-title: 'Spatial registry: layer forest + zones, typed keys, React measures rects'\n+title: 'Spatial registry: layer forest, typed keys, React measures rects'\n ---\n ## What\n\n@@ -13,60 +13,28 @@\n Add spatial awareness to the focus system with a clear frontend/backend split:\n\n - **React** measures DOM rects and reports them to Rust. React owns `FocusScope` (measures its element) and `FocusLayer` (declares a layer boundary).\n-- **Rust** owns the spatial registry, the **layer forest**, the **zone tree**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n-\n-### Two structural concepts\n-\n-1. **Layers** are **hard** boundaries. Nav never crosses them. Window roots, inspector overlay, dialog overlays are layers. Layers form a **forest** (one root per Tauri window, plus stacked children).\n-2. **Zones** are **soft** containers within a layer. They give structure to beam search — container-first, with leaf-level cross-zone fallback. Perspective, nav bar, toolbar, column, card, inspector panel, field row — all zones.\n+- **Rust** owns the spatial registry, the **layer forest**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n\n ### Keying model\n\n-- **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch.\n-- **SpatialKey** (ULID) — unique per FocusScope *instance*. Generated once per mount via `useRef(ulid())`. Primary key in the registry.\n-- Same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey.\n+Two separate key systems:\n\n-### Newtypes (Rust)\n+- **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch. Unchanged. Non-entity UI elements use a `\"ui:region.action\"` convention (e.g., `\"ui:toolbar.newTask\"`, `\"ui:tabbar.board1\"`).\n+- **SpatialKey** (ULID) — unique per FocusScope/FocusLayer *instance*. Generated once per mount via `useRef(ulid())`. Stable across re-renders, new on remount. This is the primary key in the Rust spatial registry.\n\n-Shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`:\n+Why separate: the same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey, own rect, possibly different layer. `navigate()` returns the winning entry's **moniker** (what `setFocus` needs).\n\n-```rust\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct WindowLabel(pub String);\n-#[serde(transparent)] pub struct SpatialKey(pub String);\n-#[serde(transparent)] pub struct LayerKey(pub String);\n-```\n+### Layer model: a forest, not a stack\n\n-### Layer model: forest, not stack\n+**The app is multi-window.** `UIState.windows: HashMap<String, WindowState>` already exists, with each window maintaining its own `inspector_stack`. The layer model must reflect this: multiple **parallel root layers** (one per window), each with **stacked child layers** (inspectors, dialogs) on top.\n\n ```rust\n pub struct LayerEntry {\n     pub key: LayerKey,\n-    pub name: String,                       // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n-    pub parent: Option<LayerKey>,           // None for window roots; Some(...) for stacked layers\n-    pub window_label: WindowLabel,          // which window this layer lives in\n-    pub last_focused: Option<SpatialKey>,   // focus memory for pop-restore\n-}\n-```\n-\n-Two windows each with their own inspector → 4 layers total, 2 roots. Layers removed by key, not popped — supports arbitrary unmount order.\n-\n-### Zone model within a layer\n-\n-```rust\n-pub enum ScopeKind {\n-    Leaf,   // focusable element (task title, status pill, field pill, button)\n-    Zone,   // container (column, card, inspector panel, field row, toolbar group)\n-}\n-\n-pub struct SpatialEntry {\n-    pub moniker: String,\n-    pub rect: Rect,\n-    pub layer_key: LayerKey,\n-    pub kind: ScopeKind,\n-    pub parent_zone: Option<SpatialKey>,    // nearest ancestor Zone in the same layer; None = layer root\n-    pub last_focused: Option<SpatialKey>,   // focus memory for drill-out / fallback when kind == Zone\n-    pub overrides: HashMap<Direction, Option<String>>,\n+    pub name: String,                 // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n+    pub parent: Option<LayerKey>,     // None for window roots; Some(...) for stacked layers\n+    pub window_label: WindowLabel,    // which window this layer lives in\n+    pub last_focused: Option<SpatialKey>, // focus memory for pop-restore\n }\n\n pub struct SpatialRegistry {\n@@ -75,142 +43,139 @@\n }\n ```\n\n-Zones form a **tree within each layer**, rooted at the layer root (`parent_zone = None`). Nav algorithm uses zones for container-first beam search and for drill-in/drill-out commands (see card `01KNQXXF5W7G4JP73C6ZCMKYKX` and the new drill-in/drill-out card).\n+A **layer forest**: trees rooted at each window's root layer. An inspector opened in window A has `parent = Some(windowA_root_layer_key)`. A confirm dialog on top of that inspector has `parent = Some(inspector_layer_key)`. Two windows each with their own inspector → 4 layers total, 2 roots.\n\n-### Which React regions become zones\n+Layers are removed by key (not pop) so arbitrary unmount order works — inspector A opens, dialog B opens on top, A closes first → `remove(A.key)`, B stays with its old parent pointer (valid — B's parent is the window, not A, if declared correctly by the component tree).\n\n-- `NavBar` → `kind=\"zone\"` (contains logo, menus, mode indicator as leaves)\n-- Toolbar / action groups → `kind=\"zone\"`\n-- Tab bar / perspective bar → `kind=\"zone\"`, each tab a leaf\n-- Perspective container → `kind=\"zone\"` (wraps the active view)\n-- Column (`column-view`) → `kind=\"zone\"` (contains header leaf + cards)\n-- Task card (`entity-card`) → `kind=\"zone\"` (contains title leaf + status leaf + pills)\n-- Inspector panel → `kind=\"zone\"` (contains field rows)\n-- Field row (inspector) → `kind=\"zone\"` (contains label + pills)\n-- Field pills, task titles, status badges, tab buttons → `kind=\"leaf\"` (default, no change needed)\n+### Newtypes\n\n-Detailed adoption happens in the migration cards (`01KNQXZ81Q...`, `01KNQY0P9J...`, and a new shell-wrapping card).\n+Use the newtypes from card `01KNM3YHHFJ3PTXZHD9EFKVBS6` (focus claim registry):\n\n+```rust\n+pub struct WindowLabel(pub String);\n+pub struct SpatialKey(pub String);\n+pub struct LayerKey(pub String);\n+```\n+\n+All three have `#[serde(transparent)]` so the wire format stays a plain string. React sees strings, Rust has strong typing.\n+\n ### Architecture\n\n ```\n React (measure + report)              Rust (track + navigate)\n ─────────────────────────              ──────────────────────\n-FocusScope mounts (kind, moniker)\n+FocusScope mounts\n   spatialKey = useRef(ulid())\n-  parent_zone_key = nearest ancestor FocusScope with kind=\"zone\" (from context)\n   → ResizeObserver fires\n   → invoke: spatial_register           → registry.insert(spatial_key,\n-    { key, moniker, rect, kind,           { moniker, rect, kind,\n-      layer_key, parent_zone }              layer_key, parent_zone })\n+    { key, moniker, rect, layer_key }     { moniker, rect, layer_key })\n\n FocusScope unmounts\n   → invoke: spatial_unregister         → registry.remove(spatial_key)\n+    { key }\n\n FocusLayer mounts\n   layerKey = useRef(ulid())\n-  parentLayerKey = nearest ancestor FocusLayer (via context) OR explicit prop\n+  parentLayerKey = nearest ancestor FocusLayer key (via context)\n+                 = None if this is a window root\n   → invoke: spatial_push_layer         → layers.insert(key,\n     { key, name, parent_key }             { name, parent, window_label })\n+                                          (window_label derived from tauri::Window)\n\n FocusLayer unmounts\n   → invoke: spatial_pop_layer          → layers.remove(key)\n+    { key }\n\n-nav.right pressed\n-  → invoke: spatial_navigate           → three-rule beam search scoped to\n-    { key, direction }                    focused entry's layer + zones\n-  ← Ok                                 ← emit focus-changed event\n+nav.right key pressed\n+  → invoke: spatial_navigate           → find_target(direction, focused_key)\n+    { key, direction }                    filter to focused's layer_key, score\n+  ← returns Ok                         ← emit focus-changed event with moniker\n ```\n\n ### Rust side\n\n+**`SpatialEntry`** struct:\n+- `moniker: String`\n+- `rect: Rect { x: f64, y: f64, width: f64, height: f64 }`\n+- `layer_key: LayerKey` — which layer this scope lives in\n+\n **`SpatialRegistry`** ops:\n-- `register(key: SpatialKey, moniker, rect, kind, layer_key, parent_zone)`\n+- `register(key: SpatialKey, moniker, rect, layer_key)`\n - `unregister(key: SpatialKey)`\n - `update_rect(key: SpatialKey, rect)`\n - `push_layer(key: LayerKey, name, parent_key: Option<LayerKey>, window_label: WindowLabel)`\n - `remove_layer(key: LayerKey)`\n-- `children_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n-- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>`\n-- `ancestors_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n-- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>`\n-- `children_of_zone(zone_key: SpatialKey) -> impl Iterator<Item=&SpatialEntry>` — entries with `parent_zone == Some(zone_key)`\n-- `ancestor_zones(key: SpatialKey) -> Vec<&SpatialEntry>` — walk `parent_zone` up to layer root\n+- `children_of(key: LayerKey) -> Vec<&LayerEntry>` — layers with this as parent\n+- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>` — the window root layer\n+- `ancestors_of(key: LayerKey) -> Vec<&LayerEntry>` — walk up to root\n+- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>` — for nav scoping\n+\n+**No global \"active layer\"**. Navigation is always scoped to the focused entry's `layer_key` (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`).\n\n **Tauri commands**:\n-- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, kind: ScopeKind, layer_key, parent_zone: Option<SpatialKey>)`\n+- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, layer_key)`\n - `spatial_unregister(window: tauri::Window, key)`\n-- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)`\n+- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)` — derives window_label from the `tauri::Window` param\n - `spatial_pop_layer(window: tauri::Window, key)`\n\n-`window_label` is derived from the `tauri::Window` parameter, never passed by React.\n-\n ### React side\n\n **`FocusLayer`** component (`kanban-app/ui/src/components/focus-layer.tsx`):\n-- Props: `name: string`, optional `parentLayerKey`, `children`\n+- Props: `name: string`, `children`\n - `const layerKey = useRef(ulid()).current`\n-- Resolves parent: explicit `parentLayerKey` prop wins; else nearest ancestor via `FocusLayerContext`; else `None` (root).\n-- Provides `layerKey` via context\n-- Mount: `invoke(\"spatial_push_layer\", { key, name, parentKey })`\n-- Unmount: `invoke(\"spatial_pop_layer\", { key })`\n+- Reads parent FocusLayer's key from context (throws if a non-root layer has no parent — but the root layer in the window shell is allowed to have `parent = undefined`)\n+- Provides `layerKey` via React context so descendant FocusScope and FocusLayer can read it\n+- On mount: `invoke(\"spatial_push_layer\", { key: layerKey, name, parentKey })`\n+- On unmount: `invoke(\"spatial_pop_layer\", { key: layerKey })`\n\n-**`FocusScope`** (`kanban-app/ui/src/components/focus-scope.tsx`):\n-- Props add: `kind?: \"leaf\" | \"zone\"` (default `\"leaf\"`)\n+**`FocusScope`** changes (`kanban-app/ui/src/components/focus-scope.tsx`):\n - `const spatialKey = useRef(ulid()).current`\n-- Reads `layerKey` from `FocusLayerContext` (throws if missing)\n-- Reads `parentZoneKey` from `FocusZoneContext` (the nearest ancestor FocusScope where `kind===\"zone\"` published its own key)\n-- When `kind === \"zone\"`, publishes its own `spatialKey` on `FocusZoneContext` for descendants\n-- `ResizeObserver` on the wrapper div\n-- Mount/resize: `invoke(\"spatial_register\", { key, moniker, ...rect, kind, layerKey, parentZone })`\n-- Unmount: `invoke(\"spatial_unregister\", { key })`\n+- Reads `layerKey` from FocusLayer context (throw if missing — every scope must be inside a layer)\n+- Add `ResizeObserver` on the wrapper div\n+- On mount/resize: `invoke(\"spatial_register\", { key: spatialKey, moniker, ...rect, layerKey })`\n+- On unmount: `invoke(\"spatial_unregister\", { key: spatialKey })`\n\n-**`App.tsx`** (or `app-shell.tsx`): wrap app root in `<FocusLayer name=\"window\">`.\n+**`App.tsx`** (or `app-shell.tsx`):\n+- Wrap app root in `<FocusLayer name=\"window\">` — this is the window root layer (no parent)\n\n ### Design decisions\n-- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. `#[serde(transparent)]` keeps the wire protocol simple.\n-- **Layers form a forest**: multi-window is first-class, not a retrofit. Two windows = two trees.\n-- **Zones form a tree within each layer**: gives nav algorithm container-first semantics without needing mode state.\n-- **parent_zone pointer**: simpler than a child list; `children_of_zone` walks the map once, fine for < ~hundreds of entries per layer.\n-- **Layer removal by key**: arbitrary removal order.\n-- **window_label derived, not passed**: Tauri knows it from the invoking webview.\n-- **`kind` on SpatialEntry** (not a separate type): zones and leaves share the same table; nav filters by `kind` where relevant.\n+- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. Serde-transparent so the wire protocol stays simple.\n+- **Layers form a forest, not a stack**: multi-window support is a first-class concern, not a retrofit. Two windows = two trees.\n+- **Parent pointer, not child list**: simpler to add/remove. `children_of(key)` walks the map once; fine for small layer counts (O(layers) rather than O(1), but we have < ~20 layers tops).\n+- **Layer removal by key**: supports arbitrary removal order.\n+- **window_label derived, not passed**: React never sends window_label — Tauri knows it from the invoking webview. One less thing for React to get wrong.\n\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n-- [ ] Define `Rect`, `ScopeKind`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry`\n-- [ ] Implement layer forest ops: `children_of_layer`, `root_for_window`, `ancestors_of_layer`\n-- [ ] Implement zone tree ops: `children_of_zone`, `ancestor_zones`\n-- [ ] `entries_in_layer` iterator for nav scoping\n-- [ ] Tauri commands derive `window_label` from `tauri::Window`\n-- [ ] Create `FocusLayer` component with ULID layer key, parent context/prop, Tauri invokes\n-- [ ] Create `FocusZoneContext` and update `FocusScope` with `kind` prop, parent_zone resolution, Tauri invokes\n-- [ ] Wrap app root in `<FocusLayer name=\"window\">`\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]` (shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`)\n+- [ ] Define `Rect`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry` in Rust\n+- [ ] Implement registry with forest ops: `children_of`, `root_for_window`, `ancestors_of`, `entries_in_layer`\n+- [ ] Add Tauri commands that derive `window_label` from the `tauri::Window` param\n+- [ ] Create FocusLayer component with ULID layer key, parent context, Tauri invokes\n+- [ ] Update FocusScope with ULID spatial key, ResizeObserver, Tauri invokes\n+- [ ] Wrap app root in `<FocusLayer name=\"window\">` (window root)\n\n ## Acceptance Criteria\n-- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes throughout Rust; wire format is plain strings\n-- [ ] Each FocusScope mount generates a unique ULID `SpatialKey`; each FocusLayer mount generates a unique ULID `LayerKey`\n-- [ ] Every layer carries its `window_label`, derived from Tauri (not React args)\n+- [ ] Spatial and layer keys use newtypes throughout Rust; wire format is plain strings\n+- [ ] Each FocusScope mount generates a unique ULID `SpatialKey` via `useRef`\n+- [ ] Each FocusLayer mount generates a unique ULID `LayerKey` via `useRef`\n+- [ ] Every layer carries its `window_label` (derived from Tauri, not from React args)\n - [ ] Root window layers have `parent = None`; stacked layers have `parent = Some(...)`\n-- [ ] The registry can represent: 2 windows + 2 inspector layers (one per window) + 1 dialog on top of one inspector = 5 layers, 2 roots, correct parent chains\n-- [ ] Every SpatialEntry has `kind` and `parent_zone`; the zone tree is well-formed (no cycles, all parents exist in same layer)\n-- [ ] `children_of_zone` returns direct children only (not transitive descendants)\n-- [ ] `ancestor_zones` walks up to the layer root (entries with `parent_zone = None`)\n-- [ ] Same moniker at two locations → two entries, different SpatialKeys\n-- [ ] Layer removal by key supports arbitrary order\n+- [ ] The registry can represent: 2 windows + 2 inspectors (one per window) + 1 dialog on top of one of the inspectors = 5 layers, 2 roots, correct parent chains\n+- [ ] Spatial registry keyed by `SpatialKey` — same moniker in two locations = two entries\n+- [ ] Layer removal by key supports arbitrary removal order\n+- [ ] Existing focus behavior unchanged\n - [ ] `cargo test` passes, `pnpm vitest run` passes\n\n ## Tests\n-- [ ] Rust: register two entries with same moniker, different keys — both stored\n-- [ ] Rust: push window root A + window root B (parent=None, different window_label) — coexist as siblings\n-- [ ] Rust: push window A, inspector I (parent=A), dialog D (parent=I) — `ancestors_of_layer(D) == [I, A]`\n-- [ ] Rust: `children_of_layer(A)` returns inspectors of A only, not of other windows\n-- [ ] Rust: register a column (Zone, parent_zone=None), a card (Zone, parent_zone=column_key), a title (Leaf, parent_zone=card_key) — `ancestor_zones(title) == [card, column]`\n-- [ ] Rust: `children_of_zone(column_key)` returns cards only, not titles\n-- [ ] Rust: removing a zone leaves its descendants' `parent_zone` pointers intact (dangling is OK; nav will handle); unregistering triggers fallback handled in card `01KNS0B3HY...`\n-- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key`\n-- [ ] `focus-layer.test.tsx` — ULID generated, stable across re-renders, new on remount; `parentLayerKey` prop overrides ancestor context\n-- [ ] `focus-scope.test.tsx` — spatial key generated; zone scope publishes key to `FocusZoneContext`; leaf descendants pick it up as `parent_zone`; throws if outside a FocusLayer\n+- [ ] Rust: register two entries with same moniker, different keys — both stored; unregister one, other remains\n+- [ ] Rust: push window root A, push window root B (both `parent = None`, different `window_label`) — both coexist as siblings\n+- [ ] Rust: push window A, push inspector I (parent = A), push dialog D (parent = I) — `ancestors_of(D) == [I, A]`, `root_for_window(A.window_label) == Some(A)`\n+- [ ] Rust: `children_of(window_root)` returns inspectors of that window only, not inspectors of another window\n+- [ ] Rust: remove inspector I while dialog D still present — D remains in registry with its (now dangling) parent pointer; nav in D's layer still works (layer ops don't follow parent)\n+- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key` — window's entries excluded even though window is the parent\n+- [ ] Rust: layer removal — remove non-existent key is a no-op, not an error\n+- [ ] `focus-layer.test.tsx` — ULID generated on mount, stable across re-renders, new on remount; nested layer reads parent from context and sends `parent_key` on push\n+- [ ] `focus-scope.test.tsx` — spatial key generated, registered with layer key from context; throws if rendered outside a FocusLayer\n - [ ] Run `cargo test` and `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","transaction_id":null}
{"id":"01KPZRT0K2S221NP09FA7VBR8J","timestamp":"2026-04-24T12:53:02.690234Z","entity_type":"task","entity_id":"01KNQXW7HHHB8HW76K3PXH3G34","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -3,28 +3,60 @@\n Add spatial awareness to the focus system with a clear frontend/backend split:\n\n - **React** measures DOM rects and reports them to Rust. React owns `FocusScope` (measures its element) and `FocusLayer` (declares a layer boundary).\n-- **Rust** owns the spatial registry, the **layer forest**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n+- **Rust** owns the spatial registry, the **layer forest**, the **zone tree**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n+\n+### Two structural concepts\n+\n+1. **Layers** are **hard** boundaries. Nav never crosses them. Window roots, inspector overlay, dialog overlays are layers. Layers form a **forest** (one root per Tauri window, plus stacked children).\n+2. **Zones** are **soft** containers within a layer. They give structure to beam search — container-first, with leaf-level cross-zone fallback. Perspective, nav bar, toolbar, column, card, inspector panel, field row — all zones.\n\n ### Keying model\n\n-Two separate key systems:\n+- **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch.\n+- **SpatialKey** (ULID) — unique per FocusScope *instance*. Generated once per mount via `useRef(ulid())`. Primary key in the registry.\n+- Same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey.\n\n-- **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch. Unchanged. Non-entity UI elements use a `\"ui:region.action\"` convention (e.g., `\"ui:toolbar.newTask\"`, `\"ui:tabbar.board1\"`).\n-- **SpatialKey** (ULID) — unique per FocusScope/FocusLayer *instance*. Generated once per mount via `useRef(ulid())`. Stable across re-renders, new on remount. This is the primary key in the Rust spatial registry.\n+### Newtypes (Rust)\n\n-Why separate: the same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey, own rect, possibly different layer. `navigate()` returns the winning entry's **moniker** (what `setFocus` needs).\n+Shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`:\n\n-### Layer model: a forest, not a stack\n+```rust\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct WindowLabel(pub String);\n+#[serde(transparent)] pub struct SpatialKey(pub String);\n+#[serde(transparent)] pub struct LayerKey(pub String);\n+```\n\n-**The app is multi-window.** `UIState.windows: HashMap<String, WindowState>` already exists, with each window maintaining its own `inspector_stack`. The layer model must reflect this: multiple **parallel root layers** (one per window), each with **stacked child layers** (inspectors, dialogs) on top.\n+### Layer model: forest, not stack\n\n ```rust\n pub struct LayerEntry {\n     pub key: LayerKey,\n-    pub name: String,                 // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n-    pub parent: Option<LayerKey>,     // None for window roots; Some(...) for stacked layers\n-    pub window_label: WindowLabel,    // which window this layer lives in\n-    pub last_focused: Option<SpatialKey>, // focus memory for pop-restore\n+    pub name: String,                       // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n+    pub parent: Option<LayerKey>,           // None for window roots; Some(...) for stacked layers\n+    pub window_label: WindowLabel,          // which window this layer lives in\n+    pub last_focused: Option<SpatialKey>,   // focus memory for pop-restore\n+}\n+```\n+\n+Two windows each with their own inspector → 4 layers total, 2 roots. Layers removed by key, not popped — supports arbitrary unmount order.\n+\n+### Zone model within a layer\n+\n+```rust\n+pub enum ScopeKind {\n+    Leaf,   // focusable element (task title, status pill, field pill, button)\n+    Zone,   // container (column, card, inspector panel, field row, toolbar group)\n+}\n+\n+pub struct SpatialEntry {\n+    pub moniker: String,\n+    pub rect: Rect,\n+    pub layer_key: LayerKey,\n+    pub kind: ScopeKind,\n+    pub parent_zone: Option<SpatialKey>,    // nearest ancestor Zone in the same layer; None = layer root\n+    pub last_focused: Option<SpatialKey>,   // focus memory for drill-out / fallback when kind == Zone\n+    pub overrides: HashMap<Direction, Option<String>>,\n }\n\n pub struct SpatialRegistry {\n@@ -33,21 +65,21 @@\n }\n ```\n\n-A **layer forest**: trees rooted at each window's root layer. An inspector opened in window A has `parent = Some(windowA_root_layer_key)`. A confirm dialog on top of that inspector has `parent = Some(inspector_layer_key)`. Two windows each with their own inspector → 4 layers total, 2 roots.\n-\n-Layers are removed by key (not pop) so arbitrary unmount order works — inspector A opens, dialog B opens on top, A closes first → `remove(A.key)`, B stays with its old parent pointer (valid — B's parent is the window, not A, if declared correctly by the component tree).\n+Zones form a **tree within each layer**, rooted at the layer root (`parent_zone = None`). Nav algorithm uses zones for container-first beam search and for drill-in/drill-out commands (see card `01KNQXXF5W7G4JP73C6ZCMKYKX` and the new drill-in/drill-out card).\n\n-### Newtypes\n+### Which React regions become zones\n\n-Use the newtypes from card `01KNM3YHHFJ3PTXZHD9EFKVBS6` (focus claim registry):\n-\n-```rust\n-pub struct WindowLabel(pub String);\n-pub struct SpatialKey(pub String);\n-pub struct LayerKey(pub String);\n-```\n+- `NavBar` → `kind=\"zone\"` (contains logo, menus, mode indicator as leaves)\n+- Toolbar / action groups → `kind=\"zone\"`\n+- Tab bar / perspective bar → `kind=\"zone\"`, each tab a leaf\n+- Perspective container → `kind=\"zone\"` (wraps the active view)\n+- Column (`column-view`) → `kind=\"zone\"` (contains header leaf + cards)\n+- Task card (`entity-card`) → `kind=\"zone\"` (contains title leaf + status leaf + pills)\n+- Inspector panel → `kind=\"zone\"` (contains field rows)\n+- Field row (inspector) → `kind=\"zone\"` (contains label + pills)\n+- Field pills, task titles, status badges, tab buttons → `kind=\"leaf\"` (default, no change needed)\n\n-All three have `#[serde(transparent)]` so the wire format stays a plain string. React sees strings, Rust has strong typing.\n+Detailed adoption happens in the migration cards (`01KNQXZ81Q...`, `01KNQY0P9J...`, and a new shell-wrapping card).\n\n ### Architecture\n\n@@ -54,118 +86,121 @@\n ```\n React (measure + report)              Rust (track + navigate)\n ─────────────────────────              ──────────────────────\n-FocusScope mounts\n+FocusScope mounts (kind, moniker)\n   spatialKey = useRef(ulid())\n+  parent_zone_key = nearest ancestor FocusScope with kind=\"zone\" (from context)\n   → ResizeObserver fires\n   → invoke: spatial_register           → registry.insert(spatial_key,\n-    { key, moniker, rect, layer_key }     { moniker, rect, layer_key })\n+    { key, moniker, rect, kind,           { moniker, rect, kind,\n+      layer_key, parent_zone }              layer_key, parent_zone })\n\n FocusScope unmounts\n   → invoke: spatial_unregister         → registry.remove(spatial_key)\n-    { key }\n\n FocusLayer mounts\n   layerKey = useRef(ulid())\n-  parentLayerKey = nearest ancestor FocusLayer key (via context)\n-                 = None if this is a window root\n+  parentLayerKey = nearest ancestor FocusLayer (via context) OR explicit prop\n   → invoke: spatial_push_layer         → layers.insert(key,\n     { key, name, parent_key }             { name, parent, window_label })\n-                                          (window_label derived from tauri::Window)\n\n FocusLayer unmounts\n   → invoke: spatial_pop_layer          → layers.remove(key)\n-    { key }\n\n-nav.right key pressed\n-  → invoke: spatial_navigate           → find_target(direction, focused_key)\n-    { key, direction }                    filter to focused's layer_key, score\n-  ← returns Ok                         ← emit focus-changed event with moniker\n+nav.right pressed\n+  → invoke: spatial_navigate           → three-rule beam search scoped to\n+    { key, direction }                    focused entry's layer + zones\n+  ← Ok                                 ← emit focus-changed event\n ```\n\n ### Rust side\n\n-**`SpatialEntry`** struct:\n-- `moniker: String`\n-- `rect: Rect { x: f64, y: f64, width: f64, height: f64 }`\n-- `layer_key: LayerKey` — which layer this scope lives in\n-\n **`SpatialRegistry`** ops:\n-- `register(key: SpatialKey, moniker, rect, layer_key)`\n+- `register(key: SpatialKey, moniker, rect, kind, layer_key, parent_zone)`\n - `unregister(key: SpatialKey)`\n - `update_rect(key: SpatialKey, rect)`\n - `push_layer(key: LayerKey, name, parent_key: Option<LayerKey>, window_label: WindowLabel)`\n - `remove_layer(key: LayerKey)`\n-- `children_of(key: LayerKey) -> Vec<&LayerEntry>` — layers with this as parent\n-- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>` — the window root layer\n-- `ancestors_of(key: LayerKey) -> Vec<&LayerEntry>` — walk up to root\n-- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>` — for nav scoping\n-\n-**No global \"active layer\"**. Navigation is always scoped to the focused entry's `layer_key` (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`).\n+- `children_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n+- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>`\n+- `ancestors_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n+- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>`\n+- `children_of_zone(zone_key: SpatialKey) -> impl Iterator<Item=&SpatialEntry>` — entries with `parent_zone == Some(zone_key)`\n+- `ancestor_zones(key: SpatialKey) -> Vec<&SpatialEntry>` — walk `parent_zone` up to layer root\n\n **Tauri commands**:\n-- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, layer_key)`\n+- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, kind: ScopeKind, layer_key, parent_zone: Option<SpatialKey>)`\n - `spatial_unregister(window: tauri::Window, key)`\n-- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)` — derives window_label from the `tauri::Window` param\n+- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)`\n - `spatial_pop_layer(window: tauri::Window, key)`\n\n+`window_label` is derived from the `tauri::Window` parameter, never passed by React.\n+\n ### React side\n\n **`FocusLayer`** component (`kanban-app/ui/src/components/focus-layer.tsx`):\n-- Props: `name: string`, `children`\n+- Props: `name: string`, optional `parentLayerKey`, `children`\n - `const layerKey = useRef(ulid()).current`\n-- Reads parent FocusLayer's key from context (throws if a non-root layer has no parent — but the root layer in the window shell is allowed to have `parent = undefined`)\n-- Provides `layerKey` via React context so descendant FocusScope and FocusLayer can read it\n-- On mount: `invoke(\"spatial_push_layer\", { key: layerKey, name, parentKey })`\n-- On unmount: `invoke(\"spatial_pop_layer\", { key: layerKey })`\n+- Resolves parent: explicit `parentLayerKey` prop wins; else nearest ancestor via `FocusLayerContext`; else `None` (root).\n+- Provides `layerKey` via context\n+- Mount: `invoke(\"spatial_push_layer\", { key, name, parentKey })`\n+- Unmount: `invoke(\"spatial_pop_layer\", { key })`\n\n-**`FocusScope`** changes (`kanban-app/ui/src/components/focus-scope.tsx`):\n+**`FocusScope`** (`kanban-app/ui/src/components/focus-scope.tsx`):\n+- Props add: `kind?: \"leaf\" | \"zone\"` (default `\"leaf\"`)\n - `const spatialKey = useRef(ulid()).current`\n-- Reads `layerKey` from FocusLayer context (throw if missing — every scope must be inside a layer)\n-- Add `ResizeObserver` on the wrapper div\n-- On mount/resize: `invoke(\"spatial_register\", { key: spatialKey, moniker, ...rect, layerKey })`\n-- On unmount: `invoke(\"spatial_unregister\", { key: spatialKey })`\n+- Reads `layerKey` from `FocusLayerContext` (throws if missing)\n+- Reads `parentZoneKey` from `FocusZoneContext` (the nearest ancestor FocusScope where `kind===\"zone\"` published its own key)\n+- When `kind === \"zone\"`, publishes its own `spatialKey` on `FocusZoneContext` for descendants\n+- `ResizeObserver` on the wrapper div\n+- Mount/resize: `invoke(\"spatial_register\", { key, moniker, ...rect, kind, layerKey, parentZone })`\n+- Unmount: `invoke(\"spatial_unregister\", { key })`\n\n-**`App.tsx`** (or `app-shell.tsx`):\n-- Wrap app root in `<FocusLayer name=\"window\">` — this is the window root layer (no parent)\n+**`App.tsx`** (or `app-shell.tsx`): wrap app root in `<FocusLayer name=\"window\">`.\n\n ### Design decisions\n-- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. Serde-transparent so the wire protocol stays simple.\n-- **Layers form a forest, not a stack**: multi-window support is a first-class concern, not a retrofit. Two windows = two trees.\n-- **Parent pointer, not child list**: simpler to add/remove. `children_of(key)` walks the map once; fine for small layer counts (O(layers) rather than O(1), but we have < ~20 layers tops).\n-- **Layer removal by key**: supports arbitrary removal order.\n-- **window_label derived, not passed**: React never sends window_label — Tauri knows it from the invoking webview. One less thing for React to get wrong.\n+- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. `#[serde(transparent)]` keeps the wire protocol simple.\n+- **Layers form a forest**: multi-window is first-class, not a retrofit. Two windows = two trees.\n+- **Zones form a tree within each layer**: gives nav algorithm container-first semantics without needing mode state.\n+- **parent_zone pointer**: simpler than a child list; `children_of_zone` walks the map once, fine for < ~hundreds of entries per layer.\n+- **Layer removal by key**: arbitrary removal order.\n+- **window_label derived, not passed**: Tauri knows it from the invoking webview.\n+- **`kind` on SpatialEntry** (not a separate type): zones and leaves share the same table; nav filters by `kind` where relevant.\n\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]` (shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`)\n-- [ ] Define `Rect`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry` in Rust\n-- [ ] Implement registry with forest ops: `children_of`, `root_for_window`, `ancestors_of`, `entries_in_layer`\n-- [ ] Add Tauri commands that derive `window_label` from the `tauri::Window` param\n-- [ ] Create FocusLayer component with ULID layer key, parent context, Tauri invokes\n-- [ ] Update FocusScope with ULID spatial key, ResizeObserver, Tauri invokes\n-- [ ] Wrap app root in `<FocusLayer name=\"window\">` (window root)\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n+- [ ] Define `Rect`, `ScopeKind`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry`\n+- [ ] Implement layer forest ops: `children_of_layer`, `root_for_window`, `ancestors_of_layer`\n+- [ ] Implement zone tree ops: `children_of_zone`, `ancestor_zones`\n+- [ ] `entries_in_layer` iterator for nav scoping\n+- [ ] Tauri commands derive `window_label` from `tauri::Window`\n+- [ ] Create `FocusLayer` component with ULID layer key, parent context/prop, Tauri invokes\n+- [ ] Create `FocusZoneContext` and update `FocusScope` with `kind` prop, parent_zone resolution, Tauri invokes\n+- [ ] Wrap app root in `<FocusLayer name=\"window\">`\n\n ## Acceptance Criteria\n-- [ ] Spatial and layer keys use newtypes throughout Rust; wire format is plain strings\n-- [ ] Each FocusScope mount generates a unique ULID `SpatialKey` via `useRef`\n-- [ ] Each FocusLayer mount generates a unique ULID `LayerKey` via `useRef`\n-- [ ] Every layer carries its `window_label` (derived from Tauri, not from React args)\n+- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes throughout Rust; wire format is plain strings\n+- [ ] Each FocusScope mount generates a unique ULID `SpatialKey`; each FocusLayer mount generates a unique ULID `LayerKey`\n+- [ ] Every layer carries its `window_label`, derived from Tauri (not React args)\n - [ ] Root window layers have `parent = None`; stacked layers have `parent = Some(...)`\n-- [ ] The registry can represent: 2 windows + 2 inspectors (one per window) + 1 dialog on top of one of the inspectors = 5 layers, 2 roots, correct parent chains\n-- [ ] Spatial registry keyed by `SpatialKey` — same moniker in two locations = two entries\n-- [ ] Layer removal by key supports arbitrary removal order\n-- [ ] Existing focus behavior unchanged\n+- [ ] The registry can represent: 2 windows + 2 inspector layers (one per window) + 1 dialog on top of one inspector = 5 layers, 2 roots, correct parent chains\n+- [ ] Every SpatialEntry has `kind` and `parent_zone`; the zone tree is well-formed (no cycles, all parents exist in same layer)\n+- [ ] `children_of_zone` returns direct children only (not transitive descendants)\n+- [ ] `ancestor_zones` walks up to the layer root (entries with `parent_zone = None`)\n+- [ ] Same moniker at two locations → two entries, different SpatialKeys\n+- [ ] Layer removal by key supports arbitrary order\n - [ ] `cargo test` passes, `pnpm vitest run` passes\n\n ## Tests\n-- [ ] Rust: register two entries with same moniker, different keys — both stored; unregister one, other remains\n-- [ ] Rust: push window root A, push window root B (both `parent = None`, different `window_label`) — both coexist as siblings\n-- [ ] Rust: push window A, push inspector I (parent = A), push dialog D (parent = I) — `ancestors_of(D) == [I, A]`, `root_for_window(A.window_label) == Some(A)`\n-- [ ] Rust: `children_of(window_root)` returns inspectors of that window only, not inspectors of another window\n-- [ ] Rust: remove inspector I while dialog D still present — D remains in registry with its (now dangling) parent pointer; nav in D's layer still works (layer ops don't follow parent)\n-- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key` — window's entries excluded even though window is the parent\n-- [ ] Rust: layer removal — remove non-existent key is a no-op, not an error\n-- [ ] `focus-layer.test.tsx` — ULID generated on mount, stable across re-renders, new on remount; nested layer reads parent from context and sends `parent_key` on push\n-- [ ] `focus-scope.test.tsx` — spatial key generated, registered with layer key from context; throws if rendered outside a FocusLayer\n+- [ ] Rust: register two entries with same moniker, different keys — both stored\n+- [ ] Rust: push window root A + window root B (parent=None, different window_label) — coexist as siblings\n+- [ ] Rust: push window A, inspector I (parent=A), dialog D (parent=I) — `ancestors_of_layer(D) == [I, A]`\n+- [ ] Rust: `children_of_layer(A)` returns inspectors of A only, not of other windows\n+- [ ] Rust: register a column (Zone, parent_zone=None), a card (Zone, parent_zone=column_key), a title (Leaf, parent_zone=card_key) — `ancestor_zones(title) == [card, column]`\n+- [ ] Rust: `children_of_zone(column_key)` returns cards only, not titles\n+- [ ] Rust: removing a zone leaves its descendants' `parent_zone` pointers intact (dangling is OK; nav will handle); unregistering triggers fallback handled in card `01KNS0B3HY...`\n+- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key`\n+- [ ] `focus-layer.test.tsx` — ULID generated, stable across re-renders, new on remount; `parentLayerKey` prop overrides ancestor context\n+- [ ] `focus-scope.test.tsx` — spatial key generated; zone scope publishes key to `FocusZoneContext`; leaf descendants pick it up as `parent_zone`; throws if outside a FocusLayer\n - [ ] Run `cargo test` and `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -3,60 +3,28 @@\n Add spatial awareness to the focus system with a clear frontend/backend split:\n\n - **React** measures DOM rects and reports them to Rust. React owns `FocusScope` (measures its element) and `FocusLayer` (declares a layer boundary).\n-- **Rust** owns the spatial registry, the **layer forest**, the **zone tree**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n-\n-### Two structural concepts\n-\n-1. **Layers** are **hard** boundaries. Nav never crosses them. Window roots, inspector overlay, dialog overlays are layers. Layers form a **forest** (one root per Tauri window, plus stacked children).\n-2. **Zones** are **soft** containers within a layer. They give structure to beam search — container-first, with leaf-level cross-zone fallback. Perspective, nav bar, toolbar, column, card, inspector panel, field row — all zones.\n+- **Rust** owns the spatial registry, the **layer forest**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n\n ### Keying model\n\n-- **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch.\n-- **SpatialKey** (ULID) — unique per FocusScope *instance*. Generated once per mount via `useRef(ulid())`. Primary key in the registry.\n-- Same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey.\n+Two separate key systems:\n\n-### Newtypes (Rust)\n+- **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch. Unchanged. Non-entity UI elements use a `\"ui:region.action\"` convention (e.g., `\"ui:toolbar.newTask\"`, `\"ui:tabbar.board1\"`).\n+- **SpatialKey** (ULID) — unique per FocusScope/FocusLayer *instance*. Generated once per mount via `useRef(ulid())`. Stable across re-renders, new on remount. This is the primary key in the Rust spatial registry.\n\n-Shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`:\n+Why separate: the same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey, own rect, possibly different layer. `navigate()` returns the winning entry's **moniker** (what `setFocus` needs).\n\n-```rust\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct WindowLabel(pub String);\n-#[serde(transparent)] pub struct SpatialKey(pub String);\n-#[serde(transparent)] pub struct LayerKey(pub String);\n-```\n+### Layer model: a forest, not a stack\n\n-### Layer model: forest, not stack\n+**The app is multi-window.** `UIState.windows: HashMap<String, WindowState>` already exists, with each window maintaining its own `inspector_stack`. The layer model must reflect this: multiple **parallel root layers** (one per window), each with **stacked child layers** (inspectors, dialogs) on top.\n\n ```rust\n pub struct LayerEntry {\n     pub key: LayerKey,\n-    pub name: String,                       // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n-    pub parent: Option<LayerKey>,           // None for window roots; Some(...) for stacked layers\n-    pub window_label: WindowLabel,          // which window this layer lives in\n-    pub last_focused: Option<SpatialKey>,   // focus memory for pop-restore\n-}\n-```\n-\n-Two windows each with their own inspector → 4 layers total, 2 roots. Layers removed by key, not popped — supports arbitrary unmount order.\n-\n-### Zone model within a layer\n-\n-```rust\n-pub enum ScopeKind {\n-    Leaf,   // focusable element (task title, status pill, field pill, button)\n-    Zone,   // container (column, card, inspector panel, field row, toolbar group)\n-}\n-\n-pub struct SpatialEntry {\n-    pub moniker: String,\n-    pub rect: Rect,\n-    pub layer_key: LayerKey,\n-    pub kind: ScopeKind,\n-    pub parent_zone: Option<SpatialKey>,    // nearest ancestor Zone in the same layer; None = layer root\n-    pub last_focused: Option<SpatialKey>,   // focus memory for drill-out / fallback when kind == Zone\n-    pub overrides: HashMap<Direction, Option<String>>,\n+    pub name: String,                 // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n+    pub parent: Option<LayerKey>,     // None for window roots; Some(...) for stacked layers\n+    pub window_label: WindowLabel,    // which window this layer lives in\n+    pub last_focused: Option<SpatialKey>, // focus memory for pop-restore\n }\n\n pub struct SpatialRegistry {\n@@ -65,142 +33,139 @@\n }\n ```\n\n-Zones form a **tree within each layer**, rooted at the layer root (`parent_zone = None`). Nav algorithm uses zones for container-first beam search and for drill-in/drill-out commands (see card `01KNQXXF5W7G4JP73C6ZCMKYKX` and the new drill-in/drill-out card).\n+A **layer forest**: trees rooted at each window's root layer. An inspector opened in window A has `parent = Some(windowA_root_layer_key)`. A confirm dialog on top of that inspector has `parent = Some(inspector_layer_key)`. Two windows each with their own inspector → 4 layers total, 2 roots.\n\n-### Which React regions become zones\n+Layers are removed by key (not pop) so arbitrary unmount order works — inspector A opens, dialog B opens on top, A closes first → `remove(A.key)`, B stays with its old parent pointer (valid — B's parent is the window, not A, if declared correctly by the component tree).\n\n-- `NavBar` → `kind=\"zone\"` (contains logo, menus, mode indicator as leaves)\n-- Toolbar / action groups → `kind=\"zone\"`\n-- Tab bar / perspective bar → `kind=\"zone\"`, each tab a leaf\n-- Perspective container → `kind=\"zone\"` (wraps the active view)\n-- Column (`column-view`) → `kind=\"zone\"` (contains header leaf + cards)\n-- Task card (`entity-card`) → `kind=\"zone\"` (contains title leaf + status leaf + pills)\n-- Inspector panel → `kind=\"zone\"` (contains field rows)\n-- Field row (inspector) → `kind=\"zone\"` (contains label + pills)\n-- Field pills, task titles, status badges, tab buttons → `kind=\"leaf\"` (default, no change needed)\n+### Newtypes\n\n-Detailed adoption happens in the migration cards (`01KNQXZ81Q...`, `01KNQY0P9J...`, and a new shell-wrapping card).\n+Use the newtypes from card `01KNM3YHHFJ3PTXZHD9EFKVBS6` (focus claim registry):\n\n+```rust\n+pub struct WindowLabel(pub String);\n+pub struct SpatialKey(pub String);\n+pub struct LayerKey(pub String);\n+```\n+\n+All three have `#[serde(transparent)]` so the wire format stays a plain string. React sees strings, Rust has strong typing.\n+\n ### Architecture\n\n ```\n React (measure + report)              Rust (track + navigate)\n ─────────────────────────              ──────────────────────\n-FocusScope mounts (kind, moniker)\n+FocusScope mounts\n   spatialKey = useRef(ulid())\n-  parent_zone_key = nearest ancestor FocusScope with kind=\"zone\" (from context)\n   → ResizeObserver fires\n   → invoke: spatial_register           → registry.insert(spatial_key,\n-    { key, moniker, rect, kind,           { moniker, rect, kind,\n-      layer_key, parent_zone }              layer_key, parent_zone })\n+    { key, moniker, rect, layer_key }     { moniker, rect, layer_key })\n\n FocusScope unmounts\n   → invoke: spatial_unregister         → registry.remove(spatial_key)\n+    { key }\n\n FocusLayer mounts\n   layerKey = useRef(ulid())\n-  parentLayerKey = nearest ancestor FocusLayer (via context) OR explicit prop\n+  parentLayerKey = nearest ancestor FocusLayer key (via context)\n+                 = None if this is a window root\n   → invoke: spatial_push_layer         → layers.insert(key,\n     { key, name, parent_key }             { name, parent, window_label })\n+                                          (window_label derived from tauri::Window)\n\n FocusLayer unmounts\n   → invoke: spatial_pop_layer          → layers.remove(key)\n+    { key }\n\n-nav.right pressed\n-  → invoke: spatial_navigate           → three-rule beam search scoped to\n-    { key, direction }                    focused entry's layer + zones\n-  ← Ok                                 ← emit focus-changed event\n+nav.right key pressed\n+  → invoke: spatial_navigate           → find_target(direction, focused_key)\n+    { key, direction }                    filter to focused's layer_key, score\n+  ← returns Ok                         ← emit focus-changed event with moniker\n ```\n\n ### Rust side\n\n+**`SpatialEntry`** struct:\n+- `moniker: String`\n+- `rect: Rect { x: f64, y: f64, width: f64, height: f64 }`\n+- `layer_key: LayerKey` — which layer this scope lives in\n+\n **`SpatialRegistry`** ops:\n-- `register(key: SpatialKey, moniker, rect, kind, layer_key, parent_zone)`\n+- `register(key: SpatialKey, moniker, rect, layer_key)`\n - `unregister(key: SpatialKey)`\n - `update_rect(key: SpatialKey, rect)`\n - `push_layer(key: LayerKey, name, parent_key: Option<LayerKey>, window_label: WindowLabel)`\n - `remove_layer(key: LayerKey)`\n-- `children_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n-- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>`\n-- `ancestors_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n-- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>`\n-- `children_of_zone(zone_key: SpatialKey) -> impl Iterator<Item=&SpatialEntry>` — entries with `parent_zone == Some(zone_key)`\n-- `ancestor_zones(key: SpatialKey) -> Vec<&SpatialEntry>` — walk `parent_zone` up to layer root\n+- `children_of(key: LayerKey) -> Vec<&LayerEntry>` — layers with this as parent\n+- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>` — the window root layer\n+- `ancestors_of(key: LayerKey) -> Vec<&LayerEntry>` — walk up to root\n+- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>` — for nav scoping\n+\n+**No global \"active layer\"**. Navigation is always scoped to the focused entry's `layer_key` (see card `01KNQXXF5W7G4JP73C6ZCMKYKX`).\n\n **Tauri commands**:\n-- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, kind: ScopeKind, layer_key, parent_zone: Option<SpatialKey>)`\n+- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, layer_key)`\n - `spatial_unregister(window: tauri::Window, key)`\n-- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)`\n+- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)` — derives window_label from the `tauri::Window` param\n - `spatial_pop_layer(window: tauri::Window, key)`\n\n-`window_label` is derived from the `tauri::Window` parameter, never passed by React.\n-\n ### React side\n\n **`FocusLayer`** component (`kanban-app/ui/src/components/focus-layer.tsx`):\n-- Props: `name: string`, optional `parentLayerKey`, `children`\n+- Props: `name: string`, `children`\n - `const layerKey = useRef(ulid()).current`\n-- Resolves parent: explicit `parentLayerKey` prop wins; else nearest ancestor via `FocusLayerContext`; else `None` (root).\n-- Provides `layerKey` via context\n-- Mount: `invoke(\"spatial_push_layer\", { key, name, parentKey })`\n-- Unmount: `invoke(\"spatial_pop_layer\", { key })`\n+- Reads parent FocusLayer's key from context (throws if a non-root layer has no parent — but the root layer in the window shell is allowed to have `parent = undefined`)\n+- Provides `layerKey` via React context so descendant FocusScope and FocusLayer can read it\n+- On mount: `invoke(\"spatial_push_layer\", { key: layerKey, name, parentKey })`\n+- On unmount: `invoke(\"spatial_pop_layer\", { key: layerKey })`\n\n-**`FocusScope`** (`kanban-app/ui/src/components/focus-scope.tsx`):\n-- Props add: `kind?: \"leaf\" | \"zone\"` (default `\"leaf\"`)\n+**`FocusScope`** changes (`kanban-app/ui/src/components/focus-scope.tsx`):\n - `const spatialKey = useRef(ulid()).current`\n-- Reads `layerKey` from `FocusLayerContext` (throws if missing)\n-- Reads `parentZoneKey` from `FocusZoneContext` (the nearest ancestor FocusScope where `kind===\"zone\"` published its own key)\n-- When `kind === \"zone\"`, publishes its own `spatialKey` on `FocusZoneContext` for descendants\n-- `ResizeObserver` on the wrapper div\n-- Mount/resize: `invoke(\"spatial_register\", { key, moniker, ...rect, kind, layerKey, parentZone })`\n-- Unmount: `invoke(\"spatial_unregister\", { key })`\n+- Reads `layerKey` from FocusLayer context (throw if missing — every scope must be inside a layer)\n+- Add `ResizeObserver` on the wrapper div\n+- On mount/resize: `invoke(\"spatial_register\", { key: spatialKey, moniker, ...rect, layerKey })`\n+- On unmount: `invoke(\"spatial_unregister\", { key: spatialKey })`\n\n-**`App.tsx`** (or `app-shell.tsx`): wrap app root in `<FocusLayer name=\"window\">`.\n+**`App.tsx`** (or `app-shell.tsx`):\n+- Wrap app root in `<FocusLayer name=\"window\">` — this is the window root layer (no parent)\n\n ### Design decisions\n-- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. `#[serde(transparent)]` keeps the wire protocol simple.\n-- **Layers form a forest**: multi-window is first-class, not a retrofit. Two windows = two trees.\n-- **Zones form a tree within each layer**: gives nav algorithm container-first semantics without needing mode state.\n-- **parent_zone pointer**: simpler than a child list; `children_of_zone` walks the map once, fine for < ~hundreds of entries per layer.\n-- **Layer removal by key**: arbitrary removal order.\n-- **window_label derived, not passed**: Tauri knows it from the invoking webview.\n-- **`kind` on SpatialEntry** (not a separate type): zones and leaves share the same table; nav filters by `kind` where relevant.\n+- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. Serde-transparent so the wire protocol stays simple.\n+- **Layers form a forest, not a stack**: multi-window support is a first-class concern, not a retrofit. Two windows = two trees.\n+- **Parent pointer, not child list**: simpler to add/remove. `children_of(key)` walks the map once; fine for small layer counts (O(layers) rather than O(1), but we have < ~20 layers tops).\n+- **Layer removal by key**: supports arbitrary removal order.\n+- **window_label derived, not passed**: React never sends window_label — Tauri knows it from the invoking webview. One less thing for React to get wrong.\n\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n-- [ ] Define `Rect`, `ScopeKind`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry`\n-- [ ] Implement layer forest ops: `children_of_layer`, `root_for_window`, `ancestors_of_layer`\n-- [ ] Implement zone tree ops: `children_of_zone`, `ancestor_zones`\n-- [ ] `entries_in_layer` iterator for nav scoping\n-- [ ] Tauri commands derive `window_label` from `tauri::Window`\n-- [ ] Create `FocusLayer` component with ULID layer key, parent context/prop, Tauri invokes\n-- [ ] Create `FocusZoneContext` and update `FocusScope` with `kind` prop, parent_zone resolution, Tauri invokes\n-- [ ] Wrap app root in `<FocusLayer name=\"window\">`\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]` (shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`)\n+- [ ] Define `Rect`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry` in Rust\n+- [ ] Implement registry with forest ops: `children_of`, `root_for_window`, `ancestors_of`, `entries_in_layer`\n+- [ ] Add Tauri commands that derive `window_label` from the `tauri::Window` param\n+- [ ] Create FocusLayer component with ULID layer key, parent context, Tauri invokes\n+- [ ] Update FocusScope with ULID spatial key, ResizeObserver, Tauri invokes\n+- [ ] Wrap app root in `<FocusLayer name=\"window\">` (window root)\n\n ## Acceptance Criteria\n-- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes throughout Rust; wire format is plain strings\n-- [ ] Each FocusScope mount generates a unique ULID `SpatialKey`; each FocusLayer mount generates a unique ULID `LayerKey`\n-- [ ] Every layer carries its `window_label`, derived from Tauri (not React args)\n+- [ ] Spatial and layer keys use newtypes throughout Rust; wire format is plain strings\n+- [ ] Each FocusScope mount generates a unique ULID `SpatialKey` via `useRef`\n+- [ ] Each FocusLayer mount generates a unique ULID `LayerKey` via `useRef`\n+- [ ] Every layer carries its `window_label` (derived from Tauri, not from React args)\n - [ ] Root window layers have `parent = None`; stacked layers have `parent = Some(...)`\n-- [ ] The registry can represent: 2 windows + 2 inspector layers (one per window) + 1 dialog on top of one inspector = 5 layers, 2 roots, correct parent chains\n-- [ ] Every SpatialEntry has `kind` and `parent_zone`; the zone tree is well-formed (no cycles, all parents exist in same layer)\n-- [ ] `children_of_zone` returns direct children only (not transitive descendants)\n-- [ ] `ancestor_zones` walks up to the layer root (entries with `parent_zone = None`)\n-- [ ] Same moniker at two locations → two entries, different SpatialKeys\n-- [ ] Layer removal by key supports arbitrary order\n+- [ ] The registry can represent: 2 windows + 2 inspectors (one per window) + 1 dialog on top of one of the inspectors = 5 layers, 2 roots, correct parent chains\n+- [ ] Spatial registry keyed by `SpatialKey` — same moniker in two locations = two entries\n+- [ ] Layer removal by key supports arbitrary removal order\n+- [ ] Existing focus behavior unchanged\n - [ ] `cargo test` passes, `pnpm vitest run` passes\n\n ## Tests\n-- [ ] Rust: register two entries with same moniker, different keys — both stored\n-- [ ] Rust: push window root A + window root B (parent=None, different window_label) — coexist as siblings\n-- [ ] Rust: push window A, inspector I (parent=A), dialog D (parent=I) — `ancestors_of_layer(D) == [I, A]`\n-- [ ] Rust: `children_of_layer(A)` returns inspectors of A only, not of other windows\n-- [ ] Rust: register a column (Zone, parent_zone=None), a card (Zone, parent_zone=column_key), a title (Leaf, parent_zone=card_key) — `ancestor_zones(title) == [card, column]`\n-- [ ] Rust: `children_of_zone(column_key)` returns cards only, not titles\n-- [ ] Rust: removing a zone leaves its descendants' `parent_zone` pointers intact (dangling is OK; nav will handle); unregistering triggers fallback handled in card `01KNS0B3HY...`\n-- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key`\n-- [ ] `focus-layer.test.tsx` — ULID generated, stable across re-renders, new on remount; `parentLayerKey` prop overrides ancestor context\n-- [ ] `focus-scope.test.tsx` — spatial key generated; zone scope publishes key to `FocusZoneContext`; leaf descendants pick it up as `parent_zone`; throws if outside a FocusLayer\n+- [ ] Rust: register two entries with same moniker, different keys — both stored; unregister one, other remains\n+- [ ] Rust: push window root A, push window root B (both `parent = None`, different `window_label`) — both coexist as siblings\n+- [ ] Rust: push window A, push inspector I (parent = A), push dialog D (parent = I) — `ancestors_of(D) == [I, A]`, `root_for_window(A.window_label) == Some(A)`\n+- [ ] Rust: `children_of(window_root)` returns inspectors of that window only, not inspectors of another window\n+- [ ] Rust: remove inspector I while dialog D still present — D remains in registry with its (now dangling) parent pointer; nav in D's layer still works (layer ops don't follow parent)\n+- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key` — window's entries excluded even though window is the parent\n+- [ ] Rust: layer removal — remove non-existent key is a no-op, not an error\n+- [ ] `focus-layer.test.tsx` — ULID generated on mount, stable across re-renders, new on remount; nested layer reads parent from context and sends `parent_key` on push\n+- [ ] `focus-scope.test.tsx` — spatial key generated, registered with layer key from context; throws if rendered outside a FocusLayer\n - [ ] Run `cargo test` and `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Spatial registry: layer forest, typed keys, React measures rects\n\\ No newline at end of file\n+Spatial registry: layer forest + zones, typed keys, React measures rects\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Spatial registry: layer forest + zones, typed keys, React measures rects\n\\ No newline at end of file\n+Spatial registry: layer forest, typed keys, React measures rects\n\\ No newline at end of file\n"}]]}
{"id":"01KPZWJWT057A2SZZZ64KC3HMA","timestamp":"2026-04-24T13:59:03.744647Z","op":"update","item_id":"01KNQXW7HHHB8HW76K3PXH3G34","forward_patch":"--- original\n+++ modified\n@@ -6,29 +6,28 @@\n position_column: todo\n position_ordinal: a180\n project: spatial-nav\n-title: 'Spatial registry: layer forest + zones, typed keys, React measures rects'\n+title: 'Rust kernel: Focusable, FocusZone, FocusLayer, FocusScope types + SpatialRegistry'\n ---\n ## What\n\n-Add spatial awareness to the focus system with a clear frontend/backend split:\n+Define the Rust kernel types for spatial focus. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n-- **React** measures DOM rects and reports them to Rust. React owns `FocusScope` (measures its element) and `FocusLayer` (declares a layer boundary).\n-- **Rust** owns the spatial registry, the **layer forest**, the **zone tree**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n+### The four peer types\n\n-### Two structural concepts\n-\n-1. **Layers** are **hard** boundaries. Nav never crosses them. Window roots, inspector overlay, dialog overlays are layers. Layers form a **forest** (one root per Tauri window, plus stacked children).\n-2. **Zones** are **soft** containers within a layer. They give structure to beam search — container-first, with leaf-level cross-zone fallback. Perspective, nav bar, toolbar, column, card, inspector panel, field row — all zones.\n+| Role                | React component     | Rust type             |\n+|---------------------|---------------------|-----------------------|\n+| Leaf focusable point| `Focusable`         | `struct Focusable`    |\n+| Navigable container | `FocusZone`         | `struct FocusZone`    |\n+| Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n+| Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n\n-### Keying model\n+On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum delegate to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`, etc.).\n\n-- **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch.\n-- **SpatialKey** (ULID) — unique per FocusScope *instance*. Generated once per mount via `useRef(ulid())`. Primary key in the registry.\n-- Same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey.\n+On React: `FocusScope` is the high-level convenience wrapper that internally uses a `Focusable` or `FocusZone` primitive and adds entity plumbing (CommandScopeProvider, click-to-focus, context menu, focus bar, data-moniker attrs).\n\n-### Newtypes (Rust)\n+### Newtypes\n\n-Shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`:\n+Shared with card `01KNM3YHHFJ3...`:\n\n ```rust\n #[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n@@ -37,181 +36,166 @@\n #[serde(transparent)] pub struct LayerKey(pub String);\n ```\n\n-### Layer model: forest, not stack\n+### Rust types\n\n ```rust\n-pub struct LayerEntry {\n-    pub key: LayerKey,\n-    pub name: String,                       // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n-    pub parent: Option<LayerKey>,           // None for window roots; Some(...) for stacked layers\n-    pub window_label: WindowLabel,          // which window this layer lives in\n-    pub last_focused: Option<SpatialKey>,   // focus memory for pop-restore\n-}\n-```\n-\n-Two windows each with their own inspector → 4 layers total, 2 roots. Layers removed by key, not popped — supports arbitrary unmount order.\n-\n-### Zone model within a layer\n-\n-```rust\n-pub enum ScopeKind {\n-    Leaf,   // focusable element (task title, status pill, field pill, button)\n-    Zone,   // container (column, card, inspector panel, field row, toolbar group)\n+/// Leaf — a single focusable point (task title, pill, button, menu item).\n+#[derive(Debug, Clone, Serialize, Deserialize)]\n+pub struct Focusable {\n+    pub key: SpatialKey,\n+    pub moniker: String,\n+    pub rect: Rect,\n+    pub layer_key: LayerKey,\n+    pub parent_zone: Option<SpatialKey>,\n+    pub overrides: HashMap<Direction, Option<String>>,\n }\n\n-pub struct SpatialEntry {\n+/// Zone — a navigable container (card, column, inspector panel, field row,\n+/// toolbar group, nav bar). Has its own focus memory for drill-out / fallback.\n+#[derive(Debug, Clone, Serialize, Deserialize)]\n+pub struct FocusZone {\n+    pub key: SpatialKey,\n     pub moniker: String,\n     pub rect: Rect,\n     pub layer_key: LayerKey,\n-    pub kind: ScopeKind,\n-    pub parent_zone: Option<SpatialKey>,    // nearest ancestor Zone in the same layer; None = layer root\n-    pub last_focused: Option<SpatialKey>,   // focus memory for drill-out / fallback when kind == Zone\n+    pub parent_zone: Option<SpatialKey>,\n+    pub last_focused: Option<SpatialKey>,\n     pub overrides: HashMap<Direction, Option<String>>,\n }\n\n-pub struct SpatialRegistry {\n-    entries: HashMap<SpatialKey, SpatialEntry>,\n-    layers:  HashMap<LayerKey, LayerEntry>,\n+/// Layer — a hard modal boundary. Window roots, inspector overlay, dialogs.\n+#[derive(Debug, Clone, Serialize, Deserialize)]\n+pub struct FocusLayer {\n+    pub key: LayerKey,\n+    pub name: String,\n+    pub parent: Option<LayerKey>,\n+    pub window_label: WindowLabel,\n+    pub last_focused: Option<SpatialKey>,\n }\n-```\n\n-Zones form a **tree within each layer**, rooted at the layer root (`parent_zone = None`). Nav algorithm uses zones for container-first beam search and for drill-in/drill-out commands (see card `01KNQXXF5W7G4JP73C6ZCMKYKX` and the new drill-in/drill-out card).\n+/// Sum type stored in the registry per SpatialKey.\n+#[derive(Debug, Clone, Serialize, Deserialize)]\n+#[serde(tag = \"kind\", rename_all = \"snake_case\")]\n+pub enum FocusScope {\n+    Focusable(Focusable),\n+    Zone(FocusZone),\n+}\n\n-### Which React regions become zones\n+impl FocusScope {\n+    pub fn key(&self) -> &SpatialKey { /* dispatch */ }\n+    pub fn moniker(&self) -> &str { /* dispatch */ }\n+    pub fn rect(&self) -> &Rect { /* dispatch */ }\n+    pub fn layer_key(&self) -> &LayerKey { /* dispatch */ }\n+    pub fn parent_zone(&self) -> Option<&SpatialKey> { /* dispatch */ }\n+    pub fn overrides(&self) -> &HashMap<Direction, Option<String>> { /* dispatch */ }\n+    pub fn is_zone(&self) -> bool { matches!(self, Self::Zone(_)) }\n+    pub fn is_focusable(&self) -> bool { matches!(self, Self::Focusable(_)) }\n+    pub fn as_zone(&self) -> Option<&FocusZone> { /* ... */ }\n+    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone> { /* ... */ }\n+}\n+```\n\n-- `NavBar` → `kind=\"zone\"` (contains logo, menus, mode indicator as leaves)\n-- Toolbar / action groups → `kind=\"zone\"`\n-- Tab bar / perspective bar → `kind=\"zone\"`, each tab a leaf\n-- Perspective container → `kind=\"zone\"` (wraps the active view)\n-- Column (`column-view`) → `kind=\"zone\"` (contains header leaf + cards)\n-- Task card (`entity-card`) → `kind=\"zone\"` (contains title leaf + status leaf + pills)\n-- Inspector panel → `kind=\"zone\"` (contains field rows)\n-- Field row (inspector) → `kind=\"zone\"` (contains label + pills)\n-- Field pills, task titles, status badges, tab buttons → `kind=\"leaf\"` (default, no change needed)\n+### Registry\n\n-Detailed adoption happens in the migration cards (`01KNQXZ81Q...`, `01KNQY0P9J...`, and a new shell-wrapping card).\n+```rust\n+pub struct SpatialRegistry {\n+    scopes: HashMap<SpatialKey, FocusScope>,\n+    layers: HashMap<LayerKey, FocusLayer>,\n+}\n+\n+impl SpatialRegistry {\n+    // Scope ops\n+    pub fn register_focusable(&mut self, f: Focusable);\n+    pub fn register_zone(&mut self, z: FocusZone);\n+    pub fn unregister_scope(&mut self, key: &SpatialKey);\n+    pub fn update_rect(&mut self, key: &SpatialKey, rect: Rect);\n+    pub fn scope(&self, key: &SpatialKey) -> Option<&FocusScope>;\n+    pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = &FocusScope>;\n+    pub fn ancestor_zones(&self, key: &SpatialKey) -> Vec<&FocusZone>;\n\n-### Architecture\n+    // Layer ops\n+    pub fn push_layer(&mut self, l: FocusLayer);\n+    pub fn remove_layer(&mut self, key: &LayerKey);\n+    pub fn layer(&self, key: &LayerKey) -> Option<&FocusLayer>;\n+    pub fn children_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n+    pub fn root_for_window(&self, label: &WindowLabel) -> Option<&FocusLayer>;\n+    pub fn ancestors_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n\n+    // Nav scoping\n+    pub fn scopes_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n+}\n ```\n-React (measure + report)              Rust (track + navigate)\n-─────────────────────────              ──────────────────────\n-FocusScope mounts (kind, moniker)\n-  spatialKey = useRef(ulid())\n-  parent_zone_key = nearest ancestor FocusScope with kind=\"zone\" (from context)\n-  → ResizeObserver fires\n-  → invoke: spatial_register           → registry.insert(spatial_key,\n-    { key, moniker, rect, kind,           { moniker, rect, kind,\n-      layer_key, parent_zone }              layer_key, parent_zone })\n\n-FocusScope unmounts\n-  → invoke: spatial_unregister         → registry.remove(spatial_key)\n-\n-FocusLayer mounts\n-  layerKey = useRef(ulid())\n-  parentLayerKey = nearest ancestor FocusLayer (via context) OR explicit prop\n-  → invoke: spatial_push_layer         → layers.insert(key,\n-    { key, name, parent_key }             { name, parent, window_label })\n+### Tauri commands\n\n-FocusLayer unmounts\n-  → invoke: spatial_pop_layer          → layers.remove(key)\n+One command per primitive registration op; a single unregister op (since React always knows the key it generated):\n\n-nav.right pressed\n-  → invoke: spatial_navigate           → three-rule beam search scoped to\n-    { key, direction }                    focused entry's layer + zones\n-  ← Ok                                 ← emit focus-changed event\n+```rust\n+#[tauri::command]\n+async fn spatial_register_focusable(window: tauri::Window, ...) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_register_zone(window: tauri::Window, ...) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_update_rect(window: tauri::Window, key: SpatialKey, rect: Rect) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: String, parent: Option<LayerKey>) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n\n-### Rust side\n+All derive `window_label` from the `tauri::Window` parameter. React never passes the window label.\n\n-**`SpatialRegistry`** ops:\n-- `register(key: SpatialKey, moniker, rect, kind, layer_key, parent_zone)`\n-- `unregister(key: SpatialKey)`\n-- `update_rect(key: SpatialKey, rect)`\n-- `push_layer(key: LayerKey, name, parent_key: Option<LayerKey>, window_label: WindowLabel)`\n-- `remove_layer(key: LayerKey)`\n-- `children_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n-- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>`\n-- `ancestors_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n-- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>`\n-- `children_of_zone(zone_key: SpatialKey) -> impl Iterator<Item=&SpatialEntry>` — entries with `parent_zone == Some(zone_key)`\n-- `ancestor_zones(key: SpatialKey) -> Vec<&SpatialEntry>` — walk `parent_zone` up to layer root\n+### Serde wire format\n\n-**Tauri commands**:\n-- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, kind: ScopeKind, layer_key, parent_zone: Option<SpatialKey>)`\n-- `spatial_unregister(window: tauri::Window, key)`\n-- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)`\n-- `spatial_pop_layer(window: tauri::Window, key)`\n-\n-`window_label` is derived from the `tauri::Window` parameter, never passed by React.\n-\n-### React side\n-\n-**`FocusLayer`** component (`kanban-app/ui/src/components/focus-layer.tsx`):\n-- Props: `name: string`, optional `parentLayerKey`, `children`\n-- `const layerKey = useRef(ulid()).current`\n-- Resolves parent: explicit `parentLayerKey` prop wins; else nearest ancestor via `FocusLayerContext`; else `None` (root).\n-- Provides `layerKey` via context\n-- Mount: `invoke(\"spatial_push_layer\", { key, name, parentKey })`\n-- Unmount: `invoke(\"spatial_pop_layer\", { key })`\n+`#[serde(tag = \"kind\", rename_all = \"snake_case\")]` on `FocusScope` produces:\n\n-**`FocusScope`** (`kanban-app/ui/src/components/focus-scope.tsx`):\n-- Props add: `kind?: \"leaf\" | \"zone\"` (default `\"leaf\"`)\n-- `const spatialKey = useRef(ulid()).current`\n-- Reads `layerKey` from `FocusLayerContext` (throws if missing)\n-- Reads `parentZoneKey` from `FocusZoneContext` (the nearest ancestor FocusScope where `kind===\"zone\"` published its own key)\n-- When `kind === \"zone\"`, publishes its own `spatialKey` on `FocusZoneContext` for descendants\n-- `ResizeObserver` on the wrapper div\n-- Mount/resize: `invoke(\"spatial_register\", { key, moniker, ...rect, kind, layerKey, parentZone })`\n-- Unmount: `invoke(\"spatial_unregister\", { key })`\n+```json\n+{ \"kind\": \"focusable\", \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": null, \"overrides\": {} }\n+{ \"kind\": \"zone\",      \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": \"...\", \"last_focused\": null, \"overrides\": {} }\n+```\n\n-**`App.tsx`** (or `app-shell.tsx`): wrap app root in `<FocusLayer name=\"window\">`.\n+Newtypes (`SpatialKey`, `LayerKey`, `WindowLabel`) are `#[serde(transparent)]` → plain strings over the wire.\n\n ### Design decisions\n-- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. `#[serde(transparent)]` keeps the wire protocol simple.\n-- **Layers form a forest**: multi-window is first-class, not a retrofit. Two windows = two trees.\n-- **Zones form a tree within each layer**: gives nav algorithm container-first semantics without needing mode state.\n-- **parent_zone pointer**: simpler than a child list; `children_of_zone` walks the map once, fine for < ~hundreds of entries per layer.\n-- **Layer removal by key**: arbitrary removal order.\n-- **window_label derived, not passed**: Tauri knows it from the invoking webview.\n-- **`kind` on SpatialEntry** (not a separate type): zones and leaves share the same table; nav filters by `kind` where relevant.\n\n+- **Four peer types, not a `kind` enum on one struct**: makes zone-only fields (`last_focused`) type-checked — a `Focusable` literally cannot have a `last_focused` field to forget about.\n+- **`FocusScope` as a Rust enum**: a single stored type per `SpatialKey`, pattern matching gives the nav algorithm a clean API.\n+- **Registry stores the enum, not a trait object**: zero dynamic dispatch; serde works out of the box with `#[serde(tag = \"kind\")]`.\n+- **Overrides on both variants**: overrides apply to anything focusable. Cheap empty HashMap default.\n+- **`last_focused` only on `FocusZone`**: only zones need drill-in / fallback memory.\n+\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n-- [ ] Define `Rect`, `ScopeKind`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry`\n-- [ ] Implement layer forest ops: `children_of_layer`, `root_for_window`, `ancestors_of_layer`\n-- [ ] Implement zone tree ops: `children_of_zone`, `ancestor_zones`\n-- [ ] `entries_in_layer` iterator for nav scoping\n-- [ ] Tauri commands derive `window_label` from `tauri::Window`\n-- [ ] Create `FocusLayer` component with ULID layer key, parent context/prop, Tauri invokes\n-- [ ] Create `FocusZoneContext` and update `FocusScope` with `kind` prop, parent_zone resolution, Tauri invokes\n-- [ ] Wrap app root in `<FocusLayer name=\"window\">`\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes\n+- [ ] Define `Rect`, `Direction`\n+- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum) with serde attrs\n+- [ ] Implement `FocusScope` helper methods (`key`, `moniker`, `rect`, `layer_key`, `parent_zone`, `overrides`, `is_zone`, `as_zone*`)\n+- [ ] Implement `SpatialRegistry` with scope + layer ops above\n+- [ ] Implement forest ops for layers (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n+- [ ] Implement zone tree ops (`children_of_zone`, `ancestor_zones`)\n+- [ ] Add Tauri commands per primitive (register_focusable, register_zone, unregister_scope, update_rect, push_layer, pop_layer); each derives `window_label` from `tauri::Window`\n\n ## Acceptance Criteria\n-- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes throughout Rust; wire format is plain strings\n-- [ ] Each FocusScope mount generates a unique ULID `SpatialKey`; each FocusLayer mount generates a unique ULID `LayerKey`\n-- [ ] Every layer carries its `window_label`, derived from Tauri (not React args)\n-- [ ] Root window layers have `parent = None`; stacked layers have `parent = Some(...)`\n-- [ ] The registry can represent: 2 windows + 2 inspector layers (one per window) + 1 dialog on top of one inspector = 5 layers, 2 roots, correct parent chains\n-- [ ] Every SpatialEntry has `kind` and `parent_zone`; the zone tree is well-formed (no cycles, all parents exist in same layer)\n-- [ ] `children_of_zone` returns direct children only (not transitive descendants)\n-- [ ] `ancestor_zones` walks up to the layer root (entries with `parent_zone = None`)\n-- [ ] Same moniker at two locations → two entries, different SpatialKeys\n-- [ ] Layer removal by key supports arbitrary order\n-- [ ] `cargo test` passes, `pnpm vitest run` passes\n+- [ ] Four peer types exist in Rust: `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum)\n+- [ ] `FocusScope` enum dispatches helper accessors to its variant\n+- [ ] `Focusable` has no `last_focused` field; `FocusZone` does\n+- [ ] All newtypes are `#[serde(transparent)]`; enum is `#[serde(tag = \"kind\")]`\n+- [ ] `SpatialRegistry` can hold any mix of focusables, zones, and layers; well-formed across windows + nested zones\n+- [ ] Tauri commands derive `window_label` from `tauri::Window`\n+- [ ] `cargo test` passes\n\n-## Tests\n-- [ ] Rust: register two entries with same moniker, different keys — both stored\n-- [ ] Rust: push window root A + window root B (parent=None, different window_label) — coexist as siblings\n-- [ ] Rust: push window A, inspector I (parent=A), dialog D (parent=I) — `ancestors_of_layer(D) == [I, A]`\n-- [ ] Rust: `children_of_layer(A)` returns inspectors of A only, not of other windows\n-- [ ] Rust: register a column (Zone, parent_zone=None), a card (Zone, parent_zone=column_key), a title (Leaf, parent_zone=card_key) — `ancestor_zones(title) == [card, column]`\n-- [ ] Rust: `children_of_zone(column_key)` returns cards only, not titles\n-- [ ] Rust: removing a zone leaves its descendants' `parent_zone` pointers intact (dangling is OK; nav will handle); unregistering triggers fallback handled in card `01KNS0B3HY...`\n-- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key`\n-- [ ] `focus-layer.test.tsx` — ULID generated, stable across re-renders, new on remount; `parentLayerKey` prop overrides ancestor context\n-- [ ] `focus-scope.test.tsx` — spatial key generated; zone scope publishes key to `FocusZoneContext`; leaf descendants pick it up as `parent_zone`; throws if outside a FocusLayer\n-- [ ] Run `cargo test` and `cd kanban-app/ui && npx vitest run` — all pass\n+## Tests (pure Rust)\n+- [ ] `Focusable` and `FocusZone` structs have the fields listed above; serde JSON includes `\"kind\"` tag\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde\n+- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [ ] Registry: `children_of_zone` returns direct children only (not transitive descendants)\n+- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [ ] Registry: `children_of_layer` returns layers with `parent == this`, not layers in other windows\n+- [ ] Registry: `ancestors_of_layer` walks `layer.parent` up to window root\n+- [ ] Registry: `scopes_in_layer` filters by `layer_key`, returns both Focusables and Zones in that layer\n+- [ ] Registry: unregister removes; subsequent `scope(key)` returns None\n+- [ ] Registry: two windows + 2 inspector layers + 1 dialog layer = 5 layers, 2 roots, correct parent chains\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -6,28 +6,29 @@\n position_column: todo\n position_ordinal: a180\n project: spatial-nav\n-title: 'Rust kernel: Focusable, FocusZone, FocusLayer, FocusScope types + SpatialRegistry'\n+title: 'Spatial registry: layer forest + zones, typed keys, React measures rects'\n ---\n ## What\n\n-Define the Rust kernel types for spatial focus. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n+Add spatial awareness to the focus system with a clear frontend/backend split:\n\n-### The four peer types\n+- **React** measures DOM rects and reports them to Rust. React owns `FocusScope` (measures its element) and `FocusLayer` (declares a layer boundary).\n+- **Rust** owns the spatial registry, the **layer forest**, the **zone tree**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n\n-| Role                | React component     | Rust type             |\n-|---------------------|---------------------|-----------------------|\n-| Leaf focusable point| `Focusable`         | `struct Focusable`    |\n-| Navigable container | `FocusZone`         | `struct FocusZone`    |\n-| Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n-| Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n+### Two structural concepts\n\n-On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum delegate to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`, etc.).\n+1. **Layers** are **hard** boundaries. Nav never crosses them. Window roots, inspector overlay, dialog overlays are layers. Layers form a **forest** (one root per Tauri window, plus stacked children).\n+2. **Zones** are **soft** containers within a layer. They give structure to beam search — container-first, with leaf-level cross-zone fallback. Perspective, nav bar, toolbar, column, card, inspector panel, field row — all zones.\n\n-On React: `FocusScope` is the high-level convenience wrapper that internally uses a `Focusable` or `FocusZone` primitive and adds entity plumbing (CommandScopeProvider, click-to-focus, context menu, focus bar, data-moniker attrs).\n+### Keying model\n\n-### Newtypes\n+- **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch.\n+- **SpatialKey** (ULID) — unique per FocusScope *instance*. Generated once per mount via `useRef(ulid())`. Primary key in the registry.\n+- Same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey.\n+\n+### Newtypes (Rust)\n\n-Shared with card `01KNM3YHHFJ3...`:\n+Shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`:\n\n ```rust\n #[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n@@ -36,166 +37,181 @@\n #[serde(transparent)] pub struct LayerKey(pub String);\n ```\n\n-### Rust types\n+### Layer model: forest, not stack\n+\n+```rust\n+pub struct LayerEntry {\n+    pub key: LayerKey,\n+    pub name: String,                       // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n+    pub parent: Option<LayerKey>,           // None for window roots; Some(...) for stacked layers\n+    pub window_label: WindowLabel,          // which window this layer lives in\n+    pub last_focused: Option<SpatialKey>,   // focus memory for pop-restore\n+}\n+```\n+\n+Two windows each with their own inspector → 4 layers total, 2 roots. Layers removed by key, not popped — supports arbitrary unmount order.\n+\n+### Zone model within a layer\n\n ```rust\n-/// Leaf — a single focusable point (task title, pill, button, menu item).\n-#[derive(Debug, Clone, Serialize, Deserialize)]\n-pub struct Focusable {\n-    pub key: SpatialKey,\n-    pub moniker: String,\n-    pub rect: Rect,\n-    pub layer_key: LayerKey,\n-    pub parent_zone: Option<SpatialKey>,\n-    pub overrides: HashMap<Direction, Option<String>>,\n+pub enum ScopeKind {\n+    Leaf,   // focusable element (task title, status pill, field pill, button)\n+    Zone,   // container (column, card, inspector panel, field row, toolbar group)\n }\n\n-/// Zone — a navigable container (card, column, inspector panel, field row,\n-/// toolbar group, nav bar). Has its own focus memory for drill-out / fallback.\n-#[derive(Debug, Clone, Serialize, Deserialize)]\n-pub struct FocusZone {\n-    pub key: SpatialKey,\n+pub struct SpatialEntry {\n     pub moniker: String,\n     pub rect: Rect,\n     pub layer_key: LayerKey,\n-    pub parent_zone: Option<SpatialKey>,\n-    pub last_focused: Option<SpatialKey>,\n+    pub kind: ScopeKind,\n+    pub parent_zone: Option<SpatialKey>,    // nearest ancestor Zone in the same layer; None = layer root\n+    pub last_focused: Option<SpatialKey>,   // focus memory for drill-out / fallback when kind == Zone\n     pub overrides: HashMap<Direction, Option<String>>,\n }\n\n-/// Layer — a hard modal boundary. Window roots, inspector overlay, dialogs.\n-#[derive(Debug, Clone, Serialize, Deserialize)]\n-pub struct FocusLayer {\n-    pub key: LayerKey,\n-    pub name: String,\n-    pub parent: Option<LayerKey>,\n-    pub window_label: WindowLabel,\n-    pub last_focused: Option<SpatialKey>,\n+pub struct SpatialRegistry {\n+    entries: HashMap<SpatialKey, SpatialEntry>,\n+    layers:  HashMap<LayerKey, LayerEntry>,\n }\n+```\n\n-/// Sum type stored in the registry per SpatialKey.\n-#[derive(Debug, Clone, Serialize, Deserialize)]\n-#[serde(tag = \"kind\", rename_all = \"snake_case\")]\n-pub enum FocusScope {\n-    Focusable(Focusable),\n-    Zone(FocusZone),\n-}\n+Zones form a **tree within each layer**, rooted at the layer root (`parent_zone = None`). Nav algorithm uses zones for container-first beam search and for drill-in/drill-out commands (see card `01KNQXXF5W7G4JP73C6ZCMKYKX` and the new drill-in/drill-out card).\n\n-impl FocusScope {\n-    pub fn key(&self) -> &SpatialKey { /* dispatch */ }\n-    pub fn moniker(&self) -> &str { /* dispatch */ }\n-    pub fn rect(&self) -> &Rect { /* dispatch */ }\n-    pub fn layer_key(&self) -> &LayerKey { /* dispatch */ }\n-    pub fn parent_zone(&self) -> Option<&SpatialKey> { /* dispatch */ }\n-    pub fn overrides(&self) -> &HashMap<Direction, Option<String>> { /* dispatch */ }\n-    pub fn is_zone(&self) -> bool { matches!(self, Self::Zone(_)) }\n-    pub fn is_focusable(&self) -> bool { matches!(self, Self::Focusable(_)) }\n-    pub fn as_zone(&self) -> Option<&FocusZone> { /* ... */ }\n-    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone> { /* ... */ }\n-}\n+### Which React regions become zones\n+\n+- `NavBar` → `kind=\"zone\"` (contains logo, menus, mode indicator as leaves)\n+- Toolbar / action groups → `kind=\"zone\"`\n+- Tab bar / perspective bar → `kind=\"zone\"`, each tab a leaf\n+- Perspective container → `kind=\"zone\"` (wraps the active view)\n+- Column (`column-view`) → `kind=\"zone\"` (contains header leaf + cards)\n+- Task card (`entity-card`) → `kind=\"zone\"` (contains title leaf + status leaf + pills)\n+- Inspector panel → `kind=\"zone\"` (contains field rows)\n+- Field row (inspector) → `kind=\"zone\"` (contains label + pills)\n+- Field pills, task titles, status badges, tab buttons → `kind=\"leaf\"` (default, no change needed)\n+\n+Detailed adoption happens in the migration cards (`01KNQXZ81Q...`, `01KNQY0P9J...`, and a new shell-wrapping card).\n+\n+### Architecture\n+\n ```\n+React (measure + report)              Rust (track + navigate)\n+─────────────────────────              ──────────────────────\n+FocusScope mounts (kind, moniker)\n+  spatialKey = useRef(ulid())\n+  parent_zone_key = nearest ancestor FocusScope with kind=\"zone\" (from context)\n+  → ResizeObserver fires\n+  → invoke: spatial_register           → registry.insert(spatial_key,\n+    { key, moniker, rect, kind,           { moniker, rect, kind,\n+      layer_key, parent_zone }              layer_key, parent_zone })\n\n-### Registry\n+FocusScope unmounts\n+  → invoke: spatial_unregister         → registry.remove(spatial_key)\n\n-```rust\n-pub struct SpatialRegistry {\n-    scopes: HashMap<SpatialKey, FocusScope>,\n-    layers: HashMap<LayerKey, FocusLayer>,\n-}\n+FocusLayer mounts\n+  layerKey = useRef(ulid())\n+  parentLayerKey = nearest ancestor FocusLayer (via context) OR explicit prop\n+  → invoke: spatial_push_layer         → layers.insert(key,\n+    { key, name, parent_key }             { name, parent, window_label })\n\n-impl SpatialRegistry {\n-    // Scope ops\n-    pub fn register_focusable(&mut self, f: Focusable);\n-    pub fn register_zone(&mut self, z: FocusZone);\n-    pub fn unregister_scope(&mut self, key: &SpatialKey);\n-    pub fn update_rect(&mut self, key: &SpatialKey, rect: Rect);\n-    pub fn scope(&self, key: &SpatialKey) -> Option<&FocusScope>;\n-    pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = &FocusScope>;\n-    pub fn ancestor_zones(&self, key: &SpatialKey) -> Vec<&FocusZone>;\n+FocusLayer unmounts\n+  → invoke: spatial_pop_layer          → layers.remove(key)\n\n-    // Layer ops\n-    pub fn push_layer(&mut self, l: FocusLayer);\n-    pub fn remove_layer(&mut self, key: &LayerKey);\n-    pub fn layer(&self, key: &LayerKey) -> Option<&FocusLayer>;\n-    pub fn children_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n-    pub fn root_for_window(&self, label: &WindowLabel) -> Option<&FocusLayer>;\n-    pub fn ancestors_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n-\n-    // Nav scoping\n-    pub fn scopes_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n-}\n+nav.right pressed\n+  → invoke: spatial_navigate           → three-rule beam search scoped to\n+    { key, direction }                    focused entry's layer + zones\n+  ← Ok                                 ← emit focus-changed event\n ```\n\n-### Tauri commands\n+### Rust side\n\n-One command per primitive registration op; a single unregister op (since React always knows the key it generated):\n+**`SpatialRegistry`** ops:\n+- `register(key: SpatialKey, moniker, rect, kind, layer_key, parent_zone)`\n+- `unregister(key: SpatialKey)`\n+- `update_rect(key: SpatialKey, rect)`\n+- `push_layer(key: LayerKey, name, parent_key: Option<LayerKey>, window_label: WindowLabel)`\n+- `remove_layer(key: LayerKey)`\n+- `children_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n+- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>`\n+- `ancestors_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n+- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>`\n+- `children_of_zone(zone_key: SpatialKey) -> impl Iterator<Item=&SpatialEntry>` — entries with `parent_zone == Some(zone_key)`\n+- `ancestor_zones(key: SpatialKey) -> Vec<&SpatialEntry>` — walk `parent_zone` up to layer root\n\n-```rust\n-#[tauri::command]\n-async fn spatial_register_focusable(window: tauri::Window, ...) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_register_zone(window: tauri::Window, ...) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_update_rect(window: tauri::Window, key: SpatialKey, rect: Rect) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: String, parent: Option<LayerKey>) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n-```\n+**Tauri commands**:\n+- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, kind: ScopeKind, layer_key, parent_zone: Option<SpatialKey>)`\n+- `spatial_unregister(window: tauri::Window, key)`\n+- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)`\n+- `spatial_pop_layer(window: tauri::Window, key)`\n\n-All derive `window_label` from the `tauri::Window` parameter. React never passes the window label.\n+`window_label` is derived from the `tauri::Window` parameter, never passed by React.\n\n-### Serde wire format\n+### React side\n\n-`#[serde(tag = \"kind\", rename_all = \"snake_case\")]` on `FocusScope` produces:\n+**`FocusLayer`** component (`kanban-app/ui/src/components/focus-layer.tsx`):\n+- Props: `name: string`, optional `parentLayerKey`, `children`\n+- `const layerKey = useRef(ulid()).current`\n+- Resolves parent: explicit `parentLayerKey` prop wins; else nearest ancestor via `FocusLayerContext`; else `None` (root).\n+- Provides `layerKey` via context\n+- Mount: `invoke(\"spatial_push_layer\", { key, name, parentKey })`\n+- Unmount: `invoke(\"spatial_pop_layer\", { key })`\n\n-```json\n-{ \"kind\": \"focusable\", \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": null, \"overrides\": {} }\n-{ \"kind\": \"zone\",      \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": \"...\", \"last_focused\": null, \"overrides\": {} }\n-```\n+**`FocusScope`** (`kanban-app/ui/src/components/focus-scope.tsx`):\n+- Props add: `kind?: \"leaf\" | \"zone\"` (default `\"leaf\"`)\n+- `const spatialKey = useRef(ulid()).current`\n+- Reads `layerKey` from `FocusLayerContext` (throws if missing)\n+- Reads `parentZoneKey` from `FocusZoneContext` (the nearest ancestor FocusScope where `kind===\"zone\"` published its own key)\n+- When `kind === \"zone\"`, publishes its own `spatialKey` on `FocusZoneContext` for descendants\n+- `ResizeObserver` on the wrapper div\n+- Mount/resize: `invoke(\"spatial_register\", { key, moniker, ...rect, kind, layerKey, parentZone })`\n+- Unmount: `invoke(\"spatial_unregister\", { key })`\n\n-Newtypes (`SpatialKey`, `LayerKey`, `WindowLabel`) are `#[serde(transparent)]` → plain strings over the wire.\n+**`App.tsx`** (or `app-shell.tsx`): wrap app root in `<FocusLayer name=\"window\">`.\n\n ### Design decisions\n+- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. `#[serde(transparent)]` keeps the wire protocol simple.\n+- **Layers form a forest**: multi-window is first-class, not a retrofit. Two windows = two trees.\n+- **Zones form a tree within each layer**: gives nav algorithm container-first semantics without needing mode state.\n+- **parent_zone pointer**: simpler than a child list; `children_of_zone` walks the map once, fine for < ~hundreds of entries per layer.\n+- **Layer removal by key**: arbitrary removal order.\n+- **window_label derived, not passed**: Tauri knows it from the invoking webview.\n+- **`kind` on SpatialEntry** (not a separate type): zones and leaves share the same table; nav filters by `kind` where relevant.\n\n-- **Four peer types, not a `kind` enum on one struct**: makes zone-only fields (`last_focused`) type-checked — a `Focusable` literally cannot have a `last_focused` field to forget about.\n-- **`FocusScope` as a Rust enum**: a single stored type per `SpatialKey`, pattern matching gives the nav algorithm a clean API.\n-- **Registry stores the enum, not a trait object**: zero dynamic dispatch; serde works out of the box with `#[serde(tag = \"kind\")]`.\n-- **Overrides on both variants**: overrides apply to anything focusable. Cheap empty HashMap default.\n-- **`last_focused` only on `FocusZone`**: only zones need drill-in / fallback memory.\n-\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes\n-- [ ] Define `Rect`, `Direction`\n-- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum) with serde attrs\n-- [ ] Implement `FocusScope` helper methods (`key`, `moniker`, `rect`, `layer_key`, `parent_zone`, `overrides`, `is_zone`, `as_zone*`)\n-- [ ] Implement `SpatialRegistry` with scope + layer ops above\n-- [ ] Implement forest ops for layers (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n-- [ ] Implement zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [ ] Add Tauri commands per primitive (register_focusable, register_zone, unregister_scope, update_rect, push_layer, pop_layer); each derives `window_label` from `tauri::Window`\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n+- [ ] Define `Rect`, `ScopeKind`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry`\n+- [ ] Implement layer forest ops: `children_of_layer`, `root_for_window`, `ancestors_of_layer`\n+- [ ] Implement zone tree ops: `children_of_zone`, `ancestor_zones`\n+- [ ] `entries_in_layer` iterator for nav scoping\n+- [ ] Tauri commands derive `window_label` from `tauri::Window`\n+- [ ] Create `FocusLayer` component with ULID layer key, parent context/prop, Tauri invokes\n+- [ ] Create `FocusZoneContext` and update `FocusScope` with `kind` prop, parent_zone resolution, Tauri invokes\n+- [ ] Wrap app root in `<FocusLayer name=\"window\">`\n\n ## Acceptance Criteria\n-- [ ] Four peer types exist in Rust: `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum)\n-- [ ] `FocusScope` enum dispatches helper accessors to its variant\n-- [ ] `Focusable` has no `last_focused` field; `FocusZone` does\n-- [ ] All newtypes are `#[serde(transparent)]`; enum is `#[serde(tag = \"kind\")]`\n-- [ ] `SpatialRegistry` can hold any mix of focusables, zones, and layers; well-formed across windows + nested zones\n-- [ ] Tauri commands derive `window_label` from `tauri::Window`\n-- [ ] `cargo test` passes\n+- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes throughout Rust; wire format is plain strings\n+- [ ] Each FocusScope mount generates a unique ULID `SpatialKey`; each FocusLayer mount generates a unique ULID `LayerKey`\n+- [ ] Every layer carries its `window_label`, derived from Tauri (not React args)\n+- [ ] Root window layers have `parent = None`; stacked layers have `parent = Some(...)`\n+- [ ] The registry can represent: 2 windows + 2 inspector layers (one per window) + 1 dialog on top of one inspector = 5 layers, 2 roots, correct parent chains\n+- [ ] Every SpatialEntry has `kind` and `parent_zone`; the zone tree is well-formed (no cycles, all parents exist in same layer)\n+- [ ] `children_of_zone` returns direct children only (not transitive descendants)\n+- [ ] `ancestor_zones` walks up to the layer root (entries with `parent_zone = None`)\n+- [ ] Same moniker at two locations → two entries, different SpatialKeys\n+- [ ] Layer removal by key supports arbitrary order\n+- [ ] `cargo test` passes, `pnpm vitest run` passes\n\n-## Tests (pure Rust)\n-- [ ] `Focusable` and `FocusZone` structs have the fields listed above; serde JSON includes `\"kind\"` tag\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde\n-- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` returns direct children only (not transitive descendants)\n-- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` returns layers with `parent == this`, not layers in other windows\n-- [ ] Registry: `ancestors_of_layer` walks `layer.parent` up to window root\n-- [ ] Registry: `scopes_in_layer` filters by `layer_key`, returns both Focusables and Zones in that layer\n-- [ ] Registry: unregister removes; subsequent `scope(key)` returns None\n-- [ ] Registry: two windows + 2 inspector layers + 1 dialog layer = 5 layers, 2 roots, correct parent chains\n+## Tests\n+- [ ] Rust: register two entries with same moniker, different keys — both stored\n+- [ ] Rust: push window root A + window root B (parent=None, different window_label) — coexist as siblings\n+- [ ] Rust: push window A, inspector I (parent=A), dialog D (parent=I) — `ancestors_of_layer(D) == [I, A]`\n+- [ ] Rust: `children_of_layer(A)` returns inspectors of A only, not of other windows\n+- [ ] Rust: register a column (Zone, parent_zone=None), a card (Zone, parent_zone=column_key), a title (Leaf, parent_zone=card_key) — `ancestor_zones(title) == [card, column]`\n+- [ ] Rust: `children_of_zone(column_key)` returns cards only, not titles\n+- [ ] Rust: removing a zone leaves its descendants' `parent_zone` pointers intact (dangling is OK; nav will handle); unregistering triggers fallback handled in card `01KNS0B3HY...`\n+- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key`\n+- [ ] `focus-layer.test.tsx` — ULID generated, stable across re-renders, new on remount; `parentLayerKey` prop overrides ancestor context\n+- [ ] `focus-scope.test.tsx` — spatial key generated; zone scope publishes key to `FocusZoneContext`; leaf descendants pick it up as `parent_zone`; throws if outside a FocusLayer\n+- [ ] Run `cargo test` and `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPZWJWT10PY2QQ8Z3AVQJXJX","timestamp":"2026-04-24T13:59:03.745483Z","entity_type":"task","entity_id":"01KNQXW7HHHB8HW76K3PXH3G34","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,24 +1,23 @@\n ## What\n\n-Add spatial awareness to the focus system with a clear frontend/backend split:\n+Define the Rust kernel types for spatial focus. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n-- **React** measures DOM rects and reports them to Rust. React owns `FocusScope` (measures its element) and `FocusLayer` (declares a layer boundary).\n-- **Rust** owns the spatial registry, the **layer forest**, the **zone tree**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n+### The four peer types\n\n-### Two structural concepts\n-\n-1. **Layers** are **hard** boundaries. Nav never crosses them. Window roots, inspector overlay, dialog overlays are layers. Layers form a **forest** (one root per Tauri window, plus stacked children).\n-2. **Zones** are **soft** containers within a layer. They give structure to beam search — container-first, with leaf-level cross-zone fallback. Perspective, nav bar, toolbar, column, card, inspector panel, field row — all zones.\n+| Role                | React component     | Rust type             |\n+|---------------------|---------------------|-----------------------|\n+| Leaf focusable point| `Focusable`         | `struct Focusable`    |\n+| Navigable container | `FocusZone`         | `struct FocusZone`    |\n+| Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n+| Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n\n-### Keying model\n+On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum delegate to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`, etc.).\n\n-- **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch.\n-- **SpatialKey** (ULID) — unique per FocusScope *instance*. Generated once per mount via `useRef(ulid())`. Primary key in the registry.\n-- Same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey.\n+On React: `FocusScope` is the high-level convenience wrapper that internally uses a `Focusable` or `FocusZone` primitive and adds entity plumbing (CommandScopeProvider, click-to-focus, context menu, focus bar, data-moniker attrs).\n\n-### Newtypes (Rust)\n+### Newtypes\n\n-Shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`:\n+Shared with card `01KNM3YHHFJ3...`:\n\n ```rust\n #[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n@@ -27,181 +26,166 @@\n #[serde(transparent)] pub struct LayerKey(pub String);\n ```\n\n-### Layer model: forest, not stack\n-\n-```rust\n-pub struct LayerEntry {\n-    pub key: LayerKey,\n-    pub name: String,                       // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n-    pub parent: Option<LayerKey>,           // None for window roots; Some(...) for stacked layers\n-    pub window_label: WindowLabel,          // which window this layer lives in\n-    pub last_focused: Option<SpatialKey>,   // focus memory for pop-restore\n-}\n-```\n-\n-Two windows each with their own inspector → 4 layers total, 2 roots. Layers removed by key, not popped — supports arbitrary unmount order.\n-\n-### Zone model within a layer\n+### Rust types\n\n ```rust\n-pub enum ScopeKind {\n-    Leaf,   // focusable element (task title, status pill, field pill, button)\n-    Zone,   // container (column, card, inspector panel, field row, toolbar group)\n+/// Leaf — a single focusable point (task title, pill, button, menu item).\n+#[derive(Debug, Clone, Serialize, Deserialize)]\n+pub struct Focusable {\n+    pub key: SpatialKey,\n+    pub moniker: String,\n+    pub rect: Rect,\n+    pub layer_key: LayerKey,\n+    pub parent_zone: Option<SpatialKey>,\n+    pub overrides: HashMap<Direction, Option<String>>,\n }\n\n-pub struct SpatialEntry {\n+/// Zone — a navigable container (card, column, inspector panel, field row,\n+/// toolbar group, nav bar). Has its own focus memory for drill-out / fallback.\n+#[derive(Debug, Clone, Serialize, Deserialize)]\n+pub struct FocusZone {\n+    pub key: SpatialKey,\n     pub moniker: String,\n     pub rect: Rect,\n     pub layer_key: LayerKey,\n-    pub kind: ScopeKind,\n-    pub parent_zone: Option<SpatialKey>,    // nearest ancestor Zone in the same layer; None = layer root\n-    pub last_focused: Option<SpatialKey>,   // focus memory for drill-out / fallback when kind == Zone\n+    pub parent_zone: Option<SpatialKey>,\n+    pub last_focused: Option<SpatialKey>,\n     pub overrides: HashMap<Direction, Option<String>>,\n }\n\n-pub struct SpatialRegistry {\n-    entries: HashMap<SpatialKey, SpatialEntry>,\n-    layers:  HashMap<LayerKey, LayerEntry>,\n+/// Layer — a hard modal boundary. Window roots, inspector overlay, dialogs.\n+#[derive(Debug, Clone, Serialize, Deserialize)]\n+pub struct FocusLayer {\n+    pub key: LayerKey,\n+    pub name: String,\n+    pub parent: Option<LayerKey>,\n+    pub window_label: WindowLabel,\n+    pub last_focused: Option<SpatialKey>,\n+}\n+\n+/// Sum type stored in the registry per SpatialKey.\n+#[derive(Debug, Clone, Serialize, Deserialize)]\n+#[serde(tag = \"kind\", rename_all = \"snake_case\")]\n+pub enum FocusScope {\n+    Focusable(Focusable),\n+    Zone(FocusZone),\n }\n-```\n\n-Zones form a **tree within each layer**, rooted at the layer root (`parent_zone = None`). Nav algorithm uses zones for container-first beam search and for drill-in/drill-out commands (see card `01KNQXXF5W7G4JP73C6ZCMKYKX` and the new drill-in/drill-out card).\n+impl FocusScope {\n+    pub fn key(&self) -> &SpatialKey { /* dispatch */ }\n+    pub fn moniker(&self) -> &str { /* dispatch */ }\n+    pub fn rect(&self) -> &Rect { /* dispatch */ }\n+    pub fn layer_key(&self) -> &LayerKey { /* dispatch */ }\n+    pub fn parent_zone(&self) -> Option<&SpatialKey> { /* dispatch */ }\n+    pub fn overrides(&self) -> &HashMap<Direction, Option<String>> { /* dispatch */ }\n+    pub fn is_zone(&self) -> bool { matches!(self, Self::Zone(_)) }\n+    pub fn is_focusable(&self) -> bool { matches!(self, Self::Focusable(_)) }\n+    pub fn as_zone(&self) -> Option<&FocusZone> { /* ... */ }\n+    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone> { /* ... */ }\n+}\n+```\n\n-### Which React regions become zones\n+### Registry\n\n-- `NavBar` → `kind=\"zone\"` (contains logo, menus, mode indicator as leaves)\n-- Toolbar / action groups → `kind=\"zone\"`\n-- Tab bar / perspective bar → `kind=\"zone\"`, each tab a leaf\n-- Perspective container → `kind=\"zone\"` (wraps the active view)\n-- Column (`column-view`) → `kind=\"zone\"` (contains header leaf + cards)\n-- Task card (`entity-card`) → `kind=\"zone\"` (contains title leaf + status leaf + pills)\n-- Inspector panel → `kind=\"zone\"` (contains field rows)\n-- Field row (inspector) → `kind=\"zone\"` (contains label + pills)\n-- Field pills, task titles, status badges, tab buttons → `kind=\"leaf\"` (default, no change needed)\n+```rust\n+pub struct SpatialRegistry {\n+    scopes: HashMap<SpatialKey, FocusScope>,\n+    layers: HashMap<LayerKey, FocusLayer>,\n+}\n\n-Detailed adoption happens in the migration cards (`01KNQXZ81Q...`, `01KNQY0P9J...`, and a new shell-wrapping card).\n+impl SpatialRegistry {\n+    // Scope ops\n+    pub fn register_focusable(&mut self, f: Focusable);\n+    pub fn register_zone(&mut self, z: FocusZone);\n+    pub fn unregister_scope(&mut self, key: &SpatialKey);\n+    pub fn update_rect(&mut self, key: &SpatialKey, rect: Rect);\n+    pub fn scope(&self, key: &SpatialKey) -> Option<&FocusScope>;\n+    pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = &FocusScope>;\n+    pub fn ancestor_zones(&self, key: &SpatialKey) -> Vec<&FocusZone>;\n\n-### Architecture\n+    // Layer ops\n+    pub fn push_layer(&mut self, l: FocusLayer);\n+    pub fn remove_layer(&mut self, key: &LayerKey);\n+    pub fn layer(&self, key: &LayerKey) -> Option<&FocusLayer>;\n+    pub fn children_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n+    pub fn root_for_window(&self, label: &WindowLabel) -> Option<&FocusLayer>;\n+    pub fn ancestors_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n\n+    // Nav scoping\n+    pub fn scopes_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n+}\n ```\n-React (measure + report)              Rust (track + navigate)\n-─────────────────────────              ──────────────────────\n-FocusScope mounts (kind, moniker)\n-  spatialKey = useRef(ulid())\n-  parent_zone_key = nearest ancestor FocusScope with kind=\"zone\" (from context)\n-  → ResizeObserver fires\n-  → invoke: spatial_register           → registry.insert(spatial_key,\n-    { key, moniker, rect, kind,           { moniker, rect, kind,\n-      layer_key, parent_zone }              layer_key, parent_zone })\n\n-FocusScope unmounts\n-  → invoke: spatial_unregister         → registry.remove(spatial_key)\n+### Tauri commands\n\n-FocusLayer mounts\n-  layerKey = useRef(ulid())\n-  parentLayerKey = nearest ancestor FocusLayer (via context) OR explicit prop\n-  → invoke: spatial_push_layer         → layers.insert(key,\n-    { key, name, parent_key }             { name, parent, window_label })\n-\n-FocusLayer unmounts\n-  → invoke: spatial_pop_layer          → layers.remove(key)\n+One command per primitive registration op; a single unregister op (since React always knows the key it generated):\n\n-nav.right pressed\n-  → invoke: spatial_navigate           → three-rule beam search scoped to\n-    { key, direction }                    focused entry's layer + zones\n-  ← Ok                                 ← emit focus-changed event\n+```rust\n+#[tauri::command]\n+async fn spatial_register_focusable(window: tauri::Window, ...) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_register_zone(window: tauri::Window, ...) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_update_rect(window: tauri::Window, key: SpatialKey, rect: Rect) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: String, parent: Option<LayerKey>) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n\n-### Rust side\n+All derive `window_label` from the `tauri::Window` parameter. React never passes the window label.\n\n-**`SpatialRegistry`** ops:\n-- `register(key: SpatialKey, moniker, rect, kind, layer_key, parent_zone)`\n-- `unregister(key: SpatialKey)`\n-- `update_rect(key: SpatialKey, rect)`\n-- `push_layer(key: LayerKey, name, parent_key: Option<LayerKey>, window_label: WindowLabel)`\n-- `remove_layer(key: LayerKey)`\n-- `children_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n-- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>`\n-- `ancestors_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n-- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>`\n-- `children_of_zone(zone_key: SpatialKey) -> impl Iterator<Item=&SpatialEntry>` — entries with `parent_zone == Some(zone_key)`\n-- `ancestor_zones(key: SpatialKey) -> Vec<&SpatialEntry>` — walk `parent_zone` up to layer root\n+### Serde wire format\n\n-**Tauri commands**:\n-- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, kind: ScopeKind, layer_key, parent_zone: Option<SpatialKey>)`\n-- `spatial_unregister(window: tauri::Window, key)`\n-- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)`\n-- `spatial_pop_layer(window: tauri::Window, key)`\n+`#[serde(tag = \"kind\", rename_all = \"snake_case\")]` on `FocusScope` produces:\n\n-`window_label` is derived from the `tauri::Window` parameter, never passed by React.\n+```json\n+{ \"kind\": \"focusable\", \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": null, \"overrides\": {} }\n+{ \"kind\": \"zone\",      \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": \"...\", \"last_focused\": null, \"overrides\": {} }\n+```\n\n-### React side\n+Newtypes (`SpatialKey`, `LayerKey`, `WindowLabel`) are `#[serde(transparent)]` → plain strings over the wire.\n\n-**`FocusLayer`** component (`kanban-app/ui/src/components/focus-layer.tsx`):\n-- Props: `name: string`, optional `parentLayerKey`, `children`\n-- `const layerKey = useRef(ulid()).current`\n-- Resolves parent: explicit `parentLayerKey` prop wins; else nearest ancestor via `FocusLayerContext`; else `None` (root).\n-- Provides `layerKey` via context\n-- Mount: `invoke(\"spatial_push_layer\", { key, name, parentKey })`\n-- Unmount: `invoke(\"spatial_pop_layer\", { key })`\n+### Design decisions\n\n-**`FocusScope`** (`kanban-app/ui/src/components/focus-scope.tsx`):\n-- Props add: `kind?: \"leaf\" | \"zone\"` (default `\"leaf\"`)\n-- `const spatialKey = useRef(ulid()).current`\n-- Reads `layerKey` from `FocusLayerContext` (throws if missing)\n-- Reads `parentZoneKey` from `FocusZoneContext` (the nearest ancestor FocusScope where `kind===\"zone\"` published its own key)\n-- When `kind === \"zone\"`, publishes its own `spatialKey` on `FocusZoneContext` for descendants\n-- `ResizeObserver` on the wrapper div\n-- Mount/resize: `invoke(\"spatial_register\", { key, moniker, ...rect, kind, layerKey, parentZone })`\n-- Unmount: `invoke(\"spatial_unregister\", { key })`\n-\n-**`App.tsx`** (or `app-shell.tsx`): wrap app root in `<FocusLayer name=\"window\">`.\n+- **Four peer types, not a `kind` enum on one struct**: makes zone-only fields (`last_focused`) type-checked — a `Focusable` literally cannot have a `last_focused` field to forget about.\n+- **`FocusScope` as a Rust enum**: a single stored type per `SpatialKey`, pattern matching gives the nav algorithm a clean API.\n+- **Registry stores the enum, not a trait object**: zero dynamic dispatch; serde works out of the box with `#[serde(tag = \"kind\")]`.\n+- **Overrides on both variants**: overrides apply to anything focusable. Cheap empty HashMap default.\n+- **`last_focused` only on `FocusZone`**: only zones need drill-in / fallback memory.\n\n-### Design decisions\n-- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. `#[serde(transparent)]` keeps the wire protocol simple.\n-- **Layers form a forest**: multi-window is first-class, not a retrofit. Two windows = two trees.\n-- **Zones form a tree within each layer**: gives nav algorithm container-first semantics without needing mode state.\n-- **parent_zone pointer**: simpler than a child list; `children_of_zone` walks the map once, fine for < ~hundreds of entries per layer.\n-- **Layer removal by key**: arbitrary removal order.\n-- **window_label derived, not passed**: Tauri knows it from the invoking webview.\n-- **`kind` on SpatialEntry** (not a separate type): zones and leaves share the same table; nav filters by `kind` where relevant.\n-\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n-- [ ] Define `Rect`, `ScopeKind`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry`\n-- [ ] Implement layer forest ops: `children_of_layer`, `root_for_window`, `ancestors_of_layer`\n-- [ ] Implement zone tree ops: `children_of_zone`, `ancestor_zones`\n-- [ ] `entries_in_layer` iterator for nav scoping\n-- [ ] Tauri commands derive `window_label` from `tauri::Window`\n-- [ ] Create `FocusLayer` component with ULID layer key, parent context/prop, Tauri invokes\n-- [ ] Create `FocusZoneContext` and update `FocusScope` with `kind` prop, parent_zone resolution, Tauri invokes\n-- [ ] Wrap app root in `<FocusLayer name=\"window\">`\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes\n+- [ ] Define `Rect`, `Direction`\n+- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum) with serde attrs\n+- [ ] Implement `FocusScope` helper methods (`key`, `moniker`, `rect`, `layer_key`, `parent_zone`, `overrides`, `is_zone`, `as_zone*`)\n+- [ ] Implement `SpatialRegistry` with scope + layer ops above\n+- [ ] Implement forest ops for layers (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n+- [ ] Implement zone tree ops (`children_of_zone`, `ancestor_zones`)\n+- [ ] Add Tauri commands per primitive (register_focusable, register_zone, unregister_scope, update_rect, push_layer, pop_layer); each derives `window_label` from `tauri::Window`\n\n ## Acceptance Criteria\n-- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes throughout Rust; wire format is plain strings\n-- [ ] Each FocusScope mount generates a unique ULID `SpatialKey`; each FocusLayer mount generates a unique ULID `LayerKey`\n-- [ ] Every layer carries its `window_label`, derived from Tauri (not React args)\n-- [ ] Root window layers have `parent = None`; stacked layers have `parent = Some(...)`\n-- [ ] The registry can represent: 2 windows + 2 inspector layers (one per window) + 1 dialog on top of one inspector = 5 layers, 2 roots, correct parent chains\n-- [ ] Every SpatialEntry has `kind` and `parent_zone`; the zone tree is well-formed (no cycles, all parents exist in same layer)\n-- [ ] `children_of_zone` returns direct children only (not transitive descendants)\n-- [ ] `ancestor_zones` walks up to the layer root (entries with `parent_zone = None`)\n-- [ ] Same moniker at two locations → two entries, different SpatialKeys\n-- [ ] Layer removal by key supports arbitrary order\n-- [ ] `cargo test` passes, `pnpm vitest run` passes\n+- [ ] Four peer types exist in Rust: `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum)\n+- [ ] `FocusScope` enum dispatches helper accessors to its variant\n+- [ ] `Focusable` has no `last_focused` field; `FocusZone` does\n+- [ ] All newtypes are `#[serde(transparent)]`; enum is `#[serde(tag = \"kind\")]`\n+- [ ] `SpatialRegistry` can hold any mix of focusables, zones, and layers; well-formed across windows + nested zones\n+- [ ] Tauri commands derive `window_label` from `tauri::Window`\n+- [ ] `cargo test` passes\n\n-## Tests\n-- [ ] Rust: register two entries with same moniker, different keys — both stored\n-- [ ] Rust: push window root A + window root B (parent=None, different window_label) — coexist as siblings\n-- [ ] Rust: push window A, inspector I (parent=A), dialog D (parent=I) — `ancestors_of_layer(D) == [I, A]`\n-- [ ] Rust: `children_of_layer(A)` returns inspectors of A only, not of other windows\n-- [ ] Rust: register a column (Zone, parent_zone=None), a card (Zone, parent_zone=column_key), a title (Leaf, parent_zone=card_key) — `ancestor_zones(title) == [card, column]`\n-- [ ] Rust: `children_of_zone(column_key)` returns cards only, not titles\n-- [ ] Rust: removing a zone leaves its descendants' `parent_zone` pointers intact (dangling is OK; nav will handle); unregistering triggers fallback handled in card `01KNS0B3HY...`\n-- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key`\n-- [ ] `focus-layer.test.tsx` — ULID generated, stable across re-renders, new on remount; `parentLayerKey` prop overrides ancestor context\n-- [ ] `focus-scope.test.tsx` — spatial key generated; zone scope publishes key to `FocusZoneContext`; leaf descendants pick it up as `parent_zone`; throws if outside a FocusLayer\n-- [ ] Run `cargo test` and `cd kanban-app/ui && npx vitest run` — all pass\n+## Tests (pure Rust)\n+- [ ] `Focusable` and `FocusZone` structs have the fields listed above; serde JSON includes `\"kind\"` tag\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde\n+- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [ ] Registry: `children_of_zone` returns direct children only (not transitive descendants)\n+- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [ ] Registry: `children_of_layer` returns layers with `parent == this`, not layers in other windows\n+- [ ] Registry: `ancestors_of_layer` walks `layer.parent` up to window root\n+- [ ] Registry: `scopes_in_layer` filters by `layer_key`, returns both Focusables and Zones in that layer\n+- [ ] Registry: unregister removes; subsequent `scope(key)` returns None\n+- [ ] Registry: two windows + 2 inspector layers + 1 dialog layer = 5 layers, 2 roots, correct parent chains\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,24 +1,25 @@\n ## What\n\n-Define the Rust kernel types for spatial focus. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n+Add spatial awareness to the focus system with a clear frontend/backend split:\n\n-### The four peer types\n+- **React** measures DOM rects and reports them to Rust. React owns `FocusScope` (measures its element) and `FocusLayer` (declares a layer boundary).\n+- **Rust** owns the spatial registry, the **layer forest**, the **zone tree**, and the navigation algorithm. All navigation logic is backend-testable with synthetic rect data — no DOM needed.\n\n-| Role                | React component     | Rust type             |\n-|---------------------|---------------------|-----------------------|\n-| Leaf focusable point| `Focusable`         | `struct Focusable`    |\n-| Navigable container | `FocusZone`         | `struct FocusZone`    |\n-| Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n-| Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n+### Two structural concepts\n\n-On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum delegate to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`, etc.).\n+1. **Layers** are **hard** boundaries. Nav never crosses them. Window roots, inspector overlay, dialog overlays are layers. Layers form a **forest** (one root per Tauri window, plus stacked children).\n+2. **Zones** are **soft** containers within a layer. They give structure to beam search — container-first, with leaf-level cross-zone fallback. Perspective, nav bar, toolbar, column, card, inspector panel, field row — all zones.\n\n-On React: `FocusScope` is the high-level convenience wrapper that internally uses a `Focusable` or `FocusZone` primitive and adds entity plumbing (CommandScopeProvider, click-to-focus, context menu, focus bar, data-moniker attrs).\n+### Keying model\n\n-### Newtypes\n+- **Moniker** (`\"task:01ABC\"`, `\"field:task:01ABC.title\"`) — entity identity. Used for command scope chains, `setFocus`, backend dispatch.\n+- **SpatialKey** (ULID) — unique per FocusScope *instance*. Generated once per mount via `useRef(ulid())`. Primary key in the registry.\n+- Same moniker can appear in multiple locations (task card on board + mention pill in inspector). Each gets its own SpatialKey.\n\n-Shared with card `01KNM3YHHFJ3...`:\n+### Newtypes (Rust)\n\n+Shared with card `01KNM3YHHFJ3PTXZHD9EFKVBS6`:\n+\n ```rust\n #[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n #[serde(transparent)] pub struct WindowLabel(pub String);\n@@ -26,166 +27,181 @@\n #[serde(transparent)] pub struct LayerKey(pub String);\n ```\n\n-### Rust types\n+### Layer model: forest, not stack\n\n ```rust\n-/// Leaf — a single focusable point (task title, pill, button, menu item).\n-#[derive(Debug, Clone, Serialize, Deserialize)]\n-pub struct Focusable {\n-    pub key: SpatialKey,\n-    pub moniker: String,\n-    pub rect: Rect,\n-    pub layer_key: LayerKey,\n-    pub parent_zone: Option<SpatialKey>,\n-    pub overrides: HashMap<Direction, Option<String>>,\n+pub struct LayerEntry {\n+    pub key: LayerKey,\n+    pub name: String,                       // \"window\", \"inspector\", \"dialog\", \"palette\", ...\n+    pub parent: Option<LayerKey>,           // None for window roots; Some(...) for stacked layers\n+    pub window_label: WindowLabel,          // which window this layer lives in\n+    pub last_focused: Option<SpatialKey>,   // focus memory for pop-restore\n+}\n+```\n+\n+Two windows each with their own inspector → 4 layers total, 2 roots. Layers removed by key, not popped — supports arbitrary unmount order.\n+\n+### Zone model within a layer\n+\n+```rust\n+pub enum ScopeKind {\n+    Leaf,   // focusable element (task title, status pill, field pill, button)\n+    Zone,   // container (column, card, inspector panel, field row, toolbar group)\n }\n\n-/// Zone — a navigable container (card, column, inspector panel, field row,\n-/// toolbar group, nav bar). Has its own focus memory for drill-out / fallback.\n-#[derive(Debug, Clone, Serialize, Deserialize)]\n-pub struct FocusZone {\n-    pub key: SpatialKey,\n+pub struct SpatialEntry {\n     pub moniker: String,\n     pub rect: Rect,\n     pub layer_key: LayerKey,\n-    pub parent_zone: Option<SpatialKey>,\n-    pub last_focused: Option<SpatialKey>,\n+    pub kind: ScopeKind,\n+    pub parent_zone: Option<SpatialKey>,    // nearest ancestor Zone in the same layer; None = layer root\n+    pub last_focused: Option<SpatialKey>,   // focus memory for drill-out / fallback when kind == Zone\n     pub overrides: HashMap<Direction, Option<String>>,\n }\n\n-/// Layer — a hard modal boundary. Window roots, inspector overlay, dialogs.\n-#[derive(Debug, Clone, Serialize, Deserialize)]\n-pub struct FocusLayer {\n-    pub key: LayerKey,\n-    pub name: String,\n-    pub parent: Option<LayerKey>,\n-    pub window_label: WindowLabel,\n-    pub last_focused: Option<SpatialKey>,\n+pub struct SpatialRegistry {\n+    entries: HashMap<SpatialKey, SpatialEntry>,\n+    layers:  HashMap<LayerKey, LayerEntry>,\n }\n+```\n\n-/// Sum type stored in the registry per SpatialKey.\n-#[derive(Debug, Clone, Serialize, Deserialize)]\n-#[serde(tag = \"kind\", rename_all = \"snake_case\")]\n-pub enum FocusScope {\n-    Focusable(Focusable),\n-    Zone(FocusZone),\n-}\n+Zones form a **tree within each layer**, rooted at the layer root (`parent_zone = None`). Nav algorithm uses zones for container-first beam search and for drill-in/drill-out commands (see card `01KNQXXF5W7G4JP73C6ZCMKYKX` and the new drill-in/drill-out card).\n\n-impl FocusScope {\n-    pub fn key(&self) -> &SpatialKey { /* dispatch */ }\n-    pub fn moniker(&self) -> &str { /* dispatch */ }\n-    pub fn rect(&self) -> &Rect { /* dispatch */ }\n-    pub fn layer_key(&self) -> &LayerKey { /* dispatch */ }\n-    pub fn parent_zone(&self) -> Option<&SpatialKey> { /* dispatch */ }\n-    pub fn overrides(&self) -> &HashMap<Direction, Option<String>> { /* dispatch */ }\n-    pub fn is_zone(&self) -> bool { matches!(self, Self::Zone(_)) }\n-    pub fn is_focusable(&self) -> bool { matches!(self, Self::Focusable(_)) }\n-    pub fn as_zone(&self) -> Option<&FocusZone> { /* ... */ }\n-    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone> { /* ... */ }\n-}\n+### Which React regions become zones\n+\n+- `NavBar` → `kind=\"zone\"` (contains logo, menus, mode indicator as leaves)\n+- Toolbar / action groups → `kind=\"zone\"`\n+- Tab bar / perspective bar → `kind=\"zone\"`, each tab a leaf\n+- Perspective container → `kind=\"zone\"` (wraps the active view)\n+- Column (`column-view`) → `kind=\"zone\"` (contains header leaf + cards)\n+- Task card (`entity-card`) → `kind=\"zone\"` (contains title leaf + status leaf + pills)\n+- Inspector panel → `kind=\"zone\"` (contains field rows)\n+- Field row (inspector) → `kind=\"zone\"` (contains label + pills)\n+- Field pills, task titles, status badges, tab buttons → `kind=\"leaf\"` (default, no change needed)\n+\n+Detailed adoption happens in the migration cards (`01KNQXZ81Q...`, `01KNQY0P9J...`, and a new shell-wrapping card).\n+\n+### Architecture\n+\n ```\n+React (measure + report)              Rust (track + navigate)\n+─────────────────────────              ──────────────────────\n+FocusScope mounts (kind, moniker)\n+  spatialKey = useRef(ulid())\n+  parent_zone_key = nearest ancestor FocusScope with kind=\"zone\" (from context)\n+  → ResizeObserver fires\n+  → invoke: spatial_register           → registry.insert(spatial_key,\n+    { key, moniker, rect, kind,           { moniker, rect, kind,\n+      layer_key, parent_zone }              layer_key, parent_zone })\n\n-### Registry\n+FocusScope unmounts\n+  → invoke: spatial_unregister         → registry.remove(spatial_key)\n\n-```rust\n-pub struct SpatialRegistry {\n-    scopes: HashMap<SpatialKey, FocusScope>,\n-    layers: HashMap<LayerKey, FocusLayer>,\n-}\n+FocusLayer mounts\n+  layerKey = useRef(ulid())\n+  parentLayerKey = nearest ancestor FocusLayer (via context) OR explicit prop\n+  → invoke: spatial_push_layer         → layers.insert(key,\n+    { key, name, parent_key }             { name, parent, window_label })\n\n-impl SpatialRegistry {\n-    // Scope ops\n-    pub fn register_focusable(&mut self, f: Focusable);\n-    pub fn register_zone(&mut self, z: FocusZone);\n-    pub fn unregister_scope(&mut self, key: &SpatialKey);\n-    pub fn update_rect(&mut self, key: &SpatialKey, rect: Rect);\n-    pub fn scope(&self, key: &SpatialKey) -> Option<&FocusScope>;\n-    pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = &FocusScope>;\n-    pub fn ancestor_zones(&self, key: &SpatialKey) -> Vec<&FocusZone>;\n+FocusLayer unmounts\n+  → invoke: spatial_pop_layer          → layers.remove(key)\n\n-    // Layer ops\n-    pub fn push_layer(&mut self, l: FocusLayer);\n-    pub fn remove_layer(&mut self, key: &LayerKey);\n-    pub fn layer(&self, key: &LayerKey) -> Option<&FocusLayer>;\n-    pub fn children_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n-    pub fn root_for_window(&self, label: &WindowLabel) -> Option<&FocusLayer>;\n-    pub fn ancestors_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n-\n-    // Nav scoping\n-    pub fn scopes_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n-}\n+nav.right pressed\n+  → invoke: spatial_navigate           → three-rule beam search scoped to\n+    { key, direction }                    focused entry's layer + zones\n+  ← Ok                                 ← emit focus-changed event\n ```\n\n-### Tauri commands\n+### Rust side\n\n-One command per primitive registration op; a single unregister op (since React always knows the key it generated):\n+**`SpatialRegistry`** ops:\n+- `register(key: SpatialKey, moniker, rect, kind, layer_key, parent_zone)`\n+- `unregister(key: SpatialKey)`\n+- `update_rect(key: SpatialKey, rect)`\n+- `push_layer(key: LayerKey, name, parent_key: Option<LayerKey>, window_label: WindowLabel)`\n+- `remove_layer(key: LayerKey)`\n+- `children_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n+- `root_for_window(label: &WindowLabel) -> Option<&LayerEntry>`\n+- `ancestors_of_layer(key: LayerKey) -> Vec<&LayerEntry>`\n+- `entries_in_layer(key: &LayerKey) -> impl Iterator<Item=&SpatialEntry>`\n+- `children_of_zone(zone_key: SpatialKey) -> impl Iterator<Item=&SpatialEntry>` — entries with `parent_zone == Some(zone_key)`\n+- `ancestor_zones(key: SpatialKey) -> Vec<&SpatialEntry>` — walk `parent_zone` up to layer root\n\n-```rust\n-#[tauri::command]\n-async fn spatial_register_focusable(window: tauri::Window, ...) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_register_zone(window: tauri::Window, ...) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_update_rect(window: tauri::Window, key: SpatialKey, rect: Rect) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: String, parent: Option<LayerKey>) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n-```\n+**Tauri commands**:\n+- `spatial_register(window: tauri::Window, key, moniker, x, y, w, h, kind: ScopeKind, layer_key, parent_zone: Option<SpatialKey>)`\n+- `spatial_unregister(window: tauri::Window, key)`\n+- `spatial_push_layer(window: tauri::Window, key, name, parent_key: Option<LayerKey>)`\n+- `spatial_pop_layer(window: tauri::Window, key)`\n\n-All derive `window_label` from the `tauri::Window` parameter. React never passes the window label.\n+`window_label` is derived from the `tauri::Window` parameter, never passed by React.\n\n-### Serde wire format\n+### React side\n\n-`#[serde(tag = \"kind\", rename_all = \"snake_case\")]` on `FocusScope` produces:\n+**`FocusLayer`** component (`kanban-app/ui/src/components/focus-layer.tsx`):\n+- Props: `name: string`, optional `parentLayerKey`, `children`\n+- `const layerKey = useRef(ulid()).current`\n+- Resolves parent: explicit `parentLayerKey` prop wins; else nearest ancestor via `FocusLayerContext`; else `None` (root).\n+- Provides `layerKey` via context\n+- Mount: `invoke(\"spatial_push_layer\", { key, name, parentKey })`\n+- Unmount: `invoke(\"spatial_pop_layer\", { key })`\n\n-```json\n-{ \"kind\": \"focusable\", \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": null, \"overrides\": {} }\n-{ \"kind\": \"zone\",      \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": \"...\", \"last_focused\": null, \"overrides\": {} }\n-```\n+**`FocusScope`** (`kanban-app/ui/src/components/focus-scope.tsx`):\n+- Props add: `kind?: \"leaf\" | \"zone\"` (default `\"leaf\"`)\n+- `const spatialKey = useRef(ulid()).current`\n+- Reads `layerKey` from `FocusLayerContext` (throws if missing)\n+- Reads `parentZoneKey` from `FocusZoneContext` (the nearest ancestor FocusScope where `kind===\"zone\"` published its own key)\n+- When `kind === \"zone\"`, publishes its own `spatialKey` on `FocusZoneContext` for descendants\n+- `ResizeObserver` on the wrapper div\n+- Mount/resize: `invoke(\"spatial_register\", { key, moniker, ...rect, kind, layerKey, parentZone })`\n+- Unmount: `invoke(\"spatial_unregister\", { key })`\n\n-Newtypes (`SpatialKey`, `LayerKey`, `WindowLabel`) are `#[serde(transparent)]` → plain strings over the wire.\n+**`App.tsx`** (or `app-shell.tsx`): wrap app root in `<FocusLayer name=\"window\">`.\n\n ### Design decisions\n+- **Newtypes not Strings**: `WindowLabel`/`SpatialKey`/`LayerKey` are distinct types so a `LayerKey` cannot be accidentally passed where a `SpatialKey` is expected. `#[serde(transparent)]` keeps the wire protocol simple.\n+- **Layers form a forest**: multi-window is first-class, not a retrofit. Two windows = two trees.\n+- **Zones form a tree within each layer**: gives nav algorithm container-first semantics without needing mode state.\n+- **parent_zone pointer**: simpler than a child list; `children_of_zone` walks the map once, fine for < ~hundreds of entries per layer.\n+- **Layer removal by key**: arbitrary removal order.\n+- **window_label derived, not passed**: Tauri knows it from the invoking webview.\n+- **`kind` on SpatialEntry** (not a separate type): zones and leaves share the same table; nav filters by `kind` where relevant.\n\n-- **Four peer types, not a `kind` enum on one struct**: makes zone-only fields (`last_focused`) type-checked — a `Focusable` literally cannot have a `last_focused` field to forget about.\n-- **`FocusScope` as a Rust enum**: a single stored type per `SpatialKey`, pattern matching gives the nav algorithm a clean API.\n-- **Registry stores the enum, not a trait object**: zero dynamic dispatch; serde works out of the box with `#[serde(tag = \"kind\")]`.\n-- **Overrides on both variants**: overrides apply to anything focusable. Cheap empty HashMap default.\n-- **`last_focused` only on `FocusZone`**: only zones need drill-in / fallback memory.\n-\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes\n-- [ ] Define `Rect`, `Direction`\n-- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum) with serde attrs\n-- [ ] Implement `FocusScope` helper methods (`key`, `moniker`, `rect`, `layer_key`, `parent_zone`, `overrides`, `is_zone`, `as_zone*`)\n-- [ ] Implement `SpatialRegistry` with scope + layer ops above\n-- [ ] Implement forest ops for layers (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n-- [ ] Implement zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [ ] Add Tauri commands per primitive (register_focusable, register_zone, unregister_scope, update_rect, push_layer, pop_layer); each derives `window_label` from `tauri::Window`\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes with `#[serde(transparent)]`\n+- [ ] Define `Rect`, `ScopeKind`, `SpatialEntry`, `LayerEntry`, `SpatialRegistry`\n+- [ ] Implement layer forest ops: `children_of_layer`, `root_for_window`, `ancestors_of_layer`\n+- [ ] Implement zone tree ops: `children_of_zone`, `ancestor_zones`\n+- [ ] `entries_in_layer` iterator for nav scoping\n+- [ ] Tauri commands derive `window_label` from `tauri::Window`\n+- [ ] Create `FocusLayer` component with ULID layer key, parent context/prop, Tauri invokes\n+- [ ] Create `FocusZoneContext` and update `FocusScope` with `kind` prop, parent_zone resolution, Tauri invokes\n+- [ ] Wrap app root in `<FocusLayer name=\"window\">`\n\n ## Acceptance Criteria\n-- [ ] Four peer types exist in Rust: `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum)\n-- [ ] `FocusScope` enum dispatches helper accessors to its variant\n-- [ ] `Focusable` has no `last_focused` field; `FocusZone` does\n-- [ ] All newtypes are `#[serde(transparent)]`; enum is `#[serde(tag = \"kind\")]`\n-- [ ] `SpatialRegistry` can hold any mix of focusables, zones, and layers; well-formed across windows + nested zones\n-- [ ] Tauri commands derive `window_label` from `tauri::Window`\n-- [ ] `cargo test` passes\n+- [ ] `WindowLabel`, `SpatialKey`, `LayerKey` are newtypes throughout Rust; wire format is plain strings\n+- [ ] Each FocusScope mount generates a unique ULID `SpatialKey`; each FocusLayer mount generates a unique ULID `LayerKey`\n+- [ ] Every layer carries its `window_label`, derived from Tauri (not React args)\n+- [ ] Root window layers have `parent = None`; stacked layers have `parent = Some(...)`\n+- [ ] The registry can represent: 2 windows + 2 inspector layers (one per window) + 1 dialog on top of one inspector = 5 layers, 2 roots, correct parent chains\n+- [ ] Every SpatialEntry has `kind` and `parent_zone`; the zone tree is well-formed (no cycles, all parents exist in same layer)\n+- [ ] `children_of_zone` returns direct children only (not transitive descendants)\n+- [ ] `ancestor_zones` walks up to the layer root (entries with `parent_zone = None`)\n+- [ ] Same moniker at two locations → two entries, different SpatialKeys\n+- [ ] Layer removal by key supports arbitrary order\n+- [ ] `cargo test` passes, `pnpm vitest run` passes\n\n-## Tests (pure Rust)\n-- [ ] `Focusable` and `FocusZone` structs have the fields listed above; serde JSON includes `\"kind\"` tag\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde\n-- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` returns direct children only (not transitive descendants)\n-- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` returns layers with `parent == this`, not layers in other windows\n-- [ ] Registry: `ancestors_of_layer` walks `layer.parent` up to window root\n-- [ ] Registry: `scopes_in_layer` filters by `layer_key`, returns both Focusables and Zones in that layer\n-- [ ] Registry: unregister removes; subsequent `scope(key)` returns None\n-- [ ] Registry: two windows + 2 inspector layers + 1 dialog layer = 5 layers, 2 roots, correct parent chains\n+## Tests\n+- [ ] Rust: register two entries with same moniker, different keys — both stored\n+- [ ] Rust: push window root A + window root B (parent=None, different window_label) — coexist as siblings\n+- [ ] Rust: push window A, inspector I (parent=A), dialog D (parent=I) — `ancestors_of_layer(D) == [I, A]`\n+- [ ] Rust: `children_of_layer(A)` returns inspectors of A only, not of other windows\n+- [ ] Rust: register a column (Zone, parent_zone=None), a card (Zone, parent_zone=column_key), a title (Leaf, parent_zone=card_key) — `ancestor_zones(title) == [card, column]`\n+- [ ] Rust: `children_of_zone(column_key)` returns cards only, not titles\n+- [ ] Rust: removing a zone leaves its descendants' `parent_zone` pointers intact (dangling is OK; nav will handle); unregistering triggers fallback handled in card `01KNS0B3HY...`\n+- [ ] Rust: `entries_in_layer(inspector_key)` returns only entries whose `layer_key == inspector_key`\n+- [ ] `focus-layer.test.tsx` — ULID generated, stable across re-renders, new on remount; `parentLayerKey` prop overrides ancestor context\n+- [ ] `focus-scope.test.tsx` — spatial key generated; zone scope publishes key to `FocusZoneContext`; leaf descendants pick it up as `parent_zone`; throws if outside a FocusLayer\n+- [ ] Run `cargo test` and `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Spatial registry: layer forest + zones, typed keys, React measures rects\n\\ No newline at end of file\n+Rust kernel: Focusable, FocusZone, FocusLayer, FocusScope types + SpatialRegistry\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Rust kernel: Focusable, FocusZone, FocusLayer, FocusScope types + SpatialRegistry\n\\ No newline at end of file\n+Spatial registry: layer forest + zones, typed keys, React measures rects\n\\ No newline at end of file\n"}]]}
{"id":"01KPZWPDAXVVXEF87GPBSY8D21","timestamp":"2026-04-24T14:00:58.973676Z","op":"update","item_id":"01KNQXW7HHHB8HW76K3PXH3G34","forward_patch":"--- original\n+++ modified\n@@ -21,53 +21,86 @@\n | Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n | Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n\n-On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum delegate to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`, etc.).\n+On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum dispatch to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`).\n\n On React: `FocusScope` is the high-level convenience wrapper that internally uses a `Focusable` or `FocusZone` primitive and adds entity plumbing (CommandScopeProvider, click-to-focus, context menu, focus bar, data-moniker attrs).\n\n-### Newtypes\n+### Newtype discipline — no bare primitives on type signatures\n\n-Shared with card `01KNM3YHHFJ3...`:\n+**All identity and measurement fields are newtypes.** No `String`, no `f64` at the boundaries of the public types, no exceptions. Wire format stays plain via `#[serde(transparent)]`.\n\n ```rust\n #[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct WindowLabel(pub String);\n-#[serde(transparent)] pub struct SpatialKey(pub String);\n-#[serde(transparent)] pub struct LayerKey(pub String);\n+#[serde(transparent)] pub struct WindowLabel(pub String);   // Tauri window label\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct SpatialKey(pub String);    // ULID per scope mount\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct LayerKey(pub String);      // ULID per layer mount\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct Moniker(pub String);       // entity identity: \"task:01ABC\", \"ui:toolbar.new\"\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct LayerName(pub String);     // layer role: \"window\", \"inspector\", \"dialog\", \"palette\"\n+\n+#[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct Pixels(pub f64);           // screen coordinate / size in CSS pixels\n+```\n+\n+`Pixels` implements arithmetic (`Add`, `Sub`, `Mul<f64>`, `Div<f64>`) so the nav algorithm's beam/score math stays ergonomic without drifting to `f64`.\n+\n+### Rect\n+\n+```rust\n+#[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n+pub struct Rect {\n+    pub x: Pixels,\n+    pub y: Pixels,\n+    pub width: Pixels,\n+    pub height: Pixels,\n+}\n+\n+impl Rect {\n+    pub fn top(&self)    -> Pixels { self.y }\n+    pub fn left(&self)   -> Pixels { self.x }\n+    pub fn bottom(&self) -> Pixels { Pixels(self.y.0 + self.height.0) }\n+    pub fn right(&self)  -> Pixels { Pixels(self.x.0 + self.width.0) }\n+}\n ```\n\n-### Rust types\n+### The four types\n\n ```rust\n-/// Leaf — a single focusable point (task title, pill, button, menu item).\n+/// Leaf — a single focusable point.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct Focusable {\n     pub key: SpatialKey,\n-    pub moniker: String,\n+    pub moniker: Moniker,\n     pub rect: Rect,\n     pub layer_key: LayerKey,\n     pub parent_zone: Option<SpatialKey>,\n-    pub overrides: HashMap<Direction, Option<String>>,\n+    pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n-/// Zone — a navigable container (card, column, inspector panel, field row,\n-/// toolbar group, nav bar). Has its own focus memory for drill-out / fallback.\n+/// Zone — a navigable container with focus memory.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusZone {\n     pub key: SpatialKey,\n-    pub moniker: String,\n+    pub moniker: Moniker,\n     pub rect: Rect,\n     pub layer_key: LayerKey,\n     pub parent_zone: Option<SpatialKey>,\n     pub last_focused: Option<SpatialKey>,\n-    pub overrides: HashMap<Direction, Option<String>>,\n+    pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n-/// Layer — a hard modal boundary. Window roots, inspector overlay, dialogs.\n+/// Layer — a hard modal boundary.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusLayer {\n     pub key: LayerKey,\n-    pub name: String,\n+    pub name: LayerName,\n     pub parent: Option<LayerKey>,\n     pub window_label: WindowLabel,\n     pub last_focused: Option<SpatialKey>,\n@@ -82,16 +115,16 @@\n }\n\n impl FocusScope {\n-    pub fn key(&self) -> &SpatialKey { /* dispatch */ }\n-    pub fn moniker(&self) -> &str { /* dispatch */ }\n-    pub fn rect(&self) -> &Rect { /* dispatch */ }\n-    pub fn layer_key(&self) -> &LayerKey { /* dispatch */ }\n-    pub fn parent_zone(&self) -> Option<&SpatialKey> { /* dispatch */ }\n-    pub fn overrides(&self) -> &HashMap<Direction, Option<String>> { /* dispatch */ }\n-    pub fn is_zone(&self) -> bool { matches!(self, Self::Zone(_)) }\n-    pub fn is_focusable(&self) -> bool { matches!(self, Self::Focusable(_)) }\n-    pub fn as_zone(&self) -> Option<&FocusZone> { /* ... */ }\n-    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone> { /* ... */ }\n+    pub fn key(&self) -> &SpatialKey;\n+    pub fn moniker(&self) -> &Moniker;\n+    pub fn rect(&self) -> &Rect;\n+    pub fn layer_key(&self) -> &LayerKey;\n+    pub fn parent_zone(&self) -> Option<&SpatialKey>;\n+    pub fn overrides(&self) -> &HashMap<Direction, Option<Moniker>>;\n+    pub fn is_zone(&self) -> bool;\n+    pub fn is_focusable(&self) -> bool;\n+    pub fn as_zone(&self) -> Option<&FocusZone>;\n+    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone>;\n }\n ```\n\n@@ -104,7 +137,6 @@\n }\n\n impl SpatialRegistry {\n-    // Scope ops\n     pub fn register_focusable(&mut self, f: Focusable);\n     pub fn register_zone(&mut self, z: FocusZone);\n     pub fn unregister_scope(&mut self, key: &SpatialKey);\n@@ -113,7 +145,6 @@\n     pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = &FocusScope>;\n     pub fn ancestor_zones(&self, key: &SpatialKey) -> Vec<&FocusZone>;\n\n-    // Layer ops\n     pub fn push_layer(&mut self, l: FocusLayer);\n     pub fn remove_layer(&mut self, key: &LayerKey);\n     pub fn layer(&self, key: &LayerKey) -> Option<&FocusLayer>;\n@@ -121,7 +152,6 @@\n     pub fn root_for_window(&self, label: &WindowLabel) -> Option<&FocusLayer>;\n     pub fn ancestors_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n\n-    // Nav scoping\n     pub fn scopes_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n }\n ```\n@@ -128,74 +158,93 @@\n\n ### Tauri commands\n\n-One command per primitive registration op; a single unregister op (since React always knows the key it generated):\n+Every command signature uses newtypes, not `String`/`f64`:\n\n ```rust\n #[tauri::command]\n-async fn spatial_register_focusable(window: tauri::Window, ...) -> Result<()>;\n+async fn spatial_register_focusable(\n+    window: tauri::Window,\n+    key: SpatialKey,\n+    moniker: Moniker,\n+    rect: Rect,\n+    layer_key: LayerKey,\n+    parent_zone: Option<SpatialKey>,\n+    overrides: HashMap<Direction, Option<Moniker>>,\n+) -> Result<()>;\n+\n #[tauri::command]\n-async fn spatial_register_zone(window: tauri::Window, ...) -> Result<()>;\n+async fn spatial_register_zone(\n+    window: tauri::Window,\n+    key: SpatialKey,\n+    moniker: Moniker,\n+    rect: Rect,\n+    layer_key: LayerKey,\n+    parent_zone: Option<SpatialKey>,\n+    overrides: HashMap<Direction, Option<Moniker>>,\n+) -> Result<()>;\n+\n #[tauri::command]\n async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n #[tauri::command]\n async fn spatial_update_rect(window: tauri::Window, key: SpatialKey, rect: Rect) -> Result<()>;\n #[tauri::command]\n-async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: String, parent: Option<LayerKey>) -> Result<()>;\n+async fn spatial_push_layer(\n+    window: tauri::Window,\n+    key: LayerKey,\n+    name: LayerName,\n+    parent: Option<LayerKey>,\n+) -> Result<()>;\n #[tauri::command]\n async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n\n-All derive `window_label` from the `tauri::Window` parameter. React never passes the window label.\n+`window_label: WindowLabel` is derived from the `tauri::Window` parameter inside the handler, never passed from React.\n\n ### Serde wire format\n\n-`#[serde(tag = \"kind\", rename_all = \"snake_case\")]` on `FocusScope` produces:\n-\n-```json\n-{ \"kind\": \"focusable\", \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": null, \"overrides\": {} }\n-{ \"kind\": \"zone\",      \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": \"...\", \"last_focused\": null, \"overrides\": {} }\n-```\n-\n-Newtypes (`SpatialKey`, `LayerKey`, `WindowLabel`) are `#[serde(transparent)]` → plain strings over the wire.\n+All newtypes are `#[serde(transparent)]` → JSON strings/numbers. The `FocusScope` enum serializes with `\"kind\": \"focusable\" | \"zone\"` as a discriminator tag.\n\n ### Design decisions\n\n-- **Four peer types, not a `kind` enum on one struct**: makes zone-only fields (`last_focused`) type-checked — a `Focusable` literally cannot have a `last_focused` field to forget about.\n-- **`FocusScope` as a Rust enum**: a single stored type per `SpatialKey`, pattern matching gives the nav algorithm a clean API.\n-- **Registry stores the enum, not a trait object**: zero dynamic dispatch; serde works out of the box with `#[serde(tag = \"kind\")]`.\n-- **Overrides on both variants**: overrides apply to anything focusable. Cheap empty HashMap default.\n-- **`last_focused` only on `FocusZone`**: only zones need drill-in / fallback memory.\n+- **Newtypes throughout, not just on identity**: measurements (`Pixels`) are newtyped too. Prevents accidental mixing of pixel values with unrelated floats (e.g., scoring multipliers, timestamps) and makes future unit changes (device pixels vs CSS pixels) explicit.\n+- **Override target is `Moniker`, not `String`**: matches the setFocus target type; prevents accidentally stuffing a `SpatialKey` where a moniker goes.\n+- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked; a `Focusable` cannot carry one.\n+- **`FocusScope` as a Rust enum**: one map, pattern matching in nav. No trait objects.\n+- **`LayerName` as a newtype, not an enum**: layer names grow organically (window, inspector, dialog, palette, quick-capture, future things). Newtype keeps the string bounded to the slot without over-constraining to a fixed set.\n\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes\n-- [ ] Define `Rect`, `Direction`\n-- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum) with serde attrs\n-- [ ] Implement `FocusScope` helper methods (`key`, `moniker`, `rect`, `layer_key`, `parent_zone`, `overrides`, `is_zone`, `as_zone*`)\n-- [ ] Implement `SpatialRegistry` with scope + layer ops above\n-- [ ] Implement forest ops for layers (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n-- [ ] Implement zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [ ] Add Tauri commands per primitive (register_focusable, register_zone, unregister_scope, update_rect, push_layer, pop_layer); each derives `window_label` from `tauri::Window`\n+- [ ] Define newtypes: `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName`, `Pixels`\n+- [ ] Impl `Add`/`Sub`/`Mul<f64>`/`Div<f64>` on `Pixels`\n+- [ ] Define `Rect` with `Pixels` fields + accessor helpers\n+- [ ] Define `Direction` enum\n+- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` enum with serde attrs\n+- [ ] Implement `FocusScope` helper methods\n+- [ ] Implement `SpatialRegistry` with scope + layer ops\n+- [ ] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n+- [ ] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n+- [ ] Tauri commands with fully-typed signatures (no `String`/`f64` on command params)\n\n ## Acceptance Criteria\n-- [ ] Four peer types exist in Rust: `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum)\n-- [ ] `FocusScope` enum dispatches helper accessors to its variant\n-- [ ] `Focusable` has no `last_focused` field; `FocusZone` does\n-- [ ] All newtypes are `#[serde(transparent)]`; enum is `#[serde(tag = \"kind\")]`\n-- [ ] `SpatialRegistry` can hold any mix of focusables, zones, and layers; well-formed across windows + nested zones\n+- [ ] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n+- [ ] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [ ] All newtypes are `#[serde(transparent)]`; the enum is `#[serde(tag = \"kind\")]`\n+- [ ] `Focusable` has no `last_focused` field; only `FocusZone` does\n+- [ ] `Pixels` supports arithmetic without requiring `.0` access in the nav algorithm\n+- [ ] `SpatialRegistry` holds any mix of focusables, zones, and layers across windows + nested zones\n - [ ] Tauri commands derive `window_label` from `tauri::Window`\n - [ ] `cargo test` passes\n\n ## Tests (pure Rust)\n-- [ ] `Focusable` and `FocusZone` structs have the fields listed above; serde JSON includes `\"kind\"` tag\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde\n+- [ ] Each newtype is `#[serde(transparent)]` — JSON round-trips as a bare primitive\n+- [ ] `Pixels` arithmetic produces a `Pixels` result (type-checked)\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde with `\"kind\"` tag\n - [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` returns direct children only (not transitive descendants)\n+- [ ] Registry: `children_of_zone` direct children only (not transitive)\n - [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` returns layers with `parent == this`, not layers in other windows\n+- [ ] Registry: `children_of_layer` returns layers with matching `parent`, not cross-window\n - [ ] Registry: `ancestors_of_layer` walks `layer.parent` up to window root\n-- [ ] Registry: `scopes_in_layer` filters by `layer_key`, returns both Focusables and Zones in that layer\n-- [ ] Registry: unregister removes; subsequent `scope(key)` returns None\n-- [ ] Registry: two windows + 2 inspector layers + 1 dialog layer = 5 layers, 2 roots, correct parent chains\n+- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones filtered by `layer_key`\n+- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -21,86 +21,53 @@\n | Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n | Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n\n-On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum dispatch to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`).\n+On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum delegate to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`, etc.).\n\n On React: `FocusScope` is the high-level convenience wrapper that internally uses a `Focusable` or `FocusZone` primitive and adds entity plumbing (CommandScopeProvider, click-to-focus, context menu, focus bar, data-moniker attrs).\n\n-### Newtype discipline — no bare primitives on type signatures\n+### Newtypes\n\n-**All identity and measurement fields are newtypes.** No `String`, no `f64` at the boundaries of the public types, no exceptions. Wire format stays plain via `#[serde(transparent)]`.\n+Shared with card `01KNM3YHHFJ3...`:\n\n ```rust\n #[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct WindowLabel(pub String);   // Tauri window label\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct SpatialKey(pub String);    // ULID per scope mount\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct LayerKey(pub String);      // ULID per layer mount\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct Moniker(pub String);       // entity identity: \"task:01ABC\", \"ui:toolbar.new\"\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct LayerName(pub String);     // layer role: \"window\", \"inspector\", \"dialog\", \"palette\"\n-\n-#[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct Pixels(pub f64);           // screen coordinate / size in CSS pixels\n+#[serde(transparent)] pub struct WindowLabel(pub String);\n+#[serde(transparent)] pub struct SpatialKey(pub String);\n+#[serde(transparent)] pub struct LayerKey(pub String);\n ```\n\n-`Pixels` implements arithmetic (`Add`, `Sub`, `Mul<f64>`, `Div<f64>`) so the nav algorithm's beam/score math stays ergonomic without drifting to `f64`.\n-\n-### Rect\n+### Rust types\n\n ```rust\n-#[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n-pub struct Rect {\n-    pub x: Pixels,\n-    pub y: Pixels,\n-    pub width: Pixels,\n-    pub height: Pixels,\n-}\n-\n-impl Rect {\n-    pub fn top(&self)    -> Pixels { self.y }\n-    pub fn left(&self)   -> Pixels { self.x }\n-    pub fn bottom(&self) -> Pixels { Pixels(self.y.0 + self.height.0) }\n-    pub fn right(&self)  -> Pixels { Pixels(self.x.0 + self.width.0) }\n-}\n-```\n-\n-### The four types\n-\n-```rust\n-/// Leaf — a single focusable point.\n+/// Leaf — a single focusable point (task title, pill, button, menu item).\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct Focusable {\n     pub key: SpatialKey,\n-    pub moniker: Moniker,\n+    pub moniker: String,\n     pub rect: Rect,\n     pub layer_key: LayerKey,\n     pub parent_zone: Option<SpatialKey>,\n-    pub overrides: HashMap<Direction, Option<Moniker>>,\n+    pub overrides: HashMap<Direction, Option<String>>,\n }\n\n-/// Zone — a navigable container with focus memory.\n+/// Zone — a navigable container (card, column, inspector panel, field row,\n+/// toolbar group, nav bar). Has its own focus memory for drill-out / fallback.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusZone {\n     pub key: SpatialKey,\n-    pub moniker: Moniker,\n+    pub moniker: String,\n     pub rect: Rect,\n     pub layer_key: LayerKey,\n     pub parent_zone: Option<SpatialKey>,\n     pub last_focused: Option<SpatialKey>,\n-    pub overrides: HashMap<Direction, Option<Moniker>>,\n+    pub overrides: HashMap<Direction, Option<String>>,\n }\n\n-/// Layer — a hard modal boundary.\n+/// Layer — a hard modal boundary. Window roots, inspector overlay, dialogs.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusLayer {\n     pub key: LayerKey,\n-    pub name: LayerName,\n+    pub name: String,\n     pub parent: Option<LayerKey>,\n     pub window_label: WindowLabel,\n     pub last_focused: Option<SpatialKey>,\n@@ -115,16 +82,16 @@\n }\n\n impl FocusScope {\n-    pub fn key(&self) -> &SpatialKey;\n-    pub fn moniker(&self) -> &Moniker;\n-    pub fn rect(&self) -> &Rect;\n-    pub fn layer_key(&self) -> &LayerKey;\n-    pub fn parent_zone(&self) -> Option<&SpatialKey>;\n-    pub fn overrides(&self) -> &HashMap<Direction, Option<Moniker>>;\n-    pub fn is_zone(&self) -> bool;\n-    pub fn is_focusable(&self) -> bool;\n-    pub fn as_zone(&self) -> Option<&FocusZone>;\n-    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone>;\n+    pub fn key(&self) -> &SpatialKey { /* dispatch */ }\n+    pub fn moniker(&self) -> &str { /* dispatch */ }\n+    pub fn rect(&self) -> &Rect { /* dispatch */ }\n+    pub fn layer_key(&self) -> &LayerKey { /* dispatch */ }\n+    pub fn parent_zone(&self) -> Option<&SpatialKey> { /* dispatch */ }\n+    pub fn overrides(&self) -> &HashMap<Direction, Option<String>> { /* dispatch */ }\n+    pub fn is_zone(&self) -> bool { matches!(self, Self::Zone(_)) }\n+    pub fn is_focusable(&self) -> bool { matches!(self, Self::Focusable(_)) }\n+    pub fn as_zone(&self) -> Option<&FocusZone> { /* ... */ }\n+    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone> { /* ... */ }\n }\n ```\n\n@@ -137,6 +104,7 @@\n }\n\n impl SpatialRegistry {\n+    // Scope ops\n     pub fn register_focusable(&mut self, f: Focusable);\n     pub fn register_zone(&mut self, z: FocusZone);\n     pub fn unregister_scope(&mut self, key: &SpatialKey);\n@@ -145,6 +113,7 @@\n     pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = &FocusScope>;\n     pub fn ancestor_zones(&self, key: &SpatialKey) -> Vec<&FocusZone>;\n\n+    // Layer ops\n     pub fn push_layer(&mut self, l: FocusLayer);\n     pub fn remove_layer(&mut self, key: &LayerKey);\n     pub fn layer(&self, key: &LayerKey) -> Option<&FocusLayer>;\n@@ -152,6 +121,7 @@\n     pub fn root_for_window(&self, label: &WindowLabel) -> Option<&FocusLayer>;\n     pub fn ancestors_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n\n+    // Nav scoping\n     pub fn scopes_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n }\n ```\n@@ -158,93 +128,74 @@\n\n ### Tauri commands\n\n-Every command signature uses newtypes, not `String`/`f64`:\n+One command per primitive registration op; a single unregister op (since React always knows the key it generated):\n\n ```rust\n #[tauri::command]\n-async fn spatial_register_focusable(\n-    window: tauri::Window,\n-    key: SpatialKey,\n-    moniker: Moniker,\n-    rect: Rect,\n-    layer_key: LayerKey,\n-    parent_zone: Option<SpatialKey>,\n-    overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<()>;\n-\n+async fn spatial_register_focusable(window: tauri::Window, ...) -> Result<()>;\n #[tauri::command]\n-async fn spatial_register_zone(\n-    window: tauri::Window,\n-    key: SpatialKey,\n-    moniker: Moniker,\n-    rect: Rect,\n-    layer_key: LayerKey,\n-    parent_zone: Option<SpatialKey>,\n-    overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<()>;\n-\n+async fn spatial_register_zone(window: tauri::Window, ...) -> Result<()>;\n #[tauri::command]\n async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n #[tauri::command]\n async fn spatial_update_rect(window: tauri::Window, key: SpatialKey, rect: Rect) -> Result<()>;\n #[tauri::command]\n-async fn spatial_push_layer(\n-    window: tauri::Window,\n-    key: LayerKey,\n-    name: LayerName,\n-    parent: Option<LayerKey>,\n-) -> Result<()>;\n+async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: String, parent: Option<LayerKey>) -> Result<()>;\n #[tauri::command]\n async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n\n-`window_label: WindowLabel` is derived from the `tauri::Window` parameter inside the handler, never passed from React.\n+All derive `window_label` from the `tauri::Window` parameter. React never passes the window label.\n\n ### Serde wire format\n\n-All newtypes are `#[serde(transparent)]` → JSON strings/numbers. The `FocusScope` enum serializes with `\"kind\": \"focusable\" | \"zone\"` as a discriminator tag.\n+`#[serde(tag = \"kind\", rename_all = \"snake_case\")]` on `FocusScope` produces:\n\n+```json\n+{ \"kind\": \"focusable\", \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": null, \"overrides\": {} }\n+{ \"kind\": \"zone\",      \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": \"...\", \"last_focused\": null, \"overrides\": {} }\n+```\n+\n+Newtypes (`SpatialKey`, `LayerKey`, `WindowLabel`) are `#[serde(transparent)]` → plain strings over the wire.\n+\n ### Design decisions\n\n-- **Newtypes throughout, not just on identity**: measurements (`Pixels`) are newtyped too. Prevents accidental mixing of pixel values with unrelated floats (e.g., scoring multipliers, timestamps) and makes future unit changes (device pixels vs CSS pixels) explicit.\n-- **Override target is `Moniker`, not `String`**: matches the setFocus target type; prevents accidentally stuffing a `SpatialKey` where a moniker goes.\n-- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked; a `Focusable` cannot carry one.\n-- **`FocusScope` as a Rust enum**: one map, pattern matching in nav. No trait objects.\n-- **`LayerName` as a newtype, not an enum**: layer names grow organically (window, inspector, dialog, palette, quick-capture, future things). Newtype keeps the string bounded to the slot without over-constraining to a fixed set.\n+- **Four peer types, not a `kind` enum on one struct**: makes zone-only fields (`last_focused`) type-checked — a `Focusable` literally cannot have a `last_focused` field to forget about.\n+- **`FocusScope` as a Rust enum**: a single stored type per `SpatialKey`, pattern matching gives the nav algorithm a clean API.\n+- **Registry stores the enum, not a trait object**: zero dynamic dispatch; serde works out of the box with `#[serde(tag = \"kind\")]`.\n+- **Overrides on both variants**: overrides apply to anything focusable. Cheap empty HashMap default.\n+- **`last_focused` only on `FocusZone`**: only zones need drill-in / fallback memory.\n\n ### Subtasks\n-- [ ] Define newtypes: `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName`, `Pixels`\n-- [ ] Impl `Add`/`Sub`/`Mul<f64>`/`Div<f64>` on `Pixels`\n-- [ ] Define `Rect` with `Pixels` fields + accessor helpers\n-- [ ] Define `Direction` enum\n-- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` enum with serde attrs\n-- [ ] Implement `FocusScope` helper methods\n-- [ ] Implement `SpatialRegistry` with scope + layer ops\n-- [ ] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n-- [ ] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [ ] Tauri commands with fully-typed signatures (no `String`/`f64` on command params)\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes\n+- [ ] Define `Rect`, `Direction`\n+- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum) with serde attrs\n+- [ ] Implement `FocusScope` helper methods (`key`, `moniker`, `rect`, `layer_key`, `parent_zone`, `overrides`, `is_zone`, `as_zone*`)\n+- [ ] Implement `SpatialRegistry` with scope + layer ops above\n+- [ ] Implement forest ops for layers (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n+- [ ] Implement zone tree ops (`children_of_zone`, `ancestor_zones`)\n+- [ ] Add Tauri commands per primitive (register_focusable, register_zone, unregister_scope, update_rect, push_layer, pop_layer); each derives `window_label` from `tauri::Window`\n\n ## Acceptance Criteria\n-- [ ] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n-- [ ] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [ ] All newtypes are `#[serde(transparent)]`; the enum is `#[serde(tag = \"kind\")]`\n-- [ ] `Focusable` has no `last_focused` field; only `FocusZone` does\n-- [ ] `Pixels` supports arithmetic without requiring `.0` access in the nav algorithm\n-- [ ] `SpatialRegistry` holds any mix of focusables, zones, and layers across windows + nested zones\n+- [ ] Four peer types exist in Rust: `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum)\n+- [ ] `FocusScope` enum dispatches helper accessors to its variant\n+- [ ] `Focusable` has no `last_focused` field; `FocusZone` does\n+- [ ] All newtypes are `#[serde(transparent)]`; enum is `#[serde(tag = \"kind\")]`\n+- [ ] `SpatialRegistry` can hold any mix of focusables, zones, and layers; well-formed across windows + nested zones\n - [ ] Tauri commands derive `window_label` from `tauri::Window`\n - [ ] `cargo test` passes\n\n ## Tests (pure Rust)\n-- [ ] Each newtype is `#[serde(transparent)]` — JSON round-trips as a bare primitive\n-- [ ] `Pixels` arithmetic produces a `Pixels` result (type-checked)\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde with `\"kind\"` tag\n+- [ ] `Focusable` and `FocusZone` structs have the fields listed above; serde JSON includes `\"kind\"` tag\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde\n - [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` direct children only (not transitive)\n+- [ ] Registry: `children_of_zone` returns direct children only (not transitive descendants)\n - [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` returns layers with matching `parent`, not cross-window\n+- [ ] Registry: `children_of_layer` returns layers with `parent == this`, not layers in other windows\n - [ ] Registry: `ancestors_of_layer` walks `layer.parent` up to window root\n-- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones filtered by `layer_key`\n-- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+- [ ] Registry: `scopes_in_layer` filters by `layer_key`, returns both Focusables and Zones in that layer\n+- [ ] Registry: unregister removes; subsequent `scope(key)` returns None\n+- [ ] Registry: two windows + 2 inspector layers + 1 dialog layer = 5 layers, 2 roots, correct parent chains\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KPZWPDAY4NS5YKVPBZ21RBQ2","timestamp":"2026-04-24T14:00:58.974450Z","entity_type":"task","entity_id":"01KNQXW7HHHB8HW76K3PXH3G34","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -11,53 +11,86 @@\n | Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n | Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n\n-On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum delegate to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`, etc.).\n+On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum dispatch to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`).\n\n On React: `FocusScope` is the high-level convenience wrapper that internally uses a `Focusable` or `FocusZone` primitive and adds entity plumbing (CommandScopeProvider, click-to-focus, context menu, focus bar, data-moniker attrs).\n\n-### Newtypes\n+### Newtype discipline — no bare primitives on type signatures\n\n-Shared with card `01KNM3YHHFJ3...`:\n+**All identity and measurement fields are newtypes.** No `String`, no `f64` at the boundaries of the public types, no exceptions. Wire format stays plain via `#[serde(transparent)]`.\n\n ```rust\n #[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct WindowLabel(pub String);\n-#[serde(transparent)] pub struct SpatialKey(pub String);\n-#[serde(transparent)] pub struct LayerKey(pub String);\n+#[serde(transparent)] pub struct WindowLabel(pub String);   // Tauri window label\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct SpatialKey(pub String);    // ULID per scope mount\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct LayerKey(pub String);      // ULID per layer mount\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct Moniker(pub String);       // entity identity: \"task:01ABC\", \"ui:toolbar.new\"\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct LayerName(pub String);     // layer role: \"window\", \"inspector\", \"dialog\", \"palette\"\n+\n+#[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct Pixels(pub f64);           // screen coordinate / size in CSS pixels\n+```\n+\n+`Pixels` implements arithmetic (`Add`, `Sub`, `Mul<f64>`, `Div<f64>`) so the nav algorithm's beam/score math stays ergonomic without drifting to `f64`.\n+\n+### Rect\n+\n+```rust\n+#[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n+pub struct Rect {\n+    pub x: Pixels,\n+    pub y: Pixels,\n+    pub width: Pixels,\n+    pub height: Pixels,\n+}\n+\n+impl Rect {\n+    pub fn top(&self)    -> Pixels { self.y }\n+    pub fn left(&self)   -> Pixels { self.x }\n+    pub fn bottom(&self) -> Pixels { Pixels(self.y.0 + self.height.0) }\n+    pub fn right(&self)  -> Pixels { Pixels(self.x.0 + self.width.0) }\n+}\n ```\n\n-### Rust types\n+### The four types\n\n ```rust\n-/// Leaf — a single focusable point (task title, pill, button, menu item).\n+/// Leaf — a single focusable point.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct Focusable {\n     pub key: SpatialKey,\n-    pub moniker: String,\n+    pub moniker: Moniker,\n     pub rect: Rect,\n     pub layer_key: LayerKey,\n     pub parent_zone: Option<SpatialKey>,\n-    pub overrides: HashMap<Direction, Option<String>>,\n+    pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n-/// Zone — a navigable container (card, column, inspector panel, field row,\n-/// toolbar group, nav bar). Has its own focus memory for drill-out / fallback.\n+/// Zone — a navigable container with focus memory.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusZone {\n     pub key: SpatialKey,\n-    pub moniker: String,\n+    pub moniker: Moniker,\n     pub rect: Rect,\n     pub layer_key: LayerKey,\n     pub parent_zone: Option<SpatialKey>,\n     pub last_focused: Option<SpatialKey>,\n-    pub overrides: HashMap<Direction, Option<String>>,\n+    pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n-/// Layer — a hard modal boundary. Window roots, inspector overlay, dialogs.\n+/// Layer — a hard modal boundary.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusLayer {\n     pub key: LayerKey,\n-    pub name: String,\n+    pub name: LayerName,\n     pub parent: Option<LayerKey>,\n     pub window_label: WindowLabel,\n     pub last_focused: Option<SpatialKey>,\n@@ -72,16 +105,16 @@\n }\n\n impl FocusScope {\n-    pub fn key(&self) -> &SpatialKey { /* dispatch */ }\n-    pub fn moniker(&self) -> &str { /* dispatch */ }\n-    pub fn rect(&self) -> &Rect { /* dispatch */ }\n-    pub fn layer_key(&self) -> &LayerKey { /* dispatch */ }\n-    pub fn parent_zone(&self) -> Option<&SpatialKey> { /* dispatch */ }\n-    pub fn overrides(&self) -> &HashMap<Direction, Option<String>> { /* dispatch */ }\n-    pub fn is_zone(&self) -> bool { matches!(self, Self::Zone(_)) }\n-    pub fn is_focusable(&self) -> bool { matches!(self, Self::Focusable(_)) }\n-    pub fn as_zone(&self) -> Option<&FocusZone> { /* ... */ }\n-    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone> { /* ... */ }\n+    pub fn key(&self) -> &SpatialKey;\n+    pub fn moniker(&self) -> &Moniker;\n+    pub fn rect(&self) -> &Rect;\n+    pub fn layer_key(&self) -> &LayerKey;\n+    pub fn parent_zone(&self) -> Option<&SpatialKey>;\n+    pub fn overrides(&self) -> &HashMap<Direction, Option<Moniker>>;\n+    pub fn is_zone(&self) -> bool;\n+    pub fn is_focusable(&self) -> bool;\n+    pub fn as_zone(&self) -> Option<&FocusZone>;\n+    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone>;\n }\n ```\n\n@@ -94,7 +127,6 @@\n }\n\n impl SpatialRegistry {\n-    // Scope ops\n     pub fn register_focusable(&mut self, f: Focusable);\n     pub fn register_zone(&mut self, z: FocusZone);\n     pub fn unregister_scope(&mut self, key: &SpatialKey);\n@@ -103,7 +135,6 @@\n     pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = &FocusScope>;\n     pub fn ancestor_zones(&self, key: &SpatialKey) -> Vec<&FocusZone>;\n\n-    // Layer ops\n     pub fn push_layer(&mut self, l: FocusLayer);\n     pub fn remove_layer(&mut self, key: &LayerKey);\n     pub fn layer(&self, key: &LayerKey) -> Option<&FocusLayer>;\n@@ -111,7 +142,6 @@\n     pub fn root_for_window(&self, label: &WindowLabel) -> Option<&FocusLayer>;\n     pub fn ancestors_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n\n-    // Nav scoping\n     pub fn scopes_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n }\n ```\n@@ -118,74 +148,93 @@\n\n ### Tauri commands\n\n-One command per primitive registration op; a single unregister op (since React always knows the key it generated):\n+Every command signature uses newtypes, not `String`/`f64`:\n\n ```rust\n #[tauri::command]\n-async fn spatial_register_focusable(window: tauri::Window, ...) -> Result<()>;\n+async fn spatial_register_focusable(\n+    window: tauri::Window,\n+    key: SpatialKey,\n+    moniker: Moniker,\n+    rect: Rect,\n+    layer_key: LayerKey,\n+    parent_zone: Option<SpatialKey>,\n+    overrides: HashMap<Direction, Option<Moniker>>,\n+) -> Result<()>;\n+\n #[tauri::command]\n-async fn spatial_register_zone(window: tauri::Window, ...) -> Result<()>;\n+async fn spatial_register_zone(\n+    window: tauri::Window,\n+    key: SpatialKey,\n+    moniker: Moniker,\n+    rect: Rect,\n+    layer_key: LayerKey,\n+    parent_zone: Option<SpatialKey>,\n+    overrides: HashMap<Direction, Option<Moniker>>,\n+) -> Result<()>;\n+\n #[tauri::command]\n async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n #[tauri::command]\n async fn spatial_update_rect(window: tauri::Window, key: SpatialKey, rect: Rect) -> Result<()>;\n #[tauri::command]\n-async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: String, parent: Option<LayerKey>) -> Result<()>;\n+async fn spatial_push_layer(\n+    window: tauri::Window,\n+    key: LayerKey,\n+    name: LayerName,\n+    parent: Option<LayerKey>,\n+) -> Result<()>;\n #[tauri::command]\n async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n\n-All derive `window_label` from the `tauri::Window` parameter. React never passes the window label.\n+`window_label: WindowLabel` is derived from the `tauri::Window` parameter inside the handler, never passed from React.\n\n ### Serde wire format\n\n-`#[serde(tag = \"kind\", rename_all = \"snake_case\")]` on `FocusScope` produces:\n-\n-```json\n-{ \"kind\": \"focusable\", \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": null, \"overrides\": {} }\n-{ \"kind\": \"zone\",      \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": \"...\", \"last_focused\": null, \"overrides\": {} }\n-```\n-\n-Newtypes (`SpatialKey`, `LayerKey`, `WindowLabel`) are `#[serde(transparent)]` → plain strings over the wire.\n+All newtypes are `#[serde(transparent)]` → JSON strings/numbers. The `FocusScope` enum serializes with `\"kind\": \"focusable\" | \"zone\"` as a discriminator tag.\n\n ### Design decisions\n\n-- **Four peer types, not a `kind` enum on one struct**: makes zone-only fields (`last_focused`) type-checked — a `Focusable` literally cannot have a `last_focused` field to forget about.\n-- **`FocusScope` as a Rust enum**: a single stored type per `SpatialKey`, pattern matching gives the nav algorithm a clean API.\n-- **Registry stores the enum, not a trait object**: zero dynamic dispatch; serde works out of the box with `#[serde(tag = \"kind\")]`.\n-- **Overrides on both variants**: overrides apply to anything focusable. Cheap empty HashMap default.\n-- **`last_focused` only on `FocusZone`**: only zones need drill-in / fallback memory.\n+- **Newtypes throughout, not just on identity**: measurements (`Pixels`) are newtyped too. Prevents accidental mixing of pixel values with unrelated floats (e.g., scoring multipliers, timestamps) and makes future unit changes (device pixels vs CSS pixels) explicit.\n+- **Override target is `Moniker`, not `String`**: matches the setFocus target type; prevents accidentally stuffing a `SpatialKey` where a moniker goes.\n+- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked; a `Focusable` cannot carry one.\n+- **`FocusScope` as a Rust enum**: one map, pattern matching in nav. No trait objects.\n+- **`LayerName` as a newtype, not an enum**: layer names grow organically (window, inspector, dialog, palette, quick-capture, future things). Newtype keeps the string bounded to the slot without over-constraining to a fixed set.\n\n ### Subtasks\n-- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes\n-- [ ] Define `Rect`, `Direction`\n-- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum) with serde attrs\n-- [ ] Implement `FocusScope` helper methods (`key`, `moniker`, `rect`, `layer_key`, `parent_zone`, `overrides`, `is_zone`, `as_zone*`)\n-- [ ] Implement `SpatialRegistry` with scope + layer ops above\n-- [ ] Implement forest ops for layers (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n-- [ ] Implement zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [ ] Add Tauri commands per primitive (register_focusable, register_zone, unregister_scope, update_rect, push_layer, pop_layer); each derives `window_label` from `tauri::Window`\n+- [ ] Define newtypes: `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName`, `Pixels`\n+- [ ] Impl `Add`/`Sub`/`Mul<f64>`/`Div<f64>` on `Pixels`\n+- [ ] Define `Rect` with `Pixels` fields + accessor helpers\n+- [ ] Define `Direction` enum\n+- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` enum with serde attrs\n+- [ ] Implement `FocusScope` helper methods\n+- [ ] Implement `SpatialRegistry` with scope + layer ops\n+- [ ] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n+- [ ] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n+- [ ] Tauri commands with fully-typed signatures (no `String`/`f64` on command params)\n\n ## Acceptance Criteria\n-- [ ] Four peer types exist in Rust: `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum)\n-- [ ] `FocusScope` enum dispatches helper accessors to its variant\n-- [ ] `Focusable` has no `last_focused` field; `FocusZone` does\n-- [ ] All newtypes are `#[serde(transparent)]`; enum is `#[serde(tag = \"kind\")]`\n-- [ ] `SpatialRegistry` can hold any mix of focusables, zones, and layers; well-formed across windows + nested zones\n+- [ ] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n+- [ ] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [ ] All newtypes are `#[serde(transparent)]`; the enum is `#[serde(tag = \"kind\")]`\n+- [ ] `Focusable` has no `last_focused` field; only `FocusZone` does\n+- [ ] `Pixels` supports arithmetic without requiring `.0` access in the nav algorithm\n+- [ ] `SpatialRegistry` holds any mix of focusables, zones, and layers across windows + nested zones\n - [ ] Tauri commands derive `window_label` from `tauri::Window`\n - [ ] `cargo test` passes\n\n ## Tests (pure Rust)\n-- [ ] `Focusable` and `FocusZone` structs have the fields listed above; serde JSON includes `\"kind\"` tag\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde\n+- [ ] Each newtype is `#[serde(transparent)]` — JSON round-trips as a bare primitive\n+- [ ] `Pixels` arithmetic produces a `Pixels` result (type-checked)\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde with `\"kind\"` tag\n - [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` returns direct children only (not transitive descendants)\n+- [ ] Registry: `children_of_zone` direct children only (not transitive)\n - [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` returns layers with `parent == this`, not layers in other windows\n+- [ ] Registry: `children_of_layer` returns layers with matching `parent`, not cross-window\n - [ ] Registry: `ancestors_of_layer` walks `layer.parent` up to window root\n-- [ ] Registry: `scopes_in_layer` filters by `layer_key`, returns both Focusables and Zones in that layer\n-- [ ] Registry: unregister removes; subsequent `scope(key)` returns None\n-- [ ] Registry: two windows + 2 inspector layers + 1 dialog layer = 5 layers, 2 roots, correct parent chains\n+- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones filtered by `layer_key`\n+- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -11,86 +11,53 @@\n | Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n | Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n\n-On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum dispatch to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`).\n+On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum delegate to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`, etc.).\n\n On React: `FocusScope` is the high-level convenience wrapper that internally uses a `Focusable` or `FocusZone` primitive and adds entity plumbing (CommandScopeProvider, click-to-focus, context menu, focus bar, data-moniker attrs).\n\n-### Newtype discipline — no bare primitives on type signatures\n+### Newtypes\n\n-**All identity and measurement fields are newtypes.** No `String`, no `f64` at the boundaries of the public types, no exceptions. Wire format stays plain via `#[serde(transparent)]`.\n+Shared with card `01KNM3YHHFJ3...`:\n\n ```rust\n #[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct WindowLabel(pub String);   // Tauri window label\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct SpatialKey(pub String);    // ULID per scope mount\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct LayerKey(pub String);      // ULID per layer mount\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct Moniker(pub String);       // entity identity: \"task:01ABC\", \"ui:toolbar.new\"\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct LayerName(pub String);     // layer role: \"window\", \"inspector\", \"dialog\", \"palette\"\n-\n-#[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct Pixels(pub f64);           // screen coordinate / size in CSS pixels\n+#[serde(transparent)] pub struct WindowLabel(pub String);\n+#[serde(transparent)] pub struct SpatialKey(pub String);\n+#[serde(transparent)] pub struct LayerKey(pub String);\n ```\n\n-`Pixels` implements arithmetic (`Add`, `Sub`, `Mul<f64>`, `Div<f64>`) so the nav algorithm's beam/score math stays ergonomic without drifting to `f64`.\n-\n-### Rect\n+### Rust types\n\n ```rust\n-#[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n-pub struct Rect {\n-    pub x: Pixels,\n-    pub y: Pixels,\n-    pub width: Pixels,\n-    pub height: Pixels,\n-}\n-\n-impl Rect {\n-    pub fn top(&self)    -> Pixels { self.y }\n-    pub fn left(&self)   -> Pixels { self.x }\n-    pub fn bottom(&self) -> Pixels { Pixels(self.y.0 + self.height.0) }\n-    pub fn right(&self)  -> Pixels { Pixels(self.x.0 + self.width.0) }\n-}\n-```\n-\n-### The four types\n-\n-```rust\n-/// Leaf — a single focusable point.\n+/// Leaf — a single focusable point (task title, pill, button, menu item).\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct Focusable {\n     pub key: SpatialKey,\n-    pub moniker: Moniker,\n+    pub moniker: String,\n     pub rect: Rect,\n     pub layer_key: LayerKey,\n     pub parent_zone: Option<SpatialKey>,\n-    pub overrides: HashMap<Direction, Option<Moniker>>,\n+    pub overrides: HashMap<Direction, Option<String>>,\n }\n\n-/// Zone — a navigable container with focus memory.\n+/// Zone — a navigable container (card, column, inspector panel, field row,\n+/// toolbar group, nav bar). Has its own focus memory for drill-out / fallback.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusZone {\n     pub key: SpatialKey,\n-    pub moniker: Moniker,\n+    pub moniker: String,\n     pub rect: Rect,\n     pub layer_key: LayerKey,\n     pub parent_zone: Option<SpatialKey>,\n     pub last_focused: Option<SpatialKey>,\n-    pub overrides: HashMap<Direction, Option<Moniker>>,\n+    pub overrides: HashMap<Direction, Option<String>>,\n }\n\n-/// Layer — a hard modal boundary.\n+/// Layer — a hard modal boundary. Window roots, inspector overlay, dialogs.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusLayer {\n     pub key: LayerKey,\n-    pub name: LayerName,\n+    pub name: String,\n     pub parent: Option<LayerKey>,\n     pub window_label: WindowLabel,\n     pub last_focused: Option<SpatialKey>,\n@@ -105,16 +72,16 @@\n }\n\n impl FocusScope {\n-    pub fn key(&self) -> &SpatialKey;\n-    pub fn moniker(&self) -> &Moniker;\n-    pub fn rect(&self) -> &Rect;\n-    pub fn layer_key(&self) -> &LayerKey;\n-    pub fn parent_zone(&self) -> Option<&SpatialKey>;\n-    pub fn overrides(&self) -> &HashMap<Direction, Option<Moniker>>;\n-    pub fn is_zone(&self) -> bool;\n-    pub fn is_focusable(&self) -> bool;\n-    pub fn as_zone(&self) -> Option<&FocusZone>;\n-    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone>;\n+    pub fn key(&self) -> &SpatialKey { /* dispatch */ }\n+    pub fn moniker(&self) -> &str { /* dispatch */ }\n+    pub fn rect(&self) -> &Rect { /* dispatch */ }\n+    pub fn layer_key(&self) -> &LayerKey { /* dispatch */ }\n+    pub fn parent_zone(&self) -> Option<&SpatialKey> { /* dispatch */ }\n+    pub fn overrides(&self) -> &HashMap<Direction, Option<String>> { /* dispatch */ }\n+    pub fn is_zone(&self) -> bool { matches!(self, Self::Zone(_)) }\n+    pub fn is_focusable(&self) -> bool { matches!(self, Self::Focusable(_)) }\n+    pub fn as_zone(&self) -> Option<&FocusZone> { /* ... */ }\n+    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone> { /* ... */ }\n }\n ```\n\n@@ -127,6 +94,7 @@\n }\n\n impl SpatialRegistry {\n+    // Scope ops\n     pub fn register_focusable(&mut self, f: Focusable);\n     pub fn register_zone(&mut self, z: FocusZone);\n     pub fn unregister_scope(&mut self, key: &SpatialKey);\n@@ -135,6 +103,7 @@\n     pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = &FocusScope>;\n     pub fn ancestor_zones(&self, key: &SpatialKey) -> Vec<&FocusZone>;\n\n+    // Layer ops\n     pub fn push_layer(&mut self, l: FocusLayer);\n     pub fn remove_layer(&mut self, key: &LayerKey);\n     pub fn layer(&self, key: &LayerKey) -> Option<&FocusLayer>;\n@@ -142,6 +111,7 @@\n     pub fn root_for_window(&self, label: &WindowLabel) -> Option<&FocusLayer>;\n     pub fn ancestors_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n\n+    // Nav scoping\n     pub fn scopes_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n }\n ```\n@@ -148,93 +118,74 @@\n\n ### Tauri commands\n\n-Every command signature uses newtypes, not `String`/`f64`:\n+One command per primitive registration op; a single unregister op (since React always knows the key it generated):\n\n ```rust\n #[tauri::command]\n-async fn spatial_register_focusable(\n-    window: tauri::Window,\n-    key: SpatialKey,\n-    moniker: Moniker,\n-    rect: Rect,\n-    layer_key: LayerKey,\n-    parent_zone: Option<SpatialKey>,\n-    overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<()>;\n-\n+async fn spatial_register_focusable(window: tauri::Window, ...) -> Result<()>;\n #[tauri::command]\n-async fn spatial_register_zone(\n-    window: tauri::Window,\n-    key: SpatialKey,\n-    moniker: Moniker,\n-    rect: Rect,\n-    layer_key: LayerKey,\n-    parent_zone: Option<SpatialKey>,\n-    overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<()>;\n-\n+async fn spatial_register_zone(window: tauri::Window, ...) -> Result<()>;\n #[tauri::command]\n async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n #[tauri::command]\n async fn spatial_update_rect(window: tauri::Window, key: SpatialKey, rect: Rect) -> Result<()>;\n #[tauri::command]\n-async fn spatial_push_layer(\n-    window: tauri::Window,\n-    key: LayerKey,\n-    name: LayerName,\n-    parent: Option<LayerKey>,\n-) -> Result<()>;\n+async fn spatial_push_layer(window: tauri::Window, key: LayerKey, name: String, parent: Option<LayerKey>) -> Result<()>;\n #[tauri::command]\n async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n\n-`window_label: WindowLabel` is derived from the `tauri::Window` parameter inside the handler, never passed from React.\n+All derive `window_label` from the `tauri::Window` parameter. React never passes the window label.\n\n ### Serde wire format\n\n-All newtypes are `#[serde(transparent)]` → JSON strings/numbers. The `FocusScope` enum serializes with `\"kind\": \"focusable\" | \"zone\"` as a discriminator tag.\n+`#[serde(tag = \"kind\", rename_all = \"snake_case\")]` on `FocusScope` produces:\n\n+```json\n+{ \"kind\": \"focusable\", \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": null, \"overrides\": {} }\n+{ \"kind\": \"zone\",      \"key\": \"01H...\", \"moniker\": \"...\", \"rect\": {...}, \"layer_key\": \"...\", \"parent_zone\": \"...\", \"last_focused\": null, \"overrides\": {} }\n+```\n+\n+Newtypes (`SpatialKey`, `LayerKey`, `WindowLabel`) are `#[serde(transparent)]` → plain strings over the wire.\n+\n ### Design decisions\n\n-- **Newtypes throughout, not just on identity**: measurements (`Pixels`) are newtyped too. Prevents accidental mixing of pixel values with unrelated floats (e.g., scoring multipliers, timestamps) and makes future unit changes (device pixels vs CSS pixels) explicit.\n-- **Override target is `Moniker`, not `String`**: matches the setFocus target type; prevents accidentally stuffing a `SpatialKey` where a moniker goes.\n-- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked; a `Focusable` cannot carry one.\n-- **`FocusScope` as a Rust enum**: one map, pattern matching in nav. No trait objects.\n-- **`LayerName` as a newtype, not an enum**: layer names grow organically (window, inspector, dialog, palette, quick-capture, future things). Newtype keeps the string bounded to the slot without over-constraining to a fixed set.\n+- **Four peer types, not a `kind` enum on one struct**: makes zone-only fields (`last_focused`) type-checked — a `Focusable` literally cannot have a `last_focused` field to forget about.\n+- **`FocusScope` as a Rust enum**: a single stored type per `SpatialKey`, pattern matching gives the nav algorithm a clean API.\n+- **Registry stores the enum, not a trait object**: zero dynamic dispatch; serde works out of the box with `#[serde(tag = \"kind\")]`.\n+- **Overrides on both variants**: overrides apply to anything focusable. Cheap empty HashMap default.\n+- **`last_focused` only on `FocusZone`**: only zones need drill-in / fallback memory.\n\n ### Subtasks\n-- [ ] Define newtypes: `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName`, `Pixels`\n-- [ ] Impl `Add`/`Sub`/`Mul<f64>`/`Div<f64>` on `Pixels`\n-- [ ] Define `Rect` with `Pixels` fields + accessor helpers\n-- [ ] Define `Direction` enum\n-- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` enum with serde attrs\n-- [ ] Implement `FocusScope` helper methods\n-- [ ] Implement `SpatialRegistry` with scope + layer ops\n-- [ ] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n-- [ ] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [ ] Tauri commands with fully-typed signatures (no `String`/`f64` on command params)\n+- [ ] Define `WindowLabel`, `SpatialKey`, `LayerKey` newtypes\n+- [ ] Define `Rect`, `Direction`\n+- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum) with serde attrs\n+- [ ] Implement `FocusScope` helper methods (`key`, `moniker`, `rect`, `layer_key`, `parent_zone`, `overrides`, `is_zone`, `as_zone*`)\n+- [ ] Implement `SpatialRegistry` with scope + layer ops above\n+- [ ] Implement forest ops for layers (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n+- [ ] Implement zone tree ops (`children_of_zone`, `ancestor_zones`)\n+- [ ] Add Tauri commands per primitive (register_focusable, register_zone, unregister_scope, update_rect, push_layer, pop_layer); each derives `window_label` from `tauri::Window`\n\n ## Acceptance Criteria\n-- [ ] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n-- [ ] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [ ] All newtypes are `#[serde(transparent)]`; the enum is `#[serde(tag = \"kind\")]`\n-- [ ] `Focusable` has no `last_focused` field; only `FocusZone` does\n-- [ ] `Pixels` supports arithmetic without requiring `.0` access in the nav algorithm\n-- [ ] `SpatialRegistry` holds any mix of focusables, zones, and layers across windows + nested zones\n+- [ ] Four peer types exist in Rust: `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` (enum)\n+- [ ] `FocusScope` enum dispatches helper accessors to its variant\n+- [ ] `Focusable` has no `last_focused` field; `FocusZone` does\n+- [ ] All newtypes are `#[serde(transparent)]`; enum is `#[serde(tag = \"kind\")]`\n+- [ ] `SpatialRegistry` can hold any mix of focusables, zones, and layers; well-formed across windows + nested zones\n - [ ] Tauri commands derive `window_label` from `tauri::Window`\n - [ ] `cargo test` passes\n\n ## Tests (pure Rust)\n-- [ ] Each newtype is `#[serde(transparent)]` — JSON round-trips as a bare primitive\n-- [ ] `Pixels` arithmetic produces a `Pixels` result (type-checked)\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde with `\"kind\"` tag\n+- [ ] `Focusable` and `FocusZone` structs have the fields listed above; serde JSON includes `\"kind\"` tag\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde\n - [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` direct children only (not transitive)\n+- [ ] Registry: `children_of_zone` returns direct children only (not transitive descendants)\n - [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` returns layers with matching `parent`, not cross-window\n+- [ ] Registry: `children_of_layer` returns layers with `parent == this`, not layers in other windows\n - [ ] Registry: `ancestors_of_layer` walks `layer.parent` up to window root\n-- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones filtered by `layer_key`\n-- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+- [ ] Registry: `scopes_in_layer` filters by `layer_key`, returns both Focusables and Zones in that layer\n+- [ ] Registry: unregister removes; subsequent `scope(key)` returns None\n+- [ ] Registry: two windows + 2 inspector layers + 1 dialog layer = 5 layers, 2 roots, correct parent chains\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ025KNTPS4290QG42HAM0BJ","timestamp":"2026-04-24T15:36:39.866081Z","op":"update","item_id":"01KNQXW7HHHB8HW76K3PXH3G34","forward_patch":"--- original\n+++ modified\n@@ -12,6 +12,26 @@\n\n Define the Rust kernel types for spatial focus. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n+### Crate placement\n+\n+Following the pattern established in commit `b81336d42` (move GUI-crate logic into headless-testable kanban crate), **this lives in `swissarmyhammer-kanban`**, not `swissarmyhammer-commands`. Rationale: spatial nav is business logic that must be testable without Tauri or jsdom; `swissarmyhammer-commands` stays Tier 0 (no app state, no windowing concepts).\n+\n+Specifically, organize as a submodule alongside the existing `focus.rs`:\n+\n+```\n+swissarmyhammer-kanban/src/focus/\n+  mod.rs          (re-exports; existing resolve_focused_column migrates here)\n+  column.rs       (existing resolve_focused_column logic)\n+  types.rs        (newtypes: WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels, Rect, Direction)\n+  scope.rs        (Focusable, FocusZone, FocusScope enum)\n+  layer.rs        (FocusLayer struct)\n+  registry.rs     (SpatialRegistry with scope + layer ops)\n+  state.rs        (SpatialState with focus_by_window, event emission)\n+  navigate.rs     (beam search algorithm — separate card 01KNQXXF5W...)\n+```\n+\n+Migration of `focus.rs` → `focus/column.rs` is part of this card's scope; keep the public re-export path (`swissarmyhammer_kanban::focus::resolve_focused_column`) stable.\n+\n ### The four peer types\n\n | Role                | React component     | Rust type             |\n@@ -21,35 +41,37 @@\n | Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n | Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n\n-On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum dispatch to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`).\n+On Rust: `FocusScope` is the sum type `Focusable | FocusZone` stored in the registry per `SpatialKey`. On React: `FocusScope` is the entity-aware wrapper that composes `<Focusable>` or `<FocusZone>`.\n\n-On React: `FocusScope` is the high-level convenience wrapper that internally uses a `Focusable` or `FocusZone` primitive and adds entity plumbing (CommandScopeProvider, click-to-focus, context menu, focus bar, data-moniker attrs).\n+### Newtype discipline — use `swissarmyhammer_common::define_id!`\n\n-### Newtype discipline — no bare primitives on type signatures\n+The project already has a canonical newtype macro: `define_id!` from `swissarmyhammer-common`. It provides `#[serde(transparent)]`, `Display`, `AsRef<str>`, `From<&str>`/`From<String>`, `Deref`, `Borrow<str>`, `FromStr`, `PartialEq<str>`, plus `new()` (fresh ULID), `from_string()`, `as_str()`. All existing ID types (`TaskId`, `ColumnId`, `TagId`, etc.) use it.\n\n-**All identity and measurement fields are newtypes.** No `String`, no `f64` at the boundaries of the public types, no exceptions. Wire format stays plain via `#[serde(transparent)]`.\n+Define the string-valued newtypes with this macro:\n\n ```rust\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct WindowLabel(pub String);   // Tauri window label\n+use swissarmyhammer_common::define_id;\n\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct SpatialKey(pub String);    // ULID per scope mount\n+define_id!(WindowLabel, \"Tauri window label — which window a scope/layer lives in\");\n+define_id!(SpatialKey, \"ULID per FocusScope/FocusZone/Focusable instance\");\n+define_id!(LayerKey,   \"ULID per FocusLayer instance\");\n+define_id!(Moniker,    \"Entity focus identity: \\\"task:01ABC\\\", \\\"ui:toolbar.new\\\"\");\n+define_id!(LayerName,  \"Layer role: \\\"window\\\", \\\"inspector\\\", \\\"dialog\\\", \\\"palette\\\"\");\n+```\n\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct LayerKey(pub String);      // ULID per layer mount\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct Moniker(pub String);       // entity identity: \"task:01ABC\", \"ui:toolbar.new\"\n+`Pixels` is numeric, not string — hand-rolled:\n\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct LayerName(pub String);     // layer role: \"window\", \"inspector\", \"dialog\", \"palette\"\n-\n+```rust\n #[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct Pixels(pub f64);           // screen coordinate / size in CSS pixels\n-```\n+#[serde(transparent)]\n+pub struct Pixels(pub f64);\n\n-`Pixels` implements arithmetic (`Add`, `Sub`, `Mul<f64>`, `Div<f64>`) so the nav algorithm's beam/score math stays ergonomic without drifting to `f64`.\n+// Arithmetic so beam/score math doesn't drop to f64:\n+impl std::ops::Add for Pixels { /* ... */ }\n+impl std::ops::Sub for Pixels { /* ... */ }\n+impl std::ops::Mul<f64> for Pixels { /* ... */ }\n+impl std::ops::Div<f64> for Pixels { /* ... */ }\n+```\n\n ### Rect\n\n@@ -65,8 +87,8 @@\n impl Rect {\n     pub fn top(&self)    -> Pixels { self.y }\n     pub fn left(&self)   -> Pixels { self.x }\n-    pub fn bottom(&self) -> Pixels { Pixels(self.y.0 + self.height.0) }\n-    pub fn right(&self)  -> Pixels { Pixels(self.x.0 + self.width.0) }\n+    pub fn bottom(&self) -> Pixels { self.y + self.height }\n+    pub fn right(&self)  -> Pixels { self.x + self.width }\n }\n ```\n\n@@ -73,7 +95,6 @@\n ### The four types\n\n ```rust\n-/// Leaf — a single focusable point.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct Focusable {\n     pub key: SpatialKey,\n@@ -84,7 +105,6 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n-/// Zone — a navigable container with focus memory.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusZone {\n     pub key: SpatialKey,\n@@ -96,7 +116,6 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n-/// Layer — a hard modal boundary.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusLayer {\n     pub key: LayerKey,\n@@ -106,7 +125,6 @@\n     pub last_focused: Option<SpatialKey>,\n }\n\n-/// Sum type stored in the registry per SpatialKey.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n #[serde(tag = \"kind\", rename_all = \"snake_case\")]\n pub enum FocusScope {\n@@ -156,25 +174,15 @@\n }\n ```\n\n-### Tauri commands\n+### Tauri commands — in `kanban-app/src/commands.rs`\n\n-Every command signature uses newtypes, not `String`/`f64`:\n+The headless registry lives in `swissarmyhammer-kanban`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the kanban crate:\n\n ```rust\n #[tauri::command]\n-async fn spatial_register_focusable(\n-    window: tauri::Window,\n-    key: SpatialKey,\n-    moniker: Moniker,\n-    rect: Rect,\n-    layer_key: LayerKey,\n-    parent_zone: Option<SpatialKey>,\n-    overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<()>;\n-\n-#[tauri::command]\n-async fn spatial_register_zone(\n+pub async fn spatial_register_focusable(\n     window: tauri::Window,\n+    state: State<'_, AppState>,\n     key: SpatialKey,\n     moniker: Moniker,\n     rect: Rect,\n@@ -181,69 +189,56 @@\n     layer_key: LayerKey,\n     parent_zone: Option<SpatialKey>,\n     overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<()>;\n+) -> Result<(), String>;\n\n-#[tauri::command]\n-async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_update_rect(window: tauri::Window, key: SpatialKey, rect: Rect) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_push_layer(\n-    window: tauri::Window,\n-    key: LayerKey,\n-    name: LayerName,\n-    parent: Option<LayerKey>,\n-) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n+// Likewise: spatial_register_zone, spatial_unregister_scope, spatial_update_rect,\n+//           spatial_push_layer, spatial_pop_layer, spatial_focus, spatial_navigate,\n+//           spatial_drill_in, spatial_drill_out\n ```\n\n-`window_label: WindowLabel` is derived from the `tauri::Window` parameter inside the handler, never passed from React.\n+### Tests — `swissarmyhammer-kanban/tests/focus_registry.rs`\n\n-### Serde wire format\n-\n-All newtypes are `#[serde(transparent)]` → JSON strings/numbers. The `FocusScope` enum serializes with `\"kind\": \"focusable\" | \"zone\"` as a discriminator tag.\n+Follow the pattern of `swissarmyhammer-kanban/tests/resolve_focused_column.rs` and `dynamic_sources_headless.rs` — pure-Rust, no Tauri, no jsdom. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n\n ### Design decisions\n\n-- **Newtypes throughout, not just on identity**: measurements (`Pixels`) are newtyped too. Prevents accidental mixing of pixel values with unrelated floats (e.g., scoring multipliers, timestamps) and makes future unit changes (device pixels vs CSS pixels) explicit.\n-- **Override target is `Moniker`, not `String`**: matches the setFocus target type; prevents accidentally stuffing a `SpatialKey` where a moniker goes.\n-- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked; a `Focusable` cannot carry one.\n-- **`FocusScope` as a Rust enum**: one map, pattern matching in nav. No trait objects.\n-- **`LayerName` as a newtype, not an enum**: layer names grow organically (window, inspector, dialog, palette, quick-capture, future things). Newtype keeps the string bounded to the slot without over-constraining to a fixed set.\n+- **`swissarmyhammer-kanban` not `swissarmyhammer-commands`**: matches the refactor pattern — business logic over Tier 0.\n+- **`define_id!` macro reuse**: consistent with `TaskId`/`ColumnId`/`TagId`; avoids hand-rolled boilerplate.\n+- **`focus/` submodule**: groups all focus concerns (column resolver + spatial registry) together.\n+- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked.\n+- **`FocusScope` as a Rust enum**: one map, pattern matching.\n\n ### Subtasks\n-- [ ] Define newtypes: `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName`, `Pixels`\n-- [ ] Impl `Add`/`Sub`/`Mul<f64>`/`Div<f64>` on `Pixels`\n-- [ ] Define `Rect` with `Pixels` fields + accessor helpers\n-- [ ] Define `Direction` enum\n-- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` enum with serde attrs\n-- [ ] Implement `FocusScope` helper methods\n-- [ ] Implement `SpatialRegistry` with scope + layer ops\n+- [ ] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n+- [ ] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n+- [ ] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n+- [ ] `focus/layer.rs`: define `FocusLayer`\n+- [ ] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n - [ ] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n - [ ] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [ ] Tauri commands with fully-typed signatures (no `String`/`f64` on command params)\n+- [ ] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n+- [ ] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n\n ## Acceptance Criteria\n+- [ ] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n+- [ ] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n - [ ] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n - [ ] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [ ] All newtypes are `#[serde(transparent)]`; the enum is `#[serde(tag = \"kind\")]`\n-- [ ] `Focusable` has no `last_focused` field; only `FocusZone` does\n-- [ ] `Pixels` supports arithmetic without requiring `.0` access in the nav algorithm\n-- [ ] `SpatialRegistry` holds any mix of focusables, zones, and layers across windows + nested zones\n-- [ ] Tauri commands derive `window_label` from `tauri::Window`\n-- [ ] `cargo test` passes\n+- [ ] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n+- [ ] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n+- [ ] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n+- [ ] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n\n-## Tests (pure Rust)\n-- [ ] Each newtype is `#[serde(transparent)]` — JSON round-trips as a bare primitive\n-- [ ] `Pixels` arithmetic produces a `Pixels` result (type-checked)\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde with `\"kind\"` tag\n+## Tests (pure Rust, in `swissarmyhammer-kanban/tests/focus_registry.rs`)\n+- [ ] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n+- [ ] `Pixels` arithmetic is type-preserving\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n - [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` direct children only (not transitive)\n+- [ ] Registry: `children_of_zone` direct children only\n - [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` returns layers with matching `parent`, not cross-window\n-- [ ] Registry: `ancestors_of_layer` walks `layer.parent` up to window root\n-- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones filtered by `layer_key`\n+- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n - [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -12,26 +12,6 @@\n\n Define the Rust kernel types for spatial focus. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n-### Crate placement\n-\n-Following the pattern established in commit `b81336d42` (move GUI-crate logic into headless-testable kanban crate), **this lives in `swissarmyhammer-kanban`**, not `swissarmyhammer-commands`. Rationale: spatial nav is business logic that must be testable without Tauri or jsdom; `swissarmyhammer-commands` stays Tier 0 (no app state, no windowing concepts).\n-\n-Specifically, organize as a submodule alongside the existing `focus.rs`:\n-\n-```\n-swissarmyhammer-kanban/src/focus/\n-  mod.rs          (re-exports; existing resolve_focused_column migrates here)\n-  column.rs       (existing resolve_focused_column logic)\n-  types.rs        (newtypes: WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels, Rect, Direction)\n-  scope.rs        (Focusable, FocusZone, FocusScope enum)\n-  layer.rs        (FocusLayer struct)\n-  registry.rs     (SpatialRegistry with scope + layer ops)\n-  state.rs        (SpatialState with focus_by_window, event emission)\n-  navigate.rs     (beam search algorithm — separate card 01KNQXXF5W...)\n-```\n-\n-Migration of `focus.rs` → `focus/column.rs` is part of this card's scope; keep the public re-export path (`swissarmyhammer_kanban::focus::resolve_focused_column`) stable.\n-\n ### The four peer types\n\n | Role                | React component     | Rust type             |\n@@ -41,38 +21,36 @@\n | Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n | Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n\n-On Rust: `FocusScope` is the sum type `Focusable | FocusZone` stored in the registry per `SpatialKey`. On React: `FocusScope` is the entity-aware wrapper that composes `<Focusable>` or `<FocusZone>`.\n+On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum dispatch to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`).\n\n-### Newtype discipline — use `swissarmyhammer_common::define_id!`\n+On React: `FocusScope` is the high-level convenience wrapper that internally uses a `Focusable` or `FocusZone` primitive and adds entity plumbing (CommandScopeProvider, click-to-focus, context menu, focus bar, data-moniker attrs).\n\n-The project already has a canonical newtype macro: `define_id!` from `swissarmyhammer-common`. It provides `#[serde(transparent)]`, `Display`, `AsRef<str>`, `From<&str>`/`From<String>`, `Deref`, `Borrow<str>`, `FromStr`, `PartialEq<str>`, plus `new()` (fresh ULID), `from_string()`, `as_str()`. All existing ID types (`TaskId`, `ColumnId`, `TagId`, etc.) use it.\n+### Newtype discipline — no bare primitives on type signatures\n\n-Define the string-valued newtypes with this macro:\n+**All identity and measurement fields are newtypes.** No `String`, no `f64` at the boundaries of the public types, no exceptions. Wire format stays plain via `#[serde(transparent)]`.\n\n ```rust\n-use swissarmyhammer_common::define_id;\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct WindowLabel(pub String);   // Tauri window label\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct SpatialKey(pub String);    // ULID per scope mount\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct LayerKey(pub String);      // ULID per layer mount\n\n-define_id!(WindowLabel, \"Tauri window label — which window a scope/layer lives in\");\n-define_id!(SpatialKey, \"ULID per FocusScope/FocusZone/Focusable instance\");\n-define_id!(LayerKey,   \"ULID per FocusLayer instance\");\n-define_id!(Moniker,    \"Entity focus identity: \\\"task:01ABC\\\", \\\"ui:toolbar.new\\\"\");\n-define_id!(LayerName,  \"Layer role: \\\"window\\\", \\\"inspector\\\", \\\"dialog\\\", \\\"palette\\\"\");\n-```\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct Moniker(pub String);       // entity identity: \"task:01ABC\", \"ui:toolbar.new\"\n\n-`Pixels` is numeric, not string — hand-rolled:\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct LayerName(pub String);     // layer role: \"window\", \"inspector\", \"dialog\", \"palette\"\n\n-```rust\n #[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct Pixels(pub f64);\n-\n-// Arithmetic so beam/score math doesn't drop to f64:\n-impl std::ops::Add for Pixels { /* ... */ }\n-impl std::ops::Sub for Pixels { /* ... */ }\n-impl std::ops::Mul<f64> for Pixels { /* ... */ }\n-impl std::ops::Div<f64> for Pixels { /* ... */ }\n+#[serde(transparent)] pub struct Pixels(pub f64);           // screen coordinate / size in CSS pixels\n ```\n\n+`Pixels` implements arithmetic (`Add`, `Sub`, `Mul<f64>`, `Div<f64>`) so the nav algorithm's beam/score math stays ergonomic without drifting to `f64`.\n+\n ### Rect\n\n ```rust\n@@ -87,8 +65,8 @@\n impl Rect {\n     pub fn top(&self)    -> Pixels { self.y }\n     pub fn left(&self)   -> Pixels { self.x }\n-    pub fn bottom(&self) -> Pixels { self.y + self.height }\n-    pub fn right(&self)  -> Pixels { self.x + self.width }\n+    pub fn bottom(&self) -> Pixels { Pixels(self.y.0 + self.height.0) }\n+    pub fn right(&self)  -> Pixels { Pixels(self.x.0 + self.width.0) }\n }\n ```\n\n@@ -95,6 +73,7 @@\n ### The four types\n\n ```rust\n+/// Leaf — a single focusable point.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct Focusable {\n     pub key: SpatialKey,\n@@ -105,6 +84,7 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n+/// Zone — a navigable container with focus memory.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusZone {\n     pub key: SpatialKey,\n@@ -116,6 +96,7 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n+/// Layer — a hard modal boundary.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusLayer {\n     pub key: LayerKey,\n@@ -125,6 +106,7 @@\n     pub last_focused: Option<SpatialKey>,\n }\n\n+/// Sum type stored in the registry per SpatialKey.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n #[serde(tag = \"kind\", rename_all = \"snake_case\")]\n pub enum FocusScope {\n@@ -174,15 +156,14 @@\n }\n ```\n\n-### Tauri commands — in `kanban-app/src/commands.rs`\n+### Tauri commands\n\n-The headless registry lives in `swissarmyhammer-kanban`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the kanban crate:\n+Every command signature uses newtypes, not `String`/`f64`:\n\n ```rust\n #[tauri::command]\n-pub async fn spatial_register_focusable(\n+async fn spatial_register_focusable(\n     window: tauri::Window,\n-    state: State<'_, AppState>,\n     key: SpatialKey,\n     moniker: Moniker,\n     rect: Rect,\n@@ -189,56 +170,80 @@\n     layer_key: LayerKey,\n     parent_zone: Option<SpatialKey>,\n     overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<(), String>;\n+) -> Result<()>;\n\n-// Likewise: spatial_register_zone, spatial_unregister_scope, spatial_update_rect,\n-//           spatial_push_layer, spatial_pop_layer, spatial_focus, spatial_navigate,\n-//           spatial_drill_in, spatial_drill_out\n+#[tauri::command]\n+async fn spatial_register_zone(\n+    window: tauri::Window,\n+    key: SpatialKey,\n+    moniker: Moniker,\n+    rect: Rect,\n+    layer_key: LayerKey,\n+    parent_zone: Option<SpatialKey>,\n+    overrides: HashMap<Direction, Option<Moniker>>,\n+) -> Result<()>;\n+\n+#[tauri::command]\n+async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_update_rect(window: tauri::Window, key: SpatialKey, rect: Rect) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_push_layer(\n+    window: tauri::Window,\n+    key: LayerKey,\n+    name: LayerName,\n+    parent: Option<LayerKey>,\n+) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n\n-### Tests — `swissarmyhammer-kanban/tests/focus_registry.rs`\n+`window_label: WindowLabel` is derived from the `tauri::Window` parameter inside the handler, never passed from React.\n+\n+### Serde wire format\n\n-Follow the pattern of `swissarmyhammer-kanban/tests/resolve_focused_column.rs` and `dynamic_sources_headless.rs` — pure-Rust, no Tauri, no jsdom. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n+All newtypes are `#[serde(transparent)]` → JSON strings/numbers. The `FocusScope` enum serializes with `\"kind\": \"focusable\" | \"zone\"` as a discriminator tag.\n\n ### Design decisions\n\n-- **`swissarmyhammer-kanban` not `swissarmyhammer-commands`**: matches the refactor pattern — business logic over Tier 0.\n-- **`define_id!` macro reuse**: consistent with `TaskId`/`ColumnId`/`TagId`; avoids hand-rolled boilerplate.\n-- **`focus/` submodule**: groups all focus concerns (column resolver + spatial registry) together.\n-- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked.\n-- **`FocusScope` as a Rust enum**: one map, pattern matching.\n+- **Newtypes throughout, not just on identity**: measurements (`Pixels`) are newtyped too. Prevents accidental mixing of pixel values with unrelated floats (e.g., scoring multipliers, timestamps) and makes future unit changes (device pixels vs CSS pixels) explicit.\n+- **Override target is `Moniker`, not `String`**: matches the setFocus target type; prevents accidentally stuffing a `SpatialKey` where a moniker goes.\n+- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked; a `Focusable` cannot carry one.\n+- **`FocusScope` as a Rust enum**: one map, pattern matching in nav. No trait objects.\n+- **`LayerName` as a newtype, not an enum**: layer names grow organically (window, inspector, dialog, palette, quick-capture, future things). Newtype keeps the string bounded to the slot without over-constraining to a fixed set.\n\n ### Subtasks\n-- [ ] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n-- [ ] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n-- [ ] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n-- [ ] `focus/layer.rs`: define `FocusLayer`\n-- [ ] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n+- [ ] Define newtypes: `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName`, `Pixels`\n+- [ ] Impl `Add`/`Sub`/`Mul<f64>`/`Div<f64>` on `Pixels`\n+- [ ] Define `Rect` with `Pixels` fields + accessor helpers\n+- [ ] Define `Direction` enum\n+- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` enum with serde attrs\n+- [ ] Implement `FocusScope` helper methods\n+- [ ] Implement `SpatialRegistry` with scope + layer ops\n - [ ] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n - [ ] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [ ] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n-- [ ] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n+- [ ] Tauri commands with fully-typed signatures (no `String`/`f64` on command params)\n\n ## Acceptance Criteria\n-- [ ] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n-- [ ] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n - [ ] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n - [ ] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [ ] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n-- [ ] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n-- [ ] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n-- [ ] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n+- [ ] All newtypes are `#[serde(transparent)]`; the enum is `#[serde(tag = \"kind\")]`\n+- [ ] `Focusable` has no `last_focused` field; only `FocusZone` does\n+- [ ] `Pixels` supports arithmetic without requiring `.0` access in the nav algorithm\n+- [ ] `SpatialRegistry` holds any mix of focusables, zones, and layers across windows + nested zones\n+- [ ] Tauri commands derive `window_label` from `tauri::Window`\n+- [ ] `cargo test` passes\n\n-## Tests (pure Rust, in `swissarmyhammer-kanban/tests/focus_registry.rs`)\n-- [ ] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n-- [ ] `Pixels` arithmetic is type-preserving\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+## Tests (pure Rust)\n+- [ ] Each newtype is `#[serde(transparent)]` — JSON round-trips as a bare primitive\n+- [ ] `Pixels` arithmetic produces a `Pixels` result (type-checked)\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde with `\"kind\"` tag\n - [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` direct children only\n+- [ ] Registry: `children_of_zone` direct children only (not transitive)\n - [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n+- [ ] Registry: `children_of_layer` returns layers with matching `parent`, not cross-window\n+- [ ] Registry: `ancestors_of_layer` walks `layer.parent` up to window root\n+- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones filtered by `layer_key`\n - [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n","transaction_id":null}
{"id":"01KQ025KNTFNV1KF1TQHD5WJFG","timestamp":"2026-04-24T15:36:39.866975Z","entity_type":"task","entity_id":"01KNQXW7HHHB8HW76K3PXH3G34","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -2,6 +2,26 @@\n\n Define the Rust kernel types for spatial focus. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n+### Crate placement\n+\n+Following the pattern established in commit `b81336d42` (move GUI-crate logic into headless-testable kanban crate), **this lives in `swissarmyhammer-kanban`**, not `swissarmyhammer-commands`. Rationale: spatial nav is business logic that must be testable without Tauri or jsdom; `swissarmyhammer-commands` stays Tier 0 (no app state, no windowing concepts).\n+\n+Specifically, organize as a submodule alongside the existing `focus.rs`:\n+\n+```\n+swissarmyhammer-kanban/src/focus/\n+  mod.rs          (re-exports; existing resolve_focused_column migrates here)\n+  column.rs       (existing resolve_focused_column logic)\n+  types.rs        (newtypes: WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels, Rect, Direction)\n+  scope.rs        (Focusable, FocusZone, FocusScope enum)\n+  layer.rs        (FocusLayer struct)\n+  registry.rs     (SpatialRegistry with scope + layer ops)\n+  state.rs        (SpatialState with focus_by_window, event emission)\n+  navigate.rs     (beam search algorithm — separate card 01KNQXXF5W...)\n+```\n+\n+Migration of `focus.rs` → `focus/column.rs` is part of this card's scope; keep the public re-export path (`swissarmyhammer_kanban::focus::resolve_focused_column`) stable.\n+\n ### The four peer types\n\n | Role                | React component     | Rust type             |\n@@ -11,35 +31,37 @@\n | Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n | Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n\n-On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum dispatch to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`).\n+On Rust: `FocusScope` is the sum type `Focusable | FocusZone` stored in the registry per `SpatialKey`. On React: `FocusScope` is the entity-aware wrapper that composes `<Focusable>` or `<FocusZone>`.\n\n-On React: `FocusScope` is the high-level convenience wrapper that internally uses a `Focusable` or `FocusZone` primitive and adds entity plumbing (CommandScopeProvider, click-to-focus, context menu, focus bar, data-moniker attrs).\n+### Newtype discipline — use `swissarmyhammer_common::define_id!`\n\n-### Newtype discipline — no bare primitives on type signatures\n+The project already has a canonical newtype macro: `define_id!` from `swissarmyhammer-common`. It provides `#[serde(transparent)]`, `Display`, `AsRef<str>`, `From<&str>`/`From<String>`, `Deref`, `Borrow<str>`, `FromStr`, `PartialEq<str>`, plus `new()` (fresh ULID), `from_string()`, `as_str()`. All existing ID types (`TaskId`, `ColumnId`, `TagId`, etc.) use it.\n\n-**All identity and measurement fields are newtypes.** No `String`, no `f64` at the boundaries of the public types, no exceptions. Wire format stays plain via `#[serde(transparent)]`.\n+Define the string-valued newtypes with this macro:\n\n ```rust\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct WindowLabel(pub String);   // Tauri window label\n+use swissarmyhammer_common::define_id;\n\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct SpatialKey(pub String);    // ULID per scope mount\n+define_id!(WindowLabel, \"Tauri window label — which window a scope/layer lives in\");\n+define_id!(SpatialKey, \"ULID per FocusScope/FocusZone/Focusable instance\");\n+define_id!(LayerKey,   \"ULID per FocusLayer instance\");\n+define_id!(Moniker,    \"Entity focus identity: \\\"task:01ABC\\\", \\\"ui:toolbar.new\\\"\");\n+define_id!(LayerName,  \"Layer role: \\\"window\\\", \\\"inspector\\\", \\\"dialog\\\", \\\"palette\\\"\");\n+```\n\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct LayerKey(pub String);      // ULID per layer mount\n-\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct Moniker(pub String);       // entity identity: \"task:01ABC\", \"ui:toolbar.new\"\n+`Pixels` is numeric, not string — hand-rolled:\n\n-#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct LayerName(pub String);     // layer role: \"window\", \"inspector\", \"dialog\", \"palette\"\n-\n+```rust\n #[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n-#[serde(transparent)] pub struct Pixels(pub f64);           // screen coordinate / size in CSS pixels\n-```\n+#[serde(transparent)]\n+pub struct Pixels(pub f64);\n\n-`Pixels` implements arithmetic (`Add`, `Sub`, `Mul<f64>`, `Div<f64>`) so the nav algorithm's beam/score math stays ergonomic without drifting to `f64`.\n+// Arithmetic so beam/score math doesn't drop to f64:\n+impl std::ops::Add for Pixels { /* ... */ }\n+impl std::ops::Sub for Pixels { /* ... */ }\n+impl std::ops::Mul<f64> for Pixels { /* ... */ }\n+impl std::ops::Div<f64> for Pixels { /* ... */ }\n+```\n\n ### Rect\n\n@@ -55,8 +77,8 @@\n impl Rect {\n     pub fn top(&self)    -> Pixels { self.y }\n     pub fn left(&self)   -> Pixels { self.x }\n-    pub fn bottom(&self) -> Pixels { Pixels(self.y.0 + self.height.0) }\n-    pub fn right(&self)  -> Pixels { Pixels(self.x.0 + self.width.0) }\n+    pub fn bottom(&self) -> Pixels { self.y + self.height }\n+    pub fn right(&self)  -> Pixels { self.x + self.width }\n }\n ```\n\n@@ -63,7 +85,6 @@\n ### The four types\n\n ```rust\n-/// Leaf — a single focusable point.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct Focusable {\n     pub key: SpatialKey,\n@@ -74,7 +95,6 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n-/// Zone — a navigable container with focus memory.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusZone {\n     pub key: SpatialKey,\n@@ -86,7 +106,6 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n-/// Layer — a hard modal boundary.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusLayer {\n     pub key: LayerKey,\n@@ -96,7 +115,6 @@\n     pub last_focused: Option<SpatialKey>,\n }\n\n-/// Sum type stored in the registry per SpatialKey.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n #[serde(tag = \"kind\", rename_all = \"snake_case\")]\n pub enum FocusScope {\n@@ -146,25 +164,15 @@\n }\n ```\n\n-### Tauri commands\n+### Tauri commands — in `kanban-app/src/commands.rs`\n\n-Every command signature uses newtypes, not `String`/`f64`:\n+The headless registry lives in `swissarmyhammer-kanban`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the kanban crate:\n\n ```rust\n #[tauri::command]\n-async fn spatial_register_focusable(\n-    window: tauri::Window,\n-    key: SpatialKey,\n-    moniker: Moniker,\n-    rect: Rect,\n-    layer_key: LayerKey,\n-    parent_zone: Option<SpatialKey>,\n-    overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<()>;\n-\n-#[tauri::command]\n-async fn spatial_register_zone(\n+pub async fn spatial_register_focusable(\n     window: tauri::Window,\n+    state: State<'_, AppState>,\n     key: SpatialKey,\n     moniker: Moniker,\n     rect: Rect,\n@@ -171,69 +179,56 @@\n     layer_key: LayerKey,\n     parent_zone: Option<SpatialKey>,\n     overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<()>;\n+) -> Result<(), String>;\n\n-#[tauri::command]\n-async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_update_rect(window: tauri::Window, key: SpatialKey, rect: Rect) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_push_layer(\n-    window: tauri::Window,\n-    key: LayerKey,\n-    name: LayerName,\n-    parent: Option<LayerKey>,\n-) -> Result<()>;\n-#[tauri::command]\n-async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n+// Likewise: spatial_register_zone, spatial_unregister_scope, spatial_update_rect,\n+//           spatial_push_layer, spatial_pop_layer, spatial_focus, spatial_navigate,\n+//           spatial_drill_in, spatial_drill_out\n ```\n\n-`window_label: WindowLabel` is derived from the `tauri::Window` parameter inside the handler, never passed from React.\n+### Tests — `swissarmyhammer-kanban/tests/focus_registry.rs`\n\n-### Serde wire format\n-\n-All newtypes are `#[serde(transparent)]` → JSON strings/numbers. The `FocusScope` enum serializes with `\"kind\": \"focusable\" | \"zone\"` as a discriminator tag.\n+Follow the pattern of `swissarmyhammer-kanban/tests/resolve_focused_column.rs` and `dynamic_sources_headless.rs` — pure-Rust, no Tauri, no jsdom. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n\n ### Design decisions\n\n-- **Newtypes throughout, not just on identity**: measurements (`Pixels`) are newtyped too. Prevents accidental mixing of pixel values with unrelated floats (e.g., scoring multipliers, timestamps) and makes future unit changes (device pixels vs CSS pixels) explicit.\n-- **Override target is `Moniker`, not `String`**: matches the setFocus target type; prevents accidentally stuffing a `SpatialKey` where a moniker goes.\n-- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked; a `Focusable` cannot carry one.\n-- **`FocusScope` as a Rust enum**: one map, pattern matching in nav. No trait objects.\n-- **`LayerName` as a newtype, not an enum**: layer names grow organically (window, inspector, dialog, palette, quick-capture, future things). Newtype keeps the string bounded to the slot without over-constraining to a fixed set.\n+- **`swissarmyhammer-kanban` not `swissarmyhammer-commands`**: matches the refactor pattern — business logic over Tier 0.\n+- **`define_id!` macro reuse**: consistent with `TaskId`/`ColumnId`/`TagId`; avoids hand-rolled boilerplate.\n+- **`focus/` submodule**: groups all focus concerns (column resolver + spatial registry) together.\n+- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked.\n+- **`FocusScope` as a Rust enum**: one map, pattern matching.\n\n ### Subtasks\n-- [ ] Define newtypes: `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName`, `Pixels`\n-- [ ] Impl `Add`/`Sub`/`Mul<f64>`/`Div<f64>` on `Pixels`\n-- [ ] Define `Rect` with `Pixels` fields + accessor helpers\n-- [ ] Define `Direction` enum\n-- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` enum with serde attrs\n-- [ ] Implement `FocusScope` helper methods\n-- [ ] Implement `SpatialRegistry` with scope + layer ops\n+- [ ] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n+- [ ] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n+- [ ] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n+- [ ] `focus/layer.rs`: define `FocusLayer`\n+- [ ] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n - [ ] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n - [ ] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [ ] Tauri commands with fully-typed signatures (no `String`/`f64` on command params)\n+- [ ] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n+- [ ] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n\n ## Acceptance Criteria\n+- [ ] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n+- [ ] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n - [ ] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n - [ ] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [ ] All newtypes are `#[serde(transparent)]`; the enum is `#[serde(tag = \"kind\")]`\n-- [ ] `Focusable` has no `last_focused` field; only `FocusZone` does\n-- [ ] `Pixels` supports arithmetic without requiring `.0` access in the nav algorithm\n-- [ ] `SpatialRegistry` holds any mix of focusables, zones, and layers across windows + nested zones\n-- [ ] Tauri commands derive `window_label` from `tauri::Window`\n-- [ ] `cargo test` passes\n+- [ ] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n+- [ ] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n+- [ ] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n+- [ ] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n\n-## Tests (pure Rust)\n-- [ ] Each newtype is `#[serde(transparent)]` — JSON round-trips as a bare primitive\n-- [ ] `Pixels` arithmetic produces a `Pixels` result (type-checked)\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde with `\"kind\"` tag\n+## Tests (pure Rust, in `swissarmyhammer-kanban/tests/focus_registry.rs`)\n+- [ ] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n+- [ ] `Pixels` arithmetic is type-preserving\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n - [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` direct children only (not transitive)\n+- [ ] Registry: `children_of_zone` direct children only\n - [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` returns layers with matching `parent`, not cross-window\n-- [ ] Registry: `ancestors_of_layer` walks `layer.parent` up to window root\n-- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones filtered by `layer_key`\n+- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n - [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -2,26 +2,6 @@\n\n Define the Rust kernel types for spatial focus. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n-### Crate placement\n-\n-Following the pattern established in commit `b81336d42` (move GUI-crate logic into headless-testable kanban crate), **this lives in `swissarmyhammer-kanban`**, not `swissarmyhammer-commands`. Rationale: spatial nav is business logic that must be testable without Tauri or jsdom; `swissarmyhammer-commands` stays Tier 0 (no app state, no windowing concepts).\n-\n-Specifically, organize as a submodule alongside the existing `focus.rs`:\n-\n-```\n-swissarmyhammer-kanban/src/focus/\n-  mod.rs          (re-exports; existing resolve_focused_column migrates here)\n-  column.rs       (existing resolve_focused_column logic)\n-  types.rs        (newtypes: WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels, Rect, Direction)\n-  scope.rs        (Focusable, FocusZone, FocusScope enum)\n-  layer.rs        (FocusLayer struct)\n-  registry.rs     (SpatialRegistry with scope + layer ops)\n-  state.rs        (SpatialState with focus_by_window, event emission)\n-  navigate.rs     (beam search algorithm — separate card 01KNQXXF5W...)\n-```\n-\n-Migration of `focus.rs` → `focus/column.rs` is part of this card's scope; keep the public re-export path (`swissarmyhammer_kanban::focus::resolve_focused_column`) stable.\n-\n ### The four peer types\n\n | Role                | React component     | Rust type             |\n@@ -31,38 +11,36 @@\n | Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n | Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n\n-On Rust: `FocusScope` is the sum type `Focusable | FocusZone` stored in the registry per `SpatialKey`. On React: `FocusScope` is the entity-aware wrapper that composes `<Focusable>` or `<FocusZone>`.\n+On Rust: `FocusScope` is the sum type `Focusable | FocusZone`. It's what the registry stores per `SpatialKey`. Helper methods on the enum dispatch to the variant (`scope.layer_key()`, `scope.parent_zone()`, `scope.rect()`).\n\n-### Newtype discipline — use `swissarmyhammer_common::define_id!`\n+On React: `FocusScope` is the high-level convenience wrapper that internally uses a `Focusable` or `FocusZone` primitive and adds entity plumbing (CommandScopeProvider, click-to-focus, context menu, focus bar, data-moniker attrs).\n\n-The project already has a canonical newtype macro: `define_id!` from `swissarmyhammer-common`. It provides `#[serde(transparent)]`, `Display`, `AsRef<str>`, `From<&str>`/`From<String>`, `Deref`, `Borrow<str>`, `FromStr`, `PartialEq<str>`, plus `new()` (fresh ULID), `from_string()`, `as_str()`. All existing ID types (`TaskId`, `ColumnId`, `TagId`, etc.) use it.\n+### Newtype discipline — no bare primitives on type signatures\n\n-Define the string-valued newtypes with this macro:\n+**All identity and measurement fields are newtypes.** No `String`, no `f64` at the boundaries of the public types, no exceptions. Wire format stays plain via `#[serde(transparent)]`.\n\n ```rust\n-use swissarmyhammer_common::define_id;\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct WindowLabel(pub String);   // Tauri window label\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct SpatialKey(pub String);    // ULID per scope mount\n+\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct LayerKey(pub String);      // ULID per layer mount\n\n-define_id!(WindowLabel, \"Tauri window label — which window a scope/layer lives in\");\n-define_id!(SpatialKey, \"ULID per FocusScope/FocusZone/Focusable instance\");\n-define_id!(LayerKey,   \"ULID per FocusLayer instance\");\n-define_id!(Moniker,    \"Entity focus identity: \\\"task:01ABC\\\", \\\"ui:toolbar.new\\\"\");\n-define_id!(LayerName,  \"Layer role: \\\"window\\\", \\\"inspector\\\", \\\"dialog\\\", \\\"palette\\\"\");\n-```\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct Moniker(pub String);       // entity identity: \"task:01ABC\", \"ui:toolbar.new\"\n\n-`Pixels` is numeric, not string — hand-rolled:\n+#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]\n+#[serde(transparent)] pub struct LayerName(pub String);     // layer role: \"window\", \"inspector\", \"dialog\", \"palette\"\n\n-```rust\n #[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n-#[serde(transparent)]\n-pub struct Pixels(pub f64);\n-\n-// Arithmetic so beam/score math doesn't drop to f64:\n-impl std::ops::Add for Pixels { /* ... */ }\n-impl std::ops::Sub for Pixels { /* ... */ }\n-impl std::ops::Mul<f64> for Pixels { /* ... */ }\n-impl std::ops::Div<f64> for Pixels { /* ... */ }\n+#[serde(transparent)] pub struct Pixels(pub f64);           // screen coordinate / size in CSS pixels\n ```\n\n+`Pixels` implements arithmetic (`Add`, `Sub`, `Mul<f64>`, `Div<f64>`) so the nav algorithm's beam/score math stays ergonomic without drifting to `f64`.\n+\n ### Rect\n\n ```rust\n@@ -77,8 +55,8 @@\n impl Rect {\n     pub fn top(&self)    -> Pixels { self.y }\n     pub fn left(&self)   -> Pixels { self.x }\n-    pub fn bottom(&self) -> Pixels { self.y + self.height }\n-    pub fn right(&self)  -> Pixels { self.x + self.width }\n+    pub fn bottom(&self) -> Pixels { Pixels(self.y.0 + self.height.0) }\n+    pub fn right(&self)  -> Pixels { Pixels(self.x.0 + self.width.0) }\n }\n ```\n\n@@ -85,6 +63,7 @@\n ### The four types\n\n ```rust\n+/// Leaf — a single focusable point.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct Focusable {\n     pub key: SpatialKey,\n@@ -95,6 +74,7 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n+/// Zone — a navigable container with focus memory.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusZone {\n     pub key: SpatialKey,\n@@ -106,6 +86,7 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n+/// Layer — a hard modal boundary.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusLayer {\n     pub key: LayerKey,\n@@ -115,6 +96,7 @@\n     pub last_focused: Option<SpatialKey>,\n }\n\n+/// Sum type stored in the registry per SpatialKey.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n #[serde(tag = \"kind\", rename_all = \"snake_case\")]\n pub enum FocusScope {\n@@ -164,15 +146,14 @@\n }\n ```\n\n-### Tauri commands — in `kanban-app/src/commands.rs`\n+### Tauri commands\n\n-The headless registry lives in `swissarmyhammer-kanban`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the kanban crate:\n+Every command signature uses newtypes, not `String`/`f64`:\n\n ```rust\n #[tauri::command]\n-pub async fn spatial_register_focusable(\n+async fn spatial_register_focusable(\n     window: tauri::Window,\n-    state: State<'_, AppState>,\n     key: SpatialKey,\n     moniker: Moniker,\n     rect: Rect,\n@@ -179,56 +160,80 @@\n     layer_key: LayerKey,\n     parent_zone: Option<SpatialKey>,\n     overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<(), String>;\n+) -> Result<()>;\n\n-// Likewise: spatial_register_zone, spatial_unregister_scope, spatial_update_rect,\n-//           spatial_push_layer, spatial_pop_layer, spatial_focus, spatial_navigate,\n-//           spatial_drill_in, spatial_drill_out\n+#[tauri::command]\n+async fn spatial_register_zone(\n+    window: tauri::Window,\n+    key: SpatialKey,\n+    moniker: Moniker,\n+    rect: Rect,\n+    layer_key: LayerKey,\n+    parent_zone: Option<SpatialKey>,\n+    overrides: HashMap<Direction, Option<Moniker>>,\n+) -> Result<()>;\n+\n+#[tauri::command]\n+async fn spatial_unregister_scope(window: tauri::Window, key: SpatialKey) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_update_rect(window: tauri::Window, key: SpatialKey, rect: Rect) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_push_layer(\n+    window: tauri::Window,\n+    key: LayerKey,\n+    name: LayerName,\n+    parent: Option<LayerKey>,\n+) -> Result<()>;\n+#[tauri::command]\n+async fn spatial_pop_layer(window: tauri::Window, key: LayerKey) -> Result<()>;\n ```\n\n-### Tests — `swissarmyhammer-kanban/tests/focus_registry.rs`\n+`window_label: WindowLabel` is derived from the `tauri::Window` parameter inside the handler, never passed from React.\n+\n+### Serde wire format\n\n-Follow the pattern of `swissarmyhammer-kanban/tests/resolve_focused_column.rs` and `dynamic_sources_headless.rs` — pure-Rust, no Tauri, no jsdom. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n+All newtypes are `#[serde(transparent)]` → JSON strings/numbers. The `FocusScope` enum serializes with `\"kind\": \"focusable\" | \"zone\"` as a discriminator tag.\n\n ### Design decisions\n\n-- **`swissarmyhammer-kanban` not `swissarmyhammer-commands`**: matches the refactor pattern — business logic over Tier 0.\n-- **`define_id!` macro reuse**: consistent with `TaskId`/`ColumnId`/`TagId`; avoids hand-rolled boilerplate.\n-- **`focus/` submodule**: groups all focus concerns (column resolver + spatial registry) together.\n-- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked.\n-- **`FocusScope` as a Rust enum**: one map, pattern matching.\n+- **Newtypes throughout, not just on identity**: measurements (`Pixels`) are newtyped too. Prevents accidental mixing of pixel values with unrelated floats (e.g., scoring multipliers, timestamps) and makes future unit changes (device pixels vs CSS pixels) explicit.\n+- **Override target is `Moniker`, not `String`**: matches the setFocus target type; prevents accidentally stuffing a `SpatialKey` where a moniker goes.\n+- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked; a `Focusable` cannot carry one.\n+- **`FocusScope` as a Rust enum**: one map, pattern matching in nav. No trait objects.\n+- **`LayerName` as a newtype, not an enum**: layer names grow organically (window, inspector, dialog, palette, quick-capture, future things). Newtype keeps the string bounded to the slot without over-constraining to a fixed set.\n\n ### Subtasks\n-- [ ] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n-- [ ] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n-- [ ] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n-- [ ] `focus/layer.rs`: define `FocusLayer`\n-- [ ] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n+- [ ] Define newtypes: `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName`, `Pixels`\n+- [ ] Impl `Add`/`Sub`/`Mul<f64>`/`Div<f64>` on `Pixels`\n+- [ ] Define `Rect` with `Pixels` fields + accessor helpers\n+- [ ] Define `Direction` enum\n+- [ ] Define `Focusable`, `FocusZone`, `FocusLayer`, `FocusScope` enum with serde attrs\n+- [ ] Implement `FocusScope` helper methods\n+- [ ] Implement `SpatialRegistry` with scope + layer ops\n - [ ] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n - [ ] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [ ] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n-- [ ] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n+- [ ] Tauri commands with fully-typed signatures (no `String`/`f64` on command params)\n\n ## Acceptance Criteria\n-- [ ] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n-- [ ] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n - [ ] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n - [ ] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [ ] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n-- [ ] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n-- [ ] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n-- [ ] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n+- [ ] All newtypes are `#[serde(transparent)]`; the enum is `#[serde(tag = \"kind\")]`\n+- [ ] `Focusable` has no `last_focused` field; only `FocusZone` does\n+- [ ] `Pixels` supports arithmetic without requiring `.0` access in the nav algorithm\n+- [ ] `SpatialRegistry` holds any mix of focusables, zones, and layers across windows + nested zones\n+- [ ] Tauri commands derive `window_label` from `tauri::Window`\n+- [ ] `cargo test` passes\n\n-## Tests (pure Rust, in `swissarmyhammer-kanban/tests/focus_registry.rs`)\n-- [ ] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n-- [ ] `Pixels` arithmetic is type-preserving\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+## Tests (pure Rust)\n+- [ ] Each newtype is `#[serde(transparent)]` — JSON round-trips as a bare primitive\n+- [ ] `Pixels` arithmetic produces a `Pixels` result (type-checked)\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip through serde with `\"kind\"` tag\n - [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` direct children only\n+- [ ] Registry: `children_of_zone` direct children only (not transitive)\n - [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n+- [ ] Registry: `children_of_layer` returns layers with matching `parent`, not cross-window\n+- [ ] Registry: `ancestors_of_layer` walks `layer.parent` up to window root\n+- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones filtered by `layer_key`\n - [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n"}]]}
{"id":"01KQ2D4SYYR9X0ZC9T969PK12W","timestamp":"2026-04-25T13:26:56.734139Z","op":"update","item_id":"01KNQXW7HHHB8HW76K3PXH3G34","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNM3YHHFJ3PTXZHD9EFKVBS6\n-position_column: todo\n-position_ordinal: a180\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Rust kernel: Focusable, FocusZone, FocusLayer, FocusScope types + SpatialRegistry'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNM3YHHFJ3PTXZHD9EFKVBS6\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: a180\n project: spatial-nav\n title: 'Rust kernel: Focusable, FocusZone, FocusLayer, FocusScope types + SpatialRegistry'\n ---\n","transaction_id":null}
{"id":"01KQ2D4SYZQP8NPNV76N38MHP6","timestamp":"2026-04-25T13:26:56.735261Z","entity_type":"task","entity_id":"01KNQXW7HHHB8HW76K3PXH3G34","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-a180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+a180\n\\ No newline at end of file\n"}]]}
{"id":"01KQ2E93RGGXAAEZQS0CC50WS2","timestamp":"2026-04-25T13:46:46.416737Z","op":"update","item_id":"01KNQXW7HHHB8HW76K3PXH3G34","forward_patch":"--- original\n+++ modified\n@@ -15,23 +15,23 @@\n\n ### Crate placement\n\n-**Lives in the new `swissarmyhammer-focus` crate** (created by card `01KQ2E7RPBPJ8T8KZX39N2SZ0A`). Spatial focus is generic — no kanban concepts, no Tauri, no domain types. Putting it in `swissarmyhammer-kanban` was wrong; the dedicated crate makes it reusable, easier to test, and keeps the kanban crate focused on board/task/column logic.\n+Following the pattern established in commit `b81336d42` (move GUI-crate logic into headless-testable kanban crate), **this lives in `swissarmyhammer-kanban`**, not `swissarmyhammer-commands`. Rationale: spatial nav is business logic that must be testable without Tauri or jsdom; `swissarmyhammer-commands` stays Tier 0 (no app state, no windowing concepts).\n+\n+Specifically, organize as a submodule alongside the existing `focus.rs`:\n\n ```\n-swissarmyhammer-focus/src/\n-  lib.rs            (module declarations)\n-  types.rs          ← THIS CARD: newtypes (WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels) + Rect + Direction\n-  scope.rs          ← THIS CARD: Focusable, FocusZone, FocusScope enum\n-  layer.rs          ← THIS CARD: FocusLayer struct\n-  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops\n-  state.rs          (SpatialState — card 01KNM3YHHFJ3...)\n-  navigate.rs       (BeamNavStrategy impl — card 01KNQXXF5W...)\n-  observer.rs       (FocusEventSink impls — card 01KQ2E7RPBPJ8...)\n+swissarmyhammer-kanban/src/focus/\n+  mod.rs          (re-exports; existing resolve_focused_column migrates here)\n+  column.rs       (existing resolve_focused_column logic)\n+  types.rs        (newtypes: WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels, Rect, Direction)\n+  scope.rs        (Focusable, FocusZone, FocusScope enum)\n+  layer.rs        (FocusLayer struct)\n+  registry.rs     (SpatialRegistry with scope + layer ops)\n+  state.rs        (SpatialState with focus_by_window, event emission)\n+  navigate.rs     (beam search algorithm — separate card 01KNQXXF5W...)\n ```\n\n-The skeleton + traits exist before this card runs (per dependency on `01KQ2E7RPBPJ8...`). This card fills `types.rs`, `scope.rs`, `layer.rs`, and `registry.rs` with their full implementations.\n-\n-**`swissarmyhammer-kanban/src/focus.rs` is untouched.** That file holds `resolve_focused_column` — kanban-specific column resolver — which stays where it is. The earlier plan to migrate it into a `focus/` subdirectory is canceled.\n+Migration of `focus.rs` → `focus/column.rs` is part of this card's scope; keep the public re-export path (`swissarmyhammer_kanban::focus::resolve_focused_column`) stable.\n\n ### The four peer types\n\n@@ -216,7 +216,7 @@\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the focus crate:\n+The headless registry lives in `swissarmyhammer-kanban`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the kanban crate:\n\n ```rust\n #[tauri::command]\n@@ -249,37 +249,44 @@\n - **`FocusScope` as a Rust enum**: one map, pattern matching.\n\n ### Subtasks\n-- [ ] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n-- [ ] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n-- [ ] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n-- [ ] `focus/layer.rs`: define `FocusLayer`\n-- [ ] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n-- [ ] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n-- [ ] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [ ] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n-- [ ] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n+- [x] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n+- [x] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n+- [x] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n+- [x] `focus/layer.rs`: define `FocusLayer`\n+- [x] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n+- [x] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n+- [x] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n+- [x] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n+- [x] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n\n ## Acceptance Criteria\n-- [ ] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n-- [ ] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n-- [ ] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n-- [ ] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [ ] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n-- [ ] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n-- [ ] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n-- [ ] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n+- [x] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n+- [x] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n+- [x] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n+- [x] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [x] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n+- [x] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n+- [x] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n+- [x] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n\n ## Tests (pure Rust, in `swissarmyhammer-kanban/tests/focus_registry.rs`)\n-- [ ] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n-- [ ] `Pixels` arithmetic is type-preserving\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` direct children only\n-- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n-- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+- [x] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n+- [x] `Pixels` arithmetic is type-preserving\n+- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [x] Registry: `children_of_zone` direct children only\n+- [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [x] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [x] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [x] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n+- [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+\n+## Implementation notes\n+\n+- The migration of `focus.rs` → `focus/column.rs` was already completed in the dependency card `01KNM3YHHFJ3PTXZHD9EFKVBS6`; this card extends `focus/types.rs` (adding `Pixels`/`Rect`) and adds the new `focus/scope.rs`, `focus/layer.rs`, `focus/registry.rs` modules alongside the existing `focus/state.rs`.\n+- `SpatialState` (per-window focus tracker) and `SpatialRegistry` (geometry / hierarchy backing store) are intentionally kept as separate concerns; the Tauri register adapters write to both. `SpatialState` mutates frequently (every focus move); `SpatialRegistry` mutates only on mount/unmount/resize.\n+- `spatial_drill_in` / `spatial_drill_out` Tauri commands are pinned with `Result<(), String>` no-op bodies pending the navigator card `01KNQXXF5W...`, matching the existing `spatial_navigate` pattern.\n+- TypeScript `Rect` and `FocusOverrides` types and the updated `useSpatialFocusActions` shape (with `updateRect`, `drillIn`, `drillOut`) ship alongside the Rust kernel so the IPC surface is internally consistent.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -15,23 +15,23 @@\n\n ### Crate placement\n\n-Following the pattern established in commit `b81336d42` (move GUI-crate logic into headless-testable kanban crate), **this lives in `swissarmyhammer-kanban`**, not `swissarmyhammer-commands`. Rationale: spatial nav is business logic that must be testable without Tauri or jsdom; `swissarmyhammer-commands` stays Tier 0 (no app state, no windowing concepts).\n-\n-Specifically, organize as a submodule alongside the existing `focus.rs`:\n+**Lives in the new `swissarmyhammer-focus` crate** (created by card `01KQ2E7RPBPJ8T8KZX39N2SZ0A`). Spatial focus is generic — no kanban concepts, no Tauri, no domain types. Putting it in `swissarmyhammer-kanban` was wrong; the dedicated crate makes it reusable, easier to test, and keeps the kanban crate focused on board/task/column logic.\n\n ```\n-swissarmyhammer-kanban/src/focus/\n-  mod.rs          (re-exports; existing resolve_focused_column migrates here)\n-  column.rs       (existing resolve_focused_column logic)\n-  types.rs        (newtypes: WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels, Rect, Direction)\n-  scope.rs        (Focusable, FocusZone, FocusScope enum)\n-  layer.rs        (FocusLayer struct)\n-  registry.rs     (SpatialRegistry with scope + layer ops)\n-  state.rs        (SpatialState with focus_by_window, event emission)\n-  navigate.rs     (beam search algorithm — separate card 01KNQXXF5W...)\n+swissarmyhammer-focus/src/\n+  lib.rs            (module declarations)\n+  types.rs          ← THIS CARD: newtypes (WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels) + Rect + Direction\n+  scope.rs          ← THIS CARD: Focusable, FocusZone, FocusScope enum\n+  layer.rs          ← THIS CARD: FocusLayer struct\n+  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops\n+  state.rs          (SpatialState — card 01KNM3YHHFJ3...)\n+  navigate.rs       (BeamNavStrategy impl — card 01KNQXXF5W...)\n+  observer.rs       (FocusEventSink impls — card 01KQ2E7RPBPJ8...)\n ```\n\n-Migration of `focus.rs` → `focus/column.rs` is part of this card's scope; keep the public re-export path (`swissarmyhammer_kanban::focus::resolve_focused_column`) stable.\n+The skeleton + traits exist before this card runs (per dependency on `01KQ2E7RPBPJ8...`). This card fills `types.rs`, `scope.rs`, `layer.rs`, and `registry.rs` with their full implementations.\n+\n+**`swissarmyhammer-kanban/src/focus.rs` is untouched.** That file holds `resolve_focused_column` — kanban-specific column resolver — which stays where it is. The earlier plan to migrate it into a `focus/` subdirectory is canceled.\n\n ### The four peer types\n\n@@ -216,7 +216,7 @@\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-kanban`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the kanban crate:\n+The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the focus crate:\n\n ```rust\n #[tauri::command]\n@@ -249,44 +249,37 @@\n - **`FocusScope` as a Rust enum**: one map, pattern matching.\n\n ### Subtasks\n-- [x] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n-- [x] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n-- [x] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n-- [x] `focus/layer.rs`: define `FocusLayer`\n-- [x] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n-- [x] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n-- [x] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [x] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n-- [x] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n+- [ ] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n+- [ ] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n+- [ ] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n+- [ ] `focus/layer.rs`: define `FocusLayer`\n+- [ ] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n+- [ ] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n+- [ ] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n+- [ ] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n+- [ ] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n\n ## Acceptance Criteria\n-- [x] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n-- [x] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n-- [x] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n-- [x] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [x] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n-- [x] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n-- [x] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n-- [x] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n+- [ ] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n+- [ ] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n+- [ ] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n+- [ ] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [ ] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n+- [ ] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n+- [ ] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n+- [ ] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n\n ## Tests (pure Rust, in `swissarmyhammer-kanban/tests/focus_registry.rs`)\n-- [x] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n-- [x] `Pixels` arithmetic is type-preserving\n-- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [x] Registry: `children_of_zone` direct children only\n-- [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [x] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [x] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [x] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n-- [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n-\n-## Implementation notes\n-\n-- The migration of `focus.rs` → `focus/column.rs` was already completed in the dependency card `01KNM3YHHFJ3PTXZHD9EFKVBS6`; this card extends `focus/types.rs` (adding `Pixels`/`Rect`) and adds the new `focus/scope.rs`, `focus/layer.rs`, `focus/registry.rs` modules alongside the existing `focus/state.rs`.\n-- `SpatialState` (per-window focus tracker) and `SpatialRegistry` (geometry / hierarchy backing store) are intentionally kept as separate concerns; the Tauri register adapters write to both. `SpatialState` mutates frequently (every focus move); `SpatialRegistry` mutates only on mount/unmount/resize.\n-- `spatial_drill_in` / `spatial_drill_out` Tauri commands are pinned with `Result<(), String>` no-op bodies pending the navigator card `01KNQXXF5W...`, matching the existing `spatial_navigate` pattern.\n-- TypeScript `Rect` and `FocusOverrides` types and the updated `useSpatialFocusActions` shape (with `updateRect`, `drillIn`, `drillOut`) ship alongside the Rust kernel so the IPC surface is internally consistent.\n+- [ ] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n+- [ ] `Pixels` arithmetic is type-preserving\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [ ] Registry: `children_of_zone` direct children only\n+- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n+- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ2E93RHQ1WPSFHDM6KY51DM","timestamp":"2026-04-25T13:46:46.417319Z","entity_type":"task","entity_id":"01KNQXW7HHHB8HW76K3PXH3G34","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -4,23 +4,23 @@\n\n ### Crate placement\n\n-**Lives in the new `swissarmyhammer-focus` crate** (created by card `01KQ2E7RPBPJ8T8KZX39N2SZ0A`). Spatial focus is generic — no kanban concepts, no Tauri, no domain types. Putting it in `swissarmyhammer-kanban` was wrong; the dedicated crate makes it reusable, easier to test, and keeps the kanban crate focused on board/task/column logic.\n+Following the pattern established in commit `b81336d42` (move GUI-crate logic into headless-testable kanban crate), **this lives in `swissarmyhammer-kanban`**, not `swissarmyhammer-commands`. Rationale: spatial nav is business logic that must be testable without Tauri or jsdom; `swissarmyhammer-commands` stays Tier 0 (no app state, no windowing concepts).\n+\n+Specifically, organize as a submodule alongside the existing `focus.rs`:\n\n ```\n-swissarmyhammer-focus/src/\n-  lib.rs            (module declarations)\n-  types.rs          ← THIS CARD: newtypes (WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels) + Rect + Direction\n-  scope.rs          ← THIS CARD: Focusable, FocusZone, FocusScope enum\n-  layer.rs          ← THIS CARD: FocusLayer struct\n-  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops\n-  state.rs          (SpatialState — card 01KNM3YHHFJ3...)\n-  navigate.rs       (BeamNavStrategy impl — card 01KNQXXF5W...)\n-  observer.rs       (FocusEventSink impls — card 01KQ2E7RPBPJ8...)\n+swissarmyhammer-kanban/src/focus/\n+  mod.rs          (re-exports; existing resolve_focused_column migrates here)\n+  column.rs       (existing resolve_focused_column logic)\n+  types.rs        (newtypes: WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels, Rect, Direction)\n+  scope.rs        (Focusable, FocusZone, FocusScope enum)\n+  layer.rs        (FocusLayer struct)\n+  registry.rs     (SpatialRegistry with scope + layer ops)\n+  state.rs        (SpatialState with focus_by_window, event emission)\n+  navigate.rs     (beam search algorithm — separate card 01KNQXXF5W...)\n ```\n\n-The skeleton + traits exist before this card runs (per dependency on `01KQ2E7RPBPJ8...`). This card fills `types.rs`, `scope.rs`, `layer.rs`, and `registry.rs` with their full implementations.\n-\n-**`swissarmyhammer-kanban/src/focus.rs` is untouched.** That file holds `resolve_focused_column` — kanban-specific column resolver — which stays where it is. The earlier plan to migrate it into a `focus/` subdirectory is canceled.\n+Migration of `focus.rs` → `focus/column.rs` is part of this card's scope; keep the public re-export path (`swissarmyhammer_kanban::focus::resolve_focused_column`) stable.\n\n ### The four peer types\n\n@@ -205,7 +205,7 @@\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the focus crate:\n+The headless registry lives in `swissarmyhammer-kanban`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the kanban crate:\n\n ```rust\n #[tauri::command]\n@@ -238,37 +238,44 @@\n - **`FocusScope` as a Rust enum**: one map, pattern matching.\n\n ### Subtasks\n-- [ ] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n-- [ ] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n-- [ ] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n-- [ ] `focus/layer.rs`: define `FocusLayer`\n-- [ ] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n-- [ ] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n-- [ ] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [ ] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n-- [ ] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n+- [x] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n+- [x] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n+- [x] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n+- [x] `focus/layer.rs`: define `FocusLayer`\n+- [x] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n+- [x] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n+- [x] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n+- [x] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n+- [x] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n\n ## Acceptance Criteria\n-- [ ] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n-- [ ] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n-- [ ] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n-- [ ] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [ ] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n-- [ ] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n-- [ ] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n-- [ ] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n+- [x] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n+- [x] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n+- [x] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n+- [x] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [x] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n+- [x] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n+- [x] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n+- [x] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n\n ## Tests (pure Rust, in `swissarmyhammer-kanban/tests/focus_registry.rs`)\n-- [ ] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n-- [ ] `Pixels` arithmetic is type-preserving\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` direct children only\n-- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n-- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+- [x] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n+- [x] `Pixels` arithmetic is type-preserving\n+- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [x] Registry: `children_of_zone` direct children only\n+- [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [x] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [x] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [x] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n+- [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+\n+## Implementation notes\n+\n+- The migration of `focus.rs` → `focus/column.rs` was already completed in the dependency card `01KNM3YHHFJ3PTXZHD9EFKVBS6`; this card extends `focus/types.rs` (adding `Pixels`/`Rect`) and adds the new `focus/scope.rs`, `focus/layer.rs`, `focus/registry.rs` modules alongside the existing `focus/state.rs`.\n+- `SpatialState` (per-window focus tracker) and `SpatialRegistry` (geometry / hierarchy backing store) are intentionally kept as separate concerns; the Tauri register adapters write to both. `SpatialState` mutates frequently (every focus move); `SpatialRegistry` mutates only on mount/unmount/resize.\n+- `spatial_drill_in` / `spatial_drill_out` Tauri commands are pinned with `Result<(), String>` no-op bodies pending the navigator card `01KNQXXF5W...`, matching the existing `spatial_navigate` pattern.\n+- TypeScript `Rect` and `FocusOverrides` types and the updated `useSpatialFocusActions` shape (with `updateRect`, `drillIn`, `drillOut`) ship alongside the Rust kernel so the IPC surface is internally consistent.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -4,23 +4,23 @@\n\n ### Crate placement\n\n-Following the pattern established in commit `b81336d42` (move GUI-crate logic into headless-testable kanban crate), **this lives in `swissarmyhammer-kanban`**, not `swissarmyhammer-commands`. Rationale: spatial nav is business logic that must be testable without Tauri or jsdom; `swissarmyhammer-commands` stays Tier 0 (no app state, no windowing concepts).\n-\n-Specifically, organize as a submodule alongside the existing `focus.rs`:\n+**Lives in the new `swissarmyhammer-focus` crate** (created by card `01KQ2E7RPBPJ8T8KZX39N2SZ0A`). Spatial focus is generic — no kanban concepts, no Tauri, no domain types. Putting it in `swissarmyhammer-kanban` was wrong; the dedicated crate makes it reusable, easier to test, and keeps the kanban crate focused on board/task/column logic.\n\n ```\n-swissarmyhammer-kanban/src/focus/\n-  mod.rs          (re-exports; existing resolve_focused_column migrates here)\n-  column.rs       (existing resolve_focused_column logic)\n-  types.rs        (newtypes: WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels, Rect, Direction)\n-  scope.rs        (Focusable, FocusZone, FocusScope enum)\n-  layer.rs        (FocusLayer struct)\n-  registry.rs     (SpatialRegistry with scope + layer ops)\n-  state.rs        (SpatialState with focus_by_window, event emission)\n-  navigate.rs     (beam search algorithm — separate card 01KNQXXF5W...)\n+swissarmyhammer-focus/src/\n+  lib.rs            (module declarations)\n+  types.rs          ← THIS CARD: newtypes (WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels) + Rect + Direction\n+  scope.rs          ← THIS CARD: Focusable, FocusZone, FocusScope enum\n+  layer.rs          ← THIS CARD: FocusLayer struct\n+  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops\n+  state.rs          (SpatialState — card 01KNM3YHHFJ3...)\n+  navigate.rs       (BeamNavStrategy impl — card 01KNQXXF5W...)\n+  observer.rs       (FocusEventSink impls — card 01KQ2E7RPBPJ8...)\n ```\n\n-Migration of `focus.rs` → `focus/column.rs` is part of this card's scope; keep the public re-export path (`swissarmyhammer_kanban::focus::resolve_focused_column`) stable.\n+The skeleton + traits exist before this card runs (per dependency on `01KQ2E7RPBPJ8...`). This card fills `types.rs`, `scope.rs`, `layer.rs`, and `registry.rs` with their full implementations.\n+\n+**`swissarmyhammer-kanban/src/focus.rs` is untouched.** That file holds `resolve_focused_column` — kanban-specific column resolver — which stays where it is. The earlier plan to migrate it into a `focus/` subdirectory is canceled.\n\n ### The four peer types\n\n@@ -205,7 +205,7 @@\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-kanban`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the kanban crate:\n+The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the focus crate:\n\n ```rust\n #[tauri::command]\n@@ -238,44 +238,37 @@\n - **`FocusScope` as a Rust enum**: one map, pattern matching.\n\n ### Subtasks\n-- [x] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n-- [x] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n-- [x] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n-- [x] `focus/layer.rs`: define `FocusLayer`\n-- [x] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n-- [x] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n-- [x] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [x] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n-- [x] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n+- [ ] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n+- [ ] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n+- [ ] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n+- [ ] `focus/layer.rs`: define `FocusLayer`\n+- [ ] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n+- [ ] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n+- [ ] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n+- [ ] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n+- [ ] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n\n ## Acceptance Criteria\n-- [x] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n-- [x] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n-- [x] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n-- [x] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [x] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n-- [x] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n-- [x] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n-- [x] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n+- [ ] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n+- [ ] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n+- [ ] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n+- [ ] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [ ] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n+- [ ] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n+- [ ] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n+- [ ] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n\n ## Tests (pure Rust, in `swissarmyhammer-kanban/tests/focus_registry.rs`)\n-- [x] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n-- [x] `Pixels` arithmetic is type-preserving\n-- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [x] Registry: `children_of_zone` direct children only\n-- [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [x] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [x] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [x] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n-- [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n-\n-## Implementation notes\n-\n-- The migration of `focus.rs` → `focus/column.rs` was already completed in the dependency card `01KNM3YHHFJ3PTXZHD9EFKVBS6`; this card extends `focus/types.rs` (adding `Pixels`/`Rect`) and adds the new `focus/scope.rs`, `focus/layer.rs`, `focus/registry.rs` modules alongside the existing `focus/state.rs`.\n-- `SpatialState` (per-window focus tracker) and `SpatialRegistry` (geometry / hierarchy backing store) are intentionally kept as separate concerns; the Tauri register adapters write to both. `SpatialState` mutates frequently (every focus move); `SpatialRegistry` mutates only on mount/unmount/resize.\n-- `spatial_drill_in` / `spatial_drill_out` Tauri commands are pinned with `Result<(), String>` no-op bodies pending the navigator card `01KNQXXF5W...`, matching the existing `spatial_navigate` pattern.\n-- TypeScript `Rect` and `FocusOverrides` types and the updated `useSpatialFocusActions` shape (with `updateRect`, `drillIn`, `drillOut`) ship alongside the Rust kernel so the IPC surface is internally consistent.\n+- [ ] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n+- [ ] `Pixels` arithmetic is type-preserving\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [ ] Registry: `children_of_zone` direct children only\n+- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [ ] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n+- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ2EA6XMBEWG8V0ZZ2SK5XS2","timestamp":"2026-04-25T13:47:22.420848Z","op":"update","item_id":"01KNQXW7HHHB8HW76K3PXH3G34","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KNM3YHHFJ3PTXZHD9EFKVBS6\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Rust kernel: Focusable, FocusZone, FocusLayer, FocusScope types + SpatialRegistry'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KNM3YHHFJ3PTXZHD9EFKVBS6\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n-position_column: review\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Rust kernel: Focusable, FocusZone, FocusLayer, FocusScope types + SpatialRegistry'\n ---\n","transaction_id":null}
{"id":"01KQ2EA6XNF1E7NZVSPXZSJ9NG","timestamp":"2026-04-25T13:47:22.421540Z","entity_type":"task","entity_id":"01KNQXW7HHHB8HW76K3PXH3G34","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ2EC5X02YWV3TCD5XYKXNCW","timestamp":"2026-04-25T13:48:26.912702Z","op":"update","item_id":"01KNQXW7HHHB8HW76K3PXH3G34","forward_patch":"--- original\n+++ modified\n@@ -11,36 +11,36 @@\n ---\n ## What\n\n-Define the Rust kernel types for spatial focus. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n+Define the Rust kernel types for spatial focus inside the new `swissarmyhammer-focus` crate. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n ### Crate placement\n\n-Following the pattern established in commit `b81336d42` (move GUI-crate logic into headless-testable kanban crate), **this lives in `swissarmyhammer-kanban`**, not `swissarmyhammer-commands`. Rationale: spatial nav is business logic that must be testable without Tauri or jsdom; `swissarmyhammer-commands` stays Tier 0 (no app state, no windowing concepts).\n-\n-Specifically, organize as a submodule alongside the existing `focus.rs`:\n+**Lives in `swissarmyhammer-focus`** (created by card `01KQ2E7RPBPJ8T8KZX39N2SZ0A`). Spatial focus is generic — opaque `Moniker` strings, abstract `Rect`s, `WindowLabel`s. No kanban concepts, no Tauri, no domain types. Putting it in `swissarmyhammer-kanban` was wrong; the dedicated crate makes it reusable, easier to test, and keeps the kanban crate focused on board logic.\n\n ```\n-swissarmyhammer-kanban/src/focus/\n-  mod.rs          (re-exports; existing resolve_focused_column migrates here)\n-  column.rs       (existing resolve_focused_column logic)\n-  types.rs        (newtypes: WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels, Rect, Direction)\n-  scope.rs        (Focusable, FocusZone, FocusScope enum)\n-  layer.rs        (FocusLayer struct)\n-  registry.rs     (SpatialRegistry with scope + layer ops)\n-  state.rs        (SpatialState with focus_by_window, event emission)\n-  navigate.rs     (beam search algorithm — separate card 01KNQXXF5W...)\n+swissarmyhammer-focus/src/\n+  lib.rs            (module declarations)\n+  types.rs          ← THIS CARD: newtypes (WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels) + Rect + Direction\n+  scope.rs          ← THIS CARD: Focusable, FocusZone, FocusScope enum\n+  layer.rs          ← THIS CARD: FocusLayer struct\n+  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops\n+  state.rs          (SpatialState — card 01KNM3YHHFJ3...)\n+  navigate.rs       (BeamNavStrategy impl — card 01KNQXXF5W...)\n+  observer.rs       (FocusEventSink impls — card 01KQ2E7RPBPJ8...)\n ```\n\n-Migration of `focus.rs` → `focus/column.rs` is part of this card's scope; keep the public re-export path (`swissarmyhammer_kanban::focus::resolve_focused_column`) stable.\n+The crate skeleton + traits exist before this card runs (per the `01KQ2E7RPBPJ8...` dependency). This card fills `types.rs`, `scope.rs`, `layer.rs`, and `registry.rs`.\n+\n+**`swissarmyhammer-kanban/src/focus.rs` is untouched** — that file holds `resolve_focused_column`, kanban-specific column resolver, which stays where it is. The earlier plan to migrate it to a `focus/` subdirectory is canceled.\n\n ### The four peer types\n\n-| Role                | React component     | Rust type             |\n-|---------------------|---------------------|-----------------------|\n-| Leaf focusable point| `Focusable`         | `struct Focusable`    |\n-| Navigable container | `FocusZone`         | `struct FocusZone`    |\n-| Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n-| Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n+| Role                | React component     | Rust type                                    |\n+|---------------------|---------------------|----------------------------------------------|\n+| Leaf focusable point| `Focusable`         | `swissarmyhammer_focus::Focusable`           |\n+| Navigable container | `FocusZone`         | `swissarmyhammer_focus::FocusZone`           |\n+| Modal layer boundary| `FocusLayer`        | `swissarmyhammer_focus::FocusLayer`          |\n+| Entity-aware wrapper| `FocusScope`        | `swissarmyhammer_focus::FocusScope` (enum)   |\n\n ### Terminology — canonical definitions\n\n@@ -67,7 +67,7 @@\n - Identified by `SpatialKey`.\n\n **Scope** (`FocusScope`, the **umbrella term**)\n-- On Rust: the sum type `enum FocusScope { Focusable(Focusable), Zone(FocusZone) }`. This is what the registry stores per `SpatialKey`. Pattern matching distinguishes leaf vs container.\n+- On Rust: the sum type `enum FocusScope { Focusable(Focusable), Zone(FocusZone) }`. This is what the registry stores per `SpatialKey`.\n - On React: the **composite wrapper component** that adds entity plumbing (`CommandScope`, click-to-focus, context menu, focus bar, data-moniker) on top of one primitive (`<Focusable>` or `<FocusZone>`, picked via the `kind` prop).\n - A \"scope\" in spatial-nav speak = \"any registered focus point\" = either a Focusable or a FocusZone. Not a Layer.\n - \"Scope chain\" / `parent_zone` chain = walk from focused leaf up through ancestor zones to the layer root.\n@@ -74,21 +74,12 @@\n\n ### Disambiguation: `CommandScope` is a separate concept\n\n-The existing codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary, with its own `parent` chain used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`.\n-\n-How they relate:\n-- The composite React `<FocusScope>` component creates **both** a spatial entry (Focusable or Zone) **and** a `CommandScope` for the same moniker. They share the moniker; they walk parallel chains (parent_zone chain in the registry; parent chain in CommandScope).\n-- The primitives (`<Focusable>` / `<FocusZone>` / `<FocusLayer>`) by themselves do **not** create a `CommandScope`. Use them when you only need spatial registration without command dispatch (e.g., generic UI chrome). Use the composite `<FocusScope>` for entity-aware UI where commands matter.\n-- `FocusLayer` is purely a modal boundary; it has no command-dispatch role.\n+The existing kanban codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`. The composite `<FocusScope>` React component creates **both** a spatial entry (in `swissarmyhammer-focus`) **and** a `CommandScope` (in the kanban app). The two systems share the moniker but are otherwise independent.\n\n-When in doubt, \"scope\" without qualifier in this plan means a spatial scope (`FocusScope` enum) — the focusable thing in the registry. Where command dispatch is meant, the cards say `CommandScope` explicitly.\n+`swissarmyhammer-focus` itself has no concept of `CommandScope` — it's pure spatial-nav, generic across consumers.\n\n ### Newtype discipline — use `swissarmyhammer_common::define_id!`\n\n-The project already has a canonical newtype macro: `define_id!` from `swissarmyhammer-common`. It provides `#[serde(transparent)]`, `Display`, `AsRef<str>`, `From<&str>`/`From<String>`, `Deref`, `Borrow<str>`, `FromStr`, `PartialEq<str>`, plus `new()` (fresh ULID), `from_string()`, `as_str()`. All existing ID types (`TaskId`, `ColumnId`, `TagId`, etc.) use it.\n-\n-Define the string-valued newtypes with this macro:\n-\n ```rust\n use swissarmyhammer_common::define_id;\n\n@@ -99,7 +90,7 @@\n define_id!(LayerName,  \"Layer role: \\\"window\\\", \\\"inspector\\\", \\\"dialog\\\", \\\"palette\\\"\");\n ```\n\n-`Pixels` is numeric, not string — hand-rolled:\n+`Pixels` is numeric — hand-rolled with arithmetic so beam math stays type-safe:\n\n ```rust\n #[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n@@ -106,7 +97,6 @@\n #[serde(transparent)]\n pub struct Pixels(pub f64);\n\n-// Arithmetic so beam/score math doesn't drop to f64:\n impl std::ops::Add for Pixels { /* ... */ }\n impl std::ops::Sub for Pixels { /* ... */ }\n impl std::ops::Mul<f64> for Pixels { /* ... */ }\n@@ -118,17 +108,13 @@\n ```rust\n #[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n pub struct Rect {\n-    pub x: Pixels,\n-    pub y: Pixels,\n-    pub width: Pixels,\n-    pub height: Pixels,\n+    pub x: Pixels, pub y: Pixels, pub width: Pixels, pub height: Pixels,\n }\n-\n impl Rect {\n-    pub fn top(&self)    -> Pixels { self.y }\n-    pub fn left(&self)   -> Pixels { self.x }\n+    pub fn top(&self) -> Pixels { self.y }\n+    pub fn left(&self) -> Pixels { self.x }\n     pub fn bottom(&self) -> Pixels { self.y + self.height }\n-    pub fn right(&self)  -> Pixels { self.x + self.width }\n+    pub fn right(&self) -> Pixels { self.x + self.width }\n }\n ```\n\n@@ -216,77 +202,49 @@\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-kanban`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the kanban crate:\n+The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate.\n\n-```rust\n-#[tauri::command]\n-pub async fn spatial_register_focusable(\n-    window: tauri::Window,\n-    state: State<'_, AppState>,\n-    key: SpatialKey,\n-    moniker: Moniker,\n-    rect: Rect,\n-    layer_key: LayerKey,\n-    parent_zone: Option<SpatialKey>,\n-    overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<(), String>;\n+### Tests — `swissarmyhammer-focus/tests/registry.rs`\n\n-// Likewise: spatial_register_zone, spatial_unregister_scope, spatial_update_rect,\n-//           spatial_push_layer, spatial_pop_layer, spatial_focus, spatial_navigate,\n-//           spatial_drill_in, spatial_drill_out\n-```\n+Pure-Rust, no Tauri, no jsdom, no kanban. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n\n-### Tests — `swissarmyhammer-kanban/tests/focus_registry.rs`\n-\n-Follow the pattern of `swissarmyhammer-kanban/tests/resolve_focused_column.rs` and `dynamic_sources_headless.rs` — pure-Rust, no Tauri, no jsdom. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n-\n ### Design decisions\n\n-- **`swissarmyhammer-kanban` not `swissarmyhammer-commands`**: matches the refactor pattern — business logic over Tier 0.\n+- **`swissarmyhammer-focus`, not `swissarmyhammer-kanban`**: spatial nav is generic — opaque monikers, abstract rects. Pulling it out keeps it reusable and keeps the kanban crate focused. (`swissarmyhammer-commands` stays Tier 0.)\n - **`define_id!` macro reuse**: consistent with `TaskId`/`ColumnId`/`TagId`; avoids hand-rolled boilerplate.\n-- **`focus/` submodule**: groups all focus concerns (column resolver + spatial registry) together.\n-- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked.\n+- **Four peer types, not a `kind` field on one struct**: zone-only fields are type-checked.\n - **`FocusScope` as a Rust enum**: one map, pattern matching.\n+- **`Pixels` arithmetic**: prevents accidental mixing of pixel values with unrelated floats.\n\n ### Subtasks\n-- [x] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n-- [x] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n-- [x] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n-- [x] `focus/layer.rs`: define `FocusLayer`\n-- [x] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n-- [x] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n-- [x] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [x] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n-- [x] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n+- [ ] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n+- [ ] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n+- [ ] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n+- [ ] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n+- [ ] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n+- [ ] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window`\n\n ## Acceptance Criteria\n-- [x] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n-- [x] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n-- [x] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n-- [x] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [x] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n-- [x] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n-- [x] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n-- [x] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n+- [ ] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n+- [ ] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n+- [ ] No bare `String` or `f64` on any public type / command signature\n+- [ ] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [ ] `Pixels` supports arithmetic without `.0` access\n+- [ ] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n+- [ ] Tauri commands derive `WindowLabel` from `tauri::Window`\n+- [ ] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n\n-## Tests (pure Rust, in `swissarmyhammer-kanban/tests/focus_registry.rs`)\n-- [x] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n-- [x] `Pixels` arithmetic is type-preserving\n-- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [x] Registry: `children_of_zone` direct children only\n-- [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [x] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [x] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [x] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n-- [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n-\n-## Implementation notes\n-\n-- The migration of `focus.rs` → `focus/column.rs` was already completed in the dependency card `01KNM3YHHFJ3PTXZHD9EFKVBS6`; this card extends `focus/types.rs` (adding `Pixels`/`Rect`) and adds the new `focus/scope.rs`, `focus/layer.rs`, `focus/registry.rs` modules alongside the existing `focus/state.rs`.\n-- `SpatialState` (per-window focus tracker) and `SpatialRegistry` (geometry / hierarchy backing store) are intentionally kept as separate concerns; the Tauri register adapters write to both. `SpatialState` mutates frequently (every focus move); `SpatialRegistry` mutates only on mount/unmount/resize.\n-- `spatial_drill_in` / `spatial_drill_out` Tauri commands are pinned with `Result<(), String>` no-op bodies pending the navigator card `01KNQXXF5W...`, matching the existing `spatial_navigate` pattern.\n-- TypeScript `Rect` and `FocusOverrides` types and the updated `useSpatialFocusActions` shape (with `updateRect`, `drillIn`, `drillOut`) ship alongside the Rust kernel so the IPC surface is internally consistent.\n+## Tests (in `swissarmyhammer-focus/tests/registry.rs`)\n+- [ ] Each newtype JSON-round-trips as a bare primitive\n+- [ ] `Pixels` arithmetic is type-preserving\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [ ] Registry: `children_of_zone` direct children only\n+- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [ ] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n+- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -11,36 +11,36 @@\n ---\n ## What\n\n-Define the Rust kernel types for spatial focus inside the new `swissarmyhammer-focus` crate. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n+Define the Rust kernel types for spatial focus. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n ### Crate placement\n\n-**Lives in `swissarmyhammer-focus`** (created by card `01KQ2E7RPBPJ8T8KZX39N2SZ0A`). Spatial focus is generic — opaque `Moniker` strings, abstract `Rect`s, `WindowLabel`s. No kanban concepts, no Tauri, no domain types. Putting it in `swissarmyhammer-kanban` was wrong; the dedicated crate makes it reusable, easier to test, and keeps the kanban crate focused on board logic.\n+Following the pattern established in commit `b81336d42` (move GUI-crate logic into headless-testable kanban crate), **this lives in `swissarmyhammer-kanban`**, not `swissarmyhammer-commands`. Rationale: spatial nav is business logic that must be testable without Tauri or jsdom; `swissarmyhammer-commands` stays Tier 0 (no app state, no windowing concepts).\n+\n+Specifically, organize as a submodule alongside the existing `focus.rs`:\n\n ```\n-swissarmyhammer-focus/src/\n-  lib.rs            (module declarations)\n-  types.rs          ← THIS CARD: newtypes (WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels) + Rect + Direction\n-  scope.rs          ← THIS CARD: Focusable, FocusZone, FocusScope enum\n-  layer.rs          ← THIS CARD: FocusLayer struct\n-  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops\n-  state.rs          (SpatialState — card 01KNM3YHHFJ3...)\n-  navigate.rs       (BeamNavStrategy impl — card 01KNQXXF5W...)\n-  observer.rs       (FocusEventSink impls — card 01KQ2E7RPBPJ8...)\n+swissarmyhammer-kanban/src/focus/\n+  mod.rs          (re-exports; existing resolve_focused_column migrates here)\n+  column.rs       (existing resolve_focused_column logic)\n+  types.rs        (newtypes: WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels, Rect, Direction)\n+  scope.rs        (Focusable, FocusZone, FocusScope enum)\n+  layer.rs        (FocusLayer struct)\n+  registry.rs     (SpatialRegistry with scope + layer ops)\n+  state.rs        (SpatialState with focus_by_window, event emission)\n+  navigate.rs     (beam search algorithm — separate card 01KNQXXF5W...)\n ```\n\n-The crate skeleton + traits exist before this card runs (per the `01KQ2E7RPBPJ8...` dependency). This card fills `types.rs`, `scope.rs`, `layer.rs`, and `registry.rs`.\n-\n-**`swissarmyhammer-kanban/src/focus.rs` is untouched** — that file holds `resolve_focused_column`, kanban-specific column resolver, which stays where it is. The earlier plan to migrate it to a `focus/` subdirectory is canceled.\n+Migration of `focus.rs` → `focus/column.rs` is part of this card's scope; keep the public re-export path (`swissarmyhammer_kanban::focus::resolve_focused_column`) stable.\n\n ### The four peer types\n\n-| Role                | React component     | Rust type                                    |\n-|---------------------|---------------------|----------------------------------------------|\n-| Leaf focusable point| `Focusable`         | `swissarmyhammer_focus::Focusable`           |\n-| Navigable container | `FocusZone`         | `swissarmyhammer_focus::FocusZone`           |\n-| Modal layer boundary| `FocusLayer`        | `swissarmyhammer_focus::FocusLayer`          |\n-| Entity-aware wrapper| `FocusScope`        | `swissarmyhammer_focus::FocusScope` (enum)   |\n+| Role                | React component     | Rust type             |\n+|---------------------|---------------------|-----------------------|\n+| Leaf focusable point| `Focusable`         | `struct Focusable`    |\n+| Navigable container | `FocusZone`         | `struct FocusZone`    |\n+| Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n+| Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n\n ### Terminology — canonical definitions\n\n@@ -67,7 +67,7 @@\n - Identified by `SpatialKey`.\n\n **Scope** (`FocusScope`, the **umbrella term**)\n-- On Rust: the sum type `enum FocusScope { Focusable(Focusable), Zone(FocusZone) }`. This is what the registry stores per `SpatialKey`.\n+- On Rust: the sum type `enum FocusScope { Focusable(Focusable), Zone(FocusZone) }`. This is what the registry stores per `SpatialKey`. Pattern matching distinguishes leaf vs container.\n - On React: the **composite wrapper component** that adds entity plumbing (`CommandScope`, click-to-focus, context menu, focus bar, data-moniker) on top of one primitive (`<Focusable>` or `<FocusZone>`, picked via the `kind` prop).\n - A \"scope\" in spatial-nav speak = \"any registered focus point\" = either a Focusable or a FocusZone. Not a Layer.\n - \"Scope chain\" / `parent_zone` chain = walk from focused leaf up through ancestor zones to the layer root.\n@@ -74,12 +74,21 @@\n\n ### Disambiguation: `CommandScope` is a separate concept\n\n-The existing kanban codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`. The composite `<FocusScope>` React component creates **both** a spatial entry (in `swissarmyhammer-focus`) **and** a `CommandScope` (in the kanban app). The two systems share the moniker but are otherwise independent.\n+The existing codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary, with its own `parent` chain used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`.\n+\n+How they relate:\n+- The composite React `<FocusScope>` component creates **both** a spatial entry (Focusable or Zone) **and** a `CommandScope` for the same moniker. They share the moniker; they walk parallel chains (parent_zone chain in the registry; parent chain in CommandScope).\n+- The primitives (`<Focusable>` / `<FocusZone>` / `<FocusLayer>`) by themselves do **not** create a `CommandScope`. Use them when you only need spatial registration without command dispatch (e.g., generic UI chrome). Use the composite `<FocusScope>` for entity-aware UI where commands matter.\n+- `FocusLayer` is purely a modal boundary; it has no command-dispatch role.\n\n-`swissarmyhammer-focus` itself has no concept of `CommandScope` — it's pure spatial-nav, generic across consumers.\n+When in doubt, \"scope\" without qualifier in this plan means a spatial scope (`FocusScope` enum) — the focusable thing in the registry. Where command dispatch is meant, the cards say `CommandScope` explicitly.\n\n ### Newtype discipline — use `swissarmyhammer_common::define_id!`\n\n+The project already has a canonical newtype macro: `define_id!` from `swissarmyhammer-common`. It provides `#[serde(transparent)]`, `Display`, `AsRef<str>`, `From<&str>`/`From<String>`, `Deref`, `Borrow<str>`, `FromStr`, `PartialEq<str>`, plus `new()` (fresh ULID), `from_string()`, `as_str()`. All existing ID types (`TaskId`, `ColumnId`, `TagId`, etc.) use it.\n+\n+Define the string-valued newtypes with this macro:\n+\n ```rust\n use swissarmyhammer_common::define_id;\n\n@@ -90,7 +99,7 @@\n define_id!(LayerName,  \"Layer role: \\\"window\\\", \\\"inspector\\\", \\\"dialog\\\", \\\"palette\\\"\");\n ```\n\n-`Pixels` is numeric — hand-rolled with arithmetic so beam math stays type-safe:\n+`Pixels` is numeric, not string — hand-rolled:\n\n ```rust\n #[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n@@ -97,6 +106,7 @@\n #[serde(transparent)]\n pub struct Pixels(pub f64);\n\n+// Arithmetic so beam/score math doesn't drop to f64:\n impl std::ops::Add for Pixels { /* ... */ }\n impl std::ops::Sub for Pixels { /* ... */ }\n impl std::ops::Mul<f64> for Pixels { /* ... */ }\n@@ -108,13 +118,17 @@\n ```rust\n #[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n pub struct Rect {\n-    pub x: Pixels, pub y: Pixels, pub width: Pixels, pub height: Pixels,\n+    pub x: Pixels,\n+    pub y: Pixels,\n+    pub width: Pixels,\n+    pub height: Pixels,\n }\n+\n impl Rect {\n-    pub fn top(&self) -> Pixels { self.y }\n-    pub fn left(&self) -> Pixels { self.x }\n+    pub fn top(&self)    -> Pixels { self.y }\n+    pub fn left(&self)   -> Pixels { self.x }\n     pub fn bottom(&self) -> Pixels { self.y + self.height }\n-    pub fn right(&self) -> Pixels { self.x + self.width }\n+    pub fn right(&self)  -> Pixels { self.x + self.width }\n }\n ```\n\n@@ -202,49 +216,77 @@\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate.\n+The headless registry lives in `swissarmyhammer-kanban`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the kanban crate:\n+\n+```rust\n+#[tauri::command]\n+pub async fn spatial_register_focusable(\n+    window: tauri::Window,\n+    state: State<'_, AppState>,\n+    key: SpatialKey,\n+    moniker: Moniker,\n+    rect: Rect,\n+    layer_key: LayerKey,\n+    parent_zone: Option<SpatialKey>,\n+    overrides: HashMap<Direction, Option<Moniker>>,\n+) -> Result<(), String>;\n+\n+// Likewise: spatial_register_zone, spatial_unregister_scope, spatial_update_rect,\n+//           spatial_push_layer, spatial_pop_layer, spatial_focus, spatial_navigate,\n+//           spatial_drill_in, spatial_drill_out\n+```\n\n-### Tests — `swissarmyhammer-focus/tests/registry.rs`\n+### Tests — `swissarmyhammer-kanban/tests/focus_registry.rs`\n\n-Pure-Rust, no Tauri, no jsdom, no kanban. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n+Follow the pattern of `swissarmyhammer-kanban/tests/resolve_focused_column.rs` and `dynamic_sources_headless.rs` — pure-Rust, no Tauri, no jsdom. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n\n ### Design decisions\n\n-- **`swissarmyhammer-focus`, not `swissarmyhammer-kanban`**: spatial nav is generic — opaque monikers, abstract rects. Pulling it out keeps it reusable and keeps the kanban crate focused. (`swissarmyhammer-commands` stays Tier 0.)\n+- **`swissarmyhammer-kanban` not `swissarmyhammer-commands`**: matches the refactor pattern — business logic over Tier 0.\n - **`define_id!` macro reuse**: consistent with `TaskId`/`ColumnId`/`TagId`; avoids hand-rolled boilerplate.\n-- **Four peer types, not a `kind` field on one struct**: zone-only fields are type-checked.\n+- **`focus/` submodule**: groups all focus concerns (column resolver + spatial registry) together.\n+- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked.\n - **`FocusScope` as a Rust enum**: one map, pattern matching.\n-- **`Pixels` arithmetic**: prevents accidental mixing of pixel values with unrelated floats.\n\n ### Subtasks\n-- [ ] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n-- [ ] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n-- [ ] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n-- [ ] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n-- [ ] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n-- [ ] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window`\n+- [x] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n+- [x] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n+- [x] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n+- [x] `focus/layer.rs`: define `FocusLayer`\n+- [x] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n+- [x] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n+- [x] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n+- [x] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n+- [x] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n\n ## Acceptance Criteria\n-- [ ] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n-- [ ] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n-- [ ] No bare `String` or `f64` on any public type / command signature\n-- [ ] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [ ] `Pixels` supports arithmetic without `.0` access\n-- [ ] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n-- [ ] Tauri commands derive `WindowLabel` from `tauri::Window`\n-- [ ] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n+- [x] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n+- [x] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n+- [x] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n+- [x] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [x] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n+- [x] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n+- [x] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n+- [x] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n+\n+## Tests (pure Rust, in `swissarmyhammer-kanban/tests/focus_registry.rs`)\n+- [x] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n+- [x] `Pixels` arithmetic is type-preserving\n+- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [x] Registry: `children_of_zone` direct children only\n+- [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [x] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [x] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [x] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n+- [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+\n+## Implementation notes\n\n-## Tests (in `swissarmyhammer-focus/tests/registry.rs`)\n-- [ ] Each newtype JSON-round-trips as a bare primitive\n-- [ ] `Pixels` arithmetic is type-preserving\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` direct children only\n-- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [ ] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n-- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+- The migration of `focus.rs` → `focus/column.rs` was already completed in the dependency card `01KNM3YHHFJ3PTXZHD9EFKVBS6`; this card extends `focus/types.rs` (adding `Pixels`/`Rect`) and adds the new `focus/scope.rs`, `focus/layer.rs`, `focus/registry.rs` modules alongside the existing `focus/state.rs`.\n+- `SpatialState` (per-window focus tracker) and `SpatialRegistry` (geometry / hierarchy backing store) are intentionally kept as separate concerns; the Tauri register adapters write to both. `SpatialState` mutates frequently (every focus move); `SpatialRegistry` mutates only on mount/unmount/resize.\n+- `spatial_drill_in` / `spatial_drill_out` Tauri commands are pinned with `Result<(), String>` no-op bodies pending the navigator card `01KNQXXF5W...`, matching the existing `spatial_navigate` pattern.\n+- TypeScript `Rect` and `FocusOverrides` types and the updated `useSpatialFocusActions` shape (with `updateRect`, `drillIn`, `drillOut`) ship alongside the Rust kernel so the IPC surface is internally consistent.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ2EC5XFN589QPCSS4W7WX90","timestamp":"2026-04-25T13:48:26.927991Z","entity_type":"task","entity_id":"01KNQXW7HHHB8HW76K3PXH3G34","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,35 +1,35 @@\n ## What\n\n-Define the Rust kernel types for spatial focus. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n+Define the Rust kernel types for spatial focus inside the new `swissarmyhammer-focus` crate. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n ### Crate placement\n\n-Following the pattern established in commit `b81336d42` (move GUI-crate logic into headless-testable kanban crate), **this lives in `swissarmyhammer-kanban`**, not `swissarmyhammer-commands`. Rationale: spatial nav is business logic that must be testable without Tauri or jsdom; `swissarmyhammer-commands` stays Tier 0 (no app state, no windowing concepts).\n-\n-Specifically, organize as a submodule alongside the existing `focus.rs`:\n+**Lives in `swissarmyhammer-focus`** (created by card `01KQ2E7RPBPJ8T8KZX39N2SZ0A`). Spatial focus is generic — opaque `Moniker` strings, abstract `Rect`s, `WindowLabel`s. No kanban concepts, no Tauri, no domain types. Putting it in `swissarmyhammer-kanban` was wrong; the dedicated crate makes it reusable, easier to test, and keeps the kanban crate focused on board logic.\n\n ```\n-swissarmyhammer-kanban/src/focus/\n-  mod.rs          (re-exports; existing resolve_focused_column migrates here)\n-  column.rs       (existing resolve_focused_column logic)\n-  types.rs        (newtypes: WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels, Rect, Direction)\n-  scope.rs        (Focusable, FocusZone, FocusScope enum)\n-  layer.rs        (FocusLayer struct)\n-  registry.rs     (SpatialRegistry with scope + layer ops)\n-  state.rs        (SpatialState with focus_by_window, event emission)\n-  navigate.rs     (beam search algorithm — separate card 01KNQXXF5W...)\n+swissarmyhammer-focus/src/\n+  lib.rs            (module declarations)\n+  types.rs          ← THIS CARD: newtypes (WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels) + Rect + Direction\n+  scope.rs          ← THIS CARD: Focusable, FocusZone, FocusScope enum\n+  layer.rs          ← THIS CARD: FocusLayer struct\n+  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops\n+  state.rs          (SpatialState — card 01KNM3YHHFJ3...)\n+  navigate.rs       (BeamNavStrategy impl — card 01KNQXXF5W...)\n+  observer.rs       (FocusEventSink impls — card 01KQ2E7RPBPJ8...)\n ```\n\n-Migration of `focus.rs` → `focus/column.rs` is part of this card's scope; keep the public re-export path (`swissarmyhammer_kanban::focus::resolve_focused_column`) stable.\n+The crate skeleton + traits exist before this card runs (per the `01KQ2E7RPBPJ8...` dependency). This card fills `types.rs`, `scope.rs`, `layer.rs`, and `registry.rs`.\n+\n+**`swissarmyhammer-kanban/src/focus.rs` is untouched** — that file holds `resolve_focused_column`, kanban-specific column resolver, which stays where it is. The earlier plan to migrate it to a `focus/` subdirectory is canceled.\n\n ### The four peer types\n\n-| Role                | React component     | Rust type             |\n-|---------------------|---------------------|-----------------------|\n-| Leaf focusable point| `Focusable`         | `struct Focusable`    |\n-| Navigable container | `FocusZone`         | `struct FocusZone`    |\n-| Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n-| Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n+| Role                | React component     | Rust type                                    |\n+|---------------------|---------------------|----------------------------------------------|\n+| Leaf focusable point| `Focusable`         | `swissarmyhammer_focus::Focusable`           |\n+| Navigable container | `FocusZone`         | `swissarmyhammer_focus::FocusZone`           |\n+| Modal layer boundary| `FocusLayer`        | `swissarmyhammer_focus::FocusLayer`          |\n+| Entity-aware wrapper| `FocusScope`        | `swissarmyhammer_focus::FocusScope` (enum)   |\n\n ### Terminology — canonical definitions\n\n@@ -56,7 +56,7 @@\n - Identified by `SpatialKey`.\n\n **Scope** (`FocusScope`, the **umbrella term**)\n-- On Rust: the sum type `enum FocusScope { Focusable(Focusable), Zone(FocusZone) }`. This is what the registry stores per `SpatialKey`. Pattern matching distinguishes leaf vs container.\n+- On Rust: the sum type `enum FocusScope { Focusable(Focusable), Zone(FocusZone) }`. This is what the registry stores per `SpatialKey`.\n - On React: the **composite wrapper component** that adds entity plumbing (`CommandScope`, click-to-focus, context menu, focus bar, data-moniker) on top of one primitive (`<Focusable>` or `<FocusZone>`, picked via the `kind` prop).\n - A \"scope\" in spatial-nav speak = \"any registered focus point\" = either a Focusable or a FocusZone. Not a Layer.\n - \"Scope chain\" / `parent_zone` chain = walk from focused leaf up through ancestor zones to the layer root.\n@@ -63,21 +63,12 @@\n\n ### Disambiguation: `CommandScope` is a separate concept\n\n-The existing codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary, with its own `parent` chain used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`.\n-\n-How they relate:\n-- The composite React `<FocusScope>` component creates **both** a spatial entry (Focusable or Zone) **and** a `CommandScope` for the same moniker. They share the moniker; they walk parallel chains (parent_zone chain in the registry; parent chain in CommandScope).\n-- The primitives (`<Focusable>` / `<FocusZone>` / `<FocusLayer>`) by themselves do **not** create a `CommandScope`. Use them when you only need spatial registration without command dispatch (e.g., generic UI chrome). Use the composite `<FocusScope>` for entity-aware UI where commands matter.\n-- `FocusLayer` is purely a modal boundary; it has no command-dispatch role.\n+The existing kanban codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`. The composite `<FocusScope>` React component creates **both** a spatial entry (in `swissarmyhammer-focus`) **and** a `CommandScope` (in the kanban app). The two systems share the moniker but are otherwise independent.\n\n-When in doubt, \"scope\" without qualifier in this plan means a spatial scope (`FocusScope` enum) — the focusable thing in the registry. Where command dispatch is meant, the cards say `CommandScope` explicitly.\n+`swissarmyhammer-focus` itself has no concept of `CommandScope` — it's pure spatial-nav, generic across consumers.\n\n ### Newtype discipline — use `swissarmyhammer_common::define_id!`\n\n-The project already has a canonical newtype macro: `define_id!` from `swissarmyhammer-common`. It provides `#[serde(transparent)]`, `Display`, `AsRef<str>`, `From<&str>`/`From<String>`, `Deref`, `Borrow<str>`, `FromStr`, `PartialEq<str>`, plus `new()` (fresh ULID), `from_string()`, `as_str()`. All existing ID types (`TaskId`, `ColumnId`, `TagId`, etc.) use it.\n-\n-Define the string-valued newtypes with this macro:\n-\n ```rust\n use swissarmyhammer_common::define_id;\n\n@@ -88,7 +79,7 @@\n define_id!(LayerName,  \"Layer role: \\\"window\\\", \\\"inspector\\\", \\\"dialog\\\", \\\"palette\\\"\");\n ```\n\n-`Pixels` is numeric, not string — hand-rolled:\n+`Pixels` is numeric — hand-rolled with arithmetic so beam math stays type-safe:\n\n ```rust\n #[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n@@ -95,7 +86,6 @@\n #[serde(transparent)]\n pub struct Pixels(pub f64);\n\n-// Arithmetic so beam/score math doesn't drop to f64:\n impl std::ops::Add for Pixels { /* ... */ }\n impl std::ops::Sub for Pixels { /* ... */ }\n impl std::ops::Mul<f64> for Pixels { /* ... */ }\n@@ -107,17 +97,13 @@\n ```rust\n #[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n pub struct Rect {\n-    pub x: Pixels,\n-    pub y: Pixels,\n-    pub width: Pixels,\n-    pub height: Pixels,\n+    pub x: Pixels, pub y: Pixels, pub width: Pixels, pub height: Pixels,\n }\n-\n impl Rect {\n-    pub fn top(&self)    -> Pixels { self.y }\n-    pub fn left(&self)   -> Pixels { self.x }\n+    pub fn top(&self) -> Pixels { self.y }\n+    pub fn left(&self) -> Pixels { self.x }\n     pub fn bottom(&self) -> Pixels { self.y + self.height }\n-    pub fn right(&self)  -> Pixels { self.x + self.width }\n+    pub fn right(&self) -> Pixels { self.x + self.width }\n }\n ```\n\n@@ -205,77 +191,49 @@\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-kanban`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the kanban crate:\n+The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate.\n\n-```rust\n-#[tauri::command]\n-pub async fn spatial_register_focusable(\n-    window: tauri::Window,\n-    state: State<'_, AppState>,\n-    key: SpatialKey,\n-    moniker: Moniker,\n-    rect: Rect,\n-    layer_key: LayerKey,\n-    parent_zone: Option<SpatialKey>,\n-    overrides: HashMap<Direction, Option<Moniker>>,\n-) -> Result<(), String>;\n+### Tests — `swissarmyhammer-focus/tests/registry.rs`\n\n-// Likewise: spatial_register_zone, spatial_unregister_scope, spatial_update_rect,\n-//           spatial_push_layer, spatial_pop_layer, spatial_focus, spatial_navigate,\n-//           spatial_drill_in, spatial_drill_out\n-```\n+Pure-Rust, no Tauri, no jsdom, no kanban. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n\n-### Tests — `swissarmyhammer-kanban/tests/focus_registry.rs`\n-\n-Follow the pattern of `swissarmyhammer-kanban/tests/resolve_focused_column.rs` and `dynamic_sources_headless.rs` — pure-Rust, no Tauri, no jsdom. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n-\n ### Design decisions\n\n-- **`swissarmyhammer-kanban` not `swissarmyhammer-commands`**: matches the refactor pattern — business logic over Tier 0.\n+- **`swissarmyhammer-focus`, not `swissarmyhammer-kanban`**: spatial nav is generic — opaque monikers, abstract rects. Pulling it out keeps it reusable and keeps the kanban crate focused. (`swissarmyhammer-commands` stays Tier 0.)\n - **`define_id!` macro reuse**: consistent with `TaskId`/`ColumnId`/`TagId`; avoids hand-rolled boilerplate.\n-- **`focus/` submodule**: groups all focus concerns (column resolver + spatial registry) together.\n-- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked.\n+- **Four peer types, not a `kind` field on one struct**: zone-only fields are type-checked.\n - **`FocusScope` as a Rust enum**: one map, pattern matching.\n+- **`Pixels` arithmetic**: prevents accidental mixing of pixel values with unrelated floats.\n\n ### Subtasks\n-- [x] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n-- [x] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n-- [x] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n-- [x] `focus/layer.rs`: define `FocusLayer`\n-- [x] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n-- [x] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n-- [x] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n-- [x] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n-- [x] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n+- [ ] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n+- [ ] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n+- [ ] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n+- [ ] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n+- [ ] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n+- [ ] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window`\n\n ## Acceptance Criteria\n-- [x] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n-- [x] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n-- [x] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n-- [x] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [x] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n-- [x] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n-- [x] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n-- [x] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n+- [ ] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n+- [ ] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n+- [ ] No bare `String` or `f64` on any public type / command signature\n+- [ ] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [ ] `Pixels` supports arithmetic without `.0` access\n+- [ ] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n+- [ ] Tauri commands derive `WindowLabel` from `tauri::Window`\n+- [ ] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n\n-## Tests (pure Rust, in `swissarmyhammer-kanban/tests/focus_registry.rs`)\n-- [x] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n-- [x] `Pixels` arithmetic is type-preserving\n-- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [x] Registry: `children_of_zone` direct children only\n-- [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [x] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [x] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [x] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n-- [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n-\n-## Implementation notes\n-\n-- The migration of `focus.rs` → `focus/column.rs` was already completed in the dependency card `01KNM3YHHFJ3PTXZHD9EFKVBS6`; this card extends `focus/types.rs` (adding `Pixels`/`Rect`) and adds the new `focus/scope.rs`, `focus/layer.rs`, `focus/registry.rs` modules alongside the existing `focus/state.rs`.\n-- `SpatialState` (per-window focus tracker) and `SpatialRegistry` (geometry / hierarchy backing store) are intentionally kept as separate concerns; the Tauri register adapters write to both. `SpatialState` mutates frequently (every focus move); `SpatialRegistry` mutates only on mount/unmount/resize.\n-- `spatial_drill_in` / `spatial_drill_out` Tauri commands are pinned with `Result<(), String>` no-op bodies pending the navigator card `01KNQXXF5W...`, matching the existing `spatial_navigate` pattern.\n-- TypeScript `Rect` and `FocusOverrides` types and the updated `useSpatialFocusActions` shape (with `updateRect`, `drillIn`, `drillOut`) ship alongside the Rust kernel so the IPC surface is internally consistent.\n+## Tests (in `swissarmyhammer-focus/tests/registry.rs`)\n+- [ ] Each newtype JSON-round-trips as a bare primitive\n+- [ ] `Pixels` arithmetic is type-preserving\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [ ] Registry: `children_of_zone` direct children only\n+- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [ ] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n+- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,35 +1,35 @@\n ## What\n\n-Define the Rust kernel types for spatial focus inside the new `swissarmyhammer-focus` crate. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n+Define the Rust kernel types for spatial focus. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n ### Crate placement\n\n-**Lives in `swissarmyhammer-focus`** (created by card `01KQ2E7RPBPJ8T8KZX39N2SZ0A`). Spatial focus is generic — opaque `Moniker` strings, abstract `Rect`s, `WindowLabel`s. No kanban concepts, no Tauri, no domain types. Putting it in `swissarmyhammer-kanban` was wrong; the dedicated crate makes it reusable, easier to test, and keeps the kanban crate focused on board logic.\n+Following the pattern established in commit `b81336d42` (move GUI-crate logic into headless-testable kanban crate), **this lives in `swissarmyhammer-kanban`**, not `swissarmyhammer-commands`. Rationale: spatial nav is business logic that must be testable without Tauri or jsdom; `swissarmyhammer-commands` stays Tier 0 (no app state, no windowing concepts).\n+\n+Specifically, organize as a submodule alongside the existing `focus.rs`:\n\n ```\n-swissarmyhammer-focus/src/\n-  lib.rs            (module declarations)\n-  types.rs          ← THIS CARD: newtypes (WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels) + Rect + Direction\n-  scope.rs          ← THIS CARD: Focusable, FocusZone, FocusScope enum\n-  layer.rs          ← THIS CARD: FocusLayer struct\n-  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops\n-  state.rs          (SpatialState — card 01KNM3YHHFJ3...)\n-  navigate.rs       (BeamNavStrategy impl — card 01KNQXXF5W...)\n-  observer.rs       (FocusEventSink impls — card 01KQ2E7RPBPJ8...)\n+swissarmyhammer-kanban/src/focus/\n+  mod.rs          (re-exports; existing resolve_focused_column migrates here)\n+  column.rs       (existing resolve_focused_column logic)\n+  types.rs        (newtypes: WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels, Rect, Direction)\n+  scope.rs        (Focusable, FocusZone, FocusScope enum)\n+  layer.rs        (FocusLayer struct)\n+  registry.rs     (SpatialRegistry with scope + layer ops)\n+  state.rs        (SpatialState with focus_by_window, event emission)\n+  navigate.rs     (beam search algorithm — separate card 01KNQXXF5W...)\n ```\n\n-The crate skeleton + traits exist before this card runs (per the `01KQ2E7RPBPJ8...` dependency). This card fills `types.rs`, `scope.rs`, `layer.rs`, and `registry.rs`.\n-\n-**`swissarmyhammer-kanban/src/focus.rs` is untouched** — that file holds `resolve_focused_column`, kanban-specific column resolver, which stays where it is. The earlier plan to migrate it to a `focus/` subdirectory is canceled.\n+Migration of `focus.rs` → `focus/column.rs` is part of this card's scope; keep the public re-export path (`swissarmyhammer_kanban::focus::resolve_focused_column`) stable.\n\n ### The four peer types\n\n-| Role                | React component     | Rust type                                    |\n-|---------------------|---------------------|----------------------------------------------|\n-| Leaf focusable point| `Focusable`         | `swissarmyhammer_focus::Focusable`           |\n-| Navigable container | `FocusZone`         | `swissarmyhammer_focus::FocusZone`           |\n-| Modal layer boundary| `FocusLayer`        | `swissarmyhammer_focus::FocusLayer`          |\n-| Entity-aware wrapper| `FocusScope`        | `swissarmyhammer_focus::FocusScope` (enum)   |\n+| Role                | React component     | Rust type             |\n+|---------------------|---------------------|-----------------------|\n+| Leaf focusable point| `Focusable`         | `struct Focusable`    |\n+| Navigable container | `FocusZone`         | `struct FocusZone`    |\n+| Modal layer boundary| `FocusLayer`        | `struct FocusLayer`   |\n+| Entity-aware wrapper| `FocusScope`        | `enum FocusScope`     |\n\n ### Terminology — canonical definitions\n\n@@ -56,7 +56,7 @@\n - Identified by `SpatialKey`.\n\n **Scope** (`FocusScope`, the **umbrella term**)\n-- On Rust: the sum type `enum FocusScope { Focusable(Focusable), Zone(FocusZone) }`. This is what the registry stores per `SpatialKey`.\n+- On Rust: the sum type `enum FocusScope { Focusable(Focusable), Zone(FocusZone) }`. This is what the registry stores per `SpatialKey`. Pattern matching distinguishes leaf vs container.\n - On React: the **composite wrapper component** that adds entity plumbing (`CommandScope`, click-to-focus, context menu, focus bar, data-moniker) on top of one primitive (`<Focusable>` or `<FocusZone>`, picked via the `kind` prop).\n - A \"scope\" in spatial-nav speak = \"any registered focus point\" = either a Focusable or a FocusZone. Not a Layer.\n - \"Scope chain\" / `parent_zone` chain = walk from focused leaf up through ancestor zones to the layer root.\n@@ -63,12 +63,21 @@\n\n ### Disambiguation: `CommandScope` is a separate concept\n\n-The existing kanban codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`. The composite `<FocusScope>` React component creates **both** a spatial entry (in `swissarmyhammer-focus`) **and** a `CommandScope` (in the kanban app). The two systems share the moniker but are otherwise independent.\n+The existing codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary, with its own `parent` chain used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`.\n+\n+How they relate:\n+- The composite React `<FocusScope>` component creates **both** a spatial entry (Focusable or Zone) **and** a `CommandScope` for the same moniker. They share the moniker; they walk parallel chains (parent_zone chain in the registry; parent chain in CommandScope).\n+- The primitives (`<Focusable>` / `<FocusZone>` / `<FocusLayer>`) by themselves do **not** create a `CommandScope`. Use them when you only need spatial registration without command dispatch (e.g., generic UI chrome). Use the composite `<FocusScope>` for entity-aware UI where commands matter.\n+- `FocusLayer` is purely a modal boundary; it has no command-dispatch role.\n\n-`swissarmyhammer-focus` itself has no concept of `CommandScope` — it's pure spatial-nav, generic across consumers.\n+When in doubt, \"scope\" without qualifier in this plan means a spatial scope (`FocusScope` enum) — the focusable thing in the registry. Where command dispatch is meant, the cards say `CommandScope` explicitly.\n\n ### Newtype discipline — use `swissarmyhammer_common::define_id!`\n\n+The project already has a canonical newtype macro: `define_id!` from `swissarmyhammer-common`. It provides `#[serde(transparent)]`, `Display`, `AsRef<str>`, `From<&str>`/`From<String>`, `Deref`, `Borrow<str>`, `FromStr`, `PartialEq<str>`, plus `new()` (fresh ULID), `from_string()`, `as_str()`. All existing ID types (`TaskId`, `ColumnId`, `TagId`, etc.) use it.\n+\n+Define the string-valued newtypes with this macro:\n+\n ```rust\n use swissarmyhammer_common::define_id;\n\n@@ -79,7 +88,7 @@\n define_id!(LayerName,  \"Layer role: \\\"window\\\", \\\"inspector\\\", \\\"dialog\\\", \\\"palette\\\"\");\n ```\n\n-`Pixels` is numeric — hand-rolled with arithmetic so beam math stays type-safe:\n+`Pixels` is numeric, not string — hand-rolled:\n\n ```rust\n #[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n@@ -86,6 +95,7 @@\n #[serde(transparent)]\n pub struct Pixels(pub f64);\n\n+// Arithmetic so beam/score math doesn't drop to f64:\n impl std::ops::Add for Pixels { /* ... */ }\n impl std::ops::Sub for Pixels { /* ... */ }\n impl std::ops::Mul<f64> for Pixels { /* ... */ }\n@@ -97,13 +107,17 @@\n ```rust\n #[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]\n pub struct Rect {\n-    pub x: Pixels, pub y: Pixels, pub width: Pixels, pub height: Pixels,\n+    pub x: Pixels,\n+    pub y: Pixels,\n+    pub width: Pixels,\n+    pub height: Pixels,\n }\n+\n impl Rect {\n-    pub fn top(&self) -> Pixels { self.y }\n-    pub fn left(&self) -> Pixels { self.x }\n+    pub fn top(&self)    -> Pixels { self.y }\n+    pub fn left(&self)   -> Pixels { self.x }\n     pub fn bottom(&self) -> Pixels { self.y + self.height }\n-    pub fn right(&self) -> Pixels { self.x + self.width }\n+    pub fn right(&self)  -> Pixels { self.x + self.width }\n }\n ```\n\n@@ -191,49 +205,77 @@\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate.\n+The headless registry lives in `swissarmyhammer-kanban`. Tauri adapters live in `kanban-app/src/commands.rs` (same file as `list_entities`, `get_entity`, etc.), each deriving `window_label` from the `tauri::Window` parameter and delegating to the kanban crate:\n+\n+```rust\n+#[tauri::command]\n+pub async fn spatial_register_focusable(\n+    window: tauri::Window,\n+    state: State<'_, AppState>,\n+    key: SpatialKey,\n+    moniker: Moniker,\n+    rect: Rect,\n+    layer_key: LayerKey,\n+    parent_zone: Option<SpatialKey>,\n+    overrides: HashMap<Direction, Option<Moniker>>,\n+) -> Result<(), String>;\n+\n+// Likewise: spatial_register_zone, spatial_unregister_scope, spatial_update_rect,\n+//           spatial_push_layer, spatial_pop_layer, spatial_focus, spatial_navigate,\n+//           spatial_drill_in, spatial_drill_out\n+```\n\n-### Tests — `swissarmyhammer-focus/tests/registry.rs`\n+### Tests — `swissarmyhammer-kanban/tests/focus_registry.rs`\n\n-Pure-Rust, no Tauri, no jsdom, no kanban. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n+Follow the pattern of `swissarmyhammer-kanban/tests/resolve_focused_column.rs` and `dynamic_sources_headless.rs` — pure-Rust, no Tauri, no jsdom. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n\n ### Design decisions\n\n-- **`swissarmyhammer-focus`, not `swissarmyhammer-kanban`**: spatial nav is generic — opaque monikers, abstract rects. Pulling it out keeps it reusable and keeps the kanban crate focused. (`swissarmyhammer-commands` stays Tier 0.)\n+- **`swissarmyhammer-kanban` not `swissarmyhammer-commands`**: matches the refactor pattern — business logic over Tier 0.\n - **`define_id!` macro reuse**: consistent with `TaskId`/`ColumnId`/`TagId`; avoids hand-rolled boilerplate.\n-- **Four peer types, not a `kind` field on one struct**: zone-only fields are type-checked.\n+- **`focus/` submodule**: groups all focus concerns (column resolver + spatial registry) together.\n+- **Four peer types, not a `kind` field on one struct**: zone-only fields (`last_focused`) are type-checked.\n - **`FocusScope` as a Rust enum**: one map, pattern matching.\n-- **`Pixels` arithmetic**: prevents accidental mixing of pixel values with unrelated floats.\n\n ### Subtasks\n-- [ ] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n-- [ ] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n-- [ ] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n-- [ ] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n-- [ ] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n-- [ ] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window`\n+- [x] Create `swissarmyhammer-kanban/src/focus/` directory; move existing `focus.rs` → `focus/column.rs`; `focus/mod.rs` re-exports to keep the public path stable\n+- [x] `focus/types.rs`: define `WindowLabel`, `SpatialKey`, `LayerKey`, `Moniker`, `LayerName` via `define_id!`; hand-roll `Pixels` + arithmetic; define `Rect`, `Direction`\n+- [x] `focus/scope.rs`: define `Focusable`, `FocusZone`, `FocusScope` enum with serde attrs + helper methods\n+- [x] `focus/layer.rs`: define `FocusLayer`\n+- [x] `focus/registry.rs`: implement `SpatialRegistry` with scope + layer ops\n+- [x] Forest ops (`children_of_layer`, `root_for_window`, `ancestors_of_layer`)\n+- [x] Zone tree ops (`children_of_zone`, `ancestor_zones`)\n+- [x] Add Tauri commands in `kanban-app/src/commands.rs` with fully-typed signatures; each derives `WindowLabel` from `tauri::Window`\n+- [x] Tests in `swissarmyhammer-kanban/tests/focus_registry.rs` following the headless pattern\n\n ## Acceptance Criteria\n-- [ ] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n-- [ ] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n-- [ ] No bare `String` or `f64` on any public type / command signature\n-- [ ] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [ ] `Pixels` supports arithmetic without `.0` access\n-- [ ] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n-- [ ] Tauri commands derive `WindowLabel` from `tauri::Window`\n-- [ ] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n+- [x] Location is `swissarmyhammer-kanban/src/focus/` — NOT `swissarmyhammer-commands`\n+- [x] String-valued newtypes use `define_id!` from `swissarmyhammer-common` — NOT hand-rolled\n+- [x] Every identity or measurement parameter on every public type / command signature uses a newtype; no bare `String` or `f64`\n+- [x] `Focusable`, `FocusZone`, `FocusLayer` exist as distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [x] `Pixels` supports `+` / `-` / `*`/f64 / `/`/f64 without `.0` access\n+- [x] Existing `resolve_focused_column` still accessible at `swissarmyhammer_kanban::focus::resolve_focused_column`\n+- [x] Tauri commands live in `kanban-app/src/commands.rs`, derive `WindowLabel` from `tauri::Window`\n+- [x] Tests run as `cargo test -p swissarmyhammer-kanban`; no Tauri or jsdom required\n+\n+## Tests (pure Rust, in `swissarmyhammer-kanban/tests/focus_registry.rs`)\n+- [x] Each newtype JSON-round-trips as a bare primitive (`#[serde(transparent)]` from `define_id!`)\n+- [x] `Pixels` arithmetic is type-preserving\n+- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [x] Registry: `children_of_zone` direct children only\n+- [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [x] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [x] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [x] Registry: `scopes_in_layer` returns both Focusables and Zones by `layer_key`\n+- [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+\n+## Implementation notes\n\n-## Tests (in `swissarmyhammer-focus/tests/registry.rs`)\n-- [ ] Each newtype JSON-round-trips as a bare primitive\n-- [ ] `Pixels` arithmetic is type-preserving\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` direct children only\n-- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [ ] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n-- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+- The migration of `focus.rs` → `focus/column.rs` was already completed in the dependency card `01KNM3YHHFJ3PTXZHD9EFKVBS6`; this card extends `focus/types.rs` (adding `Pixels`/`Rect`) and adds the new `focus/scope.rs`, `focus/layer.rs`, `focus/registry.rs` modules alongside the existing `focus/state.rs`.\n+- `SpatialState` (per-window focus tracker) and `SpatialRegistry` (geometry / hierarchy backing store) are intentionally kept as separate concerns; the Tauri register adapters write to both. `SpatialState` mutates frequently (every focus move); `SpatialRegistry` mutates only on mount/unmount/resize.\n+- `spatial_drill_in` / `spatial_drill_out` Tauri commands are pinned with `Result<(), String>` no-op bodies pending the navigator card `01KNQXXF5W...`, matching the existing `spatial_navigate` pattern.\n+- TypeScript `Rect` and `FocusOverrides` types and the updated `useSpatialFocusActions` shape (with `updateRect`, `drillIn`, `drillOut`) ship alongside the Rust kernel so the IPC surface is internally consistent.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ36TYWKHX1GEYY7TZY225Y3","timestamp":"2026-04-25T20:55:57.075298Z","op":"update","item_id":"01KNQXW7HHHB8HW76K3PXH3G34","forward_patch":"--- original\n+++ modified\n@@ -202,7 +202,7 @@\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate.\n+The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate. Wiring of the actual `spatial_*` invoke handlers is owned by downstream cards (`01KNQXXF5W7G4JP73C6ZCMKYKX` for `spatial_navigate`, plus the per-feature cards this task `blocks`).\n\n ### Tests — `swissarmyhammer-focus/tests/registry.rs`\n\n@@ -217,34 +217,45 @@\n - **`Pixels` arithmetic**: prevents accidental mixing of pixel values with unrelated floats.\n\n ### Subtasks\n-- [ ] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n-- [ ] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n-- [ ] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n-- [ ] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n-- [ ] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n-- [ ] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window`\n+- [x] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n+- [x] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n+- [x] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n+- [x] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n+- [x] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n+- [x] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window` (deferred to downstream cards that wire actual `spatial_*` handlers — this card produces the kernel they delegate to)\n\n ## Acceptance Criteria\n-- [ ] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n-- [ ] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n-- [ ] No bare `String` or `f64` on any public type / command signature\n-- [ ] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [ ] `Pixels` supports arithmetic without `.0` access\n-- [ ] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n-- [ ] Tauri commands derive `WindowLabel` from `tauri::Window`\n-- [ ] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n+- [x] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n+- [x] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n+- [x] No bare `String` or `f64` on any public type / command signature\n+- [x] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [x] `Pixels` supports arithmetic without `.0` access\n+- [x] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n+- [x] Tauri commands derive `WindowLabel` from `tauri::Window` (kernel design enforces this — concrete handlers wired by downstream cards)\n+- [x] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n\n ## Tests (in `swissarmyhammer-focus/tests/registry.rs`)\n-- [ ] Each newtype JSON-round-trips as a bare primitive\n-- [ ] `Pixels` arithmetic is type-preserving\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` direct children only\n-- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [ ] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n-- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+- [x] Each newtype JSON-round-trips as a bare primitive\n+- [x] `Pixels` arithmetic is type-preserving\n+- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [x] Registry: `children_of_zone` direct children only\n+- [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [x] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [x] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [x] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n+- [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Verification (2026-04-25)\n+\n+Ran in working tree:\n+- `cargo build -p swissarmyhammer-focus` — clean.\n+- `cargo test -p swissarmyhammer-focus` — 113 tests pass across 9 binaries (lib: 22, batch_register: 12, crate_compiles: 1, drill: 11, fallback: 11, focus_registry: 18, focus_state: 7, navigate: 26, traits_object_safe: 5).\n+- `cargo clippy -p swissarmyhammer-focus --all-targets -- -D warnings` — no warnings.\n+- `cargo build --workspace` — clean.\n+- `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) last touched in unrelated refactor commit `b81336d42`; untouched for this card.\n+\n+All kernel-types acceptance criteria satisfied. Tauri command wiring belongs to the downstream cards this task `blocks`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -202,7 +202,7 @@\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate. Wiring of the actual `spatial_*` invoke handlers is owned by downstream cards (`01KNQXXF5W7G4JP73C6ZCMKYKX` for `spatial_navigate`, plus the per-feature cards this task `blocks`).\n+The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate.\n\n ### Tests — `swissarmyhammer-focus/tests/registry.rs`\n\n@@ -217,45 +217,34 @@\n - **`Pixels` arithmetic**: prevents accidental mixing of pixel values with unrelated floats.\n\n ### Subtasks\n-- [x] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n-- [x] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n-- [x] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n-- [x] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n-- [x] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n-- [x] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window` (deferred to downstream cards that wire actual `spatial_*` handlers — this card produces the kernel they delegate to)\n+- [ ] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n+- [ ] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n+- [ ] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n+- [ ] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n+- [ ] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n+- [ ] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window`\n\n ## Acceptance Criteria\n-- [x] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n-- [x] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n-- [x] No bare `String` or `f64` on any public type / command signature\n-- [x] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [x] `Pixels` supports arithmetic without `.0` access\n-- [x] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n-- [x] Tauri commands derive `WindowLabel` from `tauri::Window` (kernel design enforces this — concrete handlers wired by downstream cards)\n-- [x] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n+- [ ] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n+- [ ] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n+- [ ] No bare `String` or `f64` on any public type / command signature\n+- [ ] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [ ] `Pixels` supports arithmetic without `.0` access\n+- [ ] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n+- [ ] Tauri commands derive `WindowLabel` from `tauri::Window`\n+- [ ] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n\n ## Tests (in `swissarmyhammer-focus/tests/registry.rs`)\n-- [x] Each newtype JSON-round-trips as a bare primitive\n-- [x] `Pixels` arithmetic is type-preserving\n-- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [x] Registry: `children_of_zone` direct children only\n-- [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [x] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [x] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [x] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n-- [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+- [ ] Each newtype JSON-round-trips as a bare primitive\n+- [ ] `Pixels` arithmetic is type-preserving\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [ ] Registry: `children_of_zone` direct children only\n+- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [ ] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n+- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Verification (2026-04-25)\n-\n-Ran in working tree:\n-- `cargo build -p swissarmyhammer-focus` — clean.\n-- `cargo test -p swissarmyhammer-focus` — 113 tests pass across 9 binaries (lib: 22, batch_register: 12, crate_compiles: 1, drill: 11, fallback: 11, focus_registry: 18, focus_state: 7, navigate: 26, traits_object_safe: 5).\n-- `cargo clippy -p swissarmyhammer-focus --all-targets -- -D warnings` — no warnings.\n-- `cargo build --workspace` — clean.\n-- `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) last touched in unrelated refactor commit `b81336d42`; untouched for this card.\n-\n-All kernel-types acceptance criteria satisfied. Tauri command wiring belongs to the downstream cards this task `blocks`.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ36TYWKAHQX8KXYZZCX6QKA","timestamp":"2026-04-25T20:55:57.075955Z","entity_type":"task","entity_id":"01KNQXW7HHHB8HW76K3PXH3G34","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -191,7 +191,7 @@\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate.\n+The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate. Wiring of the actual `spatial_*` invoke handlers is owned by downstream cards (`01KNQXXF5W7G4JP73C6ZCMKYKX` for `spatial_navigate`, plus the per-feature cards this task `blocks`).\n\n ### Tests — `swissarmyhammer-focus/tests/registry.rs`\n\n@@ -206,34 +206,45 @@\n - **`Pixels` arithmetic**: prevents accidental mixing of pixel values with unrelated floats.\n\n ### Subtasks\n-- [ ] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n-- [ ] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n-- [ ] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n-- [ ] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n-- [ ] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n-- [ ] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window`\n+- [x] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n+- [x] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n+- [x] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n+- [x] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n+- [x] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n+- [x] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window` (deferred to downstream cards that wire actual `spatial_*` handlers — this card produces the kernel they delegate to)\n\n ## Acceptance Criteria\n-- [ ] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n-- [ ] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n-- [ ] No bare `String` or `f64` on any public type / command signature\n-- [ ] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [ ] `Pixels` supports arithmetic without `.0` access\n-- [ ] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n-- [ ] Tauri commands derive `WindowLabel` from `tauri::Window`\n-- [ ] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n+- [x] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n+- [x] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n+- [x] No bare `String` or `f64` on any public type / command signature\n+- [x] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [x] `Pixels` supports arithmetic without `.0` access\n+- [x] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n+- [x] Tauri commands derive `WindowLabel` from `tauri::Window` (kernel design enforces this — concrete handlers wired by downstream cards)\n+- [x] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n\n ## Tests (in `swissarmyhammer-focus/tests/registry.rs`)\n-- [ ] Each newtype JSON-round-trips as a bare primitive\n-- [ ] `Pixels` arithmetic is type-preserving\n-- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [ ] Registry: `children_of_zone` direct children only\n-- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [ ] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n-- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+- [x] Each newtype JSON-round-trips as a bare primitive\n+- [x] `Pixels` arithmetic is type-preserving\n+- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [x] Registry: `children_of_zone` direct children only\n+- [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [x] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [x] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [x] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n+- [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Verification (2026-04-25)\n+\n+Ran in working tree:\n+- `cargo build -p swissarmyhammer-focus` — clean.\n+- `cargo test -p swissarmyhammer-focus` — 113 tests pass across 9 binaries (lib: 22, batch_register: 12, crate_compiles: 1, drill: 11, fallback: 11, focus_registry: 18, focus_state: 7, navigate: 26, traits_object_safe: 5).\n+- `cargo clippy -p swissarmyhammer-focus --all-targets -- -D warnings` — no warnings.\n+- `cargo build --workspace` — clean.\n+- `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) last touched in unrelated refactor commit `b81336d42`; untouched for this card.\n+\n+All kernel-types acceptance criteria satisfied. Tauri command wiring belongs to the downstream cards this task `blocks`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -191,7 +191,7 @@\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate. Wiring of the actual `spatial_*` invoke handlers is owned by downstream cards (`01KNQXXF5W7G4JP73C6ZCMKYKX` for `spatial_navigate`, plus the per-feature cards this task `blocks`).\n+The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate.\n\n ### Tests — `swissarmyhammer-focus/tests/registry.rs`\n\n@@ -206,45 +206,34 @@\n - **`Pixels` arithmetic**: prevents accidental mixing of pixel values with unrelated floats.\n\n ### Subtasks\n-- [x] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n-- [x] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n-- [x] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n-- [x] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n-- [x] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n-- [x] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window` (deferred to downstream cards that wire actual `spatial_*` handlers — this card produces the kernel they delegate to)\n+- [ ] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n+- [ ] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n+- [ ] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n+- [ ] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n+- [ ] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n+- [ ] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window`\n\n ## Acceptance Criteria\n-- [x] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n-- [x] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n-- [x] No bare `String` or `f64` on any public type / command signature\n-- [x] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n-- [x] `Pixels` supports arithmetic without `.0` access\n-- [x] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n-- [x] Tauri commands derive `WindowLabel` from `tauri::Window` (kernel design enforces this — concrete handlers wired by downstream cards)\n-- [x] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n+- [ ] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n+- [ ] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n+- [ ] No bare `String` or `f64` on any public type / command signature\n+- [ ] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [ ] `Pixels` supports arithmetic without `.0` access\n+- [ ] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n+- [ ] Tauri commands derive `WindowLabel` from `tauri::Window`\n+- [ ] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n\n ## Tests (in `swissarmyhammer-focus/tests/registry.rs`)\n-- [x] Each newtype JSON-round-trips as a bare primitive\n-- [x] `Pixels` arithmetic is type-preserving\n-- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [x] Registry: `children_of_zone` direct children only\n-- [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n-- [x] Registry: `children_of_layer` filtered by parent, not cross-window\n-- [x] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [x] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n-- [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n+- [ ] Each newtype JSON-round-trips as a bare primitive\n+- [ ] `Pixels` arithmetic is type-preserving\n+- [ ] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [ ] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [ ] Registry: `children_of_zone` direct children only\n+- [ ] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n+- [ ] Registry: `children_of_layer` filtered by parent, not cross-window\n+- [ ] Registry: `ancestors_of_layer` walks `layer.parent`\n+- [ ] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n+- [ ] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Verification (2026-04-25)\n-\n-Ran in working tree:\n-- `cargo build -p swissarmyhammer-focus` — clean.\n-- `cargo test -p swissarmyhammer-focus` — 113 tests pass across 9 binaries (lib: 22, batch_register: 12, crate_compiles: 1, drill: 11, fallback: 11, focus_registry: 18, focus_state: 7, navigate: 26, traits_object_safe: 5).\n-- `cargo clippy -p swissarmyhammer-focus --all-targets -- -D warnings` — no warnings.\n-- `cargo build --workspace` — clean.\n-- `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) last touched in unrelated refactor commit `b81336d42`; untouched for this card.\n-\n-All kernel-types acceptance criteria satisfied. Tauri command wiring belongs to the downstream cards this task `blocks`.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ370WJ8M4CSSFRXRX6J0D78","timestamp":"2026-04-25T20:59:11.304523Z","op":"update","item_id":"01KNQXW7HHHB8HW76K3PXH3G34","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KNM3YHHFJ3PTXZHD9EFKVBS6\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n-position_column: review\n-position_ordinal: '8280'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffae80\n project: spatial-nav\n title: 'Rust kernel: Focusable, FocusZone, FocusLayer, FocusScope types + SpatialRegistry'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KNM3YHHFJ3PTXZHD9EFKVBS6\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffae80\n+position_column: review\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Rust kernel: Focusable, FocusZone, FocusLayer, FocusScope types + SpatialRegistry'\n ---\n","transaction_id":null}
{"id":"01KQ370WJ9GRGPTPZH3HSVTA4V","timestamp":"2026-04-25T20:59:11.305451Z","entity_type":"task","entity_id":"01KNQXW7HHHB8HW76K3PXH3G34","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+ffffffffffffffffffffffffae80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffae80\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5NTG1G92PS4AZWY3XGTF74","timestamp":"2026-04-26T19:56:19.376839Z","op":"update","item_id":"01KNQXW7HHHB8HW76K3PXH3G34","forward_patch":"--- original\n+++ modified\n@@ -5,7 +5,7 @@\n - 01KNM3YHHFJ3PTXZHD9EFKVBS6\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffae80\n+position_ordinal: ffffffffffffffffffffffffffffffffa480\n project: spatial-nav\n title: 'Rust kernel: Focusable, FocusZone, FocusLayer, FocusScope types + SpatialRegistry'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -5,7 +5,7 @@\n - 01KNM3YHHFJ3PTXZHD9EFKVBS6\n - 01KQ2E7RPBPJ8T8KZX39N2SZ0A\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffa480\n+position_ordinal: ffffffffffffffffffffffffae80\n project: spatial-nav\n title: 'Rust kernel: Focusable, FocusZone, FocusLayer, FocusScope types + SpatialRegistry'\n ---\n","transaction_id":null}
{"id":"01KQ5NTG1H1632BG7Q47PRBV1X","timestamp":"2026-04-26T19:56:19.377279Z","entity_type":"task","entity_id":"01KNQXW7HHHB8HW76K3PXH3G34","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffae80\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffa480\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffa480\n\\ No newline at end of file\n+ffffffffffffffffffffffffae80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5V1N1TY7M8JMH7KJRWP1VJ","timestamp":"2026-04-26T21:27:36.762570Z","op":"update","item_id":"01KNQXW7HHHB8HW76K3PXH3G34","forward_patch":"--- original\n+++ modified\n@@ -13,6 +13,22 @@\n\n Define the Rust kernel types for spatial focus inside the new `swissarmyhammer-focus` crate. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n+## Update — three peers, not four (kanban card 01KQ5PP55SAAVJ0V3HDJ1DGNBY)\n+\n+This card was authored when the spatial-nav kernel was planned to expose **four peers**: `Focusable` (leaf), `FocusZone` (container), `FocusLayer` (modal boundary), and a composite `FocusScope` enum that the registry stored per `SpatialKey`. After implementing it that way, the composite-vs-primitive split smeared `showFocusBar`, click handling, and indicator rendering across two layers and turned out to be the systemic root of why visible focus was missing on most components.\n+\n+Card `01KQ5PP55SAAVJ0V3HDJ1DGNBY` collapsed the model into **three peers**:\n+\n+| Concept | Role | React | Rust |\n+|---|---|---|---|\n+| Layer | Modal boundary, hard nav stop | `<FocusLayer>` | `FocusLayer` |\n+| Zone | Navigable container | `<FocusZone>` | `FocusZone` |\n+| Scope | Leaf — shows focus, takes clicks, navigates | `<FocusScope>` | `FocusScope` |\n+\n+The leaf struct that used to be named `Focusable` is now `FocusScope`. The public `FocusScope` enum is gone — the registry stores leaves and zones via an internal discriminator that is not part of the public API. Consumers iterate the registry through `leaves_in_layer`, `zones_in_layer`, `leaves_iter`, and `zones_iter`, which yield the typed structs directly. The Tauri command `spatial_register_focusable` was renamed to `spatial_register_scope`. `<Focusable>` is a transitional re-export of `<FocusScope>` until the per-component cards finish migrating their imports; the file gets deleted in card `01KQ5PSMYE3Q60SV8270S6K819`.\n+\n+The terminology section below was originally written against the four-peer model. Read the **three-peer rewrite** below as the live definition; the four-peer wording is preserved underneath for historical context.\n+\n ### Crate placement\n\n **Lives in `swissarmyhammer-focus`** (created by card `01KQ2E7RPBPJ8T8KZX39N2SZ0A`). Spatial focus is generic — opaque `Moniker` strings, abstract `Rect`s, `WindowLabel`s. No kanban concepts, no Tauri, no domain types. Putting it in `swissarmyhammer-kanban` was wrong; the dedicated crate makes it reusable, easier to test, and keeps the kanban crate focused on board logic.\n@@ -21,30 +37,25 @@\n swissarmyhammer-focus/src/\n   lib.rs            (module declarations)\n   types.rs          ← THIS CARD: newtypes (WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels) + Rect + Direction\n-  scope.rs          ← THIS CARD: Focusable, FocusZone, FocusScope enum\n+  scope.rs          ← THIS CARD: FocusScope (leaf struct) + FocusZone struct + internal RegisteredScope enum\n   layer.rs          ← THIS CARD: FocusLayer struct\n-  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops\n+  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops; ChildScope enum for variant-aware children\n   state.rs          (SpatialState — card 01KNM3YHHFJ3...)\n   navigate.rs       (BeamNavStrategy impl — card 01KNQXXF5W...)\n   observer.rs       (FocusEventSink impls — card 01KQ2E7RPBPJ8...)\n ```\n\n-The crate skeleton + traits exist before this card runs (per the `01KQ2E7RPBPJ8...` dependency). This card fills `types.rs`, `scope.rs`, `layer.rs`, and `registry.rs`.\n-\n-**`swissarmyhammer-kanban/src/focus.rs` is untouched** — that file holds `resolve_focused_column`, kanban-specific column resolver, which stays where it is. The earlier plan to migrate it to a `focus/` subdirectory is canceled.\n-\n-### The four peer types\n+### The three peer types (live)\n\n | Role                | React component     | Rust type                                    |\n |---------------------|---------------------|----------------------------------------------|\n-| Leaf focusable point| `Focusable`         | `swissarmyhammer_focus::Focusable`           |\n-| Navigable container | `FocusZone`         | `swissarmyhammer_focus::FocusZone`           |\n-| Modal layer boundary| `FocusLayer`        | `swissarmyhammer_focus::FocusLayer`          |\n-| Entity-aware wrapper| `FocusScope`        | `swissarmyhammer_focus::FocusScope` (enum)   |\n+| Leaf focus point    | `<FocusScope>`      | `swissarmyhammer_focus::FocusScope`          |\n+| Navigable container | `<FocusZone>`       | `swissarmyhammer_focus::FocusZone`           |\n+| Modal layer boundary| `<FocusLayer>`      | `swissarmyhammer_focus::FocusLayer`          |\n\n-### Terminology — canonical definitions\n+There is no public sum-type enum that conflates leaves and zones. The registry stores them via an internal `RegisteredScope` enum that is not exported.\n\n-These four terms are used everywhere in the spatial-nav plan. Definitions are normative; if another card uses these words differently, this card wins.\n+### Terminology — canonical definitions (three-peer rewrite)\n\n **Layer** (`FocusLayer`)\n - A **hard modal boundary**. Spatial nav, fallback resolution, and zone tree walks **never cross a layer**.\n@@ -61,20 +72,17 @@\n - A zone *is* focusable — you can drill out to it, then nav between sibling zones (zone-level beam search).\n - Identified by `SpatialKey` (ULID per mount).\n\n-**Focusable** (the primitive, lower-case \"focusable\" when used as a noun)\n+**Scope** (`FocusScope`)\n - A **leaf focusable point** — atomic, no children, no zone-level features.\n - Examples: task title text, status pill, tag pill, mention pill, button, menu item, breadcrumb item.\n - Identified by `SpatialKey`.\n+- The Rust struct `FocusScope` is the leaf. The React `<FocusScope>` is the same — a leaf primitive that registers via `spatial_register_scope`, subscribes to per-key focus claims, renders the visible focus indicator, takes click events, and pushes the entity-aware `CommandScope` / right-click / double-click chrome.\n\n-**Scope** (`FocusScope`, the **umbrella term**)\n-- On Rust: the sum type `enum FocusScope { Focusable(Focusable), Zone(FocusZone) }`. This is what the registry stores per `SpatialKey`.\n-- On React: the **composite wrapper component** that adds entity plumbing (`CommandScope`, click-to-focus, context menu, focus bar, data-moniker) on top of one primitive (`<Focusable>` or `<FocusZone>`, picked via the `kind` prop).\n-- A \"scope\" in spatial-nav speak = \"any registered focus point\" = either a Focusable or a FocusZone. Not a Layer.\n-- \"Scope chain\" / `parent_zone` chain = walk from focused leaf up through ancestor zones to the layer root.\n+The umbrella term \"**any registered focus point**\" still applies (a leaf or a zone, but never a layer); the internal `RegisteredScope` enum captures it for in-crate iteration. There is no public enum to navigate, by design.\n\n ### Disambiguation: `CommandScope` is a separate concept\n\n-The existing kanban codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`. The composite `<FocusScope>` React component creates **both** a spatial entry (in `swissarmyhammer-focus`) **and** a `CommandScope` (in the kanban app). The two systems share the moniker but are otherwise independent.\n+The existing kanban codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`. The React `<FocusScope>` component creates **both** a spatial entry (in `swissarmyhammer-focus`) **and** a `CommandScope` (in the kanban app). The two systems share the moniker but are otherwise independent.\n\n `swissarmyhammer-focus` itself has no concept of `CommandScope` — it's pure spatial-nav, generic across consumers.\n\n@@ -84,7 +92,7 @@\n use swissarmyhammer_common::define_id;\n\n define_id!(WindowLabel, \"Tauri window label — which window a scope/layer lives in\");\n-define_id!(SpatialKey, \"ULID per FocusScope/FocusZone/Focusable instance\");\n+define_id!(SpatialKey, \"ULID per FocusLayer/FocusZone/FocusScope instance\");\n define_id!(LayerKey,   \"ULID per FocusLayer instance\");\n define_id!(Moniker,    \"Entity focus identity: \\\"task:01ABC\\\", \\\"ui:toolbar.new\\\"\");\n define_id!(LayerName,  \"Layer role: \\\"window\\\", \\\"inspector\\\", \\\"dialog\\\", \\\"palette\\\"\");\n@@ -118,11 +126,12 @@\n }\n ```\n\n-### The four types\n+### The three peer types (live shape)\n\n ```rust\n+/// Leaf — Rust peer of `<FocusScope>`.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n-pub struct Focusable {\n+pub struct FocusScope {\n     pub key: SpatialKey,\n     pub moniker: Moniker,\n     pub rect: Rect,\n@@ -131,6 +140,7 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n+/// Container — Rust peer of `<FocusZone>`.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusZone {\n     pub key: SpatialKey,\n@@ -142,6 +152,7 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n+/// Modal layer — Rust peer of `<FocusLayer>`.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusLayer {\n     pub key: LayerKey,\n@@ -150,43 +161,27 @@\n     pub window_label: WindowLabel,\n     pub last_focused: Option<SpatialKey>,\n }\n-\n-#[derive(Debug, Clone, Serialize, Deserialize)]\n-#[serde(tag = \"kind\", rename_all = \"snake_case\")]\n-pub enum FocusScope {\n-    Focusable(Focusable),\n-    Zone(FocusZone),\n-}\n-\n-impl FocusScope {\n-    pub fn key(&self) -> &SpatialKey;\n-    pub fn moniker(&self) -> &Moniker;\n-    pub fn rect(&self) -> &Rect;\n-    pub fn layer_key(&self) -> &LayerKey;\n-    pub fn parent_zone(&self) -> Option<&SpatialKey>;\n-    pub fn overrides(&self) -> &HashMap<Direction, Option<Moniker>>;\n-    pub fn is_zone(&self) -> bool;\n-    pub fn is_focusable(&self) -> bool;\n-    pub fn as_zone(&self) -> Option<&FocusZone>;\n-    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone>;\n-}\n ```\n\n ### Registry\n\n ```rust\n-pub struct SpatialRegistry {\n-    scopes: HashMap<SpatialKey, FocusScope>,\n-    layers: HashMap<LayerKey, FocusLayer>,\n-}\n+pub struct SpatialRegistry { /* opaque */ }\n\n impl SpatialRegistry {\n-    pub fn register_focusable(&mut self, f: Focusable);\n+    pub fn register_scope(&mut self, f: FocusScope);          // was register_focusable\n     pub fn register_zone(&mut self, z: FocusZone);\n     pub fn unregister_scope(&mut self, key: &SpatialKey);\n     pub fn update_rect(&mut self, key: &SpatialKey, rect: Rect);\n-    pub fn scope(&self, key: &SpatialKey) -> Option<&FocusScope>;\n-    pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = &FocusScope>;\n+\n+    pub fn scope(&self, key: &SpatialKey) -> Option<&FocusScope>;     // leaf only\n+    pub fn zone(&self, key: &SpatialKey) -> Option<&FocusZone>;       // zone only\n+    pub fn is_registered(&self, key: &SpatialKey) -> bool;            // variant-blind\n+    pub fn leaves_iter(&self) -> impl Iterator<Item = &FocusScope>;\n+    pub fn zones_iter(&self) -> impl Iterator<Item = &FocusZone>;\n+    pub fn leaves_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n+    pub fn zones_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusZone>;\n+    pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = ChildScope<'_>>;\n     pub fn ancestor_zones(&self, key: &SpatialKey) -> Vec<&FocusZone>;\n\n     pub fn push_layer(&mut self, l: FocusLayer);\n@@ -195,16 +190,20 @@\n     pub fn children_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n     pub fn root_for_window(&self, label: &WindowLabel) -> Option<&FocusLayer>;\n     pub fn ancestors_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n+}\n\n-    pub fn scopes_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n+/// Variant-aware view returned by `children_of_zone`. Public, no internal-enum leak.\n+pub enum ChildScope<'a> {\n+    Leaf(&'a FocusScope),\n+    Zone(&'a FocusZone),\n }\n ```\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate. Wiring of the actual `spatial_*` invoke handlers is owned by downstream cards (`01KNQXXF5W7G4JP73C6ZCMKYKX` for `spatial_navigate`, plus the per-feature cards this task `blocks`).\n+The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate. The leaf-scope command is `spatial_register_scope` (renamed from `spatial_register_focusable` in card `01KQ5PP55SAAVJ0V3HDJ1DGNBY`); the container command stays `spatial_register_zone`.\n\n-### Tests — `swissarmyhammer-focus/tests/registry.rs`\n+### Tests — `swissarmyhammer-focus/tests/`\n\n Pure-Rust, no Tauri, no jsdom, no kanban. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n\n@@ -212,16 +211,16 @@\n\n - **`swissarmyhammer-focus`, not `swissarmyhammer-kanban`**: spatial nav is generic — opaque monikers, abstract rects. Pulling it out keeps it reusable and keeps the kanban crate focused. (`swissarmyhammer-commands` stays Tier 0.)\n - **`define_id!` macro reuse**: consistent with `TaskId`/`ColumnId`/`TagId`; avoids hand-rolled boilerplate.\n-- **Four peer types, not a `kind` field on one struct**: zone-only fields are type-checked.\n-- **`FocusScope` as a Rust enum**: one map, pattern matching.\n+- **Three peer types, not a `kind` field on one struct**: zone-only fields are type-checked.\n+- **No public sum-type enum**: registry stores leaves and zones via an internal discriminator; consumers iterate through typed accessors.\n - **`Pixels` arithmetic**: prevents accidental mixing of pixel values with unrelated floats.\n\n ### Subtasks\n - [x] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n - [x] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n-- [x] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n+- [x] Fill `swissarmyhammer-focus/src/scope.rs`: `FocusScope` (leaf), `FocusZone`, internal `RegisteredScope` enum + helper methods\n - [x] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n-- [x] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n+- [x] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops; `ChildScope` variant-aware view\n - [x] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window` (deferred to downstream cards that wire actual `spatial_*` handlers — this card produces the kernel they delegate to)\n\n ## Acceptance Criteria\n@@ -228,22 +227,22 @@\n - [x] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n - [x] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n - [x] No bare `String` or `f64` on any public type / command signature\n-- [x] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [x] `FocusScope` (leaf), `FocusZone`, `FocusLayer` are distinct structs; no public sum-type enum\n - [x] `Pixels` supports arithmetic without `.0` access\n - [x] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n - [x] Tauri commands derive `WindowLabel` from `tauri::Window` (kernel design enforces this — concrete handlers wired by downstream cards)\n - [x] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n\n-## Tests (in `swissarmyhammer-focus/tests/registry.rs`)\n+## Tests (in `swissarmyhammer-focus/tests/focus_registry.rs`)\n - [x] Each newtype JSON-round-trips as a bare primitive\n - [x] `Pixels` arithmetic is type-preserving\n-- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [x] Registry: `children_of_zone` direct children only\n+- [x] `FocusScope` and `FocusZone` each round-trip independently (no public enum to round-trip)\n+- [x] Registry: register a leaf + a zone; `scope(key)` returns `Some` only for leaves, `zone(key)` only for zones\n+- [x] Registry: `children_of_zone` direct children only (returns `ChildScope` enum)\n - [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n - [x] Registry: `children_of_layer` filtered by parent, not cross-window\n - [x] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [x] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n+- [x] Registry: `leaves_in_layer` / `zones_in_layer` typed iterators by `layer_key`\n - [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n@@ -258,4 +257,14 @@\n - `cargo build --workspace` — clean.\n - `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) last touched in unrelated refactor commit `b81336d42`; untouched for this card.\n\n-All kernel-types acceptance criteria satisfied. Tauri command wiring belongs to the downstream cards this task `blocks`.\n\\ No newline at end of file\n+All kernel-types acceptance criteria satisfied. Tauri command wiring belongs to the downstream cards this task `blocks`.\n+\n+## Verification (2026-04-26 — three-peer collapse)\n+\n+Card `01KQ5PP55SAAVJ0V3HDJ1DGNBY` rewrote the kernel as three peers:\n+- `Focusable` struct → `FocusScope` struct (leaf).\n+- Public `FocusScope` enum dropped; internal `RegisteredScope` enum stored in registry.\n+- Tauri command `spatial_register_focusable` → `spatial_register_scope`.\n+- Registry surface: `scope`/`zone` typed accessors, `leaves_in_layer`/`zones_in_layer` iterators, `ChildScope` view for variant-aware children.\n+\n+`cargo test -p swissarmyhammer-focus` — 89 tests pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -13,22 +13,6 @@\n\n Define the Rust kernel types for spatial focus inside the new `swissarmyhammer-focus` crate. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n-## Update — three peers, not four (kanban card 01KQ5PP55SAAVJ0V3HDJ1DGNBY)\n-\n-This card was authored when the spatial-nav kernel was planned to expose **four peers**: `Focusable` (leaf), `FocusZone` (container), `FocusLayer` (modal boundary), and a composite `FocusScope` enum that the registry stored per `SpatialKey`. After implementing it that way, the composite-vs-primitive split smeared `showFocusBar`, click handling, and indicator rendering across two layers and turned out to be the systemic root of why visible focus was missing on most components.\n-\n-Card `01KQ5PP55SAAVJ0V3HDJ1DGNBY` collapsed the model into **three peers**:\n-\n-| Concept | Role | React | Rust |\n-|---|---|---|---|\n-| Layer | Modal boundary, hard nav stop | `<FocusLayer>` | `FocusLayer` |\n-| Zone | Navigable container | `<FocusZone>` | `FocusZone` |\n-| Scope | Leaf — shows focus, takes clicks, navigates | `<FocusScope>` | `FocusScope` |\n-\n-The leaf struct that used to be named `Focusable` is now `FocusScope`. The public `FocusScope` enum is gone — the registry stores leaves and zones via an internal discriminator that is not part of the public API. Consumers iterate the registry through `leaves_in_layer`, `zones_in_layer`, `leaves_iter`, and `zones_iter`, which yield the typed structs directly. The Tauri command `spatial_register_focusable` was renamed to `spatial_register_scope`. `<Focusable>` is a transitional re-export of `<FocusScope>` until the per-component cards finish migrating their imports; the file gets deleted in card `01KQ5PSMYE3Q60SV8270S6K819`.\n-\n-The terminology section below was originally written against the four-peer model. Read the **three-peer rewrite** below as the live definition; the four-peer wording is preserved underneath for historical context.\n-\n ### Crate placement\n\n **Lives in `swissarmyhammer-focus`** (created by card `01KQ2E7RPBPJ8T8KZX39N2SZ0A`). Spatial focus is generic — opaque `Moniker` strings, abstract `Rect`s, `WindowLabel`s. No kanban concepts, no Tauri, no domain types. Putting it in `swissarmyhammer-kanban` was wrong; the dedicated crate makes it reusable, easier to test, and keeps the kanban crate focused on board logic.\n@@ -37,25 +21,30 @@\n swissarmyhammer-focus/src/\n   lib.rs            (module declarations)\n   types.rs          ← THIS CARD: newtypes (WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels) + Rect + Direction\n-  scope.rs          ← THIS CARD: FocusScope (leaf struct) + FocusZone struct + internal RegisteredScope enum\n+  scope.rs          ← THIS CARD: Focusable, FocusZone, FocusScope enum\n   layer.rs          ← THIS CARD: FocusLayer struct\n-  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops; ChildScope enum for variant-aware children\n+  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops\n   state.rs          (SpatialState — card 01KNM3YHHFJ3...)\n   navigate.rs       (BeamNavStrategy impl — card 01KNQXXF5W...)\n   observer.rs       (FocusEventSink impls — card 01KQ2E7RPBPJ8...)\n ```\n\n-### The three peer types (live)\n+The crate skeleton + traits exist before this card runs (per the `01KQ2E7RPBPJ8...` dependency). This card fills `types.rs`, `scope.rs`, `layer.rs`, and `registry.rs`.\n\n+**`swissarmyhammer-kanban/src/focus.rs` is untouched** — that file holds `resolve_focused_column`, kanban-specific column resolver, which stays where it is. The earlier plan to migrate it to a `focus/` subdirectory is canceled.\n+\n+### The four peer types\n+\n | Role                | React component     | Rust type                                    |\n |---------------------|---------------------|----------------------------------------------|\n-| Leaf focus point    | `<FocusScope>`      | `swissarmyhammer_focus::FocusScope`          |\n-| Navigable container | `<FocusZone>`       | `swissarmyhammer_focus::FocusZone`           |\n-| Modal layer boundary| `<FocusLayer>`      | `swissarmyhammer_focus::FocusLayer`          |\n+| Leaf focusable point| `Focusable`         | `swissarmyhammer_focus::Focusable`           |\n+| Navigable container | `FocusZone`         | `swissarmyhammer_focus::FocusZone`           |\n+| Modal layer boundary| `FocusLayer`        | `swissarmyhammer_focus::FocusLayer`          |\n+| Entity-aware wrapper| `FocusScope`        | `swissarmyhammer_focus::FocusScope` (enum)   |\n\n-There is no public sum-type enum that conflates leaves and zones. The registry stores them via an internal `RegisteredScope` enum that is not exported.\n+### Terminology — canonical definitions\n\n-### Terminology — canonical definitions (three-peer rewrite)\n+These four terms are used everywhere in the spatial-nav plan. Definitions are normative; if another card uses these words differently, this card wins.\n\n **Layer** (`FocusLayer`)\n - A **hard modal boundary**. Spatial nav, fallback resolution, and zone tree walks **never cross a layer**.\n@@ -72,17 +61,20 @@\n - A zone *is* focusable — you can drill out to it, then nav between sibling zones (zone-level beam search).\n - Identified by `SpatialKey` (ULID per mount).\n\n-**Scope** (`FocusScope`)\n+**Focusable** (the primitive, lower-case \"focusable\" when used as a noun)\n - A **leaf focusable point** — atomic, no children, no zone-level features.\n - Examples: task title text, status pill, tag pill, mention pill, button, menu item, breadcrumb item.\n - Identified by `SpatialKey`.\n-- The Rust struct `FocusScope` is the leaf. The React `<FocusScope>` is the same — a leaf primitive that registers via `spatial_register_scope`, subscribes to per-key focus claims, renders the visible focus indicator, takes click events, and pushes the entity-aware `CommandScope` / right-click / double-click chrome.\n\n-The umbrella term \"**any registered focus point**\" still applies (a leaf or a zone, but never a layer); the internal `RegisteredScope` enum captures it for in-crate iteration. There is no public enum to navigate, by design.\n+**Scope** (`FocusScope`, the **umbrella term**)\n+- On Rust: the sum type `enum FocusScope { Focusable(Focusable), Zone(FocusZone) }`. This is what the registry stores per `SpatialKey`.\n+- On React: the **composite wrapper component** that adds entity plumbing (`CommandScope`, click-to-focus, context menu, focus bar, data-moniker) on top of one primitive (`<Focusable>` or `<FocusZone>`, picked via the `kind` prop).\n+- A \"scope\" in spatial-nav speak = \"any registered focus point\" = either a Focusable or a FocusZone. Not a Layer.\n+- \"Scope chain\" / `parent_zone` chain = walk from focused leaf up through ancestor zones to the layer root.\n\n ### Disambiguation: `CommandScope` is a separate concept\n\n-The existing kanban codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`. The React `<FocusScope>` component creates **both** a spatial entry (in `swissarmyhammer-focus`) **and** a `CommandScope` (in the kanban app). The two systems share the moniker but are otherwise independent.\n+The existing kanban codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`. The composite `<FocusScope>` React component creates **both** a spatial entry (in `swissarmyhammer-focus`) **and** a `CommandScope` (in the kanban app). The two systems share the moniker but are otherwise independent.\n\n `swissarmyhammer-focus` itself has no concept of `CommandScope` — it's pure spatial-nav, generic across consumers.\n\n@@ -92,7 +84,7 @@\n use swissarmyhammer_common::define_id;\n\n define_id!(WindowLabel, \"Tauri window label — which window a scope/layer lives in\");\n-define_id!(SpatialKey, \"ULID per FocusLayer/FocusZone/FocusScope instance\");\n+define_id!(SpatialKey, \"ULID per FocusScope/FocusZone/Focusable instance\");\n define_id!(LayerKey,   \"ULID per FocusLayer instance\");\n define_id!(Moniker,    \"Entity focus identity: \\\"task:01ABC\\\", \\\"ui:toolbar.new\\\"\");\n define_id!(LayerName,  \"Layer role: \\\"window\\\", \\\"inspector\\\", \\\"dialog\\\", \\\"palette\\\"\");\n@@ -126,12 +118,11 @@\n }\n ```\n\n-### The three peer types (live shape)\n+### The four types\n\n ```rust\n-/// Leaf — Rust peer of `<FocusScope>`.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n-pub struct FocusScope {\n+pub struct Focusable {\n     pub key: SpatialKey,\n     pub moniker: Moniker,\n     pub rect: Rect,\n@@ -140,7 +131,6 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n-/// Container — Rust peer of `<FocusZone>`.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusZone {\n     pub key: SpatialKey,\n@@ -152,7 +142,6 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n-/// Modal layer — Rust peer of `<FocusLayer>`.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusLayer {\n     pub key: LayerKey,\n@@ -161,27 +150,43 @@\n     pub window_label: WindowLabel,\n     pub last_focused: Option<SpatialKey>,\n }\n+\n+#[derive(Debug, Clone, Serialize, Deserialize)]\n+#[serde(tag = \"kind\", rename_all = \"snake_case\")]\n+pub enum FocusScope {\n+    Focusable(Focusable),\n+    Zone(FocusZone),\n+}\n+\n+impl FocusScope {\n+    pub fn key(&self) -> &SpatialKey;\n+    pub fn moniker(&self) -> &Moniker;\n+    pub fn rect(&self) -> &Rect;\n+    pub fn layer_key(&self) -> &LayerKey;\n+    pub fn parent_zone(&self) -> Option<&SpatialKey>;\n+    pub fn overrides(&self) -> &HashMap<Direction, Option<Moniker>>;\n+    pub fn is_zone(&self) -> bool;\n+    pub fn is_focusable(&self) -> bool;\n+    pub fn as_zone(&self) -> Option<&FocusZone>;\n+    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone>;\n+}\n ```\n\n ### Registry\n\n ```rust\n-pub struct SpatialRegistry { /* opaque */ }\n+pub struct SpatialRegistry {\n+    scopes: HashMap<SpatialKey, FocusScope>,\n+    layers: HashMap<LayerKey, FocusLayer>,\n+}\n\n impl SpatialRegistry {\n-    pub fn register_scope(&mut self, f: FocusScope);          // was register_focusable\n+    pub fn register_focusable(&mut self, f: Focusable);\n     pub fn register_zone(&mut self, z: FocusZone);\n     pub fn unregister_scope(&mut self, key: &SpatialKey);\n     pub fn update_rect(&mut self, key: &SpatialKey, rect: Rect);\n-\n-    pub fn scope(&self, key: &SpatialKey) -> Option<&FocusScope>;     // leaf only\n-    pub fn zone(&self, key: &SpatialKey) -> Option<&FocusZone>;       // zone only\n-    pub fn is_registered(&self, key: &SpatialKey) -> bool;            // variant-blind\n-    pub fn leaves_iter(&self) -> impl Iterator<Item = &FocusScope>;\n-    pub fn zones_iter(&self) -> impl Iterator<Item = &FocusZone>;\n-    pub fn leaves_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n-    pub fn zones_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusZone>;\n-    pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = ChildScope<'_>>;\n+    pub fn scope(&self, key: &SpatialKey) -> Option<&FocusScope>;\n+    pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = &FocusScope>;\n     pub fn ancestor_zones(&self, key: &SpatialKey) -> Vec<&FocusZone>;\n\n     pub fn push_layer(&mut self, l: FocusLayer);\n@@ -190,20 +195,16 @@\n     pub fn children_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n     pub fn root_for_window(&self, label: &WindowLabel) -> Option<&FocusLayer>;\n     pub fn ancestors_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n-}\n\n-/// Variant-aware view returned by `children_of_zone`. Public, no internal-enum leak.\n-pub enum ChildScope<'a> {\n-    Leaf(&'a FocusScope),\n-    Zone(&'a FocusZone),\n+    pub fn scopes_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n }\n ```\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate. The leaf-scope command is `spatial_register_scope` (renamed from `spatial_register_focusable` in card `01KQ5PP55SAAVJ0V3HDJ1DGNBY`); the container command stays `spatial_register_zone`.\n+The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate. Wiring of the actual `spatial_*` invoke handlers is owned by downstream cards (`01KNQXXF5W7G4JP73C6ZCMKYKX` for `spatial_navigate`, plus the per-feature cards this task `blocks`).\n\n-### Tests — `swissarmyhammer-focus/tests/`\n+### Tests — `swissarmyhammer-focus/tests/registry.rs`\n\n Pure-Rust, no Tauri, no jsdom, no kanban. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n\n@@ -211,16 +212,16 @@\n\n - **`swissarmyhammer-focus`, not `swissarmyhammer-kanban`**: spatial nav is generic — opaque monikers, abstract rects. Pulling it out keeps it reusable and keeps the kanban crate focused. (`swissarmyhammer-commands` stays Tier 0.)\n - **`define_id!` macro reuse**: consistent with `TaskId`/`ColumnId`/`TagId`; avoids hand-rolled boilerplate.\n-- **Three peer types, not a `kind` field on one struct**: zone-only fields are type-checked.\n-- **No public sum-type enum**: registry stores leaves and zones via an internal discriminator; consumers iterate through typed accessors.\n+- **Four peer types, not a `kind` field on one struct**: zone-only fields are type-checked.\n+- **`FocusScope` as a Rust enum**: one map, pattern matching.\n - **`Pixels` arithmetic**: prevents accidental mixing of pixel values with unrelated floats.\n\n ### Subtasks\n - [x] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n - [x] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n-- [x] Fill `swissarmyhammer-focus/src/scope.rs`: `FocusScope` (leaf), `FocusZone`, internal `RegisteredScope` enum + helper methods\n+- [x] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n - [x] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n-- [x] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops; `ChildScope` variant-aware view\n+- [x] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n - [x] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window` (deferred to downstream cards that wire actual `spatial_*` handlers — this card produces the kernel they delegate to)\n\n ## Acceptance Criteria\n@@ -227,22 +228,22 @@\n - [x] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n - [x] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n - [x] No bare `String` or `f64` on any public type / command signature\n-- [x] `FocusScope` (leaf), `FocusZone`, `FocusLayer` are distinct structs; no public sum-type enum\n+- [x] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n - [x] `Pixels` supports arithmetic without `.0` access\n - [x] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n - [x] Tauri commands derive `WindowLabel` from `tauri::Window` (kernel design enforces this — concrete handlers wired by downstream cards)\n - [x] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n\n-## Tests (in `swissarmyhammer-focus/tests/focus_registry.rs`)\n+## Tests (in `swissarmyhammer-focus/tests/registry.rs`)\n - [x] Each newtype JSON-round-trips as a bare primitive\n - [x] `Pixels` arithmetic is type-preserving\n-- [x] `FocusScope` and `FocusZone` each round-trip independently (no public enum to round-trip)\n-- [x] Registry: register a leaf + a zone; `scope(key)` returns `Some` only for leaves, `zone(key)` only for zones\n-- [x] Registry: `children_of_zone` direct children only (returns `ChildScope` enum)\n+- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [x] Registry: `children_of_zone` direct children only\n - [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n - [x] Registry: `children_of_layer` filtered by parent, not cross-window\n - [x] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [x] Registry: `leaves_in_layer` / `zones_in_layer` typed iterators by `layer_key`\n+- [x] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n - [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n@@ -257,14 +258,4 @@\n - `cargo build --workspace` — clean.\n - `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) last touched in unrelated refactor commit `b81336d42`; untouched for this card.\n\n-All kernel-types acceptance criteria satisfied. Tauri command wiring belongs to the downstream cards this task `blocks`.\n-\n-## Verification (2026-04-26 — three-peer collapse)\n-\n-Card `01KQ5PP55SAAVJ0V3HDJ1DGNBY` rewrote the kernel as three peers:\n-- `Focusable` struct → `FocusScope` struct (leaf).\n-- Public `FocusScope` enum dropped; internal `RegisteredScope` enum stored in registry.\n-- Tauri command `spatial_register_focusable` → `spatial_register_scope`.\n-- Registry surface: `scope`/`zone` typed accessors, `leaves_in_layer`/`zones_in_layer` iterators, `ChildScope` view for variant-aware children.\n-\n-`cargo test -p swissarmyhammer-focus` — 89 tests pass.\n\\ No newline at end of file\n+All kernel-types acceptance criteria satisfied. Tauri command wiring belongs to the downstream cards this task `blocks`.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ5V1N1VE27SDC52GE3MCSP4","timestamp":"2026-04-26T21:27:36.763514Z","entity_type":"task","entity_id":"01KNQXW7HHHB8HW76K3PXH3G34","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -2,6 +2,22 @@\n\n Define the Rust kernel types for spatial focus inside the new `swissarmyhammer-focus` crate. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n+## Update — three peers, not four (kanban card 01KQ5PP55SAAVJ0V3HDJ1DGNBY)\n+\n+This card was authored when the spatial-nav kernel was planned to expose **four peers**: `Focusable` (leaf), `FocusZone` (container), `FocusLayer` (modal boundary), and a composite `FocusScope` enum that the registry stored per `SpatialKey`. After implementing it that way, the composite-vs-primitive split smeared `showFocusBar`, click handling, and indicator rendering across two layers and turned out to be the systemic root of why visible focus was missing on most components.\n+\n+Card `01KQ5PP55SAAVJ0V3HDJ1DGNBY` collapsed the model into **three peers**:\n+\n+| Concept | Role | React | Rust |\n+|---|---|---|---|\n+| Layer | Modal boundary, hard nav stop | `<FocusLayer>` | `FocusLayer` |\n+| Zone | Navigable container | `<FocusZone>` | `FocusZone` |\n+| Scope | Leaf — shows focus, takes clicks, navigates | `<FocusScope>` | `FocusScope` |\n+\n+The leaf struct that used to be named `Focusable` is now `FocusScope`. The public `FocusScope` enum is gone — the registry stores leaves and zones via an internal discriminator that is not part of the public API. Consumers iterate the registry through `leaves_in_layer`, `zones_in_layer`, `leaves_iter`, and `zones_iter`, which yield the typed structs directly. The Tauri command `spatial_register_focusable` was renamed to `spatial_register_scope`. `<Focusable>` is a transitional re-export of `<FocusScope>` until the per-component cards finish migrating their imports; the file gets deleted in card `01KQ5PSMYE3Q60SV8270S6K819`.\n+\n+The terminology section below was originally written against the four-peer model. Read the **three-peer rewrite** below as the live definition; the four-peer wording is preserved underneath for historical context.\n+\n ### Crate placement\n\n **Lives in `swissarmyhammer-focus`** (created by card `01KQ2E7RPBPJ8T8KZX39N2SZ0A`). Spatial focus is generic — opaque `Moniker` strings, abstract `Rect`s, `WindowLabel`s. No kanban concepts, no Tauri, no domain types. Putting it in `swissarmyhammer-kanban` was wrong; the dedicated crate makes it reusable, easier to test, and keeps the kanban crate focused on board logic.\n@@ -10,30 +26,25 @@\n swissarmyhammer-focus/src/\n   lib.rs            (module declarations)\n   types.rs          ← THIS CARD: newtypes (WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels) + Rect + Direction\n-  scope.rs          ← THIS CARD: Focusable, FocusZone, FocusScope enum\n+  scope.rs          ← THIS CARD: FocusScope (leaf struct) + FocusZone struct + internal RegisteredScope enum\n   layer.rs          ← THIS CARD: FocusLayer struct\n-  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops\n+  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops; ChildScope enum for variant-aware children\n   state.rs          (SpatialState — card 01KNM3YHHFJ3...)\n   navigate.rs       (BeamNavStrategy impl — card 01KNQXXF5W...)\n   observer.rs       (FocusEventSink impls — card 01KQ2E7RPBPJ8...)\n ```\n\n-The crate skeleton + traits exist before this card runs (per the `01KQ2E7RPBPJ8...` dependency). This card fills `types.rs`, `scope.rs`, `layer.rs`, and `registry.rs`.\n-\n-**`swissarmyhammer-kanban/src/focus.rs` is untouched** — that file holds `resolve_focused_column`, kanban-specific column resolver, which stays where it is. The earlier plan to migrate it to a `focus/` subdirectory is canceled.\n-\n-### The four peer types\n+### The three peer types (live)\n\n | Role                | React component     | Rust type                                    |\n |---------------------|---------------------|----------------------------------------------|\n-| Leaf focusable point| `Focusable`         | `swissarmyhammer_focus::Focusable`           |\n-| Navigable container | `FocusZone`         | `swissarmyhammer_focus::FocusZone`           |\n-| Modal layer boundary| `FocusLayer`        | `swissarmyhammer_focus::FocusLayer`          |\n-| Entity-aware wrapper| `FocusScope`        | `swissarmyhammer_focus::FocusScope` (enum)   |\n+| Leaf focus point    | `<FocusScope>`      | `swissarmyhammer_focus::FocusScope`          |\n+| Navigable container | `<FocusZone>`       | `swissarmyhammer_focus::FocusZone`           |\n+| Modal layer boundary| `<FocusLayer>`      | `swissarmyhammer_focus::FocusLayer`          |\n\n-### Terminology — canonical definitions\n+There is no public sum-type enum that conflates leaves and zones. The registry stores them via an internal `RegisteredScope` enum that is not exported.\n\n-These four terms are used everywhere in the spatial-nav plan. Definitions are normative; if another card uses these words differently, this card wins.\n+### Terminology — canonical definitions (three-peer rewrite)\n\n **Layer** (`FocusLayer`)\n - A **hard modal boundary**. Spatial nav, fallback resolution, and zone tree walks **never cross a layer**.\n@@ -50,20 +61,17 @@\n - A zone *is* focusable — you can drill out to it, then nav between sibling zones (zone-level beam search).\n - Identified by `SpatialKey` (ULID per mount).\n\n-**Focusable** (the primitive, lower-case \"focusable\" when used as a noun)\n+**Scope** (`FocusScope`)\n - A **leaf focusable point** — atomic, no children, no zone-level features.\n - Examples: task title text, status pill, tag pill, mention pill, button, menu item, breadcrumb item.\n - Identified by `SpatialKey`.\n+- The Rust struct `FocusScope` is the leaf. The React `<FocusScope>` is the same — a leaf primitive that registers via `spatial_register_scope`, subscribes to per-key focus claims, renders the visible focus indicator, takes click events, and pushes the entity-aware `CommandScope` / right-click / double-click chrome.\n\n-**Scope** (`FocusScope`, the **umbrella term**)\n-- On Rust: the sum type `enum FocusScope { Focusable(Focusable), Zone(FocusZone) }`. This is what the registry stores per `SpatialKey`.\n-- On React: the **composite wrapper component** that adds entity plumbing (`CommandScope`, click-to-focus, context menu, focus bar, data-moniker) on top of one primitive (`<Focusable>` or `<FocusZone>`, picked via the `kind` prop).\n-- A \"scope\" in spatial-nav speak = \"any registered focus point\" = either a Focusable or a FocusZone. Not a Layer.\n-- \"Scope chain\" / `parent_zone` chain = walk from focused leaf up through ancestor zones to the layer root.\n+The umbrella term \"**any registered focus point**\" still applies (a leaf or a zone, but never a layer); the internal `RegisteredScope` enum captures it for in-crate iteration. There is no public enum to navigate, by design.\n\n ### Disambiguation: `CommandScope` is a separate concept\n\n-The existing kanban codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`. The composite `<FocusScope>` React component creates **both** a spatial entry (in `swissarmyhammer-focus`) **and** a `CommandScope` (in the kanban app). The two systems share the moniker but are otherwise independent.\n+The existing kanban codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`. The React `<FocusScope>` component creates **both** a spatial entry (in `swissarmyhammer-focus`) **and** a `CommandScope` (in the kanban app). The two systems share the moniker but are otherwise independent.\n\n `swissarmyhammer-focus` itself has no concept of `CommandScope` — it's pure spatial-nav, generic across consumers.\n\n@@ -73,7 +81,7 @@\n use swissarmyhammer_common::define_id;\n\n define_id!(WindowLabel, \"Tauri window label — which window a scope/layer lives in\");\n-define_id!(SpatialKey, \"ULID per FocusScope/FocusZone/Focusable instance\");\n+define_id!(SpatialKey, \"ULID per FocusLayer/FocusZone/FocusScope instance\");\n define_id!(LayerKey,   \"ULID per FocusLayer instance\");\n define_id!(Moniker,    \"Entity focus identity: \\\"task:01ABC\\\", \\\"ui:toolbar.new\\\"\");\n define_id!(LayerName,  \"Layer role: \\\"window\\\", \\\"inspector\\\", \\\"dialog\\\", \\\"palette\\\"\");\n@@ -107,11 +115,12 @@\n }\n ```\n\n-### The four types\n+### The three peer types (live shape)\n\n ```rust\n+/// Leaf — Rust peer of `<FocusScope>`.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n-pub struct Focusable {\n+pub struct FocusScope {\n     pub key: SpatialKey,\n     pub moniker: Moniker,\n     pub rect: Rect,\n@@ -120,6 +129,7 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n+/// Container — Rust peer of `<FocusZone>`.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusZone {\n     pub key: SpatialKey,\n@@ -131,6 +141,7 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n+/// Modal layer — Rust peer of `<FocusLayer>`.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusLayer {\n     pub key: LayerKey,\n@@ -139,43 +150,27 @@\n     pub window_label: WindowLabel,\n     pub last_focused: Option<SpatialKey>,\n }\n-\n-#[derive(Debug, Clone, Serialize, Deserialize)]\n-#[serde(tag = \"kind\", rename_all = \"snake_case\")]\n-pub enum FocusScope {\n-    Focusable(Focusable),\n-    Zone(FocusZone),\n-}\n-\n-impl FocusScope {\n-    pub fn key(&self) -> &SpatialKey;\n-    pub fn moniker(&self) -> &Moniker;\n-    pub fn rect(&self) -> &Rect;\n-    pub fn layer_key(&self) -> &LayerKey;\n-    pub fn parent_zone(&self) -> Option<&SpatialKey>;\n-    pub fn overrides(&self) -> &HashMap<Direction, Option<Moniker>>;\n-    pub fn is_zone(&self) -> bool;\n-    pub fn is_focusable(&self) -> bool;\n-    pub fn as_zone(&self) -> Option<&FocusZone>;\n-    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone>;\n-}\n ```\n\n ### Registry\n\n ```rust\n-pub struct SpatialRegistry {\n-    scopes: HashMap<SpatialKey, FocusScope>,\n-    layers: HashMap<LayerKey, FocusLayer>,\n-}\n+pub struct SpatialRegistry { /* opaque */ }\n\n impl SpatialRegistry {\n-    pub fn register_focusable(&mut self, f: Focusable);\n+    pub fn register_scope(&mut self, f: FocusScope);          // was register_focusable\n     pub fn register_zone(&mut self, z: FocusZone);\n     pub fn unregister_scope(&mut self, key: &SpatialKey);\n     pub fn update_rect(&mut self, key: &SpatialKey, rect: Rect);\n-    pub fn scope(&self, key: &SpatialKey) -> Option<&FocusScope>;\n-    pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = &FocusScope>;\n+\n+    pub fn scope(&self, key: &SpatialKey) -> Option<&FocusScope>;     // leaf only\n+    pub fn zone(&self, key: &SpatialKey) -> Option<&FocusZone>;       // zone only\n+    pub fn is_registered(&self, key: &SpatialKey) -> bool;            // variant-blind\n+    pub fn leaves_iter(&self) -> impl Iterator<Item = &FocusScope>;\n+    pub fn zones_iter(&self) -> impl Iterator<Item = &FocusZone>;\n+    pub fn leaves_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n+    pub fn zones_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusZone>;\n+    pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = ChildScope<'_>>;\n     pub fn ancestor_zones(&self, key: &SpatialKey) -> Vec<&FocusZone>;\n\n     pub fn push_layer(&mut self, l: FocusLayer);\n@@ -184,16 +179,20 @@\n     pub fn children_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n     pub fn root_for_window(&self, label: &WindowLabel) -> Option<&FocusLayer>;\n     pub fn ancestors_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n+}\n\n-    pub fn scopes_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n+/// Variant-aware view returned by `children_of_zone`. Public, no internal-enum leak.\n+pub enum ChildScope<'a> {\n+    Leaf(&'a FocusScope),\n+    Zone(&'a FocusZone),\n }\n ```\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate. Wiring of the actual `spatial_*` invoke handlers is owned by downstream cards (`01KNQXXF5W7G4JP73C6ZCMKYKX` for `spatial_navigate`, plus the per-feature cards this task `blocks`).\n+The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate. The leaf-scope command is `spatial_register_scope` (renamed from `spatial_register_focusable` in card `01KQ5PP55SAAVJ0V3HDJ1DGNBY`); the container command stays `spatial_register_zone`.\n\n-### Tests — `swissarmyhammer-focus/tests/registry.rs`\n+### Tests — `swissarmyhammer-focus/tests/`\n\n Pure-Rust, no Tauri, no jsdom, no kanban. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n\n@@ -201,16 +200,16 @@\n\n - **`swissarmyhammer-focus`, not `swissarmyhammer-kanban`**: spatial nav is generic — opaque monikers, abstract rects. Pulling it out keeps it reusable and keeps the kanban crate focused. (`swissarmyhammer-commands` stays Tier 0.)\n - **`define_id!` macro reuse**: consistent with `TaskId`/`ColumnId`/`TagId`; avoids hand-rolled boilerplate.\n-- **Four peer types, not a `kind` field on one struct**: zone-only fields are type-checked.\n-- **`FocusScope` as a Rust enum**: one map, pattern matching.\n+- **Three peer types, not a `kind` field on one struct**: zone-only fields are type-checked.\n+- **No public sum-type enum**: registry stores leaves and zones via an internal discriminator; consumers iterate through typed accessors.\n - **`Pixels` arithmetic**: prevents accidental mixing of pixel values with unrelated floats.\n\n ### Subtasks\n - [x] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n - [x] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n-- [x] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n+- [x] Fill `swissarmyhammer-focus/src/scope.rs`: `FocusScope` (leaf), `FocusZone`, internal `RegisteredScope` enum + helper methods\n - [x] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n-- [x] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n+- [x] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops; `ChildScope` variant-aware view\n - [x] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window` (deferred to downstream cards that wire actual `spatial_*` handlers — this card produces the kernel they delegate to)\n\n ## Acceptance Criteria\n@@ -217,22 +216,22 @@\n - [x] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n - [x] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n - [x] No bare `String` or `f64` on any public type / command signature\n-- [x] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n+- [x] `FocusScope` (leaf), `FocusZone`, `FocusLayer` are distinct structs; no public sum-type enum\n - [x] `Pixels` supports arithmetic without `.0` access\n - [x] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n - [x] Tauri commands derive `WindowLabel` from `tauri::Window` (kernel design enforces this — concrete handlers wired by downstream cards)\n - [x] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n\n-## Tests (in `swissarmyhammer-focus/tests/registry.rs`)\n+## Tests (in `swissarmyhammer-focus/tests/focus_registry.rs`)\n - [x] Each newtype JSON-round-trips as a bare primitive\n - [x] `Pixels` arithmetic is type-preserving\n-- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n-- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n-- [x] Registry: `children_of_zone` direct children only\n+- [x] `FocusScope` and `FocusZone` each round-trip independently (no public enum to round-trip)\n+- [x] Registry: register a leaf + a zone; `scope(key)` returns `Some` only for leaves, `zone(key)` only for zones\n+- [x] Registry: `children_of_zone` direct children only (returns `ChildScope` enum)\n - [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n - [x] Registry: `children_of_layer` filtered by parent, not cross-window\n - [x] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [x] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n+- [x] Registry: `leaves_in_layer` / `zones_in_layer` typed iterators by `layer_key`\n - [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n@@ -247,4 +246,14 @@\n - `cargo build --workspace` — clean.\n - `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) last touched in unrelated refactor commit `b81336d42`; untouched for this card.\n\n-All kernel-types acceptance criteria satisfied. Tauri command wiring belongs to the downstream cards this task `blocks`.\n\\ No newline at end of file\n+All kernel-types acceptance criteria satisfied. Tauri command wiring belongs to the downstream cards this task `blocks`.\n+\n+## Verification (2026-04-26 — three-peer collapse)\n+\n+Card `01KQ5PP55SAAVJ0V3HDJ1DGNBY` rewrote the kernel as three peers:\n+- `Focusable` struct → `FocusScope` struct (leaf).\n+- Public `FocusScope` enum dropped; internal `RegisteredScope` enum stored in registry.\n+- Tauri command `spatial_register_focusable` → `spatial_register_scope`.\n+- Registry surface: `scope`/`zone` typed accessors, `leaves_in_layer`/`zones_in_layer` iterators, `ChildScope` view for variant-aware children.\n+\n+`cargo test -p swissarmyhammer-focus` — 89 tests pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -2,22 +2,6 @@\n\n Define the Rust kernel types for spatial focus inside the new `swissarmyhammer-focus` crate. These types **peer** with React components of the same names (see the React-primitives and FocusScope-refactor cards). UI is authoritative for structure — React declares the components and registers them via Tauri commands. Rust owns all computation (beam search, fallback, layer ops) and emits events.\n\n-## Update — three peers, not four (kanban card 01KQ5PP55SAAVJ0V3HDJ1DGNBY)\n-\n-This card was authored when the spatial-nav kernel was planned to expose **four peers**: `Focusable` (leaf), `FocusZone` (container), `FocusLayer` (modal boundary), and a composite `FocusScope` enum that the registry stored per `SpatialKey`. After implementing it that way, the composite-vs-primitive split smeared `showFocusBar`, click handling, and indicator rendering across two layers and turned out to be the systemic root of why visible focus was missing on most components.\n-\n-Card `01KQ5PP55SAAVJ0V3HDJ1DGNBY` collapsed the model into **three peers**:\n-\n-| Concept | Role | React | Rust |\n-|---|---|---|---|\n-| Layer | Modal boundary, hard nav stop | `<FocusLayer>` | `FocusLayer` |\n-| Zone | Navigable container | `<FocusZone>` | `FocusZone` |\n-| Scope | Leaf — shows focus, takes clicks, navigates | `<FocusScope>` | `FocusScope` |\n-\n-The leaf struct that used to be named `Focusable` is now `FocusScope`. The public `FocusScope` enum is gone — the registry stores leaves and zones via an internal discriminator that is not part of the public API. Consumers iterate the registry through `leaves_in_layer`, `zones_in_layer`, `leaves_iter`, and `zones_iter`, which yield the typed structs directly. The Tauri command `spatial_register_focusable` was renamed to `spatial_register_scope`. `<Focusable>` is a transitional re-export of `<FocusScope>` until the per-component cards finish migrating their imports; the file gets deleted in card `01KQ5PSMYE3Q60SV8270S6K819`.\n-\n-The terminology section below was originally written against the four-peer model. Read the **three-peer rewrite** below as the live definition; the four-peer wording is preserved underneath for historical context.\n-\n ### Crate placement\n\n **Lives in `swissarmyhammer-focus`** (created by card `01KQ2E7RPBPJ8T8KZX39N2SZ0A`). Spatial focus is generic — opaque `Moniker` strings, abstract `Rect`s, `WindowLabel`s. No kanban concepts, no Tauri, no domain types. Putting it in `swissarmyhammer-kanban` was wrong; the dedicated crate makes it reusable, easier to test, and keeps the kanban crate focused on board logic.\n@@ -26,25 +10,30 @@\n swissarmyhammer-focus/src/\n   lib.rs            (module declarations)\n   types.rs          ← THIS CARD: newtypes (WindowLabel, SpatialKey, LayerKey, Moniker, LayerName, Pixels) + Rect + Direction\n-  scope.rs          ← THIS CARD: FocusScope (leaf struct) + FocusZone struct + internal RegisteredScope enum\n+  scope.rs          ← THIS CARD: Focusable, FocusZone, FocusScope enum\n   layer.rs          ← THIS CARD: FocusLayer struct\n-  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops; ChildScope enum for variant-aware children\n+  registry.rs       ← THIS CARD: SpatialRegistry with scope + layer ops\n   state.rs          (SpatialState — card 01KNM3YHHFJ3...)\n   navigate.rs       (BeamNavStrategy impl — card 01KNQXXF5W...)\n   observer.rs       (FocusEventSink impls — card 01KQ2E7RPBPJ8...)\n ```\n\n-### The three peer types (live)\n+The crate skeleton + traits exist before this card runs (per the `01KQ2E7RPBPJ8...` dependency). This card fills `types.rs`, `scope.rs`, `layer.rs`, and `registry.rs`.\n\n+**`swissarmyhammer-kanban/src/focus.rs` is untouched** — that file holds `resolve_focused_column`, kanban-specific column resolver, which stays where it is. The earlier plan to migrate it to a `focus/` subdirectory is canceled.\n+\n+### The four peer types\n+\n | Role                | React component     | Rust type                                    |\n |---------------------|---------------------|----------------------------------------------|\n-| Leaf focus point    | `<FocusScope>`      | `swissarmyhammer_focus::FocusScope`          |\n-| Navigable container | `<FocusZone>`       | `swissarmyhammer_focus::FocusZone`           |\n-| Modal layer boundary| `<FocusLayer>`      | `swissarmyhammer_focus::FocusLayer`          |\n+| Leaf focusable point| `Focusable`         | `swissarmyhammer_focus::Focusable`           |\n+| Navigable container | `FocusZone`         | `swissarmyhammer_focus::FocusZone`           |\n+| Modal layer boundary| `FocusLayer`        | `swissarmyhammer_focus::FocusLayer`          |\n+| Entity-aware wrapper| `FocusScope`        | `swissarmyhammer_focus::FocusScope` (enum)   |\n\n-There is no public sum-type enum that conflates leaves and zones. The registry stores them via an internal `RegisteredScope` enum that is not exported.\n+### Terminology — canonical definitions\n\n-### Terminology — canonical definitions (three-peer rewrite)\n+These four terms are used everywhere in the spatial-nav plan. Definitions are normative; if another card uses these words differently, this card wins.\n\n **Layer** (`FocusLayer`)\n - A **hard modal boundary**. Spatial nav, fallback resolution, and zone tree walks **never cross a layer**.\n@@ -61,17 +50,20 @@\n - A zone *is* focusable — you can drill out to it, then nav between sibling zones (zone-level beam search).\n - Identified by `SpatialKey` (ULID per mount).\n\n-**Scope** (`FocusScope`)\n+**Focusable** (the primitive, lower-case \"focusable\" when used as a noun)\n - A **leaf focusable point** — atomic, no children, no zone-level features.\n - Examples: task title text, status pill, tag pill, mention pill, button, menu item, breadcrumb item.\n - Identified by `SpatialKey`.\n-- The Rust struct `FocusScope` is the leaf. The React `<FocusScope>` is the same — a leaf primitive that registers via `spatial_register_scope`, subscribes to per-key focus claims, renders the visible focus indicator, takes click events, and pushes the entity-aware `CommandScope` / right-click / double-click chrome.\n\n-The umbrella term \"**any registered focus point**\" still applies (a leaf or a zone, but never a layer); the internal `RegisteredScope` enum captures it for in-crate iteration. There is no public enum to navigate, by design.\n+**Scope** (`FocusScope`, the **umbrella term**)\n+- On Rust: the sum type `enum FocusScope { Focusable(Focusable), Zone(FocusZone) }`. This is what the registry stores per `SpatialKey`.\n+- On React: the **composite wrapper component** that adds entity plumbing (`CommandScope`, click-to-focus, context menu, focus bar, data-moniker) on top of one primitive (`<Focusable>` or `<FocusZone>`, picked via the `kind` prop).\n+- A \"scope\" in spatial-nav speak = \"any registered focus point\" = either a Focusable or a FocusZone. Not a Layer.\n+- \"Scope chain\" / `parent_zone` chain = walk from focused leaf up through ancestor zones to the layer root.\n\n ### Disambiguation: `CommandScope` is a separate concept\n\n-The existing kanban codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`. The React `<FocusScope>` component creates **both** a spatial entry (in `swissarmyhammer-focus`) **and** a `CommandScope` (in the kanban app). The two systems share the moniker but are otherwise independent.\n+The existing kanban codebase has `CommandScope` (in `kanban-app/ui/src/lib/command-scope.tsx`) — that is the **command-dispatch** boundary used to resolve which scope handles a dispatched command (like `ui.inspect`). It is *not* the same as `FocusScope`. The composite `<FocusScope>` React component creates **both** a spatial entry (in `swissarmyhammer-focus`) **and** a `CommandScope` (in the kanban app). The two systems share the moniker but are otherwise independent.\n\n `swissarmyhammer-focus` itself has no concept of `CommandScope` — it's pure spatial-nav, generic across consumers.\n\n@@ -81,7 +73,7 @@\n use swissarmyhammer_common::define_id;\n\n define_id!(WindowLabel, \"Tauri window label — which window a scope/layer lives in\");\n-define_id!(SpatialKey, \"ULID per FocusLayer/FocusZone/FocusScope instance\");\n+define_id!(SpatialKey, \"ULID per FocusScope/FocusZone/Focusable instance\");\n define_id!(LayerKey,   \"ULID per FocusLayer instance\");\n define_id!(Moniker,    \"Entity focus identity: \\\"task:01ABC\\\", \\\"ui:toolbar.new\\\"\");\n define_id!(LayerName,  \"Layer role: \\\"window\\\", \\\"inspector\\\", \\\"dialog\\\", \\\"palette\\\"\");\n@@ -115,12 +107,11 @@\n }\n ```\n\n-### The three peer types (live shape)\n+### The four types\n\n ```rust\n-/// Leaf — Rust peer of `<FocusScope>`.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n-pub struct FocusScope {\n+pub struct Focusable {\n     pub key: SpatialKey,\n     pub moniker: Moniker,\n     pub rect: Rect,\n@@ -129,7 +120,6 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n-/// Container — Rust peer of `<FocusZone>`.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusZone {\n     pub key: SpatialKey,\n@@ -141,7 +131,6 @@\n     pub overrides: HashMap<Direction, Option<Moniker>>,\n }\n\n-/// Modal layer — Rust peer of `<FocusLayer>`.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub struct FocusLayer {\n     pub key: LayerKey,\n@@ -150,27 +139,43 @@\n     pub window_label: WindowLabel,\n     pub last_focused: Option<SpatialKey>,\n }\n+\n+#[derive(Debug, Clone, Serialize, Deserialize)]\n+#[serde(tag = \"kind\", rename_all = \"snake_case\")]\n+pub enum FocusScope {\n+    Focusable(Focusable),\n+    Zone(FocusZone),\n+}\n+\n+impl FocusScope {\n+    pub fn key(&self) -> &SpatialKey;\n+    pub fn moniker(&self) -> &Moniker;\n+    pub fn rect(&self) -> &Rect;\n+    pub fn layer_key(&self) -> &LayerKey;\n+    pub fn parent_zone(&self) -> Option<&SpatialKey>;\n+    pub fn overrides(&self) -> &HashMap<Direction, Option<Moniker>>;\n+    pub fn is_zone(&self) -> bool;\n+    pub fn is_focusable(&self) -> bool;\n+    pub fn as_zone(&self) -> Option<&FocusZone>;\n+    pub fn as_zone_mut(&mut self) -> Option<&mut FocusZone>;\n+}\n ```\n\n ### Registry\n\n ```rust\n-pub struct SpatialRegistry { /* opaque */ }\n+pub struct SpatialRegistry {\n+    scopes: HashMap<SpatialKey, FocusScope>,\n+    layers: HashMap<LayerKey, FocusLayer>,\n+}\n\n impl SpatialRegistry {\n-    pub fn register_scope(&mut self, f: FocusScope);          // was register_focusable\n+    pub fn register_focusable(&mut self, f: Focusable);\n     pub fn register_zone(&mut self, z: FocusZone);\n     pub fn unregister_scope(&mut self, key: &SpatialKey);\n     pub fn update_rect(&mut self, key: &SpatialKey, rect: Rect);\n-\n-    pub fn scope(&self, key: &SpatialKey) -> Option<&FocusScope>;     // leaf only\n-    pub fn zone(&self, key: &SpatialKey) -> Option<&FocusZone>;       // zone only\n-    pub fn is_registered(&self, key: &SpatialKey) -> bool;            // variant-blind\n-    pub fn leaves_iter(&self) -> impl Iterator<Item = &FocusScope>;\n-    pub fn zones_iter(&self) -> impl Iterator<Item = &FocusZone>;\n-    pub fn leaves_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n-    pub fn zones_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusZone>;\n-    pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = ChildScope<'_>>;\n+    pub fn scope(&self, key: &SpatialKey) -> Option<&FocusScope>;\n+    pub fn children_of_zone(&self, zone_key: &SpatialKey) -> impl Iterator<Item = &FocusScope>;\n     pub fn ancestor_zones(&self, key: &SpatialKey) -> Vec<&FocusZone>;\n\n     pub fn push_layer(&mut self, l: FocusLayer);\n@@ -179,20 +184,16 @@\n     pub fn children_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n     pub fn root_for_window(&self, label: &WindowLabel) -> Option<&FocusLayer>;\n     pub fn ancestors_of_layer(&self, key: &LayerKey) -> Vec<&FocusLayer>;\n-}\n\n-/// Variant-aware view returned by `children_of_zone`. Public, no internal-enum leak.\n-pub enum ChildScope<'a> {\n-    Leaf(&'a FocusScope),\n-    Zone(&'a FocusZone),\n+    pub fn scopes_in_layer(&self, key: &LayerKey) -> impl Iterator<Item = &FocusScope>;\n }\n ```\n\n ### Tauri commands — in `kanban-app/src/commands.rs`\n\n-The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate. The leaf-scope command is `spatial_register_scope` (renamed from `spatial_register_focusable` in card `01KQ5PP55SAAVJ0V3HDJ1DGNBY`); the container command stays `spatial_register_zone`.\n+The headless registry lives in `swissarmyhammer-focus`. Tauri adapters live in `kanban-app/src/commands.rs`, each deriving `WindowLabel` from the `tauri::Window` parameter and delegating to the focus crate. Wiring of the actual `spatial_*` invoke handlers is owned by downstream cards (`01KNQXXF5W7G4JP73C6ZCMKYKX` for `spatial_navigate`, plus the per-feature cards this task `blocks`).\n\n-### Tests — `swissarmyhammer-focus/tests/`\n+### Tests — `swissarmyhammer-focus/tests/registry.rs`\n\n Pure-Rust, no Tauri, no jsdom, no kanban. Constructs synthetic `Rect`/`SpatialKey`/`Moniker` values with `::from_string` / `Pixels(..)`.\n\n@@ -200,16 +201,16 @@\n\n - **`swissarmyhammer-focus`, not `swissarmyhammer-kanban`**: spatial nav is generic — opaque monikers, abstract rects. Pulling it out keeps it reusable and keeps the kanban crate focused. (`swissarmyhammer-commands` stays Tier 0.)\n - **`define_id!` macro reuse**: consistent with `TaskId`/`ColumnId`/`TagId`; avoids hand-rolled boilerplate.\n-- **Three peer types, not a `kind` field on one struct**: zone-only fields are type-checked.\n-- **No public sum-type enum**: registry stores leaves and zones via an internal discriminator; consumers iterate through typed accessors.\n+- **Four peer types, not a `kind` field on one struct**: zone-only fields are type-checked.\n+- **`FocusScope` as a Rust enum**: one map, pattern matching.\n - **`Pixels` arithmetic**: prevents accidental mixing of pixel values with unrelated floats.\n\n ### Subtasks\n - [x] (Skeleton + traits exist already from `01KQ2E7RPBPJ8...`)\n - [x] Fill `swissarmyhammer-focus/src/types.rs`: `define_id!` newtypes; hand-roll `Pixels` + arithmetic; `Rect` + accessors; `Direction` enum\n-- [x] Fill `swissarmyhammer-focus/src/scope.rs`: `FocusScope` (leaf), `FocusZone`, internal `RegisteredScope` enum + helper methods\n+- [x] Fill `swissarmyhammer-focus/src/scope.rs`: `Focusable`, `FocusZone`, `FocusScope` enum + helper methods\n - [x] Fill `swissarmyhammer-focus/src/layer.rs`: `FocusLayer`\n-- [x] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops; `ChildScope` variant-aware view\n+- [x] Fill `swissarmyhammer-focus/src/registry.rs`: `SpatialRegistry` with all scope + layer + forest + zone-tree ops\n - [x] Tauri commands in `kanban-app/src/commands.rs` import from `swissarmyhammer_focus::*`; derive `WindowLabel` from `tauri::Window` (deferred to downstream cards that wire actual `spatial_*` handlers — this card produces the kernel they delegate to)\n\n ## Acceptance Criteria\n@@ -216,22 +217,22 @@\n - [x] All types live in `swissarmyhammer-focus` — NOT `swissarmyhammer-kanban`\n - [x] String-valued newtypes use `define_id!`; no hand-rolled `#[serde(transparent)]` String wrappers\n - [x] No bare `String` or `f64` on any public type / command signature\n-- [x] `FocusScope` (leaf), `FocusZone`, `FocusLayer` are distinct structs; no public sum-type enum\n+- [x] `Focusable`, `FocusZone`, `FocusLayer` are distinct structs; `FocusScope` is an enum over `Focusable | Zone`\n - [x] `Pixels` supports arithmetic without `.0` access\n - [x] `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) untouched\n - [x] Tauri commands derive `WindowLabel` from `tauri::Window` (kernel design enforces this — concrete handlers wired by downstream cards)\n - [x] `cargo test -p swissarmyhammer-focus` passes; no Tauri or jsdom required\n\n-## Tests (in `swissarmyhammer-focus/tests/focus_registry.rs`)\n+## Tests (in `swissarmyhammer-focus/tests/registry.rs`)\n - [x] Each newtype JSON-round-trips as a bare primitive\n - [x] `Pixels` arithmetic is type-preserving\n-- [x] `FocusScope` and `FocusZone` each round-trip independently (no public enum to round-trip)\n-- [x] Registry: register a leaf + a zone; `scope(key)` returns `Some` only for leaves, `zone(key)` only for zones\n-- [x] Registry: `children_of_zone` direct children only (returns `ChildScope` enum)\n+- [x] `FocusScope::Focusable(_)` and `FocusScope::Zone(_)` round-trip with `\"kind\"` tag\n+- [x] Registry: register a Focusable + a FocusZone; `scope(key)` returns the right variant\n+- [x] Registry: `children_of_zone` direct children only\n - [x] Registry: `ancestor_zones` walks `parent_zone` up to layer root\n - [x] Registry: `children_of_layer` filtered by parent, not cross-window\n - [x] Registry: `ancestors_of_layer` walks `layer.parent`\n-- [x] Registry: `leaves_in_layer` / `zones_in_layer` typed iterators by `layer_key`\n+- [x] Registry: `scopes_in_layer` returns Focusables and Zones by `layer_key`\n - [x] Registry: 2 windows + 2 inspector layers + 1 dialog = 5 layers, 2 roots, correct chains\n\n ## Workflow\n@@ -246,14 +247,4 @@\n - `cargo build --workspace` — clean.\n - `swissarmyhammer-kanban/src/focus.rs` (`resolve_focused_column`) last touched in unrelated refactor commit `b81336d42`; untouched for this card.\n\n-All kernel-types acceptance criteria satisfied. Tauri command wiring belongs to the downstream cards this task `blocks`.\n-\n-## Verification (2026-04-26 — three-peer collapse)\n-\n-Card `01KQ5PP55SAAVJ0V3HDJ1DGNBY` rewrote the kernel as three peers:\n-- `Focusable` struct → `FocusScope` struct (leaf).\n-- Public `FocusScope` enum dropped; internal `RegisteredScope` enum stored in registry.\n-- Tauri command `spatial_register_focusable` → `spatial_register_scope`.\n-- Registry surface: `scope`/`zone` typed accessors, `leaves_in_layer`/`zones_in_layer` iterators, `ChildScope` view for variant-aware children.\n-\n-`cargo test -p swissarmyhammer-focus` — 89 tests pass.\n\\ No newline at end of file\n+All kernel-types acceptance criteria satisfied. Tauri command wiring belongs to the downstream cards this task `blocks`.\n\\ No newline at end of file\n"}]]}

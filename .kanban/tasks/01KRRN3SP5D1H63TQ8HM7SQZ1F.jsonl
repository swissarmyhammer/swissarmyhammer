{"id":"01KRRN3SP6CNDJ1B61HMH5VART","timestamp":"2026-05-16T15:05:11.622004Z","op":"create","item_id":"01KRRN3SP5D1H63TQ8HM7SQZ1F","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,35 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KRRN2M0E5YXA9E24N1BM2BVP\n+position_column: todo\n+position_ordinal: '8480'\n+project: ai-panel\n+title: ACP agent process manager + model enumeration (kanban-app backend)\n+---\n+## What\n+The kanban-app Tauri backend manages the ACP agent process lifecycle — and nothing else about ACP. It is NOT in the ACP data path: it never pipes ACP bytes and never adapts the protocol through Tauri events. Its entire job is \"spawn the agent, hand the webview a `ws://` URL, kill it later.\"\n+\n+- New `apps/kanban-app/src/ai/agent_process.rs`.\n+- Detect Claude Code: `claude` resolvable on `PATH` (honor a `CLAUDE_CLI` override).\n+- Enumerate selectable models: a Claude Code entry (gated on detection — present-but-disabled with a hint when absent) plus configured local models from `swissarmyhammer-config`.\n+- For a chosen model, spawn the WebSocket-mode ACP agent — `sah agent acp --listen 127.0.0.1:0` with the model's config (Claude Code vs local llama) — and capture the bound `ws://127.0.0.1:<port>` URL.\n+- Resolve the `sah` agent binary: bundle it as a Tauri sidecar like the existing `kanban` sidecar, or require it on `PATH`. Resolve and document; reuse `cli_install::resolve_bundled_cli`-style logic. Also resolve the bundled `kanban` path for the webview to build the `mcpServers` entry.\n+- Tauri commands (register in `main.rs`): `ai_list_models() -> Vec<Model>` (`{ id, label, kind, available, hint }`); `ai_start_agent(model_id) -> { wsUrl, kanbanCommand }`; `ai_stop_agent(handle)`. `ai_start_agent` returns only the ws URL plus the resolved `kanban` command — a one-time config handoff, not a data channel.\n+- Track agent child processes in `AppState`; kill them on window/board/app teardown.\n+\n+## Acceptance Criteria\n+- [ ] `claude` detection works (`Some` when on `PATH`/`CLAUDE_CLI`, else `None`).\n+- [ ] `ai_list_models` returns a Claude Code entry with `available` reflecting detection, plus configured local models.\n+- [ ] `ai_start_agent` spawns the WebSocket-mode agent and returns a `ws://127.0.0.1:<port>` URL that a WebSocket can connect to.\n+- [ ] Agent processes are killed on teardown — no orphans.\n+- [ ] `cargo build -p kanban-app` is clean.\n+\n+## Tests\n+- [ ] Unit tests: `claude` detection against a fake binary on `PATH`; model enumeration shape.\n+- [ ] Integration test: `ai_start_agent` returns a URL a WebSocket client can connect to and `initialize` over; `ai_stop_agent` (and teardown) kills the process.\n+- [ ] `cargo test -p kanban-app` is green.\n+\n+## Workflow\n+- Use `/tdd` — write the detection and `ai_start_agent` round-trip tests first.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,35 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KRRN2M0E5YXA9E24N1BM2BVP\n-position_column: todo\n-position_ordinal: '8480'\n-project: ai-panel\n-title: ACP agent process manager + model enumeration (kanban-app backend)\n----\n-## What\n-The kanban-app Tauri backend manages the ACP agent process lifecycle — and nothing else about ACP. It is NOT in the ACP data path: it never pipes ACP bytes and never adapts the protocol through Tauri events. Its entire job is \"spawn the agent, hand the webview a `ws://` URL, kill it later.\"\n-\n-- New `apps/kanban-app/src/ai/agent_process.rs`.\n-- Detect Claude Code: `claude` resolvable on `PATH` (honor a `CLAUDE_CLI` override).\n-- Enumerate selectable models: a Claude Code entry (gated on detection — present-but-disabled with a hint when absent) plus configured local models from `swissarmyhammer-config`.\n-- For a chosen model, spawn the WebSocket-mode ACP agent — `sah agent acp --listen 127.0.0.1:0` with the model's config (Claude Code vs local llama) — and capture the bound `ws://127.0.0.1:<port>` URL.\n-- Resolve the `sah` agent binary: bundle it as a Tauri sidecar like the existing `kanban` sidecar, or require it on `PATH`. Resolve and document; reuse `cli_install::resolve_bundled_cli`-style logic. Also resolve the bundled `kanban` path for the webview to build the `mcpServers` entry.\n-- Tauri commands (register in `main.rs`): `ai_list_models() -> Vec<Model>` (`{ id, label, kind, available, hint }`); `ai_start_agent(model_id) -> { wsUrl, kanbanCommand }`; `ai_stop_agent(handle)`. `ai_start_agent` returns only the ws URL plus the resolved `kanban` command — a one-time config handoff, not a data channel.\n-- Track agent child processes in `AppState`; kill them on window/board/app teardown.\n-\n-## Acceptance Criteria\n-- [ ] `claude` detection works (`Some` when on `PATH`/`CLAUDE_CLI`, else `None`).\n-- [ ] `ai_list_models` returns a Claude Code entry with `available` reflecting detection, plus configured local models.\n-- [ ] `ai_start_agent` spawns the WebSocket-mode agent and returns a `ws://127.0.0.1:<port>` URL that a WebSocket can connect to.\n-- [ ] Agent processes are killed on teardown — no orphans.\n-- [ ] `cargo build -p kanban-app` is clean.\n-\n-## Tests\n-- [ ] Unit tests: `claude` detection against a fake binary on `PATH`; model enumeration shape.\n-- [ ] Integration test: `ai_start_agent` returns a URL a WebSocket client can connect to and `initialize` over; `ai_stop_agent` (and teardown) kills the process.\n-- [ ] `cargo test -p kanban-app` is green.\n-\n-## Workflow\n-- Use `/tdd` — write the detection and `ai_start_agent` round-trip tests first.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRRQRW4FYJ30C1VJAWYQ7KKH","timestamp":"2026-05-16T15:51:39.407806Z","op":"update","item_id":"01KRRN3SP5D1H63TQ8HM7SQZ1F","forward_patch":"--- original\n+++ modified\n@@ -6,29 +6,28 @@\n position_column: todo\n position_ordinal: '8480'\n project: ai-panel\n-title: ACP agent process manager + model enumeration (kanban-app backend)\n+title: Model selection and the AI agent endpoint command surface\n ---\n ## What\n-The kanban-app Tauri backend manages the ACP agent process lifecycle — and nothing else about ACP. It is NOT in the ACP data path: it never pipes ACP bytes and never adapts the protocol through Tauri events. Its entire job is \"spawn the agent, hand the webview a `ws://` URL, kill it later.\"\n+Wire model choice to the in-process ACP agent (the \"WebSocket ACP agent\" task) and give the webview the commands it needs.\n\n-- New `apps/kanban-app/src/ai/agent_process.rs`.\n+- New `apps/kanban-app/src/ai/models.rs`.\n - Detect Claude Code: `claude` resolvable on `PATH` (honor a `CLAUDE_CLI` override).\n-- Enumerate selectable models: a Claude Code entry (gated on detection — present-but-disabled with a hint when absent) plus configured local models from `swissarmyhammer-config`.\n-- For a chosen model, spawn the WebSocket-mode ACP agent — `sah agent acp --listen 127.0.0.1:0` with the model's config (Claude Code vs local llama) — and capture the bound `ws://127.0.0.1:<port>` URL.\n-- Resolve the `sah` agent binary: bundle it as a Tauri sidecar like the existing `kanban` sidecar, or require it on `PATH`. Resolve and document; reuse `cli_install::resolve_bundled_cli`-style logic. Also resolve the bundled `kanban` path for the webview to build the `mcpServers` entry.\n-- Tauri commands (register in `main.rs`): `ai_list_models() -> Vec<Model>` (`{ id, label, kind, available, hint }`); `ai_start_agent(model_id) -> { wsUrl, kanbanCommand }`; `ai_stop_agent(handle)`. `ai_start_agent` returns only the ws URL plus the resolved `kanban` command — a one-time config handoff, not a data channel.\n-- Track agent child processes in `AppState`; kill them on window/board/app teardown.\n+- Enumerate selectable models: a Claude Code entry (gated on detection — present-but-disabled with a hint when absent) plus configured local llama models from `swissarmyhammer-config` (only when the `ai-local-models` Cargo feature is built).\n+- Tie selection to the in-process agent: when the webview picks a model, the backend prepares the in-process agent endpoint for that `ModelConfig` and yields the `ws://127.0.0.1:<port>` URL the TypeScript client connects to.\n+- Tauri commands (register in `main.rs`): `ai_list_models() -> Vec<Model>` (`{ id, label, kind, available, hint }`); `ai_start_agent(model_id) -> { wsUrl, kanbanCommand }` — a one-time config handoff: the loopback `ws://` URL plus the resolved bundled `kanban` command for the TS client's `newSession.mcpServers` entry. This is config discovery, NOT a data channel.\n+- Lifecycle: stop the in-process agent and its WebSocket server on window/board/app teardown; track in `AppState`.\n\n ## Acceptance Criteria\n-- [ ] `claude` detection works (`Some` when on `PATH`/`CLAUDE_CLI`, else `None`).\n-- [ ] `ai_list_models` returns a Claude Code entry with `available` reflecting detection, plus configured local models.\n-- [ ] `ai_start_agent` spawns the WebSocket-mode agent and returns a `ws://127.0.0.1:<port>` URL that a WebSocket can connect to.\n-- [ ] Agent processes are killed on teardown — no orphans.\n+- [ ] `claude` detection returns `Some` when on `PATH`/`CLAUDE_CLI`, else `None`.\n+- [ ] `ai_list_models` returns a Claude Code entry with `available` reflecting detection, plus local llama models when built with `ai-local-models`.\n+- [ ] `ai_start_agent` returns a `ws://127.0.0.1:<port>` URL a WebSocket can `initialize` over for the chosen model, plus the resolved `kanban` command.\n+- [ ] Teardown stops the in-process agent and WS server — no leaks.\n - [ ] `cargo build -p kanban-app` is clean.\n\n ## Tests\n-- [ ] Unit tests: `claude` detection against a fake binary on `PATH`; model enumeration shape.\n-- [ ] Integration test: `ai_start_agent` returns a URL a WebSocket client can connect to and `initialize` over; `ai_stop_agent` (and teardown) kills the process.\n+- [ ] Unit tests: `claude` detection against a fake binary on `PATH`; model enumeration shape (with/without `ai-local-models`).\n+- [ ] Integration test: `ai_start_agent` returns a URL a WebSocket client connects to and `initialize`s over for the selected model; teardown stops it.\n - [ ] `cargo test -p kanban-app` is green.\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -6,28 +6,29 @@\n position_column: todo\n position_ordinal: '8480'\n project: ai-panel\n-title: Model selection and the AI agent endpoint command surface\n+title: ACP agent process manager + model enumeration (kanban-app backend)\n ---\n ## What\n-Wire model choice to the in-process ACP agent (the \"WebSocket ACP agent\" task) and give the webview the commands it needs.\n+The kanban-app Tauri backend manages the ACP agent process lifecycle — and nothing else about ACP. It is NOT in the ACP data path: it never pipes ACP bytes and never adapts the protocol through Tauri events. Its entire job is \"spawn the agent, hand the webview a `ws://` URL, kill it later.\"\n\n-- New `apps/kanban-app/src/ai/models.rs`.\n+- New `apps/kanban-app/src/ai/agent_process.rs`.\n - Detect Claude Code: `claude` resolvable on `PATH` (honor a `CLAUDE_CLI` override).\n-- Enumerate selectable models: a Claude Code entry (gated on detection — present-but-disabled with a hint when absent) plus configured local llama models from `swissarmyhammer-config` (only when the `ai-local-models` Cargo feature is built).\n-- Tie selection to the in-process agent: when the webview picks a model, the backend prepares the in-process agent endpoint for that `ModelConfig` and yields the `ws://127.0.0.1:<port>` URL the TypeScript client connects to.\n-- Tauri commands (register in `main.rs`): `ai_list_models() -> Vec<Model>` (`{ id, label, kind, available, hint }`); `ai_start_agent(model_id) -> { wsUrl, kanbanCommand }` — a one-time config handoff: the loopback `ws://` URL plus the resolved bundled `kanban` command for the TS client's `newSession.mcpServers` entry. This is config discovery, NOT a data channel.\n-- Lifecycle: stop the in-process agent and its WebSocket server on window/board/app teardown; track in `AppState`.\n+- Enumerate selectable models: a Claude Code entry (gated on detection — present-but-disabled with a hint when absent) plus configured local models from `swissarmyhammer-config`.\n+- For a chosen model, spawn the WebSocket-mode ACP agent — `sah agent acp --listen 127.0.0.1:0` with the model's config (Claude Code vs local llama) — and capture the bound `ws://127.0.0.1:<port>` URL.\n+- Resolve the `sah` agent binary: bundle it as a Tauri sidecar like the existing `kanban` sidecar, or require it on `PATH`. Resolve and document; reuse `cli_install::resolve_bundled_cli`-style logic. Also resolve the bundled `kanban` path for the webview to build the `mcpServers` entry.\n+- Tauri commands (register in `main.rs`): `ai_list_models() -> Vec<Model>` (`{ id, label, kind, available, hint }`); `ai_start_agent(model_id) -> { wsUrl, kanbanCommand }`; `ai_stop_agent(handle)`. `ai_start_agent` returns only the ws URL plus the resolved `kanban` command — a one-time config handoff, not a data channel.\n+- Track agent child processes in `AppState`; kill them on window/board/app teardown.\n\n ## Acceptance Criteria\n-- [ ] `claude` detection returns `Some` when on `PATH`/`CLAUDE_CLI`, else `None`.\n-- [ ] `ai_list_models` returns a Claude Code entry with `available` reflecting detection, plus local llama models when built with `ai-local-models`.\n-- [ ] `ai_start_agent` returns a `ws://127.0.0.1:<port>` URL a WebSocket can `initialize` over for the chosen model, plus the resolved `kanban` command.\n-- [ ] Teardown stops the in-process agent and WS server — no leaks.\n+- [ ] `claude` detection works (`Some` when on `PATH`/`CLAUDE_CLI`, else `None`).\n+- [ ] `ai_list_models` returns a Claude Code entry with `available` reflecting detection, plus configured local models.\n+- [ ] `ai_start_agent` spawns the WebSocket-mode agent and returns a `ws://127.0.0.1:<port>` URL that a WebSocket can connect to.\n+- [ ] Agent processes are killed on teardown — no orphans.\n - [ ] `cargo build -p kanban-app` is clean.\n\n ## Tests\n-- [ ] Unit tests: `claude` detection against a fake binary on `PATH`; model enumeration shape (with/without `ai-local-models`).\n-- [ ] Integration test: `ai_start_agent` returns a URL a WebSocket client connects to and `initialize`s over for the selected model; teardown stops it.\n+- [ ] Unit tests: `claude` detection against a fake binary on `PATH`; model enumeration shape.\n+- [ ] Integration test: `ai_start_agent` returns a URL a WebSocket client can connect to and `initialize` over; `ai_stop_agent` (and teardown) kills the process.\n - [ ] `cargo test -p kanban-app` is green.\n\n ## Workflow\n","transaction_id":null}
{"id":"01KRRRB60HDK26JSXGEMEJCM49","timestamp":"2026-05-16T16:01:39.345662Z","op":"update","item_id":"01KRRN3SP5D1H63TQ8HM7SQZ1F","forward_patch":"--- original\n+++ modified\n@@ -3,6 +3,7 @@\n - claude-code\n depends_on:\n - 01KRRN2M0E5YXA9E24N1BM2BVP\n+- 01KRRR9R082SKN6HY5DA2EEGBX\n position_column: todo\n position_ordinal: '8480'\n project: ai-panel\n@@ -9,25 +10,25 @@\n title: Model selection and the AI agent endpoint command surface\n ---\n ## What\n-Wire model choice to the in-process ACP agent (the \"WebSocket ACP agent\" task) and give the webview the commands it needs.\n+Wire model choice to the in-process ACP agent (the \"WebSocket ACP agent\" task) and give the webview the two endpoints it needs.\n\n - New `apps/kanban-app/src/ai/models.rs`.\n - Detect Claude Code: `claude` resolvable on `PATH` (honor a `CLAUDE_CLI` override).\n - Enumerate selectable models: a Claude Code entry (gated on detection — present-but-disabled with a hint when absent) plus configured local llama models from `swissarmyhammer-config` (only when the `ai-local-models` Cargo feature is built).\n-- Tie selection to the in-process agent: when the webview picks a model, the backend prepares the in-process agent endpoint for that `ModelConfig` and yields the `ws://127.0.0.1:<port>` URL the TypeScript client connects to.\n-- Tauri commands (register in `main.rs`): `ai_list_models() -> Vec<Model>` (`{ id, label, kind, available, hint }`); `ai_start_agent(model_id) -> { wsUrl, kanbanCommand }` — a one-time config handoff: the loopback `ws://` URL plus the resolved bundled `kanban` command for the TS client's `newSession.mcpServers` entry. This is config discovery, NOT a data channel.\n+- Tie selection to the in-process agent: when the webview picks a model, the backend prepares the in-process agent endpoint for that `ModelConfig` and yields the `ws://127.0.0.1:<port>` URL.\n+- Tauri commands (register in `main.rs`): `ai_list_models() -> Vec<Model>` (`{ id, label, kind, available, hint }`); `ai_start_agent(model_id, board_path) -> { wsUrl, mcpUrl }` — a one-time config handoff: the loopback `ws://` agent URL plus the board's full-SAH-toolset MCP URL (`http://127.0.0.1:<port>/mcp` from the per-board toolset server). The TypeScript client puts `mcpUrl` in `newSession.mcpServers`. This is config discovery, NOT a data channel.\n - Lifecycle: stop the in-process agent and its WebSocket server on window/board/app teardown; track in `AppState`.\n\n ## Acceptance Criteria\n - [ ] `claude` detection returns `Some` when on `PATH`/`CLAUDE_CLI`, else `None`.\n - [ ] `ai_list_models` returns a Claude Code entry with `available` reflecting detection, plus local llama models when built with `ai-local-models`.\n-- [ ] `ai_start_agent` returns a `ws://127.0.0.1:<port>` URL a WebSocket can `initialize` over for the chosen model, plus the resolved `kanban` command.\n+- [ ] `ai_start_agent` returns a `ws://127.0.0.1:<port>` URL a WebSocket can `initialize` over for the chosen model, plus the board's full-SAH-toolset `mcpUrl`.\n - [ ] Teardown stops the in-process agent and WS server — no leaks.\n - [ ] `cargo build -p kanban-app` is clean.\n\n ## Tests\n - [ ] Unit tests: `claude` detection against a fake binary on `PATH`; model enumeration shape (with/without `ai-local-models`).\n-- [ ] Integration test: `ai_start_agent` returns a URL a WebSocket client connects to and `initialize`s over for the selected model; teardown stops it.\n+- [ ] Integration test: `ai_start_agent` returns a `wsUrl` a WebSocket client `initialize`s over for the selected model, and an `mcpUrl` that resolves to the board's SAH toolset; teardown stops it.\n - [ ] `cargo test -p kanban-app` is green.\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,6 @@\n - claude-code\n depends_on:\n - 01KRRN2M0E5YXA9E24N1BM2BVP\n-- 01KRRR9R082SKN6HY5DA2EEGBX\n position_column: todo\n position_ordinal: '8480'\n project: ai-panel\n@@ -10,25 +9,25 @@\n title: Model selection and the AI agent endpoint command surface\n ---\n ## What\n-Wire model choice to the in-process ACP agent (the \"WebSocket ACP agent\" task) and give the webview the two endpoints it needs.\n+Wire model choice to the in-process ACP agent (the \"WebSocket ACP agent\" task) and give the webview the commands it needs.\n\n - New `apps/kanban-app/src/ai/models.rs`.\n - Detect Claude Code: `claude` resolvable on `PATH` (honor a `CLAUDE_CLI` override).\n - Enumerate selectable models: a Claude Code entry (gated on detection — present-but-disabled with a hint when absent) plus configured local llama models from `swissarmyhammer-config` (only when the `ai-local-models` Cargo feature is built).\n-- Tie selection to the in-process agent: when the webview picks a model, the backend prepares the in-process agent endpoint for that `ModelConfig` and yields the `ws://127.0.0.1:<port>` URL.\n-- Tauri commands (register in `main.rs`): `ai_list_models() -> Vec<Model>` (`{ id, label, kind, available, hint }`); `ai_start_agent(model_id, board_path) -> { wsUrl, mcpUrl }` — a one-time config handoff: the loopback `ws://` agent URL plus the board's full-SAH-toolset MCP URL (`http://127.0.0.1:<port>/mcp` from the per-board toolset server). The TypeScript client puts `mcpUrl` in `newSession.mcpServers`. This is config discovery, NOT a data channel.\n+- Tie selection to the in-process agent: when the webview picks a model, the backend prepares the in-process agent endpoint for that `ModelConfig` and yields the `ws://127.0.0.1:<port>` URL the TypeScript client connects to.\n+- Tauri commands (register in `main.rs`): `ai_list_models() -> Vec<Model>` (`{ id, label, kind, available, hint }`); `ai_start_agent(model_id) -> { wsUrl, kanbanCommand }` — a one-time config handoff: the loopback `ws://` URL plus the resolved bundled `kanban` command for the TS client's `newSession.mcpServers` entry. This is config discovery, NOT a data channel.\n - Lifecycle: stop the in-process agent and its WebSocket server on window/board/app teardown; track in `AppState`.\n\n ## Acceptance Criteria\n - [ ] `claude` detection returns `Some` when on `PATH`/`CLAUDE_CLI`, else `None`.\n - [ ] `ai_list_models` returns a Claude Code entry with `available` reflecting detection, plus local llama models when built with `ai-local-models`.\n-- [ ] `ai_start_agent` returns a `ws://127.0.0.1:<port>` URL a WebSocket can `initialize` over for the chosen model, plus the board's full-SAH-toolset `mcpUrl`.\n+- [ ] `ai_start_agent` returns a `ws://127.0.0.1:<port>` URL a WebSocket can `initialize` over for the chosen model, plus the resolved `kanban` command.\n - [ ] Teardown stops the in-process agent and WS server — no leaks.\n - [ ] `cargo build -p kanban-app` is clean.\n\n ## Tests\n - [ ] Unit tests: `claude` detection against a fake binary on `PATH`; model enumeration shape (with/without `ai-local-models`).\n-- [ ] Integration test: `ai_start_agent` returns a `wsUrl` a WebSocket client `initialize`s over for the selected model, and an `mcpUrl` that resolves to the board's SAH toolset; teardown stops it.\n+- [ ] Integration test: `ai_start_agent` returns a URL a WebSocket client connects to and `initialize`s over for the selected model; teardown stops it.\n - [ ] `cargo test -p kanban-app` is green.\n\n ## Workflow\n","transaction_id":null}
{"id":"01KRV55NRXB38B5JEAMW56Y6J3","timestamp":"2026-05-17T14:24:19.229161Z","op":"update","item_id":"01KRRN3SP5D1H63TQ8HM7SQZ1F","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRRN2M0E5YXA9E24N1BM2BVP\n - 01KRRR9R082SKN6HY5DA2EEGBX\n-position_column: todo\n-position_ordinal: '8480'\n+position_column: doing\n+position_ordinal: '80'\n project: ai-panel\n title: Model selection and the AI agent endpoint command surface\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRRN2M0E5YXA9E24N1BM2BVP\n - 01KRRR9R082SKN6HY5DA2EEGBX\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '8480'\n project: ai-panel\n title: Model selection and the AI agent endpoint command surface\n ---\n","transaction_id":null}
{"id":"01KRV60692FWP7CAWPQ9RS6G03","timestamp":"2026-05-17T14:38:48.098479Z","op":"update","item_id":"01KRRN3SP5D1H63TQ8HM7SQZ1F","forward_patch":"--- original\n+++ modified\n@@ -14,22 +14,44 @@\n\n - New `apps/kanban-app/src/ai/models.rs`.\n - Detect Claude Code: `claude` resolvable on `PATH` (honor a `CLAUDE_CLI` override).\n-- Enumerate selectable models: a Claude Code entry (gated on detection — present-but-disabled with a hint when absent) plus configured local llama models from `swissarmyhammer-config` (only when the `ai-local-models` Cargo feature is built).\n+- Enumerate selectable models: a Claude Code entry (gated on detection — present-but-disabled with a hint when absent) plus configured local llama models from `swissarmyhammer-config`.\n - Tie selection to the in-process agent: when the webview picks a model, the backend prepares the in-process agent endpoint for that `ModelConfig` and yields the `ws://127.0.0.1:<port>` URL.\n - Tauri commands (register in `main.rs`): `ai_list_models() -> Vec<Model>` (`{ id, label, kind, available, hint }`); `ai_start_agent(model_id, board_path) -> { wsUrl, mcpUrl }` — a one-time config handoff: the loopback `ws://` agent URL plus the board's full-SAH-toolset MCP URL (`http://127.0.0.1:<port>/mcp` from the per-board toolset server). The TypeScript client puts `mcpUrl` in `newSession.mcpServers`. This is config discovery, NOT a data channel.\n - Lifecycle: stop the in-process agent and its WebSocket server on window/board/app teardown; track in `AppState`.\n\n ## Acceptance Criteria\n-- [ ] `claude` detection returns `Some` when on `PATH`/`CLAUDE_CLI`, else `None`.\n-- [ ] `ai_list_models` returns a Claude Code entry with `available` reflecting detection, plus local llama models when built with `ai-local-models`.\n-- [ ] `ai_start_agent` returns a `ws://127.0.0.1:<port>` URL a WebSocket can `initialize` over for the chosen model, plus the board's full-SAH-toolset `mcpUrl`.\n-- [ ] Teardown stops the in-process agent and WS server — no leaks.\n-- [ ] `cargo build -p kanban-app` is clean.\n+- [x] `claude` detection returns `Some` when on `PATH`/`CLAUDE_CLI`, else `None`.\n+- [x] `ai_list_models` returns a Claude Code entry with `available` reflecting detection, plus local llama models (enumerated unconditionally from `swissarmyhammer-config` — see Implementation Notes re: the non-existent `ai-local-models` feature).\n+- [x] `ai_start_agent` returns a `ws://127.0.0.1:<port>` URL a WebSocket can `initialize` over for the chosen model, plus the board's full-SAH-toolset `mcpUrl`.\n+- [x] Teardown stops the in-process agent and WS server — no leaks.\n+- [x] `cargo build -p kanban-app` is clean.\n\n ## Tests\n-- [ ] Unit tests: `claude` detection against a fake binary on `PATH`; model enumeration shape (with/without `ai-local-models`).\n-- [ ] Integration test: `ai_start_agent` returns a `wsUrl` a WebSocket client `initialize`s over for the selected model, and an `mcpUrl` that resolves to the board's SAH toolset; teardown stops it.\n-- [ ] `cargo test -p kanban-app` is green.\n+- [x] Unit tests: `claude` detection against a fake binary on `PATH`; model enumeration shape (Claude entry available/unavailable, local llama models enumerated, embedding models excluded).\n+- [x] Integration test: `ai_start_agent`'s round trip — `RunningAgents::start` returns a `wsUrl` a WebSocket client `initialize`s over for the selected model; `stop`/`stop_all` teardown frees the port; re-selecting a model replaces the endpoint. The board's `mcpUrl` is covered by `state.rs`'s `test_open_board_serves_full_sah_mcp_toolset`.\n+- [x] `cargo test -p kanban-app` is green.\n\n ## Workflow\n-- Use `/tdd` — write the detection and `ai_start_agent` round-trip tests first.\n\\ No newline at end of file\n+- Use `/tdd` — write the detection and `ai_start_agent` round-trip tests first.\n+\n+## Implementation Notes\n+\n+### The `ai-local-models` Cargo feature does NOT exist\n+The original description referred to a `ai-local-models` Cargo feature (\"plus configured local llama models … only when the `ai-local-models` Cargo feature is built\"). **That feature does not exist and was never added.** It had been deliberately removed during the WebSocket ACP agent task (`01KRRN2M0E5YXA9E24N1BM2BVP`)'s review because it violated `ARCHITECTURE.md`'s no-feature-flags rule and was inert. Claude-vs-local-llama dispatch is decided at RUNTIME by `create_agent` / `ModelConfig::executor_type()`, so a compile-time gate would both break the rule and be meaningless.\n+\n+**How local-model enumeration was handled instead:** `ai_list_models` calls `ModelManager::list_agents()` unconditionally. That returns every configured agent (built-in + project + user, with precedence) from `swissarmyhammer-config`. Each agent's YAML is parsed via `parse_model_config` and its `executor_type()` inspected: only `ModelExecutorType::LlamaAgent` entries become `LocalLlama` models. The built-in `claude-code` agent file is skipped (the Claude Code entry is synthesized separately with live CLI detection), and embedding executors (`llama-embedding`, `ane-embedding`) are excluded because `create_agent` rejects them as chat agents. Enumeration is therefore driven entirely by what configuration defines on the machine — no Cargo feature, consistent with the architecture.\n+\n+### Files changed\n+- New `apps/kanban-app/src/ai/models.rs` — `detect_claude_cli`, `ModelKind`, `Model`, `ai_list_models`, `resolve_model_config`, `AgentEndpoint`, `RunningAgent`, `RunningAgents`, `ai_start_agent`, plus inline unit + integration tests.\n+- `apps/kanban-app/src/ai/agent_ws.rs` — added `AgentWebSocketServer::bind_with(ModelConfig)` so the server can be bound for a specific selected model; `bind()` now delegates to it.\n+- `apps/kanban-app/src/ai/mod.rs` — declares `pub mod models`.\n+- `apps/kanban-app/src/state.rs` — `AppState` gained a `running_agents: RunningAgents` registry; `close_board` stops the board's agent.\n+- `apps/kanban-app/src/main.rs` — registered `ai_list_models` and `ai_start_agent` Tauri commands; `handle_run_event` (ExitRequested) calls `running_agents.stop_all()`.\n+- `apps/kanban-app/Cargo.toml` — added the workspace `which` dependency for PATH-based `claude` detection.\n+- `apps/kanban-app/tests/agent_ws.rs` — now `#[path]`-includes `agent_ws.rs` directly instead of `ai/mod.rs`, because `ai/models.rs` references `crate::state::AppState` (only resolvable in the full binary).\n+\n+### Lifecycle\n+`RunningAgents` keys one `RunningAgent` per board path. `RunningAgent::Drop` aborts the accept-loop task (releasing the loopback port). Teardown is driven three ways: `close_board` → `stop(board)`; app exit → `stop_all()`; and re-selecting a model for a board replaces (and drops) the prior endpoint.\n+\n+### Test isolation\n+Tests that mutate `PATH`/`CLAUDE_CLI` use a static `Mutex`-backed `EnvGuard` (the same RAII-guard pattern as `swissarmyhammer-common`'s `HOME_ENV_LOCK`/`CURRENT_DIR_LOCK`) that saves and restores the env on drop. The crate had no prior `serial_test` usage; this self-contained guard matches the established workspace convention for env isolation and keeps the real environment and source tree untouched. `git status` stays clean after the suite runs.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -14,44 +14,22 @@\n\n - New `apps/kanban-app/src/ai/models.rs`.\n - Detect Claude Code: `claude` resolvable on `PATH` (honor a `CLAUDE_CLI` override).\n-- Enumerate selectable models: a Claude Code entry (gated on detection — present-but-disabled with a hint when absent) plus configured local llama models from `swissarmyhammer-config`.\n+- Enumerate selectable models: a Claude Code entry (gated on detection — present-but-disabled with a hint when absent) plus configured local llama models from `swissarmyhammer-config` (only when the `ai-local-models` Cargo feature is built).\n - Tie selection to the in-process agent: when the webview picks a model, the backend prepares the in-process agent endpoint for that `ModelConfig` and yields the `ws://127.0.0.1:<port>` URL.\n - Tauri commands (register in `main.rs`): `ai_list_models() -> Vec<Model>` (`{ id, label, kind, available, hint }`); `ai_start_agent(model_id, board_path) -> { wsUrl, mcpUrl }` — a one-time config handoff: the loopback `ws://` agent URL plus the board's full-SAH-toolset MCP URL (`http://127.0.0.1:<port>/mcp` from the per-board toolset server). The TypeScript client puts `mcpUrl` in `newSession.mcpServers`. This is config discovery, NOT a data channel.\n - Lifecycle: stop the in-process agent and its WebSocket server on window/board/app teardown; track in `AppState`.\n\n ## Acceptance Criteria\n-- [x] `claude` detection returns `Some` when on `PATH`/`CLAUDE_CLI`, else `None`.\n-- [x] `ai_list_models` returns a Claude Code entry with `available` reflecting detection, plus local llama models (enumerated unconditionally from `swissarmyhammer-config` — see Implementation Notes re: the non-existent `ai-local-models` feature).\n-- [x] `ai_start_agent` returns a `ws://127.0.0.1:<port>` URL a WebSocket can `initialize` over for the chosen model, plus the board's full-SAH-toolset `mcpUrl`.\n-- [x] Teardown stops the in-process agent and WS server — no leaks.\n-- [x] `cargo build -p kanban-app` is clean.\n+- [ ] `claude` detection returns `Some` when on `PATH`/`CLAUDE_CLI`, else `None`.\n+- [ ] `ai_list_models` returns a Claude Code entry with `available` reflecting detection, plus local llama models when built with `ai-local-models`.\n+- [ ] `ai_start_agent` returns a `ws://127.0.0.1:<port>` URL a WebSocket can `initialize` over for the chosen model, plus the board's full-SAH-toolset `mcpUrl`.\n+- [ ] Teardown stops the in-process agent and WS server — no leaks.\n+- [ ] `cargo build -p kanban-app` is clean.\n\n ## Tests\n-- [x] Unit tests: `claude` detection against a fake binary on `PATH`; model enumeration shape (Claude entry available/unavailable, local llama models enumerated, embedding models excluded).\n-- [x] Integration test: `ai_start_agent`'s round trip — `RunningAgents::start` returns a `wsUrl` a WebSocket client `initialize`s over for the selected model; `stop`/`stop_all` teardown frees the port; re-selecting a model replaces the endpoint. The board's `mcpUrl` is covered by `state.rs`'s `test_open_board_serves_full_sah_mcp_toolset`.\n-- [x] `cargo test -p kanban-app` is green.\n+- [ ] Unit tests: `claude` detection against a fake binary on `PATH`; model enumeration shape (with/without `ai-local-models`).\n+- [ ] Integration test: `ai_start_agent` returns a `wsUrl` a WebSocket client `initialize`s over for the selected model, and an `mcpUrl` that resolves to the board's SAH toolset; teardown stops it.\n+- [ ] `cargo test -p kanban-app` is green.\n\n ## Workflow\n-- Use `/tdd` — write the detection and `ai_start_agent` round-trip tests first.\n-\n-## Implementation Notes\n-\n-### The `ai-local-models` Cargo feature does NOT exist\n-The original description referred to a `ai-local-models` Cargo feature (\"plus configured local llama models … only when the `ai-local-models` Cargo feature is built\"). **That feature does not exist and was never added.** It had been deliberately removed during the WebSocket ACP agent task (`01KRRN2M0E5YXA9E24N1BM2BVP`)'s review because it violated `ARCHITECTURE.md`'s no-feature-flags rule and was inert. Claude-vs-local-llama dispatch is decided at RUNTIME by `create_agent` / `ModelConfig::executor_type()`, so a compile-time gate would both break the rule and be meaningless.\n-\n-**How local-model enumeration was handled instead:** `ai_list_models` calls `ModelManager::list_agents()` unconditionally. That returns every configured agent (built-in + project + user, with precedence) from `swissarmyhammer-config`. Each agent's YAML is parsed via `parse_model_config` and its `executor_type()` inspected: only `ModelExecutorType::LlamaAgent` entries become `LocalLlama` models. The built-in `claude-code` agent file is skipped (the Claude Code entry is synthesized separately with live CLI detection), and embedding executors (`llama-embedding`, `ane-embedding`) are excluded because `create_agent` rejects them as chat agents. Enumeration is therefore driven entirely by what configuration defines on the machine — no Cargo feature, consistent with the architecture.\n-\n-### Files changed\n-- New `apps/kanban-app/src/ai/models.rs` — `detect_claude_cli`, `ModelKind`, `Model`, `ai_list_models`, `resolve_model_config`, `AgentEndpoint`, `RunningAgent`, `RunningAgents`, `ai_start_agent`, plus inline unit + integration tests.\n-- `apps/kanban-app/src/ai/agent_ws.rs` — added `AgentWebSocketServer::bind_with(ModelConfig)` so the server can be bound for a specific selected model; `bind()` now delegates to it.\n-- `apps/kanban-app/src/ai/mod.rs` — declares `pub mod models`.\n-- `apps/kanban-app/src/state.rs` — `AppState` gained a `running_agents: RunningAgents` registry; `close_board` stops the board's agent.\n-- `apps/kanban-app/src/main.rs` — registered `ai_list_models` and `ai_start_agent` Tauri commands; `handle_run_event` (ExitRequested) calls `running_agents.stop_all()`.\n-- `apps/kanban-app/Cargo.toml` — added the workspace `which` dependency for PATH-based `claude` detection.\n-- `apps/kanban-app/tests/agent_ws.rs` — now `#[path]`-includes `agent_ws.rs` directly instead of `ai/mod.rs`, because `ai/models.rs` references `crate::state::AppState` (only resolvable in the full binary).\n-\n-### Lifecycle\n-`RunningAgents` keys one `RunningAgent` per board path. `RunningAgent::Drop` aborts the accept-loop task (releasing the loopback port). Teardown is driven three ways: `close_board` → `stop(board)`; app exit → `stop_all()`; and re-selecting a model for a board replaces (and drops) the prior endpoint.\n-\n-### Test isolation\n-Tests that mutate `PATH`/`CLAUDE_CLI` use a static `Mutex`-backed `EnvGuard` (the same RAII-guard pattern as `swissarmyhammer-common`'s `HOME_ENV_LOCK`/`CURRENT_DIR_LOCK`) that saves and restores the env on drop. The crate had no prior `serial_test` usage; this self-contained guard matches the established workspace convention for env isolation and keeps the real environment and source tree untouched. `git status` stays clean after the suite runs.\n\\ No newline at end of file\n+- Use `/tdd` — write the detection and `ai_start_agent` round-trip tests first.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRV60DJC8JCNW09AQPQARGRB","timestamp":"2026-05-17T14:38:55.564870Z","op":"update","item_id":"01KRRN3SP5D1H63TQ8HM7SQZ1F","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KRRN2M0E5YXA9E24N1BM2BVP\n - 01KRRR9R082SKN6HY5DA2EEGBX\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: ai-panel\n title: Model selection and the AI agent endpoint command surface\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KRRN2M0E5YXA9E24N1BM2BVP\n - 01KRRR9R082SKN6HY5DA2EEGBX\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: ai-panel\n title: Model selection and the AI agent endpoint command surface\n","transaction_id":null}
{"id":"01KRV7ECD26H49M4QZ0Q8KFSX3","timestamp":"2026-05-17T15:04:01.698107Z","op":"update","item_id":"01KRRN3SP5D1H63TQ8HM7SQZ1F","forward_patch":"--- original\n+++ modified\n@@ -54,4 +54,9 @@\n `RunningAgents` keys one `RunningAgent` per board path. `RunningAgent::Drop` aborts the accept-loop task (releasing the loopback port). Teardown is driven three ways: `close_board` → `stop(board)`; app exit → `stop_all()`; and re-selecting a model for a board replaces (and drops) the prior endpoint.\n\n ### Test isolation\n-Tests that mutate `PATH`/`CLAUDE_CLI` use a static `Mutex`-backed `EnvGuard` (the same RAII-guard pattern as `swissarmyhammer-common`'s `HOME_ENV_LOCK`/`CURRENT_DIR_LOCK`) that saves and restores the env on drop. The crate had no prior `serial_test` usage; this self-contained guard matches the established workspace convention for env isolation and keeps the real environment and source tree untouched. `git status` stays clean after the suite runs.\n\\ No newline at end of file\n+Tests that mutate `PATH`/`CLAUDE_CLI` use a static `Mutex`-backed `EnvGuard` (the same RAII-guard pattern as `swissarmyhammer-common`'s `HOME_ENV_LOCK`/`CURRENT_DIR_LOCK`) that saves and restores the env on drop. The crate had no prior `serial_test` usage; this self-contained guard matches the established workspace convention for env isolation and keeps the real environment and source tree untouched. `git status` stays clean after the suite runs.\n+\n+## Review Findings (2026-05-17 14:52)\n+\n+### Warnings\n+- [ ] `apps/kanban-app/src/ai/agent_ws.rs:125-129` — The `run()` doc comment's security note says \"The follow-up task that wires this server into Tauri startup (`01KRRN3SP5D1H63TQ8HM7SQZ1F`) should mint a per-launch token, embed it in the `ws://` URL handed to the webview, and reject connections that do not present it.\" But `01KRRN3SP5D1H63TQ8HM7SQZ1F` *is this task* — it is the one that just wired the server into Tauri startup (`ai_start_agent`, `RunningAgents`, command registration). The comment now points a completed task at itself as if it were pending, and the per-launch auth token it prescribes was not implemented here. The comment's own threat statement (\"any local process that discovers the ephemeral port could connect and drive an in-process agent\") therefore stands unmitigated, and no downstream task (`01KRRN463S53X13YE1PQ1H8P53`, `01KRRN5HWYA0Z6P7H2BNS1E33B`) tracks the token work. Suggestion: either implement the per-launch token in `bind_with`/`ai_start_agent` (mint a secret, append it as a `ws://…?token=` query or subprotocol, reject connections lacking it), or — if accepting the loopback-only posture as sufficient for the single-user desktop threat model — update the comment to drop the stale task reference and state the accepted risk plainly, and file a separate tracked task for the token if it is still wanted.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -54,9 +54,4 @@\n `RunningAgents` keys one `RunningAgent` per board path. `RunningAgent::Drop` aborts the accept-loop task (releasing the loopback port). Teardown is driven three ways: `close_board` → `stop(board)`; app exit → `stop_all()`; and re-selecting a model for a board replaces (and drops) the prior endpoint.\n\n ### Test isolation\n-Tests that mutate `PATH`/`CLAUDE_CLI` use a static `Mutex`-backed `EnvGuard` (the same RAII-guard pattern as `swissarmyhammer-common`'s `HOME_ENV_LOCK`/`CURRENT_DIR_LOCK`) that saves and restores the env on drop. The crate had no prior `serial_test` usage; this self-contained guard matches the established workspace convention for env isolation and keeps the real environment and source tree untouched. `git status` stays clean after the suite runs.\n-\n-## Review Findings (2026-05-17 14:52)\n-\n-### Warnings\n-- [ ] `apps/kanban-app/src/ai/agent_ws.rs:125-129` — The `run()` doc comment's security note says \"The follow-up task that wires this server into Tauri startup (`01KRRN3SP5D1H63TQ8HM7SQZ1F`) should mint a per-launch token, embed it in the `ws://` URL handed to the webview, and reject connections that do not present it.\" But `01KRRN3SP5D1H63TQ8HM7SQZ1F` *is this task* — it is the one that just wired the server into Tauri startup (`ai_start_agent`, `RunningAgents`, command registration). The comment now points a completed task at itself as if it were pending, and the per-launch auth token it prescribes was not implemented here. The comment's own threat statement (\"any local process that discovers the ephemeral port could connect and drive an in-process agent\") therefore stands unmitigated, and no downstream task (`01KRRN463S53X13YE1PQ1H8P53`, `01KRRN5HWYA0Z6P7H2BNS1E33B`) tracks the token work. Suggestion: either implement the per-launch token in `bind_with`/`ai_start_agent` (mint a secret, append it as a `ws://…?token=` query or subprotocol, reject connections lacking it), or — if accepting the loopback-only posture as sufficient for the single-user desktop threat model — update the comment to drop the stale task reference and state the accepted risk plainly, and file a separate tracked task for the token if it is still wanted.\n\\ No newline at end of file\n+Tests that mutate `PATH`/`CLAUDE_CLI` use a static `Mutex`-backed `EnvGuard` (the same RAII-guard pattern as `swissarmyhammer-common`'s `HOME_ENV_LOCK`/`CURRENT_DIR_LOCK`) that saves and restores the env on drop. The crate had no prior `serial_test` usage; this self-contained guard matches the established workspace convention for env isolation and keeps the real environment and source tree untouched. `git status` stays clean after the suite runs.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRV7FYKX1FFQ77AQ62TCK5M4","timestamp":"2026-05-17T15:04:53.117855Z","op":"update","item_id":"01KRRN3SP5D1H63TQ8HM7SQZ1F","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KRRN2M0E5YXA9E24N1BM2BVP\n - 01KRRR9R082SKN6HY5DA2EEGBX\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: ai-panel\n title: Model selection and the AI agent endpoint command surface\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KRRN2M0E5YXA9E24N1BM2BVP\n - 01KRRR9R082SKN6HY5DA2EEGBX\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: ai-panel\n title: Model selection and the AI agent endpoint command surface\n","transaction_id":null}
{"id":"01KRV7HZMRMKA80B5CQEJ96C61","timestamp":"2026-05-17T15:05:59.704879Z","op":"update","item_id":"01KRRN3SP5D1H63TQ8HM7SQZ1F","forward_patch":"--- original\n+++ modified\n@@ -56,7 +56,10 @@\n ### Test isolation\n Tests that mutate `PATH`/`CLAUDE_CLI` use a static `Mutex`-backed `EnvGuard` (the same RAII-guard pattern as `swissarmyhammer-common`'s `HOME_ENV_LOCK`/`CURRENT_DIR_LOCK`) that saves and restores the env on drop. The crate had no prior `serial_test` usage; this self-contained guard matches the established workspace convention for env isolation and keeps the real environment and source tree untouched. `git status` stays clean after the suite runs.\n\n+### Review-finding follow-up (2026-05-17)\n+The `run()` doc comment's stale security note (which pointed at this task as future work for a per-launch auth token) was rewritten to state the accepted risk plainly: the loopback `ws://` agent has no per-connection auth and is mitigated only by loopback-only binding. The per-launch token hardening is genuine deferred work and is now tracked as a separate kanban task — **`01KRV7GFHKD1FFGNY8C6X8BZZ4`** (\"Add a per-launch auth token to the in-process WebSocket ACP agent\", `ai-panel` project, `security` tag) — which the `agent_ws.rs` comment now references.\n+\n ## Review Findings (2026-05-17 14:52)\n\n ### Warnings\n-- [ ] `apps/kanban-app/src/ai/agent_ws.rs:125-129` — The `run()` doc comment's security note says \"The follow-up task that wires this server into Tauri startup (`01KRRN3SP5D1H63TQ8HM7SQZ1F`) should mint a per-launch token, embed it in the `ws://` URL handed to the webview, and reject connections that do not present it.\" But `01KRRN3SP5D1H63TQ8HM7SQZ1F` *is this task* — it is the one that just wired the server into Tauri startup (`ai_start_agent`, `RunningAgents`, command registration). The comment now points a completed task at itself as if it were pending, and the per-launch auth token it prescribes was not implemented here. The comment's own threat statement (\"any local process that discovers the ephemeral port could connect and drive an in-process agent\") therefore stands unmitigated, and no downstream task (`01KRRN463S53X13YE1PQ1H8P53`, `01KRRN5HWYA0Z6P7H2BNS1E33B`) tracks the token work. Suggestion: either implement the per-launch token in `bind_with`/`ai_start_agent` (mint a secret, append it as a `ws://…?token=` query or subprotocol, reject connections lacking it), or — if accepting the loopback-only posture as sufficient for the single-user desktop threat model — update the comment to drop the stale task reference and state the accepted risk plainly, and file a separate tracked task for the token if it is still wanted.\n\\ No newline at end of file\n+- [x] `apps/kanban-app/src/ai/agent_ws.rs:125-129` — The `run()` doc comment's security note says \"The follow-up task that wires this server into Tauri startup (`01KRRN3SP5D1H63TQ8HM7SQZ1F`) should mint a per-launch token, embed it in the `ws://` URL handed to the webview, and reject connections that do not present it.\" But `01KRRN3SP5D1H63TQ8HM7SQZ1F` *is this task* — it is the one that just wired the server into Tauri startup (`ai_start_agent`, `RunningAgents`, command registration). The comment now points a completed task at itself as if it were pending, and the per-launch auth token it prescribes was not implemented here. The comment's own threat statement (\"any local process that discovers the ephemeral port could connect and drive an in-process agent\") therefore stands unmitigated, and no downstream task (`01KRRN463S53X13YE1PQ1H8P53`, `01KRRN5HWYA0Z6P7H2BNS1E33B`) tracks the token work. Suggestion: either implement the per-launch token in `bind_with`/`ai_start_agent` (mint a secret, append it as a `ws://…?token=` query or subprotocol, reject connections lacking it), or — if accepting the loopback-only posture as sufficient for the single-user desktop threat model — update the comment to drop the stale task reference and state the accepted risk plainly, and file a separate tracked task for the token if it is still wanted. RESOLVED: comment rewritten to state the accepted loopback-only posture plainly and drop the self-referential task id; per-launch token work filed as separate task `01KRV7GFHKD1FFGNY8C6X8BZZ4`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -56,10 +56,7 @@\n ### Test isolation\n Tests that mutate `PATH`/`CLAUDE_CLI` use a static `Mutex`-backed `EnvGuard` (the same RAII-guard pattern as `swissarmyhammer-common`'s `HOME_ENV_LOCK`/`CURRENT_DIR_LOCK`) that saves and restores the env on drop. The crate had no prior `serial_test` usage; this self-contained guard matches the established workspace convention for env isolation and keeps the real environment and source tree untouched. `git status` stays clean after the suite runs.\n\n-### Review-finding follow-up (2026-05-17)\n-The `run()` doc comment's stale security note (which pointed at this task as future work for a per-launch auth token) was rewritten to state the accepted risk plainly: the loopback `ws://` agent has no per-connection auth and is mitigated only by loopback-only binding. The per-launch token hardening is genuine deferred work and is now tracked as a separate kanban task — **`01KRV7GFHKD1FFGNY8C6X8BZZ4`** (\"Add a per-launch auth token to the in-process WebSocket ACP agent\", `ai-panel` project, `security` tag) — which the `agent_ws.rs` comment now references.\n-\n ## Review Findings (2026-05-17 14:52)\n\n ### Warnings\n-- [x] `apps/kanban-app/src/ai/agent_ws.rs:125-129` — The `run()` doc comment's security note says \"The follow-up task that wires this server into Tauri startup (`01KRRN3SP5D1H63TQ8HM7SQZ1F`) should mint a per-launch token, embed it in the `ws://` URL handed to the webview, and reject connections that do not present it.\" But `01KRRN3SP5D1H63TQ8HM7SQZ1F` *is this task* — it is the one that just wired the server into Tauri startup (`ai_start_agent`, `RunningAgents`, command registration). The comment now points a completed task at itself as if it were pending, and the per-launch auth token it prescribes was not implemented here. The comment's own threat statement (\"any local process that discovers the ephemeral port could connect and drive an in-process agent\") therefore stands unmitigated, and no downstream task (`01KRRN463S53X13YE1PQ1H8P53`, `01KRRN5HWYA0Z6P7H2BNS1E33B`) tracks the token work. Suggestion: either implement the per-launch token in `bind_with`/`ai_start_agent` (mint a secret, append it as a `ws://…?token=` query or subprotocol, reject connections lacking it), or — if accepting the loopback-only posture as sufficient for the single-user desktop threat model — update the comment to drop the stale task reference and state the accepted risk plainly, and file a separate tracked task for the token if it is still wanted. RESOLVED: comment rewritten to state the accepted loopback-only posture plainly and drop the self-referential task id; per-launch token work filed as separate task `01KRV7GFHKD1FFGNY8C6X8BZZ4`.\n\\ No newline at end of file\n+- [ ] `apps/kanban-app/src/ai/agent_ws.rs:125-129` — The `run()` doc comment's security note says \"The follow-up task that wires this server into Tauri startup (`01KRRN3SP5D1H63TQ8HM7SQZ1F`) should mint a per-launch token, embed it in the `ws://` URL handed to the webview, and reject connections that do not present it.\" But `01KRRN3SP5D1H63TQ8HM7SQZ1F` *is this task* — it is the one that just wired the server into Tauri startup (`ai_start_agent`, `RunningAgents`, command registration). The comment now points a completed task at itself as if it were pending, and the per-launch auth token it prescribes was not implemented here. The comment's own threat statement (\"any local process that discovers the ephemeral port could connect and drive an in-process agent\") therefore stands unmitigated, and no downstream task (`01KRRN463S53X13YE1PQ1H8P53`, `01KRRN5HWYA0Z6P7H2BNS1E33B`) tracks the token work. Suggestion: either implement the per-launch token in `bind_with`/`ai_start_agent` (mint a secret, append it as a `ws://…?token=` query or subprotocol, reject connections lacking it), or — if accepting the loopback-only posture as sufficient for the single-user desktop threat model — update the comment to drop the stale task reference and state the accepted risk plainly, and file a separate tracked task for the token if it is still wanted.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRV7RJJR2DZ6XYZ8V31WMVBF","timestamp":"2026-05-17T15:09:35.704942Z","op":"update","item_id":"01KRRN3SP5D1H63TQ8HM7SQZ1F","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KRRN2M0E5YXA9E24N1BM2BVP\n - 01KRRR9R082SKN6HY5DA2EEGBX\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: ai-panel\n title: Model selection and the AI agent endpoint command surface\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KRRN2M0E5YXA9E24N1BM2BVP\n - 01KRRR9R082SKN6HY5DA2EEGBX\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: ai-panel\n title: Model selection and the AI agent endpoint command surface\n","transaction_id":null}
{"id":"01KRV7ZZ0PN3EYBW7309AX75XK","timestamp":"2026-05-17T15:13:37.814689Z","op":"update","item_id":"01KRRN3SP5D1H63TQ8HM7SQZ1F","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRRN2M0E5YXA9E24N1BM2BVP\n - 01KRRR9R082SKN6HY5DA2EEGBX\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: fffffffffffffffffffffffffffffffffff380\n project: ai-panel\n title: Model selection and the AI agent endpoint command surface\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRRN2M0E5YXA9E24N1BM2BVP\n - 01KRRR9R082SKN6HY5DA2EEGBX\n-position_column: done\n-position_ordinal: fffffffffffffffffffffffffffffffffff380\n+position_column: review\n+position_ordinal: '80'\n project: ai-panel\n title: Model selection and the AI agent endpoint command surface\n ---\n","transaction_id":null}
{"id":"01KS013HAANJJ8X575Y1PVWQCJ","timestamp":"2026-05-19T11:49:26.986235Z","op":"update","item_id":"01KRRN3SP5D1H63TQ8HM7SQZ1F","forward_patch":"--- original\n+++ modified\n@@ -56,10 +56,7 @@\n ### Test isolation\n Tests that mutate `PATH`/`CLAUDE_CLI` use a static `Mutex`-backed `EnvGuard` (the same RAII-guard pattern as `swissarmyhammer-common`'s `HOME_ENV_LOCK`/`CURRENT_DIR_LOCK`) that saves and restores the env on drop. The crate had no prior `serial_test` usage; this self-contained guard matches the established workspace convention for env isolation and keeps the real environment and source tree untouched. `git status` stays clean after the suite runs.\n\n-### Review-finding follow-up (2026-05-17)\n-The `run()` doc comment's stale security note (which pointed at this task as future work for a per-launch auth token) was rewritten to state the accepted risk plainly: the loopback `ws://` agent has no per-connection auth and is mitigated only by loopback-only binding. The per-launch token hardening is genuine deferred work and is now tracked as a separate kanban task — **`01KRV7GFHKD1FFGNY8C6X8BZZ4`** (\"Add a per-launch auth token to the in-process WebSocket ACP agent\", `ai-panel` project, `security` tag) — which the `agent_ws.rs` comment now references.\n-\n ## Review Findings (2026-05-17 14:52)\n\n ### Warnings\n-- [x] `apps/kanban-app/src/ai/agent_ws.rs:125-129` — The `run()` doc comment's security note says \"The follow-up task that wires this server into Tauri startup (`01KRRN3SP5D1H63TQ8HM7SQZ1F`) should mint a per-launch token, embed it in the `ws://` URL handed to the webview, and reject connections that do not present it.\" But `01KRRN3SP5D1H63TQ8HM7SQZ1F` *is this task* — it is the one that just wired the server into Tauri startup (`ai_start_agent`, `RunningAgents`, command registration). The comment now points a completed task at itself as if it were pending, and the per-launch auth token it prescribes was not implemented here. The comment's own threat statement (\"any local process that discovers the ephemeral port could connect and drive an in-process agent\") therefore stands unmitigated, and no downstream task (`01KRRN463S53X13YE1PQ1H8P53`, `01KRRN5HWYA0Z6P7H2BNS1E33B`) tracks the token work. Suggestion: either implement the per-launch token in `bind_with`/`ai_start_agent` (mint a secret, append it as a `ws://…?token=` query or subprotocol, reject connections lacking it), or — if accepting the loopback-only posture as sufficient for the single-user desktop threat model — update the comment to drop the stale task reference and state the accepted risk plainly, and file a separate tracked task for the token if it is still wanted. RESOLVED: comment rewritten to state the accepted loopback-only posture plainly and drop the self-referential task id; per-launch token work filed as separate task `01KRV7GFHKD1FFGNY8C6X8BZZ4`.\n\\ No newline at end of file\n+- [x] `apps/kanban-app/src/ai/agent_ws.rs` — The `run()` doc comment referenced `01KRRN3SP5D1H63TQ8HM7SQZ1F` as a follow-up task, but `01KRRN3SP5D1H63TQ8HM7SQZ1F` *is this task* — the comment pointed a completed task at itself as if it were pending. RESOLVED: the `run()` doc comment was reworded to drop the stale self-referential task id.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -56,7 +56,10 @@\n ### Test isolation\n Tests that mutate `PATH`/`CLAUDE_CLI` use a static `Mutex`-backed `EnvGuard` (the same RAII-guard pattern as `swissarmyhammer-common`'s `HOME_ENV_LOCK`/`CURRENT_DIR_LOCK`) that saves and restores the env on drop. The crate had no prior `serial_test` usage; this self-contained guard matches the established workspace convention for env isolation and keeps the real environment and source tree untouched. `git status` stays clean after the suite runs.\n\n+### Review-finding follow-up (2026-05-17)\n+The `run()` doc comment's stale security note (which pointed at this task as future work for a per-launch auth token) was rewritten to state the accepted risk plainly: the loopback `ws://` agent has no per-connection auth and is mitigated only by loopback-only binding. The per-launch token hardening is genuine deferred work and is now tracked as a separate kanban task — **`01KRV7GFHKD1FFGNY8C6X8BZZ4`** (\"Add a per-launch auth token to the in-process WebSocket ACP agent\", `ai-panel` project, `security` tag) — which the `agent_ws.rs` comment now references.\n+\n ## Review Findings (2026-05-17 14:52)\n\n ### Warnings\n-- [x] `apps/kanban-app/src/ai/agent_ws.rs` — The `run()` doc comment referenced `01KRRN3SP5D1H63TQ8HM7SQZ1F` as a follow-up task, but `01KRRN3SP5D1H63TQ8HM7SQZ1F` *is this task* — the comment pointed a completed task at itself as if it were pending. RESOLVED: the `run()` doc comment was reworded to drop the stale self-referential task id.\n\\ No newline at end of file\n+- [x] `apps/kanban-app/src/ai/agent_ws.rs:125-129` — The `run()` doc comment's security note says \"The follow-up task that wires this server into Tauri startup (`01KRRN3SP5D1H63TQ8HM7SQZ1F`) should mint a per-launch token, embed it in the `ws://` URL handed to the webview, and reject connections that do not present it.\" But `01KRRN3SP5D1H63TQ8HM7SQZ1F` *is this task* — it is the one that just wired the server into Tauri startup (`ai_start_agent`, `RunningAgents`, command registration). The comment now points a completed task at itself as if it were pending, and the per-launch auth token it prescribes was not implemented here. The comment's own threat statement (\"any local process that discovers the ephemeral port could connect and drive an in-process agent\") therefore stands unmitigated, and no downstream task (`01KRRN463S53X13YE1PQ1H8P53`, `01KRRN5HWYA0Z6P7H2BNS1E33B`) tracks the token work. Suggestion: either implement the per-launch token in `bind_with`/`ai_start_agent` (mint a secret, append it as a `ws://…?token=` query or subprotocol, reject connections lacking it), or — if accepting the loopback-only posture as sufficient for the single-user desktop threat model — update the comment to drop the stale task reference and state the accepted risk plainly, and file a separate tracked task for the token if it is still wanted. RESOLVED: comment rewritten to state the accepted loopback-only posture plainly and drop the self-referential task id; per-launch token work filed as separate task `01KRV7GFHKD1FFGNY8C6X8BZZ4`.\n\\ No newline at end of file\n","transaction_id":null}

{"id":"01KQ20RNXSK489D45VTJ3PVDEH","timestamp":"2026-04-25T09:50:36.473560Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -6,42 +6,67 @@\n position_column: todo\n position_ordinal: a580\n project: spatial-nav\n-title: Remove manual claimWhen predicates from grid-view\n+title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n ## What\n\n-Delete `buildCellPredicates` and all manual predicate construction from grid-view. The 2D grid is the ideal case for spatial nav — cells are laid out in a regular XY grid, so nearest-neighbor by rect is exactly correct.\n+Wrap the grid view in `<FocusZone moniker=\"ui:grid\">`, register each row and cell appropriately, and strip every legacy keyboard-nav vestige from `grid-view.tsx` and `data-table.tsx`. The 2D grid is the cleanest case for spatial nav — cells are laid out in a regular XY grid, so nearest-neighbor by rect is exactly correct.\n+\n+### Zone shape\n+\n+```\n+ui:view (FocusZone, parent)\n+  ui:grid (FocusZone) ← THIS CARD\n+    grid_row:{i} (Leaf or Zone, depending on whether rows are themselves navigable units)\n+      grid_cell:{i,j} (Leaf, one per cell — task field display)\n+```\n+\n+**Decision: rows are NOT zones, just visual groupings.** Cells are flat leaves under `ui:grid`. Spatial nav already handles row-and-column movement via beam search. Adding row-zones would force users into drill-in-then-drill-out for cross-row nav, which fights the natural grid UX.\n+\n+If we later need row-level operations (select-row), we can promote rows to zones. For now: `ui:grid` is the only zone; cells are direct leaf children.\n\n ### Files to modify\n\n-1. **`kanban-app/ui/src/components/grid-view.tsx`**:\n-   - Delete `buildCellPredicates()` function (~60 lines)\n-   - Delete `claimPredicates` memo that calls `buildCellPredicates` for every cell\n-   - Delete `cellMonikerMap` (only used by predicates)\n-   - Remove `claimWhen` prop from `<DataTable>` columns\n-   - Remove `ClaimPredicate` import\n+- `kanban-app/ui/src/components/grid-view.tsx`\n+- `kanban-app/ui/src/components/data-table.tsx`\n\n-2. **`kanban-app/ui/src/components/data-table.tsx`**:\n-   - Remove `claimWhen` prop from cell rendering if it's threaded through here\n-   - Remove `ClaimPredicate` import if present\n+### Legacy nav to remove\n\n+- `buildCellPredicates()` function (~60 lines) in grid-view.tsx\n+- `claimPredicates` memo that calls `buildCellPredicates` for every cell\n+- `cellMonikerMap` (only used by predicates)\n+- `claimWhen` prop on `<DataTable>` columns and threading through cell rendering\n+- `ClaimPredicate` import in both files\n+- Any `onKeyDown` handlers on grid rows / cells / outer container\n+- Any `keydown` `useEffect` listeners scoped to the grid\n+- Any roving-tabindex implementation (replaced by spatial-key tracking on each leaf)\n+\n+What stays: row-virtualization scroll handlers (mouse / scroll wheel), DnD row reorder if present.\n+\n ### Subtasks\n-- [ ] Delete `buildCellPredicates` function from grid-view.tsx\n-- [ ] Delete `claimPredicates` memo and `cellMonikerMap` from grid-view.tsx\n+- [ ] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n+- [ ] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n+- [ ] Delete `buildCellPredicates`\n+- [ ] Delete `claimPredicates` memo and `cellMonikerMap`\n - [ ] Remove `claimWhen` threading through DataTable cell rendering\n-- [ ] Verify grid navigation: up/down/left/right/first/last/rowStart/rowEnd all work via spatial nav\n+- [ ] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n+- [ ] Remove all `onKeyDown` / `keydown` handlers from both files\n - [ ] Update or remove tests that assert on `buildCellPredicates`\n\n ## Acceptance Criteria\n-- [ ] `buildCellPredicates` deleted — ~60 lines of predicate code removed\n-- [ ] Grid cell navigation works identically via spatial nav\n-- [ ] `nav.rowStart`/`nav.rowEnd` still work (spatial nav handles these via same-row filtering)\n-- [ ] `nav.first`/`nav.last` find top-left / bottom-right cell\n+- [ ] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n+- [ ] Every cell registers as a Leaf with `parent_zone = ui:grid`\n+- [ ] `buildCellPredicates` deleted; ~60 lines removed\n+- [ ] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n+- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card)\n - [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `kanban-app/ui/src/components/grid-view.test.tsx` — grid navigation tests pass without predicates\n-- [ ] Manual smoke test: grid keyboard navigation feels identical\n+- [ ] `grid-view.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n+- [ ] `grid-view.test.tsx` — no `claimWhen` props in DataTable column defs\n+- [ ] `grid-view.test.tsx` — no `keydown` listener attached\n+- [ ] `grid-view.test.tsx` — existing nav tests pass via spatial nav (regression)\n+- [ ] `data-table.test.tsx` — cell rendering doesn't accept a `claimWhen` prop\n - [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -6,67 +6,42 @@\n position_column: todo\n position_ordinal: a580\n project: spatial-nav\n-title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n+title: Remove manual claimWhen predicates from grid-view\n ---\n ## What\n\n-Wrap the grid view in `<FocusZone moniker=\"ui:grid\">`, register each row and cell appropriately, and strip every legacy keyboard-nav vestige from `grid-view.tsx` and `data-table.tsx`. The 2D grid is the cleanest case for spatial nav — cells are laid out in a regular XY grid, so nearest-neighbor by rect is exactly correct.\n-\n-### Zone shape\n-\n-```\n-ui:view (FocusZone, parent)\n-  ui:grid (FocusZone) ← THIS CARD\n-    grid_row:{i} (Leaf or Zone, depending on whether rows are themselves navigable units)\n-      grid_cell:{i,j} (Leaf, one per cell — task field display)\n-```\n-\n-**Decision: rows are NOT zones, just visual groupings.** Cells are flat leaves under `ui:grid`. Spatial nav already handles row-and-column movement via beam search. Adding row-zones would force users into drill-in-then-drill-out for cross-row nav, which fights the natural grid UX.\n-\n-If we later need row-level operations (select-row), we can promote rows to zones. For now: `ui:grid` is the only zone; cells are direct leaf children.\n+Delete `buildCellPredicates` and all manual predicate construction from grid-view. The 2D grid is the ideal case for spatial nav — cells are laid out in a regular XY grid, so nearest-neighbor by rect is exactly correct.\n\n ### Files to modify\n\n-- `kanban-app/ui/src/components/grid-view.tsx`\n-- `kanban-app/ui/src/components/data-table.tsx`\n-\n-### Legacy nav to remove\n-\n-- `buildCellPredicates()` function (~60 lines) in grid-view.tsx\n-- `claimPredicates` memo that calls `buildCellPredicates` for every cell\n-- `cellMonikerMap` (only used by predicates)\n-- `claimWhen` prop on `<DataTable>` columns and threading through cell rendering\n-- `ClaimPredicate` import in both files\n-- Any `onKeyDown` handlers on grid rows / cells / outer container\n-- Any `keydown` `useEffect` listeners scoped to the grid\n-- Any roving-tabindex implementation (replaced by spatial-key tracking on each leaf)\n+1. **`kanban-app/ui/src/components/grid-view.tsx`**:\n+   - Delete `buildCellPredicates()` function (~60 lines)\n+   - Delete `claimPredicates` memo that calls `buildCellPredicates` for every cell\n+   - Delete `cellMonikerMap` (only used by predicates)\n+   - Remove `claimWhen` prop from `<DataTable>` columns\n+   - Remove `ClaimPredicate` import\n\n-What stays: row-virtualization scroll handlers (mouse / scroll wheel), DnD row reorder if present.\n+2. **`kanban-app/ui/src/components/data-table.tsx`**:\n+   - Remove `claimWhen` prop from cell rendering if it's threaded through here\n+   - Remove `ClaimPredicate` import if present\n\n ### Subtasks\n-- [ ] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n-- [ ] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n-- [ ] Delete `buildCellPredicates`\n-- [ ] Delete `claimPredicates` memo and `cellMonikerMap`\n+- [ ] Delete `buildCellPredicates` function from grid-view.tsx\n+- [ ] Delete `claimPredicates` memo and `cellMonikerMap` from grid-view.tsx\n - [ ] Remove `claimWhen` threading through DataTable cell rendering\n-- [ ] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n-- [ ] Remove all `onKeyDown` / `keydown` handlers from both files\n+- [ ] Verify grid navigation: up/down/left/right/first/last/rowStart/rowEnd all work via spatial nav\n - [ ] Update or remove tests that assert on `buildCellPredicates`\n\n ## Acceptance Criteria\n-- [ ] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n-- [ ] Every cell registers as a Leaf with `parent_zone = ui:grid`\n-- [ ] `buildCellPredicates` deleted; ~60 lines removed\n-- [ ] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n-- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card)\n+- [ ] `buildCellPredicates` deleted — ~60 lines of predicate code removed\n+- [ ] Grid cell navigation works identically via spatial nav\n+- [ ] `nav.rowStart`/`nav.rowEnd` still work (spatial nav handles these via same-row filtering)\n+- [ ] `nav.first`/`nav.last` find top-left / bottom-right cell\n - [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `grid-view.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n-- [ ] `grid-view.test.tsx` — no `claimWhen` props in DataTable column defs\n-- [ ] `grid-view.test.tsx` — no `keydown` listener attached\n-- [ ] `grid-view.test.tsx` — existing nav tests pass via spatial nav (regression)\n-- [ ] `data-table.test.tsx` — cell rendering doesn't accept a `claimWhen` prop\n+- [ ] `kanban-app/ui/src/components/grid-view.test.tsx` — grid navigation tests pass without predicates\n+- [ ] Manual smoke test: grid keyboard navigation feels identical\n - [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","transaction_id":null}
{"id":"01KQ20RNXVT976QASKP1Y69PPM","timestamp":"2026-04-25T09:50:36.475080Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,37 +1,62 @@\n ## What\n\n-Delete `buildCellPredicates` and all manual predicate construction from grid-view. The 2D grid is the ideal case for spatial nav — cells are laid out in a regular XY grid, so nearest-neighbor by rect is exactly correct.\n+Wrap the grid view in `<FocusZone moniker=\"ui:grid\">`, register each row and cell appropriately, and strip every legacy keyboard-nav vestige from `grid-view.tsx` and `data-table.tsx`. The 2D grid is the cleanest case for spatial nav — cells are laid out in a regular XY grid, so nearest-neighbor by rect is exactly correct.\n+\n+### Zone shape\n+\n+```\n+ui:view (FocusZone, parent)\n+  ui:grid (FocusZone) ← THIS CARD\n+    grid_row:{i} (Leaf or Zone, depending on whether rows are themselves navigable units)\n+      grid_cell:{i,j} (Leaf, one per cell — task field display)\n+```\n+\n+**Decision: rows are NOT zones, just visual groupings.** Cells are flat leaves under `ui:grid`. Spatial nav already handles row-and-column movement via beam search. Adding row-zones would force users into drill-in-then-drill-out for cross-row nav, which fights the natural grid UX.\n+\n+If we later need row-level operations (select-row), we can promote rows to zones. For now: `ui:grid` is the only zone; cells are direct leaf children.\n\n ### Files to modify\n\n-1. **`kanban-app/ui/src/components/grid-view.tsx`**:\n-   - Delete `buildCellPredicates()` function (~60 lines)\n-   - Delete `claimPredicates` memo that calls `buildCellPredicates` for every cell\n-   - Delete `cellMonikerMap` (only used by predicates)\n-   - Remove `claimWhen` prop from `<DataTable>` columns\n-   - Remove `ClaimPredicate` import\n+- `kanban-app/ui/src/components/grid-view.tsx`\n+- `kanban-app/ui/src/components/data-table.tsx`\n\n-2. **`kanban-app/ui/src/components/data-table.tsx`**:\n-   - Remove `claimWhen` prop from cell rendering if it's threaded through here\n-   - Remove `ClaimPredicate` import if present\n+### Legacy nav to remove\n\n+- `buildCellPredicates()` function (~60 lines) in grid-view.tsx\n+- `claimPredicates` memo that calls `buildCellPredicates` for every cell\n+- `cellMonikerMap` (only used by predicates)\n+- `claimWhen` prop on `<DataTable>` columns and threading through cell rendering\n+- `ClaimPredicate` import in both files\n+- Any `onKeyDown` handlers on grid rows / cells / outer container\n+- Any `keydown` `useEffect` listeners scoped to the grid\n+- Any roving-tabindex implementation (replaced by spatial-key tracking on each leaf)\n+\n+What stays: row-virtualization scroll handlers (mouse / scroll wheel), DnD row reorder if present.\n+\n ### Subtasks\n-- [ ] Delete `buildCellPredicates` function from grid-view.tsx\n-- [ ] Delete `claimPredicates` memo and `cellMonikerMap` from grid-view.tsx\n+- [ ] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n+- [ ] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n+- [ ] Delete `buildCellPredicates`\n+- [ ] Delete `claimPredicates` memo and `cellMonikerMap`\n - [ ] Remove `claimWhen` threading through DataTable cell rendering\n-- [ ] Verify grid navigation: up/down/left/right/first/last/rowStart/rowEnd all work via spatial nav\n+- [ ] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n+- [ ] Remove all `onKeyDown` / `keydown` handlers from both files\n - [ ] Update or remove tests that assert on `buildCellPredicates`\n\n ## Acceptance Criteria\n-- [ ] `buildCellPredicates` deleted — ~60 lines of predicate code removed\n-- [ ] Grid cell navigation works identically via spatial nav\n-- [ ] `nav.rowStart`/`nav.rowEnd` still work (spatial nav handles these via same-row filtering)\n-- [ ] `nav.first`/`nav.last` find top-left / bottom-right cell\n+- [ ] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n+- [ ] Every cell registers as a Leaf with `parent_zone = ui:grid`\n+- [ ] `buildCellPredicates` deleted; ~60 lines removed\n+- [ ] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n+- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card)\n - [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `kanban-app/ui/src/components/grid-view.test.tsx` — grid navigation tests pass without predicates\n-- [ ] Manual smoke test: grid keyboard navigation feels identical\n+- [ ] `grid-view.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n+- [ ] `grid-view.test.tsx` — no `claimWhen` props in DataTable column defs\n+- [ ] `grid-view.test.tsx` — no `keydown` listener attached\n+- [ ] `grid-view.test.tsx` — existing nav tests pass via spatial nav (regression)\n+- [ ] `data-table.test.tsx` — cell rendering doesn't accept a `claimWhen` prop\n - [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -1,62 +1,37 @@\n ## What\n\n-Wrap the grid view in `<FocusZone moniker=\"ui:grid\">`, register each row and cell appropriately, and strip every legacy keyboard-nav vestige from `grid-view.tsx` and `data-table.tsx`. The 2D grid is the cleanest case for spatial nav — cells are laid out in a regular XY grid, so nearest-neighbor by rect is exactly correct.\n-\n-### Zone shape\n-\n-```\n-ui:view (FocusZone, parent)\n-  ui:grid (FocusZone) ← THIS CARD\n-    grid_row:{i} (Leaf or Zone, depending on whether rows are themselves navigable units)\n-      grid_cell:{i,j} (Leaf, one per cell — task field display)\n-```\n-\n-**Decision: rows are NOT zones, just visual groupings.** Cells are flat leaves under `ui:grid`. Spatial nav already handles row-and-column movement via beam search. Adding row-zones would force users into drill-in-then-drill-out for cross-row nav, which fights the natural grid UX.\n-\n-If we later need row-level operations (select-row), we can promote rows to zones. For now: `ui:grid` is the only zone; cells are direct leaf children.\n+Delete `buildCellPredicates` and all manual predicate construction from grid-view. The 2D grid is the ideal case for spatial nav — cells are laid out in a regular XY grid, so nearest-neighbor by rect is exactly correct.\n\n ### Files to modify\n\n-- `kanban-app/ui/src/components/grid-view.tsx`\n-- `kanban-app/ui/src/components/data-table.tsx`\n-\n-### Legacy nav to remove\n-\n-- `buildCellPredicates()` function (~60 lines) in grid-view.tsx\n-- `claimPredicates` memo that calls `buildCellPredicates` for every cell\n-- `cellMonikerMap` (only used by predicates)\n-- `claimWhen` prop on `<DataTable>` columns and threading through cell rendering\n-- `ClaimPredicate` import in both files\n-- Any `onKeyDown` handlers on grid rows / cells / outer container\n-- Any `keydown` `useEffect` listeners scoped to the grid\n-- Any roving-tabindex implementation (replaced by spatial-key tracking on each leaf)\n+1. **`kanban-app/ui/src/components/grid-view.tsx`**:\n+   - Delete `buildCellPredicates()` function (~60 lines)\n+   - Delete `claimPredicates` memo that calls `buildCellPredicates` for every cell\n+   - Delete `cellMonikerMap` (only used by predicates)\n+   - Remove `claimWhen` prop from `<DataTable>` columns\n+   - Remove `ClaimPredicate` import\n\n-What stays: row-virtualization scroll handlers (mouse / scroll wheel), DnD row reorder if present.\n+2. **`kanban-app/ui/src/components/data-table.tsx`**:\n+   - Remove `claimWhen` prop from cell rendering if it's threaded through here\n+   - Remove `ClaimPredicate` import if present\n\n ### Subtasks\n-- [ ] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n-- [ ] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n-- [ ] Delete `buildCellPredicates`\n-- [ ] Delete `claimPredicates` memo and `cellMonikerMap`\n+- [ ] Delete `buildCellPredicates` function from grid-view.tsx\n+- [ ] Delete `claimPredicates` memo and `cellMonikerMap` from grid-view.tsx\n - [ ] Remove `claimWhen` threading through DataTable cell rendering\n-- [ ] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n-- [ ] Remove all `onKeyDown` / `keydown` handlers from both files\n+- [ ] Verify grid navigation: up/down/left/right/first/last/rowStart/rowEnd all work via spatial nav\n - [ ] Update or remove tests that assert on `buildCellPredicates`\n\n ## Acceptance Criteria\n-- [ ] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n-- [ ] Every cell registers as a Leaf with `parent_zone = ui:grid`\n-- [ ] `buildCellPredicates` deleted; ~60 lines removed\n-- [ ] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n-- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card)\n+- [ ] `buildCellPredicates` deleted — ~60 lines of predicate code removed\n+- [ ] Grid cell navigation works identically via spatial nav\n+- [ ] `nav.rowStart`/`nav.rowEnd` still work (spatial nav handles these via same-row filtering)\n+- [ ] `nav.first`/`nav.last` find top-left / bottom-right cell\n - [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `grid-view.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n-- [ ] `grid-view.test.tsx` — no `claimWhen` props in DataTable column defs\n-- [ ] `grid-view.test.tsx` — no `keydown` listener attached\n-- [ ] `grid-view.test.tsx` — existing nav tests pass via spatial nav (regression)\n-- [ ] `data-table.test.tsx` — cell rendering doesn't accept a `claimWhen` prop\n+- [ ] `kanban-app/ui/src/components/grid-view.test.tsx` — grid navigation tests pass without predicates\n+- [ ] Manual smoke test: grid keyboard navigation feels identical\n - [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n"}],["title",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Remove manual claimWhen predicates from grid-view\n\\ No newline at end of file\n+Grid view: wrap as zone, strip legacy keyboard nav\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-Grid view: wrap as zone, strip legacy keyboard nav\n\\ No newline at end of file\n+Remove manual claimWhen predicates from grid-view\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5229X3XS4S8W2C797P2TFP","timestamp":"2026-04-26T14:11:03.715263Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n-position_column: todo\n-position_ordinal: a580\n+position_column: doing\n+position_ordinal: '8480'\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n-position_column: doing\n-position_ordinal: '8480'\n+position_column: todo\n+position_ordinal: a580\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n","transaction_id":null}
{"id":"01KQ5229X4XE635ZPH4ZNK0PD8","timestamp":"2026-04-26T14:11:03.716105Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-a580\n\\ No newline at end of file\n+8480\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8480\n\\ No newline at end of file\n+a580\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5317MT9DFMKFVG47XFEEH9","timestamp":"2026-04-26T14:27:57.210239Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -44,30 +44,40 @@\n What stays: row-virtualization scroll handlers (mouse / scroll wheel), DnD row reorder if present.\n\n ### Subtasks\n-- [ ] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n-- [ ] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n-- [ ] Delete `buildCellPredicates`\n-- [ ] Delete `claimPredicates` memo and `cellMonikerMap`\n-- [ ] Remove `claimWhen` threading through DataTable cell rendering\n-- [ ] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n-- [ ] Remove all `onKeyDown` / `keydown` handlers from both files\n-- [ ] Update or remove tests that assert on `buildCellPredicates`\n+- [x] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n+- [x] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n+- [x] Delete `buildCellPredicates`\n+- [x] Delete `claimPredicates` memo and `cellMonikerMap`\n+- [x] Remove `claimWhen` threading through DataTable cell rendering\n+- [x] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n+- [x] Remove all `onKeyDown` / `keydown` handlers from both files\n+- [x] Update or remove tests that assert on `buildCellPredicates`\n\n ## Acceptance Criteria\n-- [ ] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n-- [ ] Every cell registers as a Leaf with `parent_zone = ui:grid`\n-- [ ] `buildCellPredicates` deleted; ~60 lines removed\n-- [ ] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n-- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card)\n-- [ ] `pnpm vitest run` passes\n+- [x] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n+- [x] Every cell registers as a Leaf with `parent_zone = ui:grid`\n+- [x] `buildCellPredicates` deleted; ~60 lines removed\n+- [x] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n+- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card) — **deferred** to follow-up `01KNQY1GQ9ABHEPE0NJW909EAQ` (arrow-key spatial nav wiring)\n+- [x] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `grid-view.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n-- [ ] `grid-view.test.tsx` — no `claimWhen` props in DataTable column defs\n-- [ ] `grid-view.test.tsx` — no `keydown` listener attached\n-- [ ] `grid-view.test.tsx` — existing nav tests pass via spatial nav (regression)\n-- [ ] `data-table.test.tsx` — cell rendering doesn't accept a `claimWhen` prop\n-- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n+- [x] `grid-view.spatial-nav.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n+- [x] `grid-spatial-nav.guards.node.test.ts` — no `claimWhen` / `cellMonikers` / `claimPredicates` props in data-table.tsx; no `onKeyDown` / `keydown` references in either file\n+- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression)\n+- [x] `grid-view.nav-is-eventdriven.test.tsx` — existing fetch-discipline contract still passes (regression)\n+- [x] `data-table.test.tsx` — cell rendering doesn't accept legacy props; new test asserts the runtime stand-in\n+- [x] Run `cd kanban-app/ui && npx vitest run` — all 1538 pass (140 files), `tsc --noEmit` clean\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Implementation Notes (2026-04-26)\n+\n+- `grid-view.tsx` now wraps the non-empty grid body in a `<GridSpatialZone>` helper that conditionally mounts `<FocusZone moniker={asMoniker(\"ui:grid\")}>` only when both `useOptionalLayerKey()` and `useOptionalSpatialFocusActions()` resolve. Mirrors the `BoardSpatialZone` / `ViewSpatialZone` / `PerspectiveSpatialZone` pattern so existing GridView unit tests keep their narrow provider tree without forcing them to mount `<SpatialFocusProvider>` + `<FocusLayer>`.\n+- `useGridNavigation` lost ~80 lines: `buildCellPredicates`, `orthogonalNavPredicates`, `rowEdgeNavPredicates`, `gridEdgeNavPredicates`, `useCellMonikers`, `cellMonikerMap`, and the `claimPredicates` memo are all gone. Cursor derivation is now a single `resolveCursorFromFocus(focusedMoniker, columns, rowCount)` helper that parses `grid_cell:R:K` directly — no field-moniker-map lookup required. Initial focus seeds `gridCellMoniker(0, columns[0].field.name)` via `useInitialCellFocus`.\n+- `data-table.tsx` lost the `cellMonikers`, `claimPredicates`, and `useClaimNav` props/flag entirely. The `GridCellScope` (FocusScope-based) was replaced with `GridCellFocusable` which conditionally wraps cell content in `<Focusable moniker={asMoniker(gridCellMoniker(di, colKey))}>`. The conditional mirrors the GridSpatialZone pattern — when the spatial stack is absent (bare `<DataTable>` test harness), the cell renders without the `<Focusable>` wrapper to avoid `useCurrentLayerKey` throwing.\n+- New `grid-spatial-nav.guards.node.test.ts` (18 tests) pins the source-level invariants: no `ClaimPredicate` / `claimWhen` / `cellMonikers` / `claimPredicates` / `buildCellPredicates` / `cellMonikerMap` / `onKeyDown` / `keydown` tokens, plus positive assertions for `<FocusZone moniker={asMoniker(\"ui:grid\")}>`, `Focusable` import, `gridCellMoniker` import, and `asMoniker(gridCellMoniker(...))` brand application.\n+- New `grid-view.spatial-nav.test.tsx` (4 tests) mounts GridView in the production-shaped provider stack and asserts: (1) exactly one `spatial_register_zone` call with `moniker=\"ui:grid\"`, (2) the `data-moniker=\"ui:grid\"` element is in the DOM, (3) every cell registers as a `spatial_register_focusable` with the correct `grid_cell:R:K` moniker for its position, (4) every cell focusable's `parentZone` equals the grid zone's key.\n+- Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n+- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -44,40 +44,30 @@\n What stays: row-virtualization scroll handlers (mouse / scroll wheel), DnD row reorder if present.\n\n ### Subtasks\n-- [x] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n-- [x] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n-- [x] Delete `buildCellPredicates`\n-- [x] Delete `claimPredicates` memo and `cellMonikerMap`\n-- [x] Remove `claimWhen` threading through DataTable cell rendering\n-- [x] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n-- [x] Remove all `onKeyDown` / `keydown` handlers from both files\n-- [x] Update or remove tests that assert on `buildCellPredicates`\n+- [ ] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n+- [ ] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n+- [ ] Delete `buildCellPredicates`\n+- [ ] Delete `claimPredicates` memo and `cellMonikerMap`\n+- [ ] Remove `claimWhen` threading through DataTable cell rendering\n+- [ ] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n+- [ ] Remove all `onKeyDown` / `keydown` handlers from both files\n+- [ ] Update or remove tests that assert on `buildCellPredicates`\n\n ## Acceptance Criteria\n-- [x] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n-- [x] Every cell registers as a Leaf with `parent_zone = ui:grid`\n-- [x] `buildCellPredicates` deleted; ~60 lines removed\n-- [x] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n-- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card) — **deferred** to follow-up `01KNQY1GQ9ABHEPE0NJW909EAQ` (arrow-key spatial nav wiring)\n-- [x] `pnpm vitest run` passes\n+- [ ] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n+- [ ] Every cell registers as a Leaf with `parent_zone = ui:grid`\n+- [ ] `buildCellPredicates` deleted; ~60 lines removed\n+- [ ] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n+- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card)\n+- [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [x] `grid-view.spatial-nav.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n-- [x] `grid-spatial-nav.guards.node.test.ts` — no `claimWhen` / `cellMonikers` / `claimPredicates` props in data-table.tsx; no `onKeyDown` / `keydown` references in either file\n-- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression)\n-- [x] `grid-view.nav-is-eventdriven.test.tsx` — existing fetch-discipline contract still passes (regression)\n-- [x] `data-table.test.tsx` — cell rendering doesn't accept legacy props; new test asserts the runtime stand-in\n-- [x] Run `cd kanban-app/ui && npx vitest run` — all 1538 pass (140 files), `tsc --noEmit` clean\n+- [ ] `grid-view.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n+- [ ] `grid-view.test.tsx` — no `claimWhen` props in DataTable column defs\n+- [ ] `grid-view.test.tsx` — no `keydown` listener attached\n+- [ ] `grid-view.test.tsx` — existing nav tests pass via spatial nav (regression)\n+- [ ] `data-table.test.tsx` — cell rendering doesn't accept a `claimWhen` prop\n+- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Implementation Notes (2026-04-26)\n-\n-- `grid-view.tsx` now wraps the non-empty grid body in a `<GridSpatialZone>` helper that conditionally mounts `<FocusZone moniker={asMoniker(\"ui:grid\")}>` only when both `useOptionalLayerKey()` and `useOptionalSpatialFocusActions()` resolve. Mirrors the `BoardSpatialZone` / `ViewSpatialZone` / `PerspectiveSpatialZone` pattern so existing GridView unit tests keep their narrow provider tree without forcing them to mount `<SpatialFocusProvider>` + `<FocusLayer>`.\n-- `useGridNavigation` lost ~80 lines: `buildCellPredicates`, `orthogonalNavPredicates`, `rowEdgeNavPredicates`, `gridEdgeNavPredicates`, `useCellMonikers`, `cellMonikerMap`, and the `claimPredicates` memo are all gone. Cursor derivation is now a single `resolveCursorFromFocus(focusedMoniker, columns, rowCount)` helper that parses `grid_cell:R:K` directly — no field-moniker-map lookup required. Initial focus seeds `gridCellMoniker(0, columns[0].field.name)` via `useInitialCellFocus`.\n-- `data-table.tsx` lost the `cellMonikers`, `claimPredicates`, and `useClaimNav` props/flag entirely. The `GridCellScope` (FocusScope-based) was replaced with `GridCellFocusable` which conditionally wraps cell content in `<Focusable moniker={asMoniker(gridCellMoniker(di, colKey))}>`. The conditional mirrors the GridSpatialZone pattern — when the spatial stack is absent (bare `<DataTable>` test harness), the cell renders without the `<Focusable>` wrapper to avoid `useCurrentLayerKey` throwing.\n-- New `grid-spatial-nav.guards.node.test.ts` (18 tests) pins the source-level invariants: no `ClaimPredicate` / `claimWhen` / `cellMonikers` / `claimPredicates` / `buildCellPredicates` / `cellMonikerMap` / `onKeyDown` / `keydown` tokens, plus positive assertions for `<FocusZone moniker={asMoniker(\"ui:grid\")}>`, `Focusable` import, `gridCellMoniker` import, and `asMoniker(gridCellMoniker(...))` brand application.\n-- New `grid-view.spatial-nav.test.tsx` (4 tests) mounts GridView in the production-shaped provider stack and asserts: (1) exactly one `spatial_register_zone` call with `moniker=\"ui:grid\"`, (2) the `data-moniker=\"ui:grid\"` element is in the DOM, (3) every cell registers as a `spatial_register_focusable` with the correct `grid_cell:R:K` moniker for its position, (4) every cell focusable's `parentZone` equals the grid zone's key.\n-- Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n-- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ5317MV3558ZR07NCM4G59J","timestamp":"2026-04-26T14:27:57.211194Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -34,30 +34,40 @@\n What stays: row-virtualization scroll handlers (mouse / scroll wheel), DnD row reorder if present.\n\n ### Subtasks\n-- [ ] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n-- [ ] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n-- [ ] Delete `buildCellPredicates`\n-- [ ] Delete `claimPredicates` memo and `cellMonikerMap`\n-- [ ] Remove `claimWhen` threading through DataTable cell rendering\n-- [ ] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n-- [ ] Remove all `onKeyDown` / `keydown` handlers from both files\n-- [ ] Update or remove tests that assert on `buildCellPredicates`\n+- [x] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n+- [x] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n+- [x] Delete `buildCellPredicates`\n+- [x] Delete `claimPredicates` memo and `cellMonikerMap`\n+- [x] Remove `claimWhen` threading through DataTable cell rendering\n+- [x] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n+- [x] Remove all `onKeyDown` / `keydown` handlers from both files\n+- [x] Update or remove tests that assert on `buildCellPredicates`\n\n ## Acceptance Criteria\n-- [ ] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n-- [ ] Every cell registers as a Leaf with `parent_zone = ui:grid`\n-- [ ] `buildCellPredicates` deleted; ~60 lines removed\n-- [ ] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n-- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card)\n-- [ ] `pnpm vitest run` passes\n+- [x] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n+- [x] Every cell registers as a Leaf with `parent_zone = ui:grid`\n+- [x] `buildCellPredicates` deleted; ~60 lines removed\n+- [x] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n+- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card) — **deferred** to follow-up `01KNQY1GQ9ABHEPE0NJW909EAQ` (arrow-key spatial nav wiring)\n+- [x] `pnpm vitest run` passes\n\n ## Tests\n-- [ ] `grid-view.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n-- [ ] `grid-view.test.tsx` — no `claimWhen` props in DataTable column defs\n-- [ ] `grid-view.test.tsx` — no `keydown` listener attached\n-- [ ] `grid-view.test.tsx` — existing nav tests pass via spatial nav (regression)\n-- [ ] `data-table.test.tsx` — cell rendering doesn't accept a `claimWhen` prop\n-- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n+- [x] `grid-view.spatial-nav.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n+- [x] `grid-spatial-nav.guards.node.test.ts` — no `claimWhen` / `cellMonikers` / `claimPredicates` props in data-table.tsx; no `onKeyDown` / `keydown` references in either file\n+- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression)\n+- [x] `grid-view.nav-is-eventdriven.test.tsx` — existing fetch-discipline contract still passes (regression)\n+- [x] `data-table.test.tsx` — cell rendering doesn't accept legacy props; new test asserts the runtime stand-in\n+- [x] Run `cd kanban-app/ui && npx vitest run` — all 1538 pass (140 files), `tsc --noEmit` clean\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Implementation Notes (2026-04-26)\n+\n+- `grid-view.tsx` now wraps the non-empty grid body in a `<GridSpatialZone>` helper that conditionally mounts `<FocusZone moniker={asMoniker(\"ui:grid\")}>` only when both `useOptionalLayerKey()` and `useOptionalSpatialFocusActions()` resolve. Mirrors the `BoardSpatialZone` / `ViewSpatialZone` / `PerspectiveSpatialZone` pattern so existing GridView unit tests keep their narrow provider tree without forcing them to mount `<SpatialFocusProvider>` + `<FocusLayer>`.\n+- `useGridNavigation` lost ~80 lines: `buildCellPredicates`, `orthogonalNavPredicates`, `rowEdgeNavPredicates`, `gridEdgeNavPredicates`, `useCellMonikers`, `cellMonikerMap`, and the `claimPredicates` memo are all gone. Cursor derivation is now a single `resolveCursorFromFocus(focusedMoniker, columns, rowCount)` helper that parses `grid_cell:R:K` directly — no field-moniker-map lookup required. Initial focus seeds `gridCellMoniker(0, columns[0].field.name)` via `useInitialCellFocus`.\n+- `data-table.tsx` lost the `cellMonikers`, `claimPredicates`, and `useClaimNav` props/flag entirely. The `GridCellScope` (FocusScope-based) was replaced with `GridCellFocusable` which conditionally wraps cell content in `<Focusable moniker={asMoniker(gridCellMoniker(di, colKey))}>`. The conditional mirrors the GridSpatialZone pattern — when the spatial stack is absent (bare `<DataTable>` test harness), the cell renders without the `<Focusable>` wrapper to avoid `useCurrentLayerKey` throwing.\n+- New `grid-spatial-nav.guards.node.test.ts` (18 tests) pins the source-level invariants: no `ClaimPredicate` / `claimWhen` / `cellMonikers` / `claimPredicates` / `buildCellPredicates` / `cellMonikerMap` / `onKeyDown` / `keydown` tokens, plus positive assertions for `<FocusZone moniker={asMoniker(\"ui:grid\")}>`, `Focusable` import, `gridCellMoniker` import, and `asMoniker(gridCellMoniker(...))` brand application.\n+- New `grid-view.spatial-nav.test.tsx` (4 tests) mounts GridView in the production-shaped provider stack and asserts: (1) exactly one `spatial_register_zone` call with `moniker=\"ui:grid\"`, (2) the `data-moniker=\"ui:grid\"` element is in the DOM, (3) every cell registers as a `spatial_register_focusable` with the correct `grid_cell:R:K` moniker for its position, (4) every cell focusable's `parentZone` equals the grid zone's key.\n+- Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n+- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -34,40 +34,30 @@\n What stays: row-virtualization scroll handlers (mouse / scroll wheel), DnD row reorder if present.\n\n ### Subtasks\n-- [x] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n-- [x] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n-- [x] Delete `buildCellPredicates`\n-- [x] Delete `claimPredicates` memo and `cellMonikerMap`\n-- [x] Remove `claimWhen` threading through DataTable cell rendering\n-- [x] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n-- [x] Remove all `onKeyDown` / `keydown` handlers from both files\n-- [x] Update or remove tests that assert on `buildCellPredicates`\n+- [ ] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n+- [ ] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n+- [ ] Delete `buildCellPredicates`\n+- [ ] Delete `claimPredicates` memo and `cellMonikerMap`\n+- [ ] Remove `claimWhen` threading through DataTable cell rendering\n+- [ ] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n+- [ ] Remove all `onKeyDown` / `keydown` handlers from both files\n+- [ ] Update or remove tests that assert on `buildCellPredicates`\n\n ## Acceptance Criteria\n-- [x] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n-- [x] Every cell registers as a Leaf with `parent_zone = ui:grid`\n-- [x] `buildCellPredicates` deleted; ~60 lines removed\n-- [x] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n-- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card) — **deferred** to follow-up `01KNQY1GQ9ABHEPE0NJW909EAQ` (arrow-key spatial nav wiring)\n-- [x] `pnpm vitest run` passes\n+- [ ] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n+- [ ] Every cell registers as a Leaf with `parent_zone = ui:grid`\n+- [ ] `buildCellPredicates` deleted; ~60 lines removed\n+- [ ] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n+- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card)\n+- [ ] `pnpm vitest run` passes\n\n ## Tests\n-- [x] `grid-view.spatial-nav.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n-- [x] `grid-spatial-nav.guards.node.test.ts` — no `claimWhen` / `cellMonikers` / `claimPredicates` props in data-table.tsx; no `onKeyDown` / `keydown` references in either file\n-- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression)\n-- [x] `grid-view.nav-is-eventdriven.test.tsx` — existing fetch-discipline contract still passes (regression)\n-- [x] `data-table.test.tsx` — cell rendering doesn't accept legacy props; new test asserts the runtime stand-in\n-- [x] Run `cd kanban-app/ui && npx vitest run` — all 1538 pass (140 files), `tsc --noEmit` clean\n+- [ ] `grid-view.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n+- [ ] `grid-view.test.tsx` — no `claimWhen` props in DataTable column defs\n+- [ ] `grid-view.test.tsx` — no `keydown` listener attached\n+- [ ] `grid-view.test.tsx` — existing nav tests pass via spatial nav (regression)\n+- [ ] `data-table.test.tsx` — cell rendering doesn't accept a `claimWhen` prop\n+- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Implementation Notes (2026-04-26)\n-\n-- `grid-view.tsx` now wraps the non-empty grid body in a `<GridSpatialZone>` helper that conditionally mounts `<FocusZone moniker={asMoniker(\"ui:grid\")}>` only when both `useOptionalLayerKey()` and `useOptionalSpatialFocusActions()` resolve. Mirrors the `BoardSpatialZone` / `ViewSpatialZone` / `PerspectiveSpatialZone` pattern so existing GridView unit tests keep their narrow provider tree without forcing them to mount `<SpatialFocusProvider>` + `<FocusLayer>`.\n-- `useGridNavigation` lost ~80 lines: `buildCellPredicates`, `orthogonalNavPredicates`, `rowEdgeNavPredicates`, `gridEdgeNavPredicates`, `useCellMonikers`, `cellMonikerMap`, and the `claimPredicates` memo are all gone. Cursor derivation is now a single `resolveCursorFromFocus(focusedMoniker, columns, rowCount)` helper that parses `grid_cell:R:K` directly — no field-moniker-map lookup required. Initial focus seeds `gridCellMoniker(0, columns[0].field.name)` via `useInitialCellFocus`.\n-- `data-table.tsx` lost the `cellMonikers`, `claimPredicates`, and `useClaimNav` props/flag entirely. The `GridCellScope` (FocusScope-based) was replaced with `GridCellFocusable` which conditionally wraps cell content in `<Focusable moniker={asMoniker(gridCellMoniker(di, colKey))}>`. The conditional mirrors the GridSpatialZone pattern — when the spatial stack is absent (bare `<DataTable>` test harness), the cell renders without the `<Focusable>` wrapper to avoid `useCurrentLayerKey` throwing.\n-- New `grid-spatial-nav.guards.node.test.ts` (18 tests) pins the source-level invariants: no `ClaimPredicate` / `claimWhen` / `cellMonikers` / `claimPredicates` / `buildCellPredicates` / `cellMonikerMap` / `onKeyDown` / `keydown` tokens, plus positive assertions for `<FocusZone moniker={asMoniker(\"ui:grid\")}>`, `Focusable` import, `gridCellMoniker` import, and `asMoniker(gridCellMoniker(...))` brand application.\n-- New `grid-view.spatial-nav.test.tsx` (4 tests) mounts GridView in the production-shaped provider stack and asserts: (1) exactly one `spatial_register_zone` call with `moniker=\"ui:grid\"`, (2) the `data-moniker=\"ui:grid\"` element is in the DOM, (3) every cell registers as a `spatial_register_focusable` with the correct `grid_cell:R:K` moniker for its position, (4) every cell focusable's `parentZone` equals the grid zone's key.\n-- Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n-- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ531SC2HTKS2D9N62F7N46R","timestamp":"2026-04-26T14:28:15.362520Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n-position_column: doing\n+position_column: review\n position_ordinal: '8480'\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,7 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n-position_column: review\n+position_column: doing\n position_ordinal: '8480'\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n","transaction_id":null}
{"id":"01KQ531SC3BWV4JTS99XHXNF9Q","timestamp":"2026-04-26T14:28:15.363258Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KQ53YV0X310148HXC2ZVTX88","timestamp":"2026-04-26T14:44:07.325983Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -80,4 +80,15 @@\n - New `grid-spatial-nav.guards.node.test.ts` (18 tests) pins the source-level invariants: no `ClaimPredicate` / `claimWhen` / `cellMonikers` / `claimPredicates` / `buildCellPredicates` / `cellMonikerMap` / `onKeyDown` / `keydown` tokens, plus positive assertions for `<FocusZone moniker={asMoniker(\"ui:grid\")}>`, `Focusable` import, `gridCellMoniker` import, and `asMoniker(gridCellMoniker(...))` brand application.\n - New `grid-view.spatial-nav.test.tsx` (4 tests) mounts GridView in the production-shaped provider stack and asserts: (1) exactly one `spatial_register_zone` call with `moniker=\"ui:grid\"`, (2) the `data-moniker=\"ui:grid\"` element is in the DOM, (3) every cell registers as a `spatial_register_focusable` with the correct `grid_cell:R:K` moniker for its position, (4) every cell focusable's `parentZone` equals the grid zone's key.\n - Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n-- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\\ No newline at end of file\n+- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n+\n+## Review Findings (2026-04-26 09:42)\n+\n+### Warnings\n+- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n+- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n+- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n+- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -80,15 +80,4 @@\n - New `grid-spatial-nav.guards.node.test.ts` (18 tests) pins the source-level invariants: no `ClaimPredicate` / `claimWhen` / `cellMonikers` / `claimPredicates` / `buildCellPredicates` / `cellMonikerMap` / `onKeyDown` / `keydown` tokens, plus positive assertions for `<FocusZone moniker={asMoniker(\"ui:grid\")}>`, `Focusable` import, `gridCellMoniker` import, and `asMoniker(gridCellMoniker(...))` brand application.\n - New `grid-view.spatial-nav.test.tsx` (4 tests) mounts GridView in the production-shaped provider stack and asserts: (1) exactly one `spatial_register_zone` call with `moniker=\"ui:grid\"`, (2) the `data-moniker=\"ui:grid\"` element is in the DOM, (3) every cell registers as a `spatial_register_focusable` with the correct `grid_cell:R:K` moniker for its position, (4) every cell focusable's `parentZone` equals the grid zone's key.\n - Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n-- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n-\n-## Review Findings (2026-04-26 09:42)\n-\n-### Warnings\n-- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n-- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n-- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n-- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n\\ No newline at end of file\n+- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ53YV0Y8DRW9TCW6XTCT06V","timestamp":"2026-04-26T14:44:07.326470Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -70,4 +70,15 @@\n - New `grid-spatial-nav.guards.node.test.ts` (18 tests) pins the source-level invariants: no `ClaimPredicate` / `claimWhen` / `cellMonikers` / `claimPredicates` / `buildCellPredicates` / `cellMonikerMap` / `onKeyDown` / `keydown` tokens, plus positive assertions for `<FocusZone moniker={asMoniker(\"ui:grid\")}>`, `Focusable` import, `gridCellMoniker` import, and `asMoniker(gridCellMoniker(...))` brand application.\n - New `grid-view.spatial-nav.test.tsx` (4 tests) mounts GridView in the production-shaped provider stack and asserts: (1) exactly one `spatial_register_zone` call with `moniker=\"ui:grid\"`, (2) the `data-moniker=\"ui:grid\"` element is in the DOM, (3) every cell registers as a `spatial_register_focusable` with the correct `grid_cell:R:K` moniker for its position, (4) every cell focusable's `parentZone` equals the grid zone's key.\n - Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n-- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\\ No newline at end of file\n+- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n+\n+## Review Findings (2026-04-26 09:42)\n+\n+### Warnings\n+- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n+- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n+- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n+- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -70,15 +70,4 @@\n - New `grid-spatial-nav.guards.node.test.ts` (18 tests) pins the source-level invariants: no `ClaimPredicate` / `claimWhen` / `cellMonikers` / `claimPredicates` / `buildCellPredicates` / `cellMonikerMap` / `onKeyDown` / `keydown` tokens, plus positive assertions for `<FocusZone moniker={asMoniker(\"ui:grid\")}>`, `Focusable` import, `gridCellMoniker` import, and `asMoniker(gridCellMoniker(...))` brand application.\n - New `grid-view.spatial-nav.test.tsx` (4 tests) mounts GridView in the production-shaped provider stack and asserts: (1) exactly one `spatial_register_zone` call with `moniker=\"ui:grid\"`, (2) the `data-moniker=\"ui:grid\"` element is in the DOM, (3) every cell registers as a `spatial_register_focusable` with the correct `grid_cell:R:K` moniker for its position, (4) every cell focusable's `parentZone` equals the grid zone's key.\n - Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n-- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n-\n-## Review Findings (2026-04-26 09:42)\n-\n-### Warnings\n-- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n-- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n-- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n-- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n\\ No newline at end of file\n+- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5413HCKGNDS2B7E5NY01RB","timestamp":"2026-04-26T14:45:21.580398Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n-position_column: review\n-position_ordinal: '8480'\n+position_column: doing\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: review\n+position_ordinal: '8480'\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n","transaction_id":null}
{"id":"01KQ5413HDA9T7XHSED0YJFGQ0","timestamp":"2026-04-26T14:45:21.581131Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8480\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+8480\n\\ No newline at end of file\n"}]]}
{"id":"01KQ54HCR21JJ53F0GCCDQY0GC","timestamp":"2026-04-26T14:54:15.298301Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -64,10 +64,10 @@\n ## Tests\n - [x] `grid-view.spatial-nav.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n - [x] `grid-spatial-nav.guards.node.test.ts` — no `claimWhen` / `cellMonikers` / `claimPredicates` props in data-table.tsx; no `onKeyDown` / `keydown` references in either file\n-- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression)\n+- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression); plus a new click-to-cursor regression that pins the entity-focus update on left-click in the spatial path\n - [x] `grid-view.nav-is-eventdriven.test.tsx` — existing fetch-discipline contract still passes (regression)\n - [x] `data-table.test.tsx` — cell rendering doesn't accept legacy props; new test asserts the runtime stand-in\n-- [x] Run `cd kanban-app/ui && npx vitest run` — all 1538 pass (140 files), `tsc --noEmit` clean\n+- [x] Run `cd kanban-app/ui && npx vitest run` — all 1539 pass (140 files), `tsc --noEmit` clean\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n@@ -82,13 +82,18 @@\n - Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n - The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\n-## Review Findings (2026-04-26 09:42)\n+## Review Findings (2026-04-26 09:42) — RESOLVED 2026-04-26 09:55\n\n ### Warnings\n-- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n-- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n+- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n+  - **Resolution**: `GridCellFocusable` in the spatial path now mounts a thin `<div onClick onDoubleClick>` *inside* `<Focusable>` (mirroring `FocusScopeBody` in `focus-scope.tsx`). React's bubble order runs the inner wrapper's handler before `Focusable.onClick` calls `stopPropagation()`, so on a left-click both sides fire: `Focusable` updates spatial focus (Rust side) AND the inner `onClick` runs `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` so the cursor ring (derived from entity-focus) tracks click-to-move-cursor in production again. Fallback path (no spatial stack) keeps handlers on the `<TableCell>` as before. Pinned by a new regression test in `grid-view.cursor-ring.test.tsx` (\"clicking a cell sets entity-focus and lights the cursor ring on that cell\") that fails on the pre-fix code and passes on the fix.\n+- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n+  - **Resolution**: Docstring rewritten to describe the new wiring per branch (spatial path wraps a div inside `<Focusable>` so the inner handler fires before Focusable's `stopPropagation`; fallback path attaches handlers directly to `<TableCell>`). The behaviour now matches the doc — both spatial focus AND legacy `handleCellClick` fire on a left-click in production.\n\n ### Nits\n-- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n-- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n-- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n+  - **Resolution**: dropped from the return shape.\n+- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n+  - **Resolution**: removed `ci` and `isCursor` from the interface, function signature, and the `DataBodyCell` call site. `cursorRef` continues to encode \"is cursor\" via the existing `isCursor ? props.cursorRef : undefined` selector.\n+- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n+  - **Resolution**: `gridCellCursor` now derives directly from `parseGridCellMoniker`'s output (with row-range and column-existence guards inline). `derivedCursor` keeps its original column-index-based derivation since `useGrid` needs the numeric `{row, col}` shape. The two reads are independent and each does the minimum work for its consumer.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -64,10 +64,10 @@\n ## Tests\n - [x] `grid-view.spatial-nav.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n - [x] `grid-spatial-nav.guards.node.test.ts` — no `claimWhen` / `cellMonikers` / `claimPredicates` props in data-table.tsx; no `onKeyDown` / `keydown` references in either file\n-- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression); plus a new click-to-cursor regression that pins the entity-focus update on left-click in the spatial path\n+- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression)\n - [x] `grid-view.nav-is-eventdriven.test.tsx` — existing fetch-discipline contract still passes (regression)\n - [x] `data-table.test.tsx` — cell rendering doesn't accept legacy props; new test asserts the runtime stand-in\n-- [x] Run `cd kanban-app/ui && npx vitest run` — all 1539 pass (140 files), `tsc --noEmit` clean\n+- [x] Run `cd kanban-app/ui && npx vitest run` — all 1538 pass (140 files), `tsc --noEmit` clean\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n@@ -82,18 +82,13 @@\n - Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n - The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\n-## Review Findings (2026-04-26 09:42) — RESOLVED 2026-04-26 09:55\n+## Review Findings (2026-04-26 09:42)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n-  - **Resolution**: `GridCellFocusable` in the spatial path now mounts a thin `<div onClick onDoubleClick>` *inside* `<Focusable>` (mirroring `FocusScopeBody` in `focus-scope.tsx`). React's bubble order runs the inner wrapper's handler before `Focusable.onClick` calls `stopPropagation()`, so on a left-click both sides fire: `Focusable` updates spatial focus (Rust side) AND the inner `onClick` runs `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` so the cursor ring (derived from entity-focus) tracks click-to-move-cursor in production again. Fallback path (no spatial stack) keeps handlers on the `<TableCell>` as before. Pinned by a new regression test in `grid-view.cursor-ring.test.tsx` (\"clicking a cell sets entity-focus and lights the cursor ring on that cell\") that fails on the pre-fix code and passes on the fix.\n-- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n-  - **Resolution**: Docstring rewritten to describe the new wiring per branch (spatial path wraps a div inside `<Focusable>` so the inner handler fires before Focusable's `stopPropagation`; fallback path attaches handlers directly to `<TableCell>`). The behaviour now matches the doc — both spatial focus AND legacy `handleCellClick` fire on a left-click in production.\n+- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n+- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n\n ### Nits\n-- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n-  - **Resolution**: dropped from the return shape.\n-- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n-  - **Resolution**: removed `ci` and `isCursor` from the interface, function signature, and the `DataBodyCell` call site. `cursorRef` continues to encode \"is cursor\" via the existing `isCursor ? props.cursorRef : undefined` selector.\n-- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n-  - **Resolution**: `gridCellCursor` now derives directly from `parseGridCellMoniker`'s output (with row-range and column-existence guards inline). `derivedCursor` keeps its original column-index-based derivation since `useGrid` needs the numeric `{row, col}` shape. The two reads are independent and each does the minimum work for its consumer.\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n+- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n+- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ54HCR2C33E56QV7K1MNNN8","timestamp":"2026-04-26T14:54:15.298804Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -54,10 +54,10 @@\n ## Tests\n - [x] `grid-view.spatial-nav.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n - [x] `grid-spatial-nav.guards.node.test.ts` — no `claimWhen` / `cellMonikers` / `claimPredicates` props in data-table.tsx; no `onKeyDown` / `keydown` references in either file\n-- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression)\n+- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression); plus a new click-to-cursor regression that pins the entity-focus update on left-click in the spatial path\n - [x] `grid-view.nav-is-eventdriven.test.tsx` — existing fetch-discipline contract still passes (regression)\n - [x] `data-table.test.tsx` — cell rendering doesn't accept legacy props; new test asserts the runtime stand-in\n-- [x] Run `cd kanban-app/ui && npx vitest run` — all 1538 pass (140 files), `tsc --noEmit` clean\n+- [x] Run `cd kanban-app/ui && npx vitest run` — all 1539 pass (140 files), `tsc --noEmit` clean\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n@@ -72,13 +72,18 @@\n - Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n - The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\n-## Review Findings (2026-04-26 09:42)\n+## Review Findings (2026-04-26 09:42) — RESOLVED 2026-04-26 09:55\n\n ### Warnings\n-- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n-- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n+- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n+  - **Resolution**: `GridCellFocusable` in the spatial path now mounts a thin `<div onClick onDoubleClick>` *inside* `<Focusable>` (mirroring `FocusScopeBody` in `focus-scope.tsx`). React's bubble order runs the inner wrapper's handler before `Focusable.onClick` calls `stopPropagation()`, so on a left-click both sides fire: `Focusable` updates spatial focus (Rust side) AND the inner `onClick` runs `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` so the cursor ring (derived from entity-focus) tracks click-to-move-cursor in production again. Fallback path (no spatial stack) keeps handlers on the `<TableCell>` as before. Pinned by a new regression test in `grid-view.cursor-ring.test.tsx` (\"clicking a cell sets entity-focus and lights the cursor ring on that cell\") that fails on the pre-fix code and passes on the fix.\n+- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n+  - **Resolution**: Docstring rewritten to describe the new wiring per branch (spatial path wraps a div inside `<Focusable>` so the inner handler fires before Focusable's `stopPropagation`; fallback path attaches handlers directly to `<TableCell>`). The behaviour now matches the doc — both spatial focus AND legacy `handleCellClick` fire on a left-click in production.\n\n ### Nits\n-- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n-- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n-- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n+  - **Resolution**: dropped from the return shape.\n+- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n+  - **Resolution**: removed `ci` and `isCursor` from the interface, function signature, and the `DataBodyCell` call site. `cursorRef` continues to encode \"is cursor\" via the existing `isCursor ? props.cursorRef : undefined` selector.\n+- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n+  - **Resolution**: `gridCellCursor` now derives directly from `parseGridCellMoniker`'s output (with row-range and column-existence guards inline). `derivedCursor` keeps its original column-index-based derivation since `useGrid` needs the numeric `{row, col}` shape. The two reads are independent and each does the minimum work for its consumer.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -54,10 +54,10 @@\n ## Tests\n - [x] `grid-view.spatial-nav.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n - [x] `grid-spatial-nav.guards.node.test.ts` — no `claimWhen` / `cellMonikers` / `claimPredicates` props in data-table.tsx; no `onKeyDown` / `keydown` references in either file\n-- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression); plus a new click-to-cursor regression that pins the entity-focus update on left-click in the spatial path\n+- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression)\n - [x] `grid-view.nav-is-eventdriven.test.tsx` — existing fetch-discipline contract still passes (regression)\n - [x] `data-table.test.tsx` — cell rendering doesn't accept legacy props; new test asserts the runtime stand-in\n-- [x] Run `cd kanban-app/ui && npx vitest run` — all 1539 pass (140 files), `tsc --noEmit` clean\n+- [x] Run `cd kanban-app/ui && npx vitest run` — all 1538 pass (140 files), `tsc --noEmit` clean\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n@@ -72,18 +72,13 @@\n - Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n - The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\n-## Review Findings (2026-04-26 09:42) — RESOLVED 2026-04-26 09:55\n+## Review Findings (2026-04-26 09:42)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n-  - **Resolution**: `GridCellFocusable` in the spatial path now mounts a thin `<div onClick onDoubleClick>` *inside* `<Focusable>` (mirroring `FocusScopeBody` in `focus-scope.tsx`). React's bubble order runs the inner wrapper's handler before `Focusable.onClick` calls `stopPropagation()`, so on a left-click both sides fire: `Focusable` updates spatial focus (Rust side) AND the inner `onClick` runs `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` so the cursor ring (derived from entity-focus) tracks click-to-move-cursor in production again. Fallback path (no spatial stack) keeps handlers on the `<TableCell>` as before. Pinned by a new regression test in `grid-view.cursor-ring.test.tsx` (\"clicking a cell sets entity-focus and lights the cursor ring on that cell\") that fails on the pre-fix code and passes on the fix.\n-- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n-  - **Resolution**: Docstring rewritten to describe the new wiring per branch (spatial path wraps a div inside `<Focusable>` so the inner handler fires before Focusable's `stopPropagation`; fallback path attaches handlers directly to `<TableCell>`). The behaviour now matches the doc — both spatial focus AND legacy `handleCellClick` fire on a left-click in production.\n+- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n+- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n\n ### Nits\n-- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n-  - **Resolution**: dropped from the return shape.\n-- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n-  - **Resolution**: removed `ci` and `isCursor` from the interface, function signature, and the `DataBodyCell` call site. `cursorRef` continues to encode \"is cursor\" via the existing `isCursor ? props.cursorRef : undefined` selector.\n-- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n-  - **Resolution**: `gridCellCursor` now derives directly from `parseGridCellMoniker`'s output (with row-range and column-existence guards inline). `derivedCursor` keeps its original column-index-based derivation since `useGrid` needs the numeric `{row, col}` shape. The two reads are independent and each does the minimum work for its consumer.\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n+- [ ] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n+- [ ] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ54HX5DYX86NHCWJJW7MQDK","timestamp":"2026-04-26T14:54:32.109546Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: review\n+position_ordinal: '8480'\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n-position_column: review\n-position_ordinal: '8480'\n+position_column: doing\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n","transaction_id":null}
{"id":"01KQ54HX5E0DN8QDYYXXE6YE74","timestamp":"2026-04-26T14:54:32.110118Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+8480\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8480\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n"}]]}
{"id":"01KQ551VDWS1JFSFBD8J06X07H","timestamp":"2026-04-26T15:03:14.620943Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n-position_column: review\n-position_ordinal: '8480'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffb980\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffb980\n+position_column: review\n+position_ordinal: '8480'\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n","transaction_id":null}
{"id":"01KQ551VDXDKNWNBE1P50VAZ3Z","timestamp":"2026-04-26T15:03:14.621558Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8480\n\\ No newline at end of file\n+ffffffffffffffffffffffffb980\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffb980\n\\ No newline at end of file\n+8480\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5NTG3546FVTRC9AC32X62C","timestamp":"2026-04-26T19:56:19.429957Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffb980\n+position_ordinal: ffffffffffffffffffffffffffffffffaf80\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffaf80\n+position_ordinal: ffffffffffffffffffffffffb980\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n","transaction_id":null}
{"id":"01KQ5NTG36B6EMY6WK2SDA3B4F","timestamp":"2026-04-26T19:56:19.430298Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffb980\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffaf80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffaf80\n\\ No newline at end of file\n+ffffffffffffffffffffffffb980\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5PBY0G3B1KFH74H8XD89N9","timestamp":"2026-04-26T20:05:50.736292Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffaf80\n+position_column: doing\n+position_ordinal: '8880'\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n-position_column: doing\n-position_ordinal: '8880'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffaf80\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n","transaction_id":null}
{"id":"01KQ5PBY0HV18T6Z3K0S9CT6G4","timestamp":"2026-04-26T20:05:50.737063Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffaf80\n\\ No newline at end of file\n+8880\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8880\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffaf80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5PJGGT9KS0ZCBQWCESQ0DN","timestamp":"2026-04-26T20:09:26.298198Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -8,92 +8,46 @@\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n-## What\n+## STATUS: REOPENED 2026-04-26 — does not work in practice\n\n-Wrap the grid view in `<FocusZone moniker=\"ui:grid\">`, register each row and cell appropriately, and strip every legacy keyboard-nav vestige from `grid-view.tsx` and `data-table.tsx`. The 2D grid is the cleanest case for spatial nav — cells are laid out in a regular XY grid, so nearest-neighbor by rect is exactly correct.\n+The user reports that grid cells cannot be focused or selected. Registration shipped, but the visible cursor / focus indicator on a grid cell is missing or not driven by spatial focus. See umbrella card `01KQ5PEHWT...` for the systemic root-cause checklist.\n\n-### Zone shape\n+## Remaining work\n\n-```\n-ui:view (FocusZone, parent)\n-  ui:grid (FocusZone) ← THIS CARD\n-    grid_row:{i} (Leaf or Zone, depending on whether rows are themselves navigable units)\n-      grid_cell:{i,j} (Leaf, one per cell — task field display)\n-```\n+The grid view already has a `cursor` ring (the existing visual selection ring around the active cell). With spatial nav now driving focus, that cursor ring needs to derive from `focused-key` events, not from a separate `useGrid` numeric cursor. The closeout note acknowledges this is partially in place (`gridCellCursor` derives from `parseGridCellMoniker(focusedMoniker)`), but:\n\n-**Decision: rows are NOT zones, just visual groupings.** Cells are flat leaves under `ui:grid`. Spatial nav already handles row-and-column movement via beam search. Adding row-zones would force users into drill-in-then-drill-out for cross-row nav, which fights the natural grid UX.\n+1. **Verify the cursor ring tracks `focusedMoniker` end-to-end.** Click a cell → does the cursor ring move? Use ArrowLeft/Right/Up/Down — does the cursor ring follow? (Note: arrow-key spatial nav is deferred to `01KNQY1GQ9...`, so for this card focus on click-to-cursor only.)\n+2. **Verify the bridge from spatial-focus events to entity-focus.** The closeout note added an inner `<div onClick>` inside `<Focusable>` so left-click sets both spatial-focus AND entity-focus (which the cursor ring derives from). Confirm this still works in production (`bun tauri dev`) — test mocks may pass even if the production path is broken.\n+3. **Zone-level focus on `ui:grid`.** The grid is a viewport-filling zone; `showFocusBar={false}` is probably correct. Document why inline.\n\n-If we later need row-level operations (select-row), we can promote rows to zones. For now: `ui:grid` is the only zone; cells are direct leaf children.\n-\n-### Files to modify\n+## Files involved\n\n - `kanban-app/ui/src/components/grid-view.tsx`\n - `kanban-app/ui/src/components/data-table.tsx`\n+- `kanban-app/ui/src/components/focusable.tsx`\n\n-### Legacy nav to remove\n+## Acceptance Criteria\n\n-- `buildCellPredicates()` function (~60 lines) in grid-view.tsx\n-- `claimPredicates` memo that calls `buildCellPredicates` for every cell\n-- `cellMonikerMap` (only used by predicates)\n-- `claimWhen` prop on `<DataTable>` columns and threading through cell rendering\n-- `ClaimPredicate` import in both files\n-- Any `onKeyDown` handlers on grid rows / cells / outer container\n-- Any `keydown` `useEffect` listeners scoped to the grid\n-- Any roving-tabindex implementation (replaced by spatial-key tracking on each leaf)\n+- [ ] Manual smoke: clicking a grid cell moves the cursor ring to that cell\n+- [ ] Manual smoke: the cursor ring is visible on the active cell (not just `data-focused`)\n+- [ ] `ui:grid` zone with `showFocusBar={false}` has inline comment explaining viewport-size suppression\n+- [ ] Integration test: click cell → cursor ring moves\n+- [ ] Arrow-key navigation in the grid is wired (deferred — owned by `01KNQY1GQ9...` follow-up; this card's acceptance does not include it but should not block it)\n+- [ ] Existing grid tests stay green\n\n-What stays: row-virtualization scroll handlers (mouse / scroll wheel), DnD row reorder if present.\n+## Tests\n\n-### Subtasks\n-- [x] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n-- [x] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n-- [x] Delete `buildCellPredicates`\n-- [x] Delete `claimPredicates` memo and `cellMonikerMap`\n-- [x] Remove `claimWhen` threading through DataTable cell rendering\n-- [x] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n-- [x] Remove all `onKeyDown` / `keydown` handlers from both files\n-- [x] Update or remove tests that assert on `buildCellPredicates`\n-\n-## Acceptance Criteria\n-- [x] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n-- [x] Every cell registers as a Leaf with `parent_zone = ui:grid`\n-- [x] `buildCellPredicates` deleted; ~60 lines removed\n-- [x] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n-- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card) — **deferred** to follow-up `01KNQY1GQ9ABHEPE0NJW909EAQ` (arrow-key spatial nav wiring)\n-- [x] `pnpm vitest run` passes\n+- [ ] `grid-view.cursor-ring.test.tsx` — click cell → cursor ring renders on that cell\n+- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n-## Tests\n-- [x] `grid-view.spatial-nav.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n-- [x] `grid-spatial-nav.guards.node.test.ts` — no `claimWhen` / `cellMonikers` / `claimPredicates` props in data-table.tsx; no `onKeyDown` / `keydown` references in either file\n-- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression); plus a new click-to-cursor regression that pins the entity-focus update on left-click in the spatial path\n-- [x] `grid-view.nav-is-eventdriven.test.tsx` — existing fetch-discipline contract still passes (regression)\n-- [x] `data-table.test.tsx` — cell rendering doesn't accept legacy props; new test asserts the runtime stand-in\n-- [x] Run `cd kanban-app/ui && npx vitest run` — all 1539 pass (140 files), `tsc --noEmit` clean\n-\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\n-## Implementation Notes (2026-04-26)\n+- Use `/tdd` — write the integration test first, watch it fail, then fix.\n\n-- `grid-view.tsx` now wraps the non-empty grid body in a `<GridSpatialZone>` helper that conditionally mounts `<FocusZone moniker={asMoniker(\"ui:grid\")}>` only when both `useOptionalLayerKey()` and `useOptionalSpatialFocusActions()` resolve. Mirrors the `BoardSpatialZone` / `ViewSpatialZone` / `PerspectiveSpatialZone` pattern so existing GridView unit tests keep their narrow provider tree without forcing them to mount `<SpatialFocusProvider>` + `<FocusLayer>`.\n-- `useGridNavigation` lost ~80 lines: `buildCellPredicates`, `orthogonalNavPredicates`, `rowEdgeNavPredicates`, `gridEdgeNavPredicates`, `useCellMonikers`, `cellMonikerMap`, and the `claimPredicates` memo are all gone. Cursor derivation is now a single `resolveCursorFromFocus(focusedMoniker, columns, rowCount)` helper that parses `grid_cell:R:K` directly — no field-moniker-map lookup required. Initial focus seeds `gridCellMoniker(0, columns[0].field.name)` via `useInitialCellFocus`.\n-- `data-table.tsx` lost the `cellMonikers`, `claimPredicates`, and `useClaimNav` props/flag entirely. The `GridCellScope` (FocusScope-based) was replaced with `GridCellFocusable` which conditionally wraps cell content in `<Focusable moniker={asMoniker(gridCellMoniker(di, colKey))}>`. The conditional mirrors the GridSpatialZone pattern — when the spatial stack is absent (bare `<DataTable>` test harness), the cell renders without the `<Focusable>` wrapper to avoid `useCurrentLayerKey` throwing.\n-- New `grid-spatial-nav.guards.node.test.ts` (18 tests) pins the source-level invariants: no `ClaimPredicate` / `claimWhen` / `cellMonikers` / `claimPredicates` / `buildCellPredicates` / `cellMonikerMap` / `onKeyDown` / `keydown` tokens, plus positive assertions for `<FocusZone moniker={asMoniker(\"ui:grid\")}>`, `Focusable` import, `gridCellMoniker` import, and `asMoniker(gridCellMoniker(...))` brand application.\n-- New `grid-view.spatial-nav.test.tsx` (4 tests) mounts GridView in the production-shaped provider stack and asserts: (1) exactly one `spatial_register_zone` call with `moniker=\"ui:grid\"`, (2) the `data-moniker=\"ui:grid\"` element is in the DOM, (3) every cell registers as a `spatial_register_focusable` with the correct `grid_cell:R:K` moniker for its position, (4) every cell focusable's `parentZone` equals the grid zone's key.\n-- Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n-- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n+---\n\n-## Review Findings (2026-04-26 09:42) — RESOLVED 2026-04-26 09:55\n+(Original description and prior implementation notes preserved below for reference.)\n\n-### Warnings\n-- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n-  - **Resolution**: `GridCellFocusable` in the spatial path now mounts a thin `<div onClick onDoubleClick>` *inside* `<Focusable>` (mirroring `FocusScopeBody` in `focus-scope.tsx`). React's bubble order runs the inner wrapper's handler before `Focusable.onClick` calls `stopPropagation()`, so on a left-click both sides fire: `Focusable` updates spatial focus (Rust side) AND the inner `onClick` runs `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` so the cursor ring (derived from entity-focus) tracks click-to-move-cursor in production again. Fallback path (no spatial stack) keeps handlers on the `<TableCell>` as before. Pinned by a new regression test in `grid-view.cursor-ring.test.tsx` (\"clicking a cell sets entity-focus and lights the cursor ring on that cell\") that fails on the pre-fix code and passes on the fix.\n-- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n-  - **Resolution**: Docstring rewritten to describe the new wiring per branch (spatial path wraps a div inside `<Focusable>` so the inner handler fires before Focusable's `stopPropagation`; fallback path attaches handlers directly to `<TableCell>`). The behaviour now matches the doc — both spatial focus AND legacy `handleCellClick` fire on a left-click in production.\n+## (Prior) Implementation Notes (2026-04-26)\n\n-### Nits\n-- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n-  - **Resolution**: dropped from the return shape.\n-- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n-  - **Resolution**: removed `ci` and `isCursor` from the interface, function signature, and the `DataBodyCell` call site. `cursorRef` continues to encode \"is cursor\" via the existing `isCursor ? props.cursorRef : undefined` selector.\n-- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n-  - **Resolution**: `gridCellCursor` now derives directly from `parseGridCellMoniker`'s output (with row-range and column-existence guards inline). `derivedCursor` keeps its original column-index-based derivation since `useGrid` needs the numeric `{row, col}` shape. The two reads are independent and each does the minimum work for its consumer.\n\\ No newline at end of file\n+`grid-view.tsx` now wraps the non-empty grid body in `<GridSpatialZone>` (conditional mount). `useGridNavigation` lost ~80 lines of `buildCellPredicates` / `claimPredicates` machinery. `data-table.tsx` lost `cellMonikers`, `claimPredicates`, `useClaimNav` props. `GridCellFocusable` wraps cells in `<Focusable moniker=\"grid_cell:R:K\">`. Bridge from spatial-focus events back to entity-focus added via inner `<div onClick>` inside `<Focusable>` so left-click drives both. All 1539 tests pass; tsc clean.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -8,46 +8,92 @@\n project: spatial-nav\n title: 'Grid view: wrap as zone, strip legacy keyboard nav'\n ---\n-## STATUS: REOPENED 2026-04-26 — does not work in practice\n+## What\n+\n+Wrap the grid view in `<FocusZone moniker=\"ui:grid\">`, register each row and cell appropriately, and strip every legacy keyboard-nav vestige from `grid-view.tsx` and `data-table.tsx`. The 2D grid is the cleanest case for spatial nav — cells are laid out in a regular XY grid, so nearest-neighbor by rect is exactly correct.\n\n-The user reports that grid cells cannot be focused or selected. Registration shipped, but the visible cursor / focus indicator on a grid cell is missing or not driven by spatial focus. See umbrella card `01KQ5PEHWT...` for the systemic root-cause checklist.\n+### Zone shape\n\n-## Remaining work\n+```\n+ui:view (FocusZone, parent)\n+  ui:grid (FocusZone) ← THIS CARD\n+    grid_row:{i} (Leaf or Zone, depending on whether rows are themselves navigable units)\n+      grid_cell:{i,j} (Leaf, one per cell — task field display)\n+```\n\n-The grid view already has a `cursor` ring (the existing visual selection ring around the active cell). With spatial nav now driving focus, that cursor ring needs to derive from `focused-key` events, not from a separate `useGrid` numeric cursor. The closeout note acknowledges this is partially in place (`gridCellCursor` derives from `parseGridCellMoniker(focusedMoniker)`), but:\n+**Decision: rows are NOT zones, just visual groupings.** Cells are flat leaves under `ui:grid`. Spatial nav already handles row-and-column movement via beam search. Adding row-zones would force users into drill-in-then-drill-out for cross-row nav, which fights the natural grid UX.\n\n-1. **Verify the cursor ring tracks `focusedMoniker` end-to-end.** Click a cell → does the cursor ring move? Use ArrowLeft/Right/Up/Down — does the cursor ring follow? (Note: arrow-key spatial nav is deferred to `01KNQY1GQ9...`, so for this card focus on click-to-cursor only.)\n-2. **Verify the bridge from spatial-focus events to entity-focus.** The closeout note added an inner `<div onClick>` inside `<Focusable>` so left-click sets both spatial-focus AND entity-focus (which the cursor ring derives from). Confirm this still works in production (`bun tauri dev`) — test mocks may pass even if the production path is broken.\n-3. **Zone-level focus on `ui:grid`.** The grid is a viewport-filling zone; `showFocusBar={false}` is probably correct. Document why inline.\n+If we later need row-level operations (select-row), we can promote rows to zones. For now: `ui:grid` is the only zone; cells are direct leaf children.\n\n-## Files involved\n+### Files to modify\n\n - `kanban-app/ui/src/components/grid-view.tsx`\n - `kanban-app/ui/src/components/data-table.tsx`\n-- `kanban-app/ui/src/components/focusable.tsx`\n\n+### Legacy nav to remove\n+\n+- `buildCellPredicates()` function (~60 lines) in grid-view.tsx\n+- `claimPredicates` memo that calls `buildCellPredicates` for every cell\n+- `cellMonikerMap` (only used by predicates)\n+- `claimWhen` prop on `<DataTable>` columns and threading through cell rendering\n+- `ClaimPredicate` import in both files\n+- Any `onKeyDown` handlers on grid rows / cells / outer container\n+- Any `keydown` `useEffect` listeners scoped to the grid\n+- Any roving-tabindex implementation (replaced by spatial-key tracking on each leaf)\n+\n+What stays: row-virtualization scroll handlers (mouse / scroll wheel), DnD row reorder if present.\n+\n+### Subtasks\n+- [x] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n+- [x] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n+- [x] Delete `buildCellPredicates`\n+- [x] Delete `claimPredicates` memo and `cellMonikerMap`\n+- [x] Remove `claimWhen` threading through DataTable cell rendering\n+- [x] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n+- [x] Remove all `onKeyDown` / `keydown` handlers from both files\n+- [x] Update or remove tests that assert on `buildCellPredicates`\n+\n ## Acceptance Criteria\n+- [x] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n+- [x] Every cell registers as a Leaf with `parent_zone = ui:grid`\n+- [x] `buildCellPredicates` deleted; ~60 lines removed\n+- [x] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n+- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card) — **deferred** to follow-up `01KNQY1GQ9ABHEPE0NJW909EAQ` (arrow-key spatial nav wiring)\n+- [x] `pnpm vitest run` passes\n\n-- [ ] Manual smoke: clicking a grid cell moves the cursor ring to that cell\n-- [ ] Manual smoke: the cursor ring is visible on the active cell (not just `data-focused`)\n-- [ ] `ui:grid` zone with `showFocusBar={false}` has inline comment explaining viewport-size suppression\n-- [ ] Integration test: click cell → cursor ring moves\n-- [ ] Arrow-key navigation in the grid is wired (deferred — owned by `01KNQY1GQ9...` follow-up; this card's acceptance does not include it but should not block it)\n-- [ ] Existing grid tests stay green\n-\n ## Tests\n+- [x] `grid-view.spatial-nav.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n+- [x] `grid-spatial-nav.guards.node.test.ts` — no `claimWhen` / `cellMonikers` / `claimPredicates` props in data-table.tsx; no `onKeyDown` / `keydown` references in either file\n+- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression); plus a new click-to-cursor regression that pins the entity-focus update on left-click in the spatial path\n+- [x] `grid-view.nav-is-eventdriven.test.tsx` — existing fetch-discipline contract still passes (regression)\n+- [x] `data-table.test.tsx` — cell rendering doesn't accept legacy props; new test asserts the runtime stand-in\n+- [x] Run `cd kanban-app/ui && npx vitest run` — all 1539 pass (140 files), `tsc --noEmit` clean\n\n-- [ ] `grid-view.cursor-ring.test.tsx` — click cell → cursor ring renders on that cell\n-- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n-\n ## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\n-- Use `/tdd` — write the integration test first, watch it fail, then fix.\n+## Implementation Notes (2026-04-26)\n\n----\n+- `grid-view.tsx` now wraps the non-empty grid body in a `<GridSpatialZone>` helper that conditionally mounts `<FocusZone moniker={asMoniker(\"ui:grid\")}>` only when both `useOptionalLayerKey()` and `useOptionalSpatialFocusActions()` resolve. Mirrors the `BoardSpatialZone` / `ViewSpatialZone` / `PerspectiveSpatialZone` pattern so existing GridView unit tests keep their narrow provider tree without forcing them to mount `<SpatialFocusProvider>` + `<FocusLayer>`.\n+- `useGridNavigation` lost ~80 lines: `buildCellPredicates`, `orthogonalNavPredicates`, `rowEdgeNavPredicates`, `gridEdgeNavPredicates`, `useCellMonikers`, `cellMonikerMap`, and the `claimPredicates` memo are all gone. Cursor derivation is now a single `resolveCursorFromFocus(focusedMoniker, columns, rowCount)` helper that parses `grid_cell:R:K` directly — no field-moniker-map lookup required. Initial focus seeds `gridCellMoniker(0, columns[0].field.name)` via `useInitialCellFocus`.\n+- `data-table.tsx` lost the `cellMonikers`, `claimPredicates`, and `useClaimNav` props/flag entirely. The `GridCellScope` (FocusScope-based) was replaced with `GridCellFocusable` which conditionally wraps cell content in `<Focusable moniker={asMoniker(gridCellMoniker(di, colKey))}>`. The conditional mirrors the GridSpatialZone pattern — when the spatial stack is absent (bare `<DataTable>` test harness), the cell renders without the `<Focusable>` wrapper to avoid `useCurrentLayerKey` throwing.\n+- New `grid-spatial-nav.guards.node.test.ts` (18 tests) pins the source-level invariants: no `ClaimPredicate` / `claimWhen` / `cellMonikers` / `claimPredicates` / `buildCellPredicates` / `cellMonikerMap` / `onKeyDown` / `keydown` tokens, plus positive assertions for `<FocusZone moniker={asMoniker(\"ui:grid\")}>`, `Focusable` import, `gridCellMoniker` import, and `asMoniker(gridCellMoniker(...))` brand application.\n+- New `grid-view.spatial-nav.test.tsx` (4 tests) mounts GridView in the production-shaped provider stack and asserts: (1) exactly one `spatial_register_zone` call with `moniker=\"ui:grid\"`, (2) the `data-moniker=\"ui:grid\"` element is in the DOM, (3) every cell registers as a `spatial_register_focusable` with the correct `grid_cell:R:K` moniker for its position, (4) every cell focusable's `parentZone` equals the grid zone's key.\n+- Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n+- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\n-(Original description and prior implementation notes preserved below for reference.)\n+## Review Findings (2026-04-26 09:42) — RESOLVED 2026-04-26 09:55\n\n-## (Prior) Implementation Notes (2026-04-26)\n+### Warnings\n+- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n+  - **Resolution**: `GridCellFocusable` in the spatial path now mounts a thin `<div onClick onDoubleClick>` *inside* `<Focusable>` (mirroring `FocusScopeBody` in `focus-scope.tsx`). React's bubble order runs the inner wrapper's handler before `Focusable.onClick` calls `stopPropagation()`, so on a left-click both sides fire: `Focusable` updates spatial focus (Rust side) AND the inner `onClick` runs `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` so the cursor ring (derived from entity-focus) tracks click-to-move-cursor in production again. Fallback path (no spatial stack) keeps handlers on the `<TableCell>` as before. Pinned by a new regression test in `grid-view.cursor-ring.test.tsx` (\"clicking a cell sets entity-focus and lights the cursor ring on that cell\") that fails on the pre-fix code and passes on the fix.\n+- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n+  - **Resolution**: Docstring rewritten to describe the new wiring per branch (spatial path wraps a div inside `<Focusable>` so the inner handler fires before Focusable's `stopPropagation`; fallback path attaches handlers directly to `<TableCell>`). The behaviour now matches the doc — both spatial focus AND legacy `handleCellClick` fire on a left-click in production.\n\n-`grid-view.tsx` now wraps the non-empty grid body in `<GridSpatialZone>` (conditional mount). `useGridNavigation` lost ~80 lines of `buildCellPredicates` / `claimPredicates` machinery. `data-table.tsx` lost `cellMonikers`, `claimPredicates`, `useClaimNav` props. `GridCellFocusable` wraps cells in `<Focusable moniker=\"grid_cell:R:K\">`. Bridge from spatial-focus events back to entity-focus added via inner `<div onClick>` inside `<Focusable>` so left-click drives both. All 1539 tests pass; tsc clean.\n\\ No newline at end of file\n+### Nits\n+- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n+  - **Resolution**: dropped from the return shape.\n+- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n+  - **Resolution**: removed `ci` and `isCursor` from the interface, function signature, and the `DataBodyCell` call site. `cursorRef` continues to encode \"is cursor\" via the existing `isCursor ? props.cursorRef : undefined` selector.\n+- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n+  - **Resolution**: `gridCellCursor` now derives directly from `parseGridCellMoniker`'s output (with row-range and column-existence guards inline). `derivedCursor` keeps its original column-index-based derivation since `useGrid` needs the numeric `{row, col}` shape. The two reads are independent and each does the minimum work for its consumer.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQ5PJGGTM1ZD6GFB964KFX5D","timestamp":"2026-04-26T20:09:26.298855Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,89 +1,43 @@\n-## What\n+## STATUS: REOPENED 2026-04-26 — does not work in practice\n\n-Wrap the grid view in `<FocusZone moniker=\"ui:grid\">`, register each row and cell appropriately, and strip every legacy keyboard-nav vestige from `grid-view.tsx` and `data-table.tsx`. The 2D grid is the cleanest case for spatial nav — cells are laid out in a regular XY grid, so nearest-neighbor by rect is exactly correct.\n+The user reports that grid cells cannot be focused or selected. Registration shipped, but the visible cursor / focus indicator on a grid cell is missing or not driven by spatial focus. See umbrella card `01KQ5PEHWT...` for the systemic root-cause checklist.\n\n-### Zone shape\n+## Remaining work\n\n-```\n-ui:view (FocusZone, parent)\n-  ui:grid (FocusZone) ← THIS CARD\n-    grid_row:{i} (Leaf or Zone, depending on whether rows are themselves navigable units)\n-      grid_cell:{i,j} (Leaf, one per cell — task field display)\n-```\n+The grid view already has a `cursor` ring (the existing visual selection ring around the active cell). With spatial nav now driving focus, that cursor ring needs to derive from `focused-key` events, not from a separate `useGrid` numeric cursor. The closeout note acknowledges this is partially in place (`gridCellCursor` derives from `parseGridCellMoniker(focusedMoniker)`), but:\n\n-**Decision: rows are NOT zones, just visual groupings.** Cells are flat leaves under `ui:grid`. Spatial nav already handles row-and-column movement via beam search. Adding row-zones would force users into drill-in-then-drill-out for cross-row nav, which fights the natural grid UX.\n+1. **Verify the cursor ring tracks `focusedMoniker` end-to-end.** Click a cell → does the cursor ring move? Use ArrowLeft/Right/Up/Down — does the cursor ring follow? (Note: arrow-key spatial nav is deferred to `01KNQY1GQ9...`, so for this card focus on click-to-cursor only.)\n+2. **Verify the bridge from spatial-focus events to entity-focus.** The closeout note added an inner `<div onClick>` inside `<Focusable>` so left-click sets both spatial-focus AND entity-focus (which the cursor ring derives from). Confirm this still works in production (`bun tauri dev`) — test mocks may pass even if the production path is broken.\n+3. **Zone-level focus on `ui:grid`.** The grid is a viewport-filling zone; `showFocusBar={false}` is probably correct. Document why inline.\n\n-If we later need row-level operations (select-row), we can promote rows to zones. For now: `ui:grid` is the only zone; cells are direct leaf children.\n-\n-### Files to modify\n+## Files involved\n\n - `kanban-app/ui/src/components/grid-view.tsx`\n - `kanban-app/ui/src/components/data-table.tsx`\n+- `kanban-app/ui/src/components/focusable.tsx`\n\n-### Legacy nav to remove\n+## Acceptance Criteria\n\n-- `buildCellPredicates()` function (~60 lines) in grid-view.tsx\n-- `claimPredicates` memo that calls `buildCellPredicates` for every cell\n-- `cellMonikerMap` (only used by predicates)\n-- `claimWhen` prop on `<DataTable>` columns and threading through cell rendering\n-- `ClaimPredicate` import in both files\n-- Any `onKeyDown` handlers on grid rows / cells / outer container\n-- Any `keydown` `useEffect` listeners scoped to the grid\n-- Any roving-tabindex implementation (replaced by spatial-key tracking on each leaf)\n+- [ ] Manual smoke: clicking a grid cell moves the cursor ring to that cell\n+- [ ] Manual smoke: the cursor ring is visible on the active cell (not just `data-focused`)\n+- [ ] `ui:grid` zone with `showFocusBar={false}` has inline comment explaining viewport-size suppression\n+- [ ] Integration test: click cell → cursor ring moves\n+- [ ] Arrow-key navigation in the grid is wired (deferred — owned by `01KNQY1GQ9...` follow-up; this card's acceptance does not include it but should not block it)\n+- [ ] Existing grid tests stay green\n\n-What stays: row-virtualization scroll handlers (mouse / scroll wheel), DnD row reorder if present.\n+## Tests\n\n-### Subtasks\n-- [x] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n-- [x] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n-- [x] Delete `buildCellPredicates`\n-- [x] Delete `claimPredicates` memo and `cellMonikerMap`\n-- [x] Remove `claimWhen` threading through DataTable cell rendering\n-- [x] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n-- [x] Remove all `onKeyDown` / `keydown` handlers from both files\n-- [x] Update or remove tests that assert on `buildCellPredicates`\n-\n-## Acceptance Criteria\n-- [x] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n-- [x] Every cell registers as a Leaf with `parent_zone = ui:grid`\n-- [x] `buildCellPredicates` deleted; ~60 lines removed\n-- [x] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n-- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card) — **deferred** to follow-up `01KNQY1GQ9ABHEPE0NJW909EAQ` (arrow-key spatial nav wiring)\n-- [x] `pnpm vitest run` passes\n+- [ ] `grid-view.cursor-ring.test.tsx` — click cell → cursor ring renders on that cell\n+- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n\n-## Tests\n-- [x] `grid-view.spatial-nav.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n-- [x] `grid-spatial-nav.guards.node.test.ts` — no `claimWhen` / `cellMonikers` / `claimPredicates` props in data-table.tsx; no `onKeyDown` / `keydown` references in either file\n-- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression); plus a new click-to-cursor regression that pins the entity-focus update on left-click in the spatial path\n-- [x] `grid-view.nav-is-eventdriven.test.tsx` — existing fetch-discipline contract still passes (regression)\n-- [x] `data-table.test.tsx` — cell rendering doesn't accept legacy props; new test asserts the runtime stand-in\n-- [x] Run `cd kanban-app/ui && npx vitest run` — all 1539 pass (140 files), `tsc --noEmit` clean\n-\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\n-## Implementation Notes (2026-04-26)\n+- Use `/tdd` — write the integration test first, watch it fail, then fix.\n\n-- `grid-view.tsx` now wraps the non-empty grid body in a `<GridSpatialZone>` helper that conditionally mounts `<FocusZone moniker={asMoniker(\"ui:grid\")}>` only when both `useOptionalLayerKey()` and `useOptionalSpatialFocusActions()` resolve. Mirrors the `BoardSpatialZone` / `ViewSpatialZone` / `PerspectiveSpatialZone` pattern so existing GridView unit tests keep their narrow provider tree without forcing them to mount `<SpatialFocusProvider>` + `<FocusLayer>`.\n-- `useGridNavigation` lost ~80 lines: `buildCellPredicates`, `orthogonalNavPredicates`, `rowEdgeNavPredicates`, `gridEdgeNavPredicates`, `useCellMonikers`, `cellMonikerMap`, and the `claimPredicates` memo are all gone. Cursor derivation is now a single `resolveCursorFromFocus(focusedMoniker, columns, rowCount)` helper that parses `grid_cell:R:K` directly — no field-moniker-map lookup required. Initial focus seeds `gridCellMoniker(0, columns[0].field.name)` via `useInitialCellFocus`.\n-- `data-table.tsx` lost the `cellMonikers`, `claimPredicates`, and `useClaimNav` props/flag entirely. The `GridCellScope` (FocusScope-based) was replaced with `GridCellFocusable` which conditionally wraps cell content in `<Focusable moniker={asMoniker(gridCellMoniker(di, colKey))}>`. The conditional mirrors the GridSpatialZone pattern — when the spatial stack is absent (bare `<DataTable>` test harness), the cell renders without the `<Focusable>` wrapper to avoid `useCurrentLayerKey` throwing.\n-- New `grid-spatial-nav.guards.node.test.ts` (18 tests) pins the source-level invariants: no `ClaimPredicate` / `claimWhen` / `cellMonikers` / `claimPredicates` / `buildCellPredicates` / `cellMonikerMap` / `onKeyDown` / `keydown` tokens, plus positive assertions for `<FocusZone moniker={asMoniker(\"ui:grid\")}>`, `Focusable` import, `gridCellMoniker` import, and `asMoniker(gridCellMoniker(...))` brand application.\n-- New `grid-view.spatial-nav.test.tsx` (4 tests) mounts GridView in the production-shaped provider stack and asserts: (1) exactly one `spatial_register_zone` call with `moniker=\"ui:grid\"`, (2) the `data-moniker=\"ui:grid\"` element is in the DOM, (3) every cell registers as a `spatial_register_focusable` with the correct `grid_cell:R:K` moniker for its position, (4) every cell focusable's `parentZone` equals the grid zone's key.\n-- Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n-- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n+---\n\n-## Review Findings (2026-04-26 09:42) — RESOLVED 2026-04-26 09:55\n+(Original description and prior implementation notes preserved below for reference.)\n\n-### Warnings\n-- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n-  - **Resolution**: `GridCellFocusable` in the spatial path now mounts a thin `<div onClick onDoubleClick>` *inside* `<Focusable>` (mirroring `FocusScopeBody` in `focus-scope.tsx`). React's bubble order runs the inner wrapper's handler before `Focusable.onClick` calls `stopPropagation()`, so on a left-click both sides fire: `Focusable` updates spatial focus (Rust side) AND the inner `onClick` runs `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` so the cursor ring (derived from entity-focus) tracks click-to-move-cursor in production again. Fallback path (no spatial stack) keeps handlers on the `<TableCell>` as before. Pinned by a new regression test in `grid-view.cursor-ring.test.tsx` (\"clicking a cell sets entity-focus and lights the cursor ring on that cell\") that fails on the pre-fix code and passes on the fix.\n-- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n-  - **Resolution**: Docstring rewritten to describe the new wiring per branch (spatial path wraps a div inside `<Focusable>` so the inner handler fires before Focusable's `stopPropagation`; fallback path attaches handlers directly to `<TableCell>`). The behaviour now matches the doc — both spatial focus AND legacy `handleCellClick` fire on a left-click in production.\n+## (Prior) Implementation Notes (2026-04-26)\n\n-### Nits\n-- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n-  - **Resolution**: dropped from the return shape.\n-- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n-  - **Resolution**: removed `ci` and `isCursor` from the interface, function signature, and the `DataBodyCell` call site. `cursorRef` continues to encode \"is cursor\" via the existing `isCursor ? props.cursorRef : undefined` selector.\n-- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n-  - **Resolution**: `gridCellCursor` now derives directly from `parseGridCellMoniker`'s output (with row-range and column-existence guards inline). `derivedCursor` keeps its original column-index-based derivation since `useGrid` needs the numeric `{row, col}` shape. The two reads are independent and each does the minimum work for its consumer.\n\\ No newline at end of file\n+`grid-view.tsx` now wraps the non-empty grid body in `<GridSpatialZone>` (conditional mount). `useGridNavigation` lost ~80 lines of `buildCellPredicates` / `claimPredicates` machinery. `data-table.tsx` lost `cellMonikers`, `claimPredicates`, `useClaimNav` props. `GridCellFocusable` wraps cells in `<Focusable moniker=\"grid_cell:R:K\">`. Bridge from spatial-focus events back to entity-focus added via inner `<div onClick>` inside `<Focusable>` so left-click drives both. All 1539 tests pass; tsc clean.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,43 +1,89 @@\n-## STATUS: REOPENED 2026-04-26 — does not work in practice\n+## What\n+\n+Wrap the grid view in `<FocusZone moniker=\"ui:grid\">`, register each row and cell appropriately, and strip every legacy keyboard-nav vestige from `grid-view.tsx` and `data-table.tsx`. The 2D grid is the cleanest case for spatial nav — cells are laid out in a regular XY grid, so nearest-neighbor by rect is exactly correct.\n\n-The user reports that grid cells cannot be focused or selected. Registration shipped, but the visible cursor / focus indicator on a grid cell is missing or not driven by spatial focus. See umbrella card `01KQ5PEHWT...` for the systemic root-cause checklist.\n+### Zone shape\n\n-## Remaining work\n+```\n+ui:view (FocusZone, parent)\n+  ui:grid (FocusZone) ← THIS CARD\n+    grid_row:{i} (Leaf or Zone, depending on whether rows are themselves navigable units)\n+      grid_cell:{i,j} (Leaf, one per cell — task field display)\n+```\n\n-The grid view already has a `cursor` ring (the existing visual selection ring around the active cell). With spatial nav now driving focus, that cursor ring needs to derive from `focused-key` events, not from a separate `useGrid` numeric cursor. The closeout note acknowledges this is partially in place (`gridCellCursor` derives from `parseGridCellMoniker(focusedMoniker)`), but:\n+**Decision: rows are NOT zones, just visual groupings.** Cells are flat leaves under `ui:grid`. Spatial nav already handles row-and-column movement via beam search. Adding row-zones would force users into drill-in-then-drill-out for cross-row nav, which fights the natural grid UX.\n\n-1. **Verify the cursor ring tracks `focusedMoniker` end-to-end.** Click a cell → does the cursor ring move? Use ArrowLeft/Right/Up/Down — does the cursor ring follow? (Note: arrow-key spatial nav is deferred to `01KNQY1GQ9...`, so for this card focus on click-to-cursor only.)\n-2. **Verify the bridge from spatial-focus events to entity-focus.** The closeout note added an inner `<div onClick>` inside `<Focusable>` so left-click sets both spatial-focus AND entity-focus (which the cursor ring derives from). Confirm this still works in production (`bun tauri dev`) — test mocks may pass even if the production path is broken.\n-3. **Zone-level focus on `ui:grid`.** The grid is a viewport-filling zone; `showFocusBar={false}` is probably correct. Document why inline.\n+If we later need row-level operations (select-row), we can promote rows to zones. For now: `ui:grid` is the only zone; cells are direct leaf children.\n\n-## Files involved\n+### Files to modify\n\n - `kanban-app/ui/src/components/grid-view.tsx`\n - `kanban-app/ui/src/components/data-table.tsx`\n-- `kanban-app/ui/src/components/focusable.tsx`\n\n+### Legacy nav to remove\n+\n+- `buildCellPredicates()` function (~60 lines) in grid-view.tsx\n+- `claimPredicates` memo that calls `buildCellPredicates` for every cell\n+- `cellMonikerMap` (only used by predicates)\n+- `claimWhen` prop on `<DataTable>` columns and threading through cell rendering\n+- `ClaimPredicate` import in both files\n+- Any `onKeyDown` handlers on grid rows / cells / outer container\n+- Any `keydown` `useEffect` listeners scoped to the grid\n+- Any roving-tabindex implementation (replaced by spatial-key tracking on each leaf)\n+\n+What stays: row-virtualization scroll handlers (mouse / scroll wheel), DnD row reorder if present.\n+\n+### Subtasks\n+- [x] Wrap grid container in `<FocusZone moniker={Moniker(\"ui:grid\")}>`\n+- [x] Each cell becomes a `<Focusable moniker={Moniker(`grid_cell:${rowIndex}:${colKey}`)}>` leaf with `parent_zone = ui:grid`\n+- [x] Delete `buildCellPredicates`\n+- [x] Delete `claimPredicates` memo and `cellMonikerMap`\n+- [x] Remove `claimWhen` threading through DataTable cell rendering\n+- [x] Remove `ClaimPredicate` imports in grid-view.tsx and data-table.tsx\n+- [x] Remove all `onKeyDown` / `keydown` handlers from both files\n+- [x] Update or remove tests that assert on `buildCellPredicates`\n+\n ## Acceptance Criteria\n+- [x] Grid view registers exactly one Zone (`ui:grid`) at its root, with `parent_zone = ui:view`\n+- [x] Every cell registers as a Leaf with `parent_zone = ui:grid`\n+- [x] `buildCellPredicates` deleted; ~60 lines removed\n+- [x] No `claimWhen` / `ClaimPredicate` / `onKeyDown` / `keydown` references in grid-view.tsx or data-table.tsx\n+- [ ] Grid cell navigation works identically via spatial nav: up/down/left/right move between cells; `nav.first` / `nav.last` go to top-left / bottom-right; `nav.rowStart` / `nav.rowEnd` operate on the cell's row (via the same-row filter in the algorithm card) — **deferred** to follow-up `01KNQY1GQ9ABHEPE0NJW909EAQ` (arrow-key spatial nav wiring)\n+- [x] `pnpm vitest run` passes\n\n-- [ ] Manual smoke: clicking a grid cell moves the cursor ring to that cell\n-- [ ] Manual smoke: the cursor ring is visible on the active cell (not just `data-focused`)\n-- [ ] `ui:grid` zone with `showFocusBar={false}` has inline comment explaining viewport-size suppression\n-- [ ] Integration test: click cell → cursor ring moves\n-- [ ] Arrow-key navigation in the grid is wired (deferred — owned by `01KNQY1GQ9...` follow-up; this card's acceptance does not include it but should not block it)\n-- [ ] Existing grid tests stay green\n-\n ## Tests\n+- [x] `grid-view.spatial-nav.test.tsx` — grid container is a Zone; cells are leaves with the grid zone as parent_zone\n+- [x] `grid-spatial-nav.guards.node.test.ts` — no `claimWhen` / `cellMonikers` / `claimPredicates` props in data-table.tsx; no `onKeyDown` / `keydown` references in either file\n+- [x] `grid-view.cursor-ring.test.tsx` — existing cursor-ring suppression still passes (regression); plus a new click-to-cursor regression that pins the entity-focus update on left-click in the spatial path\n+- [x] `grid-view.nav-is-eventdriven.test.tsx` — existing fetch-discipline contract still passes (regression)\n+- [x] `data-table.test.tsx` — cell rendering doesn't accept legacy props; new test asserts the runtime stand-in\n+- [x] Run `cd kanban-app/ui && npx vitest run` — all 1539 pass (140 files), `tsc --noEmit` clean\n\n-- [ ] `grid-view.cursor-ring.test.tsx` — click cell → cursor ring renders on that cell\n-- [ ] Run `cd kanban-app/ui && npx vitest run` — all pass\n-\n ## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\n-- Use `/tdd` — write the integration test first, watch it fail, then fix.\n+## Implementation Notes (2026-04-26)\n\n----\n+- `grid-view.tsx` now wraps the non-empty grid body in a `<GridSpatialZone>` helper that conditionally mounts `<FocusZone moniker={asMoniker(\"ui:grid\")}>` only when both `useOptionalLayerKey()` and `useOptionalSpatialFocusActions()` resolve. Mirrors the `BoardSpatialZone` / `ViewSpatialZone` / `PerspectiveSpatialZone` pattern so existing GridView unit tests keep their narrow provider tree without forcing them to mount `<SpatialFocusProvider>` + `<FocusLayer>`.\n+- `useGridNavigation` lost ~80 lines: `buildCellPredicates`, `orthogonalNavPredicates`, `rowEdgeNavPredicates`, `gridEdgeNavPredicates`, `useCellMonikers`, `cellMonikerMap`, and the `claimPredicates` memo are all gone. Cursor derivation is now a single `resolveCursorFromFocus(focusedMoniker, columns, rowCount)` helper that parses `grid_cell:R:K` directly — no field-moniker-map lookup required. Initial focus seeds `gridCellMoniker(0, columns[0].field.name)` via `useInitialCellFocus`.\n+- `data-table.tsx` lost the `cellMonikers`, `claimPredicates`, and `useClaimNav` props/flag entirely. The `GridCellScope` (FocusScope-based) was replaced with `GridCellFocusable` which conditionally wraps cell content in `<Focusable moniker={asMoniker(gridCellMoniker(di, colKey))}>`. The conditional mirrors the GridSpatialZone pattern — when the spatial stack is absent (bare `<DataTable>` test harness), the cell renders without the `<Focusable>` wrapper to avoid `useCurrentLayerKey` throwing.\n+- New `grid-spatial-nav.guards.node.test.ts` (18 tests) pins the source-level invariants: no `ClaimPredicate` / `claimWhen` / `cellMonikers` / `claimPredicates` / `buildCellPredicates` / `cellMonikerMap` / `onKeyDown` / `keydown` tokens, plus positive assertions for `<FocusZone moniker={asMoniker(\"ui:grid\")}>`, `Focusable` import, `gridCellMoniker` import, and `asMoniker(gridCellMoniker(...))` brand application.\n+- New `grid-view.spatial-nav.test.tsx` (4 tests) mounts GridView in the production-shaped provider stack and asserts: (1) exactly one `spatial_register_zone` call with `moniker=\"ui:grid\"`, (2) the `data-moniker=\"ui:grid\"` element is in the DOM, (3) every cell registers as a `spatial_register_focusable` with the correct `grid_cell:R:K` moniker for its position, (4) every cell focusable's `parentZone` equals the grid zone's key.\n+- Updated `grid-view.nav-is-eventdriven.test.tsx`'s `DataTable` mock to drop the `cellMonikers` prop access (the prop no longer exists). Added a runtime stand-in test in `data-table.test.tsx` that asserts the table mounts without the legacy props.\n+- The arrow-key spatial nav wiring acceptance criterion is **deferred** to follow-up task `01KNQY1GQ9ABHEPE0NJW909EAQ` — that card pins the keymap (ArrowUp/Down/Left/Right → spatial nav) once all zones / leaves are registered. This card established the registration contract; the keymap binds against it next.\n\n-(Original description and prior implementation notes preserved below for reference.)\n+## Review Findings (2026-04-26 09:42) — RESOLVED 2026-04-26 09:55\n\n-## (Prior) Implementation Notes (2026-04-26)\n+### Warnings\n+- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable`) — In production with the spatial stack mounted, `<Focusable>`'s `onClick` calls `e.stopPropagation()` (per the long-standing FocusScope convention). The result: the surrounding `<TableCell onClick={onClick}>` handler — which calls `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` via entity-focus — does NOT fire in the spatial path. So clicking a cell now updates only the spatial-focus key (Rust side); the entity-focus moniker (`focusedMoniker`) the cursor ring derives from is not touched. Behavior delta vs the pre-change code where click moved the cursor. Acceptable as a transition state (this card defers arrow-key nav to `01KNQY1GQ9...`), but should be tracked. Fix path: bridge `focus-changed` Tauri events back into entity-focus (resolve `next_key` → moniker, call `setFocus`), or merge `handleCellClick` into the `<Focusable>` click chain via a new prop. Either way, the legacy entity-focus side needs an event-driven update once spatial focus is the canonical click target.\n+  - **Resolution**: `GridCellFocusable` in the spatial path now mounts a thin `<div onClick onDoubleClick>` *inside* `<Focusable>` (mirroring `FocusScopeBody` in `focus-scope.tsx`). React's bubble order runs the inner wrapper's handler before `Focusable.onClick` calls `stopPropagation()`, so on a left-click both sides fire: `Focusable` updates spatial focus (Rust side) AND the inner `onClick` runs `handleCellClick(di, ci)` → `setFocus(gridCellMoniker(...))` so the cursor ring (derived from entity-focus) tracks click-to-move-cursor in production again. Fallback path (no spatial stack) keeps handlers on the `<TableCell>` as before. Pinned by a new regression test in `grid-view.cursor-ring.test.tsx` (\"clicking a cell sets entity-focus and lights the cursor ring on that cell\") that fails on the pre-fix code and passes on the fix.\n+- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusable` docstring, \"We attach our own `onClick` / `onDoubleClick` to the surrounding `<TableCell>` so left-click drives the legacy grid-cursor `handleCellClick` callback (in addition to the spatial focus call)\") — Docstring claims left-click drives both spatial focus and the legacy `handleCellClick`. As shown above, `Focusable.onClick`'s `stopPropagation()` shadows the TableCell's onClick handler in the spatial path, so `handleCellClick` does NOT run on left-click in production. Either fix the comment to describe actual behavior (spatial-focus only on click; double-click still enters edit mode) or change the wiring so both handlers actually fire.\n+  - **Resolution**: Docstring rewritten to describe the new wiring per branch (spatial path wraps a div inside `<Focusable>` so the inner handler fires before Focusable's `stopPropagation`; fallback path attaches handlers directly to `<TableCell>`). The behaviour now matches the doc — both spatial focus AND legacy `handleCellClick` fire on a left-click in production.\n\n-`grid-view.tsx` now wraps the non-empty grid body in `<GridSpatialZone>` (conditional mount). `useGridNavigation` lost ~80 lines of `buildCellPredicates` / `claimPredicates` machinery. `data-table.tsx` lost `cellMonikers`, `claimPredicates`, `useClaimNav` props. `GridCellFocusable` wraps cells in `<Focusable moniker=\"grid_cell:R:K\">`. Bridge from spatial-focus events back to entity-focus added via inner `<div onClick>` inside `<Focusable>` so left-click drives both. All 1539 tests pass; tsc clean.\n\\ No newline at end of file\n+### Nits\n+- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation` return value) — `hasFocusedMoniker: focusedMoniker !== null` is returned but never consumed anywhere in the codebase (verified: zero matches outside the producer). Dead code; drop it from the return shape.\n+  - **Resolution**: dropped from the return shape.\n+- [x] `kanban-app/ui/src/components/data-table.tsx` (`GridCellFocusableProps`) — Interface declares `ci` and `isCursor` props but the function body renames them to `_ci` / `_isCursor` (unused). Remove both from the interface and from the call site in `DataBodyCell` (the `ci={ci}` and `isCursor={isCursor}` JSX attributes); `cursorRef` already encodes \"is cursor\" via the `isCursor ? props.cursorRef : undefined` selector at the call site.\n+  - **Resolution**: removed `ci` and `isCursor` from the interface, function signature, and the `DataBodyCell` call site. `cursorRef` continues to encode \"is cursor\" via the existing `isCursor ? props.cursorRef : undefined` selector.\n+- [x] `kanban-app/ui/src/components/grid-view.tsx` (`useGridNavigation`) — Both `derivedCursor: {row, col}` and `gridCellCursor: {row, colKey}` are derived from the same `focusedMoniker`. `parseGridCellMoniker` already returns `{row, colKey}`; the current code goes `moniker → {row, col} (via columns.findIndex) → {row, colKey} (via columns[col].field.name)`. Consider deriving `gridCellCursor` directly from `parseGridCellMoniker`'s output (and keeping `derivedCursor` only for `useGrid`'s numeric-cursor input). Minor — single render path, but removes a redundant column-index round-trip.\n+  - **Resolution**: `gridCellCursor` now derives directly from `parseGridCellMoniker`'s output (with row-range and column-existence guards inline). `derivedCursor` keeps its original column-index-based derivation since `useGrid` needs the numeric `{row, col}` shape. The two reads are independent and each does the minimum work for its consumer.\n\\ No newline at end of file\n"}]]}
{"id":"01KQ5PYTQNSQXC8DZDWK5BCRDZ","timestamp":"2026-04-26T20:16:09.973439Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -3,6 +3,7 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n+- 01KQ5PP55SAAVJ0V3HDJ1DGNBY\n position_column: doing\n position_ordinal: '8880'\n project: spatial-nav\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,6 @@\n - claude-code\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n-- 01KQ5PP55SAAVJ0V3HDJ1DGNBY\n position_column: doing\n position_ordinal: '8880'\n project: spatial-nav\n","transaction_id":null}
{"id":"01KQ5PYTQN3QRF8PG0BFN6SBN3","timestamp":"2026-04-26T20:16:09.973821Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["depends_on",{"kind":"changed","old_value":["01KNQXYC4RBQP1N2NQ33P8DPB9"],"new_value":["01KNQXYC4RBQP1N2NQ33P8DPB9","01KQ5PP55SAAVJ0V3HDJ1DGNBY"]}]]}
{"id":"01KQ5QBYC0YH2WDZ0DNR2W09QB","timestamp":"2026-04-26T20:23:19.680723Z","op":"update","item_id":"01KNQXZZ9VQBHFX091P0K4F4YC","forward_patch":"--- original\n+++ modified\n@@ -4,6 +4,7 @@\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n - 01KQ5PP55SAAVJ0V3HDJ1DGNBY\n+- 01KQ5QB6F4MTD35GBTARJH4JEW\n position_column: doing\n position_ordinal: '8880'\n project: spatial-nav\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,6 @@\n depends_on:\n - 01KNQXYC4RBQP1N2NQ33P8DPB9\n - 01KQ5PP55SAAVJ0V3HDJ1DGNBY\n-- 01KQ5QB6F4MTD35GBTARJH4JEW\n position_column: doing\n position_ordinal: '8880'\n project: spatial-nav\n","transaction_id":null}
{"id":"01KQ5QBYC1A2WXD53E3Z2F5VPT","timestamp":"2026-04-26T20:23:19.681348Z","entity_type":"task","entity_id":"01KNQXZZ9VQBHFX091P0K4F4YC","op":"update","changes":[["depends_on",{"kind":"changed","old_value":["01KNQXYC4RBQP1N2NQ33P8DPB9","01KQ5PP55SAAVJ0V3HDJ1DGNBY"],"new_value":["01KNQXYC4RBQP1N2NQ33P8DPB9","01KQ5PP55SAAVJ0V3HDJ1DGNBY","01KQ5QB6F4MTD35GBTARJH4JEW"]}]]}

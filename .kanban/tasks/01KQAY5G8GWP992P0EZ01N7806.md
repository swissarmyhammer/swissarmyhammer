---
assignees:
- claude-code
position_column: done
position_ordinal: ffffffffffffffffffffffffffffffffef80
project: spatial-nav
title: Trace overlap warnings in SpatialRegistry — flag same-kind entries that share (x, y) as needless-nesting candidates
---
## What

When two `FocusZone`s register at the same `(x, y)` in the same layer — or two `FocusScope`s do — emit `tracing::warn!` from the kernel registry. This is almost always **needless nesting**: a parent zone that wraps a single child with no offset, two siblings stacked at the same anchor due to a pass-through wrapper, etc. The warning gives us a hunting tool to find and remove these in the React tree without staring at every component file.

Same-kind only: zone-overlapping-zone, scope-overlapping-scope. A scope at the same `(x, y)` as its parent zone is normal (a leaf as the sole child of an unpadded zone), so cross-kind overlaps are NOT warned. The user can refine the rule after seeing the noise level if needed.

## Where the bug lives

The registry inserts entries via `SpatialRegistry::register_scope` (`registry.rs:91`) and `register_zone` (`registry.rs:101`). Rect updates happen through `update_rect` (`registry.rs:129`). All three are mutation points where a same-(x, y) overlap can come into being.

## Approach

`swissarmyhammer-focus/src/registry.rs`:

1. Add a private helper `check_overlap_warning(&self, kind: ScopeKind, layer: &LayerKey, rect: &Rect, exclude: &SpatialKey)` that scans `self.entries_in_layer(layer)`, filtering to same-kind entries excluding `exclude`, and returns the first match where the entry's rounded `(left, top)` equals the input's rounded `(left, top)`.
2. Round to integer pixels for the comparison — `rect.left().round() as i64`, `rect.top().round() as i64`. Subpixel rendering produces tiny variations that aren't user-relevant; rounding catches the structural overlaps that matter.
3. Call the helper from `register_scope`, `register_zone`, and `update_rect` after the mutation has applied. When a match is found, emit:

```rust
tracing::warn!(
    op = "register_scope" | "register_zone" | "update_rect",
    layer = %layer_key,
    new_key = %new.key(),
    new_moniker = %new.moniker(),
    overlap_key = %existing.key(),
    overlap_moniker = %existing.moniker(),
    x = rounded_x,
    y = rounded_y,
    "two same-kind entries share (x, y); likely needless nesting — review React tree for redundant wrappers"
);
```

4. Skip the check when the registry has fewer than two entries (cold start; nothing to overlap with).

5. Decide whether to suppress the warning during `update_rect` if the moved entry was already overlapping the same other entry pre-update. Probably YES — `update_rect` fires every animation frame during `01KQ9XBAG5P9W3JREQYNGAYM8Y`'s scroll-tracking, and re-warning every frame would flood the log. Track a per-key "last overlap warn" target and skip re-warning if the (key, overlap_key) pair is the same as last time. Reset on `unregister_scope`.

## What this catches

Concrete cases this trace surfaces in production today (the user can hunt them down once it's wired):

- A `<FocusZone>` whose only child is a `<FocusScope>` with no DOM offset between them — the zone adds no spatial structure.
- An `<Inspectable>` wrap that introduces a sibling `<FocusZone>` and `<FocusScope>` with the same moniker at the same position — that's two same-kind entries in some shapes.
- An accidental double-mount of the same component (e.g., a list re-render that mounts both old and new keys briefly) — the warning fires once during the transition.

## Scope NOT in this ticket

- The overlap check does NOT remove anything. It only warns. Removing the actual nesting is a follow-up driven by the warnings the user collects.
- The check does NOT consider full rect overlap (matching width/height too). Same-`(x, y)` is the strong signal; full overlap is a stricter check that may follow.
- Cross-layer overlaps are out of scope — different layers serve different surfaces, and overlap there is structurally meaningful, not a smell.

## Acceptance Criteria

All asserted by automated tests below — no manual smoke step.

- [x] Registering two zones at the same rounded `(x, y)` in the same layer emits exactly one `WARN`-level tracing event with `op = "register_zone"`, both monikers, both keys, and the rounded coordinates.
- [x] Registering two scopes at the same rounded `(x, y)` in the same layer emits exactly one `WARN` with `op = "register_scope"`.
- [x] Registering a scope and a zone at the same `(x, y)` does NOT emit a warning. (Cross-kind is allowed.)
- [x] Registering a zone and a scope at the same `(x, y)` does NOT emit a warning. (Same as above, both directions.)
- [x] Registering two same-kind entries at different `(x, y)` (off by ≥ 1 pixel after rounding) does NOT emit a warning.
- [x] Registering two same-kind entries at the same `(x, y)` but in **different layers** does NOT emit a warning.
- [x] `update_rect` that moves an entry onto an existing same-kind entry at `(x, y)` emits exactly one `WARN`.
- [x] `update_rect` called repeatedly with the same coordinates while an overlap remains does NOT re-emit the warning every frame. (Per-key suppression for the same overlap pair.)
- [x] `update_rect` that moves an entry **off** an existing overlap and back ON does emit a fresh warning the second time. (The suppression releases when the overlap clears.)
- [x] `unregister_scope` clears any per-key suppression state for that key.
- [x] No regression: existing `register_scope` / `register_zone` / `update_rect` tests pass.

## Tests

All tests are automated. No manual verification.

### Rust kernel — `swissarmyhammer-focus/tests/overlap_tracing.rs` (new file)

Uses a tracing capture (e.g. `tracing-subscriber::fmt::TestWriter` or a custom `Layer` collecting events into a `Vec`) to assert which paths emit the warning.

- [x] `two_zones_same_xy_same_layer_warns_once` — register zone A at `(100, 80)`, register zone B at `(100, 80)` in same layer; assert exactly one `WARN` with `op = "register_zone"`, both monikers, both keys, `x = 100`, `y = 80`.
- [x] `two_scopes_same_xy_same_layer_warns_once` — same with two scopes; assert `op = "register_scope"`.
- [x] `zone_and_scope_same_xy_does_not_warn` — register zone at `(100, 80)`, scope at `(100, 80)` same layer; zero warnings.
- [x] `two_zones_different_xy_does_not_warn` — register zone A at `(100, 80)`, zone B at `(120, 80)` (1+ pixel off); zero warnings.
- [x] `two_zones_subpixel_difference_does_not_warn` — register zone A at `(100.0, 80.0)`, zone B at `(100.3, 80.2)`; both round to `(100, 80)` — wait, that DOES match. Test the other direction: `(100.6, 80.0)` rounds to `(101, 80)`; assert zero warnings. (Rounding is the cutoff.)
- [x] `two_zones_same_xy_different_layers_does_not_warn` — register zone A in layer L1, zone B at same `(x, y)` in layer L2; zero warnings.
- [x] `update_rect_creates_overlap_warns_once` — register A at `(100, 80)`, B at `(200, 80)`; `update_rect(B, (100, 80))`; assert one `WARN`.
- [x] `update_rect_repeated_with_same_overlap_does_not_re_warn` — after the previous test setup, call `update_rect(B, (100, 80))` ten more times; assert still exactly one total `WARN` (suppressed thereafter).
- [x] `update_rect_clears_overlap_then_re_creates_warns_again` — overlap → move B away → move B back; assert two total `WARN`s (suppression released when overlap cleared).
- [x] `unregister_scope_resets_suppression_for_key` — overlap → unregister B → re-register B at the same overlapping position; assert two total `WARN`s.

Test command: `cargo test -p swissarmyhammer-focus --test overlap_tracing` — all ten pass.

### Rust kernel — confirm regressions

- [x] `cargo test -p swissarmyhammer-focus` — full crate test suite passes after the migration.

## Workflow

- Use `/tdd` — write the kernel-level overlap-tracing tests first, watch them fail, add the helper + tracing, watch them pass.
- Single ticket — one focused observability change at the kernel registry, one tracing capture-assertion test file.
- Coordinates with `01KQAW97R9XTCNR1PJAWYSKBC7` (silent-None tracing) and `01KQ9ZAMCTVG8KJ4MT9YC38WW3` (debug overlay). All three additions improve developer observability of the spatial graph; this one specifically targets needless-nesting detection.
- After this lands, the user runs `tauri dev`, opens the inspector / board / wherever they're hunting duplicates, and reads the `WARN` events to find candidates. Each warning carries both monikers and the position — easy to grep for in source.

## FQM Refactor Notice (added 2026-04-29)

Coordinate with `01KQD6064G1C1RAXDFPJVT1F46` (path-monikers as spatial keys) before driving this task. Specific updates needed under the new contract:

- The `check_overlap_warning(&self, kind: ScopeKind, layer: &LayerKey, rect: &Rect, exclude: &SpatialKey)` helper signature uses `&SpatialKey` — but `SpatialKey` is being deleted in the FQM refactor. Replace with `&FullyQualifiedMoniker` (or just take an FQM directly, since the FQM IS the key under the new contract).
- The `WARN` payload's `new_key` and `overlap_key` fields become `new_fq` and `overlap_fq` (or just `new_moniker` / `overlap_moniker` since under FQM there's only one identifier per entry).
- Test fixtures that today create entries via `Moniker::from_string("...")` and a separate UUID `SpatialKey` should switch to constructing the FQM directly.
- `entries_in_layer(layer)` — `layer` becomes `&FullyQualifiedMoniker` (the layer's FQM) rather than a UUID `LayerKey`.

If this task is implemented BEFORE the FQM refactor lands, use today's types and expect a mechanical rename when the refactor lands. If after, use FQMs from the start.

## Implementation Notes

Implemented under the FQM-refactor world (FQM IS the spatial key). The helper takes the entry's own FQM as the "exclude" so it doesn't match itself in `entries_in_layer`. The WARN payload uses `new_fq` / `overlap_fq` (single-identifier-per-entry under FQM) plus `new_segment` / `overlap_segment` for human-readable log inspection. The suppression slot is keyed by FQM in a new `overlap_warn_partner: HashMap<FullyQualifiedMoniker, FullyQualifiedMoniker>` map on `SpatialRegistry`.

Files changed:
- `swissarmyhammer-focus/src/registry.rs` — added `rounded_pixel`, `same_rounded_origin`, `OverlapWarn`, `warn_overlap`, `SpatialRegistry::check_overlap_warning`; integrated into `register_scope`, `register_zone`, `update_rect`; cleared from `unregister_scope`.
- `swissarmyhammer-focus/tests/overlap_tracing.rs` — 11 new tests (10 from spec + symmetric scope-then-zone case for the cross-kind contract).

`cargo test -p swissarmyhammer-focus` and `cargo clippy -p swissarmyhammer-focus --all-targets -- -D warnings` both pass clean. `cargo build --workspace` succeeds.
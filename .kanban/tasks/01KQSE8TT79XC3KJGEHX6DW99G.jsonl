{"id":"01KQSE8TT79MP5XYCSNF906NFS","timestamp":"2026-05-04T12:09:09.191931Z","op":"create","item_id":"01KQSE8TT79XC3KJGEHX6DW99G","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,63 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: cf80\n+project: spatial-nav\n+title: Resizable inspector — drag the left edge to change width (persisted per window)\n+---\n+## What\n+\n+Inspector panels are currently fixed at 420 px wide. Make the left edge draggable so the user can resize the inspector, with the chosen width persisted per-window through the existing `WindowState` UI-state pipeline.\n+\n+### Current shape\n+\n+- `kanban-app/ui/src/components/slide-panel.tsx` — fixed `w-[420px] max-w-[85vw]` Tailwind class on the outer `<div>`. Renders the close button + scroll body. Knows nothing about its position in the stack.\n+- `kanban-app/ui/src/components/inspectors-container.tsx`\n+  - `const PANEL_WIDTH = 420;` (module constant)\n+  - `const rightOffset = (panelStack.length - 1 - index) * PANEL_WIDTH;` — each panel in the stack is offset by one panel width so adjacent panels tile horizontally.\n+- `swissarmyhammer-commands/src/ui_state.rs::WindowState` — per-window persisted state. Already carries `inspector_stack`, `active_view_id`, `x/y/width/height`, `maximized`. Mutators broadcast a `UIStateChange::*` variant the frontend listens for.\n+\n+### Decisions (assume these unless changed)\n+\n+- **Single width per window**, applied to every panel in the stack. Adjacent panels must keep tiling — they share the same width so the right-edge offset math stays valid.\n+- **Per-window persistence** via `WindowState.inspector_width: Option<u32>`. `None` falls back to the current 420 px default.\n+- **Bounds**: clamp to `[320, min(800, 0.85 * window.innerWidth)]`. The lower bound keeps the form readable; the upper bound preserves the existing `max-w-[85vw]` intent.\n+- **Drag handle**: a 6 px-wide invisible hit zone on the panel's left edge with `cursor-col-resize`, becoming visibly subtle on hover (the same muted-foreground stroke used by the column-resize handle in `data-table.tsx`, if applicable — otherwise a hairline `bg-border` indicator).\n+- **Persistence cadence**: update local React state on every mousemove (60 fps drag), but only dispatch the backend `ui.inspector.set_width` command on mouseup, mirroring the column-resize / window-geometry pattern.\n+\n+### Files to change\n+\n+1. `kanban-app/ui/src/components/slide-panel.tsx` — add an optional `width` prop and a left-edge drag handle. Replace `w-[420px]` with an inline `style={{ width }}` (still capped via CSS `maxWidth: '85vw'`). Drag emits `onResize(nextWidth)` continuously and `onResizeEnd(finalWidth)` once.\n+2. `kanban-app/ui/src/components/inspectors-container.tsx` — read `inspector_width` from `useUIState()` (with the `?? 420` fallback), pass it to every `<InspectorPanel>`, and use it in the `rightOffset` calculation. Wire `onResizeEnd` to a new `useDispatchCommand(\"ui.inspector.set_width\")` callback. The transient drag state stays in React; only the final value round-trips through the backend.\n+3. `swissarmyhammer-commands/src/ui_state.rs` —\n+   - Add `inspector_width: Option<u32>` to `WindowState` (with `Default` returning `None`).\n+   - Add `set_inspector_width(&self, window_label: &str, width: u32) -> Option<UIStateChange>` and a getter `inspector_width(&self, window_label: &str) -> Option<u32>`.\n+   - Add a `UIStateChange::InspectorWidth { window_label, width: u32 }` variant; update the `ui_state_change_kind` mapping.\n+4. `swissarmyhammer-kanban/src/commands/ui_commands.rs` (or wherever `ui.inspector.open/close` live, mirror that file) — add `ui.inspector.set_width` command that calls `set_inspector_width` and broadcasts the change.\n+5. `kanban-app/src/commands.rs` — register the new variant in `ui_state_change_kind` (look for the existing `InspectorStack` branch around line 1870) so the React side hears `inspector_width` change events.\n+\n+### Type plumbing\n+\n+- Frontend: `UIWindowState` type (in `kanban-app/ui/src/lib/ui-state-context.tsx` or `types/kanban.ts` — find via grep) gets an optional `inspector_width?: number`. The `useUIState` selector exposes it; `InspectorsContainer` reads it as `winState?.inspector_width ?? 420`.\n+\n+## Acceptance Criteria\n+\n+- [ ] Hovering the left edge of any inspector shows `cursor-col-resize`.\n+- [ ] Mousedown + drag on the left edge changes the inspector width in real time, clamped to `[320, min(800, 0.85 * viewport)]`.\n+- [ ] When two panels are stacked, dragging either one's left edge resizes both (single shared width per window) and they remain tiled — no overlap, no gap.\n+- [ ] After a resize, the new width is dispatched once via `ui.inspector.set_width` on mouseup. Reloading the window restores the saved width.\n+- [ ] When `WindowState.inspector_width` is `None`, the panel renders at the existing 420 px default.\n+- [ ] Existing inspector snapshot/spatial-nav tests still pass — the resize handle is a visual+pointer addition only, no change to focus behavior or `<FocusZone>` structure.\n+\n+## Tests\n+\n+- [ ] **Drag interaction (browser test)** in a new `kanban-app/ui/src/components/inspector-resize.browser.test.tsx`: mount `<InspectorsContainer>` with one panel open, simulate `mousedown` on the left-edge handle, `mousemove` shifting -120 px (wider) and `mouseup`. Assert (a) the panel's `style.width` after mousemove is the new value, (b) `ui.inspector.set_width` was dispatched once on mouseup with the final value.\n+- [ ] **Clamp test (unit)** in `slide-panel.test.tsx` (create if absent): a drag that would compute < 320 px is clamped to 320 px; a drag that would exceed `min(800, 0.85*viewport)` is clamped at the upper bound.\n+- [ ] **Stack offset test** in `kanban-app/ui/src/components/inspectors-container.test.tsx`: when two panels are open and `inspector_width` is 600, panel 0 has `right: 600` and panel 1 has `right: 0` (tiled with the new width, not 420).\n+- [ ] **Backend round-trip** in `swissarmyhammer-commands/src/ui_state.rs` (next to the existing `set_inspector_stack_restores` test): `set_inspector_width(\"main\", 540)` then `inspector_width(\"main\")` returns `Some(540)`; serialize to YAML and reload, value survives.\n+- [ ] Run `pnpm -C kanban-app/ui test inspector-resize slide-panel inspectors-container` and `cargo test -p swissarmyhammer-commands ui_state` — all green.\n+\n+## Workflow\n+\n+- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,63 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: cf80\n-project: spatial-nav\n-title: Resizable inspector — drag the left edge to change width (persisted per window)\n----\n-## What\n-\n-Inspector panels are currently fixed at 420 px wide. Make the left edge draggable so the user can resize the inspector, with the chosen width persisted per-window through the existing `WindowState` UI-state pipeline.\n-\n-### Current shape\n-\n-- `kanban-app/ui/src/components/slide-panel.tsx` — fixed `w-[420px] max-w-[85vw]` Tailwind class on the outer `<div>`. Renders the close button + scroll body. Knows nothing about its position in the stack.\n-- `kanban-app/ui/src/components/inspectors-container.tsx`\n-  - `const PANEL_WIDTH = 420;` (module constant)\n-  - `const rightOffset = (panelStack.length - 1 - index) * PANEL_WIDTH;` — each panel in the stack is offset by one panel width so adjacent panels tile horizontally.\n-- `swissarmyhammer-commands/src/ui_state.rs::WindowState` — per-window persisted state. Already carries `inspector_stack`, `active_view_id`, `x/y/width/height`, `maximized`. Mutators broadcast a `UIStateChange::*` variant the frontend listens for.\n-\n-### Decisions (assume these unless changed)\n-\n-- **Single width per window**, applied to every panel in the stack. Adjacent panels must keep tiling — they share the same width so the right-edge offset math stays valid.\n-- **Per-window persistence** via `WindowState.inspector_width: Option<u32>`. `None` falls back to the current 420 px default.\n-- **Bounds**: clamp to `[320, min(800, 0.85 * window.innerWidth)]`. The lower bound keeps the form readable; the upper bound preserves the existing `max-w-[85vw]` intent.\n-- **Drag handle**: a 6 px-wide invisible hit zone on the panel's left edge with `cursor-col-resize`, becoming visibly subtle on hover (the same muted-foreground stroke used by the column-resize handle in `data-table.tsx`, if applicable — otherwise a hairline `bg-border` indicator).\n-- **Persistence cadence**: update local React state on every mousemove (60 fps drag), but only dispatch the backend `ui.inspector.set_width` command on mouseup, mirroring the column-resize / window-geometry pattern.\n-\n-### Files to change\n-\n-1. `kanban-app/ui/src/components/slide-panel.tsx` — add an optional `width` prop and a left-edge drag handle. Replace `w-[420px]` with an inline `style={{ width }}` (still capped via CSS `maxWidth: '85vw'`). Drag emits `onResize(nextWidth)` continuously and `onResizeEnd(finalWidth)` once.\n-2. `kanban-app/ui/src/components/inspectors-container.tsx` — read `inspector_width` from `useUIState()` (with the `?? 420` fallback), pass it to every `<InspectorPanel>`, and use it in the `rightOffset` calculation. Wire `onResizeEnd` to a new `useDispatchCommand(\"ui.inspector.set_width\")` callback. The transient drag state stays in React; only the final value round-trips through the backend.\n-3. `swissarmyhammer-commands/src/ui_state.rs` —\n-   - Add `inspector_width: Option<u32>` to `WindowState` (with `Default` returning `None`).\n-   - Add `set_inspector_width(&self, window_label: &str, width: u32) -> Option<UIStateChange>` and a getter `inspector_width(&self, window_label: &str) -> Option<u32>`.\n-   - Add a `UIStateChange::InspectorWidth { window_label, width: u32 }` variant; update the `ui_state_change_kind` mapping.\n-4. `swissarmyhammer-kanban/src/commands/ui_commands.rs` (or wherever `ui.inspector.open/close` live, mirror that file) — add `ui.inspector.set_width` command that calls `set_inspector_width` and broadcasts the change.\n-5. `kanban-app/src/commands.rs` — register the new variant in `ui_state_change_kind` (look for the existing `InspectorStack` branch around line 1870) so the React side hears `inspector_width` change events.\n-\n-### Type plumbing\n-\n-- Frontend: `UIWindowState` type (in `kanban-app/ui/src/lib/ui-state-context.tsx` or `types/kanban.ts` — find via grep) gets an optional `inspector_width?: number`. The `useUIState` selector exposes it; `InspectorsContainer` reads it as `winState?.inspector_width ?? 420`.\n-\n-## Acceptance Criteria\n-\n-- [ ] Hovering the left edge of any inspector shows `cursor-col-resize`.\n-- [ ] Mousedown + drag on the left edge changes the inspector width in real time, clamped to `[320, min(800, 0.85 * viewport)]`.\n-- [ ] When two panels are stacked, dragging either one's left edge resizes both (single shared width per window) and they remain tiled — no overlap, no gap.\n-- [ ] After a resize, the new width is dispatched once via `ui.inspector.set_width` on mouseup. Reloading the window restores the saved width.\n-- [ ] When `WindowState.inspector_width` is `None`, the panel renders at the existing 420 px default.\n-- [ ] Existing inspector snapshot/spatial-nav tests still pass — the resize handle is a visual+pointer addition only, no change to focus behavior or `<FocusZone>` structure.\n-\n-## Tests\n-\n-- [ ] **Drag interaction (browser test)** in a new `kanban-app/ui/src/components/inspector-resize.browser.test.tsx`: mount `<InspectorsContainer>` with one panel open, simulate `mousedown` on the left-edge handle, `mousemove` shifting -120 px (wider) and `mouseup`. Assert (a) the panel's `style.width` after mousemove is the new value, (b) `ui.inspector.set_width` was dispatched once on mouseup with the final value.\n-- [ ] **Clamp test (unit)** in `slide-panel.test.tsx` (create if absent): a drag that would compute < 320 px is clamped to 320 px; a drag that would exceed `min(800, 0.85*viewport)` is clamped at the upper bound.\n-- [ ] **Stack offset test** in `kanban-app/ui/src/components/inspectors-container.test.tsx`: when two panels are open and `inspector_width` is 600, panel 0 has `right: 600` and panel 1 has `right: 0` (tiled with the new width, not 420).\n-- [ ] **Backend round-trip** in `swissarmyhammer-commands/src/ui_state.rs` (next to the existing `set_inspector_stack_restores` test): `set_inspector_width(\"main\", 540)` then `inspector_width(\"main\")` returns `Some(540)`; serialize to YAML and reload, value survives.\n-- [ ] Run `pnpm -C kanban-app/ui test inspector-resize slide-panel inspectors-container` and `cargo test -p swissarmyhammer-commands ui_state` — all green.\n-\n-## Workflow\n-\n-- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n","transaction_id":null}
{"id":"01KQSE8TT8S8MYT0PPCXMZ9CR3","timestamp":"2026-05-04T12:09:09.192646Z","entity_type":"task","entity_id":"01KQSE8TT79XC3KJGEHX6DW99G","op":"create","changes":[["body",{"kind":"set","value":"## What\n\nInspector panels are currently fixed at 420 px wide. Make the left edge draggable so the user can resize the inspector, with the chosen width persisted per-window through the existing `WindowState` UI-state pipeline.\n\n### Current shape\n\n- `kanban-app/ui/src/components/slide-panel.tsx` — fixed `w-[420px] max-w-[85vw]` Tailwind class on the outer `<div>`. Renders the close button + scroll body. Knows nothing about its position in the stack.\n- `kanban-app/ui/src/components/inspectors-container.tsx`\n  - `const PANEL_WIDTH = 420;` (module constant)\n  - `const rightOffset = (panelStack.length - 1 - index) * PANEL_WIDTH;` — each panel in the stack is offset by one panel width so adjacent panels tile horizontally.\n- `swissarmyhammer-commands/src/ui_state.rs::WindowState` — per-window persisted state. Already carries `inspector_stack`, `active_view_id`, `x/y/width/height`, `maximized`. Mutators broadcast a `UIStateChange::*` variant the frontend listens for.\n\n### Decisions (assume these unless changed)\n\n- **Single width per window**, applied to every panel in the stack. Adjacent panels must keep tiling — they share the same width so the right-edge offset math stays valid.\n- **Per-window persistence** via `WindowState.inspector_width: Option<u32>`. `None` falls back to the current 420 px default.\n- **Bounds**: clamp to `[320, min(800, 0.85 * window.innerWidth)]`. The lower bound keeps the form readable; the upper bound preserves the existing `max-w-[85vw]` intent.\n- **Drag handle**: a 6 px-wide invisible hit zone on the panel's left edge with `cursor-col-resize`, becoming visibly subtle on hover (the same muted-foreground stroke used by the column-resize handle in `data-table.tsx`, if applicable — otherwise a hairline `bg-border` indicator).\n- **Persistence cadence**: update local React state on every mousemove (60 fps drag), but only dispatch the backend `ui.inspector.set_width` command on mouseup, mirroring the column-resize / window-geometry pattern.\n\n### Files to change\n\n1. `kanban-app/ui/src/components/slide-panel.tsx` — add an optional `width` prop and a left-edge drag handle. Replace `w-[420px]` with an inline `style={{ width }}` (still capped via CSS `maxWidth: '85vw'`). Drag emits `onResize(nextWidth)` continuously and `onResizeEnd(finalWidth)` once.\n2. `kanban-app/ui/src/components/inspectors-container.tsx` — read `inspector_width` from `useUIState()` (with the `?? 420` fallback), pass it to every `<InspectorPanel>`, and use it in the `rightOffset` calculation. Wire `onResizeEnd` to a new `useDispatchCommand(\"ui.inspector.set_width\")` callback. The transient drag state stays in React; only the final value round-trips through the backend.\n3. `swissarmyhammer-commands/src/ui_state.rs` —\n   - Add `inspector_width: Option<u32>` to `WindowState` (with `Default` returning `None`).\n   - Add `set_inspector_width(&self, window_label: &str, width: u32) -> Option<UIStateChange>` and a getter `inspector_width(&self, window_label: &str) -> Option<u32>`.\n   - Add a `UIStateChange::InspectorWidth { window_label, width: u32 }` variant; update the `ui_state_change_kind` mapping.\n4. `swissarmyhammer-kanban/src/commands/ui_commands.rs` (or wherever `ui.inspector.open/close` live, mirror that file) — add `ui.inspector.set_width` command that calls `set_inspector_width` and broadcasts the change.\n5. `kanban-app/src/commands.rs` — register the new variant in `ui_state_change_kind` (look for the existing `InspectorStack` branch around line 1870) so the React side hears `inspector_width` change events.\n\n### Type plumbing\n\n- Frontend: `UIWindowState` type (in `kanban-app/ui/src/lib/ui-state-context.tsx` or `types/kanban.ts` — find via grep) gets an optional `inspector_width?: number`. The `useUIState` selector exposes it; `InspectorsContainer` reads it as `winState?.inspector_width ?? 420`.\n\n## Acceptance Criteria\n\n- [ ] Hovering the left edge of any inspector shows `cursor-col-resize`.\n- [ ] Mousedown + drag on the left edge changes the inspector width in real time, clamped to `[320, min(800, 0.85 * viewport)]`.\n- [ ] When two panels are stacked, dragging either one's left edge resizes both (single shared width per window) and they remain tiled — no overlap, no gap.\n- [ ] After a resize, the new width is dispatched once via `ui.inspector.set_width` on mouseup. Reloading the window restores the saved width.\n- [ ] When `WindowState.inspector_width` is `None`, the panel renders at the existing 420 px default.\n- [ ] Existing inspector snapshot/spatial-nav tests still pass — the resize handle is a visual+pointer addition only, no change to focus behavior or `<FocusZone>` structure.\n\n## Tests\n\n- [ ] **Drag interaction (browser test)** in a new `kanban-app/ui/src/components/inspector-resize.browser.test.tsx`: mount `<InspectorsContainer>` with one panel open, simulate `mousedown` on the left-edge handle, `mousemove` shifting -120 px (wider) and `mouseup`. Assert (a) the panel's `style.width` after mousemove is the new value, (b) `ui.inspector.set_width` was dispatched once on mouseup with the final value.\n- [ ] **Clamp test (unit)** in `slide-panel.test.tsx` (create if absent): a drag that would compute < 320 px is clamped to 320 px; a drag that would exceed `min(800, 0.85*viewport)` is clamped at the upper bound.\n- [ ] **Stack offset test** in `kanban-app/ui/src/components/inspectors-container.test.tsx`: when two panels are open and `inspector_width` is 600, panel 0 has `right: 600` and panel 1 has `right: 0` (tiled with the new width, not 420).\n- [ ] **Backend round-trip** in `swissarmyhammer-commands/src/ui_state.rs` (next to the existing `set_inspector_stack_restores` test): `set_inspector_width(\"main\", 540)` then `inspector_width(\"main\")` returns `Some(540)`; serialize to YAML and reload, value survives.\n- [ ] Run `pnpm -C kanban-app/ui test inspector-resize slide-panel inspectors-container` and `cargo test -p swissarmyhammer-commands ui_state` — all green.\n\n## Workflow\n\n- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n"}],["position_column",{"kind":"set","value":"todo"}],["title",{"kind":"set","value":"Resizable inspector — drag the left edge to change width (persisted per window)"}],["position_ordinal",{"kind":"set","value":"cf80"}],["project",{"kind":"set","value":"spatial-nav"}],["assignees",{"kind":"set","value":["claude-code"]}]]}
{"id":"01KQZ77R9R7YJH38APJ43TGG6R","timestamp":"2026-05-06T18:01:40.409008Z","op":"update","item_id":"01KQSE8TT79XC3KJGEHX6DW99G","forward_patch":"--- original\n+++ modified\n@@ -3,7 +3,6 @@\n - claude-code\n position_column: todo\n position_ordinal: cf80\n-project: spatial-nav\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -3,6 +3,7 @@\n - claude-code\n position_column: todo\n position_ordinal: cf80\n+project: spatial-nav\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n ## What\n","transaction_id":null}
{"id":"01KQZ77R9WD1WF615HTB2VV0EV","timestamp":"2026-05-06T18:01:40.412564Z","entity_type":"task","entity_id":"01KQSE8TT79XC3KJGEHX6DW99G","op":"update","changes":[["project",{"kind":"removed","old_value":"spatial-nav"}]]}
{"id":"01KQZ9BVK7X086JC3CY8Z9KW16","timestamp":"2026-05-06T18:38:52.007692Z","op":"update","item_id":"01KQSE8TT79XC3KJGEHX6DW99G","forward_patch":"--- original\n+++ modified\n@@ -59,4 +59,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n+- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -59,4 +59,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n\\ No newline at end of file\n+- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n","transaction_id":null}
{"id":"01KQZ9BVKB13BGTJ8VHNVEXERH","timestamp":"2026-05-06T18:38:52.011749Z","entity_type":"task","entity_id":"01KQSE8TT79XC3KJGEHX6DW99G","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -52,4 +52,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n+- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -52,4 +52,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n\\ No newline at end of file\n+- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n"}]]}
{"id":"01KR79KPRP8FTNT9F3BQRFQE8R","timestamp":"2026-05-09T21:17:04.662137Z","op":"update","item_id":"01KQSE8TT79XC3KJGEHX6DW99G","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: todo\n-position_ordinal: cf80\n+position_ordinal: '7e80'\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: todo\n-position_ordinal: '7e80'\n+position_ordinal: cf80\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n ## What\n","transaction_id":null}
{"id":"01KR79KPRQXPTAJD0PDN7RJARF","timestamp":"2026-05-09T21:17:04.663263Z","entity_type":"task","entity_id":"01KQSE8TT79XC3KJGEHX6DW99G","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-cf80\n\\ No newline at end of file\n+7e80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-7e80\n\\ No newline at end of file\n+cf80\n\\ No newline at end of file\n"}]]}
{"id":"01KR79RXED02NG5HMWXH38TAHZ","timestamp":"2026-05-09T21:19:55.341552Z","op":"update","item_id":"01KQSE8TT79XC3KJGEHX6DW99G","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: '7e80'\n+position_column: doing\n+position_ordinal: '80'\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '7e80'\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n ## What\n","transaction_id":null}
{"id":"01KR79RXEDEVZV8XNM4R74BF0K","timestamp":"2026-05-09T21:19:55.341923Z","entity_type":"task","entity_id":"01KQSE8TT79XC3KJGEHX6DW99G","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-7e80\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+7e80\n\\ No newline at end of file\n"}]]}
{"id":"01KR7AZ3TGS3CS9M2RPGDGN14Y","timestamp":"2026-05-09T21:40:47.056096Z","op":"update","item_id":"01KQSE8TT79XC3KJGEHX6DW99G","forward_patch":"--- original\n+++ modified\n@@ -42,20 +42,20 @@\n\n ## Acceptance Criteria\n\n-- [ ] Hovering the left edge of any inspector shows `cursor-col-resize`.\n-- [ ] Mousedown + drag on the left edge changes the inspector width in real time, clamped to `[320, min(800, 0.85 * viewport)]`.\n-- [ ] When two panels are stacked, dragging either one's left edge resizes both (single shared width per window) and they remain tiled — no overlap, no gap.\n-- [ ] After a resize, the new width is dispatched once via `ui.inspector.set_width` on mouseup. Reloading the window restores the saved width.\n-- [ ] When `WindowState.inspector_width` is `None`, the panel renders at the existing 420 px default.\n-- [ ] Existing inspector snapshot/spatial-nav tests still pass — the resize handle is a visual+pointer addition only, no change to focus behavior or `<FocusZone>` structure.\n+- [x] Hovering the left edge of any inspector shows `cursor-col-resize`.\n+- [x] Mousedown + drag on the left edge changes the inspector width in real time, clamped to `[320, min(800, 0.85 * viewport)]`.\n+- [x] When two panels are stacked, dragging either one's left edge resizes both (single shared width per window) and they remain tiled — no overlap, no gap.\n+- [x] After a resize, the new width is dispatched once via `ui.inspector.set_width` on mouseup. Reloading the window restores the saved width.\n+- [x] When `WindowState.inspector_width` is `None`, the panel renders at the existing 420 px default.\n+- [x] Existing inspector snapshot/spatial-nav tests still pass — the resize handle is a visual+pointer addition only, no change to focus behavior or `<FocusZone>` structure.\n\n ## Tests\n\n-- [ ] **Drag interaction (browser test)** in a new `kanban-app/ui/src/components/inspector-resize.browser.test.tsx`: mount `<InspectorsContainer>` with one panel open, simulate `mousedown` on the left-edge handle, `mousemove` shifting -120 px (wider) and `mouseup`. Assert (a) the panel's `style.width` after mousemove is the new value, (b) `ui.inspector.set_width` was dispatched once on mouseup with the final value.\n-- [ ] **Clamp test (unit)** in `slide-panel.test.tsx` (create if absent): a drag that would compute < 320 px is clamped to 320 px; a drag that would exceed `min(800, 0.85*viewport)` is clamped at the upper bound.\n-- [ ] **Stack offset test** in `kanban-app/ui/src/components/inspectors-container.test.tsx`: when two panels are open and `inspector_width` is 600, panel 0 has `right: 600` and panel 1 has `right: 0` (tiled with the new width, not 420).\n-- [ ] **Backend round-trip** in `swissarmyhammer-commands/src/ui_state.rs` (next to the existing `set_inspector_stack_restores` test): `set_inspector_width(\"main\", 540)` then `inspector_width(\"main\")` returns `Some(540)`; serialize to YAML and reload, value survives.\n-- [ ] Run `pnpm -C kanban-app/ui test inspector-resize slide-panel inspectors-container` and `cargo test -p swissarmyhammer-commands ui_state` — all green.\n+- [x] **Drag interaction (browser test)** in a new `kanban-app/ui/src/components/inspector-resize.browser.test.tsx`: mount `<InspectorsContainer>` with one panel open, simulate `mousedown` on the left-edge handle, `mousemove` shifting -120 px (wider) and `mouseup`. Assert (a) the panel's `style.width` after mousemove is the new value, (b) `ui.inspector.set_width` was dispatched once on mouseup with the final value.\n+- [x] **Clamp test (unit)** in `slide-panel.test.tsx` (create if absent): a drag that would compute < 320 px is clamped to 320 px; a drag that would exceed `min(800, 0.85*viewport)` is clamped at the upper bound.\n+- [x] **Stack offset test** in `kanban-app/ui/src/components/inspectors-container.test.tsx`: when two panels are open and `inspector_width` is 600, panel 0 has `right: 600` and panel 1 has `right: 0` (tiled with the new width, not 420).\n+- [x] **Backend round-trip** in `swissarmyhammer-commands/src/ui_state.rs` (next to the existing `set_inspector_stack_restores` test): `set_inspector_width(\"main\", 540)` then `inspector_width(\"main\")` returns `Some(540)`; serialize to YAML and reload, value survives.\n+- [x] Run `pnpm -C kanban-app/ui test inspector-resize slide-panel inspectors-container` and `cargo test -p swissarmyhammer-commands ui_state` — all green.\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -42,20 +42,20 @@\n\n ## Acceptance Criteria\n\n-- [x] Hovering the left edge of any inspector shows `cursor-col-resize`.\n-- [x] Mousedown + drag on the left edge changes the inspector width in real time, clamped to `[320, min(800, 0.85 * viewport)]`.\n-- [x] When two panels are stacked, dragging either one's left edge resizes both (single shared width per window) and they remain tiled — no overlap, no gap.\n-- [x] After a resize, the new width is dispatched once via `ui.inspector.set_width` on mouseup. Reloading the window restores the saved width.\n-- [x] When `WindowState.inspector_width` is `None`, the panel renders at the existing 420 px default.\n-- [x] Existing inspector snapshot/spatial-nav tests still pass — the resize handle is a visual+pointer addition only, no change to focus behavior or `<FocusZone>` structure.\n+- [ ] Hovering the left edge of any inspector shows `cursor-col-resize`.\n+- [ ] Mousedown + drag on the left edge changes the inspector width in real time, clamped to `[320, min(800, 0.85 * viewport)]`.\n+- [ ] When two panels are stacked, dragging either one's left edge resizes both (single shared width per window) and they remain tiled — no overlap, no gap.\n+- [ ] After a resize, the new width is dispatched once via `ui.inspector.set_width` on mouseup. Reloading the window restores the saved width.\n+- [ ] When `WindowState.inspector_width` is `None`, the panel renders at the existing 420 px default.\n+- [ ] Existing inspector snapshot/spatial-nav tests still pass — the resize handle is a visual+pointer addition only, no change to focus behavior or `<FocusZone>` structure.\n\n ## Tests\n\n-- [x] **Drag interaction (browser test)** in a new `kanban-app/ui/src/components/inspector-resize.browser.test.tsx`: mount `<InspectorsContainer>` with one panel open, simulate `mousedown` on the left-edge handle, `mousemove` shifting -120 px (wider) and `mouseup`. Assert (a) the panel's `style.width` after mousemove is the new value, (b) `ui.inspector.set_width` was dispatched once on mouseup with the final value.\n-- [x] **Clamp test (unit)** in `slide-panel.test.tsx` (create if absent): a drag that would compute < 320 px is clamped to 320 px; a drag that would exceed `min(800, 0.85*viewport)` is clamped at the upper bound.\n-- [x] **Stack offset test** in `kanban-app/ui/src/components/inspectors-container.test.tsx`: when two panels are open and `inspector_width` is 600, panel 0 has `right: 600` and panel 1 has `right: 0` (tiled with the new width, not 420).\n-- [x] **Backend round-trip** in `swissarmyhammer-commands/src/ui_state.rs` (next to the existing `set_inspector_stack_restores` test): `set_inspector_width(\"main\", 540)` then `inspector_width(\"main\")` returns `Some(540)`; serialize to YAML and reload, value survives.\n-- [x] Run `pnpm -C kanban-app/ui test inspector-resize slide-panel inspectors-container` and `cargo test -p swissarmyhammer-commands ui_state` — all green.\n+- [ ] **Drag interaction (browser test)** in a new `kanban-app/ui/src/components/inspector-resize.browser.test.tsx`: mount `<InspectorsContainer>` with one panel open, simulate `mousedown` on the left-edge handle, `mousemove` shifting -120 px (wider) and `mouseup`. Assert (a) the panel's `style.width` after mousemove is the new value, (b) `ui.inspector.set_width` was dispatched once on mouseup with the final value.\n+- [ ] **Clamp test (unit)** in `slide-panel.test.tsx` (create if absent): a drag that would compute < 320 px is clamped to 320 px; a drag that would exceed `min(800, 0.85*viewport)` is clamped at the upper bound.\n+- [ ] **Stack offset test** in `kanban-app/ui/src/components/inspectors-container.test.tsx`: when two panels are open and `inspector_width` is 600, panel 0 has `right: 600` and panel 1 has `right: 0` (tiled with the new width, not 420).\n+- [ ] **Backend round-trip** in `swissarmyhammer-commands/src/ui_state.rs` (next to the existing `set_inspector_stack_restores` test): `set_inspector_width(\"main\", 540)` then `inspector_width(\"main\")` returns `Some(540)`; serialize to YAML and reload, value survives.\n+- [ ] Run `pnpm -C kanban-app/ui test inspector-resize slide-panel inspectors-container` and `cargo test -p swissarmyhammer-commands ui_state` — all green.\n\n ## Workflow\n\n","transaction_id":null}
{"id":"01KR7AZ3TG9FZM16Y27ZMRSSTX","timestamp":"2026-05-09T21:40:47.056521Z","entity_type":"task","entity_id":"01KQSE8TT79XC3KJGEHX6DW99G","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -35,20 +35,20 @@\n\n ## Acceptance Criteria\n\n-- [ ] Hovering the left edge of any inspector shows `cursor-col-resize`.\n-- [ ] Mousedown + drag on the left edge changes the inspector width in real time, clamped to `[320, min(800, 0.85 * viewport)]`.\n-- [ ] When two panels are stacked, dragging either one's left edge resizes both (single shared width per window) and they remain tiled — no overlap, no gap.\n-- [ ] After a resize, the new width is dispatched once via `ui.inspector.set_width` on mouseup. Reloading the window restores the saved width.\n-- [ ] When `WindowState.inspector_width` is `None`, the panel renders at the existing 420 px default.\n-- [ ] Existing inspector snapshot/spatial-nav tests still pass — the resize handle is a visual+pointer addition only, no change to focus behavior or `<FocusZone>` structure.\n+- [x] Hovering the left edge of any inspector shows `cursor-col-resize`.\n+- [x] Mousedown + drag on the left edge changes the inspector width in real time, clamped to `[320, min(800, 0.85 * viewport)]`.\n+- [x] When two panels are stacked, dragging either one's left edge resizes both (single shared width per window) and they remain tiled — no overlap, no gap.\n+- [x] After a resize, the new width is dispatched once via `ui.inspector.set_width` on mouseup. Reloading the window restores the saved width.\n+- [x] When `WindowState.inspector_width` is `None`, the panel renders at the existing 420 px default.\n+- [x] Existing inspector snapshot/spatial-nav tests still pass — the resize handle is a visual+pointer addition only, no change to focus behavior or `<FocusZone>` structure.\n\n ## Tests\n\n-- [ ] **Drag interaction (browser test)** in a new `kanban-app/ui/src/components/inspector-resize.browser.test.tsx`: mount `<InspectorsContainer>` with one panel open, simulate `mousedown` on the left-edge handle, `mousemove` shifting -120 px (wider) and `mouseup`. Assert (a) the panel's `style.width` after mousemove is the new value, (b) `ui.inspector.set_width` was dispatched once on mouseup with the final value.\n-- [ ] **Clamp test (unit)** in `slide-panel.test.tsx` (create if absent): a drag that would compute < 320 px is clamped to 320 px; a drag that would exceed `min(800, 0.85*viewport)` is clamped at the upper bound.\n-- [ ] **Stack offset test** in `kanban-app/ui/src/components/inspectors-container.test.tsx`: when two panels are open and `inspector_width` is 600, panel 0 has `right: 600` and panel 1 has `right: 0` (tiled with the new width, not 420).\n-- [ ] **Backend round-trip** in `swissarmyhammer-commands/src/ui_state.rs` (next to the existing `set_inspector_stack_restores` test): `set_inspector_width(\"main\", 540)` then `inspector_width(\"main\")` returns `Some(540)`; serialize to YAML and reload, value survives.\n-- [ ] Run `pnpm -C kanban-app/ui test inspector-resize slide-panel inspectors-container` and `cargo test -p swissarmyhammer-commands ui_state` — all green.\n+- [x] **Drag interaction (browser test)** in a new `kanban-app/ui/src/components/inspector-resize.browser.test.tsx`: mount `<InspectorsContainer>` with one panel open, simulate `mousedown` on the left-edge handle, `mousemove` shifting -120 px (wider) and `mouseup`. Assert (a) the panel's `style.width` after mousemove is the new value, (b) `ui.inspector.set_width` was dispatched once on mouseup with the final value.\n+- [x] **Clamp test (unit)** in `slide-panel.test.tsx` (create if absent): a drag that would compute < 320 px is clamped to 320 px; a drag that would exceed `min(800, 0.85*viewport)` is clamped at the upper bound.\n+- [x] **Stack offset test** in `kanban-app/ui/src/components/inspectors-container.test.tsx`: when two panels are open and `inspector_width` is 600, panel 0 has `right: 600` and panel 1 has `right: 0` (tiled with the new width, not 420).\n+- [x] **Backend round-trip** in `swissarmyhammer-commands/src/ui_state.rs` (next to the existing `set_inspector_stack_restores` test): `set_inspector_width(\"main\", 540)` then `inspector_width(\"main\")` returns `Some(540)`; serialize to YAML and reload, value survives.\n+- [x] Run `pnpm -C kanban-app/ui test inspector-resize slide-panel inspectors-container` and `cargo test -p swissarmyhammer-commands ui_state` — all green.\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -35,20 +35,20 @@\n\n ## Acceptance Criteria\n\n-- [x] Hovering the left edge of any inspector shows `cursor-col-resize`.\n-- [x] Mousedown + drag on the left edge changes the inspector width in real time, clamped to `[320, min(800, 0.85 * viewport)]`.\n-- [x] When two panels are stacked, dragging either one's left edge resizes both (single shared width per window) and they remain tiled — no overlap, no gap.\n-- [x] After a resize, the new width is dispatched once via `ui.inspector.set_width` on mouseup. Reloading the window restores the saved width.\n-- [x] When `WindowState.inspector_width` is `None`, the panel renders at the existing 420 px default.\n-- [x] Existing inspector snapshot/spatial-nav tests still pass — the resize handle is a visual+pointer addition only, no change to focus behavior or `<FocusZone>` structure.\n+- [ ] Hovering the left edge of any inspector shows `cursor-col-resize`.\n+- [ ] Mousedown + drag on the left edge changes the inspector width in real time, clamped to `[320, min(800, 0.85 * viewport)]`.\n+- [ ] When two panels are stacked, dragging either one's left edge resizes both (single shared width per window) and they remain tiled — no overlap, no gap.\n+- [ ] After a resize, the new width is dispatched once via `ui.inspector.set_width` on mouseup. Reloading the window restores the saved width.\n+- [ ] When `WindowState.inspector_width` is `None`, the panel renders at the existing 420 px default.\n+- [ ] Existing inspector snapshot/spatial-nav tests still pass — the resize handle is a visual+pointer addition only, no change to focus behavior or `<FocusZone>` structure.\n\n ## Tests\n\n-- [x] **Drag interaction (browser test)** in a new `kanban-app/ui/src/components/inspector-resize.browser.test.tsx`: mount `<InspectorsContainer>` with one panel open, simulate `mousedown` on the left-edge handle, `mousemove` shifting -120 px (wider) and `mouseup`. Assert (a) the panel's `style.width` after mousemove is the new value, (b) `ui.inspector.set_width` was dispatched once on mouseup with the final value.\n-- [x] **Clamp test (unit)** in `slide-panel.test.tsx` (create if absent): a drag that would compute < 320 px is clamped to 320 px; a drag that would exceed `min(800, 0.85*viewport)` is clamped at the upper bound.\n-- [x] **Stack offset test** in `kanban-app/ui/src/components/inspectors-container.test.tsx`: when two panels are open and `inspector_width` is 600, panel 0 has `right: 600` and panel 1 has `right: 0` (tiled with the new width, not 420).\n-- [x] **Backend round-trip** in `swissarmyhammer-commands/src/ui_state.rs` (next to the existing `set_inspector_stack_restores` test): `set_inspector_width(\"main\", 540)` then `inspector_width(\"main\")` returns `Some(540)`; serialize to YAML and reload, value survives.\n-- [x] Run `pnpm -C kanban-app/ui test inspector-resize slide-panel inspectors-container` and `cargo test -p swissarmyhammer-commands ui_state` — all green.\n+- [ ] **Drag interaction (browser test)** in a new `kanban-app/ui/src/components/inspector-resize.browser.test.tsx`: mount `<InspectorsContainer>` with one panel open, simulate `mousedown` on the left-edge handle, `mousemove` shifting -120 px (wider) and `mouseup`. Assert (a) the panel's `style.width` after mousemove is the new value, (b) `ui.inspector.set_width` was dispatched once on mouseup with the final value.\n+- [ ] **Clamp test (unit)** in `slide-panel.test.tsx` (create if absent): a drag that would compute < 320 px is clamped to 320 px; a drag that would exceed `min(800, 0.85*viewport)` is clamped at the upper bound.\n+- [ ] **Stack offset test** in `kanban-app/ui/src/components/inspectors-container.test.tsx`: when two panels are open and `inspector_width` is 600, panel 0 has `right: 600` and panel 1 has `right: 0` (tiled with the new width, not 420).\n+- [ ] **Backend round-trip** in `swissarmyhammer-commands/src/ui_state.rs` (next to the existing `set_inspector_stack_restores` test): `set_inspector_width(\"main\", 540)` then `inspector_width(\"main\")` returns `Some(540)`; serialize to YAML and reload, value survives.\n+- [ ] Run `pnpm -C kanban-app/ui test inspector-resize slide-panel inspectors-container` and `cargo test -p swissarmyhammer-commands ui_state` — all green.\n\n ## Workflow\n\n"}]]}
{"id":"01KR7B0EMWJF2RDTB706KG8M7N","timestamp":"2026-05-09T21:41:30.908597Z","op":"update","item_id":"01KQSE8TT79XC3KJGEHX6DW99G","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n","transaction_id":null}
{"id":"01KR7B0EMXNGZM37NB9MV634D1","timestamp":"2026-05-09T21:41:30.909679Z","entity_type":"task","entity_id":"01KQSE8TT79XC3KJGEHX6DW99G","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KR7CRC0ZNAW19Y0RKAWMQV0Z","timestamp":"2026-05-09T22:12:03.231230Z","op":"update","item_id":"01KQSE8TT79XC3KJGEHX6DW99G","forward_patch":"--- original\n+++ modified\n@@ -59,4 +59,17 @@\n\n ## Workflow\n\n-- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n\\ No newline at end of file\n+- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n+\n+## Review Findings (2026-05-09 17:10)\n+\n+### Warnings\n+- [ ] `kanban-app/ui/src/components/slide-panel.tsx` (`handleMouseDown` / `endDrag`) — Tap-without-movement (mousedown → mouseup with zero `mousemove`) on a never-resized panel still calls `onResizeEnd(width)` with the React default 420. `inspectors-container.tsx::handleResizeEnd` then dispatches `ui.inspector.set_width { width: 420 }`, which flips `WindowState.inspector_width` from `None` to `Some(420)` and writes to disk. Backend's `set_inspector_width` only no-ops when the new width *equals* the existing value, so `None → Some(420)` is treated as a real change and persists. A \"tap with no drag\" should not mutate persisted state. Suggested fix: track a `moved` flag on `dragRef` set by `handleMouseMove`, and skip `onResizeEnd` in `endDrag` when `!moved`.\n+- [ ] `swissarmyhammer-kanban/src/commands/ui_commands.rs::InspectorSetWidthCmd` — No unit tests. Compare with `SetAppModeCmd` which has three (`set_app_mode_changes_ui_state`, `set_app_mode_noop_returns_null`, `set_app_mode_uses_window_from_scope`). Missing coverage means the negative-`width` path (which falls into `MissingArg` because `serde_json::Value::as_u64()` returns `None` for negatives), the out-of-range-`u32` path, the no-op echo path (`set_inspector_width` returning `None` on equal value), and the window-label-from-scope path are all uncovered. Suggested fix: add a `tests` block in `ui_commands.rs` next to the `SetAppMode` tests pinning each path.\n+- [ ] `kanban-app/ui/src/components/slide-panel.tsx` (`useEffect` binding `mousemove`/`mouseup` on `window`) — listeners are bound for the entire lifetime of every mounted `SlidePanel`, regardless of whether a drag is active. With multiple panels stacked (the spec calls for stacking) every panel installs its own pair, and every pointer movement anywhere on the window walks all of them (the early-return on `!drag.active` is cheap but still runs n times per move event). This works, but contradicts the \"wire mouse listeners only while dragging\" pattern used elsewhere in the codebase. Suggested fix: bind the listeners inside `handleMouseDown` (after setting `dragRef.current`) and remove them in `endDrag`. The body of `handleMouseDown` already has the moment to do this; `endDrag` already has the cleanup point.\n+- [ ] `kanban-app/ui/src/components/inspectors-container.tsx` (`useEffect(() => setLiveWidth(null), [persistedWidth])`) — if a `ui-state-changed` `inspector_width` echo arrives mid-drag (e.g. another window in the same session persisted a different width through some future code path, or a stale event from a prior drag races back), the effect resets `liveWidth` to `null` and the panel snaps to the new persisted value while the drag is still in progress. The drag continues correctly (drag-ref logic in `slide-panel.tsx` is untouched), but the visual width flickers. Today there is no realistic way to trigger this — the window only updates its own `inspector_width` and dispatch happens on mouseup — but the contract is fragile. Suggested fix: gate the reset effect with a \"not currently dragging\" predicate (e.g. track `isDragging` and skip the reset while it's true), or move the persisted/live reconciliation into `slide-panel.tsx` so the drag path owns the source of truth end-to-end.\n+\n+### Nits\n+- [ ] `swissarmyhammer-kanban/src/commands/ui_commands.rs::InspectorSetWidthCmd::execute` — When `width` is a negative integer, `v.as_u64()` returns `None` and the user sees `MissingArg(\"width\")`. The error label is misleading: the arg is present, just not coercible. Suggested fix: use `v.as_i64()` plus an explicit range check, or two separate `ok_or_else` arms — one for \"missing\", one for \"not an integer / out of range\".\n+- [ ] `swissarmyhammer-kanban/src/commands/ui_commands.rs::InspectorSetWidthCmd` — There is no lower-bound check in the command (the `[320, …]` clamp lives only in the frontend). A direct dispatch with `width: 1` from the palette/CLI/test harness is silently persisted and reloaded as a 1 px panel (which `slide-panel.tsx`'s `style.maxWidth: \"85vw\"` does not protect against on the lower end — the component just renders 1 px wide). Suggested fix: clamp or reject in `InspectorSetWidthCmd::execute` so the command itself enforces the contract documented in the task spec.\n+- [ ] `kanban-app/ui/src/components/inspectors-container.tsx::InspectorPanel` (line citing `useFirstFieldFocus`) — the docstring contains escaped unicode literals (`—` for em-dash) where surrounding code uses real em-dashes. Suggested fix: replace `—` with `—` to match the rest of the file.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -59,17 +59,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n-\n-## Review Findings (2026-05-09 17:10)\n-\n-### Warnings\n-- [ ] `kanban-app/ui/src/components/slide-panel.tsx` (`handleMouseDown` / `endDrag`) — Tap-without-movement (mousedown → mouseup with zero `mousemove`) on a never-resized panel still calls `onResizeEnd(width)` with the React default 420. `inspectors-container.tsx::handleResizeEnd` then dispatches `ui.inspector.set_width { width: 420 }`, which flips `WindowState.inspector_width` from `None` to `Some(420)` and writes to disk. Backend's `set_inspector_width` only no-ops when the new width *equals* the existing value, so `None → Some(420)` is treated as a real change and persists. A \"tap with no drag\" should not mutate persisted state. Suggested fix: track a `moved` flag on `dragRef` set by `handleMouseMove`, and skip `onResizeEnd` in `endDrag` when `!moved`.\n-- [ ] `swissarmyhammer-kanban/src/commands/ui_commands.rs::InspectorSetWidthCmd` — No unit tests. Compare with `SetAppModeCmd` which has three (`set_app_mode_changes_ui_state`, `set_app_mode_noop_returns_null`, `set_app_mode_uses_window_from_scope`). Missing coverage means the negative-`width` path (which falls into `MissingArg` because `serde_json::Value::as_u64()` returns `None` for negatives), the out-of-range-`u32` path, the no-op echo path (`set_inspector_width` returning `None` on equal value), and the window-label-from-scope path are all uncovered. Suggested fix: add a `tests` block in `ui_commands.rs` next to the `SetAppMode` tests pinning each path.\n-- [ ] `kanban-app/ui/src/components/slide-panel.tsx` (`useEffect` binding `mousemove`/`mouseup` on `window`) — listeners are bound for the entire lifetime of every mounted `SlidePanel`, regardless of whether a drag is active. With multiple panels stacked (the spec calls for stacking) every panel installs its own pair, and every pointer movement anywhere on the window walks all of them (the early-return on `!drag.active` is cheap but still runs n times per move event). This works, but contradicts the \"wire mouse listeners only while dragging\" pattern used elsewhere in the codebase. Suggested fix: bind the listeners inside `handleMouseDown` (after setting `dragRef.current`) and remove them in `endDrag`. The body of `handleMouseDown` already has the moment to do this; `endDrag` already has the cleanup point.\n-- [ ] `kanban-app/ui/src/components/inspectors-container.tsx` (`useEffect(() => setLiveWidth(null), [persistedWidth])`) — if a `ui-state-changed` `inspector_width` echo arrives mid-drag (e.g. another window in the same session persisted a different width through some future code path, or a stale event from a prior drag races back), the effect resets `liveWidth` to `null` and the panel snaps to the new persisted value while the drag is still in progress. The drag continues correctly (drag-ref logic in `slide-panel.tsx` is untouched), but the visual width flickers. Today there is no realistic way to trigger this — the window only updates its own `inspector_width` and dispatch happens on mouseup — but the contract is fragile. Suggested fix: gate the reset effect with a \"not currently dragging\" predicate (e.g. track `isDragging` and skip the reset while it's true), or move the persisted/live reconciliation into `slide-panel.tsx` so the drag path owns the source of truth end-to-end.\n-\n-### Nits\n-- [ ] `swissarmyhammer-kanban/src/commands/ui_commands.rs::InspectorSetWidthCmd::execute` — When `width` is a negative integer, `v.as_u64()` returns `None` and the user sees `MissingArg(\"width\")`. The error label is misleading: the arg is present, just not coercible. Suggested fix: use `v.as_i64()` plus an explicit range check, or two separate `ok_or_else` arms — one for \"missing\", one for \"not an integer / out of range\".\n-- [ ] `swissarmyhammer-kanban/src/commands/ui_commands.rs::InspectorSetWidthCmd` — There is no lower-bound check in the command (the `[320, …]` clamp lives only in the frontend). A direct dispatch with `width: 1` from the palette/CLI/test harness is silently persisted and reloaded as a 1 px panel (which `slide-panel.tsx`'s `style.maxWidth: \"85vw\"` does not protect against on the lower end — the component just renders 1 px wide). Suggested fix: clamp or reject in `InspectorSetWidthCmd::execute` so the command itself enforces the contract documented in the task spec.\n-- [ ] `kanban-app/ui/src/components/inspectors-container.tsx::InspectorPanel` (line citing `useFirstFieldFocus`) — the docstring contains escaped unicode literals (`—` for em-dash) where surrounding code uses real em-dashes. Suggested fix: replace `—` with `—` to match the rest of the file.\n\\ No newline at end of file\n+- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KR7CRC0Z5JA58KNS2H23QB5C","timestamp":"2026-05-09T22:12:03.231806Z","entity_type":"task","entity_id":"01KQSE8TT79XC3KJGEHX6DW99G","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -52,4 +52,17 @@\n\n ## Workflow\n\n-- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n\\ No newline at end of file\n+- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n+\n+## Review Findings (2026-05-09 17:10)\n+\n+### Warnings\n+- [ ] `kanban-app/ui/src/components/slide-panel.tsx` (`handleMouseDown` / `endDrag`) — Tap-without-movement (mousedown → mouseup with zero `mousemove`) on a never-resized panel still calls `onResizeEnd(width)` with the React default 420. `inspectors-container.tsx::handleResizeEnd` then dispatches `ui.inspector.set_width { width: 420 }`, which flips `WindowState.inspector_width` from `None` to `Some(420)` and writes to disk. Backend's `set_inspector_width` only no-ops when the new width *equals* the existing value, so `None → Some(420)` is treated as a real change and persists. A \"tap with no drag\" should not mutate persisted state. Suggested fix: track a `moved` flag on `dragRef` set by `handleMouseMove`, and skip `onResizeEnd` in `endDrag` when `!moved`.\n+- [ ] `swissarmyhammer-kanban/src/commands/ui_commands.rs::InspectorSetWidthCmd` — No unit tests. Compare with `SetAppModeCmd` which has three (`set_app_mode_changes_ui_state`, `set_app_mode_noop_returns_null`, `set_app_mode_uses_window_from_scope`). Missing coverage means the negative-`width` path (which falls into `MissingArg` because `serde_json::Value::as_u64()` returns `None` for negatives), the out-of-range-`u32` path, the no-op echo path (`set_inspector_width` returning `None` on equal value), and the window-label-from-scope path are all uncovered. Suggested fix: add a `tests` block in `ui_commands.rs` next to the `SetAppMode` tests pinning each path.\n+- [ ] `kanban-app/ui/src/components/slide-panel.tsx` (`useEffect` binding `mousemove`/`mouseup` on `window`) — listeners are bound for the entire lifetime of every mounted `SlidePanel`, regardless of whether a drag is active. With multiple panels stacked (the spec calls for stacking) every panel installs its own pair, and every pointer movement anywhere on the window walks all of them (the early-return on `!drag.active` is cheap but still runs n times per move event). This works, but contradicts the \"wire mouse listeners only while dragging\" pattern used elsewhere in the codebase. Suggested fix: bind the listeners inside `handleMouseDown` (after setting `dragRef.current`) and remove them in `endDrag`. The body of `handleMouseDown` already has the moment to do this; `endDrag` already has the cleanup point.\n+- [ ] `kanban-app/ui/src/components/inspectors-container.tsx` (`useEffect(() => setLiveWidth(null), [persistedWidth])`) — if a `ui-state-changed` `inspector_width` echo arrives mid-drag (e.g. another window in the same session persisted a different width through some future code path, or a stale event from a prior drag races back), the effect resets `liveWidth` to `null` and the panel snaps to the new persisted value while the drag is still in progress. The drag continues correctly (drag-ref logic in `slide-panel.tsx` is untouched), but the visual width flickers. Today there is no realistic way to trigger this — the window only updates its own `inspector_width` and dispatch happens on mouseup — but the contract is fragile. Suggested fix: gate the reset effect with a \"not currently dragging\" predicate (e.g. track `isDragging` and skip the reset while it's true), or move the persisted/live reconciliation into `slide-panel.tsx` so the drag path owns the source of truth end-to-end.\n+\n+### Nits\n+- [ ] `swissarmyhammer-kanban/src/commands/ui_commands.rs::InspectorSetWidthCmd::execute` — When `width` is a negative integer, `v.as_u64()` returns `None` and the user sees `MissingArg(\"width\")`. The error label is misleading: the arg is present, just not coercible. Suggested fix: use `v.as_i64()` plus an explicit range check, or two separate `ok_or_else` arms — one for \"missing\", one for \"not an integer / out of range\".\n+- [ ] `swissarmyhammer-kanban/src/commands/ui_commands.rs::InspectorSetWidthCmd` — There is no lower-bound check in the command (the `[320, …]` clamp lives only in the frontend). A direct dispatch with `width: 1` from the palette/CLI/test harness is silently persisted and reloaded as a 1 px panel (which `slide-panel.tsx`'s `style.maxWidth: \"85vw\"` does not protect against on the lower end — the component just renders 1 px wide). Suggested fix: clamp or reject in `InspectorSetWidthCmd::execute` so the command itself enforces the contract documented in the task spec.\n+- [ ] `kanban-app/ui/src/components/inspectors-container.tsx::InspectorPanel` (line citing `useFirstFieldFocus`) — the docstring contains escaped unicode literals (`—` for em-dash) where surrounding code uses real em-dashes. Suggested fix: replace `—` with `—` to match the rest of the file.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -52,17 +52,4 @@\n\n ## Workflow\n\n-- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n-\n-## Review Findings (2026-05-09 17:10)\n-\n-### Warnings\n-- [ ] `kanban-app/ui/src/components/slide-panel.tsx` (`handleMouseDown` / `endDrag`) — Tap-without-movement (mousedown → mouseup with zero `mousemove`) on a never-resized panel still calls `onResizeEnd(width)` with the React default 420. `inspectors-container.tsx::handleResizeEnd` then dispatches `ui.inspector.set_width { width: 420 }`, which flips `WindowState.inspector_width` from `None` to `Some(420)` and writes to disk. Backend's `set_inspector_width` only no-ops when the new width *equals* the existing value, so `None → Some(420)` is treated as a real change and persists. A \"tap with no drag\" should not mutate persisted state. Suggested fix: track a `moved` flag on `dragRef` set by `handleMouseMove`, and skip `onResizeEnd` in `endDrag` when `!moved`.\n-- [ ] `swissarmyhammer-kanban/src/commands/ui_commands.rs::InspectorSetWidthCmd` — No unit tests. Compare with `SetAppModeCmd` which has three (`set_app_mode_changes_ui_state`, `set_app_mode_noop_returns_null`, `set_app_mode_uses_window_from_scope`). Missing coverage means the negative-`width` path (which falls into `MissingArg` because `serde_json::Value::as_u64()` returns `None` for negatives), the out-of-range-`u32` path, the no-op echo path (`set_inspector_width` returning `None` on equal value), and the window-label-from-scope path are all uncovered. Suggested fix: add a `tests` block in `ui_commands.rs` next to the `SetAppMode` tests pinning each path.\n-- [ ] `kanban-app/ui/src/components/slide-panel.tsx` (`useEffect` binding `mousemove`/`mouseup` on `window`) — listeners are bound for the entire lifetime of every mounted `SlidePanel`, regardless of whether a drag is active. With multiple panels stacked (the spec calls for stacking) every panel installs its own pair, and every pointer movement anywhere on the window walks all of them (the early-return on `!drag.active` is cheap but still runs n times per move event). This works, but contradicts the \"wire mouse listeners only while dragging\" pattern used elsewhere in the codebase. Suggested fix: bind the listeners inside `handleMouseDown` (after setting `dragRef.current`) and remove them in `endDrag`. The body of `handleMouseDown` already has the moment to do this; `endDrag` already has the cleanup point.\n-- [ ] `kanban-app/ui/src/components/inspectors-container.tsx` (`useEffect(() => setLiveWidth(null), [persistedWidth])`) — if a `ui-state-changed` `inspector_width` echo arrives mid-drag (e.g. another window in the same session persisted a different width through some future code path, or a stale event from a prior drag races back), the effect resets `liveWidth` to `null` and the panel snaps to the new persisted value while the drag is still in progress. The drag continues correctly (drag-ref logic in `slide-panel.tsx` is untouched), but the visual width flickers. Today there is no realistic way to trigger this — the window only updates its own `inspector_width` and dispatch happens on mouseup — but the contract is fragile. Suggested fix: gate the reset effect with a \"not currently dragging\" predicate (e.g. track `isDragging` and skip the reset while it's true), or move the persisted/live reconciliation into `slide-panel.tsx` so the drag path owns the source of truth end-to-end.\n-\n-### Nits\n-- [ ] `swissarmyhammer-kanban/src/commands/ui_commands.rs::InspectorSetWidthCmd::execute` — When `width` is a negative integer, `v.as_u64()` returns `None` and the user sees `MissingArg(\"width\")`. The error label is misleading: the arg is present, just not coercible. Suggested fix: use `v.as_i64()` plus an explicit range check, or two separate `ok_or_else` arms — one for \"missing\", one for \"not an integer / out of range\".\n-- [ ] `swissarmyhammer-kanban/src/commands/ui_commands.rs::InspectorSetWidthCmd` — There is no lower-bound check in the command (the `[320, …]` clamp lives only in the frontend). A direct dispatch with `width: 1` from the palette/CLI/test harness is silently persisted and reloaded as a 1 px panel (which `slide-panel.tsx`'s `style.maxWidth: \"85vw\"` does not protect against on the lower end — the component just renders 1 px wide). Suggested fix: clamp or reject in `InspectorSetWidthCmd::execute` so the command itself enforces the contract documented in the task spec.\n-- [ ] `kanban-app/ui/src/components/inspectors-container.tsx::InspectorPanel` (line citing `useFirstFieldFocus`) — the docstring contains escaped unicode literals (`—` for em-dash) where surrounding code uses real em-dashes. Suggested fix: replace `—` with `—` to match the rest of the file.\n\\ No newline at end of file\n+- Use `/tdd` — start with the backend `set_inspector_width` round-trip test, then the inspectors-container offset assertion, then the frontend drag-interaction test. Implement until all three are green.\n\\ No newline at end of file\n"}]]}
{"id":"01KR7CVM0EWEE7M5T5QRHDWE7K","timestamp":"2026-05-09T22:13:49.710286Z","op":"update","item_id":"01KQSE8TT79XC3KJGEHX6DW99G","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n","transaction_id":null}
{"id":"01KR7CVM0FSNZ5KE8TTHPY1X5E","timestamp":"2026-05-09T22:13:49.711072Z","entity_type":"task","entity_id":"01KQSE8TT79XC3KJGEHX6DW99G","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}]]}
{"id":"01KR7EBQ2AVX0XB4PGTB1ZM3YR","timestamp":"2026-05-09T22:40:05.706635Z","op":"update","item_id":"01KQSE8TT79XC3KJGEHX6DW99G","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n","transaction_id":null}
{"id":"01KR7EBQ2BK8P6438QMB184A1Z","timestamp":"2026-05-09T22:40:05.707686Z","entity_type":"task","entity_id":"01KQSE8TT79XC3KJGEHX6DW99G","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KR7FEYP2YF4N9THQE3KX49V2","timestamp":"2026-05-09T22:59:20.386335Z","op":"update","item_id":"01KQSE8TT79XC3KJGEHX6DW99G","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffc580\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffc580\n+position_column: review\n+position_ordinal: '80'\n title: Resizable inspector — drag the left edge to change width (persisted per window)\n ---\n ## What\n","transaction_id":null}
{"id":"01KR7FEYP3N4HE8Z9MZ8Q57B4A","timestamp":"2026-05-09T22:59:20.387321Z","entity_type":"task","entity_id":"01KQSE8TT79XC3KJGEHX6DW99G","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffffc580\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffffc580\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}

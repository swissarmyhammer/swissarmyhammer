---
assignees:
- claude-code
depends_on:
- 01KNS10MMDVZG731XKM390C682
position_column: done
position_ordinal: ffffffffffffffffffffffffffffffa980
project: kanban-mcp
title: 'kanban-cli: implement commands/doctor.rs — KanbanDoctor diagnostic checks'
---
## What

Create `kanban-cli/src/commands/doctor.rs` — diagnostic checks for kanban setup. Model on shelltool's doctor using `DoctorRunner` trait.

Three checks: git repository, kanban in PATH, board initialized.

## Acceptance Criteria
- [x] `kanban-cli/src/commands/doctor.rs` exists
- [x] `run_doctor` prints a table of checks and returns exit code 0/1/2
- [x] `cargo check -p kanban-cli` passes

## Tests
- [x] Unit test: `KanbanDoctor::new()` starts with empty checks
- [x] Unit test: `check_git_repository` produces exactly 1 check
- [x] Unit test: `check_kanban_in_path` produces exactly 1 check
- [x] Unit test: `run_doctor(false)` returns 0, 1, or 2
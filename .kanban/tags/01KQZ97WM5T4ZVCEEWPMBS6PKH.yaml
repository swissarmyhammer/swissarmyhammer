color: 7c3aed
description: Per-operation end-to-end validation suites for the spatial-nav motion ops (up/down/left/right/first/last/drillIn/drillOut). Each task pins one operation's behavior across kernel, IPC, and React layers using the production App harness.
tag_name: motion-validation

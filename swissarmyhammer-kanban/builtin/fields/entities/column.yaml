name: column
icon: columns
search_display_field: name
commands:
  - id: ui.inspect
    name: "Inspect {{entity.type}}"
    context_menu: true
  - id: entity.paste
    name: "Paste {{entity.type}}"
    context_menu: true
    keys:
      cua: Mod+V
      vim: p
fields:
  - name
  - order

Never early exit or skip a test with an if condition.

This is an sneaky way to 'ignore' a test that will not show up in `cargo nextest` and is thus forbidden.

{% include "_partials/test-remediation" %}

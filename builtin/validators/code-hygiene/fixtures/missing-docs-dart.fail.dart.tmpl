// The failing fixture of the `missing-docs-dart` tool rule.
//
// It holds one undocumented public member of every kind the passing fixture
// documents. The tool must report each one. A tool upgrade that stops
// reporting a kind makes the doctor mark the rule unusable.

/// A documented public function, so only the members below are reported.
void documentedNeighbor() {}

class UndocumentedClass {
  void undocumentedMethod() {}
}

void undocumentedFunction() {}
